export default function isInteractive({stream ***REMOVED*** process.stdout} ***REMOVED*** {}) {
	return Boolean(
		stream && stream.isTTY &&
		process.env.TERM !***REMOVED******REMOVED*** 'dumb' &&
		!('CI' in process.env)
	);
}
