const copyProperty ***REMOVED*** (to, from, property, ignoreNonConfigurable) ***REMOVED***> {
	// `Function#length` should reflect the parameters of `to` not `from` since we keep its body.
	// `Function#prototype` is non-writable and non-configurable so can never be modified.
	if (property ***REMOVED******REMOVED******REMOVED*** 'length' || property ***REMOVED******REMOVED******REMOVED*** 'prototype') {
		return;
	}

	// `Function#arguments` and `Function#caller` should not be copied. They were reported to be present in `Reflect.ownKeys` for some devices in React Native (#41), so we explicitly ignore them here.
	if (property ***REMOVED******REMOVED******REMOVED*** 'arguments' || property ***REMOVED******REMOVED******REMOVED*** 'caller') {
		return;
	}

	const toDescriptor ***REMOVED*** Object.getOwnPropertyDescriptor(to, property);
	const fromDescriptor ***REMOVED*** Object.getOwnPropertyDescriptor(from, property);

	if (!canCopyProperty(toDescriptor, fromDescriptor) && ignoreNonConfigurable) {
		return;
	}

	Object.defineProperty(to, property, fromDescriptor);
};

// `Object.defineProperty()` throws if the property exists, is not configurable and either:
// - one its descriptors is changed
// - it is non-writable and its value is changed
const canCopyProperty ***REMOVED*** function (toDescriptor, fromDescriptor) {
	return toDescriptor ***REMOVED******REMOVED******REMOVED*** undefined || toDescriptor.configurable || (
		toDescriptor.writable ***REMOVED******REMOVED******REMOVED*** fromDescriptor.writable
		&& toDescriptor.enumerable ***REMOVED******REMOVED******REMOVED*** fromDescriptor.enumerable
		&& toDescriptor.configurable ***REMOVED******REMOVED******REMOVED*** fromDescriptor.configurable
		&& (toDescriptor.writable || toDescriptor.value ***REMOVED******REMOVED******REMOVED*** fromDescriptor.value)
	);
};

const changePrototype ***REMOVED*** (to, from) ***REMOVED***> {
	const fromPrototype ***REMOVED*** Object.getPrototypeOf(from);
	if (fromPrototype ***REMOVED******REMOVED******REMOVED*** Object.getPrototypeOf(to)) {
		return;
	}

	Object.setPrototypeOf(to, fromPrototype);
};

const wrappedToString ***REMOVED*** (withName, fromBody) ***REMOVED***> `/* Wrapped ${withName}*/\n${fromBody}`;

const toStringDescriptor ***REMOVED*** Object.getOwnPropertyDescriptor(Function.prototype, 'toString');
const toStringName ***REMOVED*** Object.getOwnPropertyDescriptor(Function.prototype.toString, 'name');

// We call `from.toString()` early (not lazily) to ensure `from` can be garbage collected.
// We use `bind()` instead of a closure for the same reason.
// Calling `from.toString()` early also allows caching it in case `to.toString()` is called several times.
const changeToString ***REMOVED*** (to, from, name) ***REMOVED***> {
	const withName ***REMOVED*** name ***REMOVED******REMOVED******REMOVED*** '' ? '' : `with ${name.trim()}() `;
	const newToString ***REMOVED*** wrappedToString.bind(null, withName, from.toString());
	// Ensure `to.toString.toString` is non-enumerable and has the same `same`
	Object.defineProperty(newToString, 'name', toStringName);
	const {writable, enumerable, configurable} ***REMOVED*** toStringDescriptor; // We destructue to avoid a potential `get` descriptor.
	Object.defineProperty(to, 'toString', {value: newToString, writable, enumerable, configurable});
};

export default function mimicFunction(to, from, {ignoreNonConfigurable ***REMOVED*** false} ***REMOVED*** {}) {
	const {name} ***REMOVED*** to;

	for (const property of Reflect.ownKeys(from)) {
		copyProperty(to, from, property, ignoreNonConfigurable);
	}

	changePrototype(to, from);
	changeToString(to, from, name);

	return to;
}
