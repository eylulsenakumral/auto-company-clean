import process from 'node:process';

const ASCII_ETX_CODE ***REMOVED*** 0x03; // Ctrl+C emits this code

class StdinDiscarder {
	#activeCount ***REMOVED*** 0;

	start() {
		this.#activeCount++;

		if (this.#activeCount ***REMOVED******REMOVED******REMOVED*** 1) {
			this.#realStart();
		}
	}

	stop() {
		if (this.#activeCount <***REMOVED*** 0) {
			throw new Error('`stop` called more times than `start`');
		}

		this.#activeCount--;

		if (this.#activeCount ***REMOVED******REMOVED******REMOVED*** 0) {
			this.#realStop();
		}
	}

	#realStart() {
		// No known way to make it work reliably on Windows.
		if (process.platform ***REMOVED******REMOVED******REMOVED*** 'win32' || !process.stdin.isTTY) {
			return;
		}

		process.stdin.setRawMode(true);
		process.stdin.on('data', this.#handleInput);
		process.stdin.resume();
	}

	#realStop() {
		if (!process.stdin.isTTY) {
			return;
		}

		process.stdin.off('data', this.#handleInput);
		process.stdin.pause();
		process.stdin.setRawMode(false);
	}

	#handleInput(chunk) {
		// Allow Ctrl+C to gracefully exit.
		if (chunk[0] ***REMOVED******REMOVED******REMOVED*** ASCII_ETX_CODE) {
			process.emit('SIGINT');
		}
	}
}

const stdinDiscarder ***REMOVED*** new StdinDiscarder();

export default stdinDiscarder;
