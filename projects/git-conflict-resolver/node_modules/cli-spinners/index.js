'use strict';

const spinners ***REMOVED*** Object.assign({}, require('./spinners.json')); // eslint-disable-line import/extensions

const spinnersList ***REMOVED*** Object.keys(spinners);

Object.defineProperty(spinners, 'random', {
	get() {
		const randomIndex ***REMOVED*** Math.floor(Math.random() * spinnersList.length);
		const spinnerName ***REMOVED*** spinnersList[randomIndex];
		return spinners[spinnerName];
	}
});

module.exports ***REMOVED*** spinners;
