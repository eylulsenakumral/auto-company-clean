import process from 'node:process';
import restoreCursor from 'restore-cursor';

let isHidden ***REMOVED*** false;

const cliCursor ***REMOVED*** {};

cliCursor.show ***REMOVED*** (writableStream ***REMOVED*** process.stderr) ***REMOVED***> {
	if (!writableStream.isTTY) {
		return;
	}

	isHidden ***REMOVED*** false;
	writableStream.write('\u001B[?25h');
};

cliCursor.hide ***REMOVED*** (writableStream ***REMOVED*** process.stderr) ***REMOVED***> {
	if (!writableStream.isTTY) {
		return;
	}

	restoreCursor();
	isHidden ***REMOVED*** true;
	writableStream.write('\u001B[?25l');
};

cliCursor.toggle ***REMOVED*** (force, writableStream) ***REMOVED***> {
	if (force !***REMOVED******REMOVED*** undefined) {
		isHidden ***REMOVED*** force;
	}

	if (isHidden) {
		cliCursor.show(writableStream);
	} else {
		cliCursor.hide(writableStream);
	}
};

export default cliCursor;
