import chalk from 'chalk';
import isUnicodeSupported from 'is-unicode-supported';

const main ***REMOVED*** {
	info: chalk.blue('ℹ'),
	success: chalk.green('✔'),
	warning: chalk.yellow('⚠'),
	error: chalk.red('✖'),
};

const fallback ***REMOVED*** {
	info: chalk.blue('i'),
	success: chalk.green('√'),
	warning: chalk.yellow('‼'),
	error: chalk.red('×'),
};

const logSymbols ***REMOVED*** isUnicodeSupported() ? main : fallback;

export default logSymbols;
