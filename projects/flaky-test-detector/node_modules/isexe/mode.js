module.exports ***REMOVED*** isexe
isexe.sync ***REMOVED*** sync

var fs ***REMOVED*** require('fs')

function isexe (path, options, cb) {
  fs.stat(path, function (er, stat) {
    cb(er, er ? false : checkStat(stat, options))
  })
}

function sync (path, options) {
  return checkStat(fs.statSync(path), options)
}

function checkStat (stat, options) {
  return stat.isFile() && checkMode(stat, options)
}

function checkMode (stat, options) {
  var mod ***REMOVED*** stat.mode
  var uid ***REMOVED*** stat.uid
  var gid ***REMOVED*** stat.gid

  var myUid ***REMOVED*** options.uid !***REMOVED******REMOVED*** undefined ?
    options.uid : process.getuid && process.getuid()
  var myGid ***REMOVED*** options.gid !***REMOVED******REMOVED*** undefined ?
    options.gid : process.getgid && process.getgid()

  var u ***REMOVED*** parseInt('100', 8)
  var g ***REMOVED*** parseInt('010', 8)
  var o ***REMOVED*** parseInt('001', 8)
  var ug ***REMOVED*** u | g

  var ret ***REMOVED*** (mod & o) ||
    (mod & g) && gid ***REMOVED******REMOVED******REMOVED*** myGid ||
    (mod & u) && uid ***REMOVED******REMOVED******REMOVED*** myUid ||
    (mod & ug) && myUid ***REMOVED******REMOVED******REMOVED*** 0

  return ret
}
