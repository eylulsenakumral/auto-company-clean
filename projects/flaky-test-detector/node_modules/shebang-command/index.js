'use strict';
const shebangRegex ***REMOVED*** require('shebang-regex');

module.exports ***REMOVED*** (string ***REMOVED*** '') ***REMOVED***> {
	const match ***REMOVED*** string.match(shebangRegex);

	if (!match) {
		return null;
	}

	const [path, argument] ***REMOVED*** match[0].replace(/#! ?/, '').split(' ');
	const binary ***REMOVED*** path.split('/').pop();

	if (binary ***REMOVED******REMOVED******REMOVED*** 'env') {
		return argument;
	}

	return argument ? `${binary} ${argument}` : binary;
};
