"use strict";
/**
 * @module LRUCache
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.LRUCache ***REMOVED*** void 0;
const diagnostics_channel_js_1 ***REMOVED*** require("./diagnostics-channel.js");
const perf_js_1 ***REMOVED*** require("./perf.js");
const hasSubscribers ***REMOVED*** () ***REMOVED***> diagnostics_channel_js_1.metrics.hasSubscribers || diagnostics_channel_js_1.tracing.hasSubscribers;
const warned ***REMOVED*** new Set();
/* c8 ignore start */
const PROCESS ***REMOVED*** (typeof process ***REMOVED******REMOVED******REMOVED*** 'object' && !!process ?
    process
    : {});
/* c8 ignore stop */
const emitWarning ***REMOVED*** (msg, type, code, fn) ***REMOVED***> {
    if (typeof PROCESS.emitWarning ***REMOVED******REMOVED******REMOVED*** 'function') {
        PROCESS.emitWarning(msg, type, code, fn);
    }
    else {
        //oxlint-disable-next-line no-console
        console.error(`[${code}] ${type}: ${msg}`);
    }
};
const shouldWarn ***REMOVED*** (code) ***REMOVED***> !warned.has(code);
const TYPE ***REMOVED*** Symbol('type');
const isPosInt ***REMOVED*** (n) ***REMOVED***> !!n && n ***REMOVED******REMOVED******REMOVED*** Math.floor(n) && n > 0 && isFinite(n);
// This is a little bit ridiculous, tbh.
// The maximum array length is 2^32-1 or thereabouts on most JS impls.
// And well before that point, you're caching the entire world, I mean,
// that's ~32GB of just integers for the next/prev links, plus whatever
// else to hold that many keys and values.  Just filling the memory with
// zeroes at init time is brutal when you get that big.
// But why not be complete?
// Maybe in the future, these limits will have expanded.
/* c8 ignore start */
const getUintArray ***REMOVED*** (max) ***REMOVED***> !isPosInt(max) ? null
    : max <***REMOVED*** Math.pow(2, 8) ? Uint8Array
        : max <***REMOVED*** Math.pow(2, 16) ? Uint16Array
            : max <***REMOVED*** Math.pow(2, 32) ? Uint32Array
                : max <***REMOVED*** Number.MAX_SAFE_INTEGER ? ZeroArray
                    : null;
/* c8 ignore stop */
class ZeroArray extends Array {
    constructor(size) {
        super(size);
        this.fill(0);
    }
}
class Stack {
    /* c8 ignore start - not sure why this is showing up uncovered?? */
    heap;
    /* c8 ignore stop */
    length;
    // private constructor
    static #constructing ***REMOVED*** false;
    static create(max) {
        const HeapCls ***REMOVED*** getUintArray(max);
        if (!HeapCls)
            return [];
        Stack.#constructing ***REMOVED*** true;
        const s ***REMOVED*** new Stack(max, HeapCls);
        Stack.#constructing ***REMOVED*** false;
        return s;
    }
    constructor(max, HeapCls) {
        /* c8 ignore start */
        if (!Stack.#constructing) {
            throw new TypeError('instantiate Stack using Stack.create(n)');
        }
        /* c8 ignore stop */
        this.heap ***REMOVED*** new HeapCls(max);
        this.length ***REMOVED*** 0;
    }
    push(n) {
        this.heap[this.length++] ***REMOVED*** n;
    }
    pop() {
        return this.heap[--this.length];
    }
}
/**
 * Default export, the thing you're using this module to get.
 *
 * The `K` and `V` types define the key and value types, respectively. The
 * optional `FC` type defines the type of the `context` object passed to
 * `cache.fetch()` and `cache.memo()`.
 *
 * Keys and values **must not** be `null` or `undefined`.
 *
 * All properties from the options object (with the exception of `max`,
 * `maxSize`, `fetchMethod`, `memoMethod`, `dispose` and `disposeAfter`) are
 * added as normal public members. (The listed options are read-only getters.)
 *
 * Changing any of these will alter the defaults for subsequent method calls.
 */
class LRUCache {
    // options that cannot be changed without disaster
    #max;
    #maxSize;
    #dispose;
    #onInsert;
    #disposeAfter;
    #fetchMethod;
    #memoMethod;
    #perf;
    /**
     * {@link LRUCache.OptionsBase.perf}
     */
    get perf() {
        return this.#perf;
    }
    /**
     * {@link LRUCache.OptionsBase.ttl}
     */
    ttl;
    /**
     * {@link LRUCache.OptionsBase.ttlResolution}
     */
    ttlResolution;
    /**
     * {@link LRUCache.OptionsBase.ttlAutopurge}
     */
    ttlAutopurge;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnGet}
     */
    updateAgeOnGet;
    /**
     * {@link LRUCache.OptionsBase.updateAgeOnHas}
     */
    updateAgeOnHas;
    /**
     * {@link LRUCache.OptionsBase.allowStale}
     */
    allowStale;
    /**
     * {@link LRUCache.OptionsBase.noDisposeOnSet}
     */
    noDisposeOnSet;
    /**
     * {@link LRUCache.OptionsBase.noUpdateTTL}
     */
    noUpdateTTL;
    /**
     * {@link LRUCache.OptionsBase.maxEntrySize}
     */
    maxEntrySize;
    /**
     * {@link LRUCache.OptionsBase.sizeCalculation}
     */
    sizeCalculation;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnFetchRejection}
     */
    noDeleteOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.noDeleteOnStaleGet}
     */
    noDeleteOnStaleGet;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchAbort}
     */
    allowStaleOnFetchAbort;
    /**
     * {@link LRUCache.OptionsBase.allowStaleOnFetchRejection}
     */
    allowStaleOnFetchRejection;
    /**
     * {@link LRUCache.OptionsBase.ignoreFetchAbort}
     */
    ignoreFetchAbort;
    /** {@link LRUCache.OptionsBase.backgroundFetchSize} */
    backgroundFetchSize;
    // computed properties
    #size;
    #calculatedSize;
    #keyMap;
    #keyList;
    #valList;
    #next;
    #prev;
    #head;
    #tail;
    #free;
    #disposed;
    #sizes;
    #starts;
    #ttls;
    #autopurgeTimers;
    #hasDispose;
    #hasFetchMethod;
    #hasDisposeAfter;
    #hasOnInsert;
    /**
     * Do not call this method unless you need to inspect the
     * inner workings of the cache.  If anything returned by this
     * object is modified in any way, strange breakage may occur.
     *
     * These fields are private for a reason!
     *
     * @internal
     */
    static unsafeExposeInternals(c) {
        return {
            // properties
            starts: c.#starts,
            ttls: c.#ttls,
            autopurgeTimers: c.#autopurgeTimers,
            sizes: c.#sizes,
            keyMap: c.#keyMap,
            keyList: c.#keyList,
            valList: c.#valList,
            next: c.#next,
            prev: c.#prev,
            get head() {
                return c.#head;
            },
            get tail() {
                return c.#tail;
            },
            free: c.#free,
            // methods
            isBackgroundFetch: (p) ***REMOVED***> c.#isBackgroundFetch(p),
            backgroundFetch: (k, index, options, context) ***REMOVED***> c.#backgroundFetch(k, index, options, context),
            moveToTail: (index) ***REMOVED***> c.#moveToTail(index),
            indexes: (options) ***REMOVED***> c.#indexes(options),
            rindexes: (options) ***REMOVED***> c.#rindexes(options),
            isStale: (index) ***REMOVED***> c.#isStale(index),
        };
    }
    // Protected read-only members
    /**
     * {@link LRUCache.OptionsBase.max} (read-only)
     */
    get max() {
        return this.#max;
    }
    /**
     * {@link LRUCache.OptionsBase.maxSize} (read-only)
     */
    get maxSize() {
        return this.#maxSize;
    }
    /**
     * The total computed size of items in the cache (read-only)
     */
    get calculatedSize() {
        return this.#calculatedSize;
    }
    /**
     * The number of items stored in the cache (read-only)
     */
    get size() {
        return this.#size;
    }
    /**
     * {@link LRUCache.OptionsBase.fetchMethod} (read-only)
     */
    get fetchMethod() {
        return this.#fetchMethod;
    }
    get memoMethod() {
        return this.#memoMethod;
    }
    /**
     * {@link LRUCache.OptionsBase.dispose} (read-only)
     */
    get dispose() {
        return this.#dispose;
    }
    /**
     * {@link LRUCache.OptionsBase.onInsert} (read-only)
     */
    get onInsert() {
        return this.#onInsert;
    }
    /**
     * {@link LRUCache.OptionsBase.disposeAfter} (read-only)
     */
    get disposeAfter() {
        return this.#disposeAfter;
    }
    constructor(options) {
        const { max ***REMOVED*** 0, ttl, ttlResolution ***REMOVED*** 1, ttlAutopurge, updateAgeOnGet, updateAgeOnHas, allowStale, dispose, onInsert, disposeAfter, noDisposeOnSet, noUpdateTTL, maxSize ***REMOVED*** 0, maxEntrySize ***REMOVED*** 0, sizeCalculation, fetchMethod, memoMethod, noDeleteOnFetchRejection, noDeleteOnStaleGet, allowStaleOnFetchRejection, allowStaleOnFetchAbort, ignoreFetchAbort, backgroundFetchSize ***REMOVED*** 1, perf, } ***REMOVED*** options;
        this.backgroundFetchSize ***REMOVED*** backgroundFetchSize;
        if (perf !***REMOVED******REMOVED*** undefined) {
            if (typeof perf?.now !***REMOVED******REMOVED*** 'function') {
                throw new TypeError('perf option must have a now() method if specified');
            }
        }
        this.#perf ***REMOVED*** perf ?? perf_js_1.defaultPerf;
        if (max !***REMOVED******REMOVED*** 0 && !isPosInt(max)) {
            throw new TypeError('max option must be a nonnegative integer');
        }
        const UintArray ***REMOVED*** max ? getUintArray(max) : Array;
        if (!UintArray) {
            throw new Error('invalid max value: ' + max);
        }
        this.#max ***REMOVED*** max;
        this.#maxSize ***REMOVED*** maxSize;
        this.maxEntrySize ***REMOVED*** maxEntrySize || this.#maxSize;
        this.sizeCalculation ***REMOVED*** sizeCalculation;
        if (this.sizeCalculation) {
            if (!this.#maxSize && !this.maxEntrySize) {
                throw new TypeError('cannot set sizeCalculation without setting maxSize or maxEntrySize');
            }
            if (typeof this.sizeCalculation !***REMOVED******REMOVED*** 'function') {
                throw new TypeError('sizeCalculation set to non-function');
            }
        }
        if (memoMethod !***REMOVED******REMOVED*** undefined && typeof memoMethod !***REMOVED******REMOVED*** 'function') {
            throw new TypeError('memoMethod must be a function if defined');
        }
        this.#memoMethod ***REMOVED*** memoMethod;
        if (fetchMethod !***REMOVED******REMOVED*** undefined && typeof fetchMethod !***REMOVED******REMOVED*** 'function') {
            throw new TypeError('fetchMethod must be a function if specified');
        }
        this.#fetchMethod ***REMOVED*** fetchMethod;
        this.#hasFetchMethod ***REMOVED*** !!fetchMethod;
        this.#keyMap ***REMOVED*** new Map();
        this.#keyList ***REMOVED*** Array.from({ length: max }).fill(undefined);
        this.#valList ***REMOVED*** Array.from({ length: max }).fill(undefined);
        this.#next ***REMOVED*** new UintArray(max);
        this.#prev ***REMOVED*** new UintArray(max);
        this.#head ***REMOVED*** 0;
        this.#tail ***REMOVED*** 0;
        this.#free ***REMOVED*** Stack.create(max);
        this.#size ***REMOVED*** 0;
        this.#calculatedSize ***REMOVED*** 0;
        if (typeof dispose ***REMOVED******REMOVED******REMOVED*** 'function') {
            this.#dispose ***REMOVED*** dispose;
        }
        if (typeof onInsert ***REMOVED******REMOVED******REMOVED*** 'function') {
            this.#onInsert ***REMOVED*** onInsert;
        }
        if (typeof disposeAfter ***REMOVED******REMOVED******REMOVED*** 'function') {
            this.#disposeAfter ***REMOVED*** disposeAfter;
            this.#disposed ***REMOVED*** [];
        }
        else {
            this.#disposeAfter ***REMOVED*** undefined;
            this.#disposed ***REMOVED*** undefined;
        }
        this.#hasDispose ***REMOVED*** !!this.#dispose;
        this.#hasOnInsert ***REMOVED*** !!this.#onInsert;
        this.#hasDisposeAfter ***REMOVED*** !!this.#disposeAfter;
        this.noDisposeOnSet ***REMOVED*** !!noDisposeOnSet;
        this.noUpdateTTL ***REMOVED*** !!noUpdateTTL;
        this.noDeleteOnFetchRejection ***REMOVED*** !!noDeleteOnFetchRejection;
        this.allowStaleOnFetchRejection ***REMOVED*** !!allowStaleOnFetchRejection;
        this.allowStaleOnFetchAbort ***REMOVED*** !!allowStaleOnFetchAbort;
        this.ignoreFetchAbort ***REMOVED*** !!ignoreFetchAbort;
        // NB: maxEntrySize is set to maxSize if it's set
        if (this.maxEntrySize !***REMOVED******REMOVED*** 0) {
            if (this.#maxSize !***REMOVED******REMOVED*** 0) {
                if (!isPosInt(this.#maxSize)) {
                    throw new TypeError('maxSize must be a positive integer if specified');
                }
            }
            if (!isPosInt(this.maxEntrySize)) {
                throw new TypeError('maxEntrySize must be a positive integer if specified');
            }
            this.#initializeSizeTracking();
        }
        this.allowStale ***REMOVED*** !!allowStale;
        this.noDeleteOnStaleGet ***REMOVED*** !!noDeleteOnStaleGet;
        this.updateAgeOnGet ***REMOVED*** !!updateAgeOnGet;
        this.updateAgeOnHas ***REMOVED*** !!updateAgeOnHas;
        this.ttlResolution ***REMOVED***
            isPosInt(ttlResolution) || ttlResolution ***REMOVED******REMOVED******REMOVED*** 0 ? ttlResolution : 1;
        this.ttlAutopurge ***REMOVED*** !!ttlAutopurge;
        this.ttl ***REMOVED*** ttl || 0;
        if (this.ttl) {
            if (!isPosInt(this.ttl)) {
                throw new TypeError('ttl must be a positive integer if specified');
            }
            this.#initializeTTLTracking();
        }
        // do not allow completely unbounded caches
        if (this.#max ***REMOVED******REMOVED******REMOVED*** 0 && this.ttl ***REMOVED******REMOVED******REMOVED*** 0 && this.#maxSize ***REMOVED******REMOVED******REMOVED*** 0) {
            throw new TypeError('At least one of max, maxSize, or ttl is required');
        }
        if (!this.ttlAutopurge && !this.#max && !this.#maxSize) {
            const code ***REMOVED*** 'LRU_CACHE_UNBOUNDED';
            if (shouldWarn(code)) {
                warned.add(code);
                const msg ***REMOVED*** 'TTL caching without ttlAutopurge, max, or maxSize can ' +
                    'result in unbounded memory consumption.';
                emitWarning(msg, 'UnboundedCacheWarning', code, LRUCache);
            }
        }
    }
    /**
     * Return the number of ms left in the item's TTL. If item is not in cache,
     * returns `0`. Returns `Infinity` if item is in cache without a defined TTL.
     */
    getRemainingTTL(key) {
        return this.#keyMap.has(key) ? Infinity : 0;
    }
    #initializeTTLTracking() {
        const ttls ***REMOVED*** new ZeroArray(this.#max);
        const starts ***REMOVED*** new ZeroArray(this.#max);
        this.#ttls ***REMOVED*** ttls;
        this.#starts ***REMOVED*** starts;
        const purgeTimers ***REMOVED*** this.ttlAutopurge ?
            Array.from({
                length: this.#max,
            })
            : undefined;
        this.#autopurgeTimers ***REMOVED*** purgeTimers;
        this.#setItemTTL ***REMOVED*** (index, ttl, start ***REMOVED*** this.#perf.now()) ***REMOVED***> {
            starts[index] ***REMOVED*** ttl !***REMOVED******REMOVED*** 0 ? start : 0;
            ttls[index] ***REMOVED*** ttl;
            setPurgetTimer(index, ttl);
        };
        this.#updateItemAge ***REMOVED*** index ***REMOVED***> {
            starts[index] ***REMOVED*** ttls[index] !***REMOVED******REMOVED*** 0 ? this.#perf.now() : 0;
            setPurgetTimer(index, ttls[index]);
        };
        // clear out the purge timer if we're setting TTL to 0, and
        // previously had a ttl purge timer running, so it doesn't
        // fire unnecessarily. Don't need to do this if we're not doing
        // autopurge.
        const setPurgetTimer ***REMOVED*** !this.ttlAutopurge ?
            () ***REMOVED***> { }
            : (index, ttl) ***REMOVED***> {
                if (purgeTimers?.[index]) {
                    clearTimeout(purgeTimers[index]);
                    purgeTimers[index] ***REMOVED*** undefined;
                }
                if (ttl && ttl !***REMOVED******REMOVED*** 0 && purgeTimers) {
                    const t ***REMOVED*** setTimeout(() ***REMOVED***> {
                        if (this.#isStale(index)) {
                            this.#delete(this.#keyList[index], 'expire');
                        }
                    }, ttl + 1);
                    // unref() not supported on all platforms
                    /* c8 ignore start */
                    if (t.unref) {
                        t.unref();
                    }
                    /* c8 ignore stop */
                    purgeTimers[index] ***REMOVED*** t;
                }
            };
        this.#statusTTL ***REMOVED*** (status, index) ***REMOVED***> {
            if (ttls[index]) {
                const ttl ***REMOVED*** ttls[index];
                const start ***REMOVED*** starts[index];
                /* c8 ignore start */
                if (!ttl || !start) {
                    return;
                }
                /* c8 ignore stop */
                status.ttl ***REMOVED*** ttl;
                status.start ***REMOVED*** start;
                status.now ***REMOVED*** cachedNow || getNow();
                const age ***REMOVED*** status.now - start;
                status.remainingTTL ***REMOVED*** ttl - age;
            }
        };
        // debounce calls to perf.now() to 1s so we're not hitting
        // that costly call repeatedly.
        let cachedNow ***REMOVED*** 0;
        const getNow ***REMOVED*** () ***REMOVED***> {
            const n ***REMOVED*** this.#perf.now();
            if (this.ttlResolution > 0) {
                cachedNow ***REMOVED*** n;
                const t ***REMOVED*** setTimeout(() ***REMOVED***> (cachedNow ***REMOVED*** 0), this.ttlResolution);
                // not available on all platforms
                /* c8 ignore start */
                if (t.unref) {
                    t.unref();
                }
                /* c8 ignore stop */
            }
            return n;
        };
        this.getRemainingTTL ***REMOVED*** key ***REMOVED***> {
            const index ***REMOVED*** this.#keyMap.get(key);
            if (index ***REMOVED******REMOVED******REMOVED*** undefined) {
                return 0;
            }
            const ttl ***REMOVED*** ttls[index];
            const start ***REMOVED*** starts[index];
            if (!ttl || !start) {
                return Infinity;
            }
            const age ***REMOVED*** (cachedNow || getNow()) - start;
            return ttl - age;
        };
        this.#isStale ***REMOVED*** index ***REMOVED***> {
            const s ***REMOVED*** starts[index];
            const t ***REMOVED*** ttls[index];
            return !!t && !!s && (cachedNow || getNow()) - s > t;
        };
    }
    // conditionally set private methods related to TTL
    #updateItemAge ***REMOVED*** () ***REMOVED***> { };
    #statusTTL ***REMOVED*** () ***REMOVED***> { };
    #setItemTTL ***REMOVED*** () ***REMOVED***> { };
    /* c8 ignore stop */
    #isStale ***REMOVED*** () ***REMOVED***> false;
    #initializeSizeTracking() {
        const sizes ***REMOVED*** new ZeroArray(this.#max);
        this.#calculatedSize ***REMOVED*** 0;
        this.#sizes ***REMOVED*** sizes;
        this.#removeItemSize ***REMOVED*** index ***REMOVED***> {
            this.#calculatedSize -***REMOVED*** sizes[index];
            sizes[index] ***REMOVED*** 0;
        };
        this.#requireSize ***REMOVED*** (k, v, size, sizeCalculation) ***REMOVED***> {
            if (!isPosInt(size)) {
                // provisionally accept background fetches.
                // actual value size will be checked when they return.
                if (this.#isBackgroundFetch(v)) {
                    // NB: this cannot occur if v.__staleWhileFetching is set,
                    // because in that case, it would take on the size of the
                    // existing entry that it temporarily replaces.
                    return this.backgroundFetchSize;
                }
                if (sizeCalculation) {
                    if (typeof sizeCalculation !***REMOVED******REMOVED*** 'function') {
                        throw new TypeError('sizeCalculation must be a function');
                    }
                    size ***REMOVED*** sizeCalculation(v, k);
                    if (!isPosInt(size)) {
                        throw new TypeError('sizeCalculation return invalid (expect positive integer)');
                    }
                }
                else {
                    throw new TypeError('invalid size value (must be positive integer). ' +
                        'When maxSize or maxEntrySize is used, sizeCalculation ' +
                        'or size must be set.');
                }
            }
            return size;
        };
        this.#addItemSize ***REMOVED*** (index, size, status) ***REMOVED***> {
            sizes[index] ***REMOVED*** size;
            if (this.#maxSize) {
                const maxSize ***REMOVED*** this.#maxSize - sizes[index];
                while (this.#calculatedSize > maxSize) {
                    this.#evict(true);
                }
            }
            this.#calculatedSize +***REMOVED*** sizes[index];
            if (status) {
                status.entrySize ***REMOVED*** size;
                status.totalCalculatedSize ***REMOVED*** this.#calculatedSize;
            }
        };
    }
    #removeItemSize ***REMOVED*** _i ***REMOVED***> { };
    #addItemSize ***REMOVED*** (_i, _s, _st) ***REMOVED***> { };
    #requireSize ***REMOVED*** (_k, _v, size, sizeCalculation) ***REMOVED***> {
        if (size || sizeCalculation) {
            throw new TypeError('cannot set size without setting maxSize or maxEntrySize on cache');
        }
        return 0;
    };
    *#indexes({ allowStale ***REMOVED*** this.allowStale } ***REMOVED*** {}) {
        if (this.#size) {
            for (let i ***REMOVED*** this.#tail; this.#isValidIndex(i);) {
                if (allowStale || !this.#isStale(i)) {
                    yield i;
                }
                if (i ***REMOVED******REMOVED******REMOVED*** this.#head) {
                    break;
                }
                else {
                    i ***REMOVED*** this.#prev[i];
                }
            }
        }
    }
    *#rindexes({ allowStale ***REMOVED*** this.allowStale } ***REMOVED*** {}) {
        if (this.#size) {
            for (let i ***REMOVED*** this.#head; this.#isValidIndex(i);) {
                if (allowStale || !this.#isStale(i)) {
                    yield i;
                }
                if (i ***REMOVED******REMOVED******REMOVED*** this.#tail) {
                    break;
                }
                else {
                    i ***REMOVED*** this.#next[i];
                }
            }
        }
    }
    #isValidIndex(index) {
        return (index !***REMOVED******REMOVED*** undefined &&
            this.#keyMap.get(this.#keyList[index]) ***REMOVED******REMOVED******REMOVED*** index);
    }
    /**
     * Return a generator yielding `[key, value]` pairs,
     * in order from most recently used to least recently used.
     */
    *entries() {
        for (const i of this.#indexes()) {
            if (this.#valList[i] !***REMOVED******REMOVED*** undefined &&
                this.#keyList[i] !***REMOVED******REMOVED*** undefined &&
                !this.#isBackgroundFetch(this.#valList[i])) {
                yield [this.#keyList[i], this.#valList[i]];
            }
        }
    }
    /**
     * Inverse order version of {@link LRUCache.entries}
     *
     * Return a generator yielding `[key, value]` pairs,
     * in order from least recently used to most recently used.
     */
    *rentries() {
        for (const i of this.#rindexes()) {
            if (this.#valList[i] !***REMOVED******REMOVED*** undefined &&
                this.#keyList[i] !***REMOVED******REMOVED*** undefined &&
                !this.#isBackgroundFetch(this.#valList[i])) {
                yield [this.#keyList[i], this.#valList[i]];
            }
        }
    }
    /**
     * Return a generator yielding the keys in the cache,
     * in order from most recently used to least recently used.
     */
    *keys() {
        for (const i of this.#indexes()) {
            const k ***REMOVED*** this.#keyList[i];
            if (k !***REMOVED******REMOVED*** undefined && !this.#isBackgroundFetch(this.#valList[i])) {
                yield k;
            }
        }
    }
    /**
     * Inverse order version of {@link LRUCache.keys}
     *
     * Return a generator yielding the keys in the cache,
     * in order from least recently used to most recently used.
     */
    *rkeys() {
        for (const i of this.#rindexes()) {
            const k ***REMOVED*** this.#keyList[i];
            if (k !***REMOVED******REMOVED*** undefined && !this.#isBackgroundFetch(this.#valList[i])) {
                yield k;
            }
        }
    }
    /**
     * Return a generator yielding the values in the cache,
     * in order from most recently used to least recently used.
     */
    *values() {
        for (const i of this.#indexes()) {
            const v ***REMOVED*** this.#valList[i];
            if (v !***REMOVED******REMOVED*** undefined && !this.#isBackgroundFetch(this.#valList[i])) {
                yield this.#valList[i];
            }
        }
    }
    /**
     * Inverse order version of {@link LRUCache.values}
     *
     * Return a generator yielding the values in the cache,
     * in order from least recently used to most recently used.
     */
    *rvalues() {
        for (const i of this.#rindexes()) {
            const v ***REMOVED*** this.#valList[i];
            if (v !***REMOVED******REMOVED*** undefined && !this.#isBackgroundFetch(this.#valList[i])) {
                yield this.#valList[i];
            }
        }
    }
    /**
     * Iterating over the cache itself yields the same results as
     * {@link LRUCache.entries}
     */
    [Symbol.iterator]() {
        return this.entries();
    }
    /**
     * A String value that is used in the creation of the default string
     * description of an object. Called by the built-in method
     * `Object.prototype.toString`.
     */
    [Symbol.toStringTag] ***REMOVED*** 'LRUCache';
    /**
     * Find a value for which the supplied fn method returns a truthy value,
     * similar to `Array.find()`. fn is called as `fn(value, key, cache)`.
     */
    find(fn, getOptions ***REMOVED*** {}) {
        for (const i of this.#indexes()) {
            const v ***REMOVED*** this.#valList[i];
            const value ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
            if (value ***REMOVED******REMOVED******REMOVED*** undefined)
                continue;
            if (fn(value, this.#keyList[i], this)) {
                return this.#get(this.#keyList[i], getOptions);
            }
        }
    }
    /**
     * Call the supplied function on each item in the cache, in order from most
     * recently used to least recently used.
     *
     * `fn` is called as `fn(value, key, cache)`.
     *
     * If `thisp` is provided, function will be called in the `this`-context of
     * the provided object, or the cache if no `thisp` object is provided.
     *
     * Does not update age or recenty of use, or iterate over stale values.
     */
    forEach(fn, thisp ***REMOVED*** this) {
        for (const i of this.#indexes()) {
            const v ***REMOVED*** this.#valList[i];
            const value ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
            if (value ***REMOVED******REMOVED******REMOVED*** undefined)
                continue;
            fn.call(thisp, value, this.#keyList[i], this);
        }
    }
    /**
     * The same as {@link LRUCache.forEach} but items are iterated over in
     * reverse order.  (ie, less recently used items are iterated over first.)
     */
    rforEach(fn, thisp ***REMOVED*** this) {
        for (const i of this.#rindexes()) {
            const v ***REMOVED*** this.#valList[i];
            const value ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
            if (value ***REMOVED******REMOVED******REMOVED*** undefined)
                continue;
            fn.call(thisp, value, this.#keyList[i], this);
        }
    }
    /**
     * Delete any stale entries. Returns true if anything was removed,
     * false otherwise.
     */
    purgeStale() {
        let deleted ***REMOVED*** false;
        for (const i of this.#rindexes({ allowStale: true })) {
            if (this.#isStale(i)) {
                this.#delete(this.#keyList[i], 'expire');
                deleted ***REMOVED*** true;
            }
        }
        return deleted;
    }
    /**
     * Get the extended info about a given entry, to get its value, size, and
     * TTL info simultaneously. Returns `undefined` if the key is not present.
     *
     * Unlike {@link LRUCache#dump}, which is designed to be portable and survive
     * serialization, the `start` value is always the current timestamp, and the
     * `ttl` is a calculated remaining time to live (negative if expired).
     *
     * Always returns stale values, if their info is found in the cache, so be
     * sure to check for expirations (ie, a negative {@link LRUCache.Entry#ttl})
     * if relevant.
     */
    info(key) {
        const i ***REMOVED*** this.#keyMap.get(key);
        if (i ***REMOVED******REMOVED******REMOVED*** undefined)
            return undefined;
        const v ***REMOVED*** this.#valList[i];
        /* c8 ignore start - this isn't tested for the info function,
         * but it's the same logic as found in other places. */
        const value ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
        if (value ***REMOVED******REMOVED******REMOVED*** undefined)
            return undefined;
        /* c8 ignore stop */
        const entry ***REMOVED*** { value };
        if (this.#ttls && this.#starts) {
            const ttl ***REMOVED*** this.#ttls[i];
            const start ***REMOVED*** this.#starts[i];
            if (ttl && start) {
                const remain ***REMOVED*** ttl - (this.#perf.now() - start);
                entry.ttl ***REMOVED*** remain;
                entry.start ***REMOVED*** Date.now();
            }
        }
        if (this.#sizes) {
            entry.size ***REMOVED*** this.#sizes[i];
        }
        return entry;
    }
    /**
     * Return an array of [key, {@link LRUCache.Entry}] tuples which can be
     * passed to {@link LRUCache#load}.
     *
     * The `start` fields are calculated relative to a portable `Date.now()`
     * timestamp, even if `performance.now()` is available.
     *
     * Stale entries are always included in the `dump`, even if
     * {@link LRUCache.OptionsBase.allowStale} is false.
     *
     * Note: this returns an actual array, not a generator, so it can be more
     * easily passed around.
     */
    dump() {
        const arr ***REMOVED*** [];
        for (const i of this.#indexes({ allowStale: true })) {
            const key ***REMOVED*** this.#keyList[i];
            const v ***REMOVED*** this.#valList[i];
            const value ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
            if (value ***REMOVED******REMOVED******REMOVED*** undefined || key ***REMOVED******REMOVED******REMOVED*** undefined)
                continue;
            const entry ***REMOVED*** { value };
            if (this.#ttls && this.#starts) {
                entry.ttl ***REMOVED*** this.#ttls[i];
                // always dump the start relative to a portable timestamp
                // it's ok for this to be a bit slow, it's a rare operation.
                const age ***REMOVED*** this.#perf.now() - this.#starts[i];
                entry.start ***REMOVED*** Math.floor(Date.now() - age);
            }
            if (this.#sizes) {
                entry.size ***REMOVED*** this.#sizes[i];
            }
            arr.unshift([key, entry]);
        }
        return arr;
    }
    /**
     * Reset the cache and load in the items in entries in the order listed.
     *
     * The shape of the resulting cache may be different if the same options are
     * not used in both caches.
     *
     * The `start` fields are assumed to be calculated relative to a portable
     * `Date.now()` timestamp, even if `performance.now()` is available.
     */
    load(arr) {
        this.clear();
        for (const [key, entry] of arr) {
            if (entry.start) {
                // entry.start is a portable timestamp, but we may be using
                // node's performance.now(), so calculate the offset, so that
                // we get the intended remaining TTL, no matter how long it's
                // been on ice.
                //
                // it's ok for this to be a bit slow, it's a rare operation.
                const age ***REMOVED*** Date.now() - entry.start;
                entry.start ***REMOVED*** this.#perf.now() - age;
            }
            this.#set(key, entry.value, entry);
        }
    }
    /**
     * Add a value to the cache.
     *
     * Note: if `undefined` is specified as a value, this is an alias for
     * {@link LRUCache#delete}
     *
     * Fields on the {@link LRUCache.SetOptions} options param will override
     * their corresponding values in the constructor options for the scope
     * of this single `set()` operation.
     *
     * If `start` is provided, then that will set the effective start
     * time for the TTL calculation. Note that this must be a previous
     * value of `performance.now()` if supported, or a previous value of
     * `Date.now()` if not.
     *
     * Options object may also include `size`, which will prevent
     * calling the `sizeCalculation` function and just use the specified
     * number if it is a positive integer, and `noDisposeOnSet` which
     * will prevent calling a `dispose` function in the case of
     * overwrites.
     *
     * If the `size` (or return value of `sizeCalculation`) for a given
     * entry is greater than `maxEntrySize`, then the item will not be
     * added to the cache.
     *
     * Will update the recency of the entry.
     *
     * If the value is `undefined`, then this is an alias for
     * `cache.delete(key)`. `undefined` is never stored in the cache.
     */
    set(k, v, setOptions ***REMOVED*** {}) {
        const { status ***REMOVED*** diagnostics_channel_js_1.metrics.hasSubscribers ? {} : undefined } ***REMOVED*** setOptions;
        setOptions.status ***REMOVED*** status;
        if (status) {
            status.op ***REMOVED*** 'set';
            status.key ***REMOVED*** k;
            if (v !***REMOVED******REMOVED*** undefined)
                status.value ***REMOVED*** v;
            status.cache ***REMOVED*** this;
        }
        const result ***REMOVED*** this.#set(k, v, setOptions);
        if (status && diagnostics_channel_js_1.metrics.hasSubscribers) {
            diagnostics_channel_js_1.metrics.publish(status);
        }
        return result;
    }
    #set(k, v, setOptions, bf) {
        const { ttl ***REMOVED*** this.ttl, start, noDisposeOnSet ***REMOVED*** this.noDisposeOnSet, sizeCalculation ***REMOVED*** this.sizeCalculation, status, } ***REMOVED*** setOptions;
        const isBF ***REMOVED*** this.#isBackgroundFetch(v);
        if (v ***REMOVED******REMOVED******REMOVED*** undefined) {
            if (status)
                status.set ***REMOVED*** 'deleted';
            this.delete(k);
            return this;
        }
        let { noUpdateTTL ***REMOVED*** this.noUpdateTTL } ***REMOVED*** setOptions;
        if (status && !isBF)
            status.value ***REMOVED*** v;
        const size ***REMOVED*** this.#requireSize(k, v, setOptions.size || 0, sizeCalculation, status);
        // if the item doesn't fit, don't do anything
        // NB: maxEntrySize set to maxSize by default
        if (this.maxEntrySize && size > this.maxEntrySize) {
            // have to delete, in case something is there already.
            this.#delete(k, 'set');
            if (status) {
                status.set ***REMOVED*** 'miss';
                status.maxEntrySizeExceeded ***REMOVED*** true;
            }
            return this;
        }
        let index ***REMOVED*** this.#size ***REMOVED******REMOVED******REMOVED*** 0 ? undefined : this.#keyMap.get(k);
        if (index ***REMOVED******REMOVED******REMOVED*** undefined) {
            // addition
            index ***REMOVED*** (this.#size ***REMOVED******REMOVED******REMOVED*** 0 ? this.#tail
                : this.#free.length !***REMOVED******REMOVED*** 0 ? this.#free.pop()
                    : this.#size ***REMOVED******REMOVED******REMOVED*** this.#max ? this.#evict(false)
                        : this.#size);
            this.#keyList[index] ***REMOVED*** k;
            this.#valList[index] ***REMOVED*** v;
            this.#keyMap.set(k, index);
            this.#next[this.#tail] ***REMOVED*** index;
            this.#prev[index] ***REMOVED*** this.#tail;
            this.#tail ***REMOVED*** index;
            this.#size++;
            this.#addItemSize(index, size, status);
            if (status)
                status.set ***REMOVED*** 'add';
            noUpdateTTL ***REMOVED*** false;
            if (this.#hasOnInsert && !isBF) {
                this.#onInsert?.(v, k, 'add');
            }
        }
        else {
            // update
            // might be updating a background fetch!
            this.#moveToTail(index);
            const oldVal ***REMOVED*** this.#valList[index];
            if (v !***REMOVED******REMOVED*** oldVal) {
                if (!noDisposeOnSet) {
                    if (this.#isBackgroundFetch(oldVal)) {
                        if (oldVal !***REMOVED******REMOVED*** bf) {
                            // setting over a background fetch, not merely resolving it.
                            oldVal.__abortController.abort(new Error('replaced'));
                        }
                        const { __staleWhileFetching: s } ***REMOVED*** oldVal;
                        if (s !***REMOVED******REMOVED*** undefined && s !***REMOVED******REMOVED*** v) {
                            if (this.#hasDispose) {
                                this.#dispose?.(s, k, 'set');
                            }
                            if (this.#hasDisposeAfter) {
                                this.#disposed?.push([s, k, 'set']);
                            }
                        }
                    }
                    else {
                        if (this.#hasDispose) {
                            this.#dispose?.(oldVal, k, 'set');
                        }
                        if (this.#hasDisposeAfter) {
                            this.#disposed?.push([oldVal, k, 'set']);
                        }
                    }
                }
                this.#removeItemSize(index);
                this.#addItemSize(index, size, status);
                this.#valList[index] ***REMOVED*** v;
                if (!isBF) {
                    const oldValue ***REMOVED*** oldVal && this.#isBackgroundFetch(oldVal) ?
                        oldVal.__staleWhileFetching
                        : oldVal;
                    const setType ***REMOVED*** oldValue ***REMOVED******REMOVED******REMOVED*** undefined ? 'add'
                        : v !***REMOVED******REMOVED*** oldValue ? 'replace'
                            : 'update';
                    if (status) {
                        status.set ***REMOVED*** setType;
                        if (oldValue !***REMOVED******REMOVED*** undefined)
                            status.oldValue ***REMOVED*** oldValue;
                    }
                    if (this.#hasOnInsert) {
                        this.onInsert?.(v, k, setType);
                    }
                }
            }
            else if (!isBF) {
                if (status) {
                    status.set ***REMOVED*** 'update';
                }
                if (this.#hasOnInsert) {
                    this.onInsert?.(v, k, 'update');
                }
            }
        }
        if (ttl !***REMOVED******REMOVED*** 0 && !this.#ttls) {
            this.#initializeTTLTracking();
        }
        if (this.#ttls) {
            if (!noUpdateTTL) {
                this.#setItemTTL(index, ttl, start);
            }
            if (status)
                this.#statusTTL(status, index);
        }
        if (!noDisposeOnSet && this.#hasDisposeAfter && this.#disposed) {
            const dt ***REMOVED*** this.#disposed;
            let task;
            while ((task ***REMOVED*** dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
        return this;
    }
    /**
     * Evict the least recently used item, returning its value or
     * `undefined` if cache is empty.
     */
    pop() {
        try {
            while (this.#size) {
                const val ***REMOVED*** this.#valList[this.#head];
                this.#evict(true);
                if (this.#isBackgroundFetch(val)) {
                    if (val.__staleWhileFetching) {
                        return val.__staleWhileFetching;
                    }
                }
                else if (val !***REMOVED******REMOVED*** undefined) {
                    return val;
                }
            }
        }
        finally {
            if (this.#hasDisposeAfter && this.#disposed) {
                const dt ***REMOVED*** this.#disposed;
                let task;
                while ((task ***REMOVED*** dt?.shift())) {
                    this.#disposeAfter?.(...task);
                }
            }
        }
    }
    #evict(free) {
        const head ***REMOVED*** this.#head;
        const k ***REMOVED*** this.#keyList[head];
        const v ***REMOVED*** this.#valList[head];
        const isBF ***REMOVED*** this.#isBackgroundFetch(v);
        if (isBF) {
            v.__abortController.abort(new Error('evicted'));
        }
        const oldValue ***REMOVED*** isBF ? v.__staleWhileFetching : v;
        if ((this.#hasDispose || this.#hasDisposeAfter) &&
            oldValue !***REMOVED******REMOVED*** undefined) {
            if (this.#hasDispose) {
                this.#dispose?.(oldValue, k, 'evict');
            }
            if (this.#hasDisposeAfter) {
                this.#disposed?.push([oldValue, k, 'evict']);
            }
        }
        this.#removeItemSize(head);
        if (this.#autopurgeTimers?.[head]) {
            clearTimeout(this.#autopurgeTimers[head]);
            this.#autopurgeTimers[head] ***REMOVED*** undefined;
        }
        // if we aren't about to use the index, then null these out
        if (free) {
            this.#keyList[head] ***REMOVED*** undefined;
            this.#valList[head] ***REMOVED*** undefined;
            this.#free.push(head);
        }
        if (this.#size ***REMOVED******REMOVED******REMOVED*** 1) {
            this.#head ***REMOVED*** this.#tail ***REMOVED*** 0;
            this.#free.length ***REMOVED*** 0;
        }
        else {
            this.#head ***REMOVED*** this.#next[head];
        }
        this.#keyMap.delete(k);
        this.#size--;
        return head;
    }
    /**
     * Check if a key is in the cache, without updating the recency of use.
     * Will return false if the item is stale, even though it is technically
     * in the cache.
     *
     * Check if a key is in the cache, without updating the recency of
     * use. Age is updated if {@link LRUCache.OptionsBase.updateAgeOnHas} is set
     * to `true` in either the options or the constructor.
     *
     * Will return `false` if the item is stale, even though it is technically in
     * the cache. The difference can be determined (if it matters) by using a
     * `status` argument, and inspecting the `has` field.
     *
     * Will not update item age unless
     * {@link LRUCache.OptionsBase.updateAgeOnHas} is set.
     */
    has(k, hasOptions ***REMOVED*** {}) {
        const { status ***REMOVED*** diagnostics_channel_js_1.metrics.hasSubscribers ? {} : undefined } ***REMOVED*** hasOptions;
        hasOptions.status ***REMOVED*** status;
        if (status) {
            status.op ***REMOVED*** 'has';
            status.key ***REMOVED*** k;
            status.cache ***REMOVED*** this;
        }
        const result ***REMOVED*** this.#has(k, hasOptions);
        if (diagnostics_channel_js_1.metrics.hasSubscribers)
            diagnostics_channel_js_1.metrics.publish(status);
        return result;
    }
    #has(k, hasOptions ***REMOVED*** {}) {
        const { updateAgeOnHas ***REMOVED*** this.updateAgeOnHas, status } ***REMOVED*** hasOptions;
        const index ***REMOVED*** this.#keyMap.get(k);
        if (index !***REMOVED******REMOVED*** undefined) {
            const v ***REMOVED*** this.#valList[index];
            if (this.#isBackgroundFetch(v) &&
                v.__staleWhileFetching ***REMOVED******REMOVED******REMOVED*** undefined) {
                return false;
            }
            if (!this.#isStale(index)) {
                if (updateAgeOnHas) {
                    this.#updateItemAge(index);
                }
                if (status) {
                    status.has ***REMOVED*** 'hit';
                    this.#statusTTL(status, index);
                }
                return true;
            }
            else if (status) {
                status.has ***REMOVED*** 'stale';
                this.#statusTTL(status, index);
            }
        }
        else if (status) {
            status.has ***REMOVED*** 'miss';
        }
        return false;
    }
    /**
     * Like {@link LRUCache#get} but doesn't update recency or delete stale
     * items.
     *
     * Returns `undefined` if the item is stale, unless
     * {@link LRUCache.OptionsBase.allowStale} is set.
     */
    peek(k, peekOptions ***REMOVED*** {}) {
        const { status ***REMOVED*** hasSubscribers() ? {} : undefined } ***REMOVED*** peekOptions;
        if (status) {
            status.op ***REMOVED*** 'peek';
            status.key ***REMOVED*** k;
            status.cache ***REMOVED*** this;
        }
        peekOptions.status ***REMOVED*** status;
        const result ***REMOVED*** this.#peek(k, peekOptions);
        if (diagnostics_channel_js_1.metrics.hasSubscribers) {
            diagnostics_channel_js_1.metrics.publish(status);
        }
        return result;
    }
    #peek(k, peekOptions) {
        const { status, allowStale ***REMOVED*** this.allowStale } ***REMOVED*** peekOptions;
        const index ***REMOVED*** this.#keyMap.get(k);
        if (index ***REMOVED******REMOVED******REMOVED*** undefined || (!allowStale && this.#isStale(index))) {
            if (status)
                status.peek ***REMOVED*** index ***REMOVED******REMOVED******REMOVED*** undefined ? 'miss' : 'stale';
            return undefined;
        }
        const v ***REMOVED*** this.#valList[index];
        const val ***REMOVED*** this.#isBackgroundFetch(v) ? v.__staleWhileFetching : v;
        if (status) {
            if (val !***REMOVED******REMOVED*** undefined) {
                status.peek ***REMOVED*** 'hit';
                status.value ***REMOVED*** val;
            }
            else {
                status.peek ***REMOVED*** 'miss';
            }
        }
        return val;
    }
    #backgroundFetch(k, index, options, context) {
        const v ***REMOVED*** index ***REMOVED******REMOVED******REMOVED*** undefined ? undefined : this.#valList[index];
        if (this.#isBackgroundFetch(v)) {
            return v;
        }
        const ac ***REMOVED*** new AbortController();
        const { signal } ***REMOVED*** options;
        // when/if our AC signals, then stop listening to theirs.
        signal?.addEventListener('abort', () ***REMOVED***> ac.abort(signal.reason), {
            signal: ac.signal,
        });
        const fetchOpts ***REMOVED*** {
            signal: ac.signal,
            options,
            context,
        };
        const cb ***REMOVED*** (v, updateCache ***REMOVED*** false) ***REMOVED***> {
            const { aborted } ***REMOVED*** ac.signal;
            const ignoreAbort ***REMOVED*** options.ignoreFetchAbort && v !***REMOVED******REMOVED*** undefined;
            const proceed ***REMOVED*** options.ignoreFetchAbort ||
                !!(options.allowStaleOnFetchAbort && v !***REMOVED******REMOVED*** undefined);
            if (options.status) {
                if (aborted && !updateCache) {
                    options.status.fetchAborted ***REMOVED*** true;
                    options.status.fetchError ***REMOVED*** ac.signal.reason;
                    if (ignoreAbort)
                        options.status.fetchAbortIgnored ***REMOVED*** true;
                }
                else {
                    options.status.fetchResolved ***REMOVED*** true;
                }
            }
            if (aborted && !ignoreAbort && !updateCache) {
                return fetchFail(ac.signal.reason, proceed);
            }
            // either we didn't abort, and are still here, or we did, and ignored
            const bf ***REMOVED*** p;
            // if nothing else has been written there but we're set to update the
            // cache and ignore the abort, or if it's still pending on this specific
            // background request, then write it to the cache.
            const vl ***REMOVED*** this.#valList[index];
            if (vl ***REMOVED******REMOVED******REMOVED*** p || (vl ***REMOVED******REMOVED******REMOVED*** undefined && ignoreAbort && updateCache)) {
                if (v ***REMOVED******REMOVED******REMOVED*** undefined) {
                    if (bf.__staleWhileFetching !***REMOVED******REMOVED*** undefined) {
                        this.#valList[index] ***REMOVED*** bf.__staleWhileFetching;
                    }
                    else {
                        this.#delete(k, 'fetch');
                    }
                }
                else {
                    if (options.status)
                        options.status.fetchUpdated ***REMOVED*** true;
                    this.#set(k, v, fetchOpts.options, bf);
                }
            }
            return v;
        };
        const eb ***REMOVED*** (er) ***REMOVED***> {
            if (options.status) {
                options.status.fetchRejected ***REMOVED*** true;
                options.status.fetchError ***REMOVED*** er;
            }
            // do not pass go, do not collect $200
            return fetchFail(er, false);
        };
        const fetchFail ***REMOVED*** (er, proceed) ***REMOVED***> {
            const { aborted } ***REMOVED*** ac.signal;
            const allowStaleAborted ***REMOVED*** aborted && options.allowStaleOnFetchAbort;
            const allowStale ***REMOVED*** allowStaleAborted || options.allowStaleOnFetchRejection;
            const noDelete ***REMOVED*** allowStale || options.noDeleteOnFetchRejection;
            const bf ***REMOVED*** p;
            if (this.#valList[index] ***REMOVED******REMOVED******REMOVED*** p) {
                // if we allow stale on fetch rejections, then we need to ensure that
                // the stale value is not removed from the cache when the fetch fails.
                const del ***REMOVED*** !noDelete || (!proceed && bf.__staleWhileFetching ***REMOVED******REMOVED******REMOVED*** undefined);
                if (del) {
                    this.#delete(k, 'fetch');
                }
                else if (!allowStaleAborted) {
                    // still replace the *promise* with the stale value,
                    // since we are done with the promise at this point.
                    // leave it untouched if we're still waiting for an
                    // aborted background fetch that hasn't yet returned.
                    this.#valList[index] ***REMOVED*** bf.__staleWhileFetching;
                }
            }
            if (allowStale) {
                if (options.status && bf.__staleWhileFetching !***REMOVED******REMOVED*** undefined) {
                    options.status.returnedStale ***REMOVED*** true;
                }
                return bf.__staleWhileFetching;
            }
            else if (bf.__returned ***REMOVED******REMOVED******REMOVED*** bf) {
                throw er;
            }
        };
        const pcall ***REMOVED*** (res, rej) ***REMOVED***> {
            const fmp ***REMOVED*** this.#fetchMethod?.(k, v, fetchOpts);
            // ignored, we go until we finish, regardless.
            // defer check until we are actually aborting,
            // so fetchMethod can override.
            ac.signal.addEventListener('abort', () ***REMOVED***> {
                if (!options.ignoreFetchAbort || options.allowStaleOnFetchAbort) {
                    res(undefined);
                    // when it eventually resolves, update the cache.
                    if (options.allowStaleOnFetchAbort) {
                        res ***REMOVED*** v ***REMOVED***> cb(v, true);
                    }
                }
            });
            if (fmp && fmp instanceof Promise) {
                fmp.then(v ***REMOVED***> res(v ***REMOVED******REMOVED******REMOVED*** undefined ? undefined : v), rej);
            }
            else if (fmp !***REMOVED******REMOVED*** undefined) {
                res(fmp);
            }
        };
        if (options.status)
            options.status.fetchDispatched ***REMOVED*** true;
        const p ***REMOVED*** new Promise(pcall).then(cb, eb);
        const bf ***REMOVED*** Object.assign(p, {
            __abortController: ac,
            __staleWhileFetching: v,
            __returned: undefined,
        });
        if (index ***REMOVED******REMOVED******REMOVED*** undefined) {
            // internal, don't expose status.
            this.#set(k, bf, { ...fetchOpts.options, status: undefined });
            index ***REMOVED*** this.#keyMap.get(k);
        }
        else {
            // do not call #set, because we do not want to adjust its place
            // in the lru queue, as it has not yet been "used". Also, we don't
            // need to worry about evicting for size, because a background fetch
            // over a stale value is treated as the same size as its stale value.
            this.#valList[index] ***REMOVED*** bf;
        }
        return bf;
    }
    #isBackgroundFetch(p) {
        if (!this.#hasFetchMethod)
            return false;
        const b ***REMOVED*** p;
        return (!!b &&
            b instanceof Promise &&
            b.hasOwnProperty('__staleWhileFetching') &&
            b.__abortController instanceof AbortController);
    }
    fetch(k, fetchOptions ***REMOVED*** {}) {
        const ths ***REMOVED*** diagnostics_channel_js_1.tracing.hasSubscribers;
        const { status ***REMOVED*** hasSubscribers() ? {} : undefined } ***REMOVED*** fetchOptions;
        fetchOptions.status ***REMOVED*** status;
        if (status && fetchOptions.context) {
            status.context ***REMOVED*** fetchOptions.context;
        }
        const p ***REMOVED*** this.#fetch(k, fetchOptions);
        if (status && ths) {
            status.trace ***REMOVED*** true;
            diagnostics_channel_js_1.tracing.tracePromise(() ***REMOVED***> p, status).catch(() ***REMOVED***> { });
        }
        return p;
    }
    async #fetch(k, fetchOptions ***REMOVED*** {}) {
        const { 
        // get options
        allowStale ***REMOVED*** this.allowStale, updateAgeOnGet ***REMOVED*** this.updateAgeOnGet, noDeleteOnStaleGet ***REMOVED*** this.noDeleteOnStaleGet, 
        // set options
        ttl ***REMOVED*** this.ttl, noDisposeOnSet ***REMOVED*** this.noDisposeOnSet, size ***REMOVED*** 0, sizeCalculation ***REMOVED*** this.sizeCalculation, noUpdateTTL ***REMOVED*** this.noUpdateTTL, 
        // fetch exclusive options
        noDeleteOnFetchRejection ***REMOVED*** this.noDeleteOnFetchRejection, allowStaleOnFetchRejection ***REMOVED*** this.allowStaleOnFetchRejection, ignoreFetchAbort ***REMOVED*** this.ignoreFetchAbort, allowStaleOnFetchAbort ***REMOVED*** this.allowStaleOnFetchAbort, context, forceRefresh ***REMOVED*** false, status, signal, } ***REMOVED*** fetchOptions;
        if (status) {
            status.op ***REMOVED*** 'fetch';
            status.key ***REMOVED*** k;
            if (forceRefresh)
                status.forceRefresh ***REMOVED*** true;
            status.cache ***REMOVED*** this;
        }
        if (!this.#hasFetchMethod) {
            if (status)
                status.fetch ***REMOVED*** 'get';
            return this.#get(k, {
                allowStale,
                updateAgeOnGet,
                noDeleteOnStaleGet,
                status,
            });
        }
        const options ***REMOVED*** {
            allowStale,
            updateAgeOnGet,
            noDeleteOnStaleGet,
            ttl,
            noDisposeOnSet,
            size,
            sizeCalculation,
            noUpdateTTL,
            noDeleteOnFetchRejection,
            allowStaleOnFetchRejection,
            allowStaleOnFetchAbort,
            ignoreFetchAbort,
            status,
            signal,
        };
        let index ***REMOVED*** this.#keyMap.get(k);
        if (index ***REMOVED******REMOVED******REMOVED*** undefined) {
            if (status)
                status.fetch ***REMOVED*** 'miss';
            const p ***REMOVED*** this.#backgroundFetch(k, index, options, context);
            return (p.__returned ***REMOVED*** p);
        }
        else {
            // in cache, maybe already fetching
            const v ***REMOVED*** this.#valList[index];
            if (this.#isBackgroundFetch(v)) {
                const stale ***REMOVED*** allowStale && v.__staleWhileFetching !***REMOVED******REMOVED*** undefined;
                if (status) {
                    status.fetch ***REMOVED*** 'inflight';
                    if (stale)
                        status.returnedStale ***REMOVED*** true;
                }
                return stale ? v.__staleWhileFetching : (v.__returned ***REMOVED*** v);
            }
            // if we force a refresh, that means do NOT serve the cached value,
            // unless we are already in the process of refreshing the cache.
            const isStale ***REMOVED*** this.#isStale(index);
            if (!forceRefresh && !isStale) {
                if (status)
                    status.fetch ***REMOVED*** 'hit';
                this.#moveToTail(index);
                if (updateAgeOnGet) {
                    this.#updateItemAge(index);
                }
                if (status)
                    this.#statusTTL(status, index);
                return v;
            }
            // ok, it is stale or a forced refresh, and not already fetching.
            // refresh the cache.
            const p ***REMOVED*** this.#backgroundFetch(k, index, options, context);
            const hasStale ***REMOVED*** p.__staleWhileFetching !***REMOVED******REMOVED*** undefined;
            const staleVal ***REMOVED*** hasStale && allowStale;
            if (status) {
                status.fetch ***REMOVED*** isStale ? 'stale' : 'refresh';
                if (staleVal && isStale)
                    status.returnedStale ***REMOVED*** true;
            }
            return staleVal ? p.__staleWhileFetching : (p.__returned ***REMOVED*** p);
        }
    }
    forceFetch(k, fetchOptions ***REMOVED*** {}) {
        const ths ***REMOVED*** diagnostics_channel_js_1.tracing.hasSubscribers;
        const { status ***REMOVED*** hasSubscribers() ? {} : undefined } ***REMOVED*** fetchOptions;
        fetchOptions.status ***REMOVED*** status;
        if (status && fetchOptions.context) {
            status.context ***REMOVED*** fetchOptions.context;
        }
        const p ***REMOVED*** this.#forceFetch(k, fetchOptions);
        if (status && ths) {
            status.trace ***REMOVED*** true;
            diagnostics_channel_js_1.tracing.tracePromise(() ***REMOVED***> p, status).catch(() ***REMOVED***> { });
        }
        return p;
    }
    async #forceFetch(k, fetchOptions ***REMOVED*** {}) {
        const v ***REMOVED*** await this.#fetch(k, fetchOptions);
        if (v ***REMOVED******REMOVED******REMOVED*** undefined)
            throw new Error('fetch() returned undefined');
        return v;
    }
    memo(k, memoOptions ***REMOVED*** {}) {
        const { status ***REMOVED*** diagnostics_channel_js_1.metrics.hasSubscribers ? {} : undefined } ***REMOVED*** memoOptions;
        memoOptions.status ***REMOVED*** status;
        if (status) {
            status.op ***REMOVED*** 'memo';
            status.key ***REMOVED*** k;
            if (memoOptions.context) {
                status.context ***REMOVED*** memoOptions.context;
            }
            status.cache ***REMOVED*** this;
        }
        const result ***REMOVED*** this.#memo(k, memoOptions);
        if (status)
            status.value ***REMOVED*** result;
        if (diagnostics_channel_js_1.metrics.hasSubscribers)
            diagnostics_channel_js_1.metrics.publish(status);
        return result;
    }
    #memo(k, memoOptions ***REMOVED*** {}) {
        const memoMethod ***REMOVED*** this.#memoMethod;
        if (!memoMethod) {
            throw new Error('no memoMethod provided to constructor');
        }
        const { context, status, forceRefresh, ...options } ***REMOVED*** memoOptions;
        if (status && forceRefresh)
            status.forceRefresh ***REMOVED*** true;
        const v ***REMOVED*** this.#get(k, options);
        const refresh ***REMOVED*** forceRefresh || v ***REMOVED******REMOVED******REMOVED*** undefined;
        if (status) {
            status.memo ***REMOVED*** refresh ? 'miss' : 'hit';
            if (!refresh)
                status.value ***REMOVED*** v;
        }
        if (!refresh)
            return v;
        const vv ***REMOVED*** memoMethod(k, v, {
            options,
            context,
        });
        if (status)
            status.value ***REMOVED*** vv;
        this.#set(k, vv, options);
        return vv;
    }
    /**
     * Return a value from the cache. Will update the recency of the cache
     * entry found.
     *
     * If the key is not found, get() will return `undefined`.
     */
    get(k, getOptions ***REMOVED*** {}) {
        const { status ***REMOVED*** diagnostics_channel_js_1.metrics.hasSubscribers ? {} : undefined } ***REMOVED*** getOptions;
        getOptions.status ***REMOVED*** status;
        if (status) {
            status.op ***REMOVED*** 'get';
            status.key ***REMOVED*** k;
            status.cache ***REMOVED*** this;
        }
        const result ***REMOVED*** this.#get(k, getOptions);
        if (status) {
            if (result !***REMOVED******REMOVED*** undefined)
                status.value ***REMOVED*** result;
            if (diagnostics_channel_js_1.metrics.hasSubscribers)
                diagnostics_channel_js_1.metrics.publish(status);
        }
        return result;
    }
    #get(k, getOptions ***REMOVED*** {}) {
        const { allowStale ***REMOVED*** this.allowStale, updateAgeOnGet ***REMOVED*** this.updateAgeOnGet, noDeleteOnStaleGet ***REMOVED*** this.noDeleteOnStaleGet, status, } ***REMOVED*** getOptions;
        const index ***REMOVED*** this.#keyMap.get(k);
        if (index ***REMOVED******REMOVED******REMOVED*** undefined) {
            if (status)
                status.get ***REMOVED*** 'miss';
            return undefined;
        }
        const value ***REMOVED*** this.#valList[index];
        const fetching ***REMOVED*** this.#isBackgroundFetch(value);
        if (status)
            this.#statusTTL(status, index);
        if (this.#isStale(index)) {
            // delete only if not an in-flight background fetch
            if (!fetching) {
                if (!noDeleteOnStaleGet) {
                    this.#delete(k, 'expire');
                }
                if (status)
                    status.get ***REMOVED*** 'stale';
                if (allowStale) {
                    if (status)
                        status.returnedStale ***REMOVED*** true;
                    return value;
                }
                return undefined;
            }
            if (status)
                status.get ***REMOVED*** 'stale-fetching';
            if (allowStale && value.__staleWhileFetching !***REMOVED******REMOVED*** undefined) {
                if (status)
                    status.returnedStale ***REMOVED*** true;
                return value.__staleWhileFetching;
            }
            return undefined;
        }
        // not stale
        if (status)
            status.get ***REMOVED*** fetching ? 'fetching' : 'hit';
        // if we're currently fetching it, we don't actually have it yet
        // it's not stale, which means this isn't a staleWhileRefetching.
        // If it's not stale, and fetching, AND has a __staleWhileFetching
        // value, then that means the user fetched with {forceRefresh:true},
        // so it's safe to return that value.
        this.#moveToTail(index);
        if (updateAgeOnGet) {
            this.#updateItemAge(index);
        }
        return fetching ? value.__staleWhileFetching : value;
    }
    #connect(p, n) {
        this.#prev[n] ***REMOVED*** p;
        this.#next[p] ***REMOVED*** n;
    }
    #moveToTail(index) {
        // if tail already, nothing to do
        // if head, move head to next[index]
        // else
        //   move next[prev[index]] to next[index] (head has no prev)
        //   move prev[next[index]] to prev[index]
        // prev[index] ***REMOVED*** tail
        // next[tail] ***REMOVED*** index
        // tail ***REMOVED*** index
        if (index !***REMOVED******REMOVED*** this.#tail) {
            if (index ***REMOVED******REMOVED******REMOVED*** this.#head) {
                this.#head ***REMOVED*** this.#next[index];
            }
            else {
                this.#connect(this.#prev[index], this.#next[index]);
            }
            this.#connect(this.#tail, index);
            this.#tail ***REMOVED*** index;
        }
    }
    /**
     * Deletes a key out of the cache.
     *
     * Returns true if the key was deleted, false otherwise.
     */
    delete(k) {
        return this.#delete(k, 'delete');
    }
    #delete(k, reason) {
        if (diagnostics_channel_js_1.metrics.hasSubscribers) {
            diagnostics_channel_js_1.metrics.publish({
                op: 'delete',
                delete: reason,
                key: k,
                cache: this,
            });
        }
        let deleted ***REMOVED*** false;
        if (this.#size !***REMOVED******REMOVED*** 0) {
            const index ***REMOVED*** this.#keyMap.get(k);
            if (index !***REMOVED******REMOVED*** undefined) {
                if (this.#autopurgeTimers?.[index]) {
                    clearTimeout(this.#autopurgeTimers?.[index]);
                    this.#autopurgeTimers[index] ***REMOVED*** undefined;
                }
                deleted ***REMOVED*** true;
                if (this.#size ***REMOVED******REMOVED******REMOVED*** 1) {
                    this.#clear(reason);
                }
                else {
                    this.#removeItemSize(index);
                    const v ***REMOVED*** this.#valList[index];
                    if (this.#isBackgroundFetch(v)) {
                        v.__abortController.abort(new Error('deleted'));
                    }
                    else if (this.#hasDispose || this.#hasDisposeAfter) {
                        if (this.#hasDispose) {
                            this.#dispose?.(v, k, reason);
                        }
                        if (this.#hasDisposeAfter) {
                            this.#disposed?.push([v, k, reason]);
                        }
                    }
                    this.#keyMap.delete(k);
                    this.#keyList[index] ***REMOVED*** undefined;
                    this.#valList[index] ***REMOVED*** undefined;
                    if (index ***REMOVED******REMOVED******REMOVED*** this.#tail) {
                        this.#tail ***REMOVED*** this.#prev[index];
                    }
                    else if (index ***REMOVED******REMOVED******REMOVED*** this.#head) {
                        this.#head ***REMOVED*** this.#next[index];
                    }
                    else {
                        const pi ***REMOVED*** this.#prev[index];
                        this.#next[pi] ***REMOVED*** this.#next[index];
                        const ni ***REMOVED*** this.#next[index];
                        this.#prev[ni] ***REMOVED*** this.#prev[index];
                    }
                    this.#size--;
                    this.#free.push(index);
                }
            }
        }
        if (this.#hasDisposeAfter && this.#disposed?.length) {
            const dt ***REMOVED*** this.#disposed;
            let task;
            while ((task ***REMOVED*** dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
        return deleted;
    }
    /**
     * Clear the cache entirely, throwing away all values.
     */
    clear() {
        return this.#clear('delete');
    }
    #clear(reason) {
        for (const index of this.#rindexes({ allowStale: true })) {
            const v ***REMOVED*** this.#valList[index];
            if (this.#isBackgroundFetch(v)) {
                v.__abortController.abort(new Error('deleted'));
            }
            else {
                const k ***REMOVED*** this.#keyList[index];
                if (this.#hasDispose) {
                    this.#dispose?.(v, k, reason);
                }
                if (this.#hasDisposeAfter) {
                    this.#disposed?.push([v, k, reason]);
                }
            }
        }
        this.#keyMap.clear();
        void this.#valList.fill(undefined);
        this.#keyList.fill(undefined);
        if (this.#ttls && this.#starts) {
            this.#ttls.fill(0);
            this.#starts.fill(0);
            for (const t of this.#autopurgeTimers ?? []) {
                if (t !***REMOVED******REMOVED*** undefined)
                    clearTimeout(t);
            }
            this.#autopurgeTimers?.fill(undefined);
        }
        if (this.#sizes) {
            this.#sizes.fill(0);
        }
        this.#head ***REMOVED*** 0;
        this.#tail ***REMOVED*** 0;
        this.#free.length ***REMOVED*** 0;
        this.#calculatedSize ***REMOVED*** 0;
        this.#size ***REMOVED*** 0;
        if (this.#hasDisposeAfter && this.#disposed) {
            const dt ***REMOVED*** this.#disposed;
            let task;
            while ((task ***REMOVED*** dt?.shift())) {
                this.#disposeAfter?.(...task);
            }
        }
    }
}
exports.LRUCache ***REMOVED*** LRUCache;
//# sourceMappingURL***REMOVED***index.js.map