import { spawn as nodeSpawn, } from 'child_process';
import crossSpawn from 'cross-spawn';
import { onExit } from 'signal-exit';
import { proxySignals } from './proxy-signals.js';
import { watchdog } from './watchdog.js';
/* c8 ignore start */
const spawn ***REMOVED*** process?.platform ***REMOVED******REMOVED******REMOVED*** 'win32' ? crossSpawn : nodeSpawn;
/**
 * Normalizes the arguments passed to `foregroundChild`.
 *
 * Exposed for testing.
 *
 * @internal
 */
export const normalizeFgArgs ***REMOVED*** (fgArgs) ***REMOVED***> {
    let [program, args ***REMOVED*** [], spawnOpts ***REMOVED*** {}, cleanup ***REMOVED*** () ***REMOVED***> { }] ***REMOVED*** fgArgs;
    if (typeof args ***REMOVED******REMOVED******REMOVED*** 'function') {
        cleanup ***REMOVED*** args;
        spawnOpts ***REMOVED*** {};
        args ***REMOVED*** [];
    }
    else if (!!args && typeof args ***REMOVED******REMOVED******REMOVED*** 'object' && !Array.isArray(args)) {
        if (typeof spawnOpts ***REMOVED******REMOVED******REMOVED*** 'function')
            cleanup ***REMOVED*** spawnOpts;
        spawnOpts ***REMOVED*** args;
        args ***REMOVED*** [];
    }
    else if (typeof spawnOpts ***REMOVED******REMOVED******REMOVED*** 'function') {
        cleanup ***REMOVED*** spawnOpts;
        spawnOpts ***REMOVED*** {};
    }
    if (Array.isArray(program)) {
        const [pp, ...pa] ***REMOVED*** program;
        program ***REMOVED*** pp;
        args ***REMOVED*** pa;
    }
    return [program, args, { ...spawnOpts }, cleanup];
};
export function foregroundChild(...fgArgs) {
    const [program, args, spawnOpts, cleanup] ***REMOVED*** normalizeFgArgs(fgArgs);
    spawnOpts.stdio ***REMOVED*** [0, 1, 2];
    if (process.send) {
        spawnOpts.stdio.push('ipc');
    }
    const child ***REMOVED*** spawn(program, args, spawnOpts);
    const childHangup ***REMOVED*** () ***REMOVED***> {
        try {
            child.kill('SIGHUP');
            /* c8 ignore start */
        }
        catch (_) {
            // SIGHUP is weird on windows
            child.kill('SIGTERM');
        }
        /* c8 ignore stop */
    };
    const removeOnExit ***REMOVED*** onExit(childHangup);
    proxySignals(child);
    const dog ***REMOVED*** watchdog(child);
    let done ***REMOVED*** false;
    child.on('close', async (code, signal) ***REMOVED***> {
        /* c8 ignore start */
        if (done)
            return;
        /* c8 ignore stop */
        done ***REMOVED*** true;
        const result ***REMOVED*** cleanup(code, signal, {
            watchdogPid: dog.pid,
        });
        const res ***REMOVED*** isPromise(result) ? await result : result;
        removeOnExit();
        if (res ***REMOVED******REMOVED******REMOVED*** false)
            return;
        else if (typeof res ***REMOVED******REMOVED******REMOVED*** 'string') {
            signal ***REMOVED*** res;
            code ***REMOVED*** null;
        }
        else if (typeof res ***REMOVED******REMOVED******REMOVED*** 'number') {
            code ***REMOVED*** res;
            signal ***REMOVED*** null;
        }
        if (signal) {
            // If there is nothing else keeping the event loop alive,
            // then there's a race between a graceful exit and getting
            // the signal to this process.  Put this timeout here to
            // make sure we're still alive to get the signal, and thus
            // exit with the intended signal code.
            /* istanbul ignore next */
            setTimeout(() ***REMOVED***> { }, 2000);
            try {
                process.kill(process.pid, signal);
                /* c8 ignore start */
            }
            catch (_) {
                process.kill(process.pid, 'SIGTERM');
            }
            /* c8 ignore stop */
        }
        else {
            process.exit(code || 0);
        }
    });
    if (process.send) {
        process.removeAllListeners('message');
        child.on('message', (message, sendHandle) ***REMOVED***> {
            process.send?.(message, sendHandle);
        });
        process.on('message', (message, sendHandle) ***REMOVED***> {
            child.send(message, sendHandle);
        });
    }
    return child;
}
const isPromise ***REMOVED*** (o) ***REMOVED***> !!o && typeof o ***REMOVED******REMOVED******REMOVED*** 'object' && typeof o.then ***REMOVED******REMOVED******REMOVED*** 'function';
//# sourceMappingURL***REMOVED***index.js.map