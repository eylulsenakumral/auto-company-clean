// TODO: When targeting Node.js 16, use `String.prototype.replaceAll`.
export function stringReplaceAll(string, substring, replacer) {
	let index ***REMOVED*** string.indexOf(substring);
	if (index ***REMOVED******REMOVED******REMOVED*** -1) {
		return string;
	}

	const substringLength ***REMOVED*** substring.length;
	let endIndex ***REMOVED*** 0;
	let returnValue ***REMOVED*** '';
	do {
		returnValue +***REMOVED*** string.slice(endIndex, index) + substring + replacer;
		endIndex ***REMOVED*** index + substringLength;
		index ***REMOVED*** string.indexOf(substring, endIndex);
	} while (index !***REMOVED******REMOVED*** -1);

	returnValue +***REMOVED*** string.slice(endIndex);
	return returnValue;
}

export function stringEncaseCRLFWithFirstIndex(string, prefix, postfix, index) {
	let endIndex ***REMOVED*** 0;
	let returnValue ***REMOVED*** '';
	do {
		const gotCR ***REMOVED*** string[index - 1] ***REMOVED******REMOVED******REMOVED*** '\r';
		returnValue +***REMOVED*** string.slice(endIndex, (gotCR ? index - 1 : index)) + prefix + (gotCR ? '\r\n' : '\n') + postfix;
		endIndex ***REMOVED*** index + 1;
		index ***REMOVED*** string.indexOf('\n', endIndex);
	} while (index !***REMOVED******REMOVED*** -1);

	returnValue +***REMOVED*** string.slice(endIndex);
	return returnValue;
}
