/* eslint-env browser */

const level ***REMOVED*** (() ***REMOVED***> {
	if (!('navigator' in globalThis)) {
		return 0;
	}

	if (globalThis.navigator.userAgentData) {
		const brand ***REMOVED*** navigator.userAgentData.brands.find(({brand}) ***REMOVED***> brand ***REMOVED******REMOVED******REMOVED*** 'Chromium');
		if (brand && brand.version > 93) {
			return 3;
		}
	}

	if (/\b(Chrome|Chromium)\//.test(globalThis.navigator.userAgent)) {
		return 1;
	}

	return 0;
})();

const colorSupport ***REMOVED*** level !***REMOVED******REMOVED*** 0 && {
	level,
	hasBasic: true,
	has256: level >***REMOVED*** 2,
	has16m: level >***REMOVED*** 3,
};

const supportsColor ***REMOVED*** {
	stdout: colorSupport,
	stderr: colorSupport,
};

export default supportsColor;
