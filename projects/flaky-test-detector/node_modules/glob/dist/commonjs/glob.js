"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Glob ***REMOVED*** void 0;
const minimatch_1 ***REMOVED*** require("minimatch");
const node_url_1 ***REMOVED*** require("node:url");
const path_scurry_1 ***REMOVED*** require("path-scurry");
const pattern_js_1 ***REMOVED*** require("./pattern.js");
const walker_js_1 ***REMOVED*** require("./walker.js");
// if no process global, just call it linux.
// so we default to case-sensitive, / separators
const defaultPlatform ***REMOVED*** (typeof process ***REMOVED******REMOVED******REMOVED*** 'object' &&
    process &&
    typeof process.platform ***REMOVED******REMOVED******REMOVED*** 'string') ?
    process.platform
    : 'linux';
/**
 * An object that can perform glob pattern traversals.
 */
class Glob {
    absolute;
    cwd;
    root;
    dot;
    dotRelative;
    follow;
    ignore;
    magicalBraces;
    mark;
    matchBase;
    maxDepth;
    nobrace;
    nocase;
    nodir;
    noext;
    noglobstar;
    pattern;
    platform;
    realpath;
    scurry;
    stat;
    signal;
    windowsPathsNoEscape;
    withFileTypes;
    includeChildMatches;
    /**
     * The options provided to the constructor.
     */
    opts;
    /**
     * An array of parsed immutable {@link Pattern} objects.
     */
    patterns;
    /**
     * All options are stored as properties on the `Glob` object.
     *
     * See {@link GlobOptions} for full options descriptions.
     *
     * Note that a previous `Glob` object can be passed as the
     * `GlobOptions` to another `Glob` instantiation to re-use settings
     * and caches with a new pattern.
     *
     * Traversal functions can be called multiple times to run the walk
     * again.
     */
    constructor(pattern, opts) {
        /* c8 ignore start */
        if (!opts)
            throw new TypeError('glob options required');
        /* c8 ignore stop */
        this.withFileTypes ***REMOVED*** !!opts.withFileTypes;
        this.signal ***REMOVED*** opts.signal;
        this.follow ***REMOVED*** !!opts.follow;
        this.dot ***REMOVED*** !!opts.dot;
        this.dotRelative ***REMOVED*** !!opts.dotRelative;
        this.nodir ***REMOVED*** !!opts.nodir;
        this.mark ***REMOVED*** !!opts.mark;
        if (!opts.cwd) {
            this.cwd ***REMOVED*** '';
        }
        else if (opts.cwd instanceof URL || opts.cwd.startsWith('file://')) {
            opts.cwd ***REMOVED*** (0, node_url_1.fileURLToPath)(opts.cwd);
        }
        this.cwd ***REMOVED*** opts.cwd || '';
        this.root ***REMOVED*** opts.root;
        this.magicalBraces ***REMOVED*** !!opts.magicalBraces;
        this.nobrace ***REMOVED*** !!opts.nobrace;
        this.noext ***REMOVED*** !!opts.noext;
        this.realpath ***REMOVED*** !!opts.realpath;
        this.absolute ***REMOVED*** opts.absolute;
        this.includeChildMatches ***REMOVED*** opts.includeChildMatches !***REMOVED******REMOVED*** false;
        this.noglobstar ***REMOVED*** !!opts.noglobstar;
        this.matchBase ***REMOVED*** !!opts.matchBase;
        this.maxDepth ***REMOVED***
            typeof opts.maxDepth ***REMOVED******REMOVED******REMOVED*** 'number' ? opts.maxDepth : Infinity;
        this.stat ***REMOVED*** !!opts.stat;
        this.ignore ***REMOVED*** opts.ignore;
        if (this.withFileTypes && this.absolute !***REMOVED******REMOVED*** undefined) {
            throw new Error('cannot set absolute and withFileTypes:true');
        }
        if (typeof pattern ***REMOVED******REMOVED******REMOVED*** 'string') {
            pattern ***REMOVED*** [pattern];
        }
        this.windowsPathsNoEscape ***REMOVED***
            !!opts.windowsPathsNoEscape ||
                opts.allowWindowsEscape ***REMOVED******REMOVED******REMOVED***
                    false;
        if (this.windowsPathsNoEscape) {
            pattern ***REMOVED*** pattern.map(p ***REMOVED***> p.replace(/\\/g, '/'));
        }
        if (this.matchBase) {
            if (opts.noglobstar) {
                throw new TypeError('base matching requires globstar');
            }
            pattern ***REMOVED*** pattern.map(p ***REMOVED***> (p.includes('/') ? p : `./**/${p}`));
        }
        this.pattern ***REMOVED*** pattern;
        this.platform ***REMOVED*** opts.platform || defaultPlatform;
        this.opts ***REMOVED*** { ...opts, platform: this.platform };
        if (opts.scurry) {
            this.scurry ***REMOVED*** opts.scurry;
            if (opts.nocase !***REMOVED******REMOVED*** undefined &&
                opts.nocase !***REMOVED******REMOVED*** opts.scurry.nocase) {
                throw new Error('nocase option contradicts provided scurry option');
            }
        }
        else {
            const Scurry ***REMOVED*** opts.platform ***REMOVED******REMOVED******REMOVED*** 'win32' ? path_scurry_1.PathScurryWin32
                : opts.platform ***REMOVED******REMOVED******REMOVED*** 'darwin' ? path_scurry_1.PathScurryDarwin
                    : opts.platform ? path_scurry_1.PathScurryPosix
                        : path_scurry_1.PathScurry;
            this.scurry ***REMOVED*** new Scurry(this.cwd, {
                nocase: opts.nocase,
                fs: opts.fs,
            });
        }
        this.nocase ***REMOVED*** this.scurry.nocase;
        // If you do nocase:true on a case-sensitive file system, then
        // we need to use regexps instead of strings for non-magic
        // path portions, because statting `aBc` won't return results
        // for the file `AbC` for example.
        const nocaseMagicOnly ***REMOVED*** this.platform ***REMOVED******REMOVED******REMOVED*** 'darwin' || this.platform ***REMOVED******REMOVED******REMOVED*** 'win32';
        const mmo ***REMOVED*** {
            // default nocase based on platform
            ...opts,
            dot: this.dot,
            matchBase: this.matchBase,
            nobrace: this.nobrace,
            nocase: this.nocase,
            nocaseMagicOnly,
            nocomment: true,
            noext: this.noext,
            nonegate: true,
            optimizationLevel: 2,
            platform: this.platform,
            windowsPathsNoEscape: this.windowsPathsNoEscape,
            debug: !!this.opts.debug,
        };
        const mms ***REMOVED*** this.pattern.map(p ***REMOVED***> new minimatch_1.Minimatch(p, mmo));
        const [matchSet, globParts] ***REMOVED*** mms.reduce((set, m) ***REMOVED***> {
            set[0].push(...m.set);
            set[1].push(...m.globParts);
            return set;
        }, [[], []]);
        this.patterns ***REMOVED*** matchSet.map((set, i) ***REMOVED***> {
            const g ***REMOVED*** globParts[i];
            /* c8 ignore start */
            if (!g)
                throw new Error('invalid pattern object');
            /* c8 ignore stop */
            return new pattern_js_1.Pattern(set, g, 0, this.platform);
        });
    }
    async walk() {
        // Walkers always return array of Path objects, so we just have to
        // coerce them into the right shape.  It will have already called
        // realpath() if the option was set to do so, so we know that's cached.
        // start out knowing the cwd, at least
        return [
            ...(await new walker_js_1.GlobWalker(this.patterns, this.scurry.cwd, {
                ...this.opts,
                maxDepth: this.maxDepth !***REMOVED******REMOVED*** Infinity ?
                    this.maxDepth + this.scurry.cwd.depth()
                    : Infinity,
                platform: this.platform,
                nocase: this.nocase,
                includeChildMatches: this.includeChildMatches,
            }).walk()),
        ];
    }
    walkSync() {
        return [
            ...new walker_js_1.GlobWalker(this.patterns, this.scurry.cwd, {
                ...this.opts,
                maxDepth: this.maxDepth !***REMOVED******REMOVED*** Infinity ?
                    this.maxDepth + this.scurry.cwd.depth()
                    : Infinity,
                platform: this.platform,
                nocase: this.nocase,
                includeChildMatches: this.includeChildMatches,
            }).walkSync(),
        ];
    }
    stream() {
        return new walker_js_1.GlobStream(this.patterns, this.scurry.cwd, {
            ...this.opts,
            maxDepth: this.maxDepth !***REMOVED******REMOVED*** Infinity ?
                this.maxDepth + this.scurry.cwd.depth()
                : Infinity,
            platform: this.platform,
            nocase: this.nocase,
            includeChildMatches: this.includeChildMatches,
        }).stream();
    }
    streamSync() {
        return new walker_js_1.GlobStream(this.patterns, this.scurry.cwd, {
            ...this.opts,
            maxDepth: this.maxDepth !***REMOVED******REMOVED*** Infinity ?
                this.maxDepth + this.scurry.cwd.depth()
                : Infinity,
            platform: this.platform,
            nocase: this.nocase,
            includeChildMatches: this.includeChildMatches,
        }).streamSync();
    }
    /**
     * Default sync iteration function. Returns a Generator that
     * iterates over the results.
     */
    iterateSync() {
        return this.streamSync()[Symbol.iterator]();
    }
    [Symbol.iterator]() {
        return this.iterateSync();
    }
    /**
     * Default async iteration function. Returns an AsyncGenerator that
     * iterates over the results.
     */
    iterate() {
        return this.stream()[Symbol.asyncIterator]();
    }
    [Symbol.asyncIterator]() {
        return this.iterate();
    }
}
exports.Glob ***REMOVED*** Glob;
//# sourceMappingURL***REMOVED***glob.js.map