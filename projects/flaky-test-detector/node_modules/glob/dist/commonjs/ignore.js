"use strict";
// give it a pattern, and it'll be able to tell you if
// a given path should be ignored.
// Ignoring a path ignores its children if the pattern ends in /**
// Ignores are always parsed in dot:true mode
Object.defineProperty(exports, "__esModule", { value: true });
exports.Ignore ***REMOVED*** void 0;
const minimatch_1 ***REMOVED*** require("minimatch");
const pattern_js_1 ***REMOVED*** require("./pattern.js");
const defaultPlatform ***REMOVED*** (typeof process ***REMOVED******REMOVED******REMOVED*** 'object' &&
    process &&
    typeof process.platform ***REMOVED******REMOVED******REMOVED*** 'string') ?
    process.platform
    : 'linux';
/**
 * Class used to process ignored patterns
 */
class Ignore {
    relative;
    relativeChildren;
    absolute;
    absoluteChildren;
    platform;
    mmopts;
    constructor(ignored, { nobrace, nocase, noext, noglobstar, platform ***REMOVED*** defaultPlatform, }) {
        this.relative ***REMOVED*** [];
        this.absolute ***REMOVED*** [];
        this.relativeChildren ***REMOVED*** [];
        this.absoluteChildren ***REMOVED*** [];
        this.platform ***REMOVED*** platform;
        this.mmopts ***REMOVED*** {
            dot: true,
            nobrace,
            nocase,
            noext,
            noglobstar,
            optimizationLevel: 2,
            platform,
            nocomment: true,
            nonegate: true,
        };
        for (const ign of ignored)
            this.add(ign);
    }
    add(ign) {
        // this is a little weird, but it gives us a clean set of optimized
        // minimatch matchers, without getting tripped up if one of them
        // ends in /** inside a brace section, and it's only inefficient at
        // the start of the walk, not along it.
        // It'd be nice if the Pattern class just had a .test() method, but
        // handling globstars is a bit of a pita, and that code already lives
        // in minimatch anyway.
        // Another way would be if maybe Minimatch could take its set/globParts
        // as an option, and then we could at least just use Pattern to test
        // for absolute-ness.
        // Yet another way, Minimatch could take an array of glob strings, and
        // a cwd option, and do the right thing.
        const mm ***REMOVED*** new minimatch_1.Minimatch(ign, this.mmopts);
        for (let i ***REMOVED*** 0; i < mm.set.length; i++) {
            const parsed ***REMOVED*** mm.set[i];
            const globParts ***REMOVED*** mm.globParts[i];
            /* c8 ignore start */
            if (!parsed || !globParts) {
                throw new Error('invalid pattern object');
            }
            // strip off leading ./ portions
            // https://github.com/isaacs/node-glob/issues/570
            while (parsed[0] ***REMOVED******REMOVED******REMOVED*** '.' && globParts[0] ***REMOVED******REMOVED******REMOVED*** '.') {
                parsed.shift();
                globParts.shift();
            }
            /* c8 ignore stop */
            const p ***REMOVED*** new pattern_js_1.Pattern(parsed, globParts, 0, this.platform);
            const m ***REMOVED*** new minimatch_1.Minimatch(p.globString(), this.mmopts);
            const children ***REMOVED*** globParts[globParts.length - 1] ***REMOVED******REMOVED******REMOVED*** '**';
            const absolute ***REMOVED*** p.isAbsolute();
            if (absolute)
                this.absolute.push(m);
            else
                this.relative.push(m);
            if (children) {
                if (absolute)
                    this.absoluteChildren.push(m);
                else
                    this.relativeChildren.push(m);
            }
        }
    }
    ignored(p) {
        const fullpath ***REMOVED*** p.fullpath();
        const fullpaths ***REMOVED*** `${fullpath}/`;
        const relative ***REMOVED*** p.relative() || '.';
        const relatives ***REMOVED*** `${relative}/`;
        for (const m of this.relative) {
            if (m.match(relative) || m.match(relatives))
                return true;
        }
        for (const m of this.absolute) {
            if (m.match(fullpath) || m.match(fullpaths))
                return true;
        }
        return false;
    }
    childrenIgnored(p) {
        const fullpath ***REMOVED*** p.fullpath() + '/';
        const relative ***REMOVED*** (p.relative() || '.') + '/';
        for (const m of this.relativeChildren) {
            if (m.match(relative))
                return true;
        }
        for (const m of this.absoluteChildren) {
            if (m.match(fullpath))
                return true;
        }
        return false;
    }
}
exports.Ignore ***REMOVED*** Ignore;
//# sourceMappingURL***REMOVED***ignore.js.map