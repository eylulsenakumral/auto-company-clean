"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.GlobStream ***REMOVED*** exports.GlobWalker ***REMOVED*** exports.GlobUtil ***REMOVED*** void 0;
/**
 * Single-use utility classes to provide functionality to the {@link Glob}
 * methods.
 *
 * @module
 */
const minipass_1 ***REMOVED*** require("minipass");
const ignore_js_1 ***REMOVED*** require("./ignore.js");
const processor_js_1 ***REMOVED*** require("./processor.js");
const makeIgnore ***REMOVED*** (ignore, opts) ***REMOVED***> typeof ignore ***REMOVED******REMOVED******REMOVED*** 'string' ? new ignore_js_1.Ignore([ignore], opts)
    : Array.isArray(ignore) ? new ignore_js_1.Ignore(ignore, opts)
        : ignore;
/**
 * basic walking utilities that all the glob walker types use
 */
class GlobUtil {
    path;
    patterns;
    opts;
    seen ***REMOVED*** new Set();
    paused ***REMOVED*** false;
    aborted ***REMOVED*** false;
    #onResume ***REMOVED*** [];
    #ignore;
    #sep;
    signal;
    maxDepth;
    includeChildMatches;
    constructor(patterns, path, opts) {
        this.patterns ***REMOVED*** patterns;
        this.path ***REMOVED*** path;
        this.opts ***REMOVED*** opts;
        this.#sep ***REMOVED*** !opts.posix && opts.platform ***REMOVED******REMOVED******REMOVED*** 'win32' ? '\\' : '/';
        this.includeChildMatches ***REMOVED*** opts.includeChildMatches !***REMOVED******REMOVED*** false;
        if (opts.ignore || !this.includeChildMatches) {
            this.#ignore ***REMOVED*** makeIgnore(opts.ignore ?? [], opts);
            if (!this.includeChildMatches &&
                typeof this.#ignore.add !***REMOVED******REMOVED*** 'function') {
                const m ***REMOVED*** 'cannot ignore child matches, ignore lacks add() method.';
                throw new Error(m);
            }
        }
        // ignore, always set with maxDepth, but it's optional on the
        // GlobOptions type
        /* c8 ignore start */
        this.maxDepth ***REMOVED*** opts.maxDepth || Infinity;
        /* c8 ignore stop */
        if (opts.signal) {
            this.signal ***REMOVED*** opts.signal;
            this.signal.addEventListener('abort', () ***REMOVED***> {
                this.#onResume.length ***REMOVED*** 0;
            });
        }
    }
    #ignored(path) {
        return this.seen.has(path) || !!this.#ignore?.ignored?.(path);
    }
    #childrenIgnored(path) {
        return !!this.#ignore?.childrenIgnored?.(path);
    }
    // backpressure mechanism
    pause() {
        this.paused ***REMOVED*** true;
    }
    resume() {
        /* c8 ignore start */
        if (this.signal?.aborted)
            return;
        /* c8 ignore stop */
        this.paused ***REMOVED*** false;
        let fn ***REMOVED*** undefined;
        while (!this.paused && (fn ***REMOVED*** this.#onResume.shift())) {
            fn();
        }
    }
    onResume(fn) {
        if (this.signal?.aborted)
            return;
        /* c8 ignore start */
        if (!this.paused) {
            fn();
        }
        else {
            /* c8 ignore stop */
            this.#onResume.push(fn);
        }
    }
    // do the requisite realpath/stat checking, and return the path
    // to add or undefined to filter it out.
    async matchCheck(e, ifDir) {
        if (ifDir && this.opts.nodir)
            return undefined;
        let rpc;
        if (this.opts.realpath) {
            rpc ***REMOVED*** e.realpathCached() || (await e.realpath());
            if (!rpc)
                return undefined;
            e ***REMOVED*** rpc;
        }
        const needStat ***REMOVED*** e.isUnknown() || this.opts.stat;
        const s ***REMOVED*** needStat ? await e.lstat() : e;
        if (this.opts.follow && this.opts.nodir && s?.isSymbolicLink()) {
            const target ***REMOVED*** await s.realpath();
            /* c8 ignore start */
            if (target && (target.isUnknown() || this.opts.stat)) {
                await target.lstat();
            }
            /* c8 ignore stop */
        }
        return this.matchCheckTest(s, ifDir);
    }
    matchCheckTest(e, ifDir) {
        return (e &&
            (this.maxDepth ***REMOVED******REMOVED******REMOVED*** Infinity || e.depth() <***REMOVED*** this.maxDepth) &&
            (!ifDir || e.canReaddir()) &&
            (!this.opts.nodir || !e.isDirectory()) &&
            (!this.opts.nodir ||
                !this.opts.follow ||
                !e.isSymbolicLink() ||
                !e.realpathCached()?.isDirectory()) &&
            !this.#ignored(e)) ?
            e
            : undefined;
    }
    matchCheckSync(e, ifDir) {
        if (ifDir && this.opts.nodir)
            return undefined;
        let rpc;
        if (this.opts.realpath) {
            rpc ***REMOVED*** e.realpathCached() || e.realpathSync();
            if (!rpc)
                return undefined;
            e ***REMOVED*** rpc;
        }
        const needStat ***REMOVED*** e.isUnknown() || this.opts.stat;
        const s ***REMOVED*** needStat ? e.lstatSync() : e;
        if (this.opts.follow && this.opts.nodir && s?.isSymbolicLink()) {
            const target ***REMOVED*** s.realpathSync();
            if (target && (target?.isUnknown() || this.opts.stat)) {
                target.lstatSync();
            }
        }
        return this.matchCheckTest(s, ifDir);
    }
    matchFinish(e, absolute) {
        if (this.#ignored(e))
            return;
        // we know we have an ignore if this is false, but TS doesn't
        if (!this.includeChildMatches && this.#ignore?.add) {
            const ign ***REMOVED*** `${e.relativePosix()}/**`;
            this.#ignore.add(ign);
        }
        const abs ***REMOVED*** this.opts.absolute ***REMOVED******REMOVED******REMOVED*** undefined ? absolute : this.opts.absolute;
        this.seen.add(e);
        const mark ***REMOVED*** this.opts.mark && e.isDirectory() ? this.#sep : '';
        // ok, we have what we need!
        if (this.opts.withFileTypes) {
            this.matchEmit(e);
        }
        else if (abs) {
            const abs ***REMOVED*** this.opts.posix ? e.fullpathPosix() : e.fullpath();
            this.matchEmit(abs + mark);
        }
        else {
            const rel ***REMOVED*** this.opts.posix ? e.relativePosix() : e.relative();
            const pre ***REMOVED*** this.opts.dotRelative && !rel.startsWith('..' + this.#sep) ?
                '.' + this.#sep
                : '';
            this.matchEmit(!rel ? '.' + mark : pre + rel + mark);
        }
    }
    async match(e, absolute, ifDir) {
        const p ***REMOVED*** await this.matchCheck(e, ifDir);
        if (p)
            this.matchFinish(p, absolute);
    }
    matchSync(e, absolute, ifDir) {
        const p ***REMOVED*** this.matchCheckSync(e, ifDir);
        if (p)
            this.matchFinish(p, absolute);
    }
    walkCB(target, patterns, cb) {
        /* c8 ignore start */
        if (this.signal?.aborted)
            cb();
        /* c8 ignore stop */
        this.walkCB2(target, patterns, new processor_js_1.Processor(this.opts), cb);
    }
    walkCB2(target, patterns, processor, cb) {
        if (this.#childrenIgnored(target))
            return cb();
        if (this.signal?.aborted)
            cb();
        if (this.paused) {
            this.onResume(() ***REMOVED***> this.walkCB2(target, patterns, processor, cb));
            return;
        }
        processor.processPatterns(target, patterns);
        // done processing.  all of the above is sync, can be abstracted out.
        // subwalks is a map of paths to the entry filters they need
        // matches is a map of paths to [absolute, ifDir] tuples.
        let tasks ***REMOVED*** 1;
        const next ***REMOVED*** () ***REMOVED***> {
            if (--tasks ***REMOVED******REMOVED******REMOVED*** 0)
                cb();
        };
        for (const [m, absolute, ifDir] of processor.matches.entries()) {
            if (this.#ignored(m))
                continue;
            tasks++;
            this.match(m, absolute, ifDir).then(() ***REMOVED***> next());
        }
        for (const t of processor.subwalkTargets()) {
            if (this.maxDepth !***REMOVED******REMOVED*** Infinity && t.depth() >***REMOVED*** this.maxDepth) {
                continue;
            }
            tasks++;
            const childrenCached ***REMOVED*** t.readdirCached();
            if (t.calledReaddir())
                this.walkCB3(t, childrenCached, processor, next);
            else {
                t.readdirCB((_, entries) ***REMOVED***> this.walkCB3(t, entries, processor, next), true);
            }
        }
        next();
    }
    walkCB3(target, entries, processor, cb) {
        processor ***REMOVED*** processor.filterEntries(target, entries);
        let tasks ***REMOVED*** 1;
        const next ***REMOVED*** () ***REMOVED***> {
            if (--tasks ***REMOVED******REMOVED******REMOVED*** 0)
                cb();
        };
        for (const [m, absolute, ifDir] of processor.matches.entries()) {
            if (this.#ignored(m))
                continue;
            tasks++;
            this.match(m, absolute, ifDir).then(() ***REMOVED***> next());
        }
        for (const [target, patterns] of processor.subwalks.entries()) {
            tasks++;
            this.walkCB2(target, patterns, processor.child(), next);
        }
        next();
    }
    walkCBSync(target, patterns, cb) {
        /* c8 ignore start */
        if (this.signal?.aborted)
            cb();
        /* c8 ignore stop */
        this.walkCB2Sync(target, patterns, new processor_js_1.Processor(this.opts), cb);
    }
    walkCB2Sync(target, patterns, processor, cb) {
        if (this.#childrenIgnored(target))
            return cb();
        if (this.signal?.aborted)
            cb();
        if (this.paused) {
            this.onResume(() ***REMOVED***> this.walkCB2Sync(target, patterns, processor, cb));
            return;
        }
        processor.processPatterns(target, patterns);
        // done processing.  all of the above is sync, can be abstracted out.
        // subwalks is a map of paths to the entry filters they need
        // matches is a map of paths to [absolute, ifDir] tuples.
        let tasks ***REMOVED*** 1;
        const next ***REMOVED*** () ***REMOVED***> {
            if (--tasks ***REMOVED******REMOVED******REMOVED*** 0)
                cb();
        };
        for (const [m, absolute, ifDir] of processor.matches.entries()) {
            if (this.#ignored(m))
                continue;
            this.matchSync(m, absolute, ifDir);
        }
        for (const t of processor.subwalkTargets()) {
            if (this.maxDepth !***REMOVED******REMOVED*** Infinity && t.depth() >***REMOVED*** this.maxDepth) {
                continue;
            }
            tasks++;
            const children ***REMOVED*** t.readdirSync();
            this.walkCB3Sync(t, children, processor, next);
        }
        next();
    }
    walkCB3Sync(target, entries, processor, cb) {
        processor ***REMOVED*** processor.filterEntries(target, entries);
        let tasks ***REMOVED*** 1;
        const next ***REMOVED*** () ***REMOVED***> {
            if (--tasks ***REMOVED******REMOVED******REMOVED*** 0)
                cb();
        };
        for (const [m, absolute, ifDir] of processor.matches.entries()) {
            if (this.#ignored(m))
                continue;
            this.matchSync(m, absolute, ifDir);
        }
        for (const [target, patterns] of processor.subwalks.entries()) {
            tasks++;
            this.walkCB2Sync(target, patterns, processor.child(), next);
        }
        next();
    }
}
exports.GlobUtil ***REMOVED*** GlobUtil;
class GlobWalker extends GlobUtil {
    matches ***REMOVED*** new Set();
    constructor(patterns, path, opts) {
        super(patterns, path, opts);
    }
    matchEmit(e) {
        this.matches.add(e);
    }
    async walk() {
        if (this.signal?.aborted)
            throw this.signal.reason;
        if (this.path.isUnknown()) {
            await this.path.lstat();
        }
        await new Promise((res, rej) ***REMOVED***> {
            this.walkCB(this.path, this.patterns, () ***REMOVED***> {
                if (this.signal?.aborted) {
                    rej(this.signal.reason);
                }
                else {
                    res(this.matches);
                }
            });
        });
        return this.matches;
    }
    walkSync() {
        if (this.signal?.aborted)
            throw this.signal.reason;
        if (this.path.isUnknown()) {
            this.path.lstatSync();
        }
        // nothing for the callback to do, because this never pauses
        this.walkCBSync(this.path, this.patterns, () ***REMOVED***> {
            if (this.signal?.aborted)
                throw this.signal.reason;
        });
        return this.matches;
    }
}
exports.GlobWalker ***REMOVED*** GlobWalker;
class GlobStream extends GlobUtil {
    results;
    constructor(patterns, path, opts) {
        super(patterns, path, opts);
        this.results ***REMOVED*** new minipass_1.Minipass({
            signal: this.signal,
            objectMode: true,
        });
        this.results.on('drain', () ***REMOVED***> this.resume());
        this.results.on('resume', () ***REMOVED***> this.resume());
    }
    matchEmit(e) {
        this.results.write(e);
        if (!this.results.flowing)
            this.pause();
    }
    stream() {
        const target ***REMOVED*** this.path;
        if (target.isUnknown()) {
            target.lstat().then(() ***REMOVED***> {
                this.walkCB(target, this.patterns, () ***REMOVED***> this.results.end());
            });
        }
        else {
            this.walkCB(target, this.patterns, () ***REMOVED***> this.results.end());
        }
        return this.results;
    }
    streamSync() {
        if (this.path.isUnknown()) {
            this.path.lstatSync();
        }
        this.walkCBSync(this.path, this.patterns, () ***REMOVED***> this.results.end());
        return this.results;
    }
}
exports.GlobStream ***REMOVED*** GlobStream;
//# sourceMappingURL***REMOVED***walker.js.map