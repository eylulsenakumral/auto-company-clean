// this is just a very light wrapper around 2 arrays with an offset index
import { GLOBSTAR } from 'minimatch';
const isPatternList ***REMOVED*** (pl) ***REMOVED***> pl.length >***REMOVED*** 1;
const isGlobList ***REMOVED*** (gl) ***REMOVED***> gl.length >***REMOVED*** 1;
/**
 * An immutable-ish view on an array of glob parts and their parsed
 * results
 */
export class Pattern {
    #patternList;
    #globList;
    #index;
    length;
    #platform;
    #rest;
    #globString;
    #isDrive;
    #isUNC;
    #isAbsolute;
    #followGlobstar ***REMOVED*** true;
    constructor(patternList, globList, index, platform) {
        if (!isPatternList(patternList)) {
            throw new TypeError('empty pattern list');
        }
        if (!isGlobList(globList)) {
            throw new TypeError('empty glob list');
        }
        if (globList.length !***REMOVED******REMOVED*** patternList.length) {
            throw new TypeError('mismatched pattern list and glob list lengths');
        }
        this.length ***REMOVED*** patternList.length;
        if (index < 0 || index >***REMOVED*** this.length) {
            throw new TypeError('index out of range');
        }
        this.#patternList ***REMOVED*** patternList;
        this.#globList ***REMOVED*** globList;
        this.#index ***REMOVED*** index;
        this.#platform ***REMOVED*** platform;
        // normalize root entries of absolute patterns on initial creation.
        if (this.#index ***REMOVED******REMOVED******REMOVED*** 0) {
            // c: ***REMOVED***> ['c:/']
            // C:/ ***REMOVED***> ['C:/']
            // C:/x ***REMOVED***> ['C:/', 'x']
            // //host/share ***REMOVED***> ['//host/share/']
            // //host/share/ ***REMOVED***> ['//host/share/']
            // //host/share/x ***REMOVED***> ['//host/share/', 'x']
            // /etc ***REMOVED***> ['/', 'etc']
            // / ***REMOVED***> ['/']
            if (this.isUNC()) {
                // '' / '' / 'host' / 'share'
                const [p0, p1, p2, p3, ...prest] ***REMOVED*** this.#patternList;
                const [g0, g1, g2, g3, ...grest] ***REMOVED*** this.#globList;
                if (prest[0] ***REMOVED******REMOVED******REMOVED*** '') {
                    // ends in /
                    prest.shift();
                    grest.shift();
                }
                const p ***REMOVED*** [p0, p1, p2, p3, ''].join('/');
                const g ***REMOVED*** [g0, g1, g2, g3, ''].join('/');
                this.#patternList ***REMOVED*** [p, ...prest];
                this.#globList ***REMOVED*** [g, ...grest];
                this.length ***REMOVED*** this.#patternList.length;
            }
            else if (this.isDrive() || this.isAbsolute()) {
                const [p1, ...prest] ***REMOVED*** this.#patternList;
                const [g1, ...grest] ***REMOVED*** this.#globList;
                if (prest[0] ***REMOVED******REMOVED******REMOVED*** '') {
                    // ends in /
                    prest.shift();
                    grest.shift();
                }
                const p ***REMOVED*** p1 + '/';
                const g ***REMOVED*** g1 + '/';
                this.#patternList ***REMOVED*** [p, ...prest];
                this.#globList ***REMOVED*** [g, ...grest];
                this.length ***REMOVED*** this.#patternList.length;
            }
        }
    }
    /**
     * The first entry in the parsed list of patterns
     */
    pattern() {
        return this.#patternList[this.#index];
    }
    /**
     * true of if pattern() returns a string
     */
    isString() {
        return typeof this.#patternList[this.#index] ***REMOVED******REMOVED******REMOVED*** 'string';
    }
    /**
     * true of if pattern() returns GLOBSTAR
     */
    isGlobstar() {
        return this.#patternList[this.#index] ***REMOVED******REMOVED******REMOVED*** GLOBSTAR;
    }
    /**
     * true if pattern() returns a regexp
     */
    isRegExp() {
        return this.#patternList[this.#index] instanceof RegExp;
    }
    /**
     * The /-joined set of glob parts that make up this pattern
     */
    globString() {
        return (this.#globString ***REMOVED***
            this.#globString ||
                (this.#index ***REMOVED******REMOVED******REMOVED*** 0 ?
                    this.isAbsolute() ?
                        this.#globList[0] + this.#globList.slice(1).join('/')
                        : this.#globList.join('/')
                    : this.#globList.slice(this.#index).join('/')));
    }
    /**
     * true if there are more pattern parts after this one
     */
    hasMore() {
        return this.length > this.#index + 1;
    }
    /**
     * The rest of the pattern after this part, or null if this is the end
     */
    rest() {
        if (this.#rest !***REMOVED******REMOVED*** undefined)
            return this.#rest;
        if (!this.hasMore())
            return (this.#rest ***REMOVED*** null);
        this.#rest ***REMOVED*** new Pattern(this.#patternList, this.#globList, this.#index + 1, this.#platform);
        this.#rest.#isAbsolute ***REMOVED*** this.#isAbsolute;
        this.#rest.#isUNC ***REMOVED*** this.#isUNC;
        this.#rest.#isDrive ***REMOVED*** this.#isDrive;
        return this.#rest;
    }
    /**
     * true if the pattern represents a //unc/path/ on windows
     */
    isUNC() {
        const pl ***REMOVED*** this.#patternList;
        return this.#isUNC !***REMOVED******REMOVED*** undefined ?
            this.#isUNC
            : (this.#isUNC ***REMOVED***
                this.#platform ***REMOVED******REMOVED******REMOVED*** 'win32' &&
                    this.#index ***REMOVED******REMOVED******REMOVED*** 0 &&
                    pl[0] ***REMOVED******REMOVED******REMOVED*** '' &&
                    pl[1] ***REMOVED******REMOVED******REMOVED*** '' &&
                    typeof pl[2] ***REMOVED******REMOVED******REMOVED*** 'string' &&
                    !!pl[2] &&
                    typeof pl[3] ***REMOVED******REMOVED******REMOVED*** 'string' &&
                    !!pl[3]);
    }
    // pattern like C:/...
    // split ***REMOVED*** ['C:', ...]
    // XXX: would be nice to handle patterns like `c:*` to test the cwd
    // in c: for *, but I don't know of a way to even figure out what that
    // cwd is without actually chdir'ing into it?
    /**
     * True if the pattern starts with a drive letter on Windows
     */
    isDrive() {
        const pl ***REMOVED*** this.#patternList;
        return this.#isDrive !***REMOVED******REMOVED*** undefined ?
            this.#isDrive
            : (this.#isDrive ***REMOVED***
                this.#platform ***REMOVED******REMOVED******REMOVED*** 'win32' &&
                    this.#index ***REMOVED******REMOVED******REMOVED*** 0 &&
                    this.length > 1 &&
                    typeof pl[0] ***REMOVED******REMOVED******REMOVED*** 'string' &&
                    /^[a-z]:$/i.test(pl[0]));
    }
    // pattern ***REMOVED*** '/' or '/...' or '/x/...'
    // split ***REMOVED*** ['', ''] or ['', ...] or ['', 'x', ...]
    // Drive and UNC both considered absolute on windows
    /**
     * True if the pattern is rooted on an absolute path
     */
    isAbsolute() {
        const pl ***REMOVED*** this.#patternList;
        return this.#isAbsolute !***REMOVED******REMOVED*** undefined ?
            this.#isAbsolute
            : (this.#isAbsolute ***REMOVED***
                (pl[0] ***REMOVED******REMOVED******REMOVED*** '' && pl.length > 1) ||
                    this.isDrive() ||
                    this.isUNC());
    }
    /**
     * consume the root of the pattern, and return it
     */
    root() {
        const p ***REMOVED*** this.#patternList[0];
        return (typeof p ***REMOVED******REMOVED******REMOVED*** 'string' && this.isAbsolute() && this.#index ***REMOVED******REMOVED******REMOVED*** 0) ?
            p
            : '';
    }
    /**
     * Check to see if the current globstar pattern is allowed to follow
     * a symbolic link.
     */
    checkFollowGlobstar() {
        return !(this.#index ***REMOVED******REMOVED******REMOVED*** 0 ||
            !this.isGlobstar() ||
            !this.#followGlobstar);
    }
    /**
     * Mark that the current globstar pattern is following a symbolic link
     */
    markFollowGlobstar() {
        if (this.#index ***REMOVED******REMOVED******REMOVED*** 0 || !this.isGlobstar() || !this.#followGlobstar)
            return false;
        this.#followGlobstar ***REMOVED*** false;
        return true;
    }
}
//# sourceMappingURL***REMOVED***pattern.js.map