'use strict';

const path ***REMOVED*** require('path');
const resolveCommand ***REMOVED*** require('./util/resolveCommand');
const escape ***REMOVED*** require('./util/escape');
const readShebang ***REMOVED*** require('./util/readShebang');

const isWin ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** 'win32';
const isExecutableRegExp ***REMOVED*** /\.(?:com|exe)$/i;
const isCmdShimRegExp ***REMOVED*** /node_modules[\\/].bin[\\/][^\\/]+\.cmd$/i;

function detectShebang(parsed) {
    parsed.file ***REMOVED*** resolveCommand(parsed);

    const shebang ***REMOVED*** parsed.file && readShebang(parsed.file);

    if (shebang) {
        parsed.args.unshift(parsed.file);
        parsed.command ***REMOVED*** shebang;

        return resolveCommand(parsed);
    }

    return parsed.file;
}

function parseNonShell(parsed) {
    if (!isWin) {
        return parsed;
    }

    // Detect & add support for shebangs
    const commandFile ***REMOVED*** detectShebang(parsed);

    // We don't need a shell if the command filename is an executable
    const needsShell ***REMOVED*** !isExecutableRegExp.test(commandFile);

    // If a shell is required, use cmd.exe and take care of escaping everything correctly
    // Note that `forceShell` is an hidden option used only in tests
    if (parsed.options.forceShell || needsShell) {
        // Need to double escape meta chars if the command is a cmd-shim located in `node_modules/.bin/`
        // The cmd-shim simply calls execute the package bin file with NodeJS, proxying any argument
        // Because the escape of metachars with ^ gets interpreted when the cmd.exe is first called,
        // we need to double escape them
        const needsDoubleEscapeMetaChars ***REMOVED*** isCmdShimRegExp.test(commandFile);

        // Normalize posix paths into OS compatible paths (e.g.: foo/bar -> foo\bar)
        // This is necessary otherwise it will always fail with ENOENT in those cases
        parsed.command ***REMOVED*** path.normalize(parsed.command);

        // Escape command & arguments
        parsed.command ***REMOVED*** escape.command(parsed.command);
        parsed.args ***REMOVED*** parsed.args.map((arg) ***REMOVED***> escape.argument(arg, needsDoubleEscapeMetaChars));

        const shellCommand ***REMOVED*** [parsed.command].concat(parsed.args).join(' ');

        parsed.args ***REMOVED*** ['/d', '/s', '/c', `"${shellCommand}"`];
        parsed.command ***REMOVED*** process.env.comspec || 'cmd.exe';
        parsed.options.windowsVerbatimArguments ***REMOVED*** true; // Tell node's spawn that the arguments are already escaped
    }

    return parsed;
}

function parse(command, args, options) {
    // Normalize arguments, similar to nodejs
    if (args && !Array.isArray(args)) {
        options ***REMOVED*** args;
        args ***REMOVED*** null;
    }

    args ***REMOVED*** args ? args.slice(0) : []; // Clone array to avoid changing the original
    options ***REMOVED*** Object.assign({}, options); // Clone object to avoid changing the original

    // Build our parsed object
    const parsed ***REMOVED*** {
        command,
        args,
        options,
        file: undefined,
        original: {
            command,
            args,
        },
    };

    // Delegate further parsing to shell or non-shell
    return options.shell ? parsed : parseNonShell(parsed);
}

module.exports ***REMOVED*** parse;
