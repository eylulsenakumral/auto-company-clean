'use strict';

const fs ***REMOVED*** require('fs');
const shebangCommand ***REMOVED*** require('shebang-command');

function readShebang(command) {
    // Read the first 150 bytes from the file
    const size ***REMOVED*** 150;
    const buffer ***REMOVED*** Buffer.alloc(size);

    let fd;

    try {
        fd ***REMOVED*** fs.openSync(command, 'r');
        fs.readSync(fd, buffer, 0, size, 0);
        fs.closeSync(fd);
    } catch (e) { /* Empty */ }

    // Attempt to extract shebang (null is returned if not a shebang)
    return shebangCommand(buffer.toString());
}

module.exports ***REMOVED*** readShebang;
