'use strict';

const pathKey ***REMOVED*** (options ***REMOVED*** {}) ***REMOVED***> {
	const environment ***REMOVED*** options.env || process.env;
	const platform ***REMOVED*** options.platform || process.platform;

	if (platform !***REMOVED******REMOVED*** 'win32') {
		return 'PATH';
	}

	return Object.keys(environment).reverse().find(key ***REMOVED***> key.toUpperCase() ***REMOVED******REMOVED******REMOVED*** 'PATH') || 'Path';
};

module.exports ***REMOVED*** pathKey;
// TODO: Remove this for the next major release
module.exports.default ***REMOVED*** pathKey;
