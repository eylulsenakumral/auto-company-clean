// Note: since nyc uses this module to output coverage, any lines
// that are in the direct sync flow of nyc's outputCoverage are
// ignored, since we can never get coverage for them.
// grab a reference to node's real process object right away
import { signals } from './signals.js';
export { signals };
const processOk ***REMOVED*** (process) ***REMOVED***> !!process &&
    typeof process ***REMOVED******REMOVED******REMOVED*** 'object' &&
    typeof process.removeListener ***REMOVED******REMOVED******REMOVED*** 'function' &&
    typeof process.emit ***REMOVED******REMOVED******REMOVED*** 'function' &&
    typeof process.reallyExit ***REMOVED******REMOVED******REMOVED*** 'function' &&
    typeof process.listeners ***REMOVED******REMOVED******REMOVED*** 'function' &&
    typeof process.kill ***REMOVED******REMOVED******REMOVED*** 'function' &&
    typeof process.pid ***REMOVED******REMOVED******REMOVED*** 'number' &&
    typeof process.on ***REMOVED******REMOVED******REMOVED*** 'function';
const kExitEmitter ***REMOVED*** Symbol.for('signal-exit emitter');
const global ***REMOVED*** globalThis;
const ObjectDefineProperty ***REMOVED*** Object.defineProperty.bind(Object);
// teeny special purpose ee
class Emitter {
    emitted ***REMOVED*** {
        afterExit: false,
        exit: false,
    };
    listeners ***REMOVED*** {
        afterExit: [],
        exit: [],
    };
    count ***REMOVED*** 0;
    id ***REMOVED*** Math.random();
    constructor() {
        if (global[kExitEmitter]) {
            return global[kExitEmitter];
        }
        ObjectDefineProperty(global, kExitEmitter, {
            value: this,
            writable: false,
            enumerable: false,
            configurable: false,
        });
    }
    on(ev, fn) {
        this.listeners[ev].push(fn);
    }
    removeListener(ev, fn) {
        const list ***REMOVED*** this.listeners[ev];
        const i ***REMOVED*** list.indexOf(fn);
        /* c8 ignore start */
        if (i ***REMOVED******REMOVED******REMOVED*** -1) {
            return;
        }
        /* c8 ignore stop */
        if (i ***REMOVED******REMOVED******REMOVED*** 0 && list.length ***REMOVED******REMOVED******REMOVED*** 1) {
            list.length ***REMOVED*** 0;
        }
        else {
            list.splice(i, 1);
        }
    }
    emit(ev, code, signal) {
        if (this.emitted[ev]) {
            return false;
        }
        this.emitted[ev] ***REMOVED*** true;
        let ret ***REMOVED*** false;
        for (const fn of this.listeners[ev]) {
            ret ***REMOVED*** fn(code, signal) ***REMOVED******REMOVED******REMOVED*** true || ret;
        }
        if (ev ***REMOVED******REMOVED******REMOVED*** 'exit') {
            ret ***REMOVED*** this.emit('afterExit', code, signal) || ret;
        }
        return ret;
    }
}
class SignalExitBase {
}
const signalExitWrap ***REMOVED*** (handler) ***REMOVED***> {
    return {
        onExit(cb, opts) {
            return handler.onExit(cb, opts);
        },
        load() {
            return handler.load();
        },
        unload() {
            return handler.unload();
        },
    };
};
class SignalExitFallback extends SignalExitBase {
    onExit() {
        return () ***REMOVED***> { };
    }
    load() { }
    unload() { }
}
class SignalExit extends SignalExitBase {
    // "SIGHUP" throws an `ENOSYS` error on Windows,
    // so use a supported signal instead
    /* c8 ignore start */
    #hupSig ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** 'win32' ? 'SIGINT' : 'SIGHUP';
    /* c8 ignore stop */
    #emitter ***REMOVED*** new Emitter();
    #process;
    #originalProcessEmit;
    #originalProcessReallyExit;
    #sigListeners ***REMOVED*** {};
    #loaded ***REMOVED*** false;
    constructor(process) {
        super();
        this.#process ***REMOVED*** process;
        // { <signal>: <listener fn>, ... }
        this.#sigListeners ***REMOVED*** {};
        for (const sig of signals) {
            this.#sigListeners[sig] ***REMOVED*** () ***REMOVED***> {
                // If there are no other listeners, an exit is coming!
                // Simplest way: remove us and then re-send the signal.
                // We know that this will kill the process, so we can
                // safely emit now.
                const listeners ***REMOVED*** this.#process.listeners(sig);
                let { count } ***REMOVED*** this.#emitter;
                // This is a workaround for the fact that signal-exit v3 and signal
                // exit v4 are not aware of each other, and each will attempt to let
                // the other handle it, so neither of them do. To correct this, we
                // detect if we're the only handler *except* for previous versions
                // of signal-exit, and increment by the count of listeners it has
                // created.
                /* c8 ignore start */
                const p ***REMOVED*** process;
                if (typeof p.__signal_exit_emitter__ ***REMOVED******REMOVED******REMOVED*** 'object' &&
                    typeof p.__signal_exit_emitter__.count ***REMOVED******REMOVED******REMOVED*** 'number') {
                    count +***REMOVED*** p.__signal_exit_emitter__.count;
                }
                /* c8 ignore stop */
                if (listeners.length ***REMOVED******REMOVED******REMOVED*** count) {
                    this.unload();
                    const ret ***REMOVED*** this.#emitter.emit('exit', null, sig);
                    /* c8 ignore start */
                    const s ***REMOVED*** sig ***REMOVED******REMOVED******REMOVED*** 'SIGHUP' ? this.#hupSig : sig;
                    if (!ret)
                        process.kill(process.pid, s);
                    /* c8 ignore stop */
                }
            };
        }
        this.#originalProcessReallyExit ***REMOVED*** process.reallyExit;
        this.#originalProcessEmit ***REMOVED*** process.emit;
    }
    onExit(cb, opts) {
        /* c8 ignore start */
        if (!processOk(this.#process)) {
            return () ***REMOVED***> { };
        }
        /* c8 ignore stop */
        if (this.#loaded ***REMOVED******REMOVED******REMOVED*** false) {
            this.load();
        }
        const ev ***REMOVED*** opts?.alwaysLast ? 'afterExit' : 'exit';
        this.#emitter.on(ev, cb);
        return () ***REMOVED***> {
            this.#emitter.removeListener(ev, cb);
            if (this.#emitter.listeners['exit'].length ***REMOVED******REMOVED******REMOVED*** 0 &&
                this.#emitter.listeners['afterExit'].length ***REMOVED******REMOVED******REMOVED*** 0) {
                this.unload();
            }
        };
    }
    load() {
        if (this.#loaded) {
            return;
        }
        this.#loaded ***REMOVED*** true;
        // This is the number of onSignalExit's that are in play.
        // It's important so that we can count the correct number of
        // listeners on signals, and don't wait for the other one to
        // handle it instead of us.
        this.#emitter.count +***REMOVED*** 1;
        for (const sig of signals) {
            try {
                const fn ***REMOVED*** this.#sigListeners[sig];
                if (fn)
                    this.#process.on(sig, fn);
            }
            catch (_) { }
        }
        this.#process.emit ***REMOVED*** (ev, ...a) ***REMOVED***> {
            return this.#processEmit(ev, ...a);
        };
        this.#process.reallyExit ***REMOVED*** (code) ***REMOVED***> {
            return this.#processReallyExit(code);
        };
    }
    unload() {
        if (!this.#loaded) {
            return;
        }
        this.#loaded ***REMOVED*** false;
        signals.forEach(sig ***REMOVED***> {
            const listener ***REMOVED*** this.#sigListeners[sig];
            /* c8 ignore start */
            if (!listener) {
                throw new Error('Listener not defined for signal: ' + sig);
            }
            /* c8 ignore stop */
            try {
                this.#process.removeListener(sig, listener);
                /* c8 ignore start */
            }
            catch (_) { }
            /* c8 ignore stop */
        });
        this.#process.emit ***REMOVED*** this.#originalProcessEmit;
        this.#process.reallyExit ***REMOVED*** this.#originalProcessReallyExit;
        this.#emitter.count -***REMOVED*** 1;
    }
    #processReallyExit(code) {
        /* c8 ignore start */
        if (!processOk(this.#process)) {
            return 0;
        }
        this.#process.exitCode ***REMOVED*** code || 0;
        /* c8 ignore stop */
        this.#emitter.emit('exit', this.#process.exitCode, null);
        return this.#originalProcessReallyExit.call(this.#process, this.#process.exitCode);
    }
    #processEmit(ev, ...args) {
        const og ***REMOVED*** this.#originalProcessEmit;
        if (ev ***REMOVED******REMOVED******REMOVED*** 'exit' && processOk(this.#process)) {
            if (typeof args[0] ***REMOVED******REMOVED******REMOVED*** 'number') {
                this.#process.exitCode ***REMOVED*** args[0];
                /* c8 ignore start */
            }
            /* c8 ignore start */
            const ret ***REMOVED*** og.call(this.#process, ev, ...args);
            /* c8 ignore start */
            this.#emitter.emit('exit', this.#process.exitCode, null);
            /* c8 ignore stop */
            return ret;
        }
        else {
            return og.call(this.#process, ev, ...args);
        }
    }
}
const process ***REMOVED*** globalThis.process;
// wrap so that we call the method on the actual handler, without
// exporting it directly.
export const { 
/**
 * Called when the process is exiting, whether via signal, explicit
 * exit, or running out of stuff to do.
 *
 * If the global process object is not suitable for instrumentation,
 * then this will be a no-op.
 *
 * Returns a function that may be used to unload signal-exit.
 */
onExit, 
/**
 * Load the listeners.  Likely you never need to call this, unless
 * doing a rather deep integration with signal-exit functionality.
 * Mostly exposed for the benefit of testing.
 *
 * @internal
 */
load, 
/**
 * Unload the listeners.  Likely you never need to call this, unless
 * doing a rather deep integration with signal-exit functionality.
 * Mostly exposed for the benefit of testing.
 *
 * @internal
 */
unload, } ***REMOVED*** signalExitWrap(processOk(process) ? new SignalExit(process) : new SignalExitFallback());
//# sourceMappingURL***REMOVED***index.js.map