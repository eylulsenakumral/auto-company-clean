/*! *****************************************************************************
Copyright (c) Microsoft Corporation. All rights reserved.
Licensed under the Apache License, Version 2.0 (the "License"); you may not use
this file except in compliance with the License. You may obtain a copy of the
License at http://www.apache.org/licenses/LICENSE-2.0

THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
MERCHANTABLITY OR NON-INFRINGEMENT.

See the Apache Version 2.0 License for specific language governing permissions
and limitations under the License.
***************************************************************************** */


"use strict";

// src/compiler/corePublic.ts
var versionMajorMinor ***REMOVED*** "5.9";
var version ***REMOVED*** "5.9.3";

// src/compiler/core.ts
var emptyArray ***REMOVED*** [];
var emptyMap ***REMOVED*** /* @__PURE__ */ new Map();
function length(array) {
  return array !***REMOVED******REMOVED*** void 0 ? array.length : 0;
}
function forEach(array, callback) {
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const result ***REMOVED*** callback(array[i], i);
      if (result) {
        return result;
      }
    }
  }
  return void 0;
}
function firstDefined(array, callback) {
  if (array ***REMOVED******REMOVED******REMOVED*** void 0) {
    return void 0;
  }
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    const result ***REMOVED*** callback(array[i], i);
    if (result !***REMOVED******REMOVED*** void 0) {
      return result;
    }
  }
  return void 0;
}
function firstDefinedIterator(iter, callback) {
  for (const value of iter) {
    const result ***REMOVED*** callback(value);
    if (result !***REMOVED******REMOVED*** void 0) {
      return result;
    }
  }
  return void 0;
}
function reduceLeftIterator(iterator, f, initial) {
  let result ***REMOVED*** initial;
  if (iterator) {
    let pos ***REMOVED*** 0;
    for (const value of iterator) {
      result ***REMOVED*** f(result, value, pos);
      pos++;
    }
  }
  return result;
}
function zipWith(arrayA, arrayB, callback) {
  const result ***REMOVED*** [];
  Debug.assertEqual(arrayA.length, arrayB.length);
  for (let i ***REMOVED*** 0; i < arrayA.length; i++) {
    result.push(callback(arrayA[i], arrayB[i], i));
  }
  return result;
}
function every(array, callback) {
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      if (!callback(array[i], i)) {
        return false;
      }
    }
  }
  return true;
}
function find(array, predicate, startIndex) {
  if (array ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
  for (let i ***REMOVED*** startIndex ?? 0; i < array.length; i++) {
    const value ***REMOVED*** array[i];
    if (predicate(value, i)) {
      return value;
    }
  }
  return void 0;
}
function findLast(array, predicate, startIndex) {
  if (array ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
  for (let i ***REMOVED*** startIndex ?? array.length - 1; i >***REMOVED*** 0; i--) {
    const value ***REMOVED*** array[i];
    if (predicate(value, i)) {
      return value;
    }
  }
  return void 0;
}
function findIndex(array, predicate, startIndex) {
  if (array ***REMOVED******REMOVED******REMOVED*** void 0) return -1;
  for (let i ***REMOVED*** startIndex ?? 0; i < array.length; i++) {
    if (predicate(array[i], i)) {
      return i;
    }
  }
  return -1;
}
function findLastIndex(array, predicate, startIndex) {
  if (array ***REMOVED******REMOVED******REMOVED*** void 0) return -1;
  for (let i ***REMOVED*** startIndex ?? array.length - 1; i >***REMOVED*** 0; i--) {
    if (predicate(array[i], i)) {
      return i;
    }
  }
  return -1;
}
function contains(array, value, equalityComparer ***REMOVED*** equateValues) {
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      if (equalityComparer(array[i], value)) {
        return true;
      }
    }
  }
  return false;
}
function indexOfAnyCharCode(text, charCodes, start) {
  for (let i ***REMOVED*** start ?? 0; i < text.length; i++) {
    if (contains(charCodes, text.charCodeAt(i))) {
      return i;
    }
  }
  return -1;
}
function countWhere(array, predicate) {
  let count ***REMOVED*** 0;
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const v ***REMOVED*** array[i];
      if (predicate(v, i)) {
        count++;
      }
    }
  }
  return count;
}
function filter(array, f) {
  if (array !***REMOVED******REMOVED*** void 0) {
    const len ***REMOVED*** array.length;
    let i ***REMOVED*** 0;
    while (i < len && f(array[i])) i++;
    if (i < len) {
      const result ***REMOVED*** array.slice(0, i);
      i++;
      while (i < len) {
        const item ***REMOVED*** array[i];
        if (f(item)) {
          result.push(item);
        }
        i++;
      }
      return result;
    }
  }
  return array;
}
function filterMutate(array, f) {
  let outIndex ***REMOVED*** 0;
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    if (f(array[i], i, array)) {
      array[outIndex] ***REMOVED*** array[i];
      outIndex++;
    }
  }
  array.length ***REMOVED*** outIndex;
}
function clear(array) {
  array.length ***REMOVED*** 0;
}
function map(array, f) {
  let result;
  if (array !***REMOVED******REMOVED*** void 0) {
    result ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      result.push(f(array[i], i));
    }
  }
  return result;
}
function* mapIterator(iter, mapFn) {
  for (const x of iter) {
    yield mapFn(x);
  }
}
function sameMap(array, f) {
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const item ***REMOVED*** array[i];
      const mapped ***REMOVED*** f(item, i);
      if (item !***REMOVED******REMOVED*** mapped) {
        const result ***REMOVED*** array.slice(0, i);
        result.push(mapped);
        for (i++; i < array.length; i++) {
          result.push(f(array[i], i));
        }
        return result;
      }
    }
  }
  return array;
}
function flatten(array) {
  const result ***REMOVED*** [];
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    const v ***REMOVED*** array[i];
    if (v) {
      if (isArray(v)) {
        addRange(result, v);
      } else {
        result.push(v);
      }
    }
  }
  return result;
}
function flatMap(array, mapfn) {
  let result;
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const v ***REMOVED*** mapfn(array[i], i);
      if (v) {
        if (isArray(v)) {
          result ***REMOVED*** addRange(result, v);
        } else {
          result ***REMOVED*** append(result, v);
        }
      }
    }
  }
  return result ?? emptyArray;
}
function flatMapToMutable(array, mapfn) {
  const result ***REMOVED*** [];
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const v ***REMOVED*** mapfn(array[i], i);
      if (v) {
        if (isArray(v)) {
          addRange(result, v);
        } else {
          result.push(v);
        }
      }
    }
  }
  return result;
}
function sameFlatMap(array, mapfn) {
  let result;
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const item ***REMOVED*** array[i];
      const mapped ***REMOVED*** mapfn(item, i);
      if (result || item !***REMOVED******REMOVED*** mapped || isArray(mapped)) {
        if (!result) {
          result ***REMOVED*** array.slice(0, i);
        }
        if (isArray(mapped)) {
          addRange(result, mapped);
        } else {
          result.push(mapped);
        }
      }
    }
  }
  return result ?? array;
}
function mapDefined(array, mapFn) {
  const result ***REMOVED*** [];
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const mapped ***REMOVED*** mapFn(array[i], i);
      if (mapped !***REMOVED******REMOVED*** void 0) {
        result.push(mapped);
      }
    }
  }
  return result;
}
function* mapDefinedIterator(iter, mapFn) {
  for (const x of iter) {
    const value ***REMOVED*** mapFn(x);
    if (value !***REMOVED******REMOVED*** void 0) {
      yield value;
    }
  }
}
function getOrUpdate(map2, key, callback) {
  if (map2.has(key)) {
    return map2.get(key);
  }
  const value ***REMOVED*** callback();
  map2.set(key, value);
  return value;
}
function tryAddToSet(set, value) {
  if (!set.has(value)) {
    set.add(value);
    return true;
  }
  return false;
}
function spanMap(array, keyfn, mapfn) {
  let result;
  if (array !***REMOVED******REMOVED*** void 0) {
    result ***REMOVED*** [];
    const len ***REMOVED*** array.length;
    let previousKey;
    let key;
    let start ***REMOVED*** 0;
    let pos ***REMOVED*** 0;
    while (start < len) {
      while (pos < len) {
        const value ***REMOVED*** array[pos];
        key ***REMOVED*** keyfn(value, pos);
        if (pos ***REMOVED******REMOVED******REMOVED*** 0) {
          previousKey ***REMOVED*** key;
        } else if (key !***REMOVED******REMOVED*** previousKey) {
          break;
        }
        pos++;
      }
      if (start < pos) {
        const v ***REMOVED*** mapfn(array.slice(start, pos), previousKey, start, pos);
        if (v) {
          result.push(v);
        }
        start ***REMOVED*** pos;
      }
      previousKey ***REMOVED*** key;
      pos++;
    }
  }
  return result;
}
function some(array, predicate) {
  if (array !***REMOVED******REMOVED*** void 0) {
    if (predicate !***REMOVED******REMOVED*** void 0) {
      for (let i ***REMOVED*** 0; i < array.length; i++) {
        if (predicate(array[i])) {
          return true;
        }
      }
    } else {
      return array.length > 0;
    }
  }
  return false;
}
function getRangesWhere(arr, pred, cb) {
  let start;
  for (let i ***REMOVED*** 0; i < arr.length; i++) {
    if (pred(arr[i])) {
      start ***REMOVED*** start ***REMOVED******REMOVED******REMOVED*** void 0 ? i : start;
    } else {
      if (start !***REMOVED******REMOVED*** void 0) {
        cb(start, i);
        start ***REMOVED*** void 0;
      }
    }
  }
  if (start !***REMOVED******REMOVED*** void 0) cb(start, arr.length);
}
function concatenate(array1, array2) {
  if (array2 ***REMOVED******REMOVED******REMOVED*** void 0 || array2.length ***REMOVED******REMOVED******REMOVED*** 0) return array1;
  if (array1 ***REMOVED******REMOVED******REMOVED*** void 0 || array1.length ***REMOVED******REMOVED******REMOVED*** 0) return array2;
  return [...array1, ...array2];
}
function selectIndex(_, i) {
  return i;
}
function indicesOf(array) {
  return array.map(selectIndex);
}
function deduplicateRelational(array, equalityComparer, comparer) {
  const indices ***REMOVED*** indicesOf(array);
  stableSortIndices(array, indices, comparer);
  let last2 ***REMOVED*** array[indices[0]];
  const deduplicated ***REMOVED*** [indices[0]];
  for (let i ***REMOVED*** 1; i < indices.length; i++) {
    const index ***REMOVED*** indices[i];
    const item ***REMOVED*** array[index];
    if (!equalityComparer(last2, item)) {
      deduplicated.push(index);
      last2 ***REMOVED*** item;
    }
  }
  deduplicated.sort();
  return deduplicated.map((i) ***REMOVED***> array[i]);
}
function deduplicateEquality(array, equalityComparer) {
  const result ***REMOVED*** [];
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    pushIfUnique(result, array[i], equalityComparer);
  }
  return result;
}
function deduplicate(array, equalityComparer, comparer) {
  return array.length ***REMOVED******REMOVED******REMOVED*** 0 ? [] : array.length ***REMOVED******REMOVED******REMOVED*** 1 ? array.slice() : comparer ? deduplicateRelational(array, equalityComparer, comparer) : deduplicateEquality(array, equalityComparer);
}
function deduplicateSorted(array, comparer) {
  if (array.length ***REMOVED******REMOVED******REMOVED*** 0) return emptyArray;
  let last2 ***REMOVED*** array[0];
  const deduplicated ***REMOVED*** [last2];
  for (let i ***REMOVED*** 1; i < array.length; i++) {
    const next ***REMOVED*** array[i];
    switch (comparer(next, last2)) {
      // equality comparison
      case true:
      // relational comparison
      // falls through
      case 0 /* EqualTo */:
        continue;
      case -1 /* LessThan */:
        return Debug.fail("Array is unsorted.");
    }
    deduplicated.push(last2 ***REMOVED*** next);
  }
  return deduplicated;
}
function insertSorted(array, insert, compare, equalityComparer, allowDuplicates) {
  if (array.length ***REMOVED******REMOVED******REMOVED*** 0) {
    array.push(insert);
    return true;
  }
  const insertIndex ***REMOVED*** binarySearch(array, insert, identity, compare);
  if (insertIndex < 0) {
    if (equalityComparer && !allowDuplicates) {
      const idx ***REMOVED*** ~insertIndex;
      if (idx > 0 && equalityComparer(insert, array[idx - 1])) {
        return false;
      }
      if (idx < array.length && equalityComparer(insert, array[idx])) {
        array.splice(idx, 1, insert);
        return true;
      }
    }
    array.splice(~insertIndex, 0, insert);
    return true;
  }
  if (allowDuplicates) {
    array.splice(insertIndex, 0, insert);
    return true;
  }
  return false;
}
function sortAndDeduplicate(array, comparer, equalityComparer) {
  return deduplicateSorted(toSorted(array, comparer), equalityComparer ?? comparer ?? compareStringsCaseSensitive);
}
function arrayIsEqualTo(array1, array2, equalityComparer ***REMOVED*** equateValues) {
  if (array1 ***REMOVED******REMOVED******REMOVED*** void 0 || array2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return array1 ***REMOVED******REMOVED******REMOVED*** array2;
  }
  if (array1.length !***REMOVED******REMOVED*** array2.length) {
    return false;
  }
  for (let i ***REMOVED*** 0; i < array1.length; i++) {
    if (!equalityComparer(array1[i], array2[i], i)) {
      return false;
    }
  }
  return true;
}
function compact(array) {
  let result;
  if (array !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < array.length; i++) {
      const v ***REMOVED*** array[i];
      if (result ?? !v) {
        result ?? (result ***REMOVED*** array.slice(0, i));
        if (v) {
          result.push(v);
        }
      }
    }
  }
  return result ?? array;
}
function relativeComplement(arrayA, arrayB, comparer) {
  if (!arrayB || !arrayA || arrayB.length ***REMOVED******REMOVED******REMOVED*** 0 || arrayA.length ***REMOVED******REMOVED******REMOVED*** 0) return arrayB;
  const result ***REMOVED*** [];
  loopB:
    for (let offsetA ***REMOVED*** 0, offsetB ***REMOVED*** 0; offsetB < arrayB.length; offsetB++) {
      if (offsetB > 0) {
        Debug.assertGreaterThanOrEqual(comparer(arrayB[offsetB], arrayB[offsetB - 1]), 0 /* EqualTo */);
      }
      loopA:
        for (const startA ***REMOVED*** offsetA; offsetA < arrayA.length; offsetA++) {
          if (offsetA > startA) {
            Debug.assertGreaterThanOrEqual(comparer(arrayA[offsetA], arrayA[offsetA - 1]), 0 /* EqualTo */);
          }
          switch (comparer(arrayB[offsetB], arrayA[offsetA])) {
            case -1 /* LessThan */:
              result.push(arrayB[offsetB]);
              continue loopB;
            case 0 /* EqualTo */:
              continue loopB;
            case 1 /* GreaterThan */:
              continue loopA;
          }
        }
    }
  return result;
}
function append(to, value) {
  if (value ***REMOVED******REMOVED******REMOVED*** void 0) return to;
  if (to ***REMOVED******REMOVED******REMOVED*** void 0) return [value];
  to.push(value);
  return to;
}
function toOffset(array, offset) {
  return offset < 0 ? array.length + offset : offset;
}
function addRange(to, from, start, end) {
  if (from ***REMOVED******REMOVED******REMOVED*** void 0 || from.length ***REMOVED******REMOVED******REMOVED*** 0) return to;
  if (to ***REMOVED******REMOVED******REMOVED*** void 0) return from.slice(start, end);
  start ***REMOVED*** start ***REMOVED******REMOVED******REMOVED*** void 0 ? 0 : toOffset(from, start);
  end ***REMOVED*** end ***REMOVED******REMOVED******REMOVED*** void 0 ? from.length : toOffset(from, end);
  for (let i ***REMOVED*** start; i < end && i < from.length; i++) {
    if (from[i] !***REMOVED******REMOVED*** void 0) {
      to.push(from[i]);
    }
  }
  return to;
}
function pushIfUnique(array, toAdd, equalityComparer) {
  if (contains(array, toAdd, equalityComparer)) {
    return false;
  } else {
    array.push(toAdd);
    return true;
  }
}
function appendIfUnique(array, toAdd, equalityComparer) {
  if (array !***REMOVED******REMOVED*** void 0) {
    pushIfUnique(array, toAdd, equalityComparer);
    return array;
  } else {
    return [toAdd];
  }
}
function stableSortIndices(array, indices, comparer) {
  indices.sort((x, y) ***REMOVED***> comparer(array[x], array[y]) || compareValues(x, y));
}
function toSorted(array, comparer) {
  return array.length ***REMOVED******REMOVED******REMOVED*** 0 ? emptyArray : array.slice().sort(comparer);
}
function rangeEquals(array1, array2, pos, end) {
  while (pos < end) {
    if (array1[pos] !***REMOVED******REMOVED*** array2[pos]) {
      return false;
    }
    pos++;
  }
  return true;
}
var elementAt ***REMOVED*** !!Array.prototype.at ? (array, offset) ***REMOVED***> array ***REMOVED******REMOVED*** null ? void 0 : array.at(offset) : (array, offset) ***REMOVED***> {
  if (array !***REMOVED******REMOVED*** void 0) {
    offset ***REMOVED*** toOffset(array, offset);
    if (offset < array.length) {
      return array[offset];
    }
  }
  return void 0;
};
function firstOrUndefined(array) {
  return array ***REMOVED******REMOVED******REMOVED*** void 0 || array.length ***REMOVED******REMOVED******REMOVED*** 0 ? void 0 : array[0];
}
function firstOrUndefinedIterator(iter) {
  if (iter !***REMOVED******REMOVED*** void 0) {
    for (const value of iter) {
      return value;
    }
  }
  return void 0;
}
function first(array) {
  Debug.assert(array.length !***REMOVED******REMOVED*** 0);
  return array[0];
}
function firstIterator(iter) {
  for (const value of iter) {
    return value;
  }
  Debug.fail("iterator is empty");
}
function lastOrUndefined(array) {
  return array ***REMOVED******REMOVED******REMOVED*** void 0 || array.length ***REMOVED******REMOVED******REMOVED*** 0 ? void 0 : array[array.length - 1];
}
function last(array) {
  Debug.assert(array.length !***REMOVED******REMOVED*** 0);
  return array[array.length - 1];
}
function singleOrUndefined(array) {
  return array !***REMOVED******REMOVED*** void 0 && array.length ***REMOVED******REMOVED******REMOVED*** 1 ? array[0] : void 0;
}
function singleOrMany(array) {
  return array !***REMOVED******REMOVED*** void 0 && array.length ***REMOVED******REMOVED******REMOVED*** 1 ? array[0] : array;
}
function replaceElement(array, index, value) {
  const result ***REMOVED*** array.slice(0);
  result[index] ***REMOVED*** value;
  return result;
}
function binarySearch(array, value, keySelector, keyComparer, offset) {
  return binarySearchKey(array, keySelector(value), keySelector, keyComparer, offset);
}
function binarySearchKey(array, key, keySelector, keyComparer, offset) {
  if (!some(array)) {
    return -1;
  }
  let low ***REMOVED*** offset ?? 0;
  let high ***REMOVED*** array.length - 1;
  while (low <***REMOVED*** high) {
    const middle ***REMOVED*** low + (high - low >> 1);
    const midKey ***REMOVED*** keySelector(array[middle], middle);
    switch (keyComparer(midKey, key)) {
      case -1 /* LessThan */:
        low ***REMOVED*** middle + 1;
        break;
      case 0 /* EqualTo */:
        return middle;
      case 1 /* GreaterThan */:
        high ***REMOVED*** middle - 1;
        break;
    }
  }
  return ~low;
}
function reduceLeft(array, f, initial, start, count) {
  if (array && array.length > 0) {
    const size ***REMOVED*** array.length;
    if (size > 0) {
      let pos ***REMOVED*** start ***REMOVED******REMOVED******REMOVED*** void 0 || start < 0 ? 0 : start;
      const end ***REMOVED*** count ***REMOVED******REMOVED******REMOVED*** void 0 || pos + count > size - 1 ? size - 1 : pos + count;
      let result;
      if (arguments.length <***REMOVED*** 2) {
        result ***REMOVED*** array[pos];
        pos++;
      } else {
        result ***REMOVED*** initial;
      }
      while (pos <***REMOVED*** end) {
        result ***REMOVED*** f(result, array[pos], pos);
        pos++;
      }
      return result;
    }
  }
  return initial;
}
var hasOwnProperty ***REMOVED*** Object.prototype.hasOwnProperty;
function hasProperty(map2, key) {
  return hasOwnProperty.call(map2, key);
}
function getOwnKeys(map2) {
  const keys ***REMOVED*** [];
  for (const key in map2) {
    if (hasOwnProperty.call(map2, key)) {
      keys.push(key);
    }
  }
  return keys;
}
function getOwnValues(collection) {
  const values ***REMOVED*** [];
  for (const key in collection) {
    if (hasOwnProperty.call(collection, key)) {
      values.push(collection[key]);
    }
  }
  return values;
}
function arrayOf(count, f) {
  const result ***REMOVED*** new Array(count);
  for (let i ***REMOVED*** 0; i < count; i++) {
    result[i] ***REMOVED*** f(i);
  }
  return result;
}
function arrayFrom(iterator, map2) {
  const result ***REMOVED*** [];
  for (const value of iterator) {
    result.push(map2 ? map2(value) : value);
  }
  return result;
}
function assign(t, ...args) {
  for (const arg of args) {
    if (arg ***REMOVED******REMOVED******REMOVED*** void 0) continue;
    for (const p in arg) {
      if (hasProperty(arg, p)) {
        t[p] ***REMOVED*** arg[p];
      }
    }
  }
  return t;
}
function equalOwnProperties(left, right, equalityComparer ***REMOVED*** equateValues) {
  if (left ***REMOVED******REMOVED******REMOVED*** right) return true;
  if (!left || !right) return false;
  for (const key in left) {
    if (hasOwnProperty.call(left, key)) {
      if (!hasOwnProperty.call(right, key)) return false;
      if (!equalityComparer(left[key], right[key])) return false;
    }
  }
  for (const key in right) {
    if (hasOwnProperty.call(right, key)) {
      if (!hasOwnProperty.call(left, key)) return false;
    }
  }
  return true;
}
function arrayToMap(array, makeKey, makeValue ***REMOVED*** identity) {
  const result ***REMOVED*** /* @__PURE__ */ new Map();
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    const value ***REMOVED*** array[i];
    const key ***REMOVED*** makeKey(value);
    if (key !***REMOVED******REMOVED*** void 0) result.set(key, makeValue(value));
  }
  return result;
}
function arrayToMultiMap(values, makeKey, makeValue ***REMOVED*** identity) {
  const result ***REMOVED*** createMultiMap();
  for (let i ***REMOVED*** 0; i < values.length; i++) {
    const value ***REMOVED*** values[i];
    result.add(makeKey(value), makeValue(value));
  }
  return result;
}
function group(values, getGroupId, resultSelector ***REMOVED*** identity) {
  return arrayFrom(arrayToMultiMap(values, getGroupId).values(), resultSelector);
}
function groupBy(values, keySelector) {
  const result ***REMOVED*** {};
  if (values !***REMOVED******REMOVED*** void 0) {
    for (let i ***REMOVED*** 0; i < values.length; i++) {
      const value ***REMOVED*** values[i];
      const key ***REMOVED*** `${keySelector(value)}`;
      const array ***REMOVED*** result[key] ?? (result[key] ***REMOVED*** []);
      array.push(value);
    }
  }
  return result;
}
function extend(first2, second) {
  const result ***REMOVED*** {};
  for (const id in second) {
    if (hasOwnProperty.call(second, id)) {
      result[id] ***REMOVED*** second[id];
    }
  }
  for (const id in first2) {
    if (hasOwnProperty.call(first2, id)) {
      result[id] ***REMOVED*** first2[id];
    }
  }
  return result;
}
function copyProperties(first2, second) {
  for (const id in second) {
    if (hasOwnProperty.call(second, id)) {
      first2[id] ***REMOVED*** second[id];
    }
  }
}
function maybeBind(obj, fn) {
  return fn ***REMOVED******REMOVED*** null ? void 0 : fn.bind(obj);
}
function createMultiMap() {
  const map2 ***REMOVED*** /* @__PURE__ */ new Map();
  map2.add ***REMOVED*** multiMapAdd;
  map2.remove ***REMOVED*** multiMapRemove;
  return map2;
}
function multiMapAdd(key, value) {
  let values ***REMOVED*** this.get(key);
  if (values !***REMOVED******REMOVED*** void 0) {
    values.push(value);
  } else {
    this.set(key, values ***REMOVED*** [value]);
  }
  return values;
}
function multiMapRemove(key, value) {
  const values ***REMOVED*** this.get(key);
  if (values !***REMOVED******REMOVED*** void 0) {
    unorderedRemoveItem(values, value);
    if (!values.length) {
      this.delete(key);
    }
  }
}
function createQueue(items) {
  const elements ***REMOVED*** (items ***REMOVED******REMOVED*** null ? void 0 : items.slice()) ?? [];
  let headIndex ***REMOVED*** 0;
  function isEmpty() {
    return headIndex ***REMOVED******REMOVED******REMOVED*** elements.length;
  }
  function enqueue(...items2) {
    elements.push(...items2);
  }
  function dequeue() {
    if (isEmpty()) {
      throw new Error("Queue is empty");
    }
    const result ***REMOVED*** elements[headIndex];
    elements[headIndex] ***REMOVED*** void 0;
    headIndex++;
    if (headIndex > 100 && headIndex > elements.length >> 1) {
      const newLength ***REMOVED*** elements.length - headIndex;
      elements.copyWithin(
        /*target*/
        0,
        /*start*/
        headIndex
      );
      elements.length ***REMOVED*** newLength;
      headIndex ***REMOVED*** 0;
    }
    return result;
  }
  return {
    enqueue,
    dequeue,
    isEmpty
  };
}
function isArray(value) {
  return Array.isArray(value);
}
function toArray(value) {
  return isArray(value) ? value : [value];
}
function isString(text) {
  return typeof text ***REMOVED******REMOVED******REMOVED*** "string";
}
function isNumber(x) {
  return typeof x ***REMOVED******REMOVED******REMOVED*** "number";
}
function tryCast(value, test) {
  return value !***REMOVED******REMOVED*** void 0 && test(value) ? value : void 0;
}
function cast(value, test) {
  if (value !***REMOVED******REMOVED*** void 0 && test(value)) return value;
  return Debug.fail(`Invalid cast. The supplied value ${value} did not pass the test '${Debug.getFunctionName(test)}'.`);
}
function noop(_) {
}
function returnFalse() {
  return false;
}
function returnTrue() {
  return true;
}
function returnUndefined() {
  return void 0;
}
function identity(x) {
  return x;
}
function toLowerCase(x) {
  return x.toLowerCase();
}
var fileNameLowerCaseRegExp ***REMOVED*** /[^\u0130\u0131\u00DFa-z0-9\\/:\-_. ]+/g;
function toFileNameLowerCase(x) {
  return fileNameLowerCaseRegExp.test(x) ? x.replace(fileNameLowerCaseRegExp, toLowerCase) : x;
}
function notImplemented() {
  throw new Error("Not implemented");
}
function memoize(callback) {
  let value;
  return () ***REMOVED***> {
    if (callback) {
      value ***REMOVED*** callback();
      callback ***REMOVED*** void 0;
    }
    return value;
  };
}
function memoizeOne(callback) {
  const map2 ***REMOVED*** /* @__PURE__ */ new Map();
  return (arg) ***REMOVED***> {
    const key ***REMOVED*** `${typeof arg}:${arg}`;
    let value ***REMOVED*** map2.get(key);
    if (value ***REMOVED******REMOVED******REMOVED*** void 0 && !map2.has(key)) {
      value ***REMOVED*** callback(arg);
      map2.set(key, value);
    }
    return value;
  };
}
function equateValues(a, b) {
  return a ***REMOVED******REMOVED******REMOVED*** b;
}
function equateStringsCaseInsensitive(a, b) {
  return a ***REMOVED******REMOVED******REMOVED*** b || a !***REMOVED******REMOVED*** void 0 && b !***REMOVED******REMOVED*** void 0 && a.toUpperCase() ***REMOVED******REMOVED******REMOVED*** b.toUpperCase();
}
function equateStringsCaseSensitive(a, b) {
  return equateValues(a, b);
}
function compareComparableValues(a, b) {
  return a ***REMOVED******REMOVED******REMOVED*** b ? 0 /* EqualTo */ : a ***REMOVED******REMOVED******REMOVED*** void 0 ? -1 /* LessThan */ : b ***REMOVED******REMOVED******REMOVED*** void 0 ? 1 /* GreaterThan */ : a < b ? -1 /* LessThan */ : 1 /* GreaterThan */;
}
function compareValues(a, b) {
  return compareComparableValues(a, b);
}
function maxBy(arr, init, mapper) {
  for (let i ***REMOVED*** 0; i < arr.length; i++) {
    init ***REMOVED*** Math.max(init, mapper(arr[i]));
  }
  return init;
}
function min(items, compare) {
  return reduceLeft(items, (x, y) ***REMOVED***> compare(x, y) ***REMOVED******REMOVED******REMOVED*** -1 /* LessThan */ ? x : y);
}
function compareStringsCaseInsensitive(a, b) {
  if (a ***REMOVED******REMOVED******REMOVED*** b) return 0 /* EqualTo */;
  if (a ***REMOVED******REMOVED******REMOVED*** void 0) return -1 /* LessThan */;
  if (b ***REMOVED******REMOVED******REMOVED*** void 0) return 1 /* GreaterThan */;
  a ***REMOVED*** a.toUpperCase();
  b ***REMOVED*** b.toUpperCase();
  return a < b ? -1 /* LessThan */ : a > b ? 1 /* GreaterThan */ : 0 /* EqualTo */;
}
function compareStringsCaseSensitive(a, b) {
  return compareComparableValues(a, b);
}
function getStringComparer(ignoreCase) {
  return ignoreCase ? compareStringsCaseInsensitive : compareStringsCaseSensitive;
}
var uiComparerCaseSensitive;
var uiLocale;
function setUILocale(value) {
  if (uiLocale !***REMOVED******REMOVED*** value) {
    uiLocale ***REMOVED*** value;
    uiComparerCaseSensitive ***REMOVED*** void 0;
  }
}
function compareBooleans(a, b) {
  return compareValues(a ? 1 : 0, b ? 1 : 0);
}
function getSpellingSuggestion(name, candidates, getName) {
  const maximumLengthDifference ***REMOVED*** Math.max(2, Math.floor(name.length * 0.34));
  let bestDistance ***REMOVED*** Math.floor(name.length * 0.4) + 1;
  let bestCandidate;
  for (const candidate of candidates) {
    const candidateName ***REMOVED*** getName(candidate);
    if (candidateName !***REMOVED******REMOVED*** void 0 && Math.abs(candidateName.length - name.length) <***REMOVED*** maximumLengthDifference) {
      if (candidateName ***REMOVED******REMOVED******REMOVED*** name) {
        continue;
      }
      if (candidateName.length < 3 && candidateName.toLowerCase() !***REMOVED******REMOVED*** name.toLowerCase()) {
        continue;
      }
      const distance ***REMOVED*** levenshteinWithMax(name, candidateName, bestDistance - 0.1);
      if (distance ***REMOVED******REMOVED******REMOVED*** void 0) {
        continue;
      }
      Debug.assert(distance < bestDistance);
      bestDistance ***REMOVED*** distance;
      bestCandidate ***REMOVED*** candidate;
    }
  }
  return bestCandidate;
}
function levenshteinWithMax(s1, s2, max) {
  let previous ***REMOVED*** new Array(s2.length + 1);
  let current ***REMOVED*** new Array(s2.length + 1);
  const big ***REMOVED*** max + 0.01;
  for (let i ***REMOVED*** 0; i <***REMOVED*** s2.length; i++) {
    previous[i] ***REMOVED*** i;
  }
  for (let i ***REMOVED*** 1; i <***REMOVED*** s1.length; i++) {
    const c1 ***REMOVED*** s1.charCodeAt(i - 1);
    const minJ ***REMOVED*** Math.ceil(i > max ? i - max : 1);
    const maxJ ***REMOVED*** Math.floor(s2.length > max + i ? max + i : s2.length);
    current[0] ***REMOVED*** i;
    let colMin ***REMOVED*** i;
    for (let j ***REMOVED*** 1; j < minJ; j++) {
      current[j] ***REMOVED*** big;
    }
    for (let j ***REMOVED*** minJ; j <***REMOVED*** maxJ; j++) {
      const substitutionDistance ***REMOVED*** s1[i - 1].toLowerCase() ***REMOVED******REMOVED******REMOVED*** s2[j - 1].toLowerCase() ? previous[j - 1] + 0.1 : previous[j - 1] + 2;
      const dist ***REMOVED*** c1 ***REMOVED******REMOVED******REMOVED*** s2.charCodeAt(j - 1) ? previous[j - 1] : Math.min(
        /*delete*/
        previous[j] + 1,
        /*insert*/
        current[j - 1] + 1,
        /*substitute*/
        substitutionDistance
      );
      current[j] ***REMOVED*** dist;
      colMin ***REMOVED*** Math.min(colMin, dist);
    }
    for (let j ***REMOVED*** maxJ + 1; j <***REMOVED*** s2.length; j++) {
      current[j] ***REMOVED*** big;
    }
    if (colMin > max) {
      return void 0;
    }
    const temp ***REMOVED*** previous;
    previous ***REMOVED*** current;
    current ***REMOVED*** temp;
  }
  const res ***REMOVED*** previous[s2.length];
  return res > max ? void 0 : res;
}
function endsWith(str, suffix, ignoreCase) {
  const expectedPos ***REMOVED*** str.length - suffix.length;
  return expectedPos >***REMOVED*** 0 && (ignoreCase ? equateStringsCaseInsensitive(str.slice(expectedPos), suffix) : str.indexOf(suffix, expectedPos) ***REMOVED******REMOVED******REMOVED*** expectedPos);
}
function removeSuffix(str, suffix) {
  return endsWith(str, suffix) ? str.slice(0, str.length - suffix.length) : str;
}
function orderedRemoveItem(array, item) {
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    if (array[i] ***REMOVED******REMOVED******REMOVED*** item) {
      orderedRemoveItemAt(array, i);
      return true;
    }
  }
  return false;
}
function orderedRemoveItemAt(array, index) {
  for (let i ***REMOVED*** index; i < array.length - 1; i++) {
    array[i] ***REMOVED*** array[i + 1];
  }
  array.pop();
}
function unorderedRemoveItemAt(array, index) {
  array[index] ***REMOVED*** array[array.length - 1];
  array.pop();
}
function unorderedRemoveItem(array, item) {
  return unorderedRemoveFirstItemWhere(array, (element) ***REMOVED***> element ***REMOVED******REMOVED******REMOVED*** item);
}
function unorderedRemoveFirstItemWhere(array, predicate) {
  for (let i ***REMOVED*** 0; i < array.length; i++) {
    if (predicate(array[i])) {
      unorderedRemoveItemAt(array, i);
      return true;
    }
  }
  return false;
}
function createGetCanonicalFileName(useCaseSensitiveFileNames2) {
  return useCaseSensitiveFileNames2 ? identity : toFileNameLowerCase;
}
function patternText({ prefix, suffix }) {
  return `${prefix}*${suffix}`;
}
function matchedText(pattern, candidate) {
  Debug.assert(isPatternMatch(pattern, candidate));
  return candidate.substring(pattern.prefix.length, candidate.length - pattern.suffix.length);
}
function findBestPatternMatch(values, getPattern, candidate) {
  let matchedValue;
  let longestMatchPrefixLength ***REMOVED*** -1;
  for (let i ***REMOVED*** 0; i < values.length; i++) {
    const v ***REMOVED*** values[i];
    const pattern ***REMOVED*** getPattern(v);
    if (pattern.prefix.length > longestMatchPrefixLength && isPatternMatch(pattern, candidate)) {
      longestMatchPrefixLength ***REMOVED*** pattern.prefix.length;
      matchedValue ***REMOVED*** v;
    }
  }
  return matchedValue;
}
function startsWith(str, prefix, ignoreCase) {
  return ignoreCase ? equateStringsCaseInsensitive(str.slice(0, prefix.length), prefix) : str.lastIndexOf(prefix, 0) ***REMOVED******REMOVED******REMOVED*** 0;
}
function removePrefix(str, prefix) {
  return startsWith(str, prefix) ? str.substr(prefix.length) : str;
}
function isPatternMatch({ prefix, suffix }, candidate) {
  return candidate.length >***REMOVED*** prefix.length + suffix.length && startsWith(candidate, prefix) && endsWith(candidate, suffix);
}
function and(f, g) {
  return (arg) ***REMOVED***> f(arg) && g(arg);
}
function or(...fs) {
  return (...args) ***REMOVED***> {
    let lastResult;
    for (const f of fs) {
      lastResult ***REMOVED*** f(...args);
      if (lastResult) {
        return lastResult;
      }
    }
    return lastResult;
  };
}
function not(fn) {
  return (...args) ***REMOVED***> !fn(...args);
}
function assertType(_) {
}
function singleElementArray(t) {
  return t ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : [t];
}
function enumerateInsertsAndDeletes(newItems, oldItems, comparer, inserted, deleted, unchanged) {
  unchanged ?? (unchanged ***REMOVED*** noop);
  let newIndex ***REMOVED*** 0;
  let oldIndex ***REMOVED*** 0;
  const newLen ***REMOVED*** newItems.length;
  const oldLen ***REMOVED*** oldItems.length;
  let hasChanges ***REMOVED*** false;
  while (newIndex < newLen && oldIndex < oldLen) {
    const newItem ***REMOVED*** newItems[newIndex];
    const oldItem ***REMOVED*** oldItems[oldIndex];
    const compareResult ***REMOVED*** comparer(newItem, oldItem);
    if (compareResult ***REMOVED******REMOVED******REMOVED*** -1 /* LessThan */) {
      inserted(newItem);
      newIndex++;
      hasChanges ***REMOVED*** true;
    } else if (compareResult ***REMOVED******REMOVED******REMOVED*** 1 /* GreaterThan */) {
      deleted(oldItem);
      oldIndex++;
      hasChanges ***REMOVED*** true;
    } else {
      unchanged(oldItem, newItem);
      newIndex++;
      oldIndex++;
    }
  }
  while (newIndex < newLen) {
    inserted(newItems[newIndex++]);
    hasChanges ***REMOVED*** true;
  }
  while (oldIndex < oldLen) {
    deleted(oldItems[oldIndex++]);
    hasChanges ***REMOVED*** true;
  }
  return hasChanges;
}
function cartesianProduct(arrays) {
  const result ***REMOVED*** [];
  cartesianProductWorker(
    arrays,
    result,
    /*outer*/
    void 0,
    0
  );
  return result;
}
function cartesianProductWorker(arrays, result, outer, index) {
  for (const element of arrays[index]) {
    let inner;
    if (outer) {
      inner ***REMOVED*** outer.slice();
      inner.push(element);
    } else {
      inner ***REMOVED*** [element];
    }
    if (index ***REMOVED******REMOVED******REMOVED*** arrays.length - 1) {
      result.push(inner);
    } else {
      cartesianProductWorker(arrays, result, inner, index + 1);
    }
  }
}
function takeWhile(array, predicate) {
  if (array !***REMOVED******REMOVED*** void 0) {
    const len ***REMOVED*** array.length;
    let index ***REMOVED*** 0;
    while (index < len && predicate(array[index])) {
      index++;
    }
    return array.slice(0, index);
  }
}
function skipWhile(array, predicate) {
  if (array !***REMOVED******REMOVED*** void 0) {
    const len ***REMOVED*** array.length;
    let index ***REMOVED*** 0;
    while (index < len && predicate(array[index])) {
      index++;
    }
    return array.slice(index);
  }
}
function isNodeLikeSystem() {
  return typeof process !***REMOVED******REMOVED*** "undefined" && !!process.nextTick && !process.browser && typeof require !***REMOVED******REMOVED*** "undefined";
}

// src/compiler/debug.ts
var Debug;
((Debug2) ***REMOVED***> {
  let currentAssertionLevel ***REMOVED*** 0 /* None */;
  Debug2.currentLogLevel ***REMOVED*** 2 /* Warning */;
  Debug2.isDebugging ***REMOVED*** false;
  function shouldLog(level) {
    return Debug2.currentLogLevel <***REMOVED*** level;
  }
  Debug2.shouldLog ***REMOVED*** shouldLog;
  function logMessage(level, s) {
    if (Debug2.loggingHost && shouldLog(level)) {
      Debug2.loggingHost.log(level, s);
    }
  }
  function log(s) {
    logMessage(3 /* Info */, s);
  }
  Debug2.log ***REMOVED*** log;
  ((_log) ***REMOVED***> {
    function error(s) {
      logMessage(1 /* Error */, s);
    }
    _log.error ***REMOVED*** error;
    function warn(s) {
      logMessage(2 /* Warning */, s);
    }
    _log.warn ***REMOVED*** warn;
    function log2(s) {
      logMessage(3 /* Info */, s);
    }
    _log.log ***REMOVED*** log2;
    function trace2(s) {
      logMessage(4 /* Verbose */, s);
    }
    _log.trace ***REMOVED*** trace2;
  })(log ***REMOVED*** Debug2.log || (Debug2.log ***REMOVED*** {}));
  const assertionCache ***REMOVED*** {};
  function getAssertionLevel() {
    return currentAssertionLevel;
  }
  Debug2.getAssertionLevel ***REMOVED*** getAssertionLevel;
  function setAssertionLevel(level) {
    const prevAssertionLevel ***REMOVED*** currentAssertionLevel;
    currentAssertionLevel ***REMOVED*** level;
    if (level > prevAssertionLevel) {
      for (const key of getOwnKeys(assertionCache)) {
        const cachedFunc ***REMOVED*** assertionCache[key];
        if (cachedFunc !***REMOVED******REMOVED*** void 0 && Debug2[key] !***REMOVED******REMOVED*** cachedFunc.assertion && level >***REMOVED*** cachedFunc.level) {
          Debug2[key] ***REMOVED*** cachedFunc;
          assertionCache[key] ***REMOVED*** void 0;
        }
      }
    }
  }
  Debug2.setAssertionLevel ***REMOVED*** setAssertionLevel;
  function shouldAssert(level) {
    return currentAssertionLevel >***REMOVED*** level;
  }
  Debug2.shouldAssert ***REMOVED*** shouldAssert;
  function shouldAssertFunction(level, name) {
    if (!shouldAssert(level)) {
      assertionCache[name] ***REMOVED*** { level, assertion: Debug2[name] };
      Debug2[name] ***REMOVED*** noop;
      return false;
    }
    return true;
  }
  function fail(message, stackCrawlMark) {
    debugger;
    const e ***REMOVED*** new Error(message ? `Debug Failure. ${message}` : "Debug Failure.");
    if (Error.captureStackTrace) {
      Error.captureStackTrace(e, stackCrawlMark || fail);
    }
    throw e;
  }
  Debug2.fail ***REMOVED*** fail;
  function failBadSyntaxKind(node, message, stackCrawlMark) {
    return fail(
      `${message || "Unexpected node."}\r
Node ${formatSyntaxKind(node.kind)} was unexpected.`,
      stackCrawlMark || failBadSyntaxKind
    );
  }
  Debug2.failBadSyntaxKind ***REMOVED*** failBadSyntaxKind;
  function assert(expression, message, verboseDebugInfo, stackCrawlMark) {
    if (!expression) {
      message ***REMOVED*** message ? `False expression: ${message}` : "False expression.";
      if (verboseDebugInfo) {
        message +***REMOVED*** "\r\nVerbose Debug Information: " + (typeof verboseDebugInfo ***REMOVED******REMOVED******REMOVED*** "string" ? verboseDebugInfo : verboseDebugInfo());
      }
      fail(message, stackCrawlMark || assert);
    }
  }
  Debug2.assert ***REMOVED*** assert;
  function assertEqual(a, b, msg, msg2, stackCrawlMark) {
    if (a !***REMOVED******REMOVED*** b) {
      const message ***REMOVED*** msg ? msg2 ? `${msg} ${msg2}` : msg : "";
      fail(`Expected ${a} ***REMOVED******REMOVED******REMOVED*** ${b}. ${message}`, stackCrawlMark || assertEqual);
    }
  }
  Debug2.assertEqual ***REMOVED*** assertEqual;
  function assertLessThan(a, b, msg, stackCrawlMark) {
    if (a >***REMOVED*** b) {
      fail(`Expected ${a} < ${b}. ${msg || ""}`, stackCrawlMark || assertLessThan);
    }
  }
  Debug2.assertLessThan ***REMOVED*** assertLessThan;
  function assertLessThanOrEqual(a, b, stackCrawlMark) {
    if (a > b) {
      fail(`Expected ${a} <***REMOVED*** ${b}`, stackCrawlMark || assertLessThanOrEqual);
    }
  }
  Debug2.assertLessThanOrEqual ***REMOVED*** assertLessThanOrEqual;
  function assertGreaterThanOrEqual(a, b, stackCrawlMark) {
    if (a < b) {
      fail(`Expected ${a} >***REMOVED*** ${b}`, stackCrawlMark || assertGreaterThanOrEqual);
    }
  }
  Debug2.assertGreaterThanOrEqual ***REMOVED*** assertGreaterThanOrEqual;
  function assertIsDefined(value, message, stackCrawlMark) {
    if (value ***REMOVED******REMOVED******REMOVED*** void 0 || value ***REMOVED******REMOVED******REMOVED*** null) {
      fail(message, stackCrawlMark || assertIsDefined);
    }
  }
  Debug2.assertIsDefined ***REMOVED*** assertIsDefined;
  function checkDefined(value, message, stackCrawlMark) {
    assertIsDefined(value, message, stackCrawlMark || checkDefined);
    return value;
  }
  Debug2.checkDefined ***REMOVED*** checkDefined;
  function assertEachIsDefined(value, message, stackCrawlMark) {
    for (const v of value) {
      assertIsDefined(v, message, stackCrawlMark || assertEachIsDefined);
    }
  }
  Debug2.assertEachIsDefined ***REMOVED*** assertEachIsDefined;
  function checkEachDefined(value, message, stackCrawlMark) {
    assertEachIsDefined(value, message, stackCrawlMark || checkEachDefined);
    return value;
  }
  Debug2.checkEachDefined ***REMOVED*** checkEachDefined;
  function assertNever(member, message ***REMOVED*** "Illegal value:", stackCrawlMark) {
    const detail ***REMOVED*** typeof member ***REMOVED******REMOVED******REMOVED*** "object" && hasProperty(member, "kind") && hasProperty(member, "pos") ? "SyntaxKind: " + formatSyntaxKind(member.kind) : JSON.stringify(member);
    return fail(`${message} ${detail}`, stackCrawlMark || assertNever);
  }
  Debug2.assertNever ***REMOVED*** assertNever;
  function assertEachNode(nodes, test, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertEachNode")) {
      assert(
        test ***REMOVED******REMOVED******REMOVED*** void 0 || every(nodes, test),
        message || "Unexpected node.",
        () ***REMOVED***> `Node array did not pass test '${getFunctionName(test)}'.`,
        stackCrawlMark || assertEachNode
      );
    }
  }
  Debug2.assertEachNode ***REMOVED*** assertEachNode;
  function assertNode(node, test, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertNode")) {
      assert(
        node !***REMOVED******REMOVED*** void 0 && (test ***REMOVED******REMOVED******REMOVED*** void 0 || test(node)),
        message || "Unexpected node.",
        () ***REMOVED***> `Node ${formatSyntaxKind(node ***REMOVED******REMOVED*** null ? void 0 : node.kind)} did not pass test '${getFunctionName(test)}'.`,
        stackCrawlMark || assertNode
      );
    }
  }
  Debug2.assertNode ***REMOVED*** assertNode;
  function assertNotNode(node, test, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertNotNode")) {
      assert(
        node ***REMOVED******REMOVED******REMOVED*** void 0 || test ***REMOVED******REMOVED******REMOVED*** void 0 || !test(node),
        message || "Unexpected node.",
        () ***REMOVED***> `Node ${formatSyntaxKind(node.kind)} should not have passed test '${getFunctionName(test)}'.`,
        stackCrawlMark || assertNotNode
      );
    }
  }
  Debug2.assertNotNode ***REMOVED*** assertNotNode;
  function assertOptionalNode(node, test, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertOptionalNode")) {
      assert(
        test ***REMOVED******REMOVED******REMOVED*** void 0 || node ***REMOVED******REMOVED******REMOVED*** void 0 || test(node),
        message || "Unexpected node.",
        () ***REMOVED***> `Node ${formatSyntaxKind(node ***REMOVED******REMOVED*** null ? void 0 : node.kind)} did not pass test '${getFunctionName(test)}'.`,
        stackCrawlMark || assertOptionalNode
      );
    }
  }
  Debug2.assertOptionalNode ***REMOVED*** assertOptionalNode;
  function assertOptionalToken(node, kind, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertOptionalToken")) {
      assert(
        kind ***REMOVED******REMOVED******REMOVED*** void 0 || node ***REMOVED******REMOVED******REMOVED*** void 0 || node.kind ***REMOVED******REMOVED******REMOVED*** kind,
        message || "Unexpected node.",
        () ***REMOVED***> `Node ${formatSyntaxKind(node ***REMOVED******REMOVED*** null ? void 0 : node.kind)} was not a '${formatSyntaxKind(kind)}' token.`,
        stackCrawlMark || assertOptionalToken
      );
    }
  }
  Debug2.assertOptionalToken ***REMOVED*** assertOptionalToken;
  function assertMissingNode(node, message, stackCrawlMark) {
    if (shouldAssertFunction(1 /* Normal */, "assertMissingNode")) {
      assert(
        node ***REMOVED******REMOVED******REMOVED*** void 0,
        message || "Unexpected node.",
        () ***REMOVED***> `Node ${formatSyntaxKind(node.kind)} was unexpected'.`,
        stackCrawlMark || assertMissingNode
      );
    }
  }
  Debug2.assertMissingNode ***REMOVED*** assertMissingNode;
  function type(_value) {
  }
  Debug2.type ***REMOVED*** type;
  function getFunctionName(func) {
    if (typeof func !***REMOVED******REMOVED*** "function") {
      return "";
    } else if (hasProperty(func, "name")) {
      return func.name;
    } else {
      const text ***REMOVED*** Function.prototype.toString.call(func);
      const match ***REMOVED*** /^function\s+([\w$]+)\s*\(/.exec(text);
      return match ? match[1] : "";
    }
  }
  Debug2.getFunctionName ***REMOVED*** getFunctionName;
  function formatSymbol(symbol) {
    return `{ name: ${unescapeLeadingUnderscores(symbol.escapedName)}; flags: ${formatSymbolFlags(symbol.flags)}; declarations: ${map(symbol.declarations, (node) ***REMOVED***> formatSyntaxKind(node.kind))} }`;
  }
  Debug2.formatSymbol ***REMOVED*** formatSymbol;
  function formatEnum(value ***REMOVED*** 0, enumObject, isFlags) {
    const members ***REMOVED*** getEnumMembers(enumObject);
    if (value ***REMOVED******REMOVED******REMOVED*** 0) {
      return members.length > 0 && members[0][0] ***REMOVED******REMOVED******REMOVED*** 0 ? members[0][1] : "0";
    }
    if (isFlags) {
      const result ***REMOVED*** [];
      let remainingFlags ***REMOVED*** value;
      for (const [enumValue, enumName] of members) {
        if (enumValue > value) {
          break;
        }
        if (enumValue !***REMOVED******REMOVED*** 0 && enumValue & value) {
          result.push(enumName);
          remainingFlags &***REMOVED*** ~enumValue;
        }
      }
      if (remainingFlags ***REMOVED******REMOVED******REMOVED*** 0) {
        return result.join("|");
      }
    } else {
      for (const [enumValue, enumName] of members) {
        if (enumValue ***REMOVED******REMOVED******REMOVED*** value) {
          return enumName;
        }
      }
    }
    return value.toString();
  }
  Debug2.formatEnum ***REMOVED*** formatEnum;
  const enumMemberCache ***REMOVED*** /* @__PURE__ */ new Map();
  function getEnumMembers(enumObject) {
    const existing ***REMOVED*** enumMemberCache.get(enumObject);
    if (existing) {
      return existing;
    }
    const result ***REMOVED*** [];
    for (const name in enumObject) {
      const value ***REMOVED*** enumObject[name];
      if (typeof value ***REMOVED******REMOVED******REMOVED*** "number") {
        result.push([value, name]);
      }
    }
    const sorted ***REMOVED*** toSorted(result, (x, y) ***REMOVED***> compareValues(x[0], y[0]));
    enumMemberCache.set(enumObject, sorted);
    return sorted;
  }
  function formatSyntaxKind(kind) {
    return formatEnum(
      kind,
      SyntaxKind,
      /*isFlags*/
      false
    );
  }
  Debug2.formatSyntaxKind ***REMOVED*** formatSyntaxKind;
  function formatSnippetKind(kind) {
    return formatEnum(
      kind,
      SnippetKind,
      /*isFlags*/
      false
    );
  }
  Debug2.formatSnippetKind ***REMOVED*** formatSnippetKind;
  function formatScriptKind(kind) {
    return formatEnum(
      kind,
      ScriptKind,
      /*isFlags*/
      false
    );
  }
  Debug2.formatScriptKind ***REMOVED*** formatScriptKind;
  function formatNodeFlags(flags) {
    return formatEnum(
      flags,
      NodeFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatNodeFlags ***REMOVED*** formatNodeFlags;
  function formatNodeCheckFlags(flags) {
    return formatEnum(
      flags,
      NodeCheckFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatNodeCheckFlags ***REMOVED*** formatNodeCheckFlags;
  function formatModifierFlags(flags) {
    return formatEnum(
      flags,
      ModifierFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatModifierFlags ***REMOVED*** formatModifierFlags;
  function formatTransformFlags(flags) {
    return formatEnum(
      flags,
      TransformFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatTransformFlags ***REMOVED*** formatTransformFlags;
  function formatEmitFlags(flags) {
    return formatEnum(
      flags,
      EmitFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatEmitFlags ***REMOVED*** formatEmitFlags;
  function formatSymbolFlags(flags) {
    return formatEnum(
      flags,
      SymbolFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatSymbolFlags ***REMOVED*** formatSymbolFlags;
  function formatTypeFlags(flags) {
    return formatEnum(
      flags,
      TypeFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatTypeFlags ***REMOVED*** formatTypeFlags;
  function formatSignatureFlags(flags) {
    return formatEnum(
      flags,
      SignatureFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatSignatureFlags ***REMOVED*** formatSignatureFlags;
  function formatObjectFlags(flags) {
    return formatEnum(
      flags,
      ObjectFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatObjectFlags ***REMOVED*** formatObjectFlags;
  function formatFlowFlags(flags) {
    return formatEnum(
      flags,
      FlowFlags,
      /*isFlags*/
      true
    );
  }
  Debug2.formatFlowFlags ***REMOVED*** formatFlowFlags;
  function formatRelationComparisonResult(result) {
    return formatEnum(
      result,
      RelationComparisonResult,
      /*isFlags*/
      true
    );
  }
  Debug2.formatRelationComparisonResult ***REMOVED*** formatRelationComparisonResult;
  function formatCheckMode(mode) {
    return formatEnum(
      mode,
      CheckMode,
      /*isFlags*/
      true
    );
  }
  Debug2.formatCheckMode ***REMOVED*** formatCheckMode;
  function formatSignatureCheckMode(mode) {
    return formatEnum(
      mode,
      SignatureCheckMode,
      /*isFlags*/
      true
    );
  }
  Debug2.formatSignatureCheckMode ***REMOVED*** formatSignatureCheckMode;
  function formatTypeFacts(facts) {
    return formatEnum(
      facts,
      TypeFacts,
      /*isFlags*/
      true
    );
  }
  Debug2.formatTypeFacts ***REMOVED*** formatTypeFacts;
  let isDebugInfoEnabled ***REMOVED*** false;
  let flowNodeProto;
  function attachFlowNodeDebugInfoWorker(flowNode) {
    if (!("__debugFlowFlags" in flowNode)) {
      Object.defineProperties(flowNode, {
        // for use with vscode-js-debug's new customDescriptionGenerator in launch.json
        __tsDebuggerDisplay: {
          value() {
            const flowHeader ***REMOVED*** this.flags & 2 /* Start */ ? "FlowStart" : this.flags & 4 /* BranchLabel */ ? "FlowBranchLabel" : this.flags & 8 /* LoopLabel */ ? "FlowLoopLabel" : this.flags & 16 /* Assignment */ ? "FlowAssignment" : this.flags & 32 /* TrueCondition */ ? "FlowTrueCondition" : this.flags & 64 /* FalseCondition */ ? "FlowFalseCondition" : this.flags & 128 /* SwitchClause */ ? "FlowSwitchClause" : this.flags & 256 /* ArrayMutation */ ? "FlowArrayMutation" : this.flags & 512 /* Call */ ? "FlowCall" : this.flags & 1024 /* ReduceLabel */ ? "FlowReduceLabel" : this.flags & 1 /* Unreachable */ ? "FlowUnreachable" : "UnknownFlow";
            const remainingFlags ***REMOVED*** this.flags & ~(2048 /* Referenced */ - 1);
            return `${flowHeader}${remainingFlags ? ` (${formatFlowFlags(remainingFlags)})` : ""}`;
          }
        },
        __debugFlowFlags: {
          get() {
            return formatEnum(
              this.flags,
              FlowFlags,
              /*isFlags*/
              true
            );
          }
        },
        __debugToString: {
          value() {
            return formatControlFlowGraph(this);
          }
        }
      });
    }
  }
  function attachFlowNodeDebugInfo(flowNode) {
    if (isDebugInfoEnabled) {
      if (typeof Object.setPrototypeOf ***REMOVED******REMOVED******REMOVED*** "function") {
        if (!flowNodeProto) {
          flowNodeProto ***REMOVED*** Object.create(Object.prototype);
          attachFlowNodeDebugInfoWorker(flowNodeProto);
        }
        Object.setPrototypeOf(flowNode, flowNodeProto);
      } else {
        attachFlowNodeDebugInfoWorker(flowNode);
      }
    }
    return flowNode;
  }
  Debug2.attachFlowNodeDebugInfo ***REMOVED*** attachFlowNodeDebugInfo;
  let nodeArrayProto;
  function attachNodeArrayDebugInfoWorker(array) {
    if (!("__tsDebuggerDisplay" in array)) {
      Object.defineProperties(array, {
        __tsDebuggerDisplay: {
          value(defaultValue) {
            defaultValue ***REMOVED*** String(defaultValue).replace(/(?:,[\s\w]+:[^,]+)+\]$/, "]");
            return `NodeArray ${defaultValue}`;
          }
        }
      });
    }
  }
  function attachNodeArrayDebugInfo(array) {
    if (isDebugInfoEnabled) {
      if (typeof Object.setPrototypeOf ***REMOVED******REMOVED******REMOVED*** "function") {
        if (!nodeArrayProto) {
          nodeArrayProto ***REMOVED*** Object.create(Array.prototype);
          attachNodeArrayDebugInfoWorker(nodeArrayProto);
        }
        Object.setPrototypeOf(array, nodeArrayProto);
      } else {
        attachNodeArrayDebugInfoWorker(array);
      }
    }
  }
  Debug2.attachNodeArrayDebugInfo ***REMOVED*** attachNodeArrayDebugInfo;
  function enableDebugInfo() {
    if (isDebugInfoEnabled) return;
    const weakTypeTextMap ***REMOVED*** /* @__PURE__ */ new WeakMap();
    const weakNodeTextMap ***REMOVED*** /* @__PURE__ */ new WeakMap();
    Object.defineProperties(objectAllocator.getSymbolConstructor().prototype, {
      // for use with vscode-js-debug's new customDescriptionGenerator in launch.json
      __tsDebuggerDisplay: {
        value() {
          const symbolHeader ***REMOVED*** this.flags & 33554432 /* Transient */ ? "TransientSymbol" : "Symbol";
          const remainingSymbolFlags ***REMOVED*** this.flags & ~33554432 /* Transient */;
          return `${symbolHeader} '${symbolName(this)}'${remainingSymbolFlags ? ` (${formatSymbolFlags(remainingSymbolFlags)})` : ""}`;
        }
      },
      __debugFlags: {
        get() {
          return formatSymbolFlags(this.flags);
        }
      }
    });
    Object.defineProperties(objectAllocator.getTypeConstructor().prototype, {
      // for use with vscode-js-debug's new customDescriptionGenerator in launch.json
      __tsDebuggerDisplay: {
        value() {
          const typeHeader ***REMOVED*** this.flags & 67359327 /* Intrinsic */ ? `IntrinsicType ${this.intrinsicName}${this.debugIntrinsicName ? ` (${this.debugIntrinsicName})` : ""}` : this.flags & 98304 /* Nullable */ ? "NullableType" : this.flags & 384 /* StringOrNumberLiteral */ ? `LiteralType ${JSON.stringify(this.value)}` : this.flags & 2048 /* BigIntLiteral */ ? `LiteralType ${this.value.negative ? "-" : ""}${this.value.base10Value}n` : this.flags & 8192 /* UniqueESSymbol */ ? "UniqueESSymbolType" : this.flags & 32 /* Enum */ ? "EnumType" : this.flags & 1048576 /* Union */ ? "UnionType" : this.flags & 2097152 /* Intersection */ ? "IntersectionType" : this.flags & 4194304 /* Index */ ? "IndexType" : this.flags & 8388608 /* IndexedAccess */ ? "IndexedAccessType" : this.flags & 16777216 /* Conditional */ ? "ConditionalType" : this.flags & 33554432 /* Substitution */ ? "SubstitutionType" : this.flags & 262144 /* TypeParameter */ ? "TypeParameter" : this.flags & 524288 /* Object */ ? this.objectFlags & 3 /* ClassOrInterface */ ? "InterfaceType" : this.objectFlags & 4 /* Reference */ ? "TypeReference" : this.objectFlags & 8 /* Tuple */ ? "TupleType" : this.objectFlags & 16 /* Anonymous */ ? "AnonymousType" : this.objectFlags & 32 /* Mapped */ ? "MappedType" : this.objectFlags & 1024 /* ReverseMapped */ ? "ReverseMappedType" : this.objectFlags & 256 /* EvolvingArray */ ? "EvolvingArrayType" : "ObjectType" : "Type";
          const remainingObjectFlags ***REMOVED*** this.flags & 524288 /* Object */ ? this.objectFlags & ~1343 /* ObjectTypeKindMask */ : 0;
          return `${typeHeader}${this.symbol ? ` '${symbolName(this.symbol)}'` : ""}${remainingObjectFlags ? ` (${formatObjectFlags(remainingObjectFlags)})` : ""}`;
        }
      },
      __debugFlags: {
        get() {
          return formatTypeFlags(this.flags);
        }
      },
      __debugObjectFlags: {
        get() {
          return this.flags & 524288 /* Object */ ? formatObjectFlags(this.objectFlags) : "";
        }
      },
      __debugTypeToString: {
        value() {
          let text ***REMOVED*** weakTypeTextMap.get(this);
          if (text ***REMOVED******REMOVED******REMOVED*** void 0) {
            text ***REMOVED*** this.checker.typeToString(this);
            weakTypeTextMap.set(this, text);
          }
          return text;
        }
      }
    });
    Object.defineProperties(objectAllocator.getSignatureConstructor().prototype, {
      __debugFlags: {
        get() {
          return formatSignatureFlags(this.flags);
        }
      },
      __debugSignatureToString: {
        value() {
          var _a;
          return (_a ***REMOVED*** this.checker) ***REMOVED******REMOVED*** null ? void 0 : _a.signatureToString(this);
        }
      }
    });
    const nodeConstructors ***REMOVED*** [
      objectAllocator.getNodeConstructor(),
      objectAllocator.getIdentifierConstructor(),
      objectAllocator.getTokenConstructor(),
      objectAllocator.getSourceFileConstructor()
    ];
    for (const ctor of nodeConstructors) {
      if (!hasProperty(ctor.prototype, "__debugKind")) {
        Object.defineProperties(ctor.prototype, {
          // for use with vscode-js-debug's new customDescriptionGenerator in launch.json
          __tsDebuggerDisplay: {
            value() {
              const nodeHeader ***REMOVED*** isGeneratedIdentifier(this) ? "GeneratedIdentifier" : isIdentifier(this) ? `Identifier '${idText(this)}'` : isPrivateIdentifier(this) ? `PrivateIdentifier '${idText(this)}'` : isStringLiteral(this) ? `StringLiteral ${JSON.stringify(this.text.length < 10 ? this.text : this.text.slice(10) + "...")}` : isNumericLiteral(this) ? `NumericLiteral ${this.text}` : isBigIntLiteral(this) ? `BigIntLiteral ${this.text}n` : isTypeParameterDeclaration(this) ? "TypeParameterDeclaration" : isParameter(this) ? "ParameterDeclaration" : isConstructorDeclaration(this) ? "ConstructorDeclaration" : isGetAccessorDeclaration(this) ? "GetAccessorDeclaration" : isSetAccessorDeclaration(this) ? "SetAccessorDeclaration" : isCallSignatureDeclaration(this) ? "CallSignatureDeclaration" : isConstructSignatureDeclaration(this) ? "ConstructSignatureDeclaration" : isIndexSignatureDeclaration(this) ? "IndexSignatureDeclaration" : isTypePredicateNode(this) ? "TypePredicateNode" : isTypeReferenceNode(this) ? "TypeReferenceNode" : isFunctionTypeNode(this) ? "FunctionTypeNode" : isConstructorTypeNode(this) ? "ConstructorTypeNode" : isTypeQueryNode(this) ? "TypeQueryNode" : isTypeLiteralNode(this) ? "TypeLiteralNode" : isArrayTypeNode(this) ? "ArrayTypeNode" : isTupleTypeNode(this) ? "TupleTypeNode" : isOptionalTypeNode(this) ? "OptionalTypeNode" : isRestTypeNode(this) ? "RestTypeNode" : isUnionTypeNode(this) ? "UnionTypeNode" : isIntersectionTypeNode(this) ? "IntersectionTypeNode" : isConditionalTypeNode(this) ? "ConditionalTypeNode" : isInferTypeNode(this) ? "InferTypeNode" : isParenthesizedTypeNode(this) ? "ParenthesizedTypeNode" : isThisTypeNode(this) ? "ThisTypeNode" : isTypeOperatorNode(this) ? "TypeOperatorNode" : isIndexedAccessTypeNode(this) ? "IndexedAccessTypeNode" : isMappedTypeNode(this) ? "MappedTypeNode" : isLiteralTypeNode(this) ? "LiteralTypeNode" : isNamedTupleMember(this) ? "NamedTupleMember" : isImportTypeNode(this) ? "ImportTypeNode" : formatSyntaxKind(this.kind);
              return `${nodeHeader}${this.flags ? ` (${formatNodeFlags(this.flags)})` : ""}`;
            }
          },
          __debugKind: {
            get() {
              return formatSyntaxKind(this.kind);
            }
          },
          __debugNodeFlags: {
            get() {
              return formatNodeFlags(this.flags);
            }
          },
          __debugModifierFlags: {
            get() {
              return formatModifierFlags(getEffectiveModifierFlagsNoCache(this));
            }
          },
          __debugTransformFlags: {
            get() {
              return formatTransformFlags(this.transformFlags);
            }
          },
          __debugIsParseTreeNode: {
            get() {
              return isParseTreeNode(this);
            }
          },
          __debugEmitFlags: {
            get() {
              return formatEmitFlags(getEmitFlags(this));
            }
          },
          __debugGetText: {
            value(includeTrivia) {
              if (nodeIsSynthesized(this)) return "";
              let text ***REMOVED*** weakNodeTextMap.get(this);
              if (text ***REMOVED******REMOVED******REMOVED*** void 0) {
                const parseNode ***REMOVED*** getParseTreeNode(this);
                const sourceFile ***REMOVED*** parseNode && getSourceFileOfNode(parseNode);
                text ***REMOVED*** sourceFile ? getSourceTextOfNodeFromSourceFile(sourceFile, parseNode, includeTrivia) : "";
                weakNodeTextMap.set(this, text);
              }
              return text;
            }
          }
        });
      }
    }
    isDebugInfoEnabled ***REMOVED*** true;
  }
  Debug2.enableDebugInfo ***REMOVED*** enableDebugInfo;
  function formatVariance(varianceFlags) {
    const variance ***REMOVED*** varianceFlags & 7 /* VarianceMask */;
    let result ***REMOVED*** variance ***REMOVED******REMOVED******REMOVED*** 0 /* Invariant */ ? "in out" : variance ***REMOVED******REMOVED******REMOVED*** 3 /* Bivariant */ ? "[bivariant]" : variance ***REMOVED******REMOVED******REMOVED*** 2 /* Contravariant */ ? "in" : variance ***REMOVED******REMOVED******REMOVED*** 1 /* Covariant */ ? "out" : variance ***REMOVED******REMOVED******REMOVED*** 4 /* Independent */ ? "[independent]" : "";
    if (varianceFlags & 8 /* Unmeasurable */) {
      result +***REMOVED*** " (unmeasurable)";
    } else if (varianceFlags & 16 /* Unreliable */) {
      result +***REMOVED*** " (unreliable)";
    }
    return result;
  }
  Debug2.formatVariance ***REMOVED*** formatVariance;
  class DebugTypeMapper {
    __debugToString() {
      var _a;
      type(this);
      switch (this.kind) {
        case 3 /* Function */:
          return ((_a ***REMOVED*** this.debugInfo) ***REMOVED******REMOVED*** null ? void 0 : _a.call(this)) || "(function mapper)";
        case 0 /* Simple */:
          return `${this.source.__debugTypeToString()} -> ${this.target.__debugTypeToString()}`;
        case 1 /* Array */:
          return zipWith(
            this.sources,
            this.targets || map(this.sources, () ***REMOVED***> "any"),
            (s, t) ***REMOVED***> `${s.__debugTypeToString()} -> ${typeof t ***REMOVED******REMOVED******REMOVED*** "string" ? t : t.__debugTypeToString()}`
          ).join(", ");
        case 2 /* Deferred */:
          return zipWith(
            this.sources,
            this.targets,
            (s, t) ***REMOVED***> `${s.__debugTypeToString()} -> ${t().__debugTypeToString()}`
          ).join(", ");
        case 5 /* Merged */:
        case 4 /* Composite */:
          return `m1: ${this.mapper1.__debugToString().split("\n").join("\n    ")}
m2: ${this.mapper2.__debugToString().split("\n").join("\n    ")}`;
        default:
          return assertNever(this);
      }
    }
  }
  Debug2.DebugTypeMapper ***REMOVED*** DebugTypeMapper;
  function attachDebugPrototypeIfDebug(mapper) {
    if (Debug2.isDebugging) {
      return Object.setPrototypeOf(mapper, DebugTypeMapper.prototype);
    }
    return mapper;
  }
  Debug2.attachDebugPrototypeIfDebug ***REMOVED*** attachDebugPrototypeIfDebug;
  function printControlFlowGraph(flowNode) {
    return console.log(formatControlFlowGraph(flowNode));
  }
  Debug2.printControlFlowGraph ***REMOVED*** printControlFlowGraph;
  function formatControlFlowGraph(flowNode) {
    let nextDebugFlowId ***REMOVED*** -1;
    function getDebugFlowNodeId(f) {
      if (!f.id) {
        f.id ***REMOVED*** nextDebugFlowId;
        nextDebugFlowId--;
      }
      return f.id;
    }
    let BoxCharacter;
    ((BoxCharacter2) ***REMOVED***> {
      BoxCharacter2["lr"] ***REMOVED*** "\u2500";
      BoxCharacter2["ud"] ***REMOVED*** "\u2502";
      BoxCharacter2["dr"] ***REMOVED*** "\u256D";
      BoxCharacter2["dl"] ***REMOVED*** "\u256E";
      BoxCharacter2["ul"] ***REMOVED*** "\u256F";
      BoxCharacter2["ur"] ***REMOVED*** "\u2570";
      BoxCharacter2["udr"] ***REMOVED*** "\u251C";
      BoxCharacter2["udl"] ***REMOVED*** "\u2524";
      BoxCharacter2["dlr"] ***REMOVED*** "\u252C";
      BoxCharacter2["ulr"] ***REMOVED*** "\u2534";
      BoxCharacter2["udlr"] ***REMOVED*** "\u256B";
    })(BoxCharacter || (BoxCharacter ***REMOVED*** {}));
    let Connection;
    ((Connection2) ***REMOVED***> {
      Connection2[Connection2["None"] ***REMOVED*** 0] ***REMOVED*** "None";
      Connection2[Connection2["Up"] ***REMOVED*** 1] ***REMOVED*** "Up";
      Connection2[Connection2["Down"] ***REMOVED*** 2] ***REMOVED*** "Down";
      Connection2[Connection2["Left"] ***REMOVED*** 4] ***REMOVED*** "Left";
      Connection2[Connection2["Right"] ***REMOVED*** 8] ***REMOVED*** "Right";
      Connection2[Connection2["UpDown"] ***REMOVED*** 3] ***REMOVED*** "UpDown";
      Connection2[Connection2["LeftRight"] ***REMOVED*** 12] ***REMOVED*** "LeftRight";
      Connection2[Connection2["UpLeft"] ***REMOVED*** 5] ***REMOVED*** "UpLeft";
      Connection2[Connection2["UpRight"] ***REMOVED*** 9] ***REMOVED*** "UpRight";
      Connection2[Connection2["DownLeft"] ***REMOVED*** 6] ***REMOVED*** "DownLeft";
      Connection2[Connection2["DownRight"] ***REMOVED*** 10] ***REMOVED*** "DownRight";
      Connection2[Connection2["UpDownLeft"] ***REMOVED*** 7] ***REMOVED*** "UpDownLeft";
      Connection2[Connection2["UpDownRight"] ***REMOVED*** 11] ***REMOVED*** "UpDownRight";
      Connection2[Connection2["UpLeftRight"] ***REMOVED*** 13] ***REMOVED*** "UpLeftRight";
      Connection2[Connection2["DownLeftRight"] ***REMOVED*** 14] ***REMOVED*** "DownLeftRight";
      Connection2[Connection2["UpDownLeftRight"] ***REMOVED*** 15] ***REMOVED*** "UpDownLeftRight";
      Connection2[Connection2["NoChildren"] ***REMOVED*** 16] ***REMOVED*** "NoChildren";
    })(Connection || (Connection ***REMOVED*** {}));
    const hasAntecedentFlags ***REMOVED*** 16 /* Assignment */ | 96 /* Condition */ | 128 /* SwitchClause */ | 256 /* ArrayMutation */ | 512 /* Call */ | 1024 /* ReduceLabel */;
    const hasNodeFlags ***REMOVED*** 2 /* Start */ | 16 /* Assignment */ | 512 /* Call */ | 96 /* Condition */ | 256 /* ArrayMutation */;
    const links ***REMOVED*** /* @__PURE__ */ Object.create(
      /*o*/
      null
    );
    const nodes ***REMOVED*** [];
    const edges ***REMOVED*** [];
    const root ***REMOVED*** buildGraphNode(flowNode, /* @__PURE__ */ new Set());
    for (const node of nodes) {
      node.text ***REMOVED*** renderFlowNode(node.flowNode, node.circular);
      computeLevel(node);
    }
    const height ***REMOVED*** computeHeight(root);
    const columnWidths ***REMOVED*** computeColumnWidths(height);
    computeLanes(root, 0);
    return renderGraph();
    function isFlowSwitchClause(f) {
      return !!(f.flags & 128 /* SwitchClause */);
    }
    function hasAntecedents(f) {
      return !!(f.flags & 12 /* Label */) && !!f.antecedent;
    }
    function hasAntecedent(f) {
      return !!(f.flags & hasAntecedentFlags);
    }
    function hasNode(f) {
      return !!(f.flags & hasNodeFlags);
    }
    function getChildren(node) {
      const children ***REMOVED*** [];
      for (const edge of node.edges) {
        if (edge.source ***REMOVED******REMOVED******REMOVED*** node) {
          children.push(edge.target);
        }
      }
      return children;
    }
    function getParents(node) {
      const parents ***REMOVED*** [];
      for (const edge of node.edges) {
        if (edge.target ***REMOVED******REMOVED******REMOVED*** node) {
          parents.push(edge.source);
        }
      }
      return parents;
    }
    function buildGraphNode(flowNode2, seen) {
      const id ***REMOVED*** getDebugFlowNodeId(flowNode2);
      let graphNode ***REMOVED*** links[id];
      if (graphNode && seen.has(flowNode2)) {
        graphNode.circular ***REMOVED*** true;
        graphNode ***REMOVED*** {
          id: -1,
          flowNode: flowNode2,
          edges: [],
          text: "",
          lane: -1,
          endLane: -1,
          level: -1,
          circular: "circularity"
        };
        nodes.push(graphNode);
        return graphNode;
      }
      seen.add(flowNode2);
      if (!graphNode) {
        links[id] ***REMOVED*** graphNode ***REMOVED*** { id, flowNode: flowNode2, edges: [], text: "", lane: -1, endLane: -1, level: -1, circular: false };
        nodes.push(graphNode);
        if (hasAntecedents(flowNode2)) {
          for (const antecedent of flowNode2.antecedent) {
            buildGraphEdge(graphNode, antecedent, seen);
          }
        } else if (hasAntecedent(flowNode2)) {
          buildGraphEdge(graphNode, flowNode2.antecedent, seen);
        }
      }
      seen.delete(flowNode2);
      return graphNode;
    }
    function buildGraphEdge(source, antecedent, seen) {
      const target ***REMOVED*** buildGraphNode(antecedent, seen);
      const edge ***REMOVED*** { source, target };
      edges.push(edge);
      source.edges.push(edge);
      target.edges.push(edge);
    }
    function computeLevel(node) {
      if (node.level !***REMOVED******REMOVED*** -1) {
        return node.level;
      }
      let level ***REMOVED*** 0;
      for (const parent of getParents(node)) {
        level ***REMOVED*** Math.max(level, computeLevel(parent) + 1);
      }
      return node.level ***REMOVED*** level;
    }
    function computeHeight(node) {
      let height2 ***REMOVED*** 0;
      for (const child of getChildren(node)) {
        height2 ***REMOVED*** Math.max(height2, computeHeight(child));
      }
      return height2 + 1;
    }
    function computeColumnWidths(height2) {
      const columns ***REMOVED*** fill(Array(height2), 0);
      for (const node of nodes) {
        columns[node.level] ***REMOVED*** Math.max(columns[node.level], node.text.length);
      }
      return columns;
    }
    function computeLanes(node, lane) {
      if (node.lane ***REMOVED******REMOVED******REMOVED*** -1) {
        node.lane ***REMOVED*** lane;
        node.endLane ***REMOVED*** lane;
        const children ***REMOVED*** getChildren(node);
        for (let i ***REMOVED*** 0; i < children.length; i++) {
          if (i > 0) lane++;
          const child ***REMOVED*** children[i];
          computeLanes(child, lane);
          if (child.endLane > node.endLane) {
            lane ***REMOVED*** child.endLane;
          }
        }
        node.endLane ***REMOVED*** lane;
      }
    }
    function getHeader2(flags) {
      if (flags & 2 /* Start */) return "Start";
      if (flags & 4 /* BranchLabel */) return "Branch";
      if (flags & 8 /* LoopLabel */) return "Loop";
      if (flags & 16 /* Assignment */) return "Assignment";
      if (flags & 32 /* TrueCondition */) return "True";
      if (flags & 64 /* FalseCondition */) return "False";
      if (flags & 128 /* SwitchClause */) return "SwitchClause";
      if (flags & 256 /* ArrayMutation */) return "ArrayMutation";
      if (flags & 512 /* Call */) return "Call";
      if (flags & 1024 /* ReduceLabel */) return "ReduceLabel";
      if (flags & 1 /* Unreachable */) return "Unreachable";
      throw new Error();
    }
    function getNodeText(node) {
      const sourceFile ***REMOVED*** getSourceFileOfNode(node);
      return getSourceTextOfNodeFromSourceFile(
        sourceFile,
        node,
        /*includeTrivia*/
        false
      );
    }
    function renderFlowNode(flowNode2, circular) {
      let text ***REMOVED*** getHeader2(flowNode2.flags);
      if (circular) {
        text ***REMOVED*** `${text}#${getDebugFlowNodeId(flowNode2)}`;
      }
      if (isFlowSwitchClause(flowNode2)) {
        const clauses ***REMOVED*** [];
        const { switchStatement, clauseStart, clauseEnd } ***REMOVED*** flowNode2.node;
        for (let i ***REMOVED*** clauseStart; i < clauseEnd; i++) {
          const clause ***REMOVED*** switchStatement.caseBlock.clauses[i];
          if (isDefaultClause(clause)) {
            clauses.push("default");
          } else {
            clauses.push(getNodeText(clause.expression));
          }
        }
        text +***REMOVED*** ` (${clauses.join(", ")})`;
      } else if (hasNode(flowNode2)) {
        if (flowNode2.node) {
          text +***REMOVED*** ` (${getNodeText(flowNode2.node)})`;
        }
      }
      return circular ***REMOVED******REMOVED******REMOVED*** "circularity" ? `Circular(${text})` : text;
    }
    function renderGraph() {
      const columnCount ***REMOVED*** columnWidths.length;
      const laneCount ***REMOVED*** maxBy(nodes, 0, (n) ***REMOVED***> n.lane) + 1;
      const lanes ***REMOVED*** fill(Array(laneCount), "");
      const grid ***REMOVED*** columnWidths.map(() ***REMOVED***> Array(laneCount));
      const connectors ***REMOVED*** columnWidths.map(() ***REMOVED***> fill(Array(laneCount), 0));
      for (const node of nodes) {
        grid[node.level][node.lane] ***REMOVED*** node;
        const children ***REMOVED*** getChildren(node);
        for (let i ***REMOVED*** 0; i < children.length; i++) {
          const child ***REMOVED*** children[i];
          let connector ***REMOVED*** 8 /* Right */;
          if (child.lane ***REMOVED******REMOVED******REMOVED*** node.lane) connector |***REMOVED*** 4 /* Left */;
          if (i > 0) connector |***REMOVED*** 1 /* Up */;
          if (i < children.length - 1) connector |***REMOVED*** 2 /* Down */;
          connectors[node.level][child.lane] |***REMOVED*** connector;
        }
        if (children.length ***REMOVED******REMOVED******REMOVED*** 0) {
          connectors[node.level][node.lane] |***REMOVED*** 16 /* NoChildren */;
        }
        const parents ***REMOVED*** getParents(node);
        for (let i ***REMOVED*** 0; i < parents.length; i++) {
          const parent ***REMOVED*** parents[i];
          let connector ***REMOVED*** 4 /* Left */;
          if (i > 0) connector |***REMOVED*** 1 /* Up */;
          if (i < parents.length - 1) connector |***REMOVED*** 2 /* Down */;
          connectors[node.level - 1][parent.lane] |***REMOVED*** connector;
        }
      }
      for (let column ***REMOVED*** 0; column < columnCount; column++) {
        for (let lane ***REMOVED*** 0; lane < laneCount; lane++) {
          const left ***REMOVED*** column > 0 ? connectors[column - 1][lane] : 0;
          const above ***REMOVED*** lane > 0 ? connectors[column][lane - 1] : 0;
          let connector ***REMOVED*** connectors[column][lane];
          if (!connector) {
            if (left & 8 /* Right */) connector |***REMOVED*** 12 /* LeftRight */;
            if (above & 2 /* Down */) connector |***REMOVED*** 3 /* UpDown */;
            connectors[column][lane] ***REMOVED*** connector;
          }
        }
      }
      for (let column ***REMOVED*** 0; column < columnCount; column++) {
        for (let lane ***REMOVED*** 0; lane < lanes.length; lane++) {
          const connector ***REMOVED*** connectors[column][lane];
          const fill2 ***REMOVED*** connector & 4 /* Left */ ? "\u2500" /* lr */ : " ";
          const node ***REMOVED*** grid[column][lane];
          if (!node) {
            if (column < columnCount - 1) {
              writeLane(lane, repeat(fill2, columnWidths[column] + 1));
            }
          } else {
            writeLane(lane, node.text);
            if (column < columnCount - 1) {
              writeLane(lane, " ");
              writeLane(lane, repeat(fill2, columnWidths[column] - node.text.length));
            }
          }
          writeLane(lane, getBoxCharacter(connector));
          writeLane(lane, connector & 8 /* Right */ && column < columnCount - 1 && !grid[column + 1][lane] ? "\u2500" /* lr */ : " ");
        }
      }
      return `
${lanes.join("\n")}
`;
      function writeLane(lane, text) {
        lanes[lane] +***REMOVED*** text;
      }
    }
    function getBoxCharacter(connector) {
      switch (connector) {
        case 3 /* UpDown */:
          return "\u2502" /* ud */;
        case 12 /* LeftRight */:
          return "\u2500" /* lr */;
        case 5 /* UpLeft */:
          return "\u256F" /* ul */;
        case 9 /* UpRight */:
          return "\u2570" /* ur */;
        case 6 /* DownLeft */:
          return "\u256E" /* dl */;
        case 10 /* DownRight */:
          return "\u256D" /* dr */;
        case 7 /* UpDownLeft */:
          return "\u2524" /* udl */;
        case 11 /* UpDownRight */:
          return "\u251C" /* udr */;
        case 13 /* UpLeftRight */:
          return "\u2534" /* ulr */;
        case 14 /* DownLeftRight */:
          return "\u252C" /* dlr */;
        case 15 /* UpDownLeftRight */:
          return "\u256B" /* udlr */;
      }
      return " ";
    }
    function fill(array, value) {
      if (array.fill) {
        array.fill(value);
      } else {
        for (let i ***REMOVED*** 0; i < array.length; i++) {
          array[i] ***REMOVED*** value;
        }
      }
      return array;
    }
    function repeat(ch, length2) {
      if (ch.repeat) {
        return length2 > 0 ? ch.repeat(length2) : "";
      }
      let s ***REMOVED*** "";
      while (s.length < length2) {
        s +***REMOVED*** ch;
      }
      return s;
    }
  }
  Debug2.formatControlFlowGraph ***REMOVED*** formatControlFlowGraph;
})(Debug || (Debug ***REMOVED*** {}));

// src/compiler/semver.ts
var versionRegExp ***REMOVED*** /^(0|[1-9]\d*)(?:\.(0|[1-9]\d*)(?:\.(0|[1-9]\d*)(?:-([a-z0-9-.]+))?(?:\+([a-z0-9-.]+))?)?)?$/i;
var prereleaseRegExp ***REMOVED*** /^(?:0|[1-9]\d*|[a-z-][a-z0-9-]*)(?:\.(?:0|[1-9]\d*|[a-z-][a-z0-9-]*))*$/i;
var prereleasePartRegExp ***REMOVED*** /^(?:0|[1-9]\d*|[a-z-][a-z0-9-]*)$/i;
var buildRegExp ***REMOVED*** /^[a-z0-9-]+(?:\.[a-z0-9-]+)*$/i;
var buildPartRegExp ***REMOVED*** /^[a-z0-9-]+$/i;
var numericIdentifierRegExp ***REMOVED*** /^(?:0|[1-9]\d*)$/;
var _Version ***REMOVED*** class _Version {
  constructor(major, minor ***REMOVED*** 0, patch ***REMOVED*** 0, prerelease ***REMOVED*** "", build2 ***REMOVED*** "") {
    if (typeof major ***REMOVED******REMOVED******REMOVED*** "string") {
      const result ***REMOVED*** Debug.checkDefined(tryParseComponents(major), "Invalid version");
      ({ major, minor, patch, prerelease, build: build2 } ***REMOVED*** result);
    }
    Debug.assert(major >***REMOVED*** 0, "Invalid argument: major");
    Debug.assert(minor >***REMOVED*** 0, "Invalid argument: minor");
    Debug.assert(patch >***REMOVED*** 0, "Invalid argument: patch");
    const prereleaseArray ***REMOVED*** prerelease ? isArray(prerelease) ? prerelease : prerelease.split(".") : emptyArray;
    const buildArray ***REMOVED*** build2 ? isArray(build2) ? build2 : build2.split(".") : emptyArray;
    Debug.assert(every(prereleaseArray, (s) ***REMOVED***> prereleasePartRegExp.test(s)), "Invalid argument: prerelease");
    Debug.assert(every(buildArray, (s) ***REMOVED***> buildPartRegExp.test(s)), "Invalid argument: build");
    this.major ***REMOVED*** major;
    this.minor ***REMOVED*** minor;
    this.patch ***REMOVED*** patch;
    this.prerelease ***REMOVED*** prereleaseArray;
    this.build ***REMOVED*** buildArray;
  }
  static tryParse(text) {
    const result ***REMOVED*** tryParseComponents(text);
    if (!result) return void 0;
    const { major, minor, patch, prerelease, build: build2 } ***REMOVED*** result;
    return new _Version(major, minor, patch, prerelease, build2);
  }
  compareTo(other) {
    if (this ***REMOVED******REMOVED******REMOVED*** other) return 0 /* EqualTo */;
    if (other ***REMOVED******REMOVED******REMOVED*** void 0) return 1 /* GreaterThan */;
    return compareValues(this.major, other.major) || compareValues(this.minor, other.minor) || compareValues(this.patch, other.patch) || comparePrereleaseIdentifiers(this.prerelease, other.prerelease);
  }
  increment(field) {
    switch (field) {
      case "major":
        return new _Version(this.major + 1, 0, 0);
      case "minor":
        return new _Version(this.major, this.minor + 1, 0);
      case "patch":
        return new _Version(this.major, this.minor, this.patch + 1);
      default:
        return Debug.assertNever(field);
    }
  }
  with(fields) {
    const {
      major ***REMOVED*** this.major,
      minor ***REMOVED*** this.minor,
      patch ***REMOVED*** this.patch,
      prerelease ***REMOVED*** this.prerelease,
      build: build2 ***REMOVED*** this.build
    } ***REMOVED*** fields;
    return new _Version(major, minor, patch, prerelease, build2);
  }
  toString() {
    let result ***REMOVED*** `${this.major}.${this.minor}.${this.patch}`;
    if (some(this.prerelease)) result +***REMOVED*** `-${this.prerelease.join(".")}`;
    if (some(this.build)) result +***REMOVED*** `+${this.build.join(".")}`;
    return result;
  }
};
_Version.zero ***REMOVED*** new _Version(0, 0, 0, ["0"]);
var Version ***REMOVED*** _Version;
function tryParseComponents(text) {
  const match ***REMOVED*** versionRegExp.exec(text);
  if (!match) return void 0;
  const [, major, minor ***REMOVED*** "0", patch ***REMOVED*** "0", prerelease ***REMOVED*** "", build2 ***REMOVED*** ""] ***REMOVED*** match;
  if (prerelease && !prereleaseRegExp.test(prerelease)) return void 0;
  if (build2 && !buildRegExp.test(build2)) return void 0;
  return {
    major: parseInt(major, 10),
    minor: parseInt(minor, 10),
    patch: parseInt(patch, 10),
    prerelease,
    build: build2
  };
}
function comparePrereleaseIdentifiers(left, right) {
  if (left ***REMOVED******REMOVED******REMOVED*** right) return 0 /* EqualTo */;
  if (left.length ***REMOVED******REMOVED******REMOVED*** 0) return right.length ***REMOVED******REMOVED******REMOVED*** 0 ? 0 /* EqualTo */ : 1 /* GreaterThan */;
  if (right.length ***REMOVED******REMOVED******REMOVED*** 0) return -1 /* LessThan */;
  const length2 ***REMOVED*** Math.min(left.length, right.length);
  for (let i ***REMOVED*** 0; i < length2; i++) {
    const leftIdentifier ***REMOVED*** left[i];
    const rightIdentifier ***REMOVED*** right[i];
    if (leftIdentifier ***REMOVED******REMOVED******REMOVED*** rightIdentifier) continue;
    const leftIsNumeric ***REMOVED*** numericIdentifierRegExp.test(leftIdentifier);
    const rightIsNumeric ***REMOVED*** numericIdentifierRegExp.test(rightIdentifier);
    if (leftIsNumeric || rightIsNumeric) {
      if (leftIsNumeric !***REMOVED******REMOVED*** rightIsNumeric) return leftIsNumeric ? -1 /* LessThan */ : 1 /* GreaterThan */;
      const result ***REMOVED*** compareValues(+leftIdentifier, +rightIdentifier);
      if (result) return result;
    } else {
      const result ***REMOVED*** compareStringsCaseSensitive(leftIdentifier, rightIdentifier);
      if (result) return result;
    }
  }
  return compareValues(left.length, right.length);
}
var VersionRange ***REMOVED*** class _VersionRange {
  constructor(spec) {
    this._alternatives ***REMOVED*** spec ? Debug.checkDefined(parseRange(spec), "Invalid range spec.") : emptyArray;
  }
  static tryParse(text) {
    const sets ***REMOVED*** parseRange(text);
    if (sets) {
      const range ***REMOVED*** new _VersionRange("");
      range._alternatives ***REMOVED*** sets;
      return range;
    }
    return void 0;
  }
  /**
   * Tests whether a version matches the range. This is equivalent to `satisfies(version, range, { includePrerelease: true })`.
   * in `node-semver`.
   */
  test(version2) {
    if (typeof version2 ***REMOVED******REMOVED******REMOVED*** "string") version2 ***REMOVED*** new Version(version2);
    return testDisjunction(version2, this._alternatives);
  }
  toString() {
    return formatDisjunction(this._alternatives);
  }
};
var logicalOrRegExp ***REMOVED*** /\|\|/;
var whitespaceRegExp ***REMOVED*** /\s+/;
var partialRegExp ***REMOVED*** /^([x*0]|[1-9]\d*)(?:\.([x*0]|[1-9]\d*)(?:\.([x*0]|[1-9]\d*)(?:-([a-z0-9-.]+))?(?:\+([a-z0-9-.]+))?)?)?$/i;
var hyphenRegExp ***REMOVED*** /^\s*([a-z0-9-+.*]+)\s+-\s+([a-z0-9-+.*]+)\s*$/i;
var rangeRegExp ***REMOVED*** /^([~^<>***REMOVED***]|<***REMOVED***|>***REMOVED***)?\s*([a-z0-9-+.*]+)$/i;
function parseRange(text) {
  const alternatives ***REMOVED*** [];
  for (let range of text.trim().split(logicalOrRegExp)) {
    if (!range) continue;
    const comparators ***REMOVED*** [];
    range ***REMOVED*** range.trim();
    const match ***REMOVED*** hyphenRegExp.exec(range);
    if (match) {
      if (!parseHyphen(match[1], match[2], comparators)) return void 0;
    } else {
      for (const simple of range.split(whitespaceRegExp)) {
        const match2 ***REMOVED*** rangeRegExp.exec(simple.trim());
        if (!match2 || !parseComparator(match2[1], match2[2], comparators)) return void 0;
      }
    }
    alternatives.push(comparators);
  }
  return alternatives;
}
function parsePartial(text) {
  const match ***REMOVED*** partialRegExp.exec(text);
  if (!match) return void 0;
  const [, major, minor ***REMOVED*** "*", patch ***REMOVED*** "*", prerelease, build2] ***REMOVED*** match;
  const version2 ***REMOVED*** new Version(
    isWildcard(major) ? 0 : parseInt(major, 10),
    isWildcard(major) || isWildcard(minor) ? 0 : parseInt(minor, 10),
    isWildcard(major) || isWildcard(minor) || isWildcard(patch) ? 0 : parseInt(patch, 10),
    prerelease,
    build2
  );
  return { version: version2, major, minor, patch };
}
function parseHyphen(left, right, comparators) {
  const leftResult ***REMOVED*** parsePartial(left);
  if (!leftResult) return false;
  const rightResult ***REMOVED*** parsePartial(right);
  if (!rightResult) return false;
  if (!isWildcard(leftResult.major)) {
    comparators.push(createComparator(">***REMOVED***", leftResult.version));
  }
  if (!isWildcard(rightResult.major)) {
    comparators.push(
      isWildcard(rightResult.minor) ? createComparator("<", rightResult.version.increment("major")) : isWildcard(rightResult.patch) ? createComparator("<", rightResult.version.increment("minor")) : createComparator("<***REMOVED***", rightResult.version)
    );
  }
  return true;
}
function parseComparator(operator, text, comparators) {
  const result ***REMOVED*** parsePartial(text);
  if (!result) return false;
  const { version: version2, major, minor, patch } ***REMOVED*** result;
  if (!isWildcard(major)) {
    switch (operator) {
      case "~":
        comparators.push(createComparator(">***REMOVED***", version2));
        comparators.push(createComparator(
          "<",
          version2.increment(
            isWildcard(minor) ? "major" : "minor"
          )
        ));
        break;
      case "^":
        comparators.push(createComparator(">***REMOVED***", version2));
        comparators.push(createComparator(
          "<",
          version2.increment(
            version2.major > 0 || isWildcard(minor) ? "major" : version2.minor > 0 || isWildcard(patch) ? "minor" : "patch"
          )
        ));
        break;
      case "<":
      case ">***REMOVED***":
        comparators.push(
          isWildcard(minor) || isWildcard(patch) ? createComparator(operator, version2.with({ prerelease: "0" })) : createComparator(operator, version2)
        );
        break;
      case "<***REMOVED***":
      case ">":
        comparators.push(
          isWildcard(minor) ? createComparator(operator ***REMOVED******REMOVED******REMOVED*** "<***REMOVED***" ? "<" : ">***REMOVED***", version2.increment("major").with({ prerelease: "0" })) : isWildcard(patch) ? createComparator(operator ***REMOVED******REMOVED******REMOVED*** "<***REMOVED***" ? "<" : ">***REMOVED***", version2.increment("minor").with({ prerelease: "0" })) : createComparator(operator, version2)
        );
        break;
      case "***REMOVED***":
      case void 0:
        if (isWildcard(minor) || isWildcard(patch)) {
          comparators.push(createComparator(">***REMOVED***", version2.with({ prerelease: "0" })));
          comparators.push(createComparator("<", version2.increment(isWildcard(minor) ? "major" : "minor").with({ prerelease: "0" })));
        } else {
          comparators.push(createComparator("***REMOVED***", version2));
        }
        break;
      default:
        return false;
    }
  } else if (operator ***REMOVED******REMOVED******REMOVED*** "<" || operator ***REMOVED******REMOVED******REMOVED*** ">") {
    comparators.push(createComparator("<", Version.zero));
  }
  return true;
}
function isWildcard(part) {
  return part ***REMOVED******REMOVED******REMOVED*** "*" || part ***REMOVED******REMOVED******REMOVED*** "x" || part ***REMOVED******REMOVED******REMOVED*** "X";
}
function createComparator(operator, operand) {
  return { operator, operand };
}
function testDisjunction(version2, alternatives) {
  if (alternatives.length ***REMOVED******REMOVED******REMOVED*** 0) return true;
  for (const alternative of alternatives) {
    if (testAlternative(version2, alternative)) return true;
  }
  return false;
}
function testAlternative(version2, comparators) {
  for (const comparator of comparators) {
    if (!testComparator(version2, comparator.operator, comparator.operand)) return false;
  }
  return true;
}
function testComparator(version2, operator, operand) {
  const cmp ***REMOVED*** version2.compareTo(operand);
  switch (operator) {
    case "<":
      return cmp < 0;
    case "<***REMOVED***":
      return cmp <***REMOVED*** 0;
    case ">":
      return cmp > 0;
    case ">***REMOVED***":
      return cmp >***REMOVED*** 0;
    case "***REMOVED***":
      return cmp ***REMOVED******REMOVED******REMOVED*** 0;
    default:
      return Debug.assertNever(operator);
  }
}
function formatDisjunction(alternatives) {
  return map(alternatives, formatAlternative).join(" || ") || "*";
}
function formatAlternative(comparators) {
  return map(comparators, formatComparator).join(" ");
}
function formatComparator(comparator) {
  return `${comparator.operator}${comparator.operand}`;
}

// src/compiler/performanceCore.ts
function tryGetPerformance() {
  if (isNodeLikeSystem()) {
    try {
      const { performance: performance2 } ***REMOVED*** require("perf_hooks");
      if (performance2) {
        return {
          shouldWriteNativeEvents: false,
          performance: performance2
        };
      }
    } catch {
    }
  }
  if (typeof performance ***REMOVED******REMOVED******REMOVED*** "object") {
    return {
      shouldWriteNativeEvents: true,
      performance
    };
  }
  return void 0;
}
function tryGetPerformanceHooks() {
  const p ***REMOVED*** tryGetPerformance();
  if (!p) return void 0;
  const { shouldWriteNativeEvents, performance: performance2 } ***REMOVED*** p;
  const hooks ***REMOVED*** {
    shouldWriteNativeEvents,
    performance: void 0,
    performanceTime: void 0
  };
  if (typeof performance2.timeOrigin ***REMOVED******REMOVED******REMOVED*** "number" && typeof performance2.now ***REMOVED******REMOVED******REMOVED*** "function") {
    hooks.performanceTime ***REMOVED*** performance2;
  }
  if (hooks.performanceTime && typeof performance2.mark ***REMOVED******REMOVED******REMOVED*** "function" && typeof performance2.measure ***REMOVED******REMOVED******REMOVED*** "function" && typeof performance2.clearMarks ***REMOVED******REMOVED******REMOVED*** "function" && typeof performance2.clearMeasures ***REMOVED******REMOVED******REMOVED*** "function") {
    hooks.performance ***REMOVED*** performance2;
  }
  return hooks;
}
var nativePerformanceHooks ***REMOVED*** tryGetPerformanceHooks();
var nativePerformanceTime ***REMOVED*** nativePerformanceHooks ***REMOVED******REMOVED*** null ? void 0 : nativePerformanceHooks.performanceTime;
function tryGetNativePerformanceHooks() {
  return nativePerformanceHooks;
}
var timestamp ***REMOVED*** nativePerformanceTime ? () ***REMOVED***> nativePerformanceTime.now() : Date.now;

// src/compiler/performance.ts
var perfHooks;
var performanceImpl;
function createTimerIf(condition, measureName, startMarkName, endMarkName) {
  return condition ? createTimer(measureName, startMarkName, endMarkName) : nullTimer;
}
function createTimer(measureName, startMarkName, endMarkName) {
  let enterCount ***REMOVED*** 0;
  return {
    enter,
    exit
  };
  function enter() {
    if (++enterCount ***REMOVED******REMOVED******REMOVED*** 1) {
      mark(startMarkName);
    }
  }
  function exit() {
    if (--enterCount ***REMOVED******REMOVED******REMOVED*** 0) {
      mark(endMarkName);
      measure(measureName, startMarkName, endMarkName);
    } else if (enterCount < 0) {
      Debug.fail("enter/exit count does not match.");
    }
  }
}
var nullTimer ***REMOVED*** { enter: noop, exit: noop };
var enabled ***REMOVED*** false;
var timeorigin ***REMOVED*** timestamp();
var marks ***REMOVED*** /* @__PURE__ */ new Map();
var counts ***REMOVED*** /* @__PURE__ */ new Map();
var durations ***REMOVED*** /* @__PURE__ */ new Map();
function mark(markName) {
  if (enabled) {
    const count ***REMOVED*** counts.get(markName) ?? 0;
    counts.set(markName, count + 1);
    marks.set(markName, timestamp());
    performanceImpl ***REMOVED******REMOVED*** null ? void 0 : performanceImpl.mark(markName);
    if (typeof onProfilerEvent ***REMOVED******REMOVED******REMOVED*** "function") {
      onProfilerEvent(markName);
    }
  }
}
function measure(measureName, startMarkName, endMarkName) {
  if (enabled) {
    const end ***REMOVED*** (endMarkName !***REMOVED******REMOVED*** void 0 ? marks.get(endMarkName) : void 0) ?? timestamp();
    const start ***REMOVED*** (startMarkName !***REMOVED******REMOVED*** void 0 ? marks.get(startMarkName) : void 0) ?? timeorigin;
    const previousDuration ***REMOVED*** durations.get(measureName) || 0;
    durations.set(measureName, previousDuration + (end - start));
    performanceImpl ***REMOVED******REMOVED*** null ? void 0 : performanceImpl.measure(measureName, startMarkName, endMarkName);
  }
}
function getCount(markName) {
  return counts.get(markName) || 0;
}
function getDuration(measureName) {
  return durations.get(measureName) || 0;
}
function forEachMeasure(cb) {
  durations.forEach((duration, measureName) ***REMOVED***> cb(measureName, duration));
}
function forEachMark(cb) {
  marks.forEach((_time, markName) ***REMOVED***> cb(markName));
}
function clearMeasures(name) {
  if (name !***REMOVED******REMOVED*** void 0) durations.delete(name);
  else durations.clear();
  performanceImpl ***REMOVED******REMOVED*** null ? void 0 : performanceImpl.clearMeasures(name);
}
function clearMarks(name) {
  if (name !***REMOVED******REMOVED*** void 0) {
    counts.delete(name);
    marks.delete(name);
  } else {
    counts.clear();
    marks.clear();
  }
  performanceImpl ***REMOVED******REMOVED*** null ? void 0 : performanceImpl.clearMarks(name);
}
function isEnabled() {
  return enabled;
}
function enable(system ***REMOVED*** sys) {
  var _a;
  if (!enabled) {
    enabled ***REMOVED*** true;
    perfHooks || (perfHooks ***REMOVED*** tryGetNativePerformanceHooks());
    if (perfHooks ***REMOVED******REMOVED*** null ? void 0 : perfHooks.performance) {
      timeorigin ***REMOVED*** perfHooks.performance.timeOrigin;
      if (perfHooks.shouldWriteNativeEvents || ((_a ***REMOVED*** system ***REMOVED******REMOVED*** null ? void 0 : system.cpuProfilingEnabled) ***REMOVED******REMOVED*** null ? void 0 : _a.call(system)) || (system ***REMOVED******REMOVED*** null ? void 0 : system.debugMode)) {
        performanceImpl ***REMOVED*** perfHooks.performance;
      }
    }
  }
  return true;
}
function disable() {
  if (enabled) {
    marks.clear();
    counts.clear();
    durations.clear();
    performanceImpl ***REMOVED*** void 0;
    enabled ***REMOVED*** false;
  }
}

// src/compiler/tracing.ts
var tracing;
var tracingEnabled;
((tracingEnabled2) ***REMOVED***> {
  let fs;
  let traceCount ***REMOVED*** 0;
  let traceFd ***REMOVED*** 0;
  let mode;
  const typeCatalog ***REMOVED*** [];
  let legendPath;
  const legend ***REMOVED*** [];
  function startTracing2(tracingMode, traceDir, configFilePath) {
    Debug.assert(!tracing, "Tracing already started");
    if (fs ***REMOVED******REMOVED******REMOVED*** void 0) {
      try {
        fs ***REMOVED*** require("fs");
      } catch (e) {
        throw new Error(`tracing requires having fs
(original error: ${e.message || e})`);
      }
    }
    mode ***REMOVED*** tracingMode;
    typeCatalog.length ***REMOVED*** 0;
    if (legendPath ***REMOVED******REMOVED******REMOVED*** void 0) {
      legendPath ***REMOVED*** combinePaths(traceDir, "legend.json");
    }
    if (!fs.existsSync(traceDir)) {
      fs.mkdirSync(traceDir, { recursive: true });
    }
    const countPart ***REMOVED*** mode ***REMOVED******REMOVED******REMOVED*** "build" ? `.${process.pid}-${++traceCount}` : mode ***REMOVED******REMOVED******REMOVED*** "server" ? `.${process.pid}` : ``;
    const tracePath ***REMOVED*** combinePaths(traceDir, `trace${countPart}.json`);
    const typesPath ***REMOVED*** combinePaths(traceDir, `types${countPart}.json`);
    legend.push({
      configFilePath,
      tracePath,
      typesPath
    });
    traceFd ***REMOVED*** fs.openSync(tracePath, "w");
    tracing ***REMOVED*** tracingEnabled2;
    const meta ***REMOVED*** { cat: "__metadata", ph: "M", ts: 1e3 * timestamp(), pid: 1, tid: 1 };
    fs.writeSync(
      traceFd,
      "[\n" + [{ name: "process_name", args: { name: "tsc" }, ...meta }, { name: "thread_name", args: { name: "Main" }, ...meta }, { name: "TracingStartedInBrowser", ...meta, cat: "disabled-by-default-devtools.timeline" }].map((v) ***REMOVED***> JSON.stringify(v)).join(",\n")
    );
  }
  tracingEnabled2.startTracing ***REMOVED*** startTracing2;
  function stopTracing() {
    Debug.assert(tracing, "Tracing is not in progress");
    Debug.assert(!!typeCatalog.length ***REMOVED******REMOVED******REMOVED*** (mode !***REMOVED******REMOVED*** "server"));
    fs.writeSync(traceFd, `
]
`);
    fs.closeSync(traceFd);
    tracing ***REMOVED*** void 0;
    if (typeCatalog.length) {
      dumpTypes(typeCatalog);
    } else {
      legend[legend.length - 1].typesPath ***REMOVED*** void 0;
    }
  }
  tracingEnabled2.stopTracing ***REMOVED*** stopTracing;
  function recordType(type) {
    if (mode !***REMOVED******REMOVED*** "server") {
      typeCatalog.push(type);
    }
  }
  tracingEnabled2.recordType ***REMOVED*** recordType;
  let Phase;
  ((Phase2) ***REMOVED***> {
    Phase2["Parse"] ***REMOVED*** "parse";
    Phase2["Program"] ***REMOVED*** "program";
    Phase2["Bind"] ***REMOVED*** "bind";
    Phase2["Check"] ***REMOVED*** "check";
    Phase2["CheckTypes"] ***REMOVED*** "checkTypes";
    Phase2["Emit"] ***REMOVED*** "emit";
    Phase2["Session"] ***REMOVED*** "session";
  })(Phase ***REMOVED*** tracingEnabled2.Phase || (tracingEnabled2.Phase ***REMOVED*** {}));
  function instant(phase, name, args) {
    writeEvent("I", phase, name, args, `"s":"g"`);
  }
  tracingEnabled2.instant ***REMOVED*** instant;
  const eventStack ***REMOVED*** [];
  function push(phase, name, args, separateBeginAndEnd ***REMOVED*** false) {
    if (separateBeginAndEnd) {
      writeEvent("B", phase, name, args);
    }
    eventStack.push({ phase, name, args, time: 1e3 * timestamp(), separateBeginAndEnd });
  }
  tracingEnabled2.push ***REMOVED*** push;
  function pop(results) {
    Debug.assert(eventStack.length > 0);
    writeStackEvent(eventStack.length - 1, 1e3 * timestamp(), results);
    eventStack.length--;
  }
  tracingEnabled2.pop ***REMOVED*** pop;
  function popAll() {
    const endTime ***REMOVED*** 1e3 * timestamp();
    for (let i ***REMOVED*** eventStack.length - 1; i >***REMOVED*** 0; i--) {
      writeStackEvent(i, endTime);
    }
    eventStack.length ***REMOVED*** 0;
  }
  tracingEnabled2.popAll ***REMOVED*** popAll;
  const sampleInterval ***REMOVED*** 1e3 * 10;
  function writeStackEvent(index, endTime, results) {
    const { phase, name, args, time, separateBeginAndEnd } ***REMOVED*** eventStack[index];
    if (separateBeginAndEnd) {
      Debug.assert(!results, "`results` are not supported for events with `separateBeginAndEnd`");
      writeEvent(
        "E",
        phase,
        name,
        args,
        /*extras*/
        void 0,
        endTime
      );
    } else if (sampleInterval - time % sampleInterval <***REMOVED*** endTime - time) {
      writeEvent("X", phase, name, { ...args, results }, `"dur":${endTime - time}`, time);
    }
  }
  function writeEvent(eventType, phase, name, args, extras, time ***REMOVED*** 1e3 * timestamp()) {
    if (mode ***REMOVED******REMOVED******REMOVED*** "server" && phase ***REMOVED******REMOVED******REMOVED*** "checkTypes" /* CheckTypes */) return;
    mark("beginTracing");
    fs.writeSync(traceFd, `,
{"pid":1,"tid":1,"ph":"${eventType}","cat":"${phase}","ts":${time},"name":"${name}"`);
    if (extras) fs.writeSync(traceFd, `,${extras}`);
    if (args) fs.writeSync(traceFd, `,"args":${JSON.stringify(args)}`);
    fs.writeSync(traceFd, `}`);
    mark("endTracing");
    measure("Tracing", "beginTracing", "endTracing");
  }
  function getLocation(node) {
    const file ***REMOVED*** getSourceFileOfNode(node);
    return !file ? void 0 : {
      path: file.path,
      start: indexFromOne(getLineAndCharacterOfPosition(file, node.pos)),
      end: indexFromOne(getLineAndCharacterOfPosition(file, node.end))
    };
    function indexFromOne(lc) {
      return {
        line: lc.line + 1,
        character: lc.character + 1
      };
    }
  }
  function dumpTypes(types) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s;
    mark("beginDumpTypes");
    const typesPath ***REMOVED*** legend[legend.length - 1].typesPath;
    const typesFd ***REMOVED*** fs.openSync(typesPath, "w");
    const recursionIdentityMap ***REMOVED*** /* @__PURE__ */ new Map();
    fs.writeSync(typesFd, "[");
    const numTypes ***REMOVED*** types.length;
    for (let i ***REMOVED*** 0; i < numTypes; i++) {
      const type ***REMOVED*** types[i];
      const objectFlags ***REMOVED*** type.objectFlags;
      const symbol ***REMOVED*** type.aliasSymbol ?? type.symbol;
      let display;
      if (objectFlags & 16 /* Anonymous */ | type.flags & 2944 /* Literal */) {
        try {
          display ***REMOVED*** (_a ***REMOVED*** type.checker) ***REMOVED******REMOVED*** null ? void 0 : _a.typeToString(type);
        } catch {
          display ***REMOVED*** void 0;
        }
      }
      let indexedAccessProperties ***REMOVED*** {};
      if (type.flags & 8388608 /* IndexedAccess */) {
        const indexedAccessType ***REMOVED*** type;
        indexedAccessProperties ***REMOVED*** {
          indexedAccessObjectType: (_b ***REMOVED*** indexedAccessType.objectType) ***REMOVED******REMOVED*** null ? void 0 : _b.id,
          indexedAccessIndexType: (_c ***REMOVED*** indexedAccessType.indexType) ***REMOVED******REMOVED*** null ? void 0 : _c.id
        };
      }
      let referenceProperties ***REMOVED*** {};
      if (objectFlags & 4 /* Reference */) {
        const referenceType ***REMOVED*** type;
        referenceProperties ***REMOVED*** {
          instantiatedType: (_d ***REMOVED*** referenceType.target) ***REMOVED******REMOVED*** null ? void 0 : _d.id,
          typeArguments: (_e ***REMOVED*** referenceType.resolvedTypeArguments) ***REMOVED******REMOVED*** null ? void 0 : _e.map((t) ***REMOVED***> t.id),
          referenceLocation: getLocation(referenceType.node)
        };
      }
      let conditionalProperties ***REMOVED*** {};
      if (type.flags & 16777216 /* Conditional */) {
        const conditionalType ***REMOVED*** type;
        conditionalProperties ***REMOVED*** {
          conditionalCheckType: (_f ***REMOVED*** conditionalType.checkType) ***REMOVED******REMOVED*** null ? void 0 : _f.id,
          conditionalExtendsType: (_g ***REMOVED*** conditionalType.extendsType) ***REMOVED******REMOVED*** null ? void 0 : _g.id,
          conditionalTrueType: ((_h ***REMOVED*** conditionalType.resolvedTrueType) ***REMOVED******REMOVED*** null ? void 0 : _h.id) ?? -1,
          conditionalFalseType: ((_i ***REMOVED*** conditionalType.resolvedFalseType) ***REMOVED******REMOVED*** null ? void 0 : _i.id) ?? -1
        };
      }
      let substitutionProperties ***REMOVED*** {};
      if (type.flags & 33554432 /* Substitution */) {
        const substitutionType ***REMOVED*** type;
        substitutionProperties ***REMOVED*** {
          substitutionBaseType: (_j ***REMOVED*** substitutionType.baseType) ***REMOVED******REMOVED*** null ? void 0 : _j.id,
          constraintType: (_k ***REMOVED*** substitutionType.constraint) ***REMOVED******REMOVED*** null ? void 0 : _k.id
        };
      }
      let reverseMappedProperties ***REMOVED*** {};
      if (objectFlags & 1024 /* ReverseMapped */) {
        const reverseMappedType ***REMOVED*** type;
        reverseMappedProperties ***REMOVED*** {
          reverseMappedSourceType: (_l ***REMOVED*** reverseMappedType.source) ***REMOVED******REMOVED*** null ? void 0 : _l.id,
          reverseMappedMappedType: (_m ***REMOVED*** reverseMappedType.mappedType) ***REMOVED******REMOVED*** null ? void 0 : _m.id,
          reverseMappedConstraintType: (_n ***REMOVED*** reverseMappedType.constraintType) ***REMOVED******REMOVED*** null ? void 0 : _n.id
        };
      }
      let evolvingArrayProperties ***REMOVED*** {};
      if (objectFlags & 256 /* EvolvingArray */) {
        const evolvingArrayType ***REMOVED*** type;
        evolvingArrayProperties ***REMOVED*** {
          evolvingArrayElementType: evolvingArrayType.elementType.id,
          evolvingArrayFinalType: (_o ***REMOVED*** evolvingArrayType.finalArrayType) ***REMOVED******REMOVED*** null ? void 0 : _o.id
        };
      }
      let recursionToken;
      const recursionIdentity ***REMOVED*** type.checker.getRecursionIdentity(type);
      if (recursionIdentity) {
        recursionToken ***REMOVED*** recursionIdentityMap.get(recursionIdentity);
        if (!recursionToken) {
          recursionToken ***REMOVED*** recursionIdentityMap.size;
          recursionIdentityMap.set(recursionIdentity, recursionToken);
        }
      }
      const descriptor ***REMOVED*** {
        id: type.id,
        intrinsicName: type.intrinsicName,
        symbolName: (symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.escapedName) && unescapeLeadingUnderscores(symbol.escapedName),
        recursionId: recursionToken,
        isTuple: objectFlags & 8 /* Tuple */ ? true : void 0,
        unionTypes: type.flags & 1048576 /* Union */ ? (_p ***REMOVED*** type.types) ***REMOVED******REMOVED*** null ? void 0 : _p.map((t) ***REMOVED***> t.id) : void 0,
        intersectionTypes: type.flags & 2097152 /* Intersection */ ? type.types.map((t) ***REMOVED***> t.id) : void 0,
        aliasTypeArguments: (_q ***REMOVED*** type.aliasTypeArguments) ***REMOVED******REMOVED*** null ? void 0 : _q.map((t) ***REMOVED***> t.id),
        keyofType: type.flags & 4194304 /* Index */ ? (_r ***REMOVED*** type.type) ***REMOVED******REMOVED*** null ? void 0 : _r.id : void 0,
        ...indexedAccessProperties,
        ...referenceProperties,
        ...conditionalProperties,
        ...substitutionProperties,
        ...reverseMappedProperties,
        ...evolvingArrayProperties,
        destructuringPattern: getLocation(type.pattern),
        firstDeclaration: getLocation((_s ***REMOVED*** symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _s[0]),
        flags: Debug.formatTypeFlags(type.flags).split("|"),
        display
      };
      fs.writeSync(typesFd, JSON.stringify(descriptor));
      if (i < numTypes - 1) {
        fs.writeSync(typesFd, ",\n");
      }
    }
    fs.writeSync(typesFd, "]\n");
    fs.closeSync(typesFd);
    mark("endDumpTypes");
    measure("Dump types", "beginDumpTypes", "endDumpTypes");
  }
  function dumpLegend() {
    if (!legendPath) {
      return;
    }
    fs.writeFileSync(legendPath, JSON.stringify(legend));
  }
  tracingEnabled2.dumpLegend ***REMOVED*** dumpLegend;
})(tracingEnabled || (tracingEnabled ***REMOVED*** {}));
var startTracing ***REMOVED*** tracingEnabled.startTracing;
var dumpTracingLegend ***REMOVED*** tracingEnabled.dumpLegend;

// src/compiler/types.ts
var SyntaxKind ***REMOVED*** /* @__PURE__ */ ((SyntaxKind4) ***REMOVED***> {
  SyntaxKind4[SyntaxKind4["Unknown"] ***REMOVED*** 0] ***REMOVED*** "Unknown";
  SyntaxKind4[SyntaxKind4["EndOfFileToken"] ***REMOVED*** 1] ***REMOVED*** "EndOfFileToken";
  SyntaxKind4[SyntaxKind4["SingleLineCommentTrivia"] ***REMOVED*** 2] ***REMOVED*** "SingleLineCommentTrivia";
  SyntaxKind4[SyntaxKind4["MultiLineCommentTrivia"] ***REMOVED*** 3] ***REMOVED*** "MultiLineCommentTrivia";
  SyntaxKind4[SyntaxKind4["NewLineTrivia"] ***REMOVED*** 4] ***REMOVED*** "NewLineTrivia";
  SyntaxKind4[SyntaxKind4["WhitespaceTrivia"] ***REMOVED*** 5] ***REMOVED*** "WhitespaceTrivia";
  SyntaxKind4[SyntaxKind4["ShebangTrivia"] ***REMOVED*** 6] ***REMOVED*** "ShebangTrivia";
  SyntaxKind4[SyntaxKind4["ConflictMarkerTrivia"] ***REMOVED*** 7] ***REMOVED*** "ConflictMarkerTrivia";
  SyntaxKind4[SyntaxKind4["NonTextFileMarkerTrivia"] ***REMOVED*** 8] ***REMOVED*** "NonTextFileMarkerTrivia";
  SyntaxKind4[SyntaxKind4["NumericLiteral"] ***REMOVED*** 9] ***REMOVED*** "NumericLiteral";
  SyntaxKind4[SyntaxKind4["BigIntLiteral"] ***REMOVED*** 10] ***REMOVED*** "BigIntLiteral";
  SyntaxKind4[SyntaxKind4["StringLiteral"] ***REMOVED*** 11] ***REMOVED*** "StringLiteral";
  SyntaxKind4[SyntaxKind4["JsxText"] ***REMOVED*** 12] ***REMOVED*** "JsxText";
  SyntaxKind4[SyntaxKind4["JsxTextAllWhiteSpaces"] ***REMOVED*** 13] ***REMOVED*** "JsxTextAllWhiteSpaces";
  SyntaxKind4[SyntaxKind4["RegularExpressionLiteral"] ***REMOVED*** 14] ***REMOVED*** "RegularExpressionLiteral";
  SyntaxKind4[SyntaxKind4["NoSubstitutionTemplateLiteral"] ***REMOVED*** 15] ***REMOVED*** "NoSubstitutionTemplateLiteral";
  SyntaxKind4[SyntaxKind4["TemplateHead"] ***REMOVED*** 16] ***REMOVED*** "TemplateHead";
  SyntaxKind4[SyntaxKind4["TemplateMiddle"] ***REMOVED*** 17] ***REMOVED*** "TemplateMiddle";
  SyntaxKind4[SyntaxKind4["TemplateTail"] ***REMOVED*** 18] ***REMOVED*** "TemplateTail";
  SyntaxKind4[SyntaxKind4["OpenBraceToken"] ***REMOVED*** 19] ***REMOVED*** "OpenBraceToken";
  SyntaxKind4[SyntaxKind4["CloseBraceToken"] ***REMOVED*** 20] ***REMOVED*** "CloseBraceToken";
  SyntaxKind4[SyntaxKind4["OpenParenToken"] ***REMOVED*** 21] ***REMOVED*** "OpenParenToken";
  SyntaxKind4[SyntaxKind4["CloseParenToken"] ***REMOVED*** 22] ***REMOVED*** "CloseParenToken";
  SyntaxKind4[SyntaxKind4["OpenBracketToken"] ***REMOVED*** 23] ***REMOVED*** "OpenBracketToken";
  SyntaxKind4[SyntaxKind4["CloseBracketToken"] ***REMOVED*** 24] ***REMOVED*** "CloseBracketToken";
  SyntaxKind4[SyntaxKind4["DotToken"] ***REMOVED*** 25] ***REMOVED*** "DotToken";
  SyntaxKind4[SyntaxKind4["DotDotDotToken"] ***REMOVED*** 26] ***REMOVED*** "DotDotDotToken";
  SyntaxKind4[SyntaxKind4["SemicolonToken"] ***REMOVED*** 27] ***REMOVED*** "SemicolonToken";
  SyntaxKind4[SyntaxKind4["CommaToken"] ***REMOVED*** 28] ***REMOVED*** "CommaToken";
  SyntaxKind4[SyntaxKind4["QuestionDotToken"] ***REMOVED*** 29] ***REMOVED*** "QuestionDotToken";
  SyntaxKind4[SyntaxKind4["LessThanToken"] ***REMOVED*** 30] ***REMOVED*** "LessThanToken";
  SyntaxKind4[SyntaxKind4["LessThanSlashToken"] ***REMOVED*** 31] ***REMOVED*** "LessThanSlashToken";
  SyntaxKind4[SyntaxKind4["GreaterThanToken"] ***REMOVED*** 32] ***REMOVED*** "GreaterThanToken";
  SyntaxKind4[SyntaxKind4["LessThanEqualsToken"] ***REMOVED*** 33] ***REMOVED*** "LessThanEqualsToken";
  SyntaxKind4[SyntaxKind4["GreaterThanEqualsToken"] ***REMOVED*** 34] ***REMOVED*** "GreaterThanEqualsToken";
  SyntaxKind4[SyntaxKind4["EqualsEqualsToken"] ***REMOVED*** 35] ***REMOVED*** "EqualsEqualsToken";
  SyntaxKind4[SyntaxKind4["ExclamationEqualsToken"] ***REMOVED*** 36] ***REMOVED*** "ExclamationEqualsToken";
  SyntaxKind4[SyntaxKind4["EqualsEqualsEqualsToken"] ***REMOVED*** 37] ***REMOVED*** "EqualsEqualsEqualsToken";
  SyntaxKind4[SyntaxKind4["ExclamationEqualsEqualsToken"] ***REMOVED*** 38] ***REMOVED*** "ExclamationEqualsEqualsToken";
  SyntaxKind4[SyntaxKind4["EqualsGreaterThanToken"] ***REMOVED*** 39] ***REMOVED*** "EqualsGreaterThanToken";
  SyntaxKind4[SyntaxKind4["PlusToken"] ***REMOVED*** 40] ***REMOVED*** "PlusToken";
  SyntaxKind4[SyntaxKind4["MinusToken"] ***REMOVED*** 41] ***REMOVED*** "MinusToken";
  SyntaxKind4[SyntaxKind4["AsteriskToken"] ***REMOVED*** 42] ***REMOVED*** "AsteriskToken";
  SyntaxKind4[SyntaxKind4["AsteriskAsteriskToken"] ***REMOVED*** 43] ***REMOVED*** "AsteriskAsteriskToken";
  SyntaxKind4[SyntaxKind4["SlashToken"] ***REMOVED*** 44] ***REMOVED*** "SlashToken";
  SyntaxKind4[SyntaxKind4["PercentToken"] ***REMOVED*** 45] ***REMOVED*** "PercentToken";
  SyntaxKind4[SyntaxKind4["PlusPlusToken"] ***REMOVED*** 46] ***REMOVED*** "PlusPlusToken";
  SyntaxKind4[SyntaxKind4["MinusMinusToken"] ***REMOVED*** 47] ***REMOVED*** "MinusMinusToken";
  SyntaxKind4[SyntaxKind4["LessThanLessThanToken"] ***REMOVED*** 48] ***REMOVED*** "LessThanLessThanToken";
  SyntaxKind4[SyntaxKind4["GreaterThanGreaterThanToken"] ***REMOVED*** 49] ***REMOVED*** "GreaterThanGreaterThanToken";
  SyntaxKind4[SyntaxKind4["GreaterThanGreaterThanGreaterThanToken"] ***REMOVED*** 50] ***REMOVED*** "GreaterThanGreaterThanGreaterThanToken";
  SyntaxKind4[SyntaxKind4["AmpersandToken"] ***REMOVED*** 51] ***REMOVED*** "AmpersandToken";
  SyntaxKind4[SyntaxKind4["BarToken"] ***REMOVED*** 52] ***REMOVED*** "BarToken";
  SyntaxKind4[SyntaxKind4["CaretToken"] ***REMOVED*** 53] ***REMOVED*** "CaretToken";
  SyntaxKind4[SyntaxKind4["ExclamationToken"] ***REMOVED*** 54] ***REMOVED*** "ExclamationToken";
  SyntaxKind4[SyntaxKind4["TildeToken"] ***REMOVED*** 55] ***REMOVED*** "TildeToken";
  SyntaxKind4[SyntaxKind4["AmpersandAmpersandToken"] ***REMOVED*** 56] ***REMOVED*** "AmpersandAmpersandToken";
  SyntaxKind4[SyntaxKind4["BarBarToken"] ***REMOVED*** 57] ***REMOVED*** "BarBarToken";
  SyntaxKind4[SyntaxKind4["QuestionToken"] ***REMOVED*** 58] ***REMOVED*** "QuestionToken";
  SyntaxKind4[SyntaxKind4["ColonToken"] ***REMOVED*** 59] ***REMOVED*** "ColonToken";
  SyntaxKind4[SyntaxKind4["AtToken"] ***REMOVED*** 60] ***REMOVED*** "AtToken";
  SyntaxKind4[SyntaxKind4["QuestionQuestionToken"] ***REMOVED*** 61] ***REMOVED*** "QuestionQuestionToken";
  SyntaxKind4[SyntaxKind4["BacktickToken"] ***REMOVED*** 62] ***REMOVED*** "BacktickToken";
  SyntaxKind4[SyntaxKind4["HashToken"] ***REMOVED*** 63] ***REMOVED*** "HashToken";
  SyntaxKind4[SyntaxKind4["EqualsToken"] ***REMOVED*** 64] ***REMOVED*** "EqualsToken";
  SyntaxKind4[SyntaxKind4["PlusEqualsToken"] ***REMOVED*** 65] ***REMOVED*** "PlusEqualsToken";
  SyntaxKind4[SyntaxKind4["MinusEqualsToken"] ***REMOVED*** 66] ***REMOVED*** "MinusEqualsToken";
  SyntaxKind4[SyntaxKind4["AsteriskEqualsToken"] ***REMOVED*** 67] ***REMOVED*** "AsteriskEqualsToken";
  SyntaxKind4[SyntaxKind4["AsteriskAsteriskEqualsToken"] ***REMOVED*** 68] ***REMOVED*** "AsteriskAsteriskEqualsToken";
  SyntaxKind4[SyntaxKind4["SlashEqualsToken"] ***REMOVED*** 69] ***REMOVED*** "SlashEqualsToken";
  SyntaxKind4[SyntaxKind4["PercentEqualsToken"] ***REMOVED*** 70] ***REMOVED*** "PercentEqualsToken";
  SyntaxKind4[SyntaxKind4["LessThanLessThanEqualsToken"] ***REMOVED*** 71] ***REMOVED*** "LessThanLessThanEqualsToken";
  SyntaxKind4[SyntaxKind4["GreaterThanGreaterThanEqualsToken"] ***REMOVED*** 72] ***REMOVED*** "GreaterThanGreaterThanEqualsToken";
  SyntaxKind4[SyntaxKind4["GreaterThanGreaterThanGreaterThanEqualsToken"] ***REMOVED*** 73] ***REMOVED*** "GreaterThanGreaterThanGreaterThanEqualsToken";
  SyntaxKind4[SyntaxKind4["AmpersandEqualsToken"] ***REMOVED*** 74] ***REMOVED*** "AmpersandEqualsToken";
  SyntaxKind4[SyntaxKind4["BarEqualsToken"] ***REMOVED*** 75] ***REMOVED*** "BarEqualsToken";
  SyntaxKind4[SyntaxKind4["BarBarEqualsToken"] ***REMOVED*** 76] ***REMOVED*** "BarBarEqualsToken";
  SyntaxKind4[SyntaxKind4["AmpersandAmpersandEqualsToken"] ***REMOVED*** 77] ***REMOVED*** "AmpersandAmpersandEqualsToken";
  SyntaxKind4[SyntaxKind4["QuestionQuestionEqualsToken"] ***REMOVED*** 78] ***REMOVED*** "QuestionQuestionEqualsToken";
  SyntaxKind4[SyntaxKind4["CaretEqualsToken"] ***REMOVED*** 79] ***REMOVED*** "CaretEqualsToken";
  SyntaxKind4[SyntaxKind4["Identifier"] ***REMOVED*** 80] ***REMOVED*** "Identifier";
  SyntaxKind4[SyntaxKind4["PrivateIdentifier"] ***REMOVED*** 81] ***REMOVED*** "PrivateIdentifier";
  SyntaxKind4[SyntaxKind4["JSDocCommentTextToken"] ***REMOVED*** 82] ***REMOVED*** "JSDocCommentTextToken";
  SyntaxKind4[SyntaxKind4["BreakKeyword"] ***REMOVED*** 83] ***REMOVED*** "BreakKeyword";
  SyntaxKind4[SyntaxKind4["CaseKeyword"] ***REMOVED*** 84] ***REMOVED*** "CaseKeyword";
  SyntaxKind4[SyntaxKind4["CatchKeyword"] ***REMOVED*** 85] ***REMOVED*** "CatchKeyword";
  SyntaxKind4[SyntaxKind4["ClassKeyword"] ***REMOVED*** 86] ***REMOVED*** "ClassKeyword";
  SyntaxKind4[SyntaxKind4["ConstKeyword"] ***REMOVED*** 87] ***REMOVED*** "ConstKeyword";
  SyntaxKind4[SyntaxKind4["ContinueKeyword"] ***REMOVED*** 88] ***REMOVED*** "ContinueKeyword";
  SyntaxKind4[SyntaxKind4["DebuggerKeyword"] ***REMOVED*** 89] ***REMOVED*** "DebuggerKeyword";
  SyntaxKind4[SyntaxKind4["DefaultKeyword"] ***REMOVED*** 90] ***REMOVED*** "DefaultKeyword";
  SyntaxKind4[SyntaxKind4["DeleteKeyword"] ***REMOVED*** 91] ***REMOVED*** "DeleteKeyword";
  SyntaxKind4[SyntaxKind4["DoKeyword"] ***REMOVED*** 92] ***REMOVED*** "DoKeyword";
  SyntaxKind4[SyntaxKind4["ElseKeyword"] ***REMOVED*** 93] ***REMOVED*** "ElseKeyword";
  SyntaxKind4[SyntaxKind4["EnumKeyword"] ***REMOVED*** 94] ***REMOVED*** "EnumKeyword";
  SyntaxKind4[SyntaxKind4["ExportKeyword"] ***REMOVED*** 95] ***REMOVED*** "ExportKeyword";
  SyntaxKind4[SyntaxKind4["ExtendsKeyword"] ***REMOVED*** 96] ***REMOVED*** "ExtendsKeyword";
  SyntaxKind4[SyntaxKind4["FalseKeyword"] ***REMOVED*** 97] ***REMOVED*** "FalseKeyword";
  SyntaxKind4[SyntaxKind4["FinallyKeyword"] ***REMOVED*** 98] ***REMOVED*** "FinallyKeyword";
  SyntaxKind4[SyntaxKind4["ForKeyword"] ***REMOVED*** 99] ***REMOVED*** "ForKeyword";
  SyntaxKind4[SyntaxKind4["FunctionKeyword"] ***REMOVED*** 100] ***REMOVED*** "FunctionKeyword";
  SyntaxKind4[SyntaxKind4["IfKeyword"] ***REMOVED*** 101] ***REMOVED*** "IfKeyword";
  SyntaxKind4[SyntaxKind4["ImportKeyword"] ***REMOVED*** 102] ***REMOVED*** "ImportKeyword";
  SyntaxKind4[SyntaxKind4["InKeyword"] ***REMOVED*** 103] ***REMOVED*** "InKeyword";
  SyntaxKind4[SyntaxKind4["InstanceOfKeyword"] ***REMOVED*** 104] ***REMOVED*** "InstanceOfKeyword";
  SyntaxKind4[SyntaxKind4["NewKeyword"] ***REMOVED*** 105] ***REMOVED*** "NewKeyword";
  SyntaxKind4[SyntaxKind4["NullKeyword"] ***REMOVED*** 106] ***REMOVED*** "NullKeyword";
  SyntaxKind4[SyntaxKind4["ReturnKeyword"] ***REMOVED*** 107] ***REMOVED*** "ReturnKeyword";
  SyntaxKind4[SyntaxKind4["SuperKeyword"] ***REMOVED*** 108] ***REMOVED*** "SuperKeyword";
  SyntaxKind4[SyntaxKind4["SwitchKeyword"] ***REMOVED*** 109] ***REMOVED*** "SwitchKeyword";
  SyntaxKind4[SyntaxKind4["ThisKeyword"] ***REMOVED*** 110] ***REMOVED*** "ThisKeyword";
  SyntaxKind4[SyntaxKind4["ThrowKeyword"] ***REMOVED*** 111] ***REMOVED*** "ThrowKeyword";
  SyntaxKind4[SyntaxKind4["TrueKeyword"] ***REMOVED*** 112] ***REMOVED*** "TrueKeyword";
  SyntaxKind4[SyntaxKind4["TryKeyword"] ***REMOVED*** 113] ***REMOVED*** "TryKeyword";
  SyntaxKind4[SyntaxKind4["TypeOfKeyword"] ***REMOVED*** 114] ***REMOVED*** "TypeOfKeyword";
  SyntaxKind4[SyntaxKind4["VarKeyword"] ***REMOVED*** 115] ***REMOVED*** "VarKeyword";
  SyntaxKind4[SyntaxKind4["VoidKeyword"] ***REMOVED*** 116] ***REMOVED*** "VoidKeyword";
  SyntaxKind4[SyntaxKind4["WhileKeyword"] ***REMOVED*** 117] ***REMOVED*** "WhileKeyword";
  SyntaxKind4[SyntaxKind4["WithKeyword"] ***REMOVED*** 118] ***REMOVED*** "WithKeyword";
  SyntaxKind4[SyntaxKind4["ImplementsKeyword"] ***REMOVED*** 119] ***REMOVED*** "ImplementsKeyword";
  SyntaxKind4[SyntaxKind4["InterfaceKeyword"] ***REMOVED*** 120] ***REMOVED*** "InterfaceKeyword";
  SyntaxKind4[SyntaxKind4["LetKeyword"] ***REMOVED*** 121] ***REMOVED*** "LetKeyword";
  SyntaxKind4[SyntaxKind4["PackageKeyword"] ***REMOVED*** 122] ***REMOVED*** "PackageKeyword";
  SyntaxKind4[SyntaxKind4["PrivateKeyword"] ***REMOVED*** 123] ***REMOVED*** "PrivateKeyword";
  SyntaxKind4[SyntaxKind4["ProtectedKeyword"] ***REMOVED*** 124] ***REMOVED*** "ProtectedKeyword";
  SyntaxKind4[SyntaxKind4["PublicKeyword"] ***REMOVED*** 125] ***REMOVED*** "PublicKeyword";
  SyntaxKind4[SyntaxKind4["StaticKeyword"] ***REMOVED*** 126] ***REMOVED*** "StaticKeyword";
  SyntaxKind4[SyntaxKind4["YieldKeyword"] ***REMOVED*** 127] ***REMOVED*** "YieldKeyword";
  SyntaxKind4[SyntaxKind4["AbstractKeyword"] ***REMOVED*** 128] ***REMOVED*** "AbstractKeyword";
  SyntaxKind4[SyntaxKind4["AccessorKeyword"] ***REMOVED*** 129] ***REMOVED*** "AccessorKeyword";
  SyntaxKind4[SyntaxKind4["AsKeyword"] ***REMOVED*** 130] ***REMOVED*** "AsKeyword";
  SyntaxKind4[SyntaxKind4["AssertsKeyword"] ***REMOVED*** 131] ***REMOVED*** "AssertsKeyword";
  SyntaxKind4[SyntaxKind4["AssertKeyword"] ***REMOVED*** 132] ***REMOVED*** "AssertKeyword";
  SyntaxKind4[SyntaxKind4["AnyKeyword"] ***REMOVED*** 133] ***REMOVED*** "AnyKeyword";
  SyntaxKind4[SyntaxKind4["AsyncKeyword"] ***REMOVED*** 134] ***REMOVED*** "AsyncKeyword";
  SyntaxKind4[SyntaxKind4["AwaitKeyword"] ***REMOVED*** 135] ***REMOVED*** "AwaitKeyword";
  SyntaxKind4[SyntaxKind4["BooleanKeyword"] ***REMOVED*** 136] ***REMOVED*** "BooleanKeyword";
  SyntaxKind4[SyntaxKind4["ConstructorKeyword"] ***REMOVED*** 137] ***REMOVED*** "ConstructorKeyword";
  SyntaxKind4[SyntaxKind4["DeclareKeyword"] ***REMOVED*** 138] ***REMOVED*** "DeclareKeyword";
  SyntaxKind4[SyntaxKind4["GetKeyword"] ***REMOVED*** 139] ***REMOVED*** "GetKeyword";
  SyntaxKind4[SyntaxKind4["InferKeyword"] ***REMOVED*** 140] ***REMOVED*** "InferKeyword";
  SyntaxKind4[SyntaxKind4["IntrinsicKeyword"] ***REMOVED*** 141] ***REMOVED*** "IntrinsicKeyword";
  SyntaxKind4[SyntaxKind4["IsKeyword"] ***REMOVED*** 142] ***REMOVED*** "IsKeyword";
  SyntaxKind4[SyntaxKind4["KeyOfKeyword"] ***REMOVED*** 143] ***REMOVED*** "KeyOfKeyword";
  SyntaxKind4[SyntaxKind4["ModuleKeyword"] ***REMOVED*** 144] ***REMOVED*** "ModuleKeyword";
  SyntaxKind4[SyntaxKind4["NamespaceKeyword"] ***REMOVED*** 145] ***REMOVED*** "NamespaceKeyword";
  SyntaxKind4[SyntaxKind4["NeverKeyword"] ***REMOVED*** 146] ***REMOVED*** "NeverKeyword";
  SyntaxKind4[SyntaxKind4["OutKeyword"] ***REMOVED*** 147] ***REMOVED*** "OutKeyword";
  SyntaxKind4[SyntaxKind4["ReadonlyKeyword"] ***REMOVED*** 148] ***REMOVED*** "ReadonlyKeyword";
  SyntaxKind4[SyntaxKind4["RequireKeyword"] ***REMOVED*** 149] ***REMOVED*** "RequireKeyword";
  SyntaxKind4[SyntaxKind4["NumberKeyword"] ***REMOVED*** 150] ***REMOVED*** "NumberKeyword";
  SyntaxKind4[SyntaxKind4["ObjectKeyword"] ***REMOVED*** 151] ***REMOVED*** "ObjectKeyword";
  SyntaxKind4[SyntaxKind4["SatisfiesKeyword"] ***REMOVED*** 152] ***REMOVED*** "SatisfiesKeyword";
  SyntaxKind4[SyntaxKind4["SetKeyword"] ***REMOVED*** 153] ***REMOVED*** "SetKeyword";
  SyntaxKind4[SyntaxKind4["StringKeyword"] ***REMOVED*** 154] ***REMOVED*** "StringKeyword";
  SyntaxKind4[SyntaxKind4["SymbolKeyword"] ***REMOVED*** 155] ***REMOVED*** "SymbolKeyword";
  SyntaxKind4[SyntaxKind4["TypeKeyword"] ***REMOVED*** 156] ***REMOVED*** "TypeKeyword";
  SyntaxKind4[SyntaxKind4["UndefinedKeyword"] ***REMOVED*** 157] ***REMOVED*** "UndefinedKeyword";
  SyntaxKind4[SyntaxKind4["UniqueKeyword"] ***REMOVED*** 158] ***REMOVED*** "UniqueKeyword";
  SyntaxKind4[SyntaxKind4["UnknownKeyword"] ***REMOVED*** 159] ***REMOVED*** "UnknownKeyword";
  SyntaxKind4[SyntaxKind4["UsingKeyword"] ***REMOVED*** 160] ***REMOVED*** "UsingKeyword";
  SyntaxKind4[SyntaxKind4["FromKeyword"] ***REMOVED*** 161] ***REMOVED*** "FromKeyword";
  SyntaxKind4[SyntaxKind4["GlobalKeyword"] ***REMOVED*** 162] ***REMOVED*** "GlobalKeyword";
  SyntaxKind4[SyntaxKind4["BigIntKeyword"] ***REMOVED*** 163] ***REMOVED*** "BigIntKeyword";
  SyntaxKind4[SyntaxKind4["OverrideKeyword"] ***REMOVED*** 164] ***REMOVED*** "OverrideKeyword";
  SyntaxKind4[SyntaxKind4["OfKeyword"] ***REMOVED*** 165] ***REMOVED*** "OfKeyword";
  SyntaxKind4[SyntaxKind4["DeferKeyword"] ***REMOVED*** 166] ***REMOVED*** "DeferKeyword";
  SyntaxKind4[SyntaxKind4["QualifiedName"] ***REMOVED*** 167] ***REMOVED*** "QualifiedName";
  SyntaxKind4[SyntaxKind4["ComputedPropertyName"] ***REMOVED*** 168] ***REMOVED*** "ComputedPropertyName";
  SyntaxKind4[SyntaxKind4["TypeParameter"] ***REMOVED*** 169] ***REMOVED*** "TypeParameter";
  SyntaxKind4[SyntaxKind4["Parameter"] ***REMOVED*** 170] ***REMOVED*** "Parameter";
  SyntaxKind4[SyntaxKind4["Decorator"] ***REMOVED*** 171] ***REMOVED*** "Decorator";
  SyntaxKind4[SyntaxKind4["PropertySignature"] ***REMOVED*** 172] ***REMOVED*** "PropertySignature";
  SyntaxKind4[SyntaxKind4["PropertyDeclaration"] ***REMOVED*** 173] ***REMOVED*** "PropertyDeclaration";
  SyntaxKind4[SyntaxKind4["MethodSignature"] ***REMOVED*** 174] ***REMOVED*** "MethodSignature";
  SyntaxKind4[SyntaxKind4["MethodDeclaration"] ***REMOVED*** 175] ***REMOVED*** "MethodDeclaration";
  SyntaxKind4[SyntaxKind4["ClassStaticBlockDeclaration"] ***REMOVED*** 176] ***REMOVED*** "ClassStaticBlockDeclaration";
  SyntaxKind4[SyntaxKind4["Constructor"] ***REMOVED*** 177] ***REMOVED*** "Constructor";
  SyntaxKind4[SyntaxKind4["GetAccessor"] ***REMOVED*** 178] ***REMOVED*** "GetAccessor";
  SyntaxKind4[SyntaxKind4["SetAccessor"] ***REMOVED*** 179] ***REMOVED*** "SetAccessor";
  SyntaxKind4[SyntaxKind4["CallSignature"] ***REMOVED*** 180] ***REMOVED*** "CallSignature";
  SyntaxKind4[SyntaxKind4["ConstructSignature"] ***REMOVED*** 181] ***REMOVED*** "ConstructSignature";
  SyntaxKind4[SyntaxKind4["IndexSignature"] ***REMOVED*** 182] ***REMOVED*** "IndexSignature";
  SyntaxKind4[SyntaxKind4["TypePredicate"] ***REMOVED*** 183] ***REMOVED*** "TypePredicate";
  SyntaxKind4[SyntaxKind4["TypeReference"] ***REMOVED*** 184] ***REMOVED*** "TypeReference";
  SyntaxKind4[SyntaxKind4["FunctionType"] ***REMOVED*** 185] ***REMOVED*** "FunctionType";
  SyntaxKind4[SyntaxKind4["ConstructorType"] ***REMOVED*** 186] ***REMOVED*** "ConstructorType";
  SyntaxKind4[SyntaxKind4["TypeQuery"] ***REMOVED*** 187] ***REMOVED*** "TypeQuery";
  SyntaxKind4[SyntaxKind4["TypeLiteral"] ***REMOVED*** 188] ***REMOVED*** "TypeLiteral";
  SyntaxKind4[SyntaxKind4["ArrayType"] ***REMOVED*** 189] ***REMOVED*** "ArrayType";
  SyntaxKind4[SyntaxKind4["TupleType"] ***REMOVED*** 190] ***REMOVED*** "TupleType";
  SyntaxKind4[SyntaxKind4["OptionalType"] ***REMOVED*** 191] ***REMOVED*** "OptionalType";
  SyntaxKind4[SyntaxKind4["RestType"] ***REMOVED*** 192] ***REMOVED*** "RestType";
  SyntaxKind4[SyntaxKind4["UnionType"] ***REMOVED*** 193] ***REMOVED*** "UnionType";
  SyntaxKind4[SyntaxKind4["IntersectionType"] ***REMOVED*** 194] ***REMOVED*** "IntersectionType";
  SyntaxKind4[SyntaxKind4["ConditionalType"] ***REMOVED*** 195] ***REMOVED*** "ConditionalType";
  SyntaxKind4[SyntaxKind4["InferType"] ***REMOVED*** 196] ***REMOVED*** "InferType";
  SyntaxKind4[SyntaxKind4["ParenthesizedType"] ***REMOVED*** 197] ***REMOVED*** "ParenthesizedType";
  SyntaxKind4[SyntaxKind4["ThisType"] ***REMOVED*** 198] ***REMOVED*** "ThisType";
  SyntaxKind4[SyntaxKind4["TypeOperator"] ***REMOVED*** 199] ***REMOVED*** "TypeOperator";
  SyntaxKind4[SyntaxKind4["IndexedAccessType"] ***REMOVED*** 200] ***REMOVED*** "IndexedAccessType";
  SyntaxKind4[SyntaxKind4["MappedType"] ***REMOVED*** 201] ***REMOVED*** "MappedType";
  SyntaxKind4[SyntaxKind4["LiteralType"] ***REMOVED*** 202] ***REMOVED*** "LiteralType";
  SyntaxKind4[SyntaxKind4["NamedTupleMember"] ***REMOVED*** 203] ***REMOVED*** "NamedTupleMember";
  SyntaxKind4[SyntaxKind4["TemplateLiteralType"] ***REMOVED*** 204] ***REMOVED*** "TemplateLiteralType";
  SyntaxKind4[SyntaxKind4["TemplateLiteralTypeSpan"] ***REMOVED*** 205] ***REMOVED*** "TemplateLiteralTypeSpan";
  SyntaxKind4[SyntaxKind4["ImportType"] ***REMOVED*** 206] ***REMOVED*** "ImportType";
  SyntaxKind4[SyntaxKind4["ObjectBindingPattern"] ***REMOVED*** 207] ***REMOVED*** "ObjectBindingPattern";
  SyntaxKind4[SyntaxKind4["ArrayBindingPattern"] ***REMOVED*** 208] ***REMOVED*** "ArrayBindingPattern";
  SyntaxKind4[SyntaxKind4["BindingElement"] ***REMOVED*** 209] ***REMOVED*** "BindingElement";
  SyntaxKind4[SyntaxKind4["ArrayLiteralExpression"] ***REMOVED*** 210] ***REMOVED*** "ArrayLiteralExpression";
  SyntaxKind4[SyntaxKind4["ObjectLiteralExpression"] ***REMOVED*** 211] ***REMOVED*** "ObjectLiteralExpression";
  SyntaxKind4[SyntaxKind4["PropertyAccessExpression"] ***REMOVED*** 212] ***REMOVED*** "PropertyAccessExpression";
  SyntaxKind4[SyntaxKind4["ElementAccessExpression"] ***REMOVED*** 213] ***REMOVED*** "ElementAccessExpression";
  SyntaxKind4[SyntaxKind4["CallExpression"] ***REMOVED*** 214] ***REMOVED*** "CallExpression";
  SyntaxKind4[SyntaxKind4["NewExpression"] ***REMOVED*** 215] ***REMOVED*** "NewExpression";
  SyntaxKind4[SyntaxKind4["TaggedTemplateExpression"] ***REMOVED*** 216] ***REMOVED*** "TaggedTemplateExpression";
  SyntaxKind4[SyntaxKind4["TypeAssertionExpression"] ***REMOVED*** 217] ***REMOVED*** "TypeAssertionExpression";
  SyntaxKind4[SyntaxKind4["ParenthesizedExpression"] ***REMOVED*** 218] ***REMOVED*** "ParenthesizedExpression";
  SyntaxKind4[SyntaxKind4["FunctionExpression"] ***REMOVED*** 219] ***REMOVED*** "FunctionExpression";
  SyntaxKind4[SyntaxKind4["ArrowFunction"] ***REMOVED*** 220] ***REMOVED*** "ArrowFunction";
  SyntaxKind4[SyntaxKind4["DeleteExpression"] ***REMOVED*** 221] ***REMOVED*** "DeleteExpression";
  SyntaxKind4[SyntaxKind4["TypeOfExpression"] ***REMOVED*** 222] ***REMOVED*** "TypeOfExpression";
  SyntaxKind4[SyntaxKind4["VoidExpression"] ***REMOVED*** 223] ***REMOVED*** "VoidExpression";
  SyntaxKind4[SyntaxKind4["AwaitExpression"] ***REMOVED*** 224] ***REMOVED*** "AwaitExpression";
  SyntaxKind4[SyntaxKind4["PrefixUnaryExpression"] ***REMOVED*** 225] ***REMOVED*** "PrefixUnaryExpression";
  SyntaxKind4[SyntaxKind4["PostfixUnaryExpression"] ***REMOVED*** 226] ***REMOVED*** "PostfixUnaryExpression";
  SyntaxKind4[SyntaxKind4["BinaryExpression"] ***REMOVED*** 227] ***REMOVED*** "BinaryExpression";
  SyntaxKind4[SyntaxKind4["ConditionalExpression"] ***REMOVED*** 228] ***REMOVED*** "ConditionalExpression";
  SyntaxKind4[SyntaxKind4["TemplateExpression"] ***REMOVED*** 229] ***REMOVED*** "TemplateExpression";
  SyntaxKind4[SyntaxKind4["YieldExpression"] ***REMOVED*** 230] ***REMOVED*** "YieldExpression";
  SyntaxKind4[SyntaxKind4["SpreadElement"] ***REMOVED*** 231] ***REMOVED*** "SpreadElement";
  SyntaxKind4[SyntaxKind4["ClassExpression"] ***REMOVED*** 232] ***REMOVED*** "ClassExpression";
  SyntaxKind4[SyntaxKind4["OmittedExpression"] ***REMOVED*** 233] ***REMOVED*** "OmittedExpression";
  SyntaxKind4[SyntaxKind4["ExpressionWithTypeArguments"] ***REMOVED*** 234] ***REMOVED*** "ExpressionWithTypeArguments";
  SyntaxKind4[SyntaxKind4["AsExpression"] ***REMOVED*** 235] ***REMOVED*** "AsExpression";
  SyntaxKind4[SyntaxKind4["NonNullExpression"] ***REMOVED*** 236] ***REMOVED*** "NonNullExpression";
  SyntaxKind4[SyntaxKind4["MetaProperty"] ***REMOVED*** 237] ***REMOVED*** "MetaProperty";
  SyntaxKind4[SyntaxKind4["SyntheticExpression"] ***REMOVED*** 238] ***REMOVED*** "SyntheticExpression";
  SyntaxKind4[SyntaxKind4["SatisfiesExpression"] ***REMOVED*** 239] ***REMOVED*** "SatisfiesExpression";
  SyntaxKind4[SyntaxKind4["TemplateSpan"] ***REMOVED*** 240] ***REMOVED*** "TemplateSpan";
  SyntaxKind4[SyntaxKind4["SemicolonClassElement"] ***REMOVED*** 241] ***REMOVED*** "SemicolonClassElement";
  SyntaxKind4[SyntaxKind4["Block"] ***REMOVED*** 242] ***REMOVED*** "Block";
  SyntaxKind4[SyntaxKind4["EmptyStatement"] ***REMOVED*** 243] ***REMOVED*** "EmptyStatement";
  SyntaxKind4[SyntaxKind4["VariableStatement"] ***REMOVED*** 244] ***REMOVED*** "VariableStatement";
  SyntaxKind4[SyntaxKind4["ExpressionStatement"] ***REMOVED*** 245] ***REMOVED*** "ExpressionStatement";
  SyntaxKind4[SyntaxKind4["IfStatement"] ***REMOVED*** 246] ***REMOVED*** "IfStatement";
  SyntaxKind4[SyntaxKind4["DoStatement"] ***REMOVED*** 247] ***REMOVED*** "DoStatement";
  SyntaxKind4[SyntaxKind4["WhileStatement"] ***REMOVED*** 248] ***REMOVED*** "WhileStatement";
  SyntaxKind4[SyntaxKind4["ForStatement"] ***REMOVED*** 249] ***REMOVED*** "ForStatement";
  SyntaxKind4[SyntaxKind4["ForInStatement"] ***REMOVED*** 250] ***REMOVED*** "ForInStatement";
  SyntaxKind4[SyntaxKind4["ForOfStatement"] ***REMOVED*** 251] ***REMOVED*** "ForOfStatement";
  SyntaxKind4[SyntaxKind4["ContinueStatement"] ***REMOVED*** 252] ***REMOVED*** "ContinueStatement";
  SyntaxKind4[SyntaxKind4["BreakStatement"] ***REMOVED*** 253] ***REMOVED*** "BreakStatement";
  SyntaxKind4[SyntaxKind4["ReturnStatement"] ***REMOVED*** 254] ***REMOVED*** "ReturnStatement";
  SyntaxKind4[SyntaxKind4["WithStatement"] ***REMOVED*** 255] ***REMOVED*** "WithStatement";
  SyntaxKind4[SyntaxKind4["SwitchStatement"] ***REMOVED*** 256] ***REMOVED*** "SwitchStatement";
  SyntaxKind4[SyntaxKind4["LabeledStatement"] ***REMOVED*** 257] ***REMOVED*** "LabeledStatement";
  SyntaxKind4[SyntaxKind4["ThrowStatement"] ***REMOVED*** 258] ***REMOVED*** "ThrowStatement";
  SyntaxKind4[SyntaxKind4["TryStatement"] ***REMOVED*** 259] ***REMOVED*** "TryStatement";
  SyntaxKind4[SyntaxKind4["DebuggerStatement"] ***REMOVED*** 260] ***REMOVED*** "DebuggerStatement";
  SyntaxKind4[SyntaxKind4["VariableDeclaration"] ***REMOVED*** 261] ***REMOVED*** "VariableDeclaration";
  SyntaxKind4[SyntaxKind4["VariableDeclarationList"] ***REMOVED*** 262] ***REMOVED*** "VariableDeclarationList";
  SyntaxKind4[SyntaxKind4["FunctionDeclaration"] ***REMOVED*** 263] ***REMOVED*** "FunctionDeclaration";
  SyntaxKind4[SyntaxKind4["ClassDeclaration"] ***REMOVED*** 264] ***REMOVED*** "ClassDeclaration";
  SyntaxKind4[SyntaxKind4["InterfaceDeclaration"] ***REMOVED*** 265] ***REMOVED*** "InterfaceDeclaration";
  SyntaxKind4[SyntaxKind4["TypeAliasDeclaration"] ***REMOVED*** 266] ***REMOVED*** "TypeAliasDeclaration";
  SyntaxKind4[SyntaxKind4["EnumDeclaration"] ***REMOVED*** 267] ***REMOVED*** "EnumDeclaration";
  SyntaxKind4[SyntaxKind4["ModuleDeclaration"] ***REMOVED*** 268] ***REMOVED*** "ModuleDeclaration";
  SyntaxKind4[SyntaxKind4["ModuleBlock"] ***REMOVED*** 269] ***REMOVED*** "ModuleBlock";
  SyntaxKind4[SyntaxKind4["CaseBlock"] ***REMOVED*** 270] ***REMOVED*** "CaseBlock";
  SyntaxKind4[SyntaxKind4["NamespaceExportDeclaration"] ***REMOVED*** 271] ***REMOVED*** "NamespaceExportDeclaration";
  SyntaxKind4[SyntaxKind4["ImportEqualsDeclaration"] ***REMOVED*** 272] ***REMOVED*** "ImportEqualsDeclaration";
  SyntaxKind4[SyntaxKind4["ImportDeclaration"] ***REMOVED*** 273] ***REMOVED*** "ImportDeclaration";
  SyntaxKind4[SyntaxKind4["ImportClause"] ***REMOVED*** 274] ***REMOVED*** "ImportClause";
  SyntaxKind4[SyntaxKind4["NamespaceImport"] ***REMOVED*** 275] ***REMOVED*** "NamespaceImport";
  SyntaxKind4[SyntaxKind4["NamedImports"] ***REMOVED*** 276] ***REMOVED*** "NamedImports";
  SyntaxKind4[SyntaxKind4["ImportSpecifier"] ***REMOVED*** 277] ***REMOVED*** "ImportSpecifier";
  SyntaxKind4[SyntaxKind4["ExportAssignment"] ***REMOVED*** 278] ***REMOVED*** "ExportAssignment";
  SyntaxKind4[SyntaxKind4["ExportDeclaration"] ***REMOVED*** 279] ***REMOVED*** "ExportDeclaration";
  SyntaxKind4[SyntaxKind4["NamedExports"] ***REMOVED*** 280] ***REMOVED*** "NamedExports";
  SyntaxKind4[SyntaxKind4["NamespaceExport"] ***REMOVED*** 281] ***REMOVED*** "NamespaceExport";
  SyntaxKind4[SyntaxKind4["ExportSpecifier"] ***REMOVED*** 282] ***REMOVED*** "ExportSpecifier";
  SyntaxKind4[SyntaxKind4["MissingDeclaration"] ***REMOVED*** 283] ***REMOVED*** "MissingDeclaration";
  SyntaxKind4[SyntaxKind4["ExternalModuleReference"] ***REMOVED*** 284] ***REMOVED*** "ExternalModuleReference";
  SyntaxKind4[SyntaxKind4["JsxElement"] ***REMOVED*** 285] ***REMOVED*** "JsxElement";
  SyntaxKind4[SyntaxKind4["JsxSelfClosingElement"] ***REMOVED*** 286] ***REMOVED*** "JsxSelfClosingElement";
  SyntaxKind4[SyntaxKind4["JsxOpeningElement"] ***REMOVED*** 287] ***REMOVED*** "JsxOpeningElement";
  SyntaxKind4[SyntaxKind4["JsxClosingElement"] ***REMOVED*** 288] ***REMOVED*** "JsxClosingElement";
  SyntaxKind4[SyntaxKind4["JsxFragment"] ***REMOVED*** 289] ***REMOVED*** "JsxFragment";
  SyntaxKind4[SyntaxKind4["JsxOpeningFragment"] ***REMOVED*** 290] ***REMOVED*** "JsxOpeningFragment";
  SyntaxKind4[SyntaxKind4["JsxClosingFragment"] ***REMOVED*** 291] ***REMOVED*** "JsxClosingFragment";
  SyntaxKind4[SyntaxKind4["JsxAttribute"] ***REMOVED*** 292] ***REMOVED*** "JsxAttribute";
  SyntaxKind4[SyntaxKind4["JsxAttributes"] ***REMOVED*** 293] ***REMOVED*** "JsxAttributes";
  SyntaxKind4[SyntaxKind4["JsxSpreadAttribute"] ***REMOVED*** 294] ***REMOVED*** "JsxSpreadAttribute";
  SyntaxKind4[SyntaxKind4["JsxExpression"] ***REMOVED*** 295] ***REMOVED*** "JsxExpression";
  SyntaxKind4[SyntaxKind4["JsxNamespacedName"] ***REMOVED*** 296] ***REMOVED*** "JsxNamespacedName";
  SyntaxKind4[SyntaxKind4["CaseClause"] ***REMOVED*** 297] ***REMOVED*** "CaseClause";
  SyntaxKind4[SyntaxKind4["DefaultClause"] ***REMOVED*** 298] ***REMOVED*** "DefaultClause";
  SyntaxKind4[SyntaxKind4["HeritageClause"] ***REMOVED*** 299] ***REMOVED*** "HeritageClause";
  SyntaxKind4[SyntaxKind4["CatchClause"] ***REMOVED*** 300] ***REMOVED*** "CatchClause";
  SyntaxKind4[SyntaxKind4["ImportAttributes"] ***REMOVED*** 301] ***REMOVED*** "ImportAttributes";
  SyntaxKind4[SyntaxKind4["ImportAttribute"] ***REMOVED*** 302] ***REMOVED*** "ImportAttribute";
  SyntaxKind4[SyntaxKind4["AssertClause"] ***REMOVED*** 301 /* ImportAttributes */] ***REMOVED*** "AssertClause";
  SyntaxKind4[SyntaxKind4["AssertEntry"] ***REMOVED*** 302 /* ImportAttribute */] ***REMOVED*** "AssertEntry";
  SyntaxKind4[SyntaxKind4["ImportTypeAssertionContainer"] ***REMOVED*** 303] ***REMOVED*** "ImportTypeAssertionContainer";
  SyntaxKind4[SyntaxKind4["PropertyAssignment"] ***REMOVED*** 304] ***REMOVED*** "PropertyAssignment";
  SyntaxKind4[SyntaxKind4["ShorthandPropertyAssignment"] ***REMOVED*** 305] ***REMOVED*** "ShorthandPropertyAssignment";
  SyntaxKind4[SyntaxKind4["SpreadAssignment"] ***REMOVED*** 306] ***REMOVED*** "SpreadAssignment";
  SyntaxKind4[SyntaxKind4["EnumMember"] ***REMOVED*** 307] ***REMOVED*** "EnumMember";
  SyntaxKind4[SyntaxKind4["SourceFile"] ***REMOVED*** 308] ***REMOVED*** "SourceFile";
  SyntaxKind4[SyntaxKind4["Bundle"] ***REMOVED*** 309] ***REMOVED*** "Bundle";
  SyntaxKind4[SyntaxKind4["JSDocTypeExpression"] ***REMOVED*** 310] ***REMOVED*** "JSDocTypeExpression";
  SyntaxKind4[SyntaxKind4["JSDocNameReference"] ***REMOVED*** 311] ***REMOVED*** "JSDocNameReference";
  SyntaxKind4[SyntaxKind4["JSDocMemberName"] ***REMOVED*** 312] ***REMOVED*** "JSDocMemberName";
  SyntaxKind4[SyntaxKind4["JSDocAllType"] ***REMOVED*** 313] ***REMOVED*** "JSDocAllType";
  SyntaxKind4[SyntaxKind4["JSDocUnknownType"] ***REMOVED*** 314] ***REMOVED*** "JSDocUnknownType";
  SyntaxKind4[SyntaxKind4["JSDocNullableType"] ***REMOVED*** 315] ***REMOVED*** "JSDocNullableType";
  SyntaxKind4[SyntaxKind4["JSDocNonNullableType"] ***REMOVED*** 316] ***REMOVED*** "JSDocNonNullableType";
  SyntaxKind4[SyntaxKind4["JSDocOptionalType"] ***REMOVED*** 317] ***REMOVED*** "JSDocOptionalType";
  SyntaxKind4[SyntaxKind4["JSDocFunctionType"] ***REMOVED*** 318] ***REMOVED*** "JSDocFunctionType";
  SyntaxKind4[SyntaxKind4["JSDocVariadicType"] ***REMOVED*** 319] ***REMOVED*** "JSDocVariadicType";
  SyntaxKind4[SyntaxKind4["JSDocNamepathType"] ***REMOVED*** 320] ***REMOVED*** "JSDocNamepathType";
  SyntaxKind4[SyntaxKind4["JSDoc"] ***REMOVED*** 321] ***REMOVED*** "JSDoc";
  SyntaxKind4[SyntaxKind4["JSDocComment"] ***REMOVED*** 321 /* JSDoc */] ***REMOVED*** "JSDocComment";
  SyntaxKind4[SyntaxKind4["JSDocText"] ***REMOVED*** 322] ***REMOVED*** "JSDocText";
  SyntaxKind4[SyntaxKind4["JSDocTypeLiteral"] ***REMOVED*** 323] ***REMOVED*** "JSDocTypeLiteral";
  SyntaxKind4[SyntaxKind4["JSDocSignature"] ***REMOVED*** 324] ***REMOVED*** "JSDocSignature";
  SyntaxKind4[SyntaxKind4["JSDocLink"] ***REMOVED*** 325] ***REMOVED*** "JSDocLink";
  SyntaxKind4[SyntaxKind4["JSDocLinkCode"] ***REMOVED*** 326] ***REMOVED*** "JSDocLinkCode";
  SyntaxKind4[SyntaxKind4["JSDocLinkPlain"] ***REMOVED*** 327] ***REMOVED*** "JSDocLinkPlain";
  SyntaxKind4[SyntaxKind4["JSDocTag"] ***REMOVED*** 328] ***REMOVED*** "JSDocTag";
  SyntaxKind4[SyntaxKind4["JSDocAugmentsTag"] ***REMOVED*** 329] ***REMOVED*** "JSDocAugmentsTag";
  SyntaxKind4[SyntaxKind4["JSDocImplementsTag"] ***REMOVED*** 330] ***REMOVED*** "JSDocImplementsTag";
  SyntaxKind4[SyntaxKind4["JSDocAuthorTag"] ***REMOVED*** 331] ***REMOVED*** "JSDocAuthorTag";
  SyntaxKind4[SyntaxKind4["JSDocDeprecatedTag"] ***REMOVED*** 332] ***REMOVED*** "JSDocDeprecatedTag";
  SyntaxKind4[SyntaxKind4["JSDocClassTag"] ***REMOVED*** 333] ***REMOVED*** "JSDocClassTag";
  SyntaxKind4[SyntaxKind4["JSDocPublicTag"] ***REMOVED*** 334] ***REMOVED*** "JSDocPublicTag";
  SyntaxKind4[SyntaxKind4["JSDocPrivateTag"] ***REMOVED*** 335] ***REMOVED*** "JSDocPrivateTag";
  SyntaxKind4[SyntaxKind4["JSDocProtectedTag"] ***REMOVED*** 336] ***REMOVED*** "JSDocProtectedTag";
  SyntaxKind4[SyntaxKind4["JSDocReadonlyTag"] ***REMOVED*** 337] ***REMOVED*** "JSDocReadonlyTag";
  SyntaxKind4[SyntaxKind4["JSDocOverrideTag"] ***REMOVED*** 338] ***REMOVED*** "JSDocOverrideTag";
  SyntaxKind4[SyntaxKind4["JSDocCallbackTag"] ***REMOVED*** 339] ***REMOVED*** "JSDocCallbackTag";
  SyntaxKind4[SyntaxKind4["JSDocOverloadTag"] ***REMOVED*** 340] ***REMOVED*** "JSDocOverloadTag";
  SyntaxKind4[SyntaxKind4["JSDocEnumTag"] ***REMOVED*** 341] ***REMOVED*** "JSDocEnumTag";
  SyntaxKind4[SyntaxKind4["JSDocParameterTag"] ***REMOVED*** 342] ***REMOVED*** "JSDocParameterTag";
  SyntaxKind4[SyntaxKind4["JSDocReturnTag"] ***REMOVED*** 343] ***REMOVED*** "JSDocReturnTag";
  SyntaxKind4[SyntaxKind4["JSDocThisTag"] ***REMOVED*** 344] ***REMOVED*** "JSDocThisTag";
  SyntaxKind4[SyntaxKind4["JSDocTypeTag"] ***REMOVED*** 345] ***REMOVED*** "JSDocTypeTag";
  SyntaxKind4[SyntaxKind4["JSDocTemplateTag"] ***REMOVED*** 346] ***REMOVED*** "JSDocTemplateTag";
  SyntaxKind4[SyntaxKind4["JSDocTypedefTag"] ***REMOVED*** 347] ***REMOVED*** "JSDocTypedefTag";
  SyntaxKind4[SyntaxKind4["JSDocSeeTag"] ***REMOVED*** 348] ***REMOVED*** "JSDocSeeTag";
  SyntaxKind4[SyntaxKind4["JSDocPropertyTag"] ***REMOVED*** 349] ***REMOVED*** "JSDocPropertyTag";
  SyntaxKind4[SyntaxKind4["JSDocThrowsTag"] ***REMOVED*** 350] ***REMOVED*** "JSDocThrowsTag";
  SyntaxKind4[SyntaxKind4["JSDocSatisfiesTag"] ***REMOVED*** 351] ***REMOVED*** "JSDocSatisfiesTag";
  SyntaxKind4[SyntaxKind4["JSDocImportTag"] ***REMOVED*** 352] ***REMOVED*** "JSDocImportTag";
  SyntaxKind4[SyntaxKind4["SyntaxList"] ***REMOVED*** 353] ***REMOVED*** "SyntaxList";
  SyntaxKind4[SyntaxKind4["NotEmittedStatement"] ***REMOVED*** 354] ***REMOVED*** "NotEmittedStatement";
  SyntaxKind4[SyntaxKind4["NotEmittedTypeElement"] ***REMOVED*** 355] ***REMOVED*** "NotEmittedTypeElement";
  SyntaxKind4[SyntaxKind4["PartiallyEmittedExpression"] ***REMOVED*** 356] ***REMOVED*** "PartiallyEmittedExpression";
  SyntaxKind4[SyntaxKind4["CommaListExpression"] ***REMOVED*** 357] ***REMOVED*** "CommaListExpression";
  SyntaxKind4[SyntaxKind4["SyntheticReferenceExpression"] ***REMOVED*** 358] ***REMOVED*** "SyntheticReferenceExpression";
  SyntaxKind4[SyntaxKind4["Count"] ***REMOVED*** 359] ***REMOVED*** "Count";
  SyntaxKind4[SyntaxKind4["FirstAssignment"] ***REMOVED*** 64 /* EqualsToken */] ***REMOVED*** "FirstAssignment";
  SyntaxKind4[SyntaxKind4["LastAssignment"] ***REMOVED*** 79 /* CaretEqualsToken */] ***REMOVED*** "LastAssignment";
  SyntaxKind4[SyntaxKind4["FirstCompoundAssignment"] ***REMOVED*** 65 /* PlusEqualsToken */] ***REMOVED*** "FirstCompoundAssignment";
  SyntaxKind4[SyntaxKind4["LastCompoundAssignment"] ***REMOVED*** 79 /* CaretEqualsToken */] ***REMOVED*** "LastCompoundAssignment";
  SyntaxKind4[SyntaxKind4["FirstReservedWord"] ***REMOVED*** 83 /* BreakKeyword */] ***REMOVED*** "FirstReservedWord";
  SyntaxKind4[SyntaxKind4["LastReservedWord"] ***REMOVED*** 118 /* WithKeyword */] ***REMOVED*** "LastReservedWord";
  SyntaxKind4[SyntaxKind4["FirstKeyword"] ***REMOVED*** 83 /* BreakKeyword */] ***REMOVED*** "FirstKeyword";
  SyntaxKind4[SyntaxKind4["LastKeyword"] ***REMOVED*** 166 /* DeferKeyword */] ***REMOVED*** "LastKeyword";
  SyntaxKind4[SyntaxKind4["FirstFutureReservedWord"] ***REMOVED*** 119 /* ImplementsKeyword */] ***REMOVED*** "FirstFutureReservedWord";
  SyntaxKind4[SyntaxKind4["LastFutureReservedWord"] ***REMOVED*** 127 /* YieldKeyword */] ***REMOVED*** "LastFutureReservedWord";
  SyntaxKind4[SyntaxKind4["FirstTypeNode"] ***REMOVED*** 183 /* TypePredicate */] ***REMOVED*** "FirstTypeNode";
  SyntaxKind4[SyntaxKind4["LastTypeNode"] ***REMOVED*** 206 /* ImportType */] ***REMOVED*** "LastTypeNode";
  SyntaxKind4[SyntaxKind4["FirstPunctuation"] ***REMOVED*** 19 /* OpenBraceToken */] ***REMOVED*** "FirstPunctuation";
  SyntaxKind4[SyntaxKind4["LastPunctuation"] ***REMOVED*** 79 /* CaretEqualsToken */] ***REMOVED*** "LastPunctuation";
  SyntaxKind4[SyntaxKind4["FirstToken"] ***REMOVED*** 0 /* Unknown */] ***REMOVED*** "FirstToken";
  SyntaxKind4[SyntaxKind4["LastToken"] ***REMOVED*** 166 /* LastKeyword */] ***REMOVED*** "LastToken";
  SyntaxKind4[SyntaxKind4["FirstTriviaToken"] ***REMOVED*** 2 /* SingleLineCommentTrivia */] ***REMOVED*** "FirstTriviaToken";
  SyntaxKind4[SyntaxKind4["LastTriviaToken"] ***REMOVED*** 7 /* ConflictMarkerTrivia */] ***REMOVED*** "LastTriviaToken";
  SyntaxKind4[SyntaxKind4["FirstLiteralToken"] ***REMOVED*** 9 /* NumericLiteral */] ***REMOVED*** "FirstLiteralToken";
  SyntaxKind4[SyntaxKind4["LastLiteralToken"] ***REMOVED*** 15 /* NoSubstitutionTemplateLiteral */] ***REMOVED*** "LastLiteralToken";
  SyntaxKind4[SyntaxKind4["FirstTemplateToken"] ***REMOVED*** 15 /* NoSubstitutionTemplateLiteral */] ***REMOVED*** "FirstTemplateToken";
  SyntaxKind4[SyntaxKind4["LastTemplateToken"] ***REMOVED*** 18 /* TemplateTail */] ***REMOVED*** "LastTemplateToken";
  SyntaxKind4[SyntaxKind4["FirstBinaryOperator"] ***REMOVED*** 30 /* LessThanToken */] ***REMOVED*** "FirstBinaryOperator";
  SyntaxKind4[SyntaxKind4["LastBinaryOperator"] ***REMOVED*** 79 /* CaretEqualsToken */] ***REMOVED*** "LastBinaryOperator";
  SyntaxKind4[SyntaxKind4["FirstStatement"] ***REMOVED*** 244 /* VariableStatement */] ***REMOVED*** "FirstStatement";
  SyntaxKind4[SyntaxKind4["LastStatement"] ***REMOVED*** 260 /* DebuggerStatement */] ***REMOVED*** "LastStatement";
  SyntaxKind4[SyntaxKind4["FirstNode"] ***REMOVED*** 167 /* QualifiedName */] ***REMOVED*** "FirstNode";
  SyntaxKind4[SyntaxKind4["FirstJSDocNode"] ***REMOVED*** 310 /* JSDocTypeExpression */] ***REMOVED*** "FirstJSDocNode";
  SyntaxKind4[SyntaxKind4["LastJSDocNode"] ***REMOVED*** 352 /* JSDocImportTag */] ***REMOVED*** "LastJSDocNode";
  SyntaxKind4[SyntaxKind4["FirstJSDocTagNode"] ***REMOVED*** 328 /* JSDocTag */] ***REMOVED*** "FirstJSDocTagNode";
  SyntaxKind4[SyntaxKind4["LastJSDocTagNode"] ***REMOVED*** 352 /* JSDocImportTag */] ***REMOVED*** "LastJSDocTagNode";
  SyntaxKind4[SyntaxKind4["FirstContextualKeyword"] ***REMOVED*** 128 /* AbstractKeyword */] ***REMOVED*** "FirstContextualKeyword";
  SyntaxKind4[SyntaxKind4["LastContextualKeyword"] ***REMOVED*** 166 /* LastKeyword */] ***REMOVED*** "LastContextualKeyword";
  return SyntaxKind4;
})(SyntaxKind || {});
var NodeFlags ***REMOVED*** /* @__PURE__ */ ((NodeFlags3) ***REMOVED***> {
  NodeFlags3[NodeFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  NodeFlags3[NodeFlags3["Let"] ***REMOVED*** 1] ***REMOVED*** "Let";
  NodeFlags3[NodeFlags3["Const"] ***REMOVED*** 2] ***REMOVED*** "Const";
  NodeFlags3[NodeFlags3["Using"] ***REMOVED*** 4] ***REMOVED*** "Using";
  NodeFlags3[NodeFlags3["AwaitUsing"] ***REMOVED*** 6] ***REMOVED*** "AwaitUsing";
  NodeFlags3[NodeFlags3["NestedNamespace"] ***REMOVED*** 8] ***REMOVED*** "NestedNamespace";
  NodeFlags3[NodeFlags3["Synthesized"] ***REMOVED*** 16] ***REMOVED*** "Synthesized";
  NodeFlags3[NodeFlags3["Namespace"] ***REMOVED*** 32] ***REMOVED*** "Namespace";
  NodeFlags3[NodeFlags3["OptionalChain"] ***REMOVED*** 64] ***REMOVED*** "OptionalChain";
  NodeFlags3[NodeFlags3["ExportContext"] ***REMOVED*** 128] ***REMOVED*** "ExportContext";
  NodeFlags3[NodeFlags3["ContainsThis"] ***REMOVED*** 256] ***REMOVED*** "ContainsThis";
  NodeFlags3[NodeFlags3["HasImplicitReturn"] ***REMOVED*** 512] ***REMOVED*** "HasImplicitReturn";
  NodeFlags3[NodeFlags3["HasExplicitReturn"] ***REMOVED*** 1024] ***REMOVED*** "HasExplicitReturn";
  NodeFlags3[NodeFlags3["GlobalAugmentation"] ***REMOVED*** 2048] ***REMOVED*** "GlobalAugmentation";
  NodeFlags3[NodeFlags3["HasAsyncFunctions"] ***REMOVED*** 4096] ***REMOVED*** "HasAsyncFunctions";
  NodeFlags3[NodeFlags3["DisallowInContext"] ***REMOVED*** 8192] ***REMOVED*** "DisallowInContext";
  NodeFlags3[NodeFlags3["YieldContext"] ***REMOVED*** 16384] ***REMOVED*** "YieldContext";
  NodeFlags3[NodeFlags3["DecoratorContext"] ***REMOVED*** 32768] ***REMOVED*** "DecoratorContext";
  NodeFlags3[NodeFlags3["AwaitContext"] ***REMOVED*** 65536] ***REMOVED*** "AwaitContext";
  NodeFlags3[NodeFlags3["DisallowConditionalTypesContext"] ***REMOVED*** 131072] ***REMOVED*** "DisallowConditionalTypesContext";
  NodeFlags3[NodeFlags3["ThisNodeHasError"] ***REMOVED*** 262144] ***REMOVED*** "ThisNodeHasError";
  NodeFlags3[NodeFlags3["JavaScriptFile"] ***REMOVED*** 524288] ***REMOVED*** "JavaScriptFile";
  NodeFlags3[NodeFlags3["ThisNodeOrAnySubNodesHasError"] ***REMOVED*** 1048576] ***REMOVED*** "ThisNodeOrAnySubNodesHasError";
  NodeFlags3[NodeFlags3["HasAggregatedChildData"] ***REMOVED*** 2097152] ***REMOVED*** "HasAggregatedChildData";
  NodeFlags3[NodeFlags3["PossiblyContainsDynamicImport"] ***REMOVED*** 4194304] ***REMOVED*** "PossiblyContainsDynamicImport";
  NodeFlags3[NodeFlags3["PossiblyContainsImportMeta"] ***REMOVED*** 8388608] ***REMOVED*** "PossiblyContainsImportMeta";
  NodeFlags3[NodeFlags3["JSDoc"] ***REMOVED*** 16777216] ***REMOVED*** "JSDoc";
  NodeFlags3[NodeFlags3["Ambient"] ***REMOVED*** 33554432] ***REMOVED*** "Ambient";
  NodeFlags3[NodeFlags3["InWithStatement"] ***REMOVED*** 67108864] ***REMOVED*** "InWithStatement";
  NodeFlags3[NodeFlags3["JsonFile"] ***REMOVED*** 134217728] ***REMOVED*** "JsonFile";
  NodeFlags3[NodeFlags3["TypeCached"] ***REMOVED*** 268435456] ***REMOVED*** "TypeCached";
  NodeFlags3[NodeFlags3["Deprecated"] ***REMOVED*** 536870912] ***REMOVED*** "Deprecated";
  NodeFlags3[NodeFlags3["BlockScoped"] ***REMOVED*** 7] ***REMOVED*** "BlockScoped";
  NodeFlags3[NodeFlags3["Constant"] ***REMOVED*** 6] ***REMOVED*** "Constant";
  NodeFlags3[NodeFlags3["ReachabilityCheckFlags"] ***REMOVED*** 1536] ***REMOVED*** "ReachabilityCheckFlags";
  NodeFlags3[NodeFlags3["ReachabilityAndEmitFlags"] ***REMOVED*** 5632] ***REMOVED*** "ReachabilityAndEmitFlags";
  NodeFlags3[NodeFlags3["ContextFlags"] ***REMOVED*** 101441536] ***REMOVED*** "ContextFlags";
  NodeFlags3[NodeFlags3["TypeExcludesFlags"] ***REMOVED*** 81920] ***REMOVED*** "TypeExcludesFlags";
  NodeFlags3[NodeFlags3["PermanentlySetIncrementalFlags"] ***REMOVED*** 12582912] ***REMOVED*** "PermanentlySetIncrementalFlags";
  NodeFlags3[NodeFlags3["IdentifierHasExtendedUnicodeEscape"] ***REMOVED*** 256 /* ContainsThis */] ***REMOVED*** "IdentifierHasExtendedUnicodeEscape";
  NodeFlags3[NodeFlags3["IdentifierIsInJSDocNamespace"] ***REMOVED*** 4096 /* HasAsyncFunctions */] ***REMOVED*** "IdentifierIsInJSDocNamespace";
  return NodeFlags3;
})(NodeFlags || {});
var ModifierFlags ***REMOVED*** /* @__PURE__ */ ((ModifierFlags3) ***REMOVED***> {
  ModifierFlags3[ModifierFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  ModifierFlags3[ModifierFlags3["Public"] ***REMOVED*** 1] ***REMOVED*** "Public";
  ModifierFlags3[ModifierFlags3["Private"] ***REMOVED*** 2] ***REMOVED*** "Private";
  ModifierFlags3[ModifierFlags3["Protected"] ***REMOVED*** 4] ***REMOVED*** "Protected";
  ModifierFlags3[ModifierFlags3["Readonly"] ***REMOVED*** 8] ***REMOVED*** "Readonly";
  ModifierFlags3[ModifierFlags3["Override"] ***REMOVED*** 16] ***REMOVED*** "Override";
  ModifierFlags3[ModifierFlags3["Export"] ***REMOVED*** 32] ***REMOVED*** "Export";
  ModifierFlags3[ModifierFlags3["Abstract"] ***REMOVED*** 64] ***REMOVED*** "Abstract";
  ModifierFlags3[ModifierFlags3["Ambient"] ***REMOVED*** 128] ***REMOVED*** "Ambient";
  ModifierFlags3[ModifierFlags3["Static"] ***REMOVED*** 256] ***REMOVED*** "Static";
  ModifierFlags3[ModifierFlags3["Accessor"] ***REMOVED*** 512] ***REMOVED*** "Accessor";
  ModifierFlags3[ModifierFlags3["Async"] ***REMOVED*** 1024] ***REMOVED*** "Async";
  ModifierFlags3[ModifierFlags3["Default"] ***REMOVED*** 2048] ***REMOVED*** "Default";
  ModifierFlags3[ModifierFlags3["Const"] ***REMOVED*** 4096] ***REMOVED*** "Const";
  ModifierFlags3[ModifierFlags3["In"] ***REMOVED*** 8192] ***REMOVED*** "In";
  ModifierFlags3[ModifierFlags3["Out"] ***REMOVED*** 16384] ***REMOVED*** "Out";
  ModifierFlags3[ModifierFlags3["Decorator"] ***REMOVED*** 32768] ***REMOVED*** "Decorator";
  ModifierFlags3[ModifierFlags3["Deprecated"] ***REMOVED*** 65536] ***REMOVED*** "Deprecated";
  ModifierFlags3[ModifierFlags3["JSDocPublic"] ***REMOVED*** 8388608] ***REMOVED*** "JSDocPublic";
  ModifierFlags3[ModifierFlags3["JSDocPrivate"] ***REMOVED*** 16777216] ***REMOVED*** "JSDocPrivate";
  ModifierFlags3[ModifierFlags3["JSDocProtected"] ***REMOVED*** 33554432] ***REMOVED*** "JSDocProtected";
  ModifierFlags3[ModifierFlags3["JSDocReadonly"] ***REMOVED*** 67108864] ***REMOVED*** "JSDocReadonly";
  ModifierFlags3[ModifierFlags3["JSDocOverride"] ***REMOVED*** 134217728] ***REMOVED*** "JSDocOverride";
  ModifierFlags3[ModifierFlags3["SyntacticOrJSDocModifiers"] ***REMOVED*** 31] ***REMOVED*** "SyntacticOrJSDocModifiers";
  ModifierFlags3[ModifierFlags3["SyntacticOnlyModifiers"] ***REMOVED*** 65504] ***REMOVED*** "SyntacticOnlyModifiers";
  ModifierFlags3[ModifierFlags3["SyntacticModifiers"] ***REMOVED*** 65535] ***REMOVED*** "SyntacticModifiers";
  ModifierFlags3[ModifierFlags3["JSDocCacheOnlyModifiers"] ***REMOVED*** 260046848] ***REMOVED*** "JSDocCacheOnlyModifiers";
  ModifierFlags3[ModifierFlags3["JSDocOnlyModifiers"] ***REMOVED*** 65536 /* Deprecated */] ***REMOVED*** "JSDocOnlyModifiers";
  ModifierFlags3[ModifierFlags3["NonCacheOnlyModifiers"] ***REMOVED*** 131071] ***REMOVED*** "NonCacheOnlyModifiers";
  ModifierFlags3[ModifierFlags3["HasComputedJSDocModifiers"] ***REMOVED*** 268435456] ***REMOVED*** "HasComputedJSDocModifiers";
  ModifierFlags3[ModifierFlags3["HasComputedFlags"] ***REMOVED*** 536870912] ***REMOVED*** "HasComputedFlags";
  ModifierFlags3[ModifierFlags3["AccessibilityModifier"] ***REMOVED*** 7] ***REMOVED*** "AccessibilityModifier";
  ModifierFlags3[ModifierFlags3["ParameterPropertyModifier"] ***REMOVED*** 31] ***REMOVED*** "ParameterPropertyModifier";
  ModifierFlags3[ModifierFlags3["NonPublicAccessibilityModifier"] ***REMOVED*** 6] ***REMOVED*** "NonPublicAccessibilityModifier";
  ModifierFlags3[ModifierFlags3["TypeScriptModifier"] ***REMOVED*** 28895] ***REMOVED*** "TypeScriptModifier";
  ModifierFlags3[ModifierFlags3["ExportDefault"] ***REMOVED*** 2080] ***REMOVED*** "ExportDefault";
  ModifierFlags3[ModifierFlags3["All"] ***REMOVED*** 131071] ***REMOVED*** "All";
  ModifierFlags3[ModifierFlags3["Modifier"] ***REMOVED*** 98303] ***REMOVED*** "Modifier";
  return ModifierFlags3;
})(ModifierFlags || {});
var RelationComparisonResult ***REMOVED*** /* @__PURE__ */ ((RelationComparisonResult3) ***REMOVED***> {
  RelationComparisonResult3[RelationComparisonResult3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  RelationComparisonResult3[RelationComparisonResult3["Succeeded"] ***REMOVED*** 1] ***REMOVED*** "Succeeded";
  RelationComparisonResult3[RelationComparisonResult3["Failed"] ***REMOVED*** 2] ***REMOVED*** "Failed";
  RelationComparisonResult3[RelationComparisonResult3["ReportsUnmeasurable"] ***REMOVED*** 8] ***REMOVED*** "ReportsUnmeasurable";
  RelationComparisonResult3[RelationComparisonResult3["ReportsUnreliable"] ***REMOVED*** 16] ***REMOVED*** "ReportsUnreliable";
  RelationComparisonResult3[RelationComparisonResult3["ReportsMask"] ***REMOVED*** 24] ***REMOVED*** "ReportsMask";
  RelationComparisonResult3[RelationComparisonResult3["ComplexityOverflow"] ***REMOVED*** 32] ***REMOVED*** "ComplexityOverflow";
  RelationComparisonResult3[RelationComparisonResult3["StackDepthOverflow"] ***REMOVED*** 64] ***REMOVED*** "StackDepthOverflow";
  RelationComparisonResult3[RelationComparisonResult3["Overflow"] ***REMOVED*** 96] ***REMOVED*** "Overflow";
  return RelationComparisonResult3;
})(RelationComparisonResult || {});
var GeneratedIdentifierFlags ***REMOVED*** /* @__PURE__ */ ((GeneratedIdentifierFlags2) ***REMOVED***> {
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["Auto"] ***REMOVED*** 1] ***REMOVED*** "Auto";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["Loop"] ***REMOVED*** 2] ***REMOVED*** "Loop";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["Unique"] ***REMOVED*** 3] ***REMOVED*** "Unique";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["Node"] ***REMOVED*** 4] ***REMOVED*** "Node";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["KindMask"] ***REMOVED*** 7] ***REMOVED*** "KindMask";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["ReservedInNestedScopes"] ***REMOVED*** 8] ***REMOVED*** "ReservedInNestedScopes";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["Optimistic"] ***REMOVED*** 16] ***REMOVED*** "Optimistic";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["FileLevel"] ***REMOVED*** 32] ***REMOVED*** "FileLevel";
  GeneratedIdentifierFlags2[GeneratedIdentifierFlags2["AllowNameSubstitution"] ***REMOVED*** 64] ***REMOVED*** "AllowNameSubstitution";
  return GeneratedIdentifierFlags2;
})(GeneratedIdentifierFlags || {});
var FlowFlags ***REMOVED*** /* @__PURE__ */ ((FlowFlags2) ***REMOVED***> {
  FlowFlags2[FlowFlags2["Unreachable"] ***REMOVED*** 1] ***REMOVED*** "Unreachable";
  FlowFlags2[FlowFlags2["Start"] ***REMOVED*** 2] ***REMOVED*** "Start";
  FlowFlags2[FlowFlags2["BranchLabel"] ***REMOVED*** 4] ***REMOVED*** "BranchLabel";
  FlowFlags2[FlowFlags2["LoopLabel"] ***REMOVED*** 8] ***REMOVED*** "LoopLabel";
  FlowFlags2[FlowFlags2["Assignment"] ***REMOVED*** 16] ***REMOVED*** "Assignment";
  FlowFlags2[FlowFlags2["TrueCondition"] ***REMOVED*** 32] ***REMOVED*** "TrueCondition";
  FlowFlags2[FlowFlags2["FalseCondition"] ***REMOVED*** 64] ***REMOVED*** "FalseCondition";
  FlowFlags2[FlowFlags2["SwitchClause"] ***REMOVED*** 128] ***REMOVED*** "SwitchClause";
  FlowFlags2[FlowFlags2["ArrayMutation"] ***REMOVED*** 256] ***REMOVED*** "ArrayMutation";
  FlowFlags2[FlowFlags2["Call"] ***REMOVED*** 512] ***REMOVED*** "Call";
  FlowFlags2[FlowFlags2["ReduceLabel"] ***REMOVED*** 1024] ***REMOVED*** "ReduceLabel";
  FlowFlags2[FlowFlags2["Referenced"] ***REMOVED*** 2048] ***REMOVED*** "Referenced";
  FlowFlags2[FlowFlags2["Shared"] ***REMOVED*** 4096] ***REMOVED*** "Shared";
  FlowFlags2[FlowFlags2["Label"] ***REMOVED*** 12] ***REMOVED*** "Label";
  FlowFlags2[FlowFlags2["Condition"] ***REMOVED*** 96] ***REMOVED*** "Condition";
  return FlowFlags2;
})(FlowFlags || {});
var OperationCanceledException ***REMOVED*** class {
};
var FileIncludeKind ***REMOVED*** /* @__PURE__ */ ((FileIncludeKind2) ***REMOVED***> {
  FileIncludeKind2[FileIncludeKind2["RootFile"] ***REMOVED*** 0] ***REMOVED*** "RootFile";
  FileIncludeKind2[FileIncludeKind2["SourceFromProjectReference"] ***REMOVED*** 1] ***REMOVED*** "SourceFromProjectReference";
  FileIncludeKind2[FileIncludeKind2["OutputFromProjectReference"] ***REMOVED*** 2] ***REMOVED*** "OutputFromProjectReference";
  FileIncludeKind2[FileIncludeKind2["Import"] ***REMOVED*** 3] ***REMOVED*** "Import";
  FileIncludeKind2[FileIncludeKind2["ReferenceFile"] ***REMOVED*** 4] ***REMOVED*** "ReferenceFile";
  FileIncludeKind2[FileIncludeKind2["TypeReferenceDirective"] ***REMOVED*** 5] ***REMOVED*** "TypeReferenceDirective";
  FileIncludeKind2[FileIncludeKind2["LibFile"] ***REMOVED*** 6] ***REMOVED*** "LibFile";
  FileIncludeKind2[FileIncludeKind2["LibReferenceDirective"] ***REMOVED*** 7] ***REMOVED*** "LibReferenceDirective";
  FileIncludeKind2[FileIncludeKind2["AutomaticTypeDirectiveFile"] ***REMOVED*** 8] ***REMOVED*** "AutomaticTypeDirectiveFile";
  return FileIncludeKind2;
})(FileIncludeKind || {});
var SymbolFlags ***REMOVED*** /* @__PURE__ */ ((SymbolFlags2) ***REMOVED***> {
  SymbolFlags2[SymbolFlags2["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  SymbolFlags2[SymbolFlags2["FunctionScopedVariable"] ***REMOVED*** 1] ***REMOVED*** "FunctionScopedVariable";
  SymbolFlags2[SymbolFlags2["BlockScopedVariable"] ***REMOVED*** 2] ***REMOVED*** "BlockScopedVariable";
  SymbolFlags2[SymbolFlags2["Property"] ***REMOVED*** 4] ***REMOVED*** "Property";
  SymbolFlags2[SymbolFlags2["EnumMember"] ***REMOVED*** 8] ***REMOVED*** "EnumMember";
  SymbolFlags2[SymbolFlags2["Function"] ***REMOVED*** 16] ***REMOVED*** "Function";
  SymbolFlags2[SymbolFlags2["Class"] ***REMOVED*** 32] ***REMOVED*** "Class";
  SymbolFlags2[SymbolFlags2["Interface"] ***REMOVED*** 64] ***REMOVED*** "Interface";
  SymbolFlags2[SymbolFlags2["ConstEnum"] ***REMOVED*** 128] ***REMOVED*** "ConstEnum";
  SymbolFlags2[SymbolFlags2["RegularEnum"] ***REMOVED*** 256] ***REMOVED*** "RegularEnum";
  SymbolFlags2[SymbolFlags2["ValueModule"] ***REMOVED*** 512] ***REMOVED*** "ValueModule";
  SymbolFlags2[SymbolFlags2["NamespaceModule"] ***REMOVED*** 1024] ***REMOVED*** "NamespaceModule";
  SymbolFlags2[SymbolFlags2["TypeLiteral"] ***REMOVED*** 2048] ***REMOVED*** "TypeLiteral";
  SymbolFlags2[SymbolFlags2["ObjectLiteral"] ***REMOVED*** 4096] ***REMOVED*** "ObjectLiteral";
  SymbolFlags2[SymbolFlags2["Method"] ***REMOVED*** 8192] ***REMOVED*** "Method";
  SymbolFlags2[SymbolFlags2["Constructor"] ***REMOVED*** 16384] ***REMOVED*** "Constructor";
  SymbolFlags2[SymbolFlags2["GetAccessor"] ***REMOVED*** 32768] ***REMOVED*** "GetAccessor";
  SymbolFlags2[SymbolFlags2["SetAccessor"] ***REMOVED*** 65536] ***REMOVED*** "SetAccessor";
  SymbolFlags2[SymbolFlags2["Signature"] ***REMOVED*** 131072] ***REMOVED*** "Signature";
  SymbolFlags2[SymbolFlags2["TypeParameter"] ***REMOVED*** 262144] ***REMOVED*** "TypeParameter";
  SymbolFlags2[SymbolFlags2["TypeAlias"] ***REMOVED*** 524288] ***REMOVED*** "TypeAlias";
  SymbolFlags2[SymbolFlags2["ExportValue"] ***REMOVED*** 1048576] ***REMOVED*** "ExportValue";
  SymbolFlags2[SymbolFlags2["Alias"] ***REMOVED*** 2097152] ***REMOVED*** "Alias";
  SymbolFlags2[SymbolFlags2["Prototype"] ***REMOVED*** 4194304] ***REMOVED*** "Prototype";
  SymbolFlags2[SymbolFlags2["ExportStar"] ***REMOVED*** 8388608] ***REMOVED*** "ExportStar";
  SymbolFlags2[SymbolFlags2["Optional"] ***REMOVED*** 16777216] ***REMOVED*** "Optional";
  SymbolFlags2[SymbolFlags2["Transient"] ***REMOVED*** 33554432] ***REMOVED*** "Transient";
  SymbolFlags2[SymbolFlags2["Assignment"] ***REMOVED*** 67108864] ***REMOVED*** "Assignment";
  SymbolFlags2[SymbolFlags2["ModuleExports"] ***REMOVED*** 134217728] ***REMOVED*** "ModuleExports";
  SymbolFlags2[SymbolFlags2["All"] ***REMOVED*** -1] ***REMOVED*** "All";
  SymbolFlags2[SymbolFlags2["Enum"] ***REMOVED*** 384] ***REMOVED*** "Enum";
  SymbolFlags2[SymbolFlags2["Variable"] ***REMOVED*** 3] ***REMOVED*** "Variable";
  SymbolFlags2[SymbolFlags2["Value"] ***REMOVED*** 111551] ***REMOVED*** "Value";
  SymbolFlags2[SymbolFlags2["Type"] ***REMOVED*** 788968] ***REMOVED*** "Type";
  SymbolFlags2[SymbolFlags2["Namespace"] ***REMOVED*** 1920] ***REMOVED*** "Namespace";
  SymbolFlags2[SymbolFlags2["Module"] ***REMOVED*** 1536] ***REMOVED*** "Module";
  SymbolFlags2[SymbolFlags2["Accessor"] ***REMOVED*** 98304] ***REMOVED*** "Accessor";
  SymbolFlags2[SymbolFlags2["FunctionScopedVariableExcludes"] ***REMOVED*** 111550] ***REMOVED*** "FunctionScopedVariableExcludes";
  SymbolFlags2[SymbolFlags2["BlockScopedVariableExcludes"] ***REMOVED*** 111551 /* Value */] ***REMOVED*** "BlockScopedVariableExcludes";
  SymbolFlags2[SymbolFlags2["ParameterExcludes"] ***REMOVED*** 111551 /* Value */] ***REMOVED*** "ParameterExcludes";
  SymbolFlags2[SymbolFlags2["PropertyExcludes"] ***REMOVED*** 0 /* None */] ***REMOVED*** "PropertyExcludes";
  SymbolFlags2[SymbolFlags2["EnumMemberExcludes"] ***REMOVED*** 900095] ***REMOVED*** "EnumMemberExcludes";
  SymbolFlags2[SymbolFlags2["FunctionExcludes"] ***REMOVED*** 110991] ***REMOVED*** "FunctionExcludes";
  SymbolFlags2[SymbolFlags2["ClassExcludes"] ***REMOVED*** 899503] ***REMOVED*** "ClassExcludes";
  SymbolFlags2[SymbolFlags2["InterfaceExcludes"] ***REMOVED*** 788872] ***REMOVED*** "InterfaceExcludes";
  SymbolFlags2[SymbolFlags2["RegularEnumExcludes"] ***REMOVED*** 899327] ***REMOVED*** "RegularEnumExcludes";
  SymbolFlags2[SymbolFlags2["ConstEnumExcludes"] ***REMOVED*** 899967] ***REMOVED*** "ConstEnumExcludes";
  SymbolFlags2[SymbolFlags2["ValueModuleExcludes"] ***REMOVED*** 110735] ***REMOVED*** "ValueModuleExcludes";
  SymbolFlags2[SymbolFlags2["NamespaceModuleExcludes"] ***REMOVED*** 0] ***REMOVED*** "NamespaceModuleExcludes";
  SymbolFlags2[SymbolFlags2["MethodExcludes"] ***REMOVED*** 103359] ***REMOVED*** "MethodExcludes";
  SymbolFlags2[SymbolFlags2["GetAccessorExcludes"] ***REMOVED*** 46015] ***REMOVED*** "GetAccessorExcludes";
  SymbolFlags2[SymbolFlags2["SetAccessorExcludes"] ***REMOVED*** 78783] ***REMOVED*** "SetAccessorExcludes";
  SymbolFlags2[SymbolFlags2["AccessorExcludes"] ***REMOVED*** 13247] ***REMOVED*** "AccessorExcludes";
  SymbolFlags2[SymbolFlags2["TypeParameterExcludes"] ***REMOVED*** 526824] ***REMOVED*** "TypeParameterExcludes";
  SymbolFlags2[SymbolFlags2["TypeAliasExcludes"] ***REMOVED*** 788968 /* Type */] ***REMOVED*** "TypeAliasExcludes";
  SymbolFlags2[SymbolFlags2["AliasExcludes"] ***REMOVED*** 2097152 /* Alias */] ***REMOVED*** "AliasExcludes";
  SymbolFlags2[SymbolFlags2["ModuleMember"] ***REMOVED*** 2623475] ***REMOVED*** "ModuleMember";
  SymbolFlags2[SymbolFlags2["ExportHasLocal"] ***REMOVED*** 944] ***REMOVED*** "ExportHasLocal";
  SymbolFlags2[SymbolFlags2["BlockScoped"] ***REMOVED*** 418] ***REMOVED*** "BlockScoped";
  SymbolFlags2[SymbolFlags2["PropertyOrAccessor"] ***REMOVED*** 98308] ***REMOVED*** "PropertyOrAccessor";
  SymbolFlags2[SymbolFlags2["ClassMember"] ***REMOVED*** 106500] ***REMOVED*** "ClassMember";
  SymbolFlags2[SymbolFlags2["ExportSupportsDefaultModifier"] ***REMOVED*** 112] ***REMOVED*** "ExportSupportsDefaultModifier";
  SymbolFlags2[SymbolFlags2["ExportDoesNotSupportDefaultModifier"] ***REMOVED*** -113] ***REMOVED*** "ExportDoesNotSupportDefaultModifier";
  SymbolFlags2[SymbolFlags2["Classifiable"] ***REMOVED*** 2885600] ***REMOVED*** "Classifiable";
  SymbolFlags2[SymbolFlags2["LateBindingContainer"] ***REMOVED*** 6256] ***REMOVED*** "LateBindingContainer";
  return SymbolFlags2;
})(SymbolFlags || {});
var NodeCheckFlags ***REMOVED*** /* @__PURE__ */ ((NodeCheckFlags3) ***REMOVED***> {
  NodeCheckFlags3[NodeCheckFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  NodeCheckFlags3[NodeCheckFlags3["TypeChecked"] ***REMOVED*** 1] ***REMOVED*** "TypeChecked";
  NodeCheckFlags3[NodeCheckFlags3["LexicalThis"] ***REMOVED*** 2] ***REMOVED*** "LexicalThis";
  NodeCheckFlags3[NodeCheckFlags3["CaptureThis"] ***REMOVED*** 4] ***REMOVED*** "CaptureThis";
  NodeCheckFlags3[NodeCheckFlags3["CaptureNewTarget"] ***REMOVED*** 8] ***REMOVED*** "CaptureNewTarget";
  NodeCheckFlags3[NodeCheckFlags3["SuperInstance"] ***REMOVED*** 16] ***REMOVED*** "SuperInstance";
  NodeCheckFlags3[NodeCheckFlags3["SuperStatic"] ***REMOVED*** 32] ***REMOVED*** "SuperStatic";
  NodeCheckFlags3[NodeCheckFlags3["ContextChecked"] ***REMOVED*** 64] ***REMOVED*** "ContextChecked";
  NodeCheckFlags3[NodeCheckFlags3["MethodWithSuperPropertyAccessInAsync"] ***REMOVED*** 128] ***REMOVED*** "MethodWithSuperPropertyAccessInAsync";
  NodeCheckFlags3[NodeCheckFlags3["MethodWithSuperPropertyAssignmentInAsync"] ***REMOVED*** 256] ***REMOVED*** "MethodWithSuperPropertyAssignmentInAsync";
  NodeCheckFlags3[NodeCheckFlags3["CaptureArguments"] ***REMOVED*** 512] ***REMOVED*** "CaptureArguments";
  NodeCheckFlags3[NodeCheckFlags3["EnumValuesComputed"] ***REMOVED*** 1024] ***REMOVED*** "EnumValuesComputed";
  NodeCheckFlags3[NodeCheckFlags3["LexicalModuleMergesWithClass"] ***REMOVED*** 2048] ***REMOVED*** "LexicalModuleMergesWithClass";
  NodeCheckFlags3[NodeCheckFlags3["LoopWithCapturedBlockScopedBinding"] ***REMOVED*** 4096] ***REMOVED*** "LoopWithCapturedBlockScopedBinding";
  NodeCheckFlags3[NodeCheckFlags3["ContainsCapturedBlockScopeBinding"] ***REMOVED*** 8192] ***REMOVED*** "ContainsCapturedBlockScopeBinding";
  NodeCheckFlags3[NodeCheckFlags3["CapturedBlockScopedBinding"] ***REMOVED*** 16384] ***REMOVED*** "CapturedBlockScopedBinding";
  NodeCheckFlags3[NodeCheckFlags3["BlockScopedBindingInLoop"] ***REMOVED*** 32768] ***REMOVED*** "BlockScopedBindingInLoop";
  NodeCheckFlags3[NodeCheckFlags3["NeedsLoopOutParameter"] ***REMOVED*** 65536] ***REMOVED*** "NeedsLoopOutParameter";
  NodeCheckFlags3[NodeCheckFlags3["AssignmentsMarked"] ***REMOVED*** 131072] ***REMOVED*** "AssignmentsMarked";
  NodeCheckFlags3[NodeCheckFlags3["ContainsConstructorReference"] ***REMOVED*** 262144] ***REMOVED*** "ContainsConstructorReference";
  NodeCheckFlags3[NodeCheckFlags3["ConstructorReference"] ***REMOVED*** 536870912] ***REMOVED*** "ConstructorReference";
  NodeCheckFlags3[NodeCheckFlags3["ContainsClassWithPrivateIdentifiers"] ***REMOVED*** 1048576] ***REMOVED*** "ContainsClassWithPrivateIdentifiers";
  NodeCheckFlags3[NodeCheckFlags3["ContainsSuperPropertyInStaticInitializer"] ***REMOVED*** 2097152] ***REMOVED*** "ContainsSuperPropertyInStaticInitializer";
  NodeCheckFlags3[NodeCheckFlags3["InCheckIdentifier"] ***REMOVED*** 4194304] ***REMOVED*** "InCheckIdentifier";
  NodeCheckFlags3[NodeCheckFlags3["PartiallyTypeChecked"] ***REMOVED*** 8388608] ***REMOVED*** "PartiallyTypeChecked";
  NodeCheckFlags3[NodeCheckFlags3["LazyFlags"] ***REMOVED*** 539358128] ***REMOVED*** "LazyFlags";
  return NodeCheckFlags3;
})(NodeCheckFlags || {});
var TypeFlags ***REMOVED*** /* @__PURE__ */ ((TypeFlags2) ***REMOVED***> {
  TypeFlags2[TypeFlags2["Any"] ***REMOVED*** 1] ***REMOVED*** "Any";
  TypeFlags2[TypeFlags2["Unknown"] ***REMOVED*** 2] ***REMOVED*** "Unknown";
  TypeFlags2[TypeFlags2["String"] ***REMOVED*** 4] ***REMOVED*** "String";
  TypeFlags2[TypeFlags2["Number"] ***REMOVED*** 8] ***REMOVED*** "Number";
  TypeFlags2[TypeFlags2["Boolean"] ***REMOVED*** 16] ***REMOVED*** "Boolean";
  TypeFlags2[TypeFlags2["Enum"] ***REMOVED*** 32] ***REMOVED*** "Enum";
  TypeFlags2[TypeFlags2["BigInt"] ***REMOVED*** 64] ***REMOVED*** "BigInt";
  TypeFlags2[TypeFlags2["StringLiteral"] ***REMOVED*** 128] ***REMOVED*** "StringLiteral";
  TypeFlags2[TypeFlags2["NumberLiteral"] ***REMOVED*** 256] ***REMOVED*** "NumberLiteral";
  TypeFlags2[TypeFlags2["BooleanLiteral"] ***REMOVED*** 512] ***REMOVED*** "BooleanLiteral";
  TypeFlags2[TypeFlags2["EnumLiteral"] ***REMOVED*** 1024] ***REMOVED*** "EnumLiteral";
  TypeFlags2[TypeFlags2["BigIntLiteral"] ***REMOVED*** 2048] ***REMOVED*** "BigIntLiteral";
  TypeFlags2[TypeFlags2["ESSymbol"] ***REMOVED*** 4096] ***REMOVED*** "ESSymbol";
  TypeFlags2[TypeFlags2["UniqueESSymbol"] ***REMOVED*** 8192] ***REMOVED*** "UniqueESSymbol";
  TypeFlags2[TypeFlags2["Void"] ***REMOVED*** 16384] ***REMOVED*** "Void";
  TypeFlags2[TypeFlags2["Undefined"] ***REMOVED*** 32768] ***REMOVED*** "Undefined";
  TypeFlags2[TypeFlags2["Null"] ***REMOVED*** 65536] ***REMOVED*** "Null";
  TypeFlags2[TypeFlags2["Never"] ***REMOVED*** 131072] ***REMOVED*** "Never";
  TypeFlags2[TypeFlags2["TypeParameter"] ***REMOVED*** 262144] ***REMOVED*** "TypeParameter";
  TypeFlags2[TypeFlags2["Object"] ***REMOVED*** 524288] ***REMOVED*** "Object";
  TypeFlags2[TypeFlags2["Union"] ***REMOVED*** 1048576] ***REMOVED*** "Union";
  TypeFlags2[TypeFlags2["Intersection"] ***REMOVED*** 2097152] ***REMOVED*** "Intersection";
  TypeFlags2[TypeFlags2["Index"] ***REMOVED*** 4194304] ***REMOVED*** "Index";
  TypeFlags2[TypeFlags2["IndexedAccess"] ***REMOVED*** 8388608] ***REMOVED*** "IndexedAccess";
  TypeFlags2[TypeFlags2["Conditional"] ***REMOVED*** 16777216] ***REMOVED*** "Conditional";
  TypeFlags2[TypeFlags2["Substitution"] ***REMOVED*** 33554432] ***REMOVED*** "Substitution";
  TypeFlags2[TypeFlags2["NonPrimitive"] ***REMOVED*** 67108864] ***REMOVED*** "NonPrimitive";
  TypeFlags2[TypeFlags2["TemplateLiteral"] ***REMOVED*** 134217728] ***REMOVED*** "TemplateLiteral";
  TypeFlags2[TypeFlags2["StringMapping"] ***REMOVED*** 268435456] ***REMOVED*** "StringMapping";
  TypeFlags2[TypeFlags2["Reserved1"] ***REMOVED*** 536870912] ***REMOVED*** "Reserved1";
  TypeFlags2[TypeFlags2["Reserved2"] ***REMOVED*** 1073741824] ***REMOVED*** "Reserved2";
  TypeFlags2[TypeFlags2["AnyOrUnknown"] ***REMOVED*** 3] ***REMOVED*** "AnyOrUnknown";
  TypeFlags2[TypeFlags2["Nullable"] ***REMOVED*** 98304] ***REMOVED*** "Nullable";
  TypeFlags2[TypeFlags2["Literal"] ***REMOVED*** 2944] ***REMOVED*** "Literal";
  TypeFlags2[TypeFlags2["Unit"] ***REMOVED*** 109472] ***REMOVED*** "Unit";
  TypeFlags2[TypeFlags2["Freshable"] ***REMOVED*** 2976] ***REMOVED*** "Freshable";
  TypeFlags2[TypeFlags2["StringOrNumberLiteral"] ***REMOVED*** 384] ***REMOVED*** "StringOrNumberLiteral";
  TypeFlags2[TypeFlags2["StringOrNumberLiteralOrUnique"] ***REMOVED*** 8576] ***REMOVED*** "StringOrNumberLiteralOrUnique";
  TypeFlags2[TypeFlags2["DefinitelyFalsy"] ***REMOVED*** 117632] ***REMOVED*** "DefinitelyFalsy";
  TypeFlags2[TypeFlags2["PossiblyFalsy"] ***REMOVED*** 117724] ***REMOVED*** "PossiblyFalsy";
  TypeFlags2[TypeFlags2["Intrinsic"] ***REMOVED*** 67359327] ***REMOVED*** "Intrinsic";
  TypeFlags2[TypeFlags2["StringLike"] ***REMOVED*** 402653316] ***REMOVED*** "StringLike";
  TypeFlags2[TypeFlags2["NumberLike"] ***REMOVED*** 296] ***REMOVED*** "NumberLike";
  TypeFlags2[TypeFlags2["BigIntLike"] ***REMOVED*** 2112] ***REMOVED*** "BigIntLike";
  TypeFlags2[TypeFlags2["BooleanLike"] ***REMOVED*** 528] ***REMOVED*** "BooleanLike";
  TypeFlags2[TypeFlags2["EnumLike"] ***REMOVED*** 1056] ***REMOVED*** "EnumLike";
  TypeFlags2[TypeFlags2["ESSymbolLike"] ***REMOVED*** 12288] ***REMOVED*** "ESSymbolLike";
  TypeFlags2[TypeFlags2["VoidLike"] ***REMOVED*** 49152] ***REMOVED*** "VoidLike";
  TypeFlags2[TypeFlags2["Primitive"] ***REMOVED*** 402784252] ***REMOVED*** "Primitive";
  TypeFlags2[TypeFlags2["DefinitelyNonNullable"] ***REMOVED*** 470302716] ***REMOVED*** "DefinitelyNonNullable";
  TypeFlags2[TypeFlags2["DisjointDomains"] ***REMOVED*** 469892092] ***REMOVED*** "DisjointDomains";
  TypeFlags2[TypeFlags2["UnionOrIntersection"] ***REMOVED*** 3145728] ***REMOVED*** "UnionOrIntersection";
  TypeFlags2[TypeFlags2["StructuredType"] ***REMOVED*** 3670016] ***REMOVED*** "StructuredType";
  TypeFlags2[TypeFlags2["TypeVariable"] ***REMOVED*** 8650752] ***REMOVED*** "TypeVariable";
  TypeFlags2[TypeFlags2["InstantiableNonPrimitive"] ***REMOVED*** 58982400] ***REMOVED*** "InstantiableNonPrimitive";
  TypeFlags2[TypeFlags2["InstantiablePrimitive"] ***REMOVED*** 406847488] ***REMOVED*** "InstantiablePrimitive";
  TypeFlags2[TypeFlags2["Instantiable"] ***REMOVED*** 465829888] ***REMOVED*** "Instantiable";
  TypeFlags2[TypeFlags2["StructuredOrInstantiable"] ***REMOVED*** 469499904] ***REMOVED*** "StructuredOrInstantiable";
  TypeFlags2[TypeFlags2["ObjectFlagsType"] ***REMOVED*** 3899393] ***REMOVED*** "ObjectFlagsType";
  TypeFlags2[TypeFlags2["Simplifiable"] ***REMOVED*** 25165824] ***REMOVED*** "Simplifiable";
  TypeFlags2[TypeFlags2["Singleton"] ***REMOVED*** 67358815] ***REMOVED*** "Singleton";
  TypeFlags2[TypeFlags2["Narrowable"] ***REMOVED*** 536624127] ***REMOVED*** "Narrowable";
  TypeFlags2[TypeFlags2["IncludesMask"] ***REMOVED*** 473694207] ***REMOVED*** "IncludesMask";
  TypeFlags2[TypeFlags2["IncludesMissingType"] ***REMOVED*** 262144 /* TypeParameter */] ***REMOVED*** "IncludesMissingType";
  TypeFlags2[TypeFlags2["IncludesNonWideningType"] ***REMOVED*** 4194304 /* Index */] ***REMOVED*** "IncludesNonWideningType";
  TypeFlags2[TypeFlags2["IncludesWildcard"] ***REMOVED*** 8388608 /* IndexedAccess */] ***REMOVED*** "IncludesWildcard";
  TypeFlags2[TypeFlags2["IncludesEmptyObject"] ***REMOVED*** 16777216 /* Conditional */] ***REMOVED*** "IncludesEmptyObject";
  TypeFlags2[TypeFlags2["IncludesInstantiable"] ***REMOVED*** 33554432 /* Substitution */] ***REMOVED*** "IncludesInstantiable";
  TypeFlags2[TypeFlags2["IncludesConstrainedTypeVariable"] ***REMOVED*** 536870912 /* Reserved1 */] ***REMOVED*** "IncludesConstrainedTypeVariable";
  TypeFlags2[TypeFlags2["IncludesError"] ***REMOVED*** 1073741824 /* Reserved2 */] ***REMOVED*** "IncludesError";
  TypeFlags2[TypeFlags2["NotPrimitiveUnion"] ***REMOVED*** 36323331] ***REMOVED*** "NotPrimitiveUnion";
  return TypeFlags2;
})(TypeFlags || {});
var ObjectFlags ***REMOVED*** /* @__PURE__ */ ((ObjectFlags3) ***REMOVED***> {
  ObjectFlags3[ObjectFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  ObjectFlags3[ObjectFlags3["Class"] ***REMOVED*** 1] ***REMOVED*** "Class";
  ObjectFlags3[ObjectFlags3["Interface"] ***REMOVED*** 2] ***REMOVED*** "Interface";
  ObjectFlags3[ObjectFlags3["Reference"] ***REMOVED*** 4] ***REMOVED*** "Reference";
  ObjectFlags3[ObjectFlags3["Tuple"] ***REMOVED*** 8] ***REMOVED*** "Tuple";
  ObjectFlags3[ObjectFlags3["Anonymous"] ***REMOVED*** 16] ***REMOVED*** "Anonymous";
  ObjectFlags3[ObjectFlags3["Mapped"] ***REMOVED*** 32] ***REMOVED*** "Mapped";
  ObjectFlags3[ObjectFlags3["Instantiated"] ***REMOVED*** 64] ***REMOVED*** "Instantiated";
  ObjectFlags3[ObjectFlags3["ObjectLiteral"] ***REMOVED*** 128] ***REMOVED*** "ObjectLiteral";
  ObjectFlags3[ObjectFlags3["EvolvingArray"] ***REMOVED*** 256] ***REMOVED*** "EvolvingArray";
  ObjectFlags3[ObjectFlags3["ObjectLiteralPatternWithComputedProperties"] ***REMOVED*** 512] ***REMOVED*** "ObjectLiteralPatternWithComputedProperties";
  ObjectFlags3[ObjectFlags3["ReverseMapped"] ***REMOVED*** 1024] ***REMOVED*** "ReverseMapped";
  ObjectFlags3[ObjectFlags3["JsxAttributes"] ***REMOVED*** 2048] ***REMOVED*** "JsxAttributes";
  ObjectFlags3[ObjectFlags3["JSLiteral"] ***REMOVED*** 4096] ***REMOVED*** "JSLiteral";
  ObjectFlags3[ObjectFlags3["FreshLiteral"] ***REMOVED*** 8192] ***REMOVED*** "FreshLiteral";
  ObjectFlags3[ObjectFlags3["ArrayLiteral"] ***REMOVED*** 16384] ***REMOVED*** "ArrayLiteral";
  ObjectFlags3[ObjectFlags3["PrimitiveUnion"] ***REMOVED*** 32768] ***REMOVED*** "PrimitiveUnion";
  ObjectFlags3[ObjectFlags3["ContainsWideningType"] ***REMOVED*** 65536] ***REMOVED*** "ContainsWideningType";
  ObjectFlags3[ObjectFlags3["ContainsObjectOrArrayLiteral"] ***REMOVED*** 131072] ***REMOVED*** "ContainsObjectOrArrayLiteral";
  ObjectFlags3[ObjectFlags3["NonInferrableType"] ***REMOVED*** 262144] ***REMOVED*** "NonInferrableType";
  ObjectFlags3[ObjectFlags3["CouldContainTypeVariablesComputed"] ***REMOVED*** 524288] ***REMOVED*** "CouldContainTypeVariablesComputed";
  ObjectFlags3[ObjectFlags3["CouldContainTypeVariables"] ***REMOVED*** 1048576] ***REMOVED*** "CouldContainTypeVariables";
  ObjectFlags3[ObjectFlags3["SingleSignatureType"] ***REMOVED*** 134217728] ***REMOVED*** "SingleSignatureType";
  ObjectFlags3[ObjectFlags3["ClassOrInterface"] ***REMOVED*** 3] ***REMOVED*** "ClassOrInterface";
  ObjectFlags3[ObjectFlags3["RequiresWidening"] ***REMOVED*** 196608] ***REMOVED*** "RequiresWidening";
  ObjectFlags3[ObjectFlags3["PropagatingFlags"] ***REMOVED*** 458752] ***REMOVED*** "PropagatingFlags";
  ObjectFlags3[ObjectFlags3["InstantiatedMapped"] ***REMOVED*** 96] ***REMOVED*** "InstantiatedMapped";
  ObjectFlags3[ObjectFlags3["ObjectTypeKindMask"] ***REMOVED*** 1343] ***REMOVED*** "ObjectTypeKindMask";
  ObjectFlags3[ObjectFlags3["ContainsSpread"] ***REMOVED*** 2097152] ***REMOVED*** "ContainsSpread";
  ObjectFlags3[ObjectFlags3["ObjectRestType"] ***REMOVED*** 4194304] ***REMOVED*** "ObjectRestType";
  ObjectFlags3[ObjectFlags3["InstantiationExpressionType"] ***REMOVED*** 8388608] ***REMOVED*** "InstantiationExpressionType";
  ObjectFlags3[ObjectFlags3["IsClassInstanceClone"] ***REMOVED*** 16777216] ***REMOVED*** "IsClassInstanceClone";
  ObjectFlags3[ObjectFlags3["IdenticalBaseTypeCalculated"] ***REMOVED*** 33554432] ***REMOVED*** "IdenticalBaseTypeCalculated";
  ObjectFlags3[ObjectFlags3["IdenticalBaseTypeExists"] ***REMOVED*** 67108864] ***REMOVED*** "IdenticalBaseTypeExists";
  ObjectFlags3[ObjectFlags3["IsGenericTypeComputed"] ***REMOVED*** 2097152] ***REMOVED*** "IsGenericTypeComputed";
  ObjectFlags3[ObjectFlags3["IsGenericObjectType"] ***REMOVED*** 4194304] ***REMOVED*** "IsGenericObjectType";
  ObjectFlags3[ObjectFlags3["IsGenericIndexType"] ***REMOVED*** 8388608] ***REMOVED*** "IsGenericIndexType";
  ObjectFlags3[ObjectFlags3["IsGenericType"] ***REMOVED*** 12582912] ***REMOVED*** "IsGenericType";
  ObjectFlags3[ObjectFlags3["ContainsIntersections"] ***REMOVED*** 16777216] ***REMOVED*** "ContainsIntersections";
  ObjectFlags3[ObjectFlags3["IsUnknownLikeUnionComputed"] ***REMOVED*** 33554432] ***REMOVED*** "IsUnknownLikeUnionComputed";
  ObjectFlags3[ObjectFlags3["IsUnknownLikeUnion"] ***REMOVED*** 67108864] ***REMOVED*** "IsUnknownLikeUnion";
  ObjectFlags3[ObjectFlags3["IsNeverIntersectionComputed"] ***REMOVED*** 16777216] ***REMOVED*** "IsNeverIntersectionComputed";
  ObjectFlags3[ObjectFlags3["IsNeverIntersection"] ***REMOVED*** 33554432] ***REMOVED*** "IsNeverIntersection";
  ObjectFlags3[ObjectFlags3["IsConstrainedTypeVariable"] ***REMOVED*** 67108864] ***REMOVED*** "IsConstrainedTypeVariable";
  return ObjectFlags3;
})(ObjectFlags || {});
var SignatureFlags ***REMOVED*** /* @__PURE__ */ ((SignatureFlags4) ***REMOVED***> {
  SignatureFlags4[SignatureFlags4["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  SignatureFlags4[SignatureFlags4["HasRestParameter"] ***REMOVED*** 1] ***REMOVED*** "HasRestParameter";
  SignatureFlags4[SignatureFlags4["HasLiteralTypes"] ***REMOVED*** 2] ***REMOVED*** "HasLiteralTypes";
  SignatureFlags4[SignatureFlags4["Abstract"] ***REMOVED*** 4] ***REMOVED*** "Abstract";
  SignatureFlags4[SignatureFlags4["IsInnerCallChain"] ***REMOVED*** 8] ***REMOVED*** "IsInnerCallChain";
  SignatureFlags4[SignatureFlags4["IsOuterCallChain"] ***REMOVED*** 16] ***REMOVED*** "IsOuterCallChain";
  SignatureFlags4[SignatureFlags4["IsUntypedSignatureInJSFile"] ***REMOVED*** 32] ***REMOVED*** "IsUntypedSignatureInJSFile";
  SignatureFlags4[SignatureFlags4["IsNonInferrable"] ***REMOVED*** 64] ***REMOVED*** "IsNonInferrable";
  SignatureFlags4[SignatureFlags4["IsSignatureCandidateForOverloadFailure"] ***REMOVED*** 128] ***REMOVED*** "IsSignatureCandidateForOverloadFailure";
  SignatureFlags4[SignatureFlags4["PropagatingFlags"] ***REMOVED*** 167] ***REMOVED*** "PropagatingFlags";
  SignatureFlags4[SignatureFlags4["CallChainFlags"] ***REMOVED*** 24] ***REMOVED*** "CallChainFlags";
  return SignatureFlags4;
})(SignatureFlags || {});
var DiagnosticCategory ***REMOVED*** /* @__PURE__ */ ((DiagnosticCategory2) ***REMOVED***> {
  DiagnosticCategory2[DiagnosticCategory2["Warning"] ***REMOVED*** 0] ***REMOVED*** "Warning";
  DiagnosticCategory2[DiagnosticCategory2["Error"] ***REMOVED*** 1] ***REMOVED*** "Error";
  DiagnosticCategory2[DiagnosticCategory2["Suggestion"] ***REMOVED*** 2] ***REMOVED*** "Suggestion";
  DiagnosticCategory2[DiagnosticCategory2["Message"] ***REMOVED*** 3] ***REMOVED*** "Message";
  return DiagnosticCategory2;
})(DiagnosticCategory || {});
function diagnosticCategoryName(d, lowerCase ***REMOVED*** true) {
  const name ***REMOVED*** DiagnosticCategory[d.category];
  return lowerCase ? name.toLowerCase() : name;
}
var ModuleResolutionKind ***REMOVED*** /* @__PURE__ */ ((ModuleResolutionKind2) ***REMOVED***> {
  ModuleResolutionKind2[ModuleResolutionKind2["Classic"] ***REMOVED*** 1] ***REMOVED*** "Classic";
  ModuleResolutionKind2[ModuleResolutionKind2["NodeJs"] ***REMOVED*** 2] ***REMOVED*** "NodeJs";
  ModuleResolutionKind2[ModuleResolutionKind2["Node10"] ***REMOVED*** 2] ***REMOVED*** "Node10";
  ModuleResolutionKind2[ModuleResolutionKind2["Node16"] ***REMOVED*** 3] ***REMOVED*** "Node16";
  ModuleResolutionKind2[ModuleResolutionKind2["NodeNext"] ***REMOVED*** 99] ***REMOVED*** "NodeNext";
  ModuleResolutionKind2[ModuleResolutionKind2["Bundler"] ***REMOVED*** 100] ***REMOVED*** "Bundler";
  return ModuleResolutionKind2;
})(ModuleResolutionKind || {});
var ModuleKind ***REMOVED*** /* @__PURE__ */ ((ModuleKind2) ***REMOVED***> {
  ModuleKind2[ModuleKind2["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  ModuleKind2[ModuleKind2["CommonJS"] ***REMOVED*** 1] ***REMOVED*** "CommonJS";
  ModuleKind2[ModuleKind2["AMD"] ***REMOVED*** 2] ***REMOVED*** "AMD";
  ModuleKind2[ModuleKind2["UMD"] ***REMOVED*** 3] ***REMOVED*** "UMD";
  ModuleKind2[ModuleKind2["System"] ***REMOVED*** 4] ***REMOVED*** "System";
  ModuleKind2[ModuleKind2["ES2015"] ***REMOVED*** 5] ***REMOVED*** "ES2015";
  ModuleKind2[ModuleKind2["ES2020"] ***REMOVED*** 6] ***REMOVED*** "ES2020";
  ModuleKind2[ModuleKind2["ES2022"] ***REMOVED*** 7] ***REMOVED*** "ES2022";
  ModuleKind2[ModuleKind2["ESNext"] ***REMOVED*** 99] ***REMOVED*** "ESNext";
  ModuleKind2[ModuleKind2["Node16"] ***REMOVED*** 100] ***REMOVED*** "Node16";
  ModuleKind2[ModuleKind2["Node18"] ***REMOVED*** 101] ***REMOVED*** "Node18";
  ModuleKind2[ModuleKind2["Node20"] ***REMOVED*** 102] ***REMOVED*** "Node20";
  ModuleKind2[ModuleKind2["NodeNext"] ***REMOVED*** 199] ***REMOVED*** "NodeNext";
  ModuleKind2[ModuleKind2["Preserve"] ***REMOVED*** 200] ***REMOVED*** "Preserve";
  return ModuleKind2;
})(ModuleKind || {});
var ScriptKind ***REMOVED*** /* @__PURE__ */ ((ScriptKind3) ***REMOVED***> {
  ScriptKind3[ScriptKind3["Unknown"] ***REMOVED*** 0] ***REMOVED*** "Unknown";
  ScriptKind3[ScriptKind3["JS"] ***REMOVED*** 1] ***REMOVED*** "JS";
  ScriptKind3[ScriptKind3["JSX"] ***REMOVED*** 2] ***REMOVED*** "JSX";
  ScriptKind3[ScriptKind3["TS"] ***REMOVED*** 3] ***REMOVED*** "TS";
  ScriptKind3[ScriptKind3["TSX"] ***REMOVED*** 4] ***REMOVED*** "TSX";
  ScriptKind3[ScriptKind3["External"] ***REMOVED*** 5] ***REMOVED*** "External";
  ScriptKind3[ScriptKind3["JSON"] ***REMOVED*** 6] ***REMOVED*** "JSON";
  ScriptKind3[ScriptKind3["Deferred"] ***REMOVED*** 7] ***REMOVED*** "Deferred";
  return ScriptKind3;
})(ScriptKind || {});
var TransformFlags ***REMOVED*** /* @__PURE__ */ ((TransformFlags3) ***REMOVED***> {
  TransformFlags3[TransformFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  TransformFlags3[TransformFlags3["ContainsTypeScript"] ***REMOVED*** 1] ***REMOVED*** "ContainsTypeScript";
  TransformFlags3[TransformFlags3["ContainsJsx"] ***REMOVED*** 2] ***REMOVED*** "ContainsJsx";
  TransformFlags3[TransformFlags3["ContainsESNext"] ***REMOVED*** 4] ***REMOVED*** "ContainsESNext";
  TransformFlags3[TransformFlags3["ContainsES2022"] ***REMOVED*** 8] ***REMOVED*** "ContainsES2022";
  TransformFlags3[TransformFlags3["ContainsES2021"] ***REMOVED*** 16] ***REMOVED*** "ContainsES2021";
  TransformFlags3[TransformFlags3["ContainsES2020"] ***REMOVED*** 32] ***REMOVED*** "ContainsES2020";
  TransformFlags3[TransformFlags3["ContainsES2019"] ***REMOVED*** 64] ***REMOVED*** "ContainsES2019";
  TransformFlags3[TransformFlags3["ContainsES2018"] ***REMOVED*** 128] ***REMOVED*** "ContainsES2018";
  TransformFlags3[TransformFlags3["ContainsES2017"] ***REMOVED*** 256] ***REMOVED*** "ContainsES2017";
  TransformFlags3[TransformFlags3["ContainsES2016"] ***REMOVED*** 512] ***REMOVED*** "ContainsES2016";
  TransformFlags3[TransformFlags3["ContainsES2015"] ***REMOVED*** 1024] ***REMOVED*** "ContainsES2015";
  TransformFlags3[TransformFlags3["ContainsGenerator"] ***REMOVED*** 2048] ***REMOVED*** "ContainsGenerator";
  TransformFlags3[TransformFlags3["ContainsDestructuringAssignment"] ***REMOVED*** 4096] ***REMOVED*** "ContainsDestructuringAssignment";
  TransformFlags3[TransformFlags3["ContainsTypeScriptClassSyntax"] ***REMOVED*** 8192] ***REMOVED*** "ContainsTypeScriptClassSyntax";
  TransformFlags3[TransformFlags3["ContainsLexicalThis"] ***REMOVED*** 16384] ***REMOVED*** "ContainsLexicalThis";
  TransformFlags3[TransformFlags3["ContainsRestOrSpread"] ***REMOVED*** 32768] ***REMOVED*** "ContainsRestOrSpread";
  TransformFlags3[TransformFlags3["ContainsObjectRestOrSpread"] ***REMOVED*** 65536] ***REMOVED*** "ContainsObjectRestOrSpread";
  TransformFlags3[TransformFlags3["ContainsComputedPropertyName"] ***REMOVED*** 131072] ***REMOVED*** "ContainsComputedPropertyName";
  TransformFlags3[TransformFlags3["ContainsBlockScopedBinding"] ***REMOVED*** 262144] ***REMOVED*** "ContainsBlockScopedBinding";
  TransformFlags3[TransformFlags3["ContainsBindingPattern"] ***REMOVED*** 524288] ***REMOVED*** "ContainsBindingPattern";
  TransformFlags3[TransformFlags3["ContainsYield"] ***REMOVED*** 1048576] ***REMOVED*** "ContainsYield";
  TransformFlags3[TransformFlags3["ContainsAwait"] ***REMOVED*** 2097152] ***REMOVED*** "ContainsAwait";
  TransformFlags3[TransformFlags3["ContainsHoistedDeclarationOrCompletion"] ***REMOVED*** 4194304] ***REMOVED*** "ContainsHoistedDeclarationOrCompletion";
  TransformFlags3[TransformFlags3["ContainsDynamicImport"] ***REMOVED*** 8388608] ***REMOVED*** "ContainsDynamicImport";
  TransformFlags3[TransformFlags3["ContainsClassFields"] ***REMOVED*** 16777216] ***REMOVED*** "ContainsClassFields";
  TransformFlags3[TransformFlags3["ContainsDecorators"] ***REMOVED*** 33554432] ***REMOVED*** "ContainsDecorators";
  TransformFlags3[TransformFlags3["ContainsPossibleTopLevelAwait"] ***REMOVED*** 67108864] ***REMOVED*** "ContainsPossibleTopLevelAwait";
  TransformFlags3[TransformFlags3["ContainsLexicalSuper"] ***REMOVED*** 134217728] ***REMOVED*** "ContainsLexicalSuper";
  TransformFlags3[TransformFlags3["ContainsUpdateExpressionForIdentifier"] ***REMOVED*** 268435456] ***REMOVED*** "ContainsUpdateExpressionForIdentifier";
  TransformFlags3[TransformFlags3["ContainsPrivateIdentifierInExpression"] ***REMOVED*** 536870912] ***REMOVED*** "ContainsPrivateIdentifierInExpression";
  TransformFlags3[TransformFlags3["HasComputedFlags"] ***REMOVED*** -2147483648] ***REMOVED*** "HasComputedFlags";
  TransformFlags3[TransformFlags3["AssertTypeScript"] ***REMOVED*** 1 /* ContainsTypeScript */] ***REMOVED*** "AssertTypeScript";
  TransformFlags3[TransformFlags3["AssertJsx"] ***REMOVED*** 2 /* ContainsJsx */] ***REMOVED*** "AssertJsx";
  TransformFlags3[TransformFlags3["AssertESNext"] ***REMOVED*** 4 /* ContainsESNext */] ***REMOVED*** "AssertESNext";
  TransformFlags3[TransformFlags3["AssertES2022"] ***REMOVED*** 8 /* ContainsES2022 */] ***REMOVED*** "AssertES2022";
  TransformFlags3[TransformFlags3["AssertES2021"] ***REMOVED*** 16 /* ContainsES2021 */] ***REMOVED*** "AssertES2021";
  TransformFlags3[TransformFlags3["AssertES2020"] ***REMOVED*** 32 /* ContainsES2020 */] ***REMOVED*** "AssertES2020";
  TransformFlags3[TransformFlags3["AssertES2019"] ***REMOVED*** 64 /* ContainsES2019 */] ***REMOVED*** "AssertES2019";
  TransformFlags3[TransformFlags3["AssertES2018"] ***REMOVED*** 128 /* ContainsES2018 */] ***REMOVED*** "AssertES2018";
  TransformFlags3[TransformFlags3["AssertES2017"] ***REMOVED*** 256 /* ContainsES2017 */] ***REMOVED*** "AssertES2017";
  TransformFlags3[TransformFlags3["AssertES2016"] ***REMOVED*** 512 /* ContainsES2016 */] ***REMOVED*** "AssertES2016";
  TransformFlags3[TransformFlags3["AssertES2015"] ***REMOVED*** 1024 /* ContainsES2015 */] ***REMOVED*** "AssertES2015";
  TransformFlags3[TransformFlags3["AssertGenerator"] ***REMOVED*** 2048 /* ContainsGenerator */] ***REMOVED*** "AssertGenerator";
  TransformFlags3[TransformFlags3["AssertDestructuringAssignment"] ***REMOVED*** 4096 /* ContainsDestructuringAssignment */] ***REMOVED*** "AssertDestructuringAssignment";
  TransformFlags3[TransformFlags3["OuterExpressionExcludes"] ***REMOVED*** -2147483648 /* HasComputedFlags */] ***REMOVED*** "OuterExpressionExcludes";
  TransformFlags3[TransformFlags3["PropertyAccessExcludes"] ***REMOVED*** -2147483648 /* OuterExpressionExcludes */] ***REMOVED*** "PropertyAccessExcludes";
  TransformFlags3[TransformFlags3["NodeExcludes"] ***REMOVED*** -2147483648 /* PropertyAccessExcludes */] ***REMOVED*** "NodeExcludes";
  TransformFlags3[TransformFlags3["ArrowFunctionExcludes"] ***REMOVED*** -2072174592] ***REMOVED*** "ArrowFunctionExcludes";
  TransformFlags3[TransformFlags3["FunctionExcludes"] ***REMOVED*** -1937940480] ***REMOVED*** "FunctionExcludes";
  TransformFlags3[TransformFlags3["ConstructorExcludes"] ***REMOVED*** -1937948672] ***REMOVED*** "ConstructorExcludes";
  TransformFlags3[TransformFlags3["MethodOrAccessorExcludes"] ***REMOVED*** -2005057536] ***REMOVED*** "MethodOrAccessorExcludes";
  TransformFlags3[TransformFlags3["PropertyExcludes"] ***REMOVED*** -2013249536] ***REMOVED*** "PropertyExcludes";
  TransformFlags3[TransformFlags3["ClassExcludes"] ***REMOVED*** -2147344384] ***REMOVED*** "ClassExcludes";
  TransformFlags3[TransformFlags3["ModuleExcludes"] ***REMOVED*** -1941676032] ***REMOVED*** "ModuleExcludes";
  TransformFlags3[TransformFlags3["TypeExcludes"] ***REMOVED*** -2] ***REMOVED*** "TypeExcludes";
  TransformFlags3[TransformFlags3["ObjectLiteralExcludes"] ***REMOVED*** -2147278848] ***REMOVED*** "ObjectLiteralExcludes";
  TransformFlags3[TransformFlags3["ArrayLiteralOrCallOrNewExcludes"] ***REMOVED*** -2147450880] ***REMOVED*** "ArrayLiteralOrCallOrNewExcludes";
  TransformFlags3[TransformFlags3["VariableDeclarationListExcludes"] ***REMOVED*** -2146893824] ***REMOVED*** "VariableDeclarationListExcludes";
  TransformFlags3[TransformFlags3["ParameterExcludes"] ***REMOVED*** -2147483648 /* NodeExcludes */] ***REMOVED*** "ParameterExcludes";
  TransformFlags3[TransformFlags3["CatchClauseExcludes"] ***REMOVED*** -2147418112] ***REMOVED*** "CatchClauseExcludes";
  TransformFlags3[TransformFlags3["BindingPatternExcludes"] ***REMOVED*** -2147450880] ***REMOVED*** "BindingPatternExcludes";
  TransformFlags3[TransformFlags3["ContainsLexicalThisOrSuper"] ***REMOVED*** 134234112] ***REMOVED*** "ContainsLexicalThisOrSuper";
  TransformFlags3[TransformFlags3["PropertyNamePropagatingFlags"] ***REMOVED*** 134234112] ***REMOVED*** "PropertyNamePropagatingFlags";
  return TransformFlags3;
})(TransformFlags || {});
var SnippetKind ***REMOVED*** /* @__PURE__ */ ((SnippetKind3) ***REMOVED***> {
  SnippetKind3[SnippetKind3["TabStop"] ***REMOVED*** 0] ***REMOVED*** "TabStop";
  SnippetKind3[SnippetKind3["Placeholder"] ***REMOVED*** 1] ***REMOVED*** "Placeholder";
  SnippetKind3[SnippetKind3["Choice"] ***REMOVED*** 2] ***REMOVED*** "Choice";
  SnippetKind3[SnippetKind3["Variable"] ***REMOVED*** 3] ***REMOVED*** "Variable";
  return SnippetKind3;
})(SnippetKind || {});
var EmitFlags ***REMOVED*** /* @__PURE__ */ ((EmitFlags3) ***REMOVED***> {
  EmitFlags3[EmitFlags3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  EmitFlags3[EmitFlags3["SingleLine"] ***REMOVED*** 1] ***REMOVED*** "SingleLine";
  EmitFlags3[EmitFlags3["MultiLine"] ***REMOVED*** 2] ***REMOVED*** "MultiLine";
  EmitFlags3[EmitFlags3["AdviseOnEmitNode"] ***REMOVED*** 4] ***REMOVED*** "AdviseOnEmitNode";
  EmitFlags3[EmitFlags3["NoSubstitution"] ***REMOVED*** 8] ***REMOVED*** "NoSubstitution";
  EmitFlags3[EmitFlags3["CapturesThis"] ***REMOVED*** 16] ***REMOVED*** "CapturesThis";
  EmitFlags3[EmitFlags3["NoLeadingSourceMap"] ***REMOVED*** 32] ***REMOVED*** "NoLeadingSourceMap";
  EmitFlags3[EmitFlags3["NoTrailingSourceMap"] ***REMOVED*** 64] ***REMOVED*** "NoTrailingSourceMap";
  EmitFlags3[EmitFlags3["NoSourceMap"] ***REMOVED*** 96] ***REMOVED*** "NoSourceMap";
  EmitFlags3[EmitFlags3["NoNestedSourceMaps"] ***REMOVED*** 128] ***REMOVED*** "NoNestedSourceMaps";
  EmitFlags3[EmitFlags3["NoTokenLeadingSourceMaps"] ***REMOVED*** 256] ***REMOVED*** "NoTokenLeadingSourceMaps";
  EmitFlags3[EmitFlags3["NoTokenTrailingSourceMaps"] ***REMOVED*** 512] ***REMOVED*** "NoTokenTrailingSourceMaps";
  EmitFlags3[EmitFlags3["NoTokenSourceMaps"] ***REMOVED*** 768] ***REMOVED*** "NoTokenSourceMaps";
  EmitFlags3[EmitFlags3["NoLeadingComments"] ***REMOVED*** 1024] ***REMOVED*** "NoLeadingComments";
  EmitFlags3[EmitFlags3["NoTrailingComments"] ***REMOVED*** 2048] ***REMOVED*** "NoTrailingComments";
  EmitFlags3[EmitFlags3["NoComments"] ***REMOVED*** 3072] ***REMOVED*** "NoComments";
  EmitFlags3[EmitFlags3["NoNestedComments"] ***REMOVED*** 4096] ***REMOVED*** "NoNestedComments";
  EmitFlags3[EmitFlags3["HelperName"] ***REMOVED*** 8192] ***REMOVED*** "HelperName";
  EmitFlags3[EmitFlags3["ExportName"] ***REMOVED*** 16384] ***REMOVED*** "ExportName";
  EmitFlags3[EmitFlags3["LocalName"] ***REMOVED*** 32768] ***REMOVED*** "LocalName";
  EmitFlags3[EmitFlags3["InternalName"] ***REMOVED*** 65536] ***REMOVED*** "InternalName";
  EmitFlags3[EmitFlags3["Indented"] ***REMOVED*** 131072] ***REMOVED*** "Indented";
  EmitFlags3[EmitFlags3["NoIndentation"] ***REMOVED*** 262144] ***REMOVED*** "NoIndentation";
  EmitFlags3[EmitFlags3["AsyncFunctionBody"] ***REMOVED*** 524288] ***REMOVED*** "AsyncFunctionBody";
  EmitFlags3[EmitFlags3["ReuseTempVariableScope"] ***REMOVED*** 1048576] ***REMOVED*** "ReuseTempVariableScope";
  EmitFlags3[EmitFlags3["CustomPrologue"] ***REMOVED*** 2097152] ***REMOVED*** "CustomPrologue";
  EmitFlags3[EmitFlags3["NoHoisting"] ***REMOVED*** 4194304] ***REMOVED*** "NoHoisting";
  EmitFlags3[EmitFlags3["Iterator"] ***REMOVED*** 8388608] ***REMOVED*** "Iterator";
  EmitFlags3[EmitFlags3["NoAsciiEscaping"] ***REMOVED*** 16777216] ***REMOVED*** "NoAsciiEscaping";
  return EmitFlags3;
})(EmitFlags || {});
var LanguageFeatureMinimumTarget ***REMOVED*** {
  Classes: 2 /* ES2015 */,
  ForOf: 2 /* ES2015 */,
  Generators: 2 /* ES2015 */,
  Iteration: 2 /* ES2015 */,
  SpreadElements: 2 /* ES2015 */,
  RestElements: 2 /* ES2015 */,
  TaggedTemplates: 2 /* ES2015 */,
  DestructuringAssignment: 2 /* ES2015 */,
  BindingPatterns: 2 /* ES2015 */,
  ArrowFunctions: 2 /* ES2015 */,
  BlockScopedVariables: 2 /* ES2015 */,
  ObjectAssign: 2 /* ES2015 */,
  RegularExpressionFlagsUnicode: 2 /* ES2015 */,
  RegularExpressionFlagsSticky: 2 /* ES2015 */,
  Exponentiation: 3 /* ES2016 */,
  AsyncFunctions: 4 /* ES2017 */,
  ForAwaitOf: 5 /* ES2018 */,
  AsyncGenerators: 5 /* ES2018 */,
  AsyncIteration: 5 /* ES2018 */,
  ObjectSpreadRest: 5 /* ES2018 */,
  RegularExpressionFlagsDotAll: 5 /* ES2018 */,
  BindinglessCatch: 6 /* ES2019 */,
  BigInt: 7 /* ES2020 */,
  NullishCoalesce: 7 /* ES2020 */,
  OptionalChaining: 7 /* ES2020 */,
  LogicalAssignment: 8 /* ES2021 */,
  TopLevelAwait: 9 /* ES2022 */,
  ClassFields: 9 /* ES2022 */,
  PrivateNamesAndClassStaticBlocks: 9 /* ES2022 */,
  RegularExpressionFlagsHasIndices: 9 /* ES2022 */,
  ShebangComments: 10 /* ES2023 */,
  RegularExpressionFlagsUnicodeSets: 11 /* ES2024 */,
  UsingAndAwaitUsing: 99 /* ESNext */,
  ClassAndClassElementDecorators: 99 /* ESNext */
};
var commentPragmas ***REMOVED*** {
  "reference": {
    args: [
      { name: "types", optional: true, captureSpan: true },
      { name: "lib", optional: true, captureSpan: true },
      { name: "path", optional: true, captureSpan: true },
      { name: "no-default-lib", optional: true },
      { name: "resolution-mode", optional: true },
      { name: "preserve", optional: true }
    ],
    kind: 1 /* TripleSlashXML */
  },
  "amd-dependency": {
    args: [{ name: "path" }, { name: "name", optional: true }],
    kind: 1 /* TripleSlashXML */
  },
  "amd-module": {
    args: [{ name: "name" }],
    kind: 1 /* TripleSlashXML */
  },
  "ts-check": {
    kind: 2 /* SingleLine */
  },
  "ts-nocheck": {
    kind: 2 /* SingleLine */
  },
  "jsx": {
    args: [{ name: "factory" }],
    kind: 4 /* MultiLine */
  },
  "jsxfrag": {
    args: [{ name: "factory" }],
    kind: 4 /* MultiLine */
  },
  "jsximportsource": {
    args: [{ name: "factory" }],
    kind: 4 /* MultiLine */
  },
  "jsxruntime": {
    args: [{ name: "factory" }],
    kind: 4 /* MultiLine */
  }
};

// src/compiler/sys.ts
function generateDjb2Hash(data) {
  let acc ***REMOVED*** 5381;
  for (let i ***REMOVED*** 0; i < data.length; i++) {
    acc ***REMOVED*** (acc << 5) + acc + data.charCodeAt(i);
  }
  return acc.toString();
}
var PollingInterval ***REMOVED*** /* @__PURE__ */ ((PollingInterval3) ***REMOVED***> {
  PollingInterval3[PollingInterval3["High"] ***REMOVED*** 2e3] ***REMOVED*** "High";
  PollingInterval3[PollingInterval3["Medium"] ***REMOVED*** 500] ***REMOVED*** "Medium";
  PollingInterval3[PollingInterval3["Low"] ***REMOVED*** 250] ***REMOVED*** "Low";
  return PollingInterval3;
})(PollingInterval || {});
var missingFileModifiedTime ***REMOVED*** /* @__PURE__ */ new Date(0);
function getModifiedTime(host, fileName) {
  return host.getModifiedTime(fileName) || missingFileModifiedTime;
}
function createPollingIntervalBasedLevels(levels) {
  return {
    [250 /* Low */]: levels.Low,
    [500 /* Medium */]: levels.Medium,
    [2e3 /* High */]: levels.High
  };
}
var defaultChunkLevels ***REMOVED*** { Low: 32, Medium: 64, High: 256 };
var pollingChunkSize ***REMOVED*** createPollingIntervalBasedLevels(defaultChunkLevels);
var unchangedPollThresholds ***REMOVED*** createPollingIntervalBasedLevels(defaultChunkLevels);
function setCustomPollingValues(system) {
  if (!system.getEnvironmentVariable) {
    return;
  }
  const pollingIntervalChanged ***REMOVED*** setCustomLevels("TSC_WATCH_POLLINGINTERVAL", PollingInterval);
  pollingChunkSize ***REMOVED*** getCustomPollingBasedLevels("TSC_WATCH_POLLINGCHUNKSIZE", defaultChunkLevels) || pollingChunkSize;
  unchangedPollThresholds ***REMOVED*** getCustomPollingBasedLevels("TSC_WATCH_UNCHANGEDPOLLTHRESHOLDS", defaultChunkLevels) || unchangedPollThresholds;
  function getLevel(envVar, level) {
    return system.getEnvironmentVariable(`${envVar}_${level.toUpperCase()}`);
  }
  function getCustomLevels(baseVariable) {
    let customLevels;
    setCustomLevel("Low");
    setCustomLevel("Medium");
    setCustomLevel("High");
    return customLevels;
    function setCustomLevel(level) {
      const customLevel ***REMOVED*** getLevel(baseVariable, level);
      if (customLevel) {
        (customLevels || (customLevels ***REMOVED*** {}))[level] ***REMOVED*** Number(customLevel);
      }
    }
  }
  function setCustomLevels(baseVariable, levels) {
    const customLevels ***REMOVED*** getCustomLevels(baseVariable);
    if (customLevels) {
      setLevel("Low");
      setLevel("Medium");
      setLevel("High");
      return true;
    }
    return false;
    function setLevel(level) {
      levels[level] ***REMOVED*** customLevels[level] || levels[level];
    }
  }
  function getCustomPollingBasedLevels(baseVariable, defaultLevels) {
    const customLevels ***REMOVED*** getCustomLevels(baseVariable);
    return (pollingIntervalChanged || customLevels) && createPollingIntervalBasedLevels(customLevels ? { ...defaultLevels, ...customLevels } : defaultLevels);
  }
}
function pollWatchedFileQueue(host, queue, pollIndex, chunkSize, callbackOnWatchFileStat) {
  let definedValueCopyToIndex ***REMOVED*** pollIndex;
  for (let canVisit ***REMOVED*** queue.length; chunkSize && canVisit; nextPollIndex(), canVisit--) {
    const watchedFile ***REMOVED*** queue[pollIndex];
    if (!watchedFile) {
      continue;
    } else if (watchedFile.isClosed) {
      queue[pollIndex] ***REMOVED*** void 0;
      continue;
    }
    chunkSize--;
    const fileChanged ***REMOVED*** onWatchedFileStat(watchedFile, getModifiedTime(host, watchedFile.fileName));
    if (watchedFile.isClosed) {
      queue[pollIndex] ***REMOVED*** void 0;
      continue;
    }
    callbackOnWatchFileStat ***REMOVED******REMOVED*** null ? void 0 : callbackOnWatchFileStat(watchedFile, pollIndex, fileChanged);
    if (queue[pollIndex]) {
      if (definedValueCopyToIndex < pollIndex) {
        queue[definedValueCopyToIndex] ***REMOVED*** watchedFile;
        queue[pollIndex] ***REMOVED*** void 0;
      }
      definedValueCopyToIndex++;
    }
  }
  return pollIndex;
  function nextPollIndex() {
    pollIndex++;
    if (pollIndex ***REMOVED******REMOVED******REMOVED*** queue.length) {
      if (definedValueCopyToIndex < pollIndex) {
        queue.length ***REMOVED*** definedValueCopyToIndex;
      }
      pollIndex ***REMOVED*** 0;
      definedValueCopyToIndex ***REMOVED*** 0;
    }
  }
}
function createDynamicPriorityPollingWatchFile(host) {
  const watchedFiles ***REMOVED*** [];
  const changedFilesInLastPoll ***REMOVED*** [];
  const lowPollingIntervalQueue ***REMOVED*** createPollingIntervalQueue(250 /* Low */);
  const mediumPollingIntervalQueue ***REMOVED*** createPollingIntervalQueue(500 /* Medium */);
  const highPollingIntervalQueue ***REMOVED*** createPollingIntervalQueue(2e3 /* High */);
  return watchFile2;
  function watchFile2(fileName, callback, defaultPollingInterval) {
    const file ***REMOVED*** {
      fileName,
      callback,
      unchangedPolls: 0,
      mtime: getModifiedTime(host, fileName)
    };
    watchedFiles.push(file);
    addToPollingIntervalQueue(file, defaultPollingInterval);
    return {
      close: () ***REMOVED***> {
        file.isClosed ***REMOVED*** true;
        unorderedRemoveItem(watchedFiles, file);
      }
    };
  }
  function createPollingIntervalQueue(pollingInterval) {
    const queue ***REMOVED*** [];
    queue.pollingInterval ***REMOVED*** pollingInterval;
    queue.pollIndex ***REMOVED*** 0;
    queue.pollScheduled ***REMOVED*** false;
    return queue;
  }
  function pollPollingIntervalQueue(_timeoutType, queue) {
    queue.pollIndex ***REMOVED*** pollQueue(queue, queue.pollingInterval, queue.pollIndex, pollingChunkSize[queue.pollingInterval]);
    if (queue.length) {
      scheduleNextPoll(queue.pollingInterval);
    } else {
      Debug.assert(queue.pollIndex ***REMOVED******REMOVED******REMOVED*** 0);
      queue.pollScheduled ***REMOVED*** false;
    }
  }
  function pollLowPollingIntervalQueue(_timeoutType, queue) {
    pollQueue(
      changedFilesInLastPoll,
      250 /* Low */,
      /*pollIndex*/
      0,
      changedFilesInLastPoll.length
    );
    pollPollingIntervalQueue(_timeoutType, queue);
    if (!queue.pollScheduled && changedFilesInLastPoll.length) {
      scheduleNextPoll(250 /* Low */);
    }
  }
  function pollQueue(queue, pollingInterval, pollIndex, chunkSize) {
    return pollWatchedFileQueue(
      host,
      queue,
      pollIndex,
      chunkSize,
      onWatchFileStat
    );
    function onWatchFileStat(watchedFile, pollIndex2, fileChanged) {
      if (fileChanged) {
        watchedFile.unchangedPolls ***REMOVED*** 0;
        if (queue !***REMOVED******REMOVED*** changedFilesInLastPoll) {
          queue[pollIndex2] ***REMOVED*** void 0;
          addChangedFileToLowPollingIntervalQueue(watchedFile);
        }
      } else if (watchedFile.unchangedPolls !***REMOVED******REMOVED*** unchangedPollThresholds[pollingInterval]) {
        watchedFile.unchangedPolls++;
      } else if (queue ***REMOVED******REMOVED******REMOVED*** changedFilesInLastPoll) {
        watchedFile.unchangedPolls ***REMOVED*** 1;
        queue[pollIndex2] ***REMOVED*** void 0;
        addToPollingIntervalQueue(watchedFile, 250 /* Low */);
      } else if (pollingInterval !***REMOVED******REMOVED*** 2e3 /* High */) {
        watchedFile.unchangedPolls++;
        queue[pollIndex2] ***REMOVED*** void 0;
        addToPollingIntervalQueue(watchedFile, pollingInterval ***REMOVED******REMOVED******REMOVED*** 250 /* Low */ ? 500 /* Medium */ : 2e3 /* High */);
      }
    }
  }
  function pollingIntervalQueue(pollingInterval) {
    switch (pollingInterval) {
      case 250 /* Low */:
        return lowPollingIntervalQueue;
      case 500 /* Medium */:
        return mediumPollingIntervalQueue;
      case 2e3 /* High */:
        return highPollingIntervalQueue;
    }
  }
  function addToPollingIntervalQueue(file, pollingInterval) {
    pollingIntervalQueue(pollingInterval).push(file);
    scheduleNextPollIfNotAlreadyScheduled(pollingInterval);
  }
  function addChangedFileToLowPollingIntervalQueue(file) {
    changedFilesInLastPoll.push(file);
    scheduleNextPollIfNotAlreadyScheduled(250 /* Low */);
  }
  function scheduleNextPollIfNotAlreadyScheduled(pollingInterval) {
    if (!pollingIntervalQueue(pollingInterval).pollScheduled) {
      scheduleNextPoll(pollingInterval);
    }
  }
  function scheduleNextPoll(pollingInterval) {
    pollingIntervalQueue(pollingInterval).pollScheduled ***REMOVED*** host.setTimeout(pollingInterval ***REMOVED******REMOVED******REMOVED*** 250 /* Low */ ? pollLowPollingIntervalQueue : pollPollingIntervalQueue, pollingInterval, pollingInterval ***REMOVED******REMOVED******REMOVED*** 250 /* Low */ ? "pollLowPollingIntervalQueue" : "pollPollingIntervalQueue", pollingIntervalQueue(pollingInterval));
  }
}
function createUseFsEventsOnParentDirectoryWatchFile(fsWatch, useCaseSensitiveFileNames2, getModifiedTime3, fsWatchWithTimestamp) {
  const fileWatcherCallbacks ***REMOVED*** createMultiMap();
  const fileTimestamps ***REMOVED*** fsWatchWithTimestamp ? /* @__PURE__ */ new Map() : void 0;
  const dirWatchers ***REMOVED*** /* @__PURE__ */ new Map();
  const toCanonicalName ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  return nonPollingWatchFile;
  function nonPollingWatchFile(fileName, callback, _pollingInterval, fallbackOptions) {
    const filePath ***REMOVED*** toCanonicalName(fileName);
    if (fileWatcherCallbacks.add(filePath, callback).length ***REMOVED******REMOVED******REMOVED*** 1 && fileTimestamps) {
      fileTimestamps.set(filePath, getModifiedTime3(fileName) || missingFileModifiedTime);
    }
    const dirPath ***REMOVED*** getDirectoryPath(filePath) || ".";
    const watcher ***REMOVED*** dirWatchers.get(dirPath) || createDirectoryWatcher(getDirectoryPath(fileName) || ".", dirPath, fallbackOptions);
    watcher.referenceCount++;
    return {
      close: () ***REMOVED***> {
        if (watcher.referenceCount ***REMOVED******REMOVED******REMOVED*** 1) {
          watcher.close();
          dirWatchers.delete(dirPath);
        } else {
          watcher.referenceCount--;
        }
        fileWatcherCallbacks.remove(filePath, callback);
      }
    };
  }
  function createDirectoryWatcher(dirName, dirPath, fallbackOptions) {
    const watcher ***REMOVED*** fsWatch(
      dirName,
      1 /* Directory */,
      (eventName, relativeFileName) ***REMOVED***> {
        if (!isString(relativeFileName)) return;
        const fileName ***REMOVED*** getNormalizedAbsolutePath(relativeFileName, dirName);
        const filePath ***REMOVED*** toCanonicalName(fileName);
        const callbacks ***REMOVED*** fileName && fileWatcherCallbacks.get(filePath);
        if (callbacks) {
          let currentModifiedTime;
          let eventKind ***REMOVED*** 1 /* Changed */;
          if (fileTimestamps) {
            const existingTime ***REMOVED*** fileTimestamps.get(filePath);
            if (eventName ***REMOVED******REMOVED******REMOVED*** "change") {
              currentModifiedTime ***REMOVED*** getModifiedTime3(fileName) || missingFileModifiedTime;
              if (currentModifiedTime.getTime() ***REMOVED******REMOVED******REMOVED*** existingTime.getTime()) return;
            }
            currentModifiedTime || (currentModifiedTime ***REMOVED*** getModifiedTime3(fileName) || missingFileModifiedTime);
            fileTimestamps.set(filePath, currentModifiedTime);
            if (existingTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) eventKind ***REMOVED*** 0 /* Created */;
            else if (currentModifiedTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) eventKind ***REMOVED*** 2 /* Deleted */;
          }
          for (const fileCallback of callbacks) {
            fileCallback(fileName, eventKind, currentModifiedTime);
          }
        }
      },
      /*recursive*/
      false,
      500 /* Medium */,
      fallbackOptions
    );
    watcher.referenceCount ***REMOVED*** 0;
    dirWatchers.set(dirPath, watcher);
    return watcher;
  }
}
function createFixedChunkSizePollingWatchFile(host) {
  const watchedFiles ***REMOVED*** [];
  let pollIndex ***REMOVED*** 0;
  let pollScheduled;
  return watchFile2;
  function watchFile2(fileName, callback) {
    const file ***REMOVED*** {
      fileName,
      callback,
      mtime: getModifiedTime(host, fileName)
    };
    watchedFiles.push(file);
    scheduleNextPoll();
    return {
      close: () ***REMOVED***> {
        file.isClosed ***REMOVED*** true;
        unorderedRemoveItem(watchedFiles, file);
      }
    };
  }
  function pollQueue() {
    pollScheduled ***REMOVED*** void 0;
    pollIndex ***REMOVED*** pollWatchedFileQueue(host, watchedFiles, pollIndex, pollingChunkSize[250 /* Low */]);
    scheduleNextPoll();
  }
  function scheduleNextPoll() {
    if (!watchedFiles.length || pollScheduled) return;
    pollScheduled ***REMOVED*** host.setTimeout(pollQueue, 2e3 /* High */, "pollQueue");
  }
}
function createSingleWatcherPerName(cache, useCaseSensitiveFileNames2, name, callback, createWatcher) {
  const toCanonicalFileName ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  const path ***REMOVED*** toCanonicalFileName(name);
  const existing ***REMOVED*** cache.get(path);
  if (existing) {
    existing.callbacks.push(callback);
  } else {
    cache.set(path, {
      watcher: createWatcher(
        // Cant infer types correctly so lets satisfy checker
        (param1, param2, param3) ***REMOVED***> {
          var _a;
          return (_a ***REMOVED*** cache.get(path)) ***REMOVED******REMOVED*** null ? void 0 : _a.callbacks.slice().forEach((cb) ***REMOVED***> cb(param1, param2, param3));
        }
      ),
      callbacks: [callback]
    });
  }
  return {
    close: () ***REMOVED***> {
      const watcher ***REMOVED*** cache.get(path);
      if (!watcher) return;
      if (!orderedRemoveItem(watcher.callbacks, callback) || watcher.callbacks.length) return;
      cache.delete(path);
      closeFileWatcherOf(watcher);
    }
  };
}
function onWatchedFileStat(watchedFile, modifiedTime) {
  const oldTime ***REMOVED*** watchedFile.mtime.getTime();
  const newTime ***REMOVED*** modifiedTime.getTime();
  if (oldTime !***REMOVED******REMOVED*** newTime) {
    watchedFile.mtime ***REMOVED*** modifiedTime;
    watchedFile.callback(watchedFile.fileName, getFileWatcherEventKind(oldTime, newTime), modifiedTime);
    return true;
  }
  return false;
}
function getFileWatcherEventKind(oldTime, newTime) {
  return oldTime ***REMOVED******REMOVED******REMOVED*** 0 ? 0 /* Created */ : newTime ***REMOVED******REMOVED******REMOVED*** 0 ? 2 /* Deleted */ : 1 /* Changed */;
}
var ignoredPaths ***REMOVED*** ["/node_modules/.", "/.git", "/.#"];
var curSysLog ***REMOVED*** noop;
function sysLog(s) {
  return curSysLog(s);
}
function setSysLog(logger) {
  curSysLog ***REMOVED*** logger;
}
function createDirectoryWatcherSupportingRecursive({
  watchDirectory,
  useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
  getCurrentDirectory,
  getAccessibleSortedChildDirectories,
  fileSystemEntryExists,
  realpath,
  setTimeout: setTimeout2,
  clearTimeout: clearTimeout2
}) {
  const cache ***REMOVED*** /* @__PURE__ */ new Map();
  const callbackCache ***REMOVED*** createMultiMap();
  const cacheToUpdateChildWatches ***REMOVED*** /* @__PURE__ */ new Map();
  let timerToUpdateChildWatches;
  const filePathComparer ***REMOVED*** getStringComparer(!useCaseSensitiveFileNames2);
  const toCanonicalFilePath ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  return (dirName, callback, recursive, options) ***REMOVED***> recursive ? createDirectoryWatcher(dirName, options, callback) : watchDirectory(dirName, callback, recursive, options);
  function createDirectoryWatcher(dirName, options, callback, link) {
    const dirPath ***REMOVED*** toCanonicalFilePath(dirName);
    let directoryWatcher ***REMOVED*** cache.get(dirPath);
    if (directoryWatcher) {
      directoryWatcher.refCount++;
    } else {
      directoryWatcher ***REMOVED*** {
        watcher: watchDirectory(
          dirName,
          (fileName) ***REMOVED***> {
            var _a;
            if (isIgnoredPath(fileName, options)) return;
            if (options ***REMOVED******REMOVED*** null ? void 0 : options.synchronousWatchDirectory) {
              if (!((_a ***REMOVED*** cache.get(dirPath)) ***REMOVED******REMOVED*** null ? void 0 : _a.targetWatcher)) invokeCallbacks(dirName, dirPath, fileName);
              updateChildWatches(dirName, dirPath, options);
            } else {
              nonSyncUpdateChildWatches(dirName, dirPath, fileName, options);
            }
          },
          /*recursive*/
          false,
          options
        ),
        refCount: 1,
        childWatches: emptyArray,
        targetWatcher: void 0,
        links: void 0
      };
      cache.set(dirPath, directoryWatcher);
      updateChildWatches(dirName, dirPath, options);
    }
    if (link) (directoryWatcher.links ?? (directoryWatcher.links ***REMOVED*** /* @__PURE__ */ new Set())).add(link);
    const callbackToAdd ***REMOVED*** callback && { dirName, callback };
    if (callbackToAdd) {
      callbackCache.add(dirPath, callbackToAdd);
    }
    return {
      dirName,
      close: () ***REMOVED***> {
        var _a;
        const directoryWatcher2 ***REMOVED*** Debug.checkDefined(cache.get(dirPath));
        if (callbackToAdd) callbackCache.remove(dirPath, callbackToAdd);
        if (link) (_a ***REMOVED*** directoryWatcher2.links) ***REMOVED******REMOVED*** null ? void 0 : _a.delete(link);
        directoryWatcher2.refCount--;
        if (directoryWatcher2.refCount) return;
        cache.delete(dirPath);
        directoryWatcher2.links ***REMOVED*** void 0;
        closeFileWatcherOf(directoryWatcher2);
        closeTargetWatcher(directoryWatcher2);
        directoryWatcher2.childWatches.forEach(closeFileWatcher);
      }
    };
  }
  function invokeCallbacks(dirName, dirPath, fileNameOrInvokeMap, fileNames) {
    var _a, _b;
    let fileName;
    let invokeMap;
    if (isString(fileNameOrInvokeMap)) {
      fileName ***REMOVED*** fileNameOrInvokeMap;
    } else {
      invokeMap ***REMOVED*** fileNameOrInvokeMap;
    }
    callbackCache.forEach((callbacks, rootDirName) ***REMOVED***> {
      if (invokeMap && invokeMap.get(rootDirName) ***REMOVED******REMOVED******REMOVED*** true) return;
      if (rootDirName ***REMOVED******REMOVED******REMOVED*** dirPath || startsWith(dirPath, rootDirName) && dirPath[rootDirName.length] ***REMOVED******REMOVED******REMOVED*** directorySeparator) {
        if (invokeMap) {
          if (fileNames) {
            const existing ***REMOVED*** invokeMap.get(rootDirName);
            if (existing) {
              existing.push(...fileNames);
            } else {
              invokeMap.set(rootDirName, fileNames.slice());
            }
          } else {
            invokeMap.set(rootDirName, true);
          }
        } else {
          callbacks.forEach(({ callback }) ***REMOVED***> callback(fileName));
        }
      }
    });
    (_b ***REMOVED*** (_a ***REMOVED*** cache.get(dirPath)) ***REMOVED******REMOVED*** null ? void 0 : _a.links) ***REMOVED******REMOVED*** null ? void 0 : _b.forEach((link) ***REMOVED***> {
      const toPathInLink ***REMOVED*** (fileName2) ***REMOVED***> combinePaths(link, getRelativePathFromDirectory(dirName, fileName2, toCanonicalFilePath));
      if (invokeMap) {
        invokeCallbacks(link, toCanonicalFilePath(link), invokeMap, fileNames ***REMOVED******REMOVED*** null ? void 0 : fileNames.map(toPathInLink));
      } else {
        invokeCallbacks(link, toCanonicalFilePath(link), toPathInLink(fileName));
      }
    });
  }
  function nonSyncUpdateChildWatches(dirName, dirPath, fileName, options) {
    const parentWatcher ***REMOVED*** cache.get(dirPath);
    if (parentWatcher && fileSystemEntryExists(dirName, 1 /* Directory */)) {
      scheduleUpdateChildWatches(dirName, dirPath, fileName, options);
      return;
    }
    invokeCallbacks(dirName, dirPath, fileName);
    closeTargetWatcher(parentWatcher);
    removeChildWatches(parentWatcher);
  }
  function scheduleUpdateChildWatches(dirName, dirPath, fileName, options) {
    const existing ***REMOVED*** cacheToUpdateChildWatches.get(dirPath);
    if (existing) {
      existing.fileNames.push(fileName);
    } else {
      cacheToUpdateChildWatches.set(dirPath, { dirName, options, fileNames: [fileName] });
    }
    if (timerToUpdateChildWatches) {
      clearTimeout2(timerToUpdateChildWatches);
      timerToUpdateChildWatches ***REMOVED*** void 0;
    }
    timerToUpdateChildWatches ***REMOVED*** setTimeout2(onTimerToUpdateChildWatches, 1e3, "timerToUpdateChildWatches");
  }
  function onTimerToUpdateChildWatches() {
    var _a;
    timerToUpdateChildWatches ***REMOVED*** void 0;
    sysLog(`sysLog:: onTimerToUpdateChildWatches:: ${cacheToUpdateChildWatches.size}`);
    const start ***REMOVED*** timestamp();
    const invokeMap ***REMOVED*** /* @__PURE__ */ new Map();
    while (!timerToUpdateChildWatches && cacheToUpdateChildWatches.size) {
      const result ***REMOVED*** cacheToUpdateChildWatches.entries().next();
      Debug.assert(!result.done);
      const { value: [dirPath, { dirName, options, fileNames }] } ***REMOVED*** result;
      cacheToUpdateChildWatches.delete(dirPath);
      const hasChanges ***REMOVED*** updateChildWatches(dirName, dirPath, options);
      if (!((_a ***REMOVED*** cache.get(dirPath)) ***REMOVED******REMOVED*** null ? void 0 : _a.targetWatcher)) invokeCallbacks(dirName, dirPath, invokeMap, hasChanges ? void 0 : fileNames);
    }
    sysLog(`sysLog:: invokingWatchers:: Elapsed:: ${timestamp() - start}ms:: ${cacheToUpdateChildWatches.size}`);
    callbackCache.forEach((callbacks, rootDirName) ***REMOVED***> {
      const existing ***REMOVED*** invokeMap.get(rootDirName);
      if (existing) {
        callbacks.forEach(({ callback, dirName }) ***REMOVED***> {
          if (isArray(existing)) {
            existing.forEach(callback);
          } else {
            callback(dirName);
          }
        });
      }
    });
    const elapsed ***REMOVED*** timestamp() - start;
    sysLog(`sysLog:: Elapsed:: ${elapsed}ms:: onTimerToUpdateChildWatches:: ${cacheToUpdateChildWatches.size} ${timerToUpdateChildWatches}`);
  }
  function removeChildWatches(parentWatcher) {
    if (!parentWatcher) return;
    const existingChildWatches ***REMOVED*** parentWatcher.childWatches;
    parentWatcher.childWatches ***REMOVED*** emptyArray;
    for (const childWatcher of existingChildWatches) {
      childWatcher.close();
      removeChildWatches(cache.get(toCanonicalFilePath(childWatcher.dirName)));
    }
  }
  function closeTargetWatcher(watcher) {
    if (watcher ***REMOVED******REMOVED*** null ? void 0 : watcher.targetWatcher) {
      watcher.targetWatcher.close();
      watcher.targetWatcher ***REMOVED*** void 0;
    }
  }
  function updateChildWatches(parentDir, parentDirPath, options) {
    const parentWatcher ***REMOVED*** cache.get(parentDirPath);
    if (!parentWatcher) return false;
    const target ***REMOVED*** normalizePath(realpath(parentDir));
    let hasChanges;
    let newChildWatches;
    if (filePathComparer(target, parentDir) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */) {
      hasChanges ***REMOVED*** enumerateInsertsAndDeletes(
        fileSystemEntryExists(parentDir, 1 /* Directory */) ? mapDefined(getAccessibleSortedChildDirectories(parentDir), (child) ***REMOVED***> {
          const childFullName ***REMOVED*** getNormalizedAbsolutePath(child, parentDir);
          return !isIgnoredPath(childFullName, options) && filePathComparer(childFullName, normalizePath(realpath(childFullName))) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ ? childFullName : void 0;
        }) : emptyArray,
        parentWatcher.childWatches,
        (child, childWatcher) ***REMOVED***> filePathComparer(child, childWatcher.dirName),
        createAndAddChildDirectoryWatcher,
        closeFileWatcher,
        addChildDirectoryWatcher
      );
    } else if (parentWatcher.targetWatcher && filePathComparer(target, parentWatcher.targetWatcher.dirName) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */) {
      hasChanges ***REMOVED*** false;
      Debug.assert(parentWatcher.childWatches ***REMOVED******REMOVED******REMOVED*** emptyArray);
    } else {
      closeTargetWatcher(parentWatcher);
      parentWatcher.targetWatcher ***REMOVED*** createDirectoryWatcher(
        target,
        options,
        /*callback*/
        void 0,
        parentDir
      );
      parentWatcher.childWatches.forEach(closeFileWatcher);
      hasChanges ***REMOVED*** true;
    }
    parentWatcher.childWatches ***REMOVED*** newChildWatches || emptyArray;
    return hasChanges;
    function createAndAddChildDirectoryWatcher(childName) {
      const result ***REMOVED*** createDirectoryWatcher(childName, options);
      addChildDirectoryWatcher(result);
    }
    function addChildDirectoryWatcher(childWatcher) {
      (newChildWatches || (newChildWatches ***REMOVED*** [])).push(childWatcher);
    }
  }
  function isIgnoredPath(path, options) {
    return some(ignoredPaths, (searchPath) ***REMOVED***> isInPath(path, searchPath)) || isIgnoredByWatchOptions(path, options, useCaseSensitiveFileNames2, getCurrentDirectory);
  }
  function isInPath(path, searchPath) {
    if (path.includes(searchPath)) return true;
    if (useCaseSensitiveFileNames2) return false;
    return toCanonicalFilePath(path).includes(searchPath);
  }
}
function createFileWatcherCallback(callback) {
  return (_fileName, eventKind, modifiedTime) ***REMOVED***> callback(eventKind ***REMOVED******REMOVED******REMOVED*** 1 /* Changed */ ? "change" : "rename", "", modifiedTime);
}
function createFsWatchCallbackForFileWatcherCallback(fileName, callback, getModifiedTime3) {
  return (eventName, _relativeFileName, modifiedTime) ***REMOVED***> {
    if (eventName ***REMOVED******REMOVED******REMOVED*** "rename") {
      modifiedTime || (modifiedTime ***REMOVED*** getModifiedTime3(fileName) || missingFileModifiedTime);
      callback(fileName, modifiedTime !***REMOVED******REMOVED*** missingFileModifiedTime ? 0 /* Created */ : 2 /* Deleted */, modifiedTime);
    } else {
      callback(fileName, 1 /* Changed */, modifiedTime);
    }
  };
}
function isIgnoredByWatchOptions(pathToCheck, options, useCaseSensitiveFileNames2, getCurrentDirectory) {
  return ((options ***REMOVED******REMOVED*** null ? void 0 : options.excludeDirectories) || (options ***REMOVED******REMOVED*** null ? void 0 : options.excludeFiles)) && (matchesExclude(pathToCheck, options ***REMOVED******REMOVED*** null ? void 0 : options.excludeFiles, useCaseSensitiveFileNames2, getCurrentDirectory()) || matchesExclude(pathToCheck, options ***REMOVED******REMOVED*** null ? void 0 : options.excludeDirectories, useCaseSensitiveFileNames2, getCurrentDirectory()));
}
function createFsWatchCallbackForDirectoryWatcherCallback(directoryName, callback, options, useCaseSensitiveFileNames2, getCurrentDirectory) {
  return (eventName, relativeFileName) ***REMOVED***> {
    if (eventName ***REMOVED******REMOVED******REMOVED*** "rename") {
      const fileName ***REMOVED*** !relativeFileName ? directoryName : normalizePath(combinePaths(directoryName, relativeFileName));
      if (!relativeFileName || !isIgnoredByWatchOptions(fileName, options, useCaseSensitiveFileNames2, getCurrentDirectory)) {
        callback(fileName);
      }
    }
  };
}
function createSystemWatchFunctions({
  pollingWatchFileWorker,
  getModifiedTime: getModifiedTime3,
  setTimeout: setTimeout2,
  clearTimeout: clearTimeout2,
  fsWatchWorker,
  fileSystemEntryExists,
  useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
  getCurrentDirectory,
  fsSupportsRecursiveFsWatch,
  getAccessibleSortedChildDirectories,
  realpath,
  tscWatchFile,
  useNonPollingWatchers,
  tscWatchDirectory,
  inodeWatching,
  fsWatchWithTimestamp,
  sysLog: sysLog2
}) {
  const pollingWatches ***REMOVED*** /* @__PURE__ */ new Map();
  const fsWatches ***REMOVED*** /* @__PURE__ */ new Map();
  const fsWatchesRecursive ***REMOVED*** /* @__PURE__ */ new Map();
  let dynamicPollingWatchFile;
  let fixedChunkSizePollingWatchFile;
  let nonPollingWatchFile;
  let hostRecursiveDirectoryWatcher;
  let hitSystemWatcherLimit ***REMOVED*** false;
  return {
    watchFile: watchFile2,
    watchDirectory
  };
  function watchFile2(fileName, callback, pollingInterval, options) {
    options ***REMOVED*** updateOptionsForWatchFile(options, useNonPollingWatchers);
    const watchFileKind ***REMOVED*** Debug.checkDefined(options.watchFile);
    switch (watchFileKind) {
      case 0 /* FixedPollingInterval */:
        return pollingWatchFile(
          fileName,
          callback,
          250 /* Low */,
          /*options*/
          void 0
        );
      case 1 /* PriorityPollingInterval */:
        return pollingWatchFile(
          fileName,
          callback,
          pollingInterval,
          /*options*/
          void 0
        );
      case 2 /* DynamicPriorityPolling */:
        return ensureDynamicPollingWatchFile()(
          fileName,
          callback,
          pollingInterval,
          /*options*/
          void 0
        );
      case 3 /* FixedChunkSizePolling */:
        return ensureFixedChunkSizePollingWatchFile()(
          fileName,
          callback,
          /* pollingInterval */
          void 0,
          /*options*/
          void 0
        );
      case 4 /* UseFsEvents */:
        return fsWatch(
          fileName,
          0 /* File */,
          createFsWatchCallbackForFileWatcherCallback(fileName, callback, getModifiedTime3),
          /*recursive*/
          false,
          pollingInterval,
          getFallbackOptions(options)
        );
      case 5 /* UseFsEventsOnParentDirectory */:
        if (!nonPollingWatchFile) {
          nonPollingWatchFile ***REMOVED*** createUseFsEventsOnParentDirectoryWatchFile(fsWatch, useCaseSensitiveFileNames2, getModifiedTime3, fsWatchWithTimestamp);
        }
        return nonPollingWatchFile(fileName, callback, pollingInterval, getFallbackOptions(options));
      default:
        Debug.assertNever(watchFileKind);
    }
  }
  function ensureDynamicPollingWatchFile() {
    return dynamicPollingWatchFile || (dynamicPollingWatchFile ***REMOVED*** createDynamicPriorityPollingWatchFile({ getModifiedTime: getModifiedTime3, setTimeout: setTimeout2 }));
  }
  function ensureFixedChunkSizePollingWatchFile() {
    return fixedChunkSizePollingWatchFile || (fixedChunkSizePollingWatchFile ***REMOVED*** createFixedChunkSizePollingWatchFile({ getModifiedTime: getModifiedTime3, setTimeout: setTimeout2 }));
  }
  function updateOptionsForWatchFile(options, useNonPollingWatchers2) {
    if (options && options.watchFile !***REMOVED******REMOVED*** void 0) return options;
    switch (tscWatchFile) {
      case "PriorityPollingInterval":
        return { watchFile: 1 /* PriorityPollingInterval */ };
      case "DynamicPriorityPolling":
        return { watchFile: 2 /* DynamicPriorityPolling */ };
      case "UseFsEvents":
        return generateWatchFileOptions(4 /* UseFsEvents */, 1 /* PriorityInterval */, options);
      case "UseFsEventsWithFallbackDynamicPolling":
        return generateWatchFileOptions(4 /* UseFsEvents */, 2 /* DynamicPriority */, options);
      case "UseFsEventsOnParentDirectory":
        useNonPollingWatchers2 ***REMOVED*** true;
      // fall through
      default:
        return useNonPollingWatchers2 ? (
          // Use notifications from FS to watch with falling back to fs.watchFile
          generateWatchFileOptions(5 /* UseFsEventsOnParentDirectory */, 1 /* PriorityInterval */, options)
        ) : (
          // Default to using fs events
          { watchFile: 4 /* UseFsEvents */ }
        );
    }
  }
  function generateWatchFileOptions(watchFile3, fallbackPolling, options) {
    const defaultFallbackPolling ***REMOVED*** options ***REMOVED******REMOVED*** null ? void 0 : options.fallbackPolling;
    return {
      watchFile: watchFile3,
      fallbackPolling: defaultFallbackPolling ***REMOVED******REMOVED******REMOVED*** void 0 ? fallbackPolling : defaultFallbackPolling
    };
  }
  function watchDirectory(directoryName, callback, recursive, options) {
    if (fsSupportsRecursiveFsWatch) {
      return fsWatch(
        directoryName,
        1 /* Directory */,
        createFsWatchCallbackForDirectoryWatcherCallback(directoryName, callback, options, useCaseSensitiveFileNames2, getCurrentDirectory),
        recursive,
        500 /* Medium */,
        getFallbackOptions(options)
      );
    }
    if (!hostRecursiveDirectoryWatcher) {
      hostRecursiveDirectoryWatcher ***REMOVED*** createDirectoryWatcherSupportingRecursive({
        useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
        getCurrentDirectory,
        fileSystemEntryExists,
        getAccessibleSortedChildDirectories,
        watchDirectory: nonRecursiveWatchDirectory,
        realpath,
        setTimeout: setTimeout2,
        clearTimeout: clearTimeout2
      });
    }
    return hostRecursiveDirectoryWatcher(directoryName, callback, recursive, options);
  }
  function nonRecursiveWatchDirectory(directoryName, callback, recursive, options) {
    Debug.assert(!recursive);
    const watchDirectoryOptions ***REMOVED*** updateOptionsForWatchDirectory(options);
    const watchDirectoryKind ***REMOVED*** Debug.checkDefined(watchDirectoryOptions.watchDirectory);
    switch (watchDirectoryKind) {
      case 1 /* FixedPollingInterval */:
        return pollingWatchFile(
          directoryName,
          () ***REMOVED***> callback(directoryName),
          500 /* Medium */,
          /*options*/
          void 0
        );
      case 2 /* DynamicPriorityPolling */:
        return ensureDynamicPollingWatchFile()(
          directoryName,
          () ***REMOVED***> callback(directoryName),
          500 /* Medium */,
          /*options*/
          void 0
        );
      case 3 /* FixedChunkSizePolling */:
        return ensureFixedChunkSizePollingWatchFile()(
          directoryName,
          () ***REMOVED***> callback(directoryName),
          /* pollingInterval */
          void 0,
          /*options*/
          void 0
        );
      case 0 /* UseFsEvents */:
        return fsWatch(
          directoryName,
          1 /* Directory */,
          createFsWatchCallbackForDirectoryWatcherCallback(directoryName, callback, options, useCaseSensitiveFileNames2, getCurrentDirectory),
          recursive,
          500 /* Medium */,
          getFallbackOptions(watchDirectoryOptions)
        );
      default:
        Debug.assertNever(watchDirectoryKind);
    }
  }
  function updateOptionsForWatchDirectory(options) {
    if (options && options.watchDirectory !***REMOVED******REMOVED*** void 0) return options;
    switch (tscWatchDirectory) {
      case "RecursiveDirectoryUsingFsWatchFile":
        return { watchDirectory: 1 /* FixedPollingInterval */ };
      case "RecursiveDirectoryUsingDynamicPriorityPolling":
        return { watchDirectory: 2 /* DynamicPriorityPolling */ };
      default:
        const defaultFallbackPolling ***REMOVED*** options ***REMOVED******REMOVED*** null ? void 0 : options.fallbackPolling;
        return {
          watchDirectory: 0 /* UseFsEvents */,
          fallbackPolling: defaultFallbackPolling !***REMOVED******REMOVED*** void 0 ? defaultFallbackPolling : void 0
        };
    }
  }
  function pollingWatchFile(fileName, callback, pollingInterval, options) {
    return createSingleWatcherPerName(
      pollingWatches,
      useCaseSensitiveFileNames2,
      fileName,
      callback,
      (cb) ***REMOVED***> pollingWatchFileWorker(fileName, cb, pollingInterval, options)
    );
  }
  function fsWatch(fileOrDirectory, entryKind, callback, recursive, fallbackPollingInterval, fallbackOptions) {
    return createSingleWatcherPerName(
      recursive ? fsWatchesRecursive : fsWatches,
      useCaseSensitiveFileNames2,
      fileOrDirectory,
      callback,
      (cb) ***REMOVED***> fsWatchHandlingExistenceOnHost(fileOrDirectory, entryKind, cb, recursive, fallbackPollingInterval, fallbackOptions)
    );
  }
  function fsWatchHandlingExistenceOnHost(fileOrDirectory, entryKind, callback, recursive, fallbackPollingInterval, fallbackOptions) {
    let lastDirectoryPartWithDirectorySeparator;
    let lastDirectoryPart;
    if (inodeWatching) {
      lastDirectoryPartWithDirectorySeparator ***REMOVED*** fileOrDirectory.substring(fileOrDirectory.lastIndexOf(directorySeparator));
      lastDirectoryPart ***REMOVED*** lastDirectoryPartWithDirectorySeparator.slice(directorySeparator.length);
    }
    let watcher ***REMOVED*** !fileSystemEntryExists(fileOrDirectory, entryKind) ? watchMissingFileSystemEntry() : watchPresentFileSystemEntry();
    return {
      close: () ***REMOVED***> {
        if (watcher) {
          watcher.close();
          watcher ***REMOVED*** void 0;
        }
      }
    };
    function updateWatcher(createWatcher) {
      if (watcher) {
        sysLog2(`sysLog:: ${fileOrDirectory}:: Changing watcher to ${createWatcher ***REMOVED******REMOVED******REMOVED*** watchPresentFileSystemEntry ? "Present" : "Missing"}FileSystemEntryWatcher`);
        watcher.close();
        watcher ***REMOVED*** createWatcher();
      }
    }
    function watchPresentFileSystemEntry() {
      if (hitSystemWatcherLimit) {
        sysLog2(`sysLog:: ${fileOrDirectory}:: Defaulting to watchFile`);
        return watchPresentFileSystemEntryWithFsWatchFile();
      }
      try {
        const presentWatcher ***REMOVED*** (entryKind ***REMOVED******REMOVED******REMOVED*** 1 /* Directory */ || !fsWatchWithTimestamp ? fsWatchWorker : fsWatchWorkerHandlingTimestamp)(
          fileOrDirectory,
          recursive,
          inodeWatching ? callbackChangingToMissingFileSystemEntry : callback
        );
        presentWatcher.on("error", () ***REMOVED***> {
          callback("rename", "");
          updateWatcher(watchMissingFileSystemEntry);
        });
        return presentWatcher;
      } catch (e) {
        hitSystemWatcherLimit || (hitSystemWatcherLimit ***REMOVED*** e.code ***REMOVED******REMOVED******REMOVED*** "ENOSPC");
        sysLog2(`sysLog:: ${fileOrDirectory}:: Changing to watchFile`);
        return watchPresentFileSystemEntryWithFsWatchFile();
      }
    }
    function callbackChangingToMissingFileSystemEntry(event, relativeName) {
      let originalRelativeName;
      if (relativeName && endsWith(relativeName, "~")) {
        originalRelativeName ***REMOVED*** relativeName;
        relativeName ***REMOVED*** relativeName.slice(0, relativeName.length - 1);
      }
      if (event ***REMOVED******REMOVED******REMOVED*** "rename" && (!relativeName || relativeName ***REMOVED******REMOVED******REMOVED*** lastDirectoryPart || endsWith(relativeName, lastDirectoryPartWithDirectorySeparator))) {
        const modifiedTime ***REMOVED*** getModifiedTime3(fileOrDirectory) || missingFileModifiedTime;
        if (originalRelativeName) callback(event, originalRelativeName, modifiedTime);
        callback(event, relativeName, modifiedTime);
        if (inodeWatching) {
          updateWatcher(modifiedTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime ? watchMissingFileSystemEntry : watchPresentFileSystemEntry);
        } else if (modifiedTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) {
          updateWatcher(watchMissingFileSystemEntry);
        }
      } else {
        if (originalRelativeName) callback(event, originalRelativeName);
        callback(event, relativeName);
      }
    }
    function watchPresentFileSystemEntryWithFsWatchFile() {
      return watchFile2(
        fileOrDirectory,
        createFileWatcherCallback(callback),
        fallbackPollingInterval,
        fallbackOptions
      );
    }
    function watchMissingFileSystemEntry() {
      return watchFile2(
        fileOrDirectory,
        (_fileName, eventKind, modifiedTime) ***REMOVED***> {
          if (eventKind ***REMOVED******REMOVED******REMOVED*** 0 /* Created */) {
            modifiedTime || (modifiedTime ***REMOVED*** getModifiedTime3(fileOrDirectory) || missingFileModifiedTime);
            if (modifiedTime !***REMOVED******REMOVED*** missingFileModifiedTime) {
              callback("rename", "", modifiedTime);
              updateWatcher(watchPresentFileSystemEntry);
            }
          }
        },
        fallbackPollingInterval,
        fallbackOptions
      );
    }
  }
  function fsWatchWorkerHandlingTimestamp(fileOrDirectory, recursive, callback) {
    let modifiedTime ***REMOVED*** getModifiedTime3(fileOrDirectory) || missingFileModifiedTime;
    return fsWatchWorker(fileOrDirectory, recursive, (eventName, relativeFileName, currentModifiedTime) ***REMOVED***> {
      if (eventName ***REMOVED******REMOVED******REMOVED*** "change") {
        currentModifiedTime || (currentModifiedTime ***REMOVED*** getModifiedTime3(fileOrDirectory) || missingFileModifiedTime);
        if (currentModifiedTime.getTime() ***REMOVED******REMOVED******REMOVED*** modifiedTime.getTime()) return;
      }
      modifiedTime ***REMOVED*** currentModifiedTime || getModifiedTime3(fileOrDirectory) || missingFileModifiedTime;
      callback(eventName, relativeFileName, modifiedTime);
    });
  }
}
function patchWriteFileEnsuringDirectory(sys2) {
  const originalWriteFile ***REMOVED*** sys2.writeFile;
  sys2.writeFile ***REMOVED*** (path, data, writeBom) ***REMOVED***> writeFileEnsuringDirectories(
    path,
    data,
    !!writeBom,
    (path2, data2, writeByteOrderMark) ***REMOVED***> originalWriteFile.call(sys2, path2, data2, writeByteOrderMark),
    (path2) ***REMOVED***> sys2.createDirectory(path2),
    (path2) ***REMOVED***> sys2.directoryExists(path2)
  );
}
var sys ***REMOVED*** (() ***REMOVED***> {
  const byteOrderMarkIndicator ***REMOVED*** "\uFEFF";
  function getNodeSystem() {
    const nativePattern ***REMOVED*** /^native |^\([^)]+\)$|^(?:internal[\\/]|[\w\s]+(?:\.js)?$)/;
    const _fs ***REMOVED*** require("fs");
    const _path ***REMOVED*** require("path");
    const _os ***REMOVED*** require("os");
    let _crypto;
    try {
      _crypto ***REMOVED*** require("crypto");
    } catch {
      _crypto ***REMOVED*** void 0;
    }
    let activeSession;
    let profilePath ***REMOVED*** "./profile.cpuprofile";
    const isMacOs ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** "darwin";
    const isLinuxOrMacOs ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** "linux" || isMacOs;
    const statSyncOptions ***REMOVED*** { throwIfNoEntry: false };
    const platform ***REMOVED*** _os.platform();
    const useCaseSensitiveFileNames2 ***REMOVED*** isFileSystemCaseSensitive();
    const fsRealpath ***REMOVED*** !!_fs.realpathSync.native ? process.platform ***REMOVED******REMOVED******REMOVED*** "win32" ? fsRealPathHandlingLongPath : _fs.realpathSync.native : _fs.realpathSync;
    const executingFilePath ***REMOVED*** __filename.endsWith("sys.js") ? _path.join(_path.dirname(__dirname), "__fake__.js") : __filename;
    const fsSupportsRecursiveFsWatch ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** "win32" || isMacOs;
    const getCurrentDirectory ***REMOVED*** memoize(() ***REMOVED***> process.cwd());
    const { watchFile: watchFile2, watchDirectory } ***REMOVED*** createSystemWatchFunctions({
      pollingWatchFileWorker: fsWatchFileWorker,
      getModifiedTime: getModifiedTime3,
      setTimeout,
      clearTimeout,
      fsWatchWorker,
      useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
      getCurrentDirectory,
      fileSystemEntryExists,
      // Node 4.0 `fs.watch` function supports the "recursive" option on both OSX and Windows
      // (ref: https://github.com/nodejs/node/pull/2649 and https://github.com/Microsoft/TypeScript/issues/4643)
      fsSupportsRecursiveFsWatch,
      getAccessibleSortedChildDirectories: (path) ***REMOVED***> getAccessibleFileSystemEntries(path).directories,
      realpath,
      tscWatchFile: process.env.TSC_WATCHFILE,
      useNonPollingWatchers: !!process.env.TSC_NONPOLLING_WATCHER,
      tscWatchDirectory: process.env.TSC_WATCHDIRECTORY,
      inodeWatching: isLinuxOrMacOs,
      fsWatchWithTimestamp: isMacOs,
      sysLog
    });
    const nodeSystem ***REMOVED*** {
      args: process.argv.slice(2),
      newLine: _os.EOL,
      useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
      write(s) {
        process.stdout.write(s);
      },
      getWidthOfTerminal() {
        return process.stdout.columns;
      },
      writeOutputIsTTY() {
        return process.stdout.isTTY;
      },
      readFile,
      writeFile: writeFile2,
      watchFile: watchFile2,
      watchDirectory,
      preferNonRecursiveWatch: !fsSupportsRecursiveFsWatch,
      resolvePath: (path) ***REMOVED***> _path.resolve(path),
      fileExists,
      directoryExists,
      getAccessibleFileSystemEntries,
      createDirectory(directoryName) {
        if (!nodeSystem.directoryExists(directoryName)) {
          try {
            _fs.mkdirSync(directoryName);
          } catch (e) {
            if (e.code !***REMOVED******REMOVED*** "EEXIST") {
              throw e;
            }
          }
        }
      },
      getExecutingFilePath() {
        return executingFilePath;
      },
      getCurrentDirectory,
      getDirectories,
      getEnvironmentVariable(name) {
        return process.env[name] || "";
      },
      readDirectory,
      getModifiedTime: getModifiedTime3,
      setModifiedTime,
      deleteFile,
      createHash: _crypto ? createSHA256Hash : generateDjb2Hash,
      createSHA256Hash: _crypto ? createSHA256Hash : void 0,
      getMemoryUsage() {
        if (global.gc) {
          global.gc();
        }
        return process.memoryUsage().heapUsed;
      },
      getFileSize(path) {
        const stat ***REMOVED*** statSync(path);
        if (stat ***REMOVED******REMOVED*** null ? void 0 : stat.isFile()) {
          return stat.size;
        }
        return 0;
      },
      exit(exitCode) {
        disableCPUProfiler(() ***REMOVED***> process.exit(exitCode));
      },
      enableCPUProfiler,
      disableCPUProfiler,
      cpuProfilingEnabled: () ***REMOVED***> !!activeSession || contains(process.execArgv, "--cpu-prof") || contains(process.execArgv, "--prof"),
      realpath,
      debugMode: !!process.env.NODE_INSPECTOR_IPC || !!process.env.VSCODE_INSPECTOR_OPTIONS || some(process.execArgv, (arg) ***REMOVED***> /^--(?:inspect|debug)(?:-brk)?(?:***REMOVED***\d+)?$/i.test(arg)) || !!process.recordreplay,
      tryEnableSourceMapsForHost() {
        try {
          require("source-map-support").install();
        } catch {
        }
      },
      setTimeout,
      clearTimeout,
      clearScreen: () ***REMOVED***> {
        process.stdout.write("\x1B[2J\x1B[3J\x1B[H");
      },
      setBlocking: () ***REMOVED***> {
        var _a;
        const handle ***REMOVED*** (_a ***REMOVED*** process.stdout) ***REMOVED******REMOVED*** null ? void 0 : _a._handle;
        if (handle && handle.setBlocking) {
          handle.setBlocking(true);
        }
      },
      base64decode: (input) ***REMOVED***> Buffer.from(input, "base64").toString("utf8"),
      base64encode: (input) ***REMOVED***> Buffer.from(input).toString("base64"),
      require: (baseDir, moduleName) ***REMOVED***> {
        try {
          const modulePath ***REMOVED*** resolveJSModule(moduleName, baseDir, nodeSystem);
          return { module: require(modulePath), modulePath, error: void 0 };
        } catch (error) {
          return { module: void 0, modulePath: void 0, error };
        }
      }
    };
    return nodeSystem;
    function statSync(path) {
      try {
        return _fs.statSync(path, statSyncOptions);
      } catch {
        return void 0;
      }
    }
    function enableCPUProfiler(path, cb) {
      if (activeSession) {
        cb();
        return false;
      }
      const inspector ***REMOVED*** require("inspector");
      if (!inspector || !inspector.Session) {
        cb();
        return false;
      }
      const session ***REMOVED*** new inspector.Session();
      session.connect();
      session.post("Profiler.enable", () ***REMOVED***> {
        session.post("Profiler.start", () ***REMOVED***> {
          activeSession ***REMOVED*** session;
          profilePath ***REMOVED*** path;
          cb();
        });
      });
      return true;
    }
    function cleanupPaths(profile) {
      let externalFileCounter ***REMOVED*** 0;
      const remappedPaths ***REMOVED*** /* @__PURE__ */ new Map();
      const normalizedDir ***REMOVED*** normalizeSlashes(_path.dirname(executingFilePath));
      const fileUrlRoot ***REMOVED*** `file://${getRootLength(normalizedDir) ***REMOVED******REMOVED******REMOVED*** 1 ? "" : "/"}${normalizedDir}`;
      for (const node of profile.nodes) {
        if (node.callFrame.url) {
          const url ***REMOVED*** normalizeSlashes(node.callFrame.url);
          if (containsPath(fileUrlRoot, url, useCaseSensitiveFileNames2)) {
            node.callFrame.url ***REMOVED*** getRelativePathToDirectoryOrUrl(
              fileUrlRoot,
              url,
              fileUrlRoot,
              createGetCanonicalFileName(useCaseSensitiveFileNames2),
              /*isAbsolutePathAnUrl*/
              true
            );
          } else if (!nativePattern.test(url)) {
            node.callFrame.url ***REMOVED*** (remappedPaths.has(url) ? remappedPaths : remappedPaths.set(url, `external${externalFileCounter}.js`)).get(url);
            externalFileCounter++;
          }
        }
      }
      return profile;
    }
    function disableCPUProfiler(cb) {
      if (activeSession && activeSession !***REMOVED******REMOVED*** "stopping") {
        const s ***REMOVED*** activeSession;
        activeSession.post("Profiler.stop", (err, { profile }) ***REMOVED***> {
          var _a;
          if (!err) {
            if ((_a ***REMOVED*** statSync(profilePath)) ***REMOVED******REMOVED*** null ? void 0 : _a.isDirectory()) {
              profilePath ***REMOVED*** _path.join(profilePath, `${(/* @__PURE__ */ new Date()).toISOString().replace(/:/g, "-")}+P${process.pid}.cpuprofile`);
            }
            try {
              _fs.mkdirSync(_path.dirname(profilePath), { recursive: true });
            } catch {
            }
            _fs.writeFileSync(profilePath, JSON.stringify(cleanupPaths(profile)));
          }
          activeSession ***REMOVED*** void 0;
          s.disconnect();
          cb();
        });
        activeSession ***REMOVED*** "stopping";
        return true;
      } else {
        cb();
        return false;
      }
    }
    function isFileSystemCaseSensitive() {
      if (platform ***REMOVED******REMOVED******REMOVED*** "win32" || platform ***REMOVED******REMOVED******REMOVED*** "win64") {
        return false;
      }
      return !fileExists(swapCase(__filename));
    }
    function swapCase(s) {
      return s.replace(/\w/g, (ch) ***REMOVED***> {
        const up ***REMOVED*** ch.toUpperCase();
        return ch ***REMOVED******REMOVED******REMOVED*** up ? ch.toLowerCase() : up;
      });
    }
    function fsWatchFileWorker(fileName, callback, pollingInterval) {
      _fs.watchFile(fileName, { persistent: true, interval: pollingInterval }, fileChanged);
      let eventKind;
      return {
        close: () ***REMOVED***> _fs.unwatchFile(fileName, fileChanged)
      };
      function fileChanged(curr, prev) {
        const isPreviouslyDeleted ***REMOVED*** +prev.mtime ***REMOVED******REMOVED******REMOVED*** 0 || eventKind ***REMOVED******REMOVED******REMOVED*** 2 /* Deleted */;
        if (+curr.mtime ***REMOVED******REMOVED******REMOVED*** 0) {
          if (isPreviouslyDeleted) {
            return;
          }
          eventKind ***REMOVED*** 2 /* Deleted */;
        } else if (isPreviouslyDeleted) {
          eventKind ***REMOVED*** 0 /* Created */;
        } else if (+curr.mtime ***REMOVED******REMOVED******REMOVED*** +prev.mtime) {
          return;
        } else {
          eventKind ***REMOVED*** 1 /* Changed */;
        }
        callback(fileName, eventKind, curr.mtime);
      }
    }
    function fsWatchWorker(fileOrDirectory, recursive, callback) {
      return _fs.watch(
        fileOrDirectory,
        fsSupportsRecursiveFsWatch ? { persistent: true, recursive: !!recursive } : { persistent: true },
        callback
      );
    }
    function readFile(fileName, _encoding) {
      let buffer;
      try {
        buffer ***REMOVED*** _fs.readFileSync(fileName);
      } catch {
        return void 0;
      }
      let len ***REMOVED*** buffer.length;
      if (len >***REMOVED*** 2 && buffer[0] ***REMOVED******REMOVED******REMOVED*** 254 && buffer[1] ***REMOVED******REMOVED******REMOVED*** 255) {
        len &***REMOVED*** ~1;
        for (let i ***REMOVED*** 0; i < len; i +***REMOVED*** 2) {
          const temp ***REMOVED*** buffer[i];
          buffer[i] ***REMOVED*** buffer[i + 1];
          buffer[i + 1] ***REMOVED*** temp;
        }
        return buffer.toString("utf16le", 2);
      }
      if (len >***REMOVED*** 2 && buffer[0] ***REMOVED******REMOVED******REMOVED*** 255 && buffer[1] ***REMOVED******REMOVED******REMOVED*** 254) {
        return buffer.toString("utf16le", 2);
      }
      if (len >***REMOVED*** 3 && buffer[0] ***REMOVED******REMOVED******REMOVED*** 239 && buffer[1] ***REMOVED******REMOVED******REMOVED*** 187 && buffer[2] ***REMOVED******REMOVED******REMOVED*** 191) {
        return buffer.toString("utf8", 3);
      }
      return buffer.toString("utf8");
    }
    function writeFile2(fileName, data, writeByteOrderMark) {
      if (writeByteOrderMark) {
        data ***REMOVED*** byteOrderMarkIndicator + data;
      }
      let fd;
      try {
        fd ***REMOVED*** _fs.openSync(fileName, "w");
        _fs.writeSync(
          fd,
          data,
          /*position*/
          void 0,
          "utf8"
        );
      } finally {
        if (fd !***REMOVED******REMOVED*** void 0) {
          _fs.closeSync(fd);
        }
      }
    }
    function getAccessibleFileSystemEntries(path) {
      try {
        const entries ***REMOVED*** _fs.readdirSync(path || ".", { withFileTypes: true });
        const files ***REMOVED*** [];
        const directories ***REMOVED*** [];
        for (const dirent of entries) {
          const entry ***REMOVED*** typeof dirent ***REMOVED******REMOVED******REMOVED*** "string" ? dirent : dirent.name;
          if (entry ***REMOVED******REMOVED******REMOVED*** "." || entry ***REMOVED******REMOVED******REMOVED*** "..") {
            continue;
          }
          let stat;
          if (typeof dirent ***REMOVED******REMOVED******REMOVED*** "string" || dirent.isSymbolicLink()) {
            const name ***REMOVED*** combinePaths(path, entry);
            stat ***REMOVED*** statSync(name);
            if (!stat) {
              continue;
            }
          } else {
            stat ***REMOVED*** dirent;
          }
          if (stat.isFile()) {
            files.push(entry);
          } else if (stat.isDirectory()) {
            directories.push(entry);
          }
        }
        files.sort();
        directories.sort();
        return { files, directories };
      } catch {
        return emptyFileSystemEntries;
      }
    }
    function readDirectory(path, extensions, excludes, includes, depth) {
      return matchFiles(path, extensions, excludes, includes, useCaseSensitiveFileNames2, process.cwd(), depth, getAccessibleFileSystemEntries, realpath);
    }
    function fileSystemEntryExists(path, entryKind) {
      const stat ***REMOVED*** statSync(path);
      if (!stat) {
        return false;
      }
      switch (entryKind) {
        case 0 /* File */:
          return stat.isFile();
        case 1 /* Directory */:
          return stat.isDirectory();
        default:
          return false;
      }
    }
    function fileExists(path) {
      return fileSystemEntryExists(path, 0 /* File */);
    }
    function directoryExists(path) {
      return fileSystemEntryExists(path, 1 /* Directory */);
    }
    function getDirectories(path) {
      return getAccessibleFileSystemEntries(path).directories.slice();
    }
    function fsRealPathHandlingLongPath(path) {
      return path.length < 260 ? _fs.realpathSync.native(path) : _fs.realpathSync(path);
    }
    function realpath(path) {
      try {
        return fsRealpath(path);
      } catch {
        return path;
      }
    }
    function getModifiedTime3(path) {
      var _a;
      return (_a ***REMOVED*** statSync(path)) ***REMOVED******REMOVED*** null ? void 0 : _a.mtime;
    }
    function setModifiedTime(path, time) {
      try {
        _fs.utimesSync(path, time, time);
      } catch {
        return;
      }
    }
    function deleteFile(path) {
      try {
        return _fs.unlinkSync(path);
      } catch {
        return;
      }
    }
    function createSHA256Hash(data) {
      const hash ***REMOVED*** _crypto.createHash("sha256");
      hash.update(data);
      return hash.digest("hex");
    }
  }
  let sys2;
  if (isNodeLikeSystem()) {
    sys2 ***REMOVED*** getNodeSystem();
  }
  if (sys2) {
    patchWriteFileEnsuringDirectory(sys2);
  }
  return sys2;
})();
if (sys && sys.getEnvironmentVariable) {
  setCustomPollingValues(sys);
  Debug.setAssertionLevel(
    /^development$/i.test(sys.getEnvironmentVariable("NODE_ENV")) ? 1 /* Normal */ : 0 /* None */
  );
}
if (sys && sys.debugMode) {
  Debug.isDebugging ***REMOVED*** true;
}

// src/compiler/path.ts
var directorySeparator ***REMOVED*** "/";
var altDirectorySeparator ***REMOVED*** "\\";
var urlSchemeSeparator ***REMOVED*** "://";
var backslashRegExp ***REMOVED*** /\\/g;
function isAnyDirectorySeparator(charCode) {
  return charCode ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ || charCode ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */;
}
function isRootedDiskPath(path) {
  return getEncodedRootLength(path) > 0;
}
function isDiskPathRoot(path) {
  const rootLength ***REMOVED*** getEncodedRootLength(path);
  return rootLength > 0 && rootLength ***REMOVED******REMOVED******REMOVED*** path.length;
}
function pathIsAbsolute(path) {
  return getEncodedRootLength(path) !***REMOVED******REMOVED*** 0;
}
function pathIsRelative(path) {
  return /^\.\.?(?:$|[\\/])/.test(path);
}
function pathIsBareSpecifier(path) {
  return !pathIsAbsolute(path) && !pathIsRelative(path);
}
function hasExtension(fileName) {
  return getBaseFileName(fileName).includes(".");
}
function fileExtensionIs(path, extension) {
  return path.length > extension.length && endsWith(path, extension);
}
function fileExtensionIsOneOf(path, extensions) {
  for (const extension of extensions) {
    if (fileExtensionIs(path, extension)) {
      return true;
    }
  }
  return false;
}
function hasTrailingDirectorySeparator(path) {
  return path.length > 0 && isAnyDirectorySeparator(path.charCodeAt(path.length - 1));
}
function isVolumeCharacter(charCode) {
  return charCode >***REMOVED*** 97 /* a */ && charCode <***REMOVED*** 122 /* z */ || charCode >***REMOVED*** 65 /* A */ && charCode <***REMOVED*** 90 /* Z */;
}
function getFileUrlVolumeSeparatorEnd(url, start) {
  const ch0 ***REMOVED*** url.charCodeAt(start);
  if (ch0 ***REMOVED******REMOVED******REMOVED*** 58 /* colon */) return start + 1;
  if (ch0 ***REMOVED******REMOVED******REMOVED*** 37 /* percent */ && url.charCodeAt(start + 1) ***REMOVED******REMOVED******REMOVED*** 51 /* _3 */) {
    const ch2 ***REMOVED*** url.charCodeAt(start + 2);
    if (ch2 ***REMOVED******REMOVED******REMOVED*** 97 /* a */ || ch2 ***REMOVED******REMOVED******REMOVED*** 65 /* A */) return start + 3;
  }
  return -1;
}
function getEncodedRootLength(path) {
  if (!path) return 0;
  const ch0 ***REMOVED*** path.charCodeAt(0);
  if (ch0 ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ || ch0 ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
    if (path.charCodeAt(1) !***REMOVED******REMOVED*** ch0) return 1;
    const p1 ***REMOVED*** path.indexOf(ch0 ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ ? directorySeparator : altDirectorySeparator, 2);
    if (p1 < 0) return path.length;
    return p1 + 1;
  }
  if (isVolumeCharacter(ch0) && path.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 58 /* colon */) {
    const ch2 ***REMOVED*** path.charCodeAt(2);
    if (ch2 ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ || ch2 ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) return 3;
    if (path.length ***REMOVED******REMOVED******REMOVED*** 2) return 2;
  }
  const schemeEnd ***REMOVED*** path.indexOf(urlSchemeSeparator);
  if (schemeEnd !***REMOVED******REMOVED*** -1) {
    const authorityStart ***REMOVED*** schemeEnd + urlSchemeSeparator.length;
    const authorityEnd ***REMOVED*** path.indexOf(directorySeparator, authorityStart);
    if (authorityEnd !***REMOVED******REMOVED*** -1) {
      const scheme ***REMOVED*** path.slice(0, schemeEnd);
      const authority ***REMOVED*** path.slice(authorityStart, authorityEnd);
      if (scheme ***REMOVED******REMOVED******REMOVED*** "file" && (authority ***REMOVED******REMOVED******REMOVED*** "" || authority ***REMOVED******REMOVED******REMOVED*** "localhost") && isVolumeCharacter(path.charCodeAt(authorityEnd + 1))) {
        const volumeSeparatorEnd ***REMOVED*** getFileUrlVolumeSeparatorEnd(path, authorityEnd + 2);
        if (volumeSeparatorEnd !***REMOVED******REMOVED*** -1) {
          if (path.charCodeAt(volumeSeparatorEnd) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
            return ~(volumeSeparatorEnd + 1);
          }
          if (volumeSeparatorEnd ***REMOVED******REMOVED******REMOVED*** path.length) {
            return ~volumeSeparatorEnd;
          }
        }
      }
      return ~(authorityEnd + 1);
    }
    return ~path.length;
  }
  return 0;
}
function getRootLength(path) {
  const rootLength ***REMOVED*** getEncodedRootLength(path);
  return rootLength < 0 ? ~rootLength : rootLength;
}
function getDirectoryPath(path) {
  path ***REMOVED*** normalizeSlashes(path);
  const rootLength ***REMOVED*** getRootLength(path);
  if (rootLength ***REMOVED******REMOVED******REMOVED*** path.length) return path;
  path ***REMOVED*** removeTrailingDirectorySeparator(path);
  return path.slice(0, Math.max(rootLength, path.lastIndexOf(directorySeparator)));
}
function getBaseFileName(path, extensions, ignoreCase) {
  path ***REMOVED*** normalizeSlashes(path);
  const rootLength ***REMOVED*** getRootLength(path);
  if (rootLength ***REMOVED******REMOVED******REMOVED*** path.length) return "";
  path ***REMOVED*** removeTrailingDirectorySeparator(path);
  const name ***REMOVED*** path.slice(Math.max(getRootLength(path), path.lastIndexOf(directorySeparator) + 1));
  const extension ***REMOVED*** extensions !***REMOVED******REMOVED*** void 0 && ignoreCase !***REMOVED******REMOVED*** void 0 ? getAnyExtensionFromPath(name, extensions, ignoreCase) : void 0;
  return extension ? name.slice(0, name.length - extension.length) : name;
}
function tryGetExtensionFromPath(path, extension, stringEqualityComparer) {
  if (!startsWith(extension, ".")) extension ***REMOVED*** "." + extension;
  if (path.length >***REMOVED*** extension.length && path.charCodeAt(path.length - extension.length) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */) {
    const pathExtension ***REMOVED*** path.slice(path.length - extension.length);
    if (stringEqualityComparer(pathExtension, extension)) {
      return pathExtension;
    }
  }
}
function getAnyExtensionFromPathWorker(path, extensions, stringEqualityComparer) {
  if (typeof extensions ***REMOVED******REMOVED******REMOVED*** "string") {
    return tryGetExtensionFromPath(path, extensions, stringEqualityComparer) || "";
  }
  for (const extension of extensions) {
    const result ***REMOVED*** tryGetExtensionFromPath(path, extension, stringEqualityComparer);
    if (result) return result;
  }
  return "";
}
function getAnyExtensionFromPath(path, extensions, ignoreCase) {
  if (extensions) {
    return getAnyExtensionFromPathWorker(removeTrailingDirectorySeparator(path), extensions, ignoreCase ? equateStringsCaseInsensitive : equateStringsCaseSensitive);
  }
  const baseFileName ***REMOVED*** getBaseFileName(path);
  const extensionIndex ***REMOVED*** baseFileName.lastIndexOf(".");
  if (extensionIndex >***REMOVED*** 0) {
    return baseFileName.substring(extensionIndex);
  }
  return "";
}
function pathComponents(path, rootLength) {
  const root ***REMOVED*** path.substring(0, rootLength);
  const rest ***REMOVED*** path.substring(rootLength).split(directorySeparator);
  if (rest.length && !lastOrUndefined(rest)) rest.pop();
  return [root, ...rest];
}
function getPathComponents(path, currentDirectory ***REMOVED*** "") {
  path ***REMOVED*** combinePaths(currentDirectory, path);
  return pathComponents(path, getRootLength(path));
}
function getPathFromPathComponents(pathComponents2, length2) {
  if (pathComponents2.length ***REMOVED******REMOVED******REMOVED*** 0) return "";
  const root ***REMOVED*** pathComponents2[0] && ensureTrailingDirectorySeparator(pathComponents2[0]);
  return root + pathComponents2.slice(1, length2).join(directorySeparator);
}
function normalizeSlashes(path) {
  return path.includes("\\") ? path.replace(backslashRegExp, directorySeparator) : path;
}
function reducePathComponents(components) {
  if (!some(components)) return [];
  const reduced ***REMOVED*** [components[0]];
  for (let i ***REMOVED*** 1; i < components.length; i++) {
    const component ***REMOVED*** components[i];
    if (!component) continue;
    if (component ***REMOVED******REMOVED******REMOVED*** ".") continue;
    if (component ***REMOVED******REMOVED******REMOVED*** "..") {
      if (reduced.length > 1) {
        if (reduced[reduced.length - 1] !***REMOVED******REMOVED*** "..") {
          reduced.pop();
          continue;
        }
      } else if (reduced[0]) continue;
    }
    reduced.push(component);
  }
  return reduced;
}
function combinePaths(path, ...paths) {
  if (path) path ***REMOVED*** normalizeSlashes(path);
  for (let relativePath of paths) {
    if (!relativePath) continue;
    relativePath ***REMOVED*** normalizeSlashes(relativePath);
    if (!path || getRootLength(relativePath) !***REMOVED******REMOVED*** 0) {
      path ***REMOVED*** relativePath;
    } else {
      path ***REMOVED*** ensureTrailingDirectorySeparator(path) + relativePath;
    }
  }
  return path;
}
function resolvePath(path, ...paths) {
  return normalizePath(some(paths) ? combinePaths(path, ...paths) : normalizeSlashes(path));
}
function getNormalizedPathComponents(path, currentDirectory) {
  return reducePathComponents(getPathComponents(path, currentDirectory));
}
function getNormalizedAbsolutePath(path, currentDirectory) {
  let rootLength ***REMOVED*** getRootLength(path);
  if (rootLength ***REMOVED******REMOVED******REMOVED*** 0 && currentDirectory) {
    path ***REMOVED*** combinePaths(currentDirectory, path);
    rootLength ***REMOVED*** getRootLength(path);
  } else {
    path ***REMOVED*** normalizeSlashes(path);
  }
  const simpleNormalized ***REMOVED*** simpleNormalizePath(path);
  if (simpleNormalized !***REMOVED******REMOVED*** void 0) {
    return simpleNormalized.length > rootLength ? removeTrailingDirectorySeparator(simpleNormalized) : simpleNormalized;
  }
  const length2 ***REMOVED*** path.length;
  const root ***REMOVED*** path.substring(0, rootLength);
  let normalized;
  let index ***REMOVED*** rootLength;
  let segmentStart ***REMOVED*** index;
  let normalizedUpTo ***REMOVED*** index;
  let seenNonDotDotSegment ***REMOVED*** rootLength !***REMOVED******REMOVED*** 0;
  while (index < length2) {
    segmentStart ***REMOVED*** index;
    let ch ***REMOVED*** path.charCodeAt(index);
    while (ch ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ && index + 1 < length2) {
      index++;
      ch ***REMOVED*** path.charCodeAt(index);
    }
    if (index > segmentStart) {
      normalized ?? (normalized ***REMOVED*** path.substring(0, segmentStart - 1));
      segmentStart ***REMOVED*** index;
    }
    let segmentEnd ***REMOVED*** path.indexOf(directorySeparator, index + 1);
    if (segmentEnd ***REMOVED******REMOVED******REMOVED*** -1) {
      segmentEnd ***REMOVED*** length2;
    }
    const segmentLength ***REMOVED*** segmentEnd - segmentStart;
    if (segmentLength ***REMOVED******REMOVED******REMOVED*** 1 && path.charCodeAt(index) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */) {
      normalized ?? (normalized ***REMOVED*** path.substring(0, normalizedUpTo));
    } else if (segmentLength ***REMOVED******REMOVED******REMOVED*** 2 && path.charCodeAt(index) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */ && path.charCodeAt(index + 1) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */) {
      if (!seenNonDotDotSegment) {
        if (normalized !***REMOVED******REMOVED*** void 0) {
          normalized +***REMOVED*** normalized.length ***REMOVED******REMOVED******REMOVED*** rootLength ? ".." : "/..";
        } else {
          normalizedUpTo ***REMOVED*** index + 2;
        }
      } else if (normalized ***REMOVED******REMOVED******REMOVED*** void 0) {
        if (normalizedUpTo - 2 >***REMOVED*** 0) {
          normalized ***REMOVED*** path.substring(0, Math.max(rootLength, path.lastIndexOf(directorySeparator, normalizedUpTo - 2)));
        } else {
          normalized ***REMOVED*** path.substring(0, normalizedUpTo);
        }
      } else {
        const lastSlash ***REMOVED*** normalized.lastIndexOf(directorySeparator);
        if (lastSlash !***REMOVED******REMOVED*** -1) {
          normalized ***REMOVED*** normalized.substring(0, Math.max(rootLength, lastSlash));
        } else {
          normalized ***REMOVED*** root;
        }
        if (normalized.length ***REMOVED******REMOVED******REMOVED*** rootLength) {
          seenNonDotDotSegment ***REMOVED*** rootLength !***REMOVED******REMOVED*** 0;
        }
      }
    } else if (normalized !***REMOVED******REMOVED*** void 0) {
      if (normalized.length !***REMOVED******REMOVED*** rootLength) {
        normalized +***REMOVED*** directorySeparator;
      }
      seenNonDotDotSegment ***REMOVED*** true;
      normalized +***REMOVED*** path.substring(segmentStart, segmentEnd);
    } else {
      seenNonDotDotSegment ***REMOVED*** true;
      normalizedUpTo ***REMOVED*** segmentEnd;
    }
    index ***REMOVED*** segmentEnd + 1;
  }
  return normalized ?? (length2 > rootLength ? removeTrailingDirectorySeparator(path) : path);
}
function normalizePath(path) {
  path ***REMOVED*** normalizeSlashes(path);
  let normalized ***REMOVED*** simpleNormalizePath(path);
  if (normalized !***REMOVED******REMOVED*** void 0) {
    return normalized;
  }
  normalized ***REMOVED*** getNormalizedAbsolutePath(path, "");
  return normalized && hasTrailingDirectorySeparator(path) ? ensureTrailingDirectorySeparator(normalized) : normalized;
}
function simpleNormalizePath(path) {
  if (!relativePathSegmentRegExp.test(path)) {
    return path;
  }
  let simplified ***REMOVED*** path.replace(/\/\.\//g, "/");
  if (simplified.startsWith("./")) {
    simplified ***REMOVED*** simplified.slice(2);
  }
  if (simplified !***REMOVED******REMOVED*** path) {
    path ***REMOVED*** simplified;
    if (!relativePathSegmentRegExp.test(path)) {
      return path;
    }
  }
  return void 0;
}
function getPathWithoutRoot(pathComponents2) {
  if (pathComponents2.length ***REMOVED******REMOVED******REMOVED*** 0) return "";
  return pathComponents2.slice(1).join(directorySeparator);
}
function getNormalizedAbsolutePathWithoutRoot(fileName, currentDirectory) {
  return getPathWithoutRoot(getNormalizedPathComponents(fileName, currentDirectory));
}
function toPath(fileName, basePath, getCanonicalFileName) {
  const nonCanonicalizedPath ***REMOVED*** isRootedDiskPath(fileName) ? normalizePath(fileName) : getNormalizedAbsolutePath(fileName, basePath);
  return getCanonicalFileName(nonCanonicalizedPath);
}
function removeTrailingDirectorySeparator(path) {
  if (hasTrailingDirectorySeparator(path)) {
    return path.substr(0, path.length - 1);
  }
  return path;
}
function ensureTrailingDirectorySeparator(path) {
  if (!hasTrailingDirectorySeparator(path)) {
    return path + directorySeparator;
  }
  return path;
}
function ensurePathIsNonModuleName(path) {
  return !pathIsAbsolute(path) && !pathIsRelative(path) ? "./" + path : path;
}
function changeAnyExtension(path, ext, extensions, ignoreCase) {
  const pathext ***REMOVED*** extensions !***REMOVED******REMOVED*** void 0 && ignoreCase !***REMOVED******REMOVED*** void 0 ? getAnyExtensionFromPath(path, extensions, ignoreCase) : getAnyExtensionFromPath(path);
  return pathext ? path.slice(0, path.length - pathext.length) + (startsWith(ext, ".") ? ext : "." + ext) : path;
}
function changeFullExtension(path, newExtension) {
  const declarationExtension ***REMOVED*** getDeclarationFileExtension(path);
  if (declarationExtension) {
    return path.slice(0, path.length - declarationExtension.length) + (startsWith(newExtension, ".") ? newExtension : "." + newExtension);
  }
  return changeAnyExtension(path, newExtension);
}
var relativePathSegmentRegExp ***REMOVED*** /\/\/|(?:^|\/)\.\.?(?:$|\/)/;
function comparePathsWorker(a, b, componentComparer) {
  if (a ***REMOVED******REMOVED******REMOVED*** b) return 0 /* EqualTo */;
  if (a ***REMOVED******REMOVED******REMOVED*** void 0) return -1 /* LessThan */;
  if (b ***REMOVED******REMOVED******REMOVED*** void 0) return 1 /* GreaterThan */;
  const aRoot ***REMOVED*** a.substring(0, getRootLength(a));
  const bRoot ***REMOVED*** b.substring(0, getRootLength(b));
  const result ***REMOVED*** compareStringsCaseInsensitive(aRoot, bRoot);
  if (result !***REMOVED******REMOVED*** 0 /* EqualTo */) {
    return result;
  }
  const aRest ***REMOVED*** a.substring(aRoot.length);
  const bRest ***REMOVED*** b.substring(bRoot.length);
  if (!relativePathSegmentRegExp.test(aRest) && !relativePathSegmentRegExp.test(bRest)) {
    return componentComparer(aRest, bRest);
  }
  const aComponents ***REMOVED*** reducePathComponents(getPathComponents(a));
  const bComponents ***REMOVED*** reducePathComponents(getPathComponents(b));
  const sharedLength ***REMOVED*** Math.min(aComponents.length, bComponents.length);
  for (let i ***REMOVED*** 1; i < sharedLength; i++) {
    const result2 ***REMOVED*** componentComparer(aComponents[i], bComponents[i]);
    if (result2 !***REMOVED******REMOVED*** 0 /* EqualTo */) {
      return result2;
    }
  }
  return compareValues(aComponents.length, bComponents.length);
}
function comparePaths(a, b, currentDirectory, ignoreCase) {
  if (typeof currentDirectory ***REMOVED******REMOVED******REMOVED*** "string") {
    a ***REMOVED*** combinePaths(currentDirectory, a);
    b ***REMOVED*** combinePaths(currentDirectory, b);
  } else if (typeof currentDirectory ***REMOVED******REMOVED******REMOVED*** "boolean") {
    ignoreCase ***REMOVED*** currentDirectory;
  }
  return comparePathsWorker(a, b, getStringComparer(ignoreCase));
}
function containsPath(parent, child, currentDirectory, ignoreCase) {
  if (typeof currentDirectory ***REMOVED******REMOVED******REMOVED*** "string") {
    parent ***REMOVED*** combinePaths(currentDirectory, parent);
    child ***REMOVED*** combinePaths(currentDirectory, child);
  } else if (typeof currentDirectory ***REMOVED******REMOVED******REMOVED*** "boolean") {
    ignoreCase ***REMOVED*** currentDirectory;
  }
  if (parent ***REMOVED******REMOVED******REMOVED*** void 0 || child ***REMOVED******REMOVED******REMOVED*** void 0) return false;
  if (parent ***REMOVED******REMOVED******REMOVED*** child) return true;
  const parentComponents ***REMOVED*** reducePathComponents(getPathComponents(parent));
  const childComponents ***REMOVED*** reducePathComponents(getPathComponents(child));
  if (childComponents.length < parentComponents.length) {
    return false;
  }
  const componentEqualityComparer ***REMOVED*** ignoreCase ? equateStringsCaseInsensitive : equateStringsCaseSensitive;
  for (let i ***REMOVED*** 0; i < parentComponents.length; i++) {
    const equalityComparer ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** 0 ? equateStringsCaseInsensitive : componentEqualityComparer;
    if (!equalityComparer(parentComponents[i], childComponents[i])) {
      return false;
    }
  }
  return true;
}
function startsWithDirectory(fileName, directoryName, getCanonicalFileName) {
  const canonicalFileName ***REMOVED*** getCanonicalFileName(fileName);
  const canonicalDirectoryName ***REMOVED*** getCanonicalFileName(directoryName);
  return startsWith(canonicalFileName, canonicalDirectoryName + "/") || startsWith(canonicalFileName, canonicalDirectoryName + "\\");
}
function getPathComponentsRelativeTo(from, to, stringEqualityComparer, getCanonicalFileName) {
  const fromComponents ***REMOVED*** reducePathComponents(getPathComponents(from));
  const toComponents ***REMOVED*** reducePathComponents(getPathComponents(to));
  let start;
  for (start ***REMOVED*** 0; start < fromComponents.length && start < toComponents.length; start++) {
    const fromComponent ***REMOVED*** getCanonicalFileName(fromComponents[start]);
    const toComponent ***REMOVED*** getCanonicalFileName(toComponents[start]);
    const comparer ***REMOVED*** start ***REMOVED******REMOVED******REMOVED*** 0 ? equateStringsCaseInsensitive : stringEqualityComparer;
    if (!comparer(fromComponent, toComponent)) break;
  }
  if (start ***REMOVED******REMOVED******REMOVED*** 0) {
    return toComponents;
  }
  const components ***REMOVED*** toComponents.slice(start);
  const relative ***REMOVED*** [];
  for (; start < fromComponents.length; start++) {
    relative.push("..");
  }
  return ["", ...relative, ...components];
}
function getRelativePathFromDirectory(fromDirectory, to, getCanonicalFileNameOrIgnoreCase) {
  Debug.assert(getRootLength(fromDirectory) > 0 ***REMOVED******REMOVED******REMOVED*** getRootLength(to) > 0, "Paths must either both be absolute or both be relative");
  const getCanonicalFileName ***REMOVED*** typeof getCanonicalFileNameOrIgnoreCase ***REMOVED******REMOVED******REMOVED*** "function" ? getCanonicalFileNameOrIgnoreCase : identity;
  const ignoreCase ***REMOVED*** typeof getCanonicalFileNameOrIgnoreCase ***REMOVED******REMOVED******REMOVED*** "boolean" ? getCanonicalFileNameOrIgnoreCase : false;
  const pathComponents2 ***REMOVED*** getPathComponentsRelativeTo(fromDirectory, to, ignoreCase ? equateStringsCaseInsensitive : equateStringsCaseSensitive, getCanonicalFileName);
  return getPathFromPathComponents(pathComponents2);
}
function convertToRelativePath(absoluteOrRelativePath, basePath, getCanonicalFileName) {
  return !isRootedDiskPath(absoluteOrRelativePath) ? absoluteOrRelativePath : getRelativePathToDirectoryOrUrl(
    basePath,
    absoluteOrRelativePath,
    basePath,
    getCanonicalFileName,
    /*isAbsolutePathAnUrl*/
    false
  );
}
function getRelativePathFromFile(from, to, getCanonicalFileName) {
  return ensurePathIsNonModuleName(getRelativePathFromDirectory(getDirectoryPath(from), to, getCanonicalFileName));
}
function getRelativePathToDirectoryOrUrl(directoryPathOrUrl, relativeOrAbsolutePath, currentDirectory, getCanonicalFileName, isAbsolutePathAnUrl) {
  const pathComponents2 ***REMOVED*** getPathComponentsRelativeTo(
    resolvePath(currentDirectory, directoryPathOrUrl),
    resolvePath(currentDirectory, relativeOrAbsolutePath),
    equateStringsCaseSensitive,
    getCanonicalFileName
  );
  const firstComponent ***REMOVED*** pathComponents2[0];
  if (isAbsolutePathAnUrl && isRootedDiskPath(firstComponent)) {
    const prefix ***REMOVED*** firstComponent.charAt(0) ***REMOVED******REMOVED******REMOVED*** directorySeparator ? "file://" : "file:///";
    pathComponents2[0] ***REMOVED*** prefix + firstComponent;
  }
  return getPathFromPathComponents(pathComponents2);
}
function forEachAncestorDirectory(directory, callback) {
  while (true) {
    const result ***REMOVED*** callback(directory);
    if (result !***REMOVED******REMOVED*** void 0) {
      return result;
    }
    const parentPath ***REMOVED*** getDirectoryPath(directory);
    if (parentPath ***REMOVED******REMOVED******REMOVED*** directory) {
      return void 0;
    }
    directory ***REMOVED*** parentPath;
  }
}
function isNodeModulesDirectory(dirPath) {
  return endsWith(dirPath, "/node_modules");
}

// src/compiler/diagnosticInformationMap.generated.ts
function diag(code, category, key, message, reportsUnnecessary, elidedInCompatabilityPyramid, reportsDeprecated) {
  return { code, category, key, message, reportsUnnecessary, elidedInCompatabilityPyramid, reportsDeprecated };
}
var Diagnostics ***REMOVED*** {
  Unterminated_string_literal: diag(1002, 1 /* Error */, "Unterminated_string_literal_1002", "Unterminated string literal."),
  Identifier_expected: diag(1003, 1 /* Error */, "Identifier_expected_1003", "Identifier expected."),
  _0_expected: diag(1005, 1 /* Error */, "_0_expected_1005", "'{0}' expected."),
  A_file_cannot_have_a_reference_to_itself: diag(1006, 1 /* Error */, "A_file_cannot_have_a_reference_to_itself_1006", "A file cannot have a reference to itself."),
  The_parser_expected_to_find_a_1_to_match_the_0_token_here: diag(1007, 1 /* Error */, "The_parser_expected_to_find_a_1_to_match_the_0_token_here_1007", "The parser expected to find a '{1}' to match the '{0}' token here."),
  Trailing_comma_not_allowed: diag(1009, 1 /* Error */, "Trailing_comma_not_allowed_1009", "Trailing comma not allowed."),
  Asterisk_Slash_expected: diag(1010, 1 /* Error */, "Asterisk_Slash_expected_1010", "'*/' expected."),
  An_element_access_expression_should_take_an_argument: diag(1011, 1 /* Error */, "An_element_access_expression_should_take_an_argument_1011", "An element access expression should take an argument."),
  Unexpected_token: diag(1012, 1 /* Error */, "Unexpected_token_1012", "Unexpected token."),
  A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma: diag(1013, 1 /* Error */, "A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma_1013", "A rest parameter or binding pattern may not have a trailing comma."),
  A_rest_parameter_must_be_last_in_a_parameter_list: diag(1014, 1 /* Error */, "A_rest_parameter_must_be_last_in_a_parameter_list_1014", "A rest parameter must be last in a parameter list."),
  Parameter_cannot_have_question_mark_and_initializer: diag(1015, 1 /* Error */, "Parameter_cannot_have_question_mark_and_initializer_1015", "Parameter cannot have question mark and initializer."),
  A_required_parameter_cannot_follow_an_optional_parameter: diag(1016, 1 /* Error */, "A_required_parameter_cannot_follow_an_optional_parameter_1016", "A required parameter cannot follow an optional parameter."),
  An_index_signature_cannot_have_a_rest_parameter: diag(1017, 1 /* Error */, "An_index_signature_cannot_have_a_rest_parameter_1017", "An index signature cannot have a rest parameter."),
  An_index_signature_parameter_cannot_have_an_accessibility_modifier: diag(1018, 1 /* Error */, "An_index_signature_parameter_cannot_have_an_accessibility_modifier_1018", "An index signature parameter cannot have an accessibility modifier."),
  An_index_signature_parameter_cannot_have_a_question_mark: diag(1019, 1 /* Error */, "An_index_signature_parameter_cannot_have_a_question_mark_1019", "An index signature parameter cannot have a question mark."),
  An_index_signature_parameter_cannot_have_an_initializer: diag(1020, 1 /* Error */, "An_index_signature_parameter_cannot_have_an_initializer_1020", "An index signature parameter cannot have an initializer."),
  An_index_signature_must_have_a_type_annotation: diag(1021, 1 /* Error */, "An_index_signature_must_have_a_type_annotation_1021", "An index signature must have a type annotation."),
  An_index_signature_parameter_must_have_a_type_annotation: diag(1022, 1 /* Error */, "An_index_signature_parameter_must_have_a_type_annotation_1022", "An index signature parameter must have a type annotation."),
  readonly_modifier_can_only_appear_on_a_property_declaration_or_index_signature: diag(1024, 1 /* Error */, "readonly_modifier_can_only_appear_on_a_property_declaration_or_index_signature_1024", "'readonly' modifier can only appear on a property declaration or index signature."),
  An_index_signature_cannot_have_a_trailing_comma: diag(1025, 1 /* Error */, "An_index_signature_cannot_have_a_trailing_comma_1025", "An index signature cannot have a trailing comma."),
  Accessibility_modifier_already_seen: diag(1028, 1 /* Error */, "Accessibility_modifier_already_seen_1028", "Accessibility modifier already seen."),
  _0_modifier_must_precede_1_modifier: diag(1029, 1 /* Error */, "_0_modifier_must_precede_1_modifier_1029", "'{0}' modifier must precede '{1}' modifier."),
  _0_modifier_already_seen: diag(1030, 1 /* Error */, "_0_modifier_already_seen_1030", "'{0}' modifier already seen."),
  _0_modifier_cannot_appear_on_class_elements_of_this_kind: diag(1031, 1 /* Error */, "_0_modifier_cannot_appear_on_class_elements_of_this_kind_1031", "'{0}' modifier cannot appear on class elements of this kind."),
  super_must_be_followed_by_an_argument_list_or_member_access: diag(1034, 1 /* Error */, "super_must_be_followed_by_an_argument_list_or_member_access_1034", "'super' must be followed by an argument list or member access."),
  Only_ambient_modules_can_use_quoted_names: diag(1035, 1 /* Error */, "Only_ambient_modules_can_use_quoted_names_1035", "Only ambient modules can use quoted names."),
  Statements_are_not_allowed_in_ambient_contexts: diag(1036, 1 /* Error */, "Statements_are_not_allowed_in_ambient_contexts_1036", "Statements are not allowed in ambient contexts."),
  A_declare_modifier_cannot_be_used_in_an_already_ambient_context: diag(1038, 1 /* Error */, "A_declare_modifier_cannot_be_used_in_an_already_ambient_context_1038", "A 'declare' modifier cannot be used in an already ambient context."),
  Initializers_are_not_allowed_in_ambient_contexts: diag(1039, 1 /* Error */, "Initializers_are_not_allowed_in_ambient_contexts_1039", "Initializers are not allowed in ambient contexts."),
  _0_modifier_cannot_be_used_in_an_ambient_context: diag(1040, 1 /* Error */, "_0_modifier_cannot_be_used_in_an_ambient_context_1040", "'{0}' modifier cannot be used in an ambient context."),
  _0_modifier_cannot_be_used_here: diag(1042, 1 /* Error */, "_0_modifier_cannot_be_used_here_1042", "'{0}' modifier cannot be used here."),
  _0_modifier_cannot_appear_on_a_module_or_namespace_element: diag(1044, 1 /* Error */, "_0_modifier_cannot_appear_on_a_module_or_namespace_element_1044", "'{0}' modifier cannot appear on a module or namespace element."),
  Top_level_declarations_in_d_ts_files_must_start_with_either_a_declare_or_export_modifier: diag(1046, 1 /* Error */, "Top_level_declarations_in_d_ts_files_must_start_with_either_a_declare_or_export_modifier_1046", "Top-level declarations in .d.ts files must start with either a 'declare' or 'export' modifier."),
  A_rest_parameter_cannot_be_optional: diag(1047, 1 /* Error */, "A_rest_parameter_cannot_be_optional_1047", "A rest parameter cannot be optional."),
  A_rest_parameter_cannot_have_an_initializer: diag(1048, 1 /* Error */, "A_rest_parameter_cannot_have_an_initializer_1048", "A rest parameter cannot have an initializer."),
  A_set_accessor_must_have_exactly_one_parameter: diag(1049, 1 /* Error */, "A_set_accessor_must_have_exactly_one_parameter_1049", "A 'set' accessor must have exactly one parameter."),
  A_set_accessor_cannot_have_an_optional_parameter: diag(1051, 1 /* Error */, "A_set_accessor_cannot_have_an_optional_parameter_1051", "A 'set' accessor cannot have an optional parameter."),
  A_set_accessor_parameter_cannot_have_an_initializer: diag(1052, 1 /* Error */, "A_set_accessor_parameter_cannot_have_an_initializer_1052", "A 'set' accessor parameter cannot have an initializer."),
  A_set_accessor_cannot_have_rest_parameter: diag(1053, 1 /* Error */, "A_set_accessor_cannot_have_rest_parameter_1053", "A 'set' accessor cannot have rest parameter."),
  A_get_accessor_cannot_have_parameters: diag(1054, 1 /* Error */, "A_get_accessor_cannot_have_parameters_1054", "A 'get' accessor cannot have parameters."),
  Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compatible_constructor_value: diag(1055, 1 /* Error */, "Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compa_1055", "Type '{0}' is not a valid async function return type in ES5 because it does not refer to a Promise-compatible constructor value."),
  Accessors_are_only_available_when_targeting_ECMAScript_5_and_higher: diag(1056, 1 /* Error */, "Accessors_are_only_available_when_targeting_ECMAScript_5_and_higher_1056", "Accessors are only available when targeting ECMAScript 5 and higher."),
  The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member: diag(1058, 1 /* Error */, "The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_t_1058", "The return type of an async function must either be a valid promise or must not contain a callable 'then' member."),
  A_promise_must_have_a_then_method: diag(1059, 1 /* Error */, "A_promise_must_have_a_then_method_1059", "A promise must have a 'then' method."),
  The_first_parameter_of_the_then_method_of_a_promise_must_be_a_callback: diag(1060, 1 /* Error */, "The_first_parameter_of_the_then_method_of_a_promise_must_be_a_callback_1060", "The first parameter of the 'then' method of a promise must be a callback."),
  Enum_member_must_have_initializer: diag(1061, 1 /* Error */, "Enum_member_must_have_initializer_1061", "Enum member must have initializer."),
  Type_is_referenced_directly_or_indirectly_in_the_fulfillment_callback_of_its_own_then_method: diag(1062, 1 /* Error */, "Type_is_referenced_directly_or_indirectly_in_the_fulfillment_callback_of_its_own_then_method_1062", "Type is referenced directly or indirectly in the fulfillment callback of its own 'then' method."),
  An_export_assignment_cannot_be_used_in_a_namespace: diag(1063, 1 /* Error */, "An_export_assignment_cannot_be_used_in_a_namespace_1063", "An export assignment cannot be used in a namespace."),
  The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type_Did_you_mean_to_write_Promise_0: diag(1064, 1 /* Error */, "The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type_Did_you_mean_to_wri_1064", "The return type of an async function or method must be the global Promise<T> type. Did you mean to write 'Promise<{0}>'?"),
  The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type: diag(1065, 1 /* Error */, "The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type_1065", "The return type of an async function or method must be the global Promise<T> type."),
  In_ambient_enum_declarations_member_initializer_must_be_constant_expression: diag(1066, 1 /* Error */, "In_ambient_enum_declarations_member_initializer_must_be_constant_expression_1066", "In ambient enum declarations member initializer must be constant expression."),
  Unexpected_token_A_constructor_method_accessor_or_property_was_expected: diag(1068, 1 /* Error */, "Unexpected_token_A_constructor_method_accessor_or_property_was_expected_1068", "Unexpected token. A constructor, method, accessor, or property was expected."),
  Unexpected_token_A_type_parameter_name_was_expected_without_curly_braces: diag(1069, 1 /* Error */, "Unexpected_token_A_type_parameter_name_was_expected_without_curly_braces_1069", "Unexpected token. A type parameter name was expected without curly braces."),
  _0_modifier_cannot_appear_on_a_type_member: diag(1070, 1 /* Error */, "_0_modifier_cannot_appear_on_a_type_member_1070", "'{0}' modifier cannot appear on a type member."),
  _0_modifier_cannot_appear_on_an_index_signature: diag(1071, 1 /* Error */, "_0_modifier_cannot_appear_on_an_index_signature_1071", "'{0}' modifier cannot appear on an index signature."),
  A_0_modifier_cannot_be_used_with_an_import_declaration: diag(1079, 1 /* Error */, "A_0_modifier_cannot_be_used_with_an_import_declaration_1079", "A '{0}' modifier cannot be used with an import declaration."),
  Invalid_reference_directive_syntax: diag(1084, 1 /* Error */, "Invalid_reference_directive_syntax_1084", "Invalid 'reference' directive syntax."),
  _0_modifier_cannot_appear_on_a_constructor_declaration: diag(1089, 1 /* Error */, "_0_modifier_cannot_appear_on_a_constructor_declaration_1089", "'{0}' modifier cannot appear on a constructor declaration."),
  _0_modifier_cannot_appear_on_a_parameter: diag(1090, 1 /* Error */, "_0_modifier_cannot_appear_on_a_parameter_1090", "'{0}' modifier cannot appear on a parameter."),
  Only_a_single_variable_declaration_is_allowed_in_a_for_in_statement: diag(1091, 1 /* Error */, "Only_a_single_variable_declaration_is_allowed_in_a_for_in_statement_1091", "Only a single variable declaration is allowed in a 'for...in' statement."),
  Type_parameters_cannot_appear_on_a_constructor_declaration: diag(1092, 1 /* Error */, "Type_parameters_cannot_appear_on_a_constructor_declaration_1092", "Type parameters cannot appear on a constructor declaration."),
  Type_annotation_cannot_appear_on_a_constructor_declaration: diag(1093, 1 /* Error */, "Type_annotation_cannot_appear_on_a_constructor_declaration_1093", "Type annotation cannot appear on a constructor declaration."),
  An_accessor_cannot_have_type_parameters: diag(1094, 1 /* Error */, "An_accessor_cannot_have_type_parameters_1094", "An accessor cannot have type parameters."),
  A_set_accessor_cannot_have_a_return_type_annotation: diag(1095, 1 /* Error */, "A_set_accessor_cannot_have_a_return_type_annotation_1095", "A 'set' accessor cannot have a return type annotation."),
  An_index_signature_must_have_exactly_one_parameter: diag(1096, 1 /* Error */, "An_index_signature_must_have_exactly_one_parameter_1096", "An index signature must have exactly one parameter."),
  _0_list_cannot_be_empty: diag(1097, 1 /* Error */, "_0_list_cannot_be_empty_1097", "'{0}' list cannot be empty."),
  Type_parameter_list_cannot_be_empty: diag(1098, 1 /* Error */, "Type_parameter_list_cannot_be_empty_1098", "Type parameter list cannot be empty."),
  Type_argument_list_cannot_be_empty: diag(1099, 1 /* Error */, "Type_argument_list_cannot_be_empty_1099", "Type argument list cannot be empty."),
  Invalid_use_of_0_in_strict_mode: diag(1100, 1 /* Error */, "Invalid_use_of_0_in_strict_mode_1100", "Invalid use of '{0}' in strict mode."),
  with_statements_are_not_allowed_in_strict_mode: diag(1101, 1 /* Error */, "with_statements_are_not_allowed_in_strict_mode_1101", "'with' statements are not allowed in strict mode."),
  delete_cannot_be_called_on_an_identifier_in_strict_mode: diag(1102, 1 /* Error */, "delete_cannot_be_called_on_an_identifier_in_strict_mode_1102", "'delete' cannot be called on an identifier in strict mode."),
  for_await_loops_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules: diag(1103, 1 /* Error */, "for_await_loops_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules_1103", "'for await' loops are only allowed within async functions and at the top levels of modules."),
  A_continue_statement_can_only_be_used_within_an_enclosing_iteration_statement: diag(1104, 1 /* Error */, "A_continue_statement_can_only_be_used_within_an_enclosing_iteration_statement_1104", "A 'continue' statement can only be used within an enclosing iteration statement."),
  A_break_statement_can_only_be_used_within_an_enclosing_iteration_or_switch_statement: diag(1105, 1 /* Error */, "A_break_statement_can_only_be_used_within_an_enclosing_iteration_or_switch_statement_1105", "A 'break' statement can only be used within an enclosing iteration or switch statement."),
  The_left_hand_side_of_a_for_of_statement_may_not_be_async: diag(1106, 1 /* Error */, "The_left_hand_side_of_a_for_of_statement_may_not_be_async_1106", "The left-hand side of a 'for...of' statement may not be 'async'."),
  Jump_target_cannot_cross_function_boundary: diag(1107, 1 /* Error */, "Jump_target_cannot_cross_function_boundary_1107", "Jump target cannot cross function boundary."),
  A_return_statement_can_only_be_used_within_a_function_body: diag(1108, 1 /* Error */, "A_return_statement_can_only_be_used_within_a_function_body_1108", "A 'return' statement can only be used within a function body."),
  Expression_expected: diag(1109, 1 /* Error */, "Expression_expected_1109", "Expression expected."),
  Type_expected: diag(1110, 1 /* Error */, "Type_expected_1110", "Type expected."),
  Private_field_0_must_be_declared_in_an_enclosing_class: diag(1111, 1 /* Error */, "Private_field_0_must_be_declared_in_an_enclosing_class_1111", "Private field '{0}' must be declared in an enclosing class."),
  A_default_clause_cannot_appear_more_than_once_in_a_switch_statement: diag(1113, 1 /* Error */, "A_default_clause_cannot_appear_more_than_once_in_a_switch_statement_1113", "A 'default' clause cannot appear more than once in a 'switch' statement."),
  Duplicate_label_0: diag(1114, 1 /* Error */, "Duplicate_label_0_1114", "Duplicate label '{0}'."),
  A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement: diag(1115, 1 /* Error */, "A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement_1115", "A 'continue' statement can only jump to a label of an enclosing iteration statement."),
  A_break_statement_can_only_jump_to_a_label_of_an_enclosing_statement: diag(1116, 1 /* Error */, "A_break_statement_can_only_jump_to_a_label_of_an_enclosing_statement_1116", "A 'break' statement can only jump to a label of an enclosing statement."),
  An_object_literal_cannot_have_multiple_properties_with_the_same_name: diag(1117, 1 /* Error */, "An_object_literal_cannot_have_multiple_properties_with_the_same_name_1117", "An object literal cannot have multiple properties with the same name."),
  An_object_literal_cannot_have_multiple_get_Slashset_accessors_with_the_same_name: diag(1118, 1 /* Error */, "An_object_literal_cannot_have_multiple_get_Slashset_accessors_with_the_same_name_1118", "An object literal cannot have multiple get/set accessors with the same name."),
  An_object_literal_cannot_have_property_and_accessor_with_the_same_name: diag(1119, 1 /* Error */, "An_object_literal_cannot_have_property_and_accessor_with_the_same_name_1119", "An object literal cannot have property and accessor with the same name."),
  An_export_assignment_cannot_have_modifiers: diag(1120, 1 /* Error */, "An_export_assignment_cannot_have_modifiers_1120", "An export assignment cannot have modifiers."),
  Octal_literals_are_not_allowed_Use_the_syntax_0: diag(1121, 1 /* Error */, "Octal_literals_are_not_allowed_Use_the_syntax_0_1121", "Octal literals are not allowed. Use the syntax '{0}'."),
  Variable_declaration_list_cannot_be_empty: diag(1123, 1 /* Error */, "Variable_declaration_list_cannot_be_empty_1123", "Variable declaration list cannot be empty."),
  Digit_expected: diag(1124, 1 /* Error */, "Digit_expected_1124", "Digit expected."),
  Hexadecimal_digit_expected: diag(1125, 1 /* Error */, "Hexadecimal_digit_expected_1125", "Hexadecimal digit expected."),
  Unexpected_end_of_text: diag(1126, 1 /* Error */, "Unexpected_end_of_text_1126", "Unexpected end of text."),
  Invalid_character: diag(1127, 1 /* Error */, "Invalid_character_1127", "Invalid character."),
  Declaration_or_statement_expected: diag(1128, 1 /* Error */, "Declaration_or_statement_expected_1128", "Declaration or statement expected."),
  Statement_expected: diag(1129, 1 /* Error */, "Statement_expected_1129", "Statement expected."),
  case_or_default_expected: diag(1130, 1 /* Error */, "case_or_default_expected_1130", "'case' or 'default' expected."),
  Property_or_signature_expected: diag(1131, 1 /* Error */, "Property_or_signature_expected_1131", "Property or signature expected."),
  Enum_member_expected: diag(1132, 1 /* Error */, "Enum_member_expected_1132", "Enum member expected."),
  Variable_declaration_expected: diag(1134, 1 /* Error */, "Variable_declaration_expected_1134", "Variable declaration expected."),
  Argument_expression_expected: diag(1135, 1 /* Error */, "Argument_expression_expected_1135", "Argument expression expected."),
  Property_assignment_expected: diag(1136, 1 /* Error */, "Property_assignment_expected_1136", "Property assignment expected."),
  Expression_or_comma_expected: diag(1137, 1 /* Error */, "Expression_or_comma_expected_1137", "Expression or comma expected."),
  Parameter_declaration_expected: diag(1138, 1 /* Error */, "Parameter_declaration_expected_1138", "Parameter declaration expected."),
  Type_parameter_declaration_expected: diag(1139, 1 /* Error */, "Type_parameter_declaration_expected_1139", "Type parameter declaration expected."),
  Type_argument_expected: diag(1140, 1 /* Error */, "Type_argument_expected_1140", "Type argument expected."),
  String_literal_expected: diag(1141, 1 /* Error */, "String_literal_expected_1141", "String literal expected."),
  Line_break_not_permitted_here: diag(1142, 1 /* Error */, "Line_break_not_permitted_here_1142", "Line break not permitted here."),
  or_expected: diag(1144, 1 /* Error */, "or_expected_1144", "'{' or ';' expected."),
  or_JSX_element_expected: diag(1145, 1 /* Error */, "or_JSX_element_expected_1145", "'{' or JSX element expected."),
  Declaration_expected: diag(1146, 1 /* Error */, "Declaration_expected_1146", "Declaration expected."),
  Import_declarations_in_a_namespace_cannot_reference_a_module: diag(1147, 1 /* Error */, "Import_declarations_in_a_namespace_cannot_reference_a_module_1147", "Import declarations in a namespace cannot reference a module."),
  Cannot_use_imports_exports_or_module_augmentations_when_module_is_none: diag(1148, 1 /* Error */, "Cannot_use_imports_exports_or_module_augmentations_when_module_is_none_1148", "Cannot use imports, exports, or module augmentations when '--module' is 'none'."),
  File_name_0_differs_from_already_included_file_name_1_only_in_casing: diag(1149, 1 /* Error */, "File_name_0_differs_from_already_included_file_name_1_only_in_casing_1149", "File name '{0}' differs from already included file name '{1}' only in casing."),
  _0_declarations_must_be_initialized: diag(1155, 1 /* Error */, "_0_declarations_must_be_initialized_1155", "'{0}' declarations must be initialized."),
  _0_declarations_can_only_be_declared_inside_a_block: diag(1156, 1 /* Error */, "_0_declarations_can_only_be_declared_inside_a_block_1156", "'{0}' declarations can only be declared inside a block."),
  Unterminated_template_literal: diag(1160, 1 /* Error */, "Unterminated_template_literal_1160", "Unterminated template literal."),
  Unterminated_regular_expression_literal: diag(1161, 1 /* Error */, "Unterminated_regular_expression_literal_1161", "Unterminated regular expression literal."),
  An_object_member_cannot_be_declared_optional: diag(1162, 1 /* Error */, "An_object_member_cannot_be_declared_optional_1162", "An object member cannot be declared optional."),
  A_yield_expression_is_only_allowed_in_a_generator_body: diag(1163, 1 /* Error */, "A_yield_expression_is_only_allowed_in_a_generator_body_1163", "A 'yield' expression is only allowed in a generator body."),
  Computed_property_names_are_not_allowed_in_enums: diag(1164, 1 /* Error */, "Computed_property_names_are_not_allowed_in_enums_1164", "Computed property names are not allowed in enums."),
  A_computed_property_name_in_an_ambient_context_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type: diag(1165, 1 /* Error */, "A_computed_property_name_in_an_ambient_context_must_refer_to_an_expression_whose_type_is_a_literal_t_1165", "A computed property name in an ambient context must refer to an expression whose type is a literal type or a 'unique symbol' type."),
  A_computed_property_name_in_a_class_property_declaration_must_have_a_simple_literal_type_or_a_unique_symbol_type: diag(1166, 1 /* Error */, "A_computed_property_name_in_a_class_property_declaration_must_have_a_simple_literal_type_or_a_unique_1166", "A computed property name in a class property declaration must have a simple literal type or a 'unique symbol' type."),
  A_computed_property_name_in_a_method_overload_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type: diag(1168, 1 /* Error */, "A_computed_property_name_in_a_method_overload_must_refer_to_an_expression_whose_type_is_a_literal_ty_1168", "A computed property name in a method overload must refer to an expression whose type is a literal type or a 'unique symbol' type."),
  A_computed_property_name_in_an_interface_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type: diag(1169, 1 /* Error */, "A_computed_property_name_in_an_interface_must_refer_to_an_expression_whose_type_is_a_literal_type_or_1169", "A computed property name in an interface must refer to an expression whose type is a literal type or a 'unique symbol' type."),
  A_computed_property_name_in_a_type_literal_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type: diag(1170, 1 /* Error */, "A_computed_property_name_in_a_type_literal_must_refer_to_an_expression_whose_type_is_a_literal_type__1170", "A computed property name in a type literal must refer to an expression whose type is a literal type or a 'unique symbol' type."),
  A_comma_expression_is_not_allowed_in_a_computed_property_name: diag(1171, 1 /* Error */, "A_comma_expression_is_not_allowed_in_a_computed_property_name_1171", "A comma expression is not allowed in a computed property name."),
  extends_clause_already_seen: diag(1172, 1 /* Error */, "extends_clause_already_seen_1172", "'extends' clause already seen."),
  extends_clause_must_precede_implements_clause: diag(1173, 1 /* Error */, "extends_clause_must_precede_implements_clause_1173", "'extends' clause must precede 'implements' clause."),
  Classes_can_only_extend_a_single_class: diag(1174, 1 /* Error */, "Classes_can_only_extend_a_single_class_1174", "Classes can only extend a single class."),
  implements_clause_already_seen: diag(1175, 1 /* Error */, "implements_clause_already_seen_1175", "'implements' clause already seen."),
  Interface_declaration_cannot_have_implements_clause: diag(1176, 1 /* Error */, "Interface_declaration_cannot_have_implements_clause_1176", "Interface declaration cannot have 'implements' clause."),
  Binary_digit_expected: diag(1177, 1 /* Error */, "Binary_digit_expected_1177", "Binary digit expected."),
  Octal_digit_expected: diag(1178, 1 /* Error */, "Octal_digit_expected_1178", "Octal digit expected."),
  Unexpected_token_expected: diag(1179, 1 /* Error */, "Unexpected_token_expected_1179", "Unexpected token. '{' expected."),
  Property_destructuring_pattern_expected: diag(1180, 1 /* Error */, "Property_destructuring_pattern_expected_1180", "Property destructuring pattern expected."),
  Array_element_destructuring_pattern_expected: diag(1181, 1 /* Error */, "Array_element_destructuring_pattern_expected_1181", "Array element destructuring pattern expected."),
  A_destructuring_declaration_must_have_an_initializer: diag(1182, 1 /* Error */, "A_destructuring_declaration_must_have_an_initializer_1182", "A destructuring declaration must have an initializer."),
  An_implementation_cannot_be_declared_in_ambient_contexts: diag(1183, 1 /* Error */, "An_implementation_cannot_be_declared_in_ambient_contexts_1183", "An implementation cannot be declared in ambient contexts."),
  Modifiers_cannot_appear_here: diag(1184, 1 /* Error */, "Modifiers_cannot_appear_here_1184", "Modifiers cannot appear here."),
  Merge_conflict_marker_encountered: diag(1185, 1 /* Error */, "Merge_conflict_marker_encountered_1185", "Merge conflict marker encountered."),
  A_rest_element_cannot_have_an_initializer: diag(1186, 1 /* Error */, "A_rest_element_cannot_have_an_initializer_1186", "A rest element cannot have an initializer."),
  A_parameter_property_may_not_be_declared_using_a_binding_pattern: diag(1187, 1 /* Error */, "A_parameter_property_may_not_be_declared_using_a_binding_pattern_1187", "A parameter property may not be declared using a binding pattern."),
  Only_a_single_variable_declaration_is_allowed_in_a_for_of_statement: diag(1188, 1 /* Error */, "Only_a_single_variable_declaration_is_allowed_in_a_for_of_statement_1188", "Only a single variable declaration is allowed in a 'for...of' statement."),
  The_variable_declaration_of_a_for_in_statement_cannot_have_an_initializer: diag(1189, 1 /* Error */, "The_variable_declaration_of_a_for_in_statement_cannot_have_an_initializer_1189", "The variable declaration of a 'for...in' statement cannot have an initializer."),
  The_variable_declaration_of_a_for_of_statement_cannot_have_an_initializer: diag(1190, 1 /* Error */, "The_variable_declaration_of_a_for_of_statement_cannot_have_an_initializer_1190", "The variable declaration of a 'for...of' statement cannot have an initializer."),
  An_import_declaration_cannot_have_modifiers: diag(1191, 1 /* Error */, "An_import_declaration_cannot_have_modifiers_1191", "An import declaration cannot have modifiers."),
  Module_0_has_no_default_export: diag(1192, 1 /* Error */, "Module_0_has_no_default_export_1192", "Module '{0}' has no default export."),
  An_export_declaration_cannot_have_modifiers: diag(1193, 1 /* Error */, "An_export_declaration_cannot_have_modifiers_1193", "An export declaration cannot have modifiers."),
  Export_declarations_are_not_permitted_in_a_namespace: diag(1194, 1 /* Error */, "Export_declarations_are_not_permitted_in_a_namespace_1194", "Export declarations are not permitted in a namespace."),
  export_Asterisk_does_not_re_export_a_default: diag(1195, 1 /* Error */, "export_Asterisk_does_not_re_export_a_default_1195", "'export *' does not re-export a default."),
  Catch_clause_variable_type_annotation_must_be_any_or_unknown_if_specified: diag(1196, 1 /* Error */, "Catch_clause_variable_type_annotation_must_be_any_or_unknown_if_specified_1196", "Catch clause variable type annotation must be 'any' or 'unknown' if specified."),
  Catch_clause_variable_cannot_have_an_initializer: diag(1197, 1 /* Error */, "Catch_clause_variable_cannot_have_an_initializer_1197", "Catch clause variable cannot have an initializer."),
  An_extended_Unicode_escape_value_must_be_between_0x0_and_0x10FFFF_inclusive: diag(1198, 1 /* Error */, "An_extended_Unicode_escape_value_must_be_between_0x0_and_0x10FFFF_inclusive_1198", "An extended Unicode escape value must be between 0x0 and 0x10FFFF inclusive."),
  Unterminated_Unicode_escape_sequence: diag(1199, 1 /* Error */, "Unterminated_Unicode_escape_sequence_1199", "Unterminated Unicode escape sequence."),
  Line_terminator_not_permitted_before_arrow: diag(1200, 1 /* Error */, "Line_terminator_not_permitted_before_arrow_1200", "Line terminator not permitted before arrow."),
  Import_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_import_Asterisk_as_ns_from_mod_import_a_from_mod_import_d_from_mod_or_another_module_format_instead: diag(1202, 1 /* Error */, "Import_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_import_Asterisk_as_1202", `Import assignment cannot be used when targeting ECMAScript modules. Consider using 'import * as ns from "mod"', 'import {a} from "mod"', 'import d from "mod"', or another module format instead.`),
  Export_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_export_default_or_another_module_format_instead: diag(1203, 1 /* Error */, "Export_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_export_default_or__1203", "Export assignment cannot be used when targeting ECMAScript modules. Consider using 'export default' or another module format instead."),
  Re_exporting_a_type_when_0_is_enabled_requires_using_export_type: diag(1205, 1 /* Error */, "Re_exporting_a_type_when_0_is_enabled_requires_using_export_type_1205", "Re-exporting a type when '{0}' is enabled requires using 'export type'."),
  Decorators_are_not_valid_here: diag(1206, 1 /* Error */, "Decorators_are_not_valid_here_1206", "Decorators are not valid here."),
  Decorators_cannot_be_applied_to_multiple_get_Slashset_accessors_of_the_same_name: diag(1207, 1 /* Error */, "Decorators_cannot_be_applied_to_multiple_get_Slashset_accessors_of_the_same_name_1207", "Decorators cannot be applied to multiple get/set accessors of the same name."),
  Invalid_optional_chain_from_new_expression_Did_you_mean_to_call_0: diag(1209, 1 /* Error */, "Invalid_optional_chain_from_new_expression_Did_you_mean_to_call_0_1209", "Invalid optional chain from new expression. Did you mean to call '{0}()'?"),
  Code_contained_in_a_class_is_evaluated_in_JavaScript_s_strict_mode_which_does_not_allow_this_use_of_0_For_more_information_see_https_Colon_Slash_Slashdeveloper_mozilla_org_Slashen_US_Slashdocs_SlashWeb_SlashJavaScript_SlashReference_SlashStrict_mode: diag(1210, 1 /* Error */, "Code_contained_in_a_class_is_evaluated_in_JavaScript_s_strict_mode_which_does_not_allow_this_use_of__1210", "Code contained in a class is evaluated in JavaScript's strict mode which does not allow this use of '{0}'. For more information, see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Strict_mode."),
  A_class_declaration_without_the_default_modifier_must_have_a_name: diag(1211, 1 /* Error */, "A_class_declaration_without_the_default_modifier_must_have_a_name_1211", "A class declaration without the 'default' modifier must have a name."),
  Identifier_expected_0_is_a_reserved_word_in_strict_mode: diag(1212, 1 /* Error */, "Identifier_expected_0_is_a_reserved_word_in_strict_mode_1212", "Identifier expected. '{0}' is a reserved word in strict mode."),
  Identifier_expected_0_is_a_reserved_word_in_strict_mode_Class_definitions_are_automatically_in_strict_mode: diag(1213, 1 /* Error */, "Identifier_expected_0_is_a_reserved_word_in_strict_mode_Class_definitions_are_automatically_in_stric_1213", "Identifier expected. '{0}' is a reserved word in strict mode. Class definitions are automatically in strict mode."),
  Identifier_expected_0_is_a_reserved_word_in_strict_mode_Modules_are_automatically_in_strict_mode: diag(1214, 1 /* Error */, "Identifier_expected_0_is_a_reserved_word_in_strict_mode_Modules_are_automatically_in_strict_mode_1214", "Identifier expected. '{0}' is a reserved word in strict mode. Modules are automatically in strict mode."),
  Invalid_use_of_0_Modules_are_automatically_in_strict_mode: diag(1215, 1 /* Error */, "Invalid_use_of_0_Modules_are_automatically_in_strict_mode_1215", "Invalid use of '{0}'. Modules are automatically in strict mode."),
  Identifier_expected_esModule_is_reserved_as_an_exported_marker_when_transforming_ECMAScript_modules: diag(1216, 1 /* Error */, "Identifier_expected_esModule_is_reserved_as_an_exported_marker_when_transforming_ECMAScript_modules_1216", "Identifier expected. '__esModule' is reserved as an exported marker when transforming ECMAScript modules."),
  Export_assignment_is_not_supported_when_module_flag_is_system: diag(1218, 1 /* Error */, "Export_assignment_is_not_supported_when_module_flag_is_system_1218", "Export assignment is not supported when '--module' flag is 'system'."),
  Generators_are_not_allowed_in_an_ambient_context: diag(1221, 1 /* Error */, "Generators_are_not_allowed_in_an_ambient_context_1221", "Generators are not allowed in an ambient context."),
  An_overload_signature_cannot_be_declared_as_a_generator: diag(1222, 1 /* Error */, "An_overload_signature_cannot_be_declared_as_a_generator_1222", "An overload signature cannot be declared as a generator."),
  _0_tag_already_specified: diag(1223, 1 /* Error */, "_0_tag_already_specified_1223", "'{0}' tag already specified."),
  Signature_0_must_be_a_type_predicate: diag(1224, 1 /* Error */, "Signature_0_must_be_a_type_predicate_1224", "Signature '{0}' must be a type predicate."),
  Cannot_find_parameter_0: diag(1225, 1 /* Error */, "Cannot_find_parameter_0_1225", "Cannot find parameter '{0}'."),
  Type_predicate_0_is_not_assignable_to_1: diag(1226, 1 /* Error */, "Type_predicate_0_is_not_assignable_to_1_1226", "Type predicate '{0}' is not assignable to '{1}'."),
  Parameter_0_is_not_in_the_same_position_as_parameter_1: diag(1227, 1 /* Error */, "Parameter_0_is_not_in_the_same_position_as_parameter_1_1227", "Parameter '{0}' is not in the same position as parameter '{1}'."),
  A_type_predicate_is_only_allowed_in_return_type_position_for_functions_and_methods: diag(1228, 1 /* Error */, "A_type_predicate_is_only_allowed_in_return_type_position_for_functions_and_methods_1228", "A type predicate is only allowed in return type position for functions and methods."),
  A_type_predicate_cannot_reference_a_rest_parameter: diag(1229, 1 /* Error */, "A_type_predicate_cannot_reference_a_rest_parameter_1229", "A type predicate cannot reference a rest parameter."),
  A_type_predicate_cannot_reference_element_0_in_a_binding_pattern: diag(1230, 1 /* Error */, "A_type_predicate_cannot_reference_element_0_in_a_binding_pattern_1230", "A type predicate cannot reference element '{0}' in a binding pattern."),
  An_export_assignment_must_be_at_the_top_level_of_a_file_or_module_declaration: diag(1231, 1 /* Error */, "An_export_assignment_must_be_at_the_top_level_of_a_file_or_module_declaration_1231", "An export assignment must be at the top level of a file or module declaration."),
  An_import_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module: diag(1232, 1 /* Error */, "An_import_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module_1232", "An import declaration can only be used at the top level of a namespace or module."),
  An_export_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module: diag(1233, 1 /* Error */, "An_export_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module_1233", "An export declaration can only be used at the top level of a namespace or module."),
  An_ambient_module_declaration_is_only_allowed_at_the_top_level_in_a_file: diag(1234, 1 /* Error */, "An_ambient_module_declaration_is_only_allowed_at_the_top_level_in_a_file_1234", "An ambient module declaration is only allowed at the top level in a file."),
  A_namespace_declaration_is_only_allowed_at_the_top_level_of_a_namespace_or_module: diag(1235, 1 /* Error */, "A_namespace_declaration_is_only_allowed_at_the_top_level_of_a_namespace_or_module_1235", "A namespace declaration is only allowed at the top level of a namespace or module."),
  The_return_type_of_a_property_decorator_function_must_be_either_void_or_any: diag(1236, 1 /* Error */, "The_return_type_of_a_property_decorator_function_must_be_either_void_or_any_1236", "The return type of a property decorator function must be either 'void' or 'any'."),
  The_return_type_of_a_parameter_decorator_function_must_be_either_void_or_any: diag(1237, 1 /* Error */, "The_return_type_of_a_parameter_decorator_function_must_be_either_void_or_any_1237", "The return type of a parameter decorator function must be either 'void' or 'any'."),
  Unable_to_resolve_signature_of_class_decorator_when_called_as_an_expression: diag(1238, 1 /* Error */, "Unable_to_resolve_signature_of_class_decorator_when_called_as_an_expression_1238", "Unable to resolve signature of class decorator when called as an expression."),
  Unable_to_resolve_signature_of_parameter_decorator_when_called_as_an_expression: diag(1239, 1 /* Error */, "Unable_to_resolve_signature_of_parameter_decorator_when_called_as_an_expression_1239", "Unable to resolve signature of parameter decorator when called as an expression."),
  Unable_to_resolve_signature_of_property_decorator_when_called_as_an_expression: diag(1240, 1 /* Error */, "Unable_to_resolve_signature_of_property_decorator_when_called_as_an_expression_1240", "Unable to resolve signature of property decorator when called as an expression."),
  Unable_to_resolve_signature_of_method_decorator_when_called_as_an_expression: diag(1241, 1 /* Error */, "Unable_to_resolve_signature_of_method_decorator_when_called_as_an_expression_1241", "Unable to resolve signature of method decorator when called as an expression."),
  abstract_modifier_can_only_appear_on_a_class_method_or_property_declaration: diag(1242, 1 /* Error */, "abstract_modifier_can_only_appear_on_a_class_method_or_property_declaration_1242", "'abstract' modifier can only appear on a class, method, or property declaration."),
  _0_modifier_cannot_be_used_with_1_modifier: diag(1243, 1 /* Error */, "_0_modifier_cannot_be_used_with_1_modifier_1243", "'{0}' modifier cannot be used with '{1}' modifier."),
  Abstract_methods_can_only_appear_within_an_abstract_class: diag(1244, 1 /* Error */, "Abstract_methods_can_only_appear_within_an_abstract_class_1244", "Abstract methods can only appear within an abstract class."),
  Method_0_cannot_have_an_implementation_because_it_is_marked_abstract: diag(1245, 1 /* Error */, "Method_0_cannot_have_an_implementation_because_it_is_marked_abstract_1245", "Method '{0}' cannot have an implementation because it is marked abstract."),
  An_interface_property_cannot_have_an_initializer: diag(1246, 1 /* Error */, "An_interface_property_cannot_have_an_initializer_1246", "An interface property cannot have an initializer."),
  A_type_literal_property_cannot_have_an_initializer: diag(1247, 1 /* Error */, "A_type_literal_property_cannot_have_an_initializer_1247", "A type literal property cannot have an initializer."),
  A_class_member_cannot_have_the_0_keyword: diag(1248, 1 /* Error */, "A_class_member_cannot_have_the_0_keyword_1248", "A class member cannot have the '{0}' keyword."),
  A_decorator_can_only_decorate_a_method_implementation_not_an_overload: diag(1249, 1 /* Error */, "A_decorator_can_only_decorate_a_method_implementation_not_an_overload_1249", "A decorator can only decorate a method implementation, not an overload."),
  Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5: diag(1250, 1 /* Error */, "Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_1250", "Function declarations are not allowed inside blocks in strict mode when targeting 'ES5'."),
  Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Class_definitions_are_automatically_in_strict_mode: diag(1251, 1 /* Error */, "Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Class_definiti_1251", "Function declarations are not allowed inside blocks in strict mode when targeting 'ES5'. Class definitions are automatically in strict mode."),
  Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Modules_are_automatically_in_strict_mode: diag(1252, 1 /* Error */, "Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Modules_are_au_1252", "Function declarations are not allowed inside blocks in strict mode when targeting 'ES5'. Modules are automatically in strict mode."),
  Abstract_properties_can_only_appear_within_an_abstract_class: diag(1253, 1 /* Error */, "Abstract_properties_can_only_appear_within_an_abstract_class_1253", "Abstract properties can only appear within an abstract class."),
  A_const_initializer_in_an_ambient_context_must_be_a_string_or_numeric_literal_or_literal_enum_reference: diag(1254, 1 /* Error */, "A_const_initializer_in_an_ambient_context_must_be_a_string_or_numeric_literal_or_literal_enum_refere_1254", "A 'const' initializer in an ambient context must be a string or numeric literal or literal enum reference."),
  A_definite_assignment_assertion_is_not_permitted_in_this_context: diag(1255, 1 /* Error */, "A_definite_assignment_assertion_is_not_permitted_in_this_context_1255", "A definite assignment assertion '!' is not permitted in this context."),
  A_required_element_cannot_follow_an_optional_element: diag(1257, 1 /* Error */, "A_required_element_cannot_follow_an_optional_element_1257", "A required element cannot follow an optional element."),
  A_default_export_must_be_at_the_top_level_of_a_file_or_module_declaration: diag(1258, 1 /* Error */, "A_default_export_must_be_at_the_top_level_of_a_file_or_module_declaration_1258", "A default export must be at the top level of a file or module declaration."),
  Module_0_can_only_be_default_imported_using_the_1_flag: diag(1259, 1 /* Error */, "Module_0_can_only_be_default_imported_using_the_1_flag_1259", "Module '{0}' can only be default-imported using the '{1}' flag"),
  Keywords_cannot_contain_escape_characters: diag(1260, 1 /* Error */, "Keywords_cannot_contain_escape_characters_1260", "Keywords cannot contain escape characters."),
  Already_included_file_name_0_differs_from_file_name_1_only_in_casing: diag(1261, 1 /* Error */, "Already_included_file_name_0_differs_from_file_name_1_only_in_casing_1261", "Already included file name '{0}' differs from file name '{1}' only in casing."),
  Identifier_expected_0_is_a_reserved_word_at_the_top_level_of_a_module: diag(1262, 1 /* Error */, "Identifier_expected_0_is_a_reserved_word_at_the_top_level_of_a_module_1262", "Identifier expected. '{0}' is a reserved word at the top-level of a module."),
  Declarations_with_initializers_cannot_also_have_definite_assignment_assertions: diag(1263, 1 /* Error */, "Declarations_with_initializers_cannot_also_have_definite_assignment_assertions_1263", "Declarations with initializers cannot also have definite assignment assertions."),
  Declarations_with_definite_assignment_assertions_must_also_have_type_annotations: diag(1264, 1 /* Error */, "Declarations_with_definite_assignment_assertions_must_also_have_type_annotations_1264", "Declarations with definite assignment assertions must also have type annotations."),
  A_rest_element_cannot_follow_another_rest_element: diag(1265, 1 /* Error */, "A_rest_element_cannot_follow_another_rest_element_1265", "A rest element cannot follow another rest element."),
  An_optional_element_cannot_follow_a_rest_element: diag(1266, 1 /* Error */, "An_optional_element_cannot_follow_a_rest_element_1266", "An optional element cannot follow a rest element."),
  Property_0_cannot_have_an_initializer_because_it_is_marked_abstract: diag(1267, 1 /* Error */, "Property_0_cannot_have_an_initializer_because_it_is_marked_abstract_1267", "Property '{0}' cannot have an initializer because it is marked abstract."),
  An_index_signature_parameter_type_must_be_string_number_symbol_or_a_template_literal_type: diag(1268, 1 /* Error */, "An_index_signature_parameter_type_must_be_string_number_symbol_or_a_template_literal_type_1268", "An index signature parameter type must be 'string', 'number', 'symbol', or a template literal type."),
  Cannot_use_export_import_on_a_type_or_type_only_namespace_when_0_is_enabled: diag(1269, 1 /* Error */, "Cannot_use_export_import_on_a_type_or_type_only_namespace_when_0_is_enabled_1269", "Cannot use 'export import' on a type or type-only namespace when '{0}' is enabled."),
  Decorator_function_return_type_0_is_not_assignable_to_type_1: diag(1270, 1 /* Error */, "Decorator_function_return_type_0_is_not_assignable_to_type_1_1270", "Decorator function return type '{0}' is not assignable to type '{1}'."),
  Decorator_function_return_type_is_0_but_is_expected_to_be_void_or_any: diag(1271, 1 /* Error */, "Decorator_function_return_type_is_0_but_is_expected_to_be_void_or_any_1271", "Decorator function return type is '{0}' but is expected to be 'void' or 'any'."),
  A_type_referenced_in_a_decorated_signature_must_be_imported_with_import_type_or_a_namespace_import_when_isolatedModules_and_emitDecoratorMetadata_are_enabled: diag(1272, 1 /* Error */, "A_type_referenced_in_a_decorated_signature_must_be_imported_with_import_type_or_a_namespace_import_w_1272", "A type referenced in a decorated signature must be imported with 'import type' or a namespace import when 'isolatedModules' and 'emitDecoratorMetadata' are enabled."),
  _0_modifier_cannot_appear_on_a_type_parameter: diag(1273, 1 /* Error */, "_0_modifier_cannot_appear_on_a_type_parameter_1273", "'{0}' modifier cannot appear on a type parameter"),
  _0_modifier_can_only_appear_on_a_type_parameter_of_a_class_interface_or_type_alias: diag(1274, 1 /* Error */, "_0_modifier_can_only_appear_on_a_type_parameter_of_a_class_interface_or_type_alias_1274", "'{0}' modifier can only appear on a type parameter of a class, interface or type alias"),
  accessor_modifier_can_only_appear_on_a_property_declaration: diag(1275, 1 /* Error */, "accessor_modifier_can_only_appear_on_a_property_declaration_1275", "'accessor' modifier can only appear on a property declaration."),
  An_accessor_property_cannot_be_declared_optional: diag(1276, 1 /* Error */, "An_accessor_property_cannot_be_declared_optional_1276", "An 'accessor' property cannot be declared optional."),
  _0_modifier_can_only_appear_on_a_type_parameter_of_a_function_method_or_class: diag(1277, 1 /* Error */, "_0_modifier_can_only_appear_on_a_type_parameter_of_a_function_method_or_class_1277", "'{0}' modifier can only appear on a type parameter of a function, method or class"),
  The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_0: diag(1278, 1 /* Error */, "The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_0_1278", "The runtime will invoke the decorator with {1} arguments, but the decorator expects {0}."),
  The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_at_least_0: diag(1279, 1 /* Error */, "The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_at_least_0_1279", "The runtime will invoke the decorator with {1} arguments, but the decorator expects at least {0}."),
  Namespaces_are_not_allowed_in_global_script_files_when_0_is_enabled_If_this_file_is_not_intended_to_be_a_global_script_set_moduleDetection_to_force_or_add_an_empty_export_statement: diag(1280, 1 /* Error */, "Namespaces_are_not_allowed_in_global_script_files_when_0_is_enabled_If_this_file_is_not_intended_to__1280", "Namespaces are not allowed in global script files when '{0}' is enabled. If this file is not intended to be a global script, set 'moduleDetection' to 'force' or add an empty 'export {}' statement."),
  Cannot_access_0_from_another_file_without_qualification_when_1_is_enabled_Use_2_instead: diag(1281, 1 /* Error */, "Cannot_access_0_from_another_file_without_qualification_when_1_is_enabled_Use_2_instead_1281", "Cannot access '{0}' from another file without qualification when '{1}' is enabled. Use '{2}' instead."),
  An_export_declaration_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers_to_a_type: diag(1282, 1 /* Error */, "An_export_declaration_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers__1282", "An 'export ***REMOVED***' declaration must reference a value when 'verbatimModuleSyntax' is enabled, but '{0}' only refers to a type."),
  An_export_declaration_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolves_to_a_type_only_declaration: diag(1283, 1 /* Error */, "An_export_declaration_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolve_1283", "An 'export ***REMOVED***' declaration must reference a real value when 'verbatimModuleSyntax' is enabled, but '{0}' resolves to a type-only declaration."),
  An_export_default_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers_to_a_type: diag(1284, 1 /* Error */, "An_export_default_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers_to_a_1284", "An 'export default' must reference a value when 'verbatimModuleSyntax' is enabled, but '{0}' only refers to a type."),
  An_export_default_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolves_to_a_type_only_declaration: diag(1285, 1 /* Error */, "An_export_default_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolves_to_1285", "An 'export default' must reference a real value when 'verbatimModuleSyntax' is enabled, but '{0}' resolves to a type-only declaration."),
  ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax: diag(1286, 1 /* Error */, "ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax_1286", "ECMAScript imports and exports cannot be written in a CommonJS file under 'verbatimModuleSyntax'."),
  A_top_level_export_modifier_cannot_be_used_on_value_declarations_in_a_CommonJS_module_when_verbatimModuleSyntax_is_enabled: diag(1287, 1 /* Error */, "A_top_level_export_modifier_cannot_be_used_on_value_declarations_in_a_CommonJS_module_when_verbatimM_1287", "A top-level 'export' modifier cannot be used on value declarations in a CommonJS module when 'verbatimModuleSyntax' is enabled."),
  An_import_alias_cannot_resolve_to_a_type_or_type_only_declaration_when_verbatimModuleSyntax_is_enabled: diag(1288, 1 /* Error */, "An_import_alias_cannot_resolve_to_a_type_or_type_only_declaration_when_verbatimModuleSyntax_is_enabl_1288", "An import alias cannot resolve to a type or type-only declaration when 'verbatimModuleSyntax' is enabled."),
  _0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_import_type_where_0_is_imported: diag(1289, 1 /* Error */, "_0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_1289", "'{0}' resolves to a type-only declaration and must be marked type-only in this file before re-exporting when '{1}' is enabled. Consider using 'import type' where '{0}' is imported."),
  _0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_export_type_0_as_default: diag(1290, 1 /* Error */, "_0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_1290", "'{0}' resolves to a type-only declaration and must be marked type-only in this file before re-exporting when '{1}' is enabled. Consider using 'export type { {0} as default }'."),
  _0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_import_type_where_0_is_imported: diag(1291, 1 /* Error */, "_0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enable_1291", "'{0}' resolves to a type and must be marked type-only in this file before re-exporting when '{1}' is enabled. Consider using 'import type' where '{0}' is imported."),
  _0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_export_type_0_as_default: diag(1292, 1 /* Error */, "_0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enable_1292", "'{0}' resolves to a type and must be marked type-only in this file before re-exporting when '{1}' is enabled. Consider using 'export type { {0} as default }'."),
  ECMAScript_module_syntax_is_not_allowed_in_a_CommonJS_module_when_module_is_set_to_preserve: diag(1293, 1 /* Error */, "ECMAScript_module_syntax_is_not_allowed_in_a_CommonJS_module_when_module_is_set_to_preserve_1293", "ECMAScript module syntax is not allowed in a CommonJS module when 'module' is set to 'preserve'."),
  This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled: diag(1294, 1 /* Error */, "This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled_1294", "This syntax is not allowed when 'erasableSyntaxOnly' is enabled."),
  ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax_Adjust_the_type_field_in_the_nearest_package_json_to_make_this_file_an_ECMAScript_module_or_adjust_your_verbatimModuleSyntax_module_and_moduleResolution_settings_in_TypeScript: diag(1295, 1 /* Error */, "ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax_Adjus_1295", "ECMAScript imports and exports cannot be written in a CommonJS file under 'verbatimModuleSyntax'. Adjust the 'type' field in the nearest 'package.json' to make this file an ECMAScript module, or adjust your 'verbatimModuleSyntax', 'module', and 'moduleResolution' settings in TypeScript."),
  with_statements_are_not_allowed_in_an_async_function_block: diag(1300, 1 /* Error */, "with_statements_are_not_allowed_in_an_async_function_block_1300", "'with' statements are not allowed in an async function block."),
  await_expressions_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules: diag(1308, 1 /* Error */, "await_expressions_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules_1308", "'await' expressions are only allowed within async functions and at the top levels of modules."),
  The_current_file_is_a_CommonJS_module_and_cannot_use_await_at_the_top_level: diag(1309, 1 /* Error */, "The_current_file_is_a_CommonJS_module_and_cannot_use_await_at_the_top_level_1309", "The current file is a CommonJS module and cannot use 'await' at the top level."),
  Did_you_mean_to_use_a_Colon_An_can_only_follow_a_property_name_when_the_containing_object_literal_is_part_of_a_destructuring_pattern: diag(1312, 1 /* Error */, "Did_you_mean_to_use_a_Colon_An_can_only_follow_a_property_name_when_the_containing_object_literal_is_1312", "Did you mean to use a ':'? An '***REMOVED***' can only follow a property name when the containing object literal is part of a destructuring pattern."),
  The_body_of_an_if_statement_cannot_be_the_empty_statement: diag(1313, 1 /* Error */, "The_body_of_an_if_statement_cannot_be_the_empty_statement_1313", "The body of an 'if' statement cannot be the empty statement."),
  Global_module_exports_may_only_appear_in_module_files: diag(1314, 1 /* Error */, "Global_module_exports_may_only_appear_in_module_files_1314", "Global module exports may only appear in module files."),
  Global_module_exports_may_only_appear_in_declaration_files: diag(1315, 1 /* Error */, "Global_module_exports_may_only_appear_in_declaration_files_1315", "Global module exports may only appear in declaration files."),
  Global_module_exports_may_only_appear_at_top_level: diag(1316, 1 /* Error */, "Global_module_exports_may_only_appear_at_top_level_1316", "Global module exports may only appear at top level."),
  A_parameter_property_cannot_be_declared_using_a_rest_parameter: diag(1317, 1 /* Error */, "A_parameter_property_cannot_be_declared_using_a_rest_parameter_1317", "A parameter property cannot be declared using a rest parameter."),
  An_abstract_accessor_cannot_have_an_implementation: diag(1318, 1 /* Error */, "An_abstract_accessor_cannot_have_an_implementation_1318", "An abstract accessor cannot have an implementation."),
  A_default_export_can_only_be_used_in_an_ECMAScript_style_module: diag(1319, 1 /* Error */, "A_default_export_can_only_be_used_in_an_ECMAScript_style_module_1319", "A default export can only be used in an ECMAScript-style module."),
  Type_of_await_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member: diag(1320, 1 /* Error */, "Type_of_await_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member_1320", "Type of 'await' operand must either be a valid promise or must not contain a callable 'then' member."),
  Type_of_yield_operand_in_an_async_generator_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member: diag(1321, 1 /* Error */, "Type_of_yield_operand_in_an_async_generator_must_either_be_a_valid_promise_or_must_not_contain_a_cal_1321", "Type of 'yield' operand in an async generator must either be a valid promise or must not contain a callable 'then' member."),
  Type_of_iterated_elements_of_a_yield_Asterisk_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member: diag(1322, 1 /* Error */, "Type_of_iterated_elements_of_a_yield_Asterisk_operand_must_either_be_a_valid_promise_or_must_not_con_1322", "Type of iterated elements of a 'yield*' operand must either be a valid promise or must not contain a callable 'then' member."),
  Dynamic_imports_are_only_supported_when_the_module_flag_is_set_to_es2020_es2022_esnext_commonjs_amd_system_umd_node16_node18_node20_or_nodenext: diag(1323, 1 /* Error */, "Dynamic_imports_are_only_supported_when_the_module_flag_is_set_to_es2020_es2022_esnext_commonjs_amd__1323", "Dynamic imports are only supported when the '--module' flag is set to 'es2020', 'es2022', 'esnext', 'commonjs', 'amd', 'system', 'umd', 'node16', 'node18', 'node20', or 'nodenext'."),
  Dynamic_imports_only_support_a_second_argument_when_the_module_option_is_set_to_esnext_node16_node18_node20_nodenext_or_preserve: diag(1324, 1 /* Error */, "Dynamic_imports_only_support_a_second_argument_when_the_module_option_is_set_to_esnext_node16_node18_1324", "Dynamic imports only support a second argument when the '--module' option is set to 'esnext', 'node16', 'node18', 'node20', 'nodenext', or 'preserve'."),
  Argument_of_dynamic_import_cannot_be_spread_element: diag(1325, 1 /* Error */, "Argument_of_dynamic_import_cannot_be_spread_element_1325", "Argument of dynamic import cannot be spread element."),
  This_use_of_import_is_invalid_import_calls_can_be_written_but_they_must_have_parentheses_and_cannot_have_type_arguments: diag(1326, 1 /* Error */, "This_use_of_import_is_invalid_import_calls_can_be_written_but_they_must_have_parentheses_and_cannot__1326", "This use of 'import' is invalid. 'import()' calls can be written, but they must have parentheses and cannot have type arguments."),
  String_literal_with_double_quotes_expected: diag(1327, 1 /* Error */, "String_literal_with_double_quotes_expected_1327", "String literal with double quotes expected."),
  Property_value_can_only_be_string_literal_numeric_literal_true_false_null_object_literal_or_array_literal: diag(1328, 1 /* Error */, "Property_value_can_only_be_string_literal_numeric_literal_true_false_null_object_literal_or_array_li_1328", "Property value can only be string literal, numeric literal, 'true', 'false', 'null', object literal or array literal."),
  _0_accepts_too_few_arguments_to_be_used_as_a_decorator_here_Did_you_mean_to_call_it_first_and_write_0: diag(1329, 1 /* Error */, "_0_accepts_too_few_arguments_to_be_used_as_a_decorator_here_Did_you_mean_to_call_it_first_and_write__1329", "'{0}' accepts too few arguments to be used as a decorator here. Did you mean to call it first and write '@{0}()'?"),
  A_property_of_an_interface_or_type_literal_whose_type_is_a_unique_symbol_type_must_be_readonly: diag(1330, 1 /* Error */, "A_property_of_an_interface_or_type_literal_whose_type_is_a_unique_symbol_type_must_be_readonly_1330", "A property of an interface or type literal whose type is a 'unique symbol' type must be 'readonly'."),
  A_property_of_a_class_whose_type_is_a_unique_symbol_type_must_be_both_static_and_readonly: diag(1331, 1 /* Error */, "A_property_of_a_class_whose_type_is_a_unique_symbol_type_must_be_both_static_and_readonly_1331", "A property of a class whose type is a 'unique symbol' type must be both 'static' and 'readonly'."),
  A_variable_whose_type_is_a_unique_symbol_type_must_be_const: diag(1332, 1 /* Error */, "A_variable_whose_type_is_a_unique_symbol_type_must_be_const_1332", "A variable whose type is a 'unique symbol' type must be 'const'."),
  unique_symbol_types_may_not_be_used_on_a_variable_declaration_with_a_binding_name: diag(1333, 1 /* Error */, "unique_symbol_types_may_not_be_used_on_a_variable_declaration_with_a_binding_name_1333", "'unique symbol' types may not be used on a variable declaration with a binding name."),
  unique_symbol_types_are_only_allowed_on_variables_in_a_variable_statement: diag(1334, 1 /* Error */, "unique_symbol_types_are_only_allowed_on_variables_in_a_variable_statement_1334", "'unique symbol' types are only allowed on variables in a variable statement."),
  unique_symbol_types_are_not_allowed_here: diag(1335, 1 /* Error */, "unique_symbol_types_are_not_allowed_here_1335", "'unique symbol' types are not allowed here."),
  An_index_signature_parameter_type_cannot_be_a_literal_type_or_generic_type_Consider_using_a_mapped_object_type_instead: diag(1337, 1 /* Error */, "An_index_signature_parameter_type_cannot_be_a_literal_type_or_generic_type_Consider_using_a_mapped_o_1337", "An index signature parameter type cannot be a literal type or generic type. Consider using a mapped object type instead."),
  infer_declarations_are_only_permitted_in_the_extends_clause_of_a_conditional_type: diag(1338, 1 /* Error */, "infer_declarations_are_only_permitted_in_the_extends_clause_of_a_conditional_type_1338", "'infer' declarations are only permitted in the 'extends' clause of a conditional type."),
  Module_0_does_not_refer_to_a_value_but_is_used_as_a_value_here: diag(1339, 1 /* Error */, "Module_0_does_not_refer_to_a_value_but_is_used_as_a_value_here_1339", "Module '{0}' does not refer to a value, but is used as a value here."),
  Module_0_does_not_refer_to_a_type_but_is_used_as_a_type_here_Did_you_mean_typeof_import_0: diag(1340, 1 /* Error */, "Module_0_does_not_refer_to_a_type_but_is_used_as_a_type_here_Did_you_mean_typeof_import_0_1340", "Module '{0}' does not refer to a type, but is used as a type here. Did you mean 'typeof import('{0}')'?"),
  Class_constructor_may_not_be_an_accessor: diag(1341, 1 /* Error */, "Class_constructor_may_not_be_an_accessor_1341", "Class constructor may not be an accessor."),
  The_import_meta_meta_property_is_only_allowed_when_the_module_option_is_es2020_es2022_esnext_system_node16_node18_node20_or_nodenext: diag(1343, 1 /* Error */, "The_import_meta_meta_property_is_only_allowed_when_the_module_option_is_es2020_es2022_esnext_system__1343", "The 'import.meta' meta-property is only allowed when the '--module' option is 'es2020', 'es2022', 'esnext', 'system', 'node16', 'node18', 'node20', or 'nodenext'."),
  A_label_is_not_allowed_here: diag(1344, 1 /* Error */, "A_label_is_not_allowed_here_1344", "'A label is not allowed here."),
  An_expression_of_type_void_cannot_be_tested_for_truthiness: diag(1345, 1 /* Error */, "An_expression_of_type_void_cannot_be_tested_for_truthiness_1345", "An expression of type 'void' cannot be tested for truthiness."),
  This_parameter_is_not_allowed_with_use_strict_directive: diag(1346, 1 /* Error */, "This_parameter_is_not_allowed_with_use_strict_directive_1346", "This parameter is not allowed with 'use strict' directive."),
  use_strict_directive_cannot_be_used_with_non_simple_parameter_list: diag(1347, 1 /* Error */, "use_strict_directive_cannot_be_used_with_non_simple_parameter_list_1347", "'use strict' directive cannot be used with non-simple parameter list."),
  Non_simple_parameter_declared_here: diag(1348, 1 /* Error */, "Non_simple_parameter_declared_here_1348", "Non-simple parameter declared here."),
  use_strict_directive_used_here: diag(1349, 1 /* Error */, "use_strict_directive_used_here_1349", "'use strict' directive used here."),
  Print_the_final_configuration_instead_of_building: diag(1350, 3 /* Message */, "Print_the_final_configuration_instead_of_building_1350", "Print the final configuration instead of building."),
  An_identifier_or_keyword_cannot_immediately_follow_a_numeric_literal: diag(1351, 1 /* Error */, "An_identifier_or_keyword_cannot_immediately_follow_a_numeric_literal_1351", "An identifier or keyword cannot immediately follow a numeric literal."),
  A_bigint_literal_cannot_use_exponential_notation: diag(1352, 1 /* Error */, "A_bigint_literal_cannot_use_exponential_notation_1352", "A bigint literal cannot use exponential notation."),
  A_bigint_literal_must_be_an_integer: diag(1353, 1 /* Error */, "A_bigint_literal_must_be_an_integer_1353", "A bigint literal must be an integer."),
  readonly_type_modifier_is_only_permitted_on_array_and_tuple_literal_types: diag(1354, 1 /* Error */, "readonly_type_modifier_is_only_permitted_on_array_and_tuple_literal_types_1354", "'readonly' type modifier is only permitted on array and tuple literal types."),
  A_const_assertions_can_only_be_applied_to_references_to_enum_members_or_string_number_boolean_array_or_object_literals: diag(1355, 1 /* Error */, "A_const_assertions_can_only_be_applied_to_references_to_enum_members_or_string_number_boolean_array__1355", "A 'const' assertions can only be applied to references to enum members, or string, number, boolean, array, or object literals."),
  Did_you_mean_to_mark_this_function_as_async: diag(1356, 1 /* Error */, "Did_you_mean_to_mark_this_function_as_async_1356", "Did you mean to mark this function as 'async'?"),
  An_enum_member_name_must_be_followed_by_a_or: diag(1357, 1 /* Error */, "An_enum_member_name_must_be_followed_by_a_or_1357", "An enum member name must be followed by a ',', '***REMOVED***', or '}'."),
  Tagged_template_expressions_are_not_permitted_in_an_optional_chain: diag(1358, 1 /* Error */, "Tagged_template_expressions_are_not_permitted_in_an_optional_chain_1358", "Tagged template expressions are not permitted in an optional chain."),
  Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here: diag(1359, 1 /* Error */, "Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here_1359", "Identifier expected. '{0}' is a reserved word that cannot be used here."),
  Type_0_does_not_satisfy_the_expected_type_1: diag(1360, 1 /* Error */, "Type_0_does_not_satisfy_the_expected_type_1_1360", "Type '{0}' does not satisfy the expected type '{1}'."),
  _0_cannot_be_used_as_a_value_because_it_was_imported_using_import_type: diag(1361, 1 /* Error */, "_0_cannot_be_used_as_a_value_because_it_was_imported_using_import_type_1361", "'{0}' cannot be used as a value because it was imported using 'import type'."),
  _0_cannot_be_used_as_a_value_because_it_was_exported_using_export_type: diag(1362, 1 /* Error */, "_0_cannot_be_used_as_a_value_because_it_was_exported_using_export_type_1362", "'{0}' cannot be used as a value because it was exported using 'export type'."),
  A_type_only_import_can_specify_a_default_import_or_named_bindings_but_not_both: diag(1363, 1 /* Error */, "A_type_only_import_can_specify_a_default_import_or_named_bindings_but_not_both_1363", "A type-only import can specify a default import or named bindings, but not both."),
  Convert_to_type_only_export: diag(1364, 3 /* Message */, "Convert_to_type_only_export_1364", "Convert to type-only export"),
  Convert_all_re_exported_types_to_type_only_exports: diag(1365, 3 /* Message */, "Convert_all_re_exported_types_to_type_only_exports_1365", "Convert all re-exported types to type-only exports"),
  Split_into_two_separate_import_declarations: diag(1366, 3 /* Message */, "Split_into_two_separate_import_declarations_1366", "Split into two separate import declarations"),
  Split_all_invalid_type_only_imports: diag(1367, 3 /* Message */, "Split_all_invalid_type_only_imports_1367", "Split all invalid type-only imports"),
  Class_constructor_may_not_be_a_generator: diag(1368, 1 /* Error */, "Class_constructor_may_not_be_a_generator_1368", "Class constructor may not be a generator."),
  Did_you_mean_0: diag(1369, 3 /* Message */, "Did_you_mean_0_1369", "Did you mean '{0}'?"),
  await_expressions_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module: diag(1375, 1 /* Error */, "await_expressions_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_fi_1375", "'await' expressions are only allowed at the top level of a file when that file is a module, but this file has no imports or exports. Consider adding an empty 'export {}' to make this file a module."),
  _0_was_imported_here: diag(1376, 3 /* Message */, "_0_was_imported_here_1376", "'{0}' was imported here."),
  _0_was_exported_here: diag(1377, 3 /* Message */, "_0_was_exported_here_1377", "'{0}' was exported here."),
  Top_level_await_expressions_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher: diag(1378, 1 /* Error */, "Top_level_await_expressions_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_n_1378", "Top-level 'await' expressions are only allowed when the 'module' option is set to 'es2022', 'esnext', 'system', 'node16', 'node18', 'node20', 'nodenext', or 'preserve', and the 'target' option is set to 'es2017' or higher."),
  An_import_alias_cannot_reference_a_declaration_that_was_exported_using_export_type: diag(1379, 1 /* Error */, "An_import_alias_cannot_reference_a_declaration_that_was_exported_using_export_type_1379", "An import alias cannot reference a declaration that was exported using 'export type'."),
  An_import_alias_cannot_reference_a_declaration_that_was_imported_using_import_type: diag(1380, 1 /* Error */, "An_import_alias_cannot_reference_a_declaration_that_was_imported_using_import_type_1380", "An import alias cannot reference a declaration that was imported using 'import type'."),
  Unexpected_token_Did_you_mean_or_rbrace: diag(1381, 1 /* Error */, "Unexpected_token_Did_you_mean_or_rbrace_1381", "Unexpected token. Did you mean `{'}'}` or `&rbrace;`?"),
  Unexpected_token_Did_you_mean_or_gt: diag(1382, 1 /* Error */, "Unexpected_token_Did_you_mean_or_gt_1382", "Unexpected token. Did you mean `{'>'}` or `&gt;`?"),
  Function_type_notation_must_be_parenthesized_when_used_in_a_union_type: diag(1385, 1 /* Error */, "Function_type_notation_must_be_parenthesized_when_used_in_a_union_type_1385", "Function type notation must be parenthesized when used in a union type."),
  Constructor_type_notation_must_be_parenthesized_when_used_in_a_union_type: diag(1386, 1 /* Error */, "Constructor_type_notation_must_be_parenthesized_when_used_in_a_union_type_1386", "Constructor type notation must be parenthesized when used in a union type."),
  Function_type_notation_must_be_parenthesized_when_used_in_an_intersection_type: diag(1387, 1 /* Error */, "Function_type_notation_must_be_parenthesized_when_used_in_an_intersection_type_1387", "Function type notation must be parenthesized when used in an intersection type."),
  Constructor_type_notation_must_be_parenthesized_when_used_in_an_intersection_type: diag(1388, 1 /* Error */, "Constructor_type_notation_must_be_parenthesized_when_used_in_an_intersection_type_1388", "Constructor type notation must be parenthesized when used in an intersection type."),
  _0_is_not_allowed_as_a_variable_declaration_name: diag(1389, 1 /* Error */, "_0_is_not_allowed_as_a_variable_declaration_name_1389", "'{0}' is not allowed as a variable declaration name."),
  _0_is_not_allowed_as_a_parameter_name: diag(1390, 1 /* Error */, "_0_is_not_allowed_as_a_parameter_name_1390", "'{0}' is not allowed as a parameter name."),
  An_import_alias_cannot_use_import_type: diag(1392, 1 /* Error */, "An_import_alias_cannot_use_import_type_1392", "An import alias cannot use 'import type'"),
  Imported_via_0_from_file_1: diag(1393, 3 /* Message */, "Imported_via_0_from_file_1_1393", "Imported via {0} from file '{1}'"),
  Imported_via_0_from_file_1_with_packageId_2: diag(1394, 3 /* Message */, "Imported_via_0_from_file_1_with_packageId_2_1394", "Imported via {0} from file '{1}' with packageId '{2}'"),
  Imported_via_0_from_file_1_to_import_importHelpers_as_specified_in_compilerOptions: diag(1395, 3 /* Message */, "Imported_via_0_from_file_1_to_import_importHelpers_as_specified_in_compilerOptions_1395", "Imported via {0} from file '{1}' to import 'importHelpers' as specified in compilerOptions"),
  Imported_via_0_from_file_1_with_packageId_2_to_import_importHelpers_as_specified_in_compilerOptions: diag(1396, 3 /* Message */, "Imported_via_0_from_file_1_with_packageId_2_to_import_importHelpers_as_specified_in_compilerOptions_1396", "Imported via {0} from file '{1}' with packageId '{2}' to import 'importHelpers' as specified in compilerOptions"),
  Imported_via_0_from_file_1_to_import_jsx_and_jsxs_factory_functions: diag(1397, 3 /* Message */, "Imported_via_0_from_file_1_to_import_jsx_and_jsxs_factory_functions_1397", "Imported via {0} from file '{1}' to import 'jsx' and 'jsxs' factory functions"),
  Imported_via_0_from_file_1_with_packageId_2_to_import_jsx_and_jsxs_factory_functions: diag(1398, 3 /* Message */, "Imported_via_0_from_file_1_with_packageId_2_to_import_jsx_and_jsxs_factory_functions_1398", "Imported via {0} from file '{1}' with packageId '{2}' to import 'jsx' and 'jsxs' factory functions"),
  File_is_included_via_import_here: diag(1399, 3 /* Message */, "File_is_included_via_import_here_1399", "File is included via import here."),
  Referenced_via_0_from_file_1: diag(1400, 3 /* Message */, "Referenced_via_0_from_file_1_1400", "Referenced via '{0}' from file '{1}'"),
  File_is_included_via_reference_here: diag(1401, 3 /* Message */, "File_is_included_via_reference_here_1401", "File is included via reference here."),
  Type_library_referenced_via_0_from_file_1: diag(1402, 3 /* Message */, "Type_library_referenced_via_0_from_file_1_1402", "Type library referenced via '{0}' from file '{1}'"),
  Type_library_referenced_via_0_from_file_1_with_packageId_2: diag(1403, 3 /* Message */, "Type_library_referenced_via_0_from_file_1_with_packageId_2_1403", "Type library referenced via '{0}' from file '{1}' with packageId '{2}'"),
  File_is_included_via_type_library_reference_here: diag(1404, 3 /* Message */, "File_is_included_via_type_library_reference_here_1404", "File is included via type library reference here."),
  Library_referenced_via_0_from_file_1: diag(1405, 3 /* Message */, "Library_referenced_via_0_from_file_1_1405", "Library referenced via '{0}' from file '{1}'"),
  File_is_included_via_library_reference_here: diag(1406, 3 /* Message */, "File_is_included_via_library_reference_here_1406", "File is included via library reference here."),
  Matched_by_include_pattern_0_in_1: diag(1407, 3 /* Message */, "Matched_by_include_pattern_0_in_1_1407", "Matched by include pattern '{0}' in '{1}'"),
  File_is_matched_by_include_pattern_specified_here: diag(1408, 3 /* Message */, "File_is_matched_by_include_pattern_specified_here_1408", "File is matched by include pattern specified here."),
  Part_of_files_list_in_tsconfig_json: diag(1409, 3 /* Message */, "Part_of_files_list_in_tsconfig_json_1409", "Part of 'files' list in tsconfig.json"),
  File_is_matched_by_files_list_specified_here: diag(1410, 3 /* Message */, "File_is_matched_by_files_list_specified_here_1410", "File is matched by 'files' list specified here."),
  Output_from_referenced_project_0_included_because_1_specified: diag(1411, 3 /* Message */, "Output_from_referenced_project_0_included_because_1_specified_1411", "Output from referenced project '{0}' included because '{1}' specified"),
  Output_from_referenced_project_0_included_because_module_is_specified_as_none: diag(1412, 3 /* Message */, "Output_from_referenced_project_0_included_because_module_is_specified_as_none_1412", "Output from referenced project '{0}' included because '--module' is specified as 'none'"),
  File_is_output_from_referenced_project_specified_here: diag(1413, 3 /* Message */, "File_is_output_from_referenced_project_specified_here_1413", "File is output from referenced project specified here."),
  Source_from_referenced_project_0_included_because_1_specified: diag(1414, 3 /* Message */, "Source_from_referenced_project_0_included_because_1_specified_1414", "Source from referenced project '{0}' included because '{1}' specified"),
  Source_from_referenced_project_0_included_because_module_is_specified_as_none: diag(1415, 3 /* Message */, "Source_from_referenced_project_0_included_because_module_is_specified_as_none_1415", "Source from referenced project '{0}' included because '--module' is specified as 'none'"),
  File_is_source_from_referenced_project_specified_here: diag(1416, 3 /* Message */, "File_is_source_from_referenced_project_specified_here_1416", "File is source from referenced project specified here."),
  Entry_point_of_type_library_0_specified_in_compilerOptions: diag(1417, 3 /* Message */, "Entry_point_of_type_library_0_specified_in_compilerOptions_1417", "Entry point of type library '{0}' specified in compilerOptions"),
  Entry_point_of_type_library_0_specified_in_compilerOptions_with_packageId_1: diag(1418, 3 /* Message */, "Entry_point_of_type_library_0_specified_in_compilerOptions_with_packageId_1_1418", "Entry point of type library '{0}' specified in compilerOptions with packageId '{1}'"),
  File_is_entry_point_of_type_library_specified_here: diag(1419, 3 /* Message */, "File_is_entry_point_of_type_library_specified_here_1419", "File is entry point of type library specified here."),
  Entry_point_for_implicit_type_library_0: diag(1420, 3 /* Message */, "Entry_point_for_implicit_type_library_0_1420", "Entry point for implicit type library '{0}'"),
  Entry_point_for_implicit_type_library_0_with_packageId_1: diag(1421, 3 /* Message */, "Entry_point_for_implicit_type_library_0_with_packageId_1_1421", "Entry point for implicit type library '{0}' with packageId '{1}'"),
  Library_0_specified_in_compilerOptions: diag(1422, 3 /* Message */, "Library_0_specified_in_compilerOptions_1422", "Library '{0}' specified in compilerOptions"),
  File_is_library_specified_here: diag(1423, 3 /* Message */, "File_is_library_specified_here_1423", "File is library specified here."),
  Default_library: diag(1424, 3 /* Message */, "Default_library_1424", "Default library"),
  Default_library_for_target_0: diag(1425, 3 /* Message */, "Default_library_for_target_0_1425", "Default library for target '{0}'"),
  File_is_default_library_for_target_specified_here: diag(1426, 3 /* Message */, "File_is_default_library_for_target_specified_here_1426", "File is default library for target specified here."),
  Root_file_specified_for_compilation: diag(1427, 3 /* Message */, "Root_file_specified_for_compilation_1427", "Root file specified for compilation"),
  File_is_output_of_project_reference_source_0: diag(1428, 3 /* Message */, "File_is_output_of_project_reference_source_0_1428", "File is output of project reference source '{0}'"),
  File_redirects_to_file_0: diag(1429, 3 /* Message */, "File_redirects_to_file_0_1429", "File redirects to file '{0}'"),
  The_file_is_in_the_program_because_Colon: diag(1430, 3 /* Message */, "The_file_is_in_the_program_because_Colon_1430", "The file is in the program because:"),
  for_await_loops_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module: diag(1431, 1 /* Error */, "for_await_loops_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_1431", "'for await' loops are only allowed at the top level of a file when that file is a module, but this file has no imports or exports. Consider adding an empty 'export {}' to make this file a module."),
  Top_level_for_await_loops_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher: diag(1432, 1 /* Error */, "Top_level_for_await_loops_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_nod_1432", "Top-level 'for await' loops are only allowed when the 'module' option is set to 'es2022', 'esnext', 'system', 'node16', 'node18', 'node20', 'nodenext', or 'preserve', and the 'target' option is set to 'es2017' or higher."),
  Neither_decorators_nor_modifiers_may_be_applied_to_this_parameters: diag(1433, 1 /* Error */, "Neither_decorators_nor_modifiers_may_be_applied_to_this_parameters_1433", "Neither decorators nor modifiers may be applied to 'this' parameters."),
  Unexpected_keyword_or_identifier: diag(1434, 1 /* Error */, "Unexpected_keyword_or_identifier_1434", "Unexpected keyword or identifier."),
  Unknown_keyword_or_identifier_Did_you_mean_0: diag(1435, 1 /* Error */, "Unknown_keyword_or_identifier_Did_you_mean_0_1435", "Unknown keyword or identifier. Did you mean '{0}'?"),
  Decorators_must_precede_the_name_and_all_keywords_of_property_declarations: diag(1436, 1 /* Error */, "Decorators_must_precede_the_name_and_all_keywords_of_property_declarations_1436", "Decorators must precede the name and all keywords of property declarations."),
  Namespace_must_be_given_a_name: diag(1437, 1 /* Error */, "Namespace_must_be_given_a_name_1437", "Namespace must be given a name."),
  Interface_must_be_given_a_name: diag(1438, 1 /* Error */, "Interface_must_be_given_a_name_1438", "Interface must be given a name."),
  Type_alias_must_be_given_a_name: diag(1439, 1 /* Error */, "Type_alias_must_be_given_a_name_1439", "Type alias must be given a name."),
  Variable_declaration_not_allowed_at_this_location: diag(1440, 1 /* Error */, "Variable_declaration_not_allowed_at_this_location_1440", "Variable declaration not allowed at this location."),
  Cannot_start_a_function_call_in_a_type_annotation: diag(1441, 1 /* Error */, "Cannot_start_a_function_call_in_a_type_annotation_1441", "Cannot start a function call in a type annotation."),
  Expected_for_property_initializer: diag(1442, 1 /* Error */, "Expected_for_property_initializer_1442", "Expected '***REMOVED***' for property initializer."),
  Module_declaration_names_may_only_use_or_quoted_strings: diag(1443, 1 /* Error */, "Module_declaration_names_may_only_use_or_quoted_strings_1443", `Module declaration names may only use ' or " quoted strings.`),
  _0_resolves_to_a_type_only_declaration_and_must_be_re_exported_using_a_type_only_re_export_when_1_is_enabled: diag(1448, 1 /* Error */, "_0_resolves_to_a_type_only_declaration_and_must_be_re_exported_using_a_type_only_re_export_when_1_is_1448", "'{0}' resolves to a type-only declaration and must be re-exported using a type-only re-export when '{1}' is enabled."),
  Preserve_unused_imported_values_in_the_JavaScript_output_that_would_otherwise_be_removed: diag(1449, 3 /* Message */, "Preserve_unused_imported_values_in_the_JavaScript_output_that_would_otherwise_be_removed_1449", "Preserve unused imported values in the JavaScript output that would otherwise be removed."),
  Dynamic_imports_can_only_accept_a_module_specifier_and_an_optional_set_of_attributes_as_arguments: diag(1450, 3 /* Message */, "Dynamic_imports_can_only_accept_a_module_specifier_and_an_optional_set_of_attributes_as_arguments_1450", "Dynamic imports can only accept a module specifier and an optional set of attributes as arguments"),
  Private_identifiers_are_only_allowed_in_class_bodies_and_may_only_be_used_as_part_of_a_class_member_declaration_property_access_or_on_the_left_hand_side_of_an_in_expression: diag(1451, 1 /* Error */, "Private_identifiers_are_only_allowed_in_class_bodies_and_may_only_be_used_as_part_of_a_class_member__1451", "Private identifiers are only allowed in class bodies and may only be used as part of a class member declaration, property access, or on the left-hand-side of an 'in' expression"),
  resolution_mode_should_be_either_require_or_import: diag(1453, 1 /* Error */, "resolution_mode_should_be_either_require_or_import_1453", "`resolution-mode` should be either `require` or `import`."),
  resolution_mode_can_only_be_set_for_type_only_imports: diag(1454, 1 /* Error */, "resolution_mode_can_only_be_set_for_type_only_imports_1454", "`resolution-mode` can only be set for type-only imports."),
  resolution_mode_is_the_only_valid_key_for_type_import_assertions: diag(1455, 1 /* Error */, "resolution_mode_is_the_only_valid_key_for_type_import_assertions_1455", "`resolution-mode` is the only valid key for type import assertions."),
  Type_import_assertions_should_have_exactly_one_key_resolution_mode_with_value_import_or_require: diag(1456, 1 /* Error */, "Type_import_assertions_should_have_exactly_one_key_resolution_mode_with_value_import_or_require_1456", "Type import assertions should have exactly one key - `resolution-mode` - with value `import` or `require`."),
  Matched_by_default_include_pattern_Asterisk_Asterisk_Slash_Asterisk: diag(1457, 3 /* Message */, "Matched_by_default_include_pattern_Asterisk_Asterisk_Slash_Asterisk_1457", "Matched by default include pattern '**/*'"),
  File_is_ECMAScript_module_because_0_has_field_type_with_value_module: diag(1458, 3 /* Message */, "File_is_ECMAScript_module_because_0_has_field_type_with_value_module_1458", `File is ECMAScript module because '{0}' has field "type" with value "module"`),
  File_is_CommonJS_module_because_0_has_field_type_whose_value_is_not_module: diag(1459, 3 /* Message */, "File_is_CommonJS_module_because_0_has_field_type_whose_value_is_not_module_1459", `File is CommonJS module because '{0}' has field "type" whose value is not "module"`),
  File_is_CommonJS_module_because_0_does_not_have_field_type: diag(1460, 3 /* Message */, "File_is_CommonJS_module_because_0_does_not_have_field_type_1460", `File is CommonJS module because '{0}' does not have field "type"`),
  File_is_CommonJS_module_because_package_json_was_not_found: diag(1461, 3 /* Message */, "File_is_CommonJS_module_because_package_json_was_not_found_1461", "File is CommonJS module because 'package.json' was not found"),
  resolution_mode_is_the_only_valid_key_for_type_import_attributes: diag(1463, 1 /* Error */, "resolution_mode_is_the_only_valid_key_for_type_import_attributes_1463", "'resolution-mode' is the only valid key for type import attributes."),
  Type_import_attributes_should_have_exactly_one_key_resolution_mode_with_value_import_or_require: diag(1464, 1 /* Error */, "Type_import_attributes_should_have_exactly_one_key_resolution_mode_with_value_import_or_require_1464", "Type import attributes should have exactly one key - 'resolution-mode' - with value 'import' or 'require'."),
  The_import_meta_meta_property_is_not_allowed_in_files_which_will_build_into_CommonJS_output: diag(1470, 1 /* Error */, "The_import_meta_meta_property_is_not_allowed_in_files_which_will_build_into_CommonJS_output_1470", "The 'import.meta' meta-property is not allowed in files which will build into CommonJS output."),
  Module_0_cannot_be_imported_using_this_construct_The_specifier_only_resolves_to_an_ES_module_which_cannot_be_imported_with_require_Use_an_ECMAScript_import_instead: diag(1471, 1 /* Error */, "Module_0_cannot_be_imported_using_this_construct_The_specifier_only_resolves_to_an_ES_module_which_c_1471", "Module '{0}' cannot be imported using this construct. The specifier only resolves to an ES module, which cannot be imported with 'require'. Use an ECMAScript import instead."),
  catch_or_finally_expected: diag(1472, 1 /* Error */, "catch_or_finally_expected_1472", "'catch' or 'finally' expected."),
  An_import_declaration_can_only_be_used_at_the_top_level_of_a_module: diag(1473, 1 /* Error */, "An_import_declaration_can_only_be_used_at_the_top_level_of_a_module_1473", "An import declaration can only be used at the top level of a module."),
  An_export_declaration_can_only_be_used_at_the_top_level_of_a_module: diag(1474, 1 /* Error */, "An_export_declaration_can_only_be_used_at_the_top_level_of_a_module_1474", "An export declaration can only be used at the top level of a module."),
  Control_what_method_is_used_to_detect_module_format_JS_files: diag(1475, 3 /* Message */, "Control_what_method_is_used_to_detect_module_format_JS_files_1475", "Control what method is used to detect module-format JS files."),
  auto_Colon_Treat_files_with_imports_exports_import_meta_jsx_with_jsx_Colon_react_jsx_or_esm_format_with_module_Colon_node16_as_modules: diag(1476, 3 /* Message */, "auto_Colon_Treat_files_with_imports_exports_import_meta_jsx_with_jsx_Colon_react_jsx_or_esm_format_w_1476", '"auto": Treat files with imports, exports, import.meta, jsx (with jsx: react-jsx), or esm format (with module: node16+) as modules.'),
  An_instantiation_expression_cannot_be_followed_by_a_property_access: diag(1477, 1 /* Error */, "An_instantiation_expression_cannot_be_followed_by_a_property_access_1477", "An instantiation expression cannot be followed by a property access."),
  Identifier_or_string_literal_expected: diag(1478, 1 /* Error */, "Identifier_or_string_literal_expected_1478", "Identifier or string literal expected."),
  The_current_file_is_a_CommonJS_module_whose_imports_will_produce_require_calls_however_the_referenced_file_is_an_ECMAScript_module_and_cannot_be_imported_with_require_Consider_writing_a_dynamic_import_0_call_instead: diag(1479, 1 /* Error */, "The_current_file_is_a_CommonJS_module_whose_imports_will_produce_require_calls_however_the_reference_1479", `The current file is a CommonJS module whose imports will produce 'require' calls; however, the referenced file is an ECMAScript module and cannot be imported with 'require'. Consider writing a dynamic 'import("{0}")' call instead.`),
  To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_create_a_local_package_json_file_with_type_Colon_module: diag(1480, 3 /* Message */, "To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_create_a_local_packag_1480", 'To convert this file to an ECMAScript module, change its file extension to \'{0}\' or create a local package.json file with `{ "type": "module" }`.'),
  To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_add_the_field_type_Colon_module_to_1: diag(1481, 3 /* Message */, "To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_add_the_field_type_Co_1481", `To convert this file to an ECMAScript module, change its file extension to '{0}', or add the field \`"type": "module"\` to '{1}'.`),
  To_convert_this_file_to_an_ECMAScript_module_add_the_field_type_Colon_module_to_0: diag(1482, 3 /* Message */, "To_convert_this_file_to_an_ECMAScript_module_add_the_field_type_Colon_module_to_0_1482", 'To convert this file to an ECMAScript module, add the field `"type": "module"` to \'{0}\'.'),
  To_convert_this_file_to_an_ECMAScript_module_create_a_local_package_json_file_with_type_Colon_module: diag(1483, 3 /* Message */, "To_convert_this_file_to_an_ECMAScript_module_create_a_local_package_json_file_with_type_Colon_module_1483", 'To convert this file to an ECMAScript module, create a local package.json file with `{ "type": "module" }`.'),
  _0_is_a_type_and_must_be_imported_using_a_type_only_import_when_verbatimModuleSyntax_is_enabled: diag(1484, 1 /* Error */, "_0_is_a_type_and_must_be_imported_using_a_type_only_import_when_verbatimModuleSyntax_is_enabled_1484", "'{0}' is a type and must be imported using a type-only import when 'verbatimModuleSyntax' is enabled."),
  _0_resolves_to_a_type_only_declaration_and_must_be_imported_using_a_type_only_import_when_verbatimModuleSyntax_is_enabled: diag(1485, 1 /* Error */, "_0_resolves_to_a_type_only_declaration_and_must_be_imported_using_a_type_only_import_when_verbatimMo_1485", "'{0}' resolves to a type-only declaration and must be imported using a type-only import when 'verbatimModuleSyntax' is enabled."),
  Decorator_used_before_export_here: diag(1486, 1 /* Error */, "Decorator_used_before_export_here_1486", "Decorator used before 'export' here."),
  Octal_escape_sequences_are_not_allowed_Use_the_syntax_0: diag(1487, 1 /* Error */, "Octal_escape_sequences_are_not_allowed_Use_the_syntax_0_1487", "Octal escape sequences are not allowed. Use the syntax '{0}'."),
  Escape_sequence_0_is_not_allowed: diag(1488, 1 /* Error */, "Escape_sequence_0_is_not_allowed_1488", "Escape sequence '{0}' is not allowed."),
  Decimals_with_leading_zeros_are_not_allowed: diag(1489, 1 /* Error */, "Decimals_with_leading_zeros_are_not_allowed_1489", "Decimals with leading zeros are not allowed."),
  File_appears_to_be_binary: diag(1490, 1 /* Error */, "File_appears_to_be_binary_1490", "File appears to be binary."),
  _0_modifier_cannot_appear_on_a_using_declaration: diag(1491, 1 /* Error */, "_0_modifier_cannot_appear_on_a_using_declaration_1491", "'{0}' modifier cannot appear on a 'using' declaration."),
  _0_declarations_may_not_have_binding_patterns: diag(1492, 1 /* Error */, "_0_declarations_may_not_have_binding_patterns_1492", "'{0}' declarations may not have binding patterns."),
  The_left_hand_side_of_a_for_in_statement_cannot_be_a_using_declaration: diag(1493, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_cannot_be_a_using_declaration_1493", "The left-hand side of a 'for...in' statement cannot be a 'using' declaration."),
  The_left_hand_side_of_a_for_in_statement_cannot_be_an_await_using_declaration: diag(1494, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_cannot_be_an_await_using_declaration_1494", "The left-hand side of a 'for...in' statement cannot be an 'await using' declaration."),
  _0_modifier_cannot_appear_on_an_await_using_declaration: diag(1495, 1 /* Error */, "_0_modifier_cannot_appear_on_an_await_using_declaration_1495", "'{0}' modifier cannot appear on an 'await using' declaration."),
  Identifier_string_literal_or_number_literal_expected: diag(1496, 1 /* Error */, "Identifier_string_literal_or_number_literal_expected_1496", "Identifier, string literal, or number literal expected."),
  Expression_must_be_enclosed_in_parentheses_to_be_used_as_a_decorator: diag(1497, 1 /* Error */, "Expression_must_be_enclosed_in_parentheses_to_be_used_as_a_decorator_1497", "Expression must be enclosed in parentheses to be used as a decorator."),
  Invalid_syntax_in_decorator: diag(1498, 1 /* Error */, "Invalid_syntax_in_decorator_1498", "Invalid syntax in decorator."),
  Unknown_regular_expression_flag: diag(1499, 1 /* Error */, "Unknown_regular_expression_flag_1499", "Unknown regular expression flag."),
  Duplicate_regular_expression_flag: diag(1500, 1 /* Error */, "Duplicate_regular_expression_flag_1500", "Duplicate regular expression flag."),
  This_regular_expression_flag_is_only_available_when_targeting_0_or_later: diag(1501, 1 /* Error */, "This_regular_expression_flag_is_only_available_when_targeting_0_or_later_1501", "This regular expression flag is only available when targeting '{0}' or later."),
  The_Unicode_u_flag_and_the_Unicode_Sets_v_flag_cannot_be_set_simultaneously: diag(1502, 1 /* Error */, "The_Unicode_u_flag_and_the_Unicode_Sets_v_flag_cannot_be_set_simultaneously_1502", "The Unicode (u) flag and the Unicode Sets (v) flag cannot be set simultaneously."),
  Named_capturing_groups_are_only_available_when_targeting_ES2018_or_later: diag(1503, 1 /* Error */, "Named_capturing_groups_are_only_available_when_targeting_ES2018_or_later_1503", "Named capturing groups are only available when targeting 'ES2018' or later."),
  Subpattern_flags_must_be_present_when_there_is_a_minus_sign: diag(1504, 1 /* Error */, "Subpattern_flags_must_be_present_when_there_is_a_minus_sign_1504", "Subpattern flags must be present when there is a minus sign."),
  Incomplete_quantifier_Digit_expected: diag(1505, 1 /* Error */, "Incomplete_quantifier_Digit_expected_1505", "Incomplete quantifier. Digit expected."),
  Numbers_out_of_order_in_quantifier: diag(1506, 1 /* Error */, "Numbers_out_of_order_in_quantifier_1506", "Numbers out of order in quantifier."),
  There_is_nothing_available_for_repetition: diag(1507, 1 /* Error */, "There_is_nothing_available_for_repetition_1507", "There is nothing available for repetition."),
  Unexpected_0_Did_you_mean_to_escape_it_with_backslash: diag(1508, 1 /* Error */, "Unexpected_0_Did_you_mean_to_escape_it_with_backslash_1508", "Unexpected '{0}'. Did you mean to escape it with backslash?"),
  This_regular_expression_flag_cannot_be_toggled_within_a_subpattern: diag(1509, 1 /* Error */, "This_regular_expression_flag_cannot_be_toggled_within_a_subpattern_1509", "This regular expression flag cannot be toggled within a subpattern."),
  k_must_be_followed_by_a_capturing_group_name_enclosed_in_angle_brackets: diag(1510, 1 /* Error */, "k_must_be_followed_by_a_capturing_group_name_enclosed_in_angle_brackets_1510", "'\\k' must be followed by a capturing group name enclosed in angle brackets."),
  q_is_only_available_inside_character_class: diag(1511, 1 /* Error */, "q_is_only_available_inside_character_class_1511", "'\\q' is only available inside character class."),
  c_must_be_followed_by_an_ASCII_letter: diag(1512, 1 /* Error */, "c_must_be_followed_by_an_ASCII_letter_1512", "'\\c' must be followed by an ASCII letter."),
  Undetermined_character_escape: diag(1513, 1 /* Error */, "Undetermined_character_escape_1513", "Undetermined character escape."),
  Expected_a_capturing_group_name: diag(1514, 1 /* Error */, "Expected_a_capturing_group_name_1514", "Expected a capturing group name."),
  Named_capturing_groups_with_the_same_name_must_be_mutually_exclusive_to_each_other: diag(1515, 1 /* Error */, "Named_capturing_groups_with_the_same_name_must_be_mutually_exclusive_to_each_other_1515", "Named capturing groups with the same name must be mutually exclusive to each other."),
  A_character_class_range_must_not_be_bounded_by_another_character_class: diag(1516, 1 /* Error */, "A_character_class_range_must_not_be_bounded_by_another_character_class_1516", "A character class range must not be bounded by another character class."),
  Range_out_of_order_in_character_class: diag(1517, 1 /* Error */, "Range_out_of_order_in_character_class_1517", "Range out of order in character class."),
  Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class: diag(1518, 1 /* Error */, "Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_characte_1518", "Anything that would possibly match more than a single character is invalid inside a negated character class."),
  Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead: diag(1519, 1 /* Error */, "Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead_1519", "Operators must not be mixed within a character class. Wrap it in a nested class instead."),
  Expected_a_class_set_operand: diag(1520, 1 /* Error */, "Expected_a_class_set_operand_1520", "Expected a class set operand."),
  q_must_be_followed_by_string_alternatives_enclosed_in_braces: diag(1521, 1 /* Error */, "q_must_be_followed_by_string_alternatives_enclosed_in_braces_1521", "'\\q' must be followed by string alternatives enclosed in braces."),
  A_character_class_must_not_contain_a_reserved_double_punctuator_Did_you_mean_to_escape_it_with_backslash: diag(1522, 1 /* Error */, "A_character_class_must_not_contain_a_reserved_double_punctuator_Did_you_mean_to_escape_it_with_backs_1522", "A character class must not contain a reserved double punctuator. Did you mean to escape it with backslash?"),
  Expected_a_Unicode_property_name: diag(1523, 1 /* Error */, "Expected_a_Unicode_property_name_1523", "Expected a Unicode property name."),
  Unknown_Unicode_property_name: diag(1524, 1 /* Error */, "Unknown_Unicode_property_name_1524", "Unknown Unicode property name."),
  Expected_a_Unicode_property_value: diag(1525, 1 /* Error */, "Expected_a_Unicode_property_value_1525", "Expected a Unicode property value."),
  Unknown_Unicode_property_value: diag(1526, 1 /* Error */, "Unknown_Unicode_property_value_1526", "Unknown Unicode property value."),
  Expected_a_Unicode_property_name_or_value: diag(1527, 1 /* Error */, "Expected_a_Unicode_property_name_or_value_1527", "Expected a Unicode property name or value."),
  Any_Unicode_property_that_would_possibly_match_more_than_a_single_character_is_only_available_when_the_Unicode_Sets_v_flag_is_set: diag(1528, 1 /* Error */, "Any_Unicode_property_that_would_possibly_match_more_than_a_single_character_is_only_available_when_t_1528", "Any Unicode property that would possibly match more than a single character is only available when the Unicode Sets (v) flag is set."),
  Unknown_Unicode_property_name_or_value: diag(1529, 1 /* Error */, "Unknown_Unicode_property_name_or_value_1529", "Unknown Unicode property name or value."),
  Unicode_property_value_expressions_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v_flag_is_set: diag(1530, 1 /* Error */, "Unicode_property_value_expressions_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v__1530", "Unicode property value expressions are only available when the Unicode (u) flag or the Unicode Sets (v) flag is set."),
  _0_must_be_followed_by_a_Unicode_property_value_expression_enclosed_in_braces: diag(1531, 1 /* Error */, "_0_must_be_followed_by_a_Unicode_property_value_expression_enclosed_in_braces_1531", "'\\{0}' must be followed by a Unicode property value expression enclosed in braces."),
  There_is_no_capturing_group_named_0_in_this_regular_expression: diag(1532, 1 /* Error */, "There_is_no_capturing_group_named_0_in_this_regular_expression_1532", "There is no capturing group named '{0}' in this regular expression."),
  This_backreference_refers_to_a_group_that_does_not_exist_There_are_only_0_capturing_groups_in_this_regular_expression: diag(1533, 1 /* Error */, "This_backreference_refers_to_a_group_that_does_not_exist_There_are_only_0_capturing_groups_in_this_r_1533", "This backreference refers to a group that does not exist. There are only {0} capturing groups in this regular expression."),
  This_backreference_refers_to_a_group_that_does_not_exist_There_are_no_capturing_groups_in_this_regular_expression: diag(1534, 1 /* Error */, "This_backreference_refers_to_a_group_that_does_not_exist_There_are_no_capturing_groups_in_this_regul_1534", "This backreference refers to a group that does not exist. There are no capturing groups in this regular expression."),
  This_character_cannot_be_escaped_in_a_regular_expression: diag(1535, 1 /* Error */, "This_character_cannot_be_escaped_in_a_regular_expression_1535", "This character cannot be escaped in a regular expression."),
  Octal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class_If_this_was_intended_as_an_escape_sequence_use_the_syntax_0_instead: diag(1536, 1 /* Error */, "Octal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class_If_this_was_intended__1536", "Octal escape sequences and backreferences are not allowed in a character class. If this was intended as an escape sequence, use the syntax '{0}' instead."),
  Decimal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class: diag(1537, 1 /* Error */, "Decimal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class_1537", "Decimal escape sequences and backreferences are not allowed in a character class."),
  Unicode_escape_sequences_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v_flag_is_set: diag(1538, 1 /* Error */, "Unicode_escape_sequences_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v_flag_is_se_1538", "Unicode escape sequences are only available when the Unicode (u) flag or the Unicode Sets (v) flag is set."),
  A_bigint_literal_cannot_be_used_as_a_property_name: diag(1539, 1 /* Error */, "A_bigint_literal_cannot_be_used_as_a_property_name_1539", "A 'bigint' literal cannot be used as a property name."),
  A_namespace_declaration_should_not_be_declared_using_the_module_keyword_Please_use_the_namespace_keyword_instead: diag(
    1540,
    2 /* Suggestion */,
    "A_namespace_declaration_should_not_be_declared_using_the_module_keyword_Please_use_the_namespace_key_1540",
    "A 'namespace' declaration should not be declared using the 'module' keyword. Please use the 'namespace' keyword instead.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    void 0,
    /*reportsDeprecated*/
    true
  ),
  Type_only_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribute: diag(1541, 1 /* Error */, "Type_only_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribut_1541", "Type-only import of an ECMAScript module from a CommonJS module must have a 'resolution-mode' attribute."),
  Type_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribute: diag(1542, 1 /* Error */, "Type_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribute_1542", "Type import of an ECMAScript module from a CommonJS module must have a 'resolution-mode' attribute."),
  Importing_a_JSON_file_into_an_ECMAScript_module_requires_a_type_Colon_json_import_attribute_when_module_is_set_to_0: diag(1543, 1 /* Error */, "Importing_a_JSON_file_into_an_ECMAScript_module_requires_a_type_Colon_json_import_attribute_when_mod_1543", `Importing a JSON file into an ECMAScript module requires a 'type: "json"' import attribute when 'module' is set to '{0}'.`),
  Named_imports_from_a_JSON_file_into_an_ECMAScript_module_are_not_allowed_when_module_is_set_to_0: diag(1544, 1 /* Error */, "Named_imports_from_a_JSON_file_into_an_ECMAScript_module_are_not_allowed_when_module_is_set_to_0_1544", "Named imports from a JSON file into an ECMAScript module are not allowed when 'module' is set to '{0}'."),
  using_declarations_are_not_allowed_in_ambient_contexts: diag(1545, 1 /* Error */, "using_declarations_are_not_allowed_in_ambient_contexts_1545", "'using' declarations are not allowed in ambient contexts."),
  await_using_declarations_are_not_allowed_in_ambient_contexts: diag(1546, 1 /* Error */, "await_using_declarations_are_not_allowed_in_ambient_contexts_1546", "'await using' declarations are not allowed in ambient contexts."),
  The_types_of_0_are_incompatible_between_these_types: diag(2200, 1 /* Error */, "The_types_of_0_are_incompatible_between_these_types_2200", "The types of '{0}' are incompatible between these types."),
  The_types_returned_by_0_are_incompatible_between_these_types: diag(2201, 1 /* Error */, "The_types_returned_by_0_are_incompatible_between_these_types_2201", "The types returned by '{0}' are incompatible between these types."),
  Call_signature_return_types_0_and_1_are_incompatible: diag(
    2202,
    1 /* Error */,
    "Call_signature_return_types_0_and_1_are_incompatible_2202",
    "Call signature return types '{0}' and '{1}' are incompatible.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    true
  ),
  Construct_signature_return_types_0_and_1_are_incompatible: diag(
    2203,
    1 /* Error */,
    "Construct_signature_return_types_0_and_1_are_incompatible_2203",
    "Construct signature return types '{0}' and '{1}' are incompatible.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    true
  ),
  Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1: diag(
    2204,
    1 /* Error */,
    "Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1_2204",
    "Call signatures with no arguments have incompatible return types '{0}' and '{1}'.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    true
  ),
  Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1: diag(
    2205,
    1 /* Error */,
    "Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1_2205",
    "Construct signatures with no arguments have incompatible return types '{0}' and '{1}'.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    true
  ),
  The_type_modifier_cannot_be_used_on_a_named_import_when_import_type_is_used_on_its_import_statement: diag(2206, 1 /* Error */, "The_type_modifier_cannot_be_used_on_a_named_import_when_import_type_is_used_on_its_import_statement_2206", "The 'type' modifier cannot be used on a named import when 'import type' is used on its import statement."),
  The_type_modifier_cannot_be_used_on_a_named_export_when_export_type_is_used_on_its_export_statement: diag(2207, 1 /* Error */, "The_type_modifier_cannot_be_used_on_a_named_export_when_export_type_is_used_on_its_export_statement_2207", "The 'type' modifier cannot be used on a named export when 'export type' is used on its export statement."),
  This_type_parameter_might_need_an_extends_0_constraint: diag(2208, 1 /* Error */, "This_type_parameter_might_need_an_extends_0_constraint_2208", "This type parameter might need an `extends {0}` constraint."),
  The_project_root_is_ambiguous_but_is_required_to_resolve_export_map_entry_0_in_file_1_Supply_the_rootDir_compiler_option_to_disambiguate: diag(2209, 1 /* Error */, "The_project_root_is_ambiguous_but_is_required_to_resolve_export_map_entry_0_in_file_1_Supply_the_roo_2209", "The project root is ambiguous, but is required to resolve export map entry '{0}' in file '{1}'. Supply the `rootDir` compiler option to disambiguate."),
  The_project_root_is_ambiguous_but_is_required_to_resolve_import_map_entry_0_in_file_1_Supply_the_rootDir_compiler_option_to_disambiguate: diag(2210, 1 /* Error */, "The_project_root_is_ambiguous_but_is_required_to_resolve_import_map_entry_0_in_file_1_Supply_the_roo_2210", "The project root is ambiguous, but is required to resolve import map entry '{0}' in file '{1}'. Supply the `rootDir` compiler option to disambiguate."),
  Add_extends_constraint: diag(2211, 3 /* Message */, "Add_extends_constraint_2211", "Add `extends` constraint."),
  Add_extends_constraint_to_all_type_parameters: diag(2212, 3 /* Message */, "Add_extends_constraint_to_all_type_parameters_2212", "Add `extends` constraint to all type parameters"),
  Duplicate_identifier_0: diag(2300, 1 /* Error */, "Duplicate_identifier_0_2300", "Duplicate identifier '{0}'."),
  Initializer_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor: diag(2301, 1 /* Error */, "Initializer_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor_2301", "Initializer of instance member variable '{0}' cannot reference identifier '{1}' declared in the constructor."),
  Static_members_cannot_reference_class_type_parameters: diag(2302, 1 /* Error */, "Static_members_cannot_reference_class_type_parameters_2302", "Static members cannot reference class type parameters."),
  Circular_definition_of_import_alias_0: diag(2303, 1 /* Error */, "Circular_definition_of_import_alias_0_2303", "Circular definition of import alias '{0}'."),
  Cannot_find_name_0: diag(2304, 1 /* Error */, "Cannot_find_name_0_2304", "Cannot find name '{0}'."),
  Module_0_has_no_exported_member_1: diag(2305, 1 /* Error */, "Module_0_has_no_exported_member_1_2305", "Module '{0}' has no exported member '{1}'."),
  File_0_is_not_a_module: diag(2306, 1 /* Error */, "File_0_is_not_a_module_2306", "File '{0}' is not a module."),
  Cannot_find_module_0_or_its_corresponding_type_declarations: diag(2307, 1 /* Error */, "Cannot_find_module_0_or_its_corresponding_type_declarations_2307", "Cannot find module '{0}' or its corresponding type declarations."),
  Module_0_has_already_exported_a_member_named_1_Consider_explicitly_re_exporting_to_resolve_the_ambiguity: diag(2308, 1 /* Error */, "Module_0_has_already_exported_a_member_named_1_Consider_explicitly_re_exporting_to_resolve_the_ambig_2308", "Module {0} has already exported a member named '{1}'. Consider explicitly re-exporting to resolve the ambiguity."),
  An_export_assignment_cannot_be_used_in_a_module_with_other_exported_elements: diag(2309, 1 /* Error */, "An_export_assignment_cannot_be_used_in_a_module_with_other_exported_elements_2309", "An export assignment cannot be used in a module with other exported elements."),
  Type_0_recursively_references_itself_as_a_base_type: diag(2310, 1 /* Error */, "Type_0_recursively_references_itself_as_a_base_type_2310", "Type '{0}' recursively references itself as a base type."),
  Cannot_find_name_0_Did_you_mean_to_write_this_in_an_async_function: diag(2311, 1 /* Error */, "Cannot_find_name_0_Did_you_mean_to_write_this_in_an_async_function_2311", "Cannot find name '{0}'. Did you mean to write this in an async function?"),
  An_interface_can_only_extend_an_object_type_or_intersection_of_object_types_with_statically_known_members: diag(2312, 1 /* Error */, "An_interface_can_only_extend_an_object_type_or_intersection_of_object_types_with_statically_known_me_2312", "An interface can only extend an object type or intersection of object types with statically known members."),
  Type_parameter_0_has_a_circular_constraint: diag(2313, 1 /* Error */, "Type_parameter_0_has_a_circular_constraint_2313", "Type parameter '{0}' has a circular constraint."),
  Generic_type_0_requires_1_type_argument_s: diag(2314, 1 /* Error */, "Generic_type_0_requires_1_type_argument_s_2314", "Generic type '{0}' requires {1} type argument(s)."),
  Type_0_is_not_generic: diag(2315, 1 /* Error */, "Type_0_is_not_generic_2315", "Type '{0}' is not generic."),
  Global_type_0_must_be_a_class_or_interface_type: diag(2316, 1 /* Error */, "Global_type_0_must_be_a_class_or_interface_type_2316", "Global type '{0}' must be a class or interface type."),
  Global_type_0_must_have_1_type_parameter_s: diag(2317, 1 /* Error */, "Global_type_0_must_have_1_type_parameter_s_2317", "Global type '{0}' must have {1} type parameter(s)."),
  Cannot_find_global_type_0: diag(2318, 1 /* Error */, "Cannot_find_global_type_0_2318", "Cannot find global type '{0}'."),
  Named_property_0_of_types_1_and_2_are_not_identical: diag(2319, 1 /* Error */, "Named_property_0_of_types_1_and_2_are_not_identical_2319", "Named property '{0}' of types '{1}' and '{2}' are not identical."),
  Interface_0_cannot_simultaneously_extend_types_1_and_2: diag(2320, 1 /* Error */, "Interface_0_cannot_simultaneously_extend_types_1_and_2_2320", "Interface '{0}' cannot simultaneously extend types '{1}' and '{2}'."),
  Excessive_stack_depth_comparing_types_0_and_1: diag(2321, 1 /* Error */, "Excessive_stack_depth_comparing_types_0_and_1_2321", "Excessive stack depth comparing types '{0}' and '{1}'."),
  Type_0_is_not_assignable_to_type_1: diag(2322, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_2322", "Type '{0}' is not assignable to type '{1}'."),
  Cannot_redeclare_exported_variable_0: diag(2323, 1 /* Error */, "Cannot_redeclare_exported_variable_0_2323", "Cannot redeclare exported variable '{0}'."),
  Property_0_is_missing_in_type_1: diag(2324, 1 /* Error */, "Property_0_is_missing_in_type_1_2324", "Property '{0}' is missing in type '{1}'."),
  Property_0_is_private_in_type_1_but_not_in_type_2: diag(2325, 1 /* Error */, "Property_0_is_private_in_type_1_but_not_in_type_2_2325", "Property '{0}' is private in type '{1}' but not in type '{2}'."),
  Types_of_property_0_are_incompatible: diag(2326, 1 /* Error */, "Types_of_property_0_are_incompatible_2326", "Types of property '{0}' are incompatible."),
  Property_0_is_optional_in_type_1_but_required_in_type_2: diag(2327, 1 /* Error */, "Property_0_is_optional_in_type_1_but_required_in_type_2_2327", "Property '{0}' is optional in type '{1}' but required in type '{2}'."),
  Types_of_parameters_0_and_1_are_incompatible: diag(2328, 1 /* Error */, "Types_of_parameters_0_and_1_are_incompatible_2328", "Types of parameters '{0}' and '{1}' are incompatible."),
  Index_signature_for_type_0_is_missing_in_type_1: diag(2329, 1 /* Error */, "Index_signature_for_type_0_is_missing_in_type_1_2329", "Index signature for type '{0}' is missing in type '{1}'."),
  _0_and_1_index_signatures_are_incompatible: diag(2330, 1 /* Error */, "_0_and_1_index_signatures_are_incompatible_2330", "'{0}' and '{1}' index signatures are incompatible."),
  this_cannot_be_referenced_in_a_module_or_namespace_body: diag(2331, 1 /* Error */, "this_cannot_be_referenced_in_a_module_or_namespace_body_2331", "'this' cannot be referenced in a module or namespace body."),
  this_cannot_be_referenced_in_current_location: diag(2332, 1 /* Error */, "this_cannot_be_referenced_in_current_location_2332", "'this' cannot be referenced in current location."),
  this_cannot_be_referenced_in_a_static_property_initializer: diag(2334, 1 /* Error */, "this_cannot_be_referenced_in_a_static_property_initializer_2334", "'this' cannot be referenced in a static property initializer."),
  super_can_only_be_referenced_in_a_derived_class: diag(2335, 1 /* Error */, "super_can_only_be_referenced_in_a_derived_class_2335", "'super' can only be referenced in a derived class."),
  super_cannot_be_referenced_in_constructor_arguments: diag(2336, 1 /* Error */, "super_cannot_be_referenced_in_constructor_arguments_2336", "'super' cannot be referenced in constructor arguments."),
  Super_calls_are_not_permitted_outside_constructors_or_in_nested_functions_inside_constructors: diag(2337, 1 /* Error */, "Super_calls_are_not_permitted_outside_constructors_or_in_nested_functions_inside_constructors_2337", "Super calls are not permitted outside constructors or in nested functions inside constructors."),
  super_property_access_is_permitted_only_in_a_constructor_member_function_or_member_accessor_of_a_derived_class: diag(2338, 1 /* Error */, "super_property_access_is_permitted_only_in_a_constructor_member_function_or_member_accessor_of_a_der_2338", "'super' property access is permitted only in a constructor, member function, or member accessor of a derived class."),
  Property_0_does_not_exist_on_type_1: diag(2339, 1 /* Error */, "Property_0_does_not_exist_on_type_1_2339", "Property '{0}' does not exist on type '{1}'."),
  Only_public_and_protected_methods_of_the_base_class_are_accessible_via_the_super_keyword: diag(2340, 1 /* Error */, "Only_public_and_protected_methods_of_the_base_class_are_accessible_via_the_super_keyword_2340", "Only public and protected methods of the base class are accessible via the 'super' keyword."),
  Property_0_is_private_and_only_accessible_within_class_1: diag(2341, 1 /* Error */, "Property_0_is_private_and_only_accessible_within_class_1_2341", "Property '{0}' is private and only accessible within class '{1}'."),
  This_syntax_requires_an_imported_helper_named_1_which_does_not_exist_in_0_Consider_upgrading_your_version_of_0: diag(2343, 1 /* Error */, "This_syntax_requires_an_imported_helper_named_1_which_does_not_exist_in_0_Consider_upgrading_your_ve_2343", "This syntax requires an imported helper named '{1}' which does not exist in '{0}'. Consider upgrading your version of '{0}'."),
  Type_0_does_not_satisfy_the_constraint_1: diag(2344, 1 /* Error */, "Type_0_does_not_satisfy_the_constraint_1_2344", "Type '{0}' does not satisfy the constraint '{1}'."),
  Argument_of_type_0_is_not_assignable_to_parameter_of_type_1: diag(2345, 1 /* Error */, "Argument_of_type_0_is_not_assignable_to_parameter_of_type_1_2345", "Argument of type '{0}' is not assignable to parameter of type '{1}'."),
  Call_target_does_not_contain_any_signatures: diag(2346, 1 /* Error */, "Call_target_does_not_contain_any_signatures_2346", "Call target does not contain any signatures."),
  Untyped_function_calls_may_not_accept_type_arguments: diag(2347, 1 /* Error */, "Untyped_function_calls_may_not_accept_type_arguments_2347", "Untyped function calls may not accept type arguments."),
  Value_of_type_0_is_not_callable_Did_you_mean_to_include_new: diag(2348, 1 /* Error */, "Value_of_type_0_is_not_callable_Did_you_mean_to_include_new_2348", "Value of type '{0}' is not callable. Did you mean to include 'new'?"),
  This_expression_is_not_callable: diag(2349, 1 /* Error */, "This_expression_is_not_callable_2349", "This expression is not callable."),
  Only_a_void_function_can_be_called_with_the_new_keyword: diag(2350, 1 /* Error */, "Only_a_void_function_can_be_called_with_the_new_keyword_2350", "Only a void function can be called with the 'new' keyword."),
  This_expression_is_not_constructable: diag(2351, 1 /* Error */, "This_expression_is_not_constructable_2351", "This expression is not constructable."),
  Conversion_of_type_0_to_type_1_may_be_a_mistake_because_neither_type_sufficiently_overlaps_with_the_other_If_this_was_intentional_convert_the_expression_to_unknown_first: diag(2352, 1 /* Error */, "Conversion_of_type_0_to_type_1_may_be_a_mistake_because_neither_type_sufficiently_overlaps_with_the__2352", "Conversion of type '{0}' to type '{1}' may be a mistake because neither type sufficiently overlaps with the other. If this was intentional, convert the expression to 'unknown' first."),
  Object_literal_may_only_specify_known_properties_and_0_does_not_exist_in_type_1: diag(2353, 1 /* Error */, "Object_literal_may_only_specify_known_properties_and_0_does_not_exist_in_type_1_2353", "Object literal may only specify known properties, and '{0}' does not exist in type '{1}'."),
  This_syntax_requires_an_imported_helper_but_module_0_cannot_be_found: diag(2354, 1 /* Error */, "This_syntax_requires_an_imported_helper_but_module_0_cannot_be_found_2354", "This syntax requires an imported helper but module '{0}' cannot be found."),
  A_function_whose_declared_type_is_neither_undefined_void_nor_any_must_return_a_value: diag(2355, 1 /* Error */, "A_function_whose_declared_type_is_neither_undefined_void_nor_any_must_return_a_value_2355", "A function whose declared type is neither 'undefined', 'void', nor 'any' must return a value."),
  An_arithmetic_operand_must_be_of_type_any_number_bigint_or_an_enum_type: diag(2356, 1 /* Error */, "An_arithmetic_operand_must_be_of_type_any_number_bigint_or_an_enum_type_2356", "An arithmetic operand must be of type 'any', 'number', 'bigint' or an enum type."),
  The_operand_of_an_increment_or_decrement_operator_must_be_a_variable_or_a_property_access: diag(2357, 1 /* Error */, "The_operand_of_an_increment_or_decrement_operator_must_be_a_variable_or_a_property_access_2357", "The operand of an increment or decrement operator must be a variable or a property access."),
  The_left_hand_side_of_an_instanceof_expression_must_be_of_type_any_an_object_type_or_a_type_parameter: diag(2358, 1 /* Error */, "The_left_hand_side_of_an_instanceof_expression_must_be_of_type_any_an_object_type_or_a_type_paramete_2358", "The left-hand side of an 'instanceof' expression must be of type 'any', an object type or a type parameter."),
  The_right_hand_side_of_an_instanceof_expression_must_be_either_of_type_any_a_class_function_or_other_type_assignable_to_the_Function_interface_type_or_an_object_type_with_a_Symbol_hasInstance_method: diag(2359, 1 /* Error */, "The_right_hand_side_of_an_instanceof_expression_must_be_either_of_type_any_a_class_function_or_other_2359", "The right-hand side of an 'instanceof' expression must be either of type 'any', a class, function, or other type assignable to the 'Function' interface type, or an object type with a 'Symbol.hasInstance' method."),
  The_left_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type: diag(2362, 1 /* Error */, "The_left_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type_2362", "The left-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type."),
  The_right_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type: diag(2363, 1 /* Error */, "The_right_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type_2363", "The right-hand side of an arithmetic operation must be of type 'any', 'number', 'bigint' or an enum type."),
  The_left_hand_side_of_an_assignment_expression_must_be_a_variable_or_a_property_access: diag(2364, 1 /* Error */, "The_left_hand_side_of_an_assignment_expression_must_be_a_variable_or_a_property_access_2364", "The left-hand side of an assignment expression must be a variable or a property access."),
  Operator_0_cannot_be_applied_to_types_1_and_2: diag(2365, 1 /* Error */, "Operator_0_cannot_be_applied_to_types_1_and_2_2365", "Operator '{0}' cannot be applied to types '{1}' and '{2}'."),
  Function_lacks_ending_return_statement_and_return_type_does_not_include_undefined: diag(2366, 1 /* Error */, "Function_lacks_ending_return_statement_and_return_type_does_not_include_undefined_2366", "Function lacks ending return statement and return type does not include 'undefined'."),
  This_comparison_appears_to_be_unintentional_because_the_types_0_and_1_have_no_overlap: diag(2367, 1 /* Error */, "This_comparison_appears_to_be_unintentional_because_the_types_0_and_1_have_no_overlap_2367", "This comparison appears to be unintentional because the types '{0}' and '{1}' have no overlap."),
  Type_parameter_name_cannot_be_0: diag(2368, 1 /* Error */, "Type_parameter_name_cannot_be_0_2368", "Type parameter name cannot be '{0}'."),
  A_parameter_property_is_only_allowed_in_a_constructor_implementation: diag(2369, 1 /* Error */, "A_parameter_property_is_only_allowed_in_a_constructor_implementation_2369", "A parameter property is only allowed in a constructor implementation."),
  A_rest_parameter_must_be_of_an_array_type: diag(2370, 1 /* Error */, "A_rest_parameter_must_be_of_an_array_type_2370", "A rest parameter must be of an array type."),
  A_parameter_initializer_is_only_allowed_in_a_function_or_constructor_implementation: diag(2371, 1 /* Error */, "A_parameter_initializer_is_only_allowed_in_a_function_or_constructor_implementation_2371", "A parameter initializer is only allowed in a function or constructor implementation."),
  Parameter_0_cannot_reference_itself: diag(2372, 1 /* Error */, "Parameter_0_cannot_reference_itself_2372", "Parameter '{0}' cannot reference itself."),
  Parameter_0_cannot_reference_identifier_1_declared_after_it: diag(2373, 1 /* Error */, "Parameter_0_cannot_reference_identifier_1_declared_after_it_2373", "Parameter '{0}' cannot reference identifier '{1}' declared after it."),
  Duplicate_index_signature_for_type_0: diag(2374, 1 /* Error */, "Duplicate_index_signature_for_type_0_2374", "Duplicate index signature for type '{0}'."),
  Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_types_of_the_target_s_properties: diag(2375, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefi_2375", "Type '{0}' is not assignable to type '{1}' with 'exactOptionalPropertyTypes: true'. Consider adding 'undefined' to the types of the target's properties."),
  A_super_call_must_be_the_first_statement_in_the_constructor_to_refer_to_super_or_this_when_a_derived_class_contains_initialized_properties_parameter_properties_or_private_identifiers: diag(2376, 1 /* Error */, "A_super_call_must_be_the_first_statement_in_the_constructor_to_refer_to_super_or_this_when_a_derived_2376", "A 'super' call must be the first statement in the constructor to refer to 'super' or 'this' when a derived class contains initialized properties, parameter properties, or private identifiers."),
  Constructors_for_derived_classes_must_contain_a_super_call: diag(2377, 1 /* Error */, "Constructors_for_derived_classes_must_contain_a_super_call_2377", "Constructors for derived classes must contain a 'super' call."),
  A_get_accessor_must_return_a_value: diag(2378, 1 /* Error */, "A_get_accessor_must_return_a_value_2378", "A 'get' accessor must return a value."),
  Argument_of_type_0_is_not_assignable_to_parameter_of_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_types_of_the_target_s_properties: diag(2379, 1 /* Error */, "Argument_of_type_0_is_not_assignable_to_parameter_of_type_1_with_exactOptionalPropertyTypes_Colon_tr_2379", "Argument of type '{0}' is not assignable to parameter of type '{1}' with 'exactOptionalPropertyTypes: true'. Consider adding 'undefined' to the types of the target's properties."),
  Overload_signatures_must_all_be_exported_or_non_exported: diag(2383, 1 /* Error */, "Overload_signatures_must_all_be_exported_or_non_exported_2383", "Overload signatures must all be exported or non-exported."),
  Overload_signatures_must_all_be_ambient_or_non_ambient: diag(2384, 1 /* Error */, "Overload_signatures_must_all_be_ambient_or_non_ambient_2384", "Overload signatures must all be ambient or non-ambient."),
  Overload_signatures_must_all_be_public_private_or_protected: diag(2385, 1 /* Error */, "Overload_signatures_must_all_be_public_private_or_protected_2385", "Overload signatures must all be public, private or protected."),
  Overload_signatures_must_all_be_optional_or_required: diag(2386, 1 /* Error */, "Overload_signatures_must_all_be_optional_or_required_2386", "Overload signatures must all be optional or required."),
  Function_overload_must_be_static: diag(2387, 1 /* Error */, "Function_overload_must_be_static_2387", "Function overload must be static."),
  Function_overload_must_not_be_static: diag(2388, 1 /* Error */, "Function_overload_must_not_be_static_2388", "Function overload must not be static."),
  Function_implementation_name_must_be_0: diag(2389, 1 /* Error */, "Function_implementation_name_must_be_0_2389", "Function implementation name must be '{0}'."),
  Constructor_implementation_is_missing: diag(2390, 1 /* Error */, "Constructor_implementation_is_missing_2390", "Constructor implementation is missing."),
  Function_implementation_is_missing_or_not_immediately_following_the_declaration: diag(2391, 1 /* Error */, "Function_implementation_is_missing_or_not_immediately_following_the_declaration_2391", "Function implementation is missing or not immediately following the declaration."),
  Multiple_constructor_implementations_are_not_allowed: diag(2392, 1 /* Error */, "Multiple_constructor_implementations_are_not_allowed_2392", "Multiple constructor implementations are not allowed."),
  Duplicate_function_implementation: diag(2393, 1 /* Error */, "Duplicate_function_implementation_2393", "Duplicate function implementation."),
  This_overload_signature_is_not_compatible_with_its_implementation_signature: diag(2394, 1 /* Error */, "This_overload_signature_is_not_compatible_with_its_implementation_signature_2394", "This overload signature is not compatible with its implementation signature."),
  Individual_declarations_in_merged_declaration_0_must_be_all_exported_or_all_local: diag(2395, 1 /* Error */, "Individual_declarations_in_merged_declaration_0_must_be_all_exported_or_all_local_2395", "Individual declarations in merged declaration '{0}' must be all exported or all local."),
  Duplicate_identifier_arguments_Compiler_uses_arguments_to_initialize_rest_parameters: diag(2396, 1 /* Error */, "Duplicate_identifier_arguments_Compiler_uses_arguments_to_initialize_rest_parameters_2396", "Duplicate identifier 'arguments'. Compiler uses 'arguments' to initialize rest parameters."),
  Declaration_name_conflicts_with_built_in_global_identifier_0: diag(2397, 1 /* Error */, "Declaration_name_conflicts_with_built_in_global_identifier_0_2397", "Declaration name conflicts with built-in global identifier '{0}'."),
  constructor_cannot_be_used_as_a_parameter_property_name: diag(2398, 1 /* Error */, "constructor_cannot_be_used_as_a_parameter_property_name_2398", "'constructor' cannot be used as a parameter property name."),
  Duplicate_identifier_this_Compiler_uses_variable_declaration_this_to_capture_this_reference: diag(2399, 1 /* Error */, "Duplicate_identifier_this_Compiler_uses_variable_declaration_this_to_capture_this_reference_2399", "Duplicate identifier '_this'. Compiler uses variable declaration '_this' to capture 'this' reference."),
  Expression_resolves_to_variable_declaration_this_that_compiler_uses_to_capture_this_reference: diag(2400, 1 /* Error */, "Expression_resolves_to_variable_declaration_this_that_compiler_uses_to_capture_this_reference_2400", "Expression resolves to variable declaration '_this' that compiler uses to capture 'this' reference."),
  A_super_call_must_be_a_root_level_statement_within_a_constructor_of_a_derived_class_that_contains_initialized_properties_parameter_properties_or_private_identifiers: diag(2401, 1 /* Error */, "A_super_call_must_be_a_root_level_statement_within_a_constructor_of_a_derived_class_that_contains_in_2401", "A 'super' call must be a root-level statement within a constructor of a derived class that contains initialized properties, parameter properties, or private identifiers."),
  Expression_resolves_to_super_that_compiler_uses_to_capture_base_class_reference: diag(2402, 1 /* Error */, "Expression_resolves_to_super_that_compiler_uses_to_capture_base_class_reference_2402", "Expression resolves to '_super' that compiler uses to capture base class reference."),
  Subsequent_variable_declarations_must_have_the_same_type_Variable_0_must_be_of_type_1_but_here_has_type_2: diag(2403, 1 /* Error */, "Subsequent_variable_declarations_must_have_the_same_type_Variable_0_must_be_of_type_1_but_here_has_t_2403", "Subsequent variable declarations must have the same type.  Variable '{0}' must be of type '{1}', but here has type '{2}'."),
  The_left_hand_side_of_a_for_in_statement_cannot_use_a_type_annotation: diag(2404, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_cannot_use_a_type_annotation_2404", "The left-hand side of a 'for...in' statement cannot use a type annotation."),
  The_left_hand_side_of_a_for_in_statement_must_be_of_type_string_or_any: diag(2405, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_must_be_of_type_string_or_any_2405", "The left-hand side of a 'for...in' statement must be of type 'string' or 'any'."),
  The_left_hand_side_of_a_for_in_statement_must_be_a_variable_or_a_property_access: diag(2406, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_must_be_a_variable_or_a_property_access_2406", "The left-hand side of a 'for...in' statement must be a variable or a property access."),
  The_right_hand_side_of_a_for_in_statement_must_be_of_type_any_an_object_type_or_a_type_parameter_but_here_has_type_0: diag(2407, 1 /* Error */, "The_right_hand_side_of_a_for_in_statement_must_be_of_type_any_an_object_type_or_a_type_parameter_but_2407", "The right-hand side of a 'for...in' statement must be of type 'any', an object type or a type parameter, but here has type '{0}'."),
  Setters_cannot_return_a_value: diag(2408, 1 /* Error */, "Setters_cannot_return_a_value_2408", "Setters cannot return a value."),
  Return_type_of_constructor_signature_must_be_assignable_to_the_instance_type_of_the_class: diag(2409, 1 /* Error */, "Return_type_of_constructor_signature_must_be_assignable_to_the_instance_type_of_the_class_2409", "Return type of constructor signature must be assignable to the instance type of the class."),
  The_with_statement_is_not_supported_All_symbols_in_a_with_block_will_have_type_any: diag(2410, 1 /* Error */, "The_with_statement_is_not_supported_All_symbols_in_a_with_block_will_have_type_any_2410", "The 'with' statement is not supported. All symbols in a 'with' block will have type 'any'."),
  Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_type_of_the_target: diag(2412, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefi_2412", "Type '{0}' is not assignable to type '{1}' with 'exactOptionalPropertyTypes: true'. Consider adding 'undefined' to the type of the target."),
  Property_0_of_type_1_is_not_assignable_to_2_index_type_3: diag(2411, 1 /* Error */, "Property_0_of_type_1_is_not_assignable_to_2_index_type_3_2411", "Property '{0}' of type '{1}' is not assignable to '{2}' index type '{3}'."),
  _0_index_type_1_is_not_assignable_to_2_index_type_3: diag(2413, 1 /* Error */, "_0_index_type_1_is_not_assignable_to_2_index_type_3_2413", "'{0}' index type '{1}' is not assignable to '{2}' index type '{3}'."),
  Class_name_cannot_be_0: diag(2414, 1 /* Error */, "Class_name_cannot_be_0_2414", "Class name cannot be '{0}'."),
  Class_0_incorrectly_extends_base_class_1: diag(2415, 1 /* Error */, "Class_0_incorrectly_extends_base_class_1_2415", "Class '{0}' incorrectly extends base class '{1}'."),
  Property_0_in_type_1_is_not_assignable_to_the_same_property_in_base_type_2: diag(2416, 1 /* Error */, "Property_0_in_type_1_is_not_assignable_to_the_same_property_in_base_type_2_2416", "Property '{0}' in type '{1}' is not assignable to the same property in base type '{2}'."),
  Class_static_side_0_incorrectly_extends_base_class_static_side_1: diag(2417, 1 /* Error */, "Class_static_side_0_incorrectly_extends_base_class_static_side_1_2417", "Class static side '{0}' incorrectly extends base class static side '{1}'."),
  Type_of_computed_property_s_value_is_0_which_is_not_assignable_to_type_1: diag(2418, 1 /* Error */, "Type_of_computed_property_s_value_is_0_which_is_not_assignable_to_type_1_2418", "Type of computed property's value is '{0}', which is not assignable to type '{1}'."),
  Types_of_construct_signatures_are_incompatible: diag(2419, 1 /* Error */, "Types_of_construct_signatures_are_incompatible_2419", "Types of construct signatures are incompatible."),
  Class_0_incorrectly_implements_interface_1: diag(2420, 1 /* Error */, "Class_0_incorrectly_implements_interface_1_2420", "Class '{0}' incorrectly implements interface '{1}'."),
  A_class_can_only_implement_an_object_type_or_intersection_of_object_types_with_statically_known_members: diag(2422, 1 /* Error */, "A_class_can_only_implement_an_object_type_or_intersection_of_object_types_with_statically_known_memb_2422", "A class can only implement an object type or intersection of object types with statically known members."),
  Class_0_defines_instance_member_function_1_but_extended_class_2_defines_it_as_instance_member_accessor: diag(2423, 1 /* Error */, "Class_0_defines_instance_member_function_1_but_extended_class_2_defines_it_as_instance_member_access_2423", "Class '{0}' defines instance member function '{1}', but extended class '{2}' defines it as instance member accessor."),
  Class_0_defines_instance_member_property_1_but_extended_class_2_defines_it_as_instance_member_function: diag(2425, 1 /* Error */, "Class_0_defines_instance_member_property_1_but_extended_class_2_defines_it_as_instance_member_functi_2425", "Class '{0}' defines instance member property '{1}', but extended class '{2}' defines it as instance member function."),
  Class_0_defines_instance_member_accessor_1_but_extended_class_2_defines_it_as_instance_member_function: diag(2426, 1 /* Error */, "Class_0_defines_instance_member_accessor_1_but_extended_class_2_defines_it_as_instance_member_functi_2426", "Class '{0}' defines instance member accessor '{1}', but extended class '{2}' defines it as instance member function."),
  Interface_name_cannot_be_0: diag(2427, 1 /* Error */, "Interface_name_cannot_be_0_2427", "Interface name cannot be '{0}'."),
  All_declarations_of_0_must_have_identical_type_parameters: diag(2428, 1 /* Error */, "All_declarations_of_0_must_have_identical_type_parameters_2428", "All declarations of '{0}' must have identical type parameters."),
  Interface_0_incorrectly_extends_interface_1: diag(2430, 1 /* Error */, "Interface_0_incorrectly_extends_interface_1_2430", "Interface '{0}' incorrectly extends interface '{1}'."),
  Enum_name_cannot_be_0: diag(2431, 1 /* Error */, "Enum_name_cannot_be_0_2431", "Enum name cannot be '{0}'."),
  In_an_enum_with_multiple_declarations_only_one_declaration_can_omit_an_initializer_for_its_first_enum_element: diag(2432, 1 /* Error */, "In_an_enum_with_multiple_declarations_only_one_declaration_can_omit_an_initializer_for_its_first_enu_2432", "In an enum with multiple declarations, only one declaration can omit an initializer for its first enum element."),
  A_namespace_declaration_cannot_be_in_a_different_file_from_a_class_or_function_with_which_it_is_merged: diag(2433, 1 /* Error */, "A_namespace_declaration_cannot_be_in_a_different_file_from_a_class_or_function_with_which_it_is_merg_2433", "A namespace declaration cannot be in a different file from a class or function with which it is merged."),
  A_namespace_declaration_cannot_be_located_prior_to_a_class_or_function_with_which_it_is_merged: diag(2434, 1 /* Error */, "A_namespace_declaration_cannot_be_located_prior_to_a_class_or_function_with_which_it_is_merged_2434", "A namespace declaration cannot be located prior to a class or function with which it is merged."),
  Ambient_modules_cannot_be_nested_in_other_modules_or_namespaces: diag(2435, 1 /* Error */, "Ambient_modules_cannot_be_nested_in_other_modules_or_namespaces_2435", "Ambient modules cannot be nested in other modules or namespaces."),
  Ambient_module_declaration_cannot_specify_relative_module_name: diag(2436, 1 /* Error */, "Ambient_module_declaration_cannot_specify_relative_module_name_2436", "Ambient module declaration cannot specify relative module name."),
  Module_0_is_hidden_by_a_local_declaration_with_the_same_name: diag(2437, 1 /* Error */, "Module_0_is_hidden_by_a_local_declaration_with_the_same_name_2437", "Module '{0}' is hidden by a local declaration with the same name."),
  Import_name_cannot_be_0: diag(2438, 1 /* Error */, "Import_name_cannot_be_0_2438", "Import name cannot be '{0}'."),
  Import_or_export_declaration_in_an_ambient_module_declaration_cannot_reference_module_through_relative_module_name: diag(2439, 1 /* Error */, "Import_or_export_declaration_in_an_ambient_module_declaration_cannot_reference_module_through_relati_2439", "Import or export declaration in an ambient module declaration cannot reference module through relative module name."),
  Import_declaration_conflicts_with_local_declaration_of_0: diag(2440, 1 /* Error */, "Import_declaration_conflicts_with_local_declaration_of_0_2440", "Import declaration conflicts with local declaration of '{0}'."),
  Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module: diag(2441, 1 /* Error */, "Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module_2441", "Duplicate identifier '{0}'. Compiler reserves name '{1}' in top level scope of a module."),
  Types_have_separate_declarations_of_a_private_property_0: diag(2442, 1 /* Error */, "Types_have_separate_declarations_of_a_private_property_0_2442", "Types have separate declarations of a private property '{0}'."),
  Property_0_is_protected_but_type_1_is_not_a_class_derived_from_2: diag(2443, 1 /* Error */, "Property_0_is_protected_but_type_1_is_not_a_class_derived_from_2_2443", "Property '{0}' is protected but type '{1}' is not a class derived from '{2}'."),
  Property_0_is_protected_in_type_1_but_public_in_type_2: diag(2444, 1 /* Error */, "Property_0_is_protected_in_type_1_but_public_in_type_2_2444", "Property '{0}' is protected in type '{1}' but public in type '{2}'."),
  Property_0_is_protected_and_only_accessible_within_class_1_and_its_subclasses: diag(2445, 1 /* Error */, "Property_0_is_protected_and_only_accessible_within_class_1_and_its_subclasses_2445", "Property '{0}' is protected and only accessible within class '{1}' and its subclasses."),
  Property_0_is_protected_and_only_accessible_through_an_instance_of_class_1_This_is_an_instance_of_class_2: diag(2446, 1 /* Error */, "Property_0_is_protected_and_only_accessible_through_an_instance_of_class_1_This_is_an_instance_of_cl_2446", "Property '{0}' is protected and only accessible through an instance of class '{1}'. This is an instance of class '{2}'."),
  The_0_operator_is_not_allowed_for_boolean_types_Consider_using_1_instead: diag(2447, 1 /* Error */, "The_0_operator_is_not_allowed_for_boolean_types_Consider_using_1_instead_2447", "The '{0}' operator is not allowed for boolean types. Consider using '{1}' instead."),
  Block_scoped_variable_0_used_before_its_declaration: diag(2448, 1 /* Error */, "Block_scoped_variable_0_used_before_its_declaration_2448", "Block-scoped variable '{0}' used before its declaration."),
  Class_0_used_before_its_declaration: diag(2449, 1 /* Error */, "Class_0_used_before_its_declaration_2449", "Class '{0}' used before its declaration."),
  Enum_0_used_before_its_declaration: diag(2450, 1 /* Error */, "Enum_0_used_before_its_declaration_2450", "Enum '{0}' used before its declaration."),
  Cannot_redeclare_block_scoped_variable_0: diag(2451, 1 /* Error */, "Cannot_redeclare_block_scoped_variable_0_2451", "Cannot redeclare block-scoped variable '{0}'."),
  An_enum_member_cannot_have_a_numeric_name: diag(2452, 1 /* Error */, "An_enum_member_cannot_have_a_numeric_name_2452", "An enum member cannot have a numeric name."),
  Variable_0_is_used_before_being_assigned: diag(2454, 1 /* Error */, "Variable_0_is_used_before_being_assigned_2454", "Variable '{0}' is used before being assigned."),
  Type_alias_0_circularly_references_itself: diag(2456, 1 /* Error */, "Type_alias_0_circularly_references_itself_2456", "Type alias '{0}' circularly references itself."),
  Type_alias_name_cannot_be_0: diag(2457, 1 /* Error */, "Type_alias_name_cannot_be_0_2457", "Type alias name cannot be '{0}'."),
  An_AMD_module_cannot_have_multiple_name_assignments: diag(2458, 1 /* Error */, "An_AMD_module_cannot_have_multiple_name_assignments_2458", "An AMD module cannot have multiple name assignments."),
  Module_0_declares_1_locally_but_it_is_not_exported: diag(2459, 1 /* Error */, "Module_0_declares_1_locally_but_it_is_not_exported_2459", "Module '{0}' declares '{1}' locally, but it is not exported."),
  Module_0_declares_1_locally_but_it_is_exported_as_2: diag(2460, 1 /* Error */, "Module_0_declares_1_locally_but_it_is_exported_as_2_2460", "Module '{0}' declares '{1}' locally, but it is exported as '{2}'."),
  Type_0_is_not_an_array_type: diag(2461, 1 /* Error */, "Type_0_is_not_an_array_type_2461", "Type '{0}' is not an array type."),
  A_rest_element_must_be_last_in_a_destructuring_pattern: diag(2462, 1 /* Error */, "A_rest_element_must_be_last_in_a_destructuring_pattern_2462", "A rest element must be last in a destructuring pattern."),
  A_binding_pattern_parameter_cannot_be_optional_in_an_implementation_signature: diag(2463, 1 /* Error */, "A_binding_pattern_parameter_cannot_be_optional_in_an_implementation_signature_2463", "A binding pattern parameter cannot be optional in an implementation signature."),
  A_computed_property_name_must_be_of_type_string_number_symbol_or_any: diag(2464, 1 /* Error */, "A_computed_property_name_must_be_of_type_string_number_symbol_or_any_2464", "A computed property name must be of type 'string', 'number', 'symbol', or 'any'."),
  this_cannot_be_referenced_in_a_computed_property_name: diag(2465, 1 /* Error */, "this_cannot_be_referenced_in_a_computed_property_name_2465", "'this' cannot be referenced in a computed property name."),
  super_cannot_be_referenced_in_a_computed_property_name: diag(2466, 1 /* Error */, "super_cannot_be_referenced_in_a_computed_property_name_2466", "'super' cannot be referenced in a computed property name."),
  A_computed_property_name_cannot_reference_a_type_parameter_from_its_containing_type: diag(2467, 1 /* Error */, "A_computed_property_name_cannot_reference_a_type_parameter_from_its_containing_type_2467", "A computed property name cannot reference a type parameter from its containing type."),
  Cannot_find_global_value_0: diag(2468, 1 /* Error */, "Cannot_find_global_value_0_2468", "Cannot find global value '{0}'."),
  The_0_operator_cannot_be_applied_to_type_symbol: diag(2469, 1 /* Error */, "The_0_operator_cannot_be_applied_to_type_symbol_2469", "The '{0}' operator cannot be applied to type 'symbol'."),
  Spread_operator_in_new_expressions_is_only_available_when_targeting_ECMAScript_5_and_higher: diag(2472, 1 /* Error */, "Spread_operator_in_new_expressions_is_only_available_when_targeting_ECMAScript_5_and_higher_2472", "Spread operator in 'new' expressions is only available when targeting ECMAScript 5 and higher."),
  Enum_declarations_must_all_be_const_or_non_const: diag(2473, 1 /* Error */, "Enum_declarations_must_all_be_const_or_non_const_2473", "Enum declarations must all be const or non-const."),
  const_enum_member_initializers_must_be_constant_expressions: diag(2474, 1 /* Error */, "const_enum_member_initializers_must_be_constant_expressions_2474", "const enum member initializers must be constant expressions."),
  const_enums_can_only_be_used_in_property_or_index_access_expressions_or_the_right_hand_side_of_an_import_declaration_or_export_assignment_or_type_query: diag(2475, 1 /* Error */, "const_enums_can_only_be_used_in_property_or_index_access_expressions_or_the_right_hand_side_of_an_im_2475", "'const' enums can only be used in property or index access expressions or the right hand side of an import declaration or export assignment or type query."),
  A_const_enum_member_can_only_be_accessed_using_a_string_literal: diag(2476, 1 /* Error */, "A_const_enum_member_can_only_be_accessed_using_a_string_literal_2476", "A const enum member can only be accessed using a string literal."),
  const_enum_member_initializer_was_evaluated_to_a_non_finite_value: diag(2477, 1 /* Error */, "const_enum_member_initializer_was_evaluated_to_a_non_finite_value_2477", "'const' enum member initializer was evaluated to a non-finite value."),
  const_enum_member_initializer_was_evaluated_to_disallowed_value_NaN: diag(2478, 1 /* Error */, "const_enum_member_initializer_was_evaluated_to_disallowed_value_NaN_2478", "'const' enum member initializer was evaluated to disallowed value 'NaN'."),
  let_is_not_allowed_to_be_used_as_a_name_in_let_or_const_declarations: diag(2480, 1 /* Error */, "let_is_not_allowed_to_be_used_as_a_name_in_let_or_const_declarations_2480", "'let' is not allowed to be used as a name in 'let' or 'const' declarations."),
  Cannot_initialize_outer_scoped_variable_0_in_the_same_scope_as_block_scoped_declaration_1: diag(2481, 1 /* Error */, "Cannot_initialize_outer_scoped_variable_0_in_the_same_scope_as_block_scoped_declaration_1_2481", "Cannot initialize outer scoped variable '{0}' in the same scope as block scoped declaration '{1}'."),
  The_left_hand_side_of_a_for_of_statement_cannot_use_a_type_annotation: diag(2483, 1 /* Error */, "The_left_hand_side_of_a_for_of_statement_cannot_use_a_type_annotation_2483", "The left-hand side of a 'for...of' statement cannot use a type annotation."),
  Export_declaration_conflicts_with_exported_declaration_of_0: diag(2484, 1 /* Error */, "Export_declaration_conflicts_with_exported_declaration_of_0_2484", "Export declaration conflicts with exported declaration of '{0}'."),
  The_left_hand_side_of_a_for_of_statement_must_be_a_variable_or_a_property_access: diag(2487, 1 /* Error */, "The_left_hand_side_of_a_for_of_statement_must_be_a_variable_or_a_property_access_2487", "The left-hand side of a 'for...of' statement must be a variable or a property access."),
  Type_0_must_have_a_Symbol_iterator_method_that_returns_an_iterator: diag(2488, 1 /* Error */, "Type_0_must_have_a_Symbol_iterator_method_that_returns_an_iterator_2488", "Type '{0}' must have a '[Symbol.iterator]()' method that returns an iterator."),
  An_iterator_must_have_a_next_method: diag(2489, 1 /* Error */, "An_iterator_must_have_a_next_method_2489", "An iterator must have a 'next()' method."),
  The_type_returned_by_the_0_method_of_an_iterator_must_have_a_value_property: diag(2490, 1 /* Error */, "The_type_returned_by_the_0_method_of_an_iterator_must_have_a_value_property_2490", "The type returned by the '{0}()' method of an iterator must have a 'value' property."),
  The_left_hand_side_of_a_for_in_statement_cannot_be_a_destructuring_pattern: diag(2491, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_cannot_be_a_destructuring_pattern_2491", "The left-hand side of a 'for...in' statement cannot be a destructuring pattern."),
  Cannot_redeclare_identifier_0_in_catch_clause: diag(2492, 1 /* Error */, "Cannot_redeclare_identifier_0_in_catch_clause_2492", "Cannot redeclare identifier '{0}' in catch clause."),
  Tuple_type_0_of_length_1_has_no_element_at_index_2: diag(2493, 1 /* Error */, "Tuple_type_0_of_length_1_has_no_element_at_index_2_2493", "Tuple type '{0}' of length '{1}' has no element at index '{2}'."),
  Using_a_string_in_a_for_of_statement_is_only_supported_in_ECMAScript_5_and_higher: diag(2494, 1 /* Error */, "Using_a_string_in_a_for_of_statement_is_only_supported_in_ECMAScript_5_and_higher_2494", "Using a string in a 'for...of' statement is only supported in ECMAScript 5 and higher."),
  Type_0_is_not_an_array_type_or_a_string_type: diag(2495, 1 /* Error */, "Type_0_is_not_an_array_type_or_a_string_type_2495", "Type '{0}' is not an array type or a string type."),
  The_arguments_object_cannot_be_referenced_in_an_arrow_function_in_ES5_Consider_using_a_standard_function_expression: diag(2496, 1 /* Error */, "The_arguments_object_cannot_be_referenced_in_an_arrow_function_in_ES5_Consider_using_a_standard_func_2496", "The 'arguments' object cannot be referenced in an arrow function in ES5. Consider using a standard function expression."),
  This_module_can_only_be_referenced_with_ECMAScript_imports_Slashexports_by_turning_on_the_0_flag_and_referencing_its_default_export: diag(2497, 1 /* Error */, "This_module_can_only_be_referenced_with_ECMAScript_imports_Slashexports_by_turning_on_the_0_flag_and_2497", "This module can only be referenced with ECMAScript imports/exports by turning on the '{0}' flag and referencing its default export."),
  Module_0_uses_export_and_cannot_be_used_with_export_Asterisk: diag(2498, 1 /* Error */, "Module_0_uses_export_and_cannot_be_used_with_export_Asterisk_2498", "Module '{0}' uses 'export ***REMOVED***' and cannot be used with 'export *'."),
  An_interface_can_only_extend_an_identifier_Slashqualified_name_with_optional_type_arguments: diag(2499, 1 /* Error */, "An_interface_can_only_extend_an_identifier_Slashqualified_name_with_optional_type_arguments_2499", "An interface can only extend an identifier/qualified-name with optional type arguments."),
  A_class_can_only_implement_an_identifier_Slashqualified_name_with_optional_type_arguments: diag(2500, 1 /* Error */, "A_class_can_only_implement_an_identifier_Slashqualified_name_with_optional_type_arguments_2500", "A class can only implement an identifier/qualified-name with optional type arguments."),
  A_rest_element_cannot_contain_a_binding_pattern: diag(2501, 1 /* Error */, "A_rest_element_cannot_contain_a_binding_pattern_2501", "A rest element cannot contain a binding pattern."),
  _0_is_referenced_directly_or_indirectly_in_its_own_type_annotation: diag(2502, 1 /* Error */, "_0_is_referenced_directly_or_indirectly_in_its_own_type_annotation_2502", "'{0}' is referenced directly or indirectly in its own type annotation."),
  Cannot_find_namespace_0: diag(2503, 1 /* Error */, "Cannot_find_namespace_0_2503", "Cannot find namespace '{0}'."),
  Type_0_must_have_a_Symbol_asyncIterator_method_that_returns_an_async_iterator: diag(2504, 1 /* Error */, "Type_0_must_have_a_Symbol_asyncIterator_method_that_returns_an_async_iterator_2504", "Type '{0}' must have a '[Symbol.asyncIterator]()' method that returns an async iterator."),
  A_generator_cannot_have_a_void_type_annotation: diag(2505, 1 /* Error */, "A_generator_cannot_have_a_void_type_annotation_2505", "A generator cannot have a 'void' type annotation."),
  _0_is_referenced_directly_or_indirectly_in_its_own_base_expression: diag(2506, 1 /* Error */, "_0_is_referenced_directly_or_indirectly_in_its_own_base_expression_2506", "'{0}' is referenced directly or indirectly in its own base expression."),
  Type_0_is_not_a_constructor_function_type: diag(2507, 1 /* Error */, "Type_0_is_not_a_constructor_function_type_2507", "Type '{0}' is not a constructor function type."),
  No_base_constructor_has_the_specified_number_of_type_arguments: diag(2508, 1 /* Error */, "No_base_constructor_has_the_specified_number_of_type_arguments_2508", "No base constructor has the specified number of type arguments."),
  Base_constructor_return_type_0_is_not_an_object_type_or_intersection_of_object_types_with_statically_known_members: diag(2509, 1 /* Error */, "Base_constructor_return_type_0_is_not_an_object_type_or_intersection_of_object_types_with_statically_2509", "Base constructor return type '{0}' is not an object type or intersection of object types with statically known members."),
  Base_constructors_must_all_have_the_same_return_type: diag(2510, 1 /* Error */, "Base_constructors_must_all_have_the_same_return_type_2510", "Base constructors must all have the same return type."),
  Cannot_create_an_instance_of_an_abstract_class: diag(2511, 1 /* Error */, "Cannot_create_an_instance_of_an_abstract_class_2511", "Cannot create an instance of an abstract class."),
  Overload_signatures_must_all_be_abstract_or_non_abstract: diag(2512, 1 /* Error */, "Overload_signatures_must_all_be_abstract_or_non_abstract_2512", "Overload signatures must all be abstract or non-abstract."),
  Abstract_method_0_in_class_1_cannot_be_accessed_via_super_expression: diag(2513, 1 /* Error */, "Abstract_method_0_in_class_1_cannot_be_accessed_via_super_expression_2513", "Abstract method '{0}' in class '{1}' cannot be accessed via super expression."),
  A_tuple_type_cannot_be_indexed_with_a_negative_value: diag(2514, 1 /* Error */, "A_tuple_type_cannot_be_indexed_with_a_negative_value_2514", "A tuple type cannot be indexed with a negative value."),
  Non_abstract_class_0_does_not_implement_inherited_abstract_member_1_from_class_2: diag(2515, 1 /* Error */, "Non_abstract_class_0_does_not_implement_inherited_abstract_member_1_from_class_2_2515", "Non-abstract class '{0}' does not implement inherited abstract member {1} from class '{2}'."),
  All_declarations_of_an_abstract_method_must_be_consecutive: diag(2516, 1 /* Error */, "All_declarations_of_an_abstract_method_must_be_consecutive_2516", "All declarations of an abstract method must be consecutive."),
  Cannot_assign_an_abstract_constructor_type_to_a_non_abstract_constructor_type: diag(2517, 1 /* Error */, "Cannot_assign_an_abstract_constructor_type_to_a_non_abstract_constructor_type_2517", "Cannot assign an abstract constructor type to a non-abstract constructor type."),
  A_this_based_type_guard_is_not_compatible_with_a_parameter_based_type_guard: diag(2518, 1 /* Error */, "A_this_based_type_guard_is_not_compatible_with_a_parameter_based_type_guard_2518", "A 'this'-based type guard is not compatible with a parameter-based type guard."),
  An_async_iterator_must_have_a_next_method: diag(2519, 1 /* Error */, "An_async_iterator_must_have_a_next_method_2519", "An async iterator must have a 'next()' method."),
  Duplicate_identifier_0_Compiler_uses_declaration_1_to_support_async_functions: diag(2520, 1 /* Error */, "Duplicate_identifier_0_Compiler_uses_declaration_1_to_support_async_functions_2520", "Duplicate identifier '{0}'. Compiler uses declaration '{1}' to support async functions."),
  The_arguments_object_cannot_be_referenced_in_an_async_function_or_method_in_ES5_Consider_using_a_standard_function_or_method: diag(2522, 1 /* Error */, "The_arguments_object_cannot_be_referenced_in_an_async_function_or_method_in_ES5_Consider_using_a_sta_2522", "The 'arguments' object cannot be referenced in an async function or method in ES5. Consider using a standard function or method."),
  yield_expressions_cannot_be_used_in_a_parameter_initializer: diag(2523, 1 /* Error */, "yield_expressions_cannot_be_used_in_a_parameter_initializer_2523", "'yield' expressions cannot be used in a parameter initializer."),
  await_expressions_cannot_be_used_in_a_parameter_initializer: diag(2524, 1 /* Error */, "await_expressions_cannot_be_used_in_a_parameter_initializer_2524", "'await' expressions cannot be used in a parameter initializer."),
  A_this_type_is_available_only_in_a_non_static_member_of_a_class_or_interface: diag(2526, 1 /* Error */, "A_this_type_is_available_only_in_a_non_static_member_of_a_class_or_interface_2526", "A 'this' type is available only in a non-static member of a class or interface."),
  The_inferred_type_of_0_references_an_inaccessible_1_type_A_type_annotation_is_necessary: diag(2527, 1 /* Error */, "The_inferred_type_of_0_references_an_inaccessible_1_type_A_type_annotation_is_necessary_2527", "The inferred type of '{0}' references an inaccessible '{1}' type. A type annotation is necessary."),
  A_module_cannot_have_multiple_default_exports: diag(2528, 1 /* Error */, "A_module_cannot_have_multiple_default_exports_2528", "A module cannot have multiple default exports."),
  Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module_containing_async_functions: diag(2529, 1 /* Error */, "Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module_containing_async_func_2529", "Duplicate identifier '{0}'. Compiler reserves name '{1}' in top level scope of a module containing async functions."),
  Property_0_is_incompatible_with_index_signature: diag(2530, 1 /* Error */, "Property_0_is_incompatible_with_index_signature_2530", "Property '{0}' is incompatible with index signature."),
  Object_is_possibly_null: diag(2531, 1 /* Error */, "Object_is_possibly_null_2531", "Object is possibly 'null'."),
  Object_is_possibly_undefined: diag(2532, 1 /* Error */, "Object_is_possibly_undefined_2532", "Object is possibly 'undefined'."),
  Object_is_possibly_null_or_undefined: diag(2533, 1 /* Error */, "Object_is_possibly_null_or_undefined_2533", "Object is possibly 'null' or 'undefined'."),
  A_function_returning_never_cannot_have_a_reachable_end_point: diag(2534, 1 /* Error */, "A_function_returning_never_cannot_have_a_reachable_end_point_2534", "A function returning 'never' cannot have a reachable end point."),
  Type_0_cannot_be_used_to_index_type_1: diag(2536, 1 /* Error */, "Type_0_cannot_be_used_to_index_type_1_2536", "Type '{0}' cannot be used to index type '{1}'."),
  Type_0_has_no_matching_index_signature_for_type_1: diag(2537, 1 /* Error */, "Type_0_has_no_matching_index_signature_for_type_1_2537", "Type '{0}' has no matching index signature for type '{1}'."),
  Type_0_cannot_be_used_as_an_index_type: diag(2538, 1 /* Error */, "Type_0_cannot_be_used_as_an_index_type_2538", "Type '{0}' cannot be used as an index type."),
  Cannot_assign_to_0_because_it_is_not_a_variable: diag(2539, 1 /* Error */, "Cannot_assign_to_0_because_it_is_not_a_variable_2539", "Cannot assign to '{0}' because it is not a variable."),
  Cannot_assign_to_0_because_it_is_a_read_only_property: diag(2540, 1 /* Error */, "Cannot_assign_to_0_because_it_is_a_read_only_property_2540", "Cannot assign to '{0}' because it is a read-only property."),
  Index_signature_in_type_0_only_permits_reading: diag(2542, 1 /* Error */, "Index_signature_in_type_0_only_permits_reading_2542", "Index signature in type '{0}' only permits reading."),
  Duplicate_identifier_newTarget_Compiler_uses_variable_declaration_newTarget_to_capture_new_target_meta_property_reference: diag(2543, 1 /* Error */, "Duplicate_identifier_newTarget_Compiler_uses_variable_declaration_newTarget_to_capture_new_target_me_2543", "Duplicate identifier '_newTarget'. Compiler uses variable declaration '_newTarget' to capture 'new.target' meta-property reference."),
  Expression_resolves_to_variable_declaration_newTarget_that_compiler_uses_to_capture_new_target_meta_property_reference: diag(2544, 1 /* Error */, "Expression_resolves_to_variable_declaration_newTarget_that_compiler_uses_to_capture_new_target_meta__2544", "Expression resolves to variable declaration '_newTarget' that compiler uses to capture 'new.target' meta-property reference."),
  A_mixin_class_must_have_a_constructor_with_a_single_rest_parameter_of_type_any: diag(2545, 1 /* Error */, "A_mixin_class_must_have_a_constructor_with_a_single_rest_parameter_of_type_any_2545", "A mixin class must have a constructor with a single rest parameter of type 'any[]'."),
  The_type_returned_by_the_0_method_of_an_async_iterator_must_be_a_promise_for_a_type_with_a_value_property: diag(2547, 1 /* Error */, "The_type_returned_by_the_0_method_of_an_async_iterator_must_be_a_promise_for_a_type_with_a_value_pro_2547", "The type returned by the '{0}()' method of an async iterator must be a promise for a type with a 'value' property."),
  Type_0_is_not_an_array_type_or_does_not_have_a_Symbol_iterator_method_that_returns_an_iterator: diag(2548, 1 /* Error */, "Type_0_is_not_an_array_type_or_does_not_have_a_Symbol_iterator_method_that_returns_an_iterator_2548", "Type '{0}' is not an array type or does not have a '[Symbol.iterator]()' method that returns an iterator."),
  Type_0_is_not_an_array_type_or_a_string_type_or_does_not_have_a_Symbol_iterator_method_that_returns_an_iterator: diag(2549, 1 /* Error */, "Type_0_is_not_an_array_type_or_a_string_type_or_does_not_have_a_Symbol_iterator_method_that_returns__2549", "Type '{0}' is not an array type or a string type or does not have a '[Symbol.iterator]()' method that returns an iterator."),
  Property_0_does_not_exist_on_type_1_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_2_or_later: diag(2550, 1 /* Error */, "Property_0_does_not_exist_on_type_1_Do_you_need_to_change_your_target_library_Try_changing_the_lib_c_2550", "Property '{0}' does not exist on type '{1}'. Do you need to change your target library? Try changing the 'lib' compiler option to '{2}' or later."),
  Property_0_does_not_exist_on_type_1_Did_you_mean_2: diag(2551, 1 /* Error */, "Property_0_does_not_exist_on_type_1_Did_you_mean_2_2551", "Property '{0}' does not exist on type '{1}'. Did you mean '{2}'?"),
  Cannot_find_name_0_Did_you_mean_1: diag(2552, 1 /* Error */, "Cannot_find_name_0_Did_you_mean_1_2552", "Cannot find name '{0}'. Did you mean '{1}'?"),
  Computed_values_are_not_permitted_in_an_enum_with_string_valued_members: diag(2553, 1 /* Error */, "Computed_values_are_not_permitted_in_an_enum_with_string_valued_members_2553", "Computed values are not permitted in an enum with string valued members."),
  Expected_0_arguments_but_got_1: diag(2554, 1 /* Error */, "Expected_0_arguments_but_got_1_2554", "Expected {0} arguments, but got {1}."),
  Expected_at_least_0_arguments_but_got_1: diag(2555, 1 /* Error */, "Expected_at_least_0_arguments_but_got_1_2555", "Expected at least {0} arguments, but got {1}."),
  A_spread_argument_must_either_have_a_tuple_type_or_be_passed_to_a_rest_parameter: diag(2556, 1 /* Error */, "A_spread_argument_must_either_have_a_tuple_type_or_be_passed_to_a_rest_parameter_2556", "A spread argument must either have a tuple type or be passed to a rest parameter."),
  Expected_0_type_arguments_but_got_1: diag(2558, 1 /* Error */, "Expected_0_type_arguments_but_got_1_2558", "Expected {0} type arguments, but got {1}."),
  Type_0_has_no_properties_in_common_with_type_1: diag(2559, 1 /* Error */, "Type_0_has_no_properties_in_common_with_type_1_2559", "Type '{0}' has no properties in common with type '{1}'."),
  Value_of_type_0_has_no_properties_in_common_with_type_1_Did_you_mean_to_call_it: diag(2560, 1 /* Error */, "Value_of_type_0_has_no_properties_in_common_with_type_1_Did_you_mean_to_call_it_2560", "Value of type '{0}' has no properties in common with type '{1}'. Did you mean to call it?"),
  Object_literal_may_only_specify_known_properties_but_0_does_not_exist_in_type_1_Did_you_mean_to_write_2: diag(2561, 1 /* Error */, "Object_literal_may_only_specify_known_properties_but_0_does_not_exist_in_type_1_Did_you_mean_to_writ_2561", "Object literal may only specify known properties, but '{0}' does not exist in type '{1}'. Did you mean to write '{2}'?"),
  Base_class_expressions_cannot_reference_class_type_parameters: diag(2562, 1 /* Error */, "Base_class_expressions_cannot_reference_class_type_parameters_2562", "Base class expressions cannot reference class type parameters."),
  The_containing_function_or_module_body_is_too_large_for_control_flow_analysis: diag(2563, 1 /* Error */, "The_containing_function_or_module_body_is_too_large_for_control_flow_analysis_2563", "The containing function or module body is too large for control flow analysis."),
  Property_0_has_no_initializer_and_is_not_definitely_assigned_in_the_constructor: diag(2564, 1 /* Error */, "Property_0_has_no_initializer_and_is_not_definitely_assigned_in_the_constructor_2564", "Property '{0}' has no initializer and is not definitely assigned in the constructor."),
  Property_0_is_used_before_being_assigned: diag(2565, 1 /* Error */, "Property_0_is_used_before_being_assigned_2565", "Property '{0}' is used before being assigned."),
  A_rest_element_cannot_have_a_property_name: diag(2566, 1 /* Error */, "A_rest_element_cannot_have_a_property_name_2566", "A rest element cannot have a property name."),
  Enum_declarations_can_only_merge_with_namespace_or_other_enum_declarations: diag(2567, 1 /* Error */, "Enum_declarations_can_only_merge_with_namespace_or_other_enum_declarations_2567", "Enum declarations can only merge with namespace or other enum declarations."),
  Property_0_may_not_exist_on_type_1_Did_you_mean_2: diag(2568, 1 /* Error */, "Property_0_may_not_exist_on_type_1_Did_you_mean_2_2568", "Property '{0}' may not exist on type '{1}'. Did you mean '{2}'?"),
  Could_not_find_name_0_Did_you_mean_1: diag(2570, 1 /* Error */, "Could_not_find_name_0_Did_you_mean_1_2570", "Could not find name '{0}'. Did you mean '{1}'?"),
  Object_is_of_type_unknown: diag(2571, 1 /* Error */, "Object_is_of_type_unknown_2571", "Object is of type 'unknown'."),
  A_rest_element_type_must_be_an_array_type: diag(2574, 1 /* Error */, "A_rest_element_type_must_be_an_array_type_2574", "A rest element type must be an array type."),
  No_overload_expects_0_arguments_but_overloads_do_exist_that_expect_either_1_or_2_arguments: diag(2575, 1 /* Error */, "No_overload_expects_0_arguments_but_overloads_do_exist_that_expect_either_1_or_2_arguments_2575", "No overload expects {0} arguments, but overloads do exist that expect either {1} or {2} arguments."),
  Property_0_does_not_exist_on_type_1_Did_you_mean_to_access_the_static_member_2_instead: diag(2576, 1 /* Error */, "Property_0_does_not_exist_on_type_1_Did_you_mean_to_access_the_static_member_2_instead_2576", "Property '{0}' does not exist on type '{1}'. Did you mean to access the static member '{2}' instead?"),
  Return_type_annotation_circularly_references_itself: diag(2577, 1 /* Error */, "Return_type_annotation_circularly_references_itself_2577", "Return type annotation circularly references itself."),
  Unused_ts_expect_error_directive: diag(2578, 1 /* Error */, "Unused_ts_expect_error_directive_2578", "Unused '@ts-expect-error' directive."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashnode: diag(2580, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashno_2580", "Cannot find name '{0}'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node`."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slashjquery: diag(2581, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slash_2581", "Cannot find name '{0}'. Do you need to install type definitions for jQuery? Try `npm i --save-dev @types/jquery`."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_types_Slashjest_or_npm_i_save_dev_types_Slashmocha: diag(2582, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_type_2582", "Cannot find name '{0}'. Do you need to install type definitions for a test runner? Try `npm i --save-dev @types/jest` or `npm i --save-dev @types/mocha`."),
  Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_1_or_later: diag(2583, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_2583", "Cannot find name '{0}'. Do you need to change your target library? Try changing the 'lib' compiler option to '{1}' or later."),
  Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_include_dom: diag(2584, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_2584", "Cannot find name '{0}'. Do you need to change your target library? Try changing the 'lib' compiler option to include 'dom'."),
  _0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_es2015_or_later: diag(2585, 1 /* Error */, "_0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Do_you_need_to_change_your_target_library_2585", "'{0}' only refers to a type, but is being used as a value here. Do you need to change your target library? Try changing the 'lib' compiler option to es2015 or later."),
  Cannot_assign_to_0_because_it_is_a_constant: diag(2588, 1 /* Error */, "Cannot_assign_to_0_because_it_is_a_constant_2588", "Cannot assign to '{0}' because it is a constant."),
  Type_instantiation_is_excessively_deep_and_possibly_infinite: diag(2589, 1 /* Error */, "Type_instantiation_is_excessively_deep_and_possibly_infinite_2589", "Type instantiation is excessively deep and possibly infinite."),
  Expression_produces_a_union_type_that_is_too_complex_to_represent: diag(2590, 1 /* Error */, "Expression_produces_a_union_type_that_is_too_complex_to_represent_2590", "Expression produces a union type that is too complex to represent."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashnode_and_then_add_node_to_the_types_field_in_your_tsconfig: diag(2591, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashno_2591", "Cannot find name '{0}'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slashjquery_and_then_add_jquery_to_the_types_field_in_your_tsconfig: diag(2592, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slash_2592", "Cannot find name '{0}'. Do you need to install type definitions for jQuery? Try `npm i --save-dev @types/jquery` and then add 'jquery' to the types field in your tsconfig."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_types_Slashjest_or_npm_i_save_dev_types_Slashmocha_and_then_add_jest_or_mocha_to_the_types_field_in_your_tsconfig: diag(2593, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_type_2593", "Cannot find name '{0}'. Do you need to install type definitions for a test runner? Try `npm i --save-dev @types/jest` or `npm i --save-dev @types/mocha` and then add 'jest' or 'mocha' to the types field in your tsconfig."),
  This_module_is_declared_with_export_and_can_only_be_used_with_a_default_import_when_using_the_0_flag: diag(2594, 1 /* Error */, "This_module_is_declared_with_export_and_can_only_be_used_with_a_default_import_when_using_the_0_flag_2594", "This module is declared with 'export ***REMOVED***', and can only be used with a default import when using the '{0}' flag."),
  _0_can_only_be_imported_by_using_a_default_import: diag(2595, 1 /* Error */, "_0_can_only_be_imported_by_using_a_default_import_2595", "'{0}' can only be imported by using a default import."),
  _0_can_only_be_imported_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import: diag(2596, 1 /* Error */, "_0_can_only_be_imported_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import_2596", "'{0}' can only be imported by turning on the 'esModuleInterop' flag and using a default import."),
  _0_can_only_be_imported_by_using_a_require_call_or_by_using_a_default_import: diag(2597, 1 /* Error */, "_0_can_only_be_imported_by_using_a_require_call_or_by_using_a_default_import_2597", "'{0}' can only be imported by using a 'require' call or by using a default import."),
  _0_can_only_be_imported_by_using_a_require_call_or_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import: diag(2598, 1 /* Error */, "_0_can_only_be_imported_by_using_a_require_call_or_by_turning_on_the_esModuleInterop_flag_and_using__2598", "'{0}' can only be imported by using a 'require' call or by turning on the 'esModuleInterop' flag and using a default import."),
  JSX_element_implicitly_has_type_any_because_the_global_type_JSX_Element_does_not_exist: diag(2602, 1 /* Error */, "JSX_element_implicitly_has_type_any_because_the_global_type_JSX_Element_does_not_exist_2602", "JSX element implicitly has type 'any' because the global type 'JSX.Element' does not exist."),
  Property_0_in_type_1_is_not_assignable_to_type_2: diag(2603, 1 /* Error */, "Property_0_in_type_1_is_not_assignable_to_type_2_2603", "Property '{0}' in type '{1}' is not assignable to type '{2}'."),
  JSX_element_type_0_does_not_have_any_construct_or_call_signatures: diag(2604, 1 /* Error */, "JSX_element_type_0_does_not_have_any_construct_or_call_signatures_2604", "JSX element type '{0}' does not have any construct or call signatures."),
  Property_0_of_JSX_spread_attribute_is_not_assignable_to_target_property: diag(2606, 1 /* Error */, "Property_0_of_JSX_spread_attribute_is_not_assignable_to_target_property_2606", "Property '{0}' of JSX spread attribute is not assignable to target property."),
  JSX_element_class_does_not_support_attributes_because_it_does_not_have_a_0_property: diag(2607, 1 /* Error */, "JSX_element_class_does_not_support_attributes_because_it_does_not_have_a_0_property_2607", "JSX element class does not support attributes because it does not have a '{0}' property."),
  The_global_type_JSX_0_may_not_have_more_than_one_property: diag(2608, 1 /* Error */, "The_global_type_JSX_0_may_not_have_more_than_one_property_2608", "The global type 'JSX.{0}' may not have more than one property."),
  JSX_spread_child_must_be_an_array_type: diag(2609, 1 /* Error */, "JSX_spread_child_must_be_an_array_type_2609", "JSX spread child must be an array type."),
  _0_is_defined_as_an_accessor_in_class_1_but_is_overridden_here_in_2_as_an_instance_property: diag(2610, 1 /* Error */, "_0_is_defined_as_an_accessor_in_class_1_but_is_overridden_here_in_2_as_an_instance_property_2610", "'{0}' is defined as an accessor in class '{1}', but is overridden here in '{2}' as an instance property."),
  _0_is_defined_as_a_property_in_class_1_but_is_overridden_here_in_2_as_an_accessor: diag(2611, 1 /* Error */, "_0_is_defined_as_a_property_in_class_1_but_is_overridden_here_in_2_as_an_accessor_2611", "'{0}' is defined as a property in class '{1}', but is overridden here in '{2}' as an accessor."),
  Property_0_will_overwrite_the_base_property_in_1_If_this_is_intentional_add_an_initializer_Otherwise_add_a_declare_modifier_or_remove_the_redundant_declaration: diag(2612, 1 /* Error */, "Property_0_will_overwrite_the_base_property_in_1_If_this_is_intentional_add_an_initializer_Otherwise_2612", "Property '{0}' will overwrite the base property in '{1}'. If this is intentional, add an initializer. Otherwise, add a 'declare' modifier or remove the redundant declaration."),
  Module_0_has_no_default_export_Did_you_mean_to_use_import_1_from_0_instead: diag(2613, 1 /* Error */, "Module_0_has_no_default_export_Did_you_mean_to_use_import_1_from_0_instead_2613", "Module '{0}' has no default export. Did you mean to use 'import { {1} } from {0}' instead?"),
  Module_0_has_no_exported_member_1_Did_you_mean_to_use_import_1_from_0_instead: diag(2614, 1 /* Error */, "Module_0_has_no_exported_member_1_Did_you_mean_to_use_import_1_from_0_instead_2614", "Module '{0}' has no exported member '{1}'. Did you mean to use 'import {1} from {0}' instead?"),
  Type_of_property_0_circularly_references_itself_in_mapped_type_1: diag(2615, 1 /* Error */, "Type_of_property_0_circularly_references_itself_in_mapped_type_1_2615", "Type of property '{0}' circularly references itself in mapped type '{1}'."),
  _0_can_only_be_imported_by_using_import_1_require_2_or_a_default_import: diag(2616, 1 /* Error */, "_0_can_only_be_imported_by_using_import_1_require_2_or_a_default_import_2616", "'{0}' can only be imported by using 'import {1} ***REMOVED*** require({2})' or a default import."),
  _0_can_only_be_imported_by_using_import_1_require_2_or_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import: diag(2617, 1 /* Error */, "_0_can_only_be_imported_by_using_import_1_require_2_or_by_turning_on_the_esModuleInterop_flag_and_us_2617", "'{0}' can only be imported by using 'import {1} ***REMOVED*** require({2})' or by turning on the 'esModuleInterop' flag and using a default import."),
  Source_has_0_element_s_but_target_requires_1: diag(2618, 1 /* Error */, "Source_has_0_element_s_but_target_requires_1_2618", "Source has {0} element(s) but target requires {1}."),
  Source_has_0_element_s_but_target_allows_only_1: diag(2619, 1 /* Error */, "Source_has_0_element_s_but_target_allows_only_1_2619", "Source has {0} element(s) but target allows only {1}."),
  Target_requires_0_element_s_but_source_may_have_fewer: diag(2620, 1 /* Error */, "Target_requires_0_element_s_but_source_may_have_fewer_2620", "Target requires {0} element(s) but source may have fewer."),
  Target_allows_only_0_element_s_but_source_may_have_more: diag(2621, 1 /* Error */, "Target_allows_only_0_element_s_but_source_may_have_more_2621", "Target allows only {0} element(s) but source may have more."),
  Source_provides_no_match_for_required_element_at_position_0_in_target: diag(2623, 1 /* Error */, "Source_provides_no_match_for_required_element_at_position_0_in_target_2623", "Source provides no match for required element at position {0} in target."),
  Source_provides_no_match_for_variadic_element_at_position_0_in_target: diag(2624, 1 /* Error */, "Source_provides_no_match_for_variadic_element_at_position_0_in_target_2624", "Source provides no match for variadic element at position {0} in target."),
  Variadic_element_at_position_0_in_source_does_not_match_element_at_position_1_in_target: diag(2625, 1 /* Error */, "Variadic_element_at_position_0_in_source_does_not_match_element_at_position_1_in_target_2625", "Variadic element at position {0} in source does not match element at position {1} in target."),
  Type_at_position_0_in_source_is_not_compatible_with_type_at_position_1_in_target: diag(2626, 1 /* Error */, "Type_at_position_0_in_source_is_not_compatible_with_type_at_position_1_in_target_2626", "Type at position {0} in source is not compatible with type at position {1} in target."),
  Type_at_positions_0_through_1_in_source_is_not_compatible_with_type_at_position_2_in_target: diag(2627, 1 /* Error */, "Type_at_positions_0_through_1_in_source_is_not_compatible_with_type_at_position_2_in_target_2627", "Type at positions {0} through {1} in source is not compatible with type at position {2} in target."),
  Cannot_assign_to_0_because_it_is_an_enum: diag(2628, 1 /* Error */, "Cannot_assign_to_0_because_it_is_an_enum_2628", "Cannot assign to '{0}' because it is an enum."),
  Cannot_assign_to_0_because_it_is_a_class: diag(2629, 1 /* Error */, "Cannot_assign_to_0_because_it_is_a_class_2629", "Cannot assign to '{0}' because it is a class."),
  Cannot_assign_to_0_because_it_is_a_function: diag(2630, 1 /* Error */, "Cannot_assign_to_0_because_it_is_a_function_2630", "Cannot assign to '{0}' because it is a function."),
  Cannot_assign_to_0_because_it_is_a_namespace: diag(2631, 1 /* Error */, "Cannot_assign_to_0_because_it_is_a_namespace_2631", "Cannot assign to '{0}' because it is a namespace."),
  Cannot_assign_to_0_because_it_is_an_import: diag(2632, 1 /* Error */, "Cannot_assign_to_0_because_it_is_an_import_2632", "Cannot assign to '{0}' because it is an import."),
  JSX_property_access_expressions_cannot_include_JSX_namespace_names: diag(2633, 1 /* Error */, "JSX_property_access_expressions_cannot_include_JSX_namespace_names_2633", "JSX property access expressions cannot include JSX namespace names"),
  _0_index_signatures_are_incompatible: diag(2634, 1 /* Error */, "_0_index_signatures_are_incompatible_2634", "'{0}' index signatures are incompatible."),
  Type_0_has_no_signatures_for_which_the_type_argument_list_is_applicable: diag(2635, 1 /* Error */, "Type_0_has_no_signatures_for_which_the_type_argument_list_is_applicable_2635", "Type '{0}' has no signatures for which the type argument list is applicable."),
  Type_0_is_not_assignable_to_type_1_as_implied_by_variance_annotation: diag(2636, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_as_implied_by_variance_annotation_2636", "Type '{0}' is not assignable to type '{1}' as implied by variance annotation."),
  Variance_annotations_are_only_supported_in_type_aliases_for_object_function_constructor_and_mapped_types: diag(2637, 1 /* Error */, "Variance_annotations_are_only_supported_in_type_aliases_for_object_function_constructor_and_mapped_t_2637", "Variance annotations are only supported in type aliases for object, function, constructor, and mapped types."),
  Type_0_may_represent_a_primitive_value_which_is_not_permitted_as_the_right_operand_of_the_in_operator: diag(2638, 1 /* Error */, "Type_0_may_represent_a_primitive_value_which_is_not_permitted_as_the_right_operand_of_the_in_operato_2638", "Type '{0}' may represent a primitive value, which is not permitted as the right operand of the 'in' operator."),
  React_components_cannot_include_JSX_namespace_names: diag(2639, 1 /* Error */, "React_components_cannot_include_JSX_namespace_names_2639", "React components cannot include JSX namespace names"),
  Cannot_augment_module_0_with_value_exports_because_it_resolves_to_a_non_module_entity: diag(2649, 1 /* Error */, "Cannot_augment_module_0_with_value_exports_because_it_resolves_to_a_non_module_entity_2649", "Cannot augment module '{0}' with value exports because it resolves to a non-module entity."),
  Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1_and_2_more: diag(2650, 1 /* Error */, "Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1_and__2650", "Non-abstract class expression is missing implementations for the following members of '{0}': {1} and {2} more."),
  A_member_initializer_in_a_enum_declaration_cannot_reference_members_declared_after_it_including_members_defined_in_other_enums: diag(2651, 1 /* Error */, "A_member_initializer_in_a_enum_declaration_cannot_reference_members_declared_after_it_including_memb_2651", "A member initializer in a enum declaration cannot reference members declared after it, including members defined in other enums."),
  Merged_declaration_0_cannot_include_a_default_export_declaration_Consider_adding_a_separate_export_default_0_declaration_instead: diag(2652, 1 /* Error */, "Merged_declaration_0_cannot_include_a_default_export_declaration_Consider_adding_a_separate_export_d_2652", "Merged declaration '{0}' cannot include a default export declaration. Consider adding a separate 'export default {0}' declaration instead."),
  Non_abstract_class_expression_does_not_implement_inherited_abstract_member_0_from_class_1: diag(2653, 1 /* Error */, "Non_abstract_class_expression_does_not_implement_inherited_abstract_member_0_from_class_1_2653", "Non-abstract class expression does not implement inherited abstract member '{0}' from class '{1}'."),
  Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2: diag(2654, 1 /* Error */, "Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2_2654", "Non-abstract class '{0}' is missing implementations for the following members of '{1}': {2}."),
  Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2_and_3_more: diag(2655, 1 /* Error */, "Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2_and_3_more_2655", "Non-abstract class '{0}' is missing implementations for the following members of '{1}': {2} and {3} more."),
  Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1: diag(2656, 1 /* Error */, "Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1_2656", "Non-abstract class expression is missing implementations for the following members of '{0}': {1}."),
  JSX_expressions_must_have_one_parent_element: diag(2657, 1 /* Error */, "JSX_expressions_must_have_one_parent_element_2657", "JSX expressions must have one parent element."),
  Type_0_provides_no_match_for_the_signature_1: diag(2658, 1 /* Error */, "Type_0_provides_no_match_for_the_signature_1_2658", "Type '{0}' provides no match for the signature '{1}'."),
  super_is_only_allowed_in_members_of_object_literal_expressions_when_option_target_is_ES2015_or_higher: diag(2659, 1 /* Error */, "super_is_only_allowed_in_members_of_object_literal_expressions_when_option_target_is_ES2015_or_highe_2659", "'super' is only allowed in members of object literal expressions when option 'target' is 'ES2015' or higher."),
  super_can_only_be_referenced_in_members_of_derived_classes_or_object_literal_expressions: diag(2660, 1 /* Error */, "super_can_only_be_referenced_in_members_of_derived_classes_or_object_literal_expressions_2660", "'super' can only be referenced in members of derived classes or object literal expressions."),
  Cannot_export_0_Only_local_declarations_can_be_exported_from_a_module: diag(2661, 1 /* Error */, "Cannot_export_0_Only_local_declarations_can_be_exported_from_a_module_2661", "Cannot export '{0}'. Only local declarations can be exported from a module."),
  Cannot_find_name_0_Did_you_mean_the_static_member_1_0: diag(2662, 1 /* Error */, "Cannot_find_name_0_Did_you_mean_the_static_member_1_0_2662", "Cannot find name '{0}'. Did you mean the static member '{1}.{0}'?"),
  Cannot_find_name_0_Did_you_mean_the_instance_member_this_0: diag(2663, 1 /* Error */, "Cannot_find_name_0_Did_you_mean_the_instance_member_this_0_2663", "Cannot find name '{0}'. Did you mean the instance member 'this.{0}'?"),
  Invalid_module_name_in_augmentation_module_0_cannot_be_found: diag(2664, 1 /* Error */, "Invalid_module_name_in_augmentation_module_0_cannot_be_found_2664", "Invalid module name in augmentation, module '{0}' cannot be found."),
  Invalid_module_name_in_augmentation_Module_0_resolves_to_an_untyped_module_at_1_which_cannot_be_augmented: diag(2665, 1 /* Error */, "Invalid_module_name_in_augmentation_Module_0_resolves_to_an_untyped_module_at_1_which_cannot_be_augm_2665", "Invalid module name in augmentation. Module '{0}' resolves to an untyped module at '{1}', which cannot be augmented."),
  Exports_and_export_assignments_are_not_permitted_in_module_augmentations: diag(2666, 1 /* Error */, "Exports_and_export_assignments_are_not_permitted_in_module_augmentations_2666", "Exports and export assignments are not permitted in module augmentations."),
  Imports_are_not_permitted_in_module_augmentations_Consider_moving_them_to_the_enclosing_external_module: diag(2667, 1 /* Error */, "Imports_are_not_permitted_in_module_augmentations_Consider_moving_them_to_the_enclosing_external_mod_2667", "Imports are not permitted in module augmentations. Consider moving them to the enclosing external module."),
  export_modifier_cannot_be_applied_to_ambient_modules_and_module_augmentations_since_they_are_always_visible: diag(2668, 1 /* Error */, "export_modifier_cannot_be_applied_to_ambient_modules_and_module_augmentations_since_they_are_always__2668", "'export' modifier cannot be applied to ambient modules and module augmentations since they are always visible."),
  Augmentations_for_the_global_scope_can_only_be_directly_nested_in_external_modules_or_ambient_module_declarations: diag(2669, 1 /* Error */, "Augmentations_for_the_global_scope_can_only_be_directly_nested_in_external_modules_or_ambient_module_2669", "Augmentations for the global scope can only be directly nested in external modules or ambient module declarations."),
  Augmentations_for_the_global_scope_should_have_declare_modifier_unless_they_appear_in_already_ambient_context: diag(2670, 1 /* Error */, "Augmentations_for_the_global_scope_should_have_declare_modifier_unless_they_appear_in_already_ambien_2670", "Augmentations for the global scope should have 'declare' modifier unless they appear in already ambient context."),
  Cannot_augment_module_0_because_it_resolves_to_a_non_module_entity: diag(2671, 1 /* Error */, "Cannot_augment_module_0_because_it_resolves_to_a_non_module_entity_2671", "Cannot augment module '{0}' because it resolves to a non-module entity."),
  Cannot_assign_a_0_constructor_type_to_a_1_constructor_type: diag(2672, 1 /* Error */, "Cannot_assign_a_0_constructor_type_to_a_1_constructor_type_2672", "Cannot assign a '{0}' constructor type to a '{1}' constructor type."),
  Constructor_of_class_0_is_private_and_only_accessible_within_the_class_declaration: diag(2673, 1 /* Error */, "Constructor_of_class_0_is_private_and_only_accessible_within_the_class_declaration_2673", "Constructor of class '{0}' is private and only accessible within the class declaration."),
  Constructor_of_class_0_is_protected_and_only_accessible_within_the_class_declaration: diag(2674, 1 /* Error */, "Constructor_of_class_0_is_protected_and_only_accessible_within_the_class_declaration_2674", "Constructor of class '{0}' is protected and only accessible within the class declaration."),
  Cannot_extend_a_class_0_Class_constructor_is_marked_as_private: diag(2675, 1 /* Error */, "Cannot_extend_a_class_0_Class_constructor_is_marked_as_private_2675", "Cannot extend a class '{0}'. Class constructor is marked as private."),
  Accessors_must_both_be_abstract_or_non_abstract: diag(2676, 1 /* Error */, "Accessors_must_both_be_abstract_or_non_abstract_2676", "Accessors must both be abstract or non-abstract."),
  A_type_predicate_s_type_must_be_assignable_to_its_parameter_s_type: diag(2677, 1 /* Error */, "A_type_predicate_s_type_must_be_assignable_to_its_parameter_s_type_2677", "A type predicate's type must be assignable to its parameter's type."),
  Type_0_is_not_comparable_to_type_1: diag(2678, 1 /* Error */, "Type_0_is_not_comparable_to_type_1_2678", "Type '{0}' is not comparable to type '{1}'."),
  A_function_that_is_called_with_the_new_keyword_cannot_have_a_this_type_that_is_void: diag(2679, 1 /* Error */, "A_function_that_is_called_with_the_new_keyword_cannot_have_a_this_type_that_is_void_2679", "A function that is called with the 'new' keyword cannot have a 'this' type that is 'void'."),
  A_0_parameter_must_be_the_first_parameter: diag(2680, 1 /* Error */, "A_0_parameter_must_be_the_first_parameter_2680", "A '{0}' parameter must be the first parameter."),
  A_constructor_cannot_have_a_this_parameter: diag(2681, 1 /* Error */, "A_constructor_cannot_have_a_this_parameter_2681", "A constructor cannot have a 'this' parameter."),
  this_implicitly_has_type_any_because_it_does_not_have_a_type_annotation: diag(2683, 1 /* Error */, "this_implicitly_has_type_any_because_it_does_not_have_a_type_annotation_2683", "'this' implicitly has type 'any' because it does not have a type annotation."),
  The_this_context_of_type_0_is_not_assignable_to_method_s_this_of_type_1: diag(2684, 1 /* Error */, "The_this_context_of_type_0_is_not_assignable_to_method_s_this_of_type_1_2684", "The 'this' context of type '{0}' is not assignable to method's 'this' of type '{1}'."),
  The_this_types_of_each_signature_are_incompatible: diag(2685, 1 /* Error */, "The_this_types_of_each_signature_are_incompatible_2685", "The 'this' types of each signature are incompatible."),
  _0_refers_to_a_UMD_global_but_the_current_file_is_a_module_Consider_adding_an_import_instead: diag(2686, 1 /* Error */, "_0_refers_to_a_UMD_global_but_the_current_file_is_a_module_Consider_adding_an_import_instead_2686", "'{0}' refers to a UMD global, but the current file is a module. Consider adding an import instead."),
  All_declarations_of_0_must_have_identical_modifiers: diag(2687, 1 /* Error */, "All_declarations_of_0_must_have_identical_modifiers_2687", "All declarations of '{0}' must have identical modifiers."),
  Cannot_find_type_definition_file_for_0: diag(2688, 1 /* Error */, "Cannot_find_type_definition_file_for_0_2688", "Cannot find type definition file for '{0}'."),
  Cannot_extend_an_interface_0_Did_you_mean_implements: diag(2689, 1 /* Error */, "Cannot_extend_an_interface_0_Did_you_mean_implements_2689", "Cannot extend an interface '{0}'. Did you mean 'implements'?"),
  _0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Did_you_mean_to_use_1_in_0: diag(2690, 1 /* Error */, "_0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Did_you_mean_to_use_1_in_0_2690", "'{0}' only refers to a type, but is being used as a value here. Did you mean to use '{1} in {0}'?"),
  _0_is_a_primitive_but_1_is_a_wrapper_object_Prefer_using_0_when_possible: diag(2692, 1 /* Error */, "_0_is_a_primitive_but_1_is_a_wrapper_object_Prefer_using_0_when_possible_2692", "'{0}' is a primitive, but '{1}' is a wrapper object. Prefer using '{0}' when possible."),
  _0_only_refers_to_a_type_but_is_being_used_as_a_value_here: diag(2693, 1 /* Error */, "_0_only_refers_to_a_type_but_is_being_used_as_a_value_here_2693", "'{0}' only refers to a type, but is being used as a value here."),
  Namespace_0_has_no_exported_member_1: diag(2694, 1 /* Error */, "Namespace_0_has_no_exported_member_1_2694", "Namespace '{0}' has no exported member '{1}'."),
  Left_side_of_comma_operator_is_unused_and_has_no_side_effects: diag(
    2695,
    1 /* Error */,
    "Left_side_of_comma_operator_is_unused_and_has_no_side_effects_2695",
    "Left side of comma operator is unused and has no side effects.",
    /*reportsUnnecessary*/
    true
  ),
  The_Object_type_is_assignable_to_very_few_other_types_Did_you_mean_to_use_the_any_type_instead: diag(2696, 1 /* Error */, "The_Object_type_is_assignable_to_very_few_other_types_Did_you_mean_to_use_the_any_type_instead_2696", "The 'Object' type is assignable to very few other types. Did you mean to use the 'any' type instead?"),
  An_async_function_or_method_must_return_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_include_ES2015_in_your_lib_option: diag(2697, 1 /* Error */, "An_async_function_or_method_must_return_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_in_2697", "An async function or method must return a 'Promise'. Make sure you have a declaration for 'Promise' or include 'ES2015' in your '--lib' option."),
  Spread_types_may_only_be_created_from_object_types: diag(2698, 1 /* Error */, "Spread_types_may_only_be_created_from_object_types_2698", "Spread types may only be created from object types."),
  Static_property_0_conflicts_with_built_in_property_Function_0_of_constructor_function_1: diag(2699, 1 /* Error */, "Static_property_0_conflicts_with_built_in_property_Function_0_of_constructor_function_1_2699", "Static property '{0}' conflicts with built-in property 'Function.{0}' of constructor function '{1}'."),
  Rest_types_may_only_be_created_from_object_types: diag(2700, 1 /* Error */, "Rest_types_may_only_be_created_from_object_types_2700", "Rest types may only be created from object types."),
  The_target_of_an_object_rest_assignment_must_be_a_variable_or_a_property_access: diag(2701, 1 /* Error */, "The_target_of_an_object_rest_assignment_must_be_a_variable_or_a_property_access_2701", "The target of an object rest assignment must be a variable or a property access."),
  _0_only_refers_to_a_type_but_is_being_used_as_a_namespace_here: diag(2702, 1 /* Error */, "_0_only_refers_to_a_type_but_is_being_used_as_a_namespace_here_2702", "'{0}' only refers to a type, but is being used as a namespace here."),
  The_operand_of_a_delete_operator_must_be_a_property_reference: diag(2703, 1 /* Error */, "The_operand_of_a_delete_operator_must_be_a_property_reference_2703", "The operand of a 'delete' operator must be a property reference."),
  The_operand_of_a_delete_operator_cannot_be_a_read_only_property: diag(2704, 1 /* Error */, "The_operand_of_a_delete_operator_cannot_be_a_read_only_property_2704", "The operand of a 'delete' operator cannot be a read-only property."),
  An_async_function_or_method_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_the_Promise_constructor_or_include_ES2015_in_your_lib_option: diag(2705, 1 /* Error */, "An_async_function_or_method_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_2705", "An async function or method in ES5 requires the 'Promise' constructor.  Make sure you have a declaration for the 'Promise' constructor or include 'ES2015' in your '--lib' option."),
  Required_type_parameters_may_not_follow_optional_type_parameters: diag(2706, 1 /* Error */, "Required_type_parameters_may_not_follow_optional_type_parameters_2706", "Required type parameters may not follow optional type parameters."),
  Generic_type_0_requires_between_1_and_2_type_arguments: diag(2707, 1 /* Error */, "Generic_type_0_requires_between_1_and_2_type_arguments_2707", "Generic type '{0}' requires between {1} and {2} type arguments."),
  Cannot_use_namespace_0_as_a_value: diag(2708, 1 /* Error */, "Cannot_use_namespace_0_as_a_value_2708", "Cannot use namespace '{0}' as a value."),
  Cannot_use_namespace_0_as_a_type: diag(2709, 1 /* Error */, "Cannot_use_namespace_0_as_a_type_2709", "Cannot use namespace '{0}' as a type."),
  _0_are_specified_twice_The_attribute_named_0_will_be_overwritten: diag(2710, 1 /* Error */, "_0_are_specified_twice_The_attribute_named_0_will_be_overwritten_2710", "'{0}' are specified twice. The attribute named '{0}' will be overwritten."),
  A_dynamic_import_call_returns_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_include_ES2015_in_your_lib_option: diag(2711, 1 /* Error */, "A_dynamic_import_call_returns_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_include_ES20_2711", "A dynamic import call returns a 'Promise'. Make sure you have a declaration for 'Promise' or include 'ES2015' in your '--lib' option."),
  A_dynamic_import_call_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_the_Promise_constructor_or_include_ES2015_in_your_lib_option: diag(2712, 1 /* Error */, "A_dynamic_import_call_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_t_2712", "A dynamic import call in ES5 requires the 'Promise' constructor.  Make sure you have a declaration for the 'Promise' constructor or include 'ES2015' in your '--lib' option."),
  Cannot_access_0_1_because_0_is_a_type_but_not_a_namespace_Did_you_mean_to_retrieve_the_type_of_the_property_1_in_0_with_0_1: diag(2713, 1 /* Error */, "Cannot_access_0_1_because_0_is_a_type_but_not_a_namespace_Did_you_mean_to_retrieve_the_type_of_the_p_2713", `Cannot access '{0}.{1}' because '{0}' is a type, but not a namespace. Did you mean to retrieve the type of the property '{1}' in '{0}' with '{0}["{1}"]'?`),
  The_expression_of_an_export_assignment_must_be_an_identifier_or_qualified_name_in_an_ambient_context: diag(2714, 1 /* Error */, "The_expression_of_an_export_assignment_must_be_an_identifier_or_qualified_name_in_an_ambient_context_2714", "The expression of an export assignment must be an identifier or qualified name in an ambient context."),
  Abstract_property_0_in_class_1_cannot_be_accessed_in_the_constructor: diag(2715, 1 /* Error */, "Abstract_property_0_in_class_1_cannot_be_accessed_in_the_constructor_2715", "Abstract property '{0}' in class '{1}' cannot be accessed in the constructor."),
  Type_parameter_0_has_a_circular_default: diag(2716, 1 /* Error */, "Type_parameter_0_has_a_circular_default_2716", "Type parameter '{0}' has a circular default."),
  Subsequent_property_declarations_must_have_the_same_type_Property_0_must_be_of_type_1_but_here_has_type_2: diag(2717, 1 /* Error */, "Subsequent_property_declarations_must_have_the_same_type_Property_0_must_be_of_type_1_but_here_has_t_2717", "Subsequent property declarations must have the same type.  Property '{0}' must be of type '{1}', but here has type '{2}'."),
  Duplicate_property_0: diag(2718, 1 /* Error */, "Duplicate_property_0_2718", "Duplicate property '{0}'."),
  Type_0_is_not_assignable_to_type_1_Two_different_types_with_this_name_exist_but_they_are_unrelated: diag(2719, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_Two_different_types_with_this_name_exist_but_they_are_unrelated_2719", "Type '{0}' is not assignable to type '{1}'. Two different types with this name exist, but they are unrelated."),
  Class_0_incorrectly_implements_class_1_Did_you_mean_to_extend_1_and_inherit_its_members_as_a_subclass: diag(2720, 1 /* Error */, "Class_0_incorrectly_implements_class_1_Did_you_mean_to_extend_1_and_inherit_its_members_as_a_subclas_2720", "Class '{0}' incorrectly implements class '{1}'. Did you mean to extend '{1}' and inherit its members as a subclass?"),
  Cannot_invoke_an_object_which_is_possibly_null: diag(2721, 1 /* Error */, "Cannot_invoke_an_object_which_is_possibly_null_2721", "Cannot invoke an object which is possibly 'null'."),
  Cannot_invoke_an_object_which_is_possibly_undefined: diag(2722, 1 /* Error */, "Cannot_invoke_an_object_which_is_possibly_undefined_2722", "Cannot invoke an object which is possibly 'undefined'."),
  Cannot_invoke_an_object_which_is_possibly_null_or_undefined: diag(2723, 1 /* Error */, "Cannot_invoke_an_object_which_is_possibly_null_or_undefined_2723", "Cannot invoke an object which is possibly 'null' or 'undefined'."),
  _0_has_no_exported_member_named_1_Did_you_mean_2: diag(2724, 1 /* Error */, "_0_has_no_exported_member_named_1_Did_you_mean_2_2724", "'{0}' has no exported member named '{1}'. Did you mean '{2}'?"),
  Class_name_cannot_be_Object_when_targeting_ES5_and_above_with_module_0: diag(2725, 1 /* Error */, "Class_name_cannot_be_Object_when_targeting_ES5_and_above_with_module_0_2725", "Class name cannot be 'Object' when targeting ES5 and above with module {0}."),
  Cannot_find_lib_definition_for_0: diag(2726, 1 /* Error */, "Cannot_find_lib_definition_for_0_2726", "Cannot find lib definition for '{0}'."),
  Cannot_find_lib_definition_for_0_Did_you_mean_1: diag(2727, 1 /* Error */, "Cannot_find_lib_definition_for_0_Did_you_mean_1_2727", "Cannot find lib definition for '{0}'. Did you mean '{1}'?"),
  _0_is_declared_here: diag(2728, 3 /* Message */, "_0_is_declared_here_2728", "'{0}' is declared here."),
  Property_0_is_used_before_its_initialization: diag(2729, 1 /* Error */, "Property_0_is_used_before_its_initialization_2729", "Property '{0}' is used before its initialization."),
  An_arrow_function_cannot_have_a_this_parameter: diag(2730, 1 /* Error */, "An_arrow_function_cannot_have_a_this_parameter_2730", "An arrow function cannot have a 'this' parameter."),
  Implicit_conversion_of_a_symbol_to_a_string_will_fail_at_runtime_Consider_wrapping_this_expression_in_String: diag(2731, 1 /* Error */, "Implicit_conversion_of_a_symbol_to_a_string_will_fail_at_runtime_Consider_wrapping_this_expression_i_2731", "Implicit conversion of a 'symbol' to a 'string' will fail at runtime. Consider wrapping this expression in 'String(...)'."),
  Cannot_find_module_0_Consider_using_resolveJsonModule_to_import_module_with_json_extension: diag(2732, 1 /* Error */, "Cannot_find_module_0_Consider_using_resolveJsonModule_to_import_module_with_json_extension_2732", "Cannot find module '{0}'. Consider using '--resolveJsonModule' to import module with '.json' extension."),
  Property_0_was_also_declared_here: diag(2733, 1 /* Error */, "Property_0_was_also_declared_here_2733", "Property '{0}' was also declared here."),
  Are_you_missing_a_semicolon: diag(2734, 1 /* Error */, "Are_you_missing_a_semicolon_2734", "Are you missing a semicolon?"),
  Did_you_mean_for_0_to_be_constrained_to_type_new_args_Colon_any_1: diag(2735, 1 /* Error */, "Did_you_mean_for_0_to_be_constrained_to_type_new_args_Colon_any_1_2735", "Did you mean for '{0}' to be constrained to type 'new (...args: any[]) ***REMOVED***> {1}'?"),
  Operator_0_cannot_be_applied_to_type_1: diag(2736, 1 /* Error */, "Operator_0_cannot_be_applied_to_type_1_2736", "Operator '{0}' cannot be applied to type '{1}'."),
  BigInt_literals_are_not_available_when_targeting_lower_than_ES2020: diag(2737, 1 /* Error */, "BigInt_literals_are_not_available_when_targeting_lower_than_ES2020_2737", "BigInt literals are not available when targeting lower than ES2020."),
  An_outer_value_of_this_is_shadowed_by_this_container: diag(2738, 3 /* Message */, "An_outer_value_of_this_is_shadowed_by_this_container_2738", "An outer value of 'this' is shadowed by this container."),
  Type_0_is_missing_the_following_properties_from_type_1_Colon_2: diag(2739, 1 /* Error */, "Type_0_is_missing_the_following_properties_from_type_1_Colon_2_2739", "Type '{0}' is missing the following properties from type '{1}': {2}"),
  Type_0_is_missing_the_following_properties_from_type_1_Colon_2_and_3_more: diag(2740, 1 /* Error */, "Type_0_is_missing_the_following_properties_from_type_1_Colon_2_and_3_more_2740", "Type '{0}' is missing the following properties from type '{1}': {2}, and {3} more."),
  Property_0_is_missing_in_type_1_but_required_in_type_2: diag(2741, 1 /* Error */, "Property_0_is_missing_in_type_1_but_required_in_type_2_2741", "Property '{0}' is missing in type '{1}' but required in type '{2}'."),
  The_inferred_type_of_0_cannot_be_named_without_a_reference_to_1_This_is_likely_not_portable_A_type_annotation_is_necessary: diag(2742, 1 /* Error */, "The_inferred_type_of_0_cannot_be_named_without_a_reference_to_1_This_is_likely_not_portable_A_type_a_2742", "The inferred type of '{0}' cannot be named without a reference to '{1}'. This is likely not portable. A type annotation is necessary."),
  No_overload_expects_0_type_arguments_but_overloads_do_exist_that_expect_either_1_or_2_type_arguments: diag(2743, 1 /* Error */, "No_overload_expects_0_type_arguments_but_overloads_do_exist_that_expect_either_1_or_2_type_arguments_2743", "No overload expects {0} type arguments, but overloads do exist that expect either {1} or {2} type arguments."),
  Type_parameter_defaults_can_only_reference_previously_declared_type_parameters: diag(2744, 1 /* Error */, "Type_parameter_defaults_can_only_reference_previously_declared_type_parameters_2744", "Type parameter defaults can only reference previously declared type parameters."),
  This_JSX_tag_s_0_prop_expects_type_1_which_requires_multiple_children_but_only_a_single_child_was_provided: diag(2745, 1 /* Error */, "This_JSX_tag_s_0_prop_expects_type_1_which_requires_multiple_children_but_only_a_single_child_was_pr_2745", "This JSX tag's '{0}' prop expects type '{1}' which requires multiple children, but only a single child was provided."),
  This_JSX_tag_s_0_prop_expects_a_single_child_of_type_1_but_multiple_children_were_provided: diag(2746, 1 /* Error */, "This_JSX_tag_s_0_prop_expects_a_single_child_of_type_1_but_multiple_children_were_provided_2746", "This JSX tag's '{0}' prop expects a single child of type '{1}', but multiple children were provided."),
  _0_components_don_t_accept_text_as_child_elements_Text_in_JSX_has_the_type_string_but_the_expected_type_of_1_is_2: diag(2747, 1 /* Error */, "_0_components_don_t_accept_text_as_child_elements_Text_in_JSX_has_the_type_string_but_the_expected_t_2747", "'{0}' components don't accept text as child elements. Text in JSX has the type 'string', but the expected type of '{1}' is '{2}'."),
  Cannot_access_ambient_const_enums_when_0_is_enabled: diag(2748, 1 /* Error */, "Cannot_access_ambient_const_enums_when_0_is_enabled_2748", "Cannot access ambient const enums when '{0}' is enabled."),
  _0_refers_to_a_value_but_is_being_used_as_a_type_here_Did_you_mean_typeof_0: diag(2749, 1 /* Error */, "_0_refers_to_a_value_but_is_being_used_as_a_type_here_Did_you_mean_typeof_0_2749", "'{0}' refers to a value, but is being used as a type here. Did you mean 'typeof {0}'?"),
  The_implementation_signature_is_declared_here: diag(2750, 1 /* Error */, "The_implementation_signature_is_declared_here_2750", "The implementation signature is declared here."),
  Circularity_originates_in_type_at_this_location: diag(2751, 1 /* Error */, "Circularity_originates_in_type_at_this_location_2751", "Circularity originates in type at this location."),
  The_first_export_default_is_here: diag(2752, 1 /* Error */, "The_first_export_default_is_here_2752", "The first export default is here."),
  Another_export_default_is_here: diag(2753, 1 /* Error */, "Another_export_default_is_here_2753", "Another export default is here."),
  super_may_not_use_type_arguments: diag(2754, 1 /* Error */, "super_may_not_use_type_arguments_2754", "'super' may not use type arguments."),
  No_constituent_of_type_0_is_callable: diag(2755, 1 /* Error */, "No_constituent_of_type_0_is_callable_2755", "No constituent of type '{0}' is callable."),
  Not_all_constituents_of_type_0_are_callable: diag(2756, 1 /* Error */, "Not_all_constituents_of_type_0_are_callable_2756", "Not all constituents of type '{0}' are callable."),
  Type_0_has_no_call_signatures: diag(2757, 1 /* Error */, "Type_0_has_no_call_signatures_2757", "Type '{0}' has no call signatures."),
  Each_member_of_the_union_type_0_has_signatures_but_none_of_those_signatures_are_compatible_with_each_other: diag(2758, 1 /* Error */, "Each_member_of_the_union_type_0_has_signatures_but_none_of_those_signatures_are_compatible_with_each_2758", "Each member of the union type '{0}' has signatures, but none of those signatures are compatible with each other."),
  No_constituent_of_type_0_is_constructable: diag(2759, 1 /* Error */, "No_constituent_of_type_0_is_constructable_2759", "No constituent of type '{0}' is constructable."),
  Not_all_constituents_of_type_0_are_constructable: diag(2760, 1 /* Error */, "Not_all_constituents_of_type_0_are_constructable_2760", "Not all constituents of type '{0}' are constructable."),
  Type_0_has_no_construct_signatures: diag(2761, 1 /* Error */, "Type_0_has_no_construct_signatures_2761", "Type '{0}' has no construct signatures."),
  Each_member_of_the_union_type_0_has_construct_signatures_but_none_of_those_signatures_are_compatible_with_each_other: diag(2762, 1 /* Error */, "Each_member_of_the_union_type_0_has_construct_signatures_but_none_of_those_signatures_are_compatible_2762", "Each member of the union type '{0}' has construct signatures, but none of those signatures are compatible with each other."),
  Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_for_of_will_always_send_0: diag(2763, 1 /* Error */, "Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_for_of_will_always_s_2763", "Cannot iterate value because the 'next' method of its iterator expects type '{1}', but for-of will always send '{0}'."),
  Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_spread_will_always_send_0: diag(2764, 1 /* Error */, "Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_spread_will_al_2764", "Cannot iterate value because the 'next' method of its iterator expects type '{1}', but array spread will always send '{0}'."),
  Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_destructuring_will_always_send_0: diag(2765, 1 /* Error */, "Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_destructuring__2765", "Cannot iterate value because the 'next' method of its iterator expects type '{1}', but array destructuring will always send '{0}'."),
  Cannot_delegate_iteration_to_value_because_the_next_method_of_its_iterator_expects_type_1_but_the_containing_generator_will_always_send_0: diag(2766, 1 /* Error */, "Cannot_delegate_iteration_to_value_because_the_next_method_of_its_iterator_expects_type_1_but_the_co_2766", "Cannot delegate iteration to value because the 'next' method of its iterator expects type '{1}', but the containing generator will always send '{0}'."),
  The_0_property_of_an_iterator_must_be_a_method: diag(2767, 1 /* Error */, "The_0_property_of_an_iterator_must_be_a_method_2767", "The '{0}' property of an iterator must be a method."),
  The_0_property_of_an_async_iterator_must_be_a_method: diag(2768, 1 /* Error */, "The_0_property_of_an_async_iterator_must_be_a_method_2768", "The '{0}' property of an async iterator must be a method."),
  No_overload_matches_this_call: diag(2769, 1 /* Error */, "No_overload_matches_this_call_2769", "No overload matches this call."),
  The_last_overload_gave_the_following_error: diag(2770, 1 /* Error */, "The_last_overload_gave_the_following_error_2770", "The last overload gave the following error."),
  The_last_overload_is_declared_here: diag(2771, 1 /* Error */, "The_last_overload_is_declared_here_2771", "The last overload is declared here."),
  Overload_0_of_1_2_gave_the_following_error: diag(2772, 1 /* Error */, "Overload_0_of_1_2_gave_the_following_error_2772", "Overload {0} of {1}, '{2}', gave the following error."),
  Did_you_forget_to_use_await: diag(2773, 1 /* Error */, "Did_you_forget_to_use_await_2773", "Did you forget to use 'await'?"),
  This_condition_will_always_return_true_since_this_function_is_always_defined_Did_you_mean_to_call_it_instead: diag(2774, 1 /* Error */, "This_condition_will_always_return_true_since_this_function_is_always_defined_Did_you_mean_to_call_it_2774", "This condition will always return true since this function is always defined. Did you mean to call it instead?"),
  Assertions_require_every_name_in_the_call_target_to_be_declared_with_an_explicit_type_annotation: diag(2775, 1 /* Error */, "Assertions_require_every_name_in_the_call_target_to_be_declared_with_an_explicit_type_annotation_2775", "Assertions require every name in the call target to be declared with an explicit type annotation."),
  Assertions_require_the_call_target_to_be_an_identifier_or_qualified_name: diag(2776, 1 /* Error */, "Assertions_require_the_call_target_to_be_an_identifier_or_qualified_name_2776", "Assertions require the call target to be an identifier or qualified name."),
  The_operand_of_an_increment_or_decrement_operator_may_not_be_an_optional_property_access: diag(2777, 1 /* Error */, "The_operand_of_an_increment_or_decrement_operator_may_not_be_an_optional_property_access_2777", "The operand of an increment or decrement operator may not be an optional property access."),
  The_target_of_an_object_rest_assignment_may_not_be_an_optional_property_access: diag(2778, 1 /* Error */, "The_target_of_an_object_rest_assignment_may_not_be_an_optional_property_access_2778", "The target of an object rest assignment may not be an optional property access."),
  The_left_hand_side_of_an_assignment_expression_may_not_be_an_optional_property_access: diag(2779, 1 /* Error */, "The_left_hand_side_of_an_assignment_expression_may_not_be_an_optional_property_access_2779", "The left-hand side of an assignment expression may not be an optional property access."),
  The_left_hand_side_of_a_for_in_statement_may_not_be_an_optional_property_access: diag(2780, 1 /* Error */, "The_left_hand_side_of_a_for_in_statement_may_not_be_an_optional_property_access_2780", "The left-hand side of a 'for...in' statement may not be an optional property access."),
  The_left_hand_side_of_a_for_of_statement_may_not_be_an_optional_property_access: diag(2781, 1 /* Error */, "The_left_hand_side_of_a_for_of_statement_may_not_be_an_optional_property_access_2781", "The left-hand side of a 'for...of' statement may not be an optional property access."),
  _0_needs_an_explicit_type_annotation: diag(2782, 3 /* Message */, "_0_needs_an_explicit_type_annotation_2782", "'{0}' needs an explicit type annotation."),
  _0_is_specified_more_than_once_so_this_usage_will_be_overwritten: diag(2783, 1 /* Error */, "_0_is_specified_more_than_once_so_this_usage_will_be_overwritten_2783", "'{0}' is specified more than once, so this usage will be overwritten."),
  get_and_set_accessors_cannot_declare_this_parameters: diag(2784, 1 /* Error */, "get_and_set_accessors_cannot_declare_this_parameters_2784", "'get' and 'set' accessors cannot declare 'this' parameters."),
  This_spread_always_overwrites_this_property: diag(2785, 1 /* Error */, "This_spread_always_overwrites_this_property_2785", "This spread always overwrites this property."),
  _0_cannot_be_used_as_a_JSX_component: diag(2786, 1 /* Error */, "_0_cannot_be_used_as_a_JSX_component_2786", "'{0}' cannot be used as a JSX component."),
  Its_return_type_0_is_not_a_valid_JSX_element: diag(2787, 1 /* Error */, "Its_return_type_0_is_not_a_valid_JSX_element_2787", "Its return type '{0}' is not a valid JSX element."),
  Its_instance_type_0_is_not_a_valid_JSX_element: diag(2788, 1 /* Error */, "Its_instance_type_0_is_not_a_valid_JSX_element_2788", "Its instance type '{0}' is not a valid JSX element."),
  Its_element_type_0_is_not_a_valid_JSX_element: diag(2789, 1 /* Error */, "Its_element_type_0_is_not_a_valid_JSX_element_2789", "Its element type '{0}' is not a valid JSX element."),
  The_operand_of_a_delete_operator_must_be_optional: diag(2790, 1 /* Error */, "The_operand_of_a_delete_operator_must_be_optional_2790", "The operand of a 'delete' operator must be optional."),
  Exponentiation_cannot_be_performed_on_bigint_values_unless_the_target_option_is_set_to_es2016_or_later: diag(2791, 1 /* Error */, "Exponentiation_cannot_be_performed_on_bigint_values_unless_the_target_option_is_set_to_es2016_or_lat_2791", "Exponentiation cannot be performed on 'bigint' values unless the 'target' option is set to 'es2016' or later."),
  Cannot_find_module_0_Did_you_mean_to_set_the_moduleResolution_option_to_nodenext_or_to_add_aliases_to_the_paths_option: diag(2792, 1 /* Error */, "Cannot_find_module_0_Did_you_mean_to_set_the_moduleResolution_option_to_nodenext_or_to_add_aliases_t_2792", "Cannot find module '{0}'. Did you mean to set the 'moduleResolution' option to 'nodenext', or to add aliases to the 'paths' option?"),
  The_call_would_have_succeeded_against_this_implementation_but_implementation_signatures_of_overloads_are_not_externally_visible: diag(2793, 1 /* Error */, "The_call_would_have_succeeded_against_this_implementation_but_implementation_signatures_of_overloads_2793", "The call would have succeeded against this implementation, but implementation signatures of overloads are not externally visible."),
  Expected_0_arguments_but_got_1_Did_you_forget_to_include_void_in_your_type_argument_to_Promise: diag(2794, 1 /* Error */, "Expected_0_arguments_but_got_1_Did_you_forget_to_include_void_in_your_type_argument_to_Promise_2794", "Expected {0} arguments, but got {1}. Did you forget to include 'void' in your type argument to 'Promise'?"),
  The_intrinsic_keyword_can_only_be_used_to_declare_compiler_provided_intrinsic_types: diag(2795, 1 /* Error */, "The_intrinsic_keyword_can_only_be_used_to_declare_compiler_provided_intrinsic_types_2795", "The 'intrinsic' keyword can only be used to declare compiler provided intrinsic types."),
  It_is_likely_that_you_are_missing_a_comma_to_separate_these_two_template_expressions_They_form_a_tagged_template_expression_which_cannot_be_invoked: diag(2796, 1 /* Error */, "It_is_likely_that_you_are_missing_a_comma_to_separate_these_two_template_expressions_They_form_a_tag_2796", "It is likely that you are missing a comma to separate these two template expressions. They form a tagged template expression which cannot be invoked."),
  A_mixin_class_that_extends_from_a_type_variable_containing_an_abstract_construct_signature_must_also_be_declared_abstract: diag(2797, 1 /* Error */, "A_mixin_class_that_extends_from_a_type_variable_containing_an_abstract_construct_signature_must_also_2797", "A mixin class that extends from a type variable containing an abstract construct signature must also be declared 'abstract'."),
  The_declaration_was_marked_as_deprecated_here: diag(2798, 1 /* Error */, "The_declaration_was_marked_as_deprecated_here_2798", "The declaration was marked as deprecated here."),
  Type_produces_a_tuple_type_that_is_too_large_to_represent: diag(2799, 1 /* Error */, "Type_produces_a_tuple_type_that_is_too_large_to_represent_2799", "Type produces a tuple type that is too large to represent."),
  Expression_produces_a_tuple_type_that_is_too_large_to_represent: diag(2800, 1 /* Error */, "Expression_produces_a_tuple_type_that_is_too_large_to_represent_2800", "Expression produces a tuple type that is too large to represent."),
  This_condition_will_always_return_true_since_this_0_is_always_defined: diag(2801, 1 /* Error */, "This_condition_will_always_return_true_since_this_0_is_always_defined_2801", "This condition will always return true since this '{0}' is always defined."),
  Type_0_can_only_be_iterated_through_when_using_the_downlevelIteration_flag_or_with_a_target_of_es2015_or_higher: diag(2802, 1 /* Error */, "Type_0_can_only_be_iterated_through_when_using_the_downlevelIteration_flag_or_with_a_target_of_es201_2802", "Type '{0}' can only be iterated through when using the '--downlevelIteration' flag or with a '--target' of 'es2015' or higher."),
  Cannot_assign_to_private_method_0_Private_methods_are_not_writable: diag(2803, 1 /* Error */, "Cannot_assign_to_private_method_0_Private_methods_are_not_writable_2803", "Cannot assign to private method '{0}'. Private methods are not writable."),
  Duplicate_identifier_0_Static_and_instance_elements_cannot_share_the_same_private_name: diag(2804, 1 /* Error */, "Duplicate_identifier_0_Static_and_instance_elements_cannot_share_the_same_private_name_2804", "Duplicate identifier '{0}'. Static and instance elements cannot share the same private name."),
  Private_accessor_was_defined_without_a_getter: diag(2806, 1 /* Error */, "Private_accessor_was_defined_without_a_getter_2806", "Private accessor was defined without a getter."),
  This_syntax_requires_an_imported_helper_named_1_with_2_parameters_which_is_not_compatible_with_the_one_in_0_Consider_upgrading_your_version_of_0: diag(2807, 1 /* Error */, "This_syntax_requires_an_imported_helper_named_1_with_2_parameters_which_is_not_compatible_with_the_o_2807", "This syntax requires an imported helper named '{1}' with {2} parameters, which is not compatible with the one in '{0}'. Consider upgrading your version of '{0}'."),
  A_get_accessor_must_be_at_least_as_accessible_as_the_setter: diag(2808, 1 /* Error */, "A_get_accessor_must_be_at_least_as_accessible_as_the_setter_2808", "A get accessor must be at least as accessible as the setter"),
  Declaration_or_statement_expected_This_follows_a_block_of_statements_so_if_you_intended_to_write_a_destructuring_assignment_you_might_need_to_wrap_the_whole_assignment_in_parentheses: diag(2809, 1 /* Error */, "Declaration_or_statement_expected_This_follows_a_block_of_statements_so_if_you_intended_to_write_a_d_2809", "Declaration or statement expected. This '***REMOVED***' follows a block of statements, so if you intended to write a destructuring assignment, you might need to wrap the whole assignment in parentheses."),
  Expected_1_argument_but_got_0_new_Promise_needs_a_JSDoc_hint_to_produce_a_resolve_that_can_be_called_without_arguments: diag(2810, 1 /* Error */, "Expected_1_argument_but_got_0_new_Promise_needs_a_JSDoc_hint_to_produce_a_resolve_that_can_be_called_2810", "Expected 1 argument, but got 0. 'new Promise()' needs a JSDoc hint to produce a 'resolve' that can be called without arguments."),
  Initializer_for_property_0: diag(2811, 1 /* Error */, "Initializer_for_property_0_2811", "Initializer for property '{0}'"),
  Property_0_does_not_exist_on_type_1_Try_changing_the_lib_compiler_option_to_include_dom: diag(2812, 1 /* Error */, "Property_0_does_not_exist_on_type_1_Try_changing_the_lib_compiler_option_to_include_dom_2812", "Property '{0}' does not exist on type '{1}'. Try changing the 'lib' compiler option to include 'dom'."),
  Class_declaration_cannot_implement_overload_list_for_0: diag(2813, 1 /* Error */, "Class_declaration_cannot_implement_overload_list_for_0_2813", "Class declaration cannot implement overload list for '{0}'."),
  Function_with_bodies_can_only_merge_with_classes_that_are_ambient: diag(2814, 1 /* Error */, "Function_with_bodies_can_only_merge_with_classes_that_are_ambient_2814", "Function with bodies can only merge with classes that are ambient."),
  arguments_cannot_be_referenced_in_property_initializers_or_class_static_initialization_blocks: diag(2815, 1 /* Error */, "arguments_cannot_be_referenced_in_property_initializers_or_class_static_initialization_blocks_2815", "'arguments' cannot be referenced in property initializers or class static initialization blocks."),
  Cannot_use_this_in_a_static_property_initializer_of_a_decorated_class: diag(2816, 1 /* Error */, "Cannot_use_this_in_a_static_property_initializer_of_a_decorated_class_2816", "Cannot use 'this' in a static property initializer of a decorated class."),
  Property_0_has_no_initializer_and_is_not_definitely_assigned_in_a_class_static_block: diag(2817, 1 /* Error */, "Property_0_has_no_initializer_and_is_not_definitely_assigned_in_a_class_static_block_2817", "Property '{0}' has no initializer and is not definitely assigned in a class static block."),
  Duplicate_identifier_0_Compiler_reserves_name_1_when_emitting_super_references_in_static_initializers: diag(2818, 1 /* Error */, "Duplicate_identifier_0_Compiler_reserves_name_1_when_emitting_super_references_in_static_initializer_2818", "Duplicate identifier '{0}'. Compiler reserves name '{1}' when emitting 'super' references in static initializers."),
  Namespace_name_cannot_be_0: diag(2819, 1 /* Error */, "Namespace_name_cannot_be_0_2819", "Namespace name cannot be '{0}'."),
  Type_0_is_not_assignable_to_type_1_Did_you_mean_2: diag(2820, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_Did_you_mean_2_2820", "Type '{0}' is not assignable to type '{1}'. Did you mean '{2}'?"),
  Import_assertions_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext_or_preserve: diag(2821, 1 /* Error */, "Import_assertions_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext__2821", "Import assertions are only supported when the '--module' option is set to 'esnext', 'node18', 'node20', 'nodenext', or 'preserve'."),
  Import_assertions_cannot_be_used_with_type_only_imports_or_exports: diag(2822, 1 /* Error */, "Import_assertions_cannot_be_used_with_type_only_imports_or_exports_2822", "Import assertions cannot be used with type-only imports or exports."),
  Import_attributes_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext_or_preserve: diag(2823, 1 /* Error */, "Import_attributes_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext__2823", "Import attributes are only supported when the '--module' option is set to 'esnext', 'node18', 'node20', 'nodenext', or 'preserve'."),
  Cannot_find_namespace_0_Did_you_mean_1: diag(2833, 1 /* Error */, "Cannot_find_namespace_0_Did_you_mean_1_2833", "Cannot find namespace '{0}'. Did you mean '{1}'?"),
  Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_node16_or_nodenext_Consider_adding_an_extension_to_the_import_path: diag(2834, 1 /* Error */, "Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_n_2834", "Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Consider adding an extension to the import path."),
  Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_node16_or_nodenext_Did_you_mean_0: diag(2835, 1 /* Error */, "Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_n_2835", "Relative import paths need explicit file extensions in ECMAScript imports when '--moduleResolution' is 'node16' or 'nodenext'. Did you mean '{0}'?"),
  Import_assertions_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls: diag(2836, 1 /* Error */, "Import_assertions_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls_2836", "Import assertions are not allowed on statements that compile to CommonJS 'require' calls."),
  Import_assertion_values_must_be_string_literal_expressions: diag(2837, 1 /* Error */, "Import_assertion_values_must_be_string_literal_expressions_2837", "Import assertion values must be string literal expressions."),
  All_declarations_of_0_must_have_identical_constraints: diag(2838, 1 /* Error */, "All_declarations_of_0_must_have_identical_constraints_2838", "All declarations of '{0}' must have identical constraints."),
  This_condition_will_always_return_0_since_JavaScript_compares_objects_by_reference_not_value: diag(2839, 1 /* Error */, "This_condition_will_always_return_0_since_JavaScript_compares_objects_by_reference_not_value_2839", "This condition will always return '{0}' since JavaScript compares objects by reference, not value."),
  An_interface_cannot_extend_a_primitive_type_like_0_It_can_only_extend_other_named_object_types: diag(2840, 1 /* Error */, "An_interface_cannot_extend_a_primitive_type_like_0_It_can_only_extend_other_named_object_types_2840", "An interface cannot extend a primitive type like '{0}'. It can only extend other named object types."),
  _0_is_an_unused_renaming_of_1_Did_you_intend_to_use_it_as_a_type_annotation: diag(2842, 1 /* Error */, "_0_is_an_unused_renaming_of_1_Did_you_intend_to_use_it_as_a_type_annotation_2842", "'{0}' is an unused renaming of '{1}'. Did you intend to use it as a type annotation?"),
  We_can_only_write_a_type_for_0_by_adding_a_type_for_the_entire_parameter_here: diag(2843, 1 /* Error */, "We_can_only_write_a_type_for_0_by_adding_a_type_for_the_entire_parameter_here_2843", "We can only write a type for '{0}' by adding a type for the entire parameter here."),
  Type_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor: diag(2844, 1 /* Error */, "Type_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor_2844", "Type of instance member variable '{0}' cannot reference identifier '{1}' declared in the constructor."),
  This_condition_will_always_return_0: diag(2845, 1 /* Error */, "This_condition_will_always_return_0_2845", "This condition will always return '{0}'."),
  A_declaration_file_cannot_be_imported_without_import_type_Did_you_mean_to_import_an_implementation_file_0_instead: diag(2846, 1 /* Error */, "A_declaration_file_cannot_be_imported_without_import_type_Did_you_mean_to_import_an_implementation_f_2846", "A declaration file cannot be imported without 'import type'. Did you mean to import an implementation file '{0}' instead?"),
  The_right_hand_side_of_an_instanceof_expression_must_not_be_an_instantiation_expression: diag(2848, 1 /* Error */, "The_right_hand_side_of_an_instanceof_expression_must_not_be_an_instantiation_expression_2848", "The right-hand side of an 'instanceof' expression must not be an instantiation expression."),
  Target_signature_provides_too_few_arguments_Expected_0_or_more_but_got_1: diag(2849, 1 /* Error */, "Target_signature_provides_too_few_arguments_Expected_0_or_more_but_got_1_2849", "Target signature provides too few arguments. Expected {0} or more, but got {1}."),
  The_initializer_of_a_using_declaration_must_be_either_an_object_with_a_Symbol_dispose_method_or_be_null_or_undefined: diag(2850, 1 /* Error */, "The_initializer_of_a_using_declaration_must_be_either_an_object_with_a_Symbol_dispose_method_or_be_n_2850", "The initializer of a 'using' declaration must be either an object with a '[Symbol.dispose]()' method, or be 'null' or 'undefined'."),
  The_initializer_of_an_await_using_declaration_must_be_either_an_object_with_a_Symbol_asyncDispose_or_Symbol_dispose_method_or_be_null_or_undefined: diag(2851, 1 /* Error */, "The_initializer_of_an_await_using_declaration_must_be_either_an_object_with_a_Symbol_asyncDispose_or_2851", "The initializer of an 'await using' declaration must be either an object with a '[Symbol.asyncDispose]()' or '[Symbol.dispose]()' method, or be 'null' or 'undefined'."),
  await_using_statements_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules: diag(2852, 1 /* Error */, "await_using_statements_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules_2852", "'await using' statements are only allowed within async functions and at the top levels of modules."),
  await_using_statements_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module: diag(2853, 1 /* Error */, "await_using_statements_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_th_2853", "'await using' statements are only allowed at the top level of a file when that file is a module, but this file has no imports or exports. Consider adding an empty 'export {}' to make this file a module."),
  Top_level_await_using_statements_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher: diag(2854, 1 /* Error */, "Top_level_await_using_statements_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_sys_2854", "Top-level 'await using' statements are only allowed when the 'module' option is set to 'es2022', 'esnext', 'system', 'node16', 'node18', 'node20', 'nodenext', or 'preserve', and the 'target' option is set to 'es2017' or higher."),
  Class_field_0_defined_by_the_parent_class_is_not_accessible_in_the_child_class_via_super: diag(2855, 1 /* Error */, "Class_field_0_defined_by_the_parent_class_is_not_accessible_in_the_child_class_via_super_2855", "Class field '{0}' defined by the parent class is not accessible in the child class via super."),
  Import_attributes_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls: diag(2856, 1 /* Error */, "Import_attributes_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls_2856", "Import attributes are not allowed on statements that compile to CommonJS 'require' calls."),
  Import_attributes_cannot_be_used_with_type_only_imports_or_exports: diag(2857, 1 /* Error */, "Import_attributes_cannot_be_used_with_type_only_imports_or_exports_2857", "Import attributes cannot be used with type-only imports or exports."),
  Import_attribute_values_must_be_string_literal_expressions: diag(2858, 1 /* Error */, "Import_attribute_values_must_be_string_literal_expressions_2858", "Import attribute values must be string literal expressions."),
  Excessive_complexity_comparing_types_0_and_1: diag(2859, 1 /* Error */, "Excessive_complexity_comparing_types_0_and_1_2859", "Excessive complexity comparing types '{0}' and '{1}'."),
  The_left_hand_side_of_an_instanceof_expression_must_be_assignable_to_the_first_argument_of_the_right_hand_side_s_Symbol_hasInstance_method: diag(2860, 1 /* Error */, "The_left_hand_side_of_an_instanceof_expression_must_be_assignable_to_the_first_argument_of_the_right_2860", "The left-hand side of an 'instanceof' expression must be assignable to the first argument of the right-hand side's '[Symbol.hasInstance]' method."),
  An_object_s_Symbol_hasInstance_method_must_return_a_boolean_value_for_it_to_be_used_on_the_right_hand_side_of_an_instanceof_expression: diag(2861, 1 /* Error */, "An_object_s_Symbol_hasInstance_method_must_return_a_boolean_value_for_it_to_be_used_on_the_right_han_2861", "An object's '[Symbol.hasInstance]' method must return a boolean value for it to be used on the right-hand side of an 'instanceof' expression."),
  Type_0_is_generic_and_can_only_be_indexed_for_reading: diag(2862, 1 /* Error */, "Type_0_is_generic_and_can_only_be_indexed_for_reading_2862", "Type '{0}' is generic and can only be indexed for reading."),
  A_class_cannot_extend_a_primitive_type_like_0_Classes_can_only_extend_constructable_values: diag(2863, 1 /* Error */, "A_class_cannot_extend_a_primitive_type_like_0_Classes_can_only_extend_constructable_values_2863", "A class cannot extend a primitive type like '{0}'. Classes can only extend constructable values."),
  A_class_cannot_implement_a_primitive_type_like_0_It_can_only_implement_other_named_object_types: diag(2864, 1 /* Error */, "A_class_cannot_implement_a_primitive_type_like_0_It_can_only_implement_other_named_object_types_2864", "A class cannot implement a primitive type like '{0}'. It can only implement other named object types."),
  Import_0_conflicts_with_local_value_so_must_be_declared_with_a_type_only_import_when_isolatedModules_is_enabled: diag(2865, 1 /* Error */, "Import_0_conflicts_with_local_value_so_must_be_declared_with_a_type_only_import_when_isolatedModules_2865", "Import '{0}' conflicts with local value, so must be declared with a type-only import when 'isolatedModules' is enabled."),
  Import_0_conflicts_with_global_value_used_in_this_file_so_must_be_declared_with_a_type_only_import_when_isolatedModules_is_enabled: diag(2866, 1 /* Error */, "Import_0_conflicts_with_global_value_used_in_this_file_so_must_be_declared_with_a_type_only_import_w_2866", "Import '{0}' conflicts with global value used in this file, so must be declared with a type-only import when 'isolatedModules' is enabled."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun: diag(2867, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun_2867", "Cannot find name '{0}'. Do you need to install type definitions for Bun? Try `npm i --save-dev @types/bun`."),
  Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun_and_then_add_bun_to_the_types_field_in_your_tsconfig: diag(2868, 1 /* Error */, "Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun_2868", "Cannot find name '{0}'. Do you need to install type definitions for Bun? Try `npm i --save-dev @types/bun` and then add 'bun' to the types field in your tsconfig."),
  Right_operand_of_is_unreachable_because_the_left_operand_is_never_nullish: diag(2869, 1 /* Error */, "Right_operand_of_is_unreachable_because_the_left_operand_is_never_nullish_2869", "Right operand of ?? is unreachable because the left operand is never nullish."),
  This_binary_expression_is_never_nullish_Are_you_missing_parentheses: diag(2870, 1 /* Error */, "This_binary_expression_is_never_nullish_Are_you_missing_parentheses_2870", "This binary expression is never nullish. Are you missing parentheses?"),
  This_expression_is_always_nullish: diag(2871, 1 /* Error */, "This_expression_is_always_nullish_2871", "This expression is always nullish."),
  This_kind_of_expression_is_always_truthy: diag(2872, 1 /* Error */, "This_kind_of_expression_is_always_truthy_2872", "This kind of expression is always truthy."),
  This_kind_of_expression_is_always_falsy: diag(2873, 1 /* Error */, "This_kind_of_expression_is_always_falsy_2873", "This kind of expression is always falsy."),
  This_JSX_tag_requires_0_to_be_in_scope_but_it_could_not_be_found: diag(2874, 1 /* Error */, "This_JSX_tag_requires_0_to_be_in_scope_but_it_could_not_be_found_2874", "This JSX tag requires '{0}' to be in scope, but it could not be found."),
  This_JSX_tag_requires_the_module_path_0_to_exist_but_none_could_be_found_Make_sure_you_have_types_for_the_appropriate_package_installed: diag(2875, 1 /* Error */, "This_JSX_tag_requires_the_module_path_0_to_exist_but_none_could_be_found_Make_sure_you_have_types_fo_2875", "This JSX tag requires the module path '{0}' to exist, but none could be found. Make sure you have types for the appropriate package installed."),
  This_relative_import_path_is_unsafe_to_rewrite_because_it_looks_like_a_file_name_but_actually_resolves_to_0: diag(2876, 1 /* Error */, "This_relative_import_path_is_unsafe_to_rewrite_because_it_looks_like_a_file_name_but_actually_resolv_2876", 'This relative import path is unsafe to rewrite because it looks like a file name, but actually resolves to "{0}".'),
  This_import_uses_a_0_extension_to_resolve_to_an_input_TypeScript_file_but_will_not_be_rewritten_during_emit_because_it_is_not_a_relative_path: diag(2877, 1 /* Error */, "This_import_uses_a_0_extension_to_resolve_to_an_input_TypeScript_file_but_will_not_be_rewritten_duri_2877", "This import uses a '{0}' extension to resolve to an input TypeScript file, but will not be rewritten during emit because it is not a relative path."),
  This_import_path_is_unsafe_to_rewrite_because_it_resolves_to_another_project_and_the_relative_path_between_the_projects_output_files_is_not_the_same_as_the_relative_path_between_its_input_files: diag(2878, 1 /* Error */, "This_import_path_is_unsafe_to_rewrite_because_it_resolves_to_another_project_and_the_relative_path_b_2878", "This import path is unsafe to rewrite because it resolves to another project, and the relative path between the projects' output files is not the same as the relative path between its input files."),
  Using_JSX_fragments_requires_fragment_factory_0_to_be_in_scope_but_it_could_not_be_found: diag(2879, 1 /* Error */, "Using_JSX_fragments_requires_fragment_factory_0_to_be_in_scope_but_it_could_not_be_found_2879", "Using JSX fragments requires fragment factory '{0}' to be in scope, but it could not be found."),
  Import_assertions_have_been_replaced_by_import_attributes_Use_with_instead_of_assert: diag(2880, 1 /* Error */, "Import_assertions_have_been_replaced_by_import_attributes_Use_with_instead_of_assert_2880", "Import assertions have been replaced by import attributes. Use 'with' instead of 'assert'."),
  This_expression_is_never_nullish: diag(2881, 1 /* Error */, "This_expression_is_never_nullish_2881", "This expression is never nullish."),
  Import_declaration_0_is_using_private_name_1: diag(4e3, 1 /* Error */, "Import_declaration_0_is_using_private_name_1_4000", "Import declaration '{0}' is using private name '{1}'."),
  Type_parameter_0_of_exported_class_has_or_is_using_private_name_1: diag(4002, 1 /* Error */, "Type_parameter_0_of_exported_class_has_or_is_using_private_name_1_4002", "Type parameter '{0}' of exported class has or is using private name '{1}'."),
  Type_parameter_0_of_exported_interface_has_or_is_using_private_name_1: diag(4004, 1 /* Error */, "Type_parameter_0_of_exported_interface_has_or_is_using_private_name_1_4004", "Type parameter '{0}' of exported interface has or is using private name '{1}'."),
  Type_parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1: diag(4006, 1 /* Error */, "Type_parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1_4006", "Type parameter '{0}' of constructor signature from exported interface has or is using private name '{1}'."),
  Type_parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1: diag(4008, 1 /* Error */, "Type_parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1_4008", "Type parameter '{0}' of call signature from exported interface has or is using private name '{1}'."),
  Type_parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1: diag(4010, 1 /* Error */, "Type_parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1_4010", "Type parameter '{0}' of public static method from exported class has or is using private name '{1}'."),
  Type_parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1: diag(4012, 1 /* Error */, "Type_parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1_4012", "Type parameter '{0}' of public method from exported class has or is using private name '{1}'."),
  Type_parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1: diag(4014, 1 /* Error */, "Type_parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1_4014", "Type parameter '{0}' of method from exported interface has or is using private name '{1}'."),
  Type_parameter_0_of_exported_function_has_or_is_using_private_name_1: diag(4016, 1 /* Error */, "Type_parameter_0_of_exported_function_has_or_is_using_private_name_1_4016", "Type parameter '{0}' of exported function has or is using private name '{1}'."),
  Implements_clause_of_exported_class_0_has_or_is_using_private_name_1: diag(4019, 1 /* Error */, "Implements_clause_of_exported_class_0_has_or_is_using_private_name_1_4019", "Implements clause of exported class '{0}' has or is using private name '{1}'."),
  extends_clause_of_exported_class_0_has_or_is_using_private_name_1: diag(4020, 1 /* Error */, "extends_clause_of_exported_class_0_has_or_is_using_private_name_1_4020", "'extends' clause of exported class '{0}' has or is using private name '{1}'."),
  extends_clause_of_exported_class_has_or_is_using_private_name_0: diag(4021, 1 /* Error */, "extends_clause_of_exported_class_has_or_is_using_private_name_0_4021", "'extends' clause of exported class has or is using private name '{0}'."),
  extends_clause_of_exported_interface_0_has_or_is_using_private_name_1: diag(4022, 1 /* Error */, "extends_clause_of_exported_interface_0_has_or_is_using_private_name_1_4022", "'extends' clause of exported interface '{0}' has or is using private name '{1}'."),
  Exported_variable_0_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4023, 1 /* Error */, "Exported_variable_0_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named_4023", "Exported variable '{0}' has or is using name '{1}' from external module {2} but cannot be named."),
  Exported_variable_0_has_or_is_using_name_1_from_private_module_2: diag(4024, 1 /* Error */, "Exported_variable_0_has_or_is_using_name_1_from_private_module_2_4024", "Exported variable '{0}' has or is using name '{1}' from private module '{2}'."),
  Exported_variable_0_has_or_is_using_private_name_1: diag(4025, 1 /* Error */, "Exported_variable_0_has_or_is_using_private_name_1_4025", "Exported variable '{0}' has or is using private name '{1}'."),
  Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4026, 1 /* Error */, "Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot__4026", "Public static property '{0}' of exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4027, 1 /* Error */, "Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2_4027", "Public static property '{0}' of exported class has or is using name '{1}' from private module '{2}'."),
  Public_static_property_0_of_exported_class_has_or_is_using_private_name_1: diag(4028, 1 /* Error */, "Public_static_property_0_of_exported_class_has_or_is_using_private_name_1_4028", "Public static property '{0}' of exported class has or is using private name '{1}'."),
  Public_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4029, 1 /* Error */, "Public_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_name_4029", "Public property '{0}' of exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Public_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4030, 1 /* Error */, "Public_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2_4030", "Public property '{0}' of exported class has or is using name '{1}' from private module '{2}'."),
  Public_property_0_of_exported_class_has_or_is_using_private_name_1: diag(4031, 1 /* Error */, "Public_property_0_of_exported_class_has_or_is_using_private_name_1_4031", "Public property '{0}' of exported class has or is using private name '{1}'."),
  Property_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4032, 1 /* Error */, "Property_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2_4032", "Property '{0}' of exported interface has or is using name '{1}' from private module '{2}'."),
  Property_0_of_exported_interface_has_or_is_using_private_name_1: diag(4033, 1 /* Error */, "Property_0_of_exported_interface_has_or_is_using_private_name_1_4033", "Property '{0}' of exported interface has or is using private name '{1}'."),
  Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4034, 1 /* Error */, "Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_name_1_from_private_mod_4034", "Parameter type of public static setter '{0}' from exported class has or is using name '{1}' from private module '{2}'."),
  Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_private_name_1: diag(4035, 1 /* Error */, "Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_private_name_1_4035", "Parameter type of public static setter '{0}' from exported class has or is using private name '{1}'."),
  Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4036, 1 /* Error */, "Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2_4036", "Parameter type of public setter '{0}' from exported class has or is using name '{1}' from private module '{2}'."),
  Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_private_name_1: diag(4037, 1 /* Error */, "Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_private_name_1_4037", "Parameter type of public setter '{0}' from exported class has or is using private name '{1}'."),
  Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4038, 1 /* Error */, "Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_external_modul_4038", "Return type of public static getter '{0}' from exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4039, 1 /* Error */, "Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_4039", "Return type of public static getter '{0}' from exported class has or is using name '{1}' from private module '{2}'."),
  Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_private_name_1: diag(4040, 1 /* Error */, "Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_private_name_1_4040", "Return type of public static getter '{0}' from exported class has or is using private name '{1}'."),
  Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4041, 1 /* Error */, "Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_4041", "Return type of public getter '{0}' from exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4042, 1 /* Error */, "Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2_4042", "Return type of public getter '{0}' from exported class has or is using name '{1}' from private module '{2}'."),
  Return_type_of_public_getter_0_from_exported_class_has_or_is_using_private_name_1: diag(4043, 1 /* Error */, "Return_type_of_public_getter_0_from_exported_class_has_or_is_using_private_name_1_4043", "Return type of public getter '{0}' from exported class has or is using private name '{1}'."),
  Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1: diag(4044, 1 /* Error */, "Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_name_0_from_private_mod_4044", "Return type of constructor signature from exported interface has or is using name '{0}' from private module '{1}'."),
  Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_0: diag(4045, 1 /* Error */, "Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_0_4045", "Return type of constructor signature from exported interface has or is using private name '{0}'."),
  Return_type_of_call_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1: diag(4046, 1 /* Error */, "Return_type_of_call_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1_4046", "Return type of call signature from exported interface has or is using name '{0}' from private module '{1}'."),
  Return_type_of_call_signature_from_exported_interface_has_or_is_using_private_name_0: diag(4047, 1 /* Error */, "Return_type_of_call_signature_from_exported_interface_has_or_is_using_private_name_0_4047", "Return type of call signature from exported interface has or is using private name '{0}'."),
  Return_type_of_index_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1: diag(4048, 1 /* Error */, "Return_type_of_index_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1_4048", "Return type of index signature from exported interface has or is using name '{0}' from private module '{1}'."),
  Return_type_of_index_signature_from_exported_interface_has_or_is_using_private_name_0: diag(4049, 1 /* Error */, "Return_type_of_index_signature_from_exported_interface_has_or_is_using_private_name_0_4049", "Return type of index signature from exported interface has or is using private name '{0}'."),
  Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named: diag(4050, 1 /* Error */, "Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_external_module__4050", "Return type of public static method from exported class has or is using name '{0}' from external module {1} but cannot be named."),
  Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_private_module_1: diag(4051, 1 /* Error */, "Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_private_module_1_4051", "Return type of public static method from exported class has or is using name '{0}' from private module '{1}'."),
  Return_type_of_public_static_method_from_exported_class_has_or_is_using_private_name_0: diag(4052, 1 /* Error */, "Return_type_of_public_static_method_from_exported_class_has_or_is_using_private_name_0_4052", "Return type of public static method from exported class has or is using private name '{0}'."),
  Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named: diag(4053, 1 /* Error */, "Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_external_module_1_but_c_4053", "Return type of public method from exported class has or is using name '{0}' from external module {1} but cannot be named."),
  Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_private_module_1: diag(4054, 1 /* Error */, "Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_private_module_1_4054", "Return type of public method from exported class has or is using name '{0}' from private module '{1}'."),
  Return_type_of_public_method_from_exported_class_has_or_is_using_private_name_0: diag(4055, 1 /* Error */, "Return_type_of_public_method_from_exported_class_has_or_is_using_private_name_0_4055", "Return type of public method from exported class has or is using private name '{0}'."),
  Return_type_of_method_from_exported_interface_has_or_is_using_name_0_from_private_module_1: diag(4056, 1 /* Error */, "Return_type_of_method_from_exported_interface_has_or_is_using_name_0_from_private_module_1_4056", "Return type of method from exported interface has or is using name '{0}' from private module '{1}'."),
  Return_type_of_method_from_exported_interface_has_or_is_using_private_name_0: diag(4057, 1 /* Error */, "Return_type_of_method_from_exported_interface_has_or_is_using_private_name_0_4057", "Return type of method from exported interface has or is using private name '{0}'."),
  Return_type_of_exported_function_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named: diag(4058, 1 /* Error */, "Return_type_of_exported_function_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named_4058", "Return type of exported function has or is using name '{0}' from external module {1} but cannot be named."),
  Return_type_of_exported_function_has_or_is_using_name_0_from_private_module_1: diag(4059, 1 /* Error */, "Return_type_of_exported_function_has_or_is_using_name_0_from_private_module_1_4059", "Return type of exported function has or is using name '{0}' from private module '{1}'."),
  Return_type_of_exported_function_has_or_is_using_private_name_0: diag(4060, 1 /* Error */, "Return_type_of_exported_function_has_or_is_using_private_name_0_4060", "Return type of exported function has or is using private name '{0}'."),
  Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4061, 1 /* Error */, "Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_can_4061", "Parameter '{0}' of constructor from exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4062, 1 /* Error */, "Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_private_module_2_4062", "Parameter '{0}' of constructor from exported class has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_constructor_from_exported_class_has_or_is_using_private_name_1: diag(4063, 1 /* Error */, "Parameter_0_of_constructor_from_exported_class_has_or_is_using_private_name_1_4063", "Parameter '{0}' of constructor from exported class has or is using private name '{1}'."),
  Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4064, 1 /* Error */, "Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_name_1_from_private_mod_4064", "Parameter '{0}' of constructor signature from exported interface has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1: diag(4065, 1 /* Error */, "Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1_4065", "Parameter '{0}' of constructor signature from exported interface has or is using private name '{1}'."),
  Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4066, 1 /* Error */, "Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2_4066", "Parameter '{0}' of call signature from exported interface has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1: diag(4067, 1 /* Error */, "Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1_4067", "Parameter '{0}' of call signature from exported interface has or is using private name '{1}'."),
  Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4068, 1 /* Error */, "Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_external_module__4068", "Parameter '{0}' of public static method from exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4069, 1 /* Error */, "Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_private_module_2_4069", "Parameter '{0}' of public static method from exported class has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1: diag(4070, 1 /* Error */, "Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1_4070", "Parameter '{0}' of public static method from exported class has or is using private name '{1}'."),
  Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4071, 1 /* Error */, "Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_c_4071", "Parameter '{0}' of public method from exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4072, 1 /* Error */, "Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_private_module_2_4072", "Parameter '{0}' of public method from exported class has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1: diag(4073, 1 /* Error */, "Parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1_4073", "Parameter '{0}' of public method from exported class has or is using private name '{1}'."),
  Parameter_0_of_method_from_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4074, 1 /* Error */, "Parameter_0_of_method_from_exported_interface_has_or_is_using_name_1_from_private_module_2_4074", "Parameter '{0}' of method from exported interface has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1: diag(4075, 1 /* Error */, "Parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1_4075", "Parameter '{0}' of method from exported interface has or is using private name '{1}'."),
  Parameter_0_of_exported_function_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4076, 1 /* Error */, "Parameter_0_of_exported_function_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named_4076", "Parameter '{0}' of exported function has or is using name '{1}' from external module {2} but cannot be named."),
  Parameter_0_of_exported_function_has_or_is_using_name_1_from_private_module_2: diag(4077, 1 /* Error */, "Parameter_0_of_exported_function_has_or_is_using_name_1_from_private_module_2_4077", "Parameter '{0}' of exported function has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_exported_function_has_or_is_using_private_name_1: diag(4078, 1 /* Error */, "Parameter_0_of_exported_function_has_or_is_using_private_name_1_4078", "Parameter '{0}' of exported function has or is using private name '{1}'."),
  Exported_type_alias_0_has_or_is_using_private_name_1: diag(4081, 1 /* Error */, "Exported_type_alias_0_has_or_is_using_private_name_1_4081", "Exported type alias '{0}' has or is using private name '{1}'."),
  Default_export_of_the_module_has_or_is_using_private_name_0: diag(4082, 1 /* Error */, "Default_export_of_the_module_has_or_is_using_private_name_0_4082", "Default export of the module has or is using private name '{0}'."),
  Type_parameter_0_of_exported_type_alias_has_or_is_using_private_name_1: diag(4083, 1 /* Error */, "Type_parameter_0_of_exported_type_alias_has_or_is_using_private_name_1_4083", "Type parameter '{0}' of exported type alias has or is using private name '{1}'."),
  Exported_type_alias_0_has_or_is_using_private_name_1_from_module_2: diag(4084, 1 /* Error */, "Exported_type_alias_0_has_or_is_using_private_name_1_from_module_2_4084", "Exported type alias '{0}' has or is using private name '{1}' from module {2}."),
  Extends_clause_for_inferred_type_0_has_or_is_using_private_name_1: diag(4085, 1 /* Error */, "Extends_clause_for_inferred_type_0_has_or_is_using_private_name_1_4085", "Extends clause for inferred type '{0}' has or is using private name '{1}'."),
  Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4091, 1 /* Error */, "Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2_4091", "Parameter '{0}' of index signature from exported interface has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_private_name_1: diag(4092, 1 /* Error */, "Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_private_name_1_4092", "Parameter '{0}' of index signature from exported interface has or is using private name '{1}'."),
  Property_0_of_exported_anonymous_class_type_may_not_be_private_or_protected: diag(4094, 1 /* Error */, "Property_0_of_exported_anonymous_class_type_may_not_be_private_or_protected_4094", "Property '{0}' of exported anonymous class type may not be private or protected."),
  Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4095, 1 /* Error */, "Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_4095", "Public static method '{0}' of exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4096, 1 /* Error */, "Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2_4096", "Public static method '{0}' of exported class has or is using name '{1}' from private module '{2}'."),
  Public_static_method_0_of_exported_class_has_or_is_using_private_name_1: diag(4097, 1 /* Error */, "Public_static_method_0_of_exported_class_has_or_is_using_private_name_1_4097", "Public static method '{0}' of exported class has or is using private name '{1}'."),
  Public_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4098, 1 /* Error */, "Public_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named_4098", "Public method '{0}' of exported class has or is using name '{1}' from external module {2} but cannot be named."),
  Public_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2: diag(4099, 1 /* Error */, "Public_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2_4099", "Public method '{0}' of exported class has or is using name '{1}' from private module '{2}'."),
  Public_method_0_of_exported_class_has_or_is_using_private_name_1: diag(4100, 1 /* Error */, "Public_method_0_of_exported_class_has_or_is_using_private_name_1_4100", "Public method '{0}' of exported class has or is using private name '{1}'."),
  Method_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2: diag(4101, 1 /* Error */, "Method_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2_4101", "Method '{0}' of exported interface has or is using name '{1}' from private module '{2}'."),
  Method_0_of_exported_interface_has_or_is_using_private_name_1: diag(4102, 1 /* Error */, "Method_0_of_exported_interface_has_or_is_using_private_name_1_4102", "Method '{0}' of exported interface has or is using private name '{1}'."),
  Type_parameter_0_of_exported_mapped_object_type_is_using_private_name_1: diag(4103, 1 /* Error */, "Type_parameter_0_of_exported_mapped_object_type_is_using_private_name_1_4103", "Type parameter '{0}' of exported mapped object type is using private name '{1}'."),
  The_type_0_is_readonly_and_cannot_be_assigned_to_the_mutable_type_1: diag(4104, 1 /* Error */, "The_type_0_is_readonly_and_cannot_be_assigned_to_the_mutable_type_1_4104", "The type '{0}' is 'readonly' and cannot be assigned to the mutable type '{1}'."),
  Private_or_protected_member_0_cannot_be_accessed_on_a_type_parameter: diag(4105, 1 /* Error */, "Private_or_protected_member_0_cannot_be_accessed_on_a_type_parameter_4105", "Private or protected member '{0}' cannot be accessed on a type parameter."),
  Parameter_0_of_accessor_has_or_is_using_private_name_1: diag(4106, 1 /* Error */, "Parameter_0_of_accessor_has_or_is_using_private_name_1_4106", "Parameter '{0}' of accessor has or is using private name '{1}'."),
  Parameter_0_of_accessor_has_or_is_using_name_1_from_private_module_2: diag(4107, 1 /* Error */, "Parameter_0_of_accessor_has_or_is_using_name_1_from_private_module_2_4107", "Parameter '{0}' of accessor has or is using name '{1}' from private module '{2}'."),
  Parameter_0_of_accessor_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named: diag(4108, 1 /* Error */, "Parameter_0_of_accessor_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named_4108", "Parameter '{0}' of accessor has or is using name '{1}' from external module '{2}' but cannot be named."),
  Type_arguments_for_0_circularly_reference_themselves: diag(4109, 1 /* Error */, "Type_arguments_for_0_circularly_reference_themselves_4109", "Type arguments for '{0}' circularly reference themselves."),
  Tuple_type_arguments_circularly_reference_themselves: diag(4110, 1 /* Error */, "Tuple_type_arguments_circularly_reference_themselves_4110", "Tuple type arguments circularly reference themselves."),
  Property_0_comes_from_an_index_signature_so_it_must_be_accessed_with_0: diag(4111, 1 /* Error */, "Property_0_comes_from_an_index_signature_so_it_must_be_accessed_with_0_4111", "Property '{0}' comes from an index signature, so it must be accessed with ['{0}']."),
  This_member_cannot_have_an_override_modifier_because_its_containing_class_0_does_not_extend_another_class: diag(4112, 1 /* Error */, "This_member_cannot_have_an_override_modifier_because_its_containing_class_0_does_not_extend_another__4112", "This member cannot have an 'override' modifier because its containing class '{0}' does not extend another class."),
  This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0: diag(4113, 1 /* Error */, "This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0_4113", "This member cannot have an 'override' modifier because it is not declared in the base class '{0}'."),
  This_member_must_have_an_override_modifier_because_it_overrides_a_member_in_the_base_class_0: diag(4114, 1 /* Error */, "This_member_must_have_an_override_modifier_because_it_overrides_a_member_in_the_base_class_0_4114", "This member must have an 'override' modifier because it overrides a member in the base class '{0}'."),
  This_parameter_property_must_have_an_override_modifier_because_it_overrides_a_member_in_base_class_0: diag(4115, 1 /* Error */, "This_parameter_property_must_have_an_override_modifier_because_it_overrides_a_member_in_base_class_0_4115", "This parameter property must have an 'override' modifier because it overrides a member in base class '{0}'."),
  This_member_must_have_an_override_modifier_because_it_overrides_an_abstract_method_that_is_declared_in_the_base_class_0: diag(4116, 1 /* Error */, "This_member_must_have_an_override_modifier_because_it_overrides_an_abstract_method_that_is_declared__4116", "This member must have an 'override' modifier because it overrides an abstract method that is declared in the base class '{0}'."),
  This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0_Did_you_mean_1: diag(4117, 1 /* Error */, "This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0_Did_you__4117", "This member cannot have an 'override' modifier because it is not declared in the base class '{0}'. Did you mean '{1}'?"),
  The_type_of_this_node_cannot_be_serialized_because_its_property_0_cannot_be_serialized: diag(4118, 1 /* Error */, "The_type_of_this_node_cannot_be_serialized_because_its_property_0_cannot_be_serialized_4118", "The type of this node cannot be serialized because its property '{0}' cannot be serialized."),
  This_member_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_in_the_base_class_0: diag(4119, 1 /* Error */, "This_member_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_in_the_base_4119", "This member must have a JSDoc comment with an '@override' tag because it overrides a member in the base class '{0}'."),
  This_parameter_property_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_in_the_base_class_0: diag(4120, 1 /* Error */, "This_parameter_property_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_4120", "This parameter property must have a JSDoc comment with an '@override' tag because it overrides a member in the base class '{0}'."),
  This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_containing_class_0_does_not_extend_another_class: diag(4121, 1 /* Error */, "This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_containing_class_0_does_not_4121", "This member cannot have a JSDoc comment with an '@override' tag because its containing class '{0}' does not extend another class."),
  This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base_class_0: diag(4122, 1 /* Error */, "This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base__4122", "This member cannot have a JSDoc comment with an '@override' tag because it is not declared in the base class '{0}'."),
  This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base_class_0_Did_you_mean_1: diag(4123, 1 /* Error */, "This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base__4123", "This member cannot have a JSDoc comment with an 'override' tag because it is not declared in the base class '{0}'. Did you mean '{1}'?"),
  Compiler_option_0_of_value_1_is_unstable_Use_nightly_TypeScript_to_silence_this_error_Try_updating_with_npm_install_D_typescript_next: diag(4124, 1 /* Error */, "Compiler_option_0_of_value_1_is_unstable_Use_nightly_TypeScript_to_silence_this_error_Try_updating_w_4124", "Compiler option '{0}' of value '{1}' is unstable. Use nightly TypeScript to silence this error. Try updating with 'npm install -D typescript@next'."),
  Each_declaration_of_0_1_differs_in_its_value_where_2_was_expected_but_3_was_given: diag(4125, 1 /* Error */, "Each_declaration_of_0_1_differs_in_its_value_where_2_was_expected_but_3_was_given_4125", "Each declaration of '{0}.{1}' differs in its value, where '{2}' was expected but '{3}' was given."),
  One_value_of_0_1_is_the_string_2_and_the_other_is_assumed_to_be_an_unknown_numeric_value: diag(4126, 1 /* Error */, "One_value_of_0_1_is_the_string_2_and_the_other_is_assumed_to_be_an_unknown_numeric_value_4126", "One value of '{0}.{1}' is the string '{2}', and the other is assumed to be an unknown numeric value."),
  This_member_cannot_have_an_override_modifier_because_its_name_is_dynamic: diag(4127, 1 /* Error */, "This_member_cannot_have_an_override_modifier_because_its_name_is_dynamic_4127", "This member cannot have an 'override' modifier because its name is dynamic."),
  This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_name_is_dynamic: diag(4128, 1 /* Error */, "This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_name_is_dynamic_4128", "This member cannot have a JSDoc comment with an '@override' tag because its name is dynamic."),
  The_current_host_does_not_support_the_0_option: diag(5001, 1 /* Error */, "The_current_host_does_not_support_the_0_option_5001", "The current host does not support the '{0}' option."),
  Cannot_find_the_common_subdirectory_path_for_the_input_files: diag(5009, 1 /* Error */, "Cannot_find_the_common_subdirectory_path_for_the_input_files_5009", "Cannot find the common subdirectory path for the input files."),
  File_specification_cannot_end_in_a_recursive_directory_wildcard_Asterisk_Asterisk_Colon_0: diag(5010, 1 /* Error */, "File_specification_cannot_end_in_a_recursive_directory_wildcard_Asterisk_Asterisk_Colon_0_5010", "File specification cannot end in a recursive directory wildcard ('**'): '{0}'."),
  Cannot_read_file_0_Colon_1: diag(5012, 1 /* Error */, "Cannot_read_file_0_Colon_1_5012", "Cannot read file '{0}': {1}."),
  Unknown_compiler_option_0: diag(5023, 1 /* Error */, "Unknown_compiler_option_0_5023", "Unknown compiler option '{0}'."),
  Compiler_option_0_requires_a_value_of_type_1: diag(5024, 1 /* Error */, "Compiler_option_0_requires_a_value_of_type_1_5024", "Compiler option '{0}' requires a value of type {1}."),
  Unknown_compiler_option_0_Did_you_mean_1: diag(5025, 1 /* Error */, "Unknown_compiler_option_0_Did_you_mean_1_5025", "Unknown compiler option '{0}'. Did you mean '{1}'?"),
  Could_not_write_file_0_Colon_1: diag(5033, 1 /* Error */, "Could_not_write_file_0_Colon_1_5033", "Could not write file '{0}': {1}."),
  Option_project_cannot_be_mixed_with_source_files_on_a_command_line: diag(5042, 1 /* Error */, "Option_project_cannot_be_mixed_with_source_files_on_a_command_line_5042", "Option 'project' cannot be mixed with source files on a command line."),
  Option_isolatedModules_can_only_be_used_when_either_option_module_is_provided_or_option_target_is_ES2015_or_higher: diag(5047, 1 /* Error */, "Option_isolatedModules_can_only_be_used_when_either_option_module_is_provided_or_option_target_is_ES_5047", "Option 'isolatedModules' can only be used when either option '--module' is provided or option 'target' is 'ES2015' or higher."),
  Option_0_can_only_be_used_when_either_option_inlineSourceMap_or_option_sourceMap_is_provided: diag(5051, 1 /* Error */, "Option_0_can_only_be_used_when_either_option_inlineSourceMap_or_option_sourceMap_is_provided_5051", "Option '{0} can only be used when either option '--inlineSourceMap' or option '--sourceMap' is provided."),
  Option_0_cannot_be_specified_without_specifying_option_1: diag(5052, 1 /* Error */, "Option_0_cannot_be_specified_without_specifying_option_1_5052", "Option '{0}' cannot be specified without specifying option '{1}'."),
  Option_0_cannot_be_specified_with_option_1: diag(5053, 1 /* Error */, "Option_0_cannot_be_specified_with_option_1_5053", "Option '{0}' cannot be specified with option '{1}'."),
  A_tsconfig_json_file_is_already_defined_at_Colon_0: diag(5054, 1 /* Error */, "A_tsconfig_json_file_is_already_defined_at_Colon_0_5054", "A 'tsconfig.json' file is already defined at: '{0}'."),
  Cannot_write_file_0_because_it_would_overwrite_input_file: diag(5055, 1 /* Error */, "Cannot_write_file_0_because_it_would_overwrite_input_file_5055", "Cannot write file '{0}' because it would overwrite input file."),
  Cannot_write_file_0_because_it_would_be_overwritten_by_multiple_input_files: diag(5056, 1 /* Error */, "Cannot_write_file_0_because_it_would_be_overwritten_by_multiple_input_files_5056", "Cannot write file '{0}' because it would be overwritten by multiple input files."),
  Cannot_find_a_tsconfig_json_file_at_the_specified_directory_Colon_0: diag(5057, 1 /* Error */, "Cannot_find_a_tsconfig_json_file_at_the_specified_directory_Colon_0_5057", "Cannot find a tsconfig.json file at the specified directory: '{0}'."),
  The_specified_path_does_not_exist_Colon_0: diag(5058, 1 /* Error */, "The_specified_path_does_not_exist_Colon_0_5058", "The specified path does not exist: '{0}'."),
  Invalid_value_for_reactNamespace_0_is_not_a_valid_identifier: diag(5059, 1 /* Error */, "Invalid_value_for_reactNamespace_0_is_not_a_valid_identifier_5059", "Invalid value for '--reactNamespace'. '{0}' is not a valid identifier."),
  Pattern_0_can_have_at_most_one_Asterisk_character: diag(5061, 1 /* Error */, "Pattern_0_can_have_at_most_one_Asterisk_character_5061", "Pattern '{0}' can have at most one '*' character."),
  Substitution_0_in_pattern_1_can_have_at_most_one_Asterisk_character: diag(5062, 1 /* Error */, "Substitution_0_in_pattern_1_can_have_at_most_one_Asterisk_character_5062", "Substitution '{0}' in pattern '{1}' can have at most one '*' character."),
  Substitutions_for_pattern_0_should_be_an_array: diag(5063, 1 /* Error */, "Substitutions_for_pattern_0_should_be_an_array_5063", "Substitutions for pattern '{0}' should be an array."),
  Substitution_0_for_pattern_1_has_incorrect_type_expected_string_got_2: diag(5064, 1 /* Error */, "Substitution_0_for_pattern_1_has_incorrect_type_expected_string_got_2_5064", "Substitution '{0}' for pattern '{1}' has incorrect type, expected 'string', got '{2}'."),
  File_specification_cannot_contain_a_parent_directory_that_appears_after_a_recursive_directory_wildcard_Asterisk_Asterisk_Colon_0: diag(5065, 1 /* Error */, "File_specification_cannot_contain_a_parent_directory_that_appears_after_a_recursive_directory_wildca_5065", "File specification cannot contain a parent directory ('..') that appears after a recursive directory wildcard ('**'): '{0}'."),
  Substitutions_for_pattern_0_shouldn_t_be_an_empty_array: diag(5066, 1 /* Error */, "Substitutions_for_pattern_0_shouldn_t_be_an_empty_array_5066", "Substitutions for pattern '{0}' shouldn't be an empty array."),
  Invalid_value_for_jsxFactory_0_is_not_a_valid_identifier_or_qualified_name: diag(5067, 1 /* Error */, "Invalid_value_for_jsxFactory_0_is_not_a_valid_identifier_or_qualified_name_5067", "Invalid value for 'jsxFactory'. '{0}' is not a valid identifier or qualified-name."),
  Adding_a_tsconfig_json_file_will_help_organize_projects_that_contain_both_TypeScript_and_JavaScript_files_Learn_more_at_https_Colon_Slash_Slashaka_ms_Slashtsconfig: diag(5068, 1 /* Error */, "Adding_a_tsconfig_json_file_will_help_organize_projects_that_contain_both_TypeScript_and_JavaScript__5068", "Adding a tsconfig.json file will help organize projects that contain both TypeScript and JavaScript files. Learn more at https://aka.ms/tsconfig."),
  Option_0_cannot_be_specified_without_specifying_option_1_or_option_2: diag(5069, 1 /* Error */, "Option_0_cannot_be_specified_without_specifying_option_1_or_option_2_5069", "Option '{0}' cannot be specified without specifying option '{1}' or option '{2}'."),
  Option_resolveJsonModule_cannot_be_specified_when_moduleResolution_is_set_to_classic: diag(5070, 1 /* Error */, "Option_resolveJsonModule_cannot_be_specified_when_moduleResolution_is_set_to_classic_5070", "Option '--resolveJsonModule' cannot be specified when 'moduleResolution' is set to 'classic'."),
  Option_resolveJsonModule_cannot_be_specified_when_module_is_set_to_none_system_or_umd: diag(5071, 1 /* Error */, "Option_resolveJsonModule_cannot_be_specified_when_module_is_set_to_none_system_or_umd_5071", "Option '--resolveJsonModule' cannot be specified when 'module' is set to 'none', 'system', or 'umd'."),
  Unknown_build_option_0: diag(5072, 1 /* Error */, "Unknown_build_option_0_5072", "Unknown build option '{0}'."),
  Build_option_0_requires_a_value_of_type_1: diag(5073, 1 /* Error */, "Build_option_0_requires_a_value_of_type_1_5073", "Build option '{0}' requires a value of type {1}."),
  Option_incremental_can_only_be_specified_using_tsconfig_emitting_to_single_file_or_when_option_tsBuildInfoFile_is_specified: diag(5074, 1 /* Error */, "Option_incremental_can_only_be_specified_using_tsconfig_emitting_to_single_file_or_when_option_tsBui_5074", "Option '--incremental' can only be specified using tsconfig, emitting to single file or when option '--tsBuildInfoFile' is specified."),
  _0_is_assignable_to_the_constraint_of_type_1_but_1_could_be_instantiated_with_a_different_subtype_of_constraint_2: diag(5075, 1 /* Error */, "_0_is_assignable_to_the_constraint_of_type_1_but_1_could_be_instantiated_with_a_different_subtype_of_5075", "'{0}' is assignable to the constraint of type '{1}', but '{1}' could be instantiated with a different subtype of constraint '{2}'."),
  _0_and_1_operations_cannot_be_mixed_without_parentheses: diag(5076, 1 /* Error */, "_0_and_1_operations_cannot_be_mixed_without_parentheses_5076", "'{0}' and '{1}' operations cannot be mixed without parentheses."),
  Unknown_build_option_0_Did_you_mean_1: diag(5077, 1 /* Error */, "Unknown_build_option_0_Did_you_mean_1_5077", "Unknown build option '{0}'. Did you mean '{1}'?"),
  Unknown_watch_option_0: diag(5078, 1 /* Error */, "Unknown_watch_option_0_5078", "Unknown watch option '{0}'."),
  Unknown_watch_option_0_Did_you_mean_1: diag(5079, 1 /* Error */, "Unknown_watch_option_0_Did_you_mean_1_5079", "Unknown watch option '{0}'. Did you mean '{1}'?"),
  Watch_option_0_requires_a_value_of_type_1: diag(5080, 1 /* Error */, "Watch_option_0_requires_a_value_of_type_1_5080", "Watch option '{0}' requires a value of type {1}."),
  Cannot_find_a_tsconfig_json_file_at_the_current_directory_Colon_0: diag(5081, 1 /* Error */, "Cannot_find_a_tsconfig_json_file_at_the_current_directory_Colon_0_5081", "Cannot find a tsconfig.json file at the current directory: {0}."),
  _0_could_be_instantiated_with_an_arbitrary_type_which_could_be_unrelated_to_1: diag(5082, 1 /* Error */, "_0_could_be_instantiated_with_an_arbitrary_type_which_could_be_unrelated_to_1_5082", "'{0}' could be instantiated with an arbitrary type which could be unrelated to '{1}'."),
  Cannot_read_file_0: diag(5083, 1 /* Error */, "Cannot_read_file_0_5083", "Cannot read file '{0}'."),
  A_tuple_member_cannot_be_both_optional_and_rest: diag(5085, 1 /* Error */, "A_tuple_member_cannot_be_both_optional_and_rest_5085", "A tuple member cannot be both optional and rest."),
  A_labeled_tuple_element_is_declared_as_optional_with_a_question_mark_after_the_name_and_before_the_colon_rather_than_after_the_type: diag(5086, 1 /* Error */, "A_labeled_tuple_element_is_declared_as_optional_with_a_question_mark_after_the_name_and_before_the_c_5086", "A labeled tuple element is declared as optional with a question mark after the name and before the colon, rather than after the type."),
  A_labeled_tuple_element_is_declared_as_rest_with_a_before_the_name_rather_than_before_the_type: diag(5087, 1 /* Error */, "A_labeled_tuple_element_is_declared_as_rest_with_a_before_the_name_rather_than_before_the_type_5087", "A labeled tuple element is declared as rest with a '...' before the name, rather than before the type."),
  The_inferred_type_of_0_references_a_type_with_a_cyclic_structure_which_cannot_be_trivially_serialized_A_type_annotation_is_necessary: diag(5088, 1 /* Error */, "The_inferred_type_of_0_references_a_type_with_a_cyclic_structure_which_cannot_be_trivially_serialize_5088", "The inferred type of '{0}' references a type with a cyclic structure which cannot be trivially serialized. A type annotation is necessary."),
  Option_0_cannot_be_specified_when_option_jsx_is_1: diag(5089, 1 /* Error */, "Option_0_cannot_be_specified_when_option_jsx_is_1_5089", "Option '{0}' cannot be specified when option 'jsx' is '{1}'."),
  Non_relative_paths_are_not_allowed_when_baseUrl_is_not_set_Did_you_forget_a_leading_Slash: diag(5090, 1 /* Error */, "Non_relative_paths_are_not_allowed_when_baseUrl_is_not_set_Did_you_forget_a_leading_Slash_5090", "Non-relative paths are not allowed when 'baseUrl' is not set. Did you forget a leading './'?"),
  Option_preserveConstEnums_cannot_be_disabled_when_0_is_enabled: diag(5091, 1 /* Error */, "Option_preserveConstEnums_cannot_be_disabled_when_0_is_enabled_5091", "Option 'preserveConstEnums' cannot be disabled when '{0}' is enabled."),
  The_root_value_of_a_0_file_must_be_an_object: diag(5092, 1 /* Error */, "The_root_value_of_a_0_file_must_be_an_object_5092", "The root value of a '{0}' file must be an object."),
  Compiler_option_0_may_only_be_used_with_build: diag(5093, 1 /* Error */, "Compiler_option_0_may_only_be_used_with_build_5093", "Compiler option '--{0}' may only be used with '--build'."),
  Compiler_option_0_may_not_be_used_with_build: diag(5094, 1 /* Error */, "Compiler_option_0_may_not_be_used_with_build_5094", "Compiler option '--{0}' may not be used with '--build'."),
  Option_0_can_only_be_used_when_module_is_set_to_preserve_or_to_es2015_or_later: diag(5095, 1 /* Error */, "Option_0_can_only_be_used_when_module_is_set_to_preserve_or_to_es2015_or_later_5095", "Option '{0}' can only be used when 'module' is set to 'preserve' or to 'es2015' or later."),
  Option_allowImportingTsExtensions_can_only_be_used_when_either_noEmit_or_emitDeclarationOnly_is_set: diag(5096, 1 /* Error */, "Option_allowImportingTsExtensions_can_only_be_used_when_either_noEmit_or_emitDeclarationOnly_is_set_5096", "Option 'allowImportingTsExtensions' can only be used when either 'noEmit' or 'emitDeclarationOnly' is set."),
  An_import_path_can_only_end_with_a_0_extension_when_allowImportingTsExtensions_is_enabled: diag(5097, 1 /* Error */, "An_import_path_can_only_end_with_a_0_extension_when_allowImportingTsExtensions_is_enabled_5097", "An import path can only end with a '{0}' extension when 'allowImportingTsExtensions' is enabled."),
  Option_0_can_only_be_used_when_moduleResolution_is_set_to_node16_nodenext_or_bundler: diag(5098, 1 /* Error */, "Option_0_can_only_be_used_when_moduleResolution_is_set_to_node16_nodenext_or_bundler_5098", "Option '{0}' can only be used when 'moduleResolution' is set to 'node16', 'nodenext', or 'bundler'."),
  Option_0_is_deprecated_and_will_stop_functioning_in_TypeScript_1_Specify_compilerOption_ignoreDeprecations_Colon_2_to_silence_this_error: diag(5101, 1 /* Error */, "Option_0_is_deprecated_and_will_stop_functioning_in_TypeScript_1_Specify_compilerOption_ignoreDeprec_5101", `Option '{0}' is deprecated and will stop functioning in TypeScript {1}. Specify compilerOption '"ignoreDeprecations": "{2}"' to silence this error.`),
  Option_0_has_been_removed_Please_remove_it_from_your_configuration: diag(5102, 1 /* Error */, "Option_0_has_been_removed_Please_remove_it_from_your_configuration_5102", "Option '{0}' has been removed. Please remove it from your configuration."),
  Invalid_value_for_ignoreDeprecations: diag(5103, 1 /* Error */, "Invalid_value_for_ignoreDeprecations_5103", "Invalid value for '--ignoreDeprecations'."),
  Option_0_is_redundant_and_cannot_be_specified_with_option_1: diag(5104, 1 /* Error */, "Option_0_is_redundant_and_cannot_be_specified_with_option_1_5104", "Option '{0}' is redundant and cannot be specified with option '{1}'."),
  Option_verbatimModuleSyntax_cannot_be_used_when_module_is_set_to_UMD_AMD_or_System: diag(5105, 1 /* Error */, "Option_verbatimModuleSyntax_cannot_be_used_when_module_is_set_to_UMD_AMD_or_System_5105", "Option 'verbatimModuleSyntax' cannot be used when 'module' is set to 'UMD', 'AMD', or 'System'."),
  Use_0_instead: diag(5106, 3 /* Message */, "Use_0_instead_5106", "Use '{0}' instead."),
  Option_0_1_is_deprecated_and_will_stop_functioning_in_TypeScript_2_Specify_compilerOption_ignoreDeprecations_Colon_3_to_silence_this_error: diag(5107, 1 /* Error */, "Option_0_1_is_deprecated_and_will_stop_functioning_in_TypeScript_2_Specify_compilerOption_ignoreDepr_5107", `Option '{0}***REMOVED***{1}' is deprecated and will stop functioning in TypeScript {2}. Specify compilerOption '"ignoreDeprecations": "{3}"' to silence this error.`),
  Option_0_1_has_been_removed_Please_remove_it_from_your_configuration: diag(5108, 1 /* Error */, "Option_0_1_has_been_removed_Please_remove_it_from_your_configuration_5108", "Option '{0}***REMOVED***{1}' has been removed. Please remove it from your configuration."),
  Option_moduleResolution_must_be_set_to_0_or_left_unspecified_when_option_module_is_set_to_1: diag(5109, 1 /* Error */, "Option_moduleResolution_must_be_set_to_0_or_left_unspecified_when_option_module_is_set_to_1_5109", "Option 'moduleResolution' must be set to '{0}' (or left unspecified) when option 'module' is set to '{1}'."),
  Option_module_must_be_set_to_0_when_option_moduleResolution_is_set_to_1: diag(5110, 1 /* Error */, "Option_module_must_be_set_to_0_when_option_moduleResolution_is_set_to_1_5110", "Option 'module' must be set to '{0}' when option 'moduleResolution' is set to '{1}'."),
  Generates_a_sourcemap_for_each_corresponding_d_ts_file: diag(6e3, 3 /* Message */, "Generates_a_sourcemap_for_each_corresponding_d_ts_file_6000", "Generates a sourcemap for each corresponding '.d.ts' file."),
  Concatenate_and_emit_output_to_single_file: diag(6001, 3 /* Message */, "Concatenate_and_emit_output_to_single_file_6001", "Concatenate and emit output to single file."),
  Generates_corresponding_d_ts_file: diag(6002, 3 /* Message */, "Generates_corresponding_d_ts_file_6002", "Generates corresponding '.d.ts' file."),
  Specify_the_location_where_debugger_should_locate_TypeScript_files_instead_of_source_locations: diag(6004, 3 /* Message */, "Specify_the_location_where_debugger_should_locate_TypeScript_files_instead_of_source_locations_6004", "Specify the location where debugger should locate TypeScript files instead of source locations."),
  Watch_input_files: diag(6005, 3 /* Message */, "Watch_input_files_6005", "Watch input files."),
  Redirect_output_structure_to_the_directory: diag(6006, 3 /* Message */, "Redirect_output_structure_to_the_directory_6006", "Redirect output structure to the directory."),
  Do_not_erase_const_enum_declarations_in_generated_code: diag(6007, 3 /* Message */, "Do_not_erase_const_enum_declarations_in_generated_code_6007", "Do not erase const enum declarations in generated code."),
  Do_not_emit_outputs_if_any_errors_were_reported: diag(6008, 3 /* Message */, "Do_not_emit_outputs_if_any_errors_were_reported_6008", "Do not emit outputs if any errors were reported."),
  Do_not_emit_comments_to_output: diag(6009, 3 /* Message */, "Do_not_emit_comments_to_output_6009", "Do not emit comments to output."),
  Do_not_emit_outputs: diag(6010, 3 /* Message */, "Do_not_emit_outputs_6010", "Do not emit outputs."),
  Allow_default_imports_from_modules_with_no_default_export_This_does_not_affect_code_emit_just_typechecking: diag(6011, 3 /* Message */, "Allow_default_imports_from_modules_with_no_default_export_This_does_not_affect_code_emit_just_typech_6011", "Allow default imports from modules with no default export. This does not affect code emit, just typechecking."),
  Skip_type_checking_of_declaration_files: diag(6012, 3 /* Message */, "Skip_type_checking_of_declaration_files_6012", "Skip type checking of declaration files."),
  Do_not_resolve_the_real_path_of_symlinks: diag(6013, 3 /* Message */, "Do_not_resolve_the_real_path_of_symlinks_6013", "Do not resolve the real path of symlinks."),
  Only_emit_d_ts_declaration_files: diag(6014, 3 /* Message */, "Only_emit_d_ts_declaration_files_6014", "Only emit '.d.ts' declaration files."),
  Specify_ECMAScript_target_version: diag(6015, 3 /* Message */, "Specify_ECMAScript_target_version_6015", "Specify ECMAScript target version."),
  Specify_module_code_generation: diag(6016, 3 /* Message */, "Specify_module_code_generation_6016", "Specify module code generation."),
  Print_this_message: diag(6017, 3 /* Message */, "Print_this_message_6017", "Print this message."),
  Print_the_compiler_s_version: diag(6019, 3 /* Message */, "Print_the_compiler_s_version_6019", "Print the compiler's version."),
  Compile_the_project_given_the_path_to_its_configuration_file_or_to_a_folder_with_a_tsconfig_json: diag(6020, 3 /* Message */, "Compile_the_project_given_the_path_to_its_configuration_file_or_to_a_folder_with_a_tsconfig_json_6020", "Compile the project given the path to its configuration file, or to a folder with a 'tsconfig.json'."),
  Syntax_Colon_0: diag(6023, 3 /* Message */, "Syntax_Colon_0_6023", "Syntax: {0}"),
  options: diag(6024, 3 /* Message */, "options_6024", "options"),
  file: diag(6025, 3 /* Message */, "file_6025", "file"),
  Examples_Colon_0: diag(6026, 3 /* Message */, "Examples_Colon_0_6026", "Examples: {0}"),
  Options_Colon: diag(6027, 3 /* Message */, "Options_Colon_6027", "Options:"),
  Version_0: diag(6029, 3 /* Message */, "Version_0_6029", "Version {0}"),
  Insert_command_line_options_and_files_from_a_file: diag(6030, 3 /* Message */, "Insert_command_line_options_and_files_from_a_file_6030", "Insert command line options and files from a file."),
  Starting_compilation_in_watch_mode: diag(6031, 3 /* Message */, "Starting_compilation_in_watch_mode_6031", "Starting compilation in watch mode..."),
  File_change_detected_Starting_incremental_compilation: diag(6032, 3 /* Message */, "File_change_detected_Starting_incremental_compilation_6032", "File change detected. Starting incremental compilation..."),
  KIND: diag(6034, 3 /* Message */, "KIND_6034", "KIND"),
  FILE: diag(6035, 3 /* Message */, "FILE_6035", "FILE"),
  VERSION: diag(6036, 3 /* Message */, "VERSION_6036", "VERSION"),
  LOCATION: diag(6037, 3 /* Message */, "LOCATION_6037", "LOCATION"),
  DIRECTORY: diag(6038, 3 /* Message */, "DIRECTORY_6038", "DIRECTORY"),
  STRATEGY: diag(6039, 3 /* Message */, "STRATEGY_6039", "STRATEGY"),
  FILE_OR_DIRECTORY: diag(6040, 3 /* Message */, "FILE_OR_DIRECTORY_6040", "FILE OR DIRECTORY"),
  Errors_Files: diag(6041, 3 /* Message */, "Errors_Files_6041", "Errors  Files"),
  Generates_corresponding_map_file: diag(6043, 3 /* Message */, "Generates_corresponding_map_file_6043", "Generates corresponding '.map' file."),
  Compiler_option_0_expects_an_argument: diag(6044, 1 /* Error */, "Compiler_option_0_expects_an_argument_6044", "Compiler option '{0}' expects an argument."),
  Unterminated_quoted_string_in_response_file_0: diag(6045, 1 /* Error */, "Unterminated_quoted_string_in_response_file_0_6045", "Unterminated quoted string in response file '{0}'."),
  Argument_for_0_option_must_be_Colon_1: diag(6046, 1 /* Error */, "Argument_for_0_option_must_be_Colon_1_6046", "Argument for '{0}' option must be: {1}."),
  Locale_must_be_of_the_form_language_or_language_territory_For_example_0_or_1: diag(6048, 1 /* Error */, "Locale_must_be_of_the_form_language_or_language_territory_For_example_0_or_1_6048", "Locale must be of the form <language> or <language>-<territory>. For example '{0}' or '{1}'."),
  Unable_to_open_file_0: diag(6050, 1 /* Error */, "Unable_to_open_file_0_6050", "Unable to open file '{0}'."),
  Corrupted_locale_file_0: diag(6051, 1 /* Error */, "Corrupted_locale_file_0_6051", "Corrupted locale file {0}."),
  Raise_error_on_expressions_and_declarations_with_an_implied_any_type: diag(6052, 3 /* Message */, "Raise_error_on_expressions_and_declarations_with_an_implied_any_type_6052", "Raise error on expressions and declarations with an implied 'any' type."),
  File_0_not_found: diag(6053, 1 /* Error */, "File_0_not_found_6053", "File '{0}' not found."),
  File_0_has_an_unsupported_extension_The_only_supported_extensions_are_1: diag(6054, 1 /* Error */, "File_0_has_an_unsupported_extension_The_only_supported_extensions_are_1_6054", "File '{0}' has an unsupported extension. The only supported extensions are {1}."),
  Suppress_noImplicitAny_errors_for_indexing_objects_lacking_index_signatures: diag(6055, 3 /* Message */, "Suppress_noImplicitAny_errors_for_indexing_objects_lacking_index_signatures_6055", "Suppress noImplicitAny errors for indexing objects lacking index signatures."),
  Do_not_emit_declarations_for_code_that_has_an_internal_annotation: diag(6056, 3 /* Message */, "Do_not_emit_declarations_for_code_that_has_an_internal_annotation_6056", "Do not emit declarations for code that has an '@internal' annotation."),
  Specify_the_root_directory_of_input_files_Use_to_control_the_output_directory_structure_with_outDir: diag(6058, 3 /* Message */, "Specify_the_root_directory_of_input_files_Use_to_control_the_output_directory_structure_with_outDir_6058", "Specify the root directory of input files. Use to control the output directory structure with --outDir."),
  File_0_is_not_under_rootDir_1_rootDir_is_expected_to_contain_all_source_files: diag(6059, 1 /* Error */, "File_0_is_not_under_rootDir_1_rootDir_is_expected_to_contain_all_source_files_6059", "File '{0}' is not under 'rootDir' '{1}'. 'rootDir' is expected to contain all source files."),
  Specify_the_end_of_line_sequence_to_be_used_when_emitting_files_Colon_CRLF_dos_or_LF_unix: diag(6060, 3 /* Message */, "Specify_the_end_of_line_sequence_to_be_used_when_emitting_files_Colon_CRLF_dos_or_LF_unix_6060", "Specify the end of line sequence to be used when emitting files: 'CRLF' (dos) or 'LF' (unix)."),
  NEWLINE: diag(6061, 3 /* Message */, "NEWLINE_6061", "NEWLINE"),
  Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_null_on_command_line: diag(6064, 1 /* Error */, "Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_null_on_command_line_6064", "Option '{0}' can only be specified in 'tsconfig.json' file or set to 'null' on command line."),
  Enables_experimental_support_for_ES7_decorators: diag(6065, 3 /* Message */, "Enables_experimental_support_for_ES7_decorators_6065", "Enables experimental support for ES7 decorators."),
  Enables_experimental_support_for_emitting_type_metadata_for_decorators: diag(6066, 3 /* Message */, "Enables_experimental_support_for_emitting_type_metadata_for_decorators_6066", "Enables experimental support for emitting type metadata for decorators."),
  Initializes_a_TypeScript_project_and_creates_a_tsconfig_json_file: diag(6070, 3 /* Message */, "Initializes_a_TypeScript_project_and_creates_a_tsconfig_json_file_6070", "Initializes a TypeScript project and creates a tsconfig.json file."),
  Successfully_created_a_tsconfig_json_file: diag(6071, 3 /* Message */, "Successfully_created_a_tsconfig_json_file_6071", "Successfully created a tsconfig.json file."),
  Suppress_excess_property_checks_for_object_literals: diag(6072, 3 /* Message */, "Suppress_excess_property_checks_for_object_literals_6072", "Suppress excess property checks for object literals."),
  Stylize_errors_and_messages_using_color_and_context_experimental: diag(6073, 3 /* Message */, "Stylize_errors_and_messages_using_color_and_context_experimental_6073", "Stylize errors and messages using color and context (experimental)."),
  Do_not_report_errors_on_unused_labels: diag(6074, 3 /* Message */, "Do_not_report_errors_on_unused_labels_6074", "Do not report errors on unused labels."),
  Report_error_when_not_all_code_paths_in_function_return_a_value: diag(6075, 3 /* Message */, "Report_error_when_not_all_code_paths_in_function_return_a_value_6075", "Report error when not all code paths in function return a value."),
  Report_errors_for_fallthrough_cases_in_switch_statement: diag(6076, 3 /* Message */, "Report_errors_for_fallthrough_cases_in_switch_statement_6076", "Report errors for fallthrough cases in switch statement."),
  Do_not_report_errors_on_unreachable_code: diag(6077, 3 /* Message */, "Do_not_report_errors_on_unreachable_code_6077", "Do not report errors on unreachable code."),
  Disallow_inconsistently_cased_references_to_the_same_file: diag(6078, 3 /* Message */, "Disallow_inconsistently_cased_references_to_the_same_file_6078", "Disallow inconsistently-cased references to the same file."),
  Specify_library_files_to_be_included_in_the_compilation: diag(6079, 3 /* Message */, "Specify_library_files_to_be_included_in_the_compilation_6079", "Specify library files to be included in the compilation."),
  Specify_JSX_code_generation: diag(6080, 3 /* Message */, "Specify_JSX_code_generation_6080", "Specify JSX code generation."),
  Only_amd_and_system_modules_are_supported_alongside_0: diag(6082, 1 /* Error */, "Only_amd_and_system_modules_are_supported_alongside_0_6082", "Only 'amd' and 'system' modules are supported alongside --{0}."),
  Base_directory_to_resolve_non_absolute_module_names: diag(6083, 3 /* Message */, "Base_directory_to_resolve_non_absolute_module_names_6083", "Base directory to resolve non-absolute module names."),
  Deprecated_Use_jsxFactory_instead_Specify_the_object_invoked_for_createElement_when_targeting_react_JSX_emit: diag(6084, 3 /* Message */, "Deprecated_Use_jsxFactory_instead_Specify_the_object_invoked_for_createElement_when_targeting_react__6084", "[Deprecated] Use '--jsxFactory' instead. Specify the object invoked for createElement when targeting 'react' JSX emit"),
  Enable_tracing_of_the_name_resolution_process: diag(6085, 3 /* Message */, "Enable_tracing_of_the_name_resolution_process_6085", "Enable tracing of the name resolution process."),
  Resolving_module_0_from_1: diag(6086, 3 /* Message */, "Resolving_module_0_from_1_6086", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving module '{0}' from '{1}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Explicitly_specified_module_resolution_kind_Colon_0: diag(6087, 3 /* Message */, "Explicitly_specified_module_resolution_kind_Colon_0_6087", "Explicitly specified module resolution kind: '{0}'."),
  Module_resolution_kind_is_not_specified_using_0: diag(6088, 3 /* Message */, "Module_resolution_kind_is_not_specified_using_0_6088", "Module resolution kind is not specified, using '{0}'."),
  Module_name_0_was_successfully_resolved_to_1: diag(6089, 3 /* Message */, "Module_name_0_was_successfully_resolved_to_1_6089", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Module name '{0}' was successfully resolved to '{1}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Module_name_0_was_not_resolved: diag(6090, 3 /* Message */, "Module_name_0_was_not_resolved_6090", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Module name '{0}' was not resolved. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  paths_option_is_specified_looking_for_a_pattern_to_match_module_name_0: diag(6091, 3 /* Message */, "paths_option_is_specified_looking_for_a_pattern_to_match_module_name_0_6091", "'paths' option is specified, looking for a pattern to match module name '{0}'."),
  Module_name_0_matched_pattern_1: diag(6092, 3 /* Message */, "Module_name_0_matched_pattern_1_6092", "Module name '{0}', matched pattern '{1}'."),
  Trying_substitution_0_candidate_module_location_Colon_1: diag(6093, 3 /* Message */, "Trying_substitution_0_candidate_module_location_Colon_1_6093", "Trying substitution '{0}', candidate module location: '{1}'."),
  Resolving_module_name_0_relative_to_base_url_1_2: diag(6094, 3 /* Message */, "Resolving_module_name_0_relative_to_base_url_1_2_6094", "Resolving module name '{0}' relative to base url '{1}' - '{2}'."),
  Loading_module_as_file_Slash_folder_candidate_module_location_0_target_file_types_Colon_1: diag(6095, 3 /* Message */, "Loading_module_as_file_Slash_folder_candidate_module_location_0_target_file_types_Colon_1_6095", "Loading module as file / folder, candidate module location '{0}', target file types: {1}."),
  File_0_does_not_exist: diag(6096, 3 /* Message */, "File_0_does_not_exist_6096", "File '{0}' does not exist."),
  File_0_exists_use_it_as_a_name_resolution_result: diag(6097, 3 /* Message */, "File_0_exists_use_it_as_a_name_resolution_result_6097", "File '{0}' exists - use it as a name resolution result."),
  Loading_module_0_from_node_modules_folder_target_file_types_Colon_1: diag(6098, 3 /* Message */, "Loading_module_0_from_node_modules_folder_target_file_types_Colon_1_6098", "Loading module '{0}' from 'node_modules' folder, target file types: {1}."),
  Found_package_json_at_0: diag(6099, 3 /* Message */, "Found_package_json_at_0_6099", "Found 'package.json' at '{0}'."),
  package_json_does_not_have_a_0_field: diag(6100, 3 /* Message */, "package_json_does_not_have_a_0_field_6100", "'package.json' does not have a '{0}' field."),
  package_json_has_0_field_1_that_references_2: diag(6101, 3 /* Message */, "package_json_has_0_field_1_that_references_2_6101", "'package.json' has '{0}' field '{1}' that references '{2}'."),
  Allow_javascript_files_to_be_compiled: diag(6102, 3 /* Message */, "Allow_javascript_files_to_be_compiled_6102", "Allow javascript files to be compiled."),
  Checking_if_0_is_the_longest_matching_prefix_for_1_2: diag(6104, 3 /* Message */, "Checking_if_0_is_the_longest_matching_prefix_for_1_2_6104", "Checking if '{0}' is the longest matching prefix for '{1}' - '{2}'."),
  Expected_type_of_0_field_in_package_json_to_be_1_got_2: diag(6105, 3 /* Message */, "Expected_type_of_0_field_in_package_json_to_be_1_got_2_6105", "Expected type of '{0}' field in 'package.json' to be '{1}', got '{2}'."),
  baseUrl_option_is_set_to_0_using_this_value_to_resolve_non_relative_module_name_1: diag(6106, 3 /* Message */, "baseUrl_option_is_set_to_0_using_this_value_to_resolve_non_relative_module_name_1_6106", "'baseUrl' option is set to '{0}', using this value to resolve non-relative module name '{1}'."),
  rootDirs_option_is_set_using_it_to_resolve_relative_module_name_0: diag(6107, 3 /* Message */, "rootDirs_option_is_set_using_it_to_resolve_relative_module_name_0_6107", "'rootDirs' option is set, using it to resolve relative module name '{0}'."),
  Longest_matching_prefix_for_0_is_1: diag(6108, 3 /* Message */, "Longest_matching_prefix_for_0_is_1_6108", "Longest matching prefix for '{0}' is '{1}'."),
  Loading_0_from_the_root_dir_1_candidate_location_2: diag(6109, 3 /* Message */, "Loading_0_from_the_root_dir_1_candidate_location_2_6109", "Loading '{0}' from the root dir '{1}', candidate location '{2}'."),
  Trying_other_entries_in_rootDirs: diag(6110, 3 /* Message */, "Trying_other_entries_in_rootDirs_6110", "Trying other entries in 'rootDirs'."),
  Module_resolution_using_rootDirs_has_failed: diag(6111, 3 /* Message */, "Module_resolution_using_rootDirs_has_failed_6111", "Module resolution using 'rootDirs' has failed."),
  Do_not_emit_use_strict_directives_in_module_output: diag(6112, 3 /* Message */, "Do_not_emit_use_strict_directives_in_module_output_6112", "Do not emit 'use strict' directives in module output."),
  Enable_strict_null_checks: diag(6113, 3 /* Message */, "Enable_strict_null_checks_6113", "Enable strict null checks."),
  Unknown_option_excludes_Did_you_mean_exclude: diag(6114, 1 /* Error */, "Unknown_option_excludes_Did_you_mean_exclude_6114", "Unknown option 'excludes'. Did you mean 'exclude'?"),
  Raise_error_on_this_expressions_with_an_implied_any_type: diag(6115, 3 /* Message */, "Raise_error_on_this_expressions_with_an_implied_any_type_6115", "Raise error on 'this' expressions with an implied 'any' type."),
  Resolving_type_reference_directive_0_containing_file_1_root_directory_2: diag(6116, 3 /* Message */, "Resolving_type_reference_directive_0_containing_file_1_root_directory_2_6116", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving type reference directive '{0}', containing file '{1}', root directory '{2}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Type_reference_directive_0_was_successfully_resolved_to_1_primary_Colon_2: diag(6119, 3 /* Message */, "Type_reference_directive_0_was_successfully_resolved_to_1_primary_Colon_2_6119", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Type reference directive '{0}' was successfully resolved to '{1}', primary: {2}. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Type_reference_directive_0_was_not_resolved: diag(6120, 3 /* Message */, "Type_reference_directive_0_was_not_resolved_6120", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Type reference directive '{0}' was not resolved. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Resolving_with_primary_search_path_0: diag(6121, 3 /* Message */, "Resolving_with_primary_search_path_0_6121", "Resolving with primary search path '{0}'."),
  Root_directory_cannot_be_determined_skipping_primary_search_paths: diag(6122, 3 /* Message */, "Root_directory_cannot_be_determined_skipping_primary_search_paths_6122", "Root directory cannot be determined, skipping primary search paths."),
  Resolving_type_reference_directive_0_containing_file_1_root_directory_not_set: diag(6123, 3 /* Message */, "Resolving_type_reference_directive_0_containing_file_1_root_directory_not_set_6123", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving type reference directive '{0}', containing file '{1}', root directory not set. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Type_declaration_files_to_be_included_in_compilation: diag(6124, 3 /* Message */, "Type_declaration_files_to_be_included_in_compilation_6124", "Type declaration files to be included in compilation."),
  Looking_up_in_node_modules_folder_initial_location_0: diag(6125, 3 /* Message */, "Looking_up_in_node_modules_folder_initial_location_0_6125", "Looking up in 'node_modules' folder, initial location '{0}'."),
  Containing_file_is_not_specified_and_root_directory_cannot_be_determined_skipping_lookup_in_node_modules_folder: diag(6126, 3 /* Message */, "Containing_file_is_not_specified_and_root_directory_cannot_be_determined_skipping_lookup_in_node_mod_6126", "Containing file is not specified and root directory cannot be determined, skipping lookup in 'node_modules' folder."),
  Resolving_type_reference_directive_0_containing_file_not_set_root_directory_1: diag(6127, 3 /* Message */, "Resolving_type_reference_directive_0_containing_file_not_set_root_directory_1_6127", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving type reference directive '{0}', containing file not set, root directory '{1}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Resolving_type_reference_directive_0_containing_file_not_set_root_directory_not_set: diag(6128, 3 /* Message */, "Resolving_type_reference_directive_0_containing_file_not_set_root_directory_not_set_6128", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving type reference directive '{0}', containing file not set, root directory not set. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Resolving_real_path_for_0_result_1: diag(6130, 3 /* Message */, "Resolving_real_path_for_0_result_1_6130", "Resolving real path for '{0}', result '{1}'."),
  Cannot_compile_modules_using_option_0_unless_the_module_flag_is_amd_or_system: diag(6131, 1 /* Error */, "Cannot_compile_modules_using_option_0_unless_the_module_flag_is_amd_or_system_6131", "Cannot compile modules using option '{0}' unless the '--module' flag is 'amd' or 'system'."),
  File_name_0_has_a_1_extension_stripping_it: diag(6132, 3 /* Message */, "File_name_0_has_a_1_extension_stripping_it_6132", "File name '{0}' has a '{1}' extension - stripping it."),
  _0_is_declared_but_its_value_is_never_read: diag(
    6133,
    1 /* Error */,
    "_0_is_declared_but_its_value_is_never_read_6133",
    "'{0}' is declared but its value is never read.",
    /*reportsUnnecessary*/
    true
  ),
  Report_errors_on_unused_locals: diag(6134, 3 /* Message */, "Report_errors_on_unused_locals_6134", "Report errors on unused locals."),
  Report_errors_on_unused_parameters: diag(6135, 3 /* Message */, "Report_errors_on_unused_parameters_6135", "Report errors on unused parameters."),
  The_maximum_dependency_depth_to_search_under_node_modules_and_load_JavaScript_files: diag(6136, 3 /* Message */, "The_maximum_dependency_depth_to_search_under_node_modules_and_load_JavaScript_files_6136", "The maximum dependency depth to search under node_modules and load JavaScript files."),
  Cannot_import_type_declaration_files_Consider_importing_0_instead_of_1: diag(6137, 1 /* Error */, "Cannot_import_type_declaration_files_Consider_importing_0_instead_of_1_6137", "Cannot import type declaration files. Consider importing '{0}' instead of '{1}'."),
  Property_0_is_declared_but_its_value_is_never_read: diag(
    6138,
    1 /* Error */,
    "Property_0_is_declared_but_its_value_is_never_read_6138",
    "Property '{0}' is declared but its value is never read.",
    /*reportsUnnecessary*/
    true
  ),
  Import_emit_helpers_from_tslib: diag(6139, 3 /* Message */, "Import_emit_helpers_from_tslib_6139", "Import emit helpers from 'tslib'."),
  Auto_discovery_for_typings_is_enabled_in_project_0_Running_extra_resolution_pass_for_module_1_using_cache_location_2: diag(6140, 1 /* Error */, "Auto_discovery_for_typings_is_enabled_in_project_0_Running_extra_resolution_pass_for_module_1_using__6140", "Auto discovery for typings is enabled in project '{0}'. Running extra resolution pass for module '{1}' using cache location '{2}'."),
  Parse_in_strict_mode_and_emit_use_strict_for_each_source_file: diag(6141, 3 /* Message */, "Parse_in_strict_mode_and_emit_use_strict_for_each_source_file_6141", 'Parse in strict mode and emit "use strict" for each source file.'),
  Module_0_was_resolved_to_1_but_jsx_is_not_set: diag(6142, 1 /* Error */, "Module_0_was_resolved_to_1_but_jsx_is_not_set_6142", "Module '{0}' was resolved to '{1}', but '--jsx' is not set."),
  Module_0_was_resolved_as_locally_declared_ambient_module_in_file_1: diag(6144, 3 /* Message */, "Module_0_was_resolved_as_locally_declared_ambient_module_in_file_1_6144", "Module '{0}' was resolved as locally declared ambient module in file '{1}'."),
  Specify_the_JSX_factory_function_to_use_when_targeting_react_JSX_emit_e_g_React_createElement_or_h: diag(6146, 3 /* Message */, "Specify_the_JSX_factory_function_to_use_when_targeting_react_JSX_emit_e_g_React_createElement_or_h_6146", "Specify the JSX factory function to use when targeting 'react' JSX emit, e.g. 'React.createElement' or 'h'."),
  Resolution_for_module_0_was_found_in_cache_from_location_1: diag(6147, 3 /* Message */, "Resolution_for_module_0_was_found_in_cache_from_location_1_6147", "Resolution for module '{0}' was found in cache from location '{1}'."),
  Directory_0_does_not_exist_skipping_all_lookups_in_it: diag(6148, 3 /* Message */, "Directory_0_does_not_exist_skipping_all_lookups_in_it_6148", "Directory '{0}' does not exist, skipping all lookups in it."),
  Show_diagnostic_information: diag(6149, 3 /* Message */, "Show_diagnostic_information_6149", "Show diagnostic information."),
  Show_verbose_diagnostic_information: diag(6150, 3 /* Message */, "Show_verbose_diagnostic_information_6150", "Show verbose diagnostic information."),
  Emit_a_single_file_with_source_maps_instead_of_having_a_separate_file: diag(6151, 3 /* Message */, "Emit_a_single_file_with_source_maps_instead_of_having_a_separate_file_6151", "Emit a single file with source maps instead of having a separate file."),
  Emit_the_source_alongside_the_sourcemaps_within_a_single_file_requires_inlineSourceMap_or_sourceMap_to_be_set: diag(6152, 3 /* Message */, "Emit_the_source_alongside_the_sourcemaps_within_a_single_file_requires_inlineSourceMap_or_sourceMap__6152", "Emit the source alongside the sourcemaps within a single file; requires '--inlineSourceMap' or '--sourceMap' to be set."),
  Transpile_each_file_as_a_separate_module_similar_to_ts_transpileModule: diag(6153, 3 /* Message */, "Transpile_each_file_as_a_separate_module_similar_to_ts_transpileModule_6153", "Transpile each file as a separate module (similar to 'ts.transpileModule')."),
  Print_names_of_generated_files_part_of_the_compilation: diag(6154, 3 /* Message */, "Print_names_of_generated_files_part_of_the_compilation_6154", "Print names of generated files part of the compilation."),
  Print_names_of_files_part_of_the_compilation: diag(6155, 3 /* Message */, "Print_names_of_files_part_of_the_compilation_6155", "Print names of files part of the compilation."),
  The_locale_used_when_displaying_messages_to_the_user_e_g_en_us: diag(6156, 3 /* Message */, "The_locale_used_when_displaying_messages_to_the_user_e_g_en_us_6156", "The locale used when displaying messages to the user (e.g. 'en-us')"),
  Do_not_generate_custom_helper_functions_like_extends_in_compiled_output: diag(6157, 3 /* Message */, "Do_not_generate_custom_helper_functions_like_extends_in_compiled_output_6157", "Do not generate custom helper functions like '__extends' in compiled output."),
  Do_not_include_the_default_library_file_lib_d_ts: diag(6158, 3 /* Message */, "Do_not_include_the_default_library_file_lib_d_ts_6158", "Do not include the default library file (lib.d.ts)."),
  Do_not_add_triple_slash_references_or_imported_modules_to_the_list_of_compiled_files: diag(6159, 3 /* Message */, "Do_not_add_triple_slash_references_or_imported_modules_to_the_list_of_compiled_files_6159", "Do not add triple-slash references or imported modules to the list of compiled files."),
  Deprecated_Use_skipLibCheck_instead_Skip_type_checking_of_default_library_declaration_files: diag(6160, 3 /* Message */, "Deprecated_Use_skipLibCheck_instead_Skip_type_checking_of_default_library_declaration_files_6160", "[Deprecated] Use '--skipLibCheck' instead. Skip type checking of default library declaration files."),
  List_of_folders_to_include_type_definitions_from: diag(6161, 3 /* Message */, "List_of_folders_to_include_type_definitions_from_6161", "List of folders to include type definitions from."),
  Disable_size_limitations_on_JavaScript_projects: diag(6162, 3 /* Message */, "Disable_size_limitations_on_JavaScript_projects_6162", "Disable size limitations on JavaScript projects."),
  The_character_set_of_the_input_files: diag(6163, 3 /* Message */, "The_character_set_of_the_input_files_6163", "The character set of the input files."),
  Skipping_module_0_that_looks_like_an_absolute_URI_target_file_types_Colon_1: diag(6164, 3 /* Message */, "Skipping_module_0_that_looks_like_an_absolute_URI_target_file_types_Colon_1_6164", "Skipping module '{0}' that looks like an absolute URI, target file types: {1}."),
  Do_not_truncate_error_messages: diag(6165, 3 /* Message */, "Do_not_truncate_error_messages_6165", "Do not truncate error messages."),
  Output_directory_for_generated_declaration_files: diag(6166, 3 /* Message */, "Output_directory_for_generated_declaration_files_6166", "Output directory for generated declaration files."),
  A_series_of_entries_which_re_map_imports_to_lookup_locations_relative_to_the_baseUrl: diag(6167, 3 /* Message */, "A_series_of_entries_which_re_map_imports_to_lookup_locations_relative_to_the_baseUrl_6167", "A series of entries which re-map imports to lookup locations relative to the 'baseUrl'."),
  List_of_root_folders_whose_combined_content_represents_the_structure_of_the_project_at_runtime: diag(6168, 3 /* Message */, "List_of_root_folders_whose_combined_content_represents_the_structure_of_the_project_at_runtime_6168", "List of root folders whose combined content represents the structure of the project at runtime."),
  Show_all_compiler_options: diag(6169, 3 /* Message */, "Show_all_compiler_options_6169", "Show all compiler options."),
  Deprecated_Use_outFile_instead_Concatenate_and_emit_output_to_single_file: diag(6170, 3 /* Message */, "Deprecated_Use_outFile_instead_Concatenate_and_emit_output_to_single_file_6170", "[Deprecated] Use '--outFile' instead. Concatenate and emit output to single file"),
  Command_line_Options: diag(6171, 3 /* Message */, "Command_line_Options_6171", "Command-line Options"),
  Provide_full_support_for_iterables_in_for_of_spread_and_destructuring_when_targeting_ES5: diag(6179, 3 /* Message */, "Provide_full_support_for_iterables_in_for_of_spread_and_destructuring_when_targeting_ES5_6179", "Provide full support for iterables in 'for-of', spread, and destructuring when targeting 'ES5'."),
  Enable_all_strict_type_checking_options: diag(6180, 3 /* Message */, "Enable_all_strict_type_checking_options_6180", "Enable all strict type-checking options."),
  Scoped_package_detected_looking_in_0: diag(6182, 3 /* Message */, "Scoped_package_detected_looking_in_0_6182", "Scoped package detected, looking in '{0}'"),
  Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2: diag(6183, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_6183", "Reusing resolution of module '{0}' from '{1}' of old program, it was successfully resolved to '{2}'."),
  Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3: diag(6184, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package__6184", "Reusing resolution of module '{0}' from '{1}' of old program, it was successfully resolved to '{2}' with Package ID '{3}'."),
  Enable_strict_checking_of_function_types: diag(6186, 3 /* Message */, "Enable_strict_checking_of_function_types_6186", "Enable strict checking of function types."),
  Enable_strict_checking_of_property_initialization_in_classes: diag(6187, 3 /* Message */, "Enable_strict_checking_of_property_initialization_in_classes_6187", "Enable strict checking of property initialization in classes."),
  Numeric_separators_are_not_allowed_here: diag(6188, 1 /* Error */, "Numeric_separators_are_not_allowed_here_6188", "Numeric separators are not allowed here."),
  Multiple_consecutive_numeric_separators_are_not_permitted: diag(6189, 1 /* Error */, "Multiple_consecutive_numeric_separators_are_not_permitted_6189", "Multiple consecutive numeric separators are not permitted."),
  Whether_to_keep_outdated_console_output_in_watch_mode_instead_of_clearing_the_screen: diag(6191, 3 /* Message */, "Whether_to_keep_outdated_console_output_in_watch_mode_instead_of_clearing_the_screen_6191", "Whether to keep outdated console output in watch mode instead of clearing the screen."),
  All_imports_in_import_declaration_are_unused: diag(
    6192,
    1 /* Error */,
    "All_imports_in_import_declaration_are_unused_6192",
    "All imports in import declaration are unused.",
    /*reportsUnnecessary*/
    true
  ),
  Found_1_error_Watching_for_file_changes: diag(6193, 3 /* Message */, "Found_1_error_Watching_for_file_changes_6193", "Found 1 error. Watching for file changes."),
  Found_0_errors_Watching_for_file_changes: diag(6194, 3 /* Message */, "Found_0_errors_Watching_for_file_changes_6194", "Found {0} errors. Watching for file changes."),
  Resolve_keyof_to_string_valued_property_names_only_no_numbers_or_symbols: diag(6195, 3 /* Message */, "Resolve_keyof_to_string_valued_property_names_only_no_numbers_or_symbols_6195", "Resolve 'keyof' to string valued property names only (no numbers or symbols)."),
  _0_is_declared_but_never_used: diag(
    6196,
    1 /* Error */,
    "_0_is_declared_but_never_used_6196",
    "'{0}' is declared but never used.",
    /*reportsUnnecessary*/
    true
  ),
  Include_modules_imported_with_json_extension: diag(6197, 3 /* Message */, "Include_modules_imported_with_json_extension_6197", "Include modules imported with '.json' extension"),
  All_destructured_elements_are_unused: diag(
    6198,
    1 /* Error */,
    "All_destructured_elements_are_unused_6198",
    "All destructured elements are unused.",
    /*reportsUnnecessary*/
    true
  ),
  All_variables_are_unused: diag(
    6199,
    1 /* Error */,
    "All_variables_are_unused_6199",
    "All variables are unused.",
    /*reportsUnnecessary*/
    true
  ),
  Definitions_of_the_following_identifiers_conflict_with_those_in_another_file_Colon_0: diag(6200, 1 /* Error */, "Definitions_of_the_following_identifiers_conflict_with_those_in_another_file_Colon_0_6200", "Definitions of the following identifiers conflict with those in another file: {0}"),
  Conflicts_are_in_this_file: diag(6201, 3 /* Message */, "Conflicts_are_in_this_file_6201", "Conflicts are in this file."),
  Project_references_may_not_form_a_circular_graph_Cycle_detected_Colon_0: diag(6202, 1 /* Error */, "Project_references_may_not_form_a_circular_graph_Cycle_detected_Colon_0_6202", "Project references may not form a circular graph. Cycle detected: {0}"),
  _0_was_also_declared_here: diag(6203, 3 /* Message */, "_0_was_also_declared_here_6203", "'{0}' was also declared here."),
  and_here: diag(6204, 3 /* Message */, "and_here_6204", "and here."),
  All_type_parameters_are_unused: diag(6205, 1 /* Error */, "All_type_parameters_are_unused_6205", "All type parameters are unused."),
  package_json_has_a_typesVersions_field_with_version_specific_path_mappings: diag(6206, 3 /* Message */, "package_json_has_a_typesVersions_field_with_version_specific_path_mappings_6206", "'package.json' has a 'typesVersions' field with version-specific path mappings."),
  package_json_does_not_have_a_typesVersions_entry_that_matches_version_0: diag(6207, 3 /* Message */, "package_json_does_not_have_a_typesVersions_entry_that_matches_version_0_6207", "'package.json' does not have a 'typesVersions' entry that matches version '{0}'."),
  package_json_has_a_typesVersions_entry_0_that_matches_compiler_version_1_looking_for_a_pattern_to_match_module_name_2: diag(6208, 3 /* Message */, "package_json_has_a_typesVersions_entry_0_that_matches_compiler_version_1_looking_for_a_pattern_to_ma_6208", "'package.json' has a 'typesVersions' entry '{0}' that matches compiler version '{1}', looking for a pattern to match module name '{2}'."),
  package_json_has_a_typesVersions_entry_0_that_is_not_a_valid_semver_range: diag(6209, 3 /* Message */, "package_json_has_a_typesVersions_entry_0_that_is_not_a_valid_semver_range_6209", "'package.json' has a 'typesVersions' entry '{0}' that is not a valid semver range."),
  An_argument_for_0_was_not_provided: diag(6210, 3 /* Message */, "An_argument_for_0_was_not_provided_6210", "An argument for '{0}' was not provided."),
  An_argument_matching_this_binding_pattern_was_not_provided: diag(6211, 3 /* Message */, "An_argument_matching_this_binding_pattern_was_not_provided_6211", "An argument matching this binding pattern was not provided."),
  Did_you_mean_to_call_this_expression: diag(6212, 3 /* Message */, "Did_you_mean_to_call_this_expression_6212", "Did you mean to call this expression?"),
  Did_you_mean_to_use_new_with_this_expression: diag(6213, 3 /* Message */, "Did_you_mean_to_use_new_with_this_expression_6213", "Did you mean to use 'new' with this expression?"),
  Enable_strict_bind_call_and_apply_methods_on_functions: diag(6214, 3 /* Message */, "Enable_strict_bind_call_and_apply_methods_on_functions_6214", "Enable strict 'bind', 'call', and 'apply' methods on functions."),
  Using_compiler_options_of_project_reference_redirect_0: diag(6215, 3 /* Message */, "Using_compiler_options_of_project_reference_redirect_0_6215", "Using compiler options of project reference redirect '{0}'."),
  Found_1_error: diag(6216, 3 /* Message */, "Found_1_error_6216", "Found 1 error."),
  Found_0_errors: diag(6217, 3 /* Message */, "Found_0_errors_6217", "Found {0} errors."),
  Module_name_0_was_successfully_resolved_to_1_with_Package_ID_2: diag(6218, 3 /* Message */, "Module_name_0_was_successfully_resolved_to_1_with_Package_ID_2_6218", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Module name '{0}' was successfully resolved to '{1}' with Package ID '{2}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Type_reference_directive_0_was_successfully_resolved_to_1_with_Package_ID_2_primary_Colon_3: diag(6219, 3 /* Message */, "Type_reference_directive_0_was_successfully_resolved_to_1_with_Package_ID_2_primary_Colon_3_6219", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Type reference directive '{0}' was successfully resolved to '{1}' with Package ID '{2}', primary: {3}. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  package_json_had_a_falsy_0_field: diag(6220, 3 /* Message */, "package_json_had_a_falsy_0_field_6220", "'package.json' had a falsy '{0}' field."),
  Disable_use_of_source_files_instead_of_declaration_files_from_referenced_projects: diag(6221, 3 /* Message */, "Disable_use_of_source_files_instead_of_declaration_files_from_referenced_projects_6221", "Disable use of source files instead of declaration files from referenced projects."),
  Emit_class_fields_with_Define_instead_of_Set: diag(6222, 3 /* Message */, "Emit_class_fields_with_Define_instead_of_Set_6222", "Emit class fields with Define instead of Set."),
  Generates_a_CPU_profile: diag(6223, 3 /* Message */, "Generates_a_CPU_profile_6223", "Generates a CPU profile."),
  Disable_solution_searching_for_this_project: diag(6224, 3 /* Message */, "Disable_solution_searching_for_this_project_6224", "Disable solution searching for this project."),
  Specify_strategy_for_watching_file_Colon_FixedPollingInterval_default_PriorityPollingInterval_DynamicPriorityPolling_FixedChunkSizePolling_UseFsEvents_UseFsEventsOnParentDirectory: diag(6225, 3 /* Message */, "Specify_strategy_for_watching_file_Colon_FixedPollingInterval_default_PriorityPollingInterval_Dynami_6225", "Specify strategy for watching file: 'FixedPollingInterval' (default), 'PriorityPollingInterval', 'DynamicPriorityPolling', 'FixedChunkSizePolling', 'UseFsEvents', 'UseFsEventsOnParentDirectory'."),
  Specify_strategy_for_watching_directory_on_platforms_that_don_t_support_recursive_watching_natively_Colon_UseFsEvents_default_FixedPollingInterval_DynamicPriorityPolling_FixedChunkSizePolling: diag(6226, 3 /* Message */, "Specify_strategy_for_watching_directory_on_platforms_that_don_t_support_recursive_watching_natively__6226", "Specify strategy for watching directory on platforms that don't support recursive watching natively: 'UseFsEvents' (default), 'FixedPollingInterval', 'DynamicPriorityPolling', 'FixedChunkSizePolling'."),
  Specify_strategy_for_creating_a_polling_watch_when_it_fails_to_create_using_file_system_events_Colon_FixedInterval_default_PriorityInterval_DynamicPriority_FixedChunkSize: diag(6227, 3 /* Message */, "Specify_strategy_for_creating_a_polling_watch_when_it_fails_to_create_using_file_system_events_Colon_6227", "Specify strategy for creating a polling watch when it fails to create using file system events: 'FixedInterval' (default), 'PriorityInterval', 'DynamicPriority', 'FixedChunkSize'."),
  Tag_0_expects_at_least_1_arguments_but_the_JSX_factory_2_provides_at_most_3: diag(6229, 1 /* Error */, "Tag_0_expects_at_least_1_arguments_but_the_JSX_factory_2_provides_at_most_3_6229", "Tag '{0}' expects at least '{1}' arguments, but the JSX factory '{2}' provides at most '{3}'."),
  Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_false_or_null_on_command_line: diag(6230, 1 /* Error */, "Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_false_or_null_on_command_line_6230", "Option '{0}' can only be specified in 'tsconfig.json' file or set to 'false' or 'null' on command line."),
  Could_not_resolve_the_path_0_with_the_extensions_Colon_1: diag(6231, 1 /* Error */, "Could_not_resolve_the_path_0_with_the_extensions_Colon_1_6231", "Could not resolve the path '{0}' with the extensions: {1}."),
  Declaration_augments_declaration_in_another_file_This_cannot_be_serialized: diag(6232, 1 /* Error */, "Declaration_augments_declaration_in_another_file_This_cannot_be_serialized_6232", "Declaration augments declaration in another file. This cannot be serialized."),
  This_is_the_declaration_being_augmented_Consider_moving_the_augmenting_declaration_into_the_same_file: diag(6233, 1 /* Error */, "This_is_the_declaration_being_augmented_Consider_moving_the_augmenting_declaration_into_the_same_fil_6233", "This is the declaration being augmented. Consider moving the augmenting declaration into the same file."),
  This_expression_is_not_callable_because_it_is_a_get_accessor_Did_you_mean_to_use_it_without: diag(6234, 1 /* Error */, "This_expression_is_not_callable_because_it_is_a_get_accessor_Did_you_mean_to_use_it_without_6234", "This expression is not callable because it is a 'get' accessor. Did you mean to use it without '()'?"),
  Disable_loading_referenced_projects: diag(6235, 3 /* Message */, "Disable_loading_referenced_projects_6235", "Disable loading referenced projects."),
  Arguments_for_the_rest_parameter_0_were_not_provided: diag(6236, 1 /* Error */, "Arguments_for_the_rest_parameter_0_were_not_provided_6236", "Arguments for the rest parameter '{0}' were not provided."),
  Generates_an_event_trace_and_a_list_of_types: diag(6237, 3 /* Message */, "Generates_an_event_trace_and_a_list_of_types_6237", "Generates an event trace and a list of types."),
  Specify_the_module_specifier_to_be_used_to_import_the_jsx_and_jsxs_factory_functions_from_eg_react: diag(6238, 1 /* Error */, "Specify_the_module_specifier_to_be_used_to_import_the_jsx_and_jsxs_factory_functions_from_eg_react_6238", "Specify the module specifier to be used to import the 'jsx' and 'jsxs' factory functions from. eg, react"),
  File_0_exists_according_to_earlier_cached_lookups: diag(6239, 3 /* Message */, "File_0_exists_according_to_earlier_cached_lookups_6239", "File '{0}' exists according to earlier cached lookups."),
  File_0_does_not_exist_according_to_earlier_cached_lookups: diag(6240, 3 /* Message */, "File_0_does_not_exist_according_to_earlier_cached_lookups_6240", "File '{0}' does not exist according to earlier cached lookups."),
  Resolution_for_type_reference_directive_0_was_found_in_cache_from_location_1: diag(6241, 3 /* Message */, "Resolution_for_type_reference_directive_0_was_found_in_cache_from_location_1_6241", "Resolution for type reference directive '{0}' was found in cache from location '{1}'."),
  Resolving_type_reference_directive_0_containing_file_1: diag(6242, 3 /* Message */, "Resolving_type_reference_directive_0_containing_file_1_6242", "***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED*** Resolving type reference directive '{0}', containing file '{1}'. ***REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED******REMOVED***"),
  Interpret_optional_property_types_as_written_rather_than_adding_undefined: diag(6243, 3 /* Message */, "Interpret_optional_property_types_as_written_rather_than_adding_undefined_6243", "Interpret optional property types as written, rather than adding 'undefined'."),
  Modules: diag(6244, 3 /* Message */, "Modules_6244", "Modules"),
  File_Management: diag(6245, 3 /* Message */, "File_Management_6245", "File Management"),
  Emit: diag(6246, 3 /* Message */, "Emit_6246", "Emit"),
  JavaScript_Support: diag(6247, 3 /* Message */, "JavaScript_Support_6247", "JavaScript Support"),
  Type_Checking: diag(6248, 3 /* Message */, "Type_Checking_6248", "Type Checking"),
  Editor_Support: diag(6249, 3 /* Message */, "Editor_Support_6249", "Editor Support"),
  Watch_and_Build_Modes: diag(6250, 3 /* Message */, "Watch_and_Build_Modes_6250", "Watch and Build Modes"),
  Compiler_Diagnostics: diag(6251, 3 /* Message */, "Compiler_Diagnostics_6251", "Compiler Diagnostics"),
  Interop_Constraints: diag(6252, 3 /* Message */, "Interop_Constraints_6252", "Interop Constraints"),
  Backwards_Compatibility: diag(6253, 3 /* Message */, "Backwards_Compatibility_6253", "Backwards Compatibility"),
  Language_and_Environment: diag(6254, 3 /* Message */, "Language_and_Environment_6254", "Language and Environment"),
  Projects: diag(6255, 3 /* Message */, "Projects_6255", "Projects"),
  Output_Formatting: diag(6256, 3 /* Message */, "Output_Formatting_6256", "Output Formatting"),
  Completeness: diag(6257, 3 /* Message */, "Completeness_6257", "Completeness"),
  _0_should_be_set_inside_the_compilerOptions_object_of_the_config_json_file: diag(6258, 1 /* Error */, "_0_should_be_set_inside_the_compilerOptions_object_of_the_config_json_file_6258", "'{0}' should be set inside the 'compilerOptions' object of the config json file"),
  Found_1_error_in_0: diag(6259, 3 /* Message */, "Found_1_error_in_0_6259", "Found 1 error in {0}"),
  Found_0_errors_in_the_same_file_starting_at_Colon_1: diag(6260, 3 /* Message */, "Found_0_errors_in_the_same_file_starting_at_Colon_1_6260", "Found {0} errors in the same file, starting at: {1}"),
  Found_0_errors_in_1_files: diag(6261, 3 /* Message */, "Found_0_errors_in_1_files_6261", "Found {0} errors in {1} files."),
  File_name_0_has_a_1_extension_looking_up_2_instead: diag(6262, 3 /* Message */, "File_name_0_has_a_1_extension_looking_up_2_instead_6262", "File name '{0}' has a '{1}' extension - looking up '{2}' instead."),
  Module_0_was_resolved_to_1_but_allowArbitraryExtensions_is_not_set: diag(6263, 1 /* Error */, "Module_0_was_resolved_to_1_but_allowArbitraryExtensions_is_not_set_6263", "Module '{0}' was resolved to '{1}', but '--allowArbitraryExtensions' is not set."),
  Enable_importing_files_with_any_extension_provided_a_declaration_file_is_present: diag(6264, 3 /* Message */, "Enable_importing_files_with_any_extension_provided_a_declaration_file_is_present_6264", "Enable importing files with any extension, provided a declaration file is present."),
  Resolving_type_reference_directive_for_program_that_specifies_custom_typeRoots_skipping_lookup_in_node_modules_folder: diag(6265, 3 /* Message */, "Resolving_type_reference_directive_for_program_that_specifies_custom_typeRoots_skipping_lookup_in_no_6265", "Resolving type reference directive for program that specifies custom typeRoots, skipping lookup in 'node_modules' folder."),
  Option_0_can_only_be_specified_on_command_line: diag(6266, 1 /* Error */, "Option_0_can_only_be_specified_on_command_line_6266", "Option '{0}' can only be specified on command line."),
  Directory_0_has_no_containing_package_json_scope_Imports_will_not_resolve: diag(6270, 3 /* Message */, "Directory_0_has_no_containing_package_json_scope_Imports_will_not_resolve_6270", "Directory '{0}' has no containing package.json scope. Imports will not resolve."),
  Import_specifier_0_does_not_exist_in_package_json_scope_at_path_1: diag(6271, 3 /* Message */, "Import_specifier_0_does_not_exist_in_package_json_scope_at_path_1_6271", "Import specifier '{0}' does not exist in package.json scope at path '{1}'."),
  Invalid_import_specifier_0_has_no_possible_resolutions: diag(6272, 3 /* Message */, "Invalid_import_specifier_0_has_no_possible_resolutions_6272", "Invalid import specifier '{0}' has no possible resolutions."),
  package_json_scope_0_has_no_imports_defined: diag(6273, 3 /* Message */, "package_json_scope_0_has_no_imports_defined_6273", "package.json scope '{0}' has no imports defined."),
  package_json_scope_0_explicitly_maps_specifier_1_to_null: diag(6274, 3 /* Message */, "package_json_scope_0_explicitly_maps_specifier_1_to_null_6274", "package.json scope '{0}' explicitly maps specifier '{1}' to null."),
  package_json_scope_0_has_invalid_type_for_target_of_specifier_1: diag(6275, 3 /* Message */, "package_json_scope_0_has_invalid_type_for_target_of_specifier_1_6275", "package.json scope '{0}' has invalid type for target of specifier '{1}'"),
  Export_specifier_0_does_not_exist_in_package_json_scope_at_path_1: diag(6276, 3 /* Message */, "Export_specifier_0_does_not_exist_in_package_json_scope_at_path_1_6276", "Export specifier '{0}' does not exist in package.json scope at path '{1}'."),
  Resolution_of_non_relative_name_failed_trying_with_modern_Node_resolution_features_disabled_to_see_if_npm_library_needs_configuration_update: diag(6277, 3 /* Message */, "Resolution_of_non_relative_name_failed_trying_with_modern_Node_resolution_features_disabled_to_see_i_6277", "Resolution of non-relative name failed; trying with modern Node resolution features disabled to see if npm library needs configuration update."),
  There_are_types_at_0_but_this_result_could_not_be_resolved_when_respecting_package_json_exports_The_1_library_may_need_to_update_its_package_json_or_typings: diag(6278, 3 /* Message */, "There_are_types_at_0_but_this_result_could_not_be_resolved_when_respecting_package_json_exports_The__6278", `There are types at '{0}', but this result could not be resolved when respecting package.json "exports". The '{1}' library may need to update its package.json or typings.`),
  Resolution_of_non_relative_name_failed_trying_with_moduleResolution_bundler_to_see_if_project_may_need_configuration_update: diag(6279, 3 /* Message */, "Resolution_of_non_relative_name_failed_trying_with_moduleResolution_bundler_to_see_if_project_may_ne_6279", "Resolution of non-relative name failed; trying with '--moduleResolution bundler' to see if project may need configuration update."),
  There_are_types_at_0_but_this_result_could_not_be_resolved_under_your_current_moduleResolution_setting_Consider_updating_to_node16_nodenext_or_bundler: diag(6280, 3 /* Message */, "There_are_types_at_0_but_this_result_could_not_be_resolved_under_your_current_moduleResolution_setti_6280", "There are types at '{0}', but this result could not be resolved under your current 'moduleResolution' setting. Consider updating to 'node16', 'nodenext', or 'bundler'."),
  package_json_has_a_peerDependencies_field: diag(6281, 3 /* Message */, "package_json_has_a_peerDependencies_field_6281", "'package.json' has a 'peerDependencies' field."),
  Found_peerDependency_0_with_1_version: diag(6282, 3 /* Message */, "Found_peerDependency_0_with_1_version_6282", "Found peerDependency '{0}' with '{1}' version."),
  Failed_to_find_peerDependency_0: diag(6283, 3 /* Message */, "Failed_to_find_peerDependency_0_6283", "Failed to find peerDependency '{0}'."),
  File_Layout: diag(6284, 3 /* Message */, "File_Layout_6284", "File Layout"),
  Environment_Settings: diag(6285, 3 /* Message */, "Environment_Settings_6285", "Environment Settings"),
  See_also_https_Colon_Slash_Slashaka_ms_Slashtsconfig_Slashmodule: diag(6286, 3 /* Message */, "See_also_https_Colon_Slash_Slashaka_ms_Slashtsconfig_Slashmodule_6286", "See also https://aka.ms/tsconfig/module"),
  For_nodejs_Colon: diag(6287, 3 /* Message */, "For_nodejs_Colon_6287", "For nodejs:"),
  and_npm_install_D_types_Slashnode: diag(6290, 3 /* Message */, "and_npm_install_D_types_Slashnode_6290", "and npm install -D @types/node"),
  Other_Outputs: diag(6291, 3 /* Message */, "Other_Outputs_6291", "Other Outputs"),
  Stricter_Typechecking_Options: diag(6292, 3 /* Message */, "Stricter_Typechecking_Options_6292", "Stricter Typechecking Options"),
  Style_Options: diag(6293, 3 /* Message */, "Style_Options_6293", "Style Options"),
  Recommended_Options: diag(6294, 3 /* Message */, "Recommended_Options_6294", "Recommended Options"),
  Enable_project_compilation: diag(6302, 3 /* Message */, "Enable_project_compilation_6302", "Enable project compilation"),
  Composite_projects_may_not_disable_declaration_emit: diag(6304, 1 /* Error */, "Composite_projects_may_not_disable_declaration_emit_6304", "Composite projects may not disable declaration emit."),
  Output_file_0_has_not_been_built_from_source_file_1: diag(6305, 1 /* Error */, "Output_file_0_has_not_been_built_from_source_file_1_6305", "Output file '{0}' has not been built from source file '{1}'."),
  Referenced_project_0_must_have_setting_composite_Colon_true: diag(6306, 1 /* Error */, "Referenced_project_0_must_have_setting_composite_Colon_true_6306", `Referenced project '{0}' must have setting "composite": true.`),
  File_0_is_not_listed_within_the_file_list_of_project_1_Projects_must_list_all_files_or_use_an_include_pattern: diag(6307, 1 /* Error */, "File_0_is_not_listed_within_the_file_list_of_project_1_Projects_must_list_all_files_or_use_an_includ_6307", "File '{0}' is not listed within the file list of project '{1}'. Projects must list all files or use an 'include' pattern."),
  Referenced_project_0_may_not_disable_emit: diag(6310, 1 /* Error */, "Referenced_project_0_may_not_disable_emit_6310", "Referenced project '{0}' may not disable emit."),
  Project_0_is_out_of_date_because_output_1_is_older_than_input_2: diag(6350, 3 /* Message */, "Project_0_is_out_of_date_because_output_1_is_older_than_input_2_6350", "Project '{0}' is out of date because output '{1}' is older than input '{2}'"),
  Project_0_is_up_to_date_because_newest_input_1_is_older_than_output_2: diag(6351, 3 /* Message */, "Project_0_is_up_to_date_because_newest_input_1_is_older_than_output_2_6351", "Project '{0}' is up to date because newest input '{1}' is older than output '{2}'"),
  Project_0_is_out_of_date_because_output_file_1_does_not_exist: diag(6352, 3 /* Message */, "Project_0_is_out_of_date_because_output_file_1_does_not_exist_6352", "Project '{0}' is out of date because output file '{1}' does not exist"),
  Project_0_is_out_of_date_because_its_dependency_1_is_out_of_date: diag(6353, 3 /* Message */, "Project_0_is_out_of_date_because_its_dependency_1_is_out_of_date_6353", "Project '{0}' is out of date because its dependency '{1}' is out of date"),
  Project_0_is_up_to_date_with_d_ts_files_from_its_dependencies: diag(6354, 3 /* Message */, "Project_0_is_up_to_date_with_d_ts_files_from_its_dependencies_6354", "Project '{0}' is up to date with .d.ts files from its dependencies"),
  Projects_in_this_build_Colon_0: diag(6355, 3 /* Message */, "Projects_in_this_build_Colon_0_6355", "Projects in this build: {0}"),
  A_non_dry_build_would_delete_the_following_files_Colon_0: diag(6356, 3 /* Message */, "A_non_dry_build_would_delete_the_following_files_Colon_0_6356", "A non-dry build would delete the following files: {0}"),
  A_non_dry_build_would_build_project_0: diag(6357, 3 /* Message */, "A_non_dry_build_would_build_project_0_6357", "A non-dry build would build project '{0}'"),
  Building_project_0: diag(6358, 3 /* Message */, "Building_project_0_6358", "Building project '{0}'..."),
  Updating_output_timestamps_of_project_0: diag(6359, 3 /* Message */, "Updating_output_timestamps_of_project_0_6359", "Updating output timestamps of project '{0}'..."),
  Project_0_is_up_to_date: diag(6361, 3 /* Message */, "Project_0_is_up_to_date_6361", "Project '{0}' is up to date"),
  Skipping_build_of_project_0_because_its_dependency_1_has_errors: diag(6362, 3 /* Message */, "Skipping_build_of_project_0_because_its_dependency_1_has_errors_6362", "Skipping build of project '{0}' because its dependency '{1}' has errors"),
  Project_0_can_t_be_built_because_its_dependency_1_has_errors: diag(6363, 3 /* Message */, "Project_0_can_t_be_built_because_its_dependency_1_has_errors_6363", "Project '{0}' can't be built because its dependency '{1}' has errors"),
  Build_one_or_more_projects_and_their_dependencies_if_out_of_date: diag(6364, 3 /* Message */, "Build_one_or_more_projects_and_their_dependencies_if_out_of_date_6364", "Build one or more projects and their dependencies, if out of date"),
  Delete_the_outputs_of_all_projects: diag(6365, 3 /* Message */, "Delete_the_outputs_of_all_projects_6365", "Delete the outputs of all projects."),
  Show_what_would_be_built_or_deleted_if_specified_with_clean: diag(6367, 3 /* Message */, "Show_what_would_be_built_or_deleted_if_specified_with_clean_6367", "Show what would be built (or deleted, if specified with '--clean')"),
  Option_build_must_be_the_first_command_line_argument: diag(6369, 1 /* Error */, "Option_build_must_be_the_first_command_line_argument_6369", "Option '--build' must be the first command line argument."),
  Options_0_and_1_cannot_be_combined: diag(6370, 1 /* Error */, "Options_0_and_1_cannot_be_combined_6370", "Options '{0}' and '{1}' cannot be combined."),
  Updating_unchanged_output_timestamps_of_project_0: diag(6371, 3 /* Message */, "Updating_unchanged_output_timestamps_of_project_0_6371", "Updating unchanged output timestamps of project '{0}'..."),
  A_non_dry_build_would_update_timestamps_for_output_of_project_0: diag(6374, 3 /* Message */, "A_non_dry_build_would_update_timestamps_for_output_of_project_0_6374", "A non-dry build would update timestamps for output of project '{0}'"),
  Cannot_write_file_0_because_it_will_overwrite_tsbuildinfo_file_generated_by_referenced_project_1: diag(6377, 1 /* Error */, "Cannot_write_file_0_because_it_will_overwrite_tsbuildinfo_file_generated_by_referenced_project_1_6377", "Cannot write file '{0}' because it will overwrite '.tsbuildinfo' file generated by referenced project '{1}'"),
  Composite_projects_may_not_disable_incremental_compilation: diag(6379, 1 /* Error */, "Composite_projects_may_not_disable_incremental_compilation_6379", "Composite projects may not disable incremental compilation."),
  Specify_file_to_store_incremental_compilation_information: diag(6380, 3 /* Message */, "Specify_file_to_store_incremental_compilation_information_6380", "Specify file to store incremental compilation information"),
  Project_0_is_out_of_date_because_output_for_it_was_generated_with_version_1_that_differs_with_current_version_2: diag(6381, 3 /* Message */, "Project_0_is_out_of_date_because_output_for_it_was_generated_with_version_1_that_differs_with_curren_6381", "Project '{0}' is out of date because output for it was generated with version '{1}' that differs with current version '{2}'"),
  Skipping_build_of_project_0_because_its_dependency_1_was_not_built: diag(6382, 3 /* Message */, "Skipping_build_of_project_0_because_its_dependency_1_was_not_built_6382", "Skipping build of project '{0}' because its dependency '{1}' was not built"),
  Project_0_can_t_be_built_because_its_dependency_1_was_not_built: diag(6383, 3 /* Message */, "Project_0_can_t_be_built_because_its_dependency_1_was_not_built_6383", "Project '{0}' can't be built because its dependency '{1}' was not built"),
  Have_recompiles_in_incremental_and_watch_assume_that_changes_within_a_file_will_only_affect_files_directly_depending_on_it: diag(6384, 3 /* Message */, "Have_recompiles_in_incremental_and_watch_assume_that_changes_within_a_file_will_only_affect_files_di_6384", "Have recompiles in '--incremental' and '--watch' assume that changes within a file will only affect files directly depending on it."),
  _0_is_deprecated: diag(
    6385,
    2 /* Suggestion */,
    "_0_is_deprecated_6385",
    "'{0}' is deprecated.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    void 0,
    /*reportsDeprecated*/
    true
  ),
  Performance_timings_for_diagnostics_or_extendedDiagnostics_are_not_available_in_this_session_A_native_implementation_of_the_Web_Performance_API_could_not_be_found: diag(6386, 3 /* Message */, "Performance_timings_for_diagnostics_or_extendedDiagnostics_are_not_available_in_this_session_A_nativ_6386", "Performance timings for '--diagnostics' or '--extendedDiagnostics' are not available in this session. A native implementation of the Web Performance API could not be found."),
  The_signature_0_of_1_is_deprecated: diag(
    6387,
    2 /* Suggestion */,
    "The_signature_0_of_1_is_deprecated_6387",
    "The signature '{0}' of '{1}' is deprecated.",
    /*reportsUnnecessary*/
    void 0,
    /*elidedInCompatabilityPyramid*/
    void 0,
    /*reportsDeprecated*/
    true
  ),
  Project_0_is_being_forcibly_rebuilt: diag(6388, 3 /* Message */, "Project_0_is_being_forcibly_rebuilt_6388", "Project '{0}' is being forcibly rebuilt"),
  Reusing_resolution_of_module_0_from_1_of_old_program_it_was_not_resolved: diag(6389, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_of_old_program_it_was_not_resolved_6389", "Reusing resolution of module '{0}' from '{1}' of old program, it was not resolved."),
  Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2: diag(6390, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved__6390", "Reusing resolution of type reference directive '{0}' from '{1}' of old program, it was successfully resolved to '{2}'."),
  Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3: diag(6391, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved__6391", "Reusing resolution of type reference directive '{0}' from '{1}' of old program, it was successfully resolved to '{2}' with Package ID '{3}'."),
  Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_not_resolved: diag(6392, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_not_resolved_6392", "Reusing resolution of type reference directive '{0}' from '{1}' of old program, it was not resolved."),
  Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_3: diag(6393, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_6393", "Reusing resolution of module '{0}' from '{1}' found in cache from location '{2}', it was successfully resolved to '{3}'."),
  Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_3_with_Package_ID_4: diag(6394, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_6394", "Reusing resolution of module '{0}' from '{1}' found in cache from location '{2}', it was successfully resolved to '{3}' with Package ID '{4}'."),
  Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_not_resolved: diag(6395, 3 /* Message */, "Reusing_resolution_of_module_0_from_1_found_in_cache_from_location_2_it_was_not_resolved_6395", "Reusing resolution of module '{0}' from '{1}' found in cache from location '{2}', it was not resolved."),
  Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_3: diag(6396, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_succes_6396", "Reusing resolution of type reference directive '{0}' from '{1}' found in cache from location '{2}', it was successfully resolved to '{3}'."),
  Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_successfully_resolved_to_3_with_Package_ID_4: diag(6397, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_succes_6397", "Reusing resolution of type reference directive '{0}' from '{1}' found in cache from location '{2}', it was successfully resolved to '{3}' with Package ID '{4}'."),
  Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_not_resolved: diag(6398, 3 /* Message */, "Reusing_resolution_of_type_reference_directive_0_from_1_found_in_cache_from_location_2_it_was_not_re_6398", "Reusing resolution of type reference directive '{0}' from '{1}' found in cache from location '{2}', it was not resolved."),
  Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_some_of_the_changes_were_not_emitted: diag(6399, 3 /* Message */, "Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_some_of_the_changes_were_not_emitte_6399", "Project '{0}' is out of date because buildinfo file '{1}' indicates that some of the changes were not emitted"),
  Project_0_is_up_to_date_but_needs_to_update_timestamps_of_output_files_that_are_older_than_input_files: diag(6400, 3 /* Message */, "Project_0_is_up_to_date_but_needs_to_update_timestamps_of_output_files_that_are_older_than_input_fil_6400", "Project '{0}' is up to date but needs to update timestamps of output files that are older than input files"),
  Project_0_is_out_of_date_because_there_was_error_reading_file_1: diag(6401, 3 /* Message */, "Project_0_is_out_of_date_because_there_was_error_reading_file_1_6401", "Project '{0}' is out of date because there was error reading file '{1}'"),
  Resolving_in_0_mode_with_conditions_1: diag(6402, 3 /* Message */, "Resolving_in_0_mode_with_conditions_1_6402", "Resolving in {0} mode with conditions {1}."),
  Matched_0_condition_1: diag(6403, 3 /* Message */, "Matched_0_condition_1_6403", "Matched '{0}' condition '{1}'."),
  Using_0_subpath_1_with_target_2: diag(6404, 3 /* Message */, "Using_0_subpath_1_with_target_2_6404", "Using '{0}' subpath '{1}' with target '{2}'."),
  Saw_non_matching_condition_0: diag(6405, 3 /* Message */, "Saw_non_matching_condition_0_6405", "Saw non-matching condition '{0}'."),
  Project_0_is_out_of_date_because_buildinfo_file_1_indicates_there_is_change_in_compilerOptions: diag(6406, 3 /* Message */, "Project_0_is_out_of_date_because_buildinfo_file_1_indicates_there_is_change_in_compilerOptions_6406", "Project '{0}' is out of date because buildinfo file '{1}' indicates there is change in compilerOptions"),
  Allow_imports_to_include_TypeScript_file_extensions_Requires_moduleResolution_bundler_and_either_noEmit_or_emitDeclarationOnly_to_be_set: diag(6407, 3 /* Message */, "Allow_imports_to_include_TypeScript_file_extensions_Requires_moduleResolution_bundler_and_either_noE_6407", "Allow imports to include TypeScript file extensions. Requires '--moduleResolution bundler' and either '--noEmit' or '--emitDeclarationOnly' to be set."),
  Use_the_package_json_exports_field_when_resolving_package_imports: diag(6408, 3 /* Message */, "Use_the_package_json_exports_field_when_resolving_package_imports_6408", "Use the package.json 'exports' field when resolving package imports."),
  Use_the_package_json_imports_field_when_resolving_imports: diag(6409, 3 /* Message */, "Use_the_package_json_imports_field_when_resolving_imports_6409", "Use the package.json 'imports' field when resolving imports."),
  Conditions_to_set_in_addition_to_the_resolver_specific_defaults_when_resolving_imports: diag(6410, 3 /* Message */, "Conditions_to_set_in_addition_to_the_resolver_specific_defaults_when_resolving_imports_6410", "Conditions to set in addition to the resolver-specific defaults when resolving imports."),
  true_when_moduleResolution_is_node16_nodenext_or_bundler_otherwise_false: diag(6411, 3 /* Message */, "true_when_moduleResolution_is_node16_nodenext_or_bundler_otherwise_false_6411", "`true` when 'moduleResolution' is 'node16', 'nodenext', or 'bundler'; otherwise `false`."),
  Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_file_2_was_root_file_of_compilation_but_not_any_more: diag(6412, 3 /* Message */, "Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_file_2_was_root_file_of_compilation_6412", "Project '{0}' is out of date because buildinfo file '{1}' indicates that file '{2}' was root file of compilation but not any more."),
  Entering_conditional_exports: diag(6413, 3 /* Message */, "Entering_conditional_exports_6413", "Entering conditional exports."),
  Resolved_under_condition_0: diag(6414, 3 /* Message */, "Resolved_under_condition_0_6414", "Resolved under condition '{0}'."),
  Failed_to_resolve_under_condition_0: diag(6415, 3 /* Message */, "Failed_to_resolve_under_condition_0_6415", "Failed to resolve under condition '{0}'."),
  Exiting_conditional_exports: diag(6416, 3 /* Message */, "Exiting_conditional_exports_6416", "Exiting conditional exports."),
  Searching_all_ancestor_node_modules_directories_for_preferred_extensions_Colon_0: diag(6417, 3 /* Message */, "Searching_all_ancestor_node_modules_directories_for_preferred_extensions_Colon_0_6417", "Searching all ancestor node_modules directories for preferred extensions: {0}."),
  Searching_all_ancestor_node_modules_directories_for_fallback_extensions_Colon_0: diag(6418, 3 /* Message */, "Searching_all_ancestor_node_modules_directories_for_fallback_extensions_Colon_0_6418", "Searching all ancestor node_modules directories for fallback extensions: {0}."),
  Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_program_needs_to_report_errors: diag(6419, 3 /* Message */, "Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_program_needs_to_report_errors_6419", "Project '{0}' is out of date because buildinfo file '{1}' indicates that program needs to report errors."),
  Project_0_is_out_of_date_because_1: diag(6420, 3 /* Message */, "Project_0_is_out_of_date_because_1_6420", "Project '{0}' is out of date because {1}."),
  Rewrite_ts_tsx_mts_and_cts_file_extensions_in_relative_import_paths_to_their_JavaScript_equivalent_in_output_files: diag(6421, 3 /* Message */, "Rewrite_ts_tsx_mts_and_cts_file_extensions_in_relative_import_paths_to_their_JavaScript_equivalent_i_6421", "Rewrite '.ts', '.tsx', '.mts', and '.cts' file extensions in relative import paths to their JavaScript equivalent in output files."),
  The_expected_type_comes_from_property_0_which_is_declared_here_on_type_1: diag(6500, 3 /* Message */, "The_expected_type_comes_from_property_0_which_is_declared_here_on_type_1_6500", "The expected type comes from property '{0}' which is declared here on type '{1}'"),
  The_expected_type_comes_from_this_index_signature: diag(6501, 3 /* Message */, "The_expected_type_comes_from_this_index_signature_6501", "The expected type comes from this index signature."),
  The_expected_type_comes_from_the_return_type_of_this_signature: diag(6502, 3 /* Message */, "The_expected_type_comes_from_the_return_type_of_this_signature_6502", "The expected type comes from the return type of this signature."),
  Print_names_of_files_that_are_part_of_the_compilation_and_then_stop_processing: diag(6503, 3 /* Message */, "Print_names_of_files_that_are_part_of_the_compilation_and_then_stop_processing_6503", "Print names of files that are part of the compilation and then stop processing."),
  File_0_is_a_JavaScript_file_Did_you_mean_to_enable_the_allowJs_option: diag(6504, 1 /* Error */, "File_0_is_a_JavaScript_file_Did_you_mean_to_enable_the_allowJs_option_6504", "File '{0}' is a JavaScript file. Did you mean to enable the 'allowJs' option?"),
  Print_names_of_files_and_the_reason_they_are_part_of_the_compilation: diag(6505, 3 /* Message */, "Print_names_of_files_and_the_reason_they_are_part_of_the_compilation_6505", "Print names of files and the reason they are part of the compilation."),
  Consider_adding_a_declare_modifier_to_this_class: diag(6506, 3 /* Message */, "Consider_adding_a_declare_modifier_to_this_class_6506", "Consider adding a 'declare' modifier to this class."),
  Allow_JavaScript_files_to_be_a_part_of_your_program_Use_the_checkJs_option_to_get_errors_from_these_files: diag(6600, 3 /* Message */, "Allow_JavaScript_files_to_be_a_part_of_your_program_Use_the_checkJs_option_to_get_errors_from_these__6600", "Allow JavaScript files to be a part of your program. Use the 'checkJs' option to get errors from these files."),
  Allow_import_x_from_y_when_a_module_doesn_t_have_a_default_export: diag(6601, 3 /* Message */, "Allow_import_x_from_y_when_a_module_doesn_t_have_a_default_export_6601", "Allow 'import x from y' when a module doesn't have a default export."),
  Allow_accessing_UMD_globals_from_modules: diag(6602, 3 /* Message */, "Allow_accessing_UMD_globals_from_modules_6602", "Allow accessing UMD globals from modules."),
  Disable_error_reporting_for_unreachable_code: diag(6603, 3 /* Message */, "Disable_error_reporting_for_unreachable_code_6603", "Disable error reporting for unreachable code."),
  Disable_error_reporting_for_unused_labels: diag(6604, 3 /* Message */, "Disable_error_reporting_for_unused_labels_6604", "Disable error reporting for unused labels."),
  Ensure_use_strict_is_always_emitted: diag(6605, 3 /* Message */, "Ensure_use_strict_is_always_emitted_6605", "Ensure 'use strict' is always emitted."),
  Have_recompiles_in_projects_that_use_incremental_and_watch_mode_assume_that_changes_within_a_file_will_only_affect_files_directly_depending_on_it: diag(6606, 3 /* Message */, "Have_recompiles_in_projects_that_use_incremental_and_watch_mode_assume_that_changes_within_a_file_wi_6606", "Have recompiles in projects that use 'incremental' and 'watch' mode assume that changes within a file will only affect files directly depending on it."),
  Specify_the_base_directory_to_resolve_non_relative_module_names: diag(6607, 3 /* Message */, "Specify_the_base_directory_to_resolve_non_relative_module_names_6607", "Specify the base directory to resolve non-relative module names."),
  No_longer_supported_In_early_versions_manually_set_the_text_encoding_for_reading_files: diag(6608, 3 /* Message */, "No_longer_supported_In_early_versions_manually_set_the_text_encoding_for_reading_files_6608", "No longer supported. In early versions, manually set the text encoding for reading files."),
  Enable_error_reporting_in_type_checked_JavaScript_files: diag(6609, 3 /* Message */, "Enable_error_reporting_in_type_checked_JavaScript_files_6609", "Enable error reporting in type-checked JavaScript files."),
  Enable_constraints_that_allow_a_TypeScript_project_to_be_used_with_project_references: diag(6611, 3 /* Message */, "Enable_constraints_that_allow_a_TypeScript_project_to_be_used_with_project_references_6611", "Enable constraints that allow a TypeScript project to be used with project references."),
  Generate_d_ts_files_from_TypeScript_and_JavaScript_files_in_your_project: diag(6612, 3 /* Message */, "Generate_d_ts_files_from_TypeScript_and_JavaScript_files_in_your_project_6612", "Generate .d.ts files from TypeScript and JavaScript files in your project."),
  Specify_the_output_directory_for_generated_declaration_files: diag(6613, 3 /* Message */, "Specify_the_output_directory_for_generated_declaration_files_6613", "Specify the output directory for generated declaration files."),
  Create_sourcemaps_for_d_ts_files: diag(6614, 3 /* Message */, "Create_sourcemaps_for_d_ts_files_6614", "Create sourcemaps for d.ts files."),
  Output_compiler_performance_information_after_building: diag(6615, 3 /* Message */, "Output_compiler_performance_information_after_building_6615", "Output compiler performance information after building."),
  Disables_inference_for_type_acquisition_by_looking_at_filenames_in_a_project: diag(6616, 3 /* Message */, "Disables_inference_for_type_acquisition_by_looking_at_filenames_in_a_project_6616", "Disables inference for type acquisition by looking at filenames in a project."),
  Reduce_the_number_of_projects_loaded_automatically_by_TypeScript: diag(6617, 3 /* Message */, "Reduce_the_number_of_projects_loaded_automatically_by_TypeScript_6617", "Reduce the number of projects loaded automatically by TypeScript."),
  Remove_the_20mb_cap_on_total_source_code_size_for_JavaScript_files_in_the_TypeScript_language_server: diag(6618, 3 /* Message */, "Remove_the_20mb_cap_on_total_source_code_size_for_JavaScript_files_in_the_TypeScript_language_server_6618", "Remove the 20mb cap on total source code size for JavaScript files in the TypeScript language server."),
  Opt_a_project_out_of_multi_project_reference_checking_when_editing: diag(6619, 3 /* Message */, "Opt_a_project_out_of_multi_project_reference_checking_when_editing_6619", "Opt a project out of multi-project reference checking when editing."),
  Disable_preferring_source_files_instead_of_declaration_files_when_referencing_composite_projects: diag(6620, 3 /* Message */, "Disable_preferring_source_files_instead_of_declaration_files_when_referencing_composite_projects_6620", "Disable preferring source files instead of declaration files when referencing composite projects."),
  Emit_more_compliant_but_verbose_and_less_performant_JavaScript_for_iteration: diag(6621, 3 /* Message */, "Emit_more_compliant_but_verbose_and_less_performant_JavaScript_for_iteration_6621", "Emit more compliant, but verbose and less performant JavaScript for iteration."),
  Emit_a_UTF_8_Byte_Order_Mark_BOM_in_the_beginning_of_output_files: diag(6622, 3 /* Message */, "Emit_a_UTF_8_Byte_Order_Mark_BOM_in_the_beginning_of_output_files_6622", "Emit a UTF-8 Byte Order Mark (BOM) in the beginning of output files."),
  Only_output_d_ts_files_and_not_JavaScript_files: diag(6623, 3 /* Message */, "Only_output_d_ts_files_and_not_JavaScript_files_6623", "Only output d.ts files and not JavaScript files."),
  Emit_design_type_metadata_for_decorated_declarations_in_source_files: diag(6624, 3 /* Message */, "Emit_design_type_metadata_for_decorated_declarations_in_source_files_6624", "Emit design-type metadata for decorated declarations in source files."),
  Disable_the_type_acquisition_for_JavaScript_projects: diag(6625, 3 /* Message */, "Disable_the_type_acquisition_for_JavaScript_projects_6625", "Disable the type acquisition for JavaScript projects"),
  Emit_additional_JavaScript_to_ease_support_for_importing_CommonJS_modules_This_enables_allowSyntheticDefaultImports_for_type_compatibility: diag(6626, 3 /* Message */, "Emit_additional_JavaScript_to_ease_support_for_importing_CommonJS_modules_This_enables_allowSyntheti_6626", "Emit additional JavaScript to ease support for importing CommonJS modules. This enables 'allowSyntheticDefaultImports' for type compatibility."),
  Filters_results_from_the_include_option: diag(6627, 3 /* Message */, "Filters_results_from_the_include_option_6627", "Filters results from the `include` option."),
  Remove_a_list_of_directories_from_the_watch_process: diag(6628, 3 /* Message */, "Remove_a_list_of_directories_from_the_watch_process_6628", "Remove a list of directories from the watch process."),
  Remove_a_list_of_files_from_the_watch_mode_s_processing: diag(6629, 3 /* Message */, "Remove_a_list_of_files_from_the_watch_mode_s_processing_6629", "Remove a list of files from the watch mode's processing."),
  Enable_experimental_support_for_legacy_experimental_decorators: diag(6630, 3 /* Message */, "Enable_experimental_support_for_legacy_experimental_decorators_6630", "Enable experimental support for legacy experimental decorators."),
  Print_files_read_during_the_compilation_including_why_it_was_included: diag(6631, 3 /* Message */, "Print_files_read_during_the_compilation_including_why_it_was_included_6631", "Print files read during the compilation including why it was included."),
  Output_more_detailed_compiler_performance_information_after_building: diag(6632, 3 /* Message */, "Output_more_detailed_compiler_performance_information_after_building_6632", "Output more detailed compiler performance information after building."),
  Specify_one_or_more_path_or_node_module_references_to_base_configuration_files_from_which_settings_are_inherited: diag(6633, 3 /* Message */, "Specify_one_or_more_path_or_node_module_references_to_base_configuration_files_from_which_settings_a_6633", "Specify one or more path or node module references to base configuration files from which settings are inherited."),
  Specify_what_approach_the_watcher_should_use_if_the_system_runs_out_of_native_file_watchers: diag(6634, 3 /* Message */, "Specify_what_approach_the_watcher_should_use_if_the_system_runs_out_of_native_file_watchers_6634", "Specify what approach the watcher should use if the system runs out of native file watchers."),
  Include_a_list_of_files_This_does_not_support_glob_patterns_as_opposed_to_include: diag(6635, 3 /* Message */, "Include_a_list_of_files_This_does_not_support_glob_patterns_as_opposed_to_include_6635", "Include a list of files. This does not support glob patterns, as opposed to `include`."),
  Build_all_projects_including_those_that_appear_to_be_up_to_date: diag(6636, 3 /* Message */, "Build_all_projects_including_those_that_appear_to_be_up_to_date_6636", "Build all projects, including those that appear to be up to date."),
  Ensure_that_casing_is_correct_in_imports: diag(6637, 3 /* Message */, "Ensure_that_casing_is_correct_in_imports_6637", "Ensure that casing is correct in imports."),
  Emit_a_v8_CPU_profile_of_the_compiler_run_for_debugging: diag(6638, 3 /* Message */, "Emit_a_v8_CPU_profile_of_the_compiler_run_for_debugging_6638", "Emit a v8 CPU profile of the compiler run for debugging."),
  Allow_importing_helper_functions_from_tslib_once_per_project_instead_of_including_them_per_file: diag(6639, 3 /* Message */, "Allow_importing_helper_functions_from_tslib_once_per_project_instead_of_including_them_per_file_6639", "Allow importing helper functions from tslib once per project, instead of including them per-file."),
  Skip_building_downstream_projects_on_error_in_upstream_project: diag(6640, 3 /* Message */, "Skip_building_downstream_projects_on_error_in_upstream_project_6640", "Skip building downstream projects on error in upstream project."),
  Specify_a_list_of_glob_patterns_that_match_files_to_be_included_in_compilation: diag(6641, 3 /* Message */, "Specify_a_list_of_glob_patterns_that_match_files_to_be_included_in_compilation_6641", "Specify a list of glob patterns that match files to be included in compilation."),
  Save_tsbuildinfo_files_to_allow_for_incremental_compilation_of_projects: diag(6642, 3 /* Message */, "Save_tsbuildinfo_files_to_allow_for_incremental_compilation_of_projects_6642", "Save .tsbuildinfo files to allow for incremental compilation of projects."),
  Include_sourcemap_files_inside_the_emitted_JavaScript: diag(6643, 3 /* Message */, "Include_sourcemap_files_inside_the_emitted_JavaScript_6643", "Include sourcemap files inside the emitted JavaScript."),
  Include_source_code_in_the_sourcemaps_inside_the_emitted_JavaScript: diag(6644, 3 /* Message */, "Include_source_code_in_the_sourcemaps_inside_the_emitted_JavaScript_6644", "Include source code in the sourcemaps inside the emitted JavaScript."),
  Ensure_that_each_file_can_be_safely_transpiled_without_relying_on_other_imports: diag(6645, 3 /* Message */, "Ensure_that_each_file_can_be_safely_transpiled_without_relying_on_other_imports_6645", "Ensure that each file can be safely transpiled without relying on other imports."),
  Specify_what_JSX_code_is_generated: diag(6646, 3 /* Message */, "Specify_what_JSX_code_is_generated_6646", "Specify what JSX code is generated."),
  Specify_the_JSX_factory_function_used_when_targeting_React_JSX_emit_e_g_React_createElement_or_h: diag(6647, 3 /* Message */, "Specify_the_JSX_factory_function_used_when_targeting_React_JSX_emit_e_g_React_createElement_or_h_6647", "Specify the JSX factory function used when targeting React JSX emit, e.g. 'React.createElement' or 'h'."),
  Specify_the_JSX_Fragment_reference_used_for_fragments_when_targeting_React_JSX_emit_e_g_React_Fragment_or_Fragment: diag(6648, 3 /* Message */, "Specify_the_JSX_Fragment_reference_used_for_fragments_when_targeting_React_JSX_emit_e_g_React_Fragme_6648", "Specify the JSX Fragment reference used for fragments when targeting React JSX emit e.g. 'React.Fragment' or 'Fragment'."),
  Specify_module_specifier_used_to_import_the_JSX_factory_functions_when_using_jsx_Colon_react_jsx_Asterisk: diag(6649, 3 /* Message */, "Specify_module_specifier_used_to_import_the_JSX_factory_functions_when_using_jsx_Colon_react_jsx_Ast_6649", "Specify module specifier used to import the JSX factory functions when using 'jsx: react-jsx*'."),
  Make_keyof_only_return_strings_instead_of_string_numbers_or_symbols_Legacy_option: diag(6650, 3 /* Message */, "Make_keyof_only_return_strings_instead_of_string_numbers_or_symbols_Legacy_option_6650", "Make keyof only return strings instead of string, numbers or symbols. Legacy option."),
  Specify_a_set_of_bundled_library_declaration_files_that_describe_the_target_runtime_environment: diag(6651, 3 /* Message */, "Specify_a_set_of_bundled_library_declaration_files_that_describe_the_target_runtime_environment_6651", "Specify a set of bundled library declaration files that describe the target runtime environment."),
  Print_the_names_of_emitted_files_after_a_compilation: diag(6652, 3 /* Message */, "Print_the_names_of_emitted_files_after_a_compilation_6652", "Print the names of emitted files after a compilation."),
  Print_all_of_the_files_read_during_the_compilation: diag(6653, 3 /* Message */, "Print_all_of_the_files_read_during_the_compilation_6653", "Print all of the files read during the compilation."),
  Set_the_language_of_the_messaging_from_TypeScript_This_does_not_affect_emit: diag(6654, 3 /* Message */, "Set_the_language_of_the_messaging_from_TypeScript_This_does_not_affect_emit_6654", "Set the language of the messaging from TypeScript. This does not affect emit."),
  Specify_the_location_where_debugger_should_locate_map_files_instead_of_generated_locations: diag(6655, 3 /* Message */, "Specify_the_location_where_debugger_should_locate_map_files_instead_of_generated_locations_6655", "Specify the location where debugger should locate map files instead of generated locations."),
  Specify_the_maximum_folder_depth_used_for_checking_JavaScript_files_from_node_modules_Only_applicable_with_allowJs: diag(6656, 3 /* Message */, "Specify_the_maximum_folder_depth_used_for_checking_JavaScript_files_from_node_modules_Only_applicabl_6656", "Specify the maximum folder depth used for checking JavaScript files from 'node_modules'. Only applicable with 'allowJs'."),
  Specify_what_module_code_is_generated: diag(6657, 3 /* Message */, "Specify_what_module_code_is_generated_6657", "Specify what module code is generated."),
  Specify_how_TypeScript_looks_up_a_file_from_a_given_module_specifier: diag(6658, 3 /* Message */, "Specify_how_TypeScript_looks_up_a_file_from_a_given_module_specifier_6658", "Specify how TypeScript looks up a file from a given module specifier."),
  Set_the_newline_character_for_emitting_files: diag(6659, 3 /* Message */, "Set_the_newline_character_for_emitting_files_6659", "Set the newline character for emitting files."),
  Disable_emitting_files_from_a_compilation: diag(6660, 3 /* Message */, "Disable_emitting_files_from_a_compilation_6660", "Disable emitting files from a compilation."),
  Disable_generating_custom_helper_functions_like_extends_in_compiled_output: diag(6661, 3 /* Message */, "Disable_generating_custom_helper_functions_like_extends_in_compiled_output_6661", "Disable generating custom helper functions like '__extends' in compiled output."),
  Disable_emitting_files_if_any_type_checking_errors_are_reported: diag(6662, 3 /* Message */, "Disable_emitting_files_if_any_type_checking_errors_are_reported_6662", "Disable emitting files if any type checking errors are reported."),
  Disable_truncating_types_in_error_messages: diag(6663, 3 /* Message */, "Disable_truncating_types_in_error_messages_6663", "Disable truncating types in error messages."),
  Enable_error_reporting_for_fallthrough_cases_in_switch_statements: diag(6664, 3 /* Message */, "Enable_error_reporting_for_fallthrough_cases_in_switch_statements_6664", "Enable error reporting for fallthrough cases in switch statements."),
  Enable_error_reporting_for_expressions_and_declarations_with_an_implied_any_type: diag(6665, 3 /* Message */, "Enable_error_reporting_for_expressions_and_declarations_with_an_implied_any_type_6665", "Enable error reporting for expressions and declarations with an implied 'any' type."),
  Ensure_overriding_members_in_derived_classes_are_marked_with_an_override_modifier: diag(6666, 3 /* Message */, "Ensure_overriding_members_in_derived_classes_are_marked_with_an_override_modifier_6666", "Ensure overriding members in derived classes are marked with an override modifier."),
  Enable_error_reporting_for_codepaths_that_do_not_explicitly_return_in_a_function: diag(6667, 3 /* Message */, "Enable_error_reporting_for_codepaths_that_do_not_explicitly_return_in_a_function_6667", "Enable error reporting for codepaths that do not explicitly return in a function."),
  Enable_error_reporting_when_this_is_given_the_type_any: diag(6668, 3 /* Message */, "Enable_error_reporting_when_this_is_given_the_type_any_6668", "Enable error reporting when 'this' is given the type 'any'."),
  Disable_adding_use_strict_directives_in_emitted_JavaScript_files: diag(6669, 3 /* Message */, "Disable_adding_use_strict_directives_in_emitted_JavaScript_files_6669", "Disable adding 'use strict' directives in emitted JavaScript files."),
  Disable_including_any_library_files_including_the_default_lib_d_ts: diag(6670, 3 /* Message */, "Disable_including_any_library_files_including_the_default_lib_d_ts_6670", "Disable including any library files, including the default lib.d.ts."),
  Enforces_using_indexed_accessors_for_keys_declared_using_an_indexed_type: diag(6671, 3 /* Message */, "Enforces_using_indexed_accessors_for_keys_declared_using_an_indexed_type_6671", "Enforces using indexed accessors for keys declared using an indexed type."),
  Disallow_import_s_require_s_or_reference_s_from_expanding_the_number_of_files_TypeScript_should_add_to_a_project: diag(6672, 3 /* Message */, "Disallow_import_s_require_s_or_reference_s_from_expanding_the_number_of_files_TypeScript_should_add__6672", "Disallow 'import's, 'require's or '<reference>'s from expanding the number of files TypeScript should add to a project."),
  Disable_strict_checking_of_generic_signatures_in_function_types: diag(6673, 3 /* Message */, "Disable_strict_checking_of_generic_signatures_in_function_types_6673", "Disable strict checking of generic signatures in function types."),
  Add_undefined_to_a_type_when_accessed_using_an_index: diag(6674, 3 /* Message */, "Add_undefined_to_a_type_when_accessed_using_an_index_6674", "Add 'undefined' to a type when accessed using an index."),
  Enable_error_reporting_when_local_variables_aren_t_read: diag(6675, 3 /* Message */, "Enable_error_reporting_when_local_variables_aren_t_read_6675", "Enable error reporting when local variables aren't read."),
  Raise_an_error_when_a_function_parameter_isn_t_read: diag(6676, 3 /* Message */, "Raise_an_error_when_a_function_parameter_isn_t_read_6676", "Raise an error when a function parameter isn't read."),
  Deprecated_setting_Use_outFile_instead: diag(6677, 3 /* Message */, "Deprecated_setting_Use_outFile_instead_6677", "Deprecated setting. Use 'outFile' instead."),
  Specify_an_output_folder_for_all_emitted_files: diag(6678, 3 /* Message */, "Specify_an_output_folder_for_all_emitted_files_6678", "Specify an output folder for all emitted files."),
  Specify_a_file_that_bundles_all_outputs_into_one_JavaScript_file_If_declaration_is_true_also_designates_a_file_that_bundles_all_d_ts_output: diag(6679, 3 /* Message */, "Specify_a_file_that_bundles_all_outputs_into_one_JavaScript_file_If_declaration_is_true_also_designa_6679", "Specify a file that bundles all outputs into one JavaScript file. If 'declaration' is true, also designates a file that bundles all .d.ts output."),
  Specify_a_set_of_entries_that_re_map_imports_to_additional_lookup_locations: diag(6680, 3 /* Message */, "Specify_a_set_of_entries_that_re_map_imports_to_additional_lookup_locations_6680", "Specify a set of entries that re-map imports to additional lookup locations."),
  Specify_a_list_of_language_service_plugins_to_include: diag(6681, 3 /* Message */, "Specify_a_list_of_language_service_plugins_to_include_6681", "Specify a list of language service plugins to include."),
  Disable_erasing_const_enum_declarations_in_generated_code: diag(6682, 3 /* Message */, "Disable_erasing_const_enum_declarations_in_generated_code_6682", "Disable erasing 'const enum' declarations in generated code."),
  Disable_resolving_symlinks_to_their_realpath_This_correlates_to_the_same_flag_in_node: diag(6683, 3 /* Message */, "Disable_resolving_symlinks_to_their_realpath_This_correlates_to_the_same_flag_in_node_6683", "Disable resolving symlinks to their realpath. This correlates to the same flag in node."),
  Disable_wiping_the_console_in_watch_mode: diag(6684, 3 /* Message */, "Disable_wiping_the_console_in_watch_mode_6684", "Disable wiping the console in watch mode."),
  Enable_color_and_formatting_in_TypeScript_s_output_to_make_compiler_errors_easier_to_read: diag(6685, 3 /* Message */, "Enable_color_and_formatting_in_TypeScript_s_output_to_make_compiler_errors_easier_to_read_6685", "Enable color and formatting in TypeScript's output to make compiler errors easier to read."),
  Specify_the_object_invoked_for_createElement_This_only_applies_when_targeting_react_JSX_emit: diag(6686, 3 /* Message */, "Specify_the_object_invoked_for_createElement_This_only_applies_when_targeting_react_JSX_emit_6686", "Specify the object invoked for 'createElement'. This only applies when targeting 'react' JSX emit."),
  Specify_an_array_of_objects_that_specify_paths_for_projects_Used_in_project_references: diag(6687, 3 /* Message */, "Specify_an_array_of_objects_that_specify_paths_for_projects_Used_in_project_references_6687", "Specify an array of objects that specify paths for projects. Used in project references."),
  Disable_emitting_comments: diag(6688, 3 /* Message */, "Disable_emitting_comments_6688", "Disable emitting comments."),
  Enable_importing_json_files: diag(6689, 3 /* Message */, "Enable_importing_json_files_6689", "Enable importing .json files."),
  Specify_the_root_folder_within_your_source_files: diag(6690, 3 /* Message */, "Specify_the_root_folder_within_your_source_files_6690", "Specify the root folder within your source files."),
  Allow_multiple_folders_to_be_treated_as_one_when_resolving_modules: diag(6691, 3 /* Message */, "Allow_multiple_folders_to_be_treated_as_one_when_resolving_modules_6691", "Allow multiple folders to be treated as one when resolving modules."),
  Skip_type_checking_d_ts_files_that_are_included_with_TypeScript: diag(6692, 3 /* Message */, "Skip_type_checking_d_ts_files_that_are_included_with_TypeScript_6692", "Skip type checking .d.ts files that are included with TypeScript."),
  Skip_type_checking_all_d_ts_files: diag(6693, 3 /* Message */, "Skip_type_checking_all_d_ts_files_6693", "Skip type checking all .d.ts files."),
  Create_source_map_files_for_emitted_JavaScript_files: diag(6694, 3 /* Message */, "Create_source_map_files_for_emitted_JavaScript_files_6694", "Create source map files for emitted JavaScript files."),
  Specify_the_root_path_for_debuggers_to_find_the_reference_source_code: diag(6695, 3 /* Message */, "Specify_the_root_path_for_debuggers_to_find_the_reference_source_code_6695", "Specify the root path for debuggers to find the reference source code."),
  Check_that_the_arguments_for_bind_call_and_apply_methods_match_the_original_function: diag(6697, 3 /* Message */, "Check_that_the_arguments_for_bind_call_and_apply_methods_match_the_original_function_6697", "Check that the arguments for 'bind', 'call', and 'apply' methods match the original function."),
  When_assigning_functions_check_to_ensure_parameters_and_the_return_values_are_subtype_compatible: diag(6698, 3 /* Message */, "When_assigning_functions_check_to_ensure_parameters_and_the_return_values_are_subtype_compatible_6698", "When assigning functions, check to ensure parameters and the return values are subtype-compatible."),
  When_type_checking_take_into_account_null_and_undefined: diag(6699, 3 /* Message */, "When_type_checking_take_into_account_null_and_undefined_6699", "When type checking, take into account 'null' and 'undefined'."),
  Check_for_class_properties_that_are_declared_but_not_set_in_the_constructor: diag(6700, 3 /* Message */, "Check_for_class_properties_that_are_declared_but_not_set_in_the_constructor_6700", "Check for class properties that are declared but not set in the constructor."),
  Disable_emitting_declarations_that_have_internal_in_their_JSDoc_comments: diag(6701, 3 /* Message */, "Disable_emitting_declarations_that_have_internal_in_their_JSDoc_comments_6701", "Disable emitting declarations that have '@internal' in their JSDoc comments."),
  Disable_reporting_of_excess_property_errors_during_the_creation_of_object_literals: diag(6702, 3 /* Message */, "Disable_reporting_of_excess_property_errors_during_the_creation_of_object_literals_6702", "Disable reporting of excess property errors during the creation of object literals."),
  Suppress_noImplicitAny_errors_when_indexing_objects_that_lack_index_signatures: diag(6703, 3 /* Message */, "Suppress_noImplicitAny_errors_when_indexing_objects_that_lack_index_signatures_6703", "Suppress 'noImplicitAny' errors when indexing objects that lack index signatures."),
  Synchronously_call_callbacks_and_update_the_state_of_directory_watchers_on_platforms_that_don_t_support_recursive_watching_natively: diag(6704, 3 /* Message */, "Synchronously_call_callbacks_and_update_the_state_of_directory_watchers_on_platforms_that_don_t_supp_6704", "Synchronously call callbacks and update the state of directory watchers on platforms that don`t support recursive watching natively."),
  Set_the_JavaScript_language_version_for_emitted_JavaScript_and_include_compatible_library_declarations: diag(6705, 3 /* Message */, "Set_the_JavaScript_language_version_for_emitted_JavaScript_and_include_compatible_library_declaratio_6705", "Set the JavaScript language version for emitted JavaScript and include compatible library declarations."),
  Log_paths_used_during_the_moduleResolution_process: diag(6706, 3 /* Message */, "Log_paths_used_during_the_moduleResolution_process_6706", "Log paths used during the 'moduleResolution' process."),
  Specify_the_path_to_tsbuildinfo_incremental_compilation_file: diag(6707, 3 /* Message */, "Specify_the_path_to_tsbuildinfo_incremental_compilation_file_6707", "Specify the path to .tsbuildinfo incremental compilation file."),
  Specify_options_for_automatic_acquisition_of_declaration_files: diag(6709, 3 /* Message */, "Specify_options_for_automatic_acquisition_of_declaration_files_6709", "Specify options for automatic acquisition of declaration files."),
  Specify_multiple_folders_that_act_like_Slashnode_modules_Slash_types: diag(6710, 3 /* Message */, "Specify_multiple_folders_that_act_like_Slashnode_modules_Slash_types_6710", "Specify multiple folders that act like './node_modules/@types'."),
  Specify_type_package_names_to_be_included_without_being_referenced_in_a_source_file: diag(6711, 3 /* Message */, "Specify_type_package_names_to_be_included_without_being_referenced_in_a_source_file_6711", "Specify type package names to be included without being referenced in a source file."),
  Emit_ECMAScript_standard_compliant_class_fields: diag(6712, 3 /* Message */, "Emit_ECMAScript_standard_compliant_class_fields_6712", "Emit ECMAScript-standard-compliant class fields."),
  Enable_verbose_logging: diag(6713, 3 /* Message */, "Enable_verbose_logging_6713", "Enable verbose logging."),
  Specify_how_directories_are_watched_on_systems_that_lack_recursive_file_watching_functionality: diag(6714, 3 /* Message */, "Specify_how_directories_are_watched_on_systems_that_lack_recursive_file_watching_functionality_6714", "Specify how directories are watched on systems that lack recursive file-watching functionality."),
  Specify_how_the_TypeScript_watch_mode_works: diag(6715, 3 /* Message */, "Specify_how_the_TypeScript_watch_mode_works_6715", "Specify how the TypeScript watch mode works."),
  Require_undeclared_properties_from_index_signatures_to_use_element_accesses: diag(6717, 3 /* Message */, "Require_undeclared_properties_from_index_signatures_to_use_element_accesses_6717", "Require undeclared properties from index signatures to use element accesses."),
  Specify_emit_Slashchecking_behavior_for_imports_that_are_only_used_for_types: diag(6718, 3 /* Message */, "Specify_emit_Slashchecking_behavior_for_imports_that_are_only_used_for_types_6718", "Specify emit/checking behavior for imports that are only used for types."),
  Require_sufficient_annotation_on_exports_so_other_tools_can_trivially_generate_declaration_files: diag(6719, 3 /* Message */, "Require_sufficient_annotation_on_exports_so_other_tools_can_trivially_generate_declaration_files_6719", "Require sufficient annotation on exports so other tools can trivially generate declaration files."),
  Built_in_iterators_are_instantiated_with_a_TReturn_type_of_undefined_instead_of_any: diag(6720, 3 /* Message */, "Built_in_iterators_are_instantiated_with_a_TReturn_type_of_undefined_instead_of_any_6720", "Built-in iterators are instantiated with a 'TReturn' type of 'undefined' instead of 'any'."),
  Do_not_allow_runtime_constructs_that_are_not_part_of_ECMAScript: diag(6721, 3 /* Message */, "Do_not_allow_runtime_constructs_that_are_not_part_of_ECMAScript_6721", "Do not allow runtime constructs that are not part of ECMAScript."),
  Default_catch_clause_variables_as_unknown_instead_of_any: diag(6803, 3 /* Message */, "Default_catch_clause_variables_as_unknown_instead_of_any_6803", "Default catch clause variables as 'unknown' instead of 'any'."),
  Do_not_transform_or_elide_any_imports_or_exports_not_marked_as_type_only_ensuring_they_are_written_in_the_output_file_s_format_based_on_the_module_setting: diag(6804, 3 /* Message */, "Do_not_transform_or_elide_any_imports_or_exports_not_marked_as_type_only_ensuring_they_are_written_i_6804", "Do not transform or elide any imports or exports not marked as type-only, ensuring they are written in the output file's format based on the 'module' setting."),
  Disable_full_type_checking_only_critical_parse_and_emit_errors_will_be_reported: diag(6805, 3 /* Message */, "Disable_full_type_checking_only_critical_parse_and_emit_errors_will_be_reported_6805", "Disable full type checking (only critical parse and emit errors will be reported)."),
  Check_side_effect_imports: diag(6806, 3 /* Message */, "Check_side_effect_imports_6806", "Check side effect imports."),
  This_operation_can_be_simplified_This_shift_is_identical_to_0_1_2: diag(6807, 1 /* Error */, "This_operation_can_be_simplified_This_shift_is_identical_to_0_1_2_6807", "This operation can be simplified. This shift is identical to `{0} {1} {2}`."),
  Enable_lib_replacement: diag(6808, 3 /* Message */, "Enable_lib_replacement_6808", "Enable lib replacement."),
  one_of_Colon: diag(6900, 3 /* Message */, "one_of_Colon_6900", "one of:"),
  one_or_more_Colon: diag(6901, 3 /* Message */, "one_or_more_Colon_6901", "one or more:"),
  type_Colon: diag(6902, 3 /* Message */, "type_Colon_6902", "type:"),
  default_Colon: diag(6903, 3 /* Message */, "default_Colon_6903", "default:"),
  module_system_or_esModuleInterop: diag(6904, 3 /* Message */, "module_system_or_esModuleInterop_6904", 'module ***REMOVED******REMOVED******REMOVED*** "system" or esModuleInterop'),
  false_unless_strict_is_set: diag(6905, 3 /* Message */, "false_unless_strict_is_set_6905", "`false`, unless `strict` is set"),
  false_unless_composite_is_set: diag(6906, 3 /* Message */, "false_unless_composite_is_set_6906", "`false`, unless `composite` is set"),
  node_modules_bower_components_jspm_packages_plus_the_value_of_outDir_if_one_is_specified: diag(6907, 3 /* Message */, "node_modules_bower_components_jspm_packages_plus_the_value_of_outDir_if_one_is_specified_6907", '`["node_modules", "bower_components", "jspm_packages"]`, plus the value of `outDir` if one is specified.'),
  if_files_is_specified_otherwise_Asterisk_Asterisk_Slash_Asterisk: diag(6908, 3 /* Message */, "if_files_is_specified_otherwise_Asterisk_Asterisk_Slash_Asterisk_6908", '`[]` if `files` is specified, otherwise `["**/*"]`'),
  true_if_composite_false_otherwise: diag(6909, 3 /* Message */, "true_if_composite_false_otherwise_6909", "`true` if `composite`, `false` otherwise"),
  module_AMD_or_UMD_or_System_or_ES6_then_Classic_Otherwise_Node: diag(69010, 3 /* Message */, "module_AMD_or_UMD_or_System_or_ES6_then_Classic_Otherwise_Node_69010", "module ***REMOVED******REMOVED******REMOVED*** `AMD` or `UMD` or `System` or `ES6`, then `Classic`, Otherwise `Node`"),
  Computed_from_the_list_of_input_files: diag(6911, 3 /* Message */, "Computed_from_the_list_of_input_files_6911", "Computed from the list of input files"),
  Platform_specific: diag(6912, 3 /* Message */, "Platform_specific_6912", "Platform specific"),
  You_can_learn_about_all_of_the_compiler_options_at_0: diag(6913, 3 /* Message */, "You_can_learn_about_all_of_the_compiler_options_at_0_6913", "You can learn about all of the compiler options at {0}"),
  Including_watch_w_will_start_watching_the_current_project_for_the_file_changes_Once_set_you_can_config_watch_mode_with_Colon: diag(6914, 3 /* Message */, "Including_watch_w_will_start_watching_the_current_project_for_the_file_changes_Once_set_you_can_conf_6914", "Including --watch, -w will start watching the current project for the file changes. Once set, you can config watch mode with:"),
  Using_build_b_will_make_tsc_behave_more_like_a_build_orchestrator_than_a_compiler_This_is_used_to_trigger_building_composite_projects_which_you_can_learn_more_about_at_0: diag(6915, 3 /* Message */, "Using_build_b_will_make_tsc_behave_more_like_a_build_orchestrator_than_a_compiler_This_is_used_to_tr_6915", "Using --build, -b will make tsc behave more like a build orchestrator than a compiler. This is used to trigger building composite projects which you can learn more about at {0}"),
  COMMON_COMMANDS: diag(6916, 3 /* Message */, "COMMON_COMMANDS_6916", "COMMON COMMANDS"),
  ALL_COMPILER_OPTIONS: diag(6917, 3 /* Message */, "ALL_COMPILER_OPTIONS_6917", "ALL COMPILER OPTIONS"),
  WATCH_OPTIONS: diag(6918, 3 /* Message */, "WATCH_OPTIONS_6918", "WATCH OPTIONS"),
  BUILD_OPTIONS: diag(6919, 3 /* Message */, "BUILD_OPTIONS_6919", "BUILD OPTIONS"),
  COMMON_COMPILER_OPTIONS: diag(6920, 3 /* Message */, "COMMON_COMPILER_OPTIONS_6920", "COMMON COMPILER OPTIONS"),
  COMMAND_LINE_FLAGS: diag(6921, 3 /* Message */, "COMMAND_LINE_FLAGS_6921", "COMMAND LINE FLAGS"),
  tsc_Colon_The_TypeScript_Compiler: diag(6922, 3 /* Message */, "tsc_Colon_The_TypeScript_Compiler_6922", "tsc: The TypeScript Compiler"),
  Compiles_the_current_project_tsconfig_json_in_the_working_directory: diag(6923, 3 /* Message */, "Compiles_the_current_project_tsconfig_json_in_the_working_directory_6923", "Compiles the current project (tsconfig.json in the working directory.)"),
  Ignoring_tsconfig_json_compiles_the_specified_files_with_default_compiler_options: diag(6924, 3 /* Message */, "Ignoring_tsconfig_json_compiles_the_specified_files_with_default_compiler_options_6924", "Ignoring tsconfig.json, compiles the specified files with default compiler options."),
  Build_a_composite_project_in_the_working_directory: diag(6925, 3 /* Message */, "Build_a_composite_project_in_the_working_directory_6925", "Build a composite project in the working directory."),
  Creates_a_tsconfig_json_with_the_recommended_settings_in_the_working_directory: diag(6926, 3 /* Message */, "Creates_a_tsconfig_json_with_the_recommended_settings_in_the_working_directory_6926", "Creates a tsconfig.json with the recommended settings in the working directory."),
  Compiles_the_TypeScript_project_located_at_the_specified_path: diag(6927, 3 /* Message */, "Compiles_the_TypeScript_project_located_at_the_specified_path_6927", "Compiles the TypeScript project located at the specified path."),
  An_expanded_version_of_this_information_showing_all_possible_compiler_options: diag(6928, 3 /* Message */, "An_expanded_version_of_this_information_showing_all_possible_compiler_options_6928", "An expanded version of this information, showing all possible compiler options"),
  Compiles_the_current_project_with_additional_settings: diag(6929, 3 /* Message */, "Compiles_the_current_project_with_additional_settings_6929", "Compiles the current project, with additional settings."),
  true_for_ES2022_and_above_including_ESNext: diag(6930, 3 /* Message */, "true_for_ES2022_and_above_including_ESNext_6930", "`true` for ES2022 and above, including ESNext."),
  List_of_file_name_suffixes_to_search_when_resolving_a_module: diag(6931, 1 /* Error */, "List_of_file_name_suffixes_to_search_when_resolving_a_module_6931", "List of file name suffixes to search when resolving a module."),
  Variable_0_implicitly_has_an_1_type: diag(7005, 1 /* Error */, "Variable_0_implicitly_has_an_1_type_7005", "Variable '{0}' implicitly has an '{1}' type."),
  Parameter_0_implicitly_has_an_1_type: diag(7006, 1 /* Error */, "Parameter_0_implicitly_has_an_1_type_7006", "Parameter '{0}' implicitly has an '{1}' type."),
  Member_0_implicitly_has_an_1_type: diag(7008, 1 /* Error */, "Member_0_implicitly_has_an_1_type_7008", "Member '{0}' implicitly has an '{1}' type."),
  new_expression_whose_target_lacks_a_construct_signature_implicitly_has_an_any_type: diag(7009, 1 /* Error */, "new_expression_whose_target_lacks_a_construct_signature_implicitly_has_an_any_type_7009", "'new' expression, whose target lacks a construct signature, implicitly has an 'any' type."),
  _0_which_lacks_return_type_annotation_implicitly_has_an_1_return_type: diag(7010, 1 /* Error */, "_0_which_lacks_return_type_annotation_implicitly_has_an_1_return_type_7010", "'{0}', which lacks return-type annotation, implicitly has an '{1}' return type."),
  Function_expression_which_lacks_return_type_annotation_implicitly_has_an_0_return_type: diag(7011, 1 /* Error */, "Function_expression_which_lacks_return_type_annotation_implicitly_has_an_0_return_type_7011", "Function expression, which lacks return-type annotation, implicitly has an '{0}' return type."),
  This_overload_implicitly_returns_the_type_0_because_it_lacks_a_return_type_annotation: diag(7012, 1 /* Error */, "This_overload_implicitly_returns_the_type_0_because_it_lacks_a_return_type_annotation_7012", "This overload implicitly returns the type '{0}' because it lacks a return type annotation."),
  Construct_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type: diag(7013, 1 /* Error */, "Construct_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type_7013", "Construct signature, which lacks return-type annotation, implicitly has an 'any' return type."),
  Function_type_which_lacks_return_type_annotation_implicitly_has_an_0_return_type: diag(7014, 1 /* Error */, "Function_type_which_lacks_return_type_annotation_implicitly_has_an_0_return_type_7014", "Function type, which lacks return-type annotation, implicitly has an '{0}' return type."),
  Element_implicitly_has_an_any_type_because_index_expression_is_not_of_type_number: diag(7015, 1 /* Error */, "Element_implicitly_has_an_any_type_because_index_expression_is_not_of_type_number_7015", "Element implicitly has an 'any' type because index expression is not of type 'number'."),
  Could_not_find_a_declaration_file_for_module_0_1_implicitly_has_an_any_type: diag(7016, 1 /* Error */, "Could_not_find_a_declaration_file_for_module_0_1_implicitly_has_an_any_type_7016", "Could not find a declaration file for module '{0}'. '{1}' implicitly has an 'any' type."),
  Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature: diag(7017, 1 /* Error */, "Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature_7017", "Element implicitly has an 'any' type because type '{0}' has no index signature."),
  Object_literal_s_property_0_implicitly_has_an_1_type: diag(7018, 1 /* Error */, "Object_literal_s_property_0_implicitly_has_an_1_type_7018", "Object literal's property '{0}' implicitly has an '{1}' type."),
  Rest_parameter_0_implicitly_has_an_any_type: diag(7019, 1 /* Error */, "Rest_parameter_0_implicitly_has_an_any_type_7019", "Rest parameter '{0}' implicitly has an 'any[]' type."),
  Call_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type: diag(7020, 1 /* Error */, "Call_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type_7020", "Call signature, which lacks return-type annotation, implicitly has an 'any' return type."),
  _0_implicitly_has_type_any_because_it_does_not_have_a_type_annotation_and_is_referenced_directly_or_indirectly_in_its_own_initializer: diag(7022, 1 /* Error */, "_0_implicitly_has_type_any_because_it_does_not_have_a_type_annotation_and_is_referenced_directly_or__7022", "'{0}' implicitly has type 'any' because it does not have a type annotation and is referenced directly or indirectly in its own initializer."),
  _0_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_referenced_directly_or_indirectly_in_one_of_its_return_expressions: diag(7023, 1 /* Error */, "_0_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_reference_7023", "'{0}' implicitly has return type 'any' because it does not have a return type annotation and is referenced directly or indirectly in one of its return expressions."),
  Function_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_referenced_directly_or_indirectly_in_one_of_its_return_expressions: diag(7024, 1 /* Error */, "Function_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_ref_7024", "Function implicitly has return type 'any' because it does not have a return type annotation and is referenced directly or indirectly in one of its return expressions."),
  Generator_implicitly_has_yield_type_0_Consider_supplying_a_return_type_annotation: diag(7025, 1 /* Error */, "Generator_implicitly_has_yield_type_0_Consider_supplying_a_return_type_annotation_7025", "Generator implicitly has yield type '{0}'. Consider supplying a return type annotation."),
  JSX_element_implicitly_has_type_any_because_no_interface_JSX_0_exists: diag(7026, 1 /* Error */, "JSX_element_implicitly_has_type_any_because_no_interface_JSX_0_exists_7026", "JSX element implicitly has type 'any' because no interface 'JSX.{0}' exists."),
  Unreachable_code_detected: diag(
    7027,
    1 /* Error */,
    "Unreachable_code_detected_7027",
    "Unreachable code detected.",
    /*reportsUnnecessary*/
    true
  ),
  Unused_label: diag(
    7028,
    1 /* Error */,
    "Unused_label_7028",
    "Unused label.",
    /*reportsUnnecessary*/
    true
  ),
  Fallthrough_case_in_switch: diag(7029, 1 /* Error */, "Fallthrough_case_in_switch_7029", "Fallthrough case in switch."),
  Not_all_code_paths_return_a_value: diag(7030, 1 /* Error */, "Not_all_code_paths_return_a_value_7030", "Not all code paths return a value."),
  Binding_element_0_implicitly_has_an_1_type: diag(7031, 1 /* Error */, "Binding_element_0_implicitly_has_an_1_type_7031", "Binding element '{0}' implicitly has an '{1}' type."),
  Property_0_implicitly_has_type_any_because_its_set_accessor_lacks_a_parameter_type_annotation: diag(7032, 1 /* Error */, "Property_0_implicitly_has_type_any_because_its_set_accessor_lacks_a_parameter_type_annotation_7032", "Property '{0}' implicitly has type 'any', because its set accessor lacks a parameter type annotation."),
  Property_0_implicitly_has_type_any_because_its_get_accessor_lacks_a_return_type_annotation: diag(7033, 1 /* Error */, "Property_0_implicitly_has_type_any_because_its_get_accessor_lacks_a_return_type_annotation_7033", "Property '{0}' implicitly has type 'any', because its get accessor lacks a return type annotation."),
  Variable_0_implicitly_has_type_1_in_some_locations_where_its_type_cannot_be_determined: diag(7034, 1 /* Error */, "Variable_0_implicitly_has_type_1_in_some_locations_where_its_type_cannot_be_determined_7034", "Variable '{0}' implicitly has type '{1}' in some locations where its type cannot be determined."),
  Try_npm_i_save_dev_types_Slash_1_if_it_exists_or_add_a_new_declaration_d_ts_file_containing_declare_module_0: diag(7035, 1 /* Error */, "Try_npm_i_save_dev_types_Slash_1_if_it_exists_or_add_a_new_declaration_d_ts_file_containing_declare__7035", "Try `npm i --save-dev @types/{1}` if it exists or add a new declaration (.d.ts) file containing `declare module '{0}';`"),
  Dynamic_import_s_specifier_must_be_of_type_string_but_here_has_type_0: diag(7036, 1 /* Error */, "Dynamic_import_s_specifier_must_be_of_type_string_but_here_has_type_0_7036", "Dynamic import's specifier must be of type 'string', but here has type '{0}'."),
  Enables_emit_interoperability_between_CommonJS_and_ES_Modules_via_creation_of_namespace_objects_for_all_imports_Implies_allowSyntheticDefaultImports: diag(7037, 3 /* Message */, "Enables_emit_interoperability_between_CommonJS_and_ES_Modules_via_creation_of_namespace_objects_for__7037", "Enables emit interoperability between CommonJS and ES Modules via creation of namespace objects for all imports. Implies 'allowSyntheticDefaultImports'."),
  Type_originates_at_this_import_A_namespace_style_import_cannot_be_called_or_constructed_and_will_cause_a_failure_at_runtime_Consider_using_a_default_import_or_import_require_here_instead: diag(7038, 3 /* Message */, "Type_originates_at_this_import_A_namespace_style_import_cannot_be_called_or_constructed_and_will_cau_7038", "Type originates at this import. A namespace-style import cannot be called or constructed, and will cause a failure at runtime. Consider using a default import or import require here instead."),
  Mapped_object_type_implicitly_has_an_any_template_type: diag(7039, 1 /* Error */, "Mapped_object_type_implicitly_has_an_any_template_type_7039", "Mapped object type implicitly has an 'any' template type."),
  If_the_0_package_actually_exposes_this_module_consider_sending_a_pull_request_to_amend_https_Colon_Slash_Slashgithub_com_SlashDefinitelyTyped_SlashDefinitelyTyped_Slashtree_Slashmaster_Slashtypes_Slash_1: diag(7040, 1 /* Error */, "If_the_0_package_actually_exposes_this_module_consider_sending_a_pull_request_to_amend_https_Colon_S_7040", "If the '{0}' package actually exposes this module, consider sending a pull request to amend 'https://github.com/DefinitelyTyped/DefinitelyTyped/tree/master/types/{1}'"),
  The_containing_arrow_function_captures_the_global_value_of_this: diag(7041, 1 /* Error */, "The_containing_arrow_function_captures_the_global_value_of_this_7041", "The containing arrow function captures the global value of 'this'."),
  Module_0_was_resolved_to_1_but_resolveJsonModule_is_not_used: diag(7042, 1 /* Error */, "Module_0_was_resolved_to_1_but_resolveJsonModule_is_not_used_7042", "Module '{0}' was resolved to '{1}', but '--resolveJsonModule' is not used."),
  Variable_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage: diag(7043, 2 /* Suggestion */, "Variable_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage_7043", "Variable '{0}' implicitly has an '{1}' type, but a better type may be inferred from usage."),
  Parameter_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage: diag(7044, 2 /* Suggestion */, "Parameter_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage_7044", "Parameter '{0}' implicitly has an '{1}' type, but a better type may be inferred from usage."),
  Member_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage: diag(7045, 2 /* Suggestion */, "Member_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage_7045", "Member '{0}' implicitly has an '{1}' type, but a better type may be inferred from usage."),
  Variable_0_implicitly_has_type_1_in_some_locations_but_a_better_type_may_be_inferred_from_usage: diag(7046, 2 /* Suggestion */, "Variable_0_implicitly_has_type_1_in_some_locations_but_a_better_type_may_be_inferred_from_usage_7046", "Variable '{0}' implicitly has type '{1}' in some locations, but a better type may be inferred from usage."),
  Rest_parameter_0_implicitly_has_an_any_type_but_a_better_type_may_be_inferred_from_usage: diag(7047, 2 /* Suggestion */, "Rest_parameter_0_implicitly_has_an_any_type_but_a_better_type_may_be_inferred_from_usage_7047", "Rest parameter '{0}' implicitly has an 'any[]' type, but a better type may be inferred from usage."),
  Property_0_implicitly_has_type_any_but_a_better_type_for_its_get_accessor_may_be_inferred_from_usage: diag(7048, 2 /* Suggestion */, "Property_0_implicitly_has_type_any_but_a_better_type_for_its_get_accessor_may_be_inferred_from_usage_7048", "Property '{0}' implicitly has type 'any', but a better type for its get accessor may be inferred from usage."),
  Property_0_implicitly_has_type_any_but_a_better_type_for_its_set_accessor_may_be_inferred_from_usage: diag(7049, 2 /* Suggestion */, "Property_0_implicitly_has_type_any_but_a_better_type_for_its_set_accessor_may_be_inferred_from_usage_7049", "Property '{0}' implicitly has type 'any', but a better type for its set accessor may be inferred from usage."),
  _0_implicitly_has_an_1_return_type_but_a_better_type_may_be_inferred_from_usage: diag(7050, 2 /* Suggestion */, "_0_implicitly_has_an_1_return_type_but_a_better_type_may_be_inferred_from_usage_7050", "'{0}' implicitly has an '{1}' return type, but a better type may be inferred from usage."),
  Parameter_has_a_name_but_no_type_Did_you_mean_0_Colon_1: diag(7051, 1 /* Error */, "Parameter_has_a_name_but_no_type_Did_you_mean_0_Colon_1_7051", "Parameter has a name but no type. Did you mean '{0}: {1}'?"),
  Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature_Did_you_mean_to_call_1: diag(7052, 1 /* Error */, "Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature_Did_you_mean_to_call_1_7052", "Element implicitly has an 'any' type because type '{0}' has no index signature. Did you mean to call '{1}'?"),
  Element_implicitly_has_an_any_type_because_expression_of_type_0_can_t_be_used_to_index_type_1: diag(7053, 1 /* Error */, "Element_implicitly_has_an_any_type_because_expression_of_type_0_can_t_be_used_to_index_type_1_7053", "Element implicitly has an 'any' type because expression of type '{0}' can't be used to index type '{1}'."),
  No_index_signature_with_a_parameter_of_type_0_was_found_on_type_1: diag(7054, 1 /* Error */, "No_index_signature_with_a_parameter_of_type_0_was_found_on_type_1_7054", "No index signature with a parameter of type '{0}' was found on type '{1}'."),
  _0_which_lacks_return_type_annotation_implicitly_has_an_1_yield_type: diag(7055, 1 /* Error */, "_0_which_lacks_return_type_annotation_implicitly_has_an_1_yield_type_7055", "'{0}', which lacks return-type annotation, implicitly has an '{1}' yield type."),
  The_inferred_type_of_this_node_exceeds_the_maximum_length_the_compiler_will_serialize_An_explicit_type_annotation_is_needed: diag(7056, 1 /* Error */, "The_inferred_type_of_this_node_exceeds_the_maximum_length_the_compiler_will_serialize_An_explicit_ty_7056", "The inferred type of this node exceeds the maximum length the compiler will serialize. An explicit type annotation is needed."),
  yield_expression_implicitly_results_in_an_any_type_because_its_containing_generator_lacks_a_return_type_annotation: diag(7057, 1 /* Error */, "yield_expression_implicitly_results_in_an_any_type_because_its_containing_generator_lacks_a_return_t_7057", "'yield' expression implicitly results in an 'any' type because its containing generator lacks a return-type annotation."),
  If_the_0_package_actually_exposes_this_module_try_adding_a_new_declaration_d_ts_file_containing_declare_module_1: diag(7058, 1 /* Error */, "If_the_0_package_actually_exposes_this_module_try_adding_a_new_declaration_d_ts_file_containing_decl_7058", "If the '{0}' package actually exposes this module, try adding a new declaration (.d.ts) file containing `declare module '{1}';`"),
  This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Use_an_as_expression_instead: diag(7059, 1 /* Error */, "This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Use_an_as_expression_instead_7059", "This syntax is reserved in files with the .mts or .cts extension. Use an `as` expression instead."),
  This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Add_a_trailing_comma_or_explicit_constraint: diag(7060, 1 /* Error */, "This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Add_a_trailing_comma_or_explicit_cons_7060", "This syntax is reserved in files with the .mts or .cts extension. Add a trailing comma or explicit constraint."),
  A_mapped_type_may_not_declare_properties_or_methods: diag(7061, 1 /* Error */, "A_mapped_type_may_not_declare_properties_or_methods_7061", "A mapped type may not declare properties or methods."),
  You_cannot_rename_this_element: diag(8e3, 1 /* Error */, "You_cannot_rename_this_element_8000", "You cannot rename this element."),
  You_cannot_rename_elements_that_are_defined_in_the_standard_TypeScript_library: diag(8001, 1 /* Error */, "You_cannot_rename_elements_that_are_defined_in_the_standard_TypeScript_library_8001", "You cannot rename elements that are defined in the standard TypeScript library."),
  import_can_only_be_used_in_TypeScript_files: diag(8002, 1 /* Error */, "import_can_only_be_used_in_TypeScript_files_8002", "'import ... ***REMOVED***' can only be used in TypeScript files."),
  export_can_only_be_used_in_TypeScript_files: diag(8003, 1 /* Error */, "export_can_only_be_used_in_TypeScript_files_8003", "'export ***REMOVED***' can only be used in TypeScript files."),
  Type_parameter_declarations_can_only_be_used_in_TypeScript_files: diag(8004, 1 /* Error */, "Type_parameter_declarations_can_only_be_used_in_TypeScript_files_8004", "Type parameter declarations can only be used in TypeScript files."),
  implements_clauses_can_only_be_used_in_TypeScript_files: diag(8005, 1 /* Error */, "implements_clauses_can_only_be_used_in_TypeScript_files_8005", "'implements' clauses can only be used in TypeScript files."),
  _0_declarations_can_only_be_used_in_TypeScript_files: diag(8006, 1 /* Error */, "_0_declarations_can_only_be_used_in_TypeScript_files_8006", "'{0}' declarations can only be used in TypeScript files."),
  Type_aliases_can_only_be_used_in_TypeScript_files: diag(8008, 1 /* Error */, "Type_aliases_can_only_be_used_in_TypeScript_files_8008", "Type aliases can only be used in TypeScript files."),
  The_0_modifier_can_only_be_used_in_TypeScript_files: diag(8009, 1 /* Error */, "The_0_modifier_can_only_be_used_in_TypeScript_files_8009", "The '{0}' modifier can only be used in TypeScript files."),
  Type_annotations_can_only_be_used_in_TypeScript_files: diag(8010, 1 /* Error */, "Type_annotations_can_only_be_used_in_TypeScript_files_8010", "Type annotations can only be used in TypeScript files."),
  Type_arguments_can_only_be_used_in_TypeScript_files: diag(8011, 1 /* Error */, "Type_arguments_can_only_be_used_in_TypeScript_files_8011", "Type arguments can only be used in TypeScript files."),
  Parameter_modifiers_can_only_be_used_in_TypeScript_files: diag(8012, 1 /* Error */, "Parameter_modifiers_can_only_be_used_in_TypeScript_files_8012", "Parameter modifiers can only be used in TypeScript files."),
  Non_null_assertions_can_only_be_used_in_TypeScript_files: diag(8013, 1 /* Error */, "Non_null_assertions_can_only_be_used_in_TypeScript_files_8013", "Non-null assertions can only be used in TypeScript files."),
  Type_assertion_expressions_can_only_be_used_in_TypeScript_files: diag(8016, 1 /* Error */, "Type_assertion_expressions_can_only_be_used_in_TypeScript_files_8016", "Type assertion expressions can only be used in TypeScript files."),
  Signature_declarations_can_only_be_used_in_TypeScript_files: diag(8017, 1 /* Error */, "Signature_declarations_can_only_be_used_in_TypeScript_files_8017", "Signature declarations can only be used in TypeScript files."),
  Report_errors_in_js_files: diag(8019, 3 /* Message */, "Report_errors_in_js_files_8019", "Report errors in .js files."),
  JSDoc_types_can_only_be_used_inside_documentation_comments: diag(8020, 1 /* Error */, "JSDoc_types_can_only_be_used_inside_documentation_comments_8020", "JSDoc types can only be used inside documentation comments."),
  JSDoc_typedef_tag_should_either_have_a_type_annotation_or_be_followed_by_property_or_member_tags: diag(8021, 1 /* Error */, "JSDoc_typedef_tag_should_either_have_a_type_annotation_or_be_followed_by_property_or_member_tags_8021", "JSDoc '@typedef' tag should either have a type annotation or be followed by '@property' or '@member' tags."),
  JSDoc_0_is_not_attached_to_a_class: diag(8022, 1 /* Error */, "JSDoc_0_is_not_attached_to_a_class_8022", "JSDoc '@{0}' is not attached to a class."),
  JSDoc_0_1_does_not_match_the_extends_2_clause: diag(8023, 1 /* Error */, "JSDoc_0_1_does_not_match_the_extends_2_clause_8023", "JSDoc '@{0} {1}' does not match the 'extends {2}' clause."),
  JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name: diag(8024, 1 /* Error */, "JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name_8024", "JSDoc '@param' tag has name '{0}', but there is no parameter with that name."),
  Class_declarations_cannot_have_more_than_one_augments_or_extends_tag: diag(8025, 1 /* Error */, "Class_declarations_cannot_have_more_than_one_augments_or_extends_tag_8025", "Class declarations cannot have more than one '@augments' or '@extends' tag."),
  Expected_0_type_arguments_provide_these_with_an_extends_tag: diag(8026, 1 /* Error */, "Expected_0_type_arguments_provide_these_with_an_extends_tag_8026", "Expected {0} type arguments; provide these with an '@extends' tag."),
  Expected_0_1_type_arguments_provide_these_with_an_extends_tag: diag(8027, 1 /* Error */, "Expected_0_1_type_arguments_provide_these_with_an_extends_tag_8027", "Expected {0}-{1} type arguments; provide these with an '@extends' tag."),
  JSDoc_may_only_appear_in_the_last_parameter_of_a_signature: diag(8028, 1 /* Error */, "JSDoc_may_only_appear_in_the_last_parameter_of_a_signature_8028", "JSDoc '...' may only appear in the last parameter of a signature."),
  JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name_It_would_match_arguments_if_it_had_an_array_type: diag(8029, 1 /* Error */, "JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name_It_would_match_arguments_if_it_h_8029", "JSDoc '@param' tag has name '{0}', but there is no parameter with that name. It would match 'arguments' if it had an array type."),
  The_type_of_a_function_declaration_must_match_the_function_s_signature: diag(8030, 1 /* Error */, "The_type_of_a_function_declaration_must_match_the_function_s_signature_8030", "The type of a function declaration must match the function's signature."),
  You_cannot_rename_a_module_via_a_global_import: diag(8031, 1 /* Error */, "You_cannot_rename_a_module_via_a_global_import_8031", "You cannot rename a module via a global import."),
  Qualified_name_0_is_not_allowed_without_a_leading_param_object_1: diag(8032, 1 /* Error */, "Qualified_name_0_is_not_allowed_without_a_leading_param_object_1_8032", "Qualified name '{0}' is not allowed without a leading '@param {object} {1}'."),
  A_JSDoc_typedef_comment_may_not_contain_multiple_type_tags: diag(8033, 1 /* Error */, "A_JSDoc_typedef_comment_may_not_contain_multiple_type_tags_8033", "A JSDoc '@typedef' comment may not contain multiple '@type' tags."),
  The_tag_was_first_specified_here: diag(8034, 1 /* Error */, "The_tag_was_first_specified_here_8034", "The tag was first specified here."),
  You_cannot_rename_elements_that_are_defined_in_a_node_modules_folder: diag(8035, 1 /* Error */, "You_cannot_rename_elements_that_are_defined_in_a_node_modules_folder_8035", "You cannot rename elements that are defined in a 'node_modules' folder."),
  You_cannot_rename_elements_that_are_defined_in_another_node_modules_folder: diag(8036, 1 /* Error */, "You_cannot_rename_elements_that_are_defined_in_another_node_modules_folder_8036", "You cannot rename elements that are defined in another 'node_modules' folder."),
  Type_satisfaction_expressions_can_only_be_used_in_TypeScript_files: diag(8037, 1 /* Error */, "Type_satisfaction_expressions_can_only_be_used_in_TypeScript_files_8037", "Type satisfaction expressions can only be used in TypeScript files."),
  Decorators_may_not_appear_after_export_or_export_default_if_they_also_appear_before_export: diag(8038, 1 /* Error */, "Decorators_may_not_appear_after_export_or_export_default_if_they_also_appear_before_export_8038", "Decorators may not appear after 'export' or 'export default' if they also appear before 'export'."),
  A_JSDoc_template_tag_may_not_follow_a_typedef_callback_or_overload_tag: diag(8039, 1 /* Error */, "A_JSDoc_template_tag_may_not_follow_a_typedef_callback_or_overload_tag_8039", "A JSDoc '@template' tag may not follow a '@typedef', '@callback', or '@overload' tag"),
  Declaration_emit_for_this_file_requires_using_private_name_0_An_explicit_type_annotation_may_unblock_declaration_emit: diag(9005, 1 /* Error */, "Declaration_emit_for_this_file_requires_using_private_name_0_An_explicit_type_annotation_may_unblock_9005", "Declaration emit for this file requires using private name '{0}'. An explicit type annotation may unblock declaration emit."),
  Declaration_emit_for_this_file_requires_using_private_name_0_from_module_1_An_explicit_type_annotation_may_unblock_declaration_emit: diag(9006, 1 /* Error */, "Declaration_emit_for_this_file_requires_using_private_name_0_from_module_1_An_explicit_type_annotati_9006", "Declaration emit for this file requires using private name '{0}' from module '{1}'. An explicit type annotation may unblock declaration emit."),
  Function_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations: diag(9007, 1 /* Error */, "Function_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations_9007", "Function must have an explicit return type annotation with --isolatedDeclarations."),
  Method_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations: diag(9008, 1 /* Error */, "Method_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations_9008", "Method must have an explicit return type annotation with --isolatedDeclarations."),
  At_least_one_accessor_must_have_an_explicit_type_annotation_with_isolatedDeclarations: diag(9009, 1 /* Error */, "At_least_one_accessor_must_have_an_explicit_type_annotation_with_isolatedDeclarations_9009", "At least one accessor must have an explicit type annotation with --isolatedDeclarations."),
  Variable_must_have_an_explicit_type_annotation_with_isolatedDeclarations: diag(9010, 1 /* Error */, "Variable_must_have_an_explicit_type_annotation_with_isolatedDeclarations_9010", "Variable must have an explicit type annotation with --isolatedDeclarations."),
  Parameter_must_have_an_explicit_type_annotation_with_isolatedDeclarations: diag(9011, 1 /* Error */, "Parameter_must_have_an_explicit_type_annotation_with_isolatedDeclarations_9011", "Parameter must have an explicit type annotation with --isolatedDeclarations."),
  Property_must_have_an_explicit_type_annotation_with_isolatedDeclarations: diag(9012, 1 /* Error */, "Property_must_have_an_explicit_type_annotation_with_isolatedDeclarations_9012", "Property must have an explicit type annotation with --isolatedDeclarations."),
  Expression_type_can_t_be_inferred_with_isolatedDeclarations: diag(9013, 1 /* Error */, "Expression_type_can_t_be_inferred_with_isolatedDeclarations_9013", "Expression type can't be inferred with --isolatedDeclarations."),
  Computed_properties_must_be_number_or_string_literals_variables_or_dotted_expressions_with_isolatedDeclarations: diag(9014, 1 /* Error */, "Computed_properties_must_be_number_or_string_literals_variables_or_dotted_expressions_with_isolatedD_9014", "Computed properties must be number or string literals, variables or dotted expressions with --isolatedDeclarations."),
  Objects_that_contain_spread_assignments_can_t_be_inferred_with_isolatedDeclarations: diag(9015, 1 /* Error */, "Objects_that_contain_spread_assignments_can_t_be_inferred_with_isolatedDeclarations_9015", "Objects that contain spread assignments can't be inferred with --isolatedDeclarations."),
  Objects_that_contain_shorthand_properties_can_t_be_inferred_with_isolatedDeclarations: diag(9016, 1 /* Error */, "Objects_that_contain_shorthand_properties_can_t_be_inferred_with_isolatedDeclarations_9016", "Objects that contain shorthand properties can't be inferred with --isolatedDeclarations."),
  Only_const_arrays_can_be_inferred_with_isolatedDeclarations: diag(9017, 1 /* Error */, "Only_const_arrays_can_be_inferred_with_isolatedDeclarations_9017", "Only const arrays can be inferred with --isolatedDeclarations."),
  Arrays_with_spread_elements_can_t_inferred_with_isolatedDeclarations: diag(9018, 1 /* Error */, "Arrays_with_spread_elements_can_t_inferred_with_isolatedDeclarations_9018", "Arrays with spread elements can't inferred with --isolatedDeclarations."),
  Binding_elements_can_t_be_exported_directly_with_isolatedDeclarations: diag(9019, 1 /* Error */, "Binding_elements_can_t_be_exported_directly_with_isolatedDeclarations_9019", "Binding elements can't be exported directly with --isolatedDeclarations."),
  Enum_member_initializers_must_be_computable_without_references_to_external_symbols_with_isolatedDeclarations: diag(9020, 1 /* Error */, "Enum_member_initializers_must_be_computable_without_references_to_external_symbols_with_isolatedDecl_9020", "Enum member initializers must be computable without references to external symbols with --isolatedDeclarations."),
  Extends_clause_can_t_contain_an_expression_with_isolatedDeclarations: diag(9021, 1 /* Error */, "Extends_clause_can_t_contain_an_expression_with_isolatedDeclarations_9021", "Extends clause can't contain an expression with --isolatedDeclarations."),
  Inference_from_class_expressions_is_not_supported_with_isolatedDeclarations: diag(9022, 1 /* Error */, "Inference_from_class_expressions_is_not_supported_with_isolatedDeclarations_9022", "Inference from class expressions is not supported with --isolatedDeclarations."),
  Assigning_properties_to_functions_without_declaring_them_is_not_supported_with_isolatedDeclarations_Add_an_explicit_declaration_for_the_properties_assigned_to_this_function: diag(9023, 1 /* Error */, "Assigning_properties_to_functions_without_declaring_them_is_not_supported_with_isolatedDeclarations__9023", "Assigning properties to functions without declaring them is not supported with --isolatedDeclarations. Add an explicit declaration for the properties assigned to this function."),
  Declaration_emit_for_this_parameter_requires_implicitly_adding_undefined_to_its_type_This_is_not_supported_with_isolatedDeclarations: diag(9025, 1 /* Error */, "Declaration_emit_for_this_parameter_requires_implicitly_adding_undefined_to_its_type_This_is_not_sup_9025", "Declaration emit for this parameter requires implicitly adding undefined to its type. This is not supported with --isolatedDeclarations."),
  Declaration_emit_for_this_file_requires_preserving_this_import_for_augmentations_This_is_not_supported_with_isolatedDeclarations: diag(9026, 1 /* Error */, "Declaration_emit_for_this_file_requires_preserving_this_import_for_augmentations_This_is_not_support_9026", "Declaration emit for this file requires preserving this import for augmentations. This is not supported with --isolatedDeclarations."),
  Add_a_type_annotation_to_the_variable_0: diag(9027, 1 /* Error */, "Add_a_type_annotation_to_the_variable_0_9027", "Add a type annotation to the variable {0}."),
  Add_a_type_annotation_to_the_parameter_0: diag(9028, 1 /* Error */, "Add_a_type_annotation_to_the_parameter_0_9028", "Add a type annotation to the parameter {0}."),
  Add_a_type_annotation_to_the_property_0: diag(9029, 1 /* Error */, "Add_a_type_annotation_to_the_property_0_9029", "Add a type annotation to the property {0}."),
  Add_a_return_type_to_the_function_expression: diag(9030, 1 /* Error */, "Add_a_return_type_to_the_function_expression_9030", "Add a return type to the function expression."),
  Add_a_return_type_to_the_function_declaration: diag(9031, 1 /* Error */, "Add_a_return_type_to_the_function_declaration_9031", "Add a return type to the function declaration."),
  Add_a_return_type_to_the_get_accessor_declaration: diag(9032, 1 /* Error */, "Add_a_return_type_to_the_get_accessor_declaration_9032", "Add a return type to the get accessor declaration."),
  Add_a_type_to_parameter_of_the_set_accessor_declaration: diag(9033, 1 /* Error */, "Add_a_type_to_parameter_of_the_set_accessor_declaration_9033", "Add a type to parameter of the set accessor declaration."),
  Add_a_return_type_to_the_method: diag(9034, 1 /* Error */, "Add_a_return_type_to_the_method_9034", "Add a return type to the method"),
  Add_satisfies_and_a_type_assertion_to_this_expression_satisfies_T_as_T_to_make_the_type_explicit: diag(9035, 1 /* Error */, "Add_satisfies_and_a_type_assertion_to_this_expression_satisfies_T_as_T_to_make_the_type_explicit_9035", "Add satisfies and a type assertion to this expression (satisfies T as T) to make the type explicit."),
  Move_the_expression_in_default_export_to_a_variable_and_add_a_type_annotation_to_it: diag(9036, 1 /* Error */, "Move_the_expression_in_default_export_to_a_variable_and_add_a_type_annotation_to_it_9036", "Move the expression in default export to a variable and add a type annotation to it."),
  Default_exports_can_t_be_inferred_with_isolatedDeclarations: diag(9037, 1 /* Error */, "Default_exports_can_t_be_inferred_with_isolatedDeclarations_9037", "Default exports can't be inferred with --isolatedDeclarations."),
  Computed_property_names_on_class_or_object_literals_cannot_be_inferred_with_isolatedDeclarations: diag(9038, 1 /* Error */, "Computed_property_names_on_class_or_object_literals_cannot_be_inferred_with_isolatedDeclarations_9038", "Computed property names on class or object literals cannot be inferred with --isolatedDeclarations."),
  Type_containing_private_name_0_can_t_be_used_with_isolatedDeclarations: diag(9039, 1 /* Error */, "Type_containing_private_name_0_can_t_be_used_with_isolatedDeclarations_9039", "Type containing private name '{0}' can't be used with --isolatedDeclarations."),
  JSX_attributes_must_only_be_assigned_a_non_empty_expression: diag(17e3, 1 /* Error */, "JSX_attributes_must_only_be_assigned_a_non_empty_expression_17000", "JSX attributes must only be assigned a non-empty 'expression'."),
  JSX_elements_cannot_have_multiple_attributes_with_the_same_name: diag(17001, 1 /* Error */, "JSX_elements_cannot_have_multiple_attributes_with_the_same_name_17001", "JSX elements cannot have multiple attributes with the same name."),
  Expected_corresponding_JSX_closing_tag_for_0: diag(17002, 1 /* Error */, "Expected_corresponding_JSX_closing_tag_for_0_17002", "Expected corresponding JSX closing tag for '{0}'."),
  Cannot_use_JSX_unless_the_jsx_flag_is_provided: diag(17004, 1 /* Error */, "Cannot_use_JSX_unless_the_jsx_flag_is_provided_17004", "Cannot use JSX unless the '--jsx' flag is provided."),
  A_constructor_cannot_contain_a_super_call_when_its_class_extends_null: diag(17005, 1 /* Error */, "A_constructor_cannot_contain_a_super_call_when_its_class_extends_null_17005", "A constructor cannot contain a 'super' call when its class extends 'null'."),
  An_unary_expression_with_the_0_operator_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_expression_Consider_enclosing_the_expression_in_parentheses: diag(17006, 1 /* Error */, "An_unary_expression_with_the_0_operator_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_ex_17006", "An unary expression with the '{0}' operator is not allowed in the left-hand side of an exponentiation expression. Consider enclosing the expression in parentheses."),
  A_type_assertion_expression_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_expression_Consider_enclosing_the_expression_in_parentheses: diag(17007, 1 /* Error */, "A_type_assertion_expression_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_expression_Con_17007", "A type assertion expression is not allowed in the left-hand side of an exponentiation expression. Consider enclosing the expression in parentheses."),
  JSX_element_0_has_no_corresponding_closing_tag: diag(17008, 1 /* Error */, "JSX_element_0_has_no_corresponding_closing_tag_17008", "JSX element '{0}' has no corresponding closing tag."),
  super_must_be_called_before_accessing_this_in_the_constructor_of_a_derived_class: diag(17009, 1 /* Error */, "super_must_be_called_before_accessing_this_in_the_constructor_of_a_derived_class_17009", "'super' must be called before accessing 'this' in the constructor of a derived class."),
  Unknown_type_acquisition_option_0: diag(17010, 1 /* Error */, "Unknown_type_acquisition_option_0_17010", "Unknown type acquisition option '{0}'."),
  super_must_be_called_before_accessing_a_property_of_super_in_the_constructor_of_a_derived_class: diag(17011, 1 /* Error */, "super_must_be_called_before_accessing_a_property_of_super_in_the_constructor_of_a_derived_class_17011", "'super' must be called before accessing a property of 'super' in the constructor of a derived class."),
  _0_is_not_a_valid_meta_property_for_keyword_1_Did_you_mean_2: diag(17012, 1 /* Error */, "_0_is_not_a_valid_meta_property_for_keyword_1_Did_you_mean_2_17012", "'{0}' is not a valid meta-property for keyword '{1}'. Did you mean '{2}'?"),
  Meta_property_0_is_only_allowed_in_the_body_of_a_function_declaration_function_expression_or_constructor: diag(17013, 1 /* Error */, "Meta_property_0_is_only_allowed_in_the_body_of_a_function_declaration_function_expression_or_constru_17013", "Meta-property '{0}' is only allowed in the body of a function declaration, function expression, or constructor."),
  JSX_fragment_has_no_corresponding_closing_tag: diag(17014, 1 /* Error */, "JSX_fragment_has_no_corresponding_closing_tag_17014", "JSX fragment has no corresponding closing tag."),
  Expected_corresponding_closing_tag_for_JSX_fragment: diag(17015, 1 /* Error */, "Expected_corresponding_closing_tag_for_JSX_fragment_17015", "Expected corresponding closing tag for JSX fragment."),
  The_jsxFragmentFactory_compiler_option_must_be_provided_to_use_JSX_fragments_with_the_jsxFactory_compiler_option: diag(17016, 1 /* Error */, "The_jsxFragmentFactory_compiler_option_must_be_provided_to_use_JSX_fragments_with_the_jsxFactory_com_17016", "The 'jsxFragmentFactory' compiler option must be provided to use JSX fragments with the 'jsxFactory' compiler option."),
  An_jsxFrag_pragma_is_required_when_using_an_jsx_pragma_with_JSX_fragments: diag(17017, 1 /* Error */, "An_jsxFrag_pragma_is_required_when_using_an_jsx_pragma_with_JSX_fragments_17017", "An @jsxFrag pragma is required when using an @jsx pragma with JSX fragments."),
  Unknown_type_acquisition_option_0_Did_you_mean_1: diag(17018, 1 /* Error */, "Unknown_type_acquisition_option_0_Did_you_mean_1_17018", "Unknown type acquisition option '{0}'. Did you mean '{1}'?"),
  _0_at_the_end_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1: diag(17019, 1 /* Error */, "_0_at_the_end_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1_17019", "'{0}' at the end of a type is not valid TypeScript syntax. Did you mean to write '{1}'?"),
  _0_at_the_start_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1: diag(17020, 1 /* Error */, "_0_at_the_start_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1_17020", "'{0}' at the start of a type is not valid TypeScript syntax. Did you mean to write '{1}'?"),
  Unicode_escape_sequence_cannot_appear_here: diag(17021, 1 /* Error */, "Unicode_escape_sequence_cannot_appear_here_17021", "Unicode escape sequence cannot appear here."),
  Circularity_detected_while_resolving_configuration_Colon_0: diag(18e3, 1 /* Error */, "Circularity_detected_while_resolving_configuration_Colon_0_18000", "Circularity detected while resolving configuration: {0}"),
  The_files_list_in_config_file_0_is_empty: diag(18002, 1 /* Error */, "The_files_list_in_config_file_0_is_empty_18002", "The 'files' list in config file '{0}' is empty."),
  No_inputs_were_found_in_config_file_0_Specified_include_paths_were_1_and_exclude_paths_were_2: diag(18003, 1 /* Error */, "No_inputs_were_found_in_config_file_0_Specified_include_paths_were_1_and_exclude_paths_were_2_18003", "No inputs were found in config file '{0}'. Specified 'include' paths were '{1}' and 'exclude' paths were '{2}'."),
  File_is_a_CommonJS_module_it_may_be_converted_to_an_ES_module: diag(80001, 2 /* Suggestion */, "File_is_a_CommonJS_module_it_may_be_converted_to_an_ES_module_80001", "File is a CommonJS module; it may be converted to an ES module."),
  This_constructor_function_may_be_converted_to_a_class_declaration: diag(80002, 2 /* Suggestion */, "This_constructor_function_may_be_converted_to_a_class_declaration_80002", "This constructor function may be converted to a class declaration."),
  Import_may_be_converted_to_a_default_import: diag(80003, 2 /* Suggestion */, "Import_may_be_converted_to_a_default_import_80003", "Import may be converted to a default import."),
  JSDoc_types_may_be_moved_to_TypeScript_types: diag(80004, 2 /* Suggestion */, "JSDoc_types_may_be_moved_to_TypeScript_types_80004", "JSDoc types may be moved to TypeScript types."),
  require_call_may_be_converted_to_an_import: diag(80005, 2 /* Suggestion */, "require_call_may_be_converted_to_an_import_80005", "'require' call may be converted to an import."),
  This_may_be_converted_to_an_async_function: diag(80006, 2 /* Suggestion */, "This_may_be_converted_to_an_async_function_80006", "This may be converted to an async function."),
  await_has_no_effect_on_the_type_of_this_expression: diag(80007, 2 /* Suggestion */, "await_has_no_effect_on_the_type_of_this_expression_80007", "'await' has no effect on the type of this expression."),
  Numeric_literals_with_absolute_values_equal_to_2_53_or_greater_are_too_large_to_be_represented_accurately_as_integers: diag(80008, 2 /* Suggestion */, "Numeric_literals_with_absolute_values_equal_to_2_53_or_greater_are_too_large_to_be_represented_accur_80008", "Numeric literals with absolute values equal to 2^53 or greater are too large to be represented accurately as integers."),
  JSDoc_typedef_may_be_converted_to_TypeScript_type: diag(80009, 2 /* Suggestion */, "JSDoc_typedef_may_be_converted_to_TypeScript_type_80009", "JSDoc typedef may be converted to TypeScript type."),
  JSDoc_typedefs_may_be_converted_to_TypeScript_types: diag(80010, 2 /* Suggestion */, "JSDoc_typedefs_may_be_converted_to_TypeScript_types_80010", "JSDoc typedefs may be converted to TypeScript types."),
  Add_missing_super_call: diag(90001, 3 /* Message */, "Add_missing_super_call_90001", "Add missing 'super()' call"),
  Make_super_call_the_first_statement_in_the_constructor: diag(90002, 3 /* Message */, "Make_super_call_the_first_statement_in_the_constructor_90002", "Make 'super()' call the first statement in the constructor"),
  Change_extends_to_implements: diag(90003, 3 /* Message */, "Change_extends_to_implements_90003", "Change 'extends' to 'implements'"),
  Remove_unused_declaration_for_Colon_0: diag(90004, 3 /* Message */, "Remove_unused_declaration_for_Colon_0_90004", "Remove unused declaration for: '{0}'"),
  Remove_import_from_0: diag(90005, 3 /* Message */, "Remove_import_from_0_90005", "Remove import from '{0}'"),
  Implement_interface_0: diag(90006, 3 /* Message */, "Implement_interface_0_90006", "Implement interface '{0}'"),
  Implement_inherited_abstract_class: diag(90007, 3 /* Message */, "Implement_inherited_abstract_class_90007", "Implement inherited abstract class"),
  Add_0_to_unresolved_variable: diag(90008, 3 /* Message */, "Add_0_to_unresolved_variable_90008", "Add '{0}.' to unresolved variable"),
  Remove_variable_statement: diag(90010, 3 /* Message */, "Remove_variable_statement_90010", "Remove variable statement"),
  Remove_template_tag: diag(90011, 3 /* Message */, "Remove_template_tag_90011", "Remove template tag"),
  Remove_type_parameters: diag(90012, 3 /* Message */, "Remove_type_parameters_90012", "Remove type parameters"),
  Import_0_from_1: diag(90013, 3 /* Message */, "Import_0_from_1_90013", `Import '{0}' from "{1}"`),
  Change_0_to_1: diag(90014, 3 /* Message */, "Change_0_to_1_90014", "Change '{0}' to '{1}'"),
  Declare_property_0: diag(90016, 3 /* Message */, "Declare_property_0_90016", "Declare property '{0}'"),
  Add_index_signature_for_property_0: diag(90017, 3 /* Message */, "Add_index_signature_for_property_0_90017", "Add index signature for property '{0}'"),
  Disable_checking_for_this_file: diag(90018, 3 /* Message */, "Disable_checking_for_this_file_90018", "Disable checking for this file"),
  Ignore_this_error_message: diag(90019, 3 /* Message */, "Ignore_this_error_message_90019", "Ignore this error message"),
  Initialize_property_0_in_the_constructor: diag(90020, 3 /* Message */, "Initialize_property_0_in_the_constructor_90020", "Initialize property '{0}' in the constructor"),
  Initialize_static_property_0: diag(90021, 3 /* Message */, "Initialize_static_property_0_90021", "Initialize static property '{0}'"),
  Change_spelling_to_0: diag(90022, 3 /* Message */, "Change_spelling_to_0_90022", "Change spelling to '{0}'"),
  Declare_method_0: diag(90023, 3 /* Message */, "Declare_method_0_90023", "Declare method '{0}'"),
  Declare_static_method_0: diag(90024, 3 /* Message */, "Declare_static_method_0_90024", "Declare static method '{0}'"),
  Prefix_0_with_an_underscore: diag(90025, 3 /* Message */, "Prefix_0_with_an_underscore_90025", "Prefix '{0}' with an underscore"),
  Rewrite_as_the_indexed_access_type_0: diag(90026, 3 /* Message */, "Rewrite_as_the_indexed_access_type_0_90026", "Rewrite as the indexed access type '{0}'"),
  Declare_static_property_0: diag(90027, 3 /* Message */, "Declare_static_property_0_90027", "Declare static property '{0}'"),
  Call_decorator_expression: diag(90028, 3 /* Message */, "Call_decorator_expression_90028", "Call decorator expression"),
  Add_async_modifier_to_containing_function: diag(90029, 3 /* Message */, "Add_async_modifier_to_containing_function_90029", "Add async modifier to containing function"),
  Replace_infer_0_with_unknown: diag(90030, 3 /* Message */, "Replace_infer_0_with_unknown_90030", "Replace 'infer {0}' with 'unknown'"),
  Replace_all_unused_infer_with_unknown: diag(90031, 3 /* Message */, "Replace_all_unused_infer_with_unknown_90031", "Replace all unused 'infer' with 'unknown'"),
  Add_parameter_name: diag(90034, 3 /* Message */, "Add_parameter_name_90034", "Add parameter name"),
  Declare_private_property_0: diag(90035, 3 /* Message */, "Declare_private_property_0_90035", "Declare private property '{0}'"),
  Replace_0_with_Promise_1: diag(90036, 3 /* Message */, "Replace_0_with_Promise_1_90036", "Replace '{0}' with 'Promise<{1}>'"),
  Fix_all_incorrect_return_type_of_an_async_functions: diag(90037, 3 /* Message */, "Fix_all_incorrect_return_type_of_an_async_functions_90037", "Fix all incorrect return type of an async functions"),
  Declare_private_method_0: diag(90038, 3 /* Message */, "Declare_private_method_0_90038", "Declare private method '{0}'"),
  Remove_unused_destructuring_declaration: diag(90039, 3 /* Message */, "Remove_unused_destructuring_declaration_90039", "Remove unused destructuring declaration"),
  Remove_unused_declarations_for_Colon_0: diag(90041, 3 /* Message */, "Remove_unused_declarations_for_Colon_0_90041", "Remove unused declarations for: '{0}'"),
  Declare_a_private_field_named_0: diag(90053, 3 /* Message */, "Declare_a_private_field_named_0_90053", "Declare a private field named '{0}'."),
  Includes_imports_of_types_referenced_by_0: diag(90054, 3 /* Message */, "Includes_imports_of_types_referenced_by_0_90054", "Includes imports of types referenced by '{0}'"),
  Remove_type_from_import_declaration_from_0: diag(90055, 3 /* Message */, "Remove_type_from_import_declaration_from_0_90055", `Remove 'type' from import declaration from "{0}"`),
  Remove_type_from_import_of_0_from_1: diag(90056, 3 /* Message */, "Remove_type_from_import_of_0_from_1_90056", `Remove 'type' from import of '{0}' from "{1}"`),
  Add_import_from_0: diag(90057, 3 /* Message */, "Add_import_from_0_90057", 'Add import from "{0}"'),
  Update_import_from_0: diag(90058, 3 /* Message */, "Update_import_from_0_90058", 'Update import from "{0}"'),
  Export_0_from_module_1: diag(90059, 3 /* Message */, "Export_0_from_module_1_90059", "Export '{0}' from module '{1}'"),
  Export_all_referenced_locals: diag(90060, 3 /* Message */, "Export_all_referenced_locals_90060", "Export all referenced locals"),
  Update_modifiers_of_0: diag(90061, 3 /* Message */, "Update_modifiers_of_0_90061", "Update modifiers of '{0}'"),
  Add_annotation_of_type_0: diag(90062, 3 /* Message */, "Add_annotation_of_type_0_90062", "Add annotation of type '{0}'"),
  Add_return_type_0: diag(90063, 3 /* Message */, "Add_return_type_0_90063", "Add return type '{0}'"),
  Extract_base_class_to_variable: diag(90064, 3 /* Message */, "Extract_base_class_to_variable_90064", "Extract base class to variable"),
  Extract_default_export_to_variable: diag(90065, 3 /* Message */, "Extract_default_export_to_variable_90065", "Extract default export to variable"),
  Extract_binding_expressions_to_variable: diag(90066, 3 /* Message */, "Extract_binding_expressions_to_variable_90066", "Extract binding expressions to variable"),
  Add_all_missing_type_annotations: diag(90067, 3 /* Message */, "Add_all_missing_type_annotations_90067", "Add all missing type annotations"),
  Add_satisfies_and_an_inline_type_assertion_with_0: diag(90068, 3 /* Message */, "Add_satisfies_and_an_inline_type_assertion_with_0_90068", "Add satisfies and an inline type assertion with '{0}'"),
  Extract_to_variable_and_replace_with_0_as_typeof_0: diag(90069, 3 /* Message */, "Extract_to_variable_and_replace_with_0_as_typeof_0_90069", "Extract to variable and replace with '{0} as typeof {0}'"),
  Mark_array_literal_as_const: diag(90070, 3 /* Message */, "Mark_array_literal_as_const_90070", "Mark array literal as const"),
  Annotate_types_of_properties_expando_function_in_a_namespace: diag(90071, 3 /* Message */, "Annotate_types_of_properties_expando_function_in_a_namespace_90071", "Annotate types of properties expando function in a namespace"),
  Convert_function_to_an_ES2015_class: diag(95001, 3 /* Message */, "Convert_function_to_an_ES2015_class_95001", "Convert function to an ES2015 class"),
  Convert_0_to_1_in_0: diag(95003, 3 /* Message */, "Convert_0_to_1_in_0_95003", "Convert '{0}' to '{1} in {0}'"),
  Extract_to_0_in_1: diag(95004, 3 /* Message */, "Extract_to_0_in_1_95004", "Extract to {0} in {1}"),
  Extract_function: diag(95005, 3 /* Message */, "Extract_function_95005", "Extract function"),
  Extract_constant: diag(95006, 3 /* Message */, "Extract_constant_95006", "Extract constant"),
  Extract_to_0_in_enclosing_scope: diag(95007, 3 /* Message */, "Extract_to_0_in_enclosing_scope_95007", "Extract to {0} in enclosing scope"),
  Extract_to_0_in_1_scope: diag(95008, 3 /* Message */, "Extract_to_0_in_1_scope_95008", "Extract to {0} in {1} scope"),
  Annotate_with_type_from_JSDoc: diag(95009, 3 /* Message */, "Annotate_with_type_from_JSDoc_95009", "Annotate with type from JSDoc"),
  Infer_type_of_0_from_usage: diag(95011, 3 /* Message */, "Infer_type_of_0_from_usage_95011", "Infer type of '{0}' from usage"),
  Infer_parameter_types_from_usage: diag(95012, 3 /* Message */, "Infer_parameter_types_from_usage_95012", "Infer parameter types from usage"),
  Convert_to_default_import: diag(95013, 3 /* Message */, "Convert_to_default_import_95013", "Convert to default import"),
  Install_0: diag(95014, 3 /* Message */, "Install_0_95014", "Install '{0}'"),
  Replace_import_with_0: diag(95015, 3 /* Message */, "Replace_import_with_0_95015", "Replace import with '{0}'."),
  Use_synthetic_default_member: diag(95016, 3 /* Message */, "Use_synthetic_default_member_95016", "Use synthetic 'default' member."),
  Convert_to_ES_module: diag(95017, 3 /* Message */, "Convert_to_ES_module_95017", "Convert to ES module"),
  Add_undefined_type_to_property_0: diag(95018, 3 /* Message */, "Add_undefined_type_to_property_0_95018", "Add 'undefined' type to property '{0}'"),
  Add_initializer_to_property_0: diag(95019, 3 /* Message */, "Add_initializer_to_property_0_95019", "Add initializer to property '{0}'"),
  Add_definite_assignment_assertion_to_property_0: diag(95020, 3 /* Message */, "Add_definite_assignment_assertion_to_property_0_95020", "Add definite assignment assertion to property '{0}'"),
  Convert_all_type_literals_to_mapped_type: diag(95021, 3 /* Message */, "Convert_all_type_literals_to_mapped_type_95021", "Convert all type literals to mapped type"),
  Add_all_missing_members: diag(95022, 3 /* Message */, "Add_all_missing_members_95022", "Add all missing members"),
  Infer_all_types_from_usage: diag(95023, 3 /* Message */, "Infer_all_types_from_usage_95023", "Infer all types from usage"),
  Delete_all_unused_declarations: diag(95024, 3 /* Message */, "Delete_all_unused_declarations_95024", "Delete all unused declarations"),
  Prefix_all_unused_declarations_with_where_possible: diag(95025, 3 /* Message */, "Prefix_all_unused_declarations_with_where_possible_95025", "Prefix all unused declarations with '_' where possible"),
  Fix_all_detected_spelling_errors: diag(95026, 3 /* Message */, "Fix_all_detected_spelling_errors_95026", "Fix all detected spelling errors"),
  Add_initializers_to_all_uninitialized_properties: diag(95027, 3 /* Message */, "Add_initializers_to_all_uninitialized_properties_95027", "Add initializers to all uninitialized properties"),
  Add_definite_assignment_assertions_to_all_uninitialized_properties: diag(95028, 3 /* Message */, "Add_definite_assignment_assertions_to_all_uninitialized_properties_95028", "Add definite assignment assertions to all uninitialized properties"),
  Add_undefined_type_to_all_uninitialized_properties: diag(95029, 3 /* Message */, "Add_undefined_type_to_all_uninitialized_properties_95029", "Add undefined type to all uninitialized properties"),
  Change_all_jsdoc_style_types_to_TypeScript: diag(95030, 3 /* Message */, "Change_all_jsdoc_style_types_to_TypeScript_95030", "Change all jsdoc-style types to TypeScript"),
  Change_all_jsdoc_style_types_to_TypeScript_and_add_undefined_to_nullable_types: diag(95031, 3 /* Message */, "Change_all_jsdoc_style_types_to_TypeScript_and_add_undefined_to_nullable_types_95031", "Change all jsdoc-style types to TypeScript (and add '| undefined' to nullable types)"),
  Implement_all_unimplemented_interfaces: diag(95032, 3 /* Message */, "Implement_all_unimplemented_interfaces_95032", "Implement all unimplemented interfaces"),
  Install_all_missing_types_packages: diag(95033, 3 /* Message */, "Install_all_missing_types_packages_95033", "Install all missing types packages"),
  Rewrite_all_as_indexed_access_types: diag(95034, 3 /* Message */, "Rewrite_all_as_indexed_access_types_95034", "Rewrite all as indexed access types"),
  Convert_all_to_default_imports: diag(95035, 3 /* Message */, "Convert_all_to_default_imports_95035", "Convert all to default imports"),
  Make_all_super_calls_the_first_statement_in_their_constructor: diag(95036, 3 /* Message */, "Make_all_super_calls_the_first_statement_in_their_constructor_95036", "Make all 'super()' calls the first statement in their constructor"),
  Add_qualifier_to_all_unresolved_variables_matching_a_member_name: diag(95037, 3 /* Message */, "Add_qualifier_to_all_unresolved_variables_matching_a_member_name_95037", "Add qualifier to all unresolved variables matching a member name"),
  Change_all_extended_interfaces_to_implements: diag(95038, 3 /* Message */, "Change_all_extended_interfaces_to_implements_95038", "Change all extended interfaces to 'implements'"),
  Add_all_missing_super_calls: diag(95039, 3 /* Message */, "Add_all_missing_super_calls_95039", "Add all missing super calls"),
  Implement_all_inherited_abstract_classes: diag(95040, 3 /* Message */, "Implement_all_inherited_abstract_classes_95040", "Implement all inherited abstract classes"),
  Add_all_missing_async_modifiers: diag(95041, 3 /* Message */, "Add_all_missing_async_modifiers_95041", "Add all missing 'async' modifiers"),
  Add_ts_ignore_to_all_error_messages: diag(95042, 3 /* Message */, "Add_ts_ignore_to_all_error_messages_95042", "Add '@ts-ignore' to all error messages"),
  Annotate_everything_with_types_from_JSDoc: diag(95043, 3 /* Message */, "Annotate_everything_with_types_from_JSDoc_95043", "Annotate everything with types from JSDoc"),
  Add_to_all_uncalled_decorators: diag(95044, 3 /* Message */, "Add_to_all_uncalled_decorators_95044", "Add '()' to all uncalled decorators"),
  Convert_all_constructor_functions_to_classes: diag(95045, 3 /* Message */, "Convert_all_constructor_functions_to_classes_95045", "Convert all constructor functions to classes"),
  Generate_get_and_set_accessors: diag(95046, 3 /* Message */, "Generate_get_and_set_accessors_95046", "Generate 'get' and 'set' accessors"),
  Convert_require_to_import: diag(95047, 3 /* Message */, "Convert_require_to_import_95047", "Convert 'require' to 'import'"),
  Convert_all_require_to_import: diag(95048, 3 /* Message */, "Convert_all_require_to_import_95048", "Convert all 'require' to 'import'"),
  Move_to_a_new_file: diag(95049, 3 /* Message */, "Move_to_a_new_file_95049", "Move to a new file"),
  Remove_unreachable_code: diag(95050, 3 /* Message */, "Remove_unreachable_code_95050", "Remove unreachable code"),
  Remove_all_unreachable_code: diag(95051, 3 /* Message */, "Remove_all_unreachable_code_95051", "Remove all unreachable code"),
  Add_missing_typeof: diag(95052, 3 /* Message */, "Add_missing_typeof_95052", "Add missing 'typeof'"),
  Remove_unused_label: diag(95053, 3 /* Message */, "Remove_unused_label_95053", "Remove unused label"),
  Remove_all_unused_labels: diag(95054, 3 /* Message */, "Remove_all_unused_labels_95054", "Remove all unused labels"),
  Convert_0_to_mapped_object_type: diag(95055, 3 /* Message */, "Convert_0_to_mapped_object_type_95055", "Convert '{0}' to mapped object type"),
  Convert_namespace_import_to_named_imports: diag(95056, 3 /* Message */, "Convert_namespace_import_to_named_imports_95056", "Convert namespace import to named imports"),
  Convert_named_imports_to_namespace_import: diag(95057, 3 /* Message */, "Convert_named_imports_to_namespace_import_95057", "Convert named imports to namespace import"),
  Add_or_remove_braces_in_an_arrow_function: diag(95058, 3 /* Message */, "Add_or_remove_braces_in_an_arrow_function_95058", "Add or remove braces in an arrow function"),
  Add_braces_to_arrow_function: diag(95059, 3 /* Message */, "Add_braces_to_arrow_function_95059", "Add braces to arrow function"),
  Remove_braces_from_arrow_function: diag(95060, 3 /* Message */, "Remove_braces_from_arrow_function_95060", "Remove braces from arrow function"),
  Convert_default_export_to_named_export: diag(95061, 3 /* Message */, "Convert_default_export_to_named_export_95061", "Convert default export to named export"),
  Convert_named_export_to_default_export: diag(95062, 3 /* Message */, "Convert_named_export_to_default_export_95062", "Convert named export to default export"),
  Add_missing_enum_member_0: diag(95063, 3 /* Message */, "Add_missing_enum_member_0_95063", "Add missing enum member '{0}'"),
  Add_all_missing_imports: diag(95064, 3 /* Message */, "Add_all_missing_imports_95064", "Add all missing imports"),
  Convert_to_async_function: diag(95065, 3 /* Message */, "Convert_to_async_function_95065", "Convert to async function"),
  Convert_all_to_async_functions: diag(95066, 3 /* Message */, "Convert_all_to_async_functions_95066", "Convert all to async functions"),
  Add_missing_call_parentheses: diag(95067, 3 /* Message */, "Add_missing_call_parentheses_95067", "Add missing call parentheses"),
  Add_all_missing_call_parentheses: diag(95068, 3 /* Message */, "Add_all_missing_call_parentheses_95068", "Add all missing call parentheses"),
  Add_unknown_conversion_for_non_overlapping_types: diag(95069, 3 /* Message */, "Add_unknown_conversion_for_non_overlapping_types_95069", "Add 'unknown' conversion for non-overlapping types"),
  Add_unknown_to_all_conversions_of_non_overlapping_types: diag(95070, 3 /* Message */, "Add_unknown_to_all_conversions_of_non_overlapping_types_95070", "Add 'unknown' to all conversions of non-overlapping types"),
  Add_missing_new_operator_to_call: diag(95071, 3 /* Message */, "Add_missing_new_operator_to_call_95071", "Add missing 'new' operator to call"),
  Add_missing_new_operator_to_all_calls: diag(95072, 3 /* Message */, "Add_missing_new_operator_to_all_calls_95072", "Add missing 'new' operator to all calls"),
  Add_names_to_all_parameters_without_names: diag(95073, 3 /* Message */, "Add_names_to_all_parameters_without_names_95073", "Add names to all parameters without names"),
  Enable_the_experimentalDecorators_option_in_your_configuration_file: diag(95074, 3 /* Message */, "Enable_the_experimentalDecorators_option_in_your_configuration_file_95074", "Enable the 'experimentalDecorators' option in your configuration file"),
  Convert_parameters_to_destructured_object: diag(95075, 3 /* Message */, "Convert_parameters_to_destructured_object_95075", "Convert parameters to destructured object"),
  Extract_type: diag(95077, 3 /* Message */, "Extract_type_95077", "Extract type"),
  Extract_to_type_alias: diag(95078, 3 /* Message */, "Extract_to_type_alias_95078", "Extract to type alias"),
  Extract_to_typedef: diag(95079, 3 /* Message */, "Extract_to_typedef_95079", "Extract to typedef"),
  Infer_this_type_of_0_from_usage: diag(95080, 3 /* Message */, "Infer_this_type_of_0_from_usage_95080", "Infer 'this' type of '{0}' from usage"),
  Add_const_to_unresolved_variable: diag(95081, 3 /* Message */, "Add_const_to_unresolved_variable_95081", "Add 'const' to unresolved variable"),
  Add_const_to_all_unresolved_variables: diag(95082, 3 /* Message */, "Add_const_to_all_unresolved_variables_95082", "Add 'const' to all unresolved variables"),
  Add_await: diag(95083, 3 /* Message */, "Add_await_95083", "Add 'await'"),
  Add_await_to_initializer_for_0: diag(95084, 3 /* Message */, "Add_await_to_initializer_for_0_95084", "Add 'await' to initializer for '{0}'"),
  Fix_all_expressions_possibly_missing_await: diag(95085, 3 /* Message */, "Fix_all_expressions_possibly_missing_await_95085", "Fix all expressions possibly missing 'await'"),
  Remove_unnecessary_await: diag(95086, 3 /* Message */, "Remove_unnecessary_await_95086", "Remove unnecessary 'await'"),
  Remove_all_unnecessary_uses_of_await: diag(95087, 3 /* Message */, "Remove_all_unnecessary_uses_of_await_95087", "Remove all unnecessary uses of 'await'"),
  Enable_the_jsx_flag_in_your_configuration_file: diag(95088, 3 /* Message */, "Enable_the_jsx_flag_in_your_configuration_file_95088", "Enable the '--jsx' flag in your configuration file"),
  Add_await_to_initializers: diag(95089, 3 /* Message */, "Add_await_to_initializers_95089", "Add 'await' to initializers"),
  Extract_to_interface: diag(95090, 3 /* Message */, "Extract_to_interface_95090", "Extract to interface"),
  Convert_to_a_bigint_numeric_literal: diag(95091, 3 /* Message */, "Convert_to_a_bigint_numeric_literal_95091", "Convert to a bigint numeric literal"),
  Convert_all_to_bigint_numeric_literals: diag(95092, 3 /* Message */, "Convert_all_to_bigint_numeric_literals_95092", "Convert all to bigint numeric literals"),
  Convert_const_to_let: diag(95093, 3 /* Message */, "Convert_const_to_let_95093", "Convert 'const' to 'let'"),
  Prefix_with_declare: diag(95094, 3 /* Message */, "Prefix_with_declare_95094", "Prefix with 'declare'"),
  Prefix_all_incorrect_property_declarations_with_declare: diag(95095, 3 /* Message */, "Prefix_all_incorrect_property_declarations_with_declare_95095", "Prefix all incorrect property declarations with 'declare'"),
  Convert_to_template_string: diag(95096, 3 /* Message */, "Convert_to_template_string_95096", "Convert to template string"),
  Add_export_to_make_this_file_into_a_module: diag(95097, 3 /* Message */, "Add_export_to_make_this_file_into_a_module_95097", "Add 'export {}' to make this file into a module"),
  Set_the_target_option_in_your_configuration_file_to_0: diag(95098, 3 /* Message */, "Set_the_target_option_in_your_configuration_file_to_0_95098", "Set the 'target' option in your configuration file to '{0}'"),
  Set_the_module_option_in_your_configuration_file_to_0: diag(95099, 3 /* Message */, "Set_the_module_option_in_your_configuration_file_to_0_95099", "Set the 'module' option in your configuration file to '{0}'"),
  Convert_invalid_character_to_its_html_entity_code: diag(95100, 3 /* Message */, "Convert_invalid_character_to_its_html_entity_code_95100", "Convert invalid character to its html entity code"),
  Convert_all_invalid_characters_to_HTML_entity_code: diag(95101, 3 /* Message */, "Convert_all_invalid_characters_to_HTML_entity_code_95101", "Convert all invalid characters to HTML entity code"),
  Convert_all_const_to_let: diag(95102, 3 /* Message */, "Convert_all_const_to_let_95102", "Convert all 'const' to 'let'"),
  Convert_function_expression_0_to_arrow_function: diag(95105, 3 /* Message */, "Convert_function_expression_0_to_arrow_function_95105", "Convert function expression '{0}' to arrow function"),
  Convert_function_declaration_0_to_arrow_function: diag(95106, 3 /* Message */, "Convert_function_declaration_0_to_arrow_function_95106", "Convert function declaration '{0}' to arrow function"),
  Fix_all_implicit_this_errors: diag(95107, 3 /* Message */, "Fix_all_implicit_this_errors_95107", "Fix all implicit-'this' errors"),
  Wrap_invalid_character_in_an_expression_container: diag(95108, 3 /* Message */, "Wrap_invalid_character_in_an_expression_container_95108", "Wrap invalid character in an expression container"),
  Wrap_all_invalid_characters_in_an_expression_container: diag(95109, 3 /* Message */, "Wrap_all_invalid_characters_in_an_expression_container_95109", "Wrap all invalid characters in an expression container"),
  Visit_https_Colon_Slash_Slashaka_ms_Slashtsconfig_to_read_more_about_this_file: diag(95110, 3 /* Message */, "Visit_https_Colon_Slash_Slashaka_ms_Slashtsconfig_to_read_more_about_this_file_95110", "Visit https://aka.ms/tsconfig to read more about this file"),
  Add_a_return_statement: diag(95111, 3 /* Message */, "Add_a_return_statement_95111", "Add a return statement"),
  Remove_braces_from_arrow_function_body: diag(95112, 3 /* Message */, "Remove_braces_from_arrow_function_body_95112", "Remove braces from arrow function body"),
  Wrap_the_following_body_with_parentheses_which_should_be_an_object_literal: diag(95113, 3 /* Message */, "Wrap_the_following_body_with_parentheses_which_should_be_an_object_literal_95113", "Wrap the following body with parentheses which should be an object literal"),
  Add_all_missing_return_statement: diag(95114, 3 /* Message */, "Add_all_missing_return_statement_95114", "Add all missing return statement"),
  Remove_braces_from_all_arrow_function_bodies_with_relevant_issues: diag(95115, 3 /* Message */, "Remove_braces_from_all_arrow_function_bodies_with_relevant_issues_95115", "Remove braces from all arrow function bodies with relevant issues"),
  Wrap_all_object_literal_with_parentheses: diag(95116, 3 /* Message */, "Wrap_all_object_literal_with_parentheses_95116", "Wrap all object literal with parentheses"),
  Move_labeled_tuple_element_modifiers_to_labels: diag(95117, 3 /* Message */, "Move_labeled_tuple_element_modifiers_to_labels_95117", "Move labeled tuple element modifiers to labels"),
  Convert_overload_list_to_single_signature: diag(95118, 3 /* Message */, "Convert_overload_list_to_single_signature_95118", "Convert overload list to single signature"),
  Generate_get_and_set_accessors_for_all_overriding_properties: diag(95119, 3 /* Message */, "Generate_get_and_set_accessors_for_all_overriding_properties_95119", "Generate 'get' and 'set' accessors for all overriding properties"),
  Wrap_in_JSX_fragment: diag(95120, 3 /* Message */, "Wrap_in_JSX_fragment_95120", "Wrap in JSX fragment"),
  Wrap_all_unparented_JSX_in_JSX_fragment: diag(95121, 3 /* Message */, "Wrap_all_unparented_JSX_in_JSX_fragment_95121", "Wrap all unparented JSX in JSX fragment"),
  Convert_arrow_function_or_function_expression: diag(95122, 3 /* Message */, "Convert_arrow_function_or_function_expression_95122", "Convert arrow function or function expression"),
  Convert_to_anonymous_function: diag(95123, 3 /* Message */, "Convert_to_anonymous_function_95123", "Convert to anonymous function"),
  Convert_to_named_function: diag(95124, 3 /* Message */, "Convert_to_named_function_95124", "Convert to named function"),
  Convert_to_arrow_function: diag(95125, 3 /* Message */, "Convert_to_arrow_function_95125", "Convert to arrow function"),
  Remove_parentheses: diag(95126, 3 /* Message */, "Remove_parentheses_95126", "Remove parentheses"),
  Could_not_find_a_containing_arrow_function: diag(95127, 3 /* Message */, "Could_not_find_a_containing_arrow_function_95127", "Could not find a containing arrow function"),
  Containing_function_is_not_an_arrow_function: diag(95128, 3 /* Message */, "Containing_function_is_not_an_arrow_function_95128", "Containing function is not an arrow function"),
  Could_not_find_export_statement: diag(95129, 3 /* Message */, "Could_not_find_export_statement_95129", "Could not find export statement"),
  This_file_already_has_a_default_export: diag(95130, 3 /* Message */, "This_file_already_has_a_default_export_95130", "This file already has a default export"),
  Could_not_find_import_clause: diag(95131, 3 /* Message */, "Could_not_find_import_clause_95131", "Could not find import clause"),
  Could_not_find_namespace_import_or_named_imports: diag(95132, 3 /* Message */, "Could_not_find_namespace_import_or_named_imports_95132", "Could not find namespace import or named imports"),
  Selection_is_not_a_valid_type_node: diag(95133, 3 /* Message */, "Selection_is_not_a_valid_type_node_95133", "Selection is not a valid type node"),
  No_type_could_be_extracted_from_this_type_node: diag(95134, 3 /* Message */, "No_type_could_be_extracted_from_this_type_node_95134", "No type could be extracted from this type node"),
  Could_not_find_property_for_which_to_generate_accessor: diag(95135, 3 /* Message */, "Could_not_find_property_for_which_to_generate_accessor_95135", "Could not find property for which to generate accessor"),
  Name_is_not_valid: diag(95136, 3 /* Message */, "Name_is_not_valid_95136", "Name is not valid"),
  Can_only_convert_property_with_modifier: diag(95137, 3 /* Message */, "Can_only_convert_property_with_modifier_95137", "Can only convert property with modifier"),
  Switch_each_misused_0_to_1: diag(95138, 3 /* Message */, "Switch_each_misused_0_to_1_95138", "Switch each misused '{0}' to '{1}'"),
  Convert_to_optional_chain_expression: diag(95139, 3 /* Message */, "Convert_to_optional_chain_expression_95139", "Convert to optional chain expression"),
  Could_not_find_convertible_access_expression: diag(95140, 3 /* Message */, "Could_not_find_convertible_access_expression_95140", "Could not find convertible access expression"),
  Could_not_find_matching_access_expressions: diag(95141, 3 /* Message */, "Could_not_find_matching_access_expressions_95141", "Could not find matching access expressions"),
  Can_only_convert_logical_AND_access_chains: diag(95142, 3 /* Message */, "Can_only_convert_logical_AND_access_chains_95142", "Can only convert logical AND access chains"),
  Add_void_to_Promise_resolved_without_a_value: diag(95143, 3 /* Message */, "Add_void_to_Promise_resolved_without_a_value_95143", "Add 'void' to Promise resolved without a value"),
  Add_void_to_all_Promises_resolved_without_a_value: diag(95144, 3 /* Message */, "Add_void_to_all_Promises_resolved_without_a_value_95144", "Add 'void' to all Promises resolved without a value"),
  Use_element_access_for_0: diag(95145, 3 /* Message */, "Use_element_access_for_0_95145", "Use element access for '{0}'"),
  Use_element_access_for_all_undeclared_properties: diag(95146, 3 /* Message */, "Use_element_access_for_all_undeclared_properties_95146", "Use element access for all undeclared properties."),
  Delete_all_unused_imports: diag(95147, 3 /* Message */, "Delete_all_unused_imports_95147", "Delete all unused imports"),
  Infer_function_return_type: diag(95148, 3 /* Message */, "Infer_function_return_type_95148", "Infer function return type"),
  Return_type_must_be_inferred_from_a_function: diag(95149, 3 /* Message */, "Return_type_must_be_inferred_from_a_function_95149", "Return type must be inferred from a function"),
  Could_not_determine_function_return_type: diag(95150, 3 /* Message */, "Could_not_determine_function_return_type_95150", "Could not determine function return type"),
  Could_not_convert_to_arrow_function: diag(95151, 3 /* Message */, "Could_not_convert_to_arrow_function_95151", "Could not convert to arrow function"),
  Could_not_convert_to_named_function: diag(95152, 3 /* Message */, "Could_not_convert_to_named_function_95152", "Could not convert to named function"),
  Could_not_convert_to_anonymous_function: diag(95153, 3 /* Message */, "Could_not_convert_to_anonymous_function_95153", "Could not convert to anonymous function"),
  Can_only_convert_string_concatenations_and_string_literals: diag(95154, 3 /* Message */, "Can_only_convert_string_concatenations_and_string_literals_95154", "Can only convert string concatenations and string literals"),
  Selection_is_not_a_valid_statement_or_statements: diag(95155, 3 /* Message */, "Selection_is_not_a_valid_statement_or_statements_95155", "Selection is not a valid statement or statements"),
  Add_missing_function_declaration_0: diag(95156, 3 /* Message */, "Add_missing_function_declaration_0_95156", "Add missing function declaration '{0}'"),
  Add_all_missing_function_declarations: diag(95157, 3 /* Message */, "Add_all_missing_function_declarations_95157", "Add all missing function declarations"),
  Method_not_implemented: diag(95158, 3 /* Message */, "Method_not_implemented_95158", "Method not implemented."),
  Function_not_implemented: diag(95159, 3 /* Message */, "Function_not_implemented_95159", "Function not implemented."),
  Add_override_modifier: diag(95160, 3 /* Message */, "Add_override_modifier_95160", "Add 'override' modifier"),
  Remove_override_modifier: diag(95161, 3 /* Message */, "Remove_override_modifier_95161", "Remove 'override' modifier"),
  Add_all_missing_override_modifiers: diag(95162, 3 /* Message */, "Add_all_missing_override_modifiers_95162", "Add all missing 'override' modifiers"),
  Remove_all_unnecessary_override_modifiers: diag(95163, 3 /* Message */, "Remove_all_unnecessary_override_modifiers_95163", "Remove all unnecessary 'override' modifiers"),
  Can_only_convert_named_export: diag(95164, 3 /* Message */, "Can_only_convert_named_export_95164", "Can only convert named export"),
  Add_missing_properties: diag(95165, 3 /* Message */, "Add_missing_properties_95165", "Add missing properties"),
  Add_all_missing_properties: diag(95166, 3 /* Message */, "Add_all_missing_properties_95166", "Add all missing properties"),
  Add_missing_attributes: diag(95167, 3 /* Message */, "Add_missing_attributes_95167", "Add missing attributes"),
  Add_all_missing_attributes: diag(95168, 3 /* Message */, "Add_all_missing_attributes_95168", "Add all missing attributes"),
  Add_undefined_to_optional_property_type: diag(95169, 3 /* Message */, "Add_undefined_to_optional_property_type_95169", "Add 'undefined' to optional property type"),
  Convert_named_imports_to_default_import: diag(95170, 3 /* Message */, "Convert_named_imports_to_default_import_95170", "Convert named imports to default import"),
  Delete_unused_param_tag_0: diag(95171, 3 /* Message */, "Delete_unused_param_tag_0_95171", "Delete unused '@param' tag '{0}'"),
  Delete_all_unused_param_tags: diag(95172, 3 /* Message */, "Delete_all_unused_param_tags_95172", "Delete all unused '@param' tags"),
  Rename_param_tag_name_0_to_1: diag(95173, 3 /* Message */, "Rename_param_tag_name_0_to_1_95173", "Rename '@param' tag name '{0}' to '{1}'"),
  Use_0: diag(95174, 3 /* Message */, "Use_0_95174", "Use `{0}`."),
  Use_Number_isNaN_in_all_conditions: diag(95175, 3 /* Message */, "Use_Number_isNaN_in_all_conditions_95175", "Use `Number.isNaN` in all conditions."),
  Convert_typedef_to_TypeScript_type: diag(95176, 3 /* Message */, "Convert_typedef_to_TypeScript_type_95176", "Convert typedef to TypeScript type."),
  Convert_all_typedef_to_TypeScript_types: diag(95177, 3 /* Message */, "Convert_all_typedef_to_TypeScript_types_95177", "Convert all typedef to TypeScript types."),
  Move_to_file: diag(95178, 3 /* Message */, "Move_to_file_95178", "Move to file"),
  Cannot_move_to_file_selected_file_is_invalid: diag(95179, 3 /* Message */, "Cannot_move_to_file_selected_file_is_invalid_95179", "Cannot move to file, selected file is invalid"),
  Use_import_type: diag(95180, 3 /* Message */, "Use_import_type_95180", "Use 'import type'"),
  Use_type_0: diag(95181, 3 /* Message */, "Use_type_0_95181", "Use 'type {0}'"),
  Fix_all_with_type_only_imports: diag(95182, 3 /* Message */, "Fix_all_with_type_only_imports_95182", "Fix all with type-only imports"),
  Cannot_move_statements_to_the_selected_file: diag(95183, 3 /* Message */, "Cannot_move_statements_to_the_selected_file_95183", "Cannot move statements to the selected file"),
  Inline_variable: diag(95184, 3 /* Message */, "Inline_variable_95184", "Inline variable"),
  Could_not_find_variable_to_inline: diag(95185, 3 /* Message */, "Could_not_find_variable_to_inline_95185", "Could not find variable to inline."),
  Variables_with_multiple_declarations_cannot_be_inlined: diag(95186, 3 /* Message */, "Variables_with_multiple_declarations_cannot_be_inlined_95186", "Variables with multiple declarations cannot be inlined."),
  Add_missing_comma_for_object_member_completion_0: diag(95187, 3 /* Message */, "Add_missing_comma_for_object_member_completion_0_95187", "Add missing comma for object member completion '{0}'."),
  Add_missing_parameter_to_0: diag(95188, 3 /* Message */, "Add_missing_parameter_to_0_95188", "Add missing parameter to '{0}'"),
  Add_missing_parameters_to_0: diag(95189, 3 /* Message */, "Add_missing_parameters_to_0_95189", "Add missing parameters to '{0}'"),
  Add_all_missing_parameters: diag(95190, 3 /* Message */, "Add_all_missing_parameters_95190", "Add all missing parameters"),
  Add_optional_parameter_to_0: diag(95191, 3 /* Message */, "Add_optional_parameter_to_0_95191", "Add optional parameter to '{0}'"),
  Add_optional_parameters_to_0: diag(95192, 3 /* Message */, "Add_optional_parameters_to_0_95192", "Add optional parameters to '{0}'"),
  Add_all_optional_parameters: diag(95193, 3 /* Message */, "Add_all_optional_parameters_95193", "Add all optional parameters"),
  Wrap_in_parentheses: diag(95194, 3 /* Message */, "Wrap_in_parentheses_95194", "Wrap in parentheses"),
  Wrap_all_invalid_decorator_expressions_in_parentheses: diag(95195, 3 /* Message */, "Wrap_all_invalid_decorator_expressions_in_parentheses_95195", "Wrap all invalid decorator expressions in parentheses"),
  Add_resolution_mode_import_attribute: diag(95196, 3 /* Message */, "Add_resolution_mode_import_attribute_95196", "Add 'resolution-mode' import attribute"),
  Add_resolution_mode_import_attribute_to_all_type_only_imports_that_need_it: diag(95197, 3 /* Message */, "Add_resolution_mode_import_attribute_to_all_type_only_imports_that_need_it_95197", "Add 'resolution-mode' import attribute to all type-only imports that need it"),
  No_value_exists_in_scope_for_the_shorthand_property_0_Either_declare_one_or_provide_an_initializer: diag(18004, 1 /* Error */, "No_value_exists_in_scope_for_the_shorthand_property_0_Either_declare_one_or_provide_an_initializer_18004", "No value exists in scope for the shorthand property '{0}'. Either declare one or provide an initializer."),
  Classes_may_not_have_a_field_named_constructor: diag(18006, 1 /* Error */, "Classes_may_not_have_a_field_named_constructor_18006", "Classes may not have a field named 'constructor'."),
  JSX_expressions_may_not_use_the_comma_operator_Did_you_mean_to_write_an_array: diag(18007, 1 /* Error */, "JSX_expressions_may_not_use_the_comma_operator_Did_you_mean_to_write_an_array_18007", "JSX expressions may not use the comma operator. Did you mean to write an array?"),
  Private_identifiers_cannot_be_used_as_parameters: diag(18009, 1 /* Error */, "Private_identifiers_cannot_be_used_as_parameters_18009", "Private identifiers cannot be used as parameters."),
  An_accessibility_modifier_cannot_be_used_with_a_private_identifier: diag(18010, 1 /* Error */, "An_accessibility_modifier_cannot_be_used_with_a_private_identifier_18010", "An accessibility modifier cannot be used with a private identifier."),
  The_operand_of_a_delete_operator_cannot_be_a_private_identifier: diag(18011, 1 /* Error */, "The_operand_of_a_delete_operator_cannot_be_a_private_identifier_18011", "The operand of a 'delete' operator cannot be a private identifier."),
  constructor_is_a_reserved_word: diag(18012, 1 /* Error */, "constructor_is_a_reserved_word_18012", "'#constructor' is a reserved word."),
  Property_0_is_not_accessible_outside_class_1_because_it_has_a_private_identifier: diag(18013, 1 /* Error */, "Property_0_is_not_accessible_outside_class_1_because_it_has_a_private_identifier_18013", "Property '{0}' is not accessible outside class '{1}' because it has a private identifier."),
  The_property_0_cannot_be_accessed_on_type_1_within_this_class_because_it_is_shadowed_by_another_private_identifier_with_the_same_spelling: diag(18014, 1 /* Error */, "The_property_0_cannot_be_accessed_on_type_1_within_this_class_because_it_is_shadowed_by_another_priv_18014", "The property '{0}' cannot be accessed on type '{1}' within this class because it is shadowed by another private identifier with the same spelling."),
  Property_0_in_type_1_refers_to_a_different_member_that_cannot_be_accessed_from_within_type_2: diag(18015, 1 /* Error */, "Property_0_in_type_1_refers_to_a_different_member_that_cannot_be_accessed_from_within_type_2_18015", "Property '{0}' in type '{1}' refers to a different member that cannot be accessed from within type '{2}'."),
  Private_identifiers_are_not_allowed_outside_class_bodies: diag(18016, 1 /* Error */, "Private_identifiers_are_not_allowed_outside_class_bodies_18016", "Private identifiers are not allowed outside class bodies."),
  The_shadowing_declaration_of_0_is_defined_here: diag(18017, 1 /* Error */, "The_shadowing_declaration_of_0_is_defined_here_18017", "The shadowing declaration of '{0}' is defined here"),
  The_declaration_of_0_that_you_probably_intended_to_use_is_defined_here: diag(18018, 1 /* Error */, "The_declaration_of_0_that_you_probably_intended_to_use_is_defined_here_18018", "The declaration of '{0}' that you probably intended to use is defined here"),
  _0_modifier_cannot_be_used_with_a_private_identifier: diag(18019, 1 /* Error */, "_0_modifier_cannot_be_used_with_a_private_identifier_18019", "'{0}' modifier cannot be used with a private identifier."),
  An_enum_member_cannot_be_named_with_a_private_identifier: diag(18024, 1 /* Error */, "An_enum_member_cannot_be_named_with_a_private_identifier_18024", "An enum member cannot be named with a private identifier."),
  can_only_be_used_at_the_start_of_a_file: diag(18026, 1 /* Error */, "can_only_be_used_at_the_start_of_a_file_18026", "'#!' can only be used at the start of a file."),
  Compiler_reserves_name_0_when_emitting_private_identifier_downlevel: diag(18027, 1 /* Error */, "Compiler_reserves_name_0_when_emitting_private_identifier_downlevel_18027", "Compiler reserves name '{0}' when emitting private identifier downlevel."),
  Private_identifiers_are_only_available_when_targeting_ECMAScript_2015_and_higher: diag(18028, 1 /* Error */, "Private_identifiers_are_only_available_when_targeting_ECMAScript_2015_and_higher_18028", "Private identifiers are only available when targeting ECMAScript 2015 and higher."),
  Private_identifiers_are_not_allowed_in_variable_declarations: diag(18029, 1 /* Error */, "Private_identifiers_are_not_allowed_in_variable_declarations_18029", "Private identifiers are not allowed in variable declarations."),
  An_optional_chain_cannot_contain_private_identifiers: diag(18030, 1 /* Error */, "An_optional_chain_cannot_contain_private_identifiers_18030", "An optional chain cannot contain private identifiers."),
  The_intersection_0_was_reduced_to_never_because_property_1_has_conflicting_types_in_some_constituents: diag(18031, 1 /* Error */, "The_intersection_0_was_reduced_to_never_because_property_1_has_conflicting_types_in_some_constituent_18031", "The intersection '{0}' was reduced to 'never' because property '{1}' has conflicting types in some constituents."),
  The_intersection_0_was_reduced_to_never_because_property_1_exists_in_multiple_constituents_and_is_private_in_some: diag(18032, 1 /* Error */, "The_intersection_0_was_reduced_to_never_because_property_1_exists_in_multiple_constituents_and_is_pr_18032", "The intersection '{0}' was reduced to 'never' because property '{1}' exists in multiple constituents and is private in some."),
  Type_0_is_not_assignable_to_type_1_as_required_for_computed_enum_member_values: diag(18033, 1 /* Error */, "Type_0_is_not_assignable_to_type_1_as_required_for_computed_enum_member_values_18033", "Type '{0}' is not assignable to type '{1}' as required for computed enum member values."),
  Specify_the_JSX_fragment_factory_function_to_use_when_targeting_react_JSX_emit_with_jsxFactory_compiler_option_is_specified_e_g_Fragment: diag(18034, 3 /* Message */, "Specify_the_JSX_fragment_factory_function_to_use_when_targeting_react_JSX_emit_with_jsxFactory_compi_18034", "Specify the JSX fragment factory function to use when targeting 'react' JSX emit with 'jsxFactory' compiler option is specified, e.g. 'Fragment'."),
  Invalid_value_for_jsxFragmentFactory_0_is_not_a_valid_identifier_or_qualified_name: diag(18035, 1 /* Error */, "Invalid_value_for_jsxFragmentFactory_0_is_not_a_valid_identifier_or_qualified_name_18035", "Invalid value for 'jsxFragmentFactory'. '{0}' is not a valid identifier or qualified-name."),
  Class_decorators_can_t_be_used_with_static_private_identifier_Consider_removing_the_experimental_decorator: diag(18036, 1 /* Error */, "Class_decorators_can_t_be_used_with_static_private_identifier_Consider_removing_the_experimental_dec_18036", "Class decorators can't be used with static private identifier. Consider removing the experimental decorator."),
  await_expression_cannot_be_used_inside_a_class_static_block: diag(18037, 1 /* Error */, "await_expression_cannot_be_used_inside_a_class_static_block_18037", "'await' expression cannot be used inside a class static block."),
  for_await_loops_cannot_be_used_inside_a_class_static_block: diag(18038, 1 /* Error */, "for_await_loops_cannot_be_used_inside_a_class_static_block_18038", "'for await' loops cannot be used inside a class static block."),
  Invalid_use_of_0_It_cannot_be_used_inside_a_class_static_block: diag(18039, 1 /* Error */, "Invalid_use_of_0_It_cannot_be_used_inside_a_class_static_block_18039", "Invalid use of '{0}'. It cannot be used inside a class static block."),
  A_return_statement_cannot_be_used_inside_a_class_static_block: diag(18041, 1 /* Error */, "A_return_statement_cannot_be_used_inside_a_class_static_block_18041", "A 'return' statement cannot be used inside a class static block."),
  _0_is_a_type_and_cannot_be_imported_in_JavaScript_files_Use_1_in_a_JSDoc_type_annotation: diag(18042, 1 /* Error */, "_0_is_a_type_and_cannot_be_imported_in_JavaScript_files_Use_1_in_a_JSDoc_type_annotation_18042", "'{0}' is a type and cannot be imported in JavaScript files. Use '{1}' in a JSDoc type annotation."),
  Types_cannot_appear_in_export_declarations_in_JavaScript_files: diag(18043, 1 /* Error */, "Types_cannot_appear_in_export_declarations_in_JavaScript_files_18043", "Types cannot appear in export declarations in JavaScript files."),
  _0_is_automatically_exported_here: diag(18044, 3 /* Message */, "_0_is_automatically_exported_here_18044", "'{0}' is automatically exported here."),
  Properties_with_the_accessor_modifier_are_only_available_when_targeting_ECMAScript_2015_and_higher: diag(18045, 1 /* Error */, "Properties_with_the_accessor_modifier_are_only_available_when_targeting_ECMAScript_2015_and_higher_18045", "Properties with the 'accessor' modifier are only available when targeting ECMAScript 2015 and higher."),
  _0_is_of_type_unknown: diag(18046, 1 /* Error */, "_0_is_of_type_unknown_18046", "'{0}' is of type 'unknown'."),
  _0_is_possibly_null: diag(18047, 1 /* Error */, "_0_is_possibly_null_18047", "'{0}' is possibly 'null'."),
  _0_is_possibly_undefined: diag(18048, 1 /* Error */, "_0_is_possibly_undefined_18048", "'{0}' is possibly 'undefined'."),
  _0_is_possibly_null_or_undefined: diag(18049, 1 /* Error */, "_0_is_possibly_null_or_undefined_18049", "'{0}' is possibly 'null' or 'undefined'."),
  The_value_0_cannot_be_used_here: diag(18050, 1 /* Error */, "The_value_0_cannot_be_used_here_18050", "The value '{0}' cannot be used here."),
  Compiler_option_0_cannot_be_given_an_empty_string: diag(18051, 1 /* Error */, "Compiler_option_0_cannot_be_given_an_empty_string_18051", "Compiler option '{0}' cannot be given an empty string."),
  Its_type_0_is_not_a_valid_JSX_element_type: diag(18053, 1 /* Error */, "Its_type_0_is_not_a_valid_JSX_element_type_18053", "Its type '{0}' is not a valid JSX element type."),
  await_using_statements_cannot_be_used_inside_a_class_static_block: diag(18054, 1 /* Error */, "await_using_statements_cannot_be_used_inside_a_class_static_block_18054", "'await using' statements cannot be used inside a class static block."),
  _0_has_a_string_type_but_must_have_syntactically_recognizable_string_syntax_when_isolatedModules_is_enabled: diag(18055, 1 /* Error */, "_0_has_a_string_type_but_must_have_syntactically_recognizable_string_syntax_when_isolatedModules_is__18055", "'{0}' has a string type, but must have syntactically recognizable string syntax when 'isolatedModules' is enabled."),
  Enum_member_following_a_non_literal_numeric_member_must_have_an_initializer_when_isolatedModules_is_enabled: diag(18056, 1 /* Error */, "Enum_member_following_a_non_literal_numeric_member_must_have_an_initializer_when_isolatedModules_is__18056", "Enum member following a non-literal numeric member must have an initializer when 'isolatedModules' is enabled."),
  String_literal_import_and_export_names_are_not_supported_when_the_module_flag_is_set_to_es2015_or_es2020: diag(18057, 1 /* Error */, "String_literal_import_and_export_names_are_not_supported_when_the_module_flag_is_set_to_es2015_or_es_18057", "String literal import and export names are not supported when the '--module' flag is set to 'es2015' or 'es2020'."),
  Default_imports_are_not_allowed_in_a_deferred_import: diag(18058, 1 /* Error */, "Default_imports_are_not_allowed_in_a_deferred_import_18058", "Default imports are not allowed in a deferred import."),
  Named_imports_are_not_allowed_in_a_deferred_import: diag(18059, 1 /* Error */, "Named_imports_are_not_allowed_in_a_deferred_import_18059", "Named imports are not allowed in a deferred import."),
  Deferred_imports_are_only_supported_when_the_module_flag_is_set_to_esnext_or_preserve: diag(18060, 1 /* Error */, "Deferred_imports_are_only_supported_when_the_module_flag_is_set_to_esnext_or_preserve_18060", "Deferred imports are only supported when the '--module' flag is set to 'esnext' or 'preserve'."),
  _0_is_not_a_valid_meta_property_for_keyword_import_Did_you_mean_meta_or_defer: diag(18061, 1 /* Error */, "_0_is_not_a_valid_meta_property_for_keyword_import_Did_you_mean_meta_or_defer_18061", "'{0}' is not a valid meta-property for keyword 'import'. Did you mean 'meta' or 'defer'?")
};

// src/compiler/scanner.ts
function tokenIsIdentifierOrKeyword(token) {
  return token >***REMOVED*** 80 /* Identifier */;
}
function tokenIsIdentifierOrKeywordOrGreaterThan(token) {
  return token ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ || tokenIsIdentifierOrKeyword(token);
}
var textToKeywordObj ***REMOVED*** {
  abstract: 128 /* AbstractKeyword */,
  accessor: 129 /* AccessorKeyword */,
  any: 133 /* AnyKeyword */,
  as: 130 /* AsKeyword */,
  asserts: 131 /* AssertsKeyword */,
  assert: 132 /* AssertKeyword */,
  bigint: 163 /* BigIntKeyword */,
  boolean: 136 /* BooleanKeyword */,
  break: 83 /* BreakKeyword */,
  case: 84 /* CaseKeyword */,
  catch: 85 /* CatchKeyword */,
  class: 86 /* ClassKeyword */,
  continue: 88 /* ContinueKeyword */,
  const: 87 /* ConstKeyword */,
  ["constructor"]: 137 /* ConstructorKeyword */,
  debugger: 89 /* DebuggerKeyword */,
  declare: 138 /* DeclareKeyword */,
  default: 90 /* DefaultKeyword */,
  defer: 166 /* DeferKeyword */,
  delete: 91 /* DeleteKeyword */,
  do: 92 /* DoKeyword */,
  else: 93 /* ElseKeyword */,
  enum: 94 /* EnumKeyword */,
  export: 95 /* ExportKeyword */,
  extends: 96 /* ExtendsKeyword */,
  false: 97 /* FalseKeyword */,
  finally: 98 /* FinallyKeyword */,
  for: 99 /* ForKeyword */,
  from: 161 /* FromKeyword */,
  function: 100 /* FunctionKeyword */,
  get: 139 /* GetKeyword */,
  if: 101 /* IfKeyword */,
  implements: 119 /* ImplementsKeyword */,
  import: 102 /* ImportKeyword */,
  in: 103 /* InKeyword */,
  infer: 140 /* InferKeyword */,
  instanceof: 104 /* InstanceOfKeyword */,
  interface: 120 /* InterfaceKeyword */,
  intrinsic: 141 /* IntrinsicKeyword */,
  is: 142 /* IsKeyword */,
  keyof: 143 /* KeyOfKeyword */,
  let: 121 /* LetKeyword */,
  module: 144 /* ModuleKeyword */,
  namespace: 145 /* NamespaceKeyword */,
  never: 146 /* NeverKeyword */,
  new: 105 /* NewKeyword */,
  null: 106 /* NullKeyword */,
  number: 150 /* NumberKeyword */,
  object: 151 /* ObjectKeyword */,
  package: 122 /* PackageKeyword */,
  private: 123 /* PrivateKeyword */,
  protected: 124 /* ProtectedKeyword */,
  public: 125 /* PublicKeyword */,
  override: 164 /* OverrideKeyword */,
  out: 147 /* OutKeyword */,
  readonly: 148 /* ReadonlyKeyword */,
  require: 149 /* RequireKeyword */,
  global: 162 /* GlobalKeyword */,
  return: 107 /* ReturnKeyword */,
  satisfies: 152 /* SatisfiesKeyword */,
  set: 153 /* SetKeyword */,
  static: 126 /* StaticKeyword */,
  string: 154 /* StringKeyword */,
  super: 108 /* SuperKeyword */,
  switch: 109 /* SwitchKeyword */,
  symbol: 155 /* SymbolKeyword */,
  this: 110 /* ThisKeyword */,
  throw: 111 /* ThrowKeyword */,
  true: 112 /* TrueKeyword */,
  try: 113 /* TryKeyword */,
  type: 156 /* TypeKeyword */,
  typeof: 114 /* TypeOfKeyword */,
  undefined: 157 /* UndefinedKeyword */,
  unique: 158 /* UniqueKeyword */,
  unknown: 159 /* UnknownKeyword */,
  using: 160 /* UsingKeyword */,
  var: 115 /* VarKeyword */,
  void: 116 /* VoidKeyword */,
  while: 117 /* WhileKeyword */,
  with: 118 /* WithKeyword */,
  yield: 127 /* YieldKeyword */,
  async: 134 /* AsyncKeyword */,
  await: 135 /* AwaitKeyword */,
  of: 165 /* OfKeyword */
};
var textToKeyword ***REMOVED*** new Map(Object.entries(textToKeywordObj));
var textToToken ***REMOVED*** new Map(Object.entries({
  ...textToKeywordObj,
  "{": 19 /* OpenBraceToken */,
  "}": 20 /* CloseBraceToken */,
  "(": 21 /* OpenParenToken */,
  ")": 22 /* CloseParenToken */,
  "[": 23 /* OpenBracketToken */,
  "]": 24 /* CloseBracketToken */,
  ".": 25 /* DotToken */,
  "...": 26 /* DotDotDotToken */,
  ";": 27 /* SemicolonToken */,
  ",": 28 /* CommaToken */,
  "<": 30 /* LessThanToken */,
  ">": 32 /* GreaterThanToken */,
  "<***REMOVED***": 33 /* LessThanEqualsToken */,
  ">***REMOVED***": 34 /* GreaterThanEqualsToken */,
  "***REMOVED******REMOVED***": 35 /* EqualsEqualsToken */,
  "!***REMOVED***": 36 /* ExclamationEqualsToken */,
  "***REMOVED******REMOVED******REMOVED***": 37 /* EqualsEqualsEqualsToken */,
  "!***REMOVED******REMOVED***": 38 /* ExclamationEqualsEqualsToken */,
  "***REMOVED***>": 39 /* EqualsGreaterThanToken */,
  "+": 40 /* PlusToken */,
  "-": 41 /* MinusToken */,
  "**": 43 /* AsteriskAsteriskToken */,
  "*": 42 /* AsteriskToken */,
  "/": 44 /* SlashToken */,
  "%": 45 /* PercentToken */,
  "++": 46 /* PlusPlusToken */,
  "--": 47 /* MinusMinusToken */,
  "<<": 48 /* LessThanLessThanToken */,
  "</": 31 /* LessThanSlashToken */,
  ">>": 49 /* GreaterThanGreaterThanToken */,
  ">>>": 50 /* GreaterThanGreaterThanGreaterThanToken */,
  "&": 51 /* AmpersandToken */,
  "|": 52 /* BarToken */,
  "^": 53 /* CaretToken */,
  "!": 54 /* ExclamationToken */,
  "~": 55 /* TildeToken */,
  "&&": 56 /* AmpersandAmpersandToken */,
  "||": 57 /* BarBarToken */,
  "?": 58 /* QuestionToken */,
  "??": 61 /* QuestionQuestionToken */,
  "?.": 29 /* QuestionDotToken */,
  ":": 59 /* ColonToken */,
  "***REMOVED***": 64 /* EqualsToken */,
  "+***REMOVED***": 65 /* PlusEqualsToken */,
  "-***REMOVED***": 66 /* MinusEqualsToken */,
  "****REMOVED***": 67 /* AsteriskEqualsToken */,
  "*****REMOVED***": 68 /* AsteriskAsteriskEqualsToken */,
  "/***REMOVED***": 69 /* SlashEqualsToken */,
  "%***REMOVED***": 70 /* PercentEqualsToken */,
  "<<***REMOVED***": 71 /* LessThanLessThanEqualsToken */,
  ">>***REMOVED***": 72 /* GreaterThanGreaterThanEqualsToken */,
  ">>>***REMOVED***": 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */,
  "&***REMOVED***": 74 /* AmpersandEqualsToken */,
  "|***REMOVED***": 75 /* BarEqualsToken */,
  "^***REMOVED***": 79 /* CaretEqualsToken */,
  "||***REMOVED***": 76 /* BarBarEqualsToken */,
  "&&***REMOVED***": 77 /* AmpersandAmpersandEqualsToken */,
  "??***REMOVED***": 78 /* QuestionQuestionEqualsToken */,
  "@": 60 /* AtToken */,
  "#": 63 /* HashToken */,
  "`": 62 /* BacktickToken */
}));
var charCodeToRegExpFlag ***REMOVED*** /* @__PURE__ */ new Map([
  [100 /* d */, 1 /* HasIndices */],
  [103 /* g */, 2 /* Global */],
  [105 /* i */, 4 /* IgnoreCase */],
  [109 /* m */, 8 /* Multiline */],
  [115 /* s */, 16 /* DotAll */],
  [117 /* u */, 32 /* Unicode */],
  [118 /* v */, 64 /* UnicodeSets */],
  [121 /* y */, 128 /* Sticky */]
]);
var regExpFlagToFirstAvailableLanguageVersion ***REMOVED*** /* @__PURE__ */ new Map([
  [1 /* HasIndices */, LanguageFeatureMinimumTarget.RegularExpressionFlagsHasIndices],
  [16 /* DotAll */, LanguageFeatureMinimumTarget.RegularExpressionFlagsDotAll],
  [32 /* Unicode */, LanguageFeatureMinimumTarget.RegularExpressionFlagsUnicode],
  [64 /* UnicodeSets */, LanguageFeatureMinimumTarget.RegularExpressionFlagsUnicodeSets],
  [128 /* Sticky */, LanguageFeatureMinimumTarget.RegularExpressionFlagsSticky]
]);
var unicodeES5IdentifierStart ***REMOVED*** [170, 170, 181, 181, 186, 186, 192, 214, 216, 246, 248, 705, 710, 721, 736, 740, 748, 748, 750, 750, 880, 884, 886, 887, 890, 893, 902, 902, 904, 906, 908, 908, 910, 929, 931, 1013, 1015, 1153, 1162, 1319, 1329, 1366, 1369, 1369, 1377, 1415, 1488, 1514, 1520, 1522, 1568, 1610, 1646, 1647, 1649, 1747, 1749, 1749, 1765, 1766, 1774, 1775, 1786, 1788, 1791, 1791, 1808, 1808, 1810, 1839, 1869, 1957, 1969, 1969, 1994, 2026, 2036, 2037, 2042, 2042, 2048, 2069, 2074, 2074, 2084, 2084, 2088, 2088, 2112, 2136, 2208, 2208, 2210, 2220, 2308, 2361, 2365, 2365, 2384, 2384, 2392, 2401, 2417, 2423, 2425, 2431, 2437, 2444, 2447, 2448, 2451, 2472, 2474, 2480, 2482, 2482, 2486, 2489, 2493, 2493, 2510, 2510, 2524, 2525, 2527, 2529, 2544, 2545, 2565, 2570, 2575, 2576, 2579, 2600, 2602, 2608, 2610, 2611, 2613, 2614, 2616, 2617, 2649, 2652, 2654, 2654, 2674, 2676, 2693, 2701, 2703, 2705, 2707, 2728, 2730, 2736, 2738, 2739, 2741, 2745, 2749, 2749, 2768, 2768, 2784, 2785, 2821, 2828, 2831, 2832, 2835, 2856, 2858, 2864, 2866, 2867, 2869, 2873, 2877, 2877, 2908, 2909, 2911, 2913, 2929, 2929, 2947, 2947, 2949, 2954, 2958, 2960, 2962, 2965, 2969, 2970, 2972, 2972, 2974, 2975, 2979, 2980, 2984, 2986, 2990, 3001, 3024, 3024, 3077, 3084, 3086, 3088, 3090, 3112, 3114, 3123, 3125, 3129, 3133, 3133, 3160, 3161, 3168, 3169, 3205, 3212, 3214, 3216, 3218, 3240, 3242, 3251, 3253, 3257, 3261, 3261, 3294, 3294, 3296, 3297, 3313, 3314, 3333, 3340, 3342, 3344, 3346, 3386, 3389, 3389, 3406, 3406, 3424, 3425, 3450, 3455, 3461, 3478, 3482, 3505, 3507, 3515, 3517, 3517, 3520, 3526, 3585, 3632, 3634, 3635, 3648, 3654, 3713, 3714, 3716, 3716, 3719, 3720, 3722, 3722, 3725, 3725, 3732, 3735, 3737, 3743, 3745, 3747, 3749, 3749, 3751, 3751, 3754, 3755, 3757, 3760, 3762, 3763, 3773, 3773, 3776, 3780, 3782, 3782, 3804, 3807, 3840, 3840, 3904, 3911, 3913, 3948, 3976, 3980, 4096, 4138, 4159, 4159, 4176, 4181, 4186, 4189, 4193, 4193, 4197, 4198, 4206, 4208, 4213, 4225, 4238, 4238, 4256, 4293, 4295, 4295, 4301, 4301, 4304, 4346, 4348, 4680, 4682, 4685, 4688, 4694, 4696, 4696, 4698, 4701, 4704, 4744, 4746, 4749, 4752, 4784, 4786, 4789, 4792, 4798, 4800, 4800, 4802, 4805, 4808, 4822, 4824, 4880, 4882, 4885, 4888, 4954, 4992, 5007, 5024, 5108, 5121, 5740, 5743, 5759, 5761, 5786, 5792, 5866, 5870, 5872, 5888, 5900, 5902, 5905, 5920, 5937, 5952, 5969, 5984, 5996, 5998, 6e3, 6016, 6067, 6103, 6103, 6108, 6108, 6176, 6263, 6272, 6312, 6314, 6314, 6320, 6389, 6400, 6428, 6480, 6509, 6512, 6516, 6528, 6571, 6593, 6599, 6656, 6678, 6688, 6740, 6823, 6823, 6917, 6963, 6981, 6987, 7043, 7072, 7086, 7087, 7098, 7141, 7168, 7203, 7245, 7247, 7258, 7293, 7401, 7404, 7406, 7409, 7413, 7414, 7424, 7615, 7680, 7957, 7960, 7965, 7968, 8005, 8008, 8013, 8016, 8023, 8025, 8025, 8027, 8027, 8029, 8029, 8031, 8061, 8064, 8116, 8118, 8124, 8126, 8126, 8130, 8132, 8134, 8140, 8144, 8147, 8150, 8155, 8160, 8172, 8178, 8180, 8182, 8188, 8305, 8305, 8319, 8319, 8336, 8348, 8450, 8450, 8455, 8455, 8458, 8467, 8469, 8469, 8473, 8477, 8484, 8484, 8486, 8486, 8488, 8488, 8490, 8493, 8495, 8505, 8508, 8511, 8517, 8521, 8526, 8526, 8544, 8584, 11264, 11310, 11312, 11358, 11360, 11492, 11499, 11502, 11506, 11507, 11520, 11557, 11559, 11559, 11565, 11565, 11568, 11623, 11631, 11631, 11648, 11670, 11680, 11686, 11688, 11694, 11696, 11702, 11704, 11710, 11712, 11718, 11720, 11726, 11728, 11734, 11736, 11742, 11823, 11823, 12293, 12295, 12321, 12329, 12337, 12341, 12344, 12348, 12353, 12438, 12445, 12447, 12449, 12538, 12540, 12543, 12549, 12589, 12593, 12686, 12704, 12730, 12784, 12799, 13312, 19893, 19968, 40908, 40960, 42124, 42192, 42237, 42240, 42508, 42512, 42527, 42538, 42539, 42560, 42606, 42623, 42647, 42656, 42735, 42775, 42783, 42786, 42888, 42891, 42894, 42896, 42899, 42912, 42922, 43e3, 43009, 43011, 43013, 43015, 43018, 43020, 43042, 43072, 43123, 43138, 43187, 43250, 43255, 43259, 43259, 43274, 43301, 43312, 43334, 43360, 43388, 43396, 43442, 43471, 43471, 43520, 43560, 43584, 43586, 43588, 43595, 43616, 43638, 43642, 43642, 43648, 43695, 43697, 43697, 43701, 43702, 43705, 43709, 43712, 43712, 43714, 43714, 43739, 43741, 43744, 43754, 43762, 43764, 43777, 43782, 43785, 43790, 43793, 43798, 43808, 43814, 43816, 43822, 43968, 44002, 44032, 55203, 55216, 55238, 55243, 55291, 63744, 64109, 64112, 64217, 64256, 64262, 64275, 64279, 64285, 64285, 64287, 64296, 64298, 64310, 64312, 64316, 64318, 64318, 64320, 64321, 64323, 64324, 64326, 64433, 64467, 64829, 64848, 64911, 64914, 64967, 65008, 65019, 65136, 65140, 65142, 65276, 65313, 65338, 65345, 65370, 65382, 65470, 65474, 65479, 65482, 65487, 65490, 65495, 65498, 65500];
var unicodeES5IdentifierPart ***REMOVED*** [170, 170, 181, 181, 186, 186, 192, 214, 216, 246, 248, 705, 710, 721, 736, 740, 748, 748, 750, 750, 768, 884, 886, 887, 890, 893, 902, 902, 904, 906, 908, 908, 910, 929, 931, 1013, 1015, 1153, 1155, 1159, 1162, 1319, 1329, 1366, 1369, 1369, 1377, 1415, 1425, 1469, 1471, 1471, 1473, 1474, 1476, 1477, 1479, 1479, 1488, 1514, 1520, 1522, 1552, 1562, 1568, 1641, 1646, 1747, 1749, 1756, 1759, 1768, 1770, 1788, 1791, 1791, 1808, 1866, 1869, 1969, 1984, 2037, 2042, 2042, 2048, 2093, 2112, 2139, 2208, 2208, 2210, 2220, 2276, 2302, 2304, 2403, 2406, 2415, 2417, 2423, 2425, 2431, 2433, 2435, 2437, 2444, 2447, 2448, 2451, 2472, 2474, 2480, 2482, 2482, 2486, 2489, 2492, 2500, 2503, 2504, 2507, 2510, 2519, 2519, 2524, 2525, 2527, 2531, 2534, 2545, 2561, 2563, 2565, 2570, 2575, 2576, 2579, 2600, 2602, 2608, 2610, 2611, 2613, 2614, 2616, 2617, 2620, 2620, 2622, 2626, 2631, 2632, 2635, 2637, 2641, 2641, 2649, 2652, 2654, 2654, 2662, 2677, 2689, 2691, 2693, 2701, 2703, 2705, 2707, 2728, 2730, 2736, 2738, 2739, 2741, 2745, 2748, 2757, 2759, 2761, 2763, 2765, 2768, 2768, 2784, 2787, 2790, 2799, 2817, 2819, 2821, 2828, 2831, 2832, 2835, 2856, 2858, 2864, 2866, 2867, 2869, 2873, 2876, 2884, 2887, 2888, 2891, 2893, 2902, 2903, 2908, 2909, 2911, 2915, 2918, 2927, 2929, 2929, 2946, 2947, 2949, 2954, 2958, 2960, 2962, 2965, 2969, 2970, 2972, 2972, 2974, 2975, 2979, 2980, 2984, 2986, 2990, 3001, 3006, 3010, 3014, 3016, 3018, 3021, 3024, 3024, 3031, 3031, 3046, 3055, 3073, 3075, 3077, 3084, 3086, 3088, 3090, 3112, 3114, 3123, 3125, 3129, 3133, 3140, 3142, 3144, 3146, 3149, 3157, 3158, 3160, 3161, 3168, 3171, 3174, 3183, 3202, 3203, 3205, 3212, 3214, 3216, 3218, 3240, 3242, 3251, 3253, 3257, 3260, 3268, 3270, 3272, 3274, 3277, 3285, 3286, 3294, 3294, 3296, 3299, 3302, 3311, 3313, 3314, 3330, 3331, 3333, 3340, 3342, 3344, 3346, 3386, 3389, 3396, 3398, 3400, 3402, 3406, 3415, 3415, 3424, 3427, 3430, 3439, 3450, 3455, 3458, 3459, 3461, 3478, 3482, 3505, 3507, 3515, 3517, 3517, 3520, 3526, 3530, 3530, 3535, 3540, 3542, 3542, 3544, 3551, 3570, 3571, 3585, 3642, 3648, 3662, 3664, 3673, 3713, 3714, 3716, 3716, 3719, 3720, 3722, 3722, 3725, 3725, 3732, 3735, 3737, 3743, 3745, 3747, 3749, 3749, 3751, 3751, 3754, 3755, 3757, 3769, 3771, 3773, 3776, 3780, 3782, 3782, 3784, 3789, 3792, 3801, 3804, 3807, 3840, 3840, 3864, 3865, 3872, 3881, 3893, 3893, 3895, 3895, 3897, 3897, 3902, 3911, 3913, 3948, 3953, 3972, 3974, 3991, 3993, 4028, 4038, 4038, 4096, 4169, 4176, 4253, 4256, 4293, 4295, 4295, 4301, 4301, 4304, 4346, 4348, 4680, 4682, 4685, 4688, 4694, 4696, 4696, 4698, 4701, 4704, 4744, 4746, 4749, 4752, 4784, 4786, 4789, 4792, 4798, 4800, 4800, 4802, 4805, 4808, 4822, 4824, 4880, 4882, 4885, 4888, 4954, 4957, 4959, 4992, 5007, 5024, 5108, 5121, 5740, 5743, 5759, 5761, 5786, 5792, 5866, 5870, 5872, 5888, 5900, 5902, 5908, 5920, 5940, 5952, 5971, 5984, 5996, 5998, 6e3, 6002, 6003, 6016, 6099, 6103, 6103, 6108, 6109, 6112, 6121, 6155, 6157, 6160, 6169, 6176, 6263, 6272, 6314, 6320, 6389, 6400, 6428, 6432, 6443, 6448, 6459, 6470, 6509, 6512, 6516, 6528, 6571, 6576, 6601, 6608, 6617, 6656, 6683, 6688, 6750, 6752, 6780, 6783, 6793, 6800, 6809, 6823, 6823, 6912, 6987, 6992, 7001, 7019, 7027, 7040, 7155, 7168, 7223, 7232, 7241, 7245, 7293, 7376, 7378, 7380, 7414, 7424, 7654, 7676, 7957, 7960, 7965, 7968, 8005, 8008, 8013, 8016, 8023, 8025, 8025, 8027, 8027, 8029, 8029, 8031, 8061, 8064, 8116, 8118, 8124, 8126, 8126, 8130, 8132, 8134, 8140, 8144, 8147, 8150, 8155, 8160, 8172, 8178, 8180, 8182, 8188, 8204, 8205, 8255, 8256, 8276, 8276, 8305, 8305, 8319, 8319, 8336, 8348, 8400, 8412, 8417, 8417, 8421, 8432, 8450, 8450, 8455, 8455, 8458, 8467, 8469, 8469, 8473, 8477, 8484, 8484, 8486, 8486, 8488, 8488, 8490, 8493, 8495, 8505, 8508, 8511, 8517, 8521, 8526, 8526, 8544, 8584, 11264, 11310, 11312, 11358, 11360, 11492, 11499, 11507, 11520, 11557, 11559, 11559, 11565, 11565, 11568, 11623, 11631, 11631, 11647, 11670, 11680, 11686, 11688, 11694, 11696, 11702, 11704, 11710, 11712, 11718, 11720, 11726, 11728, 11734, 11736, 11742, 11744, 11775, 11823, 11823, 12293, 12295, 12321, 12335, 12337, 12341, 12344, 12348, 12353, 12438, 12441, 12442, 12445, 12447, 12449, 12538, 12540, 12543, 12549, 12589, 12593, 12686, 12704, 12730, 12784, 12799, 13312, 19893, 19968, 40908, 40960, 42124, 42192, 42237, 42240, 42508, 42512, 42539, 42560, 42607, 42612, 42621, 42623, 42647, 42655, 42737, 42775, 42783, 42786, 42888, 42891, 42894, 42896, 42899, 42912, 42922, 43e3, 43047, 43072, 43123, 43136, 43204, 43216, 43225, 43232, 43255, 43259, 43259, 43264, 43309, 43312, 43347, 43360, 43388, 43392, 43456, 43471, 43481, 43520, 43574, 43584, 43597, 43600, 43609, 43616, 43638, 43642, 43643, 43648, 43714, 43739, 43741, 43744, 43759, 43762, 43766, 43777, 43782, 43785, 43790, 43793, 43798, 43808, 43814, 43816, 43822, 43968, 44010, 44012, 44013, 44016, 44025, 44032, 55203, 55216, 55238, 55243, 55291, 63744, 64109, 64112, 64217, 64256, 64262, 64275, 64279, 64285, 64296, 64298, 64310, 64312, 64316, 64318, 64318, 64320, 64321, 64323, 64324, 64326, 64433, 64467, 64829, 64848, 64911, 64914, 64967, 65008, 65019, 65024, 65039, 65056, 65062, 65075, 65076, 65101, 65103, 65136, 65140, 65142, 65276, 65296, 65305, 65313, 65338, 65343, 65343, 65345, 65370, 65382, 65470, 65474, 65479, 65482, 65487, 65490, 65495, 65498, 65500];
var unicodeESNextIdentifierStart ***REMOVED*** [65, 90, 97, 122, 170, 170, 181, 181, 186, 186, 192, 214, 216, 246, 248, 705, 710, 721, 736, 740, 748, 748, 750, 750, 880, 884, 886, 887, 890, 893, 895, 895, 902, 902, 904, 906, 908, 908, 910, 929, 931, 1013, 1015, 1153, 1162, 1327, 1329, 1366, 1369, 1369, 1376, 1416, 1488, 1514, 1519, 1522, 1568, 1610, 1646, 1647, 1649, 1747, 1749, 1749, 1765, 1766, 1774, 1775, 1786, 1788, 1791, 1791, 1808, 1808, 1810, 1839, 1869, 1957, 1969, 1969, 1994, 2026, 2036, 2037, 2042, 2042, 2048, 2069, 2074, 2074, 2084, 2084, 2088, 2088, 2112, 2136, 2144, 2154, 2160, 2183, 2185, 2190, 2208, 2249, 2308, 2361, 2365, 2365, 2384, 2384, 2392, 2401, 2417, 2432, 2437, 2444, 2447, 2448, 2451, 2472, 2474, 2480, 2482, 2482, 2486, 2489, 2493, 2493, 2510, 2510, 2524, 2525, 2527, 2529, 2544, 2545, 2556, 2556, 2565, 2570, 2575, 2576, 2579, 2600, 2602, 2608, 2610, 2611, 2613, 2614, 2616, 2617, 2649, 2652, 2654, 2654, 2674, 2676, 2693, 2701, 2703, 2705, 2707, 2728, 2730, 2736, 2738, 2739, 2741, 2745, 2749, 2749, 2768, 2768, 2784, 2785, 2809, 2809, 2821, 2828, 2831, 2832, 2835, 2856, 2858, 2864, 2866, 2867, 2869, 2873, 2877, 2877, 2908, 2909, 2911, 2913, 2929, 2929, 2947, 2947, 2949, 2954, 2958, 2960, 2962, 2965, 2969, 2970, 2972, 2972, 2974, 2975, 2979, 2980, 2984, 2986, 2990, 3001, 3024, 3024, 3077, 3084, 3086, 3088, 3090, 3112, 3114, 3129, 3133, 3133, 3160, 3162, 3165, 3165, 3168, 3169, 3200, 3200, 3205, 3212, 3214, 3216, 3218, 3240, 3242, 3251, 3253, 3257, 3261, 3261, 3293, 3294, 3296, 3297, 3313, 3314, 3332, 3340, 3342, 3344, 3346, 3386, 3389, 3389, 3406, 3406, 3412, 3414, 3423, 3425, 3450, 3455, 3461, 3478, 3482, 3505, 3507, 3515, 3517, 3517, 3520, 3526, 3585, 3632, 3634, 3635, 3648, 3654, 3713, 3714, 3716, 3716, 3718, 3722, 3724, 3747, 3749, 3749, 3751, 3760, 3762, 3763, 3773, 3773, 3776, 3780, 3782, 3782, 3804, 3807, 3840, 3840, 3904, 3911, 3913, 3948, 3976, 3980, 4096, 4138, 4159, 4159, 4176, 4181, 4186, 4189, 4193, 4193, 4197, 4198, 4206, 4208, 4213, 4225, 4238, 4238, 4256, 4293, 4295, 4295, 4301, 4301, 4304, 4346, 4348, 4680, 4682, 4685, 4688, 4694, 4696, 4696, 4698, 4701, 4704, 4744, 4746, 4749, 4752, 4784, 4786, 4789, 4792, 4798, 4800, 4800, 4802, 4805, 4808, 4822, 4824, 4880, 4882, 4885, 4888, 4954, 4992, 5007, 5024, 5109, 5112, 5117, 5121, 5740, 5743, 5759, 5761, 5786, 5792, 5866, 5870, 5880, 5888, 5905, 5919, 5937, 5952, 5969, 5984, 5996, 5998, 6e3, 6016, 6067, 6103, 6103, 6108, 6108, 6176, 6264, 6272, 6312, 6314, 6314, 6320, 6389, 6400, 6430, 6480, 6509, 6512, 6516, 6528, 6571, 6576, 6601, 6656, 6678, 6688, 6740, 6823, 6823, 6917, 6963, 6981, 6988, 7043, 7072, 7086, 7087, 7098, 7141, 7168, 7203, 7245, 7247, 7258, 7293, 7296, 7304, 7312, 7354, 7357, 7359, 7401, 7404, 7406, 7411, 7413, 7414, 7418, 7418, 7424, 7615, 7680, 7957, 7960, 7965, 7968, 8005, 8008, 8013, 8016, 8023, 8025, 8025, 8027, 8027, 8029, 8029, 8031, 8061, 8064, 8116, 8118, 8124, 8126, 8126, 8130, 8132, 8134, 8140, 8144, 8147, 8150, 8155, 8160, 8172, 8178, 8180, 8182, 8188, 8305, 8305, 8319, 8319, 8336, 8348, 8450, 8450, 8455, 8455, 8458, 8467, 8469, 8469, 8472, 8477, 8484, 8484, 8486, 8486, 8488, 8488, 8490, 8505, 8508, 8511, 8517, 8521, 8526, 8526, 8544, 8584, 11264, 11492, 11499, 11502, 11506, 11507, 11520, 11557, 11559, 11559, 11565, 11565, 11568, 11623, 11631, 11631, 11648, 11670, 11680, 11686, 11688, 11694, 11696, 11702, 11704, 11710, 11712, 11718, 11720, 11726, 11728, 11734, 11736, 11742, 12293, 12295, 12321, 12329, 12337, 12341, 12344, 12348, 12353, 12438, 12443, 12447, 12449, 12538, 12540, 12543, 12549, 12591, 12593, 12686, 12704, 12735, 12784, 12799, 13312, 19903, 19968, 42124, 42192, 42237, 42240, 42508, 42512, 42527, 42538, 42539, 42560, 42606, 42623, 42653, 42656, 42735, 42775, 42783, 42786, 42888, 42891, 42954, 42960, 42961, 42963, 42963, 42965, 42969, 42994, 43009, 43011, 43013, 43015, 43018, 43020, 43042, 43072, 43123, 43138, 43187, 43250, 43255, 43259, 43259, 43261, 43262, 43274, 43301, 43312, 43334, 43360, 43388, 43396, 43442, 43471, 43471, 43488, 43492, 43494, 43503, 43514, 43518, 43520, 43560, 43584, 43586, 43588, 43595, 43616, 43638, 43642, 43642, 43646, 43695, 43697, 43697, 43701, 43702, 43705, 43709, 43712, 43712, 43714, 43714, 43739, 43741, 43744, 43754, 43762, 43764, 43777, 43782, 43785, 43790, 43793, 43798, 43808, 43814, 43816, 43822, 43824, 43866, 43868, 43881, 43888, 44002, 44032, 55203, 55216, 55238, 55243, 55291, 63744, 64109, 64112, 64217, 64256, 64262, 64275, 64279, 64285, 64285, 64287, 64296, 64298, 64310, 64312, 64316, 64318, 64318, 64320, 64321, 64323, 64324, 64326, 64433, 64467, 64829, 64848, 64911, 64914, 64967, 65008, 65019, 65136, 65140, 65142, 65276, 65313, 65338, 65345, 65370, 65382, 65470, 65474, 65479, 65482, 65487, 65490, 65495, 65498, 65500, 65536, 65547, 65549, 65574, 65576, 65594, 65596, 65597, 65599, 65613, 65616, 65629, 65664, 65786, 65856, 65908, 66176, 66204, 66208, 66256, 66304, 66335, 66349, 66378, 66384, 66421, 66432, 66461, 66464, 66499, 66504, 66511, 66513, 66517, 66560, 66717, 66736, 66771, 66776, 66811, 66816, 66855, 66864, 66915, 66928, 66938, 66940, 66954, 66956, 66962, 66964, 66965, 66967, 66977, 66979, 66993, 66995, 67001, 67003, 67004, 67072, 67382, 67392, 67413, 67424, 67431, 67456, 67461, 67463, 67504, 67506, 67514, 67584, 67589, 67592, 67592, 67594, 67637, 67639, 67640, 67644, 67644, 67647, 67669, 67680, 67702, 67712, 67742, 67808, 67826, 67828, 67829, 67840, 67861, 67872, 67897, 67968, 68023, 68030, 68031, 68096, 68096, 68112, 68115, 68117, 68119, 68121, 68149, 68192, 68220, 68224, 68252, 68288, 68295, 68297, 68324, 68352, 68405, 68416, 68437, 68448, 68466, 68480, 68497, 68608, 68680, 68736, 68786, 68800, 68850, 68864, 68899, 69248, 69289, 69296, 69297, 69376, 69404, 69415, 69415, 69424, 69445, 69488, 69505, 69552, 69572, 69600, 69622, 69635, 69687, 69745, 69746, 69749, 69749, 69763, 69807, 69840, 69864, 69891, 69926, 69956, 69956, 69959, 69959, 69968, 70002, 70006, 70006, 70019, 70066, 70081, 70084, 70106, 70106, 70108, 70108, 70144, 70161, 70163, 70187, 70207, 70208, 70272, 70278, 70280, 70280, 70282, 70285, 70287, 70301, 70303, 70312, 70320, 70366, 70405, 70412, 70415, 70416, 70419, 70440, 70442, 70448, 70450, 70451, 70453, 70457, 70461, 70461, 70480, 70480, 70493, 70497, 70656, 70708, 70727, 70730, 70751, 70753, 70784, 70831, 70852, 70853, 70855, 70855, 71040, 71086, 71128, 71131, 71168, 71215, 71236, 71236, 71296, 71338, 71352, 71352, 71424, 71450, 71488, 71494, 71680, 71723, 71840, 71903, 71935, 71942, 71945, 71945, 71948, 71955, 71957, 71958, 71960, 71983, 71999, 71999, 72001, 72001, 72096, 72103, 72106, 72144, 72161, 72161, 72163, 72163, 72192, 72192, 72203, 72242, 72250, 72250, 72272, 72272, 72284, 72329, 72349, 72349, 72368, 72440, 72704, 72712, 72714, 72750, 72768, 72768, 72818, 72847, 72960, 72966, 72968, 72969, 72971, 73008, 73030, 73030, 73056, 73061, 73063, 73064, 73066, 73097, 73112, 73112, 73440, 73458, 73474, 73474, 73476, 73488, 73490, 73523, 73648, 73648, 73728, 74649, 74752, 74862, 74880, 75075, 77712, 77808, 77824, 78895, 78913, 78918, 82944, 83526, 92160, 92728, 92736, 92766, 92784, 92862, 92880, 92909, 92928, 92975, 92992, 92995, 93027, 93047, 93053, 93071, 93760, 93823, 93952, 94026, 94032, 94032, 94099, 94111, 94176, 94177, 94179, 94179, 94208, 100343, 100352, 101589, 101632, 101640, 110576, 110579, 110581, 110587, 110589, 110590, 110592, 110882, 110898, 110898, 110928, 110930, 110933, 110933, 110948, 110951, 110960, 111355, 113664, 113770, 113776, 113788, 113792, 113800, 113808, 113817, 119808, 119892, 119894, 119964, 119966, 119967, 119970, 119970, 119973, 119974, 119977, 119980, 119982, 119993, 119995, 119995, 119997, 120003, 120005, 120069, 120071, 120074, 120077, 120084, 120086, 120092, 120094, 120121, 120123, 120126, 120128, 120132, 120134, 120134, 120138, 120144, 120146, 120485, 120488, 120512, 120514, 120538, 120540, 120570, 120572, 120596, 120598, 120628, 120630, 120654, 120656, 120686, 120688, 120712, 120714, 120744, 120746, 120770, 120772, 120779, 122624, 122654, 122661, 122666, 122928, 122989, 123136, 123180, 123191, 123197, 123214, 123214, 123536, 123565, 123584, 123627, 124112, 124139, 124896, 124902, 124904, 124907, 124909, 124910, 124912, 124926, 124928, 125124, 125184, 125251, 125259, 125259, 126464, 126467, 126469, 126495, 126497, 126498, 126500, 126500, 126503, 126503, 126505, 126514, 126516, 126519, 126521, 126521, 126523, 126523, 126530, 126530, 126535, 126535, 126537, 126537, 126539, 126539, 126541, 126543, 126545, 126546, 126548, 126548, 126551, 126551, 126553, 126553, 126555, 126555, 126557, 126557, 126559, 126559, 126561, 126562, 126564, 126564, 126567, 126570, 126572, 126578, 126580, 126583, 126585, 126588, 126590, 126590, 126592, 126601, 126603, 126619, 126625, 126627, 126629, 126633, 126635, 126651, 131072, 173791, 173824, 177977, 177984, 178205, 178208, 183969, 183984, 191456, 191472, 192093, 194560, 195101, 196608, 201546, 201552, 205743];
var unicodeESNextIdentifierPart ***REMOVED*** [48, 57, 65, 90, 95, 95, 97, 122, 170, 170, 181, 181, 183, 183, 186, 186, 192, 214, 216, 246, 248, 705, 710, 721, 736, 740, 748, 748, 750, 750, 768, 884, 886, 887, 890, 893, 895, 895, 902, 906, 908, 908, 910, 929, 931, 1013, 1015, 1153, 1155, 1159, 1162, 1327, 1329, 1366, 1369, 1369, 1376, 1416, 1425, 1469, 1471, 1471, 1473, 1474, 1476, 1477, 1479, 1479, 1488, 1514, 1519, 1522, 1552, 1562, 1568, 1641, 1646, 1747, 1749, 1756, 1759, 1768, 1770, 1788, 1791, 1791, 1808, 1866, 1869, 1969, 1984, 2037, 2042, 2042, 2045, 2045, 2048, 2093, 2112, 2139, 2144, 2154, 2160, 2183, 2185, 2190, 2200, 2273, 2275, 2403, 2406, 2415, 2417, 2435, 2437, 2444, 2447, 2448, 2451, 2472, 2474, 2480, 2482, 2482, 2486, 2489, 2492, 2500, 2503, 2504, 2507, 2510, 2519, 2519, 2524, 2525, 2527, 2531, 2534, 2545, 2556, 2556, 2558, 2558, 2561, 2563, 2565, 2570, 2575, 2576, 2579, 2600, 2602, 2608, 2610, 2611, 2613, 2614, 2616, 2617, 2620, 2620, 2622, 2626, 2631, 2632, 2635, 2637, 2641, 2641, 2649, 2652, 2654, 2654, 2662, 2677, 2689, 2691, 2693, 2701, 2703, 2705, 2707, 2728, 2730, 2736, 2738, 2739, 2741, 2745, 2748, 2757, 2759, 2761, 2763, 2765, 2768, 2768, 2784, 2787, 2790, 2799, 2809, 2815, 2817, 2819, 2821, 2828, 2831, 2832, 2835, 2856, 2858, 2864, 2866, 2867, 2869, 2873, 2876, 2884, 2887, 2888, 2891, 2893, 2901, 2903, 2908, 2909, 2911, 2915, 2918, 2927, 2929, 2929, 2946, 2947, 2949, 2954, 2958, 2960, 2962, 2965, 2969, 2970, 2972, 2972, 2974, 2975, 2979, 2980, 2984, 2986, 2990, 3001, 3006, 3010, 3014, 3016, 3018, 3021, 3024, 3024, 3031, 3031, 3046, 3055, 3072, 3084, 3086, 3088, 3090, 3112, 3114, 3129, 3132, 3140, 3142, 3144, 3146, 3149, 3157, 3158, 3160, 3162, 3165, 3165, 3168, 3171, 3174, 3183, 3200, 3203, 3205, 3212, 3214, 3216, 3218, 3240, 3242, 3251, 3253, 3257, 3260, 3268, 3270, 3272, 3274, 3277, 3285, 3286, 3293, 3294, 3296, 3299, 3302, 3311, 3313, 3315, 3328, 3340, 3342, 3344, 3346, 3396, 3398, 3400, 3402, 3406, 3412, 3415, 3423, 3427, 3430, 3439, 3450, 3455, 3457, 3459, 3461, 3478, 3482, 3505, 3507, 3515, 3517, 3517, 3520, 3526, 3530, 3530, 3535, 3540, 3542, 3542, 3544, 3551, 3558, 3567, 3570, 3571, 3585, 3642, 3648, 3662, 3664, 3673, 3713, 3714, 3716, 3716, 3718, 3722, 3724, 3747, 3749, 3749, 3751, 3773, 3776, 3780, 3782, 3782, 3784, 3790, 3792, 3801, 3804, 3807, 3840, 3840, 3864, 3865, 3872, 3881, 3893, 3893, 3895, 3895, 3897, 3897, 3902, 3911, 3913, 3948, 3953, 3972, 3974, 3991, 3993, 4028, 4038, 4038, 4096, 4169, 4176, 4253, 4256, 4293, 4295, 4295, 4301, 4301, 4304, 4346, 4348, 4680, 4682, 4685, 4688, 4694, 4696, 4696, 4698, 4701, 4704, 4744, 4746, 4749, 4752, 4784, 4786, 4789, 4792, 4798, 4800, 4800, 4802, 4805, 4808, 4822, 4824, 4880, 4882, 4885, 4888, 4954, 4957, 4959, 4969, 4977, 4992, 5007, 5024, 5109, 5112, 5117, 5121, 5740, 5743, 5759, 5761, 5786, 5792, 5866, 5870, 5880, 5888, 5909, 5919, 5940, 5952, 5971, 5984, 5996, 5998, 6e3, 6002, 6003, 6016, 6099, 6103, 6103, 6108, 6109, 6112, 6121, 6155, 6157, 6159, 6169, 6176, 6264, 6272, 6314, 6320, 6389, 6400, 6430, 6432, 6443, 6448, 6459, 6470, 6509, 6512, 6516, 6528, 6571, 6576, 6601, 6608, 6618, 6656, 6683, 6688, 6750, 6752, 6780, 6783, 6793, 6800, 6809, 6823, 6823, 6832, 6845, 6847, 6862, 6912, 6988, 6992, 7001, 7019, 7027, 7040, 7155, 7168, 7223, 7232, 7241, 7245, 7293, 7296, 7304, 7312, 7354, 7357, 7359, 7376, 7378, 7380, 7418, 7424, 7957, 7960, 7965, 7968, 8005, 8008, 8013, 8016, 8023, 8025, 8025, 8027, 8027, 8029, 8029, 8031, 8061, 8064, 8116, 8118, 8124, 8126, 8126, 8130, 8132, 8134, 8140, 8144, 8147, 8150, 8155, 8160, 8172, 8178, 8180, 8182, 8188, 8204, 8205, 8255, 8256, 8276, 8276, 8305, 8305, 8319, 8319, 8336, 8348, 8400, 8412, 8417, 8417, 8421, 8432, 8450, 8450, 8455, 8455, 8458, 8467, 8469, 8469, 8472, 8477, 8484, 8484, 8486, 8486, 8488, 8488, 8490, 8505, 8508, 8511, 8517, 8521, 8526, 8526, 8544, 8584, 11264, 11492, 11499, 11507, 11520, 11557, 11559, 11559, 11565, 11565, 11568, 11623, 11631, 11631, 11647, 11670, 11680, 11686, 11688, 11694, 11696, 11702, 11704, 11710, 11712, 11718, 11720, 11726, 11728, 11734, 11736, 11742, 11744, 11775, 12293, 12295, 12321, 12335, 12337, 12341, 12344, 12348, 12353, 12438, 12441, 12447, 12449, 12543, 12549, 12591, 12593, 12686, 12704, 12735, 12784, 12799, 13312, 19903, 19968, 42124, 42192, 42237, 42240, 42508, 42512, 42539, 42560, 42607, 42612, 42621, 42623, 42737, 42775, 42783, 42786, 42888, 42891, 42954, 42960, 42961, 42963, 42963, 42965, 42969, 42994, 43047, 43052, 43052, 43072, 43123, 43136, 43205, 43216, 43225, 43232, 43255, 43259, 43259, 43261, 43309, 43312, 43347, 43360, 43388, 43392, 43456, 43471, 43481, 43488, 43518, 43520, 43574, 43584, 43597, 43600, 43609, 43616, 43638, 43642, 43714, 43739, 43741, 43744, 43759, 43762, 43766, 43777, 43782, 43785, 43790, 43793, 43798, 43808, 43814, 43816, 43822, 43824, 43866, 43868, 43881, 43888, 44010, 44012, 44013, 44016, 44025, 44032, 55203, 55216, 55238, 55243, 55291, 63744, 64109, 64112, 64217, 64256, 64262, 64275, 64279, 64285, 64296, 64298, 64310, 64312, 64316, 64318, 64318, 64320, 64321, 64323, 64324, 64326, 64433, 64467, 64829, 64848, 64911, 64914, 64967, 65008, 65019, 65024, 65039, 65056, 65071, 65075, 65076, 65101, 65103, 65136, 65140, 65142, 65276, 65296, 65305, 65313, 65338, 65343, 65343, 65345, 65370, 65381, 65470, 65474, 65479, 65482, 65487, 65490, 65495, 65498, 65500, 65536, 65547, 65549, 65574, 65576, 65594, 65596, 65597, 65599, 65613, 65616, 65629, 65664, 65786, 65856, 65908, 66045, 66045, 66176, 66204, 66208, 66256, 66272, 66272, 66304, 66335, 66349, 66378, 66384, 66426, 66432, 66461, 66464, 66499, 66504, 66511, 66513, 66517, 66560, 66717, 66720, 66729, 66736, 66771, 66776, 66811, 66816, 66855, 66864, 66915, 66928, 66938, 66940, 66954, 66956, 66962, 66964, 66965, 66967, 66977, 66979, 66993, 66995, 67001, 67003, 67004, 67072, 67382, 67392, 67413, 67424, 67431, 67456, 67461, 67463, 67504, 67506, 67514, 67584, 67589, 67592, 67592, 67594, 67637, 67639, 67640, 67644, 67644, 67647, 67669, 67680, 67702, 67712, 67742, 67808, 67826, 67828, 67829, 67840, 67861, 67872, 67897, 67968, 68023, 68030, 68031, 68096, 68099, 68101, 68102, 68108, 68115, 68117, 68119, 68121, 68149, 68152, 68154, 68159, 68159, 68192, 68220, 68224, 68252, 68288, 68295, 68297, 68326, 68352, 68405, 68416, 68437, 68448, 68466, 68480, 68497, 68608, 68680, 68736, 68786, 68800, 68850, 68864, 68903, 68912, 68921, 69248, 69289, 69291, 69292, 69296, 69297, 69373, 69404, 69415, 69415, 69424, 69456, 69488, 69509, 69552, 69572, 69600, 69622, 69632, 69702, 69734, 69749, 69759, 69818, 69826, 69826, 69840, 69864, 69872, 69881, 69888, 69940, 69942, 69951, 69956, 69959, 69968, 70003, 70006, 70006, 70016, 70084, 70089, 70092, 70094, 70106, 70108, 70108, 70144, 70161, 70163, 70199, 70206, 70209, 70272, 70278, 70280, 70280, 70282, 70285, 70287, 70301, 70303, 70312, 70320, 70378, 70384, 70393, 70400, 70403, 70405, 70412, 70415, 70416, 70419, 70440, 70442, 70448, 70450, 70451, 70453, 70457, 70459, 70468, 70471, 70472, 70475, 70477, 70480, 70480, 70487, 70487, 70493, 70499, 70502, 70508, 70512, 70516, 70656, 70730, 70736, 70745, 70750, 70753, 70784, 70853, 70855, 70855, 70864, 70873, 71040, 71093, 71096, 71104, 71128, 71133, 71168, 71232, 71236, 71236, 71248, 71257, 71296, 71352, 71360, 71369, 71424, 71450, 71453, 71467, 71472, 71481, 71488, 71494, 71680, 71738, 71840, 71913, 71935, 71942, 71945, 71945, 71948, 71955, 71957, 71958, 71960, 71989, 71991, 71992, 71995, 72003, 72016, 72025, 72096, 72103, 72106, 72151, 72154, 72161, 72163, 72164, 72192, 72254, 72263, 72263, 72272, 72345, 72349, 72349, 72368, 72440, 72704, 72712, 72714, 72758, 72760, 72768, 72784, 72793, 72818, 72847, 72850, 72871, 72873, 72886, 72960, 72966, 72968, 72969, 72971, 73014, 73018, 73018, 73020, 73021, 73023, 73031, 73040, 73049, 73056, 73061, 73063, 73064, 73066, 73102, 73104, 73105, 73107, 73112, 73120, 73129, 73440, 73462, 73472, 73488, 73490, 73530, 73534, 73538, 73552, 73561, 73648, 73648, 73728, 74649, 74752, 74862, 74880, 75075, 77712, 77808, 77824, 78895, 78912, 78933, 82944, 83526, 92160, 92728, 92736, 92766, 92768, 92777, 92784, 92862, 92864, 92873, 92880, 92909, 92912, 92916, 92928, 92982, 92992, 92995, 93008, 93017, 93027, 93047, 93053, 93071, 93760, 93823, 93952, 94026, 94031, 94087, 94095, 94111, 94176, 94177, 94179, 94180, 94192, 94193, 94208, 100343, 100352, 101589, 101632, 101640, 110576, 110579, 110581, 110587, 110589, 110590, 110592, 110882, 110898, 110898, 110928, 110930, 110933, 110933, 110948, 110951, 110960, 111355, 113664, 113770, 113776, 113788, 113792, 113800, 113808, 113817, 113821, 113822, 118528, 118573, 118576, 118598, 119141, 119145, 119149, 119154, 119163, 119170, 119173, 119179, 119210, 119213, 119362, 119364, 119808, 119892, 119894, 119964, 119966, 119967, 119970, 119970, 119973, 119974, 119977, 119980, 119982, 119993, 119995, 119995, 119997, 120003, 120005, 120069, 120071, 120074, 120077, 120084, 120086, 120092, 120094, 120121, 120123, 120126, 120128, 120132, 120134, 120134, 120138, 120144, 120146, 120485, 120488, 120512, 120514, 120538, 120540, 120570, 120572, 120596, 120598, 120628, 120630, 120654, 120656, 120686, 120688, 120712, 120714, 120744, 120746, 120770, 120772, 120779, 120782, 120831, 121344, 121398, 121403, 121452, 121461, 121461, 121476, 121476, 121499, 121503, 121505, 121519, 122624, 122654, 122661, 122666, 122880, 122886, 122888, 122904, 122907, 122913, 122915, 122916, 122918, 122922, 122928, 122989, 123023, 123023, 123136, 123180, 123184, 123197, 123200, 123209, 123214, 123214, 123536, 123566, 123584, 123641, 124112, 124153, 124896, 124902, 124904, 124907, 124909, 124910, 124912, 124926, 124928, 125124, 125136, 125142, 125184, 125259, 125264, 125273, 126464, 126467, 126469, 126495, 126497, 126498, 126500, 126500, 126503, 126503, 126505, 126514, 126516, 126519, 126521, 126521, 126523, 126523, 126530, 126530, 126535, 126535, 126537, 126537, 126539, 126539, 126541, 126543, 126545, 126546, 126548, 126548, 126551, 126551, 126553, 126553, 126555, 126555, 126557, 126557, 126559, 126559, 126561, 126562, 126564, 126564, 126567, 126570, 126572, 126578, 126580, 126583, 126585, 126588, 126590, 126590, 126592, 126601, 126603, 126619, 126625, 126627, 126629, 126633, 126635, 126651, 130032, 130041, 131072, 173791, 173824, 177977, 177984, 178205, 178208, 183969, 183984, 191456, 191472, 192093, 194560, 195101, 196608, 201546, 201552, 205743, 917760, 917999];
var commentDirectiveRegExSingleLine ***REMOVED*** /^\/\/\/?\s*@(ts-expect-error|ts-ignore)/;
var commentDirectiveRegExMultiLine ***REMOVED*** /^(?:\/|\*)*\s*@(ts-expect-error|ts-ignore)/;
var jsDocSeeOrLink ***REMOVED*** /@(?:see|link)/i;
function lookupInUnicodeMap(code, map2) {
  if (code < map2[0]) {
    return false;
  }
  let lo ***REMOVED*** 0;
  let hi ***REMOVED*** map2.length;
  let mid;
  while (lo + 1 < hi) {
    mid ***REMOVED*** lo + (hi - lo) / 2;
    mid -***REMOVED*** mid % 2;
    if (map2[mid] <***REMOVED*** code && code <***REMOVED*** map2[mid + 1]) {
      return true;
    }
    if (code < map2[mid]) {
      hi ***REMOVED*** mid;
    } else {
      lo ***REMOVED*** mid + 2;
    }
  }
  return false;
}
function isUnicodeIdentifierStart(code, languageVersion) {
  return languageVersion >***REMOVED*** 2 /* ES2015 */ ? lookupInUnicodeMap(code, unicodeESNextIdentifierStart) : lookupInUnicodeMap(code, unicodeES5IdentifierStart);
}
function isUnicodeIdentifierPart(code, languageVersion) {
  return languageVersion >***REMOVED*** 2 /* ES2015 */ ? lookupInUnicodeMap(code, unicodeESNextIdentifierPart) : lookupInUnicodeMap(code, unicodeES5IdentifierPart);
}
function makeReverseMap(source) {
  const result ***REMOVED*** [];
  source.forEach((value, name) ***REMOVED***> {
    result[value] ***REMOVED*** name;
  });
  return result;
}
var tokenStrings ***REMOVED*** makeReverseMap(textToToken);
function tokenToString(t) {
  return tokenStrings[t];
}
function stringToToken(s) {
  return textToToken.get(s);
}
var regExpFlagCharCodes ***REMOVED*** makeReverseMap(charCodeToRegExpFlag);
function characterCodeToRegularExpressionFlag(ch) {
  return charCodeToRegExpFlag.get(ch);
}
function computeLineStarts(text) {
  const result ***REMOVED*** [];
  let pos ***REMOVED*** 0;
  let lineStart ***REMOVED*** 0;
  while (pos < text.length) {
    const ch ***REMOVED*** text.charCodeAt(pos);
    pos++;
    switch (ch) {
      case 13 /* carriageReturn */:
        if (text.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
          pos++;
        }
      // falls through
      case 10 /* lineFeed */:
        result.push(lineStart);
        lineStart ***REMOVED*** pos;
        break;
      default:
        if (ch > 127 /* maxAsciiCharacter */ && isLineBreak(ch)) {
          result.push(lineStart);
          lineStart ***REMOVED*** pos;
        }
        break;
    }
  }
  result.push(lineStart);
  return result;
}
function getPositionOfLineAndCharacter(sourceFile, line, character, allowEdits) {
  return sourceFile.getPositionOfLineAndCharacter ? sourceFile.getPositionOfLineAndCharacter(line, character, allowEdits) : computePositionOfLineAndCharacter(getLineStarts(sourceFile), line, character, sourceFile.text, allowEdits);
}
function computePositionOfLineAndCharacter(lineStarts, line, character, debugText, allowEdits) {
  if (line < 0 || line >***REMOVED*** lineStarts.length) {
    if (allowEdits) {
      line ***REMOVED*** line < 0 ? 0 : line >***REMOVED*** lineStarts.length ? lineStarts.length - 1 : line;
    } else {
      Debug.fail(`Bad line number. Line: ${line}, lineStarts.length: ${lineStarts.length} , line map is correct? ${debugText !***REMOVED******REMOVED*** void 0 ? arrayIsEqualTo(lineStarts, computeLineStarts(debugText)) : "unknown"}`);
    }
  }
  const res ***REMOVED*** lineStarts[line] + character;
  if (allowEdits) {
    return res > lineStarts[line + 1] ? lineStarts[line + 1] : typeof debugText ***REMOVED******REMOVED******REMOVED*** "string" && res > debugText.length ? debugText.length : res;
  }
  if (line < lineStarts.length - 1) {
    Debug.assert(res < lineStarts[line + 1]);
  } else if (debugText !***REMOVED******REMOVED*** void 0) {
    Debug.assert(res <***REMOVED*** debugText.length);
  }
  return res;
}
function getLineStarts(sourceFile) {
  return sourceFile.lineMap || (sourceFile.lineMap ***REMOVED*** computeLineStarts(sourceFile.text));
}
function computeLineAndCharacterOfPosition(lineStarts, position) {
  const lineNumber ***REMOVED*** computeLineOfPosition(lineStarts, position);
  return {
    line: lineNumber,
    character: position - lineStarts[lineNumber]
  };
}
function computeLineOfPosition(lineStarts, position, lowerBound) {
  let lineNumber ***REMOVED*** binarySearch(lineStarts, position, identity, compareValues, lowerBound);
  if (lineNumber < 0) {
    lineNumber ***REMOVED*** ~lineNumber - 1;
    Debug.assert(lineNumber !***REMOVED******REMOVED*** -1, "position cannot precede the beginning of the file");
  }
  return lineNumber;
}
function getLinesBetweenPositions(sourceFile, pos1, pos2) {
  if (pos1 ***REMOVED******REMOVED******REMOVED*** pos2) return 0;
  const lineStarts ***REMOVED*** getLineStarts(sourceFile);
  const lower ***REMOVED*** Math.min(pos1, pos2);
  const isNegative ***REMOVED*** lower ***REMOVED******REMOVED******REMOVED*** pos2;
  const upper ***REMOVED*** isNegative ? pos1 : pos2;
  const lowerLine ***REMOVED*** computeLineOfPosition(lineStarts, lower);
  const upperLine ***REMOVED*** computeLineOfPosition(lineStarts, upper, lowerLine);
  return isNegative ? lowerLine - upperLine : upperLine - lowerLine;
}
function getLineAndCharacterOfPosition(sourceFile, position) {
  return computeLineAndCharacterOfPosition(getLineStarts(sourceFile), position);
}
function isWhiteSpaceLike(ch) {
  return isWhiteSpaceSingleLine(ch) || isLineBreak(ch);
}
function isWhiteSpaceSingleLine(ch) {
  return ch ***REMOVED******REMOVED******REMOVED*** 32 /* space */ || ch ***REMOVED******REMOVED******REMOVED*** 9 /* tab */ || ch ***REMOVED******REMOVED******REMOVED*** 11 /* verticalTab */ || ch ***REMOVED******REMOVED******REMOVED*** 12 /* formFeed */ || ch ***REMOVED******REMOVED******REMOVED*** 160 /* nonBreakingSpace */ || ch ***REMOVED******REMOVED******REMOVED*** 133 /* nextLine */ || ch ***REMOVED******REMOVED******REMOVED*** 5760 /* ogham */ || ch >***REMOVED*** 8192 /* enQuad */ && ch <***REMOVED*** 8203 /* zeroWidthSpace */ || ch ***REMOVED******REMOVED******REMOVED*** 8239 /* narrowNoBreakSpace */ || ch ***REMOVED******REMOVED******REMOVED*** 8287 /* mathematicalSpace */ || ch ***REMOVED******REMOVED******REMOVED*** 12288 /* ideographicSpace */ || ch ***REMOVED******REMOVED******REMOVED*** 65279 /* byteOrderMark */;
}
function isLineBreak(ch) {
  return ch ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */ || ch ***REMOVED******REMOVED******REMOVED*** 13 /* carriageReturn */ || ch ***REMOVED******REMOVED******REMOVED*** 8232 /* lineSeparator */ || ch ***REMOVED******REMOVED******REMOVED*** 8233 /* paragraphSeparator */;
}
function isDigit(ch) {
  return ch >***REMOVED*** 48 /* _0 */ && ch <***REMOVED*** 57 /* _9 */;
}
function isHexDigit(ch) {
  return isDigit(ch) || ch >***REMOVED*** 65 /* A */ && ch <***REMOVED*** 70 /* F */ || ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 102 /* f */;
}
function isASCIILetter(ch) {
  return ch >***REMOVED*** 65 /* A */ && ch <***REMOVED*** 90 /* Z */ || ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 122 /* z */;
}
function isWordCharacter(ch) {
  return isASCIILetter(ch) || isDigit(ch) || ch ***REMOVED******REMOVED******REMOVED*** 95 /* _ */;
}
function isOctalDigit(ch) {
  return ch >***REMOVED*** 48 /* _0 */ && ch <***REMOVED*** 55 /* _7 */;
}
function skipTrivia(text, pos, stopAfterLineBreak, stopAtComments, inJSDoc) {
  if (positionIsSynthesized(pos)) {
    return pos;
  }
  let canConsumeStar ***REMOVED*** false;
  while (true) {
    const ch ***REMOVED*** text.charCodeAt(pos);
    switch (ch) {
      case 13 /* carriageReturn */:
        if (text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
          pos++;
        }
      // falls through
      case 10 /* lineFeed */:
        pos++;
        if (stopAfterLineBreak) {
          return pos;
        }
        canConsumeStar ***REMOVED*** !!inJSDoc;
        continue;
      case 9 /* tab */:
      case 11 /* verticalTab */:
      case 12 /* formFeed */:
      case 32 /* space */:
        pos++;
        continue;
      case 47 /* slash */:
        if (stopAtComments) {
          break;
        }
        if (text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
          pos +***REMOVED*** 2;
          while (pos < text.length) {
            if (isLineBreak(text.charCodeAt(pos))) {
              break;
            }
            pos++;
          }
          canConsumeStar ***REMOVED*** false;
          continue;
        }
        if (text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
          pos +***REMOVED*** 2;
          while (pos < text.length) {
            if (text.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
              pos +***REMOVED*** 2;
              break;
            }
            pos++;
          }
          canConsumeStar ***REMOVED*** false;
          continue;
        }
        break;
      case 60 /* lessThan */:
      case 124 /* bar */:
      case 61 /* equals */:
      case 62 /* greaterThan */:
        if (isConflictMarkerTrivia(text, pos)) {
          pos ***REMOVED*** scanConflictMarkerTrivia(text, pos);
          canConsumeStar ***REMOVED*** false;
          continue;
        }
        break;
      case 35 /* hash */:
        if (pos ***REMOVED******REMOVED******REMOVED*** 0 && isShebangTrivia(text, pos)) {
          pos ***REMOVED*** scanShebangTrivia(text, pos);
          canConsumeStar ***REMOVED*** false;
          continue;
        }
        break;
      case 42 /* asterisk */:
        if (canConsumeStar) {
          pos++;
          canConsumeStar ***REMOVED*** false;
          continue;
        }
        break;
      default:
        if (ch > 127 /* maxAsciiCharacter */ && isWhiteSpaceLike(ch)) {
          pos++;
          continue;
        }
        break;
    }
    return pos;
  }
}
var mergeConflictMarkerLength ***REMOVED*** "<<<<<<<".length;
function isConflictMarkerTrivia(text, pos) {
  Debug.assert(pos >***REMOVED*** 0);
  if (pos ***REMOVED******REMOVED******REMOVED*** 0 || isLineBreak(text.charCodeAt(pos - 1))) {
    const ch ***REMOVED*** text.charCodeAt(pos);
    if (pos + mergeConflictMarkerLength < text.length) {
      for (let i ***REMOVED*** 0; i < mergeConflictMarkerLength; i++) {
        if (text.charCodeAt(pos + i) !***REMOVED******REMOVED*** ch) {
          return false;
        }
      }
      return ch ***REMOVED******REMOVED******REMOVED*** 61 /* equals */ || text.charCodeAt(pos + mergeConflictMarkerLength) ***REMOVED******REMOVED******REMOVED*** 32 /* space */;
    }
  }
  return false;
}
function scanConflictMarkerTrivia(text, pos, error) {
  if (error) {
    error(Diagnostics.Merge_conflict_marker_encountered, pos, mergeConflictMarkerLength);
  }
  const ch ***REMOVED*** text.charCodeAt(pos);
  const len ***REMOVED*** text.length;
  if (ch ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */ || ch ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) {
    while (pos < len && !isLineBreak(text.charCodeAt(pos))) {
      pos++;
    }
  } else {
    Debug.assert(ch ***REMOVED******REMOVED******REMOVED*** 124 /* bar */ || ch ***REMOVED******REMOVED******REMOVED*** 61 /* equals */);
    while (pos < len) {
      const currentChar ***REMOVED*** text.charCodeAt(pos);
      if ((currentChar ***REMOVED******REMOVED******REMOVED*** 61 /* equals */ || currentChar ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) && currentChar !***REMOVED******REMOVED*** ch && isConflictMarkerTrivia(text, pos)) {
        break;
      }
      pos++;
    }
  }
  return pos;
}
var shebangTriviaRegex ***REMOVED*** /^#!.*/;
function isShebangTrivia(text, pos) {
  Debug.assert(pos ***REMOVED******REMOVED******REMOVED*** 0);
  return shebangTriviaRegex.test(text);
}
function scanShebangTrivia(text, pos) {
  const shebang ***REMOVED*** shebangTriviaRegex.exec(text)[0];
  pos ***REMOVED*** pos + shebang.length;
  return pos;
}
function iterateCommentRanges(reduce, text, pos, trailing, cb, state, initial) {
  let pendingPos;
  let pendingEnd;
  let pendingKind;
  let pendingHasTrailingNewLine;
  let hasPendingCommentRange ***REMOVED*** false;
  let collecting ***REMOVED*** trailing;
  let accumulator ***REMOVED*** initial;
  if (pos ***REMOVED******REMOVED******REMOVED*** 0) {
    collecting ***REMOVED*** true;
    const shebang ***REMOVED*** getShebang(text);
    if (shebang) {
      pos ***REMOVED*** shebang.length;
    }
  }
  scan:
    while (pos >***REMOVED*** 0 && pos < text.length) {
      const ch ***REMOVED*** text.charCodeAt(pos);
      switch (ch) {
        case 13 /* carriageReturn */:
          if (text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
            pos++;
          }
        // falls through
        case 10 /* lineFeed */:
          pos++;
          if (trailing) {
            break scan;
          }
          collecting ***REMOVED*** true;
          if (hasPendingCommentRange) {
            pendingHasTrailingNewLine ***REMOVED*** true;
          }
          continue;
        case 9 /* tab */:
        case 11 /* verticalTab */:
        case 12 /* formFeed */:
        case 32 /* space */:
          pos++;
          continue;
        case 47 /* slash */:
          const nextChar ***REMOVED*** text.charCodeAt(pos + 1);
          let hasTrailingNewLine ***REMOVED*** false;
          if (nextChar ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ || nextChar ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
            const kind ***REMOVED*** nextChar ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ ? 2 /* SingleLineCommentTrivia */ : 3 /* MultiLineCommentTrivia */;
            const startPos ***REMOVED*** pos;
            pos +***REMOVED*** 2;
            if (nextChar ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
              while (pos < text.length) {
                if (isLineBreak(text.charCodeAt(pos))) {
                  hasTrailingNewLine ***REMOVED*** true;
                  break;
                }
                pos++;
              }
            } else {
              while (pos < text.length) {
                if (text.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
                  pos +***REMOVED*** 2;
                  break;
                }
                pos++;
              }
            }
            if (collecting) {
              if (hasPendingCommentRange) {
                accumulator ***REMOVED*** cb(pendingPos, pendingEnd, pendingKind, pendingHasTrailingNewLine, state, accumulator);
                if (!reduce && accumulator) {
                  return accumulator;
                }
              }
              pendingPos ***REMOVED*** startPos;
              pendingEnd ***REMOVED*** pos;
              pendingKind ***REMOVED*** kind;
              pendingHasTrailingNewLine ***REMOVED*** hasTrailingNewLine;
              hasPendingCommentRange ***REMOVED*** true;
            }
            continue;
          }
          break scan;
        default:
          if (ch > 127 /* maxAsciiCharacter */ && isWhiteSpaceLike(ch)) {
            if (hasPendingCommentRange && isLineBreak(ch)) {
              pendingHasTrailingNewLine ***REMOVED*** true;
            }
            pos++;
            continue;
          }
          break scan;
      }
    }
  if (hasPendingCommentRange) {
    accumulator ***REMOVED*** cb(pendingPos, pendingEnd, pendingKind, pendingHasTrailingNewLine, state, accumulator);
  }
  return accumulator;
}
function forEachLeadingCommentRange(text, pos, cb, state) {
  return iterateCommentRanges(
    /*reduce*/
    false,
    text,
    pos,
    /*trailing*/
    false,
    cb,
    state
  );
}
function forEachTrailingCommentRange(text, pos, cb, state) {
  return iterateCommentRanges(
    /*reduce*/
    false,
    text,
    pos,
    /*trailing*/
    true,
    cb,
    state
  );
}
function reduceEachLeadingCommentRange(text, pos, cb, state, initial) {
  return iterateCommentRanges(
    /*reduce*/
    true,
    text,
    pos,
    /*trailing*/
    false,
    cb,
    state,
    initial
  );
}
function reduceEachTrailingCommentRange(text, pos, cb, state, initial) {
  return iterateCommentRanges(
    /*reduce*/
    true,
    text,
    pos,
    /*trailing*/
    true,
    cb,
    state,
    initial
  );
}
function appendCommentRange(pos, end, kind, hasTrailingNewLine, _state, comments ***REMOVED*** []) {
  comments.push({ kind, pos, end, hasTrailingNewLine });
  return comments;
}
function getLeadingCommentRanges(text, pos) {
  return reduceEachLeadingCommentRange(
    text,
    pos,
    appendCommentRange,
    /*state*/
    void 0,
    /*initial*/
    void 0
  );
}
function getTrailingCommentRanges(text, pos) {
  return reduceEachTrailingCommentRange(
    text,
    pos,
    appendCommentRange,
    /*state*/
    void 0,
    /*initial*/
    void 0
  );
}
function getShebang(text) {
  const match ***REMOVED*** shebangTriviaRegex.exec(text);
  if (match) {
    return match[0];
  }
}
function isIdentifierStart(ch, languageVersion) {
  return isASCIILetter(ch) || ch ***REMOVED******REMOVED******REMOVED*** 36 /* $ */ || ch ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ || ch > 127 /* maxAsciiCharacter */ && isUnicodeIdentifierStart(ch, languageVersion);
}
function isIdentifierPart(ch, languageVersion, identifierVariant) {
  return isWordCharacter(ch) || ch ***REMOVED******REMOVED******REMOVED*** 36 /* $ */ || // "-" and ":" are valid in JSX Identifiers
  (identifierVariant ***REMOVED******REMOVED******REMOVED*** 1 /* JSX */ ? ch ***REMOVED******REMOVED******REMOVED*** 45 /* minus */ || ch ***REMOVED******REMOVED******REMOVED*** 58 /* colon */ : false) || ch > 127 /* maxAsciiCharacter */ && isUnicodeIdentifierPart(ch, languageVersion);
}
function isIdentifierText(name, languageVersion, identifierVariant) {
  let ch ***REMOVED*** codePointAt(name, 0);
  if (!isIdentifierStart(ch, languageVersion)) {
    return false;
  }
  for (let i ***REMOVED*** charSize(ch); i < name.length; i +***REMOVED*** charSize(ch)) {
    if (!isIdentifierPart(ch ***REMOVED*** codePointAt(name, i), languageVersion, identifierVariant)) {
      return false;
    }
  }
  return true;
}
function createScanner(languageVersion, skipTrivia2, languageVariant ***REMOVED*** 0 /* Standard */, textInitial, onError, start, length2) {
  var text ***REMOVED*** textInitial;
  var pos;
  var end;
  var fullStartPos;
  var tokenStart;
  var token;
  var tokenValue;
  var tokenFlags;
  var commentDirectives;
  var skipJsDocLeadingAsterisks ***REMOVED*** 0;
  var scriptKind ***REMOVED*** 0 /* Unknown */;
  var jsDocParsingMode ***REMOVED*** 0 /* ParseAll */;
  setText(text, start, length2);
  var scanner ***REMOVED*** {
    getTokenFullStart: () ***REMOVED***> fullStartPos,
    getStartPos: () ***REMOVED***> fullStartPos,
    getTokenEnd: () ***REMOVED***> pos,
    getTextPos: () ***REMOVED***> pos,
    getToken: () ***REMOVED***> token,
    getTokenStart: () ***REMOVED***> tokenStart,
    getTokenPos: () ***REMOVED***> tokenStart,
    getTokenText: () ***REMOVED***> text.substring(tokenStart, pos),
    getTokenValue: () ***REMOVED***> tokenValue,
    hasUnicodeEscape: () ***REMOVED***> (tokenFlags & 1024 /* UnicodeEscape */) !***REMOVED******REMOVED*** 0,
    hasExtendedUnicodeEscape: () ***REMOVED***> (tokenFlags & 8 /* ExtendedUnicodeEscape */) !***REMOVED******REMOVED*** 0,
    hasPrecedingLineBreak: () ***REMOVED***> (tokenFlags & 1 /* PrecedingLineBreak */) !***REMOVED******REMOVED*** 0,
    hasPrecedingJSDocComment: () ***REMOVED***> (tokenFlags & 2 /* PrecedingJSDocComment */) !***REMOVED******REMOVED*** 0,
    hasPrecedingJSDocLeadingAsterisks: () ***REMOVED***> (tokenFlags & 32768 /* PrecedingJSDocLeadingAsterisks */) !***REMOVED******REMOVED*** 0,
    isIdentifier: () ***REMOVED***> token ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || token > 118 /* LastReservedWord */,
    isReservedWord: () ***REMOVED***> token >***REMOVED*** 83 /* FirstReservedWord */ && token <***REMOVED*** 118 /* LastReservedWord */,
    isUnterminated: () ***REMOVED***> (tokenFlags & 4 /* Unterminated */) !***REMOVED******REMOVED*** 0,
    getCommentDirectives: () ***REMOVED***> commentDirectives,
    getNumericLiteralFlags: () ***REMOVED***> tokenFlags & 25584 /* NumericLiteralFlags */,
    getTokenFlags: () ***REMOVED***> tokenFlags,
    reScanGreaterToken,
    reScanAsteriskEqualsToken,
    reScanSlashToken,
    reScanTemplateToken,
    reScanTemplateHeadOrNoSubstitutionTemplate,
    scanJsxIdentifier,
    scanJsxAttributeValue,
    reScanJsxAttributeValue,
    reScanJsxToken,
    reScanLessThanToken,
    reScanHashToken,
    reScanQuestionToken,
    reScanInvalidIdentifier,
    scanJsxToken,
    scanJsDocToken,
    scanJSDocCommentTextToken,
    scan,
    getText,
    clearCommentDirectives,
    setText,
    setScriptTarget,
    setLanguageVariant,
    setScriptKind,
    setJSDocParsingMode,
    setOnError,
    resetTokenState,
    setTextPos: resetTokenState,
    setSkipJsDocLeadingAsterisks,
    tryScan,
    lookAhead,
    scanRange
  };
  if (Debug.isDebugging) {
    Object.defineProperty(scanner, "__debugShowCurrentPositionInText", {
      get: () ***REMOVED***> {
        const text2 ***REMOVED*** scanner.getText();
        return text2.slice(0, scanner.getTokenFullStart()) + "\u2551" + text2.slice(scanner.getTokenFullStart());
      }
    });
  }
  return scanner;
  function codePointUnchecked(pos2) {
    return codePointAt(text, pos2);
  }
  function codePointChecked(pos2) {
    return pos2 >***REMOVED*** 0 && pos2 < end ? codePointUnchecked(pos2) : -1 /* EOF */;
  }
  function charCodeUnchecked(pos2) {
    return text.charCodeAt(pos2);
  }
  function charCodeChecked(pos2) {
    return pos2 >***REMOVED*** 0 && pos2 < end ? charCodeUnchecked(pos2) : -1 /* EOF */;
  }
  function error(message, errPos ***REMOVED*** pos, length3, arg0) {
    if (onError) {
      const oldPos ***REMOVED*** pos;
      pos ***REMOVED*** errPos;
      onError(message, length3 || 0, arg0);
      pos ***REMOVED*** oldPos;
    }
  }
  function scanNumberFragment() {
    let start2 ***REMOVED*** pos;
    let allowSeparator ***REMOVED*** false;
    let isPreviousTokenSeparator ***REMOVED*** false;
    let result ***REMOVED*** "";
    while (true) {
      const ch ***REMOVED*** charCodeUnchecked(pos);
      if (ch ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
        tokenFlags |***REMOVED*** 512 /* ContainsSeparator */;
        if (allowSeparator) {
          allowSeparator ***REMOVED*** false;
          isPreviousTokenSeparator ***REMOVED*** true;
          result +***REMOVED*** text.substring(start2, pos);
        } else {
          tokenFlags |***REMOVED*** 16384 /* ContainsInvalidSeparator */;
          if (isPreviousTokenSeparator) {
            error(Diagnostics.Multiple_consecutive_numeric_separators_are_not_permitted, pos, 1);
          } else {
            error(Diagnostics.Numeric_separators_are_not_allowed_here, pos, 1);
          }
        }
        pos++;
        start2 ***REMOVED*** pos;
        continue;
      }
      if (isDigit(ch)) {
        allowSeparator ***REMOVED*** true;
        isPreviousTokenSeparator ***REMOVED*** false;
        pos++;
        continue;
      }
      break;
    }
    if (charCodeUnchecked(pos - 1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
      tokenFlags |***REMOVED*** 16384 /* ContainsInvalidSeparator */;
      error(Diagnostics.Numeric_separators_are_not_allowed_here, pos - 1, 1);
    }
    return result + text.substring(start2, pos);
  }
  function scanNumber() {
    let start2 ***REMOVED*** pos;
    let mainFragment;
    if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 48 /* _0 */) {
      pos++;
      if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
        tokenFlags |***REMOVED*** 512 /* ContainsSeparator */ | 16384 /* ContainsInvalidSeparator */;
        error(Diagnostics.Numeric_separators_are_not_allowed_here, pos, 1);
        pos--;
        mainFragment ***REMOVED*** scanNumberFragment();
      } else if (!scanDigits()) {
        tokenFlags |***REMOVED*** 8192 /* ContainsLeadingZero */;
        mainFragment ***REMOVED*** "" + +tokenValue;
      } else if (!tokenValue) {
        mainFragment ***REMOVED*** "0";
      } else {
        tokenValue ***REMOVED*** "" + parseInt(tokenValue, 8);
        tokenFlags |***REMOVED*** 32 /* Octal */;
        const withMinus ***REMOVED*** token ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */;
        const literal ***REMOVED*** (withMinus ? "-" : "") + "0o" + (+tokenValue).toString(8);
        if (withMinus) start2--;
        error(Diagnostics.Octal_literals_are_not_allowed_Use_the_syntax_0, start2, pos - start2, literal);
        return 9 /* NumericLiteral */;
      }
    } else {
      mainFragment ***REMOVED*** scanNumberFragment();
    }
    let decimalFragment;
    let scientificFragment;
    if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */) {
      pos++;
      decimalFragment ***REMOVED*** scanNumberFragment();
    }
    let end2 ***REMOVED*** pos;
    if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 69 /* E */ || charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 101 /* e */) {
      pos++;
      tokenFlags |***REMOVED*** 16 /* Scientific */;
      if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 43 /* plus */ || charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) pos++;
      const preNumericPart ***REMOVED*** pos;
      const finalFragment ***REMOVED*** scanNumberFragment();
      if (!finalFragment) {
        error(Diagnostics.Digit_expected);
      } else {
        scientificFragment ***REMOVED*** text.substring(end2, preNumericPart) + finalFragment;
        end2 ***REMOVED*** pos;
      }
    }
    let result;
    if (tokenFlags & 512 /* ContainsSeparator */) {
      result ***REMOVED*** mainFragment;
      if (decimalFragment) {
        result +***REMOVED*** "." + decimalFragment;
      }
      if (scientificFragment) {
        result +***REMOVED*** scientificFragment;
      }
    } else {
      result ***REMOVED*** text.substring(start2, end2);
    }
    if (tokenFlags & 8192 /* ContainsLeadingZero */) {
      error(Diagnostics.Decimals_with_leading_zeros_are_not_allowed, start2, end2 - start2);
      tokenValue ***REMOVED*** "" + +result;
      return 9 /* NumericLiteral */;
    }
    if (decimalFragment !***REMOVED******REMOVED*** void 0 || tokenFlags & 16 /* Scientific */) {
      checkForIdentifierStartAfterNumericLiteral(start2, decimalFragment ***REMOVED******REMOVED******REMOVED*** void 0 && !!(tokenFlags & 16 /* Scientific */));
      tokenValue ***REMOVED*** "" + +result;
      return 9 /* NumericLiteral */;
    } else {
      tokenValue ***REMOVED*** result;
      const type ***REMOVED*** checkBigIntSuffix();
      checkForIdentifierStartAfterNumericLiteral(start2);
      return type;
    }
  }
  function checkForIdentifierStartAfterNumericLiteral(numericStart, isScientific) {
    if (!isIdentifierStart(codePointUnchecked(pos), languageVersion)) {
      return;
    }
    const identifierStart ***REMOVED*** pos;
    const { length: length3 } ***REMOVED*** scanIdentifierParts();
    if (length3 ***REMOVED******REMOVED******REMOVED*** 1 && text[identifierStart] ***REMOVED******REMOVED******REMOVED*** "n") {
      if (isScientific) {
        error(Diagnostics.A_bigint_literal_cannot_use_exponential_notation, numericStart, identifierStart - numericStart + 1);
      } else {
        error(Diagnostics.A_bigint_literal_must_be_an_integer, numericStart, identifierStart - numericStart + 1);
      }
    } else {
      error(Diagnostics.An_identifier_or_keyword_cannot_immediately_follow_a_numeric_literal, identifierStart, length3);
      pos ***REMOVED*** identifierStart;
    }
  }
  function scanDigits() {
    const start2 ***REMOVED*** pos;
    let isOctal ***REMOVED*** true;
    while (isDigit(charCodeChecked(pos))) {
      if (!isOctalDigit(charCodeUnchecked(pos))) {
        isOctal ***REMOVED*** false;
      }
      pos++;
    }
    tokenValue ***REMOVED*** text.substring(start2, pos);
    return isOctal;
  }
  function scanExactNumberOfHexDigits(count, canHaveSeparators) {
    const valueString ***REMOVED*** scanHexDigits(
      /*minCount*/
      count,
      /*scanAsManyAsPossible*/
      false,
      canHaveSeparators
    );
    return valueString ? parseInt(valueString, 16) : -1;
  }
  function scanMinimumNumberOfHexDigits(count, canHaveSeparators) {
    return scanHexDigits(
      /*minCount*/
      count,
      /*scanAsManyAsPossible*/
      true,
      canHaveSeparators
    );
  }
  function scanHexDigits(minCount, scanAsManyAsPossible, canHaveSeparators) {
    let valueChars ***REMOVED*** [];
    let allowSeparator ***REMOVED*** false;
    let isPreviousTokenSeparator ***REMOVED*** false;
    while (valueChars.length < minCount || scanAsManyAsPossible) {
      let ch ***REMOVED*** charCodeUnchecked(pos);
      if (canHaveSeparators && ch ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
        tokenFlags |***REMOVED*** 512 /* ContainsSeparator */;
        if (allowSeparator) {
          allowSeparator ***REMOVED*** false;
          isPreviousTokenSeparator ***REMOVED*** true;
        } else if (isPreviousTokenSeparator) {
          error(Diagnostics.Multiple_consecutive_numeric_separators_are_not_permitted, pos, 1);
        } else {
          error(Diagnostics.Numeric_separators_are_not_allowed_here, pos, 1);
        }
        pos++;
        continue;
      }
      allowSeparator ***REMOVED*** canHaveSeparators;
      if (ch >***REMOVED*** 65 /* A */ && ch <***REMOVED*** 70 /* F */) {
        ch +***REMOVED*** 97 /* a */ - 65 /* A */;
      } else if (!(ch >***REMOVED*** 48 /* _0 */ && ch <***REMOVED*** 57 /* _9 */ || ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 102 /* f */)) {
        break;
      }
      valueChars.push(ch);
      pos++;
      isPreviousTokenSeparator ***REMOVED*** false;
    }
    if (valueChars.length < minCount) {
      valueChars ***REMOVED*** [];
    }
    if (charCodeUnchecked(pos - 1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
      error(Diagnostics.Numeric_separators_are_not_allowed_here, pos - 1, 1);
    }
    return String.fromCharCode(...valueChars);
  }
  function scanString(jsxAttributeString ***REMOVED*** false) {
    const quote ***REMOVED*** charCodeUnchecked(pos);
    pos++;
    let result ***REMOVED*** "";
    let start2 ***REMOVED*** pos;
    while (true) {
      if (pos >***REMOVED*** end) {
        result +***REMOVED*** text.substring(start2, pos);
        tokenFlags |***REMOVED*** 4 /* Unterminated */;
        error(Diagnostics.Unterminated_string_literal);
        break;
      }
      const ch ***REMOVED*** charCodeUnchecked(pos);
      if (ch ***REMOVED******REMOVED******REMOVED*** quote) {
        result +***REMOVED*** text.substring(start2, pos);
        pos++;
        break;
      }
      if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */ && !jsxAttributeString) {
        result +***REMOVED*** text.substring(start2, pos);
        result +***REMOVED*** scanEscapeSequence(1 /* String */ | 2 /* ReportErrors */);
        start2 ***REMOVED*** pos;
        continue;
      }
      if ((ch ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */ || ch ***REMOVED******REMOVED******REMOVED*** 13 /* carriageReturn */) && !jsxAttributeString) {
        result +***REMOVED*** text.substring(start2, pos);
        tokenFlags |***REMOVED*** 4 /* Unterminated */;
        error(Diagnostics.Unterminated_string_literal);
        break;
      }
      pos++;
    }
    return result;
  }
  function scanTemplateAndSetTokenValue(shouldEmitInvalidEscapeError) {
    const startedWithBacktick ***REMOVED*** charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 96 /* backtick */;
    pos++;
    let start2 ***REMOVED*** pos;
    let contents ***REMOVED*** "";
    let resultingToken;
    while (true) {
      if (pos >***REMOVED*** end) {
        contents +***REMOVED*** text.substring(start2, pos);
        tokenFlags |***REMOVED*** 4 /* Unterminated */;
        error(Diagnostics.Unterminated_template_literal);
        resultingToken ***REMOVED*** startedWithBacktick ? 15 /* NoSubstitutionTemplateLiteral */ : 18 /* TemplateTail */;
        break;
      }
      const currChar ***REMOVED*** charCodeUnchecked(pos);
      if (currChar ***REMOVED******REMOVED******REMOVED*** 96 /* backtick */) {
        contents +***REMOVED*** text.substring(start2, pos);
        pos++;
        resultingToken ***REMOVED*** startedWithBacktick ? 15 /* NoSubstitutionTemplateLiteral */ : 18 /* TemplateTail */;
        break;
      }
      if (currChar ***REMOVED******REMOVED******REMOVED*** 36 /* $ */ && pos + 1 < end && charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
        contents +***REMOVED*** text.substring(start2, pos);
        pos +***REMOVED*** 2;
        resultingToken ***REMOVED*** startedWithBacktick ? 16 /* TemplateHead */ : 17 /* TemplateMiddle */;
        break;
      }
      if (currChar ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        contents +***REMOVED*** text.substring(start2, pos);
        contents +***REMOVED*** scanEscapeSequence(1 /* String */ | (shouldEmitInvalidEscapeError ? 2 /* ReportErrors */ : 0));
        start2 ***REMOVED*** pos;
        continue;
      }
      if (currChar ***REMOVED******REMOVED******REMOVED*** 13 /* carriageReturn */) {
        contents +***REMOVED*** text.substring(start2, pos);
        pos++;
        if (pos < end && charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
          pos++;
        }
        contents +***REMOVED*** "\n";
        start2 ***REMOVED*** pos;
        continue;
      }
      pos++;
    }
    Debug.assert(resultingToken !***REMOVED******REMOVED*** void 0);
    tokenValue ***REMOVED*** contents;
    return resultingToken;
  }
  function scanEscapeSequence(flags) {
    const start2 ***REMOVED*** pos;
    pos++;
    if (pos >***REMOVED*** end) {
      error(Diagnostics.Unexpected_end_of_text);
      return "";
    }
    const ch ***REMOVED*** charCodeUnchecked(pos);
    pos++;
    switch (ch) {
      case 48 /* _0 */:
        if (pos >***REMOVED*** end || !isDigit(charCodeUnchecked(pos))) {
          return "\0";
        }
      // '\01', '\011'
      // falls through
      case 49 /* _1 */:
      case 50 /* _2 */:
      case 51 /* _3 */:
        if (pos < end && isOctalDigit(charCodeUnchecked(pos))) {
          pos++;
        }
      // '\17', '\177'
      // falls through
      case 52 /* _4 */:
      case 53 /* _5 */:
      case 54 /* _6 */:
      case 55 /* _7 */:
        if (pos < end && isOctalDigit(charCodeUnchecked(pos))) {
          pos++;
        }
        tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
        if (flags & 6 /* ReportInvalidEscapeErrors */) {
          const code ***REMOVED*** parseInt(text.substring(start2 + 1, pos), 8);
          if (flags & 4 /* RegularExpression */ && !(flags & 32 /* AtomEscape */) && ch !***REMOVED******REMOVED*** 48 /* _0 */) {
            error(Diagnostics.Octal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class_If_this_was_intended_as_an_escape_sequence_use_the_syntax_0_instead, start2, pos - start2, "\\x" + code.toString(16).padStart(2, "0"));
          } else {
            error(Diagnostics.Octal_escape_sequences_are_not_allowed_Use_the_syntax_0, start2, pos - start2, "\\x" + code.toString(16).padStart(2, "0"));
          }
          return String.fromCharCode(code);
        }
        return text.substring(start2, pos);
      case 56 /* _8 */:
      case 57 /* _9 */:
        tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
        if (flags & 6 /* ReportInvalidEscapeErrors */) {
          if (flags & 4 /* RegularExpression */ && !(flags & 32 /* AtomEscape */)) {
            error(Diagnostics.Decimal_escape_sequences_and_backreferences_are_not_allowed_in_a_character_class, start2, pos - start2);
          } else {
            error(Diagnostics.Escape_sequence_0_is_not_allowed, start2, pos - start2, text.substring(start2, pos));
          }
          return String.fromCharCode(ch);
        }
        return text.substring(start2, pos);
      case 98 /* b */:
        return "\b";
      case 116 /* t */:
        return "	";
      case 110 /* n */:
        return "\n";
      case 118 /* v */:
        return "\v";
      case 102 /* f */:
        return "\f";
      case 114 /* r */:
        return "\r";
      case 39 /* singleQuote */:
        return "'";
      case 34 /* doubleQuote */:
        return '"';
      case 117 /* u */:
        if (pos < end && charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
          pos -***REMOVED*** 2;
          const result ***REMOVED*** scanExtendedUnicodeEscape(!!(flags & 6 /* ReportInvalidEscapeErrors */));
          if (!(flags & 17 /* AllowExtendedUnicodeEscape */)) {
            tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
            if (flags & 6 /* ReportInvalidEscapeErrors */) {
              error(Diagnostics.Unicode_escape_sequences_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v_flag_is_set, start2, pos - start2);
            }
          }
          return result;
        }
        for (; pos < start2 + 6; pos++) {
          if (!(pos < end && isHexDigit(charCodeUnchecked(pos)))) {
            tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
            if (flags & 6 /* ReportInvalidEscapeErrors */) {
              error(Diagnostics.Hexadecimal_digit_expected);
            }
            return text.substring(start2, pos);
          }
        }
        tokenFlags |***REMOVED*** 1024 /* UnicodeEscape */;
        const escapedValue ***REMOVED*** parseInt(text.substring(start2 + 2, pos), 16);
        const escapedValueString ***REMOVED*** String.fromCharCode(escapedValue);
        if (flags & 16 /* AnyUnicodeMode */ && escapedValue >***REMOVED*** 55296 && escapedValue <***REMOVED*** 56319 && pos + 6 < end && text.substring(pos, pos + 2) ***REMOVED******REMOVED******REMOVED*** "\\u" && charCodeUnchecked(pos + 2) !***REMOVED******REMOVED*** 123 /* openBrace */) {
          const nextStart ***REMOVED*** pos;
          let nextPos ***REMOVED*** pos + 2;
          for (; nextPos < nextStart + 6; nextPos++) {
            if (!isHexDigit(charCodeUnchecked(nextPos))) {
              return escapedValueString;
            }
          }
          const nextEscapedValue ***REMOVED*** parseInt(text.substring(nextStart + 2, nextPos), 16);
          if (nextEscapedValue >***REMOVED*** 56320 && nextEscapedValue <***REMOVED*** 57343) {
            pos ***REMOVED*** nextPos;
            return escapedValueString + String.fromCharCode(nextEscapedValue);
          }
        }
        return escapedValueString;
      case 120 /* x */:
        for (; pos < start2 + 4; pos++) {
          if (!(pos < end && isHexDigit(charCodeUnchecked(pos)))) {
            tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
            if (flags & 6 /* ReportInvalidEscapeErrors */) {
              error(Diagnostics.Hexadecimal_digit_expected);
            }
            return text.substring(start2, pos);
          }
        }
        tokenFlags |***REMOVED*** 4096 /* HexEscape */;
        return String.fromCharCode(parseInt(text.substring(start2 + 2, pos), 16));
      // when encountering a LineContinuation (i.e. a backslash and a line terminator sequence),
      // the line terminator is interpreted to be "the empty code unit sequence".
      case 13 /* carriageReturn */:
        if (pos < end && charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
          pos++;
        }
      // falls through
      case 10 /* lineFeed */:
      case 8232 /* lineSeparator */:
      case 8233 /* paragraphSeparator */:
        return "";
      default:
        if (flags & 16 /* AnyUnicodeMode */ || flags & 4 /* RegularExpression */ && !(flags & 8 /* AnnexB */) && isIdentifierPart(ch, languageVersion)) {
          error(Diagnostics.This_character_cannot_be_escaped_in_a_regular_expression, pos - 2, 2);
        }
        return String.fromCharCode(ch);
    }
  }
  function scanExtendedUnicodeEscape(shouldEmitInvalidEscapeError) {
    const start2 ***REMOVED*** pos;
    pos +***REMOVED*** 3;
    const escapedStart ***REMOVED*** pos;
    const escapedValueString ***REMOVED*** scanMinimumNumberOfHexDigits(
      1,
      /*canHaveSeparators*/
      false
    );
    const escapedValue ***REMOVED*** escapedValueString ? parseInt(escapedValueString, 16) : -1;
    let isInvalidExtendedEscape ***REMOVED*** false;
    if (escapedValue < 0) {
      if (shouldEmitInvalidEscapeError) {
        error(Diagnostics.Hexadecimal_digit_expected);
      }
      isInvalidExtendedEscape ***REMOVED*** true;
    } else if (escapedValue > 1114111) {
      if (shouldEmitInvalidEscapeError) {
        error(Diagnostics.An_extended_Unicode_escape_value_must_be_between_0x0_and_0x10FFFF_inclusive, escapedStart, pos - escapedStart);
      }
      isInvalidExtendedEscape ***REMOVED*** true;
    }
    if (pos >***REMOVED*** end) {
      if (shouldEmitInvalidEscapeError) {
        error(Diagnostics.Unexpected_end_of_text);
      }
      isInvalidExtendedEscape ***REMOVED*** true;
    } else if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */) {
      pos++;
    } else {
      if (shouldEmitInvalidEscapeError) {
        error(Diagnostics.Unterminated_Unicode_escape_sequence);
      }
      isInvalidExtendedEscape ***REMOVED*** true;
    }
    if (isInvalidExtendedEscape) {
      tokenFlags |***REMOVED*** 2048 /* ContainsInvalidEscape */;
      return text.substring(start2, pos);
    }
    tokenFlags |***REMOVED*** 8 /* ExtendedUnicodeEscape */;
    return utf16EncodeAsString(escapedValue);
  }
  function peekUnicodeEscape() {
    if (pos + 5 < end && charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 117 /* u */) {
      const start2 ***REMOVED*** pos;
      pos +***REMOVED*** 2;
      const value ***REMOVED*** scanExactNumberOfHexDigits(
        4,
        /*canHaveSeparators*/
        false
      );
      pos ***REMOVED*** start2;
      return value;
    }
    return -1;
  }
  function peekExtendedUnicodeEscape() {
    if (codePointUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 117 /* u */ && codePointUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
      const start2 ***REMOVED*** pos;
      pos +***REMOVED*** 3;
      const escapedValueString ***REMOVED*** scanMinimumNumberOfHexDigits(
        1,
        /*canHaveSeparators*/
        false
      );
      const escapedValue ***REMOVED*** escapedValueString ? parseInt(escapedValueString, 16) : -1;
      pos ***REMOVED*** start2;
      return escapedValue;
    }
    return -1;
  }
  function scanIdentifierParts() {
    let result ***REMOVED*** "";
    let start2 ***REMOVED*** pos;
    while (pos < end) {
      let ch ***REMOVED*** codePointUnchecked(pos);
      if (isIdentifierPart(ch, languageVersion)) {
        pos +***REMOVED*** charSize(ch);
      } else if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        ch ***REMOVED*** peekExtendedUnicodeEscape();
        if (ch >***REMOVED*** 0 && isIdentifierPart(ch, languageVersion)) {
          result +***REMOVED*** scanExtendedUnicodeEscape(
            /*shouldEmitInvalidEscapeError*/
            true
          );
          start2 ***REMOVED*** pos;
          continue;
        }
        ch ***REMOVED*** peekUnicodeEscape();
        if (!(ch >***REMOVED*** 0 && isIdentifierPart(ch, languageVersion))) {
          break;
        }
        tokenFlags |***REMOVED*** 1024 /* UnicodeEscape */;
        result +***REMOVED*** text.substring(start2, pos);
        result +***REMOVED*** utf16EncodeAsString(ch);
        pos +***REMOVED*** 6;
        start2 ***REMOVED*** pos;
      } else {
        break;
      }
    }
    result +***REMOVED*** text.substring(start2, pos);
    return result;
  }
  function getIdentifierToken() {
    const len ***REMOVED*** tokenValue.length;
    if (len >***REMOVED*** 2 && len <***REMOVED*** 12) {
      const ch ***REMOVED*** tokenValue.charCodeAt(0);
      if (ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 122 /* z */) {
        const keyword ***REMOVED*** textToKeyword.get(tokenValue);
        if (keyword !***REMOVED******REMOVED*** void 0) {
          return token ***REMOVED*** keyword;
        }
      }
    }
    return token ***REMOVED*** 80 /* Identifier */;
  }
  function scanBinaryOrOctalDigits(base) {
    let value ***REMOVED*** "";
    let separatorAllowed ***REMOVED*** false;
    let isPreviousTokenSeparator ***REMOVED*** false;
    while (true) {
      const ch ***REMOVED*** charCodeUnchecked(pos);
      if (ch ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
        tokenFlags |***REMOVED*** 512 /* ContainsSeparator */;
        if (separatorAllowed) {
          separatorAllowed ***REMOVED*** false;
          isPreviousTokenSeparator ***REMOVED*** true;
        } else if (isPreviousTokenSeparator) {
          error(Diagnostics.Multiple_consecutive_numeric_separators_are_not_permitted, pos, 1);
        } else {
          error(Diagnostics.Numeric_separators_are_not_allowed_here, pos, 1);
        }
        pos++;
        continue;
      }
      separatorAllowed ***REMOVED*** true;
      if (!isDigit(ch) || ch - 48 /* _0 */ >***REMOVED*** base) {
        break;
      }
      value +***REMOVED*** text[pos];
      pos++;
      isPreviousTokenSeparator ***REMOVED*** false;
    }
    if (charCodeUnchecked(pos - 1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */) {
      error(Diagnostics.Numeric_separators_are_not_allowed_here, pos - 1, 1);
    }
    return value;
  }
  function checkBigIntSuffix() {
    if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 110 /* n */) {
      tokenValue +***REMOVED*** "n";
      if (tokenFlags & 384 /* BinaryOrOctalSpecifier */) {
        tokenValue ***REMOVED*** parsePseudoBigInt(tokenValue) + "n";
      }
      pos++;
      return 10 /* BigIntLiteral */;
    } else {
      const numericValue ***REMOVED*** tokenFlags & 128 /* BinarySpecifier */ ? parseInt(tokenValue.slice(2), 2) : tokenFlags & 256 /* OctalSpecifier */ ? parseInt(tokenValue.slice(2), 8) : +tokenValue;
      tokenValue ***REMOVED*** "" + numericValue;
      return 9 /* NumericLiteral */;
    }
  }
  function scan() {
    fullStartPos ***REMOVED*** pos;
    tokenFlags ***REMOVED*** 0 /* None */;
    while (true) {
      tokenStart ***REMOVED*** pos;
      if (pos >***REMOVED*** end) {
        return token ***REMOVED*** 1 /* EndOfFileToken */;
      }
      const ch ***REMOVED*** codePointUnchecked(pos);
      if (pos ***REMOVED******REMOVED******REMOVED*** 0) {
        if (ch ***REMOVED******REMOVED******REMOVED*** 35 /* hash */ && isShebangTrivia(text, pos)) {
          pos ***REMOVED*** scanShebangTrivia(text, pos);
          if (skipTrivia2) {
            continue;
          } else {
            return token ***REMOVED*** 6 /* ShebangTrivia */;
          }
        }
      }
      switch (ch) {
        case 10 /* lineFeed */:
        case 13 /* carriageReturn */:
          tokenFlags |***REMOVED*** 1 /* PrecedingLineBreak */;
          if (skipTrivia2) {
            pos++;
            continue;
          } else {
            if (ch ***REMOVED******REMOVED******REMOVED*** 13 /* carriageReturn */ && pos + 1 < end && charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
              pos +***REMOVED*** 2;
            } else {
              pos++;
            }
            return token ***REMOVED*** 4 /* NewLineTrivia */;
          }
        case 9 /* tab */:
        case 11 /* verticalTab */:
        case 12 /* formFeed */:
        case 32 /* space */:
        case 160 /* nonBreakingSpace */:
        case 5760 /* ogham */:
        case 8192 /* enQuad */:
        case 8193 /* emQuad */:
        case 8194 /* enSpace */:
        case 8195 /* emSpace */:
        case 8196 /* threePerEmSpace */:
        case 8197 /* fourPerEmSpace */:
        case 8198 /* sixPerEmSpace */:
        case 8199 /* figureSpace */:
        case 8200 /* punctuationSpace */:
        case 8201 /* thinSpace */:
        case 8202 /* hairSpace */:
        case 8203 /* zeroWidthSpace */:
        case 8239 /* narrowNoBreakSpace */:
        case 8287 /* mathematicalSpace */:
        case 12288 /* ideographicSpace */:
        case 65279 /* byteOrderMark */:
          if (skipTrivia2) {
            pos++;
            continue;
          } else {
            while (pos < end && isWhiteSpaceSingleLine(charCodeUnchecked(pos))) {
              pos++;
            }
            return token ***REMOVED*** 5 /* WhitespaceTrivia */;
          }
        case 33 /* exclamation */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 38 /* ExclamationEqualsEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 36 /* ExclamationEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 54 /* ExclamationToken */;
        case 34 /* doubleQuote */:
        case 39 /* singleQuote */:
          tokenValue ***REMOVED*** scanString();
          return token ***REMOVED*** 11 /* StringLiteral */;
        case 96 /* backtick */:
          return token ***REMOVED*** scanTemplateAndSetTokenValue(
            /*shouldEmitInvalidEscapeError*/
            false
          );
        case 37 /* percent */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 70 /* PercentEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 45 /* PercentToken */;
        case 38 /* ampersand */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 77 /* AmpersandAmpersandEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 56 /* AmpersandAmpersandToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 74 /* AmpersandEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 51 /* AmpersandToken */;
        case 40 /* openParen */:
          pos++;
          return token ***REMOVED*** 21 /* OpenParenToken */;
        case 41 /* closeParen */:
          pos++;
          return token ***REMOVED*** 22 /* CloseParenToken */;
        case 42 /* asterisk */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 67 /* AsteriskEqualsToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 68 /* AsteriskAsteriskEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 43 /* AsteriskAsteriskToken */;
          }
          pos++;
          if (skipJsDocLeadingAsterisks && (tokenFlags & 32768 /* PrecedingJSDocLeadingAsterisks */) ***REMOVED******REMOVED******REMOVED*** 0 && tokenFlags & 1 /* PrecedingLineBreak */) {
            tokenFlags |***REMOVED*** 32768 /* PrecedingJSDocLeadingAsterisks */;
            continue;
          }
          return token ***REMOVED*** 42 /* AsteriskToken */;
        case 43 /* plus */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 43 /* plus */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 46 /* PlusPlusToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 65 /* PlusEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 40 /* PlusToken */;
        case 44 /* comma */:
          pos++;
          return token ***REMOVED*** 28 /* CommaToken */;
        case 45 /* minus */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 47 /* MinusMinusToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 66 /* MinusEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 41 /* MinusToken */;
        case 46 /* dot */:
          if (isDigit(charCodeUnchecked(pos + 1))) {
            scanNumber();
            return token ***REMOVED*** 9 /* NumericLiteral */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */ && charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */) {
            return pos +***REMOVED*** 3, token ***REMOVED*** 26 /* DotDotDotToken */;
          }
          pos++;
          return token ***REMOVED*** 25 /* DotToken */;
        case 47 /* slash */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
            pos +***REMOVED*** 2;
            while (pos < end) {
              if (isLineBreak(charCodeUnchecked(pos))) {
                break;
              }
              pos++;
            }
            commentDirectives ***REMOVED*** appendIfCommentDirective(
              commentDirectives,
              text.slice(tokenStart, pos),
              commentDirectiveRegExSingleLine,
              tokenStart
            );
            if (skipTrivia2) {
              continue;
            } else {
              return token ***REMOVED*** 2 /* SingleLineCommentTrivia */;
            }
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
            pos +***REMOVED*** 2;
            const isJSDoc2 ***REMOVED*** charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && charCodeUnchecked(pos + 1) !***REMOVED******REMOVED*** 47 /* slash */;
            let commentClosed ***REMOVED*** false;
            let lastLineStart ***REMOVED*** tokenStart;
            while (pos < end) {
              const ch2 ***REMOVED*** charCodeUnchecked(pos);
              if (ch2 ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
                pos +***REMOVED*** 2;
                commentClosed ***REMOVED*** true;
                break;
              }
              pos++;
              if (isLineBreak(ch2)) {
                lastLineStart ***REMOVED*** pos;
                tokenFlags |***REMOVED*** 1 /* PrecedingLineBreak */;
              }
            }
            if (isJSDoc2 && shouldParseJSDoc()) {
              tokenFlags |***REMOVED*** 2 /* PrecedingJSDocComment */;
            }
            commentDirectives ***REMOVED*** appendIfCommentDirective(commentDirectives, text.slice(lastLineStart, pos), commentDirectiveRegExMultiLine, lastLineStart);
            if (!commentClosed) {
              error(Diagnostics.Asterisk_Slash_expected);
            }
            if (skipTrivia2) {
              continue;
            } else {
              if (!commentClosed) {
                tokenFlags |***REMOVED*** 4 /* Unterminated */;
              }
              return token ***REMOVED*** 3 /* MultiLineCommentTrivia */;
            }
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 69 /* SlashEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 44 /* SlashToken */;
        case 48 /* _0 */:
          if (pos + 2 < end && (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 88 /* X */ || charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 120 /* x */)) {
            pos +***REMOVED*** 2;
            tokenValue ***REMOVED*** scanMinimumNumberOfHexDigits(
              1,
              /*canHaveSeparators*/
              true
            );
            if (!tokenValue) {
              error(Diagnostics.Hexadecimal_digit_expected);
              tokenValue ***REMOVED*** "0";
            }
            tokenValue ***REMOVED*** "0x" + tokenValue;
            tokenFlags |***REMOVED*** 64 /* HexSpecifier */;
            return token ***REMOVED*** checkBigIntSuffix();
          } else if (pos + 2 < end && (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 66 /* B */ || charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 98 /* b */)) {
            pos +***REMOVED*** 2;
            tokenValue ***REMOVED*** scanBinaryOrOctalDigits(
              /* base */
              2
            );
            if (!tokenValue) {
              error(Diagnostics.Binary_digit_expected);
              tokenValue ***REMOVED*** "0";
            }
            tokenValue ***REMOVED*** "0b" + tokenValue;
            tokenFlags |***REMOVED*** 128 /* BinarySpecifier */;
            return token ***REMOVED*** checkBigIntSuffix();
          } else if (pos + 2 < end && (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 79 /* O */ || charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 111 /* o */)) {
            pos +***REMOVED*** 2;
            tokenValue ***REMOVED*** scanBinaryOrOctalDigits(
              /* base */
              8
            );
            if (!tokenValue) {
              error(Diagnostics.Octal_digit_expected);
              tokenValue ***REMOVED*** "0";
            }
            tokenValue ***REMOVED*** "0o" + tokenValue;
            tokenFlags |***REMOVED*** 256 /* OctalSpecifier */;
            return token ***REMOVED*** checkBigIntSuffix();
          }
        // falls through
        case 49 /* _1 */:
        case 50 /* _2 */:
        case 51 /* _3 */:
        case 52 /* _4 */:
        case 53 /* _5 */:
        case 54 /* _6 */:
        case 55 /* _7 */:
        case 56 /* _8 */:
        case 57 /* _9 */:
          return token ***REMOVED*** scanNumber();
        case 58 /* colon */:
          pos++;
          return token ***REMOVED*** 59 /* ColonToken */;
        case 59 /* semicolon */:
          pos++;
          return token ***REMOVED*** 27 /* SemicolonToken */;
        case 60 /* lessThan */:
          if (isConflictMarkerTrivia(text, pos)) {
            pos ***REMOVED*** scanConflictMarkerTrivia(text, pos, error);
            if (skipTrivia2) {
              continue;
            } else {
              return token ***REMOVED*** 7 /* ConflictMarkerTrivia */;
            }
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 71 /* LessThanLessThanEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 48 /* LessThanLessThanToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 33 /* LessThanEqualsToken */;
          }
          if (languageVariant ***REMOVED******REMOVED******REMOVED*** 1 /* JSX */ && charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ && charCodeUnchecked(pos + 2) !***REMOVED******REMOVED*** 42 /* asterisk */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 31 /* LessThanSlashToken */;
          }
          pos++;
          return token ***REMOVED*** 30 /* LessThanToken */;
        case 61 /* equals */:
          if (isConflictMarkerTrivia(text, pos)) {
            pos ***REMOVED*** scanConflictMarkerTrivia(text, pos, error);
            if (skipTrivia2) {
              continue;
            } else {
              return token ***REMOVED*** 7 /* ConflictMarkerTrivia */;
            }
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 37 /* EqualsEqualsEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 35 /* EqualsEqualsToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 39 /* EqualsGreaterThanToken */;
          }
          pos++;
          return token ***REMOVED*** 64 /* EqualsToken */;
        case 62 /* greaterThan */:
          if (isConflictMarkerTrivia(text, pos)) {
            pos ***REMOVED*** scanConflictMarkerTrivia(text, pos, error);
            if (skipTrivia2) {
              continue;
            } else {
              return token ***REMOVED*** 7 /* ConflictMarkerTrivia */;
            }
          }
          pos++;
          return token ***REMOVED*** 32 /* GreaterThanToken */;
        case 63 /* question */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 46 /* dot */ && !isDigit(charCodeUnchecked(pos + 2))) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 29 /* QuestionDotToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 63 /* question */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 78 /* QuestionQuestionEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 61 /* QuestionQuestionToken */;
          }
          pos++;
          return token ***REMOVED*** 58 /* QuestionToken */;
        case 91 /* openBracket */:
          pos++;
          return token ***REMOVED*** 23 /* OpenBracketToken */;
        case 93 /* closeBracket */:
          pos++;
          return token ***REMOVED*** 24 /* CloseBracketToken */;
        case 94 /* caret */:
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 79 /* CaretEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 53 /* CaretToken */;
        case 123 /* openBrace */:
          pos++;
          return token ***REMOVED*** 19 /* OpenBraceToken */;
        case 124 /* bar */:
          if (isConflictMarkerTrivia(text, pos)) {
            pos ***REMOVED*** scanConflictMarkerTrivia(text, pos, error);
            if (skipTrivia2) {
              continue;
            } else {
              return token ***REMOVED*** 7 /* ConflictMarkerTrivia */;
            }
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 124 /* bar */) {
            if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              return pos +***REMOVED*** 3, token ***REMOVED*** 76 /* BarBarEqualsToken */;
            }
            return pos +***REMOVED*** 2, token ***REMOVED*** 57 /* BarBarToken */;
          }
          if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 2, token ***REMOVED*** 75 /* BarEqualsToken */;
          }
          pos++;
          return token ***REMOVED*** 52 /* BarToken */;
        case 125 /* closeBrace */:
          pos++;
          return token ***REMOVED*** 20 /* CloseBraceToken */;
        case 126 /* tilde */:
          pos++;
          return token ***REMOVED*** 55 /* TildeToken */;
        case 64 /* at */:
          pos++;
          return token ***REMOVED*** 60 /* AtToken */;
        case 92 /* backslash */:
          const extendedCookedChar ***REMOVED*** peekExtendedUnicodeEscape();
          if (extendedCookedChar >***REMOVED*** 0 && isIdentifierStart(extendedCookedChar, languageVersion)) {
            tokenValue ***REMOVED*** scanExtendedUnicodeEscape(
              /*shouldEmitInvalidEscapeError*/
              true
            ) + scanIdentifierParts();
            return token ***REMOVED*** getIdentifierToken();
          }
          const cookedChar ***REMOVED*** peekUnicodeEscape();
          if (cookedChar >***REMOVED*** 0 && isIdentifierStart(cookedChar, languageVersion)) {
            pos +***REMOVED*** 6;
            tokenFlags |***REMOVED*** 1024 /* UnicodeEscape */;
            tokenValue ***REMOVED*** String.fromCharCode(cookedChar) + scanIdentifierParts();
            return token ***REMOVED*** getIdentifierToken();
          }
          error(Diagnostics.Invalid_character);
          pos++;
          return token ***REMOVED*** 0 /* Unknown */;
        case 35 /* hash */:
          if (pos !***REMOVED******REMOVED*** 0 && text[pos + 1] ***REMOVED******REMOVED******REMOVED*** "!") {
            error(Diagnostics.can_only_be_used_at_the_start_of_a_file, pos, 2);
            pos++;
            return token ***REMOVED*** 0 /* Unknown */;
          }
          const charAfterHash ***REMOVED*** codePointUnchecked(pos + 1);
          if (charAfterHash ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
            pos++;
            const extendedCookedChar2 ***REMOVED*** peekExtendedUnicodeEscape();
            if (extendedCookedChar2 >***REMOVED*** 0 && isIdentifierStart(extendedCookedChar2, languageVersion)) {
              tokenValue ***REMOVED*** "#" + scanExtendedUnicodeEscape(
                /*shouldEmitInvalidEscapeError*/
                true
              ) + scanIdentifierParts();
              return token ***REMOVED*** 81 /* PrivateIdentifier */;
            }
            const cookedChar2 ***REMOVED*** peekUnicodeEscape();
            if (cookedChar2 >***REMOVED*** 0 && isIdentifierStart(cookedChar2, languageVersion)) {
              pos +***REMOVED*** 6;
              tokenFlags |***REMOVED*** 1024 /* UnicodeEscape */;
              tokenValue ***REMOVED*** "#" + String.fromCharCode(cookedChar2) + scanIdentifierParts();
              return token ***REMOVED*** 81 /* PrivateIdentifier */;
            }
            pos--;
          }
          if (isIdentifierStart(charAfterHash, languageVersion)) {
            pos++;
            scanIdentifier(charAfterHash, languageVersion);
          } else {
            tokenValue ***REMOVED*** "#";
            error(Diagnostics.Invalid_character, pos++, charSize(ch));
          }
          return token ***REMOVED*** 81 /* PrivateIdentifier */;
        case 65533 /* replacementCharacter */:
          error(Diagnostics.File_appears_to_be_binary, 0, 0);
          pos ***REMOVED*** end;
          return token ***REMOVED*** 8 /* NonTextFileMarkerTrivia */;
        default:
          const identifierKind ***REMOVED*** scanIdentifier(ch, languageVersion);
          if (identifierKind) {
            return token ***REMOVED*** identifierKind;
          } else if (isWhiteSpaceSingleLine(ch)) {
            pos +***REMOVED*** charSize(ch);
            continue;
          } else if (isLineBreak(ch)) {
            tokenFlags |***REMOVED*** 1 /* PrecedingLineBreak */;
            pos +***REMOVED*** charSize(ch);
            continue;
          }
          const size ***REMOVED*** charSize(ch);
          error(Diagnostics.Invalid_character, pos, size);
          pos +***REMOVED*** size;
          return token ***REMOVED*** 0 /* Unknown */;
      }
    }
  }
  function shouldParseJSDoc() {
    switch (jsDocParsingMode) {
      case 0 /* ParseAll */:
        return true;
      case 1 /* ParseNone */:
        return false;
    }
    if (scriptKind !***REMOVED******REMOVED*** 3 /* TS */ && scriptKind !***REMOVED******REMOVED*** 4 /* TSX */) {
      return true;
    }
    if (jsDocParsingMode ***REMOVED******REMOVED******REMOVED*** 3 /* ParseForTypeInfo */) {
      return false;
    }
    return jsDocSeeOrLink.test(text.slice(fullStartPos, pos));
  }
  function reScanInvalidIdentifier() {
    Debug.assert(token ***REMOVED******REMOVED******REMOVED*** 0 /* Unknown */, "'reScanInvalidIdentifier' should only be called when the current token is 'SyntaxKind.Unknown'.");
    pos ***REMOVED*** tokenStart ***REMOVED*** fullStartPos;
    tokenFlags ***REMOVED*** 0;
    const ch ***REMOVED*** codePointUnchecked(pos);
    const identifierKind ***REMOVED*** scanIdentifier(ch, 99 /* ESNext */);
    if (identifierKind) {
      return token ***REMOVED*** identifierKind;
    }
    pos +***REMOVED*** charSize(ch);
    return token;
  }
  function scanIdentifier(startCharacter, languageVersion2) {
    let ch ***REMOVED*** startCharacter;
    if (isIdentifierStart(ch, languageVersion2)) {
      pos +***REMOVED*** charSize(ch);
      while (pos < end && isIdentifierPart(ch ***REMOVED*** codePointUnchecked(pos), languageVersion2)) pos +***REMOVED*** charSize(ch);
      tokenValue ***REMOVED*** text.substring(tokenStart, pos);
      if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        tokenValue +***REMOVED*** scanIdentifierParts();
      }
      return getIdentifierToken();
    }
  }
  function reScanGreaterToken() {
    if (token ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */) {
      if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) {
        if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) {
          if (charCodeUnchecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
            return pos +***REMOVED*** 3, token ***REMOVED*** 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */;
          }
          return pos +***REMOVED*** 2, token ***REMOVED*** 50 /* GreaterThanGreaterThanGreaterThanToken */;
        }
        if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
          return pos +***REMOVED*** 2, token ***REMOVED*** 72 /* GreaterThanGreaterThanEqualsToken */;
        }
        pos++;
        return token ***REMOVED*** 49 /* GreaterThanGreaterThanToken */;
      }
      if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
        pos++;
        return token ***REMOVED*** 34 /* GreaterThanEqualsToken */;
      }
    }
    return token;
  }
  function reScanAsteriskEqualsToken() {
    Debug.assert(token ***REMOVED******REMOVED******REMOVED*** 67 /* AsteriskEqualsToken */, "'reScanAsteriskEqualsToken' should only be called on a '****REMOVED***'");
    pos ***REMOVED*** tokenStart + 1;
    return token ***REMOVED*** 64 /* EqualsToken */;
  }
  function reScanSlashToken(reportErrors2) {
    if (token ***REMOVED******REMOVED******REMOVED*** 44 /* SlashToken */ || token ***REMOVED******REMOVED******REMOVED*** 69 /* SlashEqualsToken */) {
      const startOfRegExpBody ***REMOVED*** tokenStart + 1;
      pos ***REMOVED*** startOfRegExpBody;
      let inEscape ***REMOVED*** false;
      let namedCaptureGroups ***REMOVED*** false;
      let inCharacterClass ***REMOVED*** false;
      while (true) {
        const ch ***REMOVED*** charCodeChecked(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */ || isLineBreak(ch)) {
          tokenFlags |***REMOVED*** 4 /* Unterminated */;
          break;
        }
        if (inEscape) {
          inEscape ***REMOVED*** false;
        } else if (ch ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ && !inCharacterClass) {
          break;
        } else if (ch ***REMOVED******REMOVED******REMOVED*** 91 /* openBracket */) {
          inCharacterClass ***REMOVED*** true;
        } else if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
          inEscape ***REMOVED*** true;
        } else if (ch ***REMOVED******REMOVED******REMOVED*** 93 /* closeBracket */) {
          inCharacterClass ***REMOVED*** false;
        } else if (!inCharacterClass && ch ***REMOVED******REMOVED******REMOVED*** 40 /* openParen */ && charCodeChecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 63 /* question */ && charCodeChecked(pos + 2) ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */ && charCodeChecked(pos + 3) !***REMOVED******REMOVED*** 61 /* equals */ && charCodeChecked(pos + 3) !***REMOVED******REMOVED*** 33 /* exclamation */) {
          namedCaptureGroups ***REMOVED*** true;
        }
        pos++;
      }
      const endOfRegExpBody ***REMOVED*** pos;
      if (tokenFlags & 4 /* Unterminated */) {
        pos ***REMOVED*** startOfRegExpBody;
        inEscape ***REMOVED*** false;
        let characterClassDepth ***REMOVED*** 0;
        let inDecimalQuantifier ***REMOVED*** false;
        let groupDepth ***REMOVED*** 0;
        while (pos < endOfRegExpBody) {
          const ch ***REMOVED*** charCodeUnchecked(pos);
          if (inEscape) {
            inEscape ***REMOVED*** false;
          } else if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
            inEscape ***REMOVED*** true;
          } else if (ch ***REMOVED******REMOVED******REMOVED*** 91 /* openBracket */) {
            characterClassDepth++;
          } else if (ch ***REMOVED******REMOVED******REMOVED*** 93 /* closeBracket */ && characterClassDepth) {
            characterClassDepth--;
          } else if (!characterClassDepth) {
            if (ch ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
              inDecimalQuantifier ***REMOVED*** true;
            } else if (ch ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */ && inDecimalQuantifier) {
              inDecimalQuantifier ***REMOVED*** false;
            } else if (!inDecimalQuantifier) {
              if (ch ***REMOVED******REMOVED******REMOVED*** 40 /* openParen */) {
                groupDepth++;
              } else if (ch ***REMOVED******REMOVED******REMOVED*** 41 /* closeParen */ && groupDepth) {
                groupDepth--;
              } else if (ch ***REMOVED******REMOVED******REMOVED*** 41 /* closeParen */ || ch ***REMOVED******REMOVED******REMOVED*** 93 /* closeBracket */ || ch ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */) {
                break;
              }
            }
          }
          pos++;
        }
        while (isWhiteSpaceLike(charCodeChecked(pos - 1)) || charCodeChecked(pos - 1) ***REMOVED******REMOVED******REMOVED*** 59 /* semicolon */) pos--;
        error(Diagnostics.Unterminated_regular_expression_literal, tokenStart, pos - tokenStart);
      } else {
        pos++;
        let regExpFlags ***REMOVED*** 0 /* None */;
        while (true) {
          const ch ***REMOVED*** codePointChecked(pos);
          if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */ || !isIdentifierPart(ch, languageVersion)) {
            break;
          }
          const size ***REMOVED*** charSize(ch);
          if (reportErrors2) {
            const flag ***REMOVED*** characterCodeToRegularExpressionFlag(ch);
            if (flag ***REMOVED******REMOVED******REMOVED*** void 0) {
              error(Diagnostics.Unknown_regular_expression_flag, pos, size);
            } else if (regExpFlags & flag) {
              error(Diagnostics.Duplicate_regular_expression_flag, pos, size);
            } else if (((regExpFlags | flag) & 96 /* AnyUnicodeMode */) ***REMOVED******REMOVED******REMOVED*** 96 /* AnyUnicodeMode */) {
              error(Diagnostics.The_Unicode_u_flag_and_the_Unicode_Sets_v_flag_cannot_be_set_simultaneously, pos, size);
            } else {
              regExpFlags |***REMOVED*** flag;
              checkRegularExpressionFlagAvailability(flag, size);
            }
          }
          pos +***REMOVED*** size;
        }
        if (reportErrors2) {
          scanRange(startOfRegExpBody, endOfRegExpBody - startOfRegExpBody, () ***REMOVED***> {
            scanRegularExpressionWorker(
              regExpFlags,
              /*annexB*/
              true,
              namedCaptureGroups
            );
          });
        }
      }
      tokenValue ***REMOVED*** text.substring(tokenStart, pos);
      token ***REMOVED*** 14 /* RegularExpressionLiteral */;
    }
    return token;
  }
  function scanRegularExpressionWorker(regExpFlags, annexB, namedCaptureGroups) {
    var unicodeSetsMode ***REMOVED*** !!(regExpFlags & 64 /* UnicodeSets */);
    var anyUnicodeMode ***REMOVED*** !!(regExpFlags & 96 /* AnyUnicodeMode */);
    var anyUnicodeModeOrNonAnnexB ***REMOVED*** anyUnicodeMode || !annexB;
    var mayContainStrings ***REMOVED*** false;
    var numberOfCapturingGroups ***REMOVED*** 0;
    var groupSpecifiers;
    var groupNameReferences;
    var decimalEscapes;
    var namedCapturingGroupsScopeStack ***REMOVED*** [];
    var topNamedCapturingGroupsScope;
    function scanDisjunction(isInGroup) {
      while (true) {
        namedCapturingGroupsScopeStack.push(topNamedCapturingGroupsScope);
        topNamedCapturingGroupsScope ***REMOVED*** void 0;
        scanAlternative(isInGroup);
        topNamedCapturingGroupsScope ***REMOVED*** namedCapturingGroupsScopeStack.pop();
        if (charCodeChecked(pos) !***REMOVED******REMOVED*** 124 /* bar */) {
          return;
        }
        pos++;
      }
    }
    function scanAlternative(isInGroup) {
      let isPreviousTermQuantifiable ***REMOVED*** false;
      while (true) {
        const start2 ***REMOVED*** pos;
        const ch ***REMOVED*** charCodeChecked(pos);
        switch (ch) {
          case -1 /* EOF */:
            return;
          case 94 /* caret */:
          case 36 /* $ */:
            pos++;
            isPreviousTermQuantifiable ***REMOVED*** false;
            break;
          case 92 /* backslash */:
            pos++;
            switch (charCodeChecked(pos)) {
              case 98 /* b */:
              case 66 /* B */:
                pos++;
                isPreviousTermQuantifiable ***REMOVED*** false;
                break;
              default:
                scanAtomEscape();
                isPreviousTermQuantifiable ***REMOVED*** true;
                break;
            }
            break;
          case 40 /* openParen */:
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 63 /* question */) {
              pos++;
              switch (charCodeChecked(pos)) {
                case 61 /* equals */:
                case 33 /* exclamation */:
                  pos++;
                  isPreviousTermQuantifiable ***REMOVED*** !anyUnicodeModeOrNonAnnexB;
                  break;
                case 60 /* lessThan */:
                  const groupNameStart ***REMOVED*** pos;
                  pos++;
                  switch (charCodeChecked(pos)) {
                    case 61 /* equals */:
                    case 33 /* exclamation */:
                      pos++;
                      isPreviousTermQuantifiable ***REMOVED*** false;
                      break;
                    default:
                      scanGroupName(
                        /*isReference*/
                        false
                      );
                      scanExpectedChar(62 /* greaterThan */);
                      if (languageVersion < 5 /* ES2018 */) {
                        error(Diagnostics.Named_capturing_groups_are_only_available_when_targeting_ES2018_or_later, groupNameStart, pos - groupNameStart);
                      }
                      numberOfCapturingGroups++;
                      isPreviousTermQuantifiable ***REMOVED*** true;
                      break;
                  }
                  break;
                default:
                  const start3 ***REMOVED*** pos;
                  const setFlags ***REMOVED*** scanPatternModifiers(0 /* None */);
                  if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
                    pos++;
                    scanPatternModifiers(setFlags);
                    if (pos ***REMOVED******REMOVED******REMOVED*** start3 + 1) {
                      error(Diagnostics.Subpattern_flags_must_be_present_when_there_is_a_minus_sign, start3, pos - start3);
                    }
                  }
                  scanExpectedChar(58 /* colon */);
                  isPreviousTermQuantifiable ***REMOVED*** true;
                  break;
              }
            } else {
              numberOfCapturingGroups++;
              isPreviousTermQuantifiable ***REMOVED*** true;
            }
            scanDisjunction(
              /*isInGroup*/
              true
            );
            scanExpectedChar(41 /* closeParen */);
            break;
          case 123 /* openBrace */:
            pos++;
            const digitsStart ***REMOVED*** pos;
            scanDigits();
            const min2 ***REMOVED*** tokenValue;
            if (!anyUnicodeModeOrNonAnnexB && !min2) {
              isPreviousTermQuantifiable ***REMOVED*** true;
              break;
            }
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 44 /* comma */) {
              pos++;
              scanDigits();
              const max ***REMOVED*** tokenValue;
              if (!min2) {
                if (max || charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */) {
                  error(Diagnostics.Incomplete_quantifier_Digit_expected, digitsStart, 0);
                } else {
                  error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, start2, 1, String.fromCharCode(ch));
                  isPreviousTermQuantifiable ***REMOVED*** true;
                  break;
                }
              } else if (max && Number.parseInt(min2) > Number.parseInt(max) && (anyUnicodeModeOrNonAnnexB || charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */)) {
                error(Diagnostics.Numbers_out_of_order_in_quantifier, digitsStart, pos - digitsStart);
              }
            } else if (!min2) {
              if (anyUnicodeModeOrNonAnnexB) {
                error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, start2, 1, String.fromCharCode(ch));
              }
              isPreviousTermQuantifiable ***REMOVED*** true;
              break;
            }
            if (charCodeChecked(pos) !***REMOVED******REMOVED*** 125 /* closeBrace */) {
              if (anyUnicodeModeOrNonAnnexB) {
                error(Diagnostics._0_expected, pos, 0, String.fromCharCode(125 /* closeBrace */));
                pos--;
              } else {
                isPreviousTermQuantifiable ***REMOVED*** true;
                break;
              }
            }
          // falls through
          case 42 /* asterisk */:
          case 43 /* plus */:
          case 63 /* question */:
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 63 /* question */) {
              pos++;
            }
            if (!isPreviousTermQuantifiable) {
              error(Diagnostics.There_is_nothing_available_for_repetition, start2, pos - start2);
            }
            isPreviousTermQuantifiable ***REMOVED*** false;
            break;
          case 46 /* dot */:
            pos++;
            isPreviousTermQuantifiable ***REMOVED*** true;
            break;
          case 91 /* openBracket */:
            pos++;
            if (unicodeSetsMode) {
              scanClassSetExpression();
            } else {
              scanClassRanges();
            }
            scanExpectedChar(93 /* closeBracket */);
            isPreviousTermQuantifiable ***REMOVED*** true;
            break;
          case 41 /* closeParen */:
            if (isInGroup) {
              return;
            }
          // falls through
          case 93 /* closeBracket */:
          case 125 /* closeBrace */:
            if (anyUnicodeModeOrNonAnnexB || ch ***REMOVED******REMOVED******REMOVED*** 41 /* closeParen */) {
              error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos, 1, String.fromCharCode(ch));
            }
            pos++;
            isPreviousTermQuantifiable ***REMOVED*** true;
            break;
          case 47 /* slash */:
          case 124 /* bar */:
            return;
          default:
            scanSourceCharacter();
            isPreviousTermQuantifiable ***REMOVED*** true;
            break;
        }
      }
    }
    function scanPatternModifiers(currFlags) {
      while (true) {
        const ch ***REMOVED*** codePointChecked(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */ || !isIdentifierPart(ch, languageVersion)) {
          break;
        }
        const size ***REMOVED*** charSize(ch);
        const flag ***REMOVED*** characterCodeToRegularExpressionFlag(ch);
        if (flag ***REMOVED******REMOVED******REMOVED*** void 0) {
          error(Diagnostics.Unknown_regular_expression_flag, pos, size);
        } else if (currFlags & flag) {
          error(Diagnostics.Duplicate_regular_expression_flag, pos, size);
        } else if (!(flag & 28 /* Modifiers */)) {
          error(Diagnostics.This_regular_expression_flag_cannot_be_toggled_within_a_subpattern, pos, size);
        } else {
          currFlags |***REMOVED*** flag;
          checkRegularExpressionFlagAvailability(flag, size);
        }
        pos +***REMOVED*** size;
      }
      return currFlags;
    }
    function scanAtomEscape() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 92 /* backslash */);
      switch (charCodeChecked(pos)) {
        case 107 /* k */:
          pos++;
          if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */) {
            pos++;
            scanGroupName(
              /*isReference*/
              true
            );
            scanExpectedChar(62 /* greaterThan */);
          } else if (anyUnicodeModeOrNonAnnexB || namedCaptureGroups) {
            error(Diagnostics.k_must_be_followed_by_a_capturing_group_name_enclosed_in_angle_brackets, pos - 2, 2);
          }
          break;
        case 113 /* q */:
          if (unicodeSetsMode) {
            pos++;
            error(Diagnostics.q_is_only_available_inside_character_class, pos - 2, 2);
            break;
          }
        // falls through
        default:
          Debug.assert(scanCharacterClassEscape() || scanDecimalEscape() || scanCharacterEscape(
            /*atomEscape*/
            true
          ));
          break;
      }
    }
    function scanDecimalEscape() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 92 /* backslash */);
      const ch ***REMOVED*** charCodeChecked(pos);
      if (ch >***REMOVED*** 49 /* _1 */ && ch <***REMOVED*** 57 /* _9 */) {
        const start2 ***REMOVED*** pos;
        scanDigits();
        decimalEscapes ***REMOVED*** append(decimalEscapes, { pos: start2, end: pos, value: +tokenValue });
        return true;
      }
      return false;
    }
    function scanCharacterEscape(atomEscape) {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 92 /* backslash */);
      let ch ***REMOVED*** charCodeChecked(pos);
      switch (ch) {
        case -1 /* EOF */:
          error(Diagnostics.Undetermined_character_escape, pos - 1, 1);
          return "\\";
        case 99 /* c */:
          pos++;
          ch ***REMOVED*** charCodeChecked(pos);
          if (isASCIILetter(ch)) {
            pos++;
            return String.fromCharCode(ch & 31);
          }
          if (anyUnicodeModeOrNonAnnexB) {
            error(Diagnostics.c_must_be_followed_by_an_ASCII_letter, pos - 2, 2);
          } else if (atomEscape) {
            pos--;
            return "\\";
          }
          return String.fromCharCode(ch);
        case 94 /* caret */:
        case 36 /* $ */:
        case 47 /* slash */:
        case 92 /* backslash */:
        case 46 /* dot */:
        case 42 /* asterisk */:
        case 43 /* plus */:
        case 63 /* question */:
        case 40 /* openParen */:
        case 41 /* closeParen */:
        case 91 /* openBracket */:
        case 93 /* closeBracket */:
        case 123 /* openBrace */:
        case 125 /* closeBrace */:
        case 124 /* bar */:
          pos++;
          return String.fromCharCode(ch);
        default:
          pos--;
          return scanEscapeSequence(
            4 /* RegularExpression */ | (annexB ? 8 /* AnnexB */ : 0) | (anyUnicodeMode ? 16 /* AnyUnicodeMode */ : 0) | (atomEscape ? 32 /* AtomEscape */ : 0)
          );
      }
    }
    function scanGroupName(isReference) {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 60 /* lessThan */);
      tokenStart ***REMOVED*** pos;
      scanIdentifier(codePointChecked(pos), languageVersion);
      if (pos ***REMOVED******REMOVED******REMOVED*** tokenStart) {
        error(Diagnostics.Expected_a_capturing_group_name);
      } else if (isReference) {
        groupNameReferences ***REMOVED*** append(groupNameReferences, { pos: tokenStart, end: pos, name: tokenValue });
      } else if ((topNamedCapturingGroupsScope ***REMOVED******REMOVED*** null ? void 0 : topNamedCapturingGroupsScope.has(tokenValue)) || namedCapturingGroupsScopeStack.some((group2) ***REMOVED***> group2 ***REMOVED******REMOVED*** null ? void 0 : group2.has(tokenValue))) {
        error(Diagnostics.Named_capturing_groups_with_the_same_name_must_be_mutually_exclusive_to_each_other, tokenStart, pos - tokenStart);
      } else {
        topNamedCapturingGroupsScope ?? (topNamedCapturingGroupsScope ***REMOVED*** /* @__PURE__ */ new Set());
        topNamedCapturingGroupsScope.add(tokenValue);
        groupSpecifiers ?? (groupSpecifiers ***REMOVED*** /* @__PURE__ */ new Set());
        groupSpecifiers.add(tokenValue);
      }
    }
    function isClassContentExit(ch) {
      return ch ***REMOVED******REMOVED******REMOVED*** 93 /* closeBracket */ || ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */ || pos >***REMOVED*** end;
    }
    function scanClassRanges() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 91 /* openBracket */);
      if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 94 /* caret */) {
        pos++;
      }
      while (true) {
        const ch ***REMOVED*** charCodeChecked(pos);
        if (isClassContentExit(ch)) {
          return;
        }
        const minStart ***REMOVED*** pos;
        const minCharacter ***REMOVED*** scanClassAtom();
        if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
          pos++;
          const ch2 ***REMOVED*** charCodeChecked(pos);
          if (isClassContentExit(ch2)) {
            return;
          }
          if (!minCharacter && anyUnicodeModeOrNonAnnexB) {
            error(Diagnostics.A_character_class_range_must_not_be_bounded_by_another_character_class, minStart, pos - 1 - minStart);
          }
          const maxStart ***REMOVED*** pos;
          const maxCharacter ***REMOVED*** scanClassAtom();
          if (!maxCharacter && anyUnicodeModeOrNonAnnexB) {
            error(Diagnostics.A_character_class_range_must_not_be_bounded_by_another_character_class, maxStart, pos - maxStart);
            continue;
          }
          if (!minCharacter) {
            continue;
          }
          const minCharacterValue ***REMOVED*** codePointAt(minCharacter, 0);
          const maxCharacterValue ***REMOVED*** codePointAt(maxCharacter, 0);
          if (minCharacter.length ***REMOVED******REMOVED******REMOVED*** charSize(minCharacterValue) && maxCharacter.length ***REMOVED******REMOVED******REMOVED*** charSize(maxCharacterValue) && minCharacterValue > maxCharacterValue) {
            error(Diagnostics.Range_out_of_order_in_character_class, minStart, pos - minStart);
          }
        }
      }
    }
    function scanClassSetExpression() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 91 /* openBracket */);
      let isCharacterComplement ***REMOVED*** false;
      if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 94 /* caret */) {
        pos++;
        isCharacterComplement ***REMOVED*** true;
      }
      let expressionMayContainStrings ***REMOVED*** false;
      let ch ***REMOVED*** charCodeChecked(pos);
      if (isClassContentExit(ch)) {
        return;
      }
      let start2 ***REMOVED*** pos;
      let operand;
      switch (text.slice(pos, pos + 2)) {
        // TODO: don't use slice
        case "--":
        case "&&":
          error(Diagnostics.Expected_a_class_set_operand);
          mayContainStrings ***REMOVED*** false;
          break;
        default:
          operand ***REMOVED*** scanClassSetOperand();
          break;
      }
      switch (charCodeChecked(pos)) {
        case 45 /* minus */:
          if (charCodeChecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
            if (isCharacterComplement && mayContainStrings) {
              error(Diagnostics.Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class, start2, pos - start2);
            }
            expressionMayContainStrings ***REMOVED*** mayContainStrings;
            scanClassSetSubExpression(3 /* ClassSubtraction */);
            mayContainStrings ***REMOVED*** !isCharacterComplement && expressionMayContainStrings;
            return;
          }
          break;
        case 38 /* ampersand */:
          if (charCodeChecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
            scanClassSetSubExpression(2 /* ClassIntersection */);
            if (isCharacterComplement && mayContainStrings) {
              error(Diagnostics.Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class, start2, pos - start2);
            }
            expressionMayContainStrings ***REMOVED*** mayContainStrings;
            mayContainStrings ***REMOVED*** !isCharacterComplement && expressionMayContainStrings;
            return;
          } else {
            error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos, 1, String.fromCharCode(ch));
          }
          break;
        default:
          if (isCharacterComplement && mayContainStrings) {
            error(Diagnostics.Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class, start2, pos - start2);
          }
          expressionMayContainStrings ***REMOVED*** mayContainStrings;
          break;
      }
      while (true) {
        ch ***REMOVED*** charCodeChecked(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */) {
          break;
        }
        switch (ch) {
          case 45 /* minus */:
            pos++;
            ch ***REMOVED*** charCodeChecked(pos);
            if (isClassContentExit(ch)) {
              mayContainStrings ***REMOVED*** !isCharacterComplement && expressionMayContainStrings;
              return;
            }
            if (ch ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
              pos++;
              error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos - 2, 2);
              start2 ***REMOVED*** pos - 2;
              operand ***REMOVED*** text.slice(start2, pos);
              continue;
            } else {
              if (!operand) {
                error(Diagnostics.A_character_class_range_must_not_be_bounded_by_another_character_class, start2, pos - 1 - start2);
              }
              const secondStart ***REMOVED*** pos;
              const secondOperand ***REMOVED*** scanClassSetOperand();
              if (isCharacterComplement && mayContainStrings) {
                error(Diagnostics.Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class, secondStart, pos - secondStart);
              }
              expressionMayContainStrings || (expressionMayContainStrings ***REMOVED*** mayContainStrings);
              if (!secondOperand) {
                error(Diagnostics.A_character_class_range_must_not_be_bounded_by_another_character_class, secondStart, pos - secondStart);
                break;
              }
              if (!operand) {
                break;
              }
              const minCharacterValue ***REMOVED*** codePointAt(operand, 0);
              const maxCharacterValue ***REMOVED*** codePointAt(secondOperand, 0);
              if (operand.length ***REMOVED******REMOVED******REMOVED*** charSize(minCharacterValue) && secondOperand.length ***REMOVED******REMOVED******REMOVED*** charSize(maxCharacterValue) && minCharacterValue > maxCharacterValue) {
                error(Diagnostics.Range_out_of_order_in_character_class, start2, pos - start2);
              }
            }
            break;
          case 38 /* ampersand */:
            start2 ***REMOVED*** pos;
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
              pos++;
              error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos - 2, 2);
              if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
                error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos, 1, String.fromCharCode(ch));
                pos++;
              }
            } else {
              error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos - 1, 1, String.fromCharCode(ch));
            }
            operand ***REMOVED*** text.slice(start2, pos);
            continue;
        }
        if (isClassContentExit(charCodeChecked(pos))) {
          break;
        }
        start2 ***REMOVED*** pos;
        switch (text.slice(pos, pos + 2)) {
          // TODO: don't use slice
          case "--":
          case "&&":
            error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos, 2);
            pos +***REMOVED*** 2;
            operand ***REMOVED*** text.slice(start2, pos);
            break;
          default:
            operand ***REMOVED*** scanClassSetOperand();
            break;
        }
      }
      mayContainStrings ***REMOVED*** !isCharacterComplement && expressionMayContainStrings;
    }
    function scanClassSetSubExpression(expressionType) {
      let expressionMayContainStrings ***REMOVED*** mayContainStrings;
      while (true) {
        let ch ***REMOVED*** charCodeChecked(pos);
        if (isClassContentExit(ch)) {
          break;
        }
        switch (ch) {
          case 45 /* minus */:
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
              pos++;
              if (expressionType !***REMOVED******REMOVED*** 3 /* ClassSubtraction */) {
                error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos - 2, 2);
              }
            } else {
              error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos - 1, 1);
            }
            break;
          case 38 /* ampersand */:
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
              pos++;
              if (expressionType !***REMOVED******REMOVED*** 2 /* ClassIntersection */) {
                error(Diagnostics.Operators_must_not_be_mixed_within_a_character_class_Wrap_it_in_a_nested_class_instead, pos - 2, 2);
              }
              if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 38 /* ampersand */) {
                error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos, 1, String.fromCharCode(ch));
                pos++;
              }
            } else {
              error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos - 1, 1, String.fromCharCode(ch));
            }
            break;
          default:
            switch (expressionType) {
              case 3 /* ClassSubtraction */:
                error(Diagnostics._0_expected, pos, 0, "--");
                break;
              case 2 /* ClassIntersection */:
                error(Diagnostics._0_expected, pos, 0, "&&");
                break;
              default:
                break;
            }
            break;
        }
        ch ***REMOVED*** charCodeChecked(pos);
        if (isClassContentExit(ch)) {
          error(Diagnostics.Expected_a_class_set_operand);
          break;
        }
        scanClassSetOperand();
        expressionMayContainStrings && (expressionMayContainStrings ***REMOVED*** mayContainStrings);
      }
      mayContainStrings ***REMOVED*** expressionMayContainStrings;
    }
    function scanClassSetOperand() {
      mayContainStrings ***REMOVED*** false;
      switch (charCodeChecked(pos)) {
        case -1 /* EOF */:
          return "";
        case 91 /* openBracket */:
          pos++;
          scanClassSetExpression();
          scanExpectedChar(93 /* closeBracket */);
          return "";
        case 92 /* backslash */:
          pos++;
          if (scanCharacterClassEscape()) {
            return "";
          } else if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 113 /* q */) {
            pos++;
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
              pos++;
              scanClassStringDisjunctionContents();
              scanExpectedChar(125 /* closeBrace */);
              return "";
            } else {
              error(Diagnostics.q_must_be_followed_by_string_alternatives_enclosed_in_braces, pos - 2, 2);
              return "q";
            }
          }
          pos--;
        // falls through
        default:
          return scanClassSetCharacter();
      }
    }
    function scanClassStringDisjunctionContents() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 123 /* openBrace */);
      let characterCount ***REMOVED*** 0;
      while (true) {
        const ch ***REMOVED*** charCodeChecked(pos);
        switch (ch) {
          case -1 /* EOF */:
            return;
          case 125 /* closeBrace */:
            if (characterCount !***REMOVED******REMOVED*** 1) {
              mayContainStrings ***REMOVED*** true;
            }
            return;
          case 124 /* bar */:
            if (characterCount !***REMOVED******REMOVED*** 1) {
              mayContainStrings ***REMOVED*** true;
            }
            pos++;
            start ***REMOVED*** pos;
            characterCount ***REMOVED*** 0;
            break;
          default:
            scanClassSetCharacter();
            characterCount++;
            break;
        }
      }
    }
    function scanClassSetCharacter() {
      const ch ***REMOVED*** charCodeChecked(pos);
      if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */) {
        return "";
      }
      if (ch ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        pos++;
        const ch2 ***REMOVED*** charCodeChecked(pos);
        switch (ch2) {
          case 98 /* b */:
            pos++;
            return "\b";
          case 38 /* ampersand */:
          case 45 /* minus */:
          case 33 /* exclamation */:
          case 35 /* hash */:
          case 37 /* percent */:
          case 44 /* comma */:
          case 58 /* colon */:
          case 59 /* semicolon */:
          case 60 /* lessThan */:
          case 61 /* equals */:
          case 62 /* greaterThan */:
          case 64 /* at */:
          case 96 /* backtick */:
          case 126 /* tilde */:
            pos++;
            return String.fromCharCode(ch2);
          default:
            return scanCharacterEscape(
              /*atomEscape*/
              false
            );
        }
      } else if (ch ***REMOVED******REMOVED******REMOVED*** charCodeChecked(pos + 1)) {
        switch (ch) {
          case 38 /* ampersand */:
          case 33 /* exclamation */:
          case 35 /* hash */:
          case 37 /* percent */:
          case 42 /* asterisk */:
          case 43 /* plus */:
          case 44 /* comma */:
          case 46 /* dot */:
          case 58 /* colon */:
          case 59 /* semicolon */:
          case 60 /* lessThan */:
          case 61 /* equals */:
          case 62 /* greaterThan */:
          case 63 /* question */:
          case 64 /* at */:
          case 96 /* backtick */:
          case 126 /* tilde */:
            error(Diagnostics.A_character_class_must_not_contain_a_reserved_double_punctuator_Did_you_mean_to_escape_it_with_backslash, pos, 2);
            pos +***REMOVED*** 2;
            return text.substring(pos - 2, pos);
        }
      }
      switch (ch) {
        case 47 /* slash */:
        case 40 /* openParen */:
        case 41 /* closeParen */:
        case 91 /* openBracket */:
        case 93 /* closeBracket */:
        case 123 /* openBrace */:
        case 125 /* closeBrace */:
        case 45 /* minus */:
        case 124 /* bar */:
          error(Diagnostics.Unexpected_0_Did_you_mean_to_escape_it_with_backslash, pos, 1, String.fromCharCode(ch));
          pos++;
          return String.fromCharCode(ch);
      }
      return scanSourceCharacter();
    }
    function scanClassAtom() {
      if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        pos++;
        const ch ***REMOVED*** charCodeChecked(pos);
        switch (ch) {
          case 98 /* b */:
            pos++;
            return "\b";
          case 45 /* minus */:
            pos++;
            return String.fromCharCode(ch);
          default:
            if (scanCharacterClassEscape()) {
              return "";
            }
            return scanCharacterEscape(
              /*atomEscape*/
              false
            );
        }
      } else {
        return scanSourceCharacter();
      }
    }
    function scanCharacterClassEscape() {
      Debug.assertEqual(charCodeUnchecked(pos - 1), 92 /* backslash */);
      let isCharacterComplement ***REMOVED*** false;
      const start2 ***REMOVED*** pos - 1;
      const ch ***REMOVED*** charCodeChecked(pos);
      switch (ch) {
        case 100 /* d */:
        case 68 /* D */:
        case 115 /* s */:
        case 83 /* S */:
        case 119 /* w */:
        case 87 /* W */:
          pos++;
          return true;
        case 80 /* P */:
          isCharacterComplement ***REMOVED*** true;
        // falls through
        case 112 /* p */:
          pos++;
          if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
            pos++;
            const propertyNameOrValueStart ***REMOVED*** pos;
            const propertyNameOrValue ***REMOVED*** scanWordCharacters();
            if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** 61 /* equals */) {
              const propertyName ***REMOVED*** nonBinaryUnicodeProperties.get(propertyNameOrValue);
              if (pos ***REMOVED******REMOVED******REMOVED*** propertyNameOrValueStart) {
                error(Diagnostics.Expected_a_Unicode_property_name);
              } else if (propertyName ***REMOVED******REMOVED******REMOVED*** void 0) {
                error(Diagnostics.Unknown_Unicode_property_name, propertyNameOrValueStart, pos - propertyNameOrValueStart);
                const suggestion ***REMOVED*** getSpellingSuggestion(propertyNameOrValue, nonBinaryUnicodeProperties.keys(), identity);
                if (suggestion) {
                  error(Diagnostics.Did_you_mean_0, propertyNameOrValueStart, pos - propertyNameOrValueStart, suggestion);
                }
              }
              pos++;
              const propertyValueStart ***REMOVED*** pos;
              const propertyValue ***REMOVED*** scanWordCharacters();
              if (pos ***REMOVED******REMOVED******REMOVED*** propertyValueStart) {
                error(Diagnostics.Expected_a_Unicode_property_value);
              } else if (propertyName !***REMOVED******REMOVED*** void 0 && !valuesOfNonBinaryUnicodeProperties[propertyName].has(propertyValue)) {
                error(Diagnostics.Unknown_Unicode_property_value, propertyValueStart, pos - propertyValueStart);
                const suggestion ***REMOVED*** getSpellingSuggestion(propertyValue, valuesOfNonBinaryUnicodeProperties[propertyName], identity);
                if (suggestion) {
                  error(Diagnostics.Did_you_mean_0, propertyValueStart, pos - propertyValueStart, suggestion);
                }
              }
            } else {
              if (pos ***REMOVED******REMOVED******REMOVED*** propertyNameOrValueStart) {
                error(Diagnostics.Expected_a_Unicode_property_name_or_value);
              } else if (binaryUnicodePropertiesOfStrings.has(propertyNameOrValue)) {
                if (!unicodeSetsMode) {
                  error(Diagnostics.Any_Unicode_property_that_would_possibly_match_more_than_a_single_character_is_only_available_when_the_Unicode_Sets_v_flag_is_set, propertyNameOrValueStart, pos - propertyNameOrValueStart);
                } else if (isCharacterComplement) {
                  error(Diagnostics.Anything_that_would_possibly_match_more_than_a_single_character_is_invalid_inside_a_negated_character_class, propertyNameOrValueStart, pos - propertyNameOrValueStart);
                } else {
                  mayContainStrings ***REMOVED*** true;
                }
              } else if (!valuesOfNonBinaryUnicodeProperties.General_Category.has(propertyNameOrValue) && !binaryUnicodeProperties.has(propertyNameOrValue)) {
                error(Diagnostics.Unknown_Unicode_property_name_or_value, propertyNameOrValueStart, pos - propertyNameOrValueStart);
                const suggestion ***REMOVED*** getSpellingSuggestion(propertyNameOrValue, [...valuesOfNonBinaryUnicodeProperties.General_Category, ...binaryUnicodeProperties, ...binaryUnicodePropertiesOfStrings], identity);
                if (suggestion) {
                  error(Diagnostics.Did_you_mean_0, propertyNameOrValueStart, pos - propertyNameOrValueStart, suggestion);
                }
              }
            }
            scanExpectedChar(125 /* closeBrace */);
            if (!anyUnicodeMode) {
              error(Diagnostics.Unicode_property_value_expressions_are_only_available_when_the_Unicode_u_flag_or_the_Unicode_Sets_v_flag_is_set, start2, pos - start2);
            }
          } else if (anyUnicodeModeOrNonAnnexB) {
            error(Diagnostics._0_must_be_followed_by_a_Unicode_property_value_expression_enclosed_in_braces, pos - 2, 2, String.fromCharCode(ch));
          } else {
            pos--;
            return false;
          }
          return true;
      }
      return false;
    }
    function scanWordCharacters() {
      let value ***REMOVED*** "";
      while (true) {
        const ch ***REMOVED*** charCodeChecked(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */ || !isWordCharacter(ch)) {
          break;
        }
        value +***REMOVED*** String.fromCharCode(ch);
        pos++;
      }
      return value;
    }
    function scanSourceCharacter() {
      const size ***REMOVED*** anyUnicodeMode ? charSize(codePointChecked(pos)) : 1;
      pos +***REMOVED*** size;
      return size > 0 ? text.substring(pos - size, pos) : "";
    }
    function scanExpectedChar(ch) {
      if (charCodeChecked(pos) ***REMOVED******REMOVED******REMOVED*** ch) {
        pos++;
      } else {
        error(Diagnostics._0_expected, pos, 0, String.fromCharCode(ch));
      }
    }
    scanDisjunction(
      /*isInGroup*/
      false
    );
    forEach(groupNameReferences, (reference) ***REMOVED***> {
      if (!(groupSpecifiers ***REMOVED******REMOVED*** null ? void 0 : groupSpecifiers.has(reference.name))) {
        error(Diagnostics.There_is_no_capturing_group_named_0_in_this_regular_expression, reference.pos, reference.end - reference.pos, reference.name);
        if (groupSpecifiers) {
          const suggestion ***REMOVED*** getSpellingSuggestion(reference.name, groupSpecifiers, identity);
          if (suggestion) {
            error(Diagnostics.Did_you_mean_0, reference.pos, reference.end - reference.pos, suggestion);
          }
        }
      }
    });
    forEach(decimalEscapes, (escape) ***REMOVED***> {
      if (escape.value > numberOfCapturingGroups) {
        if (numberOfCapturingGroups) {
          error(Diagnostics.This_backreference_refers_to_a_group_that_does_not_exist_There_are_only_0_capturing_groups_in_this_regular_expression, escape.pos, escape.end - escape.pos, numberOfCapturingGroups);
        } else {
          error(Diagnostics.This_backreference_refers_to_a_group_that_does_not_exist_There_are_no_capturing_groups_in_this_regular_expression, escape.pos, escape.end - escape.pos);
        }
      }
    });
  }
  function checkRegularExpressionFlagAvailability(flag, size) {
    const availableFrom ***REMOVED*** regExpFlagToFirstAvailableLanguageVersion.get(flag);
    if (availableFrom && languageVersion < availableFrom) {
      error(Diagnostics.This_regular_expression_flag_is_only_available_when_targeting_0_or_later, pos, size, getNameOfScriptTarget(availableFrom));
    }
  }
  function appendIfCommentDirective(commentDirectives2, text2, commentDirectiveRegEx, lineStart) {
    const type ***REMOVED*** getDirectiveFromComment(text2.trimStart(), commentDirectiveRegEx);
    if (type ***REMOVED******REMOVED******REMOVED*** void 0) {
      return commentDirectives2;
    }
    return append(
      commentDirectives2,
      {
        range: { pos: lineStart, end: pos },
        type
      }
    );
  }
  function getDirectiveFromComment(text2, commentDirectiveRegEx) {
    const match ***REMOVED*** commentDirectiveRegEx.exec(text2);
    if (!match) {
      return void 0;
    }
    switch (match[1]) {
      case "ts-expect-error":
        return 0 /* ExpectError */;
      case "ts-ignore":
        return 1 /* Ignore */;
    }
    return void 0;
  }
  function reScanTemplateToken(isTaggedTemplate) {
    pos ***REMOVED*** tokenStart;
    return token ***REMOVED*** scanTemplateAndSetTokenValue(!isTaggedTemplate);
  }
  function reScanTemplateHeadOrNoSubstitutionTemplate() {
    pos ***REMOVED*** tokenStart;
    return token ***REMOVED*** scanTemplateAndSetTokenValue(
      /*shouldEmitInvalidEscapeError*/
      true
    );
  }
  function reScanJsxToken(allowMultilineJsxText ***REMOVED*** true) {
    pos ***REMOVED*** tokenStart ***REMOVED*** fullStartPos;
    return token ***REMOVED*** scanJsxToken(allowMultilineJsxText);
  }
  function reScanLessThanToken() {
    if (token ***REMOVED******REMOVED******REMOVED*** 48 /* LessThanLessThanToken */) {
      pos ***REMOVED*** tokenStart + 1;
      return token ***REMOVED*** 30 /* LessThanToken */;
    }
    return token;
  }
  function reScanHashToken() {
    if (token ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
      pos ***REMOVED*** tokenStart + 1;
      return token ***REMOVED*** 63 /* HashToken */;
    }
    return token;
  }
  function reScanQuestionToken() {
    Debug.assert(token ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */, "'reScanQuestionToken' should only be called on a '??'");
    pos ***REMOVED*** tokenStart + 1;
    return token ***REMOVED*** 58 /* QuestionToken */;
  }
  function scanJsxToken(allowMultilineJsxText ***REMOVED*** true) {
    fullStartPos ***REMOVED*** tokenStart ***REMOVED*** pos;
    if (pos >***REMOVED*** end) {
      return token ***REMOVED*** 1 /* EndOfFileToken */;
    }
    let char ***REMOVED*** charCodeUnchecked(pos);
    if (char ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */) {
      if (charCodeUnchecked(pos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
        pos +***REMOVED*** 2;
        return token ***REMOVED*** 31 /* LessThanSlashToken */;
      }
      pos++;
      return token ***REMOVED*** 30 /* LessThanToken */;
    }
    if (char ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
      pos++;
      return token ***REMOVED*** 19 /* OpenBraceToken */;
    }
    let firstNonWhitespace ***REMOVED*** 0;
    while (pos < end) {
      char ***REMOVED*** charCodeUnchecked(pos);
      if (char ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
        break;
      }
      if (char ***REMOVED******REMOVED******REMOVED*** 60 /* lessThan */) {
        if (isConflictMarkerTrivia(text, pos)) {
          pos ***REMOVED*** scanConflictMarkerTrivia(text, pos, error);
          return token ***REMOVED*** 7 /* ConflictMarkerTrivia */;
        }
        break;
      }
      if (char ***REMOVED******REMOVED******REMOVED*** 62 /* greaterThan */) {
        error(Diagnostics.Unexpected_token_Did_you_mean_or_gt, pos, 1);
      }
      if (char ***REMOVED******REMOVED******REMOVED*** 125 /* closeBrace */) {
        error(Diagnostics.Unexpected_token_Did_you_mean_or_rbrace, pos, 1);
      }
      if (isLineBreak(char) && firstNonWhitespace ***REMOVED******REMOVED******REMOVED*** 0) {
        firstNonWhitespace ***REMOVED*** -1;
      } else if (!allowMultilineJsxText && isLineBreak(char) && firstNonWhitespace > 0) {
        break;
      } else if (!isWhiteSpaceLike(char)) {
        firstNonWhitespace ***REMOVED*** pos;
      }
      pos++;
    }
    tokenValue ***REMOVED*** text.substring(fullStartPos, pos);
    return firstNonWhitespace ***REMOVED******REMOVED******REMOVED*** -1 ? 13 /* JsxTextAllWhiteSpaces */ : 12 /* JsxText */;
  }
  function scanJsxIdentifier() {
    if (tokenIsIdentifierOrKeyword(token)) {
      while (pos < end) {
        const ch ***REMOVED*** charCodeUnchecked(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
          tokenValue +***REMOVED*** "-";
          pos++;
          continue;
        }
        const oldPos ***REMOVED*** pos;
        tokenValue +***REMOVED*** scanIdentifierParts();
        if (pos ***REMOVED******REMOVED******REMOVED*** oldPos) {
          break;
        }
      }
      return getIdentifierToken();
    }
    return token;
  }
  function scanJsxAttributeValue() {
    fullStartPos ***REMOVED*** pos;
    switch (charCodeUnchecked(pos)) {
      case 34 /* doubleQuote */:
      case 39 /* singleQuote */:
        tokenValue ***REMOVED*** scanString(
          /*jsxAttributeString*/
          true
        );
        return token ***REMOVED*** 11 /* StringLiteral */;
      default:
        return scan();
    }
  }
  function reScanJsxAttributeValue() {
    pos ***REMOVED*** tokenStart ***REMOVED*** fullStartPos;
    return scanJsxAttributeValue();
  }
  function scanJSDocCommentTextToken(inBackticks) {
    fullStartPos ***REMOVED*** tokenStart ***REMOVED*** pos;
    tokenFlags ***REMOVED*** 0 /* None */;
    if (pos >***REMOVED*** end) {
      return token ***REMOVED*** 1 /* EndOfFileToken */;
    }
    for (let ch ***REMOVED*** charCodeUnchecked(pos); pos < end && (!isLineBreak(ch) && ch !***REMOVED******REMOVED*** 96 /* backtick */); ch ***REMOVED*** codePointUnchecked(++pos)) {
      if (!inBackticks) {
        if (ch ***REMOVED******REMOVED******REMOVED*** 123 /* openBrace */) {
          break;
        } else if (ch ***REMOVED******REMOVED******REMOVED*** 64 /* at */ && pos - 1 >***REMOVED*** 0 && isWhiteSpaceSingleLine(charCodeUnchecked(pos - 1)) && !(pos + 1 < end && isWhiteSpaceLike(charCodeUnchecked(pos + 1)))) {
          break;
        }
      }
    }
    if (pos ***REMOVED******REMOVED******REMOVED*** tokenStart) {
      return scanJsDocToken();
    }
    tokenValue ***REMOVED*** text.substring(tokenStart, pos);
    return token ***REMOVED*** 82 /* JSDocCommentTextToken */;
  }
  function scanJsDocToken() {
    fullStartPos ***REMOVED*** tokenStart ***REMOVED*** pos;
    tokenFlags ***REMOVED*** 0 /* None */;
    if (pos >***REMOVED*** end) {
      return token ***REMOVED*** 1 /* EndOfFileToken */;
    }
    const ch ***REMOVED*** codePointUnchecked(pos);
    pos +***REMOVED*** charSize(ch);
    switch (ch) {
      case 9 /* tab */:
      case 11 /* verticalTab */:
      case 12 /* formFeed */:
      case 32 /* space */:
        while (pos < end && isWhiteSpaceSingleLine(charCodeUnchecked(pos))) {
          pos++;
        }
        return token ***REMOVED*** 5 /* WhitespaceTrivia */;
      case 64 /* at */:
        return token ***REMOVED*** 60 /* AtToken */;
      case 13 /* carriageReturn */:
        if (charCodeUnchecked(pos) ***REMOVED******REMOVED******REMOVED*** 10 /* lineFeed */) {
          pos++;
        }
      // falls through
      case 10 /* lineFeed */:
        tokenFlags |***REMOVED*** 1 /* PrecedingLineBreak */;
        return token ***REMOVED*** 4 /* NewLineTrivia */;
      case 42 /* asterisk */:
        return token ***REMOVED*** 42 /* AsteriskToken */;
      case 123 /* openBrace */:
        return token ***REMOVED*** 19 /* OpenBraceToken */;
      case 125 /* closeBrace */:
        return token ***REMOVED*** 20 /* CloseBraceToken */;
      case 91 /* openBracket */:
        return token ***REMOVED*** 23 /* OpenBracketToken */;
      case 93 /* closeBracket */:
        return token ***REMOVED*** 24 /* CloseBracketToken */;
      case 40 /* openParen */:
        return token ***REMOVED*** 21 /* OpenParenToken */;
      case 41 /* closeParen */:
        return token ***REMOVED*** 22 /* CloseParenToken */;
      case 60 /* lessThan */:
        return token ***REMOVED*** 30 /* LessThanToken */;
      case 62 /* greaterThan */:
        return token ***REMOVED*** 32 /* GreaterThanToken */;
      case 61 /* equals */:
        return token ***REMOVED*** 64 /* EqualsToken */;
      case 44 /* comma */:
        return token ***REMOVED*** 28 /* CommaToken */;
      case 46 /* dot */:
        return token ***REMOVED*** 25 /* DotToken */;
      case 96 /* backtick */:
        return token ***REMOVED*** 62 /* BacktickToken */;
      case 35 /* hash */:
        return token ***REMOVED*** 63 /* HashToken */;
      case 92 /* backslash */:
        pos--;
        const extendedCookedChar ***REMOVED*** peekExtendedUnicodeEscape();
        if (extendedCookedChar >***REMOVED*** 0 && isIdentifierStart(extendedCookedChar, languageVersion)) {
          tokenValue ***REMOVED*** scanExtendedUnicodeEscape(
            /*shouldEmitInvalidEscapeError*/
            true
          ) + scanIdentifierParts();
          return token ***REMOVED*** getIdentifierToken();
        }
        const cookedChar ***REMOVED*** peekUnicodeEscape();
        if (cookedChar >***REMOVED*** 0 && isIdentifierStart(cookedChar, languageVersion)) {
          pos +***REMOVED*** 6;
          tokenFlags |***REMOVED*** 1024 /* UnicodeEscape */;
          tokenValue ***REMOVED*** String.fromCharCode(cookedChar) + scanIdentifierParts();
          return token ***REMOVED*** getIdentifierToken();
        }
        pos++;
        return token ***REMOVED*** 0 /* Unknown */;
    }
    if (isIdentifierStart(ch, languageVersion)) {
      let char ***REMOVED*** ch;
      while (pos < end && isIdentifierPart(char ***REMOVED*** codePointUnchecked(pos), languageVersion) || char ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) pos +***REMOVED*** charSize(char);
      tokenValue ***REMOVED*** text.substring(tokenStart, pos);
      if (char ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */) {
        tokenValue +***REMOVED*** scanIdentifierParts();
      }
      return token ***REMOVED*** getIdentifierToken();
    } else {
      return token ***REMOVED*** 0 /* Unknown */;
    }
  }
  function speculationHelper(callback, isLookahead) {
    const savePos ***REMOVED*** pos;
    const saveStartPos ***REMOVED*** fullStartPos;
    const saveTokenPos ***REMOVED*** tokenStart;
    const saveToken ***REMOVED*** token;
    const saveTokenValue ***REMOVED*** tokenValue;
    const saveTokenFlags ***REMOVED*** tokenFlags;
    const result ***REMOVED*** callback();
    if (!result || isLookahead) {
      pos ***REMOVED*** savePos;
      fullStartPos ***REMOVED*** saveStartPos;
      tokenStart ***REMOVED*** saveTokenPos;
      token ***REMOVED*** saveToken;
      tokenValue ***REMOVED*** saveTokenValue;
      tokenFlags ***REMOVED*** saveTokenFlags;
    }
    return result;
  }
  function scanRange(start2, length3, callback) {
    const saveEnd ***REMOVED*** end;
    const savePos ***REMOVED*** pos;
    const saveStartPos ***REMOVED*** fullStartPos;
    const saveTokenPos ***REMOVED*** tokenStart;
    const saveToken ***REMOVED*** token;
    const saveTokenValue ***REMOVED*** tokenValue;
    const saveTokenFlags ***REMOVED*** tokenFlags;
    const saveErrorExpectations ***REMOVED*** commentDirectives;
    setText(text, start2, length3);
    const result ***REMOVED*** callback();
    end ***REMOVED*** saveEnd;
    pos ***REMOVED*** savePos;
    fullStartPos ***REMOVED*** saveStartPos;
    tokenStart ***REMOVED*** saveTokenPos;
    token ***REMOVED*** saveToken;
    tokenValue ***REMOVED*** saveTokenValue;
    tokenFlags ***REMOVED*** saveTokenFlags;
    commentDirectives ***REMOVED*** saveErrorExpectations;
    return result;
  }
  function lookAhead(callback) {
    return speculationHelper(
      callback,
      /*isLookahead*/
      true
    );
  }
  function tryScan(callback) {
    return speculationHelper(
      callback,
      /*isLookahead*/
      false
    );
  }
  function getText() {
    return text;
  }
  function clearCommentDirectives() {
    commentDirectives ***REMOVED*** void 0;
  }
  function setText(newText, start2, length3) {
    text ***REMOVED*** newText || "";
    end ***REMOVED*** length3 ***REMOVED******REMOVED******REMOVED*** void 0 ? text.length : start2 + length3;
    resetTokenState(start2 || 0);
  }
  function setOnError(errorCallback) {
    onError ***REMOVED*** errorCallback;
  }
  function setScriptTarget(scriptTarget) {
    languageVersion ***REMOVED*** scriptTarget;
  }
  function setLanguageVariant(variant) {
    languageVariant ***REMOVED*** variant;
  }
  function setScriptKind(kind) {
    scriptKind ***REMOVED*** kind;
  }
  function setJSDocParsingMode(kind) {
    jsDocParsingMode ***REMOVED*** kind;
  }
  function resetTokenState(position) {
    Debug.assert(position >***REMOVED*** 0);
    pos ***REMOVED*** position;
    fullStartPos ***REMOVED*** position;
    tokenStart ***REMOVED*** position;
    token ***REMOVED*** 0 /* Unknown */;
    tokenValue ***REMOVED*** void 0;
    tokenFlags ***REMOVED*** 0 /* None */;
  }
  function setSkipJsDocLeadingAsterisks(skip) {
    skipJsDocLeadingAsterisks +***REMOVED*** skip ? 1 : -1;
  }
}
function codePointAt(s, i) {
  return s.codePointAt(i);
}
function charSize(ch) {
  if (ch >***REMOVED*** 65536) {
    return 2;
  }
  if (ch ***REMOVED******REMOVED******REMOVED*** -1 /* EOF */) {
    return 0;
  }
  return 1;
}
function utf16EncodeAsStringFallback(codePoint) {
  Debug.assert(0 <***REMOVED*** codePoint && codePoint <***REMOVED*** 1114111);
  if (codePoint <***REMOVED*** 65535) {
    return String.fromCharCode(codePoint);
  }
  const codeUnit1 ***REMOVED*** Math.floor((codePoint - 65536) / 1024) + 55296;
  const codeUnit2 ***REMOVED*** (codePoint - 65536) % 1024 + 56320;
  return String.fromCharCode(codeUnit1, codeUnit2);
}
var utf16EncodeAsStringWorker ***REMOVED*** String.fromCodePoint ? (codePoint) ***REMOVED***> String.fromCodePoint(codePoint) : utf16EncodeAsStringFallback;
function utf16EncodeAsString(codePoint) {
  return utf16EncodeAsStringWorker(codePoint);
}
var nonBinaryUnicodeProperties ***REMOVED*** new Map(Object.entries({
  General_Category: "General_Category",
  gc: "General_Category",
  Script: "Script",
  sc: "Script",
  Script_Extensions: "Script_Extensions",
  scx: "Script_Extensions"
}));
var binaryUnicodeProperties ***REMOVED*** /* @__PURE__ */ new Set(["ASCII", "ASCII_Hex_Digit", "AHex", "Alphabetic", "Alpha", "Any", "Assigned", "Bidi_Control", "Bidi_C", "Bidi_Mirrored", "Bidi_M", "Case_Ignorable", "CI", "Cased", "Changes_When_Casefolded", "CWCF", "Changes_When_Casemapped", "CWCM", "Changes_When_Lowercased", "CWL", "Changes_When_NFKC_Casefolded", "CWKCF", "Changes_When_Titlecased", "CWT", "Changes_When_Uppercased", "CWU", "Dash", "Default_Ignorable_Code_Point", "DI", "Deprecated", "Dep", "Diacritic", "Dia", "Emoji", "Emoji_Component", "EComp", "Emoji_Modifier", "EMod", "Emoji_Modifier_Base", "EBase", "Emoji_Presentation", "EPres", "Extended_Pictographic", "ExtPict", "Extender", "Ext", "Grapheme_Base", "Gr_Base", "Grapheme_Extend", "Gr_Ext", "Hex_Digit", "Hex", "IDS_Binary_Operator", "IDSB", "IDS_Trinary_Operator", "IDST", "ID_Continue", "IDC", "ID_Start", "IDS", "Ideographic", "Ideo", "Join_Control", "Join_C", "Logical_Order_Exception", "LOE", "Lowercase", "Lower", "Math", "Noncharacter_Code_Point", "NChar", "Pattern_Syntax", "Pat_Syn", "Pattern_White_Space", "Pat_WS", "Quotation_Mark", "QMark", "Radical", "Regional_Indicator", "RI", "Sentence_Terminal", "STerm", "Soft_Dotted", "SD", "Terminal_Punctuation", "Term", "Unified_Ideograph", "UIdeo", "Uppercase", "Upper", "Variation_Selector", "VS", "White_Space", "space", "XID_Continue", "XIDC", "XID_Start", "XIDS"]);
var binaryUnicodePropertiesOfStrings ***REMOVED*** /* @__PURE__ */ new Set(["Basic_Emoji", "Emoji_Keycap_Sequence", "RGI_Emoji_Modifier_Sequence", "RGI_Emoji_Flag_Sequence", "RGI_Emoji_Tag_Sequence", "RGI_Emoji_ZWJ_Sequence", "RGI_Emoji"]);
var valuesOfNonBinaryUnicodeProperties ***REMOVED*** {
  General_Category: /* @__PURE__ */ new Set(["C", "Other", "Cc", "Control", "cntrl", "Cf", "Format", "Cn", "Unassigned", "Co", "Private_Use", "Cs", "Surrogate", "L", "Letter", "LC", "Cased_Letter", "Ll", "Lowercase_Letter", "Lm", "Modifier_Letter", "Lo", "Other_Letter", "Lt", "Titlecase_Letter", "Lu", "Uppercase_Letter", "M", "Mark", "Combining_Mark", "Mc", "Spacing_Mark", "Me", "Enclosing_Mark", "Mn", "Nonspacing_Mark", "N", "Number", "Nd", "Decimal_Number", "digit", "Nl", "Letter_Number", "No", "Other_Number", "P", "Punctuation", "punct", "Pc", "Connector_Punctuation", "Pd", "Dash_Punctuation", "Pe", "Close_Punctuation", "Pf", "Final_Punctuation", "Pi", "Initial_Punctuation", "Po", "Other_Punctuation", "Ps", "Open_Punctuation", "S", "Symbol", "Sc", "Currency_Symbol", "Sk", "Modifier_Symbol", "Sm", "Math_Symbol", "So", "Other_Symbol", "Z", "Separator", "Zl", "Line_Separator", "Zp", "Paragraph_Separator", "Zs", "Space_Separator"]),
  Script: /* @__PURE__ */ new Set(["Adlm", "Adlam", "Aghb", "Caucasian_Albanian", "Ahom", "Arab", "Arabic", "Armi", "Imperial_Aramaic", "Armn", "Armenian", "Avst", "Avestan", "Bali", "Balinese", "Bamu", "Bamum", "Bass", "Bassa_Vah", "Batk", "Batak", "Beng", "Bengali", "Bhks", "Bhaiksuki", "Bopo", "Bopomofo", "Brah", "Brahmi", "Brai", "Braille", "Bugi", "Buginese", "Buhd", "Buhid", "Cakm", "Chakma", "Cans", "Canadian_Aboriginal", "Cari", "Carian", "Cham", "Cher", "Cherokee", "Chrs", "Chorasmian", "Copt", "Coptic", "Qaac", "Cpmn", "Cypro_Minoan", "Cprt", "Cypriot", "Cyrl", "Cyrillic", "Deva", "Devanagari", "Diak", "Dives_Akuru", "Dogr", "Dogra", "Dsrt", "Deseret", "Dupl", "Duployan", "Egyp", "Egyptian_Hieroglyphs", "Elba", "Elbasan", "Elym", "Elymaic", "Ethi", "Ethiopic", "Geor", "Georgian", "Glag", "Glagolitic", "Gong", "Gunjala_Gondi", "Gonm", "Masaram_Gondi", "Goth", "Gothic", "Gran", "Grantha", "Grek", "Greek", "Gujr", "Gujarati", "Guru", "Gurmukhi", "Hang", "Hangul", "Hani", "Han", "Hano", "Hanunoo", "Hatr", "Hatran", "Hebr", "Hebrew", "Hira", "Hiragana", "Hluw", "Anatolian_Hieroglyphs", "Hmng", "Pahawh_Hmong", "Hmnp", "Nyiakeng_Puachue_Hmong", "Hrkt", "Katakana_Or_Hiragana", "Hung", "Old_Hungarian", "Ital", "Old_Italic", "Java", "Javanese", "Kali", "Kayah_Li", "Kana", "Katakana", "Kawi", "Khar", "Kharoshthi", "Khmr", "Khmer", "Khoj", "Khojki", "Kits", "Khitan_Small_Script", "Knda", "Kannada", "Kthi", "Kaithi", "Lana", "Tai_Tham", "Laoo", "Lao", "Latn", "Latin", "Lepc", "Lepcha", "Limb", "Limbu", "Lina", "Linear_A", "Linb", "Linear_B", "Lisu", "Lyci", "Lycian", "Lydi", "Lydian", "Mahj", "Mahajani", "Maka", "Makasar", "Mand", "Mandaic", "Mani", "Manichaean", "Marc", "Marchen", "Medf", "Medefaidrin", "Mend", "Mende_Kikakui", "Merc", "Meroitic_Cursive", "Mero", "Meroitic_Hieroglyphs", "Mlym", "Malayalam", "Modi", "Mong", "Mongolian", "Mroo", "Mro", "Mtei", "Meetei_Mayek", "Mult", "Multani", "Mymr", "Myanmar", "Nagm", "Nag_Mundari", "Nand", "Nandinagari", "Narb", "Old_North_Arabian", "Nbat", "Nabataean", "Newa", "Nkoo", "Nko", "Nshu", "Nushu", "Ogam", "Ogham", "Olck", "Ol_Chiki", "Orkh", "Old_Turkic", "Orya", "Oriya", "Osge", "Osage", "Osma", "Osmanya", "Ougr", "Old_Uyghur", "Palm", "Palmyrene", "Pauc", "Pau_Cin_Hau", "Perm", "Old_Permic", "Phag", "Phags_Pa", "Phli", "Inscriptional_Pahlavi", "Phlp", "Psalter_Pahlavi", "Phnx", "Phoenician", "Plrd", "Miao", "Prti", "Inscriptional_Parthian", "Rjng", "Rejang", "Rohg", "Hanifi_Rohingya", "Runr", "Runic", "Samr", "Samaritan", "Sarb", "Old_South_Arabian", "Saur", "Saurashtra", "Sgnw", "SignWriting", "Shaw", "Shavian", "Shrd", "Sharada", "Sidd", "Siddham", "Sind", "Khudawadi", "Sinh", "Sinhala", "Sogd", "Sogdian", "Sogo", "Old_Sogdian", "Sora", "Sora_Sompeng", "Soyo", "Soyombo", "Sund", "Sundanese", "Sylo", "Syloti_Nagri", "Syrc", "Syriac", "Tagb", "Tagbanwa", "Takr", "Takri", "Tale", "Tai_Le", "Talu", "New_Tai_Lue", "Taml", "Tamil", "Tang", "Tangut", "Tavt", "Tai_Viet", "Telu", "Telugu", "Tfng", "Tifinagh", "Tglg", "Tagalog", "Thaa", "Thaana", "Thai", "Tibt", "Tibetan", "Tirh", "Tirhuta", "Tnsa", "Tangsa", "Toto", "Ugar", "Ugaritic", "Vaii", "Vai", "Vith", "Vithkuqi", "Wara", "Warang_Citi", "Wcho", "Wancho", "Xpeo", "Old_Persian", "Xsux", "Cuneiform", "Yezi", "Yezidi", "Yiii", "Yi", "Zanb", "Zanabazar_Square", "Zinh", "Inherited", "Qaai", "Zyyy", "Common", "Zzzz", "Unknown"]),
  Script_Extensions: void 0
};
valuesOfNonBinaryUnicodeProperties.Script_Extensions ***REMOVED*** valuesOfNonBinaryUnicodeProperties.Script;

// src/compiler/utilitiesPublic.ts
function isExternalModuleNameRelative(moduleName) {
  return pathIsRelative(moduleName) || isRootedDiskPath(moduleName);
}
function sortAndDeduplicateDiagnostics(diagnostics) {
  return sortAndDeduplicate(diagnostics, compareDiagnostics, diagnosticsEqualityComparer);
}
var targetToLibMap ***REMOVED*** /* @__PURE__ */ new Map([
  [99 /* ESNext */, "lib.esnext.full.d.ts"],
  [11 /* ES2024 */, "lib.es2024.full.d.ts"],
  [10 /* ES2023 */, "lib.es2023.full.d.ts"],
  [9 /* ES2022 */, "lib.es2022.full.d.ts"],
  [8 /* ES2021 */, "lib.es2021.full.d.ts"],
  [7 /* ES2020 */, "lib.es2020.full.d.ts"],
  [6 /* ES2019 */, "lib.es2019.full.d.ts"],
  [5 /* ES2018 */, "lib.es2018.full.d.ts"],
  [4 /* ES2017 */, "lib.es2017.full.d.ts"],
  [3 /* ES2016 */, "lib.es2016.full.d.ts"],
  [2 /* ES2015 */, "lib.es6.d.ts"]
  // We don't use lib.es2015.full.d.ts due to breaking change.
]);
function getDefaultLibFileName(options) {
  const target ***REMOVED*** getEmitScriptTarget(options);
  switch (target) {
    case 99 /* ESNext */:
    case 11 /* ES2024 */:
    case 10 /* ES2023 */:
    case 9 /* ES2022 */:
    case 8 /* ES2021 */:
    case 7 /* ES2020 */:
    case 6 /* ES2019 */:
    case 5 /* ES2018 */:
    case 4 /* ES2017 */:
    case 3 /* ES2016 */:
    case 2 /* ES2015 */:
      return targetToLibMap.get(target);
    default:
      return "lib.d.ts";
  }
}
function textSpanEnd(span) {
  return span.start + span.length;
}
function textSpanIsEmpty(span) {
  return span.length ***REMOVED******REMOVED******REMOVED*** 0;
}
function textSpanContainsPosition(span, position) {
  return position >***REMOVED*** span.start && position < textSpanEnd(span);
}
function textRangeContainsPositionInclusive(range, position) {
  return position >***REMOVED*** range.pos && position <***REMOVED*** range.end;
}
function createTextSpan(start, length2) {
  if (start < 0) {
    throw new Error("start < 0");
  }
  if (length2 < 0) {
    throw new Error("length < 0");
  }
  return { start, length: length2 };
}
function createTextSpanFromBounds(start, end) {
  return createTextSpan(start, end - start);
}
function textChangeRangeNewSpan(range) {
  return createTextSpan(range.span.start, range.newLength);
}
function textChangeRangeIsUnchanged(range) {
  return textSpanIsEmpty(range.span) && range.newLength ***REMOVED******REMOVED******REMOVED*** 0;
}
function createTextChangeRange(span, newLength) {
  if (newLength < 0) {
    throw new Error("newLength < 0");
  }
  return { span, newLength };
}
var unchangedTextChangeRange ***REMOVED*** createTextChangeRange(createTextSpan(0, 0), 0);
function isParameterPropertyDeclaration(node, parent) {
  return isParameter(node) && hasSyntacticModifier(node, 31 /* ParameterPropertyModifier */) && parent.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */;
}
function walkUpBindingElementsAndPatterns(binding) {
  let node ***REMOVED*** binding.parent;
  while (isBindingElement(node.parent)) {
    node ***REMOVED*** node.parent.parent;
  }
  return node.parent;
}
function getCombinedFlags(node, getFlags) {
  if (isBindingElement(node)) {
    node ***REMOVED*** walkUpBindingElementsAndPatterns(node);
  }
  let flags ***REMOVED*** getFlags(node);
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
    node ***REMOVED*** node.parent;
  }
  if (node && node.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
    flags |***REMOVED*** getFlags(node);
    node ***REMOVED*** node.parent;
  }
  if (node && node.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */) {
    flags |***REMOVED*** getFlags(node);
  }
  return flags;
}
function getCombinedModifierFlags(node) {
  return getCombinedFlags(node, getEffectiveModifierFlags);
}
function getCombinedNodeFlags(node) {
  return getCombinedFlags(node, getNodeFlags);
}
function getNodeFlags(node) {
  return node.flags;
}
var supportedLocaleDirectories ***REMOVED*** ["cs", "de", "es", "fr", "it", "ja", "ko", "pl", "pt-br", "ru", "tr", "zh-cn", "zh-tw"];
function validateLocaleAndSetLanguage(locale, sys2, errors) {
  const lowerCaseLocale ***REMOVED*** locale.toLowerCase();
  const matchResult ***REMOVED*** /^([a-z]+)(?:[_-]([a-z]+))?$/.exec(lowerCaseLocale);
  if (!matchResult) {
    if (errors) {
      errors.push(createCompilerDiagnostic(Diagnostics.Locale_must_be_of_the_form_language_or_language_territory_For_example_0_or_1, "en", "ja-jp"));
    }
    return;
  }
  const language ***REMOVED*** matchResult[1];
  const territory ***REMOVED*** matchResult[2];
  if (contains(supportedLocaleDirectories, lowerCaseLocale) && !trySetLanguageAndTerritory(language, territory, errors)) {
    trySetLanguageAndTerritory(
      language,
      /*territory*/
      void 0,
      errors
    );
  }
  setUILocale(locale);
  function trySetLanguageAndTerritory(language2, territory2, errors2) {
    const compilerFilePath ***REMOVED*** normalizePath(sys2.getExecutingFilePath());
    const containingDirectoryPath ***REMOVED*** getDirectoryPath(compilerFilePath);
    let filePath ***REMOVED*** combinePaths(containingDirectoryPath, language2);
    if (territory2) {
      filePath ***REMOVED*** filePath + "-" + territory2;
    }
    filePath ***REMOVED*** sys2.resolvePath(combinePaths(filePath, "diagnosticMessages.generated.json"));
    if (!sys2.fileExists(filePath)) {
      return false;
    }
    let fileContents ***REMOVED*** "";
    try {
      fileContents ***REMOVED*** sys2.readFile(filePath);
    } catch {
      if (errors2) {
        errors2.push(createCompilerDiagnostic(Diagnostics.Unable_to_open_file_0, filePath));
      }
      return false;
    }
    try {
      setLocalizedDiagnosticMessages(JSON.parse(fileContents));
    } catch {
      if (errors2) {
        errors2.push(createCompilerDiagnostic(Diagnostics.Corrupted_locale_file_0, filePath));
      }
      return false;
    }
    return true;
  }
}
function getOriginalNode(node, nodeTest) {
  if (node) {
    while (node.original !***REMOVED******REMOVED*** void 0) {
      node ***REMOVED*** node.original;
    }
  }
  if (!node || !nodeTest) {
    return node;
  }
  return nodeTest(node) ? node : void 0;
}
function findAncestor(node, callback) {
  while (node) {
    const result ***REMOVED*** callback(node);
    if (result ***REMOVED******REMOVED******REMOVED*** "quit") {
      return void 0;
    } else if (result) {
      return node;
    }
    node ***REMOVED*** node.parent;
  }
  return void 0;
}
function isParseTreeNode(node) {
  return (node.flags & 16 /* Synthesized */) ***REMOVED******REMOVED******REMOVED*** 0;
}
function getParseTreeNode(node, nodeTest) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0 || isParseTreeNode(node)) {
    return node;
  }
  node ***REMOVED*** node.original;
  while (node) {
    if (isParseTreeNode(node)) {
      return !nodeTest || nodeTest(node) ? node : void 0;
    }
    node ***REMOVED*** node.original;
  }
}
function escapeLeadingUnderscores(identifier) {
  return identifier.length >***REMOVED*** 2 && identifier.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && identifier.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ ? "_" + identifier : identifier;
}
function unescapeLeadingUnderscores(identifier) {
  const id ***REMOVED*** identifier;
  return id.length >***REMOVED*** 3 && id.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && id.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && id.charCodeAt(2) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ ? id.substr(1) : id;
}
function idText(identifierOrPrivateName) {
  return unescapeLeadingUnderscores(identifierOrPrivateName.escapedText);
}
function identifierToKeywordKind(node) {
  const token ***REMOVED*** stringToToken(node.escapedText);
  return token ? tryCast(token, isKeyword) : void 0;
}
function symbolName(symbol) {
  if (symbol.valueDeclaration && isPrivateIdentifierClassElementDeclaration(symbol.valueDeclaration)) {
    return idText(symbol.valueDeclaration.name);
  }
  return unescapeLeadingUnderscores(symbol.escapedName);
}
function nameForNamelessJSDocTypedef(declaration) {
  const hostNode ***REMOVED*** declaration.parent.parent;
  if (!hostNode) {
    return void 0;
  }
  if (isDeclaration(hostNode)) {
    return getDeclarationIdentifier(hostNode);
  }
  switch (hostNode.kind) {
    case 244 /* VariableStatement */:
      if (hostNode.declarationList && hostNode.declarationList.declarations[0]) {
        return getDeclarationIdentifier(hostNode.declarationList.declarations[0]);
      }
      break;
    case 245 /* ExpressionStatement */:
      let expr ***REMOVED*** hostNode.expression;
      if (expr.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && expr.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
        expr ***REMOVED*** expr.left;
      }
      switch (expr.kind) {
        case 212 /* PropertyAccessExpression */:
          return expr.name;
        case 213 /* ElementAccessExpression */:
          const arg ***REMOVED*** expr.argumentExpression;
          if (isIdentifier(arg)) {
            return arg;
          }
      }
      break;
    case 218 /* ParenthesizedExpression */: {
      return getDeclarationIdentifier(hostNode.expression);
    }
    case 257 /* LabeledStatement */: {
      if (isDeclaration(hostNode.statement) || isExpression(hostNode.statement)) {
        return getDeclarationIdentifier(hostNode.statement);
      }
      break;
    }
  }
}
function getDeclarationIdentifier(node) {
  const name ***REMOVED*** getNameOfDeclaration(node);
  return name && isIdentifier(name) ? name : void 0;
}
function nodeHasName(statement, name) {
  if (isNamedDeclaration(statement) && isIdentifier(statement.name) && idText(statement.name) ***REMOVED******REMOVED******REMOVED*** idText(name)) {
    return true;
  }
  if (isVariableStatement(statement) && some(statement.declarationList.declarations, (d) ***REMOVED***> nodeHasName(d, name))) {
    return true;
  }
  return false;
}
function getNameOfJSDocTypedef(declaration) {
  return declaration.name || nameForNamelessJSDocTypedef(declaration);
}
function isNamedDeclaration(node) {
  return !!node.name;
}
function getNonAssignedNameOfDeclaration(declaration) {
  switch (declaration.kind) {
    case 80 /* Identifier */:
      return declaration;
    case 349 /* JSDocPropertyTag */:
    case 342 /* JSDocParameterTag */: {
      const { name } ***REMOVED*** declaration;
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
        return name.right;
      }
      break;
    }
    case 214 /* CallExpression */:
    case 227 /* BinaryExpression */: {
      const expr2 ***REMOVED*** declaration;
      switch (getAssignmentDeclarationKind(expr2)) {
        case 1 /* ExportsProperty */:
        case 4 /* ThisProperty */:
        case 5 /* Property */:
        case 3 /* PrototypeProperty */:
          return getElementOrPropertyAccessArgumentExpressionOrName(expr2.left);
        case 7 /* ObjectDefinePropertyValue */:
        case 8 /* ObjectDefinePropertyExports */:
        case 9 /* ObjectDefinePrototypeProperty */:
          return expr2.arguments[1];
        default:
          return void 0;
      }
    }
    case 347 /* JSDocTypedefTag */:
      return getNameOfJSDocTypedef(declaration);
    case 341 /* JSDocEnumTag */:
      return nameForNamelessJSDocTypedef(declaration);
    case 278 /* ExportAssignment */: {
      const { expression } ***REMOVED*** declaration;
      return isIdentifier(expression) ? expression : void 0;
    }
    case 213 /* ElementAccessExpression */:
      const expr ***REMOVED*** declaration;
      if (isBindableStaticElementAccessExpression(expr)) {
        return expr.argumentExpression;
      }
  }
  return declaration.name;
}
function getNameOfDeclaration(declaration) {
  if (declaration ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
  return getNonAssignedNameOfDeclaration(declaration) || (isFunctionExpression(declaration) || isArrowFunction(declaration) || isClassExpression(declaration) ? getAssignedName(declaration) : void 0);
}
function getAssignedName(node) {
  if (!node.parent) {
    return void 0;
  } else if (isPropertyAssignment(node.parent) || isBindingElement(node.parent)) {
    return node.parent.name;
  } else if (isBinaryExpression(node.parent) && node ***REMOVED******REMOVED******REMOVED*** node.parent.right) {
    if (isIdentifier(node.parent.left)) {
      return node.parent.left;
    } else if (isAccessExpression(node.parent.left)) {
      return getElementOrPropertyAccessArgumentExpressionOrName(node.parent.left);
    }
  } else if (isVariableDeclaration(node.parent) && isIdentifier(node.parent.name)) {
    return node.parent.name;
  }
}
function getDecorators(node) {
  if (hasDecorators(node)) {
    return filter(node.modifiers, isDecorator);
  }
}
function getModifiers(node) {
  if (hasSyntacticModifier(node, 98303 /* Modifier */)) {
    return filter(node.modifiers, isModifier);
  }
}
function getJSDocParameterTagsWorker(param, noCache) {
  if (param.name) {
    if (isIdentifier(param.name)) {
      const name ***REMOVED*** param.name.escapedText;
      return getJSDocTagsWorker(param.parent, noCache).filter((tag) ***REMOVED***> isJSDocParameterTag(tag) && isIdentifier(tag.name) && tag.name.escapedText ***REMOVED******REMOVED******REMOVED*** name);
    } else {
      const i ***REMOVED*** param.parent.parameters.indexOf(param);
      Debug.assert(i > -1, "Parameters should always be in their parents' parameter list");
      const paramTags ***REMOVED*** getJSDocTagsWorker(param.parent, noCache).filter(isJSDocParameterTag);
      if (i < paramTags.length) {
        return [paramTags[i]];
      }
    }
  }
  return emptyArray;
}
function getJSDocParameterTags(param) {
  return getJSDocParameterTagsWorker(
    param,
    /*noCache*/
    false
  );
}
function getJSDocParameterTagsNoCache(param) {
  return getJSDocParameterTagsWorker(
    param,
    /*noCache*/
    true
  );
}
function getJSDocTypeParameterTagsWorker(param, noCache) {
  const name ***REMOVED*** param.name.escapedText;
  return getJSDocTagsWorker(param.parent, noCache).filter((tag) ***REMOVED***> isJSDocTemplateTag(tag) && tag.typeParameters.some((tp) ***REMOVED***> tp.name.escapedText ***REMOVED******REMOVED******REMOVED*** name));
}
function getJSDocTypeParameterTags(param) {
  return getJSDocTypeParameterTagsWorker(
    param,
    /*noCache*/
    false
  );
}
function getJSDocTypeParameterTagsNoCache(param) {
  return getJSDocTypeParameterTagsWorker(
    param,
    /*noCache*/
    true
  );
}
function hasJSDocParameterTags(node) {
  return !!getFirstJSDocTag(node, isJSDocParameterTag);
}
function getJSDocAugmentsTag(node) {
  return getFirstJSDocTag(node, isJSDocAugmentsTag);
}
function getJSDocImplementsTags(node) {
  return getAllJSDocTags(node, isJSDocImplementsTag);
}
function getJSDocClassTag(node) {
  return getFirstJSDocTag(node, isJSDocClassTag);
}
function getJSDocPublicTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocPublicTag,
    /*noCache*/
    true
  );
}
function getJSDocPrivateTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocPrivateTag,
    /*noCache*/
    true
  );
}
function getJSDocProtectedTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocProtectedTag,
    /*noCache*/
    true
  );
}
function getJSDocReadonlyTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocReadonlyTag,
    /*noCache*/
    true
  );
}
function getJSDocOverrideTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocOverrideTag,
    /*noCache*/
    true
  );
}
function getJSDocDeprecatedTag(node) {
  return getFirstJSDocTag(node, isJSDocDeprecatedTag);
}
function getJSDocDeprecatedTagNoCache(node) {
  return getFirstJSDocTag(
    node,
    isJSDocDeprecatedTag,
    /*noCache*/
    true
  );
}
function getJSDocEnumTag(node) {
  return getFirstJSDocTag(node, isJSDocEnumTag);
}
function getJSDocThisTag(node) {
  return getFirstJSDocTag(node, isJSDocThisTag);
}
function getJSDocReturnTag(node) {
  return getFirstJSDocTag(node, isJSDocReturnTag);
}
function getJSDocSatisfiesTag(node) {
  return getFirstJSDocTag(node, isJSDocSatisfiesTag);
}
function getJSDocTypeTag(node) {
  const tag ***REMOVED*** getFirstJSDocTag(node, isJSDocTypeTag);
  if (tag && tag.typeExpression && tag.typeExpression.type) {
    return tag;
  }
  return void 0;
}
function getJSDocType(node) {
  let tag ***REMOVED*** getFirstJSDocTag(node, isJSDocTypeTag);
  if (!tag && isParameter(node)) {
    tag ***REMOVED*** find(getJSDocParameterTags(node), (tag2) ***REMOVED***> !!tag2.typeExpression);
  }
  return tag && tag.typeExpression && tag.typeExpression.type;
}
function getJSDocReturnType(node) {
  const returnTag ***REMOVED*** getJSDocReturnTag(node);
  if (returnTag && returnTag.typeExpression) {
    return returnTag.typeExpression.type;
  }
  const typeTag ***REMOVED*** getJSDocTypeTag(node);
  if (typeTag && typeTag.typeExpression) {
    const type ***REMOVED*** typeTag.typeExpression.type;
    if (isTypeLiteralNode(type)) {
      const sig ***REMOVED*** find(type.members, isCallSignatureDeclaration);
      return sig && sig.type;
    }
    if (isFunctionTypeNode(type) || isJSDocFunctionType(type)) {
      return type.type;
    }
  }
}
function getJSDocTagsWorker(node, noCache) {
  var _a;
  if (!canHaveJSDoc(node)) return emptyArray;
  let tags ***REMOVED*** (_a ***REMOVED*** node.jsDoc) ***REMOVED******REMOVED*** null ? void 0 : _a.jsDocCache;
  if (tags ***REMOVED******REMOVED******REMOVED*** void 0 || noCache) {
    const comments ***REMOVED*** getJSDocCommentsAndTags(node, noCache);
    Debug.assert(comments.length < 2 || comments[0] !***REMOVED******REMOVED*** comments[1]);
    tags ***REMOVED*** flatMap(comments, (j) ***REMOVED***> isJSDoc(j) ? j.tags : j);
    if (!noCache) {
      node.jsDoc ?? (node.jsDoc ***REMOVED*** []);
      node.jsDoc.jsDocCache ***REMOVED*** tags;
    }
  }
  return tags;
}
function getJSDocTags(node) {
  return getJSDocTagsWorker(
    node,
    /*noCache*/
    false
  );
}
function getFirstJSDocTag(node, predicate, noCache) {
  return find(getJSDocTagsWorker(node, noCache), predicate);
}
function getAllJSDocTags(node, predicate) {
  return getJSDocTags(node).filter(predicate);
}
function getTextOfJSDocComment(comment) {
  return typeof comment ***REMOVED******REMOVED******REMOVED*** "string" ? comment : comment ***REMOVED******REMOVED*** null ? void 0 : comment.map((c) ***REMOVED***> c.kind ***REMOVED******REMOVED******REMOVED*** 322 /* JSDocText */ ? c.text : formatJSDocLink(c)).join("");
}
function formatJSDocLink(link) {
  const kind ***REMOVED*** link.kind ***REMOVED******REMOVED******REMOVED*** 325 /* JSDocLink */ ? "link" : link.kind ***REMOVED******REMOVED******REMOVED*** 326 /* JSDocLinkCode */ ? "linkcode" : "linkplain";
  const name ***REMOVED*** link.name ? entityNameToString(link.name) : "";
  const space ***REMOVED*** link.name && (link.text ***REMOVED******REMOVED******REMOVED*** "" || link.text.startsWith("://")) ? "" : " ";
  return `{@${kind} ${name}${space}${link.text}}`;
}
function getEffectiveTypeParameterDeclarations(node) {
  if (isJSDocSignature(node)) {
    if (isJSDocOverloadTag(node.parent)) {
      const jsDoc ***REMOVED*** getJSDocRoot(node.parent);
      if (jsDoc && length(jsDoc.tags)) {
        return flatMap(jsDoc.tags, (tag) ***REMOVED***> isJSDocTemplateTag(tag) ? tag.typeParameters : void 0);
      }
    }
    return emptyArray;
  }
  if (isJSDocTypeAlias(node)) {
    Debug.assert(node.parent.kind ***REMOVED******REMOVED******REMOVED*** 321 /* JSDoc */);
    return flatMap(node.parent.tags, (tag) ***REMOVED***> isJSDocTemplateTag(tag) ? tag.typeParameters : void 0);
  }
  if (node.typeParameters) {
    return node.typeParameters;
  }
  if (canHaveIllegalTypeParameters(node) && node.typeParameters) {
    return node.typeParameters;
  }
  if (isInJSFile(node)) {
    const decls ***REMOVED*** getJSDocTypeParameterDeclarations(node);
    if (decls.length) {
      return decls;
    }
    const typeTag ***REMOVED*** getJSDocType(node);
    if (typeTag && isFunctionTypeNode(typeTag) && typeTag.typeParameters) {
      return typeTag.typeParameters;
    }
  }
  return emptyArray;
}
function getEffectiveConstraintOfTypeParameter(node) {
  return node.constraint ? node.constraint : isJSDocTemplateTag(node.parent) && node ***REMOVED******REMOVED******REMOVED*** node.parent.typeParameters[0] ? node.parent.constraint : void 0;
}
function isMemberName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */;
}
function isGetOrSetAccessorDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */;
}
function isPropertyAccessChain(node) {
  return isPropertyAccessExpression(node) && !!(node.flags & 64 /* OptionalChain */);
}
function isElementAccessChain(node) {
  return isElementAccessExpression(node) && !!(node.flags & 64 /* OptionalChain */);
}
function isCallChain(node) {
  return isCallExpression(node) && !!(node.flags & 64 /* OptionalChain */);
}
function isOptionalChain(node) {
  const kind ***REMOVED*** node.kind;
  return !!(node.flags & 64 /* OptionalChain */) && (kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 236 /* NonNullExpression */);
}
function isOptionalChainRoot(node) {
  return isOptionalChain(node) && !isNonNullExpression(node) && !!node.questionDotToken;
}
function isExpressionOfOptionalChainRoot(node) {
  return isOptionalChainRoot(node.parent) && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node;
}
function isOutermostOptionalChain(node) {
  return !isOptionalChain(node.parent) || isOptionalChainRoot(node.parent) || node !***REMOVED******REMOVED*** node.parent.expression;
}
function isNullishCoalesce(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */;
}
function isConstTypeReference(node) {
  return isTypeReferenceNode(node) && isIdentifier(node.typeName) && node.typeName.escapedText ***REMOVED******REMOVED******REMOVED*** "const" && !node.typeArguments;
}
function skipPartiallyEmittedExpressions(node) {
  return skipOuterExpressions(node, 8 /* PartiallyEmittedExpressions */);
}
function isNonNullChain(node) {
  return isNonNullExpression(node) && !!(node.flags & 64 /* OptionalChain */);
}
function isNamedExportBindings(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */ || node.kind ***REMOVED******REMOVED******REMOVED*** 280 /* NamedExports */;
}
function isJSDocPropertyLikeTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */ || node.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */;
}
function isNodeKind(kind) {
  return kind >***REMOVED*** 167 /* FirstNode */;
}
function isTokenKind(kind) {
  return kind >***REMOVED*** 0 /* FirstToken */ && kind <***REMOVED*** 166 /* LastToken */;
}
function isNodeArray(array) {
  return hasProperty(array, "pos") && hasProperty(array, "end");
}
function isLiteralKind(kind) {
  return 9 /* FirstLiteralToken */ <***REMOVED*** kind && kind <***REMOVED*** 15 /* LastLiteralToken */;
}
function isLiteralExpression(node) {
  return isLiteralKind(node.kind);
}
function isLiteralExpressionOfObject(node) {
  switch (node.kind) {
    case 211 /* ObjectLiteralExpression */:
    case 210 /* ArrayLiteralExpression */:
    case 14 /* RegularExpressionLiteral */:
    case 219 /* FunctionExpression */:
    case 232 /* ClassExpression */:
      return true;
  }
  return false;
}
function isTemplateLiteralKind(kind) {
  return 15 /* FirstTemplateToken */ <***REMOVED*** kind && kind <***REMOVED*** 18 /* LastTemplateToken */;
}
function isTemplateMiddleOrTemplateTail(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 17 /* TemplateMiddle */ || kind ***REMOVED******REMOVED******REMOVED*** 18 /* TemplateTail */;
}
function isImportOrExportSpecifier(node) {
  return isImportSpecifier(node) || isExportSpecifier(node);
}
function isTypeOnlyImportDeclaration(node) {
  switch (node.kind) {
    case 277 /* ImportSpecifier */:
      return node.isTypeOnly || node.parent.parent.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    case 275 /* NamespaceImport */:
      return node.parent.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    case 274 /* ImportClause */:
      return node.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    case 272 /* ImportEqualsDeclaration */:
      return node.isTypeOnly;
  }
  return false;
}
function isTypeOnlyExportDeclaration(node) {
  switch (node.kind) {
    case 282 /* ExportSpecifier */:
      return node.isTypeOnly || node.parent.parent.isTypeOnly;
    case 279 /* ExportDeclaration */:
      return node.isTypeOnly && !!node.moduleSpecifier && !node.exportClause;
    case 281 /* NamespaceExport */:
      return node.parent.isTypeOnly;
  }
  return false;
}
function isTypeOnlyImportOrExportDeclaration(node) {
  return isTypeOnlyImportDeclaration(node) || isTypeOnlyExportDeclaration(node);
}
function isPartOfTypeOnlyImportOrExportDeclaration(node) {
  return findAncestor(node, isTypeOnlyImportOrExportDeclaration) !***REMOVED******REMOVED*** void 0;
}
function isImportAttributeName(node) {
  return isStringLiteral(node) || isIdentifier(node);
}
function isGeneratedIdentifier(node) {
  var _a;
  return isIdentifier(node) && ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.autoGenerate) !***REMOVED******REMOVED*** void 0;
}
function isGeneratedPrivateIdentifier(node) {
  var _a;
  return isPrivateIdentifier(node) && ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.autoGenerate) !***REMOVED******REMOVED*** void 0;
}
function isFileLevelReservedGeneratedIdentifier(node) {
  const flags ***REMOVED*** node.emitNode.autoGenerate.flags;
  return !!(flags & 32 /* FileLevel */) && !!(flags & 16 /* Optimistic */) && !!(flags & 8 /* ReservedInNestedScopes */);
}
function isPrivateIdentifierClassElementDeclaration(node) {
  return (isPropertyDeclaration(node) || isMethodOrAccessor(node)) && isPrivateIdentifier(node.name);
}
function isPrivateIdentifierPropertyAccessExpression(node) {
  return isPropertyAccessExpression(node) && isPrivateIdentifier(node.name);
}
function isModifierKind(token) {
  switch (token) {
    case 128 /* AbstractKeyword */:
    case 129 /* AccessorKeyword */:
    case 134 /* AsyncKeyword */:
    case 87 /* ConstKeyword */:
    case 138 /* DeclareKeyword */:
    case 90 /* DefaultKeyword */:
    case 95 /* ExportKeyword */:
    case 103 /* InKeyword */:
    case 125 /* PublicKeyword */:
    case 123 /* PrivateKeyword */:
    case 124 /* ProtectedKeyword */:
    case 148 /* ReadonlyKeyword */:
    case 126 /* StaticKeyword */:
    case 147 /* OutKeyword */:
    case 164 /* OverrideKeyword */:
      return true;
  }
  return false;
}
function isParameterPropertyModifier(kind) {
  return !!(modifierToFlag(kind) & 31 /* ParameterPropertyModifier */);
}
function isClassMemberModifier(idToken) {
  return isParameterPropertyModifier(idToken) || idToken ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */ || idToken ***REMOVED******REMOVED******REMOVED*** 164 /* OverrideKeyword */ || idToken ***REMOVED******REMOVED******REMOVED*** 129 /* AccessorKeyword */;
}
function isModifier(node) {
  return isModifierKind(node.kind);
}
function isEntityName(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */;
}
function isPropertyName(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */ || kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */;
}
function isBindingName(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ || kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */;
}
function isFunctionLike(node) {
  return !!node && isFunctionLikeKind(node.kind);
}
function isFunctionLikeOrClassStaticBlockDeclaration(node) {
  return !!node && (isFunctionLikeKind(node.kind) || isClassStaticBlockDeclaration(node));
}
function isFunctionLikeDeclaration(node) {
  return node && isFunctionLikeDeclarationKind(node.kind);
}
function isBooleanLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */ || node.kind ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */;
}
function isFunctionLikeDeclarationKind(kind) {
  switch (kind) {
    case 263 /* FunctionDeclaration */:
    case 175 /* MethodDeclaration */:
    case 177 /* Constructor */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
      return true;
    default:
      return false;
  }
}
function isFunctionLikeKind(kind) {
  switch (kind) {
    case 174 /* MethodSignature */:
    case 180 /* CallSignature */:
    case 324 /* JSDocSignature */:
    case 181 /* ConstructSignature */:
    case 182 /* IndexSignature */:
    case 185 /* FunctionType */:
    case 318 /* JSDocFunctionType */:
    case 186 /* ConstructorType */:
      return true;
    default:
      return isFunctionLikeDeclarationKind(kind);
  }
}
function isFunctionOrModuleBlock(node) {
  return isSourceFile(node) || isModuleBlock(node) || isBlock(node) && isFunctionLike(node.parent);
}
function isClassElement(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 241 /* SemicolonClassElement */;
}
function isClassLike(node) {
  return node && (node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */);
}
function isAccessor(node) {
  return node && (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */);
}
function isAutoAccessorPropertyDeclaration(node) {
  return isPropertyDeclaration(node) && hasAccessorModifier(node);
}
function isClassInstanceProperty(node) {
  if (isInJSFile(node) && isExpandoPropertyDeclaration(node)) {
    return (!isBindableStaticAccessExpression(node) || !isPrototypeAccess(node.expression)) && !isBindableStaticNameExpression(
      node,
      /*excludeThisKeyword*/
      true
    );
  }
  return node.parent && isClassLike(node.parent) && isPropertyDeclaration(node) && !hasAccessorModifier(node);
}
function isMethodOrAccessor(node) {
  switch (node.kind) {
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
      return true;
    default:
      return false;
  }
}
function isModifierLike(node) {
  return isModifier(node) || isDecorator(node);
}
function isTypeElement(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 180 /* CallSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 355 /* NotEmittedTypeElement */;
}
function isObjectLiteralElementLike(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
}
function isTypeNode(node) {
  return isTypeNodeKind(node.kind);
}
function isFunctionOrConstructorTypeNode(node) {
  switch (node.kind) {
    case 185 /* FunctionType */:
    case 186 /* ConstructorType */:
      return true;
  }
  return false;
}
function isBindingPattern(node) {
  if (node) {
    const kind ***REMOVED*** node.kind;
    return kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */ || kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */;
  }
  return false;
}
function isAssignmentPattern(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */;
}
function isArrayBindingElement(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ || kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */;
}
function isDeclarationBindingElement(bindingElement) {
  switch (bindingElement.kind) {
    case 261 /* VariableDeclaration */:
    case 170 /* Parameter */:
    case 209 /* BindingElement */:
      return true;
  }
  return false;
}
function isBindingOrAssignmentElement(node) {
  return isVariableDeclaration(node) || isParameter(node) || isObjectBindingOrAssignmentElement(node) || isArrayBindingOrAssignmentElement(node);
}
function isBindingOrAssignmentPattern(node) {
  return isObjectBindingOrAssignmentPattern(node) || isArrayBindingOrAssignmentPattern(node);
}
function isObjectBindingOrAssignmentPattern(node) {
  switch (node.kind) {
    case 207 /* ObjectBindingPattern */:
    case 211 /* ObjectLiteralExpression */:
      return true;
  }
  return false;
}
function isObjectBindingOrAssignmentElement(node) {
  switch (node.kind) {
    case 209 /* BindingElement */:
    case 304 /* PropertyAssignment */:
    // AssignmentProperty
    case 305 /* ShorthandPropertyAssignment */:
    // AssignmentProperty
    case 306 /* SpreadAssignment */:
      return true;
  }
  return false;
}
function isArrayBindingOrAssignmentPattern(node) {
  switch (node.kind) {
    case 208 /* ArrayBindingPattern */:
    case 210 /* ArrayLiteralExpression */:
      return true;
  }
  return false;
}
function isArrayBindingOrAssignmentElement(node) {
  switch (node.kind) {
    case 209 /* BindingElement */:
    case 233 /* OmittedExpression */:
    // Elision
    case 231 /* SpreadElement */:
    // AssignmentRestElement
    case 210 /* ArrayLiteralExpression */:
    // ArrayAssignmentPattern
    case 211 /* ObjectLiteralExpression */:
    // ObjectAssignmentPattern
    case 80 /* Identifier */:
    // DestructuringAssignmentTarget
    case 212 /* PropertyAccessExpression */:
    // DestructuringAssignmentTarget
    case 213 /* ElementAccessExpression */:
      return true;
  }
  return isAssignmentExpression(
    node,
    /*excludeCompoundAssignment*/
    true
  );
}
function isPropertyAccessOrQualifiedNameOrImportTypeNode(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */;
}
function isPropertyAccessOrQualifiedName(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */;
}
function isCallLikeOrFunctionLikeExpression(node) {
  return isCallLikeExpression(node) || isFunctionExpressionOrArrowFunction(node);
}
function isCallLikeExpression(node) {
  switch (node.kind) {
    case 214 /* CallExpression */:
    case 215 /* NewExpression */:
    case 216 /* TaggedTemplateExpression */:
    case 171 /* Decorator */:
    case 287 /* JsxOpeningElement */:
    case 286 /* JsxSelfClosingElement */:
    case 290 /* JsxOpeningFragment */:
      return true;
    case 227 /* BinaryExpression */:
      return node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 104 /* InstanceOfKeyword */;
    default:
      return false;
  }
}
function isCallOrNewExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */;
}
function isTemplateLiteral(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 229 /* TemplateExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */;
}
function isLeftHandSideExpression(node) {
  return isLeftHandSideExpressionKind(skipPartiallyEmittedExpressions(node).kind);
}
function isLeftHandSideExpressionKind(kind) {
  switch (kind) {
    case 212 /* PropertyAccessExpression */:
    case 213 /* ElementAccessExpression */:
    case 215 /* NewExpression */:
    case 214 /* CallExpression */:
    case 285 /* JsxElement */:
    case 286 /* JsxSelfClosingElement */:
    case 289 /* JsxFragment */:
    case 216 /* TaggedTemplateExpression */:
    case 210 /* ArrayLiteralExpression */:
    case 218 /* ParenthesizedExpression */:
    case 211 /* ObjectLiteralExpression */:
    case 232 /* ClassExpression */:
    case 219 /* FunctionExpression */:
    case 80 /* Identifier */:
    case 81 /* PrivateIdentifier */:
    // technically this is only an Expression if it's in a `#field in expr` BinaryExpression
    case 14 /* RegularExpressionLiteral */:
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
    case 11 /* StringLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 229 /* TemplateExpression */:
    case 97 /* FalseKeyword */:
    case 106 /* NullKeyword */:
    case 110 /* ThisKeyword */:
    case 112 /* TrueKeyword */:
    case 108 /* SuperKeyword */:
    case 236 /* NonNullExpression */:
    case 234 /* ExpressionWithTypeArguments */:
    case 237 /* MetaProperty */:
    case 102 /* ImportKeyword */:
    // technically this is only an Expression if it's in a CallExpression
    case 283 /* MissingDeclaration */:
      return true;
    default:
      return false;
  }
}
function isUnaryExpression(node) {
  return isUnaryExpressionKind(skipPartiallyEmittedExpressions(node).kind);
}
function isUnaryExpressionKind(kind) {
  switch (kind) {
    case 225 /* PrefixUnaryExpression */:
    case 226 /* PostfixUnaryExpression */:
    case 221 /* DeleteExpression */:
    case 222 /* TypeOfExpression */:
    case 223 /* VoidExpression */:
    case 224 /* AwaitExpression */:
    case 217 /* TypeAssertionExpression */:
      return true;
    default:
      return isLeftHandSideExpressionKind(kind);
  }
}
function isLiteralTypeLiteral(node) {
  switch (node.kind) {
    case 106 /* NullKeyword */:
    case 112 /* TrueKeyword */:
    case 97 /* FalseKeyword */:
    case 225 /* PrefixUnaryExpression */:
      return true;
    default:
      return isLiteralExpression(node);
  }
}
function isExpression(node) {
  return isExpressionKind(skipPartiallyEmittedExpressions(node).kind);
}
function isExpressionKind(kind) {
  switch (kind) {
    case 228 /* ConditionalExpression */:
    case 230 /* YieldExpression */:
    case 220 /* ArrowFunction */:
    case 227 /* BinaryExpression */:
    case 231 /* SpreadElement */:
    case 235 /* AsExpression */:
    case 233 /* OmittedExpression */:
    case 357 /* CommaListExpression */:
    case 356 /* PartiallyEmittedExpression */:
    case 239 /* SatisfiesExpression */:
      return true;
    default:
      return isUnaryExpressionKind(kind);
  }
}
function isAssertionExpression(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 235 /* AsExpression */;
}
function isIterationStatement(node, lookInLabeledStatements) {
  switch (node.kind) {
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 247 /* DoStatement */:
    case 248 /* WhileStatement */:
      return true;
    case 257 /* LabeledStatement */:
      return lookInLabeledStatements && isIterationStatement(node.statement, lookInLabeledStatements);
  }
  return false;
}
function isScopeMarker(node) {
  return isExportAssignment(node) || isExportDeclaration(node);
}
function hasScopeMarker(statements) {
  return some(statements, isScopeMarker);
}
function needsScopeMarker(result) {
  return !isAnyImportOrReExport(result) && !isExportAssignment(result) && !hasSyntacticModifier(result, 32 /* Export */) && !isAmbientModule(result);
}
function isExternalModuleIndicator(result) {
  return isAnyImportOrReExport(result) || isExportAssignment(result) || hasSyntacticModifier(result, 32 /* Export */);
}
function isForInOrOfStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ || node.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */;
}
function isConciseBody(node) {
  return isBlock(node) || isExpression(node);
}
function isForInitializer(node) {
  return isVariableDeclarationList(node) || isExpression(node);
}
function isModuleBody(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */;
}
function isNamedImportBindings(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 276 /* NamedImports */ || kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */;
}
function isModuleOrEnumDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */;
}
function canHaveSymbol(node) {
  switch (node.kind) {
    case 220 /* ArrowFunction */:
    case 227 /* BinaryExpression */:
    case 209 /* BindingElement */:
    case 214 /* CallExpression */:
    case 180 /* CallSignature */:
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
    case 176 /* ClassStaticBlockDeclaration */:
    case 177 /* Constructor */:
    case 186 /* ConstructorType */:
    case 181 /* ConstructSignature */:
    case 213 /* ElementAccessExpression */:
    case 267 /* EnumDeclaration */:
    case 307 /* EnumMember */:
    case 278 /* ExportAssignment */:
    case 279 /* ExportDeclaration */:
    case 282 /* ExportSpecifier */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 185 /* FunctionType */:
    case 178 /* GetAccessor */:
    case 80 /* Identifier */:
    case 274 /* ImportClause */:
    case 272 /* ImportEqualsDeclaration */:
    case 277 /* ImportSpecifier */:
    case 182 /* IndexSignature */:
    case 265 /* InterfaceDeclaration */:
    case 339 /* JSDocCallbackTag */:
    case 341 /* JSDocEnumTag */:
    case 318 /* JSDocFunctionType */:
    case 342 /* JSDocParameterTag */:
    case 349 /* JSDocPropertyTag */:
    case 324 /* JSDocSignature */:
    case 347 /* JSDocTypedefTag */:
    case 323 /* JSDocTypeLiteral */:
    case 292 /* JsxAttribute */:
    case 293 /* JsxAttributes */:
    case 294 /* JsxSpreadAttribute */:
    case 201 /* MappedType */:
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 268 /* ModuleDeclaration */:
    case 203 /* NamedTupleMember */:
    case 281 /* NamespaceExport */:
    case 271 /* NamespaceExportDeclaration */:
    case 275 /* NamespaceImport */:
    case 215 /* NewExpression */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 9 /* NumericLiteral */:
    case 211 /* ObjectLiteralExpression */:
    case 170 /* Parameter */:
    case 212 /* PropertyAccessExpression */:
    case 304 /* PropertyAssignment */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 179 /* SetAccessor */:
    case 305 /* ShorthandPropertyAssignment */:
    case 308 /* SourceFile */:
    case 306 /* SpreadAssignment */:
    case 11 /* StringLiteral */:
    case 266 /* TypeAliasDeclaration */:
    case 188 /* TypeLiteral */:
    case 169 /* TypeParameter */:
    case 261 /* VariableDeclaration */:
      return true;
    default:
      return false;
  }
}
function canHaveLocals(node) {
  switch (node.kind) {
    case 220 /* ArrowFunction */:
    case 242 /* Block */:
    case 180 /* CallSignature */:
    case 270 /* CaseBlock */:
    case 300 /* CatchClause */:
    case 176 /* ClassStaticBlockDeclaration */:
    case 195 /* ConditionalType */:
    case 177 /* Constructor */:
    case 186 /* ConstructorType */:
    case 181 /* ConstructSignature */:
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 185 /* FunctionType */:
    case 178 /* GetAccessor */:
    case 182 /* IndexSignature */:
    case 339 /* JSDocCallbackTag */:
    case 341 /* JSDocEnumTag */:
    case 318 /* JSDocFunctionType */:
    case 324 /* JSDocSignature */:
    case 347 /* JSDocTypedefTag */:
    case 201 /* MappedType */:
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 268 /* ModuleDeclaration */:
    case 179 /* SetAccessor */:
    case 308 /* SourceFile */:
    case 266 /* TypeAliasDeclaration */:
      return true;
    default:
      return false;
  }
}
function isDeclarationKind(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ || kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 307 /* EnumMember */ || kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ || kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ || kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 292 /* JsxAttribute */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */ || kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */ || kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ || kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 347 /* JSDocTypedefTag */ || kind ***REMOVED******REMOVED******REMOVED*** 339 /* JSDocCallbackTag */ || kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */ || kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */;
}
function isDeclarationStatementKind(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 283 /* MissingDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */;
}
function isStatementKindButNotDeclarationKind(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 252 /* ContinueStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 260 /* DebuggerStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 247 /* DoStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 243 /* EmptyStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 249 /* ForStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 246 /* IfStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 257 /* LabeledStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 256 /* SwitchStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 258 /* ThrowStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 259 /* TryStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 248 /* WhileStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 255 /* WithStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 354 /* NotEmittedStatement */;
}
function isDeclaration(node) {
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */) {
    return node.parent && node.parent.kind !***REMOVED******REMOVED*** 346 /* JSDocTemplateTag */ || isInJSFile(node);
  }
  return isDeclarationKind(node.kind);
}
function isDeclarationStatement(node) {
  return isDeclarationStatementKind(node.kind);
}
function isStatementButNotDeclaration(node) {
  return isStatementKindButNotDeclarationKind(node.kind);
}
function isStatement(node) {
  const kind ***REMOVED*** node.kind;
  return isStatementKindButNotDeclarationKind(kind) || isDeclarationStatementKind(kind) || isBlockStatement(node);
}
function isBlockStatement(node) {
  if (node.kind !***REMOVED******REMOVED*** 242 /* Block */) return false;
  if (node.parent !***REMOVED******REMOVED*** void 0) {
    if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 259 /* TryStatement */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */) {
      return false;
    }
  }
  return !isFunctionBlock(node);
}
function isStatementOrBlock(node) {
  const kind ***REMOVED*** node.kind;
  return isStatementKindButNotDeclarationKind(kind) || isDeclarationStatementKind(kind) || kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */;
}
function isModuleReference(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */ || kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */;
}
function isJsxTagNameExpression(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 296 /* JsxNamespacedName */;
}
function isJsxChild(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 285 /* JsxElement */ || kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */ || kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */ || kind ***REMOVED******REMOVED******REMOVED*** 289 /* JsxFragment */;
}
function isJsxAttributeLike(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 292 /* JsxAttribute */ || kind ***REMOVED******REMOVED******REMOVED*** 294 /* JsxSpreadAttribute */;
}
function isStringLiteralOrJsxExpression(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */;
}
function isJsxOpeningLikeElement(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 287 /* JsxOpeningElement */ || kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */;
}
function isJsxCallLike(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 287 /* JsxOpeningElement */ || kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */ || kind ***REMOVED******REMOVED******REMOVED*** 290 /* JsxOpeningFragment */;
}
function isCaseOrDefaultClause(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */ || kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */;
}
function isJSDocNode(node) {
  return node.kind >***REMOVED*** 310 /* FirstJSDocNode */ && node.kind <***REMOVED*** 352 /* LastJSDocNode */;
}
function isJSDocTag(node) {
  return node.kind >***REMOVED*** 328 /* FirstJSDocTagNode */ && node.kind <***REMOVED*** 352 /* LastJSDocTagNode */;
}
function isSetAccessor(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
}
function isGetAccessor(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */;
}
function hasJSDocNodes(node) {
  if (!canHaveJSDoc(node)) return false;
  const { jsDoc } ***REMOVED*** node;
  return !!jsDoc && jsDoc.length > 0;
}
function hasType(node) {
  return !!node.type;
}
function hasInitializer(node) {
  return !!node.initializer;
}
function hasOnlyExpressionInitializer(node) {
  switch (node.kind) {
    case 261 /* VariableDeclaration */:
    case 170 /* Parameter */:
    case 209 /* BindingElement */:
    case 173 /* PropertyDeclaration */:
    case 304 /* PropertyAssignment */:
    case 307 /* EnumMember */:
      return true;
    default:
      return false;
  }
}
function isTypeReferenceType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ || node.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */;
}
var MAX_SMI_X86 ***REMOVED*** 1073741823;
function guessIndentation(lines) {
  let indentation ***REMOVED*** MAX_SMI_X86;
  for (const line of lines) {
    if (!line.length) {
      continue;
    }
    let i ***REMOVED*** 0;
    for (; i < line.length && i < indentation; i++) {
      if (!isWhiteSpaceLike(line.charCodeAt(i))) {
        break;
      }
    }
    if (i < indentation) {
      indentation ***REMOVED*** i;
    }
    if (indentation ***REMOVED******REMOVED******REMOVED*** 0) {
      return 0;
    }
  }
  return indentation ***REMOVED******REMOVED******REMOVED*** MAX_SMI_X86 ? void 0 : indentation;
}
function isStringLiteralLike(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || node.kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */;
}
function isJSDocLinkLike(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 325 /* JSDocLink */ || node.kind ***REMOVED******REMOVED******REMOVED*** 326 /* JSDocLinkCode */ || node.kind ***REMOVED******REMOVED******REMOVED*** 327 /* JSDocLinkPlain */;
}
function hasRestParameter(s) {
  const last2 ***REMOVED*** lastOrUndefined(s.parameters);
  return !!last2 && isRestParameter(last2);
}
function isRestParameter(node) {
  const type ***REMOVED*** isJSDocParameterTag(node) ? node.typeExpression && node.typeExpression.type : node.type;
  return node.dotDotDotToken !***REMOVED******REMOVED*** void 0 || !!type && type.kind ***REMOVED******REMOVED******REMOVED*** 319 /* JSDocVariadicType */;
}
function hasInternalAnnotation(range, sourceFile) {
  const comment ***REMOVED*** sourceFile.text.substring(range.pos, range.end);
  return comment.includes("@internal");
}
function isInternalDeclaration(node, sourceFile) {
  sourceFile ?? (sourceFile ***REMOVED*** getSourceFileOfNode(node));
  const parseTreeNode ***REMOVED*** getParseTreeNode(node);
  if (parseTreeNode && parseTreeNode.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
    const paramIdx ***REMOVED*** parseTreeNode.parent.parameters.indexOf(parseTreeNode);
    const previousSibling ***REMOVED*** paramIdx > 0 ? parseTreeNode.parent.parameters[paramIdx - 1] : void 0;
    const text ***REMOVED*** sourceFile.text;
    const commentRanges ***REMOVED*** previousSibling ? concatenate(
      // to handle
      // ... parameters, /** @internal */
      // public param: string
      getTrailingCommentRanges(text, skipTrivia(
        text,
        previousSibling.end + 1,
        /*stopAfterLineBreak*/
        false,
        /*stopAtComments*/
        true
      )),
      getLeadingCommentRanges(text, node.pos)
    ) : getTrailingCommentRanges(text, skipTrivia(
      text,
      node.pos,
      /*stopAfterLineBreak*/
      false,
      /*stopAtComments*/
      true
    ));
    return some(commentRanges) && hasInternalAnnotation(last(commentRanges), sourceFile);
  }
  const leadingCommentRanges ***REMOVED*** parseTreeNode && getLeadingCommentRangesOfNode(parseTreeNode, sourceFile);
  return !!forEach(leadingCommentRanges, (range) ***REMOVED***> {
    return hasInternalAnnotation(range, sourceFile);
  });
}

// src/compiler/utilities.ts
var resolvingEmptyArray ***REMOVED*** [];
var externalHelpersModuleNameText ***REMOVED*** "tslib";
var defaultMaximumTruncationLength ***REMOVED*** 160;
var noTruncationMaximumTruncationLength ***REMOVED*** 1e6;
function getDeclarationOfKind(symbol, kind) {
  const declarations ***REMOVED*** symbol.declarations;
  if (declarations) {
    for (const declaration of declarations) {
      if (declaration.kind ***REMOVED******REMOVED******REMOVED*** kind) {
        return declaration;
      }
    }
  }
  return void 0;
}
function getDeclarationsOfKind(symbol, kind) {
  return filter(symbol.declarations || emptyArray, (d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** kind);
}
function createSymbolTable(symbols) {
  const result ***REMOVED*** /* @__PURE__ */ new Map();
  if (symbols) {
    for (const symbol of symbols) {
      result.set(symbol.escapedName, symbol);
    }
  }
  return result;
}
function isTransientSymbol(symbol) {
  return (symbol.flags & 33554432 /* Transient */) !***REMOVED******REMOVED*** 0;
}
function isExternalModuleSymbol(moduleSymbol) {
  return !!(moduleSymbol.flags & 1536 /* Module */) && moduleSymbol.escapedName.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 34 /* doubleQuote */;
}
var stringWriter ***REMOVED*** createSingleLineStringWriter();
function createSingleLineStringWriter() {
  var str ***REMOVED*** "";
  const writeText ***REMOVED*** (text) ***REMOVED***> str +***REMOVED*** text;
  return {
    getText: () ***REMOVED***> str,
    write: writeText,
    rawWrite: writeText,
    writeKeyword: writeText,
    writeOperator: writeText,
    writePunctuation: writeText,
    writeSpace: writeText,
    writeStringLiteral: writeText,
    writeLiteral: writeText,
    writeParameter: writeText,
    writeProperty: writeText,
    writeSymbol: (s, _) ***REMOVED***> writeText(s),
    writeTrailingSemicolon: writeText,
    writeComment: writeText,
    getTextPos: () ***REMOVED***> str.length,
    getLine: () ***REMOVED***> 0,
    getColumn: () ***REMOVED***> 0,
    getIndent: () ***REMOVED***> 0,
    isAtStartOfLine: () ***REMOVED***> false,
    hasTrailingComment: () ***REMOVED***> false,
    hasTrailingWhitespace: () ***REMOVED***> !!str.length && isWhiteSpaceLike(str.charCodeAt(str.length - 1)),
    // Completely ignore indentation for string writers.  And map newlines to
    // a single space.
    writeLine: () ***REMOVED***> str +***REMOVED*** " ",
    increaseIndent: noop,
    decreaseIndent: noop,
    clear: () ***REMOVED***> str ***REMOVED*** ""
  };
}
function changesAffectModuleResolution(oldOptions, newOptions) {
  return oldOptions.configFilePath !***REMOVED******REMOVED*** newOptions.configFilePath || optionsHaveModuleResolutionChanges(oldOptions, newOptions);
}
function optionsHaveModuleResolutionChanges(oldOptions, newOptions) {
  return optionsHaveChanges(oldOptions, newOptions, moduleResolutionOptionDeclarations);
}
function changesAffectingProgramStructure(oldOptions, newOptions) {
  return optionsHaveChanges(oldOptions, newOptions, optionsAffectingProgramStructure);
}
function optionsHaveChanges(oldOptions, newOptions, optionDeclarations2) {
  return oldOptions !***REMOVED******REMOVED*** newOptions && optionDeclarations2.some((o) ***REMOVED***> !isJsonEqual(getCompilerOptionValue(oldOptions, o), getCompilerOptionValue(newOptions, o)));
}
function forEachEntry(map2, callback) {
  const iterator ***REMOVED*** map2.entries();
  for (const [key, value] of iterator) {
    const result ***REMOVED*** callback(value, key);
    if (result) {
      return result;
    }
  }
  return void 0;
}
function forEachKey(map2, callback) {
  const iterator ***REMOVED*** map2.keys();
  for (const key of iterator) {
    const result ***REMOVED*** callback(key);
    if (result) {
      return result;
    }
  }
  return void 0;
}
function copyEntries(source, target) {
  source.forEach((value, key) ***REMOVED***> {
    target.set(key, value);
  });
}
function usingSingleLineStringWriter(action) {
  const oldString ***REMOVED*** stringWriter.getText();
  try {
    action(stringWriter);
    return stringWriter.getText();
  } finally {
    stringWriter.clear();
    stringWriter.writeKeyword(oldString);
  }
}
function getFullWidth(node) {
  return node.end - node.pos;
}
function projectReferenceIsEqualTo(oldRef, newRef) {
  return oldRef.path ***REMOVED******REMOVED******REMOVED*** newRef.path && !oldRef.prepend ***REMOVED******REMOVED******REMOVED*** !newRef.prepend && !oldRef.circular ***REMOVED******REMOVED******REMOVED*** !newRef.circular;
}
function moduleResolutionIsEqualTo(oldResolution, newResolution) {
  return oldResolution ***REMOVED******REMOVED******REMOVED*** newResolution || oldResolution.resolvedModule ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedModule || !!oldResolution.resolvedModule && !!newResolution.resolvedModule && oldResolution.resolvedModule.isExternalLibraryImport ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedModule.isExternalLibraryImport && oldResolution.resolvedModule.extension ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedModule.extension && oldResolution.resolvedModule.resolvedFileName ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedModule.resolvedFileName && oldResolution.resolvedModule.originalPath ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedModule.originalPath && packageIdIsEqual(oldResolution.resolvedModule.packageId, newResolution.resolvedModule.packageId) && oldResolution.alternateResult ***REMOVED******REMOVED******REMOVED*** newResolution.alternateResult;
}
function getResolvedModuleFromResolution(resolution) {
  return resolution.resolvedModule;
}
function getResolvedTypeReferenceDirectiveFromResolution(resolution) {
  return resolution.resolvedTypeReferenceDirective;
}
function createModuleNotFoundChain(sourceFile, host, moduleReference, mode, packageName) {
  var _a;
  const alternateResult ***REMOVED*** (_a ***REMOVED*** host.getResolvedModule(sourceFile, moduleReference, mode)) ***REMOVED******REMOVED*** null ? void 0 : _a.alternateResult;
  const alternateResultMessage ***REMOVED*** alternateResult && (getEmitModuleResolutionKind(host.getCompilerOptions()) ***REMOVED******REMOVED******REMOVED*** 2 /* Node10 */ ? [Diagnostics.There_are_types_at_0_but_this_result_could_not_be_resolved_under_your_current_moduleResolution_setting_Consider_updating_to_node16_nodenext_or_bundler, [alternateResult]] : [
    Diagnostics.There_are_types_at_0_but_this_result_could_not_be_resolved_when_respecting_package_json_exports_The_1_library_may_need_to_update_its_package_json_or_typings,
    [alternateResult, alternateResult.includes(nodeModulesPathPart + "@types/") ? `@types/${mangleScopedPackageName(packageName)}` : packageName]
  ]);
  const result ***REMOVED*** alternateResultMessage ? chainDiagnosticMessages(
    /*details*/
    void 0,
    alternateResultMessage[0],
    ...alternateResultMessage[1]
  ) : host.typesPackageExists(packageName) ? chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.If_the_0_package_actually_exposes_this_module_consider_sending_a_pull_request_to_amend_https_Colon_Slash_Slashgithub_com_SlashDefinitelyTyped_SlashDefinitelyTyped_Slashtree_Slashmaster_Slashtypes_Slash_1,
    packageName,
    mangleScopedPackageName(packageName)
  ) : host.packageBundlesTypes(packageName) ? chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.If_the_0_package_actually_exposes_this_module_try_adding_a_new_declaration_d_ts_file_containing_declare_module_1,
    packageName,
    moduleReference
  ) : chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.Try_npm_i_save_dev_types_Slash_1_if_it_exists_or_add_a_new_declaration_d_ts_file_containing_declare_module_0,
    moduleReference,
    mangleScopedPackageName(packageName)
  );
  if (result) result.repopulateInfo ***REMOVED*** () ***REMOVED***> ({ moduleReference, mode, packageName: packageName ***REMOVED******REMOVED******REMOVED*** moduleReference ? void 0 : packageName });
  return result;
}
function createModeMismatchDetails(currentSourceFile) {
  const ext ***REMOVED*** tryGetExtensionFromPath2(currentSourceFile.fileName);
  const scope ***REMOVED*** currentSourceFile.packageJsonScope;
  const targetExt ***REMOVED*** ext ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ ? ".mts" /* Mts */ : ext ***REMOVED******REMOVED******REMOVED*** ".js" /* Js */ ? ".mjs" /* Mjs */ : void 0;
  const result ***REMOVED*** scope && !scope.contents.packageJsonContent.type ? targetExt ? chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_add_the_field_type_Colon_module_to_1,
    targetExt,
    combinePaths(scope.packageDirectory, "package.json")
  ) : chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.To_convert_this_file_to_an_ECMAScript_module_add_the_field_type_Colon_module_to_0,
    combinePaths(scope.packageDirectory, "package.json")
  ) : targetExt ? chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.To_convert_this_file_to_an_ECMAScript_module_change_its_file_extension_to_0_or_create_a_local_package_json_file_with_type_Colon_module,
    targetExt
  ) : chainDiagnosticMessages(
    /*details*/
    void 0,
    Diagnostics.To_convert_this_file_to_an_ECMAScript_module_create_a_local_package_json_file_with_type_Colon_module
  );
  result.repopulateInfo ***REMOVED*** () ***REMOVED***> true;
  return result;
}
function packageIdIsEqual(a, b) {
  return a ***REMOVED******REMOVED******REMOVED*** b || !!a && !!b && a.name ***REMOVED******REMOVED******REMOVED*** b.name && a.subModuleName ***REMOVED******REMOVED******REMOVED*** b.subModuleName && a.version ***REMOVED******REMOVED******REMOVED*** b.version && a.peerDependencies ***REMOVED******REMOVED******REMOVED*** b.peerDependencies;
}
function packageIdToPackageName({ name, subModuleName }) {
  return subModuleName ? `${name}/${subModuleName}` : name;
}
function packageIdToString(packageId) {
  return `${packageIdToPackageName(packageId)}@${packageId.version}${packageId.peerDependencies ?? ""}`;
}
function typeDirectiveIsEqualTo(oldResolution, newResolution) {
  return oldResolution ***REMOVED******REMOVED******REMOVED*** newResolution || oldResolution.resolvedTypeReferenceDirective ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedTypeReferenceDirective || !!oldResolution.resolvedTypeReferenceDirective && !!newResolution.resolvedTypeReferenceDirective && oldResolution.resolvedTypeReferenceDirective.resolvedFileName ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedTypeReferenceDirective.resolvedFileName && !!oldResolution.resolvedTypeReferenceDirective.primary ***REMOVED******REMOVED******REMOVED*** !!newResolution.resolvedTypeReferenceDirective.primary && oldResolution.resolvedTypeReferenceDirective.originalPath ***REMOVED******REMOVED******REMOVED*** newResolution.resolvedTypeReferenceDirective.originalPath;
}
function hasChangesInResolutions(names, newResolutions, getOldResolution, comparer) {
  Debug.assert(names.length ***REMOVED******REMOVED******REMOVED*** newResolutions.length);
  for (let i ***REMOVED*** 0; i < names.length; i++) {
    const newResolution ***REMOVED*** newResolutions[i];
    const entry ***REMOVED*** names[i];
    const oldResolution ***REMOVED*** getOldResolution(entry);
    const changed ***REMOVED*** oldResolution ? !newResolution || !comparer(oldResolution, newResolution) : newResolution;
    if (changed) {
      return true;
    }
  }
  return false;
}
function containsParseError(node) {
  aggregateChildData(node);
  return (node.flags & 1048576 /* ThisNodeOrAnySubNodesHasError */) !***REMOVED******REMOVED*** 0;
}
function aggregateChildData(node) {
  if (!(node.flags & 2097152 /* HasAggregatedChildData */)) {
    const thisNodeOrAnySubNodesHasError ***REMOVED*** (node.flags & 262144 /* ThisNodeHasError */) !***REMOVED******REMOVED*** 0 || forEachChild(node, containsParseError);
    if (thisNodeOrAnySubNodesHasError) {
      node.flags |***REMOVED*** 1048576 /* ThisNodeOrAnySubNodesHasError */;
    }
    node.flags |***REMOVED*** 2097152 /* HasAggregatedChildData */;
  }
}
function getSourceFileOfNode(node) {
  while (node && node.kind !***REMOVED******REMOVED*** 308 /* SourceFile */) {
    node ***REMOVED*** node.parent;
  }
  return node;
}
function getSourceFileOfModule(module2) {
  return getSourceFileOfNode(module2.valueDeclaration || getNonAugmentationDeclaration(module2));
}
function isPlainJsFile(file, checkJs) {
  return !!file && (file.scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || file.scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */) && !file.checkJsDirective && checkJs ***REMOVED******REMOVED******REMOVED*** void 0;
}
function isStatementWithLocals(node) {
  switch (node.kind) {
    case 242 /* Block */:
    case 270 /* CaseBlock */:
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
      return true;
  }
  return false;
}
function getEndLinePosition(line, sourceFile) {
  Debug.assert(line >***REMOVED*** 0);
  const lineStarts ***REMOVED*** getLineStarts(sourceFile);
  const lineIndex ***REMOVED*** line;
  const sourceText ***REMOVED*** sourceFile.text;
  if (lineIndex + 1 ***REMOVED******REMOVED******REMOVED*** lineStarts.length) {
    return sourceText.length - 1;
  } else {
    const start ***REMOVED*** lineStarts[lineIndex];
    let pos ***REMOVED*** lineStarts[lineIndex + 1] - 1;
    Debug.assert(isLineBreak(sourceText.charCodeAt(pos)));
    while (start <***REMOVED*** pos && isLineBreak(sourceText.charCodeAt(pos))) {
      pos--;
    }
    return pos;
  }
}
function isFileLevelUniqueName(sourceFile, name, hasGlobalName) {
  return !(hasGlobalName && hasGlobalName(name)) && !sourceFile.identifiers.has(name);
}
function nodeIsMissing(node) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
    return true;
  }
  return node.pos ***REMOVED******REMOVED******REMOVED*** node.end && node.pos >***REMOVED*** 0 && node.kind !***REMOVED******REMOVED*** 1 /* EndOfFileToken */;
}
function nodeIsPresent(node) {
  return !nodeIsMissing(node);
}
function insertStatementsAfterPrologue(to, from, isPrologueDirective2) {
  if (from ***REMOVED******REMOVED******REMOVED*** void 0 || from.length ***REMOVED******REMOVED******REMOVED*** 0) return to;
  let statementIndex ***REMOVED*** 0;
  for (; statementIndex < to.length; ++statementIndex) {
    if (!isPrologueDirective2(to[statementIndex])) {
      break;
    }
  }
  to.splice(statementIndex, 0, ...from);
  return to;
}
function insertStatementAfterPrologue(to, statement, isPrologueDirective2) {
  if (statement ***REMOVED******REMOVED******REMOVED*** void 0) return to;
  let statementIndex ***REMOVED*** 0;
  for (; statementIndex < to.length; ++statementIndex) {
    if (!isPrologueDirective2(to[statementIndex])) {
      break;
    }
  }
  to.splice(statementIndex, 0, statement);
  return to;
}
function isAnyPrologueDirective(node) {
  return isPrologueDirective(node) || !!(getEmitFlags(node) & 2097152 /* CustomPrologue */);
}
function insertStatementsAfterStandardPrologue(to, from) {
  return insertStatementsAfterPrologue(to, from, isPrologueDirective);
}
function insertStatementsAfterCustomPrologue(to, from) {
  return insertStatementsAfterPrologue(to, from, isAnyPrologueDirective);
}
function insertStatementAfterCustomPrologue(to, statement) {
  return insertStatementAfterPrologue(to, statement, isAnyPrologueDirective);
}
function isRecognizedTripleSlashComment(text, commentPos, commentEnd) {
  if (text.charCodeAt(commentPos + 1) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ && commentPos + 2 < commentEnd && text.charCodeAt(commentPos + 2) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) {
    const textSubStr ***REMOVED*** text.substring(commentPos, commentEnd);
    return fullTripleSlashReferencePathRegEx.test(textSubStr) || fullTripleSlashAMDReferencePathRegEx.test(textSubStr) || fullTripleSlashAMDModuleRegEx.test(textSubStr) || fullTripleSlashReferenceTypeReferenceDirectiveRegEx.test(textSubStr) || fullTripleSlashLibReferenceRegEx.test(textSubStr) || defaultLibReferenceRegEx.test(textSubStr) ? true : false;
  }
  return false;
}
function isPinnedComment(text, start) {
  return text.charCodeAt(start + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(start + 2) ***REMOVED******REMOVED******REMOVED*** 33 /* exclamation */;
}
function createCommentDirectivesMap(sourceFile, commentDirectives) {
  const directivesByLine ***REMOVED*** new Map(
    commentDirectives.map((commentDirective) ***REMOVED***> [
      `${getLineAndCharacterOfPosition(sourceFile, commentDirective.range.end).line}`,
      commentDirective
    ])
  );
  const usedLines ***REMOVED*** /* @__PURE__ */ new Map();
  return { getUnusedExpectations, markUsed };
  function getUnusedExpectations() {
    return arrayFrom(directivesByLine.entries()).filter(([line, directive]) ***REMOVED***> directive.type ***REMOVED******REMOVED******REMOVED*** 0 /* ExpectError */ && !usedLines.get(line)).map(([_, directive]) ***REMOVED***> directive);
  }
  function markUsed(line) {
    if (!directivesByLine.has(`${line}`)) {
      return false;
    }
    usedLines.set(`${line}`, true);
    return true;
  }
}
function getTokenPosOfNode(node, sourceFile, includeJsDoc) {
  if (nodeIsMissing(node)) {
    return node.pos;
  }
  if (isJSDocNode(node) || node.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */) {
    return skipTrivia(
      (sourceFile ?? getSourceFileOfNode(node)).text,
      node.pos,
      /*stopAfterLineBreak*/
      false,
      /*stopAtComments*/
      true
    );
  }
  if (includeJsDoc && hasJSDocNodes(node)) {
    return getTokenPosOfNode(node.jsDoc[0], sourceFile);
  }
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 353 /* SyntaxList */) {
    sourceFile ?? (sourceFile ***REMOVED*** getSourceFileOfNode(node));
    const first2 ***REMOVED*** firstOrUndefined(getNodeChildren(node, sourceFile));
    if (first2) {
      return getTokenPosOfNode(first2, sourceFile, includeJsDoc);
    }
  }
  return skipTrivia(
    (sourceFile ?? getSourceFileOfNode(node)).text,
    node.pos,
    /*stopAfterLineBreak*/
    false,
    /*stopAtComments*/
    false,
    isInJSDoc(node)
  );
}
function getNonModifierTokenPosOfNode(node, sourceFile) {
  const lastModifier ***REMOVED*** !nodeIsMissing(node) && canHaveModifiers(node) && node.modifiers ? last(node.modifiers) : void 0;
  if (!lastModifier) {
    return getTokenPosOfNode(node, sourceFile);
  }
  return skipTrivia((sourceFile || getSourceFileOfNode(node)).text, lastModifier.end);
}
function getSourceTextOfNodeFromSourceFile(sourceFile, node, includeTrivia ***REMOVED*** false) {
  return getTextOfNodeFromSourceText(sourceFile.text, node, includeTrivia);
}
function isJSDocTypeExpressionOrChild(node) {
  return !!findAncestor(node, isJSDocTypeExpression);
}
function isExportNamespaceAsDefaultDeclaration(node) {
  return !!(isExportDeclaration(node) && node.exportClause && isNamespaceExport(node.exportClause) && moduleExportNameIsDefault(node.exportClause.name));
}
function moduleExportNameTextUnescaped(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? node.text : unescapeLeadingUnderscores(node.escapedText);
}
function moduleExportNameTextEscaped(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? escapeLeadingUnderscores(node.text) : node.escapedText;
}
function moduleExportNameIsDefault(node) {
  return (node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? node.text : node.escapedText) ***REMOVED******REMOVED******REMOVED*** "default" /* Default */;
}
function getTextOfNodeFromSourceText(sourceText, node, includeTrivia ***REMOVED*** false) {
  if (nodeIsMissing(node)) {
    return "";
  }
  let text ***REMOVED*** sourceText.substring(includeTrivia ? node.pos : skipTrivia(sourceText, node.pos), node.end);
  if (isJSDocTypeExpressionOrChild(node)) {
    text ***REMOVED*** text.split(/\r\n|\n|\r/).map((line) ***REMOVED***> line.replace(/^\s*\*/, "").trimStart()).join("\n");
  }
  return text;
}
function getTextOfNode(node, includeTrivia ***REMOVED*** false) {
  return getSourceTextOfNodeFromSourceFile(getSourceFileOfNode(node), node, includeTrivia);
}
function getPos(range) {
  return range.pos;
}
function indexOfNode(nodeArray, node) {
  return binarySearch(nodeArray, node, getPos, compareValues);
}
function getEmitFlags(node) {
  const emitNode ***REMOVED*** node.emitNode;
  return emitNode && emitNode.flags || 0;
}
function getInternalEmitFlags(node) {
  const emitNode ***REMOVED*** node.emitNode;
  return emitNode && emitNode.internalFlags || 0;
}
var getScriptTargetFeatures ***REMOVED*** /* @__PURE__ */ memoize(
  () ***REMOVED***> new Map(Object.entries({
    Array: new Map(Object.entries({
      es2015: [
        "find",
        "findIndex",
        "fill",
        "copyWithin",
        "entries",
        "keys",
        "values"
      ],
      es2016: [
        "includes"
      ],
      es2019: [
        "flat",
        "flatMap"
      ],
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Iterator: new Map(Object.entries({
      es2015: emptyArray
    })),
    AsyncIterator: new Map(Object.entries({
      es2015: emptyArray
    })),
    ArrayBuffer: new Map(Object.entries({
      es2024: [
        "maxByteLength",
        "resizable",
        "resize",
        "detached",
        "transfer",
        "transferToFixedLength"
      ]
    })),
    Atomics: new Map(Object.entries({
      es2017: [
        "add",
        "and",
        "compareExchange",
        "exchange",
        "isLockFree",
        "load",
        "or",
        "store",
        "sub",
        "wait",
        "notify",
        "xor"
      ],
      es2024: [
        "waitAsync"
      ],
      esnext: [
        "pause"
      ]
    })),
    SharedArrayBuffer: new Map(Object.entries({
      es2017: [
        "byteLength",
        "slice"
      ],
      es2024: [
        "growable",
        "maxByteLength",
        "grow"
      ]
    })),
    AsyncIterable: new Map(Object.entries({
      es2018: emptyArray
    })),
    AsyncIterableIterator: new Map(Object.entries({
      es2018: emptyArray
    })),
    AsyncGenerator: new Map(Object.entries({
      es2018: emptyArray
    })),
    AsyncGeneratorFunction: new Map(Object.entries({
      es2018: emptyArray
    })),
    RegExp: new Map(Object.entries({
      es2015: [
        "flags",
        "sticky",
        "unicode"
      ],
      es2018: [
        "dotAll"
      ],
      es2024: [
        "unicodeSets"
      ]
    })),
    Reflect: new Map(Object.entries({
      es2015: [
        "apply",
        "construct",
        "defineProperty",
        "deleteProperty",
        "get",
        "getOwnPropertyDescriptor",
        "getPrototypeOf",
        "has",
        "isExtensible",
        "ownKeys",
        "preventExtensions",
        "set",
        "setPrototypeOf"
      ]
    })),
    ArrayConstructor: new Map(Object.entries({
      es2015: [
        "from",
        "of"
      ],
      esnext: [
        "fromAsync"
      ]
    })),
    ObjectConstructor: new Map(Object.entries({
      es2015: [
        "assign",
        "getOwnPropertySymbols",
        "keys",
        "is",
        "setPrototypeOf"
      ],
      es2017: [
        "values",
        "entries",
        "getOwnPropertyDescriptors"
      ],
      es2019: [
        "fromEntries"
      ],
      es2022: [
        "hasOwn"
      ],
      es2024: [
        "groupBy"
      ]
    })),
    NumberConstructor: new Map(Object.entries({
      es2015: [
        "isFinite",
        "isInteger",
        "isNaN",
        "isSafeInteger",
        "parseFloat",
        "parseInt"
      ]
    })),
    Math: new Map(Object.entries({
      es2015: [
        "clz32",
        "imul",
        "sign",
        "log10",
        "log2",
        "log1p",
        "expm1",
        "cosh",
        "sinh",
        "tanh",
        "acosh",
        "asinh",
        "atanh",
        "hypot",
        "trunc",
        "fround",
        "cbrt"
      ],
      esnext: [
        "f16round"
      ]
    })),
    Map: new Map(Object.entries({
      es2015: [
        "entries",
        "keys",
        "values"
      ]
    })),
    MapConstructor: new Map(Object.entries({
      es2024: [
        "groupBy"
      ]
    })),
    Set: new Map(Object.entries({
      es2015: [
        "entries",
        "keys",
        "values"
      ],
      esnext: [
        "union",
        "intersection",
        "difference",
        "symmetricDifference",
        "isSubsetOf",
        "isSupersetOf",
        "isDisjointFrom"
      ]
    })),
    PromiseConstructor: new Map(Object.entries({
      es2015: [
        "all",
        "race",
        "reject",
        "resolve"
      ],
      es2020: [
        "allSettled"
      ],
      es2021: [
        "any"
      ],
      es2024: [
        "withResolvers"
      ]
    })),
    Symbol: new Map(Object.entries({
      es2015: [
        "for",
        "keyFor"
      ],
      es2019: [
        "description"
      ]
    })),
    WeakMap: new Map(Object.entries({
      es2015: [
        "entries",
        "keys",
        "values"
      ]
    })),
    WeakSet: new Map(Object.entries({
      es2015: [
        "entries",
        "keys",
        "values"
      ]
    })),
    String: new Map(Object.entries({
      es2015: [
        "codePointAt",
        "includes",
        "endsWith",
        "normalize",
        "repeat",
        "startsWith",
        "anchor",
        "big",
        "blink",
        "bold",
        "fixed",
        "fontcolor",
        "fontsize",
        "italics",
        "link",
        "small",
        "strike",
        "sub",
        "sup"
      ],
      es2017: [
        "padStart",
        "padEnd"
      ],
      es2019: [
        "trimStart",
        "trimEnd",
        "trimLeft",
        "trimRight"
      ],
      es2020: [
        "matchAll"
      ],
      es2021: [
        "replaceAll"
      ],
      es2022: [
        "at"
      ],
      es2024: [
        "isWellFormed",
        "toWellFormed"
      ]
    })),
    StringConstructor: new Map(Object.entries({
      es2015: [
        "fromCodePoint",
        "raw"
      ]
    })),
    DateTimeFormat: new Map(Object.entries({
      es2017: [
        "formatToParts"
      ]
    })),
    Promise: new Map(Object.entries({
      es2015: emptyArray,
      es2018: [
        "finally"
      ]
    })),
    RegExpMatchArray: new Map(Object.entries({
      es2018: [
        "groups"
      ]
    })),
    RegExpExecArray: new Map(Object.entries({
      es2018: [
        "groups"
      ]
    })),
    Intl: new Map(Object.entries({
      es2018: [
        "PluralRules"
      ]
    })),
    NumberFormat: new Map(Object.entries({
      es2018: [
        "formatToParts"
      ]
    })),
    SymbolConstructor: new Map(Object.entries({
      es2020: [
        "matchAll"
      ],
      esnext: [
        "metadata",
        "dispose",
        "asyncDispose"
      ]
    })),
    DataView: new Map(Object.entries({
      es2020: [
        "setBigInt64",
        "setBigUint64",
        "getBigInt64",
        "getBigUint64"
      ],
      esnext: [
        "setFloat16",
        "getFloat16"
      ]
    })),
    BigInt: new Map(Object.entries({
      es2020: emptyArray
    })),
    RelativeTimeFormat: new Map(Object.entries({
      es2020: [
        "format",
        "formatToParts",
        "resolvedOptions"
      ]
    })),
    Int8Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Uint8Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Uint8ClampedArray: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Int16Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Uint16Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Int32Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Uint32Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Float16Array: new Map(Object.entries({
      esnext: emptyArray
    })),
    Float32Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Float64Array: new Map(Object.entries({
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    BigInt64Array: new Map(Object.entries({
      es2020: emptyArray,
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    BigUint64Array: new Map(Object.entries({
      es2020: emptyArray,
      es2022: [
        "at"
      ],
      es2023: [
        "findLastIndex",
        "findLast",
        "toReversed",
        "toSorted",
        "toSpliced",
        "with"
      ]
    })),
    Error: new Map(Object.entries({
      es2022: [
        "cause"
      ]
    }))
  }))
);
function getLiteralText(node, sourceFile, flags) {
  if (sourceFile && canUseOriginalText(node, flags)) {
    return getSourceTextOfNodeFromSourceFile(sourceFile, node);
  }
  switch (node.kind) {
    case 11 /* StringLiteral */: {
      const escapeText ***REMOVED*** flags & 2 /* JsxAttributeEscape */ ? escapeJsxAttributeString : flags & 1 /* NeverAsciiEscape */ || getEmitFlags(node) & 16777216 /* NoAsciiEscaping */ ? escapeString : escapeNonAsciiString;
      if (node.singleQuote) {
        return "'" + escapeText(node.text, 39 /* singleQuote */) + "'";
      } else {
        return '"' + escapeText(node.text, 34 /* doubleQuote */) + '"';
      }
    }
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 16 /* TemplateHead */:
    case 17 /* TemplateMiddle */:
    case 18 /* TemplateTail */: {
      const escapeText ***REMOVED*** flags & 1 /* NeverAsciiEscape */ || getEmitFlags(node) & 16777216 /* NoAsciiEscaping */ ? escapeString : escapeNonAsciiString;
      const rawText ***REMOVED*** node.rawText ?? escapeTemplateSubstitution(escapeText(node.text, 96 /* backtick */));
      switch (node.kind) {
        case 15 /* NoSubstitutionTemplateLiteral */:
          return "`" + rawText + "`";
        case 16 /* TemplateHead */:
          return "`" + rawText + "${";
        case 17 /* TemplateMiddle */:
          return "}" + rawText + "${";
        case 18 /* TemplateTail */:
          return "}" + rawText + "`";
      }
      break;
    }
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
      return node.text;
    case 14 /* RegularExpressionLiteral */:
      if (flags & 4 /* TerminateUnterminatedLiterals */ && node.isUnterminated) {
        return node.text + (node.text.charCodeAt(node.text.length - 1) ***REMOVED******REMOVED******REMOVED*** 92 /* backslash */ ? " /" : "/");
      }
      return node.text;
  }
  return Debug.fail(`Literal kind '${node.kind}' not accounted for.`);
}
function canUseOriginalText(node, flags) {
  if (nodeIsSynthesized(node) || !node.parent || flags & 4 /* TerminateUnterminatedLiterals */ && node.isUnterminated) {
    return false;
  }
  if (isNumericLiteral(node)) {
    if (node.numericLiteralFlags & 26656 /* IsInvalid */) {
      return false;
    }
    if (node.numericLiteralFlags & 512 /* ContainsSeparator */) {
      return !!(flags & 8 /* AllowNumericSeparator */);
    }
  }
  return !isBigIntLiteral(node);
}
function makeIdentifierFromModuleName(moduleName) {
  return getBaseFileName(moduleName).replace(/^(\d)/, "_$1").replace(/\W/g, "_");
}
function isBlockOrCatchScoped(declaration) {
  return (getCombinedNodeFlags(declaration) & 7 /* BlockScoped */) !***REMOVED******REMOVED*** 0 || isCatchClauseVariableDeclarationOrBindingElement(declaration);
}
function isCatchClauseVariableDeclarationOrBindingElement(declaration) {
  const node ***REMOVED*** getRootDeclaration(declaration);
  return node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */;
}
function isAmbientModule(node) {
  return isModuleDeclaration(node) && (node.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || isGlobalScopeAugmentation(node));
}
function isModuleWithStringLiteralName(node) {
  return isModuleDeclaration(node) && node.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
}
function isNonGlobalAmbientModule(node) {
  return isModuleDeclaration(node) && isStringLiteral(node.name);
}
function isEffectiveModuleDeclaration(node) {
  return isModuleDeclaration(node) || isIdentifier(node);
}
function isShorthandAmbientModuleSymbol(moduleSymbol) {
  return isShorthandAmbientModule(moduleSymbol.valueDeclaration);
}
function isShorthandAmbientModule(node) {
  return !!node && node.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && !node.body;
}
function isBlockScopedContainerTopLevel(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || node.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || isFunctionLikeOrClassStaticBlockDeclaration(node);
}
function isGlobalScopeAugmentation(module2) {
  return !!(module2.flags & 2048 /* GlobalAugmentation */);
}
function isExternalModuleAugmentation(node) {
  return isAmbientModule(node) && isModuleAugmentationExternal(node);
}
function isModuleAugmentationExternal(node) {
  switch (node.parent.kind) {
    case 308 /* SourceFile */:
      return isExternalModule(node.parent);
    case 269 /* ModuleBlock */:
      return isAmbientModule(node.parent.parent) && isSourceFile(node.parent.parent.parent) && !isExternalModule(node.parent.parent.parent);
  }
  return false;
}
function getNonAugmentationDeclaration(symbol) {
  var _a;
  return (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find((d) ***REMOVED***> !isExternalModuleAugmentation(d) && !(isModuleDeclaration(d) && isGlobalScopeAugmentation(d)));
}
function isCommonJSContainingModuleKind(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ || 100 /* Node16 */ <***REMOVED*** kind && kind <***REMOVED*** 199 /* NodeNext */;
}
function isEffectiveExternalModule(node, compilerOptions) {
  return isExternalModule(node) || isCommonJSContainingModuleKind(getEmitModuleKind(compilerOptions)) && !!node.commonJsModuleIndicator;
}
function isEffectiveStrictModeSourceFile(node, compilerOptions) {
  switch (node.scriptKind) {
    case 1 /* JS */:
    case 3 /* TS */:
    case 2 /* JSX */:
    case 4 /* TSX */:
      break;
    default:
      return false;
  }
  if (node.isDeclarationFile) {
    return false;
  }
  if (getStrictOptionValue(compilerOptions, "alwaysStrict")) {
    return true;
  }
  if (startsWithUseStrict(node.statements)) {
    return true;
  }
  if (isExternalModule(node) || getIsolatedModules(compilerOptions)) {
    return true;
  }
  return false;
}
function isAmbientPropertyDeclaration(node) {
  return !!(node.flags & 33554432 /* Ambient */) || hasSyntacticModifier(node, 128 /* Ambient */);
}
function isBlockScope(node, parentNode) {
  switch (node.kind) {
    case 308 /* SourceFile */:
    case 270 /* CaseBlock */:
    case 300 /* CatchClause */:
    case 268 /* ModuleDeclaration */:
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 177 /* Constructor */:
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
    case 173 /* PropertyDeclaration */:
    case 176 /* ClassStaticBlockDeclaration */:
      return true;
    case 242 /* Block */:
      return !isFunctionLikeOrClassStaticBlockDeclaration(parentNode);
  }
  return false;
}
function isAnyImportSyntax(node) {
  switch (node.kind) {
    case 273 /* ImportDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
      return true;
    default:
      return false;
  }
}
function isLateVisibilityPaintedStatement(node) {
  switch (node.kind) {
    case 273 /* ImportDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
    case 244 /* VariableStatement */:
    case 264 /* ClassDeclaration */:
    case 263 /* FunctionDeclaration */:
    case 268 /* ModuleDeclaration */:
    case 266 /* TypeAliasDeclaration */:
    case 265 /* InterfaceDeclaration */:
    case 267 /* EnumDeclaration */:
      return true;
    default:
      return false;
  }
}
function hasPossibleExternalModuleReference(node) {
  return isAnyImportOrReExport(node) || isModuleDeclaration(node) || isImportTypeNode(node) || isImportCall(node);
}
function isAnyImportOrReExport(node) {
  return isAnyImportSyntax(node) || isExportDeclaration(node);
}
function getEnclosingContainer(node) {
  return findAncestor(node.parent, (n) ***REMOVED***> !!(getContainerFlags(n) & 1 /* IsContainer */));
}
function getEnclosingBlockScopeContainer(node) {
  return findAncestor(node.parent, (current) ***REMOVED***> isBlockScope(current, current.parent));
}
function forEachEnclosingBlockScopeContainer(node, cb) {
  let container ***REMOVED*** getEnclosingBlockScopeContainer(node);
  while (container) {
    cb(container);
    container ***REMOVED*** getEnclosingBlockScopeContainer(container);
  }
}
function declarationNameToString(name) {
  return !name || getFullWidth(name) ***REMOVED******REMOVED******REMOVED*** 0 ? "(Missing)" : getTextOfNode(name);
}
function getNameFromIndexInfo(info) {
  return info.declaration ? declarationNameToString(info.declaration.parameters[0].name) : void 0;
}
function isComputedNonLiteralName(name) {
  return name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ && !isStringOrNumericLiteralLike(name.expression);
}
function tryGetTextOfPropertyName(name) {
  var _a;
  switch (name.kind) {
    case 80 /* Identifier */:
    case 81 /* PrivateIdentifier */:
      return ((_a ***REMOVED*** name.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.autoGenerate) ? void 0 : name.escapedText;
    case 11 /* StringLiteral */:
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
      return escapeLeadingUnderscores(name.text);
    case 168 /* ComputedPropertyName */:
      if (isStringOrNumericLiteralLike(name.expression)) return escapeLeadingUnderscores(name.expression.text);
      return void 0;
    case 296 /* JsxNamespacedName */:
      return getEscapedTextOfJsxNamespacedName(name);
    default:
      return Debug.assertNever(name);
  }
}
function getTextOfPropertyName(name) {
  return Debug.checkDefined(tryGetTextOfPropertyName(name));
}
function entityNameToString(name) {
  switch (name.kind) {
    case 110 /* ThisKeyword */:
      return "this";
    case 81 /* PrivateIdentifier */:
    case 80 /* Identifier */:
      return getFullWidth(name) ***REMOVED******REMOVED******REMOVED*** 0 ? idText(name) : getTextOfNode(name);
    case 167 /* QualifiedName */:
      return entityNameToString(name.left) + "." + entityNameToString(name.right);
    case 212 /* PropertyAccessExpression */:
      if (isIdentifier(name.name) || isPrivateIdentifier(name.name)) {
        return entityNameToString(name.expression) + "." + entityNameToString(name.name);
      } else {
        return Debug.assertNever(name.name);
      }
    case 312 /* JSDocMemberName */:
      return entityNameToString(name.left) + "#" + entityNameToString(name.right);
    case 296 /* JsxNamespacedName */:
      return entityNameToString(name.namespace) + ":" + entityNameToString(name.name);
    default:
      return Debug.assertNever(name);
  }
}
function createDiagnosticForNode(node, message, ...args) {
  const sourceFile ***REMOVED*** getSourceFileOfNode(node);
  return createDiagnosticForNodeInSourceFile(sourceFile, node, message, ...args);
}
function createDiagnosticForNodeArray(sourceFile, nodes, message, ...args) {
  const start ***REMOVED*** skipTrivia(sourceFile.text, nodes.pos);
  return createFileDiagnostic(sourceFile, start, nodes.end - start, message, ...args);
}
function createDiagnosticForNodeInSourceFile(sourceFile, node, message, ...args) {
  const span ***REMOVED*** getErrorSpanForNode(sourceFile, node);
  return createFileDiagnostic(sourceFile, span.start, span.length, message, ...args);
}
function createDiagnosticForNodeFromMessageChain(sourceFile, node, messageChain, relatedInformation) {
  const span ***REMOVED*** getErrorSpanForNode(sourceFile, node);
  return createFileDiagnosticFromMessageChain(sourceFile, span.start, span.length, messageChain, relatedInformation);
}
function createDiagnosticForNodeArrayFromMessageChain(sourceFile, nodes, messageChain, relatedInformation) {
  const start ***REMOVED*** skipTrivia(sourceFile.text, nodes.pos);
  return createFileDiagnosticFromMessageChain(sourceFile, start, nodes.end - start, messageChain, relatedInformation);
}
function assertDiagnosticLocation(sourceText, start, length2) {
  Debug.assertGreaterThanOrEqual(start, 0);
  Debug.assertGreaterThanOrEqual(length2, 0);
  Debug.assertLessThanOrEqual(start, sourceText.length);
  Debug.assertLessThanOrEqual(start + length2, sourceText.length);
}
function createFileDiagnosticFromMessageChain(file, start, length2, messageChain, relatedInformation) {
  assertDiagnosticLocation(file.text, start, length2);
  return {
    file,
    start,
    length: length2,
    code: messageChain.code,
    category: messageChain.category,
    messageText: messageChain.next ? messageChain : messageChain.messageText,
    relatedInformation,
    canonicalHead: messageChain.canonicalHead
  };
}
function createDiagnosticForFileFromMessageChain(sourceFile, messageChain, relatedInformation) {
  return {
    file: sourceFile,
    start: 0,
    length: 0,
    code: messageChain.code,
    category: messageChain.category,
    messageText: messageChain.next ? messageChain : messageChain.messageText,
    relatedInformation
  };
}
function createDiagnosticMessageChainFromDiagnostic(diagnostic) {
  return typeof diagnostic.messageText ***REMOVED******REMOVED******REMOVED*** "string" ? {
    code: diagnostic.code,
    category: diagnostic.category,
    messageText: diagnostic.messageText,
    next: diagnostic.next
  } : diagnostic.messageText;
}
function createDiagnosticForRange(sourceFile, range, message) {
  return {
    file: sourceFile,
    start: range.pos,
    length: range.end - range.pos,
    code: message.code,
    category: message.category,
    messageText: message.message
  };
}
function getCanonicalDiagnostic(message, ...args) {
  return {
    code: message.code,
    messageText: formatMessage(message, ...args)
  };
}
function getSpanOfTokenAtPosition(sourceFile, pos) {
  const scanner ***REMOVED*** createScanner(
    sourceFile.languageVersion,
    /*skipTrivia*/
    true,
    sourceFile.languageVariant,
    sourceFile.text,
    /*onError*/
    void 0,
    pos
  );
  scanner.scan();
  const start ***REMOVED*** scanner.getTokenStart();
  return createTextSpanFromBounds(start, scanner.getTokenEnd());
}
function scanTokenAtPosition(sourceFile, pos) {
  const scanner ***REMOVED*** createScanner(
    sourceFile.languageVersion,
    /*skipTrivia*/
    true,
    sourceFile.languageVariant,
    sourceFile.text,
    /*onError*/
    void 0,
    pos
  );
  scanner.scan();
  return scanner.getToken();
}
function getErrorSpanForArrowFunction(sourceFile, node) {
  const pos ***REMOVED*** skipTrivia(sourceFile.text, node.pos);
  if (node.body && node.body.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */) {
    const { line: startLine } ***REMOVED*** getLineAndCharacterOfPosition(sourceFile, node.body.pos);
    const { line: endLine } ***REMOVED*** getLineAndCharacterOfPosition(sourceFile, node.body.end);
    if (startLine < endLine) {
      return createTextSpan(pos, getEndLinePosition(startLine, sourceFile) - pos + 1);
    }
  }
  return createTextSpanFromBounds(pos, node.end);
}
function getErrorSpanForNode(sourceFile, node) {
  let errorNode ***REMOVED*** node;
  switch (node.kind) {
    case 308 /* SourceFile */: {
      const pos2 ***REMOVED*** skipTrivia(
        sourceFile.text,
        0,
        /*stopAfterLineBreak*/
        false
      );
      if (pos2 ***REMOVED******REMOVED******REMOVED*** sourceFile.text.length) {
        return createTextSpan(0, 0);
      }
      return getSpanOfTokenAtPosition(sourceFile, pos2);
    }
    // This list is a work in progress. Add missing node kinds to improve their error
    // spans.
    case 261 /* VariableDeclaration */:
    case 209 /* BindingElement */:
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
    case 265 /* InterfaceDeclaration */:
    case 268 /* ModuleDeclaration */:
    case 267 /* EnumDeclaration */:
    case 307 /* EnumMember */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 266 /* TypeAliasDeclaration */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 275 /* NamespaceImport */:
      errorNode ***REMOVED*** node.name;
      break;
    case 220 /* ArrowFunction */:
      return getErrorSpanForArrowFunction(sourceFile, node);
    case 297 /* CaseClause */:
    case 298 /* DefaultClause */: {
      const start ***REMOVED*** skipTrivia(sourceFile.text, node.pos);
      const end ***REMOVED*** node.statements.length > 0 ? node.statements[0].pos : node.end;
      return createTextSpanFromBounds(start, end);
    }
    case 254 /* ReturnStatement */:
    case 230 /* YieldExpression */: {
      const pos2 ***REMOVED*** skipTrivia(sourceFile.text, node.pos);
      return getSpanOfTokenAtPosition(sourceFile, pos2);
    }
    case 239 /* SatisfiesExpression */: {
      const pos2 ***REMOVED*** skipTrivia(sourceFile.text, node.expression.end);
      return getSpanOfTokenAtPosition(sourceFile, pos2);
    }
    case 351 /* JSDocSatisfiesTag */: {
      const pos2 ***REMOVED*** skipTrivia(sourceFile.text, node.tagName.pos);
      return getSpanOfTokenAtPosition(sourceFile, pos2);
    }
    case 177 /* Constructor */: {
      const constructorDeclaration ***REMOVED*** node;
      const start ***REMOVED*** skipTrivia(sourceFile.text, constructorDeclaration.pos);
      const scanner ***REMOVED*** createScanner(
        sourceFile.languageVersion,
        /*skipTrivia*/
        true,
        sourceFile.languageVariant,
        sourceFile.text,
        /*onError*/
        void 0,
        start
      );
      let token ***REMOVED*** scanner.scan();
      while (token !***REMOVED******REMOVED*** 137 /* ConstructorKeyword */ && token !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
        token ***REMOVED*** scanner.scan();
      }
      const end ***REMOVED*** scanner.getTokenEnd();
      return createTextSpanFromBounds(start, end);
    }
  }
  if (errorNode ***REMOVED******REMOVED******REMOVED*** void 0) {
    return getSpanOfTokenAtPosition(sourceFile, node.pos);
  }
  Debug.assert(!isJSDoc(errorNode));
  const isMissing ***REMOVED*** nodeIsMissing(errorNode);
  const pos ***REMOVED*** isMissing || isJsxText(node) ? errorNode.pos : skipTrivia(sourceFile.text, errorNode.pos);
  if (isMissing) {
    Debug.assert(pos ***REMOVED******REMOVED******REMOVED*** errorNode.pos, "This failure could trigger https://github.com/Microsoft/TypeScript/issues/20809");
    Debug.assert(pos ***REMOVED******REMOVED******REMOVED*** errorNode.end, "This failure could trigger https://github.com/Microsoft/TypeScript/issues/20809");
  } else {
    Debug.assert(pos >***REMOVED*** errorNode.pos, "This failure could trigger https://github.com/Microsoft/TypeScript/issues/20809");
    Debug.assert(pos <***REMOVED*** errorNode.end, "This failure could trigger https://github.com/Microsoft/TypeScript/issues/20809");
  }
  return createTextSpanFromBounds(pos, errorNode.end);
}
function isGlobalSourceFile(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && !isExternalOrCommonJsModule(node);
}
function isExternalOrCommonJsModule(file) {
  return (file.externalModuleIndicator || file.commonJsModuleIndicator) !***REMOVED******REMOVED*** void 0;
}
function isJsonSourceFile(file) {
  return file.scriptKind ***REMOVED******REMOVED******REMOVED*** 6 /* JSON */;
}
function isEnumConst(node) {
  return !!(getCombinedModifierFlags(node) & 4096 /* Const */);
}
function isDeclarationReadonly(declaration) {
  return !!(getCombinedModifierFlags(declaration) & 8 /* Readonly */ && !isParameterPropertyDeclaration(declaration, declaration.parent));
}
function isVarAwaitUsing(node) {
  return (getCombinedNodeFlags(node) & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */;
}
function isVarUsing(node) {
  return (getCombinedNodeFlags(node) & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 4 /* Using */;
}
function isVarConst(node) {
  return (getCombinedNodeFlags(node) & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 2 /* Const */;
}
function isVarConstLike(node) {
  const blockScopeKind ***REMOVED*** getCombinedNodeFlags(node) & 7 /* BlockScoped */;
  return blockScopeKind ***REMOVED******REMOVED******REMOVED*** 2 /* Const */ || blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ || blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */;
}
function isLet(node) {
  return (getCombinedNodeFlags(node) & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 1 /* Let */;
}
function isSuperCall(n) {
  return n.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && n.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */;
}
function isImportCall(n) {
  if (n.kind !***REMOVED******REMOVED*** 214 /* CallExpression */) return false;
  const e ***REMOVED*** n.expression;
  return e.kind ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */ || isMetaProperty(e) && e.keywordToken ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */ && e.name.escapedText ***REMOVED******REMOVED******REMOVED*** "defer";
}
function isImportMeta(n) {
  return isMetaProperty(n) && n.keywordToken ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */ && n.name.escapedText ***REMOVED******REMOVED******REMOVED*** "meta";
}
function isLiteralImportTypeNode(n) {
  return isImportTypeNode(n) && isLiteralTypeNode(n.argument) && isStringLiteral(n.argument.literal);
}
function isPrologueDirective(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */ && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
}
function isCustomPrologue(node) {
  return !!(getEmitFlags(node) & 2097152 /* CustomPrologue */);
}
function isHoistedFunction(node) {
  return isCustomPrologue(node) && isFunctionDeclaration(node);
}
function isHoistedVariable(node) {
  return isIdentifier(node.name) && !node.initializer;
}
function isHoistedVariableStatement(node) {
  return isCustomPrologue(node) && isVariableStatement(node) && every(node.declarationList.declarations, isHoistedVariable);
}
function getLeadingCommentRangesOfNode(node, sourceFileOfNode) {
  return node.kind !***REMOVED******REMOVED*** 12 /* JsxText */ ? getLeadingCommentRanges(sourceFileOfNode.text, node.pos) : void 0;
}
function getJSDocCommentRanges(node, text) {
  const commentRanges ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ || node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || node.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ ? concatenate(getTrailingCommentRanges(text, node.pos), getLeadingCommentRanges(text, node.pos)) : getLeadingCommentRanges(text, node.pos);
  return filter(commentRanges, (comment) ***REMOVED***> comment.end <***REMOVED*** node.end && // Due to parse errors sometime empty parameter may get comments assigned to it that end up not in parameter range
  text.charCodeAt(comment.pos + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(comment.pos + 2) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(comment.pos + 3) !***REMOVED******REMOVED*** 47 /* slash */);
}
var fullTripleSlashReferencePathRegEx ***REMOVED*** /^\/\/\/\s*<reference\s+path\s****REMOVED***\s*(?:'[^']*'|"[^"]*").*?\/>/;
var fullTripleSlashReferenceTypeReferenceDirectiveRegEx ***REMOVED*** /^\/\/\/\s*<reference\s+types\s****REMOVED***\s*(?:'[^']*'|"[^"]*").*?\/>/;
var fullTripleSlashLibReferenceRegEx ***REMOVED*** /^\/\/\/\s*<reference\s+lib\s****REMOVED***\s*(?:'[^']*'|"[^"]*").*?\/>/;
var fullTripleSlashAMDReferencePathRegEx ***REMOVED*** /^\/\/\/\s*<amd-dependency\s+path\s****REMOVED***\s*(?:'[^']*'|"[^"]*").*?\/>/;
var fullTripleSlashAMDModuleRegEx ***REMOVED*** /^\/\/\/\s*<amd-module\s+(?:\S.*?)??\/>/;
var defaultLibReferenceRegEx ***REMOVED*** /^\/\/\/\s*<reference\s+no-default-lib\s****REMOVED***\s*(?:'[^']*'|"[^"]*")\s*\/>/;
function isPartOfTypeNode(node) {
  if (183 /* FirstTypeNode */ <***REMOVED*** node.kind && node.kind <***REMOVED*** 206 /* LastTypeNode */) {
    return true;
  }
  switch (node.kind) {
    case 133 /* AnyKeyword */:
    case 159 /* UnknownKeyword */:
    case 150 /* NumberKeyword */:
    case 163 /* BigIntKeyword */:
    case 154 /* StringKeyword */:
    case 136 /* BooleanKeyword */:
    case 155 /* SymbolKeyword */:
    case 151 /* ObjectKeyword */:
    case 157 /* UndefinedKeyword */:
    case 106 /* NullKeyword */:
    case 146 /* NeverKeyword */:
      return true;
    case 116 /* VoidKeyword */:
      return node.parent.kind !***REMOVED******REMOVED*** 223 /* VoidExpression */;
    case 234 /* ExpressionWithTypeArguments */:
      return isPartOfTypeExpressionWithTypeArguments(node);
    case 169 /* TypeParameter */:
      return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 196 /* InferType */;
    // Identifiers and qualified names may be type nodes, depending on their context. Climb
    // above them to find the lowest container
    case 80 /* Identifier */:
      if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ && node.parent.right ***REMOVED******REMOVED******REMOVED*** node) {
        node ***REMOVED*** node.parent;
      } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && node.parent.name ***REMOVED******REMOVED******REMOVED*** node) {
        node ***REMOVED*** node.parent;
      }
      Debug.assert(node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */, "'node' was expected to be a qualified name, identifier or property access in 'isPartOfTypeNode'.");
    // falls through
    case 167 /* QualifiedName */:
    case 212 /* PropertyAccessExpression */:
    case 110 /* ThisKeyword */: {
      const { parent } ***REMOVED*** node;
      if (parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */) {
        return false;
      }
      if (parent.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */) {
        return !parent.isTypeOf;
      }
      if (183 /* FirstTypeNode */ <***REMOVED*** parent.kind && parent.kind <***REMOVED*** 206 /* LastTypeNode */) {
        return true;
      }
      switch (parent.kind) {
        case 234 /* ExpressionWithTypeArguments */:
          return isPartOfTypeExpressionWithTypeArguments(parent);
        case 169 /* TypeParameter */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.constraint;
        case 346 /* JSDocTemplateTag */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.constraint;
        case 173 /* PropertyDeclaration */:
        case 172 /* PropertySignature */:
        case 170 /* Parameter */:
        case 261 /* VariableDeclaration */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.type;
        case 263 /* FunctionDeclaration */:
        case 219 /* FunctionExpression */:
        case 220 /* ArrowFunction */:
        case 177 /* Constructor */:
        case 175 /* MethodDeclaration */:
        case 174 /* MethodSignature */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.type;
        case 180 /* CallSignature */:
        case 181 /* ConstructSignature */:
        case 182 /* IndexSignature */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.type;
        case 217 /* TypeAssertionExpression */:
          return node ***REMOVED******REMOVED******REMOVED*** parent.type;
        case 214 /* CallExpression */:
        case 215 /* NewExpression */:
        case 216 /* TaggedTemplateExpression */:
          return contains(parent.typeArguments, node);
      }
    }
  }
  return false;
}
function isPartOfTypeExpressionWithTypeArguments(node) {
  return isJSDocImplementsTag(node.parent) || isJSDocAugmentsTag(node.parent) || isHeritageClause(node.parent) && !isExpressionWithTypeArgumentsInClassExtendsClause(node);
}
function forEachReturnStatement(body, visitor) {
  return traverse(body);
  function traverse(node) {
    switch (node.kind) {
      case 254 /* ReturnStatement */:
        return visitor(node);
      case 270 /* CaseBlock */:
      case 242 /* Block */:
      case 246 /* IfStatement */:
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
      case 249 /* ForStatement */:
      case 250 /* ForInStatement */:
      case 251 /* ForOfStatement */:
      case 255 /* WithStatement */:
      case 256 /* SwitchStatement */:
      case 297 /* CaseClause */:
      case 298 /* DefaultClause */:
      case 257 /* LabeledStatement */:
      case 259 /* TryStatement */:
      case 300 /* CatchClause */:
        return forEachChild(node, traverse);
    }
  }
}
function forEachYieldExpression(body, visitor) {
  return traverse(body);
  function traverse(node) {
    switch (node.kind) {
      case 230 /* YieldExpression */:
        visitor(node);
        const operand ***REMOVED*** node.expression;
        if (operand) {
          traverse(operand);
        }
        return;
      case 267 /* EnumDeclaration */:
      case 265 /* InterfaceDeclaration */:
      case 268 /* ModuleDeclaration */:
      case 266 /* TypeAliasDeclaration */:
        return;
      default:
        if (isFunctionLike(node)) {
          if (node.name && node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
            traverse(node.name.expression);
            return;
          }
        } else if (!isPartOfTypeNode(node)) {
          forEachChild(node, traverse);
        }
    }
  }
}
function getRestParameterElementType(node) {
  if (node && node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */) {
    return node.elementType;
  } else if (node && node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */) {
    return singleOrUndefined(node.typeArguments);
  } else {
    return void 0;
  }
}
function getMembersOfDeclaration(node) {
  switch (node.kind) {
    case 265 /* InterfaceDeclaration */:
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
    case 188 /* TypeLiteral */:
      return node.members;
    case 211 /* ObjectLiteralExpression */:
      return node.properties;
  }
}
function isVariableLike(node) {
  if (node) {
    switch (node.kind) {
      case 209 /* BindingElement */:
      case 307 /* EnumMember */:
      case 170 /* Parameter */:
      case 304 /* PropertyAssignment */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 305 /* ShorthandPropertyAssignment */:
      case 261 /* VariableDeclaration */:
        return true;
    }
  }
  return false;
}
function isVariableDeclarationInVariableStatement(node) {
  return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */ && node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */;
}
function isCommonJsExportedExpression(node) {
  if (!isInJSFile(node)) return false;
  return isObjectLiteralExpression(node.parent) && isBinaryExpression(node.parent.parent) && getAssignmentDeclarationKind(node.parent.parent) ***REMOVED******REMOVED******REMOVED*** 2 /* ModuleExports */ || isCommonJsExportPropertyAssignment(node.parent);
}
function isCommonJsExportPropertyAssignment(node) {
  if (!isInJSFile(node)) return false;
  return isBinaryExpression(node) && getAssignmentDeclarationKind(node) ***REMOVED******REMOVED******REMOVED*** 1 /* ExportsProperty */;
}
function isValidESSymbolDeclaration(node) {
  return (isVariableDeclaration(node) ? isVarConst(node) && isIdentifier(node.name) && isVariableDeclarationInVariableStatement(node) : isPropertyDeclaration(node) ? hasEffectiveReadonlyModifier(node) && hasStaticModifier(node) : isPropertySignature(node) && hasEffectiveReadonlyModifier(node)) || isCommonJsExportPropertyAssignment(node);
}
function introducesArgumentsExoticObject(node) {
  switch (node.kind) {
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 177 /* Constructor */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
      return true;
  }
  return false;
}
function unwrapInnermostStatementOfLabel(node, beforeUnwrapLabelCallback) {
  while (true) {
    if (beforeUnwrapLabelCallback) {
      beforeUnwrapLabelCallback(node);
    }
    if (node.statement.kind !***REMOVED******REMOVED*** 257 /* LabeledStatement */) {
      return node.statement;
    }
    node ***REMOVED*** node.statement;
  }
}
function isFunctionBlock(node) {
  return node && node.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */ && isFunctionLike(node.parent);
}
function isObjectLiteralMethod(node) {
  return node && node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */;
}
function isObjectLiteralOrClassExpressionMethodOrAccessor(node) {
  return (node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) && (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */);
}
function isIdentifierTypePredicate(predicate) {
  return predicate && predicate.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */;
}
function isThisTypePredicate(predicate) {
  return predicate && predicate.kind ***REMOVED******REMOVED******REMOVED*** 0 /* This */;
}
function forEachPropertyAssignment(objectLiteral, key, callback, key2) {
  return forEach(objectLiteral ***REMOVED******REMOVED*** null ? void 0 : objectLiteral.properties, (property) ***REMOVED***> {
    if (!isPropertyAssignment(property)) return void 0;
    const propName ***REMOVED*** tryGetTextOfPropertyName(property.name);
    return key ***REMOVED******REMOVED******REMOVED*** propName || key2 && key2 ***REMOVED******REMOVED******REMOVED*** propName ? callback(property) : void 0;
  });
}
function getTsConfigObjectLiteralExpression(tsConfigSourceFile) {
  if (tsConfigSourceFile && tsConfigSourceFile.statements.length) {
    const expression ***REMOVED*** tsConfigSourceFile.statements[0].expression;
    return tryCast(expression, isObjectLiteralExpression);
  }
}
function getTsConfigPropArrayElementValue(tsConfigSourceFile, propKey, elementValue) {
  return forEachTsConfigPropArray(tsConfigSourceFile, propKey, (property) ***REMOVED***> isArrayLiteralExpression(property.initializer) ? find(property.initializer.elements, (element) ***REMOVED***> isStringLiteral(element) && element.text ***REMOVED******REMOVED******REMOVED*** elementValue) : void 0);
}
function forEachTsConfigPropArray(tsConfigSourceFile, propKey, callback) {
  return forEachPropertyAssignment(getTsConfigObjectLiteralExpression(tsConfigSourceFile), propKey, callback);
}
function getContainingFunction(node) {
  return findAncestor(node.parent, isFunctionLike);
}
function getContainingClass(node) {
  return findAncestor(node.parent, isClassLike);
}
function getContainingClassStaticBlock(node) {
  return findAncestor(node.parent, (n) ***REMOVED***> {
    if (isClassLike(n) || isFunctionLike(n)) {
      return "quit";
    }
    return isClassStaticBlockDeclaration(n);
  });
}
function getContainingFunctionOrClassStaticBlock(node) {
  return findAncestor(node.parent, isFunctionLikeOrClassStaticBlockDeclaration);
}
function getContainingClassExcludingClassDecorators(node) {
  const decorator ***REMOVED*** findAncestor(node.parent, (n) ***REMOVED***> isClassLike(n) ? "quit" : isDecorator(n));
  return decorator && isClassLike(decorator.parent) ? getContainingClass(decorator.parent) : getContainingClass(decorator ?? node);
}
function getThisContainer(node, includeArrowFunctions, includeClassComputedPropertyName) {
  Debug.assert(node.kind !***REMOVED******REMOVED*** 308 /* SourceFile */);
  while (true) {
    node ***REMOVED*** node.parent;
    if (!node) {
      return Debug.fail();
    }
    switch (node.kind) {
      case 168 /* ComputedPropertyName */:
        if (includeClassComputedPropertyName && isClassLike(node.parent.parent)) {
          return node;
        }
        node ***REMOVED*** node.parent.parent;
        break;
      case 171 /* Decorator */:
        if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && isClassElement(node.parent.parent)) {
          node ***REMOVED*** node.parent.parent;
        } else if (isClassElement(node.parent)) {
          node ***REMOVED*** node.parent;
        }
        break;
      case 220 /* ArrowFunction */:
        if (!includeArrowFunctions) {
          continue;
        }
      // falls through
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 268 /* ModuleDeclaration */:
      case 176 /* ClassStaticBlockDeclaration */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 177 /* Constructor */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 180 /* CallSignature */:
      case 181 /* ConstructSignature */:
      case 182 /* IndexSignature */:
      case 267 /* EnumDeclaration */:
      case 308 /* SourceFile */:
        return node;
    }
  }
}
function isThisContainerOrFunctionBlock(node) {
  switch (node.kind) {
    // Arrow functions use the same scope, but may do so in a "delayed" manner
    // For example, `const getThis ***REMOVED*** () ***REMOVED***> this` may be before a super() call in a derived constructor
    case 220 /* ArrowFunction */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 173 /* PropertyDeclaration */:
      return true;
    case 242 /* Block */:
      switch (node.parent.kind) {
        case 177 /* Constructor */:
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          return true;
        default:
          return false;
      }
    default:
      return false;
  }
}
function isInTopLevelContext(node) {
  if (isIdentifier(node) && (isClassDeclaration(node.parent) || isFunctionDeclaration(node.parent)) && node.parent.name ***REMOVED******REMOVED******REMOVED*** node) {
    node ***REMOVED*** node.parent;
  }
  const container ***REMOVED*** getThisContainer(
    node,
    /*includeArrowFunctions*/
    true,
    /*includeClassComputedPropertyName*/
    false
  );
  return isSourceFile(container);
}
function getNewTargetContainer(node) {
  const container ***REMOVED*** getThisContainer(
    node,
    /*includeArrowFunctions*/
    false,
    /*includeClassComputedPropertyName*/
    false
  );
  if (container) {
    switch (container.kind) {
      case 177 /* Constructor */:
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
        return container;
    }
  }
  return void 0;
}
function getSuperContainer(node, stopOnFunctions) {
  while (true) {
    node ***REMOVED*** node.parent;
    if (!node) {
      return void 0;
    }
    switch (node.kind) {
      case 168 /* ComputedPropertyName */:
        node ***REMOVED*** node.parent;
        break;
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        if (!stopOnFunctions) {
          continue;
        }
      // falls through
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 177 /* Constructor */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 176 /* ClassStaticBlockDeclaration */:
        return node;
      case 171 /* Decorator */:
        if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && isClassElement(node.parent.parent)) {
          node ***REMOVED*** node.parent.parent;
        } else if (isClassElement(node.parent)) {
          node ***REMOVED*** node.parent;
        }
        break;
    }
  }
}
function getImmediatelyInvokedFunctionExpression(func) {
  if (func.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || func.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
    let prev ***REMOVED*** func;
    let parent ***REMOVED*** func.parent;
    while (parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
      prev ***REMOVED*** parent;
      parent ***REMOVED*** parent.parent;
    }
    if (parent.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && parent.expression ***REMOVED******REMOVED******REMOVED*** prev) {
      return parent;
    }
  }
}
function isSuperProperty(node) {
  const kind ***REMOVED*** node.kind;
  return (kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */;
}
function isThisProperty(node) {
  const kind ***REMOVED*** node.kind;
  return (kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
}
function isThisInitializedDeclaration(node) {
  var _a;
  return !!node && isVariableDeclaration(node) && ((_a ***REMOVED*** node.initializer) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
}
function isThisInitializedObjectBindingExpression(node) {
  return !!node && (isShorthandPropertyAssignment(node) || isPropertyAssignment(node)) && isBinaryExpression(node.parent.parent) && node.parent.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && node.parent.parent.right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
}
function getEntityNameFromTypeNode(node) {
  switch (node.kind) {
    case 184 /* TypeReference */:
      return node.typeName;
    case 234 /* ExpressionWithTypeArguments */:
      return isEntityNameExpression(node.expression) ? node.expression : void 0;
    // TODO(rbuckton): These aren't valid TypeNodes, but we treat them as such because of `isPartOfTypeNode`, which returns `true` for things that aren't `TypeNode`s.
    case 80 /* Identifier */:
    case 167 /* QualifiedName */:
      return node;
  }
  return void 0;
}
function getInvokedExpression(node) {
  switch (node.kind) {
    case 216 /* TaggedTemplateExpression */:
      return node.tag;
    case 287 /* JsxOpeningElement */:
    case 286 /* JsxSelfClosingElement */:
      return node.tagName;
    case 227 /* BinaryExpression */:
      return node.right;
    case 290 /* JsxOpeningFragment */:
      return node;
    default:
      return node.expression;
  }
}
function nodeCanBeDecorated(useLegacyDecorators, node, parent, grandparent) {
  if (useLegacyDecorators && isNamedDeclaration(node) && isPrivateIdentifier(node.name)) {
    return false;
  }
  switch (node.kind) {
    case 264 /* ClassDeclaration */:
      return true;
    case 232 /* ClassExpression */:
      return !useLegacyDecorators;
    case 173 /* PropertyDeclaration */:
      return parent !***REMOVED******REMOVED*** void 0 && (useLegacyDecorators ? isClassDeclaration(parent) : isClassLike(parent) && !hasAbstractModifier(node) && !hasAmbientModifier(node));
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 175 /* MethodDeclaration */:
      return node.body !***REMOVED******REMOVED*** void 0 && parent !***REMOVED******REMOVED*** void 0 && (useLegacyDecorators ? isClassDeclaration(parent) : isClassLike(parent));
    case 170 /* Parameter */:
      if (!useLegacyDecorators) return false;
      return parent !***REMOVED******REMOVED*** void 0 && parent.body !***REMOVED******REMOVED*** void 0 && (parent.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) && getThisParameter(parent) !***REMOVED******REMOVED*** node && grandparent !***REMOVED******REMOVED*** void 0 && grandparent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */;
  }
  return false;
}
function nodeIsDecorated(useLegacyDecorators, node, parent, grandparent) {
  return hasDecorators(node) && nodeCanBeDecorated(useLegacyDecorators, node, parent, grandparent);
}
function nodeOrChildIsDecorated(useLegacyDecorators, node, parent, grandparent) {
  return nodeIsDecorated(useLegacyDecorators, node, parent, grandparent) || childIsDecorated(useLegacyDecorators, node, parent);
}
function childIsDecorated(useLegacyDecorators, node, parent) {
  switch (node.kind) {
    case 264 /* ClassDeclaration */:
      return some(node.members, (m) ***REMOVED***> nodeOrChildIsDecorated(useLegacyDecorators, m, node, parent));
    case 232 /* ClassExpression */:
      return !useLegacyDecorators && some(node.members, (m) ***REMOVED***> nodeOrChildIsDecorated(useLegacyDecorators, m, node, parent));
    case 175 /* MethodDeclaration */:
    case 179 /* SetAccessor */:
    case 177 /* Constructor */:
      return some(node.parameters, (p) ***REMOVED***> nodeIsDecorated(useLegacyDecorators, p, node, parent));
    default:
      return false;
  }
}
function classOrConstructorParameterIsDecorated(useLegacyDecorators, node) {
  if (nodeIsDecorated(useLegacyDecorators, node)) return true;
  const constructor ***REMOVED*** getFirstConstructorWithBody(node);
  return !!constructor && childIsDecorated(useLegacyDecorators, constructor, node);
}
function classElementOrClassElementParameterIsDecorated(useLegacyDecorators, node, parent) {
  let parameters;
  if (isAccessor(node)) {
    const { firstAccessor, secondAccessor, setAccessor } ***REMOVED*** getAllAccessorDeclarations(parent.members, node);
    const firstAccessorWithDecorators ***REMOVED*** hasDecorators(firstAccessor) ? firstAccessor : secondAccessor && hasDecorators(secondAccessor) ? secondAccessor : void 0;
    if (!firstAccessorWithDecorators || node !***REMOVED******REMOVED*** firstAccessorWithDecorators) {
      return false;
    }
    parameters ***REMOVED*** setAccessor ***REMOVED******REMOVED*** null ? void 0 : setAccessor.parameters;
  } else if (isMethodDeclaration(node)) {
    parameters ***REMOVED*** node.parameters;
  }
  if (nodeIsDecorated(useLegacyDecorators, node, parent)) {
    return true;
  }
  if (parameters) {
    for (const parameter of parameters) {
      if (parameterIsThisKeyword(parameter)) continue;
      if (nodeIsDecorated(useLegacyDecorators, parameter, node, parent)) return true;
    }
  }
  return false;
}
function isEmptyStringLiteral(node) {
  if (node.textSourceNode) {
    switch (node.textSourceNode.kind) {
      case 11 /* StringLiteral */:
        return isEmptyStringLiteral(node.textSourceNode);
      case 15 /* NoSubstitutionTemplateLiteral */:
        return node.text ***REMOVED******REMOVED******REMOVED*** "";
    }
    return false;
  }
  return node.text ***REMOVED******REMOVED******REMOVED*** "";
}
function isJSXTagName(node) {
  const { parent } ***REMOVED*** node;
  if (parent.kind ***REMOVED******REMOVED******REMOVED*** 287 /* JsxOpeningElement */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 288 /* JsxClosingElement */) {
    return parent.tagName ***REMOVED******REMOVED******REMOVED*** node;
  }
  return false;
}
function isExpressionNode(node) {
  switch (node.kind) {
    case 108 /* SuperKeyword */:
    case 106 /* NullKeyword */:
    case 112 /* TrueKeyword */:
    case 97 /* FalseKeyword */:
    case 14 /* RegularExpressionLiteral */:
    case 210 /* ArrayLiteralExpression */:
    case 211 /* ObjectLiteralExpression */:
    case 212 /* PropertyAccessExpression */:
    case 213 /* ElementAccessExpression */:
    case 214 /* CallExpression */:
    case 215 /* NewExpression */:
    case 216 /* TaggedTemplateExpression */:
    case 235 /* AsExpression */:
    case 217 /* TypeAssertionExpression */:
    case 239 /* SatisfiesExpression */:
    case 236 /* NonNullExpression */:
    case 218 /* ParenthesizedExpression */:
    case 219 /* FunctionExpression */:
    case 232 /* ClassExpression */:
    case 220 /* ArrowFunction */:
    case 223 /* VoidExpression */:
    case 221 /* DeleteExpression */:
    case 222 /* TypeOfExpression */:
    case 225 /* PrefixUnaryExpression */:
    case 226 /* PostfixUnaryExpression */:
    case 227 /* BinaryExpression */:
    case 228 /* ConditionalExpression */:
    case 231 /* SpreadElement */:
    case 229 /* TemplateExpression */:
    case 233 /* OmittedExpression */:
    case 285 /* JsxElement */:
    case 286 /* JsxSelfClosingElement */:
    case 289 /* JsxFragment */:
    case 230 /* YieldExpression */:
    case 224 /* AwaitExpression */:
      return true;
    case 237 /* MetaProperty */:
      return !isImportCall(node.parent) || node.parent.expression !***REMOVED******REMOVED*** node;
    case 234 /* ExpressionWithTypeArguments */:
      return !isHeritageClause(node.parent) && !isJSDocAugmentsTag(node.parent);
    case 167 /* QualifiedName */:
      while (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
        node ***REMOVED*** node.parent;
      }
      return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ || isJSDocLinkLike(node.parent) || isJSDocNameReference(node.parent) || isJSDocMemberName(node.parent) || isJSXTagName(node);
    case 312 /* JSDocMemberName */:
      while (isJSDocMemberName(node.parent)) {
        node ***REMOVED*** node.parent;
      }
      return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ || isJSDocLinkLike(node.parent) || isJSDocNameReference(node.parent) || isJSDocMemberName(node.parent) || isJSXTagName(node);
    case 81 /* PrivateIdentifier */:
      return isBinaryExpression(node.parent) && node.parent.left ***REMOVED******REMOVED******REMOVED*** node && node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */;
    case 80 /* Identifier */:
      if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ || isJSDocLinkLike(node.parent) || isJSDocNameReference(node.parent) || isJSDocMemberName(node.parent) || isJSXTagName(node)) {
        return true;
      }
    // falls through
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
    case 11 /* StringLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 110 /* ThisKeyword */:
      return isInExpressionContext(node);
    default:
      return false;
  }
}
function isInExpressionContext(node) {
  const { parent } ***REMOVED*** node;
  switch (parent.kind) {
    case 261 /* VariableDeclaration */:
    case 170 /* Parameter */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 307 /* EnumMember */:
    case 304 /* PropertyAssignment */:
    case 209 /* BindingElement */:
      return parent.initializer ***REMOVED******REMOVED******REMOVED*** node;
    case 245 /* ExpressionStatement */:
    case 246 /* IfStatement */:
    case 247 /* DoStatement */:
    case 248 /* WhileStatement */:
    case 254 /* ReturnStatement */:
    case 255 /* WithStatement */:
    case 256 /* SwitchStatement */:
    case 297 /* CaseClause */:
    case 258 /* ThrowStatement */:
      return parent.expression ***REMOVED******REMOVED******REMOVED*** node;
    case 249 /* ForStatement */:
      const forStatement ***REMOVED*** parent;
      return forStatement.initializer ***REMOVED******REMOVED******REMOVED*** node && forStatement.initializer.kind !***REMOVED******REMOVED*** 262 /* VariableDeclarationList */ || forStatement.condition ***REMOVED******REMOVED******REMOVED*** node || forStatement.incrementor ***REMOVED******REMOVED******REMOVED*** node;
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
      const forInOrOfStatement ***REMOVED*** parent;
      return forInOrOfStatement.initializer ***REMOVED******REMOVED******REMOVED*** node && forInOrOfStatement.initializer.kind !***REMOVED******REMOVED*** 262 /* VariableDeclarationList */ || forInOrOfStatement.expression ***REMOVED******REMOVED******REMOVED*** node;
    case 217 /* TypeAssertionExpression */:
    case 235 /* AsExpression */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.expression;
    case 240 /* TemplateSpan */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.expression;
    case 168 /* ComputedPropertyName */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.expression;
    case 171 /* Decorator */:
    case 295 /* JsxExpression */:
    case 294 /* JsxSpreadAttribute */:
    case 306 /* SpreadAssignment */:
      return true;
    case 234 /* ExpressionWithTypeArguments */:
      return parent.expression ***REMOVED******REMOVED******REMOVED*** node && !isPartOfTypeNode(parent);
    case 305 /* ShorthandPropertyAssignment */:
      return parent.objectAssignmentInitializer ***REMOVED******REMOVED******REMOVED*** node;
    case 239 /* SatisfiesExpression */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.expression;
    default:
      return isExpressionNode(parent);
  }
}
function isPartOfTypeQuery(node) {
  while (node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
    node ***REMOVED*** node.parent;
  }
  return node.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */;
}
function isNamespaceReexportDeclaration(node) {
  return isNamespaceExport(node) && !!node.parent.moduleSpecifier;
}
function isExternalModuleImportEqualsDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && node.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */;
}
function getExternalModuleImportEqualsDeclarationExpression(node) {
  Debug.assert(isExternalModuleImportEqualsDeclaration(node));
  return node.moduleReference.expression;
}
function getExternalModuleRequireArgument(node) {
  return isVariableDeclarationInitializedToBareOrAccessedRequire(node) && getLeftmostAccessExpression(node.initializer).arguments[0];
}
function isInternalModuleImportEqualsDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && node.moduleReference.kind !***REMOVED******REMOVED*** 284 /* ExternalModuleReference */;
}
function isFullSourceFile(sourceFile) {
  return (sourceFile ***REMOVED******REMOVED*** null ? void 0 : sourceFile.kind) ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
}
function isSourceFileJS(file) {
  return isInJSFile(file);
}
function isInJSFile(node) {
  return !!node && !!(node.flags & 524288 /* JavaScriptFile */);
}
function isInJsonFile(node) {
  return !!node && !!(node.flags & 134217728 /* JsonFile */);
}
function isSourceFileNotJson(file) {
  return !isJsonSourceFile(file);
}
function isInJSDoc(node) {
  return !!node && !!(node.flags & 16777216 /* JSDoc */);
}
function isJSDocIndexSignature(node) {
  return isTypeReferenceNode(node) && isIdentifier(node.typeName) && node.typeName.escapedText ***REMOVED******REMOVED******REMOVED*** "Object" && node.typeArguments && node.typeArguments.length ***REMOVED******REMOVED******REMOVED*** 2 && (node.typeArguments[0].kind ***REMOVED******REMOVED******REMOVED*** 154 /* StringKeyword */ || node.typeArguments[0].kind ***REMOVED******REMOVED******REMOVED*** 150 /* NumberKeyword */);
}
function isRequireCall(callExpression, requireStringLiteralLikeArgument) {
  if (callExpression.kind !***REMOVED******REMOVED*** 214 /* CallExpression */) {
    return false;
  }
  const { expression, arguments: args } ***REMOVED*** callExpression;
  if (expression.kind !***REMOVED******REMOVED*** 80 /* Identifier */ || expression.escapedText !***REMOVED******REMOVED*** "require") {
    return false;
  }
  if (args.length !***REMOVED******REMOVED*** 1) {
    return false;
  }
  const arg ***REMOVED*** args[0];
  return !requireStringLiteralLikeArgument || isStringLiteralLike(arg);
}
function isVariableDeclarationInitializedToRequire(node) {
  return isVariableDeclarationInitializedWithRequireHelper(
    node,
    /*allowAccessedRequire*/
    false
  );
}
function isVariableDeclarationInitializedToBareOrAccessedRequire(node) {
  return isVariableDeclarationInitializedWithRequireHelper(
    node,
    /*allowAccessedRequire*/
    true
  );
}
function isBindingElementOfBareOrAccessedRequire(node) {
  return isBindingElement(node) && isVariableDeclarationInitializedToBareOrAccessedRequire(node.parent.parent);
}
function isVariableDeclarationInitializedWithRequireHelper(node, allowAccessedRequire) {
  return isVariableDeclaration(node) && !!node.initializer && isRequireCall(
    allowAccessedRequire ? getLeftmostAccessExpression(node.initializer) : node.initializer,
    /*requireStringLiteralLikeArgument*/
    true
  );
}
function isRequireVariableStatement(node) {
  return isVariableStatement(node) && node.declarationList.declarations.length > 0 && every(node.declarationList.declarations, (decl) ***REMOVED***> isVariableDeclarationInitializedToRequire(decl));
}
function isSingleOrDoubleQuote(charCode) {
  return charCode ***REMOVED******REMOVED******REMOVED*** 39 /* singleQuote */ || charCode ***REMOVED******REMOVED******REMOVED*** 34 /* doubleQuote */;
}
function isStringDoubleQuoted(str, sourceFile) {
  return getSourceTextOfNodeFromSourceFile(sourceFile, str).charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 34 /* doubleQuote */;
}
function isAssignmentDeclaration(decl) {
  return isBinaryExpression(decl) || isAccessExpression(decl) || isIdentifier(decl) || isCallExpression(decl);
}
function getEffectiveInitializer(node) {
  if (isInJSFile(node) && node.initializer && isBinaryExpression(node.initializer) && (node.initializer.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || node.initializer.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) && node.name && isEntityNameExpression(node.name) && isSameEntityName(node.name, node.initializer.left)) {
    return node.initializer.right;
  }
  return node.initializer;
}
function getDeclaredExpandoInitializer(node) {
  const init ***REMOVED*** getEffectiveInitializer(node);
  return init && getExpandoInitializer(init, isPrototypeAccess(node.name));
}
function hasExpandoValueProperty(node, isPrototypeAssignment) {
  return forEach(node.properties, (p) ***REMOVED***> isPropertyAssignment(p) && isIdentifier(p.name) && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** "value" && p.initializer && getExpandoInitializer(p.initializer, isPrototypeAssignment));
}
function getAssignedExpandoInitializer(node) {
  if (node && node.parent && isBinaryExpression(node.parent) && node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
    const isPrototypeAssignment ***REMOVED*** isPrototypeAccess(node.parent.left);
    return getExpandoInitializer(node.parent.right, isPrototypeAssignment) || getDefaultedExpandoInitializer(node.parent.left, node.parent.right, isPrototypeAssignment);
  }
  if (node && isCallExpression(node) && isBindableObjectDefinePropertyCall(node)) {
    const result ***REMOVED*** hasExpandoValueProperty(node.arguments[2], node.arguments[1].text ***REMOVED******REMOVED******REMOVED*** "prototype");
    if (result) {
      return result;
    }
  }
}
function getExpandoInitializer(initializer, isPrototypeAssignment) {
  if (isCallExpression(initializer)) {
    const e ***REMOVED*** skipParentheses(initializer.expression);
    return e.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || e.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ ? initializer : void 0;
  }
  if (initializer.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || initializer.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || initializer.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
    return initializer;
  }
  if (isObjectLiteralExpression(initializer) && (initializer.properties.length ***REMOVED******REMOVED******REMOVED*** 0 || isPrototypeAssignment)) {
    return initializer;
  }
}
function getDefaultedExpandoInitializer(name, initializer, isPrototypeAssignment) {
  const e ***REMOVED*** isBinaryExpression(initializer) && (initializer.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || initializer.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) && getExpandoInitializer(initializer.right, isPrototypeAssignment);
  if (e && isSameEntityName(name, initializer.left)) {
    return e;
  }
}
function isDefaultedExpandoInitializer(node) {
  const name ***REMOVED*** isVariableDeclaration(node.parent) ? node.parent.name : isBinaryExpression(node.parent) && node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ ? node.parent.left : void 0;
  return name && getExpandoInitializer(node.right, isPrototypeAccess(name)) && isEntityNameExpression(name) && isSameEntityName(name, node.left);
}
function getNameOfExpando(node) {
  if (isBinaryExpression(node.parent)) {
    const parent ***REMOVED*** (node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) && isBinaryExpression(node.parent.parent) ? node.parent.parent : node.parent;
    if (parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && isIdentifier(parent.left)) {
      return parent.left;
    }
  } else if (isVariableDeclaration(node.parent)) {
    return node.parent.name;
  }
}
function isSameEntityName(name, initializer) {
  if (isPropertyNameLiteral(name) && isPropertyNameLiteral(initializer)) {
    return getTextOfIdentifierOrLiteral(name) ***REMOVED******REMOVED******REMOVED*** getTextOfIdentifierOrLiteral(initializer);
  }
  if (isMemberName(name) && isLiteralLikeAccess(initializer) && (initializer.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || isIdentifier(initializer.expression) && (initializer.expression.escapedText ***REMOVED******REMOVED******REMOVED*** "window" || initializer.expression.escapedText ***REMOVED******REMOVED******REMOVED*** "self" || initializer.expression.escapedText ***REMOVED******REMOVED******REMOVED*** "global"))) {
    return isSameEntityName(name, getNameOrArgument(initializer));
  }
  if (isLiteralLikeAccess(name) && isLiteralLikeAccess(initializer)) {
    return getElementOrPropertyAccessName(name) ***REMOVED******REMOVED******REMOVED*** getElementOrPropertyAccessName(initializer) && isSameEntityName(name.expression, initializer.expression);
  }
  return false;
}
function getRightMostAssignedExpression(node) {
  while (isAssignmentExpression(
    node,
    /*excludeCompoundAssignment*/
    true
  )) {
    node ***REMOVED*** node.right;
  }
  return node;
}
function isExportsIdentifier(node) {
  return isIdentifier(node) && node.escapedText ***REMOVED******REMOVED******REMOVED*** "exports";
}
function isModuleIdentifier(node) {
  return isIdentifier(node) && node.escapedText ***REMOVED******REMOVED******REMOVED*** "module";
}
function isModuleExportsAccessExpression(node) {
  return (isPropertyAccessExpression(node) || isLiteralLikeElementAccess(node)) && isModuleIdentifier(node.expression) && getElementOrPropertyAccessName(node) ***REMOVED******REMOVED******REMOVED*** "exports";
}
function getAssignmentDeclarationKind(expr) {
  const special ***REMOVED*** getAssignmentDeclarationKindWorker(expr);
  return special ***REMOVED******REMOVED******REMOVED*** 5 /* Property */ || isInJSFile(expr) ? special : 0 /* None */;
}
function isBindableObjectDefinePropertyCall(expr) {
  return length(expr.arguments) ***REMOVED******REMOVED******REMOVED*** 3 && isPropertyAccessExpression(expr.expression) && isIdentifier(expr.expression.expression) && idText(expr.expression.expression) ***REMOVED******REMOVED******REMOVED*** "Object" && idText(expr.expression.name) ***REMOVED******REMOVED******REMOVED*** "defineProperty" && isStringOrNumericLiteralLike(expr.arguments[1]) && isBindableStaticNameExpression(
    expr.arguments[0],
    /*excludeThisKeyword*/
    true
  );
}
function isLiteralLikeAccess(node) {
  return isPropertyAccessExpression(node) || isLiteralLikeElementAccess(node);
}
function isLiteralLikeElementAccess(node) {
  return isElementAccessExpression(node) && isStringOrNumericLiteralLike(node.argumentExpression);
}
function isBindableStaticAccessExpression(node, excludeThisKeyword) {
  return isPropertyAccessExpression(node) && (!excludeThisKeyword && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || isIdentifier(node.name) && isBindableStaticNameExpression(
    node.expression,
    /*excludeThisKeyword*/
    true
  )) || isBindableStaticElementAccessExpression(node, excludeThisKeyword);
}
function isBindableStaticElementAccessExpression(node, excludeThisKeyword) {
  return isLiteralLikeElementAccess(node) && (!excludeThisKeyword && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || isEntityNameExpression(node.expression) || isBindableStaticAccessExpression(
    node.expression,
    /*excludeThisKeyword*/
    true
  ));
}
function isBindableStaticNameExpression(node, excludeThisKeyword) {
  return isEntityNameExpression(node) || isBindableStaticAccessExpression(node, excludeThisKeyword);
}
function getNameOrArgument(expr) {
  if (isPropertyAccessExpression(expr)) {
    return expr.name;
  }
  return expr.argumentExpression;
}
function getAssignmentDeclarationKindWorker(expr) {
  if (isCallExpression(expr)) {
    if (!isBindableObjectDefinePropertyCall(expr)) {
      return 0 /* None */;
    }
    const entityName ***REMOVED*** expr.arguments[0];
    if (isExportsIdentifier(entityName) || isModuleExportsAccessExpression(entityName)) {
      return 8 /* ObjectDefinePropertyExports */;
    }
    if (isBindableStaticAccessExpression(entityName) && getElementOrPropertyAccessName(entityName) ***REMOVED******REMOVED******REMOVED*** "prototype") {
      return 9 /* ObjectDefinePrototypeProperty */;
    }
    return 7 /* ObjectDefinePropertyValue */;
  }
  if (expr.operatorToken.kind !***REMOVED******REMOVED*** 64 /* EqualsToken */ || !isAccessExpression(expr.left) || isVoidZero(getRightMostAssignedExpression(expr))) {
    return 0 /* None */;
  }
  if (isBindableStaticNameExpression(
    expr.left.expression,
    /*excludeThisKeyword*/
    true
  ) && getElementOrPropertyAccessName(expr.left) ***REMOVED******REMOVED******REMOVED*** "prototype" && isObjectLiteralExpression(getInitializerOfBinaryExpression(expr))) {
    return 6 /* Prototype */;
  }
  return getAssignmentDeclarationPropertyAccessKind(expr.left);
}
function isVoidZero(node) {
  return isVoidExpression(node) && isNumericLiteral(node.expression) && node.expression.text ***REMOVED******REMOVED******REMOVED*** "0";
}
function getElementOrPropertyAccessArgumentExpressionOrName(node) {
  if (isPropertyAccessExpression(node)) {
    return node.name;
  }
  const arg ***REMOVED*** skipParentheses(node.argumentExpression);
  if (isNumericLiteral(arg) || isStringLiteralLike(arg)) {
    return arg;
  }
  return node;
}
function getElementOrPropertyAccessName(node) {
  const name ***REMOVED*** getElementOrPropertyAccessArgumentExpressionOrName(node);
  if (name) {
    if (isIdentifier(name)) {
      return name.escapedText;
    }
    if (isStringLiteralLike(name) || isNumericLiteral(name)) {
      return escapeLeadingUnderscores(name.text);
    }
  }
  return void 0;
}
function getAssignmentDeclarationPropertyAccessKind(lhs) {
  if (lhs.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
    return 4 /* ThisProperty */;
  } else if (isModuleExportsAccessExpression(lhs)) {
    return 2 /* ModuleExports */;
  } else if (isBindableStaticNameExpression(
    lhs.expression,
    /*excludeThisKeyword*/
    true
  )) {
    if (isPrototypeAccess(lhs.expression)) {
      return 3 /* PrototypeProperty */;
    }
    let nextToLast ***REMOVED*** lhs;
    while (!isIdentifier(nextToLast.expression)) {
      nextToLast ***REMOVED*** nextToLast.expression;
    }
    const id ***REMOVED*** nextToLast.expression;
    if ((id.escapedText ***REMOVED******REMOVED******REMOVED*** "exports" || id.escapedText ***REMOVED******REMOVED******REMOVED*** "module" && getElementOrPropertyAccessName(nextToLast) ***REMOVED******REMOVED******REMOVED*** "exports") && // ExportsProperty does not support binding with computed names
    isBindableStaticAccessExpression(lhs)) {
      return 1 /* ExportsProperty */;
    }
    if (isBindableStaticNameExpression(
      lhs,
      /*excludeThisKeyword*/
      true
    ) || isElementAccessExpression(lhs) && isDynamicName(lhs)) {
      return 5 /* Property */;
    }
  }
  return 0 /* None */;
}
function getInitializerOfBinaryExpression(expr) {
  while (isBinaryExpression(expr.right)) {
    expr ***REMOVED*** expr.right;
  }
  return expr.right;
}
function isPrototypePropertyAssignment(node) {
  return isBinaryExpression(node) && getAssignmentDeclarationKind(node) ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */;
}
function isSpecialPropertyDeclaration(expr) {
  return isInJSFile(expr) && expr.parent && expr.parent.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */ && (!isElementAccessExpression(expr) || isLiteralLikeElementAccess(expr)) && !!getJSDocTypeTag(expr.parent);
}
function setValueDeclaration(symbol, node) {
  const { valueDeclaration } ***REMOVED*** symbol;
  if (!valueDeclaration || !(node.flags & 33554432 /* Ambient */ && !isInJSFile(node) && !(valueDeclaration.flags & 33554432 /* Ambient */)) && (isAssignmentDeclaration(valueDeclaration) && !isAssignmentDeclaration(node)) || valueDeclaration.kind !***REMOVED******REMOVED*** node.kind && isEffectiveModuleDeclaration(valueDeclaration)) {
    symbol.valueDeclaration ***REMOVED*** node;
  }
}
function isFunctionSymbol(symbol) {
  if (!symbol || !symbol.valueDeclaration) {
    return false;
  }
  const decl ***REMOVED*** symbol.valueDeclaration;
  return decl.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || isVariableDeclaration(decl) && decl.initializer && isFunctionLike(decl.initializer);
}
function canHaveModuleSpecifier(node) {
  switch (node ***REMOVED******REMOVED*** null ? void 0 : node.kind) {
    case 261 /* VariableDeclaration */:
    case 209 /* BindingElement */:
    case 273 /* ImportDeclaration */:
    case 279 /* ExportDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
    case 274 /* ImportClause */:
    case 281 /* NamespaceExport */:
    case 275 /* NamespaceImport */:
    case 282 /* ExportSpecifier */:
    case 277 /* ImportSpecifier */:
    case 206 /* ImportType */:
      return true;
  }
  return false;
}
function tryGetModuleSpecifierFromDeclaration(node) {
  var _a, _b;
  switch (node.kind) {
    case 261 /* VariableDeclaration */:
    case 209 /* BindingElement */:
      return (_a ***REMOVED*** findAncestor(node.initializer, (node2) ***REMOVED***> isRequireCall(
        node2,
        /*requireStringLiteralLikeArgument*/
        true
      ))) ***REMOVED******REMOVED*** null ? void 0 : _a.arguments[0];
    case 273 /* ImportDeclaration */:
    case 279 /* ExportDeclaration */:
    case 352 /* JSDocImportTag */:
      return tryCast(node.moduleSpecifier, isStringLiteralLike);
    case 272 /* ImportEqualsDeclaration */:
      return tryCast((_b ***REMOVED*** tryCast(node.moduleReference, isExternalModuleReference)) ***REMOVED******REMOVED*** null ? void 0 : _b.expression, isStringLiteralLike);
    case 274 /* ImportClause */:
    case 281 /* NamespaceExport */:
      return tryCast(node.parent.moduleSpecifier, isStringLiteralLike);
    case 275 /* NamespaceImport */:
    case 282 /* ExportSpecifier */:
      return tryCast(node.parent.parent.moduleSpecifier, isStringLiteralLike);
    case 277 /* ImportSpecifier */:
      return tryCast(node.parent.parent.parent.moduleSpecifier, isStringLiteralLike);
    case 206 /* ImportType */:
      return isLiteralImportTypeNode(node) ? node.argument.literal : void 0;
    default:
      Debug.assertNever(node);
  }
}
function shouldRewriteModuleSpecifier(specifier, compilerOptions) {
  return !!compilerOptions.rewriteRelativeImportExtensions && pathIsRelative(specifier) && !isDeclarationFileName(specifier) && hasTSFileExtension(specifier);
}
function getExternalModuleName(node) {
  switch (node.kind) {
    case 273 /* ImportDeclaration */:
    case 279 /* ExportDeclaration */:
    case 352 /* JSDocImportTag */:
      return node.moduleSpecifier;
    case 272 /* ImportEqualsDeclaration */:
      return node.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */ ? node.moduleReference.expression : void 0;
    case 206 /* ImportType */:
      return isLiteralImportTypeNode(node) ? node.argument.literal : void 0;
    case 214 /* CallExpression */:
      return node.arguments[0];
    case 268 /* ModuleDeclaration */:
      return node.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? node.name : void 0;
    default:
      return Debug.assertNever(node);
  }
}
function getNamespaceDeclarationNode(node) {
  switch (node.kind) {
    case 273 /* ImportDeclaration */:
      return node.importClause && tryCast(node.importClause.namedBindings, isNamespaceImport);
    case 272 /* ImportEqualsDeclaration */:
      return node;
    case 279 /* ExportDeclaration */:
      return node.exportClause && tryCast(node.exportClause, isNamespaceExport);
    default:
      return Debug.assertNever(node);
  }
}
function isDefaultImport(node) {
  return (node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 352 /* JSDocImportTag */) && !!node.importClause && !!node.importClause.name;
}
function hasQuestionToken(node) {
  switch (node.kind) {
    case 170 /* Parameter */:
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 305 /* ShorthandPropertyAssignment */:
    case 304 /* PropertyAssignment */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
      return node.questionToken !***REMOVED******REMOVED*** void 0;
  }
  return false;
}
function isJSDocConstructSignature(node) {
  const param ***REMOVED*** isJSDocFunctionType(node) ? firstOrUndefined(node.parameters) : void 0;
  const name ***REMOVED*** tryCast(param && param.name, isIdentifier);
  return !!name && name.escapedText ***REMOVED******REMOVED******REMOVED*** "new";
}
function isJSDocTypeAlias(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 347 /* JSDocTypedefTag */ || node.kind ***REMOVED******REMOVED******REMOVED*** 339 /* JSDocCallbackTag */ || node.kind ***REMOVED******REMOVED******REMOVED*** 341 /* JSDocEnumTag */;
}
function isTypeAlias(node) {
  return isJSDocTypeAlias(node) || isTypeAliasDeclaration(node);
}
function getSourceOfAssignment(node) {
  return isExpressionStatement(node) && isBinaryExpression(node.expression) && node.expression.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ ? getRightMostAssignedExpression(node.expression) : void 0;
}
function getSourceOfDefaultedAssignment(node) {
  return isExpressionStatement(node) && isBinaryExpression(node.expression) && getAssignmentDeclarationKind(node.expression) !***REMOVED******REMOVED*** 0 /* None */ && isBinaryExpression(node.expression.right) && (node.expression.right.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || node.expression.right.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) ? node.expression.right.right : void 0;
}
function getSingleInitializerOfVariableStatementOrPropertyDeclaration(node) {
  switch (node.kind) {
    case 244 /* VariableStatement */:
      const v ***REMOVED*** getSingleVariableOfVariableStatement(node);
      return v && v.initializer;
    case 173 /* PropertyDeclaration */:
      return node.initializer;
    case 304 /* PropertyAssignment */:
      return node.initializer;
  }
}
function getSingleVariableOfVariableStatement(node) {
  return isVariableStatement(node) ? firstOrUndefined(node.declarationList.declarations) : void 0;
}
function getNestedModuleDeclaration(node) {
  return isModuleDeclaration(node) && node.body && node.body.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ ? node.body : void 0;
}
function canHaveFlowNode(node) {
  if (node.kind >***REMOVED*** 244 /* FirstStatement */ && node.kind <***REMOVED*** 260 /* LastStatement */) {
    return true;
  }
  switch (node.kind) {
    case 80 /* Identifier */:
    case 110 /* ThisKeyword */:
    case 108 /* SuperKeyword */:
    case 167 /* QualifiedName */:
    case 237 /* MetaProperty */:
    case 213 /* ElementAccessExpression */:
    case 212 /* PropertyAccessExpression */:
    case 209 /* BindingElement */:
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
      return true;
    default:
      return false;
  }
}
function canHaveJSDoc(node) {
  switch (node.kind) {
    case 220 /* ArrowFunction */:
    case 227 /* BinaryExpression */:
    case 242 /* Block */:
    case 253 /* BreakStatement */:
    case 180 /* CallSignature */:
    case 297 /* CaseClause */:
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
    case 176 /* ClassStaticBlockDeclaration */:
    case 177 /* Constructor */:
    case 186 /* ConstructorType */:
    case 181 /* ConstructSignature */:
    case 252 /* ContinueStatement */:
    case 260 /* DebuggerStatement */:
    case 247 /* DoStatement */:
    case 213 /* ElementAccessExpression */:
    case 243 /* EmptyStatement */:
    case 1 /* EndOfFileToken */:
    case 267 /* EnumDeclaration */:
    case 307 /* EnumMember */:
    case 278 /* ExportAssignment */:
    case 279 /* ExportDeclaration */:
    case 282 /* ExportSpecifier */:
    case 245 /* ExpressionStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 249 /* ForStatement */:
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 185 /* FunctionType */:
    case 178 /* GetAccessor */:
    case 80 /* Identifier */:
    case 246 /* IfStatement */:
    case 273 /* ImportDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
    case 182 /* IndexSignature */:
    case 265 /* InterfaceDeclaration */:
    case 318 /* JSDocFunctionType */:
    case 324 /* JSDocSignature */:
    case 257 /* LabeledStatement */:
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 268 /* ModuleDeclaration */:
    case 203 /* NamedTupleMember */:
    case 271 /* NamespaceExportDeclaration */:
    case 211 /* ObjectLiteralExpression */:
    case 170 /* Parameter */:
    case 218 /* ParenthesizedExpression */:
    case 212 /* PropertyAccessExpression */:
    case 304 /* PropertyAssignment */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 254 /* ReturnStatement */:
    case 241 /* SemicolonClassElement */:
    case 179 /* SetAccessor */:
    case 305 /* ShorthandPropertyAssignment */:
    case 306 /* SpreadAssignment */:
    case 256 /* SwitchStatement */:
    case 258 /* ThrowStatement */:
    case 259 /* TryStatement */:
    case 266 /* TypeAliasDeclaration */:
    case 169 /* TypeParameter */:
    case 261 /* VariableDeclaration */:
    case 244 /* VariableStatement */:
    case 248 /* WhileStatement */:
    case 255 /* WithStatement */:
      return true;
    default:
      return false;
  }
}
function getJSDocCommentsAndTags(hostNode, noCache) {
  let result;
  if (isVariableLike(hostNode) && hasInitializer(hostNode) && hasJSDocNodes(hostNode.initializer)) {
    result ***REMOVED*** addRange(result, filterOwnedJSDocTags(hostNode, hostNode.initializer.jsDoc));
  }
  let node ***REMOVED*** hostNode;
  while (node && node.parent) {
    if (hasJSDocNodes(node)) {
      result ***REMOVED*** addRange(result, filterOwnedJSDocTags(hostNode, node.jsDoc));
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
      result ***REMOVED*** addRange(result, (noCache ? getJSDocParameterTagsNoCache : getJSDocParameterTags)(node));
      break;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */) {
      result ***REMOVED*** addRange(result, (noCache ? getJSDocTypeParameterTagsNoCache : getJSDocTypeParameterTags)(node));
      break;
    }
    node ***REMOVED*** getNextJSDocCommentLocation(node);
  }
  return result || emptyArray;
}
function filterOwnedJSDocTags(hostNode, comments) {
  const lastJsDoc ***REMOVED*** last(comments);
  return flatMap(comments, (jsDoc) ***REMOVED***> {
    if (jsDoc ***REMOVED******REMOVED******REMOVED*** lastJsDoc) {
      const ownedTags ***REMOVED*** filter(jsDoc.tags, (tag) ***REMOVED***> ownsJSDocTag(hostNode, tag));
      return jsDoc.tags ***REMOVED******REMOVED******REMOVED*** ownedTags ? [jsDoc] : ownedTags;
    } else {
      return filter(jsDoc.tags, isJSDocOverloadTag);
    }
  });
}
function ownsJSDocTag(hostNode, tag) {
  return !(isJSDocTypeTag(tag) || isJSDocSatisfiesTag(tag)) || !tag.parent || !isJSDoc(tag.parent) || !isParenthesizedExpression(tag.parent.parent) || tag.parent.parent ***REMOVED******REMOVED******REMOVED*** hostNode;
}
function getNextJSDocCommentLocation(node) {
  const parent ***REMOVED*** node.parent;
  if (parent.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */ && node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */ || getNestedModuleDeclaration(parent) || isAssignmentExpression(node)) {
    return parent;
  } else if (parent.parent && (getSingleVariableOfVariableStatement(parent.parent) ***REMOVED******REMOVED******REMOVED*** node || isAssignmentExpression(parent))) {
    return parent.parent;
  } else if (parent.parent && parent.parent.parent && (getSingleVariableOfVariableStatement(parent.parent.parent) || getSingleInitializerOfVariableStatementOrPropertyDeclaration(parent.parent.parent) ***REMOVED******REMOVED******REMOVED*** node || getSourceOfDefaultedAssignment(parent.parent.parent))) {
    return parent.parent.parent;
  }
}
function getParameterSymbolFromJSDoc(node) {
  if (node.symbol) {
    return node.symbol;
  }
  if (!isIdentifier(node.name)) {
    return void 0;
  }
  const name ***REMOVED*** node.name.escapedText;
  const decl ***REMOVED*** getHostSignatureFromJSDoc(node);
  if (!decl) {
    return void 0;
  }
  const parameter ***REMOVED*** find(decl.parameters, (p) ***REMOVED***> p.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** name);
  return parameter && parameter.symbol;
}
function getEffectiveContainerForJSDocTemplateTag(node) {
  if (isJSDoc(node.parent) && node.parent.tags) {
    const typeAlias ***REMOVED*** find(node.parent.tags, isJSDocTypeAlias);
    if (typeAlias) {
      return typeAlias;
    }
  }
  return getHostSignatureFromJSDoc(node);
}
function getJSDocOverloadTags(node) {
  return getAllJSDocTags(node, isJSDocOverloadTag);
}
function getHostSignatureFromJSDoc(node) {
  const host ***REMOVED*** getEffectiveJSDocHost(node);
  if (host) {
    return isPropertySignature(host) && host.type && isFunctionLike(host.type) ? host.type : isFunctionLike(host) ? host : void 0;
  }
  return void 0;
}
function getEffectiveJSDocHost(node) {
  const host ***REMOVED*** getJSDocHost(node);
  if (host) {
    return getSourceOfDefaultedAssignment(host) || getSourceOfAssignment(host) || getSingleInitializerOfVariableStatementOrPropertyDeclaration(host) || getSingleVariableOfVariableStatement(host) || getNestedModuleDeclaration(host) || host;
  }
}
function getJSDocHost(node) {
  const jsDoc ***REMOVED*** getJSDocRoot(node);
  if (!jsDoc) {
    return void 0;
  }
  const host ***REMOVED*** jsDoc.parent;
  if (host && host.jsDoc && jsDoc ***REMOVED******REMOVED******REMOVED*** lastOrUndefined(host.jsDoc)) {
    return host;
  }
}
function getJSDocRoot(node) {
  return findAncestor(node.parent, isJSDoc);
}
function getTypeParameterFromJsDoc(node) {
  const name ***REMOVED*** node.name.escapedText;
  const { typeParameters } ***REMOVED*** node.parent.parent.parent;
  return typeParameters && find(typeParameters, (p) ***REMOVED***> p.name.escapedText ***REMOVED******REMOVED******REMOVED*** name);
}
function getAssignmentTarget(node) {
  let parent ***REMOVED*** node.parent;
  while (true) {
    switch (parent.kind) {
      case 227 /* BinaryExpression */:
        const binaryExpression ***REMOVED*** parent;
        const binaryOperator ***REMOVED*** binaryExpression.operatorToken.kind;
        return isAssignmentOperator(binaryOperator) && binaryExpression.left ***REMOVED******REMOVED******REMOVED*** node ? binaryExpression : void 0;
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        const unaryExpression ***REMOVED*** parent;
        const unaryOperator ***REMOVED*** unaryExpression.operator;
        return unaryOperator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || unaryOperator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */ ? unaryExpression : void 0;
      case 250 /* ForInStatement */:
      case 251 /* ForOfStatement */:
        const forInOrOfStatement ***REMOVED*** parent;
        return forInOrOfStatement.initializer ***REMOVED******REMOVED******REMOVED*** node ? forInOrOfStatement : void 0;
      case 218 /* ParenthesizedExpression */:
      case 210 /* ArrayLiteralExpression */:
      case 231 /* SpreadElement */:
      case 236 /* NonNullExpression */:
        node ***REMOVED*** parent;
        break;
      case 306 /* SpreadAssignment */:
        node ***REMOVED*** parent.parent;
        break;
      case 305 /* ShorthandPropertyAssignment */:
        if (parent.name !***REMOVED******REMOVED*** node) {
          return void 0;
        }
        node ***REMOVED*** parent.parent;
        break;
      case 304 /* PropertyAssignment */:
        if (parent.name ***REMOVED******REMOVED******REMOVED*** node) {
          return void 0;
        }
        node ***REMOVED*** parent.parent;
        break;
      default:
        return void 0;
    }
    parent ***REMOVED*** node.parent;
  }
}
function getAssignmentTargetKind(node) {
  const target ***REMOVED*** getAssignmentTarget(node);
  if (!target) {
    return 0 /* None */;
  }
  switch (target.kind) {
    case 227 /* BinaryExpression */:
      const binaryOperator ***REMOVED*** target.operatorToken.kind;
      return binaryOperator ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ || isLogicalOrCoalescingAssignmentOperator(binaryOperator) ? 1 /* Definite */ : 2 /* Compound */;
    case 225 /* PrefixUnaryExpression */:
    case 226 /* PostfixUnaryExpression */:
      return 2 /* Compound */;
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
      return 1 /* Definite */;
  }
}
function isAssignmentTarget(node) {
  return !!getAssignmentTarget(node);
}
function isCompoundLikeAssignment(assignment) {
  const right ***REMOVED*** skipParentheses(assignment.right);
  return right.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && isShiftOperatorOrHigher(right.operatorToken.kind);
}
function isInCompoundLikeAssignment(node) {
  const target ***REMOVED*** getAssignmentTarget(node);
  return !!target && isAssignmentExpression(
    target,
    /*excludeCompoundAssignment*/
    true
  ) && isCompoundLikeAssignment(target);
}
function isNodeWithPossibleHoistedDeclaration(node) {
  switch (node.kind) {
    case 242 /* Block */:
    case 244 /* VariableStatement */:
    case 255 /* WithStatement */:
    case 246 /* IfStatement */:
    case 256 /* SwitchStatement */:
    case 270 /* CaseBlock */:
    case 297 /* CaseClause */:
    case 298 /* DefaultClause */:
    case 257 /* LabeledStatement */:
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 247 /* DoStatement */:
    case 248 /* WhileStatement */:
    case 259 /* TryStatement */:
    case 300 /* CatchClause */:
      return true;
  }
  return false;
}
function isValueSignatureDeclaration(node) {
  return isFunctionExpression(node) || isArrowFunction(node) || isMethodOrAccessor(node) || isFunctionDeclaration(node) || isConstructorDeclaration(node);
}
function walkUp(node, kind) {
  while (node && node.kind ***REMOVED******REMOVED******REMOVED*** kind) {
    node ***REMOVED*** node.parent;
  }
  return node;
}
function walkUpParenthesizedTypes(node) {
  return walkUp(node, 197 /* ParenthesizedType */);
}
function walkUpParenthesizedExpressions(node) {
  return walkUp(node, 218 /* ParenthesizedExpression */);
}
function walkUpParenthesizedTypesAndGetParentAndChild(node) {
  let child;
  while (node && node.kind ***REMOVED******REMOVED******REMOVED*** 197 /* ParenthesizedType */) {
    child ***REMOVED*** node;
    node ***REMOVED*** node.parent;
  }
  return [child, node];
}
function skipTypeParentheses(node) {
  while (isParenthesizedTypeNode(node)) node ***REMOVED*** node.type;
  return node;
}
function skipParentheses(node, excludeJSDocTypeAssertions) {
  const flags ***REMOVED*** excludeJSDocTypeAssertions ? 1 /* Parentheses */ | -2147483648 /* ExcludeJSDocTypeAssertion */ : 1 /* Parentheses */;
  return skipOuterExpressions(node, flags);
}
function isDeleteTarget(node) {
  if (node.kind !***REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && node.kind !***REMOVED******REMOVED*** 213 /* ElementAccessExpression */) {
    return false;
  }
  node ***REMOVED*** walkUpParenthesizedExpressions(node.parent);
  return node && node.kind ***REMOVED******REMOVED******REMOVED*** 221 /* DeleteExpression */;
}
function isNodeDescendantOf(node, ancestor) {
  while (node) {
    if (node ***REMOVED******REMOVED******REMOVED*** ancestor) return true;
    node ***REMOVED*** node.parent;
  }
  return false;
}
function isDeclarationName(name) {
  return !isSourceFile(name) && !isBindingPattern(name) && isDeclaration(name.parent) && name.parent.name ***REMOVED******REMOVED******REMOVED*** name;
}
function isLiteralComputedPropertyDeclarationName(node) {
  return isStringOrNumericLiteralLike(node) && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ && isDeclaration(node.parent.parent);
}
function isIdentifierName(node) {
  const parent ***REMOVED*** node.parent;
  switch (parent.kind) {
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 175 /* MethodDeclaration */:
    case 174 /* MethodSignature */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 307 /* EnumMember */:
    case 304 /* PropertyAssignment */:
    case 212 /* PropertyAccessExpression */:
      return parent.name ***REMOVED******REMOVED******REMOVED*** node;
    case 167 /* QualifiedName */:
      return parent.right ***REMOVED******REMOVED******REMOVED*** node;
    case 209 /* BindingElement */:
    case 277 /* ImportSpecifier */:
      return parent.propertyName ***REMOVED******REMOVED******REMOVED*** node;
    case 282 /* ExportSpecifier */:
    case 292 /* JsxAttribute */:
    case 286 /* JsxSelfClosingElement */:
    case 287 /* JsxOpeningElement */:
    case 288 /* JsxClosingElement */:
      return true;
  }
  return false;
}
function getAliasDeclarationFromName(node) {
  switch (node.parent.kind) {
    case 274 /* ImportClause */:
    case 277 /* ImportSpecifier */:
    case 275 /* NamespaceImport */:
    case 282 /* ExportSpecifier */:
    case 278 /* ExportAssignment */:
    case 272 /* ImportEqualsDeclaration */:
    case 281 /* NamespaceExport */:
      return node.parent;
    case 167 /* QualifiedName */:
      do {
        node ***REMOVED*** node.parent;
      } while (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */);
      return getAliasDeclarationFromName(node);
  }
}
function isAliasableExpression(e) {
  return isEntityNameExpression(e) || isClassExpression(e);
}
function exportAssignmentIsAlias(node) {
  const e ***REMOVED*** getExportAssignmentExpression(node);
  return isAliasableExpression(e);
}
function getExportAssignmentExpression(node) {
  return isExportAssignment(node) ? node.expression : node.right;
}
function getPropertyAssignmentAliasLikeExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ ? node.name : node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? node.initializer : node.parent.right;
}
function getEffectiveBaseTypeNode(node) {
  const baseType ***REMOVED*** getClassExtendsHeritageElement(node);
  if (baseType && isInJSFile(node)) {
    const tag ***REMOVED*** getJSDocAugmentsTag(node);
    if (tag) {
      return tag.class;
    }
  }
  return baseType;
}
function getClassExtendsHeritageElement(node) {
  const heritageClause ***REMOVED*** getHeritageClause(node.heritageClauses, 96 /* ExtendsKeyword */);
  return heritageClause && heritageClause.types.length > 0 ? heritageClause.types[0] : void 0;
}
function getEffectiveImplementsTypeNodes(node) {
  if (isInJSFile(node)) {
    return getJSDocImplementsTags(node).map((n) ***REMOVED***> n.class);
  } else {
    const heritageClause ***REMOVED*** getHeritageClause(node.heritageClauses, 119 /* ImplementsKeyword */);
    return heritageClause ***REMOVED******REMOVED*** null ? void 0 : heritageClause.types;
  }
}
function getInterfaceBaseTypeNodes(node) {
  const heritageClause ***REMOVED*** getHeritageClause(node.heritageClauses, 96 /* ExtendsKeyword */);
  return heritageClause ? heritageClause.types : void 0;
}
function getHeritageClause(clauses, kind) {
  if (clauses) {
    for (const clause of clauses) {
      if (clause.token ***REMOVED******REMOVED******REMOVED*** kind) {
        return clause;
      }
    }
  }
  return void 0;
}
function getAncestor(node, kind) {
  while (node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** kind) {
      return node;
    }
    node ***REMOVED*** node.parent;
  }
  return void 0;
}
function isKeyword(token) {
  return 83 /* FirstKeyword */ <***REMOVED*** token && token <***REMOVED*** 166 /* LastKeyword */;
}
function isPunctuation(token) {
  return 19 /* FirstPunctuation */ <***REMOVED*** token && token <***REMOVED*** 79 /* LastPunctuation */;
}
function isKeywordOrPunctuation(token) {
  return isKeyword(token) || isPunctuation(token);
}
function isContextualKeyword(token) {
  return 128 /* FirstContextualKeyword */ <***REMOVED*** token && token <***REMOVED*** 166 /* LastContextualKeyword */;
}
function isNonContextualKeyword(token) {
  return isKeyword(token) && !isContextualKeyword(token);
}
function isStringANonContextualKeyword(name) {
  const token ***REMOVED*** stringToToken(name);
  return token !***REMOVED******REMOVED*** void 0 && isNonContextualKeyword(token);
}
function isIdentifierANonContextualKeyword(node) {
  const originalKeywordKind ***REMOVED*** identifierToKeywordKind(node);
  return !!originalKeywordKind && !isContextualKeyword(originalKeywordKind);
}
function getFunctionFlags(node) {
  if (!node) {
    return 4 /* Invalid */;
  }
  let flags ***REMOVED*** 0 /* Normal */;
  switch (node.kind) {
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 175 /* MethodDeclaration */:
      if (node.asteriskToken) {
        flags |***REMOVED*** 1 /* Generator */;
      }
    // falls through
    case 220 /* ArrowFunction */:
      if (hasSyntacticModifier(node, 1024 /* Async */)) {
        flags |***REMOVED*** 2 /* Async */;
      }
      break;
  }
  if (!node.body) {
    flags |***REMOVED*** 4 /* Invalid */;
  }
  return flags;
}
function isAsyncFunction(node) {
  switch (node.kind) {
    case 263 /* FunctionDeclaration */:
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
    case 175 /* MethodDeclaration */:
      return node.body !***REMOVED******REMOVED*** void 0 && node.asteriskToken ***REMOVED******REMOVED******REMOVED*** void 0 && hasSyntacticModifier(node, 1024 /* Async */);
  }
  return false;
}
function isStringOrNumericLiteralLike(node) {
  return isStringLiteralLike(node) || isNumericLiteral(node);
}
function isSignedNumericLiteral(node) {
  return isPrefixUnaryExpression(node) && (node.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) && isNumericLiteral(node.operand);
}
function hasDynamicName(declaration) {
  const name ***REMOVED*** getNameOfDeclaration(declaration);
  return !!name && isDynamicName(name);
}
function isDynamicName(name) {
  if (!(name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ || name.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */)) {
    return false;
  }
  const expr ***REMOVED*** isElementAccessExpression(name) ? skipParentheses(name.argumentExpression) : name.expression;
  return !isStringOrNumericLiteralLike(expr) && !isSignedNumericLiteral(expr);
}
function getPropertyNameForPropertyNameNode(name) {
  switch (name.kind) {
    case 80 /* Identifier */:
    case 81 /* PrivateIdentifier */:
      return name.escapedText;
    case 11 /* StringLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
      return escapeLeadingUnderscores(name.text);
    case 168 /* ComputedPropertyName */:
      const nameExpression ***REMOVED*** name.expression;
      if (isStringOrNumericLiteralLike(nameExpression)) {
        return escapeLeadingUnderscores(nameExpression.text);
      } else if (isSignedNumericLiteral(nameExpression)) {
        if (nameExpression.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
          return tokenToString(nameExpression.operator) + nameExpression.operand.text;
        }
        return nameExpression.operand.text;
      }
      return void 0;
    case 296 /* JsxNamespacedName */:
      return getEscapedTextOfJsxNamespacedName(name);
    default:
      return Debug.assertNever(name);
  }
}
function isPropertyNameLiteral(node) {
  switch (node.kind) {
    case 80 /* Identifier */:
    case 11 /* StringLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 9 /* NumericLiteral */:
      return true;
    default:
      return false;
  }
}
function getTextOfIdentifierOrLiteral(node) {
  return isMemberName(node) ? idText(node) : isJsxNamespacedName(node) ? getTextOfJsxNamespacedName(node) : node.text;
}
function getEscapedTextOfIdentifierOrLiteral(node) {
  return isMemberName(node) ? node.escapedText : isJsxNamespacedName(node) ? getEscapedTextOfJsxNamespacedName(node) : escapeLeadingUnderscores(node.text);
}
function getSymbolNameForPrivateIdentifier(containingClassSymbol, description) {
  return `__#${getSymbolId(containingClassSymbol)}@${description}`;
}
function isKnownSymbol(symbol) {
  return startsWith(symbol.escapedName, "__@");
}
function isProtoSetter(node) {
  return isIdentifier(node) ? idText(node) ***REMOVED******REMOVED******REMOVED*** "__proto__" : isStringLiteral(node) && node.text ***REMOVED******REMOVED******REMOVED*** "__proto__";
}
function isAnonymousFunctionDefinition(node, cb) {
  node ***REMOVED*** skipOuterExpressions(node);
  switch (node.kind) {
    case 232 /* ClassExpression */:
      if (classHasDeclaredOrExplicitlyAssignedName(node)) {
        return false;
      }
      break;
    case 219 /* FunctionExpression */:
      if (node.name) {
        return false;
      }
      break;
    case 220 /* ArrowFunction */:
      break;
    default:
      return false;
  }
  return typeof cb ***REMOVED******REMOVED******REMOVED*** "function" ? cb(node) : true;
}
function isNamedEvaluationSource(node) {
  switch (node.kind) {
    case 304 /* PropertyAssignment */:
      return !isProtoSetter(node.name);
    case 305 /* ShorthandPropertyAssignment */:
      return !!node.objectAssignmentInitializer;
    case 261 /* VariableDeclaration */:
      return isIdentifier(node.name) && !!node.initializer;
    case 170 /* Parameter */:
      return isIdentifier(node.name) && !!node.initializer && !node.dotDotDotToken;
    case 209 /* BindingElement */:
      return isIdentifier(node.name) && !!node.initializer && !node.dotDotDotToken;
    case 173 /* PropertyDeclaration */:
      return !!node.initializer;
    case 227 /* BinaryExpression */:
      switch (node.operatorToken.kind) {
        case 64 /* EqualsToken */:
        case 77 /* AmpersandAmpersandEqualsToken */:
        case 76 /* BarBarEqualsToken */:
        case 78 /* QuestionQuestionEqualsToken */:
          return isIdentifier(node.left);
      }
      break;
    case 278 /* ExportAssignment */:
      return true;
  }
  return false;
}
function isNamedEvaluation(node, cb) {
  if (!isNamedEvaluationSource(node)) return false;
  switch (node.kind) {
    case 304 /* PropertyAssignment */:
      return isAnonymousFunctionDefinition(node.initializer, cb);
    case 305 /* ShorthandPropertyAssignment */:
      return isAnonymousFunctionDefinition(node.objectAssignmentInitializer, cb);
    case 261 /* VariableDeclaration */:
    case 170 /* Parameter */:
    case 209 /* BindingElement */:
    case 173 /* PropertyDeclaration */:
      return isAnonymousFunctionDefinition(node.initializer, cb);
    case 227 /* BinaryExpression */:
      return isAnonymousFunctionDefinition(node.right, cb);
    case 278 /* ExportAssignment */:
      return isAnonymousFunctionDefinition(node.expression, cb);
  }
}
function isPushOrUnshiftIdentifier(node) {
  return node.escapedText ***REMOVED******REMOVED******REMOVED*** "push" || node.escapedText ***REMOVED******REMOVED******REMOVED*** "unshift";
}
function isPartOfParameterDeclaration(node) {
  const root ***REMOVED*** getRootDeclaration(node);
  return root.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */;
}
function getRootDeclaration(node) {
  while (node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */) {
    node ***REMOVED*** node.parent.parent;
  }
  return node;
}
function nodeStartsNewLexicalEnvironment(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
}
function nodeIsSynthesized(range) {
  return positionIsSynthesized(range.pos) || positionIsSynthesized(range.end);
}
function getExpressionAssociativity(expression) {
  const operator ***REMOVED*** getOperator(expression);
  const hasArguments ***REMOVED*** expression.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */ && expression.arguments !***REMOVED******REMOVED*** void 0;
  return getOperatorAssociativity(expression.kind, operator, hasArguments);
}
function getOperatorAssociativity(kind, operator, hasArguments) {
  switch (kind) {
    case 215 /* NewExpression */:
      return hasArguments ? 0 /* Left */ : 1 /* Right */;
    case 225 /* PrefixUnaryExpression */:
    case 222 /* TypeOfExpression */:
    case 223 /* VoidExpression */:
    case 221 /* DeleteExpression */:
    case 224 /* AwaitExpression */:
    case 228 /* ConditionalExpression */:
    case 230 /* YieldExpression */:
      return 1 /* Right */;
    case 227 /* BinaryExpression */:
      switch (operator) {
        case 43 /* AsteriskAsteriskToken */:
        case 64 /* EqualsToken */:
        case 65 /* PlusEqualsToken */:
        case 66 /* MinusEqualsToken */:
        case 68 /* AsteriskAsteriskEqualsToken */:
        case 67 /* AsteriskEqualsToken */:
        case 69 /* SlashEqualsToken */:
        case 70 /* PercentEqualsToken */:
        case 71 /* LessThanLessThanEqualsToken */:
        case 72 /* GreaterThanGreaterThanEqualsToken */:
        case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
        case 74 /* AmpersandEqualsToken */:
        case 79 /* CaretEqualsToken */:
        case 75 /* BarEqualsToken */:
        case 76 /* BarBarEqualsToken */:
        case 77 /* AmpersandAmpersandEqualsToken */:
        case 78 /* QuestionQuestionEqualsToken */:
          return 1 /* Right */;
      }
  }
  return 0 /* Left */;
}
function getExpressionPrecedence(expression) {
  const operator ***REMOVED*** getOperator(expression);
  const hasArguments ***REMOVED*** expression.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */ && expression.arguments !***REMOVED******REMOVED*** void 0;
  return getOperatorPrecedence(expression.kind, operator, hasArguments);
}
function getOperator(expression) {
  if (expression.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
    return expression.operatorToken.kind;
  } else if (expression.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ || expression.kind ***REMOVED******REMOVED******REMOVED*** 226 /* PostfixUnaryExpression */) {
    return expression.operator;
  } else {
    return expression.kind;
  }
}
function getOperatorPrecedence(nodeKind, operatorKind, hasArguments) {
  switch (nodeKind) {
    case 357 /* CommaListExpression */:
      return 0 /* Comma */;
    case 231 /* SpreadElement */:
      return 1 /* Spread */;
    case 230 /* YieldExpression */:
      return 2 /* Yield */;
    case 228 /* ConditionalExpression */:
      return 4 /* Conditional */;
    case 227 /* BinaryExpression */:
      switch (operatorKind) {
        case 28 /* CommaToken */:
          return 0 /* Comma */;
        case 64 /* EqualsToken */:
        case 65 /* PlusEqualsToken */:
        case 66 /* MinusEqualsToken */:
        case 68 /* AsteriskAsteriskEqualsToken */:
        case 67 /* AsteriskEqualsToken */:
        case 69 /* SlashEqualsToken */:
        case 70 /* PercentEqualsToken */:
        case 71 /* LessThanLessThanEqualsToken */:
        case 72 /* GreaterThanGreaterThanEqualsToken */:
        case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
        case 74 /* AmpersandEqualsToken */:
        case 79 /* CaretEqualsToken */:
        case 75 /* BarEqualsToken */:
        case 76 /* BarBarEqualsToken */:
        case 77 /* AmpersandAmpersandEqualsToken */:
        case 78 /* QuestionQuestionEqualsToken */:
          return 3 /* Assignment */;
        default:
          return getBinaryOperatorPrecedence(operatorKind);
      }
    // TODO: Should prefix `++` and `--` be moved to the `Update` precedence?
    case 217 /* TypeAssertionExpression */:
    case 236 /* NonNullExpression */:
    case 225 /* PrefixUnaryExpression */:
    case 222 /* TypeOfExpression */:
    case 223 /* VoidExpression */:
    case 221 /* DeleteExpression */:
    case 224 /* AwaitExpression */:
      return 16 /* Unary */;
    case 226 /* PostfixUnaryExpression */:
      return 17 /* Update */;
    case 214 /* CallExpression */:
      return 18 /* LeftHandSide */;
    case 215 /* NewExpression */:
      return hasArguments ? 19 /* Member */ : 18 /* LeftHandSide */;
    case 216 /* TaggedTemplateExpression */:
    case 212 /* PropertyAccessExpression */:
    case 213 /* ElementAccessExpression */:
    case 237 /* MetaProperty */:
      return 19 /* Member */;
    case 235 /* AsExpression */:
    case 239 /* SatisfiesExpression */:
      return 11 /* Relational */;
    case 110 /* ThisKeyword */:
    case 108 /* SuperKeyword */:
    case 80 /* Identifier */:
    case 81 /* PrivateIdentifier */:
    case 106 /* NullKeyword */:
    case 112 /* TrueKeyword */:
    case 97 /* FalseKeyword */:
    case 9 /* NumericLiteral */:
    case 10 /* BigIntLiteral */:
    case 11 /* StringLiteral */:
    case 210 /* ArrayLiteralExpression */:
    case 211 /* ObjectLiteralExpression */:
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
    case 232 /* ClassExpression */:
    case 14 /* RegularExpressionLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 229 /* TemplateExpression */:
    case 218 /* ParenthesizedExpression */:
    case 233 /* OmittedExpression */:
    case 285 /* JsxElement */:
    case 286 /* JsxSelfClosingElement */:
    case 289 /* JsxFragment */:
      return 20 /* Primary */;
    default:
      return -1 /* Invalid */;
  }
}
function getBinaryOperatorPrecedence(kind) {
  switch (kind) {
    case 61 /* QuestionQuestionToken */:
      return 5 /* Coalesce */;
    case 57 /* BarBarToken */:
      return 5 /* LogicalOR */;
    case 56 /* AmpersandAmpersandToken */:
      return 6 /* LogicalAND */;
    case 52 /* BarToken */:
      return 7 /* BitwiseOR */;
    case 53 /* CaretToken */:
      return 8 /* BitwiseXOR */;
    case 51 /* AmpersandToken */:
      return 9 /* BitwiseAND */;
    case 35 /* EqualsEqualsToken */:
    case 36 /* ExclamationEqualsToken */:
    case 37 /* EqualsEqualsEqualsToken */:
    case 38 /* ExclamationEqualsEqualsToken */:
      return 10 /* Equality */;
    case 30 /* LessThanToken */:
    case 32 /* GreaterThanToken */:
    case 33 /* LessThanEqualsToken */:
    case 34 /* GreaterThanEqualsToken */:
    case 104 /* InstanceOfKeyword */:
    case 103 /* InKeyword */:
    case 130 /* AsKeyword */:
    case 152 /* SatisfiesKeyword */:
      return 11 /* Relational */;
    case 48 /* LessThanLessThanToken */:
    case 49 /* GreaterThanGreaterThanToken */:
    case 50 /* GreaterThanGreaterThanGreaterThanToken */:
      return 12 /* Shift */;
    case 40 /* PlusToken */:
    case 41 /* MinusToken */:
      return 13 /* Additive */;
    case 42 /* AsteriskToken */:
    case 44 /* SlashToken */:
    case 45 /* PercentToken */:
      return 14 /* Multiplicative */;
    case 43 /* AsteriskAsteriskToken */:
      return 15 /* Exponentiation */;
  }
  return -1;
}
function getSemanticJsxChildren(children) {
  return filter(children, (i) ***REMOVED***> {
    switch (i.kind) {
      case 295 /* JsxExpression */:
        return !!i.expression;
      case 12 /* JsxText */:
        return !i.containsOnlyTriviaWhiteSpaces;
      default:
        return true;
    }
  });
}
function createDiagnosticCollection() {
  let nonFileDiagnostics ***REMOVED*** [];
  const filesWithDiagnostics ***REMOVED*** [];
  const fileDiagnostics ***REMOVED*** /* @__PURE__ */ new Map();
  let hasReadNonFileDiagnostics ***REMOVED*** false;
  return {
    add,
    lookup,
    getGlobalDiagnostics,
    getDiagnostics
  };
  function lookup(diagnostic) {
    let diagnostics;
    if (diagnostic.file) {
      diagnostics ***REMOVED*** fileDiagnostics.get(diagnostic.file.fileName);
    } else {
      diagnostics ***REMOVED*** nonFileDiagnostics;
    }
    if (!diagnostics) {
      return void 0;
    }
    const result ***REMOVED*** binarySearch(diagnostics, diagnostic, identity, compareDiagnosticsSkipRelatedInformation);
    if (result >***REMOVED*** 0) {
      return diagnostics[result];
    }
    if (~result > 0 && diagnosticsEqualityComparer(diagnostic, diagnostics[~result - 1])) {
      return diagnostics[~result - 1];
    }
    return void 0;
  }
  function add(diagnostic) {
    let diagnostics;
    if (diagnostic.file) {
      diagnostics ***REMOVED*** fileDiagnostics.get(diagnostic.file.fileName);
      if (!diagnostics) {
        diagnostics ***REMOVED*** [];
        fileDiagnostics.set(diagnostic.file.fileName, diagnostics);
        insertSorted(filesWithDiagnostics, diagnostic.file.fileName, compareStringsCaseSensitive);
      }
    } else {
      if (hasReadNonFileDiagnostics) {
        hasReadNonFileDiagnostics ***REMOVED*** false;
        nonFileDiagnostics ***REMOVED*** nonFileDiagnostics.slice();
      }
      diagnostics ***REMOVED*** nonFileDiagnostics;
    }
    insertSorted(diagnostics, diagnostic, compareDiagnosticsSkipRelatedInformation, diagnosticsEqualityComparer);
  }
  function getGlobalDiagnostics() {
    hasReadNonFileDiagnostics ***REMOVED*** true;
    return nonFileDiagnostics;
  }
  function getDiagnostics(fileName) {
    if (fileName) {
      return fileDiagnostics.get(fileName) || [];
    }
    const fileDiags ***REMOVED*** flatMapToMutable(filesWithDiagnostics, (f) ***REMOVED***> fileDiagnostics.get(f));
    if (!nonFileDiagnostics.length) {
      return fileDiags;
    }
    fileDiags.unshift(...nonFileDiagnostics);
    return fileDiags;
  }
}
var templateSubstitutionRegExp ***REMOVED*** /\$\{/g;
function escapeTemplateSubstitution(str) {
  return str.replace(templateSubstitutionRegExp, "\\${");
}
function containsInvalidEscapeFlag(node) {
  return !!((node.templateFlags || 0) & 2048 /* ContainsInvalidEscape */);
}
function hasInvalidEscape(template) {
  return template && !!(isNoSubstitutionTemplateLiteral(template) ? containsInvalidEscapeFlag(template) : containsInvalidEscapeFlag(template.head) || some(template.templateSpans, (span) ***REMOVED***> containsInvalidEscapeFlag(span.literal)));
}
var doubleQuoteEscapedCharsRegExp ***REMOVED*** /[\\"\u0000-\u001f\u2028\u2029\u0085]/g;
var singleQuoteEscapedCharsRegExp ***REMOVED*** /[\\'\u0000-\u001f\u2028\u2029\u0085]/g;
var backtickQuoteEscapedCharsRegExp ***REMOVED*** /\r\n|[\\`\u0000-\u0009\u000b-\u001f\u2028\u2029\u0085]/g;
var escapedCharsMap ***REMOVED*** new Map(Object.entries({
  "	": "\\t",
  "\v": "\\v",
  "\f": "\\f",
  "\b": "\\b",
  "\r": "\\r",
  "\n": "\\n",
  "\\": "\\\\",
  '"': '\\"',
  "'": "\\'",
  "`": "\\`",
  "\u2028": "\\u2028",
  // lineSeparator
  "\u2029": "\\u2029",
  // paragraphSeparator
  "\x85": "\\u0085",
  // nextLine
  "\r\n": "\\r\\n"
  // special case for CRLFs in backticks
}));
function encodeUtf16EscapeSequence(charCode) {
  const hexCharCode ***REMOVED*** charCode.toString(16).toUpperCase();
  const paddedHexCode ***REMOVED*** ("0000" + hexCharCode).slice(-4);
  return "\\u" + paddedHexCode;
}
function getReplacement(c, offset, input) {
  if (c.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 0 /* nullCharacter */) {
    const lookAhead ***REMOVED*** input.charCodeAt(offset + c.length);
    if (lookAhead >***REMOVED*** 48 /* _0 */ && lookAhead <***REMOVED*** 57 /* _9 */) {
      return "\\x00";
    }
    return "\\0";
  }
  return escapedCharsMap.get(c) || encodeUtf16EscapeSequence(c.charCodeAt(0));
}
function escapeString(s, quoteChar) {
  const escapedCharsRegExp ***REMOVED*** quoteChar ***REMOVED******REMOVED******REMOVED*** 96 /* backtick */ ? backtickQuoteEscapedCharsRegExp : quoteChar ***REMOVED******REMOVED******REMOVED*** 39 /* singleQuote */ ? singleQuoteEscapedCharsRegExp : doubleQuoteEscapedCharsRegExp;
  return s.replace(escapedCharsRegExp, getReplacement);
}
var nonAsciiCharacters ***REMOVED*** /[^\u0000-\u007F]/g;
function escapeNonAsciiString(s, quoteChar) {
  s ***REMOVED*** escapeString(s, quoteChar);
  return nonAsciiCharacters.test(s) ? s.replace(nonAsciiCharacters, (c) ***REMOVED***> encodeUtf16EscapeSequence(c.charCodeAt(0))) : s;
}
var jsxDoubleQuoteEscapedCharsRegExp ***REMOVED*** /["\u0000-\u001f\u2028\u2029\u0085]/g;
var jsxSingleQuoteEscapedCharsRegExp ***REMOVED*** /['\u0000-\u001f\u2028\u2029\u0085]/g;
var jsxEscapedCharsMap ***REMOVED*** new Map(Object.entries({
  '"': "&quot;",
  "'": "&apos;"
}));
function encodeJsxCharacterEntity(charCode) {
  const hexCharCode ***REMOVED*** charCode.toString(16).toUpperCase();
  return "&#x" + hexCharCode + ";";
}
function getJsxAttributeStringReplacement(c) {
  if (c.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 0 /* nullCharacter */) {
    return "&#0;";
  }
  return jsxEscapedCharsMap.get(c) || encodeJsxCharacterEntity(c.charCodeAt(0));
}
function escapeJsxAttributeString(s, quoteChar) {
  const escapedCharsRegExp ***REMOVED*** quoteChar ***REMOVED******REMOVED******REMOVED*** 39 /* singleQuote */ ? jsxSingleQuoteEscapedCharsRegExp : jsxDoubleQuoteEscapedCharsRegExp;
  return s.replace(escapedCharsRegExp, getJsxAttributeStringReplacement);
}
function stripQuotes(name) {
  const length2 ***REMOVED*** name.length;
  if (length2 >***REMOVED*** 2 && name.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** name.charCodeAt(length2 - 1) && isQuoteOrBacktick(name.charCodeAt(0))) {
    return name.substring(1, length2 - 1);
  }
  return name;
}
function isQuoteOrBacktick(charCode) {
  return charCode ***REMOVED******REMOVED******REMOVED*** 39 /* singleQuote */ || charCode ***REMOVED******REMOVED******REMOVED*** 34 /* doubleQuote */ || charCode ***REMOVED******REMOVED******REMOVED*** 96 /* backtick */;
}
function isIntrinsicJsxName(name) {
  const ch ***REMOVED*** name.charCodeAt(0);
  return ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 122 /* z */ || name.includes("-");
}
var indentStrings ***REMOVED*** ["", "    "];
function getIndentString(level) {
  const singleLevel ***REMOVED*** indentStrings[1];
  for (let current ***REMOVED*** indentStrings.length; current <***REMOVED*** level; current++) {
    indentStrings.push(indentStrings[current - 1] + singleLevel);
  }
  return indentStrings[level];
}
function getIndentSize() {
  return indentStrings[1].length;
}
function createTextWriter(newLine) {
  var output;
  var indent2;
  var lineStart;
  var lineCount;
  var linePos;
  var hasTrailingComment ***REMOVED*** false;
  function updateLineCountAndPosFor(s) {
    const lineStartsOfS ***REMOVED*** computeLineStarts(s);
    if (lineStartsOfS.length > 1) {
      lineCount ***REMOVED*** lineCount + lineStartsOfS.length - 1;
      linePos ***REMOVED*** output.length - s.length + last(lineStartsOfS);
      lineStart ***REMOVED*** linePos - output.length ***REMOVED******REMOVED******REMOVED*** 0;
    } else {
      lineStart ***REMOVED*** false;
    }
  }
  function writeText(s) {
    if (s && s.length) {
      if (lineStart) {
        s ***REMOVED*** getIndentString(indent2) + s;
        lineStart ***REMOVED*** false;
      }
      output +***REMOVED*** s;
      updateLineCountAndPosFor(s);
    }
  }
  function write(s) {
    if (s) hasTrailingComment ***REMOVED*** false;
    writeText(s);
  }
  function writeComment(s) {
    if (s) hasTrailingComment ***REMOVED*** true;
    writeText(s);
  }
  function reset() {
    output ***REMOVED*** "";
    indent2 ***REMOVED*** 0;
    lineStart ***REMOVED*** true;
    lineCount ***REMOVED*** 0;
    linePos ***REMOVED*** 0;
    hasTrailingComment ***REMOVED*** false;
  }
  function rawWrite(s) {
    if (s !***REMOVED******REMOVED*** void 0) {
      output +***REMOVED*** s;
      updateLineCountAndPosFor(s);
      hasTrailingComment ***REMOVED*** false;
    }
  }
  function writeLiteral(s) {
    if (s && s.length) {
      write(s);
    }
  }
  function writeLine(force) {
    if (!lineStart || force) {
      output +***REMOVED*** newLine;
      lineCount++;
      linePos ***REMOVED*** output.length;
      lineStart ***REMOVED*** true;
      hasTrailingComment ***REMOVED*** false;
    }
  }
  reset();
  return {
    write,
    rawWrite,
    writeLiteral,
    writeLine,
    increaseIndent: () ***REMOVED***> {
      indent2++;
    },
    decreaseIndent: () ***REMOVED***> {
      indent2--;
    },
    getIndent: () ***REMOVED***> indent2,
    getTextPos: () ***REMOVED***> output.length,
    getLine: () ***REMOVED***> lineCount,
    getColumn: () ***REMOVED***> lineStart ? indent2 * getIndentSize() : output.length - linePos,
    getText: () ***REMOVED***> output,
    isAtStartOfLine: () ***REMOVED***> lineStart,
    hasTrailingComment: () ***REMOVED***> hasTrailingComment,
    hasTrailingWhitespace: () ***REMOVED***> !!output.length && isWhiteSpaceLike(output.charCodeAt(output.length - 1)),
    clear: reset,
    writeKeyword: write,
    writeOperator: write,
    writeParameter: write,
    writeProperty: write,
    writePunctuation: write,
    writeSpace: write,
    writeStringLiteral: write,
    writeSymbol: (s, _) ***REMOVED***> write(s),
    writeTrailingSemicolon: write,
    writeComment
  };
}
function getTrailingSemicolonDeferringWriter(writer) {
  let pendingTrailingSemicolon ***REMOVED*** false;
  function commitPendingTrailingSemicolon() {
    if (pendingTrailingSemicolon) {
      writer.writeTrailingSemicolon(";");
      pendingTrailingSemicolon ***REMOVED*** false;
    }
  }
  return {
    ...writer,
    writeTrailingSemicolon() {
      pendingTrailingSemicolon ***REMOVED*** true;
    },
    writeLiteral(s) {
      commitPendingTrailingSemicolon();
      writer.writeLiteral(s);
    },
    writeStringLiteral(s) {
      commitPendingTrailingSemicolon();
      writer.writeStringLiteral(s);
    },
    writeSymbol(s, sym) {
      commitPendingTrailingSemicolon();
      writer.writeSymbol(s, sym);
    },
    writePunctuation(s) {
      commitPendingTrailingSemicolon();
      writer.writePunctuation(s);
    },
    writeKeyword(s) {
      commitPendingTrailingSemicolon();
      writer.writeKeyword(s);
    },
    writeOperator(s) {
      commitPendingTrailingSemicolon();
      writer.writeOperator(s);
    },
    writeParameter(s) {
      commitPendingTrailingSemicolon();
      writer.writeParameter(s);
    },
    writeSpace(s) {
      commitPendingTrailingSemicolon();
      writer.writeSpace(s);
    },
    writeProperty(s) {
      commitPendingTrailingSemicolon();
      writer.writeProperty(s);
    },
    writeComment(s) {
      commitPendingTrailingSemicolon();
      writer.writeComment(s);
    },
    writeLine() {
      commitPendingTrailingSemicolon();
      writer.writeLine();
    },
    increaseIndent() {
      commitPendingTrailingSemicolon();
      writer.increaseIndent();
    },
    decreaseIndent() {
      commitPendingTrailingSemicolon();
      writer.decreaseIndent();
    }
  };
}
function hostUsesCaseSensitiveFileNames(host) {
  return host.useCaseSensitiveFileNames ? host.useCaseSensitiveFileNames() : false;
}
function hostGetCanonicalFileName(host) {
  return createGetCanonicalFileName(hostUsesCaseSensitiveFileNames(host));
}
function getResolvedExternalModuleName(host, file, referenceFile) {
  return file.moduleName || getExternalModuleNameFromPath(host, file.fileName, referenceFile && referenceFile.fileName);
}
function getCanonicalAbsolutePath(host, path) {
  return host.getCanonicalFileName(getNormalizedAbsolutePath(path, host.getCurrentDirectory()));
}
function getExternalModuleNameFromDeclaration(host, resolver, declaration) {
  const file ***REMOVED*** resolver.getExternalModuleFileFromDeclaration(declaration);
  if (!file || file.isDeclarationFile) {
    return void 0;
  }
  const specifier ***REMOVED*** getExternalModuleName(declaration);
  if (specifier && isStringLiteralLike(specifier) && !pathIsRelative(specifier.text) && !getCanonicalAbsolutePath(host, file.path).includes(getCanonicalAbsolutePath(host, ensureTrailingDirectorySeparator(host.getCommonSourceDirectory())))) {
    return void 0;
  }
  return getResolvedExternalModuleName(host, file);
}
function getExternalModuleNameFromPath(host, fileName, referencePath) {
  const getCanonicalFileName ***REMOVED*** (f) ***REMOVED***> host.getCanonicalFileName(f);
  const dir ***REMOVED*** toPath(referencePath ? getDirectoryPath(referencePath) : host.getCommonSourceDirectory(), host.getCurrentDirectory(), getCanonicalFileName);
  const filePath ***REMOVED*** getNormalizedAbsolutePath(fileName, host.getCurrentDirectory());
  const relativePath ***REMOVED*** getRelativePathToDirectoryOrUrl(
    dir,
    filePath,
    dir,
    getCanonicalFileName,
    /*isAbsolutePathAnUrl*/
    false
  );
  const extensionless ***REMOVED*** removeFileExtension(relativePath);
  return referencePath ? ensurePathIsNonModuleName(extensionless) : extensionless;
}
function getOwnEmitOutputFilePath(fileName, host, extension) {
  const compilerOptions ***REMOVED*** host.getCompilerOptions();
  let emitOutputFilePathWithoutExtension;
  if (compilerOptions.outDir) {
    emitOutputFilePathWithoutExtension ***REMOVED*** removeFileExtension(getSourceFilePathInNewDir(fileName, host, compilerOptions.outDir));
  } else {
    emitOutputFilePathWithoutExtension ***REMOVED*** removeFileExtension(fileName);
  }
  return emitOutputFilePathWithoutExtension + extension;
}
function getDeclarationEmitOutputFilePath(fileName, host) {
  return getDeclarationEmitOutputFilePathWorker(fileName, host.getCompilerOptions(), host);
}
function getDeclarationEmitOutputFilePathWorker(fileName, options, host) {
  const outputDir ***REMOVED*** options.declarationDir || options.outDir;
  const path ***REMOVED*** outputDir ? getSourceFilePathInNewDirWorker(fileName, outputDir, host.getCurrentDirectory(), host.getCommonSourceDirectory(), (f) ***REMOVED***> host.getCanonicalFileName(f)) : fileName;
  const declarationExtension ***REMOVED*** getDeclarationEmitExtensionForPath(path);
  return removeFileExtension(path) + declarationExtension;
}
function getDeclarationEmitExtensionForPath(path) {
  return fileExtensionIsOneOf(path, [".mjs" /* Mjs */, ".mts" /* Mts */]) ? ".d.mts" /* Dmts */ : fileExtensionIsOneOf(path, [".cjs" /* Cjs */, ".cts" /* Cts */]) ? ".d.cts" /* Dcts */ : fileExtensionIsOneOf(path, [".json" /* Json */]) ? `.d.json.ts` : (
    // Drive-by redefinition of json declaration file output name so if it's ever enabled, it behaves well
    ".d.ts" /* Dts */
  );
}
function getPossibleOriginalInputExtensionForExtension(path) {
  return fileExtensionIsOneOf(path, [".d.mts" /* Dmts */, ".mjs" /* Mjs */, ".mts" /* Mts */]) ? [".mts" /* Mts */, ".mjs" /* Mjs */] : fileExtensionIsOneOf(path, [".d.cts" /* Dcts */, ".cjs" /* Cjs */, ".cts" /* Cts */]) ? [".cts" /* Cts */, ".cjs" /* Cjs */] : fileExtensionIsOneOf(path, [`.d.json.ts`]) ? [".json" /* Json */] : [".tsx" /* Tsx */, ".ts" /* Ts */, ".jsx" /* Jsx */, ".js" /* Js */];
}
function getPathsBasePath(options, host) {
  var _a;
  if (!options.paths) return void 0;
  return options.baseUrl ?? Debug.checkDefined(options.pathsBasePath || ((_a ***REMOVED*** host.getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host)), "Encountered 'paths' without a 'baseUrl', config file, or host 'getCurrentDirectory'.");
}
function getSourceFilesToEmit(host, targetSourceFile, forceDtsEmit) {
  const options ***REMOVED*** host.getCompilerOptions();
  if (options.outFile) {
    const moduleKind ***REMOVED*** getEmitModuleKind(options);
    const moduleEmitEnabled ***REMOVED*** options.emitDeclarationOnly || moduleKind ***REMOVED******REMOVED******REMOVED*** 2 /* AMD */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 4 /* System */;
    return filter(
      host.getSourceFiles(),
      (sourceFile) ***REMOVED***> (moduleEmitEnabled || !isExternalModule(sourceFile)) && sourceFileMayBeEmitted(sourceFile, host, forceDtsEmit)
    );
  } else {
    const sourceFiles ***REMOVED*** targetSourceFile ***REMOVED******REMOVED******REMOVED*** void 0 ? host.getSourceFiles() : [targetSourceFile];
    return filter(
      sourceFiles,
      (sourceFile) ***REMOVED***> sourceFileMayBeEmitted(sourceFile, host, forceDtsEmit)
    );
  }
}
function sourceFileMayBeEmitted(sourceFile, host, forceDtsEmit) {
  const options ***REMOVED*** host.getCompilerOptions();
  if (options.noEmitForJsFiles && isSourceFileJS(sourceFile)) return false;
  if (sourceFile.isDeclarationFile) return false;
  if (host.isSourceFileFromExternalLibrary(sourceFile)) return false;
  if (forceDtsEmit) return true;
  if (host.isSourceOfProjectReferenceRedirect(sourceFile.fileName)) return false;
  if (!isJsonSourceFile(sourceFile)) return true;
  if (host.getRedirectFromSourceFile(sourceFile.fileName)) return false;
  if (options.outFile) return true;
  if (!options.outDir) return false;
  if (options.rootDir || options.composite && options.configFilePath) {
    const commonDir ***REMOVED*** getNormalizedAbsolutePath(getCommonSourceDirectory(options, () ***REMOVED***> [], host.getCurrentDirectory(), host.getCanonicalFileName), host.getCurrentDirectory());
    const outputPath ***REMOVED*** getSourceFilePathInNewDirWorker(sourceFile.fileName, options.outDir, host.getCurrentDirectory(), commonDir, host.getCanonicalFileName);
    if (comparePaths(sourceFile.fileName, outputPath, host.getCurrentDirectory(), !host.useCaseSensitiveFileNames()) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */) return false;
  }
  return true;
}
function getSourceFilePathInNewDir(fileName, host, newDirPath) {
  return getSourceFilePathInNewDirWorker(fileName, newDirPath, host.getCurrentDirectory(), host.getCommonSourceDirectory(), (f) ***REMOVED***> host.getCanonicalFileName(f));
}
function getSourceFilePathInNewDirWorker(fileName, newDirPath, currentDirectory, commonSourceDirectory, getCanonicalFileName) {
  let sourceFilePath ***REMOVED*** getNormalizedAbsolutePath(fileName, currentDirectory);
  const isSourceFileInCommonSourceDirectory ***REMOVED*** getCanonicalFileName(sourceFilePath).indexOf(getCanonicalFileName(commonSourceDirectory)) ***REMOVED******REMOVED******REMOVED*** 0;
  sourceFilePath ***REMOVED*** isSourceFileInCommonSourceDirectory ? sourceFilePath.substring(commonSourceDirectory.length) : sourceFilePath;
  return combinePaths(newDirPath, sourceFilePath);
}
function writeFile(host, diagnostics, fileName, text, writeByteOrderMark, sourceFiles, data) {
  host.writeFile(
    fileName,
    text,
    writeByteOrderMark,
    (hostErrorMessage) ***REMOVED***> {
      diagnostics.add(createCompilerDiagnostic(Diagnostics.Could_not_write_file_0_Colon_1, fileName, hostErrorMessage));
    },
    sourceFiles,
    data
  );
}
function ensureDirectoriesExist(directoryPath, createDirectory, directoryExists) {
  if (directoryPath.length > getRootLength(directoryPath) && !directoryExists(directoryPath)) {
    const parentDirectory ***REMOVED*** getDirectoryPath(directoryPath);
    ensureDirectoriesExist(parentDirectory, createDirectory, directoryExists);
    createDirectory(directoryPath);
  }
}
function writeFileEnsuringDirectories(path, data, writeByteOrderMark, writeFile2, createDirectory, directoryExists) {
  try {
    writeFile2(path, data, writeByteOrderMark);
  } catch {
    ensureDirectoriesExist(getDirectoryPath(normalizePath(path)), createDirectory, directoryExists);
    writeFile2(path, data, writeByteOrderMark);
  }
}
function getLineOfLocalPositionFromLineMap(lineMap, pos) {
  return computeLineOfPosition(lineMap, pos);
}
function getFirstConstructorWithBody(node) {
  return find(node.members, (member) ***REMOVED***> isConstructorDeclaration(member) && nodeIsPresent(member.body));
}
function getSetAccessorValueParameter(accessor) {
  if (accessor && accessor.parameters.length > 0) {
    const hasThis ***REMOVED*** accessor.parameters.length ***REMOVED******REMOVED******REMOVED*** 2 && parameterIsThisKeyword(accessor.parameters[0]);
    return accessor.parameters[hasThis ? 1 : 0];
  }
}
function getSetAccessorTypeAnnotationNode(accessor) {
  const parameter ***REMOVED*** getSetAccessorValueParameter(accessor);
  return parameter && parameter.type;
}
function getThisParameter(signature) {
  if (signature.parameters.length && !isJSDocSignature(signature)) {
    const thisParameter ***REMOVED*** signature.parameters[0];
    if (parameterIsThisKeyword(thisParameter)) {
      return thisParameter;
    }
  }
}
function parameterIsThisKeyword(parameter) {
  return isThisIdentifier(parameter.name);
}
function isThisIdentifier(node) {
  return !!node && node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && identifierIsThisKeyword(node);
}
function isInTypeQuery(node) {
  return !!findAncestor(
    node,
    (n) ***REMOVED***> n.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ ? true : n.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || n.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? false : "quit"
  );
}
function isThisInTypeQuery(node) {
  if (!isThisIdentifier(node)) {
    return false;
  }
  while (isQualifiedName(node.parent) && node.parent.left ***REMOVED******REMOVED******REMOVED*** node) {
    node ***REMOVED*** node.parent;
  }
  return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */;
}
function identifierIsThisKeyword(id) {
  return id.escapedText ***REMOVED******REMOVED******REMOVED*** "this";
}
function getAllAccessorDeclarations(declarations, accessor) {
  let firstAccessor;
  let secondAccessor;
  let getAccessor;
  let setAccessor;
  if (hasDynamicName(accessor)) {
    firstAccessor ***REMOVED*** accessor;
    if (accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
      getAccessor ***REMOVED*** accessor;
    } else if (accessor.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
      setAccessor ***REMOVED*** accessor;
    } else {
      Debug.fail("Accessor has wrong kind");
    }
  } else {
    forEach(declarations, (member) ***REMOVED***> {
      if (isAccessor(member) && isStatic(member) ***REMOVED******REMOVED******REMOVED*** isStatic(accessor)) {
        const memberName ***REMOVED*** getPropertyNameForPropertyNameNode(member.name);
        const accessorName ***REMOVED*** getPropertyNameForPropertyNameNode(accessor.name);
        if (memberName ***REMOVED******REMOVED******REMOVED*** accessorName) {
          if (!firstAccessor) {
            firstAccessor ***REMOVED*** member;
          } else if (!secondAccessor) {
            secondAccessor ***REMOVED*** member;
          }
          if (member.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ && !getAccessor) {
            getAccessor ***REMOVED*** member;
          }
          if (member.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ && !setAccessor) {
            setAccessor ***REMOVED*** member;
          }
        }
      }
    });
  }
  return {
    firstAccessor,
    secondAccessor,
    getAccessor,
    setAccessor
  };
}
function getEffectiveTypeAnnotationNode(node) {
  if (!isInJSFile(node) && isFunctionDeclaration(node)) return void 0;
  if (isTypeAliasDeclaration(node)) return void 0;
  const type ***REMOVED*** node.type;
  if (type || !isInJSFile(node)) return type;
  return isJSDocPropertyLikeTag(node) ? node.typeExpression && node.typeExpression.type : getJSDocType(node);
}
function getEffectiveReturnTypeNode(node) {
  return isJSDocSignature(node) ? node.type && node.type.typeExpression && node.type.typeExpression.type : node.type || (isInJSFile(node) ? getJSDocReturnType(node) : void 0);
}
function getJSDocTypeParameterDeclarations(node) {
  return flatMap(getJSDocTags(node), (tag) ***REMOVED***> isNonTypeAliasTemplate(tag) ? tag.typeParameters : void 0);
}
function isNonTypeAliasTemplate(tag) {
  return isJSDocTemplateTag(tag) && !(tag.parent.kind ***REMOVED******REMOVED******REMOVED*** 321 /* JSDoc */ && (tag.parent.tags.some(isJSDocTypeAlias) || tag.parent.tags.some(isJSDocOverloadTag)));
}
function getEffectiveSetAccessorTypeAnnotationNode(node) {
  const parameter ***REMOVED*** getSetAccessorValueParameter(node);
  return parameter && getEffectiveTypeAnnotationNode(parameter);
}
function emitNewLineBeforeLeadingComments(lineMap, writer, node, leadingComments) {
  emitNewLineBeforeLeadingCommentsOfPosition(lineMap, writer, node.pos, leadingComments);
}
function emitNewLineBeforeLeadingCommentsOfPosition(lineMap, writer, pos, leadingComments) {
  if (leadingComments && leadingComments.length && pos !***REMOVED******REMOVED*** leadingComments[0].pos && getLineOfLocalPositionFromLineMap(lineMap, pos) !***REMOVED******REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, leadingComments[0].pos)) {
    writer.writeLine();
  }
}
function emitNewLineBeforeLeadingCommentOfPosition(lineMap, writer, pos, commentPos) {
  if (pos !***REMOVED******REMOVED*** commentPos && getLineOfLocalPositionFromLineMap(lineMap, pos) !***REMOVED******REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, commentPos)) {
    writer.writeLine();
  }
}
function emitComments(text, lineMap, writer, comments, leadingSeparator, trailingSeparator, newLine, writeComment) {
  if (comments && comments.length > 0) {
    if (leadingSeparator) {
      writer.writeSpace(" ");
    }
    let emitInterveningSeparator ***REMOVED*** false;
    for (const comment of comments) {
      if (emitInterveningSeparator) {
        writer.writeSpace(" ");
        emitInterveningSeparator ***REMOVED*** false;
      }
      writeComment(text, lineMap, writer, comment.pos, comment.end, newLine);
      if (comment.hasTrailingNewLine) {
        writer.writeLine();
      } else {
        emitInterveningSeparator ***REMOVED*** true;
      }
    }
    if (emitInterveningSeparator && trailingSeparator) {
      writer.writeSpace(" ");
    }
  }
}
function emitDetachedComments(text, lineMap, writer, writeComment, node, newLine, removeComments) {
  let leadingComments;
  let currentDetachedCommentInfo;
  if (removeComments) {
    if (node.pos ***REMOVED******REMOVED******REMOVED*** 0) {
      leadingComments ***REMOVED*** filter(getLeadingCommentRanges(text, node.pos), isPinnedCommentLocal);
    }
  } else {
    leadingComments ***REMOVED*** getLeadingCommentRanges(text, node.pos);
  }
  if (leadingComments) {
    const detachedComments ***REMOVED*** [];
    let lastComment;
    for (const comment of leadingComments) {
      if (lastComment) {
        const lastCommentLine ***REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, lastComment.end);
        const commentLine ***REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, comment.pos);
        if (commentLine >***REMOVED*** lastCommentLine + 2) {
          break;
        }
      }
      detachedComments.push(comment);
      lastComment ***REMOVED*** comment;
    }
    if (detachedComments.length) {
      const lastCommentLine ***REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, last(detachedComments).end);
      const nodeLine ***REMOVED*** getLineOfLocalPositionFromLineMap(lineMap, skipTrivia(text, node.pos));
      if (nodeLine >***REMOVED*** lastCommentLine + 2) {
        emitNewLineBeforeLeadingComments(lineMap, writer, node, leadingComments);
        emitComments(
          text,
          lineMap,
          writer,
          detachedComments,
          /*leadingSeparator*/
          false,
          /*trailingSeparator*/
          true,
          newLine,
          writeComment
        );
        currentDetachedCommentInfo ***REMOVED*** { nodePos: node.pos, detachedCommentEndPos: last(detachedComments).end };
      }
    }
  }
  return currentDetachedCommentInfo;
  function isPinnedCommentLocal(comment) {
    return isPinnedComment(text, comment.pos);
  }
}
function writeCommentRange(text, lineMap, writer, commentPos, commentEnd, newLine) {
  if (text.charCodeAt(commentPos + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
    const firstCommentLineAndCharacter ***REMOVED*** computeLineAndCharacterOfPosition(lineMap, commentPos);
    const lineCount ***REMOVED*** lineMap.length;
    let firstCommentLineIndent;
    for (let pos ***REMOVED*** commentPos, currentLine ***REMOVED*** firstCommentLineAndCharacter.line; pos < commentEnd; currentLine++) {
      const nextLineStart ***REMOVED*** currentLine + 1 ***REMOVED******REMOVED******REMOVED*** lineCount ? text.length + 1 : lineMap[currentLine + 1];
      if (pos !***REMOVED******REMOVED*** commentPos) {
        if (firstCommentLineIndent ***REMOVED******REMOVED******REMOVED*** void 0) {
          firstCommentLineIndent ***REMOVED*** calculateIndent(text, lineMap[firstCommentLineAndCharacter.line], commentPos);
        }
        const currentWriterIndentSpacing ***REMOVED*** writer.getIndent() * getIndentSize();
        const spacesToEmit ***REMOVED*** currentWriterIndentSpacing - firstCommentLineIndent + calculateIndent(text, pos, nextLineStart);
        if (spacesToEmit > 0) {
          let numberOfSingleSpacesToEmit ***REMOVED*** spacesToEmit % getIndentSize();
          const indentSizeSpaceString ***REMOVED*** getIndentString((spacesToEmit - numberOfSingleSpacesToEmit) / getIndentSize());
          writer.rawWrite(indentSizeSpaceString);
          while (numberOfSingleSpacesToEmit) {
            writer.rawWrite(" ");
            numberOfSingleSpacesToEmit--;
          }
        } else {
          writer.rawWrite("");
        }
      }
      writeTrimmedCurrentLine(text, commentEnd, writer, newLine, pos, nextLineStart);
      pos ***REMOVED*** nextLineStart;
    }
  } else {
    writer.writeComment(text.substring(commentPos, commentEnd));
  }
}
function writeTrimmedCurrentLine(text, commentEnd, writer, newLine, pos, nextLineStart) {
  const end ***REMOVED*** Math.min(commentEnd, nextLineStart - 1);
  const currentLineText ***REMOVED*** text.substring(pos, end).trim();
  if (currentLineText) {
    writer.writeComment(currentLineText);
    if (end !***REMOVED******REMOVED*** commentEnd) {
      writer.writeLine();
    }
  } else {
    writer.rawWrite(newLine);
  }
}
function calculateIndent(text, pos, end) {
  let currentLineIndent ***REMOVED*** 0;
  for (; pos < end && isWhiteSpaceSingleLine(text.charCodeAt(pos)); pos++) {
    if (text.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 9 /* tab */) {
      currentLineIndent +***REMOVED*** getIndentSize() - currentLineIndent % getIndentSize();
    } else {
      currentLineIndent++;
    }
  }
  return currentLineIndent;
}
function hasEffectiveModifiers(node) {
  return getEffectiveModifierFlags(node) !***REMOVED******REMOVED*** 0 /* None */;
}
function hasSyntacticModifiers(node) {
  return getSyntacticModifierFlags(node) !***REMOVED******REMOVED*** 0 /* None */;
}
function hasEffectiveModifier(node, flags) {
  return !!getSelectedEffectiveModifierFlags(node, flags);
}
function hasSyntacticModifier(node, flags) {
  return !!getSelectedSyntacticModifierFlags(node, flags);
}
function isStatic(node) {
  return isClassElement(node) && hasStaticModifier(node) || isClassStaticBlockDeclaration(node);
}
function hasStaticModifier(node) {
  return hasSyntacticModifier(node, 256 /* Static */);
}
function hasOverrideModifier(node) {
  return hasEffectiveModifier(node, 16 /* Override */);
}
function hasAbstractModifier(node) {
  return hasSyntacticModifier(node, 64 /* Abstract */);
}
function hasAmbientModifier(node) {
  return hasSyntacticModifier(node, 128 /* Ambient */);
}
function hasAccessorModifier(node) {
  return hasSyntacticModifier(node, 512 /* Accessor */);
}
function hasEffectiveReadonlyModifier(node) {
  return hasEffectiveModifier(node, 8 /* Readonly */);
}
function hasDecorators(node) {
  return hasSyntacticModifier(node, 32768 /* Decorator */);
}
function getSelectedEffectiveModifierFlags(node, flags) {
  return getEffectiveModifierFlags(node) & flags;
}
function getSelectedSyntacticModifierFlags(node, flags) {
  return getSyntacticModifierFlags(node) & flags;
}
function getModifierFlagsWorker(node, includeJSDoc, alwaysIncludeJSDoc) {
  if (node.kind >***REMOVED*** 0 /* FirstToken */ && node.kind <***REMOVED*** 166 /* LastToken */) {
    return 0 /* None */;
  }
  if (!(node.modifierFlagsCache & 536870912 /* HasComputedFlags */)) {
    node.modifierFlagsCache ***REMOVED*** getSyntacticModifierFlagsNoCache(node) | 536870912 /* HasComputedFlags */;
  }
  if (alwaysIncludeJSDoc || includeJSDoc && isInJSFile(node)) {
    if (!(node.modifierFlagsCache & 268435456 /* HasComputedJSDocModifiers */) && node.parent) {
      node.modifierFlagsCache |***REMOVED*** getRawJSDocModifierFlagsNoCache(node) | 268435456 /* HasComputedJSDocModifiers */;
    }
    return selectEffectiveModifierFlags(node.modifierFlagsCache);
  }
  return selectSyntacticModifierFlags(node.modifierFlagsCache);
}
function getEffectiveModifierFlags(node) {
  return getModifierFlagsWorker(
    node,
    /*includeJSDoc*/
    true
  );
}
function getSyntacticModifierFlags(node) {
  return getModifierFlagsWorker(
    node,
    /*includeJSDoc*/
    false
  );
}
function getRawJSDocModifierFlagsNoCache(node) {
  let flags ***REMOVED*** 0 /* None */;
  if (!!node.parent && !isParameter(node)) {
    if (isInJSFile(node)) {
      if (getJSDocPublicTagNoCache(node)) flags |***REMOVED*** 8388608 /* JSDocPublic */;
      if (getJSDocPrivateTagNoCache(node)) flags |***REMOVED*** 16777216 /* JSDocPrivate */;
      if (getJSDocProtectedTagNoCache(node)) flags |***REMOVED*** 33554432 /* JSDocProtected */;
      if (getJSDocReadonlyTagNoCache(node)) flags |***REMOVED*** 67108864 /* JSDocReadonly */;
      if (getJSDocOverrideTagNoCache(node)) flags |***REMOVED*** 134217728 /* JSDocOverride */;
    }
    if (getJSDocDeprecatedTagNoCache(node)) flags |***REMOVED*** 65536 /* Deprecated */;
  }
  return flags;
}
function selectSyntacticModifierFlags(flags) {
  return flags & 65535 /* SyntacticModifiers */;
}
function selectEffectiveModifierFlags(flags) {
  return flags & 131071 /* NonCacheOnlyModifiers */ | (flags & 260046848 /* JSDocCacheOnlyModifiers */) >>> 23;
}
function getJSDocModifierFlagsNoCache(node) {
  return selectEffectiveModifierFlags(getRawJSDocModifierFlagsNoCache(node));
}
function getEffectiveModifierFlagsNoCache(node) {
  return getSyntacticModifierFlagsNoCache(node) | getJSDocModifierFlagsNoCache(node);
}
function getSyntacticModifierFlagsNoCache(node) {
  let flags ***REMOVED*** canHaveModifiers(node) ? modifiersToFlags(node.modifiers) : 0 /* None */;
  if (node.flags & 8 /* NestedNamespace */ || node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && node.flags & 4096 /* IdentifierIsInJSDocNamespace */) {
    flags |***REMOVED*** 32 /* Export */;
  }
  return flags;
}
function modifiersToFlags(modifiers) {
  let flags ***REMOVED*** 0 /* None */;
  if (modifiers) {
    for (const modifier of modifiers) {
      flags |***REMOVED*** modifierToFlag(modifier.kind);
    }
  }
  return flags;
}
function modifierToFlag(token) {
  switch (token) {
    case 126 /* StaticKeyword */:
      return 256 /* Static */;
    case 125 /* PublicKeyword */:
      return 1 /* Public */;
    case 124 /* ProtectedKeyword */:
      return 4 /* Protected */;
    case 123 /* PrivateKeyword */:
      return 2 /* Private */;
    case 128 /* AbstractKeyword */:
      return 64 /* Abstract */;
    case 129 /* AccessorKeyword */:
      return 512 /* Accessor */;
    case 95 /* ExportKeyword */:
      return 32 /* Export */;
    case 138 /* DeclareKeyword */:
      return 128 /* Ambient */;
    case 87 /* ConstKeyword */:
      return 4096 /* Const */;
    case 90 /* DefaultKeyword */:
      return 2048 /* Default */;
    case 134 /* AsyncKeyword */:
      return 1024 /* Async */;
    case 148 /* ReadonlyKeyword */:
      return 8 /* Readonly */;
    case 164 /* OverrideKeyword */:
      return 16 /* Override */;
    case 103 /* InKeyword */:
      return 8192 /* In */;
    case 147 /* OutKeyword */:
      return 16384 /* Out */;
    case 171 /* Decorator */:
      return 32768 /* Decorator */;
  }
  return 0 /* None */;
}
function isBinaryLogicalOperator(token) {
  return token ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || token ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */;
}
function isLogicalOperator(token) {
  return isBinaryLogicalOperator(token) || token ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */;
}
function isLogicalOrCoalescingAssignmentOperator(token) {
  return token ***REMOVED******REMOVED******REMOVED*** 76 /* BarBarEqualsToken */ || token ***REMOVED******REMOVED******REMOVED*** 77 /* AmpersandAmpersandEqualsToken */ || token ***REMOVED******REMOVED******REMOVED*** 78 /* QuestionQuestionEqualsToken */;
}
function isLogicalOrCoalescingAssignmentExpression(expr) {
  return isBinaryExpression(expr) && isLogicalOrCoalescingAssignmentOperator(expr.operatorToken.kind);
}
function isLogicalOrCoalescingBinaryOperator(token) {
  return isBinaryLogicalOperator(token) || token ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */;
}
function isLogicalOrCoalescingBinaryExpression(expr) {
  return isBinaryExpression(expr) && isLogicalOrCoalescingBinaryOperator(expr.operatorToken.kind);
}
function isAssignmentOperator(token) {
  return token >***REMOVED*** 64 /* FirstAssignment */ && token <***REMOVED*** 79 /* LastAssignment */;
}
function tryGetClassExtendingExpressionWithTypeArguments(node) {
  const cls ***REMOVED*** tryGetClassImplementingOrExtendingExpressionWithTypeArguments(node);
  return cls && !cls.isImplements ? cls.class : void 0;
}
function tryGetClassImplementingOrExtendingExpressionWithTypeArguments(node) {
  if (isExpressionWithTypeArguments(node)) {
    if (isHeritageClause(node.parent) && isClassLike(node.parent.parent)) {
      return { class: node.parent.parent, isImplements: node.parent.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */ };
    }
    if (isJSDocAugmentsTag(node.parent)) {
      const host ***REMOVED*** getEffectiveJSDocHost(node.parent);
      if (host && isClassLike(host)) {
        return { class: host, isImplements: false };
      }
    }
  }
  return void 0;
}
function isAssignmentExpression(node, excludeCompoundAssignment) {
  return isBinaryExpression(node) && (excludeCompoundAssignment ? node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ : isAssignmentOperator(node.operatorToken.kind)) && isLeftHandSideExpression(node.left);
}
function isDestructuringAssignment(node) {
  if (isAssignmentExpression(
    node,
    /*excludeCompoundAssignment*/
    true
  )) {
    const kind ***REMOVED*** node.left.kind;
    return kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */;
  }
  return false;
}
function isExpressionWithTypeArgumentsInClassExtendsClause(node) {
  return tryGetClassExtendingExpressionWithTypeArguments(node) !***REMOVED******REMOVED*** void 0;
}
function isEntityNameExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || isPropertyAccessEntityNameExpression(node);
}
function getFirstIdentifier(node) {
  switch (node.kind) {
    case 80 /* Identifier */:
      return node;
    case 167 /* QualifiedName */:
      do {
        node ***REMOVED*** node.left;
      } while (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */);
      return node;
    case 212 /* PropertyAccessExpression */:
      do {
        node ***REMOVED*** node.expression;
      } while (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */);
      return node;
  }
}
function isDottedName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || node.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ || node.kind ***REMOVED******REMOVED******REMOVED*** 237 /* MetaProperty */ || node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && isDottedName(node.expression) || node.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ && isDottedName(node.expression);
}
function isPropertyAccessEntityNameExpression(node) {
  return isPropertyAccessExpression(node) && isIdentifier(node.name) && isEntityNameExpression(node.expression);
}
function tryGetPropertyAccessOrIdentifierToString(expr) {
  if (isPropertyAccessExpression(expr)) {
    const baseStr ***REMOVED*** tryGetPropertyAccessOrIdentifierToString(expr.expression);
    if (baseStr !***REMOVED******REMOVED*** void 0) {
      return baseStr + "." + entityNameToString(expr.name);
    }
  } else if (isElementAccessExpression(expr)) {
    const baseStr ***REMOVED*** tryGetPropertyAccessOrIdentifierToString(expr.expression);
    if (baseStr !***REMOVED******REMOVED*** void 0 && isPropertyName(expr.argumentExpression)) {
      return baseStr + "." + getPropertyNameForPropertyNameNode(expr.argumentExpression);
    }
  } else if (isIdentifier(expr)) {
    return unescapeLeadingUnderscores(expr.escapedText);
  } else if (isJsxNamespacedName(expr)) {
    return getTextOfJsxNamespacedName(expr);
  }
  return void 0;
}
function isPrototypeAccess(node) {
  return isBindableStaticAccessExpression(node) && getElementOrPropertyAccessName(node) ***REMOVED******REMOVED******REMOVED*** "prototype";
}
function isRightSideOfQualifiedNameOrPropertyAccess(node) {
  return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ && node.parent.right ***REMOVED******REMOVED******REMOVED*** node || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && node.parent.name ***REMOVED******REMOVED******REMOVED*** node || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 237 /* MetaProperty */ && node.parent.name ***REMOVED******REMOVED******REMOVED*** node;
}
function isRightSideOfAccessExpression(node) {
  return !!node.parent && (isPropertyAccessExpression(node.parent) && node.parent.name ***REMOVED******REMOVED******REMOVED*** node || isElementAccessExpression(node.parent) && node.parent.argumentExpression ***REMOVED******REMOVED******REMOVED*** node);
}
function isRightSideOfQualifiedNameOrPropertyAccessOrJSDocMemberName(node) {
  return isQualifiedName(node.parent) && node.parent.right ***REMOVED******REMOVED******REMOVED*** node || isPropertyAccessExpression(node.parent) && node.parent.name ***REMOVED******REMOVED******REMOVED*** node || isJSDocMemberName(node.parent) && node.parent.right ***REMOVED******REMOVED******REMOVED*** node;
}
function isInstanceOfExpression(node) {
  return isBinaryExpression(node) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 104 /* InstanceOfKeyword */;
}
function isRightSideOfInstanceofExpression(node) {
  return isInstanceOfExpression(node.parent) && node ***REMOVED******REMOVED******REMOVED*** node.parent.right;
}
function isEmptyObjectLiteral(expression) {
  return expression.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ && expression.properties.length ***REMOVED******REMOVED******REMOVED*** 0;
}
function isEmptyArrayLiteral(expression) {
  return expression.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ && expression.elements.length ***REMOVED******REMOVED******REMOVED*** 0;
}
function getLocalSymbolForExportDefault(symbol) {
  if (!isExportDefaultSymbol(symbol) || !symbol.declarations) return void 0;
  for (const decl of symbol.declarations) {
    if (decl.localSymbol) return decl.localSymbol;
  }
  return void 0;
}
function isExportDefaultSymbol(symbol) {
  return symbol && length(symbol.declarations) > 0 && hasSyntacticModifier(symbol.declarations[0], 2048 /* Default */);
}
function tryExtractTSExtension(fileName) {
  return find(supportedTSExtensionsForExtractExtension, (extension) ***REMOVED***> fileExtensionIs(fileName, extension));
}
function getExpandedCharCodes(input) {
  const output ***REMOVED*** [];
  const length2 ***REMOVED*** input.length;
  for (let i ***REMOVED*** 0; i < length2; i++) {
    const charCode ***REMOVED*** input.charCodeAt(i);
    if (charCode < 128) {
      output.push(charCode);
    } else if (charCode < 2048) {
      output.push(charCode >> 6 | 192);
      output.push(charCode & 63 | 128);
    } else if (charCode < 65536) {
      output.push(charCode >> 12 | 224);
      output.push(charCode >> 6 & 63 | 128);
      output.push(charCode & 63 | 128);
    } else if (charCode < 131072) {
      output.push(charCode >> 18 | 240);
      output.push(charCode >> 12 & 63 | 128);
      output.push(charCode >> 6 & 63 | 128);
      output.push(charCode & 63 | 128);
    } else {
      Debug.assert(false, "Unexpected code point");
    }
  }
  return output;
}
var base64Digits ***REMOVED*** "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/***REMOVED***";
function convertToBase64(input) {
  let result ***REMOVED*** "";
  const charCodes ***REMOVED*** getExpandedCharCodes(input);
  let i ***REMOVED*** 0;
  const length2 ***REMOVED*** charCodes.length;
  let byte1, byte2, byte3, byte4;
  while (i < length2) {
    byte1 ***REMOVED*** charCodes[i] >> 2;
    byte2 ***REMOVED*** (charCodes[i] & 3) << 4 | charCodes[i + 1] >> 4;
    byte3 ***REMOVED*** (charCodes[i + 1] & 15) << 2 | charCodes[i + 2] >> 6;
    byte4 ***REMOVED*** charCodes[i + 2] & 63;
    if (i + 1 >***REMOVED*** length2) {
      byte3 ***REMOVED*** byte4 ***REMOVED*** 64;
    } else if (i + 2 >***REMOVED*** length2) {
      byte4 ***REMOVED*** 64;
    }
    result +***REMOVED*** base64Digits.charAt(byte1) + base64Digits.charAt(byte2) + base64Digits.charAt(byte3) + base64Digits.charAt(byte4);
    i +***REMOVED*** 3;
  }
  return result;
}
function base64encode(host, input) {
  if (host && host.base64encode) {
    return host.base64encode(input);
  }
  return convertToBase64(input);
}
function readJsonOrUndefined(path, hostOrText) {
  const jsonText ***REMOVED*** isString(hostOrText) ? hostOrText : hostOrText.readFile(path);
  if (!jsonText) return void 0;
  let result ***REMOVED*** tryParseJson(jsonText);
  if (result ***REMOVED******REMOVED******REMOVED*** void 0) {
    const looseResult ***REMOVED*** parseConfigFileTextToJson(path, jsonText);
    if (!looseResult.error) {
      result ***REMOVED*** looseResult.config;
    }
  }
  return result;
}
function readJson(path, host) {
  return readJsonOrUndefined(path, host) || {};
}
function tryParseJson(text) {
  try {
    return JSON.parse(text);
  } catch {
    return void 0;
  }
}
function directoryProbablyExists(directoryName, host) {
  return !host.directoryExists || host.directoryExists(directoryName);
}
var carriageReturnLineFeed ***REMOVED*** "\r\n";
var lineFeed ***REMOVED*** "\n";
function getNewLineCharacter(options) {
  switch (options.newLine) {
    case 0 /* CarriageReturnLineFeed */:
      return carriageReturnLineFeed;
    case 1 /* LineFeed */:
    case void 0:
      return lineFeed;
  }
}
function createRange(pos, end ***REMOVED*** pos) {
  Debug.assert(end >***REMOVED*** pos || end ***REMOVED******REMOVED******REMOVED*** -1);
  return { pos, end };
}
function moveRangeEnd(range, end) {
  return createRange(range.pos, end);
}
function moveRangePos(range, pos) {
  return createRange(pos, range.end);
}
function moveRangePastDecorators(node) {
  const lastDecorator ***REMOVED*** canHaveModifiers(node) ? findLast(node.modifiers, isDecorator) : void 0;
  return lastDecorator && !positionIsSynthesized(lastDecorator.end) ? moveRangePos(node, lastDecorator.end) : node;
}
function moveRangePastModifiers(node) {
  if (isPropertyDeclaration(node) || isMethodDeclaration(node)) {
    return moveRangePos(node, node.name.pos);
  }
  const lastModifier ***REMOVED*** canHaveModifiers(node) ? lastOrUndefined(node.modifiers) : void 0;
  return lastModifier && !positionIsSynthesized(lastModifier.end) ? moveRangePos(node, lastModifier.end) : moveRangePastDecorators(node);
}
function createTokenRange(pos, token) {
  return createRange(pos, pos + tokenToString(token).length);
}
function rangeIsOnSingleLine(range, sourceFile) {
  return rangeStartIsOnSameLineAsRangeEnd(range, range, sourceFile);
}
function rangeStartPositionsAreOnSameLine(range1, range2, sourceFile) {
  return positionsAreOnSameLine(
    getStartPositionOfRange(
      range1,
      sourceFile,
      /*includeComments*/
      false
    ),
    getStartPositionOfRange(
      range2,
      sourceFile,
      /*includeComments*/
      false
    ),
    sourceFile
  );
}
function rangeEndPositionsAreOnSameLine(range1, range2, sourceFile) {
  return positionsAreOnSameLine(range1.end, range2.end, sourceFile);
}
function rangeStartIsOnSameLineAsRangeEnd(range1, range2, sourceFile) {
  return positionsAreOnSameLine(getStartPositionOfRange(
    range1,
    sourceFile,
    /*includeComments*/
    false
  ), range2.end, sourceFile);
}
function rangeEndIsOnSameLineAsRangeStart(range1, range2, sourceFile) {
  return positionsAreOnSameLine(range1.end, getStartPositionOfRange(
    range2,
    sourceFile,
    /*includeComments*/
    false
  ), sourceFile);
}
function getLinesBetweenRangeEndAndRangeStart(range1, range2, sourceFile, includeSecondRangeComments) {
  const range2Start ***REMOVED*** getStartPositionOfRange(range2, sourceFile, includeSecondRangeComments);
  return getLinesBetweenPositions(sourceFile, range1.end, range2Start);
}
function positionsAreOnSameLine(pos1, pos2, sourceFile) {
  return getLinesBetweenPositions(sourceFile, pos1, pos2) ***REMOVED******REMOVED******REMOVED*** 0;
}
function getStartPositionOfRange(range, sourceFile, includeComments) {
  return positionIsSynthesized(range.pos) ? -1 : skipTrivia(
    sourceFile.text,
    range.pos,
    /*stopAfterLineBreak*/
    false,
    includeComments
  );
}
function getLinesBetweenPositionAndPrecedingNonWhitespaceCharacter(pos, stopPos, sourceFile, includeComments) {
  const startPos ***REMOVED*** skipTrivia(
    sourceFile.text,
    pos,
    /*stopAfterLineBreak*/
    false,
    includeComments
  );
  const prevPos ***REMOVED*** getPreviousNonWhitespacePosition(startPos, stopPos, sourceFile);
  return getLinesBetweenPositions(sourceFile, prevPos ?? stopPos, startPos);
}
function getLinesBetweenPositionAndNextNonWhitespaceCharacter(pos, stopPos, sourceFile, includeComments) {
  const nextPos ***REMOVED*** skipTrivia(
    sourceFile.text,
    pos,
    /*stopAfterLineBreak*/
    false,
    includeComments
  );
  return getLinesBetweenPositions(sourceFile, pos, Math.min(stopPos, nextPos));
}
function rangeContainsRange(r1, r2) {
  return startEndContainsRange(r1.pos, r1.end, r2);
}
function startEndContainsRange(start, end, range) {
  return start <***REMOVED*** range.pos && end >***REMOVED*** range.end;
}
function getPreviousNonWhitespacePosition(pos, stopPos ***REMOVED*** 0, sourceFile) {
  while (pos-- > stopPos) {
    if (!isWhiteSpaceLike(sourceFile.text.charCodeAt(pos))) {
      return pos;
    }
  }
}
function isDeclarationNameOfEnumOrNamespace(node) {
  const parseNode ***REMOVED*** getParseTreeNode(node);
  if (parseNode) {
    switch (parseNode.parent.kind) {
      case 267 /* EnumDeclaration */:
      case 268 /* ModuleDeclaration */:
        return parseNode ***REMOVED******REMOVED******REMOVED*** parseNode.parent.name;
    }
  }
  return false;
}
function getInitializedVariables(node) {
  return filter(node.declarations, isInitializedVariable);
}
function isInitializedVariable(node) {
  return isVariableDeclaration(node) && node.initializer !***REMOVED******REMOVED*** void 0;
}
function isWatchSet(options) {
  return options.watch && hasProperty(options, "watch");
}
function closeFileWatcher(watcher) {
  watcher.close();
}
function getCheckFlags(symbol) {
  return symbol.flags & 33554432 /* Transient */ ? symbol.links.checkFlags : 0;
}
function getDeclarationModifierFlagsFromSymbol(s, isWrite ***REMOVED*** false) {
  if (s.valueDeclaration) {
    const declaration ***REMOVED*** isWrite && s.declarations && find(s.declarations, isSetAccessorDeclaration) || s.flags & 32768 /* GetAccessor */ && find(s.declarations, isGetAccessorDeclaration) || s.valueDeclaration;
    const flags ***REMOVED*** getCombinedModifierFlags(declaration);
    return s.parent && s.parent.flags & 32 /* Class */ ? flags : flags & ~7 /* AccessibilityModifier */;
  }
  if (getCheckFlags(s) & 6 /* Synthetic */) {
    const checkFlags ***REMOVED*** s.links.checkFlags;
    const accessModifier ***REMOVED*** checkFlags & 1024 /* ContainsPrivate */ ? 2 /* Private */ : checkFlags & 256 /* ContainsPublic */ ? 1 /* Public */ : 4 /* Protected */;
    const staticModifier ***REMOVED*** checkFlags & 2048 /* ContainsStatic */ ? 256 /* Static */ : 0;
    return accessModifier | staticModifier;
  }
  if (s.flags & 4194304 /* Prototype */) {
    return 1 /* Public */ | 256 /* Static */;
  }
  return 0;
}
function skipAlias(symbol, checker) {
  return symbol.flags & 2097152 /* Alias */ ? checker.getAliasedSymbol(symbol) : symbol;
}
function getCombinedLocalAndExportSymbolFlags(symbol) {
  return symbol.exportSymbol ? symbol.exportSymbol.flags | symbol.flags : symbol.flags;
}
function isWriteOnlyAccess(node) {
  return accessKind(node) ***REMOVED******REMOVED******REMOVED*** 1 /* Write */;
}
function isWriteAccess(node) {
  return accessKind(node) !***REMOVED******REMOVED*** 0 /* Read */;
}
function accessKind(node) {
  const { parent } ***REMOVED*** node;
  switch (parent ***REMOVED******REMOVED*** null ? void 0 : parent.kind) {
    case 218 /* ParenthesizedExpression */:
      return accessKind(parent);
    case 226 /* PostfixUnaryExpression */:
    case 225 /* PrefixUnaryExpression */:
      const { operator } ***REMOVED*** parent;
      return operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */ ? 2 /* ReadWrite */ : 0 /* Read */;
    case 227 /* BinaryExpression */:
      const { left, operatorToken } ***REMOVED*** parent;
      return left ***REMOVED******REMOVED******REMOVED*** node && isAssignmentOperator(operatorToken.kind) ? operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ ? 1 /* Write */ : 2 /* ReadWrite */ : 0 /* Read */;
    case 212 /* PropertyAccessExpression */:
      return parent.name !***REMOVED******REMOVED*** node ? 0 /* Read */ : accessKind(parent);
    case 304 /* PropertyAssignment */: {
      const parentAccess ***REMOVED*** accessKind(parent.parent);
      return node ***REMOVED******REMOVED******REMOVED*** parent.name ? reverseAccessKind(parentAccess) : parentAccess;
    }
    case 305 /* ShorthandPropertyAssignment */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.objectAssignmentInitializer ? 0 /* Read */ : accessKind(parent.parent);
    case 210 /* ArrayLiteralExpression */:
      return accessKind(parent);
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
      return node ***REMOVED******REMOVED******REMOVED*** parent.initializer ? 1 /* Write */ : 0 /* Read */;
    default:
      return 0 /* Read */;
  }
}
function reverseAccessKind(a) {
  switch (a) {
    case 0 /* Read */:
      return 1 /* Write */;
    case 1 /* Write */:
      return 0 /* Read */;
    case 2 /* ReadWrite */:
      return 2 /* ReadWrite */;
    default:
      return Debug.assertNever(a);
  }
}
function compareDataObjects(dst, src) {
  if (!dst || !src || Object.keys(dst).length !***REMOVED******REMOVED*** Object.keys(src).length) {
    return false;
  }
  for (const e in dst) {
    if (typeof dst[e] ***REMOVED******REMOVED******REMOVED*** "object") {
      if (!compareDataObjects(dst[e], src[e])) {
        return false;
      }
    } else if (typeof dst[e] !***REMOVED******REMOVED*** "function") {
      if (dst[e] !***REMOVED******REMOVED*** src[e]) {
        return false;
      }
    }
  }
  return true;
}
function clearMap(map2, onDeleteValue) {
  map2.forEach(onDeleteValue);
  map2.clear();
}
function mutateMapSkippingNewValues(map2, newMap, options) {
  const { onDeleteValue, onExistingValue } ***REMOVED*** options;
  map2.forEach((existingValue, key) ***REMOVED***> {
    var _a;
    if (!(newMap ***REMOVED******REMOVED*** null ? void 0 : newMap.has(key))) {
      map2.delete(key);
      onDeleteValue(existingValue, key);
    } else if (onExistingValue) {
      onExistingValue(existingValue, (_a ***REMOVED*** newMap.get) ***REMOVED******REMOVED*** null ? void 0 : _a.call(newMap, key), key);
    }
  });
}
function mutateMap(map2, newMap, options) {
  mutateMapSkippingNewValues(map2, newMap, options);
  const { createNewValue } ***REMOVED*** options;
  newMap ***REMOVED******REMOVED*** null ? void 0 : newMap.forEach((valueInNewMap, key) ***REMOVED***> {
    if (!map2.has(key)) {
      map2.set(key, createNewValue(key, valueInNewMap));
    }
  });
}
function getClassLikeDeclarationOfSymbol(symbol) {
  var _a;
  return (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isClassLike);
}
function getObjectFlags(type) {
  return type.flags & 3899393 /* ObjectFlagsType */ ? type.objectFlags : 0;
}
function isUMDExportSymbol(symbol) {
  return !!symbol && !!symbol.declarations && !!symbol.declarations[0] && isNamespaceExportDeclaration(symbol.declarations[0]);
}
function getLastChild(node) {
  let lastChild;
  forEachChild(node, (child) ***REMOVED***> {
    if (nodeIsPresent(child)) lastChild ***REMOVED*** child;
  }, (children) ***REMOVED***> {
    for (let i ***REMOVED*** children.length - 1; i >***REMOVED*** 0; i--) {
      if (nodeIsPresent(children[i])) {
        lastChild ***REMOVED*** children[i];
        break;
      }
    }
  });
  return lastChild;
}
function addToSeen(seen, key) {
  if (seen.has(key)) {
    return false;
  }
  seen.add(key);
  return true;
}
function isTypeNodeKind(kind) {
  return kind >***REMOVED*** 183 /* FirstTypeNode */ && kind <***REMOVED*** 206 /* LastTypeNode */ || kind ***REMOVED******REMOVED******REMOVED*** 133 /* AnyKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 159 /* UnknownKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 150 /* NumberKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 163 /* BigIntKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 151 /* ObjectKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 136 /* BooleanKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 154 /* StringKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 155 /* SymbolKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 116 /* VoidKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 157 /* UndefinedKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 146 /* NeverKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 141 /* IntrinsicKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */ || kind ***REMOVED******REMOVED******REMOVED*** 313 /* JSDocAllType */ || kind ***REMOVED******REMOVED******REMOVED*** 314 /* JSDocUnknownType */ || kind ***REMOVED******REMOVED******REMOVED*** 315 /* JSDocNullableType */ || kind ***REMOVED******REMOVED******REMOVED*** 316 /* JSDocNonNullableType */ || kind ***REMOVED******REMOVED******REMOVED*** 317 /* JSDocOptionalType */ || kind ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */ || kind ***REMOVED******REMOVED******REMOVED*** 319 /* JSDocVariadicType */;
}
function isAccessExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */;
}
function getLeftmostAccessExpression(expr) {
  while (isAccessExpression(expr)) {
    expr ***REMOVED*** expr.expression;
  }
  return expr;
}
function getLeftmostExpression(node, stopAtCallExpressions) {
  while (true) {
    switch (node.kind) {
      case 226 /* PostfixUnaryExpression */:
        node ***REMOVED*** node.operand;
        continue;
      case 227 /* BinaryExpression */:
        node ***REMOVED*** node.left;
        continue;
      case 228 /* ConditionalExpression */:
        node ***REMOVED*** node.condition;
        continue;
      case 216 /* TaggedTemplateExpression */:
        node ***REMOVED*** node.tag;
        continue;
      case 214 /* CallExpression */:
        if (stopAtCallExpressions) {
          return node;
        }
      // falls through
      case 235 /* AsExpression */:
      case 213 /* ElementAccessExpression */:
      case 212 /* PropertyAccessExpression */:
      case 236 /* NonNullExpression */:
      case 356 /* PartiallyEmittedExpression */:
      case 239 /* SatisfiesExpression */:
        node ***REMOVED*** node.expression;
        continue;
    }
    return node;
  }
}
function Symbol4(flags, name) {
  this.flags ***REMOVED*** flags;
  this.escapedName ***REMOVED*** name;
  this.declarations ***REMOVED*** void 0;
  this.valueDeclaration ***REMOVED*** void 0;
  this.id ***REMOVED*** 0;
  this.mergeId ***REMOVED*** 0;
  this.parent ***REMOVED*** void 0;
  this.members ***REMOVED*** void 0;
  this.exports ***REMOVED*** void 0;
  this.exportSymbol ***REMOVED*** void 0;
  this.constEnumOnlyModule ***REMOVED*** void 0;
  this.isReferenced ***REMOVED*** void 0;
  this.lastAssignmentPos ***REMOVED*** void 0;
  this.links ***REMOVED*** void 0;
}
function Type3(checker, flags) {
  this.flags ***REMOVED*** flags;
  if (Debug.isDebugging || tracing) {
    this.checker ***REMOVED*** checker;
  }
}
function Signature2(checker, flags) {
  this.flags ***REMOVED*** flags;
  if (Debug.isDebugging) {
    this.checker ***REMOVED*** checker;
  }
}
function Node4(kind, pos, end) {
  this.pos ***REMOVED*** pos;
  this.end ***REMOVED*** end;
  this.kind ***REMOVED*** kind;
  this.id ***REMOVED*** 0;
  this.flags ***REMOVED*** 0 /* None */;
  this.modifierFlagsCache ***REMOVED*** 0 /* None */;
  this.transformFlags ***REMOVED*** 0 /* None */;
  this.parent ***REMOVED*** void 0;
  this.original ***REMOVED*** void 0;
  this.emitNode ***REMOVED*** void 0;
}
function Token(kind, pos, end) {
  this.pos ***REMOVED*** pos;
  this.end ***REMOVED*** end;
  this.kind ***REMOVED*** kind;
  this.id ***REMOVED*** 0;
  this.flags ***REMOVED*** 0 /* None */;
  this.transformFlags ***REMOVED*** 0 /* None */;
  this.parent ***REMOVED*** void 0;
  this.emitNode ***REMOVED*** void 0;
}
function Identifier2(kind, pos, end) {
  this.pos ***REMOVED*** pos;
  this.end ***REMOVED*** end;
  this.kind ***REMOVED*** kind;
  this.id ***REMOVED*** 0;
  this.flags ***REMOVED*** 0 /* None */;
  this.transformFlags ***REMOVED*** 0 /* None */;
  this.parent ***REMOVED*** void 0;
  this.original ***REMOVED*** void 0;
  this.emitNode ***REMOVED*** void 0;
}
function SourceMapSource(fileName, text, skipTrivia2) {
  this.fileName ***REMOVED*** fileName;
  this.text ***REMOVED*** text;
  this.skipTrivia ***REMOVED*** skipTrivia2 || ((pos) ***REMOVED***> pos);
}
var objectAllocator ***REMOVED*** {
  getNodeConstructor: () ***REMOVED***> Node4,
  getTokenConstructor: () ***REMOVED***> Token,
  getIdentifierConstructor: () ***REMOVED***> Identifier2,
  getPrivateIdentifierConstructor: () ***REMOVED***> Node4,
  getSourceFileConstructor: () ***REMOVED***> Node4,
  getSymbolConstructor: () ***REMOVED***> Symbol4,
  getTypeConstructor: () ***REMOVED***> Type3,
  getSignatureConstructor: () ***REMOVED***> Signature2,
  getSourceMapSourceConstructor: () ***REMOVED***> SourceMapSource
};
function formatStringFromArgs(text, args) {
  return text.replace(/\{(\d+)\}/g, (_match, index) ***REMOVED***> "" + Debug.checkDefined(args[+index]));
}
var localizedDiagnosticMessages;
function setLocalizedDiagnosticMessages(messages) {
  localizedDiagnosticMessages ***REMOVED*** messages;
}
function getLocaleSpecificMessage(message) {
  return localizedDiagnosticMessages && localizedDiagnosticMessages[message.key] || message.message;
}
function createDetachedDiagnostic(fileName, sourceText, start, length2, message, ...args) {
  if (start + length2 > sourceText.length) {
    length2 ***REMOVED*** sourceText.length - start;
  }
  assertDiagnosticLocation(sourceText, start, length2);
  let text ***REMOVED*** getLocaleSpecificMessage(message);
  if (some(args)) {
    text ***REMOVED*** formatStringFromArgs(text, args);
  }
  return {
    file: void 0,
    start,
    length: length2,
    messageText: text,
    category: message.category,
    code: message.code,
    reportsUnnecessary: message.reportsUnnecessary,
    fileName
  };
}
function isDiagnosticWithDetachedLocation(diagnostic) {
  return diagnostic.file ***REMOVED******REMOVED******REMOVED*** void 0 && diagnostic.start !***REMOVED******REMOVED*** void 0 && diagnostic.length !***REMOVED******REMOVED*** void 0 && typeof diagnostic.fileName ***REMOVED******REMOVED******REMOVED*** "string";
}
function attachFileToDiagnostic(diagnostic, file) {
  const fileName ***REMOVED*** file.fileName || "";
  const length2 ***REMOVED*** file.text.length;
  Debug.assertEqual(diagnostic.fileName, fileName);
  Debug.assertLessThanOrEqual(diagnostic.start, length2);
  Debug.assertLessThanOrEqual(diagnostic.start + diagnostic.length, length2);
  const diagnosticWithLocation ***REMOVED*** {
    file,
    start: diagnostic.start,
    length: diagnostic.length,
    messageText: diagnostic.messageText,
    category: diagnostic.category,
    code: diagnostic.code,
    reportsUnnecessary: diagnostic.reportsUnnecessary
  };
  if (diagnostic.relatedInformation) {
    diagnosticWithLocation.relatedInformation ***REMOVED*** [];
    for (const related of diagnostic.relatedInformation) {
      if (isDiagnosticWithDetachedLocation(related) && related.fileName ***REMOVED******REMOVED******REMOVED*** fileName) {
        Debug.assertLessThanOrEqual(related.start, length2);
        Debug.assertLessThanOrEqual(related.start + related.length, length2);
        diagnosticWithLocation.relatedInformation.push(attachFileToDiagnostic(related, file));
      } else {
        diagnosticWithLocation.relatedInformation.push(related);
      }
    }
  }
  return diagnosticWithLocation;
}
function attachFileToDiagnostics(diagnostics, file) {
  const diagnosticsWithLocation ***REMOVED*** [];
  for (const diagnostic of diagnostics) {
    diagnosticsWithLocation.push(attachFileToDiagnostic(diagnostic, file));
  }
  return diagnosticsWithLocation;
}
function createFileDiagnostic(file, start, length2, message, ...args) {
  assertDiagnosticLocation(file.text, start, length2);
  let text ***REMOVED*** getLocaleSpecificMessage(message);
  if (some(args)) {
    text ***REMOVED*** formatStringFromArgs(text, args);
  }
  return {
    file,
    start,
    length: length2,
    messageText: text,
    category: message.category,
    code: message.code,
    reportsUnnecessary: message.reportsUnnecessary,
    reportsDeprecated: message.reportsDeprecated
  };
}
function formatMessage(message, ...args) {
  let text ***REMOVED*** getLocaleSpecificMessage(message);
  if (some(args)) {
    text ***REMOVED*** formatStringFromArgs(text, args);
  }
  return text;
}
function createCompilerDiagnostic(message, ...args) {
  let text ***REMOVED*** getLocaleSpecificMessage(message);
  if (some(args)) {
    text ***REMOVED*** formatStringFromArgs(text, args);
  }
  return {
    file: void 0,
    start: void 0,
    length: void 0,
    messageText: text,
    category: message.category,
    code: message.code,
    reportsUnnecessary: message.reportsUnnecessary,
    reportsDeprecated: message.reportsDeprecated
  };
}
function createCompilerDiagnosticFromMessageChain(chain, relatedInformation) {
  return {
    file: void 0,
    start: void 0,
    length: void 0,
    code: chain.code,
    category: chain.category,
    messageText: chain.next ? chain : chain.messageText,
    relatedInformation
  };
}
function chainDiagnosticMessages(details, message, ...args) {
  let text ***REMOVED*** getLocaleSpecificMessage(message);
  if (some(args)) {
    text ***REMOVED*** formatStringFromArgs(text, args);
  }
  return {
    messageText: text,
    category: message.category,
    code: message.code,
    next: details ***REMOVED******REMOVED******REMOVED*** void 0 || Array.isArray(details) ? details : [details]
  };
}
function concatenateDiagnosticMessageChains(headChain, tailChain) {
  let lastChain ***REMOVED*** headChain;
  while (lastChain.next) {
    lastChain ***REMOVED*** lastChain.next[0];
  }
  lastChain.next ***REMOVED*** [tailChain];
}
function getDiagnosticFilePath(diagnostic) {
  return diagnostic.file ? diagnostic.file.path : void 0;
}
function compareDiagnostics(d1, d2) {
  return compareDiagnosticsSkipRelatedInformation(d1, d2) || compareRelatedInformation(d1, d2) || 0 /* EqualTo */;
}
function compareDiagnosticsSkipRelatedInformation(d1, d2) {
  const code1 ***REMOVED*** getDiagnosticCode(d1);
  const code2 ***REMOVED*** getDiagnosticCode(d2);
  return compareStringsCaseSensitive(getDiagnosticFilePath(d1), getDiagnosticFilePath(d2)) || compareValues(d1.start, d2.start) || compareValues(d1.length, d2.length) || compareValues(code1, code2) || compareMessageText(d1, d2) || 0 /* EqualTo */;
}
function compareRelatedInformation(d1, d2) {
  if (!d1.relatedInformation && !d2.relatedInformation) {
    return 0 /* EqualTo */;
  }
  if (d1.relatedInformation && d2.relatedInformation) {
    return compareValues(d2.relatedInformation.length, d1.relatedInformation.length) || forEach(d1.relatedInformation, (d1i, index) ***REMOVED***> {
      const d2i ***REMOVED*** d2.relatedInformation[index];
      return compareDiagnostics(d1i, d2i);
    }) || 0 /* EqualTo */;
  }
  return d1.relatedInformation ? -1 /* LessThan */ : 1 /* GreaterThan */;
}
function compareMessageText(d1, d2) {
  let headMsg1 ***REMOVED*** getDiagnosticMessage(d1);
  let headMsg2 ***REMOVED*** getDiagnosticMessage(d2);
  if (typeof headMsg1 !***REMOVED******REMOVED*** "string") {
    headMsg1 ***REMOVED*** headMsg1.messageText;
  }
  if (typeof headMsg2 !***REMOVED******REMOVED*** "string") {
    headMsg2 ***REMOVED*** headMsg2.messageText;
  }
  const chain1 ***REMOVED*** typeof d1.messageText !***REMOVED******REMOVED*** "string" ? d1.messageText.next : void 0;
  const chain2 ***REMOVED*** typeof d2.messageText !***REMOVED******REMOVED*** "string" ? d2.messageText.next : void 0;
  let res ***REMOVED*** compareStringsCaseSensitive(headMsg1, headMsg2);
  if (res) {
    return res;
  }
  res ***REMOVED*** compareMessageChain(chain1, chain2);
  if (res) {
    return res;
  }
  if (d1.canonicalHead && !d2.canonicalHead) {
    return -1 /* LessThan */;
  }
  if (d2.canonicalHead && !d1.canonicalHead) {
    return 1 /* GreaterThan */;
  }
  return 0 /* EqualTo */;
}
function compareMessageChain(c1, c2) {
  if (c1 ***REMOVED******REMOVED******REMOVED*** void 0 && c2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return 0 /* EqualTo */;
  }
  if (c1 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return 1 /* GreaterThan */;
  }
  if (c2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return -1 /* LessThan */;
  }
  return compareMessageChainSize(c1, c2) || compareMessageChainContent(c1, c2);
}
function compareMessageChainSize(c1, c2) {
  if (c1 ***REMOVED******REMOVED******REMOVED*** void 0 && c2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return 0 /* EqualTo */;
  }
  if (c1 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return 1 /* GreaterThan */;
  }
  if (c2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return -1 /* LessThan */;
  }
  let res ***REMOVED*** compareValues(c2.length, c1.length);
  if (res) {
    return res;
  }
  for (let i ***REMOVED*** 0; i < c2.length; i++) {
    res ***REMOVED*** compareMessageChainSize(c1[i].next, c2[i].next);
    if (res) {
      return res;
    }
  }
  return 0 /* EqualTo */;
}
function compareMessageChainContent(c1, c2) {
  let res;
  for (let i ***REMOVED*** 0; i < c2.length; i++) {
    res ***REMOVED*** compareStringsCaseSensitive(c1[i].messageText, c2[i].messageText);
    if (res) {
      return res;
    }
    if (c1[i].next ***REMOVED******REMOVED******REMOVED*** void 0) {
      continue;
    }
    res ***REMOVED*** compareMessageChainContent(c1[i].next, c2[i].next);
    if (res) {
      return res;
    }
  }
  return 0 /* EqualTo */;
}
function diagnosticsEqualityComparer(d1, d2) {
  const code1 ***REMOVED*** getDiagnosticCode(d1);
  const code2 ***REMOVED*** getDiagnosticCode(d2);
  const msg1 ***REMOVED*** getDiagnosticMessage(d1);
  const msg2 ***REMOVED*** getDiagnosticMessage(d2);
  return compareStringsCaseSensitive(getDiagnosticFilePath(d1), getDiagnosticFilePath(d2)) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ && compareValues(d1.start, d2.start) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ && compareValues(d1.length, d2.length) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ && compareValues(code1, code2) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ && messageTextEqualityComparer(msg1, msg2);
}
function getDiagnosticCode(d) {
  var _a;
  return ((_a ***REMOVED*** d.canonicalHead) ***REMOVED******REMOVED*** null ? void 0 : _a.code) || d.code;
}
function getDiagnosticMessage(d) {
  var _a;
  return ((_a ***REMOVED*** d.canonicalHead) ***REMOVED******REMOVED*** null ? void 0 : _a.messageText) || d.messageText;
}
function messageTextEqualityComparer(m1, m2) {
  const t1 ***REMOVED*** typeof m1 ***REMOVED******REMOVED******REMOVED*** "string" ? m1 : m1.messageText;
  const t2 ***REMOVED*** typeof m2 ***REMOVED******REMOVED******REMOVED*** "string" ? m2 : m2.messageText;
  return compareStringsCaseSensitive(t1, t2) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */;
}
function getLanguageVariant(scriptKind) {
  return scriptKind ***REMOVED******REMOVED******REMOVED*** 4 /* TSX */ || scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */ || scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || scriptKind ***REMOVED******REMOVED******REMOVED*** 6 /* JSON */ ? 1 /* JSX */ : 0 /* Standard */;
}
function walkTreeForJSXTags(node) {
  if (!(node.transformFlags & 2 /* ContainsJsx */)) return void 0;
  return isJsxOpeningLikeElement(node) || isJsxFragment(node) ? node : forEachChild(node, walkTreeForJSXTags);
}
function isFileModuleFromUsingJSXTag(file) {
  return !file.isDeclarationFile ? walkTreeForJSXTags(file) : void 0;
}
function isFileForcedToBeModuleByFormat(file, options) {
  return (getImpliedNodeFormatForEmitWorker(file, options) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ || fileExtensionIsOneOf(file.fileName, [".cjs" /* Cjs */, ".cts" /* Cts */, ".mjs" /* Mjs */, ".mts" /* Mts */])) && !file.isDeclarationFile ? true : void 0;
}
function getSetExternalModuleIndicator(options) {
  switch (getEmitModuleDetectionKind(options)) {
    case 3 /* Force */:
      return (file) ***REMOVED***> {
        file.externalModuleIndicator ***REMOVED*** isFileProbablyExternalModule(file) || !file.isDeclarationFile || void 0;
      };
    case 1 /* Legacy */:
      return (file) ***REMOVED***> {
        file.externalModuleIndicator ***REMOVED*** isFileProbablyExternalModule(file);
      };
    case 2 /* Auto */:
      const checks ***REMOVED*** [isFileProbablyExternalModule];
      if (options.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || options.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
        checks.push(isFileModuleFromUsingJSXTag);
      }
      checks.push(isFileForcedToBeModuleByFormat);
      const combined ***REMOVED*** or(...checks);
      const callback ***REMOVED*** (file) ***REMOVED***> void (file.externalModuleIndicator ***REMOVED*** combined(file, options));
      return callback;
  }
}
function importSyntaxAffectsModuleResolution(options) {
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(options);
  return 3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */ || getResolvePackageJsonExports(options) || getResolvePackageJsonImports(options);
}
function createComputedCompilerOptions(options) {
  return options;
}
var _computedOptions ***REMOVED*** createComputedCompilerOptions({
  allowImportingTsExtensions: {
    dependencies: ["rewriteRelativeImportExtensions"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.allowImportingTsExtensions || compilerOptions.rewriteRelativeImportExtensions);
    }
  },
  target: {
    dependencies: ["module"],
    computeValue: (compilerOptions) ***REMOVED***> {
      const target ***REMOVED*** compilerOptions.target ***REMOVED******REMOVED******REMOVED*** 0 /* ES3 */ ? void 0 : compilerOptions.target;
      return target ?? (compilerOptions.module ***REMOVED******REMOVED******REMOVED*** 100 /* Node16 */ && 9 /* ES2022 */ || compilerOptions.module ***REMOVED******REMOVED******REMOVED*** 101 /* Node18 */ && 9 /* ES2022 */ || compilerOptions.module ***REMOVED******REMOVED******REMOVED*** 102 /* Node20 */ && 10 /* ES2023 */ || compilerOptions.module ***REMOVED******REMOVED******REMOVED*** 199 /* NodeNext */ && 99 /* ESNext */ || 1 /* ES5 */);
    }
  },
  module: {
    dependencies: ["target"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return typeof compilerOptions.module ***REMOVED******REMOVED******REMOVED*** "number" ? compilerOptions.module : _computedOptions.target.computeValue(compilerOptions) >***REMOVED*** 2 /* ES2015 */ ? 5 /* ES2015 */ : 1 /* CommonJS */;
    }
  },
  moduleResolution: {
    dependencies: ["module", "target"],
    computeValue: (compilerOptions) ***REMOVED***> {
      let moduleResolution ***REMOVED*** compilerOptions.moduleResolution;
      if (moduleResolution ***REMOVED******REMOVED******REMOVED*** void 0) {
        switch (_computedOptions.module.computeValue(compilerOptions)) {
          case 1 /* CommonJS */:
            moduleResolution ***REMOVED*** 2 /* Node10 */;
            break;
          case 100 /* Node16 */:
          case 101 /* Node18 */:
          case 102 /* Node20 */:
            moduleResolution ***REMOVED*** 3 /* Node16 */;
            break;
          case 199 /* NodeNext */:
            moduleResolution ***REMOVED*** 99 /* NodeNext */;
            break;
          case 200 /* Preserve */:
            moduleResolution ***REMOVED*** 100 /* Bundler */;
            break;
          default:
            moduleResolution ***REMOVED*** 1 /* Classic */;
            break;
        }
      }
      return moduleResolution;
    }
  },
  moduleDetection: {
    dependencies: ["module", "target"],
    computeValue: (compilerOptions) ***REMOVED***> {
      if (compilerOptions.moduleDetection !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.moduleDetection;
      }
      const moduleKind ***REMOVED*** _computedOptions.module.computeValue(compilerOptions);
      return 100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ ? 3 /* Force */ : 2 /* Auto */;
    }
  },
  isolatedModules: {
    dependencies: ["verbatimModuleSyntax"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.isolatedModules || compilerOptions.verbatimModuleSyntax);
    }
  },
  esModuleInterop: {
    dependencies: ["module", "target"],
    computeValue: (compilerOptions) ***REMOVED***> {
      if (compilerOptions.esModuleInterop !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.esModuleInterop;
      }
      switch (_computedOptions.module.computeValue(compilerOptions)) {
        case 100 /* Node16 */:
        case 101 /* Node18 */:
        case 102 /* Node20 */:
        case 199 /* NodeNext */:
        case 200 /* Preserve */:
          return true;
      }
      return false;
    }
  },
  allowSyntheticDefaultImports: {
    dependencies: ["module", "target", "moduleResolution"],
    computeValue: (compilerOptions) ***REMOVED***> {
      if (compilerOptions.allowSyntheticDefaultImports !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.allowSyntheticDefaultImports;
      }
      return _computedOptions.esModuleInterop.computeValue(compilerOptions) || _computedOptions.module.computeValue(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 4 /* System */ || _computedOptions.moduleResolution.computeValue(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */;
    }
  },
  resolvePackageJsonExports: {
    dependencies: ["moduleResolution"],
    computeValue: (compilerOptions) ***REMOVED***> {
      const moduleResolution ***REMOVED*** _computedOptions.moduleResolution.computeValue(compilerOptions);
      if (!moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
        return false;
      }
      if (compilerOptions.resolvePackageJsonExports !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.resolvePackageJsonExports;
      }
      switch (moduleResolution) {
        case 3 /* Node16 */:
        case 99 /* NodeNext */:
        case 100 /* Bundler */:
          return true;
      }
      return false;
    }
  },
  resolvePackageJsonImports: {
    dependencies: ["moduleResolution", "resolvePackageJsonExports"],
    computeValue: (compilerOptions) ***REMOVED***> {
      const moduleResolution ***REMOVED*** _computedOptions.moduleResolution.computeValue(compilerOptions);
      if (!moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
        return false;
      }
      if (compilerOptions.resolvePackageJsonImports !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.resolvePackageJsonImports;
      }
      switch (moduleResolution) {
        case 3 /* Node16 */:
        case 99 /* NodeNext */:
        case 100 /* Bundler */:
          return true;
      }
      return false;
    }
  },
  resolveJsonModule: {
    dependencies: ["moduleResolution", "module", "target"],
    computeValue: (compilerOptions) ***REMOVED***> {
      if (compilerOptions.resolveJsonModule !***REMOVED******REMOVED*** void 0) {
        return compilerOptions.resolveJsonModule;
      }
      switch (_computedOptions.module.computeValue(compilerOptions)) {
        // TODO in 6.0: uncomment
        // case ModuleKind.Node16:
        // case ModuleKind.Node18:
        case 102 /* Node20 */:
        case 199 /* NodeNext */:
          return true;
      }
      return _computedOptions.moduleResolution.computeValue(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */;
    }
  },
  declaration: {
    dependencies: ["composite"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.declaration || compilerOptions.composite);
    }
  },
  preserveConstEnums: {
    dependencies: ["isolatedModules", "verbatimModuleSyntax"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.preserveConstEnums || _computedOptions.isolatedModules.computeValue(compilerOptions));
    }
  },
  incremental: {
    dependencies: ["composite"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.incremental || compilerOptions.composite);
    }
  },
  declarationMap: {
    dependencies: ["declaration", "composite"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return !!(compilerOptions.declarationMap && _computedOptions.declaration.computeValue(compilerOptions));
    }
  },
  allowJs: {
    dependencies: ["checkJs"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return compilerOptions.allowJs ***REMOVED******REMOVED******REMOVED*** void 0 ? !!compilerOptions.checkJs : compilerOptions.allowJs;
    }
  },
  useDefineForClassFields: {
    dependencies: ["target", "module"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return compilerOptions.useDefineForClassFields ***REMOVED******REMOVED******REMOVED*** void 0 ? _computedOptions.target.computeValue(compilerOptions) >***REMOVED*** 9 /* ES2022 */ : compilerOptions.useDefineForClassFields;
    }
  },
  noImplicitAny: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "noImplicitAny");
    }
  },
  noImplicitThis: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "noImplicitThis");
    }
  },
  strictNullChecks: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "strictNullChecks");
    }
  },
  strictFunctionTypes: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "strictFunctionTypes");
    }
  },
  strictBindCallApply: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "strictBindCallApply");
    }
  },
  strictPropertyInitialization: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "strictPropertyInitialization");
    }
  },
  strictBuiltinIteratorReturn: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "strictBuiltinIteratorReturn");
    }
  },
  alwaysStrict: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "alwaysStrict");
    }
  },
  useUnknownInCatchVariables: {
    dependencies: ["strict"],
    computeValue: (compilerOptions) ***REMOVED***> {
      return getStrictOptionValue(compilerOptions, "useUnknownInCatchVariables");
    }
  }
});
var computedOptions ***REMOVED*** _computedOptions;
var getAllowImportingTsExtensions ***REMOVED*** _computedOptions.allowImportingTsExtensions.computeValue;
var getEmitScriptTarget ***REMOVED*** _computedOptions.target.computeValue;
var getEmitModuleKind ***REMOVED*** _computedOptions.module.computeValue;
var getEmitModuleResolutionKind ***REMOVED*** _computedOptions.moduleResolution.computeValue;
var getEmitModuleDetectionKind ***REMOVED*** _computedOptions.moduleDetection.computeValue;
var getIsolatedModules ***REMOVED*** _computedOptions.isolatedModules.computeValue;
var getESModuleInterop ***REMOVED*** _computedOptions.esModuleInterop.computeValue;
var getAllowSyntheticDefaultImports ***REMOVED*** _computedOptions.allowSyntheticDefaultImports.computeValue;
var getResolvePackageJsonExports ***REMOVED*** _computedOptions.resolvePackageJsonExports.computeValue;
var getResolvePackageJsonImports ***REMOVED*** _computedOptions.resolvePackageJsonImports.computeValue;
var getResolveJsonModule ***REMOVED*** _computedOptions.resolveJsonModule.computeValue;
var getEmitDeclarations ***REMOVED*** _computedOptions.declaration.computeValue;
var shouldPreserveConstEnums ***REMOVED*** _computedOptions.preserveConstEnums.computeValue;
var isIncrementalCompilation ***REMOVED*** _computedOptions.incremental.computeValue;
var getAreDeclarationMapsEnabled ***REMOVED*** _computedOptions.declarationMap.computeValue;
var getAllowJSCompilerOption ***REMOVED*** _computedOptions.allowJs.computeValue;
var getUseDefineForClassFields ***REMOVED*** _computedOptions.useDefineForClassFields.computeValue;
function emitModuleKindIsNonNodeESM(moduleKind) {
  return moduleKind >***REMOVED*** 5 /* ES2015 */ && moduleKind <***REMOVED*** 99 /* ESNext */;
}
function hasJsonModuleEmitEnabled(options) {
  switch (getEmitModuleKind(options)) {
    case 0 /* None */:
    case 4 /* System */:
    case 3 /* UMD */:
      return false;
  }
  return true;
}
function unreachableCodeIsError(options) {
  return options.allowUnreachableCode ***REMOVED******REMOVED******REMOVED*** false;
}
function unusedLabelIsError(options) {
  return options.allowUnusedLabels ***REMOVED******REMOVED******REMOVED*** false;
}
function moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution) {
  return moduleResolution >***REMOVED*** 3 /* Node16 */ && moduleResolution <***REMOVED*** 99 /* NodeNext */ || moduleResolution ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */;
}
function moduleSupportsImportAttributes(moduleKind) {
  return 101 /* Node18 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */;
}
function getStrictOptionValue(compilerOptions, flag) {
  return compilerOptions[flag] ***REMOVED******REMOVED******REMOVED*** void 0 ? !!compilerOptions.strict : !!compilerOptions[flag];
}
function getNameOfScriptTarget(scriptTarget) {
  return forEachEntry(targetOptionDeclaration.type, (value, key) ***REMOVED***> value ***REMOVED******REMOVED******REMOVED*** scriptTarget ? key : void 0);
}
function getEmitStandardClassFields(compilerOptions) {
  return compilerOptions.useDefineForClassFields !***REMOVED******REMOVED*** false && getEmitScriptTarget(compilerOptions) >***REMOVED*** 9 /* ES2022 */;
}
function compilerOptionsAffectSemanticDiagnostics(newOptions, oldOptions) {
  return optionsHaveChanges(oldOptions, newOptions, semanticDiagnosticsOptionDeclarations);
}
function compilerOptionsAffectEmit(newOptions, oldOptions) {
  return optionsHaveChanges(oldOptions, newOptions, affectsEmitOptionDeclarations);
}
function compilerOptionsAffectDeclarationPath(newOptions, oldOptions) {
  return optionsHaveChanges(oldOptions, newOptions, affectsDeclarationPathOptionDeclarations);
}
function getCompilerOptionValue(options, option) {
  return option.strictFlag ? getStrictOptionValue(options, option.name) : option.allowJsFlag ? getAllowJSCompilerOption(options) : options[option.name];
}
function getJSXTransformEnabled(options) {
  const jsx ***REMOVED*** options.jsx;
  return jsx ***REMOVED******REMOVED******REMOVED*** 2 /* React */ || jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */;
}
function getJSXImplicitImportBase(compilerOptions, file) {
  const jsxImportSourcePragmas ***REMOVED*** file ***REMOVED******REMOVED*** null ? void 0 : file.pragmas.get("jsximportsource");
  const jsxImportSourcePragma ***REMOVED*** isArray(jsxImportSourcePragmas) ? jsxImportSourcePragmas[jsxImportSourcePragmas.length - 1] : jsxImportSourcePragmas;
  const jsxRuntimePragmas ***REMOVED*** file ***REMOVED******REMOVED*** null ? void 0 : file.pragmas.get("jsxruntime");
  const jsxRuntimePragma ***REMOVED*** isArray(jsxRuntimePragmas) ? jsxRuntimePragmas[jsxRuntimePragmas.length - 1] : jsxRuntimePragmas;
  if ((jsxRuntimePragma ***REMOVED******REMOVED*** null ? void 0 : jsxRuntimePragma.arguments.factory) ***REMOVED******REMOVED******REMOVED*** "classic") {
    return void 0;
  }
  return compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */ || compilerOptions.jsxImportSource || jsxImportSourcePragma || (jsxRuntimePragma ***REMOVED******REMOVED*** null ? void 0 : jsxRuntimePragma.arguments.factory) ***REMOVED******REMOVED******REMOVED*** "automatic" ? (jsxImportSourcePragma ***REMOVED******REMOVED*** null ? void 0 : jsxImportSourcePragma.arguments.factory) || compilerOptions.jsxImportSource || "react" : void 0;
}
function getJSXRuntimeImport(base, options) {
  return base ? `${base}/${options.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */ ? "jsx-dev-runtime" : "jsx-runtime"}` : void 0;
}
function hasZeroOrOneAsteriskCharacter(str) {
  let seenAsterisk ***REMOVED*** false;
  for (let i ***REMOVED*** 0; i < str.length; i++) {
    if (str.charCodeAt(i) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
      if (!seenAsterisk) {
        seenAsterisk ***REMOVED*** true;
      } else {
        return false;
      }
    }
  }
  return true;
}
function createSymlinkCache(cwd, getCanonicalFileName) {
  let symlinkedDirectories;
  let symlinkedDirectoriesByRealpath;
  let symlinkedFiles;
  let hasProcessedResolutions ***REMOVED*** false;
  return {
    getSymlinkedFiles: () ***REMOVED***> symlinkedFiles,
    getSymlinkedDirectories: () ***REMOVED***> symlinkedDirectories,
    getSymlinkedDirectoriesByRealpath: () ***REMOVED***> symlinkedDirectoriesByRealpath,
    setSymlinkedFile: (path, real) ***REMOVED***> (symlinkedFiles || (symlinkedFiles ***REMOVED*** /* @__PURE__ */ new Map())).set(path, real),
    setSymlinkedDirectory: (symlink, real) ***REMOVED***> {
      let symlinkPath ***REMOVED*** toPath(symlink, cwd, getCanonicalFileName);
      if (!containsIgnoredPath(symlinkPath)) {
        symlinkPath ***REMOVED*** ensureTrailingDirectorySeparator(symlinkPath);
        if (real !***REMOVED******REMOVED*** false && !(symlinkedDirectories ***REMOVED******REMOVED*** null ? void 0 : symlinkedDirectories.has(symlinkPath))) {
          (symlinkedDirectoriesByRealpath || (symlinkedDirectoriesByRealpath ***REMOVED*** createMultiMap())).add(real.realPath, symlink);
        }
        (symlinkedDirectories || (symlinkedDirectories ***REMOVED*** /* @__PURE__ */ new Map())).set(symlinkPath, real);
      }
    },
    setSymlinksFromResolutions(forEachResolvedModule, forEachResolvedTypeReferenceDirective, typeReferenceDirectives) {
      Debug.assert(!hasProcessedResolutions);
      hasProcessedResolutions ***REMOVED*** true;
      forEachResolvedModule((resolution) ***REMOVED***> processResolution(this, resolution.resolvedModule));
      forEachResolvedTypeReferenceDirective((resolution) ***REMOVED***> processResolution(this, resolution.resolvedTypeReferenceDirective));
      typeReferenceDirectives.forEach((resolution) ***REMOVED***> processResolution(this, resolution.resolvedTypeReferenceDirective));
    },
    hasProcessedResolutions: () ***REMOVED***> hasProcessedResolutions,
    setSymlinksFromResolution(resolution) {
      processResolution(this, resolution);
    },
    hasAnySymlinks
  };
  function hasAnySymlinks() {
    return !!(symlinkedFiles ***REMOVED******REMOVED*** null ? void 0 : symlinkedFiles.size) || !!symlinkedDirectories && !!forEachEntry(symlinkedDirectories, (value) ***REMOVED***> !!value);
  }
  function processResolution(cache, resolution) {
    if (!resolution || !resolution.originalPath || !resolution.resolvedFileName) return;
    const { resolvedFileName, originalPath } ***REMOVED*** resolution;
    cache.setSymlinkedFile(toPath(originalPath, cwd, getCanonicalFileName), resolvedFileName);
    const [commonResolved, commonOriginal] ***REMOVED*** guessDirectorySymlink(resolvedFileName, originalPath, cwd, getCanonicalFileName) || emptyArray;
    if (commonResolved && commonOriginal) {
      cache.setSymlinkedDirectory(
        commonOriginal,
        {
          real: ensureTrailingDirectorySeparator(commonResolved),
          realPath: ensureTrailingDirectorySeparator(toPath(commonResolved, cwd, getCanonicalFileName))
        }
      );
    }
  }
}
function guessDirectorySymlink(a, b, cwd, getCanonicalFileName) {
  const aParts ***REMOVED*** getPathComponents(getNormalizedAbsolutePath(a, cwd));
  const bParts ***REMOVED*** getPathComponents(getNormalizedAbsolutePath(b, cwd));
  let isDirectory ***REMOVED*** false;
  while (aParts.length >***REMOVED*** 2 && bParts.length >***REMOVED*** 2 && !isNodeModulesOrScopedPackageDirectory(aParts[aParts.length - 2], getCanonicalFileName) && !isNodeModulesOrScopedPackageDirectory(bParts[bParts.length - 2], getCanonicalFileName) && getCanonicalFileName(aParts[aParts.length - 1]) ***REMOVED******REMOVED******REMOVED*** getCanonicalFileName(bParts[bParts.length - 1])) {
    aParts.pop();
    bParts.pop();
    isDirectory ***REMOVED*** true;
  }
  return isDirectory ? [getPathFromPathComponents(aParts), getPathFromPathComponents(bParts)] : void 0;
}
function isNodeModulesOrScopedPackageDirectory(s, getCanonicalFileName) {
  return s !***REMOVED******REMOVED*** void 0 && (getCanonicalFileName(s) ***REMOVED******REMOVED******REMOVED*** "node_modules" || startsWith(s, "@"));
}
var reservedCharacterPattern ***REMOVED*** /[^\w\s/]/g;
var wildcardCharCodes ***REMOVED*** [42 /* asterisk */, 63 /* question */];
var commonPackageFolders ***REMOVED*** ["node_modules", "bower_components", "jspm_packages"];
var implicitExcludePathRegexPattern ***REMOVED*** `(?!(?:${commonPackageFolders.join("|")})(?:/|$))`;
var filesMatcher ***REMOVED*** {
  /**
   * Matches any single directory segment unless it is the last segment and a .min.js file
   * Breakdown:
   *  [^./]                   # matches everything up to the first . character (excluding directory separators)
   *  (\\.(?!min\\.js$))?     # matches . characters but not if they are part of the .min.js file extension
   */
  singleAsteriskRegexFragment: "(?:[^./]|(?:\\.(?!min\\.js$))?)*",
  /**
   * Regex for the ** wildcard. Matches any number of subdirectories. When used for including
   * files or directories, does not match subdirectories that start with a . character
   */
  doubleAsteriskRegexFragment: `(?:/${implicitExcludePathRegexPattern}[^/.][^/]*)*?`,
  replaceWildcardCharacter: (match) ***REMOVED***> replaceWildcardCharacter(match, filesMatcher.singleAsteriskRegexFragment)
};
var directoriesMatcher ***REMOVED*** {
  singleAsteriskRegexFragment: "[^/]*",
  /**
   * Regex for the ** wildcard. Matches any number of subdirectories. When used for including
   * files or directories, does not match subdirectories that start with a . character
   */
  doubleAsteriskRegexFragment: `(?:/${implicitExcludePathRegexPattern}[^/.][^/]*)*?`,
  replaceWildcardCharacter: (match) ***REMOVED***> replaceWildcardCharacter(match, directoriesMatcher.singleAsteriskRegexFragment)
};
var excludeMatcher ***REMOVED*** {
  singleAsteriskRegexFragment: "[^/]*",
  doubleAsteriskRegexFragment: "(?:/.+?)?",
  replaceWildcardCharacter: (match) ***REMOVED***> replaceWildcardCharacter(match, excludeMatcher.singleAsteriskRegexFragment)
};
var wildcardMatchers ***REMOVED*** {
  files: filesMatcher,
  directories: directoriesMatcher,
  exclude: excludeMatcher
};
function getRegularExpressionForWildcard(specs, basePath, usage) {
  const patterns ***REMOVED*** getRegularExpressionsForWildcards(specs, basePath, usage);
  if (!patterns || !patterns.length) {
    return void 0;
  }
  const pattern ***REMOVED*** patterns.map((pattern2) ***REMOVED***> `(?:${pattern2})`).join("|");
  const terminator ***REMOVED*** usage ***REMOVED******REMOVED******REMOVED*** "exclude" ? "(?:$|/)" : "$";
  return `^(?:${pattern})${terminator}`;
}
function getRegularExpressionsForWildcards(specs, basePath, usage) {
  if (specs ***REMOVED******REMOVED******REMOVED*** void 0 || specs.length ***REMOVED******REMOVED******REMOVED*** 0) {
    return void 0;
  }
  return flatMap(specs, (spec) ***REMOVED***> spec && getSubPatternFromSpec(spec, basePath, usage, wildcardMatchers[usage]));
}
function isImplicitGlob(lastPathComponent) {
  return !/[.*?]/.test(lastPathComponent);
}
function getPatternFromSpec(spec, basePath, usage) {
  const pattern ***REMOVED*** spec && getSubPatternFromSpec(spec, basePath, usage, wildcardMatchers[usage]);
  return pattern && `^(?:${pattern})${usage ***REMOVED******REMOVED******REMOVED*** "exclude" ? "(?:$|/)" : "$"}`;
}
function getSubPatternFromSpec(spec, basePath, usage, { singleAsteriskRegexFragment, doubleAsteriskRegexFragment, replaceWildcardCharacter: replaceWildcardCharacter2 } ***REMOVED*** wildcardMatchers[usage]) {
  let subpattern ***REMOVED*** "";
  let hasWrittenComponent ***REMOVED*** false;
  const components ***REMOVED*** getNormalizedPathComponents(spec, basePath);
  const lastComponent ***REMOVED*** last(components);
  if (usage !***REMOVED******REMOVED*** "exclude" && lastComponent ***REMOVED******REMOVED******REMOVED*** "**") {
    return void 0;
  }
  components[0] ***REMOVED*** removeTrailingDirectorySeparator(components[0]);
  if (isImplicitGlob(lastComponent)) {
    components.push("**", "*");
  }
  let optionalCount ***REMOVED*** 0;
  for (let component of components) {
    if (component ***REMOVED******REMOVED******REMOVED*** "**") {
      subpattern +***REMOVED*** doubleAsteriskRegexFragment;
    } else {
      if (usage ***REMOVED******REMOVED******REMOVED*** "directories") {
        subpattern +***REMOVED*** "(?:";
        optionalCount++;
      }
      if (hasWrittenComponent) {
        subpattern +***REMOVED*** directorySeparator;
      }
      if (usage !***REMOVED******REMOVED*** "exclude") {
        let componentPattern ***REMOVED*** "";
        if (component.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */) {
          componentPattern +***REMOVED*** "(?:[^./]" + singleAsteriskRegexFragment + ")?";
          component ***REMOVED*** component.substr(1);
        } else if (component.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 63 /* question */) {
          componentPattern +***REMOVED*** "[^./]";
          component ***REMOVED*** component.substr(1);
        }
        componentPattern +***REMOVED*** component.replace(reservedCharacterPattern, replaceWildcardCharacter2);
        if (componentPattern !***REMOVED******REMOVED*** component) {
          subpattern +***REMOVED*** implicitExcludePathRegexPattern;
        }
        subpattern +***REMOVED*** componentPattern;
      } else {
        subpattern +***REMOVED*** component.replace(reservedCharacterPattern, replaceWildcardCharacter2);
      }
    }
    hasWrittenComponent ***REMOVED*** true;
  }
  while (optionalCount > 0) {
    subpattern +***REMOVED*** ")?";
    optionalCount--;
  }
  return subpattern;
}
function replaceWildcardCharacter(match, singleAsteriskRegexFragment) {
  return match ***REMOVED******REMOVED******REMOVED*** "*" ? singleAsteriskRegexFragment : match ***REMOVED******REMOVED******REMOVED*** "?" ? "[^/]" : "\\" + match;
}
function getFileMatcherPatterns(path, excludes, includes, useCaseSensitiveFileNames2, currentDirectory) {
  path ***REMOVED*** normalizePath(path);
  currentDirectory ***REMOVED*** normalizePath(currentDirectory);
  const absolutePath ***REMOVED*** combinePaths(currentDirectory, path);
  return {
    includeFilePatterns: map(getRegularExpressionsForWildcards(includes, absolutePath, "files"), (pattern) ***REMOVED***> `^${pattern}$`),
    includeFilePattern: getRegularExpressionForWildcard(includes, absolutePath, "files"),
    includeDirectoryPattern: getRegularExpressionForWildcard(includes, absolutePath, "directories"),
    excludePattern: getRegularExpressionForWildcard(excludes, absolutePath, "exclude"),
    basePaths: getBasePaths(path, includes, useCaseSensitiveFileNames2)
  };
}
function getRegexFromPattern(pattern, useCaseSensitiveFileNames2) {
  return new RegExp(pattern, useCaseSensitiveFileNames2 ? "" : "i");
}
function matchFiles(path, extensions, excludes, includes, useCaseSensitiveFileNames2, currentDirectory, depth, getFileSystemEntries, realpath) {
  path ***REMOVED*** normalizePath(path);
  currentDirectory ***REMOVED*** normalizePath(currentDirectory);
  const patterns ***REMOVED*** getFileMatcherPatterns(path, excludes, includes, useCaseSensitiveFileNames2, currentDirectory);
  const includeFileRegexes ***REMOVED*** patterns.includeFilePatterns && patterns.includeFilePatterns.map((pattern) ***REMOVED***> getRegexFromPattern(pattern, useCaseSensitiveFileNames2));
  const includeDirectoryRegex ***REMOVED*** patterns.includeDirectoryPattern && getRegexFromPattern(patterns.includeDirectoryPattern, useCaseSensitiveFileNames2);
  const excludeRegex ***REMOVED*** patterns.excludePattern && getRegexFromPattern(patterns.excludePattern, useCaseSensitiveFileNames2);
  const results ***REMOVED*** includeFileRegexes ? includeFileRegexes.map(() ***REMOVED***> []) : [[]];
  const visited ***REMOVED*** /* @__PURE__ */ new Map();
  const toCanonical ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  for (const basePath of patterns.basePaths) {
    visitDirectory(basePath, combinePaths(currentDirectory, basePath), depth);
  }
  return flatten(results);
  function visitDirectory(path2, absolutePath, depth2) {
    const canonicalPath ***REMOVED*** toCanonical(realpath(absolutePath));
    if (visited.has(canonicalPath)) return;
    visited.set(canonicalPath, true);
    const { files, directories } ***REMOVED*** getFileSystemEntries(path2);
    for (const current of toSorted(files, compareStringsCaseSensitive)) {
      const name ***REMOVED*** combinePaths(path2, current);
      const absoluteName ***REMOVED*** combinePaths(absolutePath, current);
      if (extensions && !fileExtensionIsOneOf(name, extensions)) continue;
      if (excludeRegex && excludeRegex.test(absoluteName)) continue;
      if (!includeFileRegexes) {
        results[0].push(name);
      } else {
        const includeIndex ***REMOVED*** findIndex(includeFileRegexes, (re) ***REMOVED***> re.test(absoluteName));
        if (includeIndex !***REMOVED******REMOVED*** -1) {
          results[includeIndex].push(name);
        }
      }
    }
    if (depth2 !***REMOVED******REMOVED*** void 0) {
      depth2--;
      if (depth2 ***REMOVED******REMOVED******REMOVED*** 0) {
        return;
      }
    }
    for (const current of toSorted(directories, compareStringsCaseSensitive)) {
      const name ***REMOVED*** combinePaths(path2, current);
      const absoluteName ***REMOVED*** combinePaths(absolutePath, current);
      if ((!includeDirectoryRegex || includeDirectoryRegex.test(absoluteName)) && (!excludeRegex || !excludeRegex.test(absoluteName))) {
        visitDirectory(name, absoluteName, depth2);
      }
    }
  }
}
function getBasePaths(path, includes, useCaseSensitiveFileNames2) {
  const basePaths ***REMOVED*** [path];
  if (includes) {
    const includeBasePaths ***REMOVED*** [];
    for (const include of includes) {
      const absolute ***REMOVED*** isRootedDiskPath(include) ? include : normalizePath(combinePaths(path, include));
      includeBasePaths.push(getIncludeBasePath(absolute));
    }
    includeBasePaths.sort(getStringComparer(!useCaseSensitiveFileNames2));
    for (const includeBasePath of includeBasePaths) {
      if (every(basePaths, (basePath) ***REMOVED***> !containsPath(basePath, includeBasePath, path, !useCaseSensitiveFileNames2))) {
        basePaths.push(includeBasePath);
      }
    }
  }
  return basePaths;
}
function getIncludeBasePath(absolute) {
  const wildcardOffset ***REMOVED*** indexOfAnyCharCode(absolute, wildcardCharCodes);
  if (wildcardOffset < 0) {
    return !hasExtension(absolute) ? absolute : removeTrailingDirectorySeparator(getDirectoryPath(absolute));
  }
  return absolute.substring(0, absolute.lastIndexOf(directorySeparator, wildcardOffset));
}
function ensureScriptKind(fileName, scriptKind) {
  return scriptKind || getScriptKindFromFileName(fileName) || 3 /* TS */;
}
function getScriptKindFromFileName(fileName) {
  const ext ***REMOVED*** fileName.substr(fileName.lastIndexOf("."));
  switch (ext.toLowerCase()) {
    case ".js" /* Js */:
    case ".cjs" /* Cjs */:
    case ".mjs" /* Mjs */:
      return 1 /* JS */;
    case ".jsx" /* Jsx */:
      return 2 /* JSX */;
    case ".ts" /* Ts */:
    case ".cts" /* Cts */:
    case ".mts" /* Mts */:
      return 3 /* TS */;
    case ".tsx" /* Tsx */:
      return 4 /* TSX */;
    case ".json" /* Json */:
      return 6 /* JSON */;
    default:
      return 0 /* Unknown */;
  }
}
var supportedTSExtensions ***REMOVED*** [[".ts" /* Ts */, ".tsx" /* Tsx */, ".d.ts" /* Dts */], [".cts" /* Cts */, ".d.cts" /* Dcts */], [".mts" /* Mts */, ".d.mts" /* Dmts */]];
var supportedTSExtensionsFlat ***REMOVED*** flatten(supportedTSExtensions);
var supportedTSExtensionsWithJson ***REMOVED*** [...supportedTSExtensions, [".json" /* Json */]];
var supportedTSExtensionsForExtractExtension ***REMOVED*** [".d.ts" /* Dts */, ".d.cts" /* Dcts */, ".d.mts" /* Dmts */, ".cts" /* Cts */, ".mts" /* Mts */, ".ts" /* Ts */, ".tsx" /* Tsx */];
var supportedJSExtensions ***REMOVED*** [[".js" /* Js */, ".jsx" /* Jsx */], [".mjs" /* Mjs */], [".cjs" /* Cjs */]];
var supportedJSExtensionsFlat ***REMOVED*** flatten(supportedJSExtensions);
var allSupportedExtensions ***REMOVED*** [[".ts" /* Ts */, ".tsx" /* Tsx */, ".d.ts" /* Dts */, ".js" /* Js */, ".jsx" /* Jsx */], [".cts" /* Cts */, ".d.cts" /* Dcts */, ".cjs" /* Cjs */], [".mts" /* Mts */, ".d.mts" /* Dmts */, ".mjs" /* Mjs */]];
var allSupportedExtensionsWithJson ***REMOVED*** [...allSupportedExtensions, [".json" /* Json */]];
var supportedDeclarationExtensions ***REMOVED*** [".d.ts" /* Dts */, ".d.cts" /* Dcts */, ".d.mts" /* Dmts */];
var supportedTSImplementationExtensions ***REMOVED*** [".ts" /* Ts */, ".cts" /* Cts */, ".mts" /* Mts */, ".tsx" /* Tsx */];
var extensionsNotSupportingExtensionlessResolution ***REMOVED*** [".mts" /* Mts */, ".d.mts" /* Dmts */, ".mjs" /* Mjs */, ".cts" /* Cts */, ".d.cts" /* Dcts */, ".cjs" /* Cjs */];
function getSupportedExtensions(options, extraFileExtensions) {
  const needJsExtensions ***REMOVED*** options && getAllowJSCompilerOption(options);
  if (!extraFileExtensions || extraFileExtensions.length ***REMOVED******REMOVED******REMOVED*** 0) {
    return needJsExtensions ? allSupportedExtensions : supportedTSExtensions;
  }
  const builtins ***REMOVED*** needJsExtensions ? allSupportedExtensions : supportedTSExtensions;
  const flatBuiltins ***REMOVED*** flatten(builtins);
  const extensions ***REMOVED*** [
    ...builtins,
    ...mapDefined(extraFileExtensions, (x) ***REMOVED***> x.scriptKind ***REMOVED******REMOVED******REMOVED*** 7 /* Deferred */ || needJsExtensions && isJSLike(x.scriptKind) && !flatBuiltins.includes(x.extension) ? [x.extension] : void 0)
  ];
  return extensions;
}
function getSupportedExtensionsWithJsonIfResolveJsonModule(options, supportedExtensions) {
  if (!options || !getResolveJsonModule(options)) return supportedExtensions;
  if (supportedExtensions ***REMOVED******REMOVED******REMOVED*** allSupportedExtensions) return allSupportedExtensionsWithJson;
  if (supportedExtensions ***REMOVED******REMOVED******REMOVED*** supportedTSExtensions) return supportedTSExtensionsWithJson;
  return [...supportedExtensions, [".json" /* Json */]];
}
function isJSLike(scriptKind) {
  return scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */;
}
function hasJSFileExtension(fileName) {
  return some(supportedJSExtensionsFlat, (extension) ***REMOVED***> fileExtensionIs(fileName, extension));
}
function hasTSFileExtension(fileName) {
  return some(supportedTSExtensionsFlat, (extension) ***REMOVED***> fileExtensionIs(fileName, extension));
}
function hasImplementationTSFileExtension(fileName) {
  return some(supportedTSImplementationExtensions, (extension) ***REMOVED***> fileExtensionIs(fileName, extension)) && !isDeclarationFileName(fileName);
}
function usesExtensionsOnImports({ imports }, hasExtension2 ***REMOVED*** or(hasJSFileExtension, hasTSFileExtension)) {
  return firstDefined(imports, ({ text }) ***REMOVED***> pathIsRelative(text) && !fileExtensionIsOneOf(text, extensionsNotSupportingExtensionlessResolution) ? hasExtension2(text) : void 0) || false;
}
function getModuleSpecifierEndingPreference(preference, resolutionMode, compilerOptions, sourceFile) {
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(compilerOptions);
  const moduleResolutionIsNodeNext ***REMOVED*** 3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */;
  if (preference ***REMOVED******REMOVED******REMOVED*** "js" || resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && moduleResolutionIsNodeNext) {
    if (!shouldAllowImportingTsExtension(compilerOptions)) {
      return 2 /* JsExtension */;
    }
    return inferPreference() !***REMOVED******REMOVED*** 2 /* JsExtension */ ? 3 /* TsExtension */ : 2 /* JsExtension */;
  }
  if (preference ***REMOVED******REMOVED******REMOVED*** "minimal") {
    return 0 /* Minimal */;
  }
  if (preference ***REMOVED******REMOVED******REMOVED*** "index") {
    return 1 /* Index */;
  }
  if (!shouldAllowImportingTsExtension(compilerOptions)) {
    return sourceFile && usesExtensionsOnImports(sourceFile) ? 2 /* JsExtension */ : 0 /* Minimal */;
  }
  return inferPreference();
  function inferPreference() {
    let usesJsExtensions ***REMOVED*** false;
    const specifiers ***REMOVED*** (sourceFile ***REMOVED******REMOVED*** null ? void 0 : sourceFile.imports.length) ? sourceFile.imports : sourceFile && isSourceFileJS(sourceFile) ? getRequiresAtTopOfFile(sourceFile).map((r) ***REMOVED***> r.arguments[0]) : emptyArray;
    for (const specifier of specifiers) {
      if (pathIsRelative(specifier.text)) {
        if (moduleResolutionIsNodeNext && resolutionMode ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && getModeForUsageLocation(sourceFile, specifier, compilerOptions) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */) {
          continue;
        }
        if (fileExtensionIsOneOf(specifier.text, extensionsNotSupportingExtensionlessResolution)) {
          continue;
        }
        if (hasTSFileExtension(specifier.text)) {
          return 3 /* TsExtension */;
        }
        if (hasJSFileExtension(specifier.text)) {
          usesJsExtensions ***REMOVED*** true;
        }
      }
    }
    return usesJsExtensions ? 2 /* JsExtension */ : 0 /* Minimal */;
  }
}
function getRequiresAtTopOfFile(sourceFile) {
  let nonRequireStatementCount ***REMOVED*** 0;
  let requires;
  for (const statement of sourceFile.statements) {
    if (nonRequireStatementCount > 3) {
      break;
    }
    if (isRequireVariableStatement(statement)) {
      requires ***REMOVED*** concatenate(requires, statement.declarationList.declarations.map((d) ***REMOVED***> d.initializer));
    } else if (isExpressionStatement(statement) && isRequireCall(
      statement.expression,
      /*requireStringLiteralLikeArgument*/
      true
    )) {
      requires ***REMOVED*** append(requires, statement.expression);
    } else {
      nonRequireStatementCount++;
    }
  }
  return requires || emptyArray;
}
function isSupportedSourceFileName(fileName, compilerOptions, extraFileExtensions) {
  if (!fileName) return false;
  const supportedExtensions ***REMOVED*** getSupportedExtensions(compilerOptions, extraFileExtensions);
  for (const extension of flatten(getSupportedExtensionsWithJsonIfResolveJsonModule(compilerOptions, supportedExtensions))) {
    if (fileExtensionIs(fileName, extension)) {
      return true;
    }
  }
  return false;
}
function numberOfDirectorySeparators(str) {
  const match ***REMOVED*** str.match(/\//g);
  return match ? match.length : 0;
}
function compareNumberOfDirectorySeparators(path1, path2) {
  return compareValues(
    numberOfDirectorySeparators(path1),
    numberOfDirectorySeparators(path2)
  );
}
var extensionsToRemove ***REMOVED*** [".d.ts" /* Dts */, ".d.mts" /* Dmts */, ".d.cts" /* Dcts */, ".mjs" /* Mjs */, ".mts" /* Mts */, ".cjs" /* Cjs */, ".cts" /* Cts */, ".ts" /* Ts */, ".js" /* Js */, ".tsx" /* Tsx */, ".jsx" /* Jsx */, ".json" /* Json */];
function removeFileExtension(path) {
  for (const ext of extensionsToRemove) {
    const extensionless ***REMOVED*** tryRemoveExtension(path, ext);
    if (extensionless !***REMOVED******REMOVED*** void 0) {
      return extensionless;
    }
  }
  return path;
}
function tryRemoveExtension(path, extension) {
  return fileExtensionIs(path, extension) ? removeExtension(path, extension) : void 0;
}
function removeExtension(path, extension) {
  return path.substring(0, path.length - extension.length);
}
function changeExtension(path, newExtension) {
  return changeAnyExtension(
    path,
    newExtension,
    extensionsToRemove,
    /*ignoreCase*/
    false
  );
}
function tryParsePattern(pattern) {
  const indexOfStar ***REMOVED*** pattern.indexOf("*");
  if (indexOfStar ***REMOVED******REMOVED******REMOVED*** -1) {
    return pattern;
  }
  return pattern.indexOf("*", indexOfStar + 1) !***REMOVED******REMOVED*** -1 ? void 0 : {
    prefix: pattern.substr(0, indexOfStar),
    suffix: pattern.substr(indexOfStar + 1)
  };
}
var parsedPatternsCache ***REMOVED*** /* @__PURE__ */ new WeakMap();
function tryParsePatterns(paths) {
  let result ***REMOVED*** parsedPatternsCache.get(paths);
  if (result !***REMOVED******REMOVED*** void 0) {
    return result;
  }
  let matchableStringSet;
  let patterns;
  const pathList ***REMOVED*** getOwnKeys(paths);
  for (const path of pathList) {
    const patternOrStr ***REMOVED*** tryParsePattern(path);
    if (patternOrStr ***REMOVED******REMOVED******REMOVED*** void 0) {
      continue;
    } else if (typeof patternOrStr ***REMOVED******REMOVED******REMOVED*** "string") {
      (matchableStringSet ?? (matchableStringSet ***REMOVED*** /* @__PURE__ */ new Set())).add(patternOrStr);
    } else {
      (patterns ?? (patterns ***REMOVED*** [])).push(patternOrStr);
    }
  }
  parsedPatternsCache.set(
    paths,
    result ***REMOVED*** {
      matchableStringSet,
      patterns
    }
  );
  return result;
}
function positionIsSynthesized(pos) {
  return !(pos >***REMOVED*** 0);
}
function extensionIsTS(ext) {
  return ext ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || ext ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */ || ext ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */ || ext ***REMOVED******REMOVED******REMOVED*** ".cts" /* Cts */ || ext ***REMOVED******REMOVED******REMOVED*** ".mts" /* Mts */ || ext ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */ || ext ***REMOVED******REMOVED******REMOVED*** ".d.cts" /* Dcts */ || startsWith(ext, ".d.") && endsWith(ext, ".ts");
}
function resolutionExtensionIsTSOrJson(ext) {
  return extensionIsTS(ext) || ext ***REMOVED******REMOVED******REMOVED*** ".json" /* Json */;
}
function extensionFromPath(path) {
  const ext ***REMOVED*** tryGetExtensionFromPath2(path);
  return ext !***REMOVED******REMOVED*** void 0 ? ext : Debug.fail(`File ${path} has unknown extension.`);
}
function tryGetExtensionFromPath2(path) {
  return find(extensionsToRemove, (e) ***REMOVED***> fileExtensionIs(path, e));
}
function isCheckJsEnabledForFile(sourceFile, compilerOptions) {
  return sourceFile.checkJsDirective ? sourceFile.checkJsDirective.enabled : compilerOptions.checkJs;
}
var emptyFileSystemEntries ***REMOVED*** {
  files: emptyArray,
  directories: emptyArray
};
function matchPatternOrExact(parsedPatterns, candidate) {
  const { matchableStringSet, patterns } ***REMOVED*** parsedPatterns;
  if (matchableStringSet ***REMOVED******REMOVED*** null ? void 0 : matchableStringSet.has(candidate)) {
    return candidate;
  }
  if (patterns ***REMOVED******REMOVED******REMOVED*** void 0 || patterns.length ***REMOVED******REMOVED******REMOVED*** 0) {
    return void 0;
  }
  return findBestPatternMatch(patterns, (_) ***REMOVED***> _, candidate);
}
function sliceAfter(arr, value) {
  const index ***REMOVED*** arr.indexOf(value);
  Debug.assert(index !***REMOVED******REMOVED*** -1);
  return arr.slice(index);
}
function addRelatedInfo(diagnostic, ...relatedInformation) {
  if (!relatedInformation.length) {
    return diagnostic;
  }
  if (!diagnostic.relatedInformation) {
    diagnostic.relatedInformation ***REMOVED*** [];
  }
  Debug.assert(diagnostic.relatedInformation !***REMOVED******REMOVED*** emptyArray, "Diagnostic had empty array singleton for related info, but is still being constructed!");
  diagnostic.relatedInformation.push(...relatedInformation);
  return diagnostic;
}
function minAndMax(arr, getValue) {
  Debug.assert(arr.length !***REMOVED******REMOVED*** 0);
  let min2 ***REMOVED*** getValue(arr[0]);
  let max ***REMOVED*** min2;
  for (let i ***REMOVED*** 1; i < arr.length; i++) {
    const value ***REMOVED*** getValue(arr[i]);
    if (value < min2) {
      min2 ***REMOVED*** value;
    } else if (value > max) {
      max ***REMOVED*** value;
    }
  }
  return { min: min2, max };
}
function rangeOfNode(node) {
  return { pos: getTokenPosOfNode(node), end: node.end };
}
function rangeOfTypeParameters(sourceFile, typeParameters) {
  const pos ***REMOVED*** typeParameters.pos - 1;
  const end ***REMOVED*** Math.min(sourceFile.text.length, skipTrivia(sourceFile.text, typeParameters.end) + 1);
  return { pos, end };
}
function skipTypeChecking(sourceFile, options, host) {
  return skipTypeCheckingWorker(
    sourceFile,
    options,
    host,
    /*ignoreNoCheck*/
    false
  );
}
function skipTypeCheckingIgnoringNoCheck(sourceFile, options, host) {
  return skipTypeCheckingWorker(
    sourceFile,
    options,
    host,
    /*ignoreNoCheck*/
    true
  );
}
function skipTypeCheckingWorker(sourceFile, options, host, ignoreNoCheck) {
  return options.skipLibCheck && sourceFile.isDeclarationFile || options.skipDefaultLibCheck && sourceFile.hasNoDefaultLib || !ignoreNoCheck && options.noCheck || host.isSourceOfProjectReferenceRedirect(sourceFile.fileName) || !canIncludeBindAndCheckDiagnostics(sourceFile, options);
}
function canIncludeBindAndCheckDiagnostics(sourceFile, options) {
  if (!!sourceFile.checkJsDirective && sourceFile.checkJsDirective.enabled ***REMOVED******REMOVED******REMOVED*** false) return false;
  if (sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 3 /* TS */ || sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 4 /* TSX */ || sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 5 /* External */) return true;
  const isJs ***REMOVED*** sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */;
  const isCheckJs ***REMOVED*** isJs && isCheckJsEnabledForFile(sourceFile, options);
  const isPlainJs ***REMOVED*** isPlainJsFile(sourceFile, options.checkJs);
  return isPlainJs || isCheckJs || sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 7 /* Deferred */;
}
function isJsonEqual(a, b) {
  return a ***REMOVED******REMOVED******REMOVED*** b || typeof a ***REMOVED******REMOVED******REMOVED*** "object" && a !***REMOVED******REMOVED*** null && typeof b ***REMOVED******REMOVED******REMOVED*** "object" && b !***REMOVED******REMOVED*** null && equalOwnProperties(a, b, isJsonEqual);
}
function parsePseudoBigInt(stringValue) {
  let log2Base;
  switch (stringValue.charCodeAt(1)) {
    // "x" in "0x123"
    case 98 /* b */:
    case 66 /* B */:
      log2Base ***REMOVED*** 1;
      break;
    case 111 /* o */:
    case 79 /* O */:
      log2Base ***REMOVED*** 3;
      break;
    case 120 /* x */:
    case 88 /* X */:
      log2Base ***REMOVED*** 4;
      break;
    default:
      const nIndex ***REMOVED*** stringValue.length - 1;
      let nonZeroStart ***REMOVED*** 0;
      while (stringValue.charCodeAt(nonZeroStart) ***REMOVED******REMOVED******REMOVED*** 48 /* _0 */) {
        nonZeroStart++;
      }
      return stringValue.slice(nonZeroStart, nIndex) || "0";
  }
  const startIndex ***REMOVED*** 2, endIndex ***REMOVED*** stringValue.length - 1;
  const bitsNeeded ***REMOVED*** (endIndex - startIndex) * log2Base;
  const segments ***REMOVED*** new Uint16Array((bitsNeeded >>> 4) + (bitsNeeded & 15 ? 1 : 0));
  for (let i ***REMOVED*** endIndex - 1, bitOffset ***REMOVED*** 0; i >***REMOVED*** startIndex; i--, bitOffset +***REMOVED*** log2Base) {
    const segment ***REMOVED*** bitOffset >>> 4;
    const digitChar ***REMOVED*** stringValue.charCodeAt(i);
    const digit ***REMOVED*** digitChar <***REMOVED*** 57 /* _9 */ ? digitChar - 48 /* _0 */ : 10 + digitChar - (digitChar <***REMOVED*** 70 /* F */ ? 65 /* A */ : 97 /* a */);
    const shiftedDigit ***REMOVED*** digit << (bitOffset & 15);
    segments[segment] |***REMOVED*** shiftedDigit;
    const residual ***REMOVED*** shiftedDigit >>> 16;
    if (residual) segments[segment + 1] |***REMOVED*** residual;
  }
  let base10Value ***REMOVED*** "";
  let firstNonzeroSegment ***REMOVED*** segments.length - 1;
  let segmentsRemaining ***REMOVED*** true;
  while (segmentsRemaining) {
    let mod10 ***REMOVED*** 0;
    segmentsRemaining ***REMOVED*** false;
    for (let segment ***REMOVED*** firstNonzeroSegment; segment >***REMOVED*** 0; segment--) {
      const newSegment ***REMOVED*** mod10 << 16 | segments[segment];
      const segmentValue ***REMOVED*** newSegment / 10 | 0;
      segments[segment] ***REMOVED*** segmentValue;
      mod10 ***REMOVED*** newSegment - segmentValue * 10;
      if (segmentValue && !segmentsRemaining) {
        firstNonzeroSegment ***REMOVED*** segment;
        segmentsRemaining ***REMOVED*** true;
      }
    }
    base10Value ***REMOVED*** mod10 + base10Value;
  }
  return base10Value;
}
function pseudoBigIntToString({ negative, base10Value }) {
  return (negative && base10Value !***REMOVED******REMOVED*** "0" ? "-" : "") + base10Value;
}
function parseValidBigInt(text) {
  const negative ***REMOVED*** text.startsWith("-");
  const base10Value ***REMOVED*** parsePseudoBigInt(`${negative ? text.slice(1) : text}n`);
  return { negative, base10Value };
}
function isValidBigIntString(s, roundTripOnly) {
  if (s ***REMOVED******REMOVED******REMOVED*** "") return false;
  const scanner ***REMOVED*** createScanner(
    99 /* ESNext */,
    /*skipTrivia*/
    false
  );
  let success ***REMOVED*** true;
  scanner.setOnError(() ***REMOVED***> success ***REMOVED*** false);
  scanner.setText(s + "n");
  let result ***REMOVED*** scanner.scan();
  const negative ***REMOVED*** result ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */;
  if (negative) {
    result ***REMOVED*** scanner.scan();
  }
  const flags ***REMOVED*** scanner.getTokenFlags();
  return success && result ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ && scanner.getTokenEnd() ***REMOVED******REMOVED******REMOVED*** s.length + 1 && !(flags & 512 /* ContainsSeparator */) && (!roundTripOnly || s ***REMOVED******REMOVED******REMOVED*** pseudoBigIntToString({ negative, base10Value: parsePseudoBigInt(scanner.getTokenValue()) }));
}
function isValidTypeOnlyAliasUseSite(useSite) {
  return !!(useSite.flags & 33554432 /* Ambient */) || isInJSDoc(useSite) || isPartOfTypeQuery(useSite) || isIdentifierInNonEmittingHeritageClause(useSite) || isPartOfPossiblyValidTypeOrAbstractComputedPropertyName(useSite) || !(isExpressionNode(useSite) || isShorthandPropertyNameUseSite(useSite));
}
function isShorthandPropertyNameUseSite(useSite) {
  return isIdentifier(useSite) && isShorthandPropertyAssignment(useSite.parent) && useSite.parent.name ***REMOVED******REMOVED******REMOVED*** useSite;
}
function isPartOfPossiblyValidTypeOrAbstractComputedPropertyName(node) {
  while (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
    node ***REMOVED*** node.parent;
  }
  if (node.kind !***REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
    return false;
  }
  if (hasSyntacticModifier(node.parent, 64 /* Abstract */)) {
    return true;
  }
  const containerKind ***REMOVED*** node.parent.parent.kind;
  return containerKind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || containerKind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */;
}
function isIdentifierInNonEmittingHeritageClause(node) {
  if (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */) return false;
  const heritageClause ***REMOVED*** findAncestor(node.parent, (parent) ***REMOVED***> {
    switch (parent.kind) {
      case 299 /* HeritageClause */:
        return true;
      case 212 /* PropertyAccessExpression */:
      case 234 /* ExpressionWithTypeArguments */:
        return false;
      default:
        return "quit";
    }
  });
  return (heritageClause ***REMOVED******REMOVED*** null ? void 0 : heritageClause.token) ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */ || (heritageClause ***REMOVED******REMOVED*** null ? void 0 : heritageClause.parent.kind) ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */;
}
function isIdentifierTypeReference(node) {
  return isTypeReferenceNode(node) && isIdentifier(node.typeName);
}
function arrayIsHomogeneous(array, comparer ***REMOVED*** equateValues) {
  if (array.length < 2) return true;
  const first2 ***REMOVED*** array[0];
  for (let i ***REMOVED*** 1, length2 ***REMOVED*** array.length; i < length2; i++) {
    const target ***REMOVED*** array[i];
    if (!comparer(first2, target)) return false;
  }
  return true;
}
function setTextRangePos(range, pos) {
  range.pos ***REMOVED*** pos;
  return range;
}
function setTextRangeEnd(range, end) {
  range.end ***REMOVED*** end;
  return range;
}
function setTextRangePosEnd(range, pos, end) {
  return setTextRangeEnd(setTextRangePos(range, pos), end);
}
function setTextRangePosWidth(range, pos, width) {
  return setTextRangePosEnd(range, pos, pos + width);
}
function setNodeFlags(node, newFlags) {
  if (node) {
    node.flags ***REMOVED*** newFlags;
  }
  return node;
}
function setParent(child, parent) {
  if (child && parent) {
    child.parent ***REMOVED*** parent;
  }
  return child;
}
function setParentRecursive(rootNode, incremental) {
  if (!rootNode) return rootNode;
  forEachChildRecursively(rootNode, isJSDocNode(rootNode) ? bindParentToChildIgnoringJSDoc : bindParentToChild);
  return rootNode;
  function bindParentToChildIgnoringJSDoc(child, parent) {
    if (incremental && child.parent ***REMOVED******REMOVED******REMOVED*** parent) {
      return "skip";
    }
    setParent(child, parent);
  }
  function bindJSDoc(child) {
    if (hasJSDocNodes(child)) {
      for (const doc of child.jsDoc) {
        bindParentToChildIgnoringJSDoc(doc, child);
        forEachChildRecursively(doc, bindParentToChildIgnoringJSDoc);
      }
    }
  }
  function bindParentToChild(child, parent) {
    return bindParentToChildIgnoringJSDoc(child, parent) || bindJSDoc(child);
  }
}
function isPackedElement(node) {
  return !isOmittedExpression(node);
}
function isPackedArrayLiteral(node) {
  return isArrayLiteralExpression(node) && every(node.elements, isPackedElement);
}
function expressionResultIsUnused(node) {
  Debug.assertIsDefined(node.parent);
  while (true) {
    const parent ***REMOVED*** node.parent;
    if (isParenthesizedExpression(parent)) {
      node ***REMOVED*** parent;
      continue;
    }
    if (isExpressionStatement(parent) || isVoidExpression(parent) || isForStatement(parent) && (parent.initializer ***REMOVED******REMOVED******REMOVED*** node || parent.incrementor ***REMOVED******REMOVED******REMOVED*** node)) {
      return true;
    }
    if (isCommaListExpression(parent)) {
      if (node !***REMOVED******REMOVED*** last(parent.elements)) return true;
      node ***REMOVED*** parent;
      continue;
    }
    if (isBinaryExpression(parent) && parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      if (node ***REMOVED******REMOVED******REMOVED*** parent.left) return true;
      node ***REMOVED*** parent;
      continue;
    }
    return false;
  }
}
function containsIgnoredPath(path) {
  return some(ignoredPaths, (p) ***REMOVED***> path.includes(p));
}
function getContainingNodeArray(node) {
  if (!node.parent) return void 0;
  switch (node.kind) {
    case 169 /* TypeParameter */:
      const { parent: parent2 } ***REMOVED*** node;
      return parent2.kind ***REMOVED******REMOVED******REMOVED*** 196 /* InferType */ ? void 0 : parent2.typeParameters;
    case 170 /* Parameter */:
      return node.parent.parameters;
    case 205 /* TemplateLiteralTypeSpan */:
      return node.parent.templateSpans;
    case 240 /* TemplateSpan */:
      return node.parent.templateSpans;
    case 171 /* Decorator */: {
      const { parent: parent3 } ***REMOVED*** node;
      return canHaveDecorators(parent3) ? parent3.modifiers : void 0;
    }
    case 299 /* HeritageClause */:
      return node.parent.heritageClauses;
  }
  const { parent } ***REMOVED*** node;
  if (isJSDocTag(node)) {
    return isJSDocTypeLiteral(node.parent) ? void 0 : node.parent.tags;
  }
  switch (parent.kind) {
    case 188 /* TypeLiteral */:
    case 265 /* InterfaceDeclaration */:
      return isTypeElement(node) ? parent.members : void 0;
    case 193 /* UnionType */:
    case 194 /* IntersectionType */:
      return parent.types;
    case 190 /* TupleType */:
    case 210 /* ArrayLiteralExpression */:
    case 357 /* CommaListExpression */:
    case 276 /* NamedImports */:
    case 280 /* NamedExports */:
      return parent.elements;
    case 211 /* ObjectLiteralExpression */:
    case 293 /* JsxAttributes */:
      return parent.properties;
    case 214 /* CallExpression */:
    case 215 /* NewExpression */:
      return isTypeNode(node) ? parent.typeArguments : parent.expression ***REMOVED******REMOVED******REMOVED*** node ? void 0 : parent.arguments;
    case 285 /* JsxElement */:
    case 289 /* JsxFragment */:
      return isJsxChild(node) ? parent.children : void 0;
    case 287 /* JsxOpeningElement */:
    case 286 /* JsxSelfClosingElement */:
      return isTypeNode(node) ? parent.typeArguments : void 0;
    case 242 /* Block */:
    case 297 /* CaseClause */:
    case 298 /* DefaultClause */:
    case 269 /* ModuleBlock */:
      return parent.statements;
    case 270 /* CaseBlock */:
      return parent.clauses;
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
      return isClassElement(node) ? parent.members : void 0;
    case 267 /* EnumDeclaration */:
      return isEnumMember(node) ? parent.members : void 0;
    case 308 /* SourceFile */:
      return parent.statements;
  }
}
function hasContextSensitiveParameters(node) {
  if (!node.typeParameters) {
    if (some(node.parameters, (p) ***REMOVED***> !getEffectiveTypeAnnotationNode(p))) {
      return true;
    }
    if (node.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */) {
      const parameter ***REMOVED*** firstOrUndefined(node.parameters);
      if (!(parameter && parameterIsThisKeyword(parameter))) {
        return true;
      }
    }
  }
  return false;
}
function isInfinityOrNaNString(name) {
  return name ***REMOVED******REMOVED******REMOVED*** "Infinity" || name ***REMOVED******REMOVED******REMOVED*** "-Infinity" || name ***REMOVED******REMOVED******REMOVED*** "NaN";
}
function isCatchClauseVariableDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */;
}
function isFunctionExpressionOrArrowFunction(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */;
}
function isNumericLiteralName(name) {
  return (+name).toString() ***REMOVED******REMOVED******REMOVED*** name;
}
function createPropertyNameNodeForIdentifierOrLiteral(name, target, singleQuote, stringNamed, isMethod) {
  const isMethodNamedNew ***REMOVED*** isMethod && name ***REMOVED******REMOVED******REMOVED*** "new";
  return !isMethodNamedNew && isIdentifierText(name, target) ? factory.createIdentifier(name) : !stringNamed && !isMethodNamedNew && isNumericLiteralName(name) && +name >***REMOVED*** 0 ? factory.createNumericLiteral(+name) : factory.createStringLiteral(name, !!singleQuote);
}
function isThisTypeParameter(type) {
  return !!(type.flags & 262144 /* TypeParameter */ && type.isThisType);
}
function getNodeModulePathParts(fullPath) {
  let topLevelNodeModulesIndex ***REMOVED*** 0;
  let topLevelPackageNameIndex ***REMOVED*** 0;
  let packageRootIndex ***REMOVED*** 0;
  let fileNameIndex ***REMOVED*** 0;
  let States;
  ((States2) ***REMOVED***> {
    States2[States2["BeforeNodeModules"] ***REMOVED*** 0] ***REMOVED*** "BeforeNodeModules";
    States2[States2["NodeModules"] ***REMOVED*** 1] ***REMOVED*** "NodeModules";
    States2[States2["Scope"] ***REMOVED*** 2] ***REMOVED*** "Scope";
    States2[States2["PackageContent"] ***REMOVED*** 3] ***REMOVED*** "PackageContent";
  })(States || (States ***REMOVED*** {}));
  let partStart ***REMOVED*** 0;
  let partEnd ***REMOVED*** 0;
  let state ***REMOVED*** 0 /* BeforeNodeModules */;
  while (partEnd >***REMOVED*** 0) {
    partStart ***REMOVED*** partEnd;
    partEnd ***REMOVED*** fullPath.indexOf("/", partStart + 1);
    switch (state) {
      case 0 /* BeforeNodeModules */:
        if (fullPath.indexOf(nodeModulesPathPart, partStart) ***REMOVED******REMOVED******REMOVED*** partStart) {
          topLevelNodeModulesIndex ***REMOVED*** partStart;
          topLevelPackageNameIndex ***REMOVED*** partEnd;
          state ***REMOVED*** 1 /* NodeModules */;
        }
        break;
      case 1 /* NodeModules */:
      case 2 /* Scope */:
        if (state ***REMOVED******REMOVED******REMOVED*** 1 /* NodeModules */ && fullPath.charAt(partStart + 1) ***REMOVED******REMOVED******REMOVED*** "@") {
          state ***REMOVED*** 2 /* Scope */;
        } else {
          packageRootIndex ***REMOVED*** partEnd;
          state ***REMOVED*** 3 /* PackageContent */;
        }
        break;
      case 3 /* PackageContent */:
        if (fullPath.indexOf(nodeModulesPathPart, partStart) ***REMOVED******REMOVED******REMOVED*** partStart) {
          state ***REMOVED*** 1 /* NodeModules */;
        } else {
          state ***REMOVED*** 3 /* PackageContent */;
        }
        break;
    }
  }
  fileNameIndex ***REMOVED*** partStart;
  return state > 1 /* NodeModules */ ? { topLevelNodeModulesIndex, topLevelPackageNameIndex, packageRootIndex, fileNameIndex } : void 0;
}
function isTypeDeclaration(node) {
  switch (node.kind) {
    case 169 /* TypeParameter */:
    case 264 /* ClassDeclaration */:
    case 265 /* InterfaceDeclaration */:
    case 266 /* TypeAliasDeclaration */:
    case 267 /* EnumDeclaration */:
    case 347 /* JSDocTypedefTag */:
    case 339 /* JSDocCallbackTag */:
    case 341 /* JSDocEnumTag */:
      return true;
    case 274 /* ImportClause */:
      return node.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    case 277 /* ImportSpecifier */:
      return node.parent.parent.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    case 282 /* ExportSpecifier */:
      return node.parent.parent.isTypeOnly;
    default:
      return false;
  }
}
function canHaveExportModifier(node) {
  return isEnumDeclaration(node) || isVariableStatement(node) || isFunctionDeclaration(node) || isClassDeclaration(node) || isInterfaceDeclaration(node) || isTypeDeclaration(node) || isModuleDeclaration(node) && !isExternalModuleAugmentation(node) && !isGlobalScopeAugmentation(node);
}
function isOptionalJSDocPropertyLikeTag(node) {
  if (!isJSDocPropertyLikeTag(node)) {
    return false;
  }
  const { isBracketed, typeExpression } ***REMOVED*** node;
  return isBracketed || !!typeExpression && typeExpression.type.kind ***REMOVED******REMOVED******REMOVED*** 317 /* JSDocOptionalType */;
}
function canUsePropertyAccess(name, languageVersion) {
  if (name.length ***REMOVED******REMOVED******REMOVED*** 0) {
    return false;
  }
  const firstChar ***REMOVED*** name.charCodeAt(0);
  return firstChar ***REMOVED******REMOVED******REMOVED*** 35 /* hash */ ? name.length > 1 && isIdentifierStart(name.charCodeAt(1), languageVersion) : isIdentifierStart(firstChar, languageVersion);
}
function isJSDocOptionalParameter(node) {
  return isInJSFile(node) && // node.type should only be a JSDocOptionalType when node is a parameter of a JSDocFunctionType
  (node.type && node.type.kind ***REMOVED******REMOVED******REMOVED*** 317 /* JSDocOptionalType */ || getJSDocParameterTags(node).some(isOptionalJSDocPropertyLikeTag));
}
function isOptionalDeclaration(declaration) {
  switch (declaration.kind) {
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
      return !!declaration.questionToken;
    case 170 /* Parameter */:
      return !!declaration.questionToken || isJSDocOptionalParameter(declaration);
    case 349 /* JSDocPropertyTag */:
    case 342 /* JSDocParameterTag */:
      return isOptionalJSDocPropertyLikeTag(declaration);
    default:
      return false;
  }
}
function isNonNullAccess(node) {
  const kind ***REMOVED*** node.kind;
  return (kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */) && isNonNullExpression(node.expression);
}
function isJSDocSatisfiesExpression(node) {
  return isInJSFile(node) && isParenthesizedExpression(node) && hasJSDocNodes(node) && !!getJSDocSatisfiesTag(node);
}
function getJSDocSatisfiesExpressionType(node) {
  return Debug.checkDefined(tryGetJSDocSatisfiesTypeNode(node));
}
function tryGetJSDocSatisfiesTypeNode(node) {
  const tag ***REMOVED*** getJSDocSatisfiesTag(node);
  return tag && tag.typeExpression && tag.typeExpression.type;
}
function getEscapedTextOfJsxAttributeName(node) {
  return isIdentifier(node) ? node.escapedText : getEscapedTextOfJsxNamespacedName(node);
}
function getTextOfJsxAttributeName(node) {
  return isIdentifier(node) ? idText(node) : getTextOfJsxNamespacedName(node);
}
function isJsxAttributeName(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || kind ***REMOVED******REMOVED******REMOVED*** 296 /* JsxNamespacedName */;
}
function getEscapedTextOfJsxNamespacedName(node) {
  return `${node.namespace.escapedText}:${idText(node.name)}`;
}
function getTextOfJsxNamespacedName(node) {
  return `${idText(node.namespace)}:${idText(node.name)}`;
}
function intrinsicTagNameToString(node) {
  return isIdentifier(node) ? idText(node) : getTextOfJsxNamespacedName(node);
}
function isTypeUsableAsPropertyName(type) {
  return !!(type.flags & 8576 /* StringOrNumberLiteralOrUnique */);
}
function getPropertyNameFromType(type) {
  if (type.flags & 8192 /* UniqueESSymbol */) {
    return type.escapedName;
  }
  if (type.flags & (128 /* StringLiteral */ | 256 /* NumberLiteral */)) {
    return escapeLeadingUnderscores("" + type.value);
  }
  return Debug.fail();
}
function isExpandoPropertyDeclaration(declaration) {
  return !!declaration && (isPropertyAccessExpression(declaration) || isElementAccessExpression(declaration) || isBinaryExpression(declaration));
}
function hasResolutionModeOverride(node) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
    return false;
  }
  return !!getResolutionModeOverride(node.attributes);
}
var stringReplace ***REMOVED*** String.prototype.replace;
function replaceFirstStar(s, replacement) {
  return stringReplace.call(s, "*", replacement);
}
function getNameFromImportAttribute(node) {
  return isIdentifier(node.name) ? node.name.escapedText : escapeLeadingUnderscores(node.name.text);
}
function evaluatorResult(value, isSyntacticallyString ***REMOVED*** false, resolvedOtherFiles ***REMOVED*** false, hasExternalReferences ***REMOVED*** false) {
  return { value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences };
}
function createEvaluator({ evaluateElementAccessExpression, evaluateEntityNameExpression }) {
  function evaluate(expr, location) {
    let isSyntacticallyString ***REMOVED*** false;
    let resolvedOtherFiles ***REMOVED*** false;
    let hasExternalReferences ***REMOVED*** false;
    expr ***REMOVED*** skipParentheses(expr);
    switch (expr.kind) {
      case 225 /* PrefixUnaryExpression */:
        const result ***REMOVED*** evaluate(expr.operand, location);
        resolvedOtherFiles ***REMOVED*** result.resolvedOtherFiles;
        hasExternalReferences ***REMOVED*** result.hasExternalReferences;
        if (typeof result.value ***REMOVED******REMOVED******REMOVED*** "number") {
          switch (expr.operator) {
            case 40 /* PlusToken */:
              return evaluatorResult(result.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 41 /* MinusToken */:
              return evaluatorResult(-result.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 55 /* TildeToken */:
              return evaluatorResult(~result.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
          }
        }
        break;
      case 227 /* BinaryExpression */: {
        const left ***REMOVED*** evaluate(expr.left, location);
        const right ***REMOVED*** evaluate(expr.right, location);
        isSyntacticallyString ***REMOVED*** (left.isSyntacticallyString || right.isSyntacticallyString) && expr.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */;
        resolvedOtherFiles ***REMOVED*** left.resolvedOtherFiles || right.resolvedOtherFiles;
        hasExternalReferences ***REMOVED*** left.hasExternalReferences || right.hasExternalReferences;
        if (typeof left.value ***REMOVED******REMOVED******REMOVED*** "number" && typeof right.value ***REMOVED******REMOVED******REMOVED*** "number") {
          switch (expr.operatorToken.kind) {
            case 52 /* BarToken */:
              return evaluatorResult(left.value | right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 51 /* AmpersandToken */:
              return evaluatorResult(left.value & right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 49 /* GreaterThanGreaterThanToken */:
              return evaluatorResult(left.value >> right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 50 /* GreaterThanGreaterThanGreaterThanToken */:
              return evaluatorResult(left.value >>> right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 48 /* LessThanLessThanToken */:
              return evaluatorResult(left.value << right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 53 /* CaretToken */:
              return evaluatorResult(left.value ^ right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 42 /* AsteriskToken */:
              return evaluatorResult(left.value * right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 44 /* SlashToken */:
              return evaluatorResult(left.value / right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 40 /* PlusToken */:
              return evaluatorResult(left.value + right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 41 /* MinusToken */:
              return evaluatorResult(left.value - right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 45 /* PercentToken */:
              return evaluatorResult(left.value % right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
            case 43 /* AsteriskAsteriskToken */:
              return evaluatorResult(left.value ** right.value, isSyntacticallyString, resolvedOtherFiles, hasExternalReferences);
          }
        } else if ((typeof left.value ***REMOVED******REMOVED******REMOVED*** "string" || typeof left.value ***REMOVED******REMOVED******REMOVED*** "number") && (typeof right.value ***REMOVED******REMOVED******REMOVED*** "string" || typeof right.value ***REMOVED******REMOVED******REMOVED*** "number") && expr.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
          return evaluatorResult(
            "" + left.value + right.value,
            isSyntacticallyString,
            resolvedOtherFiles,
            hasExternalReferences
          );
        }
        break;
      }
      case 11 /* StringLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
        return evaluatorResult(
          expr.text,
          /*isSyntacticallyString*/
          true
        );
      case 229 /* TemplateExpression */:
        return evaluateTemplateExpression(expr, location);
      case 9 /* NumericLiteral */:
        return evaluatorResult(+expr.text);
      case 80 /* Identifier */:
        return evaluateEntityNameExpression(expr, location);
      case 212 /* PropertyAccessExpression */:
        if (isEntityNameExpression(expr)) {
          return evaluateEntityNameExpression(expr, location);
        }
        break;
      case 213 /* ElementAccessExpression */:
        return evaluateElementAccessExpression(expr, location);
    }
    return evaluatorResult(
      /*value*/
      void 0,
      isSyntacticallyString,
      resolvedOtherFiles,
      hasExternalReferences
    );
  }
  function evaluateTemplateExpression(expr, location) {
    let result ***REMOVED*** expr.head.text;
    let resolvedOtherFiles ***REMOVED*** false;
    let hasExternalReferences ***REMOVED*** false;
    for (const span of expr.templateSpans) {
      const spanResult ***REMOVED*** evaluate(span.expression, location);
      if (spanResult.value ***REMOVED******REMOVED******REMOVED*** void 0) {
        return evaluatorResult(
          /*value*/
          void 0,
          /*isSyntacticallyString*/
          true
        );
      }
      result +***REMOVED*** spanResult.value;
      result +***REMOVED*** span.literal.text;
      resolvedOtherFiles || (resolvedOtherFiles ***REMOVED*** spanResult.resolvedOtherFiles);
      hasExternalReferences || (hasExternalReferences ***REMOVED*** spanResult.hasExternalReferences);
    }
    return evaluatorResult(
      result,
      /*isSyntacticallyString*/
      true,
      resolvedOtherFiles,
      hasExternalReferences
    );
  }
  return evaluate;
}
function isConstAssertion(location) {
  return isAssertionExpression(location) && isConstTypeReference(location.type) || isJSDocTypeTag(location) && isConstTypeReference(location.typeExpression);
}
function findConstructorDeclaration(node) {
  const members ***REMOVED*** node.members;
  for (const member of members) {
    if (member.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ && nodeIsPresent(member.body)) {
      return member;
    }
  }
}
function createNameResolver({
  compilerOptions,
  requireSymbol,
  argumentsSymbol,
  error,
  getSymbolOfDeclaration,
  globals,
  lookup,
  setRequiresScopeChangeCache ***REMOVED*** returnUndefined,
  getRequiresScopeChangeCache ***REMOVED*** returnUndefined,
  onPropertyWithInvalidInitializer ***REMOVED*** returnFalse,
  onFailedToResolveSymbol ***REMOVED*** returnUndefined,
  onSuccessfullyResolvedSymbol ***REMOVED*** returnUndefined
}) {
  var isolatedModulesLikeFlagName ***REMOVED*** compilerOptions.verbatimModuleSyntax ? "verbatimModuleSyntax" : "isolatedModules";
  var emitStandardClassFields ***REMOVED*** getEmitStandardClassFields(compilerOptions);
  var emptySymbols ***REMOVED*** createSymbolTable();
  return resolveNameHelper;
  function resolveNameHelper(location, nameArg, meaning, nameNotFoundMessage, isUse, excludeGlobals) {
    var _a, _b, _c;
    const originalLocation ***REMOVED*** location;
    let result;
    let lastLocation;
    let lastSelfReferenceLocation;
    let propertyWithInvalidInitializer;
    let associatedDeclarationForContainingInitializerOrBindingName;
    let withinDeferredContext ***REMOVED*** false;
    let grandparent;
    const name ***REMOVED*** isString(nameArg) ? nameArg : nameArg.escapedText;
    loop:
      while (location) {
        if (name ***REMOVED******REMOVED******REMOVED*** "const" && isConstAssertion(location)) {
          return void 0;
        }
        if (isModuleOrEnumDeclaration(location) && lastLocation && location.name ***REMOVED******REMOVED******REMOVED*** lastLocation) {
          lastLocation ***REMOVED*** location;
          location ***REMOVED*** location.parent;
        }
        if (canHaveLocals(location) && location.locals && !isGlobalSourceFile(location)) {
          if (result ***REMOVED*** lookup(location.locals, name, meaning)) {
            let useResult ***REMOVED*** true;
            if (isFunctionLike(location) && lastLocation && lastLocation !***REMOVED******REMOVED*** location.body) {
              if (meaning & result.flags & 788968 /* Type */ && lastLocation.kind !***REMOVED******REMOVED*** 321 /* JSDoc */) {
                useResult ***REMOVED*** result.flags & 262144 /* TypeParameter */ ? !!(lastLocation.flags & 16 /* Synthesized */) || // Synthetic fake scopes are added for signatures so type parameters are accessible from them
                lastLocation ***REMOVED******REMOVED******REMOVED*** location.type || lastLocation.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || lastLocation.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */ || lastLocation.kind ***REMOVED******REMOVED******REMOVED*** 343 /* JSDocReturnTag */ || lastLocation.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ : false;
              }
              if (meaning & result.flags & 3 /* Variable */) {
                if (useOuterVariableScopeInParameter(result, location, lastLocation)) {
                  useResult ***REMOVED*** false;
                } else if (result.flags & 1 /* FunctionScopedVariable */) {
                  useResult ***REMOVED*** lastLocation.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || !!(lastLocation.flags & 16 /* Synthesized */) || // Synthetic fake scopes are added for signatures so parameters are accessible from them
                  lastLocation ***REMOVED******REMOVED******REMOVED*** location.type && !!findAncestor(result.valueDeclaration, isParameter);
                }
              }
            } else if (location.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */) {
              useResult ***REMOVED*** lastLocation ***REMOVED******REMOVED******REMOVED*** location.trueType;
            }
            if (useResult) {
              break loop;
            } else {
              result ***REMOVED*** void 0;
            }
          }
        }
        withinDeferredContext ***REMOVED*** withinDeferredContext || getIsDeferredContext(location, lastLocation);
        switch (location.kind) {
          case 308 /* SourceFile */:
            if (!isExternalOrCommonJsModule(location)) break;
          // falls through
          case 268 /* ModuleDeclaration */:
            const moduleExports ***REMOVED*** ((_a ***REMOVED*** getSymbolOfDeclaration(location)) ***REMOVED******REMOVED*** null ? void 0 : _a.exports) || emptySymbols;
            if (location.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || isModuleDeclaration(location) && location.flags & 33554432 /* Ambient */ && !isGlobalScopeAugmentation(location)) {
              if (result ***REMOVED*** moduleExports.get("default" /* Default */)) {
                const localSymbol ***REMOVED*** getLocalSymbolForExportDefault(result);
                if (localSymbol && result.flags & meaning && localSymbol.escapedName ***REMOVED******REMOVED******REMOVED*** name) {
                  break loop;
                }
                result ***REMOVED*** void 0;
              }
              const moduleExport ***REMOVED*** moduleExports.get(name);
              if (moduleExport && moduleExport.flags ***REMOVED******REMOVED******REMOVED*** 2097152 /* Alias */ && (getDeclarationOfKind(moduleExport, 282 /* ExportSpecifier */) || getDeclarationOfKind(moduleExport, 281 /* NamespaceExport */))) {
                break;
              }
            }
            if (name !***REMOVED******REMOVED*** "default" /* Default */ && (result ***REMOVED*** lookup(moduleExports, name, meaning & 2623475 /* ModuleMember */))) {
              if (isSourceFile(location) && location.commonJsModuleIndicator && !((_b ***REMOVED*** result.declarations) ***REMOVED******REMOVED*** null ? void 0 : _b.some(isJSDocTypeAlias))) {
                result ***REMOVED*** void 0;
              } else {
                break loop;
              }
            }
            break;
          case 267 /* EnumDeclaration */:
            if (result ***REMOVED*** lookup(((_c ***REMOVED*** getSymbolOfDeclaration(location)) ***REMOVED******REMOVED*** null ? void 0 : _c.exports) || emptySymbols, name, meaning & 8 /* EnumMember */)) {
              if (nameNotFoundMessage && getIsolatedModules(compilerOptions) && !(location.flags & 33554432 /* Ambient */) && getSourceFileOfNode(location) !***REMOVED******REMOVED*** getSourceFileOfNode(result.valueDeclaration)) {
                error(
                  originalLocation,
                  Diagnostics.Cannot_access_0_from_another_file_without_qualification_when_1_is_enabled_Use_2_instead,
                  unescapeLeadingUnderscores(name),
                  isolatedModulesLikeFlagName,
                  `${unescapeLeadingUnderscores(getSymbolOfDeclaration(location).escapedName)}.${unescapeLeadingUnderscores(name)}`
                );
              }
              break loop;
            }
            break;
          case 173 /* PropertyDeclaration */:
            if (!isStatic(location)) {
              const ctor ***REMOVED*** findConstructorDeclaration(location.parent);
              if (ctor && ctor.locals) {
                if (lookup(ctor.locals, name, meaning & 111551 /* Value */)) {
                  Debug.assertNode(location, isPropertyDeclaration);
                  propertyWithInvalidInitializer ***REMOVED*** location;
                }
              }
            }
            break;
          case 264 /* ClassDeclaration */:
          case 232 /* ClassExpression */:
          case 265 /* InterfaceDeclaration */:
            if (result ***REMOVED*** lookup(getSymbolOfDeclaration(location).members || emptySymbols, name, meaning & 788968 /* Type */)) {
              if (!isTypeParameterSymbolDeclaredInContainer(result, location)) {
                result ***REMOVED*** void 0;
                break;
              }
              if (lastLocation && isStatic(lastLocation)) {
                if (nameNotFoundMessage) {
                  error(originalLocation, Diagnostics.Static_members_cannot_reference_class_type_parameters);
                }
                return void 0;
              }
              break loop;
            }
            if (isClassExpression(location) && meaning & 32 /* Class */) {
              const className ***REMOVED*** location.name;
              if (className && name ***REMOVED******REMOVED******REMOVED*** className.escapedText) {
                result ***REMOVED*** location.symbol;
                break loop;
              }
            }
            break;
          case 234 /* ExpressionWithTypeArguments */:
            if (lastLocation ***REMOVED******REMOVED******REMOVED*** location.expression && location.parent.token ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
              const container ***REMOVED*** location.parent.parent;
              if (isClassLike(container) && (result ***REMOVED*** lookup(getSymbolOfDeclaration(container).members, name, meaning & 788968 /* Type */))) {
                if (nameNotFoundMessage) {
                  error(originalLocation, Diagnostics.Base_class_expressions_cannot_reference_class_type_parameters);
                }
                return void 0;
              }
            }
            break;
          // It is not legal to reference a class's own type parameters from a computed property name that
          // belongs to the class. For example:
          //
          //   function foo<T>() { return '' }
          //   class C<T> { // <-- Class's own type parameter T
          //       [foo<T>()]() { } // <-- Reference to T from class's own computed property
          //   }
          //
          case 168 /* ComputedPropertyName */:
            grandparent ***REMOVED*** location.parent.parent;
            if (isClassLike(grandparent) || grandparent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
              if (result ***REMOVED*** lookup(getSymbolOfDeclaration(grandparent).members, name, meaning & 788968 /* Type */)) {
                if (nameNotFoundMessage) {
                  error(originalLocation, Diagnostics.A_computed_property_name_cannot_reference_a_type_parameter_from_its_containing_type);
                }
                return void 0;
              }
            }
            break;
          case 220 /* ArrowFunction */:
            if (getEmitScriptTarget(compilerOptions) >***REMOVED*** 2 /* ES2015 */) {
              break;
            }
          // falls through
          case 175 /* MethodDeclaration */:
          case 177 /* Constructor */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
          case 263 /* FunctionDeclaration */:
            if (meaning & 3 /* Variable */ && name ***REMOVED******REMOVED******REMOVED*** "arguments") {
              result ***REMOVED*** argumentsSymbol;
              break loop;
            }
            break;
          case 219 /* FunctionExpression */:
            if (meaning & 3 /* Variable */ && name ***REMOVED******REMOVED******REMOVED*** "arguments") {
              result ***REMOVED*** argumentsSymbol;
              break loop;
            }
            if (meaning & 16 /* Function */) {
              const functionName ***REMOVED*** location.name;
              if (functionName && name ***REMOVED******REMOVED******REMOVED*** functionName.escapedText) {
                result ***REMOVED*** location.symbol;
                break loop;
              }
            }
            break;
          case 171 /* Decorator */:
            if (location.parent && location.parent.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              location ***REMOVED*** location.parent;
            }
            if (location.parent && (isClassElement(location.parent) || location.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */)) {
              location ***REMOVED*** location.parent;
            }
            break;
          case 347 /* JSDocTypedefTag */:
          case 339 /* JSDocCallbackTag */:
          case 341 /* JSDocEnumTag */:
          case 352 /* JSDocImportTag */:
            const root ***REMOVED*** getJSDocRoot(location);
            if (root) {
              location ***REMOVED*** root.parent;
            }
            break;
          case 170 /* Parameter */:
            if (lastLocation && (lastLocation ***REMOVED******REMOVED******REMOVED*** location.initializer || lastLocation ***REMOVED******REMOVED******REMOVED*** location.name && isBindingPattern(lastLocation))) {
              if (!associatedDeclarationForContainingInitializerOrBindingName) {
                associatedDeclarationForContainingInitializerOrBindingName ***REMOVED*** location;
              }
            }
            break;
          case 209 /* BindingElement */:
            if (lastLocation && (lastLocation ***REMOVED******REMOVED******REMOVED*** location.initializer || lastLocation ***REMOVED******REMOVED******REMOVED*** location.name && isBindingPattern(lastLocation))) {
              if (isPartOfParameterDeclaration(location) && !associatedDeclarationForContainingInitializerOrBindingName) {
                associatedDeclarationForContainingInitializerOrBindingName ***REMOVED*** location;
              }
            }
            break;
          case 196 /* InferType */:
            if (meaning & 262144 /* TypeParameter */) {
              const parameterName ***REMOVED*** location.typeParameter.name;
              if (parameterName && name ***REMOVED******REMOVED******REMOVED*** parameterName.escapedText) {
                result ***REMOVED*** location.typeParameter.symbol;
                break loop;
              }
            }
            break;
          case 282 /* ExportSpecifier */:
            if (lastLocation && lastLocation ***REMOVED******REMOVED******REMOVED*** location.propertyName && location.parent.parent.moduleSpecifier) {
              location ***REMOVED*** location.parent.parent.parent;
            }
            break;
        }
        if (isSelfReferenceLocation(location, lastLocation)) {
          lastSelfReferenceLocation ***REMOVED*** location;
        }
        lastLocation ***REMOVED*** location;
        location ***REMOVED*** isJSDocTemplateTag(location) ? getEffectiveContainerForJSDocTemplateTag(location) || location.parent : isJSDocParameterTag(location) || isJSDocReturnTag(location) ? getHostSignatureFromJSDoc(location) || location.parent : location.parent;
      }
    if (isUse && result && (!lastSelfReferenceLocation || result !***REMOVED******REMOVED*** lastSelfReferenceLocation.symbol)) {
      result.isReferenced |***REMOVED*** meaning;
    }
    if (!result) {
      if (lastLocation) {
        Debug.assertNode(lastLocation, isSourceFile);
        if (lastLocation.commonJsModuleIndicator && name ***REMOVED******REMOVED******REMOVED*** "exports" && meaning & lastLocation.symbol.flags) {
          return lastLocation.symbol;
        }
      }
      if (!excludeGlobals) {
        result ***REMOVED*** lookup(globals, name, meaning);
      }
    }
    if (!result) {
      if (originalLocation && isInJSFile(originalLocation) && originalLocation.parent) {
        if (isRequireCall(
          originalLocation.parent,
          /*requireStringLiteralLikeArgument*/
          false
        )) {
          return requireSymbol;
        }
      }
    }
    if (nameNotFoundMessage) {
      if (propertyWithInvalidInitializer && onPropertyWithInvalidInitializer(originalLocation, name, propertyWithInvalidInitializer, result)) {
        return void 0;
      }
      if (!result) {
        onFailedToResolveSymbol(originalLocation, nameArg, meaning, nameNotFoundMessage);
      } else {
        onSuccessfullyResolvedSymbol(originalLocation, result, meaning, lastLocation, associatedDeclarationForContainingInitializerOrBindingName, withinDeferredContext);
      }
    }
    return result;
  }
  function useOuterVariableScopeInParameter(result, location, lastLocation) {
    const target ***REMOVED*** getEmitScriptTarget(compilerOptions);
    const functionLocation ***REMOVED*** location;
    if (isParameter(lastLocation) && functionLocation.body && result.valueDeclaration && result.valueDeclaration.pos >***REMOVED*** functionLocation.body.pos && result.valueDeclaration.end <***REMOVED*** functionLocation.body.end) {
      if (target >***REMOVED*** 2 /* ES2015 */) {
        let declarationRequiresScopeChange ***REMOVED*** getRequiresScopeChangeCache(functionLocation);
        if (declarationRequiresScopeChange ***REMOVED******REMOVED******REMOVED*** void 0) {
          declarationRequiresScopeChange ***REMOVED*** forEach(functionLocation.parameters, requiresScopeChange) || false;
          setRequiresScopeChangeCache(functionLocation, declarationRequiresScopeChange);
        }
        return !declarationRequiresScopeChange;
      }
    }
    return false;
    function requiresScopeChange(node) {
      return requiresScopeChangeWorker(node.name) || !!node.initializer && requiresScopeChangeWorker(node.initializer);
    }
    function requiresScopeChangeWorker(node) {
      switch (node.kind) {
        case 220 /* ArrowFunction */:
        case 219 /* FunctionExpression */:
        case 263 /* FunctionDeclaration */:
        case 177 /* Constructor */:
          return false;
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
        case 304 /* PropertyAssignment */:
          return requiresScopeChangeWorker(node.name);
        case 173 /* PropertyDeclaration */:
          if (hasStaticModifier(node)) {
            return !emitStandardClassFields;
          }
          return requiresScopeChangeWorker(node.name);
        default:
          if (isNullishCoalesce(node) || isOptionalChain(node)) {
            return target < 7 /* ES2020 */;
          }
          if (isBindingElement(node) && node.dotDotDotToken && isObjectBindingPattern(node.parent)) {
            return target < 4 /* ES2017 */;
          }
          if (isTypeNode(node)) return false;
          return forEachChild(node, requiresScopeChangeWorker) || false;
      }
    }
  }
  function getIsDeferredContext(location, lastLocation) {
    if (location.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */ && location.kind !***REMOVED******REMOVED*** 219 /* FunctionExpression */) {
      return isTypeQueryNode(location) || (isFunctionLikeDeclaration(location) || location.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && !isStatic(location)) && (!lastLocation || lastLocation !***REMOVED******REMOVED*** location.name);
    }
    if (lastLocation && lastLocation ***REMOVED******REMOVED******REMOVED*** location.name) {
      return false;
    }
    if (location.asteriskToken || hasSyntacticModifier(location, 1024 /* Async */)) {
      return true;
    }
    return !getImmediatelyInvokedFunctionExpression(location);
  }
  function isSelfReferenceLocation(node, lastLocation) {
    switch (node.kind) {
      case 170 /* Parameter */:
        return !!lastLocation && lastLocation ***REMOVED******REMOVED******REMOVED*** node.name;
      case 263 /* FunctionDeclaration */:
      case 264 /* ClassDeclaration */:
      case 265 /* InterfaceDeclaration */:
      case 267 /* EnumDeclaration */:
      case 266 /* TypeAliasDeclaration */:
      case 268 /* ModuleDeclaration */:
        return true;
      default:
        return false;
    }
  }
  function isTypeParameterSymbolDeclaredInContainer(symbol, container) {
    if (symbol.declarations) {
      for (const decl of symbol.declarations) {
        if (decl.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */) {
          const parent ***REMOVED*** isJSDocTemplateTag(decl.parent) ? getJSDocHost(decl.parent) : decl.parent;
          if (parent ***REMOVED******REMOVED******REMOVED*** container) {
            return !(isJSDocTemplateTag(decl.parent) && find(decl.parent.parent.tags, isJSDocTypeAlias));
          }
        }
      }
    }
    return false;
  }
}
function isPrimitiveLiteralValue(node, includeBigInt ***REMOVED*** true) {
  Debug.type(node);
  switch (node.kind) {
    case 112 /* TrueKeyword */:
    case 97 /* FalseKeyword */:
    case 9 /* NumericLiteral */:
    case 11 /* StringLiteral */:
    case 15 /* NoSubstitutionTemplateLiteral */:
      return true;
    case 10 /* BigIntLiteral */:
      return includeBigInt;
    case 225 /* PrefixUnaryExpression */:
      if (node.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
        return isNumericLiteral(node.operand) || includeBigInt && isBigIntLiteral(node.operand);
      }
      if (node.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
        return isNumericLiteral(node.operand);
      }
      return false;
    default:
      assertType(node);
      return false;
  }
}
function unwrapParenthesizedExpression(o) {
  while (o.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
    o ***REMOVED*** o.expression;
  }
  return o;
}
function hasInferredType(node) {
  Debug.type(node);
  switch (node.kind) {
    case 170 /* Parameter */:
    case 172 /* PropertySignature */:
    case 173 /* PropertyDeclaration */:
    case 209 /* BindingElement */:
    case 212 /* PropertyAccessExpression */:
    case 213 /* ElementAccessExpression */:
    case 227 /* BinaryExpression */:
    case 261 /* VariableDeclaration */:
    case 278 /* ExportAssignment */:
    case 304 /* PropertyAssignment */:
    case 305 /* ShorthandPropertyAssignment */:
    case 342 /* JSDocParameterTag */:
    case 349 /* JSDocPropertyTag */:
      return true;
    default:
      assertType(node);
      return false;
  }
}
function isSideEffectImport(node) {
  const ancestor ***REMOVED*** findAncestor(node, isImportDeclaration);
  return !!ancestor && !ancestor.importClause;
}
var unprefixedNodeCoreModulesList ***REMOVED*** [
  "assert",
  "assert/strict",
  "async_hooks",
  "buffer",
  "child_process",
  "cluster",
  "console",
  "constants",
  "crypto",
  "dgram",
  "diagnostics_channel",
  "dns",
  "dns/promises",
  "domain",
  "events",
  "fs",
  "fs/promises",
  "http",
  "http2",
  "https",
  "inspector",
  "inspector/promises",
  "module",
  "net",
  "os",
  "path",
  "path/posix",
  "path/win32",
  "perf_hooks",
  "process",
  "punycode",
  "querystring",
  "readline",
  "readline/promises",
  "repl",
  "stream",
  "stream/consumers",
  "stream/promises",
  "stream/web",
  "string_decoder",
  "sys",
  "test/mock_loader",
  "timers",
  "timers/promises",
  "tls",
  "trace_events",
  "tty",
  "url",
  "util",
  "util/types",
  "v8",
  "vm",
  "wasi",
  "worker_threads",
  "zlib"
];
var unprefixedNodeCoreModules ***REMOVED*** new Set(unprefixedNodeCoreModulesList);
var exclusivelyPrefixedNodeCoreModules ***REMOVED*** /* @__PURE__ */ new Set([
  "node:sea",
  "node:sqlite",
  "node:test",
  "node:test/reporters"
]);
var nodeCoreModules ***REMOVED*** /* @__PURE__ */ new Set([
  ...unprefixedNodeCoreModulesList,
  ...unprefixedNodeCoreModulesList.map((name) ***REMOVED***> `node:${name}`),
  ...exclusivelyPrefixedNodeCoreModules
]);
function forEachDynamicImportOrRequireCall(file, includeTypeSpaceImports, requireStringLiteralLikeArgument, cb) {
  const isJavaScriptFile ***REMOVED*** isInJSFile(file);
  const r ***REMOVED*** /import|require/g;
  while (r.exec(file.text) !***REMOVED******REMOVED*** null) {
    const node ***REMOVED*** getNodeAtPosition(
      file,
      r.lastIndex,
      /*includeJSDoc*/
      includeTypeSpaceImports
    );
    if (isJavaScriptFile && isRequireCall(node, requireStringLiteralLikeArgument)) {
      cb(node, node.arguments[0]);
    } else if (isImportCall(node) && node.arguments.length >***REMOVED*** 1 && (!requireStringLiteralLikeArgument || isStringLiteralLike(node.arguments[0]))) {
      cb(node, node.arguments[0]);
    } else if (includeTypeSpaceImports && isLiteralImportTypeNode(node)) {
      cb(node, node.argument.literal);
    } else if (includeTypeSpaceImports && isJSDocImportTag(node)) {
      const moduleNameExpr ***REMOVED*** getExternalModuleName(node);
      if (moduleNameExpr && isStringLiteral(moduleNameExpr) && moduleNameExpr.text) {
        cb(node, moduleNameExpr);
      }
    }
  }
}
function getNodeAtPosition(sourceFile, position, includeJSDoc) {
  const isJavaScriptFile ***REMOVED*** isInJSFile(sourceFile);
  let current ***REMOVED*** sourceFile;
  const getContainingChild ***REMOVED*** (child) ***REMOVED***> {
    if (child.pos <***REMOVED*** position && (position < child.end || position ***REMOVED******REMOVED******REMOVED*** child.end && child.kind ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */)) {
      return child;
    }
  };
  while (true) {
    const child ***REMOVED*** isJavaScriptFile && includeJSDoc && hasJSDocNodes(current) && forEach(current.jsDoc, getContainingChild) || forEachChild(current, getContainingChild);
    if (!child || isMetaProperty(child)) {
      return current;
    }
    current ***REMOVED*** child;
  }
}
function isNewScopeNode(node) {
  return isFunctionLike(node) || isJSDocSignature(node) || isMappedTypeNode(node);
}
function getLibNameFromLibReference(libReference) {
  return toFileNameLowerCase(libReference.fileName);
}
function getLibFileNameFromLibReference(libReference) {
  const libName ***REMOVED*** getLibNameFromLibReference(libReference);
  return libMap.get(libName);
}
function forEachResolvedProjectReference(resolvedProjectReferences, cb) {
  return forEachProjectReference(
    /*projectReferences*/
    void 0,
    resolvedProjectReferences,
    (resolvedRef) ***REMOVED***> resolvedRef && cb(resolvedRef)
  );
}
function forEachProjectReference(projectReferences, resolvedProjectReferences, cbResolvedRef, cbRef) {
  let seenResolvedRefs;
  return worker(
    projectReferences,
    resolvedProjectReferences,
    /*parent*/
    void 0
  );
  function worker(projectReferences2, resolvedProjectReferences2, parent) {
    if (cbRef) {
      const result ***REMOVED*** cbRef(projectReferences2, parent);
      if (result) return result;
    }
    let skipChildren;
    return forEach(
      resolvedProjectReferences2,
      (resolvedRef, index) ***REMOVED***> {
        if (resolvedRef && (seenResolvedRefs ***REMOVED******REMOVED*** null ? void 0 : seenResolvedRefs.has(resolvedRef.sourceFile.path))) {
          (skipChildren ?? (skipChildren ***REMOVED*** /* @__PURE__ */ new Set())).add(resolvedRef);
          return void 0;
        }
        const result ***REMOVED*** cbResolvedRef(resolvedRef, parent, index);
        if (result || !resolvedRef) return result;
        (seenResolvedRefs || (seenResolvedRefs ***REMOVED*** /* @__PURE__ */ new Set())).add(resolvedRef.sourceFile.path);
      }
    ) || forEach(
      resolvedProjectReferences2,
      (resolvedRef) ***REMOVED***> resolvedRef && !(skipChildren ***REMOVED******REMOVED*** null ? void 0 : skipChildren.has(resolvedRef)) ? worker(resolvedRef.commandLine.projectReferences, resolvedRef.references, resolvedRef) : void 0
    );
  }
}
function getOptionsSyntaxByArrayElementValue(optionsObject, name, value) {
  return optionsObject && getPropertyArrayElementValue(optionsObject, name, value);
}
function getPropertyArrayElementValue(objectLiteral, propKey, elementValue) {
  return forEachPropertyAssignment(objectLiteral, propKey, (property) ***REMOVED***> isArrayLiteralExpression(property.initializer) ? find(property.initializer.elements, (element) ***REMOVED***> isStringLiteral(element) && element.text ***REMOVED******REMOVED******REMOVED*** elementValue) : void 0);
}
function getOptionsSyntaxByValue(optionsObject, name, value) {
  return forEachOptionsSyntaxByName(optionsObject, name, (property) ***REMOVED***> isStringLiteral(property.initializer) && property.initializer.text ***REMOVED******REMOVED******REMOVED*** value ? property.initializer : void 0);
}
function forEachOptionsSyntaxByName(optionsObject, name, callback) {
  return forEachPropertyAssignment(optionsObject, name, callback);
}
function getSynthesizedDeepClone(node, includeTrivia ***REMOVED*** true) {
  const clone ***REMOVED*** node && getSynthesizedDeepCloneWorker(node);
  if (clone && !includeTrivia) suppressLeadingAndTrailingTrivia(clone);
  return setParentRecursive(
    clone,
    /*incremental*/
    false
  );
}
function getSynthesizedDeepCloneWithReplacements(node, includeTrivia, replaceNode) {
  let clone ***REMOVED*** replaceNode(node);
  if (clone) {
    setOriginalNode(clone, node);
  } else {
    clone ***REMOVED*** getSynthesizedDeepCloneWorker(node, replaceNode);
  }
  if (clone && !includeTrivia) suppressLeadingAndTrailingTrivia(clone);
  return clone;
}
function getSynthesizedDeepCloneWorker(node, replaceNode) {
  const nodeClone ***REMOVED*** replaceNode ? (n) ***REMOVED***> getSynthesizedDeepCloneWithReplacements(
    n,
    /*includeTrivia*/
    true,
    replaceNode
  ) : getSynthesizedDeepClone;
  const nodesClone ***REMOVED*** replaceNode ? (ns) ***REMOVED***> ns && getSynthesizedDeepClonesWithReplacements(
    ns,
    /*includeTrivia*/
    true,
    replaceNode
  ) : (ns) ***REMOVED***> ns && getSynthesizedDeepClones(ns);
  const visited ***REMOVED*** visitEachChild(
    node,
    nodeClone,
    /*context*/
    void 0,
    nodesClone,
    nodeClone
  );
  if (visited ***REMOVED******REMOVED******REMOVED*** node) {
    const clone ***REMOVED*** isStringLiteral(node) ? setOriginalNode(factory.createStringLiteralFromNode(node), node) : isNumericLiteral(node) ? setOriginalNode(factory.createNumericLiteral(node.text, node.numericLiteralFlags), node) : factory.cloneNode(node);
    return setTextRange(clone, node);
  }
  visited.parent ***REMOVED*** void 0;
  return visited;
}
function getSynthesizedDeepClones(nodes, includeTrivia ***REMOVED*** true) {
  if (nodes) {
    const cloned ***REMOVED*** factory.createNodeArray(nodes.map((n) ***REMOVED***> getSynthesizedDeepClone(n, includeTrivia)), nodes.hasTrailingComma);
    setTextRange(cloned, nodes);
    return cloned;
  }
  return nodes;
}
function getSynthesizedDeepClonesWithReplacements(nodes, includeTrivia, replaceNode) {
  return factory.createNodeArray(nodes.map((n) ***REMOVED***> getSynthesizedDeepCloneWithReplacements(n, includeTrivia, replaceNode)), nodes.hasTrailingComma);
}
function suppressLeadingAndTrailingTrivia(node) {
  suppressLeadingTrivia(node);
  suppressTrailingTrivia(node);
}
function suppressLeadingTrivia(node) {
  addEmitFlagsRecursively(node, 1024 /* NoLeadingComments */, getFirstChild);
}
function suppressTrailingTrivia(node) {
  addEmitFlagsRecursively(node, 2048 /* NoTrailingComments */, getLastChild);
}
function addEmitFlagsRecursively(node, flag, getChild) {
  addEmitFlags(node, flag);
  const child ***REMOVED*** getChild(node);
  if (child) addEmitFlagsRecursively(child, flag, getChild);
}
function getFirstChild(node) {
  return forEachChild(node, (child) ***REMOVED***> child);
}

// src/compiler/factory/baseNodeFactory.ts
function createBaseNodeFactory() {
  let NodeConstructor2;
  let TokenConstructor2;
  let IdentifierConstructor2;
  let PrivateIdentifierConstructor2;
  let SourceFileConstructor2;
  return {
    createBaseSourceFileNode,
    createBaseIdentifierNode,
    createBasePrivateIdentifierNode,
    createBaseTokenNode,
    createBaseNode
  };
  function createBaseSourceFileNode(kind) {
    return new (SourceFileConstructor2 || (SourceFileConstructor2 ***REMOVED*** objectAllocator.getSourceFileConstructor()))(
      kind,
      /*pos*/
      -1,
      /*end*/
      -1
    );
  }
  function createBaseIdentifierNode(kind) {
    return new (IdentifierConstructor2 || (IdentifierConstructor2 ***REMOVED*** objectAllocator.getIdentifierConstructor()))(
      kind,
      /*pos*/
      -1,
      /*end*/
      -1
    );
  }
  function createBasePrivateIdentifierNode(kind) {
    return new (PrivateIdentifierConstructor2 || (PrivateIdentifierConstructor2 ***REMOVED*** objectAllocator.getPrivateIdentifierConstructor()))(
      kind,
      /*pos*/
      -1,
      /*end*/
      -1
    );
  }
  function createBaseTokenNode(kind) {
    return new (TokenConstructor2 || (TokenConstructor2 ***REMOVED*** objectAllocator.getTokenConstructor()))(
      kind,
      /*pos*/
      -1,
      /*end*/
      -1
    );
  }
  function createBaseNode(kind) {
    return new (NodeConstructor2 || (NodeConstructor2 ***REMOVED*** objectAllocator.getNodeConstructor()))(
      kind,
      /*pos*/
      -1,
      /*end*/
      -1
    );
  }
}

// src/compiler/factory/parenthesizerRules.ts
function createParenthesizerRules(factory2) {
  let binaryLeftOperandParenthesizerCache;
  let binaryRightOperandParenthesizerCache;
  return {
    getParenthesizeLeftSideOfBinaryForOperator,
    getParenthesizeRightSideOfBinaryForOperator,
    parenthesizeLeftSideOfBinary,
    parenthesizeRightSideOfBinary,
    parenthesizeExpressionOfComputedPropertyName,
    parenthesizeConditionOfConditionalExpression,
    parenthesizeBranchOfConditionalExpression,
    parenthesizeExpressionOfExportDefault,
    parenthesizeExpressionOfNew,
    parenthesizeLeftSideOfAccess,
    parenthesizeOperandOfPostfixUnary,
    parenthesizeOperandOfPrefixUnary,
    parenthesizeExpressionsOfCommaDelimitedList,
    parenthesizeExpressionForDisallowedComma,
    parenthesizeExpressionOfExpressionStatement,
    parenthesizeConciseBodyOfArrowFunction,
    parenthesizeCheckTypeOfConditionalType,
    parenthesizeExtendsTypeOfConditionalType,
    parenthesizeConstituentTypesOfUnionType,
    parenthesizeConstituentTypeOfUnionType,
    parenthesizeConstituentTypesOfIntersectionType,
    parenthesizeConstituentTypeOfIntersectionType,
    parenthesizeOperandOfTypeOperator,
    parenthesizeOperandOfReadonlyTypeOperator,
    parenthesizeNonArrayTypeOfPostfixType,
    parenthesizeElementTypesOfTupleType,
    parenthesizeElementTypeOfTupleType,
    parenthesizeTypeOfOptionalType,
    parenthesizeTypeArguments,
    parenthesizeLeadingTypeArgument
  };
  function getParenthesizeLeftSideOfBinaryForOperator(operatorKind) {
    binaryLeftOperandParenthesizerCache || (binaryLeftOperandParenthesizerCache ***REMOVED*** /* @__PURE__ */ new Map());
    let parenthesizerRule ***REMOVED*** binaryLeftOperandParenthesizerCache.get(operatorKind);
    if (!parenthesizerRule) {
      parenthesizerRule ***REMOVED*** (node) ***REMOVED***> parenthesizeLeftSideOfBinary(operatorKind, node);
      binaryLeftOperandParenthesizerCache.set(operatorKind, parenthesizerRule);
    }
    return parenthesizerRule;
  }
  function getParenthesizeRightSideOfBinaryForOperator(operatorKind) {
    binaryRightOperandParenthesizerCache || (binaryRightOperandParenthesizerCache ***REMOVED*** /* @__PURE__ */ new Map());
    let parenthesizerRule ***REMOVED*** binaryRightOperandParenthesizerCache.get(operatorKind);
    if (!parenthesizerRule) {
      parenthesizerRule ***REMOVED*** (node) ***REMOVED***> parenthesizeRightSideOfBinary(
        operatorKind,
        /*leftSide*/
        void 0,
        node
      );
      binaryRightOperandParenthesizerCache.set(operatorKind, parenthesizerRule);
    }
    return parenthesizerRule;
  }
  function mixingBinaryOperatorsRequiresParentheses(a, b) {
    if (a ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
      return b ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ || b ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */;
    }
    if (b ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
      return a ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ || a ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */;
    }
    return false;
  }
  function binaryOperandNeedsParentheses(binaryOperator, operand, isLeftSideOfBinary, leftOperand) {
    const emittedOperand ***REMOVED*** skipPartiallyEmittedExpressions(operand);
    if (isBinaryExpression(emittedOperand) && mixingBinaryOperatorsRequiresParentheses(binaryOperator, emittedOperand.operatorToken.kind)) {
      return true;
    }
    const binaryOperatorPrecedence ***REMOVED*** getOperatorPrecedence(227 /* BinaryExpression */, binaryOperator);
    const binaryOperatorAssociativity ***REMOVED*** getOperatorAssociativity(227 /* BinaryExpression */, binaryOperator);
    if (!isLeftSideOfBinary && operand.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ && binaryOperatorPrecedence > 3 /* Assignment */) {
      return true;
    }
    const operandPrecedence ***REMOVED*** getExpressionPrecedence(emittedOperand);
    switch (compareValues(operandPrecedence, binaryOperatorPrecedence)) {
      case -1 /* LessThan */:
        if (!isLeftSideOfBinary && binaryOperatorAssociativity ***REMOVED******REMOVED******REMOVED*** 1 /* Right */ && operand.kind ***REMOVED******REMOVED******REMOVED*** 230 /* YieldExpression */) {
          return false;
        }
        return true;
      case 1 /* GreaterThan */:
        return false;
      case 0 /* EqualTo */:
        if (isLeftSideOfBinary) {
          return binaryOperatorAssociativity ***REMOVED******REMOVED******REMOVED*** 1 /* Right */;
        } else {
          if (isBinaryExpression(emittedOperand) && emittedOperand.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** binaryOperator) {
            if (operatorHasAssociativeProperty(binaryOperator)) {
              return false;
            }
            if (binaryOperator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
              const leftKind ***REMOVED*** leftOperand ? getLiteralKindOfBinaryPlusOperand(leftOperand) : 0 /* Unknown */;
              if (isLiteralKind(leftKind) && leftKind ***REMOVED******REMOVED******REMOVED*** getLiteralKindOfBinaryPlusOperand(emittedOperand)) {
                return false;
              }
            }
          }
          const operandAssociativity ***REMOVED*** getExpressionAssociativity(emittedOperand);
          return operandAssociativity ***REMOVED******REMOVED******REMOVED*** 0 /* Left */;
        }
    }
  }
  function operatorHasAssociativeProperty(binaryOperator) {
    return binaryOperator ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || binaryOperator ***REMOVED******REMOVED******REMOVED*** 52 /* BarToken */ || binaryOperator ***REMOVED******REMOVED******REMOVED*** 51 /* AmpersandToken */ || binaryOperator ***REMOVED******REMOVED******REMOVED*** 53 /* CaretToken */ || binaryOperator ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */;
  }
  function getLiteralKindOfBinaryPlusOperand(node) {
    node ***REMOVED*** skipPartiallyEmittedExpressions(node);
    if (isLiteralKind(node.kind)) {
      return node.kind;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
      if (node.cachedLiteralKind !***REMOVED******REMOVED*** void 0) {
        return node.cachedLiteralKind;
      }
      const leftKind ***REMOVED*** getLiteralKindOfBinaryPlusOperand(node.left);
      const literalKind ***REMOVED*** isLiteralKind(leftKind) && leftKind ***REMOVED******REMOVED******REMOVED*** getLiteralKindOfBinaryPlusOperand(node.right) ? leftKind : 0 /* Unknown */;
      node.cachedLiteralKind ***REMOVED*** literalKind;
      return literalKind;
    }
    return 0 /* Unknown */;
  }
  function parenthesizeBinaryOperand(binaryOperator, operand, isLeftSideOfBinary, leftOperand) {
    const skipped ***REMOVED*** skipPartiallyEmittedExpressions(operand);
    if (skipped.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
      return operand;
    }
    return binaryOperandNeedsParentheses(binaryOperator, operand, isLeftSideOfBinary, leftOperand) ? factory2.createParenthesizedExpression(operand) : operand;
  }
  function parenthesizeLeftSideOfBinary(binaryOperator, leftSide) {
    return parenthesizeBinaryOperand(
      binaryOperator,
      leftSide,
      /*isLeftSideOfBinary*/
      true
    );
  }
  function parenthesizeRightSideOfBinary(binaryOperator, leftSide, rightSide) {
    return parenthesizeBinaryOperand(
      binaryOperator,
      rightSide,
      /*isLeftSideOfBinary*/
      false,
      leftSide
    );
  }
  function parenthesizeExpressionOfComputedPropertyName(expression) {
    return isCommaSequence(expression) ? factory2.createParenthesizedExpression(expression) : expression;
  }
  function parenthesizeConditionOfConditionalExpression(condition) {
    const conditionalPrecedence ***REMOVED*** getOperatorPrecedence(228 /* ConditionalExpression */, 58 /* QuestionToken */);
    const emittedCondition ***REMOVED*** skipPartiallyEmittedExpressions(condition);
    const conditionPrecedence ***REMOVED*** getExpressionPrecedence(emittedCondition);
    if (compareValues(conditionPrecedence, conditionalPrecedence) !***REMOVED******REMOVED*** 1 /* GreaterThan */) {
      return factory2.createParenthesizedExpression(condition);
    }
    return condition;
  }
  function parenthesizeBranchOfConditionalExpression(branch) {
    const emittedExpression ***REMOVED*** skipPartiallyEmittedExpressions(branch);
    return isCommaSequence(emittedExpression) ? factory2.createParenthesizedExpression(branch) : branch;
  }
  function parenthesizeExpressionOfExportDefault(expression) {
    const check ***REMOVED*** skipPartiallyEmittedExpressions(expression);
    let needsParens ***REMOVED*** isCommaSequence(check);
    if (!needsParens) {
      switch (getLeftmostExpression(
        check,
        /*stopAtCallExpressions*/
        false
      ).kind) {
        case 232 /* ClassExpression */:
        case 219 /* FunctionExpression */:
          needsParens ***REMOVED*** true;
      }
    }
    return needsParens ? factory2.createParenthesizedExpression(expression) : expression;
  }
  function parenthesizeExpressionOfNew(expression) {
    const leftmostExpr ***REMOVED*** getLeftmostExpression(
      expression,
      /*stopAtCallExpressions*/
      true
    );
    switch (leftmostExpr.kind) {
      case 214 /* CallExpression */:
        return factory2.createParenthesizedExpression(expression);
      case 215 /* NewExpression */:
        return !leftmostExpr.arguments ? factory2.createParenthesizedExpression(expression) : expression;
    }
    return parenthesizeLeftSideOfAccess(expression);
  }
  function parenthesizeLeftSideOfAccess(expression, optionalChain) {
    const emittedExpression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
    if (isLeftHandSideExpression(emittedExpression) && (emittedExpression.kind !***REMOVED******REMOVED*** 215 /* NewExpression */ || emittedExpression.arguments) && (optionalChain || !isOptionalChain(emittedExpression))) {
      return expression;
    }
    return setTextRange(factory2.createParenthesizedExpression(expression), expression);
  }
  function parenthesizeOperandOfPostfixUnary(operand) {
    return isLeftHandSideExpression(operand) ? operand : setTextRange(factory2.createParenthesizedExpression(operand), operand);
  }
  function parenthesizeOperandOfPrefixUnary(operand) {
    return isUnaryExpression(operand) ? operand : setTextRange(factory2.createParenthesizedExpression(operand), operand);
  }
  function parenthesizeExpressionsOfCommaDelimitedList(elements) {
    const result ***REMOVED*** sameMap(elements, parenthesizeExpressionForDisallowedComma);
    return setTextRange(factory2.createNodeArray(result, elements.hasTrailingComma), elements);
  }
  function parenthesizeExpressionForDisallowedComma(expression) {
    const emittedExpression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
    const expressionPrecedence ***REMOVED*** getExpressionPrecedence(emittedExpression);
    const commaPrecedence ***REMOVED*** getOperatorPrecedence(227 /* BinaryExpression */, 28 /* CommaToken */);
    return expressionPrecedence > commaPrecedence ? expression : setTextRange(factory2.createParenthesizedExpression(expression), expression);
  }
  function parenthesizeExpressionOfExpressionStatement(expression) {
    const emittedExpression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
    if (isCallExpression(emittedExpression)) {
      const callee ***REMOVED*** emittedExpression.expression;
      const kind ***REMOVED*** skipPartiallyEmittedExpressions(callee).kind;
      if (kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        const updated ***REMOVED*** factory2.updateCallExpression(
          emittedExpression,
          setTextRange(factory2.createParenthesizedExpression(callee), callee),
          emittedExpression.typeArguments,
          emittedExpression.arguments
        );
        return factory2.restoreOuterExpressions(expression, updated, 8 /* PartiallyEmittedExpressions */);
      }
    }
    const leftmostExpressionKind ***REMOVED*** getLeftmostExpression(
      emittedExpression,
      /*stopAtCallExpressions*/
      false
    ).kind;
    if (leftmostExpressionKind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || leftmostExpressionKind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */) {
      return setTextRange(factory2.createParenthesizedExpression(expression), expression);
    }
    return expression;
  }
  function parenthesizeConciseBodyOfArrowFunction(body) {
    if (!isBlock(body) && (isCommaSequence(body) || getLeftmostExpression(
      body,
      /*stopAtCallExpressions*/
      false
    ).kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */)) {
      return setTextRange(factory2.createParenthesizedExpression(body), body);
    }
    return body;
  }
  function parenthesizeCheckTypeOfConditionalType(checkType) {
    switch (checkType.kind) {
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */:
      case 195 /* ConditionalType */:
        return factory2.createParenthesizedType(checkType);
    }
    return checkType;
  }
  function parenthesizeExtendsTypeOfConditionalType(extendsType) {
    switch (extendsType.kind) {
      case 195 /* ConditionalType */:
        return factory2.createParenthesizedType(extendsType);
    }
    return extendsType;
  }
  function parenthesizeConstituentTypeOfUnionType(type) {
    switch (type.kind) {
      case 193 /* UnionType */:
      // Not strictly necessary, but a union containing a union should have been flattened
      case 194 /* IntersectionType */:
        return factory2.createParenthesizedType(type);
    }
    return parenthesizeCheckTypeOfConditionalType(type);
  }
  function parenthesizeConstituentTypesOfUnionType(members) {
    return factory2.createNodeArray(sameMap(members, parenthesizeConstituentTypeOfUnionType));
  }
  function parenthesizeConstituentTypeOfIntersectionType(type) {
    switch (type.kind) {
      case 193 /* UnionType */:
      case 194 /* IntersectionType */:
        return factory2.createParenthesizedType(type);
    }
    return parenthesizeConstituentTypeOfUnionType(type);
  }
  function parenthesizeConstituentTypesOfIntersectionType(members) {
    return factory2.createNodeArray(sameMap(members, parenthesizeConstituentTypeOfIntersectionType));
  }
  function parenthesizeOperandOfTypeOperator(type) {
    switch (type.kind) {
      case 194 /* IntersectionType */:
        return factory2.createParenthesizedType(type);
    }
    return parenthesizeConstituentTypeOfIntersectionType(type);
  }
  function parenthesizeOperandOfReadonlyTypeOperator(type) {
    switch (type.kind) {
      case 199 /* TypeOperator */:
        return factory2.createParenthesizedType(type);
    }
    return parenthesizeOperandOfTypeOperator(type);
  }
  function parenthesizeNonArrayTypeOfPostfixType(type) {
    switch (type.kind) {
      case 196 /* InferType */:
      case 199 /* TypeOperator */:
      case 187 /* TypeQuery */:
        return factory2.createParenthesizedType(type);
    }
    return parenthesizeOperandOfTypeOperator(type);
  }
  function parenthesizeElementTypesOfTupleType(types) {
    return factory2.createNodeArray(sameMap(types, parenthesizeElementTypeOfTupleType));
  }
  function parenthesizeElementTypeOfTupleType(type) {
    if (hasJSDocPostfixQuestion(type)) return factory2.createParenthesizedType(type);
    return type;
  }
  function hasJSDocPostfixQuestion(type) {
    if (isJSDocNullableType(type)) return type.postfix;
    if (isNamedTupleMember(type)) return hasJSDocPostfixQuestion(type.type);
    if (isFunctionTypeNode(type) || isConstructorTypeNode(type) || isTypeOperatorNode(type)) return hasJSDocPostfixQuestion(type.type);
    if (isConditionalTypeNode(type)) return hasJSDocPostfixQuestion(type.falseType);
    if (isUnionTypeNode(type)) return hasJSDocPostfixQuestion(last(type.types));
    if (isIntersectionTypeNode(type)) return hasJSDocPostfixQuestion(last(type.types));
    if (isInferTypeNode(type)) return !!type.typeParameter.constraint && hasJSDocPostfixQuestion(type.typeParameter.constraint);
    return false;
  }
  function parenthesizeTypeOfOptionalType(type) {
    if (hasJSDocPostfixQuestion(type)) return factory2.createParenthesizedType(type);
    return parenthesizeNonArrayTypeOfPostfixType(type);
  }
  function parenthesizeLeadingTypeArgument(node) {
    return isFunctionOrConstructorTypeNode(node) && node.typeParameters ? factory2.createParenthesizedType(node) : node;
  }
  function parenthesizeOrdinalTypeArgument(node, i) {
    return i ***REMOVED******REMOVED******REMOVED*** 0 ? parenthesizeLeadingTypeArgument(node) : node;
  }
  function parenthesizeTypeArguments(typeArguments) {
    if (some(typeArguments)) {
      return factory2.createNodeArray(sameMap(typeArguments, parenthesizeOrdinalTypeArgument));
    }
  }
}
var nullParenthesizerRules ***REMOVED*** {
  getParenthesizeLeftSideOfBinaryForOperator: (_) ***REMOVED***> identity,
  getParenthesizeRightSideOfBinaryForOperator: (_) ***REMOVED***> identity,
  parenthesizeLeftSideOfBinary: (_binaryOperator, leftSide) ***REMOVED***> leftSide,
  parenthesizeRightSideOfBinary: (_binaryOperator, _leftSide, rightSide) ***REMOVED***> rightSide,
  parenthesizeExpressionOfComputedPropertyName: identity,
  parenthesizeConditionOfConditionalExpression: identity,
  parenthesizeBranchOfConditionalExpression: identity,
  parenthesizeExpressionOfExportDefault: identity,
  parenthesizeExpressionOfNew: (expression) ***REMOVED***> cast(expression, isLeftHandSideExpression),
  parenthesizeLeftSideOfAccess: (expression) ***REMOVED***> cast(expression, isLeftHandSideExpression),
  parenthesizeOperandOfPostfixUnary: (operand) ***REMOVED***> cast(operand, isLeftHandSideExpression),
  parenthesizeOperandOfPrefixUnary: (operand) ***REMOVED***> cast(operand, isUnaryExpression),
  parenthesizeExpressionsOfCommaDelimitedList: (nodes) ***REMOVED***> cast(nodes, isNodeArray),
  parenthesizeExpressionForDisallowedComma: identity,
  parenthesizeExpressionOfExpressionStatement: identity,
  parenthesizeConciseBodyOfArrowFunction: identity,
  parenthesizeCheckTypeOfConditionalType: identity,
  parenthesizeExtendsTypeOfConditionalType: identity,
  parenthesizeConstituentTypesOfUnionType: (nodes) ***REMOVED***> cast(nodes, isNodeArray),
  parenthesizeConstituentTypeOfUnionType: identity,
  parenthesizeConstituentTypesOfIntersectionType: (nodes) ***REMOVED***> cast(nodes, isNodeArray),
  parenthesizeConstituentTypeOfIntersectionType: identity,
  parenthesizeOperandOfTypeOperator: identity,
  parenthesizeOperandOfReadonlyTypeOperator: identity,
  parenthesizeNonArrayTypeOfPostfixType: identity,
  parenthesizeElementTypesOfTupleType: (nodes) ***REMOVED***> cast(nodes, isNodeArray),
  parenthesizeElementTypeOfTupleType: identity,
  parenthesizeTypeOfOptionalType: identity,
  parenthesizeTypeArguments: (nodes) ***REMOVED***> nodes && cast(nodes, isNodeArray),
  parenthesizeLeadingTypeArgument: identity
};

// src/compiler/factory/nodeConverters.ts
function createNodeConverters(factory2) {
  return {
    convertToFunctionBlock,
    convertToFunctionExpression,
    convertToClassExpression,
    convertToArrayAssignmentElement,
    convertToObjectAssignmentElement,
    convertToAssignmentPattern,
    convertToObjectAssignmentPattern,
    convertToArrayAssignmentPattern,
    convertToAssignmentElementTarget
  };
  function convertToFunctionBlock(node, multiLine) {
    if (isBlock(node)) return node;
    const returnStatement ***REMOVED*** factory2.createReturnStatement(node);
    setTextRange(returnStatement, node);
    const body ***REMOVED*** factory2.createBlock([returnStatement], multiLine);
    setTextRange(body, node);
    return body;
  }
  function convertToFunctionExpression(node) {
    var _a;
    if (!node.body) return Debug.fail(`Cannot convert a FunctionDeclaration without a body`);
    const updated ***REMOVED*** factory2.createFunctionExpression(
      (_a ***REMOVED*** getModifiers(node)) ***REMOVED******REMOVED*** null ? void 0 : _a.filter((modifier) ***REMOVED***> !isExportModifier(modifier) && !isDefaultModifier(modifier)),
      node.asteriskToken,
      node.name,
      node.typeParameters,
      node.parameters,
      node.type,
      node.body
    );
    setOriginalNode(updated, node);
    setTextRange(updated, node);
    if (getStartsOnNewLine(node)) {
      setStartsOnNewLine(
        updated,
        /*newLine*/
        true
      );
    }
    return updated;
  }
  function convertToClassExpression(node) {
    var _a;
    const updated ***REMOVED*** factory2.createClassExpression(
      (_a ***REMOVED*** node.modifiers) ***REMOVED******REMOVED*** null ? void 0 : _a.filter((modifier) ***REMOVED***> !isExportModifier(modifier) && !isDefaultModifier(modifier)),
      node.name,
      node.typeParameters,
      node.heritageClauses,
      node.members
    );
    setOriginalNode(updated, node);
    setTextRange(updated, node);
    if (getStartsOnNewLine(node)) {
      setStartsOnNewLine(
        updated,
        /*newLine*/
        true
      );
    }
    return updated;
  }
  function convertToArrayAssignmentElement(element) {
    if (isBindingElement(element)) {
      if (element.dotDotDotToken) {
        Debug.assertNode(element.name, isIdentifier);
        return setOriginalNode(setTextRange(factory2.createSpreadElement(element.name), element), element);
      }
      const expression ***REMOVED*** convertToAssignmentElementTarget(element.name);
      return element.initializer ? setOriginalNode(
        setTextRange(
          factory2.createAssignment(expression, element.initializer),
          element
        ),
        element
      ) : expression;
    }
    return cast(element, isExpression);
  }
  function convertToObjectAssignmentElement(element) {
    if (isBindingElement(element)) {
      if (element.dotDotDotToken) {
        Debug.assertNode(element.name, isIdentifier);
        return setOriginalNode(setTextRange(factory2.createSpreadAssignment(element.name), element), element);
      }
      if (element.propertyName) {
        const expression ***REMOVED*** convertToAssignmentElementTarget(element.name);
        return setOriginalNode(setTextRange(factory2.createPropertyAssignment(element.propertyName, element.initializer ? factory2.createAssignment(expression, element.initializer) : expression), element), element);
      }
      Debug.assertNode(element.name, isIdentifier);
      return setOriginalNode(setTextRange(factory2.createShorthandPropertyAssignment(element.name, element.initializer), element), element);
    }
    return cast(element, isObjectLiteralElementLike);
  }
  function convertToAssignmentPattern(node) {
    switch (node.kind) {
      case 208 /* ArrayBindingPattern */:
      case 210 /* ArrayLiteralExpression */:
        return convertToArrayAssignmentPattern(node);
      case 207 /* ObjectBindingPattern */:
      case 211 /* ObjectLiteralExpression */:
        return convertToObjectAssignmentPattern(node);
    }
  }
  function convertToObjectAssignmentPattern(node) {
    if (isObjectBindingPattern(node)) {
      return setOriginalNode(
        setTextRange(
          factory2.createObjectLiteralExpression(map(node.elements, convertToObjectAssignmentElement)),
          node
        ),
        node
      );
    }
    return cast(node, isObjectLiteralExpression);
  }
  function convertToArrayAssignmentPattern(node) {
    if (isArrayBindingPattern(node)) {
      return setOriginalNode(
        setTextRange(
          factory2.createArrayLiteralExpression(map(node.elements, convertToArrayAssignmentElement)),
          node
        ),
        node
      );
    }
    return cast(node, isArrayLiteralExpression);
  }
  function convertToAssignmentElementTarget(node) {
    if (isBindingPattern(node)) {
      return convertToAssignmentPattern(node);
    }
    return cast(node, isExpression);
  }
}
var nullNodeConverters ***REMOVED*** {
  convertToFunctionBlock: notImplemented,
  convertToFunctionExpression: notImplemented,
  convertToClassExpression: notImplemented,
  convertToArrayAssignmentElement: notImplemented,
  convertToObjectAssignmentElement: notImplemented,
  convertToAssignmentPattern: notImplemented,
  convertToObjectAssignmentPattern: notImplemented,
  convertToArrayAssignmentPattern: notImplemented,
  convertToAssignmentElementTarget: notImplemented
};

// src/compiler/factory/nodeFactory.ts
var nextAutoGenerateId ***REMOVED*** 0;
var nodeFactoryPatchers ***REMOVED*** [];
function createNodeFactory(flags, baseFactory2) {
  const setOriginal ***REMOVED*** flags & 8 /* NoOriginalNode */ ? identity : setOriginalNode;
  const parenthesizerRules ***REMOVED*** memoize(() ***REMOVED***> flags & 1 /* NoParenthesizerRules */ ? nullParenthesizerRules : createParenthesizerRules(factory2));
  const converters ***REMOVED*** memoize(() ***REMOVED***> flags & 2 /* NoNodeConverters */ ? nullNodeConverters : createNodeConverters(factory2));
  const getBinaryCreateFunction ***REMOVED*** memoizeOne((operator) ***REMOVED***> (left, right) ***REMOVED***> createBinaryExpression(left, operator, right));
  const getPrefixUnaryCreateFunction ***REMOVED*** memoizeOne((operator) ***REMOVED***> (operand) ***REMOVED***> createPrefixUnaryExpression(operator, operand));
  const getPostfixUnaryCreateFunction ***REMOVED*** memoizeOne((operator) ***REMOVED***> (operand) ***REMOVED***> createPostfixUnaryExpression(operand, operator));
  const getJSDocPrimaryTypeCreateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> () ***REMOVED***> createJSDocPrimaryTypeWorker(kind));
  const getJSDocUnaryTypeCreateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (type) ***REMOVED***> createJSDocUnaryTypeWorker(kind, type));
  const getJSDocUnaryTypeUpdateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (node, type) ***REMOVED***> updateJSDocUnaryTypeWorker(kind, node, type));
  const getJSDocPrePostfixUnaryTypeCreateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (type, postfix) ***REMOVED***> createJSDocPrePostfixUnaryTypeWorker(kind, type, postfix));
  const getJSDocPrePostfixUnaryTypeUpdateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (node, type) ***REMOVED***> updateJSDocPrePostfixUnaryTypeWorker(kind, node, type));
  const getJSDocSimpleTagCreateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (tagName, comment) ***REMOVED***> createJSDocSimpleTagWorker(kind, tagName, comment));
  const getJSDocSimpleTagUpdateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (node, tagName, comment) ***REMOVED***> updateJSDocSimpleTagWorker(kind, node, tagName, comment));
  const getJSDocTypeLikeTagCreateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (tagName, typeExpression, comment) ***REMOVED***> createJSDocTypeLikeTagWorker(kind, tagName, typeExpression, comment));
  const getJSDocTypeLikeTagUpdateFunction ***REMOVED*** memoizeOne((kind) ***REMOVED***> (node, tagName, typeExpression, comment) ***REMOVED***> updateJSDocTypeLikeTagWorker(kind, node, tagName, typeExpression, comment));
  const factory2 ***REMOVED*** {
    get parenthesizer() {
      return parenthesizerRules();
    },
    get converters() {
      return converters();
    },
    baseFactory: baseFactory2,
    flags,
    createNodeArray,
    createNumericLiteral,
    createBigIntLiteral,
    createStringLiteral,
    createStringLiteralFromNode,
    createRegularExpressionLiteral,
    createLiteralLikeNode,
    createIdentifier,
    createTempVariable,
    createLoopVariable,
    createUniqueName,
    getGeneratedNameForNode,
    createPrivateIdentifier,
    createUniquePrivateName,
    getGeneratedPrivateNameForNode,
    createToken,
    createSuper,
    createThis,
    createNull,
    createTrue,
    createFalse,
    createModifier,
    createModifiersFromModifierFlags,
    createQualifiedName,
    updateQualifiedName,
    createComputedPropertyName,
    updateComputedPropertyName,
    createTypeParameterDeclaration,
    updateTypeParameterDeclaration,
    createParameterDeclaration,
    updateParameterDeclaration,
    createDecorator,
    updateDecorator,
    createPropertySignature,
    updatePropertySignature,
    createPropertyDeclaration,
    updatePropertyDeclaration,
    createMethodSignature,
    updateMethodSignature,
    createMethodDeclaration,
    updateMethodDeclaration,
    createConstructorDeclaration,
    updateConstructorDeclaration,
    createGetAccessorDeclaration,
    updateGetAccessorDeclaration,
    createSetAccessorDeclaration,
    updateSetAccessorDeclaration,
    createCallSignature,
    updateCallSignature,
    createConstructSignature,
    updateConstructSignature,
    createIndexSignature,
    updateIndexSignature,
    createClassStaticBlockDeclaration,
    updateClassStaticBlockDeclaration,
    createTemplateLiteralTypeSpan,
    updateTemplateLiteralTypeSpan,
    createKeywordTypeNode,
    createTypePredicateNode,
    updateTypePredicateNode,
    createTypeReferenceNode,
    updateTypeReferenceNode,
    createFunctionTypeNode,
    updateFunctionTypeNode,
    createConstructorTypeNode,
    updateConstructorTypeNode,
    createTypeQueryNode,
    updateTypeQueryNode,
    createTypeLiteralNode,
    updateTypeLiteralNode,
    createArrayTypeNode,
    updateArrayTypeNode,
    createTupleTypeNode,
    updateTupleTypeNode,
    createNamedTupleMember,
    updateNamedTupleMember,
    createOptionalTypeNode,
    updateOptionalTypeNode,
    createRestTypeNode,
    updateRestTypeNode,
    createUnionTypeNode,
    updateUnionTypeNode,
    createIntersectionTypeNode,
    updateIntersectionTypeNode,
    createConditionalTypeNode,
    updateConditionalTypeNode,
    createInferTypeNode,
    updateInferTypeNode,
    createImportTypeNode,
    updateImportTypeNode,
    createParenthesizedType,
    updateParenthesizedType,
    createThisTypeNode,
    createTypeOperatorNode,
    updateTypeOperatorNode,
    createIndexedAccessTypeNode,
    updateIndexedAccessTypeNode,
    createMappedTypeNode,
    updateMappedTypeNode,
    createLiteralTypeNode,
    updateLiteralTypeNode,
    createTemplateLiteralType,
    updateTemplateLiteralType,
    createObjectBindingPattern,
    updateObjectBindingPattern,
    createArrayBindingPattern,
    updateArrayBindingPattern,
    createBindingElement,
    updateBindingElement,
    createArrayLiteralExpression,
    updateArrayLiteralExpression,
    createObjectLiteralExpression,
    updateObjectLiteralExpression,
    createPropertyAccessExpression: flags & 4 /* NoIndentationOnFreshPropertyAccess */ ? (expression, name) ***REMOVED***> setEmitFlags(createPropertyAccessExpression(expression, name), 262144 /* NoIndentation */) : createPropertyAccessExpression,
    updatePropertyAccessExpression,
    createPropertyAccessChain: flags & 4 /* NoIndentationOnFreshPropertyAccess */ ? (expression, questionDotToken, name) ***REMOVED***> setEmitFlags(createPropertyAccessChain(expression, questionDotToken, name), 262144 /* NoIndentation */) : createPropertyAccessChain,
    updatePropertyAccessChain,
    createElementAccessExpression,
    updateElementAccessExpression,
    createElementAccessChain,
    updateElementAccessChain,
    createCallExpression,
    updateCallExpression,
    createCallChain,
    updateCallChain,
    createNewExpression,
    updateNewExpression,
    createTaggedTemplateExpression,
    updateTaggedTemplateExpression,
    createTypeAssertion,
    updateTypeAssertion,
    createParenthesizedExpression,
    updateParenthesizedExpression,
    createFunctionExpression,
    updateFunctionExpression,
    createArrowFunction,
    updateArrowFunction,
    createDeleteExpression,
    updateDeleteExpression,
    createTypeOfExpression,
    updateTypeOfExpression,
    createVoidExpression,
    updateVoidExpression,
    createAwaitExpression,
    updateAwaitExpression,
    createPrefixUnaryExpression,
    updatePrefixUnaryExpression,
    createPostfixUnaryExpression,
    updatePostfixUnaryExpression,
    createBinaryExpression,
    updateBinaryExpression,
    createConditionalExpression,
    updateConditionalExpression,
    createTemplateExpression,
    updateTemplateExpression,
    createTemplateHead,
    createTemplateMiddle,
    createTemplateTail,
    createNoSubstitutionTemplateLiteral,
    createTemplateLiteralLikeNode,
    createYieldExpression,
    updateYieldExpression,
    createSpreadElement,
    updateSpreadElement,
    createClassExpression,
    updateClassExpression,
    createOmittedExpression,
    createExpressionWithTypeArguments,
    updateExpressionWithTypeArguments,
    createAsExpression,
    updateAsExpression,
    createNonNullExpression,
    updateNonNullExpression,
    createSatisfiesExpression,
    updateSatisfiesExpression,
    createNonNullChain,
    updateNonNullChain,
    createMetaProperty,
    updateMetaProperty,
    createTemplateSpan,
    updateTemplateSpan,
    createSemicolonClassElement,
    createBlock,
    updateBlock,
    createVariableStatement,
    updateVariableStatement,
    createEmptyStatement,
    createExpressionStatement,
    updateExpressionStatement,
    createIfStatement,
    updateIfStatement,
    createDoStatement,
    updateDoStatement,
    createWhileStatement,
    updateWhileStatement,
    createForStatement,
    updateForStatement,
    createForInStatement,
    updateForInStatement,
    createForOfStatement,
    updateForOfStatement,
    createContinueStatement,
    updateContinueStatement,
    createBreakStatement,
    updateBreakStatement,
    createReturnStatement,
    updateReturnStatement,
    createWithStatement,
    updateWithStatement,
    createSwitchStatement,
    updateSwitchStatement,
    createLabeledStatement,
    updateLabeledStatement,
    createThrowStatement,
    updateThrowStatement,
    createTryStatement,
    updateTryStatement,
    createDebuggerStatement,
    createVariableDeclaration,
    updateVariableDeclaration,
    createVariableDeclarationList,
    updateVariableDeclarationList,
    createFunctionDeclaration,
    updateFunctionDeclaration,
    createClassDeclaration,
    updateClassDeclaration,
    createInterfaceDeclaration,
    updateInterfaceDeclaration,
    createTypeAliasDeclaration,
    updateTypeAliasDeclaration,
    createEnumDeclaration,
    updateEnumDeclaration,
    createModuleDeclaration,
    updateModuleDeclaration,
    createModuleBlock,
    updateModuleBlock,
    createCaseBlock,
    updateCaseBlock,
    createNamespaceExportDeclaration,
    updateNamespaceExportDeclaration,
    createImportEqualsDeclaration,
    updateImportEqualsDeclaration,
    createImportDeclaration,
    updateImportDeclaration,
    createImportClause,
    updateImportClause,
    createAssertClause,
    updateAssertClause,
    createAssertEntry,
    updateAssertEntry,
    createImportTypeAssertionContainer,
    updateImportTypeAssertionContainer,
    createImportAttributes,
    updateImportAttributes,
    createImportAttribute,
    updateImportAttribute,
    createNamespaceImport,
    updateNamespaceImport,
    createNamespaceExport,
    updateNamespaceExport,
    createNamedImports,
    updateNamedImports,
    createImportSpecifier,
    updateImportSpecifier,
    createExportAssignment,
    updateExportAssignment,
    createExportDeclaration,
    updateExportDeclaration,
    createNamedExports,
    updateNamedExports,
    createExportSpecifier,
    updateExportSpecifier,
    createMissingDeclaration,
    createExternalModuleReference,
    updateExternalModuleReference,
    // lazily load factory members for JSDoc types with similar structure
    get createJSDocAllType() {
      return getJSDocPrimaryTypeCreateFunction(313 /* JSDocAllType */);
    },
    get createJSDocUnknownType() {
      return getJSDocPrimaryTypeCreateFunction(314 /* JSDocUnknownType */);
    },
    get createJSDocNonNullableType() {
      return getJSDocPrePostfixUnaryTypeCreateFunction(316 /* JSDocNonNullableType */);
    },
    get updateJSDocNonNullableType() {
      return getJSDocPrePostfixUnaryTypeUpdateFunction(316 /* JSDocNonNullableType */);
    },
    get createJSDocNullableType() {
      return getJSDocPrePostfixUnaryTypeCreateFunction(315 /* JSDocNullableType */);
    },
    get updateJSDocNullableType() {
      return getJSDocPrePostfixUnaryTypeUpdateFunction(315 /* JSDocNullableType */);
    },
    get createJSDocOptionalType() {
      return getJSDocUnaryTypeCreateFunction(317 /* JSDocOptionalType */);
    },
    get updateJSDocOptionalType() {
      return getJSDocUnaryTypeUpdateFunction(317 /* JSDocOptionalType */);
    },
    get createJSDocVariadicType() {
      return getJSDocUnaryTypeCreateFunction(319 /* JSDocVariadicType */);
    },
    get updateJSDocVariadicType() {
      return getJSDocUnaryTypeUpdateFunction(319 /* JSDocVariadicType */);
    },
    get createJSDocNamepathType() {
      return getJSDocUnaryTypeCreateFunction(320 /* JSDocNamepathType */);
    },
    get updateJSDocNamepathType() {
      return getJSDocUnaryTypeUpdateFunction(320 /* JSDocNamepathType */);
    },
    createJSDocFunctionType,
    updateJSDocFunctionType,
    createJSDocTypeLiteral,
    updateJSDocTypeLiteral,
    createJSDocTypeExpression,
    updateJSDocTypeExpression,
    createJSDocSignature,
    updateJSDocSignature,
    createJSDocTemplateTag,
    updateJSDocTemplateTag,
    createJSDocTypedefTag,
    updateJSDocTypedefTag,
    createJSDocParameterTag,
    updateJSDocParameterTag,
    createJSDocPropertyTag,
    updateJSDocPropertyTag,
    createJSDocCallbackTag,
    updateJSDocCallbackTag,
    createJSDocOverloadTag,
    updateJSDocOverloadTag,
    createJSDocAugmentsTag,
    updateJSDocAugmentsTag,
    createJSDocImplementsTag,
    updateJSDocImplementsTag,
    createJSDocSeeTag,
    updateJSDocSeeTag,
    createJSDocImportTag,
    updateJSDocImportTag,
    createJSDocNameReference,
    updateJSDocNameReference,
    createJSDocMemberName,
    updateJSDocMemberName,
    createJSDocLink,
    updateJSDocLink,
    createJSDocLinkCode,
    updateJSDocLinkCode,
    createJSDocLinkPlain,
    updateJSDocLinkPlain,
    // lazily load factory members for JSDoc tags with similar structure
    get createJSDocTypeTag() {
      return getJSDocTypeLikeTagCreateFunction(345 /* JSDocTypeTag */);
    },
    get updateJSDocTypeTag() {
      return getJSDocTypeLikeTagUpdateFunction(345 /* JSDocTypeTag */);
    },
    get createJSDocReturnTag() {
      return getJSDocTypeLikeTagCreateFunction(343 /* JSDocReturnTag */);
    },
    get updateJSDocReturnTag() {
      return getJSDocTypeLikeTagUpdateFunction(343 /* JSDocReturnTag */);
    },
    get createJSDocThisTag() {
      return getJSDocTypeLikeTagCreateFunction(344 /* JSDocThisTag */);
    },
    get updateJSDocThisTag() {
      return getJSDocTypeLikeTagUpdateFunction(344 /* JSDocThisTag */);
    },
    get createJSDocAuthorTag() {
      return getJSDocSimpleTagCreateFunction(331 /* JSDocAuthorTag */);
    },
    get updateJSDocAuthorTag() {
      return getJSDocSimpleTagUpdateFunction(331 /* JSDocAuthorTag */);
    },
    get createJSDocClassTag() {
      return getJSDocSimpleTagCreateFunction(333 /* JSDocClassTag */);
    },
    get updateJSDocClassTag() {
      return getJSDocSimpleTagUpdateFunction(333 /* JSDocClassTag */);
    },
    get createJSDocPublicTag() {
      return getJSDocSimpleTagCreateFunction(334 /* JSDocPublicTag */);
    },
    get updateJSDocPublicTag() {
      return getJSDocSimpleTagUpdateFunction(334 /* JSDocPublicTag */);
    },
    get createJSDocPrivateTag() {
      return getJSDocSimpleTagCreateFunction(335 /* JSDocPrivateTag */);
    },
    get updateJSDocPrivateTag() {
      return getJSDocSimpleTagUpdateFunction(335 /* JSDocPrivateTag */);
    },
    get createJSDocProtectedTag() {
      return getJSDocSimpleTagCreateFunction(336 /* JSDocProtectedTag */);
    },
    get updateJSDocProtectedTag() {
      return getJSDocSimpleTagUpdateFunction(336 /* JSDocProtectedTag */);
    },
    get createJSDocReadonlyTag() {
      return getJSDocSimpleTagCreateFunction(337 /* JSDocReadonlyTag */);
    },
    get updateJSDocReadonlyTag() {
      return getJSDocSimpleTagUpdateFunction(337 /* JSDocReadonlyTag */);
    },
    get createJSDocOverrideTag() {
      return getJSDocSimpleTagCreateFunction(338 /* JSDocOverrideTag */);
    },
    get updateJSDocOverrideTag() {
      return getJSDocSimpleTagUpdateFunction(338 /* JSDocOverrideTag */);
    },
    get createJSDocDeprecatedTag() {
      return getJSDocSimpleTagCreateFunction(332 /* JSDocDeprecatedTag */);
    },
    get updateJSDocDeprecatedTag() {
      return getJSDocSimpleTagUpdateFunction(332 /* JSDocDeprecatedTag */);
    },
    get createJSDocThrowsTag() {
      return getJSDocTypeLikeTagCreateFunction(350 /* JSDocThrowsTag */);
    },
    get updateJSDocThrowsTag() {
      return getJSDocTypeLikeTagUpdateFunction(350 /* JSDocThrowsTag */);
    },
    get createJSDocSatisfiesTag() {
      return getJSDocTypeLikeTagCreateFunction(351 /* JSDocSatisfiesTag */);
    },
    get updateJSDocSatisfiesTag() {
      return getJSDocTypeLikeTagUpdateFunction(351 /* JSDocSatisfiesTag */);
    },
    createJSDocEnumTag,
    updateJSDocEnumTag,
    createJSDocUnknownTag,
    updateJSDocUnknownTag,
    createJSDocText,
    updateJSDocText,
    createJSDocComment,
    updateJSDocComment,
    createJsxElement,
    updateJsxElement,
    createJsxSelfClosingElement,
    updateJsxSelfClosingElement,
    createJsxOpeningElement,
    updateJsxOpeningElement,
    createJsxClosingElement,
    updateJsxClosingElement,
    createJsxFragment,
    createJsxText,
    updateJsxText,
    createJsxOpeningFragment,
    createJsxJsxClosingFragment,
    updateJsxFragment,
    createJsxAttribute,
    updateJsxAttribute,
    createJsxAttributes,
    updateJsxAttributes,
    createJsxSpreadAttribute,
    updateJsxSpreadAttribute,
    createJsxExpression,
    updateJsxExpression,
    createJsxNamespacedName,
    updateJsxNamespacedName,
    createCaseClause,
    updateCaseClause,
    createDefaultClause,
    updateDefaultClause,
    createHeritageClause,
    updateHeritageClause,
    createCatchClause,
    updateCatchClause,
    createPropertyAssignment,
    updatePropertyAssignment,
    createShorthandPropertyAssignment,
    updateShorthandPropertyAssignment,
    createSpreadAssignment,
    updateSpreadAssignment,
    createEnumMember,
    updateEnumMember,
    createSourceFile: createSourceFile2,
    updateSourceFile,
    createRedirectedSourceFile,
    createBundle,
    updateBundle,
    createSyntheticExpression,
    createSyntaxList,
    createNotEmittedStatement,
    createNotEmittedTypeElement,
    createPartiallyEmittedExpression,
    updatePartiallyEmittedExpression,
    createCommaListExpression,
    updateCommaListExpression,
    createSyntheticReferenceExpression,
    updateSyntheticReferenceExpression,
    cloneNode,
    // Lazily load factory methods for common operator factories and utilities
    get createComma() {
      return getBinaryCreateFunction(28 /* CommaToken */);
    },
    get createAssignment() {
      return getBinaryCreateFunction(64 /* EqualsToken */);
    },
    get createLogicalOr() {
      return getBinaryCreateFunction(57 /* BarBarToken */);
    },
    get createLogicalAnd() {
      return getBinaryCreateFunction(56 /* AmpersandAmpersandToken */);
    },
    get createBitwiseOr() {
      return getBinaryCreateFunction(52 /* BarToken */);
    },
    get createBitwiseXor() {
      return getBinaryCreateFunction(53 /* CaretToken */);
    },
    get createBitwiseAnd() {
      return getBinaryCreateFunction(51 /* AmpersandToken */);
    },
    get createStrictEquality() {
      return getBinaryCreateFunction(37 /* EqualsEqualsEqualsToken */);
    },
    get createStrictInequality() {
      return getBinaryCreateFunction(38 /* ExclamationEqualsEqualsToken */);
    },
    get createEquality() {
      return getBinaryCreateFunction(35 /* EqualsEqualsToken */);
    },
    get createInequality() {
      return getBinaryCreateFunction(36 /* ExclamationEqualsToken */);
    },
    get createLessThan() {
      return getBinaryCreateFunction(30 /* LessThanToken */);
    },
    get createLessThanEquals() {
      return getBinaryCreateFunction(33 /* LessThanEqualsToken */);
    },
    get createGreaterThan() {
      return getBinaryCreateFunction(32 /* GreaterThanToken */);
    },
    get createGreaterThanEquals() {
      return getBinaryCreateFunction(34 /* GreaterThanEqualsToken */);
    },
    get createLeftShift() {
      return getBinaryCreateFunction(48 /* LessThanLessThanToken */);
    },
    get createRightShift() {
      return getBinaryCreateFunction(49 /* GreaterThanGreaterThanToken */);
    },
    get createUnsignedRightShift() {
      return getBinaryCreateFunction(50 /* GreaterThanGreaterThanGreaterThanToken */);
    },
    get createAdd() {
      return getBinaryCreateFunction(40 /* PlusToken */);
    },
    get createSubtract() {
      return getBinaryCreateFunction(41 /* MinusToken */);
    },
    get createMultiply() {
      return getBinaryCreateFunction(42 /* AsteriskToken */);
    },
    get createDivide() {
      return getBinaryCreateFunction(44 /* SlashToken */);
    },
    get createModulo() {
      return getBinaryCreateFunction(45 /* PercentToken */);
    },
    get createExponent() {
      return getBinaryCreateFunction(43 /* AsteriskAsteriskToken */);
    },
    get createPrefixPlus() {
      return getPrefixUnaryCreateFunction(40 /* PlusToken */);
    },
    get createPrefixMinus() {
      return getPrefixUnaryCreateFunction(41 /* MinusToken */);
    },
    get createPrefixIncrement() {
      return getPrefixUnaryCreateFunction(46 /* PlusPlusToken */);
    },
    get createPrefixDecrement() {
      return getPrefixUnaryCreateFunction(47 /* MinusMinusToken */);
    },
    get createBitwiseNot() {
      return getPrefixUnaryCreateFunction(55 /* TildeToken */);
    },
    get createLogicalNot() {
      return getPrefixUnaryCreateFunction(54 /* ExclamationToken */);
    },
    get createPostfixIncrement() {
      return getPostfixUnaryCreateFunction(46 /* PlusPlusToken */);
    },
    get createPostfixDecrement() {
      return getPostfixUnaryCreateFunction(47 /* MinusMinusToken */);
    },
    // Compound nodes
    createImmediatelyInvokedFunctionExpression,
    createImmediatelyInvokedArrowFunction,
    createVoidZero,
    createExportDefault,
    createExternalModuleExport,
    createTypeCheck,
    createIsNotTypeCheck,
    createMethodCall,
    createGlobalMethodCall,
    createFunctionBindCall,
    createFunctionCallCall,
    createFunctionApplyCall,
    createArraySliceCall,
    createArrayConcatCall,
    createObjectDefinePropertyCall,
    createObjectGetOwnPropertyDescriptorCall,
    createReflectGetCall,
    createReflectSetCall,
    createPropertyDescriptor,
    createCallBinding,
    createAssignmentTargetWrapper,
    // Utilities
    inlineExpressions,
    getInternalName,
    getLocalName,
    getExportName,
    getDeclarationName,
    getNamespaceMemberName,
    getExternalModuleOrNamespaceExportName,
    restoreOuterExpressions,
    restoreEnclosingLabel,
    createUseStrictPrologue,
    copyPrologue,
    copyStandardPrologue,
    copyCustomPrologue,
    ensureUseStrict,
    liftToBlock,
    mergeLexicalEnvironment,
    replaceModifiers,
    replaceDecoratorsAndModifiers,
    replacePropertyName
  };
  forEach(nodeFactoryPatchers, (fn) ***REMOVED***> fn(factory2));
  return factory2;
  function createNodeArray(elements, hasTrailingComma) {
    if (elements ***REMOVED******REMOVED******REMOVED*** void 0 || elements ***REMOVED******REMOVED******REMOVED*** emptyArray) {
      elements ***REMOVED*** [];
    } else if (isNodeArray(elements)) {
      if (hasTrailingComma ***REMOVED******REMOVED******REMOVED*** void 0 || elements.hasTrailingComma ***REMOVED******REMOVED******REMOVED*** hasTrailingComma) {
        if (elements.transformFlags ***REMOVED******REMOVED******REMOVED*** void 0) {
          aggregateChildrenFlags(elements);
        }
        Debug.attachNodeArrayDebugInfo(elements);
        return elements;
      }
      const array2 ***REMOVED*** elements.slice();
      array2.pos ***REMOVED*** elements.pos;
      array2.end ***REMOVED*** elements.end;
      array2.hasTrailingComma ***REMOVED*** hasTrailingComma;
      array2.transformFlags ***REMOVED*** elements.transformFlags;
      Debug.attachNodeArrayDebugInfo(array2);
      return array2;
    }
    const length2 ***REMOVED*** elements.length;
    const array ***REMOVED*** length2 >***REMOVED*** 1 && length2 <***REMOVED*** 4 ? elements.slice() : elements;
    array.pos ***REMOVED*** -1;
    array.end ***REMOVED*** -1;
    array.hasTrailingComma ***REMOVED*** !!hasTrailingComma;
    array.transformFlags ***REMOVED*** 0 /* None */;
    aggregateChildrenFlags(array);
    Debug.attachNodeArrayDebugInfo(array);
    return array;
  }
  function createBaseNode(kind) {
    return baseFactory2.createBaseNode(kind);
  }
  function createBaseDeclaration(kind) {
    const node ***REMOVED*** createBaseNode(kind);
    node.symbol ***REMOVED*** void 0;
    node.localSymbol ***REMOVED*** void 0;
    return node;
  }
  function finishUpdateBaseSignatureDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.typeArguments ***REMOVED*** original.typeArguments;
    }
    return update(updated, original);
  }
  function createNumericLiteral(value, numericLiteralFlags ***REMOVED*** 0 /* None */) {
    const text ***REMOVED*** typeof value ***REMOVED******REMOVED******REMOVED*** "number" ? value + "" : value;
    Debug.assert(text.charCodeAt(0) !***REMOVED******REMOVED*** 45 /* minus */, "Negative numbers should be created in combination with createPrefixUnaryExpression");
    const node ***REMOVED*** createBaseDeclaration(9 /* NumericLiteral */);
    node.text ***REMOVED*** text;
    node.numericLiteralFlags ***REMOVED*** numericLiteralFlags;
    if (numericLiteralFlags & 384 /* BinaryOrOctalSpecifier */) node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
    return node;
  }
  function createBigIntLiteral(value) {
    const node ***REMOVED*** createBaseToken(10 /* BigIntLiteral */);
    node.text ***REMOVED*** typeof value ***REMOVED******REMOVED******REMOVED*** "string" ? value : pseudoBigIntToString(value) + "n";
    node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
    return node;
  }
  function createBaseStringLiteral(text, isSingleQuote) {
    const node ***REMOVED*** createBaseDeclaration(11 /* StringLiteral */);
    node.text ***REMOVED*** text;
    node.singleQuote ***REMOVED*** isSingleQuote;
    return node;
  }
  function createStringLiteral(text, isSingleQuote, hasExtendedUnicodeEscape) {
    const node ***REMOVED*** createBaseStringLiteral(text, isSingleQuote);
    node.hasExtendedUnicodeEscape ***REMOVED*** hasExtendedUnicodeEscape;
    if (hasExtendedUnicodeEscape) node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
    return node;
  }
  function createStringLiteralFromNode(sourceNode) {
    const node ***REMOVED*** createBaseStringLiteral(
      getTextOfIdentifierOrLiteral(sourceNode),
      /*isSingleQuote*/
      void 0
    );
    node.textSourceNode ***REMOVED*** sourceNode;
    return node;
  }
  function createRegularExpressionLiteral(text) {
    const node ***REMOVED*** createBaseToken(14 /* RegularExpressionLiteral */);
    node.text ***REMOVED*** text;
    return node;
  }
  function createLiteralLikeNode(kind, text) {
    switch (kind) {
      case 9 /* NumericLiteral */:
        return createNumericLiteral(
          text,
          /*numericLiteralFlags*/
          0
        );
      case 10 /* BigIntLiteral */:
        return createBigIntLiteral(text);
      case 11 /* StringLiteral */:
        return createStringLiteral(
          text,
          /*isSingleQuote*/
          void 0
        );
      case 12 /* JsxText */:
        return createJsxText(
          text,
          /*containsOnlyTriviaWhiteSpaces*/
          false
        );
      case 13 /* JsxTextAllWhiteSpaces */:
        return createJsxText(
          text,
          /*containsOnlyTriviaWhiteSpaces*/
          true
        );
      case 14 /* RegularExpressionLiteral */:
        return createRegularExpressionLiteral(text);
      case 15 /* NoSubstitutionTemplateLiteral */:
        return createTemplateLiteralLikeNode(
          kind,
          text,
          /*rawText*/
          void 0,
          /*templateFlags*/
          0
        );
    }
  }
  function createBaseIdentifier(escapedText) {
    const node ***REMOVED*** baseFactory2.createBaseIdentifierNode(80 /* Identifier */);
    node.escapedText ***REMOVED*** escapedText;
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.symbol ***REMOVED*** void 0;
    return node;
  }
  function createBaseGeneratedIdentifier(text, autoGenerateFlags, prefix, suffix) {
    const node ***REMOVED*** createBaseIdentifier(escapeLeadingUnderscores(text));
    setIdentifierAutoGenerate(node, {
      flags: autoGenerateFlags,
      id: nextAutoGenerateId,
      prefix,
      suffix
    });
    nextAutoGenerateId++;
    return node;
  }
  function createIdentifier(text, originalKeywordKind, hasExtendedUnicodeEscape) {
    if (originalKeywordKind ***REMOVED******REMOVED******REMOVED*** void 0 && text) {
      originalKeywordKind ***REMOVED*** stringToToken(text);
    }
    if (originalKeywordKind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      originalKeywordKind ***REMOVED*** void 0;
    }
    const node ***REMOVED*** createBaseIdentifier(escapeLeadingUnderscores(text));
    if (hasExtendedUnicodeEscape) node.flags |***REMOVED*** 256 /* IdentifierHasExtendedUnicodeEscape */;
    if (node.escapedText ***REMOVED******REMOVED******REMOVED*** "await") {
      node.transformFlags |***REMOVED*** 67108864 /* ContainsPossibleTopLevelAwait */;
    }
    if (node.flags & 256 /* IdentifierHasExtendedUnicodeEscape */) {
      node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
    }
    return node;
  }
  function createTempVariable(recordTempVariable, reservedInNestedScopes, prefix, suffix) {
    let flags2 ***REMOVED*** 1 /* Auto */;
    if (reservedInNestedScopes) flags2 |***REMOVED*** 8 /* ReservedInNestedScopes */;
    const name ***REMOVED*** createBaseGeneratedIdentifier("", flags2, prefix, suffix);
    if (recordTempVariable) {
      recordTempVariable(name);
    }
    return name;
  }
  function createLoopVariable(reservedInNestedScopes) {
    let flags2 ***REMOVED*** 2 /* Loop */;
    if (reservedInNestedScopes) flags2 |***REMOVED*** 8 /* ReservedInNestedScopes */;
    return createBaseGeneratedIdentifier(
      "",
      flags2,
      /*prefix*/
      void 0,
      /*suffix*/
      void 0
    );
  }
  function createUniqueName(text, flags2 ***REMOVED*** 0 /* None */, prefix, suffix) {
    Debug.assert(!(flags2 & 7 /* KindMask */), "Argument out of range: flags");
    Debug.assert((flags2 & (16 /* Optimistic */ | 32 /* FileLevel */)) !***REMOVED******REMOVED*** 32 /* FileLevel */, "GeneratedIdentifierFlags.FileLevel cannot be set without also setting GeneratedIdentifierFlags.Optimistic");
    return createBaseGeneratedIdentifier(text, 3 /* Unique */ | flags2, prefix, suffix);
  }
  function getGeneratedNameForNode(node, flags2 ***REMOVED*** 0, prefix, suffix) {
    Debug.assert(!(flags2 & 7 /* KindMask */), "Argument out of range: flags");
    const text ***REMOVED*** !node ? "" : isMemberName(node) ? formatGeneratedName(
      /*privateName*/
      false,
      prefix,
      node,
      suffix,
      idText
    ) : `generated@${getNodeId(node)}`;
    if (prefix || suffix) flags2 |***REMOVED*** 16 /* Optimistic */;
    const name ***REMOVED*** createBaseGeneratedIdentifier(text, 4 /* Node */ | flags2, prefix, suffix);
    name.original ***REMOVED*** node;
    return name;
  }
  function createBasePrivateIdentifier(escapedText) {
    const node ***REMOVED*** baseFactory2.createBasePrivateIdentifierNode(81 /* PrivateIdentifier */);
    node.escapedText ***REMOVED*** escapedText;
    node.transformFlags |***REMOVED*** 16777216 /* ContainsClassFields */;
    return node;
  }
  function createPrivateIdentifier(text) {
    if (!startsWith(text, "#")) Debug.fail("First character of private identifier must be #: " + text);
    return createBasePrivateIdentifier(escapeLeadingUnderscores(text));
  }
  function createBaseGeneratedPrivateIdentifier(text, autoGenerateFlags, prefix, suffix) {
    const node ***REMOVED*** createBasePrivateIdentifier(escapeLeadingUnderscores(text));
    setIdentifierAutoGenerate(node, {
      flags: autoGenerateFlags,
      id: nextAutoGenerateId,
      prefix,
      suffix
    });
    nextAutoGenerateId++;
    return node;
  }
  function createUniquePrivateName(text, prefix, suffix) {
    if (text && !startsWith(text, "#")) Debug.fail("First character of private identifier must be #: " + text);
    const autoGenerateFlags ***REMOVED*** 8 /* ReservedInNestedScopes */ | (text ? 3 /* Unique */ : 1 /* Auto */);
    return createBaseGeneratedPrivateIdentifier(text ?? "", autoGenerateFlags, prefix, suffix);
  }
  function getGeneratedPrivateNameForNode(node, prefix, suffix) {
    const text ***REMOVED*** isMemberName(node) ? formatGeneratedName(
      /*privateName*/
      true,
      prefix,
      node,
      suffix,
      idText
    ) : `#generated@${getNodeId(node)}`;
    const flags2 ***REMOVED*** prefix || suffix ? 16 /* Optimistic */ : 0 /* None */;
    const name ***REMOVED*** createBaseGeneratedPrivateIdentifier(text, 4 /* Node */ | flags2, prefix, suffix);
    name.original ***REMOVED*** node;
    return name;
  }
  function createBaseToken(kind) {
    return baseFactory2.createBaseTokenNode(kind);
  }
  function createToken(token) {
    Debug.assert(token >***REMOVED*** 0 /* FirstToken */ && token <***REMOVED*** 166 /* LastToken */, "Invalid token");
    Debug.assert(token <***REMOVED*** 15 /* FirstTemplateToken */ || token >***REMOVED*** 18 /* LastTemplateToken */, "Invalid token. Use 'createTemplateLiteralLikeNode' to create template literals.");
    Debug.assert(token <***REMOVED*** 9 /* FirstLiteralToken */ || token >***REMOVED*** 15 /* LastLiteralToken */, "Invalid token. Use 'createLiteralLikeNode' to create literals.");
    Debug.assert(token !***REMOVED******REMOVED*** 80 /* Identifier */, "Invalid token. Use 'createIdentifier' to create identifiers");
    const node ***REMOVED*** createBaseToken(token);
    let transformFlags ***REMOVED*** 0 /* None */;
    switch (token) {
      case 134 /* AsyncKeyword */:
        transformFlags ***REMOVED*** 256 /* ContainsES2017 */ | 128 /* ContainsES2018 */;
        break;
      case 160 /* UsingKeyword */:
        transformFlags ***REMOVED*** 4 /* ContainsESNext */;
        break;
      case 125 /* PublicKeyword */:
      case 123 /* PrivateKeyword */:
      case 124 /* ProtectedKeyword */:
      case 148 /* ReadonlyKeyword */:
      case 128 /* AbstractKeyword */:
      case 138 /* DeclareKeyword */:
      case 87 /* ConstKeyword */:
      case 133 /* AnyKeyword */:
      case 150 /* NumberKeyword */:
      case 163 /* BigIntKeyword */:
      case 146 /* NeverKeyword */:
      case 151 /* ObjectKeyword */:
      case 103 /* InKeyword */:
      case 147 /* OutKeyword */:
      case 164 /* OverrideKeyword */:
      case 154 /* StringKeyword */:
      case 136 /* BooleanKeyword */:
      case 155 /* SymbolKeyword */:
      case 116 /* VoidKeyword */:
      case 159 /* UnknownKeyword */:
      case 157 /* UndefinedKeyword */:
        transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
        break;
      case 108 /* SuperKeyword */:
        transformFlags ***REMOVED*** 1024 /* ContainsES2015 */ | 134217728 /* ContainsLexicalSuper */;
        node.flowNode ***REMOVED*** void 0;
        break;
      case 126 /* StaticKeyword */:
        transformFlags ***REMOVED*** 1024 /* ContainsES2015 */;
        break;
      case 129 /* AccessorKeyword */:
        transformFlags ***REMOVED*** 16777216 /* ContainsClassFields */;
        break;
      case 110 /* ThisKeyword */:
        transformFlags ***REMOVED*** 16384 /* ContainsLexicalThis */;
        node.flowNode ***REMOVED*** void 0;
        break;
    }
    if (transformFlags) {
      node.transformFlags |***REMOVED*** transformFlags;
    }
    return node;
  }
  function createSuper() {
    return createToken(108 /* SuperKeyword */);
  }
  function createThis() {
    return createToken(110 /* ThisKeyword */);
  }
  function createNull() {
    return createToken(106 /* NullKeyword */);
  }
  function createTrue() {
    return createToken(112 /* TrueKeyword */);
  }
  function createFalse() {
    return createToken(97 /* FalseKeyword */);
  }
  function createModifier(kind) {
    return createToken(kind);
  }
  function createModifiersFromModifierFlags(flags2) {
    const result ***REMOVED*** [];
    if (flags2 & 32 /* Export */) result.push(createModifier(95 /* ExportKeyword */));
    if (flags2 & 128 /* Ambient */) result.push(createModifier(138 /* DeclareKeyword */));
    if (flags2 & 2048 /* Default */) result.push(createModifier(90 /* DefaultKeyword */));
    if (flags2 & 4096 /* Const */) result.push(createModifier(87 /* ConstKeyword */));
    if (flags2 & 1 /* Public */) result.push(createModifier(125 /* PublicKeyword */));
    if (flags2 & 2 /* Private */) result.push(createModifier(123 /* PrivateKeyword */));
    if (flags2 & 4 /* Protected */) result.push(createModifier(124 /* ProtectedKeyword */));
    if (flags2 & 64 /* Abstract */) result.push(createModifier(128 /* AbstractKeyword */));
    if (flags2 & 256 /* Static */) result.push(createModifier(126 /* StaticKeyword */));
    if (flags2 & 16 /* Override */) result.push(createModifier(164 /* OverrideKeyword */));
    if (flags2 & 8 /* Readonly */) result.push(createModifier(148 /* ReadonlyKeyword */));
    if (flags2 & 512 /* Accessor */) result.push(createModifier(129 /* AccessorKeyword */));
    if (flags2 & 1024 /* Async */) result.push(createModifier(134 /* AsyncKeyword */));
    if (flags2 & 8192 /* In */) result.push(createModifier(103 /* InKeyword */));
    if (flags2 & 16384 /* Out */) result.push(createModifier(147 /* OutKeyword */));
    return result.length ? result : void 0;
  }
  function createQualifiedName(left, right) {
    const node ***REMOVED*** createBaseNode(167 /* QualifiedName */);
    node.left ***REMOVED*** left;
    node.right ***REMOVED*** asName(right);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.left) | propagateIdentifierNameFlags(node.right);
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateQualifiedName(node, left, right) {
    return node.left !***REMOVED******REMOVED*** left || node.right !***REMOVED******REMOVED*** right ? update(createQualifiedName(left, right), node) : node;
  }
  function createComputedPropertyName(expression) {
    const node ***REMOVED*** createBaseNode(168 /* ComputedPropertyName */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionOfComputedPropertyName(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1024 /* ContainsES2015 */ | 131072 /* ContainsComputedPropertyName */;
    return node;
  }
  function updateComputedPropertyName(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createComputedPropertyName(expression), node) : node;
  }
  function createTypeParameterDeclaration(modifiers, name, constraint, defaultType) {
    const node ***REMOVED*** createBaseDeclaration(169 /* TypeParameter */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.constraint ***REMOVED*** constraint;
    node.default ***REMOVED*** defaultType;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.expression ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateTypeParameterDeclaration(node, modifiers, name, constraint, defaultType) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.constraint !***REMOVED******REMOVED*** constraint || node.default !***REMOVED******REMOVED*** defaultType ? update(createTypeParameterDeclaration(modifiers, name, constraint, defaultType), node) : node;
  }
  function createParameterDeclaration(modifiers, dotDotDotToken, name, questionToken, type, initializer) {
    const node ***REMOVED*** createBaseDeclaration(170 /* Parameter */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.dotDotDotToken ***REMOVED*** dotDotDotToken;
    node.name ***REMOVED*** asName(name);
    node.questionToken ***REMOVED*** questionToken;
    node.type ***REMOVED*** type;
    node.initializer ***REMOVED*** asInitializer(initializer);
    if (isThisIdentifier(node.name)) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.dotDotDotToken) | propagateNameFlags(node.name) | propagateChildFlags(node.questionToken) | propagateChildFlags(node.initializer) | (node.questionToken ?? node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | (node.dotDotDotToken ?? node.initializer ? 1024 /* ContainsES2015 */ : 0 /* None */) | (modifiersToFlags(node.modifiers) & 31 /* ParameterPropertyModifier */ ? 8192 /* ContainsTypeScriptClassSyntax */ : 0 /* None */);
    }
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateParameterDeclaration(node, modifiers, dotDotDotToken, name, questionToken, type, initializer) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.dotDotDotToken !***REMOVED******REMOVED*** dotDotDotToken || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** questionToken || node.type !***REMOVED******REMOVED*** type || node.initializer !***REMOVED******REMOVED*** initializer ? update(createParameterDeclaration(modifiers, dotDotDotToken, name, questionToken, type, initializer), node) : node;
  }
  function createDecorator(expression) {
    const node ***REMOVED*** createBaseNode(171 /* Decorator */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
      expression,
      /*optionalChain*/
      false
    );
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1 /* ContainsTypeScript */ | 8192 /* ContainsTypeScriptClassSyntax */ | 33554432 /* ContainsDecorators */;
    return node;
  }
  function updateDecorator(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createDecorator(expression), node) : node;
  }
  function createPropertySignature(modifiers, name, questionToken, type) {
    const node ***REMOVED*** createBaseDeclaration(172 /* PropertySignature */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.type ***REMOVED*** type;
    node.questionToken ***REMOVED*** questionToken;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.initializer ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updatePropertySignature(node, modifiers, name, questionToken, type) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** questionToken || node.type !***REMOVED******REMOVED*** type ? finishUpdatePropertySignature(createPropertySignature(modifiers, name, questionToken, type), node) : node;
  }
  function finishUpdatePropertySignature(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.initializer ***REMOVED*** original.initializer;
    }
    return update(updated, original);
  }
  function createPropertyDeclaration(modifiers, name, questionOrExclamationToken, type, initializer) {
    const node ***REMOVED*** createBaseDeclaration(173 /* PropertyDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.questionToken ***REMOVED*** questionOrExclamationToken && isQuestionToken(questionOrExclamationToken) ? questionOrExclamationToken : void 0;
    node.exclamationToken ***REMOVED*** questionOrExclamationToken && isExclamationToken(questionOrExclamationToken) ? questionOrExclamationToken : void 0;
    node.type ***REMOVED*** type;
    node.initializer ***REMOVED*** asInitializer(initializer);
    const isAmbient ***REMOVED*** node.flags & 33554432 /* Ambient */ || modifiersToFlags(node.modifiers) & 128 /* Ambient */;
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateNameFlags(node.name) | propagateChildFlags(node.initializer) | (isAmbient || node.questionToken || node.exclamationToken || node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | (isComputedPropertyName(node.name) || modifiersToFlags(node.modifiers) & 256 /* Static */ && node.initializer ? 8192 /* ContainsTypeScriptClassSyntax */ : 0 /* None */) | 16777216 /* ContainsClassFields */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updatePropertyDeclaration(node, modifiers, name, questionOrExclamationToken, type, initializer) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** (questionOrExclamationToken !***REMOVED******REMOVED*** void 0 && isQuestionToken(questionOrExclamationToken) ? questionOrExclamationToken : void 0) || node.exclamationToken !***REMOVED******REMOVED*** (questionOrExclamationToken !***REMOVED******REMOVED*** void 0 && isExclamationToken(questionOrExclamationToken) ? questionOrExclamationToken : void 0) || node.type !***REMOVED******REMOVED*** type || node.initializer !***REMOVED******REMOVED*** initializer ? update(createPropertyDeclaration(modifiers, name, questionOrExclamationToken, type, initializer), node) : node;
  }
  function createMethodSignature(modifiers, name, questionToken, typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(174 /* MethodSignature */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.questionToken ***REMOVED*** questionToken;
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateMethodSignature(node, modifiers, name, questionToken, typeParameters, parameters, type) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** questionToken || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? finishUpdateBaseSignatureDeclaration(createMethodSignature(modifiers, name, questionToken, typeParameters, parameters, type), node) : node;
  }
  function createMethodDeclaration(modifiers, asteriskToken, name, questionToken, typeParameters, parameters, type, body) {
    const node ***REMOVED*** createBaseDeclaration(175 /* MethodDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.asteriskToken ***REMOVED*** asteriskToken;
    node.name ***REMOVED*** asName(name);
    node.questionToken ***REMOVED*** questionToken;
    node.exclamationToken ***REMOVED*** void 0;
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.body ***REMOVED*** body;
    if (!node.body) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      const isAsync ***REMOVED*** modifiersToFlags(node.modifiers) & 1024 /* Async */;
      const isGenerator ***REMOVED*** !!node.asteriskToken;
      const isAsyncGenerator ***REMOVED*** isAsync && isGenerator;
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.asteriskToken) | propagateNameFlags(node.name) | propagateChildFlags(node.questionToken) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.type) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (isAsyncGenerator ? 128 /* ContainsES2018 */ : isAsync ? 256 /* ContainsES2017 */ : isGenerator ? 2048 /* ContainsGenerator */ : 0 /* None */) | (node.questionToken || node.typeParameters || node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | 1024 /* ContainsES2015 */;
    }
    node.typeArguments ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateMethodDeclaration(node, modifiers, asteriskToken, name, questionToken, typeParameters, parameters, type, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.asteriskToken !***REMOVED******REMOVED*** asteriskToken || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** questionToken || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.body !***REMOVED******REMOVED*** body ? finishUpdateMethodDeclaration(createMethodDeclaration(modifiers, asteriskToken, name, questionToken, typeParameters, parameters, type, body), node) : node;
  }
  function finishUpdateMethodDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.exclamationToken ***REMOVED*** original.exclamationToken;
    }
    return update(updated, original);
  }
  function createClassStaticBlockDeclaration(body) {
    const node ***REMOVED*** createBaseDeclaration(176 /* ClassStaticBlockDeclaration */);
    node.body ***REMOVED*** body;
    node.transformFlags ***REMOVED*** propagateChildFlags(body) | 16777216 /* ContainsClassFields */;
    node.modifiers ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateClassStaticBlockDeclaration(node, body) {
    return node.body !***REMOVED******REMOVED*** body ? finishUpdateClassStaticBlockDeclaration(createClassStaticBlockDeclaration(body), node) : node;
  }
  function finishUpdateClassStaticBlockDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.modifiers ***REMOVED*** original.modifiers;
    }
    return update(updated, original);
  }
  function createConstructorDeclaration(modifiers, parameters, body) {
    const node ***REMOVED*** createBaseDeclaration(177 /* Constructor */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.body ***REMOVED*** body;
    if (!node.body) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | 1024 /* ContainsES2015 */;
    }
    node.typeParameters ***REMOVED*** void 0;
    node.type ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateConstructorDeclaration(node, modifiers, parameters, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.parameters !***REMOVED******REMOVED*** parameters || node.body !***REMOVED******REMOVED*** body ? finishUpdateConstructorDeclaration(createConstructorDeclaration(modifiers, parameters, body), node) : node;
  }
  function finishUpdateConstructorDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.typeParameters ***REMOVED*** original.typeParameters;
      updated.type ***REMOVED*** original.type;
    }
    return finishUpdateBaseSignatureDeclaration(updated, original);
  }
  function createGetAccessorDeclaration(modifiers, name, parameters, type, body) {
    const node ***REMOVED*** createBaseDeclaration(178 /* GetAccessor */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.body ***REMOVED*** body;
    if (!node.body) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateNameFlags(node.name) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.type) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (node.type ? 1 /* ContainsTypeScript */ : 0 /* None */);
    }
    node.typeArguments ***REMOVED*** void 0;
    node.typeParameters ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateGetAccessorDeclaration(node, modifiers, name, parameters, type, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.body !***REMOVED******REMOVED*** body ? finishUpdateGetAccessorDeclaration(createGetAccessorDeclaration(modifiers, name, parameters, type, body), node) : node;
  }
  function finishUpdateGetAccessorDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.typeParameters ***REMOVED*** original.typeParameters;
    }
    return finishUpdateBaseSignatureDeclaration(updated, original);
  }
  function createSetAccessorDeclaration(modifiers, name, parameters, body) {
    const node ***REMOVED*** createBaseDeclaration(179 /* SetAccessor */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.body ***REMOVED*** body;
    if (!node.body) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateNameFlags(node.name) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (node.type ? 1 /* ContainsTypeScript */ : 0 /* None */);
    }
    node.typeArguments ***REMOVED*** void 0;
    node.typeParameters ***REMOVED*** void 0;
    node.type ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateSetAccessorDeclaration(node, modifiers, name, parameters, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.parameters !***REMOVED******REMOVED*** parameters || node.body !***REMOVED******REMOVED*** body ? finishUpdateSetAccessorDeclaration(createSetAccessorDeclaration(modifiers, name, parameters, body), node) : node;
  }
  function finishUpdateSetAccessorDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.typeParameters ***REMOVED*** original.typeParameters;
      updated.type ***REMOVED*** original.type;
    }
    return finishUpdateBaseSignatureDeclaration(updated, original);
  }
  function createCallSignature(typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(180 /* CallSignature */);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateCallSignature(node, typeParameters, parameters, type) {
    return node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? finishUpdateBaseSignatureDeclaration(createCallSignature(typeParameters, parameters, type), node) : node;
  }
  function createConstructSignature(typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(181 /* ConstructSignature */);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateConstructSignature(node, typeParameters, parameters, type) {
    return node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? finishUpdateBaseSignatureDeclaration(createConstructSignature(typeParameters, parameters, type), node) : node;
  }
  function createIndexSignature(modifiers, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(182 /* IndexSignature */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateIndexSignature(node, modifiers, parameters, type) {
    return node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.modifiers !***REMOVED******REMOVED*** modifiers ? finishUpdateBaseSignatureDeclaration(createIndexSignature(modifiers, parameters, type), node) : node;
  }
  function createTemplateLiteralTypeSpan(type, literal) {
    const node ***REMOVED*** createBaseNode(205 /* TemplateLiteralTypeSpan */);
    node.type ***REMOVED*** type;
    node.literal ***REMOVED*** literal;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTemplateLiteralTypeSpan(node, type, literal) {
    return node.type !***REMOVED******REMOVED*** type || node.literal !***REMOVED******REMOVED*** literal ? update(createTemplateLiteralTypeSpan(type, literal), node) : node;
  }
  function createKeywordTypeNode(kind) {
    return createToken(kind);
  }
  function createTypePredicateNode(assertsModifier, parameterName, type) {
    const node ***REMOVED*** createBaseNode(183 /* TypePredicate */);
    node.assertsModifier ***REMOVED*** assertsModifier;
    node.parameterName ***REMOVED*** asName(parameterName);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypePredicateNode(node, assertsModifier, parameterName, type) {
    return node.assertsModifier !***REMOVED******REMOVED*** assertsModifier || node.parameterName !***REMOVED******REMOVED*** parameterName || node.type !***REMOVED******REMOVED*** type ? update(createTypePredicateNode(assertsModifier, parameterName, type), node) : node;
  }
  function createTypeReferenceNode(typeName, typeArguments) {
    const node ***REMOVED*** createBaseNode(184 /* TypeReference */);
    node.typeName ***REMOVED*** asName(typeName);
    node.typeArguments ***REMOVED*** typeArguments && parenthesizerRules().parenthesizeTypeArguments(createNodeArray(typeArguments));
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypeReferenceNode(node, typeName, typeArguments) {
    return node.typeName !***REMOVED******REMOVED*** typeName || node.typeArguments !***REMOVED******REMOVED*** typeArguments ? update(createTypeReferenceNode(typeName, typeArguments), node) : node;
  }
  function createFunctionTypeNode(typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(185 /* FunctionType */);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.modifiers ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateFunctionTypeNode(node, typeParameters, parameters, type) {
    return node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? finishUpdateFunctionTypeNode(createFunctionTypeNode(typeParameters, parameters, type), node) : node;
  }
  function finishUpdateFunctionTypeNode(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.modifiers ***REMOVED*** original.modifiers;
    }
    return finishUpdateBaseSignatureDeclaration(updated, original);
  }
  function createConstructorTypeNode(...args) {
    return args.length ***REMOVED******REMOVED******REMOVED*** 4 ? createConstructorTypeNode1(...args) : args.length ***REMOVED******REMOVED******REMOVED*** 3 ? createConstructorTypeNode2(...args) : Debug.fail("Incorrect number of arguments specified.");
  }
  function createConstructorTypeNode1(modifiers, typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(186 /* ConstructorType */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function createConstructorTypeNode2(typeParameters, parameters, type) {
    return createConstructorTypeNode1(
      /*modifiers*/
      void 0,
      typeParameters,
      parameters,
      type
    );
  }
  function updateConstructorTypeNode(...args) {
    return args.length ***REMOVED******REMOVED******REMOVED*** 5 ? updateConstructorTypeNode1(...args) : args.length ***REMOVED******REMOVED******REMOVED*** 4 ? updateConstructorTypeNode2(...args) : Debug.fail("Incorrect number of arguments specified.");
  }
  function updateConstructorTypeNode1(node, modifiers, typeParameters, parameters, type) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? finishUpdateBaseSignatureDeclaration(createConstructorTypeNode(modifiers, typeParameters, parameters, type), node) : node;
  }
  function updateConstructorTypeNode2(node, typeParameters, parameters, type) {
    return updateConstructorTypeNode1(node, node.modifiers, typeParameters, parameters, type);
  }
  function createTypeQueryNode(exprName, typeArguments) {
    const node ***REMOVED*** createBaseNode(187 /* TypeQuery */);
    node.exprName ***REMOVED*** exprName;
    node.typeArguments ***REMOVED*** typeArguments && parenthesizerRules().parenthesizeTypeArguments(typeArguments);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypeQueryNode(node, exprName, typeArguments) {
    return node.exprName !***REMOVED******REMOVED*** exprName || node.typeArguments !***REMOVED******REMOVED*** typeArguments ? update(createTypeQueryNode(exprName, typeArguments), node) : node;
  }
  function createTypeLiteralNode(members) {
    const node ***REMOVED*** createBaseDeclaration(188 /* TypeLiteral */);
    node.members ***REMOVED*** createNodeArray(members);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypeLiteralNode(node, members) {
    return node.members !***REMOVED******REMOVED*** members ? update(createTypeLiteralNode(members), node) : node;
  }
  function createArrayTypeNode(elementType) {
    const node ***REMOVED*** createBaseNode(189 /* ArrayType */);
    node.elementType ***REMOVED*** parenthesizerRules().parenthesizeNonArrayTypeOfPostfixType(elementType);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateArrayTypeNode(node, elementType) {
    return node.elementType !***REMOVED******REMOVED*** elementType ? update(createArrayTypeNode(elementType), node) : node;
  }
  function createTupleTypeNode(elements) {
    const node ***REMOVED*** createBaseNode(190 /* TupleType */);
    node.elements ***REMOVED*** createNodeArray(parenthesizerRules().parenthesizeElementTypesOfTupleType(elements));
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTupleTypeNode(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createTupleTypeNode(elements), node) : node;
  }
  function createNamedTupleMember(dotDotDotToken, name, questionToken, type) {
    const node ***REMOVED*** createBaseDeclaration(203 /* NamedTupleMember */);
    node.dotDotDotToken ***REMOVED*** dotDotDotToken;
    node.name ***REMOVED*** name;
    node.questionToken ***REMOVED*** questionToken;
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateNamedTupleMember(node, dotDotDotToken, name, questionToken, type) {
    return node.dotDotDotToken !***REMOVED******REMOVED*** dotDotDotToken || node.name !***REMOVED******REMOVED*** name || node.questionToken !***REMOVED******REMOVED*** questionToken || node.type !***REMOVED******REMOVED*** type ? update(createNamedTupleMember(dotDotDotToken, name, questionToken, type), node) : node;
  }
  function createOptionalTypeNode(type) {
    const node ***REMOVED*** createBaseNode(191 /* OptionalType */);
    node.type ***REMOVED*** parenthesizerRules().parenthesizeTypeOfOptionalType(type);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateOptionalTypeNode(node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createOptionalTypeNode(type), node) : node;
  }
  function createRestTypeNode(type) {
    const node ***REMOVED*** createBaseNode(192 /* RestType */);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateRestTypeNode(node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createRestTypeNode(type), node) : node;
  }
  function createUnionOrIntersectionTypeNode(kind, types, parenthesize) {
    const node ***REMOVED*** createBaseNode(kind);
    node.types ***REMOVED*** factory2.createNodeArray(parenthesize(types));
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateUnionOrIntersectionTypeNode(node, types, parenthesize) {
    return node.types !***REMOVED******REMOVED*** types ? update(createUnionOrIntersectionTypeNode(node.kind, types, parenthesize), node) : node;
  }
  function createUnionTypeNode(types) {
    return createUnionOrIntersectionTypeNode(193 /* UnionType */, types, parenthesizerRules().parenthesizeConstituentTypesOfUnionType);
  }
  function updateUnionTypeNode(node, types) {
    return updateUnionOrIntersectionTypeNode(node, types, parenthesizerRules().parenthesizeConstituentTypesOfUnionType);
  }
  function createIntersectionTypeNode(types) {
    return createUnionOrIntersectionTypeNode(194 /* IntersectionType */, types, parenthesizerRules().parenthesizeConstituentTypesOfIntersectionType);
  }
  function updateIntersectionTypeNode(node, types) {
    return updateUnionOrIntersectionTypeNode(node, types, parenthesizerRules().parenthesizeConstituentTypesOfIntersectionType);
  }
  function createConditionalTypeNode(checkType, extendsType, trueType, falseType) {
    const node ***REMOVED*** createBaseNode(195 /* ConditionalType */);
    node.checkType ***REMOVED*** parenthesizerRules().parenthesizeCheckTypeOfConditionalType(checkType);
    node.extendsType ***REMOVED*** parenthesizerRules().parenthesizeExtendsTypeOfConditionalType(extendsType);
    node.trueType ***REMOVED*** trueType;
    node.falseType ***REMOVED*** falseType;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateConditionalTypeNode(node, checkType, extendsType, trueType, falseType) {
    return node.checkType !***REMOVED******REMOVED*** checkType || node.extendsType !***REMOVED******REMOVED*** extendsType || node.trueType !***REMOVED******REMOVED*** trueType || node.falseType !***REMOVED******REMOVED*** falseType ? update(createConditionalTypeNode(checkType, extendsType, trueType, falseType), node) : node;
  }
  function createInferTypeNode(typeParameter) {
    const node ***REMOVED*** createBaseNode(196 /* InferType */);
    node.typeParameter ***REMOVED*** typeParameter;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateInferTypeNode(node, typeParameter) {
    return node.typeParameter !***REMOVED******REMOVED*** typeParameter ? update(createInferTypeNode(typeParameter), node) : node;
  }
  function createTemplateLiteralType(head, templateSpans) {
    const node ***REMOVED*** createBaseNode(204 /* TemplateLiteralType */);
    node.head ***REMOVED*** head;
    node.templateSpans ***REMOVED*** createNodeArray(templateSpans);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTemplateLiteralType(node, head, templateSpans) {
    return node.head !***REMOVED******REMOVED*** head || node.templateSpans !***REMOVED******REMOVED*** templateSpans ? update(createTemplateLiteralType(head, templateSpans), node) : node;
  }
  function createImportTypeNode(argument, attributes, qualifier, typeArguments, isTypeOf ***REMOVED*** false) {
    const node ***REMOVED*** createBaseNode(206 /* ImportType */);
    node.argument ***REMOVED*** argument;
    node.attributes ***REMOVED*** attributes;
    if (node.assertions && node.assertions.assertClause && node.attributes) {
      node.assertions.assertClause ***REMOVED*** node.attributes;
    }
    node.qualifier ***REMOVED*** qualifier;
    node.typeArguments ***REMOVED*** typeArguments && parenthesizerRules().parenthesizeTypeArguments(typeArguments);
    node.isTypeOf ***REMOVED*** isTypeOf;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateImportTypeNode(node, argument, attributes, qualifier, typeArguments, isTypeOf ***REMOVED*** node.isTypeOf) {
    return node.argument !***REMOVED******REMOVED*** argument || node.attributes !***REMOVED******REMOVED*** attributes || node.qualifier !***REMOVED******REMOVED*** qualifier || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.isTypeOf !***REMOVED******REMOVED*** isTypeOf ? update(createImportTypeNode(argument, attributes, qualifier, typeArguments, isTypeOf), node) : node;
  }
  function createParenthesizedType(type) {
    const node ***REMOVED*** createBaseNode(197 /* ParenthesizedType */);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateParenthesizedType(node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createParenthesizedType(type), node) : node;
  }
  function createThisTypeNode() {
    const node ***REMOVED*** createBaseNode(198 /* ThisType */);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function createTypeOperatorNode(operator, type) {
    const node ***REMOVED*** createBaseNode(199 /* TypeOperator */);
    node.operator ***REMOVED*** operator;
    node.type ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */ ? parenthesizerRules().parenthesizeOperandOfReadonlyTypeOperator(type) : parenthesizerRules().parenthesizeOperandOfTypeOperator(type);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypeOperatorNode(node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createTypeOperatorNode(node.operator, type), node) : node;
  }
  function createIndexedAccessTypeNode(objectType, indexType) {
    const node ***REMOVED*** createBaseNode(200 /* IndexedAccessType */);
    node.objectType ***REMOVED*** parenthesizerRules().parenthesizeNonArrayTypeOfPostfixType(objectType);
    node.indexType ***REMOVED*** indexType;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateIndexedAccessTypeNode(node, objectType, indexType) {
    return node.objectType !***REMOVED******REMOVED*** objectType || node.indexType !***REMOVED******REMOVED*** indexType ? update(createIndexedAccessTypeNode(objectType, indexType), node) : node;
  }
  function createMappedTypeNode(readonlyToken, typeParameter, nameType, questionToken, type, members) {
    const node ***REMOVED*** createBaseDeclaration(201 /* MappedType */);
    node.readonlyToken ***REMOVED*** readonlyToken;
    node.typeParameter ***REMOVED*** typeParameter;
    node.nameType ***REMOVED*** nameType;
    node.questionToken ***REMOVED*** questionToken;
    node.type ***REMOVED*** type;
    node.members ***REMOVED*** members && createNodeArray(members);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateMappedTypeNode(node, readonlyToken, typeParameter, nameType, questionToken, type, members) {
    return node.readonlyToken !***REMOVED******REMOVED*** readonlyToken || node.typeParameter !***REMOVED******REMOVED*** typeParameter || node.nameType !***REMOVED******REMOVED*** nameType || node.questionToken !***REMOVED******REMOVED*** questionToken || node.type !***REMOVED******REMOVED*** type || node.members !***REMOVED******REMOVED*** members ? update(createMappedTypeNode(readonlyToken, typeParameter, nameType, questionToken, type, members), node) : node;
  }
  function createLiteralTypeNode(literal) {
    const node ***REMOVED*** createBaseNode(202 /* LiteralType */);
    node.literal ***REMOVED*** literal;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    return node;
  }
  function updateLiteralTypeNode(node, literal) {
    return node.literal !***REMOVED******REMOVED*** literal ? update(createLiteralTypeNode(literal), node) : node;
  }
  function createObjectBindingPattern(elements) {
    const node ***REMOVED*** createBaseNode(207 /* ObjectBindingPattern */);
    node.elements ***REMOVED*** createNodeArray(elements);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements) | 1024 /* ContainsES2015 */ | 524288 /* ContainsBindingPattern */;
    if (node.transformFlags & 32768 /* ContainsRestOrSpread */) {
      node.transformFlags |***REMOVED*** 128 /* ContainsES2018 */ | 65536 /* ContainsObjectRestOrSpread */;
    }
    return node;
  }
  function updateObjectBindingPattern(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createObjectBindingPattern(elements), node) : node;
  }
  function createArrayBindingPattern(elements) {
    const node ***REMOVED*** createBaseNode(208 /* ArrayBindingPattern */);
    node.elements ***REMOVED*** createNodeArray(elements);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements) | 1024 /* ContainsES2015 */ | 524288 /* ContainsBindingPattern */;
    return node;
  }
  function updateArrayBindingPattern(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createArrayBindingPattern(elements), node) : node;
  }
  function createBindingElement(dotDotDotToken, propertyName, name, initializer) {
    const node ***REMOVED*** createBaseDeclaration(209 /* BindingElement */);
    node.dotDotDotToken ***REMOVED*** dotDotDotToken;
    node.propertyName ***REMOVED*** asName(propertyName);
    node.name ***REMOVED*** asName(name);
    node.initializer ***REMOVED*** asInitializer(initializer);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.dotDotDotToken) | propagateNameFlags(node.propertyName) | propagateNameFlags(node.name) | propagateChildFlags(node.initializer) | (node.dotDotDotToken ? 32768 /* ContainsRestOrSpread */ : 0 /* None */) | 1024 /* ContainsES2015 */;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateBindingElement(node, dotDotDotToken, propertyName, name, initializer) {
    return node.propertyName !***REMOVED******REMOVED*** propertyName || node.dotDotDotToken !***REMOVED******REMOVED*** dotDotDotToken || node.name !***REMOVED******REMOVED*** name || node.initializer !***REMOVED******REMOVED*** initializer ? update(createBindingElement(dotDotDotToken, propertyName, name, initializer), node) : node;
  }
  function createArrayLiteralExpression(elements, multiLine) {
    const node ***REMOVED*** createBaseNode(210 /* ArrayLiteralExpression */);
    const lastElement ***REMOVED*** elements && lastOrUndefined(elements);
    const elementsArray ***REMOVED*** createNodeArray(elements, lastElement && isOmittedExpression(lastElement) ? true : void 0);
    node.elements ***REMOVED*** parenthesizerRules().parenthesizeExpressionsOfCommaDelimitedList(elementsArray);
    node.multiLine ***REMOVED*** multiLine;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements);
    return node;
  }
  function updateArrayLiteralExpression(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createArrayLiteralExpression(elements, node.multiLine), node) : node;
  }
  function createObjectLiteralExpression(properties, multiLine) {
    const node ***REMOVED*** createBaseDeclaration(211 /* ObjectLiteralExpression */);
    node.properties ***REMOVED*** createNodeArray(properties);
    node.multiLine ***REMOVED*** multiLine;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.properties);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateObjectLiteralExpression(node, properties) {
    return node.properties !***REMOVED******REMOVED*** properties ? update(createObjectLiteralExpression(properties, node.multiLine), node) : node;
  }
  function createBasePropertyAccessExpression(expression, questionDotToken, name) {
    const node ***REMOVED*** createBaseDeclaration(212 /* PropertyAccessExpression */);
    node.expression ***REMOVED*** expression;
    node.questionDotToken ***REMOVED*** questionDotToken;
    node.name ***REMOVED*** name;
    node.transformFlags ***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.questionDotToken) | (isIdentifier(node.name) ? propagateIdentifierNameFlags(node.name) : propagateChildFlags(node.name) | 536870912 /* ContainsPrivateIdentifierInExpression */);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function createPropertyAccessExpression(expression, name) {
    const node ***REMOVED*** createBasePropertyAccessExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        false
      ),
      /*questionDotToken*/
      void 0,
      asName(name)
    );
    if (isSuperKeyword(expression)) {
      node.transformFlags |***REMOVED*** 256 /* ContainsES2017 */ | 128 /* ContainsES2018 */;
    }
    return node;
  }
  function updatePropertyAccessExpression(node, expression, name) {
    if (isPropertyAccessChain(node)) {
      return updatePropertyAccessChain(node, expression, node.questionDotToken, cast(name, isIdentifier));
    }
    return node.expression !***REMOVED******REMOVED*** expression || node.name !***REMOVED******REMOVED*** name ? update(createPropertyAccessExpression(expression, name), node) : node;
  }
  function createPropertyAccessChain(expression, questionDotToken, name) {
    const node ***REMOVED*** createBasePropertyAccessExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        true
      ),
      questionDotToken,
      asName(name)
    );
    node.flags |***REMOVED*** 64 /* OptionalChain */;
    node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
    return node;
  }
  function updatePropertyAccessChain(node, expression, questionDotToken, name) {
    Debug.assert(!!(node.flags & 64 /* OptionalChain */), "Cannot update a PropertyAccessExpression using updatePropertyAccessChain. Use updatePropertyAccess instead.");
    return node.expression !***REMOVED******REMOVED*** expression || node.questionDotToken !***REMOVED******REMOVED*** questionDotToken || node.name !***REMOVED******REMOVED*** name ? update(createPropertyAccessChain(expression, questionDotToken, name), node) : node;
  }
  function createBaseElementAccessExpression(expression, questionDotToken, argumentExpression) {
    const node ***REMOVED*** createBaseDeclaration(213 /* ElementAccessExpression */);
    node.expression ***REMOVED*** expression;
    node.questionDotToken ***REMOVED*** questionDotToken;
    node.argumentExpression ***REMOVED*** argumentExpression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.questionDotToken) | propagateChildFlags(node.argumentExpression);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function createElementAccessExpression(expression, index) {
    const node ***REMOVED*** createBaseElementAccessExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        false
      ),
      /*questionDotToken*/
      void 0,
      asExpression(index)
    );
    if (isSuperKeyword(expression)) {
      node.transformFlags |***REMOVED*** 256 /* ContainsES2017 */ | 128 /* ContainsES2018 */;
    }
    return node;
  }
  function updateElementAccessExpression(node, expression, argumentExpression) {
    if (isElementAccessChain(node)) {
      return updateElementAccessChain(node, expression, node.questionDotToken, argumentExpression);
    }
    return node.expression !***REMOVED******REMOVED*** expression || node.argumentExpression !***REMOVED******REMOVED*** argumentExpression ? update(createElementAccessExpression(expression, argumentExpression), node) : node;
  }
  function createElementAccessChain(expression, questionDotToken, index) {
    const node ***REMOVED*** createBaseElementAccessExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        true
      ),
      questionDotToken,
      asExpression(index)
    );
    node.flags |***REMOVED*** 64 /* OptionalChain */;
    node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
    return node;
  }
  function updateElementAccessChain(node, expression, questionDotToken, argumentExpression) {
    Debug.assert(!!(node.flags & 64 /* OptionalChain */), "Cannot update a ElementAccessExpression using updateElementAccessChain. Use updateElementAccess instead.");
    return node.expression !***REMOVED******REMOVED*** expression || node.questionDotToken !***REMOVED******REMOVED*** questionDotToken || node.argumentExpression !***REMOVED******REMOVED*** argumentExpression ? update(createElementAccessChain(expression, questionDotToken, argumentExpression), node) : node;
  }
  function createBaseCallExpression(expression, questionDotToken, typeArguments, argumentsArray) {
    const node ***REMOVED*** createBaseDeclaration(214 /* CallExpression */);
    node.expression ***REMOVED*** expression;
    node.questionDotToken ***REMOVED*** questionDotToken;
    node.typeArguments ***REMOVED*** typeArguments;
    node.arguments ***REMOVED*** argumentsArray;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.questionDotToken) | propagateChildrenFlags(node.typeArguments) | propagateChildrenFlags(node.arguments);
    if (node.typeArguments) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    if (isSuperProperty(node.expression)) {
      node.transformFlags |***REMOVED*** 16384 /* ContainsLexicalThis */;
    }
    return node;
  }
  function createCallExpression(expression, typeArguments, argumentsArray) {
    const node ***REMOVED*** createBaseCallExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        false
      ),
      /*questionDotToken*/
      void 0,
      asNodeArray(typeArguments),
      parenthesizerRules().parenthesizeExpressionsOfCommaDelimitedList(createNodeArray(argumentsArray))
    );
    if (isImportKeyword(node.expression)) {
      node.transformFlags |***REMOVED*** 8388608 /* ContainsDynamicImport */;
    }
    return node;
  }
  function updateCallExpression(node, expression, typeArguments, argumentsArray) {
    if (isCallChain(node)) {
      return updateCallChain(node, expression, node.questionDotToken, typeArguments, argumentsArray);
    }
    return node.expression !***REMOVED******REMOVED*** expression || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.arguments !***REMOVED******REMOVED*** argumentsArray ? update(createCallExpression(expression, typeArguments, argumentsArray), node) : node;
  }
  function createCallChain(expression, questionDotToken, typeArguments, argumentsArray) {
    const node ***REMOVED*** createBaseCallExpression(
      parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        true
      ),
      questionDotToken,
      asNodeArray(typeArguments),
      parenthesizerRules().parenthesizeExpressionsOfCommaDelimitedList(createNodeArray(argumentsArray))
    );
    node.flags |***REMOVED*** 64 /* OptionalChain */;
    node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
    return node;
  }
  function updateCallChain(node, expression, questionDotToken, typeArguments, argumentsArray) {
    Debug.assert(!!(node.flags & 64 /* OptionalChain */), "Cannot update a CallExpression using updateCallChain. Use updateCall instead.");
    return node.expression !***REMOVED******REMOVED*** expression || node.questionDotToken !***REMOVED******REMOVED*** questionDotToken || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.arguments !***REMOVED******REMOVED*** argumentsArray ? update(createCallChain(expression, questionDotToken, typeArguments, argumentsArray), node) : node;
  }
  function createNewExpression(expression, typeArguments, argumentsArray) {
    const node ***REMOVED*** createBaseDeclaration(215 /* NewExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionOfNew(expression);
    node.typeArguments ***REMOVED*** asNodeArray(typeArguments);
    node.arguments ***REMOVED*** argumentsArray ? parenthesizerRules().parenthesizeExpressionsOfCommaDelimitedList(argumentsArray) : void 0;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildrenFlags(node.typeArguments) | propagateChildrenFlags(node.arguments) | 32 /* ContainsES2020 */;
    if (node.typeArguments) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    return node;
  }
  function updateNewExpression(node, expression, typeArguments, argumentsArray) {
    return node.expression !***REMOVED******REMOVED*** expression || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.arguments !***REMOVED******REMOVED*** argumentsArray ? update(createNewExpression(expression, typeArguments, argumentsArray), node) : node;
  }
  function createTaggedTemplateExpression(tag, typeArguments, template) {
    const node ***REMOVED*** createBaseNode(216 /* TaggedTemplateExpression */);
    node.tag ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
      tag,
      /*optionalChain*/
      false
    );
    node.typeArguments ***REMOVED*** asNodeArray(typeArguments);
    node.template ***REMOVED*** template;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.tag) | propagateChildrenFlags(node.typeArguments) | propagateChildFlags(node.template) | 1024 /* ContainsES2015 */;
    if (node.typeArguments) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    if (hasInvalidEscape(node.template)) {
      node.transformFlags |***REMOVED*** 128 /* ContainsES2018 */;
    }
    return node;
  }
  function updateTaggedTemplateExpression(node, tag, typeArguments, template) {
    return node.tag !***REMOVED******REMOVED*** tag || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.template !***REMOVED******REMOVED*** template ? update(createTaggedTemplateExpression(tag, typeArguments, template), node) : node;
  }
  function createTypeAssertion(type, expression) {
    const node ***REMOVED*** createBaseNode(217 /* TypeAssertionExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(expression);
    node.type ***REMOVED*** type;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.type) | 1 /* ContainsTypeScript */;
    return node;
  }
  function updateTypeAssertion(node, type, expression) {
    return node.type !***REMOVED******REMOVED*** type || node.expression !***REMOVED******REMOVED*** expression ? update(createTypeAssertion(type, expression), node) : node;
  }
  function createParenthesizedExpression(expression) {
    const node ***REMOVED*** createBaseNode(218 /* ParenthesizedExpression */);
    node.expression ***REMOVED*** expression;
    node.transformFlags ***REMOVED*** propagateChildFlags(node.expression);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateParenthesizedExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createParenthesizedExpression(expression), node) : node;
  }
  function createFunctionExpression(modifiers, asteriskToken, name, typeParameters, parameters, type, body) {
    const node ***REMOVED*** createBaseDeclaration(219 /* FunctionExpression */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.asteriskToken ***REMOVED*** asteriskToken;
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.body ***REMOVED*** body;
    const isAsync ***REMOVED*** modifiersToFlags(node.modifiers) & 1024 /* Async */;
    const isGenerator ***REMOVED*** !!node.asteriskToken;
    const isAsyncGenerator ***REMOVED*** isAsync && isGenerator;
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.asteriskToken) | propagateNameFlags(node.name) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.type) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (isAsyncGenerator ? 128 /* ContainsES2018 */ : isAsync ? 256 /* ContainsES2017 */ : isGenerator ? 2048 /* ContainsGenerator */ : 0 /* None */) | (node.typeParameters || node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    node.typeArguments ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateFunctionExpression(node, modifiers, asteriskToken, name, typeParameters, parameters, type, body) {
    return node.name !***REMOVED******REMOVED*** name || node.modifiers !***REMOVED******REMOVED*** modifiers || node.asteriskToken !***REMOVED******REMOVED*** asteriskToken || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.body !***REMOVED******REMOVED*** body ? finishUpdateBaseSignatureDeclaration(createFunctionExpression(modifiers, asteriskToken, name, typeParameters, parameters, type, body), node) : node;
  }
  function createArrowFunction(modifiers, typeParameters, parameters, type, equalsGreaterThanToken, body) {
    const node ***REMOVED*** createBaseDeclaration(220 /* ArrowFunction */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.equalsGreaterThanToken ***REMOVED*** equalsGreaterThanToken ?? createToken(39 /* EqualsGreaterThanToken */);
    node.body ***REMOVED*** parenthesizerRules().parenthesizeConciseBodyOfArrowFunction(body);
    const isAsync ***REMOVED*** modifiersToFlags(node.modifiers) & 1024 /* Async */;
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.type) | propagateChildFlags(node.equalsGreaterThanToken) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (node.typeParameters || node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | (isAsync ? 256 /* ContainsES2017 */ | 16384 /* ContainsLexicalThis */ : 0 /* None */) | 1024 /* ContainsES2015 */;
    node.typeArguments ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateArrowFunction(node, modifiers, typeParameters, parameters, type, equalsGreaterThanToken, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.equalsGreaterThanToken !***REMOVED******REMOVED*** equalsGreaterThanToken || node.body !***REMOVED******REMOVED*** body ? finishUpdateBaseSignatureDeclaration(createArrowFunction(modifiers, typeParameters, parameters, type, equalsGreaterThanToken, body), node) : node;
  }
  function createDeleteExpression(expression) {
    const node ***REMOVED*** createBaseNode(221 /* DeleteExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    return node;
  }
  function updateDeleteExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createDeleteExpression(expression), node) : node;
  }
  function createTypeOfExpression(expression) {
    const node ***REMOVED*** createBaseNode(222 /* TypeOfExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    return node;
  }
  function updateTypeOfExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createTypeOfExpression(expression), node) : node;
  }
  function createVoidExpression(expression) {
    const node ***REMOVED*** createBaseNode(223 /* VoidExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    return node;
  }
  function updateVoidExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createVoidExpression(expression), node) : node;
  }
  function createAwaitExpression(expression) {
    const node ***REMOVED*** createBaseNode(224 /* AwaitExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 256 /* ContainsES2017 */ | 128 /* ContainsES2018 */ | 2097152 /* ContainsAwait */;
    return node;
  }
  function updateAwaitExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createAwaitExpression(expression), node) : node;
  }
  function createPrefixUnaryExpression(operator, operand) {
    const node ***REMOVED*** createBaseNode(225 /* PrefixUnaryExpression */);
    node.operator ***REMOVED*** operator;
    node.operand ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPrefixUnary(operand);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.operand);
    if ((operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) && isIdentifier(node.operand) && !isGeneratedIdentifier(node.operand) && !isLocalName(node.operand)) {
      node.transformFlags |***REMOVED*** 268435456 /* ContainsUpdateExpressionForIdentifier */;
    }
    return node;
  }
  function updatePrefixUnaryExpression(node, operand) {
    return node.operand !***REMOVED******REMOVED*** operand ? update(createPrefixUnaryExpression(node.operator, operand), node) : node;
  }
  function createPostfixUnaryExpression(operand, operator) {
    const node ***REMOVED*** createBaseNode(226 /* PostfixUnaryExpression */);
    node.operator ***REMOVED*** operator;
    node.operand ***REMOVED*** parenthesizerRules().parenthesizeOperandOfPostfixUnary(operand);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.operand);
    if (isIdentifier(node.operand) && !isGeneratedIdentifier(node.operand) && !isLocalName(node.operand)) {
      node.transformFlags |***REMOVED*** 268435456 /* ContainsUpdateExpressionForIdentifier */;
    }
    return node;
  }
  function updatePostfixUnaryExpression(node, operand) {
    return node.operand !***REMOVED******REMOVED*** operand ? update(createPostfixUnaryExpression(operand, node.operator), node) : node;
  }
  function createBinaryExpression(left, operator, right) {
    const node ***REMOVED*** createBaseDeclaration(227 /* BinaryExpression */);
    const operatorToken ***REMOVED*** asToken(operator);
    const operatorKind ***REMOVED*** operatorToken.kind;
    node.left ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfBinary(operatorKind, left);
    node.operatorToken ***REMOVED*** operatorToken;
    node.right ***REMOVED*** parenthesizerRules().parenthesizeRightSideOfBinary(operatorKind, node.left, right);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.left) | propagateChildFlags(node.operatorToken) | propagateChildFlags(node.right);
    if (operatorKind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
      node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
    } else if (operatorKind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      if (isObjectLiteralExpression(node.left)) {
        node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */ | 128 /* ContainsES2018 */ | 4096 /* ContainsDestructuringAssignment */ | propagateAssignmentPatternFlags(node.left);
      } else if (isArrayLiteralExpression(node.left)) {
        node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */ | 4096 /* ContainsDestructuringAssignment */ | propagateAssignmentPatternFlags(node.left);
      }
    } else if (operatorKind ***REMOVED******REMOVED******REMOVED*** 43 /* AsteriskAsteriskToken */ || operatorKind ***REMOVED******REMOVED******REMOVED*** 68 /* AsteriskAsteriskEqualsToken */) {
      node.transformFlags |***REMOVED*** 512 /* ContainsES2016 */;
    } else if (isLogicalOrCoalescingAssignmentOperator(operatorKind)) {
      node.transformFlags |***REMOVED*** 16 /* ContainsES2021 */;
    }
    if (operatorKind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ && isPrivateIdentifier(node.left)) {
      node.transformFlags |***REMOVED*** 536870912 /* ContainsPrivateIdentifierInExpression */;
    }
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function propagateAssignmentPatternFlags(node) {
    return containsObjectRestOrSpread(node) ? 65536 /* ContainsObjectRestOrSpread */ : 0 /* None */;
  }
  function updateBinaryExpression(node, left, operator, right) {
    return node.left !***REMOVED******REMOVED*** left || node.operatorToken !***REMOVED******REMOVED*** operator || node.right !***REMOVED******REMOVED*** right ? update(createBinaryExpression(left, operator, right), node) : node;
  }
  function createConditionalExpression(condition, questionToken, whenTrue, colonToken, whenFalse) {
    const node ***REMOVED*** createBaseNode(228 /* ConditionalExpression */);
    node.condition ***REMOVED*** parenthesizerRules().parenthesizeConditionOfConditionalExpression(condition);
    node.questionToken ***REMOVED*** questionToken ?? createToken(58 /* QuestionToken */);
    node.whenTrue ***REMOVED*** parenthesizerRules().parenthesizeBranchOfConditionalExpression(whenTrue);
    node.colonToken ***REMOVED*** colonToken ?? createToken(59 /* ColonToken */);
    node.whenFalse ***REMOVED*** parenthesizerRules().parenthesizeBranchOfConditionalExpression(whenFalse);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.condition) | propagateChildFlags(node.questionToken) | propagateChildFlags(node.whenTrue) | propagateChildFlags(node.colonToken) | propagateChildFlags(node.whenFalse);
    node.flowNodeWhenFalse ***REMOVED*** void 0;
    node.flowNodeWhenTrue ***REMOVED*** void 0;
    return node;
  }
  function updateConditionalExpression(node, condition, questionToken, whenTrue, colonToken, whenFalse) {
    return node.condition !***REMOVED******REMOVED*** condition || node.questionToken !***REMOVED******REMOVED*** questionToken || node.whenTrue !***REMOVED******REMOVED*** whenTrue || node.colonToken !***REMOVED******REMOVED*** colonToken || node.whenFalse !***REMOVED******REMOVED*** whenFalse ? update(createConditionalExpression(condition, questionToken, whenTrue, colonToken, whenFalse), node) : node;
  }
  function createTemplateExpression(head, templateSpans) {
    const node ***REMOVED*** createBaseNode(229 /* TemplateExpression */);
    node.head ***REMOVED*** head;
    node.templateSpans ***REMOVED*** createNodeArray(templateSpans);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.head) | propagateChildrenFlags(node.templateSpans) | 1024 /* ContainsES2015 */;
    return node;
  }
  function updateTemplateExpression(node, head, templateSpans) {
    return node.head !***REMOVED******REMOVED*** head || node.templateSpans !***REMOVED******REMOVED*** templateSpans ? update(createTemplateExpression(head, templateSpans), node) : node;
  }
  function checkTemplateLiteralLikeNode(kind, text, rawText, templateFlags ***REMOVED*** 0 /* None */) {
    Debug.assert(!(templateFlags & ~7176 /* TemplateLiteralLikeFlags */), "Unsupported template flags.");
    let cooked ***REMOVED*** void 0;
    if (rawText !***REMOVED******REMOVED*** void 0 && rawText !***REMOVED******REMOVED*** text) {
      cooked ***REMOVED*** getCookedText(kind, rawText);
      if (typeof cooked ***REMOVED******REMOVED******REMOVED*** "object") {
        return Debug.fail("Invalid raw text");
      }
    }
    if (text ***REMOVED******REMOVED******REMOVED*** void 0) {
      if (cooked ***REMOVED******REMOVED******REMOVED*** void 0) {
        return Debug.fail("Arguments 'text' and 'rawText' may not both be undefined.");
      }
      text ***REMOVED*** cooked;
    } else if (cooked !***REMOVED******REMOVED*** void 0) {
      Debug.assert(text ***REMOVED******REMOVED******REMOVED*** cooked, "Expected argument 'text' to be the normalized (i.e. 'cooked') version of argument 'rawText'.");
    }
    return text;
  }
  function getTransformFlagsOfTemplateLiteralLike(templateFlags) {
    let transformFlags ***REMOVED*** 1024 /* ContainsES2015 */;
    if (templateFlags) {
      transformFlags |***REMOVED*** 128 /* ContainsES2018 */;
    }
    return transformFlags;
  }
  function createTemplateLiteralLikeToken(kind, text, rawText, templateFlags) {
    const node ***REMOVED*** createBaseToken(kind);
    node.text ***REMOVED*** text;
    node.rawText ***REMOVED*** rawText;
    node.templateFlags ***REMOVED*** templateFlags & 7176 /* TemplateLiteralLikeFlags */;
    node.transformFlags ***REMOVED*** getTransformFlagsOfTemplateLiteralLike(node.templateFlags);
    return node;
  }
  function createTemplateLiteralLikeDeclaration(kind, text, rawText, templateFlags) {
    const node ***REMOVED*** createBaseDeclaration(kind);
    node.text ***REMOVED*** text;
    node.rawText ***REMOVED*** rawText;
    node.templateFlags ***REMOVED*** templateFlags & 7176 /* TemplateLiteralLikeFlags */;
    node.transformFlags ***REMOVED*** getTransformFlagsOfTemplateLiteralLike(node.templateFlags);
    return node;
  }
  function createTemplateLiteralLikeNode(kind, text, rawText, templateFlags) {
    if (kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */) {
      return createTemplateLiteralLikeDeclaration(kind, text, rawText, templateFlags);
    }
    return createTemplateLiteralLikeToken(kind, text, rawText, templateFlags);
  }
  function createTemplateHead(text, rawText, templateFlags) {
    text ***REMOVED*** checkTemplateLiteralLikeNode(16 /* TemplateHead */, text, rawText, templateFlags);
    return createTemplateLiteralLikeNode(16 /* TemplateHead */, text, rawText, templateFlags);
  }
  function createTemplateMiddle(text, rawText, templateFlags) {
    text ***REMOVED*** checkTemplateLiteralLikeNode(16 /* TemplateHead */, text, rawText, templateFlags);
    return createTemplateLiteralLikeNode(17 /* TemplateMiddle */, text, rawText, templateFlags);
  }
  function createTemplateTail(text, rawText, templateFlags) {
    text ***REMOVED*** checkTemplateLiteralLikeNode(16 /* TemplateHead */, text, rawText, templateFlags);
    return createTemplateLiteralLikeNode(18 /* TemplateTail */, text, rawText, templateFlags);
  }
  function createNoSubstitutionTemplateLiteral(text, rawText, templateFlags) {
    text ***REMOVED*** checkTemplateLiteralLikeNode(16 /* TemplateHead */, text, rawText, templateFlags);
    return createTemplateLiteralLikeDeclaration(15 /* NoSubstitutionTemplateLiteral */, text, rawText, templateFlags);
  }
  function createYieldExpression(asteriskToken, expression) {
    Debug.assert(!asteriskToken || !!expression, "A `YieldExpression` with an asteriskToken must have an expression.");
    const node ***REMOVED*** createBaseNode(230 /* YieldExpression */);
    node.expression ***REMOVED*** expression && parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.asteriskToken ***REMOVED*** asteriskToken;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.asteriskToken) | 1024 /* ContainsES2015 */ | 128 /* ContainsES2018 */ | 1048576 /* ContainsYield */;
    return node;
  }
  function updateYieldExpression(node, asteriskToken, expression) {
    return node.expression !***REMOVED******REMOVED*** expression || node.asteriskToken !***REMOVED******REMOVED*** asteriskToken ? update(createYieldExpression(asteriskToken, expression), node) : node;
  }
  function createSpreadElement(expression) {
    const node ***REMOVED*** createBaseNode(231 /* SpreadElement */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1024 /* ContainsES2015 */ | 32768 /* ContainsRestOrSpread */;
    return node;
  }
  function updateSpreadElement(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createSpreadElement(expression), node) : node;
  }
  function createClassExpression(modifiers, name, typeParameters, heritageClauses, members) {
    const node ***REMOVED*** createBaseDeclaration(232 /* ClassExpression */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.heritageClauses ***REMOVED*** asNodeArray(heritageClauses);
    node.members ***REMOVED*** createNodeArray(members);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateNameFlags(node.name) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.heritageClauses) | propagateChildrenFlags(node.members) | (node.typeParameters ? 1 /* ContainsTypeScript */ : 0 /* None */) | 1024 /* ContainsES2015 */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateClassExpression(node, modifiers, name, typeParameters, heritageClauses, members) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.heritageClauses !***REMOVED******REMOVED*** heritageClauses || node.members !***REMOVED******REMOVED*** members ? update(createClassExpression(modifiers, name, typeParameters, heritageClauses, members), node) : node;
  }
  function createOmittedExpression() {
    return createBaseNode(233 /* OmittedExpression */);
  }
  function createExpressionWithTypeArguments(expression, typeArguments) {
    const node ***REMOVED*** createBaseNode(234 /* ExpressionWithTypeArguments */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
      expression,
      /*optionalChain*/
      false
    );
    node.typeArguments ***REMOVED*** typeArguments && parenthesizerRules().parenthesizeTypeArguments(typeArguments);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildrenFlags(node.typeArguments) | 1024 /* ContainsES2015 */;
    return node;
  }
  function updateExpressionWithTypeArguments(node, expression, typeArguments) {
    return node.expression !***REMOVED******REMOVED*** expression || node.typeArguments !***REMOVED******REMOVED*** typeArguments ? update(createExpressionWithTypeArguments(expression, typeArguments), node) : node;
  }
  function createAsExpression(expression, type) {
    const node ***REMOVED*** createBaseNode(235 /* AsExpression */);
    node.expression ***REMOVED*** expression;
    node.type ***REMOVED*** type;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.type) | 1 /* ContainsTypeScript */;
    return node;
  }
  function updateAsExpression(node, expression, type) {
    return node.expression !***REMOVED******REMOVED*** expression || node.type !***REMOVED******REMOVED*** type ? update(createAsExpression(expression, type), node) : node;
  }
  function createNonNullExpression(expression) {
    const node ***REMOVED*** createBaseNode(236 /* NonNullExpression */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
      expression,
      /*optionalChain*/
      false
    );
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1 /* ContainsTypeScript */;
    return node;
  }
  function updateNonNullExpression(node, expression) {
    if (isNonNullChain(node)) {
      return updateNonNullChain(node, expression);
    }
    return node.expression !***REMOVED******REMOVED*** expression ? update(createNonNullExpression(expression), node) : node;
  }
  function createSatisfiesExpression(expression, type) {
    const node ***REMOVED*** createBaseNode(239 /* SatisfiesExpression */);
    node.expression ***REMOVED*** expression;
    node.type ***REMOVED*** type;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.type) | 1 /* ContainsTypeScript */;
    return node;
  }
  function updateSatisfiesExpression(node, expression, type) {
    return node.expression !***REMOVED******REMOVED*** expression || node.type !***REMOVED******REMOVED*** type ? update(createSatisfiesExpression(expression, type), node) : node;
  }
  function createNonNullChain(expression) {
    const node ***REMOVED*** createBaseNode(236 /* NonNullExpression */);
    node.flags |***REMOVED*** 64 /* OptionalChain */;
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
      expression,
      /*optionalChain*/
      true
    );
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1 /* ContainsTypeScript */;
    return node;
  }
  function updateNonNullChain(node, expression) {
    Debug.assert(!!(node.flags & 64 /* OptionalChain */), "Cannot update a NonNullExpression using updateNonNullChain. Use updateNonNullExpression instead.");
    return node.expression !***REMOVED******REMOVED*** expression ? update(createNonNullChain(expression), node) : node;
  }
  function createMetaProperty(keywordToken, name) {
    const node ***REMOVED*** createBaseNode(237 /* MetaProperty */);
    node.keywordToken ***REMOVED*** keywordToken;
    node.name ***REMOVED*** name;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name);
    switch (keywordToken) {
      case 105 /* NewKeyword */:
        node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
        break;
      case 102 /* ImportKeyword */:
        node.transformFlags |***REMOVED*** 32 /* ContainsES2020 */;
        break;
      default:
        return Debug.assertNever(keywordToken);
    }
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateMetaProperty(node, name) {
    return node.name !***REMOVED******REMOVED*** name ? update(createMetaProperty(node.keywordToken, name), node) : node;
  }
  function createTemplateSpan(expression, literal) {
    const node ***REMOVED*** createBaseNode(240 /* TemplateSpan */);
    node.expression ***REMOVED*** expression;
    node.literal ***REMOVED*** literal;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.literal) | 1024 /* ContainsES2015 */;
    return node;
  }
  function updateTemplateSpan(node, expression, literal) {
    return node.expression !***REMOVED******REMOVED*** expression || node.literal !***REMOVED******REMOVED*** literal ? update(createTemplateSpan(expression, literal), node) : node;
  }
  function createSemicolonClassElement() {
    const node ***REMOVED*** createBaseNode(241 /* SemicolonClassElement */);
    node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
    return node;
  }
  function createBlock(statements, multiLine) {
    const node ***REMOVED*** createBaseNode(242 /* Block */);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.multiLine ***REMOVED*** multiLine;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.statements);
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateBlock(node, statements) {
    return node.statements !***REMOVED******REMOVED*** statements ? update(createBlock(statements, node.multiLine), node) : node;
  }
  function createVariableStatement(modifiers, declarationList) {
    const node ***REMOVED*** createBaseNode(244 /* VariableStatement */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.declarationList ***REMOVED*** isArray(declarationList) ? createVariableDeclarationList(declarationList) : declarationList;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.declarationList);
    if (modifiersToFlags(node.modifiers) & 128 /* Ambient */) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    }
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateVariableStatement(node, modifiers, declarationList) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.declarationList !***REMOVED******REMOVED*** declarationList ? update(createVariableStatement(modifiers, declarationList), node) : node;
  }
  function createEmptyStatement() {
    const node ***REMOVED*** createBaseNode(243 /* EmptyStatement */);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function createExpressionStatement(expression) {
    const node ***REMOVED*** createBaseNode(245 /* ExpressionStatement */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionOfExpressionStatement(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateExpressionStatement(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createExpressionStatement(expression), node) : node;
  }
  function createIfStatement(expression, thenStatement, elseStatement) {
    const node ***REMOVED*** createBaseNode(246 /* IfStatement */);
    node.expression ***REMOVED*** expression;
    node.thenStatement ***REMOVED*** asEmbeddedStatement(thenStatement);
    node.elseStatement ***REMOVED*** asEmbeddedStatement(elseStatement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.thenStatement) | propagateChildFlags(node.elseStatement);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateIfStatement(node, expression, thenStatement, elseStatement) {
    return node.expression !***REMOVED******REMOVED*** expression || node.thenStatement !***REMOVED******REMOVED*** thenStatement || node.elseStatement !***REMOVED******REMOVED*** elseStatement ? update(createIfStatement(expression, thenStatement, elseStatement), node) : node;
  }
  function createDoStatement(statement, expression) {
    const node ***REMOVED*** createBaseNode(247 /* DoStatement */);
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.statement) | propagateChildFlags(node.expression);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateDoStatement(node, statement, expression) {
    return node.statement !***REMOVED******REMOVED*** statement || node.expression !***REMOVED******REMOVED*** expression ? update(createDoStatement(statement, expression), node) : node;
  }
  function createWhileStatement(expression, statement) {
    const node ***REMOVED*** createBaseNode(248 /* WhileStatement */);
    node.expression ***REMOVED*** expression;
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.statement);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateWhileStatement(node, expression, statement) {
    return node.expression !***REMOVED******REMOVED*** expression || node.statement !***REMOVED******REMOVED*** statement ? update(createWhileStatement(expression, statement), node) : node;
  }
  function createForStatement(initializer, condition, incrementor, statement) {
    const node ***REMOVED*** createBaseNode(249 /* ForStatement */);
    node.initializer ***REMOVED*** initializer;
    node.condition ***REMOVED*** condition;
    node.incrementor ***REMOVED*** incrementor;
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.initializer) | propagateChildFlags(node.condition) | propagateChildFlags(node.incrementor) | propagateChildFlags(node.statement);
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateForStatement(node, initializer, condition, incrementor, statement) {
    return node.initializer !***REMOVED******REMOVED*** initializer || node.condition !***REMOVED******REMOVED*** condition || node.incrementor !***REMOVED******REMOVED*** incrementor || node.statement !***REMOVED******REMOVED*** statement ? update(createForStatement(initializer, condition, incrementor, statement), node) : node;
  }
  function createForInStatement(initializer, expression, statement) {
    const node ***REMOVED*** createBaseNode(250 /* ForInStatement */);
    node.initializer ***REMOVED*** initializer;
    node.expression ***REMOVED*** expression;
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.initializer) | propagateChildFlags(node.expression) | propagateChildFlags(node.statement);
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateForInStatement(node, initializer, expression, statement) {
    return node.initializer !***REMOVED******REMOVED*** initializer || node.expression !***REMOVED******REMOVED*** expression || node.statement !***REMOVED******REMOVED*** statement ? update(createForInStatement(initializer, expression, statement), node) : node;
  }
  function createForOfStatement(awaitModifier, initializer, expression, statement) {
    const node ***REMOVED*** createBaseNode(251 /* ForOfStatement */);
    node.awaitModifier ***REMOVED*** awaitModifier;
    node.initializer ***REMOVED*** initializer;
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.awaitModifier) | propagateChildFlags(node.initializer) | propagateChildFlags(node.expression) | propagateChildFlags(node.statement) | 1024 /* ContainsES2015 */;
    if (awaitModifier) node.transformFlags |***REMOVED*** 128 /* ContainsES2018 */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateForOfStatement(node, awaitModifier, initializer, expression, statement) {
    return node.awaitModifier !***REMOVED******REMOVED*** awaitModifier || node.initializer !***REMOVED******REMOVED*** initializer || node.expression !***REMOVED******REMOVED*** expression || node.statement !***REMOVED******REMOVED*** statement ? update(createForOfStatement(awaitModifier, initializer, expression, statement), node) : node;
  }
  function createContinueStatement(label) {
    const node ***REMOVED*** createBaseNode(252 /* ContinueStatement */);
    node.label ***REMOVED*** asName(label);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.label) | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateContinueStatement(node, label) {
    return node.label !***REMOVED******REMOVED*** label ? update(createContinueStatement(label), node) : node;
  }
  function createBreakStatement(label) {
    const node ***REMOVED*** createBaseNode(253 /* BreakStatement */);
    node.label ***REMOVED*** asName(label);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.label) | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateBreakStatement(node, label) {
    return node.label !***REMOVED******REMOVED*** label ? update(createBreakStatement(label), node) : node;
  }
  function createReturnStatement(expression) {
    const node ***REMOVED*** createBaseNode(254 /* ReturnStatement */);
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 128 /* ContainsES2018 */ | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateReturnStatement(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createReturnStatement(expression), node) : node;
  }
  function createWithStatement(expression, statement) {
    const node ***REMOVED*** createBaseNode(255 /* WithStatement */);
    node.expression ***REMOVED*** expression;
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.statement);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateWithStatement(node, expression, statement) {
    return node.expression !***REMOVED******REMOVED*** expression || node.statement !***REMOVED******REMOVED*** statement ? update(createWithStatement(expression, statement), node) : node;
  }
  function createSwitchStatement(expression, caseBlock) {
    const node ***REMOVED*** createBaseNode(256 /* SwitchStatement */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.caseBlock ***REMOVED*** caseBlock;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.caseBlock);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    node.possiblyExhaustive ***REMOVED*** false;
    return node;
  }
  function updateSwitchStatement(node, expression, caseBlock) {
    return node.expression !***REMOVED******REMOVED*** expression || node.caseBlock !***REMOVED******REMOVED*** caseBlock ? update(createSwitchStatement(expression, caseBlock), node) : node;
  }
  function createLabeledStatement(label, statement) {
    const node ***REMOVED*** createBaseNode(257 /* LabeledStatement */);
    node.label ***REMOVED*** asName(label);
    node.statement ***REMOVED*** asEmbeddedStatement(statement);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.label) | propagateChildFlags(node.statement);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateLabeledStatement(node, label, statement) {
    return node.label !***REMOVED******REMOVED*** label || node.statement !***REMOVED******REMOVED*** statement ? update(createLabeledStatement(label, statement), node) : node;
  }
  function createThrowStatement(expression) {
    const node ***REMOVED*** createBaseNode(258 /* ThrowStatement */);
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateThrowStatement(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createThrowStatement(expression), node) : node;
  }
  function createTryStatement(tryBlock, catchClause, finallyBlock) {
    const node ***REMOVED*** createBaseNode(259 /* TryStatement */);
    node.tryBlock ***REMOVED*** tryBlock;
    node.catchClause ***REMOVED*** catchClause;
    node.finallyBlock ***REMOVED*** finallyBlock;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.tryBlock) | propagateChildFlags(node.catchClause) | propagateChildFlags(node.finallyBlock);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function updateTryStatement(node, tryBlock, catchClause, finallyBlock) {
    return node.tryBlock !***REMOVED******REMOVED*** tryBlock || node.catchClause !***REMOVED******REMOVED*** catchClause || node.finallyBlock !***REMOVED******REMOVED*** finallyBlock ? update(createTryStatement(tryBlock, catchClause, finallyBlock), node) : node;
  }
  function createDebuggerStatement() {
    const node ***REMOVED*** createBaseNode(260 /* DebuggerStatement */);
    node.jsDoc ***REMOVED*** void 0;
    node.flowNode ***REMOVED*** void 0;
    return node;
  }
  function createVariableDeclaration(name, exclamationToken, type, initializer) {
    const node ***REMOVED*** createBaseDeclaration(261 /* VariableDeclaration */);
    node.name ***REMOVED*** asName(name);
    node.exclamationToken ***REMOVED*** exclamationToken;
    node.type ***REMOVED*** type;
    node.initializer ***REMOVED*** asInitializer(initializer);
    node.transformFlags |***REMOVED*** propagateNameFlags(node.name) | propagateChildFlags(node.initializer) | (node.exclamationToken ?? node.type ? 1 /* ContainsTypeScript */ : 0 /* None */);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateVariableDeclaration(node, name, exclamationToken, type, initializer) {
    return node.name !***REMOVED******REMOVED*** name || node.type !***REMOVED******REMOVED*** type || node.exclamationToken !***REMOVED******REMOVED*** exclamationToken || node.initializer !***REMOVED******REMOVED*** initializer ? update(createVariableDeclaration(name, exclamationToken, type, initializer), node) : node;
  }
  function createVariableDeclarationList(declarations, flags2 ***REMOVED*** 0 /* None */) {
    const node ***REMOVED*** createBaseNode(262 /* VariableDeclarationList */);
    node.flags |***REMOVED*** flags2 & 7 /* BlockScoped */;
    node.declarations ***REMOVED*** createNodeArray(declarations);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.declarations) | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    if (flags2 & 7 /* BlockScoped */) {
      node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */ | 262144 /* ContainsBlockScopedBinding */;
    }
    if (flags2 & 4 /* Using */) {
      node.transformFlags |***REMOVED*** 4 /* ContainsESNext */;
    }
    return node;
  }
  function updateVariableDeclarationList(node, declarations) {
    return node.declarations !***REMOVED******REMOVED*** declarations ? update(createVariableDeclarationList(declarations, node.flags), node) : node;
  }
  function createFunctionDeclaration(modifiers, asteriskToken, name, typeParameters, parameters, type, body) {
    const node ***REMOVED*** createBaseDeclaration(263 /* FunctionDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.asteriskToken ***REMOVED*** asteriskToken;
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.body ***REMOVED*** body;
    if (!node.body || modifiersToFlags(node.modifiers) & 128 /* Ambient */) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      const isAsync ***REMOVED*** modifiersToFlags(node.modifiers) & 1024 /* Async */;
      const isGenerator ***REMOVED*** !!node.asteriskToken;
      const isAsyncGenerator ***REMOVED*** isAsync && isGenerator;
      node.transformFlags ***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.asteriskToken) | propagateNameFlags(node.name) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.parameters) | propagateChildFlags(node.type) | propagateChildFlags(node.body) & ~67108864 /* ContainsPossibleTopLevelAwait */ | (isAsyncGenerator ? 128 /* ContainsES2018 */ : isAsync ? 256 /* ContainsES2017 */ : isGenerator ? 2048 /* ContainsGenerator */ : 0 /* None */) | (node.typeParameters || node.type ? 1 /* ContainsTypeScript */ : 0 /* None */) | 4194304 /* ContainsHoistedDeclarationOrCompletion */;
    }
    node.typeArguments ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.returnFlowNode ***REMOVED*** void 0;
    return node;
  }
  function updateFunctionDeclaration(node, modifiers, asteriskToken, name, typeParameters, parameters, type, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.asteriskToken !***REMOVED******REMOVED*** asteriskToken || node.name !***REMOVED******REMOVED*** name || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type || node.body !***REMOVED******REMOVED*** body ? finishUpdateFunctionDeclaration(createFunctionDeclaration(modifiers, asteriskToken, name, typeParameters, parameters, type, body), node) : node;
  }
  function finishUpdateFunctionDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      if (updated.modifiers ***REMOVED******REMOVED******REMOVED*** original.modifiers) {
        updated.modifiers ***REMOVED*** original.modifiers;
      }
    }
    return finishUpdateBaseSignatureDeclaration(updated, original);
  }
  function createClassDeclaration(modifiers, name, typeParameters, heritageClauses, members) {
    const node ***REMOVED*** createBaseDeclaration(264 /* ClassDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.heritageClauses ***REMOVED*** asNodeArray(heritageClauses);
    node.members ***REMOVED*** createNodeArray(members);
    if (modifiersToFlags(node.modifiers) & 128 /* Ambient */) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateNameFlags(node.name) | propagateChildrenFlags(node.typeParameters) | propagateChildrenFlags(node.heritageClauses) | propagateChildrenFlags(node.members) | (node.typeParameters ? 1 /* ContainsTypeScript */ : 0 /* None */) | 1024 /* ContainsES2015 */;
      if (node.transformFlags & 8192 /* ContainsTypeScriptClassSyntax */) {
        node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
      }
    }
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateClassDeclaration(node, modifiers, name, typeParameters, heritageClauses, members) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.heritageClauses !***REMOVED******REMOVED*** heritageClauses || node.members !***REMOVED******REMOVED*** members ? update(createClassDeclaration(modifiers, name, typeParameters, heritageClauses, members), node) : node;
  }
  function createInterfaceDeclaration(modifiers, name, typeParameters, heritageClauses, members) {
    const node ***REMOVED*** createBaseDeclaration(265 /* InterfaceDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.heritageClauses ***REMOVED*** asNodeArray(heritageClauses);
    node.members ***REMOVED*** createNodeArray(members);
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateInterfaceDeclaration(node, modifiers, name, typeParameters, heritageClauses, members) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.heritageClauses !***REMOVED******REMOVED*** heritageClauses || node.members !***REMOVED******REMOVED*** members ? update(createInterfaceDeclaration(modifiers, name, typeParameters, heritageClauses, members), node) : node;
  }
  function createTypeAliasDeclaration(modifiers, name, typeParameters, type) {
    const node ***REMOVED*** createBaseDeclaration(266 /* TypeAliasDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateTypeAliasDeclaration(node, modifiers, name, typeParameters, type) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.type !***REMOVED******REMOVED*** type ? update(createTypeAliasDeclaration(modifiers, name, typeParameters, type), node) : node;
  }
  function createEnumDeclaration(modifiers, name, members) {
    const node ***REMOVED*** createBaseDeclaration(267 /* EnumDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.members ***REMOVED*** createNodeArray(members);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.name) | propagateChildrenFlags(node.members) | 1 /* ContainsTypeScript */;
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateEnumDeclaration(node, modifiers, name, members) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.members !***REMOVED******REMOVED*** members ? update(createEnumDeclaration(modifiers, name, members), node) : node;
  }
  function createModuleDeclaration(modifiers, name, body, flags2 ***REMOVED*** 0 /* None */) {
    const node ***REMOVED*** createBaseDeclaration(268 /* ModuleDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.flags |***REMOVED*** flags2 & (32 /* Namespace */ | 8 /* NestedNamespace */ | 2048 /* GlobalAugmentation */);
    node.name ***REMOVED*** name;
    node.body ***REMOVED*** body;
    if (modifiersToFlags(node.modifiers) & 128 /* Ambient */) {
      node.transformFlags ***REMOVED*** 1 /* ContainsTypeScript */;
    } else {
      node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.name) | propagateChildFlags(node.body) | 1 /* ContainsTypeScript */;
    }
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateModuleDeclaration(node, modifiers, name, body) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.name !***REMOVED******REMOVED*** name || node.body !***REMOVED******REMOVED*** body ? update(createModuleDeclaration(modifiers, name, body, node.flags), node) : node;
  }
  function createModuleBlock(statements) {
    const node ***REMOVED*** createBaseNode(269 /* ModuleBlock */);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.statements);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateModuleBlock(node, statements) {
    return node.statements !***REMOVED******REMOVED*** statements ? update(createModuleBlock(statements), node) : node;
  }
  function createCaseBlock(clauses) {
    const node ***REMOVED*** createBaseNode(270 /* CaseBlock */);
    node.clauses ***REMOVED*** createNodeArray(clauses);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.clauses);
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateCaseBlock(node, clauses) {
    return node.clauses !***REMOVED******REMOVED*** clauses ? update(createCaseBlock(clauses), node) : node;
  }
  function createNamespaceExportDeclaration(name) {
    const node ***REMOVED*** createBaseDeclaration(271 /* NamespaceExportDeclaration */);
    node.name ***REMOVED*** asName(name);
    node.transformFlags |***REMOVED*** propagateIdentifierNameFlags(node.name) | 1 /* ContainsTypeScript */;
    node.modifiers ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateNamespaceExportDeclaration(node, name) {
    return node.name !***REMOVED******REMOVED*** name ? finishUpdateNamespaceExportDeclaration(createNamespaceExportDeclaration(name), node) : node;
  }
  function finishUpdateNamespaceExportDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.modifiers ***REMOVED*** original.modifiers;
    }
    return update(updated, original);
  }
  function createImportEqualsDeclaration(modifiers, isTypeOnly, name, moduleReference) {
    const node ***REMOVED*** createBaseDeclaration(272 /* ImportEqualsDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.name ***REMOVED*** asName(name);
    node.isTypeOnly ***REMOVED*** isTypeOnly;
    node.moduleReference ***REMOVED*** moduleReference;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateIdentifierNameFlags(node.name) | propagateChildFlags(node.moduleReference);
    if (!isExternalModuleReference(node.moduleReference)) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateImportEqualsDeclaration(node, modifiers, isTypeOnly, name, moduleReference) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.isTypeOnly !***REMOVED******REMOVED*** isTypeOnly || node.name !***REMOVED******REMOVED*** name || node.moduleReference !***REMOVED******REMOVED*** moduleReference ? update(createImportEqualsDeclaration(modifiers, isTypeOnly, name, moduleReference), node) : node;
  }
  function createImportDeclaration(modifiers, importClause, moduleSpecifier, attributes) {
    const node ***REMOVED*** createBaseNode(273 /* ImportDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.importClause ***REMOVED*** importClause;
    node.moduleSpecifier ***REMOVED*** moduleSpecifier;
    node.attributes ***REMOVED*** node.assertClause ***REMOVED*** attributes;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.importClause) | propagateChildFlags(node.moduleSpecifier);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateImportDeclaration(node, modifiers, importClause, moduleSpecifier, attributes) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.importClause !***REMOVED******REMOVED*** importClause || node.moduleSpecifier !***REMOVED******REMOVED*** moduleSpecifier || node.attributes !***REMOVED******REMOVED*** attributes ? update(createImportDeclaration(modifiers, importClause, moduleSpecifier, attributes), node) : node;
  }
  function createImportClause(phaseModifier, name, namedBindings) {
    const node ***REMOVED*** createBaseDeclaration(274 /* ImportClause */);
    if (typeof phaseModifier ***REMOVED******REMOVED******REMOVED*** "boolean") {
      phaseModifier ***REMOVED*** phaseModifier ? 156 /* TypeKeyword */ : void 0;
    }
    node.isTypeOnly ***REMOVED*** phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */;
    node.phaseModifier ***REMOVED*** phaseModifier;
    node.name ***REMOVED*** name;
    node.namedBindings ***REMOVED*** namedBindings;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name) | propagateChildFlags(node.namedBindings);
    if (phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateImportClause(node, phaseModifier, name, namedBindings) {
    if (typeof phaseModifier ***REMOVED******REMOVED******REMOVED*** "boolean") {
      phaseModifier ***REMOVED*** phaseModifier ? 156 /* TypeKeyword */ : void 0;
    }
    return node.phaseModifier !***REMOVED******REMOVED*** phaseModifier || node.name !***REMOVED******REMOVED*** name || node.namedBindings !***REMOVED******REMOVED*** namedBindings ? update(createImportClause(phaseModifier, name, namedBindings), node) : node;
  }
  function createAssertClause(elements, multiLine) {
    const node ***REMOVED*** createBaseNode(301 /* AssertClause */);
    node.elements ***REMOVED*** createNodeArray(elements);
    node.multiLine ***REMOVED*** multiLine;
    node.token ***REMOVED*** 132 /* AssertKeyword */;
    node.transformFlags |***REMOVED*** 4 /* ContainsESNext */;
    return node;
  }
  function updateAssertClause(node, elements, multiLine) {
    return node.elements !***REMOVED******REMOVED*** elements || node.multiLine !***REMOVED******REMOVED*** multiLine ? update(createAssertClause(elements, multiLine), node) : node;
  }
  function createAssertEntry(name, value) {
    const node ***REMOVED*** createBaseNode(302 /* AssertEntry */);
    node.name ***REMOVED*** name;
    node.value ***REMOVED*** value;
    node.transformFlags |***REMOVED*** 4 /* ContainsESNext */;
    return node;
  }
  function updateAssertEntry(node, name, value) {
    return node.name !***REMOVED******REMOVED*** name || node.value !***REMOVED******REMOVED*** value ? update(createAssertEntry(name, value), node) : node;
  }
  function createImportTypeAssertionContainer(clause, multiLine) {
    const node ***REMOVED*** createBaseNode(303 /* ImportTypeAssertionContainer */);
    node.assertClause ***REMOVED*** clause;
    node.multiLine ***REMOVED*** multiLine;
    return node;
  }
  function updateImportTypeAssertionContainer(node, clause, multiLine) {
    return node.assertClause !***REMOVED******REMOVED*** clause || node.multiLine !***REMOVED******REMOVED*** multiLine ? update(createImportTypeAssertionContainer(clause, multiLine), node) : node;
  }
  function createImportAttributes(elements, multiLine, token) {
    const node ***REMOVED*** createBaseNode(301 /* ImportAttributes */);
    node.token ***REMOVED*** token ?? 118 /* WithKeyword */;
    node.elements ***REMOVED*** createNodeArray(elements);
    node.multiLine ***REMOVED*** multiLine;
    node.transformFlags |***REMOVED*** 4 /* ContainsESNext */;
    return node;
  }
  function updateImportAttributes(node, elements, multiLine) {
    return node.elements !***REMOVED******REMOVED*** elements || node.multiLine !***REMOVED******REMOVED*** multiLine ? update(createImportAttributes(elements, multiLine, node.token), node) : node;
  }
  function createImportAttribute(name, value) {
    const node ***REMOVED*** createBaseNode(302 /* ImportAttribute */);
    node.name ***REMOVED*** name;
    node.value ***REMOVED*** value;
    node.transformFlags |***REMOVED*** 4 /* ContainsESNext */;
    return node;
  }
  function updateImportAttribute(node, name, value) {
    return node.name !***REMOVED******REMOVED*** name || node.value !***REMOVED******REMOVED*** value ? update(createImportAttribute(name, value), node) : node;
  }
  function createNamespaceImport(name) {
    const node ***REMOVED*** createBaseDeclaration(275 /* NamespaceImport */);
    node.name ***REMOVED*** name;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateNamespaceImport(node, name) {
    return node.name !***REMOVED******REMOVED*** name ? update(createNamespaceImport(name), node) : node;
  }
  function createNamespaceExport(name) {
    const node ***REMOVED*** createBaseDeclaration(281 /* NamespaceExport */);
    node.name ***REMOVED*** name;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name) | 32 /* ContainsES2020 */;
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateNamespaceExport(node, name) {
    return node.name !***REMOVED******REMOVED*** name ? update(createNamespaceExport(name), node) : node;
  }
  function createNamedImports(elements) {
    const node ***REMOVED*** createBaseNode(276 /* NamedImports */);
    node.elements ***REMOVED*** createNodeArray(elements);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateNamedImports(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createNamedImports(elements), node) : node;
  }
  function createImportSpecifier(isTypeOnly, propertyName, name) {
    const node ***REMOVED*** createBaseDeclaration(277 /* ImportSpecifier */);
    node.isTypeOnly ***REMOVED*** isTypeOnly;
    node.propertyName ***REMOVED*** propertyName;
    node.name ***REMOVED*** name;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.propertyName) | propagateChildFlags(node.name);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateImportSpecifier(node, isTypeOnly, propertyName, name) {
    return node.isTypeOnly !***REMOVED******REMOVED*** isTypeOnly || node.propertyName !***REMOVED******REMOVED*** propertyName || node.name !***REMOVED******REMOVED*** name ? update(createImportSpecifier(isTypeOnly, propertyName, name), node) : node;
  }
  function createExportAssignment(modifiers, isExportEquals, expression) {
    const node ***REMOVED*** createBaseDeclaration(278 /* ExportAssignment */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.isExportEquals ***REMOVED*** isExportEquals;
    node.expression ***REMOVED*** isExportEquals ? parenthesizerRules().parenthesizeRightSideOfBinary(
      64 /* EqualsToken */,
      /*leftSide*/
      void 0,
      expression
    ) : parenthesizerRules().parenthesizeExpressionOfExportDefault(expression);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.expression);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateExportAssignment(node, modifiers, expression) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.expression !***REMOVED******REMOVED*** expression ? update(createExportAssignment(modifiers, node.isExportEquals, expression), node) : node;
  }
  function createExportDeclaration(modifiers, isTypeOnly, exportClause, moduleSpecifier, attributes) {
    const node ***REMOVED*** createBaseDeclaration(279 /* ExportDeclaration */);
    node.modifiers ***REMOVED*** asNodeArray(modifiers);
    node.isTypeOnly ***REMOVED*** isTypeOnly;
    node.exportClause ***REMOVED*** exportClause;
    node.moduleSpecifier ***REMOVED*** moduleSpecifier;
    node.attributes ***REMOVED*** node.assertClause ***REMOVED*** attributes;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.modifiers) | propagateChildFlags(node.exportClause) | propagateChildFlags(node.moduleSpecifier);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateExportDeclaration(node, modifiers, isTypeOnly, exportClause, moduleSpecifier, attributes) {
    return node.modifiers !***REMOVED******REMOVED*** modifiers || node.isTypeOnly !***REMOVED******REMOVED*** isTypeOnly || node.exportClause !***REMOVED******REMOVED*** exportClause || node.moduleSpecifier !***REMOVED******REMOVED*** moduleSpecifier || node.attributes !***REMOVED******REMOVED*** attributes ? finishUpdateExportDeclaration(createExportDeclaration(modifiers, isTypeOnly, exportClause, moduleSpecifier, attributes), node) : node;
  }
  function finishUpdateExportDeclaration(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      if (updated.modifiers ***REMOVED******REMOVED******REMOVED*** original.modifiers) {
        updated.modifiers ***REMOVED*** original.modifiers;
      }
    }
    return update(updated, original);
  }
  function createNamedExports(elements) {
    const node ***REMOVED*** createBaseNode(280 /* NamedExports */);
    node.elements ***REMOVED*** createNodeArray(elements);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateNamedExports(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createNamedExports(elements), node) : node;
  }
  function createExportSpecifier(isTypeOnly, propertyName, name) {
    const node ***REMOVED*** createBaseNode(282 /* ExportSpecifier */);
    node.isTypeOnly ***REMOVED*** isTypeOnly;
    node.propertyName ***REMOVED*** asName(propertyName);
    node.name ***REMOVED*** asName(name);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.propertyName) | propagateChildFlags(node.name);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateExportSpecifier(node, isTypeOnly, propertyName, name) {
    return node.isTypeOnly !***REMOVED******REMOVED*** isTypeOnly || node.propertyName !***REMOVED******REMOVED*** propertyName || node.name !***REMOVED******REMOVED*** name ? update(createExportSpecifier(isTypeOnly, propertyName, name), node) : node;
  }
  function createMissingDeclaration() {
    const node ***REMOVED*** createBaseDeclaration(283 /* MissingDeclaration */);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function createExternalModuleReference(expression) {
    const node ***REMOVED*** createBaseNode(284 /* ExternalModuleReference */);
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression);
    node.transformFlags &***REMOVED*** ~67108864 /* ContainsPossibleTopLevelAwait */;
    return node;
  }
  function updateExternalModuleReference(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createExternalModuleReference(expression), node) : node;
  }
  function createJSDocPrimaryTypeWorker(kind) {
    return createBaseNode(kind);
  }
  function createJSDocPrePostfixUnaryTypeWorker(kind, type, postfix ***REMOVED*** false) {
    const node ***REMOVED*** createJSDocUnaryTypeWorker(
      kind,
      postfix ? type && parenthesizerRules().parenthesizeNonArrayTypeOfPostfixType(type) : type
    );
    node.postfix ***REMOVED*** postfix;
    return node;
  }
  function createJSDocUnaryTypeWorker(kind, type) {
    const node ***REMOVED*** createBaseNode(kind);
    node.type ***REMOVED*** type;
    return node;
  }
  function updateJSDocPrePostfixUnaryTypeWorker(kind, node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createJSDocPrePostfixUnaryTypeWorker(kind, type, node.postfix), node) : node;
  }
  function updateJSDocUnaryTypeWorker(kind, node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createJSDocUnaryTypeWorker(kind, type), node) : node;
  }
  function createJSDocFunctionType(parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(318 /* JSDocFunctionType */);
    node.parameters ***REMOVED*** asNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.parameters) | (node.type ? 1 /* ContainsTypeScript */ : 0 /* None */);
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.typeArguments ***REMOVED*** void 0;
    return node;
  }
  function updateJSDocFunctionType(node, parameters, type) {
    return node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? update(createJSDocFunctionType(parameters, type), node) : node;
  }
  function createJSDocTypeLiteral(propertyTags, isArrayType ***REMOVED*** false) {
    const node ***REMOVED*** createBaseDeclaration(323 /* JSDocTypeLiteral */);
    node.jsDocPropertyTags ***REMOVED*** asNodeArray(propertyTags);
    node.isArrayType ***REMOVED*** isArrayType;
    return node;
  }
  function updateJSDocTypeLiteral(node, propertyTags, isArrayType) {
    return node.jsDocPropertyTags !***REMOVED******REMOVED*** propertyTags || node.isArrayType !***REMOVED******REMOVED*** isArrayType ? update(createJSDocTypeLiteral(propertyTags, isArrayType), node) : node;
  }
  function createJSDocTypeExpression(type) {
    const node ***REMOVED*** createBaseNode(310 /* JSDocTypeExpression */);
    node.type ***REMOVED*** type;
    return node;
  }
  function updateJSDocTypeExpression(node, type) {
    return node.type !***REMOVED******REMOVED*** type ? update(createJSDocTypeExpression(type), node) : node;
  }
  function createJSDocSignature(typeParameters, parameters, type) {
    const node ***REMOVED*** createBaseDeclaration(324 /* JSDocSignature */);
    node.typeParameters ***REMOVED*** asNodeArray(typeParameters);
    node.parameters ***REMOVED*** createNodeArray(parameters);
    node.type ***REMOVED*** type;
    node.jsDoc ***REMOVED*** void 0;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateJSDocSignature(node, typeParameters, parameters, type) {
    return node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.parameters !***REMOVED******REMOVED*** parameters || node.type !***REMOVED******REMOVED*** type ? update(createJSDocSignature(typeParameters, parameters, type), node) : node;
  }
  function getDefaultTagName(node) {
    const defaultTagName ***REMOVED*** getDefaultTagNameForKind(node.kind);
    return node.tagName.escapedText ***REMOVED******REMOVED******REMOVED*** escapeLeadingUnderscores(defaultTagName) ? node.tagName : createIdentifier(defaultTagName);
  }
  function createBaseJSDocTag(kind, tagName, comment) {
    const node ***REMOVED*** createBaseNode(kind);
    node.tagName ***REMOVED*** tagName;
    node.comment ***REMOVED*** comment;
    return node;
  }
  function createBaseJSDocTagDeclaration(kind, tagName, comment) {
    const node ***REMOVED*** createBaseDeclaration(kind);
    node.tagName ***REMOVED*** tagName;
    node.comment ***REMOVED*** comment;
    return node;
  }
  function createJSDocTemplateTag(tagName, constraint, typeParameters, comment) {
    const node ***REMOVED*** createBaseJSDocTag(346 /* JSDocTemplateTag */, tagName ?? createIdentifier("template"), comment);
    node.constraint ***REMOVED*** constraint;
    node.typeParameters ***REMOVED*** createNodeArray(typeParameters);
    return node;
  }
  function updateJSDocTemplateTag(node, tagName ***REMOVED*** getDefaultTagName(node), constraint, typeParameters, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.constraint !***REMOVED******REMOVED*** constraint || node.typeParameters !***REMOVED******REMOVED*** typeParameters || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocTemplateTag(tagName, constraint, typeParameters, comment), node) : node;
  }
  function createJSDocTypedefTag(tagName, typeExpression, fullName, comment) {
    const node ***REMOVED*** createBaseJSDocTagDeclaration(347 /* JSDocTypedefTag */, tagName ?? createIdentifier("typedef"), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    node.fullName ***REMOVED*** fullName;
    node.name ***REMOVED*** getJSDocTypeAliasName(fullName);
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateJSDocTypedefTag(node, tagName ***REMOVED*** getDefaultTagName(node), typeExpression, fullName, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.fullName !***REMOVED******REMOVED*** fullName || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocTypedefTag(tagName, typeExpression, fullName, comment), node) : node;
  }
  function createJSDocParameterTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment) {
    const node ***REMOVED*** createBaseJSDocTagDeclaration(342 /* JSDocParameterTag */, tagName ?? createIdentifier("param"), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    node.name ***REMOVED*** name;
    node.isNameFirst ***REMOVED*** !!isNameFirst;
    node.isBracketed ***REMOVED*** isBracketed;
    return node;
  }
  function updateJSDocParameterTag(node, tagName ***REMOVED*** getDefaultTagName(node), name, isBracketed, typeExpression, isNameFirst, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.name !***REMOVED******REMOVED*** name || node.isBracketed !***REMOVED******REMOVED*** isBracketed || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.isNameFirst !***REMOVED******REMOVED*** isNameFirst || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocParameterTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment), node) : node;
  }
  function createJSDocPropertyTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment) {
    const node ***REMOVED*** createBaseJSDocTagDeclaration(349 /* JSDocPropertyTag */, tagName ?? createIdentifier("prop"), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    node.name ***REMOVED*** name;
    node.isNameFirst ***REMOVED*** !!isNameFirst;
    node.isBracketed ***REMOVED*** isBracketed;
    return node;
  }
  function updateJSDocPropertyTag(node, tagName ***REMOVED*** getDefaultTagName(node), name, isBracketed, typeExpression, isNameFirst, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.name !***REMOVED******REMOVED*** name || node.isBracketed !***REMOVED******REMOVED*** isBracketed || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.isNameFirst !***REMOVED******REMOVED*** isNameFirst || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocPropertyTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment), node) : node;
  }
  function createJSDocCallbackTag(tagName, typeExpression, fullName, comment) {
    const node ***REMOVED*** createBaseJSDocTagDeclaration(339 /* JSDocCallbackTag */, tagName ?? createIdentifier("callback"), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    node.fullName ***REMOVED*** fullName;
    node.name ***REMOVED*** getJSDocTypeAliasName(fullName);
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateJSDocCallbackTag(node, tagName ***REMOVED*** getDefaultTagName(node), typeExpression, fullName, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.fullName !***REMOVED******REMOVED*** fullName || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocCallbackTag(tagName, typeExpression, fullName, comment), node) : node;
  }
  function createJSDocOverloadTag(tagName, typeExpression, comment) {
    const node ***REMOVED*** createBaseJSDocTag(340 /* JSDocOverloadTag */, tagName ?? createIdentifier("overload"), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    return node;
  }
  function updateJSDocOverloadTag(node, tagName ***REMOVED*** getDefaultTagName(node), typeExpression, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocOverloadTag(tagName, typeExpression, comment), node) : node;
  }
  function createJSDocAugmentsTag(tagName, className, comment) {
    const node ***REMOVED*** createBaseJSDocTag(329 /* JSDocAugmentsTag */, tagName ?? createIdentifier("augments"), comment);
    node.class ***REMOVED*** className;
    return node;
  }
  function updateJSDocAugmentsTag(node, tagName ***REMOVED*** getDefaultTagName(node), className, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.class !***REMOVED******REMOVED*** className || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocAugmentsTag(tagName, className, comment), node) : node;
  }
  function createJSDocImplementsTag(tagName, className, comment) {
    const node ***REMOVED*** createBaseJSDocTag(330 /* JSDocImplementsTag */, tagName ?? createIdentifier("implements"), comment);
    node.class ***REMOVED*** className;
    return node;
  }
  function createJSDocSeeTag(tagName, name, comment) {
    const node ***REMOVED*** createBaseJSDocTag(348 /* JSDocSeeTag */, tagName ?? createIdentifier("see"), comment);
    node.name ***REMOVED*** name;
    return node;
  }
  function updateJSDocSeeTag(node, tagName, name, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.name !***REMOVED******REMOVED*** name || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocSeeTag(tagName, name, comment), node) : node;
  }
  function createJSDocNameReference(name) {
    const node ***REMOVED*** createBaseNode(311 /* JSDocNameReference */);
    node.name ***REMOVED*** name;
    return node;
  }
  function updateJSDocNameReference(node, name) {
    return node.name !***REMOVED******REMOVED*** name ? update(createJSDocNameReference(name), node) : node;
  }
  function createJSDocMemberName(left, right) {
    const node ***REMOVED*** createBaseNode(312 /* JSDocMemberName */);
    node.left ***REMOVED*** left;
    node.right ***REMOVED*** right;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.left) | propagateChildFlags(node.right);
    return node;
  }
  function updateJSDocMemberName(node, left, right) {
    return node.left !***REMOVED******REMOVED*** left || node.right !***REMOVED******REMOVED*** right ? update(createJSDocMemberName(left, right), node) : node;
  }
  function createJSDocLink(name, text) {
    const node ***REMOVED*** createBaseNode(325 /* JSDocLink */);
    node.name ***REMOVED*** name;
    node.text ***REMOVED*** text;
    return node;
  }
  function updateJSDocLink(node, name, text) {
    return node.name !***REMOVED******REMOVED*** name ? update(createJSDocLink(name, text), node) : node;
  }
  function createJSDocLinkCode(name, text) {
    const node ***REMOVED*** createBaseNode(326 /* JSDocLinkCode */);
    node.name ***REMOVED*** name;
    node.text ***REMOVED*** text;
    return node;
  }
  function updateJSDocLinkCode(node, name, text) {
    return node.name !***REMOVED******REMOVED*** name ? update(createJSDocLinkCode(name, text), node) : node;
  }
  function createJSDocLinkPlain(name, text) {
    const node ***REMOVED*** createBaseNode(327 /* JSDocLinkPlain */);
    node.name ***REMOVED*** name;
    node.text ***REMOVED*** text;
    return node;
  }
  function updateJSDocLinkPlain(node, name, text) {
    return node.name !***REMOVED******REMOVED*** name ? update(createJSDocLinkPlain(name, text), node) : node;
  }
  function updateJSDocImplementsTag(node, tagName ***REMOVED*** getDefaultTagName(node), className, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.class !***REMOVED******REMOVED*** className || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocImplementsTag(tagName, className, comment), node) : node;
  }
  function createJSDocSimpleTagWorker(kind, tagName, comment) {
    const node ***REMOVED*** createBaseJSDocTag(kind, tagName ?? createIdentifier(getDefaultTagNameForKind(kind)), comment);
    return node;
  }
  function updateJSDocSimpleTagWorker(kind, node, tagName ***REMOVED*** getDefaultTagName(node), comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocSimpleTagWorker(kind, tagName, comment), node) : node;
  }
  function createJSDocTypeLikeTagWorker(kind, tagName, typeExpression, comment) {
    const node ***REMOVED*** createBaseJSDocTag(kind, tagName ?? createIdentifier(getDefaultTagNameForKind(kind)), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    return node;
  }
  function updateJSDocTypeLikeTagWorker(kind, node, tagName ***REMOVED*** getDefaultTagName(node), typeExpression, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocTypeLikeTagWorker(kind, tagName, typeExpression, comment), node) : node;
  }
  function createJSDocUnknownTag(tagName, comment) {
    const node ***REMOVED*** createBaseJSDocTag(328 /* JSDocTag */, tagName, comment);
    return node;
  }
  function updateJSDocUnknownTag(node, tagName, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocUnknownTag(tagName, comment), node) : node;
  }
  function createJSDocEnumTag(tagName, typeExpression, comment) {
    const node ***REMOVED*** createBaseJSDocTagDeclaration(341 /* JSDocEnumTag */, tagName ?? createIdentifier(getDefaultTagNameForKind(341 /* JSDocEnumTag */)), comment);
    node.typeExpression ***REMOVED*** typeExpression;
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateJSDocEnumTag(node, tagName ***REMOVED*** getDefaultTagName(node), typeExpression, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeExpression !***REMOVED******REMOVED*** typeExpression || node.comment !***REMOVED******REMOVED*** comment ? update(createJSDocEnumTag(tagName, typeExpression, comment), node) : node;
  }
  function createJSDocImportTag(tagName, importClause, moduleSpecifier, attributes, comment) {
    const node ***REMOVED*** createBaseJSDocTag(352 /* JSDocImportTag */, tagName ?? createIdentifier("import"), comment);
    node.importClause ***REMOVED*** importClause;
    node.moduleSpecifier ***REMOVED*** moduleSpecifier;
    node.attributes ***REMOVED*** attributes;
    node.comment ***REMOVED*** comment;
    return node;
  }
  function updateJSDocImportTag(node, tagName, importClause, moduleSpecifier, attributes, comment) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.comment !***REMOVED******REMOVED*** comment || node.importClause !***REMOVED******REMOVED*** importClause || node.moduleSpecifier !***REMOVED******REMOVED*** moduleSpecifier || node.attributes !***REMOVED******REMOVED*** attributes ? update(createJSDocImportTag(tagName, importClause, moduleSpecifier, attributes, comment), node) : node;
  }
  function createJSDocText(text) {
    const node ***REMOVED*** createBaseNode(322 /* JSDocText */);
    node.text ***REMOVED*** text;
    return node;
  }
  function updateJSDocText(node, text) {
    return node.text !***REMOVED******REMOVED*** text ? update(createJSDocText(text), node) : node;
  }
  function createJSDocComment(comment, tags) {
    const node ***REMOVED*** createBaseNode(321 /* JSDoc */);
    node.comment ***REMOVED*** comment;
    node.tags ***REMOVED*** asNodeArray(tags);
    return node;
  }
  function updateJSDocComment(node, comment, tags) {
    return node.comment !***REMOVED******REMOVED*** comment || node.tags !***REMOVED******REMOVED*** tags ? update(createJSDocComment(comment, tags), node) : node;
  }
  function createJsxElement(openingElement, children, closingElement) {
    const node ***REMOVED*** createBaseNode(285 /* JsxElement */);
    node.openingElement ***REMOVED*** openingElement;
    node.children ***REMOVED*** createNodeArray(children);
    node.closingElement ***REMOVED*** closingElement;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.openingElement) | propagateChildrenFlags(node.children) | propagateChildFlags(node.closingElement) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxElement(node, openingElement, children, closingElement) {
    return node.openingElement !***REMOVED******REMOVED*** openingElement || node.children !***REMOVED******REMOVED*** children || node.closingElement !***REMOVED******REMOVED*** closingElement ? update(createJsxElement(openingElement, children, closingElement), node) : node;
  }
  function createJsxSelfClosingElement(tagName, typeArguments, attributes) {
    const node ***REMOVED*** createBaseNode(286 /* JsxSelfClosingElement */);
    node.tagName ***REMOVED*** tagName;
    node.typeArguments ***REMOVED*** asNodeArray(typeArguments);
    node.attributes ***REMOVED*** attributes;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.tagName) | propagateChildrenFlags(node.typeArguments) | propagateChildFlags(node.attributes) | 2 /* ContainsJsx */;
    if (node.typeArguments) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    return node;
  }
  function updateJsxSelfClosingElement(node, tagName, typeArguments, attributes) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.attributes !***REMOVED******REMOVED*** attributes ? update(createJsxSelfClosingElement(tagName, typeArguments, attributes), node) : node;
  }
  function createJsxOpeningElement(tagName, typeArguments, attributes) {
    const node ***REMOVED*** createBaseNode(287 /* JsxOpeningElement */);
    node.tagName ***REMOVED*** tagName;
    node.typeArguments ***REMOVED*** asNodeArray(typeArguments);
    node.attributes ***REMOVED*** attributes;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.tagName) | propagateChildrenFlags(node.typeArguments) | propagateChildFlags(node.attributes) | 2 /* ContainsJsx */;
    if (typeArguments) {
      node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
    }
    return node;
  }
  function updateJsxOpeningElement(node, tagName, typeArguments, attributes) {
    return node.tagName !***REMOVED******REMOVED*** tagName || node.typeArguments !***REMOVED******REMOVED*** typeArguments || node.attributes !***REMOVED******REMOVED*** attributes ? update(createJsxOpeningElement(tagName, typeArguments, attributes), node) : node;
  }
  function createJsxClosingElement(tagName) {
    const node ***REMOVED*** createBaseNode(288 /* JsxClosingElement */);
    node.tagName ***REMOVED*** tagName;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.tagName) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxClosingElement(node, tagName) {
    return node.tagName !***REMOVED******REMOVED*** tagName ? update(createJsxClosingElement(tagName), node) : node;
  }
  function createJsxFragment(openingFragment, children, closingFragment) {
    const node ***REMOVED*** createBaseNode(289 /* JsxFragment */);
    node.openingFragment ***REMOVED*** openingFragment;
    node.children ***REMOVED*** createNodeArray(children);
    node.closingFragment ***REMOVED*** closingFragment;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.openingFragment) | propagateChildrenFlags(node.children) | propagateChildFlags(node.closingFragment) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxFragment(node, openingFragment, children, closingFragment) {
    return node.openingFragment !***REMOVED******REMOVED*** openingFragment || node.children !***REMOVED******REMOVED*** children || node.closingFragment !***REMOVED******REMOVED*** closingFragment ? update(createJsxFragment(openingFragment, children, closingFragment), node) : node;
  }
  function createJsxText(text, containsOnlyTriviaWhiteSpaces) {
    const node ***REMOVED*** createBaseNode(12 /* JsxText */);
    node.text ***REMOVED*** text;
    node.containsOnlyTriviaWhiteSpaces ***REMOVED*** !!containsOnlyTriviaWhiteSpaces;
    node.transformFlags |***REMOVED*** 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxText(node, text, containsOnlyTriviaWhiteSpaces) {
    return node.text !***REMOVED******REMOVED*** text || node.containsOnlyTriviaWhiteSpaces !***REMOVED******REMOVED*** containsOnlyTriviaWhiteSpaces ? update(createJsxText(text, containsOnlyTriviaWhiteSpaces), node) : node;
  }
  function createJsxOpeningFragment() {
    const node ***REMOVED*** createBaseNode(290 /* JsxOpeningFragment */);
    node.transformFlags |***REMOVED*** 2 /* ContainsJsx */;
    return node;
  }
  function createJsxJsxClosingFragment() {
    const node ***REMOVED*** createBaseNode(291 /* JsxClosingFragment */);
    node.transformFlags |***REMOVED*** 2 /* ContainsJsx */;
    return node;
  }
  function createJsxAttribute(name, initializer) {
    const node ***REMOVED*** createBaseDeclaration(292 /* JsxAttribute */);
    node.name ***REMOVED*** name;
    node.initializer ***REMOVED*** initializer;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name) | propagateChildFlags(node.initializer) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxAttribute(node, name, initializer) {
    return node.name !***REMOVED******REMOVED*** name || node.initializer !***REMOVED******REMOVED*** initializer ? update(createJsxAttribute(name, initializer), node) : node;
  }
  function createJsxAttributes(properties) {
    const node ***REMOVED*** createBaseDeclaration(293 /* JsxAttributes */);
    node.properties ***REMOVED*** createNodeArray(properties);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.properties) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxAttributes(node, properties) {
    return node.properties !***REMOVED******REMOVED*** properties ? update(createJsxAttributes(properties), node) : node;
  }
  function createJsxSpreadAttribute(expression) {
    const node ***REMOVED*** createBaseNode(294 /* JsxSpreadAttribute */);
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxSpreadAttribute(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createJsxSpreadAttribute(expression), node) : node;
  }
  function createJsxExpression(dotDotDotToken, expression) {
    const node ***REMOVED*** createBaseNode(295 /* JsxExpression */);
    node.dotDotDotToken ***REMOVED*** dotDotDotToken;
    node.expression ***REMOVED*** expression;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.dotDotDotToken) | propagateChildFlags(node.expression) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createJsxExpression(node.dotDotDotToken, expression), node) : node;
  }
  function createJsxNamespacedName(namespace, name) {
    const node ***REMOVED*** createBaseNode(296 /* JsxNamespacedName */);
    node.namespace ***REMOVED*** namespace;
    node.name ***REMOVED*** name;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.namespace) | propagateChildFlags(node.name) | 2 /* ContainsJsx */;
    return node;
  }
  function updateJsxNamespacedName(node, namespace, name) {
    return node.namespace !***REMOVED******REMOVED*** namespace || node.name !***REMOVED******REMOVED*** name ? update(createJsxNamespacedName(namespace, name), node) : node;
  }
  function createCaseClause(expression, statements) {
    const node ***REMOVED*** createBaseNode(297 /* CaseClause */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildrenFlags(node.statements);
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateCaseClause(node, expression, statements) {
    return node.expression !***REMOVED******REMOVED*** expression || node.statements !***REMOVED******REMOVED*** statements ? update(createCaseClause(expression, statements), node) : node;
  }
  function createDefaultClause(statements) {
    const node ***REMOVED*** createBaseNode(298 /* DefaultClause */);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.statements);
    return node;
  }
  function updateDefaultClause(node, statements) {
    return node.statements !***REMOVED******REMOVED*** statements ? update(createDefaultClause(statements), node) : node;
  }
  function createHeritageClause(token, types) {
    const node ***REMOVED*** createBaseNode(299 /* HeritageClause */);
    node.token ***REMOVED*** token;
    node.types ***REMOVED*** createNodeArray(types);
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.types);
    switch (token) {
      case 96 /* ExtendsKeyword */:
        node.transformFlags |***REMOVED*** 1024 /* ContainsES2015 */;
        break;
      case 119 /* ImplementsKeyword */:
        node.transformFlags |***REMOVED*** 1 /* ContainsTypeScript */;
        break;
      default:
        return Debug.assertNever(token);
    }
    return node;
  }
  function updateHeritageClause(node, types) {
    return node.types !***REMOVED******REMOVED*** types ? update(createHeritageClause(node.token, types), node) : node;
  }
  function createCatchClause(variableDeclaration, block) {
    const node ***REMOVED*** createBaseNode(300 /* CatchClause */);
    node.variableDeclaration ***REMOVED*** asVariableDeclaration(variableDeclaration);
    node.block ***REMOVED*** block;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.variableDeclaration) | propagateChildFlags(node.block) | (!variableDeclaration ? 64 /* ContainsES2019 */ : 0 /* None */);
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    return node;
  }
  function updateCatchClause(node, variableDeclaration, block) {
    return node.variableDeclaration !***REMOVED******REMOVED*** variableDeclaration || node.block !***REMOVED******REMOVED*** block ? update(createCatchClause(variableDeclaration, block), node) : node;
  }
  function createPropertyAssignment(name, initializer) {
    const node ***REMOVED*** createBaseDeclaration(304 /* PropertyAssignment */);
    node.name ***REMOVED*** asName(name);
    node.initializer ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(initializer);
    node.transformFlags |***REMOVED*** propagateNameFlags(node.name) | propagateChildFlags(node.initializer);
    node.modifiers ***REMOVED*** void 0;
    node.questionToken ***REMOVED*** void 0;
    node.exclamationToken ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updatePropertyAssignment(node, name, initializer) {
    return node.name !***REMOVED******REMOVED*** name || node.initializer !***REMOVED******REMOVED*** initializer ? finishUpdatePropertyAssignment(createPropertyAssignment(name, initializer), node) : node;
  }
  function finishUpdatePropertyAssignment(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.modifiers ***REMOVED*** original.modifiers;
      updated.questionToken ***REMOVED*** original.questionToken;
      updated.exclamationToken ***REMOVED*** original.exclamationToken;
    }
    return update(updated, original);
  }
  function createShorthandPropertyAssignment(name, objectAssignmentInitializer) {
    const node ***REMOVED*** createBaseDeclaration(305 /* ShorthandPropertyAssignment */);
    node.name ***REMOVED*** asName(name);
    node.objectAssignmentInitializer ***REMOVED*** objectAssignmentInitializer && parenthesizerRules().parenthesizeExpressionForDisallowedComma(objectAssignmentInitializer);
    node.transformFlags |***REMOVED*** propagateIdentifierNameFlags(node.name) | propagateChildFlags(node.objectAssignmentInitializer) | 1024 /* ContainsES2015 */;
    node.equalsToken ***REMOVED*** void 0;
    node.modifiers ***REMOVED*** void 0;
    node.questionToken ***REMOVED*** void 0;
    node.exclamationToken ***REMOVED*** void 0;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateShorthandPropertyAssignment(node, name, objectAssignmentInitializer) {
    return node.name !***REMOVED******REMOVED*** name || node.objectAssignmentInitializer !***REMOVED******REMOVED*** objectAssignmentInitializer ? finishUpdateShorthandPropertyAssignment(createShorthandPropertyAssignment(name, objectAssignmentInitializer), node) : node;
  }
  function finishUpdateShorthandPropertyAssignment(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      updated.modifiers ***REMOVED*** original.modifiers;
      updated.questionToken ***REMOVED*** original.questionToken;
      updated.exclamationToken ***REMOVED*** original.exclamationToken;
      updated.equalsToken ***REMOVED*** original.equalsToken;
    }
    return update(updated, original);
  }
  function createSpreadAssignment(expression) {
    const node ***REMOVED*** createBaseDeclaration(306 /* SpreadAssignment */);
    node.expression ***REMOVED*** parenthesizerRules().parenthesizeExpressionForDisallowedComma(expression);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 128 /* ContainsES2018 */ | 65536 /* ContainsObjectRestOrSpread */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateSpreadAssignment(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createSpreadAssignment(expression), node) : node;
  }
  function createEnumMember(name, initializer) {
    const node ***REMOVED*** createBaseDeclaration(307 /* EnumMember */);
    node.name ***REMOVED*** asName(name);
    node.initializer ***REMOVED*** initializer && parenthesizerRules().parenthesizeExpressionForDisallowedComma(initializer);
    node.transformFlags |***REMOVED*** propagateChildFlags(node.name) | propagateChildFlags(node.initializer) | 1 /* ContainsTypeScript */;
    node.jsDoc ***REMOVED*** void 0;
    return node;
  }
  function updateEnumMember(node, name, initializer) {
    return node.name !***REMOVED******REMOVED*** name || node.initializer !***REMOVED******REMOVED*** initializer ? update(createEnumMember(name, initializer), node) : node;
  }
  function createSourceFile2(statements, endOfFileToken, flags2) {
    const node ***REMOVED*** baseFactory2.createBaseSourceFileNode(308 /* SourceFile */);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.endOfFileToken ***REMOVED*** endOfFileToken;
    node.flags |***REMOVED*** flags2;
    node.text ***REMOVED*** "";
    node.fileName ***REMOVED*** "";
    node.path ***REMOVED*** "";
    node.resolvedPath ***REMOVED*** "";
    node.originalFileName ***REMOVED*** "";
    node.languageVersion ***REMOVED*** 1 /* ES5 */;
    node.languageVariant ***REMOVED*** 0;
    node.scriptKind ***REMOVED*** 0;
    node.isDeclarationFile ***REMOVED*** false;
    node.hasNoDefaultLib ***REMOVED*** false;
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.statements) | propagateChildFlags(node.endOfFileToken);
    node.locals ***REMOVED*** void 0;
    node.nextContainer ***REMOVED*** void 0;
    node.endFlowNode ***REMOVED*** void 0;
    node.nodeCount ***REMOVED*** 0;
    node.identifierCount ***REMOVED*** 0;
    node.symbolCount ***REMOVED*** 0;
    node.parseDiagnostics ***REMOVED*** void 0;
    node.bindDiagnostics ***REMOVED*** void 0;
    node.bindSuggestionDiagnostics ***REMOVED*** void 0;
    node.lineMap ***REMOVED*** void 0;
    node.externalModuleIndicator ***REMOVED*** void 0;
    node.setExternalModuleIndicator ***REMOVED*** void 0;
    node.pragmas ***REMOVED*** void 0;
    node.checkJsDirective ***REMOVED*** void 0;
    node.referencedFiles ***REMOVED*** void 0;
    node.typeReferenceDirectives ***REMOVED*** void 0;
    node.libReferenceDirectives ***REMOVED*** void 0;
    node.amdDependencies ***REMOVED*** void 0;
    node.commentDirectives ***REMOVED*** void 0;
    node.identifiers ***REMOVED*** void 0;
    node.packageJsonLocations ***REMOVED*** void 0;
    node.packageJsonScope ***REMOVED*** void 0;
    node.imports ***REMOVED*** void 0;
    node.moduleAugmentations ***REMOVED*** void 0;
    node.ambientModuleNames ***REMOVED*** void 0;
    node.classifiableNames ***REMOVED*** void 0;
    node.impliedNodeFormat ***REMOVED*** void 0;
    return node;
  }
  function createRedirectedSourceFile(redirectInfo) {
    const node ***REMOVED*** Object.create(redirectInfo.redirectTarget);
    Object.defineProperties(node, {
      id: {
        get() {
          return this.redirectInfo.redirectTarget.id;
        },
        set(value) {
          this.redirectInfo.redirectTarget.id ***REMOVED*** value;
        }
      },
      symbol: {
        get() {
          return this.redirectInfo.redirectTarget.symbol;
        },
        set(value) {
          this.redirectInfo.redirectTarget.symbol ***REMOVED*** value;
        }
      }
    });
    node.redirectInfo ***REMOVED*** redirectInfo;
    return node;
  }
  function cloneRedirectedSourceFile(source) {
    const node ***REMOVED*** createRedirectedSourceFile(source.redirectInfo);
    node.flags |***REMOVED*** source.flags & ~16 /* Synthesized */;
    node.fileName ***REMOVED*** source.fileName;
    node.path ***REMOVED*** source.path;
    node.resolvedPath ***REMOVED*** source.resolvedPath;
    node.originalFileName ***REMOVED*** source.originalFileName;
    node.packageJsonLocations ***REMOVED*** source.packageJsonLocations;
    node.packageJsonScope ***REMOVED*** source.packageJsonScope;
    node.emitNode ***REMOVED*** void 0;
    return node;
  }
  function cloneSourceFileWorker(source) {
    const node ***REMOVED*** baseFactory2.createBaseSourceFileNode(308 /* SourceFile */);
    node.flags |***REMOVED*** source.flags & ~16 /* Synthesized */;
    for (const p in source) {
      if (hasProperty(node, p) || !hasProperty(source, p)) {
        continue;
      }
      if (p ***REMOVED******REMOVED******REMOVED*** "emitNode") {
        node.emitNode ***REMOVED*** void 0;
        continue;
      }
      node[p] ***REMOVED*** source[p];
    }
    return node;
  }
  function cloneSourceFile(source) {
    const node ***REMOVED*** source.redirectInfo ? cloneRedirectedSourceFile(source) : cloneSourceFileWorker(source);
    setOriginal(node, source);
    return node;
  }
  function cloneSourceFileWithChanges(source, statements, isDeclarationFile, referencedFiles, typeReferences, hasNoDefaultLib, libReferences) {
    const node ***REMOVED*** cloneSourceFile(source);
    node.statements ***REMOVED*** createNodeArray(statements);
    node.isDeclarationFile ***REMOVED*** isDeclarationFile;
    node.referencedFiles ***REMOVED*** referencedFiles;
    node.typeReferenceDirectives ***REMOVED*** typeReferences;
    node.hasNoDefaultLib ***REMOVED*** hasNoDefaultLib;
    node.libReferenceDirectives ***REMOVED*** libReferences;
    node.transformFlags ***REMOVED*** propagateChildrenFlags(node.statements) | propagateChildFlags(node.endOfFileToken);
    return node;
  }
  function updateSourceFile(node, statements, isDeclarationFile ***REMOVED*** node.isDeclarationFile, referencedFiles ***REMOVED*** node.referencedFiles, typeReferenceDirectives ***REMOVED*** node.typeReferenceDirectives, hasNoDefaultLib ***REMOVED*** node.hasNoDefaultLib, libReferenceDirectives ***REMOVED*** node.libReferenceDirectives) {
    return node.statements !***REMOVED******REMOVED*** statements || node.isDeclarationFile !***REMOVED******REMOVED*** isDeclarationFile || node.referencedFiles !***REMOVED******REMOVED*** referencedFiles || node.typeReferenceDirectives !***REMOVED******REMOVED*** typeReferenceDirectives || node.hasNoDefaultLib !***REMOVED******REMOVED*** hasNoDefaultLib || node.libReferenceDirectives !***REMOVED******REMOVED*** libReferenceDirectives ? update(cloneSourceFileWithChanges(node, statements, isDeclarationFile, referencedFiles, typeReferenceDirectives, hasNoDefaultLib, libReferenceDirectives), node) : node;
  }
  function createBundle(sourceFiles) {
    const node ***REMOVED*** createBaseNode(309 /* Bundle */);
    node.sourceFiles ***REMOVED*** sourceFiles;
    node.syntheticFileReferences ***REMOVED*** void 0;
    node.syntheticTypeReferences ***REMOVED*** void 0;
    node.syntheticLibReferences ***REMOVED*** void 0;
    node.hasNoDefaultLib ***REMOVED*** void 0;
    return node;
  }
  function updateBundle(node, sourceFiles) {
    return node.sourceFiles !***REMOVED******REMOVED*** sourceFiles ? update(createBundle(sourceFiles), node) : node;
  }
  function createSyntheticExpression(type, isSpread ***REMOVED*** false, tupleNameSource) {
    const node ***REMOVED*** createBaseNode(238 /* SyntheticExpression */);
    node.type ***REMOVED*** type;
    node.isSpread ***REMOVED*** isSpread;
    node.tupleNameSource ***REMOVED*** tupleNameSource;
    return node;
  }
  function createSyntaxList(children) {
    const node ***REMOVED*** createBaseNode(353 /* SyntaxList */);
    node._children ***REMOVED*** children;
    return node;
  }
  function createNotEmittedStatement(original) {
    const node ***REMOVED*** createBaseNode(354 /* NotEmittedStatement */);
    node.original ***REMOVED*** original;
    setTextRange(node, original);
    return node;
  }
  function createPartiallyEmittedExpression(expression, original) {
    const node ***REMOVED*** createBaseNode(356 /* PartiallyEmittedExpression */);
    node.expression ***REMOVED*** expression;
    node.original ***REMOVED*** original;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | 1 /* ContainsTypeScript */;
    setTextRange(node, original);
    return node;
  }
  function updatePartiallyEmittedExpression(node, expression) {
    return node.expression !***REMOVED******REMOVED*** expression ? update(createPartiallyEmittedExpression(expression, node.original), node) : node;
  }
  function createNotEmittedTypeElement() {
    return createBaseNode(355 /* NotEmittedTypeElement */);
  }
  function flattenCommaElements(node) {
    if (nodeIsSynthesized(node) && !isParseTreeNode(node) && !node.original && !node.emitNode && !node.id) {
      if (isCommaListExpression(node)) {
        return node.elements;
      }
      if (isBinaryExpression(node) && isCommaToken(node.operatorToken)) {
        return [node.left, node.right];
      }
    }
    return node;
  }
  function createCommaListExpression(elements) {
    const node ***REMOVED*** createBaseNode(357 /* CommaListExpression */);
    node.elements ***REMOVED*** createNodeArray(sameFlatMap(elements, flattenCommaElements));
    node.transformFlags |***REMOVED*** propagateChildrenFlags(node.elements);
    return node;
  }
  function updateCommaListExpression(node, elements) {
    return node.elements !***REMOVED******REMOVED*** elements ? update(createCommaListExpression(elements), node) : node;
  }
  function createSyntheticReferenceExpression(expression, thisArg) {
    const node ***REMOVED*** createBaseNode(358 /* SyntheticReferenceExpression */);
    node.expression ***REMOVED*** expression;
    node.thisArg ***REMOVED*** thisArg;
    node.transformFlags |***REMOVED*** propagateChildFlags(node.expression) | propagateChildFlags(node.thisArg);
    return node;
  }
  function updateSyntheticReferenceExpression(node, expression, thisArg) {
    return node.expression !***REMOVED******REMOVED*** expression || node.thisArg !***REMOVED******REMOVED*** thisArg ? update(createSyntheticReferenceExpression(expression, thisArg), node) : node;
  }
  function cloneGeneratedIdentifier(node) {
    const clone ***REMOVED*** createBaseIdentifier(node.escapedText);
    clone.flags |***REMOVED*** node.flags & ~16 /* Synthesized */;
    clone.transformFlags ***REMOVED*** node.transformFlags;
    setOriginal(clone, node);
    setIdentifierAutoGenerate(clone, { ...node.emitNode.autoGenerate });
    return clone;
  }
  function cloneIdentifier(node) {
    const clone ***REMOVED*** createBaseIdentifier(node.escapedText);
    clone.flags |***REMOVED*** node.flags & ~16 /* Synthesized */;
    clone.jsDoc ***REMOVED*** node.jsDoc;
    clone.flowNode ***REMOVED*** node.flowNode;
    clone.symbol ***REMOVED*** node.symbol;
    clone.transformFlags ***REMOVED*** node.transformFlags;
    setOriginal(clone, node);
    const typeArguments ***REMOVED*** getIdentifierTypeArguments(node);
    if (typeArguments) setIdentifierTypeArguments(clone, typeArguments);
    return clone;
  }
  function cloneGeneratedPrivateIdentifier(node) {
    const clone ***REMOVED*** createBasePrivateIdentifier(node.escapedText);
    clone.flags |***REMOVED*** node.flags & ~16 /* Synthesized */;
    clone.transformFlags ***REMOVED*** node.transformFlags;
    setOriginal(clone, node);
    setIdentifierAutoGenerate(clone, { ...node.emitNode.autoGenerate });
    return clone;
  }
  function clonePrivateIdentifier(node) {
    const clone ***REMOVED*** createBasePrivateIdentifier(node.escapedText);
    clone.flags |***REMOVED*** node.flags & ~16 /* Synthesized */;
    clone.transformFlags ***REMOVED*** node.transformFlags;
    setOriginal(clone, node);
    return clone;
  }
  function cloneNode(node) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
      return node;
    }
    if (isSourceFile(node)) {
      return cloneSourceFile(node);
    }
    if (isGeneratedIdentifier(node)) {
      return cloneGeneratedIdentifier(node);
    }
    if (isIdentifier(node)) {
      return cloneIdentifier(node);
    }
    if (isGeneratedPrivateIdentifier(node)) {
      return cloneGeneratedPrivateIdentifier(node);
    }
    if (isPrivateIdentifier(node)) {
      return clonePrivateIdentifier(node);
    }
    const clone ***REMOVED*** !isNodeKind(node.kind) ? baseFactory2.createBaseTokenNode(node.kind) : baseFactory2.createBaseNode(node.kind);
    clone.flags |***REMOVED*** node.flags & ~16 /* Synthesized */;
    clone.transformFlags ***REMOVED*** node.transformFlags;
    setOriginal(clone, node);
    for (const key in node) {
      if (hasProperty(clone, key) || !hasProperty(node, key)) {
        continue;
      }
      clone[key] ***REMOVED*** node[key];
    }
    return clone;
  }
  function createImmediatelyInvokedFunctionExpression(statements, param, paramValue) {
    return createCallExpression(
      createFunctionExpression(
        /*modifiers*/
        void 0,
        /*asteriskToken*/
        void 0,
        /*name*/
        void 0,
        /*typeParameters*/
        void 0,
        /*parameters*/
        param ? [param] : [],
        /*type*/
        void 0,
        createBlock(
          statements,
          /*multiLine*/
          true
        )
      ),
      /*typeArguments*/
      void 0,
      /*argumentsArray*/
      paramValue ? [paramValue] : []
    );
  }
  function createImmediatelyInvokedArrowFunction(statements, param, paramValue) {
    return createCallExpression(
      createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        /*parameters*/
        param ? [param] : [],
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        createBlock(
          statements,
          /*multiLine*/
          true
        )
      ),
      /*typeArguments*/
      void 0,
      /*argumentsArray*/
      paramValue ? [paramValue] : []
    );
  }
  function createVoidZero() {
    return createVoidExpression(createNumericLiteral("0"));
  }
  function createExportDefault(expression) {
    return createExportAssignment(
      /*modifiers*/
      void 0,
      /*isExportEquals*/
      false,
      expression
    );
  }
  function createExternalModuleExport(exportName) {
    return createExportDeclaration(
      /*modifiers*/
      void 0,
      /*isTypeOnly*/
      false,
      createNamedExports([
        createExportSpecifier(
          /*isTypeOnly*/
          false,
          /*propertyName*/
          void 0,
          exportName
        )
      ])
    );
  }
  function createTypeCheck(value, tag) {
    return tag ***REMOVED******REMOVED******REMOVED*** "null" ? factory2.createStrictEquality(value, createNull()) : tag ***REMOVED******REMOVED******REMOVED*** "undefined" ? factory2.createStrictEquality(value, createVoidZero()) : factory2.createStrictEquality(createTypeOfExpression(value), createStringLiteral(tag));
  }
  function createIsNotTypeCheck(value, tag) {
    return tag ***REMOVED******REMOVED******REMOVED*** "null" ? factory2.createStrictInequality(value, createNull()) : tag ***REMOVED******REMOVED******REMOVED*** "undefined" ? factory2.createStrictInequality(value, createVoidZero()) : factory2.createStrictInequality(createTypeOfExpression(value), createStringLiteral(tag));
  }
  function createMethodCall(object, methodName, argumentsList) {
    if (isCallChain(object)) {
      return createCallChain(
        createPropertyAccessChain(
          object,
          /*questionDotToken*/
          void 0,
          methodName
        ),
        /*questionDotToken*/
        void 0,
        /*typeArguments*/
        void 0,
        argumentsList
      );
    }
    return createCallExpression(
      createPropertyAccessExpression(object, methodName),
      /*typeArguments*/
      void 0,
      argumentsList
    );
  }
  function createFunctionBindCall(target, thisArg, argumentsList) {
    return createMethodCall(target, "bind", [thisArg, ...argumentsList]);
  }
  function createFunctionCallCall(target, thisArg, argumentsList) {
    return createMethodCall(target, "call", [thisArg, ...argumentsList]);
  }
  function createFunctionApplyCall(target, thisArg, argumentsExpression) {
    return createMethodCall(target, "apply", [thisArg, argumentsExpression]);
  }
  function createGlobalMethodCall(globalObjectName, methodName, argumentsList) {
    return createMethodCall(createIdentifier(globalObjectName), methodName, argumentsList);
  }
  function createArraySliceCall(array, start) {
    return createMethodCall(array, "slice", start ***REMOVED******REMOVED******REMOVED*** void 0 ? [] : [asExpression(start)]);
  }
  function createArrayConcatCall(array, argumentsList) {
    return createMethodCall(array, "concat", argumentsList);
  }
  function createObjectDefinePropertyCall(target, propertyName, attributes) {
    return createGlobalMethodCall("Object", "defineProperty", [target, asExpression(propertyName), attributes]);
  }
  function createObjectGetOwnPropertyDescriptorCall(target, propertyName) {
    return createGlobalMethodCall("Object", "getOwnPropertyDescriptor", [target, asExpression(propertyName)]);
  }
  function createReflectGetCall(target, propertyKey, receiver) {
    return createGlobalMethodCall("Reflect", "get", receiver ? [target, propertyKey, receiver] : [target, propertyKey]);
  }
  function createReflectSetCall(target, propertyKey, value, receiver) {
    return createGlobalMethodCall("Reflect", "set", receiver ? [target, propertyKey, value, receiver] : [target, propertyKey, value]);
  }
  function tryAddPropertyAssignment(properties, propertyName, expression) {
    if (expression) {
      properties.push(createPropertyAssignment(propertyName, expression));
      return true;
    }
    return false;
  }
  function createPropertyDescriptor(attributes, singleLine) {
    const properties ***REMOVED*** [];
    tryAddPropertyAssignment(properties, "enumerable", asExpression(attributes.enumerable));
    tryAddPropertyAssignment(properties, "configurable", asExpression(attributes.configurable));
    let isData ***REMOVED*** tryAddPropertyAssignment(properties, "writable", asExpression(attributes.writable));
    isData ***REMOVED*** tryAddPropertyAssignment(properties, "value", attributes.value) || isData;
    let isAccessor2 ***REMOVED*** tryAddPropertyAssignment(properties, "get", attributes.get);
    isAccessor2 ***REMOVED*** tryAddPropertyAssignment(properties, "set", attributes.set) || isAccessor2;
    Debug.assert(!(isData && isAccessor2), "A PropertyDescriptor may not be both an accessor descriptor and a data descriptor.");
    return createObjectLiteralExpression(properties, !singleLine);
  }
  function updateOuterExpression(outerExpression, expression) {
    switch (outerExpression.kind) {
      case 218 /* ParenthesizedExpression */:
        return updateParenthesizedExpression(outerExpression, expression);
      case 217 /* TypeAssertionExpression */:
        return updateTypeAssertion(outerExpression, outerExpression.type, expression);
      case 235 /* AsExpression */:
        return updateAsExpression(outerExpression, expression, outerExpression.type);
      case 239 /* SatisfiesExpression */:
        return updateSatisfiesExpression(outerExpression, expression, outerExpression.type);
      case 236 /* NonNullExpression */:
        return updateNonNullExpression(outerExpression, expression);
      case 234 /* ExpressionWithTypeArguments */:
        return updateExpressionWithTypeArguments(outerExpression, expression, outerExpression.typeArguments);
      case 356 /* PartiallyEmittedExpression */:
        return updatePartiallyEmittedExpression(outerExpression, expression);
    }
  }
  function isIgnorableParen(node) {
    return isParenthesizedExpression(node) && nodeIsSynthesized(node) && nodeIsSynthesized(getSourceMapRange(node)) && nodeIsSynthesized(getCommentRange(node)) && !some(getSyntheticLeadingComments(node)) && !some(getSyntheticTrailingComments(node));
  }
  function restoreOuterExpressions(outerExpression, innerExpression, kinds ***REMOVED*** 63 /* All */) {
    if (outerExpression && isOuterExpression(outerExpression, kinds) && !isIgnorableParen(outerExpression)) {
      return updateOuterExpression(
        outerExpression,
        restoreOuterExpressions(outerExpression.expression, innerExpression)
      );
    }
    return innerExpression;
  }
  function restoreEnclosingLabel(node, outermostLabeledStatement, afterRestoreLabelCallback) {
    if (!outermostLabeledStatement) {
      return node;
    }
    const updated ***REMOVED*** updateLabeledStatement(
      outermostLabeledStatement,
      outermostLabeledStatement.label,
      isLabeledStatement(outermostLabeledStatement.statement) ? restoreEnclosingLabel(node, outermostLabeledStatement.statement) : node
    );
    if (afterRestoreLabelCallback) {
      afterRestoreLabelCallback(outermostLabeledStatement);
    }
    return updated;
  }
  function shouldBeCapturedInTempVariable(node, cacheIdentifiers) {
    const target ***REMOVED*** skipParentheses(node);
    switch (target.kind) {
      case 80 /* Identifier */:
        return cacheIdentifiers;
      case 110 /* ThisKeyword */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 11 /* StringLiteral */:
        return false;
      case 210 /* ArrayLiteralExpression */:
        const elements ***REMOVED*** target.elements;
        if (elements.length ***REMOVED******REMOVED******REMOVED*** 0) {
          return false;
        }
        return true;
      case 211 /* ObjectLiteralExpression */:
        return target.properties.length > 0;
      default:
        return true;
    }
  }
  function createCallBinding(expression, recordTempVariable, languageVersion, cacheIdentifiers ***REMOVED*** false) {
    const callee ***REMOVED*** skipOuterExpressions(expression, 63 /* All */);
    let thisArg;
    let target;
    if (isSuperProperty(callee)) {
      thisArg ***REMOVED*** createThis();
      target ***REMOVED*** callee;
    } else if (isSuperKeyword(callee)) {
      thisArg ***REMOVED*** createThis();
      target ***REMOVED*** languageVersion !***REMOVED******REMOVED*** void 0 && languageVersion < 2 /* ES2015 */ ? setTextRange(createIdentifier("_super"), callee) : callee;
    } else if (getEmitFlags(callee) & 8192 /* HelperName */) {
      thisArg ***REMOVED*** createVoidZero();
      target ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
        callee,
        /*optionalChain*/
        false
      );
    } else if (isPropertyAccessExpression(callee)) {
      if (shouldBeCapturedInTempVariable(callee.expression, cacheIdentifiers)) {
        thisArg ***REMOVED*** createTempVariable(recordTempVariable);
        target ***REMOVED*** createPropertyAccessExpression(
          setTextRange(
            factory2.createAssignment(
              thisArg,
              callee.expression
            ),
            callee.expression
          ),
          callee.name
        );
        setTextRange(target, callee);
      } else {
        thisArg ***REMOVED*** callee.expression;
        target ***REMOVED*** callee;
      }
    } else if (isElementAccessExpression(callee)) {
      if (shouldBeCapturedInTempVariable(callee.expression, cacheIdentifiers)) {
        thisArg ***REMOVED*** createTempVariable(recordTempVariable);
        target ***REMOVED*** createElementAccessExpression(
          setTextRange(
            factory2.createAssignment(
              thisArg,
              callee.expression
            ),
            callee.expression
          ),
          callee.argumentExpression
        );
        setTextRange(target, callee);
      } else {
        thisArg ***REMOVED*** callee.expression;
        target ***REMOVED*** callee;
      }
    } else {
      thisArg ***REMOVED*** createVoidZero();
      target ***REMOVED*** parenthesizerRules().parenthesizeLeftSideOfAccess(
        expression,
        /*optionalChain*/
        false
      );
    }
    return { target, thisArg };
  }
  function createAssignmentTargetWrapper(paramName, expression) {
    return createPropertyAccessExpression(
      // Explicit parens required because of v8 regression (https://bugs.chromium.org/p/v8/issues/detail?id***REMOVED***9560)
      createParenthesizedExpression(
        createObjectLiteralExpression([
          createSetAccessorDeclaration(
            /*modifiers*/
            void 0,
            "value",
            [createParameterDeclaration(
              /*modifiers*/
              void 0,
              /*dotDotDotToken*/
              void 0,
              paramName,
              /*questionToken*/
              void 0,
              /*type*/
              void 0,
              /*initializer*/
              void 0
            )],
            createBlock([
              createExpressionStatement(expression)
            ])
          )
        ])
      ),
      "value"
    );
  }
  function inlineExpressions(expressions) {
    return expressions.length > 10 ? createCommaListExpression(expressions) : reduceLeft(expressions, factory2.createComma);
  }
  function getName(node, allowComments, allowSourceMaps, emitFlags ***REMOVED*** 0, ignoreAssignedName) {
    const nodeName ***REMOVED*** ignoreAssignedName ? node && getNonAssignedNameOfDeclaration(node) : getNameOfDeclaration(node);
    if (nodeName && isIdentifier(nodeName) && !isGeneratedIdentifier(nodeName)) {
      const name ***REMOVED*** setParent(setTextRange(cloneNode(nodeName), nodeName), nodeName.parent);
      emitFlags |***REMOVED*** getEmitFlags(nodeName);
      if (!allowSourceMaps) emitFlags |***REMOVED*** 96 /* NoSourceMap */;
      if (!allowComments) emitFlags |***REMOVED*** 3072 /* NoComments */;
      if (emitFlags) setEmitFlags(name, emitFlags);
      return name;
    }
    return getGeneratedNameForNode(node);
  }
  function getInternalName(node, allowComments, allowSourceMaps) {
    return getName(node, allowComments, allowSourceMaps, 32768 /* LocalName */ | 65536 /* InternalName */);
  }
  function getLocalName(node, allowComments, allowSourceMaps, ignoreAssignedName) {
    return getName(node, allowComments, allowSourceMaps, 32768 /* LocalName */, ignoreAssignedName);
  }
  function getExportName(node, allowComments, allowSourceMaps) {
    return getName(node, allowComments, allowSourceMaps, 16384 /* ExportName */);
  }
  function getDeclarationName(node, allowComments, allowSourceMaps) {
    return getName(node, allowComments, allowSourceMaps);
  }
  function getNamespaceMemberName(ns, name, allowComments, allowSourceMaps) {
    const qualifiedName ***REMOVED*** createPropertyAccessExpression(ns, nodeIsSynthesized(name) ? name : cloneNode(name));
    setTextRange(qualifiedName, name);
    let emitFlags ***REMOVED*** 0;
    if (!allowSourceMaps) emitFlags |***REMOVED*** 96 /* NoSourceMap */;
    if (!allowComments) emitFlags |***REMOVED*** 3072 /* NoComments */;
    if (emitFlags) setEmitFlags(qualifiedName, emitFlags);
    return qualifiedName;
  }
  function getExternalModuleOrNamespaceExportName(ns, node, allowComments, allowSourceMaps) {
    if (ns && hasSyntacticModifier(node, 32 /* Export */)) {
      return getNamespaceMemberName(ns, getName(node), allowComments, allowSourceMaps);
    }
    return getExportName(node, allowComments, allowSourceMaps);
  }
  function copyPrologue(source, target, ensureUseStrict2, visitor) {
    const offset ***REMOVED*** copyStandardPrologue(source, target, 0, ensureUseStrict2);
    return copyCustomPrologue(source, target, offset, visitor);
  }
  function isUseStrictPrologue2(node) {
    return isStringLiteral(node.expression) && node.expression.text ***REMOVED******REMOVED******REMOVED*** "use strict";
  }
  function createUseStrictPrologue() {
    return startOnNewLine(createExpressionStatement(createStringLiteral("use strict")));
  }
  function copyStandardPrologue(source, target, statementOffset ***REMOVED*** 0, ensureUseStrict2) {
    Debug.assert(target.length ***REMOVED******REMOVED******REMOVED*** 0, "Prologue directives should be at the first statement in the target statements array");
    let foundUseStrict ***REMOVED*** false;
    const numStatements ***REMOVED*** source.length;
    while (statementOffset < numStatements) {
      const statement ***REMOVED*** source[statementOffset];
      if (isPrologueDirective(statement)) {
        if (isUseStrictPrologue2(statement)) {
          foundUseStrict ***REMOVED*** true;
        }
        target.push(statement);
      } else {
        break;
      }
      statementOffset++;
    }
    if (ensureUseStrict2 && !foundUseStrict) {
      target.push(createUseStrictPrologue());
    }
    return statementOffset;
  }
  function copyCustomPrologue(source, target, statementOffset, visitor, filter2 ***REMOVED*** returnTrue) {
    const numStatements ***REMOVED*** source.length;
    while (statementOffset !***REMOVED******REMOVED*** void 0 && statementOffset < numStatements) {
      const statement ***REMOVED*** source[statementOffset];
      if (getEmitFlags(statement) & 2097152 /* CustomPrologue */ && filter2(statement)) {
        append(target, visitor ? visitNode(statement, visitor, isStatement) : statement);
      } else {
        break;
      }
      statementOffset++;
    }
    return statementOffset;
  }
  function ensureUseStrict(statements) {
    const foundUseStrict ***REMOVED*** findUseStrictPrologue(statements);
    if (!foundUseStrict) {
      return setTextRange(createNodeArray([createUseStrictPrologue(), ...statements]), statements);
    }
    return statements;
  }
  function liftToBlock(nodes) {
    Debug.assert(every(nodes, isStatementOrBlock), "Cannot lift nodes to a Block.");
    return singleOrUndefined(nodes) || createBlock(nodes);
  }
  function findSpanEnd(array, test, start) {
    let i ***REMOVED*** start;
    while (i < array.length && test(array[i])) {
      i++;
    }
    return i;
  }
  function mergeLexicalEnvironment(statements, declarations) {
    if (!some(declarations)) {
      return statements;
    }
    const leftStandardPrologueEnd ***REMOVED*** findSpanEnd(statements, isPrologueDirective, 0);
    const leftHoistedFunctionsEnd ***REMOVED*** findSpanEnd(statements, isHoistedFunction, leftStandardPrologueEnd);
    const leftHoistedVariablesEnd ***REMOVED*** findSpanEnd(statements, isHoistedVariableStatement, leftHoistedFunctionsEnd);
    const rightStandardPrologueEnd ***REMOVED*** findSpanEnd(declarations, isPrologueDirective, 0);
    const rightHoistedFunctionsEnd ***REMOVED*** findSpanEnd(declarations, isHoistedFunction, rightStandardPrologueEnd);
    const rightHoistedVariablesEnd ***REMOVED*** findSpanEnd(declarations, isHoistedVariableStatement, rightHoistedFunctionsEnd);
    const rightCustomPrologueEnd ***REMOVED*** findSpanEnd(declarations, isCustomPrologue, rightHoistedVariablesEnd);
    Debug.assert(rightCustomPrologueEnd ***REMOVED******REMOVED******REMOVED*** declarations.length, "Expected declarations to be valid standard or custom prologues");
    const left ***REMOVED*** isNodeArray(statements) ? statements.slice() : statements;
    if (rightCustomPrologueEnd > rightHoistedVariablesEnd) {
      left.splice(leftHoistedVariablesEnd, 0, ...declarations.slice(rightHoistedVariablesEnd, rightCustomPrologueEnd));
    }
    if (rightHoistedVariablesEnd > rightHoistedFunctionsEnd) {
      left.splice(leftHoistedFunctionsEnd, 0, ...declarations.slice(rightHoistedFunctionsEnd, rightHoistedVariablesEnd));
    }
    if (rightHoistedFunctionsEnd > rightStandardPrologueEnd) {
      left.splice(leftStandardPrologueEnd, 0, ...declarations.slice(rightStandardPrologueEnd, rightHoistedFunctionsEnd));
    }
    if (rightStandardPrologueEnd > 0) {
      if (leftStandardPrologueEnd ***REMOVED******REMOVED******REMOVED*** 0) {
        left.splice(0, 0, ...declarations.slice(0, rightStandardPrologueEnd));
      } else {
        const leftPrologues ***REMOVED*** /* @__PURE__ */ new Map();
        for (let i ***REMOVED*** 0; i < leftStandardPrologueEnd; i++) {
          const leftPrologue ***REMOVED*** statements[i];
          leftPrologues.set(leftPrologue.expression.text, true);
        }
        for (let i ***REMOVED*** rightStandardPrologueEnd - 1; i >***REMOVED*** 0; i--) {
          const rightPrologue ***REMOVED*** declarations[i];
          if (!leftPrologues.has(rightPrologue.expression.text)) {
            left.unshift(rightPrologue);
          }
        }
      }
    }
    if (isNodeArray(statements)) {
      return setTextRange(createNodeArray(left, statements.hasTrailingComma), statements);
    }
    return statements;
  }
  function replaceModifiers(node, modifiers) {
    let modifierArray;
    if (typeof modifiers ***REMOVED******REMOVED******REMOVED*** "number") {
      modifierArray ***REMOVED*** createModifiersFromModifierFlags(modifiers);
    } else {
      modifierArray ***REMOVED*** modifiers;
    }
    return isTypeParameterDeclaration(node) ? updateTypeParameterDeclaration(node, modifierArray, node.name, node.constraint, node.default) : isParameter(node) ? updateParameterDeclaration(node, modifierArray, node.dotDotDotToken, node.name, node.questionToken, node.type, node.initializer) : isConstructorTypeNode(node) ? updateConstructorTypeNode1(node, modifierArray, node.typeParameters, node.parameters, node.type) : isPropertySignature(node) ? updatePropertySignature(node, modifierArray, node.name, node.questionToken, node.type) : isPropertyDeclaration(node) ? updatePropertyDeclaration(node, modifierArray, node.name, node.questionToken ?? node.exclamationToken, node.type, node.initializer) : isMethodSignature(node) ? updateMethodSignature(node, modifierArray, node.name, node.questionToken, node.typeParameters, node.parameters, node.type) : isMethodDeclaration(node) ? updateMethodDeclaration(node, modifierArray, node.asteriskToken, node.name, node.questionToken, node.typeParameters, node.parameters, node.type, node.body) : isConstructorDeclaration(node) ? updateConstructorDeclaration(node, modifierArray, node.parameters, node.body) : isGetAccessorDeclaration(node) ? updateGetAccessorDeclaration(node, modifierArray, node.name, node.parameters, node.type, node.body) : isSetAccessorDeclaration(node) ? updateSetAccessorDeclaration(node, modifierArray, node.name, node.parameters, node.body) : isIndexSignatureDeclaration(node) ? updateIndexSignature(node, modifierArray, node.parameters, node.type) : isFunctionExpression(node) ? updateFunctionExpression(node, modifierArray, node.asteriskToken, node.name, node.typeParameters, node.parameters, node.type, node.body) : isArrowFunction(node) ? updateArrowFunction(node, modifierArray, node.typeParameters, node.parameters, node.type, node.equalsGreaterThanToken, node.body) : isClassExpression(node) ? updateClassExpression(node, modifierArray, node.name, node.typeParameters, node.heritageClauses, node.members) : isVariableStatement(node) ? updateVariableStatement(node, modifierArray, node.declarationList) : isFunctionDeclaration(node) ? updateFunctionDeclaration(node, modifierArray, node.asteriskToken, node.name, node.typeParameters, node.parameters, node.type, node.body) : isClassDeclaration(node) ? updateClassDeclaration(node, modifierArray, node.name, node.typeParameters, node.heritageClauses, node.members) : isInterfaceDeclaration(node) ? updateInterfaceDeclaration(node, modifierArray, node.name, node.typeParameters, node.heritageClauses, node.members) : isTypeAliasDeclaration(node) ? updateTypeAliasDeclaration(node, modifierArray, node.name, node.typeParameters, node.type) : isEnumDeclaration(node) ? updateEnumDeclaration(node, modifierArray, node.name, node.members) : isModuleDeclaration(node) ? updateModuleDeclaration(node, modifierArray, node.name, node.body) : isImportEqualsDeclaration(node) ? updateImportEqualsDeclaration(node, modifierArray, node.isTypeOnly, node.name, node.moduleReference) : isImportDeclaration(node) ? updateImportDeclaration(node, modifierArray, node.importClause, node.moduleSpecifier, node.attributes) : isExportAssignment(node) ? updateExportAssignment(node, modifierArray, node.expression) : isExportDeclaration(node) ? updateExportDeclaration(node, modifierArray, node.isTypeOnly, node.exportClause, node.moduleSpecifier, node.attributes) : Debug.assertNever(node);
  }
  function replaceDecoratorsAndModifiers(node, modifierArray) {
    return isParameter(node) ? updateParameterDeclaration(node, modifierArray, node.dotDotDotToken, node.name, node.questionToken, node.type, node.initializer) : isPropertyDeclaration(node) ? updatePropertyDeclaration(node, modifierArray, node.name, node.questionToken ?? node.exclamationToken, node.type, node.initializer) : isMethodDeclaration(node) ? updateMethodDeclaration(node, modifierArray, node.asteriskToken, node.name, node.questionToken, node.typeParameters, node.parameters, node.type, node.body) : isGetAccessorDeclaration(node) ? updateGetAccessorDeclaration(node, modifierArray, node.name, node.parameters, node.type, node.body) : isSetAccessorDeclaration(node) ? updateSetAccessorDeclaration(node, modifierArray, node.name, node.parameters, node.body) : isClassExpression(node) ? updateClassExpression(node, modifierArray, node.name, node.typeParameters, node.heritageClauses, node.members) : isClassDeclaration(node) ? updateClassDeclaration(node, modifierArray, node.name, node.typeParameters, node.heritageClauses, node.members) : Debug.assertNever(node);
  }
  function replacePropertyName(node, name) {
    switch (node.kind) {
      case 178 /* GetAccessor */:
        return updateGetAccessorDeclaration(node, node.modifiers, name, node.parameters, node.type, node.body);
      case 179 /* SetAccessor */:
        return updateSetAccessorDeclaration(node, node.modifiers, name, node.parameters, node.body);
      case 175 /* MethodDeclaration */:
        return updateMethodDeclaration(node, node.modifiers, node.asteriskToken, name, node.questionToken, node.typeParameters, node.parameters, node.type, node.body);
      case 174 /* MethodSignature */:
        return updateMethodSignature(node, node.modifiers, name, node.questionToken, node.typeParameters, node.parameters, node.type);
      case 173 /* PropertyDeclaration */:
        return updatePropertyDeclaration(node, node.modifiers, name, node.questionToken ?? node.exclamationToken, node.type, node.initializer);
      case 172 /* PropertySignature */:
        return updatePropertySignature(node, node.modifiers, name, node.questionToken, node.type);
      case 304 /* PropertyAssignment */:
        return updatePropertyAssignment(node, name, node.initializer);
    }
  }
  function asNodeArray(array) {
    return array ? createNodeArray(array) : void 0;
  }
  function asName(name) {
    return typeof name ***REMOVED******REMOVED******REMOVED*** "string" ? createIdentifier(name) : name;
  }
  function asExpression(value) {
    return typeof value ***REMOVED******REMOVED******REMOVED*** "string" ? createStringLiteral(value) : typeof value ***REMOVED******REMOVED******REMOVED*** "number" ? createNumericLiteral(value) : typeof value ***REMOVED******REMOVED******REMOVED*** "boolean" ? value ? createTrue() : createFalse() : value;
  }
  function asInitializer(node) {
    return node && parenthesizerRules().parenthesizeExpressionForDisallowedComma(node);
  }
  function asToken(value) {
    return typeof value ***REMOVED******REMOVED******REMOVED*** "number" ? createToken(value) : value;
  }
  function asEmbeddedStatement(statement) {
    return statement && isNotEmittedStatement(statement) ? setTextRange(setOriginal(createEmptyStatement(), statement), statement) : statement;
  }
  function asVariableDeclaration(variableDeclaration) {
    if (typeof variableDeclaration ***REMOVED******REMOVED******REMOVED*** "string" || variableDeclaration && !isVariableDeclaration(variableDeclaration)) {
      return createVariableDeclaration(
        variableDeclaration,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        /*initializer*/
        void 0
      );
    }
    return variableDeclaration;
  }
  function update(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      setOriginal(updated, original);
      setTextRange(updated, original);
    }
    return updated;
  }
}
function getDefaultTagNameForKind(kind) {
  switch (kind) {
    case 345 /* JSDocTypeTag */:
      return "type";
    case 343 /* JSDocReturnTag */:
      return "returns";
    case 344 /* JSDocThisTag */:
      return "this";
    case 341 /* JSDocEnumTag */:
      return "enum";
    case 331 /* JSDocAuthorTag */:
      return "author";
    case 333 /* JSDocClassTag */:
      return "class";
    case 334 /* JSDocPublicTag */:
      return "public";
    case 335 /* JSDocPrivateTag */:
      return "private";
    case 336 /* JSDocProtectedTag */:
      return "protected";
    case 337 /* JSDocReadonlyTag */:
      return "readonly";
    case 338 /* JSDocOverrideTag */:
      return "override";
    case 346 /* JSDocTemplateTag */:
      return "template";
    case 347 /* JSDocTypedefTag */:
      return "typedef";
    case 342 /* JSDocParameterTag */:
      return "param";
    case 349 /* JSDocPropertyTag */:
      return "prop";
    case 339 /* JSDocCallbackTag */:
      return "callback";
    case 340 /* JSDocOverloadTag */:
      return "overload";
    case 329 /* JSDocAugmentsTag */:
      return "augments";
    case 330 /* JSDocImplementsTag */:
      return "implements";
    case 352 /* JSDocImportTag */:
      return "import";
    default:
      return Debug.fail(`Unsupported kind: ${Debug.formatSyntaxKind(kind)}`);
  }
}
var rawTextScanner;
var invalidValueSentinel ***REMOVED*** {};
function getCookedText(kind, rawText) {
  if (!rawTextScanner) {
    rawTextScanner ***REMOVED*** createScanner(
      99 /* Latest */,
      /*skipTrivia*/
      false,
      0 /* Standard */
    );
  }
  switch (kind) {
    case 15 /* NoSubstitutionTemplateLiteral */:
      rawTextScanner.setText("`" + rawText + "`");
      break;
    case 16 /* TemplateHead */:
      rawTextScanner.setText("`" + rawText + "${");
      break;
    case 17 /* TemplateMiddle */:
      rawTextScanner.setText("}" + rawText + "${");
      break;
    case 18 /* TemplateTail */:
      rawTextScanner.setText("}" + rawText + "`");
      break;
  }
  let token ***REMOVED*** rawTextScanner.scan();
  if (token ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */) {
    token ***REMOVED*** rawTextScanner.reScanTemplateToken(
      /*isTaggedTemplate*/
      false
    );
  }
  if (rawTextScanner.isUnterminated()) {
    rawTextScanner.setText(void 0);
    return invalidValueSentinel;
  }
  let tokenValue;
  switch (token) {
    case 15 /* NoSubstitutionTemplateLiteral */:
    case 16 /* TemplateHead */:
    case 17 /* TemplateMiddle */:
    case 18 /* TemplateTail */:
      tokenValue ***REMOVED*** rawTextScanner.getTokenValue();
      break;
  }
  if (tokenValue ***REMOVED******REMOVED******REMOVED*** void 0 || rawTextScanner.scan() !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
    rawTextScanner.setText(void 0);
    return invalidValueSentinel;
  }
  rawTextScanner.setText(void 0);
  return tokenValue;
}
function propagateNameFlags(node) {
  return node && isIdentifier(node) ? propagateIdentifierNameFlags(node) : propagateChildFlags(node);
}
function propagateIdentifierNameFlags(node) {
  return propagateChildFlags(node) & ~67108864 /* ContainsPossibleTopLevelAwait */;
}
function propagatePropertyNameFlagsOfChild(node, transformFlags) {
  return transformFlags | node.transformFlags & 134234112 /* PropertyNamePropagatingFlags */;
}
function propagateChildFlags(child) {
  if (!child) return 0 /* None */;
  const childFlags ***REMOVED*** child.transformFlags & ~getTransformFlagsSubtreeExclusions(child.kind);
  return isNamedDeclaration(child) && isPropertyName(child.name) ? propagatePropertyNameFlagsOfChild(child.name, childFlags) : childFlags;
}
function propagateChildrenFlags(children) {
  return children ? children.transformFlags : 0 /* None */;
}
function aggregateChildrenFlags(children) {
  let subtreeFlags ***REMOVED*** 0 /* None */;
  for (const child of children) {
    subtreeFlags |***REMOVED*** propagateChildFlags(child);
  }
  children.transformFlags ***REMOVED*** subtreeFlags;
}
function getTransformFlagsSubtreeExclusions(kind) {
  if (kind >***REMOVED*** 183 /* FirstTypeNode */ && kind <***REMOVED*** 206 /* LastTypeNode */) {
    return -2 /* TypeExcludes */;
  }
  switch (kind) {
    case 214 /* CallExpression */:
    case 215 /* NewExpression */:
    case 210 /* ArrayLiteralExpression */:
      return -2147450880 /* ArrayLiteralOrCallOrNewExcludes */;
    case 268 /* ModuleDeclaration */:
      return -1941676032 /* ModuleExcludes */;
    case 170 /* Parameter */:
      return -2147483648 /* ParameterExcludes */;
    case 220 /* ArrowFunction */:
      return -2072174592 /* ArrowFunctionExcludes */;
    case 219 /* FunctionExpression */:
    case 263 /* FunctionDeclaration */:
      return -1937940480 /* FunctionExcludes */;
    case 262 /* VariableDeclarationList */:
      return -2146893824 /* VariableDeclarationListExcludes */;
    case 264 /* ClassDeclaration */:
    case 232 /* ClassExpression */:
      return -2147344384 /* ClassExcludes */;
    case 177 /* Constructor */:
      return -1937948672 /* ConstructorExcludes */;
    case 173 /* PropertyDeclaration */:
      return -2013249536 /* PropertyExcludes */;
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
      return -2005057536 /* MethodOrAccessorExcludes */;
    case 133 /* AnyKeyword */:
    case 150 /* NumberKeyword */:
    case 163 /* BigIntKeyword */:
    case 146 /* NeverKeyword */:
    case 154 /* StringKeyword */:
    case 151 /* ObjectKeyword */:
    case 136 /* BooleanKeyword */:
    case 155 /* SymbolKeyword */:
    case 116 /* VoidKeyword */:
    case 169 /* TypeParameter */:
    case 172 /* PropertySignature */:
    case 174 /* MethodSignature */:
    case 180 /* CallSignature */:
    case 181 /* ConstructSignature */:
    case 182 /* IndexSignature */:
    case 265 /* InterfaceDeclaration */:
    case 266 /* TypeAliasDeclaration */:
      return -2 /* TypeExcludes */;
    case 211 /* ObjectLiteralExpression */:
      return -2147278848 /* ObjectLiteralExcludes */;
    case 300 /* CatchClause */:
      return -2147418112 /* CatchClauseExcludes */;
    case 207 /* ObjectBindingPattern */:
    case 208 /* ArrayBindingPattern */:
      return -2147450880 /* BindingPatternExcludes */;
    case 217 /* TypeAssertionExpression */:
    case 239 /* SatisfiesExpression */:
    case 235 /* AsExpression */:
    case 356 /* PartiallyEmittedExpression */:
    case 218 /* ParenthesizedExpression */:
    case 108 /* SuperKeyword */:
      return -2147483648 /* OuterExpressionExcludes */;
    case 212 /* PropertyAccessExpression */:
    case 213 /* ElementAccessExpression */:
      return -2147483648 /* PropertyAccessExcludes */;
    default:
      return -2147483648 /* NodeExcludes */;
  }
}
var baseFactory ***REMOVED*** createBaseNodeFactory();
function makeSynthetic(node) {
  node.flags |***REMOVED*** 16 /* Synthesized */;
  return node;
}
var syntheticFactory ***REMOVED*** {
  createBaseSourceFileNode: (kind) ***REMOVED***> makeSynthetic(baseFactory.createBaseSourceFileNode(kind)),
  createBaseIdentifierNode: (kind) ***REMOVED***> makeSynthetic(baseFactory.createBaseIdentifierNode(kind)),
  createBasePrivateIdentifierNode: (kind) ***REMOVED***> makeSynthetic(baseFactory.createBasePrivateIdentifierNode(kind)),
  createBaseTokenNode: (kind) ***REMOVED***> makeSynthetic(baseFactory.createBaseTokenNode(kind)),
  createBaseNode: (kind) ***REMOVED***> makeSynthetic(baseFactory.createBaseNode(kind))
};
var factory ***REMOVED*** createNodeFactory(4 /* NoIndentationOnFreshPropertyAccess */, syntheticFactory);
function setOriginalNode(node, original) {
  if (node.original !***REMOVED******REMOVED*** original) {
    node.original ***REMOVED*** original;
    if (original) {
      const emitNode ***REMOVED*** original.emitNode;
      if (emitNode) node.emitNode ***REMOVED*** mergeEmitNode(emitNode, node.emitNode);
    }
  }
  return node;
}
function mergeEmitNode(sourceEmitNode, destEmitNode) {
  const {
    flags,
    internalFlags,
    leadingComments,
    trailingComments,
    commentRange,
    sourceMapRange,
    tokenSourceMapRanges,
    constantValue,
    helpers,
    startsOnNewLine,
    snippetElement,
    classThis,
    assignedName
  } ***REMOVED*** sourceEmitNode;
  if (!destEmitNode) destEmitNode ***REMOVED*** {};
  if (flags) {
    destEmitNode.flags ***REMOVED*** flags;
  }
  if (internalFlags) {
    destEmitNode.internalFlags ***REMOVED*** internalFlags & ~8 /* Immutable */;
  }
  if (leadingComments) {
    destEmitNode.leadingComments ***REMOVED*** addRange(leadingComments.slice(), destEmitNode.leadingComments);
  }
  if (trailingComments) {
    destEmitNode.trailingComments ***REMOVED*** addRange(trailingComments.slice(), destEmitNode.trailingComments);
  }
  if (commentRange) {
    destEmitNode.commentRange ***REMOVED*** commentRange;
  }
  if (sourceMapRange) {
    destEmitNode.sourceMapRange ***REMOVED*** sourceMapRange;
  }
  if (tokenSourceMapRanges) {
    destEmitNode.tokenSourceMapRanges ***REMOVED*** mergeTokenSourceMapRanges(tokenSourceMapRanges, destEmitNode.tokenSourceMapRanges);
  }
  if (constantValue !***REMOVED******REMOVED*** void 0) {
    destEmitNode.constantValue ***REMOVED*** constantValue;
  }
  if (helpers) {
    for (const helper of helpers) {
      destEmitNode.helpers ***REMOVED*** appendIfUnique(destEmitNode.helpers, helper);
    }
  }
  if (startsOnNewLine !***REMOVED******REMOVED*** void 0) {
    destEmitNode.startsOnNewLine ***REMOVED*** startsOnNewLine;
  }
  if (snippetElement !***REMOVED******REMOVED*** void 0) {
    destEmitNode.snippetElement ***REMOVED*** snippetElement;
  }
  if (classThis) {
    destEmitNode.classThis ***REMOVED*** classThis;
  }
  if (assignedName) {
    destEmitNode.assignedName ***REMOVED*** assignedName;
  }
  return destEmitNode;
}
function mergeTokenSourceMapRanges(sourceRanges, destRanges) {
  if (!destRanges) destRanges ***REMOVED*** [];
  for (const key in sourceRanges) {
    destRanges[key] ***REMOVED*** sourceRanges[key];
  }
  return destRanges;
}

// src/compiler/factory/emitNode.ts
function getOrCreateEmitNode(node) {
  if (!node.emitNode) {
    if (isParseTreeNode(node)) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
        return node.emitNode ***REMOVED*** { annotatedNodes: [node] };
      }
      const sourceFile ***REMOVED*** getSourceFileOfNode(getParseTreeNode(getSourceFileOfNode(node))) ?? Debug.fail("Could not determine parsed source file.");
      getOrCreateEmitNode(sourceFile).annotatedNodes.push(node);
    }
    node.emitNode ***REMOVED*** {};
  } else {
    Debug.assert(!(node.emitNode.internalFlags & 8 /* Immutable */), "Invalid attempt to mutate an immutable node.");
  }
  return node.emitNode;
}
function disposeEmitNodes(sourceFile) {
  var _a, _b;
  const annotatedNodes ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** getSourceFileOfNode(getParseTreeNode(sourceFile))) ***REMOVED******REMOVED*** null ? void 0 : _a.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _b.annotatedNodes;
  if (annotatedNodes) {
    for (const node of annotatedNodes) {
      node.emitNode ***REMOVED*** void 0;
    }
  }
}
function removeAllComments(node) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.flags |***REMOVED*** 3072 /* NoComments */;
  emitNode.leadingComments ***REMOVED*** void 0;
  emitNode.trailingComments ***REMOVED*** void 0;
  return node;
}
function setEmitFlags(node, emitFlags) {
  getOrCreateEmitNode(node).flags ***REMOVED*** emitFlags;
  return node;
}
function addEmitFlags(node, emitFlags) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.flags ***REMOVED*** emitNode.flags | emitFlags;
  return node;
}
function setInternalEmitFlags(node, emitFlags) {
  getOrCreateEmitNode(node).internalFlags ***REMOVED*** emitFlags;
  return node;
}
function addInternalEmitFlags(node, emitFlags) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.internalFlags ***REMOVED*** emitNode.internalFlags | emitFlags;
  return node;
}
function getSourceMapRange(node) {
  var _a;
  return ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.sourceMapRange) ?? node;
}
function setSourceMapRange(node, range) {
  getOrCreateEmitNode(node).sourceMapRange ***REMOVED*** range;
  return node;
}
function setTokenSourceMapRange(node, token, range) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  const tokenSourceMapRanges ***REMOVED*** emitNode.tokenSourceMapRanges ?? (emitNode.tokenSourceMapRanges ***REMOVED*** []);
  tokenSourceMapRanges[token] ***REMOVED*** range;
  return node;
}
function getStartsOnNewLine(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.startsOnNewLine;
}
function setStartsOnNewLine(node, newLine) {
  getOrCreateEmitNode(node).startsOnNewLine ***REMOVED*** newLine;
  return node;
}
function getCommentRange(node) {
  var _a;
  return ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.commentRange) ?? node;
}
function setCommentRange(node, range) {
  getOrCreateEmitNode(node).commentRange ***REMOVED*** range;
  return node;
}
function getSyntheticLeadingComments(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.leadingComments;
}
function setSyntheticLeadingComments(node, comments) {
  getOrCreateEmitNode(node).leadingComments ***REMOVED*** comments;
  return node;
}
function addSyntheticLeadingComment(node, kind, text, hasTrailingNewLine) {
  return setSyntheticLeadingComments(node, append(getSyntheticLeadingComments(node), { kind, pos: -1, end: -1, hasTrailingNewLine, text }));
}
function getSyntheticTrailingComments(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.trailingComments;
}
function setSyntheticTrailingComments(node, comments) {
  getOrCreateEmitNode(node).trailingComments ***REMOVED*** comments;
  return node;
}
function addSyntheticTrailingComment(node, kind, text, hasTrailingNewLine) {
  return setSyntheticTrailingComments(node, append(getSyntheticTrailingComments(node), { kind, pos: -1, end: -1, hasTrailingNewLine, text }));
}
function moveSyntheticComments(node, original) {
  setSyntheticLeadingComments(node, getSyntheticLeadingComments(original));
  setSyntheticTrailingComments(node, getSyntheticTrailingComments(original));
  const emit ***REMOVED*** getOrCreateEmitNode(original);
  emit.leadingComments ***REMOVED*** void 0;
  emit.trailingComments ***REMOVED*** void 0;
  return node;
}
function getConstantValue(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.constantValue;
}
function setConstantValue(node, value) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.constantValue ***REMOVED*** value;
  return node;
}
function addEmitHelper(node, helper) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.helpers ***REMOVED*** append(emitNode.helpers, helper);
  return node;
}
function addEmitHelpers(node, helpers) {
  if (some(helpers)) {
    const emitNode ***REMOVED*** getOrCreateEmitNode(node);
    for (const helper of helpers) {
      emitNode.helpers ***REMOVED*** appendIfUnique(emitNode.helpers, helper);
    }
  }
  return node;
}
function getEmitHelpers(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.helpers;
}
function moveEmitHelpers(source, target, predicate) {
  const sourceEmitNode ***REMOVED*** source.emitNode;
  const sourceEmitHelpers ***REMOVED*** sourceEmitNode && sourceEmitNode.helpers;
  if (!some(sourceEmitHelpers)) return;
  const targetEmitNode ***REMOVED*** getOrCreateEmitNode(target);
  let helpersRemoved ***REMOVED*** 0;
  for (let i ***REMOVED*** 0; i < sourceEmitHelpers.length; i++) {
    const helper ***REMOVED*** sourceEmitHelpers[i];
    if (predicate(helper)) {
      helpersRemoved++;
      targetEmitNode.helpers ***REMOVED*** appendIfUnique(targetEmitNode.helpers, helper);
    } else if (helpersRemoved > 0) {
      sourceEmitHelpers[i - helpersRemoved] ***REMOVED*** helper;
    }
  }
  if (helpersRemoved > 0) {
    sourceEmitHelpers.length -***REMOVED*** helpersRemoved;
  }
}
function getSnippetElement(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.snippetElement;
}
function setTypeNode(node, type) {
  const emitNode ***REMOVED*** getOrCreateEmitNode(node);
  emitNode.typeNode ***REMOVED*** type;
  return node;
}
function getTypeNode(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.typeNode;
}
function setIdentifierTypeArguments(node, typeArguments) {
  getOrCreateEmitNode(node).identifierTypeArguments ***REMOVED*** typeArguments;
  return node;
}
function getIdentifierTypeArguments(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.identifierTypeArguments;
}
function setIdentifierAutoGenerate(node, autoGenerate) {
  getOrCreateEmitNode(node).autoGenerate ***REMOVED*** autoGenerate;
  return node;
}
function setIdentifierGeneratedImportReference(node, value) {
  getOrCreateEmitNode(node).generatedImportReference ***REMOVED*** value;
  return node;
}
function getIdentifierGeneratedImportReference(node) {
  var _a;
  return (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.generatedImportReference;
}

// src/compiler/factory/emitHelpers.ts
function createEmitHelperFactory(context) {
  const factory2 ***REMOVED*** context.factory;
  const immutableTrue ***REMOVED*** memoize(() ***REMOVED***> setInternalEmitFlags(factory2.createTrue(), 8 /* Immutable */));
  const immutableFalse ***REMOVED*** memoize(() ***REMOVED***> setInternalEmitFlags(factory2.createFalse(), 8 /* Immutable */));
  return {
    getUnscopedHelperName,
    // TypeScript Helpers
    createDecorateHelper,
    createMetadataHelper,
    createParamHelper,
    // ES Decorators Helpers
    createESDecorateHelper,
    createRunInitializersHelper,
    // ES2018 Helpers
    createAssignHelper,
    createAwaitHelper,
    createAsyncGeneratorHelper,
    createAsyncDelegatorHelper,
    createAsyncValuesHelper,
    // ES2018 Destructuring Helpers
    createRestHelper,
    // ES2017 Helpers
    createAwaiterHelper,
    // ES2015 Helpers
    createExtendsHelper,
    createTemplateObjectHelper,
    createSpreadArrayHelper,
    createPropKeyHelper,
    createSetFunctionNameHelper,
    // ES2015 Destructuring Helpers
    createValuesHelper,
    createReadHelper,
    // ES2015 Generator Helpers
    createGeneratorHelper,
    // ES Module Helpers
    createImportStarHelper,
    createImportStarCallbackHelper,
    createImportDefaultHelper,
    createExportStarHelper,
    // Class Fields Helpers
    createClassPrivateFieldGetHelper,
    createClassPrivateFieldSetHelper,
    createClassPrivateFieldInHelper,
    // 'using' helpers
    createAddDisposableResourceHelper,
    createDisposeResourcesHelper,
    // --rewriteRelativeImportExtensions helpers
    createRewriteRelativeImportExtensionsHelper
  };
  function getUnscopedHelperName(name) {
    return setEmitFlags(factory2.createIdentifier(name), 8192 /* HelperName */ | 4 /* AdviseOnEmitNode */);
  }
  function createDecorateHelper(decoratorExpressions, target, memberName, descriptor) {
    context.requestEmitHelper(decorateHelper);
    const argumentsArray ***REMOVED*** [];
    argumentsArray.push(factory2.createArrayLiteralExpression(
      decoratorExpressions,
      /*multiLine*/
      true
    ));
    argumentsArray.push(target);
    if (memberName) {
      argumentsArray.push(memberName);
      if (descriptor) {
        argumentsArray.push(descriptor);
      }
    }
    return factory2.createCallExpression(
      getUnscopedHelperName("__decorate"),
      /*typeArguments*/
      void 0,
      argumentsArray
    );
  }
  function createMetadataHelper(metadataKey, metadataValue) {
    context.requestEmitHelper(metadataHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__metadata"),
      /*typeArguments*/
      void 0,
      [
        factory2.createStringLiteral(metadataKey),
        metadataValue
      ]
    );
  }
  function createParamHelper(expression, parameterOffset, location) {
    context.requestEmitHelper(paramHelper);
    return setTextRange(
      factory2.createCallExpression(
        getUnscopedHelperName("__param"),
        /*typeArguments*/
        void 0,
        [
          factory2.createNumericLiteral(parameterOffset + ""),
          expression
        ]
      ),
      location
    );
  }
  function createESDecorateClassContextObject(contextIn) {
    const properties ***REMOVED*** [
      factory2.createPropertyAssignment(factory2.createIdentifier("kind"), factory2.createStringLiteral("class")),
      factory2.createPropertyAssignment(factory2.createIdentifier("name"), contextIn.name),
      factory2.createPropertyAssignment(factory2.createIdentifier("metadata"), contextIn.metadata)
    ];
    return factory2.createObjectLiteralExpression(properties);
  }
  function createESDecorateClassElementAccessGetMethod(elementName) {
    const accessor ***REMOVED*** elementName.computed ? factory2.createElementAccessExpression(factory2.createIdentifier("obj"), elementName.name) : factory2.createPropertyAccessExpression(factory2.createIdentifier("obj"), elementName.name);
    return factory2.createPropertyAssignment(
      "get",
      factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        [factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          factory2.createIdentifier("obj")
        )],
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        accessor
      )
    );
  }
  function createESDecorateClassElementAccessSetMethod(elementName) {
    const accessor ***REMOVED*** elementName.computed ? factory2.createElementAccessExpression(factory2.createIdentifier("obj"), elementName.name) : factory2.createPropertyAccessExpression(factory2.createIdentifier("obj"), elementName.name);
    return factory2.createPropertyAssignment(
      "set",
      factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        [
          factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            factory2.createIdentifier("obj")
          ),
          factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            factory2.createIdentifier("value")
          )
        ],
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        factory2.createBlock([
          factory2.createExpressionStatement(
            factory2.createAssignment(
              accessor,
              factory2.createIdentifier("value")
            )
          )
        ])
      )
    );
  }
  function createESDecorateClassElementAccessHasMethod(elementName) {
    const propertyName ***REMOVED*** elementName.computed ? elementName.name : isIdentifier(elementName.name) ? factory2.createStringLiteralFromNode(elementName.name) : elementName.name;
    return factory2.createPropertyAssignment(
      "has",
      factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        [factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          factory2.createIdentifier("obj")
        )],
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        factory2.createBinaryExpression(
          propertyName,
          103 /* InKeyword */,
          factory2.createIdentifier("obj")
        )
      )
    );
  }
  function createESDecorateClassElementAccessObject(name, access) {
    const properties ***REMOVED*** [];
    properties.push(createESDecorateClassElementAccessHasMethod(name));
    if (access.get) properties.push(createESDecorateClassElementAccessGetMethod(name));
    if (access.set) properties.push(createESDecorateClassElementAccessSetMethod(name));
    return factory2.createObjectLiteralExpression(properties);
  }
  function createESDecorateClassElementContextObject(contextIn) {
    const properties ***REMOVED*** [
      factory2.createPropertyAssignment(factory2.createIdentifier("kind"), factory2.createStringLiteral(contextIn.kind)),
      factory2.createPropertyAssignment(factory2.createIdentifier("name"), contextIn.name.computed ? contextIn.name.name : factory2.createStringLiteralFromNode(contextIn.name.name)),
      factory2.createPropertyAssignment(factory2.createIdentifier("static"), contextIn.static ? factory2.createTrue() : factory2.createFalse()),
      factory2.createPropertyAssignment(factory2.createIdentifier("private"), contextIn.private ? factory2.createTrue() : factory2.createFalse()),
      factory2.createPropertyAssignment(factory2.createIdentifier("access"), createESDecorateClassElementAccessObject(contextIn.name, contextIn.access)),
      factory2.createPropertyAssignment(factory2.createIdentifier("metadata"), contextIn.metadata)
    ];
    return factory2.createObjectLiteralExpression(properties);
  }
  function createESDecorateContextObject(contextIn) {
    return contextIn.kind ***REMOVED******REMOVED******REMOVED*** "class" ? createESDecorateClassContextObject(contextIn) : createESDecorateClassElementContextObject(contextIn);
  }
  function createESDecorateHelper(ctor, descriptorIn, decorators, contextIn, initializers, extraInitializers) {
    context.requestEmitHelper(esDecorateHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__esDecorate"),
      /*typeArguments*/
      void 0,
      [
        ctor ?? factory2.createNull(),
        descriptorIn ?? factory2.createNull(),
        decorators,
        createESDecorateContextObject(contextIn),
        initializers,
        extraInitializers
      ]
    );
  }
  function createRunInitializersHelper(thisArg, initializers, value) {
    context.requestEmitHelper(runInitializersHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__runInitializers"),
      /*typeArguments*/
      void 0,
      value ? [thisArg, initializers, value] : [thisArg, initializers]
    );
  }
  function createAssignHelper(attributesSegments) {
    if (getEmitScriptTarget(context.getCompilerOptions()) >***REMOVED*** 2 /* ES2015 */) {
      return factory2.createCallExpression(
        factory2.createPropertyAccessExpression(factory2.createIdentifier("Object"), "assign"),
        /*typeArguments*/
        void 0,
        attributesSegments
      );
    }
    context.requestEmitHelper(assignHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__assign"),
      /*typeArguments*/
      void 0,
      attributesSegments
    );
  }
  function createAwaitHelper(expression) {
    context.requestEmitHelper(awaitHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__await"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createAsyncGeneratorHelper(generatorFunc, hasLexicalThis) {
    context.requestEmitHelper(awaitHelper);
    context.requestEmitHelper(asyncGeneratorHelper);
    (generatorFunc.emitNode || (generatorFunc.emitNode ***REMOVED*** {})).flags |***REMOVED*** 524288 /* AsyncFunctionBody */ | 1048576 /* ReuseTempVariableScope */;
    return factory2.createCallExpression(
      getUnscopedHelperName("__asyncGenerator"),
      /*typeArguments*/
      void 0,
      [
        hasLexicalThis ? factory2.createThis() : factory2.createVoidZero(),
        factory2.createIdentifier("arguments"),
        generatorFunc
      ]
    );
  }
  function createAsyncDelegatorHelper(expression) {
    context.requestEmitHelper(awaitHelper);
    context.requestEmitHelper(asyncDelegator);
    return factory2.createCallExpression(
      getUnscopedHelperName("__asyncDelegator"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createAsyncValuesHelper(expression) {
    context.requestEmitHelper(asyncValues);
    return factory2.createCallExpression(
      getUnscopedHelperName("__asyncValues"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createRestHelper(value, elements, computedTempVariables, location) {
    context.requestEmitHelper(restHelper);
    const propertyNames ***REMOVED*** [];
    let computedTempVariableOffset ***REMOVED*** 0;
    for (let i ***REMOVED*** 0; i < elements.length - 1; i++) {
      const propertyName ***REMOVED*** getPropertyNameOfBindingOrAssignmentElement(elements[i]);
      if (propertyName) {
        if (isComputedPropertyName(propertyName)) {
          Debug.assertIsDefined(computedTempVariables, "Encountered computed property name but 'computedTempVariables' argument was not provided.");
          const temp ***REMOVED*** computedTempVariables[computedTempVariableOffset];
          computedTempVariableOffset++;
          propertyNames.push(
            factory2.createConditionalExpression(
              factory2.createTypeCheck(temp, "symbol"),
              /*questionToken*/
              void 0,
              temp,
              /*colonToken*/
              void 0,
              factory2.createAdd(temp, factory2.createStringLiteral(""))
            )
          );
        } else {
          propertyNames.push(factory2.createStringLiteralFromNode(propertyName));
        }
      }
    }
    return factory2.createCallExpression(
      getUnscopedHelperName("__rest"),
      /*typeArguments*/
      void 0,
      [
        value,
        setTextRange(
          factory2.createArrayLiteralExpression(propertyNames),
          location
        )
      ]
    );
  }
  function createAwaiterHelper(hasLexicalThis, argumentsExpression, promiseConstructor, parameters, body) {
    context.requestEmitHelper(awaiterHelper);
    const generatorFunc ***REMOVED*** factory2.createFunctionExpression(
      /*modifiers*/
      void 0,
      factory2.createToken(42 /* AsteriskToken */),
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      parameters ?? [],
      /*type*/
      void 0,
      body
    );
    (generatorFunc.emitNode || (generatorFunc.emitNode ***REMOVED*** {})).flags |***REMOVED*** 524288 /* AsyncFunctionBody */ | 1048576 /* ReuseTempVariableScope */;
    return factory2.createCallExpression(
      getUnscopedHelperName("__awaiter"),
      /*typeArguments*/
      void 0,
      [
        hasLexicalThis ? factory2.createThis() : factory2.createVoidZero(),
        argumentsExpression ?? factory2.createVoidZero(),
        promiseConstructor ? createExpressionFromEntityName(factory2, promiseConstructor) : factory2.createVoidZero(),
        generatorFunc
      ]
    );
  }
  function createExtendsHelper(name) {
    context.requestEmitHelper(extendsHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__extends"),
      /*typeArguments*/
      void 0,
      [name, factory2.createUniqueName("_super", 16 /* Optimistic */ | 32 /* FileLevel */)]
    );
  }
  function createTemplateObjectHelper(cooked, raw) {
    context.requestEmitHelper(templateObjectHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__makeTemplateObject"),
      /*typeArguments*/
      void 0,
      [cooked, raw]
    );
  }
  function createSpreadArrayHelper(to, from, packFrom) {
    context.requestEmitHelper(spreadArrayHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__spreadArray"),
      /*typeArguments*/
      void 0,
      [to, from, packFrom ? immutableTrue() : immutableFalse()]
    );
  }
  function createPropKeyHelper(expr) {
    context.requestEmitHelper(propKeyHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__propKey"),
      /*typeArguments*/
      void 0,
      [expr]
    );
  }
  function createSetFunctionNameHelper(f, name, prefix) {
    context.requestEmitHelper(setFunctionNameHelper);
    return context.factory.createCallExpression(
      getUnscopedHelperName("__setFunctionName"),
      /*typeArguments*/
      void 0,
      prefix ? [f, name, context.factory.createStringLiteral(prefix)] : [f, name]
    );
  }
  function createValuesHelper(expression) {
    context.requestEmitHelper(valuesHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__values"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createReadHelper(iteratorRecord, count) {
    context.requestEmitHelper(readHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__read"),
      /*typeArguments*/
      void 0,
      count !***REMOVED******REMOVED*** void 0 ? [iteratorRecord, factory2.createNumericLiteral(count + "")] : [iteratorRecord]
    );
  }
  function createGeneratorHelper(body) {
    context.requestEmitHelper(generatorHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__generator"),
      /*typeArguments*/
      void 0,
      [factory2.createThis(), body]
    );
  }
  function createImportStarHelper(expression) {
    context.requestEmitHelper(importStarHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__importStar"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createImportStarCallbackHelper() {
    context.requestEmitHelper(importStarHelper);
    return getUnscopedHelperName("__importStar");
  }
  function createImportDefaultHelper(expression) {
    context.requestEmitHelper(importDefaultHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__importDefault"),
      /*typeArguments*/
      void 0,
      [expression]
    );
  }
  function createExportStarHelper(moduleExpression, exportsExpression ***REMOVED*** factory2.createIdentifier("exports")) {
    context.requestEmitHelper(exportStarHelper);
    context.requestEmitHelper(createBindingHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__exportStar"),
      /*typeArguments*/
      void 0,
      [moduleExpression, exportsExpression]
    );
  }
  function createClassPrivateFieldGetHelper(receiver, state, kind, f) {
    context.requestEmitHelper(classPrivateFieldGetHelper);
    let args;
    if (!f) {
      args ***REMOVED*** [receiver, state, factory2.createStringLiteral(kind)];
    } else {
      args ***REMOVED*** [receiver, state, factory2.createStringLiteral(kind), f];
    }
    return factory2.createCallExpression(
      getUnscopedHelperName("__classPrivateFieldGet"),
      /*typeArguments*/
      void 0,
      args
    );
  }
  function createClassPrivateFieldSetHelper(receiver, state, value, kind, f) {
    context.requestEmitHelper(classPrivateFieldSetHelper);
    let args;
    if (!f) {
      args ***REMOVED*** [receiver, state, value, factory2.createStringLiteral(kind)];
    } else {
      args ***REMOVED*** [receiver, state, value, factory2.createStringLiteral(kind), f];
    }
    return factory2.createCallExpression(
      getUnscopedHelperName("__classPrivateFieldSet"),
      /*typeArguments*/
      void 0,
      args
    );
  }
  function createClassPrivateFieldInHelper(state, receiver) {
    context.requestEmitHelper(classPrivateFieldInHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__classPrivateFieldIn"),
      /*typeArguments*/
      void 0,
      [state, receiver]
    );
  }
  function createAddDisposableResourceHelper(envBinding, value, async) {
    context.requestEmitHelper(addDisposableResourceHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__addDisposableResource"),
      /*typeArguments*/
      void 0,
      [envBinding, value, async ? factory2.createTrue() : factory2.createFalse()]
    );
  }
  function createDisposeResourcesHelper(envBinding) {
    context.requestEmitHelper(disposeResourcesHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__disposeResources"),
      /*typeArguments*/
      void 0,
      [envBinding]
    );
  }
  function createRewriteRelativeImportExtensionsHelper(expression) {
    context.requestEmitHelper(rewriteRelativeImportExtensionsHelper);
    return factory2.createCallExpression(
      getUnscopedHelperName("__rewriteRelativeImportExtension"),
      /*typeArguments*/
      void 0,
      context.getCompilerOptions().jsx ***REMOVED******REMOVED******REMOVED*** 1 /* Preserve */ ? [expression, factory2.createTrue()] : [expression]
    );
  }
}
function compareEmitHelpers(x, y) {
  if (x ***REMOVED******REMOVED******REMOVED*** y) return 0 /* EqualTo */;
  if (x.priority ***REMOVED******REMOVED******REMOVED*** y.priority) return 0 /* EqualTo */;
  if (x.priority ***REMOVED******REMOVED******REMOVED*** void 0) return 1 /* GreaterThan */;
  if (y.priority ***REMOVED******REMOVED******REMOVED*** void 0) return -1 /* LessThan */;
  return compareValues(x.priority, y.priority);
}
function helperString(input, ...args) {
  return (uniqueName) ***REMOVED***> {
    let result ***REMOVED*** "";
    for (let i ***REMOVED*** 0; i < args.length; i++) {
      result +***REMOVED*** input[i];
      result +***REMOVED*** uniqueName(args[i]);
    }
    result +***REMOVED*** input[input.length - 1];
    return result;
  };
}
var decorateHelper ***REMOVED*** {
  name: "typescript:decorate",
  importName: "__decorate",
  scoped: false,
  priority: 2,
  text: `
            var __decorate ***REMOVED*** (this && this.__decorate) || function (decorators, target, key, desc) {
                var c ***REMOVED*** arguments.length, r ***REMOVED*** c < 3 ? target : desc ***REMOVED******REMOVED******REMOVED*** null ? desc ***REMOVED*** Object.getOwnPropertyDescriptor(target, key) : desc, d;
                if (typeof Reflect ***REMOVED******REMOVED******REMOVED*** "object" && typeof Reflect.decorate ***REMOVED******REMOVED******REMOVED*** "function") r ***REMOVED*** Reflect.decorate(decorators, target, key, desc);
                else for (var i ***REMOVED*** decorators.length - 1; i >***REMOVED*** 0; i--) if (d ***REMOVED*** decorators[i]) r ***REMOVED*** (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
                return c > 3 && r && Object.defineProperty(target, key, r), r;
            };`
};
var metadataHelper ***REMOVED*** {
  name: "typescript:metadata",
  importName: "__metadata",
  scoped: false,
  priority: 3,
  text: `
            var __metadata ***REMOVED*** (this && this.__metadata) || function (k, v) {
                if (typeof Reflect ***REMOVED******REMOVED******REMOVED*** "object" && typeof Reflect.metadata ***REMOVED******REMOVED******REMOVED*** "function") return Reflect.metadata(k, v);
            };`
};
var paramHelper ***REMOVED*** {
  name: "typescript:param",
  importName: "__param",
  scoped: false,
  priority: 4,
  text: `
            var __param ***REMOVED*** (this && this.__param) || function (paramIndex, decorator) {
                return function (target, key) { decorator(target, key, paramIndex); }
            };`
};
var esDecorateHelper ***REMOVED*** {
  name: "typescript:esDecorate",
  importName: "__esDecorate",
  scoped: false,
  priority: 2,
  text: `
        var __esDecorate ***REMOVED*** (this && this.__esDecorate) || function (ctor, descriptorIn, decorators, contextIn, initializers, extraInitializers) {
            function accept(f) { if (f !***REMOVED******REMOVED*** void 0 && typeof f !***REMOVED******REMOVED*** "function") throw new TypeError("Function expected"); return f; }
            var kind ***REMOVED*** contextIn.kind, key ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** "getter" ? "get" : kind ***REMOVED******REMOVED******REMOVED*** "setter" ? "set" : "value";
            var target ***REMOVED*** !descriptorIn && ctor ? contextIn["static"] ? ctor : ctor.prototype : null;
            var descriptor ***REMOVED*** descriptorIn || (target ? Object.getOwnPropertyDescriptor(target, contextIn.name) : {});
            var _, done ***REMOVED*** false;
            for (var i ***REMOVED*** decorators.length - 1; i >***REMOVED*** 0; i--) {
                var context ***REMOVED*** {};
                for (var p in contextIn) context[p] ***REMOVED*** p ***REMOVED******REMOVED******REMOVED*** "access" ? {} : contextIn[p];
                for (var p in contextIn.access) context.access[p] ***REMOVED*** contextIn.access[p];
                context.addInitializer ***REMOVED*** function (f) { if (done) throw new TypeError("Cannot add initializers after decoration has completed"); extraInitializers.push(accept(f || null)); };
                var result ***REMOVED*** (0, decorators[i])(kind ***REMOVED******REMOVED******REMOVED*** "accessor" ? { get: descriptor.get, set: descriptor.set } : descriptor[key], context);
                if (kind ***REMOVED******REMOVED******REMOVED*** "accessor") {
                    if (result ***REMOVED******REMOVED******REMOVED*** void 0) continue;
                    if (result ***REMOVED******REMOVED******REMOVED*** null || typeof result !***REMOVED******REMOVED*** "object") throw new TypeError("Object expected");
                    if (_ ***REMOVED*** accept(result.get)) descriptor.get ***REMOVED*** _;
                    if (_ ***REMOVED*** accept(result.set)) descriptor.set ***REMOVED*** _;
                    if (_ ***REMOVED*** accept(result.init)) initializers.unshift(_);
                }
                else if (_ ***REMOVED*** accept(result)) {
                    if (kind ***REMOVED******REMOVED******REMOVED*** "field") initializers.unshift(_);
                    else descriptor[key] ***REMOVED*** _;
                }
            }
            if (target) Object.defineProperty(target, contextIn.name, descriptor);
            done ***REMOVED*** true;
        };`
};
var runInitializersHelper ***REMOVED*** {
  name: "typescript:runInitializers",
  importName: "__runInitializers",
  scoped: false,
  priority: 2,
  text: `
        var __runInitializers ***REMOVED*** (this && this.__runInitializers) || function (thisArg, initializers, value) {
            var useValue ***REMOVED*** arguments.length > 2;
            for (var i ***REMOVED*** 0; i < initializers.length; i++) {
                value ***REMOVED*** useValue ? initializers[i].call(thisArg, value) : initializers[i].call(thisArg);
            }
            return useValue ? value : void 0;
        };`
};
var assignHelper ***REMOVED*** {
  name: "typescript:assign",
  importName: "__assign",
  scoped: false,
  priority: 1,
  text: `
            var __assign ***REMOVED*** (this && this.__assign) || function () {
                __assign ***REMOVED*** Object.assign || function(t) {
                    for (var s, i ***REMOVED*** 1, n ***REMOVED*** arguments.length; i < n; i++) {
                        s ***REMOVED*** arguments[i];
                        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                            t[p] ***REMOVED*** s[p];
                    }
                    return t;
                };
                return __assign.apply(this, arguments);
            };`
};
var awaitHelper ***REMOVED*** {
  name: "typescript:await",
  importName: "__await",
  scoped: false,
  text: `
            var __await ***REMOVED*** (this && this.__await) || function (v) { return this instanceof __await ? (this.v ***REMOVED*** v, this) : new __await(v); }`
};
var asyncGeneratorHelper ***REMOVED*** {
  name: "typescript:asyncGenerator",
  importName: "__asyncGenerator",
  scoped: false,
  dependencies: [awaitHelper],
  text: `
        var __asyncGenerator ***REMOVED*** (this && this.__asyncGenerator) || function (thisArg, _arguments, generator) {
            if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
            var g ***REMOVED*** generator.apply(thisArg, _arguments || []), i, q ***REMOVED*** [];
            return i ***REMOVED*** Object.create((typeof AsyncIterator ***REMOVED******REMOVED******REMOVED*** "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i[Symbol.asyncIterator] ***REMOVED*** function () { return this; }, i;
            function awaitReturn(f) { return function (v) { return Promise.resolve(v).then(f, reject); }; }
            function verb(n, f) { if (g[n]) { i[n] ***REMOVED*** function (v) { return new Promise(function (a, b) { q.push([n, v, a, b]) > 1 || resume(n, v); }); }; if (f) i[n] ***REMOVED*** f(i[n]); } }
            function resume(n, v) { try { step(g[n](v)); } catch (e) { settle(q[0][3], e); } }
            function step(r) { r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r); }
            function fulfill(value) { resume("next", value); }
            function reject(value) { resume("throw", value); }
            function settle(f, v) { if (f(v), q.shift(), q.length) resume(q[0][0], q[0][1]); }
        };`
};
var asyncDelegator ***REMOVED*** {
  name: "typescript:asyncDelegator",
  importName: "__asyncDelegator",
  scoped: false,
  dependencies: [awaitHelper],
  text: `
            var __asyncDelegator ***REMOVED*** (this && this.__asyncDelegator) || function (o) {
                var i, p;
                return i ***REMOVED*** {}, verb("next"), verb("throw", function (e) { throw e; }), verb("return"), i[Symbol.iterator] ***REMOVED*** function () { return this; }, i;
                function verb(n, f) { i[n] ***REMOVED*** o[n] ? function (v) { return (p ***REMOVED*** !p) ? { value: __await(o[n](v)), done: false } : f ? f(v) : v; } : f; }
            };`
};
var asyncValues ***REMOVED*** {
  name: "typescript:asyncValues",
  importName: "__asyncValues",
  scoped: false,
  text: `
            var __asyncValues ***REMOVED*** (this && this.__asyncValues) || function (o) {
                if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
                var m ***REMOVED*** o[Symbol.asyncIterator], i;
                return m ? m.call(o) : (o ***REMOVED*** typeof __values ***REMOVED******REMOVED******REMOVED*** "function" ? __values(o) : o[Symbol.iterator](), i ***REMOVED*** {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] ***REMOVED*** function () { return this; }, i);
                function verb(n) { i[n] ***REMOVED*** o[n] && function (v) { return new Promise(function (resolve, reject) { v ***REMOVED*** o[n](v), settle(resolve, reject, v.done, v.value); }); }; }
                function settle(resolve, reject, d, v) { Promise.resolve(v).then(function(v) { resolve({ value: v, done: d }); }, reject); }
            };`
};
var restHelper ***REMOVED*** {
  name: "typescript:rest",
  importName: "__rest",
  scoped: false,
  text: `
            var __rest ***REMOVED*** (this && this.__rest) || function (s, e) {
                var t ***REMOVED*** {};
                for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
                    t[p] ***REMOVED*** s[p];
                if (s !***REMOVED*** null && typeof Object.getOwnPropertySymbols ***REMOVED******REMOVED******REMOVED*** "function")
                    for (var i ***REMOVED*** 0, p ***REMOVED*** Object.getOwnPropertySymbols(s); i < p.length; i++) {
                        if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                            t[p[i]] ***REMOVED*** s[p[i]];
                    }
                return t;
            };`
};
var awaiterHelper ***REMOVED*** {
  name: "typescript:awaiter",
  importName: "__awaiter",
  scoped: false,
  priority: 5,
  text: `
            var __awaiter ***REMOVED*** (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
                function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
                return new (P || (P ***REMOVED*** Promise))(function (resolve, reject) {
                    function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
                    function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
                    function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
                    step((generator ***REMOVED*** generator.apply(thisArg, _arguments || [])).next());
                });
            };`
};
var extendsHelper ***REMOVED*** {
  name: "typescript:extends",
  importName: "__extends",
  scoped: false,
  priority: 0,
  text: `
            var __extends ***REMOVED*** (this && this.__extends) || (function () {
                var extendStatics ***REMOVED*** function (d, b) {
                    extendStatics ***REMOVED*** Object.setPrototypeOf ||
                        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ ***REMOVED*** b; }) ||
                        function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] ***REMOVED*** b[p]; };
                    return extendStatics(d, b);
                };

                return function (d, b) {
                    if (typeof b !***REMOVED******REMOVED*** "function" && b !***REMOVED******REMOVED*** null)
                        throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
                    extendStatics(d, b);
                    function __() { this.constructor ***REMOVED*** d; }
                    d.prototype ***REMOVED*** b ***REMOVED******REMOVED******REMOVED*** null ? Object.create(b) : (__.prototype ***REMOVED*** b.prototype, new __());
                };
            })();`
};
var templateObjectHelper ***REMOVED*** {
  name: "typescript:makeTemplateObject",
  importName: "__makeTemplateObject",
  scoped: false,
  priority: 0,
  text: `
            var __makeTemplateObject ***REMOVED*** (this && this.__makeTemplateObject) || function (cooked, raw) {
                if (Object.defineProperty) { Object.defineProperty(cooked, "raw", { value: raw }); } else { cooked.raw ***REMOVED*** raw; }
                return cooked;
            };`
};
var readHelper ***REMOVED*** {
  name: "typescript:read",
  importName: "__read",
  scoped: false,
  text: `
            var __read ***REMOVED*** (this && this.__read) || function (o, n) {
                var m ***REMOVED*** typeof Symbol ***REMOVED******REMOVED******REMOVED*** "function" && o[Symbol.iterator];
                if (!m) return o;
                var i ***REMOVED*** m.call(o), r, ar ***REMOVED*** [], e;
                try {
                    while ((n ***REMOVED******REMOVED******REMOVED*** void 0 || n-- > 0) && !(r ***REMOVED*** i.next()).done) ar.push(r.value);
                }
                catch (error) { e ***REMOVED*** { error: error }; }
                finally {
                    try {
                        if (r && !r.done && (m ***REMOVED*** i["return"])) m.call(i);
                    }
                    finally { if (e) throw e.error; }
                }
                return ar;
            };`
};
var spreadArrayHelper ***REMOVED*** {
  name: "typescript:spreadArray",
  importName: "__spreadArray",
  scoped: false,
  text: `
            var __spreadArray ***REMOVED*** (this && this.__spreadArray) || function (to, from, pack) {
                if (pack || arguments.length ***REMOVED******REMOVED******REMOVED*** 2) for (var i ***REMOVED*** 0, l ***REMOVED*** from.length, ar; i < l; i++) {
                    if (ar || !(i in from)) {
                        if (!ar) ar ***REMOVED*** Array.prototype.slice.call(from, 0, i);
                        ar[i] ***REMOVED*** from[i];
                    }
                }
                return to.concat(ar || Array.prototype.slice.call(from));
            };`
};
var propKeyHelper ***REMOVED*** {
  name: "typescript:propKey",
  importName: "__propKey",
  scoped: false,
  text: `
        var __propKey ***REMOVED*** (this && this.__propKey) || function (x) {
            return typeof x ***REMOVED******REMOVED******REMOVED*** "symbol" ? x : "".concat(x);
        };`
};
var setFunctionNameHelper ***REMOVED*** {
  name: "typescript:setFunctionName",
  importName: "__setFunctionName",
  scoped: false,
  text: `
        var __setFunctionName ***REMOVED*** (this && this.__setFunctionName) || function (f, name, prefix) {
            if (typeof name ***REMOVED******REMOVED******REMOVED*** "symbol") name ***REMOVED*** name.description ? "[".concat(name.description, "]") : "";
            return Object.defineProperty(f, "name", { configurable: true, value: prefix ? "".concat(prefix, " ", name) : name });
        };`
};
var valuesHelper ***REMOVED*** {
  name: "typescript:values",
  importName: "__values",
  scoped: false,
  text: `
            var __values ***REMOVED*** (this && this.__values) || function(o) {
                var s ***REMOVED*** typeof Symbol ***REMOVED******REMOVED******REMOVED*** "function" && Symbol.iterator, m ***REMOVED*** s && o[s], i ***REMOVED*** 0;
                if (m) return m.call(o);
                if (o && typeof o.length ***REMOVED******REMOVED******REMOVED*** "number") return {
                    next: function () {
                        if (o && i >***REMOVED*** o.length) o ***REMOVED*** void 0;
                        return { value: o && o[i++], done: !o };
                    }
                };
                throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
            };`
};
var generatorHelper ***REMOVED*** {
  name: "typescript:generator",
  importName: "__generator",
  scoped: false,
  priority: 6,
  text: `
            var __generator ***REMOVED*** (this && this.__generator) || function (thisArg, body) {
                var _ ***REMOVED*** { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g ***REMOVED*** Object.create((typeof Iterator ***REMOVED******REMOVED******REMOVED*** "function" ? Iterator : Object).prototype);
                return g.next ***REMOVED*** verb(0), g["throw"] ***REMOVED*** verb(1), g["return"] ***REMOVED*** verb(2), typeof Symbol ***REMOVED******REMOVED******REMOVED*** "function" && (g[Symbol.iterator] ***REMOVED*** function() { return this; }), g;
                function verb(n) { return function (v) { return step([n, v]); }; }
                function step(op) {
                    if (f) throw new TypeError("Generator is already executing.");
                    while (g && (g ***REMOVED*** 0, op[0] && (_ ***REMOVED*** 0)), _) try {
                        if (f ***REMOVED*** 1, y && (t ***REMOVED*** op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t ***REMOVED*** y["return"]) && t.call(y), 0) : y.next) && !(t ***REMOVED*** t.call(y, op[1])).done) return t;
                        if (y ***REMOVED*** 0, t) op ***REMOVED*** [op[0] & 2, t.value];
                        switch (op[0]) {
                            case 0: case 1: t ***REMOVED*** op; break;
                            case 4: _.label++; return { value: op[1], done: false };
                            case 5: _.label++; y ***REMOVED*** op[1]; op ***REMOVED*** [0]; continue;
                            case 7: op ***REMOVED*** _.ops.pop(); _.trys.pop(); continue;
                            default:
                                if (!(t ***REMOVED*** _.trys, t ***REMOVED*** t.length > 0 && t[t.length - 1]) && (op[0] ***REMOVED******REMOVED******REMOVED*** 6 || op[0] ***REMOVED******REMOVED******REMOVED*** 2)) { _ ***REMOVED*** 0; continue; }
                                if (op[0] ***REMOVED******REMOVED******REMOVED*** 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label ***REMOVED*** op[1]; break; }
                                if (op[0] ***REMOVED******REMOVED******REMOVED*** 6 && _.label < t[1]) { _.label ***REMOVED*** t[1]; t ***REMOVED*** op; break; }
                                if (t && _.label < t[2]) { _.label ***REMOVED*** t[2]; _.ops.push(op); break; }
                                if (t[2]) _.ops.pop();
                                _.trys.pop(); continue;
                        }
                        op ***REMOVED*** body.call(thisArg, _);
                    } catch (e) { op ***REMOVED*** [6, e]; y ***REMOVED*** 0; } finally { f ***REMOVED*** t ***REMOVED*** 0; }
                    if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
                }
            };`
};
var createBindingHelper ***REMOVED*** {
  name: "typescript:commonjscreatebinding",
  importName: "__createBinding",
  scoped: false,
  priority: 1,
  text: `
            var __createBinding ***REMOVED*** (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
                if (k2 ***REMOVED******REMOVED******REMOVED*** undefined) k2 ***REMOVED*** k;
                var desc ***REMOVED*** Object.getOwnPropertyDescriptor(m, k);
                if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
                  desc ***REMOVED*** { enumerable: true, get: function() { return m[k]; } };
                }
                Object.defineProperty(o, k2, desc);
            }) : (function(o, m, k, k2) {
                if (k2 ***REMOVED******REMOVED******REMOVED*** undefined) k2 ***REMOVED*** k;
                o[k2] ***REMOVED*** m[k];
            }));`
};
var setModuleDefaultHelper ***REMOVED*** {
  name: "typescript:commonjscreatevalue",
  importName: "__setModuleDefault",
  scoped: false,
  priority: 1,
  text: `
            var __setModuleDefault ***REMOVED*** (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
                Object.defineProperty(o, "default", { enumerable: true, value: v });
            }) : function(o, v) {
                o["default"] ***REMOVED*** v;
            });`
};
var importStarHelper ***REMOVED*** {
  name: "typescript:commonjsimportstar",
  importName: "__importStar",
  scoped: false,
  dependencies: [createBindingHelper, setModuleDefaultHelper],
  priority: 2,
  text: `
            var __importStar ***REMOVED*** (this && this.__importStar) || (function () {
                var ownKeys ***REMOVED*** function(o) {
                    ownKeys ***REMOVED*** Object.getOwnPropertyNames || function (o) {
                        var ar ***REMOVED*** [];
                        for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] ***REMOVED*** k;
                        return ar;
                    };
                    return ownKeys(o);
                };
                return function (mod) {
                    if (mod && mod.__esModule) return mod;
                    var result ***REMOVED*** {};
                    if (mod !***REMOVED*** null) for (var k ***REMOVED*** ownKeys(mod), i ***REMOVED*** 0; i < k.length; i++) if (k[i] !***REMOVED******REMOVED*** "default") __createBinding(result, mod, k[i]);
                    __setModuleDefault(result, mod);
                    return result;
                };
            })();`
};
var importDefaultHelper ***REMOVED*** {
  name: "typescript:commonjsimportdefault",
  importName: "__importDefault",
  scoped: false,
  text: `
            var __importDefault ***REMOVED*** (this && this.__importDefault) || function (mod) {
                return (mod && mod.__esModule) ? mod : { "default": mod };
            };`
};
var exportStarHelper ***REMOVED*** {
  name: "typescript:export-star",
  importName: "__exportStar",
  scoped: false,
  dependencies: [createBindingHelper],
  priority: 2,
  text: `
            var __exportStar ***REMOVED*** (this && this.__exportStar) || function(m, exports) {
                for (var p in m) if (p !***REMOVED******REMOVED*** "default" && !Object.prototype.hasOwnProperty.call(exports, p)) __createBinding(exports, m, p);
            };`
};
var classPrivateFieldGetHelper ***REMOVED*** {
  name: "typescript:classPrivateFieldGet",
  importName: "__classPrivateFieldGet",
  scoped: false,
  text: `
            var __classPrivateFieldGet ***REMOVED*** (this && this.__classPrivateFieldGet) || function (receiver, state, kind, f) {
                if (kind ***REMOVED******REMOVED******REMOVED*** "a" && !f) throw new TypeError("Private accessor was defined without a getter");
                if (typeof state ***REMOVED******REMOVED******REMOVED*** "function" ? receiver !***REMOVED******REMOVED*** state || !f : !state.has(receiver)) throw new TypeError("Cannot read private member from an object whose class did not declare it");
                return kind ***REMOVED******REMOVED******REMOVED*** "m" ? f : kind ***REMOVED******REMOVED******REMOVED*** "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
            };`
};
var classPrivateFieldSetHelper ***REMOVED*** {
  name: "typescript:classPrivateFieldSet",
  importName: "__classPrivateFieldSet",
  scoped: false,
  text: `
            var __classPrivateFieldSet ***REMOVED*** (this && this.__classPrivateFieldSet) || function (receiver, state, value, kind, f) {
                if (kind ***REMOVED******REMOVED******REMOVED*** "m") throw new TypeError("Private method is not writable");
                if (kind ***REMOVED******REMOVED******REMOVED*** "a" && !f) throw new TypeError("Private accessor was defined without a setter");
                if (typeof state ***REMOVED******REMOVED******REMOVED*** "function" ? receiver !***REMOVED******REMOVED*** state || !f : !state.has(receiver)) throw new TypeError("Cannot write private member to an object whose class did not declare it");
                return (kind ***REMOVED******REMOVED******REMOVED*** "a" ? f.call(receiver, value) : f ? f.value ***REMOVED*** value : state.set(receiver, value)), value;
            };`
};
var classPrivateFieldInHelper ***REMOVED*** {
  name: "typescript:classPrivateFieldIn",
  importName: "__classPrivateFieldIn",
  scoped: false,
  text: `
            var __classPrivateFieldIn ***REMOVED*** (this && this.__classPrivateFieldIn) || function(state, receiver) {
                if (receiver ***REMOVED******REMOVED******REMOVED*** null || (typeof receiver !***REMOVED******REMOVED*** "object" && typeof receiver !***REMOVED******REMOVED*** "function")) throw new TypeError("Cannot use 'in' operator on non-object");
                return typeof state ***REMOVED******REMOVED******REMOVED*** "function" ? receiver ***REMOVED******REMOVED******REMOVED*** state : state.has(receiver);
            };`
};
var addDisposableResourceHelper ***REMOVED*** {
  name: "typescript:addDisposableResource",
  importName: "__addDisposableResource",
  scoped: false,
  text: `
        var __addDisposableResource ***REMOVED*** (this && this.__addDisposableResource) || function (env, value, async) {
            if (value !***REMOVED******REMOVED*** null && value !***REMOVED******REMOVED*** void 0) {
                if (typeof value !***REMOVED******REMOVED*** "object" && typeof value !***REMOVED******REMOVED*** "function") throw new TypeError("Object expected.");
                var dispose, inner;
                if (async) {
                    if (!Symbol.asyncDispose) throw new TypeError("Symbol.asyncDispose is not defined.");
                    dispose ***REMOVED*** value[Symbol.asyncDispose];
                }
                if (dispose ***REMOVED******REMOVED******REMOVED*** void 0) {
                    if (!Symbol.dispose) throw new TypeError("Symbol.dispose is not defined.");
                    dispose ***REMOVED*** value[Symbol.dispose];
                    if (async) inner ***REMOVED*** dispose;
                }
                if (typeof dispose !***REMOVED******REMOVED*** "function") throw new TypeError("Object not disposable.");
                if (inner) dispose ***REMOVED*** function() { try { inner.call(this); } catch (e) { return Promise.reject(e); } };
                env.stack.push({ value: value, dispose: dispose, async: async });
            }
            else if (async) {
                env.stack.push({ async: true });
            }
            return value;
        };`
};
var disposeResourcesHelper ***REMOVED*** {
  name: "typescript:disposeResources",
  importName: "__disposeResources",
  scoped: false,
  text: `
        var __disposeResources ***REMOVED*** (this && this.__disposeResources) || (function (SuppressedError) {
            return function (env) {
                function fail(e) {
                    env.error ***REMOVED*** env.hasError ? new SuppressedError(e, env.error, "An error was suppressed during disposal.") : e;
                    env.hasError ***REMOVED*** true;
                }
                var r, s ***REMOVED*** 0;
                function next() {
                    while (r ***REMOVED*** env.stack.pop()) {
                        try {
                            if (!r.async && s ***REMOVED******REMOVED******REMOVED*** 1) return s ***REMOVED*** 0, env.stack.push(r), Promise.resolve().then(next);
                            if (r.dispose) {
                                var result ***REMOVED*** r.dispose.call(r.value);
                                if (r.async) return s |***REMOVED*** 2, Promise.resolve(result).then(next, function(e) { fail(e); return next(); });
                            }
                            else s |***REMOVED*** 1;
                        }
                        catch (e) {
                            fail(e);
                        }
                    }
                    if (s ***REMOVED******REMOVED******REMOVED*** 1) return env.hasError ? Promise.reject(env.error) : Promise.resolve();
                    if (env.hasError) throw env.error;
                }
                return next();
            };
        })(typeof SuppressedError ***REMOVED******REMOVED******REMOVED*** "function" ? SuppressedError : function (error, suppressed, message) {
            var e ***REMOVED*** new Error(message);
            return e.name ***REMOVED*** "SuppressedError", e.error ***REMOVED*** error, e.suppressed ***REMOVED*** suppressed, e;
        });`
};
var rewriteRelativeImportExtensionsHelper ***REMOVED*** {
  name: "typescript:rewriteRelativeImportExtensions",
  importName: "__rewriteRelativeImportExtension",
  scoped: false,
  text: `
        var __rewriteRelativeImportExtension ***REMOVED*** (this && this.__rewriteRelativeImportExtension) || function (path, preserveJsx) {
            if (typeof path ***REMOVED******REMOVED******REMOVED*** "string" && /^\\.\\.?\\//.test(path)) {
                return path.replace(/\\.(tsx)$|((?:\\.d)?)((?:\\.[^./]+?)?)\\.([cm]?)ts$/i, function (m, tsx, d, ext, cm) {
                    return tsx ? preserveJsx ? ".jsx" : ".js" : d && (!ext || !cm) ? m : (d + ext + "." + cm.toLowerCase() + "js");
                });
            }
            return path;
        };`
};
var asyncSuperHelper ***REMOVED*** {
  name: "typescript:async-super",
  scoped: true,
  text: helperString`
            const ${"_superIndex"} ***REMOVED*** name ***REMOVED***> super[name];`
};
var advancedAsyncSuperHelper ***REMOVED*** {
  name: "typescript:advanced-async-super",
  scoped: true,
  text: helperString`
            const ${"_superIndex"} ***REMOVED*** (function (geti, seti) {
                const cache ***REMOVED*** Object.create(null);
                return name ***REMOVED***> cache[name] || (cache[name] ***REMOVED*** { get value() { return geti(name); }, set value(v) { seti(name, v); } });
            })(name ***REMOVED***> super[name], (name, value) ***REMOVED***> super[name] ***REMOVED*** value);`
};
function isCallToHelper(firstSegment, helperName) {
  return isCallExpression(firstSegment) && isIdentifier(firstSegment.expression) && (getEmitFlags(firstSegment.expression) & 8192 /* HelperName */) !***REMOVED******REMOVED*** 0 && firstSegment.expression.escapedText ***REMOVED******REMOVED******REMOVED*** helperName;
}

// src/compiler/factory/nodeTests.ts
function isNumericLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */;
}
function isBigIntLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */;
}
function isStringLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
}
function isJsxText(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */;
}
function isNoSubstitutionTemplateLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */;
}
function isTemplateHead(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 16 /* TemplateHead */;
}
function isDotDotDotToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */;
}
function isCommaToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */;
}
function isPlusToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */;
}
function isMinusToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */;
}
function isAsteriskToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */;
}
function isExclamationToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */;
}
function isQuestionToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 58 /* QuestionToken */;
}
function isColonToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */;
}
function isQuestionDotToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 29 /* QuestionDotToken */;
}
function isEqualsGreaterThanToken(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */;
}
function isIdentifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */;
}
function isPrivateIdentifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */;
}
function isExportModifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 95 /* ExportKeyword */;
}
function isDefaultModifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */;
}
function isAsyncModifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */;
}
function isAssertsKeyword(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 131 /* AssertsKeyword */;
}
function isAwaitKeyword(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */;
}
function isReadonlyKeyword(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */;
}
function isStaticModifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */;
}
function isAccessorModifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 129 /* AccessorKeyword */;
}
function isSuperKeyword(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */;
}
function isImportKeyword(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */;
}
function isQualifiedName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */;
}
function isComputedPropertyName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */;
}
function isTypeParameterDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */;
}
function isParameter(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */;
}
function isDecorator(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 171 /* Decorator */;
}
function isPropertySignature(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */;
}
function isPropertyDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */;
}
function isMethodSignature(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */;
}
function isMethodDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */;
}
function isClassStaticBlockDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */;
}
function isConstructorDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */;
}
function isGetAccessorDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */;
}
function isSetAccessorDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
}
function isCallSignatureDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 180 /* CallSignature */;
}
function isConstructSignatureDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */;
}
function isIndexSignatureDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */;
}
function isTypePredicateNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 183 /* TypePredicate */;
}
function isTypeReferenceNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */;
}
function isFunctionTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 185 /* FunctionType */;
}
function isConstructorTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */;
}
function isTypeQueryNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */;
}
function isTypeLiteralNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */;
}
function isArrayTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */;
}
function isTupleTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */;
}
function isNamedTupleMember(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */;
}
function isOptionalTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 191 /* OptionalType */;
}
function isRestTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 192 /* RestType */;
}
function isUnionTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 193 /* UnionType */;
}
function isIntersectionTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 194 /* IntersectionType */;
}
function isConditionalTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */;
}
function isInferTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 196 /* InferType */;
}
function isParenthesizedTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 197 /* ParenthesizedType */;
}
function isThisTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 198 /* ThisType */;
}
function isTypeOperatorNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 199 /* TypeOperator */;
}
function isIndexedAccessTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 200 /* IndexedAccessType */;
}
function isMappedTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */;
}
function isLiteralTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 202 /* LiteralType */;
}
function isImportTypeNode(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */;
}
function isTemplateLiteralTypeSpan(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 205 /* TemplateLiteralTypeSpan */;
}
function isObjectBindingPattern(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */;
}
function isArrayBindingPattern(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */;
}
function isBindingElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */;
}
function isArrayLiteralExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */;
}
function isObjectLiteralExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */;
}
function isPropertyAccessExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */;
}
function isElementAccessExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */;
}
function isCallExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */;
}
function isNewExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */;
}
function isTaggedTemplateExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */;
}
function isTypeAssertionExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */;
}
function isParenthesizedExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */;
}
function isFunctionExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */;
}
function isArrowFunction(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */;
}
function isTypeOfExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */;
}
function isVoidExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 223 /* VoidExpression */;
}
function isAwaitExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 224 /* AwaitExpression */;
}
function isPrefixUnaryExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */;
}
function isPostfixUnaryExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 226 /* PostfixUnaryExpression */;
}
function isBinaryExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */;
}
function isConditionalExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 228 /* ConditionalExpression */;
}
function isSpreadElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */;
}
function isClassExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */;
}
function isOmittedExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */;
}
function isExpressionWithTypeArguments(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */;
}
function isAsExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 235 /* AsExpression */;
}
function isSatisfiesExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 239 /* SatisfiesExpression */;
}
function isNonNullExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 236 /* NonNullExpression */;
}
function isMetaProperty(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 237 /* MetaProperty */;
}
function isPartiallyEmittedExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 356 /* PartiallyEmittedExpression */;
}
function isCommaListExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 357 /* CommaListExpression */;
}
function isTemplateSpan(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 240 /* TemplateSpan */;
}
function isSemicolonClassElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 241 /* SemicolonClassElement */;
}
function isBlock(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */;
}
function isVariableStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */;
}
function isEmptyStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 243 /* EmptyStatement */;
}
function isExpressionStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */;
}
function isIfStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 246 /* IfStatement */;
}
function isForStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 249 /* ForStatement */;
}
function isForInStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */;
}
function isForOfStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */;
}
function isReturnStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */;
}
function isWithStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 255 /* WithStatement */;
}
function isSwitchStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 256 /* SwitchStatement */;
}
function isLabeledStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 257 /* LabeledStatement */;
}
function isTryStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 259 /* TryStatement */;
}
function isVariableDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */;
}
function isVariableDeclarationList(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */;
}
function isFunctionDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */;
}
function isClassDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */;
}
function isInterfaceDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */;
}
function isTypeAliasDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */;
}
function isEnumDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */;
}
function isModuleDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */;
}
function isModuleBlock(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */;
}
function isCaseBlock(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 270 /* CaseBlock */;
}
function isNamespaceExportDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */;
}
function isImportEqualsDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */;
}
function isImportDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */;
}
function isImportClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */;
}
function isAssertClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 301 /* AssertClause */;
}
function isImportAttributes(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 301 /* ImportAttributes */;
}
function isImportAttribute(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 302 /* ImportAttribute */;
}
function isNamespaceImport(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */;
}
function isNamespaceExport(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */;
}
function isNamedImports(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 276 /* NamedImports */;
}
function isImportSpecifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */;
}
function isExportAssignment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */;
}
function isExportDeclaration(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */;
}
function isNamedExports(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 280 /* NamedExports */;
}
function isExportSpecifier(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */;
}
function isModuleExportName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
}
function isNotEmittedStatement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 354 /* NotEmittedStatement */;
}
function isSyntheticReference(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 358 /* SyntheticReferenceExpression */;
}
function isExternalModuleReference(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */;
}
function isJsxElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 285 /* JsxElement */;
}
function isJsxSelfClosingElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */;
}
function isJsxOpeningElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 287 /* JsxOpeningElement */;
}
function isJsxClosingElement(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 288 /* JsxClosingElement */;
}
function isJsxFragment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 289 /* JsxFragment */;
}
function isJsxOpeningFragment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 290 /* JsxOpeningFragment */;
}
function isJsxClosingFragment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 291 /* JsxClosingFragment */;
}
function isJsxAttribute(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 292 /* JsxAttribute */;
}
function isJsxAttributes(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 293 /* JsxAttributes */;
}
function isJsxSpreadAttribute(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 294 /* JsxSpreadAttribute */;
}
function isJsxExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */;
}
function isJsxNamespacedName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 296 /* JsxNamespacedName */;
}
function isCaseClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */;
}
function isDefaultClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */;
}
function isHeritageClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 299 /* HeritageClause */;
}
function isCatchClause(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */;
}
function isPropertyAssignment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */;
}
function isShorthandPropertyAssignment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */;
}
function isSpreadAssignment(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */;
}
function isEnumMember(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 307 /* EnumMember */;
}
function isSourceFile(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
}
function isBundle(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */;
}
function isJSDocTypeExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 310 /* JSDocTypeExpression */;
}
function isJSDocNameReference(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 311 /* JSDocNameReference */;
}
function isJSDocMemberName(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 312 /* JSDocMemberName */;
}
function isJSDocAllType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 313 /* JSDocAllType */;
}
function isJSDocUnknownType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 314 /* JSDocUnknownType */;
}
function isJSDocNullableType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 315 /* JSDocNullableType */;
}
function isJSDocNonNullableType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 316 /* JSDocNonNullableType */;
}
function isJSDocOptionalType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 317 /* JSDocOptionalType */;
}
function isJSDocFunctionType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */;
}
function isJSDocVariadicType(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 319 /* JSDocVariadicType */;
}
function isJSDoc(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 321 /* JSDoc */;
}
function isJSDocTypeLiteral(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 323 /* JSDocTypeLiteral */;
}
function isJSDocSignature(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 324 /* JSDocSignature */;
}
function isJSDocAugmentsTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 329 /* JSDocAugmentsTag */;
}
function isJSDocClassTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 333 /* JSDocClassTag */;
}
function isJSDocCallbackTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 339 /* JSDocCallbackTag */;
}
function isJSDocPublicTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 334 /* JSDocPublicTag */;
}
function isJSDocPrivateTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 335 /* JSDocPrivateTag */;
}
function isJSDocProtectedTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 336 /* JSDocProtectedTag */;
}
function isJSDocReadonlyTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 337 /* JSDocReadonlyTag */;
}
function isJSDocOverrideTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 338 /* JSDocOverrideTag */;
}
function isJSDocOverloadTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 340 /* JSDocOverloadTag */;
}
function isJSDocDeprecatedTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 332 /* JSDocDeprecatedTag */;
}
function isJSDocEnumTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 341 /* JSDocEnumTag */;
}
function isJSDocParameterTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */;
}
function isJSDocReturnTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 343 /* JSDocReturnTag */;
}
function isJSDocThisTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 344 /* JSDocThisTag */;
}
function isJSDocTypeTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 345 /* JSDocTypeTag */;
}
function isJSDocTemplateTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 346 /* JSDocTemplateTag */;
}
function isJSDocTypedefTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 347 /* JSDocTypedefTag */;
}
function isJSDocPropertyTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */;
}
function isJSDocImplementsTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 330 /* JSDocImplementsTag */;
}
function isJSDocSatisfiesTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 351 /* JSDocSatisfiesTag */;
}
function isJSDocImportTag(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 352 /* JSDocImportTag */;
}

// src/compiler/factory/nodeChildren.ts
var sourceFileToNodeChildren ***REMOVED*** /* @__PURE__ */ new WeakMap();
function getNodeChildren(node, sourceFile) {
  var _a;
  const kind ***REMOVED*** node.kind;
  if (!isNodeKind(kind)) {
    return emptyArray;
  }
  if (kind ***REMOVED******REMOVED******REMOVED*** 353 /* SyntaxList */) {
    return node._children;
  }
  return (_a ***REMOVED*** sourceFileToNodeChildren.get(sourceFile)) ***REMOVED******REMOVED*** null ? void 0 : _a.get(node);
}
function unsetNodeChildren(node, origSourceFile) {
  var _a;
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 353 /* SyntaxList */) {
    Debug.fail("Did not expect to unset the children of a SyntaxList.");
  }
  (_a ***REMOVED*** sourceFileToNodeChildren.get(origSourceFile)) ***REMOVED******REMOVED*** null ? void 0 : _a.delete(node);
}
function transferSourceFileChildren(sourceFile, targetSourceFile) {
  const map2 ***REMOVED*** sourceFileToNodeChildren.get(sourceFile);
  if (map2 !***REMOVED******REMOVED*** void 0) {
    sourceFileToNodeChildren.delete(sourceFile);
    sourceFileToNodeChildren.set(targetSourceFile, map2);
  }
}

// src/compiler/factory/utilities.ts
function createEmptyExports(factory2) {
  return factory2.createExportDeclaration(
    /*modifiers*/
    void 0,
    /*isTypeOnly*/
    false,
    factory2.createNamedExports([]),
    /*moduleSpecifier*/
    void 0
  );
}
function createMemberAccessForPropertyName(factory2, target, memberName, location) {
  if (isComputedPropertyName(memberName)) {
    return setTextRange(factory2.createElementAccessExpression(target, memberName.expression), location);
  } else {
    const expression ***REMOVED*** setTextRange(
      isMemberName(memberName) ? factory2.createPropertyAccessExpression(target, memberName) : factory2.createElementAccessExpression(target, memberName),
      memberName
    );
    addEmitFlags(expression, 128 /* NoNestedSourceMaps */);
    return expression;
  }
}
function createReactNamespace(reactNamespace, parent) {
  const react ***REMOVED*** parseNodeFactory.createIdentifier(reactNamespace || "React");
  setParent(react, getParseTreeNode(parent));
  return react;
}
function createJsxFactoryExpressionFromEntityName(factory2, jsxFactory, parent) {
  if (isQualifiedName(jsxFactory)) {
    const left ***REMOVED*** createJsxFactoryExpressionFromEntityName(factory2, jsxFactory.left, parent);
    const right ***REMOVED*** factory2.createIdentifier(idText(jsxFactory.right));
    right.escapedText ***REMOVED*** jsxFactory.right.escapedText;
    return factory2.createPropertyAccessExpression(left, right);
  } else {
    return createReactNamespace(idText(jsxFactory), parent);
  }
}
function createJsxFactoryExpression(factory2, jsxFactoryEntity, reactNamespace, parent) {
  return jsxFactoryEntity ? createJsxFactoryExpressionFromEntityName(factory2, jsxFactoryEntity, parent) : factory2.createPropertyAccessExpression(
    createReactNamespace(reactNamespace, parent),
    "createElement"
  );
}
function createJsxFragmentFactoryExpression(factory2, jsxFragmentFactoryEntity, reactNamespace, parent) {
  return jsxFragmentFactoryEntity ? createJsxFactoryExpressionFromEntityName(factory2, jsxFragmentFactoryEntity, parent) : factory2.createPropertyAccessExpression(
    createReactNamespace(reactNamespace, parent),
    "Fragment"
  );
}
function createExpressionForJsxElement(factory2, callee, tagName, props, children, location) {
  const argumentsList ***REMOVED*** [tagName];
  if (props) {
    argumentsList.push(props);
  }
  if (children && children.length > 0) {
    if (!props) {
      argumentsList.push(factory2.createNull());
    }
    if (children.length > 1) {
      for (const child of children) {
        startOnNewLine(child);
        argumentsList.push(child);
      }
    } else {
      argumentsList.push(children[0]);
    }
  }
  return setTextRange(
    factory2.createCallExpression(
      callee,
      /*typeArguments*/
      void 0,
      argumentsList
    ),
    location
  );
}
function createExpressionForJsxFragment(factory2, jsxFactoryEntity, jsxFragmentFactoryEntity, reactNamespace, children, parentElement, location) {
  const tagName ***REMOVED*** createJsxFragmentFactoryExpression(factory2, jsxFragmentFactoryEntity, reactNamespace, parentElement);
  const argumentsList ***REMOVED*** [tagName, factory2.createNull()];
  if (children && children.length > 0) {
    if (children.length > 1) {
      for (const child of children) {
        startOnNewLine(child);
        argumentsList.push(child);
      }
    } else {
      argumentsList.push(children[0]);
    }
  }
  return setTextRange(
    factory2.createCallExpression(
      createJsxFactoryExpression(factory2, jsxFactoryEntity, reactNamespace, parentElement),
      /*typeArguments*/
      void 0,
      argumentsList
    ),
    location
  );
}
function createForOfBindingStatement(factory2, node, boundValue) {
  if (isVariableDeclarationList(node)) {
    const firstDeclaration ***REMOVED*** first(node.declarations);
    const updatedDeclaration ***REMOVED*** factory2.updateVariableDeclaration(
      firstDeclaration,
      firstDeclaration.name,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      boundValue
    );
    return setTextRange(
      factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.updateVariableDeclarationList(node, [updatedDeclaration])
      ),
      /*location*/
      node
    );
  } else {
    const updatedExpression ***REMOVED*** setTextRange(
      factory2.createAssignment(node, boundValue),
      /*location*/
      node
    );
    return setTextRange(
      factory2.createExpressionStatement(updatedExpression),
      /*location*/
      node
    );
  }
}
function createExpressionFromEntityName(factory2, node) {
  if (isQualifiedName(node)) {
    const left ***REMOVED*** createExpressionFromEntityName(factory2, node.left);
    const right ***REMOVED*** setParent(setTextRange(factory2.cloneNode(node.right), node.right), node.right.parent);
    return setTextRange(factory2.createPropertyAccessExpression(left, right), node);
  } else {
    return setParent(setTextRange(factory2.cloneNode(node), node), node.parent);
  }
}
function createExpressionForPropertyName(factory2, memberName) {
  if (isIdentifier(memberName)) {
    return factory2.createStringLiteralFromNode(memberName);
  } else if (isComputedPropertyName(memberName)) {
    return setParent(setTextRange(factory2.cloneNode(memberName.expression), memberName.expression), memberName.expression.parent);
  } else {
    return setParent(setTextRange(factory2.cloneNode(memberName), memberName), memberName.parent);
  }
}
function createExpressionForAccessorDeclaration(factory2, properties, property, receiver, multiLine) {
  const { firstAccessor, getAccessor, setAccessor } ***REMOVED*** getAllAccessorDeclarations(properties, property);
  if (property ***REMOVED******REMOVED******REMOVED*** firstAccessor) {
    return setTextRange(
      factory2.createObjectDefinePropertyCall(
        receiver,
        createExpressionForPropertyName(factory2, property.name),
        factory2.createPropertyDescriptor({
          enumerable: factory2.createFalse(),
          configurable: true,
          get: getAccessor && setTextRange(
            setOriginalNode(
              factory2.createFunctionExpression(
                getModifiers(getAccessor),
                /*asteriskToken*/
                void 0,
                /*name*/
                void 0,
                /*typeParameters*/
                void 0,
                getAccessor.parameters,
                /*type*/
                void 0,
                getAccessor.body
                // TODO: GH#18217
              ),
              getAccessor
            ),
            getAccessor
          ),
          set: setAccessor && setTextRange(
            setOriginalNode(
              factory2.createFunctionExpression(
                getModifiers(setAccessor),
                /*asteriskToken*/
                void 0,
                /*name*/
                void 0,
                /*typeParameters*/
                void 0,
                setAccessor.parameters,
                /*type*/
                void 0,
                setAccessor.body
                // TODO: GH#18217
              ),
              setAccessor
            ),
            setAccessor
          )
        }, !multiLine)
      ),
      firstAccessor
    );
  }
  return void 0;
}
function createExpressionForPropertyAssignment(factory2, property, receiver) {
  return setOriginalNode(
    setTextRange(
      factory2.createAssignment(
        createMemberAccessForPropertyName(
          factory2,
          receiver,
          property.name,
          /*location*/
          property.name
        ),
        property.initializer
      ),
      property
    ),
    property
  );
}
function createExpressionForShorthandPropertyAssignment(factory2, property, receiver) {
  return setOriginalNode(
    setTextRange(
      factory2.createAssignment(
        createMemberAccessForPropertyName(
          factory2,
          receiver,
          property.name,
          /*location*/
          property.name
        ),
        factory2.cloneNode(property.name)
      ),
      /*location*/
      property
    ),
    /*original*/
    property
  );
}
function createExpressionForMethodDeclaration(factory2, method, receiver) {
  return setOriginalNode(
    setTextRange(
      factory2.createAssignment(
        createMemberAccessForPropertyName(
          factory2,
          receiver,
          method.name,
          /*location*/
          method.name
        ),
        setOriginalNode(
          setTextRange(
            factory2.createFunctionExpression(
              getModifiers(method),
              method.asteriskToken,
              /*name*/
              void 0,
              /*typeParameters*/
              void 0,
              method.parameters,
              /*type*/
              void 0,
              method.body
              // TODO: GH#18217
            ),
            /*location*/
            method
          ),
          /*original*/
          method
        )
      ),
      /*location*/
      method
    ),
    /*original*/
    method
  );
}
function createExpressionForObjectLiteralElementLike(factory2, node, property, receiver) {
  if (property.name && isPrivateIdentifier(property.name)) {
    Debug.failBadSyntaxKind(property.name, "Private identifiers are not allowed in object literals.");
  }
  switch (property.kind) {
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
      return createExpressionForAccessorDeclaration(factory2, node.properties, property, receiver, !!node.multiLine);
    case 304 /* PropertyAssignment */:
      return createExpressionForPropertyAssignment(factory2, property, receiver);
    case 305 /* ShorthandPropertyAssignment */:
      return createExpressionForShorthandPropertyAssignment(factory2, property, receiver);
    case 175 /* MethodDeclaration */:
      return createExpressionForMethodDeclaration(factory2, property, receiver);
  }
}
function expandPreOrPostfixIncrementOrDecrementExpression(factory2, node, expression, recordTempVariable, resultVariable) {
  const operator ***REMOVED*** node.operator;
  Debug.assert(operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */, "Expected 'node' to be a pre- or post-increment or pre- or post-decrement expression");
  const temp ***REMOVED*** factory2.createTempVariable(recordTempVariable);
  expression ***REMOVED*** factory2.createAssignment(temp, expression);
  setTextRange(expression, node.operand);
  let operation ***REMOVED*** isPrefixUnaryExpression(node) ? factory2.createPrefixUnaryExpression(operator, temp) : factory2.createPostfixUnaryExpression(temp, operator);
  setTextRange(operation, node);
  if (resultVariable) {
    operation ***REMOVED*** factory2.createAssignment(resultVariable, operation);
    setTextRange(operation, node);
  }
  expression ***REMOVED*** factory2.createComma(expression, operation);
  setTextRange(expression, node);
  if (isPostfixUnaryExpression(node)) {
    expression ***REMOVED*** factory2.createComma(expression, temp);
    setTextRange(expression, node);
  }
  return expression;
}
function isInternalName(node) {
  return (getEmitFlags(node) & 65536 /* InternalName */) !***REMOVED******REMOVED*** 0;
}
function isLocalName(node) {
  return (getEmitFlags(node) & 32768 /* LocalName */) !***REMOVED******REMOVED*** 0;
}
function isExportName(node) {
  return (getEmitFlags(node) & 16384 /* ExportName */) !***REMOVED******REMOVED*** 0;
}
function isUseStrictPrologue(node) {
  return isStringLiteral(node.expression) && node.expression.text ***REMOVED******REMOVED******REMOVED*** "use strict";
}
function findUseStrictPrologue(statements) {
  for (const statement of statements) {
    if (isPrologueDirective(statement)) {
      if (isUseStrictPrologue(statement)) {
        return statement;
      }
    } else {
      break;
    }
  }
  return void 0;
}
function startsWithUseStrict(statements) {
  const firstStatement ***REMOVED*** firstOrUndefined(statements);
  return firstStatement !***REMOVED******REMOVED*** void 0 && isPrologueDirective(firstStatement) && isUseStrictPrologue(firstStatement);
}
function isCommaExpression(node) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */;
}
function isCommaSequence(node) {
  return isCommaExpression(node) || isCommaListExpression(node);
}
function isJSDocTypeAssertion(node) {
  return isParenthesizedExpression(node) && isInJSFile(node) && !!getJSDocTypeTag(node);
}
function getJSDocTypeAssertionType(node) {
  const type ***REMOVED*** getJSDocType(node);
  Debug.assertIsDefined(type);
  return type;
}
function isOuterExpression(node, kinds ***REMOVED*** 63 /* All */) {
  switch (node.kind) {
    case 218 /* ParenthesizedExpression */:
      if (kinds & -2147483648 /* ExcludeJSDocTypeAssertion */ && isJSDocTypeAssertion(node)) {
        return false;
      }
      return (kinds & 1 /* Parentheses */) !***REMOVED******REMOVED*** 0;
    case 217 /* TypeAssertionExpression */:
    case 235 /* AsExpression */:
      return (kinds & 2 /* TypeAssertions */) !***REMOVED******REMOVED*** 0;
    case 239 /* SatisfiesExpression */:
      return (kinds & (2 /* TypeAssertions */ | 32 /* Satisfies */)) !***REMOVED******REMOVED*** 0;
    case 234 /* ExpressionWithTypeArguments */:
      return (kinds & 16 /* ExpressionsWithTypeArguments */) !***REMOVED******REMOVED*** 0;
    case 236 /* NonNullExpression */:
      return (kinds & 4 /* NonNullAssertions */) !***REMOVED******REMOVED*** 0;
    case 356 /* PartiallyEmittedExpression */:
      return (kinds & 8 /* PartiallyEmittedExpressions */) !***REMOVED******REMOVED*** 0;
  }
  return false;
}
function skipOuterExpressions(node, kinds ***REMOVED*** 63 /* All */) {
  while (isOuterExpression(node, kinds)) {
    node ***REMOVED*** node.expression;
  }
  return node;
}
function walkUpOuterExpressions(node, kinds ***REMOVED*** 63 /* All */) {
  let parent ***REMOVED*** node.parent;
  while (isOuterExpression(parent, kinds)) {
    parent ***REMOVED*** parent.parent;
    Debug.assert(parent);
  }
  return parent;
}
function startOnNewLine(node) {
  return setStartsOnNewLine(
    node,
    /*newLine*/
    true
  );
}
function getExternalHelpersModuleName(node) {
  const parseNode ***REMOVED*** getOriginalNode(node, isSourceFile);
  const emitNode ***REMOVED*** parseNode && parseNode.emitNode;
  return emitNode && emitNode.externalHelpersModuleName;
}
function hasRecordedExternalHelpers(sourceFile) {
  const parseNode ***REMOVED*** getOriginalNode(sourceFile, isSourceFile);
  const emitNode ***REMOVED*** parseNode && parseNode.emitNode;
  return !!emitNode && (!!emitNode.externalHelpersModuleName || !!emitNode.externalHelpers);
}
function createExternalHelpersImportDeclarationIfNeeded(nodeFactory, helperFactory, sourceFile, compilerOptions, hasExportStarsToExportValues, hasImportStar, hasImportDefault) {
  if (compilerOptions.importHelpers && isEffectiveExternalModule(sourceFile, compilerOptions)) {
    const moduleKind ***REMOVED*** getEmitModuleKind(compilerOptions);
    const impliedModuleKind ***REMOVED*** getImpliedNodeFormatForEmitWorker(sourceFile, compilerOptions);
    const helpers ***REMOVED*** getImportedHelpers(sourceFile);
    if (impliedModuleKind !***REMOVED******REMOVED*** 1 /* CommonJS */ && (moduleKind >***REMOVED*** 5 /* ES2015 */ && moduleKind <***REMOVED*** 99 /* ESNext */ || impliedModuleKind ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ || impliedModuleKind ***REMOVED******REMOVED******REMOVED*** void 0 && moduleKind ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */)) {
      if (helpers) {
        const helperNames ***REMOVED*** [];
        for (const helper of helpers) {
          const importName ***REMOVED*** helper.importName;
          if (importName) {
            pushIfUnique(helperNames, importName);
          }
        }
        if (some(helperNames)) {
          helperNames.sort(compareStringsCaseSensitive);
          const namedBindings ***REMOVED*** nodeFactory.createNamedImports(
            map(helperNames, (name) ***REMOVED***> isFileLevelUniqueName(sourceFile, name) ? nodeFactory.createImportSpecifier(
              /*isTypeOnly*/
              false,
              /*propertyName*/
              void 0,
              nodeFactory.createIdentifier(name)
            ) : nodeFactory.createImportSpecifier(
              /*isTypeOnly*/
              false,
              nodeFactory.createIdentifier(name),
              helperFactory.getUnscopedHelperName(name)
            ))
          );
          const parseNode ***REMOVED*** getOriginalNode(sourceFile, isSourceFile);
          const emitNode ***REMOVED*** getOrCreateEmitNode(parseNode);
          emitNode.externalHelpers ***REMOVED*** true;
          const externalHelpersImportDeclaration ***REMOVED*** nodeFactory.createImportDeclaration(
            /*modifiers*/
            void 0,
            nodeFactory.createImportClause(
              /*phaseModifier*/
              void 0,
              /*name*/
              void 0,
              namedBindings
            ),
            nodeFactory.createStringLiteral(externalHelpersModuleNameText),
            /*attributes*/
            void 0
          );
          addInternalEmitFlags(externalHelpersImportDeclaration, 2 /* NeverApplyImportHelper */);
          return externalHelpersImportDeclaration;
        }
      }
    } else {
      const externalHelpersModuleName ***REMOVED*** getOrCreateExternalHelpersModuleNameIfNeeded(nodeFactory, sourceFile, compilerOptions, helpers, hasExportStarsToExportValues, hasImportStar || hasImportDefault);
      if (externalHelpersModuleName) {
        const externalHelpersImportDeclaration ***REMOVED*** nodeFactory.createImportEqualsDeclaration(
          /*modifiers*/
          void 0,
          /*isTypeOnly*/
          false,
          externalHelpersModuleName,
          nodeFactory.createExternalModuleReference(nodeFactory.createStringLiteral(externalHelpersModuleNameText))
        );
        addInternalEmitFlags(externalHelpersImportDeclaration, 2 /* NeverApplyImportHelper */);
        return externalHelpersImportDeclaration;
      }
    }
  }
}
function getImportedHelpers(sourceFile) {
  return filter(getEmitHelpers(sourceFile), (helper) ***REMOVED***> !helper.scoped);
}
function getOrCreateExternalHelpersModuleNameIfNeeded(factory2, node, compilerOptions, helpers, hasExportStarsToExportValues, hasImportStarOrImportDefault) {
  const externalHelpersModuleName ***REMOVED*** getExternalHelpersModuleName(node);
  if (externalHelpersModuleName) {
    return externalHelpersModuleName;
  }
  const create ***REMOVED*** some(helpers) || (hasExportStarsToExportValues || getESModuleInterop(compilerOptions) && hasImportStarOrImportDefault) && getEmitModuleFormatOfFileWorker(node, compilerOptions) < 4 /* System */;
  if (create) {
    const parseNode ***REMOVED*** getOriginalNode(node, isSourceFile);
    const emitNode ***REMOVED*** getOrCreateEmitNode(parseNode);
    return emitNode.externalHelpersModuleName || (emitNode.externalHelpersModuleName ***REMOVED*** factory2.createUniqueName(externalHelpersModuleNameText));
  }
}
function getLocalNameForExternalImport(factory2, node, sourceFile) {
  const namespaceDeclaration ***REMOVED*** getNamespaceDeclarationNode(node);
  if (namespaceDeclaration && !isDefaultImport(node) && !isExportNamespaceAsDefaultDeclaration(node)) {
    const name ***REMOVED*** namespaceDeclaration.name;
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      return factory2.getGeneratedNameForNode(node);
    }
    return isGeneratedIdentifier(name) ? name : factory2.createIdentifier(getSourceTextOfNodeFromSourceFile(sourceFile, name) || idText(name));
  }
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ && node.importClause) {
    return factory2.getGeneratedNameForNode(node);
  }
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ && node.moduleSpecifier) {
    return factory2.getGeneratedNameForNode(node);
  }
  return void 0;
}
function getExternalModuleNameLiteral(factory2, importNode, sourceFile, host, resolver, compilerOptions) {
  const moduleName ***REMOVED*** getExternalModuleName(importNode);
  if (moduleName && isStringLiteral(moduleName)) {
    return tryGetModuleNameFromDeclaration(importNode, host, factory2, resolver, compilerOptions) || tryRenameExternalModule(factory2, moduleName, sourceFile) || factory2.cloneNode(moduleName);
  }
  return void 0;
}
function tryRenameExternalModule(factory2, moduleName, sourceFile) {
  const rename ***REMOVED*** sourceFile.renamedDependencies && sourceFile.renamedDependencies.get(moduleName.text);
  return rename ? factory2.createStringLiteral(rename) : void 0;
}
function tryGetModuleNameFromFile(factory2, file, host, options) {
  if (!file) {
    return void 0;
  }
  if (file.moduleName) {
    return factory2.createStringLiteral(file.moduleName);
  }
  if (!file.isDeclarationFile && options.outFile) {
    return factory2.createStringLiteral(getExternalModuleNameFromPath(host, file.fileName));
  }
  return void 0;
}
function tryGetModuleNameFromDeclaration(declaration, host, factory2, resolver, compilerOptions) {
  return tryGetModuleNameFromFile(factory2, resolver.getExternalModuleFileFromDeclaration(declaration), host, compilerOptions);
}
function getInitializerOfBindingOrAssignmentElement(bindingElement) {
  if (isDeclarationBindingElement(bindingElement)) {
    return bindingElement.initializer;
  }
  if (isPropertyAssignment(bindingElement)) {
    const initializer ***REMOVED*** bindingElement.initializer;
    return isAssignmentExpression(
      initializer,
      /*excludeCompoundAssignment*/
      true
    ) ? initializer.right : void 0;
  }
  if (isShorthandPropertyAssignment(bindingElement)) {
    return bindingElement.objectAssignmentInitializer;
  }
  if (isAssignmentExpression(
    bindingElement,
    /*excludeCompoundAssignment*/
    true
  )) {
    return bindingElement.right;
  }
  if (isSpreadElement(bindingElement)) {
    return getInitializerOfBindingOrAssignmentElement(bindingElement.expression);
  }
}
function getTargetOfBindingOrAssignmentElement(bindingElement) {
  if (isDeclarationBindingElement(bindingElement)) {
    return bindingElement.name;
  }
  if (isObjectLiteralElementLike(bindingElement)) {
    switch (bindingElement.kind) {
      case 304 /* PropertyAssignment */:
        return getTargetOfBindingOrAssignmentElement(bindingElement.initializer);
      case 305 /* ShorthandPropertyAssignment */:
        return bindingElement.name;
      case 306 /* SpreadAssignment */:
        return getTargetOfBindingOrAssignmentElement(bindingElement.expression);
    }
    return void 0;
  }
  if (isAssignmentExpression(
    bindingElement,
    /*excludeCompoundAssignment*/
    true
  )) {
    return getTargetOfBindingOrAssignmentElement(bindingElement.left);
  }
  if (isSpreadElement(bindingElement)) {
    return getTargetOfBindingOrAssignmentElement(bindingElement.expression);
  }
  return bindingElement;
}
function getRestIndicatorOfBindingOrAssignmentElement(bindingElement) {
  switch (bindingElement.kind) {
    case 170 /* Parameter */:
    case 209 /* BindingElement */:
      return bindingElement.dotDotDotToken;
    case 231 /* SpreadElement */:
    case 306 /* SpreadAssignment */:
      return bindingElement;
  }
  return void 0;
}
function getPropertyNameOfBindingOrAssignmentElement(bindingElement) {
  const propertyName ***REMOVED*** tryGetPropertyNameOfBindingOrAssignmentElement(bindingElement);
  Debug.assert(!!propertyName || isSpreadAssignment(bindingElement), "Invalid property name for binding element.");
  return propertyName;
}
function tryGetPropertyNameOfBindingOrAssignmentElement(bindingElement) {
  switch (bindingElement.kind) {
    case 209 /* BindingElement */:
      if (bindingElement.propertyName) {
        const propertyName ***REMOVED*** bindingElement.propertyName;
        if (isPrivateIdentifier(propertyName)) {
          return Debug.failBadSyntaxKind(propertyName);
        }
        return isComputedPropertyName(propertyName) && isStringOrNumericLiteral(propertyName.expression) ? propertyName.expression : propertyName;
      }
      break;
    case 304 /* PropertyAssignment */:
      if (bindingElement.name) {
        const propertyName ***REMOVED*** bindingElement.name;
        if (isPrivateIdentifier(propertyName)) {
          return Debug.failBadSyntaxKind(propertyName);
        }
        return isComputedPropertyName(propertyName) && isStringOrNumericLiteral(propertyName.expression) ? propertyName.expression : propertyName;
      }
      break;
    case 306 /* SpreadAssignment */:
      if (bindingElement.name && isPrivateIdentifier(bindingElement.name)) {
        return Debug.failBadSyntaxKind(bindingElement.name);
      }
      return bindingElement.name;
  }
  const target ***REMOVED*** getTargetOfBindingOrAssignmentElement(bindingElement);
  if (target && isPropertyName(target)) {
    return target;
  }
}
function isStringOrNumericLiteral(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */;
}
function getElementsOfBindingOrAssignmentPattern(name) {
  switch (name.kind) {
    case 207 /* ObjectBindingPattern */:
    case 208 /* ArrayBindingPattern */:
    case 210 /* ArrayLiteralExpression */:
      return name.elements;
    case 211 /* ObjectLiteralExpression */:
      return name.properties;
  }
}
function getJSDocTypeAliasName(fullName) {
  if (fullName) {
    let rightNode ***REMOVED*** fullName;
    while (true) {
      if (isIdentifier(rightNode) || !rightNode.body) {
        return isIdentifier(rightNode) ? rightNode : rightNode.name;
      }
      rightNode ***REMOVED*** rightNode.body;
    }
  }
}
function canHaveIllegalTypeParameters(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
}
function canHaveIllegalDecorators(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 283 /* MissingDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */;
}
function canHaveIllegalModifiers(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 283 /* MissingDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */;
}
function isQuestionOrExclamationToken(node) {
  return isQuestionToken(node) || isExclamationToken(node);
}
function isIdentifierOrThisTypeNode(node) {
  return isIdentifier(node) || isThisTypeNode(node);
}
function isReadonlyKeywordOrPlusOrMinusToken(node) {
  return isReadonlyKeyword(node) || isPlusToken(node) || isMinusToken(node);
}
function isQuestionOrPlusOrMinusToken(node) {
  return isQuestionToken(node) || isPlusToken(node) || isMinusToken(node);
}
function isModuleName(node) {
  return isIdentifier(node) || isStringLiteral(node);
}
function isExponentiationOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 43 /* AsteriskAsteriskToken */;
}
function isMultiplicativeOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || kind ***REMOVED******REMOVED******REMOVED*** 44 /* SlashToken */ || kind ***REMOVED******REMOVED******REMOVED*** 45 /* PercentToken */;
}
function isMultiplicativeOperatorOrHigher(kind) {
  return isExponentiationOperator(kind) || isMultiplicativeOperator(kind);
}
function isAdditiveOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || kind ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */;
}
function isAdditiveOperatorOrHigher(kind) {
  return isAdditiveOperator(kind) || isMultiplicativeOperatorOrHigher(kind);
}
function isShiftOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 48 /* LessThanLessThanToken */ || kind ***REMOVED******REMOVED******REMOVED*** 49 /* GreaterThanGreaterThanToken */ || kind ***REMOVED******REMOVED******REMOVED*** 50 /* GreaterThanGreaterThanGreaterThanToken */;
}
function isShiftOperatorOrHigher(kind) {
  return isShiftOperator(kind) || isAdditiveOperatorOrHigher(kind);
}
function isRelationalOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ || kind ***REMOVED******REMOVED******REMOVED*** 33 /* LessThanEqualsToken */ || kind ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ || kind ***REMOVED******REMOVED******REMOVED*** 34 /* GreaterThanEqualsToken */ || kind ***REMOVED******REMOVED******REMOVED*** 104 /* InstanceOfKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */;
}
function isRelationalOperatorOrHigher(kind) {
  return isRelationalOperator(kind) || isShiftOperatorOrHigher(kind);
}
function isEqualityOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ || kind ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */ || kind ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ || kind ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */;
}
function isEqualityOperatorOrHigher(kind) {
  return isEqualityOperator(kind) || isRelationalOperatorOrHigher(kind);
}
function isBitwiseOperator(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 51 /* AmpersandToken */ || kind ***REMOVED******REMOVED******REMOVED*** 52 /* BarToken */ || kind ***REMOVED******REMOVED******REMOVED*** 53 /* CaretToken */;
}
function isBitwiseOperatorOrHigher(kind) {
  return isBitwiseOperator(kind) || isEqualityOperatorOrHigher(kind);
}
function isLogicalOperator2(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ || kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */;
}
function isLogicalOperatorOrHigher(kind) {
  return isLogicalOperator2(kind) || isBitwiseOperatorOrHigher(kind);
}
function isAssignmentOperatorOrHigher(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */ || isLogicalOperatorOrHigher(kind) || isAssignmentOperator(kind);
}
function isBinaryOperator(kind) {
  return isAssignmentOperatorOrHigher(kind) || kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */;
}
function isBinaryOperatorToken(node) {
  return isBinaryOperator(node.kind);
}
var BinaryExpressionState;
((BinaryExpressionState2) ***REMOVED***> {
  function enter(machine, stackIndex, stateStack, nodeStack, userStateStack, _resultHolder, outerState) {
    const prevUserState ***REMOVED*** stackIndex > 0 ? userStateStack[stackIndex - 1] : void 0;
    Debug.assertEqual(stateStack[stackIndex], enter);
    userStateStack[stackIndex] ***REMOVED*** machine.onEnter(nodeStack[stackIndex], prevUserState, outerState);
    stateStack[stackIndex] ***REMOVED*** nextState(machine, enter);
    return stackIndex;
  }
  BinaryExpressionState2.enter ***REMOVED*** enter;
  function left(machine, stackIndex, stateStack, nodeStack, userStateStack, _resultHolder, _outerState) {
    Debug.assertEqual(stateStack[stackIndex], left);
    Debug.assertIsDefined(machine.onLeft);
    stateStack[stackIndex] ***REMOVED*** nextState(machine, left);
    const nextNode ***REMOVED*** machine.onLeft(nodeStack[stackIndex].left, userStateStack[stackIndex], nodeStack[stackIndex]);
    if (nextNode) {
      checkCircularity(stackIndex, nodeStack, nextNode);
      return pushStack(stackIndex, stateStack, nodeStack, userStateStack, nextNode);
    }
    return stackIndex;
  }
  BinaryExpressionState2.left ***REMOVED*** left;
  function operator(machine, stackIndex, stateStack, nodeStack, userStateStack, _resultHolder, _outerState) {
    Debug.assertEqual(stateStack[stackIndex], operator);
    Debug.assertIsDefined(machine.onOperator);
    stateStack[stackIndex] ***REMOVED*** nextState(machine, operator);
    machine.onOperator(nodeStack[stackIndex].operatorToken, userStateStack[stackIndex], nodeStack[stackIndex]);
    return stackIndex;
  }
  BinaryExpressionState2.operator ***REMOVED*** operator;
  function right(machine, stackIndex, stateStack, nodeStack, userStateStack, _resultHolder, _outerState) {
    Debug.assertEqual(stateStack[stackIndex], right);
    Debug.assertIsDefined(machine.onRight);
    stateStack[stackIndex] ***REMOVED*** nextState(machine, right);
    const nextNode ***REMOVED*** machine.onRight(nodeStack[stackIndex].right, userStateStack[stackIndex], nodeStack[stackIndex]);
    if (nextNode) {
      checkCircularity(stackIndex, nodeStack, nextNode);
      return pushStack(stackIndex, stateStack, nodeStack, userStateStack, nextNode);
    }
    return stackIndex;
  }
  BinaryExpressionState2.right ***REMOVED*** right;
  function exit(machine, stackIndex, stateStack, nodeStack, userStateStack, resultHolder, _outerState) {
    Debug.assertEqual(stateStack[stackIndex], exit);
    stateStack[stackIndex] ***REMOVED*** nextState(machine, exit);
    const result ***REMOVED*** machine.onExit(nodeStack[stackIndex], userStateStack[stackIndex]);
    if (stackIndex > 0) {
      stackIndex--;
      if (machine.foldState) {
        const side ***REMOVED*** stateStack[stackIndex] ***REMOVED******REMOVED******REMOVED*** exit ? "right" : "left";
        userStateStack[stackIndex] ***REMOVED*** machine.foldState(userStateStack[stackIndex], result, side);
      }
    } else {
      resultHolder.value ***REMOVED*** result;
    }
    return stackIndex;
  }
  BinaryExpressionState2.exit ***REMOVED*** exit;
  function done(_machine, stackIndex, stateStack, _nodeStack, _userStateStack, _resultHolder, _outerState) {
    Debug.assertEqual(stateStack[stackIndex], done);
    return stackIndex;
  }
  BinaryExpressionState2.done ***REMOVED*** done;
  function nextState(machine, currentState) {
    switch (currentState) {
      case enter:
        if (machine.onLeft) return left;
      // falls through
      case left:
        if (machine.onOperator) return operator;
      // falls through
      case operator:
        if (machine.onRight) return right;
      // falls through
      case right:
        return exit;
      case exit:
        return done;
      case done:
        return done;
      default:
        Debug.fail("Invalid state");
    }
  }
  BinaryExpressionState2.nextState ***REMOVED*** nextState;
  function pushStack(stackIndex, stateStack, nodeStack, userStateStack, node) {
    stackIndex++;
    stateStack[stackIndex] ***REMOVED*** enter;
    nodeStack[stackIndex] ***REMOVED*** node;
    userStateStack[stackIndex] ***REMOVED*** void 0;
    return stackIndex;
  }
  function checkCircularity(stackIndex, nodeStack, node) {
    if (Debug.shouldAssert(2 /* Aggressive */)) {
      while (stackIndex >***REMOVED*** 0) {
        Debug.assert(nodeStack[stackIndex] !***REMOVED******REMOVED*** node, "Circular traversal detected.");
        stackIndex--;
      }
    }
  }
})(BinaryExpressionState || (BinaryExpressionState ***REMOVED*** {}));
var BinaryExpressionStateMachine ***REMOVED*** class {
  constructor(onEnter, onLeft, onOperator, onRight, onExit, foldState) {
    this.onEnter ***REMOVED*** onEnter;
    this.onLeft ***REMOVED*** onLeft;
    this.onOperator ***REMOVED*** onOperator;
    this.onRight ***REMOVED*** onRight;
    this.onExit ***REMOVED*** onExit;
    this.foldState ***REMOVED*** foldState;
  }
};
function createBinaryExpressionTrampoline(onEnter, onLeft, onOperator, onRight, onExit, foldState) {
  const machine ***REMOVED*** new BinaryExpressionStateMachine(onEnter, onLeft, onOperator, onRight, onExit, foldState);
  return trampoline;
  function trampoline(node, outerState) {
    const resultHolder ***REMOVED*** { value: void 0 };
    const stateStack ***REMOVED*** [BinaryExpressionState.enter];
    const nodeStack ***REMOVED*** [node];
    const userStateStack ***REMOVED*** [void 0];
    let stackIndex ***REMOVED*** 0;
    while (stateStack[stackIndex] !***REMOVED******REMOVED*** BinaryExpressionState.done) {
      stackIndex ***REMOVED*** stateStack[stackIndex](machine, stackIndex, stateStack, nodeStack, userStateStack, resultHolder, outerState);
    }
    Debug.assertEqual(stackIndex, 0);
    return resultHolder.value;
  }
}
function isExportOrDefaultKeywordKind(kind) {
  return kind ***REMOVED******REMOVED******REMOVED*** 95 /* ExportKeyword */ || kind ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */;
}
function isExportOrDefaultModifier(node) {
  const kind ***REMOVED*** node.kind;
  return isExportOrDefaultKeywordKind(kind);
}
function elideNodes(factory2, nodes) {
  if (nodes ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
  if (nodes.length ***REMOVED******REMOVED******REMOVED*** 0) return nodes;
  return setTextRange(factory2.createNodeArray([], nodes.hasTrailingComma), nodes);
}
function getNodeForGeneratedName(name) {
  var _a;
  const autoGenerate ***REMOVED*** name.emitNode.autoGenerate;
  if (autoGenerate.flags & 4 /* Node */) {
    const autoGenerateId ***REMOVED*** autoGenerate.id;
    let node ***REMOVED*** name;
    let original ***REMOVED*** node.original;
    while (original) {
      node ***REMOVED*** original;
      const autoGenerate2 ***REMOVED*** (_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.autoGenerate;
      if (isMemberName(node) && (autoGenerate2 ***REMOVED******REMOVED******REMOVED*** void 0 || !!(autoGenerate2.flags & 4 /* Node */) && autoGenerate2.id !***REMOVED******REMOVED*** autoGenerateId)) {
        break;
      }
      original ***REMOVED*** node.original;
    }
    return node;
  }
  return name;
}
function formatGeneratedNamePart(part, generateName) {
  return typeof part ***REMOVED******REMOVED******REMOVED*** "object" ? formatGeneratedName(
    /*privateName*/
    false,
    part.prefix,
    part.node,
    part.suffix,
    generateName
  ) : typeof part ***REMOVED******REMOVED******REMOVED*** "string" ? part.length > 0 && part.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 35 /* hash */ ? part.slice(1) : part : "";
}
function formatIdentifier(name, generateName) {
  return typeof name ***REMOVED******REMOVED******REMOVED*** "string" ? name : formatIdentifierWorker(name, Debug.checkDefined(generateName));
}
function formatIdentifierWorker(node, generateName) {
  return isGeneratedPrivateIdentifier(node) ? generateName(node).slice(1) : isGeneratedIdentifier(node) ? generateName(node) : isPrivateIdentifier(node) ? node.escapedText.slice(1) : idText(node);
}
function formatGeneratedName(privateName, prefix, baseName, suffix, generateName) {
  prefix ***REMOVED*** formatGeneratedNamePart(prefix, generateName);
  suffix ***REMOVED*** formatGeneratedNamePart(suffix, generateName);
  baseName ***REMOVED*** formatIdentifier(baseName, generateName);
  return `${privateName ? "#" : ""}${prefix}${baseName}${suffix}`;
}
function createAccessorPropertyBackingField(factory2, node, modifiers, initializer) {
  return factory2.updatePropertyDeclaration(
    node,
    modifiers,
    factory2.getGeneratedPrivateNameForNode(
      node.name,
      /*prefix*/
      void 0,
      "_accessor_storage"
    ),
    /*questionOrExclamationToken*/
    void 0,
    /*type*/
    void 0,
    initializer
  );
}
function createAccessorPropertyGetRedirector(factory2, node, modifiers, name, receiver ***REMOVED*** factory2.createThis()) {
  return factory2.createGetAccessorDeclaration(
    modifiers,
    name,
    [],
    /*type*/
    void 0,
    factory2.createBlock([
      factory2.createReturnStatement(
        factory2.createPropertyAccessExpression(
          receiver,
          factory2.getGeneratedPrivateNameForNode(
            node.name,
            /*prefix*/
            void 0,
            "_accessor_storage"
          )
        )
      )
    ])
  );
}
function createAccessorPropertySetRedirector(factory2, node, modifiers, name, receiver ***REMOVED*** factory2.createThis()) {
  return factory2.createSetAccessorDeclaration(
    modifiers,
    name,
    [factory2.createParameterDeclaration(
      /*modifiers*/
      void 0,
      /*dotDotDotToken*/
      void 0,
      "value"
    )],
    factory2.createBlock([
      factory2.createExpressionStatement(
        factory2.createAssignment(
          factory2.createPropertyAccessExpression(
            receiver,
            factory2.getGeneratedPrivateNameForNode(
              node.name,
              /*prefix*/
              void 0,
              "_accessor_storage"
            )
          ),
          factory2.createIdentifier("value")
        )
      )
    ])
  );
}
function findComputedPropertyNameCacheAssignment(name) {
  let node ***REMOVED*** name.expression;
  while (true) {
    node ***REMOVED*** skipOuterExpressions(node);
    if (isCommaListExpression(node)) {
      node ***REMOVED*** last(node.elements);
      continue;
    }
    if (isCommaExpression(node)) {
      node ***REMOVED*** node.right;
      continue;
    }
    if (isAssignmentExpression(
      node,
      /*excludeCompoundAssignment*/
      true
    ) && isGeneratedIdentifier(node.left)) {
      return node;
    }
    break;
  }
}
function isSyntheticParenthesizedExpression(node) {
  return isParenthesizedExpression(node) && nodeIsSynthesized(node) && !node.emitNode;
}
function flattenCommaListWorker(node, expressions) {
  if (isSyntheticParenthesizedExpression(node)) {
    flattenCommaListWorker(node.expression, expressions);
  } else if (isCommaExpression(node)) {
    flattenCommaListWorker(node.left, expressions);
    flattenCommaListWorker(node.right, expressions);
  } else if (isCommaListExpression(node)) {
    for (const child of node.elements) {
      flattenCommaListWorker(child, expressions);
    }
  } else {
    expressions.push(node);
  }
}
function flattenCommaList(node) {
  const expressions ***REMOVED*** [];
  flattenCommaListWorker(node, expressions);
  return expressions;
}
function containsObjectRestOrSpread(node) {
  if (node.transformFlags & 65536 /* ContainsObjectRestOrSpread */) return true;
  if (node.transformFlags & 128 /* ContainsES2018 */) {
    for (const element of getElementsOfBindingOrAssignmentPattern(node)) {
      const target ***REMOVED*** getTargetOfBindingOrAssignmentElement(element);
      if (target && isAssignmentPattern(target)) {
        if (target.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
          return true;
        }
        if (target.transformFlags & 128 /* ContainsES2018 */) {
          if (containsObjectRestOrSpread(target)) return true;
        }
      }
    }
  }
  return false;
}

// src/compiler/factory/utilitiesPublic.ts
function setTextRange(range, location) {
  return location ? setTextRangePosEnd(range, location.pos, location.end) : range;
}
function canHaveModifiers(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ || kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */ || kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ || kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ || kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */;
}
function canHaveDecorators(node) {
  const kind ***REMOVED*** node.kind;
  return kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ || kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */;
}

// src/compiler/parser.ts
var NodeConstructor;
var TokenConstructor;
var IdentifierConstructor;
var PrivateIdentifierConstructor;
var SourceFileConstructor;
var parseBaseNodeFactory ***REMOVED*** {
  createBaseSourceFileNode: (kind) ***REMOVED***> new (SourceFileConstructor || (SourceFileConstructor ***REMOVED*** objectAllocator.getSourceFileConstructor()))(kind, -1, -1),
  createBaseIdentifierNode: (kind) ***REMOVED***> new (IdentifierConstructor || (IdentifierConstructor ***REMOVED*** objectAllocator.getIdentifierConstructor()))(kind, -1, -1),
  createBasePrivateIdentifierNode: (kind) ***REMOVED***> new (PrivateIdentifierConstructor || (PrivateIdentifierConstructor ***REMOVED*** objectAllocator.getPrivateIdentifierConstructor()))(kind, -1, -1),
  createBaseTokenNode: (kind) ***REMOVED***> new (TokenConstructor || (TokenConstructor ***REMOVED*** objectAllocator.getTokenConstructor()))(kind, -1, -1),
  createBaseNode: (kind) ***REMOVED***> new (NodeConstructor || (NodeConstructor ***REMOVED*** objectAllocator.getNodeConstructor()))(kind, -1, -1)
};
var parseNodeFactory ***REMOVED*** createNodeFactory(1 /* NoParenthesizerRules */, parseBaseNodeFactory);
function visitNode2(cbNode, node) {
  return node && cbNode(node);
}
function visitNodes(cbNode, cbNodes, nodes) {
  if (nodes) {
    if (cbNodes) {
      return cbNodes(nodes);
    }
    for (const node of nodes) {
      const result ***REMOVED*** cbNode(node);
      if (result) {
        return result;
      }
    }
  }
}
function isJSDocLikeText(text, start) {
  return text.charCodeAt(start + 1) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(start + 2) ***REMOVED******REMOVED******REMOVED*** 42 /* asterisk */ && text.charCodeAt(start + 3) !***REMOVED******REMOVED*** 47 /* slash */;
}
function isFileProbablyExternalModule(sourceFile) {
  return forEach(sourceFile.statements, isAnExternalModuleIndicatorNode) || getImportMetaIfNecessary(sourceFile);
}
function isAnExternalModuleIndicatorNode(node) {
  return canHaveModifiers(node) && hasModifierOfKind(node, 95 /* ExportKeyword */) || isImportEqualsDeclaration(node) && isExternalModuleReference(node.moduleReference) || isImportDeclaration(node) || isExportAssignment(node) || isExportDeclaration(node) ? node : void 0;
}
function getImportMetaIfNecessary(sourceFile) {
  return sourceFile.flags & 8388608 /* PossiblyContainsImportMeta */ ? walkTreeForImportMeta(sourceFile) : void 0;
}
function walkTreeForImportMeta(node) {
  return isImportMeta2(node) ? node : forEachChild(node, walkTreeForImportMeta);
}
function hasModifierOfKind(node, kind) {
  return some(node.modifiers, (m) ***REMOVED***> m.kind ***REMOVED******REMOVED******REMOVED*** kind);
}
function isImportMeta2(node) {
  return isMetaProperty(node) && node.keywordToken ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */ && node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "meta";
}
var forEachChildTable ***REMOVED*** {
  [167 /* QualifiedName */]: function forEachChildInQualifiedName(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.left) || visitNode2(cbNode, node.right);
  },
  [169 /* TypeParameter */]: function forEachChildInTypeParameter(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.constraint) || visitNode2(cbNode, node.default) || visitNode2(cbNode, node.expression);
  },
  [305 /* ShorthandPropertyAssignment */]: function forEachChildInShorthandPropertyAssignment(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.exclamationToken) || visitNode2(cbNode, node.equalsToken) || visitNode2(cbNode, node.objectAssignmentInitializer);
  },
  [306 /* SpreadAssignment */]: function forEachChildInSpreadAssignment(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [170 /* Parameter */]: function forEachChildInParameter(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.dotDotDotToken) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.initializer);
  },
  [173 /* PropertyDeclaration */]: function forEachChildInPropertyDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.exclamationToken) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.initializer);
  },
  [172 /* PropertySignature */]: function forEachChildInPropertySignature(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.initializer);
  },
  [304 /* PropertyAssignment */]: function forEachChildInPropertyAssignment(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.exclamationToken) || visitNode2(cbNode, node.initializer);
  },
  [261 /* VariableDeclaration */]: function forEachChildInVariableDeclaration(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name) || visitNode2(cbNode, node.exclamationToken) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.initializer);
  },
  [209 /* BindingElement */]: function forEachChildInBindingElement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.dotDotDotToken) || visitNode2(cbNode, node.propertyName) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.initializer);
  },
  [182 /* IndexSignature */]: function forEachChildInIndexSignature(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
  },
  [186 /* ConstructorType */]: function forEachChildInConstructorType(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
  },
  [185 /* FunctionType */]: function forEachChildInFunctionType(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
  },
  [180 /* CallSignature */]: forEachChildInCallOrConstructSignature,
  [181 /* ConstructSignature */]: forEachChildInCallOrConstructSignature,
  [175 /* MethodDeclaration */]: function forEachChildInMethodDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.asteriskToken) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.exclamationToken) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [174 /* MethodSignature */]: function forEachChildInMethodSignature(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
  },
  [177 /* Constructor */]: function forEachChildInConstructor(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [178 /* GetAccessor */]: function forEachChildInGetAccessor(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [179 /* SetAccessor */]: function forEachChildInSetAccessor(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [263 /* FunctionDeclaration */]: function forEachChildInFunctionDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.asteriskToken) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [219 /* FunctionExpression */]: function forEachChildInFunctionExpression(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.asteriskToken) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.body);
  },
  [220 /* ArrowFunction */]: function forEachChildInArrowFunction(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type) || visitNode2(cbNode, node.equalsGreaterThanToken) || visitNode2(cbNode, node.body);
  },
  [176 /* ClassStaticBlockDeclaration */]: function forEachChildInClassStaticBlockDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.body);
  },
  [184 /* TypeReference */]: function forEachChildInTypeReference(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.typeName) || visitNodes(cbNode, cbNodes, node.typeArguments);
  },
  [183 /* TypePredicate */]: function forEachChildInTypePredicate(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.assertsModifier) || visitNode2(cbNode, node.parameterName) || visitNode2(cbNode, node.type);
  },
  [187 /* TypeQuery */]: function forEachChildInTypeQuery(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.exprName) || visitNodes(cbNode, cbNodes, node.typeArguments);
  },
  [188 /* TypeLiteral */]: function forEachChildInTypeLiteral(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.members);
  },
  [189 /* ArrayType */]: function forEachChildInArrayType(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.elementType);
  },
  [190 /* TupleType */]: function forEachChildInTupleType(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.elements);
  },
  [193 /* UnionType */]: forEachChildInUnionOrIntersectionType,
  [194 /* IntersectionType */]: forEachChildInUnionOrIntersectionType,
  [195 /* ConditionalType */]: function forEachChildInConditionalType(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.checkType) || visitNode2(cbNode, node.extendsType) || visitNode2(cbNode, node.trueType) || visitNode2(cbNode, node.falseType);
  },
  [196 /* InferType */]: function forEachChildInInferType(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.typeParameter);
  },
  [206 /* ImportType */]: function forEachChildInImportType(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.argument) || visitNode2(cbNode, node.attributes) || visitNode2(cbNode, node.qualifier) || visitNodes(cbNode, cbNodes, node.typeArguments);
  },
  [303 /* ImportTypeAssertionContainer */]: function forEachChildInImportTypeAssertionContainer(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.assertClause);
  },
  [197 /* ParenthesizedType */]: forEachChildInParenthesizedTypeOrTypeOperator,
  [199 /* TypeOperator */]: forEachChildInParenthesizedTypeOrTypeOperator,
  [200 /* IndexedAccessType */]: function forEachChildInIndexedAccessType(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.objectType) || visitNode2(cbNode, node.indexType);
  },
  [201 /* MappedType */]: function forEachChildInMappedType(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.readonlyToken) || visitNode2(cbNode, node.typeParameter) || visitNode2(cbNode, node.nameType) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.type) || visitNodes(cbNode, cbNodes, node.members);
  },
  [202 /* LiteralType */]: function forEachChildInLiteralType(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.literal);
  },
  [203 /* NamedTupleMember */]: function forEachChildInNamedTupleMember(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.dotDotDotToken) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.type);
  },
  [207 /* ObjectBindingPattern */]: forEachChildInObjectOrArrayBindingPattern,
  [208 /* ArrayBindingPattern */]: forEachChildInObjectOrArrayBindingPattern,
  [210 /* ArrayLiteralExpression */]: function forEachChildInArrayLiteralExpression(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.elements);
  },
  [211 /* ObjectLiteralExpression */]: function forEachChildInObjectLiteralExpression(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.properties);
  },
  [212 /* PropertyAccessExpression */]: function forEachChildInPropertyAccessExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.questionDotToken) || visitNode2(cbNode, node.name);
  },
  [213 /* ElementAccessExpression */]: function forEachChildInElementAccessExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.questionDotToken) || visitNode2(cbNode, node.argumentExpression);
  },
  [214 /* CallExpression */]: forEachChildInCallOrNewExpression,
  [215 /* NewExpression */]: forEachChildInCallOrNewExpression,
  [216 /* TaggedTemplateExpression */]: function forEachChildInTaggedTemplateExpression(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tag) || visitNode2(cbNode, node.questionDotToken) || visitNodes(cbNode, cbNodes, node.typeArguments) || visitNode2(cbNode, node.template);
  },
  [217 /* TypeAssertionExpression */]: function forEachChildInTypeAssertionExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.type) || visitNode2(cbNode, node.expression);
  },
  [218 /* ParenthesizedExpression */]: function forEachChildInParenthesizedExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [221 /* DeleteExpression */]: function forEachChildInDeleteExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [222 /* TypeOfExpression */]: function forEachChildInTypeOfExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [223 /* VoidExpression */]: function forEachChildInVoidExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [225 /* PrefixUnaryExpression */]: function forEachChildInPrefixUnaryExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.operand);
  },
  [230 /* YieldExpression */]: function forEachChildInYieldExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.asteriskToken) || visitNode2(cbNode, node.expression);
  },
  [224 /* AwaitExpression */]: function forEachChildInAwaitExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [226 /* PostfixUnaryExpression */]: function forEachChildInPostfixUnaryExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.operand);
  },
  [227 /* BinaryExpression */]: function forEachChildInBinaryExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.left) || visitNode2(cbNode, node.operatorToken) || visitNode2(cbNode, node.right);
  },
  [235 /* AsExpression */]: function forEachChildInAsExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.type);
  },
  [236 /* NonNullExpression */]: function forEachChildInNonNullExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [239 /* SatisfiesExpression */]: function forEachChildInSatisfiesExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.type);
  },
  [237 /* MetaProperty */]: function forEachChildInMetaProperty(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name);
  },
  [228 /* ConditionalExpression */]: function forEachChildInConditionalExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.condition) || visitNode2(cbNode, node.questionToken) || visitNode2(cbNode, node.whenTrue) || visitNode2(cbNode, node.colonToken) || visitNode2(cbNode, node.whenFalse);
  },
  [231 /* SpreadElement */]: function forEachChildInSpreadElement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [242 /* Block */]: forEachChildInBlock,
  [269 /* ModuleBlock */]: forEachChildInBlock,
  [308 /* SourceFile */]: function forEachChildInSourceFile(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.statements) || visitNode2(cbNode, node.endOfFileToken);
  },
  [244 /* VariableStatement */]: function forEachChildInVariableStatement(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.declarationList);
  },
  [262 /* VariableDeclarationList */]: function forEachChildInVariableDeclarationList(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.declarations);
  },
  [245 /* ExpressionStatement */]: function forEachChildInExpressionStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [246 /* IfStatement */]: function forEachChildInIfStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.thenStatement) || visitNode2(cbNode, node.elseStatement);
  },
  [247 /* DoStatement */]: function forEachChildInDoStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.statement) || visitNode2(cbNode, node.expression);
  },
  [248 /* WhileStatement */]: function forEachChildInWhileStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.statement);
  },
  [249 /* ForStatement */]: function forEachChildInForStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.initializer) || visitNode2(cbNode, node.condition) || visitNode2(cbNode, node.incrementor) || visitNode2(cbNode, node.statement);
  },
  [250 /* ForInStatement */]: function forEachChildInForInStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.initializer) || visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.statement);
  },
  [251 /* ForOfStatement */]: function forEachChildInForOfStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.awaitModifier) || visitNode2(cbNode, node.initializer) || visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.statement);
  },
  [252 /* ContinueStatement */]: forEachChildInContinueOrBreakStatement,
  [253 /* BreakStatement */]: forEachChildInContinueOrBreakStatement,
  [254 /* ReturnStatement */]: function forEachChildInReturnStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [255 /* WithStatement */]: function forEachChildInWithStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.statement);
  },
  [256 /* SwitchStatement */]: function forEachChildInSwitchStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.caseBlock);
  },
  [270 /* CaseBlock */]: function forEachChildInCaseBlock(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.clauses);
  },
  [297 /* CaseClause */]: function forEachChildInCaseClause(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNodes(cbNode, cbNodes, node.statements);
  },
  [298 /* DefaultClause */]: function forEachChildInDefaultClause(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.statements);
  },
  [257 /* LabeledStatement */]: function forEachChildInLabeledStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.label) || visitNode2(cbNode, node.statement);
  },
  [258 /* ThrowStatement */]: function forEachChildInThrowStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [259 /* TryStatement */]: function forEachChildInTryStatement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.tryBlock) || visitNode2(cbNode, node.catchClause) || visitNode2(cbNode, node.finallyBlock);
  },
  [300 /* CatchClause */]: function forEachChildInCatchClause(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.variableDeclaration) || visitNode2(cbNode, node.block);
  },
  [171 /* Decorator */]: function forEachChildInDecorator(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [264 /* ClassDeclaration */]: forEachChildInClassDeclarationOrExpression,
  [232 /* ClassExpression */]: forEachChildInClassDeclarationOrExpression,
  [265 /* InterfaceDeclaration */]: function forEachChildInInterfaceDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.heritageClauses) || visitNodes(cbNode, cbNodes, node.members);
  },
  [266 /* TypeAliasDeclaration */]: function forEachChildInTypeAliasDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNode2(cbNode, node.type);
  },
  [267 /* EnumDeclaration */]: function forEachChildInEnumDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.members);
  },
  [307 /* EnumMember */]: function forEachChildInEnumMember(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name) || visitNode2(cbNode, node.initializer);
  },
  [268 /* ModuleDeclaration */]: function forEachChildInModuleDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.body);
  },
  [272 /* ImportEqualsDeclaration */]: function forEachChildInImportEqualsDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNode2(cbNode, node.moduleReference);
  },
  [273 /* ImportDeclaration */]: function forEachChildInImportDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.importClause) || visitNode2(cbNode, node.moduleSpecifier) || visitNode2(cbNode, node.attributes);
  },
  [274 /* ImportClause */]: function forEachChildInImportClause(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name) || visitNode2(cbNode, node.namedBindings);
  },
  [301 /* ImportAttributes */]: function forEachChildInImportAttributes(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.elements);
  },
  [302 /* ImportAttribute */]: function forEachChildInImportAttribute(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name) || visitNode2(cbNode, node.value);
  },
  [271 /* NamespaceExportDeclaration */]: function forEachChildInNamespaceExportDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name);
  },
  [275 /* NamespaceImport */]: function forEachChildInNamespaceImport(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name);
  },
  [281 /* NamespaceExport */]: function forEachChildInNamespaceExport(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name);
  },
  [276 /* NamedImports */]: forEachChildInNamedImportsOrExports,
  [280 /* NamedExports */]: forEachChildInNamedImportsOrExports,
  [279 /* ExportDeclaration */]: function forEachChildInExportDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.exportClause) || visitNode2(cbNode, node.moduleSpecifier) || visitNode2(cbNode, node.attributes);
  },
  [277 /* ImportSpecifier */]: forEachChildInImportOrExportSpecifier,
  [282 /* ExportSpecifier */]: forEachChildInImportOrExportSpecifier,
  [278 /* ExportAssignment */]: function forEachChildInExportAssignment(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.expression);
  },
  [229 /* TemplateExpression */]: function forEachChildInTemplateExpression(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.head) || visitNodes(cbNode, cbNodes, node.templateSpans);
  },
  [240 /* TemplateSpan */]: function forEachChildInTemplateSpan(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNode2(cbNode, node.literal);
  },
  [204 /* TemplateLiteralType */]: function forEachChildInTemplateLiteralType(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.head) || visitNodes(cbNode, cbNodes, node.templateSpans);
  },
  [205 /* TemplateLiteralTypeSpan */]: function forEachChildInTemplateLiteralTypeSpan(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.type) || visitNode2(cbNode, node.literal);
  },
  [168 /* ComputedPropertyName */]: function forEachChildInComputedPropertyName(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [299 /* HeritageClause */]: function forEachChildInHeritageClause(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.types);
  },
  [234 /* ExpressionWithTypeArguments */]: function forEachChildInExpressionWithTypeArguments(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.expression) || visitNodes(cbNode, cbNodes, node.typeArguments);
  },
  [284 /* ExternalModuleReference */]: function forEachChildInExternalModuleReference(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [283 /* MissingDeclaration */]: function forEachChildInMissingDeclaration(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.modifiers);
  },
  [357 /* CommaListExpression */]: function forEachChildInCommaListExpression(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.elements);
  },
  [285 /* JsxElement */]: function forEachChildInJsxElement(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.openingElement) || visitNodes(cbNode, cbNodes, node.children) || visitNode2(cbNode, node.closingElement);
  },
  [289 /* JsxFragment */]: function forEachChildInJsxFragment(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.openingFragment) || visitNodes(cbNode, cbNodes, node.children) || visitNode2(cbNode, node.closingFragment);
  },
  [286 /* JsxSelfClosingElement */]: forEachChildInJsxOpeningOrSelfClosingElement,
  [287 /* JsxOpeningElement */]: forEachChildInJsxOpeningOrSelfClosingElement,
  [293 /* JsxAttributes */]: function forEachChildInJsxAttributes(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.properties);
  },
  [292 /* JsxAttribute */]: function forEachChildInJsxAttribute(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name) || visitNode2(cbNode, node.initializer);
  },
  [294 /* JsxSpreadAttribute */]: function forEachChildInJsxSpreadAttribute(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.expression);
  },
  [295 /* JsxExpression */]: function forEachChildInJsxExpression(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.dotDotDotToken) || visitNode2(cbNode, node.expression);
  },
  [288 /* JsxClosingElement */]: function forEachChildInJsxClosingElement(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.tagName);
  },
  [296 /* JsxNamespacedName */]: function forEachChildInJsxNamespacedName(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.namespace) || visitNode2(cbNode, node.name);
  },
  [191 /* OptionalType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [192 /* RestType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [310 /* JSDocTypeExpression */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [316 /* JSDocNonNullableType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [315 /* JSDocNullableType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [317 /* JSDocOptionalType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [319 /* JSDocVariadicType */]: forEachChildInOptionalRestOrJSDocParameterModifier,
  [318 /* JSDocFunctionType */]: function forEachChildInJSDocFunctionType(node, cbNode, cbNodes) {
    return visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
  },
  [321 /* JSDoc */]: function forEachChildInJSDoc(node, cbNode, cbNodes) {
    return (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment)) || visitNodes(cbNode, cbNodes, node.tags);
  },
  [348 /* JSDocSeeTag */]: function forEachChildInJSDocSeeTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.name) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [311 /* JSDocNameReference */]: function forEachChildInJSDocNameReference(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.name);
  },
  [312 /* JSDocMemberName */]: function forEachChildInJSDocMemberName(node, cbNode, _cbNodes) {
    return visitNode2(cbNode, node.left) || visitNode2(cbNode, node.right);
  },
  [342 /* JSDocParameterTag */]: forEachChildInJSDocParameterOrPropertyTag,
  [349 /* JSDocPropertyTag */]: forEachChildInJSDocParameterOrPropertyTag,
  [331 /* JSDocAuthorTag */]: function forEachChildInJSDocAuthorTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [330 /* JSDocImplementsTag */]: function forEachChildInJSDocImplementsTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.class) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [329 /* JSDocAugmentsTag */]: function forEachChildInJSDocAugmentsTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.class) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [346 /* JSDocTemplateTag */]: function forEachChildInJSDocTemplateTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.constraint) || visitNodes(cbNode, cbNodes, node.typeParameters) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [347 /* JSDocTypedefTag */]: function forEachChildInJSDocTypedefTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || (node.typeExpression && node.typeExpression.kind ***REMOVED******REMOVED******REMOVED*** 310 /* JSDocTypeExpression */ ? visitNode2(cbNode, node.typeExpression) || visitNode2(cbNode, node.fullName) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment)) : visitNode2(cbNode, node.fullName) || visitNode2(cbNode, node.typeExpression) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment)));
  },
  [339 /* JSDocCallbackTag */]: function forEachChildInJSDocCallbackTag(node, cbNode, cbNodes) {
    return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.fullName) || visitNode2(cbNode, node.typeExpression) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
  },
  [343 /* JSDocReturnTag */]: forEachChildInJSDocTypeLikeTag,
  [345 /* JSDocTypeTag */]: forEachChildInJSDocTypeLikeTag,
  [344 /* JSDocThisTag */]: forEachChildInJSDocTypeLikeTag,
  [341 /* JSDocEnumTag */]: forEachChildInJSDocTypeLikeTag,
  [351 /* JSDocSatisfiesTag */]: forEachChildInJSDocTypeLikeTag,
  [350 /* JSDocThrowsTag */]: forEachChildInJSDocTypeLikeTag,
  [340 /* JSDocOverloadTag */]: forEachChildInJSDocTypeLikeTag,
  [324 /* JSDocSignature */]: function forEachChildInJSDocSignature(node, cbNode, _cbNodes) {
    return forEach(node.typeParameters, cbNode) || forEach(node.parameters, cbNode) || visitNode2(cbNode, node.type);
  },
  [325 /* JSDocLink */]: forEachChildInJSDocLinkCodeOrPlain,
  [326 /* JSDocLinkCode */]: forEachChildInJSDocLinkCodeOrPlain,
  [327 /* JSDocLinkPlain */]: forEachChildInJSDocLinkCodeOrPlain,
  [323 /* JSDocTypeLiteral */]: function forEachChildInJSDocTypeLiteral(node, cbNode, _cbNodes) {
    return forEach(node.jsDocPropertyTags, cbNode);
  },
  [328 /* JSDocTag */]: forEachChildInJSDocTag,
  [333 /* JSDocClassTag */]: forEachChildInJSDocTag,
  [334 /* JSDocPublicTag */]: forEachChildInJSDocTag,
  [335 /* JSDocPrivateTag */]: forEachChildInJSDocTag,
  [336 /* JSDocProtectedTag */]: forEachChildInJSDocTag,
  [337 /* JSDocReadonlyTag */]: forEachChildInJSDocTag,
  [332 /* JSDocDeprecatedTag */]: forEachChildInJSDocTag,
  [338 /* JSDocOverrideTag */]: forEachChildInJSDocTag,
  [352 /* JSDocImportTag */]: forEachChildInJSDocImportTag,
  [356 /* PartiallyEmittedExpression */]: forEachChildInPartiallyEmittedExpression
};
function forEachChildInCallOrConstructSignature(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.parameters) || visitNode2(cbNode, node.type);
}
function forEachChildInUnionOrIntersectionType(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.types);
}
function forEachChildInParenthesizedTypeOrTypeOperator(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.type);
}
function forEachChildInObjectOrArrayBindingPattern(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.elements);
}
function forEachChildInCallOrNewExpression(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.expression) || // TODO: should we separate these branches out?
  visitNode2(cbNode, node.questionDotToken) || visitNodes(cbNode, cbNodes, node.typeArguments) || visitNodes(cbNode, cbNodes, node.arguments);
}
function forEachChildInBlock(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.statements);
}
function forEachChildInContinueOrBreakStatement(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.label);
}
function forEachChildInClassDeclarationOrExpression(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.modifiers) || visitNode2(cbNode, node.name) || visitNodes(cbNode, cbNodes, node.typeParameters) || visitNodes(cbNode, cbNodes, node.heritageClauses) || visitNodes(cbNode, cbNodes, node.members);
}
function forEachChildInNamedImportsOrExports(node, cbNode, cbNodes) {
  return visitNodes(cbNode, cbNodes, node.elements);
}
function forEachChildInImportOrExportSpecifier(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.propertyName) || visitNode2(cbNode, node.name);
}
function forEachChildInJsxOpeningOrSelfClosingElement(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.tagName) || visitNodes(cbNode, cbNodes, node.typeArguments) || visitNode2(cbNode, node.attributes);
}
function forEachChildInOptionalRestOrJSDocParameterModifier(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.type);
}
function forEachChildInJSDocParameterOrPropertyTag(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.tagName) || (node.isNameFirst ? visitNode2(cbNode, node.name) || visitNode2(cbNode, node.typeExpression) : visitNode2(cbNode, node.typeExpression) || visitNode2(cbNode, node.name)) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
}
function forEachChildInJSDocTypeLikeTag(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.typeExpression) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
}
function forEachChildInJSDocLinkCodeOrPlain(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.name);
}
function forEachChildInJSDocTag(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.tagName) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
}
function forEachChildInJSDocImportTag(node, cbNode, cbNodes) {
  return visitNode2(cbNode, node.tagName) || visitNode2(cbNode, node.importClause) || visitNode2(cbNode, node.moduleSpecifier) || visitNode2(cbNode, node.attributes) || (typeof node.comment ***REMOVED******REMOVED******REMOVED*** "string" ? void 0 : visitNodes(cbNode, cbNodes, node.comment));
}
function forEachChildInPartiallyEmittedExpression(node, cbNode, _cbNodes) {
  return visitNode2(cbNode, node.expression);
}
function forEachChild(node, cbNode, cbNodes) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0 || node.kind <***REMOVED*** 166 /* LastToken */) {
    return;
  }
  const fn ***REMOVED*** forEachChildTable[node.kind];
  return fn ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : fn(node, cbNode, cbNodes);
}
function forEachChildRecursively(rootNode, cbNode, cbNodes) {
  const queue ***REMOVED*** gatherPossibleChildren(rootNode);
  const parents ***REMOVED*** [];
  while (parents.length < queue.length) {
    parents.push(rootNode);
  }
  while (queue.length !***REMOVED******REMOVED*** 0) {
    const current ***REMOVED*** queue.pop();
    const parent ***REMOVED*** parents.pop();
    if (isArray(current)) {
      if (cbNodes) {
        const res ***REMOVED*** cbNodes(current, parent);
        if (res) {
          if (res ***REMOVED******REMOVED******REMOVED*** "skip") continue;
          return res;
        }
      }
      for (let i ***REMOVED*** current.length - 1; i >***REMOVED*** 0; --i) {
        queue.push(current[i]);
        parents.push(parent);
      }
    } else {
      const res ***REMOVED*** cbNode(current, parent);
      if (res) {
        if (res ***REMOVED******REMOVED******REMOVED*** "skip") continue;
        return res;
      }
      if (current.kind >***REMOVED*** 167 /* FirstNode */) {
        for (const child of gatherPossibleChildren(current)) {
          queue.push(child);
          parents.push(current);
        }
      }
    }
  }
}
function gatherPossibleChildren(node) {
  const children ***REMOVED*** [];
  forEachChild(node, addWorkItem, addWorkItem);
  return children;
  function addWorkItem(n) {
    children.unshift(n);
  }
}
function setExternalModuleIndicator(sourceFile) {
  sourceFile.externalModuleIndicator ***REMOVED*** isFileProbablyExternalModule(sourceFile);
}
function createSourceFile(fileName, sourceText, languageVersionOrOptions, setParentNodes ***REMOVED*** false, scriptKind) {
  var _a, _b;
  (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(
    tracing.Phase.Parse,
    "createSourceFile",
    { path: fileName },
    /*separateBeginAndEnd*/
    true
  );
  mark("beforeParse");
  let result;
  const {
    languageVersion,
    setExternalModuleIndicator: overrideSetExternalModuleIndicator,
    impliedNodeFormat: format,
    jsDocParsingMode
  } ***REMOVED*** typeof languageVersionOrOptions ***REMOVED******REMOVED******REMOVED*** "object" ? languageVersionOrOptions : { languageVersion: languageVersionOrOptions };
  if (languageVersion ***REMOVED******REMOVED******REMOVED*** 100 /* JSON */) {
    result ***REMOVED*** Parser.parseSourceFile(
      fileName,
      sourceText,
      languageVersion,
      /*syntaxCursor*/
      void 0,
      setParentNodes,
      6 /* JSON */,
      noop,
      jsDocParsingMode
    );
  } else {
    const setIndicator ***REMOVED*** format ***REMOVED******REMOVED******REMOVED*** void 0 ? overrideSetExternalModuleIndicator : (file) ***REMOVED***> {
      file.impliedNodeFormat ***REMOVED*** format;
      return (overrideSetExternalModuleIndicator || setExternalModuleIndicator)(file);
    };
    result ***REMOVED*** Parser.parseSourceFile(
      fileName,
      sourceText,
      languageVersion,
      /*syntaxCursor*/
      void 0,
      setParentNodes,
      scriptKind,
      setIndicator,
      jsDocParsingMode
    );
  }
  mark("afterParse");
  measure("Parse", "beforeParse", "afterParse");
  (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  return result;
}
function parseIsolatedEntityName(text, languageVersion) {
  return Parser.parseIsolatedEntityName(text, languageVersion);
}
function parseJsonText(fileName, sourceText) {
  return Parser.parseJsonText(fileName, sourceText);
}
function isExternalModule(file) {
  return file.externalModuleIndicator !***REMOVED******REMOVED*** void 0;
}
var Parser;
((Parser2) ***REMOVED***> {
  var scanner ***REMOVED*** createScanner(
    99 /* Latest */,
    /*skipTrivia*/
    true
  );
  var disallowInAndDecoratorContext ***REMOVED*** 8192 /* DisallowInContext */ | 32768 /* DecoratorContext */;
  var NodeConstructor2;
  var TokenConstructor2;
  var IdentifierConstructor2;
  var PrivateIdentifierConstructor2;
  var SourceFileConstructor2;
  function countNode(node) {
    nodeCount++;
    return node;
  }
  var baseNodeFactory ***REMOVED*** {
    createBaseSourceFileNode: (kind) ***REMOVED***> countNode(new SourceFileConstructor2(
      kind,
      /*pos*/
      0,
      /*end*/
      0
    )),
    createBaseIdentifierNode: (kind) ***REMOVED***> countNode(new IdentifierConstructor2(
      kind,
      /*pos*/
      0,
      /*end*/
      0
    )),
    createBasePrivateIdentifierNode: (kind) ***REMOVED***> countNode(new PrivateIdentifierConstructor2(
      kind,
      /*pos*/
      0,
      /*end*/
      0
    )),
    createBaseTokenNode: (kind) ***REMOVED***> countNode(new TokenConstructor2(
      kind,
      /*pos*/
      0,
      /*end*/
      0
    )),
    createBaseNode: (kind) ***REMOVED***> countNode(new NodeConstructor2(
      kind,
      /*pos*/
      0,
      /*end*/
      0
    ))
  };
  var factory2 ***REMOVED*** createNodeFactory(1 /* NoParenthesizerRules */ | 2 /* NoNodeConverters */ | 8 /* NoOriginalNode */, baseNodeFactory);
  var {
    createNodeArray: factoryCreateNodeArray,
    createNumericLiteral: factoryCreateNumericLiteral,
    createStringLiteral: factoryCreateStringLiteral,
    createLiteralLikeNode: factoryCreateLiteralLikeNode,
    createIdentifier: factoryCreateIdentifier,
    createPrivateIdentifier: factoryCreatePrivateIdentifier,
    createToken: factoryCreateToken,
    createArrayLiteralExpression: factoryCreateArrayLiteralExpression,
    createObjectLiteralExpression: factoryCreateObjectLiteralExpression,
    createPropertyAccessExpression: factoryCreatePropertyAccessExpression,
    createPropertyAccessChain: factoryCreatePropertyAccessChain,
    createElementAccessExpression: factoryCreateElementAccessExpression,
    createElementAccessChain: factoryCreateElementAccessChain,
    createCallExpression: factoryCreateCallExpression,
    createCallChain: factoryCreateCallChain,
    createNewExpression: factoryCreateNewExpression,
    createParenthesizedExpression: factoryCreateParenthesizedExpression,
    createBlock: factoryCreateBlock,
    createVariableStatement: factoryCreateVariableStatement,
    createExpressionStatement: factoryCreateExpressionStatement,
    createIfStatement: factoryCreateIfStatement,
    createWhileStatement: factoryCreateWhileStatement,
    createForStatement: factoryCreateForStatement,
    createForOfStatement: factoryCreateForOfStatement,
    createVariableDeclaration: factoryCreateVariableDeclaration,
    createVariableDeclarationList: factoryCreateVariableDeclarationList
  } ***REMOVED*** factory2;
  var fileName;
  var sourceFlags;
  var sourceText;
  var languageVersion;
  var scriptKind;
  var languageVariant;
  var parseDiagnostics;
  var jsDocDiagnostics;
  var syntaxCursor;
  var currentToken;
  var nodeCount;
  var identifiers;
  var identifierCount;
  var parsingContext;
  var notParenthesizedArrow;
  var contextFlags;
  var topLevel ***REMOVED*** true;
  var parseErrorBeforeNextFinishedNode ***REMOVED*** false;
  function parseSourceFile(fileName2, sourceText2, languageVersion2, syntaxCursor2, setParentNodes ***REMOVED*** false, scriptKind2, setExternalModuleIndicatorOverride, jsDocParsingMode ***REMOVED*** 0 /* ParseAll */) {
    var _a;
    scriptKind2 ***REMOVED*** ensureScriptKind(fileName2, scriptKind2);
    if (scriptKind2 ***REMOVED******REMOVED******REMOVED*** 6 /* JSON */) {
      const result2 ***REMOVED*** parseJsonText2(fileName2, sourceText2, languageVersion2, syntaxCursor2, setParentNodes);
      convertToJson(
        result2,
        (_a ***REMOVED*** result2.statements[0]) ***REMOVED******REMOVED*** null ? void 0 : _a.expression,
        result2.parseDiagnostics,
        /*returnValue*/
        false,
        /*jsonConversionNotifier*/
        void 0
      );
      result2.referencedFiles ***REMOVED*** emptyArray;
      result2.typeReferenceDirectives ***REMOVED*** emptyArray;
      result2.libReferenceDirectives ***REMOVED*** emptyArray;
      result2.amdDependencies ***REMOVED*** emptyArray;
      result2.hasNoDefaultLib ***REMOVED*** false;
      result2.pragmas ***REMOVED*** emptyMap;
      return result2;
    }
    initializeState(fileName2, sourceText2, languageVersion2, syntaxCursor2, scriptKind2, jsDocParsingMode);
    const result ***REMOVED*** parseSourceFileWorker(languageVersion2, setParentNodes, scriptKind2, setExternalModuleIndicatorOverride || setExternalModuleIndicator, jsDocParsingMode);
    clearState();
    return result;
  }
  Parser2.parseSourceFile ***REMOVED*** parseSourceFile;
  function parseIsolatedEntityName2(content, languageVersion2) {
    initializeState(
      "",
      content,
      languageVersion2,
      /*syntaxCursor*/
      void 0,
      1 /* JS */,
      0 /* ParseAll */
    );
    nextToken();
    const entityName ***REMOVED*** parseEntityName(
      /*allowReservedWords*/
      true
    );
    const isValid ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */ && !parseDiagnostics.length;
    clearState();
    return isValid ? entityName : void 0;
  }
  Parser2.parseIsolatedEntityName ***REMOVED*** parseIsolatedEntityName2;
  function parseJsonText2(fileName2, sourceText2, languageVersion2 ***REMOVED*** 2 /* ES2015 */, syntaxCursor2, setParentNodes ***REMOVED*** false) {
    initializeState(fileName2, sourceText2, languageVersion2, syntaxCursor2, 6 /* JSON */, 0 /* ParseAll */);
    sourceFlags ***REMOVED*** contextFlags;
    nextToken();
    const pos ***REMOVED*** getNodePos();
    let statements, endOfFileToken;
    if (token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
      statements ***REMOVED*** createNodeArray([], pos, pos);
      endOfFileToken ***REMOVED*** parseTokenNode();
    } else {
      let expressions;
      while (token() !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
        let expression2;
        switch (token()) {
          case 23 /* OpenBracketToken */:
            expression2 ***REMOVED*** parseArrayLiteralExpression();
            break;
          case 112 /* TrueKeyword */:
          case 97 /* FalseKeyword */:
          case 106 /* NullKeyword */:
            expression2 ***REMOVED*** parseTokenNode();
            break;
          case 41 /* MinusToken */:
            if (lookAhead(() ***REMOVED***> nextToken() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ && nextToken() !***REMOVED******REMOVED*** 59 /* ColonToken */)) {
              expression2 ***REMOVED*** parsePrefixUnaryExpression();
            } else {
              expression2 ***REMOVED*** parseObjectLiteralExpression();
            }
            break;
          case 9 /* NumericLiteral */:
          case 11 /* StringLiteral */:
            if (lookAhead(() ***REMOVED***> nextToken() !***REMOVED******REMOVED*** 59 /* ColonToken */)) {
              expression2 ***REMOVED*** parseLiteralNode();
              break;
            }
          // falls through
          default:
            expression2 ***REMOVED*** parseObjectLiteralExpression();
            break;
        }
        if (expressions && isArray(expressions)) {
          expressions.push(expression2);
        } else if (expressions) {
          expressions ***REMOVED*** [expressions, expression2];
        } else {
          expressions ***REMOVED*** expression2;
          if (token() !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
            parseErrorAtCurrentToken(Diagnostics.Unexpected_token);
          }
        }
      }
      const expression ***REMOVED*** isArray(expressions) ? finishNode(factoryCreateArrayLiteralExpression(expressions), pos) : Debug.checkDefined(expressions);
      const statement ***REMOVED*** factoryCreateExpressionStatement(expression);
      finishNode(statement, pos);
      statements ***REMOVED*** createNodeArray([statement], pos);
      endOfFileToken ***REMOVED*** parseExpectedToken(1 /* EndOfFileToken */, Diagnostics.Unexpected_token);
    }
    const sourceFile ***REMOVED*** createSourceFile2(
      fileName2,
      2 /* ES2015 */,
      6 /* JSON */,
      /*isDeclarationFile*/
      false,
      statements,
      endOfFileToken,
      sourceFlags,
      noop
    );
    if (setParentNodes) {
      fixupParentReferences(sourceFile);
    }
    sourceFile.nodeCount ***REMOVED*** nodeCount;
    sourceFile.identifierCount ***REMOVED*** identifierCount;
    sourceFile.identifiers ***REMOVED*** identifiers;
    sourceFile.parseDiagnostics ***REMOVED*** attachFileToDiagnostics(parseDiagnostics, sourceFile);
    if (jsDocDiagnostics) {
      sourceFile.jsDocDiagnostics ***REMOVED*** attachFileToDiagnostics(jsDocDiagnostics, sourceFile);
    }
    const result ***REMOVED*** sourceFile;
    clearState();
    return result;
  }
  Parser2.parseJsonText ***REMOVED*** parseJsonText2;
  function initializeState(_fileName, _sourceText, _languageVersion, _syntaxCursor, _scriptKind, _jsDocParsingMode) {
    NodeConstructor2 ***REMOVED*** objectAllocator.getNodeConstructor();
    TokenConstructor2 ***REMOVED*** objectAllocator.getTokenConstructor();
    IdentifierConstructor2 ***REMOVED*** objectAllocator.getIdentifierConstructor();
    PrivateIdentifierConstructor2 ***REMOVED*** objectAllocator.getPrivateIdentifierConstructor();
    SourceFileConstructor2 ***REMOVED*** objectAllocator.getSourceFileConstructor();
    fileName ***REMOVED*** normalizePath(_fileName);
    sourceText ***REMOVED*** _sourceText;
    languageVersion ***REMOVED*** _languageVersion;
    syntaxCursor ***REMOVED*** _syntaxCursor;
    scriptKind ***REMOVED*** _scriptKind;
    languageVariant ***REMOVED*** getLanguageVariant(_scriptKind);
    parseDiagnostics ***REMOVED*** [];
    parsingContext ***REMOVED*** 0;
    identifiers ***REMOVED*** /* @__PURE__ */ new Map();
    identifierCount ***REMOVED*** 0;
    nodeCount ***REMOVED*** 0;
    sourceFlags ***REMOVED*** 0;
    topLevel ***REMOVED*** true;
    switch (scriptKind) {
      case 1 /* JS */:
      case 2 /* JSX */:
        contextFlags ***REMOVED*** 524288 /* JavaScriptFile */;
        break;
      case 6 /* JSON */:
        contextFlags ***REMOVED*** 524288 /* JavaScriptFile */ | 134217728 /* JsonFile */;
        break;
      default:
        contextFlags ***REMOVED*** 0 /* None */;
        break;
    }
    parseErrorBeforeNextFinishedNode ***REMOVED*** false;
    scanner.setText(sourceText);
    scanner.setOnError(scanError);
    scanner.setScriptTarget(languageVersion);
    scanner.setLanguageVariant(languageVariant);
    scanner.setScriptKind(scriptKind);
    scanner.setJSDocParsingMode(_jsDocParsingMode);
  }
  function clearState() {
    scanner.clearCommentDirectives();
    scanner.setText("");
    scanner.setOnError(void 0);
    scanner.setScriptKind(0 /* Unknown */);
    scanner.setJSDocParsingMode(0 /* ParseAll */);
    sourceText ***REMOVED*** void 0;
    languageVersion ***REMOVED*** void 0;
    syntaxCursor ***REMOVED*** void 0;
    scriptKind ***REMOVED*** void 0;
    languageVariant ***REMOVED*** void 0;
    sourceFlags ***REMOVED*** 0;
    parseDiagnostics ***REMOVED*** void 0;
    jsDocDiagnostics ***REMOVED*** void 0;
    parsingContext ***REMOVED*** 0;
    identifiers ***REMOVED*** void 0;
    notParenthesizedArrow ***REMOVED*** void 0;
    topLevel ***REMOVED*** true;
  }
  function parseSourceFileWorker(languageVersion2, setParentNodes, scriptKind2, setExternalModuleIndicator2, jsDocParsingMode) {
    const isDeclarationFile ***REMOVED*** isDeclarationFileName(fileName);
    if (isDeclarationFile) {
      contextFlags |***REMOVED*** 33554432 /* Ambient */;
    }
    sourceFlags ***REMOVED*** contextFlags;
    nextToken();
    const statements ***REMOVED*** parseList(0 /* SourceElements */, parseStatement);
    Debug.assert(token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */);
    const endHasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const endOfFileToken ***REMOVED*** withJSDoc(parseTokenNode(), endHasJSDoc);
    const sourceFile ***REMOVED*** createSourceFile2(fileName, languageVersion2, scriptKind2, isDeclarationFile, statements, endOfFileToken, sourceFlags, setExternalModuleIndicator2);
    processCommentPragmas(sourceFile, sourceText);
    processPragmasIntoFields(sourceFile, reportPragmaDiagnostic);
    sourceFile.commentDirectives ***REMOVED*** scanner.getCommentDirectives();
    sourceFile.nodeCount ***REMOVED*** nodeCount;
    sourceFile.identifierCount ***REMOVED*** identifierCount;
    sourceFile.identifiers ***REMOVED*** identifiers;
    sourceFile.parseDiagnostics ***REMOVED*** attachFileToDiagnostics(parseDiagnostics, sourceFile);
    sourceFile.jsDocParsingMode ***REMOVED*** jsDocParsingMode;
    if (jsDocDiagnostics) {
      sourceFile.jsDocDiagnostics ***REMOVED*** attachFileToDiagnostics(jsDocDiagnostics, sourceFile);
    }
    if (setParentNodes) {
      fixupParentReferences(sourceFile);
    }
    return sourceFile;
    function reportPragmaDiagnostic(pos, end, diagnostic) {
      parseDiagnostics.push(createDetachedDiagnostic(fileName, sourceText, pos, end, diagnostic));
    }
  }
  let hasDeprecatedTag ***REMOVED*** false;
  function withJSDoc(node, hasJSDoc) {
    if (!hasJSDoc) {
      return node;
    }
    Debug.assert(!node.jsDoc);
    const jsDoc ***REMOVED*** mapDefined(getJSDocCommentRanges(node, sourceText), (comment) ***REMOVED***> JSDocParser.parseJSDocComment(node, comment.pos, comment.end - comment.pos));
    if (jsDoc.length) node.jsDoc ***REMOVED*** jsDoc;
    if (hasDeprecatedTag) {
      hasDeprecatedTag ***REMOVED*** false;
      node.flags |***REMOVED*** 536870912 /* Deprecated */;
    }
    return node;
  }
  function reparseTopLevelAwait(sourceFile) {
    const savedSyntaxCursor ***REMOVED*** syntaxCursor;
    const baseSyntaxCursor ***REMOVED*** IncrementalParser.createSyntaxCursor(sourceFile);
    syntaxCursor ***REMOVED*** { currentNode: currentNode2 };
    const statements ***REMOVED*** [];
    const savedParseDiagnostics ***REMOVED*** parseDiagnostics;
    parseDiagnostics ***REMOVED*** [];
    let pos ***REMOVED*** 0;
    let start ***REMOVED*** findNextStatementWithAwait(sourceFile.statements, 0);
    while (start !***REMOVED******REMOVED*** -1) {
      const prevStatement ***REMOVED*** sourceFile.statements[pos];
      const nextStatement ***REMOVED*** sourceFile.statements[start];
      addRange(statements, sourceFile.statements, pos, start);
      pos ***REMOVED*** findNextStatementWithoutAwait(sourceFile.statements, start);
      const diagnosticStart ***REMOVED*** findIndex(savedParseDiagnostics, (diagnostic) ***REMOVED***> diagnostic.start >***REMOVED*** prevStatement.pos);
      const diagnosticEnd ***REMOVED*** diagnosticStart >***REMOVED*** 0 ? findIndex(savedParseDiagnostics, (diagnostic) ***REMOVED***> diagnostic.start >***REMOVED*** nextStatement.pos, diagnosticStart) : -1;
      if (diagnosticStart >***REMOVED*** 0) {
        addRange(parseDiagnostics, savedParseDiagnostics, diagnosticStart, diagnosticEnd >***REMOVED*** 0 ? diagnosticEnd : void 0);
      }
      speculationHelper(() ***REMOVED***> {
        const savedContextFlags ***REMOVED*** contextFlags;
        contextFlags |***REMOVED*** 65536 /* AwaitContext */;
        scanner.resetTokenState(nextStatement.pos);
        nextToken();
        while (token() !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
          const startPos ***REMOVED*** scanner.getTokenFullStart();
          const statement ***REMOVED*** parseListElement(0 /* SourceElements */, parseStatement);
          statements.push(statement);
          if (startPos ***REMOVED******REMOVED******REMOVED*** scanner.getTokenFullStart()) {
            nextToken();
          }
          if (pos >***REMOVED*** 0) {
            const nonAwaitStatement ***REMOVED*** sourceFile.statements[pos];
            if (statement.end ***REMOVED******REMOVED******REMOVED*** nonAwaitStatement.pos) {
              break;
            }
            if (statement.end > nonAwaitStatement.pos) {
              pos ***REMOVED*** findNextStatementWithoutAwait(sourceFile.statements, pos + 1);
            }
          }
        }
        contextFlags ***REMOVED*** savedContextFlags;
      }, 2 /* Reparse */);
      start ***REMOVED*** pos >***REMOVED*** 0 ? findNextStatementWithAwait(sourceFile.statements, pos) : -1;
    }
    if (pos >***REMOVED*** 0) {
      const prevStatement ***REMOVED*** sourceFile.statements[pos];
      addRange(statements, sourceFile.statements, pos);
      const diagnosticStart ***REMOVED*** findIndex(savedParseDiagnostics, (diagnostic) ***REMOVED***> diagnostic.start >***REMOVED*** prevStatement.pos);
      if (diagnosticStart >***REMOVED*** 0) {
        addRange(parseDiagnostics, savedParseDiagnostics, diagnosticStart);
      }
    }
    syntaxCursor ***REMOVED*** savedSyntaxCursor;
    return factory2.updateSourceFile(sourceFile, setTextRange(factoryCreateNodeArray(statements), sourceFile.statements));
    function containsPossibleTopLevelAwait(node) {
      return !(node.flags & 65536 /* AwaitContext */) && !!(node.transformFlags & 67108864 /* ContainsPossibleTopLevelAwait */);
    }
    function findNextStatementWithAwait(statements2, start2) {
      for (let i ***REMOVED*** start2; i < statements2.length; i++) {
        if (containsPossibleTopLevelAwait(statements2[i])) {
          return i;
        }
      }
      return -1;
    }
    function findNextStatementWithoutAwait(statements2, start2) {
      for (let i ***REMOVED*** start2; i < statements2.length; i++) {
        if (!containsPossibleTopLevelAwait(statements2[i])) {
          return i;
        }
      }
      return -1;
    }
    function currentNode2(position) {
      const node ***REMOVED*** baseSyntaxCursor.currentNode(position);
      if (topLevel && node && containsPossibleTopLevelAwait(node)) {
        markAsIntersectingIncrementalChange(node);
      }
      return node;
    }
  }
  function fixupParentReferences(rootNode) {
    setParentRecursive(
      rootNode,
      /*incremental*/
      true
    );
  }
  Parser2.fixupParentReferences ***REMOVED*** fixupParentReferences;
  function createSourceFile2(fileName2, languageVersion2, scriptKind2, isDeclarationFile, statements, endOfFileToken, flags, setExternalModuleIndicator2) {
    let sourceFile ***REMOVED*** factory2.createSourceFile(statements, endOfFileToken, flags);
    setTextRangePosWidth(sourceFile, 0, sourceText.length);
    setFields(sourceFile);
    if (!isDeclarationFile && isExternalModule(sourceFile) && sourceFile.transformFlags & 67108864 /* ContainsPossibleTopLevelAwait */) {
      const oldSourceFile ***REMOVED*** sourceFile;
      sourceFile ***REMOVED*** reparseTopLevelAwait(sourceFile);
      if (oldSourceFile !***REMOVED******REMOVED*** sourceFile) setFields(sourceFile);
    }
    return sourceFile;
    function setFields(sourceFile2) {
      sourceFile2.text ***REMOVED*** sourceText;
      sourceFile2.bindDiagnostics ***REMOVED*** [];
      sourceFile2.bindSuggestionDiagnostics ***REMOVED*** void 0;
      sourceFile2.languageVersion ***REMOVED*** languageVersion2;
      sourceFile2.fileName ***REMOVED*** fileName2;
      sourceFile2.languageVariant ***REMOVED*** getLanguageVariant(scriptKind2);
      sourceFile2.isDeclarationFile ***REMOVED*** isDeclarationFile;
      sourceFile2.scriptKind ***REMOVED*** scriptKind2;
      setExternalModuleIndicator2(sourceFile2);
      sourceFile2.setExternalModuleIndicator ***REMOVED*** setExternalModuleIndicator2;
    }
  }
  function setContextFlag(val, flag) {
    if (val) {
      contextFlags |***REMOVED*** flag;
    } else {
      contextFlags &***REMOVED*** ~flag;
    }
  }
  function setDisallowInContext(val) {
    setContextFlag(val, 8192 /* DisallowInContext */);
  }
  function setYieldContext(val) {
    setContextFlag(val, 16384 /* YieldContext */);
  }
  function setDecoratorContext(val) {
    setContextFlag(val, 32768 /* DecoratorContext */);
  }
  function setAwaitContext(val) {
    setContextFlag(val, 65536 /* AwaitContext */);
  }
  function doOutsideOfContext(context, func) {
    const contextFlagsToClear ***REMOVED*** context & contextFlags;
    if (contextFlagsToClear) {
      setContextFlag(
        /*val*/
        false,
        contextFlagsToClear
      );
      const result ***REMOVED*** func();
      setContextFlag(
        /*val*/
        true,
        contextFlagsToClear
      );
      return result;
    }
    return func();
  }
  function doInsideOfContext(context, func) {
    const contextFlagsToSet ***REMOVED*** context & ~contextFlags;
    if (contextFlagsToSet) {
      setContextFlag(
        /*val*/
        true,
        contextFlagsToSet
      );
      const result ***REMOVED*** func();
      setContextFlag(
        /*val*/
        false,
        contextFlagsToSet
      );
      return result;
    }
    return func();
  }
  function allowInAnd(func) {
    return doOutsideOfContext(8192 /* DisallowInContext */, func);
  }
  function disallowInAnd(func) {
    return doInsideOfContext(8192 /* DisallowInContext */, func);
  }
  function allowConditionalTypesAnd(func) {
    return doOutsideOfContext(131072 /* DisallowConditionalTypesContext */, func);
  }
  function disallowConditionalTypesAnd(func) {
    return doInsideOfContext(131072 /* DisallowConditionalTypesContext */, func);
  }
  function doInYieldContext(func) {
    return doInsideOfContext(16384 /* YieldContext */, func);
  }
  function doInDecoratorContext(func) {
    return doInsideOfContext(32768 /* DecoratorContext */, func);
  }
  function doInAwaitContext(func) {
    return doInsideOfContext(65536 /* AwaitContext */, func);
  }
  function doOutsideOfAwaitContext(func) {
    return doOutsideOfContext(65536 /* AwaitContext */, func);
  }
  function doInYieldAndAwaitContext(func) {
    return doInsideOfContext(16384 /* YieldContext */ | 65536 /* AwaitContext */, func);
  }
  function doOutsideOfYieldAndAwaitContext(func) {
    return doOutsideOfContext(16384 /* YieldContext */ | 65536 /* AwaitContext */, func);
  }
  function inContext(flags) {
    return (contextFlags & flags) !***REMOVED******REMOVED*** 0;
  }
  function inYieldContext() {
    return inContext(16384 /* YieldContext */);
  }
  function inDisallowInContext() {
    return inContext(8192 /* DisallowInContext */);
  }
  function inDisallowConditionalTypesContext() {
    return inContext(131072 /* DisallowConditionalTypesContext */);
  }
  function inDecoratorContext() {
    return inContext(32768 /* DecoratorContext */);
  }
  function inAwaitContext() {
    return inContext(65536 /* AwaitContext */);
  }
  function parseErrorAtCurrentToken(message, ...args) {
    return parseErrorAt(scanner.getTokenStart(), scanner.getTokenEnd(), message, ...args);
  }
  function parseErrorAtPosition(start, length2, message, ...args) {
    const lastError ***REMOVED*** lastOrUndefined(parseDiagnostics);
    let result;
    if (!lastError || start !***REMOVED******REMOVED*** lastError.start) {
      result ***REMOVED*** createDetachedDiagnostic(fileName, sourceText, start, length2, message, ...args);
      parseDiagnostics.push(result);
    }
    parseErrorBeforeNextFinishedNode ***REMOVED*** true;
    return result;
  }
  function parseErrorAt(start, end, message, ...args) {
    return parseErrorAtPosition(start, end - start, message, ...args);
  }
  function parseErrorAtRange(range, message, ...args) {
    parseErrorAt(range.pos, range.end, message, ...args);
  }
  function scanError(message, length2, arg0) {
    parseErrorAtPosition(scanner.getTokenEnd(), length2, message, arg0);
  }
  function getNodePos() {
    return scanner.getTokenFullStart();
  }
  function hasPrecedingJSDocComment() {
    return scanner.hasPrecedingJSDocComment();
  }
  function token() {
    return currentToken;
  }
  function nextTokenWithoutCheck() {
    return currentToken ***REMOVED*** scanner.scan();
  }
  function nextTokenAnd(func) {
    nextToken();
    return func();
  }
  function nextToken() {
    if (isKeyword(currentToken) && (scanner.hasUnicodeEscape() || scanner.hasExtendedUnicodeEscape())) {
      parseErrorAt(scanner.getTokenStart(), scanner.getTokenEnd(), Diagnostics.Keywords_cannot_contain_escape_characters);
    }
    return nextTokenWithoutCheck();
  }
  function nextTokenJSDoc() {
    return currentToken ***REMOVED*** scanner.scanJsDocToken();
  }
  function nextJSDocCommentTextToken(inBackticks) {
    return currentToken ***REMOVED*** scanner.scanJSDocCommentTextToken(inBackticks);
  }
  function reScanGreaterToken() {
    return currentToken ***REMOVED*** scanner.reScanGreaterToken();
  }
  function reScanSlashToken() {
    return currentToken ***REMOVED*** scanner.reScanSlashToken();
  }
  function reScanTemplateToken(isTaggedTemplate) {
    return currentToken ***REMOVED*** scanner.reScanTemplateToken(isTaggedTemplate);
  }
  function reScanLessThanToken() {
    return currentToken ***REMOVED*** scanner.reScanLessThanToken();
  }
  function reScanHashToken() {
    return currentToken ***REMOVED*** scanner.reScanHashToken();
  }
  function scanJsxIdentifier() {
    return currentToken ***REMOVED*** scanner.scanJsxIdentifier();
  }
  function scanJsxText() {
    return currentToken ***REMOVED*** scanner.scanJsxToken();
  }
  function scanJsxAttributeValue() {
    return currentToken ***REMOVED*** scanner.scanJsxAttributeValue();
  }
  function speculationHelper(callback, speculationKind) {
    const saveToken ***REMOVED*** currentToken;
    const saveParseDiagnosticsLength ***REMOVED*** parseDiagnostics.length;
    const saveParseErrorBeforeNextFinishedNode ***REMOVED*** parseErrorBeforeNextFinishedNode;
    const saveContextFlags ***REMOVED*** contextFlags;
    const result ***REMOVED*** speculationKind !***REMOVED******REMOVED*** 0 /* TryParse */ ? scanner.lookAhead(callback) : scanner.tryScan(callback);
    Debug.assert(saveContextFlags ***REMOVED******REMOVED******REMOVED*** contextFlags);
    if (!result || speculationKind !***REMOVED******REMOVED*** 0 /* TryParse */) {
      currentToken ***REMOVED*** saveToken;
      if (speculationKind !***REMOVED******REMOVED*** 2 /* Reparse */) {
        parseDiagnostics.length ***REMOVED*** saveParseDiagnosticsLength;
      }
      parseErrorBeforeNextFinishedNode ***REMOVED*** saveParseErrorBeforeNextFinishedNode;
    }
    return result;
  }
  function lookAhead(callback) {
    return speculationHelper(callback, 1 /* Lookahead */);
  }
  function tryParse(callback) {
    return speculationHelper(callback, 0 /* TryParse */);
  }
  function isBindingIdentifier() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return true;
    }
    return token() > 118 /* LastReservedWord */;
  }
  function isIdentifier2() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return true;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 127 /* YieldKeyword */ && inYieldContext()) {
      return false;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */ && inAwaitContext()) {
      return false;
    }
    return token() > 118 /* LastReservedWord */;
  }
  function parseExpected(kind, diagnosticMessage, shouldAdvance ***REMOVED*** true) {
    if (token() ***REMOVED******REMOVED******REMOVED*** kind) {
      if (shouldAdvance) {
        nextToken();
      }
      return true;
    }
    if (diagnosticMessage) {
      parseErrorAtCurrentToken(diagnosticMessage);
    } else {
      parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(kind));
    }
    return false;
  }
  const viableKeywordSuggestions ***REMOVED*** Object.keys(textToKeywordObj).filter((keyword) ***REMOVED***> keyword.length > 2);
  function parseErrorForMissingSemicolonAfter(node) {
    if (isTaggedTemplateExpression(node)) {
      parseErrorAt(skipTrivia(sourceText, node.template.pos), node.template.end, Diagnostics.Module_declaration_names_may_only_use_or_quoted_strings);
      return;
    }
    const expressionText ***REMOVED*** isIdentifier(node) ? idText(node) : void 0;
    if (!expressionText || !isIdentifierText(expressionText, languageVersion)) {
      parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(27 /* SemicolonToken */));
      return;
    }
    const pos ***REMOVED*** skipTrivia(sourceText, node.pos);
    switch (expressionText) {
      case "const":
      case "let":
      case "var":
        parseErrorAt(pos, node.end, Diagnostics.Variable_declaration_not_allowed_at_this_location);
        return;
      case "declare":
        return;
      case "interface":
        parseErrorForInvalidName(Diagnostics.Interface_name_cannot_be_0, Diagnostics.Interface_must_be_given_a_name, 19 /* OpenBraceToken */);
        return;
      case "is":
        parseErrorAt(pos, scanner.getTokenStart(), Diagnostics.A_type_predicate_is_only_allowed_in_return_type_position_for_functions_and_methods);
        return;
      case "module":
      case "namespace":
        parseErrorForInvalidName(Diagnostics.Namespace_name_cannot_be_0, Diagnostics.Namespace_must_be_given_a_name, 19 /* OpenBraceToken */);
        return;
      case "type":
        parseErrorForInvalidName(Diagnostics.Type_alias_name_cannot_be_0, Diagnostics.Type_alias_must_be_given_a_name, 64 /* EqualsToken */);
        return;
    }
    const suggestion ***REMOVED*** getSpellingSuggestion(expressionText, viableKeywordSuggestions, identity) ?? getSpaceSuggestion(expressionText);
    if (suggestion) {
      parseErrorAt(pos, node.end, Diagnostics.Unknown_keyword_or_identifier_Did_you_mean_0, suggestion);
      return;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 0 /* Unknown */) {
      return;
    }
    parseErrorAt(pos, node.end, Diagnostics.Unexpected_keyword_or_identifier);
  }
  function parseErrorForInvalidName(nameDiagnostic, blankDiagnostic, tokenIfBlankName) {
    if (token() ***REMOVED******REMOVED******REMOVED*** tokenIfBlankName) {
      parseErrorAtCurrentToken(blankDiagnostic);
    } else {
      parseErrorAtCurrentToken(nameDiagnostic, scanner.getTokenValue());
    }
  }
  function getSpaceSuggestion(expressionText) {
    for (const keyword of viableKeywordSuggestions) {
      if (expressionText.length > keyword.length + 2 && startsWith(expressionText, keyword)) {
        return `${keyword} ${expressionText.slice(keyword.length)}`;
      }
    }
    return void 0;
  }
  function parseSemicolonAfterPropertyName(name, type, initializer) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ && !scanner.hasPrecedingLineBreak()) {
      parseErrorAtCurrentToken(Diagnostics.Decorators_must_precede_the_name_and_all_keywords_of_property_declarations);
      return;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */) {
      parseErrorAtCurrentToken(Diagnostics.Cannot_start_a_function_call_in_a_type_annotation);
      nextToken();
      return;
    }
    if (type && !canParseSemicolon()) {
      if (initializer) {
        parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(27 /* SemicolonToken */));
      } else {
        parseErrorAtCurrentToken(Diagnostics.Expected_for_property_initializer);
      }
      return;
    }
    if (tryParseSemicolon()) {
      return;
    }
    if (initializer) {
      parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(27 /* SemicolonToken */));
      return;
    }
    parseErrorForMissingSemicolonAfter(name);
  }
  function parseExpectedJSDoc(kind) {
    if (token() ***REMOVED******REMOVED******REMOVED*** kind) {
      nextTokenJSDoc();
      return true;
    }
    Debug.assert(isKeywordOrPunctuation(kind));
    parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(kind));
    return false;
  }
  function parseExpectedMatchingBrackets(openKind, closeKind, openParsed, openPosition) {
    if (token() ***REMOVED******REMOVED******REMOVED*** closeKind) {
      nextToken();
      return;
    }
    const lastError ***REMOVED*** parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(closeKind));
    if (!openParsed) {
      return;
    }
    if (lastError) {
      addRelatedInfo(
        lastError,
        createDetachedDiagnostic(fileName, sourceText, openPosition, 1, Diagnostics.The_parser_expected_to_find_a_1_to_match_the_0_token_here, tokenToString(openKind), tokenToString(closeKind))
      );
    }
  }
  function parseOptional(t) {
    if (token() ***REMOVED******REMOVED******REMOVED*** t) {
      nextToken();
      return true;
    }
    return false;
  }
  function parseOptionalToken(t) {
    if (token() ***REMOVED******REMOVED******REMOVED*** t) {
      return parseTokenNode();
    }
    return void 0;
  }
  function parseOptionalTokenJSDoc(t) {
    if (token() ***REMOVED******REMOVED******REMOVED*** t) {
      return parseTokenNodeJSDoc();
    }
    return void 0;
  }
  function parseExpectedToken(t, diagnosticMessage, arg0) {
    return parseOptionalToken(t) || createMissingNode(
      t,
      /*reportAtCurrentPosition*/
      false,
      diagnosticMessage || Diagnostics._0_expected,
      arg0 || tokenToString(t)
    );
  }
  function parseExpectedTokenJSDoc(t) {
    const optional ***REMOVED*** parseOptionalTokenJSDoc(t);
    if (optional) return optional;
    Debug.assert(isKeywordOrPunctuation(t));
    return createMissingNode(
      t,
      /*reportAtCurrentPosition*/
      false,
      Diagnostics._0_expected,
      tokenToString(t)
    );
  }
  function parseTokenNode() {
    const pos ***REMOVED*** getNodePos();
    const kind ***REMOVED*** token();
    nextToken();
    return finishNode(factoryCreateToken(kind), pos);
  }
  function parseTokenNodeJSDoc() {
    const pos ***REMOVED*** getNodePos();
    const kind ***REMOVED*** token();
    nextTokenJSDoc();
    return finishNode(factoryCreateToken(kind), pos);
  }
  function canParseSemicolon() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */) {
      return true;
    }
    return token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */ || scanner.hasPrecedingLineBreak();
  }
  function tryParseSemicolon() {
    if (!canParseSemicolon()) {
      return false;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */) {
      nextToken();
    }
    return true;
  }
  function parseSemicolon() {
    return tryParseSemicolon() || parseExpected(27 /* SemicolonToken */);
  }
  function createNodeArray(elements, pos, end, hasTrailingComma) {
    const array ***REMOVED*** factoryCreateNodeArray(elements, hasTrailingComma);
    setTextRangePosEnd(array, pos, end ?? scanner.getTokenFullStart());
    return array;
  }
  function finishNode(node, pos, end) {
    setTextRangePosEnd(node, pos, end ?? scanner.getTokenFullStart());
    if (contextFlags) {
      node.flags |***REMOVED*** contextFlags;
    }
    if (parseErrorBeforeNextFinishedNode) {
      parseErrorBeforeNextFinishedNode ***REMOVED*** false;
      node.flags |***REMOVED*** 262144 /* ThisNodeHasError */;
    }
    return node;
  }
  function createMissingNode(kind, reportAtCurrentPosition, diagnosticMessage, ...args) {
    if (reportAtCurrentPosition) {
      parseErrorAtPosition(scanner.getTokenFullStart(), 0, diagnosticMessage, ...args);
    } else if (diagnosticMessage) {
      parseErrorAtCurrentToken(diagnosticMessage, ...args);
    }
    const pos ***REMOVED*** getNodePos();
    const result ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? factoryCreateIdentifier(
      "",
      /*originalKeywordKind*/
      void 0
    ) : isTemplateLiteralKind(kind) ? factory2.createTemplateLiteralLikeNode(
      kind,
      "",
      "",
      /*templateFlags*/
      void 0
    ) : kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ ? factoryCreateNumericLiteral(
      "",
      /*numericLiteralFlags*/
      void 0
    ) : kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factoryCreateStringLiteral(
      "",
      /*isSingleQuote*/
      void 0
    ) : kind ***REMOVED******REMOVED******REMOVED*** 283 /* MissingDeclaration */ ? factory2.createMissingDeclaration() : factoryCreateToken(kind);
    return finishNode(result, pos);
  }
  function internIdentifier(text) {
    let identifier ***REMOVED*** identifiers.get(text);
    if (identifier ***REMOVED******REMOVED******REMOVED*** void 0) {
      identifiers.set(text, identifier ***REMOVED*** text);
    }
    return identifier;
  }
  function createIdentifier(isIdentifier3, diagnosticMessage, privateIdentifierDiagnosticMessage) {
    if (isIdentifier3) {
      identifierCount++;
      const pos ***REMOVED*** scanner.hasPrecedingJSDocLeadingAsterisks() ? scanner.getTokenStart() : getNodePos();
      const originalKeywordKind ***REMOVED*** token();
      const text ***REMOVED*** internIdentifier(scanner.getTokenValue());
      const hasExtendedUnicodeEscape ***REMOVED*** scanner.hasExtendedUnicodeEscape();
      nextTokenWithoutCheck();
      return finishNode(factoryCreateIdentifier(text, originalKeywordKind, hasExtendedUnicodeEscape), pos);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
      parseErrorAtCurrentToken(privateIdentifierDiagnosticMessage || Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
      return createIdentifier(
        /*isIdentifier*/
        true
      );
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 0 /* Unknown */ && scanner.tryScan(() ***REMOVED***> scanner.reScanInvalidIdentifier() ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */)) {
      return createIdentifier(
        /*isIdentifier*/
        true
      );
    }
    identifierCount++;
    const reportAtCurrentPosition ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */;
    const isReservedWord ***REMOVED*** scanner.isReservedWord();
    const msgArg ***REMOVED*** scanner.getTokenText();
    const defaultMessage ***REMOVED*** isReservedWord ? Diagnostics.Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here : Diagnostics.Identifier_expected;
    return createMissingNode(80 /* Identifier */, reportAtCurrentPosition, diagnosticMessage || defaultMessage, msgArg);
  }
  function parseBindingIdentifier(privateIdentifierDiagnosticMessage) {
    return createIdentifier(
      isBindingIdentifier(),
      /*diagnosticMessage*/
      void 0,
      privateIdentifierDiagnosticMessage
    );
  }
  function parseIdentifier(diagnosticMessage, privateIdentifierDiagnosticMessage) {
    return createIdentifier(isIdentifier2(), diagnosticMessage, privateIdentifierDiagnosticMessage);
  }
  function parseIdentifierName(diagnosticMessage) {
    return createIdentifier(tokenIsIdentifierOrKeyword(token()), diagnosticMessage);
  }
  function parseIdentifierNameErrorOnUnicodeEscapeSequence() {
    if (scanner.hasUnicodeEscape() || scanner.hasExtendedUnicodeEscape()) {
      parseErrorAtCurrentToken(Diagnostics.Unicode_escape_sequence_cannot_appear_here);
    }
    return createIdentifier(tokenIsIdentifierOrKeyword(token()));
  }
  function isLiteralPropertyName() {
    return tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */;
  }
  function isImportAttributeName2() {
    return tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
  }
  function parsePropertyNameWorker(allowComputedPropertyNames) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */) {
      const node ***REMOVED*** parseLiteralNode();
      node.text ***REMOVED*** internIdentifier(node.text);
      return node;
    }
    if (allowComputedPropertyNames && token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      return parseComputedPropertyName();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
      return parsePrivateIdentifier();
    }
    return parseIdentifierName();
  }
  function parsePropertyName() {
    return parsePropertyNameWorker(
      /*allowComputedPropertyNames*/
      true
    );
  }
  function parseComputedPropertyName() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(23 /* OpenBracketToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpected(24 /* CloseBracketToken */);
    return finishNode(factory2.createComputedPropertyName(expression), pos);
  }
  function parsePrivateIdentifier() {
    const pos ***REMOVED*** getNodePos();
    const node ***REMOVED*** factoryCreatePrivateIdentifier(internIdentifier(scanner.getTokenValue()));
    nextToken();
    return finishNode(node, pos);
  }
  function parseContextualModifier(t) {
    return token() ***REMOVED******REMOVED******REMOVED*** t && tryParse(nextTokenCanFollowModifier);
  }
  function nextTokenIsOnSameLineAndCanFollowModifier() {
    nextToken();
    if (scanner.hasPrecedingLineBreak()) {
      return false;
    }
    return canFollowModifier();
  }
  function nextTokenCanFollowModifier() {
    switch (token()) {
      case 87 /* ConstKeyword */:
        return nextToken() ***REMOVED******REMOVED******REMOVED*** 94 /* EnumKeyword */;
      case 95 /* ExportKeyword */:
        nextToken();
        if (token() ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */) {
          return lookAhead(nextTokenCanFollowDefaultKeyword);
        }
        if (token() ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */) {
          return lookAhead(nextTokenCanFollowExportModifier);
        }
        return canFollowExportModifier();
      case 90 /* DefaultKeyword */:
        return nextTokenCanFollowDefaultKeyword();
      case 126 /* StaticKeyword */:
        nextToken();
        return canFollowModifier();
      case 139 /* GetKeyword */:
      case 153 /* SetKeyword */:
        nextToken();
        return canFollowGetOrSetKeyword();
      default:
        return nextTokenIsOnSameLineAndCanFollowModifier();
    }
  }
  function canFollowExportModifier() {
    return token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ || token() !***REMOVED******REMOVED*** 42 /* AsteriskToken */ && token() !***REMOVED******REMOVED*** 130 /* AsKeyword */ && token() !***REMOVED******REMOVED*** 19 /* OpenBraceToken */ && canFollowModifier();
  }
  function nextTokenCanFollowExportModifier() {
    nextToken();
    return canFollowExportModifier();
  }
  function parseAnyContextualModifier() {
    return isModifierKind(token()) && tryParse(nextTokenCanFollowModifier);
  }
  function canFollowModifier() {
    return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || isLiteralPropertyName();
  }
  function canFollowGetOrSetKeyword() {
    return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || isLiteralPropertyName();
  }
  function nextTokenCanFollowDefaultKeyword() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 86 /* ClassKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 100 /* FunctionKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 120 /* InterfaceKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ || token() ***REMOVED******REMOVED******REMOVED*** 128 /* AbstractKeyword */ && lookAhead(nextTokenIsClassKeywordOnSameLine) || token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */ && lookAhead(nextTokenIsFunctionKeywordOnSameLine);
  }
  function isListElement(parsingContext2, inErrorRecovery) {
    const node ***REMOVED*** currentNode(parsingContext2);
    if (node) {
      return true;
    }
    switch (parsingContext2) {
      case 0 /* SourceElements */:
      case 1 /* BlockStatements */:
      case 3 /* SwitchClauseStatements */:
        return !(token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */ && inErrorRecovery) && isStartOfStatement();
      case 2 /* SwitchClauses */:
        return token() ***REMOVED******REMOVED******REMOVED*** 84 /* CaseKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */;
      case 4 /* TypeMembers */:
        return lookAhead(isTypeMemberStart);
      case 5 /* ClassMembers */:
        return lookAhead(isClassMemberStart) || token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */ && !inErrorRecovery;
      case 6 /* EnumMembers */:
        return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || isLiteralPropertyName();
      case 12 /* ObjectLiteralMembers */:
        switch (token()) {
          case 23 /* OpenBracketToken */:
          case 42 /* AsteriskToken */:
          case 26 /* DotDotDotToken */:
          case 25 /* DotToken */:
            return true;
          default:
            return isLiteralPropertyName();
        }
      case 18 /* RestProperties */:
        return isLiteralPropertyName();
      case 9 /* ObjectBindingElements */:
        return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || isLiteralPropertyName();
      case 24 /* ImportAttributes */:
        return isImportAttributeName2();
      case 7 /* HeritageClauseElement */:
        if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
          return lookAhead(isValidHeritageClauseObjectLiteral);
        }
        if (!inErrorRecovery) {
          return isStartOfLeftHandSideExpression() && !isHeritageClauseExtendsOrImplementsKeyword();
        } else {
          return isIdentifier2() && !isHeritageClauseExtendsOrImplementsKeyword();
        }
      case 8 /* VariableDeclarations */:
        return isBindingIdentifierOrPrivateIdentifierOrPattern();
      case 10 /* ArrayBindingElements */:
        return token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || isBindingIdentifierOrPrivateIdentifierOrPattern();
      case 19 /* TypeParameters */:
        return token() ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 87 /* ConstKeyword */ || isIdentifier2();
      case 15 /* ArrayLiteralMembers */:
        switch (token()) {
          case 28 /* CommaToken */:
          case 25 /* DotToken */:
            return true;
        }
      // falls through
      case 11 /* ArgumentExpressions */:
        return token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || isStartOfExpression();
      case 16 /* Parameters */:
        return isStartOfParameter(
          /*isJSDocParameter*/
          false
        );
      case 17 /* JSDocParameters */:
        return isStartOfParameter(
          /*isJSDocParameter*/
          true
        );
      case 20 /* TypeArguments */:
      case 21 /* TupleElementTypes */:
        return token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || isStartOfType();
      case 22 /* HeritageClauses */:
        return isHeritageClause2();
      case 23 /* ImportOrExportSpecifiers */:
        if (token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */ && lookAhead(nextTokenIsStringLiteral)) {
          return false;
        }
        if (token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
          return true;
        }
        return tokenIsIdentifierOrKeyword(token());
      case 13 /* JsxAttributes */:
        return tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */;
      case 14 /* JsxChildren */:
        return true;
      case 25 /* JSDocComment */:
        return true;
      case 26 /* Count */:
        return Debug.fail("ParsingContext.Count used as a context");
      // Not a real context, only a marker.
      default:
        Debug.assertNever(parsingContext2, "Non-exhaustive case in 'isListElement'.");
    }
  }
  function isValidHeritageClauseObjectLiteral() {
    Debug.assert(token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */);
    if (nextToken() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */) {
      const next ***REMOVED*** nextToken();
      return next ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || next ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || next ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ || next ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */;
    }
    return true;
  }
  function nextTokenIsIdentifier() {
    nextToken();
    return isIdentifier2();
  }
  function nextTokenIsIdentifierOrKeyword() {
    nextToken();
    return tokenIsIdentifierOrKeyword(token());
  }
  function nextTokenIsIdentifierOrKeywordOrGreaterThan() {
    nextToken();
    return tokenIsIdentifierOrKeywordOrGreaterThan(token());
  }
  function isHeritageClauseExtendsOrImplementsKeyword() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
      return lookAhead(nextTokenIsStartOfExpression);
    }
    return false;
  }
  function nextTokenIsStartOfExpression() {
    nextToken();
    return isStartOfExpression();
  }
  function nextTokenIsStartOfType() {
    nextToken();
    return isStartOfType();
  }
  function isListTerminator(kind) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
      return true;
    }
    switch (kind) {
      case 1 /* BlockStatements */:
      case 2 /* SwitchClauses */:
      case 4 /* TypeMembers */:
      case 5 /* ClassMembers */:
      case 6 /* EnumMembers */:
      case 12 /* ObjectLiteralMembers */:
      case 9 /* ObjectBindingElements */:
      case 23 /* ImportOrExportSpecifiers */:
      case 24 /* ImportAttributes */:
        return token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */;
      case 3 /* SwitchClauseStatements */:
        return token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 84 /* CaseKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */;
      case 7 /* HeritageClauseElement */:
        return token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */;
      case 8 /* VariableDeclarations */:
        return isVariableDeclaratorListTerminator();
      case 19 /* TypeParameters */:
        return token() ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */;
      case 11 /* ArgumentExpressions */:
        return token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */;
      case 15 /* ArrayLiteralMembers */:
      case 21 /* TupleElementTypes */:
      case 10 /* ArrayBindingElements */:
        return token() ***REMOVED******REMOVED******REMOVED*** 24 /* CloseBracketToken */;
      case 17 /* JSDocParameters */:
      case 16 /* Parameters */:
      case 18 /* RestProperties */:
        return token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 24 /* CloseBracketToken */;
      case 20 /* TypeArguments */:
        return token() !***REMOVED******REMOVED*** 28 /* CommaToken */;
      case 22 /* HeritageClauses */:
        return token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */;
      case 13 /* JsxAttributes */:
        return token() ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 44 /* SlashToken */;
      case 14 /* JsxChildren */:
        return token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ && lookAhead(nextTokenIsSlash);
      default:
        return false;
    }
  }
  function isVariableDeclaratorListTerminator() {
    if (canParseSemicolon()) {
      return true;
    }
    if (isInOrOfKeyword(token())) {
      return true;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
      return true;
    }
    return false;
  }
  function isInSomeParsingContext() {
    Debug.assert(parsingContext, "Missing parsing context");
    for (let kind ***REMOVED*** 0; kind < 26 /* Count */; kind++) {
      if (parsingContext & 1 << kind) {
        if (isListElement(
          kind,
          /*inErrorRecovery*/
          true
        ) || isListTerminator(kind)) {
          return true;
        }
      }
    }
    return false;
  }
  function parseList(kind, parseElement) {
    const saveParsingContext ***REMOVED*** parsingContext;
    parsingContext |***REMOVED*** 1 << kind;
    const list ***REMOVED*** [];
    const listPos ***REMOVED*** getNodePos();
    while (!isListTerminator(kind)) {
      if (isListElement(
        kind,
        /*inErrorRecovery*/
        false
      )) {
        list.push(parseListElement(kind, parseElement));
        continue;
      }
      if (abortParsingListOrMoveToNextToken(kind)) {
        break;
      }
    }
    parsingContext ***REMOVED*** saveParsingContext;
    return createNodeArray(list, listPos);
  }
  function parseListElement(parsingContext2, parseElement) {
    const node ***REMOVED*** currentNode(parsingContext2);
    if (node) {
      return consumeNode(node);
    }
    return parseElement();
  }
  function currentNode(parsingContext2, pos) {
    var _a;
    if (!syntaxCursor || !isReusableParsingContext(parsingContext2) || parseErrorBeforeNextFinishedNode) {
      return void 0;
    }
    const node ***REMOVED*** syntaxCursor.currentNode(pos ?? scanner.getTokenFullStart());
    if (nodeIsMissing(node) || intersectsIncrementalChange(node) || containsParseError(node)) {
      return void 0;
    }
    const nodeContextFlags ***REMOVED*** node.flags & 101441536 /* ContextFlags */;
    if (nodeContextFlags !***REMOVED******REMOVED*** contextFlags) {
      return void 0;
    }
    if (!canReuseNode(node, parsingContext2)) {
      return void 0;
    }
    if (canHaveJSDoc(node) && ((_a ***REMOVED*** node.jsDoc) ***REMOVED******REMOVED*** null ? void 0 : _a.jsDocCache)) {
      node.jsDoc.jsDocCache ***REMOVED*** void 0;
    }
    return node;
  }
  function consumeNode(node) {
    scanner.resetTokenState(node.end);
    nextToken();
    return node;
  }
  function isReusableParsingContext(parsingContext2) {
    switch (parsingContext2) {
      case 5 /* ClassMembers */:
      case 2 /* SwitchClauses */:
      case 0 /* SourceElements */:
      case 1 /* BlockStatements */:
      case 3 /* SwitchClauseStatements */:
      case 6 /* EnumMembers */:
      case 4 /* TypeMembers */:
      case 8 /* VariableDeclarations */:
      case 17 /* JSDocParameters */:
      case 16 /* Parameters */:
        return true;
    }
    return false;
  }
  function canReuseNode(node, parsingContext2) {
    switch (parsingContext2) {
      case 5 /* ClassMembers */:
        return isReusableClassMember(node);
      case 2 /* SwitchClauses */:
        return isReusableSwitchClause(node);
      case 0 /* SourceElements */:
      case 1 /* BlockStatements */:
      case 3 /* SwitchClauseStatements */:
        return isReusableStatement(node);
      case 6 /* EnumMembers */:
        return isReusableEnumMember(node);
      case 4 /* TypeMembers */:
        return isReusableTypeMember(node);
      case 8 /* VariableDeclarations */:
        return isReusableVariableDeclaration(node);
      case 17 /* JSDocParameters */:
      case 16 /* Parameters */:
        return isReusableParameter(node);
    }
    return false;
  }
  function isReusableClassMember(node) {
    if (node) {
      switch (node.kind) {
        case 177 /* Constructor */:
        case 182 /* IndexSignature */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
        case 173 /* PropertyDeclaration */:
        case 241 /* SemicolonClassElement */:
          return true;
        case 175 /* MethodDeclaration */:
          const methodDeclaration ***REMOVED*** node;
          const nameIsConstructor ***REMOVED*** methodDeclaration.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && methodDeclaration.name.escapedText ***REMOVED******REMOVED******REMOVED*** "constructor";
          return !nameIsConstructor;
      }
    }
    return false;
  }
  function isReusableSwitchClause(node) {
    if (node) {
      switch (node.kind) {
        case 297 /* CaseClause */:
        case 298 /* DefaultClause */:
          return true;
      }
    }
    return false;
  }
  function isReusableStatement(node) {
    if (node) {
      switch (node.kind) {
        case 263 /* FunctionDeclaration */:
        case 244 /* VariableStatement */:
        case 242 /* Block */:
        case 246 /* IfStatement */:
        case 245 /* ExpressionStatement */:
        case 258 /* ThrowStatement */:
        case 254 /* ReturnStatement */:
        case 256 /* SwitchStatement */:
        case 253 /* BreakStatement */:
        case 252 /* ContinueStatement */:
        case 250 /* ForInStatement */:
        case 251 /* ForOfStatement */:
        case 249 /* ForStatement */:
        case 248 /* WhileStatement */:
        case 255 /* WithStatement */:
        case 243 /* EmptyStatement */:
        case 259 /* TryStatement */:
        case 257 /* LabeledStatement */:
        case 247 /* DoStatement */:
        case 260 /* DebuggerStatement */:
        case 273 /* ImportDeclaration */:
        case 272 /* ImportEqualsDeclaration */:
        case 279 /* ExportDeclaration */:
        case 278 /* ExportAssignment */:
        case 268 /* ModuleDeclaration */:
        case 264 /* ClassDeclaration */:
        case 265 /* InterfaceDeclaration */:
        case 267 /* EnumDeclaration */:
        case 266 /* TypeAliasDeclaration */:
          return true;
      }
    }
    return false;
  }
  function isReusableEnumMember(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 307 /* EnumMember */;
  }
  function isReusableTypeMember(node) {
    if (node) {
      switch (node.kind) {
        case 181 /* ConstructSignature */:
        case 174 /* MethodSignature */:
        case 182 /* IndexSignature */:
        case 172 /* PropertySignature */:
        case 180 /* CallSignature */:
          return true;
      }
    }
    return false;
  }
  function isReusableVariableDeclaration(node) {
    if (node.kind !***REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
      return false;
    }
    const variableDeclarator ***REMOVED*** node;
    return variableDeclarator.initializer ***REMOVED******REMOVED******REMOVED*** void 0;
  }
  function isReusableParameter(node) {
    if (node.kind !***REMOVED******REMOVED*** 170 /* Parameter */) {
      return false;
    }
    const parameter ***REMOVED*** node;
    return parameter.initializer ***REMOVED******REMOVED******REMOVED*** void 0;
  }
  function abortParsingListOrMoveToNextToken(kind) {
    parsingContextErrors(kind);
    if (isInSomeParsingContext()) {
      return true;
    }
    nextToken();
    return false;
  }
  function parsingContextErrors(context) {
    switch (context) {
      case 0 /* SourceElements */:
        return token() ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */ ? parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(95 /* ExportKeyword */)) : parseErrorAtCurrentToken(Diagnostics.Declaration_or_statement_expected);
      case 1 /* BlockStatements */:
        return parseErrorAtCurrentToken(Diagnostics.Declaration_or_statement_expected);
      case 2 /* SwitchClauses */:
        return parseErrorAtCurrentToken(Diagnostics.case_or_default_expected);
      case 3 /* SwitchClauseStatements */:
        return parseErrorAtCurrentToken(Diagnostics.Statement_expected);
      case 18 /* RestProperties */:
      // fallthrough
      case 4 /* TypeMembers */:
        return parseErrorAtCurrentToken(Diagnostics.Property_or_signature_expected);
      case 5 /* ClassMembers */:
        return parseErrorAtCurrentToken(Diagnostics.Unexpected_token_A_constructor_method_accessor_or_property_was_expected);
      case 6 /* EnumMembers */:
        return parseErrorAtCurrentToken(Diagnostics.Enum_member_expected);
      case 7 /* HeritageClauseElement */:
        return parseErrorAtCurrentToken(Diagnostics.Expression_expected);
      case 8 /* VariableDeclarations */:
        return isKeyword(token()) ? parseErrorAtCurrentToken(Diagnostics._0_is_not_allowed_as_a_variable_declaration_name, tokenToString(token())) : parseErrorAtCurrentToken(Diagnostics.Variable_declaration_expected);
      case 9 /* ObjectBindingElements */:
        return parseErrorAtCurrentToken(Diagnostics.Property_destructuring_pattern_expected);
      case 10 /* ArrayBindingElements */:
        return parseErrorAtCurrentToken(Diagnostics.Array_element_destructuring_pattern_expected);
      case 11 /* ArgumentExpressions */:
        return parseErrorAtCurrentToken(Diagnostics.Argument_expression_expected);
      case 12 /* ObjectLiteralMembers */:
        return parseErrorAtCurrentToken(Diagnostics.Property_assignment_expected);
      case 15 /* ArrayLiteralMembers */:
        return parseErrorAtCurrentToken(Diagnostics.Expression_or_comma_expected);
      case 17 /* JSDocParameters */:
        return parseErrorAtCurrentToken(Diagnostics.Parameter_declaration_expected);
      case 16 /* Parameters */:
        return isKeyword(token()) ? parseErrorAtCurrentToken(Diagnostics._0_is_not_allowed_as_a_parameter_name, tokenToString(token())) : parseErrorAtCurrentToken(Diagnostics.Parameter_declaration_expected);
      case 19 /* TypeParameters */:
        return parseErrorAtCurrentToken(Diagnostics.Type_parameter_declaration_expected);
      case 20 /* TypeArguments */:
        return parseErrorAtCurrentToken(Diagnostics.Type_argument_expected);
      case 21 /* TupleElementTypes */:
        return parseErrorAtCurrentToken(Diagnostics.Type_expected);
      case 22 /* HeritageClauses */:
        return parseErrorAtCurrentToken(Diagnostics.Unexpected_token_expected);
      case 23 /* ImportOrExportSpecifiers */:
        if (token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */) {
          return parseErrorAtCurrentToken(Diagnostics._0_expected, "}");
        }
        return parseErrorAtCurrentToken(Diagnostics.Identifier_expected);
      case 13 /* JsxAttributes */:
        return parseErrorAtCurrentToken(Diagnostics.Identifier_expected);
      case 14 /* JsxChildren */:
        return parseErrorAtCurrentToken(Diagnostics.Identifier_expected);
      case 24 /* ImportAttributes */:
        return parseErrorAtCurrentToken(Diagnostics.Identifier_or_string_literal_expected);
      case 25 /* JSDocComment */:
        return parseErrorAtCurrentToken(Diagnostics.Identifier_expected);
      case 26 /* Count */:
        return Debug.fail("ParsingContext.Count used as a context");
      // Not a real context, only a marker.
      default:
        Debug.assertNever(context);
    }
  }
  function parseDelimitedList(kind, parseElement, considerSemicolonAsDelimiter) {
    const saveParsingContext ***REMOVED*** parsingContext;
    parsingContext |***REMOVED*** 1 << kind;
    const list ***REMOVED*** [];
    const listPos ***REMOVED*** getNodePos();
    let commaStart ***REMOVED*** -1;
    while (true) {
      if (isListElement(
        kind,
        /*inErrorRecovery*/
        false
      )) {
        const startPos ***REMOVED*** scanner.getTokenFullStart();
        const result ***REMOVED*** parseListElement(kind, parseElement);
        if (!result) {
          parsingContext ***REMOVED*** saveParsingContext;
          return void 0;
        }
        list.push(result);
        commaStart ***REMOVED*** scanner.getTokenStart();
        if (parseOptional(28 /* CommaToken */)) {
          continue;
        }
        commaStart ***REMOVED*** -1;
        if (isListTerminator(kind)) {
          break;
        }
        parseExpected(28 /* CommaToken */, getExpectedCommaDiagnostic(kind));
        if (considerSemicolonAsDelimiter && token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */ && !scanner.hasPrecedingLineBreak()) {
          nextToken();
        }
        if (startPos ***REMOVED******REMOVED******REMOVED*** scanner.getTokenFullStart()) {
          nextToken();
        }
        continue;
      }
      if (isListTerminator(kind)) {
        break;
      }
      if (abortParsingListOrMoveToNextToken(kind)) {
        break;
      }
    }
    parsingContext ***REMOVED*** saveParsingContext;
    return createNodeArray(
      list,
      listPos,
      /*end*/
      void 0,
      commaStart >***REMOVED*** 0
    );
  }
  function getExpectedCommaDiagnostic(kind) {
    return kind ***REMOVED******REMOVED******REMOVED*** 6 /* EnumMembers */ ? Diagnostics.An_enum_member_name_must_be_followed_by_a_or : void 0;
  }
  function createMissingList() {
    const list ***REMOVED*** createNodeArray([], getNodePos());
    list.isMissingList ***REMOVED*** true;
    return list;
  }
  function isMissingList(arr) {
    return !!arr.isMissingList;
  }
  function parseBracketedList(kind, parseElement, open, close) {
    if (parseExpected(open)) {
      const result ***REMOVED*** parseDelimitedList(kind, parseElement);
      parseExpected(close);
      return result;
    }
    return createMissingList();
  }
  function parseEntityName(allowReservedWords, diagnosticMessage) {
    const pos ***REMOVED*** getNodePos();
    let entity ***REMOVED*** allowReservedWords ? parseIdentifierName(diagnosticMessage) : parseIdentifier(diagnosticMessage);
    while (parseOptional(25 /* DotToken */)) {
      if (token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
        break;
      }
      entity ***REMOVED*** finishNode(
        factory2.createQualifiedName(
          entity,
          parseRightSideOfDot(
            allowReservedWords,
            /*allowPrivateIdentifiers*/
            false,
            /*allowUnicodeEscapeSequenceInIdentifierName*/
            true
          )
        ),
        pos
      );
    }
    return entity;
  }
  function createQualifiedName(entity, name) {
    return finishNode(factory2.createQualifiedName(entity, name), entity.pos);
  }
  function parseRightSideOfDot(allowIdentifierNames, allowPrivateIdentifiers, allowUnicodeEscapeSequenceInIdentifierName) {
    if (scanner.hasPrecedingLineBreak() && tokenIsIdentifierOrKeyword(token())) {
      const matchesPattern ***REMOVED*** lookAhead(nextTokenIsIdentifierOrKeywordOnSameLine);
      if (matchesPattern) {
        return createMissingNode(
          80 /* Identifier */,
          /*reportAtCurrentPosition*/
          true,
          Diagnostics.Identifier_expected
        );
      }
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
      const node ***REMOVED*** parsePrivateIdentifier();
      return allowPrivateIdentifiers ? node : createMissingNode(
        80 /* Identifier */,
        /*reportAtCurrentPosition*/
        true,
        Diagnostics.Identifier_expected
      );
    }
    if (allowIdentifierNames) {
      return allowUnicodeEscapeSequenceInIdentifierName ? parseIdentifierName() : parseIdentifierNameErrorOnUnicodeEscapeSequence();
    }
    return parseIdentifier();
  }
  function parseTemplateSpans(isTaggedTemplate) {
    const pos ***REMOVED*** getNodePos();
    const list ***REMOVED*** [];
    let node;
    do {
      node ***REMOVED*** parseTemplateSpan(isTaggedTemplate);
      list.push(node);
    } while (node.literal.kind ***REMOVED******REMOVED******REMOVED*** 17 /* TemplateMiddle */);
    return createNodeArray(list, pos);
  }
  function parseTemplateExpression(isTaggedTemplate) {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTemplateExpression(
        parseTemplateHead(isTaggedTemplate),
        parseTemplateSpans(isTaggedTemplate)
      ),
      pos
    );
  }
  function parseTemplateType() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTemplateLiteralType(
        parseTemplateHead(
          /*isTaggedTemplate*/
          false
        ),
        parseTemplateTypeSpans()
      ),
      pos
    );
  }
  function parseTemplateTypeSpans() {
    const pos ***REMOVED*** getNodePos();
    const list ***REMOVED*** [];
    let node;
    do {
      node ***REMOVED*** parseTemplateTypeSpan();
      list.push(node);
    } while (node.literal.kind ***REMOVED******REMOVED******REMOVED*** 17 /* TemplateMiddle */);
    return createNodeArray(list, pos);
  }
  function parseTemplateTypeSpan() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTemplateLiteralTypeSpan(
        parseType(),
        parseLiteralOfTemplateSpan(
          /*isTaggedTemplate*/
          false
        )
      ),
      pos
    );
  }
  function parseLiteralOfTemplateSpan(isTaggedTemplate) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */) {
      reScanTemplateToken(isTaggedTemplate);
      return parseTemplateMiddleOrTemplateTail();
    } else {
      return parseExpectedToken(18 /* TemplateTail */, Diagnostics._0_expected, tokenToString(20 /* CloseBraceToken */));
    }
  }
  function parseTemplateSpan(isTaggedTemplate) {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTemplateSpan(
        allowInAnd(parseExpression),
        parseLiteralOfTemplateSpan(isTaggedTemplate)
      ),
      pos
    );
  }
  function parseLiteralNode() {
    return parseLiteralLikeNode(token());
  }
  function parseTemplateHead(isTaggedTemplate) {
    if (!isTaggedTemplate && scanner.getTokenFlags() & 26656 /* IsInvalid */) {
      reScanTemplateToken(
        /*isTaggedTemplate*/
        false
      );
    }
    const fragment ***REMOVED*** parseLiteralLikeNode(token());
    Debug.assert(fragment.kind ***REMOVED******REMOVED******REMOVED*** 16 /* TemplateHead */, "Template head has wrong token kind");
    return fragment;
  }
  function parseTemplateMiddleOrTemplateTail() {
    const fragment ***REMOVED*** parseLiteralLikeNode(token());
    Debug.assert(fragment.kind ***REMOVED******REMOVED******REMOVED*** 17 /* TemplateMiddle */ || fragment.kind ***REMOVED******REMOVED******REMOVED*** 18 /* TemplateTail */, "Template fragment has wrong token kind");
    return fragment;
  }
  function getTemplateLiteralRawText(kind) {
    const isLast ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */ || kind ***REMOVED******REMOVED******REMOVED*** 18 /* TemplateTail */;
    const tokenText ***REMOVED*** scanner.getTokenText();
    return tokenText.substring(1, tokenText.length - (scanner.isUnterminated() ? 0 : isLast ? 1 : 2));
  }
  function parseLiteralLikeNode(kind) {
    const pos ***REMOVED*** getNodePos();
    const node ***REMOVED*** isTemplateLiteralKind(kind) ? factory2.createTemplateLiteralLikeNode(kind, scanner.getTokenValue(), getTemplateLiteralRawText(kind), scanner.getTokenFlags() & 7176 /* TemplateLiteralLikeFlags */) : (
      // Note that theoretically the following condition would hold true literals like 009,
      // which is not octal. But because of how the scanner separates the tokens, we would
      // never get a token like this. Instead, we would get 00 and 9 as two separate tokens.
      // We also do not need to check for negatives because any prefix operator would be part of a
      // parent unary expression.
      kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ ? factoryCreateNumericLiteral(scanner.getTokenValue(), scanner.getNumericLiteralFlags()) : kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factoryCreateStringLiteral(
        scanner.getTokenValue(),
        /*isSingleQuote*/
        void 0,
        scanner.hasExtendedUnicodeEscape()
      ) : isLiteralKind(kind) ? factoryCreateLiteralLikeNode(kind, scanner.getTokenValue()) : Debug.fail()
    );
    if (scanner.hasExtendedUnicodeEscape()) {
      node.hasExtendedUnicodeEscape ***REMOVED*** true;
    }
    if (scanner.isUnterminated()) {
      node.isUnterminated ***REMOVED*** true;
    }
    nextToken();
    return finishNode(node, pos);
  }
  function parseEntityNameOfTypeReference() {
    return parseEntityName(
      /*allowReservedWords*/
      true,
      Diagnostics.Type_expected
    );
  }
  function parseTypeArgumentsOfTypeReference() {
    if (!scanner.hasPrecedingLineBreak() && reScanLessThanToken() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return parseBracketedList(20 /* TypeArguments */, parseType, 30 /* LessThanToken */, 32 /* GreaterThanToken */);
    }
  }
  function parseTypeReference() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTypeReferenceNode(
        parseEntityNameOfTypeReference(),
        parseTypeArgumentsOfTypeReference()
      ),
      pos
    );
  }
  function typeHasArrowFunctionBlockingParseError(node) {
    switch (node.kind) {
      case 184 /* TypeReference */:
        return nodeIsMissing(node.typeName);
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */: {
        const { parameters, type } ***REMOVED*** node;
        return isMissingList(parameters) || typeHasArrowFunctionBlockingParseError(type);
      }
      case 197 /* ParenthesizedType */:
        return typeHasArrowFunctionBlockingParseError(node.type);
      default:
        return false;
    }
  }
  function parseThisTypePredicate(lhs) {
    nextToken();
    return finishNode(factory2.createTypePredicateNode(
      /*assertsModifier*/
      void 0,
      lhs,
      parseType()
    ), lhs.pos);
  }
  function parseThisTypeNode() {
    const pos ***REMOVED*** getNodePos();
    nextToken();
    return finishNode(factory2.createThisTypeNode(), pos);
  }
  function parseJSDocAllType() {
    const pos ***REMOVED*** getNodePos();
    nextToken();
    return finishNode(factory2.createJSDocAllType(), pos);
  }
  function parseJSDocNonNullableType() {
    const pos ***REMOVED*** getNodePos();
    nextToken();
    return finishNode(factory2.createJSDocNonNullableType(
      parseNonArrayType(),
      /*postfix*/
      false
    ), pos);
  }
  function parseJSDocUnknownOrNullableType() {
    const pos ***REMOVED*** getNodePos();
    nextToken();
    if (token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ || token() ***REMOVED******REMOVED******REMOVED*** 52 /* BarToken */) {
      return finishNode(factory2.createJSDocUnknownType(), pos);
    } else {
      return finishNode(factory2.createJSDocNullableType(
        parseType(),
        /*postfix*/
        false
      ), pos);
    }
  }
  function parseJSDocFunctionType() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    if (tryParse(nextTokenIsOpenParen)) {
      const parameters ***REMOVED*** parseParameters(4 /* Type */ | 32 /* JSDoc */);
      const type ***REMOVED*** parseReturnType(
        59 /* ColonToken */,
        /*isType*/
        false
      );
      return withJSDoc(finishNode(factory2.createJSDocFunctionType(parameters, type), pos), hasJSDoc);
    }
    return finishNode(factory2.createTypeReferenceNode(
      parseIdentifierName(),
      /*typeArguments*/
      void 0
    ), pos);
  }
  function parseJSDocParameter() {
    const pos ***REMOVED*** getNodePos();
    let name;
    if (token() ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */) {
      name ***REMOVED*** parseIdentifierName();
      parseExpected(59 /* ColonToken */);
    }
    return finishNode(
      factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        // TODO(rbuckton): JSDoc parameters don't have names (except `this`/`new`), should we manufacture an empty identifier?
        name,
        /*questionToken*/
        void 0,
        parseJSDocType(),
        /*initializer*/
        void 0
      ),
      pos
    );
  }
  function parseJSDocType() {
    scanner.setSkipJsDocLeadingAsterisks(true);
    const pos ***REMOVED*** getNodePos();
    if (parseOptional(144 /* ModuleKeyword */)) {
      const moduleTag ***REMOVED*** factory2.createJSDocNamepathType(
        /*type*/
        void 0
      );
      terminate:
        while (true) {
          switch (token()) {
            case 20 /* CloseBraceToken */:
            case 1 /* EndOfFileToken */:
            case 28 /* CommaToken */:
            case 5 /* WhitespaceTrivia */:
              break terminate;
            default:
              nextTokenJSDoc();
          }
        }
      scanner.setSkipJsDocLeadingAsterisks(false);
      return finishNode(moduleTag, pos);
    }
    const hasDotDotDot ***REMOVED*** parseOptional(26 /* DotDotDotToken */);
    let type ***REMOVED*** parseTypeOrTypePredicate();
    scanner.setSkipJsDocLeadingAsterisks(false);
    if (hasDotDotDot) {
      type ***REMOVED*** finishNode(factory2.createJSDocVariadicType(type), pos);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      nextToken();
      return finishNode(factory2.createJSDocOptionalType(type), pos);
    }
    return type;
  }
  function parseTypeQuery() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(114 /* TypeOfKeyword */);
    const entityName ***REMOVED*** parseEntityName(
      /*allowReservedWords*/
      true
    );
    const typeArguments ***REMOVED*** !scanner.hasPrecedingLineBreak() ? tryParseTypeArguments() : void 0;
    return finishNode(factory2.createTypeQueryNode(entityName, typeArguments), pos);
  }
  function parseTypeParameter() {
    const pos ***REMOVED*** getNodePos();
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      false,
      /*permitConstAsModifier*/
      true
    );
    const name ***REMOVED*** parseIdentifier();
    let constraint;
    let expression;
    if (parseOptional(96 /* ExtendsKeyword */)) {
      if (isStartOfType() || !isStartOfExpression()) {
        constraint ***REMOVED*** parseType();
      } else {
        expression ***REMOVED*** parseUnaryExpressionOrHigher();
      }
    }
    const defaultType ***REMOVED*** parseOptional(64 /* EqualsToken */) ? parseType() : void 0;
    const node ***REMOVED*** factory2.createTypeParameterDeclaration(modifiers, name, constraint, defaultType);
    node.expression ***REMOVED*** expression;
    return finishNode(node, pos);
  }
  function parseTypeParameters() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return parseBracketedList(19 /* TypeParameters */, parseTypeParameter, 30 /* LessThanToken */, 32 /* GreaterThanToken */);
    }
  }
  function isStartOfParameter(isJSDocParameter) {
    return token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || isBindingIdentifierOrPrivateIdentifierOrPattern() || isModifierKind(token()) || token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ || isStartOfType(
      /*inStartOfParameter*/
      !isJSDocParameter
    );
  }
  function parseNameOfParameter(modifiers) {
    const name ***REMOVED*** parseIdentifierOrPattern(Diagnostics.Private_identifiers_cannot_be_used_as_parameters);
    if (getFullWidth(name) ***REMOVED******REMOVED******REMOVED*** 0 && !some(modifiers) && isModifierKind(token())) {
      nextToken();
    }
    return name;
  }
  function isParameterNameStart() {
    return isBindingIdentifier() || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */;
  }
  function parseParameter(inOuterAwaitContext) {
    return parseParameterWorker(inOuterAwaitContext);
  }
  function parseParameterForSpeculation(inOuterAwaitContext) {
    return parseParameterWorker(
      inOuterAwaitContext,
      /*allowAmbiguity*/
      false
    );
  }
  function parseParameterWorker(inOuterAwaitContext, allowAmbiguity ***REMOVED*** true) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** inOuterAwaitContext ? doInAwaitContext(() ***REMOVED***> parseModifiers(
      /*allowDecorators*/
      true
    )) : doOutsideOfAwaitContext(() ***REMOVED***> parseModifiers(
      /*allowDecorators*/
      true
    ));
    if (token() ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      const node2 ***REMOVED*** factory2.createParameterDeclaration(
        modifiers,
        /*dotDotDotToken*/
        void 0,
        createIdentifier(
          /*isIdentifier*/
          true
        ),
        /*questionToken*/
        void 0,
        parseTypeAnnotation(),
        /*initializer*/
        void 0
      );
      const modifier ***REMOVED*** firstOrUndefined(modifiers);
      if (modifier) {
        parseErrorAtRange(modifier, Diagnostics.Neither_decorators_nor_modifiers_may_be_applied_to_this_parameters);
      }
      return withJSDoc(finishNode(node2, pos), hasJSDoc);
    }
    const savedTopLevel ***REMOVED*** topLevel;
    topLevel ***REMOVED*** false;
    const dotDotDotToken ***REMOVED*** parseOptionalToken(26 /* DotDotDotToken */);
    if (!allowAmbiguity && !isParameterNameStart()) {
      return void 0;
    }
    const node ***REMOVED*** withJSDoc(
      finishNode(
        factory2.createParameterDeclaration(
          modifiers,
          dotDotDotToken,
          parseNameOfParameter(modifiers),
          parseOptionalToken(58 /* QuestionToken */),
          parseTypeAnnotation(),
          parseInitializer()
        ),
        pos
      ),
      hasJSDoc
    );
    topLevel ***REMOVED*** savedTopLevel;
    return node;
  }
  function parseReturnType(returnToken, isType) {
    if (shouldParseReturnType(returnToken, isType)) {
      return allowConditionalTypesAnd(parseTypeOrTypePredicate);
    }
  }
  function shouldParseReturnType(returnToken, isType) {
    if (returnToken ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
      parseExpected(returnToken);
      return true;
    } else if (parseOptional(59 /* ColonToken */)) {
      return true;
    } else if (isType && token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
      parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(59 /* ColonToken */));
      nextToken();
      return true;
    }
    return false;
  }
  function parseParametersWorker(flags, allowAmbiguity) {
    const savedYieldContext ***REMOVED*** inYieldContext();
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    setYieldContext(!!(flags & 1 /* Yield */));
    setAwaitContext(!!(flags & 2 /* Await */));
    const parameters ***REMOVED*** flags & 32 /* JSDoc */ ? parseDelimitedList(17 /* JSDocParameters */, parseJSDocParameter) : parseDelimitedList(16 /* Parameters */, () ***REMOVED***> allowAmbiguity ? parseParameter(savedAwaitContext) : parseParameterForSpeculation(savedAwaitContext));
    setYieldContext(savedYieldContext);
    setAwaitContext(savedAwaitContext);
    return parameters;
  }
  function parseParameters(flags) {
    if (!parseExpected(21 /* OpenParenToken */)) {
      return createMissingList();
    }
    const parameters ***REMOVED*** parseParametersWorker(
      flags,
      /*allowAmbiguity*/
      true
    );
    parseExpected(22 /* CloseParenToken */);
    return parameters;
  }
  function parseTypeMemberSemicolon() {
    if (parseOptional(28 /* CommaToken */)) {
      return;
    }
    parseSemicolon();
  }
  function parseSignatureMember(kind) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    if (kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */) {
      parseExpected(105 /* NewKeyword */);
    }
    const typeParameters ***REMOVED*** parseTypeParameters();
    const parameters ***REMOVED*** parseParameters(4 /* Type */);
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      true
    );
    parseTypeMemberSemicolon();
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 180 /* CallSignature */ ? factory2.createCallSignature(typeParameters, parameters, type) : factory2.createConstructSignature(typeParameters, parameters, type);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function isIndexSignature() {
    return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ && lookAhead(isUnambiguouslyIndexSignature);
  }
  function isUnambiguouslyIndexSignature() {
    nextToken();
    if (token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ || token() ***REMOVED******REMOVED******REMOVED*** 24 /* CloseBracketToken */) {
      return true;
    }
    if (isModifierKind(token())) {
      nextToken();
      if (isIdentifier2()) {
        return true;
      }
    } else if (!isIdentifier2()) {
      return false;
    } else {
      nextToken();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      return true;
    }
    if (token() !***REMOVED******REMOVED*** 58 /* QuestionToken */) {
      return false;
    }
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 24 /* CloseBracketToken */;
  }
  function parseIndexSignatureDeclaration(pos, hasJSDoc, modifiers) {
    const parameters ***REMOVED*** parseBracketedList(16 /* Parameters */, () ***REMOVED***> parseParameter(
      /*inOuterAwaitContext*/
      false
    ), 23 /* OpenBracketToken */, 24 /* CloseBracketToken */);
    const type ***REMOVED*** parseTypeAnnotation();
    parseTypeMemberSemicolon();
    const node ***REMOVED*** factory2.createIndexSignature(modifiers, parameters, type);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parsePropertyOrMethodSignature(pos, hasJSDoc, modifiers) {
    const name ***REMOVED*** parsePropertyName();
    const questionToken ***REMOVED*** parseOptionalToken(58 /* QuestionToken */);
    let node;
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      const typeParameters ***REMOVED*** parseTypeParameters();
      const parameters ***REMOVED*** parseParameters(4 /* Type */);
      const type ***REMOVED*** parseReturnType(
        59 /* ColonToken */,
        /*isType*/
        true
      );
      node ***REMOVED*** factory2.createMethodSignature(modifiers, name, questionToken, typeParameters, parameters, type);
    } else {
      const type ***REMOVED*** parseTypeAnnotation();
      node ***REMOVED*** factory2.createPropertySignature(modifiers, name, questionToken, type);
      if (token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) node.initializer ***REMOVED*** parseInitializer();
    }
    parseTypeMemberSemicolon();
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function isTypeMemberStart() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 139 /* GetKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 153 /* SetKeyword */) {
      return true;
    }
    let idToken ***REMOVED*** false;
    while (isModifierKind(token())) {
      idToken ***REMOVED*** true;
      nextToken();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      return true;
    }
    if (isLiteralPropertyName()) {
      idToken ***REMOVED*** true;
      nextToken();
    }
    if (idToken) {
      return token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 58 /* QuestionToken */ || token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || canParseSemicolon();
    }
    return false;
  }
  function parseTypeMember() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return parseSignatureMember(180 /* CallSignature */);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */ && lookAhead(nextTokenIsOpenParenOrLessThan)) {
      return parseSignatureMember(181 /* ConstructSignature */);
    }
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      false
    );
    if (parseContextualModifier(139 /* GetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 178 /* GetAccessor */, 4 /* Type */);
    }
    if (parseContextualModifier(153 /* SetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 179 /* SetAccessor */, 4 /* Type */);
    }
    if (isIndexSignature()) {
      return parseIndexSignatureDeclaration(pos, hasJSDoc, modifiers);
    }
    return parsePropertyOrMethodSignature(pos, hasJSDoc, modifiers);
  }
  function nextTokenIsOpenParenOrLessThan() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */;
  }
  function nextTokenIsDot() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 25 /* DotToken */;
  }
  function nextTokenIsOpenParenOrLessThanOrDot() {
    switch (nextToken()) {
      case 21 /* OpenParenToken */:
      case 30 /* LessThanToken */:
      case 25 /* DotToken */:
        return true;
    }
    return false;
  }
  function parseTypeLiteral() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createTypeLiteralNode(parseObjectTypeMembers()), pos);
  }
  function parseObjectTypeMembers() {
    let members;
    if (parseExpected(19 /* OpenBraceToken */)) {
      members ***REMOVED*** parseList(4 /* TypeMembers */, parseTypeMember);
      parseExpected(20 /* CloseBraceToken */);
    } else {
      members ***REMOVED*** createMissingList();
    }
    return members;
  }
  function isStartOfMappedType() {
    nextToken();
    if (token() ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || token() ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
      return nextToken() ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
      nextToken();
    }
    return token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ && nextTokenIsIdentifier() && nextToken() ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */;
  }
  function parseMappedTypeParameter() {
    const pos ***REMOVED*** getNodePos();
    const name ***REMOVED*** parseIdentifierName();
    parseExpected(103 /* InKeyword */);
    const type ***REMOVED*** parseType();
    return finishNode(factory2.createTypeParameterDeclaration(
      /*modifiers*/
      void 0,
      name,
      type,
      /*defaultType*/
      void 0
    ), pos);
  }
  function parseMappedType() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(19 /* OpenBraceToken */);
    let readonlyToken;
    if (token() ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || token() ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
      readonlyToken ***REMOVED*** parseTokenNode();
      if (readonlyToken.kind !***REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
        parseExpected(148 /* ReadonlyKeyword */);
      }
    }
    parseExpected(23 /* OpenBracketToken */);
    const typeParameter ***REMOVED*** parseMappedTypeParameter();
    const nameType ***REMOVED*** parseOptional(130 /* AsKeyword */) ? parseType() : void 0;
    parseExpected(24 /* CloseBracketToken */);
    let questionToken;
    if (token() ***REMOVED******REMOVED******REMOVED*** 58 /* QuestionToken */ || token() ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || token() ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
      questionToken ***REMOVED*** parseTokenNode();
      if (questionToken.kind !***REMOVED******REMOVED*** 58 /* QuestionToken */) {
        parseExpected(58 /* QuestionToken */);
      }
    }
    const type ***REMOVED*** parseTypeAnnotation();
    parseSemicolon();
    const members ***REMOVED*** parseList(4 /* TypeMembers */, parseTypeMember);
    parseExpected(20 /* CloseBraceToken */);
    return finishNode(factory2.createMappedTypeNode(readonlyToken, typeParameter, nameType, questionToken, type, members), pos);
  }
  function parseTupleElementType() {
    const pos ***REMOVED*** getNodePos();
    if (parseOptional(26 /* DotDotDotToken */)) {
      return finishNode(factory2.createRestTypeNode(parseType()), pos);
    }
    const type ***REMOVED*** parseType();
    if (isJSDocNullableType(type) && type.pos ***REMOVED******REMOVED******REMOVED*** type.type.pos) {
      const node ***REMOVED*** factory2.createOptionalTypeNode(type.type);
      setTextRange(node, type);
      node.flags ***REMOVED*** type.flags;
      return node;
    }
    return type;
  }
  function isNextTokenColonOrQuestionColon() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 58 /* QuestionToken */ && nextToken() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */;
  }
  function isTupleElementName() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */) {
      return tokenIsIdentifierOrKeyword(nextToken()) && isNextTokenColonOrQuestionColon();
    }
    return tokenIsIdentifierOrKeyword(token()) && isNextTokenColonOrQuestionColon();
  }
  function parseTupleElementNameOrTupleElementType() {
    if (lookAhead(isTupleElementName)) {
      const pos ***REMOVED*** getNodePos();
      const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
      const dotDotDotToken ***REMOVED*** parseOptionalToken(26 /* DotDotDotToken */);
      const name ***REMOVED*** parseIdentifierName();
      const questionToken ***REMOVED*** parseOptionalToken(58 /* QuestionToken */);
      parseExpected(59 /* ColonToken */);
      const type ***REMOVED*** parseTupleElementType();
      const node ***REMOVED*** factory2.createNamedTupleMember(dotDotDotToken, name, questionToken, type);
      return withJSDoc(finishNode(node, pos), hasJSDoc);
    }
    return parseTupleElementType();
  }
  function parseTupleType() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(
      factory2.createTupleTypeNode(
        parseBracketedList(21 /* TupleElementTypes */, parseTupleElementNameOrTupleElementType, 23 /* OpenBracketToken */, 24 /* CloseBracketToken */)
      ),
      pos
    );
  }
  function parseParenthesizedType() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(21 /* OpenParenToken */);
    const type ***REMOVED*** parseType();
    parseExpected(22 /* CloseParenToken */);
    return finishNode(factory2.createParenthesizedType(type), pos);
  }
  function parseModifiersForConstructorType() {
    let modifiers;
    if (token() ***REMOVED******REMOVED******REMOVED*** 128 /* AbstractKeyword */) {
      const pos ***REMOVED*** getNodePos();
      nextToken();
      const modifier ***REMOVED*** finishNode(factoryCreateToken(128 /* AbstractKeyword */), pos);
      modifiers ***REMOVED*** createNodeArray([modifier], pos);
    }
    return modifiers;
  }
  function parseFunctionOrConstructorType() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiersForConstructorType();
    const isConstructorType ***REMOVED*** parseOptional(105 /* NewKeyword */);
    Debug.assert(!modifiers || isConstructorType, "Per isStartOfFunctionOrConstructorType, a function type cannot have modifiers.");
    const typeParameters ***REMOVED*** parseTypeParameters();
    const parameters ***REMOVED*** parseParameters(4 /* Type */);
    const type ***REMOVED*** parseReturnType(
      39 /* EqualsGreaterThanToken */,
      /*isType*/
      false
    );
    const node ***REMOVED*** isConstructorType ? factory2.createConstructorTypeNode(modifiers, typeParameters, parameters, type) : factory2.createFunctionTypeNode(typeParameters, parameters, type);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseKeywordAndNoDot() {
    const node ***REMOVED*** parseTokenNode();
    return token() ***REMOVED******REMOVED******REMOVED*** 25 /* DotToken */ ? void 0 : node;
  }
  function parseLiteralTypeNode(negative) {
    const pos ***REMOVED*** getNodePos();
    if (negative) {
      nextToken();
    }
    let expression ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */ ? parseTokenNode() : parseLiteralLikeNode(token());
    if (negative) {
      expression ***REMOVED*** finishNode(factory2.createPrefixUnaryExpression(41 /* MinusToken */, expression), pos);
    }
    return finishNode(factory2.createLiteralTypeNode(expression), pos);
  }
  function isStartOfTypeOfImportType() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */;
  }
  function parseImportType() {
    sourceFlags |***REMOVED*** 4194304 /* PossiblyContainsDynamicImport */;
    const pos ***REMOVED*** getNodePos();
    const isTypeOf ***REMOVED*** parseOptional(114 /* TypeOfKeyword */);
    parseExpected(102 /* ImportKeyword */);
    parseExpected(21 /* OpenParenToken */);
    const type ***REMOVED*** parseType();
    let attributes;
    if (parseOptional(28 /* CommaToken */)) {
      const openBracePosition ***REMOVED*** scanner.getTokenStart();
      parseExpected(19 /* OpenBraceToken */);
      const currentToken2 ***REMOVED*** token();
      if (currentToken2 ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 132 /* AssertKeyword */) {
        nextToken();
      } else {
        parseErrorAtCurrentToken(Diagnostics._0_expected, tokenToString(118 /* WithKeyword */));
      }
      parseExpected(59 /* ColonToken */);
      attributes ***REMOVED*** parseImportAttributes(
        currentToken2,
        /*skipKeyword*/
        true
      );
      parseOptional(28 /* CommaToken */);
      if (!parseExpected(20 /* CloseBraceToken */)) {
        const lastError ***REMOVED*** lastOrUndefined(parseDiagnostics);
        if (lastError && lastError.code ***REMOVED******REMOVED******REMOVED*** Diagnostics._0_expected.code) {
          addRelatedInfo(
            lastError,
            createDetachedDiagnostic(fileName, sourceText, openBracePosition, 1, Diagnostics.The_parser_expected_to_find_a_1_to_match_the_0_token_here, "{", "}")
          );
        }
      }
    }
    parseExpected(22 /* CloseParenToken */);
    const qualifier ***REMOVED*** parseOptional(25 /* DotToken */) ? parseEntityNameOfTypeReference() : void 0;
    const typeArguments ***REMOVED*** parseTypeArgumentsOfTypeReference();
    return finishNode(factory2.createImportTypeNode(type, attributes, qualifier, typeArguments, isTypeOf), pos);
  }
  function nextTokenIsNumericOrBigIntLiteral() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */;
  }
  function parseNonArrayType() {
    switch (token()) {
      case 133 /* AnyKeyword */:
      case 159 /* UnknownKeyword */:
      case 154 /* StringKeyword */:
      case 150 /* NumberKeyword */:
      case 163 /* BigIntKeyword */:
      case 155 /* SymbolKeyword */:
      case 136 /* BooleanKeyword */:
      case 157 /* UndefinedKeyword */:
      case 146 /* NeverKeyword */:
      case 151 /* ObjectKeyword */:
        return tryParse(parseKeywordAndNoDot) || parseTypeReference();
      case 67 /* AsteriskEqualsToken */:
        scanner.reScanAsteriskEqualsToken();
      // falls through
      case 42 /* AsteriskToken */:
        return parseJSDocAllType();
      case 61 /* QuestionQuestionToken */:
        scanner.reScanQuestionToken();
      // falls through
      case 58 /* QuestionToken */:
        return parseJSDocUnknownOrNullableType();
      case 100 /* FunctionKeyword */:
        return parseJSDocFunctionType();
      case 54 /* ExclamationToken */:
        return parseJSDocNonNullableType();
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 11 /* StringLiteral */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 106 /* NullKeyword */:
        return parseLiteralTypeNode();
      case 41 /* MinusToken */:
        return lookAhead(nextTokenIsNumericOrBigIntLiteral) ? parseLiteralTypeNode(
          /*negative*/
          true
        ) : parseTypeReference();
      case 116 /* VoidKeyword */:
        return parseTokenNode();
      case 110 /* ThisKeyword */: {
        const thisKeyword ***REMOVED*** parseThisTypeNode();
        if (token() ***REMOVED******REMOVED******REMOVED*** 142 /* IsKeyword */ && !scanner.hasPrecedingLineBreak()) {
          return parseThisTypePredicate(thisKeyword);
        } else {
          return thisKeyword;
        }
      }
      case 114 /* TypeOfKeyword */:
        return lookAhead(isStartOfTypeOfImportType) ? parseImportType() : parseTypeQuery();
      case 19 /* OpenBraceToken */:
        return lookAhead(isStartOfMappedType) ? parseMappedType() : parseTypeLiteral();
      case 23 /* OpenBracketToken */:
        return parseTupleType();
      case 21 /* OpenParenToken */:
        return parseParenthesizedType();
      case 102 /* ImportKeyword */:
        return parseImportType();
      case 131 /* AssertsKeyword */:
        return lookAhead(nextTokenIsIdentifierOrKeywordOnSameLine) ? parseAssertsTypePredicate() : parseTypeReference();
      case 16 /* TemplateHead */:
        return parseTemplateType();
      default:
        return parseTypeReference();
    }
  }
  function isStartOfType(inStartOfParameter) {
    switch (token()) {
      case 133 /* AnyKeyword */:
      case 159 /* UnknownKeyword */:
      case 154 /* StringKeyword */:
      case 150 /* NumberKeyword */:
      case 163 /* BigIntKeyword */:
      case 136 /* BooleanKeyword */:
      case 148 /* ReadonlyKeyword */:
      case 155 /* SymbolKeyword */:
      case 158 /* UniqueKeyword */:
      case 116 /* VoidKeyword */:
      case 157 /* UndefinedKeyword */:
      case 106 /* NullKeyword */:
      case 110 /* ThisKeyword */:
      case 114 /* TypeOfKeyword */:
      case 146 /* NeverKeyword */:
      case 19 /* OpenBraceToken */:
      case 23 /* OpenBracketToken */:
      case 30 /* LessThanToken */:
      case 52 /* BarToken */:
      case 51 /* AmpersandToken */:
      case 105 /* NewKeyword */:
      case 11 /* StringLiteral */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 151 /* ObjectKeyword */:
      case 42 /* AsteriskToken */:
      case 58 /* QuestionToken */:
      case 54 /* ExclamationToken */:
      case 26 /* DotDotDotToken */:
      case 140 /* InferKeyword */:
      case 102 /* ImportKeyword */:
      case 131 /* AssertsKeyword */:
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 16 /* TemplateHead */:
        return true;
      case 100 /* FunctionKeyword */:
        return !inStartOfParameter;
      case 41 /* MinusToken */:
        return !inStartOfParameter && lookAhead(nextTokenIsNumericOrBigIntLiteral);
      case 21 /* OpenParenToken */:
        return !inStartOfParameter && lookAhead(isStartOfParenthesizedOrFunctionType);
      default:
        return isIdentifier2();
    }
  }
  function isStartOfParenthesizedOrFunctionType() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */ || isStartOfParameter(
      /*isJSDocParameter*/
      false
    ) || isStartOfType();
  }
  function parsePostfixTypeOrHigher() {
    const pos ***REMOVED*** getNodePos();
    let type ***REMOVED*** parseNonArrayType();
    while (!scanner.hasPrecedingLineBreak()) {
      switch (token()) {
        case 54 /* ExclamationToken */:
          nextToken();
          type ***REMOVED*** finishNode(factory2.createJSDocNonNullableType(
            type,
            /*postfix*/
            true
          ), pos);
          break;
        case 58 /* QuestionToken */:
          if (lookAhead(nextTokenIsStartOfType)) {
            return type;
          }
          nextToken();
          type ***REMOVED*** finishNode(factory2.createJSDocNullableType(
            type,
            /*postfix*/
            true
          ), pos);
          break;
        case 23 /* OpenBracketToken */:
          parseExpected(23 /* OpenBracketToken */);
          if (isStartOfType()) {
            const indexType ***REMOVED*** parseType();
            parseExpected(24 /* CloseBracketToken */);
            type ***REMOVED*** finishNode(factory2.createIndexedAccessTypeNode(type, indexType), pos);
          } else {
            parseExpected(24 /* CloseBracketToken */);
            type ***REMOVED*** finishNode(factory2.createArrayTypeNode(type), pos);
          }
          break;
        default:
          return type;
      }
    }
    return type;
  }
  function parseTypeOperator(operator) {
    const pos ***REMOVED*** getNodePos();
    parseExpected(operator);
    return finishNode(factory2.createTypeOperatorNode(operator, parseTypeOperatorOrHigher()), pos);
  }
  function tryParseConstraintOfInferType() {
    if (parseOptional(96 /* ExtendsKeyword */)) {
      const constraint ***REMOVED*** disallowConditionalTypesAnd(parseType);
      if (inDisallowConditionalTypesContext() || token() !***REMOVED******REMOVED*** 58 /* QuestionToken */) {
        return constraint;
      }
    }
  }
  function parseTypeParameterOfInferType() {
    const pos ***REMOVED*** getNodePos();
    const name ***REMOVED*** parseIdentifier();
    const constraint ***REMOVED*** tryParse(tryParseConstraintOfInferType);
    const node ***REMOVED*** factory2.createTypeParameterDeclaration(
      /*modifiers*/
      void 0,
      name,
      constraint
    );
    return finishNode(node, pos);
  }
  function parseInferType() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(140 /* InferKeyword */);
    return finishNode(factory2.createInferTypeNode(parseTypeParameterOfInferType()), pos);
  }
  function parseTypeOperatorOrHigher() {
    const operator ***REMOVED*** token();
    switch (operator) {
      case 143 /* KeyOfKeyword */:
      case 158 /* UniqueKeyword */:
      case 148 /* ReadonlyKeyword */:
        return parseTypeOperator(operator);
      case 140 /* InferKeyword */:
        return parseInferType();
    }
    return allowConditionalTypesAnd(parsePostfixTypeOrHigher);
  }
  function parseFunctionOrConstructorTypeToError(isInUnionType) {
    if (isStartOfFunctionTypeOrConstructorType()) {
      const type ***REMOVED*** parseFunctionOrConstructorType();
      let diagnostic;
      if (isFunctionTypeNode(type)) {
        diagnostic ***REMOVED*** isInUnionType ? Diagnostics.Function_type_notation_must_be_parenthesized_when_used_in_a_union_type : Diagnostics.Function_type_notation_must_be_parenthesized_when_used_in_an_intersection_type;
      } else {
        diagnostic ***REMOVED*** isInUnionType ? Diagnostics.Constructor_type_notation_must_be_parenthesized_when_used_in_a_union_type : Diagnostics.Constructor_type_notation_must_be_parenthesized_when_used_in_an_intersection_type;
      }
      parseErrorAtRange(type, diagnostic);
      return type;
    }
    return void 0;
  }
  function parseUnionOrIntersectionType(operator, parseConstituentType, createTypeNode) {
    const pos ***REMOVED*** getNodePos();
    const isUnionType ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 52 /* BarToken */;
    const hasLeadingOperator ***REMOVED*** parseOptional(operator);
    let type ***REMOVED*** hasLeadingOperator && parseFunctionOrConstructorTypeToError(isUnionType) || parseConstituentType();
    if (token() ***REMOVED******REMOVED******REMOVED*** operator || hasLeadingOperator) {
      const types ***REMOVED*** [type];
      while (parseOptional(operator)) {
        types.push(parseFunctionOrConstructorTypeToError(isUnionType) || parseConstituentType());
      }
      type ***REMOVED*** finishNode(createTypeNode(createNodeArray(types, pos)), pos);
    }
    return type;
  }
  function parseIntersectionTypeOrHigher() {
    return parseUnionOrIntersectionType(51 /* AmpersandToken */, parseTypeOperatorOrHigher, factory2.createIntersectionTypeNode);
  }
  function parseUnionTypeOrHigher() {
    return parseUnionOrIntersectionType(52 /* BarToken */, parseIntersectionTypeOrHigher, factory2.createUnionTypeNode);
  }
  function nextTokenIsNewKeyword() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */;
  }
  function isStartOfFunctionTypeOrConstructorType() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return true;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ && lookAhead(isUnambiguouslyStartOfFunctionType)) {
      return true;
    }
    return token() ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 128 /* AbstractKeyword */ && lookAhead(nextTokenIsNewKeyword);
  }
  function skipParameterStart() {
    if (isModifierKind(token())) {
      parseModifiers(
        /*allowDecorators*/
        false
      );
    }
    if (isIdentifier2() || token() ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      nextToken();
      return true;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      const previousErrorCount ***REMOVED*** parseDiagnostics.length;
      parseIdentifierOrPattern();
      return previousErrorCount ***REMOVED******REMOVED******REMOVED*** parseDiagnostics.length;
    }
    return false;
  }
  function isUnambiguouslyStartOfFunctionType() {
    nextToken();
    if (token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */) {
      return true;
    }
    if (skipParameterStart()) {
      if (token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 58 /* QuestionToken */ || token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
        return true;
      }
      if (token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */) {
        nextToken();
        if (token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
          return true;
        }
      }
    }
    return false;
  }
  function parseTypeOrTypePredicate() {
    const pos ***REMOVED*** getNodePos();
    const typePredicateVariable ***REMOVED*** isIdentifier2() && tryParse(parseTypePredicatePrefix);
    const type ***REMOVED*** parseType();
    if (typePredicateVariable) {
      return finishNode(factory2.createTypePredicateNode(
        /*assertsModifier*/
        void 0,
        typePredicateVariable,
        type
      ), pos);
    } else {
      return type;
    }
  }
  function parseTypePredicatePrefix() {
    const id ***REMOVED*** parseIdentifier();
    if (token() ***REMOVED******REMOVED******REMOVED*** 142 /* IsKeyword */ && !scanner.hasPrecedingLineBreak()) {
      nextToken();
      return id;
    }
  }
  function parseAssertsTypePredicate() {
    const pos ***REMOVED*** getNodePos();
    const assertsModifier ***REMOVED*** parseExpectedToken(131 /* AssertsKeyword */);
    const parameterName ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ ? parseThisTypeNode() : parseIdentifier();
    const type ***REMOVED*** parseOptional(142 /* IsKeyword */) ? parseType() : void 0;
    return finishNode(factory2.createTypePredicateNode(assertsModifier, parameterName, type), pos);
  }
  function parseType() {
    if (contextFlags & 81920 /* TypeExcludesFlags */) {
      return doOutsideOfContext(81920 /* TypeExcludesFlags */, parseType);
    }
    if (isStartOfFunctionTypeOrConstructorType()) {
      return parseFunctionOrConstructorType();
    }
    const pos ***REMOVED*** getNodePos();
    const type ***REMOVED*** parseUnionTypeOrHigher();
    if (!inDisallowConditionalTypesContext() && !scanner.hasPrecedingLineBreak() && parseOptional(96 /* ExtendsKeyword */)) {
      const extendsType ***REMOVED*** disallowConditionalTypesAnd(parseType);
      parseExpected(58 /* QuestionToken */);
      const trueType ***REMOVED*** allowConditionalTypesAnd(parseType);
      parseExpected(59 /* ColonToken */);
      const falseType ***REMOVED*** allowConditionalTypesAnd(parseType);
      return finishNode(factory2.createConditionalTypeNode(type, extendsType, trueType, falseType), pos);
    }
    return type;
  }
  function parseTypeAnnotation() {
    return parseOptional(59 /* ColonToken */) ? parseType() : void 0;
  }
  function isStartOfLeftHandSideExpression() {
    switch (token()) {
      case 110 /* ThisKeyword */:
      case 108 /* SuperKeyword */:
      case 106 /* NullKeyword */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 11 /* StringLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 16 /* TemplateHead */:
      case 21 /* OpenParenToken */:
      case 23 /* OpenBracketToken */:
      case 19 /* OpenBraceToken */:
      case 100 /* FunctionKeyword */:
      case 86 /* ClassKeyword */:
      case 105 /* NewKeyword */:
      case 44 /* SlashToken */:
      case 69 /* SlashEqualsToken */:
      case 80 /* Identifier */:
        return true;
      case 102 /* ImportKeyword */:
        return lookAhead(nextTokenIsOpenParenOrLessThanOrDot);
      default:
        return isIdentifier2();
    }
  }
  function isStartOfExpression() {
    if (isStartOfLeftHandSideExpression()) {
      return true;
    }
    switch (token()) {
      case 40 /* PlusToken */:
      case 41 /* MinusToken */:
      case 55 /* TildeToken */:
      case 54 /* ExclamationToken */:
      case 91 /* DeleteKeyword */:
      case 114 /* TypeOfKeyword */:
      case 116 /* VoidKeyword */:
      case 46 /* PlusPlusToken */:
      case 47 /* MinusMinusToken */:
      case 30 /* LessThanToken */:
      case 135 /* AwaitKeyword */:
      case 127 /* YieldKeyword */:
      case 81 /* PrivateIdentifier */:
      case 60 /* AtToken */:
        return true;
      default:
        if (isBinaryOperator2()) {
          return true;
        }
        return isIdentifier2();
    }
  }
  function isStartOfExpressionStatement() {
    return token() !***REMOVED******REMOVED*** 19 /* OpenBraceToken */ && token() !***REMOVED******REMOVED*** 100 /* FunctionKeyword */ && token() !***REMOVED******REMOVED*** 86 /* ClassKeyword */ && token() !***REMOVED******REMOVED*** 60 /* AtToken */ && isStartOfExpression();
  }
  function parseExpression() {
    const saveDecoratorContext ***REMOVED*** inDecoratorContext();
    if (saveDecoratorContext) {
      setDecoratorContext(
        /*val*/
        false
      );
    }
    const pos ***REMOVED*** getNodePos();
    let expr ***REMOVED*** parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    );
    let operatorToken;
    while (operatorToken ***REMOVED*** parseOptionalToken(28 /* CommaToken */)) {
      expr ***REMOVED*** makeBinaryExpression(expr, operatorToken, parseAssignmentExpressionOrHigher(
        /*allowReturnTypeInArrowFunction*/
        true
      ), pos);
    }
    if (saveDecoratorContext) {
      setDecoratorContext(
        /*val*/
        true
      );
    }
    return expr;
  }
  function parseInitializer() {
    return parseOptional(64 /* EqualsToken */) ? parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    ) : void 0;
  }
  function parseAssignmentExpressionOrHigher(allowReturnTypeInArrowFunction) {
    if (isYieldExpression()) {
      return parseYieldExpression();
    }
    const arrowExpression ***REMOVED*** tryParseParenthesizedArrowFunctionExpression(allowReturnTypeInArrowFunction) || tryParseAsyncSimpleArrowFunctionExpression(allowReturnTypeInArrowFunction);
    if (arrowExpression) {
      return arrowExpression;
    }
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const expr ***REMOVED*** parseBinaryExpressionOrHigher(0 /* Lowest */);
    if (expr.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
      return parseSimpleArrowFunctionExpression(
        pos,
        expr,
        allowReturnTypeInArrowFunction,
        hasJSDoc,
        /*asyncModifier*/
        void 0
      );
    }
    if (isLeftHandSideExpression(expr) && isAssignmentOperator(reScanGreaterToken())) {
      return makeBinaryExpression(expr, parseTokenNode(), parseAssignmentExpressionOrHigher(allowReturnTypeInArrowFunction), pos);
    }
    return parseConditionalExpressionRest(expr, pos, allowReturnTypeInArrowFunction);
  }
  function isYieldExpression() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 127 /* YieldKeyword */) {
      if (inYieldContext()) {
        return true;
      }
      return lookAhead(nextTokenIsIdentifierOrKeywordOrLiteralOnSameLine);
    }
    return false;
  }
  function nextTokenIsIdentifierOnSameLine() {
    nextToken();
    return !scanner.hasPrecedingLineBreak() && isIdentifier2();
  }
  function parseYieldExpression() {
    const pos ***REMOVED*** getNodePos();
    nextToken();
    if (!scanner.hasPrecedingLineBreak() && (token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || isStartOfExpression())) {
      return finishNode(
        factory2.createYieldExpression(
          parseOptionalToken(42 /* AsteriskToken */),
          parseAssignmentExpressionOrHigher(
            /*allowReturnTypeInArrowFunction*/
            true
          )
        ),
        pos
      );
    } else {
      return finishNode(factory2.createYieldExpression(
        /*asteriskToken*/
        void 0,
        /*expression*/
        void 0
      ), pos);
    }
  }
  function parseSimpleArrowFunctionExpression(pos, identifier, allowReturnTypeInArrowFunction, hasJSDoc, asyncModifier) {
    Debug.assert(token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */, "parseSimpleArrowFunctionExpression should only have been called if we had a ***REMOVED***>");
    const parameter ***REMOVED*** factory2.createParameterDeclaration(
      /*modifiers*/
      void 0,
      /*dotDotDotToken*/
      void 0,
      identifier,
      /*questionToken*/
      void 0,
      /*type*/
      void 0,
      /*initializer*/
      void 0
    );
    finishNode(parameter, identifier.pos);
    const parameters ***REMOVED*** createNodeArray([parameter], parameter.pos, parameter.end);
    const equalsGreaterThanToken ***REMOVED*** parseExpectedToken(39 /* EqualsGreaterThanToken */);
    const body ***REMOVED*** parseArrowFunctionExpressionBody(
      /*isAsync*/
      !!asyncModifier,
      allowReturnTypeInArrowFunction
    );
    const node ***REMOVED*** factory2.createArrowFunction(
      asyncModifier,
      /*typeParameters*/
      void 0,
      parameters,
      /*type*/
      void 0,
      equalsGreaterThanToken,
      body
    );
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function tryParseParenthesizedArrowFunctionExpression(allowReturnTypeInArrowFunction) {
    const triState ***REMOVED*** isParenthesizedArrowFunctionExpression();
    if (triState ***REMOVED******REMOVED******REMOVED*** 0 /* False */) {
      return void 0;
    }
    return triState ***REMOVED******REMOVED******REMOVED*** 1 /* True */ ? parseParenthesizedArrowFunctionExpression(
      /*allowAmbiguity*/
      true,
      /*allowReturnTypeInArrowFunction*/
      true
    ) : tryParse(() ***REMOVED***> parsePossibleParenthesizedArrowFunctionExpression(allowReturnTypeInArrowFunction));
  }
  function isParenthesizedArrowFunctionExpression() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ || token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      return lookAhead(isParenthesizedArrowFunctionExpressionWorker);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
      return 1 /* True */;
    }
    return 0 /* False */;
  }
  function isParenthesizedArrowFunctionExpressionWorker() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      nextToken();
      if (scanner.hasPrecedingLineBreak()) {
        return 0 /* False */;
      }
      if (token() !***REMOVED******REMOVED*** 21 /* OpenParenToken */ && token() !***REMOVED******REMOVED*** 30 /* LessThanToken */) {
        return 0 /* False */;
      }
    }
    const first2 ***REMOVED*** token();
    const second ***REMOVED*** nextToken();
    if (first2 ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */) {
      if (second ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */) {
        const third ***REMOVED*** nextToken();
        switch (third) {
          case 39 /* EqualsGreaterThanToken */:
          case 59 /* ColonToken */:
          case 19 /* OpenBraceToken */:
            return 1 /* True */;
          default:
            return 0 /* False */;
        }
      }
      if (second ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || second ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
        return 2 /* Unknown */;
      }
      if (second ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */) {
        return 1 /* True */;
      }
      if (isModifierKind(second) && second !***REMOVED******REMOVED*** 134 /* AsyncKeyword */ && lookAhead(nextTokenIsIdentifier)) {
        if (nextToken() ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */) {
          return 0 /* False */;
        }
        return 1 /* True */;
      }
      if (!isIdentifier2() && second !***REMOVED******REMOVED*** 110 /* ThisKeyword */) {
        return 0 /* False */;
      }
      switch (nextToken()) {
        case 59 /* ColonToken */:
          return 1 /* True */;
        case 58 /* QuestionToken */:
          nextToken();
          if (token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ || token() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */) {
            return 1 /* True */;
          }
          return 0 /* False */;
        case 28 /* CommaToken */:
        case 64 /* EqualsToken */:
        case 22 /* CloseParenToken */:
          return 2 /* Unknown */;
      }
      return 0 /* False */;
    } else {
      Debug.assert(first2 ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */);
      if (!isIdentifier2() && token() !***REMOVED******REMOVED*** 87 /* ConstKeyword */) {
        return 0 /* False */;
      }
      if (languageVariant ***REMOVED******REMOVED******REMOVED*** 1 /* JSX */) {
        const isArrowFunctionInJsx ***REMOVED*** lookAhead(() ***REMOVED***> {
          parseOptional(87 /* ConstKeyword */);
          const third ***REMOVED*** nextToken();
          if (third ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
            const fourth ***REMOVED*** nextToken();
            switch (fourth) {
              case 64 /* EqualsToken */:
              case 32 /* GreaterThanToken */:
              case 44 /* SlashToken */:
                return false;
              default:
                return true;
            }
          } else if (third ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || third ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
            return true;
          }
          return false;
        });
        if (isArrowFunctionInJsx) {
          return 1 /* True */;
        }
        return 0 /* False */;
      }
      return 2 /* Unknown */;
    }
  }
  function parsePossibleParenthesizedArrowFunctionExpression(allowReturnTypeInArrowFunction) {
    const tokenPos ***REMOVED*** scanner.getTokenStart();
    if (notParenthesizedArrow ***REMOVED******REMOVED*** null ? void 0 : notParenthesizedArrow.has(tokenPos)) {
      return void 0;
    }
    const result ***REMOVED*** parseParenthesizedArrowFunctionExpression(
      /*allowAmbiguity*/
      false,
      allowReturnTypeInArrowFunction
    );
    if (!result) {
      (notParenthesizedArrow || (notParenthesizedArrow ***REMOVED*** /* @__PURE__ */ new Set())).add(tokenPos);
    }
    return result;
  }
  function tryParseAsyncSimpleArrowFunctionExpression(allowReturnTypeInArrowFunction) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      if (lookAhead(isUnParenthesizedAsyncArrowFunctionWorker) ***REMOVED******REMOVED******REMOVED*** 1 /* True */) {
        const pos ***REMOVED*** getNodePos();
        const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
        const asyncModifier ***REMOVED*** parseModifiersForArrowFunction();
        const expr ***REMOVED*** parseBinaryExpressionOrHigher(0 /* Lowest */);
        return parseSimpleArrowFunctionExpression(pos, expr, allowReturnTypeInArrowFunction, hasJSDoc, asyncModifier);
      }
    }
    return void 0;
  }
  function isUnParenthesizedAsyncArrowFunctionWorker() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      nextToken();
      if (scanner.hasPrecedingLineBreak() || token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
        return 0 /* False */;
      }
      const expr ***REMOVED*** parseBinaryExpressionOrHigher(0 /* Lowest */);
      if (!scanner.hasPrecedingLineBreak() && expr.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && token() ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */) {
        return 1 /* True */;
      }
    }
    return 0 /* False */;
  }
  function parseParenthesizedArrowFunctionExpression(allowAmbiguity, allowReturnTypeInArrowFunction) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiersForArrowFunction();
    const isAsync ***REMOVED*** some(modifiers, isAsyncModifier) ? 2 /* Await */ : 0 /* None */;
    const typeParameters ***REMOVED*** parseTypeParameters();
    let parameters;
    if (!parseExpected(21 /* OpenParenToken */)) {
      if (!allowAmbiguity) {
        return void 0;
      }
      parameters ***REMOVED*** createMissingList();
    } else {
      if (!allowAmbiguity) {
        const maybeParameters ***REMOVED*** parseParametersWorker(isAsync, allowAmbiguity);
        if (!maybeParameters) {
          return void 0;
        }
        parameters ***REMOVED*** maybeParameters;
      } else {
        parameters ***REMOVED*** parseParametersWorker(isAsync, allowAmbiguity);
      }
      if (!parseExpected(22 /* CloseParenToken */) && !allowAmbiguity) {
        return void 0;
      }
    }
    const hasReturnColon ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */;
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      false
    );
    if (type && !allowAmbiguity && typeHasArrowFunctionBlockingParseError(type)) {
      return void 0;
    }
    let unwrappedType ***REMOVED*** type;
    while ((unwrappedType ***REMOVED******REMOVED*** null ? void 0 : unwrappedType.kind) ***REMOVED******REMOVED******REMOVED*** 197 /* ParenthesizedType */) {
      unwrappedType ***REMOVED*** unwrappedType.type;
    }
    const hasJSDocFunctionType ***REMOVED*** unwrappedType && isJSDocFunctionType(unwrappedType);
    if (!allowAmbiguity && token() !***REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */ && (hasJSDocFunctionType || token() !***REMOVED******REMOVED*** 19 /* OpenBraceToken */)) {
      return void 0;
    }
    const lastToken ***REMOVED*** token();
    const equalsGreaterThanToken ***REMOVED*** parseExpectedToken(39 /* EqualsGreaterThanToken */);
    const body ***REMOVED*** lastToken ***REMOVED******REMOVED******REMOVED*** 39 /* EqualsGreaterThanToken */ || lastToken ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ ? parseArrowFunctionExpressionBody(some(modifiers, isAsyncModifier), allowReturnTypeInArrowFunction) : parseIdentifier();
    if (!allowReturnTypeInArrowFunction && hasReturnColon) {
      if (token() !***REMOVED******REMOVED*** 59 /* ColonToken */) {
        return void 0;
      }
    }
    const node ***REMOVED*** factory2.createArrowFunction(modifiers, typeParameters, parameters, type, equalsGreaterThanToken, body);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseArrowFunctionExpressionBody(isAsync, allowReturnTypeInArrowFunction) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      return parseFunctionBlock(isAsync ? 2 /* Await */ : 0 /* None */);
    }
    if (token() !***REMOVED******REMOVED*** 27 /* SemicolonToken */ && token() !***REMOVED******REMOVED*** 100 /* FunctionKeyword */ && token() !***REMOVED******REMOVED*** 86 /* ClassKeyword */ && isStartOfStatement() && !isStartOfExpressionStatement()) {
      return parseFunctionBlock(16 /* IgnoreMissingOpenBrace */ | (isAsync ? 2 /* Await */ : 0 /* None */));
    }
    const savedYieldContext ***REMOVED*** inYieldContext();
    setYieldContext(false);
    const savedTopLevel ***REMOVED*** topLevel;
    topLevel ***REMOVED*** false;
    const node ***REMOVED*** isAsync ? doInAwaitContext(() ***REMOVED***> parseAssignmentExpressionOrHigher(allowReturnTypeInArrowFunction)) : doOutsideOfAwaitContext(() ***REMOVED***> parseAssignmentExpressionOrHigher(allowReturnTypeInArrowFunction));
    topLevel ***REMOVED*** savedTopLevel;
    setYieldContext(savedYieldContext);
    return node;
  }
  function parseConditionalExpressionRest(leftOperand, pos, allowReturnTypeInArrowFunction) {
    const questionToken ***REMOVED*** parseOptionalToken(58 /* QuestionToken */);
    if (!questionToken) {
      return leftOperand;
    }
    let colonToken;
    return finishNode(
      factory2.createConditionalExpression(
        leftOperand,
        questionToken,
        doOutsideOfContext(disallowInAndDecoratorContext, () ***REMOVED***> parseAssignmentExpressionOrHigher(
          /*allowReturnTypeInArrowFunction*/
          false
        )),
        colonToken ***REMOVED*** parseExpectedToken(59 /* ColonToken */),
        nodeIsPresent(colonToken) ? parseAssignmentExpressionOrHigher(allowReturnTypeInArrowFunction) : createMissingNode(
          80 /* Identifier */,
          /*reportAtCurrentPosition*/
          false,
          Diagnostics._0_expected,
          tokenToString(59 /* ColonToken */)
        )
      ),
      pos
    );
  }
  function parseBinaryExpressionOrHigher(precedence) {
    const pos ***REMOVED*** getNodePos();
    const leftOperand ***REMOVED*** parseUnaryExpressionOrHigher();
    return parseBinaryExpressionRest(precedence, leftOperand, pos);
  }
  function isInOrOfKeyword(t) {
    return t ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ || t ***REMOVED******REMOVED******REMOVED*** 165 /* OfKeyword */;
  }
  function parseBinaryExpressionRest(precedence, leftOperand, pos) {
    while (true) {
      reScanGreaterToken();
      const newPrecedence ***REMOVED*** getBinaryOperatorPrecedence(token());
      const consumeCurrentOperator ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 43 /* AsteriskAsteriskToken */ ? newPrecedence >***REMOVED*** precedence : newPrecedence > precedence;
      if (!consumeCurrentOperator) {
        break;
      }
      if (token() ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ && inDisallowInContext()) {
        break;
      }
      if (token() ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 152 /* SatisfiesKeyword */) {
        if (scanner.hasPrecedingLineBreak()) {
          break;
        } else {
          const keywordKind ***REMOVED*** token();
          nextToken();
          leftOperand ***REMOVED*** keywordKind ***REMOVED******REMOVED******REMOVED*** 152 /* SatisfiesKeyword */ ? makeSatisfiesExpression(leftOperand, parseType()) : makeAsExpression(leftOperand, parseType());
        }
      } else {
        leftOperand ***REMOVED*** makeBinaryExpression(leftOperand, parseTokenNode(), parseBinaryExpressionOrHigher(newPrecedence), pos);
      }
    }
    return leftOperand;
  }
  function isBinaryOperator2() {
    if (inDisallowInContext() && token() ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */) {
      return false;
    }
    return getBinaryOperatorPrecedence(token()) > 0;
  }
  function makeSatisfiesExpression(left, right) {
    return finishNode(factory2.createSatisfiesExpression(left, right), left.pos);
  }
  function makeBinaryExpression(left, operatorToken, right, pos) {
    return finishNode(factory2.createBinaryExpression(left, operatorToken, right), pos);
  }
  function makeAsExpression(left, right) {
    return finishNode(factory2.createAsExpression(left, right), left.pos);
  }
  function parsePrefixUnaryExpression() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createPrefixUnaryExpression(token(), nextTokenAnd(parseSimpleUnaryExpression)), pos);
  }
  function parseDeleteExpression() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createDeleteExpression(nextTokenAnd(parseSimpleUnaryExpression)), pos);
  }
  function parseTypeOfExpression() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createTypeOfExpression(nextTokenAnd(parseSimpleUnaryExpression)), pos);
  }
  function parseVoidExpression() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createVoidExpression(nextTokenAnd(parseSimpleUnaryExpression)), pos);
  }
  function isAwaitExpression2() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */) {
      if (inAwaitContext()) {
        return true;
      }
      return lookAhead(nextTokenIsIdentifierOrKeywordOrLiteralOnSameLine);
    }
    return false;
  }
  function parseAwaitExpression() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createAwaitExpression(nextTokenAnd(parseSimpleUnaryExpression)), pos);
  }
  function parseUnaryExpressionOrHigher() {
    if (isUpdateExpression()) {
      const pos ***REMOVED*** getNodePos();
      const updateExpression ***REMOVED*** parseUpdateExpression();
      return token() ***REMOVED******REMOVED******REMOVED*** 43 /* AsteriskAsteriskToken */ ? parseBinaryExpressionRest(getBinaryOperatorPrecedence(token()), updateExpression, pos) : updateExpression;
    }
    const unaryOperator ***REMOVED*** token();
    const simpleUnaryExpression ***REMOVED*** parseSimpleUnaryExpression();
    if (token() ***REMOVED******REMOVED******REMOVED*** 43 /* AsteriskAsteriskToken */) {
      const pos ***REMOVED*** skipTrivia(sourceText, simpleUnaryExpression.pos);
      const { end } ***REMOVED*** simpleUnaryExpression;
      if (simpleUnaryExpression.kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */) {
        parseErrorAt(pos, end, Diagnostics.A_type_assertion_expression_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_expression_Consider_enclosing_the_expression_in_parentheses);
      } else {
        Debug.assert(isKeywordOrPunctuation(unaryOperator));
        parseErrorAt(pos, end, Diagnostics.An_unary_expression_with_the_0_operator_is_not_allowed_in_the_left_hand_side_of_an_exponentiation_expression_Consider_enclosing_the_expression_in_parentheses, tokenToString(unaryOperator));
      }
    }
    return simpleUnaryExpression;
  }
  function parseSimpleUnaryExpression() {
    switch (token()) {
      case 40 /* PlusToken */:
      case 41 /* MinusToken */:
      case 55 /* TildeToken */:
      case 54 /* ExclamationToken */:
        return parsePrefixUnaryExpression();
      case 91 /* DeleteKeyword */:
        return parseDeleteExpression();
      case 114 /* TypeOfKeyword */:
        return parseTypeOfExpression();
      case 116 /* VoidKeyword */:
        return parseVoidExpression();
      case 30 /* LessThanToken */:
        if (languageVariant ***REMOVED******REMOVED******REMOVED*** 1 /* JSX */) {
          return parseJsxElementOrSelfClosingElementOrFragment(
            /*inExpressionContext*/
            true,
            /*topInvalidNodePosition*/
            void 0,
            /*openingTag*/
            void 0,
            /*mustBeUnary*/
            true
          );
        }
        return parseTypeAssertion();
      case 135 /* AwaitKeyword */:
        if (isAwaitExpression2()) {
          return parseAwaitExpression();
        }
      // falls through
      default:
        return parseUpdateExpression();
    }
  }
  function isUpdateExpression() {
    switch (token()) {
      case 40 /* PlusToken */:
      case 41 /* MinusToken */:
      case 55 /* TildeToken */:
      case 54 /* ExclamationToken */:
      case 91 /* DeleteKeyword */:
      case 114 /* TypeOfKeyword */:
      case 116 /* VoidKeyword */:
      case 135 /* AwaitKeyword */:
        return false;
      case 30 /* LessThanToken */:
        if (languageVariant !***REMOVED******REMOVED*** 1 /* JSX */) {
          return false;
        }
      // We are in JSX context and the token is part of JSXElement.
      // falls through
      default:
        return true;
    }
  }
  function parseUpdateExpression() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || token() ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
      const pos ***REMOVED*** getNodePos();
      return finishNode(factory2.createPrefixUnaryExpression(token(), nextTokenAnd(parseLeftHandSideExpressionOrHigher)), pos);
    } else if (languageVariant ***REMOVED******REMOVED******REMOVED*** 1 /* JSX */ && token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ && lookAhead(nextTokenIsIdentifierOrKeywordOrGreaterThan)) {
      return parseJsxElementOrSelfClosingElementOrFragment(
        /*inExpressionContext*/
        true
      );
    }
    const expression ***REMOVED*** parseLeftHandSideExpressionOrHigher();
    Debug.assert(isLeftHandSideExpression(expression));
    if ((token() ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || token() ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) && !scanner.hasPrecedingLineBreak()) {
      const operator ***REMOVED*** token();
      nextToken();
      return finishNode(factory2.createPostfixUnaryExpression(expression, operator), expression.pos);
    }
    return expression;
  }
  function parseLeftHandSideExpressionOrHigher() {
    const pos ***REMOVED*** getNodePos();
    let expression;
    if (token() ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */) {
      if (lookAhead(nextTokenIsOpenParenOrLessThan)) {
        sourceFlags |***REMOVED*** 4194304 /* PossiblyContainsDynamicImport */;
        expression ***REMOVED*** parseTokenNode();
      } else if (lookAhead(nextTokenIsDot)) {
        nextToken();
        nextToken();
        expression ***REMOVED*** finishNode(factory2.createMetaProperty(102 /* ImportKeyword */, parseIdentifierName()), pos);
        if (expression.name.escapedText ***REMOVED******REMOVED******REMOVED*** "defer") {
          if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
            sourceFlags |***REMOVED*** 4194304 /* PossiblyContainsDynamicImport */;
          }
        } else {
          sourceFlags |***REMOVED*** 8388608 /* PossiblyContainsImportMeta */;
        }
      } else {
        expression ***REMOVED*** parseMemberExpressionOrHigher();
      }
    } else {
      expression ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ ? parseSuperExpression() : parseMemberExpressionOrHigher();
    }
    return parseCallExpressionRest(pos, expression);
  }
  function parseMemberExpressionOrHigher() {
    const pos ***REMOVED*** getNodePos();
    const expression ***REMOVED*** parsePrimaryExpression();
    return parseMemberExpressionRest(
      pos,
      expression,
      /*allowOptionalChain*/
      true
    );
  }
  function parseSuperExpression() {
    const pos ***REMOVED*** getNodePos();
    let expression ***REMOVED*** parseTokenNode();
    if (token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      const startPos ***REMOVED*** getNodePos();
      const typeArguments ***REMOVED*** tryParse(parseTypeArgumentsInExpression);
      if (typeArguments !***REMOVED******REMOVED*** void 0) {
        parseErrorAt(startPos, getNodePos(), Diagnostics.super_may_not_use_type_arguments);
        if (!isTemplateStartOfTaggedTemplate()) {
          expression ***REMOVED*** factory2.createExpressionWithTypeArguments(expression, typeArguments);
        }
      }
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 25 /* DotToken */ || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      return expression;
    }
    parseExpectedToken(25 /* DotToken */, Diagnostics.super_must_be_followed_by_an_argument_list_or_member_access);
    return finishNode(factoryCreatePropertyAccessExpression(expression, parseRightSideOfDot(
      /*allowIdentifierNames*/
      true,
      /*allowPrivateIdentifiers*/
      true,
      /*allowUnicodeEscapeSequenceInIdentifierName*/
      true
    )), pos);
  }
  function parseJsxElementOrSelfClosingElementOrFragment(inExpressionContext, topInvalidNodePosition, openingTag, mustBeUnary ***REMOVED*** false) {
    const pos ***REMOVED*** getNodePos();
    const opening ***REMOVED*** parseJsxOpeningOrSelfClosingElementOrOpeningFragment(inExpressionContext);
    let result;
    if (opening.kind ***REMOVED******REMOVED******REMOVED*** 287 /* JsxOpeningElement */) {
      let children ***REMOVED*** parseJsxChildren(opening);
      let closingElement;
      const lastChild ***REMOVED*** children[children.length - 1];
      if ((lastChild ***REMOVED******REMOVED*** null ? void 0 : lastChild.kind) ***REMOVED******REMOVED******REMOVED*** 285 /* JsxElement */ && !tagNamesAreEquivalent(lastChild.openingElement.tagName, lastChild.closingElement.tagName) && tagNamesAreEquivalent(opening.tagName, lastChild.closingElement.tagName)) {
        const end ***REMOVED*** lastChild.children.end;
        const newLast ***REMOVED*** finishNode(
          factory2.createJsxElement(
            lastChild.openingElement,
            lastChild.children,
            finishNode(factory2.createJsxClosingElement(finishNode(factoryCreateIdentifier(""), end, end)), end, end)
          ),
          lastChild.openingElement.pos,
          end
        );
        children ***REMOVED*** createNodeArray([...children.slice(0, children.length - 1), newLast], children.pos, end);
        closingElement ***REMOVED*** lastChild.closingElement;
      } else {
        closingElement ***REMOVED*** parseJsxClosingElement(opening, inExpressionContext);
        if (!tagNamesAreEquivalent(opening.tagName, closingElement.tagName)) {
          if (openingTag && isJsxOpeningElement(openingTag) && tagNamesAreEquivalent(closingElement.tagName, openingTag.tagName)) {
            parseErrorAtRange(opening.tagName, Diagnostics.JSX_element_0_has_no_corresponding_closing_tag, getTextOfNodeFromSourceText(sourceText, opening.tagName));
          } else {
            parseErrorAtRange(closingElement.tagName, Diagnostics.Expected_corresponding_JSX_closing_tag_for_0, getTextOfNodeFromSourceText(sourceText, opening.tagName));
          }
        }
      }
      result ***REMOVED*** finishNode(factory2.createJsxElement(opening, children, closingElement), pos);
    } else if (opening.kind ***REMOVED******REMOVED******REMOVED*** 290 /* JsxOpeningFragment */) {
      result ***REMOVED*** finishNode(factory2.createJsxFragment(opening, parseJsxChildren(opening), parseJsxClosingFragment(inExpressionContext)), pos);
    } else {
      Debug.assert(opening.kind ***REMOVED******REMOVED******REMOVED*** 286 /* JsxSelfClosingElement */);
      result ***REMOVED*** opening;
    }
    if (!mustBeUnary && inExpressionContext && token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      const topBadPos ***REMOVED*** typeof topInvalidNodePosition ***REMOVED******REMOVED******REMOVED*** "undefined" ? result.pos : topInvalidNodePosition;
      const invalidElement ***REMOVED*** tryParse(() ***REMOVED***> parseJsxElementOrSelfClosingElementOrFragment(
        /*inExpressionContext*/
        true,
        topBadPos
      ));
      if (invalidElement) {
        const operatorToken ***REMOVED*** createMissingNode(
          28 /* CommaToken */,
          /*reportAtCurrentPosition*/
          false
        );
        setTextRangePosWidth(operatorToken, invalidElement.pos, 0);
        parseErrorAt(skipTrivia(sourceText, topBadPos), invalidElement.end, Diagnostics.JSX_expressions_must_have_one_parent_element);
        return finishNode(factory2.createBinaryExpression(result, operatorToken, invalidElement), pos);
      }
    }
    return result;
  }
  function parseJsxText() {
    const pos ***REMOVED*** getNodePos();
    const node ***REMOVED*** factory2.createJsxText(scanner.getTokenValue(), currentToken ***REMOVED******REMOVED******REMOVED*** 13 /* JsxTextAllWhiteSpaces */);
    currentToken ***REMOVED*** scanner.scanJsxToken();
    return finishNode(node, pos);
  }
  function parseJsxChild(openingTag, token2) {
    switch (token2) {
      case 1 /* EndOfFileToken */:
        if (isJsxOpeningFragment(openingTag)) {
          parseErrorAtRange(openingTag, Diagnostics.JSX_fragment_has_no_corresponding_closing_tag);
        } else {
          const tag ***REMOVED*** openingTag.tagName;
          const start ***REMOVED*** Math.min(skipTrivia(sourceText, tag.pos), tag.end);
          parseErrorAt(start, tag.end, Diagnostics.JSX_element_0_has_no_corresponding_closing_tag, getTextOfNodeFromSourceText(sourceText, openingTag.tagName));
        }
        return void 0;
      case 31 /* LessThanSlashToken */:
      case 7 /* ConflictMarkerTrivia */:
        return void 0;
      case 12 /* JsxText */:
      case 13 /* JsxTextAllWhiteSpaces */:
        return parseJsxText();
      case 19 /* OpenBraceToken */:
        return parseJsxExpression(
          /*inExpressionContext*/
          false
        );
      case 30 /* LessThanToken */:
        return parseJsxElementOrSelfClosingElementOrFragment(
          /*inExpressionContext*/
          false,
          /*topInvalidNodePosition*/
          void 0,
          openingTag
        );
      default:
        return Debug.assertNever(token2);
    }
  }
  function parseJsxChildren(openingTag) {
    const list ***REMOVED*** [];
    const listPos ***REMOVED*** getNodePos();
    const saveParsingContext ***REMOVED*** parsingContext;
    parsingContext |***REMOVED*** 1 << 14 /* JsxChildren */;
    while (true) {
      const child ***REMOVED*** parseJsxChild(openingTag, currentToken ***REMOVED*** scanner.reScanJsxToken());
      if (!child) break;
      list.push(child);
      if (isJsxOpeningElement(openingTag) && (child ***REMOVED******REMOVED*** null ? void 0 : child.kind) ***REMOVED******REMOVED******REMOVED*** 285 /* JsxElement */ && !tagNamesAreEquivalent(child.openingElement.tagName, child.closingElement.tagName) && tagNamesAreEquivalent(openingTag.tagName, child.closingElement.tagName)) {
        break;
      }
    }
    parsingContext ***REMOVED*** saveParsingContext;
    return createNodeArray(list, listPos);
  }
  function parseJsxAttributes() {
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createJsxAttributes(parseList(13 /* JsxAttributes */, parseJsxAttribute)), pos);
  }
  function parseJsxOpeningOrSelfClosingElementOrOpeningFragment(inExpressionContext) {
    const pos ***REMOVED*** getNodePos();
    parseExpected(30 /* LessThanToken */);
    if (token() ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */) {
      scanJsxText();
      return finishNode(factory2.createJsxOpeningFragment(), pos);
    }
    const tagName ***REMOVED*** parseJsxElementName();
    const typeArguments ***REMOVED*** (contextFlags & 524288 /* JavaScriptFile */) ***REMOVED******REMOVED******REMOVED*** 0 ? tryParseTypeArguments() : void 0;
    const attributes ***REMOVED*** parseJsxAttributes();
    let node;
    if (token() ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */) {
      scanJsxText();
      node ***REMOVED*** factory2.createJsxOpeningElement(tagName, typeArguments, attributes);
    } else {
      parseExpected(44 /* SlashToken */);
      if (parseExpected(
        32 /* GreaterThanToken */,
        /*diagnosticMessage*/
        void 0,
        /*shouldAdvance*/
        false
      )) {
        if (inExpressionContext) {
          nextToken();
        } else {
          scanJsxText();
        }
      }
      node ***REMOVED*** factory2.createJsxSelfClosingElement(tagName, typeArguments, attributes);
    }
    return finishNode(node, pos);
  }
  function parseJsxElementName() {
    const pos ***REMOVED*** getNodePos();
    const initialExpression ***REMOVED*** parseJsxTagName();
    if (isJsxNamespacedName(initialExpression)) {
      return initialExpression;
    }
    let expression ***REMOVED*** initialExpression;
    while (parseOptional(25 /* DotToken */)) {
      expression ***REMOVED*** finishNode(factoryCreatePropertyAccessExpression(expression, parseRightSideOfDot(
        /*allowIdentifierNames*/
        true,
        /*allowPrivateIdentifiers*/
        false,
        /*allowUnicodeEscapeSequenceInIdentifierName*/
        false
      )), pos);
    }
    return expression;
  }
  function parseJsxTagName() {
    const pos ***REMOVED*** getNodePos();
    scanJsxIdentifier();
    const isThis ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
    const tagName ***REMOVED*** parseIdentifierNameErrorOnUnicodeEscapeSequence();
    if (parseOptional(59 /* ColonToken */)) {
      scanJsxIdentifier();
      return finishNode(factory2.createJsxNamespacedName(tagName, parseIdentifierNameErrorOnUnicodeEscapeSequence()), pos);
    }
    return isThis ? finishNode(factory2.createToken(110 /* ThisKeyword */), pos) : tagName;
  }
  function parseJsxExpression(inExpressionContext) {
    const pos ***REMOVED*** getNodePos();
    if (!parseExpected(19 /* OpenBraceToken */)) {
      return void 0;
    }
    let dotDotDotToken;
    let expression;
    if (token() !***REMOVED******REMOVED*** 20 /* CloseBraceToken */) {
      if (!inExpressionContext) {
        dotDotDotToken ***REMOVED*** parseOptionalToken(26 /* DotDotDotToken */);
      }
      expression ***REMOVED*** parseExpression();
    }
    if (inExpressionContext) {
      parseExpected(20 /* CloseBraceToken */);
    } else {
      if (parseExpected(
        20 /* CloseBraceToken */,
        /*diagnosticMessage*/
        void 0,
        /*shouldAdvance*/
        false
      )) {
        scanJsxText();
      }
    }
    return finishNode(factory2.createJsxExpression(dotDotDotToken, expression), pos);
  }
  function parseJsxAttribute() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      return parseJsxSpreadAttribute();
    }
    const pos ***REMOVED*** getNodePos();
    return finishNode(factory2.createJsxAttribute(parseJsxAttributeName(), parseJsxAttributeValue()), pos);
  }
  function parseJsxAttributeValue() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      if (scanJsxAttributeValue() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
        return parseLiteralNode();
      }
      if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
        return parseJsxExpression(
          /*inExpressionContext*/
          true
        );
      }
      if (token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
        return parseJsxElementOrSelfClosingElementOrFragment(
          /*inExpressionContext*/
          true
        );
      }
      parseErrorAtCurrentToken(Diagnostics.or_JSX_element_expected);
    }
    return void 0;
  }
  function parseJsxAttributeName() {
    const pos ***REMOVED*** getNodePos();
    scanJsxIdentifier();
    const attrName ***REMOVED*** parseIdentifierNameErrorOnUnicodeEscapeSequence();
    if (parseOptional(59 /* ColonToken */)) {
      scanJsxIdentifier();
      return finishNode(factory2.createJsxNamespacedName(attrName, parseIdentifierNameErrorOnUnicodeEscapeSequence()), pos);
    }
    return attrName;
  }
  function parseJsxSpreadAttribute() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(19 /* OpenBraceToken */);
    parseExpected(26 /* DotDotDotToken */);
    const expression ***REMOVED*** parseExpression();
    parseExpected(20 /* CloseBraceToken */);
    return finishNode(factory2.createJsxSpreadAttribute(expression), pos);
  }
  function parseJsxClosingElement(open, inExpressionContext) {
    const pos ***REMOVED*** getNodePos();
    parseExpected(31 /* LessThanSlashToken */);
    const tagName ***REMOVED*** parseJsxElementName();
    if (parseExpected(
      32 /* GreaterThanToken */,
      /*diagnosticMessage*/
      void 0,
      /*shouldAdvance*/
      false
    )) {
      if (inExpressionContext || !tagNamesAreEquivalent(open.tagName, tagName)) {
        nextToken();
      } else {
        scanJsxText();
      }
    }
    return finishNode(factory2.createJsxClosingElement(tagName), pos);
  }
  function parseJsxClosingFragment(inExpressionContext) {
    const pos ***REMOVED*** getNodePos();
    parseExpected(31 /* LessThanSlashToken */);
    if (parseExpected(
      32 /* GreaterThanToken */,
      Diagnostics.Expected_corresponding_closing_tag_for_JSX_fragment,
      /*shouldAdvance*/
      false
    )) {
      if (inExpressionContext) {
        nextToken();
      } else {
        scanJsxText();
      }
    }
    return finishNode(factory2.createJsxJsxClosingFragment(), pos);
  }
  function parseTypeAssertion() {
    Debug.assert(languageVariant !***REMOVED******REMOVED*** 1 /* JSX */, "Type assertions should never be parsed in JSX; they should be parsed as comparisons or JSX elements/fragments.");
    const pos ***REMOVED*** getNodePos();
    parseExpected(30 /* LessThanToken */);
    const type ***REMOVED*** parseType();
    parseExpected(32 /* GreaterThanToken */);
    const expression ***REMOVED*** parseSimpleUnaryExpression();
    return finishNode(factory2.createTypeAssertion(type, expression), pos);
  }
  function nextTokenIsIdentifierOrKeywordOrOpenBracketOrTemplate() {
    nextToken();
    return tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || isTemplateStartOfTaggedTemplate();
  }
  function isStartOfOptionalPropertyOrElementAccessChain() {
    return token() ***REMOVED******REMOVED******REMOVED*** 29 /* QuestionDotToken */ && lookAhead(nextTokenIsIdentifierOrKeywordOrOpenBracketOrTemplate);
  }
  function tryReparseOptionalChain(node) {
    if (node.flags & 64 /* OptionalChain */) {
      return true;
    }
    if (isNonNullExpression(node)) {
      let expr ***REMOVED*** node.expression;
      while (isNonNullExpression(expr) && !(expr.flags & 64 /* OptionalChain */)) {
        expr ***REMOVED*** expr.expression;
      }
      if (expr.flags & 64 /* OptionalChain */) {
        while (isNonNullExpression(node)) {
          node.flags |***REMOVED*** 64 /* OptionalChain */;
          node ***REMOVED*** node.expression;
        }
        return true;
      }
    }
    return false;
  }
  function parsePropertyAccessExpressionRest(pos, expression, questionDotToken) {
    const name ***REMOVED*** parseRightSideOfDot(
      /*allowIdentifierNames*/
      true,
      /*allowPrivateIdentifiers*/
      true,
      /*allowUnicodeEscapeSequenceInIdentifierName*/
      true
    );
    const isOptionalChain2 ***REMOVED*** questionDotToken || tryReparseOptionalChain(expression);
    const propertyAccess ***REMOVED*** isOptionalChain2 ? factoryCreatePropertyAccessChain(expression, questionDotToken, name) : factoryCreatePropertyAccessExpression(expression, name);
    if (isOptionalChain2 && isPrivateIdentifier(propertyAccess.name)) {
      parseErrorAtRange(propertyAccess.name, Diagnostics.An_optional_chain_cannot_contain_private_identifiers);
    }
    if (isExpressionWithTypeArguments(expression) && expression.typeArguments) {
      const pos2 ***REMOVED*** expression.typeArguments.pos - 1;
      const end ***REMOVED*** skipTrivia(sourceText, expression.typeArguments.end) + 1;
      parseErrorAt(pos2, end, Diagnostics.An_instantiation_expression_cannot_be_followed_by_a_property_access);
    }
    return finishNode(propertyAccess, pos);
  }
  function parseElementAccessExpressionRest(pos, expression, questionDotToken) {
    let argumentExpression;
    if (token() ***REMOVED******REMOVED******REMOVED*** 24 /* CloseBracketToken */) {
      argumentExpression ***REMOVED*** createMissingNode(
        80 /* Identifier */,
        /*reportAtCurrentPosition*/
        true,
        Diagnostics.An_element_access_expression_should_take_an_argument
      );
    } else {
      const argument ***REMOVED*** allowInAnd(parseExpression);
      if (isStringOrNumericLiteralLike(argument)) {
        argument.text ***REMOVED*** internIdentifier(argument.text);
      }
      argumentExpression ***REMOVED*** argument;
    }
    parseExpected(24 /* CloseBracketToken */);
    const indexedAccess ***REMOVED*** questionDotToken || tryReparseOptionalChain(expression) ? factoryCreateElementAccessChain(expression, questionDotToken, argumentExpression) : factoryCreateElementAccessExpression(expression, argumentExpression);
    return finishNode(indexedAccess, pos);
  }
  function parseMemberExpressionRest(pos, expression, allowOptionalChain) {
    while (true) {
      let questionDotToken;
      let isPropertyAccess ***REMOVED*** false;
      if (allowOptionalChain && isStartOfOptionalPropertyOrElementAccessChain()) {
        questionDotToken ***REMOVED*** parseExpectedToken(29 /* QuestionDotToken */);
        isPropertyAccess ***REMOVED*** tokenIsIdentifierOrKeyword(token());
      } else {
        isPropertyAccess ***REMOVED*** parseOptional(25 /* DotToken */);
      }
      if (isPropertyAccess) {
        expression ***REMOVED*** parsePropertyAccessExpressionRest(pos, expression, questionDotToken);
        continue;
      }
      if ((questionDotToken || !inDecoratorContext()) && parseOptional(23 /* OpenBracketToken */)) {
        expression ***REMOVED*** parseElementAccessExpressionRest(pos, expression, questionDotToken);
        continue;
      }
      if (isTemplateStartOfTaggedTemplate()) {
        expression ***REMOVED*** !questionDotToken && expression.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */ ? parseTaggedTemplateRest(pos, expression.expression, questionDotToken, expression.typeArguments) : parseTaggedTemplateRest(
          pos,
          expression,
          questionDotToken,
          /*typeArguments*/
          void 0
        );
        continue;
      }
      if (!questionDotToken) {
        if (token() ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */ && !scanner.hasPrecedingLineBreak()) {
          nextToken();
          expression ***REMOVED*** finishNode(factory2.createNonNullExpression(expression), pos);
          continue;
        }
        const typeArguments ***REMOVED*** tryParse(parseTypeArgumentsInExpression);
        if (typeArguments) {
          expression ***REMOVED*** finishNode(factory2.createExpressionWithTypeArguments(expression, typeArguments), pos);
          continue;
        }
      }
      return expression;
    }
  }
  function isTemplateStartOfTaggedTemplate() {
    return token() ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 16 /* TemplateHead */;
  }
  function parseTaggedTemplateRest(pos, tag, questionDotToken, typeArguments) {
    const tagExpression ***REMOVED*** factory2.createTaggedTemplateExpression(
      tag,
      typeArguments,
      token() ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */ ? (reScanTemplateToken(
        /*isTaggedTemplate*/
        true
      ), parseLiteralNode()) : parseTemplateExpression(
        /*isTaggedTemplate*/
        true
      )
    );
    if (questionDotToken || tag.flags & 64 /* OptionalChain */) {
      tagExpression.flags |***REMOVED*** 64 /* OptionalChain */;
    }
    tagExpression.questionDotToken ***REMOVED*** questionDotToken;
    return finishNode(tagExpression, pos);
  }
  function parseCallExpressionRest(pos, expression) {
    while (true) {
      expression ***REMOVED*** parseMemberExpressionRest(
        pos,
        expression,
        /*allowOptionalChain*/
        true
      );
      let typeArguments;
      const questionDotToken ***REMOVED*** parseOptionalToken(29 /* QuestionDotToken */);
      if (questionDotToken) {
        typeArguments ***REMOVED*** tryParse(parseTypeArgumentsInExpression);
        if (isTemplateStartOfTaggedTemplate()) {
          expression ***REMOVED*** parseTaggedTemplateRest(pos, expression, questionDotToken, typeArguments);
          continue;
        }
      }
      if (typeArguments || token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */) {
        if (!questionDotToken && expression.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */) {
          typeArguments ***REMOVED*** expression.typeArguments;
          expression ***REMOVED*** expression.expression;
        }
        const argumentList ***REMOVED*** parseArgumentList();
        const callExpr ***REMOVED*** questionDotToken || tryReparseOptionalChain(expression) ? factoryCreateCallChain(expression, questionDotToken, typeArguments, argumentList) : factoryCreateCallExpression(expression, typeArguments, argumentList);
        expression ***REMOVED*** finishNode(callExpr, pos);
        continue;
      }
      if (questionDotToken) {
        const name ***REMOVED*** createMissingNode(
          80 /* Identifier */,
          /*reportAtCurrentPosition*/
          false,
          Diagnostics.Identifier_expected
        );
        expression ***REMOVED*** finishNode(factoryCreatePropertyAccessChain(expression, questionDotToken, name), pos);
      }
      break;
    }
    return expression;
  }
  function parseArgumentList() {
    parseExpected(21 /* OpenParenToken */);
    const result ***REMOVED*** parseDelimitedList(11 /* ArgumentExpressions */, parseArgumentExpression);
    parseExpected(22 /* CloseParenToken */);
    return result;
  }
  function parseTypeArgumentsInExpression() {
    if ((contextFlags & 524288 /* JavaScriptFile */) !***REMOVED******REMOVED*** 0) {
      return void 0;
    }
    if (reScanLessThanToken() !***REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return void 0;
    }
    nextToken();
    const typeArguments ***REMOVED*** parseDelimitedList(20 /* TypeArguments */, parseType);
    if (reScanGreaterToken() !***REMOVED******REMOVED*** 32 /* GreaterThanToken */) {
      return void 0;
    }
    nextToken();
    return typeArguments && canFollowTypeArgumentsInExpression() ? typeArguments : void 0;
  }
  function canFollowTypeArgumentsInExpression() {
    switch (token()) {
      // These tokens can follow a type argument list in a call expression.
      case 21 /* OpenParenToken */:
      // foo<x>(
      case 15 /* NoSubstitutionTemplateLiteral */:
      // foo<T> `...`
      case 16 /* TemplateHead */:
        return true;
      // A type argument list followed by `<` never makes sense, and a type argument list followed
      // by `>` is ambiguous with a (re-scanned) `>>` operator, so we disqualify both. Also, in
      // this context, `+` and `-` are unary operators, not binary operators.
      case 30 /* LessThanToken */:
      case 32 /* GreaterThanToken */:
      case 40 /* PlusToken */:
      case 41 /* MinusToken */:
        return false;
    }
    return scanner.hasPrecedingLineBreak() || isBinaryOperator2() || !isStartOfExpression();
  }
  function parsePrimaryExpression() {
    switch (token()) {
      case 15 /* NoSubstitutionTemplateLiteral */:
        if (scanner.getTokenFlags() & 26656 /* IsInvalid */) {
          reScanTemplateToken(
            /*isTaggedTemplate*/
            false
          );
        }
      // falls through
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 11 /* StringLiteral */:
        return parseLiteralNode();
      case 110 /* ThisKeyword */:
      case 108 /* SuperKeyword */:
      case 106 /* NullKeyword */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
        return parseTokenNode();
      case 21 /* OpenParenToken */:
        return parseParenthesizedExpression();
      case 23 /* OpenBracketToken */:
        return parseArrayLiteralExpression();
      case 19 /* OpenBraceToken */:
        return parseObjectLiteralExpression();
      case 134 /* AsyncKeyword */:
        if (!lookAhead(nextTokenIsFunctionKeywordOnSameLine)) {
          break;
        }
        return parseFunctionExpression();
      case 60 /* AtToken */:
        return parseDecoratedExpression();
      case 86 /* ClassKeyword */:
        return parseClassExpression();
      case 100 /* FunctionKeyword */:
        return parseFunctionExpression();
      case 105 /* NewKeyword */:
        return parseNewExpressionOrNewDotTarget();
      case 44 /* SlashToken */:
      case 69 /* SlashEqualsToken */:
        if (reScanSlashToken() ***REMOVED******REMOVED******REMOVED*** 14 /* RegularExpressionLiteral */) {
          return parseLiteralNode();
        }
        break;
      case 16 /* TemplateHead */:
        return parseTemplateExpression(
          /*isTaggedTemplate*/
          false
        );
      case 81 /* PrivateIdentifier */:
        return parsePrivateIdentifier();
    }
    return parseIdentifier(Diagnostics.Expression_expected);
  }
  function parseParenthesizedExpression() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpected(22 /* CloseParenToken */);
    return withJSDoc(finishNode(factoryCreateParenthesizedExpression(expression), pos), hasJSDoc);
  }
  function parseSpreadElement() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(26 /* DotDotDotToken */);
    const expression ***REMOVED*** parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    );
    return finishNode(factory2.createSpreadElement(expression), pos);
  }
  function parseArgumentOrArrayLiteralElement() {
    return token() ***REMOVED******REMOVED******REMOVED*** 26 /* DotDotDotToken */ ? parseSpreadElement() : token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ ? finishNode(factory2.createOmittedExpression(), getNodePos()) : parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    );
  }
  function parseArgumentExpression() {
    return doOutsideOfContext(disallowInAndDecoratorContext, parseArgumentOrArrayLiteralElement);
  }
  function parseArrayLiteralExpression() {
    const pos ***REMOVED*** getNodePos();
    const openBracketPosition ***REMOVED*** scanner.getTokenStart();
    const openBracketParsed ***REMOVED*** parseExpected(23 /* OpenBracketToken */);
    const multiLine ***REMOVED*** scanner.hasPrecedingLineBreak();
    const elements ***REMOVED*** parseDelimitedList(15 /* ArrayLiteralMembers */, parseArgumentOrArrayLiteralElement);
    parseExpectedMatchingBrackets(23 /* OpenBracketToken */, 24 /* CloseBracketToken */, openBracketParsed, openBracketPosition);
    return finishNode(factoryCreateArrayLiteralExpression(elements, multiLine), pos);
  }
  function parseObjectLiteralElement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    if (parseOptionalToken(26 /* DotDotDotToken */)) {
      const expression ***REMOVED*** parseAssignmentExpressionOrHigher(
        /*allowReturnTypeInArrowFunction*/
        true
      );
      return withJSDoc(finishNode(factory2.createSpreadAssignment(expression), pos), hasJSDoc);
    }
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      true
    );
    if (parseContextualModifier(139 /* GetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 178 /* GetAccessor */, 0 /* None */);
    }
    if (parseContextualModifier(153 /* SetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 179 /* SetAccessor */, 0 /* None */);
    }
    const asteriskToken ***REMOVED*** parseOptionalToken(42 /* AsteriskToken */);
    const tokenIsIdentifier ***REMOVED*** isIdentifier2();
    const name ***REMOVED*** parsePropertyName();
    const questionToken ***REMOVED*** parseOptionalToken(58 /* QuestionToken */);
    const exclamationToken ***REMOVED*** parseOptionalToken(54 /* ExclamationToken */);
    if (asteriskToken || token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return parseMethodDeclaration(pos, hasJSDoc, modifiers, asteriskToken, name, questionToken, exclamationToken);
    }
    let node;
    const isShorthandPropertyAssignment2 ***REMOVED*** tokenIsIdentifier && token() !***REMOVED******REMOVED*** 59 /* ColonToken */;
    if (isShorthandPropertyAssignment2) {
      const equalsToken ***REMOVED*** parseOptionalToken(64 /* EqualsToken */);
      const objectAssignmentInitializer ***REMOVED*** equalsToken ? allowInAnd(() ***REMOVED***> parseAssignmentExpressionOrHigher(
        /*allowReturnTypeInArrowFunction*/
        true
      )) : void 0;
      node ***REMOVED*** factory2.createShorthandPropertyAssignment(name, objectAssignmentInitializer);
      node.equalsToken ***REMOVED*** equalsToken;
    } else {
      parseExpected(59 /* ColonToken */);
      const initializer ***REMOVED*** allowInAnd(() ***REMOVED***> parseAssignmentExpressionOrHigher(
        /*allowReturnTypeInArrowFunction*/
        true
      ));
      node ***REMOVED*** factory2.createPropertyAssignment(name, initializer);
    }
    node.modifiers ***REMOVED*** modifiers;
    node.questionToken ***REMOVED*** questionToken;
    node.exclamationToken ***REMOVED*** exclamationToken;
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseObjectLiteralExpression() {
    const pos ***REMOVED*** getNodePos();
    const openBracePosition ***REMOVED*** scanner.getTokenStart();
    const openBraceParsed ***REMOVED*** parseExpected(19 /* OpenBraceToken */);
    const multiLine ***REMOVED*** scanner.hasPrecedingLineBreak();
    const properties ***REMOVED*** parseDelimitedList(
      12 /* ObjectLiteralMembers */,
      parseObjectLiteralElement,
      /*considerSemicolonAsDelimiter*/
      true
    );
    parseExpectedMatchingBrackets(19 /* OpenBraceToken */, 20 /* CloseBraceToken */, openBraceParsed, openBracePosition);
    return finishNode(factoryCreateObjectLiteralExpression(properties, multiLine), pos);
  }
  function parseFunctionExpression() {
    const savedDecoratorContext ***REMOVED*** inDecoratorContext();
    setDecoratorContext(
      /*val*/
      false
    );
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      false
    );
    parseExpected(100 /* FunctionKeyword */);
    const asteriskToken ***REMOVED*** parseOptionalToken(42 /* AsteriskToken */);
    const isGenerator ***REMOVED*** asteriskToken ? 1 /* Yield */ : 0 /* None */;
    const isAsync ***REMOVED*** some(modifiers, isAsyncModifier) ? 2 /* Await */ : 0 /* None */;
    const name ***REMOVED*** isGenerator && isAsync ? doInYieldAndAwaitContext(parseOptionalBindingIdentifier) : isGenerator ? doInYieldContext(parseOptionalBindingIdentifier) : isAsync ? doInAwaitContext(parseOptionalBindingIdentifier) : parseOptionalBindingIdentifier();
    const typeParameters ***REMOVED*** parseTypeParameters();
    const parameters ***REMOVED*** parseParameters(isGenerator | isAsync);
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      false
    );
    const body ***REMOVED*** parseFunctionBlock(isGenerator | isAsync);
    setDecoratorContext(savedDecoratorContext);
    const node ***REMOVED*** factory2.createFunctionExpression(modifiers, asteriskToken, name, typeParameters, parameters, type, body);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseOptionalBindingIdentifier() {
    return isBindingIdentifier() ? parseBindingIdentifier() : void 0;
  }
  function parseNewExpressionOrNewDotTarget() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(105 /* NewKeyword */);
    if (parseOptional(25 /* DotToken */)) {
      const name ***REMOVED*** parseIdentifierName();
      return finishNode(factory2.createMetaProperty(105 /* NewKeyword */, name), pos);
    }
    const expressionPos ***REMOVED*** getNodePos();
    let expression ***REMOVED*** parseMemberExpressionRest(
      expressionPos,
      parsePrimaryExpression(),
      /*allowOptionalChain*/
      false
    );
    let typeArguments;
    if (expression.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */) {
      typeArguments ***REMOVED*** expression.typeArguments;
      expression ***REMOVED*** expression.expression;
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 29 /* QuestionDotToken */) {
      parseErrorAtCurrentToken(Diagnostics.Invalid_optional_chain_from_new_expression_Did_you_mean_to_call_0, getTextOfNodeFromSourceText(sourceText, expression));
    }
    const argumentList ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ ? parseArgumentList() : void 0;
    return finishNode(factoryCreateNewExpression(expression, typeArguments, argumentList), pos);
  }
  function parseBlock(ignoreMissingOpenBrace, diagnosticMessage) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const openBracePosition ***REMOVED*** scanner.getTokenStart();
    const openBraceParsed ***REMOVED*** parseExpected(19 /* OpenBraceToken */, diagnosticMessage);
    if (openBraceParsed || ignoreMissingOpenBrace) {
      const multiLine ***REMOVED*** scanner.hasPrecedingLineBreak();
      const statements ***REMOVED*** parseList(1 /* BlockStatements */, parseStatement);
      parseExpectedMatchingBrackets(19 /* OpenBraceToken */, 20 /* CloseBraceToken */, openBraceParsed, openBracePosition);
      const result ***REMOVED*** withJSDoc(finishNode(factoryCreateBlock(statements, multiLine), pos), hasJSDoc);
      if (token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
        parseErrorAtCurrentToken(Diagnostics.Declaration_or_statement_expected_This_follows_a_block_of_statements_so_if_you_intended_to_write_a_destructuring_assignment_you_might_need_to_wrap_the_whole_assignment_in_parentheses);
        nextToken();
      }
      return result;
    } else {
      const statements ***REMOVED*** createMissingList();
      return withJSDoc(finishNode(factoryCreateBlock(
        statements,
        /*multiLine*/
        void 0
      ), pos), hasJSDoc);
    }
  }
  function parseFunctionBlock(flags, diagnosticMessage) {
    const savedYieldContext ***REMOVED*** inYieldContext();
    setYieldContext(!!(flags & 1 /* Yield */));
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    setAwaitContext(!!(flags & 2 /* Await */));
    const savedTopLevel ***REMOVED*** topLevel;
    topLevel ***REMOVED*** false;
    const saveDecoratorContext ***REMOVED*** inDecoratorContext();
    if (saveDecoratorContext) {
      setDecoratorContext(
        /*val*/
        false
      );
    }
    const block ***REMOVED*** parseBlock(!!(flags & 16 /* IgnoreMissingOpenBrace */), diagnosticMessage);
    if (saveDecoratorContext) {
      setDecoratorContext(
        /*val*/
        true
      );
    }
    topLevel ***REMOVED*** savedTopLevel;
    setYieldContext(savedYieldContext);
    setAwaitContext(savedAwaitContext);
    return block;
  }
  function parseEmptyStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(27 /* SemicolonToken */);
    return withJSDoc(finishNode(factory2.createEmptyStatement(), pos), hasJSDoc);
  }
  function parseIfStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(101 /* IfKeyword */);
    const openParenPosition ***REMOVED*** scanner.getTokenStart();
    const openParenParsed ***REMOVED*** parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpectedMatchingBrackets(21 /* OpenParenToken */, 22 /* CloseParenToken */, openParenParsed, openParenPosition);
    const thenStatement ***REMOVED*** parseStatement();
    const elseStatement ***REMOVED*** parseOptional(93 /* ElseKeyword */) ? parseStatement() : void 0;
    return withJSDoc(finishNode(factoryCreateIfStatement(expression, thenStatement, elseStatement), pos), hasJSDoc);
  }
  function parseDoStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(92 /* DoKeyword */);
    const statement ***REMOVED*** parseStatement();
    parseExpected(117 /* WhileKeyword */);
    const openParenPosition ***REMOVED*** scanner.getTokenStart();
    const openParenParsed ***REMOVED*** parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpectedMatchingBrackets(21 /* OpenParenToken */, 22 /* CloseParenToken */, openParenParsed, openParenPosition);
    parseOptional(27 /* SemicolonToken */);
    return withJSDoc(finishNode(factory2.createDoStatement(statement, expression), pos), hasJSDoc);
  }
  function parseWhileStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(117 /* WhileKeyword */);
    const openParenPosition ***REMOVED*** scanner.getTokenStart();
    const openParenParsed ***REMOVED*** parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpectedMatchingBrackets(21 /* OpenParenToken */, 22 /* CloseParenToken */, openParenParsed, openParenPosition);
    const statement ***REMOVED*** parseStatement();
    return withJSDoc(finishNode(factoryCreateWhileStatement(expression, statement), pos), hasJSDoc);
  }
  function parseForOrForInOrForOfStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(99 /* ForKeyword */);
    const awaitToken ***REMOVED*** parseOptionalToken(135 /* AwaitKeyword */);
    parseExpected(21 /* OpenParenToken */);
    let initializer;
    if (token() !***REMOVED******REMOVED*** 27 /* SemicolonToken */) {
      if (token() ***REMOVED******REMOVED******REMOVED*** 115 /* VarKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 121 /* LetKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 87 /* ConstKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 160 /* UsingKeyword */ && lookAhead(nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLineDisallowOf) || // this one is meant to allow of
      token() ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */ && lookAhead(nextTokenIsUsingKeywordThenBindingIdentifierOrStartOfObjectDestructuringOnSameLine)) {
        initializer ***REMOVED*** parseVariableDeclarationList(
          /*inForStatementInitializer*/
          true
        );
      } else {
        initializer ***REMOVED*** disallowInAnd(parseExpression);
      }
    }
    let node;
    if (awaitToken ? parseExpected(165 /* OfKeyword */) : parseOptional(165 /* OfKeyword */)) {
      const expression ***REMOVED*** allowInAnd(() ***REMOVED***> parseAssignmentExpressionOrHigher(
        /*allowReturnTypeInArrowFunction*/
        true
      ));
      parseExpected(22 /* CloseParenToken */);
      node ***REMOVED*** factoryCreateForOfStatement(awaitToken, initializer, expression, parseStatement());
    } else if (parseOptional(103 /* InKeyword */)) {
      const expression ***REMOVED*** allowInAnd(parseExpression);
      parseExpected(22 /* CloseParenToken */);
      node ***REMOVED*** factory2.createForInStatement(initializer, expression, parseStatement());
    } else {
      parseExpected(27 /* SemicolonToken */);
      const condition ***REMOVED*** token() !***REMOVED******REMOVED*** 27 /* SemicolonToken */ && token() !***REMOVED******REMOVED*** 22 /* CloseParenToken */ ? allowInAnd(parseExpression) : void 0;
      parseExpected(27 /* SemicolonToken */);
      const incrementor ***REMOVED*** token() !***REMOVED******REMOVED*** 22 /* CloseParenToken */ ? allowInAnd(parseExpression) : void 0;
      parseExpected(22 /* CloseParenToken */);
      node ***REMOVED*** factoryCreateForStatement(initializer, condition, incrementor, parseStatement());
    }
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseBreakOrContinueStatement(kind) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? 83 /* BreakKeyword */ : 88 /* ContinueKeyword */);
    const label ***REMOVED*** canParseSemicolon() ? void 0 : parseIdentifier();
    parseSemicolon();
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? factory2.createBreakStatement(label) : factory2.createContinueStatement(label);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseReturnStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(107 /* ReturnKeyword */);
    const expression ***REMOVED*** canParseSemicolon() ? void 0 : allowInAnd(parseExpression);
    parseSemicolon();
    return withJSDoc(finishNode(factory2.createReturnStatement(expression), pos), hasJSDoc);
  }
  function parseWithStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(118 /* WithKeyword */);
    const openParenPosition ***REMOVED*** scanner.getTokenStart();
    const openParenParsed ***REMOVED*** parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpectedMatchingBrackets(21 /* OpenParenToken */, 22 /* CloseParenToken */, openParenParsed, openParenPosition);
    const statement ***REMOVED*** doInsideOfContext(67108864 /* InWithStatement */, parseStatement);
    return withJSDoc(finishNode(factory2.createWithStatement(expression, statement), pos), hasJSDoc);
  }
  function parseCaseClause() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(84 /* CaseKeyword */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpected(59 /* ColonToken */);
    const statements ***REMOVED*** parseList(3 /* SwitchClauseStatements */, parseStatement);
    return withJSDoc(finishNode(factory2.createCaseClause(expression, statements), pos), hasJSDoc);
  }
  function parseDefaultClause() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(90 /* DefaultKeyword */);
    parseExpected(59 /* ColonToken */);
    const statements ***REMOVED*** parseList(3 /* SwitchClauseStatements */, parseStatement);
    return finishNode(factory2.createDefaultClause(statements), pos);
  }
  function parseCaseOrDefaultClause() {
    return token() ***REMOVED******REMOVED******REMOVED*** 84 /* CaseKeyword */ ? parseCaseClause() : parseDefaultClause();
  }
  function parseCaseBlock() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(19 /* OpenBraceToken */);
    const clauses ***REMOVED*** parseList(2 /* SwitchClauses */, parseCaseOrDefaultClause);
    parseExpected(20 /* CloseBraceToken */);
    return finishNode(factory2.createCaseBlock(clauses), pos);
  }
  function parseSwitchStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(109 /* SwitchKeyword */);
    parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** allowInAnd(parseExpression);
    parseExpected(22 /* CloseParenToken */);
    const caseBlock ***REMOVED*** parseCaseBlock();
    return withJSDoc(finishNode(factory2.createSwitchStatement(expression, caseBlock), pos), hasJSDoc);
  }
  function parseThrowStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(111 /* ThrowKeyword */);
    let expression ***REMOVED*** scanner.hasPrecedingLineBreak() ? void 0 : allowInAnd(parseExpression);
    if (expression ***REMOVED******REMOVED******REMOVED*** void 0) {
      identifierCount++;
      expression ***REMOVED*** finishNode(factoryCreateIdentifier(""), getNodePos());
    }
    if (!tryParseSemicolon()) {
      parseErrorForMissingSemicolonAfter(expression);
    }
    return withJSDoc(finishNode(factory2.createThrowStatement(expression), pos), hasJSDoc);
  }
  function parseTryStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(113 /* TryKeyword */);
    const tryBlock ***REMOVED*** parseBlock(
      /*ignoreMissingOpenBrace*/
      false
    );
    const catchClause ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 85 /* CatchKeyword */ ? parseCatchClause() : void 0;
    let finallyBlock;
    if (!catchClause || token() ***REMOVED******REMOVED******REMOVED*** 98 /* FinallyKeyword */) {
      parseExpected(98 /* FinallyKeyword */, Diagnostics.catch_or_finally_expected);
      finallyBlock ***REMOVED*** parseBlock(
        /*ignoreMissingOpenBrace*/
        false
      );
    }
    return withJSDoc(finishNode(factory2.createTryStatement(tryBlock, catchClause, finallyBlock), pos), hasJSDoc);
  }
  function parseCatchClause() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(85 /* CatchKeyword */);
    let variableDeclaration;
    if (parseOptional(21 /* OpenParenToken */)) {
      variableDeclaration ***REMOVED*** parseVariableDeclaration();
      parseExpected(22 /* CloseParenToken */);
    } else {
      variableDeclaration ***REMOVED*** void 0;
    }
    const block ***REMOVED*** parseBlock(
      /*ignoreMissingOpenBrace*/
      false
    );
    return finishNode(factory2.createCatchClause(variableDeclaration, block), pos);
  }
  function parseDebuggerStatement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    parseExpected(89 /* DebuggerKeyword */);
    parseSemicolon();
    return withJSDoc(finishNode(factory2.createDebuggerStatement(), pos), hasJSDoc);
  }
  function parseExpressionOrLabeledStatement() {
    const pos ***REMOVED*** getNodePos();
    let hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    let node;
    const hasParen ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */;
    const expression ***REMOVED*** allowInAnd(parseExpression);
    if (isIdentifier(expression) && parseOptional(59 /* ColonToken */)) {
      node ***REMOVED*** factory2.createLabeledStatement(expression, parseStatement());
    } else {
      if (!tryParseSemicolon()) {
        parseErrorForMissingSemicolonAfter(expression);
      }
      node ***REMOVED*** factoryCreateExpressionStatement(expression);
      if (hasParen) {
        hasJSDoc ***REMOVED*** false;
      }
    }
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function nextTokenIsIdentifierOrKeywordOnSameLine() {
    nextToken();
    return tokenIsIdentifierOrKeyword(token()) && !scanner.hasPrecedingLineBreak();
  }
  function nextTokenIsClassKeywordOnSameLine() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 86 /* ClassKeyword */ && !scanner.hasPrecedingLineBreak();
  }
  function nextTokenIsFunctionKeywordOnSameLine() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 100 /* FunctionKeyword */ && !scanner.hasPrecedingLineBreak();
  }
  function nextTokenIsIdentifierOrKeywordOrLiteralOnSameLine() {
    nextToken();
    return (tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) && !scanner.hasPrecedingLineBreak();
  }
  function isDeclaration2() {
    while (true) {
      switch (token()) {
        case 115 /* VarKeyword */:
        case 121 /* LetKeyword */:
        case 87 /* ConstKeyword */:
        case 100 /* FunctionKeyword */:
        case 86 /* ClassKeyword */:
        case 94 /* EnumKeyword */:
          return true;
        case 160 /* UsingKeyword */:
          return isUsingDeclaration();
        case 135 /* AwaitKeyword */:
          return isAwaitUsingDeclaration();
        // 'declare', 'module', 'namespace', 'interface'* and 'type' are all legal JavaScript identifiers;
        // however, an identifier cannot be followed by another identifier on the same line. This is what we
        // count on to parse out the respective declarations. For instance, we exploit this to say that
        //
        //    namespace n
        //
        // can be none other than the beginning of a namespace declaration, but need to respect that JavaScript sees
        //
        //    namespace
        //    n
        //
        // as the identifier 'namespace' on one line followed by the identifier 'n' on another.
        // We need to look one token ahead to see if it permissible to try parsing a declaration.
        //
        // *Note*: 'interface' is actually a strict mode reserved word. So while
        //
        //   "use strict"
        //   interface
        //   I {}
        //
        // could be legal, it would add complexity for very little gain.
        case 120 /* InterfaceKeyword */:
        case 156 /* TypeKeyword */:
        case 166 /* DeferKeyword */:
          return nextTokenIsIdentifierOnSameLine();
        case 144 /* ModuleKeyword */:
        case 145 /* NamespaceKeyword */:
          return nextTokenIsIdentifierOrStringLiteralOnSameLine();
        case 128 /* AbstractKeyword */:
        case 129 /* AccessorKeyword */:
        case 134 /* AsyncKeyword */:
        case 138 /* DeclareKeyword */:
        case 123 /* PrivateKeyword */:
        case 124 /* ProtectedKeyword */:
        case 125 /* PublicKeyword */:
        case 148 /* ReadonlyKeyword */:
          const previousToken ***REMOVED*** token();
          nextToken();
          if (scanner.hasPrecedingLineBreak()) {
            return false;
          }
          if (previousToken ***REMOVED******REMOVED******REMOVED*** 138 /* DeclareKeyword */ && token() ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */) {
            return true;
          }
          continue;
        case 162 /* GlobalKeyword */:
          nextToken();
          return token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || token() ***REMOVED******REMOVED******REMOVED*** 95 /* ExportKeyword */;
        case 102 /* ImportKeyword */:
          nextToken();
          return token() ***REMOVED******REMOVED******REMOVED*** 166 /* DeferKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || tokenIsIdentifierOrKeyword(token());
        case 95 /* ExportKeyword */:
          let currentToken2 ***REMOVED*** nextToken();
          if (currentToken2 ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */) {
            currentToken2 ***REMOVED*** lookAhead(nextToken);
          }
          if (currentToken2 ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 90 /* DefaultKeyword */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */) {
            return true;
          }
          continue;
        case 126 /* StaticKeyword */:
          nextToken();
          continue;
        default:
          return false;
      }
    }
  }
  function isStartOfDeclaration() {
    return lookAhead(isDeclaration2);
  }
  function isStartOfStatement() {
    switch (token()) {
      case 60 /* AtToken */:
      case 27 /* SemicolonToken */:
      case 19 /* OpenBraceToken */:
      case 115 /* VarKeyword */:
      case 121 /* LetKeyword */:
      case 160 /* UsingKeyword */:
      case 100 /* FunctionKeyword */:
      case 86 /* ClassKeyword */:
      case 94 /* EnumKeyword */:
      case 101 /* IfKeyword */:
      case 92 /* DoKeyword */:
      case 117 /* WhileKeyword */:
      case 99 /* ForKeyword */:
      case 88 /* ContinueKeyword */:
      case 83 /* BreakKeyword */:
      case 107 /* ReturnKeyword */:
      case 118 /* WithKeyword */:
      case 109 /* SwitchKeyword */:
      case 111 /* ThrowKeyword */:
      case 113 /* TryKeyword */:
      case 89 /* DebuggerKeyword */:
      // 'catch' and 'finally' do not actually indicate that the code is part of a statement,
      // however, we say they are here so that we may gracefully parse them and error later.
      // falls through
      case 85 /* CatchKeyword */:
      case 98 /* FinallyKeyword */:
        return true;
      case 102 /* ImportKeyword */:
        return isStartOfDeclaration() || lookAhead(nextTokenIsOpenParenOrLessThanOrDot);
      case 87 /* ConstKeyword */:
      case 95 /* ExportKeyword */:
        return isStartOfDeclaration();
      case 134 /* AsyncKeyword */:
      case 138 /* DeclareKeyword */:
      case 120 /* InterfaceKeyword */:
      case 144 /* ModuleKeyword */:
      case 145 /* NamespaceKeyword */:
      case 156 /* TypeKeyword */:
      case 162 /* GlobalKeyword */:
      case 166 /* DeferKeyword */:
        return true;
      case 129 /* AccessorKeyword */:
      case 125 /* PublicKeyword */:
      case 123 /* PrivateKeyword */:
      case 124 /* ProtectedKeyword */:
      case 126 /* StaticKeyword */:
      case 148 /* ReadonlyKeyword */:
        return isStartOfDeclaration() || !lookAhead(nextTokenIsIdentifierOrKeywordOnSameLine);
      default:
        return isStartOfExpression();
    }
  }
  function nextTokenIsBindingIdentifierOrStartOfDestructuring() {
    nextToken();
    return isBindingIdentifier() || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */;
  }
  function isLetDeclaration() {
    return lookAhead(nextTokenIsBindingIdentifierOrStartOfDestructuring);
  }
  function nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLineDisallowOf() {
    return nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLine(
      /*disallowOf*/
      true
    );
  }
  function nextTokenIsEqualsOrSemicolonOrColonToken() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ || token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */ || token() ***REMOVED******REMOVED******REMOVED*** 59 /* ColonToken */;
  }
  function nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLine(disallowOf) {
    nextToken();
    if (disallowOf && token() ***REMOVED******REMOVED******REMOVED*** 165 /* OfKeyword */) {
      return lookAhead(nextTokenIsEqualsOrSemicolonOrColonToken);
    }
    return (isBindingIdentifier() || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) && !scanner.hasPrecedingLineBreak();
  }
  function isUsingDeclaration() {
    return lookAhead(nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLine);
  }
  function nextTokenIsUsingKeywordThenBindingIdentifierOrStartOfObjectDestructuringOnSameLine(disallowOf) {
    if (nextToken() ***REMOVED******REMOVED******REMOVED*** 160 /* UsingKeyword */) {
      return nextTokenIsBindingIdentifierOrStartOfDestructuringOnSameLine(disallowOf);
    }
    return false;
  }
  function isAwaitUsingDeclaration() {
    return lookAhead(nextTokenIsUsingKeywordThenBindingIdentifierOrStartOfObjectDestructuringOnSameLine);
  }
  function parseStatement() {
    switch (token()) {
      case 27 /* SemicolonToken */:
        return parseEmptyStatement();
      case 19 /* OpenBraceToken */:
        return parseBlock(
          /*ignoreMissingOpenBrace*/
          false
        );
      case 115 /* VarKeyword */:
        return parseVariableStatement(
          getNodePos(),
          hasPrecedingJSDocComment(),
          /*modifiers*/
          void 0
        );
      case 121 /* LetKeyword */:
        if (isLetDeclaration()) {
          return parseVariableStatement(
            getNodePos(),
            hasPrecedingJSDocComment(),
            /*modifiers*/
            void 0
          );
        }
        break;
      case 135 /* AwaitKeyword */:
        if (isAwaitUsingDeclaration()) {
          return parseVariableStatement(
            getNodePos(),
            hasPrecedingJSDocComment(),
            /*modifiers*/
            void 0
          );
        }
        break;
      case 160 /* UsingKeyword */:
        if (isUsingDeclaration()) {
          return parseVariableStatement(
            getNodePos(),
            hasPrecedingJSDocComment(),
            /*modifiers*/
            void 0
          );
        }
        break;
      case 100 /* FunctionKeyword */:
        return parseFunctionDeclaration(
          getNodePos(),
          hasPrecedingJSDocComment(),
          /*modifiers*/
          void 0
        );
      case 86 /* ClassKeyword */:
        return parseClassDeclaration(
          getNodePos(),
          hasPrecedingJSDocComment(),
          /*modifiers*/
          void 0
        );
      case 101 /* IfKeyword */:
        return parseIfStatement();
      case 92 /* DoKeyword */:
        return parseDoStatement();
      case 117 /* WhileKeyword */:
        return parseWhileStatement();
      case 99 /* ForKeyword */:
        return parseForOrForInOrForOfStatement();
      case 88 /* ContinueKeyword */:
        return parseBreakOrContinueStatement(252 /* ContinueStatement */);
      case 83 /* BreakKeyword */:
        return parseBreakOrContinueStatement(253 /* BreakStatement */);
      case 107 /* ReturnKeyword */:
        return parseReturnStatement();
      case 118 /* WithKeyword */:
        return parseWithStatement();
      case 109 /* SwitchKeyword */:
        return parseSwitchStatement();
      case 111 /* ThrowKeyword */:
        return parseThrowStatement();
      case 113 /* TryKeyword */:
      // Include 'catch' and 'finally' for error recovery.
      // falls through
      case 85 /* CatchKeyword */:
      case 98 /* FinallyKeyword */:
        return parseTryStatement();
      case 89 /* DebuggerKeyword */:
        return parseDebuggerStatement();
      case 60 /* AtToken */:
        return parseDeclaration();
      case 134 /* AsyncKeyword */:
      case 120 /* InterfaceKeyword */:
      case 156 /* TypeKeyword */:
      case 144 /* ModuleKeyword */:
      case 145 /* NamespaceKeyword */:
      case 138 /* DeclareKeyword */:
      case 87 /* ConstKeyword */:
      case 94 /* EnumKeyword */:
      case 95 /* ExportKeyword */:
      case 102 /* ImportKeyword */:
      case 123 /* PrivateKeyword */:
      case 124 /* ProtectedKeyword */:
      case 125 /* PublicKeyword */:
      case 128 /* AbstractKeyword */:
      case 129 /* AccessorKeyword */:
      case 126 /* StaticKeyword */:
      case 148 /* ReadonlyKeyword */:
      case 162 /* GlobalKeyword */:
        if (isStartOfDeclaration()) {
          return parseDeclaration();
        }
        break;
    }
    return parseExpressionOrLabeledStatement();
  }
  function isDeclareModifier(modifier) {
    return modifier.kind ***REMOVED******REMOVED******REMOVED*** 138 /* DeclareKeyword */;
  }
  function parseDeclaration() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      true
    );
    const isAmbient ***REMOVED*** some(modifiers, isDeclareModifier);
    if (isAmbient) {
      const node ***REMOVED*** tryReuseAmbientDeclaration(pos);
      if (node) {
        return node;
      }
      for (const m of modifiers) {
        m.flags |***REMOVED*** 33554432 /* Ambient */;
      }
      return doInsideOfContext(33554432 /* Ambient */, () ***REMOVED***> parseDeclarationWorker(pos, hasJSDoc, modifiers));
    } else {
      return parseDeclarationWorker(pos, hasJSDoc, modifiers);
    }
  }
  function tryReuseAmbientDeclaration(pos) {
    return doInsideOfContext(33554432 /* Ambient */, () ***REMOVED***> {
      const node ***REMOVED*** currentNode(parsingContext, pos);
      if (node) {
        return consumeNode(node);
      }
    });
  }
  function parseDeclarationWorker(pos, hasJSDoc, modifiersIn) {
    switch (token()) {
      case 115 /* VarKeyword */:
      case 121 /* LetKeyword */:
      case 87 /* ConstKeyword */:
      case 160 /* UsingKeyword */:
      case 135 /* AwaitKeyword */:
        return parseVariableStatement(pos, hasJSDoc, modifiersIn);
      case 100 /* FunctionKeyword */:
        return parseFunctionDeclaration(pos, hasJSDoc, modifiersIn);
      case 86 /* ClassKeyword */:
        return parseClassDeclaration(pos, hasJSDoc, modifiersIn);
      case 120 /* InterfaceKeyword */:
        return parseInterfaceDeclaration(pos, hasJSDoc, modifiersIn);
      case 156 /* TypeKeyword */:
        return parseTypeAliasDeclaration(pos, hasJSDoc, modifiersIn);
      case 94 /* EnumKeyword */:
        return parseEnumDeclaration(pos, hasJSDoc, modifiersIn);
      case 162 /* GlobalKeyword */:
      case 144 /* ModuleKeyword */:
      case 145 /* NamespaceKeyword */:
        return parseModuleDeclaration(pos, hasJSDoc, modifiersIn);
      case 102 /* ImportKeyword */:
        return parseImportDeclarationOrImportEqualsDeclaration(pos, hasJSDoc, modifiersIn);
      case 95 /* ExportKeyword */:
        nextToken();
        switch (token()) {
          case 90 /* DefaultKeyword */:
          case 64 /* EqualsToken */:
            return parseExportAssignment(pos, hasJSDoc, modifiersIn);
          case 130 /* AsKeyword */:
            return parseNamespaceExportDeclaration(pos, hasJSDoc, modifiersIn);
          default:
            return parseExportDeclaration(pos, hasJSDoc, modifiersIn);
        }
      default:
        if (modifiersIn) {
          const missing ***REMOVED*** createMissingNode(
            283 /* MissingDeclaration */,
            /*reportAtCurrentPosition*/
            true,
            Diagnostics.Declaration_expected
          );
          setTextRangePos(missing, pos);
          missing.modifiers ***REMOVED*** modifiersIn;
          return missing;
        }
        return void 0;
    }
  }
  function nextTokenIsStringLiteral() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
  }
  function nextTokenIsFromKeywordOrEqualsToken() {
    nextToken();
    return token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */;
  }
  function nextTokenIsIdentifierOrStringLiteralOnSameLine() {
    nextToken();
    return !scanner.hasPrecedingLineBreak() && (isIdentifier2() || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */);
  }
  function parseFunctionBlockOrSemicolon(flags, diagnosticMessage) {
    if (token() !***REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      if (flags & 4 /* Type */) {
        parseTypeMemberSemicolon();
        return;
      }
      if (canParseSemicolon()) {
        parseSemicolon();
        return;
      }
    }
    return parseFunctionBlock(flags, diagnosticMessage);
  }
  function parseArrayBindingElement() {
    const pos ***REMOVED*** getNodePos();
    if (token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      return finishNode(factory2.createOmittedExpression(), pos);
    }
    const dotDotDotToken ***REMOVED*** parseOptionalToken(26 /* DotDotDotToken */);
    const name ***REMOVED*** parseIdentifierOrPattern();
    const initializer ***REMOVED*** parseInitializer();
    return finishNode(factory2.createBindingElement(
      dotDotDotToken,
      /*propertyName*/
      void 0,
      name,
      initializer
    ), pos);
  }
  function parseObjectBindingElement() {
    const pos ***REMOVED*** getNodePos();
    const dotDotDotToken ***REMOVED*** parseOptionalToken(26 /* DotDotDotToken */);
    const tokenIsIdentifier ***REMOVED*** isBindingIdentifier();
    let propertyName ***REMOVED*** parsePropertyName();
    let name;
    if (tokenIsIdentifier && token() !***REMOVED******REMOVED*** 59 /* ColonToken */) {
      name ***REMOVED*** propertyName;
      propertyName ***REMOVED*** void 0;
    } else {
      parseExpected(59 /* ColonToken */);
      name ***REMOVED*** parseIdentifierOrPattern();
    }
    const initializer ***REMOVED*** parseInitializer();
    return finishNode(factory2.createBindingElement(dotDotDotToken, propertyName, name, initializer), pos);
  }
  function parseObjectBindingPattern() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(19 /* OpenBraceToken */);
    const elements ***REMOVED*** allowInAnd(() ***REMOVED***> parseDelimitedList(9 /* ObjectBindingElements */, parseObjectBindingElement));
    parseExpected(20 /* CloseBraceToken */);
    return finishNode(factory2.createObjectBindingPattern(elements), pos);
  }
  function parseArrayBindingPattern() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(23 /* OpenBracketToken */);
    const elements ***REMOVED*** allowInAnd(() ***REMOVED***> parseDelimitedList(10 /* ArrayBindingElements */, parseArrayBindingElement));
    parseExpected(24 /* CloseBracketToken */);
    return finishNode(factory2.createArrayBindingPattern(elements), pos);
  }
  function isBindingIdentifierOrPrivateIdentifierOrPattern() {
    return token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */ || isBindingIdentifier();
  }
  function parseIdentifierOrPattern(privateIdentifierDiagnosticMessage) {
    if (token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      return parseArrayBindingPattern();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      return parseObjectBindingPattern();
    }
    return parseBindingIdentifier(privateIdentifierDiagnosticMessage);
  }
  function parseVariableDeclarationAllowExclamation() {
    return parseVariableDeclaration(
      /*allowExclamation*/
      true
    );
  }
  function parseVariableDeclaration(allowExclamation) {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const name ***REMOVED*** parseIdentifierOrPattern(Diagnostics.Private_identifiers_are_not_allowed_in_variable_declarations);
    let exclamationToken;
    if (allowExclamation && name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && token() ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */ && !scanner.hasPrecedingLineBreak()) {
      exclamationToken ***REMOVED*** parseTokenNode();
    }
    const type ***REMOVED*** parseTypeAnnotation();
    const initializer ***REMOVED*** isInOrOfKeyword(token()) ? void 0 : parseInitializer();
    const node ***REMOVED*** factoryCreateVariableDeclaration(name, exclamationToken, type, initializer);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseVariableDeclarationList(inForStatementInitializer) {
    const pos ***REMOVED*** getNodePos();
    let flags ***REMOVED*** 0;
    switch (token()) {
      case 115 /* VarKeyword */:
        break;
      case 121 /* LetKeyword */:
        flags |***REMOVED*** 1 /* Let */;
        break;
      case 87 /* ConstKeyword */:
        flags |***REMOVED*** 2 /* Const */;
        break;
      case 160 /* UsingKeyword */:
        flags |***REMOVED*** 4 /* Using */;
        break;
      case 135 /* AwaitKeyword */:
        Debug.assert(isAwaitUsingDeclaration());
        flags |***REMOVED*** 6 /* AwaitUsing */;
        nextToken();
        break;
      default:
        Debug.fail();
    }
    nextToken();
    let declarations;
    if (token() ***REMOVED******REMOVED******REMOVED*** 165 /* OfKeyword */ && lookAhead(canFollowContextualOfKeyword)) {
      declarations ***REMOVED*** createMissingList();
    } else {
      const savedDisallowIn ***REMOVED*** inDisallowInContext();
      setDisallowInContext(inForStatementInitializer);
      declarations ***REMOVED*** parseDelimitedList(
        8 /* VariableDeclarations */,
        inForStatementInitializer ? parseVariableDeclaration : parseVariableDeclarationAllowExclamation
      );
      setDisallowInContext(savedDisallowIn);
    }
    return finishNode(factoryCreateVariableDeclarationList(declarations, flags), pos);
  }
  function canFollowContextualOfKeyword() {
    return nextTokenIsIdentifier() && nextToken() ***REMOVED******REMOVED******REMOVED*** 22 /* CloseParenToken */;
  }
  function parseVariableStatement(pos, hasJSDoc, modifiers) {
    const declarationList ***REMOVED*** parseVariableDeclarationList(
      /*inForStatementInitializer*/
      false
    );
    parseSemicolon();
    const node ***REMOVED*** factoryCreateVariableStatement(modifiers, declarationList);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseFunctionDeclaration(pos, hasJSDoc, modifiers) {
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    const modifierFlags ***REMOVED*** modifiersToFlags(modifiers);
    parseExpected(100 /* FunctionKeyword */);
    const asteriskToken ***REMOVED*** parseOptionalToken(42 /* AsteriskToken */);
    const name ***REMOVED*** modifierFlags & 2048 /* Default */ ? parseOptionalBindingIdentifier() : parseBindingIdentifier();
    const isGenerator ***REMOVED*** asteriskToken ? 1 /* Yield */ : 0 /* None */;
    const isAsync ***REMOVED*** modifierFlags & 1024 /* Async */ ? 2 /* Await */ : 0 /* None */;
    const typeParameters ***REMOVED*** parseTypeParameters();
    if (modifierFlags & 32 /* Export */) setAwaitContext(
      /*value*/
      true
    );
    const parameters ***REMOVED*** parseParameters(isGenerator | isAsync);
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      false
    );
    const body ***REMOVED*** parseFunctionBlockOrSemicolon(isGenerator | isAsync, Diagnostics.or_expected);
    setAwaitContext(savedAwaitContext);
    const node ***REMOVED*** factory2.createFunctionDeclaration(modifiers, asteriskToken, name, typeParameters, parameters, type, body);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseConstructorName() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 137 /* ConstructorKeyword */) {
      return parseExpected(137 /* ConstructorKeyword */);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ && lookAhead(nextToken) ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */) {
      return tryParse(() ***REMOVED***> {
        const literalNode ***REMOVED*** parseLiteralNode();
        return literalNode.text ***REMOVED******REMOVED******REMOVED*** "constructor" ? literalNode : void 0;
      });
    }
  }
  function tryParseConstructorDeclaration(pos, hasJSDoc, modifiers) {
    return tryParse(() ***REMOVED***> {
      if (parseConstructorName()) {
        const typeParameters ***REMOVED*** parseTypeParameters();
        const parameters ***REMOVED*** parseParameters(0 /* None */);
        const type ***REMOVED*** parseReturnType(
          59 /* ColonToken */,
          /*isType*/
          false
        );
        const body ***REMOVED*** parseFunctionBlockOrSemicolon(0 /* None */, Diagnostics.or_expected);
        const node ***REMOVED*** factory2.createConstructorDeclaration(modifiers, parameters, body);
        node.typeParameters ***REMOVED*** typeParameters;
        node.type ***REMOVED*** type;
        return withJSDoc(finishNode(node, pos), hasJSDoc);
      }
    });
  }
  function parseMethodDeclaration(pos, hasJSDoc, modifiers, asteriskToken, name, questionToken, exclamationToken, diagnosticMessage) {
    const isGenerator ***REMOVED*** asteriskToken ? 1 /* Yield */ : 0 /* None */;
    const isAsync ***REMOVED*** some(modifiers, isAsyncModifier) ? 2 /* Await */ : 0 /* None */;
    const typeParameters ***REMOVED*** parseTypeParameters();
    const parameters ***REMOVED*** parseParameters(isGenerator | isAsync);
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      false
    );
    const body ***REMOVED*** parseFunctionBlockOrSemicolon(isGenerator | isAsync, diagnosticMessage);
    const node ***REMOVED*** factory2.createMethodDeclaration(
      modifiers,
      asteriskToken,
      name,
      questionToken,
      typeParameters,
      parameters,
      type,
      body
    );
    node.exclamationToken ***REMOVED*** exclamationToken;
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parsePropertyDeclaration(pos, hasJSDoc, modifiers, name, questionToken) {
    const exclamationToken ***REMOVED*** !questionToken && !scanner.hasPrecedingLineBreak() ? parseOptionalToken(54 /* ExclamationToken */) : void 0;
    const type ***REMOVED*** parseTypeAnnotation();
    const initializer ***REMOVED*** doOutsideOfContext(16384 /* YieldContext */ | 65536 /* AwaitContext */ | 8192 /* DisallowInContext */, parseInitializer);
    parseSemicolonAfterPropertyName(name, type, initializer);
    const node ***REMOVED*** factory2.createPropertyDeclaration(
      modifiers,
      name,
      questionToken || exclamationToken,
      type,
      initializer
    );
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parsePropertyOrMethodDeclaration(pos, hasJSDoc, modifiers) {
    const asteriskToken ***REMOVED*** parseOptionalToken(42 /* AsteriskToken */);
    const name ***REMOVED*** parsePropertyName();
    const questionToken ***REMOVED*** parseOptionalToken(58 /* QuestionToken */);
    if (asteriskToken || token() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */ || token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
      return parseMethodDeclaration(
        pos,
        hasJSDoc,
        modifiers,
        asteriskToken,
        name,
        questionToken,
        /*exclamationToken*/
        void 0,
        Diagnostics.or_expected
      );
    }
    return parsePropertyDeclaration(pos, hasJSDoc, modifiers, name, questionToken);
  }
  function parseAccessorDeclaration(pos, hasJSDoc, modifiers, kind, flags) {
    const name ***REMOVED*** parsePropertyName();
    const typeParameters ***REMOVED*** parseTypeParameters();
    const parameters ***REMOVED*** parseParameters(0 /* None */);
    const type ***REMOVED*** parseReturnType(
      59 /* ColonToken */,
      /*isType*/
      false
    );
    const body ***REMOVED*** parseFunctionBlockOrSemicolon(flags);
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? factory2.createGetAccessorDeclaration(modifiers, name, parameters, type, body) : factory2.createSetAccessorDeclaration(modifiers, name, parameters, body);
    node.typeParameters ***REMOVED*** typeParameters;
    if (isSetAccessorDeclaration(node)) node.type ***REMOVED*** type;
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function isClassMemberStart() {
    let idToken;
    if (token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */) {
      return true;
    }
    while (isModifierKind(token())) {
      idToken ***REMOVED*** token();
      if (isClassMemberModifier(idToken)) {
        return true;
      }
      nextToken();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */) {
      return true;
    }
    if (isLiteralPropertyName()) {
      idToken ***REMOVED*** token();
      nextToken();
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      return true;
    }
    if (idToken !***REMOVED******REMOVED*** void 0) {
      if (!isKeyword(idToken) || idToken ***REMOVED******REMOVED******REMOVED*** 153 /* SetKeyword */ || idToken ***REMOVED******REMOVED******REMOVED*** 139 /* GetKeyword */) {
        return true;
      }
      switch (token()) {
        case 21 /* OpenParenToken */:
        // Method declaration
        case 30 /* LessThanToken */:
        // Generic Method declaration
        case 54 /* ExclamationToken */:
        // Non-null assertion on property name
        case 59 /* ColonToken */:
        // Type Annotation for declaration
        case 64 /* EqualsToken */:
        // Initializer for declaration
        case 58 /* QuestionToken */:
          return true;
        default:
          return canParseSemicolon();
      }
    }
    return false;
  }
  function parseClassStaticBlockDeclaration(pos, hasJSDoc, modifiers) {
    parseExpectedToken(126 /* StaticKeyword */);
    const body ***REMOVED*** parseClassStaticBlockBody();
    const node ***REMOVED*** withJSDoc(finishNode(factory2.createClassStaticBlockDeclaration(body), pos), hasJSDoc);
    node.modifiers ***REMOVED*** modifiers;
    return node;
  }
  function parseClassStaticBlockBody() {
    const savedYieldContext ***REMOVED*** inYieldContext();
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    setYieldContext(false);
    setAwaitContext(true);
    const body ***REMOVED*** parseBlock(
      /*ignoreMissingOpenBrace*/
      false
    );
    setYieldContext(savedYieldContext);
    setAwaitContext(savedAwaitContext);
    return body;
  }
  function parseDecoratorExpression() {
    if (inAwaitContext() && token() ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */) {
      const pos ***REMOVED*** getNodePos();
      const awaitExpression ***REMOVED*** parseIdentifier(Diagnostics.Expression_expected);
      nextToken();
      const memberExpression ***REMOVED*** parseMemberExpressionRest(
        pos,
        awaitExpression,
        /*allowOptionalChain*/
        true
      );
      return parseCallExpressionRest(pos, memberExpression);
    }
    return parseLeftHandSideExpressionOrHigher();
  }
  function tryParseDecorator() {
    const pos ***REMOVED*** getNodePos();
    if (!parseOptional(60 /* AtToken */)) {
      return void 0;
    }
    const expression ***REMOVED*** doInDecoratorContext(parseDecoratorExpression);
    return finishNode(factory2.createDecorator(expression), pos);
  }
  function tryParseModifier(hasSeenStaticModifier, permitConstAsModifier, stopOnStartOfClassStaticBlock) {
    const pos ***REMOVED*** getNodePos();
    const kind ***REMOVED*** token();
    if (token() ***REMOVED******REMOVED******REMOVED*** 87 /* ConstKeyword */ && permitConstAsModifier) {
      if (!tryParse(nextTokenIsOnSameLineAndCanFollowModifier)) {
        return void 0;
      }
    } else if (stopOnStartOfClassStaticBlock && token() ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */ && lookAhead(nextTokenIsOpenBrace)) {
      return void 0;
    } else if (hasSeenStaticModifier && token() ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */) {
      return void 0;
    } else {
      if (!parseAnyContextualModifier()) {
        return void 0;
      }
    }
    return finishNode(factoryCreateToken(kind), pos);
  }
  function parseModifiers(allowDecorators, permitConstAsModifier, stopOnStartOfClassStaticBlock) {
    const pos ***REMOVED*** getNodePos();
    let list;
    let decorator, modifier, hasSeenStaticModifier ***REMOVED*** false, hasLeadingModifier ***REMOVED*** false, hasTrailingDecorator ***REMOVED*** false;
    if (allowDecorators && token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */) {
      while (decorator ***REMOVED*** tryParseDecorator()) {
        list ***REMOVED*** append(list, decorator);
      }
    }
    while (modifier ***REMOVED*** tryParseModifier(hasSeenStaticModifier, permitConstAsModifier, stopOnStartOfClassStaticBlock)) {
      if (modifier.kind ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */) hasSeenStaticModifier ***REMOVED*** true;
      list ***REMOVED*** append(list, modifier);
      hasLeadingModifier ***REMOVED*** true;
    }
    if (hasLeadingModifier && allowDecorators && token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */) {
      while (decorator ***REMOVED*** tryParseDecorator()) {
        list ***REMOVED*** append(list, decorator);
        hasTrailingDecorator ***REMOVED*** true;
      }
    }
    if (hasTrailingDecorator) {
      while (modifier ***REMOVED*** tryParseModifier(hasSeenStaticModifier, permitConstAsModifier, stopOnStartOfClassStaticBlock)) {
        if (modifier.kind ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */) hasSeenStaticModifier ***REMOVED*** true;
        list ***REMOVED*** append(list, modifier);
      }
    }
    return list && createNodeArray(list, pos);
  }
  function parseModifiersForArrowFunction() {
    let modifiers;
    if (token() ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      const pos ***REMOVED*** getNodePos();
      nextToken();
      const modifier ***REMOVED*** finishNode(factoryCreateToken(134 /* AsyncKeyword */), pos);
      modifiers ***REMOVED*** createNodeArray([modifier], pos);
    }
    return modifiers;
  }
  function parseClassElement() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    if (token() ***REMOVED******REMOVED******REMOVED*** 27 /* SemicolonToken */) {
      nextToken();
      return withJSDoc(finishNode(factory2.createSemicolonClassElement(), pos), hasJSDoc);
    }
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      true,
      /*permitConstAsModifier*/
      true,
      /*stopOnStartOfClassStaticBlock*/
      true
    );
    if (token() ***REMOVED******REMOVED******REMOVED*** 126 /* StaticKeyword */ && lookAhead(nextTokenIsOpenBrace)) {
      return parseClassStaticBlockDeclaration(pos, hasJSDoc, modifiers);
    }
    if (parseContextualModifier(139 /* GetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 178 /* GetAccessor */, 0 /* None */);
    }
    if (parseContextualModifier(153 /* SetKeyword */)) {
      return parseAccessorDeclaration(pos, hasJSDoc, modifiers, 179 /* SetAccessor */, 0 /* None */);
    }
    if (token() ***REMOVED******REMOVED******REMOVED*** 137 /* ConstructorKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      const constructorDeclaration ***REMOVED*** tryParseConstructorDeclaration(pos, hasJSDoc, modifiers);
      if (constructorDeclaration) {
        return constructorDeclaration;
      }
    }
    if (isIndexSignature()) {
      return parseIndexSignatureDeclaration(pos, hasJSDoc, modifiers);
    }
    if (tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ || token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */) {
      const isAmbient ***REMOVED*** some(modifiers, isDeclareModifier);
      if (isAmbient) {
        for (const m of modifiers) {
          m.flags |***REMOVED*** 33554432 /* Ambient */;
        }
        return doInsideOfContext(33554432 /* Ambient */, () ***REMOVED***> parsePropertyOrMethodDeclaration(pos, hasJSDoc, modifiers));
      } else {
        return parsePropertyOrMethodDeclaration(pos, hasJSDoc, modifiers);
      }
    }
    if (modifiers) {
      const name ***REMOVED*** createMissingNode(
        80 /* Identifier */,
        /*reportAtCurrentPosition*/
        true,
        Diagnostics.Declaration_expected
      );
      return parsePropertyDeclaration(
        pos,
        hasJSDoc,
        modifiers,
        name,
        /*questionToken*/
        void 0
      );
    }
    return Debug.fail("Should not have attempted to parse class member declaration.");
  }
  function parseDecoratedExpression() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const modifiers ***REMOVED*** parseModifiers(
      /*allowDecorators*/
      true
    );
    if (token() ***REMOVED******REMOVED******REMOVED*** 86 /* ClassKeyword */) {
      return parseClassDeclarationOrExpression(pos, hasJSDoc, modifiers, 232 /* ClassExpression */);
    }
    const missing ***REMOVED*** createMissingNode(
      283 /* MissingDeclaration */,
      /*reportAtCurrentPosition*/
      true,
      Diagnostics.Expression_expected
    );
    setTextRangePos(missing, pos);
    missing.modifiers ***REMOVED*** modifiers;
    return missing;
  }
  function parseClassExpression() {
    return parseClassDeclarationOrExpression(
      getNodePos(),
      hasPrecedingJSDocComment(),
      /*modifiers*/
      void 0,
      232 /* ClassExpression */
    );
  }
  function parseClassDeclaration(pos, hasJSDoc, modifiers) {
    return parseClassDeclarationOrExpression(pos, hasJSDoc, modifiers, 264 /* ClassDeclaration */);
  }
  function parseClassDeclarationOrExpression(pos, hasJSDoc, modifiers, kind) {
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    parseExpected(86 /* ClassKeyword */);
    const name ***REMOVED*** parseNameOfClassDeclarationOrExpression();
    const typeParameters ***REMOVED*** parseTypeParameters();
    if (some(modifiers, isExportModifier)) setAwaitContext(
      /*value*/
      true
    );
    const heritageClauses ***REMOVED*** parseHeritageClauses();
    let members;
    if (parseExpected(19 /* OpenBraceToken */)) {
      members ***REMOVED*** parseClassMembers();
      parseExpected(20 /* CloseBraceToken */);
    } else {
      members ***REMOVED*** createMissingList();
    }
    setAwaitContext(savedAwaitContext);
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ ? factory2.createClassDeclaration(modifiers, name, typeParameters, heritageClauses, members) : factory2.createClassExpression(modifiers, name, typeParameters, heritageClauses, members);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseNameOfClassDeclarationOrExpression() {
    return isBindingIdentifier() && !isImplementsClause() ? createIdentifier(isBindingIdentifier()) : void 0;
  }
  function isImplementsClause() {
    return token() ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */ && lookAhead(nextTokenIsIdentifierOrKeyword);
  }
  function parseHeritageClauses() {
    if (isHeritageClause2()) {
      return parseList(22 /* HeritageClauses */, parseHeritageClause);
    }
    return void 0;
  }
  function parseHeritageClause() {
    const pos ***REMOVED*** getNodePos();
    const tok ***REMOVED*** token();
    Debug.assert(tok ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ || tok ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */);
    nextToken();
    const types ***REMOVED*** parseDelimitedList(7 /* HeritageClauseElement */, parseExpressionWithTypeArguments);
    return finishNode(factory2.createHeritageClause(tok, types), pos);
  }
  function parseExpressionWithTypeArguments() {
    const pos ***REMOVED*** getNodePos();
    const expression ***REMOVED*** parseLeftHandSideExpressionOrHigher();
    if (expression.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */) {
      return expression;
    }
    const typeArguments ***REMOVED*** tryParseTypeArguments();
    return finishNode(factory2.createExpressionWithTypeArguments(expression, typeArguments), pos);
  }
  function tryParseTypeArguments() {
    return token() ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */ ? parseBracketedList(20 /* TypeArguments */, parseType, 30 /* LessThanToken */, 32 /* GreaterThanToken */) : void 0;
  }
  function isHeritageClause2() {
    return token() ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */;
  }
  function parseClassMembers() {
    return parseList(5 /* ClassMembers */, parseClassElement);
  }
  function parseInterfaceDeclaration(pos, hasJSDoc, modifiers) {
    parseExpected(120 /* InterfaceKeyword */);
    const name ***REMOVED*** parseIdentifier();
    const typeParameters ***REMOVED*** parseTypeParameters();
    const heritageClauses ***REMOVED*** parseHeritageClauses();
    const members ***REMOVED*** parseObjectTypeMembers();
    const node ***REMOVED*** factory2.createInterfaceDeclaration(modifiers, name, typeParameters, heritageClauses, members);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseTypeAliasDeclaration(pos, hasJSDoc, modifiers) {
    parseExpected(156 /* TypeKeyword */);
    if (scanner.hasPrecedingLineBreak()) {
      parseErrorAtCurrentToken(Diagnostics.Line_break_not_permitted_here);
    }
    const name ***REMOVED*** parseIdentifier();
    const typeParameters ***REMOVED*** parseTypeParameters();
    parseExpected(64 /* EqualsToken */);
    const type ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 141 /* IntrinsicKeyword */ && tryParse(parseKeywordAndNoDot) || parseType();
    parseSemicolon();
    const node ***REMOVED*** factory2.createTypeAliasDeclaration(modifiers, name, typeParameters, type);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseEnumMember() {
    const pos ***REMOVED*** getNodePos();
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    const name ***REMOVED*** parsePropertyName();
    const initializer ***REMOVED*** allowInAnd(parseInitializer);
    return withJSDoc(finishNode(factory2.createEnumMember(name, initializer), pos), hasJSDoc);
  }
  function parseEnumDeclaration(pos, hasJSDoc, modifiers) {
    parseExpected(94 /* EnumKeyword */);
    const name ***REMOVED*** parseIdentifier();
    let members;
    if (parseExpected(19 /* OpenBraceToken */)) {
      members ***REMOVED*** doOutsideOfYieldAndAwaitContext(() ***REMOVED***> parseDelimitedList(6 /* EnumMembers */, parseEnumMember));
      parseExpected(20 /* CloseBraceToken */);
    } else {
      members ***REMOVED*** createMissingList();
    }
    const node ***REMOVED*** factory2.createEnumDeclaration(modifiers, name, members);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseModuleBlock() {
    const pos ***REMOVED*** getNodePos();
    let statements;
    if (parseExpected(19 /* OpenBraceToken */)) {
      statements ***REMOVED*** parseList(1 /* BlockStatements */, parseStatement);
      parseExpected(20 /* CloseBraceToken */);
    } else {
      statements ***REMOVED*** createMissingList();
    }
    return finishNode(factory2.createModuleBlock(statements), pos);
  }
  function parseModuleOrNamespaceDeclaration(pos, hasJSDoc, modifiers, flags) {
    const namespaceFlag ***REMOVED*** flags & 32 /* Namespace */;
    const name ***REMOVED*** flags & 8 /* NestedNamespace */ ? parseIdentifierName() : parseIdentifier();
    const body ***REMOVED*** parseOptional(25 /* DotToken */) ? parseModuleOrNamespaceDeclaration(
      getNodePos(),
      /*hasJSDoc*/
      false,
      /*modifiers*/
      void 0,
      8 /* NestedNamespace */ | namespaceFlag
    ) : parseModuleBlock();
    const node ***REMOVED*** factory2.createModuleDeclaration(modifiers, name, body, flags);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseAmbientExternalModuleDeclaration(pos, hasJSDoc, modifiersIn) {
    let flags ***REMOVED*** 0;
    let name;
    if (token() ***REMOVED******REMOVED******REMOVED*** 162 /* GlobalKeyword */) {
      name ***REMOVED*** parseIdentifier();
      flags |***REMOVED*** 2048 /* GlobalAugmentation */;
    } else {
      name ***REMOVED*** parseLiteralNode();
      name.text ***REMOVED*** internIdentifier(name.text);
    }
    let body;
    if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      body ***REMOVED*** parseModuleBlock();
    } else {
      parseSemicolon();
    }
    const node ***REMOVED*** factory2.createModuleDeclaration(modifiersIn, name, body, flags);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseModuleDeclaration(pos, hasJSDoc, modifiersIn) {
    let flags ***REMOVED*** 0;
    if (token() ***REMOVED******REMOVED******REMOVED*** 162 /* GlobalKeyword */) {
      return parseAmbientExternalModuleDeclaration(pos, hasJSDoc, modifiersIn);
    } else if (parseOptional(145 /* NamespaceKeyword */)) {
      flags |***REMOVED*** 32 /* Namespace */;
    } else {
      parseExpected(144 /* ModuleKeyword */);
      if (token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
        return parseAmbientExternalModuleDeclaration(pos, hasJSDoc, modifiersIn);
      }
    }
    return parseModuleOrNamespaceDeclaration(pos, hasJSDoc, modifiersIn, flags);
  }
  function isExternalModuleReference2() {
    return token() ***REMOVED******REMOVED******REMOVED*** 149 /* RequireKeyword */ && lookAhead(nextTokenIsOpenParen);
  }
  function nextTokenIsOpenParen() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 21 /* OpenParenToken */;
  }
  function nextTokenIsOpenBrace() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */;
  }
  function nextTokenIsSlash() {
    return nextToken() ***REMOVED******REMOVED******REMOVED*** 44 /* SlashToken */;
  }
  function parseNamespaceExportDeclaration(pos, hasJSDoc, modifiers) {
    parseExpected(130 /* AsKeyword */);
    parseExpected(145 /* NamespaceKeyword */);
    const name ***REMOVED*** parseIdentifier();
    parseSemicolon();
    const node ***REMOVED*** factory2.createNamespaceExportDeclaration(name);
    node.modifiers ***REMOVED*** modifiers;
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseImportDeclarationOrImportEqualsDeclaration(pos, hasJSDoc, modifiers) {
    parseExpected(102 /* ImportKeyword */);
    const afterImportPos ***REMOVED*** scanner.getTokenFullStart();
    let identifier;
    if (isIdentifier2()) {
      identifier ***REMOVED*** parseIdentifier();
    }
    let phaseModifier;
    if ((identifier ***REMOVED******REMOVED*** null ? void 0 : identifier.escapedText) ***REMOVED******REMOVED******REMOVED*** "type" && (token() !***REMOVED******REMOVED*** 161 /* FromKeyword */ || isIdentifier2() && lookAhead(nextTokenIsFromKeywordOrEqualsToken)) && (isIdentifier2() || tokenAfterImportDefinitelyProducesImportDeclaration())) {
      phaseModifier ***REMOVED*** 156 /* TypeKeyword */;
      identifier ***REMOVED*** isIdentifier2() ? parseIdentifier() : void 0;
    } else if ((identifier ***REMOVED******REMOVED*** null ? void 0 : identifier.escapedText) ***REMOVED******REMOVED******REMOVED*** "defer" && (token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */ ? !lookAhead(nextTokenIsStringLiteral) : token() !***REMOVED******REMOVED*** 28 /* CommaToken */ && token() !***REMOVED******REMOVED*** 64 /* EqualsToken */)) {
      phaseModifier ***REMOVED*** 166 /* DeferKeyword */;
      identifier ***REMOVED*** isIdentifier2() ? parseIdentifier() : void 0;
    }
    if (identifier && !tokenAfterImportedIdentifierDefinitelyProducesImportDeclaration() && phaseModifier !***REMOVED******REMOVED*** 166 /* DeferKeyword */) {
      return parseImportEqualsDeclaration(pos, hasJSDoc, modifiers, identifier, phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */);
    }
    const importClause ***REMOVED*** tryParseImportClause(
      identifier,
      afterImportPos,
      phaseModifier,
      /*skipJsDocLeadingAsterisks*/
      void 0
    );
    const moduleSpecifier ***REMOVED*** parseModuleSpecifier();
    const attributes ***REMOVED*** tryParseImportAttributes();
    parseSemicolon();
    const node ***REMOVED*** factory2.createImportDeclaration(modifiers, importClause, moduleSpecifier, attributes);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function tryParseImportClause(identifier, pos, phaseModifier, skipJsDocLeadingAsterisks ***REMOVED*** false) {
    let importClause;
    if (identifier || // import id
    token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || // import *
    token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */) {
      importClause ***REMOVED*** parseImportClause(identifier, pos, phaseModifier, skipJsDocLeadingAsterisks);
      parseExpected(161 /* FromKeyword */);
    }
    return importClause;
  }
  function tryParseImportAttributes() {
    const currentToken2 ***REMOVED*** token();
    if ((currentToken2 ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 132 /* AssertKeyword */) && !scanner.hasPrecedingLineBreak()) {
      return parseImportAttributes(currentToken2);
    }
  }
  function parseImportAttribute() {
    const pos ***REMOVED*** getNodePos();
    const name ***REMOVED*** tokenIsIdentifierOrKeyword(token()) ? parseIdentifierName() : parseLiteralLikeNode(11 /* StringLiteral */);
    parseExpected(59 /* ColonToken */);
    const value ***REMOVED*** parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    );
    return finishNode(factory2.createImportAttribute(name, value), pos);
  }
  function parseImportAttributes(token2, skipKeyword) {
    const pos ***REMOVED*** getNodePos();
    if (!skipKeyword) {
      parseExpected(token2);
    }
    const openBracePosition ***REMOVED*** scanner.getTokenStart();
    if (parseExpected(19 /* OpenBraceToken */)) {
      const multiLine ***REMOVED*** scanner.hasPrecedingLineBreak();
      const elements ***REMOVED*** parseDelimitedList(
        24 /* ImportAttributes */,
        parseImportAttribute,
        /*considerSemicolonAsDelimiter*/
        true
      );
      if (!parseExpected(20 /* CloseBraceToken */)) {
        const lastError ***REMOVED*** lastOrUndefined(parseDiagnostics);
        if (lastError && lastError.code ***REMOVED******REMOVED******REMOVED*** Diagnostics._0_expected.code) {
          addRelatedInfo(
            lastError,
            createDetachedDiagnostic(fileName, sourceText, openBracePosition, 1, Diagnostics.The_parser_expected_to_find_a_1_to_match_the_0_token_here, "{", "}")
          );
        }
      }
      return finishNode(factory2.createImportAttributes(elements, multiLine, token2), pos);
    } else {
      const elements ***REMOVED*** createNodeArray(
        [],
        getNodePos(),
        /*end*/
        void 0,
        /*hasTrailingComma*/
        false
      );
      return finishNode(factory2.createImportAttributes(
        elements,
        /*multiLine*/
        false,
        token2
      ), pos);
    }
  }
  function tokenAfterImportDefinitelyProducesImportDeclaration() {
    return token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */;
  }
  function tokenAfterImportedIdentifierDefinitelyProducesImportDeclaration() {
    return token() ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ || token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */;
  }
  function parseImportEqualsDeclaration(pos, hasJSDoc, modifiers, identifier, isTypeOnly) {
    parseExpected(64 /* EqualsToken */);
    const moduleReference ***REMOVED*** parseModuleReference();
    parseSemicolon();
    const node ***REMOVED*** factory2.createImportEqualsDeclaration(modifiers, isTypeOnly, identifier, moduleReference);
    const finished ***REMOVED*** withJSDoc(finishNode(node, pos), hasJSDoc);
    return finished;
  }
  function parseImportClause(identifier, pos, phaseModifier, skipJsDocLeadingAsterisks) {
    let namedBindings;
    if (!identifier || parseOptional(28 /* CommaToken */)) {
      if (skipJsDocLeadingAsterisks) scanner.setSkipJsDocLeadingAsterisks(true);
      if (token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */) {
        namedBindings ***REMOVED*** parseNamespaceImport();
      } else {
        namedBindings ***REMOVED*** parseNamedImportsOrExports(276 /* NamedImports */);
      }
      if (skipJsDocLeadingAsterisks) scanner.setSkipJsDocLeadingAsterisks(false);
    }
    return finishNode(factory2.createImportClause(phaseModifier, identifier, namedBindings), pos);
  }
  function parseModuleReference() {
    return isExternalModuleReference2() ? parseExternalModuleReference() : parseEntityName(
      /*allowReservedWords*/
      false
    );
  }
  function parseExternalModuleReference() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(149 /* RequireKeyword */);
    parseExpected(21 /* OpenParenToken */);
    const expression ***REMOVED*** parseModuleSpecifier();
    parseExpected(22 /* CloseParenToken */);
    return finishNode(factory2.createExternalModuleReference(expression), pos);
  }
  function parseModuleSpecifier() {
    if (token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      const result ***REMOVED*** parseLiteralNode();
      result.text ***REMOVED*** internIdentifier(result.text);
      return result;
    } else {
      return parseExpression();
    }
  }
  function parseNamespaceImport() {
    const pos ***REMOVED*** getNodePos();
    parseExpected(42 /* AsteriskToken */);
    parseExpected(130 /* AsKeyword */);
    const name ***REMOVED*** parseIdentifier();
    return finishNode(factory2.createNamespaceImport(name), pos);
  }
  function canParseModuleExportName() {
    return tokenIsIdentifierOrKeyword(token()) || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
  }
  function parseModuleExportName(parseName) {
    return token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? parseLiteralNode() : parseName();
  }
  function parseNamedImportsOrExports(kind) {
    const pos ***REMOVED*** getNodePos();
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 276 /* NamedImports */ ? factory2.createNamedImports(parseBracketedList(23 /* ImportOrExportSpecifiers */, parseImportSpecifier, 19 /* OpenBraceToken */, 20 /* CloseBraceToken */)) : factory2.createNamedExports(parseBracketedList(23 /* ImportOrExportSpecifiers */, parseExportSpecifier, 19 /* OpenBraceToken */, 20 /* CloseBraceToken */));
    return finishNode(node, pos);
  }
  function parseExportSpecifier() {
    const hasJSDoc ***REMOVED*** hasPrecedingJSDocComment();
    return withJSDoc(parseImportOrExportSpecifier(282 /* ExportSpecifier */), hasJSDoc);
  }
  function parseImportSpecifier() {
    return parseImportOrExportSpecifier(277 /* ImportSpecifier */);
  }
  function parseImportOrExportSpecifier(kind) {
    const pos ***REMOVED*** getNodePos();
    let checkIdentifierIsKeyword ***REMOVED*** isKeyword(token()) && !isIdentifier2();
    let checkIdentifierStart ***REMOVED*** scanner.getTokenStart();
    let checkIdentifierEnd ***REMOVED*** scanner.getTokenEnd();
    let isTypeOnly ***REMOVED*** false;
    let propertyName;
    let canParseAsKeyword ***REMOVED*** true;
    let name ***REMOVED*** parseModuleExportName(parseIdentifierName);
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && name.escapedText ***REMOVED******REMOVED******REMOVED*** "type") {
      if (token() ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */) {
        const firstAs ***REMOVED*** parseIdentifierName();
        if (token() ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */) {
          const secondAs ***REMOVED*** parseIdentifierName();
          if (canParseModuleExportName()) {
            isTypeOnly ***REMOVED*** true;
            propertyName ***REMOVED*** firstAs;
            name ***REMOVED*** parseModuleExportName(parseNameWithKeywordCheck);
            canParseAsKeyword ***REMOVED*** false;
          } else {
            propertyName ***REMOVED*** name;
            name ***REMOVED*** secondAs;
            canParseAsKeyword ***REMOVED*** false;
          }
        } else if (canParseModuleExportName()) {
          propertyName ***REMOVED*** name;
          canParseAsKeyword ***REMOVED*** false;
          name ***REMOVED*** parseModuleExportName(parseNameWithKeywordCheck);
        } else {
          isTypeOnly ***REMOVED*** true;
          name ***REMOVED*** firstAs;
        }
      } else if (canParseModuleExportName()) {
        isTypeOnly ***REMOVED*** true;
        name ***REMOVED*** parseModuleExportName(parseNameWithKeywordCheck);
      }
    }
    if (canParseAsKeyword && token() ***REMOVED******REMOVED******REMOVED*** 130 /* AsKeyword */) {
      propertyName ***REMOVED*** name;
      parseExpected(130 /* AsKeyword */);
      name ***REMOVED*** parseModuleExportName(parseNameWithKeywordCheck);
    }
    if (kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */) {
      if (name.kind !***REMOVED******REMOVED*** 80 /* Identifier */) {
        parseErrorAt(skipTrivia(sourceText, name.pos), name.end, Diagnostics.Identifier_expected);
        name ***REMOVED*** setTextRangePosEnd(createMissingNode(
          80 /* Identifier */,
          /*reportAtCurrentPosition*/
          false
        ), name.pos, name.pos);
      } else if (checkIdentifierIsKeyword) {
        parseErrorAt(checkIdentifierStart, checkIdentifierEnd, Diagnostics.Identifier_expected);
      }
    }
    const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ ? factory2.createImportSpecifier(isTypeOnly, propertyName, name) : factory2.createExportSpecifier(isTypeOnly, propertyName, name);
    return finishNode(node, pos);
    function parseNameWithKeywordCheck() {
      checkIdentifierIsKeyword ***REMOVED*** isKeyword(token()) && !isIdentifier2();
      checkIdentifierStart ***REMOVED*** scanner.getTokenStart();
      checkIdentifierEnd ***REMOVED*** scanner.getTokenEnd();
      return parseIdentifierName();
    }
  }
  function parseNamespaceExport(pos) {
    return finishNode(factory2.createNamespaceExport(parseModuleExportName(parseIdentifierName)), pos);
  }
  function parseExportDeclaration(pos, hasJSDoc, modifiers) {
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    setAwaitContext(
      /*value*/
      true
    );
    let exportClause;
    let moduleSpecifier;
    let attributes;
    const isTypeOnly ***REMOVED*** parseOptional(156 /* TypeKeyword */);
    const namespaceExportPos ***REMOVED*** getNodePos();
    if (parseOptional(42 /* AsteriskToken */)) {
      if (parseOptional(130 /* AsKeyword */)) {
        exportClause ***REMOVED*** parseNamespaceExport(namespaceExportPos);
      }
      parseExpected(161 /* FromKeyword */);
      moduleSpecifier ***REMOVED*** parseModuleSpecifier();
    } else {
      exportClause ***REMOVED*** parseNamedImportsOrExports(280 /* NamedExports */);
      if (token() ***REMOVED******REMOVED******REMOVED*** 161 /* FromKeyword */ || token() ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ && !scanner.hasPrecedingLineBreak()) {
        parseExpected(161 /* FromKeyword */);
        moduleSpecifier ***REMOVED*** parseModuleSpecifier();
      }
    }
    const currentToken2 ***REMOVED*** token();
    if (moduleSpecifier && (currentToken2 ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ || currentToken2 ***REMOVED******REMOVED******REMOVED*** 132 /* AssertKeyword */) && !scanner.hasPrecedingLineBreak()) {
      attributes ***REMOVED*** parseImportAttributes(currentToken2);
    }
    parseSemicolon();
    setAwaitContext(savedAwaitContext);
    const node ***REMOVED*** factory2.createExportDeclaration(modifiers, isTypeOnly, exportClause, moduleSpecifier, attributes);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  function parseExportAssignment(pos, hasJSDoc, modifiers) {
    const savedAwaitContext ***REMOVED*** inAwaitContext();
    setAwaitContext(
      /*value*/
      true
    );
    let isExportEquals;
    if (parseOptional(64 /* EqualsToken */)) {
      isExportEquals ***REMOVED*** true;
    } else {
      parseExpected(90 /* DefaultKeyword */);
    }
    const expression ***REMOVED*** parseAssignmentExpressionOrHigher(
      /*allowReturnTypeInArrowFunction*/
      true
    );
    parseSemicolon();
    setAwaitContext(savedAwaitContext);
    const node ***REMOVED*** factory2.createExportAssignment(modifiers, isExportEquals, expression);
    return withJSDoc(finishNode(node, pos), hasJSDoc);
  }
  let ParsingContext;
  ((ParsingContext2) ***REMOVED***> {
    ParsingContext2[ParsingContext2["SourceElements"] ***REMOVED*** 0] ***REMOVED*** "SourceElements";
    ParsingContext2[ParsingContext2["BlockStatements"] ***REMOVED*** 1] ***REMOVED*** "BlockStatements";
    ParsingContext2[ParsingContext2["SwitchClauses"] ***REMOVED*** 2] ***REMOVED*** "SwitchClauses";
    ParsingContext2[ParsingContext2["SwitchClauseStatements"] ***REMOVED*** 3] ***REMOVED*** "SwitchClauseStatements";
    ParsingContext2[ParsingContext2["TypeMembers"] ***REMOVED*** 4] ***REMOVED*** "TypeMembers";
    ParsingContext2[ParsingContext2["ClassMembers"] ***REMOVED*** 5] ***REMOVED*** "ClassMembers";
    ParsingContext2[ParsingContext2["EnumMembers"] ***REMOVED*** 6] ***REMOVED*** "EnumMembers";
    ParsingContext2[ParsingContext2["HeritageClauseElement"] ***REMOVED*** 7] ***REMOVED*** "HeritageClauseElement";
    ParsingContext2[ParsingContext2["VariableDeclarations"] ***REMOVED*** 8] ***REMOVED*** "VariableDeclarations";
    ParsingContext2[ParsingContext2["ObjectBindingElements"] ***REMOVED*** 9] ***REMOVED*** "ObjectBindingElements";
    ParsingContext2[ParsingContext2["ArrayBindingElements"] ***REMOVED*** 10] ***REMOVED*** "ArrayBindingElements";
    ParsingContext2[ParsingContext2["ArgumentExpressions"] ***REMOVED*** 11] ***REMOVED*** "ArgumentExpressions";
    ParsingContext2[ParsingContext2["ObjectLiteralMembers"] ***REMOVED*** 12] ***REMOVED*** "ObjectLiteralMembers";
    ParsingContext2[ParsingContext2["JsxAttributes"] ***REMOVED*** 13] ***REMOVED*** "JsxAttributes";
    ParsingContext2[ParsingContext2["JsxChildren"] ***REMOVED*** 14] ***REMOVED*** "JsxChildren";
    ParsingContext2[ParsingContext2["ArrayLiteralMembers"] ***REMOVED*** 15] ***REMOVED*** "ArrayLiteralMembers";
    ParsingContext2[ParsingContext2["Parameters"] ***REMOVED*** 16] ***REMOVED*** "Parameters";
    ParsingContext2[ParsingContext2["JSDocParameters"] ***REMOVED*** 17] ***REMOVED*** "JSDocParameters";
    ParsingContext2[ParsingContext2["RestProperties"] ***REMOVED*** 18] ***REMOVED*** "RestProperties";
    ParsingContext2[ParsingContext2["TypeParameters"] ***REMOVED*** 19] ***REMOVED*** "TypeParameters";
    ParsingContext2[ParsingContext2["TypeArguments"] ***REMOVED*** 20] ***REMOVED*** "TypeArguments";
    ParsingContext2[ParsingContext2["TupleElementTypes"] ***REMOVED*** 21] ***REMOVED*** "TupleElementTypes";
    ParsingContext2[ParsingContext2["HeritageClauses"] ***REMOVED*** 22] ***REMOVED*** "HeritageClauses";
    ParsingContext2[ParsingContext2["ImportOrExportSpecifiers"] ***REMOVED*** 23] ***REMOVED*** "ImportOrExportSpecifiers";
    ParsingContext2[ParsingContext2["ImportAttributes"] ***REMOVED*** 24] ***REMOVED*** "ImportAttributes";
    ParsingContext2[ParsingContext2["JSDocComment"] ***REMOVED*** 25] ***REMOVED*** "JSDocComment";
    ParsingContext2[ParsingContext2["Count"] ***REMOVED*** 26] ***REMOVED*** "Count";
  })(ParsingContext || (ParsingContext ***REMOVED*** {}));
  let Tristate;
  ((Tristate2) ***REMOVED***> {
    Tristate2[Tristate2["False"] ***REMOVED*** 0] ***REMOVED*** "False";
    Tristate2[Tristate2["True"] ***REMOVED*** 1] ***REMOVED*** "True";
    Tristate2[Tristate2["Unknown"] ***REMOVED*** 2] ***REMOVED*** "Unknown";
  })(Tristate || (Tristate ***REMOVED*** {}));
  let JSDocParser;
  ((JSDocParser2) ***REMOVED***> {
    function parseJSDocTypeExpressionForTests(content, start, length2) {
      initializeState(
        "file.js",
        content,
        99 /* Latest */,
        /*syntaxCursor*/
        void 0,
        1 /* JS */,
        0 /* ParseAll */
      );
      scanner.setText(content, start, length2);
      currentToken ***REMOVED*** scanner.scan();
      const jsDocTypeExpression ***REMOVED*** parseJSDocTypeExpression();
      const sourceFile ***REMOVED*** createSourceFile2(
        "file.js",
        99 /* Latest */,
        1 /* JS */,
        /*isDeclarationFile*/
        false,
        [],
        factoryCreateToken(1 /* EndOfFileToken */),
        0 /* None */,
        noop
      );
      const diagnostics ***REMOVED*** attachFileToDiagnostics(parseDiagnostics, sourceFile);
      if (jsDocDiagnostics) {
        sourceFile.jsDocDiagnostics ***REMOVED*** attachFileToDiagnostics(jsDocDiagnostics, sourceFile);
      }
      clearState();
      return jsDocTypeExpression ? { jsDocTypeExpression, diagnostics } : void 0;
    }
    JSDocParser2.parseJSDocTypeExpressionForTests ***REMOVED*** parseJSDocTypeExpressionForTests;
    function parseJSDocTypeExpression(mayOmitBraces) {
      const pos ***REMOVED*** getNodePos();
      const hasBrace ***REMOVED*** (mayOmitBraces ? parseOptional : parseExpected)(19 /* OpenBraceToken */);
      const type ***REMOVED*** doInsideOfContext(16777216 /* JSDoc */, parseJSDocType);
      if (!mayOmitBraces || hasBrace) {
        parseExpectedJSDoc(20 /* CloseBraceToken */);
      }
      const result ***REMOVED*** factory2.createJSDocTypeExpression(type);
      fixupParentReferences(result);
      return finishNode(result, pos);
    }
    JSDocParser2.parseJSDocTypeExpression ***REMOVED*** parseJSDocTypeExpression;
    function parseJSDocNameReference() {
      const pos ***REMOVED*** getNodePos();
      const hasBrace ***REMOVED*** parseOptional(19 /* OpenBraceToken */);
      const p2 ***REMOVED*** getNodePos();
      let entityName ***REMOVED*** parseEntityName(
        /*allowReservedWords*/
        false
      );
      while (token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
        reScanHashToken();
        nextTokenJSDoc();
        entityName ***REMOVED*** finishNode(factory2.createJSDocMemberName(entityName, parseIdentifier()), p2);
      }
      if (hasBrace) {
        parseExpectedJSDoc(20 /* CloseBraceToken */);
      }
      const result ***REMOVED*** factory2.createJSDocNameReference(entityName);
      fixupParentReferences(result);
      return finishNode(result, pos);
    }
    JSDocParser2.parseJSDocNameReference ***REMOVED*** parseJSDocNameReference;
    function parseIsolatedJSDocComment(content, start, length2) {
      initializeState(
        "",
        content,
        99 /* Latest */,
        /*syntaxCursor*/
        void 0,
        1 /* JS */,
        0 /* ParseAll */
      );
      const jsDoc ***REMOVED*** doInsideOfContext(16777216 /* JSDoc */, () ***REMOVED***> parseJSDocCommentWorker(start, length2));
      const sourceFile ***REMOVED*** { languageVariant: 0 /* Standard */, text: content };
      const diagnostics ***REMOVED*** attachFileToDiagnostics(parseDiagnostics, sourceFile);
      clearState();
      return jsDoc ? { jsDoc, diagnostics } : void 0;
    }
    JSDocParser2.parseIsolatedJSDocComment ***REMOVED*** parseIsolatedJSDocComment;
    function parseJSDocComment(parent, start, length2) {
      const saveToken ***REMOVED*** currentToken;
      const saveParseDiagnosticsLength ***REMOVED*** parseDiagnostics.length;
      const saveParseErrorBeforeNextFinishedNode ***REMOVED*** parseErrorBeforeNextFinishedNode;
      const comment ***REMOVED*** doInsideOfContext(16777216 /* JSDoc */, () ***REMOVED***> parseJSDocCommentWorker(start, length2));
      setParent(comment, parent);
      if (contextFlags & 524288 /* JavaScriptFile */) {
        if (!jsDocDiagnostics) {
          jsDocDiagnostics ***REMOVED*** [];
        }
        addRange(jsDocDiagnostics, parseDiagnostics, saveParseDiagnosticsLength);
      }
      currentToken ***REMOVED*** saveToken;
      parseDiagnostics.length ***REMOVED*** saveParseDiagnosticsLength;
      parseErrorBeforeNextFinishedNode ***REMOVED*** saveParseErrorBeforeNextFinishedNode;
      return comment;
    }
    JSDocParser2.parseJSDocComment ***REMOVED*** parseJSDocComment;
    let JSDocState;
    ((JSDocState2) ***REMOVED***> {
      JSDocState2[JSDocState2["BeginningOfLine"] ***REMOVED*** 0] ***REMOVED*** "BeginningOfLine";
      JSDocState2[JSDocState2["SawAsterisk"] ***REMOVED*** 1] ***REMOVED*** "SawAsterisk";
      JSDocState2[JSDocState2["SavingComments"] ***REMOVED*** 2] ***REMOVED*** "SavingComments";
      JSDocState2[JSDocState2["SavingBackticks"] ***REMOVED*** 3] ***REMOVED*** "SavingBackticks";
    })(JSDocState || (JSDocState ***REMOVED*** {}));
    let PropertyLikeParse;
    ((PropertyLikeParse2) ***REMOVED***> {
      PropertyLikeParse2[PropertyLikeParse2["Property"] ***REMOVED*** 1] ***REMOVED*** "Property";
      PropertyLikeParse2[PropertyLikeParse2["Parameter"] ***REMOVED*** 2] ***REMOVED*** "Parameter";
      PropertyLikeParse2[PropertyLikeParse2["CallbackParameter"] ***REMOVED*** 4] ***REMOVED*** "CallbackParameter";
    })(PropertyLikeParse || (PropertyLikeParse ***REMOVED*** {}));
    function parseJSDocCommentWorker(start ***REMOVED*** 0, length2) {
      const content ***REMOVED*** sourceText;
      const end ***REMOVED*** length2 ***REMOVED******REMOVED******REMOVED*** void 0 ? content.length : start + length2;
      length2 ***REMOVED*** end - start;
      Debug.assert(start >***REMOVED*** 0);
      Debug.assert(start <***REMOVED*** end);
      Debug.assert(end <***REMOVED*** content.length);
      if (!isJSDocLikeText(content, start)) {
        return void 0;
      }
      let tags;
      let tagsPos;
      let tagsEnd;
      let linkEnd;
      let commentsPos;
      let comments ***REMOVED*** [];
      const parts ***REMOVED*** [];
      const saveParsingContext ***REMOVED*** parsingContext;
      parsingContext |***REMOVED*** 1 << 25 /* JSDocComment */;
      const result ***REMOVED*** scanner.scanRange(start + 3, length2 - 5, doJSDocScan);
      parsingContext ***REMOVED*** saveParsingContext;
      return result;
      function doJSDocScan() {
        let state ***REMOVED*** 1 /* SawAsterisk */;
        let margin;
        let indent2 ***REMOVED*** start - (content.lastIndexOf("\n", start) + 1) + 4;
        function pushComment(text) {
          if (!margin) {
            margin ***REMOVED*** indent2;
          }
          comments.push(text);
          indent2 +***REMOVED*** text.length;
        }
        nextTokenJSDoc();
        while (parseOptionalJsdoc(5 /* WhitespaceTrivia */)) ;
        if (parseOptionalJsdoc(4 /* NewLineTrivia */)) {
          state ***REMOVED*** 0 /* BeginningOfLine */;
          indent2 ***REMOVED*** 0;
        }
        loop:
          while (true) {
            switch (token()) {
              case 60 /* AtToken */:
                removeTrailingWhitespace(comments);
                if (!commentsPos) commentsPos ***REMOVED*** getNodePos();
                addTag(parseTag(indent2));
                state ***REMOVED*** 0 /* BeginningOfLine */;
                margin ***REMOVED*** void 0;
                break;
              case 4 /* NewLineTrivia */:
                comments.push(scanner.getTokenText());
                state ***REMOVED*** 0 /* BeginningOfLine */;
                indent2 ***REMOVED*** 0;
                break;
              case 42 /* AsteriskToken */:
                const asterisk ***REMOVED*** scanner.getTokenText();
                if (state ***REMOVED******REMOVED******REMOVED*** 1 /* SawAsterisk */) {
                  state ***REMOVED*** 2 /* SavingComments */;
                  pushComment(asterisk);
                } else {
                  Debug.assert(state ***REMOVED******REMOVED******REMOVED*** 0 /* BeginningOfLine */);
                  state ***REMOVED*** 1 /* SawAsterisk */;
                  indent2 +***REMOVED*** asterisk.length;
                }
                break;
              case 5 /* WhitespaceTrivia */:
                Debug.assert(state !***REMOVED******REMOVED*** 2 /* SavingComments */, "whitespace shouldn't come from the scanner while saving top-level comment text");
                const whitespace ***REMOVED*** scanner.getTokenText();
                if (margin !***REMOVED******REMOVED*** void 0 && indent2 + whitespace.length > margin) {
                  comments.push(whitespace.slice(margin - indent2));
                }
                indent2 +***REMOVED*** whitespace.length;
                break;
              case 1 /* EndOfFileToken */:
                break loop;
              case 82 /* JSDocCommentTextToken */:
                state ***REMOVED*** 2 /* SavingComments */;
                pushComment(scanner.getTokenValue());
                break;
              case 19 /* OpenBraceToken */:
                state ***REMOVED*** 2 /* SavingComments */;
                const commentEnd ***REMOVED*** scanner.getTokenFullStart();
                const linkStart ***REMOVED*** scanner.getTokenEnd() - 1;
                const link ***REMOVED*** parseJSDocLink(linkStart);
                if (link) {
                  if (!linkEnd) {
                    removeLeadingNewlines(comments);
                  }
                  parts.push(finishNode(factory2.createJSDocText(comments.join("")), linkEnd ?? start, commentEnd));
                  parts.push(link);
                  comments ***REMOVED*** [];
                  linkEnd ***REMOVED*** scanner.getTokenEnd();
                  break;
                }
              // fallthrough if it's not a {@link sequence
              default:
                state ***REMOVED*** 2 /* SavingComments */;
                pushComment(scanner.getTokenText());
                break;
            }
            if (state ***REMOVED******REMOVED******REMOVED*** 2 /* SavingComments */) {
              nextJSDocCommentTextToken(
                /*inBackticks*/
                false
              );
            } else {
              nextTokenJSDoc();
            }
          }
        const trimmedComments ***REMOVED*** comments.join("").trimEnd();
        if (parts.length && trimmedComments.length) {
          parts.push(finishNode(factory2.createJSDocText(trimmedComments), linkEnd ?? start, commentsPos));
        }
        if (parts.length && tags) Debug.assertIsDefined(commentsPos, "having parsed tags implies that the end of the comment span should be set");
        const tagsArray ***REMOVED*** tags && createNodeArray(tags, tagsPos, tagsEnd);
        return finishNode(factory2.createJSDocComment(parts.length ? createNodeArray(parts, start, commentsPos) : trimmedComments.length ? trimmedComments : void 0, tagsArray), start, end);
      }
      function removeLeadingNewlines(comments2) {
        while (comments2.length && (comments2[0] ***REMOVED******REMOVED******REMOVED*** "\n" || comments2[0] ***REMOVED******REMOVED******REMOVED*** "\r")) {
          comments2.shift();
        }
      }
      function removeTrailingWhitespace(comments2) {
        while (comments2.length) {
          const trimmed ***REMOVED*** comments2[comments2.length - 1].trimEnd();
          if (trimmed ***REMOVED******REMOVED******REMOVED*** "") {
            comments2.pop();
          } else if (trimmed.length < comments2[comments2.length - 1].length) {
            comments2[comments2.length - 1] ***REMOVED*** trimmed;
            break;
          } else {
            break;
          }
        }
      }
      function isNextNonwhitespaceTokenEndOfFile() {
        while (true) {
          nextTokenJSDoc();
          if (token() ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
            return true;
          }
          if (!(token() ***REMOVED******REMOVED******REMOVED*** 5 /* WhitespaceTrivia */ || token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */)) {
            return false;
          }
        }
      }
      function skipWhitespace() {
        if (token() ***REMOVED******REMOVED******REMOVED*** 5 /* WhitespaceTrivia */ || token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
          if (lookAhead(isNextNonwhitespaceTokenEndOfFile)) {
            return;
          }
        }
        while (token() ***REMOVED******REMOVED******REMOVED*** 5 /* WhitespaceTrivia */ || token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
          nextTokenJSDoc();
        }
      }
      function skipWhitespaceOrAsterisk() {
        if (token() ***REMOVED******REMOVED******REMOVED*** 5 /* WhitespaceTrivia */ || token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
          if (lookAhead(isNextNonwhitespaceTokenEndOfFile)) {
            return "";
          }
        }
        let precedingLineBreak ***REMOVED*** scanner.hasPrecedingLineBreak();
        let seenLineBreak ***REMOVED*** false;
        let indentText ***REMOVED*** "";
        while (precedingLineBreak && token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */ || token() ***REMOVED******REMOVED******REMOVED*** 5 /* WhitespaceTrivia */ || token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
          indentText +***REMOVED*** scanner.getTokenText();
          if (token() ***REMOVED******REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
            precedingLineBreak ***REMOVED*** true;
            seenLineBreak ***REMOVED*** true;
            indentText ***REMOVED*** "";
          } else if (token() ***REMOVED******REMOVED******REMOVED*** 42 /* AsteriskToken */) {
            precedingLineBreak ***REMOVED*** false;
          }
          nextTokenJSDoc();
        }
        return seenLineBreak ? indentText : "";
      }
      function parseTag(margin) {
        Debug.assert(token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */);
        const start2 ***REMOVED*** scanner.getTokenStart();
        nextTokenJSDoc();
        const tagName ***REMOVED*** parseJSDocIdentifierName(
          /*message*/
          void 0
        );
        const indentText ***REMOVED*** skipWhitespaceOrAsterisk();
        let tag;
        switch (tagName.escapedText) {
          case "author":
            tag ***REMOVED*** parseAuthorTag(start2, tagName, margin, indentText);
            break;
          case "implements":
            tag ***REMOVED*** parseImplementsTag(start2, tagName, margin, indentText);
            break;
          case "augments":
          case "extends":
            tag ***REMOVED*** parseAugmentsTag(start2, tagName, margin, indentText);
            break;
          case "class":
          case "constructor":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocClassTag, tagName, margin, indentText);
            break;
          case "public":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocPublicTag, tagName, margin, indentText);
            break;
          case "private":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocPrivateTag, tagName, margin, indentText);
            break;
          case "protected":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocProtectedTag, tagName, margin, indentText);
            break;
          case "readonly":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocReadonlyTag, tagName, margin, indentText);
            break;
          case "override":
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocOverrideTag, tagName, margin, indentText);
            break;
          case "deprecated":
            hasDeprecatedTag ***REMOVED*** true;
            tag ***REMOVED*** parseSimpleTag(start2, factory2.createJSDocDeprecatedTag, tagName, margin, indentText);
            break;
          case "this":
            tag ***REMOVED*** parseThisTag(start2, tagName, margin, indentText);
            break;
          case "enum":
            tag ***REMOVED*** parseEnumTag(start2, tagName, margin, indentText);
            break;
          case "arg":
          case "argument":
          case "param":
            return parseParameterOrPropertyTag(start2, tagName, 2 /* Parameter */, margin);
          case "return":
          case "returns":
            tag ***REMOVED*** parseReturnTag(start2, tagName, margin, indentText);
            break;
          case "template":
            tag ***REMOVED*** parseTemplateTag(start2, tagName, margin, indentText);
            break;
          case "type":
            tag ***REMOVED*** parseTypeTag(start2, tagName, margin, indentText);
            break;
          case "typedef":
            tag ***REMOVED*** parseTypedefTag(start2, tagName, margin, indentText);
            break;
          case "callback":
            tag ***REMOVED*** parseCallbackTag(start2, tagName, margin, indentText);
            break;
          case "overload":
            tag ***REMOVED*** parseOverloadTag(start2, tagName, margin, indentText);
            break;
          case "satisfies":
            tag ***REMOVED*** parseSatisfiesTag(start2, tagName, margin, indentText);
            break;
          case "see":
            tag ***REMOVED*** parseSeeTag(start2, tagName, margin, indentText);
            break;
          case "exception":
          case "throws":
            tag ***REMOVED*** parseThrowsTag(start2, tagName, margin, indentText);
            break;
          case "import":
            tag ***REMOVED*** parseImportTag(start2, tagName, margin, indentText);
            break;
          default:
            tag ***REMOVED*** parseUnknownTag(start2, tagName, margin, indentText);
            break;
        }
        return tag;
      }
      function parseTrailingTagComments(pos, end2, margin, indentText) {
        if (!indentText) {
          margin +***REMOVED*** end2 - pos;
        }
        return parseTagComments(margin, indentText.slice(margin));
      }
      function parseTagComments(indent2, initialMargin) {
        const commentsPos2 ***REMOVED*** getNodePos();
        let comments2 ***REMOVED*** [];
        const parts2 ***REMOVED*** [];
        let linkEnd2;
        let state ***REMOVED*** 0 /* BeginningOfLine */;
        let margin;
        function pushComment(text) {
          if (!margin) {
            margin ***REMOVED*** indent2;
          }
          comments2.push(text);
          indent2 +***REMOVED*** text.length;
        }
        if (initialMargin !***REMOVED******REMOVED*** void 0) {
          if (initialMargin !***REMOVED******REMOVED*** "") {
            pushComment(initialMargin);
          }
          state ***REMOVED*** 1 /* SawAsterisk */;
        }
        let tok ***REMOVED*** token();
        loop:
          while (true) {
            switch (tok) {
              case 4 /* NewLineTrivia */:
                state ***REMOVED*** 0 /* BeginningOfLine */;
                comments2.push(scanner.getTokenText());
                indent2 ***REMOVED*** 0;
                break;
              case 60 /* AtToken */:
                scanner.resetTokenState(scanner.getTokenEnd() - 1);
                break loop;
              case 1 /* EndOfFileToken */:
                break loop;
              case 5 /* WhitespaceTrivia */:
                Debug.assert(state !***REMOVED******REMOVED*** 2 /* SavingComments */ && state !***REMOVED******REMOVED*** 3 /* SavingBackticks */, "whitespace shouldn't come from the scanner while saving comment text");
                const whitespace ***REMOVED*** scanner.getTokenText();
                if (margin !***REMOVED******REMOVED*** void 0 && indent2 + whitespace.length > margin) {
                  comments2.push(whitespace.slice(margin - indent2));
                  state ***REMOVED*** 2 /* SavingComments */;
                }
                indent2 +***REMOVED*** whitespace.length;
                break;
              case 19 /* OpenBraceToken */:
                state ***REMOVED*** 2 /* SavingComments */;
                const commentEnd ***REMOVED*** scanner.getTokenFullStart();
                const linkStart ***REMOVED*** scanner.getTokenEnd() - 1;
                const link ***REMOVED*** parseJSDocLink(linkStart);
                if (link) {
                  parts2.push(finishNode(factory2.createJSDocText(comments2.join("")), linkEnd2 ?? commentsPos2, commentEnd));
                  parts2.push(link);
                  comments2 ***REMOVED*** [];
                  linkEnd2 ***REMOVED*** scanner.getTokenEnd();
                } else {
                  pushComment(scanner.getTokenText());
                }
                break;
              case 62 /* BacktickToken */:
                if (state ***REMOVED******REMOVED******REMOVED*** 3 /* SavingBackticks */) {
                  state ***REMOVED*** 2 /* SavingComments */;
                } else {
                  state ***REMOVED*** 3 /* SavingBackticks */;
                }
                pushComment(scanner.getTokenText());
                break;
              case 82 /* JSDocCommentTextToken */:
                if (state !***REMOVED******REMOVED*** 3 /* SavingBackticks */) {
                  state ***REMOVED*** 2 /* SavingComments */;
                }
                pushComment(scanner.getTokenValue());
                break;
              case 42 /* AsteriskToken */:
                if (state ***REMOVED******REMOVED******REMOVED*** 0 /* BeginningOfLine */) {
                  state ***REMOVED*** 1 /* SawAsterisk */;
                  indent2 +***REMOVED*** 1;
                  break;
                }
              // record the * as a comment
              // falls through
              default:
                if (state !***REMOVED******REMOVED*** 3 /* SavingBackticks */) {
                  state ***REMOVED*** 2 /* SavingComments */;
                }
                pushComment(scanner.getTokenText());
                break;
            }
            if (state ***REMOVED******REMOVED******REMOVED*** 2 /* SavingComments */ || state ***REMOVED******REMOVED******REMOVED*** 3 /* SavingBackticks */) {
              tok ***REMOVED*** nextJSDocCommentTextToken(state ***REMOVED******REMOVED******REMOVED*** 3 /* SavingBackticks */);
            } else {
              tok ***REMOVED*** nextTokenJSDoc();
            }
          }
        removeLeadingNewlines(comments2);
        const trimmedComments ***REMOVED*** comments2.join("").trimEnd();
        if (parts2.length) {
          if (trimmedComments.length) {
            parts2.push(finishNode(factory2.createJSDocText(trimmedComments), linkEnd2 ?? commentsPos2));
          }
          return createNodeArray(parts2, commentsPos2, scanner.getTokenEnd());
        } else if (trimmedComments.length) {
          return trimmedComments;
        }
      }
      function parseJSDocLink(start2) {
        const linkType ***REMOVED*** tryParse(parseJSDocLinkPrefix);
        if (!linkType) {
          return void 0;
        }
        nextTokenJSDoc();
        skipWhitespace();
        const name ***REMOVED*** parseJSDocLinkName();
        const text ***REMOVED*** [];
        while (token() !***REMOVED******REMOVED*** 20 /* CloseBraceToken */ && token() !***REMOVED******REMOVED*** 4 /* NewLineTrivia */ && token() !***REMOVED******REMOVED*** 1 /* EndOfFileToken */) {
          text.push(scanner.getTokenText());
          nextTokenJSDoc();
        }
        const create ***REMOVED*** linkType ***REMOVED******REMOVED******REMOVED*** "link" ? factory2.createJSDocLink : linkType ***REMOVED******REMOVED******REMOVED*** "linkcode" ? factory2.createJSDocLinkCode : factory2.createJSDocLinkPlain;
        return finishNode(create(name, text.join("")), start2, scanner.getTokenEnd());
      }
      function parseJSDocLinkName() {
        if (tokenIsIdentifierOrKeyword(token())) {
          const pos ***REMOVED*** getNodePos();
          let name ***REMOVED*** parseIdentifierName();
          while (parseOptional(25 /* DotToken */)) {
            name ***REMOVED*** finishNode(factory2.createQualifiedName(name, token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */ ? createMissingNode(
              80 /* Identifier */,
              /*reportAtCurrentPosition*/
              false
            ) : parseIdentifierName()), pos);
          }
          while (token() ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
            reScanHashToken();
            nextTokenJSDoc();
            name ***REMOVED*** finishNode(factory2.createJSDocMemberName(name, parseIdentifier()), pos);
          }
          return name;
        }
        return void 0;
      }
      function parseJSDocLinkPrefix() {
        skipWhitespaceOrAsterisk();
        if (token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ && nextTokenJSDoc() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ && tokenIsIdentifierOrKeyword(nextTokenJSDoc())) {
          const kind ***REMOVED*** scanner.getTokenValue();
          if (isJSDocLinkTag(kind)) return kind;
        }
      }
      function isJSDocLinkTag(kind) {
        return kind ***REMOVED******REMOVED******REMOVED*** "link" || kind ***REMOVED******REMOVED******REMOVED*** "linkcode" || kind ***REMOVED******REMOVED******REMOVED*** "linkplain";
      }
      function parseUnknownTag(start2, tagName, indent2, indentText) {
        return finishNode(factory2.createJSDocUnknownTag(tagName, parseTrailingTagComments(start2, getNodePos(), indent2, indentText)), start2);
      }
      function addTag(tag) {
        if (!tag) {
          return;
        }
        if (!tags) {
          tags ***REMOVED*** [tag];
          tagsPos ***REMOVED*** tag.pos;
        } else {
          tags.push(tag);
        }
        tagsEnd ***REMOVED*** tag.end;
      }
      function tryParseTypeExpression() {
        skipWhitespaceOrAsterisk();
        return token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ ? parseJSDocTypeExpression() : void 0;
      }
      function parseBracketNameInPropertyAndParamTag() {
        const isBracketed ***REMOVED*** parseOptionalJsdoc(23 /* OpenBracketToken */);
        if (isBracketed) {
          skipWhitespace();
        }
        const isBackquoted ***REMOVED*** parseOptionalJsdoc(62 /* BacktickToken */);
        const name ***REMOVED*** parseJSDocEntityName();
        if (isBackquoted) {
          parseExpectedTokenJSDoc(62 /* BacktickToken */);
        }
        if (isBracketed) {
          skipWhitespace();
          if (parseOptionalToken(64 /* EqualsToken */)) {
            parseExpression();
          }
          parseExpected(24 /* CloseBracketToken */);
        }
        return { name, isBracketed };
      }
      function isObjectOrObjectArrayTypeReference(node) {
        switch (node.kind) {
          case 151 /* ObjectKeyword */:
            return true;
          case 189 /* ArrayType */:
            return isObjectOrObjectArrayTypeReference(node.elementType);
          default:
            return isTypeReferenceNode(node) && isIdentifier(node.typeName) && node.typeName.escapedText ***REMOVED******REMOVED******REMOVED*** "Object" && !node.typeArguments;
        }
      }
      function parseParameterOrPropertyTag(start2, tagName, target, indent2) {
        let typeExpression ***REMOVED*** tryParseTypeExpression();
        let isNameFirst ***REMOVED*** !typeExpression;
        skipWhitespaceOrAsterisk();
        const { name, isBracketed } ***REMOVED*** parseBracketNameInPropertyAndParamTag();
        const indentText ***REMOVED*** skipWhitespaceOrAsterisk();
        if (isNameFirst && !lookAhead(parseJSDocLinkPrefix)) {
          typeExpression ***REMOVED*** tryParseTypeExpression();
        }
        const comment ***REMOVED*** parseTrailingTagComments(start2, getNodePos(), indent2, indentText);
        const nestedTypeLiteral ***REMOVED*** parseNestedTypeLiteral(typeExpression, name, target, indent2);
        if (nestedTypeLiteral) {
          typeExpression ***REMOVED*** nestedTypeLiteral;
          isNameFirst ***REMOVED*** true;
        }
        const result2 ***REMOVED*** target ***REMOVED******REMOVED******REMOVED*** 1 /* Property */ ? factory2.createJSDocPropertyTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment) : factory2.createJSDocParameterTag(tagName, name, isBracketed, typeExpression, isNameFirst, comment);
        return finishNode(result2, start2);
      }
      function parseNestedTypeLiteral(typeExpression, name, target, indent2) {
        if (typeExpression && isObjectOrObjectArrayTypeReference(typeExpression.type)) {
          const pos ***REMOVED*** getNodePos();
          let child;
          let children;
          while (child ***REMOVED*** tryParse(() ***REMOVED***> parseChildParameterOrPropertyTag(target, indent2, name))) {
            if (child.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */ || child.kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */) {
              children ***REMOVED*** append(children, child);
            } else if (child.kind ***REMOVED******REMOVED******REMOVED*** 346 /* JSDocTemplateTag */) {
              parseErrorAtRange(child.tagName, Diagnostics.A_JSDoc_template_tag_may_not_follow_a_typedef_callback_or_overload_tag);
            }
          }
          if (children) {
            const literal ***REMOVED*** finishNode(factory2.createJSDocTypeLiteral(children, typeExpression.type.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */), pos);
            return finishNode(factory2.createJSDocTypeExpression(literal), pos);
          }
        }
      }
      function parseReturnTag(start2, tagName, indent2, indentText) {
        if (some(tags, isJSDocReturnTag)) {
          parseErrorAt(tagName.pos, scanner.getTokenStart(), Diagnostics._0_tag_already_specified, unescapeLeadingUnderscores(tagName.escapedText));
        }
        const typeExpression ***REMOVED*** tryParseTypeExpression();
        return finishNode(factory2.createJSDocReturnTag(tagName, typeExpression, parseTrailingTagComments(start2, getNodePos(), indent2, indentText)), start2);
      }
      function parseTypeTag(start2, tagName, indent2, indentText) {
        if (some(tags, isJSDocTypeTag)) {
          parseErrorAt(tagName.pos, scanner.getTokenStart(), Diagnostics._0_tag_already_specified, unescapeLeadingUnderscores(tagName.escapedText));
        }
        const typeExpression ***REMOVED*** parseJSDocTypeExpression(
          /*mayOmitBraces*/
          true
        );
        const comments2 ***REMOVED*** indent2 !***REMOVED******REMOVED*** void 0 && indentText !***REMOVED******REMOVED*** void 0 ? parseTrailingTagComments(start2, getNodePos(), indent2, indentText) : void 0;
        return finishNode(factory2.createJSDocTypeTag(tagName, typeExpression, comments2), start2);
      }
      function parseSeeTag(start2, tagName, indent2, indentText) {
        const isMarkdownOrJSDocLink ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 23 /* OpenBracketToken */ || lookAhead(() ***REMOVED***> nextTokenJSDoc() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ && tokenIsIdentifierOrKeyword(nextTokenJSDoc()) && isJSDocLinkTag(scanner.getTokenValue()));
        const nameExpression ***REMOVED*** isMarkdownOrJSDocLink ? void 0 : parseJSDocNameReference();
        const comments2 ***REMOVED*** indent2 !***REMOVED******REMOVED*** void 0 && indentText !***REMOVED******REMOVED*** void 0 ? parseTrailingTagComments(start2, getNodePos(), indent2, indentText) : void 0;
        return finishNode(factory2.createJSDocSeeTag(tagName, nameExpression, comments2), start2);
      }
      function parseThrowsTag(start2, tagName, indent2, indentText) {
        const typeExpression ***REMOVED*** tryParseTypeExpression();
        const comment ***REMOVED*** parseTrailingTagComments(start2, getNodePos(), indent2, indentText);
        return finishNode(factory2.createJSDocThrowsTag(tagName, typeExpression, comment), start2);
      }
      function parseAuthorTag(start2, tagName, indent2, indentText) {
        const commentStart ***REMOVED*** getNodePos();
        const textOnly ***REMOVED*** parseAuthorNameAndEmail();
        let commentEnd ***REMOVED*** scanner.getTokenFullStart();
        const comments2 ***REMOVED*** parseTrailingTagComments(start2, commentEnd, indent2, indentText);
        if (!comments2) {
          commentEnd ***REMOVED*** scanner.getTokenFullStart();
        }
        const allParts ***REMOVED*** typeof comments2 !***REMOVED******REMOVED*** "string" ? createNodeArray(concatenate([finishNode(textOnly, commentStart, commentEnd)], comments2), commentStart) : textOnly.text + comments2;
        return finishNode(factory2.createJSDocAuthorTag(tagName, allParts), start2);
      }
      function parseAuthorNameAndEmail() {
        const comments2 ***REMOVED*** [];
        let inEmail ***REMOVED*** false;
        let token2 ***REMOVED*** scanner.getToken();
        while (token2 !***REMOVED******REMOVED*** 1 /* EndOfFileToken */ && token2 !***REMOVED******REMOVED*** 4 /* NewLineTrivia */) {
          if (token2 ***REMOVED******REMOVED******REMOVED*** 30 /* LessThanToken */) {
            inEmail ***REMOVED*** true;
          } else if (token2 ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */ && !inEmail) {
            break;
          } else if (token2 ***REMOVED******REMOVED******REMOVED*** 32 /* GreaterThanToken */ && inEmail) {
            comments2.push(scanner.getTokenText());
            scanner.resetTokenState(scanner.getTokenEnd());
            break;
          }
          comments2.push(scanner.getTokenText());
          token2 ***REMOVED*** nextTokenJSDoc();
        }
        return factory2.createJSDocText(comments2.join(""));
      }
      function parseImplementsTag(start2, tagName, margin, indentText) {
        const className ***REMOVED*** parseExpressionWithTypeArgumentsForAugments();
        return finishNode(factory2.createJSDocImplementsTag(tagName, className, parseTrailingTagComments(start2, getNodePos(), margin, indentText)), start2);
      }
      function parseAugmentsTag(start2, tagName, margin, indentText) {
        const className ***REMOVED*** parseExpressionWithTypeArgumentsForAugments();
        return finishNode(factory2.createJSDocAugmentsTag(tagName, className, parseTrailingTagComments(start2, getNodePos(), margin, indentText)), start2);
      }
      function parseSatisfiesTag(start2, tagName, margin, indentText) {
        const typeExpression ***REMOVED*** parseJSDocTypeExpression(
          /*mayOmitBraces*/
          false
        );
        const comments2 ***REMOVED*** margin !***REMOVED******REMOVED*** void 0 && indentText !***REMOVED******REMOVED*** void 0 ? parseTrailingTagComments(start2, getNodePos(), margin, indentText) : void 0;
        return finishNode(factory2.createJSDocSatisfiesTag(tagName, typeExpression, comments2), start2);
      }
      function parseImportTag(start2, tagName, margin, indentText) {
        const afterImportTagPos ***REMOVED*** scanner.getTokenFullStart();
        let identifier;
        if (isIdentifier2()) {
          identifier ***REMOVED*** parseIdentifier();
        }
        const importClause ***REMOVED*** tryParseImportClause(
          identifier,
          afterImportTagPos,
          156 /* TypeKeyword */,
          /*skipJsDocLeadingAsterisks*/
          true
        );
        const moduleSpecifier ***REMOVED*** parseModuleSpecifier();
        const attributes ***REMOVED*** tryParseImportAttributes();
        const comments2 ***REMOVED*** margin !***REMOVED******REMOVED*** void 0 && indentText !***REMOVED******REMOVED*** void 0 ? parseTrailingTagComments(start2, getNodePos(), margin, indentText) : void 0;
        return finishNode(factory2.createJSDocImportTag(tagName, importClause, moduleSpecifier, attributes, comments2), start2);
      }
      function parseExpressionWithTypeArgumentsForAugments() {
        const usedBrace ***REMOVED*** parseOptional(19 /* OpenBraceToken */);
        const pos ***REMOVED*** getNodePos();
        const expression ***REMOVED*** parsePropertyAccessEntityNameExpression();
        scanner.setSkipJsDocLeadingAsterisks(true);
        const typeArguments ***REMOVED*** tryParseTypeArguments();
        scanner.setSkipJsDocLeadingAsterisks(false);
        const node ***REMOVED*** factory2.createExpressionWithTypeArguments(expression, typeArguments);
        const res ***REMOVED*** finishNode(node, pos);
        if (usedBrace) {
          skipWhitespace();
          parseExpected(20 /* CloseBraceToken */);
        }
        return res;
      }
      function parsePropertyAccessEntityNameExpression() {
        const pos ***REMOVED*** getNodePos();
        let node ***REMOVED*** parseJSDocIdentifierName();
        while (parseOptional(25 /* DotToken */)) {
          const name ***REMOVED*** parseJSDocIdentifierName();
          node ***REMOVED*** finishNode(factoryCreatePropertyAccessExpression(node, name), pos);
        }
        return node;
      }
      function parseSimpleTag(start2, createTag, tagName, margin, indentText) {
        return finishNode(createTag(tagName, parseTrailingTagComments(start2, getNodePos(), margin, indentText)), start2);
      }
      function parseThisTag(start2, tagName, margin, indentText) {
        const typeExpression ***REMOVED*** parseJSDocTypeExpression(
          /*mayOmitBraces*/
          true
        );
        skipWhitespace();
        return finishNode(factory2.createJSDocThisTag(tagName, typeExpression, parseTrailingTagComments(start2, getNodePos(), margin, indentText)), start2);
      }
      function parseEnumTag(start2, tagName, margin, indentText) {
        const typeExpression ***REMOVED*** parseJSDocTypeExpression(
          /*mayOmitBraces*/
          true
        );
        skipWhitespace();
        return finishNode(factory2.createJSDocEnumTag(tagName, typeExpression, parseTrailingTagComments(start2, getNodePos(), margin, indentText)), start2);
      }
      function parseTypedefTag(start2, tagName, indent2, indentText) {
        let typeExpression ***REMOVED*** tryParseTypeExpression();
        skipWhitespaceOrAsterisk();
        const fullName ***REMOVED*** parseJSDocTypeNameWithNamespace();
        skipWhitespace();
        let comment ***REMOVED*** parseTagComments(indent2);
        let end2;
        if (!typeExpression || isObjectOrObjectArrayTypeReference(typeExpression.type)) {
          let child;
          let childTypeTag;
          let jsDocPropertyTags;
          let hasChildren ***REMOVED*** false;
          while (child ***REMOVED*** tryParse(() ***REMOVED***> parseChildPropertyTag(indent2))) {
            if (child.kind ***REMOVED******REMOVED******REMOVED*** 346 /* JSDocTemplateTag */) {
              break;
            }
            hasChildren ***REMOVED*** true;
            if (child.kind ***REMOVED******REMOVED******REMOVED*** 345 /* JSDocTypeTag */) {
              if (childTypeTag) {
                const lastError ***REMOVED*** parseErrorAtCurrentToken(Diagnostics.A_JSDoc_typedef_comment_may_not_contain_multiple_type_tags);
                if (lastError) {
                  addRelatedInfo(lastError, createDetachedDiagnostic(fileName, sourceText, 0, 0, Diagnostics.The_tag_was_first_specified_here));
                }
                break;
              } else {
                childTypeTag ***REMOVED*** child;
              }
            } else {
              jsDocPropertyTags ***REMOVED*** append(jsDocPropertyTags, child);
            }
          }
          if (hasChildren) {
            const isArrayType ***REMOVED*** typeExpression && typeExpression.type.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */;
            const jsdocTypeLiteral ***REMOVED*** factory2.createJSDocTypeLiteral(jsDocPropertyTags, isArrayType);
            typeExpression ***REMOVED*** childTypeTag && childTypeTag.typeExpression && !isObjectOrObjectArrayTypeReference(childTypeTag.typeExpression.type) ? childTypeTag.typeExpression : finishNode(jsdocTypeLiteral, start2);
            end2 ***REMOVED*** typeExpression.end;
          }
        }
        end2 ***REMOVED*** end2 || comment !***REMOVED******REMOVED*** void 0 ? getNodePos() : (fullName ?? typeExpression ?? tagName).end;
        if (!comment) {
          comment ***REMOVED*** parseTrailingTagComments(start2, end2, indent2, indentText);
        }
        const typedefTag ***REMOVED*** factory2.createJSDocTypedefTag(tagName, typeExpression, fullName, comment);
        return finishNode(typedefTag, start2, end2);
      }
      function parseJSDocTypeNameWithNamespace(nested) {
        const start2 ***REMOVED*** scanner.getTokenStart();
        if (!tokenIsIdentifierOrKeyword(token())) {
          return void 0;
        }
        const typeNameOrNamespaceName ***REMOVED*** parseJSDocIdentifierName();
        if (parseOptional(25 /* DotToken */)) {
          const body ***REMOVED*** parseJSDocTypeNameWithNamespace(
            /*nested*/
            true
          );
          const jsDocNamespaceNode ***REMOVED*** factory2.createModuleDeclaration(
            /*modifiers*/
            void 0,
            typeNameOrNamespaceName,
            body,
            nested ? 8 /* NestedNamespace */ : void 0
          );
          return finishNode(jsDocNamespaceNode, start2);
        }
        if (nested) {
          typeNameOrNamespaceName.flags |***REMOVED*** 4096 /* IdentifierIsInJSDocNamespace */;
        }
        return typeNameOrNamespaceName;
      }
      function parseCallbackTagParameters(indent2) {
        const pos ***REMOVED*** getNodePos();
        let child;
        let parameters;
        while (child ***REMOVED*** tryParse(() ***REMOVED***> parseChildParameterOrPropertyTag(4 /* CallbackParameter */, indent2))) {
          if (child.kind ***REMOVED******REMOVED******REMOVED*** 346 /* JSDocTemplateTag */) {
            parseErrorAtRange(child.tagName, Diagnostics.A_JSDoc_template_tag_may_not_follow_a_typedef_callback_or_overload_tag);
            break;
          }
          parameters ***REMOVED*** append(parameters, child);
        }
        return createNodeArray(parameters || [], pos);
      }
      function parseJSDocSignature(start2, indent2) {
        const parameters ***REMOVED*** parseCallbackTagParameters(indent2);
        const returnTag ***REMOVED*** tryParse(() ***REMOVED***> {
          if (parseOptionalJsdoc(60 /* AtToken */)) {
            const tag ***REMOVED*** parseTag(indent2);
            if (tag && tag.kind ***REMOVED******REMOVED******REMOVED*** 343 /* JSDocReturnTag */) {
              return tag;
            }
          }
        });
        return finishNode(factory2.createJSDocSignature(
          /*typeParameters*/
          void 0,
          parameters,
          returnTag
        ), start2);
      }
      function parseCallbackTag(start2, tagName, indent2, indentText) {
        const fullName ***REMOVED*** parseJSDocTypeNameWithNamespace();
        skipWhitespace();
        let comment ***REMOVED*** parseTagComments(indent2);
        const typeExpression ***REMOVED*** parseJSDocSignature(start2, indent2);
        if (!comment) {
          comment ***REMOVED*** parseTrailingTagComments(start2, getNodePos(), indent2, indentText);
        }
        const end2 ***REMOVED*** comment !***REMOVED******REMOVED*** void 0 ? getNodePos() : typeExpression.end;
        return finishNode(factory2.createJSDocCallbackTag(tagName, typeExpression, fullName, comment), start2, end2);
      }
      function parseOverloadTag(start2, tagName, indent2, indentText) {
        skipWhitespace();
        let comment ***REMOVED*** parseTagComments(indent2);
        const typeExpression ***REMOVED*** parseJSDocSignature(start2, indent2);
        if (!comment) {
          comment ***REMOVED*** parseTrailingTagComments(start2, getNodePos(), indent2, indentText);
        }
        const end2 ***REMOVED*** comment !***REMOVED******REMOVED*** void 0 ? getNodePos() : typeExpression.end;
        return finishNode(factory2.createJSDocOverloadTag(tagName, typeExpression, comment), start2, end2);
      }
      function escapedTextsEqual(a, b) {
        while (!isIdentifier(a) || !isIdentifier(b)) {
          if (!isIdentifier(a) && !isIdentifier(b) && a.right.escapedText ***REMOVED******REMOVED******REMOVED*** b.right.escapedText) {
            a ***REMOVED*** a.left;
            b ***REMOVED*** b.left;
          } else {
            return false;
          }
        }
        return a.escapedText ***REMOVED******REMOVED******REMOVED*** b.escapedText;
      }
      function parseChildPropertyTag(indent2) {
        return parseChildParameterOrPropertyTag(1 /* Property */, indent2);
      }
      function parseChildParameterOrPropertyTag(target, indent2, name) {
        let canParseTag ***REMOVED*** true;
        let seenAsterisk ***REMOVED*** false;
        while (true) {
          switch (nextTokenJSDoc()) {
            case 60 /* AtToken */:
              if (canParseTag) {
                const child ***REMOVED*** tryParseChildTag(target, indent2);
                if (child && (child.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */ || child.kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */) && name && (isIdentifier(child.name) || !escapedTextsEqual(name, child.name.left))) {
                  return false;
                }
                return child;
              }
              seenAsterisk ***REMOVED*** false;
              break;
            case 4 /* NewLineTrivia */:
              canParseTag ***REMOVED*** true;
              seenAsterisk ***REMOVED*** false;
              break;
            case 42 /* AsteriskToken */:
              if (seenAsterisk) {
                canParseTag ***REMOVED*** false;
              }
              seenAsterisk ***REMOVED*** true;
              break;
            case 80 /* Identifier */:
              canParseTag ***REMOVED*** false;
              break;
            case 1 /* EndOfFileToken */:
              return false;
          }
        }
      }
      function tryParseChildTag(target, indent2) {
        Debug.assert(token() ***REMOVED******REMOVED******REMOVED*** 60 /* AtToken */);
        const start2 ***REMOVED*** scanner.getTokenFullStart();
        nextTokenJSDoc();
        const tagName ***REMOVED*** parseJSDocIdentifierName();
        const indentText ***REMOVED*** skipWhitespaceOrAsterisk();
        let t;
        switch (tagName.escapedText) {
          case "type":
            return target ***REMOVED******REMOVED******REMOVED*** 1 /* Property */ && parseTypeTag(start2, tagName);
          case "prop":
          case "property":
            t ***REMOVED*** 1 /* Property */;
            break;
          case "arg":
          case "argument":
          case "param":
            t ***REMOVED*** 2 /* Parameter */ | 4 /* CallbackParameter */;
            break;
          case "template":
            return parseTemplateTag(start2, tagName, indent2, indentText);
          case "this":
            return parseThisTag(start2, tagName, indent2, indentText);
          default:
            return false;
        }
        if (!(target & t)) {
          return false;
        }
        return parseParameterOrPropertyTag(start2, tagName, target, indent2);
      }
      function parseTemplateTagTypeParameter() {
        const typeParameterPos ***REMOVED*** getNodePos();
        const isBracketed ***REMOVED*** parseOptionalJsdoc(23 /* OpenBracketToken */);
        if (isBracketed) {
          skipWhitespace();
        }
        const modifiers ***REMOVED*** parseModifiers(
          /*allowDecorators*/
          false,
          /*permitConstAsModifier*/
          true
        );
        const name ***REMOVED*** parseJSDocIdentifierName(Diagnostics.Unexpected_token_A_type_parameter_name_was_expected_without_curly_braces);
        let defaultType;
        if (isBracketed) {
          skipWhitespace();
          parseExpected(64 /* EqualsToken */);
          defaultType ***REMOVED*** doInsideOfContext(16777216 /* JSDoc */, parseJSDocType);
          parseExpected(24 /* CloseBracketToken */);
        }
        if (nodeIsMissing(name)) {
          return void 0;
        }
        return finishNode(factory2.createTypeParameterDeclaration(
          modifiers,
          name,
          /*constraint*/
          void 0,
          defaultType
        ), typeParameterPos);
      }
      function parseTemplateTagTypeParameters() {
        const pos ***REMOVED*** getNodePos();
        const typeParameters ***REMOVED*** [];
        do {
          skipWhitespace();
          const node ***REMOVED*** parseTemplateTagTypeParameter();
          if (node !***REMOVED******REMOVED*** void 0) {
            typeParameters.push(node);
          }
          skipWhitespaceOrAsterisk();
        } while (parseOptionalJsdoc(28 /* CommaToken */));
        return createNodeArray(typeParameters, pos);
      }
      function parseTemplateTag(start2, tagName, indent2, indentText) {
        const constraint ***REMOVED*** token() ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ ? parseJSDocTypeExpression() : void 0;
        const typeParameters ***REMOVED*** parseTemplateTagTypeParameters();
        return finishNode(factory2.createJSDocTemplateTag(tagName, constraint, typeParameters, parseTrailingTagComments(start2, getNodePos(), indent2, indentText)), start2);
      }
      function parseOptionalJsdoc(t) {
        if (token() ***REMOVED******REMOVED******REMOVED*** t) {
          nextTokenJSDoc();
          return true;
        }
        return false;
      }
      function parseJSDocEntityName() {
        let entity ***REMOVED*** parseJSDocIdentifierName();
        if (parseOptional(23 /* OpenBracketToken */)) {
          parseExpected(24 /* CloseBracketToken */);
        }
        while (parseOptional(25 /* DotToken */)) {
          const name ***REMOVED*** parseJSDocIdentifierName();
          if (parseOptional(23 /* OpenBracketToken */)) {
            parseExpected(24 /* CloseBracketToken */);
          }
          entity ***REMOVED*** createQualifiedName(entity, name);
        }
        return entity;
      }
      function parseJSDocIdentifierName(message) {
        if (!tokenIsIdentifierOrKeyword(token())) {
          return createMissingNode(
            80 /* Identifier */,
            /*reportAtCurrentPosition*/
            !message,
            message || Diagnostics.Identifier_expected
          );
        }
        identifierCount++;
        const start2 ***REMOVED*** scanner.getTokenStart();
        const end2 ***REMOVED*** scanner.getTokenEnd();
        const originalKeywordKind ***REMOVED*** token();
        const text ***REMOVED*** internIdentifier(scanner.getTokenValue());
        const result2 ***REMOVED*** finishNode(factoryCreateIdentifier(text, originalKeywordKind), start2, end2);
        nextTokenJSDoc();
        return result2;
      }
    }
  })(JSDocParser ***REMOVED*** Parser2.JSDocParser || (Parser2.JSDocParser ***REMOVED*** {}));
})(Parser || (Parser ***REMOVED*** {}));
var incrementallyParsedFiles ***REMOVED*** /* @__PURE__ */ new WeakSet();
function markAsIncrementallyParsed(sourceFile) {
  if (incrementallyParsedFiles.has(sourceFile)) {
    Debug.fail("Source file has already been incrementally parsed");
  }
  incrementallyParsedFiles.add(sourceFile);
}
var intersectingChangeSet ***REMOVED*** /* @__PURE__ */ new WeakSet();
function intersectsIncrementalChange(node) {
  return intersectingChangeSet.has(node);
}
function markAsIntersectingIncrementalChange(node) {
  intersectingChangeSet.add(node);
}
var IncrementalParser;
((IncrementalParser2) ***REMOVED***> {
  function updateSourceFile(sourceFile, newText, textChangeRange, aggressiveChecks) {
    aggressiveChecks ***REMOVED*** aggressiveChecks || Debug.shouldAssert(2 /* Aggressive */);
    checkChangeRange(sourceFile, newText, textChangeRange, aggressiveChecks);
    if (textChangeRangeIsUnchanged(textChangeRange)) {
      return sourceFile;
    }
    if (sourceFile.statements.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return Parser.parseSourceFile(
        sourceFile.fileName,
        newText,
        sourceFile.languageVersion,
        /*syntaxCursor*/
        void 0,
        /*setParentNodes*/
        true,
        sourceFile.scriptKind,
        sourceFile.setExternalModuleIndicator,
        sourceFile.jsDocParsingMode
      );
    }
    markAsIncrementallyParsed(sourceFile);
    Parser.fixupParentReferences(sourceFile);
    const oldText ***REMOVED*** sourceFile.text;
    const syntaxCursor ***REMOVED*** createSyntaxCursor(sourceFile);
    const changeRange ***REMOVED*** extendToAffectedRange(sourceFile, textChangeRange);
    checkChangeRange(sourceFile, newText, changeRange, aggressiveChecks);
    Debug.assert(changeRange.span.start <***REMOVED*** textChangeRange.span.start);
    Debug.assert(textSpanEnd(changeRange.span) ***REMOVED******REMOVED******REMOVED*** textSpanEnd(textChangeRange.span));
    Debug.assert(textSpanEnd(textChangeRangeNewSpan(changeRange)) ***REMOVED******REMOVED******REMOVED*** textSpanEnd(textChangeRangeNewSpan(textChangeRange)));
    const delta ***REMOVED*** textChangeRangeNewSpan(changeRange).length - changeRange.span.length;
    updateTokenPositionsAndMarkElements(sourceFile, changeRange.span.start, textSpanEnd(changeRange.span), textSpanEnd(textChangeRangeNewSpan(changeRange)), delta, oldText, newText, aggressiveChecks);
    const result ***REMOVED*** Parser.parseSourceFile(
      sourceFile.fileName,
      newText,
      sourceFile.languageVersion,
      syntaxCursor,
      /*setParentNodes*/
      true,
      sourceFile.scriptKind,
      sourceFile.setExternalModuleIndicator,
      sourceFile.jsDocParsingMode
    );
    result.commentDirectives ***REMOVED*** getNewCommentDirectives(
      sourceFile.commentDirectives,
      result.commentDirectives,
      changeRange.span.start,
      textSpanEnd(changeRange.span),
      delta,
      oldText,
      newText,
      aggressiveChecks
    );
    result.impliedNodeFormat ***REMOVED*** sourceFile.impliedNodeFormat;
    transferSourceFileChildren(sourceFile, result);
    return result;
  }
  IncrementalParser2.updateSourceFile ***REMOVED*** updateSourceFile;
  function getNewCommentDirectives(oldDirectives, newDirectives, changeStart, changeRangeOldEnd, delta, oldText, newText, aggressiveChecks) {
    if (!oldDirectives) return newDirectives;
    let commentDirectives;
    let addedNewlyScannedDirectives ***REMOVED*** false;
    for (const directive of oldDirectives) {
      const { range, type } ***REMOVED*** directive;
      if (range.end < changeStart) {
        commentDirectives ***REMOVED*** append(commentDirectives, directive);
      } else if (range.pos > changeRangeOldEnd) {
        addNewlyScannedDirectives();
        const updatedDirective ***REMOVED*** {
          range: { pos: range.pos + delta, end: range.end + delta },
          type
        };
        commentDirectives ***REMOVED*** append(commentDirectives, updatedDirective);
        if (aggressiveChecks) {
          Debug.assert(oldText.substring(range.pos, range.end) ***REMOVED******REMOVED******REMOVED*** newText.substring(updatedDirective.range.pos, updatedDirective.range.end));
        }
      }
    }
    addNewlyScannedDirectives();
    return commentDirectives;
    function addNewlyScannedDirectives() {
      if (addedNewlyScannedDirectives) return;
      addedNewlyScannedDirectives ***REMOVED*** true;
      if (!commentDirectives) {
        commentDirectives ***REMOVED*** newDirectives;
      } else if (newDirectives) {
        commentDirectives.push(...newDirectives);
      }
    }
  }
  function moveElementEntirelyPastChangeRange(element, origSourceFile, isArray2, delta, oldText, newText, aggressiveChecks) {
    if (isArray2) {
      visitArray2(element);
    } else {
      visitNode3(element);
    }
    return;
    function visitNode3(node) {
      let text ***REMOVED*** "";
      if (aggressiveChecks && shouldCheckNode(node)) {
        text ***REMOVED*** oldText.substring(node.pos, node.end);
      }
      unsetNodeChildren(node, origSourceFile);
      setTextRangePosEnd(node, node.pos + delta, node.end + delta);
      if (aggressiveChecks && shouldCheckNode(node)) {
        Debug.assert(text ***REMOVED******REMOVED******REMOVED*** newText.substring(node.pos, node.end));
      }
      forEachChild(node, visitNode3, visitArray2);
      if (hasJSDocNodes(node)) {
        for (const jsDocComment of node.jsDoc) {
          visitNode3(jsDocComment);
        }
      }
      checkNodePositions(node, aggressiveChecks);
    }
    function visitArray2(array) {
      setTextRangePosEnd(array, array.pos + delta, array.end + delta);
      for (const node of array) {
        visitNode3(node);
      }
    }
  }
  function shouldCheckNode(node) {
    switch (node.kind) {
      case 11 /* StringLiteral */:
      case 9 /* NumericLiteral */:
      case 80 /* Identifier */:
        return true;
    }
    return false;
  }
  function adjustIntersectingElement(element, changeStart, changeRangeOldEnd, changeRangeNewEnd, delta) {
    Debug.assert(element.end >***REMOVED*** changeStart, "Adjusting an element that was entirely before the change range");
    Debug.assert(element.pos <***REMOVED*** changeRangeOldEnd, "Adjusting an element that was entirely after the change range");
    Debug.assert(element.pos <***REMOVED*** element.end);
    const pos ***REMOVED*** Math.min(element.pos, changeRangeNewEnd);
    const end ***REMOVED*** element.end >***REMOVED*** changeRangeOldEnd ? (
      // Element ends after the change range.  Always adjust the end pos.
      element.end + delta
    ) : (
      // Element ends in the change range.  The element will keep its position if
      // possible. Or Move backward to the new-end if it's in the 'Y' range.
      Math.min(element.end, changeRangeNewEnd)
    );
    Debug.assert(pos <***REMOVED*** end);
    if (element.parent) {
      const parent ***REMOVED*** element.parent;
      Debug.assertGreaterThanOrEqual(pos, parent.pos);
      Debug.assertLessThanOrEqual(end, parent.end);
    }
    setTextRangePosEnd(element, pos, end);
  }
  function checkNodePositions(node, aggressiveChecks) {
    if (aggressiveChecks) {
      let pos ***REMOVED*** node.pos;
      const visitNode3 ***REMOVED*** (child) ***REMOVED***> {
        Debug.assert(child.pos >***REMOVED*** pos);
        pos ***REMOVED*** child.end;
      };
      if (hasJSDocNodes(node)) {
        for (const jsDocComment of node.jsDoc) {
          visitNode3(jsDocComment);
        }
      }
      forEachChild(node, visitNode3);
      Debug.assert(pos <***REMOVED*** node.end);
    }
  }
  function updateTokenPositionsAndMarkElements(sourceFile, changeStart, changeRangeOldEnd, changeRangeNewEnd, delta, oldText, newText, aggressiveChecks) {
    visitNode3(sourceFile);
    return;
    function visitNode3(child) {
      Debug.assert(child.pos <***REMOVED*** child.end);
      if (child.pos > changeRangeOldEnd) {
        moveElementEntirelyPastChangeRange(
          child,
          sourceFile,
          /*isArray*/
          false,
          delta,
          oldText,
          newText,
          aggressiveChecks
        );
        return;
      }
      const fullEnd ***REMOVED*** child.end;
      if (fullEnd >***REMOVED*** changeStart) {
        markAsIntersectingIncrementalChange(child);
        unsetNodeChildren(child, sourceFile);
        adjustIntersectingElement(child, changeStart, changeRangeOldEnd, changeRangeNewEnd, delta);
        forEachChild(child, visitNode3, visitArray2);
        if (hasJSDocNodes(child)) {
          for (const jsDocComment of child.jsDoc) {
            visitNode3(jsDocComment);
          }
        }
        checkNodePositions(child, aggressiveChecks);
        return;
      }
      Debug.assert(fullEnd < changeStart);
    }
    function visitArray2(array) {
      Debug.assert(array.pos <***REMOVED*** array.end);
      if (array.pos > changeRangeOldEnd) {
        moveElementEntirelyPastChangeRange(
          array,
          sourceFile,
          /*isArray*/
          true,
          delta,
          oldText,
          newText,
          aggressiveChecks
        );
        return;
      }
      const fullEnd ***REMOVED*** array.end;
      if (fullEnd >***REMOVED*** changeStart) {
        markAsIntersectingIncrementalChange(array);
        adjustIntersectingElement(array, changeStart, changeRangeOldEnd, changeRangeNewEnd, delta);
        for (const node of array) {
          visitNode3(node);
        }
        return;
      }
      Debug.assert(fullEnd < changeStart);
    }
  }
  function extendToAffectedRange(sourceFile, changeRange) {
    const maxLookahead ***REMOVED*** 1;
    let start ***REMOVED*** changeRange.span.start;
    for (let i ***REMOVED*** 0; start > 0 && i <***REMOVED*** maxLookahead; i++) {
      const nearestNode ***REMOVED*** findNearestNodeStartingBeforeOrAtPosition(sourceFile, start);
      Debug.assert(nearestNode.pos <***REMOVED*** start);
      const position ***REMOVED*** nearestNode.pos;
      start ***REMOVED*** Math.max(0, position - 1);
    }
    const finalSpan ***REMOVED*** createTextSpanFromBounds(start, textSpanEnd(changeRange.span));
    const finalLength ***REMOVED*** changeRange.newLength + (changeRange.span.start - start);
    return createTextChangeRange(finalSpan, finalLength);
  }
  function findNearestNodeStartingBeforeOrAtPosition(sourceFile, position) {
    let bestResult ***REMOVED*** sourceFile;
    let lastNodeEntirelyBeforePosition;
    forEachChild(sourceFile, visit);
    if (lastNodeEntirelyBeforePosition) {
      const lastChildOfLastEntireNodeBeforePosition ***REMOVED*** getLastDescendant(lastNodeEntirelyBeforePosition);
      if (lastChildOfLastEntireNodeBeforePosition.pos > bestResult.pos) {
        bestResult ***REMOVED*** lastChildOfLastEntireNodeBeforePosition;
      }
    }
    return bestResult;
    function getLastDescendant(node) {
      while (true) {
        const lastChild ***REMOVED*** getLastChild(node);
        if (lastChild) {
          node ***REMOVED*** lastChild;
        } else {
          return node;
        }
      }
    }
    function visit(child) {
      if (nodeIsMissing(child)) {
        return;
      }
      if (child.pos <***REMOVED*** position) {
        if (child.pos >***REMOVED*** bestResult.pos) {
          bestResult ***REMOVED*** child;
        }
        if (position < child.end) {
          forEachChild(child, visit);
          return true;
        } else {
          Debug.assert(child.end <***REMOVED*** position);
          lastNodeEntirelyBeforePosition ***REMOVED*** child;
        }
      } else {
        Debug.assert(child.pos > position);
        return true;
      }
    }
  }
  function checkChangeRange(sourceFile, newText, textChangeRange, aggressiveChecks) {
    const oldText ***REMOVED*** sourceFile.text;
    if (textChangeRange) {
      Debug.assert(oldText.length - textChangeRange.span.length + textChangeRange.newLength ***REMOVED******REMOVED******REMOVED*** newText.length);
      if (aggressiveChecks || Debug.shouldAssert(3 /* VeryAggressive */)) {
        const oldTextPrefix ***REMOVED*** oldText.substr(0, textChangeRange.span.start);
        const newTextPrefix ***REMOVED*** newText.substr(0, textChangeRange.span.start);
        Debug.assert(oldTextPrefix ***REMOVED******REMOVED******REMOVED*** newTextPrefix);
        const oldTextSuffix ***REMOVED*** oldText.substring(textSpanEnd(textChangeRange.span), oldText.length);
        const newTextSuffix ***REMOVED*** newText.substring(textSpanEnd(textChangeRangeNewSpan(textChangeRange)), newText.length);
        Debug.assert(oldTextSuffix ***REMOVED******REMOVED******REMOVED*** newTextSuffix);
      }
    }
  }
  function createSyntaxCursor(sourceFile) {
    let currentArray ***REMOVED*** sourceFile.statements;
    let currentArrayIndex ***REMOVED*** 0;
    Debug.assert(currentArrayIndex < currentArray.length);
    let current ***REMOVED*** currentArray[currentArrayIndex];
    let lastQueriedPosition ***REMOVED*** -1 /* Value */;
    return {
      currentNode(position) {
        if (position !***REMOVED******REMOVED*** lastQueriedPosition) {
          if (current && current.end ***REMOVED******REMOVED******REMOVED*** position && currentArrayIndex < currentArray.length - 1) {
            currentArrayIndex++;
            current ***REMOVED*** currentArray[currentArrayIndex];
          }
          if (!current || current.pos !***REMOVED******REMOVED*** position) {
            findHighestListElementThatStartsAtPosition(position);
          }
        }
        lastQueriedPosition ***REMOVED*** position;
        Debug.assert(!current || current.pos ***REMOVED******REMOVED******REMOVED*** position);
        return current;
      }
    };
    function findHighestListElementThatStartsAtPosition(position) {
      currentArray ***REMOVED*** void 0;
      currentArrayIndex ***REMOVED*** -1 /* Value */;
      current ***REMOVED*** void 0;
      forEachChild(sourceFile, visitNode3, visitArray2);
      return;
      function visitNode3(node) {
        if (position >***REMOVED*** node.pos && position < node.end) {
          forEachChild(node, visitNode3, visitArray2);
          return true;
        }
        return false;
      }
      function visitArray2(array) {
        if (position >***REMOVED*** array.pos && position < array.end) {
          for (let i ***REMOVED*** 0; i < array.length; i++) {
            const child ***REMOVED*** array[i];
            if (child) {
              if (child.pos ***REMOVED******REMOVED******REMOVED*** position) {
                currentArray ***REMOVED*** array;
                currentArrayIndex ***REMOVED*** i;
                current ***REMOVED*** child;
                return true;
              } else {
                if (child.pos < position && position < child.end) {
                  forEachChild(child, visitNode3, visitArray2);
                  return true;
                }
              }
            }
          }
        }
        return false;
      }
    }
  }
  IncrementalParser2.createSyntaxCursor ***REMOVED*** createSyntaxCursor;
  let InvalidPosition;
  ((InvalidPosition2) ***REMOVED***> {
    InvalidPosition2[InvalidPosition2["Value"] ***REMOVED*** -1] ***REMOVED*** "Value";
  })(InvalidPosition || (InvalidPosition ***REMOVED*** {}));
})(IncrementalParser || (IncrementalParser ***REMOVED*** {}));
function isDeclarationFileName(fileName) {
  return getDeclarationFileExtension(fileName) !***REMOVED******REMOVED*** void 0;
}
function getDeclarationFileExtension(fileName) {
  const standardExtension ***REMOVED*** getAnyExtensionFromPath(
    fileName,
    supportedDeclarationExtensions,
    /*ignoreCase*/
    false
  );
  if (standardExtension) {
    return standardExtension;
  }
  if (fileExtensionIs(fileName, ".ts" /* Ts */)) {
    const baseName ***REMOVED*** getBaseFileName(fileName);
    const index ***REMOVED*** baseName.lastIndexOf(".d.");
    if (index >***REMOVED*** 0) {
      return baseName.substring(index);
    }
  }
  return void 0;
}
function parseResolutionMode(mode, pos, end, reportDiagnostic) {
  if (!mode) {
    return void 0;
  }
  if (mode ***REMOVED******REMOVED******REMOVED*** "import") {
    return 99 /* ESNext */;
  }
  if (mode ***REMOVED******REMOVED******REMOVED*** "require") {
    return 1 /* CommonJS */;
  }
  reportDiagnostic(pos, end - pos, Diagnostics.resolution_mode_should_be_either_require_or_import);
  return void 0;
}
function processCommentPragmas(context, sourceText) {
  const pragmas ***REMOVED*** [];
  for (const range of getLeadingCommentRanges(sourceText, 0) || emptyArray) {
    const comment ***REMOVED*** sourceText.substring(range.pos, range.end);
    extractPragmas(pragmas, range, comment);
  }
  context.pragmas ***REMOVED*** /* @__PURE__ */ new Map();
  for (const pragma of pragmas) {
    if (context.pragmas.has(pragma.name)) {
      const currentValue ***REMOVED*** context.pragmas.get(pragma.name);
      if (currentValue instanceof Array) {
        currentValue.push(pragma.args);
      } else {
        context.pragmas.set(pragma.name, [currentValue, pragma.args]);
      }
      continue;
    }
    context.pragmas.set(pragma.name, pragma.args);
  }
}
function processPragmasIntoFields(context, reportDiagnostic) {
  context.checkJsDirective ***REMOVED*** void 0;
  context.referencedFiles ***REMOVED*** [];
  context.typeReferenceDirectives ***REMOVED*** [];
  context.libReferenceDirectives ***REMOVED*** [];
  context.amdDependencies ***REMOVED*** [];
  context.hasNoDefaultLib ***REMOVED*** false;
  context.pragmas.forEach((entryOrList, key) ***REMOVED***> {
    switch (key) {
      case "reference": {
        const referencedFiles ***REMOVED*** context.referencedFiles;
        const typeReferenceDirectives ***REMOVED*** context.typeReferenceDirectives;
        const libReferenceDirectives ***REMOVED*** context.libReferenceDirectives;
        forEach(toArray(entryOrList), (arg) ***REMOVED***> {
          const { types, lib, path, ["resolution-mode"]: res, preserve: _preserve } ***REMOVED*** arg.arguments;
          const preserve ***REMOVED*** _preserve ***REMOVED******REMOVED******REMOVED*** "true" ? true : void 0;
          if (arg.arguments["no-default-lib"] ***REMOVED******REMOVED******REMOVED*** "true") {
            context.hasNoDefaultLib ***REMOVED*** true;
          } else if (types) {
            const parsed ***REMOVED*** parseResolutionMode(res, types.pos, types.end, reportDiagnostic);
            typeReferenceDirectives.push({ pos: types.pos, end: types.end, fileName: types.value, ...parsed ? { resolutionMode: parsed } : {}, ...preserve ? { preserve } : {} });
          } else if (lib) {
            libReferenceDirectives.push({ pos: lib.pos, end: lib.end, fileName: lib.value, ...preserve ? { preserve } : {} });
          } else if (path) {
            referencedFiles.push({ pos: path.pos, end: path.end, fileName: path.value, ...preserve ? { preserve } : {} });
          } else {
            reportDiagnostic(arg.range.pos, arg.range.end - arg.range.pos, Diagnostics.Invalid_reference_directive_syntax);
          }
        });
        break;
      }
      case "amd-dependency": {
        context.amdDependencies ***REMOVED*** map(
          toArray(entryOrList),
          (x) ***REMOVED***> ({ name: x.arguments.name, path: x.arguments.path })
        );
        break;
      }
      case "amd-module": {
        if (entryOrList instanceof Array) {
          for (const entry of entryOrList) {
            if (context.moduleName) {
              reportDiagnostic(entry.range.pos, entry.range.end - entry.range.pos, Diagnostics.An_AMD_module_cannot_have_multiple_name_assignments);
            }
            context.moduleName ***REMOVED*** entry.arguments.name;
          }
        } else {
          context.moduleName ***REMOVED*** entryOrList.arguments.name;
        }
        break;
      }
      case "ts-nocheck":
      case "ts-check": {
        forEach(toArray(entryOrList), (entry) ***REMOVED***> {
          if (!context.checkJsDirective || entry.range.pos > context.checkJsDirective.pos) {
            context.checkJsDirective ***REMOVED*** {
              enabled: key ***REMOVED******REMOVED******REMOVED*** "ts-check",
              end: entry.range.end,
              pos: entry.range.pos
            };
          }
        });
        break;
      }
      case "jsx":
      case "jsxfrag":
      case "jsximportsource":
      case "jsxruntime":
        return;
      // Accessed directly
      default:
        Debug.fail("Unhandled pragma kind");
    }
  });
}
var namedArgRegExCache ***REMOVED*** /* @__PURE__ */ new Map();
function getNamedArgRegEx(name) {
  if (namedArgRegExCache.has(name)) {
    return namedArgRegExCache.get(name);
  }
  const result ***REMOVED*** new RegExp(`(\\s${name}\\s****REMOVED***\\s*)(?:(?:'([^']*)')|(?:"([^"]*)"))`, "im");
  namedArgRegExCache.set(name, result);
  return result;
}
var tripleSlashXMLCommentStartRegEx ***REMOVED*** /^\/\/\/\s*<(\S+)\s.*?\/>/m;
var singleLinePragmaRegEx ***REMOVED*** /^\/\/\/?\s*@([^\s:]+)((?:[^\S\r\n]|:).*)?$/m;
function extractPragmas(pragmas, range, text) {
  const tripleSlash ***REMOVED*** range.kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */ && tripleSlashXMLCommentStartRegEx.exec(text);
  if (tripleSlash) {
    const name ***REMOVED*** tripleSlash[1].toLowerCase();
    const pragma ***REMOVED*** commentPragmas[name];
    if (!pragma || !(pragma.kind & 1 /* TripleSlashXML */)) {
      return;
    }
    if (pragma.args) {
      const argument ***REMOVED*** {};
      for (const arg of pragma.args) {
        const matcher ***REMOVED*** getNamedArgRegEx(arg.name);
        const matchResult ***REMOVED*** matcher.exec(text);
        if (!matchResult && !arg.optional) {
          return;
        } else if (matchResult) {
          const value ***REMOVED*** matchResult[2] || matchResult[3];
          if (arg.captureSpan) {
            const startPos ***REMOVED*** range.pos + matchResult.index + matchResult[1].length + 1;
            argument[arg.name] ***REMOVED*** {
              value,
              pos: startPos,
              end: startPos + value.length
            };
          } else {
            argument[arg.name] ***REMOVED*** value;
          }
        }
      }
      pragmas.push({ name, args: { arguments: argument, range } });
    } else {
      pragmas.push({ name, args: { arguments: {}, range } });
    }
    return;
  }
  const singleLine ***REMOVED*** range.kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */ && singleLinePragmaRegEx.exec(text);
  if (singleLine) {
    return addPragmaForMatch(pragmas, range, 2 /* SingleLine */, singleLine);
  }
  if (range.kind ***REMOVED******REMOVED******REMOVED*** 3 /* MultiLineCommentTrivia */) {
    const multiLinePragmaRegEx ***REMOVED*** /@(\S+)(\s+(?:\S.*)?)?$/gm;
    let multiLineMatch;
    while (multiLineMatch ***REMOVED*** multiLinePragmaRegEx.exec(text)) {
      addPragmaForMatch(pragmas, range, 4 /* MultiLine */, multiLineMatch);
    }
  }
}
function addPragmaForMatch(pragmas, range, kind, match) {
  if (!match) return;
  const name ***REMOVED*** match[1].toLowerCase();
  const pragma ***REMOVED*** commentPragmas[name];
  if (!pragma || !(pragma.kind & kind)) {
    return;
  }
  const args ***REMOVED*** match[2];
  const argument ***REMOVED*** getNamedPragmaArguments(pragma, args);
  if (argument ***REMOVED******REMOVED******REMOVED*** "fail") return;
  pragmas.push({ name, args: { arguments: argument, range } });
  return;
}
function getNamedPragmaArguments(pragma, text) {
  if (!text) return {};
  if (!pragma.args) return {};
  const args ***REMOVED*** text.trim().split(/\s+/);
  const argMap ***REMOVED*** {};
  for (let i ***REMOVED*** 0; i < pragma.args.length; i++) {
    const argument ***REMOVED*** pragma.args[i];
    if (!args[i] && !argument.optional) {
      return "fail";
    }
    if (argument.captureSpan) {
      return Debug.fail("Capture spans not yet implemented for non-xml pragmas");
    }
    argMap[argument.name] ***REMOVED*** args[i];
  }
  return argMap;
}
function tagNamesAreEquivalent(lhs, rhs) {
  if (lhs.kind !***REMOVED******REMOVED*** rhs.kind) {
    return false;
  }
  if (lhs.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
    return lhs.escapedText ***REMOVED******REMOVED******REMOVED*** rhs.escapedText;
  }
  if (lhs.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
    return true;
  }
  if (lhs.kind ***REMOVED******REMOVED******REMOVED*** 296 /* JsxNamespacedName */) {
    return lhs.namespace.escapedText ***REMOVED******REMOVED******REMOVED*** rhs.namespace.escapedText && lhs.name.escapedText ***REMOVED******REMOVED******REMOVED*** rhs.name.escapedText;
  }
  return lhs.name.escapedText ***REMOVED******REMOVED******REMOVED*** rhs.name.escapedText && tagNamesAreEquivalent(lhs.expression, rhs.expression);
}

// src/compiler/commandLineParser.ts
var compileOnSaveCommandLineOption ***REMOVED*** {
  name: "compileOnSave",
  type: "boolean",
  defaultValueDescription: false
};
var jsxOptionMap ***REMOVED*** new Map(Object.entries({
  "preserve": 1 /* Preserve */,
  "react-native": 3 /* ReactNative */,
  "react-jsx": 4 /* ReactJSX */,
  "react-jsxdev": 5 /* ReactJSXDev */,
  "react": 2 /* React */
}));
var inverseJsxOptionMap ***REMOVED*** new Map(mapIterator(jsxOptionMap.entries(), ([key, value]) ***REMOVED***> ["" + value, key]));
var libEntries ***REMOVED*** [
  // JavaScript only
  ["es5", "lib.es5.d.ts"],
  ["es6", "lib.es2015.d.ts"],
  ["es2015", "lib.es2015.d.ts"],
  ["es7", "lib.es2016.d.ts"],
  ["es2016", "lib.es2016.d.ts"],
  ["es2017", "lib.es2017.d.ts"],
  ["es2018", "lib.es2018.d.ts"],
  ["es2019", "lib.es2019.d.ts"],
  ["es2020", "lib.es2020.d.ts"],
  ["es2021", "lib.es2021.d.ts"],
  ["es2022", "lib.es2022.d.ts"],
  ["es2023", "lib.es2023.d.ts"],
  ["es2024", "lib.es2024.d.ts"],
  ["esnext", "lib.esnext.d.ts"],
  // Host only
  ["dom", "lib.dom.d.ts"],
  ["dom.iterable", "lib.dom.iterable.d.ts"],
  ["dom.asynciterable", "lib.dom.asynciterable.d.ts"],
  ["webworker", "lib.webworker.d.ts"],
  ["webworker.importscripts", "lib.webworker.importscripts.d.ts"],
  ["webworker.iterable", "lib.webworker.iterable.d.ts"],
  ["webworker.asynciterable", "lib.webworker.asynciterable.d.ts"],
  ["scripthost", "lib.scripthost.d.ts"],
  // ES2015 Or ESNext By-feature options
  ["es2015.core", "lib.es2015.core.d.ts"],
  ["es2015.collection", "lib.es2015.collection.d.ts"],
  ["es2015.generator", "lib.es2015.generator.d.ts"],
  ["es2015.iterable", "lib.es2015.iterable.d.ts"],
  ["es2015.promise", "lib.es2015.promise.d.ts"],
  ["es2015.proxy", "lib.es2015.proxy.d.ts"],
  ["es2015.reflect", "lib.es2015.reflect.d.ts"],
  ["es2015.symbol", "lib.es2015.symbol.d.ts"],
  ["es2015.symbol.wellknown", "lib.es2015.symbol.wellknown.d.ts"],
  ["es2016.array.include", "lib.es2016.array.include.d.ts"],
  ["es2016.intl", "lib.es2016.intl.d.ts"],
  ["es2017.arraybuffer", "lib.es2017.arraybuffer.d.ts"],
  ["es2017.date", "lib.es2017.date.d.ts"],
  ["es2017.object", "lib.es2017.object.d.ts"],
  ["es2017.sharedmemory", "lib.es2017.sharedmemory.d.ts"],
  ["es2017.string", "lib.es2017.string.d.ts"],
  ["es2017.intl", "lib.es2017.intl.d.ts"],
  ["es2017.typedarrays", "lib.es2017.typedarrays.d.ts"],
  ["es2018.asyncgenerator", "lib.es2018.asyncgenerator.d.ts"],
  ["es2018.asynciterable", "lib.es2018.asynciterable.d.ts"],
  ["es2018.intl", "lib.es2018.intl.d.ts"],
  ["es2018.promise", "lib.es2018.promise.d.ts"],
  ["es2018.regexp", "lib.es2018.regexp.d.ts"],
  ["es2019.array", "lib.es2019.array.d.ts"],
  ["es2019.object", "lib.es2019.object.d.ts"],
  ["es2019.string", "lib.es2019.string.d.ts"],
  ["es2019.symbol", "lib.es2019.symbol.d.ts"],
  ["es2019.intl", "lib.es2019.intl.d.ts"],
  ["es2020.bigint", "lib.es2020.bigint.d.ts"],
  ["es2020.date", "lib.es2020.date.d.ts"],
  ["es2020.promise", "lib.es2020.promise.d.ts"],
  ["es2020.sharedmemory", "lib.es2020.sharedmemory.d.ts"],
  ["es2020.string", "lib.es2020.string.d.ts"],
  ["es2020.symbol.wellknown", "lib.es2020.symbol.wellknown.d.ts"],
  ["es2020.intl", "lib.es2020.intl.d.ts"],
  ["es2020.number", "lib.es2020.number.d.ts"],
  ["es2021.promise", "lib.es2021.promise.d.ts"],
  ["es2021.string", "lib.es2021.string.d.ts"],
  ["es2021.weakref", "lib.es2021.weakref.d.ts"],
  ["es2021.intl", "lib.es2021.intl.d.ts"],
  ["es2022.array", "lib.es2022.array.d.ts"],
  ["es2022.error", "lib.es2022.error.d.ts"],
  ["es2022.intl", "lib.es2022.intl.d.ts"],
  ["es2022.object", "lib.es2022.object.d.ts"],
  ["es2022.string", "lib.es2022.string.d.ts"],
  ["es2022.regexp", "lib.es2022.regexp.d.ts"],
  ["es2023.array", "lib.es2023.array.d.ts"],
  ["es2023.collection", "lib.es2023.collection.d.ts"],
  ["es2023.intl", "lib.es2023.intl.d.ts"],
  ["es2024.arraybuffer", "lib.es2024.arraybuffer.d.ts"],
  ["es2024.collection", "lib.es2024.collection.d.ts"],
  ["es2024.object", "lib.es2024.object.d.ts"],
  ["es2024.promise", "lib.es2024.promise.d.ts"],
  ["es2024.regexp", "lib.es2024.regexp.d.ts"],
  ["es2024.sharedmemory", "lib.es2024.sharedmemory.d.ts"],
  ["es2024.string", "lib.es2024.string.d.ts"],
  ["esnext.array", "lib.es2023.array.d.ts"],
  ["esnext.collection", "lib.esnext.collection.d.ts"],
  ["esnext.symbol", "lib.es2019.symbol.d.ts"],
  ["esnext.asynciterable", "lib.es2018.asynciterable.d.ts"],
  ["esnext.intl", "lib.esnext.intl.d.ts"],
  ["esnext.disposable", "lib.esnext.disposable.d.ts"],
  ["esnext.bigint", "lib.es2020.bigint.d.ts"],
  ["esnext.string", "lib.es2022.string.d.ts"],
  ["esnext.promise", "lib.es2024.promise.d.ts"],
  ["esnext.weakref", "lib.es2021.weakref.d.ts"],
  ["esnext.decorators", "lib.esnext.decorators.d.ts"],
  ["esnext.object", "lib.es2024.object.d.ts"],
  ["esnext.array", "lib.esnext.array.d.ts"],
  ["esnext.regexp", "lib.es2024.regexp.d.ts"],
  ["esnext.string", "lib.es2024.string.d.ts"],
  ["esnext.iterator", "lib.esnext.iterator.d.ts"],
  ["esnext.promise", "lib.esnext.promise.d.ts"],
  ["esnext.float16", "lib.esnext.float16.d.ts"],
  ["esnext.error", "lib.esnext.error.d.ts"],
  ["esnext.sharedmemory", "lib.esnext.sharedmemory.d.ts"],
  ["decorators", "lib.decorators.d.ts"],
  ["decorators.legacy", "lib.decorators.legacy.d.ts"]
];
var libs ***REMOVED*** libEntries.map((entry) ***REMOVED***> entry[0]);
var libMap ***REMOVED*** new Map(libEntries);
var optionsForWatch ***REMOVED*** [
  {
    name: "watchFile",
    type: new Map(Object.entries({
      fixedpollinginterval: 0 /* FixedPollingInterval */,
      prioritypollinginterval: 1 /* PriorityPollingInterval */,
      dynamicprioritypolling: 2 /* DynamicPriorityPolling */,
      fixedchunksizepolling: 3 /* FixedChunkSizePolling */,
      usefsevents: 4 /* UseFsEvents */,
      usefseventsonparentdirectory: 5 /* UseFsEventsOnParentDirectory */
    })),
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Specify_how_the_TypeScript_watch_mode_works,
    defaultValueDescription: 4 /* UseFsEvents */
  },
  {
    name: "watchDirectory",
    type: new Map(Object.entries({
      usefsevents: 0 /* UseFsEvents */,
      fixedpollinginterval: 1 /* FixedPollingInterval */,
      dynamicprioritypolling: 2 /* DynamicPriorityPolling */,
      fixedchunksizepolling: 3 /* FixedChunkSizePolling */
    })),
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Specify_how_directories_are_watched_on_systems_that_lack_recursive_file_watching_functionality,
    defaultValueDescription: 0 /* UseFsEvents */
  },
  {
    name: "fallbackPolling",
    type: new Map(Object.entries({
      fixedinterval: 0 /* FixedInterval */,
      priorityinterval: 1 /* PriorityInterval */,
      dynamicpriority: 2 /* DynamicPriority */,
      fixedchunksize: 3 /* FixedChunkSize */
    })),
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Specify_what_approach_the_watcher_should_use_if_the_system_runs_out_of_native_file_watchers,
    defaultValueDescription: 1 /* PriorityInterval */
  },
  {
    name: "synchronousWatchDirectory",
    type: "boolean",
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Synchronously_call_callbacks_and_update_the_state_of_directory_watchers_on_platforms_that_don_t_support_recursive_watching_natively,
    defaultValueDescription: false
  },
  {
    name: "excludeDirectories",
    type: "list",
    element: {
      name: "excludeDirectory",
      type: "string",
      isFilePath: true,
      extraValidation: specToDiagnostic
    },
    allowConfigDirTemplateSubstitution: true,
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Remove_a_list_of_directories_from_the_watch_process
  },
  {
    name: "excludeFiles",
    type: "list",
    element: {
      name: "excludeFile",
      type: "string",
      isFilePath: true,
      extraValidation: specToDiagnostic
    },
    allowConfigDirTemplateSubstitution: true,
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Remove_a_list_of_files_from_the_watch_mode_s_processing
  }
];
var commonOptionsWithBuild ***REMOVED*** [
  {
    name: "help",
    shortName: "h",
    type: "boolean",
    showInSimplifiedHelpView: true,
    isCommandLineOnly: true,
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Print_this_message,
    defaultValueDescription: false
  },
  {
    name: "help",
    shortName: "?",
    type: "boolean",
    isCommandLineOnly: true,
    category: Diagnostics.Command_line_Options,
    defaultValueDescription: false
  },
  {
    name: "watch",
    shortName: "w",
    type: "boolean",
    showInSimplifiedHelpView: true,
    isCommandLineOnly: true,
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Watch_input_files,
    defaultValueDescription: false
  },
  {
    name: "preserveWatchOutput",
    type: "boolean",
    showInSimplifiedHelpView: false,
    category: Diagnostics.Output_Formatting,
    description: Diagnostics.Disable_wiping_the_console_in_watch_mode,
    defaultValueDescription: false
  },
  {
    name: "listFiles",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Print_all_of_the_files_read_during_the_compilation,
    defaultValueDescription: false
  },
  {
    name: "explainFiles",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Print_files_read_during_the_compilation_including_why_it_was_included,
    defaultValueDescription: false
  },
  {
    name: "listEmittedFiles",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Print_the_names_of_emitted_files_after_a_compilation,
    defaultValueDescription: false
  },
  {
    name: "pretty",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Output_Formatting,
    description: Diagnostics.Enable_color_and_formatting_in_TypeScript_s_output_to_make_compiler_errors_easier_to_read,
    defaultValueDescription: true
  },
  {
    name: "traceResolution",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Log_paths_used_during_the_moduleResolution_process,
    defaultValueDescription: false
  },
  {
    name: "diagnostics",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Output_compiler_performance_information_after_building,
    defaultValueDescription: false
  },
  {
    name: "extendedDiagnostics",
    type: "boolean",
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Output_more_detailed_compiler_performance_information_after_building,
    defaultValueDescription: false
  },
  {
    name: "generateCpuProfile",
    type: "string",
    isFilePath: true,
    paramType: Diagnostics.FILE_OR_DIRECTORY,
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Emit_a_v8_CPU_profile_of_the_compiler_run_for_debugging,
    defaultValueDescription: "profile.cpuprofile"
  },
  {
    name: "generateTrace",
    type: "string",
    isFilePath: true,
    paramType: Diagnostics.DIRECTORY,
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Generates_an_event_trace_and_a_list_of_types
  },
  {
    name: "incremental",
    shortName: "i",
    type: "boolean",
    category: Diagnostics.Projects,
    description: Diagnostics.Save_tsbuildinfo_files_to_allow_for_incremental_compilation_of_projects,
    transpileOptionValue: void 0,
    defaultValueDescription: Diagnostics.false_unless_composite_is_set
  },
  {
    name: "declaration",
    shortName: "d",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    transpileOptionValue: void 0,
    description: Diagnostics.Generate_d_ts_files_from_TypeScript_and_JavaScript_files_in_your_project,
    defaultValueDescription: Diagnostics.false_unless_composite_is_set
  },
  {
    name: "declarationMap",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    defaultValueDescription: false,
    description: Diagnostics.Create_sourcemaps_for_d_ts_files
  },
  {
    name: "emitDeclarationOnly",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Only_output_d_ts_files_and_not_JavaScript_files,
    transpileOptionValue: void 0,
    defaultValueDescription: false
  },
  {
    name: "sourceMap",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    defaultValueDescription: false,
    description: Diagnostics.Create_source_map_files_for_emitted_JavaScript_files
  },
  {
    name: "inlineSourceMap",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Include_sourcemap_files_inside_the_emitted_JavaScript,
    defaultValueDescription: false
  },
  {
    name: "noCheck",
    type: "boolean",
    showInSimplifiedHelpView: false,
    category: Diagnostics.Compiler_Diagnostics,
    description: Diagnostics.Disable_full_type_checking_only_critical_parse_and_emit_errors_will_be_reported,
    transpileOptionValue: true,
    defaultValueDescription: false
    // Not setting affectsSemanticDiagnostics or affectsBuildInfo because we dont want all diagnostics to go away, its handled in builder
  },
  {
    name: "noEmit",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Disable_emitting_files_from_a_compilation,
    transpileOptionValue: void 0,
    defaultValueDescription: false
  },
  {
    name: "assumeChangesOnlyAffectDirectDependencies",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Watch_and_Build_Modes,
    description: Diagnostics.Have_recompiles_in_projects_that_use_incremental_and_watch_mode_assume_that_changes_within_a_file_will_only_affect_files_directly_depending_on_it,
    defaultValueDescription: false
  },
  {
    name: "locale",
    type: "string",
    category: Diagnostics.Command_line_Options,
    isCommandLineOnly: true,
    description: Diagnostics.Set_the_language_of_the_messaging_from_TypeScript_This_does_not_affect_emit,
    defaultValueDescription: Diagnostics.Platform_specific
  }
];
var targetOptionDeclaration ***REMOVED*** {
  name: "target",
  shortName: "t",
  type: new Map(Object.entries({
    es3: 0 /* ES3 */,
    es5: 1 /* ES5 */,
    es6: 2 /* ES2015 */,
    es2015: 2 /* ES2015 */,
    es2016: 3 /* ES2016 */,
    es2017: 4 /* ES2017 */,
    es2018: 5 /* ES2018 */,
    es2019: 6 /* ES2019 */,
    es2020: 7 /* ES2020 */,
    es2021: 8 /* ES2021 */,
    es2022: 9 /* ES2022 */,
    es2023: 10 /* ES2023 */,
    es2024: 11 /* ES2024 */,
    esnext: 99 /* ESNext */
  })),
  affectsSourceFile: true,
  affectsModuleResolution: true,
  affectsEmit: true,
  affectsBuildInfo: true,
  deprecatedKeys: /* @__PURE__ */ new Set(["es3"]),
  paramType: Diagnostics.VERSION,
  showInSimplifiedHelpView: true,
  category: Diagnostics.Language_and_Environment,
  description: Diagnostics.Set_the_JavaScript_language_version_for_emitted_JavaScript_and_include_compatible_library_declarations,
  defaultValueDescription: 1 /* ES5 */
};
var moduleOptionDeclaration ***REMOVED*** {
  name: "module",
  shortName: "m",
  type: new Map(Object.entries({
    none: 0 /* None */,
    commonjs: 1 /* CommonJS */,
    amd: 2 /* AMD */,
    system: 4 /* System */,
    umd: 3 /* UMD */,
    es6: 5 /* ES2015 */,
    es2015: 5 /* ES2015 */,
    es2020: 6 /* ES2020 */,
    es2022: 7 /* ES2022 */,
    esnext: 99 /* ESNext */,
    node16: 100 /* Node16 */,
    node18: 101 /* Node18 */,
    node20: 102 /* Node20 */,
    nodenext: 199 /* NodeNext */,
    preserve: 200 /* Preserve */
  })),
  affectsSourceFile: true,
  affectsModuleResolution: true,
  affectsEmit: true,
  affectsBuildInfo: true,
  paramType: Diagnostics.KIND,
  showInSimplifiedHelpView: true,
  category: Diagnostics.Modules,
  description: Diagnostics.Specify_what_module_code_is_generated,
  defaultValueDescription: void 0
};
var commandOptionsWithoutBuild ***REMOVED*** [
  // CommandLine only options
  {
    name: "all",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Show_all_compiler_options,
    defaultValueDescription: false
  },
  {
    name: "version",
    shortName: "v",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Print_the_compiler_s_version,
    defaultValueDescription: false
  },
  {
    name: "init",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Initializes_a_TypeScript_project_and_creates_a_tsconfig_json_file,
    defaultValueDescription: false
  },
  {
    name: "project",
    shortName: "p",
    type: "string",
    isFilePath: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Command_line_Options,
    paramType: Diagnostics.FILE_OR_DIRECTORY,
    description: Diagnostics.Compile_the_project_given_the_path_to_its_configuration_file_or_to_a_folder_with_a_tsconfig_json
  },
  {
    name: "showConfig",
    type: "boolean",
    showInSimplifiedHelpView: true,
    category: Diagnostics.Command_line_Options,
    isCommandLineOnly: true,
    description: Diagnostics.Print_the_final_configuration_instead_of_building,
    defaultValueDescription: false
  },
  {
    name: "listFilesOnly",
    type: "boolean",
    category: Diagnostics.Command_line_Options,
    isCommandLineOnly: true,
    description: Diagnostics.Print_names_of_files_that_are_part_of_the_compilation_and_then_stop_processing,
    defaultValueDescription: false
  },
  // Basic
  targetOptionDeclaration,
  moduleOptionDeclaration,
  {
    name: "lib",
    type: "list",
    element: {
      name: "lib",
      type: libMap,
      defaultValueDescription: void 0
    },
    affectsProgramStructure: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_a_set_of_bundled_library_declaration_files_that_describe_the_target_runtime_environment,
    transpileOptionValue: void 0
  },
  {
    name: "allowJs",
    type: "boolean",
    allowJsFlag: true,
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.JavaScript_Support,
    description: Diagnostics.Allow_JavaScript_files_to_be_a_part_of_your_program_Use_the_checkJs_option_to_get_errors_from_these_files,
    defaultValueDescription: false
  },
  {
    name: "checkJs",
    type: "boolean",
    affectsModuleResolution: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.JavaScript_Support,
    description: Diagnostics.Enable_error_reporting_in_type_checked_JavaScript_files,
    defaultValueDescription: false
  },
  {
    name: "jsx",
    type: jsxOptionMap,
    affectsSourceFile: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsModuleResolution: true,
    // The checker emits an error when it sees JSX but this option is not set in compilerOptions.
    // This is effectively a semantic error, so mark this option as affecting semantic diagnostics
    // so we know to refresh errors when this option is changed.
    affectsSemanticDiagnostics: true,
    paramType: Diagnostics.KIND,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_what_JSX_code_is_generated,
    defaultValueDescription: void 0
  },
  {
    name: "outFile",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsDeclarationPath: true,
    isFilePath: true,
    paramType: Diagnostics.FILE,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Specify_a_file_that_bundles_all_outputs_into_one_JavaScript_file_If_declaration_is_true_also_designates_a_file_that_bundles_all_d_ts_output,
    transpileOptionValue: void 0
  },
  {
    name: "outDir",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsDeclarationPath: true,
    isFilePath: true,
    paramType: Diagnostics.DIRECTORY,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Specify_an_output_folder_for_all_emitted_files
  },
  {
    name: "rootDir",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsDeclarationPath: true,
    isFilePath: true,
    paramType: Diagnostics.LOCATION,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_the_root_folder_within_your_source_files,
    defaultValueDescription: Diagnostics.Computed_from_the_list_of_input_files
  },
  {
    name: "composite",
    type: "boolean",
    // Not setting affectsEmit because we calculate this flag might not affect full emit
    affectsBuildInfo: true,
    isTSConfigOnly: true,
    category: Diagnostics.Projects,
    transpileOptionValue: void 0,
    defaultValueDescription: false,
    description: Diagnostics.Enable_constraints_that_allow_a_TypeScript_project_to_be_used_with_project_references
  },
  {
    name: "tsBuildInfoFile",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    isFilePath: true,
    paramType: Diagnostics.FILE,
    category: Diagnostics.Projects,
    transpileOptionValue: void 0,
    defaultValueDescription: ".tsbuildinfo",
    description: Diagnostics.Specify_the_path_to_tsbuildinfo_incremental_compilation_file
  },
  {
    name: "removeComments",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Emit,
    defaultValueDescription: false,
    description: Diagnostics.Disable_emitting_comments
  },
  {
    name: "importHelpers",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsSourceFile: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Allow_importing_helper_functions_from_tslib_once_per_project_instead_of_including_them_per_file,
    defaultValueDescription: false
  },
  {
    name: "importsNotUsedAsValues",
    type: new Map(Object.entries({
      remove: 0 /* Remove */,
      preserve: 1 /* Preserve */,
      error: 2 /* Error */
    })),
    affectsEmit: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Specify_emit_Slashchecking_behavior_for_imports_that_are_only_used_for_types,
    defaultValueDescription: 0 /* Remove */
  },
  {
    name: "downlevelIteration",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Emit_more_compliant_but_verbose_and_less_performant_JavaScript_for_iteration,
    defaultValueDescription: false
  },
  {
    name: "isolatedModules",
    type: "boolean",
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Ensure_that_each_file_can_be_safely_transpiled_without_relying_on_other_imports,
    transpileOptionValue: true,
    defaultValueDescription: false
  },
  {
    name: "verbatimModuleSyntax",
    type: "boolean",
    affectsEmit: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Do_not_transform_or_elide_any_imports_or_exports_not_marked_as_type_only_ensuring_they_are_written_in_the_output_file_s_format_based_on_the_module_setting,
    defaultValueDescription: false
  },
  {
    name: "isolatedDeclarations",
    type: "boolean",
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Require_sufficient_annotation_on_exports_so_other_tools_can_trivially_generate_declaration_files,
    defaultValueDescription: false,
    affectsBuildInfo: true,
    affectsSemanticDiagnostics: true
  },
  {
    name: "erasableSyntaxOnly",
    type: "boolean",
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Do_not_allow_runtime_constructs_that_are_not_part_of_ECMAScript,
    defaultValueDescription: false,
    affectsBuildInfo: true,
    affectsSemanticDiagnostics: true
  },
  {
    name: "libReplacement",
    type: "boolean",
    affectsProgramStructure: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Enable_lib_replacement,
    defaultValueDescription: true
  },
  // Strict Type Checks
  {
    name: "strict",
    type: "boolean",
    // Though this affects semantic diagnostics, affectsSemanticDiagnostics is not set here
    // The value of each strictFlag depends on own strictFlag value or this and never accessed directly.
    // But we need to store `strict` in builf info, even though it won't be examined directly, so that the
    // flags it controls (e.g. `strictNullChecks`) will be retrieved correctly
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_all_strict_type_checking_options,
    defaultValueDescription: false
  },
  {
    name: "noImplicitAny",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_error_reporting_for_expressions_and_declarations_with_an_implied_any_type,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "strictNullChecks",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.When_type_checking_take_into_account_null_and_undefined,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "strictFunctionTypes",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.When_assigning_functions_check_to_ensure_parameters_and_the_return_values_are_subtype_compatible,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "strictBindCallApply",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Check_that_the_arguments_for_bind_call_and_apply_methods_match_the_original_function,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "strictPropertyInitialization",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Check_for_class_properties_that_are_declared_but_not_set_in_the_constructor,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "strictBuiltinIteratorReturn",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Built_in_iterators_are_instantiated_with_a_TReturn_type_of_undefined_instead_of_any,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "noImplicitThis",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_error_reporting_when_this_is_given_the_type_any,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "useUnknownInCatchVariables",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Default_catch_clause_variables_as_unknown_instead_of_any,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  {
    name: "alwaysStrict",
    type: "boolean",
    affectsSourceFile: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    strictFlag: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Ensure_use_strict_is_always_emitted,
    defaultValueDescription: Diagnostics.false_unless_strict_is_set
  },
  // Additional Checks
  {
    name: "noUnusedLocals",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_error_reporting_when_local_variables_aren_t_read,
    defaultValueDescription: false
  },
  {
    name: "noUnusedParameters",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Raise_an_error_when_a_function_parameter_isn_t_read,
    defaultValueDescription: false
  },
  {
    name: "exactOptionalPropertyTypes",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Interpret_optional_property_types_as_written_rather_than_adding_undefined,
    defaultValueDescription: false
  },
  {
    name: "noImplicitReturns",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_error_reporting_for_codepaths_that_do_not_explicitly_return_in_a_function,
    defaultValueDescription: false
  },
  {
    name: "noFallthroughCasesInSwitch",
    type: "boolean",
    affectsBindDiagnostics: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enable_error_reporting_for_fallthrough_cases_in_switch_statements,
    defaultValueDescription: false
  },
  {
    name: "noUncheckedIndexedAccess",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Add_undefined_to_a_type_when_accessed_using_an_index,
    defaultValueDescription: false
  },
  {
    name: "noImplicitOverride",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Ensure_overriding_members_in_derived_classes_are_marked_with_an_override_modifier,
    defaultValueDescription: false
  },
  {
    name: "noPropertyAccessFromIndexSignature",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    showInSimplifiedHelpView: false,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Enforces_using_indexed_accessors_for_keys_declared_using_an_indexed_type,
    defaultValueDescription: false
  },
  // Module Resolution
  {
    name: "moduleResolution",
    type: new Map(Object.entries({
      // N.B. The first entry specifies the value shown in `tsc --init`
      node10: 2 /* Node10 */,
      node: 2 /* Node10 */,
      classic: 1 /* Classic */,
      node16: 3 /* Node16 */,
      nodenext: 99 /* NodeNext */,
      bundler: 100 /* Bundler */
    })),
    deprecatedKeys: /* @__PURE__ */ new Set(["node"]),
    affectsSourceFile: true,
    affectsModuleResolution: true,
    paramType: Diagnostics.STRATEGY,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_how_TypeScript_looks_up_a_file_from_a_given_module_specifier,
    defaultValueDescription: Diagnostics.module_AMD_or_UMD_or_System_or_ES6_then_Classic_Otherwise_Node
  },
  {
    name: "baseUrl",
    type: "string",
    affectsModuleResolution: true,
    isFilePath: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_the_base_directory_to_resolve_non_relative_module_names
  },
  {
    // this option can only be specified in tsconfig.json
    // use type ***REMOVED*** object to copy the value as-is
    name: "paths",
    type: "object",
    affectsModuleResolution: true,
    allowConfigDirTemplateSubstitution: true,
    isTSConfigOnly: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_a_set_of_entries_that_re_map_imports_to_additional_lookup_locations,
    transpileOptionValue: void 0
  },
  {
    // this option can only be specified in tsconfig.json
    // use type ***REMOVED*** object to copy the value as-is
    name: "rootDirs",
    type: "list",
    isTSConfigOnly: true,
    element: {
      name: "rootDirs",
      type: "string",
      isFilePath: true
    },
    affectsModuleResolution: true,
    allowConfigDirTemplateSubstitution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Allow_multiple_folders_to_be_treated_as_one_when_resolving_modules,
    transpileOptionValue: void 0,
    defaultValueDescription: Diagnostics.Computed_from_the_list_of_input_files
  },
  {
    name: "typeRoots",
    type: "list",
    element: {
      name: "typeRoots",
      type: "string",
      isFilePath: true
    },
    affectsModuleResolution: true,
    allowConfigDirTemplateSubstitution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_multiple_folders_that_act_like_Slashnode_modules_Slash_types
  },
  {
    name: "types",
    type: "list",
    element: {
      name: "types",
      type: "string"
    },
    affectsProgramStructure: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Specify_type_package_names_to_be_included_without_being_referenced_in_a_source_file,
    transpileOptionValue: void 0
  },
  {
    name: "allowSyntheticDefaultImports",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Allow_import_x_from_y_when_a_module_doesn_t_have_a_default_export,
    defaultValueDescription: Diagnostics.module_system_or_esModuleInterop
  },
  {
    name: "esModuleInterop",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    showInSimplifiedHelpView: true,
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Emit_additional_JavaScript_to_ease_support_for_importing_CommonJS_modules_This_enables_allowSyntheticDefaultImports_for_type_compatibility,
    defaultValueDescription: false
  },
  {
    name: "preserveSymlinks",
    type: "boolean",
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Disable_resolving_symlinks_to_their_realpath_This_correlates_to_the_same_flag_in_node,
    defaultValueDescription: false
  },
  {
    name: "allowUmdGlobalAccess",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Allow_accessing_UMD_globals_from_modules,
    defaultValueDescription: false
  },
  {
    name: "moduleSuffixes",
    type: "list",
    element: {
      name: "suffix",
      type: "string"
    },
    listPreserveFalsyValues: true,
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.List_of_file_name_suffixes_to_search_when_resolving_a_module
  },
  {
    name: "allowImportingTsExtensions",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Allow_imports_to_include_TypeScript_file_extensions_Requires_moduleResolution_bundler_and_either_noEmit_or_emitDeclarationOnly_to_be_set,
    defaultValueDescription: false,
    transpileOptionValue: void 0
  },
  {
    name: "rewriteRelativeImportExtensions",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Rewrite_ts_tsx_mts_and_cts_file_extensions_in_relative_import_paths_to_their_JavaScript_equivalent_in_output_files,
    defaultValueDescription: false
  },
  {
    name: "resolvePackageJsonExports",
    type: "boolean",
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Use_the_package_json_exports_field_when_resolving_package_imports,
    defaultValueDescription: Diagnostics.true_when_moduleResolution_is_node16_nodenext_or_bundler_otherwise_false
  },
  {
    name: "resolvePackageJsonImports",
    type: "boolean",
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Use_the_package_json_imports_field_when_resolving_imports,
    defaultValueDescription: Diagnostics.true_when_moduleResolution_is_node16_nodenext_or_bundler_otherwise_false
  },
  {
    name: "customConditions",
    type: "list",
    element: {
      name: "condition",
      type: "string"
    },
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Conditions_to_set_in_addition_to_the_resolver_specific_defaults_when_resolving_imports
  },
  {
    name: "noUncheckedSideEffectImports",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Check_side_effect_imports,
    defaultValueDescription: false
  },
  // Source Maps
  {
    name: "sourceRoot",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    paramType: Diagnostics.LOCATION,
    category: Diagnostics.Emit,
    description: Diagnostics.Specify_the_root_path_for_debuggers_to_find_the_reference_source_code
  },
  {
    name: "mapRoot",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    paramType: Diagnostics.LOCATION,
    category: Diagnostics.Emit,
    description: Diagnostics.Specify_the_location_where_debugger_should_locate_map_files_instead_of_generated_locations
  },
  {
    name: "inlineSources",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Include_source_code_in_the_sourcemaps_inside_the_emitted_JavaScript,
    defaultValueDescription: false
  },
  // Experimental
  {
    name: "experimentalDecorators",
    type: "boolean",
    affectsEmit: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Enable_experimental_support_for_legacy_experimental_decorators,
    defaultValueDescription: false
  },
  {
    name: "emitDecoratorMetadata",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Emit_design_type_metadata_for_decorated_declarations_in_source_files,
    defaultValueDescription: false
  },
  // Advanced
  {
    name: "jsxFactory",
    type: "string",
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_the_JSX_factory_function_used_when_targeting_React_JSX_emit_e_g_React_createElement_or_h,
    defaultValueDescription: "`React.createElement`"
  },
  {
    name: "jsxFragmentFactory",
    type: "string",
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_the_JSX_Fragment_reference_used_for_fragments_when_targeting_React_JSX_emit_e_g_React_Fragment_or_Fragment,
    defaultValueDescription: "React.Fragment"
  },
  {
    name: "jsxImportSource",
    type: "string",
    affectsSemanticDiagnostics: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsModuleResolution: true,
    affectsSourceFile: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_module_specifier_used_to_import_the_JSX_factory_functions_when_using_jsx_Colon_react_jsx_Asterisk,
    defaultValueDescription: "react"
  },
  {
    name: "resolveJsonModule",
    type: "boolean",
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Enable_importing_json_files,
    defaultValueDescription: false
  },
  {
    name: "allowArbitraryExtensions",
    type: "boolean",
    affectsProgramStructure: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Enable_importing_files_with_any_extension_provided_a_declaration_file_is_present,
    defaultValueDescription: false
  },
  {
    name: "out",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsDeclarationPath: true,
    isFilePath: false,
    // This is intentionally broken to support compatibility with existing tsconfig files
    // for correct behaviour, please use outFile
    category: Diagnostics.Backwards_Compatibility,
    paramType: Diagnostics.FILE,
    transpileOptionValue: void 0,
    description: Diagnostics.Deprecated_setting_Use_outFile_instead
  },
  {
    name: "reactNamespace",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Specify_the_object_invoked_for_createElement_This_only_applies_when_targeting_react_JSX_emit,
    defaultValueDescription: "`React`"
  },
  {
    name: "skipDefaultLibCheck",
    type: "boolean",
    // We need to store these to determine whether `lib` files need to be rechecked
    affectsBuildInfo: true,
    category: Diagnostics.Completeness,
    description: Diagnostics.Skip_type_checking_d_ts_files_that_are_included_with_TypeScript,
    defaultValueDescription: false
  },
  {
    name: "charset",
    type: "string",
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.No_longer_supported_In_early_versions_manually_set_the_text_encoding_for_reading_files,
    defaultValueDescription: "utf8"
  },
  {
    name: "emitBOM",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Emit_a_UTF_8_Byte_Order_Mark_BOM_in_the_beginning_of_output_files,
    defaultValueDescription: false
  },
  {
    name: "newLine",
    type: new Map(Object.entries({
      crlf: 0 /* CarriageReturnLineFeed */,
      lf: 1 /* LineFeed */
    })),
    affectsEmit: true,
    affectsBuildInfo: true,
    paramType: Diagnostics.NEWLINE,
    category: Diagnostics.Emit,
    description: Diagnostics.Set_the_newline_character_for_emitting_files,
    defaultValueDescription: "lf"
  },
  {
    name: "noErrorTruncation",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Output_Formatting,
    description: Diagnostics.Disable_truncating_types_in_error_messages,
    defaultValueDescription: false
  },
  {
    name: "noLib",
    type: "boolean",
    category: Diagnostics.Language_and_Environment,
    affectsProgramStructure: true,
    description: Diagnostics.Disable_including_any_library_files_including_the_default_lib_d_ts,
    // We are not returning a sourceFile for lib file when asked by the program,
    // so pass --noLib to avoid reporting a file not found error.
    transpileOptionValue: true,
    defaultValueDescription: false
  },
  {
    name: "noResolve",
    type: "boolean",
    affectsModuleResolution: true,
    category: Diagnostics.Modules,
    description: Diagnostics.Disallow_import_s_require_s_or_reference_s_from_expanding_the_number_of_files_TypeScript_should_add_to_a_project,
    // We are not doing a full typecheck, we are not resolving the whole context,
    // so pass --noResolve to avoid reporting missing file errors.
    transpileOptionValue: true,
    defaultValueDescription: false
  },
  {
    name: "stripInternal",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Disable_emitting_declarations_that_have_internal_in_their_JSDoc_comments,
    defaultValueDescription: false
  },
  {
    name: "disableSizeLimit",
    type: "boolean",
    affectsProgramStructure: true,
    category: Diagnostics.Editor_Support,
    description: Diagnostics.Remove_the_20mb_cap_on_total_source_code_size_for_JavaScript_files_in_the_TypeScript_language_server,
    defaultValueDescription: false
  },
  {
    name: "disableSourceOfProjectReferenceRedirect",
    type: "boolean",
    isTSConfigOnly: true,
    category: Diagnostics.Projects,
    description: Diagnostics.Disable_preferring_source_files_instead_of_declaration_files_when_referencing_composite_projects,
    defaultValueDescription: false
  },
  {
    name: "disableSolutionSearching",
    type: "boolean",
    isTSConfigOnly: true,
    category: Diagnostics.Projects,
    description: Diagnostics.Opt_a_project_out_of_multi_project_reference_checking_when_editing,
    defaultValueDescription: false
  },
  {
    name: "disableReferencedProjectLoad",
    type: "boolean",
    isTSConfigOnly: true,
    category: Diagnostics.Projects,
    description: Diagnostics.Reduce_the_number_of_projects_loaded_automatically_by_TypeScript,
    defaultValueDescription: false
  },
  {
    name: "noImplicitUseStrict",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Disable_adding_use_strict_directives_in_emitted_JavaScript_files,
    defaultValueDescription: false
  },
  {
    name: "noEmitHelpers",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Disable_generating_custom_helper_functions_like_extends_in_compiled_output,
    defaultValueDescription: false
  },
  {
    name: "noEmitOnError",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    transpileOptionValue: void 0,
    description: Diagnostics.Disable_emitting_files_if_any_type_checking_errors_are_reported,
    defaultValueDescription: false
  },
  {
    name: "preserveConstEnums",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Emit,
    description: Diagnostics.Disable_erasing_const_enum_declarations_in_generated_code,
    defaultValueDescription: false
  },
  {
    name: "declarationDir",
    type: "string",
    affectsEmit: true,
    affectsBuildInfo: true,
    affectsDeclarationPath: true,
    isFilePath: true,
    paramType: Diagnostics.DIRECTORY,
    category: Diagnostics.Emit,
    transpileOptionValue: void 0,
    description: Diagnostics.Specify_the_output_directory_for_generated_declaration_files
  },
  {
    name: "skipLibCheck",
    type: "boolean",
    // We need to store these to determine whether `lib` files need to be rechecked
    affectsBuildInfo: true,
    category: Diagnostics.Completeness,
    description: Diagnostics.Skip_type_checking_all_d_ts_files,
    defaultValueDescription: false
  },
  {
    name: "allowUnusedLabels",
    type: "boolean",
    affectsBindDiagnostics: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Disable_error_reporting_for_unused_labels,
    defaultValueDescription: void 0
  },
  {
    name: "allowUnreachableCode",
    type: "boolean",
    affectsBindDiagnostics: true,
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Type_Checking,
    description: Diagnostics.Disable_error_reporting_for_unreachable_code,
    defaultValueDescription: void 0
  },
  {
    name: "suppressExcessPropertyErrors",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Disable_reporting_of_excess_property_errors_during_the_creation_of_object_literals,
    defaultValueDescription: false
  },
  {
    name: "suppressImplicitAnyIndexErrors",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Suppress_noImplicitAny_errors_when_indexing_objects_that_lack_index_signatures,
    defaultValueDescription: false
  },
  {
    name: "forceConsistentCasingInFileNames",
    type: "boolean",
    affectsModuleResolution: true,
    category: Diagnostics.Interop_Constraints,
    description: Diagnostics.Ensure_that_casing_is_correct_in_imports,
    defaultValueDescription: true
  },
  {
    name: "maxNodeModuleJsDepth",
    type: "number",
    affectsModuleResolution: true,
    category: Diagnostics.JavaScript_Support,
    description: Diagnostics.Specify_the_maximum_folder_depth_used_for_checking_JavaScript_files_from_node_modules_Only_applicable_with_allowJs,
    defaultValueDescription: 0
  },
  {
    name: "noStrictGenericChecks",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Disable_strict_checking_of_generic_signatures_in_function_types,
    defaultValueDescription: false
  },
  {
    name: "useDefineForClassFields",
    type: "boolean",
    affectsSemanticDiagnostics: true,
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Language_and_Environment,
    description: Diagnostics.Emit_ECMAScript_standard_compliant_class_fields,
    defaultValueDescription: Diagnostics.true_for_ES2022_and_above_including_ESNext
  },
  {
    name: "preserveValueImports",
    type: "boolean",
    affectsEmit: true,
    affectsBuildInfo: true,
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Preserve_unused_imported_values_in_the_JavaScript_output_that_would_otherwise_be_removed,
    defaultValueDescription: false
  },
  {
    name: "keyofStringsOnly",
    type: "boolean",
    category: Diagnostics.Backwards_Compatibility,
    description: Diagnostics.Make_keyof_only_return_strings_instead_of_string_numbers_or_symbols_Legacy_option,
    defaultValueDescription: false
  },
  {
    // A list of plugins to load in the language service
    name: "plugins",
    type: "list",
    isTSConfigOnly: true,
    element: {
      name: "plugin",
      type: "object"
    },
    description: Diagnostics.Specify_a_list_of_language_service_plugins_to_include,
    category: Diagnostics.Editor_Support
  },
  {
    name: "moduleDetection",
    type: new Map(Object.entries({
      auto: 2 /* Auto */,
      legacy: 1 /* Legacy */,
      force: 3 /* Force */
    })),
    affectsSourceFile: true,
    affectsModuleResolution: true,
    description: Diagnostics.Control_what_method_is_used_to_detect_module_format_JS_files,
    category: Diagnostics.Language_and_Environment,
    defaultValueDescription: Diagnostics.auto_Colon_Treat_files_with_imports_exports_import_meta_jsx_with_jsx_Colon_react_jsx_or_esm_format_with_module_Colon_node16_as_modules
  },
  {
    name: "ignoreDeprecations",
    type: "string",
    defaultValueDescription: void 0
  }
];
var optionDeclarations ***REMOVED*** [
  ...commonOptionsWithBuild,
  ...commandOptionsWithoutBuild
];
var semanticDiagnosticsOptionDeclarations ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsSemanticDiagnostics);
var affectsEmitOptionDeclarations ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsEmit);
var affectsDeclarationPathOptionDeclarations ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsDeclarationPath);
var moduleResolutionOptionDeclarations ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsModuleResolution);
var sourceFileAffectingCompilerOptions ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsSourceFile || !!option.affectsBindDiagnostics);
var optionsAffectingProgramStructure ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> !!option.affectsProgramStructure);
var transpileOptionValueCompilerOptions ***REMOVED*** optionDeclarations.filter((option) ***REMOVED***> hasProperty(option, "transpileOptionValue"));
var configDirTemplateSubstitutionOptions ***REMOVED*** optionDeclarations.filter(
  (option) ***REMOVED***> option.allowConfigDirTemplateSubstitution || !option.isCommandLineOnly && option.isFilePath
);
var configDirTemplateSubstitutionWatchOptions ***REMOVED*** optionsForWatch.filter(
  (option) ***REMOVED***> option.allowConfigDirTemplateSubstitution || !option.isCommandLineOnly && option.isFilePath
);
var commandLineOptionOfCustomType ***REMOVED*** optionDeclarations.filter(isCommandLineOptionOfCustomType);
function isCommandLineOptionOfCustomType(option) {
  return !isString(option.type);
}
var tscBuildOption ***REMOVED*** {
  name: "build",
  type: "boolean",
  shortName: "b",
  showInSimplifiedHelpView: true,
  category: Diagnostics.Command_line_Options,
  description: Diagnostics.Build_one_or_more_projects_and_their_dependencies_if_out_of_date,
  defaultValueDescription: false
};
var optionsForBuild ***REMOVED*** [
  tscBuildOption,
  {
    name: "verbose",
    shortName: "v",
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Enable_verbose_logging,
    type: "boolean",
    defaultValueDescription: false
  },
  {
    name: "dry",
    shortName: "d",
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Show_what_would_be_built_or_deleted_if_specified_with_clean,
    type: "boolean",
    defaultValueDescription: false
  },
  {
    name: "force",
    shortName: "f",
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Build_all_projects_including_those_that_appear_to_be_up_to_date,
    type: "boolean",
    defaultValueDescription: false
  },
  {
    name: "clean",
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Delete_the_outputs_of_all_projects,
    type: "boolean",
    defaultValueDescription: false
  },
  {
    name: "stopBuildOnErrors",
    category: Diagnostics.Command_line_Options,
    description: Diagnostics.Skip_building_downstream_projects_on_error_in_upstream_project,
    type: "boolean",
    defaultValueDescription: false
  }
];
var buildOpts ***REMOVED*** [
  ...commonOptionsWithBuild,
  ...optionsForBuild
];
var typeAcquisitionDeclarations ***REMOVED*** [
  {
    name: "enable",
    type: "boolean",
    defaultValueDescription: false
  },
  {
    name: "include",
    type: "list",
    element: {
      name: "include",
      type: "string"
    }
  },
  {
    name: "exclude",
    type: "list",
    element: {
      name: "exclude",
      type: "string"
    }
  },
  {
    name: "disableFilenameBasedTypeAcquisition",
    type: "boolean",
    defaultValueDescription: false
  }
];
function createOptionNameMap(optionDeclarations2) {
  const optionsNameMap ***REMOVED*** /* @__PURE__ */ new Map();
  const shortOptionNames ***REMOVED*** /* @__PURE__ */ new Map();
  forEach(optionDeclarations2, (option) ***REMOVED***> {
    optionsNameMap.set(option.name.toLowerCase(), option);
    if (option.shortName) {
      shortOptionNames.set(option.shortName, option.name);
    }
  });
  return { optionsNameMap, shortOptionNames };
}
var optionsNameMapCache;
function getOptionsNameMap() {
  return optionsNameMapCache || (optionsNameMapCache ***REMOVED*** createOptionNameMap(optionDeclarations));
}
var compilerOptionsAlternateMode ***REMOVED*** {
  diagnostic: Diagnostics.Compiler_option_0_may_only_be_used_with_build,
  getOptionsNameMap: getBuildOptionsNameMap
};
var defaultInitCompilerOptions ***REMOVED*** {
  module: 1 /* CommonJS */,
  target: 3 /* ES2016 */,
  strict: true,
  esModuleInterop: true,
  forceConsistentCasingInFileNames: true,
  skipLibCheck: true
};
function createDiagnosticForInvalidCustomType(opt, createDiagnostic) {
  const namesOfType ***REMOVED*** arrayFrom(opt.type.keys());
  const stringNames ***REMOVED*** (opt.deprecatedKeys ? namesOfType.filter((k) ***REMOVED***> !opt.deprecatedKeys.has(k)) : namesOfType).map((key) ***REMOVED***> `'${key}'`).join(", ");
  return createDiagnostic(Diagnostics.Argument_for_0_option_must_be_Colon_1, `--${opt.name}`, stringNames);
}
function parseCustomTypeOption(opt, value, errors) {
  return convertJsonOptionOfCustomType(opt, (value ?? "").trim(), errors);
}
function parseListTypeOption(opt, value ***REMOVED*** "", errors) {
  value ***REMOVED*** value.trim();
  if (startsWith(value, "-")) {
    return void 0;
  }
  if (opt.type ***REMOVED******REMOVED******REMOVED*** "listOrElement" && !value.includes(",")) {
    return validateJsonOptionValue(opt, value, errors);
  }
  if (value ***REMOVED******REMOVED******REMOVED*** "") {
    return [];
  }
  const values ***REMOVED*** value.split(",");
  switch (opt.element.type) {
    case "number":
      return mapDefined(values, (v) ***REMOVED***> validateJsonOptionValue(opt.element, parseInt(v), errors));
    case "string":
      return mapDefined(values, (v) ***REMOVED***> validateJsonOptionValue(opt.element, v || "", errors));
    case "boolean":
    case "object":
      return Debug.fail(`List of ${opt.element.type} is not yet supported.`);
    default:
      return mapDefined(values, (v) ***REMOVED***> parseCustomTypeOption(opt.element, v, errors));
  }
}
function getOptionName(option) {
  return option.name;
}
function createUnknownOptionError(unknownOption, diagnostics, unknownOptionErrorText, node, sourceFile) {
  var _a;
  const otherOption ***REMOVED*** (_a ***REMOVED*** diagnostics.alternateMode) ***REMOVED******REMOVED*** null ? void 0 : _a.getOptionsNameMap().optionsNameMap.get(unknownOption.toLowerCase());
  if (otherOption) {
    return createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(
      sourceFile,
      node,
      otherOption !***REMOVED******REMOVED*** tscBuildOption ? diagnostics.alternateMode.diagnostic : Diagnostics.Option_build_must_be_the_first_command_line_argument,
      unknownOption
    );
  }
  const possibleOption ***REMOVED*** getSpellingSuggestion(unknownOption, diagnostics.optionDeclarations, getOptionName);
  return possibleOption ? createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, node, diagnostics.unknownDidYouMeanDiagnostic, unknownOptionErrorText || unknownOption, possibleOption.name) : createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, node, diagnostics.unknownOptionDiagnostic, unknownOptionErrorText || unknownOption);
}
function parseCommandLineWorker(diagnostics, commandLine, readFile) {
  const options ***REMOVED*** {};
  let watchOptions;
  const fileNames ***REMOVED*** [];
  const errors ***REMOVED*** [];
  parseStrings(commandLine);
  return {
    options,
    watchOptions,
    fileNames,
    errors
  };
  function parseStrings(args) {
    let i ***REMOVED*** 0;
    while (i < args.length) {
      const s ***REMOVED*** args[i];
      i++;
      if (s.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 64 /* at */) {
        parseResponseFile(s.slice(1));
      } else if (s.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
        const inputOptionName ***REMOVED*** s.slice(s.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */ ? 2 : 1);
        const opt ***REMOVED*** getOptionDeclarationFromName(
          diagnostics.getOptionsNameMap,
          inputOptionName,
          /*allowShort*/
          true
        );
        if (opt) {
          i ***REMOVED*** parseOptionValue(args, i, diagnostics, opt, options, errors);
        } else {
          const watchOpt ***REMOVED*** getOptionDeclarationFromName(
            watchOptionsDidYouMeanDiagnostics.getOptionsNameMap,
            inputOptionName,
            /*allowShort*/
            true
          );
          if (watchOpt) {
            i ***REMOVED*** parseOptionValue(args, i, watchOptionsDidYouMeanDiagnostics, watchOpt, watchOptions || (watchOptions ***REMOVED*** {}), errors);
          } else {
            errors.push(createUnknownOptionError(inputOptionName, diagnostics, s));
          }
        }
      } else {
        fileNames.push(s);
      }
    }
  }
  function parseResponseFile(fileName) {
    const text ***REMOVED*** tryReadFile(fileName, readFile || ((fileName2) ***REMOVED***> sys.readFile(fileName2)));
    if (!isString(text)) {
      errors.push(text);
      return;
    }
    const args ***REMOVED*** [];
    let pos ***REMOVED*** 0;
    while (true) {
      while (pos < text.length && text.charCodeAt(pos) <***REMOVED*** 32 /* space */) pos++;
      if (pos >***REMOVED*** text.length) break;
      const start ***REMOVED*** pos;
      if (text.charCodeAt(start) ***REMOVED******REMOVED******REMOVED*** 34 /* doubleQuote */) {
        pos++;
        while (pos < text.length && text.charCodeAt(pos) !***REMOVED******REMOVED*** 34 /* doubleQuote */) pos++;
        if (pos < text.length) {
          args.push(text.substring(start + 1, pos));
          pos++;
        } else {
          errors.push(createCompilerDiagnostic(Diagnostics.Unterminated_quoted_string_in_response_file_0, fileName));
        }
      } else {
        while (text.charCodeAt(pos) > 32 /* space */) pos++;
        args.push(text.substring(start, pos));
      }
    }
    parseStrings(args);
  }
}
function parseOptionValue(args, i, diagnostics, opt, options, errors) {
  if (opt.isTSConfigOnly) {
    const optValue ***REMOVED*** args[i];
    if (optValue ***REMOVED******REMOVED******REMOVED*** "null") {
      options[opt.name] ***REMOVED*** void 0;
      i++;
    } else if (opt.type ***REMOVED******REMOVED******REMOVED*** "boolean") {
      if (optValue ***REMOVED******REMOVED******REMOVED*** "false") {
        options[opt.name] ***REMOVED*** validateJsonOptionValue(
          opt,
          /*value*/
          false,
          errors
        );
        i++;
      } else {
        if (optValue ***REMOVED******REMOVED******REMOVED*** "true") i++;
        errors.push(createCompilerDiagnostic(Diagnostics.Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_false_or_null_on_command_line, opt.name));
      }
    } else {
      errors.push(createCompilerDiagnostic(Diagnostics.Option_0_can_only_be_specified_in_tsconfig_json_file_or_set_to_null_on_command_line, opt.name));
      if (optValue && !startsWith(optValue, "-")) i++;
    }
  } else {
    if (!args[i] && opt.type !***REMOVED******REMOVED*** "boolean") {
      errors.push(createCompilerDiagnostic(diagnostics.optionTypeMismatchDiagnostic, opt.name, getCompilerOptionValueTypeString(opt)));
    }
    if (args[i] !***REMOVED******REMOVED*** "null") {
      switch (opt.type) {
        case "number":
          options[opt.name] ***REMOVED*** validateJsonOptionValue(opt, parseInt(args[i]), errors);
          i++;
          break;
        case "boolean":
          const optValue ***REMOVED*** args[i];
          options[opt.name] ***REMOVED*** validateJsonOptionValue(opt, optValue !***REMOVED******REMOVED*** "false", errors);
          if (optValue ***REMOVED******REMOVED******REMOVED*** "false" || optValue ***REMOVED******REMOVED******REMOVED*** "true") {
            i++;
          }
          break;
        case "string":
          options[opt.name] ***REMOVED*** validateJsonOptionValue(opt, args[i] || "", errors);
          i++;
          break;
        case "list":
          const result ***REMOVED*** parseListTypeOption(opt, args[i], errors);
          options[opt.name] ***REMOVED*** result || [];
          if (result) {
            i++;
          }
          break;
        case "listOrElement":
          Debug.fail("listOrElement not supported here");
          break;
        // If not a primitive, the possible types are specified in what is effectively a map of options.
        default:
          options[opt.name] ***REMOVED*** parseCustomTypeOption(opt, args[i], errors);
          i++;
          break;
      }
    } else {
      options[opt.name] ***REMOVED*** void 0;
      i++;
    }
  }
  return i;
}
var compilerOptionsDidYouMeanDiagnostics ***REMOVED*** {
  alternateMode: compilerOptionsAlternateMode,
  getOptionsNameMap,
  optionDeclarations,
  unknownOptionDiagnostic: Diagnostics.Unknown_compiler_option_0,
  unknownDidYouMeanDiagnostic: Diagnostics.Unknown_compiler_option_0_Did_you_mean_1,
  optionTypeMismatchDiagnostic: Diagnostics.Compiler_option_0_expects_an_argument
};
function parseCommandLine(commandLine, readFile) {
  return parseCommandLineWorker(compilerOptionsDidYouMeanDiagnostics, commandLine, readFile);
}
function getOptionDeclarationFromName(getOptionNameMap, optionName, allowShort ***REMOVED*** false) {
  optionName ***REMOVED*** optionName.toLowerCase();
  const { optionsNameMap, shortOptionNames } ***REMOVED*** getOptionNameMap();
  if (allowShort) {
    const short ***REMOVED*** shortOptionNames.get(optionName);
    if (short !***REMOVED******REMOVED*** void 0) {
      optionName ***REMOVED*** short;
    }
  }
  return optionsNameMap.get(optionName);
}
var buildOptionsNameMapCache;
function getBuildOptionsNameMap() {
  return buildOptionsNameMapCache || (buildOptionsNameMapCache ***REMOVED*** createOptionNameMap(buildOpts));
}
var buildOptionsAlternateMode ***REMOVED*** {
  diagnostic: Diagnostics.Compiler_option_0_may_not_be_used_with_build,
  getOptionsNameMap
};
var buildOptionsDidYouMeanDiagnostics ***REMOVED*** {
  alternateMode: buildOptionsAlternateMode,
  getOptionsNameMap: getBuildOptionsNameMap,
  optionDeclarations: buildOpts,
  unknownOptionDiagnostic: Diagnostics.Unknown_build_option_0,
  unknownDidYouMeanDiagnostic: Diagnostics.Unknown_build_option_0_Did_you_mean_1,
  optionTypeMismatchDiagnostic: Diagnostics.Build_option_0_requires_a_value_of_type_1
};
function parseBuildCommand(commandLine) {
  const { options, watchOptions, fileNames: projects, errors } ***REMOVED*** parseCommandLineWorker(
    buildOptionsDidYouMeanDiagnostics,
    commandLine
  );
  const buildOptions ***REMOVED*** options;
  if (projects.length ***REMOVED******REMOVED******REMOVED*** 0) {
    projects.push(".");
  }
  if (buildOptions.clean && buildOptions.force) {
    errors.push(createCompilerDiagnostic(Diagnostics.Options_0_and_1_cannot_be_combined, "clean", "force"));
  }
  if (buildOptions.clean && buildOptions.verbose) {
    errors.push(createCompilerDiagnostic(Diagnostics.Options_0_and_1_cannot_be_combined, "clean", "verbose"));
  }
  if (buildOptions.clean && buildOptions.watch) {
    errors.push(createCompilerDiagnostic(Diagnostics.Options_0_and_1_cannot_be_combined, "clean", "watch"));
  }
  if (buildOptions.watch && buildOptions.dry) {
    errors.push(createCompilerDiagnostic(Diagnostics.Options_0_and_1_cannot_be_combined, "watch", "dry"));
  }
  return { buildOptions, watchOptions, projects, errors };
}
function getDiagnosticText(message, ...args) {
  return cast(createCompilerDiagnostic(message, ...args).messageText, isString);
}
function getParsedCommandLineOfConfigFile(configFileName, optionsToExtend, host, extendedConfigCache, watchOptionsToExtend, extraFileExtensions) {
  const configFileText ***REMOVED*** tryReadFile(configFileName, (fileName) ***REMOVED***> host.readFile(fileName));
  if (!isString(configFileText)) {
    host.onUnRecoverableConfigFileDiagnostic(configFileText);
    return void 0;
  }
  const result ***REMOVED*** parseJsonText(configFileName, configFileText);
  const cwd ***REMOVED*** host.getCurrentDirectory();
  result.path ***REMOVED*** toPath(configFileName, cwd, createGetCanonicalFileName(host.useCaseSensitiveFileNames));
  result.resolvedPath ***REMOVED*** result.path;
  result.originalFileName ***REMOVED*** result.fileName;
  return parseJsonSourceFileConfigFileContent(
    result,
    host,
    getNormalizedAbsolutePath(getDirectoryPath(configFileName), cwd),
    optionsToExtend,
    getNormalizedAbsolutePath(configFileName, cwd),
    /*resolutionStack*/
    void 0,
    extraFileExtensions,
    extendedConfigCache,
    watchOptionsToExtend
  );
}
function parseConfigFileTextToJson(fileName, jsonText) {
  const jsonSourceFile ***REMOVED*** parseJsonText(fileName, jsonText);
  return {
    config: convertConfigFileToObject(
      jsonSourceFile,
      jsonSourceFile.parseDiagnostics,
      /*jsonConversionNotifier*/
      void 0
    ),
    error: jsonSourceFile.parseDiagnostics.length ? jsonSourceFile.parseDiagnostics[0] : void 0
  };
}
function readJsonConfigFile(fileName, readFile) {
  const textOrDiagnostic ***REMOVED*** tryReadFile(fileName, readFile);
  return isString(textOrDiagnostic) ? parseJsonText(fileName, textOrDiagnostic) : { fileName, parseDiagnostics: [textOrDiagnostic] };
}
function tryReadFile(fileName, readFile) {
  let text;
  try {
    text ***REMOVED*** readFile(fileName);
  } catch (e) {
    return createCompilerDiagnostic(Diagnostics.Cannot_read_file_0_Colon_1, fileName, e.message);
  }
  return text ***REMOVED******REMOVED******REMOVED*** void 0 ? createCompilerDiagnostic(Diagnostics.Cannot_read_file_0, fileName) : text;
}
function commandLineOptionsToMap(options) {
  return arrayToMap(options, getOptionName);
}
var typeAcquisitionDidYouMeanDiagnostics ***REMOVED*** {
  optionDeclarations: typeAcquisitionDeclarations,
  unknownOptionDiagnostic: Diagnostics.Unknown_type_acquisition_option_0,
  unknownDidYouMeanDiagnostic: Diagnostics.Unknown_type_acquisition_option_0_Did_you_mean_1
};
var watchOptionsNameMapCache;
function getWatchOptionsNameMap() {
  return watchOptionsNameMapCache || (watchOptionsNameMapCache ***REMOVED*** createOptionNameMap(optionsForWatch));
}
var watchOptionsDidYouMeanDiagnostics ***REMOVED*** {
  getOptionsNameMap: getWatchOptionsNameMap,
  optionDeclarations: optionsForWatch,
  unknownOptionDiagnostic: Diagnostics.Unknown_watch_option_0,
  unknownDidYouMeanDiagnostic: Diagnostics.Unknown_watch_option_0_Did_you_mean_1,
  optionTypeMismatchDiagnostic: Diagnostics.Watch_option_0_requires_a_value_of_type_1
};
var commandLineCompilerOptionsMapCache;
function getCommandLineCompilerOptionsMap() {
  return commandLineCompilerOptionsMapCache || (commandLineCompilerOptionsMapCache ***REMOVED*** commandLineOptionsToMap(optionDeclarations));
}
var commandLineWatchOptionsMapCache;
function getCommandLineWatchOptionsMap() {
  return commandLineWatchOptionsMapCache || (commandLineWatchOptionsMapCache ***REMOVED*** commandLineOptionsToMap(optionsForWatch));
}
var commandLineTypeAcquisitionMapCache;
function getCommandLineTypeAcquisitionMap() {
  return commandLineTypeAcquisitionMapCache || (commandLineTypeAcquisitionMapCache ***REMOVED*** commandLineOptionsToMap(typeAcquisitionDeclarations));
}
var extendsOptionDeclaration ***REMOVED*** {
  name: "extends",
  type: "listOrElement",
  element: {
    name: "extends",
    type: "string"
  },
  category: Diagnostics.File_Management,
  disallowNullOrUndefined: true
};
var compilerOptionsDeclaration ***REMOVED*** {
  name: "compilerOptions",
  type: "object",
  elementOptions: getCommandLineCompilerOptionsMap(),
  extraKeyDiagnostics: compilerOptionsDidYouMeanDiagnostics
};
var watchOptionsDeclaration ***REMOVED*** {
  name: "watchOptions",
  type: "object",
  elementOptions: getCommandLineWatchOptionsMap(),
  extraKeyDiagnostics: watchOptionsDidYouMeanDiagnostics
};
var typeAcquisitionDeclaration ***REMOVED*** {
  name: "typeAcquisition",
  type: "object",
  elementOptions: getCommandLineTypeAcquisitionMap(),
  extraKeyDiagnostics: typeAcquisitionDidYouMeanDiagnostics
};
var _tsconfigRootOptions;
function getTsconfigRootOptionsMap() {
  if (_tsconfigRootOptions ***REMOVED******REMOVED******REMOVED*** void 0) {
    _tsconfigRootOptions ***REMOVED*** {
      name: void 0,
      // should never be needed since this is root
      type: "object",
      elementOptions: commandLineOptionsToMap([
        compilerOptionsDeclaration,
        watchOptionsDeclaration,
        typeAcquisitionDeclaration,
        extendsOptionDeclaration,
        {
          name: "references",
          type: "list",
          element: {
            name: "references",
            type: "object"
          },
          category: Diagnostics.Projects
        },
        {
          name: "files",
          type: "list",
          element: {
            name: "files",
            type: "string"
          },
          category: Diagnostics.File_Management
        },
        {
          name: "include",
          type: "list",
          element: {
            name: "include",
            type: "string"
          },
          category: Diagnostics.File_Management,
          defaultValueDescription: Diagnostics.if_files_is_specified_otherwise_Asterisk_Asterisk_Slash_Asterisk
        },
        {
          name: "exclude",
          type: "list",
          element: {
            name: "exclude",
            type: "string"
          },
          category: Diagnostics.File_Management,
          defaultValueDescription: Diagnostics.node_modules_bower_components_jspm_packages_plus_the_value_of_outDir_if_one_is_specified
        },
        compileOnSaveCommandLineOption
      ])
    };
  }
  return _tsconfigRootOptions;
}
function convertConfigFileToObject(sourceFile, errors, jsonConversionNotifier) {
  var _a;
  const rootExpression ***REMOVED*** (_a ***REMOVED*** sourceFile.statements[0]) ***REMOVED******REMOVED*** null ? void 0 : _a.expression;
  if (rootExpression && rootExpression.kind !***REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
    errors.push(createDiagnosticForNodeInSourceFile(
      sourceFile,
      rootExpression,
      Diagnostics.The_root_value_of_a_0_file_must_be_an_object,
      getBaseFileName(sourceFile.fileName) ***REMOVED******REMOVED******REMOVED*** "jsconfig.json" ? "jsconfig.json" : "tsconfig.json"
    ));
    if (isArrayLiteralExpression(rootExpression)) {
      const firstObject ***REMOVED*** find(rootExpression.elements, isObjectLiteralExpression);
      if (firstObject) {
        return convertToJson(
          sourceFile,
          firstObject,
          errors,
          /*returnValue*/
          true,
          jsonConversionNotifier
        );
      }
    }
    return {};
  }
  return convertToJson(
    sourceFile,
    rootExpression,
    errors,
    /*returnValue*/
    true,
    jsonConversionNotifier
  );
}
function convertToObject(sourceFile, errors) {
  var _a;
  return convertToJson(
    sourceFile,
    (_a ***REMOVED*** sourceFile.statements[0]) ***REMOVED******REMOVED*** null ? void 0 : _a.expression,
    errors,
    /*returnValue*/
    true,
    /*jsonConversionNotifier*/
    void 0
  );
}
function convertToJson(sourceFile, rootExpression, errors, returnValue, jsonConversionNotifier) {
  if (!rootExpression) {
    return returnValue ? {} : void 0;
  }
  return convertPropertyValueToJson(rootExpression, jsonConversionNotifier ***REMOVED******REMOVED*** null ? void 0 : jsonConversionNotifier.rootOptions);
  function convertObjectLiteralExpressionToJson(node, objectOption) {
    var _a;
    const result ***REMOVED*** returnValue ? {} : void 0;
    for (const element of node.properties) {
      if (element.kind !***REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
        errors.push(createDiagnosticForNodeInSourceFile(sourceFile, element, Diagnostics.Property_assignment_expected));
        continue;
      }
      if (element.questionToken) {
        errors.push(createDiagnosticForNodeInSourceFile(sourceFile, element.questionToken, Diagnostics.The_0_modifier_can_only_be_used_in_TypeScript_files, "?"));
      }
      if (!isDoubleQuotedString(element.name)) {
        errors.push(createDiagnosticForNodeInSourceFile(sourceFile, element.name, Diagnostics.String_literal_with_double_quotes_expected));
      }
      const textOfKey ***REMOVED*** isComputedNonLiteralName(element.name) ? void 0 : getTextOfPropertyName(element.name);
      const keyText ***REMOVED*** textOfKey && unescapeLeadingUnderscores(textOfKey);
      const option ***REMOVED*** keyText ? (_a ***REMOVED*** objectOption ***REMOVED******REMOVED*** null ? void 0 : objectOption.elementOptions) ***REMOVED******REMOVED*** null ? void 0 : _a.get(keyText) : void 0;
      const value ***REMOVED*** convertPropertyValueToJson(element.initializer, option);
      if (typeof keyText !***REMOVED******REMOVED*** "undefined") {
        if (returnValue) {
          result[keyText] ***REMOVED*** value;
        }
        jsonConversionNotifier ***REMOVED******REMOVED*** null ? void 0 : jsonConversionNotifier.onPropertySet(keyText, value, element, objectOption, option);
      }
    }
    return result;
  }
  function convertArrayLiteralExpressionToJson(elements, elementOption) {
    if (!returnValue) {
      elements.forEach((element) ***REMOVED***> convertPropertyValueToJson(element, elementOption));
      return void 0;
    }
    return filter(elements.map((element) ***REMOVED***> convertPropertyValueToJson(element, elementOption)), (v) ***REMOVED***> v !***REMOVED******REMOVED*** void 0);
  }
  function convertPropertyValueToJson(valueExpression, option) {
    switch (valueExpression.kind) {
      case 112 /* TrueKeyword */:
        return true;
      case 97 /* FalseKeyword */:
        return false;
      case 106 /* NullKeyword */:
        return null;
      // eslint-disable-line no-restricted-syntax
      case 11 /* StringLiteral */:
        if (!isDoubleQuotedString(valueExpression)) {
          errors.push(createDiagnosticForNodeInSourceFile(sourceFile, valueExpression, Diagnostics.String_literal_with_double_quotes_expected));
        }
        return valueExpression.text;
      case 9 /* NumericLiteral */:
        return Number(valueExpression.text);
      case 225 /* PrefixUnaryExpression */:
        if (valueExpression.operator !***REMOVED******REMOVED*** 41 /* MinusToken */ || valueExpression.operand.kind !***REMOVED******REMOVED*** 9 /* NumericLiteral */) {
          break;
        }
        return -Number(valueExpression.operand.text);
      case 211 /* ObjectLiteralExpression */:
        const objectLiteralExpression ***REMOVED*** valueExpression;
        return convertObjectLiteralExpressionToJson(objectLiteralExpression, option);
      case 210 /* ArrayLiteralExpression */:
        return convertArrayLiteralExpressionToJson(
          valueExpression.elements,
          option && option.element
        );
    }
    if (option) {
      errors.push(createDiagnosticForNodeInSourceFile(sourceFile, valueExpression, Diagnostics.Compiler_option_0_requires_a_value_of_type_1, option.name, getCompilerOptionValueTypeString(option)));
    } else {
      errors.push(createDiagnosticForNodeInSourceFile(sourceFile, valueExpression, Diagnostics.Property_value_can_only_be_string_literal_numeric_literal_true_false_null_object_literal_or_array_literal));
    }
    return void 0;
  }
  function isDoubleQuotedString(node) {
    return isStringLiteral(node) && isStringDoubleQuoted(node, sourceFile);
  }
}
function getCompilerOptionValueTypeString(option) {
  return option.type ***REMOVED******REMOVED******REMOVED*** "listOrElement" ? `${getCompilerOptionValueTypeString(option.element)} or Array` : option.type ***REMOVED******REMOVED******REMOVED*** "list" ? "Array" : isString(option.type) ? option.type : "string";
}
function isCompilerOptionsValue(option, value) {
  if (option) {
    if (isNullOrUndefined(value)) return !option.disallowNullOrUndefined;
    if (option.type ***REMOVED******REMOVED******REMOVED*** "list") {
      return isArray(value);
    }
    if (option.type ***REMOVED******REMOVED******REMOVED*** "listOrElement") {
      return isArray(value) || isCompilerOptionsValue(option.element, value);
    }
    const expectedType ***REMOVED*** isString(option.type) ? option.type : "string";
    return typeof value ***REMOVED******REMOVED******REMOVED*** expectedType;
  }
  return false;
}
function convertToTSConfig(configParseResult, configFileName, host) {
  var _a, _b, _c;
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames);
  const files ***REMOVED*** map(
    filter(
      configParseResult.fileNames,
      !((_b ***REMOVED*** (_a ***REMOVED*** configParseResult.options.configFile) ***REMOVED******REMOVED*** null ? void 0 : _a.configFileSpecs) ***REMOVED******REMOVED*** null ? void 0 : _b.validatedIncludeSpecs) ? returnTrue : matchesSpecs(
        configFileName,
        configParseResult.options.configFile.configFileSpecs.validatedIncludeSpecs,
        configParseResult.options.configFile.configFileSpecs.validatedExcludeSpecs,
        host
      )
    ),
    (f) ***REMOVED***> getRelativePathFromFile(getNormalizedAbsolutePath(configFileName, host.getCurrentDirectory()), getNormalizedAbsolutePath(f, host.getCurrentDirectory()), getCanonicalFileName)
  );
  const pathOptions ***REMOVED*** { configFilePath: getNormalizedAbsolutePath(configFileName, host.getCurrentDirectory()), useCaseSensitiveFileNames: host.useCaseSensitiveFileNames };
  const optionMap ***REMOVED*** serializeCompilerOptions(configParseResult.options, pathOptions);
  const watchOptionMap ***REMOVED*** configParseResult.watchOptions && serializeWatchOptions(configParseResult.watchOptions);
  const config ***REMOVED*** {
    compilerOptions: {
      ...optionMapToObject(optionMap),
      showConfig: void 0,
      configFile: void 0,
      configFilePath: void 0,
      help: void 0,
      init: void 0,
      listFiles: void 0,
      listEmittedFiles: void 0,
      project: void 0,
      build: void 0,
      version: void 0
    },
    watchOptions: watchOptionMap && optionMapToObject(watchOptionMap),
    references: map(configParseResult.projectReferences, (r) ***REMOVED***> ({ ...r, path: r.originalPath ? r.originalPath : "", originalPath: void 0 })),
    files: length(files) ? files : void 0,
    ...((_c ***REMOVED*** configParseResult.options.configFile) ***REMOVED******REMOVED*** null ? void 0 : _c.configFileSpecs) ? {
      include: filterSameAsDefaultInclude(configParseResult.options.configFile.configFileSpecs.validatedIncludeSpecs),
      exclude: configParseResult.options.configFile.configFileSpecs.validatedExcludeSpecs
    } : {},
    compileOnSave: !!configParseResult.compileOnSave ? true : void 0
  };
  const providedKeys ***REMOVED*** new Set(optionMap.keys());
  const impliedCompilerOptions ***REMOVED*** {};
  for (const option in computedOptions) {
    if (!providedKeys.has(option) && optionDependsOn(option, providedKeys)) {
      const implied ***REMOVED*** computedOptions[option].computeValue(configParseResult.options);
      const defaultValue ***REMOVED*** computedOptions[option].computeValue({});
      if (implied !***REMOVED******REMOVED*** defaultValue) {
        impliedCompilerOptions[option] ***REMOVED*** computedOptions[option].computeValue(configParseResult.options);
      }
    }
  }
  assign(config.compilerOptions, optionMapToObject(serializeCompilerOptions(impliedCompilerOptions, pathOptions)));
  return config;
}
function optionDependsOn(option, dependsOn) {
  const seen ***REMOVED*** /* @__PURE__ */ new Set();
  return optionDependsOnRecursive(option);
  function optionDependsOnRecursive(option2) {
    var _a;
    if (addToSeen(seen, option2)) {
      return some((_a ***REMOVED*** computedOptions[option2]) ***REMOVED******REMOVED*** null ? void 0 : _a.dependencies, (dep) ***REMOVED***> dependsOn.has(dep) || optionDependsOnRecursive(dep));
    }
    return false;
  }
}
function optionMapToObject(optionMap) {
  return Object.fromEntries(optionMap);
}
function filterSameAsDefaultInclude(specs) {
  if (!length(specs)) return void 0;
  if (length(specs) !***REMOVED******REMOVED*** 1) return specs;
  if (specs[0] ***REMOVED******REMOVED******REMOVED*** defaultIncludeSpec) return void 0;
  return specs;
}
function matchesSpecs(path, includeSpecs, excludeSpecs, host) {
  if (!includeSpecs) return returnTrue;
  const patterns ***REMOVED*** getFileMatcherPatterns(path, excludeSpecs, includeSpecs, host.useCaseSensitiveFileNames, host.getCurrentDirectory());
  const excludeRe ***REMOVED*** patterns.excludePattern && getRegexFromPattern(patterns.excludePattern, host.useCaseSensitiveFileNames);
  const includeRe ***REMOVED*** patterns.includeFilePattern && getRegexFromPattern(patterns.includeFilePattern, host.useCaseSensitiveFileNames);
  if (includeRe) {
    if (excludeRe) {
      return (path2) ***REMOVED***> !(includeRe.test(path2) && !excludeRe.test(path2));
    }
    return (path2) ***REMOVED***> !includeRe.test(path2);
  }
  if (excludeRe) {
    return (path2) ***REMOVED***> excludeRe.test(path2);
  }
  return returnTrue;
}
function getCustomTypeMapOfCommandLineOption(optionDefinition) {
  switch (optionDefinition.type) {
    case "string":
    case "number":
    case "boolean":
    case "object":
      return void 0;
    case "list":
    case "listOrElement":
      return getCustomTypeMapOfCommandLineOption(optionDefinition.element);
    default:
      return optionDefinition.type;
  }
}
function getNameOfCompilerOptionValue(value, customTypeMap) {
  return forEachEntry(customTypeMap, (mapValue, key) ***REMOVED***> {
    if (mapValue ***REMOVED******REMOVED******REMOVED*** value) {
      return key;
    }
  });
}
function serializeCompilerOptions(options, pathOptions) {
  return serializeOptionBaseObject(options, getOptionsNameMap(), pathOptions);
}
function serializeWatchOptions(options) {
  return serializeOptionBaseObject(options, getWatchOptionsNameMap());
}
function serializeOptionBaseObject(options, { optionsNameMap }, pathOptions) {
  const result ***REMOVED*** /* @__PURE__ */ new Map();
  const getCanonicalFileName ***REMOVED*** pathOptions && createGetCanonicalFileName(pathOptions.useCaseSensitiveFileNames);
  for (const name in options) {
    if (hasProperty(options, name)) {
      if (optionsNameMap.has(name) && (optionsNameMap.get(name).category ***REMOVED******REMOVED******REMOVED*** Diagnostics.Command_line_Options || optionsNameMap.get(name).category ***REMOVED******REMOVED******REMOVED*** Diagnostics.Output_Formatting)) {
        continue;
      }
      const value ***REMOVED*** options[name];
      const optionDefinition ***REMOVED*** optionsNameMap.get(name.toLowerCase());
      if (optionDefinition) {
        Debug.assert(optionDefinition.type !***REMOVED******REMOVED*** "listOrElement");
        const customTypeMap ***REMOVED*** getCustomTypeMapOfCommandLineOption(optionDefinition);
        if (!customTypeMap) {
          if (pathOptions && optionDefinition.isFilePath) {
            result.set(name, getRelativePathFromFile(pathOptions.configFilePath, getNormalizedAbsolutePath(value, getDirectoryPath(pathOptions.configFilePath)), getCanonicalFileName));
          } else if (pathOptions && optionDefinition.type ***REMOVED******REMOVED******REMOVED*** "list" && optionDefinition.element.isFilePath) {
            result.set(name, value.map((v) ***REMOVED***> getRelativePathFromFile(pathOptions.configFilePath, getNormalizedAbsolutePath(v, getDirectoryPath(pathOptions.configFilePath)), getCanonicalFileName)));
          } else {
            result.set(name, value);
          }
        } else {
          if (optionDefinition.type ***REMOVED******REMOVED******REMOVED*** "list") {
            result.set(name, value.map((element) ***REMOVED***> getNameOfCompilerOptionValue(element, customTypeMap)));
          } else {
            result.set(name, getNameOfCompilerOptionValue(value, customTypeMap));
          }
        }
      }
    }
  }
  return result;
}
function generateTSConfig(options, newLine) {
  const tab ***REMOVED*** "  ";
  const result ***REMOVED*** [];
  const allSetOptions ***REMOVED*** Object.keys(options).filter((k) ***REMOVED***> k !***REMOVED******REMOVED*** "init" && k !***REMOVED******REMOVED*** "help" && k !***REMOVED******REMOVED*** "watch");
  result.push(`{`);
  result.push(`${tab}// ${getLocaleSpecificMessage(Diagnostics.Visit_https_Colon_Slash_Slashaka_ms_Slashtsconfig_to_read_more_about_this_file)}`);
  result.push(`${tab}"compilerOptions": {`);
  emitHeader(Diagnostics.File_Layout);
  emitOption("rootDir", "./src", "optional");
  emitOption("outDir", "./dist", "optional");
  newline();
  emitHeader(Diagnostics.Environment_Settings);
  emitHeader(Diagnostics.See_also_https_Colon_Slash_Slashaka_ms_Slashtsconfig_Slashmodule);
  emitOption("module", 199 /* NodeNext */);
  emitOption("target", 99 /* ESNext */);
  emitOption("types", []);
  if (options.lib) {
    emitOption("lib", options.lib);
  }
  emitHeader(Diagnostics.For_nodejs_Colon);
  result.push(`${tab}${tab}// "lib": ["esnext"],`);
  result.push(`${tab}${tab}// "types": ["node"],`);
  emitHeader(Diagnostics.and_npm_install_D_types_Slashnode);
  newline();
  emitHeader(Diagnostics.Other_Outputs);
  emitOption(
    "sourceMap",
    /*defaultValue*/
    true
  );
  emitOption(
    "declaration",
    /*defaultValue*/
    true
  );
  emitOption(
    "declarationMap",
    /*defaultValue*/
    true
  );
  newline();
  emitHeader(Diagnostics.Stricter_Typechecking_Options);
  emitOption(
    "noUncheckedIndexedAccess",
    /*defaultValue*/
    true
  );
  emitOption(
    "exactOptionalPropertyTypes",
    /*defaultValue*/
    true
  );
  newline();
  emitHeader(Diagnostics.Style_Options);
  emitOption(
    "noImplicitReturns",
    /*defaultValue*/
    true,
    "optional"
  );
  emitOption(
    "noImplicitOverride",
    /*defaultValue*/
    true,
    "optional"
  );
  emitOption(
    "noUnusedLocals",
    /*defaultValue*/
    true,
    "optional"
  );
  emitOption(
    "noUnusedParameters",
    /*defaultValue*/
    true,
    "optional"
  );
  emitOption(
    "noFallthroughCasesInSwitch",
    /*defaultValue*/
    true,
    "optional"
  );
  emitOption(
    "noPropertyAccessFromIndexSignature",
    /*defaultValue*/
    true,
    "optional"
  );
  newline();
  emitHeader(Diagnostics.Recommended_Options);
  emitOption(
    "strict",
    /*defaultValue*/
    true
  );
  emitOption("jsx", 4 /* ReactJSX */);
  emitOption(
    "verbatimModuleSyntax",
    /*defaultValue*/
    true
  );
  emitOption(
    "isolatedModules",
    /*defaultValue*/
    true
  );
  emitOption(
    "noUncheckedSideEffectImports",
    /*defaultValue*/
    true
  );
  emitOption("moduleDetection", 3 /* Force */);
  emitOption(
    "skipLibCheck",
    /*defaultValue*/
    true
  );
  if (allSetOptions.length > 0) {
    newline();
    while (allSetOptions.length > 0) {
      emitOption(allSetOptions[0], options[allSetOptions[0]]);
    }
  }
  function newline() {
    result.push("");
  }
  function emitHeader(header) {
    result.push(`${tab}${tab}// ${getLocaleSpecificMessage(header)}`);
  }
  function emitOption(setting, defaultValue, commented ***REMOVED*** "never") {
    const existingOptionIndex ***REMOVED*** allSetOptions.indexOf(setting);
    if (existingOptionIndex >***REMOVED*** 0) {
      allSetOptions.splice(existingOptionIndex, 1);
    }
    let comment;
    if (commented ***REMOVED******REMOVED******REMOVED*** "always") {
      comment ***REMOVED*** true;
    } else if (commented ***REMOVED******REMOVED******REMOVED*** "never") {
      comment ***REMOVED*** false;
    } else {
      comment ***REMOVED*** !hasProperty(options, setting);
    }
    const value ***REMOVED*** options[setting] ?? defaultValue;
    if (comment) {
      result.push(`${tab}${tab}// "${setting}": ${formatValueOrArray(setting, value)},`);
    } else {
      result.push(`${tab}${tab}"${setting}": ${formatValueOrArray(setting, value)},`);
    }
  }
  function formatValueOrArray(settingName, value) {
    const option ***REMOVED*** optionDeclarations.filter((c) ***REMOVED***> c.name ***REMOVED******REMOVED******REMOVED*** settingName)[0];
    if (!option) Debug.fail(`No option named ${settingName}?`);
    const map2 ***REMOVED*** option.type instanceof Map ? option.type : void 0;
    if (isArray(value)) {
      const map3 ***REMOVED*** "element" in option && option.element.type instanceof Map ? option.element.type : void 0;
      return `[${value.map((v) ***REMOVED***> formatSingleValue(v, map3)).join(", ")}]`;
    } else {
      return formatSingleValue(value, map2);
    }
  }
  function formatSingleValue(value, map2) {
    if (map2) {
      value ***REMOVED*** getNameOfCompilerOptionValue(value, map2) ?? Debug.fail(`No matching value of ${value}`);
    }
    return JSON.stringify(value);
  }
  result.push(`${tab}}`);
  result.push(`}`);
  result.push(``);
  return result.join(newLine);
}
function convertToOptionsWithAbsolutePaths(options, toAbsolutePath) {
  const result ***REMOVED*** {};
  const optionsNameMap ***REMOVED*** getOptionsNameMap().optionsNameMap;
  for (const name in options) {
    if (hasProperty(options, name)) {
      result[name] ***REMOVED*** convertToOptionValueWithAbsolutePaths(
        optionsNameMap.get(name.toLowerCase()),
        options[name],
        toAbsolutePath
      );
    }
  }
  if (result.configFilePath) {
    result.configFilePath ***REMOVED*** toAbsolutePath(result.configFilePath);
  }
  return result;
}
function convertToOptionValueWithAbsolutePaths(option, value, toAbsolutePath) {
  if (option && !isNullOrUndefined(value)) {
    if (option.type ***REMOVED******REMOVED******REMOVED*** "list") {
      const values ***REMOVED*** value;
      if (option.element.isFilePath && values.length) {
        return values.map(toAbsolutePath);
      }
    } else if (option.isFilePath) {
      return toAbsolutePath(value);
    }
    Debug.assert(option.type !***REMOVED******REMOVED*** "listOrElement");
  }
  return value;
}
function parseJsonSourceFileConfigFileContent(sourceFile, host, basePath, existingOptions, configFileName, resolutionStack, extraFileExtensions, extendedConfigCache, existingWatchOptions) {
  var _a, _b;
  (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Parse, "parseJsonSourceFileConfigFileContent", { path: sourceFile.fileName });
  const result ***REMOVED*** parseJsonConfigFileContentWorker(
    /*json*/
    void 0,
    sourceFile,
    host,
    basePath,
    existingOptions,
    existingWatchOptions,
    configFileName,
    resolutionStack,
    extraFileExtensions,
    extendedConfigCache
  );
  (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  return result;
}
function setConfigFileInOptions(options, configFile) {
  if (configFile) {
    Object.defineProperty(options, "configFile", { enumerable: false, writable: false, value: configFile });
  }
}
function isNullOrUndefined(x) {
  return x ***REMOVED******REMOVED******REMOVED*** void 0 || x ***REMOVED******REMOVED******REMOVED*** null;
}
function directoryOfCombinedPath(fileName, basePath) {
  return getDirectoryPath(getNormalizedAbsolutePath(fileName, basePath));
}
var defaultIncludeSpec ***REMOVED*** "**/*";
function parseJsonConfigFileContentWorker(json, sourceFile, host, basePath, existingOptions ***REMOVED*** {}, existingWatchOptions, configFileName, resolutionStack ***REMOVED*** [], extraFileExtensions ***REMOVED*** [], extendedConfigCache) {
  Debug.assert(json ***REMOVED******REMOVED******REMOVED*** void 0 && sourceFile !***REMOVED******REMOVED*** void 0 || json !***REMOVED******REMOVED*** void 0 && sourceFile ***REMOVED******REMOVED******REMOVED*** void 0);
  const errors ***REMOVED*** [];
  const parsedConfig ***REMOVED*** parseConfig(json, sourceFile, host, basePath, configFileName, resolutionStack, errors, extendedConfigCache);
  const { raw } ***REMOVED*** parsedConfig;
  const options ***REMOVED*** handleOptionConfigDirTemplateSubstitution(
    extend(existingOptions, parsedConfig.options || {}),
    configDirTemplateSubstitutionOptions,
    basePath
  );
  const watchOptions ***REMOVED*** handleWatchOptionsConfigDirTemplateSubstitution(
    existingWatchOptions && parsedConfig.watchOptions ? extend(existingWatchOptions, parsedConfig.watchOptions) : parsedConfig.watchOptions || existingWatchOptions,
    basePath
  );
  options.configFilePath ***REMOVED*** configFileName && normalizeSlashes(configFileName);
  const basePathForFileNames ***REMOVED*** normalizePath(configFileName ? directoryOfCombinedPath(configFileName, basePath) : basePath);
  const configFileSpecs ***REMOVED*** getConfigFileSpecs();
  if (sourceFile) sourceFile.configFileSpecs ***REMOVED*** configFileSpecs;
  setConfigFileInOptions(options, sourceFile);
  return {
    options,
    watchOptions,
    fileNames: getFileNames(basePathForFileNames),
    projectReferences: getProjectReferences(basePathForFileNames),
    typeAcquisition: parsedConfig.typeAcquisition || getDefaultTypeAcquisition(),
    raw,
    errors,
    // Wildcard directories (provided as part of a wildcard path) are stored in a
    // file map that marks whether it was a regular wildcard match (with a `*` or `?` token),
    // or a recursive directory. This information is used by filesystem watchers to monitor for
    // new entries in these paths.
    wildcardDirectories: getWildcardDirectories(configFileSpecs, basePathForFileNames, host.useCaseSensitiveFileNames),
    compileOnSave: !!raw.compileOnSave
  };
  function getConfigFileSpecs() {
    const referencesOfRaw ***REMOVED*** getPropFromRaw("references", (element) ***REMOVED***> typeof element ***REMOVED******REMOVED******REMOVED*** "object", "object");
    const filesSpecs ***REMOVED*** toPropValue(getSpecsFromRaw("files"));
    if (filesSpecs) {
      const hasZeroOrNoReferences ***REMOVED*** referencesOfRaw ***REMOVED******REMOVED******REMOVED*** "no-prop" || isArray(referencesOfRaw) && referencesOfRaw.length ***REMOVED******REMOVED******REMOVED*** 0;
      const hasExtends ***REMOVED*** hasProperty(raw, "extends");
      if (filesSpecs.length ***REMOVED******REMOVED******REMOVED*** 0 && hasZeroOrNoReferences && !hasExtends) {
        if (sourceFile) {
          const fileName ***REMOVED*** configFileName || "tsconfig.json";
          const diagnosticMessage ***REMOVED*** Diagnostics.The_files_list_in_config_file_0_is_empty;
          const nodeValue ***REMOVED*** forEachTsConfigPropArray(sourceFile, "files", (property) ***REMOVED***> property.initializer);
          const error ***REMOVED*** createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, nodeValue, diagnosticMessage, fileName);
          errors.push(error);
        } else {
          createCompilerDiagnosticOnlyIfJson(Diagnostics.The_files_list_in_config_file_0_is_empty, configFileName || "tsconfig.json");
        }
      }
    }
    let includeSpecs ***REMOVED*** toPropValue(getSpecsFromRaw("include"));
    const excludeOfRaw ***REMOVED*** getSpecsFromRaw("exclude");
    let isDefaultIncludeSpec ***REMOVED*** false;
    let excludeSpecs ***REMOVED*** toPropValue(excludeOfRaw);
    if (excludeOfRaw ***REMOVED******REMOVED******REMOVED*** "no-prop") {
      const outDir ***REMOVED*** options.outDir;
      const declarationDir ***REMOVED*** options.declarationDir;
      if (outDir || declarationDir) {
        excludeSpecs ***REMOVED*** filter([outDir, declarationDir], (d) ***REMOVED***> !!d);
      }
    }
    if (filesSpecs ***REMOVED******REMOVED******REMOVED*** void 0 && includeSpecs ***REMOVED******REMOVED******REMOVED*** void 0) {
      includeSpecs ***REMOVED*** [defaultIncludeSpec];
      isDefaultIncludeSpec ***REMOVED*** true;
    }
    let validatedIncludeSpecsBeforeSubstitution, validatedExcludeSpecsBeforeSubstitution;
    let validatedIncludeSpecs, validatedExcludeSpecs;
    if (includeSpecs) {
      validatedIncludeSpecsBeforeSubstitution ***REMOVED*** validateSpecs(
        includeSpecs,
        errors,
        /*disallowTrailingRecursion*/
        true,
        sourceFile,
        "include"
      );
      validatedIncludeSpecs ***REMOVED*** getSubstitutedStringArrayWithConfigDirTemplate(
        validatedIncludeSpecsBeforeSubstitution,
        basePathForFileNames
      ) || validatedIncludeSpecsBeforeSubstitution;
    }
    if (excludeSpecs) {
      validatedExcludeSpecsBeforeSubstitution ***REMOVED*** validateSpecs(
        excludeSpecs,
        errors,
        /*disallowTrailingRecursion*/
        false,
        sourceFile,
        "exclude"
      );
      validatedExcludeSpecs ***REMOVED*** getSubstitutedStringArrayWithConfigDirTemplate(
        validatedExcludeSpecsBeforeSubstitution,
        basePathForFileNames
      ) || validatedExcludeSpecsBeforeSubstitution;
    }
    const validatedFilesSpecBeforeSubstitution ***REMOVED*** filter(filesSpecs, isString);
    const validatedFilesSpec ***REMOVED*** getSubstitutedStringArrayWithConfigDirTemplate(
      validatedFilesSpecBeforeSubstitution,
      basePathForFileNames
    ) || validatedFilesSpecBeforeSubstitution;
    return {
      filesSpecs,
      includeSpecs,
      excludeSpecs,
      validatedFilesSpec,
      validatedIncludeSpecs,
      validatedExcludeSpecs,
      validatedFilesSpecBeforeSubstitution,
      validatedIncludeSpecsBeforeSubstitution,
      validatedExcludeSpecsBeforeSubstitution,
      isDefaultIncludeSpec
    };
  }
  function getFileNames(basePath2) {
    const fileNames ***REMOVED*** getFileNamesFromConfigSpecs(configFileSpecs, basePath2, options, host, extraFileExtensions);
    if (shouldReportNoInputFiles(fileNames, canJsonReportNoInputFiles(raw), resolutionStack)) {
      errors.push(getErrorForNoInputFiles(configFileSpecs, configFileName));
    }
    return fileNames;
  }
  function getProjectReferences(basePath2) {
    let projectReferences;
    const referencesOfRaw ***REMOVED*** getPropFromRaw("references", (element) ***REMOVED***> typeof element ***REMOVED******REMOVED******REMOVED*** "object", "object");
    if (isArray(referencesOfRaw)) {
      for (const ref of referencesOfRaw) {
        if (typeof ref.path !***REMOVED******REMOVED*** "string") {
          createCompilerDiagnosticOnlyIfJson(Diagnostics.Compiler_option_0_requires_a_value_of_type_1, "reference.path", "string");
        } else {
          (projectReferences || (projectReferences ***REMOVED*** [])).push({
            path: getNormalizedAbsolutePath(ref.path, basePath2),
            originalPath: ref.path,
            prepend: ref.prepend,
            circular: ref.circular
          });
        }
      }
    }
    return projectReferences;
  }
  function toPropValue(specResult) {
    return isArray(specResult) ? specResult : void 0;
  }
  function getSpecsFromRaw(prop) {
    return getPropFromRaw(prop, isString, "string");
  }
  function getPropFromRaw(prop, validateElement, elementTypeName) {
    if (hasProperty(raw, prop) && !isNullOrUndefined(raw[prop])) {
      if (isArray(raw[prop])) {
        const result ***REMOVED*** raw[prop];
        if (!sourceFile && !every(result, validateElement)) {
          errors.push(createCompilerDiagnostic(Diagnostics.Compiler_option_0_requires_a_value_of_type_1, prop, elementTypeName));
        }
        return result;
      } else {
        createCompilerDiagnosticOnlyIfJson(Diagnostics.Compiler_option_0_requires_a_value_of_type_1, prop, "Array");
        return "not-array";
      }
    }
    return "no-prop";
  }
  function createCompilerDiagnosticOnlyIfJson(message, ...args) {
    if (!sourceFile) {
      errors.push(createCompilerDiagnostic(message, ...args));
    }
  }
}
function handleWatchOptionsConfigDirTemplateSubstitution(watchOptions, basePath) {
  return handleOptionConfigDirTemplateSubstitution(watchOptions, configDirTemplateSubstitutionWatchOptions, basePath);
}
function handleOptionConfigDirTemplateSubstitution(options, optionDeclarations2, basePath) {
  if (!options) return options;
  let result;
  for (const option of optionDeclarations2) {
    if (options[option.name] !***REMOVED******REMOVED*** void 0) {
      const value ***REMOVED*** options[option.name];
      switch (option.type) {
        case "string":
          Debug.assert(option.isFilePath);
          if (startsWithConfigDirTemplate(value)) {
            setOptionValue(option, getSubstitutedPathWithConfigDirTemplate(value, basePath));
          }
          break;
        case "list":
          Debug.assert(option.element.isFilePath);
          const listResult ***REMOVED*** getSubstitutedStringArrayWithConfigDirTemplate(value, basePath);
          if (listResult) setOptionValue(option, listResult);
          break;
        case "object":
          Debug.assert(option.name ***REMOVED******REMOVED******REMOVED*** "paths");
          const objectResult ***REMOVED*** getSubstitutedMapLikeOfStringArrayWithConfigDirTemplate(value, basePath);
          if (objectResult) setOptionValue(option, objectResult);
          break;
        default:
          Debug.fail("option type not supported");
      }
    }
  }
  return result || options;
  function setOptionValue(option, value) {
    (result ?? (result ***REMOVED*** assign({}, options)))[option.name] ***REMOVED*** value;
  }
}
var configDirTemplate ***REMOVED*** `\${configDir}`;
function startsWithConfigDirTemplate(value) {
  return isString(value) && startsWith(
    value,
    configDirTemplate,
    /*ignoreCase*/
    true
  );
}
function getSubstitutedPathWithConfigDirTemplate(value, basePath) {
  return getNormalizedAbsolutePath(value.replace(configDirTemplate, "./"), basePath);
}
function getSubstitutedStringArrayWithConfigDirTemplate(list, basePath) {
  if (!list) return list;
  let result;
  list.forEach((element, index) ***REMOVED***> {
    if (!startsWithConfigDirTemplate(element)) return;
    (result ?? (result ***REMOVED*** list.slice()))[index] ***REMOVED*** getSubstitutedPathWithConfigDirTemplate(element, basePath);
  });
  return result;
}
function getSubstitutedMapLikeOfStringArrayWithConfigDirTemplate(mapLike, basePath) {
  let result;
  const ownKeys ***REMOVED*** getOwnKeys(mapLike);
  ownKeys.forEach((key) ***REMOVED***> {
    if (!isArray(mapLike[key])) return;
    const subStitution ***REMOVED*** getSubstitutedStringArrayWithConfigDirTemplate(mapLike[key], basePath);
    if (!subStitution) return;
    (result ?? (result ***REMOVED*** assign({}, mapLike)))[key] ***REMOVED*** subStitution;
  });
  return result;
}
function isErrorNoInputFiles(error) {
  return error.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.No_inputs_were_found_in_config_file_0_Specified_include_paths_were_1_and_exclude_paths_were_2.code;
}
function getErrorForNoInputFiles({ includeSpecs, excludeSpecs }, configFileName) {
  return createCompilerDiagnostic(
    Diagnostics.No_inputs_were_found_in_config_file_0_Specified_include_paths_were_1_and_exclude_paths_were_2,
    configFileName || "tsconfig.json",
    JSON.stringify(includeSpecs || []),
    JSON.stringify(excludeSpecs || [])
  );
}
function shouldReportNoInputFiles(fileNames, canJsonReportNoInutFiles, resolutionStack) {
  return fileNames.length ***REMOVED******REMOVED******REMOVED*** 0 && canJsonReportNoInutFiles && (!resolutionStack || resolutionStack.length ***REMOVED******REMOVED******REMOVED*** 0);
}
function isSolutionConfig(config) {
  return !config.fileNames.length && hasProperty(config.raw, "references");
}
function canJsonReportNoInputFiles(raw) {
  return !hasProperty(raw, "files") && !hasProperty(raw, "references");
}
function updateErrorForNoInputFiles(fileNames, configFileName, configFileSpecs, configParseDiagnostics, canJsonReportNoInutFiles) {
  const existingErrors ***REMOVED*** configParseDiagnostics.length;
  if (shouldReportNoInputFiles(fileNames, canJsonReportNoInutFiles)) {
    configParseDiagnostics.push(getErrorForNoInputFiles(configFileSpecs, configFileName));
  } else {
    filterMutate(configParseDiagnostics, (error) ***REMOVED***> !isErrorNoInputFiles(error));
  }
  return existingErrors !***REMOVED******REMOVED*** configParseDiagnostics.length;
}
function isSuccessfulParsedTsconfig(value) {
  return !!value.options;
}
function parseConfig(json, sourceFile, host, basePath, configFileName, resolutionStack, errors, extendedConfigCache) {
  var _a;
  basePath ***REMOVED*** normalizeSlashes(basePath);
  const resolvedPath ***REMOVED*** getNormalizedAbsolutePath(configFileName || "", basePath);
  if (resolutionStack.includes(resolvedPath)) {
    errors.push(createCompilerDiagnostic(Diagnostics.Circularity_detected_while_resolving_configuration_Colon_0, [...resolutionStack, resolvedPath].join(" -> ")));
    return { raw: json || convertToObject(sourceFile, errors) };
  }
  const ownConfig ***REMOVED*** json ? parseOwnConfigOfJson(json, host, basePath, configFileName, errors) : parseOwnConfigOfJsonSourceFile(sourceFile, host, basePath, configFileName, errors);
  if ((_a ***REMOVED*** ownConfig.options) ***REMOVED******REMOVED*** null ? void 0 : _a.paths) {
    ownConfig.options.pathsBasePath ***REMOVED*** basePath;
  }
  if (ownConfig.extendedConfigPath) {
    resolutionStack ***REMOVED*** resolutionStack.concat([resolvedPath]);
    const result ***REMOVED*** { options: {} };
    if (isString(ownConfig.extendedConfigPath)) {
      applyExtendedConfig(result, ownConfig.extendedConfigPath);
    } else {
      ownConfig.extendedConfigPath.forEach((extendedConfigPath) ***REMOVED***> applyExtendedConfig(result, extendedConfigPath));
    }
    if (result.include) ownConfig.raw.include ***REMOVED*** result.include;
    if (result.exclude) ownConfig.raw.exclude ***REMOVED*** result.exclude;
    if (result.files) ownConfig.raw.files ***REMOVED*** result.files;
    if (ownConfig.raw.compileOnSave ***REMOVED******REMOVED******REMOVED*** void 0 && result.compileOnSave) ownConfig.raw.compileOnSave ***REMOVED*** result.compileOnSave;
    if (sourceFile && result.extendedSourceFiles) sourceFile.extendedSourceFiles ***REMOVED*** arrayFrom(result.extendedSourceFiles.keys());
    ownConfig.options ***REMOVED*** assign(result.options, ownConfig.options);
    ownConfig.watchOptions ***REMOVED*** ownConfig.watchOptions && result.watchOptions ? assignWatchOptions(result, ownConfig.watchOptions) : ownConfig.watchOptions || result.watchOptions;
  }
  return ownConfig;
  function applyExtendedConfig(result, extendedConfigPath) {
    const extendedConfig ***REMOVED*** getExtendedConfig(sourceFile, extendedConfigPath, host, resolutionStack, errors, extendedConfigCache, result);
    if (extendedConfig && isSuccessfulParsedTsconfig(extendedConfig)) {
      const extendsRaw ***REMOVED*** extendedConfig.raw;
      let relativeDifference;
      const setPropertyInResultIfNotUndefined ***REMOVED*** (propertyName) ***REMOVED***> {
        if (ownConfig.raw[propertyName]) return;
        if (extendsRaw[propertyName]) {
          result[propertyName] ***REMOVED*** map(extendsRaw[propertyName], (path) ***REMOVED***> startsWithConfigDirTemplate(path) || isRootedDiskPath(path) ? path : combinePaths(
            relativeDifference || (relativeDifference ***REMOVED*** convertToRelativePath(getDirectoryPath(extendedConfigPath), basePath, createGetCanonicalFileName(host.useCaseSensitiveFileNames))),
            path
          ));
        }
      };
      setPropertyInResultIfNotUndefined("include");
      setPropertyInResultIfNotUndefined("exclude");
      setPropertyInResultIfNotUndefined("files");
      if (extendsRaw.compileOnSave !***REMOVED******REMOVED*** void 0) {
        result.compileOnSave ***REMOVED*** extendsRaw.compileOnSave;
      }
      assign(result.options, extendedConfig.options);
      result.watchOptions ***REMOVED*** result.watchOptions && extendedConfig.watchOptions ? assignWatchOptions(result, extendedConfig.watchOptions) : result.watchOptions || extendedConfig.watchOptions;
    }
  }
  function assignWatchOptions(result, watchOptions) {
    if (result.watchOptionsCopied) return assign(result.watchOptions, watchOptions);
    result.watchOptionsCopied ***REMOVED*** true;
    return assign({}, result.watchOptions, watchOptions);
  }
}
function parseOwnConfigOfJson(json, host, basePath, configFileName, errors) {
  if (hasProperty(json, "excludes")) {
    errors.push(createCompilerDiagnostic(Diagnostics.Unknown_option_excludes_Did_you_mean_exclude));
  }
  const options ***REMOVED*** convertCompilerOptionsFromJsonWorker(json.compilerOptions, basePath, errors, configFileName);
  const typeAcquisition ***REMOVED*** convertTypeAcquisitionFromJsonWorker(json.typeAcquisition, basePath, errors, configFileName);
  const watchOptions ***REMOVED*** convertWatchOptionsFromJsonWorker(json.watchOptions, basePath, errors);
  json.compileOnSave ***REMOVED*** convertCompileOnSaveOptionFromJson(json, basePath, errors);
  const extendedConfigPath ***REMOVED*** json.extends || json.extends ***REMOVED******REMOVED******REMOVED*** "" ? getExtendsConfigPathOrArray(json.extends, host, basePath, configFileName, errors) : void 0;
  return { raw: json, options, watchOptions, typeAcquisition, extendedConfigPath };
}
function getExtendsConfigPathOrArray(value, host, basePath, configFileName, errors, propertyAssignment, valueExpression, sourceFile) {
  let extendedConfigPath;
  const newBase ***REMOVED*** configFileName ? directoryOfCombinedPath(configFileName, basePath) : basePath;
  if (isString(value)) {
    extendedConfigPath ***REMOVED*** getExtendsConfigPath(
      value,
      host,
      newBase,
      errors,
      valueExpression,
      sourceFile
    );
  } else if (isArray(value)) {
    extendedConfigPath ***REMOVED*** [];
    for (let index ***REMOVED*** 0; index < value.length; index++) {
      const fileName ***REMOVED*** value[index];
      if (isString(fileName)) {
        extendedConfigPath ***REMOVED*** append(
          extendedConfigPath,
          getExtendsConfigPath(
            fileName,
            host,
            newBase,
            errors,
            valueExpression ***REMOVED******REMOVED*** null ? void 0 : valueExpression.elements[index],
            sourceFile
          )
        );
      } else {
        convertJsonOption(extendsOptionDeclaration.element, value, basePath, errors, propertyAssignment, valueExpression ***REMOVED******REMOVED*** null ? void 0 : valueExpression.elements[index], sourceFile);
      }
    }
  } else {
    convertJsonOption(extendsOptionDeclaration, value, basePath, errors, propertyAssignment, valueExpression, sourceFile);
  }
  return extendedConfigPath;
}
function parseOwnConfigOfJsonSourceFile(sourceFile, host, basePath, configFileName, errors) {
  const options ***REMOVED*** getDefaultCompilerOptions(configFileName);
  let typeAcquisition;
  let watchOptions;
  let extendedConfigPath;
  let rootCompilerOptions;
  const rootOptions ***REMOVED*** getTsconfigRootOptionsMap();
  const json ***REMOVED*** convertConfigFileToObject(
    sourceFile,
    errors,
    { rootOptions, onPropertySet }
  );
  if (!typeAcquisition) {
    typeAcquisition ***REMOVED*** getDefaultTypeAcquisition(configFileName);
  }
  if (rootCompilerOptions && json && json.compilerOptions ***REMOVED******REMOVED******REMOVED*** void 0) {
    errors.push(createDiagnosticForNodeInSourceFile(sourceFile, rootCompilerOptions[0], Diagnostics._0_should_be_set_inside_the_compilerOptions_object_of_the_config_json_file, getTextOfPropertyName(rootCompilerOptions[0])));
  }
  return { raw: json, options, watchOptions, typeAcquisition, extendedConfigPath };
  function onPropertySet(keyText, value, propertyAssignment, parentOption, option) {
    if (option && option !***REMOVED******REMOVED*** extendsOptionDeclaration) value ***REMOVED*** convertJsonOption(option, value, basePath, errors, propertyAssignment, propertyAssignment.initializer, sourceFile);
    if (parentOption ***REMOVED******REMOVED*** null ? void 0 : parentOption.name) {
      if (option) {
        let currentOption;
        if (parentOption ***REMOVED******REMOVED******REMOVED*** compilerOptionsDeclaration) currentOption ***REMOVED*** options;
        else if (parentOption ***REMOVED******REMOVED******REMOVED*** watchOptionsDeclaration) currentOption ***REMOVED*** watchOptions ?? (watchOptions ***REMOVED*** {});
        else if (parentOption ***REMOVED******REMOVED******REMOVED*** typeAcquisitionDeclaration) currentOption ***REMOVED*** typeAcquisition ?? (typeAcquisition ***REMOVED*** getDefaultTypeAcquisition(configFileName));
        else Debug.fail("Unknown option");
        currentOption[option.name] ***REMOVED*** value;
      } else if (keyText && (parentOption ***REMOVED******REMOVED*** null ? void 0 : parentOption.extraKeyDiagnostics)) {
        if (parentOption.elementOptions) {
          errors.push(createUnknownOptionError(
            keyText,
            parentOption.extraKeyDiagnostics,
            /*unknownOptionErrorText*/
            void 0,
            propertyAssignment.name,
            sourceFile
          ));
        } else {
          errors.push(createDiagnosticForNodeInSourceFile(sourceFile, propertyAssignment.name, parentOption.extraKeyDiagnostics.unknownOptionDiagnostic, keyText));
        }
      }
    } else if (parentOption ***REMOVED******REMOVED******REMOVED*** rootOptions) {
      if (option ***REMOVED******REMOVED******REMOVED*** extendsOptionDeclaration) {
        extendedConfigPath ***REMOVED*** getExtendsConfigPathOrArray(value, host, basePath, configFileName, errors, propertyAssignment, propertyAssignment.initializer, sourceFile);
      } else if (!option) {
        if (keyText ***REMOVED******REMOVED******REMOVED*** "excludes") {
          errors.push(createDiagnosticForNodeInSourceFile(sourceFile, propertyAssignment.name, Diagnostics.Unknown_option_excludes_Did_you_mean_exclude));
        }
        if (find(commandOptionsWithoutBuild, (opt) ***REMOVED***> opt.name ***REMOVED******REMOVED******REMOVED*** keyText)) {
          rootCompilerOptions ***REMOVED*** append(rootCompilerOptions, propertyAssignment.name);
        }
      }
    }
  }
}
function getExtendsConfigPath(extendedConfig, host, basePath, errors, valueExpression, sourceFile) {
  extendedConfig ***REMOVED*** normalizeSlashes(extendedConfig);
  if (isRootedDiskPath(extendedConfig) || startsWith(extendedConfig, "./") || startsWith(extendedConfig, "../")) {
    let extendedConfigPath ***REMOVED*** getNormalizedAbsolutePath(extendedConfig, basePath);
    if (!host.fileExists(extendedConfigPath) && !endsWith(extendedConfigPath, ".json" /* Json */)) {
      extendedConfigPath ***REMOVED*** `${extendedConfigPath}.json`;
      if (!host.fileExists(extendedConfigPath)) {
        errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, Diagnostics.File_0_not_found, extendedConfig));
        return void 0;
      }
    }
    return extendedConfigPath;
  }
  const resolved ***REMOVED*** nodeNextJsonConfigResolver(extendedConfig, combinePaths(basePath, "tsconfig.json"), host);
  if (resolved.resolvedModule) {
    return resolved.resolvedModule.resolvedFileName;
  }
  if (extendedConfig ***REMOVED******REMOVED******REMOVED*** "") {
    errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, Diagnostics.Compiler_option_0_cannot_be_given_an_empty_string, "extends"));
  } else {
    errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, Diagnostics.File_0_not_found, extendedConfig));
  }
  return void 0;
}
function getExtendedConfig(sourceFile, extendedConfigPath, host, resolutionStack, errors, extendedConfigCache, result) {
  const path ***REMOVED*** host.useCaseSensitiveFileNames ? extendedConfigPath : toFileNameLowerCase(extendedConfigPath);
  let value;
  let extendedResult;
  let extendedConfig;
  if (extendedConfigCache && (value ***REMOVED*** extendedConfigCache.get(path))) {
    ({ extendedResult, extendedConfig } ***REMOVED*** value);
  } else {
    extendedResult ***REMOVED*** readJsonConfigFile(extendedConfigPath, (path2) ***REMOVED***> host.readFile(path2));
    if (!extendedResult.parseDiagnostics.length) {
      extendedConfig ***REMOVED*** parseConfig(
        /*json*/
        void 0,
        extendedResult,
        host,
        getDirectoryPath(extendedConfigPath),
        getBaseFileName(extendedConfigPath),
        resolutionStack,
        errors,
        extendedConfigCache
      );
    }
    if (extendedConfigCache) {
      extendedConfigCache.set(path, { extendedResult, extendedConfig });
    }
  }
  if (sourceFile) {
    (result.extendedSourceFiles ?? (result.extendedSourceFiles ***REMOVED*** /* @__PURE__ */ new Set())).add(extendedResult.fileName);
    if (extendedResult.extendedSourceFiles) {
      for (const extenedSourceFile of extendedResult.extendedSourceFiles) {
        result.extendedSourceFiles.add(extenedSourceFile);
      }
    }
  }
  if (extendedResult.parseDiagnostics.length) {
    errors.push(...extendedResult.parseDiagnostics);
    return void 0;
  }
  return extendedConfig;
}
function convertCompileOnSaveOptionFromJson(jsonOption, basePath, errors) {
  if (!hasProperty(jsonOption, compileOnSaveCommandLineOption.name)) {
    return false;
  }
  const result ***REMOVED*** convertJsonOption(compileOnSaveCommandLineOption, jsonOption.compileOnSave, basePath, errors);
  return typeof result ***REMOVED******REMOVED******REMOVED*** "boolean" && result;
}
function getDefaultCompilerOptions(configFileName) {
  const options ***REMOVED*** configFileName && getBaseFileName(configFileName) ***REMOVED******REMOVED******REMOVED*** "jsconfig.json" ? { allowJs: true, maxNodeModuleJsDepth: 2, allowSyntheticDefaultImports: true, skipLibCheck: true, noEmit: true } : {};
  return options;
}
function convertCompilerOptionsFromJsonWorker(jsonOptions, basePath, errors, configFileName) {
  const options ***REMOVED*** getDefaultCompilerOptions(configFileName);
  convertOptionsFromJson(getCommandLineCompilerOptionsMap(), jsonOptions, basePath, options, compilerOptionsDidYouMeanDiagnostics, errors);
  if (configFileName) {
    options.configFilePath ***REMOVED*** normalizeSlashes(configFileName);
  }
  return options;
}
function getDefaultTypeAcquisition(configFileName) {
  return { enable: !!configFileName && getBaseFileName(configFileName) ***REMOVED******REMOVED******REMOVED*** "jsconfig.json", include: [], exclude: [] };
}
function convertTypeAcquisitionFromJsonWorker(jsonOptions, basePath, errors, configFileName) {
  const options ***REMOVED*** getDefaultTypeAcquisition(configFileName);
  convertOptionsFromJson(getCommandLineTypeAcquisitionMap(), jsonOptions, basePath, options, typeAcquisitionDidYouMeanDiagnostics, errors);
  return options;
}
function convertWatchOptionsFromJsonWorker(jsonOptions, basePath, errors) {
  return convertOptionsFromJson(
    getCommandLineWatchOptionsMap(),
    jsonOptions,
    basePath,
    /*defaultOptions*/
    void 0,
    watchOptionsDidYouMeanDiagnostics,
    errors
  );
}
function convertOptionsFromJson(optionsNameMap, jsonOptions, basePath, defaultOptions, diagnostics, errors) {
  if (!jsonOptions) {
    return;
  }
  for (const id in jsonOptions) {
    const opt ***REMOVED*** optionsNameMap.get(id);
    if (opt) {
      (defaultOptions || (defaultOptions ***REMOVED*** {}))[opt.name] ***REMOVED*** convertJsonOption(opt, jsonOptions[id], basePath, errors);
    } else {
      errors.push(createUnknownOptionError(id, diagnostics));
    }
  }
  return defaultOptions;
}
function createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, node, message, ...args) {
  return sourceFile && node ? createDiagnosticForNodeInSourceFile(sourceFile, node, message, ...args) : createCompilerDiagnostic(message, ...args);
}
function convertJsonOption(opt, value, basePath, errors, propertyAssignment, valueExpression, sourceFile) {
  if (opt.isCommandLineOnly) {
    errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, propertyAssignment ***REMOVED******REMOVED*** null ? void 0 : propertyAssignment.name, Diagnostics.Option_0_can_only_be_specified_on_command_line, opt.name));
    return void 0;
  }
  if (isCompilerOptionsValue(opt, value)) {
    const optType ***REMOVED*** opt.type;
    if (optType ***REMOVED******REMOVED******REMOVED*** "list" && isArray(value)) {
      return convertJsonOptionOfListType(opt, value, basePath, errors, propertyAssignment, valueExpression, sourceFile);
    } else if (optType ***REMOVED******REMOVED******REMOVED*** "listOrElement") {
      return isArray(value) ? convertJsonOptionOfListType(opt, value, basePath, errors, propertyAssignment, valueExpression, sourceFile) : convertJsonOption(opt.element, value, basePath, errors, propertyAssignment, valueExpression, sourceFile);
    } else if (!isString(opt.type)) {
      return convertJsonOptionOfCustomType(opt, value, errors, valueExpression, sourceFile);
    }
    const validatedValue ***REMOVED*** validateJsonOptionValue(opt, value, errors, valueExpression, sourceFile);
    return isNullOrUndefined(validatedValue) ? validatedValue : normalizeNonListOptionValue(opt, basePath, validatedValue);
  } else {
    errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, Diagnostics.Compiler_option_0_requires_a_value_of_type_1, opt.name, getCompilerOptionValueTypeString(opt)));
  }
}
function normalizeNonListOptionValue(option, basePath, value) {
  if (option.isFilePath) {
    value ***REMOVED*** normalizeSlashes(value);
    value ***REMOVED*** !startsWithConfigDirTemplate(value) ? getNormalizedAbsolutePath(value, basePath) : value;
    if (value ***REMOVED******REMOVED******REMOVED*** "") {
      value ***REMOVED*** ".";
    }
  }
  return value;
}
function validateJsonOptionValue(opt, value, errors, valueExpression, sourceFile) {
  var _a;
  if (isNullOrUndefined(value)) return void 0;
  const d ***REMOVED*** (_a ***REMOVED*** opt.extraValidation) ***REMOVED******REMOVED*** null ? void 0 : _a.call(opt, value);
  if (!d) return value;
  errors.push(createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, ...d));
  return void 0;
}
function convertJsonOptionOfCustomType(opt, value, errors, valueExpression, sourceFile) {
  if (isNullOrUndefined(value)) return void 0;
  const key ***REMOVED*** value.toLowerCase();
  const val ***REMOVED*** opt.type.get(key);
  if (val !***REMOVED******REMOVED*** void 0) {
    return validateJsonOptionValue(opt, val, errors, valueExpression, sourceFile);
  } else {
    errors.push(createDiagnosticForInvalidCustomType(opt, (message, ...args) ***REMOVED***> createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(sourceFile, valueExpression, message, ...args)));
  }
}
function convertJsonOptionOfListType(option, values, basePath, errors, propertyAssignment, valueExpression, sourceFile) {
  return filter(map(values, (v, index) ***REMOVED***> convertJsonOption(option.element, v, basePath, errors, propertyAssignment, valueExpression ***REMOVED******REMOVED*** null ? void 0 : valueExpression.elements[index], sourceFile)), (v) ***REMOVED***> option.listPreserveFalsyValues ? true : !!v);
}
var invalidTrailingRecursionPattern ***REMOVED*** /(?:^|\/)\*\*\/?$/;
var wildcardDirectoryPattern ***REMOVED*** /^[^*?]*(?***REMOVED***\/[^/]*[*?])/;
function getFileNamesFromConfigSpecs(configFileSpecs, basePath, options, host, extraFileExtensions ***REMOVED*** emptyArray) {
  basePath ***REMOVED*** normalizePath(basePath);
  const keyMapper ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames);
  const literalFileMap ***REMOVED*** /* @__PURE__ */ new Map();
  const wildcardFileMap ***REMOVED*** /* @__PURE__ */ new Map();
  const wildCardJsonFileMap ***REMOVED*** /* @__PURE__ */ new Map();
  const { validatedFilesSpec, validatedIncludeSpecs, validatedExcludeSpecs } ***REMOVED*** configFileSpecs;
  const supportedExtensions ***REMOVED*** getSupportedExtensions(options, extraFileExtensions);
  const supportedExtensionsWithJsonIfResolveJsonModule ***REMOVED*** getSupportedExtensionsWithJsonIfResolveJsonModule(options, supportedExtensions);
  if (validatedFilesSpec) {
    for (const fileName of validatedFilesSpec) {
      const file ***REMOVED*** getNormalizedAbsolutePath(fileName, basePath);
      literalFileMap.set(keyMapper(file), file);
    }
  }
  let jsonOnlyIncludeRegexes;
  if (validatedIncludeSpecs && validatedIncludeSpecs.length > 0) {
    for (const file of host.readDirectory(
      basePath,
      flatten(supportedExtensionsWithJsonIfResolveJsonModule),
      validatedExcludeSpecs,
      validatedIncludeSpecs,
      /*depth*/
      void 0
    )) {
      if (fileExtensionIs(file, ".json" /* Json */)) {
        if (!jsonOnlyIncludeRegexes) {
          const includes ***REMOVED*** validatedIncludeSpecs.filter((s) ***REMOVED***> endsWith(s, ".json" /* Json */));
          const includeFilePatterns ***REMOVED*** map(getRegularExpressionsForWildcards(includes, basePath, "files"), (pattern) ***REMOVED***> `^${pattern}$`);
          jsonOnlyIncludeRegexes ***REMOVED*** includeFilePatterns ? includeFilePatterns.map((pattern) ***REMOVED***> getRegexFromPattern(pattern, host.useCaseSensitiveFileNames)) : emptyArray;
        }
        const includeIndex ***REMOVED*** findIndex(jsonOnlyIncludeRegexes, (re) ***REMOVED***> re.test(file));
        if (includeIndex !***REMOVED******REMOVED*** -1) {
          const key2 ***REMOVED*** keyMapper(file);
          if (!literalFileMap.has(key2) && !wildCardJsonFileMap.has(key2)) {
            wildCardJsonFileMap.set(key2, file);
          }
        }
        continue;
      }
      if (hasFileWithHigherPriorityExtension(file, literalFileMap, wildcardFileMap, supportedExtensions, keyMapper)) {
        continue;
      }
      removeWildcardFilesWithLowerPriorityExtension(file, wildcardFileMap, supportedExtensions, keyMapper);
      const key ***REMOVED*** keyMapper(file);
      if (!literalFileMap.has(key) && !wildcardFileMap.has(key)) {
        wildcardFileMap.set(key, file);
      }
    }
  }
  const literalFiles ***REMOVED*** arrayFrom(literalFileMap.values());
  const wildcardFiles ***REMOVED*** arrayFrom(wildcardFileMap.values());
  return literalFiles.concat(wildcardFiles, arrayFrom(wildCardJsonFileMap.values()));
}
function isExcludedFile(pathToCheck, spec, basePath, useCaseSensitiveFileNames2, currentDirectory) {
  const { validatedFilesSpec, validatedIncludeSpecs, validatedExcludeSpecs } ***REMOVED*** spec;
  if (!length(validatedIncludeSpecs) || !length(validatedExcludeSpecs)) return false;
  basePath ***REMOVED*** normalizePath(basePath);
  const keyMapper ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  if (validatedFilesSpec) {
    for (const fileName of validatedFilesSpec) {
      if (keyMapper(getNormalizedAbsolutePath(fileName, basePath)) ***REMOVED******REMOVED******REMOVED*** pathToCheck) return false;
    }
  }
  return matchesExcludeWorker(pathToCheck, validatedExcludeSpecs, useCaseSensitiveFileNames2, currentDirectory, basePath);
}
function invalidDotDotAfterRecursiveWildcard(s) {
  const wildcardIndex ***REMOVED*** startsWith(s, "**/") ? 0 : s.indexOf("/**/");
  if (wildcardIndex ***REMOVED******REMOVED******REMOVED*** -1) {
    return false;
  }
  const lastDotIndex ***REMOVED*** endsWith(s, "/..") ? s.length : s.lastIndexOf("/../");
  return lastDotIndex > wildcardIndex;
}
function matchesExclude(pathToCheck, excludeSpecs, useCaseSensitiveFileNames2, currentDirectory) {
  return matchesExcludeWorker(
    pathToCheck,
    filter(excludeSpecs, (spec) ***REMOVED***> !invalidDotDotAfterRecursiveWildcard(spec)),
    useCaseSensitiveFileNames2,
    currentDirectory
  );
}
function matchesExcludeWorker(pathToCheck, excludeSpecs, useCaseSensitiveFileNames2, currentDirectory, basePath) {
  const excludePattern ***REMOVED*** getRegularExpressionForWildcard(excludeSpecs, combinePaths(normalizePath(currentDirectory), basePath), "exclude");
  const excludeRegex ***REMOVED*** excludePattern && getRegexFromPattern(excludePattern, useCaseSensitiveFileNames2);
  if (!excludeRegex) return false;
  if (excludeRegex.test(pathToCheck)) return true;
  return !hasExtension(pathToCheck) && excludeRegex.test(ensureTrailingDirectorySeparator(pathToCheck));
}
function validateSpecs(specs, errors, disallowTrailingRecursion, jsonSourceFile, specKey) {
  return specs.filter((spec) ***REMOVED***> {
    if (!isString(spec)) return false;
    const diag2 ***REMOVED*** specToDiagnostic(spec, disallowTrailingRecursion);
    if (diag2 !***REMOVED******REMOVED*** void 0) {
      errors.push(createDiagnostic(...diag2));
    }
    return diag2 ***REMOVED******REMOVED******REMOVED*** void 0;
  });
  function createDiagnostic(message, spec) {
    const element ***REMOVED*** getTsConfigPropArrayElementValue(jsonSourceFile, specKey, spec);
    return createDiagnosticForNodeInSourceFileOrCompilerDiagnostic(jsonSourceFile, element, message, spec);
  }
}
function specToDiagnostic(spec, disallowTrailingRecursion) {
  Debug.assert(typeof spec ***REMOVED******REMOVED******REMOVED*** "string");
  if (disallowTrailingRecursion && invalidTrailingRecursionPattern.test(spec)) {
    return [Diagnostics.File_specification_cannot_end_in_a_recursive_directory_wildcard_Asterisk_Asterisk_Colon_0, spec];
  } else if (invalidDotDotAfterRecursiveWildcard(spec)) {
    return [Diagnostics.File_specification_cannot_contain_a_parent_directory_that_appears_after_a_recursive_directory_wildcard_Asterisk_Asterisk_Colon_0, spec];
  }
}
function getWildcardDirectories({ validatedIncludeSpecs: include, validatedExcludeSpecs: exclude }, basePath, useCaseSensitiveFileNames2) {
  const rawExcludeRegex ***REMOVED*** getRegularExpressionForWildcard(exclude, basePath, "exclude");
  const excludeRegex ***REMOVED*** rawExcludeRegex && new RegExp(rawExcludeRegex, useCaseSensitiveFileNames2 ? "" : "i");
  const wildcardDirectories ***REMOVED*** {};
  const wildCardKeyToPath ***REMOVED*** /* @__PURE__ */ new Map();
  if (include !***REMOVED******REMOVED*** void 0) {
    const recursiveKeys ***REMOVED*** [];
    for (const file of include) {
      const spec ***REMOVED*** normalizePath(combinePaths(basePath, file));
      if (excludeRegex && excludeRegex.test(spec)) {
        continue;
      }
      const match ***REMOVED*** getWildcardDirectoryFromSpec(spec, useCaseSensitiveFileNames2);
      if (match) {
        const { key, path, flags } ***REMOVED*** match;
        const existingPath ***REMOVED*** wildCardKeyToPath.get(key);
        const existingFlags ***REMOVED*** existingPath !***REMOVED******REMOVED*** void 0 ? wildcardDirectories[existingPath] : void 0;
        if (existingFlags ***REMOVED******REMOVED******REMOVED*** void 0 || existingFlags < flags) {
          wildcardDirectories[existingPath !***REMOVED******REMOVED*** void 0 ? existingPath : path] ***REMOVED*** flags;
          if (existingPath ***REMOVED******REMOVED******REMOVED*** void 0) wildCardKeyToPath.set(key, path);
          if (flags ***REMOVED******REMOVED******REMOVED*** 1 /* Recursive */) {
            recursiveKeys.push(key);
          }
        }
      }
    }
    for (const path in wildcardDirectories) {
      if (hasProperty(wildcardDirectories, path)) {
        for (const recursiveKey of recursiveKeys) {
          const key ***REMOVED*** toCanonicalKey(path, useCaseSensitiveFileNames2);
          if (key !***REMOVED******REMOVED*** recursiveKey && containsPath(recursiveKey, key, basePath, !useCaseSensitiveFileNames2)) {
            delete wildcardDirectories[path];
          }
        }
      }
    }
  }
  return wildcardDirectories;
}
function toCanonicalKey(path, useCaseSensitiveFileNames2) {
  return useCaseSensitiveFileNames2 ? path : toFileNameLowerCase(path);
}
function getWildcardDirectoryFromSpec(spec, useCaseSensitiveFileNames2) {
  const match ***REMOVED*** wildcardDirectoryPattern.exec(spec);
  if (match) {
    const questionWildcardIndex ***REMOVED*** spec.indexOf("?");
    const starWildcardIndex ***REMOVED*** spec.indexOf("*");
    const lastDirectorySeperatorIndex ***REMOVED*** spec.lastIndexOf(directorySeparator);
    return {
      key: toCanonicalKey(match[0], useCaseSensitiveFileNames2),
      path: match[0],
      flags: questionWildcardIndex !***REMOVED******REMOVED*** -1 && questionWildcardIndex < lastDirectorySeperatorIndex || starWildcardIndex !***REMOVED******REMOVED*** -1 && starWildcardIndex < lastDirectorySeperatorIndex ? 1 /* Recursive */ : 0 /* None */
    };
  }
  if (isImplicitGlob(spec.substring(spec.lastIndexOf(directorySeparator) + 1))) {
    const path ***REMOVED*** removeTrailingDirectorySeparator(spec);
    return {
      key: toCanonicalKey(path, useCaseSensitiveFileNames2),
      path,
      flags: 1 /* Recursive */
    };
  }
  return void 0;
}
function hasFileWithHigherPriorityExtension(file, literalFiles, wildcardFiles, extensions, keyMapper) {
  const extensionGroup ***REMOVED*** forEach(extensions, (group2) ***REMOVED***> fileExtensionIsOneOf(file, group2) ? group2 : void 0);
  if (!extensionGroup) {
    return false;
  }
  for (const ext of extensionGroup) {
    if (fileExtensionIs(file, ext) && (ext !***REMOVED******REMOVED*** ".ts" /* Ts */ || !fileExtensionIs(file, ".d.ts" /* Dts */))) {
      return false;
    }
    const higherPriorityPath ***REMOVED*** keyMapper(changeExtension(file, ext));
    if (literalFiles.has(higherPriorityPath) || wildcardFiles.has(higherPriorityPath)) {
      if (ext ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */ && (fileExtensionIs(file, ".js" /* Js */) || fileExtensionIs(file, ".jsx" /* Jsx */))) {
        continue;
      }
      return true;
    }
  }
  return false;
}
function removeWildcardFilesWithLowerPriorityExtension(file, wildcardFiles, extensions, keyMapper) {
  const extensionGroup ***REMOVED*** forEach(extensions, (group2) ***REMOVED***> fileExtensionIsOneOf(file, group2) ? group2 : void 0);
  if (!extensionGroup) {
    return;
  }
  for (let i ***REMOVED*** extensionGroup.length - 1; i >***REMOVED*** 0; i--) {
    const ext ***REMOVED*** extensionGroup[i];
    if (fileExtensionIs(file, ext)) {
      return;
    }
    const lowerPriorityPath ***REMOVED*** keyMapper(changeExtension(file, ext));
    wildcardFiles.delete(lowerPriorityPath);
  }
}

// src/compiler/moduleNameResolver.ts
function trace(host, message, ...args) {
  host.trace(formatMessage(message, ...args));
}
function isTraceEnabled(compilerOptions, host) {
  return !!compilerOptions.traceResolution && host.trace !***REMOVED******REMOVED*** void 0;
}
function withPackageId(packageInfo, r, state) {
  let packageId;
  if (r && packageInfo) {
    const packageJsonContent ***REMOVED*** packageInfo.contents.packageJsonContent;
    if (typeof packageJsonContent.name ***REMOVED******REMOVED******REMOVED*** "string" && typeof packageJsonContent.version ***REMOVED******REMOVED******REMOVED*** "string") {
      packageId ***REMOVED*** {
        name: packageJsonContent.name,
        subModuleName: r.path.slice(packageInfo.packageDirectory.length + directorySeparator.length),
        version: packageJsonContent.version,
        peerDependencies: getPeerDependenciesOfPackageJsonInfo(packageInfo, state)
      };
    }
  }
  return r && { path: r.path, extension: r.ext, packageId, resolvedUsingTsExtension: r.resolvedUsingTsExtension };
}
function noPackageId(r) {
  return withPackageId(
    /*packageInfo*/
    void 0,
    r,
    /*state*/
    void 0
  );
}
function removeIgnoredPackageId(r) {
  if (r) {
    Debug.assert(r.packageId ***REMOVED******REMOVED******REMOVED*** void 0);
    return { path: r.path, ext: r.extension, resolvedUsingTsExtension: r.resolvedUsingTsExtension };
  }
}
function formatExtensions(extensions) {
  const result ***REMOVED*** [];
  if (extensions & 1 /* TypeScript */) result.push("TypeScript");
  if (extensions & 2 /* JavaScript */) result.push("JavaScript");
  if (extensions & 4 /* Declaration */) result.push("Declaration");
  if (extensions & 8 /* Json */) result.push("JSON");
  return result.join(", ");
}
function resolvedTypeScriptOnly(resolved) {
  if (!resolved) {
    return void 0;
  }
  Debug.assert(extensionIsTS(resolved.extension));
  return { fileName: resolved.path, packageId: resolved.packageId };
}
function createResolvedModuleWithFailedLookupLocationsHandlingSymlink(moduleName, resolved, isExternalLibraryImport, failedLookupLocations, affectingLocations, diagnostics, state, cache, alternateResult) {
  if (!state.resultFromCache && !state.compilerOptions.preserveSymlinks && resolved && isExternalLibraryImport && !resolved.originalPath && !isExternalModuleNameRelative(moduleName)) {
    const { resolvedFileName, originalPath } ***REMOVED*** getOriginalAndResolvedFileName(resolved.path, state.host, state.traceEnabled);
    if (originalPath) resolved ***REMOVED*** { ...resolved, path: resolvedFileName, originalPath };
  }
  return createResolvedModuleWithFailedLookupLocations(
    resolved,
    isExternalLibraryImport,
    failedLookupLocations,
    affectingLocations,
    diagnostics,
    state.resultFromCache,
    cache,
    alternateResult
  );
}
function createResolvedModuleWithFailedLookupLocations(resolved, isExternalLibraryImport, failedLookupLocations, affectingLocations, diagnostics, resultFromCache, cache, alternateResult) {
  if (resultFromCache) {
    if (!(cache ***REMOVED******REMOVED*** null ? void 0 : cache.isReadonly)) {
      resultFromCache.failedLookupLocations ***REMOVED*** updateResolutionField(resultFromCache.failedLookupLocations, failedLookupLocations);
      resultFromCache.affectingLocations ***REMOVED*** updateResolutionField(resultFromCache.affectingLocations, affectingLocations);
      resultFromCache.resolutionDiagnostics ***REMOVED*** updateResolutionField(resultFromCache.resolutionDiagnostics, diagnostics);
      return resultFromCache;
    } else {
      return {
        ...resultFromCache,
        failedLookupLocations: initializeResolutionFieldForReadonlyCache(resultFromCache.failedLookupLocations, failedLookupLocations),
        affectingLocations: initializeResolutionFieldForReadonlyCache(resultFromCache.affectingLocations, affectingLocations),
        resolutionDiagnostics: initializeResolutionFieldForReadonlyCache(resultFromCache.resolutionDiagnostics, diagnostics)
      };
    }
  }
  return {
    resolvedModule: resolved && {
      resolvedFileName: resolved.path,
      originalPath: resolved.originalPath ***REMOVED******REMOVED******REMOVED*** true ? void 0 : resolved.originalPath,
      extension: resolved.extension,
      isExternalLibraryImport,
      packageId: resolved.packageId,
      resolvedUsingTsExtension: !!resolved.resolvedUsingTsExtension
    },
    failedLookupLocations: initializeResolutionField(failedLookupLocations),
    affectingLocations: initializeResolutionField(affectingLocations),
    resolutionDiagnostics: initializeResolutionField(diagnostics),
    alternateResult
  };
}
function initializeResolutionField(value) {
  return value.length ? value : void 0;
}
function updateResolutionField(to, value) {
  if (!(value ***REMOVED******REMOVED*** null ? void 0 : value.length)) return to;
  if (!(to ***REMOVED******REMOVED*** null ? void 0 : to.length)) return value;
  to.push(...value);
  return to;
}
function initializeResolutionFieldForReadonlyCache(fromCache, value) {
  if (!(fromCache ***REMOVED******REMOVED*** null ? void 0 : fromCache.length)) return initializeResolutionField(value);
  if (!value.length) return fromCache.slice();
  return [...fromCache, ...value];
}
function readPackageJsonField(jsonContent, fieldName, typeOfTag, state) {
  if (!hasProperty(jsonContent, fieldName)) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_does_not_have_a_0_field, fieldName);
    }
    return;
  }
  const value ***REMOVED*** jsonContent[fieldName];
  if (typeof value !***REMOVED******REMOVED*** typeOfTag || value ***REMOVED******REMOVED******REMOVED*** null) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Expected_type_of_0_field_in_package_json_to_be_1_got_2, fieldName, typeOfTag, value ***REMOVED******REMOVED******REMOVED*** null ? "null" : typeof value);
    }
    return;
  }
  return value;
}
function readPackageJsonPathField(jsonContent, fieldName, baseDirectory, state) {
  const fileName ***REMOVED*** readPackageJsonField(jsonContent, fieldName, "string", state);
  if (fileName ***REMOVED******REMOVED******REMOVED*** void 0) {
    return;
  }
  if (!fileName) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_had_a_falsy_0_field, fieldName);
    }
    return;
  }
  const path ***REMOVED*** normalizePath(combinePaths(baseDirectory, fileName));
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.package_json_has_0_field_1_that_references_2, fieldName, fileName, path);
  }
  return path;
}
function readPackageJsonTypesFields(jsonContent, baseDirectory, state) {
  return readPackageJsonPathField(jsonContent, "typings", baseDirectory, state) || readPackageJsonPathField(jsonContent, "types", baseDirectory, state);
}
function readPackageJsonTSConfigField(jsonContent, baseDirectory, state) {
  return readPackageJsonPathField(jsonContent, "tsconfig", baseDirectory, state);
}
function readPackageJsonMainField(jsonContent, baseDirectory, state) {
  return readPackageJsonPathField(jsonContent, "main", baseDirectory, state);
}
function readPackageJsonTypesVersionsField(jsonContent, state) {
  const typesVersions ***REMOVED*** readPackageJsonField(jsonContent, "typesVersions", "object", state);
  if (typesVersions ***REMOVED******REMOVED******REMOVED*** void 0) return;
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.package_json_has_a_typesVersions_field_with_version_specific_path_mappings);
  }
  return typesVersions;
}
function readPackageJsonTypesVersionPaths(jsonContent, state) {
  const typesVersions ***REMOVED*** readPackageJsonTypesVersionsField(jsonContent, state);
  if (typesVersions ***REMOVED******REMOVED******REMOVED*** void 0) return;
  if (state.traceEnabled) {
    for (const key in typesVersions) {
      if (hasProperty(typesVersions, key) && !VersionRange.tryParse(key)) {
        trace(state.host, Diagnostics.package_json_has_a_typesVersions_entry_0_that_is_not_a_valid_semver_range, key);
      }
    }
  }
  const result ***REMOVED*** getPackageJsonTypesVersionsPaths(typesVersions);
  if (!result) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_does_not_have_a_typesVersions_entry_that_matches_version_0, versionMajorMinor);
    }
    return;
  }
  const { version: bestVersionKey, paths: bestVersionPaths } ***REMOVED*** result;
  if (typeof bestVersionPaths !***REMOVED******REMOVED*** "object") {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Expected_type_of_0_field_in_package_json_to_be_1_got_2, `typesVersions['${bestVersionKey}']`, "object", typeof bestVersionPaths);
    }
    return;
  }
  return result;
}
var typeScriptVersion;
function getPackageJsonTypesVersionsPaths(typesVersions) {
  if (!typeScriptVersion) typeScriptVersion ***REMOVED*** new Version(version);
  for (const key in typesVersions) {
    if (!hasProperty(typesVersions, key)) continue;
    const keyRange ***REMOVED*** VersionRange.tryParse(key);
    if (keyRange ***REMOVED******REMOVED******REMOVED*** void 0) {
      continue;
    }
    if (keyRange.test(typeScriptVersion)) {
      return { version: key, paths: typesVersions[key] };
    }
  }
}
function getEffectiveTypeRoots(options, host) {
  if (options.typeRoots) {
    return options.typeRoots;
  }
  let currentDirectory;
  if (options.configFilePath) {
    currentDirectory ***REMOVED*** getDirectoryPath(options.configFilePath);
  } else if (host.getCurrentDirectory) {
    currentDirectory ***REMOVED*** host.getCurrentDirectory();
  }
  if (currentDirectory !***REMOVED******REMOVED*** void 0) {
    return getDefaultTypeRoots(currentDirectory);
  }
}
function getDefaultTypeRoots(currentDirectory) {
  let typeRoots;
  forEachAncestorDirectory(normalizePath(currentDirectory), (directory) ***REMOVED***> {
    const atTypes ***REMOVED*** combinePaths(directory, nodeModulesAtTypes);
    (typeRoots ?? (typeRoots ***REMOVED*** [])).push(atTypes);
  });
  return typeRoots;
}
var nodeModulesAtTypes ***REMOVED*** combinePaths("node_modules", "@types");
function arePathsEqual(path1, path2, host) {
  const useCaseSensitiveFileNames2 ***REMOVED*** typeof host.useCaseSensitiveFileNames ***REMOVED******REMOVED******REMOVED*** "function" ? host.useCaseSensitiveFileNames() : host.useCaseSensitiveFileNames;
  return comparePaths(path1, path2, !useCaseSensitiveFileNames2) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */;
}
function getOriginalAndResolvedFileName(fileName, host, traceEnabled) {
  const resolvedFileName ***REMOVED*** realPath(fileName, host, traceEnabled);
  const pathsAreEqual ***REMOVED*** arePathsEqual(fileName, resolvedFileName, host);
  return {
    // If the fileName and realpath are differing only in casing prefer fileName so that we can issue correct errors for casing under forceConsistentCasingInFileNames
    resolvedFileName: pathsAreEqual ? fileName : resolvedFileName,
    originalPath: pathsAreEqual ? void 0 : fileName
  };
}
function getCandidateFromTypeRoot(typeRoot, typeReferenceDirectiveName, moduleResolutionState) {
  const nameForLookup ***REMOVED*** endsWith(typeRoot, "/node_modules/@types") || endsWith(typeRoot, "/node_modules/@types/") ? mangleScopedPackageNameWithTrace(typeReferenceDirectiveName, moduleResolutionState) : typeReferenceDirectiveName;
  return combinePaths(typeRoot, nameForLookup);
}
function resolveTypeReferenceDirective(typeReferenceDirectiveName, containingFile, options, host, redirectedReference, cache, resolutionMode) {
  Debug.assert(typeof typeReferenceDirectiveName ***REMOVED******REMOVED******REMOVED*** "string", "Non-string value passed to `ts.resolveTypeReferenceDirective`, likely by a wrapping package working with an outdated `resolveTypeReferenceDirectives` signature. This is probably not a problem in TS itself.");
  const traceEnabled ***REMOVED*** isTraceEnabled(options, host);
  if (redirectedReference) {
    options ***REMOVED*** redirectedReference.commandLine.options;
  }
  const containingDirectory ***REMOVED*** containingFile ? getDirectoryPath(containingFile) : void 0;
  let result ***REMOVED*** containingDirectory ? cache ***REMOVED******REMOVED*** null ? void 0 : cache.getFromDirectoryCache(typeReferenceDirectiveName, resolutionMode, containingDirectory, redirectedReference) : void 0;
  if (!result && containingDirectory && !isExternalModuleNameRelative(typeReferenceDirectiveName)) {
    result ***REMOVED*** cache ***REMOVED******REMOVED*** null ? void 0 : cache.getFromNonRelativeNameCache(typeReferenceDirectiveName, resolutionMode, containingDirectory, redirectedReference);
  }
  if (result) {
    if (traceEnabled) {
      trace(host, Diagnostics.Resolving_type_reference_directive_0_containing_file_1, typeReferenceDirectiveName, containingFile);
      if (redirectedReference) trace(host, Diagnostics.Using_compiler_options_of_project_reference_redirect_0, redirectedReference.sourceFile.fileName);
      trace(host, Diagnostics.Resolution_for_type_reference_directive_0_was_found_in_cache_from_location_1, typeReferenceDirectiveName, containingDirectory);
      traceResult(result);
    }
    return result;
  }
  const typeRoots ***REMOVED*** getEffectiveTypeRoots(options, host);
  if (traceEnabled) {
    if (containingFile ***REMOVED******REMOVED******REMOVED*** void 0) {
      if (typeRoots ***REMOVED******REMOVED******REMOVED*** void 0) {
        trace(host, Diagnostics.Resolving_type_reference_directive_0_containing_file_not_set_root_directory_not_set, typeReferenceDirectiveName);
      } else {
        trace(host, Diagnostics.Resolving_type_reference_directive_0_containing_file_not_set_root_directory_1, typeReferenceDirectiveName, typeRoots);
      }
    } else {
      if (typeRoots ***REMOVED******REMOVED******REMOVED*** void 0) {
        trace(host, Diagnostics.Resolving_type_reference_directive_0_containing_file_1_root_directory_not_set, typeReferenceDirectiveName, containingFile);
      } else {
        trace(host, Diagnostics.Resolving_type_reference_directive_0_containing_file_1_root_directory_2, typeReferenceDirectiveName, containingFile, typeRoots);
      }
    }
    if (redirectedReference) {
      trace(host, Diagnostics.Using_compiler_options_of_project_reference_redirect_0, redirectedReference.sourceFile.fileName);
    }
  }
  const failedLookupLocations ***REMOVED*** [];
  const affectingLocations ***REMOVED*** [];
  let features ***REMOVED*** getNodeResolutionFeatures(options);
  if (resolutionMode !***REMOVED******REMOVED*** void 0) {
    features |***REMOVED*** 30 /* AllFeatures */;
  }
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(options);
  if (resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && (3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */)) {
    features |***REMOVED*** 32 /* EsmMode */;
  }
  const conditions ***REMOVED*** features & 8 /* Exports */ ? getConditions(options, resolutionMode) : [];
  const diagnostics ***REMOVED*** [];
  const moduleResolutionState ***REMOVED*** {
    compilerOptions: options,
    host,
    traceEnabled,
    failedLookupLocations,
    affectingLocations,
    packageJsonInfoCache: cache,
    features,
    conditions,
    requestContainingDirectory: containingDirectory,
    reportDiagnostic: (diag2) ***REMOVED***> void diagnostics.push(diag2),
    isConfigLookup: false,
    candidateIsFromPackageJsonField: false,
    resolvedPackageDirectory: false
  };
  let resolved ***REMOVED*** primaryLookup();
  let primary ***REMOVED*** true;
  if (!resolved) {
    resolved ***REMOVED*** secondaryLookup();
    primary ***REMOVED*** false;
  }
  let resolvedTypeReferenceDirective;
  if (resolved) {
    const { fileName, packageId } ***REMOVED*** resolved;
    let resolvedFileName ***REMOVED*** fileName, originalPath;
    if (!options.preserveSymlinks) ({ resolvedFileName, originalPath } ***REMOVED*** getOriginalAndResolvedFileName(fileName, host, traceEnabled));
    resolvedTypeReferenceDirective ***REMOVED*** {
      primary,
      resolvedFileName,
      originalPath,
      packageId,
      isExternalLibraryImport: pathContainsNodeModules(fileName)
    };
  }
  result ***REMOVED*** {
    resolvedTypeReferenceDirective,
    failedLookupLocations: initializeResolutionField(failedLookupLocations),
    affectingLocations: initializeResolutionField(affectingLocations),
    resolutionDiagnostics: initializeResolutionField(diagnostics)
  };
  if (containingDirectory && cache && !cache.isReadonly) {
    cache.getOrCreateCacheForDirectory(containingDirectory, redirectedReference).set(
      typeReferenceDirectiveName,
      /*mode*/
      resolutionMode,
      result
    );
    if (!isExternalModuleNameRelative(typeReferenceDirectiveName)) {
      cache.getOrCreateCacheForNonRelativeName(typeReferenceDirectiveName, resolutionMode, redirectedReference).set(containingDirectory, result);
    }
  }
  if (traceEnabled) traceResult(result);
  return result;
  function traceResult(result2) {
    var _a;
    if (!((_a ***REMOVED*** result2.resolvedTypeReferenceDirective) ***REMOVED******REMOVED*** null ? void 0 : _a.resolvedFileName)) {
      trace(host, Diagnostics.Type_reference_directive_0_was_not_resolved, typeReferenceDirectiveName);
    } else if (result2.resolvedTypeReferenceDirective.packageId) {
      trace(host, Diagnostics.Type_reference_directive_0_was_successfully_resolved_to_1_with_Package_ID_2_primary_Colon_3, typeReferenceDirectiveName, result2.resolvedTypeReferenceDirective.resolvedFileName, packageIdToString(result2.resolvedTypeReferenceDirective.packageId), result2.resolvedTypeReferenceDirective.primary);
    } else {
      trace(host, Diagnostics.Type_reference_directive_0_was_successfully_resolved_to_1_primary_Colon_2, typeReferenceDirectiveName, result2.resolvedTypeReferenceDirective.resolvedFileName, result2.resolvedTypeReferenceDirective.primary);
    }
  }
  function primaryLookup() {
    if (typeRoots && typeRoots.length) {
      if (traceEnabled) {
        trace(host, Diagnostics.Resolving_with_primary_search_path_0, typeRoots.join(", "));
      }
      return firstDefined(typeRoots, (typeRoot) ***REMOVED***> {
        const candidate ***REMOVED*** getCandidateFromTypeRoot(typeRoot, typeReferenceDirectiveName, moduleResolutionState);
        const directoryExists ***REMOVED*** directoryProbablyExists(typeRoot, host);
        if (!directoryExists && traceEnabled) {
          trace(host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, typeRoot);
        }
        if (options.typeRoots) {
          const resolvedFromFile ***REMOVED*** loadModuleFromFile(4 /* Declaration */, candidate, !directoryExists, moduleResolutionState);
          if (resolvedFromFile) {
            const packageDirectory ***REMOVED*** parseNodeModuleFromPath(resolvedFromFile.path);
            const packageInfo ***REMOVED*** packageDirectory ? getPackageJsonInfo(
              packageDirectory,
              /*onlyRecordFailures*/
              false,
              moduleResolutionState
            ) : void 0;
            return resolvedTypeScriptOnly(withPackageId(packageInfo, resolvedFromFile, moduleResolutionState));
          }
        }
        return resolvedTypeScriptOnly(
          loadNodeModuleFromDirectory(4 /* Declaration */, candidate, !directoryExists, moduleResolutionState)
        );
      });
    } else {
      if (traceEnabled) {
        trace(host, Diagnostics.Root_directory_cannot_be_determined_skipping_primary_search_paths);
      }
    }
  }
  function secondaryLookup() {
    const initialLocationForSecondaryLookup ***REMOVED*** containingFile && getDirectoryPath(containingFile);
    if (initialLocationForSecondaryLookup !***REMOVED******REMOVED*** void 0) {
      let result2;
      if (!options.typeRoots || !endsWith(containingFile, inferredTypesContainingFile)) {
        if (traceEnabled) {
          trace(host, Diagnostics.Looking_up_in_node_modules_folder_initial_location_0, initialLocationForSecondaryLookup);
        }
        if (!isExternalModuleNameRelative(typeReferenceDirectiveName)) {
          const searchResult ***REMOVED*** loadModuleFromNearestNodeModulesDirectory(
            4 /* Declaration */,
            typeReferenceDirectiveName,
            initialLocationForSecondaryLookup,
            moduleResolutionState,
            /*cache*/
            void 0,
            /*redirectedReference*/
            void 0
          );
          result2 ***REMOVED*** searchResult && searchResult.value;
        } else {
          const { path: candidate } ***REMOVED*** normalizePathForCJSResolution(initialLocationForSecondaryLookup, typeReferenceDirectiveName);
          result2 ***REMOVED*** nodeLoadModuleByRelativeName(
            4 /* Declaration */,
            candidate,
            /*onlyRecordFailures*/
            false,
            moduleResolutionState,
            /*considerPackageJson*/
            true
          );
        }
      } else if (traceEnabled) {
        trace(host, Diagnostics.Resolving_type_reference_directive_for_program_that_specifies_custom_typeRoots_skipping_lookup_in_node_modules_folder);
      }
      return resolvedTypeScriptOnly(result2);
    } else {
      if (traceEnabled) {
        trace(host, Diagnostics.Containing_file_is_not_specified_and_root_directory_cannot_be_determined_skipping_lookup_in_node_modules_folder);
      }
    }
  }
}
function getNodeResolutionFeatures(options) {
  let features ***REMOVED*** 0 /* None */;
  switch (getEmitModuleResolutionKind(options)) {
    case 3 /* Node16 */:
      features ***REMOVED*** 30 /* Node16Default */;
      break;
    case 99 /* NodeNext */:
      features ***REMOVED*** 30 /* NodeNextDefault */;
      break;
    case 100 /* Bundler */:
      features ***REMOVED*** 30 /* BundlerDefault */;
      break;
  }
  if (options.resolvePackageJsonExports) {
    features |***REMOVED*** 8 /* Exports */;
  } else if (options.resolvePackageJsonExports ***REMOVED******REMOVED******REMOVED*** false) {
    features &***REMOVED*** ~8 /* Exports */;
  }
  if (options.resolvePackageJsonImports) {
    features |***REMOVED*** 2 /* Imports */;
  } else if (options.resolvePackageJsonImports ***REMOVED******REMOVED******REMOVED*** false) {
    features &***REMOVED*** ~2 /* Imports */;
  }
  return features;
}
function getConditions(options, resolutionMode) {
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(options);
  if (resolutionMode ***REMOVED******REMOVED******REMOVED*** void 0) {
    if (moduleResolution ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */) {
      resolutionMode ***REMOVED*** 99 /* ESNext */;
    } else if (moduleResolution ***REMOVED******REMOVED******REMOVED*** 2 /* Node10 */) {
      return [];
    }
  }
  const conditions ***REMOVED*** resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? ["import"] : ["require"];
  if (!options.noDtsResolution) {
    conditions.push("types");
  }
  if (moduleResolution !***REMOVED******REMOVED*** 100 /* Bundler */) {
    conditions.push("node");
  }
  return concatenate(conditions, options.customConditions);
}
function getAutomaticTypeDirectiveNames(options, host) {
  if (options.types) {
    return options.types;
  }
  const result ***REMOVED*** [];
  if (host.directoryExists && host.getDirectories) {
    const typeRoots ***REMOVED*** getEffectiveTypeRoots(options, host);
    if (typeRoots) {
      for (const root of typeRoots) {
        if (host.directoryExists(root)) {
          for (const typeDirectivePath of host.getDirectories(root)) {
            const normalized ***REMOVED*** normalizePath(typeDirectivePath);
            const packageJsonPath ***REMOVED*** combinePaths(root, normalized, "package.json");
            const isNotNeededPackage ***REMOVED*** host.fileExists(packageJsonPath) && readJson(packageJsonPath, host).typings ***REMOVED******REMOVED******REMOVED*** null;
            if (!isNotNeededPackage) {
              const baseFileName ***REMOVED*** getBaseFileName(normalized);
              if (baseFileName.charCodeAt(0) !***REMOVED******REMOVED*** 46 /* dot */) {
                result.push(baseFileName);
              }
            }
          }
        }
      }
    }
  }
  return result;
}
function isPackageJsonInfo(entry) {
  return !!(entry ***REMOVED******REMOVED*** null ? void 0 : entry.contents);
}
function isMissingPackageJsonInfo(entry) {
  return !!entry && !entry.contents;
}
function compilerOptionValueToString(value) {
  var _a;
  if (value ***REMOVED******REMOVED******REMOVED*** null || typeof value !***REMOVED******REMOVED*** "object") {
    return "" + value;
  }
  if (isArray(value)) {
    return `[${(_a ***REMOVED*** value.map((e) ***REMOVED***> compilerOptionValueToString(e))) ***REMOVED******REMOVED*** null ? void 0 : _a.join(",")}]`;
  }
  let str ***REMOVED*** "{";
  for (const key in value) {
    if (hasProperty(value, key)) {
      str +***REMOVED*** `${key}: ${compilerOptionValueToString(value[key])}`;
    }
  }
  return str + "}";
}
function getKeyForCompilerOptions(options, affectingOptionDeclarations) {
  return affectingOptionDeclarations.map((option) ***REMOVED***> compilerOptionValueToString(getCompilerOptionValue(options, option))).join("|") + `|${options.pathsBasePath}`;
}
function createCacheWithRedirects(ownOptions, optionsToRedirectsKey) {
  const redirectsMap ***REMOVED*** /* @__PURE__ */ new Map();
  const redirectsKeyToMap ***REMOVED*** /* @__PURE__ */ new Map();
  let ownMap ***REMOVED*** /* @__PURE__ */ new Map();
  if (ownOptions) redirectsMap.set(ownOptions, ownMap);
  return {
    getMapOfCacheRedirects,
    getOrCreateMapOfCacheRedirects,
    update,
    clear: clear2,
    getOwnMap: () ***REMOVED***> ownMap
  };
  function getMapOfCacheRedirects(redirectedReference) {
    return redirectedReference ? getOrCreateMap(
      redirectedReference.commandLine.options,
      /*create*/
      false
    ) : ownMap;
  }
  function getOrCreateMapOfCacheRedirects(redirectedReference) {
    return redirectedReference ? getOrCreateMap(
      redirectedReference.commandLine.options,
      /*create*/
      true
    ) : ownMap;
  }
  function update(newOptions) {
    if (ownOptions !***REMOVED******REMOVED*** newOptions) {
      if (ownOptions) ownMap ***REMOVED*** getOrCreateMap(
        newOptions,
        /*create*/
        true
      );
      else redirectsMap.set(newOptions, ownMap);
      ownOptions ***REMOVED*** newOptions;
    }
  }
  function getOrCreateMap(redirectOptions, create) {
    let result ***REMOVED*** redirectsMap.get(redirectOptions);
    if (result) return result;
    const key ***REMOVED*** getRedirectsCacheKey(redirectOptions);
    result ***REMOVED*** redirectsKeyToMap.get(key);
    if (!result) {
      if (ownOptions) {
        const ownKey ***REMOVED*** getRedirectsCacheKey(ownOptions);
        if (ownKey ***REMOVED******REMOVED******REMOVED*** key) result ***REMOVED*** ownMap;
        else if (!redirectsKeyToMap.has(ownKey)) redirectsKeyToMap.set(ownKey, ownMap);
      }
      if (create) result ?? (result ***REMOVED*** /* @__PURE__ */ new Map());
      if (result) redirectsKeyToMap.set(key, result);
    }
    if (result) redirectsMap.set(redirectOptions, result);
    return result;
  }
  function clear2() {
    const ownKey ***REMOVED*** ownOptions && optionsToRedirectsKey.get(ownOptions);
    ownMap.clear();
    redirectsMap.clear();
    optionsToRedirectsKey.clear();
    redirectsKeyToMap.clear();
    if (ownOptions) {
      if (ownKey) optionsToRedirectsKey.set(ownOptions, ownKey);
      redirectsMap.set(ownOptions, ownMap);
    }
  }
  function getRedirectsCacheKey(options) {
    let result ***REMOVED*** optionsToRedirectsKey.get(options);
    if (!result) {
      optionsToRedirectsKey.set(options, result ***REMOVED*** getKeyForCompilerOptions(options, moduleResolutionOptionDeclarations));
    }
    return result;
  }
}
function createPackageJsonInfoCache(currentDirectory, getCanonicalFileName) {
  let cache;
  return { getPackageJsonInfo: getPackageJsonInfo2, setPackageJsonInfo, clear: clear2, getInternalMap };
  function getPackageJsonInfo2(packageJsonPath) {
    return cache ***REMOVED******REMOVED*** null ? void 0 : cache.get(toPath(packageJsonPath, currentDirectory, getCanonicalFileName));
  }
  function setPackageJsonInfo(packageJsonPath, info) {
    (cache || (cache ***REMOVED*** /* @__PURE__ */ new Map())).set(toPath(packageJsonPath, currentDirectory, getCanonicalFileName), info);
  }
  function clear2() {
    cache ***REMOVED*** void 0;
  }
  function getInternalMap() {
    return cache;
  }
}
function getOrCreateCache(cacheWithRedirects, redirectedReference, key, create) {
  const cache ***REMOVED*** cacheWithRedirects.getOrCreateMapOfCacheRedirects(redirectedReference);
  let result ***REMOVED*** cache.get(key);
  if (!result) {
    result ***REMOVED*** create();
    cache.set(key, result);
  }
  return result;
}
function createPerDirectoryResolutionCache(currentDirectory, getCanonicalFileName, options, optionsToRedirectsKey) {
  const directoryToModuleNameMap ***REMOVED*** createCacheWithRedirects(options, optionsToRedirectsKey);
  return {
    getFromDirectoryCache,
    getOrCreateCacheForDirectory,
    clear: clear2,
    update,
    directoryToModuleNameMap
  };
  function clear2() {
    directoryToModuleNameMap.clear();
  }
  function update(options2) {
    directoryToModuleNameMap.update(options2);
  }
  function getOrCreateCacheForDirectory(directoryName, redirectedReference) {
    const path ***REMOVED*** toPath(directoryName, currentDirectory, getCanonicalFileName);
    return getOrCreateCache(directoryToModuleNameMap, redirectedReference, path, () ***REMOVED***> createModeAwareCache());
  }
  function getFromDirectoryCache(name, mode, directoryName, redirectedReference) {
    var _a, _b;
    const path ***REMOVED*** toPath(directoryName, currentDirectory, getCanonicalFileName);
    return (_b ***REMOVED*** (_a ***REMOVED*** directoryToModuleNameMap.getMapOfCacheRedirects(redirectedReference)) ***REMOVED******REMOVED*** null ? void 0 : _a.get(path)) ***REMOVED******REMOVED*** null ? void 0 : _b.get(name, mode);
  }
}
function createModeAwareCacheKey(specifier, mode) {
  return mode ***REMOVED******REMOVED******REMOVED*** void 0 ? specifier : `${mode}|${specifier}`;
}
function createModeAwareCache() {
  const underlying ***REMOVED*** /* @__PURE__ */ new Map();
  const memoizedReverseKeys ***REMOVED*** /* @__PURE__ */ new Map();
  const cache ***REMOVED*** {
    get(specifier, mode) {
      return underlying.get(getUnderlyingCacheKey(specifier, mode));
    },
    set(specifier, mode, value) {
      underlying.set(getUnderlyingCacheKey(specifier, mode), value);
      return cache;
    },
    delete(specifier, mode) {
      underlying.delete(getUnderlyingCacheKey(specifier, mode));
      return cache;
    },
    has(specifier, mode) {
      return underlying.has(getUnderlyingCacheKey(specifier, mode));
    },
    forEach(cb) {
      return underlying.forEach((elem, key) ***REMOVED***> {
        const [specifier, mode] ***REMOVED*** memoizedReverseKeys.get(key);
        return cb(elem, specifier, mode);
      });
    },
    size() {
      return underlying.size;
    }
  };
  return cache;
  function getUnderlyingCacheKey(specifier, mode) {
    const result ***REMOVED*** createModeAwareCacheKey(specifier, mode);
    memoizedReverseKeys.set(result, [specifier, mode]);
    return result;
  }
}
function getOriginalOrResolvedModuleFileName(result) {
  return result.resolvedModule && (result.resolvedModule.originalPath || result.resolvedModule.resolvedFileName);
}
function getOriginalOrResolvedTypeReferenceFileName(result) {
  return result.resolvedTypeReferenceDirective && (result.resolvedTypeReferenceDirective.originalPath || result.resolvedTypeReferenceDirective.resolvedFileName);
}
function createNonRelativeNameResolutionCache(currentDirectory, getCanonicalFileName, options, getResolvedFileName, optionsToRedirectsKey) {
  const moduleNameToDirectoryMap ***REMOVED*** createCacheWithRedirects(options, optionsToRedirectsKey);
  return {
    getFromNonRelativeNameCache,
    getOrCreateCacheForNonRelativeName,
    clear: clear2,
    update
  };
  function clear2() {
    moduleNameToDirectoryMap.clear();
  }
  function update(options2) {
    moduleNameToDirectoryMap.update(options2);
  }
  function getFromNonRelativeNameCache(nonRelativeModuleName, mode, directoryName, redirectedReference) {
    var _a, _b;
    Debug.assert(!isExternalModuleNameRelative(nonRelativeModuleName));
    return (_b ***REMOVED*** (_a ***REMOVED*** moduleNameToDirectoryMap.getMapOfCacheRedirects(redirectedReference)) ***REMOVED******REMOVED*** null ? void 0 : _a.get(createModeAwareCacheKey(nonRelativeModuleName, mode))) ***REMOVED******REMOVED*** null ? void 0 : _b.get(directoryName);
  }
  function getOrCreateCacheForNonRelativeName(nonRelativeModuleName, mode, redirectedReference) {
    Debug.assert(!isExternalModuleNameRelative(nonRelativeModuleName));
    return getOrCreateCache(moduleNameToDirectoryMap, redirectedReference, createModeAwareCacheKey(nonRelativeModuleName, mode), createPerModuleNameCache);
  }
  function createPerModuleNameCache() {
    const directoryPathMap ***REMOVED*** /* @__PURE__ */ new Map();
    return { get, set };
    function get(directory) {
      return directoryPathMap.get(toPath(directory, currentDirectory, getCanonicalFileName));
    }
    function set(directory, result) {
      const path ***REMOVED*** toPath(directory, currentDirectory, getCanonicalFileName);
      if (directoryPathMap.has(path)) {
        return;
      }
      directoryPathMap.set(path, result);
      const resolvedFileName ***REMOVED*** getResolvedFileName(result);
      const commonPrefix ***REMOVED*** resolvedFileName && getCommonPrefix(path, resolvedFileName);
      let current ***REMOVED*** path;
      while (current !***REMOVED******REMOVED*** commonPrefix) {
        const parent ***REMOVED*** getDirectoryPath(current);
        if (parent ***REMOVED******REMOVED******REMOVED*** current || directoryPathMap.has(parent)) {
          break;
        }
        directoryPathMap.set(parent, result);
        current ***REMOVED*** parent;
      }
    }
    function getCommonPrefix(directory, resolution) {
      const resolutionDirectory ***REMOVED*** toPath(getDirectoryPath(resolution), currentDirectory, getCanonicalFileName);
      let i ***REMOVED*** 0;
      const limit ***REMOVED*** Math.min(directory.length, resolutionDirectory.length);
      while (i < limit && directory.charCodeAt(i) ***REMOVED******REMOVED******REMOVED*** resolutionDirectory.charCodeAt(i)) {
        i++;
      }
      if (i ***REMOVED******REMOVED******REMOVED*** directory.length && (resolutionDirectory.length ***REMOVED******REMOVED******REMOVED*** i || resolutionDirectory[i] ***REMOVED******REMOVED******REMOVED*** directorySeparator)) {
        return directory;
      }
      const rootLength ***REMOVED*** getRootLength(directory);
      if (i < rootLength) {
        return void 0;
      }
      const sep ***REMOVED*** directory.lastIndexOf(directorySeparator, i - 1);
      if (sep ***REMOVED******REMOVED******REMOVED*** -1) {
        return void 0;
      }
      return directory.substr(0, Math.max(sep, rootLength));
    }
  }
}
function createModuleOrTypeReferenceResolutionCache(currentDirectory, getCanonicalFileName, options, packageJsonInfoCache, getResolvedFileName, optionsToRedirectsKey) {
  optionsToRedirectsKey ?? (optionsToRedirectsKey ***REMOVED*** /* @__PURE__ */ new Map());
  const perDirectoryResolutionCache ***REMOVED*** createPerDirectoryResolutionCache(
    currentDirectory,
    getCanonicalFileName,
    options,
    optionsToRedirectsKey
  );
  const nonRelativeNameResolutionCache ***REMOVED*** createNonRelativeNameResolutionCache(
    currentDirectory,
    getCanonicalFileName,
    options,
    getResolvedFileName,
    optionsToRedirectsKey
  );
  packageJsonInfoCache ?? (packageJsonInfoCache ***REMOVED*** createPackageJsonInfoCache(currentDirectory, getCanonicalFileName));
  return {
    ...packageJsonInfoCache,
    ...perDirectoryResolutionCache,
    ...nonRelativeNameResolutionCache,
    clear: clear2,
    update,
    getPackageJsonInfoCache: () ***REMOVED***> packageJsonInfoCache,
    clearAllExceptPackageJsonInfoCache,
    optionsToRedirectsKey
  };
  function clear2() {
    clearAllExceptPackageJsonInfoCache();
    packageJsonInfoCache.clear();
  }
  function clearAllExceptPackageJsonInfoCache() {
    perDirectoryResolutionCache.clear();
    nonRelativeNameResolutionCache.clear();
  }
  function update(options2) {
    perDirectoryResolutionCache.update(options2);
    nonRelativeNameResolutionCache.update(options2);
  }
}
function createModuleResolutionCache(currentDirectory, getCanonicalFileName, options, packageJsonInfoCache, optionsToRedirectsKey) {
  const result ***REMOVED*** createModuleOrTypeReferenceResolutionCache(
    currentDirectory,
    getCanonicalFileName,
    options,
    packageJsonInfoCache,
    getOriginalOrResolvedModuleFileName,
    optionsToRedirectsKey
  );
  result.getOrCreateCacheForModuleName ***REMOVED*** (nonRelativeName, mode, redirectedReference) ***REMOVED***> result.getOrCreateCacheForNonRelativeName(nonRelativeName, mode, redirectedReference);
  return result;
}
function createTypeReferenceDirectiveResolutionCache(currentDirectory, getCanonicalFileName, options, packageJsonInfoCache, optionsToRedirectsKey) {
  return createModuleOrTypeReferenceResolutionCache(
    currentDirectory,
    getCanonicalFileName,
    options,
    packageJsonInfoCache,
    getOriginalOrResolvedTypeReferenceFileName,
    optionsToRedirectsKey
  );
}
function getOptionsForLibraryResolution(options) {
  return { moduleResolution: 2 /* Node10 */, traceResolution: options.traceResolution };
}
function resolveLibrary(libraryName, resolveFrom, compilerOptions, host, cache) {
  return resolveModuleName(libraryName, resolveFrom, getOptionsForLibraryResolution(compilerOptions), host, cache);
}
function resolveModuleName(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode) {
  const traceEnabled ***REMOVED*** isTraceEnabled(compilerOptions, host);
  if (redirectedReference) {
    compilerOptions ***REMOVED*** redirectedReference.commandLine.options;
  }
  if (traceEnabled) {
    trace(host, Diagnostics.Resolving_module_0_from_1, moduleName, containingFile);
    if (redirectedReference) {
      trace(host, Diagnostics.Using_compiler_options_of_project_reference_redirect_0, redirectedReference.sourceFile.fileName);
    }
  }
  const containingDirectory ***REMOVED*** getDirectoryPath(containingFile);
  let result ***REMOVED*** cache ***REMOVED******REMOVED*** null ? void 0 : cache.getFromDirectoryCache(moduleName, resolutionMode, containingDirectory, redirectedReference);
  if (result) {
    if (traceEnabled) {
      trace(host, Diagnostics.Resolution_for_module_0_was_found_in_cache_from_location_1, moduleName, containingDirectory);
    }
  } else {
    let moduleResolution ***REMOVED*** compilerOptions.moduleResolution;
    if (moduleResolution ***REMOVED******REMOVED******REMOVED*** void 0) {
      moduleResolution ***REMOVED*** getEmitModuleResolutionKind(compilerOptions);
      if (traceEnabled) {
        trace(host, Diagnostics.Module_resolution_kind_is_not_specified_using_0, ModuleResolutionKind[moduleResolution]);
      }
    } else {
      if (traceEnabled) {
        trace(host, Diagnostics.Explicitly_specified_module_resolution_kind_Colon_0, ModuleResolutionKind[moduleResolution]);
      }
    }
    switch (moduleResolution) {
      case 3 /* Node16 */:
        result ***REMOVED*** node16ModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode);
        break;
      case 99 /* NodeNext */:
        result ***REMOVED*** nodeNextModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode);
        break;
      case 2 /* Node10 */:
        result ***REMOVED*** nodeModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode ? getConditions(compilerOptions, resolutionMode) : void 0);
        break;
      case 1 /* Classic */:
        result ***REMOVED*** classicNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference);
        break;
      case 100 /* Bundler */:
        result ***REMOVED*** bundlerModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode ? getConditions(compilerOptions, resolutionMode) : void 0);
        break;
      default:
        return Debug.fail(`Unexpected moduleResolution: ${moduleResolution}`);
    }
    if (cache && !cache.isReadonly) {
      cache.getOrCreateCacheForDirectory(containingDirectory, redirectedReference).set(moduleName, resolutionMode, result);
      if (!isExternalModuleNameRelative(moduleName)) {
        cache.getOrCreateCacheForNonRelativeName(moduleName, resolutionMode, redirectedReference).set(containingDirectory, result);
      }
    }
  }
  if (traceEnabled) {
    if (result.resolvedModule) {
      if (result.resolvedModule.packageId) {
        trace(host, Diagnostics.Module_name_0_was_successfully_resolved_to_1_with_Package_ID_2, moduleName, result.resolvedModule.resolvedFileName, packageIdToString(result.resolvedModule.packageId));
      } else {
        trace(host, Diagnostics.Module_name_0_was_successfully_resolved_to_1, moduleName, result.resolvedModule.resolvedFileName);
      }
    } else {
      trace(host, Diagnostics.Module_name_0_was_not_resolved, moduleName);
    }
  }
  return result;
}
function tryLoadModuleUsingOptionalResolutionSettings(extensions, moduleName, containingDirectory, loader, state) {
  const resolved ***REMOVED*** tryLoadModuleUsingPathsIfEligible(extensions, moduleName, loader, state);
  if (resolved) return resolved.value;
  if (!isExternalModuleNameRelative(moduleName)) {
    return tryLoadModuleUsingBaseUrl(extensions, moduleName, loader, state);
  } else {
    return tryLoadModuleUsingRootDirs(extensions, moduleName, containingDirectory, loader, state);
  }
}
function tryLoadModuleUsingPathsIfEligible(extensions, moduleName, loader, state) {
  const { baseUrl, paths } ***REMOVED*** state.compilerOptions;
  if (paths && !pathIsRelative(moduleName)) {
    if (state.traceEnabled) {
      if (baseUrl) {
        trace(state.host, Diagnostics.baseUrl_option_is_set_to_0_using_this_value_to_resolve_non_relative_module_name_1, baseUrl, moduleName);
      }
      trace(state.host, Diagnostics.paths_option_is_specified_looking_for_a_pattern_to_match_module_name_0, moduleName);
    }
    const baseDirectory ***REMOVED*** getPathsBasePath(state.compilerOptions, state.host);
    const pathPatterns ***REMOVED*** tryParsePatterns(paths);
    return tryLoadModuleUsingPaths(
      extensions,
      moduleName,
      baseDirectory,
      paths,
      pathPatterns,
      loader,
      /*onlyRecordFailures*/
      false,
      state
    );
  }
}
function tryLoadModuleUsingRootDirs(extensions, moduleName, containingDirectory, loader, state) {
  if (!state.compilerOptions.rootDirs) {
    return void 0;
  }
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.rootDirs_option_is_set_using_it_to_resolve_relative_module_name_0, moduleName);
  }
  const candidate ***REMOVED*** normalizePath(combinePaths(containingDirectory, moduleName));
  let matchedRootDir;
  let matchedNormalizedPrefix;
  for (const rootDir of state.compilerOptions.rootDirs) {
    let normalizedRoot ***REMOVED*** normalizePath(rootDir);
    if (!endsWith(normalizedRoot, directorySeparator)) {
      normalizedRoot +***REMOVED*** directorySeparator;
    }
    const isLongestMatchingPrefix ***REMOVED*** startsWith(candidate, normalizedRoot) && (matchedNormalizedPrefix ***REMOVED******REMOVED******REMOVED*** void 0 || matchedNormalizedPrefix.length < normalizedRoot.length);
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Checking_if_0_is_the_longest_matching_prefix_for_1_2, normalizedRoot, candidate, isLongestMatchingPrefix);
    }
    if (isLongestMatchingPrefix) {
      matchedNormalizedPrefix ***REMOVED*** normalizedRoot;
      matchedRootDir ***REMOVED*** rootDir;
    }
  }
  if (matchedNormalizedPrefix) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Longest_matching_prefix_for_0_is_1, candidate, matchedNormalizedPrefix);
    }
    const suffix ***REMOVED*** candidate.substr(matchedNormalizedPrefix.length);
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Loading_0_from_the_root_dir_1_candidate_location_2, suffix, matchedNormalizedPrefix, candidate);
    }
    const resolvedFileName ***REMOVED*** loader(extensions, candidate, !directoryProbablyExists(containingDirectory, state.host), state);
    if (resolvedFileName) {
      return resolvedFileName;
    }
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Trying_other_entries_in_rootDirs);
    }
    for (const rootDir of state.compilerOptions.rootDirs) {
      if (rootDir ***REMOVED******REMOVED******REMOVED*** matchedRootDir) {
        continue;
      }
      const candidate2 ***REMOVED*** combinePaths(normalizePath(rootDir), suffix);
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Loading_0_from_the_root_dir_1_candidate_location_2, suffix, rootDir, candidate2);
      }
      const baseDirectory ***REMOVED*** getDirectoryPath(candidate2);
      const resolvedFileName2 ***REMOVED*** loader(extensions, candidate2, !directoryProbablyExists(baseDirectory, state.host), state);
      if (resolvedFileName2) {
        return resolvedFileName2;
      }
    }
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Module_resolution_using_rootDirs_has_failed);
    }
  }
  return void 0;
}
function tryLoadModuleUsingBaseUrl(extensions, moduleName, loader, state) {
  const { baseUrl } ***REMOVED*** state.compilerOptions;
  if (!baseUrl) {
    return void 0;
  }
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.baseUrl_option_is_set_to_0_using_this_value_to_resolve_non_relative_module_name_1, baseUrl, moduleName);
  }
  const candidate ***REMOVED*** normalizePath(combinePaths(baseUrl, moduleName));
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.Resolving_module_name_0_relative_to_base_url_1_2, moduleName, baseUrl, candidate);
  }
  return loader(extensions, candidate, !directoryProbablyExists(getDirectoryPath(candidate), state.host), state);
}
function resolveJSModule(moduleName, initialDir, host) {
  const { resolvedModule, failedLookupLocations } ***REMOVED*** tryResolveJSModuleWorker(moduleName, initialDir, host);
  if (!resolvedModule) {
    throw new Error(`Could not resolve JS module '${moduleName}' starting at '${initialDir}'. Looked in: ${failedLookupLocations ***REMOVED******REMOVED*** null ? void 0 : failedLookupLocations.join(", ")}`);
  }
  return resolvedModule.resolvedFileName;
}
function node16ModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode) {
  return nodeNextModuleNameResolverWorker(
    30 /* Node16Default */,
    moduleName,
    containingFile,
    compilerOptions,
    host,
    cache,
    redirectedReference,
    resolutionMode
  );
}
function nodeNextModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode) {
  return nodeNextModuleNameResolverWorker(
    30 /* NodeNextDefault */,
    moduleName,
    containingFile,
    compilerOptions,
    host,
    cache,
    redirectedReference,
    resolutionMode
  );
}
function nodeNextModuleNameResolverWorker(features, moduleName, containingFile, compilerOptions, host, cache, redirectedReference, resolutionMode, conditions) {
  const containingDirectory ***REMOVED*** getDirectoryPath(containingFile);
  const esmMode ***REMOVED*** resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? 32 /* EsmMode */ : 0;
  let extensions ***REMOVED*** compilerOptions.noDtsResolution ? 3 /* ImplementationFiles */ : 1 /* TypeScript */ | 2 /* JavaScript */ | 4 /* Declaration */;
  if (getResolveJsonModule(compilerOptions)) {
    extensions |***REMOVED*** 8 /* Json */;
  }
  return nodeModuleNameResolverWorker(
    features | esmMode,
    moduleName,
    containingDirectory,
    compilerOptions,
    host,
    cache,
    extensions,
    /*isConfigLookup*/
    false,
    redirectedReference,
    conditions
  );
}
function tryResolveJSModuleWorker(moduleName, initialDir, host) {
  return nodeModuleNameResolverWorker(
    0 /* None */,
    moduleName,
    initialDir,
    { moduleResolution: 2 /* Node10 */, allowJs: true },
    host,
    /*cache*/
    void 0,
    2 /* JavaScript */,
    /*isConfigLookup*/
    false,
    /*redirectedReference*/
    void 0,
    /*conditions*/
    void 0
  );
}
function bundlerModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, conditions) {
  const containingDirectory ***REMOVED*** getDirectoryPath(containingFile);
  let extensions ***REMOVED*** compilerOptions.noDtsResolution ? 3 /* ImplementationFiles */ : 1 /* TypeScript */ | 2 /* JavaScript */ | 4 /* Declaration */;
  if (getResolveJsonModule(compilerOptions)) {
    extensions |***REMOVED*** 8 /* Json */;
  }
  return nodeModuleNameResolverWorker(
    getNodeResolutionFeatures(compilerOptions),
    moduleName,
    containingDirectory,
    compilerOptions,
    host,
    cache,
    extensions,
    /*isConfigLookup*/
    false,
    redirectedReference,
    conditions
  );
}
function nodeModuleNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference, conditions, isConfigLookup) {
  let extensions;
  if (isConfigLookup) {
    extensions ***REMOVED*** 8 /* Json */;
  } else if (compilerOptions.noDtsResolution) {
    extensions ***REMOVED*** 3 /* ImplementationFiles */;
    if (getResolveJsonModule(compilerOptions)) extensions |***REMOVED*** 8 /* Json */;
  } else {
    extensions ***REMOVED*** getResolveJsonModule(compilerOptions) ? 1 /* TypeScript */ | 2 /* JavaScript */ | 4 /* Declaration */ | 8 /* Json */ : 1 /* TypeScript */ | 2 /* JavaScript */ | 4 /* Declaration */;
  }
  return nodeModuleNameResolverWorker(conditions ? 30 /* AllFeatures */ : 0 /* None */, moduleName, getDirectoryPath(containingFile), compilerOptions, host, cache, extensions, !!isConfigLookup, redirectedReference, conditions);
}
function nodeNextJsonConfigResolver(moduleName, containingFile, host) {
  return nodeModuleNameResolverWorker(
    30 /* NodeNextDefault */,
    moduleName,
    getDirectoryPath(containingFile),
    { moduleResolution: 99 /* NodeNext */ },
    host,
    /*cache*/
    void 0,
    8 /* Json */,
    /*isConfigLookup*/
    true,
    /*redirectedReference*/
    void 0,
    /*conditions*/
    void 0
  );
}
function nodeModuleNameResolverWorker(features, moduleName, containingDirectory, compilerOptions, host, cache, extensions, isConfigLookup, redirectedReference, conditions) {
  var _a, _b, _c, _d, _e;
  const traceEnabled ***REMOVED*** isTraceEnabled(compilerOptions, host);
  const failedLookupLocations ***REMOVED*** [];
  const affectingLocations ***REMOVED*** [];
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(compilerOptions);
  conditions ?? (conditions ***REMOVED*** getConditions(
    compilerOptions,
    moduleResolution ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */ || moduleResolution ***REMOVED******REMOVED******REMOVED*** 2 /* Node10 */ ? void 0 : features & 32 /* EsmMode */ ? 99 /* ESNext */ : 1 /* CommonJS */
  ));
  const diagnostics ***REMOVED*** [];
  const state ***REMOVED*** {
    compilerOptions,
    host,
    traceEnabled,
    failedLookupLocations,
    affectingLocations,
    packageJsonInfoCache: cache,
    features,
    conditions: conditions ?? emptyArray,
    requestContainingDirectory: containingDirectory,
    reportDiagnostic: (diag2) ***REMOVED***> void diagnostics.push(diag2),
    isConfigLookup,
    candidateIsFromPackageJsonField: false,
    resolvedPackageDirectory: false
  };
  if (traceEnabled && moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
    trace(host, Diagnostics.Resolving_in_0_mode_with_conditions_1, features & 32 /* EsmMode */ ? "ESM" : "CJS", state.conditions.map((c) ***REMOVED***> `'${c}'`).join(", "));
  }
  let result;
  if (moduleResolution ***REMOVED******REMOVED******REMOVED*** 2 /* Node10 */) {
    const priorityExtensions ***REMOVED*** extensions & (1 /* TypeScript */ | 4 /* Declaration */);
    const secondaryExtensions ***REMOVED*** extensions & ~(1 /* TypeScript */ | 4 /* Declaration */);
    result ***REMOVED*** priorityExtensions && tryResolve(priorityExtensions, state) || secondaryExtensions && tryResolve(secondaryExtensions, state) || void 0;
  } else {
    result ***REMOVED*** tryResolve(extensions, state);
  }
  let alternateResult;
  if (state.resolvedPackageDirectory && !isConfigLookup && !isExternalModuleNameRelative(moduleName)) {
    const wantedTypesButGotJs ***REMOVED*** (result ***REMOVED******REMOVED*** null ? void 0 : result.value) && extensions & (1 /* TypeScript */ | 4 /* Declaration */) && !extensionIsOk(1 /* TypeScript */ | 4 /* Declaration */, result.value.resolved.extension);
    if (((_a ***REMOVED*** result ***REMOVED******REMOVED*** null ? void 0 : result.value) ***REMOVED******REMOVED*** null ? void 0 : _a.isExternalLibraryImport) && wantedTypesButGotJs && features & 8 /* Exports */ && (conditions ***REMOVED******REMOVED*** null ? void 0 : conditions.includes("import"))) {
      traceIfEnabled(state, Diagnostics.Resolution_of_non_relative_name_failed_trying_with_modern_Node_resolution_features_disabled_to_see_if_npm_library_needs_configuration_update);
      const diagnosticState ***REMOVED*** {
        ...state,
        features: state.features & ~8 /* Exports */,
        reportDiagnostic: noop
      };
      const diagnosticResult ***REMOVED*** tryResolve(extensions & (1 /* TypeScript */ | 4 /* Declaration */), diagnosticState);
      if ((_b ***REMOVED*** diagnosticResult ***REMOVED******REMOVED*** null ? void 0 : diagnosticResult.value) ***REMOVED******REMOVED*** null ? void 0 : _b.isExternalLibraryImport) {
        alternateResult ***REMOVED*** diagnosticResult.value.resolved.path;
      }
    } else if ((!(result ***REMOVED******REMOVED*** null ? void 0 : result.value) || wantedTypesButGotJs) && moduleResolution ***REMOVED******REMOVED******REMOVED*** 2 /* Node10 */) {
      traceIfEnabled(state, Diagnostics.Resolution_of_non_relative_name_failed_trying_with_moduleResolution_bundler_to_see_if_project_may_need_configuration_update);
      const diagnosticsCompilerOptions ***REMOVED*** { ...state.compilerOptions, moduleResolution: 100 /* Bundler */ };
      const diagnosticState ***REMOVED*** {
        ...state,
        compilerOptions: diagnosticsCompilerOptions,
        features: 30 /* BundlerDefault */,
        conditions: getConditions(diagnosticsCompilerOptions),
        reportDiagnostic: noop
      };
      const diagnosticResult ***REMOVED*** tryResolve(extensions & (1 /* TypeScript */ | 4 /* Declaration */), diagnosticState);
      if ((_c ***REMOVED*** diagnosticResult ***REMOVED******REMOVED*** null ? void 0 : diagnosticResult.value) ***REMOVED******REMOVED*** null ? void 0 : _c.isExternalLibraryImport) {
        alternateResult ***REMOVED*** diagnosticResult.value.resolved.path;
      }
    }
  }
  return createResolvedModuleWithFailedLookupLocationsHandlingSymlink(
    moduleName,
    (_d ***REMOVED*** result ***REMOVED******REMOVED*** null ? void 0 : result.value) ***REMOVED******REMOVED*** null ? void 0 : _d.resolved,
    (_e ***REMOVED*** result ***REMOVED******REMOVED*** null ? void 0 : result.value) ***REMOVED******REMOVED*** null ? void 0 : _e.isExternalLibraryImport,
    failedLookupLocations,
    affectingLocations,
    diagnostics,
    state,
    cache,
    alternateResult
  );
  function tryResolve(extensions2, state2) {
    const loader ***REMOVED*** (extensions3, candidate, onlyRecordFailures, state3) ***REMOVED***> nodeLoadModuleByRelativeName(
      extensions3,
      candidate,
      onlyRecordFailures,
      state3,
      /*considerPackageJson*/
      true
    );
    const resolved ***REMOVED*** tryLoadModuleUsingOptionalResolutionSettings(extensions2, moduleName, containingDirectory, loader, state2);
    if (resolved) {
      return toSearchResult({ resolved, isExternalLibraryImport: pathContainsNodeModules(resolved.path) });
    }
    if (!isExternalModuleNameRelative(moduleName)) {
      if (features & 2 /* Imports */ && startsWith(moduleName, "#")) {
        const resolved3 ***REMOVED*** loadModuleFromImports(extensions2, moduleName, containingDirectory, state2, cache, redirectedReference);
        if (resolved3) {
          return resolved3.value && { value: { resolved: resolved3.value, isExternalLibraryImport: false } };
        }
      }
      if (features & 4 /* SelfName */) {
        const resolved3 ***REMOVED*** loadModuleFromSelfNameReference(extensions2, moduleName, containingDirectory, state2, cache, redirectedReference);
        if (resolved3) {
          return resolved3.value && { value: { resolved: resolved3.value, isExternalLibraryImport: false } };
        }
      }
      if (moduleName.includes(":")) {
        if (traceEnabled) {
          trace(host, Diagnostics.Skipping_module_0_that_looks_like_an_absolute_URI_target_file_types_Colon_1, moduleName, formatExtensions(extensions2));
        }
        return void 0;
      }
      if (traceEnabled) {
        trace(host, Diagnostics.Loading_module_0_from_node_modules_folder_target_file_types_Colon_1, moduleName, formatExtensions(extensions2));
      }
      let resolved2 ***REMOVED*** loadModuleFromNearestNodeModulesDirectory(extensions2, moduleName, containingDirectory, state2, cache, redirectedReference);
      if (extensions2 & 4 /* Declaration */) {
        resolved2 ?? (resolved2 ***REMOVED*** resolveFromTypeRoot(moduleName, state2));
      }
      return resolved2 && { value: resolved2.value && { resolved: resolved2.value, isExternalLibraryImport: true } };
    } else {
      const { path: candidate, parts } ***REMOVED*** normalizePathForCJSResolution(containingDirectory, moduleName);
      const resolved2 ***REMOVED*** nodeLoadModuleByRelativeName(
        extensions2,
        candidate,
        /*onlyRecordFailures*/
        false,
        state2,
        /*considerPackageJson*/
        true
      );
      return resolved2 && toSearchResult({ resolved: resolved2, isExternalLibraryImport: contains(parts, "node_modules") });
    }
  }
}
function normalizePathForCJSResolution(containingDirectory, moduleName) {
  const combined ***REMOVED*** combinePaths(containingDirectory, moduleName);
  const parts ***REMOVED*** getPathComponents(combined);
  const lastPart ***REMOVED*** lastOrUndefined(parts);
  const path ***REMOVED*** lastPart ***REMOVED******REMOVED******REMOVED*** "." || lastPart ***REMOVED******REMOVED******REMOVED*** ".." ? ensureTrailingDirectorySeparator(normalizePath(combined)) : normalizePath(combined);
  return { path, parts };
}
function realPath(path, host, traceEnabled) {
  if (!host.realpath) {
    return path;
  }
  const real ***REMOVED*** normalizePath(host.realpath(path));
  if (traceEnabled) {
    trace(host, Diagnostics.Resolving_real_path_for_0_result_1, path, real);
  }
  return real;
}
function nodeLoadModuleByRelativeName(extensions, candidate, onlyRecordFailures, state, considerPackageJson) {
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.Loading_module_as_file_Slash_folder_candidate_module_location_0_target_file_types_Colon_1, candidate, formatExtensions(extensions));
  }
  if (!hasTrailingDirectorySeparator(candidate)) {
    if (!onlyRecordFailures) {
      const parentOfCandidate ***REMOVED*** getDirectoryPath(candidate);
      if (!directoryProbablyExists(parentOfCandidate, state.host)) {
        if (state.traceEnabled) {
          trace(state.host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, parentOfCandidate);
        }
        onlyRecordFailures ***REMOVED*** true;
      }
    }
    const resolvedFromFile ***REMOVED*** loadModuleFromFile(extensions, candidate, onlyRecordFailures, state);
    if (resolvedFromFile) {
      const packageDirectory ***REMOVED*** considerPackageJson ? parseNodeModuleFromPath(resolvedFromFile.path) : void 0;
      const packageInfo ***REMOVED*** packageDirectory ? getPackageJsonInfo(
        packageDirectory,
        /*onlyRecordFailures*/
        false,
        state
      ) : void 0;
      return withPackageId(packageInfo, resolvedFromFile, state);
    }
  }
  if (!onlyRecordFailures) {
    const candidateExists ***REMOVED*** directoryProbablyExists(candidate, state.host);
    if (!candidateExists) {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, candidate);
      }
      onlyRecordFailures ***REMOVED*** true;
    }
  }
  if (!(state.features & 32 /* EsmMode */)) {
    return loadNodeModuleFromDirectory(extensions, candidate, onlyRecordFailures, state, considerPackageJson);
  }
  return void 0;
}
var nodeModulesPathPart ***REMOVED*** "/node_modules/";
function pathContainsNodeModules(path) {
  return path.includes(nodeModulesPathPart);
}
function parseNodeModuleFromPath(resolved, isFolder) {
  const path ***REMOVED*** normalizePath(resolved);
  const idx ***REMOVED*** path.lastIndexOf(nodeModulesPathPart);
  if (idx ***REMOVED******REMOVED******REMOVED*** -1) {
    return void 0;
  }
  const indexAfterNodeModules ***REMOVED*** idx + nodeModulesPathPart.length;
  let indexAfterPackageName ***REMOVED*** moveToNextDirectorySeparatorIfAvailable(path, indexAfterNodeModules, isFolder);
  if (path.charCodeAt(indexAfterNodeModules) ***REMOVED******REMOVED******REMOVED*** 64 /* at */) {
    indexAfterPackageName ***REMOVED*** moveToNextDirectorySeparatorIfAvailable(path, indexAfterPackageName, isFolder);
  }
  return path.slice(0, indexAfterPackageName);
}
function moveToNextDirectorySeparatorIfAvailable(path, prevSeparatorIndex, isFolder) {
  const nextSeparatorIndex ***REMOVED*** path.indexOf(directorySeparator, prevSeparatorIndex + 1);
  return nextSeparatorIndex ***REMOVED******REMOVED******REMOVED*** -1 ? isFolder ? path.length : prevSeparatorIndex : nextSeparatorIndex;
}
function loadModuleFromFileNoPackageId(extensions, candidate, onlyRecordFailures, state) {
  return noPackageId(loadModuleFromFile(extensions, candidate, onlyRecordFailures, state));
}
function loadModuleFromFile(extensions, candidate, onlyRecordFailures, state) {
  const resolvedByReplacingExtension ***REMOVED*** loadModuleFromFileNoImplicitExtensions(extensions, candidate, onlyRecordFailures, state);
  if (resolvedByReplacingExtension) {
    return resolvedByReplacingExtension;
  }
  if (!(state.features & 32 /* EsmMode */)) {
    const resolvedByAddingExtension ***REMOVED*** tryAddingExtensions(candidate, extensions, "", onlyRecordFailures, state);
    if (resolvedByAddingExtension) {
      return resolvedByAddingExtension;
    }
  }
}
function loadModuleFromFileNoImplicitExtensions(extensions, candidate, onlyRecordFailures, state) {
  const filename ***REMOVED*** getBaseFileName(candidate);
  if (!filename.includes(".")) {
    return void 0;
  }
  let extensionless ***REMOVED*** removeFileExtension(candidate);
  if (extensionless ***REMOVED******REMOVED******REMOVED*** candidate) {
    extensionless ***REMOVED*** candidate.substring(0, candidate.lastIndexOf("."));
  }
  const extension ***REMOVED*** candidate.substring(extensionless.length);
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.File_name_0_has_a_1_extension_stripping_it, candidate, extension);
  }
  return tryAddingExtensions(extensionless, extensions, extension, onlyRecordFailures, state);
}
function loadFileNameFromPackageJsonField(extensions, candidate, packageJsonValue, onlyRecordFailures, state) {
  if (extensions & 1 /* TypeScript */ && fileExtensionIsOneOf(candidate, supportedTSImplementationExtensions) || extensions & 4 /* Declaration */ && fileExtensionIsOneOf(candidate, supportedDeclarationExtensions)) {
    const result ***REMOVED*** tryFile(candidate, onlyRecordFailures, state);
    const ext ***REMOVED*** tryExtractTSExtension(candidate);
    return result !***REMOVED******REMOVED*** void 0 ? { path: candidate, ext, resolvedUsingTsExtension: packageJsonValue ? !endsWith(packageJsonValue, ext) : void 0 } : void 0;
  }
  if (state.isConfigLookup && extensions ***REMOVED******REMOVED******REMOVED*** 8 /* Json */ && fileExtensionIs(candidate, ".json" /* Json */)) {
    const result ***REMOVED*** tryFile(candidate, onlyRecordFailures, state);
    return result !***REMOVED******REMOVED*** void 0 ? { path: candidate, ext: ".json" /* Json */, resolvedUsingTsExtension: void 0 } : void 0;
  }
  return loadModuleFromFileNoImplicitExtensions(extensions, candidate, onlyRecordFailures, state);
}
function tryAddingExtensions(candidate, extensions, originalExtension, onlyRecordFailures, state) {
  if (!onlyRecordFailures) {
    const directory ***REMOVED*** getDirectoryPath(candidate);
    if (directory) {
      onlyRecordFailures ***REMOVED*** !directoryProbablyExists(directory, state.host);
    }
  }
  switch (originalExtension) {
    case ".mjs" /* Mjs */:
    case ".mts" /* Mts */:
    case ".d.mts" /* Dmts */:
      return extensions & 1 /* TypeScript */ && tryExtension(".mts" /* Mts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".mts" /* Mts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */) || extensions & 4 /* Declaration */ && tryExtension(".d.mts" /* Dmts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".mts" /* Mts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */) || extensions & 2 /* JavaScript */ && tryExtension(".mjs" /* Mjs */) || void 0;
    case ".cjs" /* Cjs */:
    case ".cts" /* Cts */:
    case ".d.cts" /* Dcts */:
      return extensions & 1 /* TypeScript */ && tryExtension(".cts" /* Cts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".cts" /* Cts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.cts" /* Dcts */) || extensions & 4 /* Declaration */ && tryExtension(".d.cts" /* Dcts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".cts" /* Cts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.cts" /* Dcts */) || extensions & 2 /* JavaScript */ && tryExtension(".cjs" /* Cjs */) || void 0;
    case ".json" /* Json */:
      return extensions & 4 /* Declaration */ && tryExtension(".d.json.ts") || extensions & 8 /* Json */ && tryExtension(".json" /* Json */) || void 0;
    case ".tsx" /* Tsx */:
    case ".jsx" /* Jsx */:
      return extensions & 1 /* TypeScript */ && (tryExtension(".tsx" /* Tsx */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */) || tryExtension(".ts" /* Ts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */)) || extensions & 4 /* Declaration */ && tryExtension(".d.ts" /* Dts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */) || extensions & 2 /* JavaScript */ && (tryExtension(".jsx" /* Jsx */) || tryExtension(".js" /* Js */)) || void 0;
    case ".ts" /* Ts */:
    case ".d.ts" /* Dts */:
    case ".js" /* Js */:
    case "":
      return extensions & 1 /* TypeScript */ && (tryExtension(".ts" /* Ts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */) || tryExtension(".tsx" /* Tsx */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */)) || extensions & 4 /* Declaration */ && tryExtension(".d.ts" /* Dts */, originalExtension ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || originalExtension ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */) || extensions & 2 /* JavaScript */ && (tryExtension(".js" /* Js */) || tryExtension(".jsx" /* Jsx */)) || state.isConfigLookup && tryExtension(".json" /* Json */) || void 0;
    default:
      return extensions & 4 /* Declaration */ && !isDeclarationFileName(candidate + originalExtension) && tryExtension(`.d${originalExtension}.ts`) || void 0;
  }
  function tryExtension(ext, resolvedUsingTsExtension) {
    const path ***REMOVED*** tryFile(candidate + ext, onlyRecordFailures, state);
    return path ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : { path, ext, resolvedUsingTsExtension: !state.candidateIsFromPackageJsonField && resolvedUsingTsExtension };
  }
}
function tryFile(fileName, onlyRecordFailures, state) {
  var _a;
  if (!((_a ***REMOVED*** state.compilerOptions.moduleSuffixes) ***REMOVED******REMOVED*** null ? void 0 : _a.length)) {
    return tryFileLookup(fileName, onlyRecordFailures, state);
  }
  const ext ***REMOVED*** tryGetExtensionFromPath2(fileName) ?? "";
  const fileNameNoExtension ***REMOVED*** ext ? removeExtension(fileName, ext) : fileName;
  return forEach(state.compilerOptions.moduleSuffixes, (suffix) ***REMOVED***> tryFileLookup(fileNameNoExtension + suffix + ext, onlyRecordFailures, state));
}
function tryFileLookup(fileName, onlyRecordFailures, state) {
  var _a;
  if (!onlyRecordFailures) {
    if (state.host.fileExists(fileName)) {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.File_0_exists_use_it_as_a_name_resolution_result, fileName);
      }
      return fileName;
    } else {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.File_0_does_not_exist, fileName);
      }
    }
  }
  (_a ***REMOVED*** state.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.push(fileName);
  return void 0;
}
function loadNodeModuleFromDirectory(extensions, candidate, onlyRecordFailures, state, considerPackageJson ***REMOVED*** true) {
  const packageInfo ***REMOVED*** considerPackageJson ? getPackageJsonInfo(candidate, onlyRecordFailures, state) : void 0;
  return withPackageId(packageInfo, loadNodeModuleFromDirectoryWorker(extensions, candidate, onlyRecordFailures, state, packageInfo), state);
}
function getTemporaryModuleResolutionState(packageJsonInfoCache, host, options) {
  return {
    host,
    compilerOptions: options,
    traceEnabled: isTraceEnabled(options, host),
    failedLookupLocations: void 0,
    affectingLocations: void 0,
    packageJsonInfoCache,
    features: 0 /* None */,
    conditions: emptyArray,
    requestContainingDirectory: void 0,
    reportDiagnostic: noop,
    isConfigLookup: false,
    candidateIsFromPackageJsonField: false,
    resolvedPackageDirectory: false
  };
}
function getPackageScopeForPath(directory, state) {
  return forEachAncestorDirectoryStoppingAtGlobalCache(
    state.host,
    directory,
    (dir) ***REMOVED***> getPackageJsonInfo(
      dir,
      /*onlyRecordFailures*/
      false,
      state
    )
  );
}
function getVersionPathsOfPackageJsonInfo(packageJsonInfo, state) {
  if (packageJsonInfo.contents.versionPaths ***REMOVED******REMOVED******REMOVED*** void 0) {
    packageJsonInfo.contents.versionPaths ***REMOVED*** readPackageJsonTypesVersionPaths(packageJsonInfo.contents.packageJsonContent, state) || false;
  }
  return packageJsonInfo.contents.versionPaths || void 0;
}
function getPeerDependenciesOfPackageJsonInfo(packageJsonInfo, state) {
  if (packageJsonInfo.contents.peerDependencies ***REMOVED******REMOVED******REMOVED*** void 0) {
    packageJsonInfo.contents.peerDependencies ***REMOVED*** readPackageJsonPeerDependencies(packageJsonInfo, state) || false;
  }
  return packageJsonInfo.contents.peerDependencies || void 0;
}
function readPackageJsonPeerDependencies(packageJsonInfo, state) {
  const peerDependencies ***REMOVED*** readPackageJsonField(packageJsonInfo.contents.packageJsonContent, "peerDependencies", "object", state);
  if (peerDependencies ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
  if (state.traceEnabled) trace(state.host, Diagnostics.package_json_has_a_peerDependencies_field);
  const packageDirectory ***REMOVED*** realPath(packageJsonInfo.packageDirectory, state.host, state.traceEnabled);
  const nodeModules ***REMOVED*** packageDirectory.substring(0, packageDirectory.lastIndexOf("node_modules") + "node_modules".length) + directorySeparator;
  let result ***REMOVED*** "";
  for (const key in peerDependencies) {
    if (hasProperty(peerDependencies, key)) {
      const peerPackageJson ***REMOVED*** getPackageJsonInfo(
        nodeModules + key,
        /*onlyRecordFailures*/
        false,
        state
      );
      if (peerPackageJson) {
        const version2 ***REMOVED*** peerPackageJson.contents.packageJsonContent.version;
        result +***REMOVED*** `+${key}@${version2}`;
        if (state.traceEnabled) trace(state.host, Diagnostics.Found_peerDependency_0_with_1_version, key, version2);
      } else {
        if (state.traceEnabled) trace(state.host, Diagnostics.Failed_to_find_peerDependency_0, key);
      }
    }
  }
  return result;
}
function getPackageJsonInfo(packageDirectory, onlyRecordFailures, state) {
  var _a, _b, _c, _d, _e, _f;
  const { host, traceEnabled } ***REMOVED*** state;
  const packageJsonPath ***REMOVED*** combinePaths(packageDirectory, "package.json");
  if (onlyRecordFailures) {
    (_a ***REMOVED*** state.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.push(packageJsonPath);
    return void 0;
  }
  const existing ***REMOVED*** (_b ***REMOVED*** state.packageJsonInfoCache) ***REMOVED******REMOVED*** null ? void 0 : _b.getPackageJsonInfo(packageJsonPath);
  if (existing !***REMOVED******REMOVED*** void 0) {
    if (isPackageJsonInfo(existing)) {
      if (traceEnabled) trace(host, Diagnostics.File_0_exists_according_to_earlier_cached_lookups, packageJsonPath);
      (_c ***REMOVED*** state.affectingLocations) ***REMOVED******REMOVED*** null ? void 0 : _c.push(packageJsonPath);
      return existing.packageDirectory ***REMOVED******REMOVED******REMOVED*** packageDirectory ? existing : { packageDirectory, contents: existing.contents };
    } else {
      if (existing.directoryExists && traceEnabled) trace(host, Diagnostics.File_0_does_not_exist_according_to_earlier_cached_lookups, packageJsonPath);
      (_d ***REMOVED*** state.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _d.push(packageJsonPath);
      return void 0;
    }
  }
  const directoryExists ***REMOVED*** directoryProbablyExists(packageDirectory, host);
  if (directoryExists && host.fileExists(packageJsonPath)) {
    const packageJsonContent ***REMOVED*** readJson(packageJsonPath, host);
    if (traceEnabled) {
      trace(host, Diagnostics.Found_package_json_at_0, packageJsonPath);
    }
    const result ***REMOVED*** { packageDirectory, contents: { packageJsonContent, versionPaths: void 0, resolvedEntrypoints: void 0, peerDependencies: void 0 } };
    if (state.packageJsonInfoCache && !state.packageJsonInfoCache.isReadonly) state.packageJsonInfoCache.setPackageJsonInfo(packageJsonPath, result);
    (_e ***REMOVED*** state.affectingLocations) ***REMOVED******REMOVED*** null ? void 0 : _e.push(packageJsonPath);
    return result;
  } else {
    if (directoryExists && traceEnabled) {
      trace(host, Diagnostics.File_0_does_not_exist, packageJsonPath);
    }
    if (state.packageJsonInfoCache && !state.packageJsonInfoCache.isReadonly) state.packageJsonInfoCache.setPackageJsonInfo(packageJsonPath, { packageDirectory, directoryExists });
    (_f ***REMOVED*** state.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _f.push(packageJsonPath);
  }
}
function loadNodeModuleFromDirectoryWorker(extensions, candidate, onlyRecordFailures, state, packageJson) {
  const versionPaths ***REMOVED*** packageJson && getVersionPathsOfPackageJsonInfo(packageJson, state);
  let packageFile;
  if (packageJson && arePathsEqual(packageJson ***REMOVED******REMOVED*** null ? void 0 : packageJson.packageDirectory, candidate, state.host)) {
    if (state.isConfigLookup) {
      packageFile ***REMOVED*** readPackageJsonTSConfigField(packageJson.contents.packageJsonContent, packageJson.packageDirectory, state);
    } else {
      packageFile ***REMOVED*** extensions & 4 /* Declaration */ && readPackageJsonTypesFields(packageJson.contents.packageJsonContent, packageJson.packageDirectory, state) || extensions & (3 /* ImplementationFiles */ | 4 /* Declaration */) && readPackageJsonMainField(packageJson.contents.packageJsonContent, packageJson.packageDirectory, state) || void 0;
    }
  }
  const loader ***REMOVED*** (extensions2, candidate2, onlyRecordFailures2, state2) ***REMOVED***> {
    const fromFile ***REMOVED*** loadFileNameFromPackageJsonField(
      extensions2,
      candidate2,
      /*packageJsonValue*/
      void 0,
      onlyRecordFailures2,
      state2
    );
    if (fromFile) {
      return noPackageId(fromFile);
    }
    const expandedExtensions ***REMOVED*** extensions2 ***REMOVED******REMOVED******REMOVED*** 4 /* Declaration */ ? 1 /* TypeScript */ | 4 /* Declaration */ : extensions2;
    const features ***REMOVED*** state2.features;
    const candidateIsFromPackageJsonField ***REMOVED*** state2.candidateIsFromPackageJsonField;
    state2.candidateIsFromPackageJsonField ***REMOVED*** true;
    if ((packageJson ***REMOVED******REMOVED*** null ? void 0 : packageJson.contents.packageJsonContent.type) !***REMOVED******REMOVED*** "module") {
      state2.features &***REMOVED*** ~32 /* EsmMode */;
    }
    const result ***REMOVED*** nodeLoadModuleByRelativeName(
      expandedExtensions,
      candidate2,
      onlyRecordFailures2,
      state2,
      /*considerPackageJson*/
      false
    );
    state2.features ***REMOVED*** features;
    state2.candidateIsFromPackageJsonField ***REMOVED*** candidateIsFromPackageJsonField;
    return result;
  };
  const onlyRecordFailuresForPackageFile ***REMOVED*** packageFile ? !directoryProbablyExists(getDirectoryPath(packageFile), state.host) : void 0;
  const onlyRecordFailuresForIndex ***REMOVED*** onlyRecordFailures || !directoryProbablyExists(candidate, state.host);
  const indexPath ***REMOVED*** combinePaths(candidate, state.isConfigLookup ? "tsconfig" : "index");
  if (versionPaths && (!packageFile || containsPath(candidate, packageFile))) {
    const moduleName ***REMOVED*** getRelativePathFromDirectory(
      candidate,
      packageFile || indexPath,
      /*ignoreCase*/
      false
    );
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_has_a_typesVersions_entry_0_that_matches_compiler_version_1_looking_for_a_pattern_to_match_module_name_2, versionPaths.version, version, moduleName);
    }
    const pathPatterns ***REMOVED*** tryParsePatterns(versionPaths.paths);
    const result ***REMOVED*** tryLoadModuleUsingPaths(extensions, moduleName, candidate, versionPaths.paths, pathPatterns, loader, onlyRecordFailuresForPackageFile || onlyRecordFailuresForIndex, state);
    if (result) {
      return removeIgnoredPackageId(result.value);
    }
  }
  const packageFileResult ***REMOVED*** packageFile && removeIgnoredPackageId(loader(extensions, packageFile, onlyRecordFailuresForPackageFile, state));
  if (packageFileResult) return packageFileResult;
  if (!(state.features & 32 /* EsmMode */)) {
    return loadModuleFromFile(extensions, indexPath, onlyRecordFailuresForIndex, state);
  }
}
function extensionIsOk(extensions, extension) {
  return extensions & 2 /* JavaScript */ && (extension ***REMOVED******REMOVED******REMOVED*** ".js" /* Js */ || extension ***REMOVED******REMOVED******REMOVED*** ".jsx" /* Jsx */ || extension ***REMOVED******REMOVED******REMOVED*** ".mjs" /* Mjs */ || extension ***REMOVED******REMOVED******REMOVED*** ".cjs" /* Cjs */) || extensions & 1 /* TypeScript */ && (extension ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || extension ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */ || extension ***REMOVED******REMOVED******REMOVED*** ".mts" /* Mts */ || extension ***REMOVED******REMOVED******REMOVED*** ".cts" /* Cts */) || extensions & 4 /* Declaration */ && (extension ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */ || extension ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */ || extension ***REMOVED******REMOVED******REMOVED*** ".d.cts" /* Dcts */) || extensions & 8 /* Json */ && extension ***REMOVED******REMOVED******REMOVED*** ".json" /* Json */ || false;
}
function parsePackageName(moduleName) {
  let idx ***REMOVED*** moduleName.indexOf(directorySeparator);
  if (moduleName[0] ***REMOVED******REMOVED******REMOVED*** "@") {
    idx ***REMOVED*** moduleName.indexOf(directorySeparator, idx + 1);
  }
  return idx ***REMOVED******REMOVED******REMOVED*** -1 ? { packageName: moduleName, rest: "" } : { packageName: moduleName.slice(0, idx), rest: moduleName.slice(idx + 1) };
}
function allKeysStartWithDot(obj) {
  return every(getOwnKeys(obj), (k) ***REMOVED***> startsWith(k, "."));
}
function noKeyStartsWithDot(obj) {
  return !some(getOwnKeys(obj), (k) ***REMOVED***> startsWith(k, "."));
}
function loadModuleFromSelfNameReference(extensions, moduleName, directory, state, cache, redirectedReference) {
  var _a, _b;
  const directoryPath ***REMOVED*** getNormalizedAbsolutePath(directory, (_b ***REMOVED*** (_a ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a));
  const scope ***REMOVED*** getPackageScopeForPath(directoryPath, state);
  if (!scope || !scope.contents.packageJsonContent.exports) {
    return void 0;
  }
  if (typeof scope.contents.packageJsonContent.name !***REMOVED******REMOVED*** "string") {
    return void 0;
  }
  const parts ***REMOVED*** getPathComponents(moduleName);
  const nameParts ***REMOVED*** getPathComponents(scope.contents.packageJsonContent.name);
  if (!every(nameParts, (p, i) ***REMOVED***> parts[i] ***REMOVED******REMOVED******REMOVED*** p)) {
    return void 0;
  }
  const trailingParts ***REMOVED*** parts.slice(nameParts.length);
  const subpath ***REMOVED*** !length(trailingParts) ? "." : `.${directorySeparator}${trailingParts.join(directorySeparator)}`;
  if (getAllowJSCompilerOption(state.compilerOptions) && !pathContainsNodeModules(directory)) {
    return loadModuleFromExports(scope, extensions, subpath, state, cache, redirectedReference);
  }
  const priorityExtensions ***REMOVED*** extensions & (1 /* TypeScript */ | 4 /* Declaration */);
  const secondaryExtensions ***REMOVED*** extensions & ~(1 /* TypeScript */ | 4 /* Declaration */);
  return loadModuleFromExports(scope, priorityExtensions, subpath, state, cache, redirectedReference) || loadModuleFromExports(scope, secondaryExtensions, subpath, state, cache, redirectedReference);
}
function loadModuleFromExports(scope, extensions, subpath, state, cache, redirectedReference) {
  if (!scope.contents.packageJsonContent.exports) {
    return void 0;
  }
  if (subpath ***REMOVED******REMOVED******REMOVED*** ".") {
    let mainExport;
    if (typeof scope.contents.packageJsonContent.exports ***REMOVED******REMOVED******REMOVED*** "string" || Array.isArray(scope.contents.packageJsonContent.exports) || typeof scope.contents.packageJsonContent.exports ***REMOVED******REMOVED******REMOVED*** "object" && noKeyStartsWithDot(scope.contents.packageJsonContent.exports)) {
      mainExport ***REMOVED*** scope.contents.packageJsonContent.exports;
    } else if (hasProperty(scope.contents.packageJsonContent.exports, ".")) {
      mainExport ***REMOVED*** scope.contents.packageJsonContent.exports["."];
    }
    if (mainExport) {
      const loadModuleFromTargetExportOrImport ***REMOVED*** getLoadModuleFromTargetExportOrImport(
        extensions,
        state,
        cache,
        redirectedReference,
        subpath,
        scope,
        /*isImports*/
        false
      );
      return loadModuleFromTargetExportOrImport(
        mainExport,
        "",
        /*pattern*/
        false,
        "."
      );
    }
  } else if (allKeysStartWithDot(scope.contents.packageJsonContent.exports)) {
    if (typeof scope.contents.packageJsonContent.exports !***REMOVED******REMOVED*** "object") {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Export_specifier_0_does_not_exist_in_package_json_scope_at_path_1, subpath, scope.packageDirectory);
      }
      return toSearchResult(
        /*value*/
        void 0
      );
    }
    const result ***REMOVED*** loadModuleFromExportsOrImports(
      extensions,
      state,
      cache,
      redirectedReference,
      subpath,
      scope.contents.packageJsonContent.exports,
      scope,
      /*isImports*/
      false
    );
    if (result) {
      return result;
    }
  }
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.Export_specifier_0_does_not_exist_in_package_json_scope_at_path_1, subpath, scope.packageDirectory);
  }
  return toSearchResult(
    /*value*/
    void 0
  );
}
function loadModuleFromImports(extensions, moduleName, directory, state, cache, redirectedReference) {
  var _a, _b;
  if (moduleName ***REMOVED******REMOVED******REMOVED*** "#" || startsWith(moduleName, "#/")) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Invalid_import_specifier_0_has_no_possible_resolutions, moduleName);
    }
    return toSearchResult(
      /*value*/
      void 0
    );
  }
  const directoryPath ***REMOVED*** getNormalizedAbsolutePath(directory, (_b ***REMOVED*** (_a ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a));
  const scope ***REMOVED*** getPackageScopeForPath(directoryPath, state);
  if (!scope) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Directory_0_has_no_containing_package_json_scope_Imports_will_not_resolve, directoryPath);
    }
    return toSearchResult(
      /*value*/
      void 0
    );
  }
  if (!scope.contents.packageJsonContent.imports) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_scope_0_has_no_imports_defined, scope.packageDirectory);
    }
    return toSearchResult(
      /*value*/
      void 0
    );
  }
  const result ***REMOVED*** loadModuleFromExportsOrImports(
    extensions,
    state,
    cache,
    redirectedReference,
    moduleName,
    scope.contents.packageJsonContent.imports,
    scope,
    /*isImports*/
    true
  );
  if (result) {
    return result;
  }
  if (state.traceEnabled) {
    trace(state.host, Diagnostics.Import_specifier_0_does_not_exist_in_package_json_scope_at_path_1, moduleName, scope.packageDirectory);
  }
  return toSearchResult(
    /*value*/
    void 0
  );
}
function comparePatternKeys(a, b) {
  const aPatternIndex ***REMOVED*** a.indexOf("*");
  const bPatternIndex ***REMOVED*** b.indexOf("*");
  const baseLenA ***REMOVED*** aPatternIndex ***REMOVED******REMOVED******REMOVED*** -1 ? a.length : aPatternIndex + 1;
  const baseLenB ***REMOVED*** bPatternIndex ***REMOVED******REMOVED******REMOVED*** -1 ? b.length : bPatternIndex + 1;
  if (baseLenA > baseLenB) return -1 /* LessThan */;
  if (baseLenB > baseLenA) return 1 /* GreaterThan */;
  if (aPatternIndex ***REMOVED******REMOVED******REMOVED*** -1) return 1 /* GreaterThan */;
  if (bPatternIndex ***REMOVED******REMOVED******REMOVED*** -1) return -1 /* LessThan */;
  if (a.length > b.length) return -1 /* LessThan */;
  if (b.length > a.length) return 1 /* GreaterThan */;
  return 0 /* EqualTo */;
}
function loadModuleFromExportsOrImports(extensions, state, cache, redirectedReference, moduleName, lookupTable, scope, isImports) {
  const loadModuleFromTargetExportOrImport ***REMOVED*** getLoadModuleFromTargetExportOrImport(extensions, state, cache, redirectedReference, moduleName, scope, isImports);
  if (!endsWith(moduleName, directorySeparator) && !moduleName.includes("*") && hasProperty(lookupTable, moduleName)) {
    const target ***REMOVED*** lookupTable[moduleName];
    return loadModuleFromTargetExportOrImport(
      target,
      /*subpath*/
      "",
      /*pattern*/
      false,
      moduleName
    );
  }
  const expandingKeys ***REMOVED*** toSorted(filter(getOwnKeys(lookupTable), (k) ***REMOVED***> hasOneAsterisk(k) || endsWith(k, "/")), comparePatternKeys);
  for (const potentialTarget of expandingKeys) {
    if (state.features & 16 /* ExportsPatternTrailers */ && matchesPatternWithTrailer(potentialTarget, moduleName)) {
      const target ***REMOVED*** lookupTable[potentialTarget];
      const starPos ***REMOVED*** potentialTarget.indexOf("*");
      const subpath ***REMOVED*** moduleName.substring(potentialTarget.substring(0, starPos).length, moduleName.length - (potentialTarget.length - 1 - starPos));
      return loadModuleFromTargetExportOrImport(
        target,
        subpath,
        /*pattern*/
        true,
        potentialTarget
      );
    } else if (endsWith(potentialTarget, "*") && startsWith(moduleName, potentialTarget.substring(0, potentialTarget.length - 1))) {
      const target ***REMOVED*** lookupTable[potentialTarget];
      const subpath ***REMOVED*** moduleName.substring(potentialTarget.length - 1);
      return loadModuleFromTargetExportOrImport(
        target,
        subpath,
        /*pattern*/
        true,
        potentialTarget
      );
    } else if (startsWith(moduleName, potentialTarget)) {
      const target ***REMOVED*** lookupTable[potentialTarget];
      const subpath ***REMOVED*** moduleName.substring(potentialTarget.length);
      return loadModuleFromTargetExportOrImport(
        target,
        subpath,
        /*pattern*/
        false,
        potentialTarget
      );
    }
  }
  function matchesPatternWithTrailer(target, name) {
    if (endsWith(target, "*")) return false;
    const starPos ***REMOVED*** target.indexOf("*");
    if (starPos ***REMOVED******REMOVED******REMOVED*** -1) return false;
    return startsWith(name, target.substring(0, starPos)) && endsWith(name, target.substring(starPos + 1));
  }
}
function hasOneAsterisk(patternKey) {
  const firstStar ***REMOVED*** patternKey.indexOf("*");
  return firstStar !***REMOVED******REMOVED*** -1 && firstStar ***REMOVED******REMOVED******REMOVED*** patternKey.lastIndexOf("*");
}
function getLoadModuleFromTargetExportOrImport(extensions, state, cache, redirectedReference, moduleName, scope, isImports) {
  return loadModuleFromTargetExportOrImport;
  function loadModuleFromTargetExportOrImport(target, subpath, pattern, key) {
    var _a, _b;
    if (typeof target ***REMOVED******REMOVED******REMOVED*** "string") {
      if (!pattern && subpath.length > 0 && !endsWith(target, "/")) {
        if (state.traceEnabled) {
          trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
        }
        return toSearchResult(
          /*value*/
          void 0
        );
      }
      if (!startsWith(target, "./")) {
        if (isImports && !startsWith(target, "../") && !startsWith(target, "/") && !isRootedDiskPath(target)) {
          const combinedLookup ***REMOVED*** pattern ? target.replace(/\*/g, subpath) : target + subpath;
          traceIfEnabled(state, Diagnostics.Using_0_subpath_1_with_target_2, "imports", key, combinedLookup);
          traceIfEnabled(state, Diagnostics.Resolving_module_0_from_1, combinedLookup, scope.packageDirectory + "/");
          const result ***REMOVED*** nodeModuleNameResolverWorker(
            state.features,
            combinedLookup,
            scope.packageDirectory + "/",
            state.compilerOptions,
            state.host,
            cache,
            extensions,
            /*isConfigLookup*/
            false,
            redirectedReference,
            state.conditions
          );
          (_a ***REMOVED*** state.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.push(...result.failedLookupLocations ?? emptyArray);
          (_b ***REMOVED*** state.affectingLocations) ***REMOVED******REMOVED*** null ? void 0 : _b.push(...result.affectingLocations ?? emptyArray);
          return toSearchResult(
            result.resolvedModule ? {
              path: result.resolvedModule.resolvedFileName,
              extension: result.resolvedModule.extension,
              packageId: result.resolvedModule.packageId,
              originalPath: result.resolvedModule.originalPath,
              resolvedUsingTsExtension: result.resolvedModule.resolvedUsingTsExtension
            } : void 0
          );
        }
        if (state.traceEnabled) {
          trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
        }
        return toSearchResult(
          /*value*/
          void 0
        );
      }
      const parts ***REMOVED*** pathIsRelative(target) ? getPathComponents(target).slice(1) : getPathComponents(target);
      const partsAfterFirst ***REMOVED*** parts.slice(1);
      if (partsAfterFirst.includes("..") || partsAfterFirst.includes(".") || partsAfterFirst.includes("node_modules")) {
        if (state.traceEnabled) {
          trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
        }
        return toSearchResult(
          /*value*/
          void 0
        );
      }
      const resolvedTarget ***REMOVED*** combinePaths(scope.packageDirectory, target);
      const subpathParts ***REMOVED*** getPathComponents(subpath);
      if (subpathParts.includes("..") || subpathParts.includes(".") || subpathParts.includes("node_modules")) {
        if (state.traceEnabled) {
          trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
        }
        return toSearchResult(
          /*value*/
          void 0
        );
      }
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Using_0_subpath_1_with_target_2, isImports ? "imports" : "exports", key, pattern ? target.replace(/\*/g, subpath) : target + subpath);
      }
      const finalPath ***REMOVED*** toAbsolutePath(pattern ? resolvedTarget.replace(/\*/g, subpath) : resolvedTarget + subpath);
      const inputLink ***REMOVED*** tryLoadInputFileForPath(finalPath, subpath, combinePaths(scope.packageDirectory, "package.json"), isImports);
      if (inputLink) return inputLink;
      return toSearchResult(withPackageId(scope, loadFileNameFromPackageJsonField(
        extensions,
        finalPath,
        target,
        /*onlyRecordFailures*/
        false,
        state
      ), state));
    } else if (typeof target ***REMOVED******REMOVED******REMOVED*** "object" && target !***REMOVED******REMOVED*** null) {
      if (!Array.isArray(target)) {
        traceIfEnabled(state, Diagnostics.Entering_conditional_exports);
        for (const condition of getOwnKeys(target)) {
          if (condition ***REMOVED******REMOVED******REMOVED*** "default" || state.conditions.includes(condition) || isApplicableVersionedTypesKey(state.conditions, condition)) {
            traceIfEnabled(state, Diagnostics.Matched_0_condition_1, isImports ? "imports" : "exports", condition);
            const subTarget ***REMOVED*** target[condition];
            const result ***REMOVED*** loadModuleFromTargetExportOrImport(subTarget, subpath, pattern, key);
            if (result) {
              traceIfEnabled(state, Diagnostics.Resolved_under_condition_0, condition);
              traceIfEnabled(state, Diagnostics.Exiting_conditional_exports);
              return result;
            } else {
              traceIfEnabled(state, Diagnostics.Failed_to_resolve_under_condition_0, condition);
            }
          } else {
            traceIfEnabled(state, Diagnostics.Saw_non_matching_condition_0, condition);
          }
        }
        traceIfEnabled(state, Diagnostics.Exiting_conditional_exports);
        return void 0;
      } else {
        if (!length(target)) {
          if (state.traceEnabled) {
            trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
          }
          return toSearchResult(
            /*value*/
            void 0
          );
        }
        for (const elem of target) {
          const result ***REMOVED*** loadModuleFromTargetExportOrImport(elem, subpath, pattern, key);
          if (result) {
            return result;
          }
        }
      }
    } else if (target ***REMOVED******REMOVED******REMOVED*** null) {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.package_json_scope_0_explicitly_maps_specifier_1_to_null, scope.packageDirectory, moduleName);
      }
      return toSearchResult(
        /*value*/
        void 0
      );
    }
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_scope_0_has_invalid_type_for_target_of_specifier_1, scope.packageDirectory, moduleName);
    }
    return toSearchResult(
      /*value*/
      void 0
    );
    function toAbsolutePath(path) {
      var _a2, _b2;
      if (path ***REMOVED******REMOVED******REMOVED*** void 0) return path;
      return getNormalizedAbsolutePath(path, (_b2 ***REMOVED*** (_a2 ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _b2.call(_a2));
    }
    function combineDirectoryPath(root, dir) {
      return ensureTrailingDirectorySeparator(combinePaths(root, dir));
    }
    function tryLoadInputFileForPath(finalPath, entry, packagePath, isImports2) {
      var _a2, _b2, _c, _d;
      if (!state.isConfigLookup && (state.compilerOptions.declarationDir || state.compilerOptions.outDir) && !finalPath.includes("/node_modules/") && (state.compilerOptions.configFile ? containsPath(scope.packageDirectory, toAbsolutePath(state.compilerOptions.configFile.fileName), !useCaseSensitiveFileNames(state)) : true)) {
        const getCanonicalFileName ***REMOVED*** hostGetCanonicalFileName({ useCaseSensitiveFileNames: () ***REMOVED***> useCaseSensitiveFileNames(state) });
        const commonSourceDirGuesses ***REMOVED*** [];
        if (state.compilerOptions.rootDir || state.compilerOptions.composite && state.compilerOptions.configFilePath) {
          const commonDir ***REMOVED*** toAbsolutePath(getCommonSourceDirectory(state.compilerOptions, () ***REMOVED***> [], ((_b2 ***REMOVED*** (_a2 ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _b2.call(_a2)) || "", getCanonicalFileName));
          commonSourceDirGuesses.push(commonDir);
        } else if (state.requestContainingDirectory) {
          const requestingFile ***REMOVED*** toAbsolutePath(combinePaths(state.requestContainingDirectory, "index.ts"));
          const commonDir ***REMOVED*** toAbsolutePath(getCommonSourceDirectory(state.compilerOptions, () ***REMOVED***> [requestingFile, toAbsolutePath(packagePath)], ((_d ***REMOVED*** (_c ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _d.call(_c)) || "", getCanonicalFileName));
          commonSourceDirGuesses.push(commonDir);
          let fragment ***REMOVED*** ensureTrailingDirectorySeparator(commonDir);
          while (fragment && fragment.length > 1) {
            const parts ***REMOVED*** getPathComponents(fragment);
            parts.pop();
            const commonDir2 ***REMOVED*** getPathFromPathComponents(parts);
            commonSourceDirGuesses.unshift(commonDir2);
            fragment ***REMOVED*** ensureTrailingDirectorySeparator(commonDir2);
          }
        }
        if (commonSourceDirGuesses.length > 1) {
          state.reportDiagnostic(createCompilerDiagnostic(
            isImports2 ? Diagnostics.The_project_root_is_ambiguous_but_is_required_to_resolve_import_map_entry_0_in_file_1_Supply_the_rootDir_compiler_option_to_disambiguate : Diagnostics.The_project_root_is_ambiguous_but_is_required_to_resolve_export_map_entry_0_in_file_1_Supply_the_rootDir_compiler_option_to_disambiguate,
            entry ***REMOVED******REMOVED******REMOVED*** "" ? "." : entry,
            // replace empty string with `.` - the reverse of the operation done when entries are built - so main entrypoint errors don't look weird
            packagePath
          ));
        }
        for (const commonSourceDirGuess of commonSourceDirGuesses) {
          const candidateDirectories ***REMOVED*** getOutputDirectoriesForBaseDirectory(commonSourceDirGuess);
          for (const candidateDir of candidateDirectories) {
            if (containsPath(candidateDir, finalPath, !useCaseSensitiveFileNames(state))) {
              const pathFragment ***REMOVED*** finalPath.slice(candidateDir.length + 1);
              const possibleInputBase ***REMOVED*** combinePaths(commonSourceDirGuess, pathFragment);
              const jsAndDtsExtensions ***REMOVED*** [".mjs" /* Mjs */, ".cjs" /* Cjs */, ".js" /* Js */, ".json" /* Json */, ".d.mts" /* Dmts */, ".d.cts" /* Dcts */, ".d.ts" /* Dts */];
              for (const ext of jsAndDtsExtensions) {
                if (fileExtensionIs(possibleInputBase, ext)) {
                  const inputExts ***REMOVED*** getPossibleOriginalInputExtensionForExtension(possibleInputBase);
                  for (const possibleExt of inputExts) {
                    if (!extensionIsOk(extensions, possibleExt)) continue;
                    const possibleInputWithInputExtension ***REMOVED*** changeAnyExtension(possibleInputBase, possibleExt, ext, !useCaseSensitiveFileNames(state));
                    if (state.host.fileExists(possibleInputWithInputExtension)) {
                      return toSearchResult(withPackageId(scope, loadFileNameFromPackageJsonField(
                        extensions,
                        possibleInputWithInputExtension,
                        /*packageJsonValue*/
                        void 0,
                        /*onlyRecordFailures*/
                        false,
                        state
                      ), state));
                    }
                  }
                }
              }
            }
          }
        }
      }
      return void 0;
      function getOutputDirectoriesForBaseDirectory(commonSourceDirGuess) {
        var _a3, _b3;
        const currentDir ***REMOVED*** state.compilerOptions.configFile ? ((_b3 ***REMOVED*** (_a3 ***REMOVED*** state.host).getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _b3.call(_a3)) || "" : commonSourceDirGuess;
        const candidateDirectories ***REMOVED*** [];
        if (state.compilerOptions.declarationDir) {
          candidateDirectories.push(toAbsolutePath(combineDirectoryPath(currentDir, state.compilerOptions.declarationDir)));
        }
        if (state.compilerOptions.outDir && state.compilerOptions.outDir !***REMOVED******REMOVED*** state.compilerOptions.declarationDir) {
          candidateDirectories.push(toAbsolutePath(combineDirectoryPath(currentDir, state.compilerOptions.outDir)));
        }
        return candidateDirectories;
      }
    }
  }
}
function isApplicableVersionedTypesKey(conditions, key) {
  if (!conditions.includes("types")) return false;
  if (!startsWith(key, "types@")) return false;
  const range ***REMOVED*** VersionRange.tryParse(key.substring("types@".length));
  if (!range) return false;
  return range.test(version);
}
function loadModuleFromNearestNodeModulesDirectory(extensions, moduleName, directory, state, cache, redirectedReference) {
  return loadModuleFromNearestNodeModulesDirectoryWorker(
    extensions,
    moduleName,
    directory,
    state,
    /*typesScopeOnly*/
    false,
    cache,
    redirectedReference
  );
}
function loadModuleFromNearestNodeModulesDirectoryTypesScope(moduleName, directory, state) {
  return loadModuleFromNearestNodeModulesDirectoryWorker(
    4 /* Declaration */,
    moduleName,
    directory,
    state,
    /*typesScopeOnly*/
    true,
    /*cache*/
    void 0,
    /*redirectedReference*/
    void 0
  );
}
function loadModuleFromNearestNodeModulesDirectoryWorker(extensions, moduleName, directory, state, typesScopeOnly, cache, redirectedReference) {
  const mode ***REMOVED*** state.features ***REMOVED******REMOVED******REMOVED*** 0 ? void 0 : state.features & 32 /* EsmMode */ || state.conditions.includes("import") ? 99 /* ESNext */ : 1 /* CommonJS */;
  const priorityExtensions ***REMOVED*** extensions & (1 /* TypeScript */ | 4 /* Declaration */);
  const secondaryExtensions ***REMOVED*** extensions & ~(1 /* TypeScript */ | 4 /* Declaration */);
  if (priorityExtensions) {
    traceIfEnabled(state, Diagnostics.Searching_all_ancestor_node_modules_directories_for_preferred_extensions_Colon_0, formatExtensions(priorityExtensions));
    const result ***REMOVED*** lookup(priorityExtensions);
    if (result) return result;
  }
  if (secondaryExtensions && !typesScopeOnly) {
    traceIfEnabled(state, Diagnostics.Searching_all_ancestor_node_modules_directories_for_fallback_extensions_Colon_0, formatExtensions(secondaryExtensions));
    return lookup(secondaryExtensions);
  }
  function lookup(extensions2) {
    return forEachAncestorDirectoryStoppingAtGlobalCache(
      state.host,
      normalizeSlashes(directory),
      (ancestorDirectory) ***REMOVED***> {
        if (getBaseFileName(ancestorDirectory) !***REMOVED******REMOVED*** "node_modules") {
          const resolutionFromCache ***REMOVED*** tryFindNonRelativeModuleNameInCache(cache, moduleName, mode, ancestorDirectory, redirectedReference, state);
          if (resolutionFromCache) {
            return resolutionFromCache;
          }
          return toSearchResult(loadModuleFromImmediateNodeModulesDirectory(extensions2, moduleName, ancestorDirectory, state, typesScopeOnly, cache, redirectedReference));
        }
      }
    );
  }
}
function forEachAncestorDirectoryStoppingAtGlobalCache(host, directory, callback) {
  var _a;
  const globalCache ***REMOVED*** (_a ***REMOVED*** host ***REMOVED******REMOVED*** null ? void 0 : host.getGlobalTypingsCacheLocation) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host);
  return forEachAncestorDirectory(directory, (ancestorDirectory) ***REMOVED***> {
    const result ***REMOVED*** callback(ancestorDirectory);
    if (result !***REMOVED******REMOVED*** void 0) return result;
    if (ancestorDirectory ***REMOVED******REMOVED******REMOVED*** globalCache) return false;
  }) || void 0;
}
function loadModuleFromImmediateNodeModulesDirectory(extensions, moduleName, directory, state, typesScopeOnly, cache, redirectedReference) {
  const nodeModulesFolder ***REMOVED*** combinePaths(directory, "node_modules");
  const nodeModulesFolderExists ***REMOVED*** directoryProbablyExists(nodeModulesFolder, state.host);
  if (!nodeModulesFolderExists && state.traceEnabled) {
    trace(state.host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, nodeModulesFolder);
  }
  if (!typesScopeOnly) {
    const packageResult ***REMOVED*** loadModuleFromSpecificNodeModulesDirectory(extensions, moduleName, nodeModulesFolder, nodeModulesFolderExists, state, cache, redirectedReference);
    if (packageResult) {
      return packageResult;
    }
  }
  if (extensions & 4 /* Declaration */) {
    const nodeModulesAtTypes2 ***REMOVED*** combinePaths(nodeModulesFolder, "@types");
    let nodeModulesAtTypesExists ***REMOVED*** nodeModulesFolderExists;
    if (nodeModulesFolderExists && !directoryProbablyExists(nodeModulesAtTypes2, state.host)) {
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, nodeModulesAtTypes2);
      }
      nodeModulesAtTypesExists ***REMOVED*** false;
    }
    return loadModuleFromSpecificNodeModulesDirectory(4 /* Declaration */, mangleScopedPackageNameWithTrace(moduleName, state), nodeModulesAtTypes2, nodeModulesAtTypesExists, state, cache, redirectedReference);
  }
}
function loadModuleFromSpecificNodeModulesDirectory(extensions, moduleName, nodeModulesDirectory, nodeModulesDirectoryExists, state, cache, redirectedReference) {
  var _a, _b;
  const candidate ***REMOVED*** normalizePath(combinePaths(nodeModulesDirectory, moduleName));
  const { packageName, rest } ***REMOVED*** parsePackageName(moduleName);
  const packageDirectory ***REMOVED*** combinePaths(nodeModulesDirectory, packageName);
  let rootPackageInfo;
  let packageInfo ***REMOVED*** getPackageJsonInfo(candidate, !nodeModulesDirectoryExists, state);
  if (rest !***REMOVED******REMOVED*** "" && packageInfo && (!(state.features & 8 /* Exports */) || !hasProperty(((_a ***REMOVED*** rootPackageInfo ***REMOVED*** getPackageJsonInfo(packageDirectory, !nodeModulesDirectoryExists, state)) ***REMOVED******REMOVED*** null ? void 0 : _a.contents.packageJsonContent) ?? emptyArray, "exports"))) {
    const fromFile ***REMOVED*** loadModuleFromFile(extensions, candidate, !nodeModulesDirectoryExists, state);
    if (fromFile) {
      return noPackageId(fromFile);
    }
    const fromDirectory ***REMOVED*** loadNodeModuleFromDirectoryWorker(
      extensions,
      candidate,
      !nodeModulesDirectoryExists,
      state,
      packageInfo
    );
    return withPackageId(packageInfo, fromDirectory, state);
  }
  const loader ***REMOVED*** (extensions2, candidate2, onlyRecordFailures, state2) ***REMOVED***> {
    let pathAndExtension ***REMOVED*** (rest || !(state2.features & 32 /* EsmMode */)) && loadModuleFromFile(extensions2, candidate2, onlyRecordFailures, state2) || loadNodeModuleFromDirectoryWorker(
      extensions2,
      candidate2,
      onlyRecordFailures,
      state2,
      packageInfo
    );
    if (!pathAndExtension && !rest && packageInfo && (packageInfo.contents.packageJsonContent.exports ***REMOVED******REMOVED******REMOVED*** void 0 || packageInfo.contents.packageJsonContent.exports ***REMOVED******REMOVED******REMOVED*** null) && state2.features & 32 /* EsmMode */) {
      pathAndExtension ***REMOVED*** loadModuleFromFile(extensions2, combinePaths(candidate2, "index.js"), onlyRecordFailures, state2);
    }
    return withPackageId(packageInfo, pathAndExtension, state2);
  };
  if (rest !***REMOVED******REMOVED*** "") {
    packageInfo ***REMOVED*** rootPackageInfo ?? getPackageJsonInfo(packageDirectory, !nodeModulesDirectoryExists, state);
  }
  if (packageInfo) {
    state.resolvedPackageDirectory ***REMOVED*** true;
  }
  if (packageInfo && packageInfo.contents.packageJsonContent.exports && state.features & 8 /* Exports */) {
    return (_b ***REMOVED*** loadModuleFromExports(packageInfo, extensions, combinePaths(".", rest), state, cache, redirectedReference)) ***REMOVED******REMOVED*** null ? void 0 : _b.value;
  }
  const versionPaths ***REMOVED*** rest !***REMOVED******REMOVED*** "" && packageInfo ? getVersionPathsOfPackageJsonInfo(packageInfo, state) : void 0;
  if (versionPaths) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.package_json_has_a_typesVersions_entry_0_that_matches_compiler_version_1_looking_for_a_pattern_to_match_module_name_2, versionPaths.version, version, rest);
    }
    const packageDirectoryExists ***REMOVED*** nodeModulesDirectoryExists && directoryProbablyExists(packageDirectory, state.host);
    const pathPatterns ***REMOVED*** tryParsePatterns(versionPaths.paths);
    const fromPaths ***REMOVED*** tryLoadModuleUsingPaths(extensions, rest, packageDirectory, versionPaths.paths, pathPatterns, loader, !packageDirectoryExists, state);
    if (fromPaths) {
      return fromPaths.value;
    }
  }
  return loader(extensions, candidate, !nodeModulesDirectoryExists, state);
}
function tryLoadModuleUsingPaths(extensions, moduleName, baseDirectory, paths, pathPatterns, loader, onlyRecordFailures, state) {
  const matchedPattern ***REMOVED*** matchPatternOrExact(pathPatterns, moduleName);
  if (matchedPattern) {
    const matchedStar ***REMOVED*** isString(matchedPattern) ? void 0 : matchedText(matchedPattern, moduleName);
    const matchedPatternText ***REMOVED*** isString(matchedPattern) ? matchedPattern : patternText(matchedPattern);
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Module_name_0_matched_pattern_1, moduleName, matchedPatternText);
    }
    const resolved ***REMOVED*** forEach(paths[matchedPatternText], (subst) ***REMOVED***> {
      const path ***REMOVED*** matchedStar ? replaceFirstStar(subst, matchedStar) : subst;
      const candidate ***REMOVED*** normalizePath(combinePaths(baseDirectory, path));
      if (state.traceEnabled) {
        trace(state.host, Diagnostics.Trying_substitution_0_candidate_module_location_Colon_1, subst, path);
      }
      const extension ***REMOVED*** tryGetExtensionFromPath2(subst);
      if (extension !***REMOVED******REMOVED*** void 0) {
        const path2 ***REMOVED*** tryFile(candidate, onlyRecordFailures, state);
        if (path2 !***REMOVED******REMOVED*** void 0) {
          return noPackageId({ path: path2, ext: extension, resolvedUsingTsExtension: void 0 });
        }
      }
      return loader(extensions, candidate, onlyRecordFailures || !directoryProbablyExists(getDirectoryPath(candidate), state.host), state);
    });
    return { value: resolved };
  }
}
var mangledScopedPackageSeparator ***REMOVED*** "__";
function mangleScopedPackageNameWithTrace(packageName, state) {
  const mangled ***REMOVED*** mangleScopedPackageName(packageName);
  if (state.traceEnabled && mangled !***REMOVED******REMOVED*** packageName) {
    trace(state.host, Diagnostics.Scoped_package_detected_looking_in_0, mangled);
  }
  return mangled;
}
function getTypesPackageName(packageName) {
  return `@types/${mangleScopedPackageName(packageName)}`;
}
function mangleScopedPackageName(packageName) {
  if (startsWith(packageName, "@")) {
    const replaceSlash ***REMOVED*** packageName.replace(directorySeparator, mangledScopedPackageSeparator);
    if (replaceSlash !***REMOVED******REMOVED*** packageName) {
      return replaceSlash.slice(1);
    }
  }
  return packageName;
}
function getPackageNameFromTypesPackageName(mangledName) {
  const withoutAtTypePrefix ***REMOVED*** removePrefix(mangledName, "@types/");
  if (withoutAtTypePrefix !***REMOVED******REMOVED*** mangledName) {
    return unmangleScopedPackageName(withoutAtTypePrefix);
  }
  return mangledName;
}
function unmangleScopedPackageName(typesPackageName) {
  return typesPackageName.includes(mangledScopedPackageSeparator) ? "@" + typesPackageName.replace(mangledScopedPackageSeparator, directorySeparator) : typesPackageName;
}
function tryFindNonRelativeModuleNameInCache(cache, moduleName, mode, containingDirectory, redirectedReference, state) {
  const result ***REMOVED*** cache && cache.getFromNonRelativeNameCache(moduleName, mode, containingDirectory, redirectedReference);
  if (result) {
    if (state.traceEnabled) {
      trace(state.host, Diagnostics.Resolution_for_module_0_was_found_in_cache_from_location_1, moduleName, containingDirectory);
    }
    state.resultFromCache ***REMOVED*** result;
    return {
      value: result.resolvedModule && {
        path: result.resolvedModule.resolvedFileName,
        originalPath: result.resolvedModule.originalPath || true,
        extension: result.resolvedModule.extension,
        packageId: result.resolvedModule.packageId,
        resolvedUsingTsExtension: result.resolvedModule.resolvedUsingTsExtension
      }
    };
  }
}
function classicNameResolver(moduleName, containingFile, compilerOptions, host, cache, redirectedReference) {
  const traceEnabled ***REMOVED*** isTraceEnabled(compilerOptions, host);
  const failedLookupLocations ***REMOVED*** [];
  const affectingLocations ***REMOVED*** [];
  const containingDirectory ***REMOVED*** getDirectoryPath(containingFile);
  const diagnostics ***REMOVED*** [];
  const state ***REMOVED*** {
    compilerOptions,
    host,
    traceEnabled,
    failedLookupLocations,
    affectingLocations,
    packageJsonInfoCache: cache,
    features: 0 /* None */,
    conditions: [],
    requestContainingDirectory: containingDirectory,
    reportDiagnostic: (diag2) ***REMOVED***> void diagnostics.push(diag2),
    isConfigLookup: false,
    candidateIsFromPackageJsonField: false,
    resolvedPackageDirectory: false
  };
  const resolved ***REMOVED*** tryResolve(1 /* TypeScript */ | 4 /* Declaration */) || tryResolve(2 /* JavaScript */ | (compilerOptions.resolveJsonModule ? 8 /* Json */ : 0));
  return createResolvedModuleWithFailedLookupLocationsHandlingSymlink(
    moduleName,
    resolved && resolved.value,
    (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.value) && pathContainsNodeModules(resolved.value.path),
    failedLookupLocations,
    affectingLocations,
    diagnostics,
    state,
    cache
  );
  function tryResolve(extensions) {
    const resolvedUsingSettings ***REMOVED*** tryLoadModuleUsingOptionalResolutionSettings(extensions, moduleName, containingDirectory, loadModuleFromFileNoPackageId, state);
    if (resolvedUsingSettings) {
      return { value: resolvedUsingSettings };
    }
    if (!isExternalModuleNameRelative(moduleName)) {
      const resolved2 ***REMOVED*** forEachAncestorDirectoryStoppingAtGlobalCache(
        state.host,
        containingDirectory,
        (directory) ***REMOVED***> {
          const resolutionFromCache ***REMOVED*** tryFindNonRelativeModuleNameInCache(
            cache,
            moduleName,
            /*mode*/
            void 0,
            directory,
            redirectedReference,
            state
          );
          if (resolutionFromCache) {
            return resolutionFromCache;
          }
          const searchName ***REMOVED*** normalizePath(combinePaths(directory, moduleName));
          return toSearchResult(loadModuleFromFileNoPackageId(
            extensions,
            searchName,
            /*onlyRecordFailures*/
            false,
            state
          ));
        }
      );
      if (resolved2) return resolved2;
      if (extensions & (1 /* TypeScript */ | 4 /* Declaration */)) {
        let resolved3 ***REMOVED*** loadModuleFromNearestNodeModulesDirectoryTypesScope(moduleName, containingDirectory, state);
        if (extensions & 4 /* Declaration */) resolved3 ?? (resolved3 ***REMOVED*** resolveFromTypeRoot(moduleName, state));
        return resolved3;
      }
    } else {
      const candidate ***REMOVED*** normalizePath(combinePaths(containingDirectory, moduleName));
      return toSearchResult(loadModuleFromFileNoPackageId(
        extensions,
        candidate,
        /*onlyRecordFailures*/
        false,
        state
      ));
    }
  }
}
function resolveFromTypeRoot(moduleName, state) {
  if (!state.compilerOptions.typeRoots) return;
  for (const typeRoot of state.compilerOptions.typeRoots) {
    const candidate ***REMOVED*** getCandidateFromTypeRoot(typeRoot, moduleName, state);
    const directoryExists ***REMOVED*** directoryProbablyExists(typeRoot, state.host);
    if (!directoryExists && state.traceEnabled) {
      trace(state.host, Diagnostics.Directory_0_does_not_exist_skipping_all_lookups_in_it, typeRoot);
    }
    const resolvedFromFile ***REMOVED*** loadModuleFromFile(4 /* Declaration */, candidate, !directoryExists, state);
    if (resolvedFromFile) {
      const packageDirectory ***REMOVED*** parseNodeModuleFromPath(resolvedFromFile.path);
      const packageInfo ***REMOVED*** packageDirectory ? getPackageJsonInfo(
        packageDirectory,
        /*onlyRecordFailures*/
        false,
        state
      ) : void 0;
      return toSearchResult(withPackageId(packageInfo, resolvedFromFile, state));
    }
    const resolved ***REMOVED*** loadNodeModuleFromDirectory(4 /* Declaration */, candidate, !directoryExists, state);
    if (resolved) return toSearchResult(resolved);
  }
}
function shouldAllowImportingTsExtension(compilerOptions, fromFileName) {
  return getAllowImportingTsExtensions(compilerOptions) || !!fromFileName && isDeclarationFileName(fromFileName);
}
function loadModuleFromGlobalCache(moduleName, projectName, compilerOptions, host, globalCache, packageJsonInfoCache) {
  const traceEnabled ***REMOVED*** isTraceEnabled(compilerOptions, host);
  if (traceEnabled) {
    trace(host, Diagnostics.Auto_discovery_for_typings_is_enabled_in_project_0_Running_extra_resolution_pass_for_module_1_using_cache_location_2, projectName, moduleName, globalCache);
  }
  const failedLookupLocations ***REMOVED*** [];
  const affectingLocations ***REMOVED*** [];
  const diagnostics ***REMOVED*** [];
  const state ***REMOVED*** {
    compilerOptions,
    host,
    traceEnabled,
    failedLookupLocations,
    affectingLocations,
    packageJsonInfoCache,
    features: 0 /* None */,
    conditions: [],
    requestContainingDirectory: void 0,
    reportDiagnostic: (diag2) ***REMOVED***> void diagnostics.push(diag2),
    isConfigLookup: false,
    candidateIsFromPackageJsonField: false,
    resolvedPackageDirectory: false
  };
  const resolved ***REMOVED*** loadModuleFromImmediateNodeModulesDirectory(
    4 /* Declaration */,
    moduleName,
    globalCache,
    state,
    /*typesScopeOnly*/
    false,
    /*cache*/
    void 0,
    /*redirectedReference*/
    void 0
  );
  return createResolvedModuleWithFailedLookupLocations(
    resolved,
    /*isExternalLibraryImport*/
    true,
    failedLookupLocations,
    affectingLocations,
    diagnostics,
    state.resultFromCache,
    /*cache*/
    void 0
  );
}
function toSearchResult(value) {
  return value !***REMOVED******REMOVED*** void 0 ? { value } : void 0;
}
function traceIfEnabled(state, diagnostic, ...args) {
  if (state.traceEnabled) {
    trace(state.host, diagnostic, ...args);
  }
}
function useCaseSensitiveFileNames(state) {
  return !state.host.useCaseSensitiveFileNames ? true : typeof state.host.useCaseSensitiveFileNames ***REMOVED******REMOVED******REMOVED*** "boolean" ? state.host.useCaseSensitiveFileNames : state.host.useCaseSensitiveFileNames();
}

// src/compiler/binder.ts
function getModuleInstanceState(node, visited) {
  if (node.body && !node.body.parent) {
    setParent(node.body, node);
    setParentRecursive(
      node.body,
      /*incremental*/
      false
    );
  }
  return node.body ? getModuleInstanceStateCached(node.body, visited) : 1 /* Instantiated */;
}
function getModuleInstanceStateCached(node, visited ***REMOVED*** /* @__PURE__ */ new Map()) {
  const nodeId ***REMOVED*** getNodeId(node);
  if (visited.has(nodeId)) {
    return visited.get(nodeId) || 0 /* NonInstantiated */;
  }
  visited.set(nodeId, void 0);
  const result ***REMOVED*** getModuleInstanceStateWorker(node, visited);
  visited.set(nodeId, result);
  return result;
}
function getModuleInstanceStateWorker(node, visited) {
  switch (node.kind) {
    // 1. interface declarations, type alias declarations
    case 265 /* InterfaceDeclaration */:
    case 266 /* TypeAliasDeclaration */:
      return 0 /* NonInstantiated */;
    // 2. const enum declarations
    case 267 /* EnumDeclaration */:
      if (isEnumConst(node)) {
        return 2 /* ConstEnumOnly */;
      }
      break;
    // 3. non-exported import declarations
    case 273 /* ImportDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
      if (!hasSyntacticModifier(node, 32 /* Export */)) {
        return 0 /* NonInstantiated */;
      }
      break;
    // 4. Export alias declarations pointing at only uninstantiated modules or things uninstantiated modules contain
    case 279 /* ExportDeclaration */:
      const exportDeclaration ***REMOVED*** node;
      if (!exportDeclaration.moduleSpecifier && exportDeclaration.exportClause && exportDeclaration.exportClause.kind ***REMOVED******REMOVED******REMOVED*** 280 /* NamedExports */) {
        let state ***REMOVED*** 0 /* NonInstantiated */;
        for (const specifier of exportDeclaration.exportClause.elements) {
          const specifierState ***REMOVED*** getModuleInstanceStateForAliasTarget(specifier, visited);
          if (specifierState > state) {
            state ***REMOVED*** specifierState;
          }
          if (state ***REMOVED******REMOVED******REMOVED*** 1 /* Instantiated */) {
            return state;
          }
        }
        return state;
      }
      break;
    // 5. other uninstantiated module declarations.
    case 269 /* ModuleBlock */: {
      let state ***REMOVED*** 0 /* NonInstantiated */;
      forEachChild(node, (n) ***REMOVED***> {
        const childState ***REMOVED*** getModuleInstanceStateCached(n, visited);
        switch (childState) {
          case 0 /* NonInstantiated */:
            return;
          case 2 /* ConstEnumOnly */:
            state ***REMOVED*** 2 /* ConstEnumOnly */;
            return;
          case 1 /* Instantiated */:
            state ***REMOVED*** 1 /* Instantiated */;
            return true;
          default:
            Debug.assertNever(childState);
        }
      });
      return state;
    }
    case 268 /* ModuleDeclaration */:
      return getModuleInstanceState(node, visited);
    case 80 /* Identifier */:
      if (node.flags & 4096 /* IdentifierIsInJSDocNamespace */) {
        return 0 /* NonInstantiated */;
      }
  }
  return 1 /* Instantiated */;
}
function getModuleInstanceStateForAliasTarget(specifier, visited) {
  const name ***REMOVED*** specifier.propertyName || specifier.name;
  if (name.kind !***REMOVED******REMOVED*** 80 /* Identifier */) {
    return 1 /* Instantiated */;
  }
  let p ***REMOVED*** specifier.parent;
  while (p) {
    if (isBlock(p) || isModuleBlock(p) || isSourceFile(p)) {
      const statements ***REMOVED*** p.statements;
      let found;
      for (const statement of statements) {
        if (nodeHasName(statement, name)) {
          if (!statement.parent) {
            setParent(statement, p);
            setParentRecursive(
              statement,
              /*incremental*/
              false
            );
          }
          const state ***REMOVED*** getModuleInstanceStateCached(statement, visited);
          if (found ***REMOVED******REMOVED******REMOVED*** void 0 || state > found) {
            found ***REMOVED*** state;
          }
          if (found ***REMOVED******REMOVED******REMOVED*** 1 /* Instantiated */) {
            return found;
          }
          if (statement.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */) {
            found ***REMOVED*** 1 /* Instantiated */;
          }
        }
      }
      if (found !***REMOVED******REMOVED*** void 0) {
        return found;
      }
    }
    p ***REMOVED*** p.parent;
  }
  return 1 /* Instantiated */;
}
function createFlowNode(flags, node, antecedent) {
  return Debug.attachFlowNodeDebugInfo({ flags, id: 0, node, antecedent });
}
var binder ***REMOVED*** /* @__PURE__ */ createBinder();
function bindSourceFile(file, options) {
  mark("beforeBind");
  binder(file, options);
  mark("afterBind");
  measure("Bind", "beforeBind", "afterBind");
}
function createBinder() {
  var file;
  var options;
  var languageVersion;
  var parent;
  var container;
  var thisParentContainer;
  var blockScopeContainer;
  var lastContainer;
  var delayedTypeAliases;
  var seenThisKeyword;
  var jsDocImports;
  var currentFlow;
  var currentBreakTarget;
  var currentContinueTarget;
  var currentReturnTarget;
  var currentTrueTarget;
  var currentFalseTarget;
  var currentExceptionTarget;
  var preSwitchCaseFlow;
  var activeLabelList;
  var hasExplicitReturn;
  var inReturnPosition;
  var hasFlowEffects;
  var emitFlags;
  var inStrictMode;
  var inAssignmentPattern ***REMOVED*** false;
  var symbolCount ***REMOVED*** 0;
  var Symbol13;
  var classifiableNames;
  var unreachableFlow ***REMOVED*** createFlowNode(
    1 /* Unreachable */,
    /*node*/
    void 0,
    /*antecedent*/
    void 0
  );
  var reportedUnreachableFlow ***REMOVED*** createFlowNode(
    1 /* Unreachable */,
    /*node*/
    void 0,
    /*antecedent*/
    void 0
  );
  var bindBinaryExpressionFlow ***REMOVED*** createBindBinaryExpressionFlow();
  return bindSourceFile2;
  function createDiagnosticForNode2(node, message, ...args) {
    return createDiagnosticForNodeInSourceFile(getSourceFileOfNode(node) || file, node, message, ...args);
  }
  function bindSourceFile2(f, opts) {
    var _a, _b;
    file ***REMOVED*** f;
    options ***REMOVED*** opts;
    languageVersion ***REMOVED*** getEmitScriptTarget(options);
    inStrictMode ***REMOVED*** bindInStrictMode(file, opts);
    classifiableNames ***REMOVED*** /* @__PURE__ */ new Set();
    symbolCount ***REMOVED*** 0;
    Symbol13 ***REMOVED*** objectAllocator.getSymbolConstructor();
    Debug.attachFlowNodeDebugInfo(unreachableFlow);
    Debug.attachFlowNodeDebugInfo(reportedUnreachableFlow);
    if (!file.locals) {
      (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(
        tracing.Phase.Bind,
        "bindSourceFile",
        { path: file.path },
        /*separateBeginAndEnd*/
        true
      );
      bind(file);
      (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
      file.symbolCount ***REMOVED*** symbolCount;
      file.classifiableNames ***REMOVED*** classifiableNames;
      delayedBindJSDocTypedefTag();
      bindJSDocImports();
    }
    file ***REMOVED*** void 0;
    options ***REMOVED*** void 0;
    languageVersion ***REMOVED*** void 0;
    parent ***REMOVED*** void 0;
    container ***REMOVED*** void 0;
    thisParentContainer ***REMOVED*** void 0;
    blockScopeContainer ***REMOVED*** void 0;
    lastContainer ***REMOVED*** void 0;
    delayedTypeAliases ***REMOVED*** void 0;
    jsDocImports ***REMOVED*** void 0;
    seenThisKeyword ***REMOVED*** false;
    currentFlow ***REMOVED*** void 0;
    currentBreakTarget ***REMOVED*** void 0;
    currentContinueTarget ***REMOVED*** void 0;
    currentReturnTarget ***REMOVED*** void 0;
    currentTrueTarget ***REMOVED*** void 0;
    currentFalseTarget ***REMOVED*** void 0;
    currentExceptionTarget ***REMOVED*** void 0;
    activeLabelList ***REMOVED*** void 0;
    hasExplicitReturn ***REMOVED*** false;
    inReturnPosition ***REMOVED*** false;
    hasFlowEffects ***REMOVED*** false;
    inAssignmentPattern ***REMOVED*** false;
    emitFlags ***REMOVED*** 0 /* None */;
  }
  function bindInStrictMode(file2, opts) {
    if (getStrictOptionValue(opts, "alwaysStrict") && !file2.isDeclarationFile) {
      return true;
    } else {
      return !!file2.externalModuleIndicator;
    }
  }
  function createSymbol(flags, name) {
    symbolCount++;
    return new Symbol13(flags, name);
  }
  function addDeclarationToSymbol(symbol, node, symbolFlags) {
    symbol.flags |***REMOVED*** symbolFlags;
    node.symbol ***REMOVED*** symbol;
    symbol.declarations ***REMOVED*** appendIfUnique(symbol.declarations, node);
    if (symbolFlags & (32 /* Class */ | 384 /* Enum */ | 1536 /* Module */ | 3 /* Variable */) && !symbol.exports) {
      symbol.exports ***REMOVED*** createSymbolTable();
    }
    if (symbolFlags & (32 /* Class */ | 64 /* Interface */ | 2048 /* TypeLiteral */ | 4096 /* ObjectLiteral */) && !symbol.members) {
      symbol.members ***REMOVED*** createSymbolTable();
    }
    if (symbol.constEnumOnlyModule && symbol.flags & (16 /* Function */ | 32 /* Class */ | 256 /* RegularEnum */)) {
      symbol.constEnumOnlyModule ***REMOVED*** false;
    }
    if (symbolFlags & 111551 /* Value */) {
      setValueDeclaration(symbol, node);
    }
  }
  function getDeclarationName(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */) {
      return node.isExportEquals ? "export***REMOVED***" /* ExportEquals */ : "default" /* Default */;
    }
    const name ***REMOVED*** getNameOfDeclaration(node);
    if (name) {
      if (isAmbientModule(node)) {
        const moduleName ***REMOVED*** getTextOfIdentifierOrLiteral(name);
        return isGlobalScopeAugmentation(node) ? "__global" : `"${moduleName}"`;
      }
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        const nameExpression ***REMOVED*** name.expression;
        if (isStringOrNumericLiteralLike(nameExpression)) {
          return escapeLeadingUnderscores(nameExpression.text);
        }
        if (isSignedNumericLiteral(nameExpression)) {
          return tokenToString(nameExpression.operator) + nameExpression.operand.text;
        } else {
          Debug.fail("Only computed properties with literal names have declaration names");
        }
      }
      if (isPrivateIdentifier(name)) {
        const containingClass ***REMOVED*** getContainingClass(node);
        if (!containingClass) {
          return void 0;
        }
        const containingClassSymbol ***REMOVED*** containingClass.symbol;
        return getSymbolNameForPrivateIdentifier(containingClassSymbol, name.escapedText);
      }
      if (isJsxNamespacedName(name)) {
        return getEscapedTextOfJsxNamespacedName(name);
      }
      return isPropertyNameLiteral(name) ? getEscapedTextOfIdentifierOrLiteral(name) : void 0;
    }
    switch (node.kind) {
      case 177 /* Constructor */:
        return "__constructor" /* Constructor */;
      case 185 /* FunctionType */:
      case 180 /* CallSignature */:
      case 324 /* JSDocSignature */:
        return "__call" /* Call */;
      case 186 /* ConstructorType */:
      case 181 /* ConstructSignature */:
        return "__new" /* New */;
      case 182 /* IndexSignature */:
        return "__index" /* Index */;
      case 279 /* ExportDeclaration */:
        return "__export" /* ExportStar */;
      case 308 /* SourceFile */:
        return "export***REMOVED***" /* ExportEquals */;
      case 227 /* BinaryExpression */:
        if (getAssignmentDeclarationKind(node) ***REMOVED******REMOVED******REMOVED*** 2 /* ModuleExports */) {
          return "export***REMOVED***" /* ExportEquals */;
        }
        Debug.fail("Unknown binary declaration kind");
        break;
      case 318 /* JSDocFunctionType */:
        return isJSDocConstructSignature(node) ? "__new" /* New */ : "__call" /* Call */;
      case 170 /* Parameter */:
        Debug.assert(node.parent.kind ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */, "Impossible parameter parent kind", () ***REMOVED***> `parent is: ${Debug.formatSyntaxKind(node.parent.kind)}, expected JSDocFunctionType`);
        const functionType ***REMOVED*** node.parent;
        const index ***REMOVED*** functionType.parameters.indexOf(node);
        return "arg" + index;
    }
  }
  function getDisplayName(node) {
    return isNamedDeclaration(node) ? declarationNameToString(node.name) : unescapeLeadingUnderscores(Debug.checkDefined(getDeclarationName(node)));
  }
  function declareSymbol(symbolTable, parent2, node, includes, excludes, isReplaceableByMethod, isComputedName) {
    Debug.assert(isComputedName || !hasDynamicName(node));
    const isDefaultExport ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */) || isExportSpecifier(node) && moduleExportNameIsDefault(node.name);
    const name ***REMOVED*** isComputedName ? "__computed" /* Computed */ : isDefaultExport && parent2 ? "default" /* Default */ : getDeclarationName(node);
    let symbol;
    if (name ***REMOVED******REMOVED******REMOVED*** void 0) {
      symbol ***REMOVED*** createSymbol(0 /* None */, "__missing" /* Missing */);
    } else {
      symbol ***REMOVED*** symbolTable.get(name);
      if (includes & 2885600 /* Classifiable */) {
        classifiableNames.add(name);
      }
      if (!symbol) {
        symbolTable.set(name, symbol ***REMOVED*** createSymbol(0 /* None */, name));
        if (isReplaceableByMethod) symbol.isReplaceableByMethod ***REMOVED*** true;
      } else if (isReplaceableByMethod && !symbol.isReplaceableByMethod) {
        return symbol;
      } else if (symbol.flags & excludes) {
        if (symbol.isReplaceableByMethod) {
          symbolTable.set(name, symbol ***REMOVED*** createSymbol(0 /* None */, name));
        } else if (!(includes & 3 /* Variable */ && symbol.flags & 67108864 /* Assignment */)) {
          if (isNamedDeclaration(node)) {
            setParent(node.name, node);
          }
          let message ***REMOVED*** symbol.flags & 2 /* BlockScopedVariable */ ? Diagnostics.Cannot_redeclare_block_scoped_variable_0 : Diagnostics.Duplicate_identifier_0;
          let messageNeedsName ***REMOVED*** true;
          if (symbol.flags & 384 /* Enum */ || includes & 384 /* Enum */) {
            message ***REMOVED*** Diagnostics.Enum_declarations_can_only_merge_with_namespace_or_other_enum_declarations;
            messageNeedsName ***REMOVED*** false;
          }
          let multipleDefaultExports ***REMOVED*** false;
          if (length(symbol.declarations)) {
            if (isDefaultExport) {
              message ***REMOVED*** Diagnostics.A_module_cannot_have_multiple_default_exports;
              messageNeedsName ***REMOVED*** false;
              multipleDefaultExports ***REMOVED*** true;
            } else {
              if (symbol.declarations && symbol.declarations.length && (node.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ && !node.isExportEquals)) {
                message ***REMOVED*** Diagnostics.A_module_cannot_have_multiple_default_exports;
                messageNeedsName ***REMOVED*** false;
                multipleDefaultExports ***REMOVED*** true;
              }
            }
          }
          const relatedInformation ***REMOVED*** [];
          if (isTypeAliasDeclaration(node) && nodeIsMissing(node.type) && hasSyntacticModifier(node, 32 /* Export */) && symbol.flags & (2097152 /* Alias */ | 788968 /* Type */ | 1920 /* Namespace */)) {
            relatedInformation.push(createDiagnosticForNode2(node, Diagnostics.Did_you_mean_0, `export type { ${unescapeLeadingUnderscores(node.name.escapedText)} }`));
          }
          const declarationName ***REMOVED*** getNameOfDeclaration(node) || node;
          forEach(symbol.declarations, (declaration, index) ***REMOVED***> {
            const decl ***REMOVED*** getNameOfDeclaration(declaration) || declaration;
            const diag3 ***REMOVED*** messageNeedsName ? createDiagnosticForNode2(decl, message, getDisplayName(declaration)) : createDiagnosticForNode2(decl, message);
            file.bindDiagnostics.push(
              multipleDefaultExports ? addRelatedInfo(diag3, createDiagnosticForNode2(declarationName, index ***REMOVED******REMOVED******REMOVED*** 0 ? Diagnostics.Another_export_default_is_here : Diagnostics.and_here)) : diag3
            );
            if (multipleDefaultExports) {
              relatedInformation.push(createDiagnosticForNode2(decl, Diagnostics.The_first_export_default_is_here));
            }
          });
          const diag2 ***REMOVED*** messageNeedsName ? createDiagnosticForNode2(declarationName, message, getDisplayName(node)) : createDiagnosticForNode2(declarationName, message);
          file.bindDiagnostics.push(addRelatedInfo(diag2, ...relatedInformation));
          symbol ***REMOVED*** createSymbol(0 /* None */, name);
        }
      }
    }
    addDeclarationToSymbol(symbol, node, includes);
    if (symbol.parent) {
      Debug.assert(symbol.parent ***REMOVED******REMOVED******REMOVED*** parent2, "Existing symbol parent should match new one");
    } else {
      symbol.parent ***REMOVED*** parent2;
    }
    return symbol;
  }
  function declareModuleMember(node, symbolFlags, symbolExcludes) {
    const hasExportModifier ***REMOVED*** !!(getCombinedModifierFlags(node) & 32 /* Export */) || jsdocTreatAsExported(node);
    if (symbolFlags & 2097152 /* Alias */) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && hasExportModifier) {
        return declareSymbol(container.symbol.exports, container.symbol, node, symbolFlags, symbolExcludes);
      } else {
        Debug.assertNode(container, canHaveLocals);
        return declareSymbol(
          container.locals,
          /*parent*/
          void 0,
          node,
          symbolFlags,
          symbolExcludes
        );
      }
    } else {
      if (isJSDocTypeAlias(node)) Debug.assert(isInJSFile(node));
      if (!isAmbientModule(node) && (hasExportModifier || container.flags & 128 /* ExportContext */)) {
        if (!canHaveLocals(container) || !container.locals || hasSyntacticModifier(node, 2048 /* Default */) && !getDeclarationName(node)) {
          return declareSymbol(container.symbol.exports, container.symbol, node, symbolFlags, symbolExcludes);
        }
        const exportKind ***REMOVED*** symbolFlags & 111551 /* Value */ ? 1048576 /* ExportValue */ : 0;
        const local ***REMOVED*** declareSymbol(
          container.locals,
          /*parent*/
          void 0,
          node,
          exportKind,
          symbolExcludes
        );
        local.exportSymbol ***REMOVED*** declareSymbol(container.symbol.exports, container.symbol, node, symbolFlags, symbolExcludes);
        node.localSymbol ***REMOVED*** local;
        return local;
      } else {
        Debug.assertNode(container, canHaveLocals);
        return declareSymbol(
          container.locals,
          /*parent*/
          void 0,
          node,
          symbolFlags,
          symbolExcludes
        );
      }
    }
  }
  function jsdocTreatAsExported(node) {
    if (node.parent && isModuleDeclaration(node)) {
      node ***REMOVED*** node.parent;
    }
    if (!isJSDocTypeAlias(node)) return false;
    if (!isJSDocEnumTag(node) && !!node.fullName) return true;
    const declName ***REMOVED*** getNameOfDeclaration(node);
    if (!declName) return false;
    if (isPropertyAccessEntityNameExpression(declName.parent) && isTopLevelNamespaceAssignment(declName.parent)) return true;
    if (isDeclaration(declName.parent) && getCombinedModifierFlags(declName.parent) & 32 /* Export */) return true;
    return false;
  }
  function bindContainer(node, containerFlags) {
    const saveContainer ***REMOVED*** container;
    const saveThisParentContainer ***REMOVED*** thisParentContainer;
    const savedBlockScopeContainer ***REMOVED*** blockScopeContainer;
    const savedInReturnPosition ***REMOVED*** inReturnPosition;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ && node.body.kind !***REMOVED******REMOVED*** 242 /* Block */) inReturnPosition ***REMOVED*** true;
    if (containerFlags & 1 /* IsContainer */) {
      if (node.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        thisParentContainer ***REMOVED*** container;
      }
      container ***REMOVED*** blockScopeContainer ***REMOVED*** node;
      if (containerFlags & 32 /* HasLocals */) {
        container.locals ***REMOVED*** createSymbolTable();
        addToContainerChain(container);
      }
    } else if (containerFlags & 2 /* IsBlockScopedContainer */) {
      blockScopeContainer ***REMOVED*** node;
      if (containerFlags & 32 /* HasLocals */) {
        blockScopeContainer.locals ***REMOVED*** void 0;
      }
    }
    if (containerFlags & 4 /* IsControlFlowContainer */) {
      const saveCurrentFlow ***REMOVED*** currentFlow;
      const saveBreakTarget ***REMOVED*** currentBreakTarget;
      const saveContinueTarget ***REMOVED*** currentContinueTarget;
      const saveReturnTarget ***REMOVED*** currentReturnTarget;
      const saveExceptionTarget ***REMOVED*** currentExceptionTarget;
      const saveActiveLabelList ***REMOVED*** activeLabelList;
      const saveHasExplicitReturn ***REMOVED*** hasExplicitReturn;
      const isImmediatelyInvoked ***REMOVED*** containerFlags & 16 /* IsFunctionExpression */ && !hasSyntacticModifier(node, 1024 /* Async */) && !node.asteriskToken && !!getImmediatelyInvokedFunctionExpression(node) || node.kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */;
      if (!isImmediatelyInvoked) {
        currentFlow ***REMOVED*** createFlowNode(
          2 /* Start */,
          /*node*/
          void 0,
          /*antecedent*/
          void 0
        );
        if (containerFlags & (16 /* IsFunctionExpression */ | 128 /* IsObjectLiteralOrClassExpressionMethodOrAccessor */)) {
          currentFlow.node ***REMOVED*** node;
        }
      }
      currentReturnTarget ***REMOVED*** isImmediatelyInvoked || node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || isInJSFile(node) && (node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */) ? createBranchLabel() : void 0;
      currentExceptionTarget ***REMOVED*** void 0;
      currentBreakTarget ***REMOVED*** void 0;
      currentContinueTarget ***REMOVED*** void 0;
      activeLabelList ***REMOVED*** void 0;
      hasExplicitReturn ***REMOVED*** false;
      bindChildren(node);
      node.flags &***REMOVED*** ~5632 /* ReachabilityAndEmitFlags */;
      if (!(currentFlow.flags & 1 /* Unreachable */) && containerFlags & 8 /* IsFunctionLike */ && nodeIsPresent(node.body)) {
        node.flags |***REMOVED*** 512 /* HasImplicitReturn */;
        if (hasExplicitReturn) node.flags |***REMOVED*** 1024 /* HasExplicitReturn */;
        node.endFlowNode ***REMOVED*** currentFlow;
      }
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
        node.flags |***REMOVED*** emitFlags;
        node.endFlowNode ***REMOVED*** currentFlow;
      }
      if (currentReturnTarget) {
        addAntecedent(currentReturnTarget, currentFlow);
        currentFlow ***REMOVED*** finishFlowLabel(currentReturnTarget);
        if (node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */ || isInJSFile(node) && (node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */)) {
          node.returnFlowNode ***REMOVED*** currentFlow;
        }
      }
      if (!isImmediatelyInvoked) {
        currentFlow ***REMOVED*** saveCurrentFlow;
      }
      currentBreakTarget ***REMOVED*** saveBreakTarget;
      currentContinueTarget ***REMOVED*** saveContinueTarget;
      currentReturnTarget ***REMOVED*** saveReturnTarget;
      currentExceptionTarget ***REMOVED*** saveExceptionTarget;
      activeLabelList ***REMOVED*** saveActiveLabelList;
      hasExplicitReturn ***REMOVED*** saveHasExplicitReturn;
    } else if (containerFlags & 64 /* IsInterface */) {
      seenThisKeyword ***REMOVED*** false;
      bindChildren(node);
      Debug.assertNotNode(node, isIdentifier);
      node.flags ***REMOVED*** seenThisKeyword ? node.flags | 256 /* ContainsThis */ : node.flags & ~256 /* ContainsThis */;
    } else {
      bindChildren(node);
    }
    inReturnPosition ***REMOVED*** savedInReturnPosition;
    container ***REMOVED*** saveContainer;
    thisParentContainer ***REMOVED*** saveThisParentContainer;
    blockScopeContainer ***REMOVED*** savedBlockScopeContainer;
  }
  function bindEachFunctionsFirst(nodes) {
    bindEach(nodes, (n) ***REMOVED***> n.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ ? bind(n) : void 0);
    bindEach(nodes, (n) ***REMOVED***> n.kind !***REMOVED******REMOVED*** 263 /* FunctionDeclaration */ ? bind(n) : void 0);
  }
  function bindEach(nodes, bindFunction ***REMOVED*** bind) {
    if (nodes ***REMOVED******REMOVED******REMOVED*** void 0) {
      return;
    }
    forEach(nodes, bindFunction);
  }
  function bindEachChild(node) {
    forEachChild(node, bind, bindEach);
  }
  function bindChildren(node) {
    const saveInAssignmentPattern ***REMOVED*** inAssignmentPattern;
    inAssignmentPattern ***REMOVED*** false;
    if (checkUnreachable(node)) {
      if (canHaveFlowNode(node) && node.flowNode) {
        node.flowNode ***REMOVED*** void 0;
      }
      bindEachChild(node);
      bindJSDoc(node);
      inAssignmentPattern ***REMOVED*** saveInAssignmentPattern;
      return;
    }
    if (node.kind >***REMOVED*** 244 /* FirstStatement */ && node.kind <***REMOVED*** 260 /* LastStatement */ && (!options.allowUnreachableCode || node.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */)) {
      node.flowNode ***REMOVED*** currentFlow;
    }
    switch (node.kind) {
      case 248 /* WhileStatement */:
        bindWhileStatement(node);
        break;
      case 247 /* DoStatement */:
        bindDoStatement(node);
        break;
      case 249 /* ForStatement */:
        bindForStatement(node);
        break;
      case 250 /* ForInStatement */:
      case 251 /* ForOfStatement */:
        bindForInOrForOfStatement(node);
        break;
      case 246 /* IfStatement */:
        bindIfStatement(node);
        break;
      case 254 /* ReturnStatement */:
      case 258 /* ThrowStatement */:
        bindReturnOrThrow(node);
        break;
      case 253 /* BreakStatement */:
      case 252 /* ContinueStatement */:
        bindBreakOrContinueStatement(node);
        break;
      case 259 /* TryStatement */:
        bindTryStatement(node);
        break;
      case 256 /* SwitchStatement */:
        bindSwitchStatement(node);
        break;
      case 270 /* CaseBlock */:
        bindCaseBlock(node);
        break;
      case 297 /* CaseClause */:
        bindCaseClause(node);
        break;
      case 245 /* ExpressionStatement */:
        bindExpressionStatement(node);
        break;
      case 257 /* LabeledStatement */:
        bindLabeledStatement(node);
        break;
      case 225 /* PrefixUnaryExpression */:
        bindPrefixUnaryExpressionFlow(node);
        break;
      case 226 /* PostfixUnaryExpression */:
        bindPostfixUnaryExpressionFlow(node);
        break;
      case 227 /* BinaryExpression */:
        if (isDestructuringAssignment(node)) {
          inAssignmentPattern ***REMOVED*** saveInAssignmentPattern;
          bindDestructuringAssignmentFlow(node);
          return;
        }
        bindBinaryExpressionFlow(node);
        break;
      case 221 /* DeleteExpression */:
        bindDeleteExpressionFlow(node);
        break;
      case 228 /* ConditionalExpression */:
        bindConditionalExpressionFlow(node);
        break;
      case 261 /* VariableDeclaration */:
        bindVariableDeclarationFlow(node);
        break;
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        bindAccessExpressionFlow(node);
        break;
      case 214 /* CallExpression */:
        bindCallExpressionFlow(node);
        break;
      case 236 /* NonNullExpression */:
        bindNonNullExpressionFlow(node);
        break;
      case 347 /* JSDocTypedefTag */:
      case 339 /* JSDocCallbackTag */:
      case 341 /* JSDocEnumTag */:
        bindJSDocTypeAlias(node);
        break;
      case 352 /* JSDocImportTag */:
        bindJSDocImportTag(node);
        break;
      // In source files and blocks, bind functions first to match hoisting that occurs at runtime
      case 308 /* SourceFile */: {
        bindEachFunctionsFirst(node.statements);
        bind(node.endOfFileToken);
        break;
      }
      case 242 /* Block */:
      case 269 /* ModuleBlock */:
        bindEachFunctionsFirst(node.statements);
        break;
      case 209 /* BindingElement */:
        bindBindingElementFlow(node);
        break;
      case 170 /* Parameter */:
        bindParameterFlow(node);
        break;
      case 211 /* ObjectLiteralExpression */:
      case 210 /* ArrayLiteralExpression */:
      case 304 /* PropertyAssignment */:
      case 231 /* SpreadElement */:
        inAssignmentPattern ***REMOVED*** saveInAssignmentPattern;
      // falls through
      default:
        bindEachChild(node);
        break;
    }
    bindJSDoc(node);
    inAssignmentPattern ***REMOVED*** saveInAssignmentPattern;
  }
  function isNarrowingExpression(expr) {
    switch (expr.kind) {
      case 80 /* Identifier */:
      case 110 /* ThisKeyword */:
        return true;
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        return containsNarrowableReference(expr);
      case 214 /* CallExpression */:
        return hasNarrowableArgument(expr);
      case 218 /* ParenthesizedExpression */:
        if (isJSDocTypeAssertion(expr)) {
          return false;
        }
      // fallthrough
      case 236 /* NonNullExpression */:
        return isNarrowingExpression(expr.expression);
      case 227 /* BinaryExpression */:
        return isNarrowingBinaryExpression(expr);
      case 225 /* PrefixUnaryExpression */:
        return expr.operator ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */ && isNarrowingExpression(expr.operand);
      case 222 /* TypeOfExpression */:
        return isNarrowingExpression(expr.expression);
    }
    return false;
  }
  function isNarrowableReference(expr) {
    switch (expr.kind) {
      case 80 /* Identifier */:
      case 110 /* ThisKeyword */:
      case 108 /* SuperKeyword */:
      case 237 /* MetaProperty */:
        return true;
      case 212 /* PropertyAccessExpression */:
      case 218 /* ParenthesizedExpression */:
      case 236 /* NonNullExpression */:
        return isNarrowableReference(expr.expression);
      case 213 /* ElementAccessExpression */:
        return (isStringOrNumericLiteralLike(expr.argumentExpression) || isEntityNameExpression(expr.argumentExpression)) && isNarrowableReference(expr.expression);
      case 227 /* BinaryExpression */:
        return expr.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ && isNarrowableReference(expr.right) || isAssignmentOperator(expr.operatorToken.kind) && isLeftHandSideExpression(expr.left);
    }
    return false;
  }
  function containsNarrowableReference(expr) {
    return isNarrowableReference(expr) || isOptionalChain(expr) && containsNarrowableReference(expr.expression);
  }
  function hasNarrowableArgument(expr) {
    if (expr.arguments) {
      for (const argument of expr.arguments) {
        if (containsNarrowableReference(argument)) {
          return true;
        }
      }
    }
    if (expr.expression.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && containsNarrowableReference(expr.expression.expression)) {
      return true;
    }
    return false;
  }
  function isNarrowingTypeofOperands(expr1, expr2) {
    return isTypeOfExpression(expr1) && isNarrowableOperand(expr1.expression) && isStringLiteralLike(expr2);
  }
  function isNarrowingBinaryExpression(expr) {
    switch (expr.operatorToken.kind) {
      case 64 /* EqualsToken */:
      case 76 /* BarBarEqualsToken */:
      case 77 /* AmpersandAmpersandEqualsToken */:
      case 78 /* QuestionQuestionEqualsToken */:
        return containsNarrowableReference(expr.left);
      case 35 /* EqualsEqualsToken */:
      case 36 /* ExclamationEqualsToken */:
      case 37 /* EqualsEqualsEqualsToken */:
      case 38 /* ExclamationEqualsEqualsToken */:
        const left ***REMOVED*** skipParentheses(expr.left);
        const right ***REMOVED*** skipParentheses(expr.right);
        return isNarrowableOperand(left) || isNarrowableOperand(right) || isNarrowingTypeofOperands(right, left) || isNarrowingTypeofOperands(left, right) || (isBooleanLiteral(right) && isNarrowingExpression(left) || isBooleanLiteral(left) && isNarrowingExpression(right));
      case 104 /* InstanceOfKeyword */:
        return isNarrowableOperand(expr.left);
      case 103 /* InKeyword */:
        return isNarrowingExpression(expr.right);
      case 28 /* CommaToken */:
        return isNarrowingExpression(expr.right);
    }
    return false;
  }
  function isNarrowableOperand(expr) {
    switch (expr.kind) {
      case 218 /* ParenthesizedExpression */:
        return isNarrowableOperand(expr.expression);
      case 227 /* BinaryExpression */:
        switch (expr.operatorToken.kind) {
          case 64 /* EqualsToken */:
            return isNarrowableOperand(expr.left);
          case 28 /* CommaToken */:
            return isNarrowableOperand(expr.right);
        }
    }
    return containsNarrowableReference(expr);
  }
  function createBranchLabel() {
    return createFlowNode(
      4 /* BranchLabel */,
      /*node*/
      void 0,
      /*antecedent*/
      void 0
    );
  }
  function createLoopLabel() {
    return createFlowNode(
      8 /* LoopLabel */,
      /*node*/
      void 0,
      /*antecedent*/
      void 0
    );
  }
  function createReduceLabel(target, antecedents, antecedent) {
    return createFlowNode(1024 /* ReduceLabel */, { target, antecedents }, antecedent);
  }
  function setFlowNodeReferenced(flow) {
    flow.flags |***REMOVED*** flow.flags & 2048 /* Referenced */ ? 4096 /* Shared */ : 2048 /* Referenced */;
  }
  function addAntecedent(label, antecedent) {
    if (!(antecedent.flags & 1 /* Unreachable */) && !contains(label.antecedent, antecedent)) {
      (label.antecedent || (label.antecedent ***REMOVED*** [])).push(antecedent);
      setFlowNodeReferenced(antecedent);
    }
  }
  function createFlowCondition(flags, antecedent, expression) {
    if (antecedent.flags & 1 /* Unreachable */) {
      return antecedent;
    }
    if (!expression) {
      return flags & 32 /* TrueCondition */ ? antecedent : unreachableFlow;
    }
    if ((expression.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */ && flags & 64 /* FalseCondition */ || expression.kind ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */ && flags & 32 /* TrueCondition */) && !isExpressionOfOptionalChainRoot(expression) && !isNullishCoalesce(expression.parent)) {
      return unreachableFlow;
    }
    if (!isNarrowingExpression(expression)) {
      return antecedent;
    }
    setFlowNodeReferenced(antecedent);
    return createFlowNode(flags, expression, antecedent);
  }
  function createFlowSwitchClause(antecedent, switchStatement, clauseStart, clauseEnd) {
    setFlowNodeReferenced(antecedent);
    return createFlowNode(128 /* SwitchClause */, { switchStatement, clauseStart, clauseEnd }, antecedent);
  }
  function createFlowMutation(flags, antecedent, node) {
    setFlowNodeReferenced(antecedent);
    hasFlowEffects ***REMOVED*** true;
    const result ***REMOVED*** createFlowNode(flags, node, antecedent);
    if (currentExceptionTarget) {
      addAntecedent(currentExceptionTarget, result);
    }
    return result;
  }
  function createFlowCall(antecedent, node) {
    setFlowNodeReferenced(antecedent);
    hasFlowEffects ***REMOVED*** true;
    return createFlowNode(512 /* Call */, node, antecedent);
  }
  function finishFlowLabel(flow) {
    const antecedents ***REMOVED*** flow.antecedent;
    if (!antecedents) {
      return unreachableFlow;
    }
    if (antecedents.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return antecedents[0];
    }
    return flow;
  }
  function isStatementCondition(node) {
    const parent2 ***REMOVED*** node.parent;
    switch (parent2.kind) {
      case 246 /* IfStatement */:
      case 248 /* WhileStatement */:
      case 247 /* DoStatement */:
        return parent2.expression ***REMOVED******REMOVED******REMOVED*** node;
      case 249 /* ForStatement */:
      case 228 /* ConditionalExpression */:
        return parent2.condition ***REMOVED******REMOVED******REMOVED*** node;
    }
    return false;
  }
  function isLogicalExpression(node) {
    while (true) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
        node ***REMOVED*** node.expression;
      } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ && node.operator ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */) {
        node ***REMOVED*** node.operand;
      } else {
        return isLogicalOrCoalescingBinaryExpression(node);
      }
    }
  }
  function isLogicalAssignmentExpression(node) {
    return isLogicalOrCoalescingAssignmentExpression(skipParentheses(node));
  }
  function isTopLevelLogicalExpression(node) {
    while (isParenthesizedExpression(node.parent) || isPrefixUnaryExpression(node.parent) && node.parent.operator ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */) {
      node ***REMOVED*** node.parent;
    }
    return !isStatementCondition(node) && !isLogicalExpression(node.parent) && !(isOptionalChain(node.parent) && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node);
  }
  function doWithConditionalBranches(action, value, trueTarget, falseTarget) {
    const savedTrueTarget ***REMOVED*** currentTrueTarget;
    const savedFalseTarget ***REMOVED*** currentFalseTarget;
    currentTrueTarget ***REMOVED*** trueTarget;
    currentFalseTarget ***REMOVED*** falseTarget;
    action(value);
    currentTrueTarget ***REMOVED*** savedTrueTarget;
    currentFalseTarget ***REMOVED*** savedFalseTarget;
  }
  function bindCondition(node, trueTarget, falseTarget) {
    doWithConditionalBranches(bind, node, trueTarget, falseTarget);
    if (!node || !isLogicalAssignmentExpression(node) && !isLogicalExpression(node) && !(isOptionalChain(node) && isOutermostOptionalChain(node))) {
      addAntecedent(trueTarget, createFlowCondition(32 /* TrueCondition */, currentFlow, node));
      addAntecedent(falseTarget, createFlowCondition(64 /* FalseCondition */, currentFlow, node));
    }
  }
  function bindIterativeStatement(node, breakTarget, continueTarget) {
    const saveBreakTarget ***REMOVED*** currentBreakTarget;
    const saveContinueTarget ***REMOVED*** currentContinueTarget;
    currentBreakTarget ***REMOVED*** breakTarget;
    currentContinueTarget ***REMOVED*** continueTarget;
    bind(node);
    currentBreakTarget ***REMOVED*** saveBreakTarget;
    currentContinueTarget ***REMOVED*** saveContinueTarget;
  }
  function setContinueTarget(node, target) {
    let label ***REMOVED*** activeLabelList;
    while (label && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 257 /* LabeledStatement */) {
      label.continueTarget ***REMOVED*** target;
      label ***REMOVED*** label.next;
      node ***REMOVED*** node.parent;
    }
    return target;
  }
  function bindWhileStatement(node) {
    const preWhileLabel ***REMOVED*** setContinueTarget(node, createLoopLabel());
    const preBodyLabel ***REMOVED*** createBranchLabel();
    const postWhileLabel ***REMOVED*** createBranchLabel();
    addAntecedent(preWhileLabel, currentFlow);
    currentFlow ***REMOVED*** preWhileLabel;
    bindCondition(node.expression, preBodyLabel, postWhileLabel);
    currentFlow ***REMOVED*** finishFlowLabel(preBodyLabel);
    bindIterativeStatement(node.statement, postWhileLabel, preWhileLabel);
    addAntecedent(preWhileLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(postWhileLabel);
  }
  function bindDoStatement(node) {
    const preDoLabel ***REMOVED*** createLoopLabel();
    const preConditionLabel ***REMOVED*** setContinueTarget(node, createBranchLabel());
    const postDoLabel ***REMOVED*** createBranchLabel();
    addAntecedent(preDoLabel, currentFlow);
    currentFlow ***REMOVED*** preDoLabel;
    bindIterativeStatement(node.statement, postDoLabel, preConditionLabel);
    addAntecedent(preConditionLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(preConditionLabel);
    bindCondition(node.expression, preDoLabel, postDoLabel);
    currentFlow ***REMOVED*** finishFlowLabel(postDoLabel);
  }
  function bindForStatement(node) {
    const preLoopLabel ***REMOVED*** setContinueTarget(node, createLoopLabel());
    const preBodyLabel ***REMOVED*** createBranchLabel();
    const preIncrementorLabel ***REMOVED*** createBranchLabel();
    const postLoopLabel ***REMOVED*** createBranchLabel();
    bind(node.initializer);
    addAntecedent(preLoopLabel, currentFlow);
    currentFlow ***REMOVED*** preLoopLabel;
    bindCondition(node.condition, preBodyLabel, postLoopLabel);
    currentFlow ***REMOVED*** finishFlowLabel(preBodyLabel);
    bindIterativeStatement(node.statement, postLoopLabel, preIncrementorLabel);
    addAntecedent(preIncrementorLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(preIncrementorLabel);
    bind(node.incrementor);
    addAntecedent(preLoopLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(postLoopLabel);
  }
  function bindForInOrForOfStatement(node) {
    const preLoopLabel ***REMOVED*** setContinueTarget(node, createLoopLabel());
    const postLoopLabel ***REMOVED*** createBranchLabel();
    bind(node.expression);
    addAntecedent(preLoopLabel, currentFlow);
    currentFlow ***REMOVED*** preLoopLabel;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */) {
      bind(node.awaitModifier);
    }
    addAntecedent(postLoopLabel, currentFlow);
    bind(node.initializer);
    if (node.initializer.kind !***REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
      bindAssignmentTargetFlow(node.initializer);
    }
    bindIterativeStatement(node.statement, postLoopLabel, preLoopLabel);
    addAntecedent(preLoopLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(postLoopLabel);
  }
  function bindIfStatement(node) {
    const thenLabel ***REMOVED*** createBranchLabel();
    const elseLabel ***REMOVED*** createBranchLabel();
    const postIfLabel ***REMOVED*** createBranchLabel();
    bindCondition(node.expression, thenLabel, elseLabel);
    currentFlow ***REMOVED*** finishFlowLabel(thenLabel);
    bind(node.thenStatement);
    addAntecedent(postIfLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(elseLabel);
    bind(node.elseStatement);
    addAntecedent(postIfLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(postIfLabel);
  }
  function bindReturnOrThrow(node) {
    const savedInReturnPosition ***REMOVED*** inReturnPosition;
    inReturnPosition ***REMOVED*** true;
    bind(node.expression);
    inReturnPosition ***REMOVED*** savedInReturnPosition;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */) {
      hasExplicitReturn ***REMOVED*** true;
      if (currentReturnTarget) {
        addAntecedent(currentReturnTarget, currentFlow);
      }
    }
    currentFlow ***REMOVED*** unreachableFlow;
    hasFlowEffects ***REMOVED*** true;
  }
  function findActiveLabel(name) {
    for (let label ***REMOVED*** activeLabelList; label; label ***REMOVED*** label.next) {
      if (label.name ***REMOVED******REMOVED******REMOVED*** name) {
        return label;
      }
    }
    return void 0;
  }
  function bindBreakOrContinueFlow(node, breakTarget, continueTarget) {
    const flowLabel ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? breakTarget : continueTarget;
    if (flowLabel) {
      addAntecedent(flowLabel, currentFlow);
      currentFlow ***REMOVED*** unreachableFlow;
      hasFlowEffects ***REMOVED*** true;
    }
  }
  function bindBreakOrContinueStatement(node) {
    bind(node.label);
    if (node.label) {
      const activeLabel ***REMOVED*** findActiveLabel(node.label.escapedText);
      if (activeLabel) {
        activeLabel.referenced ***REMOVED*** true;
        bindBreakOrContinueFlow(node, activeLabel.breakTarget, activeLabel.continueTarget);
      }
    } else {
      bindBreakOrContinueFlow(node, currentBreakTarget, currentContinueTarget);
    }
  }
  function bindTryStatement(node) {
    const saveReturnTarget ***REMOVED*** currentReturnTarget;
    const saveExceptionTarget ***REMOVED*** currentExceptionTarget;
    const normalExitLabel ***REMOVED*** createBranchLabel();
    const returnLabel ***REMOVED*** createBranchLabel();
    let exceptionLabel ***REMOVED*** createBranchLabel();
    if (node.finallyBlock) {
      currentReturnTarget ***REMOVED*** returnLabel;
    }
    addAntecedent(exceptionLabel, currentFlow);
    currentExceptionTarget ***REMOVED*** exceptionLabel;
    bind(node.tryBlock);
    addAntecedent(normalExitLabel, currentFlow);
    if (node.catchClause) {
      currentFlow ***REMOVED*** finishFlowLabel(exceptionLabel);
      exceptionLabel ***REMOVED*** createBranchLabel();
      addAntecedent(exceptionLabel, currentFlow);
      currentExceptionTarget ***REMOVED*** exceptionLabel;
      bind(node.catchClause);
      addAntecedent(normalExitLabel, currentFlow);
    }
    currentReturnTarget ***REMOVED*** saveReturnTarget;
    currentExceptionTarget ***REMOVED*** saveExceptionTarget;
    if (node.finallyBlock) {
      const finallyLabel ***REMOVED*** createBranchLabel();
      finallyLabel.antecedent ***REMOVED*** concatenate(concatenate(normalExitLabel.antecedent, exceptionLabel.antecedent), returnLabel.antecedent);
      currentFlow ***REMOVED*** finallyLabel;
      bind(node.finallyBlock);
      if (currentFlow.flags & 1 /* Unreachable */) {
        currentFlow ***REMOVED*** unreachableFlow;
      } else {
        if (currentReturnTarget && returnLabel.antecedent) {
          addAntecedent(currentReturnTarget, createReduceLabel(finallyLabel, returnLabel.antecedent, currentFlow));
        }
        if (currentExceptionTarget && exceptionLabel.antecedent) {
          addAntecedent(currentExceptionTarget, createReduceLabel(finallyLabel, exceptionLabel.antecedent, currentFlow));
        }
        currentFlow ***REMOVED*** normalExitLabel.antecedent ? createReduceLabel(finallyLabel, normalExitLabel.antecedent, currentFlow) : unreachableFlow;
      }
    } else {
      currentFlow ***REMOVED*** finishFlowLabel(normalExitLabel);
    }
  }
  function bindSwitchStatement(node) {
    const postSwitchLabel ***REMOVED*** createBranchLabel();
    bind(node.expression);
    const saveBreakTarget ***REMOVED*** currentBreakTarget;
    const savePreSwitchCaseFlow ***REMOVED*** preSwitchCaseFlow;
    currentBreakTarget ***REMOVED*** postSwitchLabel;
    preSwitchCaseFlow ***REMOVED*** currentFlow;
    bind(node.caseBlock);
    addAntecedent(postSwitchLabel, currentFlow);
    const hasDefault ***REMOVED*** forEach(node.caseBlock.clauses, (c) ***REMOVED***> c.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */);
    node.possiblyExhaustive ***REMOVED*** !hasDefault && !postSwitchLabel.antecedent;
    if (!hasDefault) {
      addAntecedent(postSwitchLabel, createFlowSwitchClause(preSwitchCaseFlow, node, 0, 0));
    }
    currentBreakTarget ***REMOVED*** saveBreakTarget;
    preSwitchCaseFlow ***REMOVED*** savePreSwitchCaseFlow;
    currentFlow ***REMOVED*** finishFlowLabel(postSwitchLabel);
  }
  function bindCaseBlock(node) {
    const clauses ***REMOVED*** node.clauses;
    const isNarrowingSwitch ***REMOVED*** node.parent.expression.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */ || isNarrowingExpression(node.parent.expression);
    let fallthroughFlow ***REMOVED*** unreachableFlow;
    for (let i ***REMOVED*** 0; i < clauses.length; i++) {
      const clauseStart ***REMOVED*** i;
      while (!clauses[i].statements.length && i + 1 < clauses.length) {
        if (fallthroughFlow ***REMOVED******REMOVED******REMOVED*** unreachableFlow) {
          currentFlow ***REMOVED*** preSwitchCaseFlow;
        }
        bind(clauses[i]);
        i++;
      }
      const preCaseLabel ***REMOVED*** createBranchLabel();
      addAntecedent(preCaseLabel, isNarrowingSwitch ? createFlowSwitchClause(preSwitchCaseFlow, node.parent, clauseStart, i + 1) : preSwitchCaseFlow);
      addAntecedent(preCaseLabel, fallthroughFlow);
      currentFlow ***REMOVED*** finishFlowLabel(preCaseLabel);
      const clause ***REMOVED*** clauses[i];
      bind(clause);
      fallthroughFlow ***REMOVED*** currentFlow;
      if (!(currentFlow.flags & 1 /* Unreachable */) && i !***REMOVED******REMOVED*** clauses.length - 1 && options.noFallthroughCasesInSwitch) {
        clause.fallthroughFlowNode ***REMOVED*** currentFlow;
      }
    }
  }
  function bindCaseClause(node) {
    const saveCurrentFlow ***REMOVED*** currentFlow;
    currentFlow ***REMOVED*** preSwitchCaseFlow;
    bind(node.expression);
    currentFlow ***REMOVED*** saveCurrentFlow;
    bindEach(node.statements);
  }
  function bindExpressionStatement(node) {
    bind(node.expression);
    maybeBindExpressionFlowIfCall(node.expression);
  }
  function maybeBindExpressionFlowIfCall(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */) {
      const call ***REMOVED*** node;
      if (call.expression.kind !***REMOVED******REMOVED*** 108 /* SuperKeyword */ && isDottedName(call.expression)) {
        currentFlow ***REMOVED*** createFlowCall(currentFlow, call);
      }
    }
  }
  function bindLabeledStatement(node) {
    const postStatementLabel ***REMOVED*** createBranchLabel();
    activeLabelList ***REMOVED*** {
      next: activeLabelList,
      name: node.label.escapedText,
      breakTarget: postStatementLabel,
      continueTarget: void 0,
      referenced: false
    };
    bind(node.label);
    bind(node.statement);
    if (!activeLabelList.referenced && !options.allowUnusedLabels) {
      errorOrSuggestionOnNode(unusedLabelIsError(options), node.label, Diagnostics.Unused_label);
    }
    activeLabelList ***REMOVED*** activeLabelList.next;
    addAntecedent(postStatementLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(postStatementLabel);
  }
  function bindDestructuringTargetFlow(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      bindAssignmentTargetFlow(node.left);
    } else {
      bindAssignmentTargetFlow(node);
    }
  }
  function bindAssignmentTargetFlow(node) {
    if (isNarrowableReference(node)) {
      currentFlow ***REMOVED*** createFlowMutation(16 /* Assignment */, currentFlow, node);
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */) {
      for (const e of node.elements) {
        if (e.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */) {
          bindAssignmentTargetFlow(e.expression);
        } else {
          bindDestructuringTargetFlow(e);
        }
      }
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
      for (const p of node.properties) {
        if (p.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
          bindDestructuringTargetFlow(p.initializer);
        } else if (p.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
          bindAssignmentTargetFlow(p.name);
        } else if (p.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
          bindAssignmentTargetFlow(p.expression);
        }
      }
    }
  }
  function bindLogicalLikeExpression(node, trueTarget, falseTarget) {
    const preRightLabel ***REMOVED*** createBranchLabel();
    if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ || node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 77 /* AmpersandAmpersandEqualsToken */) {
      bindCondition(node.left, preRightLabel, falseTarget);
    } else {
      bindCondition(node.left, trueTarget, preRightLabel);
    }
    currentFlow ***REMOVED*** finishFlowLabel(preRightLabel);
    bind(node.operatorToken);
    if (isLogicalOrCoalescingAssignmentOperator(node.operatorToken.kind)) {
      doWithConditionalBranches(bind, node.right, trueTarget, falseTarget);
      bindAssignmentTargetFlow(node.left);
      addAntecedent(trueTarget, createFlowCondition(32 /* TrueCondition */, currentFlow, node));
      addAntecedent(falseTarget, createFlowCondition(64 /* FalseCondition */, currentFlow, node));
    } else {
      bindCondition(node.right, trueTarget, falseTarget);
    }
  }
  function bindPrefixUnaryExpressionFlow(node) {
    if (node.operator ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */) {
      const saveTrueTarget ***REMOVED*** currentTrueTarget;
      currentTrueTarget ***REMOVED*** currentFalseTarget;
      currentFalseTarget ***REMOVED*** saveTrueTarget;
      bindEachChild(node);
      currentFalseTarget ***REMOVED*** currentTrueTarget;
      currentTrueTarget ***REMOVED*** saveTrueTarget;
    } else {
      bindEachChild(node);
      if (node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
        bindAssignmentTargetFlow(node.operand);
      }
    }
  }
  function bindPostfixUnaryExpressionFlow(node) {
    bindEachChild(node);
    if (node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
      bindAssignmentTargetFlow(node.operand);
    }
  }
  function bindDestructuringAssignmentFlow(node) {
    if (inAssignmentPattern) {
      inAssignmentPattern ***REMOVED*** false;
      bind(node.operatorToken);
      bind(node.right);
      inAssignmentPattern ***REMOVED*** true;
      bind(node.left);
    } else {
      inAssignmentPattern ***REMOVED*** true;
      bind(node.left);
      inAssignmentPattern ***REMOVED*** false;
      bind(node.operatorToken);
      bind(node.right);
    }
    bindAssignmentTargetFlow(node.left);
  }
  function createBindBinaryExpressionFlow() {
    return createBinaryExpressionTrampoline(
      onEnter,
      onLeft,
      onOperator,
      onRight,
      onExit,
      /*foldState*/
      void 0
    );
    function onEnter(node, state) {
      if (state) {
        state.stackIndex++;
        setParent(node, parent);
        const saveInStrictMode ***REMOVED*** inStrictMode;
        bindWorker(node);
        const saveParent ***REMOVED*** parent;
        parent ***REMOVED*** node;
        state.skip ***REMOVED*** false;
        state.inStrictModeStack[state.stackIndex] ***REMOVED*** saveInStrictMode;
        state.parentStack[state.stackIndex] ***REMOVED*** saveParent;
      } else {
        state ***REMOVED*** {
          stackIndex: 0,
          skip: false,
          inStrictModeStack: [void 0],
          parentStack: [void 0]
        };
      }
      const operator ***REMOVED*** node.operatorToken.kind;
      if (isLogicalOrCoalescingBinaryOperator(operator) || isLogicalOrCoalescingAssignmentOperator(operator)) {
        if (isTopLevelLogicalExpression(node)) {
          const postExpressionLabel ***REMOVED*** createBranchLabel();
          const saveCurrentFlow ***REMOVED*** currentFlow;
          const saveHasFlowEffects ***REMOVED*** hasFlowEffects;
          hasFlowEffects ***REMOVED*** false;
          bindLogicalLikeExpression(node, postExpressionLabel, postExpressionLabel);
          currentFlow ***REMOVED*** hasFlowEffects ? finishFlowLabel(postExpressionLabel) : saveCurrentFlow;
          hasFlowEffects || (hasFlowEffects ***REMOVED*** saveHasFlowEffects);
        } else {
          bindLogicalLikeExpression(node, currentTrueTarget, currentFalseTarget);
        }
        state.skip ***REMOVED*** true;
      }
      return state;
    }
    function onLeft(left, state, node) {
      if (!state.skip) {
        const maybeBound ***REMOVED*** maybeBind2(left);
        if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
          maybeBindExpressionFlowIfCall(left);
        }
        return maybeBound;
      }
    }
    function onOperator(operatorToken, state, _node) {
      if (!state.skip) {
        bind(operatorToken);
      }
    }
    function onRight(right, state, node) {
      if (!state.skip) {
        const maybeBound ***REMOVED*** maybeBind2(right);
        if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
          maybeBindExpressionFlowIfCall(right);
        }
        return maybeBound;
      }
    }
    function onExit(node, state) {
      if (!state.skip) {
        const operator ***REMOVED*** node.operatorToken.kind;
        if (isAssignmentOperator(operator) && !isAssignmentTarget(node)) {
          bindAssignmentTargetFlow(node.left);
          if (operator ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && node.left.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */) {
            const elementAccess ***REMOVED*** node.left;
            if (isNarrowableOperand(elementAccess.expression)) {
              currentFlow ***REMOVED*** createFlowMutation(256 /* ArrayMutation */, currentFlow, node);
            }
          }
        }
      }
      const savedInStrictMode ***REMOVED*** state.inStrictModeStack[state.stackIndex];
      const savedParent ***REMOVED*** state.parentStack[state.stackIndex];
      if (savedInStrictMode !***REMOVED******REMOVED*** void 0) {
        inStrictMode ***REMOVED*** savedInStrictMode;
      }
      if (savedParent !***REMOVED******REMOVED*** void 0) {
        parent ***REMOVED*** savedParent;
      }
      state.skip ***REMOVED*** false;
      state.stackIndex--;
    }
    function maybeBind2(node) {
      if (node && isBinaryExpression(node) && !isDestructuringAssignment(node)) {
        return node;
      }
      bind(node);
    }
  }
  function bindDeleteExpressionFlow(node) {
    bindEachChild(node);
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
      bindAssignmentTargetFlow(node.expression);
    }
  }
  function bindConditionalExpressionFlow(node) {
    const trueLabel ***REMOVED*** createBranchLabel();
    const falseLabel ***REMOVED*** createBranchLabel();
    const postExpressionLabel ***REMOVED*** createBranchLabel();
    const saveCurrentFlow ***REMOVED*** currentFlow;
    const saveHasFlowEffects ***REMOVED*** hasFlowEffects;
    hasFlowEffects ***REMOVED*** false;
    bindCondition(node.condition, trueLabel, falseLabel);
    currentFlow ***REMOVED*** finishFlowLabel(trueLabel);
    if (inReturnPosition) {
      node.flowNodeWhenTrue ***REMOVED*** currentFlow;
    }
    bind(node.questionToken);
    bind(node.whenTrue);
    addAntecedent(postExpressionLabel, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(falseLabel);
    if (inReturnPosition) {
      node.flowNodeWhenFalse ***REMOVED*** currentFlow;
    }
    bind(node.colonToken);
    bind(node.whenFalse);
    addAntecedent(postExpressionLabel, currentFlow);
    currentFlow ***REMOVED*** hasFlowEffects ? finishFlowLabel(postExpressionLabel) : saveCurrentFlow;
    hasFlowEffects || (hasFlowEffects ***REMOVED*** saveHasFlowEffects);
  }
  function bindInitializedVariableFlow(node) {
    const name ***REMOVED*** !isOmittedExpression(node) ? node.name : void 0;
    if (isBindingPattern(name)) {
      for (const child of name.elements) {
        bindInitializedVariableFlow(child);
      }
    } else {
      currentFlow ***REMOVED*** createFlowMutation(16 /* Assignment */, currentFlow, node);
    }
  }
  function bindVariableDeclarationFlow(node) {
    bindEachChild(node);
    if (node.initializer || isForInOrOfStatement(node.parent.parent)) {
      bindInitializedVariableFlow(node);
    }
  }
  function bindBindingElementFlow(node) {
    bind(node.dotDotDotToken);
    bind(node.propertyName);
    bindInitializer(node.initializer);
    bind(node.name);
  }
  function bindParameterFlow(node) {
    bindEach(node.modifiers);
    bind(node.dotDotDotToken);
    bind(node.questionToken);
    bind(node.type);
    bindInitializer(node.initializer);
    bind(node.name);
  }
  function bindInitializer(node) {
    if (!node) {
      return;
    }
    const entryFlow ***REMOVED*** currentFlow;
    bind(node);
    if (entryFlow ***REMOVED******REMOVED******REMOVED*** unreachableFlow || entryFlow ***REMOVED******REMOVED******REMOVED*** currentFlow) {
      return;
    }
    const exitFlow ***REMOVED*** createBranchLabel();
    addAntecedent(exitFlow, entryFlow);
    addAntecedent(exitFlow, currentFlow);
    currentFlow ***REMOVED*** finishFlowLabel(exitFlow);
  }
  function bindJSDocTypeAlias(node) {
    bind(node.tagName);
    if (node.kind !***REMOVED******REMOVED*** 341 /* JSDocEnumTag */ && node.fullName) {
      setParent(node.fullName, node);
      setParentRecursive(
        node.fullName,
        /*incremental*/
        false
      );
    }
    if (typeof node.comment !***REMOVED******REMOVED*** "string") {
      bindEach(node.comment);
    }
  }
  function bindJSDocClassTag(node) {
    bindEachChild(node);
    const host ***REMOVED*** getHostSignatureFromJSDoc(node);
    if (host && host.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */) {
      addDeclarationToSymbol(host.symbol, host, 32 /* Class */);
    }
  }
  function bindJSDocImportTag(node) {
    bind(node.tagName);
    bind(node.moduleSpecifier);
    bind(node.attributes);
    if (typeof node.comment !***REMOVED******REMOVED*** "string") {
      bindEach(node.comment);
    }
  }
  function bindOptionalExpression(node, trueTarget, falseTarget) {
    doWithConditionalBranches(bind, node, trueTarget, falseTarget);
    if (!isOptionalChain(node) || isOutermostOptionalChain(node)) {
      addAntecedent(trueTarget, createFlowCondition(32 /* TrueCondition */, currentFlow, node));
      addAntecedent(falseTarget, createFlowCondition(64 /* FalseCondition */, currentFlow, node));
    }
  }
  function bindOptionalChainRest(node) {
    switch (node.kind) {
      case 212 /* PropertyAccessExpression */:
        bind(node.questionDotToken);
        bind(node.name);
        break;
      case 213 /* ElementAccessExpression */:
        bind(node.questionDotToken);
        bind(node.argumentExpression);
        break;
      case 214 /* CallExpression */:
        bind(node.questionDotToken);
        bindEach(node.typeArguments);
        bindEach(node.arguments);
        break;
    }
  }
  function bindOptionalChain(node, trueTarget, falseTarget) {
    const preChainLabel ***REMOVED*** isOptionalChainRoot(node) ? createBranchLabel() : void 0;
    bindOptionalExpression(node.expression, preChainLabel || trueTarget, falseTarget);
    if (preChainLabel) {
      currentFlow ***REMOVED*** finishFlowLabel(preChainLabel);
    }
    doWithConditionalBranches(bindOptionalChainRest, node, trueTarget, falseTarget);
    if (isOutermostOptionalChain(node)) {
      addAntecedent(trueTarget, createFlowCondition(32 /* TrueCondition */, currentFlow, node));
      addAntecedent(falseTarget, createFlowCondition(64 /* FalseCondition */, currentFlow, node));
    }
  }
  function bindOptionalChainFlow(node) {
    if (isTopLevelLogicalExpression(node)) {
      const postExpressionLabel ***REMOVED*** createBranchLabel();
      const saveCurrentFlow ***REMOVED*** currentFlow;
      const saveHasFlowEffects ***REMOVED*** hasFlowEffects;
      bindOptionalChain(node, postExpressionLabel, postExpressionLabel);
      currentFlow ***REMOVED*** hasFlowEffects ? finishFlowLabel(postExpressionLabel) : saveCurrentFlow;
      hasFlowEffects || (hasFlowEffects ***REMOVED*** saveHasFlowEffects);
    } else {
      bindOptionalChain(node, currentTrueTarget, currentFalseTarget);
    }
  }
  function bindNonNullExpressionFlow(node) {
    if (isOptionalChain(node)) {
      bindOptionalChainFlow(node);
    } else {
      bindEachChild(node);
    }
  }
  function bindAccessExpressionFlow(node) {
    if (isOptionalChain(node)) {
      bindOptionalChainFlow(node);
    } else {
      bindEachChild(node);
    }
  }
  function bindCallExpressionFlow(node) {
    if (isOptionalChain(node)) {
      bindOptionalChainFlow(node);
    } else {
      const expr ***REMOVED*** skipParentheses(node.expression);
      if (expr.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || expr.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        bindEach(node.typeArguments);
        bindEach(node.arguments);
        bind(node.expression);
      } else {
        bindEachChild(node);
        if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          currentFlow ***REMOVED*** createFlowCall(currentFlow, node);
        }
      }
    }
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
      const propertyAccess ***REMOVED*** node.expression;
      if (isIdentifier(propertyAccess.name) && isNarrowableOperand(propertyAccess.expression) && isPushOrUnshiftIdentifier(propertyAccess.name)) {
        currentFlow ***REMOVED*** createFlowMutation(256 /* ArrayMutation */, currentFlow, node);
      }
    }
  }
  function addToContainerChain(next) {
    if (lastContainer) {
      lastContainer.nextContainer ***REMOVED*** next;
    }
    lastContainer ***REMOVED*** next;
  }
  function declareSymbolAndAddToSymbolTable(node, symbolFlags, symbolExcludes) {
    switch (container.kind) {
      // Modules, source files, and classes need specialized handling for how their
      // members are declared (for example, a member of a class will go into a specific
      // symbol table depending on if it is static or not). We defer to specialized
      // handlers to take care of declaring these child members.
      case 268 /* ModuleDeclaration */:
        return declareModuleMember(node, symbolFlags, symbolExcludes);
      case 308 /* SourceFile */:
        return declareSourceFileMember(node, symbolFlags, symbolExcludes);
      case 232 /* ClassExpression */:
      case 264 /* ClassDeclaration */:
        return declareClassMember(node, symbolFlags, symbolExcludes);
      case 267 /* EnumDeclaration */:
        return declareSymbol(container.symbol.exports, container.symbol, node, symbolFlags, symbolExcludes);
      case 188 /* TypeLiteral */:
      case 323 /* JSDocTypeLiteral */:
      case 211 /* ObjectLiteralExpression */:
      case 265 /* InterfaceDeclaration */:
      case 293 /* JsxAttributes */:
        return declareSymbol(container.symbol.members, container.symbol, node, symbolFlags, symbolExcludes);
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */:
      case 180 /* CallSignature */:
      case 181 /* ConstructSignature */:
      case 324 /* JSDocSignature */:
      case 182 /* IndexSignature */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 177 /* Constructor */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 318 /* JSDocFunctionType */:
      case 176 /* ClassStaticBlockDeclaration */:
      case 266 /* TypeAliasDeclaration */:
      case 201 /* MappedType */:
        if (container.locals) Debug.assertNode(container, canHaveLocals);
        return declareSymbol(
          container.locals,
          /*parent*/
          void 0,
          node,
          symbolFlags,
          symbolExcludes
        );
    }
  }
  function declareClassMember(node, symbolFlags, symbolExcludes) {
    return isStatic(node) ? declareSymbol(container.symbol.exports, container.symbol, node, symbolFlags, symbolExcludes) : declareSymbol(container.symbol.members, container.symbol, node, symbolFlags, symbolExcludes);
  }
  function declareSourceFileMember(node, symbolFlags, symbolExcludes) {
    return isExternalModule(file) ? declareModuleMember(node, symbolFlags, symbolExcludes) : declareSymbol(
      file.locals,
      /*parent*/
      void 0,
      node,
      symbolFlags,
      symbolExcludes
    );
  }
  function hasExportDeclarations(node) {
    const body ***REMOVED*** isSourceFile(node) ? node : tryCast(node.body, isModuleBlock);
    return !!body && body.statements.some((s) ***REMOVED***> isExportDeclaration(s) || isExportAssignment(s));
  }
  function setExportContextFlag(node) {
    if (node.flags & 33554432 /* Ambient */ && !hasExportDeclarations(node)) {
      node.flags |***REMOVED*** 128 /* ExportContext */;
    } else {
      node.flags &***REMOVED*** ~128 /* ExportContext */;
    }
  }
  function bindModuleDeclaration(node) {
    setExportContextFlag(node);
    if (isAmbientModule(node)) {
      if (hasSyntacticModifier(node, 32 /* Export */)) {
        errorOnFirstToken(node, Diagnostics.export_modifier_cannot_be_applied_to_ambient_modules_and_module_augmentations_since_they_are_always_visible);
      }
      if (isModuleAugmentationExternal(node)) {
        declareModuleSymbol(node);
      } else {
        let pattern;
        if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
          const { text } ***REMOVED*** node.name;
          pattern ***REMOVED*** tryParsePattern(text);
          if (pattern ***REMOVED******REMOVED******REMOVED*** void 0) {
            errorOnFirstToken(node.name, Diagnostics.Pattern_0_can_have_at_most_one_Asterisk_character, text);
          }
        }
        const symbol ***REMOVED*** declareSymbolAndAddToSymbolTable(node, 512 /* ValueModule */, 110735 /* ValueModuleExcludes */);
        file.patternAmbientModules ***REMOVED*** append(file.patternAmbientModules, pattern && !isString(pattern) ? { pattern, symbol } : void 0);
      }
    } else {
      const state ***REMOVED*** declareModuleSymbol(node);
      if (state !***REMOVED******REMOVED*** 0 /* NonInstantiated */) {
        const { symbol } ***REMOVED*** node;
        symbol.constEnumOnlyModule ***REMOVED*** !(symbol.flags & (16 /* Function */ | 32 /* Class */ | 256 /* RegularEnum */)) && state ***REMOVED******REMOVED******REMOVED*** 2 /* ConstEnumOnly */ && symbol.constEnumOnlyModule !***REMOVED******REMOVED*** false;
      }
    }
  }
  function declareModuleSymbol(node) {
    const state ***REMOVED*** getModuleInstanceState(node);
    const instantiated ***REMOVED*** state !***REMOVED******REMOVED*** 0 /* NonInstantiated */;
    declareSymbolAndAddToSymbolTable(
      node,
      instantiated ? 512 /* ValueModule */ : 1024 /* NamespaceModule */,
      instantiated ? 110735 /* ValueModuleExcludes */ : 0 /* NamespaceModuleExcludes */
    );
    return state;
  }
  function bindFunctionOrConstructorType(node) {
    const symbol ***REMOVED*** createSymbol(131072 /* Signature */, getDeclarationName(node));
    addDeclarationToSymbol(symbol, node, 131072 /* Signature */);
    const typeLiteralSymbol ***REMOVED*** createSymbol(2048 /* TypeLiteral */, "__type" /* Type */);
    addDeclarationToSymbol(typeLiteralSymbol, node, 2048 /* TypeLiteral */);
    typeLiteralSymbol.members ***REMOVED*** createSymbolTable();
    typeLiteralSymbol.members.set(symbol.escapedName, symbol);
  }
  function bindObjectLiteralExpression(node) {
    return bindAnonymousDeclaration(node, 4096 /* ObjectLiteral */, "__object" /* Object */);
  }
  function bindJsxAttributes(node) {
    return bindAnonymousDeclaration(node, 4096 /* ObjectLiteral */, "__jsxAttributes" /* JSXAttributes */);
  }
  function bindJsxAttribute(node, symbolFlags, symbolExcludes) {
    return declareSymbolAndAddToSymbolTable(node, symbolFlags, symbolExcludes);
  }
  function bindAnonymousDeclaration(node, symbolFlags, name) {
    const symbol ***REMOVED*** createSymbol(symbolFlags, name);
    if (symbolFlags & (8 /* EnumMember */ | 106500 /* ClassMember */)) {
      symbol.parent ***REMOVED*** container.symbol;
    }
    addDeclarationToSymbol(symbol, node, symbolFlags);
    return symbol;
  }
  function bindBlockScopedDeclaration(node, symbolFlags, symbolExcludes) {
    switch (blockScopeContainer.kind) {
      case 268 /* ModuleDeclaration */:
        declareModuleMember(node, symbolFlags, symbolExcludes);
        break;
      case 308 /* SourceFile */:
        if (isExternalOrCommonJsModule(container)) {
          declareModuleMember(node, symbolFlags, symbolExcludes);
          break;
        }
      // falls through
      default:
        Debug.assertNode(blockScopeContainer, canHaveLocals);
        if (!blockScopeContainer.locals) {
          blockScopeContainer.locals ***REMOVED*** createSymbolTable();
          addToContainerChain(blockScopeContainer);
        }
        declareSymbol(
          blockScopeContainer.locals,
          /*parent*/
          void 0,
          node,
          symbolFlags,
          symbolExcludes
        );
    }
  }
  function delayedBindJSDocTypedefTag() {
    if (!delayedTypeAliases) {
      return;
    }
    const saveContainer ***REMOVED*** container;
    const saveLastContainer ***REMOVED*** lastContainer;
    const saveBlockScopeContainer ***REMOVED*** blockScopeContainer;
    const saveParent ***REMOVED*** parent;
    const saveCurrentFlow ***REMOVED*** currentFlow;
    for (const typeAlias of delayedTypeAliases) {
      const host ***REMOVED*** typeAlias.parent.parent;
      container ***REMOVED*** getEnclosingContainer(host) || file;
      blockScopeContainer ***REMOVED*** getEnclosingBlockScopeContainer(host) || file;
      currentFlow ***REMOVED*** createFlowNode(
        2 /* Start */,
        /*node*/
        void 0,
        /*antecedent*/
        void 0
      );
      parent ***REMOVED*** typeAlias;
      bind(typeAlias.typeExpression);
      const declName ***REMOVED*** getNameOfDeclaration(typeAlias);
      if ((isJSDocEnumTag(typeAlias) || !typeAlias.fullName) && declName && isPropertyAccessEntityNameExpression(declName.parent)) {
        const isTopLevel ***REMOVED*** isTopLevelNamespaceAssignment(declName.parent);
        if (isTopLevel) {
          bindPotentiallyMissingNamespaces(
            file.symbol,
            declName.parent,
            isTopLevel,
            !!findAncestor(declName, (d) ***REMOVED***> isPropertyAccessExpression(d) && d.name.escapedText ***REMOVED******REMOVED******REMOVED*** "prototype"),
            /*containerIsClass*/
            false
          );
          const oldContainer ***REMOVED*** container;
          switch (getAssignmentDeclarationPropertyAccessKind(declName.parent)) {
            case 1 /* ExportsProperty */:
            case 2 /* ModuleExports */:
              if (!isExternalOrCommonJsModule(file)) {
                container ***REMOVED*** void 0;
              } else {
                container ***REMOVED*** file;
              }
              break;
            case 4 /* ThisProperty */:
              container ***REMOVED*** declName.parent.expression;
              break;
            case 3 /* PrototypeProperty */:
              container ***REMOVED*** declName.parent.expression.name;
              break;
            case 5 /* Property */:
              container ***REMOVED*** isExportsOrModuleExportsOrAlias(file, declName.parent.expression) ? file : isPropertyAccessExpression(declName.parent.expression) ? declName.parent.expression.name : declName.parent.expression;
              break;
            case 0 /* None */:
              return Debug.fail("Shouldn't have detected typedef or enum on non-assignment declaration");
          }
          if (container) {
            declareModuleMember(typeAlias, 524288 /* TypeAlias */, 788968 /* TypeAliasExcludes */);
          }
          container ***REMOVED*** oldContainer;
        }
      } else if (isJSDocEnumTag(typeAlias) || !typeAlias.fullName || typeAlias.fullName.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
        parent ***REMOVED*** typeAlias.parent;
        bindBlockScopedDeclaration(typeAlias, 524288 /* TypeAlias */, 788968 /* TypeAliasExcludes */);
      } else {
        bind(typeAlias.fullName);
      }
    }
    container ***REMOVED*** saveContainer;
    lastContainer ***REMOVED*** saveLastContainer;
    blockScopeContainer ***REMOVED*** saveBlockScopeContainer;
    parent ***REMOVED*** saveParent;
    currentFlow ***REMOVED*** saveCurrentFlow;
  }
  function bindJSDocImports() {
    if (jsDocImports ***REMOVED******REMOVED******REMOVED*** void 0) {
      return;
    }
    const saveContainer ***REMOVED*** container;
    const saveLastContainer ***REMOVED*** lastContainer;
    const saveBlockScopeContainer ***REMOVED*** blockScopeContainer;
    const saveParent ***REMOVED*** parent;
    const saveCurrentFlow ***REMOVED*** currentFlow;
    for (const jsDocImportTag of jsDocImports) {
      const host ***REMOVED*** getJSDocHost(jsDocImportTag);
      const enclosingContainer ***REMOVED*** host ? getEnclosingContainer(host) : void 0;
      const enclosingBlockScopeContainer ***REMOVED*** host ? getEnclosingBlockScopeContainer(host) : void 0;
      container ***REMOVED*** enclosingContainer || file;
      blockScopeContainer ***REMOVED*** enclosingBlockScopeContainer || file;
      currentFlow ***REMOVED*** createFlowNode(
        2 /* Start */,
        /*node*/
        void 0,
        /*antecedent*/
        void 0
      );
      parent ***REMOVED*** jsDocImportTag;
      bind(jsDocImportTag.importClause);
    }
    container ***REMOVED*** saveContainer;
    lastContainer ***REMOVED*** saveLastContainer;
    blockScopeContainer ***REMOVED*** saveBlockScopeContainer;
    parent ***REMOVED*** saveParent;
    currentFlow ***REMOVED*** saveCurrentFlow;
  }
  function checkContextualIdentifier(node) {
    if (!file.parseDiagnostics.length && !(node.flags & 33554432 /* Ambient */) && !(node.flags & 16777216 /* JSDoc */) && !isIdentifierName(node)) {
      const originalKeywordKind ***REMOVED*** identifierToKeywordKind(node);
      if (originalKeywordKind ***REMOVED******REMOVED******REMOVED*** void 0) {
        return;
      }
      if (inStrictMode && originalKeywordKind >***REMOVED*** 119 /* FirstFutureReservedWord */ && originalKeywordKind <***REMOVED*** 127 /* LastFutureReservedWord */) {
        file.bindDiagnostics.push(createDiagnosticForNode2(node, getStrictModeIdentifierMessage(node), declarationNameToString(node)));
      } else if (originalKeywordKind ***REMOVED******REMOVED******REMOVED*** 135 /* AwaitKeyword */) {
        if (isExternalModule(file) && isInTopLevelContext(node)) {
          file.bindDiagnostics.push(createDiagnosticForNode2(node, Diagnostics.Identifier_expected_0_is_a_reserved_word_at_the_top_level_of_a_module, declarationNameToString(node)));
        } else if (node.flags & 65536 /* AwaitContext */) {
          file.bindDiagnostics.push(createDiagnosticForNode2(node, Diagnostics.Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here, declarationNameToString(node)));
        }
      } else if (originalKeywordKind ***REMOVED******REMOVED******REMOVED*** 127 /* YieldKeyword */ && node.flags & 16384 /* YieldContext */) {
        file.bindDiagnostics.push(createDiagnosticForNode2(node, Diagnostics.Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here, declarationNameToString(node)));
      }
    }
  }
  function getStrictModeIdentifierMessage(node) {
    if (getContainingClass(node)) {
      return Diagnostics.Identifier_expected_0_is_a_reserved_word_in_strict_mode_Class_definitions_are_automatically_in_strict_mode;
    }
    if (file.externalModuleIndicator) {
      return Diagnostics.Identifier_expected_0_is_a_reserved_word_in_strict_mode_Modules_are_automatically_in_strict_mode;
    }
    return Diagnostics.Identifier_expected_0_is_a_reserved_word_in_strict_mode;
  }
  function checkPrivateIdentifier(node) {
    if (node.escapedText ***REMOVED******REMOVED******REMOVED*** "#constructor") {
      if (!file.parseDiagnostics.length) {
        file.bindDiagnostics.push(createDiagnosticForNode2(node, Diagnostics.constructor_is_a_reserved_word, declarationNameToString(node)));
      }
    }
  }
  function checkStrictModeBinaryExpression(node) {
    if (inStrictMode && isLeftHandSideExpression(node.left) && isAssignmentOperator(node.operatorToken.kind)) {
      checkStrictModeEvalOrArguments(node, node.left);
    }
  }
  function checkStrictModeCatchClause(node) {
    if (inStrictMode && node.variableDeclaration) {
      checkStrictModeEvalOrArguments(node, node.variableDeclaration.name);
    }
  }
  function checkStrictModeDeleteExpression(node) {
    if (inStrictMode && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const span ***REMOVED*** getErrorSpanForNode(file, node.expression);
      file.bindDiagnostics.push(createFileDiagnostic(file, span.start, span.length, Diagnostics.delete_cannot_be_called_on_an_identifier_in_strict_mode));
    }
  }
  function isEvalOrArgumentsIdentifier(node) {
    return isIdentifier(node) && (node.escapedText ***REMOVED******REMOVED******REMOVED*** "eval" || node.escapedText ***REMOVED******REMOVED******REMOVED*** "arguments");
  }
  function checkStrictModeEvalOrArguments(contextNode, name) {
    if (name && name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const identifier ***REMOVED*** name;
      if (isEvalOrArgumentsIdentifier(identifier)) {
        const span ***REMOVED*** getErrorSpanForNode(file, name);
        file.bindDiagnostics.push(createFileDiagnostic(file, span.start, span.length, getStrictModeEvalOrArgumentsMessage(contextNode), idText(identifier)));
      }
    }
  }
  function getStrictModeEvalOrArgumentsMessage(node) {
    if (getContainingClass(node)) {
      return Diagnostics.Code_contained_in_a_class_is_evaluated_in_JavaScript_s_strict_mode_which_does_not_allow_this_use_of_0_For_more_information_see_https_Colon_Slash_Slashdeveloper_mozilla_org_Slashen_US_Slashdocs_SlashWeb_SlashJavaScript_SlashReference_SlashStrict_mode;
    }
    if (file.externalModuleIndicator) {
      return Diagnostics.Invalid_use_of_0_Modules_are_automatically_in_strict_mode;
    }
    return Diagnostics.Invalid_use_of_0_in_strict_mode;
  }
  function checkStrictModeFunctionName(node) {
    if (inStrictMode && !(node.flags & 33554432 /* Ambient */)) {
      checkStrictModeEvalOrArguments(node, node.name);
    }
  }
  function getStrictModeBlockScopeFunctionDeclarationMessage(node) {
    if (getContainingClass(node)) {
      return Diagnostics.Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Class_definitions_are_automatically_in_strict_mode;
    }
    if (file.externalModuleIndicator) {
      return Diagnostics.Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5_Modules_are_automatically_in_strict_mode;
    }
    return Diagnostics.Function_declarations_are_not_allowed_inside_blocks_in_strict_mode_when_targeting_ES5;
  }
  function checkStrictModeFunctionDeclaration(node) {
    if (languageVersion < 2 /* ES2015 */) {
      if (blockScopeContainer.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ && blockScopeContainer.kind !***REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && !isFunctionLikeOrClassStaticBlockDeclaration(blockScopeContainer)) {
        const errorSpan ***REMOVED*** getErrorSpanForNode(file, node);
        file.bindDiagnostics.push(createFileDiagnostic(file, errorSpan.start, errorSpan.length, getStrictModeBlockScopeFunctionDeclarationMessage(node)));
      }
    }
  }
  function checkStrictModePostfixUnaryExpression(node) {
    if (inStrictMode) {
      checkStrictModeEvalOrArguments(node, node.operand);
    }
  }
  function checkStrictModePrefixUnaryExpression(node) {
    if (inStrictMode) {
      if (node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
        checkStrictModeEvalOrArguments(node, node.operand);
      }
    }
  }
  function checkStrictModeWithStatement(node) {
    if (inStrictMode) {
      errorOnFirstToken(node, Diagnostics.with_statements_are_not_allowed_in_strict_mode);
    }
  }
  function checkStrictModeLabeledStatement(node) {
    if (inStrictMode && getEmitScriptTarget(options) >***REMOVED*** 2 /* ES2015 */) {
      if (isDeclarationStatement(node.statement) || isVariableStatement(node.statement)) {
        errorOnFirstToken(node.label, Diagnostics.A_label_is_not_allowed_here);
      }
    }
  }
  function errorOnFirstToken(node, message, ...args) {
    const span ***REMOVED*** getSpanOfTokenAtPosition(file, node.pos);
    file.bindDiagnostics.push(createFileDiagnostic(file, span.start, span.length, message, ...args));
  }
  function errorOrSuggestionOnNode(isError, node, message) {
    errorOrSuggestionOnRange(isError, node, node, message);
  }
  function errorOrSuggestionOnRange(isError, startNode, endNode, message) {
    addErrorOrSuggestionDiagnostic(isError, { pos: getTokenPosOfNode(startNode, file), end: endNode.end }, message);
  }
  function addErrorOrSuggestionDiagnostic(isError, range, message) {
    const diag2 ***REMOVED*** createFileDiagnostic(file, range.pos, range.end - range.pos, message);
    if (isError) {
      file.bindDiagnostics.push(diag2);
    } else {
      file.bindSuggestionDiagnostics ***REMOVED*** append(file.bindSuggestionDiagnostics, { ...diag2, category: 2 /* Suggestion */ });
    }
  }
  function bind(node) {
    if (!node) {
      return;
    }
    setParent(node, parent);
    if (tracing) node.tracingPath ***REMOVED*** file.path;
    const saveInStrictMode ***REMOVED*** inStrictMode;
    bindWorker(node);
    if (node.kind > 166 /* LastToken */) {
      const saveParent ***REMOVED*** parent;
      parent ***REMOVED*** node;
      const containerFlags ***REMOVED*** getContainerFlags(node);
      if (containerFlags ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
        bindChildren(node);
      } else {
        bindContainer(node, containerFlags);
      }
      parent ***REMOVED*** saveParent;
    } else {
      const saveParent ***REMOVED*** parent;
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 1 /* EndOfFileToken */) parent ***REMOVED*** node;
      bindJSDoc(node);
      parent ***REMOVED*** saveParent;
    }
    inStrictMode ***REMOVED*** saveInStrictMode;
  }
  function bindJSDoc(node) {
    if (hasJSDocNodes(node)) {
      if (isInJSFile(node)) {
        for (const j of node.jsDoc) {
          bind(j);
        }
      } else {
        for (const j of node.jsDoc) {
          setParent(j, node);
          setParentRecursive(
            j,
            /*incremental*/
            false
          );
        }
      }
    }
  }
  function updateStrictModeStatementList(statements) {
    if (!inStrictMode) {
      for (const statement of statements) {
        if (!isPrologueDirective(statement)) {
          return;
        }
        if (isUseStrictPrologueDirective(statement)) {
          inStrictMode ***REMOVED*** true;
          return;
        }
      }
    }
  }
  function isUseStrictPrologueDirective(node) {
    const nodeText ***REMOVED*** getSourceTextOfNodeFromSourceFile(file, node.expression);
    return nodeText ***REMOVED******REMOVED******REMOVED*** '"use strict"' || nodeText ***REMOVED******REMOVED******REMOVED*** "'use strict'";
  }
  function bindWorker(node) {
    switch (node.kind) {
      /* Strict mode checks */
      case 80 /* Identifier */:
        if (node.flags & 4096 /* IdentifierIsInJSDocNamespace */) {
          let parentNode ***REMOVED*** node.parent;
          while (parentNode && !isJSDocTypeAlias(parentNode)) {
            parentNode ***REMOVED*** parentNode.parent;
          }
          bindBlockScopedDeclaration(parentNode, 524288 /* TypeAlias */, 788968 /* TypeAliasExcludes */);
          break;
        }
      // falls through
      case 110 /* ThisKeyword */:
        if (currentFlow && (isExpression(node) || parent.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */)) {
          node.flowNode ***REMOVED*** currentFlow;
        }
        return checkContextualIdentifier(node);
      case 167 /* QualifiedName */:
        if (currentFlow && isPartOfTypeQuery(node)) {
          node.flowNode ***REMOVED*** currentFlow;
        }
        break;
      case 237 /* MetaProperty */:
      case 108 /* SuperKeyword */:
        node.flowNode ***REMOVED*** currentFlow;
        break;
      case 81 /* PrivateIdentifier */:
        return checkPrivateIdentifier(node);
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        const expr ***REMOVED*** node;
        if (currentFlow && isNarrowableReference(expr)) {
          expr.flowNode ***REMOVED*** currentFlow;
        }
        if (isSpecialPropertyDeclaration(expr)) {
          bindSpecialPropertyDeclaration(expr);
        }
        if (isInJSFile(expr) && file.commonJsModuleIndicator && isModuleExportsAccessExpression(expr) && !lookupSymbolForName(blockScopeContainer, "module")) {
          declareSymbol(
            file.locals,
            /*parent*/
            void 0,
            expr.expression,
            1 /* FunctionScopedVariable */ | 134217728 /* ModuleExports */,
            111550 /* FunctionScopedVariableExcludes */
          );
        }
        break;
      case 227 /* BinaryExpression */:
        const specialKind ***REMOVED*** getAssignmentDeclarationKind(node);
        switch (specialKind) {
          case 1 /* ExportsProperty */:
            bindExportsPropertyAssignment(node);
            break;
          case 2 /* ModuleExports */:
            bindModuleExportsAssignment(node);
            break;
          case 3 /* PrototypeProperty */:
            bindPrototypePropertyAssignment(node.left, node);
            break;
          case 6 /* Prototype */:
            bindPrototypeAssignment(node);
            break;
          case 4 /* ThisProperty */:
            bindThisPropertyAssignment(node);
            break;
          case 5 /* Property */:
            const expression ***REMOVED*** node.left.expression;
            if (isInJSFile(node) && isIdentifier(expression)) {
              const symbol ***REMOVED*** lookupSymbolForName(blockScopeContainer, expression.escapedText);
              if (isThisInitializedDeclaration(symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.valueDeclaration)) {
                bindThisPropertyAssignment(node);
                break;
              }
            }
            bindSpecialPropertyAssignment(node);
            break;
          case 0 /* None */:
            break;
          default:
            Debug.fail("Unknown binary expression special property assignment kind");
        }
        return checkStrictModeBinaryExpression(node);
      case 300 /* CatchClause */:
        return checkStrictModeCatchClause(node);
      case 221 /* DeleteExpression */:
        return checkStrictModeDeleteExpression(node);
      case 226 /* PostfixUnaryExpression */:
        return checkStrictModePostfixUnaryExpression(node);
      case 225 /* PrefixUnaryExpression */:
        return checkStrictModePrefixUnaryExpression(node);
      case 255 /* WithStatement */:
        return checkStrictModeWithStatement(node);
      case 257 /* LabeledStatement */:
        return checkStrictModeLabeledStatement(node);
      case 198 /* ThisType */:
        seenThisKeyword ***REMOVED*** true;
        return;
      case 183 /* TypePredicate */:
        break;
      // Binding the children will handle everything
      case 169 /* TypeParameter */:
        return bindTypeParameter(node);
      case 170 /* Parameter */:
        return bindParameter(node);
      case 261 /* VariableDeclaration */:
        return bindVariableDeclarationOrBindingElement(node);
      case 209 /* BindingElement */:
        node.flowNode ***REMOVED*** currentFlow;
        return bindVariableDeclarationOrBindingElement(node);
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
        return bindPropertyWorker(node);
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
        return bindPropertyOrMethodOrAccessor(node, 4 /* Property */, 0 /* PropertyExcludes */);
      case 307 /* EnumMember */:
        return bindPropertyOrMethodOrAccessor(node, 8 /* EnumMember */, 900095 /* EnumMemberExcludes */);
      case 180 /* CallSignature */:
      case 181 /* ConstructSignature */:
      case 182 /* IndexSignature */:
        return declareSymbolAndAddToSymbolTable(node, 131072 /* Signature */, 0 /* None */);
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        return bindPropertyOrMethodOrAccessor(node, 8192 /* Method */ | (node.questionToken ? 16777216 /* Optional */ : 0 /* None */), isObjectLiteralMethod(node) ? 0 /* PropertyExcludes */ : 103359 /* MethodExcludes */);
      case 263 /* FunctionDeclaration */:
        return bindFunctionDeclaration(node);
      case 177 /* Constructor */:
        return declareSymbolAndAddToSymbolTable(
          node,
          16384 /* Constructor */,
          /*symbolExcludes:*/
          0 /* None */
        );
      case 178 /* GetAccessor */:
        return bindPropertyOrMethodOrAccessor(node, 32768 /* GetAccessor */, 46015 /* GetAccessorExcludes */);
      case 179 /* SetAccessor */:
        return bindPropertyOrMethodOrAccessor(node, 65536 /* SetAccessor */, 78783 /* SetAccessorExcludes */);
      case 185 /* FunctionType */:
      case 318 /* JSDocFunctionType */:
      case 324 /* JSDocSignature */:
      case 186 /* ConstructorType */:
        return bindFunctionOrConstructorType(node);
      case 188 /* TypeLiteral */:
      case 323 /* JSDocTypeLiteral */:
      case 201 /* MappedType */:
        return bindAnonymousTypeWorker(node);
      case 333 /* JSDocClassTag */:
        return bindJSDocClassTag(node);
      case 211 /* ObjectLiteralExpression */:
        return bindObjectLiteralExpression(node);
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        return bindFunctionExpression(node);
      case 214 /* CallExpression */:
        const assignmentKind ***REMOVED*** getAssignmentDeclarationKind(node);
        switch (assignmentKind) {
          case 7 /* ObjectDefinePropertyValue */:
            return bindObjectDefinePropertyAssignment(node);
          case 8 /* ObjectDefinePropertyExports */:
            return bindObjectDefinePropertyExport(node);
          case 9 /* ObjectDefinePrototypeProperty */:
            return bindObjectDefinePrototypeProperty(node);
          case 0 /* None */:
            break;
          // Nothing to do
          default:
            return Debug.fail("Unknown call expression assignment declaration kind");
        }
        if (isInJSFile(node)) {
          bindCallExpression(node);
        }
        break;
      // Members of classes, interfaces, and modules
      case 232 /* ClassExpression */:
      case 264 /* ClassDeclaration */:
        inStrictMode ***REMOVED*** true;
        return bindClassLikeDeclaration(node);
      case 265 /* InterfaceDeclaration */:
        return bindBlockScopedDeclaration(node, 64 /* Interface */, 788872 /* InterfaceExcludes */);
      case 266 /* TypeAliasDeclaration */:
        return bindBlockScopedDeclaration(node, 524288 /* TypeAlias */, 788968 /* TypeAliasExcludes */);
      case 267 /* EnumDeclaration */:
        return bindEnumDeclaration(node);
      case 268 /* ModuleDeclaration */:
        return bindModuleDeclaration(node);
      // Jsx-attributes
      case 293 /* JsxAttributes */:
        return bindJsxAttributes(node);
      case 292 /* JsxAttribute */:
        return bindJsxAttribute(node, 4 /* Property */, 0 /* PropertyExcludes */);
      // Imports and exports
      case 272 /* ImportEqualsDeclaration */:
      case 275 /* NamespaceImport */:
      case 277 /* ImportSpecifier */:
      case 282 /* ExportSpecifier */:
        return declareSymbolAndAddToSymbolTable(node, 2097152 /* Alias */, 2097152 /* AliasExcludes */);
      case 271 /* NamespaceExportDeclaration */:
        return bindNamespaceExportDeclaration(node);
      case 274 /* ImportClause */:
        return bindImportClause(node);
      case 279 /* ExportDeclaration */:
        return bindExportDeclaration(node);
      case 278 /* ExportAssignment */:
        return bindExportAssignment(node);
      case 308 /* SourceFile */:
        updateStrictModeStatementList(node.statements);
        return bindSourceFileIfExternalModule();
      case 242 /* Block */:
        if (!isFunctionLikeOrClassStaticBlockDeclaration(node.parent)) {
          return;
        }
      // falls through
      case 269 /* ModuleBlock */:
        return updateStrictModeStatementList(node.statements);
      case 342 /* JSDocParameterTag */:
        if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 324 /* JSDocSignature */) {
          return bindParameter(node);
        }
        if (node.parent.kind !***REMOVED******REMOVED*** 323 /* JSDocTypeLiteral */) {
          break;
        }
      // falls through
      case 349 /* JSDocPropertyTag */:
        const propTag ***REMOVED*** node;
        const flags ***REMOVED*** propTag.isBracketed || propTag.typeExpression && propTag.typeExpression.type.kind ***REMOVED******REMOVED******REMOVED*** 317 /* JSDocOptionalType */ ? 4 /* Property */ | 16777216 /* Optional */ : 4 /* Property */;
        return declareSymbolAndAddToSymbolTable(propTag, flags, 0 /* PropertyExcludes */);
      case 347 /* JSDocTypedefTag */:
      case 339 /* JSDocCallbackTag */:
      case 341 /* JSDocEnumTag */:
        return (delayedTypeAliases || (delayedTypeAliases ***REMOVED*** [])).push(node);
      case 340 /* JSDocOverloadTag */:
        return bind(node.typeExpression);
      case 352 /* JSDocImportTag */:
        return (jsDocImports || (jsDocImports ***REMOVED*** [])).push(node);
    }
  }
  function bindPropertyWorker(node) {
    const isAutoAccessor ***REMOVED*** isAutoAccessorPropertyDeclaration(node);
    const includes ***REMOVED*** isAutoAccessor ? 98304 /* Accessor */ : 4 /* Property */;
    const excludes ***REMOVED*** isAutoAccessor ? 13247 /* AccessorExcludes */ : 0 /* PropertyExcludes */;
    return bindPropertyOrMethodOrAccessor(node, includes | (node.questionToken ? 16777216 /* Optional */ : 0 /* None */), excludes);
  }
  function bindAnonymousTypeWorker(node) {
    return bindAnonymousDeclaration(node, 2048 /* TypeLiteral */, "__type" /* Type */);
  }
  function bindSourceFileIfExternalModule() {
    setExportContextFlag(file);
    if (isExternalModule(file)) {
      bindSourceFileAsExternalModule();
    } else if (isJsonSourceFile(file)) {
      bindSourceFileAsExternalModule();
      const originalSymbol ***REMOVED*** file.symbol;
      declareSymbol(file.symbol.exports, file.symbol, file, 4 /* Property */, -1 /* All */);
      file.symbol ***REMOVED*** originalSymbol;
    }
  }
  function bindSourceFileAsExternalModule() {
    bindAnonymousDeclaration(file, 512 /* ValueModule */, `"${removeFileExtension(file.fileName)}"`);
  }
  function bindExportAssignment(node) {
    if (!container.symbol || !container.symbol.exports) {
      bindAnonymousDeclaration(node, 111551 /* Value */, getDeclarationName(node));
    } else {
      const flags ***REMOVED*** exportAssignmentIsAlias(node) ? 2097152 /* Alias */ : 4 /* Property */;
      const symbol ***REMOVED*** declareSymbol(container.symbol.exports, container.symbol, node, flags, -1 /* All */);
      if (node.isExportEquals) {
        setValueDeclaration(symbol, node);
      }
    }
  }
  function bindNamespaceExportDeclaration(node) {
    if (some(node.modifiers)) {
      file.bindDiagnostics.push(createDiagnosticForNode2(node, Diagnostics.Modifiers_cannot_appear_here));
    }
    const diag2 ***REMOVED*** !isSourceFile(node.parent) ? Diagnostics.Global_module_exports_may_only_appear_at_top_level : !isExternalModule(node.parent) ? Diagnostics.Global_module_exports_may_only_appear_in_module_files : !node.parent.isDeclarationFile ? Diagnostics.Global_module_exports_may_only_appear_in_declaration_files : void 0;
    if (diag2) {
      file.bindDiagnostics.push(createDiagnosticForNode2(node, diag2));
    } else {
      file.symbol.globalExports ***REMOVED*** file.symbol.globalExports || createSymbolTable();
      declareSymbol(file.symbol.globalExports, file.symbol, node, 2097152 /* Alias */, 2097152 /* AliasExcludes */);
    }
  }
  function bindExportDeclaration(node) {
    if (!container.symbol || !container.symbol.exports) {
      bindAnonymousDeclaration(node, 8388608 /* ExportStar */, getDeclarationName(node));
    } else if (!node.exportClause) {
      declareSymbol(container.symbol.exports, container.symbol, node, 8388608 /* ExportStar */, 0 /* None */);
    } else if (isNamespaceExport(node.exportClause)) {
      setParent(node.exportClause, node);
      declareSymbol(container.symbol.exports, container.symbol, node.exportClause, 2097152 /* Alias */, 2097152 /* AliasExcludes */);
    }
  }
  function bindImportClause(node) {
    if (node.name) {
      declareSymbolAndAddToSymbolTable(node, 2097152 /* Alias */, 2097152 /* AliasExcludes */);
    }
  }
  function setCommonJsModuleIndicator(node) {
    if (file.externalModuleIndicator && file.externalModuleIndicator !***REMOVED******REMOVED*** true) {
      return false;
    }
    if (!file.commonJsModuleIndicator) {
      file.commonJsModuleIndicator ***REMOVED*** node;
      if (!file.externalModuleIndicator) {
        bindSourceFileAsExternalModule();
      }
    }
    return true;
  }
  function bindObjectDefinePropertyExport(node) {
    if (!setCommonJsModuleIndicator(node)) {
      return;
    }
    const symbol ***REMOVED*** forEachIdentifierInEntityName(
      node.arguments[0],
      /*parent*/
      void 0,
      (id, symbol2) ***REMOVED***> {
        if (symbol2) {
          addDeclarationToSymbol(symbol2, id, 1536 /* Module */ | 67108864 /* Assignment */);
        }
        return symbol2;
      }
    );
    if (symbol) {
      const flags ***REMOVED*** 4 /* Property */ | 1048576 /* ExportValue */;
      declareSymbol(symbol.exports, symbol, node, flags, 0 /* None */);
    }
  }
  function bindExportsPropertyAssignment(node) {
    if (!setCommonJsModuleIndicator(node)) {
      return;
    }
    const symbol ***REMOVED*** forEachIdentifierInEntityName(
      node.left.expression,
      /*parent*/
      void 0,
      (id, symbol2) ***REMOVED***> {
        if (symbol2) {
          addDeclarationToSymbol(symbol2, id, 1536 /* Module */ | 67108864 /* Assignment */);
        }
        return symbol2;
      }
    );
    if (symbol) {
      const isAlias ***REMOVED*** isAliasableExpression(node.right) && (isExportsIdentifier(node.left.expression) || isModuleExportsAccessExpression(node.left.expression));
      const flags ***REMOVED*** isAlias ? 2097152 /* Alias */ : 4 /* Property */ | 1048576 /* ExportValue */;
      setParent(node.left, node);
      declareSymbol(symbol.exports, symbol, node.left, flags, 0 /* None */);
    }
  }
  function bindModuleExportsAssignment(node) {
    if (!setCommonJsModuleIndicator(node)) {
      return;
    }
    const assignedExpression ***REMOVED*** getRightMostAssignedExpression(node.right);
    if (isEmptyObjectLiteral(assignedExpression) || container ***REMOVED******REMOVED******REMOVED*** file && isExportsOrModuleExportsOrAlias(file, assignedExpression)) {
      return;
    }
    if (isObjectLiteralExpression(assignedExpression) && every(assignedExpression.properties, isShorthandPropertyAssignment)) {
      forEach(assignedExpression.properties, bindExportAssignedObjectMemberAlias);
      return;
    }
    const flags ***REMOVED*** exportAssignmentIsAlias(node) ? 2097152 /* Alias */ : 4 /* Property */ | 1048576 /* ExportValue */ | 512 /* ValueModule */;
    const symbol ***REMOVED*** declareSymbol(file.symbol.exports, file.symbol, node, flags | 67108864 /* Assignment */, 0 /* None */);
    setValueDeclaration(symbol, node);
  }
  function bindExportAssignedObjectMemberAlias(node) {
    declareSymbol(file.symbol.exports, file.symbol, node, 2097152 /* Alias */ | 67108864 /* Assignment */, 0 /* None */);
  }
  function bindThisPropertyAssignment(node) {
    Debug.assert(isInJSFile(node));
    const hasPrivateIdentifier ***REMOVED*** isBinaryExpression(node) && isPropertyAccessExpression(node.left) && isPrivateIdentifier(node.left.name) || isPropertyAccessExpression(node) && isPrivateIdentifier(node.name);
    if (hasPrivateIdentifier) {
      return;
    }
    const thisContainer ***REMOVED*** getThisContainer(
      node,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    switch (thisContainer.kind) {
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
        let constructorSymbol ***REMOVED*** thisContainer.symbol;
        if (isBinaryExpression(thisContainer.parent) && thisContainer.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
          const l ***REMOVED*** thisContainer.parent.left;
          if (isBindableStaticAccessExpression(l) && isPrototypeAccess(l.expression)) {
            constructorSymbol ***REMOVED*** lookupSymbolForPropertyAccess(l.expression.expression, thisParentContainer);
          }
        }
        if (constructorSymbol && constructorSymbol.valueDeclaration) {
          constructorSymbol.members ***REMOVED*** constructorSymbol.members || createSymbolTable();
          if (hasDynamicName(node)) {
            bindDynamicallyNamedThisPropertyAssignment(node, constructorSymbol, constructorSymbol.members);
          } else {
            declareSymbol(constructorSymbol.members, constructorSymbol, node, 4 /* Property */ | 67108864 /* Assignment */, 0 /* PropertyExcludes */ & ~4 /* Property */);
          }
          addDeclarationToSymbol(constructorSymbol, constructorSymbol.valueDeclaration, 32 /* Class */);
        }
        break;
      case 177 /* Constructor */:
      case 173 /* PropertyDeclaration */:
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 176 /* ClassStaticBlockDeclaration */:
        const containingClass ***REMOVED*** thisContainer.parent;
        const symbolTable ***REMOVED*** isStatic(thisContainer) ? containingClass.symbol.exports : containingClass.symbol.members;
        if (hasDynamicName(node)) {
          bindDynamicallyNamedThisPropertyAssignment(node, containingClass.symbol, symbolTable);
        } else {
          declareSymbol(
            symbolTable,
            containingClass.symbol,
            node,
            4 /* Property */ | 67108864 /* Assignment */,
            0 /* None */,
            /*isReplaceableByMethod*/
            true
          );
        }
        break;
      case 308 /* SourceFile */:
        if (hasDynamicName(node)) {
          break;
        } else if (thisContainer.commonJsModuleIndicator) {
          declareSymbol(thisContainer.symbol.exports, thisContainer.symbol, node, 4 /* Property */ | 1048576 /* ExportValue */, 0 /* None */);
        } else {
          declareSymbolAndAddToSymbolTable(node, 1 /* FunctionScopedVariable */, 111550 /* FunctionScopedVariableExcludes */);
        }
        break;
      // Namespaces are not allowed in javascript files, so do nothing here
      case 268 /* ModuleDeclaration */:
        break;
      default:
        Debug.failBadSyntaxKind(thisContainer);
    }
  }
  function bindDynamicallyNamedThisPropertyAssignment(node, symbol, symbolTable) {
    declareSymbol(
      symbolTable,
      symbol,
      node,
      4 /* Property */,
      0 /* None */,
      /*isReplaceableByMethod*/
      true,
      /*isComputedName*/
      true
    );
    addLateBoundAssignmentDeclarationToSymbol(node, symbol);
  }
  function addLateBoundAssignmentDeclarationToSymbol(node, symbol) {
    if (symbol) {
      (symbol.assignmentDeclarationMembers || (symbol.assignmentDeclarationMembers ***REMOVED*** /* @__PURE__ */ new Map())).set(getNodeId(node), node);
    }
  }
  function bindSpecialPropertyDeclaration(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      bindThisPropertyAssignment(node);
    } else if (isBindableStaticAccessExpression(node) && node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
      if (isPrototypeAccess(node.expression)) {
        bindPrototypePropertyAssignment(node, node.parent);
      } else {
        bindStaticPropertyAssignment(node);
      }
    }
  }
  function bindPrototypeAssignment(node) {
    setParent(node.left, node);
    setParent(node.right, node);
    bindPropertyAssignment(
      node.left.expression,
      node.left,
      /*isPrototypeProperty*/
      false,
      /*containerIsClass*/
      true
    );
  }
  function bindObjectDefinePrototypeProperty(node) {
    const namespaceSymbol ***REMOVED*** lookupSymbolForPropertyAccess(node.arguments[0].expression);
    if (namespaceSymbol && namespaceSymbol.valueDeclaration) {
      addDeclarationToSymbol(namespaceSymbol, namespaceSymbol.valueDeclaration, 32 /* Class */);
    }
    bindPotentiallyNewExpandoMemberToNamespace(
      node,
      namespaceSymbol,
      /*isPrototypeProperty*/
      true
    );
  }
  function bindPrototypePropertyAssignment(lhs, parent2) {
    const classPrototype ***REMOVED*** lhs.expression;
    const constructorFunction ***REMOVED*** classPrototype.expression;
    setParent(constructorFunction, classPrototype);
    setParent(classPrototype, lhs);
    setParent(lhs, parent2);
    bindPropertyAssignment(
      constructorFunction,
      lhs,
      /*isPrototypeProperty*/
      true,
      /*containerIsClass*/
      true
    );
  }
  function bindObjectDefinePropertyAssignment(node) {
    let namespaceSymbol ***REMOVED*** lookupSymbolForPropertyAccess(node.arguments[0]);
    const isToplevel ***REMOVED*** node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
    namespaceSymbol ***REMOVED*** bindPotentiallyMissingNamespaces(
      namespaceSymbol,
      node.arguments[0],
      isToplevel,
      /*isPrototypeProperty*/
      false,
      /*containerIsClass*/
      false
    );
    bindPotentiallyNewExpandoMemberToNamespace(
      node,
      namespaceSymbol,
      /*isPrototypeProperty*/
      false
    );
  }
  function bindSpecialPropertyAssignment(node) {
    var _a;
    const parentSymbol ***REMOVED*** lookupSymbolForPropertyAccess(node.left.expression, blockScopeContainer) || lookupSymbolForPropertyAccess(node.left.expression, container);
    if (!isInJSFile(node) && !isFunctionSymbol(parentSymbol)) {
      return;
    }
    const rootExpr ***REMOVED*** getLeftmostAccessExpression(node.left);
    if (isIdentifier(rootExpr) && ((_a ***REMOVED*** lookupSymbolForName(container, rootExpr.escapedText)) ***REMOVED******REMOVED*** null ? void 0 : _a.flags) & 2097152 /* Alias */) {
      return;
    }
    setParent(node.left, node);
    setParent(node.right, node);
    if (isIdentifier(node.left.expression) && container ***REMOVED******REMOVED******REMOVED*** file && isExportsOrModuleExportsOrAlias(file, node.left.expression)) {
      bindExportsPropertyAssignment(node);
    } else if (hasDynamicName(node)) {
      bindAnonymousDeclaration(node, 4 /* Property */ | 67108864 /* Assignment */, "__computed" /* Computed */);
      const sym ***REMOVED*** bindPotentiallyMissingNamespaces(
        parentSymbol,
        node.left.expression,
        isTopLevelNamespaceAssignment(node.left),
        /*isPrototypeProperty*/
        false,
        /*containerIsClass*/
        false
      );
      addLateBoundAssignmentDeclarationToSymbol(node, sym);
    } else {
      bindStaticPropertyAssignment(cast(node.left, isBindableStaticNameExpression));
    }
  }
  function bindStaticPropertyAssignment(node) {
    Debug.assert(!isIdentifier(node));
    setParent(node.expression, node);
    bindPropertyAssignment(
      node.expression,
      node,
      /*isPrototypeProperty*/
      false,
      /*containerIsClass*/
      false
    );
  }
  function bindPotentiallyMissingNamespaces(namespaceSymbol, entityName, isToplevel, isPrototypeProperty, containerIsClass) {
    if ((namespaceSymbol ***REMOVED******REMOVED*** null ? void 0 : namespaceSymbol.flags) & 2097152 /* Alias */) {
      return namespaceSymbol;
    }
    if (isToplevel && !isPrototypeProperty) {
      const flags ***REMOVED*** 1536 /* Module */ | 67108864 /* Assignment */;
      const excludeFlags ***REMOVED*** 110735 /* ValueModuleExcludes */ & ~67108864 /* Assignment */;
      namespaceSymbol ***REMOVED*** forEachIdentifierInEntityName(entityName, namespaceSymbol, (id, symbol, parent2) ***REMOVED***> {
        if (symbol) {
          addDeclarationToSymbol(symbol, id, flags);
          return symbol;
        } else {
          const table ***REMOVED*** parent2 ? parent2.exports : file.jsGlobalAugmentations || (file.jsGlobalAugmentations ***REMOVED*** createSymbolTable());
          return declareSymbol(table, parent2, id, flags, excludeFlags);
        }
      });
    }
    if (containerIsClass && namespaceSymbol && namespaceSymbol.valueDeclaration) {
      addDeclarationToSymbol(namespaceSymbol, namespaceSymbol.valueDeclaration, 32 /* Class */);
    }
    return namespaceSymbol;
  }
  function bindPotentiallyNewExpandoMemberToNamespace(declaration, namespaceSymbol, isPrototypeProperty) {
    if (!namespaceSymbol || !isExpandoSymbol(namespaceSymbol)) {
      return;
    }
    const symbolTable ***REMOVED*** isPrototypeProperty ? namespaceSymbol.members || (namespaceSymbol.members ***REMOVED*** createSymbolTable()) : namespaceSymbol.exports || (namespaceSymbol.exports ***REMOVED*** createSymbolTable());
    let includes ***REMOVED*** 0 /* None */;
    let excludes ***REMOVED*** 0 /* None */;
    if (isFunctionLikeDeclaration(getAssignedExpandoInitializer(declaration))) {
      includes ***REMOVED*** 8192 /* Method */;
      excludes ***REMOVED*** 103359 /* MethodExcludes */;
    } else if (isCallExpression(declaration) && isBindableObjectDefinePropertyCall(declaration)) {
      if (some(declaration.arguments[2].properties, (p) ***REMOVED***> {
        const id ***REMOVED*** getNameOfDeclaration(p);
        return !!id && isIdentifier(id) && idText(id) ***REMOVED******REMOVED******REMOVED*** "set";
      })) {
        includes |***REMOVED*** 65536 /* SetAccessor */ | 4 /* Property */;
        excludes |***REMOVED*** 78783 /* SetAccessorExcludes */;
      }
      if (some(declaration.arguments[2].properties, (p) ***REMOVED***> {
        const id ***REMOVED*** getNameOfDeclaration(p);
        return !!id && isIdentifier(id) && idText(id) ***REMOVED******REMOVED******REMOVED*** "get";
      })) {
        includes |***REMOVED*** 32768 /* GetAccessor */ | 4 /* Property */;
        excludes |***REMOVED*** 46015 /* GetAccessorExcludes */;
      }
    }
    if (includes ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      includes ***REMOVED*** 4 /* Property */;
      excludes ***REMOVED*** 0 /* PropertyExcludes */;
    }
    declareSymbol(symbolTable, namespaceSymbol, declaration, includes | 67108864 /* Assignment */, excludes & ~67108864 /* Assignment */);
  }
  function isTopLevelNamespaceAssignment(propertyAccess) {
    return isBinaryExpression(propertyAccess.parent) ? getParentOfBinaryExpression(propertyAccess.parent).parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ : propertyAccess.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
  }
  function bindPropertyAssignment(name, propertyAccess, isPrototypeProperty, containerIsClass) {
    let namespaceSymbol ***REMOVED*** lookupSymbolForPropertyAccess(name, blockScopeContainer) || lookupSymbolForPropertyAccess(name, container);
    const isToplevel ***REMOVED*** isTopLevelNamespaceAssignment(propertyAccess);
    namespaceSymbol ***REMOVED*** bindPotentiallyMissingNamespaces(namespaceSymbol, propertyAccess.expression, isToplevel, isPrototypeProperty, containerIsClass);
    bindPotentiallyNewExpandoMemberToNamespace(propertyAccess, namespaceSymbol, isPrototypeProperty);
  }
  function isExpandoSymbol(symbol) {
    if (symbol.flags & (16 /* Function */ | 32 /* Class */ | 1024 /* NamespaceModule */)) {
      return true;
    }
    const node ***REMOVED*** symbol.valueDeclaration;
    if (node && isCallExpression(node)) {
      return !!getAssignedExpandoInitializer(node);
    }
    let init ***REMOVED*** !node ? void 0 : isVariableDeclaration(node) ? node.initializer : isBinaryExpression(node) ? node.right : isPropertyAccessExpression(node) && isBinaryExpression(node.parent) ? node.parent.right : void 0;
    init ***REMOVED*** init && getRightMostAssignedExpression(init);
    if (init) {
      const isPrototypeAssignment ***REMOVED*** isPrototypeAccess(isVariableDeclaration(node) ? node.name : isBinaryExpression(node) ? node.left : node);
      return !!getExpandoInitializer(isBinaryExpression(init) && (init.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || init.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) ? init.right : init, isPrototypeAssignment);
    }
    return false;
  }
  function getParentOfBinaryExpression(expr) {
    while (isBinaryExpression(expr.parent)) {
      expr ***REMOVED*** expr.parent;
    }
    return expr.parent;
  }
  function lookupSymbolForPropertyAccess(node, lookupContainer ***REMOVED*** container) {
    if (isIdentifier(node)) {
      return lookupSymbolForName(lookupContainer, node.escapedText);
    } else {
      const symbol ***REMOVED*** lookupSymbolForPropertyAccess(node.expression);
      return symbol && symbol.exports && symbol.exports.get(getElementOrPropertyAccessName(node));
    }
  }
  function forEachIdentifierInEntityName(e, parent2, action) {
    if (isExportsOrModuleExportsOrAlias(file, e)) {
      return file.symbol;
    } else if (isIdentifier(e)) {
      return action(e, lookupSymbolForPropertyAccess(e), parent2);
    } else {
      const s ***REMOVED*** forEachIdentifierInEntityName(e.expression, parent2, action);
      const name ***REMOVED*** getNameOrArgument(e);
      if (isPrivateIdentifier(name)) {
        Debug.fail("unexpected PrivateIdentifier");
      }
      return action(name, s && s.exports && s.exports.get(getElementOrPropertyAccessName(e)), s);
    }
  }
  function bindCallExpression(node) {
    if (!file.commonJsModuleIndicator && isRequireCall(
      node,
      /*requireStringLiteralLikeArgument*/
      false
    )) {
      setCommonJsModuleIndicator(node);
    }
  }
  function bindClassLikeDeclaration(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
      bindBlockScopedDeclaration(node, 32 /* Class */, 899503 /* ClassExcludes */);
    } else {
      const bindingName ***REMOVED*** node.name ? node.name.escapedText : "__class" /* Class */;
      bindAnonymousDeclaration(node, 32 /* Class */, bindingName);
      if (node.name) {
        classifiableNames.add(node.name.escapedText);
      }
    }
    const { symbol } ***REMOVED*** node;
    const prototypeSymbol ***REMOVED*** createSymbol(4 /* Property */ | 4194304 /* Prototype */, "prototype");
    const symbolExport ***REMOVED*** symbol.exports.get(prototypeSymbol.escapedName);
    if (symbolExport) {
      if (node.name) {
        setParent(node.name, node);
      }
      file.bindDiagnostics.push(createDiagnosticForNode2(symbolExport.declarations[0], Diagnostics.Duplicate_identifier_0, symbolName(prototypeSymbol)));
    }
    symbol.exports.set(prototypeSymbol.escapedName, prototypeSymbol);
    prototypeSymbol.parent ***REMOVED*** symbol;
  }
  function bindEnumDeclaration(node) {
    return isEnumConst(node) ? bindBlockScopedDeclaration(node, 128 /* ConstEnum */, 899967 /* ConstEnumExcludes */) : bindBlockScopedDeclaration(node, 256 /* RegularEnum */, 899327 /* RegularEnumExcludes */);
  }
  function bindVariableDeclarationOrBindingElement(node) {
    if (inStrictMode) {
      checkStrictModeEvalOrArguments(node, node.name);
    }
    if (!isBindingPattern(node.name)) {
      const possibleVariableDecl ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ ? node : node.parent.parent;
      if (isInJSFile(node) && isVariableDeclarationInitializedToBareOrAccessedRequire(possibleVariableDecl) && !getJSDocTypeTag(node) && !(getCombinedModifierFlags(node) & 32 /* Export */)) {
        declareSymbolAndAddToSymbolTable(node, 2097152 /* Alias */, 2097152 /* AliasExcludes */);
      } else if (isBlockOrCatchScoped(node)) {
        bindBlockScopedDeclaration(node, 2 /* BlockScopedVariable */, 111551 /* BlockScopedVariableExcludes */);
      } else if (isPartOfParameterDeclaration(node)) {
        declareSymbolAndAddToSymbolTable(node, 1 /* FunctionScopedVariable */, 111551 /* ParameterExcludes */);
      } else {
        declareSymbolAndAddToSymbolTable(node, 1 /* FunctionScopedVariable */, 111550 /* FunctionScopedVariableExcludes */);
      }
    }
  }
  function bindParameter(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */ && container.kind !***REMOVED******REMOVED*** 324 /* JSDocSignature */) {
      return;
    }
    if (inStrictMode && !(node.flags & 33554432 /* Ambient */)) {
      checkStrictModeEvalOrArguments(node, node.name);
    }
    if (isBindingPattern(node.name)) {
      bindAnonymousDeclaration(node, 1 /* FunctionScopedVariable */, "__" + node.parent.parameters.indexOf(node));
    } else {
      declareSymbolAndAddToSymbolTable(node, 1 /* FunctionScopedVariable */, 111551 /* ParameterExcludes */);
    }
    if (isParameterPropertyDeclaration(node, node.parent)) {
      const classDeclaration ***REMOVED*** node.parent.parent;
      declareSymbol(classDeclaration.symbol.members, classDeclaration.symbol, node, 4 /* Property */ | (node.questionToken ? 16777216 /* Optional */ : 0 /* None */), 0 /* PropertyExcludes */);
    }
  }
  function bindFunctionDeclaration(node) {
    if (!file.isDeclarationFile && !(node.flags & 33554432 /* Ambient */)) {
      if (isAsyncFunction(node)) {
        emitFlags |***REMOVED*** 4096 /* HasAsyncFunctions */;
      }
    }
    checkStrictModeFunctionName(node);
    if (inStrictMode) {
      checkStrictModeFunctionDeclaration(node);
      bindBlockScopedDeclaration(node, 16 /* Function */, 110991 /* FunctionExcludes */);
    } else {
      declareSymbolAndAddToSymbolTable(node, 16 /* Function */, 110991 /* FunctionExcludes */);
    }
  }
  function bindFunctionExpression(node) {
    if (!file.isDeclarationFile && !(node.flags & 33554432 /* Ambient */)) {
      if (isAsyncFunction(node)) {
        emitFlags |***REMOVED*** 4096 /* HasAsyncFunctions */;
      }
    }
    if (currentFlow) {
      node.flowNode ***REMOVED*** currentFlow;
    }
    checkStrictModeFunctionName(node);
    const bindingName ***REMOVED*** node.name ? node.name.escapedText : "__function" /* Function */;
    return bindAnonymousDeclaration(node, 16 /* Function */, bindingName);
  }
  function bindPropertyOrMethodOrAccessor(node, symbolFlags, symbolExcludes) {
    if (!file.isDeclarationFile && !(node.flags & 33554432 /* Ambient */) && isAsyncFunction(node)) {
      emitFlags |***REMOVED*** 4096 /* HasAsyncFunctions */;
    }
    if (currentFlow && isObjectLiteralOrClassExpressionMethodOrAccessor(node)) {
      node.flowNode ***REMOVED*** currentFlow;
    }
    return hasDynamicName(node) ? bindAnonymousDeclaration(node, symbolFlags, "__computed" /* Computed */) : declareSymbolAndAddToSymbolTable(node, symbolFlags, symbolExcludes);
  }
  function getInferTypeContainer(node) {
    const extendsType ***REMOVED*** findAncestor(node, (n) ***REMOVED***> n.parent && isConditionalTypeNode(n.parent) && n.parent.extendsType ***REMOVED******REMOVED******REMOVED*** n);
    return extendsType && extendsType.parent;
  }
  function bindTypeParameter(node) {
    if (isJSDocTemplateTag(node.parent)) {
      const container2 ***REMOVED*** getEffectiveContainerForJSDocTemplateTag(node.parent);
      if (container2) {
        Debug.assertNode(container2, canHaveLocals);
        container2.locals ?? (container2.locals ***REMOVED*** createSymbolTable());
        declareSymbol(
          container2.locals,
          /*parent*/
          void 0,
          node,
          262144 /* TypeParameter */,
          526824 /* TypeParameterExcludes */
        );
      } else {
        declareSymbolAndAddToSymbolTable(node, 262144 /* TypeParameter */, 526824 /* TypeParameterExcludes */);
      }
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 196 /* InferType */) {
      const container2 ***REMOVED*** getInferTypeContainer(node.parent);
      if (container2) {
        Debug.assertNode(container2, canHaveLocals);
        container2.locals ?? (container2.locals ***REMOVED*** createSymbolTable());
        declareSymbol(
          container2.locals,
          /*parent*/
          void 0,
          node,
          262144 /* TypeParameter */,
          526824 /* TypeParameterExcludes */
        );
      } else {
        bindAnonymousDeclaration(node, 262144 /* TypeParameter */, getDeclarationName(node));
      }
    } else {
      declareSymbolAndAddToSymbolTable(node, 262144 /* TypeParameter */, 526824 /* TypeParameterExcludes */);
    }
  }
  function shouldReportErrorOnModuleDeclaration(node) {
    const instanceState ***REMOVED*** getModuleInstanceState(node);
    return instanceState ***REMOVED******REMOVED******REMOVED*** 1 /* Instantiated */ || instanceState ***REMOVED******REMOVED******REMOVED*** 2 /* ConstEnumOnly */ && shouldPreserveConstEnums(options);
  }
  function checkUnreachable(node) {
    if (!(currentFlow.flags & 1 /* Unreachable */)) {
      return false;
    }
    if (currentFlow ***REMOVED******REMOVED******REMOVED*** unreachableFlow) {
      const reportError ***REMOVED*** (
        // report error on all statements except empty ones
        isStatementButNotDeclaration(node) && node.kind !***REMOVED******REMOVED*** 243 /* EmptyStatement */ || // report error on class declarations
        node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || // report errors on enums with preserved emit
        isEnumDeclarationWithPreservedEmit(node, options) || // report error on instantiated modules
        node.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && shouldReportErrorOnModuleDeclaration(node)
      );
      if (reportError) {
        currentFlow ***REMOVED*** reportedUnreachableFlow;
        if (!options.allowUnreachableCode) {
          const isError ***REMOVED*** unreachableCodeIsError(options) && !(node.flags & 33554432 /* Ambient */) && (!isVariableStatement(node) || !!(getCombinedNodeFlags(node.declarationList) & 7 /* BlockScoped */) || node.declarationList.declarations.some((d) ***REMOVED***> !!d.initializer));
          eachUnreachableRange(node, options, (start, end) ***REMOVED***> errorOrSuggestionOnRange(isError, start, end, Diagnostics.Unreachable_code_detected));
        }
      }
    }
    return true;
  }
}
function isEnumDeclarationWithPreservedEmit(node, options) {
  return node.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */ && (!isEnumConst(node) || shouldPreserveConstEnums(options));
}
function eachUnreachableRange(node, options, cb) {
  if (isStatement(node) && isExecutableStatement(node) && isBlock(node.parent)) {
    const { statements } ***REMOVED*** node.parent;
    const slice ***REMOVED*** sliceAfter(statements, node);
    getRangesWhere(slice, isExecutableStatement, (start, afterEnd) ***REMOVED***> cb(slice[start], slice[afterEnd - 1]));
  } else {
    cb(node, node);
  }
  function isExecutableStatement(s) {
    return !isFunctionDeclaration(s) && !isPurelyTypeDeclaration(s) && // `var x;` may declare a variable used above
    !(isVariableStatement(s) && !(getCombinedNodeFlags(s) & 7 /* BlockScoped */) && s.declarationList.declarations.some((d) ***REMOVED***> !d.initializer));
  }
  function isPurelyTypeDeclaration(s) {
    switch (s.kind) {
      case 265 /* InterfaceDeclaration */:
      case 266 /* TypeAliasDeclaration */:
        return true;
      case 268 /* ModuleDeclaration */:
        return getModuleInstanceState(s) !***REMOVED******REMOVED*** 1 /* Instantiated */;
      case 267 /* EnumDeclaration */:
        return !isEnumDeclarationWithPreservedEmit(s, options);
      default:
        return false;
    }
  }
}
function isExportsOrModuleExportsOrAlias(sourceFile, node) {
  let i ***REMOVED*** 0;
  const q ***REMOVED*** createQueue();
  q.enqueue(node);
  while (!q.isEmpty() && i < 100) {
    i++;
    node ***REMOVED*** q.dequeue();
    if (isExportsIdentifier(node) || isModuleExportsAccessExpression(node)) {
      return true;
    } else if (isIdentifier(node)) {
      const symbol ***REMOVED*** lookupSymbolForName(sourceFile, node.escapedText);
      if (!!symbol && !!symbol.valueDeclaration && isVariableDeclaration(symbol.valueDeclaration) && !!symbol.valueDeclaration.initializer) {
        const init ***REMOVED*** symbol.valueDeclaration.initializer;
        q.enqueue(init);
        if (isAssignmentExpression(
          init,
          /*excludeCompoundAssignment*/
          true
        )) {
          q.enqueue(init.left);
          q.enqueue(init.right);
        }
      }
    }
  }
  return false;
}
function getContainerFlags(node) {
  switch (node.kind) {
    case 232 /* ClassExpression */:
    case 264 /* ClassDeclaration */:
    case 267 /* EnumDeclaration */:
    case 211 /* ObjectLiteralExpression */:
    case 188 /* TypeLiteral */:
    case 323 /* JSDocTypeLiteral */:
    case 293 /* JsxAttributes */:
      return 1 /* IsContainer */;
    case 265 /* InterfaceDeclaration */:
      return 1 /* IsContainer */ | 64 /* IsInterface */;
    case 268 /* ModuleDeclaration */:
    case 266 /* TypeAliasDeclaration */:
    case 201 /* MappedType */:
    case 182 /* IndexSignature */:
      return 1 /* IsContainer */ | 32 /* HasLocals */;
    case 308 /* SourceFile */:
      return 1 /* IsContainer */ | 4 /* IsControlFlowContainer */ | 32 /* HasLocals */;
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 175 /* MethodDeclaration */:
      if (isObjectLiteralOrClassExpressionMethodOrAccessor(node)) {
        return 1 /* IsContainer */ | 4 /* IsControlFlowContainer */ | 32 /* HasLocals */ | 8 /* IsFunctionLike */ | 128 /* IsObjectLiteralOrClassExpressionMethodOrAccessor */;
      }
    // falls through
    case 177 /* Constructor */:
    case 263 /* FunctionDeclaration */:
    case 174 /* MethodSignature */:
    case 180 /* CallSignature */:
    case 324 /* JSDocSignature */:
    case 318 /* JSDocFunctionType */:
    case 185 /* FunctionType */:
    case 181 /* ConstructSignature */:
    case 186 /* ConstructorType */:
    case 176 /* ClassStaticBlockDeclaration */:
      return 1 /* IsContainer */ | 4 /* IsControlFlowContainer */ | 32 /* HasLocals */ | 8 /* IsFunctionLike */;
    case 352 /* JSDocImportTag */:
      return 1 /* IsContainer */ | 4 /* IsControlFlowContainer */ | 32 /* HasLocals */;
    case 219 /* FunctionExpression */:
    case 220 /* ArrowFunction */:
      return 1 /* IsContainer */ | 4 /* IsControlFlowContainer */ | 32 /* HasLocals */ | 8 /* IsFunctionLike */ | 16 /* IsFunctionExpression */;
    case 269 /* ModuleBlock */:
      return 4 /* IsControlFlowContainer */;
    case 173 /* PropertyDeclaration */:
      return node.initializer ? 4 /* IsControlFlowContainer */ : 0;
    case 300 /* CatchClause */:
    case 249 /* ForStatement */:
    case 250 /* ForInStatement */:
    case 251 /* ForOfStatement */:
    case 270 /* CaseBlock */:
      return 2 /* IsBlockScopedContainer */ | 32 /* HasLocals */;
    case 242 /* Block */:
      return isFunctionLike(node.parent) || isClassStaticBlockDeclaration(node.parent) ? 0 /* None */ : 2 /* IsBlockScopedContainer */ | 32 /* HasLocals */;
  }
  return 0 /* None */;
}
function lookupSymbolForName(container, name) {
  var _a, _b, _c, _d;
  const local ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** tryCast(container, canHaveLocals)) ***REMOVED******REMOVED*** null ? void 0 : _a.locals) ***REMOVED******REMOVED*** null ? void 0 : _b.get(name);
  if (local) {
    return local.exportSymbol ?? local;
  }
  if (isSourceFile(container) && container.jsGlobalAugmentations && container.jsGlobalAugmentations.has(name)) {
    return container.jsGlobalAugmentations.get(name);
  }
  if (canHaveSymbol(container)) {
    return (_d ***REMOVED*** (_c ***REMOVED*** container.symbol) ***REMOVED******REMOVED*** null ? void 0 : _c.exports) ***REMOVED******REMOVED*** null ? void 0 : _d.get(name);
  }
}

// src/compiler/symbolWalker.ts
function createGetSymbolWalker(getRestTypeOfSignature, getTypePredicateOfSignature, getReturnTypeOfSignature, getBaseTypes, resolveStructuredTypeMembers, getTypeOfSymbol, getResolvedSymbol, getConstraintOfTypeParameter, getFirstIdentifier2, getTypeArguments) {
  return getSymbolWalker;
  function getSymbolWalker(accept ***REMOVED*** () ***REMOVED***> true) {
    const visitedTypes ***REMOVED*** [];
    const visitedSymbols ***REMOVED*** [];
    return {
      walkType: (type) ***REMOVED***> {
        try {
          visitType(type);
          return { visitedTypes: getOwnValues(visitedTypes), visitedSymbols: getOwnValues(visitedSymbols) };
        } finally {
          clear(visitedTypes);
          clear(visitedSymbols);
        }
      },
      walkSymbol: (symbol) ***REMOVED***> {
        try {
          visitSymbol(symbol);
          return { visitedTypes: getOwnValues(visitedTypes), visitedSymbols: getOwnValues(visitedSymbols) };
        } finally {
          clear(visitedTypes);
          clear(visitedSymbols);
        }
      }
    };
    function visitType(type) {
      if (!type) {
        return;
      }
      if (visitedTypes[type.id]) {
        return;
      }
      visitedTypes[type.id] ***REMOVED*** type;
      const shouldBail ***REMOVED*** visitSymbol(type.symbol);
      if (shouldBail) return;
      if (type.flags & 524288 /* Object */) {
        const objectType ***REMOVED*** type;
        const objectFlags ***REMOVED*** objectType.objectFlags;
        if (objectFlags & 4 /* Reference */) {
          visitTypeReference(type);
        }
        if (objectFlags & 32 /* Mapped */) {
          visitMappedType(type);
        }
        if (objectFlags & (1 /* Class */ | 2 /* Interface */)) {
          visitInterfaceType(type);
        }
        if (objectFlags & (8 /* Tuple */ | 16 /* Anonymous */)) {
          visitObjectType(objectType);
        }
      }
      if (type.flags & 262144 /* TypeParameter */) {
        visitTypeParameter(type);
      }
      if (type.flags & 3145728 /* UnionOrIntersection */) {
        visitUnionOrIntersectionType(type);
      }
      if (type.flags & 4194304 /* Index */) {
        visitIndexType(type);
      }
      if (type.flags & 8388608 /* IndexedAccess */) {
        visitIndexedAccessType(type);
      }
    }
    function visitTypeReference(type) {
      visitType(type.target);
      forEach(getTypeArguments(type), visitType);
    }
    function visitTypeParameter(type) {
      visitType(getConstraintOfTypeParameter(type));
    }
    function visitUnionOrIntersectionType(type) {
      forEach(type.types, visitType);
    }
    function visitIndexType(type) {
      visitType(type.type);
    }
    function visitIndexedAccessType(type) {
      visitType(type.objectType);
      visitType(type.indexType);
      visitType(type.constraint);
    }
    function visitMappedType(type) {
      visitType(type.typeParameter);
      visitType(type.constraintType);
      visitType(type.templateType);
      visitType(type.modifiersType);
    }
    function visitSignature(signature) {
      const typePredicate ***REMOVED*** getTypePredicateOfSignature(signature);
      if (typePredicate) {
        visitType(typePredicate.type);
      }
      forEach(signature.typeParameters, visitType);
      for (const parameter of signature.parameters) {
        visitSymbol(parameter);
      }
      visitType(getRestTypeOfSignature(signature));
      visitType(getReturnTypeOfSignature(signature));
    }
    function visitInterfaceType(interfaceT) {
      visitObjectType(interfaceT);
      forEach(interfaceT.typeParameters, visitType);
      forEach(getBaseTypes(interfaceT), visitType);
      visitType(interfaceT.thisType);
    }
    function visitObjectType(type) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      for (const info of resolved.indexInfos) {
        visitType(info.keyType);
        visitType(info.type);
      }
      for (const signature of resolved.callSignatures) {
        visitSignature(signature);
      }
      for (const signature of resolved.constructSignatures) {
        visitSignature(signature);
      }
      for (const p of resolved.properties) {
        visitSymbol(p);
      }
    }
    function visitSymbol(symbol) {
      if (!symbol) {
        return false;
      }
      const symbolId ***REMOVED*** getSymbolId(symbol);
      if (visitedSymbols[symbolId]) {
        return false;
      }
      visitedSymbols[symbolId] ***REMOVED*** symbol;
      if (!accept(symbol)) {
        return true;
      }
      const t ***REMOVED*** getTypeOfSymbol(symbol);
      visitType(t);
      if (symbol.exports) {
        symbol.exports.forEach(visitSymbol);
      }
      forEach(symbol.declarations, (d) ***REMOVED***> {
        if (d.type && d.type.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */) {
          const query ***REMOVED*** d.type;
          const entity ***REMOVED*** getResolvedSymbol(getFirstIdentifier2(query.exprName));
          visitSymbol(entity);
        }
      });
      return false;
    }
  }
}

// src/compiler/moduleSpecifiers.ts
var stringToRegex ***REMOVED*** memoizeOne((pattern) ***REMOVED***> {
  try {
    let slash ***REMOVED*** pattern.indexOf("/");
    if (slash !***REMOVED******REMOVED*** 0) {
      return new RegExp(pattern);
    }
    const lastSlash ***REMOVED*** pattern.lastIndexOf("/");
    if (slash ***REMOVED******REMOVED******REMOVED*** lastSlash) {
      return new RegExp(pattern);
    }
    while ((slash ***REMOVED*** pattern.indexOf("/", slash + 1)) !***REMOVED******REMOVED*** lastSlash) {
      if (pattern[slash - 1] !***REMOVED******REMOVED*** "\\") {
        return new RegExp(pattern);
      }
    }
    const flags ***REMOVED*** pattern.substring(lastSlash + 1).replace(/[^iu]/g, "");
    pattern ***REMOVED*** pattern.substring(1, lastSlash);
    return new RegExp(pattern, flags);
  } catch {
    return void 0;
  }
});
function getModuleSpecifierPreferences({ importModuleSpecifierPreference, importModuleSpecifierEnding, autoImportSpecifierExcludeRegexes }, host, compilerOptions, importingSourceFile, oldImportSpecifier) {
  const filePreferredEnding ***REMOVED*** getPreferredEnding();
  return {
    excludeRegexes: autoImportSpecifierExcludeRegexes,
    relativePreference: oldImportSpecifier !***REMOVED******REMOVED*** void 0 ? isExternalModuleNameRelative(oldImportSpecifier) ? 0 /* Relative */ : 1 /* NonRelative */ : importModuleSpecifierPreference ***REMOVED******REMOVED******REMOVED*** "relative" ? 0 /* Relative */ : importModuleSpecifierPreference ***REMOVED******REMOVED******REMOVED*** "non-relative" ? 1 /* NonRelative */ : importModuleSpecifierPreference ***REMOVED******REMOVED******REMOVED*** "project-relative" ? 3 /* ExternalNonRelative */ : 2 /* Shortest */,
    getAllowedEndingsInPreferredOrder: (syntaxImpliedNodeFormat) ***REMOVED***> {
      const impliedNodeFormat ***REMOVED*** getDefaultResolutionModeForFile(importingSourceFile, host, compilerOptions);
      const preferredEnding ***REMOVED*** syntaxImpliedNodeFormat !***REMOVED******REMOVED*** impliedNodeFormat ? getPreferredEnding(syntaxImpliedNodeFormat) : filePreferredEnding;
      const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(compilerOptions);
      if ((syntaxImpliedNodeFormat ?? impliedNodeFormat) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && 3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */) {
        if (shouldAllowImportingTsExtension(compilerOptions, importingSourceFile.fileName)) {
          return [3 /* TsExtension */, 2 /* JsExtension */];
        }
        return [2 /* JsExtension */];
      }
      if (getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 1 /* Classic */) {
        return preferredEnding ***REMOVED******REMOVED******REMOVED*** 2 /* JsExtension */ ? [2 /* JsExtension */, 1 /* Index */] : [1 /* Index */, 2 /* JsExtension */];
      }
      const allowImportingTsExtension ***REMOVED*** shouldAllowImportingTsExtension(compilerOptions, importingSourceFile.fileName);
      switch (preferredEnding) {
        case 2 /* JsExtension */:
          return allowImportingTsExtension ? [2 /* JsExtension */, 3 /* TsExtension */, 0 /* Minimal */, 1 /* Index */] : [2 /* JsExtension */, 0 /* Minimal */, 1 /* Index */];
        case 3 /* TsExtension */:
          return [3 /* TsExtension */, 0 /* Minimal */, 2 /* JsExtension */, 1 /* Index */];
        case 1 /* Index */:
          return allowImportingTsExtension ? [1 /* Index */, 0 /* Minimal */, 3 /* TsExtension */, 2 /* JsExtension */] : [1 /* Index */, 0 /* Minimal */, 2 /* JsExtension */];
        case 0 /* Minimal */:
          return allowImportingTsExtension ? [0 /* Minimal */, 1 /* Index */, 3 /* TsExtension */, 2 /* JsExtension */] : [0 /* Minimal */, 1 /* Index */, 2 /* JsExtension */];
        default:
          Debug.assertNever(preferredEnding);
      }
    }
  };
  function getPreferredEnding(resolutionMode) {
    if (oldImportSpecifier !***REMOVED******REMOVED*** void 0) {
      if (hasJSFileExtension(oldImportSpecifier)) return 2 /* JsExtension */;
      if (endsWith(oldImportSpecifier, "/index")) return 1 /* Index */;
    }
    return getModuleSpecifierEndingPreference(
      importModuleSpecifierEnding,
      resolutionMode ?? getDefaultResolutionModeForFile(importingSourceFile, host, compilerOptions),
      compilerOptions,
      isFullSourceFile(importingSourceFile) ? importingSourceFile : void 0
    );
  }
}
function tryGetModuleSpecifiersFromCacheWorker(moduleSymbol, importingSourceFile, host, userPreferences, options ***REMOVED*** {}) {
  var _a;
  const moduleSourceFile ***REMOVED*** getSourceFileOfModule(moduleSymbol);
  if (!moduleSourceFile) {
    return emptyArray;
  }
  const cache ***REMOVED*** (_a ***REMOVED*** host.getModuleSpecifierCache) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host);
  const cached ***REMOVED*** cache ***REMOVED******REMOVED*** null ? void 0 : cache.get(importingSourceFile.path, moduleSourceFile.path, userPreferences, options);
  return [cached ***REMOVED******REMOVED*** null ? void 0 : cached.kind, cached ***REMOVED******REMOVED*** null ? void 0 : cached.moduleSpecifiers, moduleSourceFile, cached ***REMOVED******REMOVED*** null ? void 0 : cached.modulePaths, cache];
}
function getModuleSpecifiers(moduleSymbol, checker, compilerOptions, importingSourceFile, host, userPreferences, options ***REMOVED*** {}) {
  return getModuleSpecifiersWithCacheInfo(
    moduleSymbol,
    checker,
    compilerOptions,
    importingSourceFile,
    host,
    userPreferences,
    options,
    /*forAutoImport*/
    false
  ).moduleSpecifiers;
}
function getModuleSpecifiersWithCacheInfo(moduleSymbol, checker, compilerOptions, importingSourceFile, host, userPreferences, options ***REMOVED*** {}, forAutoImport) {
  let computedWithoutCache ***REMOVED*** false;
  const ambient ***REMOVED*** tryGetModuleNameFromAmbientModule(moduleSymbol, checker);
  if (ambient) {
    return {
      kind: "ambient",
      moduleSpecifiers: !(forAutoImport && isExcludedByRegex(ambient, userPreferences.autoImportSpecifierExcludeRegexes)) ? [ambient] : emptyArray,
      computedWithoutCache
    };
  }
  let [kind, specifiers, moduleSourceFile, modulePaths, cache] ***REMOVED*** tryGetModuleSpecifiersFromCacheWorker(
    moduleSymbol,
    importingSourceFile,
    host,
    userPreferences,
    options
  );
  if (specifiers) return { kind, moduleSpecifiers: specifiers, computedWithoutCache };
  if (!moduleSourceFile) return { kind: void 0, moduleSpecifiers: emptyArray, computedWithoutCache };
  computedWithoutCache ***REMOVED*** true;
  modulePaths || (modulePaths ***REMOVED*** getAllModulePathsWorker(getInfo(importingSourceFile.fileName, host), moduleSourceFile.originalFileName, host, compilerOptions, options));
  const result ***REMOVED*** computeModuleSpecifiers(
    modulePaths,
    compilerOptions,
    importingSourceFile,
    host,
    userPreferences,
    options,
    forAutoImport
  );
  cache ***REMOVED******REMOVED*** null ? void 0 : cache.set(importingSourceFile.path, moduleSourceFile.path, userPreferences, options, result.kind, modulePaths, result.moduleSpecifiers);
  return result;
}
function computeModuleSpecifiers(modulePaths, compilerOptions, importingSourceFile, host, userPreferences, options ***REMOVED*** {}, forAutoImport) {
  const info ***REMOVED*** getInfo(importingSourceFile.fileName, host);
  const preferences ***REMOVED*** getModuleSpecifierPreferences(userPreferences, host, compilerOptions, importingSourceFile);
  const existingSpecifier ***REMOVED*** isFullSourceFile(importingSourceFile) && forEach(modulePaths, (modulePath) ***REMOVED***> forEach(
    host.getFileIncludeReasons().get(toPath(modulePath.path, host.getCurrentDirectory(), info.getCanonicalFileName)),
    (reason) ***REMOVED***> {
      if (reason.kind !***REMOVED******REMOVED*** 3 /* Import */ || reason.file !***REMOVED******REMOVED*** importingSourceFile.path) return void 0;
      const existingMode ***REMOVED*** host.getModeForResolutionAtIndex(importingSourceFile, reason.index);
      const targetMode ***REMOVED*** options.overrideImportMode ?? host.getDefaultResolutionModeForFile(importingSourceFile);
      if (existingMode !***REMOVED******REMOVED*** targetMode && existingMode !***REMOVED******REMOVED*** void 0 && targetMode !***REMOVED******REMOVED*** void 0) {
        return void 0;
      }
      const specifier ***REMOVED*** getModuleNameStringLiteralAt(importingSourceFile, reason.index).text;
      return preferences.relativePreference !***REMOVED******REMOVED*** 1 /* NonRelative */ || !pathIsRelative(specifier) ? specifier : void 0;
    }
  ));
  if (existingSpecifier) {
    return { kind: void 0, moduleSpecifiers: [existingSpecifier], computedWithoutCache: true };
  }
  const importedFileIsInNodeModules ***REMOVED*** some(modulePaths, (p) ***REMOVED***> p.isInNodeModules);
  let nodeModulesSpecifiers;
  let pathsSpecifiers;
  let redirectPathsSpecifiers;
  let relativeSpecifiers;
  for (const modulePath of modulePaths) {
    const specifier ***REMOVED*** modulePath.isInNodeModules ? tryGetModuleNameAsNodeModule(
      modulePath,
      info,
      importingSourceFile,
      host,
      compilerOptions,
      userPreferences,
      /*packageNameOnly*/
      void 0,
      options.overrideImportMode
    ) : void 0;
    if (specifier && !(forAutoImport && isExcludedByRegex(specifier, preferences.excludeRegexes))) {
      nodeModulesSpecifiers ***REMOVED*** append(nodeModulesSpecifiers, specifier);
      if (modulePath.isRedirect) {
        return { kind: "node_modules", moduleSpecifiers: nodeModulesSpecifiers, computedWithoutCache: true };
      }
    }
    const local ***REMOVED*** getLocalModuleSpecifier(
      modulePath.path,
      info,
      compilerOptions,
      host,
      options.overrideImportMode || importingSourceFile.impliedNodeFormat,
      preferences,
      /*pathsOnly*/
      modulePath.isRedirect || !!specifier
    );
    if (!local || forAutoImport && isExcludedByRegex(local, preferences.excludeRegexes)) {
      continue;
    }
    if (modulePath.isRedirect) {
      redirectPathsSpecifiers ***REMOVED*** append(redirectPathsSpecifiers, local);
    } else if (pathIsBareSpecifier(local)) {
      if (pathContainsNodeModules(local)) {
        relativeSpecifiers ***REMOVED*** append(relativeSpecifiers, local);
      } else {
        pathsSpecifiers ***REMOVED*** append(pathsSpecifiers, local);
      }
    } else if (forAutoImport || !importedFileIsInNodeModules || modulePath.isInNodeModules) {
      relativeSpecifiers ***REMOVED*** append(relativeSpecifiers, local);
    }
  }
  return (pathsSpecifiers ***REMOVED******REMOVED*** null ? void 0 : pathsSpecifiers.length) ? { kind: "paths", moduleSpecifiers: pathsSpecifiers, computedWithoutCache: true } : (redirectPathsSpecifiers ***REMOVED******REMOVED*** null ? void 0 : redirectPathsSpecifiers.length) ? { kind: "redirect", moduleSpecifiers: redirectPathsSpecifiers, computedWithoutCache: true } : (nodeModulesSpecifiers ***REMOVED******REMOVED*** null ? void 0 : nodeModulesSpecifiers.length) ? { kind: "node_modules", moduleSpecifiers: nodeModulesSpecifiers, computedWithoutCache: true } : { kind: "relative", moduleSpecifiers: relativeSpecifiers ?? emptyArray, computedWithoutCache: true };
}
function isExcludedByRegex(moduleSpecifier, excludeRegexes) {
  return some(excludeRegexes, (pattern) ***REMOVED***> {
    var _a;
    return !!((_a ***REMOVED*** stringToRegex(pattern)) ***REMOVED******REMOVED*** null ? void 0 : _a.test(moduleSpecifier));
  });
}
function getInfo(importingSourceFileName, host) {
  importingSourceFileName ***REMOVED*** getNormalizedAbsolutePath(importingSourceFileName, host.getCurrentDirectory());
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames ? host.useCaseSensitiveFileNames() : true);
  const sourceDirectory ***REMOVED*** getDirectoryPath(importingSourceFileName);
  return {
    getCanonicalFileName,
    importingSourceFileName,
    sourceDirectory,
    canonicalSourceDirectory: getCanonicalFileName(sourceDirectory)
  };
}
function getLocalModuleSpecifier(moduleFileName, info, compilerOptions, host, importMode, { getAllowedEndingsInPreferredOrder: getAllowedEndingsInPrefererredOrder, relativePreference, excludeRegexes }, pathsOnly) {
  const { baseUrl, paths, rootDirs } ***REMOVED*** compilerOptions;
  if (pathsOnly && !paths) {
    return void 0;
  }
  const { sourceDirectory, canonicalSourceDirectory, getCanonicalFileName } ***REMOVED*** info;
  const allowedEndings ***REMOVED*** getAllowedEndingsInPrefererredOrder(importMode);
  const relativePath ***REMOVED*** rootDirs && tryGetModuleNameFromRootDirs(rootDirs, moduleFileName, sourceDirectory, getCanonicalFileName, allowedEndings, compilerOptions) || processEnding(ensurePathIsNonModuleName(getRelativePathFromDirectory(sourceDirectory, moduleFileName, getCanonicalFileName)), allowedEndings, compilerOptions);
  if (!baseUrl && !paths && !getResolvePackageJsonImports(compilerOptions) || relativePreference ***REMOVED******REMOVED******REMOVED*** 0 /* Relative */) {
    return pathsOnly ? void 0 : relativePath;
  }
  const baseDirectory ***REMOVED*** getNormalizedAbsolutePath(getPathsBasePath(compilerOptions, host) || baseUrl, host.getCurrentDirectory());
  const relativeToBaseUrl ***REMOVED*** getRelativePathIfInSameVolume(moduleFileName, baseDirectory, getCanonicalFileName);
  if (!relativeToBaseUrl) {
    return pathsOnly ? void 0 : relativePath;
  }
  const fromPackageJsonImports ***REMOVED*** pathsOnly ? void 0 : tryGetModuleNameFromPackageJsonImports(
    moduleFileName,
    sourceDirectory,
    compilerOptions,
    host,
    importMode,
    prefersTsExtension(allowedEndings)
  );
  const fromPaths ***REMOVED*** pathsOnly || fromPackageJsonImports ***REMOVED******REMOVED******REMOVED*** void 0 ? paths && tryGetModuleNameFromPaths(relativeToBaseUrl, paths, allowedEndings, baseDirectory, getCanonicalFileName, host, compilerOptions) : void 0;
  if (pathsOnly) {
    return fromPaths;
  }
  const maybeNonRelative ***REMOVED*** fromPackageJsonImports ?? (fromPaths ***REMOVED******REMOVED******REMOVED*** void 0 && baseUrl !***REMOVED******REMOVED*** void 0 ? processEnding(relativeToBaseUrl, allowedEndings, compilerOptions) : fromPaths);
  if (!maybeNonRelative) {
    return relativePath;
  }
  const relativeIsExcluded ***REMOVED*** isExcludedByRegex(relativePath, excludeRegexes);
  const nonRelativeIsExcluded ***REMOVED*** isExcludedByRegex(maybeNonRelative, excludeRegexes);
  if (!relativeIsExcluded && nonRelativeIsExcluded) {
    return relativePath;
  }
  if (relativeIsExcluded && !nonRelativeIsExcluded) {
    return maybeNonRelative;
  }
  if (relativePreference ***REMOVED******REMOVED******REMOVED*** 1 /* NonRelative */ && !pathIsRelative(maybeNonRelative)) {
    return maybeNonRelative;
  }
  if (relativePreference ***REMOVED******REMOVED******REMOVED*** 3 /* ExternalNonRelative */ && !pathIsRelative(maybeNonRelative)) {
    const projectDirectory ***REMOVED*** compilerOptions.configFilePath ? toPath(getDirectoryPath(compilerOptions.configFilePath), host.getCurrentDirectory(), info.getCanonicalFileName) : info.getCanonicalFileName(host.getCurrentDirectory());
    const modulePath ***REMOVED*** toPath(moduleFileName, projectDirectory, getCanonicalFileName);
    const sourceIsInternal ***REMOVED*** startsWith(canonicalSourceDirectory, projectDirectory);
    const targetIsInternal ***REMOVED*** startsWith(modulePath, projectDirectory);
    if (sourceIsInternal && !targetIsInternal || !sourceIsInternal && targetIsInternal) {
      return maybeNonRelative;
    }
    const nearestTargetPackageJson ***REMOVED*** getNearestAncestorDirectoryWithPackageJson(host, getDirectoryPath(modulePath));
    const nearestSourcePackageJson ***REMOVED*** getNearestAncestorDirectoryWithPackageJson(host, sourceDirectory);
    const ignoreCase ***REMOVED*** !hostUsesCaseSensitiveFileNames(host);
    if (!packageJsonPathsAreEqual(nearestTargetPackageJson, nearestSourcePackageJson, ignoreCase)) {
      return maybeNonRelative;
    }
    return relativePath;
  }
  return isPathRelativeToParent(maybeNonRelative) || countPathComponents(relativePath) < countPathComponents(maybeNonRelative) ? relativePath : maybeNonRelative;
}
function packageJsonPathsAreEqual(a, b, ignoreCase) {
  if (a ***REMOVED******REMOVED******REMOVED*** b) return true;
  if (a ***REMOVED******REMOVED******REMOVED*** void 0 || b ***REMOVED******REMOVED******REMOVED*** void 0) return false;
  return comparePaths(a, b, ignoreCase) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */;
}
function countPathComponents(path) {
  let count ***REMOVED*** 0;
  for (let i ***REMOVED*** startsWith(path, "./") ? 2 : 0; i < path.length; i++) {
    if (path.charCodeAt(i) ***REMOVED******REMOVED******REMOVED*** 47 /* slash */) count++;
  }
  return count;
}
function comparePathsByRedirectAndNumberOfDirectorySeparators(a, b) {
  return compareBooleans(b.isRedirect, a.isRedirect) || compareNumberOfDirectorySeparators(a.path, b.path);
}
function getNearestAncestorDirectoryWithPackageJson(host, fileName) {
  if (host.getNearestAncestorDirectoryWithPackageJson) {
    return host.getNearestAncestorDirectoryWithPackageJson(fileName);
  }
  return forEachAncestorDirectoryStoppingAtGlobalCache(
    host,
    fileName,
    (directory) ***REMOVED***> host.fileExists(combinePaths(directory, "package.json")) ? directory : void 0
  );
}
function forEachFileNameOfModule(importingFileName, importedFileName, host, preferSymlinks, cb) {
  var _a, _b;
  const getCanonicalFileName ***REMOVED*** hostGetCanonicalFileName(host);
  const cwd ***REMOVED*** host.getCurrentDirectory();
  const referenceRedirect ***REMOVED*** host.isSourceOfProjectReferenceRedirect(importedFileName) ? (_a ***REMOVED*** host.getRedirectFromSourceFile(importedFileName)) ***REMOVED******REMOVED*** null ? void 0 : _a.outputDts : void 0;
  const importedPath ***REMOVED*** toPath(importedFileName, cwd, getCanonicalFileName);
  const redirects ***REMOVED*** host.redirectTargetsMap.get(importedPath) || emptyArray;
  const importedFileNames ***REMOVED*** [...referenceRedirect ? [referenceRedirect] : emptyArray, importedFileName, ...redirects];
  const targets ***REMOVED*** importedFileNames.map((f) ***REMOVED***> getNormalizedAbsolutePath(f, cwd));
  let shouldFilterIgnoredPaths ***REMOVED*** !every(targets, containsIgnoredPath);
  if (!preferSymlinks) {
    const result2 ***REMOVED*** forEach(targets, (p) ***REMOVED***> !(shouldFilterIgnoredPaths && containsIgnoredPath(p)) && cb(p, referenceRedirect ***REMOVED******REMOVED******REMOVED*** p));
    if (result2) return result2;
  }
  const symlinkedDirectories ***REMOVED*** (_b ***REMOVED*** host.getSymlinkCache) ***REMOVED******REMOVED*** null ? void 0 : _b.call(host).getSymlinkedDirectoriesByRealpath();
  const fullImportedFileName ***REMOVED*** getNormalizedAbsolutePath(importedFileName, cwd);
  const result ***REMOVED*** symlinkedDirectories && forEachAncestorDirectoryStoppingAtGlobalCache(
    host,
    getDirectoryPath(fullImportedFileName),
    (realPathDirectory) ***REMOVED***> {
      const symlinkDirectories ***REMOVED*** symlinkedDirectories.get(ensureTrailingDirectorySeparator(toPath(realPathDirectory, cwd, getCanonicalFileName)));
      if (!symlinkDirectories) return void 0;
      if (startsWithDirectory(importingFileName, realPathDirectory, getCanonicalFileName)) {
        return false;
      }
      return forEach(targets, (target) ***REMOVED***> {
        if (!startsWithDirectory(target, realPathDirectory, getCanonicalFileName)) {
          return;
        }
        const relative ***REMOVED*** getRelativePathFromDirectory(realPathDirectory, target, getCanonicalFileName);
        for (const symlinkDirectory of symlinkDirectories) {
          const option ***REMOVED*** resolvePath(symlinkDirectory, relative);
          const result2 ***REMOVED*** cb(option, target ***REMOVED******REMOVED******REMOVED*** referenceRedirect);
          shouldFilterIgnoredPaths ***REMOVED*** true;
          if (result2) return result2;
        }
      });
    }
  );
  return result || (preferSymlinks ? forEach(targets, (p) ***REMOVED***> shouldFilterIgnoredPaths && containsIgnoredPath(p) ? void 0 : cb(p, p ***REMOVED******REMOVED******REMOVED*** referenceRedirect)) : void 0);
}
var runtimeDependencyFields ***REMOVED*** ["dependencies", "peerDependencies", "optionalDependencies"];
function getAllRuntimeDependencies(packageJson) {
  let result;
  for (const field of runtimeDependencyFields) {
    const deps ***REMOVED*** packageJson[field];
    if (deps && typeof deps ***REMOVED******REMOVED******REMOVED*** "object") {
      result ***REMOVED*** concatenate(result, getOwnKeys(deps));
    }
  }
  return result;
}
function getAllModulePathsWorker(info, importedFileName, host, compilerOptions, options) {
  var _a, _b;
  const cache ***REMOVED*** (_a ***REMOVED*** host.getModuleResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host);
  const links ***REMOVED*** (_b ***REMOVED*** host.getSymlinkCache) ***REMOVED******REMOVED*** null ? void 0 : _b.call(host);
  if (cache && links && host.readFile && !pathContainsNodeModules(info.importingSourceFileName)) {
    Debug.type(host);
    const state ***REMOVED*** getTemporaryModuleResolutionState(cache.getPackageJsonInfoCache(), host, {});
    const packageJson ***REMOVED*** getPackageScopeForPath(getDirectoryPath(info.importingSourceFileName), state);
    if (packageJson) {
      const toResolve ***REMOVED*** getAllRuntimeDependencies(packageJson.contents.packageJsonContent);
      for (const depName of toResolve || emptyArray) {
        const resolved ***REMOVED*** resolveModuleName(
          depName,
          combinePaths(packageJson.packageDirectory, "package.json"),
          compilerOptions,
          host,
          cache,
          /*redirectedReference*/
          void 0,
          options.overrideImportMode
        );
        links.setSymlinksFromResolution(resolved.resolvedModule);
      }
    }
  }
  const allFileNames ***REMOVED*** /* @__PURE__ */ new Map();
  let importedFileFromNodeModules ***REMOVED*** false;
  forEachFileNameOfModule(
    info.importingSourceFileName,
    importedFileName,
    host,
    /*preferSymlinks*/
    true,
    (path, isRedirect) ***REMOVED***> {
      const isInNodeModules ***REMOVED*** pathContainsNodeModules(path);
      allFileNames.set(path, { path: info.getCanonicalFileName(path), isRedirect, isInNodeModules });
      importedFileFromNodeModules ***REMOVED*** importedFileFromNodeModules || isInNodeModules;
    }
  );
  const sortedPaths ***REMOVED*** [];
  for (let directory ***REMOVED*** info.canonicalSourceDirectory; allFileNames.size !***REMOVED******REMOVED*** 0; ) {
    const directoryStart ***REMOVED*** ensureTrailingDirectorySeparator(directory);
    let pathsInDirectory;
    allFileNames.forEach(({ path, isRedirect, isInNodeModules }, fileName) ***REMOVED***> {
      if (startsWith(path, directoryStart)) {
        (pathsInDirectory || (pathsInDirectory ***REMOVED*** [])).push({ path: fileName, isRedirect, isInNodeModules });
        allFileNames.delete(fileName);
      }
    });
    if (pathsInDirectory) {
      if (pathsInDirectory.length > 1) {
        pathsInDirectory.sort(comparePathsByRedirectAndNumberOfDirectorySeparators);
      }
      sortedPaths.push(...pathsInDirectory);
    }
    const newDirectory ***REMOVED*** getDirectoryPath(directory);
    if (newDirectory ***REMOVED******REMOVED******REMOVED*** directory) break;
    directory ***REMOVED*** newDirectory;
  }
  if (allFileNames.size) {
    const remainingPaths ***REMOVED*** arrayFrom(
      allFileNames.entries(),
      ([fileName, { isRedirect, isInNodeModules }]) ***REMOVED***> ({ path: fileName, isRedirect, isInNodeModules })
    );
    if (remainingPaths.length > 1) remainingPaths.sort(comparePathsByRedirectAndNumberOfDirectorySeparators);
    sortedPaths.push(...remainingPaths);
  }
  return sortedPaths;
}
function tryGetModuleNameFromAmbientModule(moduleSymbol, checker) {
  var _a;
  const decl ***REMOVED*** (_a ***REMOVED*** moduleSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(
    (d) ***REMOVED***> isNonGlobalAmbientModule(d) && (!isExternalModuleAugmentation(d) || !isExternalModuleNameRelative(getTextOfIdentifierOrLiteral(d.name)))
  );
  if (decl) {
    return decl.name.text;
  }
  const ambientModuleDeclareCandidates ***REMOVED*** mapDefined(moduleSymbol.declarations, (d) ***REMOVED***> {
    var _a2, _b, _c, _d;
    if (!isModuleDeclaration(d)) return;
    const topNamespace ***REMOVED*** getTopNamespace(d);
    if (!(((_a2 ***REMOVED*** topNamespace ***REMOVED******REMOVED*** null ? void 0 : topNamespace.parent) ***REMOVED******REMOVED*** null ? void 0 : _a2.parent) && isModuleBlock(topNamespace.parent) && isAmbientModule(topNamespace.parent.parent) && isSourceFile(topNamespace.parent.parent.parent))) return;
    const exportAssignment ***REMOVED*** (_d ***REMOVED*** (_c ***REMOVED*** (_b ***REMOVED*** topNamespace.parent.parent.symbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.get("export***REMOVED***")) ***REMOVED******REMOVED*** null ? void 0 : _c.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _d.expression;
    if (!exportAssignment) return;
    const exportSymbol ***REMOVED*** checker.getSymbolAtLocation(exportAssignment);
    if (!exportSymbol) return;
    const originalExportSymbol ***REMOVED*** (exportSymbol ***REMOVED******REMOVED*** null ? void 0 : exportSymbol.flags) & 2097152 /* Alias */ ? checker.getAliasedSymbol(exportSymbol) : exportSymbol;
    if (originalExportSymbol ***REMOVED******REMOVED******REMOVED*** d.symbol) return topNamespace.parent.parent;
    function getTopNamespace(namespaceDeclaration) {
      while (namespaceDeclaration.flags & 8 /* NestedNamespace */) {
        namespaceDeclaration ***REMOVED*** namespaceDeclaration.parent;
      }
      return namespaceDeclaration;
    }
  });
  const ambientModuleDeclare ***REMOVED*** ambientModuleDeclareCandidates[0];
  if (ambientModuleDeclare) {
    return ambientModuleDeclare.name.text;
  }
}
function tryGetModuleNameFromPaths(relativeToBaseUrl, paths, allowedEndings, baseDirectory, getCanonicalFileName, host, compilerOptions) {
  for (const key in paths) {
    for (const patternText2 of paths[key]) {
      const normalized ***REMOVED*** normalizePath(patternText2);
      const pattern ***REMOVED*** getRelativePathIfInSameVolume(normalized, baseDirectory, getCanonicalFileName) ?? normalized;
      const indexOfStar ***REMOVED*** pattern.indexOf("*");
      const candidates ***REMOVED*** allowedEndings.map((ending) ***REMOVED***> ({
        ending,
        value: processEnding(relativeToBaseUrl, [ending], compilerOptions)
      }));
      if (tryGetExtensionFromPath2(pattern)) {
        candidates.push({ ending: void 0, value: relativeToBaseUrl });
      }
      if (indexOfStar !***REMOVED******REMOVED*** -1) {
        const prefix ***REMOVED*** pattern.substring(0, indexOfStar);
        const suffix ***REMOVED*** pattern.substring(indexOfStar + 1);
        for (const { ending, value } of candidates) {
          if (value.length >***REMOVED*** prefix.length + suffix.length && startsWith(value, prefix) && endsWith(value, suffix) && validateEnding({ ending, value })) {
            const matchedStar ***REMOVED*** value.substring(prefix.length, value.length - suffix.length);
            if (!pathIsRelative(matchedStar)) {
              return replaceFirstStar(key, matchedStar);
            }
          }
        }
      } else if (some(candidates, (c) ***REMOVED***> c.ending !***REMOVED******REMOVED*** 0 /* Minimal */ && pattern ***REMOVED******REMOVED******REMOVED*** c.value) || some(candidates, (c) ***REMOVED***> c.ending ***REMOVED******REMOVED******REMOVED*** 0 /* Minimal */ && pattern ***REMOVED******REMOVED******REMOVED*** c.value && validateEnding(c))) {
        return key;
      }
    }
  }
  function validateEnding({ ending, value }) {
    return ending !***REMOVED******REMOVED*** 0 /* Minimal */ || value ***REMOVED******REMOVED******REMOVED*** processEnding(relativeToBaseUrl, [ending], compilerOptions, host);
  }
}
function tryGetModuleNameFromExportsOrImports(options, host, targetFilePath, packageDirectory, packageName, exports2, conditions, mode, isImports, preferTsExtension) {
  if (typeof exports2 ***REMOVED******REMOVED******REMOVED*** "string") {
    const ignoreCase ***REMOVED*** !hostUsesCaseSensitiveFileNames(host);
    const getCommonSourceDirectory2 ***REMOVED*** () ***REMOVED***> host.getCommonSourceDirectory();
    const outputFile ***REMOVED*** isImports && getOutputJSFileNameWorker(targetFilePath, options, ignoreCase, getCommonSourceDirectory2);
    const declarationFile ***REMOVED*** isImports && getOutputDeclarationFileNameWorker(targetFilePath, options, ignoreCase, getCommonSourceDirectory2);
    const pathOrPattern ***REMOVED*** getNormalizedAbsolutePath(
      combinePaths(packageDirectory, exports2),
      /*currentDirectory*/
      void 0
    );
    const extensionSwappedTarget ***REMOVED*** hasTSFileExtension(targetFilePath) ? removeFileExtension(targetFilePath) + tryGetJSExtensionForFile(targetFilePath, options) : void 0;
    const canTryTsExtension ***REMOVED*** preferTsExtension && hasImplementationTSFileExtension(targetFilePath);
    switch (mode) {
      case 0 /* Exact */:
        if (extensionSwappedTarget && comparePaths(extensionSwappedTarget, pathOrPattern, ignoreCase) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ || comparePaths(targetFilePath, pathOrPattern, ignoreCase) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ || outputFile && comparePaths(outputFile, pathOrPattern, ignoreCase) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ || declarationFile && comparePaths(declarationFile, pathOrPattern, ignoreCase) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */) {
          return { moduleFileToTry: packageName };
        }
        break;
      case 1 /* Directory */:
        if (canTryTsExtension && containsPath(targetFilePath, pathOrPattern, ignoreCase)) {
          const fragment ***REMOVED*** getRelativePathFromDirectory(
            pathOrPattern,
            targetFilePath,
            /*ignoreCase*/
            false
          );
          return { moduleFileToTry: getNormalizedAbsolutePath(
            combinePaths(combinePaths(packageName, exports2), fragment),
            /*currentDirectory*/
            void 0
          ) };
        }
        if (extensionSwappedTarget && containsPath(pathOrPattern, extensionSwappedTarget, ignoreCase)) {
          const fragment ***REMOVED*** getRelativePathFromDirectory(
            pathOrPattern,
            extensionSwappedTarget,
            /*ignoreCase*/
            false
          );
          return { moduleFileToTry: getNormalizedAbsolutePath(
            combinePaths(combinePaths(packageName, exports2), fragment),
            /*currentDirectory*/
            void 0
          ) };
        }
        if (!canTryTsExtension && containsPath(pathOrPattern, targetFilePath, ignoreCase)) {
          const fragment ***REMOVED*** getRelativePathFromDirectory(
            pathOrPattern,
            targetFilePath,
            /*ignoreCase*/
            false
          );
          return { moduleFileToTry: getNormalizedAbsolutePath(
            combinePaths(combinePaths(packageName, exports2), fragment),
            /*currentDirectory*/
            void 0
          ) };
        }
        if (outputFile && containsPath(pathOrPattern, outputFile, ignoreCase)) {
          const fragment ***REMOVED*** getRelativePathFromDirectory(
            pathOrPattern,
            outputFile,
            /*ignoreCase*/
            false
          );
          return { moduleFileToTry: combinePaths(packageName, fragment) };
        }
        if (declarationFile && containsPath(pathOrPattern, declarationFile, ignoreCase)) {
          const fragment ***REMOVED*** changeFullExtension(getRelativePathFromDirectory(
            pathOrPattern,
            declarationFile,
            /*ignoreCase*/
            false
          ), getJSExtensionForFile(declarationFile, options));
          return { moduleFileToTry: combinePaths(packageName, fragment) };
        }
        break;
      case 2 /* Pattern */:
        const starPos ***REMOVED*** pathOrPattern.indexOf("*");
        const leadingSlice ***REMOVED*** pathOrPattern.slice(0, starPos);
        const trailingSlice ***REMOVED*** pathOrPattern.slice(starPos + 1);
        if (canTryTsExtension && startsWith(targetFilePath, leadingSlice, ignoreCase) && endsWith(targetFilePath, trailingSlice, ignoreCase)) {
          const starReplacement ***REMOVED*** targetFilePath.slice(leadingSlice.length, targetFilePath.length - trailingSlice.length);
          return { moduleFileToTry: replaceFirstStar(packageName, starReplacement) };
        }
        if (extensionSwappedTarget && startsWith(extensionSwappedTarget, leadingSlice, ignoreCase) && endsWith(extensionSwappedTarget, trailingSlice, ignoreCase)) {
          const starReplacement ***REMOVED*** extensionSwappedTarget.slice(leadingSlice.length, extensionSwappedTarget.length - trailingSlice.length);
          return { moduleFileToTry: replaceFirstStar(packageName, starReplacement) };
        }
        if (!canTryTsExtension && startsWith(targetFilePath, leadingSlice, ignoreCase) && endsWith(targetFilePath, trailingSlice, ignoreCase)) {
          const starReplacement ***REMOVED*** targetFilePath.slice(leadingSlice.length, targetFilePath.length - trailingSlice.length);
          return { moduleFileToTry: replaceFirstStar(packageName, starReplacement) };
        }
        if (outputFile && startsWith(outputFile, leadingSlice, ignoreCase) && endsWith(outputFile, trailingSlice, ignoreCase)) {
          const starReplacement ***REMOVED*** outputFile.slice(leadingSlice.length, outputFile.length - trailingSlice.length);
          return { moduleFileToTry: replaceFirstStar(packageName, starReplacement) };
        }
        if (declarationFile && startsWith(declarationFile, leadingSlice, ignoreCase) && endsWith(declarationFile, trailingSlice, ignoreCase)) {
          const starReplacement ***REMOVED*** declarationFile.slice(leadingSlice.length, declarationFile.length - trailingSlice.length);
          const substituted ***REMOVED*** replaceFirstStar(packageName, starReplacement);
          const jsExtension ***REMOVED*** tryGetJSExtensionForFile(declarationFile, options);
          return jsExtension ? { moduleFileToTry: changeFullExtension(substituted, jsExtension) } : void 0;
        }
        break;
    }
  } else if (Array.isArray(exports2)) {
    return forEach(exports2, (e) ***REMOVED***> tryGetModuleNameFromExportsOrImports(options, host, targetFilePath, packageDirectory, packageName, e, conditions, mode, isImports, preferTsExtension));
  } else if (typeof exports2 ***REMOVED******REMOVED******REMOVED*** "object" && exports2 !***REMOVED******REMOVED*** null) {
    for (const key of getOwnKeys(exports2)) {
      if (key ***REMOVED******REMOVED******REMOVED*** "default" || conditions.indexOf(key) >***REMOVED*** 0 || isApplicableVersionedTypesKey(conditions, key)) {
        const subTarget ***REMOVED*** exports2[key];
        const result ***REMOVED*** tryGetModuleNameFromExportsOrImports(options, host, targetFilePath, packageDirectory, packageName, subTarget, conditions, mode, isImports, preferTsExtension);
        if (result) {
          return result;
        }
      }
    }
  }
  return void 0;
}
function tryGetModuleNameFromExports(options, host, targetFilePath, packageDirectory, packageName, exports2, conditions) {
  if (typeof exports2 ***REMOVED******REMOVED******REMOVED*** "object" && exports2 !***REMOVED******REMOVED*** null && !Array.isArray(exports2) && allKeysStartWithDot(exports2)) {
    return forEach(getOwnKeys(exports2), (k) ***REMOVED***> {
      const subPackageName ***REMOVED*** getNormalizedAbsolutePath(
        combinePaths(packageName, k),
        /*currentDirectory*/
        void 0
      );
      const mode ***REMOVED*** endsWith(k, "/") ? 1 /* Directory */ : k.includes("*") ? 2 /* Pattern */ : 0 /* Exact */;
      return tryGetModuleNameFromExportsOrImports(
        options,
        host,
        targetFilePath,
        packageDirectory,
        subPackageName,
        exports2[k],
        conditions,
        mode,
        /*isImports*/
        false,
        /*preferTsExtension*/
        false
      );
    });
  }
  return tryGetModuleNameFromExportsOrImports(
    options,
    host,
    targetFilePath,
    packageDirectory,
    packageName,
    exports2,
    conditions,
    0 /* Exact */,
    /*isImports*/
    false,
    /*preferTsExtension*/
    false
  );
}
function tryGetModuleNameFromPackageJsonImports(moduleFileName, sourceDirectory, options, host, importMode, preferTsExtension) {
  var _a, _b, _c;
  if (!host.readFile || !getResolvePackageJsonImports(options)) {
    return void 0;
  }
  const ancestorDirectoryWithPackageJson ***REMOVED*** getNearestAncestorDirectoryWithPackageJson(host, sourceDirectory);
  if (!ancestorDirectoryWithPackageJson) {
    return void 0;
  }
  const packageJsonPath ***REMOVED*** combinePaths(ancestorDirectoryWithPackageJson, "package.json");
  const cachedPackageJson ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** host.getPackageJsonInfoCache) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host)) ***REMOVED******REMOVED*** null ? void 0 : _b.getPackageJsonInfo(packageJsonPath);
  if (isMissingPackageJsonInfo(cachedPackageJson) || !host.fileExists(packageJsonPath)) {
    return void 0;
  }
  const packageJsonContent ***REMOVED*** (cachedPackageJson ***REMOVED******REMOVED*** null ? void 0 : cachedPackageJson.contents.packageJsonContent) || tryParseJson(host.readFile(packageJsonPath));
  const imports ***REMOVED*** packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.imports;
  if (!imports) {
    return void 0;
  }
  const conditions ***REMOVED*** getConditions(options, importMode);
  return (_c ***REMOVED*** forEach(getOwnKeys(imports), (k) ***REMOVED***> {
    if (!startsWith(k, "#") || k ***REMOVED******REMOVED******REMOVED*** "#" || startsWith(k, "#/")) return void 0;
    const mode ***REMOVED*** endsWith(k, "/") ? 1 /* Directory */ : k.includes("*") ? 2 /* Pattern */ : 0 /* Exact */;
    return tryGetModuleNameFromExportsOrImports(
      options,
      host,
      moduleFileName,
      ancestorDirectoryWithPackageJson,
      k,
      imports[k],
      conditions,
      mode,
      /*isImports*/
      true,
      preferTsExtension
    );
  })) ***REMOVED******REMOVED*** null ? void 0 : _c.moduleFileToTry;
}
function tryGetModuleNameFromRootDirs(rootDirs, moduleFileName, sourceDirectory, getCanonicalFileName, allowedEndings, compilerOptions) {
  const normalizedTargetPaths ***REMOVED*** getPathsRelativeToRootDirs(moduleFileName, rootDirs, getCanonicalFileName);
  if (normalizedTargetPaths ***REMOVED******REMOVED******REMOVED*** void 0) {
    return void 0;
  }
  const normalizedSourcePaths ***REMOVED*** getPathsRelativeToRootDirs(sourceDirectory, rootDirs, getCanonicalFileName);
  const relativePaths ***REMOVED*** flatMap(normalizedSourcePaths, (sourcePath) ***REMOVED***> {
    return map(normalizedTargetPaths, (targetPath) ***REMOVED***> ensurePathIsNonModuleName(getRelativePathFromDirectory(sourcePath, targetPath, getCanonicalFileName)));
  });
  const shortest ***REMOVED*** min(relativePaths, compareNumberOfDirectorySeparators);
  if (!shortest) {
    return void 0;
  }
  return processEnding(shortest, allowedEndings, compilerOptions);
}
function tryGetModuleNameAsNodeModule({ path, isRedirect }, { getCanonicalFileName, canonicalSourceDirectory }, importingSourceFile, host, options, userPreferences, packageNameOnly, overrideMode) {
  if (!host.fileExists || !host.readFile) {
    return void 0;
  }
  const parts ***REMOVED*** getNodeModulePathParts(path);
  if (!parts) {
    return void 0;
  }
  const preferences ***REMOVED*** getModuleSpecifierPreferences(userPreferences, host, options, importingSourceFile);
  const allowedEndings ***REMOVED*** preferences.getAllowedEndingsInPreferredOrder();
  let moduleSpecifier ***REMOVED*** path;
  let isPackageRootPath ***REMOVED*** false;
  if (!packageNameOnly) {
    let packageRootIndex ***REMOVED*** parts.packageRootIndex;
    let moduleFileName;
    while (true) {
      const { moduleFileToTry, packageRootPath, blockedByExports, verbatimFromExports } ***REMOVED*** tryDirectoryWithPackageJson(packageRootIndex);
      if (getEmitModuleResolutionKind(options) !***REMOVED******REMOVED*** 1 /* Classic */) {
        if (blockedByExports) {
          return void 0;
        }
        if (verbatimFromExports) {
          return moduleFileToTry;
        }
      }
      if (packageRootPath) {
        moduleSpecifier ***REMOVED*** packageRootPath;
        isPackageRootPath ***REMOVED*** true;
        break;
      }
      if (!moduleFileName) moduleFileName ***REMOVED*** moduleFileToTry;
      packageRootIndex ***REMOVED*** path.indexOf(directorySeparator, packageRootIndex + 1);
      if (packageRootIndex ***REMOVED******REMOVED******REMOVED*** -1) {
        moduleSpecifier ***REMOVED*** processEnding(moduleFileName, allowedEndings, options, host);
        break;
      }
    }
  }
  if (isRedirect && !isPackageRootPath) {
    return void 0;
  }
  const globalTypingsCacheLocation ***REMOVED*** host.getGlobalTypingsCacheLocation && host.getGlobalTypingsCacheLocation();
  const pathToTopLevelNodeModules ***REMOVED*** getCanonicalFileName(moduleSpecifier.substring(0, parts.topLevelNodeModulesIndex));
  if (!(startsWith(canonicalSourceDirectory, pathToTopLevelNodeModules) || globalTypingsCacheLocation && startsWith(getCanonicalFileName(globalTypingsCacheLocation), pathToTopLevelNodeModules))) {
    return void 0;
  }
  const nodeModulesDirectoryName ***REMOVED*** moduleSpecifier.substring(parts.topLevelPackageNameIndex + 1);
  const packageName ***REMOVED*** getPackageNameFromTypesPackageName(nodeModulesDirectoryName);
  return getEmitModuleResolutionKind(options) ***REMOVED******REMOVED******REMOVED*** 1 /* Classic */ && packageName ***REMOVED******REMOVED******REMOVED*** nodeModulesDirectoryName ? void 0 : packageName;
  function tryDirectoryWithPackageJson(packageRootIndex) {
    var _a, _b;
    const packageRootPath ***REMOVED*** path.substring(0, packageRootIndex);
    const packageJsonPath ***REMOVED*** combinePaths(packageRootPath, "package.json");
    let moduleFileToTry ***REMOVED*** path;
    let maybeBlockedByTypesVersions ***REMOVED*** false;
    const cachedPackageJson ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** host.getPackageJsonInfoCache) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host)) ***REMOVED******REMOVED*** null ? void 0 : _b.getPackageJsonInfo(packageJsonPath);
    if (isPackageJsonInfo(cachedPackageJson) || cachedPackageJson ***REMOVED******REMOVED******REMOVED*** void 0 && host.fileExists(packageJsonPath)) {
      const packageJsonContent ***REMOVED*** (cachedPackageJson ***REMOVED******REMOVED*** null ? void 0 : cachedPackageJson.contents.packageJsonContent) || tryParseJson(host.readFile(packageJsonPath));
      const importMode ***REMOVED*** overrideMode || getDefaultResolutionModeForFile(importingSourceFile, host, options);
      if (getResolvePackageJsonExports(options)) {
        const nodeModulesDirectoryName2 ***REMOVED*** packageRootPath.substring(parts.topLevelPackageNameIndex + 1);
        const packageName2 ***REMOVED*** getPackageNameFromTypesPackageName(nodeModulesDirectoryName2);
        const conditions ***REMOVED*** getConditions(options, importMode);
        const fromExports ***REMOVED*** (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.exports) ? tryGetModuleNameFromExports(
          options,
          host,
          path,
          packageRootPath,
          packageName2,
          packageJsonContent.exports,
          conditions
        ) : void 0;
        if (fromExports) {
          return { ...fromExports, verbatimFromExports: true };
        }
        if (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.exports) {
          return { moduleFileToTry: path, blockedByExports: true };
        }
      }
      const versionPaths ***REMOVED*** (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.typesVersions) ? getPackageJsonTypesVersionsPaths(packageJsonContent.typesVersions) : void 0;
      if (versionPaths) {
        const subModuleName ***REMOVED*** path.slice(packageRootPath.length + 1);
        const fromPaths ***REMOVED*** tryGetModuleNameFromPaths(
          subModuleName,
          versionPaths.paths,
          allowedEndings,
          packageRootPath,
          getCanonicalFileName,
          host,
          options
        );
        if (fromPaths ***REMOVED******REMOVED******REMOVED*** void 0) {
          maybeBlockedByTypesVersions ***REMOVED*** true;
        } else {
          moduleFileToTry ***REMOVED*** combinePaths(packageRootPath, fromPaths);
        }
      }
      const mainFileRelative ***REMOVED*** (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.typings) || (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.types) || (packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.main) || "index.js";
      if (isString(mainFileRelative) && !(maybeBlockedByTypesVersions && matchPatternOrExact(tryParsePatterns(versionPaths.paths), mainFileRelative))) {
        const mainExportFile ***REMOVED*** toPath(mainFileRelative, packageRootPath, getCanonicalFileName);
        const canonicalModuleFileToTry ***REMOVED*** getCanonicalFileName(moduleFileToTry);
        if (removeFileExtension(mainExportFile) ***REMOVED******REMOVED******REMOVED*** removeFileExtension(canonicalModuleFileToTry)) {
          return { packageRootPath, moduleFileToTry };
        } else if ((packageJsonContent ***REMOVED******REMOVED*** null ? void 0 : packageJsonContent.type) !***REMOVED******REMOVED*** "module" && !fileExtensionIsOneOf(canonicalModuleFileToTry, extensionsNotSupportingExtensionlessResolution) && startsWith(canonicalModuleFileToTry, mainExportFile) && getDirectoryPath(canonicalModuleFileToTry) ***REMOVED******REMOVED******REMOVED*** removeTrailingDirectorySeparator(mainExportFile) && removeFileExtension(getBaseFileName(canonicalModuleFileToTry)) ***REMOVED******REMOVED******REMOVED*** "index") {
          return { packageRootPath, moduleFileToTry };
        }
      }
    } else {
      const fileName ***REMOVED*** getCanonicalFileName(moduleFileToTry.substring(parts.packageRootIndex + 1));
      if (fileName ***REMOVED******REMOVED******REMOVED*** "index.d.ts" || fileName ***REMOVED******REMOVED******REMOVED*** "index.js" || fileName ***REMOVED******REMOVED******REMOVED*** "index.ts" || fileName ***REMOVED******REMOVED******REMOVED*** "index.tsx") {
        return { moduleFileToTry, packageRootPath };
      }
    }
    return { moduleFileToTry };
  }
}
function tryGetAnyFileFromPath(host, path) {
  if (!host.fileExists) return;
  const extensions ***REMOVED*** flatten(getSupportedExtensions({ allowJs: true }, [{ extension: "node", isMixedContent: false }, { extension: "json", isMixedContent: false, scriptKind: 6 /* JSON */ }]));
  for (const e of extensions) {
    const fullPath ***REMOVED*** path + e;
    if (host.fileExists(fullPath)) {
      return fullPath;
    }
  }
}
function getPathsRelativeToRootDirs(path, rootDirs, getCanonicalFileName) {
  return mapDefined(rootDirs, (rootDir) ***REMOVED***> {
    const relativePath ***REMOVED*** getRelativePathIfInSameVolume(path, rootDir, getCanonicalFileName);
    return relativePath !***REMOVED******REMOVED*** void 0 && isPathRelativeToParent(relativePath) ? void 0 : relativePath;
  });
}
function processEnding(fileName, allowedEndings, options, host) {
  if (fileExtensionIsOneOf(fileName, [".json" /* Json */, ".mjs" /* Mjs */, ".cjs" /* Cjs */])) {
    return fileName;
  }
  const noExtension ***REMOVED*** removeFileExtension(fileName);
  if (fileName ***REMOVED******REMOVED******REMOVED*** noExtension) {
    return fileName;
  }
  const jsPriority ***REMOVED*** allowedEndings.indexOf(2 /* JsExtension */);
  const tsPriority ***REMOVED*** allowedEndings.indexOf(3 /* TsExtension */);
  if (fileExtensionIsOneOf(fileName, [".mts" /* Mts */, ".cts" /* Cts */]) && tsPriority !***REMOVED******REMOVED*** -1 && tsPriority < jsPriority) {
    return fileName;
  } else if (fileExtensionIsOneOf(fileName, [".d.mts" /* Dmts */, ".mts" /* Mts */, ".d.cts" /* Dcts */, ".cts" /* Cts */])) {
    return noExtension + getJSExtensionForFile(fileName, options);
  } else if (!fileExtensionIsOneOf(fileName, [".d.ts" /* Dts */]) && fileExtensionIsOneOf(fileName, [".ts" /* Ts */]) && fileName.includes(".d.")) {
    return tryGetRealFileNameForNonJsDeclarationFileName(fileName);
  }
  switch (allowedEndings[0]) {
    case 0 /* Minimal */:
      const withoutIndex ***REMOVED*** removeSuffix(noExtension, "/index");
      if (host && withoutIndex !***REMOVED******REMOVED*** noExtension && tryGetAnyFileFromPath(host, withoutIndex)) {
        return noExtension;
      }
      return withoutIndex;
    case 1 /* Index */:
      return noExtension;
    case 2 /* JsExtension */:
      return noExtension + getJSExtensionForFile(fileName, options);
    case 3 /* TsExtension */:
      if (isDeclarationFileName(fileName)) {
        const extensionlessPriority ***REMOVED*** allowedEndings.findIndex((e) ***REMOVED***> e ***REMOVED******REMOVED******REMOVED*** 0 /* Minimal */ || e ***REMOVED******REMOVED******REMOVED*** 1 /* Index */);
        return extensionlessPriority !***REMOVED******REMOVED*** -1 && extensionlessPriority < jsPriority ? noExtension : noExtension + getJSExtensionForFile(fileName, options);
      }
      return fileName;
    default:
      return Debug.assertNever(allowedEndings[0]);
  }
}
function tryGetRealFileNameForNonJsDeclarationFileName(fileName) {
  const baseName ***REMOVED*** getBaseFileName(fileName);
  if (!endsWith(fileName, ".ts" /* Ts */) || !baseName.includes(".d.") || fileExtensionIsOneOf(baseName, [".d.ts" /* Dts */])) return void 0;
  const noExtension ***REMOVED*** removeExtension(fileName, ".ts" /* Ts */);
  const ext ***REMOVED*** noExtension.substring(noExtension.lastIndexOf("."));
  return noExtension.substring(0, noExtension.indexOf(".d.")) + ext;
}
function getJSExtensionForFile(fileName, options) {
  return tryGetJSExtensionForFile(fileName, options) ?? Debug.fail(`Extension ${extensionFromPath(fileName)} is unsupported:: FileName:: ${fileName}`);
}
function tryGetJSExtensionForFile(fileName, options) {
  const ext ***REMOVED*** tryGetExtensionFromPath2(fileName);
  switch (ext) {
    case ".ts" /* Ts */:
    case ".d.ts" /* Dts */:
      return ".js" /* Js */;
    case ".tsx" /* Tsx */:
      return options.jsx ***REMOVED******REMOVED******REMOVED*** 1 /* Preserve */ ? ".jsx" /* Jsx */ : ".js" /* Js */;
    case ".js" /* Js */:
    case ".jsx" /* Jsx */:
    case ".json" /* Json */:
      return ext;
    case ".d.mts" /* Dmts */:
    case ".mts" /* Mts */:
    case ".mjs" /* Mjs */:
      return ".mjs" /* Mjs */;
    case ".d.cts" /* Dcts */:
    case ".cts" /* Cts */:
    case ".cjs" /* Cjs */:
      return ".cjs" /* Cjs */;
    default:
      return void 0;
  }
}
function getRelativePathIfInSameVolume(path, directoryPath, getCanonicalFileName) {
  const relativePath ***REMOVED*** getRelativePathToDirectoryOrUrl(
    directoryPath,
    path,
    directoryPath,
    getCanonicalFileName,
    /*isAbsolutePathAnUrl*/
    false
  );
  return isRootedDiskPath(relativePath) ? void 0 : relativePath;
}
function isPathRelativeToParent(path) {
  return startsWith(path, "..");
}
function getDefaultResolutionModeForFile(file, host, compilerOptions) {
  return isFullSourceFile(file) ? host.getDefaultResolutionModeForFile(file) : getDefaultResolutionModeForFileWorker(file, compilerOptions);
}
function prefersTsExtension(allowedEndings) {
  const tsPriority ***REMOVED*** allowedEndings.indexOf(3 /* TsExtension */);
  return tsPriority > -1 && tsPriority < allowedEndings.indexOf(2 /* JsExtension */);
}

// src/compiler/checker.ts
var ambientModuleSymbolRegex ***REMOVED*** /^".+"$/;
var anon ***REMOVED*** "(anonymous)";
var nextSymbolId ***REMOVED*** 1;
var nextNodeId ***REMOVED*** 1;
var nextMergeId ***REMOVED*** 1;
var nextFlowId ***REMOVED*** 1;
var TypeFacts ***REMOVED*** /* @__PURE__ */ ((TypeFacts3) ***REMOVED***> {
  TypeFacts3[TypeFacts3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  TypeFacts3[TypeFacts3["TypeofEQString"] ***REMOVED*** 1] ***REMOVED*** "TypeofEQString";
  TypeFacts3[TypeFacts3["TypeofEQNumber"] ***REMOVED*** 2] ***REMOVED*** "TypeofEQNumber";
  TypeFacts3[TypeFacts3["TypeofEQBigInt"] ***REMOVED*** 4] ***REMOVED*** "TypeofEQBigInt";
  TypeFacts3[TypeFacts3["TypeofEQBoolean"] ***REMOVED*** 8] ***REMOVED*** "TypeofEQBoolean";
  TypeFacts3[TypeFacts3["TypeofEQSymbol"] ***REMOVED*** 16] ***REMOVED*** "TypeofEQSymbol";
  TypeFacts3[TypeFacts3["TypeofEQObject"] ***REMOVED*** 32] ***REMOVED*** "TypeofEQObject";
  TypeFacts3[TypeFacts3["TypeofEQFunction"] ***REMOVED*** 64] ***REMOVED*** "TypeofEQFunction";
  TypeFacts3[TypeFacts3["TypeofEQHostObject"] ***REMOVED*** 128] ***REMOVED*** "TypeofEQHostObject";
  TypeFacts3[TypeFacts3["TypeofNEString"] ***REMOVED*** 256] ***REMOVED*** "TypeofNEString";
  TypeFacts3[TypeFacts3["TypeofNENumber"] ***REMOVED*** 512] ***REMOVED*** "TypeofNENumber";
  TypeFacts3[TypeFacts3["TypeofNEBigInt"] ***REMOVED*** 1024] ***REMOVED*** "TypeofNEBigInt";
  TypeFacts3[TypeFacts3["TypeofNEBoolean"] ***REMOVED*** 2048] ***REMOVED*** "TypeofNEBoolean";
  TypeFacts3[TypeFacts3["TypeofNESymbol"] ***REMOVED*** 4096] ***REMOVED*** "TypeofNESymbol";
  TypeFacts3[TypeFacts3["TypeofNEObject"] ***REMOVED*** 8192] ***REMOVED*** "TypeofNEObject";
  TypeFacts3[TypeFacts3["TypeofNEFunction"] ***REMOVED*** 16384] ***REMOVED*** "TypeofNEFunction";
  TypeFacts3[TypeFacts3["TypeofNEHostObject"] ***REMOVED*** 32768] ***REMOVED*** "TypeofNEHostObject";
  TypeFacts3[TypeFacts3["EQUndefined"] ***REMOVED*** 65536] ***REMOVED*** "EQUndefined";
  TypeFacts3[TypeFacts3["EQNull"] ***REMOVED*** 131072] ***REMOVED*** "EQNull";
  TypeFacts3[TypeFacts3["EQUndefinedOrNull"] ***REMOVED*** 262144] ***REMOVED*** "EQUndefinedOrNull";
  TypeFacts3[TypeFacts3["NEUndefined"] ***REMOVED*** 524288] ***REMOVED*** "NEUndefined";
  TypeFacts3[TypeFacts3["NENull"] ***REMOVED*** 1048576] ***REMOVED*** "NENull";
  TypeFacts3[TypeFacts3["NEUndefinedOrNull"] ***REMOVED*** 2097152] ***REMOVED*** "NEUndefinedOrNull";
  TypeFacts3[TypeFacts3["Truthy"] ***REMOVED*** 4194304] ***REMOVED*** "Truthy";
  TypeFacts3[TypeFacts3["Falsy"] ***REMOVED*** 8388608] ***REMOVED*** "Falsy";
  TypeFacts3[TypeFacts3["IsUndefined"] ***REMOVED*** 16777216] ***REMOVED*** "IsUndefined";
  TypeFacts3[TypeFacts3["IsNull"] ***REMOVED*** 33554432] ***REMOVED*** "IsNull";
  TypeFacts3[TypeFacts3["IsUndefinedOrNull"] ***REMOVED*** 50331648] ***REMOVED*** "IsUndefinedOrNull";
  TypeFacts3[TypeFacts3["All"] ***REMOVED*** 134217727] ***REMOVED*** "All";
  TypeFacts3[TypeFacts3["BaseStringStrictFacts"] ***REMOVED*** 3735041] ***REMOVED*** "BaseStringStrictFacts";
  TypeFacts3[TypeFacts3["BaseStringFacts"] ***REMOVED*** 12582401] ***REMOVED*** "BaseStringFacts";
  TypeFacts3[TypeFacts3["StringStrictFacts"] ***REMOVED*** 16317953] ***REMOVED*** "StringStrictFacts";
  TypeFacts3[TypeFacts3["StringFacts"] ***REMOVED*** 16776705] ***REMOVED*** "StringFacts";
  TypeFacts3[TypeFacts3["EmptyStringStrictFacts"] ***REMOVED*** 12123649] ***REMOVED*** "EmptyStringStrictFacts";
  TypeFacts3[TypeFacts3["EmptyStringFacts"] ***REMOVED*** 12582401 /* BaseStringFacts */] ***REMOVED*** "EmptyStringFacts";
  TypeFacts3[TypeFacts3["NonEmptyStringStrictFacts"] ***REMOVED*** 7929345] ***REMOVED*** "NonEmptyStringStrictFacts";
  TypeFacts3[TypeFacts3["NonEmptyStringFacts"] ***REMOVED*** 16776705] ***REMOVED*** "NonEmptyStringFacts";
  TypeFacts3[TypeFacts3["BaseNumberStrictFacts"] ***REMOVED*** 3734786] ***REMOVED*** "BaseNumberStrictFacts";
  TypeFacts3[TypeFacts3["BaseNumberFacts"] ***REMOVED*** 12582146] ***REMOVED*** "BaseNumberFacts";
  TypeFacts3[TypeFacts3["NumberStrictFacts"] ***REMOVED*** 16317698] ***REMOVED*** "NumberStrictFacts";
  TypeFacts3[TypeFacts3["NumberFacts"] ***REMOVED*** 16776450] ***REMOVED*** "NumberFacts";
  TypeFacts3[TypeFacts3["ZeroNumberStrictFacts"] ***REMOVED*** 12123394] ***REMOVED*** "ZeroNumberStrictFacts";
  TypeFacts3[TypeFacts3["ZeroNumberFacts"] ***REMOVED*** 12582146 /* BaseNumberFacts */] ***REMOVED*** "ZeroNumberFacts";
  TypeFacts3[TypeFacts3["NonZeroNumberStrictFacts"] ***REMOVED*** 7929090] ***REMOVED*** "NonZeroNumberStrictFacts";
  TypeFacts3[TypeFacts3["NonZeroNumberFacts"] ***REMOVED*** 16776450] ***REMOVED*** "NonZeroNumberFacts";
  TypeFacts3[TypeFacts3["BaseBigIntStrictFacts"] ***REMOVED*** 3734276] ***REMOVED*** "BaseBigIntStrictFacts";
  TypeFacts3[TypeFacts3["BaseBigIntFacts"] ***REMOVED*** 12581636] ***REMOVED*** "BaseBigIntFacts";
  TypeFacts3[TypeFacts3["BigIntStrictFacts"] ***REMOVED*** 16317188] ***REMOVED*** "BigIntStrictFacts";
  TypeFacts3[TypeFacts3["BigIntFacts"] ***REMOVED*** 16775940] ***REMOVED*** "BigIntFacts";
  TypeFacts3[TypeFacts3["ZeroBigIntStrictFacts"] ***REMOVED*** 12122884] ***REMOVED*** "ZeroBigIntStrictFacts";
  TypeFacts3[TypeFacts3["ZeroBigIntFacts"] ***REMOVED*** 12581636 /* BaseBigIntFacts */] ***REMOVED*** "ZeroBigIntFacts";
  TypeFacts3[TypeFacts3["NonZeroBigIntStrictFacts"] ***REMOVED*** 7928580] ***REMOVED*** "NonZeroBigIntStrictFacts";
  TypeFacts3[TypeFacts3["NonZeroBigIntFacts"] ***REMOVED*** 16775940] ***REMOVED*** "NonZeroBigIntFacts";
  TypeFacts3[TypeFacts3["BaseBooleanStrictFacts"] ***REMOVED*** 3733256] ***REMOVED*** "BaseBooleanStrictFacts";
  TypeFacts3[TypeFacts3["BaseBooleanFacts"] ***REMOVED*** 12580616] ***REMOVED*** "BaseBooleanFacts";
  TypeFacts3[TypeFacts3["BooleanStrictFacts"] ***REMOVED*** 16316168] ***REMOVED*** "BooleanStrictFacts";
  TypeFacts3[TypeFacts3["BooleanFacts"] ***REMOVED*** 16774920] ***REMOVED*** "BooleanFacts";
  TypeFacts3[TypeFacts3["FalseStrictFacts"] ***REMOVED*** 12121864] ***REMOVED*** "FalseStrictFacts";
  TypeFacts3[TypeFacts3["FalseFacts"] ***REMOVED*** 12580616 /* BaseBooleanFacts */] ***REMOVED*** "FalseFacts";
  TypeFacts3[TypeFacts3["TrueStrictFacts"] ***REMOVED*** 7927560] ***REMOVED*** "TrueStrictFacts";
  TypeFacts3[TypeFacts3["TrueFacts"] ***REMOVED*** 16774920] ***REMOVED*** "TrueFacts";
  TypeFacts3[TypeFacts3["SymbolStrictFacts"] ***REMOVED*** 7925520] ***REMOVED*** "SymbolStrictFacts";
  TypeFacts3[TypeFacts3["SymbolFacts"] ***REMOVED*** 16772880] ***REMOVED*** "SymbolFacts";
  TypeFacts3[TypeFacts3["ObjectStrictFacts"] ***REMOVED*** 7888800] ***REMOVED*** "ObjectStrictFacts";
  TypeFacts3[TypeFacts3["ObjectFacts"] ***REMOVED*** 16736160] ***REMOVED*** "ObjectFacts";
  TypeFacts3[TypeFacts3["FunctionStrictFacts"] ***REMOVED*** 7880640] ***REMOVED*** "FunctionStrictFacts";
  TypeFacts3[TypeFacts3["FunctionFacts"] ***REMOVED*** 16728e3] ***REMOVED*** "FunctionFacts";
  TypeFacts3[TypeFacts3["VoidFacts"] ***REMOVED*** 9830144] ***REMOVED*** "VoidFacts";
  TypeFacts3[TypeFacts3["UndefinedFacts"] ***REMOVED*** 26607360] ***REMOVED*** "UndefinedFacts";
  TypeFacts3[TypeFacts3["NullFacts"] ***REMOVED*** 42917664] ***REMOVED*** "NullFacts";
  TypeFacts3[TypeFacts3["EmptyObjectStrictFacts"] ***REMOVED*** 83427327] ***REMOVED*** "EmptyObjectStrictFacts";
  TypeFacts3[TypeFacts3["EmptyObjectFacts"] ***REMOVED*** 83886079] ***REMOVED*** "EmptyObjectFacts";
  TypeFacts3[TypeFacts3["UnknownFacts"] ***REMOVED*** 83886079] ***REMOVED*** "UnknownFacts";
  TypeFacts3[TypeFacts3["AllTypeofNE"] ***REMOVED*** 556800] ***REMOVED*** "AllTypeofNE";
  TypeFacts3[TypeFacts3["OrFactsMask"] ***REMOVED*** 8256] ***REMOVED*** "OrFactsMask";
  TypeFacts3[TypeFacts3["AndFactsMask"] ***REMOVED*** 134209471] ***REMOVED*** "AndFactsMask";
  return TypeFacts3;
})(TypeFacts || {});
var typeofNEFacts ***REMOVED*** new Map(Object.entries({
  string: 256 /* TypeofNEString */,
  number: 512 /* TypeofNENumber */,
  bigint: 1024 /* TypeofNEBigInt */,
  boolean: 2048 /* TypeofNEBoolean */,
  symbol: 4096 /* TypeofNESymbol */,
  undefined: 524288 /* NEUndefined */,
  object: 8192 /* TypeofNEObject */,
  function: 16384 /* TypeofNEFunction */
}));
var CheckMode ***REMOVED*** /* @__PURE__ */ ((CheckMode3) ***REMOVED***> {
  CheckMode3[CheckMode3["Normal"] ***REMOVED*** 0] ***REMOVED*** "Normal";
  CheckMode3[CheckMode3["Contextual"] ***REMOVED*** 1] ***REMOVED*** "Contextual";
  CheckMode3[CheckMode3["Inferential"] ***REMOVED*** 2] ***REMOVED*** "Inferential";
  CheckMode3[CheckMode3["SkipContextSensitive"] ***REMOVED*** 4] ***REMOVED*** "SkipContextSensitive";
  CheckMode3[CheckMode3["SkipGenericFunctions"] ***REMOVED*** 8] ***REMOVED*** "SkipGenericFunctions";
  CheckMode3[CheckMode3["IsForSignatureHelp"] ***REMOVED*** 16] ***REMOVED*** "IsForSignatureHelp";
  CheckMode3[CheckMode3["RestBindingElement"] ***REMOVED*** 32] ***REMOVED*** "RestBindingElement";
  CheckMode3[CheckMode3["TypeOnly"] ***REMOVED*** 64] ***REMOVED*** "TypeOnly";
  return CheckMode3;
})(CheckMode || {});
var SignatureCheckMode ***REMOVED*** /* @__PURE__ */ ((SignatureCheckMode3) ***REMOVED***> {
  SignatureCheckMode3[SignatureCheckMode3["None"] ***REMOVED*** 0] ***REMOVED*** "None";
  SignatureCheckMode3[SignatureCheckMode3["BivariantCallback"] ***REMOVED*** 1] ***REMOVED*** "BivariantCallback";
  SignatureCheckMode3[SignatureCheckMode3["StrictCallback"] ***REMOVED*** 2] ***REMOVED*** "StrictCallback";
  SignatureCheckMode3[SignatureCheckMode3["IgnoreReturnTypes"] ***REMOVED*** 4] ***REMOVED*** "IgnoreReturnTypes";
  SignatureCheckMode3[SignatureCheckMode3["StrictArity"] ***REMOVED*** 8] ***REMOVED*** "StrictArity";
  SignatureCheckMode3[SignatureCheckMode3["StrictTopSignature"] ***REMOVED*** 16] ***REMOVED*** "StrictTopSignature";
  SignatureCheckMode3[SignatureCheckMode3["Callback"] ***REMOVED*** 3] ***REMOVED*** "Callback";
  return SignatureCheckMode3;
})(SignatureCheckMode || {});
var isNotOverloadAndNotAccessor ***REMOVED*** and(isNotOverload, isNotAccessor);
var intrinsicTypeKinds ***REMOVED*** new Map(Object.entries({
  Uppercase: 0 /* Uppercase */,
  Lowercase: 1 /* Lowercase */,
  Capitalize: 2 /* Capitalize */,
  Uncapitalize: 3 /* Uncapitalize */,
  NoInfer: 4 /* NoInfer */
}));
var SymbolLinks ***REMOVED*** class {
};
function NodeLinks() {
  this.flags ***REMOVED*** 0 /* None */;
}
function getNodeId(node) {
  if (!node.id) {
    node.id ***REMOVED*** nextNodeId;
    nextNodeId++;
  }
  return node.id;
}
function getSymbolId(symbol) {
  if (!symbol.id) {
    symbol.id ***REMOVED*** nextSymbolId;
    nextSymbolId++;
  }
  return symbol.id;
}
function isInstantiatedModule(node, preserveConstEnums) {
  const moduleState ***REMOVED*** getModuleInstanceState(node);
  return moduleState ***REMOVED******REMOVED******REMOVED*** 1 /* Instantiated */ || preserveConstEnums && moduleState ***REMOVED******REMOVED******REMOVED*** 2 /* ConstEnumOnly */;
}
function createTypeChecker(host) {
  var deferredDiagnosticsCallbacks ***REMOVED*** [];
  var addLazyDiagnostic ***REMOVED*** (arg) ***REMOVED***> {
    deferredDiagnosticsCallbacks.push(arg);
  };
  var cancellationToken;
  var scanner;
  var Symbol13 ***REMOVED*** objectAllocator.getSymbolConstructor();
  var Type7 ***REMOVED*** objectAllocator.getTypeConstructor();
  var Signature5 ***REMOVED*** objectAllocator.getSignatureConstructor();
  var typeCount ***REMOVED*** 0;
  var symbolCount ***REMOVED*** 0;
  var totalInstantiationCount ***REMOVED*** 0;
  var instantiationCount ***REMOVED*** 0;
  var instantiationDepth ***REMOVED*** 0;
  var inlineLevel ***REMOVED*** 0;
  var currentNode;
  var varianceTypeParameter;
  var isInferencePartiallyBlocked ***REMOVED*** false;
  var emptySymbols ***REMOVED*** createSymbolTable();
  var arrayVariances ***REMOVED*** [1 /* Covariant */];
  var compilerOptions ***REMOVED*** host.getCompilerOptions();
  var languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  var moduleKind ***REMOVED*** getEmitModuleKind(compilerOptions);
  var legacyDecorators ***REMOVED*** !!compilerOptions.experimentalDecorators;
  var useDefineForClassFields ***REMOVED*** getUseDefineForClassFields(compilerOptions);
  var emitStandardClassFields ***REMOVED*** getEmitStandardClassFields(compilerOptions);
  var allowSyntheticDefaultImports ***REMOVED*** getAllowSyntheticDefaultImports(compilerOptions);
  var strictNullChecks ***REMOVED*** getStrictOptionValue(compilerOptions, "strictNullChecks");
  var strictFunctionTypes ***REMOVED*** getStrictOptionValue(compilerOptions, "strictFunctionTypes");
  var strictBindCallApply ***REMOVED*** getStrictOptionValue(compilerOptions, "strictBindCallApply");
  var strictPropertyInitialization ***REMOVED*** getStrictOptionValue(compilerOptions, "strictPropertyInitialization");
  var strictBuiltinIteratorReturn ***REMOVED*** getStrictOptionValue(compilerOptions, "strictBuiltinIteratorReturn");
  var noImplicitAny ***REMOVED*** getStrictOptionValue(compilerOptions, "noImplicitAny");
  var noImplicitThis ***REMOVED*** getStrictOptionValue(compilerOptions, "noImplicitThis");
  var useUnknownInCatchVariables ***REMOVED*** getStrictOptionValue(compilerOptions, "useUnknownInCatchVariables");
  var exactOptionalPropertyTypes ***REMOVED*** compilerOptions.exactOptionalPropertyTypes;
  var noUncheckedSideEffectImports ***REMOVED*** !!compilerOptions.noUncheckedSideEffectImports;
  var checkBinaryExpression ***REMOVED*** createCheckBinaryExpression();
  var emitResolver ***REMOVED*** createResolver();
  var nodeBuilder ***REMOVED*** createNodeBuilder();
  var syntacticNodeBuilder ***REMOVED*** createSyntacticTypeNodeBuilder(compilerOptions, nodeBuilder.syntacticBuilderResolver);
  var evaluate ***REMOVED*** createEvaluator({
    evaluateElementAccessExpression,
    evaluateEntityNameExpression
  });
  var globals ***REMOVED*** createSymbolTable();
  var undefinedSymbol ***REMOVED*** createSymbol(4 /* Property */, "undefined");
  undefinedSymbol.declarations ***REMOVED*** [];
  var globalThisSymbol ***REMOVED*** createSymbol(1536 /* Module */, "globalThis", 8 /* Readonly */);
  globalThisSymbol.exports ***REMOVED*** globals;
  globalThisSymbol.declarations ***REMOVED*** [];
  globals.set(globalThisSymbol.escapedName, globalThisSymbol);
  var argumentsSymbol ***REMOVED*** createSymbol(4 /* Property */, "arguments");
  var requireSymbol ***REMOVED*** createSymbol(4 /* Property */, "require");
  var isolatedModulesLikeFlagName ***REMOVED*** compilerOptions.verbatimModuleSyntax ? "verbatimModuleSyntax" : "isolatedModules";
  var canCollectSymbolAliasAccessabilityData ***REMOVED*** !compilerOptions.verbatimModuleSyntax;
  var apparentArgumentCount;
  var lastGetCombinedNodeFlagsNode;
  var lastGetCombinedNodeFlagsResult ***REMOVED*** 0 /* None */;
  var lastGetCombinedModifierFlagsNode;
  var lastGetCombinedModifierFlagsResult ***REMOVED*** 0 /* None */;
  var resolveName ***REMOVED*** createNameResolver({
    compilerOptions,
    requireSymbol,
    argumentsSymbol,
    globals,
    getSymbolOfDeclaration,
    error,
    getRequiresScopeChangeCache,
    setRequiresScopeChangeCache,
    lookup: getSymbol,
    onPropertyWithInvalidInitializer: checkAndReportErrorForInvalidInitializer,
    onFailedToResolveSymbol,
    onSuccessfullyResolvedSymbol
  });
  var resolveNameForSymbolSuggestion ***REMOVED*** createNameResolver({
    compilerOptions,
    requireSymbol,
    argumentsSymbol,
    globals,
    getSymbolOfDeclaration,
    error,
    getRequiresScopeChangeCache,
    setRequiresScopeChangeCache,
    lookup: getSuggestionForSymbolNameLookup
  });
  const checker ***REMOVED*** {
    getNodeCount: () ***REMOVED***> reduceLeft(host.getSourceFiles(), (n, s) ***REMOVED***> n + s.nodeCount, 0),
    getIdentifierCount: () ***REMOVED***> reduceLeft(host.getSourceFiles(), (n, s) ***REMOVED***> n + s.identifierCount, 0),
    getSymbolCount: () ***REMOVED***> reduceLeft(host.getSourceFiles(), (n, s) ***REMOVED***> n + s.symbolCount, symbolCount),
    getTypeCount: () ***REMOVED***> typeCount,
    getInstantiationCount: () ***REMOVED***> totalInstantiationCount,
    getRelationCacheSizes: () ***REMOVED***> ({
      assignable: assignableRelation.size,
      identity: identityRelation.size,
      subtype: subtypeRelation.size,
      strictSubtype: strictSubtypeRelation.size
    }),
    isUndefinedSymbol: (symbol) ***REMOVED***> symbol ***REMOVED******REMOVED******REMOVED*** undefinedSymbol,
    isArgumentsSymbol: (symbol) ***REMOVED***> symbol ***REMOVED******REMOVED******REMOVED*** argumentsSymbol,
    isUnknownSymbol: (symbol) ***REMOVED***> symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol,
    getMergedSymbol,
    symbolIsValue,
    getDiagnostics,
    getGlobalDiagnostics,
    getRecursionIdentity,
    getUnmatchedProperties,
    getTypeOfSymbolAtLocation: (symbol, locationIn) ***REMOVED***> {
      const location ***REMOVED*** getParseTreeNode(locationIn);
      return location ? getTypeOfSymbolAtLocation(symbol, location) : errorType;
    },
    getTypeOfSymbol,
    getSymbolsOfParameterPropertyDeclaration: (parameterIn, parameterName) ***REMOVED***> {
      const parameter ***REMOVED*** getParseTreeNode(parameterIn, isParameter);
      if (parameter ***REMOVED******REMOVED******REMOVED*** void 0) return Debug.fail("Cannot get symbols of a synthetic parameter that cannot be resolved to a parse-tree node.");
      Debug.assert(isParameterPropertyDeclaration(parameter, parameter.parent));
      return getSymbolsOfParameterPropertyDeclaration(parameter, escapeLeadingUnderscores(parameterName));
    },
    getDeclaredTypeOfSymbol,
    getPropertiesOfType,
    getPropertyOfType: (type, name) ***REMOVED***> getPropertyOfType(type, escapeLeadingUnderscores(name)),
    getPrivateIdentifierPropertyOfType: (leftType, name, location) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(location);
      if (!node) {
        return void 0;
      }
      const propName ***REMOVED*** escapeLeadingUnderscores(name);
      const lexicallyScopedIdentifier ***REMOVED*** lookupSymbolForPrivateIdentifierDeclaration(propName, node);
      return lexicallyScopedIdentifier ? getPrivateIdentifierPropertyOfType(leftType, lexicallyScopedIdentifier) : void 0;
    },
    getTypeOfPropertyOfType: (type, name) ***REMOVED***> getTypeOfPropertyOfType(type, escapeLeadingUnderscores(name)),
    getIndexInfoOfType: (type, kind) ***REMOVED***> getIndexInfoOfType(type, kind ***REMOVED******REMOVED******REMOVED*** 0 /* String */ ? stringType : numberType),
    getIndexInfosOfType,
    getIndexInfosOfIndexSymbol,
    getSignaturesOfType,
    getIndexTypeOfType: (type, kind) ***REMOVED***> getIndexTypeOfType(type, kind ***REMOVED******REMOVED******REMOVED*** 0 /* String */ ? stringType : numberType),
    getIndexType: (type) ***REMOVED***> getIndexType(type),
    getBaseTypes,
    getBaseTypeOfLiteralType,
    getWidenedType,
    getWidenedLiteralType,
    fillMissingTypeArguments,
    getTypeFromTypeNode: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isTypeNode);
      return node ? getTypeFromTypeNode(node) : errorType;
    },
    getParameterType: getTypeAtPosition,
    getParameterIdentifierInfoAtPosition,
    getPromisedTypeOfPromise,
    getAwaitedType: (type) ***REMOVED***> getAwaitedType(type),
    getReturnTypeOfSignature,
    isNullableType,
    getNullableType,
    getNonNullableType,
    getNonOptionalType: removeOptionalTypeMarker,
    getTypeArguments,
    typeToTypeNode: nodeBuilder.typeToTypeNode,
    typePredicateToTypePredicateNode: nodeBuilder.typePredicateToTypePredicateNode,
    indexInfoToIndexSignatureDeclaration: nodeBuilder.indexInfoToIndexSignatureDeclaration,
    signatureToSignatureDeclaration: nodeBuilder.signatureToSignatureDeclaration,
    symbolToEntityName: nodeBuilder.symbolToEntityName,
    symbolToExpression: nodeBuilder.symbolToExpression,
    symbolToNode: nodeBuilder.symbolToNode,
    symbolToTypeParameterDeclarations: nodeBuilder.symbolToTypeParameterDeclarations,
    symbolToParameterDeclaration: nodeBuilder.symbolToParameterDeclaration,
    typeParameterToDeclaration: nodeBuilder.typeParameterToDeclaration,
    getSymbolsInScope: (locationIn, meaning) ***REMOVED***> {
      const location ***REMOVED*** getParseTreeNode(locationIn);
      return location ? getSymbolsInScope(location, meaning) : [];
    },
    getSymbolAtLocation: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn);
      return node ? getSymbolAtLocation(
        node,
        /*ignoreErrors*/
        true
      ) : void 0;
    },
    getIndexInfosAtLocation: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn);
      return node ? getIndexInfosAtLocation(node) : void 0;
    },
    getShorthandAssignmentValueSymbol: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn);
      return node ? getShorthandAssignmentValueSymbol(node) : void 0;
    },
    getExportSpecifierLocalTargetSymbol: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isExportSpecifier);
      return node ? getExportSpecifierLocalTargetSymbol(node) : void 0;
    },
    getExportSymbolOfSymbol(symbol) {
      return getMergedSymbol(symbol.exportSymbol || symbol);
    },
    getTypeAtLocation: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn);
      return node ? getTypeOfNode(node) : errorType;
    },
    getTypeOfAssignmentPattern: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isAssignmentPattern);
      return node && getTypeOfAssignmentPattern(node) || errorType;
    },
    getPropertySymbolOfDestructuringAssignment: (locationIn) ***REMOVED***> {
      const location ***REMOVED*** getParseTreeNode(locationIn, isIdentifier);
      return location ? getPropertySymbolOfDestructuringAssignment(location) : void 0;
    },
    signatureToString: (signature, enclosingDeclaration, flags, kind) ***REMOVED***> {
      return signatureToString(signature, getParseTreeNode(enclosingDeclaration), flags, kind);
    },
    typeToString: (type, enclosingDeclaration, flags) ***REMOVED***> {
      return typeToString(type, getParseTreeNode(enclosingDeclaration), flags);
    },
    symbolToString: (symbol, enclosingDeclaration, meaning, flags) ***REMOVED***> {
      return symbolToString(symbol, getParseTreeNode(enclosingDeclaration), meaning, flags);
    },
    typePredicateToString: (predicate, enclosingDeclaration, flags) ***REMOVED***> {
      return typePredicateToString(predicate, getParseTreeNode(enclosingDeclaration), flags);
    },
    writeSignature: (signature, enclosingDeclaration, flags, kind, writer, maximumLength, verbosityLevel, out) ***REMOVED***> {
      return signatureToString(signature, getParseTreeNode(enclosingDeclaration), flags, kind, writer, maximumLength, verbosityLevel, out);
    },
    writeType: (type, enclosingDeclaration, flags, writer, maximumLength, verbosityLevel, out) ***REMOVED***> {
      return typeToString(type, getParseTreeNode(enclosingDeclaration), flags, writer, maximumLength, verbosityLevel, out);
    },
    writeSymbol: (symbol, enclosingDeclaration, meaning, flags, writer) ***REMOVED***> {
      return symbolToString(symbol, getParseTreeNode(enclosingDeclaration), meaning, flags, writer);
    },
    writeTypePredicate: (predicate, enclosingDeclaration, flags, writer) ***REMOVED***> {
      return typePredicateToString(predicate, getParseTreeNode(enclosingDeclaration), flags, writer);
    },
    getAugmentedPropertiesOfType,
    getRootSymbols,
    getSymbolOfExpando,
    getContextualType: (nodeIn, contextFlags) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isExpression);
      if (!node) {
        return void 0;
      }
      if (contextFlags & 4 /* Completions */) {
        return runWithInferenceBlockedFromSourceNode(node, () ***REMOVED***> getContextualType(node, contextFlags));
      }
      return getContextualType(node, contextFlags);
    },
    getContextualTypeForObjectLiteralElement: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isObjectLiteralElementLike);
      return node ? getContextualTypeForObjectLiteralElement(
        node,
        /*contextFlags*/
        void 0
      ) : void 0;
    },
    getContextualTypeForArgumentAtIndex: (nodeIn, argIndex) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isCallLikeExpression);
      return node && getContextualTypeForArgumentAtIndex(node, argIndex);
    },
    getContextualTypeForJsxAttribute: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isJsxAttributeLike);
      return node && getContextualTypeForJsxAttribute(
        node,
        /*contextFlags*/
        void 0
      );
    },
    isContextSensitive,
    getTypeOfPropertyOfContextualType,
    getFullyQualifiedName,
    getResolvedSignature: (node, candidatesOutArray, argumentCount) ***REMOVED***> getResolvedSignatureWorker(node, candidatesOutArray, argumentCount, 0 /* Normal */),
    getCandidateSignaturesForStringLiteralCompletions,
    getResolvedSignatureForSignatureHelp: (node, candidatesOutArray, argumentCount) ***REMOVED***> runWithoutResolvedSignatureCaching(node, () ***REMOVED***> getResolvedSignatureWorker(node, candidatesOutArray, argumentCount, 16 /* IsForSignatureHelp */)),
    getExpandedParameters,
    hasEffectiveRestParameter,
    containsArgumentsReference,
    getConstantValue: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, canHaveConstantValue);
      return node ? getConstantValue2(node) : void 0;
    },
    isValidPropertyAccess: (nodeIn, propertyName) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isPropertyAccessOrQualifiedNameOrImportTypeNode);
      return !!node && isValidPropertyAccess(node, escapeLeadingUnderscores(propertyName));
    },
    isValidPropertyAccessForCompletions: (nodeIn, type, property) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isPropertyAccessExpression);
      return !!node && isValidPropertyAccessForCompletions(node, type, property);
    },
    getSignatureFromDeclaration: (declarationIn) ***REMOVED***> {
      const declaration ***REMOVED*** getParseTreeNode(declarationIn, isFunctionLike);
      return declaration ? getSignatureFromDeclaration(declaration) : void 0;
    },
    isImplementationOfOverload: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isFunctionLike);
      return node ? isImplementationOfOverload(node) : void 0;
    },
    getImmediateAliasedSymbol,
    getAliasedSymbol: resolveAlias,
    getEmitResolver,
    requiresAddingImplicitUndefined,
    getExportsOfModule: getExportsOfModuleAsArray,
    getExportsAndPropertiesOfModule,
    forEachExportAndPropertyOfModule,
    getSymbolWalker: createGetSymbolWalker(
      getRestTypeOfSignature,
      getTypePredicateOfSignature,
      getReturnTypeOfSignature,
      getBaseTypes,
      resolveStructuredTypeMembers,
      getTypeOfSymbol,
      getResolvedSymbol,
      getConstraintOfTypeParameter,
      getFirstIdentifier,
      getTypeArguments
    ),
    getAmbientModules,
    getJsxIntrinsicTagNamesAt,
    isOptionalParameter: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isParameter);
      return node ? isOptionalParameter(node) : false;
    },
    tryGetMemberInModuleExports: (name, symbol) ***REMOVED***> tryGetMemberInModuleExports(escapeLeadingUnderscores(name), symbol),
    tryGetMemberInModuleExportsAndProperties: (name, symbol) ***REMOVED***> tryGetMemberInModuleExportsAndProperties(escapeLeadingUnderscores(name), symbol),
    tryFindAmbientModule: (moduleName) ***REMOVED***> tryFindAmbientModule(
      moduleName,
      /*withAugmentations*/
      true
    ),
    getApparentType,
    getUnionType,
    isTypeAssignableTo,
    createAnonymousType,
    createSignature,
    createSymbol,
    createIndexInfo,
    getAnyType: () ***REMOVED***> anyType,
    getStringType: () ***REMOVED***> stringType,
    getStringLiteralType,
    getNumberType: () ***REMOVED***> numberType,
    getNumberLiteralType,
    getBigIntType: () ***REMOVED***> bigintType,
    getBigIntLiteralType,
    getUnknownType: () ***REMOVED***> unknownType,
    createPromiseType,
    createArrayType,
    getElementTypeOfArrayType,
    getBooleanType: () ***REMOVED***> booleanType,
    getFalseType: (fresh) ***REMOVED***> fresh ? falseType : regularFalseType,
    getTrueType: (fresh) ***REMOVED***> fresh ? trueType : regularTrueType,
    getVoidType: () ***REMOVED***> voidType,
    getUndefinedType: () ***REMOVED***> undefinedType,
    getNullType: () ***REMOVED***> nullType,
    getESSymbolType: () ***REMOVED***> esSymbolType,
    getNeverType: () ***REMOVED***> neverType,
    getNonPrimitiveType: () ***REMOVED***> nonPrimitiveType,
    getOptionalType: () ***REMOVED***> optionalType,
    getPromiseType: () ***REMOVED***> getGlobalPromiseType(
      /*reportErrors*/
      false
    ),
    getPromiseLikeType: () ***REMOVED***> getGlobalPromiseLikeType(
      /*reportErrors*/
      false
    ),
    getAnyAsyncIterableType: () ***REMOVED***> {
      const type ***REMOVED*** getGlobalAsyncIterableType(
        /*reportErrors*/
        false
      );
      if (type ***REMOVED******REMOVED******REMOVED*** emptyGenericType) return void 0;
      return createTypeReference(type, [anyType, anyType, anyType]);
    },
    isSymbolAccessible,
    isArrayType,
    isTupleType,
    isArrayLikeType,
    isEmptyAnonymousObjectType,
    isTypeInvalidDueToUnionDiscriminant,
    getExactOptionalProperties,
    getAllPossiblePropertiesOfTypes,
    getSuggestedSymbolForNonexistentProperty,
    getSuggestedSymbolForNonexistentJSXAttribute,
    getSuggestedSymbolForNonexistentSymbol: (location, name, meaning) ***REMOVED***> getSuggestedSymbolForNonexistentSymbol(location, escapeLeadingUnderscores(name), meaning),
    getSuggestedSymbolForNonexistentModule,
    getSuggestedSymbolForNonexistentClassMember,
    getBaseConstraintOfType,
    getDefaultFromTypeParameter: (type) ***REMOVED***> type && type.flags & 262144 /* TypeParameter */ ? getDefaultFromTypeParameter(type) : void 0,
    resolveName(name, location, meaning, excludeGlobals) {
      return resolveName(
        location,
        escapeLeadingUnderscores(name),
        meaning,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false,
        excludeGlobals
      );
    },
    getJsxNamespace: (n) ***REMOVED***> unescapeLeadingUnderscores(getJsxNamespace(n)),
    getJsxFragmentFactory: (n) ***REMOVED***> {
      const jsxFragmentFactory ***REMOVED*** getJsxFragmentFactoryEntity(n);
      return jsxFragmentFactory && unescapeLeadingUnderscores(getFirstIdentifier(jsxFragmentFactory).escapedText);
    },
    getAccessibleSymbolChain,
    getTypePredicateOfSignature,
    resolveExternalModuleName: (moduleSpecifierIn) ***REMOVED***> {
      const moduleSpecifier ***REMOVED*** getParseTreeNode(moduleSpecifierIn, isExpression);
      return moduleSpecifier && resolveExternalModuleName(
        moduleSpecifier,
        moduleSpecifier,
        /*ignoreErrors*/
        true
      );
    },
    resolveExternalModuleSymbol,
    tryGetThisTypeAt: (nodeIn, includeGlobalThis, container) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn);
      return node && tryGetThisTypeAt(node, includeGlobalThis, container);
    },
    getTypeArgumentConstraint: (nodeIn) ***REMOVED***> {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isTypeNode);
      return node && getTypeArgumentConstraint(node);
    },
    getSuggestionDiagnostics: (fileIn, ct) ***REMOVED***> {
      const file ***REMOVED*** getParseTreeNode(fileIn, isSourceFile) || Debug.fail("Could not determine parsed source file.");
      if (skipTypeChecking(file, compilerOptions, host)) {
        return emptyArray;
      }
      let diagnostics2;
      try {
        cancellationToken ***REMOVED*** ct;
        checkSourceFileWithEagerDiagnostics(file);
        Debug.assert(!!(getNodeLinks(file).flags & 1 /* TypeChecked */));
        diagnostics2 ***REMOVED*** addRange(diagnostics2, suggestionDiagnostics.getDiagnostics(file.fileName));
        checkUnusedIdentifiers(getPotentiallyUnusedIdentifiers(file), (containingNode, kind, diag2) ***REMOVED***> {
          if (!containsParseError(containingNode) && !unusedIsError(kind, !!(containingNode.flags & 33554432 /* Ambient */))) {
            (diagnostics2 || (diagnostics2 ***REMOVED*** [])).push({ ...diag2, category: 2 /* Suggestion */ });
          }
        });
        return diagnostics2 || emptyArray;
      } finally {
        cancellationToken ***REMOVED*** void 0;
      }
    },
    runWithCancellationToken: (token, callback) ***REMOVED***> {
      try {
        cancellationToken ***REMOVED*** token;
        return callback(checker);
      } finally {
        cancellationToken ***REMOVED*** void 0;
      }
    },
    getLocalTypeParametersOfClassOrInterfaceOrTypeAlias,
    isDeclarationVisible,
    isPropertyAccessible,
    getTypeOnlyAliasDeclaration,
    getMemberOverrideModifierStatus,
    isTypeParameterPossiblyReferenced,
    typeHasCallOrConstructSignatures,
    getSymbolFlags,
    getTypeArgumentsForResolvedSignature,
    isLibType
  };
  function getTypeArgumentsForResolvedSignature(signature) {
    if (signature.mapper ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
    return instantiateTypes((signature.target || signature).typeParameters, signature.mapper);
  }
  function getCandidateSignaturesForStringLiteralCompletions(call, editingArgument) {
    const candidatesSet ***REMOVED*** /* @__PURE__ */ new Set();
    const candidates ***REMOVED*** [];
    runWithInferenceBlockedFromSourceNode(editingArgument, () ***REMOVED***> getResolvedSignatureWorker(
      call,
      candidates,
      /*argumentCount*/
      void 0,
      0 /* Normal */
    ));
    for (const candidate of candidates) {
      candidatesSet.add(candidate);
    }
    candidates.length ***REMOVED*** 0;
    runWithoutResolvedSignatureCaching(editingArgument, () ***REMOVED***> getResolvedSignatureWorker(
      call,
      candidates,
      /*argumentCount*/
      void 0,
      0 /* Normal */
    ));
    for (const candidate of candidates) {
      candidatesSet.add(candidate);
    }
    return arrayFrom(candidatesSet);
  }
  function runWithoutResolvedSignatureCaching(node, fn) {
    node ***REMOVED*** findAncestor(node, isCallLikeOrFunctionLikeExpression);
    if (node) {
      const cachedResolvedSignatures ***REMOVED*** [];
      const cachedTypes2 ***REMOVED*** [];
      while (node) {
        const nodeLinks2 ***REMOVED*** getNodeLinks(node);
        cachedResolvedSignatures.push([nodeLinks2, nodeLinks2.resolvedSignature]);
        nodeLinks2.resolvedSignature ***REMOVED*** void 0;
        if (isFunctionExpressionOrArrowFunction(node)) {
          const symbolLinks2 ***REMOVED*** getSymbolLinks(getSymbolOfDeclaration(node));
          const type ***REMOVED*** symbolLinks2.type;
          cachedTypes2.push([symbolLinks2, type]);
          symbolLinks2.type ***REMOVED*** void 0;
        }
        node ***REMOVED*** findAncestor(node.parent, isCallLikeOrFunctionLikeExpression);
      }
      const result ***REMOVED*** fn();
      for (const [nodeLinks2, resolvedSignature] of cachedResolvedSignatures) {
        nodeLinks2.resolvedSignature ***REMOVED*** resolvedSignature;
      }
      for (const [symbolLinks2, type] of cachedTypes2) {
        symbolLinks2.type ***REMOVED*** type;
      }
      return result;
    }
    return fn();
  }
  function runWithInferenceBlockedFromSourceNode(node, fn) {
    const containingCall ***REMOVED*** findAncestor(node, isCallLikeExpression);
    if (containingCall) {
      let toMarkSkip ***REMOVED*** node;
      do {
        getNodeLinks(toMarkSkip).skipDirectInference ***REMOVED*** true;
        toMarkSkip ***REMOVED*** toMarkSkip.parent;
      } while (toMarkSkip && toMarkSkip !***REMOVED******REMOVED*** containingCall);
    }
    isInferencePartiallyBlocked ***REMOVED*** true;
    const result ***REMOVED*** runWithoutResolvedSignatureCaching(node, fn);
    isInferencePartiallyBlocked ***REMOVED*** false;
    if (containingCall) {
      let toMarkSkip ***REMOVED*** node;
      do {
        getNodeLinks(toMarkSkip).skipDirectInference ***REMOVED*** void 0;
        toMarkSkip ***REMOVED*** toMarkSkip.parent;
      } while (toMarkSkip && toMarkSkip !***REMOVED******REMOVED*** containingCall);
    }
    return result;
  }
  function getResolvedSignatureWorker(nodeIn, candidatesOutArray, argumentCount, checkMode) {
    const node ***REMOVED*** getParseTreeNode(nodeIn, isCallLikeExpression);
    apparentArgumentCount ***REMOVED*** argumentCount;
    const res ***REMOVED*** !node ? void 0 : getResolvedSignature(node, candidatesOutArray, checkMode);
    apparentArgumentCount ***REMOVED*** void 0;
    return res;
  }
  var tupleTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var unionTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var unionOfUnionTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var intersectionTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var stringLiteralTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var numberLiteralTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var bigIntLiteralTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var enumLiteralTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var indexedAccessTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var templateLiteralTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var stringMappingTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var substitutionTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var subtypeReductionCache ***REMOVED*** /* @__PURE__ */ new Map();
  var decoratorContextOverrideTypeCache ***REMOVED*** /* @__PURE__ */ new Map();
  var cachedTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var evolvingArrayTypes ***REMOVED*** [];
  var undefinedProperties ***REMOVED*** /* @__PURE__ */ new Map();
  var markerTypes ***REMOVED*** /* @__PURE__ */ new Set();
  var unknownSymbol ***REMOVED*** createSymbol(4 /* Property */, "unknown");
  var resolvingSymbol ***REMOVED*** createSymbol(0, "__resolving__" /* Resolving */);
  var unresolvedSymbols ***REMOVED*** /* @__PURE__ */ new Map();
  var errorTypes ***REMOVED*** /* @__PURE__ */ new Map();
  var seenIntrinsicNames ***REMOVED*** /* @__PURE__ */ new Set();
  var anyType ***REMOVED*** createIntrinsicType(1 /* Any */, "any");
  var autoType ***REMOVED*** createIntrinsicType(1 /* Any */, "any", 262144 /* NonInferrableType */, "auto");
  var wildcardType ***REMOVED*** createIntrinsicType(
    1 /* Any */,
    "any",
    /*objectFlags*/
    void 0,
    "wildcard"
  );
  var blockedStringType ***REMOVED*** createIntrinsicType(
    1 /* Any */,
    "any",
    /*objectFlags*/
    void 0,
    "blocked string"
  );
  var errorType ***REMOVED*** createIntrinsicType(1 /* Any */, "error");
  var unresolvedType ***REMOVED*** createIntrinsicType(1 /* Any */, "unresolved");
  var nonInferrableAnyType ***REMOVED*** createIntrinsicType(1 /* Any */, "any", 65536 /* ContainsWideningType */, "non-inferrable");
  var intrinsicMarkerType ***REMOVED*** createIntrinsicType(1 /* Any */, "intrinsic");
  var unknownType ***REMOVED*** createIntrinsicType(2 /* Unknown */, "unknown");
  var undefinedType ***REMOVED*** createIntrinsicType(32768 /* Undefined */, "undefined");
  var undefinedWideningType ***REMOVED*** strictNullChecks ? undefinedType : createIntrinsicType(32768 /* Undefined */, "undefined", 65536 /* ContainsWideningType */, "widening");
  var missingType ***REMOVED*** createIntrinsicType(
    32768 /* Undefined */,
    "undefined",
    /*objectFlags*/
    void 0,
    "missing"
  );
  var undefinedOrMissingType ***REMOVED*** exactOptionalPropertyTypes ? missingType : undefinedType;
  var optionalType ***REMOVED*** createIntrinsicType(
    32768 /* Undefined */,
    "undefined",
    /*objectFlags*/
    void 0,
    "optional"
  );
  var nullType ***REMOVED*** createIntrinsicType(65536 /* Null */, "null");
  var nullWideningType ***REMOVED*** strictNullChecks ? nullType : createIntrinsicType(65536 /* Null */, "null", 65536 /* ContainsWideningType */, "widening");
  var stringType ***REMOVED*** createIntrinsicType(4 /* String */, "string");
  var numberType ***REMOVED*** createIntrinsicType(8 /* Number */, "number");
  var bigintType ***REMOVED*** createIntrinsicType(64 /* BigInt */, "bigint");
  var falseType ***REMOVED*** createIntrinsicType(
    512 /* BooleanLiteral */,
    "false",
    /*objectFlags*/
    void 0,
    "fresh"
  );
  var regularFalseType ***REMOVED*** createIntrinsicType(512 /* BooleanLiteral */, "false");
  var trueType ***REMOVED*** createIntrinsicType(
    512 /* BooleanLiteral */,
    "true",
    /*objectFlags*/
    void 0,
    "fresh"
  );
  var regularTrueType ***REMOVED*** createIntrinsicType(512 /* BooleanLiteral */, "true");
  trueType.regularType ***REMOVED*** regularTrueType;
  trueType.freshType ***REMOVED*** trueType;
  regularTrueType.regularType ***REMOVED*** regularTrueType;
  regularTrueType.freshType ***REMOVED*** trueType;
  falseType.regularType ***REMOVED*** regularFalseType;
  falseType.freshType ***REMOVED*** falseType;
  regularFalseType.regularType ***REMOVED*** regularFalseType;
  regularFalseType.freshType ***REMOVED*** falseType;
  var booleanType ***REMOVED*** getUnionType([regularFalseType, regularTrueType]);
  var esSymbolType ***REMOVED*** createIntrinsicType(4096 /* ESSymbol */, "symbol");
  var voidType ***REMOVED*** createIntrinsicType(16384 /* Void */, "void");
  var neverType ***REMOVED*** createIntrinsicType(131072 /* Never */, "never");
  var silentNeverType ***REMOVED*** createIntrinsicType(131072 /* Never */, "never", 262144 /* NonInferrableType */, "silent");
  var implicitNeverType ***REMOVED*** createIntrinsicType(
    131072 /* Never */,
    "never",
    /*objectFlags*/
    void 0,
    "implicit"
  );
  var unreachableNeverType ***REMOVED*** createIntrinsicType(
    131072 /* Never */,
    "never",
    /*objectFlags*/
    void 0,
    "unreachable"
  );
  var nonPrimitiveType ***REMOVED*** createIntrinsicType(67108864 /* NonPrimitive */, "object");
  var stringOrNumberType ***REMOVED*** getUnionType([stringType, numberType]);
  var stringNumberSymbolType ***REMOVED*** getUnionType([stringType, numberType, esSymbolType]);
  var numberOrBigIntType ***REMOVED*** getUnionType([numberType, bigintType]);
  var templateConstraintType ***REMOVED*** getUnionType([stringType, numberType, booleanType, bigintType, nullType, undefinedType]);
  var numericStringType ***REMOVED*** getTemplateLiteralType(["", ""], [numberType]);
  var restrictiveMapper ***REMOVED*** makeFunctionTypeMapper((t) ***REMOVED***> t.flags & 262144 /* TypeParameter */ ? getRestrictiveTypeParameter(t) : t, () ***REMOVED***> "(restrictive mapper)");
  var permissiveMapper ***REMOVED*** makeFunctionTypeMapper((t) ***REMOVED***> t.flags & 262144 /* TypeParameter */ ? wildcardType : t, () ***REMOVED***> "(permissive mapper)");
  var uniqueLiteralType ***REMOVED*** createIntrinsicType(
    131072 /* Never */,
    "never",
    /*objectFlags*/
    void 0,
    "unique literal"
  );
  var uniqueLiteralMapper ***REMOVED*** makeFunctionTypeMapper((t) ***REMOVED***> t.flags & 262144 /* TypeParameter */ ? uniqueLiteralType : t, () ***REMOVED***> "(unique literal mapper)");
  var outofbandVarianceMarkerHandler;
  var reportUnreliableMapper ***REMOVED*** makeFunctionTypeMapper((t) ***REMOVED***> {
    if (outofbandVarianceMarkerHandler && (t ***REMOVED******REMOVED******REMOVED*** markerSuperType || t ***REMOVED******REMOVED******REMOVED*** markerSubType || t ***REMOVED******REMOVED******REMOVED*** markerOtherType)) {
      outofbandVarianceMarkerHandler(
        /*onlyUnreliable*/
        true
      );
    }
    return t;
  }, () ***REMOVED***> "(unmeasurable reporter)");
  var reportUnmeasurableMapper ***REMOVED*** makeFunctionTypeMapper((t) ***REMOVED***> {
    if (outofbandVarianceMarkerHandler && (t ***REMOVED******REMOVED******REMOVED*** markerSuperType || t ***REMOVED******REMOVED******REMOVED*** markerSubType || t ***REMOVED******REMOVED******REMOVED*** markerOtherType)) {
      outofbandVarianceMarkerHandler(
        /*onlyUnreliable*/
        false
      );
    }
    return t;
  }, () ***REMOVED***> "(unreliable reporter)");
  var emptyObjectType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  var emptyJsxObjectType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  emptyJsxObjectType.objectFlags |***REMOVED*** 2048 /* JsxAttributes */;
  var emptyFreshJsxObjectType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  emptyFreshJsxObjectType.objectFlags |***REMOVED*** 2048 /* JsxAttributes */ | 8192 /* FreshLiteral */ | 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
  var emptyTypeLiteralSymbol ***REMOVED*** createSymbol(2048 /* TypeLiteral */, "__type" /* Type */);
  emptyTypeLiteralSymbol.members ***REMOVED*** createSymbolTable();
  var emptyTypeLiteralType ***REMOVED*** createAnonymousType(emptyTypeLiteralSymbol, emptySymbols, emptyArray, emptyArray, emptyArray);
  var unknownEmptyObjectType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  var unknownUnionType ***REMOVED*** strictNullChecks ? getUnionType([undefinedType, nullType, unknownEmptyObjectType]) : unknownType;
  var emptyGenericType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  emptyGenericType.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
  var anyFunctionType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  anyFunctionType.objectFlags |***REMOVED*** 262144 /* NonInferrableType */;
  var noConstraintType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  var circularConstraintType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  var resolvingDefaultType ***REMOVED*** createAnonymousType(
    /*symbol*/
    void 0,
    emptySymbols,
    emptyArray,
    emptyArray,
    emptyArray
  );
  var markerSuperType ***REMOVED*** createTypeParameter();
  var markerSubType ***REMOVED*** createTypeParameter();
  markerSubType.constraint ***REMOVED*** markerSuperType;
  var markerOtherType ***REMOVED*** createTypeParameter();
  var markerSuperTypeForCheck ***REMOVED*** createTypeParameter();
  var markerSubTypeForCheck ***REMOVED*** createTypeParameter();
  markerSubTypeForCheck.constraint ***REMOVED*** markerSuperTypeForCheck;
  var noTypePredicate ***REMOVED*** createTypePredicate(1 /* Identifier */, "<<unresolved>>", 0, anyType);
  var anySignature ***REMOVED*** createSignature(
    /*declaration*/
    void 0,
    /*typeParameters*/
    void 0,
    /*thisParameter*/
    void 0,
    emptyArray,
    anyType,
    /*resolvedTypePredicate*/
    void 0,
    0,
    0 /* None */
  );
  var unknownSignature ***REMOVED*** createSignature(
    /*declaration*/
    void 0,
    /*typeParameters*/
    void 0,
    /*thisParameter*/
    void 0,
    emptyArray,
    errorType,
    /*resolvedTypePredicate*/
    void 0,
    0,
    0 /* None */
  );
  var resolvingSignature ***REMOVED*** createSignature(
    /*declaration*/
    void 0,
    /*typeParameters*/
    void 0,
    /*thisParameter*/
    void 0,
    emptyArray,
    anyType,
    /*resolvedTypePredicate*/
    void 0,
    0,
    0 /* None */
  );
  var silentNeverSignature ***REMOVED*** createSignature(
    /*declaration*/
    void 0,
    /*typeParameters*/
    void 0,
    /*thisParameter*/
    void 0,
    emptyArray,
    silentNeverType,
    /*resolvedTypePredicate*/
    void 0,
    0,
    0 /* None */
  );
  var enumNumberIndexInfo ***REMOVED*** createIndexInfo(
    numberType,
    stringType,
    /*isReadonly*/
    true
  );
  var anyBaseTypeIndexInfo ***REMOVED*** createIndexInfo(
    stringType,
    anyType,
    /*isReadonly*/
    false
  );
  var iterationTypesCache ***REMOVED*** /* @__PURE__ */ new Map();
  var noIterationTypes ***REMOVED*** {
    get yieldType() {
      return Debug.fail("Not supported");
    },
    get returnType() {
      return Debug.fail("Not supported");
    },
    get nextType() {
      return Debug.fail("Not supported");
    }
  };
  var anyIterationTypes ***REMOVED*** createIterationTypes(anyType, anyType, anyType);
  var silentNeverIterationTypes ***REMOVED*** createIterationTypes(silentNeverType, silentNeverType, silentNeverType);
  var asyncIterationTypesResolver ***REMOVED*** {
    iterableCacheKey: "iterationTypesOfAsyncIterable",
    iteratorCacheKey: "iterationTypesOfAsyncIterator",
    iteratorSymbolName: "asyncIterator",
    getGlobalIteratorType: getGlobalAsyncIteratorType,
    getGlobalIterableType: getGlobalAsyncIterableType,
    getGlobalIterableIteratorType: getGlobalAsyncIterableIteratorType,
    getGlobalIteratorObjectType: getGlobalAsyncIteratorObjectType,
    getGlobalGeneratorType: getGlobalAsyncGeneratorType,
    getGlobalBuiltinIteratorTypes: getGlobalBuiltinAsyncIteratorTypes,
    resolveIterationType: (type, errorNode) ***REMOVED***> getAwaitedType(type, errorNode, Diagnostics.Type_of_await_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member),
    mustHaveANextMethodDiagnostic: Diagnostics.An_async_iterator_must_have_a_next_method,
    mustBeAMethodDiagnostic: Diagnostics.The_0_property_of_an_async_iterator_must_be_a_method,
    mustHaveAValueDiagnostic: Diagnostics.The_type_returned_by_the_0_method_of_an_async_iterator_must_be_a_promise_for_a_type_with_a_value_property
  };
  var syncIterationTypesResolver ***REMOVED*** {
    iterableCacheKey: "iterationTypesOfIterable",
    iteratorCacheKey: "iterationTypesOfIterator",
    iteratorSymbolName: "iterator",
    getGlobalIteratorType,
    getGlobalIterableType,
    getGlobalIterableIteratorType,
    getGlobalIteratorObjectType,
    getGlobalGeneratorType,
    getGlobalBuiltinIteratorTypes,
    resolveIterationType: (type, _errorNode) ***REMOVED***> type,
    mustHaveANextMethodDiagnostic: Diagnostics.An_iterator_must_have_a_next_method,
    mustBeAMethodDiagnostic: Diagnostics.The_0_property_of_an_iterator_must_be_a_method,
    mustHaveAValueDiagnostic: Diagnostics.The_type_returned_by_the_0_method_of_an_iterator_must_have_a_value_property
  };
  var amalgamatedDuplicates;
  var reverseMappedCache ***REMOVED*** /* @__PURE__ */ new Map();
  var reverseHomomorphicMappedCache ***REMOVED*** /* @__PURE__ */ new Map();
  var ambientModulesCache;
  var patternAmbientModules;
  var patternAmbientModuleAugmentations;
  var globalObjectType;
  var globalFunctionType;
  var globalCallableFunctionType;
  var globalNewableFunctionType;
  var globalArrayType;
  var globalReadonlyArrayType;
  var globalStringType;
  var globalNumberType;
  var globalBooleanType;
  var globalRegExpType;
  var globalThisType;
  var anyArrayType;
  var autoArrayType;
  var anyReadonlyArrayType;
  var deferredGlobalNonNullableTypeAlias;
  var deferredGlobalESSymbolConstructorSymbol;
  var deferredGlobalESSymbolConstructorTypeSymbol;
  var deferredGlobalESSymbolType;
  var deferredGlobalTypedPropertyDescriptorType;
  var deferredGlobalPromiseType;
  var deferredGlobalPromiseLikeType;
  var deferredGlobalPromiseConstructorSymbol;
  var deferredGlobalPromiseConstructorLikeType;
  var deferredGlobalIterableType;
  var deferredGlobalIteratorType;
  var deferredGlobalIterableIteratorType;
  var deferredGlobalIteratorObjectType;
  var deferredGlobalGeneratorType;
  var deferredGlobalIteratorYieldResultType;
  var deferredGlobalIteratorReturnResultType;
  var deferredGlobalAsyncIterableType;
  var deferredGlobalAsyncIteratorType;
  var deferredGlobalAsyncIterableIteratorType;
  var deferredGlobalBuiltinIteratorTypes;
  var deferredGlobalBuiltinAsyncIteratorTypes;
  var deferredGlobalAsyncIteratorObjectType;
  var deferredGlobalAsyncGeneratorType;
  var deferredGlobalTemplateStringsArrayType;
  var deferredGlobalImportMetaType;
  var deferredGlobalImportMetaExpressionType;
  var deferredGlobalImportCallOptionsType;
  var deferredGlobalImportAttributesType;
  var deferredGlobalDisposableType;
  var deferredGlobalAsyncDisposableType;
  var deferredGlobalExtractSymbol;
  var deferredGlobalOmitSymbol;
  var deferredGlobalAwaitedSymbol;
  var deferredGlobalBigIntType;
  var deferredGlobalNaNSymbol;
  var deferredGlobalRecordSymbol;
  var deferredGlobalClassDecoratorContextType;
  var deferredGlobalClassMethodDecoratorContextType;
  var deferredGlobalClassGetterDecoratorContextType;
  var deferredGlobalClassSetterDecoratorContextType;
  var deferredGlobalClassAccessorDecoratorContextType;
  var deferredGlobalClassAccessorDecoratorTargetType;
  var deferredGlobalClassAccessorDecoratorResultType;
  var deferredGlobalClassFieldDecoratorContextType;
  var allPotentiallyUnusedIdentifiers ***REMOVED*** /* @__PURE__ */ new Map();
  var flowLoopStart ***REMOVED*** 0;
  var flowLoopCount ***REMOVED*** 0;
  var sharedFlowCount ***REMOVED*** 0;
  var flowAnalysisDisabled ***REMOVED*** false;
  var flowInvocationCount ***REMOVED*** 0;
  var lastFlowNode;
  var lastFlowNodeReachable;
  var flowTypeCache;
  var contextualTypeNodes ***REMOVED*** [];
  var contextualTypes ***REMOVED*** [];
  var contextualIsCache ***REMOVED*** [];
  var contextualTypeCount ***REMOVED*** 0;
  var contextualBindingPatterns ***REMOVED*** [];
  var inferenceContextNodes ***REMOVED*** [];
  var inferenceContexts ***REMOVED*** [];
  var inferenceContextCount ***REMOVED*** 0;
  var activeTypeMappers ***REMOVED*** [];
  var activeTypeMappersCaches ***REMOVED*** [];
  var activeTypeMappersCount ***REMOVED*** 0;
  var emptyStringType ***REMOVED*** getStringLiteralType("");
  var zeroType ***REMOVED*** getNumberLiteralType(0);
  var zeroBigIntType ***REMOVED*** getBigIntLiteralType({ negative: false, base10Value: "0" });
  var resolutionTargets ***REMOVED*** [];
  var resolutionResults ***REMOVED*** [];
  var resolutionPropertyNames ***REMOVED*** [];
  var resolutionStart ***REMOVED*** 0;
  var inVarianceComputation ***REMOVED*** false;
  var suggestionCount ***REMOVED*** 0;
  var maximumSuggestionCount ***REMOVED*** 10;
  var mergedSymbols ***REMOVED*** [];
  var symbolLinks ***REMOVED*** [];
  var nodeLinks ***REMOVED*** [];
  var flowLoopCaches ***REMOVED*** [];
  var flowLoopNodes ***REMOVED*** [];
  var flowLoopKeys ***REMOVED*** [];
  var flowLoopTypes ***REMOVED*** [];
  var sharedFlowNodes ***REMOVED*** [];
  var sharedFlowTypes ***REMOVED*** [];
  var flowNodeReachable ***REMOVED*** [];
  var flowNodePostSuper ***REMOVED*** [];
  var potentialThisCollisions ***REMOVED*** [];
  var potentialNewTargetCollisions ***REMOVED*** [];
  var potentialWeakMapSetCollisions ***REMOVED*** [];
  var potentialReflectCollisions ***REMOVED*** [];
  var potentialUnusedRenamedBindingElementsInTypes ***REMOVED*** [];
  var awaitedTypeStack ***REMOVED*** [];
  var reverseMappedSourceStack ***REMOVED*** [];
  var reverseMappedTargetStack ***REMOVED*** [];
  var reverseExpandingFlags ***REMOVED*** 0 /* None */;
  var diagnostics ***REMOVED*** createDiagnosticCollection();
  var suggestionDiagnostics ***REMOVED*** createDiagnosticCollection();
  var typeofType ***REMOVED*** createTypeofType();
  var _jsxNamespace;
  var _jsxFactoryEntity;
  var subtypeRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var strictSubtypeRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var assignableRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var comparableRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var identityRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var enumRelation ***REMOVED*** /* @__PURE__ */ new Map();
  var suggestedExtensions ***REMOVED*** [
    [".mts", ".mjs"],
    [".ts", ".js"],
    [".cts", ".cjs"],
    [".mjs", ".mjs"],
    [".js", ".js"],
    [".cjs", ".cjs"],
    [".tsx", compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 1 /* Preserve */ ? ".jsx" : ".js"],
    [".jsx", ".jsx"],
    [".json", ".json"]
  ];
  initializeTypeChecker();
  return checker;
  function isDefinitelyReferenceToGlobalSymbolObject(node) {
    if (!isPropertyAccessExpression(node)) return false;
    if (!isIdentifier(node.name)) return false;
    if (!isPropertyAccessExpression(node.expression) && !isIdentifier(node.expression)) return false;
    if (isIdentifier(node.expression)) {
      return idText(node.expression) ***REMOVED******REMOVED******REMOVED*** "Symbol" && getResolvedSymbol(node.expression) ***REMOVED******REMOVED******REMOVED*** (getGlobalSymbol(
        "Symbol",
        111551 /* Value */ | 1048576 /* ExportValue */,
        /*diagnostic*/
        void 0
      ) || unknownSymbol);
    }
    if (!isIdentifier(node.expression.expression)) return false;
    return idText(node.expression.name) ***REMOVED******REMOVED******REMOVED*** "Symbol" && idText(node.expression.expression) ***REMOVED******REMOVED******REMOVED*** "globalThis" && getResolvedSymbol(node.expression.expression) ***REMOVED******REMOVED******REMOVED*** globalThisSymbol;
  }
  function getCachedType(key) {
    return key ? cachedTypes.get(key) : void 0;
  }
  function setCachedType(key, type) {
    if (key) cachedTypes.set(key, type);
    return type;
  }
  function getJsxNamespace(location) {
    if (location) {
      const file ***REMOVED*** getSourceFileOfNode(location);
      if (file) {
        if (isJsxOpeningFragment(location)) {
          if (file.localJsxFragmentNamespace) {
            return file.localJsxFragmentNamespace;
          }
          const jsxFragmentPragma ***REMOVED*** file.pragmas.get("jsxfrag");
          if (jsxFragmentPragma) {
            const chosenPragma ***REMOVED*** isArray(jsxFragmentPragma) ? jsxFragmentPragma[0] : jsxFragmentPragma;
            file.localJsxFragmentFactory ***REMOVED*** parseIsolatedEntityName(chosenPragma.arguments.factory, languageVersion);
            visitNode(file.localJsxFragmentFactory, markAsSynthetic, isEntityName);
            if (file.localJsxFragmentFactory) {
              return file.localJsxFragmentNamespace ***REMOVED*** getFirstIdentifier(file.localJsxFragmentFactory).escapedText;
            }
          }
          const entity ***REMOVED*** getJsxFragmentFactoryEntity(location);
          if (entity) {
            file.localJsxFragmentFactory ***REMOVED*** entity;
            return file.localJsxFragmentNamespace ***REMOVED*** getFirstIdentifier(entity).escapedText;
          }
        } else {
          const localJsxNamespace ***REMOVED*** getLocalJsxNamespace(file);
          if (localJsxNamespace) {
            return file.localJsxNamespace ***REMOVED*** localJsxNamespace;
          }
        }
      }
    }
    if (!_jsxNamespace) {
      _jsxNamespace ***REMOVED*** "React";
      if (compilerOptions.jsxFactory) {
        _jsxFactoryEntity ***REMOVED*** parseIsolatedEntityName(compilerOptions.jsxFactory, languageVersion);
        visitNode(_jsxFactoryEntity, markAsSynthetic);
        if (_jsxFactoryEntity) {
          _jsxNamespace ***REMOVED*** getFirstIdentifier(_jsxFactoryEntity).escapedText;
        }
      } else if (compilerOptions.reactNamespace) {
        _jsxNamespace ***REMOVED*** escapeLeadingUnderscores(compilerOptions.reactNamespace);
      }
    }
    if (!_jsxFactoryEntity) {
      _jsxFactoryEntity ***REMOVED*** factory.createQualifiedName(factory.createIdentifier(unescapeLeadingUnderscores(_jsxNamespace)), "createElement");
    }
    return _jsxNamespace;
  }
  function getLocalJsxNamespace(file) {
    if (file.localJsxNamespace) {
      return file.localJsxNamespace;
    }
    const jsxPragma ***REMOVED*** file.pragmas.get("jsx");
    if (jsxPragma) {
      const chosenPragma ***REMOVED*** isArray(jsxPragma) ? jsxPragma[0] : jsxPragma;
      file.localJsxFactory ***REMOVED*** parseIsolatedEntityName(chosenPragma.arguments.factory, languageVersion);
      visitNode(file.localJsxFactory, markAsSynthetic, isEntityName);
      if (file.localJsxFactory) {
        return file.localJsxNamespace ***REMOVED*** getFirstIdentifier(file.localJsxFactory).escapedText;
      }
    }
  }
  function markAsSynthetic(node) {
    setTextRangePosEnd(node, -1, -1);
    return visitEachChild(
      node,
      markAsSynthetic,
      /*context*/
      void 0
    );
  }
  function getEmitResolver(sourceFile, cancellationToken2, skipDiagnostics) {
    if (!skipDiagnostics) getDiagnostics(sourceFile, cancellationToken2);
    return emitResolver;
  }
  function lookupOrIssueError(location, message, ...args) {
    const diagnostic ***REMOVED*** location ? createDiagnosticForNode(location, message, ...args) : createCompilerDiagnostic(message, ...args);
    const existing ***REMOVED*** diagnostics.lookup(diagnostic);
    if (existing) {
      return existing;
    } else {
      diagnostics.add(diagnostic);
      return diagnostic;
    }
  }
  function errorSkippedOn(key, location, message, ...args) {
    const diagnostic ***REMOVED*** error(location, message, ...args);
    diagnostic.skippedOn ***REMOVED*** key;
    return diagnostic;
  }
  function createError(location, message, ...args) {
    return location ? createDiagnosticForNode(location, message, ...args) : createCompilerDiagnostic(message, ...args);
  }
  function error(location, message, ...args) {
    const diagnostic ***REMOVED*** createError(location, message, ...args);
    diagnostics.add(diagnostic);
    return diagnostic;
  }
  function getVerbatimModuleSyntaxErrorMessage(node) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    const fileName ***REMOVED*** sourceFile.fileName;
    if (fileExtensionIsOneOf(fileName, [".cts" /* Cts */, ".cjs" /* Cjs */])) {
      return Diagnostics.ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax;
    } else {
      return Diagnostics.ECMAScript_imports_and_exports_cannot_be_written_in_a_CommonJS_file_under_verbatimModuleSyntax_Adjust_the_type_field_in_the_nearest_package_json_to_make_this_file_an_ECMAScript_module_or_adjust_your_verbatimModuleSyntax_module_and_moduleResolution_settings_in_TypeScript;
    }
  }
  function addErrorOrSuggestion(isError, diagnostic) {
    if (isError) {
      diagnostics.add(diagnostic);
    } else {
      suggestionDiagnostics.add({ ...diagnostic, category: 2 /* Suggestion */ });
    }
  }
  function errorOrSuggestion(isError, location, message, ...args) {
    if (location.pos < 0 || location.end < 0) {
      if (!isError) {
        return;
      }
      const file ***REMOVED*** getSourceFileOfNode(location);
      addErrorOrSuggestion(isError, "message" in message ? createFileDiagnostic(file, 0, 0, message, ...args) : createDiagnosticForFileFromMessageChain(file, message));
      return;
    }
    addErrorOrSuggestion(isError, "message" in message ? createDiagnosticForNode(location, message, ...args) : createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(location), location, message));
  }
  function errorAndMaybeSuggestAwait(location, maybeMissingAwait, message, ...args) {
    const diagnostic ***REMOVED*** error(location, message, ...args);
    if (maybeMissingAwait) {
      const related ***REMOVED*** createDiagnosticForNode(location, Diagnostics.Did_you_forget_to_use_await);
      addRelatedInfo(diagnostic, related);
    }
    return diagnostic;
  }
  function addDeprecatedSuggestionWorker(declarations, diagnostic) {
    const deprecatedTag ***REMOVED*** Array.isArray(declarations) ? forEach(declarations, getJSDocDeprecatedTag) : getJSDocDeprecatedTag(declarations);
    if (deprecatedTag) {
      addRelatedInfo(
        diagnostic,
        createDiagnosticForNode(deprecatedTag, Diagnostics.The_declaration_was_marked_as_deprecated_here)
      );
    }
    suggestionDiagnostics.add(diagnostic);
    return diagnostic;
  }
  function isDeprecatedSymbol(symbol) {
    const parentSymbol ***REMOVED*** getParentOfSymbol(symbol);
    if (parentSymbol && length(symbol.declarations) > 1) {
      return parentSymbol.flags & 64 /* Interface */ ? some(symbol.declarations, isDeprecatedDeclaration) : every(symbol.declarations, isDeprecatedDeclaration);
    }
    return !!symbol.valueDeclaration && isDeprecatedDeclaration(symbol.valueDeclaration) || length(symbol.declarations) && every(symbol.declarations, isDeprecatedDeclaration);
  }
  function isDeprecatedDeclaration(declaration) {
    return !!(getCombinedNodeFlagsCached(declaration) & 536870912 /* Deprecated */);
  }
  function addDeprecatedSuggestion(location, declarations, deprecatedEntity) {
    const diagnostic ***REMOVED*** createDiagnosticForNode(location, Diagnostics._0_is_deprecated, deprecatedEntity);
    return addDeprecatedSuggestionWorker(declarations, diagnostic);
  }
  function addDeprecatedSuggestionWithSignature(location, declaration, deprecatedEntity, signatureString) {
    const diagnostic ***REMOVED*** deprecatedEntity ? createDiagnosticForNode(location, Diagnostics.The_signature_0_of_1_is_deprecated, signatureString, deprecatedEntity) : createDiagnosticForNode(location, Diagnostics._0_is_deprecated, signatureString);
    return addDeprecatedSuggestionWorker(declaration, diagnostic);
  }
  function createSymbol(flags, name, checkFlags) {
    symbolCount++;
    const symbol ***REMOVED*** new Symbol13(flags | 33554432 /* Transient */, name);
    symbol.links ***REMOVED*** new SymbolLinks();
    symbol.links.checkFlags ***REMOVED*** checkFlags || 0 /* None */;
    return symbol;
  }
  function createParameter(name, type) {
    const symbol ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, name);
    symbol.links.type ***REMOVED*** type;
    return symbol;
  }
  function createProperty(name, type) {
    const symbol ***REMOVED*** createSymbol(4 /* Property */, name);
    symbol.links.type ***REMOVED*** type;
    return symbol;
  }
  function getExcludedSymbolFlags(flags) {
    let result ***REMOVED*** 0;
    if (flags & 2 /* BlockScopedVariable */) result |***REMOVED*** 111551 /* BlockScopedVariableExcludes */;
    if (flags & 1 /* FunctionScopedVariable */) result |***REMOVED*** 111550 /* FunctionScopedVariableExcludes */;
    if (flags & 4 /* Property */) result |***REMOVED*** 0 /* PropertyExcludes */;
    if (flags & 8 /* EnumMember */) result |***REMOVED*** 900095 /* EnumMemberExcludes */;
    if (flags & 16 /* Function */) result |***REMOVED*** 110991 /* FunctionExcludes */;
    if (flags & 32 /* Class */) result |***REMOVED*** 899503 /* ClassExcludes */;
    if (flags & 64 /* Interface */) result |***REMOVED*** 788872 /* InterfaceExcludes */;
    if (flags & 256 /* RegularEnum */) result |***REMOVED*** 899327 /* RegularEnumExcludes */;
    if (flags & 128 /* ConstEnum */) result |***REMOVED*** 899967 /* ConstEnumExcludes */;
    if (flags & 512 /* ValueModule */) result |***REMOVED*** 110735 /* ValueModuleExcludes */;
    if (flags & 8192 /* Method */) result |***REMOVED*** 103359 /* MethodExcludes */;
    if (flags & 32768 /* GetAccessor */) result |***REMOVED*** 46015 /* GetAccessorExcludes */;
    if (flags & 65536 /* SetAccessor */) result |***REMOVED*** 78783 /* SetAccessorExcludes */;
    if (flags & 262144 /* TypeParameter */) result |***REMOVED*** 526824 /* TypeParameterExcludes */;
    if (flags & 524288 /* TypeAlias */) result |***REMOVED*** 788968 /* TypeAliasExcludes */;
    if (flags & 2097152 /* Alias */) result |***REMOVED*** 2097152 /* AliasExcludes */;
    return result;
  }
  function recordMergedSymbol(target, source) {
    if (!source.mergeId) {
      source.mergeId ***REMOVED*** nextMergeId;
      nextMergeId++;
    }
    mergedSymbols[source.mergeId] ***REMOVED*** target;
  }
  function cloneSymbol(symbol) {
    const result ***REMOVED*** createSymbol(symbol.flags, symbol.escapedName);
    result.declarations ***REMOVED*** symbol.declarations ? symbol.declarations.slice() : [];
    result.parent ***REMOVED*** symbol.parent;
    if (symbol.valueDeclaration) result.valueDeclaration ***REMOVED*** symbol.valueDeclaration;
    if (symbol.constEnumOnlyModule) result.constEnumOnlyModule ***REMOVED*** true;
    if (symbol.members) result.members ***REMOVED*** new Map(symbol.members);
    if (symbol.exports) result.exports ***REMOVED*** new Map(symbol.exports);
    recordMergedSymbol(result, symbol);
    return result;
  }
  function mergeSymbol(target, source, unidirectional ***REMOVED*** false) {
    if (!(target.flags & getExcludedSymbolFlags(source.flags)) || (source.flags | target.flags) & 67108864 /* Assignment */) {
      if (source ***REMOVED******REMOVED******REMOVED*** target) {
        return target;
      }
      if (!(target.flags & 33554432 /* Transient */)) {
        const resolvedTarget ***REMOVED*** resolveSymbol(target);
        if (resolvedTarget ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
          return source;
        }
        if (!(resolvedTarget.flags & getExcludedSymbolFlags(source.flags)) || (source.flags | resolvedTarget.flags) & 67108864 /* Assignment */) {
          target ***REMOVED*** cloneSymbol(resolvedTarget);
        } else {
          reportMergeSymbolError(target, source);
          return source;
        }
      }
      if (source.flags & 512 /* ValueModule */ && target.flags & 512 /* ValueModule */ && target.constEnumOnlyModule && !source.constEnumOnlyModule) {
        target.constEnumOnlyModule ***REMOVED*** false;
      }
      target.flags |***REMOVED*** source.flags;
      if (source.valueDeclaration) {
        setValueDeclaration(target, source.valueDeclaration);
      }
      addRange(target.declarations, source.declarations);
      if (source.members) {
        if (!target.members) target.members ***REMOVED*** createSymbolTable();
        mergeSymbolTable(target.members, source.members, unidirectional);
      }
      if (source.exports) {
        if (!target.exports) target.exports ***REMOVED*** createSymbolTable();
        mergeSymbolTable(target.exports, source.exports, unidirectional, target);
      }
      if (!unidirectional) {
        recordMergedSymbol(target, source);
      }
    } else if (target.flags & 1024 /* NamespaceModule */) {
      if (target !***REMOVED******REMOVED*** globalThisSymbol) {
        error(
          source.declarations && getNameOfDeclaration(source.declarations[0]),
          Diagnostics.Cannot_augment_module_0_with_value_exports_because_it_resolves_to_a_non_module_entity,
          symbolToString(target)
        );
      }
    } else {
      reportMergeSymbolError(target, source);
    }
    return target;
    function reportMergeSymbolError(target2, source2) {
      const isEitherEnum ***REMOVED*** !!(target2.flags & 384 /* Enum */ || source2.flags & 384 /* Enum */);
      const isEitherBlockScoped ***REMOVED*** !!(target2.flags & 2 /* BlockScopedVariable */ || source2.flags & 2 /* BlockScopedVariable */);
      const message ***REMOVED*** isEitherEnum ? Diagnostics.Enum_declarations_can_only_merge_with_namespace_or_other_enum_declarations : isEitherBlockScoped ? Diagnostics.Cannot_redeclare_block_scoped_variable_0 : Diagnostics.Duplicate_identifier_0;
      const sourceSymbolFile ***REMOVED*** source2.declarations && getSourceFileOfNode(source2.declarations[0]);
      const targetSymbolFile ***REMOVED*** target2.declarations && getSourceFileOfNode(target2.declarations[0]);
      const isSourcePlainJs ***REMOVED*** isPlainJsFile(sourceSymbolFile, compilerOptions.checkJs);
      const isTargetPlainJs ***REMOVED*** isPlainJsFile(targetSymbolFile, compilerOptions.checkJs);
      const symbolName2 ***REMOVED*** symbolToString(source2);
      if (sourceSymbolFile && targetSymbolFile && amalgamatedDuplicates && !isEitherEnum && sourceSymbolFile !***REMOVED******REMOVED*** targetSymbolFile) {
        const firstFile ***REMOVED*** comparePaths(sourceSymbolFile.path, targetSymbolFile.path) ***REMOVED******REMOVED******REMOVED*** -1 /* LessThan */ ? sourceSymbolFile : targetSymbolFile;
        const secondFile ***REMOVED*** firstFile ***REMOVED******REMOVED******REMOVED*** sourceSymbolFile ? targetSymbolFile : sourceSymbolFile;
        const filesDuplicates ***REMOVED*** getOrUpdate(amalgamatedDuplicates, `${firstFile.path}|${secondFile.path}`, () ***REMOVED***> ({ firstFile, secondFile, conflictingSymbols: /* @__PURE__ */ new Map() }));
        const conflictingSymbolInfo ***REMOVED*** getOrUpdate(filesDuplicates.conflictingSymbols, symbolName2, () ***REMOVED***> ({ isBlockScoped: isEitherBlockScoped, firstFileLocations: [], secondFileLocations: [] }));
        if (!isSourcePlainJs) addDuplicateLocations(conflictingSymbolInfo.firstFileLocations, source2);
        if (!isTargetPlainJs) addDuplicateLocations(conflictingSymbolInfo.secondFileLocations, target2);
      } else {
        if (!isSourcePlainJs) addDuplicateDeclarationErrorsForSymbols(source2, message, symbolName2, target2);
        if (!isTargetPlainJs) addDuplicateDeclarationErrorsForSymbols(target2, message, symbolName2, source2);
      }
    }
    function addDuplicateLocations(locs, symbol) {
      if (symbol.declarations) {
        for (const decl of symbol.declarations) {
          pushIfUnique(locs, decl);
        }
      }
    }
  }
  function addDuplicateDeclarationErrorsForSymbols(target, message, symbolName2, source) {
    forEach(target.declarations, (node) ***REMOVED***> {
      addDuplicateDeclarationError(node, message, symbolName2, source.declarations);
    });
  }
  function addDuplicateDeclarationError(node, message, symbolName2, relatedNodes) {
    const errorNode ***REMOVED*** (getExpandoInitializer(
      node,
      /*isPrototypeAssignment*/
      false
    ) ? getNameOfExpando(node) : getNameOfDeclaration(node)) || node;
    const err ***REMOVED*** lookupOrIssueError(errorNode, message, symbolName2);
    for (const relatedNode of relatedNodes || emptyArray) {
      const adjustedNode ***REMOVED*** (getExpandoInitializer(
        relatedNode,
        /*isPrototypeAssignment*/
        false
      ) ? getNameOfExpando(relatedNode) : getNameOfDeclaration(relatedNode)) || relatedNode;
      if (adjustedNode ***REMOVED******REMOVED******REMOVED*** errorNode) continue;
      err.relatedInformation ***REMOVED*** err.relatedInformation || [];
      const leadingMessage ***REMOVED*** createDiagnosticForNode(adjustedNode, Diagnostics._0_was_also_declared_here, symbolName2);
      const followOnMessage ***REMOVED*** createDiagnosticForNode(adjustedNode, Diagnostics.and_here);
      if (length(err.relatedInformation) >***REMOVED*** 5 || some(err.relatedInformation, (r) ***REMOVED***> compareDiagnostics(r, followOnMessage) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */ || compareDiagnostics(r, leadingMessage) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */)) continue;
      addRelatedInfo(err, !length(err.relatedInformation) ? leadingMessage : followOnMessage);
    }
  }
  function combineSymbolTables(first2, second) {
    if (!(first2 ***REMOVED******REMOVED*** null ? void 0 : first2.size)) return second;
    if (!(second ***REMOVED******REMOVED*** null ? void 0 : second.size)) return first2;
    const combined ***REMOVED*** createSymbolTable();
    mergeSymbolTable(combined, first2);
    mergeSymbolTable(combined, second);
    return combined;
  }
  function mergeSymbolTable(target, source, unidirectional ***REMOVED*** false, mergedParent) {
    source.forEach((sourceSymbol, id) ***REMOVED***> {
      const targetSymbol ***REMOVED*** target.get(id);
      const merged ***REMOVED*** targetSymbol ? mergeSymbol(targetSymbol, sourceSymbol, unidirectional) : getMergedSymbol(sourceSymbol);
      if (mergedParent && targetSymbol) {
        merged.parent ***REMOVED*** mergedParent;
      }
      target.set(id, merged);
    });
  }
  function mergeModuleAugmentation(moduleName) {
    var _a, _b, _c;
    const moduleAugmentation ***REMOVED*** moduleName.parent;
    if (((_a ***REMOVED*** moduleAugmentation.symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0]) !***REMOVED******REMOVED*** moduleAugmentation) {
      Debug.assert(moduleAugmentation.symbol.declarations.length > 1);
      return;
    }
    if (isGlobalScopeAugmentation(moduleAugmentation)) {
      mergeSymbolTable(globals, moduleAugmentation.symbol.exports);
    } else {
      const moduleNotFoundError ***REMOVED*** !(moduleName.parent.parent.flags & 33554432 /* Ambient */) ? Diagnostics.Invalid_module_name_in_augmentation_module_0_cannot_be_found : void 0;
      let mainModule ***REMOVED*** resolveExternalModuleNameWorker(
        moduleName,
        moduleName,
        moduleNotFoundError,
        /*ignoreErrors*/
        false,
        /*isForAugmentation*/
        true
      );
      if (!mainModule) {
        return;
      }
      mainModule ***REMOVED*** resolveExternalModuleSymbol(mainModule);
      if (mainModule.flags & 1920 /* Namespace */) {
        if (some(patternAmbientModules, (module2) ***REMOVED***> mainModule ***REMOVED******REMOVED******REMOVED*** module2.symbol)) {
          const merged ***REMOVED*** mergeSymbol(
            moduleAugmentation.symbol,
            mainModule,
            /*unidirectional*/
            true
          );
          if (!patternAmbientModuleAugmentations) {
            patternAmbientModuleAugmentations ***REMOVED*** /* @__PURE__ */ new Map();
          }
          patternAmbientModuleAugmentations.set(moduleName.text, merged);
        } else {
          if (((_b ***REMOVED*** mainModule.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.get("__export" /* ExportStar */)) && ((_c ***REMOVED*** moduleAugmentation.symbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _c.size)) {
            const resolvedExports ***REMOVED*** getResolvedMembersOrExportsOfSymbol(mainModule, "resolvedExports" /* resolvedExports */);
            for (const [key, value] of arrayFrom(moduleAugmentation.symbol.exports.entries())) {
              if (resolvedExports.has(key) && !mainModule.exports.has(key)) {
                mergeSymbol(resolvedExports.get(key), value);
              }
            }
          }
          mergeSymbol(mainModule, moduleAugmentation.symbol);
        }
      } else {
        error(moduleName, Diagnostics.Cannot_augment_module_0_because_it_resolves_to_a_non_module_entity, moduleName.text);
      }
    }
  }
  function addUndefinedToGlobalsOrErrorOnRedeclaration() {
    const name ***REMOVED*** undefinedSymbol.escapedName;
    const targetSymbol ***REMOVED*** globals.get(name);
    if (targetSymbol) {
      forEach(targetSymbol.declarations, (declaration) ***REMOVED***> {
        if (!isTypeDeclaration(declaration)) {
          diagnostics.add(createDiagnosticForNode(declaration, Diagnostics.Declaration_name_conflicts_with_built_in_global_identifier_0, unescapeLeadingUnderscores(name)));
        }
      });
    } else {
      globals.set(name, undefinedSymbol);
    }
  }
  function getSymbolLinks(symbol) {
    if (symbol.flags & 33554432 /* Transient */) return symbol.links;
    const id ***REMOVED*** getSymbolId(symbol);
    return symbolLinks[id] ?? (symbolLinks[id] ***REMOVED*** new SymbolLinks());
  }
  function getNodeLinks(node) {
    const nodeId ***REMOVED*** getNodeId(node);
    return nodeLinks[nodeId] || (nodeLinks[nodeId] ***REMOVED*** new NodeLinks());
  }
  function getSymbol(symbols, name, meaning) {
    if (meaning) {
      const symbol ***REMOVED*** getMergedSymbol(symbols.get(name));
      if (symbol) {
        if (symbol.flags & meaning) {
          return symbol;
        }
        if (symbol.flags & 2097152 /* Alias */) {
          const targetFlags ***REMOVED*** getSymbolFlags(symbol);
          if (targetFlags & meaning) {
            return symbol;
          }
        }
      }
    }
  }
  function getSymbolsOfParameterPropertyDeclaration(parameter, parameterName) {
    const constructorDeclaration ***REMOVED*** parameter.parent;
    const classDeclaration ***REMOVED*** parameter.parent.parent;
    const parameterSymbol ***REMOVED*** getSymbol(constructorDeclaration.locals, parameterName, 111551 /* Value */);
    const propertySymbol ***REMOVED*** getSymbol(getMembersOfSymbol(classDeclaration.symbol), parameterName, 111551 /* Value */);
    if (parameterSymbol && propertySymbol) {
      return [parameterSymbol, propertySymbol];
    }
    return Debug.fail("There should exist two symbols, one as property declaration and one as parameter declaration");
  }
  function isBlockScopedNameDeclaredBeforeUse(declaration, usage) {
    const declarationFile ***REMOVED*** getSourceFileOfNode(declaration);
    const useFile ***REMOVED*** getSourceFileOfNode(usage);
    const declContainer ***REMOVED*** getEnclosingBlockScopeContainer(declaration);
    if (declarationFile !***REMOVED******REMOVED*** useFile) {
      if (moduleKind && (declarationFile.externalModuleIndicator || useFile.externalModuleIndicator) || !compilerOptions.outFile || isInTypeQuery(usage) || declaration.flags & 33554432 /* Ambient */) {
        return true;
      }
      if (isUsedInFunctionOrInstanceProperty(usage, declaration)) {
        return true;
      }
      const sourceFiles ***REMOVED*** host.getSourceFiles();
      return sourceFiles.indexOf(declarationFile) <***REMOVED*** sourceFiles.indexOf(useFile);
    }
    if (!!(usage.flags & 16777216 /* JSDoc */) || isInTypeQuery(usage) || isInAmbientOrTypeNode(usage)) {
      return true;
    }
    if (declaration.pos <***REMOVED*** usage.pos && !(isPropertyDeclaration(declaration) && isThisProperty(usage.parent) && !declaration.initializer && !declaration.exclamationToken)) {
      if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */) {
        const errorBindingElement ***REMOVED*** getAncestor(usage, 209 /* BindingElement */);
        if (errorBindingElement) {
          return findAncestor(errorBindingElement, isBindingElement) !***REMOVED******REMOVED*** findAncestor(declaration, isBindingElement) || declaration.pos < errorBindingElement.pos;
        }
        return isBlockScopedNameDeclaredBeforeUse(getAncestor(declaration, 261 /* VariableDeclaration */), usage);
      } else if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
        return !isImmediatelyUsedInInitializerOfBlockScopedVariable(declaration, usage);
      } else if (isClassLike(declaration)) {
        const container ***REMOVED*** findAncestor(usage, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** declaration ? "quit" : isComputedPropertyName(n) ? n.parent.parent ***REMOVED******REMOVED******REMOVED*** declaration : !legacyDecorators && isDecorator(n) && (n.parent ***REMOVED******REMOVED******REMOVED*** declaration || isMethodDeclaration(n.parent) && n.parent.parent ***REMOVED******REMOVED******REMOVED*** declaration || isGetOrSetAccessorDeclaration(n.parent) && n.parent.parent ***REMOVED******REMOVED******REMOVED*** declaration || isPropertyDeclaration(n.parent) && n.parent.parent ***REMOVED******REMOVED******REMOVED*** declaration || isParameter(n.parent) && n.parent.parent.parent ***REMOVED******REMOVED******REMOVED*** declaration));
        if (!container) {
          return true;
        }
        if (!legacyDecorators && isDecorator(container)) {
          return !!findAncestor(usage, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** container ? "quit" : isFunctionLike(n) && !getImmediatelyInvokedFunctionExpression(n));
        }
        return false;
      } else if (isPropertyDeclaration(declaration)) {
        return !isPropertyImmediatelyReferencedWithinDeclaration(
          declaration,
          usage,
          /*stopAtAnyPropertyDeclaration*/
          false
        );
      } else if (isParameterPropertyDeclaration(declaration, declaration.parent)) {
        return !(emitStandardClassFields && getContainingClass(declaration) ***REMOVED******REMOVED******REMOVED*** getContainingClass(usage) && isUsedInFunctionOrInstanceProperty(usage, declaration));
      }
      return true;
    }
    if (usage.parent.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || usage.parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ && usage.parent.isExportEquals) {
      return true;
    }
    if (usage.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ && usage.isExportEquals) {
      return true;
    }
    if (isUsedInFunctionOrInstanceProperty(usage, declaration)) {
      if (emitStandardClassFields && getContainingClass(declaration) && (isPropertyDeclaration(declaration) || isParameterPropertyDeclaration(declaration, declaration.parent))) {
        return !isPropertyImmediatelyReferencedWithinDeclaration(
          declaration,
          usage,
          /*stopAtAnyPropertyDeclaration*/
          true
        );
      } else {
        return true;
      }
    }
    return false;
    function isImmediatelyUsedInInitializerOfBlockScopedVariable(declaration2, usage2) {
      switch (declaration2.parent.parent.kind) {
        case 244 /* VariableStatement */:
        case 249 /* ForStatement */:
        case 251 /* ForOfStatement */:
          if (isSameScopeDescendentOf(usage2, declaration2, declContainer)) {
            return true;
          }
          break;
      }
      const grandparent ***REMOVED*** declaration2.parent.parent;
      return isForInOrOfStatement(grandparent) && isSameScopeDescendentOf(usage2, grandparent.expression, declContainer);
    }
    function isUsedInFunctionOrInstanceProperty(usage2, declaration2) {
      return isUsedInFunctionOrInstancePropertyWorker(usage2, declaration2);
    }
    function isUsedInFunctionOrInstancePropertyWorker(usage2, declaration2) {
      return !!findAncestor(usage2, (current) ***REMOVED***> {
        if (current ***REMOVED******REMOVED******REMOVED*** declContainer) {
          return "quit";
        }
        if (isFunctionLike(current)) {
          return !getImmediatelyInvokedFunctionExpression(current);
        }
        if (isClassStaticBlockDeclaration(current)) {
          return declaration2.pos < usage2.pos;
        }
        const propertyDeclaration ***REMOVED*** tryCast(current.parent, isPropertyDeclaration);
        if (propertyDeclaration) {
          const initializerOfProperty ***REMOVED*** propertyDeclaration.initializer ***REMOVED******REMOVED******REMOVED*** current;
          if (initializerOfProperty) {
            if (isStatic(current.parent)) {
              if (declaration2.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */) {
                return true;
              }
              if (isPropertyDeclaration(declaration2) && getContainingClass(usage2) ***REMOVED******REMOVED******REMOVED*** getContainingClass(declaration2)) {
                const propName ***REMOVED*** declaration2.name;
                if (isIdentifier(propName) || isPrivateIdentifier(propName)) {
                  const type ***REMOVED*** getTypeOfSymbol(getSymbolOfDeclaration(declaration2));
                  const staticBlocks ***REMOVED*** filter(declaration2.parent.members, isClassStaticBlockDeclaration);
                  if (isPropertyInitializedInStaticBlocks(propName, type, staticBlocks, declaration2.parent.pos, current.pos)) {
                    return true;
                  }
                }
              }
            } else {
              const isDeclarationInstanceProperty ***REMOVED*** declaration2.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && !isStatic(declaration2);
              if (!isDeclarationInstanceProperty || getContainingClass(usage2) !***REMOVED******REMOVED*** getContainingClass(declaration2)) {
                return true;
              }
            }
          }
        }
        const decorator ***REMOVED*** tryCast(current.parent, isDecorator);
        if (decorator && decorator.expression ***REMOVED******REMOVED******REMOVED*** current) {
          if (isParameter(decorator.parent)) {
            return isUsedInFunctionOrInstancePropertyWorker(decorator.parent.parent.parent, declaration2) ? true : "quit";
          }
          if (isMethodDeclaration(decorator.parent)) {
            return isUsedInFunctionOrInstancePropertyWorker(decorator.parent.parent, declaration2) ? true : "quit";
          }
        }
        return false;
      });
    }
    function isPropertyImmediatelyReferencedWithinDeclaration(declaration2, usage2, stopAtAnyPropertyDeclaration) {
      if (usage2.end > declaration2.end) {
        return false;
      }
      const ancestorChangingReferenceScope ***REMOVED*** findAncestor(usage2, (node) ***REMOVED***> {
        if (node ***REMOVED******REMOVED******REMOVED*** declaration2) {
          return "quit";
        }
        switch (node.kind) {
          case 220 /* ArrowFunction */:
            return true;
          case 173 /* PropertyDeclaration */:
            return stopAtAnyPropertyDeclaration && (isPropertyDeclaration(declaration2) && node.parent ***REMOVED******REMOVED******REMOVED*** declaration2.parent || isParameterPropertyDeclaration(declaration2, declaration2.parent) && node.parent ***REMOVED******REMOVED******REMOVED*** declaration2.parent.parent) ? "quit" : true;
          case 242 /* Block */:
            switch (node.parent.kind) {
              case 178 /* GetAccessor */:
              case 175 /* MethodDeclaration */:
              case 179 /* SetAccessor */:
                return true;
              default:
                return false;
            }
          default:
            return false;
        }
      });
      return ancestorChangingReferenceScope ***REMOVED******REMOVED******REMOVED*** void 0;
    }
  }
  function getRequiresScopeChangeCache(node) {
    return getNodeLinks(node).declarationRequiresScopeChange;
  }
  function setRequiresScopeChangeCache(node, value) {
    getNodeLinks(node).declarationRequiresScopeChange ***REMOVED*** value;
  }
  function checkAndReportErrorForInvalidInitializer(errorLocation, name, propertyWithInvalidInitializer, result) {
    if (!emitStandardClassFields) {
      if (errorLocation && !result && checkAndReportErrorForMissingPrefix(errorLocation, name, name)) {
        return true;
      }
      error(
        errorLocation,
        errorLocation && propertyWithInvalidInitializer.type && textRangeContainsPositionInclusive(propertyWithInvalidInitializer.type, errorLocation.pos) ? Diagnostics.Type_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor : Diagnostics.Initializer_of_instance_member_variable_0_cannot_reference_identifier_1_declared_in_the_constructor,
        declarationNameToString(propertyWithInvalidInitializer.name),
        diagnosticName(name)
      );
      return true;
    }
    return false;
  }
  function onFailedToResolveSymbol(errorLocation, nameArg, meaning, nameNotFoundMessage) {
    const name ***REMOVED*** isString(nameArg) ? nameArg : nameArg.escapedText;
    addLazyDiagnostic(() ***REMOVED***> {
      if (!errorLocation || errorLocation.parent.kind !***REMOVED******REMOVED*** 325 /* JSDocLink */ && !checkAndReportErrorForMissingPrefix(errorLocation, name, nameArg) && !checkAndReportErrorForExtendingInterface(errorLocation) && !checkAndReportErrorForUsingTypeAsNamespace(errorLocation, name, meaning) && !checkAndReportErrorForExportingPrimitiveType(errorLocation, name) && !checkAndReportErrorForUsingNamespaceAsTypeOrValue(errorLocation, name, meaning) && !checkAndReportErrorForUsingTypeAsValue(errorLocation, name, meaning) && !checkAndReportErrorForUsingValueAsType(errorLocation, name, meaning)) {
        let suggestion;
        let suggestedLib;
        if (nameArg) {
          suggestedLib ***REMOVED*** getSuggestedLibForNonExistentName(nameArg);
          if (suggestedLib) {
            error(errorLocation, nameNotFoundMessage, diagnosticName(nameArg), suggestedLib);
          }
        }
        if (!suggestedLib && suggestionCount < maximumSuggestionCount) {
          suggestion ***REMOVED*** getSuggestedSymbolForNonexistentSymbol(errorLocation, name, meaning);
          const isGlobalScopeAugmentationDeclaration ***REMOVED*** (suggestion ***REMOVED******REMOVED*** null ? void 0 : suggestion.valueDeclaration) && isAmbientModule(suggestion.valueDeclaration) && isGlobalScopeAugmentation(suggestion.valueDeclaration);
          if (isGlobalScopeAugmentationDeclaration) {
            suggestion ***REMOVED*** void 0;
          }
          if (suggestion) {
            const suggestionName ***REMOVED*** symbolToString(suggestion);
            const isUncheckedJS ***REMOVED*** isUncheckedJSSuggestion(
              errorLocation,
              suggestion,
              /*excludeClasses*/
              false
            );
            const message ***REMOVED*** meaning ***REMOVED******REMOVED******REMOVED*** 1920 /* Namespace */ || nameArg && typeof nameArg !***REMOVED******REMOVED*** "string" && nodeIsSynthesized(nameArg) ? Diagnostics.Cannot_find_namespace_0_Did_you_mean_1 : isUncheckedJS ? Diagnostics.Could_not_find_name_0_Did_you_mean_1 : Diagnostics.Cannot_find_name_0_Did_you_mean_1;
            const diagnostic ***REMOVED*** createError(errorLocation, message, diagnosticName(nameArg), suggestionName);
            diagnostic.canonicalHead ***REMOVED*** getCanonicalDiagnostic(nameNotFoundMessage, diagnosticName(nameArg));
            addErrorOrSuggestion(!isUncheckedJS, diagnostic);
            if (suggestion.valueDeclaration) {
              addRelatedInfo(
                diagnostic,
                createDiagnosticForNode(suggestion.valueDeclaration, Diagnostics._0_is_declared_here, suggestionName)
              );
            }
          }
        }
        if (!suggestion && !suggestedLib && nameArg) {
          error(errorLocation, nameNotFoundMessage, diagnosticName(nameArg));
        }
        suggestionCount++;
      }
    });
  }
  function onSuccessfullyResolvedSymbol(errorLocation, result, meaning, lastLocation, associatedDeclarationForContainingInitializerOrBindingName, withinDeferredContext) {
    addLazyDiagnostic(() ***REMOVED***> {
      var _a;
      const name ***REMOVED*** result.escapedName;
      const isInExternalModule ***REMOVED*** lastLocation && isSourceFile(lastLocation) && isExternalOrCommonJsModule(lastLocation);
      if (errorLocation && (meaning & 2 /* BlockScopedVariable */ || (meaning & 32 /* Class */ || meaning & 384 /* Enum */) && (meaning & 111551 /* Value */) ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */)) {
        const exportOrLocalSymbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(result);
        if (exportOrLocalSymbol.flags & 2 /* BlockScopedVariable */ || exportOrLocalSymbol.flags & 32 /* Class */ || exportOrLocalSymbol.flags & 384 /* Enum */) {
          checkResolvedBlockScopedVariable(exportOrLocalSymbol, errorLocation);
        }
      }
      if (isInExternalModule && (meaning & 111551 /* Value */) ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */ && !(errorLocation.flags & 16777216 /* JSDoc */)) {
        const merged ***REMOVED*** getMergedSymbol(result);
        if (length(merged.declarations) && every(merged.declarations, (d) ***REMOVED***> isNamespaceExportDeclaration(d) || isSourceFile(d) && !!d.symbol.globalExports)) {
          errorOrSuggestion(!compilerOptions.allowUmdGlobalAccess, errorLocation, Diagnostics._0_refers_to_a_UMD_global_but_the_current_file_is_a_module_Consider_adding_an_import_instead, unescapeLeadingUnderscores(name));
        }
      }
      if (associatedDeclarationForContainingInitializerOrBindingName && !withinDeferredContext && (meaning & 111551 /* Value */) ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */) {
        const candidate ***REMOVED*** getMergedSymbol(getLateBoundSymbol(result));
        const root ***REMOVED*** getRootDeclaration(associatedDeclarationForContainingInitializerOrBindingName);
        if (candidate ***REMOVED******REMOVED******REMOVED*** getSymbolOfDeclaration(associatedDeclarationForContainingInitializerOrBindingName)) {
          error(errorLocation, Diagnostics.Parameter_0_cannot_reference_itself, declarationNameToString(associatedDeclarationForContainingInitializerOrBindingName.name));
        } else if (candidate.valueDeclaration && candidate.valueDeclaration.pos > associatedDeclarationForContainingInitializerOrBindingName.pos && root.parent.locals && getSymbol(root.parent.locals, candidate.escapedName, meaning) ***REMOVED******REMOVED******REMOVED*** candidate) {
          error(errorLocation, Diagnostics.Parameter_0_cannot_reference_identifier_1_declared_after_it, declarationNameToString(associatedDeclarationForContainingInitializerOrBindingName.name), declarationNameToString(errorLocation));
        }
      }
      if (errorLocation && meaning & 111551 /* Value */ && result.flags & 2097152 /* Alias */ && !(result.flags & 111551 /* Value */) && !isValidTypeOnlyAliasUseSite(errorLocation)) {
        const typeOnlyDeclaration ***REMOVED*** getTypeOnlyAliasDeclaration(result, 111551 /* Value */);
        if (typeOnlyDeclaration) {
          const message ***REMOVED*** typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */ ? Diagnostics._0_cannot_be_used_as_a_value_because_it_was_exported_using_export_type : Diagnostics._0_cannot_be_used_as_a_value_because_it_was_imported_using_import_type;
          const unescapedName ***REMOVED*** unescapeLeadingUnderscores(name);
          addTypeOnlyDeclarationRelatedInfo(
            error(errorLocation, message, unescapedName),
            typeOnlyDeclaration,
            unescapedName
          );
        }
      }
      if (compilerOptions.isolatedModules && result && isInExternalModule && (meaning & 111551 /* Value */) ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */) {
        const isGlobal ***REMOVED*** getSymbol(globals, name, meaning) ***REMOVED******REMOVED******REMOVED*** result;
        const nonValueSymbol ***REMOVED*** isGlobal && isSourceFile(lastLocation) && lastLocation.locals && getSymbol(lastLocation.locals, name, ~111551 /* Value */);
        if (nonValueSymbol) {
          const importDecl ***REMOVED*** (_a ***REMOVED*** nonValueSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find((d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ || d.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ || d.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */ || d.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */);
          if (importDecl && !isTypeOnlyImportDeclaration(importDecl)) {
            error(importDecl, Diagnostics.Import_0_conflicts_with_global_value_used_in_this_file_so_must_be_declared_with_a_type_only_import_when_isolatedModules_is_enabled, unescapeLeadingUnderscores(name));
          }
        }
      }
    });
  }
  function addTypeOnlyDeclarationRelatedInfo(diagnostic, typeOnlyDeclaration, unescapedName) {
    if (!typeOnlyDeclaration) return diagnostic;
    return addRelatedInfo(
      diagnostic,
      createDiagnosticForNode(
        typeOnlyDeclaration,
        typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */ ? Diagnostics._0_was_exported_here : Diagnostics._0_was_imported_here,
        unescapedName
      )
    );
  }
  function diagnosticName(nameArg) {
    return isString(nameArg) ? unescapeLeadingUnderscores(nameArg) : declarationNameToString(nameArg);
  }
  function checkAndReportErrorForMissingPrefix(errorLocation, name, nameArg) {
    if (!isIdentifier(errorLocation) || errorLocation.escapedText !***REMOVED******REMOVED*** name || isTypeReferenceIdentifier(errorLocation) || isInTypeQuery(errorLocation)) {
      return false;
    }
    const container ***REMOVED*** getThisContainer(
      errorLocation,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    let location ***REMOVED*** container;
    while (location) {
      if (isClassLike(location.parent)) {
        const classSymbol ***REMOVED*** getSymbolOfDeclaration(location.parent);
        if (!classSymbol) {
          break;
        }
        const constructorType ***REMOVED*** getTypeOfSymbol(classSymbol);
        if (getPropertyOfType(constructorType, name)) {
          error(errorLocation, Diagnostics.Cannot_find_name_0_Did_you_mean_the_static_member_1_0, diagnosticName(nameArg), symbolToString(classSymbol));
          return true;
        }
        if (location ***REMOVED******REMOVED******REMOVED*** container && !isStatic(location)) {
          const instanceType ***REMOVED*** getDeclaredTypeOfSymbol(classSymbol).thisType;
          if (getPropertyOfType(instanceType, name)) {
            error(errorLocation, Diagnostics.Cannot_find_name_0_Did_you_mean_the_instance_member_this_0, diagnosticName(nameArg));
            return true;
          }
        }
      }
      location ***REMOVED*** location.parent;
    }
    return false;
  }
  function checkAndReportErrorForExtendingInterface(errorLocation) {
    const expression ***REMOVED*** getEntityNameForExtendingInterface(errorLocation);
    if (expression && resolveEntityName(
      expression,
      64 /* Interface */,
      /*ignoreErrors*/
      true
    )) {
      error(errorLocation, Diagnostics.Cannot_extend_an_interface_0_Did_you_mean_implements, getTextOfNode(expression));
      return true;
    }
    return false;
  }
  function getEntityNameForExtendingInterface(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
      case 212 /* PropertyAccessExpression */:
        return node.parent ? getEntityNameForExtendingInterface(node.parent) : void 0;
      case 234 /* ExpressionWithTypeArguments */:
        if (isEntityNameExpression(node.expression)) {
          return node.expression;
        }
      // falls through
      default:
        return void 0;
    }
  }
  function checkAndReportErrorForUsingTypeAsNamespace(errorLocation, name, meaning) {
    const namespaceMeaning ***REMOVED*** 1920 /* Namespace */ | (isInJSFile(errorLocation) ? 111551 /* Value */ : 0);
    if (meaning ***REMOVED******REMOVED******REMOVED*** namespaceMeaning) {
      const symbol ***REMOVED*** resolveSymbol(resolveName(
        errorLocation,
        name,
        788968 /* Type */ & ~namespaceMeaning,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      ));
      const parent ***REMOVED*** errorLocation.parent;
      if (symbol) {
        if (isQualifiedName(parent)) {
          Debug.assert(parent.left ***REMOVED******REMOVED******REMOVED*** errorLocation, "Should only be resolving left side of qualified name as a namespace");
          const propName ***REMOVED*** parent.right.escapedText;
          const propType ***REMOVED*** getPropertyOfType(getDeclaredTypeOfSymbol(symbol), propName);
          if (propType) {
            error(
              parent,
              Diagnostics.Cannot_access_0_1_because_0_is_a_type_but_not_a_namespace_Did_you_mean_to_retrieve_the_type_of_the_property_1_in_0_with_0_1,
              unescapeLeadingUnderscores(name),
              unescapeLeadingUnderscores(propName)
            );
            return true;
          }
        }
        error(errorLocation, Diagnostics._0_only_refers_to_a_type_but_is_being_used_as_a_namespace_here, unescapeLeadingUnderscores(name));
        return true;
      }
    }
    return false;
  }
  function checkAndReportErrorForUsingValueAsType(errorLocation, name, meaning) {
    if (meaning & (788968 /* Type */ & ~1920 /* Namespace */)) {
      const symbol ***REMOVED*** resolveSymbol(resolveName(
        errorLocation,
        name,
        ~788968 /* Type */ & 111551 /* Value */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      ));
      if (symbol && !(symbol.flags & 1920 /* Namespace */)) {
        error(errorLocation, Diagnostics._0_refers_to_a_value_but_is_being_used_as_a_type_here_Did_you_mean_typeof_0, unescapeLeadingUnderscores(name));
        return true;
      }
    }
    return false;
  }
  function isPrimitiveTypeName(name) {
    return name ***REMOVED******REMOVED******REMOVED*** "any" || name ***REMOVED******REMOVED******REMOVED*** "string" || name ***REMOVED******REMOVED******REMOVED*** "number" || name ***REMOVED******REMOVED******REMOVED*** "boolean" || name ***REMOVED******REMOVED******REMOVED*** "never" || name ***REMOVED******REMOVED******REMOVED*** "unknown";
  }
  function checkAndReportErrorForExportingPrimitiveType(errorLocation, name) {
    if (isPrimitiveTypeName(name) && errorLocation.parent.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */) {
      error(errorLocation, Diagnostics.Cannot_export_0_Only_local_declarations_can_be_exported_from_a_module, name);
      return true;
    }
    return false;
  }
  function checkAndReportErrorForUsingTypeAsValue(errorLocation, name, meaning) {
    if (meaning & 111551 /* Value */) {
      if (isPrimitiveTypeName(name)) {
        const grandparent ***REMOVED*** errorLocation.parent.parent;
        if (grandparent && grandparent.parent && isHeritageClause(grandparent)) {
          const heritageKind ***REMOVED*** grandparent.token;
          const containerKind ***REMOVED*** grandparent.parent.kind;
          if (containerKind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ && heritageKind ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
            error(errorLocation, Diagnostics.An_interface_cannot_extend_a_primitive_type_like_0_It_can_only_extend_other_named_object_types, unescapeLeadingUnderscores(name));
          } else if (isClassLike(grandparent.parent) && heritageKind ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
            error(errorLocation, Diagnostics.A_class_cannot_extend_a_primitive_type_like_0_Classes_can_only_extend_constructable_values, unescapeLeadingUnderscores(name));
          } else if (isClassLike(grandparent.parent) && heritageKind ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */) {
            error(errorLocation, Diagnostics.A_class_cannot_implement_a_primitive_type_like_0_It_can_only_implement_other_named_object_types, unescapeLeadingUnderscores(name));
          }
        } else {
          error(errorLocation, Diagnostics._0_only_refers_to_a_type_but_is_being_used_as_a_value_here, unescapeLeadingUnderscores(name));
        }
        return true;
      }
      const symbol ***REMOVED*** resolveSymbol(resolveName(
        errorLocation,
        name,
        788968 /* Type */ & ~111551 /* Value */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      ));
      const allFlags ***REMOVED*** symbol && getSymbolFlags(symbol);
      if (symbol && allFlags !***REMOVED******REMOVED*** void 0 && !(allFlags & 111551 /* Value */)) {
        const rawName ***REMOVED*** unescapeLeadingUnderscores(name);
        if (isES2015OrLaterConstructorName(name)) {
          error(errorLocation, Diagnostics._0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_es2015_or_later, rawName);
        } else if (maybeMappedType(errorLocation, symbol)) {
          error(errorLocation, Diagnostics._0_only_refers_to_a_type_but_is_being_used_as_a_value_here_Did_you_mean_to_use_1_in_0, rawName, rawName ***REMOVED******REMOVED******REMOVED*** "K" ? "P" : "K");
        } else {
          error(errorLocation, Diagnostics._0_only_refers_to_a_type_but_is_being_used_as_a_value_here, rawName);
        }
        return true;
      }
    }
    return false;
  }
  function maybeMappedType(node, symbol) {
    const container ***REMOVED*** findAncestor(node.parent, (n) ***REMOVED***> isComputedPropertyName(n) || isPropertySignature(n) ? false : isTypeLiteralNode(n) || "quit");
    if (container && container.members.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
      return !!(type.flags & 1048576 /* Union */) && allTypesAssignableToKind(
        type,
        384 /* StringOrNumberLiteral */,
        /*strict*/
        true
      );
    }
    return false;
  }
  function isES2015OrLaterConstructorName(n) {
    switch (n) {
      case "Promise":
      case "Symbol":
      case "Map":
      case "WeakMap":
      case "Set":
      case "WeakSet":
        return true;
    }
    return false;
  }
  function checkAndReportErrorForUsingNamespaceAsTypeOrValue(errorLocation, name, meaning) {
    if (meaning & (111551 /* Value */ & ~788968 /* Type */)) {
      const symbol ***REMOVED*** resolveSymbol(resolveName(
        errorLocation,
        name,
        1024 /* NamespaceModule */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      ));
      if (symbol) {
        error(
          errorLocation,
          Diagnostics.Cannot_use_namespace_0_as_a_value,
          unescapeLeadingUnderscores(name)
        );
        return true;
      }
    } else if (meaning & (788968 /* Type */ & ~111551 /* Value */)) {
      const symbol ***REMOVED*** resolveSymbol(resolveName(
        errorLocation,
        name,
        1536 /* Module */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      ));
      if (symbol) {
        error(errorLocation, Diagnostics.Cannot_use_namespace_0_as_a_type, unescapeLeadingUnderscores(name));
        return true;
      }
    }
    return false;
  }
  function checkResolvedBlockScopedVariable(result, errorLocation) {
    var _a;
    Debug.assert(!!(result.flags & 2 /* BlockScopedVariable */ || result.flags & 32 /* Class */ || result.flags & 384 /* Enum */));
    if (result.flags & (16 /* Function */ | 1 /* FunctionScopedVariable */ | 67108864 /* Assignment */) && result.flags & 32 /* Class */) {
      return;
    }
    const declaration ***REMOVED*** (_a ***REMOVED*** result.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(
      (d) ***REMOVED***> isBlockOrCatchScoped(d) || isClassLike(d) || d.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */
    );
    if (declaration ***REMOVED******REMOVED******REMOVED*** void 0) return Debug.fail("checkResolvedBlockScopedVariable could not find block-scoped declaration");
    if (!(declaration.flags & 33554432 /* Ambient */) && !isBlockScopedNameDeclaredBeforeUse(declaration, errorLocation)) {
      let diagnosticMessage;
      const declarationName ***REMOVED*** declarationNameToString(getNameOfDeclaration(declaration));
      if (result.flags & 2 /* BlockScopedVariable */) {
        diagnosticMessage ***REMOVED*** error(errorLocation, Diagnostics.Block_scoped_variable_0_used_before_its_declaration, declarationName);
      } else if (result.flags & 32 /* Class */) {
        diagnosticMessage ***REMOVED*** error(errorLocation, Diagnostics.Class_0_used_before_its_declaration, declarationName);
      } else if (result.flags & 256 /* RegularEnum */) {
        diagnosticMessage ***REMOVED*** error(errorLocation, Diagnostics.Enum_0_used_before_its_declaration, declarationName);
      } else {
        Debug.assert(!!(result.flags & 128 /* ConstEnum */));
        if (getIsolatedModules(compilerOptions)) {
          diagnosticMessage ***REMOVED*** error(errorLocation, Diagnostics.Enum_0_used_before_its_declaration, declarationName);
        }
      }
      if (diagnosticMessage) {
        addRelatedInfo(diagnosticMessage, createDiagnosticForNode(declaration, Diagnostics._0_is_declared_here, declarationName));
      }
    }
  }
  function isSameScopeDescendentOf(initial, parent, stopAt) {
    return !!parent && !!findAncestor(initial, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** parent || (n ***REMOVED******REMOVED******REMOVED*** stopAt || isFunctionLike(n) && (!getImmediatelyInvokedFunctionExpression(n) || getFunctionFlags(n) & 3 /* AsyncGenerator */) ? "quit" : false));
  }
  function getAnyImportSyntax(node) {
    switch (node.kind) {
      case 272 /* ImportEqualsDeclaration */:
        return node;
      case 274 /* ImportClause */:
        return node.parent;
      case 275 /* NamespaceImport */:
        return node.parent.parent;
      case 277 /* ImportSpecifier */:
        return node.parent.parent.parent;
      default:
        return void 0;
    }
  }
  function getDeclarationOfAliasSymbol(symbol) {
    return symbol.declarations && findLast(symbol.declarations, isAliasSymbolDeclaration);
  }
  function isAliasSymbolDeclaration(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ && !!node.name || node.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */ || node.kind ***REMOVED******REMOVED******REMOVED*** 281 /* NamespaceExport */ || node.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ && exportAssignmentIsAlias(node) || isBinaryExpression(node) && getAssignmentDeclarationKind(node) ***REMOVED******REMOVED******REMOVED*** 2 /* ModuleExports */ && exportAssignmentIsAlias(node) || isAccessExpression(node) && isBinaryExpression(node.parent) && node.parent.left ***REMOVED******REMOVED******REMOVED*** node && node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && isAliasableOrJsExpression(node.parent.right) || node.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ && isAliasableOrJsExpression(node.initializer) || node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && isVariableDeclarationInitializedToBareOrAccessedRequire(node) || node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && isVariableDeclarationInitializedToBareOrAccessedRequire(node.parent.parent);
  }
  function isAliasableOrJsExpression(e) {
    return isAliasableExpression(e) || isFunctionExpression(e) && isJSConstructor(e);
  }
  function getTargetOfImportEqualsDeclaration(node, dontResolveAlias) {
    const commonJSPropertyAccess ***REMOVED*** getCommonJSPropertyAccess(node);
    if (commonJSPropertyAccess) {
      const name ***REMOVED*** getLeftmostAccessExpression(commonJSPropertyAccess.expression).arguments[0];
      return isIdentifier(commonJSPropertyAccess.name) ? resolveSymbol(getPropertyOfType(resolveExternalModuleTypeByLiteral(name), commonJSPropertyAccess.name.escapedText)) : void 0;
    }
    if (isVariableDeclaration(node) || node.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */) {
      const immediate ***REMOVED*** resolveExternalModuleName(
        node,
        getExternalModuleRequireArgument(node) || getExternalModuleImportEqualsDeclarationExpression(node)
      );
      const resolved2 ***REMOVED*** resolveExternalModuleSymbol(immediate);
      if (resolved2 && 102 /* Node20 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) {
        const moduleExports ***REMOVED*** getExportOfModule(resolved2, "module.exports", node, dontResolveAlias);
        if (moduleExports) {
          return moduleExports;
        }
      }
      markSymbolOfAliasDeclarationIfTypeOnly(
        node,
        immediate,
        resolved2,
        /*overwriteEmpty*/
        false
      );
      return resolved2;
    }
    const resolved ***REMOVED*** getSymbolOfPartOfRightHandSideOfImportEquals(node.moduleReference, dontResolveAlias);
    checkAndReportErrorForResolvingImportAliasToTypeOnlySymbol(node, resolved);
    return resolved;
  }
  function checkAndReportErrorForResolvingImportAliasToTypeOnlySymbol(node, resolved) {
    if (markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      /*immediateTarget*/
      void 0,
      resolved,
      /*overwriteEmpty*/
      false
    ) && !node.isTypeOnly) {
      const typeOnlyDeclaration ***REMOVED*** getTypeOnlyAliasDeclaration(getSymbolOfDeclaration(node));
      const isExport ***REMOVED*** typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ || typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */;
      const message ***REMOVED*** isExport ? Diagnostics.An_import_alias_cannot_reference_a_declaration_that_was_exported_using_export_type : Diagnostics.An_import_alias_cannot_reference_a_declaration_that_was_imported_using_import_type;
      const relatedMessage ***REMOVED*** isExport ? Diagnostics._0_was_exported_here : Diagnostics._0_was_imported_here;
      const name ***REMOVED*** typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ ? "*" : moduleExportNameTextUnescaped(typeOnlyDeclaration.name);
      addRelatedInfo(error(node.moduleReference, message), createDiagnosticForNode(typeOnlyDeclaration, relatedMessage, name));
    }
  }
  function resolveExportByName(moduleSymbol, name, sourceNode, dontResolveAlias) {
    const exportValue ***REMOVED*** moduleSymbol.exports.get("export***REMOVED***" /* ExportEquals */);
    const exportSymbol ***REMOVED*** exportValue ? getPropertyOfType(
      getTypeOfSymbol(exportValue),
      name,
      /*skipObjectFunctionPropertyAugment*/
      true
    ) : moduleSymbol.exports.get(name);
    const resolved ***REMOVED*** resolveSymbol(exportSymbol, dontResolveAlias);
    markSymbolOfAliasDeclarationIfTypeOnly(
      sourceNode,
      exportSymbol,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function isSyntacticDefault(node) {
    return isExportAssignment(node) && !node.isExportEquals || hasSyntacticModifier(node, 2048 /* Default */) || isExportSpecifier(node) || isNamespaceExport(node);
  }
  function getEmitSyntaxForModuleSpecifierExpression(usage) {
    return isStringLiteralLike(usage) ? host.getEmitSyntaxForUsageLocation(getSourceFileOfNode(usage), usage) : void 0;
  }
  function isESMFormatImportImportingCommonjsFormatFile(usageMode, targetMode) {
    return usageMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && targetMode ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */;
  }
  function isOnlyImportableAsDefault(usage, resolvedModule) {
    if (100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) {
      const usageMode ***REMOVED*** getEmitSyntaxForModuleSpecifierExpression(usage);
      if (usageMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */) {
        resolvedModule ?? (resolvedModule ***REMOVED*** resolveExternalModuleName(
          usage,
          usage,
          /*ignoreErrors*/
          true
        ));
        const targetFile ***REMOVED*** resolvedModule && getSourceFileOfModule(resolvedModule);
        return targetFile && (isJsonSourceFile(targetFile) || getDeclarationFileExtension(targetFile.fileName) ***REMOVED******REMOVED******REMOVED*** ".d.json.ts");
      }
    }
    return false;
  }
  function canHaveSyntheticDefault(file, moduleSymbol, dontResolveAlias, usage) {
    const usageMode ***REMOVED*** file && getEmitSyntaxForModuleSpecifierExpression(usage);
    if (file && usageMode !***REMOVED******REMOVED*** void 0) {
      const targetMode ***REMOVED*** host.getImpliedNodeFormatForEmit(file);
      if (usageMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && targetMode ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && 100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) {
        return true;
      }
      if (usageMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && targetMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */) {
        return false;
      }
    }
    if (!allowSyntheticDefaultImports) {
      return false;
    }
    if (!file || file.isDeclarationFile) {
      const defaultExportSymbol ***REMOVED*** resolveExportByName(
        moduleSymbol,
        "default" /* Default */,
        /*sourceNode*/
        void 0,
        /*dontResolveAlias*/
        true
      );
      if (defaultExportSymbol && some(defaultExportSymbol.declarations, isSyntacticDefault)) {
        return false;
      }
      if (resolveExportByName(
        moduleSymbol,
        escapeLeadingUnderscores("__esModule"),
        /*sourceNode*/
        void 0,
        dontResolveAlias
      )) {
        return false;
      }
      return true;
    }
    if (!isSourceFileJS(file)) {
      return hasExportAssignmentSymbol(moduleSymbol);
    }
    return typeof file.externalModuleIndicator !***REMOVED******REMOVED*** "object" && !resolveExportByName(
      moduleSymbol,
      escapeLeadingUnderscores("__esModule"),
      /*sourceNode*/
      void 0,
      dontResolveAlias
    );
  }
  function getTargetOfImportClause(node, dontResolveAlias) {
    const moduleSymbol ***REMOVED*** resolveExternalModuleName(node, node.parent.moduleSpecifier);
    if (moduleSymbol) {
      return getTargetofModuleDefault(moduleSymbol, node, dontResolveAlias);
    }
  }
  function getTargetofModuleDefault(moduleSymbol, node, dontResolveAlias) {
    var _a;
    const file ***REMOVED*** (_a ***REMOVED*** moduleSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isSourceFile);
    const specifier ***REMOVED*** getModuleSpecifierForImportOrExport(node);
    let exportDefaultSymbol;
    let exportModuleDotExportsSymbol;
    if (isShorthandAmbientModuleSymbol(moduleSymbol)) {
      exportDefaultSymbol ***REMOVED*** moduleSymbol;
    } else if (file && specifier && 102 /* Node20 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ && getEmitSyntaxForModuleSpecifierExpression(specifier) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && host.getImpliedNodeFormatForEmit(file) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && (exportModuleDotExportsSymbol ***REMOVED*** resolveExportByName(moduleSymbol, "module.exports", node, dontResolveAlias))) {
      if (!getESModuleInterop(compilerOptions)) {
        error(node.name, Diagnostics.Module_0_can_only_be_default_imported_using_the_1_flag, symbolToString(moduleSymbol), "esModuleInterop");
        return void 0;
      }
      markSymbolOfAliasDeclarationIfTypeOnly(
        node,
        exportModuleDotExportsSymbol,
        /*finalTarget*/
        void 0,
        /*overwriteEmpty*/
        false
      );
      return exportModuleDotExportsSymbol;
    } else {
      exportDefaultSymbol ***REMOVED*** resolveExportByName(moduleSymbol, "default" /* Default */, node, dontResolveAlias);
    }
    if (!specifier) {
      return exportDefaultSymbol;
    }
    const hasDefaultOnly ***REMOVED*** isOnlyImportableAsDefault(specifier, moduleSymbol);
    const hasSyntheticDefault ***REMOVED*** canHaveSyntheticDefault(file, moduleSymbol, dontResolveAlias, specifier);
    if (!exportDefaultSymbol && !hasSyntheticDefault && !hasDefaultOnly) {
      if (hasExportAssignmentSymbol(moduleSymbol) && !allowSyntheticDefaultImports) {
        const compilerOptionName ***REMOVED*** moduleKind >***REMOVED*** 5 /* ES2015 */ ? "allowSyntheticDefaultImports" : "esModuleInterop";
        const exportEqualsSymbol ***REMOVED*** moduleSymbol.exports.get("export***REMOVED***" /* ExportEquals */);
        const exportAssignment ***REMOVED*** exportEqualsSymbol.valueDeclaration;
        const err ***REMOVED*** error(node.name, Diagnostics.Module_0_can_only_be_default_imported_using_the_1_flag, symbolToString(moduleSymbol), compilerOptionName);
        if (exportAssignment) {
          addRelatedInfo(
            err,
            createDiagnosticForNode(
              exportAssignment,
              Diagnostics.This_module_is_declared_with_export_and_can_only_be_used_with_a_default_import_when_using_the_0_flag,
              compilerOptionName
            )
          );
        }
      } else if (isImportClause(node)) {
        reportNonDefaultExport(moduleSymbol, node);
      } else {
        errorNoModuleMemberSymbol(moduleSymbol, moduleSymbol, node, isImportOrExportSpecifier(node) && node.propertyName || node.name);
      }
    } else if (hasSyntheticDefault || hasDefaultOnly) {
      const resolved ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol, dontResolveAlias) || resolveSymbol(moduleSymbol, dontResolveAlias);
      markSymbolOfAliasDeclarationIfTypeOnly(
        node,
        moduleSymbol,
        resolved,
        /*overwriteEmpty*/
        false
      );
      return resolved;
    }
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      exportDefaultSymbol,
      /*finalTarget*/
      void 0,
      /*overwriteEmpty*/
      false
    );
    return exportDefaultSymbol;
  }
  function getModuleSpecifierForImportOrExport(node) {
    switch (node.kind) {
      case 274 /* ImportClause */:
        return node.parent.moduleSpecifier;
      case 272 /* ImportEqualsDeclaration */:
        return isExternalModuleReference(node.moduleReference) ? node.moduleReference.expression : void 0;
      case 275 /* NamespaceImport */:
        return node.parent.parent.moduleSpecifier;
      case 277 /* ImportSpecifier */:
        return node.parent.parent.parent.moduleSpecifier;
      case 282 /* ExportSpecifier */:
        return node.parent.parent.moduleSpecifier;
      default:
        return Debug.assertNever(node);
    }
  }
  function reportNonDefaultExport(moduleSymbol, node) {
    var _a, _b, _c;
    if ((_a ***REMOVED*** moduleSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.has(node.symbol.escapedName)) {
      error(
        node.name,
        Diagnostics.Module_0_has_no_default_export_Did_you_mean_to_use_import_1_from_0_instead,
        symbolToString(moduleSymbol),
        symbolToString(node.symbol)
      );
    } else {
      const diagnostic ***REMOVED*** error(node.name, Diagnostics.Module_0_has_no_default_export, symbolToString(moduleSymbol));
      const exportStar ***REMOVED*** (_b ***REMOVED*** moduleSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.get("__export" /* ExportStar */);
      if (exportStar) {
        const defaultExport ***REMOVED*** (_c ***REMOVED*** exportStar.declarations) ***REMOVED******REMOVED*** null ? void 0 : _c.find(
          (decl) ***REMOVED***> {
            var _a2, _b2;
            return !!(isExportDeclaration(decl) && decl.moduleSpecifier && ((_b2 ***REMOVED*** (_a2 ***REMOVED*** resolveExternalModuleName(decl, decl.moduleSpecifier)) ***REMOVED******REMOVED*** null ? void 0 : _a2.exports) ***REMOVED******REMOVED*** null ? void 0 : _b2.has("default" /* Default */)));
          }
        );
        if (defaultExport) {
          addRelatedInfo(diagnostic, createDiagnosticForNode(defaultExport, Diagnostics.export_Asterisk_does_not_re_export_a_default));
        }
      }
    }
  }
  function getTargetOfNamespaceImport(node, dontResolveAlias) {
    const moduleSpecifier ***REMOVED*** node.parent.parent.moduleSpecifier;
    const immediate ***REMOVED*** resolveExternalModuleName(node, moduleSpecifier);
    const resolved ***REMOVED*** resolveESModuleSymbol(
      immediate,
      moduleSpecifier,
      dontResolveAlias,
      /*suppressInteropError*/
      false
    );
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      immediate,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function getTargetOfNamespaceExport(node, dontResolveAlias) {
    const moduleSpecifier ***REMOVED*** node.parent.moduleSpecifier;
    const immediate ***REMOVED*** moduleSpecifier && resolveExternalModuleName(node, moduleSpecifier);
    const resolved ***REMOVED*** moduleSpecifier && resolveESModuleSymbol(
      immediate,
      moduleSpecifier,
      dontResolveAlias,
      /*suppressInteropError*/
      false
    );
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      immediate,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function combineValueAndTypeSymbols(valueSymbol, typeSymbol) {
    if (valueSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol && typeSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return unknownSymbol;
    }
    if (valueSymbol.flags & (788968 /* Type */ | 1920 /* Namespace */)) {
      return valueSymbol;
    }
    const result ***REMOVED*** createSymbol(valueSymbol.flags | typeSymbol.flags, valueSymbol.escapedName);
    Debug.assert(valueSymbol.declarations || typeSymbol.declarations);
    result.declarations ***REMOVED*** deduplicate(concatenate(valueSymbol.declarations, typeSymbol.declarations), equateValues);
    result.parent ***REMOVED*** valueSymbol.parent || typeSymbol.parent;
    if (valueSymbol.valueDeclaration) result.valueDeclaration ***REMOVED*** valueSymbol.valueDeclaration;
    if (typeSymbol.members) result.members ***REMOVED*** new Map(typeSymbol.members);
    if (valueSymbol.exports) result.exports ***REMOVED*** new Map(valueSymbol.exports);
    return result;
  }
  function getExportOfModule(symbol, nameText, specifier, dontResolveAlias) {
    var _a;
    if (symbol.flags & 1536 /* Module */) {
      const exportSymbol ***REMOVED*** getExportsOfSymbol(symbol).get(nameText);
      const resolved ***REMOVED*** resolveSymbol(exportSymbol, dontResolveAlias);
      const exportStarDeclaration ***REMOVED*** (_a ***REMOVED*** getSymbolLinks(symbol).typeOnlyExportStarMap) ***REMOVED******REMOVED*** null ? void 0 : _a.get(nameText);
      markSymbolOfAliasDeclarationIfTypeOnly(
        specifier,
        exportSymbol,
        resolved,
        /*overwriteEmpty*/
        false,
        exportStarDeclaration,
        nameText
      );
      return resolved;
    }
  }
  function getPropertyOfVariable(symbol, name) {
    if (symbol.flags & 3 /* Variable */) {
      const typeAnnotation ***REMOVED*** symbol.valueDeclaration.type;
      if (typeAnnotation) {
        return resolveSymbol(getPropertyOfType(getTypeFromTypeNode(typeAnnotation), name));
      }
    }
  }
  function getExternalModuleMember(node, specifier, dontResolveAlias ***REMOVED*** false) {
    var _a;
    const moduleSpecifier ***REMOVED*** getExternalModuleRequireArgument(node) || node.moduleSpecifier;
    const moduleSymbol ***REMOVED*** resolveExternalModuleName(node, moduleSpecifier);
    const name ***REMOVED*** !isPropertyAccessExpression(specifier) && specifier.propertyName || specifier.name;
    if (!isIdentifier(name) && name.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
      return void 0;
    }
    const nameText ***REMOVED*** moduleExportNameTextEscaped(name);
    const suppressInteropError ***REMOVED*** nameText ***REMOVED******REMOVED******REMOVED*** "default" /* Default */ && allowSyntheticDefaultImports;
    const targetSymbol ***REMOVED*** resolveESModuleSymbol(
      moduleSymbol,
      moduleSpecifier,
      /*dontResolveAlias*/
      false,
      suppressInteropError
    );
    if (targetSymbol) {
      if (nameText || name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
        if (isShorthandAmbientModuleSymbol(moduleSymbol)) {
          return moduleSymbol;
        }
        let symbolFromVariable;
        if (moduleSymbol && moduleSymbol.exports && moduleSymbol.exports.get("export***REMOVED***" /* ExportEquals */)) {
          symbolFromVariable ***REMOVED*** getPropertyOfType(
            getTypeOfSymbol(targetSymbol),
            nameText,
            /*skipObjectFunctionPropertyAugment*/
            true
          );
        } else {
          symbolFromVariable ***REMOVED*** getPropertyOfVariable(targetSymbol, nameText);
        }
        symbolFromVariable ***REMOVED*** resolveSymbol(symbolFromVariable, dontResolveAlias);
        let symbolFromModule ***REMOVED*** getExportOfModule(targetSymbol, nameText, specifier, dontResolveAlias);
        if (symbolFromModule ***REMOVED******REMOVED******REMOVED*** void 0 && nameText ***REMOVED******REMOVED******REMOVED*** "default" /* Default */) {
          const file ***REMOVED*** (_a ***REMOVED*** moduleSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isSourceFile);
          if (isOnlyImportableAsDefault(moduleSpecifier, moduleSymbol) || canHaveSyntheticDefault(file, moduleSymbol, dontResolveAlias, moduleSpecifier)) {
            symbolFromModule ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol, dontResolveAlias) || resolveSymbol(moduleSymbol, dontResolveAlias);
          }
        }
        const symbol ***REMOVED*** symbolFromModule && symbolFromVariable && symbolFromModule !***REMOVED******REMOVED*** symbolFromVariable ? combineValueAndTypeSymbols(symbolFromVariable, symbolFromModule) : symbolFromModule || symbolFromVariable;
        if (isImportOrExportSpecifier(specifier) && isOnlyImportableAsDefault(moduleSpecifier, moduleSymbol) && nameText !***REMOVED******REMOVED*** "default" /* Default */) {
          error(name, Diagnostics.Named_imports_from_a_JSON_file_into_an_ECMAScript_module_are_not_allowed_when_module_is_set_to_0, ModuleKind[moduleKind]);
        } else if (!symbol) {
          errorNoModuleMemberSymbol(moduleSymbol, targetSymbol, node, name);
        }
        return symbol;
      }
    }
  }
  function errorNoModuleMemberSymbol(moduleSymbol, targetSymbol, node, name) {
    var _a;
    const moduleName ***REMOVED*** getFullyQualifiedName(moduleSymbol, node);
    const declarationName ***REMOVED*** declarationNameToString(name);
    const suggestion ***REMOVED*** isIdentifier(name) ? getSuggestedSymbolForNonexistentModule(name, targetSymbol) : void 0;
    if (suggestion !***REMOVED******REMOVED*** void 0) {
      const suggestionName ***REMOVED*** symbolToString(suggestion);
      const diagnostic ***REMOVED*** error(name, Diagnostics._0_has_no_exported_member_named_1_Did_you_mean_2, moduleName, declarationName, suggestionName);
      if (suggestion.valueDeclaration) {
        addRelatedInfo(diagnostic, createDiagnosticForNode(suggestion.valueDeclaration, Diagnostics._0_is_declared_here, suggestionName));
      }
    } else {
      if ((_a ***REMOVED*** moduleSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.has("default" /* Default */)) {
        error(
          name,
          Diagnostics.Module_0_has_no_exported_member_1_Did_you_mean_to_use_import_1_from_0_instead,
          moduleName,
          declarationName
        );
      } else {
        reportNonExportedMember(node, name, declarationName, moduleSymbol, moduleName);
      }
    }
  }
  function reportNonExportedMember(node, name, declarationName, moduleSymbol, moduleName) {
    var _a, _b;
    const localSymbol ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** tryCast(moduleSymbol.valueDeclaration, canHaveLocals)) ***REMOVED******REMOVED*** null ? void 0 : _a.locals) ***REMOVED******REMOVED*** null ? void 0 : _b.get(moduleExportNameTextEscaped(name));
    const exports2 ***REMOVED*** moduleSymbol.exports;
    if (localSymbol) {
      const exportedEqualsSymbol ***REMOVED*** exports2 ***REMOVED******REMOVED*** null ? void 0 : exports2.get("export***REMOVED***" /* ExportEquals */);
      if (exportedEqualsSymbol) {
        getSymbolIfSameReference(exportedEqualsSymbol, localSymbol) ? reportInvalidImportEqualsExportMember(node, name, declarationName, moduleName) : error(name, Diagnostics.Module_0_has_no_exported_member_1, moduleName, declarationName);
      } else {
        const exportedSymbol ***REMOVED*** exports2 ? find(symbolsToArray(exports2), (symbol) ***REMOVED***> !!getSymbolIfSameReference(symbol, localSymbol)) : void 0;
        const diagnostic ***REMOVED*** exportedSymbol ? error(name, Diagnostics.Module_0_declares_1_locally_but_it_is_exported_as_2, moduleName, declarationName, symbolToString(exportedSymbol)) : error(name, Diagnostics.Module_0_declares_1_locally_but_it_is_not_exported, moduleName, declarationName);
        if (localSymbol.declarations) {
          addRelatedInfo(diagnostic, ...map(localSymbol.declarations, (decl, index) ***REMOVED***> createDiagnosticForNode(decl, index ***REMOVED******REMOVED******REMOVED*** 0 ? Diagnostics._0_is_declared_here : Diagnostics.and_here, declarationName)));
        }
      }
    } else {
      error(name, Diagnostics.Module_0_has_no_exported_member_1, moduleName, declarationName);
    }
  }
  function reportInvalidImportEqualsExportMember(node, name, declarationName, moduleName) {
    if (moduleKind >***REMOVED*** 5 /* ES2015 */) {
      const message ***REMOVED*** getESModuleInterop(compilerOptions) ? Diagnostics._0_can_only_be_imported_by_using_a_default_import : Diagnostics._0_can_only_be_imported_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import;
      error(name, message, declarationName);
    } else {
      if (isInJSFile(node)) {
        const message ***REMOVED*** getESModuleInterop(compilerOptions) ? Diagnostics._0_can_only_be_imported_by_using_a_require_call_or_by_using_a_default_import : Diagnostics._0_can_only_be_imported_by_using_a_require_call_or_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import;
        error(name, message, declarationName);
      } else {
        const message ***REMOVED*** getESModuleInterop(compilerOptions) ? Diagnostics._0_can_only_be_imported_by_using_import_1_require_2_or_a_default_import : Diagnostics._0_can_only_be_imported_by_using_import_1_require_2_or_by_turning_on_the_esModuleInterop_flag_and_using_a_default_import;
        error(name, message, declarationName, declarationName, moduleName);
      }
    }
  }
  function getTargetOfImportSpecifier(node, dontResolveAlias) {
    if (isImportSpecifier(node) && moduleExportNameIsDefault(node.propertyName || node.name)) {
      const specifier ***REMOVED*** getModuleSpecifierForImportOrExport(node);
      const moduleSymbol ***REMOVED*** specifier && resolveExternalModuleName(node, specifier);
      if (moduleSymbol) {
        return getTargetofModuleDefault(moduleSymbol, node, dontResolveAlias);
      }
    }
    const root ***REMOVED*** isBindingElement(node) ? getRootDeclaration(node) : node.parent.parent.parent;
    const commonJSPropertyAccess ***REMOVED*** getCommonJSPropertyAccess(root);
    const resolved ***REMOVED*** getExternalModuleMember(root, commonJSPropertyAccess || node, dontResolveAlias);
    const name ***REMOVED*** node.propertyName || node.name;
    if (commonJSPropertyAccess && resolved && isIdentifier(name)) {
      return resolveSymbol(getPropertyOfType(getTypeOfSymbol(resolved), name.escapedText), dontResolveAlias);
    }
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      /*immediateTarget*/
      void 0,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function getCommonJSPropertyAccess(node) {
    if (isVariableDeclaration(node) && node.initializer && isPropertyAccessExpression(node.initializer)) {
      return node.initializer;
    }
  }
  function getTargetOfNamespaceExportDeclaration(node, dontResolveAlias) {
    if (canHaveSymbol(node.parent)) {
      const resolved ***REMOVED*** resolveExternalModuleSymbol(node.parent.symbol, dontResolveAlias);
      markSymbolOfAliasDeclarationIfTypeOnly(
        node,
        /*immediateTarget*/
        void 0,
        resolved,
        /*overwriteEmpty*/
        false
      );
      return resolved;
    }
  }
  function getTargetOfExportSpecifier(node, meaning, dontResolveAlias) {
    const name ***REMOVED*** node.propertyName || node.name;
    if (moduleExportNameIsDefault(name)) {
      const specifier ***REMOVED*** getModuleSpecifierForImportOrExport(node);
      const moduleSymbol ***REMOVED*** specifier && resolveExternalModuleName(node, specifier);
      if (moduleSymbol) {
        return getTargetofModuleDefault(moduleSymbol, node, !!dontResolveAlias);
      }
    }
    const resolved ***REMOVED*** node.parent.parent.moduleSpecifier ? getExternalModuleMember(node.parent.parent, node, dontResolveAlias) : name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? void 0 : (
      // Skip for invalid syntax like this: export { "x" }
      resolveEntityName(
        name,
        meaning,
        /*ignoreErrors*/
        false,
        dontResolveAlias
      )
    );
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      /*immediateTarget*/
      void 0,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function getTargetOfExportAssignment(node, dontResolveAlias) {
    const expression ***REMOVED*** isExportAssignment(node) ? node.expression : node.right;
    const resolved ***REMOVED*** getTargetOfAliasLikeExpression(expression, dontResolveAlias);
    markSymbolOfAliasDeclarationIfTypeOnly(
      node,
      /*immediateTarget*/
      void 0,
      resolved,
      /*overwriteEmpty*/
      false
    );
    return resolved;
  }
  function getTargetOfAliasLikeExpression(expression, dontResolveAlias) {
    if (isClassExpression(expression)) {
      return checkExpressionCached(expression).symbol;
    }
    if (!isEntityName(expression) && !isEntityNameExpression(expression)) {
      return void 0;
    }
    const aliasLike ***REMOVED*** resolveEntityName(
      expression,
      111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */,
      /*ignoreErrors*/
      true,
      dontResolveAlias
    );
    if (aliasLike) {
      return aliasLike;
    }
    checkExpressionCached(expression);
    return getNodeLinks(expression).resolvedSymbol;
  }
  function getTargetOfAccessExpression(node, dontRecursivelyResolve) {
    if (!(isBinaryExpression(node.parent) && node.parent.left ***REMOVED******REMOVED******REMOVED*** node && node.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */)) {
      return void 0;
    }
    return getTargetOfAliasLikeExpression(node.parent.right, dontRecursivelyResolve);
  }
  function getTargetOfAliasDeclaration(node, dontRecursivelyResolve ***REMOVED*** false) {
    switch (node.kind) {
      case 272 /* ImportEqualsDeclaration */:
      case 261 /* VariableDeclaration */:
        return getTargetOfImportEqualsDeclaration(node, dontRecursivelyResolve);
      case 274 /* ImportClause */:
        return getTargetOfImportClause(node, dontRecursivelyResolve);
      case 275 /* NamespaceImport */:
        return getTargetOfNamespaceImport(node, dontRecursivelyResolve);
      case 281 /* NamespaceExport */:
        return getTargetOfNamespaceExport(node, dontRecursivelyResolve);
      case 277 /* ImportSpecifier */:
      case 209 /* BindingElement */:
        return getTargetOfImportSpecifier(node, dontRecursivelyResolve);
      case 282 /* ExportSpecifier */:
        return getTargetOfExportSpecifier(node, 111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */, dontRecursivelyResolve);
      case 278 /* ExportAssignment */:
      case 227 /* BinaryExpression */:
        return getTargetOfExportAssignment(node, dontRecursivelyResolve);
      case 271 /* NamespaceExportDeclaration */:
        return getTargetOfNamespaceExportDeclaration(node, dontRecursivelyResolve);
      case 305 /* ShorthandPropertyAssignment */:
        return resolveEntityName(
          node.name,
          111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */,
          /*ignoreErrors*/
          true,
          dontRecursivelyResolve
        );
      case 304 /* PropertyAssignment */:
        return getTargetOfAliasLikeExpression(node.initializer, dontRecursivelyResolve);
      case 213 /* ElementAccessExpression */:
      case 212 /* PropertyAccessExpression */:
        return getTargetOfAccessExpression(node, dontRecursivelyResolve);
      default:
        return Debug.fail();
    }
  }
  function isNonLocalAlias(symbol, excludes ***REMOVED*** 111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */) {
    if (!symbol) return false;
    return (symbol.flags & (2097152 /* Alias */ | excludes)) ***REMOVED******REMOVED******REMOVED*** 2097152 /* Alias */ || !!(symbol.flags & 2097152 /* Alias */ && symbol.flags & 67108864 /* Assignment */);
  }
  function resolveSymbol(symbol, dontResolveAlias) {
    return !dontResolveAlias && isNonLocalAlias(symbol) ? resolveAlias(symbol) : symbol;
  }
  function resolveAlias(symbol) {
    Debug.assert((symbol.flags & 2097152 /* Alias */) !***REMOVED******REMOVED*** 0, "Should only get Alias here.");
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.aliasTarget) {
      links.aliasTarget ***REMOVED*** resolvingSymbol;
      const node ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
      if (!node) return Debug.fail();
      const target ***REMOVED*** getTargetOfAliasDeclaration(node);
      if (links.aliasTarget ***REMOVED******REMOVED******REMOVED*** resolvingSymbol) {
        links.aliasTarget ***REMOVED*** target || unknownSymbol;
      } else {
        error(node, Diagnostics.Circular_definition_of_import_alias_0, symbolToString(symbol));
      }
    } else if (links.aliasTarget ***REMOVED******REMOVED******REMOVED*** resolvingSymbol) {
      links.aliasTarget ***REMOVED*** unknownSymbol;
    }
    return links.aliasTarget;
  }
  function tryResolveAlias(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (links.aliasTarget !***REMOVED******REMOVED*** resolvingSymbol) {
      return resolveAlias(symbol);
    }
    return void 0;
  }
  function getSymbolFlags(symbol, excludeTypeOnlyMeanings, excludeLocalMeanings) {
    const typeOnlyDeclaration ***REMOVED*** excludeTypeOnlyMeanings && getTypeOnlyAliasDeclaration(symbol);
    const typeOnlyDeclarationIsExportStar ***REMOVED*** typeOnlyDeclaration && isExportDeclaration(typeOnlyDeclaration);
    const typeOnlyResolution ***REMOVED*** typeOnlyDeclaration && (typeOnlyDeclarationIsExportStar ? resolveExternalModuleName(
      typeOnlyDeclaration.moduleSpecifier,
      typeOnlyDeclaration.moduleSpecifier,
      /*ignoreErrors*/
      true
    ) : resolveAlias(typeOnlyDeclaration.symbol));
    const typeOnlyExportStarTargets ***REMOVED*** typeOnlyDeclarationIsExportStar && typeOnlyResolution ? getExportsOfModule(typeOnlyResolution) : void 0;
    let flags ***REMOVED*** excludeLocalMeanings ? 0 /* None */ : symbol.flags;
    let seenSymbols;
    while (symbol.flags & 2097152 /* Alias */) {
      const target ***REMOVED*** getExportSymbolOfValueSymbolIfExported(resolveAlias(symbol));
      if (!typeOnlyDeclarationIsExportStar && target ***REMOVED******REMOVED******REMOVED*** typeOnlyResolution || (typeOnlyExportStarTargets ***REMOVED******REMOVED*** null ? void 0 : typeOnlyExportStarTargets.get(target.escapedName)) ***REMOVED******REMOVED******REMOVED*** target) {
        break;
      }
      if (target ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
        return -1 /* All */;
      }
      if (target ***REMOVED******REMOVED******REMOVED*** symbol || (seenSymbols ***REMOVED******REMOVED*** null ? void 0 : seenSymbols.has(target))) {
        break;
      }
      if (target.flags & 2097152 /* Alias */) {
        if (seenSymbols) {
          seenSymbols.add(target);
        } else {
          seenSymbols ***REMOVED*** /* @__PURE__ */ new Set([symbol, target]);
        }
      }
      flags |***REMOVED*** target.flags;
      symbol ***REMOVED*** target;
    }
    return flags;
  }
  function markSymbolOfAliasDeclarationIfTypeOnly(aliasDeclaration, immediateTarget, finalTarget, overwriteEmpty, exportStarDeclaration, exportStarName) {
    if (!aliasDeclaration || isPropertyAccessExpression(aliasDeclaration)) return false;
    const sourceSymbol ***REMOVED*** getSymbolOfDeclaration(aliasDeclaration);
    if (isTypeOnlyImportOrExportDeclaration(aliasDeclaration)) {
      const links2 ***REMOVED*** getSymbolLinks(sourceSymbol);
      links2.typeOnlyDeclaration ***REMOVED*** aliasDeclaration;
      return true;
    }
    if (exportStarDeclaration) {
      const links2 ***REMOVED*** getSymbolLinks(sourceSymbol);
      links2.typeOnlyDeclaration ***REMOVED*** exportStarDeclaration;
      if (sourceSymbol.escapedName !***REMOVED******REMOVED*** exportStarName) {
        links2.typeOnlyExportStarName ***REMOVED*** exportStarName;
      }
      return true;
    }
    const links ***REMOVED*** getSymbolLinks(sourceSymbol);
    return markSymbolOfAliasDeclarationIfTypeOnlyWorker(links, immediateTarget, overwriteEmpty) || markSymbolOfAliasDeclarationIfTypeOnlyWorker(links, finalTarget, overwriteEmpty);
  }
  function markSymbolOfAliasDeclarationIfTypeOnlyWorker(aliasDeclarationLinks, target, overwriteEmpty) {
    var _a;
    if (target && (aliasDeclarationLinks.typeOnlyDeclaration ***REMOVED******REMOVED******REMOVED*** void 0 || overwriteEmpty && aliasDeclarationLinks.typeOnlyDeclaration ***REMOVED******REMOVED******REMOVED*** false)) {
      const exportSymbol ***REMOVED*** ((_a ***REMOVED*** target.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.get("export***REMOVED***" /* ExportEquals */)) ?? target;
      const typeOnly ***REMOVED*** exportSymbol.declarations && find(exportSymbol.declarations, isTypeOnlyImportOrExportDeclaration);
      aliasDeclarationLinks.typeOnlyDeclaration ***REMOVED*** typeOnly ?? getSymbolLinks(exportSymbol).typeOnlyDeclaration ?? false;
    }
    return !!aliasDeclarationLinks.typeOnlyDeclaration;
  }
  function getTypeOnlyAliasDeclaration(symbol, include) {
    var _a;
    if (!(symbol.flags & 2097152 /* Alias */)) {
      return void 0;
    }
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (links.typeOnlyDeclaration ***REMOVED******REMOVED******REMOVED*** void 0) {
      links.typeOnlyDeclaration ***REMOVED*** false;
      const resolved ***REMOVED*** resolveSymbol(symbol);
      markSymbolOfAliasDeclarationIfTypeOnly(
        (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0],
        getDeclarationOfAliasSymbol(symbol) && getImmediateAliasedSymbol(symbol),
        resolved,
        /*overwriteEmpty*/
        true
      );
    }
    if (include ***REMOVED******REMOVED******REMOVED*** void 0) {
      return links.typeOnlyDeclaration || void 0;
    }
    if (links.typeOnlyDeclaration) {
      const resolved ***REMOVED*** links.typeOnlyDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ ? resolveSymbol(getExportsOfModule(links.typeOnlyDeclaration.symbol.parent).get(links.typeOnlyExportStarName || symbol.escapedName)) : resolveAlias(links.typeOnlyDeclaration.symbol);
      return getSymbolFlags(resolved) & include ? links.typeOnlyDeclaration : void 0;
    }
    return void 0;
  }
  function getSymbolOfPartOfRightHandSideOfImportEquals(entityName, dontResolveAlias) {
    if (entityName.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && isRightSideOfQualifiedNameOrPropertyAccess(entityName)) {
      entityName ***REMOVED*** entityName.parent;
    }
    if (entityName.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
      return resolveEntityName(
        entityName,
        1920 /* Namespace */,
        /*ignoreErrors*/
        false,
        dontResolveAlias
      );
    } else {
      Debug.assert(entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */);
      return resolveEntityName(
        entityName,
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */,
        /*ignoreErrors*/
        false,
        dontResolveAlias
      );
    }
  }
  function getFullyQualifiedName(symbol, containingLocation) {
    return symbol.parent ? getFullyQualifiedName(symbol.parent, containingLocation) + "." + symbolToString(symbol) : symbolToString(
      symbol,
      containingLocation,
      /*meaning*/
      void 0,
      32 /* DoNotIncludeSymbolChain */ | 4 /* AllowAnyNodeKind */
    );
  }
  function getContainingQualifiedNameNode(node) {
    while (isQualifiedName(node.parent)) {
      node ***REMOVED*** node.parent;
    }
    return node;
  }
  function tryGetQualifiedNameAsValue(node) {
    let left ***REMOVED*** getFirstIdentifier(node);
    let symbol ***REMOVED*** resolveName(
      left,
      left,
      111551 /* Value */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true
    );
    if (!symbol) {
      return void 0;
    }
    while (isQualifiedName(left.parent)) {
      const type ***REMOVED*** getTypeOfSymbol(symbol);
      symbol ***REMOVED*** getPropertyOfType(type, left.parent.right.escapedText);
      if (!symbol) {
        return void 0;
      }
      left ***REMOVED*** left.parent;
    }
    return symbol;
  }
  function resolveEntityName(name, meaning, ignoreErrors, dontResolveAlias, location) {
    if (nodeIsMissing(name)) {
      return void 0;
    }
    const namespaceMeaning ***REMOVED*** 1920 /* Namespace */ | (isInJSFile(name) ? meaning & 111551 /* Value */ : 0);
    let symbol;
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const message ***REMOVED*** meaning ***REMOVED******REMOVED******REMOVED*** namespaceMeaning || nodeIsSynthesized(name) ? Diagnostics.Cannot_find_namespace_0 : getCannotFindNameDiagnosticForName(getFirstIdentifier(name));
      const symbolFromJSPrototype ***REMOVED*** isInJSFile(name) && !nodeIsSynthesized(name) ? resolveEntityNameFromAssignmentDeclaration(name, meaning) : void 0;
      symbol ***REMOVED*** getMergedSymbol(resolveName(
        location || name,
        name,
        meaning,
        ignoreErrors || symbolFromJSPrototype ? void 0 : message,
        /*isUse*/
        true,
        /*excludeGlobals*/
        false
      ));
      if (!symbol) {
        return getMergedSymbol(symbolFromJSPrototype);
      }
    } else if (name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || name.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
      const left ***REMOVED*** name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? name.left : name.expression;
      const right ***REMOVED*** name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? name.right : name.name;
      let namespace ***REMOVED*** resolveEntityName(
        left,
        namespaceMeaning,
        ignoreErrors,
        /*dontResolveAlias*/
        false,
        location
      );
      if (!namespace || nodeIsMissing(right)) {
        return void 0;
      } else if (namespace ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
        return namespace;
      }
      if (namespace.valueDeclaration && isInJSFile(namespace.valueDeclaration) && getEmitModuleResolutionKind(compilerOptions) !***REMOVED******REMOVED*** 100 /* Bundler */ && isVariableDeclaration(namespace.valueDeclaration) && namespace.valueDeclaration.initializer && isCommonJsRequire(namespace.valueDeclaration.initializer)) {
        const moduleName ***REMOVED*** namespace.valueDeclaration.initializer.arguments[0];
        const moduleSym ***REMOVED*** resolveExternalModuleName(moduleName, moduleName);
        if (moduleSym) {
          const resolvedModuleSymbol ***REMOVED*** resolveExternalModuleSymbol(moduleSym);
          if (resolvedModuleSymbol) {
            namespace ***REMOVED*** resolvedModuleSymbol;
          }
        }
      }
      symbol ***REMOVED*** getMergedSymbol(getSymbol(getExportsOfSymbol(namespace), right.escapedText, meaning));
      if (!symbol && namespace.flags & 2097152 /* Alias */) {
        symbol ***REMOVED*** getMergedSymbol(getSymbol(getExportsOfSymbol(resolveAlias(namespace)), right.escapedText, meaning));
      }
      if (!symbol) {
        if (!ignoreErrors) {
          const namespaceName ***REMOVED*** getFullyQualifiedName(namespace);
          const declarationName ***REMOVED*** declarationNameToString(right);
          const suggestionForNonexistentModule ***REMOVED*** getSuggestedSymbolForNonexistentModule(right, namespace);
          if (suggestionForNonexistentModule) {
            error(right, Diagnostics._0_has_no_exported_member_named_1_Did_you_mean_2, namespaceName, declarationName, symbolToString(suggestionForNonexistentModule));
            return void 0;
          }
          const containingQualifiedName ***REMOVED*** isQualifiedName(name) && getContainingQualifiedNameNode(name);
          const canSuggestTypeof ***REMOVED*** globalObjectType && meaning & 788968 /* Type */ && containingQualifiedName && !isTypeOfExpression(containingQualifiedName.parent) && tryGetQualifiedNameAsValue(containingQualifiedName);
          if (canSuggestTypeof) {
            error(
              containingQualifiedName,
              Diagnostics._0_refers_to_a_value_but_is_being_used_as_a_type_here_Did_you_mean_typeof_0,
              entityNameToString(containingQualifiedName)
            );
            return void 0;
          }
          if (meaning & 1920 /* Namespace */ && isQualifiedName(name.parent)) {
            const exportedTypeSymbol ***REMOVED*** getMergedSymbol(getSymbol(getExportsOfSymbol(namespace), right.escapedText, 788968 /* Type */));
            if (exportedTypeSymbol) {
              error(
                name.parent.right,
                Diagnostics.Cannot_access_0_1_because_0_is_a_type_but_not_a_namespace_Did_you_mean_to_retrieve_the_type_of_the_property_1_in_0_with_0_1,
                symbolToString(exportedTypeSymbol),
                unescapeLeadingUnderscores(name.parent.right.escapedText)
              );
              return void 0;
            }
          }
          error(right, Diagnostics.Namespace_0_has_no_exported_member_1, namespaceName, declarationName);
        }
        return void 0;
      }
    } else {
      Debug.assertNever(name, "Unknown entity name kind.");
    }
    if (!nodeIsSynthesized(name) && isEntityName(name) && (symbol.flags & 2097152 /* Alias */ || name.parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */)) {
      markSymbolOfAliasDeclarationIfTypeOnly(
        getAliasDeclarationFromName(name),
        symbol,
        /*finalTarget*/
        void 0,
        /*overwriteEmpty*/
        true
      );
    }
    return symbol.flags & meaning || dontResolveAlias ? symbol : resolveAlias(symbol);
  }
  function resolveEntityNameFromAssignmentDeclaration(name, meaning) {
    if (isJSDocTypeReference(name.parent)) {
      const secondaryLocation ***REMOVED*** getAssignmentDeclarationLocation(name.parent);
      if (secondaryLocation) {
        return resolveName(
          secondaryLocation,
          name,
          meaning,
          /*nameNotFoundMessage*/
          void 0,
          /*isUse*/
          true
        );
      }
    }
  }
  function getAssignmentDeclarationLocation(node) {
    const typeAlias ***REMOVED*** findAncestor(node, (node2) ***REMOVED***> !(isJSDocNode(node2) || node2.flags & 16777216 /* JSDoc */) ? "quit" : isJSDocTypeAlias(node2));
    if (typeAlias) {
      return;
    }
    const host2 ***REMOVED*** getJSDocHost(node);
    if (host2 && isExpressionStatement(host2) && isPrototypePropertyAssignment(host2.expression)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(host2.expression.left);
      if (symbol) {
        return getDeclarationOfJSPrototypeContainer(symbol);
      }
    }
    if (host2 && isFunctionExpression(host2) && isPrototypePropertyAssignment(host2.parent) && isExpressionStatement(host2.parent.parent)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(host2.parent.left);
      if (symbol) {
        return getDeclarationOfJSPrototypeContainer(symbol);
      }
    }
    if (host2 && (isObjectLiteralMethod(host2) || isPropertyAssignment(host2)) && isBinaryExpression(host2.parent.parent) && getAssignmentDeclarationKind(host2.parent.parent) ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(host2.parent.parent.left);
      if (symbol) {
        return getDeclarationOfJSPrototypeContainer(symbol);
      }
    }
    const sig ***REMOVED*** getEffectiveJSDocHost(node);
    if (sig && isFunctionLike(sig)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(sig);
      return symbol && symbol.valueDeclaration;
    }
  }
  function getDeclarationOfJSPrototypeContainer(symbol) {
    const decl ***REMOVED*** symbol.parent.valueDeclaration;
    if (!decl) {
      return void 0;
    }
    const initializer ***REMOVED*** isAssignmentDeclaration(decl) ? getAssignedExpandoInitializer(decl) : hasOnlyExpressionInitializer(decl) ? getDeclaredExpandoInitializer(decl) : void 0;
    return initializer || decl;
  }
  function getExpandoSymbol(symbol) {
    const decl ***REMOVED*** symbol.valueDeclaration;
    if (!decl || !isInJSFile(decl) || symbol.flags & 524288 /* TypeAlias */ || getExpandoInitializer(
      decl,
      /*isPrototypeAssignment*/
      false
    )) {
      return void 0;
    }
    const init ***REMOVED*** isVariableDeclaration(decl) ? getDeclaredExpandoInitializer(decl) : getAssignedExpandoInitializer(decl);
    if (init) {
      const initSymbol ***REMOVED*** getSymbolOfNode(init);
      if (initSymbol) {
        return mergeJSSymbols(initSymbol, symbol);
      }
    }
  }
  function resolveExternalModuleName(location, moduleReferenceExpression, ignoreErrors) {
    const isClassic ***REMOVED*** getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 1 /* Classic */;
    const errorMessage ***REMOVED*** isClassic ? Diagnostics.Cannot_find_module_0_Did_you_mean_to_set_the_moduleResolution_option_to_nodenext_or_to_add_aliases_to_the_paths_option : Diagnostics.Cannot_find_module_0_or_its_corresponding_type_declarations;
    return resolveExternalModuleNameWorker(location, moduleReferenceExpression, ignoreErrors ? void 0 : errorMessage, ignoreErrors);
  }
  function resolveExternalModuleNameWorker(location, moduleReferenceExpression, moduleNotFoundError, ignoreErrors ***REMOVED*** false, isForAugmentation ***REMOVED*** false) {
    return isStringLiteralLike(moduleReferenceExpression) ? resolveExternalModule(location, moduleReferenceExpression.text, moduleNotFoundError, !ignoreErrors ? moduleReferenceExpression : void 0, isForAugmentation) : void 0;
  }
  function resolveExternalModule(location, moduleReference, moduleNotFoundError, errorNode, isForAugmentation ***REMOVED*** false) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    if (errorNode && startsWith(moduleReference, "@types/")) {
      const diag2 ***REMOVED*** Diagnostics.Cannot_import_type_declaration_files_Consider_importing_0_instead_of_1;
      const withoutAtTypePrefix ***REMOVED*** removePrefix(moduleReference, "@types/");
      error(errorNode, diag2, withoutAtTypePrefix, moduleReference);
    }
    const ambientModule ***REMOVED*** tryFindAmbientModule(
      moduleReference,
      /*withAugmentations*/
      true
    );
    if (ambientModule) {
      return ambientModule;
    }
    const currentSourceFile ***REMOVED*** getSourceFileOfNode(location);
    const contextSpecifier ***REMOVED*** isStringLiteralLike(location) ? location : ((_a ***REMOVED*** isModuleDeclaration(location) ? location : location.parent && isModuleDeclaration(location.parent) && location.parent.name ***REMOVED******REMOVED******REMOVED*** location ? location.parent : void 0) ***REMOVED******REMOVED*** null ? void 0 : _a.name) || ((_b ***REMOVED*** isLiteralImportTypeNode(location) ? location : void 0) ***REMOVED******REMOVED*** null ? void 0 : _b.argument.literal) || (isVariableDeclaration(location) && location.initializer && isRequireCall(
      location.initializer,
      /*requireStringLiteralLikeArgument*/
      true
    ) ? location.initializer.arguments[0] : void 0) || ((_c ***REMOVED*** findAncestor(location, isImportCall)) ***REMOVED******REMOVED*** null ? void 0 : _c.arguments[0]) || ((_d ***REMOVED*** findAncestor(location, or(isImportDeclaration, isJSDocImportTag, isExportDeclaration))) ***REMOVED******REMOVED*** null ? void 0 : _d.moduleSpecifier) || ((_e ***REMOVED*** findAncestor(location, isExternalModuleImportEqualsDeclaration)) ***REMOVED******REMOVED*** null ? void 0 : _e.moduleReference.expression);
    const mode ***REMOVED*** contextSpecifier && isStringLiteralLike(contextSpecifier) ? host.getModeForUsageLocation(currentSourceFile, contextSpecifier) : host.getDefaultResolutionModeForFile(currentSourceFile);
    const moduleResolutionKind ***REMOVED*** getEmitModuleResolutionKind(compilerOptions);
    const resolvedModule ***REMOVED*** (_f ***REMOVED*** host.getResolvedModule(currentSourceFile, moduleReference, mode)) ***REMOVED******REMOVED*** null ? void 0 : _f.resolvedModule;
    const resolutionDiagnostic ***REMOVED*** errorNode && resolvedModule && getResolutionDiagnostic(compilerOptions, resolvedModule, currentSourceFile);
    const sourceFile ***REMOVED*** resolvedModule && (!resolutionDiagnostic || resolutionDiagnostic ***REMOVED******REMOVED******REMOVED*** Diagnostics.Module_0_was_resolved_to_1_but_jsx_is_not_set) && host.getSourceFile(resolvedModule.resolvedFileName);
    if (sourceFile) {
      if (resolutionDiagnostic) {
        error(errorNode, resolutionDiagnostic, moduleReference, resolvedModule.resolvedFileName);
      }
      if (resolvedModule.resolvedUsingTsExtension && isDeclarationFileName(moduleReference)) {
        const importOrExport ***REMOVED*** ((_g ***REMOVED*** findAncestor(location, isImportDeclaration)) ***REMOVED******REMOVED*** null ? void 0 : _g.importClause) || findAncestor(location, or(isImportEqualsDeclaration, isExportDeclaration));
        if (errorNode && importOrExport && !importOrExport.isTypeOnly || findAncestor(location, isImportCall)) {
          error(
            errorNode,
            Diagnostics.A_declaration_file_cannot_be_imported_without_import_type_Did_you_mean_to_import_an_implementation_file_0_instead,
            getSuggestedImportSource(Debug.checkDefined(tryExtractTSExtension(moduleReference)))
          );
        }
      } else if (resolvedModule.resolvedUsingTsExtension && !shouldAllowImportingTsExtension(compilerOptions, currentSourceFile.fileName)) {
        const importOrExport ***REMOVED*** ((_h ***REMOVED*** findAncestor(location, isImportDeclaration)) ***REMOVED******REMOVED*** null ? void 0 : _h.importClause) || findAncestor(location, or(isImportEqualsDeclaration, isExportDeclaration));
        if (errorNode && !((importOrExport ***REMOVED******REMOVED*** null ? void 0 : importOrExport.isTypeOnly) || findAncestor(location, isImportTypeNode))) {
          const tsExtension ***REMOVED*** Debug.checkDefined(tryExtractTSExtension(moduleReference));
          error(errorNode, Diagnostics.An_import_path_can_only_end_with_a_0_extension_when_allowImportingTsExtensions_is_enabled, tsExtension);
        }
      } else if (compilerOptions.rewriteRelativeImportExtensions && !(location.flags & 33554432 /* Ambient */) && !isDeclarationFileName(moduleReference) && !isLiteralImportTypeNode(location) && !isPartOfTypeOnlyImportOrExportDeclaration(location)) {
        const shouldRewrite ***REMOVED*** shouldRewriteModuleSpecifier(moduleReference, compilerOptions);
        if (!resolvedModule.resolvedUsingTsExtension && shouldRewrite) {
          error(
            errorNode,
            Diagnostics.This_relative_import_path_is_unsafe_to_rewrite_because_it_looks_like_a_file_name_but_actually_resolves_to_0,
            getRelativePathFromFile(getNormalizedAbsolutePath(currentSourceFile.fileName, host.getCurrentDirectory()), resolvedModule.resolvedFileName, hostGetCanonicalFileName(host))
          );
        } else if (resolvedModule.resolvedUsingTsExtension && !shouldRewrite && sourceFileMayBeEmitted(sourceFile, host)) {
          error(
            errorNode,
            Diagnostics.This_import_uses_a_0_extension_to_resolve_to_an_input_TypeScript_file_but_will_not_be_rewritten_during_emit_because_it_is_not_a_relative_path,
            getAnyExtensionFromPath(moduleReference)
          );
        } else if (resolvedModule.resolvedUsingTsExtension && shouldRewrite) {
          const redirect ***REMOVED*** (_i ***REMOVED*** host.getRedirectFromSourceFile(sourceFile.path)) ***REMOVED******REMOVED*** null ? void 0 : _i.resolvedRef;
          if (redirect) {
            const ignoreCase ***REMOVED*** !host.useCaseSensitiveFileNames();
            const ownRootDir ***REMOVED*** host.getCommonSourceDirectory();
            const otherRootDir ***REMOVED*** getCommonSourceDirectoryOfConfig(redirect.commandLine, ignoreCase);
            const rootDirPath ***REMOVED*** getRelativePathFromDirectory(ownRootDir, otherRootDir, ignoreCase);
            const outDirPath ***REMOVED*** getRelativePathFromDirectory(compilerOptions.outDir || ownRootDir, redirect.commandLine.options.outDir || otherRootDir, ignoreCase);
            if (rootDirPath !***REMOVED******REMOVED*** outDirPath) {
              error(
                errorNode,
                Diagnostics.This_import_path_is_unsafe_to_rewrite_because_it_resolves_to_another_project_and_the_relative_path_between_the_projects_output_files_is_not_the_same_as_the_relative_path_between_its_input_files
              );
            }
          }
        }
      }
      if (sourceFile.symbol) {
        if (errorNode && resolvedModule.isExternalLibraryImport && !resolutionExtensionIsTSOrJson(resolvedModule.extension)) {
          errorOnImplicitAnyModule(
            /*isError*/
            false,
            errorNode,
            currentSourceFile,
            mode,
            resolvedModule,
            moduleReference
          );
        }
        if (errorNode && (moduleKind ***REMOVED******REMOVED******REMOVED*** 100 /* Node16 */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 101 /* Node18 */)) {
          const isSyncImport ***REMOVED*** currentSourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && !findAncestor(location, isImportCall) || !!findAncestor(location, isImportEqualsDeclaration);
          const overrideHost ***REMOVED*** findAncestor(location, (l) ***REMOVED***> isImportTypeNode(l) || isExportDeclaration(l) || isImportDeclaration(l) || isJSDocImportTag(l));
          if (isSyncImport && sourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && !hasResolutionModeOverride(overrideHost)) {
            if (findAncestor(location, isImportEqualsDeclaration)) {
              error(errorNode, Diagnostics.Module_0_cannot_be_imported_using_this_construct_The_specifier_only_resolves_to_an_ES_module_which_cannot_be_imported_with_require_Use_an_ECMAScript_import_instead, moduleReference);
            } else {
              let diagnosticDetails;
              const ext ***REMOVED*** tryGetExtensionFromPath2(currentSourceFile.fileName);
              if (ext ***REMOVED******REMOVED******REMOVED*** ".ts" /* Ts */ || ext ***REMOVED******REMOVED******REMOVED*** ".js" /* Js */ || ext ***REMOVED******REMOVED******REMOVED*** ".tsx" /* Tsx */ || ext ***REMOVED******REMOVED******REMOVED*** ".jsx" /* Jsx */) {
                diagnosticDetails ***REMOVED*** createModeMismatchDetails(currentSourceFile);
              }
              const message ***REMOVED*** (overrideHost ***REMOVED******REMOVED*** null ? void 0 : overrideHost.kind) ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ && ((_j ***REMOVED*** overrideHost.importClause) ***REMOVED******REMOVED*** null ? void 0 : _j.isTypeOnly) ? Diagnostics.Type_only_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribute : (overrideHost ***REMOVED******REMOVED*** null ? void 0 : overrideHost.kind) ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */ ? Diagnostics.Type_import_of_an_ECMAScript_module_from_a_CommonJS_module_must_have_a_resolution_mode_attribute : Diagnostics.The_current_file_is_a_CommonJS_module_whose_imports_will_produce_require_calls_however_the_referenced_file_is_an_ECMAScript_module_and_cannot_be_imported_with_require_Consider_writing_a_dynamic_import_0_call_instead;
              diagnostics.add(createDiagnosticForNodeFromMessageChain(
                getSourceFileOfNode(errorNode),
                errorNode,
                chainDiagnosticMessages(diagnosticDetails, message, moduleReference)
              ));
            }
          }
        }
        return getMergedSymbol(sourceFile.symbol);
      }
      if (errorNode && moduleNotFoundError && !isSideEffectImport(errorNode)) {
        error(errorNode, Diagnostics.File_0_is_not_a_module, sourceFile.fileName);
      }
      return void 0;
    }
    if (patternAmbientModules) {
      const pattern ***REMOVED*** findBestPatternMatch(patternAmbientModules, (_) ***REMOVED***> _.pattern, moduleReference);
      if (pattern) {
        const augmentation ***REMOVED*** patternAmbientModuleAugmentations && patternAmbientModuleAugmentations.get(moduleReference);
        if (augmentation) {
          return getMergedSymbol(augmentation);
        }
        return getMergedSymbol(pattern.symbol);
      }
    }
    if (!errorNode) {
      return void 0;
    }
    if (resolvedModule && !resolutionExtensionIsTSOrJson(resolvedModule.extension) && resolutionDiagnostic ***REMOVED******REMOVED******REMOVED*** void 0 || resolutionDiagnostic ***REMOVED******REMOVED******REMOVED*** Diagnostics.Could_not_find_a_declaration_file_for_module_0_1_implicitly_has_an_any_type) {
      if (isForAugmentation) {
        const diag2 ***REMOVED*** Diagnostics.Invalid_module_name_in_augmentation_Module_0_resolves_to_an_untyped_module_at_1_which_cannot_be_augmented;
        error(errorNode, diag2, moduleReference, resolvedModule.resolvedFileName);
      } else {
        errorOnImplicitAnyModule(
          /*isError*/
          noImplicitAny && !!moduleNotFoundError,
          errorNode,
          currentSourceFile,
          mode,
          resolvedModule,
          moduleReference
        );
      }
      return void 0;
    }
    if (moduleNotFoundError) {
      if (resolvedModule) {
        const redirect ***REMOVED*** host.getRedirectFromSourceFile(resolvedModule.resolvedFileName);
        if (redirect ***REMOVED******REMOVED*** null ? void 0 : redirect.outputDts) {
          error(errorNode, Diagnostics.Output_file_0_has_not_been_built_from_source_file_1, redirect.outputDts, resolvedModule.resolvedFileName);
          return void 0;
        }
      }
      if (resolutionDiagnostic) {
        error(errorNode, resolutionDiagnostic, moduleReference, resolvedModule.resolvedFileName);
      } else {
        const isExtensionlessRelativePathImport ***REMOVED*** pathIsRelative(moduleReference) && !hasExtension(moduleReference);
        const resolutionIsNode16OrNext ***REMOVED*** moduleResolutionKind ***REMOVED******REMOVED******REMOVED*** 3 /* Node16 */ || moduleResolutionKind ***REMOVED******REMOVED******REMOVED*** 99 /* NodeNext */;
        if (!getResolveJsonModule(compilerOptions) && fileExtensionIs(moduleReference, ".json" /* Json */) && moduleResolutionKind !***REMOVED******REMOVED*** 1 /* Classic */ && hasJsonModuleEmitEnabled(compilerOptions)) {
          error(errorNode, Diagnostics.Cannot_find_module_0_Consider_using_resolveJsonModule_to_import_module_with_json_extension, moduleReference);
        } else if (mode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && resolutionIsNode16OrNext && isExtensionlessRelativePathImport) {
          const absoluteRef ***REMOVED*** getNormalizedAbsolutePath(moduleReference, getDirectoryPath(currentSourceFile.path));
          const suggestedExt ***REMOVED*** (_k ***REMOVED*** suggestedExtensions.find(([actualExt, _importExt]) ***REMOVED***> host.fileExists(absoluteRef + actualExt))) ***REMOVED******REMOVED*** null ? void 0 : _k[1];
          if (suggestedExt) {
            error(errorNode, Diagnostics.Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_node16_or_nodenext_Did_you_mean_0, moduleReference + suggestedExt);
          } else {
            error(errorNode, Diagnostics.Relative_import_paths_need_explicit_file_extensions_in_ECMAScript_imports_when_moduleResolution_is_node16_or_nodenext_Consider_adding_an_extension_to_the_import_path);
          }
        } else {
          if ((_l ***REMOVED*** host.getResolvedModule(currentSourceFile, moduleReference, mode)) ***REMOVED******REMOVED*** null ? void 0 : _l.alternateResult) {
            const errorInfo ***REMOVED*** createModuleNotFoundChain(currentSourceFile, host, moduleReference, mode, moduleReference);
            errorOrSuggestion(
              /*isError*/
              true,
              errorNode,
              chainDiagnosticMessages(errorInfo, moduleNotFoundError, moduleReference)
            );
          } else {
            error(errorNode, moduleNotFoundError, moduleReference);
          }
        }
      }
    }
    return void 0;
    function getSuggestedImportSource(tsExtension) {
      const importSourceWithoutExtension ***REMOVED*** removeExtension(moduleReference, tsExtension);
      if (emitModuleKindIsNonNodeESM(moduleKind) || mode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */) {
        const preferTs ***REMOVED*** isDeclarationFileName(moduleReference) && shouldAllowImportingTsExtension(compilerOptions);
        const ext ***REMOVED*** tsExtension ***REMOVED******REMOVED******REMOVED*** ".mts" /* Mts */ || tsExtension ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */ ? preferTs ? ".mts" : ".mjs" : tsExtension ***REMOVED******REMOVED******REMOVED*** ".cts" /* Cts */ || tsExtension ***REMOVED******REMOVED******REMOVED*** ".d.mts" /* Dmts */ ? preferTs ? ".cts" : ".cjs" : preferTs ? ".ts" : ".js";
        return importSourceWithoutExtension + ext;
      }
      return importSourceWithoutExtension;
    }
  }
  function errorOnImplicitAnyModule(isError, errorNode, sourceFile, mode, { packageId, resolvedFileName }, moduleReference) {
    if (isSideEffectImport(errorNode)) {
      return;
    }
    let errorInfo;
    if (!isExternalModuleNameRelative(moduleReference) && packageId) {
      errorInfo ***REMOVED*** createModuleNotFoundChain(sourceFile, host, moduleReference, mode, packageId.name);
    }
    errorOrSuggestion(
      isError,
      errorNode,
      chainDiagnosticMessages(
        errorInfo,
        Diagnostics.Could_not_find_a_declaration_file_for_module_0_1_implicitly_has_an_any_type,
        moduleReference,
        resolvedFileName
      )
    );
  }
  function resolveExternalModuleSymbol(moduleSymbol, dontResolveAlias) {
    if (moduleSymbol ***REMOVED******REMOVED*** null ? void 0 : moduleSymbol.exports) {
      const exportEquals ***REMOVED*** resolveSymbol(moduleSymbol.exports.get("export***REMOVED***" /* ExportEquals */), dontResolveAlias);
      const exported ***REMOVED*** getCommonJsExportEquals(getMergedSymbol(exportEquals), getMergedSymbol(moduleSymbol));
      return getMergedSymbol(exported) || moduleSymbol;
    }
    return void 0;
  }
  function getCommonJsExportEquals(exported, moduleSymbol) {
    if (!exported || exported ***REMOVED******REMOVED******REMOVED*** unknownSymbol || exported ***REMOVED******REMOVED******REMOVED*** moduleSymbol || moduleSymbol.exports.size ***REMOVED******REMOVED******REMOVED*** 1 || exported.flags & 2097152 /* Alias */) {
      return exported;
    }
    const links ***REMOVED*** getSymbolLinks(exported);
    if (links.cjsExportMerged) {
      return links.cjsExportMerged;
    }
    const merged ***REMOVED*** exported.flags & 33554432 /* Transient */ ? exported : cloneSymbol(exported);
    merged.flags ***REMOVED*** merged.flags | 512 /* ValueModule */;
    if (merged.exports ***REMOVED******REMOVED******REMOVED*** void 0) {
      merged.exports ***REMOVED*** createSymbolTable();
    }
    moduleSymbol.exports.forEach((s, name) ***REMOVED***> {
      if (name ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) return;
      merged.exports.set(name, merged.exports.has(name) ? mergeSymbol(merged.exports.get(name), s) : s);
    });
    if (merged ***REMOVED******REMOVED******REMOVED*** exported) {
      getSymbolLinks(merged).resolvedExports ***REMOVED*** void 0;
      getSymbolLinks(merged).resolvedMembers ***REMOVED*** void 0;
    }
    getSymbolLinks(merged).cjsExportMerged ***REMOVED*** merged;
    return links.cjsExportMerged ***REMOVED*** merged;
  }
  function resolveESModuleSymbol(moduleSymbol, referencingLocation, dontResolveAlias, suppressInteropError) {
    var _a;
    const symbol ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol, dontResolveAlias);
    if (!dontResolveAlias && symbol) {
      if (!suppressInteropError && !(symbol.flags & (1536 /* Module */ | 3 /* Variable */)) && !getDeclarationOfKind(symbol, 308 /* SourceFile */)) {
        const compilerOptionName ***REMOVED*** moduleKind >***REMOVED*** 5 /* ES2015 */ ? "allowSyntheticDefaultImports" : "esModuleInterop";
        error(referencingLocation, Diagnostics.This_module_can_only_be_referenced_with_ECMAScript_imports_Slashexports_by_turning_on_the_0_flag_and_referencing_its_default_export, compilerOptionName);
        return symbol;
      }
      const referenceParent ***REMOVED*** referencingLocation.parent;
      const namespaceImport ***REMOVED*** isImportDeclaration(referenceParent) && getNamespaceDeclarationNode(referenceParent);
      if (namespaceImport || isImportCall(referenceParent)) {
        const reference ***REMOVED*** isImportCall(referenceParent) ? referenceParent.arguments[0] : referenceParent.moduleSpecifier;
        const type ***REMOVED*** getTypeOfSymbol(symbol);
        const defaultOnlyType ***REMOVED*** getTypeWithSyntheticDefaultOnly(type, symbol, moduleSymbol, reference);
        if (defaultOnlyType) {
          return cloneTypeAsModuleType(symbol, defaultOnlyType, referenceParent);
        }
        const targetFile ***REMOVED*** (_a ***REMOVED*** moduleSymbol ***REMOVED******REMOVED*** null ? void 0 : moduleSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isSourceFile);
        const usageMode ***REMOVED*** getEmitSyntaxForModuleSpecifierExpression(reference);
        let exportModuleDotExportsSymbol;
        if (namespaceImport && targetFile && 102 /* Node20 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ && usageMode ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && host.getImpliedNodeFormatForEmit(targetFile) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && (exportModuleDotExportsSymbol ***REMOVED*** resolveExportByName(symbol, "module.exports", namespaceImport, dontResolveAlias))) {
          if (!suppressInteropError && !(symbol.flags & (1536 /* Module */ | 3 /* Variable */))) {
            error(referencingLocation, Diagnostics.This_module_can_only_be_referenced_with_ECMAScript_imports_Slashexports_by_turning_on_the_0_flag_and_referencing_its_default_export, "esModuleInterop");
          }
          if (getESModuleInterop(compilerOptions) && hasSignatures(type)) {
            return cloneTypeAsModuleType(exportModuleDotExportsSymbol, type, referenceParent);
          }
          return exportModuleDotExportsSymbol;
        }
        const isEsmCjsRef ***REMOVED*** targetFile && isESMFormatImportImportingCommonjsFormatFile(usageMode, host.getImpliedNodeFormatForEmit(targetFile));
        if (getESModuleInterop(compilerOptions) || isEsmCjsRef) {
          if (hasSignatures(type) || getPropertyOfType(
            type,
            "default" /* Default */,
            /*skipObjectFunctionPropertyAugment*/
            true
          ) || isEsmCjsRef) {
            const moduleType ***REMOVED*** type.flags & 3670016 /* StructuredType */ ? getTypeWithSyntheticDefaultImportType(type, symbol, moduleSymbol, reference) : createDefaultPropertyWrapperForModule(symbol, symbol.parent);
            return cloneTypeAsModuleType(symbol, moduleType, referenceParent);
          }
        }
      }
    }
    return symbol;
  }
  function hasSignatures(type) {
    return some(getSignaturesOfStructuredType(type, 0 /* Call */)) || some(getSignaturesOfStructuredType(type, 1 /* Construct */));
  }
  function cloneTypeAsModuleType(symbol, moduleType, referenceParent) {
    const result ***REMOVED*** createSymbol(symbol.flags, symbol.escapedName);
    result.declarations ***REMOVED*** symbol.declarations ? symbol.declarations.slice() : [];
    result.parent ***REMOVED*** symbol.parent;
    result.links.target ***REMOVED*** symbol;
    result.links.originatingImport ***REMOVED*** referenceParent;
    if (symbol.valueDeclaration) result.valueDeclaration ***REMOVED*** symbol.valueDeclaration;
    if (symbol.constEnumOnlyModule) result.constEnumOnlyModule ***REMOVED*** true;
    if (symbol.members) result.members ***REMOVED*** new Map(symbol.members);
    if (symbol.exports) result.exports ***REMOVED*** new Map(symbol.exports);
    const resolvedModuleType ***REMOVED*** resolveStructuredTypeMembers(moduleType);
    result.links.type ***REMOVED*** createAnonymousType(result, resolvedModuleType.members, emptyArray, emptyArray, resolvedModuleType.indexInfos);
    return result;
  }
  function hasExportAssignmentSymbol(moduleSymbol) {
    return moduleSymbol.exports.get("export***REMOVED***" /* ExportEquals */) !***REMOVED******REMOVED*** void 0;
  }
  function getExportsOfModuleAsArray(moduleSymbol) {
    return symbolsToArray(getExportsOfModule(moduleSymbol));
  }
  function getExportsAndPropertiesOfModule(moduleSymbol) {
    const exports2 ***REMOVED*** getExportsOfModuleAsArray(moduleSymbol);
    const exportEquals ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol);
    if (exportEquals !***REMOVED******REMOVED*** moduleSymbol) {
      const type ***REMOVED*** getTypeOfSymbol(exportEquals);
      if (shouldTreatPropertiesOfExternalModuleAsExports(type)) {
        addRange(exports2, getPropertiesOfType(type));
      }
    }
    return exports2;
  }
  function forEachExportAndPropertyOfModule(moduleSymbol, cb) {
    const exports2 ***REMOVED*** getExportsOfModule(moduleSymbol);
    exports2.forEach((symbol, key) ***REMOVED***> {
      if (!isReservedMemberName(key)) {
        cb(symbol, key);
      }
    });
    const exportEquals ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol);
    if (exportEquals !***REMOVED******REMOVED*** moduleSymbol) {
      const type ***REMOVED*** getTypeOfSymbol(exportEquals);
      if (shouldTreatPropertiesOfExternalModuleAsExports(type)) {
        forEachPropertyOfType(type, (symbol, escapedName) ***REMOVED***> {
          cb(symbol, escapedName);
        });
      }
    }
  }
  function tryGetMemberInModuleExports(memberName, moduleSymbol) {
    const symbolTable ***REMOVED*** getExportsOfModule(moduleSymbol);
    if (symbolTable) {
      return symbolTable.get(memberName);
    }
  }
  function tryGetMemberInModuleExportsAndProperties(memberName, moduleSymbol) {
    const symbol ***REMOVED*** tryGetMemberInModuleExports(memberName, moduleSymbol);
    if (symbol) {
      return symbol;
    }
    const exportEquals ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol);
    if (exportEquals ***REMOVED******REMOVED******REMOVED*** moduleSymbol) {
      return void 0;
    }
    const type ***REMOVED*** getTypeOfSymbol(exportEquals);
    return shouldTreatPropertiesOfExternalModuleAsExports(type) ? getPropertyOfType(type, memberName) : void 0;
  }
  function shouldTreatPropertiesOfExternalModuleAsExports(resolvedExternalModuleType) {
    return !(resolvedExternalModuleType.flags & 402784252 /* Primitive */ || getObjectFlags(resolvedExternalModuleType) & 1 /* Class */ || // `isArrayOrTupleLikeType` is too expensive to use in this auto-imports hot path
    isArrayType(resolvedExternalModuleType) || isTupleType(resolvedExternalModuleType));
  }
  function getExportsOfSymbol(symbol) {
    return symbol.flags & 6256 /* LateBindingContainer */ ? getResolvedMembersOrExportsOfSymbol(symbol, "resolvedExports" /* resolvedExports */) : symbol.flags & 1536 /* Module */ ? getExportsOfModule(symbol) : symbol.exports || emptySymbols;
  }
  function getExportsOfModule(moduleSymbol) {
    const links ***REMOVED*** getSymbolLinks(moduleSymbol);
    if (!links.resolvedExports) {
      const { exports: exports2, typeOnlyExportStarMap } ***REMOVED*** getExportsOfModuleWorker(moduleSymbol);
      links.resolvedExports ***REMOVED*** exports2;
      links.typeOnlyExportStarMap ***REMOVED*** typeOnlyExportStarMap;
    }
    return links.resolvedExports;
  }
  function extendExportSymbols(target, source, lookupTable, exportNode) {
    if (!source) return;
    source.forEach((sourceSymbol, id) ***REMOVED***> {
      if (id ***REMOVED******REMOVED******REMOVED*** "default" /* Default */) return;
      const targetSymbol ***REMOVED*** target.get(id);
      if (!targetSymbol) {
        target.set(id, sourceSymbol);
        if (lookupTable && exportNode) {
          lookupTable.set(id, {
            specifierText: getTextOfNode(exportNode.moduleSpecifier)
          });
        }
      } else if (lookupTable && exportNode && targetSymbol && resolveSymbol(targetSymbol) !***REMOVED******REMOVED*** resolveSymbol(sourceSymbol)) {
        const collisionTracker ***REMOVED*** lookupTable.get(id);
        if (!collisionTracker.exportsWithDuplicate) {
          collisionTracker.exportsWithDuplicate ***REMOVED*** [exportNode];
        } else {
          collisionTracker.exportsWithDuplicate.push(exportNode);
        }
      }
    });
  }
  function getExportsOfModuleWorker(moduleSymbol) {
    const visitedSymbols ***REMOVED*** [];
    let typeOnlyExportStarMap;
    const nonTypeOnlyNames ***REMOVED*** /* @__PURE__ */ new Set();
    moduleSymbol ***REMOVED*** resolveExternalModuleSymbol(moduleSymbol);
    const exports2 ***REMOVED*** visit(moduleSymbol) || emptySymbols;
    if (typeOnlyExportStarMap) {
      nonTypeOnlyNames.forEach((name) ***REMOVED***> typeOnlyExportStarMap.delete(name));
    }
    return {
      exports: exports2,
      typeOnlyExportStarMap
    };
    function visit(symbol, exportStar, isTypeOnly) {
      if (!isTypeOnly && (symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.exports)) {
        symbol.exports.forEach((_, name) ***REMOVED***> nonTypeOnlyNames.add(name));
      }
      if (!(symbol && symbol.exports && pushIfUnique(visitedSymbols, symbol))) {
        return;
      }
      const symbols ***REMOVED*** new Map(symbol.exports);
      const exportStars ***REMOVED*** symbol.exports.get("__export" /* ExportStar */);
      if (exportStars) {
        const nestedSymbols ***REMOVED*** createSymbolTable();
        const lookupTable ***REMOVED*** /* @__PURE__ */ new Map();
        if (exportStars.declarations) {
          for (const node of exportStars.declarations) {
            const resolvedModule ***REMOVED*** resolveExternalModuleName(node, node.moduleSpecifier);
            const exportedSymbols ***REMOVED*** visit(resolvedModule, node, isTypeOnly || node.isTypeOnly);
            extendExportSymbols(
              nestedSymbols,
              exportedSymbols,
              lookupTable,
              node
            );
          }
        }
        lookupTable.forEach(({ exportsWithDuplicate }, id) ***REMOVED***> {
          if (id ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" || !(exportsWithDuplicate && exportsWithDuplicate.length) || symbols.has(id)) {
            return;
          }
          for (const node of exportsWithDuplicate) {
            diagnostics.add(createDiagnosticForNode(
              node,
              Diagnostics.Module_0_has_already_exported_a_member_named_1_Consider_explicitly_re_exporting_to_resolve_the_ambiguity,
              lookupTable.get(id).specifierText,
              unescapeLeadingUnderscores(id)
            ));
          }
        });
        extendExportSymbols(symbols, nestedSymbols);
      }
      if (exportStar ***REMOVED******REMOVED*** null ? void 0 : exportStar.isTypeOnly) {
        typeOnlyExportStarMap ?? (typeOnlyExportStarMap ***REMOVED*** /* @__PURE__ */ new Map());
        symbols.forEach(
          (_, escapedName) ***REMOVED***> typeOnlyExportStarMap.set(
            escapedName,
            exportStar
          )
        );
      }
      return symbols;
    }
  }
  function getMergedSymbol(symbol) {
    let merged;
    return symbol && symbol.mergeId && (merged ***REMOVED*** mergedSymbols[symbol.mergeId]) ? merged : symbol;
  }
  function getSymbolOfDeclaration(node) {
    return getMergedSymbol(node.symbol && getLateBoundSymbol(node.symbol));
  }
  function getSymbolOfNode(node) {
    return canHaveSymbol(node) ? getSymbolOfDeclaration(node) : void 0;
  }
  function getParentOfSymbol(symbol) {
    return getMergedSymbol(symbol.parent && getLateBoundSymbol(symbol.parent));
  }
  function getFunctionExpressionParentSymbolOrSymbol(symbol) {
    var _a, _b;
    return ((_a ***REMOVED*** symbol.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || ((_b ***REMOVED*** symbol.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _b.kind) ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ ? getSymbolOfNode(symbol.valueDeclaration.parent) || symbol : symbol;
  }
  function getAlternativeContainingModules(symbol, enclosingDeclaration) {
    const containingFile ***REMOVED*** getSourceFileOfNode(enclosingDeclaration);
    const id ***REMOVED*** getNodeId(containingFile);
    const links ***REMOVED*** getSymbolLinks(symbol);
    let results;
    if (links.extendedContainersByFile && (results ***REMOVED*** links.extendedContainersByFile.get(id))) {
      return results;
    }
    if (containingFile && containingFile.imports) {
      for (const importRef of containingFile.imports) {
        if (nodeIsSynthesized(importRef)) continue;
        const resolvedModule ***REMOVED*** resolveExternalModuleName(
          enclosingDeclaration,
          importRef,
          /*ignoreErrors*/
          true
        );
        if (!resolvedModule) continue;
        const ref ***REMOVED*** getAliasForSymbolInContainer(resolvedModule, symbol);
        if (!ref) continue;
        results ***REMOVED*** append(results, resolvedModule);
      }
      if (length(results)) {
        (links.extendedContainersByFile || (links.extendedContainersByFile ***REMOVED*** /* @__PURE__ */ new Map())).set(id, results);
        return results;
      }
    }
    if (links.extendedContainers) {
      return links.extendedContainers;
    }
    const otherFiles ***REMOVED*** host.getSourceFiles();
    for (const file of otherFiles) {
      if (!isExternalModule(file)) continue;
      const sym ***REMOVED*** getSymbolOfDeclaration(file);
      const ref ***REMOVED*** getAliasForSymbolInContainer(sym, symbol);
      if (!ref) continue;
      results ***REMOVED*** append(results, sym);
    }
    return links.extendedContainers ***REMOVED*** results || emptyArray;
  }
  function getContainersOfSymbol(symbol, enclosingDeclaration, meaning) {
    const container ***REMOVED*** getParentOfSymbol(symbol);
    if (container && !(symbol.flags & 262144 /* TypeParameter */)) {
      return getWithAlternativeContainers(container);
    }
    const candidates ***REMOVED*** mapDefined(symbol.declarations, (d) ***REMOVED***> {
      if (!isAmbientModule(d) && d.parent) {
        if (hasNonGlobalAugmentationExternalModuleSymbol(d.parent)) {
          return getSymbolOfDeclaration(d.parent);
        }
        if (isModuleBlock(d.parent) && d.parent.parent && resolveExternalModuleSymbol(getSymbolOfDeclaration(d.parent.parent)) ***REMOVED******REMOVED******REMOVED*** symbol) {
          return getSymbolOfDeclaration(d.parent.parent);
        }
      }
      if (isClassExpression(d) && isBinaryExpression(d.parent) && d.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && isAccessExpression(d.parent.left) && isEntityNameExpression(d.parent.left.expression)) {
        if (isModuleExportsAccessExpression(d.parent.left) || isExportsIdentifier(d.parent.left.expression)) {
          return getSymbolOfDeclaration(getSourceFileOfNode(d));
        }
        checkExpressionCached(d.parent.left.expression);
        return getNodeLinks(d.parent.left.expression).resolvedSymbol;
      }
    });
    if (!length(candidates)) {
      return void 0;
    }
    const containers ***REMOVED*** mapDefined(candidates, (candidate) ***REMOVED***> getAliasForSymbolInContainer(candidate, symbol) ? candidate : void 0);
    let bestContainers ***REMOVED*** [];
    let alternativeContainers ***REMOVED*** [];
    for (const container2 of containers) {
      const [bestMatch, ...rest] ***REMOVED*** getWithAlternativeContainers(container2);
      bestContainers ***REMOVED*** append(bestContainers, bestMatch);
      alternativeContainers ***REMOVED*** addRange(alternativeContainers, rest);
    }
    return concatenate(bestContainers, alternativeContainers);
    function getWithAlternativeContainers(container2) {
      const additionalContainers ***REMOVED*** mapDefined(container2.declarations, fileSymbolIfFileSymbolExportEqualsContainer);
      const reexportContainers ***REMOVED*** enclosingDeclaration && getAlternativeContainingModules(symbol, enclosingDeclaration);
      const objectLiteralContainer ***REMOVED*** getVariableDeclarationOfObjectLiteral(container2, meaning);
      if (enclosingDeclaration && container2.flags & getQualifiedLeftMeaning(meaning) && getAccessibleSymbolChain(
        container2,
        enclosingDeclaration,
        1920 /* Namespace */,
        /*useOnlyExternalAliasing*/
        false
      )) {
        return append(concatenate(concatenate([container2], additionalContainers), reexportContainers), objectLiteralContainer);
      }
      const firstVariableMatch ***REMOVED*** !(container2.flags & getQualifiedLeftMeaning(meaning)) && container2.flags & 788968 /* Type */ && getDeclaredTypeOfSymbol(container2).flags & 524288 /* Object */ && meaning ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */ ? forEachSymbolTableInScope(enclosingDeclaration, (t) ***REMOVED***> {
        return forEachEntry(t, (s) ***REMOVED***> {
          if (s.flags & getQualifiedLeftMeaning(meaning) && getTypeOfSymbol(s) ***REMOVED******REMOVED******REMOVED*** getDeclaredTypeOfSymbol(container2)) {
            return s;
          }
        });
      }) : void 0;
      let res ***REMOVED*** firstVariableMatch ? [firstVariableMatch, ...additionalContainers, container2] : [...additionalContainers, container2];
      res ***REMOVED*** append(res, objectLiteralContainer);
      res ***REMOVED*** addRange(res, reexportContainers);
      return res;
    }
    function fileSymbolIfFileSymbolExportEqualsContainer(d) {
      return container && getFileSymbolIfFileSymbolExportEqualsContainer(d, container);
    }
  }
  function getVariableDeclarationOfObjectLiteral(symbol, meaning) {
    const firstDecl ***REMOVED*** !!length(symbol.declarations) && first(symbol.declarations);
    if (meaning & 111551 /* Value */ && firstDecl && firstDecl.parent && isVariableDeclaration(firstDecl.parent)) {
      if (isObjectLiteralExpression(firstDecl) && firstDecl ***REMOVED******REMOVED******REMOVED*** firstDecl.parent.initializer || isTypeLiteralNode(firstDecl) && firstDecl ***REMOVED******REMOVED******REMOVED*** firstDecl.parent.type) {
        return getSymbolOfDeclaration(firstDecl.parent);
      }
    }
  }
  function getFileSymbolIfFileSymbolExportEqualsContainer(d, container) {
    const fileSymbol ***REMOVED*** getExternalModuleContainer(d);
    const exported ***REMOVED*** fileSymbol && fileSymbol.exports && fileSymbol.exports.get("export***REMOVED***" /* ExportEquals */);
    return exported && getSymbolIfSameReference(exported, container) ? fileSymbol : void 0;
  }
  function getAliasForSymbolInContainer(container, symbol) {
    if (container ***REMOVED******REMOVED******REMOVED*** getParentOfSymbol(symbol)) {
      return symbol;
    }
    const exportEquals ***REMOVED*** container.exports && container.exports.get("export***REMOVED***" /* ExportEquals */);
    if (exportEquals && getSymbolIfSameReference(exportEquals, symbol)) {
      return container;
    }
    const exports2 ***REMOVED*** getExportsOfSymbol(container);
    const quick ***REMOVED*** exports2.get(symbol.escapedName);
    if (quick && getSymbolIfSameReference(quick, symbol)) {
      return quick;
    }
    return forEachEntry(exports2, (exported) ***REMOVED***> {
      if (getSymbolIfSameReference(exported, symbol)) {
        return exported;
      }
    });
  }
  function getSymbolIfSameReference(s1, s2) {
    if (getMergedSymbol(resolveSymbol(getMergedSymbol(s1))) ***REMOVED******REMOVED******REMOVED*** getMergedSymbol(resolveSymbol(getMergedSymbol(s2)))) {
      return s1;
    }
  }
  function getExportSymbolOfValueSymbolIfExported(symbol) {
    return getMergedSymbol(symbol && (symbol.flags & 1048576 /* ExportValue */) !***REMOVED******REMOVED*** 0 && symbol.exportSymbol || symbol);
  }
  function symbolIsValue(symbol, includeTypeOnlyMembers) {
    return !!(symbol.flags & 111551 /* Value */ || symbol.flags & 2097152 /* Alias */ && getSymbolFlags(symbol, !includeTypeOnlyMembers) & 111551 /* Value */);
  }
  function createType(flags) {
    var _a;
    const result ***REMOVED*** new Type7(checker, flags);
    typeCount++;
    result.id ***REMOVED*** typeCount;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.recordType(result);
    return result;
  }
  function createTypeWithSymbol(flags, symbol) {
    const result ***REMOVED*** createType(flags);
    result.symbol ***REMOVED*** symbol;
    return result;
  }
  function createOriginType(flags) {
    return new Type7(checker, flags);
  }
  function createIntrinsicType(kind, intrinsicName, objectFlags ***REMOVED*** 0 /* None */, debugIntrinsicName) {
    checkIntrinsicName(intrinsicName, debugIntrinsicName);
    const type ***REMOVED*** createType(kind);
    type.intrinsicName ***REMOVED*** intrinsicName;
    type.debugIntrinsicName ***REMOVED*** debugIntrinsicName;
    type.objectFlags ***REMOVED*** objectFlags | 524288 /* CouldContainTypeVariablesComputed */ | 2097152 /* IsGenericTypeComputed */ | 33554432 /* IsUnknownLikeUnionComputed */ | 16777216 /* IsNeverIntersectionComputed */;
    return type;
  }
  function checkIntrinsicName(name, debug) {
    const key ***REMOVED*** `${name},${debug ?? ""}`;
    if (seenIntrinsicNames.has(key)) {
      Debug.fail(`Duplicate intrinsic type name ${name}${debug ? ` (${debug})` : ""}; you may need to pass a name to createIntrinsicType.`);
    }
    seenIntrinsicNames.add(key);
  }
  function createObjectType(objectFlags, symbol) {
    const type ***REMOVED*** createTypeWithSymbol(524288 /* Object */, symbol);
    type.objectFlags ***REMOVED*** objectFlags;
    type.members ***REMOVED*** void 0;
    type.properties ***REMOVED*** void 0;
    type.callSignatures ***REMOVED*** void 0;
    type.constructSignatures ***REMOVED*** void 0;
    type.indexInfos ***REMOVED*** void 0;
    return type;
  }
  function createTypeofType() {
    return getUnionType(arrayFrom(typeofNEFacts.keys(), getStringLiteralType));
  }
  function createTypeParameter(symbol) {
    return createTypeWithSymbol(262144 /* TypeParameter */, symbol);
  }
  function isReservedMemberName(name) {
    return name.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && name.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && name.charCodeAt(2) !***REMOVED******REMOVED*** 95 /* _ */ && name.charCodeAt(2) !***REMOVED******REMOVED*** 64 /* at */ && name.charCodeAt(2) !***REMOVED******REMOVED*** 35 /* hash */;
  }
  function getNamedMembers(members) {
    let result;
    members.forEach((symbol, id) ***REMOVED***> {
      if (isNamedMember(symbol, id)) {
        (result || (result ***REMOVED*** [])).push(symbol);
      }
    });
    return result || emptyArray;
  }
  function isNamedMember(member, escapedName) {
    return !isReservedMemberName(escapedName) && symbolIsValue(member);
  }
  function getNamedOrIndexSignatureMembers(members) {
    const result ***REMOVED*** getNamedMembers(members);
    const index ***REMOVED*** getIndexSymbolFromSymbolTable(members);
    return index ? concatenate(result, [index]) : result;
  }
  function setStructuredTypeMembers(type, members, callSignatures, constructSignatures, indexInfos) {
    const resolved ***REMOVED*** type;
    resolved.members ***REMOVED*** members;
    resolved.properties ***REMOVED*** emptyArray;
    resolved.callSignatures ***REMOVED*** callSignatures;
    resolved.constructSignatures ***REMOVED*** constructSignatures;
    resolved.indexInfos ***REMOVED*** indexInfos;
    if (members !***REMOVED******REMOVED*** emptySymbols) resolved.properties ***REMOVED*** getNamedMembers(members);
    return resolved;
  }
  function createAnonymousType(symbol, members, callSignatures, constructSignatures, indexInfos) {
    return setStructuredTypeMembers(createObjectType(16 /* Anonymous */, symbol), members, callSignatures, constructSignatures, indexInfos);
  }
  function getResolvedTypeWithoutAbstractConstructSignatures(type) {
    if (type.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) return type;
    if (type.objectTypeWithoutAbstractConstructSignatures) return type.objectTypeWithoutAbstractConstructSignatures;
    const constructSignatures ***REMOVED*** filter(type.constructSignatures, (signature) ***REMOVED***> !(signature.flags & 4 /* Abstract */));
    if (type.constructSignatures ***REMOVED******REMOVED******REMOVED*** constructSignatures) return type;
    const typeCopy ***REMOVED*** createAnonymousType(
      type.symbol,
      type.members,
      type.callSignatures,
      some(constructSignatures) ? constructSignatures : emptyArray,
      type.indexInfos
    );
    type.objectTypeWithoutAbstractConstructSignatures ***REMOVED*** typeCopy;
    typeCopy.objectTypeWithoutAbstractConstructSignatures ***REMOVED*** typeCopy;
    return typeCopy;
  }
  function forEachSymbolTableInScope(enclosingDeclaration, callback) {
    let result;
    for (let location ***REMOVED*** enclosingDeclaration; location; location ***REMOVED*** location.parent) {
      if (canHaveLocals(location) && location.locals && !isGlobalSourceFile(location)) {
        if (result ***REMOVED*** callback(
          location.locals,
          /*ignoreQualification*/
          void 0,
          /*isLocalNameLookup*/
          true,
          location
        )) {
          return result;
        }
      }
      switch (location.kind) {
        case 308 /* SourceFile */:
          if (!isExternalOrCommonJsModule(location)) {
            break;
          }
        // falls through
        case 268 /* ModuleDeclaration */:
          const sym ***REMOVED*** getSymbolOfDeclaration(location);
          if (result ***REMOVED*** callback(
            (sym ***REMOVED******REMOVED*** null ? void 0 : sym.exports) || emptySymbols,
            /*ignoreQualification*/
            void 0,
            /*isLocalNameLookup*/
            true,
            location
          )) {
            return result;
          }
          break;
        case 264 /* ClassDeclaration */:
        case 232 /* ClassExpression */:
        case 265 /* InterfaceDeclaration */:
          let table;
          (getSymbolOfDeclaration(location).members || emptySymbols).forEach((memberSymbol, key) ***REMOVED***> {
            if (memberSymbol.flags & (788968 /* Type */ & ~67108864 /* Assignment */)) {
              (table || (table ***REMOVED*** createSymbolTable())).set(key, memberSymbol);
            }
          });
          if (table && (result ***REMOVED*** callback(
            table,
            /*ignoreQualification*/
            void 0,
            /*isLocalNameLookup*/
            false,
            location
          ))) {
            return result;
          }
          break;
      }
    }
    return callback(
      globals,
      /*ignoreQualification*/
      void 0,
      /*isLocalNameLookup*/
      true
    );
  }
  function getQualifiedLeftMeaning(rightMeaning) {
    return rightMeaning ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */ ? 111551 /* Value */ : 1920 /* Namespace */;
  }
  function getAccessibleSymbolChain(symbol, enclosingDeclaration, meaning, useOnlyExternalAliasing, visitedSymbolTablesMap ***REMOVED*** /* @__PURE__ */ new Map()) {
    if (!(symbol && !isPropertyOrMethodDeclarationSymbol(symbol))) {
      return void 0;
    }
    const links ***REMOVED*** getSymbolLinks(symbol);
    const cache ***REMOVED*** links.accessibleChainCache || (links.accessibleChainCache ***REMOVED*** /* @__PURE__ */ new Map());
    const firstRelevantLocation ***REMOVED*** forEachSymbolTableInScope(enclosingDeclaration, (_, __, ___, node) ***REMOVED***> node);
    const key ***REMOVED*** `${useOnlyExternalAliasing ? 0 : 1}|${firstRelevantLocation ? getNodeId(firstRelevantLocation) : 0}|${meaning}`;
    if (cache.has(key)) {
      return cache.get(key);
    }
    const id ***REMOVED*** getSymbolId(symbol);
    let visitedSymbolTables ***REMOVED*** visitedSymbolTablesMap.get(id);
    if (!visitedSymbolTables) {
      visitedSymbolTablesMap.set(id, visitedSymbolTables ***REMOVED*** []);
    }
    const result ***REMOVED*** forEachSymbolTableInScope(enclosingDeclaration, getAccessibleSymbolChainFromSymbolTable);
    cache.set(key, result);
    return result;
    function getAccessibleSymbolChainFromSymbolTable(symbols, ignoreQualification, isLocalNameLookup) {
      if (!pushIfUnique(visitedSymbolTables, symbols)) {
        return void 0;
      }
      const result2 ***REMOVED*** trySymbolTable(symbols, ignoreQualification, isLocalNameLookup);
      visitedSymbolTables.pop();
      return result2;
    }
    function canQualifySymbol(symbolFromSymbolTable, meaning2) {
      return !needsQualification(symbolFromSymbolTable, enclosingDeclaration, meaning2) || // If symbol needs qualification, make sure that parent is accessible, if it is then this symbol is accessible too
      !!getAccessibleSymbolChain(symbolFromSymbolTable.parent, enclosingDeclaration, getQualifiedLeftMeaning(meaning2), useOnlyExternalAliasing, visitedSymbolTablesMap);
    }
    function isAccessible(symbolFromSymbolTable, resolvedAliasSymbol, ignoreQualification) {
      return (symbol ***REMOVED******REMOVED******REMOVED*** (resolvedAliasSymbol || symbolFromSymbolTable) || getMergedSymbol(symbol) ***REMOVED******REMOVED******REMOVED*** getMergedSymbol(resolvedAliasSymbol || symbolFromSymbolTable)) && // if the symbolFromSymbolTable is not external module (it could be if it was determined as ambient external module and would be in globals table)
      // and if symbolFromSymbolTable or alias resolution matches the symbol,
      // check the symbol can be qualified, it is only then this symbol is accessible
      !some(symbolFromSymbolTable.declarations, hasNonGlobalAugmentationExternalModuleSymbol) && (ignoreQualification || canQualifySymbol(getMergedSymbol(symbolFromSymbolTable), meaning));
    }
    function trySymbolTable(symbols, ignoreQualification, isLocalNameLookup) {
      if (isAccessible(
        symbols.get(symbol.escapedName),
        /*resolvedAliasSymbol*/
        void 0,
        ignoreQualification
      )) {
        return [symbol];
      }
      const result2 ***REMOVED*** forEachEntry(symbols, (symbolFromSymbolTable) ***REMOVED***> {
        if (symbolFromSymbolTable.flags & 2097152 /* Alias */ && symbolFromSymbolTable.escapedName !***REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */ && symbolFromSymbolTable.escapedName !***REMOVED******REMOVED*** "default" /* Default */ && !(isUMDExportSymbol(symbolFromSymbolTable) && enclosingDeclaration && isExternalModule(getSourceFileOfNode(enclosingDeclaration))) && (!useOnlyExternalAliasing || some(symbolFromSymbolTable.declarations, isExternalModuleImportEqualsDeclaration)) && (isLocalNameLookup ? !some(symbolFromSymbolTable.declarations, isNamespaceReexportDeclaration) : true) && (ignoreQualification || !getDeclarationOfKind(symbolFromSymbolTable, 282 /* ExportSpecifier */))) {
          const resolvedImportedSymbol ***REMOVED*** resolveAlias(symbolFromSymbolTable);
          const candidate ***REMOVED*** getCandidateListForSymbol(symbolFromSymbolTable, resolvedImportedSymbol, ignoreQualification);
          if (candidate) {
            return candidate;
          }
        }
        if (symbolFromSymbolTable.escapedName ***REMOVED******REMOVED******REMOVED*** symbol.escapedName && symbolFromSymbolTable.exportSymbol) {
          if (isAccessible(
            getMergedSymbol(symbolFromSymbolTable.exportSymbol),
            /*resolvedAliasSymbol*/
            void 0,
            ignoreQualification
          )) {
            return [symbol];
          }
        }
      });
      return result2 || (symbols ***REMOVED******REMOVED******REMOVED*** globals ? getCandidateListForSymbol(globalThisSymbol, globalThisSymbol, ignoreQualification) : void 0);
    }
    function getCandidateListForSymbol(symbolFromSymbolTable, resolvedImportedSymbol, ignoreQualification) {
      if (isAccessible(symbolFromSymbolTable, resolvedImportedSymbol, ignoreQualification)) {
        return [symbolFromSymbolTable];
      }
      const candidateTable ***REMOVED*** getExportsOfSymbol(resolvedImportedSymbol);
      const accessibleSymbolsFromExports ***REMOVED*** candidateTable && getAccessibleSymbolChainFromSymbolTable(
        candidateTable,
        /*ignoreQualification*/
        true
      );
      if (accessibleSymbolsFromExports && canQualifySymbol(symbolFromSymbolTable, getQualifiedLeftMeaning(meaning))) {
        return [symbolFromSymbolTable].concat(accessibleSymbolsFromExports);
      }
    }
  }
  function needsQualification(symbol, enclosingDeclaration, meaning) {
    let qualify ***REMOVED*** false;
    forEachSymbolTableInScope(enclosingDeclaration, (symbolTable) ***REMOVED***> {
      let symbolFromSymbolTable ***REMOVED*** getMergedSymbol(symbolTable.get(symbol.escapedName));
      if (!symbolFromSymbolTable) {
        return false;
      }
      if (symbolFromSymbolTable ***REMOVED******REMOVED******REMOVED*** symbol) {
        return true;
      }
      const shouldResolveAlias ***REMOVED*** symbolFromSymbolTable.flags & 2097152 /* Alias */ && !getDeclarationOfKind(symbolFromSymbolTable, 282 /* ExportSpecifier */);
      symbolFromSymbolTable ***REMOVED*** shouldResolveAlias ? resolveAlias(symbolFromSymbolTable) : symbolFromSymbolTable;
      const flags ***REMOVED*** shouldResolveAlias ? getSymbolFlags(symbolFromSymbolTable) : symbolFromSymbolTable.flags;
      if (flags & meaning) {
        qualify ***REMOVED*** true;
        return true;
      }
      return false;
    });
    return qualify;
  }
  function isPropertyOrMethodDeclarationSymbol(symbol) {
    if (symbol.declarations && symbol.declarations.length) {
      for (const declaration of symbol.declarations) {
        switch (declaration.kind) {
          case 173 /* PropertyDeclaration */:
          case 175 /* MethodDeclaration */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
            continue;
          default:
            return false;
        }
      }
      return true;
    }
    return false;
  }
  function isTypeSymbolAccessible(typeSymbol, enclosingDeclaration) {
    const access ***REMOVED*** isSymbolAccessibleWorker(
      typeSymbol,
      enclosingDeclaration,
      788968 /* Type */,
      /*shouldComputeAliasesToMakeVisible*/
      false,
      /*allowModules*/
      true
    );
    return access.accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */;
  }
  function isValueSymbolAccessible(typeSymbol, enclosingDeclaration) {
    const access ***REMOVED*** isSymbolAccessibleWorker(
      typeSymbol,
      enclosingDeclaration,
      111551 /* Value */,
      /*shouldComputeAliasesToMakeVisible*/
      false,
      /*allowModules*/
      true
    );
    return access.accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */;
  }
  function isSymbolAccessibleByFlags(typeSymbol, enclosingDeclaration, flags) {
    const access ***REMOVED*** isSymbolAccessibleWorker(
      typeSymbol,
      enclosingDeclaration,
      flags,
      /*shouldComputeAliasesToMakeVisible*/
      false,
      /*allowModules*/
      false
    );
    return access.accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */;
  }
  function isAnySymbolAccessible(symbols, enclosingDeclaration, initialSymbol, meaning, shouldComputeAliasesToMakeVisible, allowModules) {
    if (!length(symbols)) return;
    let hadAccessibleChain;
    let earlyModuleBail ***REMOVED*** false;
    for (const symbol of symbols) {
      const accessibleSymbolChain ***REMOVED*** getAccessibleSymbolChain(
        symbol,
        enclosingDeclaration,
        meaning,
        /*useOnlyExternalAliasing*/
        false
      );
      if (accessibleSymbolChain) {
        hadAccessibleChain ***REMOVED*** symbol;
        const hasAccessibleDeclarations ***REMOVED*** hasVisibleDeclarations(accessibleSymbolChain[0], shouldComputeAliasesToMakeVisible);
        if (hasAccessibleDeclarations) {
          return hasAccessibleDeclarations;
        }
      }
      if (allowModules) {
        if (some(symbol.declarations, hasNonGlobalAugmentationExternalModuleSymbol)) {
          if (shouldComputeAliasesToMakeVisible) {
            earlyModuleBail ***REMOVED*** true;
            continue;
          }
          return {
            accessibility: 0 /* Accessible */
          };
        }
      }
      const containers ***REMOVED*** getContainersOfSymbol(symbol, enclosingDeclaration, meaning);
      const parentResult ***REMOVED*** isAnySymbolAccessible(containers, enclosingDeclaration, initialSymbol, initialSymbol ***REMOVED******REMOVED******REMOVED*** symbol ? getQualifiedLeftMeaning(meaning) : meaning, shouldComputeAliasesToMakeVisible, allowModules);
      if (parentResult) {
        return parentResult;
      }
    }
    if (earlyModuleBail) {
      return {
        accessibility: 0 /* Accessible */
      };
    }
    if (hadAccessibleChain) {
      return {
        accessibility: 1 /* NotAccessible */,
        errorSymbolName: symbolToString(initialSymbol, enclosingDeclaration, meaning),
        errorModuleName: hadAccessibleChain !***REMOVED******REMOVED*** initialSymbol ? symbolToString(hadAccessibleChain, enclosingDeclaration, 1920 /* Namespace */) : void 0
      };
    }
  }
  function isSymbolAccessible(symbol, enclosingDeclaration, meaning, shouldComputeAliasesToMakeVisible) {
    return isSymbolAccessibleWorker(
      symbol,
      enclosingDeclaration,
      meaning,
      shouldComputeAliasesToMakeVisible,
      /*allowModules*/
      true
    );
  }
  function isSymbolAccessibleWorker(symbol, enclosingDeclaration, meaning, shouldComputeAliasesToMakeVisible, allowModules) {
    if (symbol && enclosingDeclaration) {
      const result ***REMOVED*** isAnySymbolAccessible([symbol], enclosingDeclaration, symbol, meaning, shouldComputeAliasesToMakeVisible, allowModules);
      if (result) {
        return result;
      }
      const symbolExternalModule ***REMOVED*** forEach(symbol.declarations, getExternalModuleContainer);
      if (symbolExternalModule) {
        const enclosingExternalModule ***REMOVED*** getExternalModuleContainer(enclosingDeclaration);
        if (symbolExternalModule !***REMOVED******REMOVED*** enclosingExternalModule) {
          return {
            accessibility: 2 /* CannotBeNamed */,
            errorSymbolName: symbolToString(symbol, enclosingDeclaration, meaning),
            errorModuleName: symbolToString(symbolExternalModule),
            errorNode: isInJSFile(enclosingDeclaration) ? enclosingDeclaration : void 0
          };
        }
      }
      return {
        accessibility: 1 /* NotAccessible */,
        errorSymbolName: symbolToString(symbol, enclosingDeclaration, meaning)
      };
    }
    return { accessibility: 0 /* Accessible */ };
  }
  function getExternalModuleContainer(declaration) {
    const node ***REMOVED*** findAncestor(declaration, hasExternalModuleSymbol);
    return node && getSymbolOfDeclaration(node);
  }
  function hasExternalModuleSymbol(declaration) {
    return isAmbientModule(declaration) || declaration.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && isExternalOrCommonJsModule(declaration);
  }
  function hasNonGlobalAugmentationExternalModuleSymbol(declaration) {
    return isModuleWithStringLiteralName(declaration) || declaration.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && isExternalOrCommonJsModule(declaration);
  }
  function hasVisibleDeclarations(symbol, shouldComputeAliasToMakeVisible) {
    let aliasesToMakeVisible;
    if (!every(filter(symbol.declarations, (d) ***REMOVED***> d.kind !***REMOVED******REMOVED*** 80 /* Identifier */), getIsDeclarationVisible)) {
      return void 0;
    }
    return { accessibility: 0 /* Accessible */, aliasesToMakeVisible };
    function getIsDeclarationVisible(declaration) {
      var _a, _b;
      if (!isDeclarationVisible(declaration)) {
        const anyImportSyntax ***REMOVED*** getAnyImportSyntax(declaration);
        if (anyImportSyntax && !hasSyntacticModifier(anyImportSyntax, 32 /* Export */) && // import clause without export
        isDeclarationVisible(anyImportSyntax.parent)) {
          return addVisibleAlias(declaration, anyImportSyntax);
        } else if (isVariableDeclaration(declaration) && isVariableStatement(declaration.parent.parent) && !hasSyntacticModifier(declaration.parent.parent, 32 /* Export */) && // unexported variable statement
        isDeclarationVisible(declaration.parent.parent.parent)) {
          return addVisibleAlias(declaration, declaration.parent.parent);
        } else if (isLateVisibilityPaintedStatement(declaration) && !hasSyntacticModifier(declaration, 32 /* Export */) && isDeclarationVisible(declaration.parent)) {
          return addVisibleAlias(declaration, declaration);
        } else if (isBindingElement(declaration)) {
          if (symbol.flags & 2097152 /* Alias */ && isInJSFile(declaration) && ((_a ***REMOVED*** declaration.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) && isVariableDeclaration(declaration.parent.parent) && ((_b ***REMOVED*** declaration.parent.parent.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.parent) && isVariableStatement(declaration.parent.parent.parent.parent) && !hasSyntacticModifier(declaration.parent.parent.parent.parent, 32 /* Export */) && declaration.parent.parent.parent.parent.parent && isDeclarationVisible(declaration.parent.parent.parent.parent.parent)) {
            return addVisibleAlias(declaration, declaration.parent.parent.parent.parent);
          } else if (symbol.flags & 2 /* BlockScopedVariable */) {
            const rootDeclaration ***REMOVED*** walkUpBindingElementsAndPatterns(declaration);
            if (rootDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              return false;
            }
            const variableStatement ***REMOVED*** rootDeclaration.parent.parent;
            if (variableStatement.kind !***REMOVED******REMOVED*** 244 /* VariableStatement */) {
              return false;
            }
            if (hasSyntacticModifier(variableStatement, 32 /* Export */)) {
              return true;
            }
            if (!isDeclarationVisible(variableStatement.parent)) {
              return false;
            }
            return addVisibleAlias(declaration, variableStatement);
          }
        }
        return false;
      }
      return true;
    }
    function addVisibleAlias(declaration, aliasingStatement) {
      if (shouldComputeAliasToMakeVisible) {
        getNodeLinks(declaration).isVisible ***REMOVED*** true;
        aliasesToMakeVisible ***REMOVED*** appendIfUnique(aliasesToMakeVisible, aliasingStatement);
      }
      return true;
    }
  }
  function getMeaningOfEntityNameReference(entityName) {
    let meaning;
    if (entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */ && !isPartOfTypeNode(entityName.parent) || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 183 /* TypePredicate */ && entityName.parent.parameterName ***REMOVED******REMOVED******REMOVED*** entityName) {
      meaning ***REMOVED*** 111551 /* Value */ | 1048576 /* ExportValue */;
    } else if (entityName.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || entityName.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ && entityName.parent.left ***REMOVED******REMOVED******REMOVED*** entityName || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && entityName.parent.expression ***REMOVED******REMOVED******REMOVED*** entityName || entityName.parent.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && entityName.parent.expression ***REMOVED******REMOVED******REMOVED*** entityName) {
      meaning ***REMOVED*** 1920 /* Namespace */;
    } else {
      meaning ***REMOVED*** 788968 /* Type */;
    }
    return meaning;
  }
  function isEntityNameVisible(entityName, enclosingDeclaration, shouldComputeAliasToMakeVisible ***REMOVED*** true) {
    const meaning ***REMOVED*** getMeaningOfEntityNameReference(entityName);
    const firstIdentifier ***REMOVED*** getFirstIdentifier(entityName);
    const symbol ***REMOVED*** resolveName(
      enclosingDeclaration,
      firstIdentifier.escapedText,
      meaning,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      false
    );
    if (symbol && symbol.flags & 262144 /* TypeParameter */ && meaning & 788968 /* Type */) {
      return { accessibility: 0 /* Accessible */ };
    }
    if (!symbol && isThisIdentifier(firstIdentifier) && isSymbolAccessible(
      getSymbolOfDeclaration(getThisContainer(
        firstIdentifier,
        /*includeArrowFunctions*/
        false,
        /*includeClassComputedPropertyName*/
        false
      )),
      firstIdentifier,
      meaning,
      /*shouldComputeAliasesToMakeVisible*/
      false
    ).accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */) {
      return { accessibility: 0 /* Accessible */ };
    }
    if (!symbol) {
      return {
        accessibility: 3 /* NotResolved */,
        errorSymbolName: getTextOfNode(firstIdentifier),
        errorNode: firstIdentifier
      };
    }
    return hasVisibleDeclarations(symbol, shouldComputeAliasToMakeVisible) || {
      accessibility: 1 /* NotAccessible */,
      errorSymbolName: getTextOfNode(firstIdentifier),
      errorNode: firstIdentifier
    };
  }
  function symbolToString(symbol, enclosingDeclaration, meaning, flags ***REMOVED*** 4 /* AllowAnyNodeKind */, writer) {
    let nodeFlags ***REMOVED*** 70221824 /* IgnoreErrors */;
    let internalNodeFlags ***REMOVED*** 0 /* None */;
    if (flags & 2 /* UseOnlyExternalAliasing */) {
      nodeFlags |***REMOVED*** 128 /* UseOnlyExternalAliasing */;
    }
    if (flags & 1 /* WriteTypeParametersOrArguments */) {
      nodeFlags |***REMOVED*** 512 /* WriteTypeParametersInQualifiedName */;
    }
    if (flags & 8 /* UseAliasDefinedOutsideCurrentScope */) {
      nodeFlags |***REMOVED*** 16384 /* UseAliasDefinedOutsideCurrentScope */;
    }
    if (flags & 32 /* DoNotIncludeSymbolChain */) {
      internalNodeFlags |***REMOVED*** 4 /* DoNotIncludeSymbolChain */;
    }
    if (flags & 16 /* WriteComputedProps */) {
      internalNodeFlags |***REMOVED*** 1 /* WriteComputedProps */;
    }
    const builder ***REMOVED*** flags & 4 /* AllowAnyNodeKind */ ? nodeBuilder.symbolToNode : nodeBuilder.symbolToEntityName;
    return writer ? symbolToStringWorker(writer).getText() : usingSingleLineStringWriter(symbolToStringWorker);
    function symbolToStringWorker(writer2) {
      const entity ***REMOVED*** builder(symbol, meaning, enclosingDeclaration, nodeFlags, internalNodeFlags);
      const printer ***REMOVED*** (enclosingDeclaration ***REMOVED******REMOVED*** null ? void 0 : enclosingDeclaration.kind) ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? createPrinterWithRemoveCommentsNeverAsciiEscape() : createPrinterWithRemoveComments();
      const sourceFile ***REMOVED*** enclosingDeclaration && getSourceFileOfNode(enclosingDeclaration);
      printer.writeNode(
        4 /* Unspecified */,
        entity,
        /*sourceFile*/
        sourceFile,
        writer2
      );
      return writer2;
    }
  }
  function signatureToString(signature, enclosingDeclaration, flags ***REMOVED*** 0 /* None */, kind, writer, maximumLength, verbosityLevel, out) {
    return writer ? signatureToStringWorker(writer).getText() : usingSingleLineStringWriter(signatureToStringWorker);
    function signatureToStringWorker(writer2) {
      let sigOutput;
      if (flags & 262144 /* WriteArrowStyleSignature */) {
        sigOutput ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ ? 186 /* ConstructorType */ : 185 /* FunctionType */;
      } else {
        sigOutput ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ ? 181 /* ConstructSignature */ : 180 /* CallSignature */;
      }
      const sig ***REMOVED*** nodeBuilder.signatureToSignatureDeclaration(
        signature,
        sigOutput,
        enclosingDeclaration,
        toNodeBuilderFlags(flags) | 70221824 /* IgnoreErrors */ | 512 /* WriteTypeParametersInQualifiedName */,
        /*internalFlags*/
        void 0,
        /*tracker*/
        void 0,
        maximumLength,
        verbosityLevel,
        out
      );
      const printer ***REMOVED*** createPrinterWithRemoveCommentsOmitTrailingSemicolon();
      const sourceFile ***REMOVED*** enclosingDeclaration && getSourceFileOfNode(enclosingDeclaration);
      printer.writeNode(
        4 /* Unspecified */,
        sig,
        /*sourceFile*/
        sourceFile,
        getTrailingSemicolonDeferringWriter(writer2)
      );
      return writer2;
    }
  }
  function typeToString(type, enclosingDeclaration, flags ***REMOVED*** 1048576 /* AllowUniqueESSymbolType */ | 16384 /* UseAliasDefinedOutsideCurrentScope */, writer ***REMOVED*** createTextWriter(""), maximumLength, verbosityLevel, out) {
    const noTruncation ***REMOVED*** !maximumLength && compilerOptions.noErrorTruncation || flags & 1 /* NoTruncation */;
    const typeNode ***REMOVED*** nodeBuilder.typeToTypeNode(
      type,
      enclosingDeclaration,
      toNodeBuilderFlags(flags) | 70221824 /* IgnoreErrors */ | (noTruncation ? 1 /* NoTruncation */ : 0),
      /*internalFlags*/
      void 0,
      /*tracker*/
      void 0,
      maximumLength,
      verbosityLevel,
      out
    );
    if (typeNode ***REMOVED******REMOVED******REMOVED*** void 0) return Debug.fail("should always get typenode");
    const printer ***REMOVED*** type !***REMOVED******REMOVED*** unresolvedType ? createPrinterWithRemoveComments() : createPrinterWithDefaults();
    const sourceFile ***REMOVED*** enclosingDeclaration && getSourceFileOfNode(enclosingDeclaration);
    printer.writeNode(
      4 /* Unspecified */,
      typeNode,
      /*sourceFile*/
      sourceFile,
      writer
    );
    const result ***REMOVED*** writer.getText();
    const maxLength ***REMOVED*** maximumLength || (noTruncation ? noTruncationMaximumTruncationLength * 2 : defaultMaximumTruncationLength * 2);
    if (maxLength && result && result.length >***REMOVED*** maxLength) {
      return result.substr(0, maxLength - "...".length) + "...";
    }
    return result;
  }
  function getTypeNamesForErrorDisplay(left, right) {
    let leftStr ***REMOVED*** symbolValueDeclarationIsContextSensitive(left.symbol) ? typeToString(left, left.symbol.valueDeclaration) : typeToString(left);
    let rightStr ***REMOVED*** symbolValueDeclarationIsContextSensitive(right.symbol) ? typeToString(right, right.symbol.valueDeclaration) : typeToString(right);
    if (leftStr ***REMOVED******REMOVED******REMOVED*** rightStr) {
      leftStr ***REMOVED*** getTypeNameForErrorDisplay(left);
      rightStr ***REMOVED*** getTypeNameForErrorDisplay(right);
    }
    return [leftStr, rightStr];
  }
  function getTypeNameForErrorDisplay(type) {
    return typeToString(
      type,
      /*enclosingDeclaration*/
      void 0,
      64 /* UseFullyQualifiedType */
    );
  }
  function symbolValueDeclarationIsContextSensitive(symbol) {
    return symbol && !!symbol.valueDeclaration && isExpression(symbol.valueDeclaration) && !isContextSensitive(symbol.valueDeclaration);
  }
  function toNodeBuilderFlags(flags ***REMOVED*** 0 /* None */) {
    return flags & 848330095 /* NodeBuilderFlagsMask */;
  }
  function isClassInstanceSide(type) {
    return !!type.symbol && !!(type.symbol.flags & 32 /* Class */) && (type ***REMOVED******REMOVED******REMOVED*** getDeclaredTypeOfClassOrInterface(type.symbol) || !!(type.flags & 524288 /* Object */) && !!(getObjectFlags(type) & 16777216 /* IsClassInstanceClone */));
  }
  function getTypeFromTypeNodeWithoutContext(node) {
    return getTypeFromTypeNode(node);
  }
  function createNodeBuilder() {
    const syntacticBuilderResolver ***REMOVED*** {
      evaluateEntityNameExpression,
      isExpandoFunctionDeclaration,
      hasLateBindableName,
      shouldRemoveDeclaration(context, node) {
        return !(context.internalFlags & 8 /* AllowUnresolvedNames */ && isEntityNameExpression(node.name.expression) && checkComputedPropertyName(node.name).flags & 1 /* Any */);
      },
      createRecoveryBoundary(context) {
        return createRecoveryBoundary(context);
      },
      isDefinitelyReferenceToGlobalSymbolObject,
      getAllAccessorDeclarations: getAllAccessorDeclarationsForDeclaration,
      requiresAddingImplicitUndefined(declaration, symbol, enclosingDeclaration) {
        var _a;
        switch (declaration.kind) {
          case 173 /* PropertyDeclaration */:
          case 172 /* PropertySignature */:
          case 349 /* JSDocPropertyTag */:
            symbol ?? (symbol ***REMOVED*** getSymbolOfDeclaration(declaration));
            const type ***REMOVED*** getTypeOfSymbol(symbol);
            return !!(symbol.flags & 4 /* Property */ && symbol.flags & 16777216 /* Optional */ && isOptionalDeclaration(declaration) && ((_a ***REMOVED*** symbol.links) ***REMOVED******REMOVED*** null ? void 0 : _a.mappedType) && containsNonMissingUndefinedType(type));
          case 170 /* Parameter */:
          case 342 /* JSDocParameterTag */:
            return requiresAddingImplicitUndefined(declaration, enclosingDeclaration);
          default:
            Debug.assertNever(declaration);
        }
      },
      isOptionalParameter,
      isUndefinedIdentifierExpression(node) {
        return getSymbolAtLocation(node) ***REMOVED******REMOVED******REMOVED*** undefinedSymbol;
      },
      isEntityNameVisible(context, entityName, shouldComputeAliasToMakeVisible) {
        return isEntityNameVisible(entityName, context.enclosingDeclaration, shouldComputeAliasToMakeVisible);
      },
      serializeExistingTypeNode(context, typeNode, addUndefined) {
        return serializeExistingTypeNode(context, typeNode, !!addUndefined);
      },
      serializeReturnTypeForSignature(syntacticContext, signatureDeclaration, symbol) {
        const context ***REMOVED*** syntacticContext;
        const signature ***REMOVED*** getSignatureFromDeclaration(signatureDeclaration);
        symbol ?? (symbol ***REMOVED*** getSymbolOfDeclaration(signatureDeclaration));
        const returnType ***REMOVED*** context.enclosingSymbolTypes.get(getSymbolId(symbol)) ?? instantiateType(getReturnTypeOfSignature(signature), context.mapper);
        return serializeInferredReturnTypeForSignature(context, signature, returnType);
      },
      serializeTypeOfExpression(syntacticContext, expr) {
        const context ***REMOVED*** syntacticContext;
        const type ***REMOVED*** instantiateType(getWidenedType(getRegularTypeOfExpression(expr)), context.mapper);
        return typeToTypeNodeHelper(type, context);
      },
      serializeTypeOfDeclaration(syntacticContext, declaration, symbol) {
        var _a;
        const context ***REMOVED*** syntacticContext;
        symbol ?? (symbol ***REMOVED*** getSymbolOfDeclaration(declaration));
        let type ***REMOVED*** (_a ***REMOVED*** context.enclosingSymbolTypes) ***REMOVED******REMOVED*** null ? void 0 : _a.get(getSymbolId(symbol));
        if (type ***REMOVED******REMOVED******REMOVED*** void 0) {
          type ***REMOVED*** symbol.flags & 98304 /* Accessor */ && declaration.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ ? instantiateType(getWriteTypeOfSymbol(symbol), context.mapper) : symbol && !(symbol.flags & (2048 /* TypeLiteral */ | 131072 /* Signature */)) ? instantiateType(getWidenedLiteralType(getTypeOfSymbol(symbol)), context.mapper) : errorType;
        }
        const addUndefinedForParameter ***REMOVED*** declaration && (isParameter(declaration) || isJSDocParameterTag(declaration)) && requiresAddingImplicitUndefined(declaration, context.enclosingDeclaration);
        if (addUndefinedForParameter) {
          type ***REMOVED*** getOptionalType(type);
        }
        return serializeInferredTypeForDeclaration(symbol, context, type);
      },
      serializeNameOfParameter(context, parameter) {
        return parameterToParameterDeclarationName(getSymbolOfDeclaration(parameter), parameter, context);
      },
      serializeEntityName(syntacticContext, node) {
        const context ***REMOVED*** syntacticContext;
        const symbol ***REMOVED*** getSymbolAtLocation(
          node,
          /*ignoreErrors*/
          true
        );
        if (!symbol) return void 0;
        if (!isValueSymbolAccessible(symbol, context.enclosingDeclaration)) return void 0;
        return symbolToExpression(symbol, context, 111551 /* Value */ | 1048576 /* ExportValue */);
      },
      serializeTypeName(context, node, isTypeOf, typeArguments) {
        return serializeTypeName(context, node, isTypeOf, typeArguments);
      },
      getJsDocPropertyOverride(syntacticContext, jsDocTypeLiteral, jsDocProperty) {
        const context ***REMOVED*** syntacticContext;
        const name ***REMOVED*** isIdentifier(jsDocProperty.name) ? jsDocProperty.name : jsDocProperty.name.right;
        const typeViaParent ***REMOVED*** getTypeOfPropertyOfType(getTypeFromTypeNode2(context, jsDocTypeLiteral), name.escapedText);
        const overrideTypeNode ***REMOVED*** typeViaParent && jsDocProperty.typeExpression && getTypeFromTypeNode2(context, jsDocProperty.typeExpression.type) !***REMOVED******REMOVED*** typeViaParent ? typeToTypeNodeHelper(typeViaParent, context) : void 0;
        return overrideTypeNode;
      },
      enterNewScope(context, node) {
        if (isFunctionLike(node) || isJSDocSignature(node)) {
          const signature ***REMOVED*** getSignatureFromDeclaration(node);
          return enterNewScope(context, node, signature.parameters, signature.typeParameters);
        } else {
          const typeParameters ***REMOVED*** isConditionalTypeNode(node) ? getInferTypeParameters(node) : [getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(node.typeParameter))];
          return enterNewScope(
            context,
            node,
            /*expandedParams*/
            void 0,
            typeParameters
          );
        }
      },
      markNodeReuse(context, range, location) {
        return setTextRange2(context, range, location);
      },
      trackExistingEntityName(context, node) {
        return trackExistingEntityName(node, context);
      },
      trackComputedName(context, accessExpression) {
        trackComputedName(accessExpression, context.enclosingDeclaration, context);
      },
      getModuleSpecifierOverride(syntacticContext, parent, lit) {
        const context ***REMOVED*** syntacticContext;
        if (context.bundled || context.enclosingFile !***REMOVED******REMOVED*** getSourceFileOfNode(lit)) {
          let name ***REMOVED*** lit.text;
          const originalName ***REMOVED*** name;
          const nodeSymbol ***REMOVED*** getNodeLinks(parent).resolvedSymbol;
          const meaning ***REMOVED*** parent.isTypeOf ? 111551 /* Value */ : 788968 /* Type */;
          const parentSymbol ***REMOVED*** nodeSymbol && isSymbolAccessible(
            nodeSymbol,
            context.enclosingDeclaration,
            meaning,
            /*shouldComputeAliasesToMakeVisible*/
            false
          ).accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */ && lookupSymbolChain(
            nodeSymbol,
            context,
            meaning,
            /*yieldModuleSymbol*/
            true
          )[0];
          if (parentSymbol && isExternalModuleSymbol(parentSymbol)) {
            name ***REMOVED*** getSpecifierForModuleSymbol(parentSymbol, context);
          } else {
            const targetFile ***REMOVED*** getExternalModuleFileFromDeclaration(parent);
            if (targetFile) {
              name ***REMOVED*** getSpecifierForModuleSymbol(targetFile.symbol, context);
            }
          }
          if (name.includes("/node_modules/")) {
            context.encounteredError ***REMOVED*** true;
            if (context.tracker.reportLikelyUnsafeImportRequiredError) {
              context.tracker.reportLikelyUnsafeImportRequiredError(name);
            }
          }
          if (name !***REMOVED******REMOVED*** originalName) {
            return name;
          }
        }
      },
      canReuseTypeNode(context, typeNode) {
        return canReuseTypeNode(context, typeNode);
      },
      canReuseTypeNodeAnnotation(syntacticContext, node, existing, symbol, requiresAddingUndefined) {
        var _a;
        const context ***REMOVED*** syntacticContext;
        if (context.enclosingDeclaration ***REMOVED******REMOVED******REMOVED*** void 0) return false;
        symbol ?? (symbol ***REMOVED*** getSymbolOfDeclaration(node));
        let type ***REMOVED*** (_a ***REMOVED*** context.enclosingSymbolTypes) ***REMOVED******REMOVED*** null ? void 0 : _a.get(getSymbolId(symbol));
        if (type ***REMOVED******REMOVED******REMOVED*** void 0) {
          if (symbol.flags & 98304 /* Accessor */) {
            type ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ ? getWriteTypeOfSymbol(symbol) : getTypeOfAccessors(symbol);
          } else if (isValueSignatureDeclaration(node)) {
            type ***REMOVED*** getReturnTypeOfSignature(getSignatureFromDeclaration(node));
          } else {
            type ***REMOVED*** getTypeOfSymbol(symbol);
          }
        }
        let annotationType ***REMOVED*** getTypeFromTypeNodeWithoutContext(existing);
        if (isErrorType(annotationType)) {
          return true;
        }
        if (requiresAddingUndefined && annotationType) {
          annotationType ***REMOVED*** addOptionality(annotationType, !isParameter(node));
        }
        return !!annotationType && typeNodeIsEquivalentToType(node, type, annotationType) && existingTypeNodeIsNotReferenceOrIsReferenceWithCompatibleTypeArgumentCount(existing, type);
      }
    };
    return {
      syntacticBuilderResolver,
      typeToTypeNode: (type, enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, out) ***REMOVED***> withContext(enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, (context) ***REMOVED***> typeToTypeNodeHelper(type, context), out),
      typePredicateToTypePredicateNode: (typePredicate, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> typePredicateToTypePredicateNodeHelper(typePredicate, context)
      ),
      serializeTypeForDeclaration: (declaration, symbol, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> syntacticNodeBuilder.serializeTypeOfDeclaration(declaration, symbol, context)
      ),
      serializeReturnTypeForSignature: (signature, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> syntacticNodeBuilder.serializeReturnTypeForSignature(signature, getSymbolOfDeclaration(signature), context)
      ),
      serializeTypeForExpression: (expr, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> syntacticNodeBuilder.serializeTypeOfExpression(expr, context)
      ),
      indexInfoToIndexSignatureDeclaration: (indexInfo, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> indexInfoToIndexSignatureDeclarationHelper(
          indexInfo,
          context,
          /*typeNode*/
          void 0
        )
      ),
      signatureToSignatureDeclaration: (signature, kind, enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, out) ***REMOVED***> withContext(enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, (context) ***REMOVED***> signatureToSignatureDeclarationHelper(signature, kind, context), out),
      symbolToEntityName: (symbol, meaning, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> symbolToName(
          symbol,
          context,
          meaning,
          /*expectsIdentifier*/
          false
        )
      ),
      symbolToExpression: (symbol, meaning, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> symbolToExpression(symbol, context, meaning)
      ),
      symbolToTypeParameterDeclarations: (symbol, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> typeParametersToTypeParameterDeclarations(symbol, context)
      ),
      symbolToParameterDeclaration: (symbol, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> symbolToParameterDeclaration(symbol, context)
      ),
      typeParameterToDeclaration: (parameter, enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, out) ***REMOVED***> withContext(enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, (context) ***REMOVED***> typeParameterToDeclaration(parameter, context), out),
      symbolTableToDeclarationStatements: (symbolTable, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> symbolTableToDeclarationStatements(symbolTable, context)
      ),
      symbolToNode: (symbol, meaning, enclosingDeclaration, flags, internalFlags, tracker) ***REMOVED***> withContext(
        enclosingDeclaration,
        flags,
        internalFlags,
        tracker,
        /*maximumLength*/
        void 0,
        /*verbosityLevel*/
        void 0,
        (context) ***REMOVED***> symbolToNode(symbol, context, meaning)
      ),
      symbolToDeclarations
    };
    function getTypeFromTypeNode2(context, node, noMappedTypes) {
      const type ***REMOVED*** getTypeFromTypeNodeWithoutContext(node);
      if (!context.mapper) return type;
      const mappedType ***REMOVED*** instantiateType(type, context.mapper);
      return noMappedTypes && mappedType !***REMOVED******REMOVED*** type ? void 0 : mappedType;
    }
    function setTextRange2(context, range, location) {
      if (!nodeIsSynthesized(range) || !(range.flags & 16 /* Synthesized */) || !context.enclosingFile || context.enclosingFile !***REMOVED******REMOVED*** getSourceFileOfNode(getOriginalNode(range))) {
        range ***REMOVED*** factory.cloneNode(range);
      }
      if (range ***REMOVED******REMOVED******REMOVED*** location) return range;
      if (!location) {
        return range;
      }
      let original ***REMOVED*** range.original;
      while (original && original !***REMOVED******REMOVED*** location) {
        original ***REMOVED*** original.original;
      }
      if (!original) {
        setOriginalNode(range, location);
      }
      if (context.enclosingFile && context.enclosingFile ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(getOriginalNode(location))) {
        return setTextRange(range, location);
      }
      return range;
    }
    function symbolToNode(symbol, context, meaning) {
      if (context.internalFlags & 1 /* WriteComputedProps */) {
        if (symbol.valueDeclaration) {
          const name ***REMOVED*** getNameOfDeclaration(symbol.valueDeclaration);
          if (name && isComputedPropertyName(name)) return name;
        }
        const nameType ***REMOVED*** getSymbolLinks(symbol).nameType;
        if (nameType && nameType.flags & (1024 /* EnumLiteral */ | 8192 /* UniqueESSymbol */)) {
          context.enclosingDeclaration ***REMOVED*** nameType.symbol.valueDeclaration;
          return factory.createComputedPropertyName(symbolToExpression(nameType.symbol, context, meaning));
        }
      }
      return symbolToExpression(symbol, context, meaning);
    }
    function symbolToDeclarations(symbol, meaning, flags, maximumLength, verbosityLevel, out) {
      const nodes ***REMOVED*** withContext(
        /*enclosingDeclaration*/
        void 0,
        flags,
        /*internalFlags*/
        void 0,
        /*tracker*/
        void 0,
        maximumLength,
        verbosityLevel,
        (context) ***REMOVED***> symbolToDeclarationsWorker(symbol, context),
        out
      );
      return mapDefined(nodes, (node) ***REMOVED***> {
        switch (node.kind) {
          case 264 /* ClassDeclaration */:
            return simplifyClassDeclaration(node, symbol);
          case 267 /* EnumDeclaration */:
            return simplifyModifiers(node, isEnumDeclaration, symbol);
          case 265 /* InterfaceDeclaration */:
            return simplifyInterfaceDeclaration(node, symbol, meaning);
          case 268 /* ModuleDeclaration */:
            return simplifyModifiers(node, isModuleDeclaration, symbol);
          default:
            return void 0;
        }
      });
    }
    function simplifyClassDeclaration(classDecl, symbol) {
      const classDeclarations ***REMOVED*** filter(symbol.declarations, isClassLike);
      const originalClassDecl ***REMOVED*** classDeclarations && classDeclarations.length > 0 ? classDeclarations[0] : classDecl;
      const modifiers ***REMOVED*** getEffectiveModifierFlags(originalClassDecl) & ~(32 /* Export */ | 128 /* Ambient */);
      const isAnonymous ***REMOVED*** isClassExpression(originalClassDecl);
      if (isAnonymous) {
        classDecl ***REMOVED*** factory.updateClassDeclaration(
          classDecl,
          classDecl.modifiers,
          /*name*/
          void 0,
          classDecl.typeParameters,
          classDecl.heritageClauses,
          classDecl.members
        );
      }
      return factory.replaceModifiers(classDecl, modifiers);
    }
    function simplifyModifiers(newDecl, isDeclKind, symbol) {
      const decls ***REMOVED*** filter(symbol.declarations, isDeclKind);
      const declWithModifiers ***REMOVED*** decls && decls.length > 0 ? decls[0] : newDecl;
      const modifiers ***REMOVED*** getEffectiveModifierFlags(declWithModifiers) & ~(32 /* Export */ | 128 /* Ambient */);
      return factory.replaceModifiers(newDecl, modifiers);
    }
    function simplifyInterfaceDeclaration(interfaceDecl, symbol, meaning) {
      if (!(meaning & 64 /* Interface */)) {
        return void 0;
      }
      return simplifyModifiers(interfaceDecl, isInterfaceDeclaration, symbol);
    }
    function symbolToDeclarationsWorker(symbol, context) {
      const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
      context.typeStack.push(type.id);
      context.typeStack.push(-1);
      const table ***REMOVED*** createSymbolTable([symbol]);
      const statements ***REMOVED*** symbolTableToDeclarationStatements(table, context);
      context.typeStack.pop();
      context.typeStack.pop();
      return statements;
    }
    function withContext(enclosingDeclaration, flags, internalFlags, tracker, maximumLength, verbosityLevel, cb, out) {
      const moduleResolverHost ***REMOVED*** (tracker ***REMOVED******REMOVED*** null ? void 0 : tracker.trackSymbol) ? tracker.moduleResolverHost : (internalFlags || 0 /* None */) & 4 /* DoNotIncludeSymbolChain */ ? createBasicNodeBuilderModuleSpecifierResolutionHost(host) : void 0;
      flags ***REMOVED*** flags || 0 /* None */;
      const maxTruncationLength ***REMOVED*** maximumLength || (flags & 1 /* NoTruncation */ ? noTruncationMaximumTruncationLength : defaultMaximumTruncationLength);
      const context ***REMOVED*** {
        enclosingDeclaration,
        enclosingFile: enclosingDeclaration && getSourceFileOfNode(enclosingDeclaration),
        flags,
        internalFlags: internalFlags || 0 /* None */,
        tracker: void 0,
        maxTruncationLength,
        maxExpansionDepth: verbosityLevel ?? -1,
        encounteredError: false,
        suppressReportInferenceFallback: false,
        reportedDiagnostic: false,
        visitedTypes: void 0,
        symbolDepth: void 0,
        inferTypeParameters: void 0,
        approximateLength: 0,
        trackedSymbols: void 0,
        bundled: !!compilerOptions.outFile && !!enclosingDeclaration && isExternalOrCommonJsModule(getSourceFileOfNode(enclosingDeclaration)),
        truncating: false,
        usedSymbolNames: void 0,
        remappedSymbolNames: void 0,
        remappedSymbolReferences: void 0,
        reverseMappedStack: void 0,
        mustCreateTypeParameterSymbolList: true,
        typeParameterSymbolList: void 0,
        mustCreateTypeParametersNamesLookups: true,
        typeParameterNames: void 0,
        typeParameterNamesByText: void 0,
        typeParameterNamesByTextNextNameCount: void 0,
        enclosingSymbolTypes: /* @__PURE__ */ new Map(),
        mapper: void 0,
        depth: 0,
        typeStack: [],
        out: {
          canIncreaseExpansionDepth: false,
          truncated: false
        }
      };
      context.tracker ***REMOVED*** new SymbolTrackerImpl(context, tracker, moduleResolverHost);
      const resultingNode ***REMOVED*** cb(context);
      if (context.truncating && context.flags & 1 /* NoTruncation */) {
        context.tracker.reportTruncationError();
      }
      if (out) {
        out.canIncreaseExpansionDepth ***REMOVED*** context.out.canIncreaseExpansionDepth;
        out.truncated ***REMOVED*** context.out.truncated;
      }
      return context.encounteredError ? void 0 : resultingNode;
    }
    function addSymbolTypeToContext(context, symbol, type) {
      const id ***REMOVED*** getSymbolId(symbol);
      const oldType ***REMOVED*** context.enclosingSymbolTypes.get(id);
      context.enclosingSymbolTypes.set(id, type);
      return restore;
      function restore() {
        if (oldType) {
          context.enclosingSymbolTypes.set(id, oldType);
        } else {
          context.enclosingSymbolTypes.delete(id);
        }
      }
    }
    function saveRestoreFlags(context) {
      const flags ***REMOVED*** context.flags;
      const internalFlags ***REMOVED*** context.internalFlags;
      const depth ***REMOVED*** context.depth;
      return restore;
      function restore() {
        context.flags ***REMOVED*** flags;
        context.internalFlags ***REMOVED*** internalFlags;
        context.depth ***REMOVED*** depth;
      }
    }
    function checkTruncationLengthIfExpanding(context) {
      return context.maxExpansionDepth >***REMOVED*** 0 && checkTruncationLength(context);
    }
    function checkTruncationLength(context) {
      if (context.truncating) return context.truncating;
      return context.truncating ***REMOVED*** context.approximateLength > context.maxTruncationLength;
    }
    function canPossiblyExpandType(type, context) {
      for (let i ***REMOVED*** 0; i < context.typeStack.length - 1; i++) {
        if (context.typeStack[i] ***REMOVED******REMOVED******REMOVED*** type.id) {
          return false;
        }
      }
      return context.depth < context.maxExpansionDepth || context.depth ***REMOVED******REMOVED******REMOVED*** context.maxExpansionDepth && !context.out.canIncreaseExpansionDepth;
    }
    function shouldExpandType(type, context, isAlias ***REMOVED*** false) {
      if (!isAlias && isLibType(type)) {
        return false;
      }
      for (let i ***REMOVED*** 0; i < context.typeStack.length - 1; i++) {
        if (context.typeStack[i] ***REMOVED******REMOVED******REMOVED*** type.id) {
          return false;
        }
      }
      const result ***REMOVED*** context.depth < context.maxExpansionDepth;
      if (!result) {
        context.out.canIncreaseExpansionDepth ***REMOVED*** true;
      }
      return result;
    }
    function typeToTypeNodeHelper(type, context) {
      const restoreFlags ***REMOVED*** saveRestoreFlags(context);
      if (type) context.typeStack.push(type.id);
      const typeNode ***REMOVED*** typeToTypeNodeWorker(type, context);
      if (type) context.typeStack.pop();
      restoreFlags();
      return typeNode;
    }
    function typeToTypeNodeWorker(type, context) {
      var _a, _b;
      if (cancellationToken && cancellationToken.throwIfCancellationRequested) {
        cancellationToken.throwIfCancellationRequested();
      }
      const inTypeAlias ***REMOVED*** context.flags & 8388608 /* InTypeAlias */;
      context.flags &***REMOVED*** ~8388608 /* InTypeAlias */;
      let expandingEnum ***REMOVED*** false;
      if (!type) {
        if (!(context.flags & 262144 /* AllowEmptyUnionOrIntersection */)) {
          context.encounteredError ***REMOVED*** true;
          return void 0;
        }
        context.approximateLength +***REMOVED*** 3;
        return factory.createKeywordTypeNode(133 /* AnyKeyword */);
      }
      if (!(context.flags & 536870912 /* NoTypeReduction */)) {
        type ***REMOVED*** getReducedType(type);
      }
      if (type.flags & 1 /* Any */) {
        if (type.aliasSymbol) {
          return factory.createTypeReferenceNode(symbolToEntityNameNode(type.aliasSymbol), mapToTypeNodes(type.aliasTypeArguments, context));
        }
        if (type ***REMOVED******REMOVED******REMOVED*** unresolvedType) {
          return addSyntheticLeadingComment(factory.createKeywordTypeNode(133 /* AnyKeyword */), 3 /* MultiLineCommentTrivia */, "unresolved");
        }
        context.approximateLength +***REMOVED*** 3;
        return factory.createKeywordTypeNode(type ***REMOVED******REMOVED******REMOVED*** intrinsicMarkerType ? 141 /* IntrinsicKeyword */ : 133 /* AnyKeyword */);
      }
      if (type.flags & 2 /* Unknown */) {
        return factory.createKeywordTypeNode(159 /* UnknownKeyword */);
      }
      if (type.flags & 4 /* String */) {
        context.approximateLength +***REMOVED*** 6;
        return factory.createKeywordTypeNode(154 /* StringKeyword */);
      }
      if (type.flags & 8 /* Number */) {
        context.approximateLength +***REMOVED*** 6;
        return factory.createKeywordTypeNode(150 /* NumberKeyword */);
      }
      if (type.flags & 64 /* BigInt */) {
        context.approximateLength +***REMOVED*** 6;
        return factory.createKeywordTypeNode(163 /* BigIntKeyword */);
      }
      if (type.flags & 16 /* Boolean */ && !type.aliasSymbol) {
        context.approximateLength +***REMOVED*** 7;
        return factory.createKeywordTypeNode(136 /* BooleanKeyword */);
      }
      if (type.flags & 1056 /* EnumLike */) {
        if (type.symbol.flags & 8 /* EnumMember */) {
          const parentSymbol ***REMOVED*** getParentOfSymbol(type.symbol);
          const parentName ***REMOVED*** symbolToTypeNode(parentSymbol, context, 788968 /* Type */);
          if (getDeclaredTypeOfSymbol(parentSymbol) ***REMOVED******REMOVED******REMOVED*** type) {
            return parentName;
          }
          const memberName ***REMOVED*** symbolName(type.symbol);
          if (isIdentifierText(memberName, 1 /* ES5 */)) {
            return appendReferenceToType(
              parentName,
              factory.createTypeReferenceNode(
                memberName,
                /*typeArguments*/
                void 0
              )
            );
          }
          if (isImportTypeNode(parentName)) {
            parentName.isTypeOf ***REMOVED*** true;
            return factory.createIndexedAccessTypeNode(parentName, factory.createLiteralTypeNode(factory.createStringLiteral(memberName)));
          } else if (isTypeReferenceNode(parentName)) {
            return factory.createIndexedAccessTypeNode(factory.createTypeQueryNode(parentName.typeName), factory.createLiteralTypeNode(factory.createStringLiteral(memberName)));
          } else {
            return Debug.fail("Unhandled type node kind returned from `symbolToTypeNode`.");
          }
        }
        if (!shouldExpandType(type, context)) {
          return symbolToTypeNode(type.symbol, context, 788968 /* Type */);
        } else {
          expandingEnum ***REMOVED*** true;
        }
      }
      if (type.flags & 128 /* StringLiteral */) {
        context.approximateLength +***REMOVED*** type.value.length + 2;
        return factory.createLiteralTypeNode(setEmitFlags(factory.createStringLiteral(type.value, !!(context.flags & 268435456 /* UseSingleQuotesForStringLiteralType */)), 16777216 /* NoAsciiEscaping */));
      }
      if (type.flags & 256 /* NumberLiteral */) {
        const value ***REMOVED*** type.value;
        context.approximateLength +***REMOVED*** ("" + value).length;
        return factory.createLiteralTypeNode(value < 0 ? factory.createPrefixUnaryExpression(41 /* MinusToken */, factory.createNumericLiteral(-value)) : factory.createNumericLiteral(value));
      }
      if (type.flags & 2048 /* BigIntLiteral */) {
        context.approximateLength +***REMOVED*** pseudoBigIntToString(type.value).length + 1;
        return factory.createLiteralTypeNode(factory.createBigIntLiteral(type.value));
      }
      if (type.flags & 512 /* BooleanLiteral */) {
        context.approximateLength +***REMOVED*** type.intrinsicName.length;
        return factory.createLiteralTypeNode(type.intrinsicName ***REMOVED******REMOVED******REMOVED*** "true" ? factory.createTrue() : factory.createFalse());
      }
      if (type.flags & 8192 /* UniqueESSymbol */) {
        if (!(context.flags & 1048576 /* AllowUniqueESSymbolType */)) {
          if (isValueSymbolAccessible(type.symbol, context.enclosingDeclaration)) {
            context.approximateLength +***REMOVED*** 6;
            return symbolToTypeNode(type.symbol, context, 111551 /* Value */);
          }
          if (context.tracker.reportInaccessibleUniqueSymbolError) {
            context.tracker.reportInaccessibleUniqueSymbolError();
          }
        }
        context.approximateLength +***REMOVED*** 13;
        return factory.createTypeOperatorNode(158 /* UniqueKeyword */, factory.createKeywordTypeNode(155 /* SymbolKeyword */));
      }
      if (type.flags & 16384 /* Void */) {
        context.approximateLength +***REMOVED*** 4;
        return factory.createKeywordTypeNode(116 /* VoidKeyword */);
      }
      if (type.flags & 32768 /* Undefined */) {
        context.approximateLength +***REMOVED*** 9;
        return factory.createKeywordTypeNode(157 /* UndefinedKeyword */);
      }
      if (type.flags & 65536 /* Null */) {
        context.approximateLength +***REMOVED*** 4;
        return factory.createLiteralTypeNode(factory.createNull());
      }
      if (type.flags & 131072 /* Never */) {
        context.approximateLength +***REMOVED*** 5;
        return factory.createKeywordTypeNode(146 /* NeverKeyword */);
      }
      if (type.flags & 4096 /* ESSymbol */) {
        context.approximateLength +***REMOVED*** 6;
        return factory.createKeywordTypeNode(155 /* SymbolKeyword */);
      }
      if (type.flags & 67108864 /* NonPrimitive */) {
        context.approximateLength +***REMOVED*** 6;
        return factory.createKeywordTypeNode(151 /* ObjectKeyword */);
      }
      if (isThisTypeParameter(type)) {
        if (context.flags & 4194304 /* InObjectTypeLiteral */) {
          if (!context.encounteredError && !(context.flags & 32768 /* AllowThisInObjectLiteral */)) {
            context.encounteredError ***REMOVED*** true;
          }
          (_b ***REMOVED*** (_a ***REMOVED*** context.tracker).reportInaccessibleThisError) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a);
        }
        context.approximateLength +***REMOVED*** 4;
        return factory.createThisTypeNode();
      }
      if (!inTypeAlias && type.aliasSymbol && (context.flags & 16384 /* UseAliasDefinedOutsideCurrentScope */ || isTypeSymbolAccessible(type.aliasSymbol, context.enclosingDeclaration))) {
        if (!shouldExpandType(
          type,
          context,
          /*isAlias*/
          true
        )) {
          const typeArgumentNodes ***REMOVED*** mapToTypeNodes(type.aliasTypeArguments, context);
          if (isReservedMemberName(type.aliasSymbol.escapedName) && !(type.aliasSymbol.flags & 32 /* Class */)) return factory.createTypeReferenceNode(factory.createIdentifier(""), typeArgumentNodes);
          if (length(typeArgumentNodes) ***REMOVED******REMOVED******REMOVED*** 1 && type.aliasSymbol ***REMOVED******REMOVED******REMOVED*** globalArrayType.symbol) {
            return factory.createArrayTypeNode(typeArgumentNodes[0]);
          }
          return symbolToTypeNode(type.aliasSymbol, context, 788968 /* Type */, typeArgumentNodes);
        }
        context.depth +***REMOVED*** 1;
      }
      const objectFlags ***REMOVED*** getObjectFlags(type);
      if (objectFlags & 4 /* Reference */) {
        Debug.assert(!!(type.flags & 524288 /* Object */));
        if (shouldExpandType(type, context)) {
          context.depth +***REMOVED*** 1;
          return createAnonymousTypeNode(
            type,
            /*forceClassExpansion*/
            true,
            /*forceExpansion*/
            true
          );
        }
        return type.node ? visitAndTransformType(type, typeReferenceToTypeNode) : typeReferenceToTypeNode(type);
      }
      if (type.flags & 262144 /* TypeParameter */ || objectFlags & 3 /* ClassOrInterface */) {
        if (type.flags & 262144 /* TypeParameter */ && contains(context.inferTypeParameters, type)) {
          context.approximateLength +***REMOVED*** symbolName(type.symbol).length + 6;
          let constraintNode;
          const constraint ***REMOVED*** getConstraintOfTypeParameter(type);
          if (constraint) {
            const inferredConstraint ***REMOVED*** getInferredTypeParameterConstraint(
              type,
              /*omitTypeReferences*/
              true
            );
            if (!(inferredConstraint && isTypeIdenticalTo(constraint, inferredConstraint))) {
              context.approximateLength +***REMOVED*** 9;
              constraintNode ***REMOVED*** constraint && typeToTypeNodeHelper(constraint, context);
            }
          }
          return factory.createInferTypeNode(typeParameterToDeclarationWithConstraint(type, context, constraintNode));
        }
        if (context.flags & 4 /* GenerateNamesForShadowedTypeParams */ && type.flags & 262144 /* TypeParameter */) {
          const name2 ***REMOVED*** typeParameterToName(type, context);
          context.approximateLength +***REMOVED*** idText(name2).length;
          return factory.createTypeReferenceNode(
            factory.createIdentifier(idText(name2)),
            /*typeArguments*/
            void 0
          );
        }
        if (objectFlags & 3 /* ClassOrInterface */ && shouldExpandType(type, context)) {
          context.depth +***REMOVED*** 1;
          return createAnonymousTypeNode(
            type,
            /*forceClassExpansion*/
            true,
            /*forceExpansion*/
            true
          );
        }
        if (type.symbol) {
          return symbolToTypeNode(type.symbol, context, 788968 /* Type */);
        }
        const name ***REMOVED*** (type ***REMOVED******REMOVED******REMOVED*** markerSuperTypeForCheck || type ***REMOVED******REMOVED******REMOVED*** markerSubTypeForCheck) && varianceTypeParameter && varianceTypeParameter.symbol ? (type ***REMOVED******REMOVED******REMOVED*** markerSubTypeForCheck ? "sub-" : "super-") + symbolName(varianceTypeParameter.symbol) : "?";
        return factory.createTypeReferenceNode(
          factory.createIdentifier(name),
          /*typeArguments*/
          void 0
        );
      }
      if (type.flags & 1048576 /* Union */ && type.origin) {
        type ***REMOVED*** type.origin;
      }
      if (type.flags & (1048576 /* Union */ | 2097152 /* Intersection */)) {
        const types ***REMOVED*** type.flags & 1048576 /* Union */ ? formatUnionTypes(type.types, expandingEnum) : type.types;
        if (length(types) ***REMOVED******REMOVED******REMOVED*** 1) {
          return typeToTypeNodeHelper(types[0], context);
        }
        const typeNodes ***REMOVED*** mapToTypeNodes(
          types,
          context,
          /*isBareList*/
          true
        );
        if (typeNodes && typeNodes.length > 0) {
          return type.flags & 1048576 /* Union */ ? factory.createUnionTypeNode(typeNodes) : factory.createIntersectionTypeNode(typeNodes);
        } else {
          if (!context.encounteredError && !(context.flags & 262144 /* AllowEmptyUnionOrIntersection */)) {
            context.encounteredError ***REMOVED*** true;
          }
          return void 0;
        }
      }
      if (objectFlags & (16 /* Anonymous */ | 32 /* Mapped */)) {
        Debug.assert(!!(type.flags & 524288 /* Object */));
        return createAnonymousTypeNode(type);
      }
      if (type.flags & 4194304 /* Index */) {
        const indexedType ***REMOVED*** type.type;
        context.approximateLength +***REMOVED*** 6;
        const indexTypeNode ***REMOVED*** typeToTypeNodeHelper(indexedType, context);
        return factory.createTypeOperatorNode(143 /* KeyOfKeyword */, indexTypeNode);
      }
      if (type.flags & 134217728 /* TemplateLiteral */) {
        const texts ***REMOVED*** type.texts;
        const types ***REMOVED*** type.types;
        const templateHead ***REMOVED*** factory.createTemplateHead(texts[0]);
        const templateSpans ***REMOVED*** factory.createNodeArray(
          map(types, (t, i) ***REMOVED***> factory.createTemplateLiteralTypeSpan(
            typeToTypeNodeHelper(t, context),
            (i < types.length - 1 ? factory.createTemplateMiddle : factory.createTemplateTail)(texts[i + 1])
          ))
        );
        context.approximateLength +***REMOVED*** 2;
        return factory.createTemplateLiteralType(templateHead, templateSpans);
      }
      if (type.flags & 268435456 /* StringMapping */) {
        const typeNode ***REMOVED*** typeToTypeNodeHelper(type.type, context);
        return symbolToTypeNode(type.symbol, context, 788968 /* Type */, [typeNode]);
      }
      if (type.flags & 8388608 /* IndexedAccess */) {
        const objectTypeNode ***REMOVED*** typeToTypeNodeHelper(type.objectType, context);
        const indexTypeNode ***REMOVED*** typeToTypeNodeHelper(type.indexType, context);
        context.approximateLength +***REMOVED*** 2;
        return factory.createIndexedAccessTypeNode(objectTypeNode, indexTypeNode);
      }
      if (type.flags & 16777216 /* Conditional */) {
        return visitAndTransformType(type, (type2) ***REMOVED***> conditionalTypeToTypeNode(type2));
      }
      if (type.flags & 33554432 /* Substitution */) {
        const typeNode ***REMOVED*** typeToTypeNodeHelper(type.baseType, context);
        const noInferSymbol ***REMOVED*** isNoInferType(type) && getGlobalTypeSymbol(
          "NoInfer",
          /*reportErrors*/
          false
        );
        return noInferSymbol ? symbolToTypeNode(noInferSymbol, context, 788968 /* Type */, [typeNode]) : typeNode;
      }
      return Debug.fail("Should be unreachable.");
      function conditionalTypeToTypeNode(type2) {
        const checkTypeNode ***REMOVED*** typeToTypeNodeHelper(type2.checkType, context);
        context.approximateLength +***REMOVED*** 15;
        if (context.flags & 4 /* GenerateNamesForShadowedTypeParams */ && type2.root.isDistributive && !(type2.checkType.flags & 262144 /* TypeParameter */)) {
          const newParam ***REMOVED*** createTypeParameter(createSymbol(262144 /* TypeParameter */, "T"));
          const name ***REMOVED*** typeParameterToName(newParam, context);
          const newTypeVariable ***REMOVED*** factory.createTypeReferenceNode(name);
          context.approximateLength +***REMOVED*** 37;
          const newMapper ***REMOVED*** prependTypeMapping(type2.root.checkType, newParam, type2.mapper);
          const saveInferTypeParameters2 ***REMOVED*** context.inferTypeParameters;
          context.inferTypeParameters ***REMOVED*** type2.root.inferTypeParameters;
          const extendsTypeNode2 ***REMOVED*** typeToTypeNodeHelper(instantiateType(type2.root.extendsType, newMapper), context);
          context.inferTypeParameters ***REMOVED*** saveInferTypeParameters2;
          const trueTypeNode2 ***REMOVED*** typeToTypeNodeOrCircularityElision(instantiateType(getTypeFromTypeNode2(context, type2.root.node.trueType), newMapper));
          const falseTypeNode2 ***REMOVED*** typeToTypeNodeOrCircularityElision(instantiateType(getTypeFromTypeNode2(context, type2.root.node.falseType), newMapper));
          return factory.createConditionalTypeNode(
            checkTypeNode,
            factory.createInferTypeNode(factory.createTypeParameterDeclaration(
              /*modifiers*/
              void 0,
              factory.cloneNode(newTypeVariable.typeName)
            )),
            factory.createConditionalTypeNode(
              factory.createTypeReferenceNode(factory.cloneNode(name)),
              typeToTypeNodeHelper(type2.checkType, context),
              factory.createConditionalTypeNode(newTypeVariable, extendsTypeNode2, trueTypeNode2, falseTypeNode2),
              factory.createKeywordTypeNode(146 /* NeverKeyword */)
            ),
            factory.createKeywordTypeNode(146 /* NeverKeyword */)
          );
        }
        const saveInferTypeParameters ***REMOVED*** context.inferTypeParameters;
        context.inferTypeParameters ***REMOVED*** type2.root.inferTypeParameters;
        const extendsTypeNode ***REMOVED*** typeToTypeNodeHelper(type2.extendsType, context);
        context.inferTypeParameters ***REMOVED*** saveInferTypeParameters;
        const trueTypeNode ***REMOVED*** typeToTypeNodeOrCircularityElision(getTrueTypeFromConditionalType(type2));
        const falseTypeNode ***REMOVED*** typeToTypeNodeOrCircularityElision(getFalseTypeFromConditionalType(type2));
        return factory.createConditionalTypeNode(checkTypeNode, extendsTypeNode, trueTypeNode, falseTypeNode);
      }
      function typeToTypeNodeOrCircularityElision(type2) {
        var _a2, _b2, _c;
        if (type2.flags & 1048576 /* Union */) {
          if ((_a2 ***REMOVED*** context.visitedTypes) ***REMOVED******REMOVED*** null ? void 0 : _a2.has(getTypeId(type2))) {
            if (!(context.flags & 131072 /* AllowAnonymousIdentifier */)) {
              context.encounteredError ***REMOVED*** true;
              (_c ***REMOVED*** (_b2 ***REMOVED*** context.tracker) ***REMOVED******REMOVED*** null ? void 0 : _b2.reportCyclicStructureError) ***REMOVED******REMOVED*** null ? void 0 : _c.call(_b2);
            }
            return createElidedInformationPlaceholder(context);
          }
          return visitAndTransformType(type2, (type3) ***REMOVED***> typeToTypeNodeHelper(type3, context));
        }
        return typeToTypeNodeHelper(type2, context);
      }
      function isMappedTypeHomomorphic(type2) {
        return !!getHomomorphicTypeVariable(type2);
      }
      function isHomomorphicMappedTypeWithNonHomomorphicInstantiation(type2) {
        return !!type2.target && isMappedTypeHomomorphic(type2.target) && !isMappedTypeHomomorphic(type2);
      }
      function createMappedTypeNodeFromType(type2) {
        var _a2;
        Debug.assert(!!(type2.flags & 524288 /* Object */));
        const readonlyToken ***REMOVED*** type2.declaration.readonlyToken ? factory.createToken(type2.declaration.readonlyToken.kind) : void 0;
        const questionToken ***REMOVED*** type2.declaration.questionToken ? factory.createToken(type2.declaration.questionToken.kind) : void 0;
        let appropriateConstraintTypeNode;
        let newTypeVariable;
        let templateType ***REMOVED*** getTemplateTypeFromMappedType(type2);
        const typeParameter ***REMOVED*** getTypeParameterFromMappedType(type2);
        const needsModifierPreservingWrapper ***REMOVED*** !isMappedTypeWithKeyofConstraintDeclaration(type2) && !(getModifiersTypeFromMappedType(type2).flags & 2 /* Unknown */) && context.flags & 4 /* GenerateNamesForShadowedTypeParams */ && !(getConstraintTypeFromMappedType(type2).flags & 262144 /* TypeParameter */ && ((_a2 ***REMOVED*** getConstraintOfTypeParameter(getConstraintTypeFromMappedType(type2))) ***REMOVED******REMOVED*** null ? void 0 : _a2.flags) & 4194304 /* Index */);
        if (isMappedTypeWithKeyofConstraintDeclaration(type2)) {
          if (isHomomorphicMappedTypeWithNonHomomorphicInstantiation(type2) && context.flags & 4 /* GenerateNamesForShadowedTypeParams */) {
            const newConstraintParam ***REMOVED*** createTypeParameter(createSymbol(262144 /* TypeParameter */, "T"));
            const name ***REMOVED*** typeParameterToName(newConstraintParam, context);
            const target ***REMOVED*** type2.target;
            newTypeVariable ***REMOVED*** factory.createTypeReferenceNode(name);
            templateType ***REMOVED*** instantiateType(
              getTemplateTypeFromMappedType(target),
              makeArrayTypeMapper([getTypeParameterFromMappedType(target), getModifiersTypeFromMappedType(target)], [typeParameter, newConstraintParam])
            );
          }
          appropriateConstraintTypeNode ***REMOVED*** factory.createTypeOperatorNode(143 /* KeyOfKeyword */, newTypeVariable || typeToTypeNodeHelper(getModifiersTypeFromMappedType(type2), context));
        } else if (needsModifierPreservingWrapper) {
          const newParam ***REMOVED*** createTypeParameter(createSymbol(262144 /* TypeParameter */, "T"));
          const name ***REMOVED*** typeParameterToName(newParam, context);
          newTypeVariable ***REMOVED*** factory.createTypeReferenceNode(name);
          appropriateConstraintTypeNode ***REMOVED*** newTypeVariable;
        } else {
          appropriateConstraintTypeNode ***REMOVED*** typeToTypeNodeHelper(getConstraintTypeFromMappedType(type2), context);
        }
        const typeParameterNode ***REMOVED*** typeParameterToDeclarationWithConstraint(typeParameter, context, appropriateConstraintTypeNode);
        const cleanup ***REMOVED*** enterNewScope(
          context,
          type2.declaration,
          /*expandedParams*/
          void 0,
          [getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(type2.declaration.typeParameter))]
        );
        const nameTypeNode ***REMOVED*** type2.declaration.nameType ? typeToTypeNodeHelper(getNameTypeFromMappedType(type2), context) : void 0;
        const templateTypeNode ***REMOVED*** typeToTypeNodeHelper(removeMissingType(templateType, !!(getMappedTypeModifiers(type2) & 4 /* IncludeOptional */)), context);
        cleanup();
        const mappedTypeNode ***REMOVED*** factory.createMappedTypeNode(
          readonlyToken,
          typeParameterNode,
          nameTypeNode,
          questionToken,
          templateTypeNode,
          /*members*/
          void 0
        );
        context.approximateLength +***REMOVED*** 10;
        const result ***REMOVED*** setEmitFlags(mappedTypeNode, 1 /* SingleLine */);
        if (isHomomorphicMappedTypeWithNonHomomorphicInstantiation(type2) && context.flags & 4 /* GenerateNamesForShadowedTypeParams */) {
          const originalConstraint ***REMOVED*** instantiateType(getConstraintOfTypeParameter(getTypeFromTypeNode2(context, type2.declaration.typeParameter.constraint.type)) || unknownType, type2.mapper);
          return factory.createConditionalTypeNode(
            typeToTypeNodeHelper(getModifiersTypeFromMappedType(type2), context),
            factory.createInferTypeNode(factory.createTypeParameterDeclaration(
              /*modifiers*/
              void 0,
              factory.cloneNode(newTypeVariable.typeName),
              originalConstraint.flags & 2 /* Unknown */ ? void 0 : typeToTypeNodeHelper(originalConstraint, context)
            )),
            result,
            factory.createKeywordTypeNode(146 /* NeverKeyword */)
          );
        } else if (needsModifierPreservingWrapper) {
          return factory.createConditionalTypeNode(
            typeToTypeNodeHelper(getConstraintTypeFromMappedType(type2), context),
            factory.createInferTypeNode(factory.createTypeParameterDeclaration(
              /*modifiers*/
              void 0,
              factory.cloneNode(newTypeVariable.typeName),
              factory.createTypeOperatorNode(143 /* KeyOfKeyword */, typeToTypeNodeHelper(getModifiersTypeFromMappedType(type2), context))
            )),
            result,
            factory.createKeywordTypeNode(146 /* NeverKeyword */)
          );
        }
        return result;
      }
      function createAnonymousTypeNode(type2, forceClassExpansion ***REMOVED*** false, forceExpansion ***REMOVED*** false) {
        var _a2, _b2;
        const typeId ***REMOVED*** type2.id;
        const symbol ***REMOVED*** type2.symbol;
        if (symbol) {
          const isInstantiationExpressionType ***REMOVED*** !!(getObjectFlags(type2) & 8388608 /* InstantiationExpressionType */);
          if (isInstantiationExpressionType) {
            const instantiationExpressionType ***REMOVED*** type2;
            const existing ***REMOVED*** instantiationExpressionType.node;
            if (isTypeQueryNode(existing) && getTypeFromTypeNode2(context, existing) ***REMOVED******REMOVED******REMOVED*** type2) {
              const typeNode ***REMOVED*** syntacticNodeBuilder.tryReuseExistingTypeNode(context, existing);
              if (typeNode) {
                return typeNode;
              }
            }
            if ((_a2 ***REMOVED*** context.visitedTypes) ***REMOVED******REMOVED*** null ? void 0 : _a2.has(typeId)) {
              return createElidedInformationPlaceholder(context);
            }
            return visitAndTransformType(type2, createTypeNodeFromObjectType);
          }
          const isInstanceType ***REMOVED*** isClassInstanceSide(type2) ? 788968 /* Type */ : 111551 /* Value */;
          if (isJSConstructor(symbol.valueDeclaration)) {
            return symbolToTypeNode(symbol, context, isInstanceType);
          } else if (!forceExpansion && (symbol.flags & 32 /* Class */ && !forceClassExpansion && !getBaseTypeVariableOfClass(symbol) && !(symbol.valueDeclaration && isClassLike(symbol.valueDeclaration) && context.flags & 2048 /* WriteClassExpressionAsTypeLiteral */ && (!isClassDeclaration(symbol.valueDeclaration) || isSymbolAccessible(
            symbol,
            context.enclosingDeclaration,
            isInstanceType,
            /*shouldComputeAliasesToMakeVisible*/
            false
          ).accessibility !***REMOVED******REMOVED*** 0 /* Accessible */)) || symbol.flags & (384 /* Enum */ | 512 /* ValueModule */) || shouldWriteTypeOfFunctionSymbol())) {
            if (shouldExpandType(type2, context)) {
              context.depth +***REMOVED*** 1;
            } else {
              return symbolToTypeNode(symbol, context, isInstanceType);
            }
          }
          if ((_b2 ***REMOVED*** context.visitedTypes) ***REMOVED******REMOVED*** null ? void 0 : _b2.has(typeId)) {
            const typeAlias ***REMOVED*** getTypeAliasForTypeLiteral(type2);
            if (typeAlias) {
              return symbolToTypeNode(typeAlias, context, 788968 /* Type */);
            } else {
              return createElidedInformationPlaceholder(context);
            }
          } else {
            return visitAndTransformType(type2, createTypeNodeFromObjectType);
          }
        } else {
          return createTypeNodeFromObjectType(type2);
        }
        function shouldWriteTypeOfFunctionSymbol() {
          var _a3;
          const isStaticMethodSymbol ***REMOVED*** !!(symbol.flags & 8192 /* Method */) && // typeof static method
          some(symbol.declarations, (declaration) ***REMOVED***> isStatic(declaration) && !isLateBindableIndexSignature(getNameOfDeclaration(declaration)));
          const isNonLocalFunctionSymbol ***REMOVED*** !!(symbol.flags & 16 /* Function */) && (symbol.parent || // is exported function symbol
          forEach(symbol.declarations, (declaration) ***REMOVED***> declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */));
          if (isStaticMethodSymbol || isNonLocalFunctionSymbol) {
            return (!!(context.flags & 4096 /* UseTypeOfFunction */) || ((_a3 ***REMOVED*** context.visitedTypes) ***REMOVED******REMOVED*** null ? void 0 : _a3.has(typeId))) && // it is type of the symbol uses itself recursively
            (!(context.flags & 8 /* UseStructuralFallback */) || isValueSymbolAccessible(symbol, context.enclosingDeclaration));
          }
        }
      }
      function visitAndTransformType(type2, transform) {
        var _a2, _b2, _c;
        const typeId ***REMOVED*** type2.id;
        const isConstructorObject ***REMOVED*** getObjectFlags(type2) & 16 /* Anonymous */ && type2.symbol && type2.symbol.flags & 32 /* Class */;
        const id ***REMOVED*** getObjectFlags(type2) & 4 /* Reference */ && type2.node ? "N" + getNodeId(type2.node) : type2.flags & 16777216 /* Conditional */ ? "N" + getNodeId(type2.root.node) : type2.symbol ? (isConstructorObject ? "+" : "") + getSymbolId(type2.symbol) : void 0;
        if (!context.visitedTypes) {
          context.visitedTypes ***REMOVED*** /* @__PURE__ */ new Set();
        }
        if (id && !context.symbolDepth) {
          context.symbolDepth ***REMOVED*** /* @__PURE__ */ new Map();
        }
        const links ***REMOVED*** context.maxExpansionDepth >***REMOVED*** 0 ? void 0 : context.enclosingDeclaration && getNodeLinks(context.enclosingDeclaration);
        const key ***REMOVED*** `${getTypeId(type2)}|${context.flags}|${context.internalFlags}`;
        if (links) {
          links.serializedTypes || (links.serializedTypes ***REMOVED*** /* @__PURE__ */ new Map());
        }
        const cachedResult ***REMOVED*** (_a2 ***REMOVED*** links ***REMOVED******REMOVED*** null ? void 0 : links.serializedTypes) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(key);
        if (cachedResult) {
          (_b2 ***REMOVED*** cachedResult.trackedSymbols) ***REMOVED******REMOVED*** null ? void 0 : _b2.forEach(
            ([symbol, enclosingDeclaration, meaning]) ***REMOVED***> context.tracker.trackSymbol(
              symbol,
              enclosingDeclaration,
              meaning
            )
          );
          if (cachedResult.truncating) {
            context.truncating ***REMOVED*** true;
          }
          context.approximateLength +***REMOVED*** cachedResult.addedLength;
          return deepCloneOrReuseNode(cachedResult.node);
        }
        let depth;
        if (id) {
          depth ***REMOVED*** context.symbolDepth.get(id) || 0;
          if (depth > 10) {
            return createElidedInformationPlaceholder(context);
          }
          context.symbolDepth.set(id, depth + 1);
        }
        context.visitedTypes.add(typeId);
        const prevTrackedSymbols ***REMOVED*** context.trackedSymbols;
        context.trackedSymbols ***REMOVED*** void 0;
        const startLength ***REMOVED*** context.approximateLength;
        const result ***REMOVED*** transform(type2);
        const addedLength ***REMOVED*** context.approximateLength - startLength;
        if (!context.reportedDiagnostic && !context.encounteredError) {
          (_c ***REMOVED*** links ***REMOVED******REMOVED*** null ? void 0 : links.serializedTypes) ***REMOVED******REMOVED*** null ? void 0 : _c.set(key, {
            node: result,
            truncating: context.truncating,
            addedLength,
            trackedSymbols: context.trackedSymbols
          });
        }
        context.visitedTypes.delete(typeId);
        if (id) {
          context.symbolDepth.set(id, depth);
        }
        context.trackedSymbols ***REMOVED*** prevTrackedSymbols;
        return result;
        function deepCloneOrReuseNode(node) {
          if (!nodeIsSynthesized(node) && getParseTreeNode(node) ***REMOVED******REMOVED******REMOVED*** node) {
            return node;
          }
          return setTextRange2(context, factory.cloneNode(visitEachChild(
            node,
            deepCloneOrReuseNode,
            /*context*/
            void 0,
            deepCloneOrReuseNodes,
            deepCloneOrReuseNode
          )), node);
        }
        function deepCloneOrReuseNodes(nodes, visitor, test, start, count) {
          if (nodes && nodes.length ***REMOVED******REMOVED******REMOVED*** 0) {
            return setTextRange(factory.createNodeArray(
              /*elements*/
              void 0,
              nodes.hasTrailingComma
            ), nodes);
          }
          return visitNodes2(nodes, visitor, test, start, count);
        }
      }
      function createTypeNodeFromObjectType(type2) {
        if (isGenericMappedType(type2) || type2.containsError) {
          return createMappedTypeNodeFromType(type2);
        }
        const resolved ***REMOVED*** resolveStructuredTypeMembers(type2);
        if (!resolved.properties.length && !resolved.indexInfos.length) {
          if (!resolved.callSignatures.length && !resolved.constructSignatures.length) {
            context.approximateLength +***REMOVED*** 2;
            return setEmitFlags(factory.createTypeLiteralNode(
              /*members*/
              void 0
            ), 1 /* SingleLine */);
          }
          if (resolved.callSignatures.length ***REMOVED******REMOVED******REMOVED*** 1 && !resolved.constructSignatures.length) {
            const signature ***REMOVED*** resolved.callSignatures[0];
            const signatureNode ***REMOVED*** signatureToSignatureDeclarationHelper(signature, 185 /* FunctionType */, context);
            return signatureNode;
          }
          if (resolved.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 1 && !resolved.callSignatures.length) {
            const signature ***REMOVED*** resolved.constructSignatures[0];
            const signatureNode ***REMOVED*** signatureToSignatureDeclarationHelper(signature, 186 /* ConstructorType */, context);
            return signatureNode;
          }
        }
        const abstractSignatures ***REMOVED*** filter(resolved.constructSignatures, (signature) ***REMOVED***> !!(signature.flags & 4 /* Abstract */));
        if (some(abstractSignatures)) {
          const types ***REMOVED*** map(abstractSignatures, getOrCreateTypeFromSignature);
          const typeElementCount ***REMOVED*** resolved.callSignatures.length + (resolved.constructSignatures.length - abstractSignatures.length) + resolved.indexInfos.length + // exclude `prototype` when writing a class expression as a type literal, as per
          // the logic in `createTypeNodesFromResolvedType`.
          (context.flags & 2048 /* WriteClassExpressionAsTypeLiteral */ ? countWhere(resolved.properties, (p) ***REMOVED***> !(p.flags & 4194304 /* Prototype */)) : length(resolved.properties));
          if (typeElementCount) {
            types.push(getResolvedTypeWithoutAbstractConstructSignatures(resolved));
          }
          return typeToTypeNodeHelper(getIntersectionType(types), context);
        }
        const restoreFlags ***REMOVED*** saveRestoreFlags(context);
        context.flags |***REMOVED*** 4194304 /* InObjectTypeLiteral */;
        const members ***REMOVED*** createTypeNodesFromResolvedType(resolved);
        restoreFlags();
        const typeLiteralNode ***REMOVED*** factory.createTypeLiteralNode(members);
        context.approximateLength +***REMOVED*** 2;
        setEmitFlags(typeLiteralNode, context.flags & 1024 /* MultilineObjectLiterals */ ? 0 : 1 /* SingleLine */);
        return typeLiteralNode;
      }
      function typeReferenceToTypeNode(type2) {
        let typeArguments ***REMOVED*** getTypeArguments(type2);
        if (type2.target ***REMOVED******REMOVED******REMOVED*** globalArrayType || type2.target ***REMOVED******REMOVED******REMOVED*** globalReadonlyArrayType) {
          if (context.flags & 2 /* WriteArrayAsGenericType */) {
            const typeArgumentNode ***REMOVED*** typeToTypeNodeHelper(typeArguments[0], context);
            return factory.createTypeReferenceNode(type2.target ***REMOVED******REMOVED******REMOVED*** globalArrayType ? "Array" : "ReadonlyArray", [typeArgumentNode]);
          }
          const elementType ***REMOVED*** typeToTypeNodeHelper(typeArguments[0], context);
          const arrayType ***REMOVED*** factory.createArrayTypeNode(elementType);
          return type2.target ***REMOVED******REMOVED******REMOVED*** globalArrayType ? arrayType : factory.createTypeOperatorNode(148 /* ReadonlyKeyword */, arrayType);
        } else if (type2.target.objectFlags & 8 /* Tuple */) {
          typeArguments ***REMOVED*** sameMap(typeArguments, (t, i) ***REMOVED***> removeMissingType(t, !!(type2.target.elementFlags[i] & 2 /* Optional */)));
          if (typeArguments.length > 0) {
            const arity ***REMOVED*** getTypeReferenceArity(type2);
            const tupleConstituentNodes ***REMOVED*** mapToTypeNodes(typeArguments.slice(0, arity), context);
            if (tupleConstituentNodes) {
              const { labeledElementDeclarations } ***REMOVED*** type2.target;
              for (let i ***REMOVED*** 0; i < tupleConstituentNodes.length; i++) {
                const flags ***REMOVED*** type2.target.elementFlags[i];
                const labeledElementDeclaration ***REMOVED*** labeledElementDeclarations ***REMOVED******REMOVED*** null ? void 0 : labeledElementDeclarations[i];
                if (labeledElementDeclaration) {
                  tupleConstituentNodes[i] ***REMOVED*** factory.createNamedTupleMember(
                    flags & 12 /* Variable */ ? factory.createToken(26 /* DotDotDotToken */) : void 0,
                    factory.createIdentifier(unescapeLeadingUnderscores(getTupleElementLabel(labeledElementDeclaration))),
                    flags & 2 /* Optional */ ? factory.createToken(58 /* QuestionToken */) : void 0,
                    flags & 4 /* Rest */ ? factory.createArrayTypeNode(tupleConstituentNodes[i]) : tupleConstituentNodes[i]
                  );
                } else {
                  tupleConstituentNodes[i] ***REMOVED*** flags & 12 /* Variable */ ? factory.createRestTypeNode(flags & 4 /* Rest */ ? factory.createArrayTypeNode(tupleConstituentNodes[i]) : tupleConstituentNodes[i]) : flags & 2 /* Optional */ ? factory.createOptionalTypeNode(tupleConstituentNodes[i]) : tupleConstituentNodes[i];
                }
              }
              const tupleTypeNode ***REMOVED*** setEmitFlags(factory.createTupleTypeNode(tupleConstituentNodes), 1 /* SingleLine */);
              return type2.target.readonly ? factory.createTypeOperatorNode(148 /* ReadonlyKeyword */, tupleTypeNode) : tupleTypeNode;
            }
          }
          if (context.encounteredError || context.flags & 524288 /* AllowEmptyTuple */) {
            const tupleTypeNode ***REMOVED*** setEmitFlags(factory.createTupleTypeNode([]), 1 /* SingleLine */);
            return type2.target.readonly ? factory.createTypeOperatorNode(148 /* ReadonlyKeyword */, tupleTypeNode) : tupleTypeNode;
          }
          context.encounteredError ***REMOVED*** true;
          return void 0;
        } else if (context.flags & 2048 /* WriteClassExpressionAsTypeLiteral */ && type2.symbol.valueDeclaration && isClassLike(type2.symbol.valueDeclaration) && !isValueSymbolAccessible(type2.symbol, context.enclosingDeclaration)) {
          return createAnonymousTypeNode(type2);
        } else {
          const outerTypeParameters ***REMOVED*** type2.target.outerTypeParameters;
          let i ***REMOVED*** 0;
          let resultType;
          if (outerTypeParameters) {
            const length2 ***REMOVED*** outerTypeParameters.length;
            while (i < length2) {
              const start ***REMOVED*** i;
              const parent ***REMOVED*** getParentSymbolOfTypeParameter(outerTypeParameters[i]);
              do {
                i++;
              } while (i < length2 && getParentSymbolOfTypeParameter(outerTypeParameters[i]) ***REMOVED******REMOVED******REMOVED*** parent);
              if (!rangeEquals(outerTypeParameters, typeArguments, start, i)) {
                const typeArgumentSlice ***REMOVED*** mapToTypeNodes(typeArguments.slice(start, i), context);
                const restoreFlags2 ***REMOVED*** saveRestoreFlags(context);
                context.flags |***REMOVED*** 16 /* ForbidIndexedAccessSymbolReferences */;
                const ref ***REMOVED*** symbolToTypeNode(parent, context, 788968 /* Type */, typeArgumentSlice);
                restoreFlags2();
                resultType ***REMOVED*** !resultType ? ref : appendReferenceToType(resultType, ref);
              }
            }
          }
          let typeArgumentNodes;
          if (typeArguments.length > 0) {
            let typeParameterCount ***REMOVED*** 0;
            if (type2.target.typeParameters) {
              typeParameterCount ***REMOVED*** Math.min(type2.target.typeParameters.length, typeArguments.length);
              if (isReferenceToType(type2, getGlobalIterableType(
                /*reportErrors*/
                false
              )) || isReferenceToType(type2, getGlobalIterableIteratorType(
                /*reportErrors*/
                false
              )) || isReferenceToType(type2, getGlobalAsyncIterableType(
                /*reportErrors*/
                false
              )) || isReferenceToType(type2, getGlobalAsyncIterableIteratorType(
                /*reportErrors*/
                false
              ))) {
                if (!type2.node || !isTypeReferenceNode(type2.node) || !type2.node.typeArguments || type2.node.typeArguments.length < typeParameterCount) {
                  while (typeParameterCount > 0) {
                    const typeArgument ***REMOVED*** typeArguments[typeParameterCount - 1];
                    const typeParameter ***REMOVED*** type2.target.typeParameters[typeParameterCount - 1];
                    const defaultType ***REMOVED*** getDefaultFromTypeParameter(typeParameter);
                    if (!defaultType || !isTypeIdenticalTo(typeArgument, defaultType)) {
                      break;
                    }
                    typeParameterCount--;
                  }
                }
              }
            }
            typeArgumentNodes ***REMOVED*** mapToTypeNodes(typeArguments.slice(i, typeParameterCount), context);
          }
          const restoreFlags ***REMOVED*** saveRestoreFlags(context);
          context.flags |***REMOVED*** 16 /* ForbidIndexedAccessSymbolReferences */;
          const finalRef ***REMOVED*** symbolToTypeNode(type2.symbol, context, 788968 /* Type */, typeArgumentNodes);
          restoreFlags();
          return !resultType ? finalRef : appendReferenceToType(resultType, finalRef);
        }
      }
      function appendReferenceToType(root, ref) {
        if (isImportTypeNode(root)) {
          let typeArguments ***REMOVED*** root.typeArguments;
          let qualifier ***REMOVED*** root.qualifier;
          if (qualifier) {
            if (isIdentifier(qualifier)) {
              if (typeArguments !***REMOVED******REMOVED*** getIdentifierTypeArguments(qualifier)) {
                qualifier ***REMOVED*** setIdentifierTypeArguments(factory.cloneNode(qualifier), typeArguments);
              }
            } else {
              if (typeArguments !***REMOVED******REMOVED*** getIdentifierTypeArguments(qualifier.right)) {
                qualifier ***REMOVED*** factory.updateQualifiedName(qualifier, qualifier.left, setIdentifierTypeArguments(factory.cloneNode(qualifier.right), typeArguments));
              }
            }
          }
          typeArguments ***REMOVED*** ref.typeArguments;
          const ids ***REMOVED*** getAccessStack(ref);
          for (const id of ids) {
            qualifier ***REMOVED*** qualifier ? factory.createQualifiedName(qualifier, id) : id;
          }
          return factory.updateImportTypeNode(
            root,
            root.argument,
            root.attributes,
            qualifier,
            typeArguments,
            root.isTypeOf
          );
        } else {
          let typeArguments ***REMOVED*** root.typeArguments;
          let typeName ***REMOVED*** root.typeName;
          if (isIdentifier(typeName)) {
            if (typeArguments !***REMOVED******REMOVED*** getIdentifierTypeArguments(typeName)) {
              typeName ***REMOVED*** setIdentifierTypeArguments(factory.cloneNode(typeName), typeArguments);
            }
          } else {
            if (typeArguments !***REMOVED******REMOVED*** getIdentifierTypeArguments(typeName.right)) {
              typeName ***REMOVED*** factory.updateQualifiedName(typeName, typeName.left, setIdentifierTypeArguments(factory.cloneNode(typeName.right), typeArguments));
            }
          }
          typeArguments ***REMOVED*** ref.typeArguments;
          const ids ***REMOVED*** getAccessStack(ref);
          for (const id of ids) {
            typeName ***REMOVED*** factory.createQualifiedName(typeName, id);
          }
          return factory.updateTypeReferenceNode(
            root,
            typeName,
            typeArguments
          );
        }
      }
      function getAccessStack(ref) {
        let state ***REMOVED*** ref.typeName;
        const ids ***REMOVED*** [];
        while (!isIdentifier(state)) {
          ids.unshift(state.right);
          state ***REMOVED*** state.left;
        }
        ids.unshift(state);
        return ids;
      }
      function indexInfoToObjectComputedNamesOrSignatureDeclaration(indexInfo, context2, typeNode) {
        if (indexInfo.components) {
          const allComponentComputedNamesSerializable ***REMOVED*** every(indexInfo.components, (e) ***REMOVED***> {
            var _a2;
            return !!(e.name && isComputedPropertyName(e.name) && isEntityNameExpression(e.name.expression) && context2.enclosingDeclaration && ((_a2 ***REMOVED*** isEntityNameVisible(
              e.name.expression,
              context2.enclosingDeclaration,
              /*shouldComputeAliasToMakeVisible*/
              false
            )) ***REMOVED******REMOVED*** null ? void 0 : _a2.accessibility) ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */);
          });
          if (allComponentComputedNamesSerializable) {
            const newComponents ***REMOVED*** filter(indexInfo.components, (e) ***REMOVED***> {
              return !hasLateBindableName(e);
            });
            return map(newComponents, (e) ***REMOVED***> {
              trackComputedName(e.name.expression, context2.enclosingDeclaration, context2);
              return setTextRange2(
                context2,
                factory.createPropertySignature(
                  indexInfo.isReadonly ? [factory.createModifier(148 /* ReadonlyKeyword */)] : void 0,
                  e.name,
                  (isPropertySignature(e) || isPropertyDeclaration(e) || isMethodSignature(e) || isMethodDeclaration(e) || isGetAccessor(e) || isSetAccessor(e)) && e.questionToken ? factory.createToken(58 /* QuestionToken */) : void 0,
                  typeNode || typeToTypeNodeHelper(getTypeOfSymbol(e.symbol), context2)
                ),
                e
              );
            });
          }
        }
        return [indexInfoToIndexSignatureDeclarationHelper(indexInfo, context2, typeNode)];
      }
      function createTypeNodesFromResolvedType(resolvedType) {
        if (checkTruncationLength(context)) {
          context.out.truncated ***REMOVED*** true;
          if (context.flags & 1 /* NoTruncation */) {
            return [addSyntheticTrailingComment(factory.createNotEmittedTypeElement(), 3 /* MultiLineCommentTrivia */, "elided")];
          }
          return [factory.createPropertySignature(
            /*modifiers*/
            void 0,
            "...",
            /*questionToken*/
            void 0,
            /*type*/
            void 0
          )];
        }
        context.typeStack.push(-1);
        const typeElements ***REMOVED*** [];
        for (const signature of resolvedType.callSignatures) {
          typeElements.push(signatureToSignatureDeclarationHelper(signature, 180 /* CallSignature */, context));
        }
        for (const signature of resolvedType.constructSignatures) {
          if (signature.flags & 4 /* Abstract */) continue;
          typeElements.push(signatureToSignatureDeclarationHelper(signature, 181 /* ConstructSignature */, context));
        }
        for (const info of resolvedType.indexInfos) {
          typeElements.push(...indexInfoToObjectComputedNamesOrSignatureDeclaration(info, context, resolvedType.objectFlags & 1024 /* ReverseMapped */ ? createElidedInformationPlaceholder(context) : void 0));
        }
        const properties ***REMOVED*** resolvedType.properties;
        if (!properties) {
          context.typeStack.pop();
          return typeElements;
        }
        let i ***REMOVED*** 0;
        for (const propertySymbol of properties) {
          if (isExpanding(context) && propertySymbol.flags & 4194304 /* Prototype */) {
            continue;
          }
          i++;
          if (context.flags & 2048 /* WriteClassExpressionAsTypeLiteral */) {
            if (propertySymbol.flags & 4194304 /* Prototype */) {
              continue;
            }
            if (getDeclarationModifierFlagsFromSymbol(propertySymbol) & (2 /* Private */ | 4 /* Protected */) && context.tracker.reportPrivateInBaseOfClassExpression) {
              context.tracker.reportPrivateInBaseOfClassExpression(unescapeLeadingUnderscores(propertySymbol.escapedName));
            }
          }
          if (checkTruncationLength(context) && i + 2 < properties.length - 1) {
            context.out.truncated ***REMOVED*** true;
            if (context.flags & 1 /* NoTruncation */) {
              const typeElement ***REMOVED*** typeElements.pop();
              typeElements.push(addSyntheticTrailingComment(typeElement, 3 /* MultiLineCommentTrivia */, `... ${properties.length - i} more elided ...`));
            } else {
              typeElements.push(factory.createPropertySignature(
                /*modifiers*/
                void 0,
                `... ${properties.length - i} more ...`,
                /*questionToken*/
                void 0,
                /*type*/
                void 0
              ));
            }
            addPropertyToElementList(properties[properties.length - 1], context, typeElements);
            break;
          }
          addPropertyToElementList(propertySymbol, context, typeElements);
        }
        context.typeStack.pop();
        return typeElements.length ? typeElements : void 0;
      }
    }
    function createElidedInformationPlaceholder(context) {
      context.approximateLength +***REMOVED*** 3;
      if (!(context.flags & 1 /* NoTruncation */)) {
        return factory.createTypeReferenceNode(
          factory.createIdentifier("..."),
          /*typeArguments*/
          void 0
        );
      }
      return addSyntheticLeadingComment(factory.createKeywordTypeNode(133 /* AnyKeyword */), 3 /* MultiLineCommentTrivia */, "elided");
    }
    function shouldUsePlaceholderForProperty(propertySymbol, context) {
      var _a;
      const depth ***REMOVED*** 3;
      return !!(getCheckFlags(propertySymbol) & 8192 /* ReverseMapped */) && (contains(context.reverseMappedStack, propertySymbol) || ((_a ***REMOVED*** context.reverseMappedStack) ***REMOVED******REMOVED*** null ? void 0 : _a[0]) && !(getObjectFlags(last(context.reverseMappedStack).links.propertyType) & 16 /* Anonymous */) || isDeeplyNestedReverseMappedTypeProperty());
      function isDeeplyNestedReverseMappedTypeProperty() {
        var _a2;
        if ((((_a2 ***REMOVED*** context.reverseMappedStack) ***REMOVED******REMOVED*** null ? void 0 : _a2.length) ?? 0) < depth) {
          return false;
        }
        for (let i ***REMOVED*** 0; i < depth; i++) {
          const prop ***REMOVED*** context.reverseMappedStack[context.reverseMappedStack.length - 1 - i];
          if (prop.links.mappedType.symbol !***REMOVED******REMOVED*** propertySymbol.links.mappedType.symbol) {
            return false;
          }
        }
        return true;
      }
    }
    function addPropertyToElementList(propertySymbol, context, typeElements) {
      var _a;
      const propertyIsReverseMapped ***REMOVED*** !!(getCheckFlags(propertySymbol) & 8192 /* ReverseMapped */);
      const propertyType ***REMOVED*** shouldUsePlaceholderForProperty(propertySymbol, context) ? anyType : getNonMissingTypeOfSymbol(propertySymbol);
      const saveEnclosingDeclaration ***REMOVED*** context.enclosingDeclaration;
      context.enclosingDeclaration ***REMOVED*** void 0;
      if (context.tracker.canTrackSymbol && isLateBoundName(propertySymbol.escapedName)) {
        if (propertySymbol.declarations) {
          const decl ***REMOVED*** first(propertySymbol.declarations);
          if (hasLateBindableName(decl)) {
            if (isBinaryExpression(decl)) {
              const name ***REMOVED*** getNameOfDeclaration(decl);
              if (name && isElementAccessExpression(name) && isPropertyAccessEntityNameExpression(name.argumentExpression)) {
                trackComputedName(name.argumentExpression, saveEnclosingDeclaration, context);
              }
            } else {
              trackComputedName(decl.name.expression, saveEnclosingDeclaration, context);
            }
          }
        } else {
          context.tracker.reportNonSerializableProperty(symbolToString(propertySymbol));
        }
      }
      context.enclosingDeclaration ***REMOVED*** propertySymbol.valueDeclaration || ((_a ***REMOVED*** propertySymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0]) || saveEnclosingDeclaration;
      const propertyName ***REMOVED*** getPropertyNameNodeForSymbol(propertySymbol, context);
      context.enclosingDeclaration ***REMOVED*** saveEnclosingDeclaration;
      context.approximateLength +***REMOVED*** symbolName(propertySymbol).length + 1;
      if (propertySymbol.flags & 98304 /* Accessor */) {
        const writeType ***REMOVED*** getWriteTypeOfSymbol(propertySymbol);
        if (!isErrorType(propertyType) && !isErrorType(writeType)) {
          const symbolMapper ***REMOVED*** getSymbolLinks(propertySymbol).mapper;
          const propDeclaration ***REMOVED*** getDeclarationOfKind(propertySymbol, 173 /* PropertyDeclaration */);
          if (propertyType !***REMOVED******REMOVED*** writeType || propertySymbol.parent.flags & 32 /* Class */ && !propDeclaration) {
            const getterDeclaration ***REMOVED*** getDeclarationOfKind(propertySymbol, 178 /* GetAccessor */);
            if (getterDeclaration) {
              const getterSignature ***REMOVED*** getSignatureFromDeclaration(getterDeclaration);
              typeElements.push(
                setCommentRange2(
                  context,
                  signatureToSignatureDeclarationHelper(symbolMapper ? instantiateSignature(getterSignature, symbolMapper) : getterSignature, 178 /* GetAccessor */, context, { name: propertyName }),
                  getterDeclaration
                )
              );
            }
            const setterDeclaration ***REMOVED*** getDeclarationOfKind(propertySymbol, 179 /* SetAccessor */);
            if (setterDeclaration) {
              const setterSignature ***REMOVED*** getSignatureFromDeclaration(setterDeclaration);
              typeElements.push(
                setCommentRange2(
                  context,
                  signatureToSignatureDeclarationHelper(symbolMapper ? instantiateSignature(setterSignature, symbolMapper) : setterSignature, 179 /* SetAccessor */, context, { name: propertyName }),
                  setterDeclaration
                )
              );
            }
            return;
          }
          if (propertySymbol.parent.flags & 32 /* Class */ && propDeclaration && find(propDeclaration.modifiers, isAccessorModifier)) {
            const fakeGetterSignature ***REMOVED*** createSignature(
              /*declaration*/
              void 0,
              /*typeParameters*/
              void 0,
              /*thisParameter*/
              void 0,
              emptyArray,
              propertyType,
              /*resolvedTypePredicate*/
              void 0,
              0,
              0 /* None */
            );
            typeElements.push(
              setCommentRange2(
                context,
                signatureToSignatureDeclarationHelper(fakeGetterSignature, 178 /* GetAccessor */, context, { name: propertyName }),
                propDeclaration
              )
            );
            const setterParam ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, "arg");
            setterParam.links.type ***REMOVED*** writeType;
            const fakeSetterSignature ***REMOVED*** createSignature(
              /*declaration*/
              void 0,
              /*typeParameters*/
              void 0,
              /*thisParameter*/
              void 0,
              [setterParam],
              voidType,
              /*resolvedTypePredicate*/
              void 0,
              0,
              0 /* None */
            );
            typeElements.push(
              signatureToSignatureDeclarationHelper(fakeSetterSignature, 179 /* SetAccessor */, context, { name: propertyName })
            );
            return;
          }
        }
      }
      const optionalToken ***REMOVED*** propertySymbol.flags & 16777216 /* Optional */ ? factory.createToken(58 /* QuestionToken */) : void 0;
      if (propertySymbol.flags & (16 /* Function */ | 8192 /* Method */) && !getPropertiesOfObjectType(propertyType).length && !isReadonlySymbol(propertySymbol)) {
        const signatures ***REMOVED*** getSignaturesOfType(filterType(propertyType, (t) ***REMOVED***> !(t.flags & 32768 /* Undefined */)), 0 /* Call */);
        for (const signature of signatures) {
          const methodDeclaration ***REMOVED*** signatureToSignatureDeclarationHelper(signature, 174 /* MethodSignature */, context, { name: propertyName, questionToken: optionalToken });
          typeElements.push(preserveCommentsOn(methodDeclaration, signature.declaration || propertySymbol.valueDeclaration));
        }
        if (signatures.length || !optionalToken) {
          return;
        }
      }
      let propertyTypeNode;
      if (shouldUsePlaceholderForProperty(propertySymbol, context)) {
        propertyTypeNode ***REMOVED*** createElidedInformationPlaceholder(context);
      } else {
        if (propertyIsReverseMapped) {
          context.reverseMappedStack || (context.reverseMappedStack ***REMOVED*** []);
          context.reverseMappedStack.push(propertySymbol);
        }
        propertyTypeNode ***REMOVED*** propertyType ? serializeTypeForDeclaration(
          context,
          /*declaration*/
          void 0,
          propertyType,
          propertySymbol
        ) : factory.createKeywordTypeNode(133 /* AnyKeyword */);
        if (propertyIsReverseMapped) {
          context.reverseMappedStack.pop();
        }
      }
      const modifiers ***REMOVED*** isReadonlySymbol(propertySymbol) ? [factory.createToken(148 /* ReadonlyKeyword */)] : void 0;
      if (modifiers) {
        context.approximateLength +***REMOVED*** 9;
      }
      const propertySignature ***REMOVED*** factory.createPropertySignature(
        modifiers,
        propertyName,
        optionalToken,
        propertyTypeNode
      );
      typeElements.push(preserveCommentsOn(propertySignature, propertySymbol.valueDeclaration));
      function preserveCommentsOn(node, range) {
        var _a2;
        const jsdocPropertyTag ***REMOVED*** (_a2 ***REMOVED*** propertySymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a2.find((d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** 349 /* JSDocPropertyTag */);
        if (jsdocPropertyTag) {
          const commentText ***REMOVED*** getTextOfJSDocComment(jsdocPropertyTag.comment);
          if (commentText) {
            setSyntheticLeadingComments(node, [{ kind: 3 /* MultiLineCommentTrivia */, text: "*\n * " + commentText.replace(/\n/g, "\n * ") + "\n ", pos: -1, end: -1, hasTrailingNewLine: true }]);
          }
        } else if (range) {
          setCommentRange2(context, node, range);
        }
        return node;
      }
    }
    function setCommentRange2(context, node, range) {
      if (context.enclosingFile && context.enclosingFile ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(range)) {
        return setCommentRange(node, range);
      }
      return node;
    }
    function mapToTypeNodes(types, context, isBareList) {
      if (some(types)) {
        if (checkTruncationLength(context)) {
          context.out.truncated ***REMOVED*** true;
          if (!isBareList) {
            return [
              context.flags & 1 /* NoTruncation */ ? addSyntheticLeadingComment(factory.createKeywordTypeNode(133 /* AnyKeyword */), 3 /* MultiLineCommentTrivia */, "elided") : factory.createTypeReferenceNode(
                "...",
                /*typeArguments*/
                void 0
              )
            ];
          } else if (types.length > 2) {
            return [
              typeToTypeNodeHelper(types[0], context),
              context.flags & 1 /* NoTruncation */ ? addSyntheticLeadingComment(factory.createKeywordTypeNode(133 /* AnyKeyword */), 3 /* MultiLineCommentTrivia */, `... ${types.length - 2} more elided ...`) : factory.createTypeReferenceNode(
                `... ${types.length - 2} more ...`,
                /*typeArguments*/
                void 0
              ),
              typeToTypeNodeHelper(types[types.length - 1], context)
            ];
          }
        }
        const mayHaveNameCollisions ***REMOVED*** !(context.flags & 64 /* UseFullyQualifiedType */);
        const seenNames ***REMOVED*** mayHaveNameCollisions ? createMultiMap() : void 0;
        const result ***REMOVED*** [];
        let i ***REMOVED*** 0;
        for (const type of types) {
          i++;
          if (checkTruncationLength(context) && i + 2 < types.length - 1) {
            context.out.truncated ***REMOVED*** true;
            result.push(
              context.flags & 1 /* NoTruncation */ ? addSyntheticLeadingComment(factory.createKeywordTypeNode(133 /* AnyKeyword */), 3 /* MultiLineCommentTrivia */, `... ${types.length - i} more elided ...`) : factory.createTypeReferenceNode(
                `... ${types.length - i} more ...`,
                /*typeArguments*/
                void 0
              )
            );
            const typeNode2 ***REMOVED*** typeToTypeNodeHelper(types[types.length - 1], context);
            if (typeNode2) {
              result.push(typeNode2);
            }
            break;
          }
          context.approximateLength +***REMOVED*** 2;
          const typeNode ***REMOVED*** typeToTypeNodeHelper(type, context);
          if (typeNode) {
            result.push(typeNode);
            if (seenNames && isIdentifierTypeReference(typeNode)) {
              seenNames.add(typeNode.typeName.escapedText, [type, result.length - 1]);
            }
          }
        }
        if (seenNames) {
          const restoreFlags ***REMOVED*** saveRestoreFlags(context);
          context.flags |***REMOVED*** 64 /* UseFullyQualifiedType */;
          seenNames.forEach((types2) ***REMOVED***> {
            if (!arrayIsHomogeneous(types2, ([a], [b]) ***REMOVED***> typesAreSameReference(a, b))) {
              for (const [type, resultIndex] of types2) {
                result[resultIndex] ***REMOVED*** typeToTypeNodeHelper(type, context);
              }
            }
          });
          restoreFlags();
        }
        return result;
      }
    }
    function typesAreSameReference(a, b) {
      return a ***REMOVED******REMOVED******REMOVED*** b || !!a.symbol && a.symbol ***REMOVED******REMOVED******REMOVED*** b.symbol || !!a.aliasSymbol && a.aliasSymbol ***REMOVED******REMOVED******REMOVED*** b.aliasSymbol;
    }
    function indexInfoToIndexSignatureDeclarationHelper(indexInfo, context, typeNode) {
      const name ***REMOVED*** getNameFromIndexInfo(indexInfo) || "x";
      const indexerTypeNode ***REMOVED*** typeToTypeNodeHelper(indexInfo.keyType, context);
      const indexingParameter ***REMOVED*** factory.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        name,
        /*questionToken*/
        void 0,
        indexerTypeNode,
        /*initializer*/
        void 0
      );
      if (!typeNode) {
        typeNode ***REMOVED*** typeToTypeNodeHelper(indexInfo.type || anyType, context);
      }
      if (!indexInfo.type && !(context.flags & 2097152 /* AllowEmptyIndexInfoType */)) {
        context.encounteredError ***REMOVED*** true;
      }
      context.approximateLength +***REMOVED*** name.length + 4;
      return factory.createIndexSignature(
        indexInfo.isReadonly ? [factory.createToken(148 /* ReadonlyKeyword */)] : void 0,
        [indexingParameter],
        typeNode
      );
    }
    function signatureToSignatureDeclarationHelper(signature, kind, context, options) {
      var _a;
      let typeParameters;
      let typeArguments;
      const expandedParams ***REMOVED*** getExpandedParameters(
        signature,
        /*skipUnionExpanding*/
        true
      )[0];
      const cleanup ***REMOVED*** enterNewScope(context, signature.declaration, expandedParams, signature.typeParameters, signature.parameters, signature.mapper);
      context.approximateLength +***REMOVED*** 3;
      if (context.flags & 32 /* WriteTypeArgumentsOfSignature */ && signature.target && signature.mapper && signature.target.typeParameters) {
        typeArguments ***REMOVED*** signature.target.typeParameters.map((parameter) ***REMOVED***> typeToTypeNodeHelper(instantiateType(parameter, signature.mapper), context));
      } else {
        typeParameters ***REMOVED*** signature.typeParameters && signature.typeParameters.map((parameter) ***REMOVED***> typeParameterToDeclaration(parameter, context));
      }
      const restoreFlags ***REMOVED*** saveRestoreFlags(context);
      context.flags &***REMOVED*** ~256 /* SuppressAnyReturnType */;
      const parameters ***REMOVED*** (some(expandedParams, (p) ***REMOVED***> p !***REMOVED******REMOVED*** expandedParams[expandedParams.length - 1] && !!(getCheckFlags(p) & 32768 /* RestParameter */)) ? signature.parameters : expandedParams).map((parameter) ***REMOVED***> symbolToParameterDeclaration(parameter, context, kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */));
      const thisParameter ***REMOVED*** context.flags & 33554432 /* OmitThisParameter */ ? void 0 : tryGetThisParameterDeclaration(signature, context);
      if (thisParameter) {
        parameters.unshift(thisParameter);
      }
      restoreFlags();
      const returnTypeNode ***REMOVED*** serializeReturnTypeForSignature(context, signature);
      let modifiers ***REMOVED*** options ***REMOVED******REMOVED*** null ? void 0 : options.modifiers;
      if (kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */ && signature.flags & 4 /* Abstract */) {
        const flags ***REMOVED*** modifiersToFlags(modifiers);
        modifiers ***REMOVED*** factory.createModifiersFromModifierFlags(flags | 64 /* Abstract */);
      }
      const node ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 180 /* CallSignature */ ? factory.createCallSignature(typeParameters, parameters, returnTypeNode) : kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */ ? factory.createConstructSignature(typeParameters, parameters, returnTypeNode) : kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ ? factory.createMethodSignature(modifiers, (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ?? factory.createIdentifier(""), options ***REMOVED******REMOVED*** null ? void 0 : options.questionToken, typeParameters, parameters, returnTypeNode) : kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ ? factory.createMethodDeclaration(
        modifiers,
        /*asteriskToken*/
        void 0,
        (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ?? factory.createIdentifier(""),
        /*questionToken*/
        void 0,
        typeParameters,
        parameters,
        returnTypeNode,
        /*body*/
        void 0
      ) : kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ ? factory.createConstructorDeclaration(
        modifiers,
        parameters,
        /*body*/
        void 0
      ) : kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? factory.createGetAccessorDeclaration(
        modifiers,
        (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ?? factory.createIdentifier(""),
        parameters,
        returnTypeNode,
        /*body*/
        void 0
      ) : kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ ? factory.createSetAccessorDeclaration(
        modifiers,
        (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ?? factory.createIdentifier(""),
        parameters,
        /*body*/
        void 0
      ) : kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ ? factory.createIndexSignature(modifiers, parameters, returnTypeNode) : kind ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */ ? factory.createJSDocFunctionType(parameters, returnTypeNode) : kind ***REMOVED******REMOVED******REMOVED*** 185 /* FunctionType */ ? factory.createFunctionTypeNode(typeParameters, parameters, returnTypeNode ?? factory.createTypeReferenceNode(factory.createIdentifier(""))) : kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */ ? factory.createConstructorTypeNode(modifiers, typeParameters, parameters, returnTypeNode ?? factory.createTypeReferenceNode(factory.createIdentifier(""))) : kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ ? factory.createFunctionDeclaration(
        modifiers,
        /*asteriskToken*/
        void 0,
        (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ? cast(options.name, isIdentifier) : factory.createIdentifier(""),
        typeParameters,
        parameters,
        returnTypeNode,
        /*body*/
        void 0
      ) : kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ ? factory.createFunctionExpression(
        modifiers,
        /*asteriskToken*/
        void 0,
        (options ***REMOVED******REMOVED*** null ? void 0 : options.name) ? cast(options.name, isIdentifier) : factory.createIdentifier(""),
        typeParameters,
        parameters,
        returnTypeNode,
        factory.createBlock([])
      ) : kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ ? factory.createArrowFunction(
        modifiers,
        typeParameters,
        parameters,
        returnTypeNode,
        /*equalsGreaterThanToken*/
        void 0,
        factory.createBlock([])
      ) : Debug.assertNever(kind);
      if (typeArguments) {
        node.typeArguments ***REMOVED*** factory.createNodeArray(typeArguments);
      }
      if (((_a ***REMOVED*** signature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 324 /* JSDocSignature */ && signature.declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 340 /* JSDocOverloadTag */) {
        const comment ***REMOVED*** getTextOfNode(
          signature.declaration.parent.parent,
          /*includeTrivia*/
          true
        ).slice(2, -2).split(/\r\n|\n|\r/).map((line) ***REMOVED***> line.replace(/^\s+/, " ")).join("\n");
        addSyntheticLeadingComment(
          node,
          3 /* MultiLineCommentTrivia */,
          comment,
          /*hasTrailingNewLine*/
          true
        );
      }
      cleanup ***REMOVED******REMOVED*** null ? void 0 : cleanup();
      return node;
    }
    function createRecoveryBoundary(context) {
      if (cancellationToken && cancellationToken.throwIfCancellationRequested) {
        cancellationToken.throwIfCancellationRequested();
      }
      let trackedSymbols;
      let unreportedErrors;
      let hadError ***REMOVED*** false;
      const oldTracker ***REMOVED*** context.tracker;
      const oldTrackedSymbols ***REMOVED*** context.trackedSymbols;
      context.trackedSymbols ***REMOVED*** void 0;
      const oldEncounteredError ***REMOVED*** context.encounteredError;
      context.tracker ***REMOVED*** new SymbolTrackerImpl(context, {
        ...oldTracker.inner,
        reportCyclicStructureError() {
          markError(() ***REMOVED***> oldTracker.reportCyclicStructureError());
        },
        reportInaccessibleThisError() {
          markError(() ***REMOVED***> oldTracker.reportInaccessibleThisError());
        },
        reportInaccessibleUniqueSymbolError() {
          markError(() ***REMOVED***> oldTracker.reportInaccessibleUniqueSymbolError());
        },
        reportLikelyUnsafeImportRequiredError(specifier) {
          markError(() ***REMOVED***> oldTracker.reportLikelyUnsafeImportRequiredError(specifier));
        },
        reportNonSerializableProperty(name) {
          markError(() ***REMOVED***> oldTracker.reportNonSerializableProperty(name));
        },
        reportPrivateInBaseOfClassExpression(propertyName) {
          markError(() ***REMOVED***> oldTracker.reportPrivateInBaseOfClassExpression(propertyName));
        },
        trackSymbol(sym, decl, meaning) {
          (trackedSymbols ?? (trackedSymbols ***REMOVED*** [])).push([sym, decl, meaning]);
          return false;
        },
        moduleResolverHost: context.tracker.moduleResolverHost
      }, context.tracker.moduleResolverHost);
      return {
        startRecoveryScope,
        finalizeBoundary,
        markError,
        hadError: () ***REMOVED***> hadError
      };
      function markError(unreportedError) {
        hadError ***REMOVED*** true;
        if (unreportedError) {
          (unreportedErrors ?? (unreportedErrors ***REMOVED*** [])).push(unreportedError);
        }
      }
      function startRecoveryScope() {
        const trackedSymbolsTop ***REMOVED*** (trackedSymbols ***REMOVED******REMOVED*** null ? void 0 : trackedSymbols.length) ?? 0;
        const unreportedErrorsTop ***REMOVED*** (unreportedErrors ***REMOVED******REMOVED*** null ? void 0 : unreportedErrors.length) ?? 0;
        return () ***REMOVED***> {
          hadError ***REMOVED*** false;
          if (trackedSymbols) {
            trackedSymbols.length ***REMOVED*** trackedSymbolsTop;
          }
          if (unreportedErrors) {
            unreportedErrors.length ***REMOVED*** unreportedErrorsTop;
          }
        };
      }
      function finalizeBoundary() {
        context.tracker ***REMOVED*** oldTracker;
        context.trackedSymbols ***REMOVED*** oldTrackedSymbols;
        context.encounteredError ***REMOVED*** oldEncounteredError;
        unreportedErrors ***REMOVED******REMOVED*** null ? void 0 : unreportedErrors.forEach((fn) ***REMOVED***> fn());
        if (hadError) {
          return false;
        }
        trackedSymbols ***REMOVED******REMOVED*** null ? void 0 : trackedSymbols.forEach(
          ([symbol, enclosingDeclaration, meaning]) ***REMOVED***> context.tracker.trackSymbol(
            symbol,
            enclosingDeclaration,
            meaning
          )
        );
        return true;
      }
    }
    function enterNewScope(context, declaration, expandedParams, typeParameters, originalParameters, mapper) {
      const cleanupContext ***REMOVED*** cloneNodeBuilderContext(context);
      let cleanupParams;
      let cleanupTypeParams;
      const oldEnclosingDecl ***REMOVED*** context.enclosingDeclaration;
      const oldMapper ***REMOVED*** context.mapper;
      if (mapper) {
        context.mapper ***REMOVED*** mapper;
      }
      if (context.enclosingDeclaration && declaration) {
        let pushFakeScope2 ***REMOVED*** function(kind, addAll) {
          Debug.assert(context.enclosingDeclaration);
          let existingFakeScope;
          if (getNodeLinks(context.enclosingDeclaration).fakeScopeForSignatureDeclaration ***REMOVED******REMOVED******REMOVED*** kind) {
            existingFakeScope ***REMOVED*** context.enclosingDeclaration;
          } else if (context.enclosingDeclaration.parent && getNodeLinks(context.enclosingDeclaration.parent).fakeScopeForSignatureDeclaration ***REMOVED******REMOVED******REMOVED*** kind) {
            existingFakeScope ***REMOVED*** context.enclosingDeclaration.parent;
          }
          Debug.assertOptionalNode(existingFakeScope, isBlock);
          const locals ***REMOVED*** (existingFakeScope ***REMOVED******REMOVED*** null ? void 0 : existingFakeScope.locals) ?? createSymbolTable();
          let newLocals;
          let oldLocals;
          addAll((name, symbol) ***REMOVED***> {
            if (existingFakeScope) {
              const oldSymbol ***REMOVED*** locals.get(name);
              if (!oldSymbol) {
                newLocals ***REMOVED*** append(newLocals, name);
              } else {
                oldLocals ***REMOVED*** append(oldLocals, { name, oldSymbol });
              }
            }
            locals.set(name, symbol);
          });
          if (!existingFakeScope) {
            const fakeScope ***REMOVED*** factory.createBlock(emptyArray);
            getNodeLinks(fakeScope).fakeScopeForSignatureDeclaration ***REMOVED*** kind;
            fakeScope.locals ***REMOVED*** locals;
            setParent(fakeScope, context.enclosingDeclaration);
            context.enclosingDeclaration ***REMOVED*** fakeScope;
          } else {
            return function undo() {
              forEach(newLocals, (s) ***REMOVED***> locals.delete(s));
              forEach(oldLocals, (s) ***REMOVED***> locals.set(s.name, s.oldSymbol));
            };
          }
        };
        var pushFakeScope ***REMOVED*** pushFakeScope2;
        cleanupParams ***REMOVED*** !some(expandedParams) ? void 0 : pushFakeScope2(
          "params",
          (add) ***REMOVED***> {
            if (!expandedParams) return;
            for (let pIndex ***REMOVED*** 0; pIndex < expandedParams.length; pIndex++) {
              const param ***REMOVED*** expandedParams[pIndex];
              const originalParam ***REMOVED*** originalParameters ***REMOVED******REMOVED*** null ? void 0 : originalParameters[pIndex];
              if (originalParameters && originalParam !***REMOVED******REMOVED*** param) {
                add(param.escapedName, unknownSymbol);
                if (originalParam) {
                  add(originalParam.escapedName, unknownSymbol);
                }
              } else if (!forEach(param.declarations, (d) ***REMOVED***> {
                if (isParameter(d) && isBindingPattern(d.name)) {
                  bindPattern(d.name);
                  return true;
                }
                return void 0;
                function bindPattern(p) {
                  forEach(p.elements, (e) ***REMOVED***> {
                    switch (e.kind) {
                      case 233 /* OmittedExpression */:
                        return;
                      case 209 /* BindingElement */:
                        return bindElement(e);
                      default:
                        return Debug.assertNever(e);
                    }
                  });
                }
                function bindElement(e) {
                  if (isBindingPattern(e.name)) {
                    return bindPattern(e.name);
                  }
                  const symbol ***REMOVED*** getSymbolOfDeclaration(e);
                  add(symbol.escapedName, symbol);
                }
              })) {
                add(param.escapedName, param);
              }
            }
          }
        );
        if (context.flags & 4 /* GenerateNamesForShadowedTypeParams */ && some(typeParameters)) {
          cleanupTypeParams ***REMOVED*** pushFakeScope2(
            "typeParams",
            (add) ***REMOVED***> {
              for (const typeParam of typeParameters ?? emptyArray) {
                const typeParamName ***REMOVED*** typeParameterToName(typeParam, context).escapedText;
                add(typeParamName, typeParam.symbol);
              }
            }
          );
        }
      }
      return () ***REMOVED***> {
        cleanupParams ***REMOVED******REMOVED*** null ? void 0 : cleanupParams();
        cleanupTypeParams ***REMOVED******REMOVED*** null ? void 0 : cleanupTypeParams();
        cleanupContext();
        context.enclosingDeclaration ***REMOVED*** oldEnclosingDecl;
        context.mapper ***REMOVED*** oldMapper;
      };
    }
    function tryGetThisParameterDeclaration(signature, context) {
      if (signature.thisParameter) {
        return symbolToParameterDeclaration(signature.thisParameter, context);
      }
      if (signature.declaration && isInJSFile(signature.declaration)) {
        const thisTag ***REMOVED*** getJSDocThisTag(signature.declaration);
        if (thisTag && thisTag.typeExpression) {
          return factory.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            "this",
            /*questionToken*/
            void 0,
            typeToTypeNodeHelper(getTypeFromTypeNode2(context, thisTag.typeExpression), context)
          );
        }
      }
    }
    function typeParameterToDeclarationWithConstraint(type, context, constraintNode) {
      const restoreFlags ***REMOVED*** saveRestoreFlags(context);
      context.flags &***REMOVED*** ~512 /* WriteTypeParametersInQualifiedName */;
      const modifiers ***REMOVED*** factory.createModifiersFromModifierFlags(getTypeParameterModifiers(type));
      const name ***REMOVED*** typeParameterToName(type, context);
      const defaultParameter ***REMOVED*** getDefaultFromTypeParameter(type);
      const defaultParameterNode ***REMOVED*** defaultParameter && typeToTypeNodeHelper(defaultParameter, context);
      restoreFlags();
      return factory.createTypeParameterDeclaration(modifiers, name, constraintNode, defaultParameterNode);
    }
    function typeToTypeNodeHelperWithPossibleReusableTypeNode(type, typeNode, context) {
      return !canPossiblyExpandType(type, context) && typeNode && getTypeFromTypeNode2(context, typeNode) ***REMOVED******REMOVED******REMOVED*** type && syntacticNodeBuilder.tryReuseExistingTypeNode(context, typeNode) || typeToTypeNodeHelper(type, context);
    }
    function typeParameterToDeclaration(type, context, constraint ***REMOVED*** getConstraintOfTypeParameter(type)) {
      const constraintNode ***REMOVED*** constraint && typeToTypeNodeHelperWithPossibleReusableTypeNode(constraint, getConstraintDeclaration(type), context);
      return typeParameterToDeclarationWithConstraint(type, context, constraintNode);
    }
    function typePredicateToTypePredicateNodeHelper(typePredicate, context) {
      const assertsModifier ***REMOVED*** typePredicate.kind ***REMOVED******REMOVED******REMOVED*** 2 /* AssertsThis */ || typePredicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */ ? factory.createToken(131 /* AssertsKeyword */) : void 0;
      const parameterName ***REMOVED*** typePredicate.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */ || typePredicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */ ? setEmitFlags(factory.createIdentifier(typePredicate.parameterName), 16777216 /* NoAsciiEscaping */) : factory.createThisTypeNode();
      const typeNode ***REMOVED*** typePredicate.type && typeToTypeNodeHelper(typePredicate.type, context);
      return factory.createTypePredicateNode(assertsModifier, parameterName, typeNode);
    }
    function getEffectiveParameterDeclaration(parameterSymbol) {
      const parameterDeclaration ***REMOVED*** getDeclarationOfKind(parameterSymbol, 170 /* Parameter */);
      if (parameterDeclaration) {
        return parameterDeclaration;
      }
      if (!isTransientSymbol(parameterSymbol)) {
        return getDeclarationOfKind(parameterSymbol, 342 /* JSDocParameterTag */);
      }
    }
    function symbolToParameterDeclaration(parameterSymbol, context, preserveModifierFlags) {
      const parameterDeclaration ***REMOVED*** getEffectiveParameterDeclaration(parameterSymbol);
      const parameterType ***REMOVED*** getTypeOfSymbol(parameterSymbol);
      const parameterTypeNode ***REMOVED*** serializeTypeForDeclaration(context, parameterDeclaration, parameterType, parameterSymbol);
      const modifiers ***REMOVED*** !(context.flags & 8192 /* OmitParameterModifiers */) && preserveModifierFlags && parameterDeclaration && canHaveModifiers(parameterDeclaration) ? map(getModifiers(parameterDeclaration), factory.cloneNode) : void 0;
      const isRest ***REMOVED*** parameterDeclaration && isRestParameter(parameterDeclaration) || getCheckFlags(parameterSymbol) & 32768 /* RestParameter */;
      const dotDotDotToken ***REMOVED*** isRest ? factory.createToken(26 /* DotDotDotToken */) : void 0;
      const name ***REMOVED*** parameterToParameterDeclarationName(parameterSymbol, parameterDeclaration, context);
      const isOptional ***REMOVED*** parameterDeclaration && isOptionalParameter(parameterDeclaration) || getCheckFlags(parameterSymbol) & 16384 /* OptionalParameter */;
      const questionToken ***REMOVED*** isOptional ? factory.createToken(58 /* QuestionToken */) : void 0;
      const parameterNode ***REMOVED*** factory.createParameterDeclaration(
        modifiers,
        dotDotDotToken,
        name,
        questionToken,
        parameterTypeNode,
        /*initializer*/
        void 0
      );
      context.approximateLength +***REMOVED*** symbolName(parameterSymbol).length + 3;
      return parameterNode;
    }
    function parameterToParameterDeclarationName(parameterSymbol, parameterDeclaration, context) {
      return parameterDeclaration ? parameterDeclaration.name ? parameterDeclaration.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? setEmitFlags(factory.cloneNode(parameterDeclaration.name), 16777216 /* NoAsciiEscaping */) : parameterDeclaration.name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? setEmitFlags(factory.cloneNode(parameterDeclaration.name.right), 16777216 /* NoAsciiEscaping */) : cloneBindingName(parameterDeclaration.name) : symbolName(parameterSymbol) : symbolName(parameterSymbol);
      function cloneBindingName(node) {
        return elideInitializerAndSetEmitFlags(node);
        function elideInitializerAndSetEmitFlags(node2) {
          if (context.tracker.canTrackSymbol && isComputedPropertyName(node2) && isLateBindableName(node2)) {
            trackComputedName(node2.expression, context.enclosingDeclaration, context);
          }
          let visited ***REMOVED*** visitEachChild(
            node2,
            elideInitializerAndSetEmitFlags,
            /*context*/
            void 0,
            /*nodesVisitor*/
            void 0,
            elideInitializerAndSetEmitFlags
          );
          if (isBindingElement(visited)) {
            visited ***REMOVED*** factory.updateBindingElement(
              visited,
              visited.dotDotDotToken,
              visited.propertyName,
              visited.name,
              /*initializer*/
              void 0
            );
          }
          if (!nodeIsSynthesized(visited)) {
            visited ***REMOVED*** factory.cloneNode(visited);
          }
          return setEmitFlags(visited, 1 /* SingleLine */ | 16777216 /* NoAsciiEscaping */);
        }
      }
    }
    function trackComputedName(accessExpression, enclosingDeclaration, context) {
      if (!context.tracker.canTrackSymbol) return;
      const firstIdentifier ***REMOVED*** getFirstIdentifier(accessExpression);
      const name ***REMOVED*** resolveName(
        enclosingDeclaration,
        firstIdentifier.escapedText,
        111551 /* Value */ | 1048576 /* ExportValue */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        true
      );
      if (name) {
        context.tracker.trackSymbol(name, enclosingDeclaration, 111551 /* Value */);
      } else {
        const fallback ***REMOVED*** resolveName(
          firstIdentifier,
          firstIdentifier.escapedText,
          111551 /* Value */ | 1048576 /* ExportValue */,
          /*nameNotFoundMessage*/
          void 0,
          /*isUse*/
          true
        );
        if (fallback) {
          context.tracker.trackSymbol(fallback, enclosingDeclaration, 111551 /* Value */);
        }
      }
    }
    function lookupSymbolChain(symbol, context, meaning, yieldModuleSymbol) {
      context.tracker.trackSymbol(symbol, context.enclosingDeclaration, meaning);
      return lookupSymbolChainWorker(symbol, context, meaning, yieldModuleSymbol);
    }
    function lookupSymbolChainWorker(symbol, context, meaning, yieldModuleSymbol) {
      let chain;
      const isTypeParameter ***REMOVED*** symbol.flags & 262144 /* TypeParameter */;
      if (!isTypeParameter && (context.enclosingDeclaration || context.flags & 64 /* UseFullyQualifiedType */) && !(context.internalFlags & 4 /* DoNotIncludeSymbolChain */)) {
        chain ***REMOVED*** Debug.checkDefined(getSymbolChain(
          symbol,
          meaning,
          /*endOfChain*/
          true
        ));
        Debug.assert(chain && chain.length > 0);
      } else {
        chain ***REMOVED*** [symbol];
      }
      return chain;
      function getSymbolChain(symbol2, meaning2, endOfChain) {
        let accessibleSymbolChain ***REMOVED*** getAccessibleSymbolChain(symbol2, context.enclosingDeclaration, meaning2, !!(context.flags & 128 /* UseOnlyExternalAliasing */));
        let parentSpecifiers;
        if (!accessibleSymbolChain || needsQualification(accessibleSymbolChain[0], context.enclosingDeclaration, accessibleSymbolChain.length ***REMOVED******REMOVED******REMOVED*** 1 ? meaning2 : getQualifiedLeftMeaning(meaning2))) {
          const parents ***REMOVED*** getContainersOfSymbol(accessibleSymbolChain ? accessibleSymbolChain[0] : symbol2, context.enclosingDeclaration, meaning2);
          if (length(parents)) {
            parentSpecifiers ***REMOVED*** parents.map(
              (symbol3) ***REMOVED***> some(symbol3.declarations, hasNonGlobalAugmentationExternalModuleSymbol) ? getSpecifierForModuleSymbol(symbol3, context) : void 0
            );
            const indices ***REMOVED*** parents.map((_, i) ***REMOVED***> i);
            indices.sort(sortByBestName);
            const sortedParents ***REMOVED*** indices.map((i) ***REMOVED***> parents[i]);
            for (const parent of sortedParents) {
              const parentChain ***REMOVED*** getSymbolChain(
                parent,
                getQualifiedLeftMeaning(meaning2),
                /*endOfChain*/
                false
              );
              if (parentChain) {
                if (parent.exports && parent.exports.get("export***REMOVED***" /* ExportEquals */) && getSymbolIfSameReference(parent.exports.get("export***REMOVED***" /* ExportEquals */), symbol2)) {
                  accessibleSymbolChain ***REMOVED*** parentChain;
                  break;
                }
                accessibleSymbolChain ***REMOVED*** parentChain.concat(accessibleSymbolChain || [getAliasForSymbolInContainer(parent, symbol2) || symbol2]);
                break;
              }
            }
          }
        }
        if (accessibleSymbolChain) {
          return accessibleSymbolChain;
        }
        if (
          // If this is the last part of outputting the symbol, always output. The cases apply only to parent symbols.
          endOfChain || // If a parent symbol is an anonymous type, don't write it.
          !(symbol2.flags & (2048 /* TypeLiteral */ | 4096 /* ObjectLiteral */))
        ) {
          if (!endOfChain && !yieldModuleSymbol && !!forEach(symbol2.declarations, hasNonGlobalAugmentationExternalModuleSymbol)) {
            return;
          }
          return [symbol2];
        }
        function sortByBestName(a, b) {
          const specifierA ***REMOVED*** parentSpecifiers[a];
          const specifierB ***REMOVED*** parentSpecifiers[b];
          if (specifierA && specifierB) {
            const isBRelative ***REMOVED*** pathIsRelative(specifierB);
            if (pathIsRelative(specifierA) ***REMOVED******REMOVED******REMOVED*** isBRelative) {
              return countPathComponents(specifierA) - countPathComponents(specifierB);
            }
            if (isBRelative) {
              return -1;
            }
            return 1;
          }
          return 0;
        }
      }
    }
    function typeParametersToTypeParameterDeclarations(symbol, context) {
      let typeParameterNodes;
      const targetSymbol ***REMOVED*** getTargetSymbol(symbol);
      if (targetSymbol.flags & (32 /* Class */ | 64 /* Interface */ | 524288 /* TypeAlias */)) {
        typeParameterNodes ***REMOVED*** factory.createNodeArray(map(getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol), (tp) ***REMOVED***> typeParameterToDeclaration(tp, context)));
      }
      return typeParameterNodes;
    }
    function lookupTypeParameterNodes(chain, index, context) {
      var _a;
      Debug.assert(chain && 0 <***REMOVED*** index && index < chain.length);
      const symbol ***REMOVED*** chain[index];
      const symbolId ***REMOVED*** getSymbolId(symbol);
      if ((_a ***REMOVED*** context.typeParameterSymbolList) ***REMOVED******REMOVED*** null ? void 0 : _a.has(symbolId)) {
        return void 0;
      }
      if (context.mustCreateTypeParameterSymbolList) {
        context.mustCreateTypeParameterSymbolList ***REMOVED*** false;
        context.typeParameterSymbolList ***REMOVED*** new Set(context.typeParameterSymbolList);
      }
      context.typeParameterSymbolList.add(symbolId);
      let typeParameterNodes;
      if (context.flags & 512 /* WriteTypeParametersInQualifiedName */ && index < chain.length - 1) {
        const parentSymbol ***REMOVED*** symbol;
        const nextSymbol ***REMOVED*** chain[index + 1];
        if (getCheckFlags(nextSymbol) & 1 /* Instantiated */) {
          const params ***REMOVED*** getTypeParametersOfClassOrInterface(
            parentSymbol.flags & 2097152 /* Alias */ ? resolveAlias(parentSymbol) : parentSymbol
          );
          typeParameterNodes ***REMOVED*** mapToTypeNodes(map(params, (t) ***REMOVED***> getMappedType(t, nextSymbol.links.mapper)), context);
        } else {
          typeParameterNodes ***REMOVED*** typeParametersToTypeParameterDeclarations(symbol, context);
        }
      }
      return typeParameterNodes;
    }
    function getTopmostIndexedAccessType(top) {
      if (isIndexedAccessTypeNode(top.objectType)) {
        return getTopmostIndexedAccessType(top.objectType);
      }
      return top;
    }
    function getSpecifierForModuleSymbol(symbol, context, overrideImportMode) {
      let file ***REMOVED*** getDeclarationOfKind(symbol, 308 /* SourceFile */);
      if (!file) {
        const equivalentFileSymbol ***REMOVED*** firstDefined(symbol.declarations, (d) ***REMOVED***> getFileSymbolIfFileSymbolExportEqualsContainer(d, symbol));
        if (equivalentFileSymbol) {
          file ***REMOVED*** getDeclarationOfKind(equivalentFileSymbol, 308 /* SourceFile */);
        }
      }
      if (file && file.moduleName !***REMOVED******REMOVED*** void 0) {
        return file.moduleName;
      }
      if (!file) {
        if (ambientModuleSymbolRegex.test(symbol.escapedName)) {
          return symbol.escapedName.substring(1, symbol.escapedName.length - 1);
        }
      }
      if (!context.enclosingFile || !context.tracker.moduleResolverHost) {
        if (ambientModuleSymbolRegex.test(symbol.escapedName)) {
          return symbol.escapedName.substring(1, symbol.escapedName.length - 1);
        }
        return getSourceFileOfNode(getNonAugmentationDeclaration(symbol)).fileName;
      }
      const enclosingDeclaration ***REMOVED*** getOriginalNode(context.enclosingDeclaration);
      const originalModuleSpecifier ***REMOVED*** canHaveModuleSpecifier(enclosingDeclaration) ? tryGetModuleSpecifierFromDeclaration(enclosingDeclaration) : void 0;
      const contextFile ***REMOVED*** context.enclosingFile;
      const resolutionMode ***REMOVED*** overrideImportMode || originalModuleSpecifier && host.getModeForUsageLocation(contextFile, originalModuleSpecifier) || contextFile && host.getDefaultResolutionModeForFile(contextFile);
      const cacheKey ***REMOVED*** createModeAwareCacheKey(contextFile.path, resolutionMode);
      const links ***REMOVED*** getSymbolLinks(symbol);
      let specifier ***REMOVED*** links.specifierCache && links.specifierCache.get(cacheKey);
      if (!specifier) {
        const isBundle2 ***REMOVED*** !!compilerOptions.outFile;
        const { moduleResolverHost } ***REMOVED*** context.tracker;
        const specifierCompilerOptions ***REMOVED*** isBundle2 ? { ...compilerOptions, baseUrl: moduleResolverHost.getCommonSourceDirectory() } : compilerOptions;
        specifier ***REMOVED*** first(getModuleSpecifiers(
          symbol,
          checker,
          specifierCompilerOptions,
          contextFile,
          moduleResolverHost,
          {
            importModuleSpecifierPreference: isBundle2 ? "non-relative" : "project-relative",
            importModuleSpecifierEnding: isBundle2 ? "minimal" : resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? "js" : void 0
          },
          { overrideImportMode }
        ));
        links.specifierCache ?? (links.specifierCache ***REMOVED*** /* @__PURE__ */ new Map());
        links.specifierCache.set(cacheKey, specifier);
      }
      return specifier;
    }
    function symbolToEntityNameNode(symbol) {
      const identifier ***REMOVED*** factory.createIdentifier(unescapeLeadingUnderscores(symbol.escapedName));
      return symbol.parent ? factory.createQualifiedName(symbolToEntityNameNode(symbol.parent), identifier) : identifier;
    }
    function symbolToTypeNode(symbol, context, meaning, overrideTypeArguments) {
      const chain ***REMOVED*** lookupSymbolChain(symbol, context, meaning, !(context.flags & 16384 /* UseAliasDefinedOutsideCurrentScope */));
      const isTypeOf ***REMOVED*** meaning ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */;
      if (some(chain[0].declarations, hasNonGlobalAugmentationExternalModuleSymbol)) {
        const nonRootParts ***REMOVED*** chain.length > 1 ? createAccessFromSymbolChain(chain, chain.length - 1, 1) : void 0;
        const typeParameterNodes ***REMOVED*** overrideTypeArguments || lookupTypeParameterNodes(chain, 0, context);
        const contextFile ***REMOVED*** getSourceFileOfNode(getOriginalNode(context.enclosingDeclaration));
        const targetFile ***REMOVED*** getSourceFileOfModule(chain[0]);
        let specifier;
        let attributes;
        if (getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 3 /* Node16 */ || getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 99 /* NodeNext */) {
          if ((targetFile ***REMOVED******REMOVED*** null ? void 0 : targetFile.impliedNodeFormat) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && targetFile.impliedNodeFormat !***REMOVED******REMOVED*** (contextFile ***REMOVED******REMOVED*** null ? void 0 : contextFile.impliedNodeFormat)) {
            specifier ***REMOVED*** getSpecifierForModuleSymbol(chain[0], context, 99 /* ESNext */);
            attributes ***REMOVED*** factory.createImportAttributes(
              factory.createNodeArray([
                factory.createImportAttribute(
                  factory.createStringLiteral("resolution-mode"),
                  factory.createStringLiteral("import")
                )
              ])
            );
          }
        }
        if (!specifier) {
          specifier ***REMOVED*** getSpecifierForModuleSymbol(chain[0], context);
        }
        if (!(context.flags & 67108864 /* AllowNodeModulesRelativePaths */) && getEmitModuleResolutionKind(compilerOptions) !***REMOVED******REMOVED*** 1 /* Classic */ && specifier.includes("/node_modules/")) {
          const oldSpecifier ***REMOVED*** specifier;
          if (getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 3 /* Node16 */ || getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 99 /* NodeNext */) {
            const swappedMode ***REMOVED*** (contextFile ***REMOVED******REMOVED*** null ? void 0 : contextFile.impliedNodeFormat) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? 1 /* CommonJS */ : 99 /* ESNext */;
            specifier ***REMOVED*** getSpecifierForModuleSymbol(chain[0], context, swappedMode);
            if (specifier.includes("/node_modules/")) {
              specifier ***REMOVED*** oldSpecifier;
            } else {
              attributes ***REMOVED*** factory.createImportAttributes(
                factory.createNodeArray([
                  factory.createImportAttribute(
                    factory.createStringLiteral("resolution-mode"),
                    factory.createStringLiteral(swappedMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? "import" : "require")
                  )
                ])
              );
            }
          }
          if (!attributes) {
            context.encounteredError ***REMOVED*** true;
            if (context.tracker.reportLikelyUnsafeImportRequiredError) {
              context.tracker.reportLikelyUnsafeImportRequiredError(oldSpecifier);
            }
          }
        }
        const lit ***REMOVED*** factory.createLiteralTypeNode(factory.createStringLiteral(specifier));
        context.approximateLength +***REMOVED*** specifier.length + 10;
        if (!nonRootParts || isEntityName(nonRootParts)) {
          if (nonRootParts) {
            const lastId ***REMOVED*** isIdentifier(nonRootParts) ? nonRootParts : nonRootParts.right;
            setIdentifierTypeArguments(
              lastId,
              /*typeArguments*/
              void 0
            );
          }
          return factory.createImportTypeNode(lit, attributes, nonRootParts, typeParameterNodes, isTypeOf);
        } else {
          const splitNode ***REMOVED*** getTopmostIndexedAccessType(nonRootParts);
          const qualifier ***REMOVED*** splitNode.objectType.typeName;
          return factory.createIndexedAccessTypeNode(factory.createImportTypeNode(lit, attributes, qualifier, typeParameterNodes, isTypeOf), splitNode.indexType);
        }
      }
      const entityName ***REMOVED*** createAccessFromSymbolChain(chain, chain.length - 1, 0);
      if (isIndexedAccessTypeNode(entityName)) {
        return entityName;
      }
      if (isTypeOf) {
        return factory.createTypeQueryNode(entityName);
      } else {
        const lastId ***REMOVED*** isIdentifier(entityName) ? entityName : entityName.right;
        const lastTypeArgs ***REMOVED*** getIdentifierTypeArguments(lastId);
        setIdentifierTypeArguments(
          lastId,
          /*typeArguments*/
          void 0
        );
        return factory.createTypeReferenceNode(entityName, lastTypeArgs);
      }
      function createAccessFromSymbolChain(chain2, index, stopper) {
        const typeParameterNodes ***REMOVED*** index ***REMOVED******REMOVED******REMOVED*** chain2.length - 1 ? overrideTypeArguments : lookupTypeParameterNodes(chain2, index, context);
        const symbol2 ***REMOVED*** chain2[index];
        const parent ***REMOVED*** chain2[index - 1];
        let symbolName2;
        if (index ***REMOVED******REMOVED******REMOVED*** 0) {
          context.flags |***REMOVED*** 16777216 /* InInitialEntityName */;
          symbolName2 ***REMOVED*** getNameOfSymbolAsWritten(symbol2, context);
          context.approximateLength +***REMOVED*** (symbolName2 ? symbolName2.length : 0) + 1;
          context.flags ^***REMOVED*** 16777216 /* InInitialEntityName */;
        } else {
          if (parent && getExportsOfSymbol(parent)) {
            const exports2 ***REMOVED*** getExportsOfSymbol(parent);
            forEachEntry(exports2, (ex, name) ***REMOVED***> {
              if (getSymbolIfSameReference(ex, symbol2) && !isLateBoundName(name) && name !***REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) {
                symbolName2 ***REMOVED*** unescapeLeadingUnderscores(name);
                return true;
              }
            });
          }
        }
        if (symbolName2 ***REMOVED******REMOVED******REMOVED*** void 0) {
          const name ***REMOVED*** firstDefined(symbol2.declarations, getNameOfDeclaration);
          if (name && isComputedPropertyName(name) && isEntityName(name.expression)) {
            const LHS ***REMOVED*** createAccessFromSymbolChain(chain2, index - 1, stopper);
            if (isEntityName(LHS)) {
              return factory.createIndexedAccessTypeNode(factory.createParenthesizedType(factory.createTypeQueryNode(LHS)), factory.createTypeQueryNode(name.expression));
            }
            return LHS;
          }
          symbolName2 ***REMOVED*** getNameOfSymbolAsWritten(symbol2, context);
        }
        context.approximateLength +***REMOVED*** symbolName2.length + 1;
        if (!(context.flags & 16 /* ForbidIndexedAccessSymbolReferences */) && parent && getMembersOfSymbol(parent) && getMembersOfSymbol(parent).get(symbol2.escapedName) && getSymbolIfSameReference(getMembersOfSymbol(parent).get(symbol2.escapedName), symbol2)) {
          const LHS ***REMOVED*** createAccessFromSymbolChain(chain2, index - 1, stopper);
          if (isIndexedAccessTypeNode(LHS)) {
            return factory.createIndexedAccessTypeNode(LHS, factory.createLiteralTypeNode(factory.createStringLiteral(symbolName2)));
          } else {
            return factory.createIndexedAccessTypeNode(factory.createTypeReferenceNode(LHS, typeParameterNodes), factory.createLiteralTypeNode(factory.createStringLiteral(symbolName2)));
          }
        }
        const identifier ***REMOVED*** setEmitFlags(factory.createIdentifier(symbolName2), 16777216 /* NoAsciiEscaping */);
        if (typeParameterNodes) setIdentifierTypeArguments(identifier, factory.createNodeArray(typeParameterNodes));
        identifier.symbol ***REMOVED*** symbol2;
        if (index > stopper) {
          const LHS ***REMOVED*** createAccessFromSymbolChain(chain2, index - 1, stopper);
          if (!isEntityName(LHS)) {
            return Debug.fail("Impossible construct - an export of an indexed access cannot be reachable");
          }
          return factory.createQualifiedName(LHS, identifier);
        }
        return identifier;
      }
    }
    function typeParameterShadowsOtherTypeParameterInScope(escapedName, context, type) {
      const result ***REMOVED*** resolveName(
        context.enclosingDeclaration,
        escapedName,
        788968 /* Type */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      );
      if (result && result.flags & 262144 /* TypeParameter */) {
        return result !***REMOVED******REMOVED*** type.symbol;
      }
      return false;
    }
    function typeParameterToName(type, context) {
      var _a, _b, _c, _d;
      if (context.flags & 4 /* GenerateNamesForShadowedTypeParams */ && context.typeParameterNames) {
        const cached ***REMOVED*** context.typeParameterNames.get(getTypeId(type));
        if (cached) {
          return cached;
        }
      }
      let result ***REMOVED*** symbolToName(
        type.symbol,
        context,
        788968 /* Type */,
        /*expectsIdentifier*/
        true
      );
      if (!(result.kind & 80 /* Identifier */)) {
        return factory.createIdentifier("(Missing type parameter)");
      }
      const decl ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** type.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.declarations) ***REMOVED******REMOVED*** null ? void 0 : _b[0];
      if (decl && isTypeParameterDeclaration(decl)) {
        result ***REMOVED*** setTextRange2(context, result, decl.name);
      }
      if (context.flags & 4 /* GenerateNamesForShadowedTypeParams */) {
        const rawtext ***REMOVED*** result.escapedText;
        let i ***REMOVED*** ((_c ***REMOVED*** context.typeParameterNamesByTextNextNameCount) ***REMOVED******REMOVED*** null ? void 0 : _c.get(rawtext)) || 0;
        let text ***REMOVED*** rawtext;
        while (((_d ***REMOVED*** context.typeParameterNamesByText) ***REMOVED******REMOVED*** null ? void 0 : _d.has(text)) || typeParameterShadowsOtherTypeParameterInScope(text, context, type)) {
          i++;
          text ***REMOVED*** `${rawtext}_${i}`;
        }
        if (text !***REMOVED******REMOVED*** rawtext) {
          const typeArguments ***REMOVED*** getIdentifierTypeArguments(result);
          result ***REMOVED*** factory.createIdentifier(text);
          setIdentifierTypeArguments(result, typeArguments);
        }
        if (context.mustCreateTypeParametersNamesLookups) {
          context.mustCreateTypeParametersNamesLookups ***REMOVED*** false;
          context.typeParameterNames ***REMOVED*** new Map(context.typeParameterNames);
          context.typeParameterNamesByTextNextNameCount ***REMOVED*** new Map(context.typeParameterNamesByTextNextNameCount);
          context.typeParameterNamesByText ***REMOVED*** new Set(context.typeParameterNamesByText);
        }
        context.typeParameterNamesByTextNextNameCount.set(rawtext, i);
        context.typeParameterNames.set(getTypeId(type), result);
        context.typeParameterNamesByText.add(text);
      }
      return result;
    }
    function symbolToName(symbol, context, meaning, expectsIdentifier) {
      const chain ***REMOVED*** lookupSymbolChain(symbol, context, meaning);
      if (expectsIdentifier && chain.length !***REMOVED******REMOVED*** 1 && !context.encounteredError && !(context.flags & 65536 /* AllowQualifiedNameInPlaceOfIdentifier */)) {
        context.encounteredError ***REMOVED*** true;
      }
      return createEntityNameFromSymbolChain(chain, chain.length - 1);
      function createEntityNameFromSymbolChain(chain2, index) {
        const typeParameterNodes ***REMOVED*** lookupTypeParameterNodes(chain2, index, context);
        const symbol2 ***REMOVED*** chain2[index];
        if (index ***REMOVED******REMOVED******REMOVED*** 0) {
          context.flags |***REMOVED*** 16777216 /* InInitialEntityName */;
        }
        const symbolName2 ***REMOVED*** getNameOfSymbolAsWritten(symbol2, context);
        if (index ***REMOVED******REMOVED******REMOVED*** 0) {
          context.flags ^***REMOVED*** 16777216 /* InInitialEntityName */;
        }
        const identifier ***REMOVED*** setEmitFlags(factory.createIdentifier(symbolName2), 16777216 /* NoAsciiEscaping */);
        if (typeParameterNodes) setIdentifierTypeArguments(identifier, factory.createNodeArray(typeParameterNodes));
        identifier.symbol ***REMOVED*** symbol2;
        return index > 0 ? factory.createQualifiedName(createEntityNameFromSymbolChain(chain2, index - 1), identifier) : identifier;
      }
    }
    function symbolToExpression(symbol, context, meaning) {
      const chain ***REMOVED*** lookupSymbolChain(symbol, context, meaning);
      return createExpressionFromSymbolChain(chain, chain.length - 1);
      function createExpressionFromSymbolChain(chain2, index) {
        const typeParameterNodes ***REMOVED*** lookupTypeParameterNodes(chain2, index, context);
        const symbol2 ***REMOVED*** chain2[index];
        if (index ***REMOVED******REMOVED******REMOVED*** 0) {
          context.flags |***REMOVED*** 16777216 /* InInitialEntityName */;
        }
        let symbolName2 ***REMOVED*** getNameOfSymbolAsWritten(symbol2, context);
        if (index ***REMOVED******REMOVED******REMOVED*** 0) {
          context.flags ^***REMOVED*** 16777216 /* InInitialEntityName */;
        }
        let firstChar ***REMOVED*** symbolName2.charCodeAt(0);
        if (isSingleOrDoubleQuote(firstChar) && some(symbol2.declarations, hasNonGlobalAugmentationExternalModuleSymbol)) {
          const specifier ***REMOVED*** getSpecifierForModuleSymbol(symbol2, context);
          context.approximateLength +***REMOVED*** 2 + specifier.length;
          return factory.createStringLiteral(specifier);
        }
        if (index ***REMOVED******REMOVED******REMOVED*** 0 || canUsePropertyAccess(symbolName2, languageVersion)) {
          const identifier ***REMOVED*** setEmitFlags(factory.createIdentifier(symbolName2), 16777216 /* NoAsciiEscaping */);
          if (typeParameterNodes) setIdentifierTypeArguments(identifier, factory.createNodeArray(typeParameterNodes));
          identifier.symbol ***REMOVED*** symbol2;
          context.approximateLength +***REMOVED*** 1 + symbolName2.length;
          return index > 0 ? factory.createPropertyAccessExpression(createExpressionFromSymbolChain(chain2, index - 1), identifier) : identifier;
        } else {
          if (firstChar ***REMOVED******REMOVED******REMOVED*** 91 /* openBracket */) {
            symbolName2 ***REMOVED*** symbolName2.substring(1, symbolName2.length - 1);
            firstChar ***REMOVED*** symbolName2.charCodeAt(0);
          }
          let expression;
          if (isSingleOrDoubleQuote(firstChar) && !(symbol2.flags & 8 /* EnumMember */)) {
            const literalText ***REMOVED*** stripQuotes(symbolName2).replace(/\\./g, (s) ***REMOVED***> s.substring(1));
            context.approximateLength +***REMOVED*** literalText.length + 2;
            expression ***REMOVED*** factory.createStringLiteral(literalText, firstChar ***REMOVED******REMOVED******REMOVED*** 39 /* singleQuote */);
          } else if ("" + +symbolName2 ***REMOVED******REMOVED******REMOVED*** symbolName2) {
            context.approximateLength +***REMOVED*** symbolName2.length;
            expression ***REMOVED*** factory.createNumericLiteral(+symbolName2);
          }
          if (!expression) {
            const identifier ***REMOVED*** setEmitFlags(factory.createIdentifier(symbolName2), 16777216 /* NoAsciiEscaping */);
            if (typeParameterNodes) setIdentifierTypeArguments(identifier, factory.createNodeArray(typeParameterNodes));
            identifier.symbol ***REMOVED*** symbol2;
            context.approximateLength +***REMOVED*** symbolName2.length;
            expression ***REMOVED*** identifier;
          }
          context.approximateLength +***REMOVED*** 2;
          return factory.createElementAccessExpression(createExpressionFromSymbolChain(chain2, index - 1), expression);
        }
      }
    }
    function isStringNamed(d) {
      const name ***REMOVED*** getNameOfDeclaration(d);
      if (!name) {
        return false;
      }
      if (isComputedPropertyName(name)) {
        const type ***REMOVED*** checkExpression(name.expression);
        return !!(type.flags & 402653316 /* StringLike */);
      }
      if (isElementAccessExpression(name)) {
        const type ***REMOVED*** checkExpression(name.argumentExpression);
        return !!(type.flags & 402653316 /* StringLike */);
      }
      return isStringLiteral(name);
    }
    function isSingleQuotedStringNamed(d) {
      const name ***REMOVED*** getNameOfDeclaration(d);
      return !!(name && isStringLiteral(name) && (name.singleQuote || !nodeIsSynthesized(name) && startsWith(getTextOfNode(
        name,
        /*includeTrivia*/
        false
      ), "'")));
    }
    function getPropertyNameNodeForSymbol(symbol, context) {
      const hashPrivateName ***REMOVED*** getClonedHashPrivateName(symbol);
      if (hashPrivateName) {
        const shouldEmitErroneousFieldName ***REMOVED*** !!context.tracker.reportPrivateInBaseOfClassExpression && context.flags & 2048 /* WriteClassExpressionAsTypeLiteral */;
        if (!shouldEmitErroneousFieldName) {
          return hashPrivateName;
        } else {
          let rawName2 ***REMOVED*** unescapeLeadingUnderscores(symbol.escapedName);
          rawName2 ***REMOVED*** rawName2.replace(/__#\d+@#/g, "__#private@#");
          return createPropertyNameNodeForIdentifierOrLiteral(
            rawName2,
            getEmitScriptTarget(compilerOptions),
            /*singleQuote*/
            false,
            /*stringNamed*/
            true,
            !!(symbol.flags & 8192 /* Method */)
          );
        }
      }
      const stringNamed ***REMOVED*** !!length(symbol.declarations) && every(symbol.declarations, isStringNamed);
      const singleQuote ***REMOVED*** !!length(symbol.declarations) && every(symbol.declarations, isSingleQuotedStringNamed);
      const isMethod ***REMOVED*** !!(symbol.flags & 8192 /* Method */);
      const fromNameType ***REMOVED*** getPropertyNameNodeForSymbolFromNameType(symbol, context, singleQuote, stringNamed, isMethod);
      if (fromNameType) {
        return fromNameType;
      }
      const rawName ***REMOVED*** unescapeLeadingUnderscores(symbol.escapedName);
      return createPropertyNameNodeForIdentifierOrLiteral(rawName, getEmitScriptTarget(compilerOptions), singleQuote, stringNamed, isMethod);
    }
    function getPropertyNameNodeForSymbolFromNameType(symbol, context, singleQuote, stringNamed, isMethod) {
      const nameType ***REMOVED*** getSymbolLinks(symbol).nameType;
      if (nameType) {
        if (nameType.flags & 384 /* StringOrNumberLiteral */) {
          const name ***REMOVED*** "" + nameType.value;
          if (!isIdentifierText(name, getEmitScriptTarget(compilerOptions)) && (stringNamed || !isNumericLiteralName(name))) {
            return factory.createStringLiteral(name, !!singleQuote);
          }
          if (isNumericLiteralName(name) && startsWith(name, "-")) {
            return factory.createComputedPropertyName(factory.createPrefixUnaryExpression(41 /* MinusToken */, factory.createNumericLiteral(-name)));
          }
          return createPropertyNameNodeForIdentifierOrLiteral(name, getEmitScriptTarget(compilerOptions), singleQuote, stringNamed, isMethod);
        }
        if (nameType.flags & 8192 /* UniqueESSymbol */) {
          return factory.createComputedPropertyName(symbolToExpression(nameType.symbol, context, 111551 /* Value */));
        }
      }
    }
    function cloneNodeBuilderContext(context) {
      const oldMustCreateTypeParameterSymbolList ***REMOVED*** context.mustCreateTypeParameterSymbolList;
      const oldMustCreateTypeParametersNamesLookups ***REMOVED*** context.mustCreateTypeParametersNamesLookups;
      context.mustCreateTypeParameterSymbolList ***REMOVED*** true;
      context.mustCreateTypeParametersNamesLookups ***REMOVED*** true;
      const oldTypeParameterNames ***REMOVED*** context.typeParameterNames;
      const oldTypeParameterNamesByText ***REMOVED*** context.typeParameterNamesByText;
      const oldTypeParameterNamesByTextNextNameCount ***REMOVED*** context.typeParameterNamesByTextNextNameCount;
      const oldTypeParameterSymbolList ***REMOVED*** context.typeParameterSymbolList;
      return () ***REMOVED***> {
        context.typeParameterNames ***REMOVED*** oldTypeParameterNames;
        context.typeParameterNamesByText ***REMOVED*** oldTypeParameterNamesByText;
        context.typeParameterNamesByTextNextNameCount ***REMOVED*** oldTypeParameterNamesByTextNextNameCount;
        context.typeParameterSymbolList ***REMOVED*** oldTypeParameterSymbolList;
        context.mustCreateTypeParameterSymbolList ***REMOVED*** oldMustCreateTypeParameterSymbolList;
        context.mustCreateTypeParametersNamesLookups ***REMOVED*** oldMustCreateTypeParametersNamesLookups;
      };
    }
    function getDeclarationWithTypeAnnotation(symbol, enclosingDeclaration) {
      return symbol.declarations && find(symbol.declarations, (s) ***REMOVED***> !!getNonlocalEffectiveTypeAnnotationNode(s) && (!enclosingDeclaration || !!findAncestor(s, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** enclosingDeclaration)));
    }
    function existingTypeNodeIsNotReferenceOrIsReferenceWithCompatibleTypeArgumentCount(existing, type) {
      if (!(getObjectFlags(type) & 4 /* Reference */)) return true;
      if (!isTypeReferenceNode(existing)) return true;
      void getTypeFromTypeReference(existing);
      const symbol ***REMOVED*** getNodeLinks(existing).resolvedSymbol;
      const existingTarget ***REMOVED*** symbol && getDeclaredTypeOfSymbol(symbol);
      if (!existingTarget || existingTarget !***REMOVED******REMOVED*** type.target) return true;
      return length(existing.typeArguments) >***REMOVED*** getMinTypeArgumentCount(type.target.typeParameters);
    }
    function getEnclosingDeclarationIgnoringFakeScope(enclosingDeclaration) {
      while (getNodeLinks(enclosingDeclaration).fakeScopeForSignatureDeclaration) {
        enclosingDeclaration ***REMOVED*** enclosingDeclaration.parent;
      }
      return enclosingDeclaration;
    }
    function serializeInferredTypeForDeclaration(symbol, context, type) {
      if (type.flags & 8192 /* UniqueESSymbol */ && type.symbol ***REMOVED******REMOVED******REMOVED*** symbol && (!context.enclosingDeclaration || some(symbol.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** context.enclosingFile))) {
        context.flags |***REMOVED*** 1048576 /* AllowUniqueESSymbolType */;
      }
      const result ***REMOVED*** typeToTypeNodeHelper(type, context);
      return result;
    }
    function serializeTypeForDeclaration(context, declaration, type, symbol) {
      var _a;
      let result;
      const addUndefinedForParameter ***REMOVED*** declaration && (isParameter(declaration) || isJSDocParameterTag(declaration)) && requiresAddingImplicitUndefined(declaration, context.enclosingDeclaration);
      const decl ***REMOVED*** declaration ?? symbol.valueDeclaration ?? getDeclarationWithTypeAnnotation(symbol) ?? ((_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0]);
      if (!canPossiblyExpandType(type, context) && decl) {
        const restore ***REMOVED*** addSymbolTypeToContext(context, symbol, type);
        if (isAccessor(decl)) {
          result ***REMOVED*** syntacticNodeBuilder.serializeTypeOfAccessor(decl, symbol, context);
        } else if (hasInferredType(decl) && !nodeIsSynthesized(decl) && !(getObjectFlags(type) & 196608 /* RequiresWidening */)) {
          result ***REMOVED*** syntacticNodeBuilder.serializeTypeOfDeclaration(decl, symbol, context);
        }
        restore();
      }
      if (!result) {
        if (addUndefinedForParameter) {
          type ***REMOVED*** getOptionalType(type);
        }
        result ***REMOVED*** serializeInferredTypeForDeclaration(symbol, context, type);
      }
      return result ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
    function typeNodeIsEquivalentToType(annotatedDeclaration, type, typeFromTypeNode) {
      if (typeFromTypeNode ***REMOVED******REMOVED******REMOVED*** type) {
        return true;
      }
      if (!annotatedDeclaration) {
        return false;
      }
      if ((isPropertySignature(annotatedDeclaration) || isPropertyDeclaration(annotatedDeclaration)) && annotatedDeclaration.questionToken) {
        return getTypeWithFacts(type, 524288 /* NEUndefined */) ***REMOVED******REMOVED******REMOVED*** typeFromTypeNode;
      }
      if (isParameter(annotatedDeclaration) && hasEffectiveQuestionToken(annotatedDeclaration)) {
        return getTypeWithFacts(type, 524288 /* NEUndefined */) ***REMOVED******REMOVED******REMOVED*** typeFromTypeNode;
      }
      return false;
    }
    function serializeReturnTypeForSignature(context, signature) {
      const suppressAny ***REMOVED*** context.flags & 256 /* SuppressAnyReturnType */;
      const restoreFlags ***REMOVED*** saveRestoreFlags(context);
      if (suppressAny) context.flags &***REMOVED*** ~256 /* SuppressAnyReturnType */;
      let returnTypeNode;
      const returnType ***REMOVED*** getReturnTypeOfSignature(signature);
      if (!(suppressAny && isTypeAny(returnType))) {
        if (signature.declaration && !nodeIsSynthesized(signature.declaration) && !canPossiblyExpandType(returnType, context)) {
          const declarationSymbol ***REMOVED*** getSymbolOfDeclaration(signature.declaration);
          const restore ***REMOVED*** addSymbolTypeToContext(context, declarationSymbol, returnType);
          returnTypeNode ***REMOVED*** syntacticNodeBuilder.serializeReturnTypeForSignature(signature.declaration, declarationSymbol, context);
          restore();
        }
        if (!returnTypeNode) {
          returnTypeNode ***REMOVED*** serializeInferredReturnTypeForSignature(context, signature, returnType);
        }
      }
      if (!returnTypeNode && !suppressAny) {
        returnTypeNode ***REMOVED*** factory.createKeywordTypeNode(133 /* AnyKeyword */);
      }
      restoreFlags();
      return returnTypeNode;
    }
    function serializeInferredReturnTypeForSignature(context, signature, returnType) {
      const oldSuppressReportInferenceFallback ***REMOVED*** context.suppressReportInferenceFallback;
      context.suppressReportInferenceFallback ***REMOVED*** true;
      const typePredicate ***REMOVED*** getTypePredicateOfSignature(signature);
      const returnTypeNode ***REMOVED*** typePredicate ? typePredicateToTypePredicateNodeHelper(context.mapper ? instantiateTypePredicate(typePredicate, context.mapper) : typePredicate, context) : typeToTypeNodeHelper(returnType, context);
      context.suppressReportInferenceFallback ***REMOVED*** oldSuppressReportInferenceFallback;
      return returnTypeNode;
    }
    function trackExistingEntityName(node, context, enclosingDeclaration ***REMOVED*** context.enclosingDeclaration) {
      let introducesError ***REMOVED*** false;
      const leftmost ***REMOVED*** getFirstIdentifier(node);
      if (isInJSFile(node) && (isExportsIdentifier(leftmost) || isModuleExportsAccessExpression(leftmost.parent) || isQualifiedName(leftmost.parent) && isModuleIdentifier(leftmost.parent.left) && isExportsIdentifier(leftmost.parent.right))) {
        introducesError ***REMOVED*** true;
        return { introducesError, node };
      }
      const meaning ***REMOVED*** getMeaningOfEntityNameReference(node);
      let sym;
      if (isThisIdentifier(leftmost)) {
        sym ***REMOVED*** getSymbolOfDeclaration(getThisContainer(
          leftmost,
          /*includeArrowFunctions*/
          false,
          /*includeClassComputedPropertyName*/
          false
        ));
        if (isSymbolAccessible(
          sym,
          leftmost,
          meaning,
          /*shouldComputeAliasesToMakeVisible*/
          false
        ).accessibility !***REMOVED******REMOVED*** 0 /* Accessible */) {
          introducesError ***REMOVED*** true;
          context.tracker.reportInaccessibleThisError();
        }
        return { introducesError, node: attachSymbolToLeftmostIdentifier(node) };
      }
      sym ***REMOVED*** resolveEntityName(
        leftmost,
        meaning,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        true
      );
      if (context.enclosingDeclaration && !(sym && sym.flags & 262144 /* TypeParameter */)) {
        sym ***REMOVED*** getExportSymbolOfValueSymbolIfExported(sym);
        const symAtLocation ***REMOVED*** resolveEntityName(
          leftmost,
          meaning,
          /*ignoreErrors*/
          true,
          /*dontResolveAlias*/
          true,
          context.enclosingDeclaration
        );
        if (
          // Check for unusable parameters symbols
          symAtLocation ***REMOVED******REMOVED******REMOVED*** unknownSymbol || // If the symbol is not found, but was not found in the original scope either we probably have an error, don't reuse the node
          symAtLocation ***REMOVED******REMOVED******REMOVED*** void 0 && sym !***REMOVED******REMOVED*** void 0 || // If the symbol is found both in declaration scope and in current scope then it should point to the same reference
          symAtLocation && sym && !getSymbolIfSameReference(getExportSymbolOfValueSymbolIfExported(symAtLocation), sym)
        ) {
          if (symAtLocation !***REMOVED******REMOVED*** unknownSymbol) {
            context.tracker.reportInferenceFallback(node);
          }
          introducesError ***REMOVED*** true;
          return { introducesError, node, sym };
        } else {
          sym ***REMOVED*** symAtLocation;
        }
      }
      if (sym) {
        if (sym.flags & 1 /* FunctionScopedVariable */ && sym.valueDeclaration) {
          if (isPartOfParameterDeclaration(sym.valueDeclaration) || isJSDocParameterTag(sym.valueDeclaration)) {
            return { introducesError, node: attachSymbolToLeftmostIdentifier(node) };
          }
        }
        if (!(sym.flags & 262144 /* TypeParameter */) && // Type parameters are visible in the current context if they are are resolvable
        !isDeclarationName(node) && isSymbolAccessible(
          sym,
          enclosingDeclaration,
          meaning,
          /*shouldComputeAliasesToMakeVisible*/
          false
        ).accessibility !***REMOVED******REMOVED*** 0 /* Accessible */) {
          context.tracker.reportInferenceFallback(node);
          introducesError ***REMOVED*** true;
        } else {
          context.tracker.trackSymbol(sym, enclosingDeclaration, meaning);
        }
        return { introducesError, node: attachSymbolToLeftmostIdentifier(node) };
      }
      return { introducesError, node };
      function attachSymbolToLeftmostIdentifier(node2) {
        if (node2 ***REMOVED******REMOVED******REMOVED*** leftmost) {
          const type ***REMOVED*** getDeclaredTypeOfSymbol(sym);
          const name ***REMOVED*** sym.flags & 262144 /* TypeParameter */ ? typeParameterToName(type, context) : factory.cloneNode(node2);
          name.symbol ***REMOVED*** sym;
          return setTextRange2(context, setEmitFlags(name, 16777216 /* NoAsciiEscaping */), node2);
        }
        const updated ***REMOVED*** visitEachChild(
          node2,
          (c) ***REMOVED***> attachSymbolToLeftmostIdentifier(c),
          /*context*/
          void 0
        );
        return setTextRange2(context, updated, node2);
      }
    }
    function serializeTypeName(context, node, isTypeOf, typeArguments) {
      const meaning ***REMOVED*** isTypeOf ? 111551 /* Value */ : 788968 /* Type */;
      const symbol ***REMOVED*** resolveEntityName(
        node,
        meaning,
        /*ignoreErrors*/
        true
      );
      if (!symbol) return void 0;
      const resolvedSymbol ***REMOVED*** symbol.flags & 2097152 /* Alias */ ? resolveAlias(symbol) : symbol;
      if (isSymbolAccessible(
        symbol,
        context.enclosingDeclaration,
        meaning,
        /*shouldComputeAliasesToMakeVisible*/
        false
      ).accessibility !***REMOVED******REMOVED*** 0 /* Accessible */) return void 0;
      return symbolToTypeNode(resolvedSymbol, context, meaning, typeArguments);
    }
    function canReuseTypeNode(context, existing) {
      const type ***REMOVED*** getTypeFromTypeNode2(
        context,
        existing,
        /*noMappedTypes*/
        true
      );
      if (!type) {
        return false;
      }
      if (isInJSFile(existing)) {
        if (isLiteralImportTypeNode(existing)) {
          void getTypeFromImportTypeNode(existing);
          const nodeSymbol ***REMOVED*** getNodeLinks(existing).resolvedSymbol;
          return !nodeSymbol || !// The import type resolved using jsdoc fallback logic
          (!existing.isTypeOf && !(nodeSymbol.flags & 788968 /* Type */) || // The import type had type arguments autofilled by js fallback logic
          !(length(existing.typeArguments) >***REMOVED*** getMinTypeArgumentCount(getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(nodeSymbol))));
        }
      }
      if (isTypeReferenceNode(existing)) {
        if (isConstTypeReference(existing)) return false;
        const symbol ***REMOVED*** getNodeLinks(existing).resolvedSymbol;
        if (!symbol) return false;
        if (symbol.flags & 262144 /* TypeParameter */) {
          const declaredType ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
          return !(context.mapper && getMappedType(declaredType, context.mapper) !***REMOVED******REMOVED*** declaredType);
        }
        if (isInJSDoc(existing)) {
          return existingTypeNodeIsNotReferenceOrIsReferenceWithCompatibleTypeArgumentCount(existing, type) && !getIntendedTypeFromJSDocTypeReference(existing) && !!(symbol.flags & 788968 /* Type */);
        }
      }
      if (isTypeOperatorNode(existing) && existing.operator ***REMOVED******REMOVED******REMOVED*** 158 /* UniqueKeyword */ && existing.type.kind ***REMOVED******REMOVED******REMOVED*** 155 /* SymbolKeyword */) {
        const effectiveEnclosingContext ***REMOVED*** context.enclosingDeclaration && getEnclosingDeclarationIgnoringFakeScope(context.enclosingDeclaration);
        return !!findAncestor(existing, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** effectiveEnclosingContext);
      }
      return true;
    }
    function serializeExistingTypeNode(context, typeNode, addUndefined) {
      const type ***REMOVED*** getTypeFromTypeNode2(context, typeNode);
      if (addUndefined && !someType(type, (t) ***REMOVED***> !!(t.flags & 32768 /* Undefined */)) && canReuseTypeNode(context, typeNode)) {
        const clone ***REMOVED*** syntacticNodeBuilder.tryReuseExistingTypeNode(context, typeNode);
        if (clone) {
          return factory.createUnionTypeNode([clone, factory.createKeywordTypeNode(157 /* UndefinedKeyword */)]);
        }
      }
      return typeToTypeNodeHelper(type, context);
    }
    function symbolTableToDeclarationStatements(symbolTable, context) {
      var _a;
      const serializePropertySymbolForClass ***REMOVED*** makeSerializePropertySymbol(
        factory.createPropertyDeclaration,
        175 /* MethodDeclaration */,
        /*useAccessors*/
        true
      );
      const serializePropertySymbolForInterfaceWorker ***REMOVED*** makeSerializePropertySymbol(
        (mods, name, question, type) ***REMOVED***> factory.createPropertySignature(mods, name, question, type),
        174 /* MethodSignature */,
        /*useAccessors*/
        false
      );
      const enclosingDeclaration ***REMOVED*** context.enclosingDeclaration;
      let results ***REMOVED*** [];
      const visitedSymbols ***REMOVED*** /* @__PURE__ */ new Set();
      const deferredPrivatesStack ***REMOVED*** [];
      const oldcontext ***REMOVED*** context;
      context ***REMOVED*** {
        ...oldcontext,
        usedSymbolNames: new Set(oldcontext.usedSymbolNames),
        remappedSymbolNames: /* @__PURE__ */ new Map(),
        remappedSymbolReferences: new Map((_a ***REMOVED*** oldcontext.remappedSymbolReferences) ***REMOVED******REMOVED*** null ? void 0 : _a.entries()),
        tracker: void 0
      };
      const tracker ***REMOVED*** {
        ...oldcontext.tracker.inner,
        trackSymbol: (sym, decl, meaning) ***REMOVED***> {
          var _a2, _b;
          if ((_a2 ***REMOVED*** context.remappedSymbolNames) ***REMOVED******REMOVED*** null ? void 0 : _a2.has(getSymbolId(sym))) return false;
          const accessibleResult ***REMOVED*** isSymbolAccessible(
            sym,
            decl,
            meaning,
            /*shouldComputeAliasesToMakeVisible*/
            false
          );
          if (accessibleResult.accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */) {
            const chain ***REMOVED*** lookupSymbolChainWorker(sym, context, meaning);
            if (!(sym.flags & 4 /* Property */)) {
              const root ***REMOVED*** chain[0];
              const contextFile ***REMOVED*** getSourceFileOfNode(oldcontext.enclosingDeclaration);
              if (some(root.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** contextFile)) {
                includePrivateSymbol(root);
              }
            }
          } else if ((_b ***REMOVED*** oldcontext.tracker.inner) ***REMOVED******REMOVED*** null ? void 0 : _b.trackSymbol) {
            return oldcontext.tracker.inner.trackSymbol(sym, decl, meaning);
          }
          return false;
        }
      };
      context.tracker ***REMOVED*** new SymbolTrackerImpl(context, tracker, oldcontext.tracker.moduleResolverHost);
      forEachEntry(symbolTable, (symbol, name) ***REMOVED***> {
        const baseName ***REMOVED*** unescapeLeadingUnderscores(name);
        void getInternalSymbolName(symbol, baseName);
      });
      let addingDeclare ***REMOVED*** !context.bundled;
      const exportEquals ***REMOVED*** symbolTable.get("export***REMOVED***" /* ExportEquals */);
      if (exportEquals && symbolTable.size > 1 && exportEquals.flags & (2097152 /* Alias */ | 1536 /* Module */)) {
        symbolTable ***REMOVED*** createSymbolTable();
        symbolTable.set("export***REMOVED***" /* ExportEquals */, exportEquals);
      }
      visitSymbolTable(symbolTable);
      return mergeRedundantStatements(results);
      function isIdentifierAndNotUndefined(node) {
        return !!node && node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */;
      }
      function getNamesOfDeclaration(statement) {
        if (isVariableStatement(statement)) {
          return filter(map(statement.declarationList.declarations, getNameOfDeclaration), isIdentifierAndNotUndefined);
        }
        return filter([getNameOfDeclaration(statement)], isIdentifierAndNotUndefined);
      }
      function flattenExportAssignedNamespace(statements) {
        const exportAssignment ***REMOVED*** find(statements, isExportAssignment);
        const nsIndex ***REMOVED*** findIndex(statements, isModuleDeclaration);
        let ns ***REMOVED*** nsIndex !***REMOVED******REMOVED*** -1 ? statements[nsIndex] : void 0;
        if (ns && exportAssignment && exportAssignment.isExportEquals && isIdentifier(exportAssignment.expression) && isIdentifier(ns.name) && idText(ns.name) ***REMOVED******REMOVED******REMOVED*** idText(exportAssignment.expression) && ns.body && isModuleBlock(ns.body)) {
          const excessExports ***REMOVED*** filter(statements, (s) ***REMOVED***> !!(getEffectiveModifierFlags(s) & 32 /* Export */));
          const name ***REMOVED*** ns.name;
          let body ***REMOVED*** ns.body;
          if (length(excessExports)) {
            ns ***REMOVED*** factory.updateModuleDeclaration(
              ns,
              ns.modifiers,
              ns.name,
              body ***REMOVED*** factory.updateModuleBlock(
                body,
                factory.createNodeArray([
                  ...ns.body.statements,
                  factory.createExportDeclaration(
                    /*modifiers*/
                    void 0,
                    /*isTypeOnly*/
                    false,
                    factory.createNamedExports(map(flatMap(excessExports, (e) ***REMOVED***> getNamesOfDeclaration(e)), (id) ***REMOVED***> factory.createExportSpecifier(
                      /*isTypeOnly*/
                      false,
                      /*propertyName*/
                      void 0,
                      id
                    ))),
                    /*moduleSpecifier*/
                    void 0
                  )
                ])
              )
            );
            statements ***REMOVED*** [...statements.slice(0, nsIndex), ns, ...statements.slice(nsIndex + 1)];
          }
          if (!find(statements, (s) ***REMOVED***> s !***REMOVED******REMOVED*** ns && nodeHasName(s, name))) {
            results ***REMOVED*** [];
            const mixinExportFlag ***REMOVED*** !some(body.statements, (s) ***REMOVED***> hasSyntacticModifier(s, 32 /* Export */) || isExportAssignment(s) || isExportDeclaration(s));
            forEach(body.statements, (s) ***REMOVED***> {
              addResult(s, mixinExportFlag ? 32 /* Export */ : 0 /* None */);
            });
            statements ***REMOVED*** [...filter(statements, (s) ***REMOVED***> s !***REMOVED******REMOVED*** ns && s !***REMOVED******REMOVED*** exportAssignment), ...results];
          }
        }
        return statements;
      }
      function mergeExportDeclarations(statements) {
        const exports2 ***REMOVED*** filter(statements, (d) ***REMOVED***> isExportDeclaration(d) && !d.moduleSpecifier && !!d.exportClause && isNamedExports(d.exportClause));
        if (length(exports2) > 1) {
          const nonExports ***REMOVED*** filter(statements, (d) ***REMOVED***> !isExportDeclaration(d) || !!d.moduleSpecifier || !d.exportClause);
          statements ***REMOVED*** [
            ...nonExports,
            factory.createExportDeclaration(
              /*modifiers*/
              void 0,
              /*isTypeOnly*/
              false,
              factory.createNamedExports(flatMap(exports2, (e) ***REMOVED***> cast(e.exportClause, isNamedExports).elements)),
              /*moduleSpecifier*/
              void 0
            )
          ];
        }
        const reexports ***REMOVED*** filter(statements, (d) ***REMOVED***> isExportDeclaration(d) && !!d.moduleSpecifier && !!d.exportClause && isNamedExports(d.exportClause));
        if (length(reexports) > 1) {
          const groups ***REMOVED*** group(reexports, (decl) ***REMOVED***> isStringLiteral(decl.moduleSpecifier) ? ">" + decl.moduleSpecifier.text : ">");
          if (groups.length !***REMOVED******REMOVED*** reexports.length) {
            for (const group2 of groups) {
              if (group2.length > 1) {
                statements ***REMOVED*** [
                  ...filter(statements, (s) ***REMOVED***> !group2.includes(s)),
                  factory.createExportDeclaration(
                    /*modifiers*/
                    void 0,
                    /*isTypeOnly*/
                    false,
                    factory.createNamedExports(flatMap(group2, (e) ***REMOVED***> cast(e.exportClause, isNamedExports).elements)),
                    group2[0].moduleSpecifier
                  )
                ];
              }
            }
          }
        }
        return statements;
      }
      function inlineExportModifiers(statements) {
        const index ***REMOVED*** findIndex(statements, (d) ***REMOVED***> isExportDeclaration(d) && !d.moduleSpecifier && !d.attributes && !!d.exportClause && isNamedExports(d.exportClause));
        if (index >***REMOVED*** 0) {
          const exportDecl ***REMOVED*** statements[index];
          const replacements ***REMOVED*** mapDefined(exportDecl.exportClause.elements, (e) ***REMOVED***> {
            if (!e.propertyName && e.name.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
              const name ***REMOVED*** e.name;
              const indices ***REMOVED*** indicesOf(statements);
              const associatedIndices ***REMOVED*** filter(indices, (i) ***REMOVED***> nodeHasName(statements[i], name));
              if (length(associatedIndices) && every(associatedIndices, (i) ***REMOVED***> canHaveExportModifier(statements[i]))) {
                for (const index2 of associatedIndices) {
                  statements[index2] ***REMOVED*** addExportModifier(statements[index2]);
                }
                return void 0;
              }
            }
            return e;
          });
          if (!length(replacements)) {
            orderedRemoveItemAt(statements, index);
          } else {
            statements[index] ***REMOVED*** factory.updateExportDeclaration(
              exportDecl,
              exportDecl.modifiers,
              exportDecl.isTypeOnly,
              factory.updateNamedExports(
                exportDecl.exportClause,
                replacements
              ),
              exportDecl.moduleSpecifier,
              exportDecl.attributes
            );
          }
        }
        return statements;
      }
      function mergeRedundantStatements(statements) {
        statements ***REMOVED*** flattenExportAssignedNamespace(statements);
        statements ***REMOVED*** mergeExportDeclarations(statements);
        statements ***REMOVED*** inlineExportModifiers(statements);
        if (enclosingDeclaration && (isSourceFile(enclosingDeclaration) && isExternalOrCommonJsModule(enclosingDeclaration) || isModuleDeclaration(enclosingDeclaration)) && (!some(statements, isExternalModuleIndicator) || !hasScopeMarker(statements) && some(statements, needsScopeMarker))) {
          statements.push(createEmptyExports(factory));
        }
        return statements;
      }
      function addExportModifier(node) {
        const flags ***REMOVED*** (getEffectiveModifierFlags(node) | 32 /* Export */) & ~128 /* Ambient */;
        return factory.replaceModifiers(node, flags);
      }
      function removeExportModifier(node) {
        const flags ***REMOVED*** getEffectiveModifierFlags(node) & ~32 /* Export */;
        return factory.replaceModifiers(node, flags);
      }
      function visitSymbolTable(symbolTable2, suppressNewPrivateContext, propertyAsAlias) {
        if (!suppressNewPrivateContext) {
          deferredPrivatesStack.push(/* @__PURE__ */ new Map());
        }
        let i ***REMOVED*** 0;
        const symbols ***REMOVED*** Array.from(symbolTable2.values());
        for (const symbol of symbols) {
          i++;
          if (checkTruncationLengthIfExpanding(context) && i + 2 < symbolTable2.size - 1) {
            context.out.truncated ***REMOVED*** true;
            results.push(createTruncationStatement(`... (${symbolTable2.size - i} more ...)`));
            serializeSymbol(
              symbols[symbols.length - 1],
              /*isPrivate*/
              false,
              !!propertyAsAlias
            );
            break;
          }
          serializeSymbol(
            symbol,
            /*isPrivate*/
            false,
            !!propertyAsAlias
          );
        }
        if (!suppressNewPrivateContext) {
          deferredPrivatesStack[deferredPrivatesStack.length - 1].forEach((symbol) ***REMOVED***> {
            serializeSymbol(
              symbol,
              /*isPrivate*/
              true,
              !!propertyAsAlias
            );
          });
          deferredPrivatesStack.pop();
        }
      }
      function serializeSymbol(symbol, isPrivate, propertyAsAlias) {
        void getPropertiesOfType(getTypeOfSymbol(symbol));
        const visitedSym ***REMOVED*** getMergedSymbol(symbol);
        if (visitedSymbols.has(getSymbolId(visitedSym))) {
          return;
        }
        visitedSymbols.add(getSymbolId(visitedSym));
        const skipMembershipCheck ***REMOVED*** !isPrivate;
        if (skipMembershipCheck || !!length(symbol.declarations) && some(symbol.declarations, (d) ***REMOVED***> !!findAncestor(d, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** enclosingDeclaration))) {
          const scopeCleanup ***REMOVED*** cloneNodeBuilderContext(context);
          context.tracker.pushErrorFallbackNode(find(symbol.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** context.enclosingFile));
          serializeSymbolWorker(symbol, isPrivate, propertyAsAlias);
          context.tracker.popErrorFallbackNode();
          scopeCleanup();
        }
      }
      function serializeSymbolWorker(symbol, isPrivate, propertyAsAlias, escapedSymbolName ***REMOVED*** symbol.escapedName) {
        var _a2, _b, _c, _d, _e, _f, _g;
        const symbolName2 ***REMOVED*** unescapeLeadingUnderscores(escapedSymbolName);
        const isDefault ***REMOVED*** escapedSymbolName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */;
        if (isPrivate && !(context.flags & 131072 /* AllowAnonymousIdentifier */) && isStringANonContextualKeyword(symbolName2) && !isDefault) {
          context.encounteredError ***REMOVED*** true;
          return;
        }
        let needsPostExportDefault ***REMOVED*** isDefault && !!(symbol.flags & -113 /* ExportDoesNotSupportDefaultModifier */ || symbol.flags & 16 /* Function */ && length(getPropertiesOfType(getTypeOfSymbol(symbol)))) && !(symbol.flags & 2097152 /* Alias */);
        let needsExportDeclaration ***REMOVED*** !needsPostExportDefault && !isPrivate && isStringANonContextualKeyword(symbolName2) && !isDefault;
        if (needsPostExportDefault || needsExportDeclaration) {
          isPrivate ***REMOVED*** true;
        }
        const modifierFlags ***REMOVED*** (!isPrivate ? 32 /* Export */ : 0) | (isDefault && !needsPostExportDefault ? 2048 /* Default */ : 0);
        const isConstMergedWithNS ***REMOVED*** symbol.flags & 1536 /* Module */ && symbol.flags & (2 /* BlockScopedVariable */ | 1 /* FunctionScopedVariable */ | 4 /* Property */) && escapedSymbolName !***REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */;
        const isConstMergedWithNSPrintableAsSignatureMerge ***REMOVED*** isConstMergedWithNS && isTypeRepresentableAsFunctionNamespaceMerge(getTypeOfSymbol(symbol), symbol);
        if (symbol.flags & (16 /* Function */ | 8192 /* Method */) || isConstMergedWithNSPrintableAsSignatureMerge) {
          serializeAsFunctionNamespaceMerge(getTypeOfSymbol(symbol), symbol, getInternalSymbolName(symbol, symbolName2), modifierFlags);
        }
        if (symbol.flags & 524288 /* TypeAlias */) {
          serializeTypeAlias(symbol, symbolName2, modifierFlags);
        }
        if (symbol.flags & (2 /* BlockScopedVariable */ | 1 /* FunctionScopedVariable */ | 4 /* Property */ | 98304 /* Accessor */) && escapedSymbolName !***REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */ && !(symbol.flags & 4194304 /* Prototype */) && !(symbol.flags & 32 /* Class */) && !(symbol.flags & 8192 /* Method */) && !isConstMergedWithNSPrintableAsSignatureMerge) {
          if (propertyAsAlias) {
            const createdExport ***REMOVED*** serializeMaybeAliasAssignment(symbol);
            if (createdExport) {
              needsExportDeclaration ***REMOVED*** false;
              needsPostExportDefault ***REMOVED*** false;
            }
          } else {
            const type ***REMOVED*** getTypeOfSymbol(symbol);
            const localName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
            if (type.symbol && type.symbol !***REMOVED******REMOVED*** symbol && type.symbol.flags & 16 /* Function */ && some(type.symbol.declarations, isFunctionExpressionOrArrowFunction) && (((_a2 ***REMOVED*** type.symbol.members) ***REMOVED******REMOVED*** null ? void 0 : _a2.size) || ((_b ***REMOVED*** type.symbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.size))) {
              if (!context.remappedSymbolReferences) {
                context.remappedSymbolReferences ***REMOVED*** /* @__PURE__ */ new Map();
              }
              context.remappedSymbolReferences.set(getSymbolId(type.symbol), symbol);
              serializeSymbolWorker(type.symbol, isPrivate, propertyAsAlias, escapedSymbolName);
              context.remappedSymbolReferences.delete(getSymbolId(type.symbol));
            } else if (!(symbol.flags & 16 /* Function */) && isTypeRepresentableAsFunctionNamespaceMerge(type, symbol)) {
              serializeAsFunctionNamespaceMerge(type, symbol, localName, modifierFlags);
            } else {
              const flags ***REMOVED*** !(symbol.flags & 2 /* BlockScopedVariable */) ? ((_c ***REMOVED*** symbol.parent) ***REMOVED******REMOVED*** null ? void 0 : _c.valueDeclaration) && isSourceFile((_d ***REMOVED*** symbol.parent) ***REMOVED******REMOVED*** null ? void 0 : _d.valueDeclaration) ? 2 /* Const */ : void 0 : isConstantVariable(symbol) ? 2 /* Const */ : 1 /* Let */;
              const name ***REMOVED*** needsPostExportDefault || !(symbol.flags & 4 /* Property */) ? localName : getUnusedName(localName, symbol);
              let textRange ***REMOVED*** symbol.declarations && find(symbol.declarations, (d) ***REMOVED***> isVariableDeclaration(d));
              if (textRange && isVariableDeclarationList(textRange.parent) && textRange.parent.declarations.length ***REMOVED******REMOVED******REMOVED*** 1) {
                textRange ***REMOVED*** textRange.parent.parent;
              }
              const propertyAccessRequire ***REMOVED*** (_e ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _e.find(isPropertyAccessExpression);
              if (propertyAccessRequire && isBinaryExpression(propertyAccessRequire.parent) && isIdentifier(propertyAccessRequire.parent.right) && ((_f ***REMOVED*** type.symbol) ***REMOVED******REMOVED*** null ? void 0 : _f.valueDeclaration) && isSourceFile(type.symbol.valueDeclaration)) {
                const alias ***REMOVED*** localName ***REMOVED******REMOVED******REMOVED*** propertyAccessRequire.parent.right.escapedText ? void 0 : propertyAccessRequire.parent.right;
                context.approximateLength +***REMOVED*** 12 + (((_g ***REMOVED*** alias ***REMOVED******REMOVED*** null ? void 0 : alias.escapedText) ***REMOVED******REMOVED*** null ? void 0 : _g.length) ?? 0);
                addResult(
                  factory.createExportDeclaration(
                    /*modifiers*/
                    void 0,
                    /*isTypeOnly*/
                    false,
                    factory.createNamedExports([factory.createExportSpecifier(
                      /*isTypeOnly*/
                      false,
                      alias,
                      localName
                    )])
                  ),
                  0 /* None */
                );
                context.tracker.trackSymbol(type.symbol, context.enclosingDeclaration, 111551 /* Value */);
              } else {
                const statement ***REMOVED*** setTextRange2(
                  context,
                  factory.createVariableStatement(
                    /*modifiers*/
                    void 0,
                    factory.createVariableDeclarationList([
                      factory.createVariableDeclaration(
                        name,
                        /*exclamationToken*/
                        void 0,
                        serializeTypeForDeclaration(
                          context,
                          /*declaration*/
                          void 0,
                          type,
                          symbol
                        )
                      )
                    ], flags)
                  ),
                  textRange
                );
                context.approximateLength +***REMOVED*** 7 + name.length;
                addResult(statement, name !***REMOVED******REMOVED*** localName ? modifierFlags & ~32 /* Export */ : modifierFlags);
                if (name !***REMOVED******REMOVED*** localName && !isPrivate) {
                  context.approximateLength +***REMOVED*** 16 + name.length + localName.length;
                  addResult(
                    factory.createExportDeclaration(
                      /*modifiers*/
                      void 0,
                      /*isTypeOnly*/
                      false,
                      factory.createNamedExports([factory.createExportSpecifier(
                        /*isTypeOnly*/
                        false,
                        name,
                        localName
                      )])
                    ),
                    0 /* None */
                  );
                  needsExportDeclaration ***REMOVED*** false;
                  needsPostExportDefault ***REMOVED*** false;
                }
              }
            }
          }
        }
        if (symbol.flags & 384 /* Enum */) {
          serializeEnum(symbol, symbolName2, modifierFlags);
        }
        if (symbol.flags & 32 /* Class */) {
          if (symbol.flags & 4 /* Property */ && symbol.valueDeclaration && isBinaryExpression(symbol.valueDeclaration.parent) && isClassExpression(symbol.valueDeclaration.parent.right)) {
            serializeAsAlias(symbol, getInternalSymbolName(symbol, symbolName2), modifierFlags);
          } else {
            serializeAsClass(symbol, getInternalSymbolName(symbol, symbolName2), modifierFlags);
          }
        }
        if (symbol.flags & (512 /* ValueModule */ | 1024 /* NamespaceModule */) && (!isConstMergedWithNS || isTypeOnlyNamespace(symbol)) || isConstMergedWithNSPrintableAsSignatureMerge) {
          serializeModule(symbol, symbolName2, modifierFlags);
        }
        if (symbol.flags & 64 /* Interface */ && !(symbol.flags & 32 /* Class */)) {
          serializeInterface(symbol, symbolName2, modifierFlags);
        }
        if (symbol.flags & 2097152 /* Alias */) {
          serializeAsAlias(symbol, getInternalSymbolName(symbol, symbolName2), modifierFlags);
        }
        if (symbol.flags & 4 /* Property */ && symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) {
          serializeMaybeAliasAssignment(symbol);
        }
        if (symbol.flags & 8388608 /* ExportStar */) {
          if (symbol.declarations) {
            for (const node of symbol.declarations) {
              const resolvedModule ***REMOVED*** resolveExternalModuleName(node, node.moduleSpecifier);
              if (!resolvedModule) continue;
              const isTypeOnly ***REMOVED*** node.isTypeOnly;
              const specifier ***REMOVED*** getSpecifierForModuleSymbol(resolvedModule, context);
              context.approximateLength +***REMOVED*** 17 + specifier.length;
              addResult(factory.createExportDeclaration(
                /*modifiers*/
                void 0,
                isTypeOnly,
                /*exportClause*/
                void 0,
                factory.createStringLiteral(specifier)
              ), 0 /* None */);
            }
          }
        }
        if (needsPostExportDefault) {
          const internalSymbolName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
          context.approximateLength +***REMOVED*** 16 + internalSymbolName.length;
          addResult(factory.createExportAssignment(
            /*modifiers*/
            void 0,
            /*isExportEquals*/
            false,
            factory.createIdentifier(internalSymbolName)
          ), 0 /* None */);
        } else if (needsExportDeclaration) {
          const internalSymbolName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
          context.approximateLength +***REMOVED*** 22 + symbolName2.length + internalSymbolName.length;
          addResult(
            factory.createExportDeclaration(
              /*modifiers*/
              void 0,
              /*isTypeOnly*/
              false,
              factory.createNamedExports([factory.createExportSpecifier(
                /*isTypeOnly*/
                false,
                internalSymbolName,
                symbolName2
              )])
            ),
            0 /* None */
          );
        }
      }
      function includePrivateSymbol(symbol) {
        if (some(symbol.declarations, isPartOfParameterDeclaration)) return;
        Debug.assertIsDefined(deferredPrivatesStack[deferredPrivatesStack.length - 1]);
        getUnusedName(unescapeLeadingUnderscores(symbol.escapedName), symbol);
        const isExternalImportAlias ***REMOVED*** !!(symbol.flags & 2097152 /* Alias */) && !some(symbol.declarations, (d) ***REMOVED***> !!findAncestor(d, isExportDeclaration) || isNamespaceExport(d) || isImportEqualsDeclaration(d) && !isExternalModuleReference(d.moduleReference));
        deferredPrivatesStack[isExternalImportAlias ? 0 : deferredPrivatesStack.length - 1].set(getSymbolId(symbol), symbol);
      }
      function isExportingScope(enclosingDeclaration2) {
        return isSourceFile(enclosingDeclaration2) && (isExternalOrCommonJsModule(enclosingDeclaration2) || isJsonSourceFile(enclosingDeclaration2)) || isAmbientModule(enclosingDeclaration2) && !isGlobalScopeAugmentation(enclosingDeclaration2);
      }
      function addResult(node, additionalModifierFlags) {
        if (canHaveModifiers(node)) {
          const oldModifierFlags ***REMOVED*** getEffectiveModifierFlags(node);
          let newModifierFlags ***REMOVED*** 0 /* None */;
          const enclosingDeclaration2 ***REMOVED*** context.enclosingDeclaration && (isJSDocTypeAlias(context.enclosingDeclaration) ? getSourceFileOfNode(context.enclosingDeclaration) : context.enclosingDeclaration);
          if (additionalModifierFlags & 32 /* Export */ && enclosingDeclaration2 && (isExportingScope(enclosingDeclaration2) || isModuleDeclaration(enclosingDeclaration2)) && canHaveExportModifier(node)) {
            newModifierFlags |***REMOVED*** 32 /* Export */;
          }
          if (addingDeclare && !(newModifierFlags & 32 /* Export */) && (!enclosingDeclaration2 || !(enclosingDeclaration2.flags & 33554432 /* Ambient */)) && (isEnumDeclaration(node) || isVariableStatement(node) || isFunctionDeclaration(node) || isClassDeclaration(node) || isModuleDeclaration(node))) {
            newModifierFlags |***REMOVED*** 128 /* Ambient */;
          }
          if (additionalModifierFlags & 2048 /* Default */ && (isClassDeclaration(node) || isInterfaceDeclaration(node) || isFunctionDeclaration(node))) {
            newModifierFlags |***REMOVED*** 2048 /* Default */;
          }
          if (newModifierFlags) {
            node ***REMOVED*** factory.replaceModifiers(node, newModifierFlags | oldModifierFlags);
          }
          context.approximateLength +***REMOVED*** modifiersLength(newModifierFlags | oldModifierFlags);
        }
        results.push(node);
      }
      function serializeTypeAlias(symbol, symbolName2, modifierFlags) {
        var _a2;
        const aliasType ***REMOVED*** getDeclaredTypeOfTypeAlias(symbol);
        const typeParams ***REMOVED*** getSymbolLinks(symbol).typeParameters;
        const typeParamDecls ***REMOVED*** map(typeParams, (p) ***REMOVED***> typeParameterToDeclaration(p, context));
        const jsdocAliasDecl ***REMOVED*** (_a2 ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a2.find(isJSDocTypeAlias);
        const commentText ***REMOVED*** getTextOfJSDocComment(jsdocAliasDecl ? jsdocAliasDecl.comment || jsdocAliasDecl.parent.comment : void 0);
        const restoreFlags ***REMOVED*** saveRestoreFlags(context);
        context.flags |***REMOVED*** 8388608 /* InTypeAlias */;
        const oldEnclosingDecl ***REMOVED*** context.enclosingDeclaration;
        context.enclosingDeclaration ***REMOVED*** jsdocAliasDecl;
        const typeNode ***REMOVED*** jsdocAliasDecl && jsdocAliasDecl.typeExpression && isJSDocTypeExpression(jsdocAliasDecl.typeExpression) && syntacticNodeBuilder.tryReuseExistingTypeNode(context, jsdocAliasDecl.typeExpression.type) || typeToTypeNodeHelper(aliasType, context);
        const internalSymbolName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
        context.approximateLength +***REMOVED*** 8 + ((commentText ***REMOVED******REMOVED*** null ? void 0 : commentText.length) ?? 0) + internalSymbolName.length;
        addResult(
          setSyntheticLeadingComments(
            factory.createTypeAliasDeclaration(
              /*modifiers*/
              void 0,
              internalSymbolName,
              typeParamDecls,
              typeNode
            ),
            !commentText ? [] : [{ kind: 3 /* MultiLineCommentTrivia */, text: "*\n * " + commentText.replace(/\n/g, "\n * ") + "\n ", pos: -1, end: -1, hasTrailingNewLine: true }]
          ),
          modifierFlags
        );
        restoreFlags();
        context.enclosingDeclaration ***REMOVED*** oldEnclosingDecl;
      }
      function serializeInterface(symbol, symbolName2, modifierFlags) {
        const internalSymbolName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
        context.approximateLength +***REMOVED*** 14 + internalSymbolName.length;
        const interfaceType ***REMOVED*** getDeclaredTypeOfClassOrInterface(symbol);
        const localParams ***REMOVED*** getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol);
        const typeParamDecls ***REMOVED*** map(localParams, (p) ***REMOVED***> typeParameterToDeclaration(p, context));
        const baseTypes ***REMOVED*** getBaseTypes(interfaceType);
        const baseType ***REMOVED*** length(baseTypes) ? getIntersectionType(baseTypes) : void 0;
        const members ***REMOVED*** serializePropertySymbolsForClassOrInterface(
          getPropertiesOfType(interfaceType),
          /*isClass*/
          false,
          baseType
        );
        const callSignatures ***REMOVED*** serializeSignatures(0 /* Call */, interfaceType, baseType, 180 /* CallSignature */);
        const constructSignatures ***REMOVED*** serializeSignatures(1 /* Construct */, interfaceType, baseType, 181 /* ConstructSignature */);
        const indexSignatures ***REMOVED*** serializeIndexSignatures(interfaceType, baseType);
        const heritageClauses ***REMOVED*** !length(baseTypes) ? void 0 : [factory.createHeritageClause(96 /* ExtendsKeyword */, mapDefined(baseTypes, (b) ***REMOVED***> trySerializeAsTypeReference(b, 111551 /* Value */)))];
        addResult(
          factory.createInterfaceDeclaration(
            /*modifiers*/
            void 0,
            internalSymbolName,
            typeParamDecls,
            heritageClauses,
            [...indexSignatures, ...constructSignatures, ...callSignatures, ...members]
          ),
          modifierFlags
        );
      }
      function serializePropertySymbolsForClassOrInterface(props, isClass, baseType, isStatic2) {
        const elements ***REMOVED*** [];
        let i ***REMOVED*** 0;
        for (const prop of props) {
          i++;
          if (checkTruncationLengthIfExpanding(context) && i + 2 < props.length - 1) {
            context.out.truncated ***REMOVED*** true;
            const placeholder ***REMOVED*** createTruncationProperty(`... ${props.length - i} more ... `, isClass);
            elements.push(placeholder);
            const result2 ***REMOVED*** isClass ? serializePropertySymbolForClass(props[props.length - 1], isStatic2, baseType) : serializePropertySymbolForInterface(props[props.length - 1], baseType);
            if (isArray(result2)) {
              elements.push(...result2);
            } else {
              elements.push(result2);
            }
            break;
          }
          context.approximateLength +***REMOVED*** 1;
          const result ***REMOVED*** isClass ? serializePropertySymbolForClass(prop, isStatic2, baseType) : serializePropertySymbolForInterface(prop, baseType);
          if (isArray(result)) {
            elements.push(...result);
          } else {
            elements.push(result);
          }
        }
        return elements;
      }
      function createTruncationProperty(dotDotDotText, isClass) {
        if (context.flags & 1 /* NoTruncation */) {
          return addSyntheticLeadingComment(factory.createNotEmittedTypeElement(), 3 /* MultiLineCommentTrivia */, dotDotDotText);
        }
        return isClass ? factory.createPropertyDeclaration(
          /*modifiers*/
          void 0,
          dotDotDotText,
          /*questionOrExclamationToken*/
          void 0,
          /*type*/
          void 0,
          /*initializer*/
          void 0
        ) : factory.createPropertySignature(
          /*modifiers*/
          void 0,
          dotDotDotText,
          /*questionToken*/
          void 0,
          /*type*/
          void 0
        );
      }
      function getNamespaceMembersForSerialization(symbol) {
        let exports2 ***REMOVED*** arrayFrom(getExportsOfSymbol(symbol).values());
        const merged ***REMOVED*** getMergedSymbol(symbol);
        if (merged !***REMOVED******REMOVED*** symbol) {
          const membersSet ***REMOVED*** new Set(exports2);
          for (const exported of getExportsOfSymbol(merged).values()) {
            if (!(getSymbolFlags(resolveSymbol(exported)) & 111551 /* Value */)) {
              membersSet.add(exported);
            }
          }
          exports2 ***REMOVED*** arrayFrom(membersSet);
        }
        return filter(exports2, (m) ***REMOVED***> isNamespaceMember(m) && isIdentifierText(m.escapedName, 99 /* ESNext */));
      }
      function isTypeOnlyNamespace(symbol) {
        return every(getNamespaceMembersForSerialization(symbol), (m) ***REMOVED***> !(getSymbolFlags(resolveSymbol(m)) & 111551 /* Value */));
      }
      function serializeModule(symbol, symbolName2, modifierFlags) {
        const members ***REMOVED*** getNamespaceMembersForSerialization(symbol);
        const expanding ***REMOVED*** isExpanding(context);
        const locationMap ***REMOVED*** arrayToMultiMap(members, (m) ***REMOVED***> m.parent && m.parent ***REMOVED******REMOVED******REMOVED*** symbol || expanding ? "real" : "merged");
        const realMembers ***REMOVED*** locationMap.get("real") || emptyArray;
        const mergedMembers ***REMOVED*** locationMap.get("merged") || emptyArray;
        if (length(realMembers) || expanding) {
          let localName;
          if (expanding) {
            const oldFlags ***REMOVED*** context.flags;
            context.flags |***REMOVED*** 512 /* WriteTypeParametersInQualifiedName */ | 2 /* UseOnlyExternalAliasing */;
            localName ***REMOVED*** symbolToNode(
              symbol,
              context,
              /*meaning*/
              -1 /* All */
            );
            context.flags ***REMOVED*** oldFlags;
          } else {
            const localText ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
            localName ***REMOVED*** factory.createIdentifier(localText);
            context.approximateLength +***REMOVED*** localText.length;
          }
          serializeAsNamespaceDeclaration(realMembers, localName, modifierFlags, !!(symbol.flags & (16 /* Function */ | 67108864 /* Assignment */)));
        }
        if (length(mergedMembers)) {
          const containingFile ***REMOVED*** getSourceFileOfNode(context.enclosingDeclaration);
          const localName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
          const nsBody ***REMOVED*** factory.createModuleBlock([factory.createExportDeclaration(
            /*modifiers*/
            void 0,
            /*isTypeOnly*/
            false,
            factory.createNamedExports(mapDefined(filter(mergedMembers, (n) ***REMOVED***> n.escapedName !***REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */), (s) ***REMOVED***> {
              var _a2, _b;
              const name ***REMOVED*** unescapeLeadingUnderscores(s.escapedName);
              const localName2 ***REMOVED*** getInternalSymbolName(s, name);
              const aliasDecl ***REMOVED*** s.declarations && getDeclarationOfAliasSymbol(s);
              if (containingFile && (aliasDecl ? containingFile !***REMOVED******REMOVED*** getSourceFileOfNode(aliasDecl) : !some(s.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** containingFile))) {
                (_b ***REMOVED*** (_a2 ***REMOVED*** context.tracker) ***REMOVED******REMOVED*** null ? void 0 : _a2.reportNonlocalAugmentation) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a2, containingFile, symbol, s);
                return void 0;
              }
              const target ***REMOVED*** aliasDecl && getTargetOfAliasDeclaration(
                aliasDecl,
                /*dontRecursivelyResolve*/
                true
              );
              includePrivateSymbol(target || s);
              const targetName ***REMOVED*** target ? getInternalSymbolName(target, unescapeLeadingUnderscores(target.escapedName)) : localName2;
              return factory.createExportSpecifier(
                /*isTypeOnly*/
                false,
                name ***REMOVED******REMOVED******REMOVED*** targetName ? void 0 : targetName,
                name
              );
            }))
          )]);
          addResult(
            factory.createModuleDeclaration(
              /*modifiers*/
              void 0,
              factory.createIdentifier(localName),
              nsBody,
              32 /* Namespace */
            ),
            0 /* None */
          );
        }
      }
      function serializeEnum(symbol, symbolName2, modifierFlags) {
        const internalSymbolName ***REMOVED*** getInternalSymbolName(symbol, symbolName2);
        context.approximateLength +***REMOVED*** 9 + internalSymbolName.length;
        const members ***REMOVED*** [];
        const memberProps ***REMOVED*** filter(getPropertiesOfType(getTypeOfSymbol(symbol)), (p) ***REMOVED***> !!(p.flags & 8 /* EnumMember */));
        let i ***REMOVED*** 0;
        for (const p of memberProps) {
          i++;
          if (checkTruncationLengthIfExpanding(context) && i + 2 < memberProps.length - 1) {
            context.out.truncated ***REMOVED*** true;
            members.push(factory.createEnumMember(` ... ${memberProps.length - i} more ... `));
            const last2 ***REMOVED*** memberProps[memberProps.length - 1];
            const initializedValue ***REMOVED*** last2.declarations && last2.declarations[0] && isEnumMember(last2.declarations[0]) ? getConstantValue2(last2.declarations[0]) : void 0;
            const initializer2 ***REMOVED*** initializedValue ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : typeof initializedValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory.createStringLiteral(initializedValue) : factory.createNumericLiteral(initializedValue);
            const memberName2 ***REMOVED*** unescapeLeadingUnderscores(last2.escapedName);
            const member2 ***REMOVED*** factory.createEnumMember(
              memberName2,
              initializer2
            );
            members.push(member2);
            break;
          }
          const memberDecl ***REMOVED*** p.declarations && p.declarations[0] && isEnumMember(p.declarations[0]) ? p.declarations[0] : void 0;
          let initializer;
          let initializerLength;
          if (isExpanding(context) && memberDecl && memberDecl.initializer) {
            initializer ***REMOVED*** getSynthesizedDeepClone(memberDecl.initializer);
            initializerLength ***REMOVED*** memberDecl.initializer.end - memberDecl.initializer.pos;
          } else {
            const initializedValue ***REMOVED*** memberDecl && getConstantValue2(memberDecl);
            initializer ***REMOVED*** initializedValue ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : typeof initializedValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory.createStringLiteral(initializedValue) : factory.createNumericLiteral(initializedValue);
            initializerLength ***REMOVED*** (initializer ***REMOVED******REMOVED*** null ? void 0 : initializer.text.length) ?? 0;
          }
          const memberName ***REMOVED*** unescapeLeadingUnderscores(p.escapedName);
          context.approximateLength +***REMOVED*** 4 + memberName.length + initializerLength;
          const member ***REMOVED*** factory.createEnumMember(
            memberName,
            initializer
          );
          members.push(member);
        }
        addResult(
          factory.createEnumDeclaration(
            factory.createModifiersFromModifierFlags(isConstEnumSymbol(symbol) ? 4096 /* Const */ : 0),
            internalSymbolName,
            members
          ),
          modifierFlags
        );
      }
      function serializeAsFunctionNamespaceMerge(type, symbol, localName, modifierFlags) {
        const signatures ***REMOVED*** getSignaturesOfType(type, 0 /* Call */);
        for (const sig of signatures) {
          context.approximateLength +***REMOVED*** 1;
          const decl ***REMOVED*** signatureToSignatureDeclarationHelper(sig, 263 /* FunctionDeclaration */, context, { name: factory.createIdentifier(localName) });
          addResult(setTextRange2(context, decl, getSignatureTextRangeLocation(sig)), modifierFlags);
        }
        if (!(symbol.flags & (512 /* ValueModule */ | 1024 /* NamespaceModule */) && !!symbol.exports && !!symbol.exports.size)) {
          const props ***REMOVED*** filter(getPropertiesOfType(type), isNamespaceMember);
          context.approximateLength +***REMOVED*** localName.length;
          serializeAsNamespaceDeclaration(
            props,
            factory.createIdentifier(localName),
            modifierFlags,
            /*suppressNewPrivateContext*/
            true
          );
        }
      }
      function createTruncationStatement(dotDotDotText) {
        if (context.flags & 1 /* NoTruncation */) {
          return addSyntheticLeadingComment(factory.createEmptyStatement(), 3 /* MultiLineCommentTrivia */, dotDotDotText);
        }
        return factory.createExpressionStatement(factory.createIdentifier(dotDotDotText));
      }
      function getSignatureTextRangeLocation(signature) {
        if (signature.declaration && signature.declaration.parent) {
          if (isBinaryExpression(signature.declaration.parent) && getAssignmentDeclarationKind(signature.declaration.parent) ***REMOVED******REMOVED******REMOVED*** 5 /* Property */) {
            return signature.declaration.parent;
          }
          if (isVariableDeclaration(signature.declaration.parent) && signature.declaration.parent.parent) {
            return signature.declaration.parent.parent;
          }
        }
        return signature.declaration;
      }
      function serializeAsNamespaceDeclaration(props, localName, modifierFlags, suppressNewPrivateContext) {
        const nodeFlags ***REMOVED*** isIdentifier(localName) ? 32 /* Namespace */ : 0 /* None */;
        const expanding ***REMOVED*** isExpanding(context);
        if (length(props)) {
          context.approximateLength +***REMOVED*** 14;
          const localVsRemoteMap ***REMOVED*** arrayToMultiMap(props, (p) ***REMOVED***> !length(p.declarations) || some(p.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(context.enclosingDeclaration)) || expanding ? "local" : "remote");
          const localProps ***REMOVED*** localVsRemoteMap.get("local") || emptyArray;
          let fakespace ***REMOVED*** parseNodeFactory.createModuleDeclaration(
            /*modifiers*/
            void 0,
            localName,
            factory.createModuleBlock([]),
            nodeFlags
          );
          setParent(fakespace, enclosingDeclaration);
          fakespace.locals ***REMOVED*** createSymbolTable(props);
          fakespace.symbol ***REMOVED*** props[0].parent;
          const oldResults ***REMOVED*** results;
          results ***REMOVED*** [];
          const oldAddingDeclare ***REMOVED*** addingDeclare;
          addingDeclare ***REMOVED*** false;
          const subcontext ***REMOVED*** { ...context, enclosingDeclaration: fakespace };
          const oldContext ***REMOVED*** context;
          context ***REMOVED*** subcontext;
          visitSymbolTable(
            createSymbolTable(localProps),
            suppressNewPrivateContext,
            /*propertyAsAlias*/
            true
          );
          context ***REMOVED*** oldContext;
          addingDeclare ***REMOVED*** oldAddingDeclare;
          const declarations ***REMOVED*** results;
          results ***REMOVED*** oldResults;
          const defaultReplaced ***REMOVED*** map(declarations, (d) ***REMOVED***> isExportAssignment(d) && !d.isExportEquals && isIdentifier(d.expression) ? factory.createExportDeclaration(
            /*modifiers*/
            void 0,
            /*isTypeOnly*/
            false,
            factory.createNamedExports([factory.createExportSpecifier(
              /*isTypeOnly*/
              false,
              d.expression,
              factory.createIdentifier("default" /* Default */)
            )])
          ) : d);
          const exportModifierStripped ***REMOVED*** every(defaultReplaced, (d) ***REMOVED***> hasSyntacticModifier(d, 32 /* Export */)) ? map(defaultReplaced, removeExportModifier) : defaultReplaced;
          fakespace ***REMOVED*** factory.updateModuleDeclaration(
            fakespace,
            fakespace.modifiers,
            fakespace.name,
            factory.createModuleBlock(exportModifierStripped)
          );
          addResult(fakespace, modifierFlags);
        } else if (expanding) {
          context.approximateLength +***REMOVED*** 14;
          addResult(
            factory.createModuleDeclaration(
              /*modifiers*/
              void 0,
              localName,
              factory.createModuleBlock([]),
              nodeFlags
            ),
            modifierFlags
          );
        }
      }
      function isNamespaceMember(p) {
        return !!(p.flags & (788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */)) || !(p.flags & 4194304 /* Prototype */ || p.escapedName ***REMOVED******REMOVED******REMOVED*** "prototype" || p.valueDeclaration && isStatic(p.valueDeclaration) && isClassLike(p.valueDeclaration.parent));
      }
      function sanitizeJSDocImplements(clauses) {
        const result ***REMOVED*** mapDefined(clauses, (e) ***REMOVED***> {
          const oldEnclosing ***REMOVED*** context.enclosingDeclaration;
          context.enclosingDeclaration ***REMOVED*** e;
          let expr ***REMOVED*** e.expression;
          if (isEntityNameExpression(expr)) {
            if (isIdentifier(expr) && idText(expr) ***REMOVED******REMOVED******REMOVED*** "") {
              return cleanup(
                /*result*/
                void 0
              );
            }
            let introducesError;
            ({ introducesError, node: expr } ***REMOVED*** trackExistingEntityName(expr, context));
            if (introducesError) {
              return cleanup(
                /*result*/
                void 0
              );
            }
          }
          return cleanup(factory.createExpressionWithTypeArguments(
            expr,
            map(e.typeArguments, (a) ***REMOVED***> syntacticNodeBuilder.tryReuseExistingTypeNode(context, a) || typeToTypeNodeHelper(getTypeFromTypeNode2(context, a), context))
          ));
          function cleanup(result2) {
            context.enclosingDeclaration ***REMOVED*** oldEnclosing;
            return result2;
          }
        });
        if (result.length ***REMOVED******REMOVED******REMOVED*** clauses.length) {
          return result;
        }
        return void 0;
      }
      function serializeAsClass(symbol, localName, modifierFlags) {
        var _a2, _b;
        context.approximateLength +***REMOVED*** 9 + localName.length;
        const originalDecl ***REMOVED*** (_a2 ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a2.find(isClassLike);
        const oldEnclosing ***REMOVED*** context.enclosingDeclaration;
        context.enclosingDeclaration ***REMOVED*** originalDecl || oldEnclosing;
        const localParams ***REMOVED*** getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol);
        const typeParamDecls ***REMOVED*** map(localParams, (p) ***REMOVED***> typeParameterToDeclaration(p, context));
        forEach(localParams, (p) ***REMOVED***> context.approximateLength +***REMOVED*** symbolName(p.symbol).length);
        const classType ***REMOVED*** getTypeWithThisArgument(getDeclaredTypeOfClassOrInterface(symbol));
        const baseTypes ***REMOVED*** getBaseTypes(classType);
        const originalImplements ***REMOVED*** originalDecl && getEffectiveImplementsTypeNodes(originalDecl);
        const implementsExpressions ***REMOVED*** originalImplements && sanitizeJSDocImplements(originalImplements) || mapDefined(getImplementsTypes(classType), serializeImplementedType);
        const staticType ***REMOVED*** getTypeOfSymbol(symbol);
        const isClass ***REMOVED*** !!((_b ***REMOVED*** staticType.symbol) ***REMOVED******REMOVED*** null ? void 0 : _b.valueDeclaration) && isClassLike(staticType.symbol.valueDeclaration);
        const staticBaseType ***REMOVED*** isClass ? getBaseConstructorTypeOfClass(staticType) : anyType;
        context.approximateLength +***REMOVED*** (length(baseTypes) ? 8 : 0) + (length(implementsExpressions) ? 11 : 0);
        const heritageClauses ***REMOVED*** [
          ...!length(baseTypes) ? [] : [factory.createHeritageClause(96 /* ExtendsKeyword */, map(baseTypes, (b) ***REMOVED***> serializeBaseType(b, staticBaseType, localName)))],
          ...!length(implementsExpressions) ? [] : [factory.createHeritageClause(119 /* ImplementsKeyword */, implementsExpressions)]
        ];
        const symbolProps ***REMOVED*** getNonInheritedProperties(classType, baseTypes, getPropertiesOfType(classType));
        const publicSymbolProps ***REMOVED*** filter(symbolProps, (s) ***REMOVED***> !isHashPrivate(s));
        const hasPrivateIdentifier ***REMOVED*** some(symbolProps, isHashPrivate);
        const privateProperties ***REMOVED*** hasPrivateIdentifier ? isExpanding(context) ? serializePropertySymbolsForClassOrInterface(
          filter(symbolProps, isHashPrivate),
          /*isClass*/
          true,
          baseTypes[0],
          /*isStatic*/
          false
        ) : [factory.createPropertyDeclaration(
          /*modifiers*/
          void 0,
          factory.createPrivateIdentifier("#private"),
          /*questionOrExclamationToken*/
          void 0,
          /*type*/
          void 0,
          /*initializer*/
          void 0
        )] : emptyArray;
        if (hasPrivateIdentifier && !isExpanding(context)) {
          context.approximateLength +***REMOVED*** 9;
        }
        const publicProperties ***REMOVED*** serializePropertySymbolsForClassOrInterface(
          publicSymbolProps,
          /*isClass*/
          true,
          baseTypes[0],
          /*isStatic*/
          false
        );
        const staticMembers ***REMOVED*** serializePropertySymbolsForClassOrInterface(
          filter(getPropertiesOfType(staticType), (p) ***REMOVED***> !(p.flags & 4194304 /* Prototype */) && p.escapedName !***REMOVED******REMOVED*** "prototype" && !isNamespaceMember(p)),
          /*isClass*/
          true,
          staticBaseType,
          /*isStatic*/
          true
        );
        const isNonConstructableClassLikeInJsFile ***REMOVED*** !isClass && !!symbol.valueDeclaration && isInJSFile(symbol.valueDeclaration) && !some(getSignaturesOfType(staticType, 1 /* Construct */));
        if (isNonConstructableClassLikeInJsFile) context.approximateLength +***REMOVED*** 21;
        const constructors ***REMOVED*** isNonConstructableClassLikeInJsFile ? [factory.createConstructorDeclaration(
          factory.createModifiersFromModifierFlags(2 /* Private */),
          [],
          /*body*/
          void 0
        )] : serializeSignatures(1 /* Construct */, staticType, staticBaseType, 177 /* Constructor */);
        const indexSignatures ***REMOVED*** serializeIndexSignatures(classType, baseTypes[0]);
        context.enclosingDeclaration ***REMOVED*** oldEnclosing;
        addResult(
          setTextRange2(
            context,
            factory.createClassDeclaration(
              /*modifiers*/
              void 0,
              localName,
              typeParamDecls,
              heritageClauses,
              [...indexSignatures, ...staticMembers, ...constructors, ...publicProperties, ...privateProperties]
            ),
            symbol.declarations && filter(symbol.declarations, (d) ***REMOVED***> isClassDeclaration(d) || isClassExpression(d))[0]
          ),
          modifierFlags
        );
      }
      function getSomeTargetNameFromDeclarations(declarations) {
        return firstDefined(declarations, (d) ***REMOVED***> {
          if (isImportSpecifier(d) || isExportSpecifier(d)) {
            return moduleExportNameTextUnescaped(d.propertyName || d.name);
          }
          if (isBinaryExpression(d) || isExportAssignment(d)) {
            const expression ***REMOVED*** isExportAssignment(d) ? d.expression : d.right;
            if (isPropertyAccessExpression(expression)) {
              return idText(expression.name);
            }
          }
          if (isAliasSymbolDeclaration(d)) {
            const name ***REMOVED*** getNameOfDeclaration(d);
            if (name && isIdentifier(name)) {
              return idText(name);
            }
          }
          return void 0;
        });
      }
      function serializeAsAlias(symbol, localName, modifierFlags) {
        var _a2, _b, _c, _d, _e;
        const node ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
        if (!node) return Debug.fail();
        const target ***REMOVED*** getMergedSymbol(getTargetOfAliasDeclaration(
          node,
          /*dontRecursivelyResolve*/
          true
        ));
        if (!target) {
          return;
        }
        let verbatimTargetName ***REMOVED*** isShorthandAmbientModuleSymbol(target) && getSomeTargetNameFromDeclarations(symbol.declarations) || unescapeLeadingUnderscores(target.escapedName);
        if (verbatimTargetName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */ && allowSyntheticDefaultImports) {
          verbatimTargetName ***REMOVED*** "default" /* Default */;
        }
        const targetName ***REMOVED*** getInternalSymbolName(target, verbatimTargetName);
        includePrivateSymbol(target);
        switch (node.kind) {
          case 209 /* BindingElement */:
            if (((_b ***REMOVED*** (_a2 ***REMOVED*** node.parent) ***REMOVED******REMOVED*** null ? void 0 : _a2.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.kind) ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
              const specifier2 ***REMOVED*** getSpecifierForModuleSymbol(target.parent || target, context);
              const { propertyName } ***REMOVED*** node;
              const propertyNameText ***REMOVED*** propertyName && isIdentifier(propertyName) ? idText(propertyName) : void 0;
              context.approximateLength +***REMOVED*** 24 + localName.length + specifier2.length + ((propertyNameText ***REMOVED******REMOVED*** null ? void 0 : propertyNameText.length) ?? 0);
              addResult(
                factory.createImportDeclaration(
                  /*modifiers*/
                  void 0,
                  factory.createImportClause(
                    /*phaseModifier*/
                    void 0,
                    /*name*/
                    void 0,
                    factory.createNamedImports([factory.createImportSpecifier(
                      /*isTypeOnly*/
                      false,
                      propertyNameText ? factory.createIdentifier(propertyNameText) : void 0,
                      factory.createIdentifier(localName)
                    )])
                  ),
                  factory.createStringLiteral(specifier2),
                  /*attributes*/
                  void 0
                ),
                0 /* None */
              );
              break;
            }
            Debug.failBadSyntaxKind(((_c ***REMOVED*** node.parent) ***REMOVED******REMOVED*** null ? void 0 : _c.parent) || node, "Unhandled binding element grandparent kind in declaration serialization");
            break;
          case 305 /* ShorthandPropertyAssignment */:
            if (((_e ***REMOVED*** (_d ***REMOVED*** node.parent) ***REMOVED******REMOVED*** null ? void 0 : _d.parent) ***REMOVED******REMOVED*** null ? void 0 : _e.kind) ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
              serializeExportSpecifier(
                unescapeLeadingUnderscores(symbol.escapedName),
                targetName
              );
            }
            break;
          case 261 /* VariableDeclaration */:
            if (isPropertyAccessExpression(node.initializer)) {
              const initializer ***REMOVED*** node.initializer;
              const uniqueName ***REMOVED*** factory.createUniqueName(localName);
              const specifier2 ***REMOVED*** getSpecifierForModuleSymbol(target.parent || target, context);
              context.approximateLength +***REMOVED*** 22 + specifier2.length + idText(uniqueName).length;
              addResult(
                factory.createImportEqualsDeclaration(
                  /*modifiers*/
                  void 0,
                  /*isTypeOnly*/
                  false,
                  uniqueName,
                  factory.createExternalModuleReference(factory.createStringLiteral(specifier2))
                ),
                0 /* None */
              );
              context.approximateLength +***REMOVED*** 12 + localName.length + idText(uniqueName).length + idText(initializer.name).length;
              addResult(
                factory.createImportEqualsDeclaration(
                  /*modifiers*/
                  void 0,
                  /*isTypeOnly*/
                  false,
                  factory.createIdentifier(localName),
                  factory.createQualifiedName(uniqueName, initializer.name)
                ),
                modifierFlags
              );
              break;
            }
          // else fall through and treat commonjs require just like import***REMOVED***
          case 272 /* ImportEqualsDeclaration */:
            if (target.escapedName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */ && some(target.declarations, (d) ***REMOVED***> isSourceFile(d) && isJsonSourceFile(d))) {
              serializeMaybeAliasAssignment(symbol);
              break;
            }
            const isLocalImport ***REMOVED*** !(target.flags & 512 /* ValueModule */) && !isVariableDeclaration(node);
            context.approximateLength +***REMOVED*** 11 + localName.length + unescapeLeadingUnderscores(target.escapedName).length;
            addResult(
              factory.createImportEqualsDeclaration(
                /*modifiers*/
                void 0,
                /*isTypeOnly*/
                false,
                factory.createIdentifier(localName),
                isLocalImport ? symbolToName(
                  target,
                  context,
                  -1 /* All */,
                  /*expectsIdentifier*/
                  false
                ) : factory.createExternalModuleReference(factory.createStringLiteral(getSpecifierForModuleSymbol(target, context)))
              ),
              isLocalImport ? modifierFlags : 0 /* None */
            );
            break;
          case 271 /* NamespaceExportDeclaration */:
            addResult(factory.createNamespaceExportDeclaration(idText(node.name)), 0 /* None */);
            break;
          case 274 /* ImportClause */: {
            const generatedSpecifier ***REMOVED*** getSpecifierForModuleSymbol(target.parent || target, context);
            const specifier2 ***REMOVED*** context.bundled ? factory.createStringLiteral(generatedSpecifier) : node.parent.moduleSpecifier;
            const attributes ***REMOVED*** isImportDeclaration(node.parent) ? node.parent.attributes : void 0;
            const isTypeOnly ***REMOVED*** isJSDocImportTag(node.parent);
            context.approximateLength +***REMOVED*** 14 + localName.length + 3 + (isTypeOnly ? 4 : 0);
            addResult(
              factory.createImportDeclaration(
                /*modifiers*/
                void 0,
                factory.createImportClause(
                  /* phaseModifier */
                  isTypeOnly ? 156 /* TypeKeyword */ : void 0,
                  factory.createIdentifier(localName),
                  /*namedBindings*/
                  void 0
                ),
                specifier2,
                attributes
              ),
              0 /* None */
            );
            break;
          }
          case 275 /* NamespaceImport */: {
            const generatedSpecifier ***REMOVED*** getSpecifierForModuleSymbol(target.parent || target, context);
            const specifier2 ***REMOVED*** context.bundled ? factory.createStringLiteral(generatedSpecifier) : node.parent.parent.moduleSpecifier;
            const isTypeOnly ***REMOVED*** isJSDocImportTag(node.parent.parent);
            context.approximateLength +***REMOVED*** 19 + localName.length + 3 + (isTypeOnly ? 4 : 0);
            addResult(
              factory.createImportDeclaration(
                /*modifiers*/
                void 0,
                factory.createImportClause(
                  /* phaseModifier */
                  isTypeOnly ? 156 /* TypeKeyword */ : void 0,
                  /*name*/
                  void 0,
                  factory.createNamespaceImport(factory.createIdentifier(localName))
                ),
                specifier2,
                node.parent.attributes
              ),
              0 /* None */
            );
            break;
          }
          case 281 /* NamespaceExport */:
            context.approximateLength +***REMOVED*** 19 + localName.length + 3;
            addResult(
              factory.createExportDeclaration(
                /*modifiers*/
                void 0,
                /*isTypeOnly*/
                false,
                factory.createNamespaceExport(factory.createIdentifier(localName)),
                factory.createStringLiteral(getSpecifierForModuleSymbol(target, context))
              ),
              0 /* None */
            );
            break;
          case 277 /* ImportSpecifier */: {
            const generatedSpecifier ***REMOVED*** getSpecifierForModuleSymbol(target.parent || target, context);
            const specifier2 ***REMOVED*** context.bundled ? factory.createStringLiteral(generatedSpecifier) : node.parent.parent.parent.moduleSpecifier;
            const isTypeOnly ***REMOVED*** isJSDocImportTag(node.parent.parent.parent);
            context.approximateLength +***REMOVED*** 19 + localName.length + 3 + (isTypeOnly ? 4 : 0);
            addResult(
              factory.createImportDeclaration(
                /*modifiers*/
                void 0,
                factory.createImportClause(
                  /* phaseModifier */
                  isTypeOnly ? 156 /* TypeKeyword */ : void 0,
                  /*name*/
                  void 0,
                  factory.createNamedImports([
                    factory.createImportSpecifier(
                      /*isTypeOnly*/
                      false,
                      localName !***REMOVED******REMOVED*** verbatimTargetName ? factory.createIdentifier(verbatimTargetName) : void 0,
                      factory.createIdentifier(localName)
                    )
                  ])
                ),
                specifier2,
                node.parent.parent.parent.attributes
              ),
              0 /* None */
            );
            break;
          }
          case 282 /* ExportSpecifier */:
            const specifier ***REMOVED*** node.parent.parent.moduleSpecifier;
            if (specifier) {
              const propertyName ***REMOVED*** node.propertyName;
              if (propertyName && moduleExportNameIsDefault(propertyName)) {
                verbatimTargetName ***REMOVED*** "default" /* Default */;
              }
            }
            serializeExportSpecifier(
              unescapeLeadingUnderscores(symbol.escapedName),
              specifier ? verbatimTargetName : targetName,
              specifier && isStringLiteralLike(specifier) ? factory.createStringLiteral(specifier.text) : void 0
            );
            break;
          case 278 /* ExportAssignment */:
            serializeMaybeAliasAssignment(symbol);
            break;
          case 227 /* BinaryExpression */:
          case 212 /* PropertyAccessExpression */:
          case 213 /* ElementAccessExpression */:
            if (symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */ || symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) {
              serializeMaybeAliasAssignment(symbol);
            } else {
              serializeExportSpecifier(localName, targetName);
            }
            break;
          default:
            return Debug.failBadSyntaxKind(node, "Unhandled alias declaration kind in symbol serializer!");
        }
      }
      function serializeExportSpecifier(localName, targetName, specifier) {
        context.approximateLength +***REMOVED*** 16 + localName.length + (localName !***REMOVED******REMOVED*** targetName ? targetName.length : 0);
        addResult(
          factory.createExportDeclaration(
            /*modifiers*/
            void 0,
            /*isTypeOnly*/
            false,
            factory.createNamedExports([factory.createExportSpecifier(
              /*isTypeOnly*/
              false,
              localName !***REMOVED******REMOVED*** targetName ? targetName : void 0,
              localName
            )]),
            specifier
          ),
          0 /* None */
        );
      }
      function serializeMaybeAliasAssignment(symbol) {
        var _a2;
        if (symbol.flags & 4194304 /* Prototype */) {
          return false;
        }
        const name ***REMOVED*** unescapeLeadingUnderscores(symbol.escapedName);
        const isExportEquals ***REMOVED*** name ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */;
        const isDefault ***REMOVED*** name ***REMOVED******REMOVED******REMOVED*** "default" /* Default */;
        const isExportAssignmentCompatibleSymbolName ***REMOVED*** isExportEquals || isDefault;
        const aliasDecl ***REMOVED*** symbol.declarations && getDeclarationOfAliasSymbol(symbol);
        const target ***REMOVED*** aliasDecl && getTargetOfAliasDeclaration(
          aliasDecl,
          /*dontRecursivelyResolve*/
          true
        );
        if (target && length(target.declarations) && some(target.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(enclosingDeclaration))) {
          const expr ***REMOVED*** aliasDecl && (isExportAssignment(aliasDecl) || isBinaryExpression(aliasDecl) ? getExportAssignmentExpression(aliasDecl) : getPropertyAssignmentAliasLikeExpression(aliasDecl));
          const first2 ***REMOVED*** expr && isEntityNameExpression(expr) ? getFirstNonModuleExportsIdentifier(expr) : void 0;
          const referenced ***REMOVED*** first2 && resolveEntityName(
            first2,
            -1 /* All */,
            /*ignoreErrors*/
            true,
            /*dontResolveAlias*/
            true,
            enclosingDeclaration
          );
          if (referenced || target) {
            includePrivateSymbol(referenced || target);
          }
          const prevDisableTrackSymbol ***REMOVED*** context.tracker.disableTrackSymbol;
          context.tracker.disableTrackSymbol ***REMOVED*** true;
          if (isExportAssignmentCompatibleSymbolName) {
            context.approximateLength +***REMOVED*** 10;
            results.push(factory.createExportAssignment(
              /*modifiers*/
              void 0,
              isExportEquals,
              symbolToExpression(target, context, -1 /* All */)
            ));
          } else {
            if (first2 ***REMOVED******REMOVED******REMOVED*** expr && first2) {
              serializeExportSpecifier(name, idText(first2));
            } else if (expr && isClassExpression(expr)) {
              serializeExportSpecifier(name, getInternalSymbolName(target, symbolName(target)));
            } else {
              const varName ***REMOVED*** getUnusedName(name, symbol);
              context.approximateLength +***REMOVED*** varName.length + 10;
              addResult(
                factory.createImportEqualsDeclaration(
                  /*modifiers*/
                  void 0,
                  /*isTypeOnly*/
                  false,
                  factory.createIdentifier(varName),
                  symbolToName(
                    target,
                    context,
                    -1 /* All */,
                    /*expectsIdentifier*/
                    false
                  )
                ),
                0 /* None */
              );
              serializeExportSpecifier(name, varName);
            }
          }
          context.tracker.disableTrackSymbol ***REMOVED*** prevDisableTrackSymbol;
          return true;
        } else {
          const varName ***REMOVED*** getUnusedName(name, symbol);
          const typeToSerialize ***REMOVED*** getWidenedType(getTypeOfSymbol(getMergedSymbol(symbol)));
          if (isTypeRepresentableAsFunctionNamespaceMerge(typeToSerialize, symbol)) {
            serializeAsFunctionNamespaceMerge(typeToSerialize, symbol, varName, isExportAssignmentCompatibleSymbolName ? 0 /* None */ : 32 /* Export */);
          } else {
            const flags ***REMOVED*** ((_a2 ***REMOVED*** context.enclosingDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _a2.kind) ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && (!(symbol.flags & 98304 /* Accessor */) || symbol.flags & 65536 /* SetAccessor */) ? 1 /* Let */ : 2 /* Const */;
            context.approximateLength +***REMOVED*** varName.length + 5;
            const statement ***REMOVED*** factory.createVariableStatement(
              /*modifiers*/
              void 0,
              factory.createVariableDeclarationList([
                factory.createVariableDeclaration(
                  varName,
                  /*exclamationToken*/
                  void 0,
                  serializeTypeForDeclaration(
                    context,
                    /*declaration*/
                    void 0,
                    typeToSerialize,
                    symbol
                  )
                )
              ], flags)
            );
            addResult(
              statement,
              target && target.flags & 4 /* Property */ && target.escapedName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */ ? 128 /* Ambient */ : name ***REMOVED******REMOVED******REMOVED*** varName ? 32 /* Export */ : 0 /* None */
            );
          }
          if (isExportAssignmentCompatibleSymbolName) {
            context.approximateLength +***REMOVED*** varName.length + 10;
            results.push(factory.createExportAssignment(
              /*modifiers*/
              void 0,
              isExportEquals,
              factory.createIdentifier(varName)
            ));
            return true;
          } else if (name !***REMOVED******REMOVED*** varName) {
            serializeExportSpecifier(name, varName);
            return true;
          }
          return false;
        }
      }
      function isTypeRepresentableAsFunctionNamespaceMerge(typeToSerialize, hostSymbol) {
        var _a2;
        const ctxSrc ***REMOVED*** getSourceFileOfNode(context.enclosingDeclaration);
        return getObjectFlags(typeToSerialize) & (16 /* Anonymous */ | 32 /* Mapped */) && !some((_a2 ***REMOVED*** typeToSerialize.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a2.declarations, isTypeNode) && // If the type comes straight from a type node, we shouldn't try to break it up
        !length(getIndexInfosOfType(typeToSerialize)) && !isClassInstanceSide(typeToSerialize) && // While a class instance is potentially representable as a NS, prefer printing a reference to the instance type and serializing the class
        !!(length(filter(getPropertiesOfType(typeToSerialize), isNamespaceMember)) || length(getSignaturesOfType(typeToSerialize, 0 /* Call */))) && !length(getSignaturesOfType(typeToSerialize, 1 /* Construct */)) && // TODO: could probably serialize as function + ns + class, now that that's OK
        !getDeclarationWithTypeAnnotation(hostSymbol, enclosingDeclaration) && !(typeToSerialize.symbol && some(typeToSerialize.symbol.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) !***REMOVED******REMOVED*** ctxSrc)) && !some(getPropertiesOfType(typeToSerialize), (p) ***REMOVED***> isLateBoundName(p.escapedName)) && !some(getPropertiesOfType(typeToSerialize), (p) ***REMOVED***> some(p.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) !***REMOVED******REMOVED*** ctxSrc)) && every(getPropertiesOfType(typeToSerialize), (p) ***REMOVED***> {
          if (!isIdentifierText(symbolName(p), languageVersion)) {
            return false;
          }
          if (!(p.flags & 98304 /* Accessor */)) {
            return true;
          }
          return getNonMissingTypeOfSymbol(p) ***REMOVED******REMOVED******REMOVED*** getWriteTypeOfSymbol(p);
        });
      }
      function makeSerializePropertySymbol(createProperty2, methodKind, useAccessors) {
        return function serializePropertySymbol(p, isStatic2, baseType) {
          var _a2, _b, _c, _d, _e, _f;
          const modifierFlags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(p);
          const omitType ***REMOVED*** !!(modifierFlags & 2 /* Private */) && !isExpanding(context);
          if (isStatic2 && p.flags & (788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */)) {
            return [];
          }
          if (p.flags & 4194304 /* Prototype */ || p.escapedName ***REMOVED******REMOVED******REMOVED*** "constructor" || baseType && getPropertyOfType(baseType, p.escapedName) && isReadonlySymbol(getPropertyOfType(baseType, p.escapedName)) ***REMOVED******REMOVED******REMOVED*** isReadonlySymbol(p) && (p.flags & 16777216 /* Optional */) ***REMOVED******REMOVED******REMOVED*** (getPropertyOfType(baseType, p.escapedName).flags & 16777216 /* Optional */) && isTypeIdenticalTo(getTypeOfSymbol(p), getTypeOfPropertyOfType(baseType, p.escapedName))) {
            return [];
          }
          const flag ***REMOVED*** modifierFlags & ~1024 /* Async */ | (isStatic2 ? 256 /* Static */ : 0);
          const name ***REMOVED*** getPropertyNameNodeForSymbol(p, context);
          const firstPropertyLikeDecl ***REMOVED*** (_a2 ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a2.find(or(isPropertyDeclaration, isAccessor, isVariableDeclaration, isPropertySignature, isBinaryExpression, isPropertyAccessExpression));
          if (p.flags & 98304 /* Accessor */ && useAccessors) {
            const result ***REMOVED*** [];
            if (p.flags & 65536 /* SetAccessor */) {
              const setter ***REMOVED*** p.declarations && forEach(p.declarations, (d) ***REMOVED***> {
                if (d.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
                  return d;
                }
                if (isCallExpression(d) && isBindableObjectDefinePropertyCall(d)) {
                  return forEach(d.arguments[2].properties, (propDecl) ***REMOVED***> {
                    const id ***REMOVED*** getNameOfDeclaration(propDecl);
                    if (!!id && isIdentifier(id) && idText(id) ***REMOVED******REMOVED******REMOVED*** "set") {
                      return propDecl;
                    }
                  });
                }
              });
              Debug.assert(!!setter);
              const paramSymbol ***REMOVED*** isFunctionLikeDeclaration(setter) ? getSignatureFromDeclaration(setter).parameters[0] : void 0;
              const setterDeclaration ***REMOVED*** (_b ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _b.find(isSetAccessor);
              context.approximateLength +***REMOVED*** modifiersLength(flag) + 7 + (paramSymbol ? symbolName(paramSymbol).length : 5) + (omitType ? 0 : 2);
              result.push(setTextRange2(
                context,
                factory.createSetAccessorDeclaration(
                  factory.createModifiersFromModifierFlags(flag),
                  name,
                  [factory.createParameterDeclaration(
                    /*modifiers*/
                    void 0,
                    /*dotDotDotToken*/
                    void 0,
                    paramSymbol ? parameterToParameterDeclarationName(paramSymbol, getEffectiveParameterDeclaration(paramSymbol), context) : "value",
                    /*questionToken*/
                    void 0,
                    omitType ? void 0 : serializeTypeForDeclaration(context, setterDeclaration, getWriteTypeOfSymbol(p), p)
                  )],
                  /*body*/
                  void 0
                ),
                setterDeclaration ?? firstPropertyLikeDecl
              ));
            }
            if (p.flags & 32768 /* GetAccessor */) {
              const getterDeclaration ***REMOVED*** (_c ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _c.find(isGetAccessor);
              context.approximateLength +***REMOVED*** modifiersLength(flag) + 8 + (omitType ? 0 : 2);
              result.push(setTextRange2(
                context,
                factory.createGetAccessorDeclaration(
                  factory.createModifiersFromModifierFlags(flag),
                  name,
                  [],
                  omitType ? void 0 : serializeTypeForDeclaration(context, getterDeclaration, getTypeOfSymbol(p), p),
                  /*body*/
                  void 0
                ),
                getterDeclaration ?? firstPropertyLikeDecl
              ));
            }
            return result;
          } else if (p.flags & (4 /* Property */ | 3 /* Variable */ | 98304 /* Accessor */)) {
            const modifierFlags2 ***REMOVED*** (isReadonlySymbol(p) ? 8 /* Readonly */ : 0) | flag;
            context.approximateLength +***REMOVED*** 2 + (omitType ? 0 : 2) + modifiersLength(modifierFlags2);
            return setTextRange2(
              context,
              createProperty2(
                factory.createModifiersFromModifierFlags(modifierFlags2),
                name,
                p.flags & 16777216 /* Optional */ ? factory.createToken(58 /* QuestionToken */) : void 0,
                omitType ? void 0 : serializeTypeForDeclaration(context, (_d ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _d.find(isSetAccessorDeclaration), getWriteTypeOfSymbol(p), p),
                // TODO: https://github.com/microsoft/TypeScript/pull/32372#discussion_r328386357
                // interface members can't have initializers, however class members _can_
                /*initializer*/
                void 0
              ),
              ((_e ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _e.find(or(isPropertyDeclaration, isVariableDeclaration))) || firstPropertyLikeDecl
            );
          }
          if (p.flags & (8192 /* Method */ | 16 /* Function */)) {
            const type ***REMOVED*** getTypeOfSymbol(p);
            const signatures ***REMOVED*** getSignaturesOfType(type, 0 /* Call */);
            if (omitType) {
              const modifierFlags2 ***REMOVED*** (isReadonlySymbol(p) ? 8 /* Readonly */ : 0) | flag;
              context.approximateLength +***REMOVED*** 1 + modifiersLength(modifierFlags2);
              return setTextRange2(
                context,
                createProperty2(
                  factory.createModifiersFromModifierFlags(modifierFlags2),
                  name,
                  p.flags & 16777216 /* Optional */ ? factory.createToken(58 /* QuestionToken */) : void 0,
                  /*type*/
                  void 0,
                  /*initializer*/
                  void 0
                ),
                ((_f ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _f.find(isFunctionLikeDeclaration)) || signatures[0] && signatures[0].declaration || p.declarations && p.declarations[0]
              );
            }
            const results2 ***REMOVED*** [];
            for (const sig of signatures) {
              context.approximateLength +***REMOVED*** 1;
              const decl ***REMOVED*** signatureToSignatureDeclarationHelper(
                sig,
                methodKind,
                context,
                {
                  name,
                  questionToken: p.flags & 16777216 /* Optional */ ? factory.createToken(58 /* QuestionToken */) : void 0,
                  modifiers: flag ? factory.createModifiersFromModifierFlags(flag) : void 0
                }
              );
              const location ***REMOVED*** sig.declaration && isPrototypePropertyAssignment(sig.declaration.parent) ? sig.declaration.parent : sig.declaration;
              results2.push(setTextRange2(context, decl, location));
            }
            return results2;
          }
          return Debug.fail(`Unhandled class member kind! ${p.__debugFlags || p.flags}`);
        };
      }
      function modifiersLength(flags) {
        let result ***REMOVED*** 0;
        if (flags & 32 /* Export */) result +***REMOVED*** 7;
        if (flags & 128 /* Ambient */) result +***REMOVED*** 8;
        if (flags & 2048 /* Default */) result +***REMOVED*** 8;
        if (flags & 4096 /* Const */) result +***REMOVED*** 6;
        if (flags & 1 /* Public */) result +***REMOVED*** 7;
        if (flags & 2 /* Private */) result +***REMOVED*** 8;
        if (flags & 4 /* Protected */) result +***REMOVED*** 10;
        if (flags & 64 /* Abstract */) result +***REMOVED*** 9;
        if (flags & 256 /* Static */) result +***REMOVED*** 7;
        if (flags & 16 /* Override */) result +***REMOVED*** 9;
        if (flags & 8 /* Readonly */) result +***REMOVED*** 9;
        if (flags & 512 /* Accessor */) result +***REMOVED*** 9;
        if (flags & 1024 /* Async */) result +***REMOVED*** 6;
        if (flags & 8192 /* In */) result +***REMOVED*** 3;
        if (flags & 16384 /* Out */) result +***REMOVED*** 4;
        return result;
      }
      function serializePropertySymbolForInterface(p, baseType) {
        return serializePropertySymbolForInterfaceWorker(
          p,
          /*isStatic*/
          false,
          baseType
        );
      }
      function serializeSignatures(kind, input, baseType, outputKind) {
        const signatures ***REMOVED*** getSignaturesOfType(input, kind);
        if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */) {
          if (!baseType && every(signatures, (s) ***REMOVED***> length(s.parameters) ***REMOVED******REMOVED******REMOVED*** 0)) {
            return [];
          }
          if (baseType) {
            const baseSigs ***REMOVED*** getSignaturesOfType(baseType, 1 /* Construct */);
            if (!length(baseSigs) && every(signatures, (s) ***REMOVED***> length(s.parameters) ***REMOVED******REMOVED******REMOVED*** 0)) {
              return [];
            }
            if (baseSigs.length ***REMOVED******REMOVED******REMOVED*** signatures.length) {
              let failed2 ***REMOVED*** false;
              for (let i ***REMOVED*** 0; i < baseSigs.length; i++) {
                if (!compareSignaturesIdentical(
                  signatures[i],
                  baseSigs[i],
                  /*partialMatch*/
                  false,
                  /*ignoreThisTypes*/
                  false,
                  /*ignoreReturnTypes*/
                  true,
                  compareTypesIdentical
                )) {
                  failed2 ***REMOVED*** true;
                  break;
                }
              }
              if (!failed2) {
                return [];
              }
            }
          }
          let privateProtected ***REMOVED*** 0;
          for (const s of signatures) {
            if (s.declaration) {
              privateProtected |***REMOVED*** getSelectedEffectiveModifierFlags(s.declaration, 2 /* Private */ | 4 /* Protected */);
            }
          }
          if (privateProtected) {
            return [setTextRange2(
              context,
              factory.createConstructorDeclaration(
                factory.createModifiersFromModifierFlags(privateProtected),
                /*parameters*/
                [],
                /*body*/
                void 0
              ),
              signatures[0].declaration
            )];
          }
        }
        const results2 ***REMOVED*** [];
        for (const sig of signatures) {
          context.approximateLength +***REMOVED*** 1;
          const decl ***REMOVED*** signatureToSignatureDeclarationHelper(sig, outputKind, context);
          results2.push(setTextRange2(context, decl, sig.declaration));
        }
        return results2;
      }
      function serializeIndexSignatures(input, baseType) {
        const results2 ***REMOVED*** [];
        for (const info of getIndexInfosOfType(input)) {
          if (baseType) {
            const baseInfo ***REMOVED*** getIndexInfoOfType(baseType, info.keyType);
            if (baseInfo) {
              if (isTypeIdenticalTo(info.type, baseInfo.type)) {
                continue;
              }
            }
          }
          results2.push(indexInfoToIndexSignatureDeclarationHelper(
            info,
            context,
            /*typeNode*/
            void 0
          ));
        }
        return results2;
      }
      function serializeBaseType(t, staticType, rootName) {
        const ref ***REMOVED*** trySerializeAsTypeReference(t, 111551 /* Value */);
        if (ref) {
          return ref;
        }
        const tempName ***REMOVED*** getUnusedName(`${rootName}_base`);
        const statement ***REMOVED*** factory.createVariableStatement(
          /*modifiers*/
          void 0,
          factory.createVariableDeclarationList([
            factory.createVariableDeclaration(
              tempName,
              /*exclamationToken*/
              void 0,
              typeToTypeNodeHelper(staticType, context)
            )
          ], 2 /* Const */)
        );
        addResult(statement, 0 /* None */);
        return factory.createExpressionWithTypeArguments(
          factory.createIdentifier(tempName),
          /*typeArguments*/
          void 0
        );
      }
      function trySerializeAsTypeReference(t, flags) {
        let typeArgs;
        let reference;
        if (t.target && isSymbolAccessibleByFlags(t.target.symbol, enclosingDeclaration, flags)) {
          typeArgs ***REMOVED*** map(getTypeArguments(t), (t2) ***REMOVED***> typeToTypeNodeHelper(t2, context));
          reference ***REMOVED*** symbolToExpression(t.target.symbol, context, 788968 /* Type */);
        } else if (t.symbol && isSymbolAccessibleByFlags(t.symbol, enclosingDeclaration, flags)) {
          reference ***REMOVED*** symbolToExpression(t.symbol, context, 788968 /* Type */);
        }
        if (reference) {
          return factory.createExpressionWithTypeArguments(reference, typeArgs);
        }
      }
      function serializeImplementedType(t) {
        const ref ***REMOVED*** trySerializeAsTypeReference(t, 788968 /* Type */);
        if (ref) {
          return ref;
        }
        if (t.symbol) {
          return factory.createExpressionWithTypeArguments(
            symbolToExpression(t.symbol, context, 788968 /* Type */),
            /*typeArguments*/
            void 0
          );
        }
      }
      function getUnusedName(input, symbol) {
        var _a2, _b;
        const id ***REMOVED*** symbol ? getSymbolId(symbol) : void 0;
        if (id) {
          if (context.remappedSymbolNames.has(id)) {
            return context.remappedSymbolNames.get(id);
          }
        }
        if (symbol) {
          input ***REMOVED*** getNameCandidateWorker(symbol, input);
        }
        let i ***REMOVED*** 0;
        const original ***REMOVED*** input;
        while ((_a2 ***REMOVED*** context.usedSymbolNames) ***REMOVED******REMOVED*** null ? void 0 : _a2.has(input)) {
          i++;
          input ***REMOVED*** `${original}_${i}`;
        }
        (_b ***REMOVED*** context.usedSymbolNames) ***REMOVED******REMOVED*** null ? void 0 : _b.add(input);
        if (id) {
          context.remappedSymbolNames.set(id, input);
        }
        return input;
      }
      function getNameCandidateWorker(symbol, localName) {
        if (localName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */ || localName ***REMOVED******REMOVED******REMOVED*** "__class" /* Class */ || localName ***REMOVED******REMOVED******REMOVED*** "__function" /* Function */) {
          const restoreFlags ***REMOVED*** saveRestoreFlags(context);
          context.flags |***REMOVED*** 16777216 /* InInitialEntityName */;
          const nameCandidate ***REMOVED*** getNameOfSymbolAsWritten(symbol, context);
          restoreFlags();
          localName ***REMOVED*** nameCandidate.length > 0 && isSingleOrDoubleQuote(nameCandidate.charCodeAt(0)) ? stripQuotes(nameCandidate) : nameCandidate;
        }
        if (localName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */) {
          localName ***REMOVED*** "_default";
        } else if (localName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) {
          localName ***REMOVED*** "_exports";
        }
        localName ***REMOVED*** isIdentifierText(localName, languageVersion) && !isStringANonContextualKeyword(localName) ? localName : "_" + localName.replace(/[^a-z0-9]/gi, "_");
        return localName;
      }
      function getInternalSymbolName(symbol, localName) {
        const id ***REMOVED*** getSymbolId(symbol);
        if (context.remappedSymbolNames.has(id)) {
          return context.remappedSymbolNames.get(id);
        }
        localName ***REMOVED*** getNameCandidateWorker(symbol, localName);
        context.remappedSymbolNames.set(id, localName);
        return localName;
      }
    }
    function isExpanding(context) {
      return context.maxExpansionDepth !***REMOVED******REMOVED*** -1;
    }
    function isHashPrivate(s) {
      return !!s.valueDeclaration && isNamedDeclaration(s.valueDeclaration) && isPrivateIdentifier(s.valueDeclaration.name);
    }
    function getClonedHashPrivateName(s) {
      if (s.valueDeclaration && isNamedDeclaration(s.valueDeclaration) && isPrivateIdentifier(s.valueDeclaration.name)) {
        return factory.cloneNode(s.valueDeclaration.name);
      }
      return void 0;
    }
  }
  function isLibType(type) {
    var _a;
    const symbol ***REMOVED*** (getObjectFlags(type) & 4 /* Reference */) !***REMOVED******REMOVED*** 0 ? type.target.symbol : type.symbol;
    return isTupleType(type) || !!((_a ***REMOVED*** symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.some((decl) ***REMOVED***> host.isSourceFileDefaultLibrary(getSourceFileOfNode(decl))));
  }
  function typePredicateToString(typePredicate, enclosingDeclaration, flags ***REMOVED*** 16384 /* UseAliasDefinedOutsideCurrentScope */, writer) {
    return writer ? typePredicateToStringWorker(writer).getText() : usingSingleLineStringWriter(typePredicateToStringWorker);
    function typePredicateToStringWorker(writer2) {
      const nodeBuilderFlags ***REMOVED*** toNodeBuilderFlags(flags) | 70221824 /* IgnoreErrors */ | 512 /* WriteTypeParametersInQualifiedName */;
      const predicate ***REMOVED*** nodeBuilder.typePredicateToTypePredicateNode(typePredicate, enclosingDeclaration, nodeBuilderFlags);
      const printer ***REMOVED*** createPrinterWithRemoveComments();
      const sourceFile ***REMOVED*** enclosingDeclaration && getSourceFileOfNode(enclosingDeclaration);
      printer.writeNode(
        4 /* Unspecified */,
        predicate,
        /*sourceFile*/
        sourceFile,
        writer2
      );
      return writer2;
    }
  }
  function formatUnionTypes(types, expandingEnum) {
    const result ***REMOVED*** [];
    let flags ***REMOVED*** 0;
    for (let i ***REMOVED*** 0; i < types.length; i++) {
      const t ***REMOVED*** types[i];
      flags |***REMOVED*** t.flags;
      if (!(t.flags & 98304 /* Nullable */)) {
        if (t.flags & 512 /* BooleanLiteral */ || !expandingEnum && t.flags | 1056 /* EnumLike */) {
          const baseType ***REMOVED*** t.flags & 512 /* BooleanLiteral */ ? booleanType : getBaseTypeOfEnumLikeType(t);
          if (baseType.flags & 1048576 /* Union */) {
            const count ***REMOVED*** baseType.types.length;
            if (i + count <***REMOVED*** types.length && getRegularTypeOfLiteralType(types[i + count - 1]) ***REMOVED******REMOVED******REMOVED*** getRegularTypeOfLiteralType(baseType.types[count - 1])) {
              result.push(baseType);
              i +***REMOVED*** count - 1;
              continue;
            }
          }
        }
        result.push(t);
      }
    }
    if (flags & 65536 /* Null */) result.push(nullType);
    if (flags & 32768 /* Undefined */) result.push(undefinedType);
    return result || types;
  }
  function visibilityToString(flags) {
    if (flags ***REMOVED******REMOVED******REMOVED*** 2 /* Private */) {
      return "private";
    }
    if (flags ***REMOVED******REMOVED******REMOVED*** 4 /* Protected */) {
      return "protected";
    }
    return "public";
  }
  function getTypeAliasForTypeLiteral(type) {
    if (type.symbol && type.symbol.flags & 2048 /* TypeLiteral */ && type.symbol.declarations) {
      const node ***REMOVED*** walkUpParenthesizedTypes(type.symbol.declarations[0].parent);
      if (isTypeAliasDeclaration(node)) {
        return getSymbolOfDeclaration(node);
      }
    }
    return void 0;
  }
  function isTopLevelInExternalModuleAugmentation(node) {
    return node && node.parent && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ && isExternalModuleAugmentation(node.parent.parent);
  }
  function isDefaultBindingContext(location) {
    return location.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || isAmbientModule(location);
  }
  function getNameOfSymbolFromNameType(symbol, context) {
    const nameType ***REMOVED*** getSymbolLinks(symbol).nameType;
    if (nameType) {
      if (nameType.flags & 384 /* StringOrNumberLiteral */) {
        const name ***REMOVED*** "" + nameType.value;
        if (!isIdentifierText(name, getEmitScriptTarget(compilerOptions)) && !isNumericLiteralName(name)) {
          return `"${escapeString(name, 34 /* doubleQuote */)}"`;
        }
        if (isNumericLiteralName(name) && startsWith(name, "-")) {
          return `[${name}]`;
        }
        return name;
      }
      if (nameType.flags & 8192 /* UniqueESSymbol */) {
        return `[${getNameOfSymbolAsWritten(nameType.symbol, context)}]`;
      }
    }
  }
  function getNameOfSymbolAsWritten(symbol, context) {
    var _a;
    if ((_a ***REMOVED*** context ***REMOVED******REMOVED*** null ? void 0 : context.remappedSymbolReferences) ***REMOVED******REMOVED*** null ? void 0 : _a.has(getSymbolId(symbol))) {
      symbol ***REMOVED*** context.remappedSymbolReferences.get(getSymbolId(symbol));
    }
    if (context && symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */ && !(context.flags & 16384 /* UseAliasDefinedOutsideCurrentScope */) && // If it's not the first part of an entity name, it must print as `default`
    (!(context.flags & 16777216 /* InInitialEntityName */) || // if the symbol is synthesized, it will only be referenced externally it must print as `default`
    !symbol.declarations || // if not in the same binding context (source file, module declaration), it must print as `default`
    context.enclosingDeclaration && findAncestor(symbol.declarations[0], isDefaultBindingContext) !***REMOVED******REMOVED*** findAncestor(context.enclosingDeclaration, isDefaultBindingContext))) {
      return "default";
    }
    if (symbol.declarations && symbol.declarations.length) {
      let declaration ***REMOVED*** firstDefined(symbol.declarations, (d) ***REMOVED***> getNameOfDeclaration(d) ? d : void 0);
      const name2 ***REMOVED*** declaration && getNameOfDeclaration(declaration);
      if (declaration && name2) {
        if (isCallExpression(declaration) && isBindableObjectDefinePropertyCall(declaration)) {
          return symbolName(symbol);
        }
        if (isComputedPropertyName(name2) && !(getCheckFlags(symbol) & 4096 /* Late */)) {
          const nameType ***REMOVED*** getSymbolLinks(symbol).nameType;
          if (nameType && nameType.flags & 384 /* StringOrNumberLiteral */) {
            const result ***REMOVED*** getNameOfSymbolFromNameType(symbol, context);
            if (result !***REMOVED******REMOVED*** void 0) {
              return result;
            }
          }
        }
        return declarationNameToString(name2);
      }
      if (!declaration) {
        declaration ***REMOVED*** symbol.declarations[0];
      }
      if (declaration.parent && declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
        return declarationNameToString(declaration.parent.name);
      }
      switch (declaration.kind) {
        case 232 /* ClassExpression */:
        case 219 /* FunctionExpression */:
        case 220 /* ArrowFunction */:
          if (context && !context.encounteredError && !(context.flags & 131072 /* AllowAnonymousIdentifier */)) {
            context.encounteredError ***REMOVED*** true;
          }
          return declaration.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ ? "(Anonymous class)" : "(Anonymous function)";
      }
    }
    const name ***REMOVED*** getNameOfSymbolFromNameType(symbol, context);
    return name !***REMOVED******REMOVED*** void 0 ? name : symbolName(symbol);
  }
  function isDeclarationVisible(node) {
    if (node) {
      const links ***REMOVED*** getNodeLinks(node);
      if (links.isVisible ***REMOVED******REMOVED******REMOVED*** void 0) {
        links.isVisible ***REMOVED*** !!determineIfDeclarationIsVisible();
      }
      return links.isVisible;
    }
    return false;
    function determineIfDeclarationIsVisible() {
      switch (node.kind) {
        case 339 /* JSDocCallbackTag */:
        case 347 /* JSDocTypedefTag */:
        case 341 /* JSDocEnumTag */:
          return !!(node.parent && node.parent.parent && node.parent.parent.parent && isSourceFile(node.parent.parent.parent));
        case 209 /* BindingElement */:
          return isDeclarationVisible(node.parent.parent);
        case 261 /* VariableDeclaration */:
          if (isBindingPattern(node.name) && !node.name.elements.length) {
            return false;
          }
        // falls through
        case 268 /* ModuleDeclaration */:
        case 264 /* ClassDeclaration */:
        case 265 /* InterfaceDeclaration */:
        case 266 /* TypeAliasDeclaration */:
        case 263 /* FunctionDeclaration */:
        case 267 /* EnumDeclaration */:
        case 272 /* ImportEqualsDeclaration */:
          if (isExternalModuleAugmentation(node)) {
            return true;
          }
          const parent ***REMOVED*** getDeclarationContainer(node);
          if (!(getCombinedModifierFlagsCached(node) & 32 /* Export */) && !(node.kind !***REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && parent.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ && parent.flags & 33554432 /* Ambient */)) {
            return isGlobalSourceFile(parent);
          }
          return isDeclarationVisible(parent);
        case 173 /* PropertyDeclaration */:
        case 172 /* PropertySignature */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
        case 175 /* MethodDeclaration */:
        case 174 /* MethodSignature */:
          if (hasEffectiveModifier(node, 2 /* Private */ | 4 /* Protected */)) {
            return false;
          }
        // Public properties/methods are visible if its parents are visible, so:
        // falls through
        case 177 /* Constructor */:
        case 181 /* ConstructSignature */:
        case 180 /* CallSignature */:
        case 182 /* IndexSignature */:
        case 170 /* Parameter */:
        case 269 /* ModuleBlock */:
        case 185 /* FunctionType */:
        case 186 /* ConstructorType */:
        case 188 /* TypeLiteral */:
        case 184 /* TypeReference */:
        case 189 /* ArrayType */:
        case 190 /* TupleType */:
        case 193 /* UnionType */:
        case 194 /* IntersectionType */:
        case 197 /* ParenthesizedType */:
        case 203 /* NamedTupleMember */:
          return isDeclarationVisible(node.parent);
        // Default binding, import specifier and namespace import is visible
        // only on demand so by default it is not visible
        case 274 /* ImportClause */:
        case 275 /* NamespaceImport */:
        case 277 /* ImportSpecifier */:
          return false;
        // Type parameters are always visible
        case 169 /* TypeParameter */:
        // Source file and namespace export are always visible
        // falls through
        case 308 /* SourceFile */:
        case 271 /* NamespaceExportDeclaration */:
          return true;
        // Export assignments do not create name bindings outside the module
        case 278 /* ExportAssignment */:
          return false;
        default:
          return false;
      }
    }
  }
  function collectLinkedAliases(node, setVisibility) {
    let exportSymbol;
    if (node.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */ && node.parent && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */) {
      exportSymbol ***REMOVED*** resolveName(
        node,
        node,
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      );
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */) {
      exportSymbol ***REMOVED*** getTargetOfExportSpecifier(node.parent, 111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */);
    }
    let result;
    let visited;
    if (exportSymbol) {
      visited ***REMOVED*** /* @__PURE__ */ new Set();
      visited.add(getSymbolId(exportSymbol));
      buildVisibleNodeList(exportSymbol.declarations);
    }
    return result;
    function buildVisibleNodeList(declarations) {
      forEach(declarations, (declaration) ***REMOVED***> {
        const resultNode ***REMOVED*** getAnyImportSyntax(declaration) || declaration;
        if (setVisibility) {
          getNodeLinks(declaration).isVisible ***REMOVED*** true;
        } else {
          result ***REMOVED*** result || [];
          pushIfUnique(result, resultNode);
        }
        if (isInternalModuleImportEqualsDeclaration(declaration)) {
          const internalModuleReference ***REMOVED*** declaration.moduleReference;
          const firstIdentifier ***REMOVED*** getFirstIdentifier(internalModuleReference);
          const importSymbol ***REMOVED*** resolveName(
            declaration,
            firstIdentifier.escapedText,
            111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */,
            /*nameNotFoundMessage*/
            void 0,
            /*isUse*/
            false
          );
          if (importSymbol && visited) {
            if (tryAddToSet(visited, getSymbolId(importSymbol))) {
              buildVisibleNodeList(importSymbol.declarations);
            }
          }
        }
      });
    }
  }
  function pushTypeResolution(target, propertyName) {
    const resolutionCycleStartIndex ***REMOVED*** findResolutionCycleStartIndex(target, propertyName);
    if (resolutionCycleStartIndex >***REMOVED*** 0) {
      const { length: length2 } ***REMOVED*** resolutionTargets;
      for (let i ***REMOVED*** resolutionCycleStartIndex; i < length2; i++) {
        resolutionResults[i] ***REMOVED*** false;
      }
      return false;
    }
    resolutionTargets.push(target);
    resolutionResults.push(
      /*items*/
      true
    );
    resolutionPropertyNames.push(propertyName);
    return true;
  }
  function findResolutionCycleStartIndex(target, propertyName) {
    for (let i ***REMOVED*** resolutionTargets.length - 1; i >***REMOVED*** resolutionStart; i--) {
      if (resolutionTargetHasProperty(resolutionTargets[i], resolutionPropertyNames[i])) {
        return -1;
      }
      if (resolutionTargets[i] ***REMOVED******REMOVED******REMOVED*** target && resolutionPropertyNames[i] ***REMOVED******REMOVED******REMOVED*** propertyName) {
        return i;
      }
    }
    return -1;
  }
  function resolutionTargetHasProperty(target, propertyName) {
    switch (propertyName) {
      case 0 /* Type */:
        return !!getSymbolLinks(target).type;
      case 2 /* DeclaredType */:
        return !!getSymbolLinks(target).declaredType;
      case 1 /* ResolvedBaseConstructorType */:
        return !!target.resolvedBaseConstructorType;
      case 3 /* ResolvedReturnType */:
        return !!target.resolvedReturnType;
      case 4 /* ImmediateBaseConstraint */:
        return !!target.immediateBaseConstraint;
      case 5 /* ResolvedTypeArguments */:
        return !!target.resolvedTypeArguments;
      case 6 /* ResolvedBaseTypes */:
        return !!target.baseTypesResolved;
      case 7 /* WriteType */:
        return !!getSymbolLinks(target).writeType;
      case 8 /* ParameterInitializerContainsUndefined */:
        return getNodeLinks(target).parameterInitializerContainsUndefined !***REMOVED******REMOVED*** void 0;
    }
    return Debug.assertNever(propertyName);
  }
  function popTypeResolution() {
    resolutionTargets.pop();
    resolutionPropertyNames.pop();
    return resolutionResults.pop();
  }
  function getDeclarationContainer(node) {
    return findAncestor(getRootDeclaration(node), (node2) ***REMOVED***> {
      switch (node2.kind) {
        case 261 /* VariableDeclaration */:
        case 262 /* VariableDeclarationList */:
        case 277 /* ImportSpecifier */:
        case 276 /* NamedImports */:
        case 275 /* NamespaceImport */:
        case 274 /* ImportClause */:
          return false;
        default:
          return true;
      }
    }).parent;
  }
  function getTypeOfPrototypeProperty(prototype) {
    const classType ***REMOVED*** getDeclaredTypeOfSymbol(getParentOfSymbol(prototype));
    return classType.typeParameters ? createTypeReference(classType, map(classType.typeParameters, (_) ***REMOVED***> anyType)) : classType;
  }
  function getTypeOfPropertyOfType(type, name) {
    const prop ***REMOVED*** getPropertyOfType(type, name);
    return prop ? getTypeOfSymbol(prop) : void 0;
  }
  function getTypeOfPropertyOrIndexSignatureOfType(type, name) {
    var _a;
    let propType;
    return getTypeOfPropertyOfType(type, name) || (propType ***REMOVED*** (_a ***REMOVED*** getApplicableIndexInfoForName(type, name)) ***REMOVED******REMOVED*** null ? void 0 : _a.type) && addOptionality(
      propType,
      /*isProperty*/
      true,
      /*isOptional*/
      true
    );
  }
  function isTypeAny(type) {
    return type && (type.flags & 1 /* Any */) !***REMOVED******REMOVED*** 0;
  }
  function isErrorType(type) {
    return type ***REMOVED******REMOVED******REMOVED*** errorType || !!(type.flags & 1 /* Any */ && type.aliasSymbol);
  }
  function getTypeForBindingElementParent(node, checkMode) {
    if (checkMode !***REMOVED******REMOVED*** 0 /* Normal */) {
      return getTypeForVariableLikeDeclaration(
        node,
        /*includeOptionality*/
        false,
        checkMode
      );
    }
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    return symbol && getSymbolLinks(symbol).type || getTypeForVariableLikeDeclaration(
      node,
      /*includeOptionality*/
      false,
      checkMode
    );
  }
  function getRestType(source, properties, symbol) {
    source ***REMOVED*** filterType(source, (t) ***REMOVED***> !(t.flags & 98304 /* Nullable */));
    if (source.flags & 131072 /* Never */) {
      return emptyObjectType;
    }
    if (source.flags & 1048576 /* Union */) {
      return mapType(source, (t) ***REMOVED***> getRestType(t, properties, symbol));
    }
    let omitKeyType ***REMOVED*** getUnionType(map(properties, getLiteralTypeFromPropertyName));
    const spreadableProperties ***REMOVED*** [];
    const unspreadableToRestKeys ***REMOVED*** [];
    for (const prop of getPropertiesOfType(source)) {
      const literalTypeFromProperty ***REMOVED*** getLiteralTypeFromProperty(prop, 8576 /* StringOrNumberLiteralOrUnique */);
      if (!isTypeAssignableTo(literalTypeFromProperty, omitKeyType) && !(getDeclarationModifierFlagsFromSymbol(prop) & (2 /* Private */ | 4 /* Protected */)) && isSpreadableProperty(prop)) {
        spreadableProperties.push(prop);
      } else {
        unspreadableToRestKeys.push(literalTypeFromProperty);
      }
    }
    if (isGenericObjectType(source) || isGenericIndexType(omitKeyType)) {
      if (unspreadableToRestKeys.length) {
        omitKeyType ***REMOVED*** getUnionType([omitKeyType, ...unspreadableToRestKeys]);
      }
      if (omitKeyType.flags & 131072 /* Never */) {
        return source;
      }
      const omitTypeAlias ***REMOVED*** getGlobalOmitSymbol();
      if (!omitTypeAlias) {
        return errorType;
      }
      return getTypeAliasInstantiation(omitTypeAlias, [source, omitKeyType]);
    }
    const members ***REMOVED*** createSymbolTable();
    for (const prop of spreadableProperties) {
      members.set(prop.escapedName, getSpreadSymbol(
        prop,
        /*readonly*/
        false
      ));
    }
    const result ***REMOVED*** createAnonymousType(symbol, members, emptyArray, emptyArray, getIndexInfosOfType(source));
    result.objectFlags |***REMOVED*** 4194304 /* ObjectRestType */;
    return result;
  }
  function isGenericTypeWithUndefinedConstraint(type) {
    return !!(type.flags & 465829888 /* Instantiable */) && maybeTypeOfKind(getBaseConstraintOfType(type) || unknownType, 32768 /* Undefined */);
  }
  function getNonUndefinedType(type) {
    const typeOrConstraint ***REMOVED*** someType(type, isGenericTypeWithUndefinedConstraint) ? mapType(type, (t) ***REMOVED***> t.flags & 465829888 /* Instantiable */ ? getBaseConstraintOrType(t) : t) : type;
    return getTypeWithFacts(typeOrConstraint, 524288 /* NEUndefined */);
  }
  function getFlowTypeOfDestructuring(node, declaredType) {
    const reference ***REMOVED*** getSyntheticElementAccess(node);
    return reference ? getFlowTypeOfReference(reference, declaredType) : declaredType;
  }
  function getSyntheticElementAccess(node) {
    const parentAccess ***REMOVED*** getParentElementAccess(node);
    if (parentAccess && canHaveFlowNode(parentAccess) && parentAccess.flowNode) {
      const propName ***REMOVED*** getDestructuringPropertyName(node);
      if (propName) {
        const literal ***REMOVED*** setTextRange(parseNodeFactory.createStringLiteral(propName), node);
        const lhsExpr ***REMOVED*** isLeftHandSideExpression(parentAccess) ? parentAccess : parseNodeFactory.createParenthesizedExpression(parentAccess);
        const result ***REMOVED*** setTextRange(parseNodeFactory.createElementAccessExpression(lhsExpr, literal), node);
        setParent(literal, result);
        setParent(result, node);
        if (lhsExpr !***REMOVED******REMOVED*** parentAccess) {
          setParent(lhsExpr, result);
        }
        result.flowNode ***REMOVED*** parentAccess.flowNode;
        return result;
      }
    }
  }
  function getParentElementAccess(node) {
    const ancestor ***REMOVED*** node.parent.parent;
    switch (ancestor.kind) {
      case 209 /* BindingElement */:
      case 304 /* PropertyAssignment */:
        return getSyntheticElementAccess(ancestor);
      case 210 /* ArrayLiteralExpression */:
        return getSyntheticElementAccess(node.parent);
      case 261 /* VariableDeclaration */:
        return ancestor.initializer;
      case 227 /* BinaryExpression */:
        return ancestor.right;
    }
  }
  function getDestructuringPropertyName(node) {
    const parent ***REMOVED*** node.parent;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && parent.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */) {
      return getLiteralPropertyNameText(node.propertyName || node.name);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || node.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
      return getLiteralPropertyNameText(node.name);
    }
    return "" + parent.elements.indexOf(node);
  }
  function getLiteralPropertyNameText(name) {
    const type ***REMOVED*** getLiteralTypeFromPropertyName(name);
    return type.flags & (128 /* StringLiteral */ | 256 /* NumberLiteral */) ? "" + type.value : void 0;
  }
  function getTypeForBindingElement(declaration) {
    const checkMode ***REMOVED*** declaration.dotDotDotToken ? 32 /* RestBindingElement */ : 0 /* Normal */;
    const parentType ***REMOVED*** getTypeForBindingElementParent(declaration.parent.parent, checkMode);
    return parentType && getBindingElementTypeFromParentType(
      declaration,
      parentType,
      /*noTupleBoundsCheck*/
      false
    );
  }
  function getBindingElementTypeFromParentType(declaration, parentType, noTupleBoundsCheck) {
    if (isTypeAny(parentType)) {
      return parentType;
    }
    const pattern ***REMOVED*** declaration.parent;
    if (strictNullChecks && declaration.flags & 33554432 /* Ambient */ && isPartOfParameterDeclaration(declaration)) {
      parentType ***REMOVED*** getNonNullableType(parentType);
    } else if (strictNullChecks && pattern.parent.initializer && !hasTypeFacts(getTypeOfInitializer(pattern.parent.initializer), 65536 /* EQUndefined */)) {
      parentType ***REMOVED*** getTypeWithFacts(parentType, 524288 /* NEUndefined */);
    }
    const accessFlags ***REMOVED*** 32 /* ExpressionPosition */ | (noTupleBoundsCheck || hasDefaultValue(declaration) ? 16 /* AllowMissing */ : 0);
    let type;
    if (pattern.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */) {
      if (declaration.dotDotDotToken) {
        parentType ***REMOVED*** getReducedType(parentType);
        if (parentType.flags & 2 /* Unknown */ || !isValidSpreadType(parentType)) {
          error(declaration, Diagnostics.Rest_types_may_only_be_created_from_object_types);
          return errorType;
        }
        const literalMembers ***REMOVED*** [];
        for (const element of pattern.elements) {
          if (!element.dotDotDotToken) {
            literalMembers.push(element.propertyName || element.name);
          }
        }
        type ***REMOVED*** getRestType(parentType, literalMembers, declaration.symbol);
      } else {
        const name ***REMOVED*** declaration.propertyName || declaration.name;
        const indexType ***REMOVED*** getLiteralTypeFromPropertyName(name);
        const declaredType ***REMOVED*** getIndexedAccessType(parentType, indexType, accessFlags, name);
        type ***REMOVED*** getFlowTypeOfDestructuring(declaration, declaredType);
      }
    } else {
      const elementType ***REMOVED*** checkIteratedTypeOrElementType(65 /* Destructuring */ | (declaration.dotDotDotToken ? 0 : 128 /* PossiblyOutOfBounds */), parentType, undefinedType, pattern);
      const index ***REMOVED*** pattern.elements.indexOf(declaration);
      if (declaration.dotDotDotToken) {
        const baseConstraint ***REMOVED*** mapType(parentType, (t) ***REMOVED***> t.flags & 58982400 /* InstantiableNonPrimitive */ ? getBaseConstraintOrType(t) : t);
        type ***REMOVED*** everyType(baseConstraint, isTupleType) ? mapType(baseConstraint, (t) ***REMOVED***> sliceTupleType(t, index)) : createArrayType(elementType);
      } else if (isArrayLikeType(parentType)) {
        const indexType ***REMOVED*** getNumberLiteralType(index);
        const declaredType ***REMOVED*** getIndexedAccessTypeOrUndefined(parentType, indexType, accessFlags, declaration.name) || errorType;
        type ***REMOVED*** getFlowTypeOfDestructuring(declaration, declaredType);
      } else {
        type ***REMOVED*** elementType;
      }
    }
    if (!declaration.initializer) {
      return type;
    }
    if (getEffectiveTypeAnnotationNode(walkUpBindingElementsAndPatterns(declaration))) {
      return strictNullChecks && !hasTypeFacts(checkDeclarationInitializer(declaration, 0 /* Normal */), 16777216 /* IsUndefined */) ? getNonUndefinedType(type) : type;
    }
    return widenTypeInferredFromInitializer(declaration, getUnionType([getNonUndefinedType(type), checkDeclarationInitializer(declaration, 0 /* Normal */)], 2 /* Subtype */));
  }
  function getTypeForDeclarationFromJSDocComment(declaration) {
    const jsdocType ***REMOVED*** getJSDocType(declaration);
    if (jsdocType) {
      return getTypeFromTypeNode(jsdocType);
    }
    return void 0;
  }
  function isNullOrUndefined2(node) {
    const expr ***REMOVED*** skipParentheses(
      node,
      /*excludeJSDocTypeAssertions*/
      true
    );
    return expr.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */ || expr.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && getResolvedSymbol(expr) ***REMOVED******REMOVED******REMOVED*** undefinedSymbol;
  }
  function isEmptyArrayLiteral2(node) {
    const expr ***REMOVED*** skipParentheses(
      node,
      /*excludeJSDocTypeAssertions*/
      true
    );
    return expr.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ && expr.elements.length ***REMOVED******REMOVED******REMOVED*** 0;
  }
  function addOptionality(type, isProperty ***REMOVED*** false, isOptional ***REMOVED*** true) {
    return strictNullChecks && isOptional ? getOptionalType(type, isProperty) : type;
  }
  function getTypeForVariableLikeDeclaration(declaration, includeOptionality, checkMode) {
    if (isVariableDeclaration(declaration) && declaration.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */) {
      const indexType ***REMOVED*** getIndexType(getNonNullableTypeIfNeeded(checkExpression(
        declaration.parent.parent.expression,
        /*checkMode*/
        checkMode
      )));
      return indexType.flags & (262144 /* TypeParameter */ | 4194304 /* Index */) ? getExtractStringType(indexType) : stringType;
    }
    if (isVariableDeclaration(declaration) && declaration.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */) {
      const forOfStatement ***REMOVED*** declaration.parent.parent;
      return checkRightHandSideOfForOf(forOfStatement) || anyType;
    }
    if (isBindingPattern(declaration.parent)) {
      return getTypeForBindingElement(declaration);
    }
    const isProperty ***REMOVED*** isPropertyDeclaration(declaration) && !hasAccessorModifier(declaration) || isPropertySignature(declaration) || isJSDocPropertyTag(declaration);
    const isOptional ***REMOVED*** includeOptionality && isOptionalDeclaration(declaration);
    const declaredType ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration);
    if (isCatchClauseVariableDeclarationOrBindingElement(declaration)) {
      if (declaredType) {
        return isTypeAny(declaredType) || declaredType ***REMOVED******REMOVED******REMOVED*** unknownType ? declaredType : errorType;
      }
      return useUnknownInCatchVariables ? unknownType : anyType;
    }
    if (declaredType) {
      return addOptionality(declaredType, isProperty, isOptional);
    }
    if ((noImplicitAny || isInJSFile(declaration)) && isVariableDeclaration(declaration) && !isBindingPattern(declaration.name) && !(getCombinedModifierFlagsCached(declaration) & 32 /* Export */) && !(declaration.flags & 33554432 /* Ambient */)) {
      if (!(getCombinedNodeFlagsCached(declaration) & 6 /* Constant */) && (!declaration.initializer || isNullOrUndefined2(declaration.initializer))) {
        return autoType;
      }
      if (declaration.initializer && isEmptyArrayLiteral2(declaration.initializer)) {
        return autoArrayType;
      }
    }
    if (isParameter(declaration)) {
      if (!declaration.symbol) {
        return;
      }
      const func ***REMOVED*** declaration.parent;
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ && hasBindableName(func)) {
        const getter ***REMOVED*** getDeclarationOfKind(getSymbolOfDeclaration(declaration.parent), 178 /* GetAccessor */);
        if (getter) {
          const getterSignature ***REMOVED*** getSignatureFromDeclaration(getter);
          const thisParameter ***REMOVED*** getAccessorThisParameter(func);
          if (thisParameter && declaration ***REMOVED******REMOVED******REMOVED*** thisParameter) {
            Debug.assert(!thisParameter.type);
            return getTypeOfSymbol(getterSignature.thisParameter);
          }
          return getReturnTypeOfSignature(getterSignature);
        }
      }
      const parameterTypeOfTypeTag ***REMOVED*** getParameterTypeOfTypeTag(func, declaration);
      if (parameterTypeOfTypeTag) return parameterTypeOfTypeTag;
      const type ***REMOVED*** declaration.symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "this" /* This */ ? getContextualThisParameterType(func) : getContextuallyTypedParameterType(declaration);
      if (type) {
        return addOptionality(
          type,
          /*isProperty*/
          false,
          isOptional
        );
      }
    }
    if (hasOnlyExpressionInitializer(declaration) && !!declaration.initializer) {
      if (isInJSFile(declaration) && !isParameter(declaration)) {
        const containerObjectType ***REMOVED*** getJSContainerObjectType(declaration, getSymbolOfDeclaration(declaration), getDeclaredExpandoInitializer(declaration));
        if (containerObjectType) {
          return containerObjectType;
        }
      }
      const type ***REMOVED*** widenTypeInferredFromInitializer(declaration, checkDeclarationInitializer(declaration, checkMode));
      return addOptionality(type, isProperty, isOptional);
    }
    if (isPropertyDeclaration(declaration) && (noImplicitAny || isInJSFile(declaration))) {
      if (!hasStaticModifier(declaration)) {
        const constructor ***REMOVED*** findConstructorDeclaration(declaration.parent);
        const type ***REMOVED*** constructor ? getFlowTypeInConstructor(declaration.symbol, constructor) : getEffectiveModifierFlags(declaration) & 128 /* Ambient */ ? getTypeOfPropertyInBaseClass(declaration.symbol) : void 0;
        return type && addOptionality(
          type,
          /*isProperty*/
          true,
          isOptional
        );
      } else {
        const staticBlocks ***REMOVED*** filter(declaration.parent.members, isClassStaticBlockDeclaration);
        const type ***REMOVED*** staticBlocks.length ? getFlowTypeInStaticBlocks(declaration.symbol, staticBlocks) : getEffectiveModifierFlags(declaration) & 128 /* Ambient */ ? getTypeOfPropertyInBaseClass(declaration.symbol) : void 0;
        return type && addOptionality(
          type,
          /*isProperty*/
          true,
          isOptional
        );
      }
    }
    if (isJsxAttribute(declaration)) {
      return trueType;
    }
    if (isBindingPattern(declaration.name)) {
      return getTypeFromBindingPattern(
        declaration.name,
        /*includePatternInType*/
        false,
        /*reportErrors*/
        true
      );
    }
    return void 0;
  }
  function isConstructorDeclaredProperty(symbol) {
    if (symbol.valueDeclaration && isBinaryExpression(symbol.valueDeclaration)) {
      const links ***REMOVED*** getSymbolLinks(symbol);
      if (links.isConstructorDeclaredProperty ***REMOVED******REMOVED******REMOVED*** void 0) {
        links.isConstructorDeclaredProperty ***REMOVED*** false;
        links.isConstructorDeclaredProperty ***REMOVED*** !!getDeclaringConstructor(symbol) && every(symbol.declarations, (declaration) ***REMOVED***> isBinaryExpression(declaration) && isPossiblyAliasedThisProperty(declaration) && (declaration.left.kind !***REMOVED******REMOVED*** 213 /* ElementAccessExpression */ || isStringOrNumericLiteralLike(declaration.left.argumentExpression)) && !getAnnotatedTypeForAssignmentDeclaration(
          /*declaredType*/
          void 0,
          declaration,
          symbol,
          declaration
        ));
      }
      return links.isConstructorDeclaredProperty;
    }
    return false;
  }
  function isAutoTypedProperty(symbol) {
    const declaration ***REMOVED*** symbol.valueDeclaration;
    return declaration && isPropertyDeclaration(declaration) && !getEffectiveTypeAnnotationNode(declaration) && !declaration.initializer && (noImplicitAny || isInJSFile(declaration));
  }
  function getDeclaringConstructor(symbol) {
    if (!symbol.declarations) {
      return;
    }
    for (const declaration of symbol.declarations) {
      const container ***REMOVED*** getThisContainer(
        declaration,
        /*includeArrowFunctions*/
        false,
        /*includeClassComputedPropertyName*/
        false
      );
      if (container && (container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || isJSConstructor(container))) {
        return container;
      }
    }
  }
  function getFlowTypeFromCommonJSExport(symbol) {
    const file ***REMOVED*** getSourceFileOfNode(symbol.declarations[0]);
    const accessName ***REMOVED*** unescapeLeadingUnderscores(symbol.escapedName);
    const areAllModuleExports ***REMOVED*** symbol.declarations.every((d) ***REMOVED***> isInJSFile(d) && isAccessExpression(d) && isModuleExportsAccessExpression(d.expression));
    const reference ***REMOVED*** areAllModuleExports ? factory.createPropertyAccessExpression(factory.createPropertyAccessExpression(factory.createIdentifier("module"), factory.createIdentifier("exports")), accessName) : factory.createPropertyAccessExpression(factory.createIdentifier("exports"), accessName);
    if (areAllModuleExports) {
      setParent(reference.expression.expression, reference.expression);
    }
    setParent(reference.expression, reference);
    setParent(reference, file);
    reference.flowNode ***REMOVED*** file.endFlowNode;
    return getFlowTypeOfReference(reference, autoType, undefinedType);
  }
  function getFlowTypeInStaticBlocks(symbol, staticBlocks) {
    const accessName ***REMOVED*** startsWith(symbol.escapedName, "__#") ? factory.createPrivateIdentifier(symbol.escapedName.split("@")[1]) : unescapeLeadingUnderscores(symbol.escapedName);
    for (const staticBlock of staticBlocks) {
      const reference ***REMOVED*** factory.createPropertyAccessExpression(factory.createThis(), accessName);
      setParent(reference.expression, reference);
      setParent(reference, staticBlock);
      reference.flowNode ***REMOVED*** staticBlock.returnFlowNode;
      const flowType ***REMOVED*** getFlowTypeOfProperty(reference, symbol);
      if (noImplicitAny && (flowType ***REMOVED******REMOVED******REMOVED*** autoType || flowType ***REMOVED******REMOVED******REMOVED*** autoArrayType)) {
        error(symbol.valueDeclaration, Diagnostics.Member_0_implicitly_has_an_1_type, symbolToString(symbol), typeToString(flowType));
      }
      if (everyType(flowType, isNullableType)) {
        continue;
      }
      return convertAutoToAny(flowType);
    }
  }
  function getFlowTypeInConstructor(symbol, constructor) {
    const accessName ***REMOVED*** startsWith(symbol.escapedName, "__#") ? factory.createPrivateIdentifier(symbol.escapedName.split("@")[1]) : unescapeLeadingUnderscores(symbol.escapedName);
    const reference ***REMOVED*** factory.createPropertyAccessExpression(factory.createThis(), accessName);
    setParent(reference.expression, reference);
    setParent(reference, constructor);
    reference.flowNode ***REMOVED*** constructor.returnFlowNode;
    const flowType ***REMOVED*** getFlowTypeOfProperty(reference, symbol);
    if (noImplicitAny && (flowType ***REMOVED******REMOVED******REMOVED*** autoType || flowType ***REMOVED******REMOVED******REMOVED*** autoArrayType)) {
      error(symbol.valueDeclaration, Diagnostics.Member_0_implicitly_has_an_1_type, symbolToString(symbol), typeToString(flowType));
    }
    return everyType(flowType, isNullableType) ? void 0 : convertAutoToAny(flowType);
  }
  function getFlowTypeOfProperty(reference, prop) {
    const initialType ***REMOVED*** (prop ***REMOVED******REMOVED*** null ? void 0 : prop.valueDeclaration) && (!isAutoTypedProperty(prop) || getEffectiveModifierFlags(prop.valueDeclaration) & 128 /* Ambient */) && getTypeOfPropertyInBaseClass(prop) || undefinedType;
    return getFlowTypeOfReference(reference, autoType, initialType);
  }
  function getWidenedTypeForAssignmentDeclaration(symbol, resolvedSymbol) {
    const container ***REMOVED*** getAssignedExpandoInitializer(symbol.valueDeclaration);
    if (container) {
      const tag ***REMOVED*** isInJSFile(container) ? getJSDocTypeTag(container) : void 0;
      if (tag && tag.typeExpression) {
        return getTypeFromTypeNode(tag.typeExpression);
      }
      const containerObjectType ***REMOVED*** symbol.valueDeclaration && getJSContainerObjectType(symbol.valueDeclaration, symbol, container);
      return containerObjectType || getWidenedLiteralType(checkExpressionCached(container));
    }
    let type;
    let definedInConstructor ***REMOVED*** false;
    let definedInMethod ***REMOVED*** false;
    if (isConstructorDeclaredProperty(symbol)) {
      type ***REMOVED*** getFlowTypeInConstructor(symbol, getDeclaringConstructor(symbol));
    }
    if (!type) {
      let types;
      if (symbol.declarations) {
        let jsdocType;
        for (const declaration of symbol.declarations) {
          const expression ***REMOVED*** isBinaryExpression(declaration) || isCallExpression(declaration) ? declaration : isAccessExpression(declaration) ? isBinaryExpression(declaration.parent) ? declaration.parent : declaration : void 0;
          if (!expression) {
            continue;
          }
          const kind ***REMOVED*** isAccessExpression(expression) ? getAssignmentDeclarationPropertyAccessKind(expression) : getAssignmentDeclarationKind(expression);
          if (kind ***REMOVED******REMOVED******REMOVED*** 4 /* ThisProperty */ || isBinaryExpression(expression) && isPossiblyAliasedThisProperty(expression, kind)) {
            if (isDeclarationInConstructor(expression)) {
              definedInConstructor ***REMOVED*** true;
            } else {
              definedInMethod ***REMOVED*** true;
            }
          }
          if (!isCallExpression(expression)) {
            jsdocType ***REMOVED*** getAnnotatedTypeForAssignmentDeclaration(jsdocType, expression, symbol, declaration);
          }
          if (!jsdocType) {
            (types || (types ***REMOVED*** [])).push(isBinaryExpression(expression) || isCallExpression(expression) ? getInitializerTypeFromAssignmentDeclaration(symbol, resolvedSymbol, expression, kind) : neverType);
          }
        }
        type ***REMOVED*** jsdocType;
      }
      if (!type) {
        if (!length(types)) {
          return errorType;
        }
        let constructorTypes ***REMOVED*** definedInConstructor && symbol.declarations ? getConstructorDefinedThisAssignmentTypes(types, symbol.declarations) : void 0;
        if (definedInMethod) {
          const propType ***REMOVED*** getTypeOfPropertyInBaseClass(symbol);
          if (propType) {
            (constructorTypes || (constructorTypes ***REMOVED*** [])).push(propType);
            definedInConstructor ***REMOVED*** true;
          }
        }
        const sourceTypes ***REMOVED*** some(constructorTypes, (t) ***REMOVED***> !!(t.flags & ~98304 /* Nullable */)) ? constructorTypes : types;
        type ***REMOVED*** getUnionType(sourceTypes);
      }
    }
    const widened ***REMOVED*** getWidenedType(addOptionality(
      type,
      /*isProperty*/
      false,
      definedInMethod && !definedInConstructor
    ));
    if (symbol.valueDeclaration && isInJSFile(symbol.valueDeclaration) && filterType(widened, (t) ***REMOVED***> !!(t.flags & ~98304 /* Nullable */)) ***REMOVED******REMOVED******REMOVED*** neverType) {
      reportImplicitAny(symbol.valueDeclaration, anyType);
      return anyType;
    }
    return widened;
  }
  function getJSContainerObjectType(decl, symbol, init) {
    var _a, _b;
    if (!isInJSFile(decl) || !init || !isObjectLiteralExpression(init) || init.properties.length) {
      return void 0;
    }
    const exports2 ***REMOVED*** createSymbolTable();
    while (isBinaryExpression(decl) || isPropertyAccessExpression(decl)) {
      const s2 ***REMOVED*** getSymbolOfNode(decl);
      if ((_a ***REMOVED*** s2 ***REMOVED******REMOVED*** null ? void 0 : s2.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.size) {
        mergeSymbolTable(exports2, s2.exports);
      }
      decl ***REMOVED*** isBinaryExpression(decl) ? decl.parent : decl.parent.parent;
    }
    const s ***REMOVED*** getSymbolOfNode(decl);
    if ((_b ***REMOVED*** s ***REMOVED******REMOVED*** null ? void 0 : s.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.size) {
      mergeSymbolTable(exports2, s.exports);
    }
    const type ***REMOVED*** createAnonymousType(symbol, exports2, emptyArray, emptyArray, emptyArray);
    type.objectFlags |***REMOVED*** 4096 /* JSLiteral */;
    return type;
  }
  function getAnnotatedTypeForAssignmentDeclaration(declaredType, expression, symbol, declaration) {
    var _a;
    const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(expression.parent);
    if (typeNode) {
      const type ***REMOVED*** getWidenedType(getTypeFromTypeNode(typeNode));
      if (!declaredType) {
        return type;
      } else if (!isErrorType(declaredType) && !isErrorType(type) && !isTypeIdenticalTo(declaredType, type)) {
        errorNextVariableOrPropertyDeclarationMustHaveSameType(
          /*firstDeclaration*/
          void 0,
          declaredType,
          declaration,
          type
        );
      }
    }
    if ((_a ***REMOVED*** symbol.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.valueDeclaration) {
      const possiblyAnnotatedSymbol ***REMOVED*** getFunctionExpressionParentSymbolOrSymbol(symbol.parent);
      if (possiblyAnnotatedSymbol.valueDeclaration) {
        const typeNode2 ***REMOVED*** getEffectiveTypeAnnotationNode(possiblyAnnotatedSymbol.valueDeclaration);
        if (typeNode2) {
          const annotationSymbol ***REMOVED*** getPropertyOfType(getTypeFromTypeNode(typeNode2), symbol.escapedName);
          if (annotationSymbol) {
            return getNonMissingTypeOfSymbol(annotationSymbol);
          }
        }
      }
    }
    return declaredType;
  }
  function getInitializerTypeFromAssignmentDeclaration(symbol, resolvedSymbol, expression, kind) {
    if (isCallExpression(expression)) {
      if (resolvedSymbol) {
        return getTypeOfSymbol(resolvedSymbol);
      }
      const objectLitType ***REMOVED*** checkExpressionCached(expression.arguments[2]);
      const valueType ***REMOVED*** getTypeOfPropertyOfType(objectLitType, "value");
      if (valueType) {
        return valueType;
      }
      const getFunc ***REMOVED*** getTypeOfPropertyOfType(objectLitType, "get");
      if (getFunc) {
        const getSig ***REMOVED*** getSingleCallSignature(getFunc);
        if (getSig) {
          return getReturnTypeOfSignature(getSig);
        }
      }
      const setFunc ***REMOVED*** getTypeOfPropertyOfType(objectLitType, "set");
      if (setFunc) {
        const setSig ***REMOVED*** getSingleCallSignature(setFunc);
        if (setSig) {
          return getTypeOfFirstParameterOfSignature(setSig);
        }
      }
      return anyType;
    }
    if (containsSameNamedThisProperty(expression.left, expression.right)) {
      return anyType;
    }
    const isDirectExport ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 1 /* ExportsProperty */ && (isPropertyAccessExpression(expression.left) || isElementAccessExpression(expression.left)) && (isModuleExportsAccessExpression(expression.left.expression) || isIdentifier(expression.left.expression) && isExportsIdentifier(expression.left.expression));
    const type ***REMOVED*** resolvedSymbol ? getTypeOfSymbol(resolvedSymbol) : isDirectExport ? getRegularTypeOfLiteralType(checkExpressionCached(expression.right)) : getWidenedLiteralType(checkExpressionCached(expression.right));
    if (type.flags & 524288 /* Object */ && kind ***REMOVED******REMOVED******REMOVED*** 2 /* ModuleExports */ && symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "export***REMOVED***" /* ExportEquals */) {
      const exportedType ***REMOVED*** resolveStructuredTypeMembers(type);
      const members ***REMOVED*** createSymbolTable();
      copyEntries(exportedType.members, members);
      const initialSize ***REMOVED*** members.size;
      if (resolvedSymbol && !resolvedSymbol.exports) {
        resolvedSymbol.exports ***REMOVED*** createSymbolTable();
      }
      (resolvedSymbol || symbol).exports.forEach((s, name) ***REMOVED***> {
        var _a;
        const exportedMember ***REMOVED*** members.get(name);
        if (exportedMember && exportedMember !***REMOVED******REMOVED*** s && !(s.flags & 2097152 /* Alias */)) {
          if (s.flags & 111551 /* Value */ && exportedMember.flags & 111551 /* Value */) {
            if (s.valueDeclaration && exportedMember.valueDeclaration && getSourceFileOfNode(s.valueDeclaration) !***REMOVED******REMOVED*** getSourceFileOfNode(exportedMember.valueDeclaration)) {
              const unescapedName ***REMOVED*** unescapeLeadingUnderscores(s.escapedName);
              const exportedMemberName ***REMOVED*** ((_a ***REMOVED*** tryCast(exportedMember.valueDeclaration, isNamedDeclaration)) ***REMOVED******REMOVED*** null ? void 0 : _a.name) || exportedMember.valueDeclaration;
              addRelatedInfo(
                error(s.valueDeclaration, Diagnostics.Duplicate_identifier_0, unescapedName),
                createDiagnosticForNode(exportedMemberName, Diagnostics._0_was_also_declared_here, unescapedName)
              );
              addRelatedInfo(
                error(exportedMemberName, Diagnostics.Duplicate_identifier_0, unescapedName),
                createDiagnosticForNode(s.valueDeclaration, Diagnostics._0_was_also_declared_here, unescapedName)
              );
            }
            const union ***REMOVED*** createSymbol(s.flags | exportedMember.flags, name);
            union.links.type ***REMOVED*** getUnionType([getTypeOfSymbol(s), getTypeOfSymbol(exportedMember)]);
            union.valueDeclaration ***REMOVED*** exportedMember.valueDeclaration;
            union.declarations ***REMOVED*** concatenate(exportedMember.declarations, s.declarations);
            members.set(name, union);
          } else {
            members.set(name, mergeSymbol(s, exportedMember));
          }
        } else {
          members.set(name, s);
        }
      });
      const result ***REMOVED*** createAnonymousType(
        initialSize !***REMOVED******REMOVED*** members.size ? void 0 : exportedType.symbol,
        // Only set the type's symbol if it looks to be the same as the original type
        members,
        exportedType.callSignatures,
        exportedType.constructSignatures,
        exportedType.indexInfos
      );
      if (initialSize ***REMOVED******REMOVED******REMOVED*** members.size) {
        if (type.aliasSymbol) {
          result.aliasSymbol ***REMOVED*** type.aliasSymbol;
          result.aliasTypeArguments ***REMOVED*** type.aliasTypeArguments;
        }
        if (getObjectFlags(type) & 4 /* Reference */) {
          result.aliasSymbol ***REMOVED*** type.symbol;
          const args ***REMOVED*** getTypeArguments(type);
          result.aliasTypeArguments ***REMOVED*** length(args) ? args : void 0;
        }
      }
      result.objectFlags |***REMOVED*** getPropagatingFlagsOfTypes([type]) | getObjectFlags(type) & (4096 /* JSLiteral */ | 16384 /* ArrayLiteral */ | 128 /* ObjectLiteral */);
      if (result.symbol && result.symbol.flags & 32 /* Class */ && type ***REMOVED******REMOVED******REMOVED*** getDeclaredTypeOfClassOrInterface(result.symbol)) {
        result.objectFlags |***REMOVED*** 16777216 /* IsClassInstanceClone */;
      }
      return result;
    }
    if (isEmptyArrayLiteralType(type)) {
      reportImplicitAny(expression, anyArrayType);
      return anyArrayType;
    }
    return type;
  }
  function containsSameNamedThisProperty(thisProperty, expression) {
    return isPropertyAccessExpression(thisProperty) && thisProperty.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && forEachChildRecursively(expression, (n) ***REMOVED***> isMatchingReference(thisProperty, n));
  }
  function isDeclarationInConstructor(expression) {
    const thisContainer ***REMOVED*** getThisContainer(
      expression,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    return thisContainer.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || thisContainer.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || thisContainer.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ && !isPrototypePropertyAssignment(thisContainer.parent);
  }
  function getConstructorDefinedThisAssignmentTypes(types, declarations) {
    Debug.assert(types.length ***REMOVED******REMOVED******REMOVED*** declarations.length);
    return types.filter((_, i) ***REMOVED***> {
      const declaration ***REMOVED*** declarations[i];
      const expression ***REMOVED*** isBinaryExpression(declaration) ? declaration : isBinaryExpression(declaration.parent) ? declaration.parent : void 0;
      return expression && isDeclarationInConstructor(expression);
    });
  }
  function getTypeFromBindingElement(element, includePatternInType, reportErrors2) {
    if (element.initializer) {
      const contextualType ***REMOVED*** isBindingPattern(element.name) ? getTypeFromBindingPattern(
        element.name,
        /*includePatternInType*/
        true,
        /*reportErrors*/
        false
      ) : unknownType;
      return addOptionality(getWidenedLiteralTypeForInitializer(element, checkDeclarationInitializer(element, 0 /* Normal */, contextualType)));
    }
    if (isBindingPattern(element.name)) {
      return getTypeFromBindingPattern(element.name, includePatternInType, reportErrors2);
    }
    if (reportErrors2 && !declarationBelongsToPrivateAmbientMember(element)) {
      reportImplicitAny(element, anyType);
    }
    return includePatternInType ? nonInferrableAnyType : anyType;
  }
  function getTypeFromObjectBindingPattern(pattern, includePatternInType, reportErrors2) {
    const members ***REMOVED*** createSymbolTable();
    let stringIndexInfo;
    let objectFlags ***REMOVED*** 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
    forEach(pattern.elements, (e) ***REMOVED***> {
      const name ***REMOVED*** e.propertyName || e.name;
      if (e.dotDotDotToken) {
        stringIndexInfo ***REMOVED*** createIndexInfo(
          stringType,
          anyType,
          /*isReadonly*/
          false
        );
        return;
      }
      const exprType ***REMOVED*** getLiteralTypeFromPropertyName(name);
      if (!isTypeUsableAsPropertyName(exprType)) {
        objectFlags |***REMOVED*** 512 /* ObjectLiteralPatternWithComputedProperties */;
        return;
      }
      const text ***REMOVED*** getPropertyNameFromType(exprType);
      const flags ***REMOVED*** 4 /* Property */ | (e.initializer ? 16777216 /* Optional */ : 0);
      const symbol ***REMOVED*** createSymbol(flags, text);
      symbol.links.type ***REMOVED*** getTypeFromBindingElement(e, includePatternInType, reportErrors2);
      members.set(symbol.escapedName, symbol);
    });
    const result ***REMOVED*** createAnonymousType(
      /*symbol*/
      void 0,
      members,
      emptyArray,
      emptyArray,
      stringIndexInfo ? [stringIndexInfo] : emptyArray
    );
    result.objectFlags |***REMOVED*** objectFlags;
    if (includePatternInType) {
      result.pattern ***REMOVED*** pattern;
      result.objectFlags |***REMOVED*** 131072 /* ContainsObjectOrArrayLiteral */;
    }
    return result;
  }
  function getTypeFromArrayBindingPattern(pattern, includePatternInType, reportErrors2) {
    const elements ***REMOVED*** pattern.elements;
    const lastElement ***REMOVED*** lastOrUndefined(elements);
    const restElement ***REMOVED*** lastElement && lastElement.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && lastElement.dotDotDotToken ? lastElement : void 0;
    if (elements.length ***REMOVED******REMOVED******REMOVED*** 0 || elements.length ***REMOVED******REMOVED******REMOVED*** 1 && restElement) {
      return languageVersion >***REMOVED*** 2 /* ES2015 */ ? createIterableType(anyType) : anyArrayType;
    }
    const elementTypes ***REMOVED*** map(elements, (e) ***REMOVED***> isOmittedExpression(e) ? anyType : getTypeFromBindingElement(e, includePatternInType, reportErrors2));
    const minLength ***REMOVED*** findLastIndex(elements, (e) ***REMOVED***> !(e ***REMOVED******REMOVED******REMOVED*** restElement || isOmittedExpression(e) || hasDefaultValue(e)), elements.length - 1) + 1;
    const elementFlags ***REMOVED*** map(elements, (e, i) ***REMOVED***> e ***REMOVED******REMOVED******REMOVED*** restElement ? 4 /* Rest */ : i >***REMOVED*** minLength ? 2 /* Optional */ : 1 /* Required */);
    let result ***REMOVED*** createTupleType(elementTypes, elementFlags);
    if (includePatternInType) {
      result ***REMOVED*** cloneTypeReference(result);
      result.pattern ***REMOVED*** pattern;
      result.objectFlags |***REMOVED*** 131072 /* ContainsObjectOrArrayLiteral */;
    }
    return result;
  }
  function getTypeFromBindingPattern(pattern, includePatternInType ***REMOVED*** false, reportErrors2 ***REMOVED*** false) {
    if (includePatternInType) contextualBindingPatterns.push(pattern);
    const result ***REMOVED*** pattern.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ ? getTypeFromObjectBindingPattern(pattern, includePatternInType, reportErrors2) : getTypeFromArrayBindingPattern(pattern, includePatternInType, reportErrors2);
    if (includePatternInType) contextualBindingPatterns.pop();
    return result;
  }
  function getWidenedTypeForVariableLikeDeclaration(declaration, reportErrors2) {
    return widenTypeForVariableLikeDeclaration(getTypeForVariableLikeDeclaration(
      declaration,
      /*includeOptionality*/
      true,
      0 /* Normal */
    ), declaration, reportErrors2);
  }
  function getTypeFromImportAttributes(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const symbol ***REMOVED*** createSymbol(4096 /* ObjectLiteral */, "__importAttributes" /* ImportAttributes */);
      const members ***REMOVED*** createSymbolTable();
      forEach(node.elements, (attr) ***REMOVED***> {
        const member ***REMOVED*** createSymbol(4 /* Property */, getNameFromImportAttribute(attr));
        member.parent ***REMOVED*** symbol;
        member.links.type ***REMOVED*** checkImportAttribute(attr);
        member.links.target ***REMOVED*** member;
        members.set(member.escapedName, member);
      });
      const type ***REMOVED*** createAnonymousType(symbol, members, emptyArray, emptyArray, emptyArray);
      type.objectFlags |***REMOVED*** 128 /* ObjectLiteral */ | 262144 /* NonInferrableType */;
      links.resolvedType ***REMOVED*** type;
    }
    return links.resolvedType;
  }
  function isGlobalSymbolConstructor(node) {
    const symbol ***REMOVED*** getSymbolOfNode(node);
    const globalSymbol ***REMOVED*** getGlobalESSymbolConstructorTypeSymbol(
      /*reportErrors*/
      false
    );
    return globalSymbol && symbol && symbol ***REMOVED******REMOVED******REMOVED*** globalSymbol;
  }
  function widenTypeForVariableLikeDeclaration(type, declaration, reportErrors2) {
    if (type) {
      if (type.flags & 4096 /* ESSymbol */ && isGlobalSymbolConstructor(declaration.parent)) {
        type ***REMOVED*** getESSymbolLikeTypeForNode(declaration);
      }
      if (reportErrors2) {
        reportErrorsFromWidening(declaration, type);
      }
      if (type.flags & 8192 /* UniqueESSymbol */ && (isBindingElement(declaration) || !tryGetTypeFromEffectiveTypeNode(declaration)) && type.symbol !***REMOVED******REMOVED*** getSymbolOfDeclaration(declaration)) {
        type ***REMOVED*** esSymbolType;
      }
      return getWidenedType(type);
    }
    type ***REMOVED*** isParameter(declaration) && declaration.dotDotDotToken ? anyArrayType : anyType;
    if (reportErrors2) {
      if (!declarationBelongsToPrivateAmbientMember(declaration)) {
        reportImplicitAny(declaration, type);
      }
    }
    return type;
  }
  function declarationBelongsToPrivateAmbientMember(declaration) {
    const root ***REMOVED*** getRootDeclaration(declaration);
    const memberDeclaration ***REMOVED*** root.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ ? root.parent : root;
    return isPrivateWithinAmbient(memberDeclaration);
  }
  function tryGetTypeFromEffectiveTypeNode(node) {
    const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    if (typeNode) {
      return getTypeFromTypeNode(typeNode);
    }
  }
  function isParameterOfContextSensitiveSignature(symbol) {
    let decl ***REMOVED*** symbol.valueDeclaration;
    if (!decl) {
      return false;
    }
    if (isBindingElement(decl)) {
      decl ***REMOVED*** walkUpBindingElementsAndPatterns(decl);
    }
    if (isParameter(decl)) {
      return isContextSensitiveFunctionOrObjectLiteralMethod(decl.parent);
    }
    return false;
  }
  function getTypeOfVariableOrParameterOrProperty(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.type) {
      const type ***REMOVED*** getTypeOfVariableOrParameterOrPropertyWorker(symbol);
      if (!links.type && !isParameterOfContextSensitiveSignature(symbol)) {
        links.type ***REMOVED*** type;
      }
      return type;
    }
    return links.type;
  }
  function getTypeOfVariableOrParameterOrPropertyWorker(symbol) {
    if (symbol.flags & 4194304 /* Prototype */) {
      return getTypeOfPrototypeProperty(symbol);
    }
    if (symbol ***REMOVED******REMOVED******REMOVED*** requireSymbol) {
      return anyType;
    }
    if (symbol.flags & 134217728 /* ModuleExports */ && symbol.valueDeclaration) {
      const fileSymbol ***REMOVED*** getSymbolOfDeclaration(getSourceFileOfNode(symbol.valueDeclaration));
      const result ***REMOVED*** createSymbol(fileSymbol.flags, "exports");
      result.declarations ***REMOVED*** fileSymbol.declarations ? fileSymbol.declarations.slice() : [];
      result.parent ***REMOVED*** symbol;
      result.links.target ***REMOVED*** fileSymbol;
      if (fileSymbol.valueDeclaration) result.valueDeclaration ***REMOVED*** fileSymbol.valueDeclaration;
      if (fileSymbol.members) result.members ***REMOVED*** new Map(fileSymbol.members);
      if (fileSymbol.exports) result.exports ***REMOVED*** new Map(fileSymbol.exports);
      const members ***REMOVED*** createSymbolTable();
      members.set("exports", result);
      return createAnonymousType(symbol, members, emptyArray, emptyArray, emptyArray);
    }
    Debug.assertIsDefined(symbol.valueDeclaration);
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (isSourceFile(declaration) && isJsonSourceFile(declaration)) {
      if (!declaration.statements.length) {
        return emptyObjectType;
      }
      return getWidenedType(getWidenedLiteralType(checkExpression(declaration.statements[0].expression)));
    }
    if (isAccessor(declaration)) {
      return getTypeOfAccessors(symbol);
    }
    if (!pushTypeResolution(symbol, 0 /* Type */)) {
      if (symbol.flags & 512 /* ValueModule */ && !(symbol.flags & 67108864 /* Assignment */)) {
        return getTypeOfFuncClassEnumModule(symbol);
      }
      return reportCircularityError(symbol);
    }
    let type;
    if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */) {
      type ***REMOVED*** widenTypeForVariableLikeDeclaration(tryGetTypeFromEffectiveTypeNode(declaration) || checkExpressionCached(declaration.expression), declaration);
    } else if (isBinaryExpression(declaration) || isInJSFile(declaration) && (isCallExpression(declaration) || (isPropertyAccessExpression(declaration) || isBindableStaticElementAccessExpression(declaration)) && isBinaryExpression(declaration.parent))) {
      type ***REMOVED*** getWidenedTypeForAssignmentDeclaration(symbol);
    } else if (isPropertyAccessExpression(declaration) || isElementAccessExpression(declaration) || isIdentifier(declaration) || isStringLiteralLike(declaration) || isNumericLiteral(declaration) || isClassDeclaration(declaration) || isFunctionDeclaration(declaration) || isMethodDeclaration(declaration) && !isObjectLiteralMethod(declaration) || isMethodSignature(declaration) || isSourceFile(declaration)) {
      if (symbol.flags & (16 /* Function */ | 8192 /* Method */ | 32 /* Class */ | 384 /* Enum */ | 512 /* ValueModule */)) {
        return getTypeOfFuncClassEnumModule(symbol);
      }
      type ***REMOVED*** isBinaryExpression(declaration.parent) ? getWidenedTypeForAssignmentDeclaration(symbol) : tryGetTypeFromEffectiveTypeNode(declaration) || anyType;
    } else if (isPropertyAssignment(declaration)) {
      type ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration) || checkPropertyAssignment(declaration);
    } else if (isJsxAttribute(declaration)) {
      type ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration) || checkJsxAttribute(declaration);
    } else if (isShorthandPropertyAssignment(declaration)) {
      type ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration) || checkExpressionForMutableLocation(declaration.name, 0 /* Normal */);
    } else if (isObjectLiteralMethod(declaration)) {
      type ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration) || checkObjectLiteralMethod(declaration, 0 /* Normal */);
    } else if (isParameter(declaration) || isPropertyDeclaration(declaration) || isPropertySignature(declaration) || isVariableDeclaration(declaration) || isBindingElement(declaration) || isJSDocPropertyLikeTag(declaration)) {
      type ***REMOVED*** getWidenedTypeForVariableLikeDeclaration(
        declaration,
        /*reportErrors*/
        true
      );
    } else if (isEnumDeclaration(declaration)) {
      type ***REMOVED*** getTypeOfFuncClassEnumModule(symbol);
    } else if (isEnumMember(declaration)) {
      type ***REMOVED*** getTypeOfEnumMember(symbol);
    } else {
      return Debug.fail("Unhandled declaration kind! " + Debug.formatSyntaxKind(declaration.kind) + " for " + Debug.formatSymbol(symbol));
    }
    if (!popTypeResolution()) {
      if (symbol.flags & 512 /* ValueModule */ && !(symbol.flags & 67108864 /* Assignment */)) {
        return getTypeOfFuncClassEnumModule(symbol);
      }
      return reportCircularityError(symbol);
    }
    return type;
  }
  function getAnnotatedAccessorTypeNode(accessor) {
    if (accessor) {
      switch (accessor.kind) {
        case 178 /* GetAccessor */:
          const getterTypeAnnotation ***REMOVED*** getEffectiveReturnTypeNode(accessor);
          return getterTypeAnnotation;
        case 179 /* SetAccessor */:
          const setterTypeAnnotation ***REMOVED*** getEffectiveSetAccessorTypeAnnotationNode(accessor);
          return setterTypeAnnotation;
        case 173 /* PropertyDeclaration */:
          Debug.assert(hasAccessorModifier(accessor));
          const accessorTypeAnnotation ***REMOVED*** getEffectiveTypeAnnotationNode(accessor);
          return accessorTypeAnnotation;
      }
    }
    return void 0;
  }
  function getAnnotatedAccessorType(accessor) {
    const node ***REMOVED*** getAnnotatedAccessorTypeNode(accessor);
    return node && getTypeFromTypeNode(node);
  }
  function getAnnotatedAccessorThisParameter(accessor) {
    const parameter ***REMOVED*** getAccessorThisParameter(accessor);
    return parameter && parameter.symbol;
  }
  function getThisTypeOfDeclaration(declaration) {
    return getThisTypeOfSignature(getSignatureFromDeclaration(declaration));
  }
  function getTypeOfAccessors(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.type) {
      if (!pushTypeResolution(symbol, 0 /* Type */)) {
        return errorType;
      }
      const getter ***REMOVED*** getDeclarationOfKind(symbol, 178 /* GetAccessor */);
      const setter ***REMOVED*** getDeclarationOfKind(symbol, 179 /* SetAccessor */);
      const accessor ***REMOVED*** tryCast(getDeclarationOfKind(symbol, 173 /* PropertyDeclaration */), isAutoAccessorPropertyDeclaration);
      let type ***REMOVED*** getter && isInJSFile(getter) && getTypeForDeclarationFromJSDocComment(getter) || getAnnotatedAccessorType(getter) || getAnnotatedAccessorType(setter) || getAnnotatedAccessorType(accessor) || getter && getter.body && getReturnTypeFromBody(getter) || accessor && getWidenedTypeForVariableLikeDeclaration(
        accessor,
        /*reportErrors*/
        true
      );
      if (!type) {
        if (setter && !isPrivateWithinAmbient(setter)) {
          errorOrSuggestion(noImplicitAny, setter, Diagnostics.Property_0_implicitly_has_type_any_because_its_set_accessor_lacks_a_parameter_type_annotation, symbolToString(symbol));
        } else if (getter && !isPrivateWithinAmbient(getter)) {
          errorOrSuggestion(noImplicitAny, getter, Diagnostics.Property_0_implicitly_has_type_any_because_its_get_accessor_lacks_a_return_type_annotation, symbolToString(symbol));
        } else if (accessor && !isPrivateWithinAmbient(accessor)) {
          errorOrSuggestion(noImplicitAny, accessor, Diagnostics.Member_0_implicitly_has_an_1_type, symbolToString(symbol), "any");
        }
        type ***REMOVED*** anyType;
      }
      if (!popTypeResolution()) {
        if (getAnnotatedAccessorTypeNode(getter)) {
          error(getter, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_type_annotation, symbolToString(symbol));
        } else if (getAnnotatedAccessorTypeNode(setter)) {
          error(setter, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_type_annotation, symbolToString(symbol));
        } else if (getAnnotatedAccessorTypeNode(accessor)) {
          error(setter, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_type_annotation, symbolToString(symbol));
        } else if (getter && noImplicitAny) {
          error(getter, Diagnostics._0_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_referenced_directly_or_indirectly_in_one_of_its_return_expressions, symbolToString(symbol));
        }
        type ***REMOVED*** anyType;
      }
      links.type ?? (links.type ***REMOVED*** type);
    }
    return links.type;
  }
  function getWriteTypeOfAccessors(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.writeType) {
      if (!pushTypeResolution(symbol, 7 /* WriteType */)) {
        return errorType;
      }
      const setter ***REMOVED*** getDeclarationOfKind(symbol, 179 /* SetAccessor */) ?? tryCast(getDeclarationOfKind(symbol, 173 /* PropertyDeclaration */), isAutoAccessorPropertyDeclaration);
      let writeType ***REMOVED*** getAnnotatedAccessorType(setter);
      if (!popTypeResolution()) {
        if (getAnnotatedAccessorTypeNode(setter)) {
          error(setter, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_type_annotation, symbolToString(symbol));
        }
        writeType ***REMOVED*** anyType;
      }
      links.writeType ?? (links.writeType ***REMOVED*** writeType || getTypeOfAccessors(symbol));
    }
    return links.writeType;
  }
  function getBaseTypeVariableOfClass(symbol) {
    const baseConstructorType ***REMOVED*** getBaseConstructorTypeOfClass(getDeclaredTypeOfClassOrInterface(symbol));
    return baseConstructorType.flags & 8650752 /* TypeVariable */ ? baseConstructorType : baseConstructorType.flags & 2097152 /* Intersection */ ? find(baseConstructorType.types, (t) ***REMOVED***> !!(t.flags & 8650752 /* TypeVariable */)) : void 0;
  }
  function getTypeOfFuncClassEnumModule(symbol) {
    let links ***REMOVED*** getSymbolLinks(symbol);
    const originalLinks ***REMOVED*** links;
    if (!links.type) {
      const expando ***REMOVED*** symbol.valueDeclaration && getSymbolOfExpando(
        symbol.valueDeclaration,
        /*allowDeclaration*/
        false
      );
      if (expando) {
        const merged ***REMOVED*** mergeJSSymbols(symbol, expando);
        if (merged) {
          symbol ***REMOVED*** merged;
          links ***REMOVED*** merged.links;
        }
      }
      originalLinks.type ***REMOVED*** links.type ***REMOVED*** getTypeOfFuncClassEnumModuleWorker(symbol);
    }
    return links.type;
  }
  function getTypeOfFuncClassEnumModuleWorker(symbol) {
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (symbol.flags & 1536 /* Module */ && isShorthandAmbientModuleSymbol(symbol)) {
      return anyType;
    } else if (declaration && (declaration.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ || isAccessExpression(declaration) && declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */)) {
      return getWidenedTypeForAssignmentDeclaration(symbol);
    } else if (symbol.flags & 512 /* ValueModule */ && declaration && isSourceFile(declaration) && declaration.commonJsModuleIndicator) {
      const resolvedModule ***REMOVED*** resolveExternalModuleSymbol(symbol);
      if (resolvedModule !***REMOVED******REMOVED*** symbol) {
        if (!pushTypeResolution(symbol, 0 /* Type */)) {
          return errorType;
        }
        const exportEquals ***REMOVED*** getMergedSymbol(symbol.exports.get("export***REMOVED***" /* ExportEquals */));
        const type2 ***REMOVED*** getWidenedTypeForAssignmentDeclaration(exportEquals, exportEquals ***REMOVED******REMOVED******REMOVED*** resolvedModule ? void 0 : resolvedModule);
        if (!popTypeResolution()) {
          return reportCircularityError(symbol);
        }
        return type2;
      }
    }
    const type ***REMOVED*** createObjectType(16 /* Anonymous */, symbol);
    if (symbol.flags & 32 /* Class */) {
      const baseTypeVariable ***REMOVED*** getBaseTypeVariableOfClass(symbol);
      return baseTypeVariable ? getIntersectionType([type, baseTypeVariable]) : type;
    } else {
      return strictNullChecks && symbol.flags & 16777216 /* Optional */ ? getOptionalType(
        type,
        /*isProperty*/
        true
      ) : type;
    }
  }
  function getTypeOfEnumMember(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    return links.type || (links.type ***REMOVED*** getDeclaredTypeOfEnumMember(symbol));
  }
  function getTypeOfAlias(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.type) {
      if (!pushTypeResolution(symbol, 0 /* Type */)) {
        return errorType;
      }
      const targetSymbol ***REMOVED*** resolveAlias(symbol);
      const exportSymbol ***REMOVED*** symbol.declarations && getTargetOfAliasDeclaration(
        getDeclarationOfAliasSymbol(symbol),
        /*dontRecursivelyResolve*/
        true
      );
      const declaredType ***REMOVED*** firstDefined(exportSymbol ***REMOVED******REMOVED*** null ? void 0 : exportSymbol.declarations, (d) ***REMOVED***> isExportAssignment(d) ? tryGetTypeFromEffectiveTypeNode(d) : void 0);
      links.type ?? (links.type ***REMOVED*** (exportSymbol ***REMOVED******REMOVED*** null ? void 0 : exportSymbol.declarations) && isDuplicatedCommonJSExport(exportSymbol.declarations) && symbol.declarations.length ? getFlowTypeFromCommonJSExport(exportSymbol) : isDuplicatedCommonJSExport(symbol.declarations) ? autoType : declaredType ? declaredType : getSymbolFlags(targetSymbol) & 111551 /* Value */ ? getTypeOfSymbol(targetSymbol) : errorType);
      if (!popTypeResolution()) {
        reportCircularityError(exportSymbol ?? symbol);
        return links.type ?? (links.type ***REMOVED*** errorType);
      }
    }
    return links.type;
  }
  function getTypeOfInstantiatedSymbol(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    return links.type || (links.type ***REMOVED*** instantiateType(getTypeOfSymbol(links.target), links.mapper));
  }
  function getWriteTypeOfInstantiatedSymbol(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    return links.writeType || (links.writeType ***REMOVED*** instantiateType(getWriteTypeOfSymbol(links.target), links.mapper));
  }
  function reportCircularityError(symbol) {
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (declaration) {
      if (getEffectiveTypeAnnotationNode(declaration)) {
        error(symbol.valueDeclaration, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_type_annotation, symbolToString(symbol));
        return errorType;
      }
      if (noImplicitAny && (declaration.kind !***REMOVED******REMOVED*** 170 /* Parameter */ || declaration.initializer)) {
        error(symbol.valueDeclaration, Diagnostics._0_implicitly_has_type_any_because_it_does_not_have_a_type_annotation_and_is_referenced_directly_or_indirectly_in_its_own_initializer, symbolToString(symbol));
      }
    } else if (symbol.flags & 2097152 /* Alias */) {
      const node ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
      if (node) {
        error(node, Diagnostics.Circular_definition_of_import_alias_0, symbolToString(symbol));
      }
    }
    return anyType;
  }
  function getTypeOfSymbolWithDeferredType(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.type) {
      Debug.assertIsDefined(links.deferralParent);
      Debug.assertIsDefined(links.deferralConstituents);
      links.type ***REMOVED*** links.deferralParent.flags & 1048576 /* Union */ ? getUnionType(links.deferralConstituents) : getIntersectionType(links.deferralConstituents);
    }
    return links.type;
  }
  function getWriteTypeOfSymbolWithDeferredType(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.writeType && links.deferralWriteConstituents) {
      Debug.assertIsDefined(links.deferralParent);
      Debug.assertIsDefined(links.deferralConstituents);
      links.writeType ***REMOVED*** links.deferralParent.flags & 1048576 /* Union */ ? getUnionType(links.deferralWriteConstituents) : getIntersectionType(links.deferralWriteConstituents);
    }
    return links.writeType;
  }
  function getWriteTypeOfSymbol(symbol) {
    const checkFlags ***REMOVED*** getCheckFlags(symbol);
    if (checkFlags & 2 /* SyntheticProperty */) {
      return checkFlags & 65536 /* DeferredType */ ? getWriteTypeOfSymbolWithDeferredType(symbol) || getTypeOfSymbolWithDeferredType(symbol) : (
        // NOTE: cast to TransientSymbol should be safe because only TransientSymbols can have CheckFlags.SyntheticProperty
        symbol.links.writeType || symbol.links.type
      );
    }
    if (symbol.flags & 4 /* Property */) {
      return removeMissingType(getTypeOfSymbol(symbol), !!(symbol.flags & 16777216 /* Optional */));
    }
    if (symbol.flags & 98304 /* Accessor */) {
      return checkFlags & 1 /* Instantiated */ ? getWriteTypeOfInstantiatedSymbol(symbol) : getWriteTypeOfAccessors(symbol);
    }
    return getTypeOfSymbol(symbol);
  }
  function getTypeOfSymbol(symbol) {
    const checkFlags ***REMOVED*** getCheckFlags(symbol);
    if (checkFlags & 65536 /* DeferredType */) {
      return getTypeOfSymbolWithDeferredType(symbol);
    }
    if (checkFlags & 1 /* Instantiated */) {
      return getTypeOfInstantiatedSymbol(symbol);
    }
    if (checkFlags & 262144 /* Mapped */) {
      return getTypeOfMappedSymbol(symbol);
    }
    if (checkFlags & 8192 /* ReverseMapped */) {
      return getTypeOfReverseMappedSymbol(symbol);
    }
    if (symbol.flags & (3 /* Variable */ | 4 /* Property */)) {
      return getTypeOfVariableOrParameterOrProperty(symbol);
    }
    if (symbol.flags & (16 /* Function */ | 8192 /* Method */ | 32 /* Class */ | 384 /* Enum */ | 512 /* ValueModule */)) {
      return getTypeOfFuncClassEnumModule(symbol);
    }
    if (symbol.flags & 8 /* EnumMember */) {
      return getTypeOfEnumMember(symbol);
    }
    if (symbol.flags & 98304 /* Accessor */) {
      return getTypeOfAccessors(symbol);
    }
    if (symbol.flags & 2097152 /* Alias */) {
      return getTypeOfAlias(symbol);
    }
    return errorType;
  }
  function getNonMissingTypeOfSymbol(symbol) {
    return removeMissingType(getTypeOfSymbol(symbol), !!(symbol.flags & 16777216 /* Optional */));
  }
  function isReferenceToSomeType(type, targets) {
    if (type ***REMOVED******REMOVED******REMOVED*** void 0 || (getObjectFlags(type) & 4 /* Reference */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return false;
    }
    for (const target of targets) {
      if (type.target ***REMOVED******REMOVED******REMOVED*** target) {
        return true;
      }
    }
    return false;
  }
  function isReferenceToType(type, target) {
    return type !***REMOVED******REMOVED*** void 0 && target !***REMOVED******REMOVED*** void 0 && (getObjectFlags(type) & 4 /* Reference */) !***REMOVED******REMOVED*** 0 && type.target ***REMOVED******REMOVED******REMOVED*** target;
  }
  function getTargetType(type) {
    return getObjectFlags(type) & 4 /* Reference */ ? type.target : type;
  }
  function hasBaseType(type, checkBase) {
    return check(type);
    function check(type2) {
      if (getObjectFlags(type2) & (3 /* ClassOrInterface */ | 4 /* Reference */)) {
        const target ***REMOVED*** getTargetType(type2);
        return target ***REMOVED******REMOVED******REMOVED*** checkBase || some(getBaseTypes(target), check);
      } else if (type2.flags & 2097152 /* Intersection */) {
        return some(type2.types, check);
      }
      return false;
    }
  }
  function appendTypeParameters(typeParameters, declarations) {
    for (const declaration of declarations) {
      typeParameters ***REMOVED*** appendIfUnique(typeParameters, getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(declaration)));
    }
    return typeParameters;
  }
  function getOuterTypeParameters(node, includeThisTypes) {
    while (true) {
      node ***REMOVED*** node.parent;
      if (node && isBinaryExpression(node)) {
        const assignmentKind ***REMOVED*** getAssignmentDeclarationKind(node);
        if (assignmentKind ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */ || assignmentKind ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */) {
          const symbol ***REMOVED*** getSymbolOfDeclaration(node.left);
          if (symbol && symbol.parent && !findAncestor(symbol.parent.valueDeclaration, (d) ***REMOVED***> node ***REMOVED******REMOVED******REMOVED*** d)) {
            node ***REMOVED*** symbol.parent.valueDeclaration;
          }
        }
      }
      if (!node) {
        return void 0;
      }
      const kind ***REMOVED*** node.kind;
      switch (kind) {
        case 264 /* ClassDeclaration */:
        case 232 /* ClassExpression */:
        case 265 /* InterfaceDeclaration */:
        case 180 /* CallSignature */:
        case 181 /* ConstructSignature */:
        case 174 /* MethodSignature */:
        case 185 /* FunctionType */:
        case 186 /* ConstructorType */:
        case 318 /* JSDocFunctionType */:
        case 263 /* FunctionDeclaration */:
        case 175 /* MethodDeclaration */:
        case 219 /* FunctionExpression */:
        case 220 /* ArrowFunction */:
        case 266 /* TypeAliasDeclaration */:
        case 346 /* JSDocTemplateTag */:
        case 347 /* JSDocTypedefTag */:
        case 341 /* JSDocEnumTag */:
        case 339 /* JSDocCallbackTag */:
        case 201 /* MappedType */:
        case 195 /* ConditionalType */: {
          const outerTypeParameters ***REMOVED*** getOuterTypeParameters(node, includeThisTypes);
          if ((kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || isObjectLiteralMethod(node)) && isContextSensitive(node)) {
            const signature ***REMOVED*** firstOrUndefined(getSignaturesOfType(getTypeOfSymbol(getSymbolOfDeclaration(node)), 0 /* Call */));
            if (signature && signature.typeParameters) {
              return [...outerTypeParameters || emptyArray, ...signature.typeParameters];
            }
          }
          if (kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */) {
            return append(outerTypeParameters, getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(node.typeParameter)));
          } else if (kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */) {
            return concatenate(outerTypeParameters, getInferTypeParameters(node));
          }
          const outerAndOwnTypeParameters ***REMOVED*** appendTypeParameters(outerTypeParameters, getEffectiveTypeParameterDeclarations(node));
          const thisType ***REMOVED*** includeThisTypes && (kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || isJSConstructor(node)) && getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(node)).thisType;
          return thisType ? append(outerAndOwnTypeParameters, thisType) : outerAndOwnTypeParameters;
        }
        case 342 /* JSDocParameterTag */:
          const paramSymbol ***REMOVED*** getParameterSymbolFromJSDoc(node);
          if (paramSymbol) {
            node ***REMOVED*** paramSymbol.valueDeclaration;
          }
          break;
        case 321 /* JSDoc */: {
          const outerTypeParameters ***REMOVED*** getOuterTypeParameters(node, includeThisTypes);
          return node.tags ? appendTypeParameters(outerTypeParameters, flatMap(node.tags, (t) ***REMOVED***> isJSDocTemplateTag(t) ? t.typeParameters : void 0)) : outerTypeParameters;
        }
      }
    }
  }
  function getOuterTypeParametersOfClassOrInterface(symbol) {
    var _a;
    const declaration ***REMOVED*** symbol.flags & 32 /* Class */ || symbol.flags & 16 /* Function */ ? symbol.valueDeclaration : (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find((decl) ***REMOVED***> {
      if (decl.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
        return true;
      }
      if (decl.kind !***REMOVED******REMOVED*** 261 /* VariableDeclaration */) {
        return false;
      }
      const initializer ***REMOVED*** decl.initializer;
      return !!initializer && (initializer.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || initializer.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */);
    });
    Debug.assert(!!declaration, "Class was missing valueDeclaration -OR- non-class had no interface declarations");
    return getOuterTypeParameters(declaration);
  }
  function getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol) {
    if (!symbol.declarations) {
      return;
    }
    let result;
    for (const node of symbol.declarations) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */ || isJSConstructor(node) || isTypeAlias(node)) {
        const declaration ***REMOVED*** node;
        result ***REMOVED*** appendTypeParameters(result, getEffectiveTypeParameterDeclarations(declaration));
      }
    }
    return result;
  }
  function getTypeParametersOfClassOrInterface(symbol) {
    return concatenate(getOuterTypeParametersOfClassOrInterface(symbol), getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol));
  }
  function isMixinConstructorType(type) {
    const signatures ***REMOVED*** getSignaturesOfType(type, 1 /* Construct */);
    if (signatures.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const s ***REMOVED*** signatures[0];
      if (!s.typeParameters && s.parameters.length ***REMOVED******REMOVED******REMOVED*** 1 && signatureHasRestParameter(s)) {
        const paramType ***REMOVED*** getTypeOfParameter(s.parameters[0]);
        return isTypeAny(paramType) || getElementTypeOfArrayType(paramType) ***REMOVED******REMOVED******REMOVED*** anyType;
      }
    }
    return false;
  }
  function isConstructorType(type) {
    if (getSignaturesOfType(type, 1 /* Construct */).length > 0) {
      return true;
    }
    if (type.flags & 8650752 /* TypeVariable */) {
      const constraint ***REMOVED*** getBaseConstraintOfType(type);
      return !!constraint && isMixinConstructorType(constraint);
    }
    return false;
  }
  function getBaseTypeNodeOfClass(type) {
    const decl ***REMOVED*** getClassLikeDeclarationOfSymbol(type.symbol);
    return decl && getEffectiveBaseTypeNode(decl);
  }
  function getConstructorsForTypeArguments(type, typeArgumentNodes, location) {
    const typeArgCount ***REMOVED*** length(typeArgumentNodes);
    const isJavascript ***REMOVED*** isInJSFile(location);
    return filter(getSignaturesOfType(type, 1 /* Construct */), (sig) ***REMOVED***> (isJavascript || typeArgCount >***REMOVED*** getMinTypeArgumentCount(sig.typeParameters)) && typeArgCount <***REMOVED*** length(sig.typeParameters));
  }
  function getInstantiatedConstructorsForTypeArguments(type, typeArgumentNodes, location) {
    const signatures ***REMOVED*** getConstructorsForTypeArguments(type, typeArgumentNodes, location);
    const typeArguments ***REMOVED*** map(typeArgumentNodes, getTypeFromTypeNode);
    return sameMap(signatures, (sig) ***REMOVED***> some(sig.typeParameters) ? getSignatureInstantiation(sig, typeArguments, isInJSFile(location)) : sig);
  }
  function getBaseConstructorTypeOfClass(type) {
    if (!type.resolvedBaseConstructorType) {
      const decl ***REMOVED*** getClassLikeDeclarationOfSymbol(type.symbol);
      const extended ***REMOVED*** decl && getEffectiveBaseTypeNode(decl);
      const baseTypeNode ***REMOVED*** getBaseTypeNodeOfClass(type);
      if (!baseTypeNode) {
        return type.resolvedBaseConstructorType ***REMOVED*** undefinedType;
      }
      if (!pushTypeResolution(type, 1 /* ResolvedBaseConstructorType */)) {
        return errorType;
      }
      const baseConstructorType ***REMOVED*** checkExpression(baseTypeNode.expression);
      if (extended && baseTypeNode !***REMOVED******REMOVED*** extended) {
        Debug.assert(!extended.typeArguments);
        checkExpression(extended.expression);
      }
      if (baseConstructorType.flags & (524288 /* Object */ | 2097152 /* Intersection */)) {
        resolveStructuredTypeMembers(baseConstructorType);
      }
      if (!popTypeResolution()) {
        error(type.symbol.valueDeclaration, Diagnostics._0_is_referenced_directly_or_indirectly_in_its_own_base_expression, symbolToString(type.symbol));
        return type.resolvedBaseConstructorType ?? (type.resolvedBaseConstructorType ***REMOVED*** errorType);
      }
      if (!(baseConstructorType.flags & 1 /* Any */) && baseConstructorType !***REMOVED******REMOVED*** nullWideningType && !isConstructorType(baseConstructorType)) {
        const err ***REMOVED*** error(baseTypeNode.expression, Diagnostics.Type_0_is_not_a_constructor_function_type, typeToString(baseConstructorType));
        if (baseConstructorType.flags & 262144 /* TypeParameter */) {
          const constraint ***REMOVED*** getConstraintFromTypeParameter(baseConstructorType);
          let ctorReturn ***REMOVED*** unknownType;
          if (constraint) {
            const ctorSig ***REMOVED*** getSignaturesOfType(constraint, 1 /* Construct */);
            if (ctorSig[0]) {
              ctorReturn ***REMOVED*** getReturnTypeOfSignature(ctorSig[0]);
            }
          }
          if (baseConstructorType.symbol.declarations) {
            addRelatedInfo(err, createDiagnosticForNode(baseConstructorType.symbol.declarations[0], Diagnostics.Did_you_mean_for_0_to_be_constrained_to_type_new_args_Colon_any_1, symbolToString(baseConstructorType.symbol), typeToString(ctorReturn)));
          }
        }
        return type.resolvedBaseConstructorType ?? (type.resolvedBaseConstructorType ***REMOVED*** errorType);
      }
      type.resolvedBaseConstructorType ?? (type.resolvedBaseConstructorType ***REMOVED*** baseConstructorType);
    }
    return type.resolvedBaseConstructorType;
  }
  function getImplementsTypes(type) {
    let resolvedImplementsTypes ***REMOVED*** emptyArray;
    if (type.symbol.declarations) {
      for (const declaration of type.symbol.declarations) {
        const implementsTypeNodes ***REMOVED*** getEffectiveImplementsTypeNodes(declaration);
        if (!implementsTypeNodes) continue;
        for (const node of implementsTypeNodes) {
          const implementsType ***REMOVED*** getTypeFromTypeNode(node);
          if (!isErrorType(implementsType)) {
            if (resolvedImplementsTypes ***REMOVED******REMOVED******REMOVED*** emptyArray) {
              resolvedImplementsTypes ***REMOVED*** [implementsType];
            } else {
              resolvedImplementsTypes.push(implementsType);
            }
          }
        }
      }
    }
    return resolvedImplementsTypes;
  }
  function reportCircularBaseType(node, type) {
    error(node, Diagnostics.Type_0_recursively_references_itself_as_a_base_type, typeToString(
      type,
      /*enclosingDeclaration*/
      void 0,
      2 /* WriteArrayAsGenericType */
    ));
  }
  function getBaseTypes(type) {
    if (!type.baseTypesResolved) {
      if (pushTypeResolution(type, 6 /* ResolvedBaseTypes */)) {
        if (type.objectFlags & 8 /* Tuple */) {
          type.resolvedBaseTypes ***REMOVED*** [getTupleBaseType(type)];
        } else if (type.symbol.flags & (32 /* Class */ | 64 /* Interface */)) {
          if (type.symbol.flags & 32 /* Class */) {
            resolveBaseTypesOfClass(type);
          }
          if (type.symbol.flags & 64 /* Interface */) {
            resolveBaseTypesOfInterface(type);
          }
        } else {
          Debug.fail("type must be class or interface");
        }
        if (!popTypeResolution() && type.symbol.declarations) {
          for (const declaration of type.symbol.declarations) {
            if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || declaration.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
              reportCircularBaseType(declaration, type);
            }
          }
        }
      }
      type.baseTypesResolved ***REMOVED*** true;
    }
    return type.resolvedBaseTypes;
  }
  function getTupleBaseType(type) {
    const elementTypes ***REMOVED*** sameMap(type.typeParameters, (t, i) ***REMOVED***> type.elementFlags[i] & 8 /* Variadic */ ? getIndexedAccessType(t, numberType) : t);
    return createArrayType(getUnionType(elementTypes || emptyArray), type.readonly);
  }
  function resolveBaseTypesOfClass(type) {
    type.resolvedBaseTypes ***REMOVED*** resolvingEmptyArray;
    const baseConstructorType ***REMOVED*** getApparentType(getBaseConstructorTypeOfClass(type));
    if (!(baseConstructorType.flags & (524288 /* Object */ | 2097152 /* Intersection */ | 1 /* Any */))) {
      return type.resolvedBaseTypes ***REMOVED*** emptyArray;
    }
    const baseTypeNode ***REMOVED*** getBaseTypeNodeOfClass(type);
    let baseType;
    const originalBaseType ***REMOVED*** baseConstructorType.symbol ? getDeclaredTypeOfSymbol(baseConstructorType.symbol) : void 0;
    if (baseConstructorType.symbol && baseConstructorType.symbol.flags & 32 /* Class */ && areAllOuterTypeParametersApplied(originalBaseType)) {
      baseType ***REMOVED*** getTypeFromClassOrInterfaceReference(baseTypeNode, baseConstructorType.symbol);
    } else if (baseConstructorType.flags & 1 /* Any */) {
      baseType ***REMOVED*** baseConstructorType;
    } else {
      const constructors ***REMOVED*** getInstantiatedConstructorsForTypeArguments(baseConstructorType, baseTypeNode.typeArguments, baseTypeNode);
      if (!constructors.length) {
        error(baseTypeNode.expression, Diagnostics.No_base_constructor_has_the_specified_number_of_type_arguments);
        return type.resolvedBaseTypes ***REMOVED*** emptyArray;
      }
      baseType ***REMOVED*** getReturnTypeOfSignature(constructors[0]);
    }
    if (isErrorType(baseType)) {
      return type.resolvedBaseTypes ***REMOVED*** emptyArray;
    }
    const reducedBaseType ***REMOVED*** getReducedType(baseType);
    if (!isValidBaseType(reducedBaseType)) {
      const elaboration ***REMOVED*** elaborateNeverIntersection(
        /*errorInfo*/
        void 0,
        baseType
      );
      const diagnostic ***REMOVED*** chainDiagnosticMessages(elaboration, Diagnostics.Base_constructor_return_type_0_is_not_an_object_type_or_intersection_of_object_types_with_statically_known_members, typeToString(reducedBaseType));
      diagnostics.add(createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(baseTypeNode.expression), baseTypeNode.expression, diagnostic));
      return type.resolvedBaseTypes ***REMOVED*** emptyArray;
    }
    if (type ***REMOVED******REMOVED******REMOVED*** reducedBaseType || hasBaseType(reducedBaseType, type)) {
      error(type.symbol.valueDeclaration, Diagnostics.Type_0_recursively_references_itself_as_a_base_type, typeToString(
        type,
        /*enclosingDeclaration*/
        void 0,
        2 /* WriteArrayAsGenericType */
      ));
      return type.resolvedBaseTypes ***REMOVED*** emptyArray;
    }
    if (type.resolvedBaseTypes ***REMOVED******REMOVED******REMOVED*** resolvingEmptyArray) {
      type.members ***REMOVED*** void 0;
    }
    return type.resolvedBaseTypes ***REMOVED*** [reducedBaseType];
  }
  function areAllOuterTypeParametersApplied(type) {
    const outerTypeParameters ***REMOVED*** type.outerTypeParameters;
    if (outerTypeParameters) {
      const last2 ***REMOVED*** outerTypeParameters.length - 1;
      const typeArguments ***REMOVED*** getTypeArguments(type);
      return outerTypeParameters[last2].symbol !***REMOVED******REMOVED*** typeArguments[last2].symbol;
    }
    return true;
  }
  function isValidBaseType(type) {
    if (type.flags & 262144 /* TypeParameter */) {
      const constraint ***REMOVED*** getBaseConstraintOfType(type);
      if (constraint) {
        return isValidBaseType(constraint);
      }
    }
    return !!(type.flags & (524288 /* Object */ | 67108864 /* NonPrimitive */ | 1 /* Any */) && !isGenericMappedType(type) || type.flags & 2097152 /* Intersection */ && every(type.types, isValidBaseType));
  }
  function resolveBaseTypesOfInterface(type) {
    type.resolvedBaseTypes ***REMOVED*** type.resolvedBaseTypes || emptyArray;
    if (type.symbol.declarations) {
      for (const declaration of type.symbol.declarations) {
        if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ && getInterfaceBaseTypeNodes(declaration)) {
          for (const node of getInterfaceBaseTypeNodes(declaration)) {
            const baseType ***REMOVED*** getReducedType(getTypeFromTypeNode(node));
            if (!isErrorType(baseType)) {
              if (isValidBaseType(baseType)) {
                if (type !***REMOVED******REMOVED*** baseType && !hasBaseType(baseType, type)) {
                  if (type.resolvedBaseTypes ***REMOVED******REMOVED******REMOVED*** emptyArray) {
                    type.resolvedBaseTypes ***REMOVED*** [baseType];
                  } else {
                    type.resolvedBaseTypes.push(baseType);
                  }
                } else {
                  reportCircularBaseType(declaration, type);
                }
              } else {
                error(node, Diagnostics.An_interface_can_only_extend_an_object_type_or_intersection_of_object_types_with_statically_known_members);
              }
            }
          }
        }
      }
    }
  }
  function isThislessInterface(symbol) {
    if (!symbol.declarations) {
      return true;
    }
    for (const declaration of symbol.declarations) {
      if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
        if (declaration.flags & 256 /* ContainsThis */) {
          return false;
        }
        const baseTypeNodes ***REMOVED*** getInterfaceBaseTypeNodes(declaration);
        if (baseTypeNodes) {
          for (const node of baseTypeNodes) {
            if (isEntityNameExpression(node.expression)) {
              const baseSymbol ***REMOVED*** resolveEntityName(
                node.expression,
                788968 /* Type */,
                /*ignoreErrors*/
                true
              );
              if (!baseSymbol || !(baseSymbol.flags & 64 /* Interface */) || getDeclaredTypeOfClassOrInterface(baseSymbol).thisType) {
                return false;
              }
            }
          }
        }
      }
    }
    return true;
  }
  function getDeclaredTypeOfClassOrInterface(symbol) {
    let links ***REMOVED*** getSymbolLinks(symbol);
    const originalLinks ***REMOVED*** links;
    if (!links.declaredType) {
      const kind ***REMOVED*** symbol.flags & 32 /* Class */ ? 1 /* Class */ : 2 /* Interface */;
      const merged ***REMOVED*** mergeJSSymbols(symbol, symbol.valueDeclaration && getAssignedClassSymbol(symbol.valueDeclaration));
      if (merged) {
        symbol ***REMOVED*** merged;
        links ***REMOVED*** merged.links;
      }
      const type ***REMOVED*** originalLinks.declaredType ***REMOVED*** links.declaredType ***REMOVED*** createObjectType(kind, symbol);
      const outerTypeParameters ***REMOVED*** getOuterTypeParametersOfClassOrInterface(symbol);
      const localTypeParameters ***REMOVED*** getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol);
      if (outerTypeParameters || localTypeParameters || kind ***REMOVED******REMOVED******REMOVED*** 1 /* Class */ || !isThislessInterface(symbol)) {
        type.objectFlags |***REMOVED*** 4 /* Reference */;
        type.typeParameters ***REMOVED*** concatenate(outerTypeParameters, localTypeParameters);
        type.outerTypeParameters ***REMOVED*** outerTypeParameters;
        type.localTypeParameters ***REMOVED*** localTypeParameters;
        type.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
        type.instantiations.set(getTypeListId(type.typeParameters), type);
        type.target ***REMOVED*** type;
        type.resolvedTypeArguments ***REMOVED*** type.typeParameters;
        type.thisType ***REMOVED*** createTypeParameter(symbol);
        type.thisType.isThisType ***REMOVED*** true;
        type.thisType.constraint ***REMOVED*** type;
      }
    }
    return links.declaredType;
  }
  function getDeclaredTypeOfTypeAlias(symbol) {
    var _a;
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.declaredType) {
      if (!pushTypeResolution(symbol, 2 /* DeclaredType */)) {
        return errorType;
      }
      const declaration ***REMOVED*** Debug.checkDefined((_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isTypeAlias), "Type alias symbol with no valid declaration found");
      const typeNode ***REMOVED*** isJSDocTypeAlias(declaration) ? declaration.typeExpression : declaration.type;
      let type ***REMOVED*** typeNode ? getTypeFromTypeNode(typeNode) : errorType;
      if (popTypeResolution()) {
        const typeParameters ***REMOVED*** getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol);
        if (typeParameters) {
          links.typeParameters ***REMOVED*** typeParameters;
          links.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
          links.instantiations.set(getTypeListId(typeParameters), type);
        }
        if (type ***REMOVED******REMOVED******REMOVED*** intrinsicMarkerType && symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "BuiltinIteratorReturn") {
          type ***REMOVED*** getBuiltinIteratorReturnType();
        }
      } else {
        type ***REMOVED*** errorType;
        if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 341 /* JSDocEnumTag */) {
          error(declaration.typeExpression.type, Diagnostics.Type_alias_0_circularly_references_itself, symbolToString(symbol));
        } else {
          error(isNamedDeclaration(declaration) ? declaration.name || declaration : declaration, Diagnostics.Type_alias_0_circularly_references_itself, symbolToString(symbol));
        }
      }
      links.declaredType ?? (links.declaredType ***REMOVED*** type);
    }
    return links.declaredType;
  }
  function getBaseTypeOfEnumLikeType(type) {
    return type.flags & 1056 /* EnumLike */ && type.symbol.flags & 8 /* EnumMember */ ? getDeclaredTypeOfSymbol(getParentOfSymbol(type.symbol)) : type;
  }
  function getDeclaredTypeOfEnum(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.declaredType) {
      const memberTypeList ***REMOVED*** [];
      if (symbol.declarations) {
        for (const declaration of symbol.declarations) {
          if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */) {
            for (const member of declaration.members) {
              if (hasBindableName(member)) {
                const memberSymbol ***REMOVED*** getSymbolOfDeclaration(member);
                const value ***REMOVED*** getEnumMemberValue(member).value;
                const memberType ***REMOVED*** getFreshTypeOfLiteralType(
                  value !***REMOVED******REMOVED*** void 0 ? getEnumLiteralType(value, getSymbolId(symbol), memberSymbol) : createComputedEnumType(memberSymbol)
                );
                getSymbolLinks(memberSymbol).declaredType ***REMOVED*** memberType;
                memberTypeList.push(getRegularTypeOfLiteralType(memberType));
              }
            }
          }
        }
      }
      const enumType ***REMOVED*** memberTypeList.length ? getUnionType(
        memberTypeList,
        1 /* Literal */,
        symbol,
        /*aliasTypeArguments*/
        void 0
      ) : createComputedEnumType(symbol);
      if (enumType.flags & 1048576 /* Union */) {
        enumType.flags |***REMOVED*** 1024 /* EnumLiteral */;
        enumType.symbol ***REMOVED*** symbol;
      }
      links.declaredType ***REMOVED*** enumType;
    }
    return links.declaredType;
  }
  function createComputedEnumType(symbol) {
    const regularType ***REMOVED*** createTypeWithSymbol(32 /* Enum */, symbol);
    const freshType ***REMOVED*** createTypeWithSymbol(32 /* Enum */, symbol);
    regularType.regularType ***REMOVED*** regularType;
    regularType.freshType ***REMOVED*** freshType;
    freshType.regularType ***REMOVED*** regularType;
    freshType.freshType ***REMOVED*** freshType;
    return regularType;
  }
  function getDeclaredTypeOfEnumMember(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.declaredType) {
      const enumType ***REMOVED*** getDeclaredTypeOfEnum(getParentOfSymbol(symbol));
      if (!links.declaredType) {
        links.declaredType ***REMOVED*** enumType;
      }
    }
    return links.declaredType;
  }
  function getDeclaredTypeOfTypeParameter(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    return links.declaredType || (links.declaredType ***REMOVED*** createTypeParameter(symbol));
  }
  function getDeclaredTypeOfAlias(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    return links.declaredType || (links.declaredType ***REMOVED*** getDeclaredTypeOfSymbol(resolveAlias(symbol)));
  }
  function getDeclaredTypeOfSymbol(symbol) {
    return tryGetDeclaredTypeOfSymbol(symbol) || errorType;
  }
  function tryGetDeclaredTypeOfSymbol(symbol) {
    if (symbol.flags & (32 /* Class */ | 64 /* Interface */)) {
      return getDeclaredTypeOfClassOrInterface(symbol);
    }
    if (symbol.flags & 524288 /* TypeAlias */) {
      return getDeclaredTypeOfTypeAlias(symbol);
    }
    if (symbol.flags & 262144 /* TypeParameter */) {
      return getDeclaredTypeOfTypeParameter(symbol);
    }
    if (symbol.flags & 384 /* Enum */) {
      return getDeclaredTypeOfEnum(symbol);
    }
    if (symbol.flags & 8 /* EnumMember */) {
      return getDeclaredTypeOfEnumMember(symbol);
    }
    if (symbol.flags & 2097152 /* Alias */) {
      return getDeclaredTypeOfAlias(symbol);
    }
    return void 0;
  }
  function isThislessType(node) {
    switch (node.kind) {
      case 133 /* AnyKeyword */:
      case 159 /* UnknownKeyword */:
      case 154 /* StringKeyword */:
      case 150 /* NumberKeyword */:
      case 163 /* BigIntKeyword */:
      case 136 /* BooleanKeyword */:
      case 155 /* SymbolKeyword */:
      case 151 /* ObjectKeyword */:
      case 116 /* VoidKeyword */:
      case 157 /* UndefinedKeyword */:
      case 146 /* NeverKeyword */:
      case 202 /* LiteralType */:
        return true;
      case 189 /* ArrayType */:
        return isThislessType(node.elementType);
      case 184 /* TypeReference */:
        return !node.typeArguments || node.typeArguments.every(isThislessType);
    }
    return false;
  }
  function isThislessTypeParameter(node) {
    const constraint ***REMOVED*** getEffectiveConstraintOfTypeParameter(node);
    return !constraint || isThislessType(constraint);
  }
  function isThislessVariableLikeDeclaration(node) {
    const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    return typeNode ? isThislessType(typeNode) : !hasInitializer(node);
  }
  function isThislessFunctionLikeDeclaration(node) {
    const returnType ***REMOVED*** getEffectiveReturnTypeNode(node);
    const typeParameters ***REMOVED*** getEffectiveTypeParameterDeclarations(node);
    return (node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || !!returnType && isThislessType(returnType)) && node.parameters.every(isThislessVariableLikeDeclaration) && typeParameters.every(isThislessTypeParameter);
  }
  function isThisless(symbol) {
    if (symbol.declarations && symbol.declarations.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const declaration ***REMOVED*** symbol.declarations[0];
      if (declaration) {
        switch (declaration.kind) {
          case 173 /* PropertyDeclaration */:
          case 172 /* PropertySignature */:
            return isThislessVariableLikeDeclaration(declaration);
          case 175 /* MethodDeclaration */:
          case 174 /* MethodSignature */:
          case 177 /* Constructor */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
            return isThislessFunctionLikeDeclaration(declaration);
        }
      }
    }
    return false;
  }
  function createInstantiatedSymbolTable(symbols, mapper, mappingThisOnly) {
    const result ***REMOVED*** createSymbolTable();
    for (const symbol of symbols) {
      result.set(symbol.escapedName, mappingThisOnly && isThisless(symbol) ? symbol : instantiateSymbol(symbol, mapper));
    }
    return result;
  }
  function addInheritedMembers(symbols, baseSymbols) {
    for (const base of baseSymbols) {
      if (isStaticPrivateIdentifierProperty(base)) {
        continue;
      }
      const derived ***REMOVED*** symbols.get(base.escapedName);
      if (!derived || derived.valueDeclaration && isBinaryExpression(derived.valueDeclaration) && !isConstructorDeclaredProperty(derived) && !getContainingClassStaticBlock(derived.valueDeclaration)) {
        symbols.set(base.escapedName, base);
        symbols.set(base.escapedName, base);
      }
    }
  }
  function isStaticPrivateIdentifierProperty(s) {
    return !!s.valueDeclaration && isPrivateIdentifierClassElementDeclaration(s.valueDeclaration) && isStatic(s.valueDeclaration);
  }
  function resolveDeclaredMembers(type) {
    if (!type.declaredProperties) {
      const symbol ***REMOVED*** type.symbol;
      const members ***REMOVED*** getMembersOfSymbol(symbol);
      type.declaredProperties ***REMOVED*** getNamedMembers(members);
      type.declaredCallSignatures ***REMOVED*** emptyArray;
      type.declaredConstructSignatures ***REMOVED*** emptyArray;
      type.declaredIndexInfos ***REMOVED*** emptyArray;
      type.declaredCallSignatures ***REMOVED*** getSignaturesOfSymbol(members.get("__call" /* Call */));
      type.declaredConstructSignatures ***REMOVED*** getSignaturesOfSymbol(members.get("__new" /* New */));
      type.declaredIndexInfos ***REMOVED*** getIndexInfosOfSymbol(symbol);
    }
    return type;
  }
  function isLateBindableName(node) {
    return isLateBindableAST(node) && isTypeUsableAsPropertyName(isComputedPropertyName(node) ? checkComputedPropertyName(node) : checkExpressionCached(node.argumentExpression));
  }
  function isLateBindableIndexSignature(node) {
    return isLateBindableAST(node) && isTypeUsableAsIndexSignature(isComputedPropertyName(node) ? checkComputedPropertyName(node) : checkExpressionCached(node.argumentExpression));
  }
  function isLateBindableAST(node) {
    if (!isComputedPropertyName(node) && !isElementAccessExpression(node)) {
      return false;
    }
    const expr ***REMOVED*** isComputedPropertyName(node) ? node.expression : node.argumentExpression;
    return isEntityNameExpression(expr);
  }
  function isTypeUsableAsIndexSignature(type) {
    return isTypeAssignableTo(type, stringNumberSymbolType);
  }
  function isLateBoundName(name) {
    return name.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && name.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */ && name.charCodeAt(2) ***REMOVED******REMOVED******REMOVED*** 64 /* at */;
  }
  function hasLateBindableName(node) {
    const name ***REMOVED*** getNameOfDeclaration(node);
    return !!name && isLateBindableName(name);
  }
  function hasLateBindableIndexSignature(node) {
    const name ***REMOVED*** getNameOfDeclaration(node);
    return !!name && isLateBindableIndexSignature(name);
  }
  function hasBindableName(node) {
    return !hasDynamicName(node) || hasLateBindableName(node);
  }
  function isNonBindableDynamicName(node) {
    return isDynamicName(node) && !isLateBindableName(node);
  }
  function addDeclarationToLateBoundSymbol(symbol, member, symbolFlags) {
    Debug.assert(!!(getCheckFlags(symbol) & 4096 /* Late */), "Expected a late-bound symbol.");
    symbol.flags |***REMOVED*** symbolFlags;
    getSymbolLinks(member.symbol).lateSymbol ***REMOVED*** symbol;
    if (!symbol.declarations) {
      symbol.declarations ***REMOVED*** [member];
    } else if (!member.symbol.isReplaceableByMethod) {
      symbol.declarations.push(member);
    }
    if (symbolFlags & 111551 /* Value */) {
      setValueDeclaration(symbol, member);
    }
  }
  function lateBindMember(parent, earlySymbols, lateSymbols, decl) {
    Debug.assert(!!decl.symbol, "The member is expected to have a symbol.");
    const links ***REMOVED*** getNodeLinks(decl);
    if (!links.resolvedSymbol) {
      links.resolvedSymbol ***REMOVED*** decl.symbol;
      const declName ***REMOVED*** isBinaryExpression(decl) ? decl.left : decl.name;
      const type ***REMOVED*** isElementAccessExpression(declName) ? checkExpressionCached(declName.argumentExpression) : checkComputedPropertyName(declName);
      if (isTypeUsableAsPropertyName(type)) {
        const memberName ***REMOVED*** getPropertyNameFromType(type);
        const symbolFlags ***REMOVED*** decl.symbol.flags;
        let lateSymbol ***REMOVED*** lateSymbols.get(memberName);
        if (!lateSymbol) lateSymbols.set(memberName, lateSymbol ***REMOVED*** createSymbol(0 /* None */, memberName, 4096 /* Late */));
        const earlySymbol ***REMOVED*** earlySymbols && earlySymbols.get(memberName);
        if (!(parent.flags & 32 /* Class */) && lateSymbol.flags & getExcludedSymbolFlags(symbolFlags)) {
          const declarations ***REMOVED*** earlySymbol ? concatenate(earlySymbol.declarations, lateSymbol.declarations) : lateSymbol.declarations;
          const name ***REMOVED*** !(type.flags & 8192 /* UniqueESSymbol */) && unescapeLeadingUnderscores(memberName) || declarationNameToString(declName);
          forEach(declarations, (declaration) ***REMOVED***> error(getNameOfDeclaration(declaration) || declaration, Diagnostics.Property_0_was_also_declared_here, name));
          error(declName || decl, Diagnostics.Duplicate_property_0, name);
          lateSymbol ***REMOVED*** createSymbol(0 /* None */, memberName, 4096 /* Late */);
        }
        lateSymbol.links.nameType ***REMOVED*** type;
        addDeclarationToLateBoundSymbol(lateSymbol, decl, symbolFlags);
        if (lateSymbol.parent) {
          Debug.assert(lateSymbol.parent ***REMOVED******REMOVED******REMOVED*** parent, "Existing symbol parent should match new one");
        } else {
          lateSymbol.parent ***REMOVED*** parent;
        }
        return links.resolvedSymbol ***REMOVED*** lateSymbol;
      }
    }
    return links.resolvedSymbol;
  }
  function lateBindIndexSignature(parent, earlySymbols, lateSymbols, decl) {
    let indexSymbol ***REMOVED*** lateSymbols.get("__index" /* Index */);
    if (!indexSymbol) {
      const early ***REMOVED*** earlySymbols ***REMOVED******REMOVED*** null ? void 0 : earlySymbols.get("__index" /* Index */);
      if (!early) {
        indexSymbol ***REMOVED*** createSymbol(0 /* None */, "__index" /* Index */, 4096 /* Late */);
      } else {
        indexSymbol ***REMOVED*** cloneSymbol(early);
        indexSymbol.links.checkFlags |***REMOVED*** 4096 /* Late */;
      }
      lateSymbols.set("__index" /* Index */, indexSymbol);
    }
    if (!indexSymbol.declarations) {
      indexSymbol.declarations ***REMOVED*** [decl];
    } else if (!decl.symbol.isReplaceableByMethod) {
      indexSymbol.declarations.push(decl);
    }
  }
  function getResolvedMembersOrExportsOfSymbol(symbol, resolutionKind) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links[resolutionKind]) {
      const isStatic2 ***REMOVED*** resolutionKind ***REMOVED******REMOVED******REMOVED*** "resolvedExports" /* resolvedExports */;
      const earlySymbols ***REMOVED*** !isStatic2 ? symbol.members : symbol.flags & 1536 /* Module */ ? getExportsOfModuleWorker(symbol).exports : symbol.exports;
      links[resolutionKind] ***REMOVED*** earlySymbols || emptySymbols;
      const lateSymbols ***REMOVED*** createSymbolTable();
      for (const decl of symbol.declarations || emptyArray) {
        const members ***REMOVED*** getMembersOfDeclaration(decl);
        if (members) {
          for (const member of members) {
            if (isStatic2 ***REMOVED******REMOVED******REMOVED*** hasStaticModifier(member)) {
              if (hasLateBindableName(member)) {
                lateBindMember(symbol, earlySymbols, lateSymbols, member);
              } else if (hasLateBindableIndexSignature(member)) {
                lateBindIndexSignature(symbol, earlySymbols, lateSymbols, member);
              }
            }
          }
        }
      }
      const assignments ***REMOVED*** getFunctionExpressionParentSymbolOrSymbol(symbol).assignmentDeclarationMembers;
      if (assignments) {
        const decls ***REMOVED*** arrayFrom(assignments.values());
        for (const member of decls) {
          const assignmentKind ***REMOVED*** getAssignmentDeclarationKind(member);
          const isInstanceMember ***REMOVED*** assignmentKind ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */ || isBinaryExpression(member) && isPossiblyAliasedThisProperty(member, assignmentKind) || assignmentKind ***REMOVED******REMOVED******REMOVED*** 9 /* ObjectDefinePrototypeProperty */ || assignmentKind ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */;
          if (isStatic2 ***REMOVED******REMOVED******REMOVED*** !isInstanceMember) {
            if (hasLateBindableName(member)) {
              lateBindMember(symbol, earlySymbols, lateSymbols, member);
            }
          }
        }
      }
      let resolved ***REMOVED*** combineSymbolTables(earlySymbols, lateSymbols);
      if (symbol.flags & 33554432 /* Transient */ && links.cjsExportMerged && symbol.declarations) {
        for (const decl of symbol.declarations) {
          const original ***REMOVED*** getSymbolLinks(decl.symbol)[resolutionKind];
          if (!resolved) {
            resolved ***REMOVED*** original;
            continue;
          }
          if (!original) continue;
          original.forEach((s, name) ***REMOVED***> {
            const existing ***REMOVED*** resolved.get(name);
            if (!existing) resolved.set(name, s);
            else if (existing ***REMOVED******REMOVED******REMOVED*** s) return;
            else resolved.set(name, mergeSymbol(existing, s));
          });
        }
      }
      links[resolutionKind] ***REMOVED*** resolved || emptySymbols;
    }
    return links[resolutionKind];
  }
  function getMembersOfSymbol(symbol) {
    return symbol.flags & 6256 /* LateBindingContainer */ ? getResolvedMembersOrExportsOfSymbol(symbol, "resolvedMembers" /* resolvedMembers */) : symbol.members || emptySymbols;
  }
  function getLateBoundSymbol(symbol) {
    if (symbol.flags & 106500 /* ClassMember */ && symbol.escapedName ***REMOVED******REMOVED******REMOVED*** "__computed" /* Computed */) {
      const links ***REMOVED*** getSymbolLinks(symbol);
      if (!links.lateSymbol && some(symbol.declarations, hasLateBindableName)) {
        const parent ***REMOVED*** getMergedSymbol(symbol.parent);
        if (some(symbol.declarations, hasStaticModifier)) {
          getExportsOfSymbol(parent);
        } else {
          getMembersOfSymbol(parent);
        }
      }
      return links.lateSymbol || (links.lateSymbol ***REMOVED*** symbol);
    }
    return symbol;
  }
  function getTypeWithThisArgument(type, thisArgument, needApparentType) {
    if (getObjectFlags(type) & 4 /* Reference */) {
      const target ***REMOVED*** type.target;
      const typeArguments ***REMOVED*** getTypeArguments(type);
      return length(target.typeParameters) ***REMOVED******REMOVED******REMOVED*** length(typeArguments) ? createTypeReference(target, concatenate(typeArguments, [thisArgument || target.thisType])) : type;
    } else if (type.flags & 2097152 /* Intersection */) {
      const types ***REMOVED*** sameMap(type.types, (t) ***REMOVED***> getTypeWithThisArgument(t, thisArgument, needApparentType));
      return types !***REMOVED******REMOVED*** type.types ? getIntersectionType(types) : type;
    }
    return needApparentType ? getApparentType(type) : type;
  }
  function resolveObjectTypeMembers(type, source, typeParameters, typeArguments) {
    let mapper;
    let members;
    let callSignatures;
    let constructSignatures;
    let indexInfos;
    if (rangeEquals(typeParameters, typeArguments, 0, typeParameters.length)) {
      members ***REMOVED*** source.symbol ? getMembersOfSymbol(source.symbol) : createSymbolTable(source.declaredProperties);
      callSignatures ***REMOVED*** source.declaredCallSignatures;
      constructSignatures ***REMOVED*** source.declaredConstructSignatures;
      indexInfos ***REMOVED*** source.declaredIndexInfos;
    } else {
      mapper ***REMOVED*** createTypeMapper(typeParameters, typeArguments);
      members ***REMOVED*** createInstantiatedSymbolTable(
        source.declaredProperties,
        mapper,
        /*mappingThisOnly*/
        typeParameters.length ***REMOVED******REMOVED******REMOVED*** 1
      );
      callSignatures ***REMOVED*** instantiateSignatures(source.declaredCallSignatures, mapper);
      constructSignatures ***REMOVED*** instantiateSignatures(source.declaredConstructSignatures, mapper);
      indexInfos ***REMOVED*** instantiateIndexInfos(source.declaredIndexInfos, mapper);
    }
    const baseTypes ***REMOVED*** getBaseTypes(source);
    if (baseTypes.length) {
      if (source.symbol && members ***REMOVED******REMOVED******REMOVED*** getMembersOfSymbol(source.symbol)) {
        const symbolTable ***REMOVED*** createSymbolTable(source.declaredProperties);
        const sourceIndex ***REMOVED*** getIndexSymbol(source.symbol);
        if (sourceIndex) {
          symbolTable.set("__index" /* Index */, sourceIndex);
        }
        members ***REMOVED*** symbolTable;
      }
      setStructuredTypeMembers(type, members, callSignatures, constructSignatures, indexInfos);
      const thisArgument ***REMOVED*** lastOrUndefined(typeArguments);
      for (const baseType of baseTypes) {
        const instantiatedBaseType ***REMOVED*** thisArgument ? getTypeWithThisArgument(instantiateType(baseType, mapper), thisArgument) : baseType;
        addInheritedMembers(members, getPropertiesOfType(instantiatedBaseType));
        callSignatures ***REMOVED*** concatenate(callSignatures, getSignaturesOfType(instantiatedBaseType, 0 /* Call */));
        constructSignatures ***REMOVED*** concatenate(constructSignatures, getSignaturesOfType(instantiatedBaseType, 1 /* Construct */));
        const inheritedIndexInfos ***REMOVED*** instantiatedBaseType !***REMOVED******REMOVED*** anyType ? getIndexInfosOfType(instantiatedBaseType) : [anyBaseTypeIndexInfo];
        indexInfos ***REMOVED*** concatenate(indexInfos, filter(inheritedIndexInfos, (info) ***REMOVED***> !findIndexInfo(indexInfos, info.keyType)));
      }
    }
    setStructuredTypeMembers(type, members, callSignatures, constructSignatures, indexInfos);
  }
  function resolveClassOrInterfaceMembers(type) {
    resolveObjectTypeMembers(type, resolveDeclaredMembers(type), emptyArray, emptyArray);
  }
  function resolveTypeReferenceMembers(type) {
    const source ***REMOVED*** resolveDeclaredMembers(type.target);
    const typeParameters ***REMOVED*** concatenate(source.typeParameters, [source.thisType]);
    const typeArguments ***REMOVED*** getTypeArguments(type);
    const paddedTypeArguments ***REMOVED*** typeArguments.length ***REMOVED******REMOVED******REMOVED*** typeParameters.length ? typeArguments : concatenate(typeArguments, [type]);
    resolveObjectTypeMembers(type, source, typeParameters, paddedTypeArguments);
  }
  function createSignature(declaration, typeParameters, thisParameter, parameters, resolvedReturnType, resolvedTypePredicate, minArgumentCount, flags) {
    const sig ***REMOVED*** new Signature5(checker, flags);
    sig.declaration ***REMOVED*** declaration;
    sig.typeParameters ***REMOVED*** typeParameters;
    sig.parameters ***REMOVED*** parameters;
    sig.thisParameter ***REMOVED*** thisParameter;
    sig.resolvedReturnType ***REMOVED*** resolvedReturnType;
    sig.resolvedTypePredicate ***REMOVED*** resolvedTypePredicate;
    sig.minArgumentCount ***REMOVED*** minArgumentCount;
    sig.resolvedMinArgumentCount ***REMOVED*** void 0;
    sig.target ***REMOVED*** void 0;
    sig.mapper ***REMOVED*** void 0;
    sig.compositeSignatures ***REMOVED*** void 0;
    sig.compositeKind ***REMOVED*** void 0;
    return sig;
  }
  function cloneSignature(sig) {
    const result ***REMOVED*** createSignature(
      sig.declaration,
      sig.typeParameters,
      sig.thisParameter,
      sig.parameters,
      /*resolvedReturnType*/
      void 0,
      /*resolvedTypePredicate*/
      void 0,
      sig.minArgumentCount,
      sig.flags & 167 /* PropagatingFlags */
    );
    result.target ***REMOVED*** sig.target;
    result.mapper ***REMOVED*** sig.mapper;
    result.compositeSignatures ***REMOVED*** sig.compositeSignatures;
    result.compositeKind ***REMOVED*** sig.compositeKind;
    return result;
  }
  function createUnionSignature(signature, unionSignatures) {
    const result ***REMOVED*** cloneSignature(signature);
    result.compositeSignatures ***REMOVED*** unionSignatures;
    result.compositeKind ***REMOVED*** 1048576 /* Union */;
    result.target ***REMOVED*** void 0;
    result.mapper ***REMOVED*** void 0;
    return result;
  }
  function getOptionalCallSignature(signature, callChainFlags) {
    if ((signature.flags & 24 /* CallChainFlags */) ***REMOVED******REMOVED******REMOVED*** callChainFlags) {
      return signature;
    }
    if (!signature.optionalCallSignatureCache) {
      signature.optionalCallSignatureCache ***REMOVED*** {};
    }
    const key ***REMOVED*** callChainFlags ***REMOVED******REMOVED******REMOVED*** 8 /* IsInnerCallChain */ ? "inner" : "outer";
    return signature.optionalCallSignatureCache[key] || (signature.optionalCallSignatureCache[key] ***REMOVED*** createOptionalCallSignature(signature, callChainFlags));
  }
  function createOptionalCallSignature(signature, callChainFlags) {
    Debug.assert(callChainFlags ***REMOVED******REMOVED******REMOVED*** 8 /* IsInnerCallChain */ || callChainFlags ***REMOVED******REMOVED******REMOVED*** 16 /* IsOuterCallChain */, "An optional call signature can either be for an inner call chain or an outer call chain, but not both.");
    const result ***REMOVED*** cloneSignature(signature);
    result.flags |***REMOVED*** callChainFlags;
    return result;
  }
  function getExpandedParameters(sig, skipUnionExpanding) {
    if (signatureHasRestParameter(sig)) {
      const restIndex ***REMOVED*** sig.parameters.length - 1;
      const restSymbol ***REMOVED*** sig.parameters[restIndex];
      const restType ***REMOVED*** getTypeOfSymbol(restSymbol);
      if (isTupleType(restType)) {
        return [expandSignatureParametersWithTupleMembers(restType, restIndex, restSymbol)];
      } else if (!skipUnionExpanding && restType.flags & 1048576 /* Union */ && every(restType.types, isTupleType)) {
        return map(restType.types, (t) ***REMOVED***> expandSignatureParametersWithTupleMembers(t, restIndex, restSymbol));
      }
    }
    return [sig.parameters];
    function expandSignatureParametersWithTupleMembers(restType, restIndex, restSymbol) {
      const elementTypes ***REMOVED*** getTypeArguments(restType);
      const associatedNames ***REMOVED*** getUniqAssociatedNamesFromTupleType(restType, restSymbol);
      const restParams ***REMOVED*** map(elementTypes, (t, i) ***REMOVED***> {
        const name ***REMOVED*** associatedNames && associatedNames[i] ? associatedNames[i] : getParameterNameAtPosition(sig, restIndex + i, restType);
        const flags ***REMOVED*** restType.target.elementFlags[i];
        const checkFlags ***REMOVED*** flags & 12 /* Variable */ ? 32768 /* RestParameter */ : flags & 2 /* Optional */ ? 16384 /* OptionalParameter */ : 0;
        const symbol ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, name, checkFlags);
        symbol.links.type ***REMOVED*** flags & 4 /* Rest */ ? createArrayType(t) : t;
        return symbol;
      });
      return concatenate(sig.parameters.slice(0, restIndex), restParams);
    }
    function getUniqAssociatedNamesFromTupleType(type, restSymbol) {
      const names ***REMOVED*** map(type.target.labeledElementDeclarations, (labeledElement, i) ***REMOVED***> getTupleElementLabel(labeledElement, i, type.target.elementFlags[i], restSymbol));
      if (names) {
        const duplicates ***REMOVED*** [];
        const uniqueNames ***REMOVED*** /* @__PURE__ */ new Set();
        for (let i ***REMOVED*** 0; i < names.length; i++) {
          const name ***REMOVED*** names[i];
          if (!tryAddToSet(uniqueNames, name)) {
            duplicates.push(i);
          }
        }
        const counters ***REMOVED*** /* @__PURE__ */ new Map();
        for (const i of duplicates) {
          let counter ***REMOVED*** counters.get(names[i]) ?? 1;
          let name;
          while (!tryAddToSet(uniqueNames, name ***REMOVED*** `${names[i]}_${counter}`)) {
            counter++;
          }
          names[i] ***REMOVED*** name;
          counters.set(names[i], counter + 1);
        }
      }
      return names;
    }
  }
  function getDefaultConstructSignatures(classType) {
    const baseConstructorType ***REMOVED*** getBaseConstructorTypeOfClass(classType);
    const baseSignatures ***REMOVED*** getSignaturesOfType(baseConstructorType, 1 /* Construct */);
    const declaration ***REMOVED*** getClassLikeDeclarationOfSymbol(classType.symbol);
    const isAbstract ***REMOVED*** !!declaration && hasSyntacticModifier(declaration, 64 /* Abstract */);
    if (baseSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return [createSignature(
        /*declaration*/
        void 0,
        classType.localTypeParameters,
        /*thisParameter*/
        void 0,
        emptyArray,
        classType,
        /*resolvedTypePredicate*/
        void 0,
        0,
        isAbstract ? 4 /* Abstract */ : 0 /* None */
      )];
    }
    const baseTypeNode ***REMOVED*** getBaseTypeNodeOfClass(classType);
    const isJavaScript ***REMOVED*** isInJSFile(baseTypeNode);
    const typeArguments ***REMOVED*** typeArgumentsFromTypeReferenceNode(baseTypeNode);
    const typeArgCount ***REMOVED*** length(typeArguments);
    const result ***REMOVED*** [];
    for (const baseSig of baseSignatures) {
      const minTypeArgumentCount ***REMOVED*** getMinTypeArgumentCount(baseSig.typeParameters);
      const typeParamCount ***REMOVED*** length(baseSig.typeParameters);
      if (isJavaScript || typeArgCount >***REMOVED*** minTypeArgumentCount && typeArgCount <***REMOVED*** typeParamCount) {
        const sig ***REMOVED*** typeParamCount ? createSignatureInstantiation(baseSig, fillMissingTypeArguments(typeArguments, baseSig.typeParameters, minTypeArgumentCount, isJavaScript)) : cloneSignature(baseSig);
        sig.typeParameters ***REMOVED*** classType.localTypeParameters;
        sig.resolvedReturnType ***REMOVED*** classType;
        sig.flags ***REMOVED*** isAbstract ? sig.flags | 4 /* Abstract */ : sig.flags & ~4 /* Abstract */;
        result.push(sig);
      }
    }
    return result;
  }
  function findMatchingSignature(signatureList, signature, partialMatch, ignoreThisTypes, ignoreReturnTypes) {
    for (const s of signatureList) {
      if (compareSignaturesIdentical(s, signature, partialMatch, ignoreThisTypes, ignoreReturnTypes, partialMatch ? compareTypesSubtypeOf : compareTypesIdentical)) {
        return s;
      }
    }
  }
  function findMatchingSignatures(signatureLists, signature, listIndex) {
    if (signature.typeParameters) {
      if (listIndex > 0) {
        return void 0;
      }
      for (let i ***REMOVED*** 1; i < signatureLists.length; i++) {
        if (!findMatchingSignature(
          signatureLists[i],
          signature,
          /*partialMatch*/
          false,
          /*ignoreThisTypes*/
          false,
          /*ignoreReturnTypes*/
          false
        )) {
          return void 0;
        }
      }
      return [signature];
    }
    let result;
    for (let i ***REMOVED*** 0; i < signatureLists.length; i++) {
      const match ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** listIndex ? signature : findMatchingSignature(
        signatureLists[i],
        signature,
        /*partialMatch*/
        false,
        /*ignoreThisTypes*/
        false,
        /*ignoreReturnTypes*/
        true
      ) || findMatchingSignature(
        signatureLists[i],
        signature,
        /*partialMatch*/
        true,
        /*ignoreThisTypes*/
        false,
        /*ignoreReturnTypes*/
        true
      );
      if (!match) {
        return void 0;
      }
      result ***REMOVED*** appendIfUnique(result, match);
    }
    return result;
  }
  function getUnionSignatures(signatureLists) {
    let result;
    let indexWithLengthOverOne;
    for (let i ***REMOVED*** 0; i < signatureLists.length; i++) {
      if (signatureLists[i].length ***REMOVED******REMOVED******REMOVED*** 0) return emptyArray;
      if (signatureLists[i].length > 1) {
        indexWithLengthOverOne ***REMOVED*** indexWithLengthOverOne ***REMOVED******REMOVED******REMOVED*** void 0 ? i : -1;
      }
      for (const signature of signatureLists[i]) {
        if (!result || !findMatchingSignature(
          result,
          signature,
          /*partialMatch*/
          false,
          /*ignoreThisTypes*/
          false,
          /*ignoreReturnTypes*/
          true
        )) {
          const unionSignatures ***REMOVED*** findMatchingSignatures(signatureLists, signature, i);
          if (unionSignatures) {
            let s ***REMOVED*** signature;
            if (unionSignatures.length > 1) {
              let thisParameter ***REMOVED*** signature.thisParameter;
              const firstThisParameterOfUnionSignatures ***REMOVED*** forEach(unionSignatures, (sig) ***REMOVED***> sig.thisParameter);
              if (firstThisParameterOfUnionSignatures) {
                const thisType ***REMOVED*** getIntersectionType(mapDefined(unionSignatures, (sig) ***REMOVED***> sig.thisParameter && getTypeOfSymbol(sig.thisParameter)));
                thisParameter ***REMOVED*** createSymbolWithType(firstThisParameterOfUnionSignatures, thisType);
              }
              s ***REMOVED*** createUnionSignature(signature, unionSignatures);
              s.thisParameter ***REMOVED*** thisParameter;
            }
            (result || (result ***REMOVED*** [])).push(s);
          }
        }
      }
    }
    if (!length(result) && indexWithLengthOverOne !***REMOVED******REMOVED*** -1) {
      const masterList ***REMOVED*** signatureLists[indexWithLengthOverOne !***REMOVED******REMOVED*** void 0 ? indexWithLengthOverOne : 0];
      let results ***REMOVED*** masterList.slice();
      for (const signatures of signatureLists) {
        if (signatures !***REMOVED******REMOVED*** masterList) {
          const signature ***REMOVED*** signatures[0];
          Debug.assert(!!signature, "getUnionSignatures bails early on empty signature lists and should not have empty lists on second pass");
          results ***REMOVED*** !!signature.typeParameters && some(results, (s) ***REMOVED***> !!s.typeParameters && !compareTypeParametersIdentical(signature.typeParameters, s.typeParameters)) ? void 0 : map(results, (sig) ***REMOVED***> combineSignaturesOfUnionMembers(sig, signature));
          if (!results) {
            break;
          }
        }
      }
      result ***REMOVED*** results;
    }
    return result || emptyArray;
  }
  function compareTypeParametersIdentical(sourceParams, targetParams) {
    if (length(sourceParams) !***REMOVED******REMOVED*** length(targetParams)) {
      return false;
    }
    if (!sourceParams || !targetParams) {
      return true;
    }
    const mapper ***REMOVED*** createTypeMapper(targetParams, sourceParams);
    for (let i ***REMOVED*** 0; i < sourceParams.length; i++) {
      const source ***REMOVED*** sourceParams[i];
      const target ***REMOVED*** targetParams[i];
      if (source ***REMOVED******REMOVED******REMOVED*** target) continue;
      if (!isTypeIdenticalTo(getConstraintFromTypeParameter(source) || unknownType, instantiateType(getConstraintFromTypeParameter(target) || unknownType, mapper))) return false;
    }
    return true;
  }
  function combineUnionThisParam(left, right, mapper) {
    if (!left || !right) {
      return left || right;
    }
    const thisType ***REMOVED*** getIntersectionType([getTypeOfSymbol(left), instantiateType(getTypeOfSymbol(right), mapper)]);
    return createSymbolWithType(left, thisType);
  }
  function combineUnionParameters(left, right, mapper) {
    const leftCount ***REMOVED*** getParameterCount(left);
    const rightCount ***REMOVED*** getParameterCount(right);
    const longest ***REMOVED*** leftCount >***REMOVED*** rightCount ? left : right;
    const shorter ***REMOVED*** longest ***REMOVED******REMOVED******REMOVED*** left ? right : left;
    const longestCount ***REMOVED*** longest ***REMOVED******REMOVED******REMOVED*** left ? leftCount : rightCount;
    const eitherHasEffectiveRest ***REMOVED*** hasEffectiveRestParameter(left) || hasEffectiveRestParameter(right);
    const needsExtraRestElement ***REMOVED*** eitherHasEffectiveRest && !hasEffectiveRestParameter(longest);
    const params ***REMOVED*** new Array(longestCount + (needsExtraRestElement ? 1 : 0));
    for (let i ***REMOVED*** 0; i < longestCount; i++) {
      let longestParamType ***REMOVED*** tryGetTypeAtPosition(longest, i);
      if (longest ***REMOVED******REMOVED******REMOVED*** right) {
        longestParamType ***REMOVED*** instantiateType(longestParamType, mapper);
      }
      let shorterParamType ***REMOVED*** tryGetTypeAtPosition(shorter, i) || unknownType;
      if (shorter ***REMOVED******REMOVED******REMOVED*** right) {
        shorterParamType ***REMOVED*** instantiateType(shorterParamType, mapper);
      }
      const unionParamType ***REMOVED*** getIntersectionType([longestParamType, shorterParamType]);
      const isRestParam ***REMOVED*** eitherHasEffectiveRest && !needsExtraRestElement && i ***REMOVED******REMOVED******REMOVED*** longestCount - 1;
      const isOptional ***REMOVED*** i >***REMOVED*** getMinArgumentCount(longest) && i >***REMOVED*** getMinArgumentCount(shorter);
      const leftName ***REMOVED*** i >***REMOVED*** leftCount ? void 0 : getParameterNameAtPosition(left, i);
      const rightName ***REMOVED*** i >***REMOVED*** rightCount ? void 0 : getParameterNameAtPosition(right, i);
      const paramName ***REMOVED*** leftName ***REMOVED******REMOVED******REMOVED*** rightName ? leftName : !leftName ? rightName : !rightName ? leftName : void 0;
      const paramSymbol ***REMOVED*** createSymbol(
        1 /* FunctionScopedVariable */ | (isOptional && !isRestParam ? 16777216 /* Optional */ : 0),
        paramName || `arg${i}`,
        isRestParam ? 32768 /* RestParameter */ : isOptional ? 16384 /* OptionalParameter */ : 0
      );
      paramSymbol.links.type ***REMOVED*** isRestParam ? createArrayType(unionParamType) : unionParamType;
      params[i] ***REMOVED*** paramSymbol;
    }
    if (needsExtraRestElement) {
      const restParamSymbol ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, "args", 32768 /* RestParameter */);
      restParamSymbol.links.type ***REMOVED*** createArrayType(getTypeAtPosition(shorter, longestCount));
      if (shorter ***REMOVED******REMOVED******REMOVED*** right) {
        restParamSymbol.links.type ***REMOVED*** instantiateType(restParamSymbol.links.type, mapper);
      }
      params[longestCount] ***REMOVED*** restParamSymbol;
    }
    return params;
  }
  function combineSignaturesOfUnionMembers(left, right) {
    const typeParams ***REMOVED*** left.typeParameters || right.typeParameters;
    let paramMapper;
    if (left.typeParameters && right.typeParameters) {
      paramMapper ***REMOVED*** createTypeMapper(right.typeParameters, left.typeParameters);
    }
    let flags ***REMOVED*** (left.flags | right.flags) & (167 /* PropagatingFlags */ & ~1 /* HasRestParameter */);
    const declaration ***REMOVED*** left.declaration;
    const params ***REMOVED*** combineUnionParameters(left, right, paramMapper);
    const lastParam ***REMOVED*** lastOrUndefined(params);
    if (lastParam && getCheckFlags(lastParam) & 32768 /* RestParameter */) {
      flags |***REMOVED*** 1 /* HasRestParameter */;
    }
    const thisParam ***REMOVED*** combineUnionThisParam(left.thisParameter, right.thisParameter, paramMapper);
    const minArgCount ***REMOVED*** Math.max(left.minArgumentCount, right.minArgumentCount);
    const result ***REMOVED*** createSignature(
      declaration,
      typeParams,
      thisParam,
      params,
      /*resolvedReturnType*/
      void 0,
      /*resolvedTypePredicate*/
      void 0,
      minArgCount,
      flags
    );
    result.compositeKind ***REMOVED*** 1048576 /* Union */;
    result.compositeSignatures ***REMOVED*** concatenate(left.compositeKind !***REMOVED******REMOVED*** 2097152 /* Intersection */ && left.compositeSignatures || [left], [right]);
    if (paramMapper) {
      result.mapper ***REMOVED*** left.compositeKind !***REMOVED******REMOVED*** 2097152 /* Intersection */ && left.mapper && left.compositeSignatures ? combineTypeMappers(left.mapper, paramMapper) : paramMapper;
    } else if (left.compositeKind !***REMOVED******REMOVED*** 2097152 /* Intersection */ && left.mapper && left.compositeSignatures) {
      result.mapper ***REMOVED*** left.mapper;
    }
    return result;
  }
  function getUnionIndexInfos(types) {
    const sourceInfos ***REMOVED*** getIndexInfosOfType(types[0]);
    if (sourceInfos) {
      const result ***REMOVED*** [];
      for (const info of sourceInfos) {
        const indexType ***REMOVED*** info.keyType;
        if (every(types, (t) ***REMOVED***> !!getIndexInfoOfType(t, indexType))) {
          result.push(createIndexInfo(indexType, getUnionType(map(types, (t) ***REMOVED***> getIndexTypeOfType(t, indexType))), some(types, (t) ***REMOVED***> getIndexInfoOfType(t, indexType).isReadonly)));
        }
      }
      return result;
    }
    return emptyArray;
  }
  function resolveUnionTypeMembers(type) {
    const callSignatures ***REMOVED*** getUnionSignatures(map(type.types, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** globalFunctionType ? [unknownSignature] : getSignaturesOfType(t, 0 /* Call */)));
    const constructSignatures ***REMOVED*** getUnionSignatures(map(type.types, (t) ***REMOVED***> getSignaturesOfType(t, 1 /* Construct */)));
    const indexInfos ***REMOVED*** getUnionIndexInfos(type.types);
    setStructuredTypeMembers(type, emptySymbols, callSignatures, constructSignatures, indexInfos);
  }
  function intersectTypes(type1, type2) {
    return !type1 ? type2 : !type2 ? type1 : getIntersectionType([type1, type2]);
  }
  function findMixins(types) {
    const constructorTypeCount ***REMOVED*** countWhere(types, (t) ***REMOVED***> getSignaturesOfType(t, 1 /* Construct */).length > 0);
    const mixinFlags ***REMOVED*** map(types, isMixinConstructorType);
    if (constructorTypeCount > 0 && constructorTypeCount ***REMOVED******REMOVED******REMOVED*** countWhere(mixinFlags, (b) ***REMOVED***> b)) {
      const firstMixinIndex ***REMOVED*** mixinFlags.indexOf(
        /*searchElement*/
        true
      );
      mixinFlags[firstMixinIndex] ***REMOVED*** false;
    }
    return mixinFlags;
  }
  function includeMixinType(type, types, mixinFlags, index) {
    const mixedTypes ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < types.length; i++) {
      if (i ***REMOVED******REMOVED******REMOVED*** index) {
        mixedTypes.push(type);
      } else if (mixinFlags[i]) {
        mixedTypes.push(getReturnTypeOfSignature(getSignaturesOfType(types[i], 1 /* Construct */)[0]));
      }
    }
    return getIntersectionType(mixedTypes);
  }
  function resolveIntersectionTypeMembers(type) {
    let callSignatures;
    let constructSignatures;
    let indexInfos;
    const types ***REMOVED*** type.types;
    const mixinFlags ***REMOVED*** findMixins(types);
    const mixinCount ***REMOVED*** countWhere(mixinFlags, (b) ***REMOVED***> b);
    for (let i ***REMOVED*** 0; i < types.length; i++) {
      const t ***REMOVED*** type.types[i];
      if (!mixinFlags[i]) {
        let signatures ***REMOVED*** getSignaturesOfType(t, 1 /* Construct */);
        if (signatures.length && mixinCount > 0) {
          signatures ***REMOVED*** map(signatures, (s) ***REMOVED***> {
            const clone ***REMOVED*** cloneSignature(s);
            clone.resolvedReturnType ***REMOVED*** includeMixinType(getReturnTypeOfSignature(s), types, mixinFlags, i);
            return clone;
          });
        }
        constructSignatures ***REMOVED*** appendSignatures(constructSignatures, signatures);
      }
      callSignatures ***REMOVED*** appendSignatures(callSignatures, getSignaturesOfType(t, 0 /* Call */));
      indexInfos ***REMOVED*** reduceLeft(getIndexInfosOfType(t), (infos, newInfo) ***REMOVED***> appendIndexInfo(
        infos,
        newInfo,
        /*union*/
        false
      ), indexInfos);
    }
    setStructuredTypeMembers(type, emptySymbols, callSignatures || emptyArray, constructSignatures || emptyArray, indexInfos || emptyArray);
  }
  function appendSignatures(signatures, newSignatures) {
    for (const sig of newSignatures) {
      if (!signatures || every(signatures, (s) ***REMOVED***> !compareSignaturesIdentical(
        s,
        sig,
        /*partialMatch*/
        false,
        /*ignoreThisTypes*/
        false,
        /*ignoreReturnTypes*/
        false,
        compareTypesIdentical
      ))) {
        signatures ***REMOVED*** append(signatures, sig);
      }
    }
    return signatures;
  }
  function appendIndexInfo(indexInfos, newInfo, union) {
    if (indexInfos) {
      for (let i ***REMOVED*** 0; i < indexInfos.length; i++) {
        const info ***REMOVED*** indexInfos[i];
        if (info.keyType ***REMOVED******REMOVED******REMOVED*** newInfo.keyType) {
          indexInfos[i] ***REMOVED*** createIndexInfo(info.keyType, union ? getUnionType([info.type, newInfo.type]) : getIntersectionType([info.type, newInfo.type]), union ? info.isReadonly || newInfo.isReadonly : info.isReadonly && newInfo.isReadonly);
          return indexInfos;
        }
      }
    }
    return append(indexInfos, newInfo);
  }
  function resolveAnonymousTypeMembers(type) {
    if (type.target) {
      setStructuredTypeMembers(type, emptySymbols, emptyArray, emptyArray, emptyArray);
      const members2 ***REMOVED*** createInstantiatedSymbolTable(
        getPropertiesOfObjectType(type.target),
        type.mapper,
        /*mappingThisOnly*/
        false
      );
      const callSignatures ***REMOVED*** instantiateSignatures(getSignaturesOfType(type.target, 0 /* Call */), type.mapper);
      const constructSignatures ***REMOVED*** instantiateSignatures(getSignaturesOfType(type.target, 1 /* Construct */), type.mapper);
      const indexInfos2 ***REMOVED*** instantiateIndexInfos(getIndexInfosOfType(type.target), type.mapper);
      setStructuredTypeMembers(type, members2, callSignatures, constructSignatures, indexInfos2);
      return;
    }
    const symbol ***REMOVED*** getMergedSymbol(type.symbol);
    if (symbol.flags & 2048 /* TypeLiteral */) {
      setStructuredTypeMembers(type, emptySymbols, emptyArray, emptyArray, emptyArray);
      const members2 ***REMOVED*** getMembersOfSymbol(symbol);
      const callSignatures ***REMOVED*** getSignaturesOfSymbol(members2.get("__call" /* Call */));
      const constructSignatures ***REMOVED*** getSignaturesOfSymbol(members2.get("__new" /* New */));
      const indexInfos2 ***REMOVED*** getIndexInfosOfSymbol(symbol);
      setStructuredTypeMembers(type, members2, callSignatures, constructSignatures, indexInfos2);
      return;
    }
    let members ***REMOVED*** getExportsOfSymbol(symbol);
    let indexInfos;
    if (symbol ***REMOVED******REMOVED******REMOVED*** globalThisSymbol) {
      const varsOnly ***REMOVED*** /* @__PURE__ */ new Map();
      members.forEach((p) ***REMOVED***> {
        var _a;
        if (!(p.flags & 418 /* BlockScoped */) && !(p.flags & 512 /* ValueModule */ && ((_a ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.length) && every(p.declarations, isAmbientModule))) {
          varsOnly.set(p.escapedName, p);
        }
      });
      members ***REMOVED*** varsOnly;
    }
    let baseConstructorIndexInfo;
    setStructuredTypeMembers(type, members, emptyArray, emptyArray, emptyArray);
    if (symbol.flags & 32 /* Class */) {
      const classType ***REMOVED*** getDeclaredTypeOfClassOrInterface(symbol);
      const baseConstructorType ***REMOVED*** getBaseConstructorTypeOfClass(classType);
      if (baseConstructorType.flags & (524288 /* Object */ | 2097152 /* Intersection */ | 8650752 /* TypeVariable */)) {
        members ***REMOVED*** createSymbolTable(getNamedOrIndexSignatureMembers(members));
        addInheritedMembers(members, getPropertiesOfType(baseConstructorType));
      } else if (baseConstructorType ***REMOVED******REMOVED******REMOVED*** anyType) {
        baseConstructorIndexInfo ***REMOVED*** anyBaseTypeIndexInfo;
      }
    }
    const indexSymbol ***REMOVED*** getIndexSymbolFromSymbolTable(members);
    if (indexSymbol) {
      indexInfos ***REMOVED*** getIndexInfosOfIndexSymbol(indexSymbol, arrayFrom(members.values()));
    } else {
      if (baseConstructorIndexInfo) {
        indexInfos ***REMOVED*** append(indexInfos, baseConstructorIndexInfo);
      }
      if (symbol.flags & 384 /* Enum */ && (getDeclaredTypeOfSymbol(symbol).flags & 32 /* Enum */ || some(type.properties, (prop) ***REMOVED***> !!(getTypeOfSymbol(prop).flags & 296 /* NumberLike */)))) {
        indexInfos ***REMOVED*** append(indexInfos, enumNumberIndexInfo);
      }
    }
    setStructuredTypeMembers(type, members, emptyArray, emptyArray, indexInfos || emptyArray);
    if (symbol.flags & (16 /* Function */ | 8192 /* Method */)) {
      type.callSignatures ***REMOVED*** getSignaturesOfSymbol(symbol);
    }
    if (symbol.flags & 32 /* Class */) {
      const classType ***REMOVED*** getDeclaredTypeOfClassOrInterface(symbol);
      let constructSignatures ***REMOVED*** symbol.members ? getSignaturesOfSymbol(symbol.members.get("__constructor" /* Constructor */)) : emptyArray;
      if (symbol.flags & 16 /* Function */) {
        constructSignatures ***REMOVED*** addRange(
          constructSignatures.slice(),
          mapDefined(
            type.callSignatures,
            (sig) ***REMOVED***> isJSConstructor(sig.declaration) ? createSignature(
              sig.declaration,
              sig.typeParameters,
              sig.thisParameter,
              sig.parameters,
              classType,
              /*resolvedTypePredicate*/
              void 0,
              sig.minArgumentCount,
              sig.flags & 167 /* PropagatingFlags */
            ) : void 0
          )
        );
      }
      if (!constructSignatures.length) {
        constructSignatures ***REMOVED*** getDefaultConstructSignatures(classType);
      }
      type.constructSignatures ***REMOVED*** constructSignatures;
    }
  }
  function replaceIndexedAccess(instantiable, type, replacement) {
    return instantiateType(instantiable, createTypeMapper([type.indexType, type.objectType], [getNumberLiteralType(0), createTupleType([replacement])]));
  }
  function getLimitedConstraint(type) {
    const constraint ***REMOVED*** getConstraintTypeFromMappedType(type.mappedType);
    if (!(constraint.flags & 1048576 /* Union */ || constraint.flags & 2097152 /* Intersection */)) {
      return;
    }
    const origin ***REMOVED*** constraint.flags & 1048576 /* Union */ ? constraint.origin : constraint;
    if (!origin || !(origin.flags & 2097152 /* Intersection */)) {
      return;
    }
    const limitedConstraint ***REMOVED*** getIntersectionType(origin.types.filter((t) ***REMOVED***> t !***REMOVED******REMOVED*** type.constraintType));
    return limitedConstraint !***REMOVED******REMOVED*** neverType ? limitedConstraint : void 0;
  }
  function resolveReverseMappedTypeMembers(type) {
    const indexInfo ***REMOVED*** getIndexInfoOfType(type.source, stringType);
    const modifiers ***REMOVED*** getMappedTypeModifiers(type.mappedType);
    const readonlyMask ***REMOVED*** modifiers & 1 /* IncludeReadonly */ ? false : true;
    const optionalMask ***REMOVED*** modifiers & 4 /* IncludeOptional */ ? 0 : 16777216 /* Optional */;
    const indexInfos ***REMOVED*** indexInfo ? [createIndexInfo(stringType, inferReverseMappedType(indexInfo.type, type.mappedType, type.constraintType) || unknownType, readonlyMask && indexInfo.isReadonly)] : emptyArray;
    const members ***REMOVED*** createSymbolTable();
    const limitedConstraint ***REMOVED*** getLimitedConstraint(type);
    for (const prop of getPropertiesOfType(type.source)) {
      if (limitedConstraint) {
        const propertyNameType ***REMOVED*** getLiteralTypeFromProperty(prop, 8576 /* StringOrNumberLiteralOrUnique */);
        if (!isTypeAssignableTo(propertyNameType, limitedConstraint)) {
          continue;
        }
      }
      const checkFlags ***REMOVED*** 8192 /* ReverseMapped */ | (readonlyMask && isReadonlySymbol(prop) ? 8 /* Readonly */ : 0);
      const inferredProp ***REMOVED*** createSymbol(4 /* Property */ | prop.flags & optionalMask, prop.escapedName, checkFlags);
      inferredProp.declarations ***REMOVED*** prop.declarations;
      inferredProp.links.nameType ***REMOVED*** getSymbolLinks(prop).nameType;
      inferredProp.links.propertyType ***REMOVED*** getTypeOfSymbol(prop);
      if (type.constraintType.type.flags & 8388608 /* IndexedAccess */ && type.constraintType.type.objectType.flags & 262144 /* TypeParameter */ && type.constraintType.type.indexType.flags & 262144 /* TypeParameter */) {
        const newTypeParam ***REMOVED*** type.constraintType.type.objectType;
        const newMappedType ***REMOVED*** replaceIndexedAccess(type.mappedType, type.constraintType.type, newTypeParam);
        inferredProp.links.mappedType ***REMOVED*** newMappedType;
        inferredProp.links.constraintType ***REMOVED*** getIndexType(newTypeParam);
      } else {
        inferredProp.links.mappedType ***REMOVED*** type.mappedType;
        inferredProp.links.constraintType ***REMOVED*** type.constraintType;
      }
      members.set(prop.escapedName, inferredProp);
    }
    setStructuredTypeMembers(type, members, emptyArray, emptyArray, indexInfos);
  }
  function getLowerBoundOfKeyType(type) {
    if (type.flags & 4194304 /* Index */) {
      const t ***REMOVED*** getApparentType(type.type);
      return isGenericTupleType(t) ? getKnownKeysOfTupleType(t) : getIndexType(t);
    }
    if (type.flags & 16777216 /* Conditional */) {
      if (type.root.isDistributive) {
        const checkType ***REMOVED*** type.checkType;
        const constraint ***REMOVED*** getLowerBoundOfKeyType(checkType);
        if (constraint !***REMOVED******REMOVED*** checkType) {
          return getConditionalTypeInstantiation(
            type,
            prependTypeMapping(type.root.checkType, constraint, type.mapper),
            /*forConstraint*/
            false
          );
        }
      }
      return type;
    }
    if (type.flags & 1048576 /* Union */) {
      return mapType(
        type,
        getLowerBoundOfKeyType,
        /*noReductions*/
        true
      );
    }
    if (type.flags & 2097152 /* Intersection */) {
      const types ***REMOVED*** type.types;
      if (types.length ***REMOVED******REMOVED******REMOVED*** 2 && !!(types[0].flags & (4 /* String */ | 8 /* Number */ | 64 /* BigInt */)) && types[1] ***REMOVED******REMOVED******REMOVED*** emptyTypeLiteralType) {
        return type;
      }
      return getIntersectionType(sameMap(type.types, getLowerBoundOfKeyType));
    }
    return type;
  }
  function getIsLateCheckFlag(s) {
    return getCheckFlags(s) & 4096 /* Late */;
  }
  function forEachMappedTypePropertyKeyTypeAndIndexSignatureKeyType(type, include, stringsOnly, cb) {
    for (const prop of getPropertiesOfType(type)) {
      cb(getLiteralTypeFromProperty(prop, include));
    }
    if (type.flags & 1 /* Any */) {
      cb(stringType);
    } else {
      for (const info of getIndexInfosOfType(type)) {
        if (!stringsOnly || info.keyType.flags & (4 /* String */ | 134217728 /* TemplateLiteral */)) {
          cb(info.keyType);
        }
      }
    }
  }
  function resolveMappedTypeMembers(type) {
    const members ***REMOVED*** createSymbolTable();
    let indexInfos;
    setStructuredTypeMembers(type, emptySymbols, emptyArray, emptyArray, emptyArray);
    const typeParameter ***REMOVED*** getTypeParameterFromMappedType(type);
    const constraintType ***REMOVED*** getConstraintTypeFromMappedType(type);
    const mappedType ***REMOVED*** type.target || type;
    const nameType ***REMOVED*** getNameTypeFromMappedType(mappedType);
    const shouldLinkPropDeclarations ***REMOVED*** getMappedTypeNameTypeKind(mappedType) !***REMOVED******REMOVED*** 2 /* Remapping */;
    const templateType ***REMOVED*** getTemplateTypeFromMappedType(mappedType);
    const modifiersType ***REMOVED*** getApparentType(getModifiersTypeFromMappedType(type));
    const templateModifiers ***REMOVED*** getMappedTypeModifiers(type);
    const include ***REMOVED*** 8576 /* StringOrNumberLiteralOrUnique */;
    if (isMappedTypeWithKeyofConstraintDeclaration(type)) {
      forEachMappedTypePropertyKeyTypeAndIndexSignatureKeyType(
        modifiersType,
        include,
        /*stringsOnly*/
        false,
        addMemberForKeyType
      );
    } else {
      forEachType(getLowerBoundOfKeyType(constraintType), addMemberForKeyType);
    }
    setStructuredTypeMembers(type, members, emptyArray, emptyArray, indexInfos || emptyArray);
    function addMemberForKeyType(keyType) {
      const propNameType ***REMOVED*** nameType ? instantiateType(nameType, appendTypeMapping(type.mapper, typeParameter, keyType)) : keyType;
      forEachType(propNameType, (t) ***REMOVED***> addMemberForKeyTypeWorker(keyType, t));
    }
    function addMemberForKeyTypeWorker(keyType, propNameType) {
      if (isTypeUsableAsPropertyName(propNameType)) {
        const propName ***REMOVED*** getPropertyNameFromType(propNameType);
        const existingProp ***REMOVED*** members.get(propName);
        if (existingProp) {
          existingProp.links.nameType ***REMOVED*** getUnionType([existingProp.links.nameType, propNameType]);
          existingProp.links.keyType ***REMOVED*** getUnionType([existingProp.links.keyType, keyType]);
        } else {
          const modifiersProp ***REMOVED*** isTypeUsableAsPropertyName(keyType) ? getPropertyOfType(modifiersType, getPropertyNameFromType(keyType)) : void 0;
          const isOptional ***REMOVED*** !!(templateModifiers & 4 /* IncludeOptional */ || !(templateModifiers & 8 /* ExcludeOptional */) && modifiersProp && modifiersProp.flags & 16777216 /* Optional */);
          const isReadonly ***REMOVED*** !!(templateModifiers & 1 /* IncludeReadonly */ || !(templateModifiers & 2 /* ExcludeReadonly */) && modifiersProp && isReadonlySymbol(modifiersProp));
          const stripOptional ***REMOVED*** strictNullChecks && !isOptional && modifiersProp && modifiersProp.flags & 16777216 /* Optional */;
          const lateFlag ***REMOVED*** modifiersProp ? getIsLateCheckFlag(modifiersProp) : 0;
          const prop ***REMOVED*** createSymbol(4 /* Property */ | (isOptional ? 16777216 /* Optional */ : 0), propName, lateFlag | 262144 /* Mapped */ | (isReadonly ? 8 /* Readonly */ : 0) | (stripOptional ? 524288 /* StripOptional */ : 0));
          prop.links.mappedType ***REMOVED*** type;
          prop.links.nameType ***REMOVED*** propNameType;
          prop.links.keyType ***REMOVED*** keyType;
          if (modifiersProp) {
            prop.links.syntheticOrigin ***REMOVED*** modifiersProp;
            prop.declarations ***REMOVED*** shouldLinkPropDeclarations ? modifiersProp.declarations : void 0;
          }
          members.set(propName, prop);
        }
      } else if (isValidIndexKeyType(propNameType) || propNameType.flags & (1 /* Any */ | 32 /* Enum */)) {
        const indexKeyType ***REMOVED*** propNameType.flags & (1 /* Any */ | 4 /* String */) ? stringType : propNameType.flags & (8 /* Number */ | 32 /* Enum */) ? numberType : propNameType;
        const propType ***REMOVED*** instantiateType(templateType, appendTypeMapping(type.mapper, typeParameter, keyType));
        const modifiersIndexInfo ***REMOVED*** getApplicableIndexInfo(modifiersType, propNameType);
        const isReadonly ***REMOVED*** !!(templateModifiers & 1 /* IncludeReadonly */ || !(templateModifiers & 2 /* ExcludeReadonly */) && (modifiersIndexInfo ***REMOVED******REMOVED*** null ? void 0 : modifiersIndexInfo.isReadonly));
        const indexInfo ***REMOVED*** createIndexInfo(indexKeyType, propType, isReadonly);
        indexInfos ***REMOVED*** appendIndexInfo(
          indexInfos,
          indexInfo,
          /*union*/
          true
        );
      }
    }
  }
  function getTypeOfMappedSymbol(symbol) {
    var _a;
    if (!symbol.links.type) {
      const mappedType ***REMOVED*** symbol.links.mappedType;
      if (!pushTypeResolution(symbol, 0 /* Type */)) {
        mappedType.containsError ***REMOVED*** true;
        return errorType;
      }
      const templateType ***REMOVED*** getTemplateTypeFromMappedType(mappedType.target || mappedType);
      const mapper ***REMOVED*** appendTypeMapping(mappedType.mapper, getTypeParameterFromMappedType(mappedType), symbol.links.keyType);
      const propType ***REMOVED*** instantiateType(templateType, mapper);
      let type ***REMOVED*** strictNullChecks && symbol.flags & 16777216 /* Optional */ && !maybeTypeOfKind(propType, 32768 /* Undefined */ | 16384 /* Void */) ? getOptionalType(
        propType,
        /*isProperty*/
        true
      ) : symbol.links.checkFlags & 524288 /* StripOptional */ ? removeMissingOrUndefinedType(propType) : propType;
      if (!popTypeResolution()) {
        error(currentNode, Diagnostics.Type_of_property_0_circularly_references_itself_in_mapped_type_1, symbolToString(symbol), typeToString(mappedType));
        type ***REMOVED*** errorType;
      }
      (_a ***REMOVED*** symbol.links).type ?? (_a.type ***REMOVED*** type);
    }
    return symbol.links.type;
  }
  function getTypeParameterFromMappedType(type) {
    return type.typeParameter || (type.typeParameter ***REMOVED*** getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(type.declaration.typeParameter)));
  }
  function getConstraintTypeFromMappedType(type) {
    return type.constraintType || (type.constraintType ***REMOVED*** getConstraintOfTypeParameter(getTypeParameterFromMappedType(type)) || errorType);
  }
  function getNameTypeFromMappedType(type) {
    return type.declaration.nameType ? type.nameType || (type.nameType ***REMOVED*** instantiateType(getTypeFromTypeNode(type.declaration.nameType), type.mapper)) : void 0;
  }
  function getTemplateTypeFromMappedType(type) {
    return type.templateType || (type.templateType ***REMOVED*** type.declaration.type ? instantiateType(addOptionality(
      getTypeFromTypeNode(type.declaration.type),
      /*isProperty*/
      true,
      !!(getMappedTypeModifiers(type) & 4 /* IncludeOptional */)
    ), type.mapper) : errorType);
  }
  function getConstraintDeclarationForMappedType(type) {
    return getEffectiveConstraintOfTypeParameter(type.declaration.typeParameter);
  }
  function isMappedTypeWithKeyofConstraintDeclaration(type) {
    const constraintDeclaration ***REMOVED*** getConstraintDeclarationForMappedType(type);
    return constraintDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 199 /* TypeOperator */ && constraintDeclaration.operator ***REMOVED******REMOVED******REMOVED*** 143 /* KeyOfKeyword */;
  }
  function getModifiersTypeFromMappedType(type) {
    if (!type.modifiersType) {
      if (isMappedTypeWithKeyofConstraintDeclaration(type)) {
        type.modifiersType ***REMOVED*** instantiateType(getTypeFromTypeNode(getConstraintDeclarationForMappedType(type).type), type.mapper);
      } else {
        const declaredType ***REMOVED*** getTypeFromMappedTypeNode(type.declaration);
        const constraint ***REMOVED*** getConstraintTypeFromMappedType(declaredType);
        const extendedConstraint ***REMOVED*** constraint && constraint.flags & 262144 /* TypeParameter */ ? getConstraintOfTypeParameter(constraint) : constraint;
        type.modifiersType ***REMOVED*** extendedConstraint && extendedConstraint.flags & 4194304 /* Index */ ? instantiateType(extendedConstraint.type, type.mapper) : unknownType;
      }
    }
    return type.modifiersType;
  }
  function getMappedTypeModifiers(type) {
    const declaration ***REMOVED*** type.declaration;
    return (declaration.readonlyToken ? declaration.readonlyToken.kind ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ ? 2 /* ExcludeReadonly */ : 1 /* IncludeReadonly */ : 0) | (declaration.questionToken ? declaration.questionToken.kind ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ ? 8 /* ExcludeOptional */ : 4 /* IncludeOptional */ : 0);
  }
  function getMappedTypeOptionality(type) {
    const modifiers ***REMOVED*** getMappedTypeModifiers(type);
    return modifiers & 8 /* ExcludeOptional */ ? -1 : modifiers & 4 /* IncludeOptional */ ? 1 : 0;
  }
  function getCombinedMappedTypeOptionality(type) {
    if (getObjectFlags(type) & 32 /* Mapped */) {
      return getMappedTypeOptionality(type) || getCombinedMappedTypeOptionality(getModifiersTypeFromMappedType(type));
    }
    if (type.flags & 2097152 /* Intersection */) {
      const optionality ***REMOVED*** getCombinedMappedTypeOptionality(type.types[0]);
      return every(type.types, (t, i) ***REMOVED***> i ***REMOVED******REMOVED******REMOVED*** 0 || getCombinedMappedTypeOptionality(t) ***REMOVED******REMOVED******REMOVED*** optionality) ? optionality : 0;
    }
    return 0;
  }
  function isPartialMappedType(type) {
    return !!(getObjectFlags(type) & 32 /* Mapped */ && getMappedTypeModifiers(type) & 4 /* IncludeOptional */);
  }
  function isGenericMappedType(type) {
    if (getObjectFlags(type) & 32 /* Mapped */) {
      const constraint ***REMOVED*** getConstraintTypeFromMappedType(type);
      if (isGenericIndexType(constraint)) {
        return true;
      }
      const nameType ***REMOVED*** getNameTypeFromMappedType(type);
      if (nameType && isGenericIndexType(instantiateType(nameType, makeUnaryTypeMapper(getTypeParameterFromMappedType(type), constraint)))) {
        return true;
      }
    }
    return false;
  }
  function getMappedTypeNameTypeKind(type) {
    const nameType ***REMOVED*** getNameTypeFromMappedType(type);
    if (!nameType) {
      return 0 /* None */;
    }
    return isTypeAssignableTo(nameType, getTypeParameterFromMappedType(type)) ? 1 /* Filtering */ : 2 /* Remapping */;
  }
  function resolveStructuredTypeMembers(type) {
    if (!type.members) {
      if (type.flags & 524288 /* Object */) {
        if (type.objectFlags & 4 /* Reference */) {
          resolveTypeReferenceMembers(type);
        } else if (type.objectFlags & 3 /* ClassOrInterface */) {
          resolveClassOrInterfaceMembers(type);
        } else if (type.objectFlags & 1024 /* ReverseMapped */) {
          resolveReverseMappedTypeMembers(type);
        } else if (type.objectFlags & 16 /* Anonymous */) {
          resolveAnonymousTypeMembers(type);
        } else if (type.objectFlags & 32 /* Mapped */) {
          resolveMappedTypeMembers(type);
        } else {
          Debug.fail("Unhandled object type " + Debug.formatObjectFlags(type.objectFlags));
        }
      } else if (type.flags & 1048576 /* Union */) {
        resolveUnionTypeMembers(type);
      } else if (type.flags & 2097152 /* Intersection */) {
        resolveIntersectionTypeMembers(type);
      } else {
        Debug.fail("Unhandled type " + Debug.formatTypeFlags(type.flags));
      }
    }
    return type;
  }
  function getPropertiesOfObjectType(type) {
    if (type.flags & 524288 /* Object */) {
      return resolveStructuredTypeMembers(type).properties;
    }
    return emptyArray;
  }
  function getPropertyOfObjectType(type, name) {
    if (type.flags & 524288 /* Object */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      const symbol ***REMOVED*** resolved.members.get(name);
      if (symbol && symbolIsValue(symbol)) {
        return symbol;
      }
    }
  }
  function getPropertiesOfUnionOrIntersectionType(type) {
    if (!type.resolvedProperties) {
      const members ***REMOVED*** createSymbolTable();
      for (const current of type.types) {
        for (const prop of getPropertiesOfType(current)) {
          if (!members.has(prop.escapedName)) {
            const combinedProp ***REMOVED*** getPropertyOfUnionOrIntersectionType(
              type,
              prop.escapedName,
              /*skipObjectFunctionPropertyAugment*/
              !!(type.flags & 2097152 /* Intersection */)
            );
            if (combinedProp) {
              members.set(prop.escapedName, combinedProp);
            }
          }
        }
        if (type.flags & 1048576 /* Union */ && getIndexInfosOfType(current).length ***REMOVED******REMOVED******REMOVED*** 0) {
          break;
        }
      }
      type.resolvedProperties ***REMOVED*** getNamedMembers(members);
    }
    return type.resolvedProperties;
  }
  function getPropertiesOfType(type) {
    type ***REMOVED*** getReducedApparentType(type);
    return type.flags & 3145728 /* UnionOrIntersection */ ? getPropertiesOfUnionOrIntersectionType(type) : getPropertiesOfObjectType(type);
  }
  function forEachPropertyOfType(type, action) {
    type ***REMOVED*** getReducedApparentType(type);
    if (type.flags & 3670016 /* StructuredType */) {
      resolveStructuredTypeMembers(type).members.forEach((symbol, escapedName) ***REMOVED***> {
        if (isNamedMember(symbol, escapedName)) {
          action(symbol, escapedName);
        }
      });
    }
  }
  function isTypeInvalidDueToUnionDiscriminant(contextualType, obj) {
    const list ***REMOVED*** obj.properties;
    return list.some((property) ***REMOVED***> {
      const nameType ***REMOVED*** property.name && (isJsxNamespacedName(property.name) ? getStringLiteralType(getTextOfJsxAttributeName(property.name)) : getLiteralTypeFromPropertyName(property.name));
      const name ***REMOVED*** nameType && isTypeUsableAsPropertyName(nameType) ? getPropertyNameFromType(nameType) : void 0;
      const expected ***REMOVED*** name ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : getTypeOfPropertyOfType(contextualType, name);
      return !!expected && isLiteralType(expected) && !isTypeAssignableTo(getTypeOfNode(property), expected);
    });
  }
  function getAllPossiblePropertiesOfTypes(types) {
    const unionType ***REMOVED*** getUnionType(types);
    if (!(unionType.flags & 1048576 /* Union */)) {
      return getAugmentedPropertiesOfType(unionType);
    }
    const props ***REMOVED*** createSymbolTable();
    for (const memberType of types) {
      for (const { escapedName } of getAugmentedPropertiesOfType(memberType)) {
        if (!props.has(escapedName)) {
          const prop ***REMOVED*** createUnionOrIntersectionProperty(unionType, escapedName);
          if (prop) props.set(escapedName, prop);
        }
      }
    }
    return arrayFrom(props.values());
  }
  function getConstraintOfType(type) {
    return type.flags & 262144 /* TypeParameter */ ? getConstraintOfTypeParameter(type) : type.flags & 8388608 /* IndexedAccess */ ? getConstraintOfIndexedAccess(type) : type.flags & 16777216 /* Conditional */ ? getConstraintOfConditionalType(type) : getBaseConstraintOfType(type);
  }
  function getConstraintOfTypeParameter(typeParameter) {
    return hasNonCircularBaseConstraint(typeParameter) ? getConstraintFromTypeParameter(typeParameter) : void 0;
  }
  function isConstMappedType(type, depth) {
    const typeVariable ***REMOVED*** getHomomorphicTypeVariable(type);
    return !!typeVariable && isConstTypeVariable(typeVariable, depth);
  }
  function isConstTypeVariable(type, depth ***REMOVED*** 0) {
    var _a;
    return depth < 5 && !!(type && (type.flags & 262144 /* TypeParameter */ && some((_a ***REMOVED*** type.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.declarations, (d) ***REMOVED***> hasSyntacticModifier(d, 4096 /* Const */)) || type.flags & 3145728 /* UnionOrIntersection */ && some(type.types, (t) ***REMOVED***> isConstTypeVariable(t, depth)) || type.flags & 8388608 /* IndexedAccess */ && isConstTypeVariable(type.objectType, depth + 1) || type.flags & 16777216 /* Conditional */ && isConstTypeVariable(getConstraintOfConditionalType(type), depth + 1) || type.flags & 33554432 /* Substitution */ && isConstTypeVariable(type.baseType, depth) || getObjectFlags(type) & 32 /* Mapped */ && isConstMappedType(type, depth) || isGenericTupleType(type) && findIndex(getElementTypes(type), (t, i) ***REMOVED***> !!(type.target.elementFlags[i] & 8 /* Variadic */) && isConstTypeVariable(t, depth)) >***REMOVED*** 0));
  }
  function getConstraintOfIndexedAccess(type) {
    return hasNonCircularBaseConstraint(type) ? getConstraintFromIndexedAccess(type) : void 0;
  }
  function getSimplifiedTypeOrConstraint(type) {
    const simplified ***REMOVED*** getSimplifiedType(
      type,
      /*writing*/
      false
    );
    return simplified !***REMOVED******REMOVED*** type ? simplified : getConstraintOfType(type);
  }
  function getConstraintFromIndexedAccess(type) {
    if (isMappedTypeGenericIndexedAccess(type)) {
      return substituteIndexedMappedType(type.objectType, type.indexType);
    }
    const indexConstraint ***REMOVED*** getSimplifiedTypeOrConstraint(type.indexType);
    if (indexConstraint && indexConstraint !***REMOVED******REMOVED*** type.indexType) {
      const indexedAccess ***REMOVED*** getIndexedAccessTypeOrUndefined(type.objectType, indexConstraint, type.accessFlags);
      if (indexedAccess) {
        return indexedAccess;
      }
    }
    const objectConstraint ***REMOVED*** getSimplifiedTypeOrConstraint(type.objectType);
    if (objectConstraint && objectConstraint !***REMOVED******REMOVED*** type.objectType) {
      return getIndexedAccessTypeOrUndefined(objectConstraint, type.indexType, type.accessFlags);
    }
    return void 0;
  }
  function getDefaultConstraintOfConditionalType(type) {
    if (!type.resolvedDefaultConstraint) {
      const trueConstraint ***REMOVED*** getInferredTrueTypeFromConditionalType(type);
      const falseConstraint ***REMOVED*** getFalseTypeFromConditionalType(type);
      type.resolvedDefaultConstraint ***REMOVED*** isTypeAny(trueConstraint) ? falseConstraint : isTypeAny(falseConstraint) ? trueConstraint : getUnionType([trueConstraint, falseConstraint]);
    }
    return type.resolvedDefaultConstraint;
  }
  function getConstraintOfDistributiveConditionalType(type) {
    if (type.resolvedConstraintOfDistributive !***REMOVED******REMOVED*** void 0) {
      return type.resolvedConstraintOfDistributive || void 0;
    }
    if (type.root.isDistributive && type.restrictiveInstantiation !***REMOVED******REMOVED*** type) {
      const simplified ***REMOVED*** getSimplifiedType(
        type.checkType,
        /*writing*/
        false
      );
      const constraint ***REMOVED*** simplified ***REMOVED******REMOVED******REMOVED*** type.checkType ? getConstraintOfType(simplified) : simplified;
      if (constraint && constraint !***REMOVED******REMOVED*** type.checkType) {
        const instantiated ***REMOVED*** getConditionalTypeInstantiation(
          type,
          prependTypeMapping(type.root.checkType, constraint, type.mapper),
          /*forConstraint*/
          true
        );
        if (!(instantiated.flags & 131072 /* Never */)) {
          type.resolvedConstraintOfDistributive ***REMOVED*** instantiated;
          return instantiated;
        }
      }
    }
    type.resolvedConstraintOfDistributive ***REMOVED*** false;
    return void 0;
  }
  function getConstraintFromConditionalType(type) {
    return getConstraintOfDistributiveConditionalType(type) || getDefaultConstraintOfConditionalType(type);
  }
  function getConstraintOfConditionalType(type) {
    return hasNonCircularBaseConstraint(type) ? getConstraintFromConditionalType(type) : void 0;
  }
  function getEffectiveConstraintOfIntersection(types, targetIsUnion) {
    let constraints;
    let hasDisjointDomainType ***REMOVED*** false;
    for (const t of types) {
      if (t.flags & 465829888 /* Instantiable */) {
        let constraint ***REMOVED*** getConstraintOfType(t);
        while (constraint && constraint.flags & (262144 /* TypeParameter */ | 4194304 /* Index */ | 16777216 /* Conditional */)) {
          constraint ***REMOVED*** getConstraintOfType(constraint);
        }
        if (constraint) {
          constraints ***REMOVED*** append(constraints, constraint);
          if (targetIsUnion) {
            constraints ***REMOVED*** append(constraints, t);
          }
        }
      } else if (t.flags & 469892092 /* DisjointDomains */ || isEmptyAnonymousObjectType(t)) {
        hasDisjointDomainType ***REMOVED*** true;
      }
    }
    if (constraints && (targetIsUnion || hasDisjointDomainType)) {
      if (hasDisjointDomainType) {
        for (const t of types) {
          if (t.flags & 469892092 /* DisjointDomains */ || isEmptyAnonymousObjectType(t)) {
            constraints ***REMOVED*** append(constraints, t);
          }
        }
      }
      return getNormalizedType(
        getIntersectionType(constraints, 2 /* NoConstraintReduction */),
        /*writing*/
        false
      );
    }
    return void 0;
  }
  function getBaseConstraintOfType(type) {
    if (type.flags & (58982400 /* InstantiableNonPrimitive */ | 3145728 /* UnionOrIntersection */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) || isGenericTupleType(type)) {
      const constraint ***REMOVED*** getResolvedBaseConstraint(type);
      return constraint !***REMOVED******REMOVED*** noConstraintType && constraint !***REMOVED******REMOVED*** circularConstraintType ? constraint : void 0;
    }
    return type.flags & 4194304 /* Index */ ? stringNumberSymbolType : void 0;
  }
  function getBaseConstraintOrType(type) {
    return getBaseConstraintOfType(type) || type;
  }
  function hasNonCircularBaseConstraint(type) {
    return getResolvedBaseConstraint(type) !***REMOVED******REMOVED*** circularConstraintType;
  }
  function getResolvedBaseConstraint(type) {
    if (type.resolvedBaseConstraint) {
      return type.resolvedBaseConstraint;
    }
    const stack ***REMOVED*** [];
    return type.resolvedBaseConstraint ***REMOVED*** getImmediateBaseConstraint(type);
    function getImmediateBaseConstraint(t) {
      if (!t.immediateBaseConstraint) {
        if (!pushTypeResolution(t, 4 /* ImmediateBaseConstraint */)) {
          return circularConstraintType;
        }
        let result;
        const identity2 ***REMOVED*** getRecursionIdentity(t);
        if (stack.length < 10 || stack.length < 50 && !contains(stack, identity2)) {
          stack.push(identity2);
          result ***REMOVED*** computeBaseConstraint(getSimplifiedType(
            t,
            /*writing*/
            false
          ));
          stack.pop();
        }
        if (!popTypeResolution()) {
          if (t.flags & 262144 /* TypeParameter */) {
            const errorNode ***REMOVED*** getConstraintDeclaration(t);
            if (errorNode) {
              const diagnostic ***REMOVED*** error(errorNode, Diagnostics.Type_parameter_0_has_a_circular_constraint, typeToString(t));
              if (currentNode && !isNodeDescendantOf(errorNode, currentNode) && !isNodeDescendantOf(currentNode, errorNode)) {
                addRelatedInfo(diagnostic, createDiagnosticForNode(currentNode, Diagnostics.Circularity_originates_in_type_at_this_location));
              }
            }
          }
          result ***REMOVED*** circularConstraintType;
        }
        t.immediateBaseConstraint ?? (t.immediateBaseConstraint ***REMOVED*** result || noConstraintType);
      }
      return t.immediateBaseConstraint;
    }
    function getBaseConstraint(t) {
      const c ***REMOVED*** getImmediateBaseConstraint(t);
      return c !***REMOVED******REMOVED*** noConstraintType && c !***REMOVED******REMOVED*** circularConstraintType ? c : void 0;
    }
    function computeBaseConstraint(t) {
      if (t.flags & 262144 /* TypeParameter */) {
        const constraint ***REMOVED*** getConstraintFromTypeParameter(t);
        return t.isThisType || !constraint ? constraint : getBaseConstraint(constraint);
      }
      if (t.flags & 3145728 /* UnionOrIntersection */) {
        const types ***REMOVED*** t.types;
        const baseTypes ***REMOVED*** [];
        let different ***REMOVED*** false;
        for (const type2 of types) {
          const baseType ***REMOVED*** getBaseConstraint(type2);
          if (baseType) {
            if (baseType !***REMOVED******REMOVED*** type2) {
              different ***REMOVED*** true;
            }
            baseTypes.push(baseType);
          } else {
            different ***REMOVED*** true;
          }
        }
        if (!different) {
          return t;
        }
        return t.flags & 1048576 /* Union */ && baseTypes.length ***REMOVED******REMOVED******REMOVED*** types.length ? getUnionType(baseTypes) : t.flags & 2097152 /* Intersection */ && baseTypes.length ? getIntersectionType(baseTypes) : void 0;
      }
      if (t.flags & 4194304 /* Index */) {
        return stringNumberSymbolType;
      }
      if (t.flags & 134217728 /* TemplateLiteral */) {
        const types ***REMOVED*** t.types;
        const constraints ***REMOVED*** mapDefined(types, getBaseConstraint);
        return constraints.length ***REMOVED******REMOVED******REMOVED*** types.length ? getTemplateLiteralType(t.texts, constraints) : stringType;
      }
      if (t.flags & 268435456 /* StringMapping */) {
        const constraint ***REMOVED*** getBaseConstraint(t.type);
        return constraint && constraint !***REMOVED******REMOVED*** t.type ? getStringMappingType(t.symbol, constraint) : stringType;
      }
      if (t.flags & 8388608 /* IndexedAccess */) {
        if (isMappedTypeGenericIndexedAccess(t)) {
          return getBaseConstraint(substituteIndexedMappedType(t.objectType, t.indexType));
        }
        const baseObjectType ***REMOVED*** getBaseConstraint(t.objectType);
        const baseIndexType ***REMOVED*** getBaseConstraint(t.indexType);
        const baseIndexedAccess ***REMOVED*** baseObjectType && baseIndexType && getIndexedAccessTypeOrUndefined(baseObjectType, baseIndexType, t.accessFlags);
        return baseIndexedAccess && getBaseConstraint(baseIndexedAccess);
      }
      if (t.flags & 16777216 /* Conditional */) {
        const constraint ***REMOVED*** getConstraintFromConditionalType(t);
        return constraint && getBaseConstraint(constraint);
      }
      if (t.flags & 33554432 /* Substitution */) {
        return getBaseConstraint(getSubstitutionIntersection(t));
      }
      if (isGenericTupleType(t)) {
        const newElements ***REMOVED*** map(getElementTypes(t), (v, i) ***REMOVED***> {
          const constraint ***REMOVED*** v.flags & 262144 /* TypeParameter */ && t.target.elementFlags[i] & 8 /* Variadic */ && getBaseConstraint(v) || v;
          return constraint !***REMOVED******REMOVED*** v && everyType(constraint, (c) ***REMOVED***> isArrayOrTupleType(c) && !isGenericTupleType(c)) ? constraint : v;
        });
        return createTupleType(newElements, t.target.elementFlags, t.target.readonly, t.target.labeledElementDeclarations);
      }
      return t;
    }
  }
  function getApparentTypeOfIntersectionType(type, thisArgument) {
    if (type ***REMOVED******REMOVED******REMOVED*** thisArgument) {
      return type.resolvedApparentType || (type.resolvedApparentType ***REMOVED*** getTypeWithThisArgument(
        type,
        thisArgument,
        /*needApparentType*/
        true
      ));
    }
    const key ***REMOVED*** `I${getTypeId(type)},${getTypeId(thisArgument)}`;
    return getCachedType(key) ?? setCachedType(key, getTypeWithThisArgument(
      type,
      thisArgument,
      /*needApparentType*/
      true
    ));
  }
  function getResolvedTypeParameterDefault(typeParameter) {
    if (!typeParameter.default) {
      if (typeParameter.target) {
        const targetDefault ***REMOVED*** getResolvedTypeParameterDefault(typeParameter.target);
        typeParameter.default ***REMOVED*** targetDefault ? instantiateType(targetDefault, typeParameter.mapper) : noConstraintType;
      } else {
        typeParameter.default ***REMOVED*** resolvingDefaultType;
        const defaultDeclaration ***REMOVED*** typeParameter.symbol && forEach(typeParameter.symbol.declarations, (decl) ***REMOVED***> isTypeParameterDeclaration(decl) && decl.default);
        const defaultType ***REMOVED*** defaultDeclaration ? getTypeFromTypeNode(defaultDeclaration) : noConstraintType;
        if (typeParameter.default ***REMOVED******REMOVED******REMOVED*** resolvingDefaultType) {
          typeParameter.default ***REMOVED*** defaultType;
        }
      }
    } else if (typeParameter.default ***REMOVED******REMOVED******REMOVED*** resolvingDefaultType) {
      typeParameter.default ***REMOVED*** circularConstraintType;
    }
    return typeParameter.default;
  }
  function getDefaultFromTypeParameter(typeParameter) {
    const defaultType ***REMOVED*** getResolvedTypeParameterDefault(typeParameter);
    return defaultType !***REMOVED******REMOVED*** noConstraintType && defaultType !***REMOVED******REMOVED*** circularConstraintType ? defaultType : void 0;
  }
  function hasNonCircularTypeParameterDefault(typeParameter) {
    return getResolvedTypeParameterDefault(typeParameter) !***REMOVED******REMOVED*** circularConstraintType;
  }
  function hasTypeParameterDefault(typeParameter) {
    return !!(typeParameter.symbol && forEach(typeParameter.symbol.declarations, (decl) ***REMOVED***> isTypeParameterDeclaration(decl) && decl.default));
  }
  function getApparentTypeOfMappedType(type) {
    return type.resolvedApparentType || (type.resolvedApparentType ***REMOVED*** getResolvedApparentTypeOfMappedType(type));
  }
  function getResolvedApparentTypeOfMappedType(type) {
    const target ***REMOVED*** type.target ?? type;
    const typeVariable ***REMOVED*** getHomomorphicTypeVariable(target);
    if (typeVariable && !target.declaration.nameType) {
      const modifiersType ***REMOVED*** getModifiersTypeFromMappedType(type);
      const baseConstraint ***REMOVED*** isGenericMappedType(modifiersType) ? getApparentTypeOfMappedType(modifiersType) : getBaseConstraintOfType(modifiersType);
      if (baseConstraint && everyType(baseConstraint, (t) ***REMOVED***> isArrayOrTupleType(t) || isArrayOrTupleOrIntersection(t))) {
        return instantiateType(target, prependTypeMapping(typeVariable, baseConstraint, type.mapper));
      }
    }
    return type;
  }
  function isArrayOrTupleOrIntersection(type) {
    return !!(type.flags & 2097152 /* Intersection */) && every(type.types, isArrayOrTupleType);
  }
  function isMappedTypeGenericIndexedAccess(type) {
    let objectType;
    return !!(type.flags & 8388608 /* IndexedAccess */ && getObjectFlags(objectType ***REMOVED*** type.objectType) & 32 /* Mapped */ && !isGenericMappedType(objectType) && isGenericIndexType(type.indexType) && !(getMappedTypeModifiers(objectType) & 8 /* ExcludeOptional */) && !objectType.declaration.nameType);
  }
  function getApparentType(type) {
    const t ***REMOVED*** type.flags & 465829888 /* Instantiable */ ? getBaseConstraintOfType(type) || unknownType : type;
    const objectFlags ***REMOVED*** getObjectFlags(t);
    return objectFlags & 32 /* Mapped */ ? getApparentTypeOfMappedType(t) : objectFlags & 4 /* Reference */ && t !***REMOVED******REMOVED*** type ? getTypeWithThisArgument(t, type) : t.flags & 2097152 /* Intersection */ ? getApparentTypeOfIntersectionType(t, type) : t.flags & 402653316 /* StringLike */ ? globalStringType : t.flags & 296 /* NumberLike */ ? globalNumberType : t.flags & 2112 /* BigIntLike */ ? getGlobalBigIntType() : t.flags & 528 /* BooleanLike */ ? globalBooleanType : t.flags & 12288 /* ESSymbolLike */ ? getGlobalESSymbolType() : t.flags & 67108864 /* NonPrimitive */ ? emptyObjectType : t.flags & 4194304 /* Index */ ? stringNumberSymbolType : t.flags & 2 /* Unknown */ && !strictNullChecks ? emptyObjectType : t;
  }
  function getReducedApparentType(type) {
    return getReducedType(getApparentType(getReducedType(type)));
  }
  function createUnionOrIntersectionProperty(containingType, name, skipObjectFunctionPropertyAugment) {
    var _a, _b, _c;
    let propFlags ***REMOVED*** 0 /* None */;
    let singleProp;
    let propSet;
    let indexTypes;
    const isUnion ***REMOVED*** containingType.flags & 1048576 /* Union */;
    let optionalFlag;
    let syntheticFlag ***REMOVED*** 4 /* SyntheticMethod */;
    let checkFlags ***REMOVED*** isUnion ? 0 : 8 /* Readonly */;
    let mergedInstantiations ***REMOVED*** false;
    for (const current of containingType.types) {
      const type ***REMOVED*** getApparentType(current);
      if (!(isErrorType(type) || type.flags & 131072 /* Never */)) {
        const prop ***REMOVED*** getPropertyOfType(type, name, skipObjectFunctionPropertyAugment);
        const modifiers ***REMOVED*** prop ? getDeclarationModifierFlagsFromSymbol(prop) : 0;
        if (prop) {
          if (prop.flags & 106500 /* ClassMember */) {
            optionalFlag ?? (optionalFlag ***REMOVED*** isUnion ? 0 /* None */ : 16777216 /* Optional */);
            if (isUnion) {
              optionalFlag |***REMOVED*** prop.flags & 16777216 /* Optional */;
            } else {
              optionalFlag &***REMOVED*** prop.flags;
            }
          }
          if (!singleProp) {
            singleProp ***REMOVED*** prop;
            propFlags ***REMOVED*** prop.flags & 98304 /* Accessor */ || 4 /* Property */;
          } else if (prop !***REMOVED******REMOVED*** singleProp) {
            const isInstantiation ***REMOVED*** (getTargetSymbol(prop) || prop) ***REMOVED******REMOVED******REMOVED*** (getTargetSymbol(singleProp) || singleProp);
            if (isInstantiation && compareProperties(singleProp, prop, (a, b) ***REMOVED***> a ***REMOVED******REMOVED******REMOVED*** b ? -1 /* True */ : 0 /* False */) ***REMOVED******REMOVED******REMOVED*** -1 /* True */) {
              mergedInstantiations ***REMOVED*** !!singleProp.parent && !!length(getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(singleProp.parent));
            } else {
              if (!propSet) {
                propSet ***REMOVED*** /* @__PURE__ */ new Map();
                propSet.set(getSymbolId(singleProp), singleProp);
              }
              const id ***REMOVED*** getSymbolId(prop);
              if (!propSet.has(id)) {
                propSet.set(id, prop);
              }
            }
            if (propFlags & 98304 /* Accessor */ && (prop.flags & 98304 /* Accessor */) !***REMOVED******REMOVED*** (propFlags & 98304 /* Accessor */)) {
              propFlags ***REMOVED*** propFlags & ~98304 /* Accessor */ | 4 /* Property */;
            }
          }
          if (isUnion && isReadonlySymbol(prop)) {
            checkFlags |***REMOVED*** 8 /* Readonly */;
          } else if (!isUnion && !isReadonlySymbol(prop)) {
            checkFlags &***REMOVED*** ~8 /* Readonly */;
          }
          checkFlags |***REMOVED*** (!(modifiers & 6 /* NonPublicAccessibilityModifier */) ? 256 /* ContainsPublic */ : 0) | (modifiers & 4 /* Protected */ ? 512 /* ContainsProtected */ : 0) | (modifiers & 2 /* Private */ ? 1024 /* ContainsPrivate */ : 0) | (modifiers & 256 /* Static */ ? 2048 /* ContainsStatic */ : 0);
          if (!isPrototypeProperty(prop)) {
            syntheticFlag ***REMOVED*** 2 /* SyntheticProperty */;
          }
        } else if (isUnion) {
          const indexInfo ***REMOVED*** !isLateBoundName(name) && getApplicableIndexInfoForName(type, name);
          if (indexInfo) {
            propFlags ***REMOVED*** propFlags & ~98304 /* Accessor */ | 4 /* Property */;
            checkFlags |***REMOVED*** 32 /* WritePartial */ | (indexInfo.isReadonly ? 8 /* Readonly */ : 0);
            indexTypes ***REMOVED*** append(indexTypes, isTupleType(type) ? getRestTypeOfTupleType(type) || undefinedType : indexInfo.type);
          } else if (isObjectLiteralType(type) && !(getObjectFlags(type) & 2097152 /* ContainsSpread */)) {
            checkFlags |***REMOVED*** 32 /* WritePartial */;
            indexTypes ***REMOVED*** append(indexTypes, undefinedType);
          } else {
            checkFlags |***REMOVED*** 16 /* ReadPartial */;
          }
        }
      }
    }
    if (!singleProp || isUnion && (propSet || checkFlags & 48 /* Partial */) && checkFlags & (1024 /* ContainsPrivate */ | 512 /* ContainsProtected */) && !(propSet && getCommonDeclarationsOfSymbols(propSet.values()))) {
      return void 0;
    }
    if (!propSet && !(checkFlags & 16 /* ReadPartial */) && !indexTypes) {
      if (mergedInstantiations) {
        const links ***REMOVED*** (_a ***REMOVED*** tryCast(singleProp, isTransientSymbol)) ***REMOVED******REMOVED*** null ? void 0 : _a.links;
        const clone ***REMOVED*** createSymbolWithType(singleProp, links ***REMOVED******REMOVED*** null ? void 0 : links.type);
        clone.parent ***REMOVED*** (_c ***REMOVED*** (_b ***REMOVED*** singleProp.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _b.symbol) ***REMOVED******REMOVED*** null ? void 0 : _c.parent;
        clone.links.containingType ***REMOVED*** containingType;
        clone.links.mapper ***REMOVED*** links ***REMOVED******REMOVED*** null ? void 0 : links.mapper;
        clone.links.writeType ***REMOVED*** getWriteTypeOfSymbol(singleProp);
        return clone;
      } else {
        return singleProp;
      }
    }
    const props ***REMOVED*** propSet ? arrayFrom(propSet.values()) : [singleProp];
    let declarations;
    let firstType;
    let nameType;
    const propTypes ***REMOVED*** [];
    let writeTypes;
    let firstValueDeclaration;
    let hasNonUniformValueDeclaration ***REMOVED*** false;
    for (const prop of props) {
      if (!firstValueDeclaration) {
        firstValueDeclaration ***REMOVED*** prop.valueDeclaration;
      } else if (prop.valueDeclaration && prop.valueDeclaration !***REMOVED******REMOVED*** firstValueDeclaration) {
        hasNonUniformValueDeclaration ***REMOVED*** true;
      }
      declarations ***REMOVED*** addRange(declarations, prop.declarations);
      const type ***REMOVED*** getTypeOfSymbol(prop);
      if (!firstType) {
        firstType ***REMOVED*** type;
        nameType ***REMOVED*** getSymbolLinks(prop).nameType;
      }
      const writeType ***REMOVED*** getWriteTypeOfSymbol(prop);
      if (writeTypes || writeType !***REMOVED******REMOVED*** type) {
        writeTypes ***REMOVED*** append(!writeTypes ? propTypes.slice() : writeTypes, writeType);
      }
      if (type !***REMOVED******REMOVED*** firstType) {
        checkFlags |***REMOVED*** 64 /* HasNonUniformType */;
      }
      if (isLiteralType(type) || isPatternLiteralType(type)) {
        checkFlags |***REMOVED*** 128 /* HasLiteralType */;
      }
      if (type.flags & 131072 /* Never */ && type !***REMOVED******REMOVED*** uniqueLiteralType) {
        checkFlags |***REMOVED*** 131072 /* HasNeverType */;
      }
      propTypes.push(type);
    }
    addRange(propTypes, indexTypes);
    const result ***REMOVED*** createSymbol(propFlags | (optionalFlag ?? 0), name, syntheticFlag | checkFlags);
    result.links.containingType ***REMOVED*** containingType;
    if (!hasNonUniformValueDeclaration && firstValueDeclaration) {
      result.valueDeclaration ***REMOVED*** firstValueDeclaration;
      if (firstValueDeclaration.symbol.parent) {
        result.parent ***REMOVED*** firstValueDeclaration.symbol.parent;
      }
    }
    result.declarations ***REMOVED*** declarations;
    result.links.nameType ***REMOVED*** nameType;
    if (propTypes.length > 2) {
      result.links.checkFlags |***REMOVED*** 65536 /* DeferredType */;
      result.links.deferralParent ***REMOVED*** containingType;
      result.links.deferralConstituents ***REMOVED*** propTypes;
      result.links.deferralWriteConstituents ***REMOVED*** writeTypes;
    } else {
      result.links.type ***REMOVED*** isUnion ? getUnionType(propTypes) : getIntersectionType(propTypes);
      if (writeTypes) {
        result.links.writeType ***REMOVED*** isUnion ? getUnionType(writeTypes) : getIntersectionType(writeTypes);
      }
    }
    return result;
  }
  function getUnionOrIntersectionProperty(type, name, skipObjectFunctionPropertyAugment) {
    var _a, _b, _c;
    let property ***REMOVED*** skipObjectFunctionPropertyAugment ? (_a ***REMOVED*** type.propertyCacheWithoutObjectFunctionPropertyAugment) ***REMOVED******REMOVED*** null ? void 0 : _a.get(name) : (_b ***REMOVED*** type.propertyCache) ***REMOVED******REMOVED*** null ? void 0 : _b.get(name);
    if (!property) {
      property ***REMOVED*** createUnionOrIntersectionProperty(type, name, skipObjectFunctionPropertyAugment);
      if (property) {
        const properties ***REMOVED*** skipObjectFunctionPropertyAugment ? type.propertyCacheWithoutObjectFunctionPropertyAugment || (type.propertyCacheWithoutObjectFunctionPropertyAugment ***REMOVED*** createSymbolTable()) : type.propertyCache || (type.propertyCache ***REMOVED*** createSymbolTable());
        properties.set(name, property);
        if (skipObjectFunctionPropertyAugment && !(getCheckFlags(property) & 48 /* Partial */) && !((_c ***REMOVED*** type.propertyCache) ***REMOVED******REMOVED*** null ? void 0 : _c.get(name))) {
          const properties2 ***REMOVED*** type.propertyCache || (type.propertyCache ***REMOVED*** createSymbolTable());
          properties2.set(name, property);
        }
      }
    }
    return property;
  }
  function getCommonDeclarationsOfSymbols(symbols) {
    let commonDeclarations;
    for (const symbol of symbols) {
      if (!symbol.declarations) {
        return void 0;
      }
      if (!commonDeclarations) {
        commonDeclarations ***REMOVED*** new Set(symbol.declarations);
        continue;
      }
      commonDeclarations.forEach((declaration) ***REMOVED***> {
        if (!contains(symbol.declarations, declaration)) {
          commonDeclarations.delete(declaration);
        }
      });
      if (commonDeclarations.size ***REMOVED******REMOVED******REMOVED*** 0) {
        return void 0;
      }
    }
    return commonDeclarations;
  }
  function getPropertyOfUnionOrIntersectionType(type, name, skipObjectFunctionPropertyAugment) {
    const property ***REMOVED*** getUnionOrIntersectionProperty(type, name, skipObjectFunctionPropertyAugment);
    return property && !(getCheckFlags(property) & 16 /* ReadPartial */) ? property : void 0;
  }
  function getReducedType(type) {
    if (type.flags & 1048576 /* Union */ && type.objectFlags & 16777216 /* ContainsIntersections */) {
      return type.resolvedReducedType || (type.resolvedReducedType ***REMOVED*** getReducedUnionType(type));
    } else if (type.flags & 2097152 /* Intersection */) {
      if (!(type.objectFlags & 16777216 /* IsNeverIntersectionComputed */)) {
        type.objectFlags |***REMOVED*** 16777216 /* IsNeverIntersectionComputed */ | (some(getPropertiesOfUnionOrIntersectionType(type), isNeverReducedProperty) ? 33554432 /* IsNeverIntersection */ : 0);
      }
      return type.objectFlags & 33554432 /* IsNeverIntersection */ ? neverType : type;
    }
    return type;
  }
  function getReducedUnionType(unionType) {
    const reducedTypes ***REMOVED*** sameMap(unionType.types, getReducedType);
    if (reducedTypes ***REMOVED******REMOVED******REMOVED*** unionType.types) {
      return unionType;
    }
    const reduced ***REMOVED*** getUnionType(reducedTypes);
    if (reduced.flags & 1048576 /* Union */) {
      reduced.resolvedReducedType ***REMOVED*** reduced;
    }
    return reduced;
  }
  function isNeverReducedProperty(prop) {
    return isDiscriminantWithNeverType(prop) || isConflictingPrivateProperty(prop);
  }
  function isDiscriminantWithNeverType(prop) {
    return !(prop.flags & 16777216 /* Optional */) && (getCheckFlags(prop) & (192 /* Discriminant */ | 131072 /* HasNeverType */)) ***REMOVED******REMOVED******REMOVED*** 192 /* Discriminant */ && !!(getTypeOfSymbol(prop).flags & 131072 /* Never */);
  }
  function isConflictingPrivateProperty(prop) {
    return !prop.valueDeclaration && !!(getCheckFlags(prop) & 1024 /* ContainsPrivate */);
  }
  function isGenericReducibleType(type) {
    return !!(type.flags & 1048576 /* Union */ && type.objectFlags & 16777216 /* ContainsIntersections */ && some(type.types, isGenericReducibleType) || type.flags & 2097152 /* Intersection */ && isReducibleIntersection(type));
  }
  function isReducibleIntersection(type) {
    const uniqueFilled ***REMOVED*** type.uniqueLiteralFilledInstantiation || (type.uniqueLiteralFilledInstantiation ***REMOVED*** instantiateType(type, uniqueLiteralMapper));
    return getReducedType(uniqueFilled) !***REMOVED******REMOVED*** uniqueFilled;
  }
  function elaborateNeverIntersection(errorInfo, type) {
    if (type.flags & 2097152 /* Intersection */ && getObjectFlags(type) & 33554432 /* IsNeverIntersection */) {
      const neverProp ***REMOVED*** find(getPropertiesOfUnionOrIntersectionType(type), isDiscriminantWithNeverType);
      if (neverProp) {
        return chainDiagnosticMessages(errorInfo, Diagnostics.The_intersection_0_was_reduced_to_never_because_property_1_has_conflicting_types_in_some_constituents, typeToString(
          type,
          /*enclosingDeclaration*/
          void 0,
          536870912 /* NoTypeReduction */
        ), symbolToString(neverProp));
      }
      const privateProp ***REMOVED*** find(getPropertiesOfUnionOrIntersectionType(type), isConflictingPrivateProperty);
      if (privateProp) {
        return chainDiagnosticMessages(errorInfo, Diagnostics.The_intersection_0_was_reduced_to_never_because_property_1_exists_in_multiple_constituents_and_is_private_in_some, typeToString(
          type,
          /*enclosingDeclaration*/
          void 0,
          536870912 /* NoTypeReduction */
        ), symbolToString(privateProp));
      }
    }
    return errorInfo;
  }
  function getPropertyOfType(type, name, skipObjectFunctionPropertyAugment, includeTypeOnlyMembers) {
    var _a, _b;
    type ***REMOVED*** getReducedApparentType(type);
    if (type.flags & 524288 /* Object */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      const symbol ***REMOVED*** resolved.members.get(name);
      if (symbol && !includeTypeOnlyMembers && ((_a ***REMOVED*** type.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.flags) & 512 /* ValueModule */ && ((_b ***REMOVED*** getSymbolLinks(type.symbol).typeOnlyExportStarMap) ***REMOVED******REMOVED*** null ? void 0 : _b.has(name))) {
        return void 0;
      }
      if (symbol && symbolIsValue(symbol, includeTypeOnlyMembers)) {
        return symbol;
      }
      if (skipObjectFunctionPropertyAugment) return void 0;
      const functionType ***REMOVED*** resolved ***REMOVED******REMOVED******REMOVED*** anyFunctionType ? globalFunctionType : resolved.callSignatures.length ? globalCallableFunctionType : resolved.constructSignatures.length ? globalNewableFunctionType : void 0;
      if (functionType) {
        const symbol2 ***REMOVED*** getPropertyOfObjectType(functionType, name);
        if (symbol2) {
          return symbol2;
        }
      }
      return getPropertyOfObjectType(globalObjectType, name);
    }
    if (type.flags & 2097152 /* Intersection */) {
      const prop ***REMOVED*** getPropertyOfUnionOrIntersectionType(
        type,
        name,
        /*skipObjectFunctionPropertyAugment*/
        true
      );
      if (prop) {
        return prop;
      }
      if (!skipObjectFunctionPropertyAugment) {
        return getPropertyOfUnionOrIntersectionType(type, name, skipObjectFunctionPropertyAugment);
      }
      return void 0;
    }
    if (type.flags & 1048576 /* Union */) {
      return getPropertyOfUnionOrIntersectionType(type, name, skipObjectFunctionPropertyAugment);
    }
    return void 0;
  }
  function getSignaturesOfStructuredType(type, kind) {
    if (type.flags & 3670016 /* StructuredType */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      return kind ***REMOVED******REMOVED******REMOVED*** 0 /* Call */ ? resolved.callSignatures : resolved.constructSignatures;
    }
    return emptyArray;
  }
  function getSignaturesOfType(type, kind) {
    const result ***REMOVED*** getSignaturesOfStructuredType(getReducedApparentType(type), kind);
    if (kind ***REMOVED******REMOVED******REMOVED*** 0 /* Call */ && !length(result) && type.flags & 1048576 /* Union */) {
      if (type.arrayFallbackSignatures) {
        return type.arrayFallbackSignatures;
      }
      let memberName;
      if (everyType(type, (t) ***REMOVED***> {
        var _a;
        return !!((_a ***REMOVED*** t.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) && isArrayOrTupleSymbol(t.symbol.parent) && (!memberName ? (memberName ***REMOVED*** t.symbol.escapedName, true) : memberName ***REMOVED******REMOVED******REMOVED*** t.symbol.escapedName);
      })) {
        const arrayArg ***REMOVED*** mapType(type, (t) ***REMOVED***> getMappedType((isReadonlyArraySymbol(t.symbol.parent) ? globalReadonlyArrayType : globalArrayType).typeParameters[0], t.mapper));
        const arrayType ***REMOVED*** createArrayType(arrayArg, someType(type, (t) ***REMOVED***> isReadonlyArraySymbol(t.symbol.parent)));
        return type.arrayFallbackSignatures ***REMOVED*** getSignaturesOfType(getTypeOfPropertyOfType(arrayType, memberName), kind);
      }
      type.arrayFallbackSignatures ***REMOVED*** result;
    }
    return result;
  }
  function isArrayOrTupleSymbol(symbol) {
    if (!symbol || !globalArrayType.symbol || !globalReadonlyArrayType.symbol) {
      return false;
    }
    return !!getSymbolIfSameReference(symbol, globalArrayType.symbol) || !!getSymbolIfSameReference(symbol, globalReadonlyArrayType.symbol);
  }
  function isReadonlyArraySymbol(symbol) {
    if (!symbol || !globalReadonlyArrayType.symbol) {
      return false;
    }
    return !!getSymbolIfSameReference(symbol, globalReadonlyArrayType.symbol);
  }
  function findIndexInfo(indexInfos, keyType) {
    return find(indexInfos, (info) ***REMOVED***> info.keyType ***REMOVED******REMOVED******REMOVED*** keyType);
  }
  function findApplicableIndexInfo(indexInfos, keyType) {
    let stringIndexInfo;
    let applicableInfo;
    let applicableInfos;
    for (const info of indexInfos) {
      if (info.keyType ***REMOVED******REMOVED******REMOVED*** stringType) {
        stringIndexInfo ***REMOVED*** info;
      } else if (isApplicableIndexType(keyType, info.keyType)) {
        if (!applicableInfo) {
          applicableInfo ***REMOVED*** info;
        } else {
          (applicableInfos || (applicableInfos ***REMOVED*** [applicableInfo])).push(info);
        }
      }
    }
    return applicableInfos ? createIndexInfo(unknownType, getIntersectionType(map(applicableInfos, (info) ***REMOVED***> info.type)), reduceLeft(
      applicableInfos,
      (isReadonly, info) ***REMOVED***> isReadonly && info.isReadonly,
      /*initial*/
      true
    )) : applicableInfo ? applicableInfo : stringIndexInfo && isApplicableIndexType(keyType, stringType) ? stringIndexInfo : void 0;
  }
  function isApplicableIndexType(source, target) {
    return isTypeAssignableTo(source, target) || target ***REMOVED******REMOVED******REMOVED*** stringType && isTypeAssignableTo(source, numberType) || target ***REMOVED******REMOVED******REMOVED*** numberType && (source ***REMOVED******REMOVED******REMOVED*** numericStringType || !!(source.flags & 128 /* StringLiteral */) && isNumericLiteralName(source.value));
  }
  function getIndexInfosOfStructuredType(type) {
    if (type.flags & 3670016 /* StructuredType */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      return resolved.indexInfos;
    }
    return emptyArray;
  }
  function getIndexInfosOfType(type) {
    return getIndexInfosOfStructuredType(getReducedApparentType(type));
  }
  function getIndexInfoOfType(type, keyType) {
    return findIndexInfo(getIndexInfosOfType(type), keyType);
  }
  function getIndexTypeOfType(type, keyType) {
    var _a;
    return (_a ***REMOVED*** getIndexInfoOfType(type, keyType)) ***REMOVED******REMOVED*** null ? void 0 : _a.type;
  }
  function getApplicableIndexInfos(type, keyType) {
    return getIndexInfosOfType(type).filter((info) ***REMOVED***> isApplicableIndexType(keyType, info.keyType));
  }
  function getApplicableIndexInfo(type, keyType) {
    return findApplicableIndexInfo(getIndexInfosOfType(type), keyType);
  }
  function getApplicableIndexInfoForName(type, name) {
    return getApplicableIndexInfo(type, isLateBoundName(name) ? esSymbolType : getStringLiteralType(unescapeLeadingUnderscores(name)));
  }
  function getTypeParametersFromDeclaration(declaration) {
    var _a;
    let result;
    for (const node of getEffectiveTypeParameterDeclarations(declaration)) {
      result ***REMOVED*** appendIfUnique(result, getDeclaredTypeOfTypeParameter(node.symbol));
    }
    return (result ***REMOVED******REMOVED*** null ? void 0 : result.length) ? result : isFunctionDeclaration(declaration) ? (_a ***REMOVED*** getSignatureOfTypeTag(declaration)) ***REMOVED******REMOVED*** null ? void 0 : _a.typeParameters : void 0;
  }
  function symbolsToArray(symbols) {
    const result ***REMOVED*** [];
    symbols.forEach((symbol, id) ***REMOVED***> {
      if (!isReservedMemberName(id)) {
        result.push(symbol);
      }
    });
    return result;
  }
  function tryFindAmbientModule(moduleName, withAugmentations) {
    if (isExternalModuleNameRelative(moduleName)) {
      return void 0;
    }
    const symbol ***REMOVED*** getSymbol(globals, '"' + moduleName + '"', 512 /* ValueModule */);
    return symbol && withAugmentations ? getMergedSymbol(symbol) : symbol;
  }
  function hasEffectiveQuestionToken(node) {
    return hasQuestionToken(node) || isOptionalJSDocPropertyLikeTag(node) || isParameter(node) && isJSDocOptionalParameter(node);
  }
  function isOptionalParameter(node) {
    if (hasEffectiveQuestionToken(node)) {
      return true;
    }
    if (!isParameter(node)) {
      return false;
    }
    if (node.initializer) {
      const signature ***REMOVED*** getSignatureFromDeclaration(node.parent);
      const parameterIndex ***REMOVED*** node.parent.parameters.indexOf(node);
      Debug.assert(parameterIndex >***REMOVED*** 0);
      return parameterIndex >***REMOVED*** getMinArgumentCount(signature, 1 /* StrongArityForUntypedJS */ | 2 /* VoidIsNonOptional */);
    }
    const iife ***REMOVED*** getImmediatelyInvokedFunctionExpression(node.parent);
    if (iife) {
      return !node.type && !node.dotDotDotToken && node.parent.parameters.indexOf(node) >***REMOVED*** getEffectiveCallArguments(iife).length;
    }
    return false;
  }
  function isOptionalPropertyDeclaration(node) {
    return isPropertyDeclaration(node) && !hasAccessorModifier(node) && node.questionToken;
  }
  function createTypePredicate(kind, parameterName, parameterIndex, type) {
    return { kind, parameterName, parameterIndex, type };
  }
  function getMinTypeArgumentCount(typeParameters) {
    let minTypeArgumentCount ***REMOVED*** 0;
    if (typeParameters) {
      for (let i ***REMOVED*** 0; i < typeParameters.length; i++) {
        if (!hasTypeParameterDefault(typeParameters[i])) {
          minTypeArgumentCount ***REMOVED*** i + 1;
        }
      }
    }
    return minTypeArgumentCount;
  }
  function fillMissingTypeArguments(typeArguments, typeParameters, minTypeArgumentCount, isJavaScriptImplicitAny) {
    const numTypeParameters ***REMOVED*** length(typeParameters);
    if (!numTypeParameters) {
      return [];
    }
    const numTypeArguments ***REMOVED*** length(typeArguments);
    if (isJavaScriptImplicitAny || numTypeArguments >***REMOVED*** minTypeArgumentCount && numTypeArguments <***REMOVED*** numTypeParameters) {
      const result ***REMOVED*** typeArguments ? typeArguments.slice() : [];
      for (let i ***REMOVED*** numTypeArguments; i < numTypeParameters; i++) {
        result[i] ***REMOVED*** errorType;
      }
      const baseDefaultType ***REMOVED*** getDefaultTypeArgumentType(isJavaScriptImplicitAny);
      for (let i ***REMOVED*** numTypeArguments; i < numTypeParameters; i++) {
        let defaultType ***REMOVED*** getDefaultFromTypeParameter(typeParameters[i]);
        if (isJavaScriptImplicitAny && defaultType && (isTypeIdenticalTo(defaultType, unknownType) || isTypeIdenticalTo(defaultType, emptyObjectType))) {
          defaultType ***REMOVED*** anyType;
        }
        result[i] ***REMOVED*** defaultType ? instantiateType(defaultType, createTypeMapper(typeParameters, result)) : baseDefaultType;
      }
      result.length ***REMOVED*** typeParameters.length;
      return result;
    }
    return typeArguments && typeArguments.slice();
  }
  function getSignatureFromDeclaration(declaration) {
    const links ***REMOVED*** getNodeLinks(declaration);
    if (!links.resolvedSignature) {
      const parameters ***REMOVED*** [];
      let flags ***REMOVED*** 0 /* None */;
      let minArgumentCount ***REMOVED*** 0;
      let thisParameter;
      let thisTag ***REMOVED*** isInJSFile(declaration) ? getJSDocThisTag(declaration) : void 0;
      let hasThisParameter ***REMOVED*** false;
      const iife ***REMOVED*** getImmediatelyInvokedFunctionExpression(declaration);
      const isJSConstructSignature ***REMOVED*** isJSDocConstructSignature(declaration);
      const isUntypedSignatureInJSFile ***REMOVED*** !iife && isInJSFile(declaration) && isValueSignatureDeclaration(declaration) && !hasJSDocParameterTags(declaration) && !some(declaration.parameters, (p) ***REMOVED***> !!getJSDocType(p)) && !getJSDocType(declaration) && !getContextualSignatureForFunctionLikeDeclaration(declaration);
      if (isUntypedSignatureInJSFile) {
        flags |***REMOVED*** 32 /* IsUntypedSignatureInJSFile */;
      }
      for (let i ***REMOVED*** isJSConstructSignature ? 1 : 0; i < declaration.parameters.length; i++) {
        const param ***REMOVED*** declaration.parameters[i];
        if (isInJSFile(param) && isJSDocThisTag(param)) {
          thisTag ***REMOVED*** param;
          continue;
        }
        let paramSymbol ***REMOVED*** param.symbol;
        const type ***REMOVED*** isJSDocParameterTag(param) ? param.typeExpression && param.typeExpression.type : param.type;
        if (paramSymbol && !!(paramSymbol.flags & 4 /* Property */) && !isBindingPattern(param.name)) {
          const resolvedSymbol ***REMOVED*** resolveName(
            param,
            paramSymbol.escapedName,
            111551 /* Value */,
            /*nameNotFoundMessage*/
            void 0,
            /*isUse*/
            false
          );
          paramSymbol ***REMOVED*** resolvedSymbol;
        }
        if (i ***REMOVED******REMOVED******REMOVED*** 0 && paramSymbol.escapedName ***REMOVED******REMOVED******REMOVED*** "this" /* This */) {
          hasThisParameter ***REMOVED*** true;
          thisParameter ***REMOVED*** param.symbol;
        } else {
          parameters.push(paramSymbol);
        }
        if (type && type.kind ***REMOVED******REMOVED******REMOVED*** 202 /* LiteralType */) {
          flags |***REMOVED*** 2 /* HasLiteralTypes */;
        }
        const isOptionalParameter2 ***REMOVED*** hasEffectiveQuestionToken(param) || isParameter(param) && param.initializer || isRestParameter(param) || iife && parameters.length > iife.arguments.length && !type;
        if (!isOptionalParameter2) {
          minArgumentCount ***REMOVED*** parameters.length;
        }
      }
      if ((declaration.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || declaration.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) && hasBindableName(declaration) && (!hasThisParameter || !thisParameter)) {
        const otherKind ***REMOVED*** declaration.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? 179 /* SetAccessor */ : 178 /* GetAccessor */;
        const other ***REMOVED*** getDeclarationOfKind(getSymbolOfDeclaration(declaration), otherKind);
        if (other) {
          thisParameter ***REMOVED*** getAnnotatedAccessorThisParameter(other);
        }
      }
      if (thisTag && thisTag.typeExpression) {
        thisParameter ***REMOVED*** createSymbolWithType(createSymbol(1 /* FunctionScopedVariable */, "this" /* This */), getTypeFromTypeNode(thisTag.typeExpression));
      }
      const hostDeclaration ***REMOVED*** isJSDocSignature(declaration) ? getEffectiveJSDocHost(declaration) : declaration;
      const classType ***REMOVED*** hostDeclaration && isConstructorDeclaration(hostDeclaration) ? getDeclaredTypeOfClassOrInterface(getMergedSymbol(hostDeclaration.parent.symbol)) : void 0;
      const typeParameters ***REMOVED*** classType ? classType.localTypeParameters : getTypeParametersFromDeclaration(declaration);
      if (hasRestParameter(declaration) || isInJSFile(declaration) && maybeAddJsSyntheticRestParameter(declaration, parameters)) {
        flags |***REMOVED*** 1 /* HasRestParameter */;
      }
      if (isConstructorTypeNode(declaration) && hasSyntacticModifier(declaration, 64 /* Abstract */) || isConstructorDeclaration(declaration) && hasSyntacticModifier(declaration.parent, 64 /* Abstract */)) {
        flags |***REMOVED*** 4 /* Abstract */;
      }
      links.resolvedSignature ***REMOVED*** createSignature(
        declaration,
        typeParameters,
        thisParameter,
        parameters,
        /*resolvedReturnType*/
        void 0,
        /*resolvedTypePredicate*/
        void 0,
        minArgumentCount,
        flags
      );
    }
    return links.resolvedSignature;
  }
  function maybeAddJsSyntheticRestParameter(declaration, parameters) {
    if (isJSDocSignature(declaration) || !containsArgumentsReference(declaration)) {
      return false;
    }
    const lastParam ***REMOVED*** lastOrUndefined(declaration.parameters);
    const lastParamTags ***REMOVED*** lastParam ? getJSDocParameterTags(lastParam) : getJSDocTags(declaration).filter(isJSDocParameterTag);
    const lastParamVariadicType ***REMOVED*** firstDefined(lastParamTags, (p) ***REMOVED***> p.typeExpression && isJSDocVariadicType(p.typeExpression.type) ? p.typeExpression.type : void 0);
    const syntheticArgsSymbol ***REMOVED*** createSymbol(3 /* Variable */, "args", 32768 /* RestParameter */);
    if (lastParamVariadicType) {
      syntheticArgsSymbol.links.type ***REMOVED*** createArrayType(getTypeFromTypeNode(lastParamVariadicType.type));
    } else {
      syntheticArgsSymbol.links.checkFlags |***REMOVED*** 65536 /* DeferredType */;
      syntheticArgsSymbol.links.deferralParent ***REMOVED*** neverType;
      syntheticArgsSymbol.links.deferralConstituents ***REMOVED*** [anyArrayType];
      syntheticArgsSymbol.links.deferralWriteConstituents ***REMOVED*** [anyArrayType];
    }
    if (lastParamVariadicType) {
      parameters.pop();
    }
    parameters.push(syntheticArgsSymbol);
    return true;
  }
  function getSignatureOfTypeTag(node) {
    if (!(isInJSFile(node) && isFunctionLikeDeclaration(node))) return void 0;
    const typeTag ***REMOVED*** getJSDocTypeTag(node);
    return (typeTag ***REMOVED******REMOVED*** null ? void 0 : typeTag.typeExpression) && getSingleCallSignature(getTypeFromTypeNode(typeTag.typeExpression));
  }
  function getParameterTypeOfTypeTag(func, parameter) {
    const signature ***REMOVED*** getSignatureOfTypeTag(func);
    if (!signature) return void 0;
    const pos ***REMOVED*** func.parameters.indexOf(parameter);
    return parameter.dotDotDotToken ? getRestTypeAtPosition(signature, pos) : getTypeAtPosition(signature, pos);
  }
  function getReturnTypeOfTypeTag(node) {
    const signature ***REMOVED*** getSignatureOfTypeTag(node);
    return signature && getReturnTypeOfSignature(signature);
  }
  function containsArgumentsReference(declaration) {
    const links ***REMOVED*** getNodeLinks(declaration);
    if (links.containsArgumentsReference ***REMOVED******REMOVED******REMOVED*** void 0) {
      if (links.flags & 512 /* CaptureArguments */) {
        links.containsArgumentsReference ***REMOVED*** true;
      } else {
        links.containsArgumentsReference ***REMOVED*** traverse(declaration.body);
      }
    }
    return links.containsArgumentsReference;
    function traverse(node) {
      if (!node) return false;
      switch (node.kind) {
        case 80 /* Identifier */:
          return node.escapedText ***REMOVED******REMOVED******REMOVED*** argumentsSymbol.escapedName && getReferencedValueSymbol(node) ***REMOVED******REMOVED******REMOVED*** argumentsSymbol;
        case 173 /* PropertyDeclaration */:
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          return node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ && traverse(node.name);
        case 212 /* PropertyAccessExpression */:
        case 213 /* ElementAccessExpression */:
          return traverse(node.expression);
        case 304 /* PropertyAssignment */:
          return traverse(node.initializer);
        default:
          return !nodeStartsNewLexicalEnvironment(node) && !isPartOfTypeNode(node) && !!forEachChild(node, traverse);
      }
    }
  }
  function getSignaturesOfSymbol(symbol) {
    if (!symbol || !symbol.declarations) return emptyArray;
    const result ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < symbol.declarations.length; i++) {
      const decl ***REMOVED*** symbol.declarations[i];
      if (!isFunctionLike(decl)) continue;
      if (i > 0 && decl.body) {
        const previous ***REMOVED*** symbol.declarations[i - 1];
        if (decl.parent ***REMOVED******REMOVED******REMOVED*** previous.parent && decl.kind ***REMOVED******REMOVED******REMOVED*** previous.kind && decl.pos ***REMOVED******REMOVED******REMOVED*** previous.end) {
          continue;
        }
      }
      if (isInJSFile(decl) && decl.jsDoc) {
        const tags ***REMOVED*** getJSDocOverloadTags(decl);
        if (length(tags)) {
          for (const tag of tags) {
            const jsDocSignature ***REMOVED*** tag.typeExpression;
            if (jsDocSignature.type ***REMOVED******REMOVED******REMOVED*** void 0 && !isConstructorDeclaration(decl)) {
              reportImplicitAny(jsDocSignature, anyType);
            }
            result.push(getSignatureFromDeclaration(jsDocSignature));
          }
          continue;
        }
      }
      result.push(
        !isFunctionExpressionOrArrowFunction(decl) && !isObjectLiteralMethod(decl) && getSignatureOfTypeTag(decl) || getSignatureFromDeclaration(decl)
      );
    }
    return result;
  }
  function resolveExternalModuleTypeByLiteral(name) {
    const moduleSym ***REMOVED*** resolveExternalModuleName(name, name);
    if (moduleSym) {
      const resolvedModuleSymbol ***REMOVED*** resolveExternalModuleSymbol(moduleSym);
      if (resolvedModuleSymbol) {
        return getTypeOfSymbol(resolvedModuleSymbol);
      }
    }
    return anyType;
  }
  function getThisTypeOfSignature(signature) {
    if (signature.thisParameter) {
      return getTypeOfSymbol(signature.thisParameter);
    }
  }
  function getTypePredicateOfSignature(signature) {
    if (!signature.resolvedTypePredicate) {
      if (signature.target) {
        const targetTypePredicate ***REMOVED*** getTypePredicateOfSignature(signature.target);
        signature.resolvedTypePredicate ***REMOVED*** targetTypePredicate ? instantiateTypePredicate(targetTypePredicate, signature.mapper) : noTypePredicate;
      } else if (signature.compositeSignatures) {
        signature.resolvedTypePredicate ***REMOVED*** getUnionOrIntersectionTypePredicate(signature.compositeSignatures, signature.compositeKind) || noTypePredicate;
      } else {
        const type ***REMOVED*** signature.declaration && getEffectiveReturnTypeNode(signature.declaration);
        let jsdocPredicate;
        if (!type) {
          const jsdocSignature ***REMOVED*** getSignatureOfTypeTag(signature.declaration);
          if (jsdocSignature && signature !***REMOVED******REMOVED*** jsdocSignature) {
            jsdocPredicate ***REMOVED*** getTypePredicateOfSignature(jsdocSignature);
          }
        }
        if (type || jsdocPredicate) {
          signature.resolvedTypePredicate ***REMOVED*** type && isTypePredicateNode(type) ? createTypePredicateFromTypePredicateNode(type, signature) : jsdocPredicate || noTypePredicate;
        } else if (signature.declaration && isFunctionLikeDeclaration(signature.declaration) && (!signature.resolvedReturnType || signature.resolvedReturnType.flags & 16 /* Boolean */) && getParameterCount(signature) > 0) {
          const { declaration } ***REMOVED*** signature;
          signature.resolvedTypePredicate ***REMOVED*** noTypePredicate;
          signature.resolvedTypePredicate ***REMOVED*** getTypePredicateFromBody(declaration) || noTypePredicate;
        } else {
          signature.resolvedTypePredicate ***REMOVED*** noTypePredicate;
        }
      }
      Debug.assert(!!signature.resolvedTypePredicate);
    }
    return signature.resolvedTypePredicate ***REMOVED******REMOVED******REMOVED*** noTypePredicate ? void 0 : signature.resolvedTypePredicate;
  }
  function createTypePredicateFromTypePredicateNode(node, signature) {
    const parameterName ***REMOVED*** node.parameterName;
    const type ***REMOVED*** node.type && getTypeFromTypeNode(node.type);
    return parameterName.kind ***REMOVED******REMOVED******REMOVED*** 198 /* ThisType */ ? createTypePredicate(
      node.assertsModifier ? 2 /* AssertsThis */ : 0 /* This */,
      /*parameterName*/
      void 0,
      /*parameterIndex*/
      void 0,
      type
    ) : createTypePredicate(node.assertsModifier ? 3 /* AssertsIdentifier */ : 1 /* Identifier */, parameterName.escapedText, findIndex(signature.parameters, (p) ***REMOVED***> p.escapedName ***REMOVED******REMOVED******REMOVED*** parameterName.escapedText), type);
  }
  function getUnionOrIntersectionType(types, kind, unionReduction) {
    return kind !***REMOVED******REMOVED*** 2097152 /* Intersection */ ? getUnionType(types, unionReduction) : getIntersectionType(types);
  }
  function getReturnTypeOfSignature(signature) {
    if (!signature.resolvedReturnType) {
      if (!pushTypeResolution(signature, 3 /* ResolvedReturnType */)) {
        return errorType;
      }
      let type ***REMOVED*** signature.target ? instantiateType(getReturnTypeOfSignature(signature.target), signature.mapper) : signature.compositeSignatures ? instantiateType(getUnionOrIntersectionType(map(signature.compositeSignatures, getReturnTypeOfSignature), signature.compositeKind, 2 /* Subtype */), signature.mapper) : getReturnTypeFromAnnotation(signature.declaration) || (nodeIsMissing(signature.declaration.body) ? anyType : getReturnTypeFromBody(signature.declaration));
      if (signature.flags & 8 /* IsInnerCallChain */) {
        type ***REMOVED*** addOptionalTypeMarker(type);
      } else if (signature.flags & 16 /* IsOuterCallChain */) {
        type ***REMOVED*** getOptionalType(type);
      }
      if (!popTypeResolution()) {
        if (signature.declaration) {
          const typeNode ***REMOVED*** getEffectiveReturnTypeNode(signature.declaration);
          if (typeNode) {
            error(typeNode, Diagnostics.Return_type_annotation_circularly_references_itself);
          } else if (noImplicitAny) {
            const declaration ***REMOVED*** signature.declaration;
            const name ***REMOVED*** getNameOfDeclaration(declaration);
            if (name) {
              error(name, Diagnostics._0_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_referenced_directly_or_indirectly_in_one_of_its_return_expressions, declarationNameToString(name));
            } else {
              error(declaration, Diagnostics.Function_implicitly_has_return_type_any_because_it_does_not_have_a_return_type_annotation_and_is_referenced_directly_or_indirectly_in_one_of_its_return_expressions);
            }
          }
        }
        type ***REMOVED*** anyType;
      }
      signature.resolvedReturnType ?? (signature.resolvedReturnType ***REMOVED*** type);
    }
    return signature.resolvedReturnType;
  }
  function getReturnTypeFromAnnotation(declaration) {
    if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      return getDeclaredTypeOfClassOrInterface(getMergedSymbol(declaration.parent.symbol));
    }
    const typeNode ***REMOVED*** getEffectiveReturnTypeNode(declaration);
    if (isJSDocSignature(declaration)) {
      const root ***REMOVED*** getJSDocRoot(declaration);
      if (root && isConstructorDeclaration(root.parent) && !typeNode) {
        return getDeclaredTypeOfClassOrInterface(getMergedSymbol(root.parent.parent.symbol));
      }
    }
    if (isJSDocConstructSignature(declaration)) {
      return getTypeFromTypeNode(declaration.parameters[0].type);
    }
    if (typeNode) {
      return getTypeFromTypeNode(typeNode);
    }
    if (declaration.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ && hasBindableName(declaration)) {
      const jsDocType ***REMOVED*** isInJSFile(declaration) && getTypeForDeclarationFromJSDocComment(declaration);
      if (jsDocType) {
        return jsDocType;
      }
      const setter ***REMOVED*** getDeclarationOfKind(getSymbolOfDeclaration(declaration), 179 /* SetAccessor */);
      const setterType ***REMOVED*** getAnnotatedAccessorType(setter);
      if (setterType) {
        return setterType;
      }
    }
    return getReturnTypeOfTypeTag(declaration);
  }
  function isResolvingReturnTypeOfSignature(signature) {
    return signature.compositeSignatures && some(signature.compositeSignatures, isResolvingReturnTypeOfSignature) || !signature.resolvedReturnType && findResolutionCycleStartIndex(signature, 3 /* ResolvedReturnType */) >***REMOVED*** 0;
  }
  function getRestTypeOfSignature(signature) {
    return tryGetRestTypeOfSignature(signature) || anyType;
  }
  function tryGetRestTypeOfSignature(signature) {
    if (signatureHasRestParameter(signature)) {
      const sigRestType ***REMOVED*** getTypeOfSymbol(signature.parameters[signature.parameters.length - 1]);
      const restType ***REMOVED*** isTupleType(sigRestType) ? getRestTypeOfTupleType(sigRestType) : sigRestType;
      return restType && getIndexTypeOfType(restType, numberType);
    }
    return void 0;
  }
  function getSignatureInstantiation(signature, typeArguments, isJavascript, inferredTypeParameters) {
    const instantiatedSignature ***REMOVED*** getSignatureInstantiationWithoutFillingInTypeArguments(signature, fillMissingTypeArguments(typeArguments, signature.typeParameters, getMinTypeArgumentCount(signature.typeParameters), isJavascript));
    if (inferredTypeParameters) {
      const returnSignature ***REMOVED*** getSingleCallOrConstructSignature(getReturnTypeOfSignature(instantiatedSignature));
      if (returnSignature) {
        const newReturnSignature ***REMOVED*** cloneSignature(returnSignature);
        newReturnSignature.typeParameters ***REMOVED*** inferredTypeParameters;
        const newReturnType ***REMOVED*** getOrCreateTypeFromSignature(newReturnSignature);
        newReturnType.mapper ***REMOVED*** instantiatedSignature.mapper;
        const newInstantiatedSignature ***REMOVED*** cloneSignature(instantiatedSignature);
        newInstantiatedSignature.resolvedReturnType ***REMOVED*** newReturnType;
        return newInstantiatedSignature;
      }
    }
    return instantiatedSignature;
  }
  function getSignatureInstantiationWithoutFillingInTypeArguments(signature, typeArguments) {
    const instantiations ***REMOVED*** signature.instantiations || (signature.instantiations ***REMOVED*** /* @__PURE__ */ new Map());
    const id ***REMOVED*** getTypeListId(typeArguments);
    let instantiation ***REMOVED*** instantiations.get(id);
    if (!instantiation) {
      instantiations.set(id, instantiation ***REMOVED*** createSignatureInstantiation(signature, typeArguments));
    }
    return instantiation;
  }
  function createSignatureInstantiation(signature, typeArguments) {
    return instantiateSignature(
      signature,
      createSignatureTypeMapper(signature, typeArguments),
      /*eraseTypeParameters*/
      true
    );
  }
  function getTypeParametersForMapper(signature) {
    return sameMap(signature.typeParameters, (tp) ***REMOVED***> tp.mapper ? instantiateType(tp, tp.mapper) : tp);
  }
  function createSignatureTypeMapper(signature, typeArguments) {
    return createTypeMapper(getTypeParametersForMapper(signature), typeArguments);
  }
  function getErasedSignature(signature) {
    return signature.typeParameters ? signature.erasedSignatureCache || (signature.erasedSignatureCache ***REMOVED*** createErasedSignature(signature)) : signature;
  }
  function createErasedSignature(signature) {
    return instantiateSignature(
      signature,
      createTypeEraser(signature.typeParameters),
      /*eraseTypeParameters*/
      true
    );
  }
  function getCanonicalSignature(signature) {
    return signature.typeParameters ? signature.canonicalSignatureCache || (signature.canonicalSignatureCache ***REMOVED*** createCanonicalSignature(signature)) : signature;
  }
  function createCanonicalSignature(signature) {
    return getSignatureInstantiation(
      signature,
      map(signature.typeParameters, (tp) ***REMOVED***> tp.target && !getConstraintOfTypeParameter(tp.target) ? tp.target : tp),
      isInJSFile(signature.declaration)
    );
  }
  function getBaseSignature(signature) {
    const typeParameters ***REMOVED*** signature.typeParameters;
    if (typeParameters) {
      if (signature.baseSignatureCache) {
        return signature.baseSignatureCache;
      }
      const typeEraser ***REMOVED*** createTypeEraser(typeParameters);
      const baseConstraintMapper ***REMOVED*** createTypeMapper(typeParameters, map(typeParameters, (tp) ***REMOVED***> getConstraintOfTypeParameter(tp) || unknownType));
      let baseConstraints ***REMOVED*** map(typeParameters, (tp) ***REMOVED***> instantiateType(tp, baseConstraintMapper) || unknownType);
      for (let i ***REMOVED*** 0; i < typeParameters.length - 1; i++) {
        baseConstraints ***REMOVED*** instantiateTypes(baseConstraints, baseConstraintMapper);
      }
      baseConstraints ***REMOVED*** instantiateTypes(baseConstraints, typeEraser);
      return signature.baseSignatureCache ***REMOVED*** instantiateSignature(
        signature,
        createTypeMapper(typeParameters, baseConstraints),
        /*eraseTypeParameters*/
        true
      );
    }
    return signature;
  }
  function getOrCreateTypeFromSignature(signature) {
    var _a, _b;
    if (!signature.isolatedSignatureType) {
      const kind ***REMOVED*** (_a ***REMOVED*** signature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a.kind;
      const isConstructor ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** void 0 || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */;
      const type ***REMOVED*** createObjectType(16 /* Anonymous */ | 134217728 /* SingleSignatureType */, (_b ***REMOVED*** signature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _b.symbol);
      type.members ***REMOVED*** emptySymbols;
      type.properties ***REMOVED*** emptyArray;
      type.callSignatures ***REMOVED*** !isConstructor ? [signature] : emptyArray;
      type.constructSignatures ***REMOVED*** isConstructor ? [signature] : emptyArray;
      type.indexInfos ***REMOVED*** emptyArray;
      signature.isolatedSignatureType ***REMOVED*** type;
    }
    return signature.isolatedSignatureType;
  }
  function getIndexSymbol(symbol) {
    return symbol.members ? getIndexSymbolFromSymbolTable(getMembersOfSymbol(symbol)) : void 0;
  }
  function getIndexSymbolFromSymbolTable(symbolTable) {
    return symbolTable.get("__index" /* Index */);
  }
  function createIndexInfo(keyType, type, isReadonly, declaration, components) {
    return { keyType, type, isReadonly, declaration, components };
  }
  function getIndexInfosOfSymbol(symbol) {
    const indexSymbol ***REMOVED*** getIndexSymbol(symbol);
    return indexSymbol ? getIndexInfosOfIndexSymbol(indexSymbol, arrayFrom(getMembersOfSymbol(symbol).values())) : emptyArray;
  }
  function getIndexInfosOfIndexSymbol(indexSymbol, siblingSymbols ***REMOVED*** indexSymbol.parent ? arrayFrom(getMembersOfSymbol(indexSymbol.parent).values()) : void 0) {
    if (indexSymbol.declarations) {
      const indexInfos ***REMOVED*** [];
      let hasComputedNumberProperty ***REMOVED*** false;
      let readonlyComputedNumberProperty ***REMOVED*** true;
      let hasComputedSymbolProperty ***REMOVED*** false;
      let readonlyComputedSymbolProperty ***REMOVED*** true;
      let hasComputedStringProperty ***REMOVED*** false;
      let readonlyComputedStringProperty ***REMOVED*** true;
      const computedPropertySymbols ***REMOVED*** [];
      for (const declaration of indexSymbol.declarations) {
        if (isIndexSignatureDeclaration(declaration)) {
          if (declaration.parameters.length ***REMOVED******REMOVED******REMOVED*** 1) {
            const parameter ***REMOVED*** declaration.parameters[0];
            if (parameter.type) {
              forEachType(getTypeFromTypeNode(parameter.type), (keyType) ***REMOVED***> {
                if (isValidIndexKeyType(keyType) && !findIndexInfo(indexInfos, keyType)) {
                  indexInfos.push(createIndexInfo(keyType, declaration.type ? getTypeFromTypeNode(declaration.type) : anyType, hasEffectiveModifier(declaration, 8 /* Readonly */), declaration));
                }
              });
            }
          }
        } else if (hasLateBindableIndexSignature(declaration)) {
          const declName ***REMOVED*** isBinaryExpression(declaration) ? declaration.left : declaration.name;
          const keyType ***REMOVED*** isElementAccessExpression(declName) ? checkExpressionCached(declName.argumentExpression) : checkComputedPropertyName(declName);
          if (findIndexInfo(indexInfos, keyType)) {
            continue;
          }
          if (isTypeAssignableTo(keyType, stringNumberSymbolType)) {
            if (isTypeAssignableTo(keyType, numberType)) {
              hasComputedNumberProperty ***REMOVED*** true;
              if (!hasEffectiveReadonlyModifier(declaration)) {
                readonlyComputedNumberProperty ***REMOVED*** false;
              }
            } else if (isTypeAssignableTo(keyType, esSymbolType)) {
              hasComputedSymbolProperty ***REMOVED*** true;
              if (!hasEffectiveReadonlyModifier(declaration)) {
                readonlyComputedSymbolProperty ***REMOVED*** false;
              }
            } else {
              hasComputedStringProperty ***REMOVED*** true;
              if (!hasEffectiveReadonlyModifier(declaration)) {
                readonlyComputedStringProperty ***REMOVED*** false;
              }
            }
            computedPropertySymbols.push(declaration.symbol);
          }
        }
      }
      const allPropertySymbols ***REMOVED*** concatenate(computedPropertySymbols, filter(siblingSymbols, (s) ***REMOVED***> s !***REMOVED******REMOVED*** indexSymbol));
      if (hasComputedStringProperty && !findIndexInfo(indexInfos, stringType)) indexInfos.push(getObjectLiteralIndexInfo(readonlyComputedStringProperty, 0, allPropertySymbols, stringType));
      if (hasComputedNumberProperty && !findIndexInfo(indexInfos, numberType)) indexInfos.push(getObjectLiteralIndexInfo(readonlyComputedNumberProperty, 0, allPropertySymbols, numberType));
      if (hasComputedSymbolProperty && !findIndexInfo(indexInfos, esSymbolType)) indexInfos.push(getObjectLiteralIndexInfo(readonlyComputedSymbolProperty, 0, allPropertySymbols, esSymbolType));
      return indexInfos;
    }
    return emptyArray;
  }
  function isValidIndexKeyType(type) {
    return !!(type.flags & (4 /* String */ | 8 /* Number */ | 4096 /* ESSymbol */)) || isPatternLiteralType(type) || !!(type.flags & 2097152 /* Intersection */) && !isGenericType(type) && some(type.types, isValidIndexKeyType);
  }
  function getConstraintDeclaration(type) {
    return mapDefined(filter(type.symbol && type.symbol.declarations, isTypeParameterDeclaration), getEffectiveConstraintOfTypeParameter)[0];
  }
  function getInferredTypeParameterConstraint(typeParameter, omitTypeReferences) {
    var _a;
    let inferences;
    if ((_a ***REMOVED*** typeParameter.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.declarations) {
      for (const declaration of typeParameter.symbol.declarations) {
        if (declaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 196 /* InferType */) {
          const [childTypeParameter ***REMOVED*** declaration.parent, grandParent] ***REMOVED*** walkUpParenthesizedTypesAndGetParentAndChild(declaration.parent.parent);
          if (grandParent.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ && !omitTypeReferences) {
            const typeReference ***REMOVED*** grandParent;
            const typeParameters ***REMOVED*** getTypeParametersForTypeReferenceOrImport(typeReference);
            if (typeParameters) {
              const index ***REMOVED*** typeReference.typeArguments.indexOf(childTypeParameter);
              if (index < typeParameters.length) {
                const declaredConstraint ***REMOVED*** getConstraintOfTypeParameter(typeParameters[index]);
                if (declaredConstraint) {
                  const mapper ***REMOVED*** makeDeferredTypeMapper(
                    typeParameters,
                    typeParameters.map((_, index2) ***REMOVED***> () ***REMOVED***> {
                      return getEffectiveTypeArgumentAtIndex(typeReference, typeParameters, index2);
                    })
                  );
                  const constraint ***REMOVED*** instantiateType(declaredConstraint, mapper);
                  if (constraint !***REMOVED******REMOVED*** typeParameter) {
                    inferences ***REMOVED*** append(inferences, constraint);
                  }
                }
              }
            }
          } else if (grandParent.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && grandParent.dotDotDotToken || grandParent.kind ***REMOVED******REMOVED******REMOVED*** 192 /* RestType */ || grandParent.kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */ && grandParent.dotDotDotToken) {
            inferences ***REMOVED*** append(inferences, createArrayType(unknownType));
          } else if (grandParent.kind ***REMOVED******REMOVED******REMOVED*** 205 /* TemplateLiteralTypeSpan */) {
            inferences ***REMOVED*** append(inferences, stringType);
          } else if (grandParent.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ && grandParent.parent.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */) {
            inferences ***REMOVED*** append(inferences, stringNumberSymbolType);
          } else if (grandParent.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */ && grandParent.type && skipParentheses(grandParent.type) ***REMOVED******REMOVED******REMOVED*** declaration.parent && grandParent.parent.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */ && grandParent.parent.extendsType ***REMOVED******REMOVED******REMOVED*** grandParent && grandParent.parent.checkType.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */ && grandParent.parent.checkType.type) {
            const checkMappedType2 ***REMOVED*** grandParent.parent.checkType;
            const nodeType ***REMOVED*** getTypeFromTypeNode(checkMappedType2.type);
            inferences ***REMOVED*** append(inferences, instantiateType(nodeType, makeUnaryTypeMapper(getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(checkMappedType2.typeParameter)), checkMappedType2.typeParameter.constraint ? getTypeFromTypeNode(checkMappedType2.typeParameter.constraint) : stringNumberSymbolType)));
          }
        }
      }
    }
    return inferences && getIntersectionType(inferences);
  }
  function getConstraintFromTypeParameter(typeParameter) {
    if (!typeParameter.constraint) {
      if (typeParameter.target) {
        const targetConstraint ***REMOVED*** getConstraintOfTypeParameter(typeParameter.target);
        typeParameter.constraint ***REMOVED*** targetConstraint ? instantiateType(targetConstraint, typeParameter.mapper) : noConstraintType;
      } else {
        const constraintDeclaration ***REMOVED*** getConstraintDeclaration(typeParameter);
        if (!constraintDeclaration) {
          typeParameter.constraint ***REMOVED*** getInferredTypeParameterConstraint(typeParameter) || noConstraintType;
        } else {
          let type ***REMOVED*** getTypeFromTypeNode(constraintDeclaration);
          if (type.flags & 1 /* Any */ && !isErrorType(type)) {
            type ***REMOVED*** constraintDeclaration.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */ ? stringNumberSymbolType : unknownType;
          }
          typeParameter.constraint ***REMOVED*** type;
        }
      }
    }
    return typeParameter.constraint ***REMOVED******REMOVED******REMOVED*** noConstraintType ? void 0 : typeParameter.constraint;
  }
  function getParentSymbolOfTypeParameter(typeParameter) {
    const tp ***REMOVED*** getDeclarationOfKind(typeParameter.symbol, 169 /* TypeParameter */);
    const host2 ***REMOVED*** isJSDocTemplateTag(tp.parent) ? getEffectiveContainerForJSDocTemplateTag(tp.parent) : tp.parent;
    return host2 && getSymbolOfNode(host2);
  }
  function getTypeListId(types) {
    let result ***REMOVED*** "";
    if (types) {
      const length2 ***REMOVED*** types.length;
      let i ***REMOVED*** 0;
      while (i < length2) {
        const startId ***REMOVED*** types[i].id;
        let count ***REMOVED*** 1;
        while (i + count < length2 && types[i + count].id ***REMOVED******REMOVED******REMOVED*** startId + count) {
          count++;
        }
        if (result.length) {
          result +***REMOVED*** ",";
        }
        result +***REMOVED*** startId;
        if (count > 1) {
          result +***REMOVED*** ":" + count;
        }
        i +***REMOVED*** count;
      }
    }
    return result;
  }
  function getAliasId(aliasSymbol, aliasTypeArguments) {
    return aliasSymbol ? `@${getSymbolId(aliasSymbol)}` + (aliasTypeArguments ? `:${getTypeListId(aliasTypeArguments)}` : "") : "";
  }
  function getPropagatingFlagsOfTypes(types, excludeKinds) {
    let result ***REMOVED*** 0;
    for (const type of types) {
      if (excludeKinds ***REMOVED******REMOVED******REMOVED*** void 0 || !(type.flags & excludeKinds)) {
        result |***REMOVED*** getObjectFlags(type);
      }
    }
    return result & 458752 /* PropagatingFlags */;
  }
  function tryCreateTypeReference(target, typeArguments) {
    if (some(typeArguments) && target ***REMOVED******REMOVED******REMOVED*** emptyGenericType) {
      return unknownType;
    }
    return createTypeReference(target, typeArguments);
  }
  function createTypeReference(target, typeArguments) {
    const id ***REMOVED*** getTypeListId(typeArguments);
    let type ***REMOVED*** target.instantiations.get(id);
    if (!type) {
      type ***REMOVED*** createObjectType(4 /* Reference */, target.symbol);
      target.instantiations.set(id, type);
      type.objectFlags |***REMOVED*** typeArguments ? getPropagatingFlagsOfTypes(typeArguments) : 0;
      type.target ***REMOVED*** target;
      type.resolvedTypeArguments ***REMOVED*** typeArguments;
    }
    return type;
  }
  function cloneTypeReference(source) {
    const type ***REMOVED*** createTypeWithSymbol(source.flags, source.symbol);
    type.objectFlags ***REMOVED*** source.objectFlags;
    type.target ***REMOVED*** source.target;
    type.resolvedTypeArguments ***REMOVED*** source.resolvedTypeArguments;
    return type;
  }
  function createDeferredTypeReference(target, node, mapper, aliasSymbol, aliasTypeArguments) {
    if (!aliasSymbol) {
      aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      const localAliasTypeArguments ***REMOVED*** getTypeArgumentsForAliasSymbol(aliasSymbol);
      aliasTypeArguments ***REMOVED*** mapper ? instantiateTypes(localAliasTypeArguments, mapper) : localAliasTypeArguments;
    }
    const type ***REMOVED*** createObjectType(4 /* Reference */, target.symbol);
    type.target ***REMOVED*** target;
    type.node ***REMOVED*** node;
    type.mapper ***REMOVED*** mapper;
    type.aliasSymbol ***REMOVED*** aliasSymbol;
    type.aliasTypeArguments ***REMOVED*** aliasTypeArguments;
    return type;
  }
  function getTypeArguments(type) {
    var _a, _b;
    if (!type.resolvedTypeArguments) {
      if (!pushTypeResolution(type, 5 /* ResolvedTypeArguments */)) {
        return concatenate(type.target.outerTypeParameters, (_a ***REMOVED*** type.target.localTypeParameters) ***REMOVED******REMOVED*** null ? void 0 : _a.map(() ***REMOVED***> errorType)) || emptyArray;
      }
      const node ***REMOVED*** type.node;
      const typeArguments ***REMOVED*** !node ? emptyArray : node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ ? concatenate(type.target.outerTypeParameters, getEffectiveTypeArguments(node, type.target.localTypeParameters)) : node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */ ? [getTypeFromTypeNode(node.elementType)] : map(node.elements, getTypeFromTypeNode);
      if (popTypeResolution()) {
        type.resolvedTypeArguments ?? (type.resolvedTypeArguments ***REMOVED*** type.mapper ? instantiateTypes(typeArguments, type.mapper) : typeArguments);
      } else {
        type.resolvedTypeArguments ?? (type.resolvedTypeArguments ***REMOVED*** concatenate(type.target.outerTypeParameters, ((_b ***REMOVED*** type.target.localTypeParameters) ***REMOVED******REMOVED*** null ? void 0 : _b.map(() ***REMOVED***> errorType)) || emptyArray));
        error(
          type.node || currentNode,
          type.target.symbol ? Diagnostics.Type_arguments_for_0_circularly_reference_themselves : Diagnostics.Tuple_type_arguments_circularly_reference_themselves,
          type.target.symbol && symbolToString(type.target.symbol)
        );
      }
    }
    return type.resolvedTypeArguments;
  }
  function getTypeReferenceArity(type) {
    return length(type.target.typeParameters);
  }
  function getTypeFromClassOrInterfaceReference(node, symbol) {
    const type ***REMOVED*** getDeclaredTypeOfSymbol(getMergedSymbol(symbol));
    const typeParameters ***REMOVED*** type.localTypeParameters;
    if (typeParameters) {
      const numTypeArguments ***REMOVED*** length(node.typeArguments);
      const minTypeArgumentCount ***REMOVED*** getMinTypeArgumentCount(typeParameters);
      const isJs ***REMOVED*** isInJSFile(node);
      const isJsImplicitAny ***REMOVED*** !noImplicitAny && isJs;
      if (!isJsImplicitAny && (numTypeArguments < minTypeArgumentCount || numTypeArguments > typeParameters.length)) {
        const missingAugmentsTag ***REMOVED*** isJs && isExpressionWithTypeArguments(node) && !isJSDocAugmentsTag(node.parent);
        const diag2 ***REMOVED*** minTypeArgumentCount ***REMOVED******REMOVED******REMOVED*** typeParameters.length ? missingAugmentsTag ? Diagnostics.Expected_0_type_arguments_provide_these_with_an_extends_tag : Diagnostics.Generic_type_0_requires_1_type_argument_s : missingAugmentsTag ? Diagnostics.Expected_0_1_type_arguments_provide_these_with_an_extends_tag : Diagnostics.Generic_type_0_requires_between_1_and_2_type_arguments;
        const typeStr ***REMOVED*** typeToString(
          type,
          /*enclosingDeclaration*/
          void 0,
          2 /* WriteArrayAsGenericType */
        );
        error(node, diag2, typeStr, minTypeArgumentCount, typeParameters.length);
        if (!isJs) {
          return errorType;
        }
      }
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ && isDeferredTypeReferenceNode(node, length(node.typeArguments) !***REMOVED******REMOVED*** typeParameters.length)) {
        return createDeferredTypeReference(
          type,
          node,
          /*mapper*/
          void 0
        );
      }
      const typeArguments ***REMOVED*** concatenate(type.outerTypeParameters, fillMissingTypeArguments(typeArgumentsFromTypeReferenceNode(node), typeParameters, minTypeArgumentCount, isJs));
      return createTypeReference(type, typeArguments);
    }
    return checkNoTypeArguments(node, symbol) ? type : errorType;
  }
  function getTypeAliasInstantiation(symbol, typeArguments, aliasSymbol, aliasTypeArguments) {
    const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
    if (type ***REMOVED******REMOVED******REMOVED*** intrinsicMarkerType) {
      const typeKind ***REMOVED*** intrinsicTypeKinds.get(symbol.escapedName);
      if (typeKind !***REMOVED******REMOVED*** void 0 && typeArguments && typeArguments.length ***REMOVED******REMOVED******REMOVED*** 1) {
        return typeKind ***REMOVED******REMOVED******REMOVED*** 4 /* NoInfer */ ? getNoInferType(typeArguments[0]) : getStringMappingType(symbol, typeArguments[0]);
      }
    }
    const links ***REMOVED*** getSymbolLinks(symbol);
    const typeParameters ***REMOVED*** links.typeParameters;
    const id ***REMOVED*** getTypeListId(typeArguments) + getAliasId(aliasSymbol, aliasTypeArguments);
    let instantiation ***REMOVED*** links.instantiations.get(id);
    if (!instantiation) {
      links.instantiations.set(id, instantiation ***REMOVED*** instantiateTypeWithAlias(type, createTypeMapper(typeParameters, fillMissingTypeArguments(typeArguments, typeParameters, getMinTypeArgumentCount(typeParameters), isInJSFile(symbol.valueDeclaration))), aliasSymbol, aliasTypeArguments));
    }
    return instantiation;
  }
  function getTypeFromTypeAliasReference(node, symbol) {
    if (getCheckFlags(symbol) & 1048576 /* Unresolved */) {
      const typeArguments ***REMOVED*** typeArgumentsFromTypeReferenceNode(node);
      const id ***REMOVED*** getAliasId(symbol, typeArguments);
      let errorType2 ***REMOVED*** errorTypes.get(id);
      if (!errorType2) {
        errorType2 ***REMOVED*** createIntrinsicType(
          1 /* Any */,
          "error",
          /*objectFlags*/
          void 0,
          `alias ${id}`
        );
        errorType2.aliasSymbol ***REMOVED*** symbol;
        errorType2.aliasTypeArguments ***REMOVED*** typeArguments;
        errorTypes.set(id, errorType2);
      }
      return errorType2;
    }
    const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
    const typeParameters ***REMOVED*** getSymbolLinks(symbol).typeParameters;
    if (typeParameters) {
      const numTypeArguments ***REMOVED*** length(node.typeArguments);
      const minTypeArgumentCount ***REMOVED*** getMinTypeArgumentCount(typeParameters);
      if (numTypeArguments < minTypeArgumentCount || numTypeArguments > typeParameters.length) {
        error(
          node,
          minTypeArgumentCount ***REMOVED******REMOVED******REMOVED*** typeParameters.length ? Diagnostics.Generic_type_0_requires_1_type_argument_s : Diagnostics.Generic_type_0_requires_between_1_and_2_type_arguments,
          symbolToString(symbol),
          minTypeArgumentCount,
          typeParameters.length
        );
        return errorType;
      }
      const aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      let newAliasSymbol ***REMOVED*** aliasSymbol && (isLocalTypeAlias(symbol) || !isLocalTypeAlias(aliasSymbol)) ? aliasSymbol : void 0;
      let aliasTypeArguments;
      if (newAliasSymbol) {
        aliasTypeArguments ***REMOVED*** getTypeArgumentsForAliasSymbol(newAliasSymbol);
      } else if (isTypeReferenceType(node)) {
        const aliasSymbol2 ***REMOVED*** resolveTypeReferenceName(
          node,
          2097152 /* Alias */,
          /*ignoreErrors*/
          true
        );
        if (aliasSymbol2 && aliasSymbol2 !***REMOVED******REMOVED*** unknownSymbol) {
          const resolved ***REMOVED*** resolveAlias(aliasSymbol2);
          if (resolved && resolved.flags & 524288 /* TypeAlias */) {
            newAliasSymbol ***REMOVED*** resolved;
            aliasTypeArguments ***REMOVED*** typeArgumentsFromTypeReferenceNode(node) || (typeParameters ? [] : void 0);
          }
        }
      }
      return getTypeAliasInstantiation(symbol, typeArgumentsFromTypeReferenceNode(node), newAliasSymbol, aliasTypeArguments);
    }
    return checkNoTypeArguments(node, symbol) ? type : errorType;
  }
  function isLocalTypeAlias(symbol) {
    var _a;
    const declaration ***REMOVED*** (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isTypeAlias);
    return !!(declaration && getContainingFunction(declaration));
  }
  function getTypeReferenceName(node) {
    switch (node.kind) {
      case 184 /* TypeReference */:
        return node.typeName;
      case 234 /* ExpressionWithTypeArguments */:
        const expr ***REMOVED*** node.expression;
        if (isEntityNameExpression(expr)) {
          return expr;
        }
    }
    return void 0;
  }
  function getSymbolPath(symbol) {
    return symbol.parent ? `${getSymbolPath(symbol.parent)}.${symbol.escapedName}` : symbol.escapedName;
  }
  function getUnresolvedSymbolForEntityName(name) {
    const identifier ***REMOVED*** name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? name.right : name.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ ? name.name : name;
    const text ***REMOVED*** identifier.escapedText;
    if (text) {
      const parentSymbol ***REMOVED*** name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? getUnresolvedSymbolForEntityName(name.left) : name.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ ? getUnresolvedSymbolForEntityName(name.expression) : void 0;
      const path ***REMOVED*** parentSymbol ? `${getSymbolPath(parentSymbol)}.${text}` : text;
      let result ***REMOVED*** unresolvedSymbols.get(path);
      if (!result) {
        unresolvedSymbols.set(path, result ***REMOVED*** createSymbol(524288 /* TypeAlias */, text, 1048576 /* Unresolved */));
        result.parent ***REMOVED*** parentSymbol;
        result.links.declaredType ***REMOVED*** unresolvedType;
      }
      return result;
    }
    return unknownSymbol;
  }
  function resolveTypeReferenceName(typeReference, meaning, ignoreErrors) {
    const name ***REMOVED*** getTypeReferenceName(typeReference);
    if (!name) {
      return unknownSymbol;
    }
    const symbol ***REMOVED*** resolveEntityName(name, meaning, ignoreErrors);
    return symbol && symbol !***REMOVED******REMOVED*** unknownSymbol ? symbol : ignoreErrors ? unknownSymbol : getUnresolvedSymbolForEntityName(name);
  }
  function getTypeReferenceType(node, symbol) {
    if (symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return errorType;
    }
    symbol ***REMOVED*** getExpandoSymbol(symbol) || symbol;
    if (symbol.flags & (32 /* Class */ | 64 /* Interface */)) {
      return getTypeFromClassOrInterfaceReference(node, symbol);
    }
    if (symbol.flags & 524288 /* TypeAlias */) {
      return getTypeFromTypeAliasReference(node, symbol);
    }
    const res ***REMOVED*** tryGetDeclaredTypeOfSymbol(symbol);
    if (res) {
      return checkNoTypeArguments(node, symbol) ? getRegularTypeOfLiteralType(res) : errorType;
    }
    if (symbol.flags & 111551 /* Value */ && isJSDocTypeReference(node)) {
      const jsdocType ***REMOVED*** getTypeFromJSDocValueReference(node, symbol);
      if (jsdocType) {
        return jsdocType;
      } else {
        resolveTypeReferenceName(node, 788968 /* Type */);
        return getTypeOfSymbol(symbol);
      }
    }
    return errorType;
  }
  function getTypeFromJSDocValueReference(node, symbol) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedJSDocType) {
      const valueType ***REMOVED*** getTypeOfSymbol(symbol);
      let typeType ***REMOVED*** valueType;
      if (symbol.valueDeclaration) {
        const isImportTypeWithQualifier ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */ && node.qualifier;
        if (valueType.symbol && valueType.symbol !***REMOVED******REMOVED*** symbol && isImportTypeWithQualifier) {
          typeType ***REMOVED*** getTypeReferenceType(node, valueType.symbol);
        }
      }
      links.resolvedJSDocType ***REMOVED*** typeType;
    }
    return links.resolvedJSDocType;
  }
  function getNoInferType(type) {
    return isNoInferTargetType(type) ? getOrCreateSubstitutionType(type, unknownType) : type;
  }
  function isNoInferTargetType(type) {
    return !!(type.flags & 3145728 /* UnionOrIntersection */ && some(type.types, isNoInferTargetType) || type.flags & 33554432 /* Substitution */ && !isNoInferType(type) && isNoInferTargetType(type.baseType) || type.flags & 524288 /* Object */ && !isEmptyAnonymousObjectType(type) || type.flags & (465829888 /* Instantiable */ & ~33554432 /* Substitution */) && !isPatternLiteralType(type));
  }
  function isNoInferType(type) {
    return !!(type.flags & 33554432 /* Substitution */ && type.constraint.flags & 2 /* Unknown */);
  }
  function getSubstitutionType(baseType, constraint) {
    return constraint.flags & 3 /* AnyOrUnknown */ || constraint ***REMOVED******REMOVED******REMOVED*** baseType || baseType.flags & 1 /* Any */ ? baseType : getOrCreateSubstitutionType(baseType, constraint);
  }
  function getOrCreateSubstitutionType(baseType, constraint) {
    const id ***REMOVED*** `${getTypeId(baseType)}>${getTypeId(constraint)}`;
    const cached ***REMOVED*** substitutionTypes.get(id);
    if (cached) {
      return cached;
    }
    const result ***REMOVED*** createType(33554432 /* Substitution */);
    result.baseType ***REMOVED*** baseType;
    result.constraint ***REMOVED*** constraint;
    substitutionTypes.set(id, result);
    return result;
  }
  function getSubstitutionIntersection(substitutionType) {
    return isNoInferType(substitutionType) ? substitutionType.baseType : getIntersectionType([substitutionType.constraint, substitutionType.baseType]);
  }
  function isUnaryTupleTypeNode(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */ && node.elements.length ***REMOVED******REMOVED******REMOVED*** 1;
  }
  function getImpliedConstraint(type, checkNode, extendsNode) {
    return isUnaryTupleTypeNode(checkNode) && isUnaryTupleTypeNode(extendsNode) ? getImpliedConstraint(type, checkNode.elements[0], extendsNode.elements[0]) : getActualTypeVariable(getTypeFromTypeNode(checkNode)) ***REMOVED******REMOVED******REMOVED*** getActualTypeVariable(type) ? getTypeFromTypeNode(extendsNode) : void 0;
  }
  function getConditionalFlowTypeOfType(type, node) {
    let constraints;
    let covariant ***REMOVED*** true;
    while (node && !isStatement(node) && node.kind !***REMOVED******REMOVED*** 321 /* JSDoc */) {
      const parent ***REMOVED*** node.parent;
      if (parent.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
        covariant ***REMOVED*** !covariant;
      }
      if ((covariant || type.flags & 8650752 /* TypeVariable */) && parent.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */ && node ***REMOVED******REMOVED******REMOVED*** parent.trueType) {
        const constraint ***REMOVED*** getImpliedConstraint(type, parent.checkType, parent.extendsType);
        if (constraint) {
          constraints ***REMOVED*** append(constraints, constraint);
        }
      } else if (type.flags & 262144 /* TypeParameter */ && parent.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */ && !parent.nameType && node ***REMOVED******REMOVED******REMOVED*** parent.type) {
        const mappedType ***REMOVED*** getTypeFromTypeNode(parent);
        if (getTypeParameterFromMappedType(mappedType) ***REMOVED******REMOVED******REMOVED*** getActualTypeVariable(type)) {
          const typeParameter ***REMOVED*** getHomomorphicTypeVariable(mappedType);
          if (typeParameter) {
            const constraint ***REMOVED*** getConstraintOfTypeParameter(typeParameter);
            if (constraint && everyType(constraint, isArrayOrTupleType)) {
              constraints ***REMOVED*** append(constraints, getUnionType([numberType, numericStringType]));
            }
          }
        }
      }
      node ***REMOVED*** parent;
    }
    return constraints ? getSubstitutionType(type, getIntersectionType(constraints)) : type;
  }
  function isJSDocTypeReference(node) {
    return !!(node.flags & 16777216 /* JSDoc */) && (node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ || node.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */);
  }
  function checkNoTypeArguments(node, symbol) {
    if (node.typeArguments) {
      error(node, Diagnostics.Type_0_is_not_generic, symbol ? symbolToString(symbol) : node.typeName ? declarationNameToString(node.typeName) : anon);
      return false;
    }
    return true;
  }
  function getIntendedTypeFromJSDocTypeReference(node) {
    if (isIdentifier(node.typeName)) {
      const typeArgs ***REMOVED*** node.typeArguments;
      switch (node.typeName.escapedText) {
        case "String":
          checkNoTypeArguments(node);
          return stringType;
        case "Number":
          checkNoTypeArguments(node);
          return numberType;
        case "BigInt":
          checkNoTypeArguments(node);
          return bigintType;
        case "Boolean":
          checkNoTypeArguments(node);
          return booleanType;
        case "Void":
          checkNoTypeArguments(node);
          return voidType;
        case "Undefined":
          checkNoTypeArguments(node);
          return undefinedType;
        case "Null":
          checkNoTypeArguments(node);
          return nullType;
        case "Function":
        case "function":
          checkNoTypeArguments(node);
          return globalFunctionType;
        case "array":
          return (!typeArgs || !typeArgs.length) && !noImplicitAny ? anyArrayType : void 0;
        case "promise":
          return (!typeArgs || !typeArgs.length) && !noImplicitAny ? createPromiseType(anyType) : void 0;
        case "Object":
          if (typeArgs && typeArgs.length ***REMOVED******REMOVED******REMOVED*** 2) {
            if (isJSDocIndexSignature(node)) {
              const indexed ***REMOVED*** getTypeFromTypeNode(typeArgs[0]);
              const target ***REMOVED*** getTypeFromTypeNode(typeArgs[1]);
              const indexInfo ***REMOVED*** indexed ***REMOVED******REMOVED******REMOVED*** stringType || indexed ***REMOVED******REMOVED******REMOVED*** numberType ? [createIndexInfo(
                indexed,
                target,
                /*isReadonly*/
                false
              )] : emptyArray;
              return createAnonymousType(
                /*symbol*/
                void 0,
                emptySymbols,
                emptyArray,
                emptyArray,
                indexInfo
              );
            }
            return anyType;
          }
          checkNoTypeArguments(node);
          return !noImplicitAny ? anyType : void 0;
      }
    }
  }
  function getTypeFromJSDocNullableTypeNode(node) {
    const type ***REMOVED*** getTypeFromTypeNode(node.type);
    return strictNullChecks ? getNullableType(type, 65536 /* Null */) : type;
  }
  function getTypeFromTypeReference(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      if (isConstTypeReference(node) && isAssertionExpression(node.parent)) {
        links.resolvedSymbol ***REMOVED*** unknownSymbol;
        return links.resolvedType ***REMOVED*** checkExpressionCached(node.parent.expression);
      }
      let symbol;
      let type;
      const meaning ***REMOVED*** 788968 /* Type */;
      if (isJSDocTypeReference(node)) {
        type ***REMOVED*** getIntendedTypeFromJSDocTypeReference(node);
        if (!type) {
          symbol ***REMOVED*** resolveTypeReferenceName(
            node,
            meaning,
            /*ignoreErrors*/
            true
          );
          if (symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
            symbol ***REMOVED*** resolveTypeReferenceName(node, meaning | 111551 /* Value */);
          } else {
            resolveTypeReferenceName(node, meaning);
          }
          type ***REMOVED*** getTypeReferenceType(node, symbol);
        }
      }
      if (!type) {
        symbol ***REMOVED*** resolveTypeReferenceName(node, meaning);
        type ***REMOVED*** getTypeReferenceType(node, symbol);
      }
      links.resolvedSymbol ***REMOVED*** symbol;
      links.resolvedType ***REMOVED*** type;
    }
    return links.resolvedType;
  }
  function typeArgumentsFromTypeReferenceNode(node) {
    return map(node.typeArguments, getTypeFromTypeNode);
  }
  function getTypeFromTypeQueryNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const type ***REMOVED*** checkExpressionWithTypeArguments(node);
      links.resolvedType ***REMOVED*** getRegularTypeOfLiteralType(getWidenedType(type));
    }
    return links.resolvedType;
  }
  function getTypeOfGlobalSymbol(symbol, arity) {
    function getTypeDeclaration(symbol2) {
      const declarations ***REMOVED*** symbol2.declarations;
      if (declarations) {
        for (const declaration of declarations) {
          switch (declaration.kind) {
            case 264 /* ClassDeclaration */:
            case 265 /* InterfaceDeclaration */:
            case 267 /* EnumDeclaration */:
              return declaration;
          }
        }
      }
    }
    if (!symbol) {
      return arity ? emptyGenericType : emptyObjectType;
    }
    const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
    if (!(type.flags & 524288 /* Object */)) {
      error(getTypeDeclaration(symbol), Diagnostics.Global_type_0_must_be_a_class_or_interface_type, symbolName(symbol));
      return arity ? emptyGenericType : emptyObjectType;
    }
    if (length(type.typeParameters) !***REMOVED******REMOVED*** arity) {
      error(getTypeDeclaration(symbol), Diagnostics.Global_type_0_must_have_1_type_parameter_s, symbolName(symbol), arity);
      return arity ? emptyGenericType : emptyObjectType;
    }
    return type;
  }
  function getGlobalValueSymbol(name, reportErrors2) {
    return getGlobalSymbol(name, 111551 /* Value */, reportErrors2 ? Diagnostics.Cannot_find_global_value_0 : void 0);
  }
  function getGlobalTypeSymbol(name, reportErrors2) {
    return getGlobalSymbol(name, 788968 /* Type */, reportErrors2 ? Diagnostics.Cannot_find_global_type_0 : void 0);
  }
  function getGlobalTypeAliasSymbol(name, arity, reportErrors2) {
    const symbol ***REMOVED*** getGlobalSymbol(name, 788968 /* Type */, reportErrors2 ? Diagnostics.Cannot_find_global_type_0 : void 0);
    if (symbol) {
      getDeclaredTypeOfSymbol(symbol);
      if (length(getSymbolLinks(symbol).typeParameters) !***REMOVED******REMOVED*** arity) {
        const decl ***REMOVED*** symbol.declarations && find(symbol.declarations, isTypeAliasDeclaration);
        error(decl, Diagnostics.Global_type_0_must_have_1_type_parameter_s, symbolName(symbol), arity);
        return void 0;
      }
    }
    return symbol;
  }
  function getGlobalSymbol(name, meaning, diagnostic) {
    return resolveName(
      /*location*/
      void 0,
      name,
      meaning,
      diagnostic,
      /*isUse*/
      false,
      /*excludeGlobals*/
      false
    );
  }
  function getGlobalType(name, arity, reportErrors2) {
    const symbol ***REMOVED*** getGlobalTypeSymbol(name, reportErrors2);
    return symbol || reportErrors2 ? getTypeOfGlobalSymbol(symbol, arity) : void 0;
  }
  function getGlobalBuiltinTypes(typeNames, arity) {
    let types;
    for (const typeName of typeNames) {
      types ***REMOVED*** append(types, getGlobalType(
        typeName,
        arity,
        /*reportErrors*/
        false
      ));
    }
    return types ?? emptyArray;
  }
  function getGlobalTypedPropertyDescriptorType() {
    return deferredGlobalTypedPropertyDescriptorType || (deferredGlobalTypedPropertyDescriptorType ***REMOVED*** getGlobalType(
      "TypedPropertyDescriptor",
      /*arity*/
      1,
      /*reportErrors*/
      true
    ) || emptyGenericType);
  }
  function getGlobalTemplateStringsArrayType() {
    return deferredGlobalTemplateStringsArrayType || (deferredGlobalTemplateStringsArrayType ***REMOVED*** getGlobalType(
      "TemplateStringsArray",
      /*arity*/
      0,
      /*reportErrors*/
      true
    ) || emptyObjectType);
  }
  function getGlobalImportMetaType() {
    return deferredGlobalImportMetaType || (deferredGlobalImportMetaType ***REMOVED*** getGlobalType(
      "ImportMeta",
      /*arity*/
      0,
      /*reportErrors*/
      true
    ) || emptyObjectType);
  }
  function getGlobalImportMetaExpressionType() {
    if (!deferredGlobalImportMetaExpressionType) {
      const symbol ***REMOVED*** createSymbol(0 /* None */, "ImportMetaExpression");
      const importMetaType ***REMOVED*** getGlobalImportMetaType();
      const metaPropertySymbol ***REMOVED*** createSymbol(4 /* Property */, "meta", 8 /* Readonly */);
      metaPropertySymbol.parent ***REMOVED*** symbol;
      metaPropertySymbol.links.type ***REMOVED*** importMetaType;
      const members ***REMOVED*** createSymbolTable([metaPropertySymbol]);
      symbol.members ***REMOVED*** members;
      deferredGlobalImportMetaExpressionType ***REMOVED*** createAnonymousType(symbol, members, emptyArray, emptyArray, emptyArray);
    }
    return deferredGlobalImportMetaExpressionType;
  }
  function getGlobalImportCallOptionsType(reportErrors2) {
    return deferredGlobalImportCallOptionsType || (deferredGlobalImportCallOptionsType ***REMOVED*** getGlobalType(
      "ImportCallOptions",
      /*arity*/
      0,
      reportErrors2
    )) || emptyObjectType;
  }
  function getGlobalImportAttributesType(reportErrors2) {
    return deferredGlobalImportAttributesType || (deferredGlobalImportAttributesType ***REMOVED*** getGlobalType(
      "ImportAttributes",
      /*arity*/
      0,
      reportErrors2
    )) || emptyObjectType;
  }
  function getGlobalESSymbolConstructorSymbol(reportErrors2) {
    return deferredGlobalESSymbolConstructorSymbol || (deferredGlobalESSymbolConstructorSymbol ***REMOVED*** getGlobalValueSymbol("Symbol", reportErrors2));
  }
  function getGlobalESSymbolConstructorTypeSymbol(reportErrors2) {
    return deferredGlobalESSymbolConstructorTypeSymbol || (deferredGlobalESSymbolConstructorTypeSymbol ***REMOVED*** getGlobalTypeSymbol("SymbolConstructor", reportErrors2));
  }
  function getGlobalESSymbolType() {
    return deferredGlobalESSymbolType || (deferredGlobalESSymbolType ***REMOVED*** getGlobalType(
      "Symbol",
      /*arity*/
      0,
      /*reportErrors*/
      false
    )) || emptyObjectType;
  }
  function getGlobalPromiseType(reportErrors2) {
    return deferredGlobalPromiseType || (deferredGlobalPromiseType ***REMOVED*** getGlobalType(
      "Promise",
      /*arity*/
      1,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalPromiseLikeType(reportErrors2) {
    return deferredGlobalPromiseLikeType || (deferredGlobalPromiseLikeType ***REMOVED*** getGlobalType(
      "PromiseLike",
      /*arity*/
      1,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalPromiseConstructorSymbol(reportErrors2) {
    return deferredGlobalPromiseConstructorSymbol || (deferredGlobalPromiseConstructorSymbol ***REMOVED*** getGlobalValueSymbol("Promise", reportErrors2));
  }
  function getGlobalPromiseConstructorLikeType(reportErrors2) {
    return deferredGlobalPromiseConstructorLikeType || (deferredGlobalPromiseConstructorLikeType ***REMOVED*** getGlobalType(
      "PromiseConstructorLike",
      /*arity*/
      0,
      reportErrors2
    )) || emptyObjectType;
  }
  function getGlobalAsyncIterableType(reportErrors2) {
    return deferredGlobalAsyncIterableType || (deferredGlobalAsyncIterableType ***REMOVED*** getGlobalType(
      "AsyncIterable",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalAsyncIteratorType(reportErrors2) {
    return deferredGlobalAsyncIteratorType || (deferredGlobalAsyncIteratorType ***REMOVED*** getGlobalType(
      "AsyncIterator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalAsyncIterableIteratorType(reportErrors2) {
    return deferredGlobalAsyncIterableIteratorType || (deferredGlobalAsyncIterableIteratorType ***REMOVED*** getGlobalType(
      "AsyncIterableIterator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalBuiltinAsyncIteratorTypes() {
    return deferredGlobalBuiltinAsyncIteratorTypes ?? (deferredGlobalBuiltinAsyncIteratorTypes ***REMOVED*** getGlobalBuiltinTypes(["ReadableStreamAsyncIterator"], 1));
  }
  function getGlobalAsyncIteratorObjectType(reportErrors2) {
    return deferredGlobalAsyncIteratorObjectType || (deferredGlobalAsyncIteratorObjectType ***REMOVED*** getGlobalType(
      "AsyncIteratorObject",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalAsyncGeneratorType(reportErrors2) {
    return deferredGlobalAsyncGeneratorType || (deferredGlobalAsyncGeneratorType ***REMOVED*** getGlobalType(
      "AsyncGenerator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalIterableType(reportErrors2) {
    return deferredGlobalIterableType || (deferredGlobalIterableType ***REMOVED*** getGlobalType(
      "Iterable",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalIteratorType(reportErrors2) {
    return deferredGlobalIteratorType || (deferredGlobalIteratorType ***REMOVED*** getGlobalType(
      "Iterator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalIterableIteratorType(reportErrors2) {
    return deferredGlobalIterableIteratorType || (deferredGlobalIterableIteratorType ***REMOVED*** getGlobalType(
      "IterableIterator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getBuiltinIteratorReturnType() {
    return strictBuiltinIteratorReturn ? undefinedType : anyType;
  }
  function getGlobalBuiltinIteratorTypes() {
    return deferredGlobalBuiltinIteratorTypes ?? (deferredGlobalBuiltinIteratorTypes ***REMOVED*** getGlobalBuiltinTypes(["ArrayIterator", "MapIterator", "SetIterator", "StringIterator"], 1));
  }
  function getGlobalIteratorObjectType(reportErrors2) {
    return deferredGlobalIteratorObjectType || (deferredGlobalIteratorObjectType ***REMOVED*** getGlobalType(
      "IteratorObject",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalGeneratorType(reportErrors2) {
    return deferredGlobalGeneratorType || (deferredGlobalGeneratorType ***REMOVED*** getGlobalType(
      "Generator",
      /*arity*/
      3,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalIteratorYieldResultType(reportErrors2) {
    return deferredGlobalIteratorYieldResultType || (deferredGlobalIteratorYieldResultType ***REMOVED*** getGlobalType(
      "IteratorYieldResult",
      /*arity*/
      1,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalIteratorReturnResultType(reportErrors2) {
    return deferredGlobalIteratorReturnResultType || (deferredGlobalIteratorReturnResultType ***REMOVED*** getGlobalType(
      "IteratorReturnResult",
      /*arity*/
      1,
      reportErrors2
    )) || emptyGenericType;
  }
  function getGlobalDisposableType(reportErrors2) {
    return deferredGlobalDisposableType || (deferredGlobalDisposableType ***REMOVED*** getGlobalType(
      "Disposable",
      /*arity*/
      0,
      reportErrors2
    )) || emptyObjectType;
  }
  function getGlobalAsyncDisposableType(reportErrors2) {
    return deferredGlobalAsyncDisposableType || (deferredGlobalAsyncDisposableType ***REMOVED*** getGlobalType(
      "AsyncDisposable",
      /*arity*/
      0,
      reportErrors2
    )) || emptyObjectType;
  }
  function getGlobalTypeOrUndefined(name, arity ***REMOVED*** 0) {
    const symbol ***REMOVED*** getGlobalSymbol(
      name,
      788968 /* Type */,
      /*diagnostic*/
      void 0
    );
    return symbol && getTypeOfGlobalSymbol(symbol, arity);
  }
  function getGlobalExtractSymbol() {
    deferredGlobalExtractSymbol || (deferredGlobalExtractSymbol ***REMOVED*** getGlobalTypeAliasSymbol(
      "Extract",
      /*arity*/
      2,
      /*reportErrors*/
      true
    ) || unknownSymbol);
    return deferredGlobalExtractSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : deferredGlobalExtractSymbol;
  }
  function getGlobalOmitSymbol() {
    deferredGlobalOmitSymbol || (deferredGlobalOmitSymbol ***REMOVED*** getGlobalTypeAliasSymbol(
      "Omit",
      /*arity*/
      2,
      /*reportErrors*/
      true
    ) || unknownSymbol);
    return deferredGlobalOmitSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : deferredGlobalOmitSymbol;
  }
  function getGlobalAwaitedSymbol(reportErrors2) {
    deferredGlobalAwaitedSymbol || (deferredGlobalAwaitedSymbol ***REMOVED*** getGlobalTypeAliasSymbol(
      "Awaited",
      /*arity*/
      1,
      reportErrors2
    ) || (reportErrors2 ? unknownSymbol : void 0));
    return deferredGlobalAwaitedSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : deferredGlobalAwaitedSymbol;
  }
  function getGlobalBigIntType() {
    return deferredGlobalBigIntType || (deferredGlobalBigIntType ***REMOVED*** getGlobalType(
      "BigInt",
      /*arity*/
      0,
      /*reportErrors*/
      false
    )) || emptyObjectType;
  }
  function getGlobalClassDecoratorContextType(reportErrors2) {
    return deferredGlobalClassDecoratorContextType ?? (deferredGlobalClassDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassDecoratorContext",
      /*arity*/
      1,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassMethodDecoratorContextType(reportErrors2) {
    return deferredGlobalClassMethodDecoratorContextType ?? (deferredGlobalClassMethodDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassMethodDecoratorContext",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassGetterDecoratorContextType(reportErrors2) {
    return deferredGlobalClassGetterDecoratorContextType ?? (deferredGlobalClassGetterDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassGetterDecoratorContext",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassSetterDecoratorContextType(reportErrors2) {
    return deferredGlobalClassSetterDecoratorContextType ?? (deferredGlobalClassSetterDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassSetterDecoratorContext",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassAccessorDecoratorContextType(reportErrors2) {
    return deferredGlobalClassAccessorDecoratorContextType ?? (deferredGlobalClassAccessorDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassAccessorDecoratorContext",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassAccessorDecoratorTargetType(reportErrors2) {
    return deferredGlobalClassAccessorDecoratorTargetType ?? (deferredGlobalClassAccessorDecoratorTargetType ***REMOVED*** getGlobalType(
      "ClassAccessorDecoratorTarget",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassAccessorDecoratorResultType(reportErrors2) {
    return deferredGlobalClassAccessorDecoratorResultType ?? (deferredGlobalClassAccessorDecoratorResultType ***REMOVED*** getGlobalType(
      "ClassAccessorDecoratorResult",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalClassFieldDecoratorContextType(reportErrors2) {
    return deferredGlobalClassFieldDecoratorContextType ?? (deferredGlobalClassFieldDecoratorContextType ***REMOVED*** getGlobalType(
      "ClassFieldDecoratorContext",
      /*arity*/
      2,
      reportErrors2
    )) ?? emptyGenericType;
  }
  function getGlobalNaNSymbol() {
    return deferredGlobalNaNSymbol || (deferredGlobalNaNSymbol ***REMOVED*** getGlobalValueSymbol(
      "NaN",
      /*reportErrors*/
      false
    ));
  }
  function getGlobalRecordSymbol() {
    deferredGlobalRecordSymbol || (deferredGlobalRecordSymbol ***REMOVED*** getGlobalTypeAliasSymbol(
      "Record",
      /*arity*/
      2,
      /*reportErrors*/
      true
    ) || unknownSymbol);
    return deferredGlobalRecordSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : deferredGlobalRecordSymbol;
  }
  function createTypeFromGenericGlobalType(genericGlobalType, typeArguments) {
    return genericGlobalType !***REMOVED******REMOVED*** emptyGenericType ? createTypeReference(genericGlobalType, typeArguments) : emptyObjectType;
  }
  function createTypedPropertyDescriptorType(propertyType) {
    return createTypeFromGenericGlobalType(getGlobalTypedPropertyDescriptorType(), [propertyType]);
  }
  function createIterableType(iteratedType) {
    return createTypeFromGenericGlobalType(getGlobalIterableType(
      /*reportErrors*/
      true
    ), [iteratedType, voidType, undefinedType]);
  }
  function createArrayType(elementType, readonly) {
    return createTypeFromGenericGlobalType(readonly ? globalReadonlyArrayType : globalArrayType, [elementType]);
  }
  function getTupleElementFlags(node) {
    switch (node.kind) {
      case 191 /* OptionalType */:
        return 2 /* Optional */;
      case 192 /* RestType */:
        return getRestTypeElementFlags(node);
      case 203 /* NamedTupleMember */:
        return node.questionToken ? 2 /* Optional */ : node.dotDotDotToken ? getRestTypeElementFlags(node) : 1 /* Required */;
      default:
        return 1 /* Required */;
    }
  }
  function getRestTypeElementFlags(node) {
    return getArrayElementTypeNode(node.type) ? 4 /* Rest */ : 8 /* Variadic */;
  }
  function getArrayOrTupleTargetType(node) {
    const readonly ***REMOVED*** isReadonlyTypeOperator(node.parent);
    const elementType ***REMOVED*** getArrayElementTypeNode(node);
    if (elementType) {
      return readonly ? globalReadonlyArrayType : globalArrayType;
    }
    const elementFlags ***REMOVED*** map(node.elements, getTupleElementFlags);
    return getTupleTargetType(elementFlags, readonly, map(node.elements, memberIfLabeledElementDeclaration));
  }
  function memberIfLabeledElementDeclaration(member) {
    return isNamedTupleMember(member) || isParameter(member) ? member : void 0;
  }
  function isDeferredTypeReferenceNode(node, hasDefaultTypeArguments) {
    return !!getAliasSymbolForTypeNode(node) || isResolvedByTypeAlias(node) && (node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */ ? mayResolveTypeAlias(node.elementType) : node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */ ? some(node.elements, mayResolveTypeAlias) : hasDefaultTypeArguments || some(node.typeArguments, mayResolveTypeAlias));
  }
  function isResolvedByTypeAlias(node) {
    const parent ***REMOVED*** node.parent;
    switch (parent.kind) {
      case 197 /* ParenthesizedType */:
      case 203 /* NamedTupleMember */:
      case 184 /* TypeReference */:
      case 193 /* UnionType */:
      case 194 /* IntersectionType */:
      case 200 /* IndexedAccessType */:
      case 195 /* ConditionalType */:
      case 199 /* TypeOperator */:
      case 189 /* ArrayType */:
      case 190 /* TupleType */:
        return isResolvedByTypeAlias(parent);
      case 266 /* TypeAliasDeclaration */:
        return true;
    }
    return false;
  }
  function mayResolveTypeAlias(node) {
    switch (node.kind) {
      case 184 /* TypeReference */:
        return isJSDocTypeReference(node) || !!(resolveTypeReferenceName(node, 788968 /* Type */).flags & 524288 /* TypeAlias */);
      case 187 /* TypeQuery */:
        return true;
      case 199 /* TypeOperator */:
        return node.operator !***REMOVED******REMOVED*** 158 /* UniqueKeyword */ && mayResolveTypeAlias(node.type);
      case 197 /* ParenthesizedType */:
      case 191 /* OptionalType */:
      case 203 /* NamedTupleMember */:
      case 317 /* JSDocOptionalType */:
      case 315 /* JSDocNullableType */:
      case 316 /* JSDocNonNullableType */:
      case 310 /* JSDocTypeExpression */:
        return mayResolveTypeAlias(node.type);
      case 192 /* RestType */:
        return node.type.kind !***REMOVED******REMOVED*** 189 /* ArrayType */ || mayResolveTypeAlias(node.type.elementType);
      case 193 /* UnionType */:
      case 194 /* IntersectionType */:
        return some(node.types, mayResolveTypeAlias);
      case 200 /* IndexedAccessType */:
        return mayResolveTypeAlias(node.objectType) || mayResolveTypeAlias(node.indexType);
      case 195 /* ConditionalType */:
        return mayResolveTypeAlias(node.checkType) || mayResolveTypeAlias(node.extendsType) || mayResolveTypeAlias(node.trueType) || mayResolveTypeAlias(node.falseType);
    }
    return false;
  }
  function getTypeFromArrayOrTupleTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const target ***REMOVED*** getArrayOrTupleTargetType(node);
      if (target ***REMOVED******REMOVED******REMOVED*** emptyGenericType) {
        links.resolvedType ***REMOVED*** emptyObjectType;
      } else if (!(node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */ && some(node.elements, (e) ***REMOVED***> !!(getTupleElementFlags(e) & 8 /* Variadic */))) && isDeferredTypeReferenceNode(node)) {
        links.resolvedType ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */ && node.elements.length ***REMOVED******REMOVED******REMOVED*** 0 ? target : createDeferredTypeReference(
          target,
          node,
          /*mapper*/
          void 0
        );
      } else {
        const elementTypes ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */ ? [getTypeFromTypeNode(node.elementType)] : map(node.elements, getTypeFromTypeNode);
        links.resolvedType ***REMOVED*** createNormalizedTypeReference(target, elementTypes);
      }
    }
    return links.resolvedType;
  }
  function isReadonlyTypeOperator(node) {
    return isTypeOperatorNode(node) && node.operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */;
  }
  function createTupleType(elementTypes, elementFlags, readonly ***REMOVED*** false, namedMemberDeclarations ***REMOVED*** []) {
    const tupleTarget ***REMOVED*** getTupleTargetType(elementFlags || map(elementTypes, (_) ***REMOVED***> 1 /* Required */), readonly, namedMemberDeclarations);
    return tupleTarget ***REMOVED******REMOVED******REMOVED*** emptyGenericType ? emptyObjectType : elementTypes.length ? createNormalizedTypeReference(tupleTarget, elementTypes) : tupleTarget;
  }
  function getTupleTargetType(elementFlags, readonly, namedMemberDeclarations) {
    if (elementFlags.length ***REMOVED******REMOVED******REMOVED*** 1 && elementFlags[0] & 4 /* Rest */) {
      return readonly ? globalReadonlyArrayType : globalArrayType;
    }
    const key ***REMOVED*** map(elementFlags, (f) ***REMOVED***> f & 1 /* Required */ ? "#" : f & 2 /* Optional */ ? "?" : f & 4 /* Rest */ ? "." : "*").join() + (readonly ? "R" : "") + (some(namedMemberDeclarations, (node) ***REMOVED***> !!node) ? "," + map(namedMemberDeclarations, (node) ***REMOVED***> node ? getNodeId(node) : "_").join(",") : "");
    let type ***REMOVED*** tupleTypes.get(key);
    if (!type) {
      tupleTypes.set(key, type ***REMOVED*** createTupleTargetType(elementFlags, readonly, namedMemberDeclarations));
    }
    return type;
  }
  function createTupleTargetType(elementFlags, readonly, namedMemberDeclarations) {
    const arity ***REMOVED*** elementFlags.length;
    const minLength ***REMOVED*** countWhere(elementFlags, (f) ***REMOVED***> !!(f & (1 /* Required */ | 8 /* Variadic */)));
    let typeParameters;
    const properties ***REMOVED*** [];
    let combinedFlags ***REMOVED*** 0;
    if (arity) {
      typeParameters ***REMOVED*** new Array(arity);
      for (let i ***REMOVED*** 0; i < arity; i++) {
        const typeParameter ***REMOVED*** typeParameters[i] ***REMOVED*** createTypeParameter();
        const flags ***REMOVED*** elementFlags[i];
        combinedFlags |***REMOVED*** flags;
        if (!(combinedFlags & 12 /* Variable */)) {
          const property ***REMOVED*** createSymbol(4 /* Property */ | (flags & 2 /* Optional */ ? 16777216 /* Optional */ : 0), "" + i, readonly ? 8 /* Readonly */ : 0);
          property.links.tupleLabelDeclaration ***REMOVED*** namedMemberDeclarations ***REMOVED******REMOVED*** null ? void 0 : namedMemberDeclarations[i];
          property.links.type ***REMOVED*** typeParameter;
          properties.push(property);
        }
      }
    }
    const fixedLength ***REMOVED*** properties.length;
    const lengthSymbol ***REMOVED*** createSymbol(4 /* Property */, "length", readonly ? 8 /* Readonly */ : 0);
    if (combinedFlags & 12 /* Variable */) {
      lengthSymbol.links.type ***REMOVED*** numberType;
    } else {
      const literalTypes ***REMOVED*** [];
      for (let i ***REMOVED*** minLength; i <***REMOVED*** arity; i++) literalTypes.push(getNumberLiteralType(i));
      lengthSymbol.links.type ***REMOVED*** getUnionType(literalTypes);
    }
    properties.push(lengthSymbol);
    const type ***REMOVED*** createObjectType(8 /* Tuple */ | 4 /* Reference */);
    type.typeParameters ***REMOVED*** typeParameters;
    type.outerTypeParameters ***REMOVED*** void 0;
    type.localTypeParameters ***REMOVED*** typeParameters;
    type.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
    type.instantiations.set(getTypeListId(type.typeParameters), type);
    type.target ***REMOVED*** type;
    type.resolvedTypeArguments ***REMOVED*** type.typeParameters;
    type.thisType ***REMOVED*** createTypeParameter();
    type.thisType.isThisType ***REMOVED*** true;
    type.thisType.constraint ***REMOVED*** type;
    type.declaredProperties ***REMOVED*** properties;
    type.declaredCallSignatures ***REMOVED*** emptyArray;
    type.declaredConstructSignatures ***REMOVED*** emptyArray;
    type.declaredIndexInfos ***REMOVED*** emptyArray;
    type.elementFlags ***REMOVED*** elementFlags;
    type.minLength ***REMOVED*** minLength;
    type.fixedLength ***REMOVED*** fixedLength;
    type.hasRestElement ***REMOVED*** !!(combinedFlags & 12 /* Variable */);
    type.combinedFlags ***REMOVED*** combinedFlags;
    type.readonly ***REMOVED*** readonly;
    type.labeledElementDeclarations ***REMOVED*** namedMemberDeclarations;
    return type;
  }
  function createNormalizedTypeReference(target, typeArguments) {
    return target.objectFlags & 8 /* Tuple */ ? createNormalizedTupleType(target, typeArguments) : createTypeReference(target, typeArguments);
  }
  function createNormalizedTupleType(target, elementTypes) {
    var _a, _b, _c, _d;
    if (!(target.combinedFlags & 14 /* NonRequired */)) {
      return createTypeReference(target, elementTypes);
    }
    if (target.combinedFlags & 8 /* Variadic */) {
      const unionIndex ***REMOVED*** findIndex(elementTypes, (t, i) ***REMOVED***> !!(target.elementFlags[i] & 8 /* Variadic */ && t.flags & (131072 /* Never */ | 1048576 /* Union */)));
      if (unionIndex >***REMOVED*** 0) {
        return checkCrossProductUnion(map(elementTypes, (t, i) ***REMOVED***> target.elementFlags[i] & 8 /* Variadic */ ? t : unknownType)) ? mapType(elementTypes[unionIndex], (t) ***REMOVED***> createNormalizedTupleType(target, replaceElement(elementTypes, unionIndex, t))) : errorType;
      }
    }
    const expandedTypes ***REMOVED*** [];
    const expandedFlags ***REMOVED*** [];
    const expandedDeclarations ***REMOVED*** [];
    let lastRequiredIndex ***REMOVED*** -1;
    let firstRestIndex ***REMOVED*** -1;
    let lastOptionalOrRestIndex ***REMOVED*** -1;
    for (let i ***REMOVED*** 0; i < elementTypes.length; i++) {
      const type ***REMOVED*** elementTypes[i];
      const flags ***REMOVED*** target.elementFlags[i];
      if (flags & 8 /* Variadic */) {
        if (type.flags & 1 /* Any */) {
          addElement(type, 4 /* Rest */, (_a ***REMOVED*** target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _a[i]);
        } else if (type.flags & 58982400 /* InstantiableNonPrimitive */ || isGenericMappedType(type)) {
          addElement(type, 8 /* Variadic */, (_b ***REMOVED*** target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _b[i]);
        } else if (isTupleType(type)) {
          const elements ***REMOVED*** getElementTypes(type);
          if (elements.length + expandedTypes.length >***REMOVED*** 1e4) {
            error(
              currentNode,
              isPartOfTypeNode(currentNode) ? Diagnostics.Type_produces_a_tuple_type_that_is_too_large_to_represent : Diagnostics.Expression_produces_a_tuple_type_that_is_too_large_to_represent
            );
            return errorType;
          }
          forEach(elements, (t, n) ***REMOVED***> {
            var _a2;
            return addElement(t, type.target.elementFlags[n], (_a2 ***REMOVED*** type.target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _a2[n]);
          });
        } else {
          addElement(isArrayLikeType(type) && getIndexTypeOfType(type, numberType) || errorType, 4 /* Rest */, (_c ***REMOVED*** target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _c[i]);
        }
      } else {
        addElement(type, flags, (_d ***REMOVED*** target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _d[i]);
      }
    }
    for (let i ***REMOVED*** 0; i < lastRequiredIndex; i++) {
      if (expandedFlags[i] & 2 /* Optional */) expandedFlags[i] ***REMOVED*** 1 /* Required */;
    }
    if (firstRestIndex >***REMOVED*** 0 && firstRestIndex < lastOptionalOrRestIndex) {
      expandedTypes[firstRestIndex] ***REMOVED*** getUnionType(sameMap(expandedTypes.slice(firstRestIndex, lastOptionalOrRestIndex + 1), (t, i) ***REMOVED***> expandedFlags[firstRestIndex + i] & 8 /* Variadic */ ? getIndexedAccessType(t, numberType) : t));
      expandedTypes.splice(firstRestIndex + 1, lastOptionalOrRestIndex - firstRestIndex);
      expandedFlags.splice(firstRestIndex + 1, lastOptionalOrRestIndex - firstRestIndex);
      expandedDeclarations.splice(firstRestIndex + 1, lastOptionalOrRestIndex - firstRestIndex);
    }
    const tupleTarget ***REMOVED*** getTupleTargetType(expandedFlags, target.readonly, expandedDeclarations);
    return tupleTarget ***REMOVED******REMOVED******REMOVED*** emptyGenericType ? emptyObjectType : expandedFlags.length ? createTypeReference(tupleTarget, expandedTypes) : tupleTarget;
    function addElement(type, flags, declaration) {
      if (flags & 1 /* Required */) {
        lastRequiredIndex ***REMOVED*** expandedFlags.length;
      }
      if (flags & 4 /* Rest */ && firstRestIndex < 0) {
        firstRestIndex ***REMOVED*** expandedFlags.length;
      }
      if (flags & (2 /* Optional */ | 4 /* Rest */)) {
        lastOptionalOrRestIndex ***REMOVED*** expandedFlags.length;
      }
      expandedTypes.push(flags & 2 /* Optional */ ? addOptionality(
        type,
        /*isProperty*/
        true
      ) : type);
      expandedFlags.push(flags);
      expandedDeclarations.push(declaration);
    }
  }
  function sliceTupleType(type, index, endSkipCount ***REMOVED*** 0) {
    const target ***REMOVED*** type.target;
    const endIndex ***REMOVED*** getTypeReferenceArity(type) - endSkipCount;
    return index > target.fixedLength ? getRestArrayTypeOfTupleType(type) || createTupleType(emptyArray) : createTupleType(
      getTypeArguments(type).slice(index, endIndex),
      target.elementFlags.slice(index, endIndex),
      /*readonly*/
      false,
      target.labeledElementDeclarations && target.labeledElementDeclarations.slice(index, endIndex)
    );
  }
  function getKnownKeysOfTupleType(type) {
    return getUnionType(append(arrayOf(type.target.fixedLength, (i) ***REMOVED***> getStringLiteralType("" + i)), getIndexType(type.target.readonly ? globalReadonlyArrayType : globalArrayType)));
  }
  function getStartElementCount(type, flags) {
    const index ***REMOVED*** findIndex(type.elementFlags, (f) ***REMOVED***> !(f & flags));
    return index >***REMOVED*** 0 ? index : type.elementFlags.length;
  }
  function getEndElementCount(type, flags) {
    return type.elementFlags.length - findLastIndex(type.elementFlags, (f) ***REMOVED***> !(f & flags)) - 1;
  }
  function getTotalFixedElementCount(type) {
    return type.fixedLength + getEndElementCount(type, 3 /* Fixed */);
  }
  function getElementTypes(type) {
    const typeArguments ***REMOVED*** getTypeArguments(type);
    const arity ***REMOVED*** getTypeReferenceArity(type);
    return typeArguments.length ***REMOVED******REMOVED******REMOVED*** arity ? typeArguments : typeArguments.slice(0, arity);
  }
  function getTypeFromOptionalTypeNode(node) {
    return addOptionality(
      getTypeFromTypeNode(node.type),
      /*isProperty*/
      true
    );
  }
  function getTypeId(type) {
    return type.id;
  }
  function containsType(types, type) {
    return binarySearch(types, type, getTypeId, compareValues) >***REMOVED*** 0;
  }
  function insertType(types, type) {
    const index ***REMOVED*** binarySearch(types, type, getTypeId, compareValues);
    if (index < 0) {
      types.splice(~index, 0, type);
      return true;
    }
    return false;
  }
  function addTypeToUnion(typeSet, includes, type) {
    const flags ***REMOVED*** type.flags;
    if (!(flags & 131072 /* Never */)) {
      includes |***REMOVED*** flags & 473694207 /* IncludesMask */;
      if (flags & 465829888 /* Instantiable */) includes |***REMOVED*** 33554432 /* IncludesInstantiable */;
      if (flags & 2097152 /* Intersection */ && getObjectFlags(type) & 67108864 /* IsConstrainedTypeVariable */) includes |***REMOVED*** 536870912 /* IncludesConstrainedTypeVariable */;
      if (type ***REMOVED******REMOVED******REMOVED*** wildcardType) includes |***REMOVED*** 8388608 /* IncludesWildcard */;
      if (isErrorType(type)) includes |***REMOVED*** 1073741824 /* IncludesError */;
      if (!strictNullChecks && flags & 98304 /* Nullable */) {
        if (!(getObjectFlags(type) & 65536 /* ContainsWideningType */)) includes |***REMOVED*** 4194304 /* IncludesNonWideningType */;
      } else {
        const len ***REMOVED*** typeSet.length;
        const index ***REMOVED*** len && type.id > typeSet[len - 1].id ? ~len : binarySearch(typeSet, type, getTypeId, compareValues);
        if (index < 0) {
          typeSet.splice(~index, 0, type);
        }
      }
    }
    return includes;
  }
  function addTypesToUnion(typeSet, includes, types) {
    let lastType;
    for (const type of types) {
      if (type !***REMOVED******REMOVED*** lastType) {
        includes ***REMOVED*** type.flags & 1048576 /* Union */ ? addTypesToUnion(typeSet, includes | (isNamedUnionType(type) ? 1048576 /* Union */ : 0), type.types) : addTypeToUnion(typeSet, includes, type);
        lastType ***REMOVED*** type;
      }
    }
    return includes;
  }
  function removeSubtypes(types, hasObjectTypes) {
    var _a;
    if (types.length < 2) {
      return types;
    }
    const id ***REMOVED*** getTypeListId(types);
    const match ***REMOVED*** subtypeReductionCache.get(id);
    if (match) {
      return match;
    }
    const hasEmptyObject ***REMOVED*** hasObjectTypes && some(types, (t) ***REMOVED***> !!(t.flags & 524288 /* Object */) && !isGenericMappedType(t) && isEmptyResolvedType(resolveStructuredTypeMembers(t)));
    const len ***REMOVED*** types.length;
    let i ***REMOVED*** len;
    let count ***REMOVED*** 0;
    while (i > 0) {
      i--;
      const source ***REMOVED*** types[i];
      if (hasEmptyObject || source.flags & 469499904 /* StructuredOrInstantiable */) {
        if (source.flags & 262144 /* TypeParameter */ && getBaseConstraintOrType(source).flags & 1048576 /* Union */) {
          if (isTypeRelatedTo(source, getUnionType(map(types, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** source ? neverType : t)), strictSubtypeRelation)) {
            orderedRemoveItemAt(types, i);
          }
          continue;
        }
        const keyProperty ***REMOVED*** source.flags & (524288 /* Object */ | 2097152 /* Intersection */ | 58982400 /* InstantiableNonPrimitive */) ? find(getPropertiesOfType(source), (p) ***REMOVED***> isUnitType(getTypeOfSymbol(p))) : void 0;
        const keyPropertyType ***REMOVED*** keyProperty && getRegularTypeOfLiteralType(getTypeOfSymbol(keyProperty));
        for (const target of types) {
          if (source !***REMOVED******REMOVED*** target) {
            if (count ***REMOVED******REMOVED******REMOVED*** 1e5) {
              const estimatedCount ***REMOVED*** count / (len - i) * len;
              if (estimatedCount > 1e6) {
                (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.instant(tracing.Phase.CheckTypes, "removeSubtypes_DepthLimit", { typeIds: types.map((t) ***REMOVED***> t.id) });
                error(currentNode, Diagnostics.Expression_produces_a_union_type_that_is_too_complex_to_represent);
                return void 0;
              }
            }
            count++;
            if (keyProperty && target.flags & (524288 /* Object */ | 2097152 /* Intersection */ | 58982400 /* InstantiableNonPrimitive */)) {
              const t ***REMOVED*** getTypeOfPropertyOfType(target, keyProperty.escapedName);
              if (t && isUnitType(t) && getRegularTypeOfLiteralType(t) !***REMOVED******REMOVED*** keyPropertyType) {
                continue;
              }
            }
            if (isTypeRelatedTo(source, target, strictSubtypeRelation) && (!(getObjectFlags(getTargetType(source)) & 1 /* Class */) || !(getObjectFlags(getTargetType(target)) & 1 /* Class */) || isTypeDerivedFrom(source, target))) {
              orderedRemoveItemAt(types, i);
              break;
            }
          }
        }
      }
    }
    subtypeReductionCache.set(id, types);
    return types;
  }
  function removeRedundantLiteralTypes(types, includes, reduceVoidUndefined) {
    let i ***REMOVED*** types.length;
    while (i > 0) {
      i--;
      const t ***REMOVED*** types[i];
      const flags ***REMOVED*** t.flags;
      const remove ***REMOVED*** flags & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) && includes & 4 /* String */ || flags & 256 /* NumberLiteral */ && includes & 8 /* Number */ || flags & 2048 /* BigIntLiteral */ && includes & 64 /* BigInt */ || flags & 8192 /* UniqueESSymbol */ && includes & 4096 /* ESSymbol */ || reduceVoidUndefined && flags & 32768 /* Undefined */ && includes & 16384 /* Void */ || isFreshLiteralType(t) && containsType(types, t.regularType);
      if (remove) {
        orderedRemoveItemAt(types, i);
      }
    }
  }
  function removeStringLiteralsMatchedByTemplateLiterals(types) {
    const templates ***REMOVED*** filter(types, isPatternLiteralType);
    if (templates.length) {
      let i ***REMOVED*** types.length;
      while (i > 0) {
        i--;
        const t ***REMOVED*** types[i];
        if (t.flags & 128 /* StringLiteral */ && some(templates, (template) ***REMOVED***> isTypeMatchedByTemplateLiteralOrStringMapping(t, template))) {
          orderedRemoveItemAt(types, i);
        }
      }
    }
  }
  function isTypeMatchedByTemplateLiteralOrStringMapping(type, template) {
    return template.flags & 134217728 /* TemplateLiteral */ ? isTypeMatchedByTemplateLiteralType(type, template) : isMemberOfStringMapping(type, template);
  }
  function removeConstrainedTypeVariables(types) {
    const typeVariables ***REMOVED*** [];
    for (const type of types) {
      if (type.flags & 2097152 /* Intersection */ && getObjectFlags(type) & 67108864 /* IsConstrainedTypeVariable */) {
        const index ***REMOVED*** type.types[0].flags & 8650752 /* TypeVariable */ ? 0 : 1;
        pushIfUnique(typeVariables, type.types[index]);
      }
    }
    for (const typeVariable of typeVariables) {
      const primitives ***REMOVED*** [];
      for (const type of types) {
        if (type.flags & 2097152 /* Intersection */ && getObjectFlags(type) & 67108864 /* IsConstrainedTypeVariable */) {
          const index ***REMOVED*** type.types[0].flags & 8650752 /* TypeVariable */ ? 0 : 1;
          if (type.types[index] ***REMOVED******REMOVED******REMOVED*** typeVariable) {
            insertType(primitives, type.types[1 - index]);
          }
        }
      }
      const constraint ***REMOVED*** getBaseConstraintOfType(typeVariable);
      if (everyType(constraint, (t) ***REMOVED***> containsType(primitives, t))) {
        let i ***REMOVED*** types.length;
        while (i > 0) {
          i--;
          const type ***REMOVED*** types[i];
          if (type.flags & 2097152 /* Intersection */ && getObjectFlags(type) & 67108864 /* IsConstrainedTypeVariable */) {
            const index ***REMOVED*** type.types[0].flags & 8650752 /* TypeVariable */ ? 0 : 1;
            if (type.types[index] ***REMOVED******REMOVED******REMOVED*** typeVariable && containsType(primitives, type.types[1 - index])) {
              orderedRemoveItemAt(types, i);
            }
          }
        }
        insertType(types, typeVariable);
      }
    }
  }
  function isNamedUnionType(type) {
    return !!(type.flags & 1048576 /* Union */ && (type.aliasSymbol || type.origin));
  }
  function addNamedUnions(namedUnions, types) {
    for (const t of types) {
      if (t.flags & 1048576 /* Union */) {
        const origin ***REMOVED*** t.origin;
        if (t.aliasSymbol || origin && !(origin.flags & 1048576 /* Union */)) {
          pushIfUnique(namedUnions, t);
        } else if (origin && origin.flags & 1048576 /* Union */) {
          addNamedUnions(namedUnions, origin.types);
        }
      }
    }
  }
  function createOriginUnionOrIntersectionType(flags, types) {
    const result ***REMOVED*** createOriginType(flags);
    result.types ***REMOVED*** types;
    return result;
  }
  function getUnionType(types, unionReduction ***REMOVED*** 1 /* Literal */, aliasSymbol, aliasTypeArguments, origin) {
    if (types.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return neverType;
    }
    if (types.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return types[0];
    }
    if (types.length ***REMOVED******REMOVED******REMOVED*** 2 && !origin && (types[0].flags & 1048576 /* Union */ || types[1].flags & 1048576 /* Union */)) {
      const infix ***REMOVED*** unionReduction ***REMOVED******REMOVED******REMOVED*** 0 /* None */ ? "N" : unionReduction ***REMOVED******REMOVED******REMOVED*** 2 /* Subtype */ ? "S" : "L";
      const index ***REMOVED*** types[0].id < types[1].id ? 0 : 1;
      const id ***REMOVED*** types[index].id + infix + types[1 - index].id + getAliasId(aliasSymbol, aliasTypeArguments);
      let type ***REMOVED*** unionOfUnionTypes.get(id);
      if (!type) {
        type ***REMOVED*** getUnionTypeWorker(
          types,
          unionReduction,
          aliasSymbol,
          aliasTypeArguments,
          /*origin*/
          void 0
        );
        unionOfUnionTypes.set(id, type);
      }
      return type;
    }
    return getUnionTypeWorker(types, unionReduction, aliasSymbol, aliasTypeArguments, origin);
  }
  function getUnionTypeWorker(types, unionReduction, aliasSymbol, aliasTypeArguments, origin) {
    let typeSet ***REMOVED*** [];
    const includes ***REMOVED*** addTypesToUnion(typeSet, 0, types);
    if (unionReduction !***REMOVED******REMOVED*** 0 /* None */) {
      if (includes & 3 /* AnyOrUnknown */) {
        return includes & 1 /* Any */ ? includes & 8388608 /* IncludesWildcard */ ? wildcardType : includes & 1073741824 /* IncludesError */ ? errorType : anyType : unknownType;
      }
      if (includes & 32768 /* Undefined */) {
        if (typeSet.length >***REMOVED*** 2 && typeSet[0] ***REMOVED******REMOVED******REMOVED*** undefinedType && typeSet[1] ***REMOVED******REMOVED******REMOVED*** missingType) {
          orderedRemoveItemAt(typeSet, 1);
        }
      }
      if (includes & (32 /* Enum */ | 2944 /* Literal */ | 8192 /* UniqueESSymbol */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) || includes & 16384 /* Void */ && includes & 32768 /* Undefined */) {
        removeRedundantLiteralTypes(typeSet, includes, !!(unionReduction & 2 /* Subtype */));
      }
      if (includes & 128 /* StringLiteral */ && includes & (134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */)) {
        removeStringLiteralsMatchedByTemplateLiterals(typeSet);
      }
      if (includes & 536870912 /* IncludesConstrainedTypeVariable */) {
        removeConstrainedTypeVariables(typeSet);
      }
      if (unionReduction ***REMOVED******REMOVED******REMOVED*** 2 /* Subtype */) {
        typeSet ***REMOVED*** removeSubtypes(typeSet, !!(includes & 524288 /* Object */));
        if (!typeSet) {
          return errorType;
        }
      }
      if (typeSet.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return includes & 65536 /* Null */ ? includes & 4194304 /* IncludesNonWideningType */ ? nullType : nullWideningType : includes & 32768 /* Undefined */ ? includes & 4194304 /* IncludesNonWideningType */ ? undefinedType : undefinedWideningType : neverType;
      }
    }
    if (!origin && includes & 1048576 /* Union */) {
      const namedUnions ***REMOVED*** [];
      addNamedUnions(namedUnions, types);
      const reducedTypes ***REMOVED*** [];
      for (const t of typeSet) {
        if (!some(namedUnions, (union) ***REMOVED***> containsType(union.types, t))) {
          reducedTypes.push(t);
        }
      }
      if (!aliasSymbol && namedUnions.length ***REMOVED******REMOVED******REMOVED*** 1 && reducedTypes.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return namedUnions[0];
      }
      const namedTypesCount ***REMOVED*** reduceLeft(namedUnions, (sum, union) ***REMOVED***> sum + union.types.length, 0);
      if (namedTypesCount + reducedTypes.length ***REMOVED******REMOVED******REMOVED*** typeSet.length) {
        for (const t of namedUnions) {
          insertType(reducedTypes, t);
        }
        origin ***REMOVED*** createOriginUnionOrIntersectionType(1048576 /* Union */, reducedTypes);
      }
    }
    const objectFlags ***REMOVED*** (includes & 36323331 /* NotPrimitiveUnion */ ? 0 : 32768 /* PrimitiveUnion */) | (includes & 2097152 /* Intersection */ ? 16777216 /* ContainsIntersections */ : 0);
    return getUnionTypeFromSortedList(typeSet, objectFlags, aliasSymbol, aliasTypeArguments, origin);
  }
  function getUnionOrIntersectionTypePredicate(signatures, kind) {
    let last2;
    const types ***REMOVED*** [];
    for (const sig of signatures) {
      const pred ***REMOVED*** getTypePredicateOfSignature(sig);
      if (pred) {
        if (pred.kind !***REMOVED******REMOVED*** 0 /* This */ && pred.kind !***REMOVED******REMOVED*** 1 /* Identifier */ || last2 && !typePredicateKindsMatch(last2, pred)) {
          return void 0;
        }
        last2 ***REMOVED*** pred;
        types.push(pred.type);
      } else {
        const returnType ***REMOVED*** kind !***REMOVED******REMOVED*** 2097152 /* Intersection */ ? getReturnTypeOfSignature(sig) : void 0;
        if (returnType !***REMOVED******REMOVED*** falseType && returnType !***REMOVED******REMOVED*** regularFalseType) {
          return void 0;
        }
      }
    }
    if (!last2) {
      return void 0;
    }
    const compositeType ***REMOVED*** getUnionOrIntersectionType(types, kind);
    return createTypePredicate(last2.kind, last2.parameterName, last2.parameterIndex, compositeType);
  }
  function typePredicateKindsMatch(a, b) {
    return a.kind ***REMOVED******REMOVED******REMOVED*** b.kind && a.parameterIndex ***REMOVED******REMOVED******REMOVED*** b.parameterIndex;
  }
  function getUnionTypeFromSortedList(types, precomputedObjectFlags, aliasSymbol, aliasTypeArguments, origin) {
    if (types.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return neverType;
    }
    if (types.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return types[0];
    }
    const typeKey ***REMOVED*** !origin ? getTypeListId(types) : origin.flags & 1048576 /* Union */ ? `|${getTypeListId(origin.types)}` : origin.flags & 2097152 /* Intersection */ ? `&${getTypeListId(origin.types)}` : `#${origin.type.id}|${getTypeListId(types)}`;
    const id ***REMOVED*** typeKey + getAliasId(aliasSymbol, aliasTypeArguments);
    let type ***REMOVED*** unionTypes.get(id);
    if (!type) {
      type ***REMOVED*** createType(1048576 /* Union */);
      type.objectFlags ***REMOVED*** precomputedObjectFlags | getPropagatingFlagsOfTypes(
        types,
        /*excludeKinds*/
        98304 /* Nullable */
      );
      type.types ***REMOVED*** types;
      type.origin ***REMOVED*** origin;
      type.aliasSymbol ***REMOVED*** aliasSymbol;
      type.aliasTypeArguments ***REMOVED*** aliasTypeArguments;
      if (types.length ***REMOVED******REMOVED******REMOVED*** 2 && types[0].flags & 512 /* BooleanLiteral */ && types[1].flags & 512 /* BooleanLiteral */) {
        type.flags |***REMOVED*** 16 /* Boolean */;
        type.intrinsicName ***REMOVED*** "boolean";
      }
      unionTypes.set(id, type);
    }
    return type;
  }
  function getTypeFromUnionTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      links.resolvedType ***REMOVED*** getUnionType(map(node.types, getTypeFromTypeNode), 1 /* Literal */, aliasSymbol, getTypeArgumentsForAliasSymbol(aliasSymbol));
    }
    return links.resolvedType;
  }
  function addTypeToIntersection(typeSet, includes, type) {
    const flags ***REMOVED*** type.flags;
    if (flags & 2097152 /* Intersection */) {
      return addTypesToIntersection(typeSet, includes, type.types);
    }
    if (isEmptyAnonymousObjectType(type)) {
      if (!(includes & 16777216 /* IncludesEmptyObject */)) {
        includes |***REMOVED*** 16777216 /* IncludesEmptyObject */;
        typeSet.set(type.id.toString(), type);
      }
    } else {
      if (flags & 3 /* AnyOrUnknown */) {
        if (type ***REMOVED******REMOVED******REMOVED*** wildcardType) includes |***REMOVED*** 8388608 /* IncludesWildcard */;
        if (isErrorType(type)) includes |***REMOVED*** 1073741824 /* IncludesError */;
      } else if (strictNullChecks || !(flags & 98304 /* Nullable */)) {
        if (type ***REMOVED******REMOVED******REMOVED*** missingType) {
          includes |***REMOVED*** 262144 /* IncludesMissingType */;
          type ***REMOVED*** undefinedType;
        }
        if (!typeSet.has(type.id.toString())) {
          if (type.flags & 109472 /* Unit */ && includes & 109472 /* Unit */) {
            includes |***REMOVED*** 67108864 /* NonPrimitive */;
          }
          typeSet.set(type.id.toString(), type);
        }
      }
      includes |***REMOVED*** flags & 473694207 /* IncludesMask */;
    }
    return includes;
  }
  function addTypesToIntersection(typeSet, includes, types) {
    for (const type of types) {
      includes ***REMOVED*** addTypeToIntersection(typeSet, includes, getRegularTypeOfLiteralType(type));
    }
    return includes;
  }
  function removeRedundantSupertypes(types, includes) {
    let i ***REMOVED*** types.length;
    while (i > 0) {
      i--;
      const t ***REMOVED*** types[i];
      const remove ***REMOVED*** t.flags & 4 /* String */ && includes & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) || t.flags & 8 /* Number */ && includes & 256 /* NumberLiteral */ || t.flags & 64 /* BigInt */ && includes & 2048 /* BigIntLiteral */ || t.flags & 4096 /* ESSymbol */ && includes & 8192 /* UniqueESSymbol */ || t.flags & 16384 /* Void */ && includes & 32768 /* Undefined */ || isEmptyAnonymousObjectType(t) && includes & 470302716 /* DefinitelyNonNullable */;
      if (remove) {
        orderedRemoveItemAt(types, i);
      }
    }
  }
  function eachUnionContains(unionTypes2, type) {
    for (const u of unionTypes2) {
      if (!containsType(u.types, type)) {
        if (type ***REMOVED******REMOVED******REMOVED*** missingType) {
          return containsType(u.types, undefinedType);
        }
        if (type ***REMOVED******REMOVED******REMOVED*** undefinedType) {
          return containsType(u.types, missingType);
        }
        const primitive ***REMOVED*** type.flags & 128 /* StringLiteral */ ? stringType : type.flags & (32 /* Enum */ | 256 /* NumberLiteral */) ? numberType : type.flags & 2048 /* BigIntLiteral */ ? bigintType : type.flags & 8192 /* UniqueESSymbol */ ? esSymbolType : void 0;
        if (!primitive || !containsType(u.types, primitive)) {
          return false;
        }
      }
    }
    return true;
  }
  function extractRedundantTemplateLiterals(types) {
    let i ***REMOVED*** types.length;
    const literals ***REMOVED*** filter(types, (t) ***REMOVED***> !!(t.flags & 128 /* StringLiteral */));
    while (i > 0) {
      i--;
      const t ***REMOVED*** types[i];
      if (!(t.flags & (134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */))) continue;
      for (const t2 of literals) {
        if (isTypeSubtypeOf(t2, t)) {
          orderedRemoveItemAt(types, i);
          break;
        } else if (isPatternLiteralType(t)) {
          return true;
        }
      }
    }
    return false;
  }
  function removeFromEach(types, flag) {
    for (let i ***REMOVED*** 0; i < types.length; i++) {
      types[i] ***REMOVED*** filterType(types[i], (t) ***REMOVED***> !(t.flags & flag));
    }
  }
  function intersectUnionsOfPrimitiveTypes(types) {
    let unionTypes2;
    const index ***REMOVED*** findIndex(types, (t) ***REMOVED***> !!(getObjectFlags(t) & 32768 /* PrimitiveUnion */));
    if (index < 0) {
      return false;
    }
    let i ***REMOVED*** index + 1;
    while (i < types.length) {
      const t ***REMOVED*** types[i];
      if (getObjectFlags(t) & 32768 /* PrimitiveUnion */) {
        (unionTypes2 || (unionTypes2 ***REMOVED*** [types[index]])).push(t);
        orderedRemoveItemAt(types, i);
      } else {
        i++;
      }
    }
    if (!unionTypes2) {
      return false;
    }
    const checked ***REMOVED*** [];
    const result ***REMOVED*** [];
    for (const u of unionTypes2) {
      for (const t of u.types) {
        if (insertType(checked, t)) {
          if (eachUnionContains(unionTypes2, t)) {
            if (t ***REMOVED******REMOVED******REMOVED*** undefinedType && result.length && result[0] ***REMOVED******REMOVED******REMOVED*** missingType) {
              continue;
            }
            if (t ***REMOVED******REMOVED******REMOVED*** missingType && result.length && result[0] ***REMOVED******REMOVED******REMOVED*** undefinedType) {
              result[0] ***REMOVED*** missingType;
              continue;
            }
            insertType(result, t);
          }
        }
      }
    }
    types[index] ***REMOVED*** getUnionTypeFromSortedList(result, 32768 /* PrimitiveUnion */);
    return true;
  }
  function createIntersectionType(types, objectFlags, aliasSymbol, aliasTypeArguments) {
    const result ***REMOVED*** createType(2097152 /* Intersection */);
    result.objectFlags ***REMOVED*** objectFlags | getPropagatingFlagsOfTypes(
      types,
      /*excludeKinds*/
      98304 /* Nullable */
    );
    result.types ***REMOVED*** types;
    result.aliasSymbol ***REMOVED*** aliasSymbol;
    result.aliasTypeArguments ***REMOVED*** aliasTypeArguments;
    return result;
  }
  function getIntersectionType(types, flags ***REMOVED*** 0 /* None */, aliasSymbol, aliasTypeArguments) {
    const typeMembershipMap ***REMOVED*** /* @__PURE__ */ new Map();
    const includes ***REMOVED*** addTypesToIntersection(typeMembershipMap, 0, types);
    const typeSet ***REMOVED*** arrayFrom(typeMembershipMap.values());
    let objectFlags ***REMOVED*** 0 /* None */;
    if (includes & 131072 /* Never */) {
      return contains(typeSet, silentNeverType) ? silentNeverType : neverType;
    }
    if (strictNullChecks && includes & 98304 /* Nullable */ && includes & (524288 /* Object */ | 67108864 /* NonPrimitive */ | 16777216 /* IncludesEmptyObject */) || includes & 67108864 /* NonPrimitive */ && includes & (469892092 /* DisjointDomains */ & ~67108864 /* NonPrimitive */) || includes & 402653316 /* StringLike */ && includes & (469892092 /* DisjointDomains */ & ~402653316 /* StringLike */) || includes & 296 /* NumberLike */ && includes & (469892092 /* DisjointDomains */ & ~296 /* NumberLike */) || includes & 2112 /* BigIntLike */ && includes & (469892092 /* DisjointDomains */ & ~2112 /* BigIntLike */) || includes & 12288 /* ESSymbolLike */ && includes & (469892092 /* DisjointDomains */ & ~12288 /* ESSymbolLike */) || includes & 49152 /* VoidLike */ && includes & (469892092 /* DisjointDomains */ & ~49152 /* VoidLike */)) {
      return neverType;
    }
    if (includes & (134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) && includes & 128 /* StringLiteral */ && extractRedundantTemplateLiterals(typeSet)) {
      return neverType;
    }
    if (includes & 1 /* Any */) {
      return includes & 8388608 /* IncludesWildcard */ ? wildcardType : includes & 1073741824 /* IncludesError */ ? errorType : anyType;
    }
    if (!strictNullChecks && includes & 98304 /* Nullable */) {
      return includes & 16777216 /* IncludesEmptyObject */ ? neverType : includes & 32768 /* Undefined */ ? undefinedType : nullType;
    }
    if (includes & 4 /* String */ && includes & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) || includes & 8 /* Number */ && includes & 256 /* NumberLiteral */ || includes & 64 /* BigInt */ && includes & 2048 /* BigIntLiteral */ || includes & 4096 /* ESSymbol */ && includes & 8192 /* UniqueESSymbol */ || includes & 16384 /* Void */ && includes & 32768 /* Undefined */ || includes & 16777216 /* IncludesEmptyObject */ && includes & 470302716 /* DefinitelyNonNullable */) {
      if (!(flags & 1 /* NoSupertypeReduction */)) removeRedundantSupertypes(typeSet, includes);
    }
    if (includes & 262144 /* IncludesMissingType */) {
      typeSet[typeSet.indexOf(undefinedType)] ***REMOVED*** missingType;
    }
    if (typeSet.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return unknownType;
    }
    if (typeSet.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return typeSet[0];
    }
    if (typeSet.length ***REMOVED******REMOVED******REMOVED*** 2 && !(flags & 2 /* NoConstraintReduction */)) {
      const typeVarIndex ***REMOVED*** typeSet[0].flags & 8650752 /* TypeVariable */ ? 0 : 1;
      const typeVariable ***REMOVED*** typeSet[typeVarIndex];
      const primitiveType ***REMOVED*** typeSet[1 - typeVarIndex];
      if (typeVariable.flags & 8650752 /* TypeVariable */ && (primitiveType.flags & (402784252 /* Primitive */ | 67108864 /* NonPrimitive */) && !isGenericStringLikeType(primitiveType) || includes & 16777216 /* IncludesEmptyObject */)) {
        const constraint ***REMOVED*** getBaseConstraintOfType(typeVariable);
        if (constraint && everyType(constraint, (t) ***REMOVED***> !!(t.flags & (402784252 /* Primitive */ | 67108864 /* NonPrimitive */)) || isEmptyAnonymousObjectType(t))) {
          if (isTypeStrictSubtypeOf(constraint, primitiveType)) {
            return typeVariable;
          }
          if (!(constraint.flags & 1048576 /* Union */ && someType(constraint, (c) ***REMOVED***> isTypeStrictSubtypeOf(c, primitiveType)))) {
            if (!isTypeStrictSubtypeOf(primitiveType, constraint)) {
              return neverType;
            }
          }
          objectFlags ***REMOVED*** 67108864 /* IsConstrainedTypeVariable */;
        }
      }
    }
    const id ***REMOVED*** getTypeListId(typeSet) + (flags & 2 /* NoConstraintReduction */ ? "*" : getAliasId(aliasSymbol, aliasTypeArguments));
    let result ***REMOVED*** intersectionTypes.get(id);
    if (!result) {
      if (includes & 1048576 /* Union */) {
        if (intersectUnionsOfPrimitiveTypes(typeSet)) {
          result ***REMOVED*** getIntersectionType(typeSet, flags, aliasSymbol, aliasTypeArguments);
        } else if (every(typeSet, (t) ***REMOVED***> !!(t.flags & 1048576 /* Union */ && t.types[0].flags & 32768 /* Undefined */))) {
          const containedUndefinedType ***REMOVED*** some(typeSet, containsMissingType) ? missingType : undefinedType;
          removeFromEach(typeSet, 32768 /* Undefined */);
          result ***REMOVED*** getUnionType([getIntersectionType(typeSet, flags), containedUndefinedType], 1 /* Literal */, aliasSymbol, aliasTypeArguments);
        } else if (every(typeSet, (t) ***REMOVED***> !!(t.flags & 1048576 /* Union */ && (t.types[0].flags & 65536 /* Null */ || t.types[1].flags & 65536 /* Null */)))) {
          removeFromEach(typeSet, 65536 /* Null */);
          result ***REMOVED*** getUnionType([getIntersectionType(typeSet, flags), nullType], 1 /* Literal */, aliasSymbol, aliasTypeArguments);
        } else if (typeSet.length >***REMOVED*** 3 && types.length > 2) {
          const middle ***REMOVED*** Math.floor(typeSet.length / 2);
          result ***REMOVED*** getIntersectionType([getIntersectionType(typeSet.slice(0, middle), flags), getIntersectionType(typeSet.slice(middle), flags)], flags, aliasSymbol, aliasTypeArguments);
        } else {
          if (!checkCrossProductUnion(typeSet)) {
            return errorType;
          }
          const constituents ***REMOVED*** getCrossProductIntersections(typeSet, flags);
          const origin ***REMOVED*** some(constituents, (t) ***REMOVED***> !!(t.flags & 2097152 /* Intersection */)) && getConstituentCountOfTypes(constituents) > getConstituentCountOfTypes(typeSet) ? createOriginUnionOrIntersectionType(2097152 /* Intersection */, typeSet) : void 0;
          result ***REMOVED*** getUnionType(constituents, 1 /* Literal */, aliasSymbol, aliasTypeArguments, origin);
        }
      } else {
        result ***REMOVED*** createIntersectionType(typeSet, objectFlags, aliasSymbol, aliasTypeArguments);
      }
      intersectionTypes.set(id, result);
    }
    return result;
  }
  function getCrossProductUnionSize(types) {
    return reduceLeft(types, (n, t) ***REMOVED***> t.flags & 1048576 /* Union */ ? n * t.types.length : t.flags & 131072 /* Never */ ? 0 : n, 1);
  }
  function checkCrossProductUnion(types) {
    var _a;
    const size ***REMOVED*** getCrossProductUnionSize(types);
    if (size >***REMOVED*** 1e5) {
      (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.instant(tracing.Phase.CheckTypes, "checkCrossProductUnion_DepthLimit", { typeIds: types.map((t) ***REMOVED***> t.id), size });
      error(currentNode, Diagnostics.Expression_produces_a_union_type_that_is_too_complex_to_represent);
      return false;
    }
    return true;
  }
  function getCrossProductIntersections(types, flags) {
    const count ***REMOVED*** getCrossProductUnionSize(types);
    const intersections ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < count; i++) {
      const constituents ***REMOVED*** types.slice();
      let n ***REMOVED*** i;
      for (let j ***REMOVED*** types.length - 1; j >***REMOVED*** 0; j--) {
        if (types[j].flags & 1048576 /* Union */) {
          const sourceTypes ***REMOVED*** types[j].types;
          const length2 ***REMOVED*** sourceTypes.length;
          constituents[j] ***REMOVED*** sourceTypes[n % length2];
          n ***REMOVED*** Math.floor(n / length2);
        }
      }
      const t ***REMOVED*** getIntersectionType(constituents, flags);
      if (!(t.flags & 131072 /* Never */)) intersections.push(t);
    }
    return intersections;
  }
  function getConstituentCount(type) {
    return !(type.flags & 3145728 /* UnionOrIntersection */) || type.aliasSymbol ? 1 : type.flags & 1048576 /* Union */ && type.origin ? getConstituentCount(type.origin) : getConstituentCountOfTypes(type.types);
  }
  function getConstituentCountOfTypes(types) {
    return reduceLeft(types, (n, t) ***REMOVED***> n + getConstituentCount(t), 0);
  }
  function getTypeFromIntersectionTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      const types ***REMOVED*** map(node.types, getTypeFromTypeNode);
      const emptyIndex ***REMOVED*** types.length ***REMOVED******REMOVED******REMOVED*** 2 ? types.indexOf(emptyTypeLiteralType) : -1;
      const t ***REMOVED*** emptyIndex >***REMOVED*** 0 ? types[1 - emptyIndex] : unknownType;
      const noSupertypeReduction ***REMOVED*** !!(t.flags & (4 /* String */ | 8 /* Number */ | 64 /* BigInt */) || t.flags & 134217728 /* TemplateLiteral */ && isPatternLiteralType(t));
      links.resolvedType ***REMOVED*** getIntersectionType(types, noSupertypeReduction ? 1 /* NoSupertypeReduction */ : 0, aliasSymbol, getTypeArgumentsForAliasSymbol(aliasSymbol));
    }
    return links.resolvedType;
  }
  function createIndexType(type, indexFlags) {
    const result ***REMOVED*** createType(4194304 /* Index */);
    result.type ***REMOVED*** type;
    result.indexFlags ***REMOVED*** indexFlags;
    return result;
  }
  function createOriginIndexType(type) {
    const result ***REMOVED*** createOriginType(4194304 /* Index */);
    result.type ***REMOVED*** type;
    return result;
  }
  function getIndexTypeForGenericType(type, indexFlags) {
    return indexFlags & 1 /* StringsOnly */ ? type.resolvedStringIndexType || (type.resolvedStringIndexType ***REMOVED*** createIndexType(type, 1 /* StringsOnly */)) : type.resolvedIndexType || (type.resolvedIndexType ***REMOVED*** createIndexType(type, 0 /* None */));
  }
  function getIndexTypeForMappedType(type, indexFlags) {
    const typeParameter ***REMOVED*** getTypeParameterFromMappedType(type);
    const constraintType ***REMOVED*** getConstraintTypeFromMappedType(type);
    const nameType ***REMOVED*** getNameTypeFromMappedType(type.target || type);
    if (!nameType && !(indexFlags & 2 /* NoIndexSignatures */)) {
      return constraintType;
    }
    const keyTypes ***REMOVED*** [];
    if (isGenericIndexType(constraintType)) {
      if (isMappedTypeWithKeyofConstraintDeclaration(type)) {
        return getIndexTypeForGenericType(type, indexFlags);
      }
      forEachType(constraintType, addMemberForKeyType);
    } else if (isMappedTypeWithKeyofConstraintDeclaration(type)) {
      const modifiersType ***REMOVED*** getApparentType(getModifiersTypeFromMappedType(type));
      forEachMappedTypePropertyKeyTypeAndIndexSignatureKeyType(modifiersType, 8576 /* StringOrNumberLiteralOrUnique */, !!(indexFlags & 1 /* StringsOnly */), addMemberForKeyType);
    } else {
      forEachType(getLowerBoundOfKeyType(constraintType), addMemberForKeyType);
    }
    const result ***REMOVED*** indexFlags & 2 /* NoIndexSignatures */ ? filterType(getUnionType(keyTypes), (t) ***REMOVED***> !(t.flags & (1 /* Any */ | 4 /* String */))) : getUnionType(keyTypes);
    if (result.flags & 1048576 /* Union */ && constraintType.flags & 1048576 /* Union */ && getTypeListId(result.types) ***REMOVED******REMOVED******REMOVED*** getTypeListId(constraintType.types)) {
      return constraintType;
    }
    return result;
    function addMemberForKeyType(keyType) {
      const propNameType ***REMOVED*** nameType ? instantiateType(nameType, appendTypeMapping(type.mapper, typeParameter, keyType)) : keyType;
      keyTypes.push(propNameType ***REMOVED******REMOVED******REMOVED*** stringType ? stringOrNumberType : propNameType);
    }
  }
  function hasDistributiveNameType(mappedType) {
    const typeVariable ***REMOVED*** getTypeParameterFromMappedType(mappedType);
    return isDistributive(getNameTypeFromMappedType(mappedType) || typeVariable);
    function isDistributive(type) {
      return type.flags & (3 /* AnyOrUnknown */ | 402784252 /* Primitive */ | 131072 /* Never */ | 262144 /* TypeParameter */ | 524288 /* Object */ | 67108864 /* NonPrimitive */) ? true : type.flags & 16777216 /* Conditional */ ? type.root.isDistributive && type.checkType ***REMOVED******REMOVED******REMOVED*** typeVariable : type.flags & (3145728 /* UnionOrIntersection */ | 134217728 /* TemplateLiteral */) ? every(type.types, isDistributive) : type.flags & 8388608 /* IndexedAccess */ ? isDistributive(type.objectType) && isDistributive(type.indexType) : type.flags & 33554432 /* Substitution */ ? isDistributive(type.baseType) && isDistributive(type.constraint) : type.flags & 268435456 /* StringMapping */ ? isDistributive(type.type) : false;
    }
  }
  function getLiteralTypeFromPropertyName(name) {
    if (isPrivateIdentifier(name)) {
      return neverType;
    }
    if (isNumericLiteral(name)) {
      return getRegularTypeOfLiteralType(checkExpression(name));
    }
    if (isComputedPropertyName(name)) {
      return getRegularTypeOfLiteralType(checkComputedPropertyName(name));
    }
    const propertyName ***REMOVED*** getPropertyNameForPropertyNameNode(name);
    if (propertyName !***REMOVED******REMOVED*** void 0) {
      return getStringLiteralType(unescapeLeadingUnderscores(propertyName));
    }
    if (isExpression(name)) {
      return getRegularTypeOfLiteralType(checkExpression(name));
    }
    return neverType;
  }
  function getLiteralTypeFromProperty(prop, include, includeNonPublic) {
    if (includeNonPublic || !(getDeclarationModifierFlagsFromSymbol(prop) & 6 /* NonPublicAccessibilityModifier */)) {
      let type ***REMOVED*** getSymbolLinks(getLateBoundSymbol(prop)).nameType;
      if (!type) {
        const name ***REMOVED*** getNameOfDeclaration(prop.valueDeclaration);
        type ***REMOVED*** prop.escapedName ***REMOVED******REMOVED******REMOVED*** "default" /* Default */ ? getStringLiteralType("default") : name && getLiteralTypeFromPropertyName(name) || (!isKnownSymbol(prop) ? getStringLiteralType(symbolName(prop)) : void 0);
      }
      if (type && type.flags & include) {
        return type;
      }
    }
    return neverType;
  }
  function isKeyTypeIncluded(keyType, include) {
    return !!(keyType.flags & include || keyType.flags & 2097152 /* Intersection */ && some(keyType.types, (t) ***REMOVED***> isKeyTypeIncluded(t, include)));
  }
  function getLiteralTypeFromProperties(type, include, includeOrigin) {
    const origin ***REMOVED*** includeOrigin && (getObjectFlags(type) & (3 /* ClassOrInterface */ | 4 /* Reference */) || type.aliasSymbol) ? createOriginIndexType(type) : void 0;
    const propertyTypes ***REMOVED*** map(getPropertiesOfType(type), (prop) ***REMOVED***> getLiteralTypeFromProperty(prop, include));
    const indexKeyTypes ***REMOVED*** map(getIndexInfosOfType(type), (info) ***REMOVED***> info !***REMOVED******REMOVED*** enumNumberIndexInfo && isKeyTypeIncluded(info.keyType, include) ? info.keyType ***REMOVED******REMOVED******REMOVED*** stringType && include & 8 /* Number */ ? stringOrNumberType : info.keyType : neverType);
    return getUnionType(
      concatenate(propertyTypes, indexKeyTypes),
      1 /* Literal */,
      /*aliasSymbol*/
      void 0,
      /*aliasTypeArguments*/
      void 0,
      origin
    );
  }
  function shouldDeferIndexType(type, indexFlags ***REMOVED*** 0 /* None */) {
    return !!(type.flags & 58982400 /* InstantiableNonPrimitive */ || isGenericTupleType(type) || isGenericMappedType(type) && (!hasDistributiveNameType(type) || getMappedTypeNameTypeKind(type) ***REMOVED******REMOVED******REMOVED*** 2 /* Remapping */) || type.flags & 1048576 /* Union */ && !(indexFlags & 4 /* NoReducibleCheck */) && isGenericReducibleType(type) || type.flags & 2097152 /* Intersection */ && maybeTypeOfKind(type, 465829888 /* Instantiable */) && some(type.types, isEmptyAnonymousObjectType));
  }
  function getIndexType(type, indexFlags ***REMOVED*** 0 /* None */) {
    type ***REMOVED*** getReducedType(type);
    return isNoInferType(type) ? getNoInferType(getIndexType(type.baseType, indexFlags)) : shouldDeferIndexType(type, indexFlags) ? getIndexTypeForGenericType(type, indexFlags) : type.flags & 1048576 /* Union */ ? getIntersectionType(map(type.types, (t) ***REMOVED***> getIndexType(t, indexFlags))) : type.flags & 2097152 /* Intersection */ ? getUnionType(map(type.types, (t) ***REMOVED***> getIndexType(t, indexFlags))) : getObjectFlags(type) & 32 /* Mapped */ ? getIndexTypeForMappedType(type, indexFlags) : type ***REMOVED******REMOVED******REMOVED*** wildcardType ? wildcardType : type.flags & 2 /* Unknown */ ? neverType : type.flags & (1 /* Any */ | 131072 /* Never */) ? stringNumberSymbolType : getLiteralTypeFromProperties(type, (indexFlags & 2 /* NoIndexSignatures */ ? 128 /* StringLiteral */ : 402653316 /* StringLike */) | (indexFlags & 1 /* StringsOnly */ ? 0 : 296 /* NumberLike */ | 12288 /* ESSymbolLike */), indexFlags ***REMOVED******REMOVED******REMOVED*** 0 /* None */);
  }
  function getExtractStringType(type) {
    const extractTypeAlias ***REMOVED*** getGlobalExtractSymbol();
    return extractTypeAlias ? getTypeAliasInstantiation(extractTypeAlias, [type, stringType]) : stringType;
  }
  function getIndexTypeOrString(type) {
    const indexType ***REMOVED*** getExtractStringType(getIndexType(type));
    return indexType.flags & 131072 /* Never */ ? stringType : indexType;
  }
  function getTypeFromTypeOperatorNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      switch (node.operator) {
        case 143 /* KeyOfKeyword */:
          links.resolvedType ***REMOVED*** getIndexType(getTypeFromTypeNode(node.type));
          break;
        case 158 /* UniqueKeyword */:
          links.resolvedType ***REMOVED*** node.type.kind ***REMOVED******REMOVED******REMOVED*** 155 /* SymbolKeyword */ ? getESSymbolLikeTypeForNode(walkUpParenthesizedTypes(node.parent)) : errorType;
          break;
        case 148 /* ReadonlyKeyword */:
          links.resolvedType ***REMOVED*** getTypeFromTypeNode(node.type);
          break;
        default:
          Debug.assertNever(node.operator);
      }
    }
    return links.resolvedType;
  }
  function getTypeFromTemplateTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      links.resolvedType ***REMOVED*** getTemplateLiteralType(
        [node.head.text, ...map(node.templateSpans, (span) ***REMOVED***> span.literal.text)],
        map(node.templateSpans, (span) ***REMOVED***> getTypeFromTypeNode(span.type))
      );
    }
    return links.resolvedType;
  }
  function getTemplateLiteralType(texts, types) {
    const unionIndex ***REMOVED*** findIndex(types, (t) ***REMOVED***> !!(t.flags & (131072 /* Never */ | 1048576 /* Union */)));
    if (unionIndex >***REMOVED*** 0) {
      return checkCrossProductUnion(types) ? mapType(types[unionIndex], (t) ***REMOVED***> getTemplateLiteralType(texts, replaceElement(types, unionIndex, t))) : errorType;
    }
    if (contains(types, wildcardType)) {
      return wildcardType;
    }
    const newTypes ***REMOVED*** [];
    const newTexts ***REMOVED*** [];
    let text ***REMOVED*** texts[0];
    if (!addSpans(texts, types)) {
      return stringType;
    }
    if (newTypes.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return getStringLiteralType(text);
    }
    newTexts.push(text);
    if (every(newTexts, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** "")) {
      if (every(newTypes, (t) ***REMOVED***> !!(t.flags & 4 /* String */))) {
        return stringType;
      }
      if (newTypes.length ***REMOVED******REMOVED******REMOVED*** 1 && isPatternLiteralType(newTypes[0])) {
        return newTypes[0];
      }
    }
    const id ***REMOVED*** `${getTypeListId(newTypes)}|${map(newTexts, (t) ***REMOVED***> t.length).join(",")}|${newTexts.join("")}`;
    let type ***REMOVED*** templateLiteralTypes.get(id);
    if (!type) {
      templateLiteralTypes.set(id, type ***REMOVED*** createTemplateLiteralType(newTexts, newTypes));
    }
    return type;
    function addSpans(texts2, types2) {
      for (let i ***REMOVED*** 0; i < types2.length; i++) {
        const t ***REMOVED*** types2[i];
        if (t.flags & (2944 /* Literal */ | 65536 /* Null */ | 32768 /* Undefined */)) {
          text +***REMOVED*** getTemplateStringForType(t) || "";
          text +***REMOVED*** texts2[i + 1];
        } else if (t.flags & 134217728 /* TemplateLiteral */) {
          text +***REMOVED*** t.texts[0];
          if (!addSpans(t.texts, t.types)) return false;
          text +***REMOVED*** texts2[i + 1];
        } else if (isGenericIndexType(t) || isPatternLiteralPlaceholderType(t)) {
          newTypes.push(t);
          newTexts.push(text);
          text ***REMOVED*** texts2[i + 1];
        } else {
          return false;
        }
      }
      return true;
    }
  }
  function getTemplateStringForType(type) {
    return type.flags & 128 /* StringLiteral */ ? type.value : type.flags & 256 /* NumberLiteral */ ? "" + type.value : type.flags & 2048 /* BigIntLiteral */ ? pseudoBigIntToString(type.value) : type.flags & (512 /* BooleanLiteral */ | 98304 /* Nullable */) ? type.intrinsicName : void 0;
  }
  function createTemplateLiteralType(texts, types) {
    const type ***REMOVED*** createType(134217728 /* TemplateLiteral */);
    type.texts ***REMOVED*** texts;
    type.types ***REMOVED*** types;
    return type;
  }
  function getStringMappingType(symbol, type) {
    return type.flags & (1048576 /* Union */ | 131072 /* Never */) ? mapType(type, (t) ***REMOVED***> getStringMappingType(symbol, t)) : type.flags & 128 /* StringLiteral */ ? getStringLiteralType(applyStringMapping(symbol, type.value)) : type.flags & 134217728 /* TemplateLiteral */ ? getTemplateLiteralType(...applyTemplateStringMapping(symbol, type.texts, type.types)) : (
      // Mapping<Mapping<T>> ***REMOVED******REMOVED******REMOVED*** Mapping<T>
      type.flags & 268435456 /* StringMapping */ && symbol ***REMOVED******REMOVED******REMOVED*** type.symbol ? type : type.flags & (1 /* Any */ | 4 /* String */ | 268435456 /* StringMapping */) || isGenericIndexType(type) ? getStringMappingTypeForGenericType(symbol, type) : (
        // This handles Mapping<`${number}`> and Mapping<`${bigint}`>
        isPatternLiteralPlaceholderType(type) ? getStringMappingTypeForGenericType(symbol, getTemplateLiteralType(["", ""], [type])) : type
      )
    );
  }
  function applyStringMapping(symbol, str) {
    switch (intrinsicTypeKinds.get(symbol.escapedName)) {
      case 0 /* Uppercase */:
        return str.toUpperCase();
      case 1 /* Lowercase */:
        return str.toLowerCase();
      case 2 /* Capitalize */:
        return str.charAt(0).toUpperCase() + str.slice(1);
      case 3 /* Uncapitalize */:
        return str.charAt(0).toLowerCase() + str.slice(1);
    }
    return str;
  }
  function applyTemplateStringMapping(symbol, texts, types) {
    switch (intrinsicTypeKinds.get(symbol.escapedName)) {
      case 0 /* Uppercase */:
        return [texts.map((t) ***REMOVED***> t.toUpperCase()), types.map((t) ***REMOVED***> getStringMappingType(symbol, t))];
      case 1 /* Lowercase */:
        return [texts.map((t) ***REMOVED***> t.toLowerCase()), types.map((t) ***REMOVED***> getStringMappingType(symbol, t))];
      case 2 /* Capitalize */:
        return [texts[0] ***REMOVED******REMOVED******REMOVED*** "" ? texts : [texts[0].charAt(0).toUpperCase() + texts[0].slice(1), ...texts.slice(1)], texts[0] ***REMOVED******REMOVED******REMOVED*** "" ? [getStringMappingType(symbol, types[0]), ...types.slice(1)] : types];
      case 3 /* Uncapitalize */:
        return [texts[0] ***REMOVED******REMOVED******REMOVED*** "" ? texts : [texts[0].charAt(0).toLowerCase() + texts[0].slice(1), ...texts.slice(1)], texts[0] ***REMOVED******REMOVED******REMOVED*** "" ? [getStringMappingType(symbol, types[0]), ...types.slice(1)] : types];
    }
    return [texts, types];
  }
  function getStringMappingTypeForGenericType(symbol, type) {
    const id ***REMOVED*** `${getSymbolId(symbol)},${getTypeId(type)}`;
    let result ***REMOVED*** stringMappingTypes.get(id);
    if (!result) {
      stringMappingTypes.set(id, result ***REMOVED*** createStringMappingType(symbol, type));
    }
    return result;
  }
  function createStringMappingType(symbol, type) {
    const result ***REMOVED*** createTypeWithSymbol(268435456 /* StringMapping */, symbol);
    result.type ***REMOVED*** type;
    return result;
  }
  function createIndexedAccessType(objectType, indexType, accessFlags, aliasSymbol, aliasTypeArguments) {
    const type ***REMOVED*** createType(8388608 /* IndexedAccess */);
    type.objectType ***REMOVED*** objectType;
    type.indexType ***REMOVED*** indexType;
    type.accessFlags ***REMOVED*** accessFlags;
    type.aliasSymbol ***REMOVED*** aliasSymbol;
    type.aliasTypeArguments ***REMOVED*** aliasTypeArguments;
    return type;
  }
  function isJSLiteralType(type) {
    if (noImplicitAny) {
      return false;
    }
    if (getObjectFlags(type) & 4096 /* JSLiteral */) {
      return true;
    }
    if (type.flags & 1048576 /* Union */) {
      return every(type.types, isJSLiteralType);
    }
    if (type.flags & 2097152 /* Intersection */) {
      return some(type.types, isJSLiteralType);
    }
    if (type.flags & 465829888 /* Instantiable */) {
      const constraint ***REMOVED*** getResolvedBaseConstraint(type);
      return constraint !***REMOVED******REMOVED*** type && isJSLiteralType(constraint);
    }
    return false;
  }
  function getPropertyNameFromIndex(indexType, accessNode) {
    return isTypeUsableAsPropertyName(indexType) ? getPropertyNameFromType(indexType) : accessNode && isPropertyName(accessNode) ? (
      // late bound names are handled in the first branch, so here we only need to handle normal names
      getPropertyNameForPropertyNameNode(accessNode)
    ) : void 0;
  }
  function isUncalledFunctionReference(node, symbol) {
    if (symbol.flags & (16 /* Function */ | 8192 /* Method */)) {
      const parent ***REMOVED*** findAncestor(node.parent, (n) ***REMOVED***> !isAccessExpression(n)) || node.parent;
      if (isCallLikeExpression(parent)) {
        return isCallOrNewExpression(parent) && isIdentifier(node) && hasMatchingArgument(parent, node);
      }
      return every(symbol.declarations, (d) ***REMOVED***> !isFunctionLike(d) || isDeprecatedDeclaration(d));
    }
    return true;
  }
  function getPropertyTypeForIndexType(originalObjectType, objectType, indexType, fullIndexType, accessNode, accessFlags) {
    const accessExpression ***REMOVED*** accessNode && accessNode.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ ? accessNode : void 0;
    const propName ***REMOVED*** accessNode && isPrivateIdentifier(accessNode) ? void 0 : getPropertyNameFromIndex(indexType, accessNode);
    if (propName !***REMOVED******REMOVED*** void 0) {
      if (accessFlags & 256 /* Contextual */) {
        return getTypeOfPropertyOfContextualType(objectType, propName) || anyType;
      }
      const prop ***REMOVED*** getPropertyOfType(objectType, propName);
      if (prop) {
        if (accessFlags & 64 /* ReportDeprecated */ && accessNode && prop.declarations && isDeprecatedSymbol(prop) && isUncalledFunctionReference(accessNode, prop)) {
          const deprecatedNode ***REMOVED*** (accessExpression ***REMOVED******REMOVED*** null ? void 0 : accessExpression.argumentExpression) ?? (isIndexedAccessTypeNode(accessNode) ? accessNode.indexType : accessNode);
          addDeprecatedSuggestion(deprecatedNode, prop.declarations, propName);
        }
        if (accessExpression) {
          markPropertyAsReferenced(prop, accessExpression, isSelfTypeAccess(accessExpression.expression, objectType.symbol));
          if (isAssignmentToReadonlyEntity(accessExpression, prop, getAssignmentTargetKind(accessExpression))) {
            error(accessExpression.argumentExpression, Diagnostics.Cannot_assign_to_0_because_it_is_a_read_only_property, symbolToString(prop));
            return void 0;
          }
          if (accessFlags & 8 /* CacheSymbol */) {
            getNodeLinks(accessNode).resolvedSymbol ***REMOVED*** prop;
          }
          if (isThisPropertyAccessInConstructor(accessExpression, prop)) {
            return autoType;
          }
        }
        const propType ***REMOVED*** accessFlags & 4 /* Writing */ ? getWriteTypeOfSymbol(prop) : getTypeOfSymbol(prop);
        return accessExpression && getAssignmentTargetKind(accessExpression) !***REMOVED******REMOVED*** 1 /* Definite */ ? getFlowTypeOfReference(accessExpression, propType) : accessNode && isIndexedAccessTypeNode(accessNode) && containsMissingType(propType) ? getUnionType([propType, undefinedType]) : propType;
      }
      if (everyType(objectType, isTupleType) && isNumericLiteralName(propName)) {
        const index ***REMOVED*** +propName;
        if (accessNode && everyType(objectType, (t) ***REMOVED***> !(t.target.combinedFlags & 12 /* Variable */)) && !(accessFlags & 16 /* AllowMissing */)) {
          const indexNode ***REMOVED*** getIndexNodeForAccessExpression(accessNode);
          if (isTupleType(objectType)) {
            if (index < 0) {
              error(indexNode, Diagnostics.A_tuple_type_cannot_be_indexed_with_a_negative_value);
              return undefinedType;
            }
            error(indexNode, Diagnostics.Tuple_type_0_of_length_1_has_no_element_at_index_2, typeToString(objectType), getTypeReferenceArity(objectType), unescapeLeadingUnderscores(propName));
          } else {
            error(indexNode, Diagnostics.Property_0_does_not_exist_on_type_1, unescapeLeadingUnderscores(propName), typeToString(objectType));
          }
        }
        if (index >***REMOVED*** 0) {
          errorIfWritingToReadonlyIndex(getIndexInfoOfType(objectType, numberType));
          return getTupleElementTypeOutOfStartCount(objectType, index, accessFlags & 1 /* IncludeUndefined */ ? missingType : void 0);
        }
      }
    }
    if (!(indexType.flags & 98304 /* Nullable */) && isTypeAssignableToKind(indexType, 402653316 /* StringLike */ | 296 /* NumberLike */ | 12288 /* ESSymbolLike */)) {
      if (objectType.flags & (1 /* Any */ | 131072 /* Never */)) {
        return objectType;
      }
      const indexInfo ***REMOVED*** getApplicableIndexInfo(objectType, indexType) || getIndexInfoOfType(objectType, stringType);
      if (indexInfo) {
        if (accessFlags & 2 /* NoIndexSignatures */ && indexInfo.keyType !***REMOVED******REMOVED*** numberType) {
          if (accessExpression) {
            if (accessFlags & 4 /* Writing */) {
              error(accessExpression, Diagnostics.Type_0_is_generic_and_can_only_be_indexed_for_reading, typeToString(originalObjectType));
            } else {
              error(accessExpression, Diagnostics.Type_0_cannot_be_used_to_index_type_1, typeToString(indexType), typeToString(originalObjectType));
            }
          }
          return void 0;
        }
        if (accessNode && indexInfo.keyType ***REMOVED******REMOVED******REMOVED*** stringType && !isTypeAssignableToKind(indexType, 4 /* String */ | 8 /* Number */)) {
          const indexNode ***REMOVED*** getIndexNodeForAccessExpression(accessNode);
          error(indexNode, Diagnostics.Type_0_cannot_be_used_as_an_index_type, typeToString(indexType));
          return accessFlags & 1 /* IncludeUndefined */ ? getUnionType([indexInfo.type, missingType]) : indexInfo.type;
        }
        errorIfWritingToReadonlyIndex(indexInfo);
        if (accessFlags & 1 /* IncludeUndefined */ && !(objectType.symbol && objectType.symbol.flags & (256 /* RegularEnum */ | 128 /* ConstEnum */) && (indexType.symbol && indexType.flags & 1024 /* EnumLiteral */ && getParentOfSymbol(indexType.symbol) ***REMOVED******REMOVED******REMOVED*** objectType.symbol))) {
          return getUnionType([indexInfo.type, missingType]);
        }
        return indexInfo.type;
      }
      if (indexType.flags & 131072 /* Never */) {
        return neverType;
      }
      if (isJSLiteralType(objectType)) {
        return anyType;
      }
      if (accessExpression && !isConstEnumObjectType(objectType)) {
        if (isObjectLiteralType(objectType)) {
          if (noImplicitAny && indexType.flags & (128 /* StringLiteral */ | 256 /* NumberLiteral */)) {
            diagnostics.add(createDiagnosticForNode(accessExpression, Diagnostics.Property_0_does_not_exist_on_type_1, indexType.value, typeToString(objectType)));
            return undefinedType;
          } else if (indexType.flags & (8 /* Number */ | 4 /* String */)) {
            const types ***REMOVED*** map(objectType.properties, (property) ***REMOVED***> {
              return getTypeOfSymbol(property);
            });
            return getUnionType(append(types, undefinedType));
          }
        }
        if (objectType.symbol ***REMOVED******REMOVED******REMOVED*** globalThisSymbol && propName !***REMOVED******REMOVED*** void 0 && globalThisSymbol.exports.has(propName) && globalThisSymbol.exports.get(propName).flags & 418 /* BlockScoped */) {
          error(accessExpression, Diagnostics.Property_0_does_not_exist_on_type_1, unescapeLeadingUnderscores(propName), typeToString(objectType));
        } else if (noImplicitAny && !(accessFlags & 128 /* SuppressNoImplicitAnyError */)) {
          if (propName !***REMOVED******REMOVED*** void 0 && typeHasStaticProperty(propName, objectType)) {
            const typeName ***REMOVED*** typeToString(objectType);
            error(accessExpression, Diagnostics.Property_0_does_not_exist_on_type_1_Did_you_mean_to_access_the_static_member_2_instead, propName, typeName, typeName + "[" + getTextOfNode(accessExpression.argumentExpression) + "]");
          } else if (getIndexTypeOfType(objectType, numberType)) {
            error(accessExpression.argumentExpression, Diagnostics.Element_implicitly_has_an_any_type_because_index_expression_is_not_of_type_number);
          } else {
            let suggestion;
            if (propName !***REMOVED******REMOVED*** void 0 && (suggestion ***REMOVED*** getSuggestionForNonexistentProperty(propName, objectType))) {
              if (suggestion !***REMOVED******REMOVED*** void 0) {
                error(accessExpression.argumentExpression, Diagnostics.Property_0_does_not_exist_on_type_1_Did_you_mean_2, propName, typeToString(objectType), suggestion);
              }
            } else {
              const suggestion2 ***REMOVED*** getSuggestionForNonexistentIndexSignature(objectType, accessExpression, indexType);
              if (suggestion2 !***REMOVED******REMOVED*** void 0) {
                error(accessExpression, Diagnostics.Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature_Did_you_mean_to_call_1, typeToString(objectType), suggestion2);
              } else {
                let errorInfo;
                if (indexType.flags & 1024 /* EnumLiteral */) {
                  errorInfo ***REMOVED*** chainDiagnosticMessages(
                    /*details*/
                    void 0,
                    Diagnostics.Property_0_does_not_exist_on_type_1,
                    "[" + typeToString(indexType) + "]",
                    typeToString(objectType)
                  );
                } else if (indexType.flags & 8192 /* UniqueESSymbol */) {
                  const symbolName2 ***REMOVED*** getFullyQualifiedName(indexType.symbol, accessExpression);
                  errorInfo ***REMOVED*** chainDiagnosticMessages(
                    /*details*/
                    void 0,
                    Diagnostics.Property_0_does_not_exist_on_type_1,
                    "[" + symbolName2 + "]",
                    typeToString(objectType)
                  );
                } else if (indexType.flags & 128 /* StringLiteral */) {
                  errorInfo ***REMOVED*** chainDiagnosticMessages(
                    /*details*/
                    void 0,
                    Diagnostics.Property_0_does_not_exist_on_type_1,
                    indexType.value,
                    typeToString(objectType)
                  );
                } else if (indexType.flags & 256 /* NumberLiteral */) {
                  errorInfo ***REMOVED*** chainDiagnosticMessages(
                    /*details*/
                    void 0,
                    Diagnostics.Property_0_does_not_exist_on_type_1,
                    indexType.value,
                    typeToString(objectType)
                  );
                } else if (indexType.flags & (8 /* Number */ | 4 /* String */)) {
                  errorInfo ***REMOVED*** chainDiagnosticMessages(
                    /*details*/
                    void 0,
                    Diagnostics.No_index_signature_with_a_parameter_of_type_0_was_found_on_type_1,
                    typeToString(indexType),
                    typeToString(objectType)
                  );
                }
                errorInfo ***REMOVED*** chainDiagnosticMessages(
                  errorInfo,
                  Diagnostics.Element_implicitly_has_an_any_type_because_expression_of_type_0_can_t_be_used_to_index_type_1,
                  typeToString(fullIndexType),
                  typeToString(objectType)
                );
                diagnostics.add(createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(accessExpression), accessExpression, errorInfo));
              }
            }
          }
        }
        return void 0;
      }
    }
    if (accessFlags & 16 /* AllowMissing */ && isObjectLiteralType(objectType)) {
      return undefinedType;
    }
    if (isJSLiteralType(objectType)) {
      return anyType;
    }
    if (accessNode) {
      const indexNode ***REMOVED*** getIndexNodeForAccessExpression(accessNode);
      if (indexNode.kind !***REMOVED******REMOVED*** 10 /* BigIntLiteral */ && indexType.flags & (128 /* StringLiteral */ | 256 /* NumberLiteral */)) {
        error(indexNode, Diagnostics.Property_0_does_not_exist_on_type_1, "" + indexType.value, typeToString(objectType));
      } else if (indexType.flags & (4 /* String */ | 8 /* Number */)) {
        error(indexNode, Diagnostics.Type_0_has_no_matching_index_signature_for_type_1, typeToString(objectType), typeToString(indexType));
      } else {
        const typeString ***REMOVED*** indexNode.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ ? "bigint" : typeToString(indexType);
        error(indexNode, Diagnostics.Type_0_cannot_be_used_as_an_index_type, typeString);
      }
    }
    if (isTypeAny(indexType)) {
      return indexType;
    }
    return void 0;
    function errorIfWritingToReadonlyIndex(indexInfo) {
      if (indexInfo && indexInfo.isReadonly && accessExpression && (isAssignmentTarget(accessExpression) || isDeleteTarget(accessExpression))) {
        error(accessExpression, Diagnostics.Index_signature_in_type_0_only_permits_reading, typeToString(objectType));
      }
    }
  }
  function getIndexNodeForAccessExpression(accessNode) {
    return accessNode.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ ? accessNode.argumentExpression : accessNode.kind ***REMOVED******REMOVED******REMOVED*** 200 /* IndexedAccessType */ ? accessNode.indexType : accessNode.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ ? accessNode.expression : accessNode;
  }
  function isPatternLiteralPlaceholderType(type) {
    if (type.flags & 2097152 /* Intersection */) {
      let seenPlaceholder ***REMOVED*** false;
      for (const t of type.types) {
        if (t.flags & (2944 /* Literal */ | 98304 /* Nullable */) || isPatternLiteralPlaceholderType(t)) {
          seenPlaceholder ***REMOVED*** true;
        } else if (!(t.flags & 524288 /* Object */)) {
          return false;
        }
      }
      return seenPlaceholder;
    }
    return !!(type.flags & (1 /* Any */ | 4 /* String */ | 8 /* Number */ | 64 /* BigInt */)) || isPatternLiteralType(type);
  }
  function isPatternLiteralType(type) {
    return !!(type.flags & 134217728 /* TemplateLiteral */) && every(type.types, isPatternLiteralPlaceholderType) || !!(type.flags & 268435456 /* StringMapping */) && isPatternLiteralPlaceholderType(type.type);
  }
  function isGenericStringLikeType(type) {
    return !!(type.flags & (134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */)) && !isPatternLiteralType(type);
  }
  function isGenericType(type) {
    return !!getGenericObjectFlags(type);
  }
  function isGenericObjectType(type) {
    return !!(getGenericObjectFlags(type) & 4194304 /* IsGenericObjectType */);
  }
  function isGenericIndexType(type) {
    return !!(getGenericObjectFlags(type) & 8388608 /* IsGenericIndexType */);
  }
  function getGenericObjectFlags(type) {
    if (type.flags & 3145728 /* UnionOrIntersection */) {
      if (!(type.objectFlags & 2097152 /* IsGenericTypeComputed */)) {
        type.objectFlags |***REMOVED*** 2097152 /* IsGenericTypeComputed */ | reduceLeft(type.types, (flags, t) ***REMOVED***> flags | getGenericObjectFlags(t), 0);
      }
      return type.objectFlags & 12582912 /* IsGenericType */;
    }
    if (type.flags & 33554432 /* Substitution */) {
      if (!(type.objectFlags & 2097152 /* IsGenericTypeComputed */)) {
        type.objectFlags |***REMOVED*** 2097152 /* IsGenericTypeComputed */ | getGenericObjectFlags(type.baseType) | getGenericObjectFlags(type.constraint);
      }
      return type.objectFlags & 12582912 /* IsGenericType */;
    }
    return (type.flags & 58982400 /* InstantiableNonPrimitive */ || isGenericMappedType(type) || isGenericTupleType(type) ? 4194304 /* IsGenericObjectType */ : 0) | (type.flags & (58982400 /* InstantiableNonPrimitive */ | 4194304 /* Index */) || isGenericStringLikeType(type) ? 8388608 /* IsGenericIndexType */ : 0);
  }
  function getSimplifiedType(type, writing) {
    return type.flags & 8388608 /* IndexedAccess */ ? getSimplifiedIndexedAccessType(type, writing) : type.flags & 16777216 /* Conditional */ ? getSimplifiedConditionalType(type, writing) : type;
  }
  function distributeIndexOverObjectType(objectType, indexType, writing) {
    if (objectType.flags & 1048576 /* Union */ || objectType.flags & 2097152 /* Intersection */ && !shouldDeferIndexType(objectType)) {
      const types ***REMOVED*** map(objectType.types, (t) ***REMOVED***> getSimplifiedType(getIndexedAccessType(t, indexType), writing));
      return objectType.flags & 2097152 /* Intersection */ || writing ? getIntersectionType(types) : getUnionType(types);
    }
  }
  function distributeObjectOverIndexType(objectType, indexType, writing) {
    if (indexType.flags & 1048576 /* Union */) {
      const types ***REMOVED*** map(indexType.types, (t) ***REMOVED***> getSimplifiedType(getIndexedAccessType(objectType, t), writing));
      return writing ? getIntersectionType(types) : getUnionType(types);
    }
  }
  function getSimplifiedIndexedAccessType(type, writing) {
    const cache ***REMOVED*** writing ? "simplifiedForWriting" : "simplifiedForReading";
    if (type[cache]) {
      return type[cache] ***REMOVED******REMOVED******REMOVED*** circularConstraintType ? type : type[cache];
    }
    type[cache] ***REMOVED*** circularConstraintType;
    const objectType ***REMOVED*** getSimplifiedType(type.objectType, writing);
    const indexType ***REMOVED*** getSimplifiedType(type.indexType, writing);
    const distributedOverIndex ***REMOVED*** distributeObjectOverIndexType(objectType, indexType, writing);
    if (distributedOverIndex) {
      return type[cache] ***REMOVED*** distributedOverIndex;
    }
    if (!(indexType.flags & 465829888 /* Instantiable */)) {
      const distributedOverObject ***REMOVED*** distributeIndexOverObjectType(objectType, indexType, writing);
      if (distributedOverObject) {
        return type[cache] ***REMOVED*** distributedOverObject;
      }
    }
    if (isGenericTupleType(objectType) && indexType.flags & 296 /* NumberLike */) {
      const elementType ***REMOVED*** getElementTypeOfSliceOfTupleType(
        objectType,
        indexType.flags & 8 /* Number */ ? 0 : objectType.target.fixedLength,
        /*endSkipCount*/
        0,
        writing
      );
      if (elementType) {
        return type[cache] ***REMOVED*** elementType;
      }
    }
    if (isGenericMappedType(objectType)) {
      if (getMappedTypeNameTypeKind(objectType) !***REMOVED******REMOVED*** 2 /* Remapping */) {
        return type[cache] ***REMOVED*** mapType(substituteIndexedMappedType(objectType, type.indexType), (t) ***REMOVED***> getSimplifiedType(t, writing));
      }
    }
    return type[cache] ***REMOVED*** type;
  }
  function getSimplifiedConditionalType(type, writing) {
    const checkType ***REMOVED*** type.checkType;
    const extendsType ***REMOVED*** type.extendsType;
    const trueType2 ***REMOVED*** getTrueTypeFromConditionalType(type);
    const falseType2 ***REMOVED*** getFalseTypeFromConditionalType(type);
    if (falseType2.flags & 131072 /* Never */ && getActualTypeVariable(trueType2) ***REMOVED******REMOVED******REMOVED*** getActualTypeVariable(checkType)) {
      if (checkType.flags & 1 /* Any */ || isTypeAssignableTo(getRestrictiveInstantiation(checkType), getRestrictiveInstantiation(extendsType))) {
        return getSimplifiedType(trueType2, writing);
      } else if (isIntersectionEmpty(checkType, extendsType)) {
        return neverType;
      }
    } else if (trueType2.flags & 131072 /* Never */ && getActualTypeVariable(falseType2) ***REMOVED******REMOVED******REMOVED*** getActualTypeVariable(checkType)) {
      if (!(checkType.flags & 1 /* Any */) && isTypeAssignableTo(getRestrictiveInstantiation(checkType), getRestrictiveInstantiation(extendsType))) {
        return neverType;
      } else if (checkType.flags & 1 /* Any */ || isIntersectionEmpty(checkType, extendsType)) {
        return getSimplifiedType(falseType2, writing);
      }
    }
    return type;
  }
  function isIntersectionEmpty(type1, type2) {
    return !!(getUnionType([intersectTypes(type1, type2), neverType]).flags & 131072 /* Never */);
  }
  function substituteIndexedMappedType(objectType, index) {
    const mapper ***REMOVED*** createTypeMapper([getTypeParameterFromMappedType(objectType)], [index]);
    const templateMapper ***REMOVED*** combineTypeMappers(objectType.mapper, mapper);
    const instantiatedTemplateType ***REMOVED*** instantiateType(getTemplateTypeFromMappedType(objectType.target || objectType), templateMapper);
    const isOptional ***REMOVED*** getMappedTypeOptionality(objectType) > 0 || (isGenericType(objectType) ? getCombinedMappedTypeOptionality(getModifiersTypeFromMappedType(objectType)) > 0 : couldAccessOptionalProperty(objectType, index));
    return addOptionality(
      instantiatedTemplateType,
      /*isProperty*/
      true,
      isOptional
    );
  }
  function couldAccessOptionalProperty(objectType, indexType) {
    const indexConstraint ***REMOVED*** getBaseConstraintOfType(indexType);
    return !!indexConstraint && some(getPropertiesOfType(objectType), (p) ***REMOVED***> !!(p.flags & 16777216 /* Optional */) && isTypeAssignableTo(getLiteralTypeFromProperty(p, 8576 /* StringOrNumberLiteralOrUnique */), indexConstraint));
  }
  function getIndexedAccessType(objectType, indexType, accessFlags ***REMOVED*** 0 /* None */, accessNode, aliasSymbol, aliasTypeArguments) {
    return getIndexedAccessTypeOrUndefined(objectType, indexType, accessFlags, accessNode, aliasSymbol, aliasTypeArguments) || (accessNode ? errorType : unknownType);
  }
  function indexTypeLessThan(indexType, limit) {
    return everyType(indexType, (t) ***REMOVED***> {
      if (t.flags & 384 /* StringOrNumberLiteral */) {
        const propName ***REMOVED*** getPropertyNameFromType(t);
        if (isNumericLiteralName(propName)) {
          const index ***REMOVED*** +propName;
          return index >***REMOVED*** 0 && index < limit;
        }
      }
      return false;
    });
  }
  function getIndexedAccessTypeOrUndefined(objectType, indexType, accessFlags ***REMOVED*** 0 /* None */, accessNode, aliasSymbol, aliasTypeArguments) {
    if (objectType ***REMOVED******REMOVED******REMOVED*** wildcardType || indexType ***REMOVED******REMOVED******REMOVED*** wildcardType) {
      return wildcardType;
    }
    objectType ***REMOVED*** getReducedType(objectType);
    if (isStringIndexSignatureOnlyType(objectType) && !(indexType.flags & 98304 /* Nullable */) && isTypeAssignableToKind(indexType, 4 /* String */ | 8 /* Number */)) {
      indexType ***REMOVED*** stringType;
    }
    if (compilerOptions.noUncheckedIndexedAccess && accessFlags & 32 /* ExpressionPosition */) accessFlags |***REMOVED*** 1 /* IncludeUndefined */;
    if (isGenericIndexType(indexType) || (accessNode && accessNode.kind !***REMOVED******REMOVED*** 200 /* IndexedAccessType */ ? isGenericTupleType(objectType) && !indexTypeLessThan(indexType, getTotalFixedElementCount(objectType.target)) : isGenericObjectType(objectType) && !(isTupleType(objectType) && indexTypeLessThan(indexType, getTotalFixedElementCount(objectType.target))) || isGenericReducibleType(objectType))) {
      if (objectType.flags & 3 /* AnyOrUnknown */) {
        return objectType;
      }
      const persistentAccessFlags ***REMOVED*** accessFlags & 1 /* Persistent */;
      const id ***REMOVED*** objectType.id + "," + indexType.id + "," + persistentAccessFlags + getAliasId(aliasSymbol, aliasTypeArguments);
      let type ***REMOVED*** indexedAccessTypes.get(id);
      if (!type) {
        indexedAccessTypes.set(id, type ***REMOVED*** createIndexedAccessType(objectType, indexType, persistentAccessFlags, aliasSymbol, aliasTypeArguments));
      }
      return type;
    }
    const apparentObjectType ***REMOVED*** getReducedApparentType(objectType);
    if (indexType.flags & 1048576 /* Union */ && !(indexType.flags & 16 /* Boolean */)) {
      const propTypes ***REMOVED*** [];
      let wasMissingProp ***REMOVED*** false;
      for (const t of indexType.types) {
        const propType ***REMOVED*** getPropertyTypeForIndexType(objectType, apparentObjectType, t, indexType, accessNode, accessFlags | (wasMissingProp ? 128 /* SuppressNoImplicitAnyError */ : 0));
        if (propType) {
          propTypes.push(propType);
        } else if (!accessNode) {
          return void 0;
        } else {
          wasMissingProp ***REMOVED*** true;
        }
      }
      if (wasMissingProp) {
        return void 0;
      }
      return accessFlags & 4 /* Writing */ ? getIntersectionType(propTypes, 0 /* None */, aliasSymbol, aliasTypeArguments) : getUnionType(propTypes, 1 /* Literal */, aliasSymbol, aliasTypeArguments);
    }
    return getPropertyTypeForIndexType(objectType, apparentObjectType, indexType, indexType, accessNode, accessFlags | 8 /* CacheSymbol */ | 64 /* ReportDeprecated */);
  }
  function getTypeFromIndexedAccessTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const objectType ***REMOVED*** getTypeFromTypeNode(node.objectType);
      const indexType ***REMOVED*** getTypeFromTypeNode(node.indexType);
      const potentialAlias ***REMOVED*** getAliasSymbolForTypeNode(node);
      links.resolvedType ***REMOVED*** getIndexedAccessType(objectType, indexType, 0 /* None */, node, potentialAlias, getTypeArgumentsForAliasSymbol(potentialAlias));
    }
    return links.resolvedType;
  }
  function getTypeFromMappedTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const type ***REMOVED*** createObjectType(32 /* Mapped */, node.symbol);
      type.declaration ***REMOVED*** node;
      type.aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      type.aliasTypeArguments ***REMOVED*** getTypeArgumentsForAliasSymbol(type.aliasSymbol);
      links.resolvedType ***REMOVED*** type;
      getConstraintTypeFromMappedType(type);
    }
    return links.resolvedType;
  }
  function getActualTypeVariable(type) {
    if (type.flags & 33554432 /* Substitution */) {
      return getActualTypeVariable(type.baseType);
    }
    if (type.flags & 8388608 /* IndexedAccess */ && (type.objectType.flags & 33554432 /* Substitution */ || type.indexType.flags & 33554432 /* Substitution */)) {
      return getIndexedAccessType(getActualTypeVariable(type.objectType), getActualTypeVariable(type.indexType));
    }
    return type;
  }
  function isSimpleTupleType(node) {
    return isTupleTypeNode(node) && length(node.elements) > 0 && !some(node.elements, (e) ***REMOVED***> isOptionalTypeNode(e) || isRestTypeNode(e) || isNamedTupleMember(e) && !!(e.questionToken || e.dotDotDotToken));
  }
  function isDeferredType(type, checkTuples) {
    return isGenericType(type) || checkTuples && isTupleType(type) && some(getElementTypes(type), isGenericType);
  }
  function getConditionalType(root, mapper, forConstraint, aliasSymbol, aliasTypeArguments) {
    let result;
    let extraTypes;
    let tailCount ***REMOVED*** 0;
    while (true) {
      if (tailCount ***REMOVED******REMOVED******REMOVED*** 1e3) {
        error(currentNode, Diagnostics.Type_instantiation_is_excessively_deep_and_possibly_infinite);
        return errorType;
      }
      const checkType ***REMOVED*** instantiateType(getActualTypeVariable(root.checkType), mapper);
      const extendsType ***REMOVED*** instantiateType(root.extendsType, mapper);
      if (checkType ***REMOVED******REMOVED******REMOVED*** errorType || extendsType ***REMOVED******REMOVED******REMOVED*** errorType) {
        return errorType;
      }
      if (checkType ***REMOVED******REMOVED******REMOVED*** wildcardType || extendsType ***REMOVED******REMOVED******REMOVED*** wildcardType) {
        return wildcardType;
      }
      const checkTypeNode ***REMOVED*** skipTypeParentheses(root.node.checkType);
      const extendsTypeNode ***REMOVED*** skipTypeParentheses(root.node.extendsType);
      const checkTuples ***REMOVED*** isSimpleTupleType(checkTypeNode) && isSimpleTupleType(extendsTypeNode) && length(checkTypeNode.elements) ***REMOVED******REMOVED******REMOVED*** length(extendsTypeNode.elements);
      const checkTypeDeferred ***REMOVED*** isDeferredType(checkType, checkTuples);
      let combinedMapper;
      if (root.inferTypeParameters) {
        const context ***REMOVED*** createInferenceContext(
          root.inferTypeParameters,
          /*signature*/
          void 0,
          0 /* None */
        );
        if (mapper) {
          context.nonFixingMapper ***REMOVED*** combineTypeMappers(context.nonFixingMapper, mapper);
        }
        if (!checkTypeDeferred) {
          inferTypes(context.inferences, checkType, extendsType, 512 /* NoConstraints */ | 1024 /* AlwaysStrict */);
        }
        combinedMapper ***REMOVED*** mapper ? combineTypeMappers(context.mapper, mapper) : context.mapper;
      }
      const inferredExtendsType ***REMOVED*** combinedMapper ? instantiateType(root.extendsType, combinedMapper) : extendsType;
      if (!checkTypeDeferred && !isDeferredType(inferredExtendsType, checkTuples)) {
        if (!(inferredExtendsType.flags & 3 /* AnyOrUnknown */) && (checkType.flags & 1 /* Any */ || !isTypeAssignableTo(getPermissiveInstantiation(checkType), getPermissiveInstantiation(inferredExtendsType)))) {
          if (checkType.flags & 1 /* Any */ || forConstraint && !(inferredExtendsType.flags & 131072 /* Never */) && someType(getPermissiveInstantiation(inferredExtendsType), (t) ***REMOVED***> isTypeAssignableTo(t, getPermissiveInstantiation(checkType)))) {
            (extraTypes || (extraTypes ***REMOVED*** [])).push(instantiateType(getTypeFromTypeNode(root.node.trueType), combinedMapper || mapper));
          }
          const falseType2 ***REMOVED*** getTypeFromTypeNode(root.node.falseType);
          if (falseType2.flags & 16777216 /* Conditional */) {
            const newRoot ***REMOVED*** falseType2.root;
            if (newRoot.node.parent ***REMOVED******REMOVED******REMOVED*** root.node && (!newRoot.isDistributive || newRoot.checkType ***REMOVED******REMOVED******REMOVED*** root.checkType)) {
              root ***REMOVED*** newRoot;
              continue;
            }
            if (canTailRecurse(falseType2, mapper)) {
              continue;
            }
          }
          result ***REMOVED*** instantiateType(falseType2, mapper);
          break;
        }
        if (inferredExtendsType.flags & 3 /* AnyOrUnknown */ || isTypeAssignableTo(getRestrictiveInstantiation(checkType), getRestrictiveInstantiation(inferredExtendsType))) {
          const trueType2 ***REMOVED*** getTypeFromTypeNode(root.node.trueType);
          const trueMapper ***REMOVED*** combinedMapper || mapper;
          if (canTailRecurse(trueType2, trueMapper)) {
            continue;
          }
          result ***REMOVED*** instantiateType(trueType2, trueMapper);
          break;
        }
      }
      result ***REMOVED*** createType(16777216 /* Conditional */);
      result.root ***REMOVED*** root;
      result.checkType ***REMOVED*** instantiateType(root.checkType, mapper);
      result.extendsType ***REMOVED*** instantiateType(root.extendsType, mapper);
      result.mapper ***REMOVED*** mapper;
      result.combinedMapper ***REMOVED*** combinedMapper;
      result.aliasSymbol ***REMOVED*** aliasSymbol || root.aliasSymbol;
      result.aliasTypeArguments ***REMOVED*** aliasSymbol ? aliasTypeArguments : instantiateTypes(root.aliasTypeArguments, mapper);
      break;
    }
    return extraTypes ? getUnionType(append(extraTypes, result)) : result;
    function canTailRecurse(newType, newMapper) {
      if (newType.flags & 16777216 /* Conditional */ && newMapper) {
        const newRoot ***REMOVED*** newType.root;
        if (newRoot.outerTypeParameters) {
          const typeParamMapper ***REMOVED*** combineTypeMappers(newType.mapper, newMapper);
          const typeArguments ***REMOVED*** map(newRoot.outerTypeParameters, (t) ***REMOVED***> getMappedType(t, typeParamMapper));
          const newRootMapper ***REMOVED*** createTypeMapper(newRoot.outerTypeParameters, typeArguments);
          const newCheckType ***REMOVED*** newRoot.isDistributive ? getMappedType(newRoot.checkType, newRootMapper) : void 0;
          if (!newCheckType || newCheckType ***REMOVED******REMOVED******REMOVED*** newRoot.checkType || !(newCheckType.flags & (1048576 /* Union */ | 131072 /* Never */))) {
            root ***REMOVED*** newRoot;
            mapper ***REMOVED*** newRootMapper;
            aliasSymbol ***REMOVED*** void 0;
            aliasTypeArguments ***REMOVED*** void 0;
            if (newRoot.aliasSymbol) {
              tailCount++;
            }
            return true;
          }
        }
      }
      return false;
    }
  }
  function getTrueTypeFromConditionalType(type) {
    return type.resolvedTrueType || (type.resolvedTrueType ***REMOVED*** instantiateType(getTypeFromTypeNode(type.root.node.trueType), type.mapper));
  }
  function getFalseTypeFromConditionalType(type) {
    return type.resolvedFalseType || (type.resolvedFalseType ***REMOVED*** instantiateType(getTypeFromTypeNode(type.root.node.falseType), type.mapper));
  }
  function getInferredTrueTypeFromConditionalType(type) {
    return type.resolvedInferredTrueType || (type.resolvedInferredTrueType ***REMOVED*** type.combinedMapper ? instantiateType(getTypeFromTypeNode(type.root.node.trueType), type.combinedMapper) : getTrueTypeFromConditionalType(type));
  }
  function getInferTypeParameters(node) {
    let result;
    if (node.locals) {
      node.locals.forEach((symbol) ***REMOVED***> {
        if (symbol.flags & 262144 /* TypeParameter */) {
          result ***REMOVED*** append(result, getDeclaredTypeOfSymbol(symbol));
        }
      });
    }
    return result;
  }
  function isDistributionDependent(root) {
    return root.isDistributive && (isTypeParameterPossiblyReferenced(root.checkType, root.node.trueType) || isTypeParameterPossiblyReferenced(root.checkType, root.node.falseType));
  }
  function getTypeFromConditionalTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const checkType ***REMOVED*** getTypeFromTypeNode(node.checkType);
      const aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      const aliasTypeArguments ***REMOVED*** getTypeArgumentsForAliasSymbol(aliasSymbol);
      const allOuterTypeParameters ***REMOVED*** getOuterTypeParameters(
        node,
        /*includeThisTypes*/
        true
      );
      const outerTypeParameters ***REMOVED*** aliasTypeArguments ? allOuterTypeParameters : filter(allOuterTypeParameters, (tp) ***REMOVED***> isTypeParameterPossiblyReferenced(tp, node));
      const root ***REMOVED*** {
        node,
        checkType,
        extendsType: getTypeFromTypeNode(node.extendsType),
        isDistributive: !!(checkType.flags & 262144 /* TypeParameter */),
        inferTypeParameters: getInferTypeParameters(node),
        outerTypeParameters,
        instantiations: void 0,
        aliasSymbol,
        aliasTypeArguments
      };
      links.resolvedType ***REMOVED*** getConditionalType(
        root,
        /*mapper*/
        void 0,
        /*forConstraint*/
        false
      );
      if (outerTypeParameters) {
        root.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
        root.instantiations.set(getTypeListId(outerTypeParameters), links.resolvedType);
      }
    }
    return links.resolvedType;
  }
  function getTypeFromInferTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      links.resolvedType ***REMOVED*** getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(node.typeParameter));
    }
    return links.resolvedType;
  }
  function getIdentifierChain(node) {
    if (isIdentifier(node)) {
      return [node];
    } else {
      return append(getIdentifierChain(node.left), node.right);
    }
  }
  function getTypeFromImportTypeNode(node) {
    var _a;
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      if (!isLiteralImportTypeNode(node)) {
        error(node.argument, Diagnostics.String_literal_expected);
        links.resolvedSymbol ***REMOVED*** unknownSymbol;
        return links.resolvedType ***REMOVED*** errorType;
      }
      const targetMeaning ***REMOVED*** node.isTypeOf ? 111551 /* Value */ : node.flags & 16777216 /* JSDoc */ ? 111551 /* Value */ | 788968 /* Type */ : 788968 /* Type */;
      const innerModuleSymbol ***REMOVED*** resolveExternalModuleName(node, node.argument.literal);
      if (!innerModuleSymbol) {
        links.resolvedSymbol ***REMOVED*** unknownSymbol;
        return links.resolvedType ***REMOVED*** errorType;
      }
      const isExportEquals ***REMOVED*** !!((_a ***REMOVED*** innerModuleSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.get("export***REMOVED***" /* ExportEquals */));
      const moduleSymbol ***REMOVED*** resolveExternalModuleSymbol(
        innerModuleSymbol,
        /*dontResolveAlias*/
        false
      );
      if (!nodeIsMissing(node.qualifier)) {
        const nameStack ***REMOVED*** getIdentifierChain(node.qualifier);
        let currentNamespace ***REMOVED*** moduleSymbol;
        let current;
        while (current ***REMOVED*** nameStack.shift()) {
          const meaning ***REMOVED*** nameStack.length ? 1920 /* Namespace */ : targetMeaning;
          const mergedResolvedSymbol ***REMOVED*** getMergedSymbol(resolveSymbol(currentNamespace));
          const symbolFromVariable ***REMOVED*** node.isTypeOf || isInJSFile(node) && isExportEquals ? getPropertyOfType(
            getTypeOfSymbol(mergedResolvedSymbol),
            current.escapedText,
            /*skipObjectFunctionPropertyAugment*/
            false,
            /*includeTypeOnlyMembers*/
            true
          ) : void 0;
          const symbolFromModule ***REMOVED*** node.isTypeOf ? void 0 : getSymbol(getExportsOfSymbol(mergedResolvedSymbol), current.escapedText, meaning);
          const next ***REMOVED*** symbolFromModule ?? symbolFromVariable;
          if (!next) {
            error(current, Diagnostics.Namespace_0_has_no_exported_member_1, getFullyQualifiedName(currentNamespace), declarationNameToString(current));
            return links.resolvedType ***REMOVED*** errorType;
          }
          getNodeLinks(current).resolvedSymbol ***REMOVED*** next;
          getNodeLinks(current.parent).resolvedSymbol ***REMOVED*** next;
          currentNamespace ***REMOVED*** next;
        }
        links.resolvedType ***REMOVED*** resolveImportSymbolType(node, links, currentNamespace, targetMeaning);
      } else {
        if (moduleSymbol.flags & targetMeaning) {
          links.resolvedType ***REMOVED*** resolveImportSymbolType(node, links, moduleSymbol, targetMeaning);
        } else {
          const errorMessage ***REMOVED*** targetMeaning ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */ ? Diagnostics.Module_0_does_not_refer_to_a_value_but_is_used_as_a_value_here : Diagnostics.Module_0_does_not_refer_to_a_type_but_is_used_as_a_type_here_Did_you_mean_typeof_import_0;
          error(node, errorMessage, node.argument.literal.text);
          links.resolvedSymbol ***REMOVED*** unknownSymbol;
          links.resolvedType ***REMOVED*** errorType;
        }
      }
    }
    return links.resolvedType;
  }
  function resolveImportSymbolType(node, links, symbol, meaning) {
    const resolvedSymbol ***REMOVED*** resolveSymbol(symbol);
    links.resolvedSymbol ***REMOVED*** resolvedSymbol;
    if (meaning ***REMOVED******REMOVED******REMOVED*** 111551 /* Value */) {
      return getInstantiationExpressionType(getTypeOfSymbol(symbol), node);
    } else {
      return getTypeReferenceType(node, resolvedSymbol);
    }
  }
  function getTypeFromTypeLiteralOrFunctionOrConstructorTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const aliasSymbol ***REMOVED*** getAliasSymbolForTypeNode(node);
      if (!node.symbol || getMembersOfSymbol(node.symbol).size ***REMOVED******REMOVED******REMOVED*** 0 && !aliasSymbol) {
        links.resolvedType ***REMOVED*** emptyTypeLiteralType;
      } else {
        let type ***REMOVED*** createObjectType(16 /* Anonymous */, node.symbol);
        type.aliasSymbol ***REMOVED*** aliasSymbol;
        type.aliasTypeArguments ***REMOVED*** getTypeArgumentsForAliasSymbol(aliasSymbol);
        if (isJSDocTypeLiteral(node) && node.isArrayType) {
          type ***REMOVED*** createArrayType(type);
        }
        links.resolvedType ***REMOVED*** type;
      }
    }
    return links.resolvedType;
  }
  function getAliasSymbolForTypeNode(node) {
    let host2 ***REMOVED*** node.parent;
    while (isParenthesizedTypeNode(host2) || isJSDocTypeExpression(host2) || isTypeOperatorNode(host2) && host2.operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
      host2 ***REMOVED*** host2.parent;
    }
    return isTypeAlias(host2) ? getSymbolOfDeclaration(host2) : void 0;
  }
  function getTypeArgumentsForAliasSymbol(symbol) {
    return symbol ? getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(symbol) : void 0;
  }
  function isNonGenericObjectType(type) {
    return !!(type.flags & 524288 /* Object */) && !isGenericMappedType(type);
  }
  function isEmptyObjectTypeOrSpreadsIntoEmptyObject(type) {
    return isEmptyObjectType(type) || !!(type.flags & (65536 /* Null */ | 32768 /* Undefined */ | 528 /* BooleanLike */ | 296 /* NumberLike */ | 2112 /* BigIntLike */ | 402653316 /* StringLike */ | 1056 /* EnumLike */ | 67108864 /* NonPrimitive */ | 4194304 /* Index */));
  }
  function tryMergeUnionOfObjectTypeAndEmptyObject(type, readonly) {
    if (!(type.flags & 1048576 /* Union */)) {
      return type;
    }
    if (every(type.types, isEmptyObjectTypeOrSpreadsIntoEmptyObject)) {
      return find(type.types, isEmptyObjectType) || emptyObjectType;
    }
    const firstType ***REMOVED*** find(type.types, (t) ***REMOVED***> !isEmptyObjectTypeOrSpreadsIntoEmptyObject(t));
    if (!firstType) {
      return type;
    }
    const secondType ***REMOVED*** find(type.types, (t) ***REMOVED***> t !***REMOVED******REMOVED*** firstType && !isEmptyObjectTypeOrSpreadsIntoEmptyObject(t));
    if (secondType) {
      return type;
    }
    return getAnonymousPartialType(firstType);
    function getAnonymousPartialType(type2) {
      const members ***REMOVED*** createSymbolTable();
      for (const prop of getPropertiesOfType(type2)) {
        if (getDeclarationModifierFlagsFromSymbol(prop) & (2 /* Private */ | 4 /* Protected */)) {
        } else if (isSpreadableProperty(prop)) {
          const isSetonlyAccessor ***REMOVED*** prop.flags & 65536 /* SetAccessor */ && !(prop.flags & 32768 /* GetAccessor */);
          const flags ***REMOVED*** 4 /* Property */ | 16777216 /* Optional */;
          const result ***REMOVED*** createSymbol(flags, prop.escapedName, getIsLateCheckFlag(prop) | (readonly ? 8 /* Readonly */ : 0));
          result.links.type ***REMOVED*** isSetonlyAccessor ? undefinedType : addOptionality(
            getTypeOfSymbol(prop),
            /*isProperty*/
            true
          );
          result.declarations ***REMOVED*** prop.declarations;
          result.links.nameType ***REMOVED*** getSymbolLinks(prop).nameType;
          result.links.syntheticOrigin ***REMOVED*** prop;
          members.set(prop.escapedName, result);
        }
      }
      const spread ***REMOVED*** createAnonymousType(type2.symbol, members, emptyArray, emptyArray, getIndexInfosOfType(type2));
      spread.objectFlags |***REMOVED*** 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
      return spread;
    }
  }
  function getSpreadType(left, right, symbol, objectFlags, readonly) {
    if (left.flags & 1 /* Any */ || right.flags & 1 /* Any */) {
      return anyType;
    }
    if (left.flags & 2 /* Unknown */ || right.flags & 2 /* Unknown */) {
      return unknownType;
    }
    if (left.flags & 131072 /* Never */) {
      return right;
    }
    if (right.flags & 131072 /* Never */) {
      return left;
    }
    left ***REMOVED*** tryMergeUnionOfObjectTypeAndEmptyObject(left, readonly);
    if (left.flags & 1048576 /* Union */) {
      return checkCrossProductUnion([left, right]) ? mapType(left, (t) ***REMOVED***> getSpreadType(t, right, symbol, objectFlags, readonly)) : errorType;
    }
    right ***REMOVED*** tryMergeUnionOfObjectTypeAndEmptyObject(right, readonly);
    if (right.flags & 1048576 /* Union */) {
      return checkCrossProductUnion([left, right]) ? mapType(right, (t) ***REMOVED***> getSpreadType(left, t, symbol, objectFlags, readonly)) : errorType;
    }
    if (right.flags & (528 /* BooleanLike */ | 296 /* NumberLike */ | 2112 /* BigIntLike */ | 402653316 /* StringLike */ | 1056 /* EnumLike */ | 67108864 /* NonPrimitive */ | 4194304 /* Index */)) {
      return left;
    }
    if (isGenericObjectType(left) || isGenericObjectType(right)) {
      if (isEmptyObjectType(left)) {
        return right;
      }
      if (left.flags & 2097152 /* Intersection */) {
        const types ***REMOVED*** left.types;
        const lastLeft ***REMOVED*** types[types.length - 1];
        if (isNonGenericObjectType(lastLeft) && isNonGenericObjectType(right)) {
          return getIntersectionType(concatenate(types.slice(0, types.length - 1), [getSpreadType(lastLeft, right, symbol, objectFlags, readonly)]));
        }
      }
      return getIntersectionType([left, right]);
    }
    const members ***REMOVED*** createSymbolTable();
    const skippedPrivateMembers ***REMOVED*** /* @__PURE__ */ new Set();
    const indexInfos ***REMOVED*** left ***REMOVED******REMOVED******REMOVED*** emptyObjectType ? getIndexInfosOfType(right) : getUnionIndexInfos([left, right]);
    for (const rightProp of getPropertiesOfType(right)) {
      if (getDeclarationModifierFlagsFromSymbol(rightProp) & (2 /* Private */ | 4 /* Protected */)) {
        skippedPrivateMembers.add(rightProp.escapedName);
      } else if (isSpreadableProperty(rightProp)) {
        members.set(rightProp.escapedName, getSpreadSymbol(rightProp, readonly));
      }
    }
    for (const leftProp of getPropertiesOfType(left)) {
      if (skippedPrivateMembers.has(leftProp.escapedName) || !isSpreadableProperty(leftProp)) {
        continue;
      }
      if (members.has(leftProp.escapedName)) {
        const rightProp ***REMOVED*** members.get(leftProp.escapedName);
        const rightType ***REMOVED*** getTypeOfSymbol(rightProp);
        if (rightProp.flags & 16777216 /* Optional */) {
          const declarations ***REMOVED*** concatenate(leftProp.declarations, rightProp.declarations);
          const flags ***REMOVED*** 4 /* Property */ | leftProp.flags & 16777216 /* Optional */;
          const result ***REMOVED*** createSymbol(flags, leftProp.escapedName);
          const leftType ***REMOVED*** getTypeOfSymbol(leftProp);
          const leftTypeWithoutUndefined ***REMOVED*** removeMissingOrUndefinedType(leftType);
          const rightTypeWithoutUndefined ***REMOVED*** removeMissingOrUndefinedType(rightType);
          result.links.type ***REMOVED*** leftTypeWithoutUndefined ***REMOVED******REMOVED******REMOVED*** rightTypeWithoutUndefined ? leftType : getUnionType([leftType, rightTypeWithoutUndefined], 2 /* Subtype */);
          result.links.leftSpread ***REMOVED*** leftProp;
          result.links.rightSpread ***REMOVED*** rightProp;
          result.declarations ***REMOVED*** declarations;
          result.links.nameType ***REMOVED*** getSymbolLinks(leftProp).nameType;
          members.set(leftProp.escapedName, result);
        }
      } else {
        members.set(leftProp.escapedName, getSpreadSymbol(leftProp, readonly));
      }
    }
    const spread ***REMOVED*** createAnonymousType(symbol, members, emptyArray, emptyArray, sameMap(indexInfos, (info) ***REMOVED***> getIndexInfoWithReadonly(info, readonly)));
    spread.objectFlags |***REMOVED*** 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */ | 2097152 /* ContainsSpread */ | objectFlags;
    return spread;
  }
  function isSpreadableProperty(prop) {
    var _a;
    return !some(prop.declarations, isPrivateIdentifierClassElementDeclaration) && (!(prop.flags & (8192 /* Method */ | 32768 /* GetAccessor */ | 65536 /* SetAccessor */)) || !((_a ***REMOVED*** prop.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.some((decl) ***REMOVED***> isClassLike(decl.parent))));
  }
  function getSpreadSymbol(prop, readonly) {
    const isSetonlyAccessor ***REMOVED*** prop.flags & 65536 /* SetAccessor */ && !(prop.flags & 32768 /* GetAccessor */);
    if (!isSetonlyAccessor && readonly ***REMOVED******REMOVED******REMOVED*** isReadonlySymbol(prop)) {
      return prop;
    }
    const flags ***REMOVED*** 4 /* Property */ | prop.flags & 16777216 /* Optional */;
    const result ***REMOVED*** createSymbol(flags, prop.escapedName, getIsLateCheckFlag(prop) | (readonly ? 8 /* Readonly */ : 0));
    result.links.type ***REMOVED*** isSetonlyAccessor ? undefinedType : getTypeOfSymbol(prop);
    result.declarations ***REMOVED*** prop.declarations;
    result.links.nameType ***REMOVED*** getSymbolLinks(prop).nameType;
    result.links.syntheticOrigin ***REMOVED*** prop;
    return result;
  }
  function getIndexInfoWithReadonly(info, readonly) {
    return info.isReadonly !***REMOVED******REMOVED*** readonly ? createIndexInfo(info.keyType, info.type, readonly, info.declaration, info.components) : info;
  }
  function createLiteralType(flags, value, symbol, regularType) {
    const type ***REMOVED*** createTypeWithSymbol(flags, symbol);
    type.value ***REMOVED*** value;
    type.regularType ***REMOVED*** regularType || type;
    return type;
  }
  function getFreshTypeOfLiteralType(type) {
    if (type.flags & 2976 /* Freshable */) {
      if (!type.freshType) {
        const freshType ***REMOVED*** createLiteralType(type.flags, type.value, type.symbol, type);
        freshType.freshType ***REMOVED*** freshType;
        type.freshType ***REMOVED*** freshType;
      }
      return type.freshType;
    }
    return type;
  }
  function getRegularTypeOfLiteralType(type) {
    return type.flags & 2976 /* Freshable */ ? type.regularType : type.flags & 1048576 /* Union */ ? type.regularType || (type.regularType ***REMOVED*** mapType(type, getRegularTypeOfLiteralType)) : type;
  }
  function isFreshLiteralType(type) {
    return !!(type.flags & 2976 /* Freshable */) && type.freshType ***REMOVED******REMOVED******REMOVED*** type;
  }
  function getStringLiteralType(value) {
    let type;
    return stringLiteralTypes.get(value) || (stringLiteralTypes.set(value, type ***REMOVED*** createLiteralType(128 /* StringLiteral */, value)), type);
  }
  function getNumberLiteralType(value) {
    let type;
    return numberLiteralTypes.get(value) || (numberLiteralTypes.set(value, type ***REMOVED*** createLiteralType(256 /* NumberLiteral */, value)), type);
  }
  function getBigIntLiteralType(value) {
    let type;
    const key ***REMOVED*** pseudoBigIntToString(value);
    return bigIntLiteralTypes.get(key) || (bigIntLiteralTypes.set(key, type ***REMOVED*** createLiteralType(2048 /* BigIntLiteral */, value)), type);
  }
  function getEnumLiteralType(value, enumId, symbol) {
    let type;
    const key ***REMOVED*** `${enumId}${typeof value ***REMOVED******REMOVED******REMOVED*** "string" ? "@" : "#"}${value}`;
    const flags ***REMOVED*** 1024 /* EnumLiteral */ | (typeof value ***REMOVED******REMOVED******REMOVED*** "string" ? 128 /* StringLiteral */ : 256 /* NumberLiteral */);
    return enumLiteralTypes.get(key) || (enumLiteralTypes.set(key, type ***REMOVED*** createLiteralType(flags, value, symbol)), type);
  }
  function getTypeFromLiteralTypeNode(node) {
    if (node.literal.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */) {
      return nullType;
    }
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      links.resolvedType ***REMOVED*** getRegularTypeOfLiteralType(checkExpression(node.literal));
    }
    return links.resolvedType;
  }
  function createUniqueESSymbolType(symbol) {
    const type ***REMOVED*** createTypeWithSymbol(8192 /* UniqueESSymbol */, symbol);
    type.escapedName ***REMOVED*** `__@${type.symbol.escapedName}@${getSymbolId(type.symbol)}`;
    return type;
  }
  function getESSymbolLikeTypeForNode(node) {
    if (isInJSFile(node) && isJSDocTypeExpression(node)) {
      const host2 ***REMOVED*** getJSDocHost(node);
      if (host2) {
        node ***REMOVED*** getSingleVariableOfVariableStatement(host2) || host2;
      }
    }
    if (isValidESSymbolDeclaration(node)) {
      const symbol ***REMOVED*** isCommonJsExportPropertyAssignment(node) ? getSymbolOfNode(node.left) : getSymbolOfNode(node);
      if (symbol) {
        const links ***REMOVED*** getSymbolLinks(symbol);
        return links.uniqueESSymbolType || (links.uniqueESSymbolType ***REMOVED*** createUniqueESSymbolType(symbol));
      }
    }
    return esSymbolType;
  }
  function getThisType(node) {
    const container ***REMOVED*** getThisContainer(
      node,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    const parent ***REMOVED*** container && container.parent;
    if (parent && (isClassLike(parent) || parent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */)) {
      if (!isStatic(container) && (!isConstructorDeclaration(container) || isNodeDescendantOf(node, container.body))) {
        return getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(parent)).thisType;
      }
    }
    if (parent && isObjectLiteralExpression(parent) && isBinaryExpression(parent.parent) && getAssignmentDeclarationKind(parent.parent) ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */) {
      return getDeclaredTypeOfClassOrInterface(getSymbolOfNode(parent.parent.left).parent).thisType;
    }
    const host2 ***REMOVED*** node.flags & 16777216 /* JSDoc */ ? getHostSignatureFromJSDoc(node) : void 0;
    if (host2 && isFunctionExpression(host2) && isBinaryExpression(host2.parent) && getAssignmentDeclarationKind(host2.parent) ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */) {
      return getDeclaredTypeOfClassOrInterface(getSymbolOfNode(host2.parent.left).parent).thisType;
    }
    if (isJSConstructor(container) && isNodeDescendantOf(node, container.body)) {
      return getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(container)).thisType;
    }
    error(node, Diagnostics.A_this_type_is_available_only_in_a_non_static_member_of_a_class_or_interface);
    return errorType;
  }
  function getTypeFromThisTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      links.resolvedType ***REMOVED*** getThisType(node);
    }
    return links.resolvedType;
  }
  function getTypeFromRestTypeNode(node) {
    return getTypeFromTypeNode(getArrayElementTypeNode(node.type) || node.type);
  }
  function getArrayElementTypeNode(node) {
    switch (node.kind) {
      case 197 /* ParenthesizedType */:
        return getArrayElementTypeNode(node.type);
      case 190 /* TupleType */:
        if (node.elements.length ***REMOVED******REMOVED******REMOVED*** 1) {
          node ***REMOVED*** node.elements[0];
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 192 /* RestType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */ && node.dotDotDotToken) {
            return getArrayElementTypeNode(node.type);
          }
        }
        break;
      case 189 /* ArrayType */:
        return node.elementType;
    }
    return void 0;
  }
  function getTypeFromNamedTupleTypeNode(node) {
    const links ***REMOVED*** getNodeLinks(node);
    return links.resolvedType || (links.resolvedType ***REMOVED*** node.dotDotDotToken ? getTypeFromRestTypeNode(node) : addOptionality(
      getTypeFromTypeNode(node.type),
      /*isProperty*/
      true,
      !!node.questionToken
    ));
  }
  function getTypeFromTypeNode(node) {
    return getConditionalFlowTypeOfType(getTypeFromTypeNodeWorker(node), node);
  }
  function getTypeFromTypeNodeWorker(node) {
    switch (node.kind) {
      case 133 /* AnyKeyword */:
      case 313 /* JSDocAllType */:
      case 314 /* JSDocUnknownType */:
        return anyType;
      case 159 /* UnknownKeyword */:
        return unknownType;
      case 154 /* StringKeyword */:
        return stringType;
      case 150 /* NumberKeyword */:
        return numberType;
      case 163 /* BigIntKeyword */:
        return bigintType;
      case 136 /* BooleanKeyword */:
        return booleanType;
      case 155 /* SymbolKeyword */:
        return esSymbolType;
      case 116 /* VoidKeyword */:
        return voidType;
      case 157 /* UndefinedKeyword */:
        return undefinedType;
      case 106 /* NullKeyword */:
        return nullType;
      case 146 /* NeverKeyword */:
        return neverType;
      case 151 /* ObjectKeyword */:
        return node.flags & 524288 /* JavaScriptFile */ && !noImplicitAny ? anyType : nonPrimitiveType;
      case 141 /* IntrinsicKeyword */:
        return intrinsicMarkerType;
      case 198 /* ThisType */:
      case 110 /* ThisKeyword */:
        return getTypeFromThisTypeNode(node);
      case 202 /* LiteralType */:
        return getTypeFromLiteralTypeNode(node);
      case 184 /* TypeReference */:
        return getTypeFromTypeReference(node);
      case 183 /* TypePredicate */:
        return node.assertsModifier ? voidType : booleanType;
      case 234 /* ExpressionWithTypeArguments */:
        return getTypeFromTypeReference(node);
      case 187 /* TypeQuery */:
        return getTypeFromTypeQueryNode(node);
      case 189 /* ArrayType */:
      case 190 /* TupleType */:
        return getTypeFromArrayOrTupleTypeNode(node);
      case 191 /* OptionalType */:
        return getTypeFromOptionalTypeNode(node);
      case 193 /* UnionType */:
        return getTypeFromUnionTypeNode(node);
      case 194 /* IntersectionType */:
        return getTypeFromIntersectionTypeNode(node);
      case 315 /* JSDocNullableType */:
        return getTypeFromJSDocNullableTypeNode(node);
      case 317 /* JSDocOptionalType */:
        return addOptionality(getTypeFromTypeNode(node.type));
      case 203 /* NamedTupleMember */:
        return getTypeFromNamedTupleTypeNode(node);
      case 197 /* ParenthesizedType */:
      case 316 /* JSDocNonNullableType */:
      case 310 /* JSDocTypeExpression */:
        return getTypeFromTypeNode(node.type);
      case 192 /* RestType */:
        return getTypeFromRestTypeNode(node);
      case 319 /* JSDocVariadicType */:
        return getTypeFromJSDocVariadicType(node);
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */:
      case 188 /* TypeLiteral */:
      case 323 /* JSDocTypeLiteral */:
      case 318 /* JSDocFunctionType */:
      case 324 /* JSDocSignature */:
        return getTypeFromTypeLiteralOrFunctionOrConstructorTypeNode(node);
      case 199 /* TypeOperator */:
        return getTypeFromTypeOperatorNode(node);
      case 200 /* IndexedAccessType */:
        return getTypeFromIndexedAccessTypeNode(node);
      case 201 /* MappedType */:
        return getTypeFromMappedTypeNode(node);
      case 195 /* ConditionalType */:
        return getTypeFromConditionalTypeNode(node);
      case 196 /* InferType */:
        return getTypeFromInferTypeNode(node);
      case 204 /* TemplateLiteralType */:
        return getTypeFromTemplateTypeNode(node);
      case 206 /* ImportType */:
        return getTypeFromImportTypeNode(node);
      // This function assumes that an identifier, qualified name, or property access expression is a type expression
      // Callers should first ensure this by calling `isPartOfTypeNode`
      // TODO(rbuckton): These aren't valid TypeNodes, but we treat them as such because of `isPartOfTypeNode`, which returns `true` for things that aren't `TypeNode`s.
      case 80 /* Identifier */:
      case 167 /* QualifiedName */:
      case 212 /* PropertyAccessExpression */:
        const symbol ***REMOVED*** getSymbolAtLocation(node);
        return symbol ? getDeclaredTypeOfSymbol(symbol) : errorType;
      default:
        return errorType;
    }
  }
  function instantiateList(items, mapper, instantiator) {
    if (items && items.length) {
      for (let i ***REMOVED*** 0; i < items.length; i++) {
        const item ***REMOVED*** items[i];
        const mapped ***REMOVED*** instantiator(item, mapper);
        if (item !***REMOVED******REMOVED*** mapped) {
          const result ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** 0 ? [] : items.slice(0, i);
          result.push(mapped);
          for (i++; i < items.length; i++) {
            result.push(instantiator(items[i], mapper));
          }
          return result;
        }
      }
    }
    return items;
  }
  function instantiateTypes(types, mapper) {
    return instantiateList(types, mapper, instantiateType);
  }
  function instantiateSignatures(signatures, mapper) {
    return instantiateList(signatures, mapper, instantiateSignature);
  }
  function instantiateIndexInfos(indexInfos, mapper) {
    return instantiateList(indexInfos, mapper, instantiateIndexInfo);
  }
  function createTypeMapper(sources, targets) {
    return sources.length ***REMOVED******REMOVED******REMOVED*** 1 ? makeUnaryTypeMapper(sources[0], targets ? targets[0] : anyType) : makeArrayTypeMapper(sources, targets);
  }
  function getMappedType(type, mapper) {
    switch (mapper.kind) {
      case 0 /* Simple */:
        return type ***REMOVED******REMOVED******REMOVED*** mapper.source ? mapper.target : type;
      case 1 /* Array */: {
        const sources ***REMOVED*** mapper.sources;
        const targets ***REMOVED*** mapper.targets;
        for (let i ***REMOVED*** 0; i < sources.length; i++) {
          if (type ***REMOVED******REMOVED******REMOVED*** sources[i]) {
            return targets ? targets[i] : anyType;
          }
        }
        return type;
      }
      case 2 /* Deferred */: {
        const sources ***REMOVED*** mapper.sources;
        const targets ***REMOVED*** mapper.targets;
        for (let i ***REMOVED*** 0; i < sources.length; i++) {
          if (type ***REMOVED******REMOVED******REMOVED*** sources[i]) {
            return targets[i]();
          }
        }
        return type;
      }
      case 3 /* Function */:
        return mapper.func(type);
      case 4 /* Composite */:
      case 5 /* Merged */:
        const t1 ***REMOVED*** getMappedType(type, mapper.mapper1);
        return t1 !***REMOVED******REMOVED*** type && mapper.kind ***REMOVED******REMOVED******REMOVED*** 4 /* Composite */ ? instantiateType(t1, mapper.mapper2) : getMappedType(t1, mapper.mapper2);
    }
  }
  function makeUnaryTypeMapper(source, target) {
    return Debug.attachDebugPrototypeIfDebug({ kind: 0 /* Simple */, source, target });
  }
  function makeArrayTypeMapper(sources, targets) {
    return Debug.attachDebugPrototypeIfDebug({ kind: 1 /* Array */, sources, targets });
  }
  function makeFunctionTypeMapper(func, debugInfo) {
    return Debug.attachDebugPrototypeIfDebug({ kind: 3 /* Function */, func, debugInfo: Debug.isDebugging ? debugInfo : void 0 });
  }
  function makeDeferredTypeMapper(sources, targets) {
    return Debug.attachDebugPrototypeIfDebug({ kind: 2 /* Deferred */, sources, targets });
  }
  function makeCompositeTypeMapper(kind, mapper1, mapper2) {
    return Debug.attachDebugPrototypeIfDebug({ kind, mapper1, mapper2 });
  }
  function createTypeEraser(sources) {
    return createTypeMapper(
      sources,
      /*targets*/
      void 0
    );
  }
  function createBackreferenceMapper(context, index) {
    const forwardInferences ***REMOVED*** context.inferences.slice(index);
    return createTypeMapper(map(forwardInferences, (i) ***REMOVED***> i.typeParameter), map(forwardInferences, () ***REMOVED***> unknownType));
  }
  function createOuterReturnMapper(context) {
    return context.outerReturnMapper ?? (context.outerReturnMapper ***REMOVED*** mergeTypeMappers(context.returnMapper, cloneInferenceContext(context).mapper));
  }
  function combineTypeMappers(mapper1, mapper2) {
    return mapper1 ? makeCompositeTypeMapper(4 /* Composite */, mapper1, mapper2) : mapper2;
  }
  function mergeTypeMappers(mapper1, mapper2) {
    return mapper1 ? makeCompositeTypeMapper(5 /* Merged */, mapper1, mapper2) : mapper2;
  }
  function prependTypeMapping(source, target, mapper) {
    return !mapper ? makeUnaryTypeMapper(source, target) : makeCompositeTypeMapper(5 /* Merged */, makeUnaryTypeMapper(source, target), mapper);
  }
  function appendTypeMapping(mapper, source, target) {
    return !mapper ? makeUnaryTypeMapper(source, target) : makeCompositeTypeMapper(5 /* Merged */, mapper, makeUnaryTypeMapper(source, target));
  }
  function getRestrictiveTypeParameter(tp) {
    return !tp.constraint && !getConstraintDeclaration(tp) || tp.constraint ***REMOVED******REMOVED******REMOVED*** noConstraintType ? tp : tp.restrictiveInstantiation || (tp.restrictiveInstantiation ***REMOVED*** createTypeParameter(tp.symbol), tp.restrictiveInstantiation.constraint ***REMOVED*** noConstraintType, tp.restrictiveInstantiation);
  }
  function cloneTypeParameter(typeParameter) {
    const result ***REMOVED*** createTypeParameter(typeParameter.symbol);
    result.target ***REMOVED*** typeParameter;
    return result;
  }
  function instantiateTypePredicate(predicate, mapper) {
    return createTypePredicate(predicate.kind, predicate.parameterName, predicate.parameterIndex, instantiateType(predicate.type, mapper));
  }
  function instantiateSignature(signature, mapper, eraseTypeParameters) {
    let freshTypeParameters;
    if (signature.typeParameters && !eraseTypeParameters) {
      freshTypeParameters ***REMOVED*** map(signature.typeParameters, cloneTypeParameter);
      mapper ***REMOVED*** combineTypeMappers(createTypeMapper(signature.typeParameters, freshTypeParameters), mapper);
      for (const tp of freshTypeParameters) {
        tp.mapper ***REMOVED*** mapper;
      }
    }
    const result ***REMOVED*** createSignature(
      signature.declaration,
      freshTypeParameters,
      signature.thisParameter && instantiateSymbol(signature.thisParameter, mapper),
      instantiateList(signature.parameters, mapper, instantiateSymbol),
      /*resolvedReturnType*/
      void 0,
      /*resolvedTypePredicate*/
      void 0,
      signature.minArgumentCount,
      signature.flags & 167 /* PropagatingFlags */
    );
    result.target ***REMOVED*** signature;
    result.mapper ***REMOVED*** mapper;
    return result;
  }
  function instantiateSymbol(symbol, mapper) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (links.type && !couldContainTypeVariables(links.type)) {
      if (!(symbol.flags & 65536 /* SetAccessor */)) {
        return symbol;
      }
      if (links.writeType && !couldContainTypeVariables(links.writeType)) {
        return symbol;
      }
    }
    if (getCheckFlags(symbol) & 1 /* Instantiated */) {
      symbol ***REMOVED*** links.target;
      mapper ***REMOVED*** combineTypeMappers(links.mapper, mapper);
    }
    const result ***REMOVED*** createSymbol(symbol.flags, symbol.escapedName, 1 /* Instantiated */ | getCheckFlags(symbol) & (8 /* Readonly */ | 4096 /* Late */ | 16384 /* OptionalParameter */ | 32768 /* RestParameter */));
    result.declarations ***REMOVED*** symbol.declarations;
    result.parent ***REMOVED*** symbol.parent;
    result.links.target ***REMOVED*** symbol;
    result.links.mapper ***REMOVED*** mapper;
    if (symbol.valueDeclaration) {
      result.valueDeclaration ***REMOVED*** symbol.valueDeclaration;
    }
    if (links.nameType) {
      result.links.nameType ***REMOVED*** links.nameType;
    }
    return result;
  }
  function getObjectTypeInstantiation(type, mapper, aliasSymbol, aliasTypeArguments) {
    const declaration ***REMOVED*** type.objectFlags & 4 /* Reference */ ? type.node : type.objectFlags & 8388608 /* InstantiationExpressionType */ ? type.node : type.symbol.declarations[0];
    const links ***REMOVED*** getNodeLinks(declaration);
    const target ***REMOVED*** type.objectFlags & 4 /* Reference */ ? links.resolvedType : type.objectFlags & 64 /* Instantiated */ ? type.target : type;
    let typeParameters ***REMOVED*** links.outerTypeParameters;
    if (!typeParameters) {
      let outerTypeParameters ***REMOVED*** getOuterTypeParameters(
        declaration,
        /*includeThisTypes*/
        true
      );
      if (isJSConstructor(declaration)) {
        const templateTagParameters ***REMOVED*** getTypeParametersFromDeclaration(declaration);
        outerTypeParameters ***REMOVED*** addRange(outerTypeParameters, templateTagParameters);
      }
      typeParameters ***REMOVED*** outerTypeParameters || emptyArray;
      const allDeclarations ***REMOVED*** type.objectFlags & (4 /* Reference */ | 8388608 /* InstantiationExpressionType */) ? [declaration] : type.symbol.declarations;
      typeParameters ***REMOVED*** (target.objectFlags & (4 /* Reference */ | 8388608 /* InstantiationExpressionType */) || target.symbol.flags & 8192 /* Method */ || target.symbol.flags & 2048 /* TypeLiteral */) && !target.aliasTypeArguments ? filter(typeParameters, (tp) ***REMOVED***> some(allDeclarations, (d) ***REMOVED***> isTypeParameterPossiblyReferenced(tp, d))) : typeParameters;
      links.outerTypeParameters ***REMOVED*** typeParameters;
    }
    if (typeParameters.length) {
      const combinedMapper ***REMOVED*** combineTypeMappers(type.mapper, mapper);
      const typeArguments ***REMOVED*** map(typeParameters, (t) ***REMOVED***> getMappedType(t, combinedMapper));
      const newAliasSymbol ***REMOVED*** aliasSymbol || type.aliasSymbol;
      const newAliasTypeArguments ***REMOVED*** aliasSymbol ? aliasTypeArguments : instantiateTypes(type.aliasTypeArguments, mapper);
      const id ***REMOVED*** getTypeListId(typeArguments) + getAliasId(newAliasSymbol, newAliasTypeArguments);
      if (!target.instantiations) {
        target.instantiations ***REMOVED*** /* @__PURE__ */ new Map();
        target.instantiations.set(getTypeListId(typeParameters) + getAliasId(target.aliasSymbol, target.aliasTypeArguments), target);
      }
      let result ***REMOVED*** target.instantiations.get(id);
      if (!result) {
        let newMapper ***REMOVED*** createTypeMapper(typeParameters, typeArguments);
        if (target.objectFlags & 134217728 /* SingleSignatureType */ && mapper) {
          newMapper ***REMOVED*** combineTypeMappers(newMapper, mapper);
        }
        result ***REMOVED*** target.objectFlags & 4 /* Reference */ ? createDeferredTypeReference(type.target, type.node, newMapper, newAliasSymbol, newAliasTypeArguments) : target.objectFlags & 32 /* Mapped */ ? instantiateMappedType(target, newMapper, newAliasSymbol, newAliasTypeArguments) : instantiateAnonymousType(target, newMapper, newAliasSymbol, newAliasTypeArguments);
        target.instantiations.set(id, result);
        const resultObjectFlags ***REMOVED*** getObjectFlags(result);
        if (result.flags & 3899393 /* ObjectFlagsType */ && !(resultObjectFlags & 524288 /* CouldContainTypeVariablesComputed */)) {
          const resultCouldContainTypeVariables ***REMOVED*** some(typeArguments, couldContainTypeVariables);
          if (!(getObjectFlags(result) & 524288 /* CouldContainTypeVariablesComputed */)) {
            if (resultObjectFlags & (32 /* Mapped */ | 16 /* Anonymous */ | 4 /* Reference */)) {
              result.objectFlags |***REMOVED*** 524288 /* CouldContainTypeVariablesComputed */ | (resultCouldContainTypeVariables ? 1048576 /* CouldContainTypeVariables */ : 0);
            } else {
              result.objectFlags |***REMOVED*** !resultCouldContainTypeVariables ? 524288 /* CouldContainTypeVariablesComputed */ : 0;
            }
          }
        }
      }
      return result;
    }
    return type;
  }
  function maybeTypeParameterReference(node) {
    return !(node.parent.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ && node.parent.typeArguments && node ***REMOVED******REMOVED******REMOVED*** node.parent.typeName || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */ && node.parent.typeArguments && node ***REMOVED******REMOVED******REMOVED*** node.parent.qualifier);
  }
  function isTypeParameterPossiblyReferenced(tp, node) {
    if (tp.symbol && tp.symbol.declarations && tp.symbol.declarations.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const container ***REMOVED*** tp.symbol.declarations[0].parent;
      for (let n ***REMOVED*** node; n !***REMOVED******REMOVED*** container; n ***REMOVED*** n.parent) {
        if (!n || n.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */ || n.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */ && forEachChild(n.extendsType, containsReference)) {
          return true;
        }
      }
      return containsReference(node);
    }
    return true;
    function containsReference(node2) {
      switch (node2.kind) {
        case 198 /* ThisType */:
          return !!tp.isThisType;
        case 80 /* Identifier */:
          return !tp.isThisType && isPartOfTypeNode(node2) && maybeTypeParameterReference(node2) && getTypeFromTypeNodeWorker(node2) ***REMOVED******REMOVED******REMOVED*** tp;
        // use worker because we're looking for ***REMOVED******REMOVED******REMOVED*** equality
        case 187 /* TypeQuery */:
          const entityName ***REMOVED*** node2.exprName;
          const firstIdentifier ***REMOVED*** getFirstIdentifier(entityName);
          if (!isThisIdentifier(firstIdentifier)) {
            const firstIdentifierSymbol ***REMOVED*** getResolvedSymbol(firstIdentifier);
            const tpDeclaration ***REMOVED*** tp.symbol.declarations[0];
            const tpScope ***REMOVED*** tpDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ ? tpDeclaration.parent : (
              // Type parameter is a regular type parameter, e.g. foo<T>
              tp.isThisType ? tpDeclaration : (
                // Type parameter is the this type, and its declaration is the class declaration.
                void 0
              )
            );
            if (firstIdentifierSymbol.declarations && tpScope) {
              return some(firstIdentifierSymbol.declarations, (idDecl) ***REMOVED***> isNodeDescendantOf(idDecl, tpScope)) || some(node2.typeArguments, containsReference);
            }
          }
          return true;
        case 175 /* MethodDeclaration */:
        case 174 /* MethodSignature */:
          return !node2.type && !!node2.body || some(node2.typeParameters, containsReference) || some(node2.parameters, containsReference) || !!node2.type && containsReference(node2.type);
      }
      return !!forEachChild(node2, containsReference);
    }
  }
  function getHomomorphicTypeVariable(type) {
    const constraintType ***REMOVED*** getConstraintTypeFromMappedType(type);
    if (constraintType.flags & 4194304 /* Index */) {
      const typeVariable ***REMOVED*** getActualTypeVariable(constraintType.type);
      if (typeVariable.flags & 262144 /* TypeParameter */) {
        return typeVariable;
      }
    }
    return void 0;
  }
  function instantiateMappedType(type, mapper, aliasSymbol, aliasTypeArguments) {
    const typeVariable ***REMOVED*** getHomomorphicTypeVariable(type);
    if (typeVariable) {
      const mappedTypeVariable ***REMOVED*** instantiateType(typeVariable, mapper);
      if (typeVariable !***REMOVED******REMOVED*** mappedTypeVariable) {
        return mapTypeWithAlias(getReducedType(mappedTypeVariable), instantiateConstituent, aliasSymbol, aliasTypeArguments);
      }
    }
    return instantiateType(getConstraintTypeFromMappedType(type), mapper) ***REMOVED******REMOVED******REMOVED*** wildcardType ? wildcardType : instantiateAnonymousType(type, mapper, aliasSymbol, aliasTypeArguments);
    function instantiateConstituent(t) {
      if (t.flags & (3 /* AnyOrUnknown */ | 58982400 /* InstantiableNonPrimitive */ | 524288 /* Object */ | 2097152 /* Intersection */) && t !***REMOVED******REMOVED*** wildcardType && !isErrorType(t)) {
        if (!type.declaration.nameType) {
          let constraint;
          if (isArrayType(t) || t.flags & 1 /* Any */ && findResolutionCycleStartIndex(typeVariable, 4 /* ImmediateBaseConstraint */) < 0 && (constraint ***REMOVED*** getConstraintOfTypeParameter(typeVariable)) && everyType(constraint, isArrayOrTupleType)) {
            return instantiateMappedArrayType(t, type, prependTypeMapping(typeVariable, t, mapper));
          }
          if (isTupleType(t)) {
            return instantiateMappedTupleType(t, type, typeVariable, mapper);
          }
          if (isArrayOrTupleOrIntersection(t)) {
            return getIntersectionType(map(t.types, instantiateConstituent));
          }
        }
        return instantiateAnonymousType(type, prependTypeMapping(typeVariable, t, mapper));
      }
      return t;
    }
  }
  function getModifiedReadonlyState(state, modifiers) {
    return modifiers & 1 /* IncludeReadonly */ ? true : modifiers & 2 /* ExcludeReadonly */ ? false : state;
  }
  function instantiateMappedTupleType(tupleType, mappedType, typeVariable, mapper) {
    const elementFlags ***REMOVED*** tupleType.target.elementFlags;
    const fixedLength ***REMOVED*** tupleType.target.fixedLength;
    const fixedMapper ***REMOVED*** fixedLength ? prependTypeMapping(typeVariable, tupleType, mapper) : mapper;
    const newElementTypes ***REMOVED*** map(getElementTypes(tupleType), (type, i) ***REMOVED***> {
      const flags ***REMOVED*** elementFlags[i];
      return i < fixedLength ? instantiateMappedTypeTemplate(mappedType, getStringLiteralType("" + i), !!(flags & 2 /* Optional */), fixedMapper) : flags & 8 /* Variadic */ ? instantiateType(mappedType, prependTypeMapping(typeVariable, type, mapper)) : getElementTypeOfArrayType(instantiateType(mappedType, prependTypeMapping(typeVariable, createArrayType(type), mapper))) ?? unknownType;
    });
    const modifiers ***REMOVED*** getMappedTypeModifiers(mappedType);
    const newElementFlags ***REMOVED*** modifiers & 4 /* IncludeOptional */ ? map(elementFlags, (f) ***REMOVED***> f & 1 /* Required */ ? 2 /* Optional */ : f) : modifiers & 8 /* ExcludeOptional */ ? map(elementFlags, (f) ***REMOVED***> f & 2 /* Optional */ ? 1 /* Required */ : f) : elementFlags;
    const newReadonly ***REMOVED*** getModifiedReadonlyState(tupleType.target.readonly, getMappedTypeModifiers(mappedType));
    return contains(newElementTypes, errorType) ? errorType : createTupleType(newElementTypes, newElementFlags, newReadonly, tupleType.target.labeledElementDeclarations);
  }
  function instantiateMappedArrayType(arrayType, mappedType, mapper) {
    const elementType ***REMOVED*** instantiateMappedTypeTemplate(
      mappedType,
      numberType,
      /*isOptional*/
      true,
      mapper
    );
    return isErrorType(elementType) ? errorType : createArrayType(elementType, getModifiedReadonlyState(isReadonlyArrayType(arrayType), getMappedTypeModifiers(mappedType)));
  }
  function instantiateMappedTypeTemplate(type, key, isOptional, mapper) {
    const templateMapper ***REMOVED*** appendTypeMapping(mapper, getTypeParameterFromMappedType(type), key);
    const propType ***REMOVED*** instantiateType(getTemplateTypeFromMappedType(type.target || type), templateMapper);
    const modifiers ***REMOVED*** getMappedTypeModifiers(type);
    return strictNullChecks && modifiers & 4 /* IncludeOptional */ && !maybeTypeOfKind(propType, 32768 /* Undefined */ | 16384 /* Void */) ? getOptionalType(
      propType,
      /*isProperty*/
      true
    ) : strictNullChecks && modifiers & 8 /* ExcludeOptional */ && isOptional ? getTypeWithFacts(propType, 524288 /* NEUndefined */) : propType;
  }
  function instantiateAnonymousType(type, mapper, aliasSymbol, aliasTypeArguments) {
    Debug.assert(type.symbol, "anonymous type must have symbol to be instantiated");
    const result ***REMOVED*** createObjectType(type.objectFlags & ~(524288 /* CouldContainTypeVariablesComputed */ | 1048576 /* CouldContainTypeVariables */) | 64 /* Instantiated */, type.symbol);
    if (type.objectFlags & 32 /* Mapped */) {
      result.declaration ***REMOVED*** type.declaration;
      const origTypeParameter ***REMOVED*** getTypeParameterFromMappedType(type);
      const freshTypeParameter ***REMOVED*** cloneTypeParameter(origTypeParameter);
      result.typeParameter ***REMOVED*** freshTypeParameter;
      mapper ***REMOVED*** combineTypeMappers(makeUnaryTypeMapper(origTypeParameter, freshTypeParameter), mapper);
      freshTypeParameter.mapper ***REMOVED*** mapper;
    }
    if (type.objectFlags & 8388608 /* InstantiationExpressionType */) {
      result.node ***REMOVED*** type.node;
    }
    result.target ***REMOVED*** type;
    result.mapper ***REMOVED*** mapper;
    result.aliasSymbol ***REMOVED*** aliasSymbol || type.aliasSymbol;
    result.aliasTypeArguments ***REMOVED*** aliasSymbol ? aliasTypeArguments : instantiateTypes(type.aliasTypeArguments, mapper);
    result.objectFlags |***REMOVED*** result.aliasTypeArguments ? getPropagatingFlagsOfTypes(result.aliasTypeArguments) : 0;
    return result;
  }
  function getConditionalTypeInstantiation(type, mapper, forConstraint, aliasSymbol, aliasTypeArguments) {
    const root ***REMOVED*** type.root;
    if (root.outerTypeParameters) {
      const typeArguments ***REMOVED*** map(root.outerTypeParameters, (t) ***REMOVED***> getMappedType(t, mapper));
      const id ***REMOVED*** (forConstraint ? "C" : "") + getTypeListId(typeArguments) + getAliasId(aliasSymbol, aliasTypeArguments);
      let result ***REMOVED*** root.instantiations.get(id);
      if (!result) {
        const newMapper ***REMOVED*** createTypeMapper(root.outerTypeParameters, typeArguments);
        const checkType ***REMOVED*** root.checkType;
        const distributionType ***REMOVED*** root.isDistributive ? getReducedType(getMappedType(checkType, newMapper)) : void 0;
        result ***REMOVED*** distributionType && checkType !***REMOVED******REMOVED*** distributionType && distributionType.flags & (1048576 /* Union */ | 131072 /* Never */) ? mapTypeWithAlias(distributionType, (t) ***REMOVED***> getConditionalType(root, prependTypeMapping(checkType, t, newMapper), forConstraint), aliasSymbol, aliasTypeArguments) : getConditionalType(root, newMapper, forConstraint, aliasSymbol, aliasTypeArguments);
        root.instantiations.set(id, result);
      }
      return result;
    }
    return type;
  }
  function instantiateType(type, mapper) {
    return type && mapper ? instantiateTypeWithAlias(
      type,
      mapper,
      /*aliasSymbol*/
      void 0,
      /*aliasTypeArguments*/
      void 0
    ) : type;
  }
  function instantiateTypeWithAlias(type, mapper, aliasSymbol, aliasTypeArguments) {
    var _a;
    if (!couldContainTypeVariables(type)) {
      return type;
    }
    if (instantiationDepth ***REMOVED******REMOVED******REMOVED*** 100 || instantiationCount >***REMOVED*** 5e6) {
      (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.instant(tracing.Phase.CheckTypes, "instantiateType_DepthLimit", { typeId: type.id, instantiationDepth, instantiationCount });
      error(currentNode, Diagnostics.Type_instantiation_is_excessively_deep_and_possibly_infinite);
      return errorType;
    }
    const index ***REMOVED*** findActiveMapper(mapper);
    if (index ***REMOVED******REMOVED******REMOVED*** -1) {
      pushActiveMapper(mapper);
    }
    const key ***REMOVED*** type.id + getAliasId(aliasSymbol, aliasTypeArguments);
    const mapperCache ***REMOVED*** activeTypeMappersCaches[index !***REMOVED******REMOVED*** -1 ? index : activeTypeMappersCount - 1];
    const cached ***REMOVED*** mapperCache.get(key);
    if (cached) {
      return cached;
    }
    totalInstantiationCount++;
    instantiationCount++;
    instantiationDepth++;
    const result ***REMOVED*** instantiateTypeWorker(type, mapper, aliasSymbol, aliasTypeArguments);
    if (index ***REMOVED******REMOVED******REMOVED*** -1) {
      popActiveMapper();
    } else {
      mapperCache.set(key, result);
    }
    instantiationDepth--;
    return result;
  }
  function instantiateTypeWorker(type, mapper, aliasSymbol, aliasTypeArguments) {
    const flags ***REMOVED*** type.flags;
    if (flags & 262144 /* TypeParameter */) {
      return getMappedType(type, mapper);
    }
    if (flags & 524288 /* Object */) {
      const objectFlags ***REMOVED*** type.objectFlags;
      if (objectFlags & (4 /* Reference */ | 16 /* Anonymous */ | 32 /* Mapped */)) {
        if (objectFlags & 4 /* Reference */ && !type.node) {
          const resolvedTypeArguments ***REMOVED*** type.resolvedTypeArguments;
          const newTypeArguments ***REMOVED*** instantiateTypes(resolvedTypeArguments, mapper);
          return newTypeArguments !***REMOVED******REMOVED*** resolvedTypeArguments ? createNormalizedTypeReference(type.target, newTypeArguments) : type;
        }
        if (objectFlags & 1024 /* ReverseMapped */) {
          return instantiateReverseMappedType(type, mapper);
        }
        return getObjectTypeInstantiation(type, mapper, aliasSymbol, aliasTypeArguments);
      }
      return type;
    }
    if (flags & 3145728 /* UnionOrIntersection */) {
      const origin ***REMOVED*** type.flags & 1048576 /* Union */ ? type.origin : void 0;
      const types ***REMOVED*** origin && origin.flags & 3145728 /* UnionOrIntersection */ ? origin.types : type.types;
      const newTypes ***REMOVED*** instantiateTypes(types, mapper);
      if (newTypes ***REMOVED******REMOVED******REMOVED*** types && aliasSymbol ***REMOVED******REMOVED******REMOVED*** type.aliasSymbol) {
        return type;
      }
      const newAliasSymbol ***REMOVED*** aliasSymbol || type.aliasSymbol;
      const newAliasTypeArguments ***REMOVED*** aliasSymbol ? aliasTypeArguments : instantiateTypes(type.aliasTypeArguments, mapper);
      return flags & 2097152 /* Intersection */ || origin && origin.flags & 2097152 /* Intersection */ ? getIntersectionType(newTypes, 0 /* None */, newAliasSymbol, newAliasTypeArguments) : getUnionType(newTypes, 1 /* Literal */, newAliasSymbol, newAliasTypeArguments);
    }
    if (flags & 4194304 /* Index */) {
      return getIndexType(instantiateType(type.type, mapper));
    }
    if (flags & 134217728 /* TemplateLiteral */) {
      return getTemplateLiteralType(type.texts, instantiateTypes(type.types, mapper));
    }
    if (flags & 268435456 /* StringMapping */) {
      return getStringMappingType(type.symbol, instantiateType(type.type, mapper));
    }
    if (flags & 8388608 /* IndexedAccess */) {
      const newAliasSymbol ***REMOVED*** aliasSymbol || type.aliasSymbol;
      const newAliasTypeArguments ***REMOVED*** aliasSymbol ? aliasTypeArguments : instantiateTypes(type.aliasTypeArguments, mapper);
      return getIndexedAccessType(
        instantiateType(type.objectType, mapper),
        instantiateType(type.indexType, mapper),
        type.accessFlags,
        /*accessNode*/
        void 0,
        newAliasSymbol,
        newAliasTypeArguments
      );
    }
    if (flags & 16777216 /* Conditional */) {
      return getConditionalTypeInstantiation(
        type,
        combineTypeMappers(type.mapper, mapper),
        /*forConstraint*/
        false,
        aliasSymbol,
        aliasTypeArguments
      );
    }
    if (flags & 33554432 /* Substitution */) {
      const newBaseType ***REMOVED*** instantiateType(type.baseType, mapper);
      if (isNoInferType(type)) {
        return getNoInferType(newBaseType);
      }
      const newConstraint ***REMOVED*** instantiateType(type.constraint, mapper);
      if (newBaseType.flags & 8650752 /* TypeVariable */ && isGenericType(newConstraint)) {
        return getSubstitutionType(newBaseType, newConstraint);
      }
      if (newConstraint.flags & 3 /* AnyOrUnknown */ || isTypeAssignableTo(getRestrictiveInstantiation(newBaseType), getRestrictiveInstantiation(newConstraint))) {
        return newBaseType;
      }
      return newBaseType.flags & 8650752 /* TypeVariable */ ? getSubstitutionType(newBaseType, newConstraint) : getIntersectionType([newConstraint, newBaseType]);
    }
    return type;
  }
  function instantiateReverseMappedType(type, mapper) {
    const innerMappedType ***REMOVED*** instantiateType(type.mappedType, mapper);
    if (!(getObjectFlags(innerMappedType) & 32 /* Mapped */)) {
      return type;
    }
    const innerIndexType ***REMOVED*** instantiateType(type.constraintType, mapper);
    if (!(innerIndexType.flags & 4194304 /* Index */)) {
      return type;
    }
    const instantiated ***REMOVED*** inferTypeForHomomorphicMappedType(
      instantiateType(type.source, mapper),
      innerMappedType,
      innerIndexType
    );
    if (instantiated) {
      return instantiated;
    }
    return type;
  }
  function getPermissiveInstantiation(type) {
    return type.flags & (402784252 /* Primitive */ | 3 /* AnyOrUnknown */ | 131072 /* Never */) ? type : type.permissiveInstantiation || (type.permissiveInstantiation ***REMOVED*** instantiateType(type, permissiveMapper));
  }
  function getRestrictiveInstantiation(type) {
    if (type.flags & (402784252 /* Primitive */ | 3 /* AnyOrUnknown */ | 131072 /* Never */)) {
      return type;
    }
    if (type.restrictiveInstantiation) {
      return type.restrictiveInstantiation;
    }
    type.restrictiveInstantiation ***REMOVED*** instantiateType(type, restrictiveMapper);
    type.restrictiveInstantiation.restrictiveInstantiation ***REMOVED*** type.restrictiveInstantiation;
    return type.restrictiveInstantiation;
  }
  function instantiateIndexInfo(info, mapper) {
    return createIndexInfo(info.keyType, instantiateType(info.type, mapper), info.isReadonly, info.declaration, info.components);
  }
  function isContextSensitive(node) {
    Debug.assert(node.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ || isObjectLiteralMethod(node));
    switch (node.kind) {
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 175 /* MethodDeclaration */:
      case 263 /* FunctionDeclaration */:
        return isContextSensitiveFunctionLikeDeclaration(node);
      case 211 /* ObjectLiteralExpression */:
        return some(node.properties, isContextSensitive);
      case 210 /* ArrayLiteralExpression */:
        return some(node.elements, isContextSensitive);
      case 228 /* ConditionalExpression */:
        return isContextSensitive(node.whenTrue) || isContextSensitive(node.whenFalse);
      case 227 /* BinaryExpression */:
        return (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) && (isContextSensitive(node.left) || isContextSensitive(node.right));
      case 304 /* PropertyAssignment */:
        return isContextSensitive(node.initializer);
      case 218 /* ParenthesizedExpression */:
        return isContextSensitive(node.expression);
      case 293 /* JsxAttributes */:
        return some(node.properties, isContextSensitive) || isJsxOpeningElement(node.parent) && some(node.parent.parent.children, isContextSensitive);
      case 292 /* JsxAttribute */: {
        const { initializer } ***REMOVED*** node;
        return !!initializer && isContextSensitive(initializer);
      }
      case 295 /* JsxExpression */: {
        const { expression } ***REMOVED*** node;
        return !!expression && isContextSensitive(expression);
      }
    }
    return false;
  }
  function isContextSensitiveFunctionLikeDeclaration(node) {
    return hasContextSensitiveParameters(node) || hasContextSensitiveReturnExpression(node);
  }
  function hasContextSensitiveReturnExpression(node) {
    if (node.typeParameters || getEffectiveReturnTypeNode(node) || !node.body) {
      return false;
    }
    if (node.body.kind !***REMOVED******REMOVED*** 242 /* Block */) {
      return isContextSensitive(node.body);
    }
    return !!forEachReturnStatement(node.body, (statement) ***REMOVED***> !!statement.expression && isContextSensitive(statement.expression));
  }
  function isContextSensitiveFunctionOrObjectLiteralMethod(func) {
    return (isFunctionExpressionOrArrowFunction(func) || isObjectLiteralMethod(func)) && isContextSensitiveFunctionLikeDeclaration(func);
  }
  function getTypeWithoutSignatures(type) {
    if (type.flags & 524288 /* Object */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      if (resolved.constructSignatures.length || resolved.callSignatures.length) {
        const result ***REMOVED*** createObjectType(16 /* Anonymous */, type.symbol);
        result.members ***REMOVED*** resolved.members;
        result.properties ***REMOVED*** resolved.properties;
        result.callSignatures ***REMOVED*** emptyArray;
        result.constructSignatures ***REMOVED*** emptyArray;
        result.indexInfos ***REMOVED*** emptyArray;
        return result;
      }
    } else if (type.flags & 2097152 /* Intersection */) {
      return getIntersectionType(map(type.types, getTypeWithoutSignatures));
    }
    return type;
  }
  function isTypeIdenticalTo(source, target) {
    return isTypeRelatedTo(source, target, identityRelation);
  }
  function compareTypesIdentical(source, target) {
    return isTypeRelatedTo(source, target, identityRelation) ? -1 /* True */ : 0 /* False */;
  }
  function compareTypesAssignable(source, target) {
    return isTypeRelatedTo(source, target, assignableRelation) ? -1 /* True */ : 0 /* False */;
  }
  function compareTypesSubtypeOf(source, target) {
    return isTypeRelatedTo(source, target, subtypeRelation) ? -1 /* True */ : 0 /* False */;
  }
  function isTypeSubtypeOf(source, target) {
    return isTypeRelatedTo(source, target, subtypeRelation);
  }
  function isTypeStrictSubtypeOf(source, target) {
    return isTypeRelatedTo(source, target, strictSubtypeRelation);
  }
  function isTypeAssignableTo(source, target) {
    return isTypeRelatedTo(source, target, assignableRelation);
  }
  function isTypeDerivedFrom(source, target) {
    return source.flags & 1048576 /* Union */ ? every(source.types, (t) ***REMOVED***> isTypeDerivedFrom(t, target)) : target.flags & 1048576 /* Union */ ? some(target.types, (t) ***REMOVED***> isTypeDerivedFrom(source, t)) : source.flags & 2097152 /* Intersection */ ? some(source.types, (t) ***REMOVED***> isTypeDerivedFrom(t, target)) : source.flags & 58982400 /* InstantiableNonPrimitive */ ? isTypeDerivedFrom(getBaseConstraintOfType(source) || unknownType, target) : isEmptyAnonymousObjectType(target) ? !!(source.flags & (524288 /* Object */ | 67108864 /* NonPrimitive */)) : target ***REMOVED******REMOVED******REMOVED*** globalObjectType ? !!(source.flags & (524288 /* Object */ | 67108864 /* NonPrimitive */)) && !isEmptyAnonymousObjectType(source) : target ***REMOVED******REMOVED******REMOVED*** globalFunctionType ? !!(source.flags & 524288 /* Object */) && isFunctionObjectType(source) : hasBaseType(source, getTargetType(target)) || isArrayType(target) && !isReadonlyArrayType(target) && isTypeDerivedFrom(source, globalReadonlyArrayType);
  }
  function isTypeComparableTo(source, target) {
    return isTypeRelatedTo(source, target, comparableRelation);
  }
  function areTypesComparable(type1, type2) {
    return isTypeComparableTo(type1, type2) || isTypeComparableTo(type2, type1);
  }
  function checkTypeAssignableTo(source, target, errorNode, headMessage, containingMessageChain, errorOutputObject) {
    return checkTypeRelatedTo(source, target, assignableRelation, errorNode, headMessage, containingMessageChain, errorOutputObject);
  }
  function checkTypeAssignableToAndOptionallyElaborate(source, target, errorNode, expr, headMessage, containingMessageChain) {
    return checkTypeRelatedToAndOptionallyElaborate(
      source,
      target,
      assignableRelation,
      errorNode,
      expr,
      headMessage,
      containingMessageChain,
      /*errorOutputContainer*/
      void 0
    );
  }
  function checkTypeRelatedToAndOptionallyElaborate(source, target, relation, errorNode, expr, headMessage, containingMessageChain, errorOutputContainer) {
    if (isTypeRelatedTo(source, target, relation)) return true;
    if (!errorNode || !elaborateError(expr, source, target, relation, headMessage, containingMessageChain, errorOutputContainer)) {
      return checkTypeRelatedTo(source, target, relation, errorNode, headMessage, containingMessageChain, errorOutputContainer);
    }
    return false;
  }
  function isOrHasGenericConditional(type) {
    return !!(type.flags & 16777216 /* Conditional */ || type.flags & 2097152 /* Intersection */ && some(type.types, isOrHasGenericConditional));
  }
  function elaborateError(node, source, target, relation, headMessage, containingMessageChain, errorOutputContainer) {
    if (!node || isOrHasGenericConditional(target)) return false;
    if (!checkTypeRelatedTo(
      source,
      target,
      relation,
      /*errorNode*/
      void 0
    ) && elaborateDidYouMeanToCallOrConstruct(node, source, target, relation, headMessage, containingMessageChain, errorOutputContainer)) {
      return true;
    }
    switch (node.kind) {
      case 235 /* AsExpression */:
        if (!isConstAssertion(node)) {
          break;
        }
      // fallthrough
      case 295 /* JsxExpression */:
      case 218 /* ParenthesizedExpression */:
        return elaborateError(node.expression, source, target, relation, headMessage, containingMessageChain, errorOutputContainer);
      case 227 /* BinaryExpression */:
        switch (node.operatorToken.kind) {
          case 64 /* EqualsToken */:
          case 28 /* CommaToken */:
            return elaborateError(node.right, source, target, relation, headMessage, containingMessageChain, errorOutputContainer);
        }
        break;
      case 211 /* ObjectLiteralExpression */:
        return elaborateObjectLiteral(node, source, target, relation, containingMessageChain, errorOutputContainer);
      case 210 /* ArrayLiteralExpression */:
        return elaborateArrayLiteral(node, source, target, relation, containingMessageChain, errorOutputContainer);
      case 293 /* JsxAttributes */:
        return elaborateJsxComponents(node, source, target, relation, containingMessageChain, errorOutputContainer);
      case 220 /* ArrowFunction */:
        return elaborateArrowFunction(node, source, target, relation, containingMessageChain, errorOutputContainer);
    }
    return false;
  }
  function elaborateDidYouMeanToCallOrConstruct(node, source, target, relation, headMessage, containingMessageChain, errorOutputContainer) {
    const callSignatures ***REMOVED*** getSignaturesOfType(source, 0 /* Call */);
    const constructSignatures ***REMOVED*** getSignaturesOfType(source, 1 /* Construct */);
    for (const signatures of [constructSignatures, callSignatures]) {
      if (some(signatures, (s) ***REMOVED***> {
        const returnType ***REMOVED*** getReturnTypeOfSignature(s);
        return !(returnType.flags & (1 /* Any */ | 131072 /* Never */)) && checkTypeRelatedTo(
          returnType,
          target,
          relation,
          /*errorNode*/
          void 0
        );
      })) {
        const resultObj ***REMOVED*** errorOutputContainer || {};
        checkTypeAssignableTo(source, target, node, headMessage, containingMessageChain, resultObj);
        const diagnostic ***REMOVED*** resultObj.errors[resultObj.errors.length - 1];
        addRelatedInfo(
          diagnostic,
          createDiagnosticForNode(
            node,
            signatures ***REMOVED******REMOVED******REMOVED*** constructSignatures ? Diagnostics.Did_you_mean_to_use_new_with_this_expression : Diagnostics.Did_you_mean_to_call_this_expression
          )
        );
        return true;
      }
    }
    return false;
  }
  function elaborateArrowFunction(node, source, target, relation, containingMessageChain, errorOutputContainer) {
    if (isBlock(node.body)) {
      return false;
    }
    if (some(node.parameters, hasType)) {
      return false;
    }
    const sourceSig ***REMOVED*** getSingleCallSignature(source);
    if (!sourceSig) {
      return false;
    }
    const targetSignatures ***REMOVED*** getSignaturesOfType(target, 0 /* Call */);
    if (!length(targetSignatures)) {
      return false;
    }
    const returnExpression ***REMOVED*** node.body;
    const sourceReturn ***REMOVED*** getReturnTypeOfSignature(sourceSig);
    const targetReturn ***REMOVED*** getUnionType(map(targetSignatures, getReturnTypeOfSignature));
    if (!checkTypeRelatedTo(
      sourceReturn,
      targetReturn,
      relation,
      /*errorNode*/
      void 0
    )) {
      const elaborated ***REMOVED*** returnExpression && elaborateError(
        returnExpression,
        sourceReturn,
        targetReturn,
        relation,
        /*headMessage*/
        void 0,
        containingMessageChain,
        errorOutputContainer
      );
      if (elaborated) {
        return elaborated;
      }
      const resultObj ***REMOVED*** errorOutputContainer || {};
      checkTypeRelatedTo(
        sourceReturn,
        targetReturn,
        relation,
        returnExpression,
        /*headMessage*/
        void 0,
        containingMessageChain,
        resultObj
      );
      if (resultObj.errors) {
        if (target.symbol && length(target.symbol.declarations)) {
          addRelatedInfo(
            resultObj.errors[resultObj.errors.length - 1],
            createDiagnosticForNode(
              target.symbol.declarations[0],
              Diagnostics.The_expected_type_comes_from_the_return_type_of_this_signature
            )
          );
        }
        if ((getFunctionFlags(node) & 2 /* Async */) ***REMOVED******REMOVED******REMOVED*** 0 && !getTypeOfPropertyOfType(sourceReturn, "then") && checkTypeRelatedTo(
          createPromiseType(sourceReturn),
          targetReturn,
          relation,
          /*errorNode*/
          void 0
        )) {
          addRelatedInfo(
            resultObj.errors[resultObj.errors.length - 1],
            createDiagnosticForNode(
              node,
              Diagnostics.Did_you_mean_to_mark_this_function_as_async
            )
          );
        }
        return true;
      }
    }
    return false;
  }
  function getBestMatchIndexedAccessTypeOrUndefined(source, target, nameType) {
    const idx ***REMOVED*** getIndexedAccessTypeOrUndefined(target, nameType);
    if (idx) {
      return idx;
    }
    if (target.flags & 1048576 /* Union */) {
      const best ***REMOVED*** getBestMatchingType(source, target);
      if (best) {
        return getIndexedAccessTypeOrUndefined(best, nameType);
      }
    }
  }
  function checkExpressionForMutableLocationWithContextualType(next, sourcePropType) {
    pushContextualType(
      next,
      sourcePropType,
      /*isCache*/
      false
    );
    const result ***REMOVED*** checkExpressionForMutableLocation(next, 1 /* Contextual */);
    popContextualType();
    return result;
  }
  function elaborateElementwise(iterator, source, target, relation, containingMessageChain, errorOutputContainer) {
    let reportedError ***REMOVED*** false;
    for (const value of iterator) {
      const { errorNode: prop, innerExpression: next, nameType, errorMessage } ***REMOVED*** value;
      let targetPropType ***REMOVED*** getBestMatchIndexedAccessTypeOrUndefined(source, target, nameType);
      if (!targetPropType || targetPropType.flags & 8388608 /* IndexedAccess */) continue;
      let sourcePropType ***REMOVED*** getIndexedAccessTypeOrUndefined(source, nameType);
      if (!sourcePropType) continue;
      const propName ***REMOVED*** getPropertyNameFromIndex(
        nameType,
        /*accessNode*/
        void 0
      );
      if (!checkTypeRelatedTo(
        sourcePropType,
        targetPropType,
        relation,
        /*errorNode*/
        void 0
      )) {
        const elaborated ***REMOVED*** next && elaborateError(
          next,
          sourcePropType,
          targetPropType,
          relation,
          /*headMessage*/
          void 0,
          containingMessageChain,
          errorOutputContainer
        );
        reportedError ***REMOVED*** true;
        if (!elaborated) {
          const resultObj ***REMOVED*** errorOutputContainer || {};
          const specificSource ***REMOVED*** next ? checkExpressionForMutableLocationWithContextualType(next, sourcePropType) : sourcePropType;
          if (exactOptionalPropertyTypes && isExactOptionalPropertyMismatch(specificSource, targetPropType)) {
            const diag2 ***REMOVED*** createDiagnosticForNode(prop, Diagnostics.Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_type_of_the_target, typeToString(specificSource), typeToString(targetPropType));
            diagnostics.add(diag2);
            resultObj.errors ***REMOVED*** [diag2];
          } else {
            const targetIsOptional ***REMOVED*** !!(propName && (getPropertyOfType(target, propName) || unknownSymbol).flags & 16777216 /* Optional */);
            const sourceIsOptional ***REMOVED*** !!(propName && (getPropertyOfType(source, propName) || unknownSymbol).flags & 16777216 /* Optional */);
            targetPropType ***REMOVED*** removeMissingType(targetPropType, targetIsOptional);
            sourcePropType ***REMOVED*** removeMissingType(sourcePropType, targetIsOptional && sourceIsOptional);
            const result ***REMOVED*** checkTypeRelatedTo(specificSource, targetPropType, relation, prop, errorMessage, containingMessageChain, resultObj);
            if (result && specificSource !***REMOVED******REMOVED*** sourcePropType) {
              checkTypeRelatedTo(sourcePropType, targetPropType, relation, prop, errorMessage, containingMessageChain, resultObj);
            }
          }
          if (resultObj.errors) {
            const reportedDiag ***REMOVED*** resultObj.errors[resultObj.errors.length - 1];
            const propertyName ***REMOVED*** isTypeUsableAsPropertyName(nameType) ? getPropertyNameFromType(nameType) : void 0;
            const targetProp ***REMOVED*** propertyName !***REMOVED******REMOVED*** void 0 ? getPropertyOfType(target, propertyName) : void 0;
            let issuedElaboration ***REMOVED*** false;
            if (!targetProp) {
              const indexInfo ***REMOVED*** getApplicableIndexInfo(target, nameType);
              if (indexInfo && indexInfo.declaration && !getSourceFileOfNode(indexInfo.declaration).hasNoDefaultLib) {
                issuedElaboration ***REMOVED*** true;
                addRelatedInfo(reportedDiag, createDiagnosticForNode(indexInfo.declaration, Diagnostics.The_expected_type_comes_from_this_index_signature));
              }
            }
            if (!issuedElaboration && (targetProp && length(targetProp.declarations) || target.symbol && length(target.symbol.declarations))) {
              const targetNode ***REMOVED*** targetProp && length(targetProp.declarations) ? targetProp.declarations[0] : target.symbol.declarations[0];
              if (!getSourceFileOfNode(targetNode).hasNoDefaultLib) {
                addRelatedInfo(
                  reportedDiag,
                  createDiagnosticForNode(
                    targetNode,
                    Diagnostics.The_expected_type_comes_from_property_0_which_is_declared_here_on_type_1,
                    propertyName && !(nameType.flags & 8192 /* UniqueESSymbol */) ? unescapeLeadingUnderscores(propertyName) : typeToString(nameType),
                    typeToString(target)
                  )
                );
              }
            }
          }
        }
      }
    }
    return reportedError;
  }
  function elaborateIterableOrArrayLikeTargetElementwise(iterator, source, target, relation, containingMessageChain, errorOutputContainer) {
    const tupleOrArrayLikeTargetParts ***REMOVED*** filterType(target, isArrayOrTupleLikeType);
    const nonTupleOrArrayLikeTargetParts ***REMOVED*** filterType(target, (t) ***REMOVED***> !isArrayOrTupleLikeType(t));
    const iterationType ***REMOVED*** nonTupleOrArrayLikeTargetParts !***REMOVED******REMOVED*** neverType ? getIterationTypeOfIterable(
      13 /* ForOf */,
      0 /* Yield */,
      nonTupleOrArrayLikeTargetParts,
      /*errorNode*/
      void 0
    ) : void 0;
    let reportedError ***REMOVED*** false;
    for (let status ***REMOVED*** iterator.next(); !status.done; status ***REMOVED*** iterator.next()) {
      const { errorNode: prop, innerExpression: next, nameType, errorMessage } ***REMOVED*** status.value;
      let targetPropType ***REMOVED*** iterationType;
      const targetIndexedPropType ***REMOVED*** tupleOrArrayLikeTargetParts !***REMOVED******REMOVED*** neverType ? getBestMatchIndexedAccessTypeOrUndefined(source, tupleOrArrayLikeTargetParts, nameType) : void 0;
      if (targetIndexedPropType && !(targetIndexedPropType.flags & 8388608 /* IndexedAccess */)) {
        targetPropType ***REMOVED*** iterationType ? getUnionType([iterationType, targetIndexedPropType]) : targetIndexedPropType;
      }
      if (!targetPropType) continue;
      let sourcePropType ***REMOVED*** getIndexedAccessTypeOrUndefined(source, nameType);
      if (!sourcePropType) continue;
      const propName ***REMOVED*** getPropertyNameFromIndex(
        nameType,
        /*accessNode*/
        void 0
      );
      if (!checkTypeRelatedTo(
        sourcePropType,
        targetPropType,
        relation,
        /*errorNode*/
        void 0
      )) {
        const elaborated ***REMOVED*** next && elaborateError(
          next,
          sourcePropType,
          targetPropType,
          relation,
          /*headMessage*/
          void 0,
          containingMessageChain,
          errorOutputContainer
        );
        reportedError ***REMOVED*** true;
        if (!elaborated) {
          const resultObj ***REMOVED*** errorOutputContainer || {};
          const specificSource ***REMOVED*** next ? checkExpressionForMutableLocationWithContextualType(next, sourcePropType) : sourcePropType;
          if (exactOptionalPropertyTypes && isExactOptionalPropertyMismatch(specificSource, targetPropType)) {
            const diag2 ***REMOVED*** createDiagnosticForNode(prop, Diagnostics.Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_type_of_the_target, typeToString(specificSource), typeToString(targetPropType));
            diagnostics.add(diag2);
            resultObj.errors ***REMOVED*** [diag2];
          } else {
            const targetIsOptional ***REMOVED*** !!(propName && (getPropertyOfType(tupleOrArrayLikeTargetParts, propName) || unknownSymbol).flags & 16777216 /* Optional */);
            const sourceIsOptional ***REMOVED*** !!(propName && (getPropertyOfType(source, propName) || unknownSymbol).flags & 16777216 /* Optional */);
            targetPropType ***REMOVED*** removeMissingType(targetPropType, targetIsOptional);
            sourcePropType ***REMOVED*** removeMissingType(sourcePropType, targetIsOptional && sourceIsOptional);
            const result ***REMOVED*** checkTypeRelatedTo(specificSource, targetPropType, relation, prop, errorMessage, containingMessageChain, resultObj);
            if (result && specificSource !***REMOVED******REMOVED*** sourcePropType) {
              checkTypeRelatedTo(sourcePropType, targetPropType, relation, prop, errorMessage, containingMessageChain, resultObj);
            }
          }
        }
      }
    }
    return reportedError;
  }
  function* generateJsxAttributes(node) {
    if (!length(node.properties)) return;
    for (const prop of node.properties) {
      if (isJsxSpreadAttribute(prop) || isHyphenatedJsxName(getTextOfJsxAttributeName(prop.name))) continue;
      yield { errorNode: prop.name, innerExpression: prop.initializer, nameType: getStringLiteralType(getTextOfJsxAttributeName(prop.name)) };
    }
  }
  function* generateJsxChildren(node, getInvalidTextDiagnostic) {
    if (!length(node.children)) return;
    let memberOffset ***REMOVED*** 0;
    for (let i ***REMOVED*** 0; i < node.children.length; i++) {
      const child ***REMOVED*** node.children[i];
      const nameType ***REMOVED*** getNumberLiteralType(i - memberOffset);
      const elem ***REMOVED*** getElaborationElementForJsxChild(child, nameType, getInvalidTextDiagnostic);
      if (elem) {
        yield elem;
      } else {
        memberOffset++;
      }
    }
  }
  function getElaborationElementForJsxChild(child, nameType, getInvalidTextDiagnostic) {
    switch (child.kind) {
      case 295 /* JsxExpression */:
        return { errorNode: child, innerExpression: child.expression, nameType };
      case 12 /* JsxText */:
        if (child.containsOnlyTriviaWhiteSpaces) {
          break;
        }
        return { errorNode: child, innerExpression: void 0, nameType, errorMessage: getInvalidTextDiagnostic() };
      case 285 /* JsxElement */:
      case 286 /* JsxSelfClosingElement */:
      case 289 /* JsxFragment */:
        return { errorNode: child, innerExpression: child, nameType };
      default:
        return Debug.assertNever(child, "Found invalid jsx child");
    }
  }
  function elaborateJsxComponents(node, source, target, relation, containingMessageChain, errorOutputContainer) {
    let result ***REMOVED*** elaborateElementwise(generateJsxAttributes(node), source, target, relation, containingMessageChain, errorOutputContainer);
    let invalidTextDiagnostic;
    if (isJsxOpeningElement(node.parent) && isJsxElement(node.parent.parent)) {
      const containingElement ***REMOVED*** node.parent.parent;
      const childPropName ***REMOVED*** getJsxElementChildrenPropertyName(getJsxNamespaceAt(node));
      const childrenPropName ***REMOVED*** childPropName ***REMOVED******REMOVED******REMOVED*** void 0 ? "children" : unescapeLeadingUnderscores(childPropName);
      const childrenNameType ***REMOVED*** getStringLiteralType(childrenPropName);
      const childrenTargetType ***REMOVED*** getIndexedAccessType(target, childrenNameType);
      const validChildren ***REMOVED*** getSemanticJsxChildren(containingElement.children);
      if (!length(validChildren)) {
        return result;
      }
      const moreThanOneRealChildren ***REMOVED*** length(validChildren) > 1;
      let arrayLikeTargetParts;
      let nonArrayLikeTargetParts;
      const iterableType ***REMOVED*** getGlobalIterableType(
        /*reportErrors*/
        false
      );
      if (iterableType !***REMOVED******REMOVED*** emptyGenericType) {
        const anyIterable ***REMOVED*** createIterableType(anyType);
        arrayLikeTargetParts ***REMOVED*** filterType(childrenTargetType, (t) ***REMOVED***> isTypeAssignableTo(t, anyIterable));
        nonArrayLikeTargetParts ***REMOVED*** filterType(childrenTargetType, (t) ***REMOVED***> !isTypeAssignableTo(t, anyIterable));
      } else {
        arrayLikeTargetParts ***REMOVED*** filterType(childrenTargetType, isArrayOrTupleLikeType);
        nonArrayLikeTargetParts ***REMOVED*** filterType(childrenTargetType, (t) ***REMOVED***> !isArrayOrTupleLikeType(t));
      }
      if (moreThanOneRealChildren) {
        if (arrayLikeTargetParts !***REMOVED******REMOVED*** neverType) {
          const realSource ***REMOVED*** createTupleType(checkJsxChildren(containingElement, 0 /* Normal */));
          const children ***REMOVED*** generateJsxChildren(containingElement, getInvalidTextualChildDiagnostic);
          result ***REMOVED*** elaborateIterableOrArrayLikeTargetElementwise(children, realSource, arrayLikeTargetParts, relation, containingMessageChain, errorOutputContainer) || result;
        } else if (!isTypeRelatedTo(getIndexedAccessType(source, childrenNameType), childrenTargetType, relation)) {
          result ***REMOVED*** true;
          const diag2 ***REMOVED*** error(
            containingElement.openingElement.tagName,
            Diagnostics.This_JSX_tag_s_0_prop_expects_a_single_child_of_type_1_but_multiple_children_were_provided,
            childrenPropName,
            typeToString(childrenTargetType)
          );
          if (errorOutputContainer && errorOutputContainer.skipLogging) {
            (errorOutputContainer.errors || (errorOutputContainer.errors ***REMOVED*** [])).push(diag2);
          }
        }
      } else {
        if (nonArrayLikeTargetParts !***REMOVED******REMOVED*** neverType) {
          const child ***REMOVED*** validChildren[0];
          const elem ***REMOVED*** getElaborationElementForJsxChild(child, childrenNameType, getInvalidTextualChildDiagnostic);
          if (elem) {
            result ***REMOVED*** elaborateElementwise(
              function* () {
                yield elem;
              }(),
              source,
              target,
              relation,
              containingMessageChain,
              errorOutputContainer
            ) || result;
          }
        } else if (!isTypeRelatedTo(getIndexedAccessType(source, childrenNameType), childrenTargetType, relation)) {
          result ***REMOVED*** true;
          const diag2 ***REMOVED*** error(
            containingElement.openingElement.tagName,
            Diagnostics.This_JSX_tag_s_0_prop_expects_type_1_which_requires_multiple_children_but_only_a_single_child_was_provided,
            childrenPropName,
            typeToString(childrenTargetType)
          );
          if (errorOutputContainer && errorOutputContainer.skipLogging) {
            (errorOutputContainer.errors || (errorOutputContainer.errors ***REMOVED*** [])).push(diag2);
          }
        }
      }
    }
    return result;
    function getInvalidTextualChildDiagnostic() {
      if (!invalidTextDiagnostic) {
        const tagNameText ***REMOVED*** getTextOfNode(node.parent.tagName);
        const childPropName ***REMOVED*** getJsxElementChildrenPropertyName(getJsxNamespaceAt(node));
        const childrenPropName ***REMOVED*** childPropName ***REMOVED******REMOVED******REMOVED*** void 0 ? "children" : unescapeLeadingUnderscores(childPropName);
        const childrenTargetType ***REMOVED*** getIndexedAccessType(target, getStringLiteralType(childrenPropName));
        const diagnostic ***REMOVED*** Diagnostics._0_components_don_t_accept_text_as_child_elements_Text_in_JSX_has_the_type_string_but_the_expected_type_of_1_is_2;
        invalidTextDiagnostic ***REMOVED*** { ...diagnostic, key: "!!ALREADY FORMATTED!!", message: formatMessage(diagnostic, tagNameText, childrenPropName, typeToString(childrenTargetType)) };
      }
      return invalidTextDiagnostic;
    }
  }
  function* generateLimitedTupleElements(node, target) {
    const len ***REMOVED*** length(node.elements);
    if (!len) return;
    for (let i ***REMOVED*** 0; i < len; i++) {
      if (isTupleLikeType(target) && !getPropertyOfType(target, "" + i)) continue;
      const elem ***REMOVED*** node.elements[i];
      if (isOmittedExpression(elem)) continue;
      const nameType ***REMOVED*** getNumberLiteralType(i);
      const checkNode ***REMOVED*** getEffectiveCheckNode(elem);
      yield { errorNode: checkNode, innerExpression: checkNode, nameType };
    }
  }
  function elaborateArrayLiteral(node, source, target, relation, containingMessageChain, errorOutputContainer) {
    if (target.flags & (402784252 /* Primitive */ | 131072 /* Never */)) return false;
    if (isTupleLikeType(source)) {
      return elaborateElementwise(generateLimitedTupleElements(node, target), source, target, relation, containingMessageChain, errorOutputContainer);
    }
    pushContextualType(
      node,
      target,
      /*isCache*/
      false
    );
    const tupleizedType ***REMOVED*** checkArrayLiteral(
      node,
      1 /* Contextual */,
      /*forceTuple*/
      true
    );
    popContextualType();
    if (isTupleLikeType(tupleizedType)) {
      return elaborateElementwise(generateLimitedTupleElements(node, target), tupleizedType, target, relation, containingMessageChain, errorOutputContainer);
    }
    return false;
  }
  function* generateObjectLiteralElements(node) {
    if (!length(node.properties)) return;
    for (const prop of node.properties) {
      if (isSpreadAssignment(prop)) continue;
      const type ***REMOVED*** getLiteralTypeFromProperty(getSymbolOfDeclaration(prop), 8576 /* StringOrNumberLiteralOrUnique */);
      if (!type || type.flags & 131072 /* Never */) {
        continue;
      }
      switch (prop.kind) {
        case 179 /* SetAccessor */:
        case 178 /* GetAccessor */:
        case 175 /* MethodDeclaration */:
        case 305 /* ShorthandPropertyAssignment */:
          yield { errorNode: prop.name, innerExpression: void 0, nameType: type };
          break;
        case 304 /* PropertyAssignment */:
          yield { errorNode: prop.name, innerExpression: prop.initializer, nameType: type, errorMessage: isComputedNonLiteralName(prop.name) ? Diagnostics.Type_of_computed_property_s_value_is_0_which_is_not_assignable_to_type_1 : void 0 };
          break;
        default:
          Debug.assertNever(prop);
      }
    }
  }
  function elaborateObjectLiteral(node, source, target, relation, containingMessageChain, errorOutputContainer) {
    if (target.flags & (402784252 /* Primitive */ | 131072 /* Never */)) return false;
    return elaborateElementwise(generateObjectLiteralElements(node), source, target, relation, containingMessageChain, errorOutputContainer);
  }
  function checkTypeComparableTo(source, target, errorNode, headMessage, containingMessageChain) {
    return checkTypeRelatedTo(source, target, comparableRelation, errorNode, headMessage, containingMessageChain);
  }
  function isSignatureAssignableTo(source, target, ignoreReturnTypes) {
    return compareSignaturesRelated(
      source,
      target,
      ignoreReturnTypes ? 4 /* IgnoreReturnTypes */ : 0 /* None */,
      /*reportErrors*/
      false,
      /*errorReporter*/
      void 0,
      /*incompatibleErrorReporter*/
      void 0,
      compareTypesAssignable,
      /*reportUnreliableMarkers*/
      void 0
    ) !***REMOVED******REMOVED*** 0 /* False */;
  }
  function isTopSignature(s) {
    if (!s.typeParameters && (!s.thisParameter || isTypeAny(getTypeOfParameter(s.thisParameter))) && s.parameters.length ***REMOVED******REMOVED******REMOVED*** 1 && signatureHasRestParameter(s)) {
      const paramType ***REMOVED*** getTypeOfParameter(s.parameters[0]);
      const restType ***REMOVED*** isArrayType(paramType) ? getTypeArguments(paramType)[0] : paramType;
      return !!(restType.flags & (1 /* Any */ | 131072 /* Never */) && getReturnTypeOfSignature(s).flags & 3 /* AnyOrUnknown */);
    }
    return false;
  }
  function compareSignaturesRelated(source, target, checkMode, reportErrors2, errorReporter, incompatibleErrorReporter, compareTypes, reportUnreliableMarkers) {
    if (source ***REMOVED******REMOVED******REMOVED*** target) {
      return -1 /* True */;
    }
    if (!(checkMode & 16 /* StrictTopSignature */ && isTopSignature(source)) && isTopSignature(target)) {
      return -1 /* True */;
    }
    if (checkMode & 16 /* StrictTopSignature */ && isTopSignature(source) && !isTopSignature(target)) {
      return 0 /* False */;
    }
    const targetCount ***REMOVED*** getParameterCount(target);
    const sourceHasMoreParameters ***REMOVED*** !hasEffectiveRestParameter(target) && (checkMode & 8 /* StrictArity */ ? hasEffectiveRestParameter(source) || getParameterCount(source) > targetCount : getMinArgumentCount(source) > targetCount);
    if (sourceHasMoreParameters) {
      if (reportErrors2 && !(checkMode & 8 /* StrictArity */)) {
        errorReporter(Diagnostics.Target_signature_provides_too_few_arguments_Expected_0_or_more_but_got_1, getMinArgumentCount(source), targetCount);
      }
      return 0 /* False */;
    }
    if (source.typeParameters && source.typeParameters !***REMOVED******REMOVED*** target.typeParameters) {
      target ***REMOVED*** getCanonicalSignature(target);
      source ***REMOVED*** instantiateSignatureInContextOf(
        source,
        target,
        /*inferenceContext*/
        void 0,
        compareTypes
      );
    }
    const sourceCount ***REMOVED*** getParameterCount(source);
    const sourceRestType ***REMOVED*** getNonArrayRestType(source);
    const targetRestType ***REMOVED*** getNonArrayRestType(target);
    if (sourceRestType || targetRestType) {
      void instantiateType(sourceRestType || targetRestType, reportUnreliableMarkers);
    }
    const kind ***REMOVED*** target.declaration ? target.declaration.kind : 0 /* Unknown */;
    const strictVariance ***REMOVED*** !(checkMode & 3 /* Callback */) && strictFunctionTypes && kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ && kind !***REMOVED******REMOVED*** 174 /* MethodSignature */ && kind !***REMOVED******REMOVED*** 177 /* Constructor */;
    let result ***REMOVED*** -1 /* True */;
    const sourceThisType ***REMOVED*** getThisTypeOfSignature(source);
    if (sourceThisType && sourceThisType !***REMOVED******REMOVED*** voidType) {
      const targetThisType ***REMOVED*** getThisTypeOfSignature(target);
      if (targetThisType) {
        const related ***REMOVED*** !strictVariance && compareTypes(
          sourceThisType,
          targetThisType,
          /*reportErrors*/
          false
        ) || compareTypes(targetThisType, sourceThisType, reportErrors2);
        if (!related) {
          if (reportErrors2) {
            errorReporter(Diagnostics.The_this_types_of_each_signature_are_incompatible);
          }
          return 0 /* False */;
        }
        result &***REMOVED*** related;
      }
    }
    const paramCount ***REMOVED*** sourceRestType || targetRestType ? Math.min(sourceCount, targetCount) : Math.max(sourceCount, targetCount);
    const restIndex ***REMOVED*** sourceRestType || targetRestType ? paramCount - 1 : -1;
    for (let i ***REMOVED*** 0; i < paramCount; i++) {
      const sourceType ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** restIndex ? getRestOrAnyTypeAtPosition(source, i) : tryGetTypeAtPosition(source, i);
      const targetType ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** restIndex ? getRestOrAnyTypeAtPosition(target, i) : tryGetTypeAtPosition(target, i);
      if (sourceType && targetType && (sourceType !***REMOVED******REMOVED*** targetType || checkMode & 8 /* StrictArity */)) {
        const sourceSig ***REMOVED*** checkMode & 3 /* Callback */ || isInstantiatedGenericParameter(source, i) ? void 0 : getSingleCallSignature(getNonNullableType(sourceType));
        const targetSig ***REMOVED*** checkMode & 3 /* Callback */ || isInstantiatedGenericParameter(target, i) ? void 0 : getSingleCallSignature(getNonNullableType(targetType));
        const callbacks ***REMOVED*** sourceSig && targetSig && !getTypePredicateOfSignature(sourceSig) && !getTypePredicateOfSignature(targetSig) && getTypeFacts(sourceType, 50331648 /* IsUndefinedOrNull */) ***REMOVED******REMOVED******REMOVED*** getTypeFacts(targetType, 50331648 /* IsUndefinedOrNull */);
        let related ***REMOVED*** callbacks ? compareSignaturesRelated(targetSig, sourceSig, checkMode & 8 /* StrictArity */ | (strictVariance ? 2 /* StrictCallback */ : 1 /* BivariantCallback */), reportErrors2, errorReporter, incompatibleErrorReporter, compareTypes, reportUnreliableMarkers) : !(checkMode & 3 /* Callback */) && !strictVariance && compareTypes(
          sourceType,
          targetType,
          /*reportErrors*/
          false
        ) || compareTypes(targetType, sourceType, reportErrors2);
        if (related && checkMode & 8 /* StrictArity */ && i >***REMOVED*** getMinArgumentCount(source) && i < getMinArgumentCount(target) && compareTypes(
          sourceType,
          targetType,
          /*reportErrors*/
          false
        )) {
          related ***REMOVED*** 0 /* False */;
        }
        if (!related) {
          if (reportErrors2) {
            errorReporter(Diagnostics.Types_of_parameters_0_and_1_are_incompatible, unescapeLeadingUnderscores(getParameterNameAtPosition(source, i)), unescapeLeadingUnderscores(getParameterNameAtPosition(target, i)));
          }
          return 0 /* False */;
        }
        result &***REMOVED*** related;
      }
    }
    if (!(checkMode & 4 /* IgnoreReturnTypes */)) {
      const targetReturnType ***REMOVED*** isResolvingReturnTypeOfSignature(target) ? anyType : target.declaration && isJSConstructor(target.declaration) ? getDeclaredTypeOfClassOrInterface(getMergedSymbol(target.declaration.symbol)) : getReturnTypeOfSignature(target);
      if (targetReturnType ***REMOVED******REMOVED******REMOVED*** voidType || targetReturnType ***REMOVED******REMOVED******REMOVED*** anyType) {
        return result;
      }
      const sourceReturnType ***REMOVED*** isResolvingReturnTypeOfSignature(source) ? anyType : source.declaration && isJSConstructor(source.declaration) ? getDeclaredTypeOfClassOrInterface(getMergedSymbol(source.declaration.symbol)) : getReturnTypeOfSignature(source);
      const targetTypePredicate ***REMOVED*** getTypePredicateOfSignature(target);
      if (targetTypePredicate) {
        const sourceTypePredicate ***REMOVED*** getTypePredicateOfSignature(source);
        if (sourceTypePredicate) {
          result &***REMOVED*** compareTypePredicateRelatedTo(sourceTypePredicate, targetTypePredicate, reportErrors2, errorReporter, compareTypes);
        } else if (isIdentifierTypePredicate(targetTypePredicate) || isThisTypePredicate(targetTypePredicate)) {
          if (reportErrors2) {
            errorReporter(Diagnostics.Signature_0_must_be_a_type_predicate, signatureToString(source));
          }
          return 0 /* False */;
        }
      } else {
        result &***REMOVED*** checkMode & 1 /* BivariantCallback */ && compareTypes(
          targetReturnType,
          sourceReturnType,
          /*reportErrors*/
          false
        ) || compareTypes(sourceReturnType, targetReturnType, reportErrors2);
        if (!result && reportErrors2 && incompatibleErrorReporter) {
          incompatibleErrorReporter(sourceReturnType, targetReturnType);
        }
      }
    }
    return result;
  }
  function compareTypePredicateRelatedTo(source, target, reportErrors2, errorReporter, compareTypes) {
    if (source.kind !***REMOVED******REMOVED*** target.kind) {
      if (reportErrors2) {
        errorReporter(Diagnostics.A_this_based_type_guard_is_not_compatible_with_a_parameter_based_type_guard);
        errorReporter(Diagnostics.Type_predicate_0_is_not_assignable_to_1, typePredicateToString(source), typePredicateToString(target));
      }
      return 0 /* False */;
    }
    if (source.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */ || source.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */) {
      if (source.parameterIndex !***REMOVED******REMOVED*** target.parameterIndex) {
        if (reportErrors2) {
          errorReporter(Diagnostics.Parameter_0_is_not_in_the_same_position_as_parameter_1, source.parameterName, target.parameterName);
          errorReporter(Diagnostics.Type_predicate_0_is_not_assignable_to_1, typePredicateToString(source), typePredicateToString(target));
        }
        return 0 /* False */;
      }
    }
    const related ***REMOVED*** source.type ***REMOVED******REMOVED******REMOVED*** target.type ? -1 /* True */ : source.type && target.type ? compareTypes(source.type, target.type, reportErrors2) : 0 /* False */;
    if (related ***REMOVED******REMOVED******REMOVED*** 0 /* False */ && reportErrors2) {
      errorReporter(Diagnostics.Type_predicate_0_is_not_assignable_to_1, typePredicateToString(source), typePredicateToString(target));
    }
    return related;
  }
  function isImplementationCompatibleWithOverload(implementation, overload) {
    const erasedSource ***REMOVED*** getErasedSignature(implementation);
    const erasedTarget ***REMOVED*** getErasedSignature(overload);
    const sourceReturnType ***REMOVED*** getReturnTypeOfSignature(erasedSource);
    const targetReturnType ***REMOVED*** getReturnTypeOfSignature(erasedTarget);
    if (targetReturnType ***REMOVED******REMOVED******REMOVED*** voidType || isTypeRelatedTo(targetReturnType, sourceReturnType, assignableRelation) || isTypeRelatedTo(sourceReturnType, targetReturnType, assignableRelation)) {
      return isSignatureAssignableTo(
        erasedSource,
        erasedTarget,
        /*ignoreReturnTypes*/
        true
      );
    }
    return false;
  }
  function isEmptyResolvedType(t) {
    return t !***REMOVED******REMOVED*** anyFunctionType && t.properties.length ***REMOVED******REMOVED******REMOVED*** 0 && t.callSignatures.length ***REMOVED******REMOVED******REMOVED*** 0 && t.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 0 && t.indexInfos.length ***REMOVED******REMOVED******REMOVED*** 0;
  }
  function isEmptyObjectType(type) {
    return type.flags & 524288 /* Object */ ? !isGenericMappedType(type) && isEmptyResolvedType(resolveStructuredTypeMembers(type)) : type.flags & 67108864 /* NonPrimitive */ ? true : type.flags & 1048576 /* Union */ ? some(type.types, isEmptyObjectType) : type.flags & 2097152 /* Intersection */ ? every(type.types, isEmptyObjectType) : false;
  }
  function isEmptyAnonymousObjectType(type) {
    return !!(getObjectFlags(type) & 16 /* Anonymous */ && (type.members && isEmptyResolvedType(type) || type.symbol && type.symbol.flags & 2048 /* TypeLiteral */ && getMembersOfSymbol(type.symbol).size ***REMOVED******REMOVED******REMOVED*** 0));
  }
  function isUnknownLikeUnionType(type) {
    if (strictNullChecks && type.flags & 1048576 /* Union */) {
      if (!(type.objectFlags & 33554432 /* IsUnknownLikeUnionComputed */)) {
        const types ***REMOVED*** type.types;
        type.objectFlags |***REMOVED*** 33554432 /* IsUnknownLikeUnionComputed */ | (types.length >***REMOVED*** 3 && types[0].flags & 32768 /* Undefined */ && types[1].flags & 65536 /* Null */ && some(types, isEmptyAnonymousObjectType) ? 67108864 /* IsUnknownLikeUnion */ : 0);
      }
      return !!(type.objectFlags & 67108864 /* IsUnknownLikeUnion */);
    }
    return false;
  }
  function containsUndefinedType(type) {
    return !!((type.flags & 1048576 /* Union */ ? type.types[0] : type).flags & 32768 /* Undefined */);
  }
  function containsNonMissingUndefinedType(type) {
    const candidate ***REMOVED*** type.flags & 1048576 /* Union */ ? type.types[0] : type;
    return !!(candidate.flags & 32768 /* Undefined */) && candidate !***REMOVED******REMOVED*** missingType;
  }
  function isStringIndexSignatureOnlyType(type) {
    return type.flags & 524288 /* Object */ && !isGenericMappedType(type) && getPropertiesOfType(type).length ***REMOVED******REMOVED******REMOVED*** 0 && getIndexInfosOfType(type).length ***REMOVED******REMOVED******REMOVED*** 1 && !!getIndexInfoOfType(type, stringType) || type.flags & 3145728 /* UnionOrIntersection */ && every(type.types, isStringIndexSignatureOnlyType) || false;
  }
  function isEnumTypeRelatedTo(source, target, errorReporter) {
    const sourceSymbol ***REMOVED*** source.flags & 8 /* EnumMember */ ? getParentOfSymbol(source) : source;
    const targetSymbol ***REMOVED*** target.flags & 8 /* EnumMember */ ? getParentOfSymbol(target) : target;
    if (sourceSymbol ***REMOVED******REMOVED******REMOVED*** targetSymbol) {
      return true;
    }
    if (sourceSymbol.escapedName !***REMOVED******REMOVED*** targetSymbol.escapedName || !(sourceSymbol.flags & 256 /* RegularEnum */) || !(targetSymbol.flags & 256 /* RegularEnum */)) {
      return false;
    }
    const id ***REMOVED*** getSymbolId(sourceSymbol) + "," + getSymbolId(targetSymbol);
    const entry ***REMOVED*** enumRelation.get(id);
    if (entry !***REMOVED******REMOVED*** void 0 && !(entry & 2 /* Failed */ && errorReporter)) {
      return !!(entry & 1 /* Succeeded */);
    }
    const targetEnumType ***REMOVED*** getTypeOfSymbol(targetSymbol);
    for (const sourceProperty of getPropertiesOfType(getTypeOfSymbol(sourceSymbol))) {
      if (sourceProperty.flags & 8 /* EnumMember */) {
        const targetProperty ***REMOVED*** getPropertyOfType(targetEnumType, sourceProperty.escapedName);
        if (!targetProperty || !(targetProperty.flags & 8 /* EnumMember */)) {
          if (errorReporter) {
            errorReporter(Diagnostics.Property_0_is_missing_in_type_1, symbolName(sourceProperty), typeToString(
              getDeclaredTypeOfSymbol(targetSymbol),
              /*enclosingDeclaration*/
              void 0,
              64 /* UseFullyQualifiedType */
            ));
          }
          enumRelation.set(id, 2 /* Failed */);
          return false;
        }
        const sourceValue ***REMOVED*** getEnumMemberValue(getDeclarationOfKind(sourceProperty, 307 /* EnumMember */)).value;
        const targetValue ***REMOVED*** getEnumMemberValue(getDeclarationOfKind(targetProperty, 307 /* EnumMember */)).value;
        if (sourceValue !***REMOVED******REMOVED*** targetValue) {
          const sourceIsString ***REMOVED*** typeof sourceValue ***REMOVED******REMOVED******REMOVED*** "string";
          const targetIsString ***REMOVED*** typeof targetValue ***REMOVED******REMOVED******REMOVED*** "string";
          if (sourceValue !***REMOVED******REMOVED*** void 0 && targetValue !***REMOVED******REMOVED*** void 0) {
            if (errorReporter) {
              const escapedSource ***REMOVED*** sourceIsString ? `"${escapeString(sourceValue)}"` : sourceValue;
              const escapedTarget ***REMOVED*** targetIsString ? `"${escapeString(targetValue)}"` : targetValue;
              errorReporter(Diagnostics.Each_declaration_of_0_1_differs_in_its_value_where_2_was_expected_but_3_was_given, symbolName(targetSymbol), symbolName(targetProperty), escapedTarget, escapedSource);
            }
            enumRelation.set(id, 2 /* Failed */);
            return false;
          }
          if (sourceIsString || targetIsString) {
            if (errorReporter) {
              const knownStringValue ***REMOVED*** sourceValue ?? targetValue;
              Debug.assert(typeof knownStringValue ***REMOVED******REMOVED******REMOVED*** "string");
              const escapedValue ***REMOVED*** `"${escapeString(knownStringValue)}"`;
              errorReporter(Diagnostics.One_value_of_0_1_is_the_string_2_and_the_other_is_assumed_to_be_an_unknown_numeric_value, symbolName(targetSymbol), symbolName(targetProperty), escapedValue);
            }
            enumRelation.set(id, 2 /* Failed */);
            return false;
          }
        }
      }
    }
    enumRelation.set(id, 1 /* Succeeded */);
    return true;
  }
  function isSimpleTypeRelatedTo(source, target, relation, errorReporter) {
    const s ***REMOVED*** source.flags;
    const t ***REMOVED*** target.flags;
    if (t & 1 /* Any */ || s & 131072 /* Never */ || source ***REMOVED******REMOVED******REMOVED*** wildcardType) return true;
    if (t & 2 /* Unknown */ && !(relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation && s & 1 /* Any */)) return true;
    if (t & 131072 /* Never */) return false;
    if (s & 402653316 /* StringLike */ && t & 4 /* String */) return true;
    if (s & 128 /* StringLiteral */ && s & 1024 /* EnumLiteral */ && t & 128 /* StringLiteral */ && !(t & 1024 /* EnumLiteral */) && source.value ***REMOVED******REMOVED******REMOVED*** target.value) return true;
    if (s & 296 /* NumberLike */ && t & 8 /* Number */) return true;
    if (s & 256 /* NumberLiteral */ && s & 1024 /* EnumLiteral */ && t & 256 /* NumberLiteral */ && !(t & 1024 /* EnumLiteral */) && source.value ***REMOVED******REMOVED******REMOVED*** target.value) return true;
    if (s & 2112 /* BigIntLike */ && t & 64 /* BigInt */) return true;
    if (s & 528 /* BooleanLike */ && t & 16 /* Boolean */) return true;
    if (s & 12288 /* ESSymbolLike */ && t & 4096 /* ESSymbol */) return true;
    if (s & 32 /* Enum */ && t & 32 /* Enum */ && source.symbol.escapedName ***REMOVED******REMOVED******REMOVED*** target.symbol.escapedName && isEnumTypeRelatedTo(source.symbol, target.symbol, errorReporter)) return true;
    if (s & 1024 /* EnumLiteral */ && t & 1024 /* EnumLiteral */) {
      if (s & 1048576 /* Union */ && t & 1048576 /* Union */ && isEnumTypeRelatedTo(source.symbol, target.symbol, errorReporter)) return true;
      if (s & 2944 /* Literal */ && t & 2944 /* Literal */ && source.value ***REMOVED******REMOVED******REMOVED*** target.value && isEnumTypeRelatedTo(source.symbol, target.symbol, errorReporter)) return true;
    }
    if (s & 32768 /* Undefined */ && (!strictNullChecks && !(t & 3145728 /* UnionOrIntersection */) || t & (32768 /* Undefined */ | 16384 /* Void */))) return true;
    if (s & 65536 /* Null */ && (!strictNullChecks && !(t & 3145728 /* UnionOrIntersection */) || t & 65536 /* Null */)) return true;
    if (s & 524288 /* Object */ && t & 67108864 /* NonPrimitive */ && !(relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation && isEmptyAnonymousObjectType(source) && !(getObjectFlags(source) & 8192 /* FreshLiteral */))) return true;
    if (relation ***REMOVED******REMOVED******REMOVED*** assignableRelation || relation ***REMOVED******REMOVED******REMOVED*** comparableRelation) {
      if (s & 1 /* Any */) return true;
      if (s & 8 /* Number */ && (t & 32 /* Enum */ || t & 256 /* NumberLiteral */ && t & 1024 /* EnumLiteral */)) return true;
      if (s & 256 /* NumberLiteral */ && !(s & 1024 /* EnumLiteral */) && (t & 32 /* Enum */ || t & 256 /* NumberLiteral */ && t & 1024 /* EnumLiteral */ && source.value ***REMOVED******REMOVED******REMOVED*** target.value)) return true;
      if (isUnknownLikeUnionType(target)) return true;
    }
    return false;
  }
  function isTypeRelatedTo(source, target, relation) {
    if (isFreshLiteralType(source)) {
      source ***REMOVED*** source.regularType;
    }
    if (isFreshLiteralType(target)) {
      target ***REMOVED*** target.regularType;
    }
    if (source ***REMOVED******REMOVED******REMOVED*** target) {
      return true;
    }
    if (relation !***REMOVED******REMOVED*** identityRelation) {
      if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation && !(target.flags & 131072 /* Never */) && isSimpleTypeRelatedTo(target, source, relation) || isSimpleTypeRelatedTo(source, target, relation)) {
        return true;
      }
    } else if (!((source.flags | target.flags) & (3145728 /* UnionOrIntersection */ | 8388608 /* IndexedAccess */ | 16777216 /* Conditional */ | 33554432 /* Substitution */))) {
      if (source.flags !***REMOVED******REMOVED*** target.flags) return false;
      if (source.flags & 67358815 /* Singleton */) return true;
    }
    if (source.flags & 524288 /* Object */ && target.flags & 524288 /* Object */) {
      const related ***REMOVED*** relation.get(getRelationKey(
        source,
        target,
        0 /* None */,
        relation,
        /*ignoreConstraints*/
        false
      ));
      if (related !***REMOVED******REMOVED*** void 0) {
        return !!(related & 1 /* Succeeded */);
      }
    }
    if (source.flags & 469499904 /* StructuredOrInstantiable */ || target.flags & 469499904 /* StructuredOrInstantiable */) {
      return checkTypeRelatedTo(
        source,
        target,
        relation,
        /*errorNode*/
        void 0
      );
    }
    return false;
  }
  function isIgnoredJsxProperty(source, sourceProp) {
    return getObjectFlags(source) & 2048 /* JsxAttributes */ && isHyphenatedJsxName(sourceProp.escapedName);
  }
  function getNormalizedType(type, writing) {
    while (true) {
      const t ***REMOVED*** isFreshLiteralType(type) ? type.regularType : isGenericTupleType(type) ? getNormalizedTupleType(type, writing) : getObjectFlags(type) & 4 /* Reference */ ? type.node ? createTypeReference(type.target, getTypeArguments(type)) : getSingleBaseForNonAugmentingSubtype(type) || type : type.flags & 3145728 /* UnionOrIntersection */ ? getNormalizedUnionOrIntersectionType(type, writing) : type.flags & 33554432 /* Substitution */ ? writing ? type.baseType : getSubstitutionIntersection(type) : type.flags & 25165824 /* Simplifiable */ ? getSimplifiedType(type, writing) : type;
      if (t ***REMOVED******REMOVED******REMOVED*** type) return t;
      type ***REMOVED*** t;
    }
  }
  function getNormalizedUnionOrIntersectionType(type, writing) {
    const reduced ***REMOVED*** getReducedType(type);
    if (reduced !***REMOVED******REMOVED*** type) {
      return reduced;
    }
    if (type.flags & 2097152 /* Intersection */ && shouldNormalizeIntersection(type)) {
      const normalizedTypes ***REMOVED*** sameMap(type.types, (t) ***REMOVED***> getNormalizedType(t, writing));
      if (normalizedTypes !***REMOVED******REMOVED*** type.types) {
        return getIntersectionType(normalizedTypes);
      }
    }
    return type;
  }
  function shouldNormalizeIntersection(type) {
    let hasInstantiable ***REMOVED*** false;
    let hasNullableOrEmpty ***REMOVED*** false;
    for (const t of type.types) {
      hasInstantiable || (hasInstantiable ***REMOVED*** !!(t.flags & 465829888 /* Instantiable */));
      hasNullableOrEmpty || (hasNullableOrEmpty ***REMOVED*** !!(t.flags & 98304 /* Nullable */) || isEmptyAnonymousObjectType(t));
      if (hasInstantiable && hasNullableOrEmpty) return true;
    }
    return false;
  }
  function getNormalizedTupleType(type, writing) {
    const elements ***REMOVED*** getElementTypes(type);
    const normalizedElements ***REMOVED*** sameMap(elements, (t) ***REMOVED***> t.flags & 25165824 /* Simplifiable */ ? getSimplifiedType(t, writing) : t);
    return elements !***REMOVED******REMOVED*** normalizedElements ? createNormalizedTupleType(type.target, normalizedElements) : type;
  }
  function checkTypeRelatedTo(source, target, relation, errorNode, headMessage, containingMessageChain, errorOutputContainer) {
    var _a;
    let errorInfo;
    let relatedInfo;
    let maybeKeys;
    let maybeKeysSet;
    let sourceStack;
    let targetStack;
    let maybeCount ***REMOVED*** 0;
    let sourceDepth ***REMOVED*** 0;
    let targetDepth ***REMOVED*** 0;
    let expandingFlags ***REMOVED*** 0 /* None */;
    let overflow ***REMOVED*** false;
    let overrideNextErrorInfo ***REMOVED*** 0;
    let skipParentCounter ***REMOVED*** 0;
    let lastSkippedInfo;
    let incompatibleStack;
    let relationCount ***REMOVED*** 16e6 - relation.size >> 3;
    Debug.assert(relation !***REMOVED******REMOVED*** identityRelation || !errorNode, "no error reporting in identity checking");
    const result ***REMOVED*** isRelatedTo(
      source,
      target,
      3 /* Both */,
      /*reportErrors*/
      !!errorNode,
      headMessage
    );
    if (incompatibleStack) {
      reportIncompatibleStack();
    }
    if (overflow) {
      const id ***REMOVED*** getRelationKey(
        source,
        target,
        /*intersectionState*/
        0 /* None */,
        relation,
        /*ignoreConstraints*/
        false
      );
      relation.set(id, 2 /* Failed */ | (relationCount <***REMOVED*** 0 ? 32 /* ComplexityOverflow */ : 64 /* StackDepthOverflow */));
      (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.instant(tracing.Phase.CheckTypes, "checkTypeRelatedTo_DepthLimit", { sourceId: source.id, targetId: target.id, depth: sourceDepth, targetDepth });
      const message ***REMOVED*** relationCount <***REMOVED*** 0 ? Diagnostics.Excessive_complexity_comparing_types_0_and_1 : Diagnostics.Excessive_stack_depth_comparing_types_0_and_1;
      const diag2 ***REMOVED*** error(errorNode || currentNode, message, typeToString(source), typeToString(target));
      if (errorOutputContainer) {
        (errorOutputContainer.errors || (errorOutputContainer.errors ***REMOVED*** [])).push(diag2);
      }
    } else if (errorInfo) {
      if (containingMessageChain) {
        const chain ***REMOVED*** containingMessageChain();
        if (chain) {
          concatenateDiagnosticMessageChains(chain, errorInfo);
          errorInfo ***REMOVED*** chain;
        }
      }
      let relatedInformation;
      if (headMessage && errorNode && !result && source.symbol) {
        const links ***REMOVED*** getSymbolLinks(source.symbol);
        if (links.originatingImport && !isImportCall(links.originatingImport)) {
          const helpfulRetry ***REMOVED*** checkTypeRelatedTo(
            getTypeOfSymbol(links.target),
            target,
            relation,
            /*errorNode*/
            void 0
          );
          if (helpfulRetry) {
            const diag3 ***REMOVED*** createDiagnosticForNode(links.originatingImport, Diagnostics.Type_originates_at_this_import_A_namespace_style_import_cannot_be_called_or_constructed_and_will_cause_a_failure_at_runtime_Consider_using_a_default_import_or_import_require_here_instead);
            relatedInformation ***REMOVED*** append(relatedInformation, diag3);
          }
        }
      }
      const diag2 ***REMOVED*** createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(errorNode), errorNode, errorInfo, relatedInformation);
      if (relatedInfo) {
        addRelatedInfo(diag2, ...relatedInfo);
      }
      if (errorOutputContainer) {
        (errorOutputContainer.errors || (errorOutputContainer.errors ***REMOVED*** [])).push(diag2);
      }
      if (!errorOutputContainer || !errorOutputContainer.skipLogging) {
        diagnostics.add(diag2);
      }
    }
    if (errorNode && errorOutputContainer && errorOutputContainer.skipLogging && result ***REMOVED******REMOVED******REMOVED*** 0 /* False */) {
      Debug.assert(!!errorOutputContainer.errors, "missed opportunity to interact with error.");
    }
    return result !***REMOVED******REMOVED*** 0 /* False */;
    function resetErrorInfo(saved) {
      errorInfo ***REMOVED*** saved.errorInfo;
      lastSkippedInfo ***REMOVED*** saved.lastSkippedInfo;
      incompatibleStack ***REMOVED*** saved.incompatibleStack;
      overrideNextErrorInfo ***REMOVED*** saved.overrideNextErrorInfo;
      skipParentCounter ***REMOVED*** saved.skipParentCounter;
      relatedInfo ***REMOVED*** saved.relatedInfo;
    }
    function captureErrorCalculationState() {
      return {
        errorInfo,
        lastSkippedInfo,
        incompatibleStack: incompatibleStack ***REMOVED******REMOVED*** null ? void 0 : incompatibleStack.slice(),
        overrideNextErrorInfo,
        skipParentCounter,
        relatedInfo: relatedInfo ***REMOVED******REMOVED*** null ? void 0 : relatedInfo.slice()
      };
    }
    function reportIncompatibleError(message, ...args) {
      overrideNextErrorInfo++;
      lastSkippedInfo ***REMOVED*** void 0;
      (incompatibleStack || (incompatibleStack ***REMOVED*** [])).push([message, ...args]);
    }
    function reportIncompatibleStack() {
      const stack ***REMOVED*** incompatibleStack || [];
      incompatibleStack ***REMOVED*** void 0;
      const info ***REMOVED*** lastSkippedInfo;
      lastSkippedInfo ***REMOVED*** void 0;
      if (stack.length ***REMOVED******REMOVED******REMOVED*** 1) {
        reportError(...stack[0]);
        if (info) {
          reportRelationError(
            /*message*/
            void 0,
            ...info
          );
        }
        return;
      }
      let path ***REMOVED*** "";
      const secondaryRootErrors ***REMOVED*** [];
      while (stack.length) {
        const [msg, ...args] ***REMOVED*** stack.pop();
        switch (msg.code) {
          case Diagnostics.Types_of_property_0_are_incompatible.code: {
            if (path.indexOf("new ") ***REMOVED******REMOVED******REMOVED*** 0) {
              path ***REMOVED*** `(${path})`;
            }
            const str ***REMOVED*** "" + args[0];
            if (path.length ***REMOVED******REMOVED******REMOVED*** 0) {
              path ***REMOVED*** `${str}`;
            } else if (isIdentifierText(str, getEmitScriptTarget(compilerOptions))) {
              path ***REMOVED*** `${path}.${str}`;
            } else if (str[0] ***REMOVED******REMOVED******REMOVED*** "[" && str[str.length - 1] ***REMOVED******REMOVED******REMOVED*** "]") {
              path ***REMOVED*** `${path}${str}`;
            } else {
              path ***REMOVED*** `${path}[${str}]`;
            }
            break;
          }
          case Diagnostics.Call_signature_return_types_0_and_1_are_incompatible.code:
          case Diagnostics.Construct_signature_return_types_0_and_1_are_incompatible.code:
          case Diagnostics.Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code:
          case Diagnostics.Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code: {
            if (path.length ***REMOVED******REMOVED******REMOVED*** 0) {
              let mappedMsg ***REMOVED*** msg;
              if (msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code) {
                mappedMsg ***REMOVED*** Diagnostics.Call_signature_return_types_0_and_1_are_incompatible;
              } else if (msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code) {
                mappedMsg ***REMOVED*** Diagnostics.Construct_signature_return_types_0_and_1_are_incompatible;
              }
              secondaryRootErrors.unshift([mappedMsg, args[0], args[1]]);
            } else {
              const prefix ***REMOVED*** msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Construct_signature_return_types_0_and_1_are_incompatible.code || msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code ? "new " : "";
              const params ***REMOVED*** msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code || msg.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1.code ? "" : "...";
              path ***REMOVED*** `${prefix}${path}(${params})`;
            }
            break;
          }
          case Diagnostics.Type_at_position_0_in_source_is_not_compatible_with_type_at_position_1_in_target.code: {
            secondaryRootErrors.unshift([Diagnostics.Type_at_position_0_in_source_is_not_compatible_with_type_at_position_1_in_target, args[0], args[1]]);
            break;
          }
          case Diagnostics.Type_at_positions_0_through_1_in_source_is_not_compatible_with_type_at_position_2_in_target.code: {
            secondaryRootErrors.unshift([Diagnostics.Type_at_positions_0_through_1_in_source_is_not_compatible_with_type_at_position_2_in_target, args[0], args[1], args[2]]);
            break;
          }
          default:
            return Debug.fail(`Unhandled Diagnostic: ${msg.code}`);
        }
      }
      if (path) {
        reportError(
          path[path.length - 1] ***REMOVED******REMOVED******REMOVED*** ")" ? Diagnostics.The_types_returned_by_0_are_incompatible_between_these_types : Diagnostics.The_types_of_0_are_incompatible_between_these_types,
          path
        );
      } else {
        secondaryRootErrors.shift();
      }
      for (const [msg, ...args] of secondaryRootErrors) {
        const originalValue ***REMOVED*** msg.elidedInCompatabilityPyramid;
        msg.elidedInCompatabilityPyramid ***REMOVED*** false;
        reportError(msg, ...args);
        msg.elidedInCompatabilityPyramid ***REMOVED*** originalValue;
      }
      if (info) {
        reportRelationError(
          /*message*/
          void 0,
          ...info
        );
      }
    }
    function reportError(message, ...args) {
      Debug.assert(!!errorNode);
      if (incompatibleStack) reportIncompatibleStack();
      if (message.elidedInCompatabilityPyramid) return;
      if (skipParentCounter ***REMOVED******REMOVED******REMOVED*** 0) {
        errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, message, ...args);
      } else {
        skipParentCounter--;
      }
    }
    function reportParentSkippedError(message, ...args) {
      reportError(message, ...args);
      skipParentCounter++;
    }
    function associateRelatedInfo(info) {
      Debug.assert(!!errorInfo);
      if (!relatedInfo) {
        relatedInfo ***REMOVED*** [info];
      } else {
        relatedInfo.push(info);
      }
    }
    function reportRelationError(message, source2, target2) {
      if (incompatibleStack) reportIncompatibleStack();
      const [sourceType, targetType] ***REMOVED*** getTypeNamesForErrorDisplay(source2, target2);
      let generalizedSource ***REMOVED*** source2;
      let generalizedSourceType ***REMOVED*** sourceType;
      if (!(target2.flags & 131072 /* Never */) && isLiteralType(source2) && !typeCouldHaveTopLevelSingletonTypes(target2)) {
        generalizedSource ***REMOVED*** getBaseTypeOfLiteralType(source2);
        Debug.assert(!isTypeAssignableTo(generalizedSource, target2), "generalized source shouldn't be assignable");
        generalizedSourceType ***REMOVED*** getTypeNameForErrorDisplay(generalizedSource);
      }
      const targetFlags ***REMOVED*** target2.flags & 8388608 /* IndexedAccess */ && !(source2.flags & 8388608 /* IndexedAccess */) ? target2.objectType.flags : target2.flags;
      if (targetFlags & 262144 /* TypeParameter */ && target2 !***REMOVED******REMOVED*** markerSuperTypeForCheck && target2 !***REMOVED******REMOVED*** markerSubTypeForCheck) {
        const constraint ***REMOVED*** getBaseConstraintOfType(target2);
        let needsOriginalSource;
        if (constraint && (isTypeAssignableTo(generalizedSource, constraint) || (needsOriginalSource ***REMOVED*** isTypeAssignableTo(source2, constraint)))) {
          reportError(
            Diagnostics._0_is_assignable_to_the_constraint_of_type_1_but_1_could_be_instantiated_with_a_different_subtype_of_constraint_2,
            needsOriginalSource ? sourceType : generalizedSourceType,
            targetType,
            typeToString(constraint)
          );
        } else {
          errorInfo ***REMOVED*** void 0;
          reportError(
            Diagnostics._0_could_be_instantiated_with_an_arbitrary_type_which_could_be_unrelated_to_1,
            targetType,
            generalizedSourceType
          );
        }
      }
      if (!message) {
        if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation) {
          message ***REMOVED*** Diagnostics.Type_0_is_not_comparable_to_type_1;
        } else if (sourceType ***REMOVED******REMOVED******REMOVED*** targetType) {
          message ***REMOVED*** Diagnostics.Type_0_is_not_assignable_to_type_1_Two_different_types_with_this_name_exist_but_they_are_unrelated;
        } else if (exactOptionalPropertyTypes && getExactOptionalUnassignableProperties(source2, target2).length) {
          message ***REMOVED*** Diagnostics.Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_types_of_the_target_s_properties;
        } else {
          if (source2.flags & 128 /* StringLiteral */ && target2.flags & 1048576 /* Union */) {
            const suggestedType ***REMOVED*** getSuggestedTypeForNonexistentStringLiteralType(source2, target2);
            if (suggestedType) {
              reportError(Diagnostics.Type_0_is_not_assignable_to_type_1_Did_you_mean_2, generalizedSourceType, targetType, typeToString(suggestedType));
              return;
            }
          }
          message ***REMOVED*** Diagnostics.Type_0_is_not_assignable_to_type_1;
        }
      } else if (message ***REMOVED******REMOVED******REMOVED*** Diagnostics.Argument_of_type_0_is_not_assignable_to_parameter_of_type_1 && exactOptionalPropertyTypes && getExactOptionalUnassignableProperties(source2, target2).length) {
        message ***REMOVED*** Diagnostics.Argument_of_type_0_is_not_assignable_to_parameter_of_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_types_of_the_target_s_properties;
      }
      reportError(message, generalizedSourceType, targetType);
    }
    function tryElaborateErrorsForPrimitivesAndObjects(source2, target2) {
      const sourceType ***REMOVED*** symbolValueDeclarationIsContextSensitive(source2.symbol) ? typeToString(source2, source2.symbol.valueDeclaration) : typeToString(source2);
      const targetType ***REMOVED*** symbolValueDeclarationIsContextSensitive(target2.symbol) ? typeToString(target2, target2.symbol.valueDeclaration) : typeToString(target2);
      if (globalStringType ***REMOVED******REMOVED******REMOVED*** source2 && stringType ***REMOVED******REMOVED******REMOVED*** target2 || globalNumberType ***REMOVED******REMOVED******REMOVED*** source2 && numberType ***REMOVED******REMOVED******REMOVED*** target2 || globalBooleanType ***REMOVED******REMOVED******REMOVED*** source2 && booleanType ***REMOVED******REMOVED******REMOVED*** target2 || getGlobalESSymbolType() ***REMOVED******REMOVED******REMOVED*** source2 && esSymbolType ***REMOVED******REMOVED******REMOVED*** target2) {
        reportError(Diagnostics._0_is_a_primitive_but_1_is_a_wrapper_object_Prefer_using_0_when_possible, targetType, sourceType);
      }
    }
    function tryElaborateArrayLikeErrors(source2, target2, reportErrors2) {
      if (isTupleType(source2)) {
        if (source2.target.readonly && isMutableArrayOrTuple(target2)) {
          if (reportErrors2) {
            reportError(Diagnostics.The_type_0_is_readonly_and_cannot_be_assigned_to_the_mutable_type_1, typeToString(source2), typeToString(target2));
          }
          return false;
        }
        return isArrayOrTupleType(target2);
      }
      if (isReadonlyArrayType(source2) && isMutableArrayOrTuple(target2)) {
        if (reportErrors2) {
          reportError(Diagnostics.The_type_0_is_readonly_and_cannot_be_assigned_to_the_mutable_type_1, typeToString(source2), typeToString(target2));
        }
        return false;
      }
      if (isTupleType(target2)) {
        return isArrayType(source2);
      }
      return true;
    }
    function isRelatedToWorker(source2, target2, reportErrors2) {
      return isRelatedTo(source2, target2, 3 /* Both */, reportErrors2);
    }
    function isRelatedTo(originalSource, originalTarget, recursionFlags ***REMOVED*** 3 /* Both */, reportErrors2 ***REMOVED*** false, headMessage2, intersectionState ***REMOVED*** 0 /* None */) {
      if (originalSource ***REMOVED******REMOVED******REMOVED*** originalTarget) return -1 /* True */;
      if (originalSource.flags & 524288 /* Object */ && originalTarget.flags & 402784252 /* Primitive */) {
        if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation && !(originalTarget.flags & 131072 /* Never */) && isSimpleTypeRelatedTo(originalTarget, originalSource, relation) || isSimpleTypeRelatedTo(originalSource, originalTarget, relation, reportErrors2 ? reportError : void 0)) {
          return -1 /* True */;
        }
        if (reportErrors2) {
          reportErrorResults(originalSource, originalTarget, originalSource, originalTarget, headMessage2);
        }
        return 0 /* False */;
      }
      const source2 ***REMOVED*** getNormalizedType(
        originalSource,
        /*writing*/
        false
      );
      let target2 ***REMOVED*** getNormalizedType(
        originalTarget,
        /*writing*/
        true
      );
      if (source2 ***REMOVED******REMOVED******REMOVED*** target2) return -1 /* True */;
      if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        if (source2.flags !***REMOVED******REMOVED*** target2.flags) return 0 /* False */;
        if (source2.flags & 67358815 /* Singleton */) return -1 /* True */;
        traceUnionsOrIntersectionsTooLarge(source2, target2);
        return recursiveTypeRelatedTo(
          source2,
          target2,
          /*reportErrors*/
          false,
          0 /* None */,
          recursionFlags
        );
      }
      if (source2.flags & 262144 /* TypeParameter */ && getConstraintOfType(source2) ***REMOVED******REMOVED******REMOVED*** target2) {
        return -1 /* True */;
      }
      if (source2.flags & 470302716 /* DefinitelyNonNullable */ && target2.flags & 1048576 /* Union */) {
        const types ***REMOVED*** target2.types;
        const candidate ***REMOVED*** types.length ***REMOVED******REMOVED******REMOVED*** 2 && types[0].flags & 98304 /* Nullable */ ? types[1] : types.length ***REMOVED******REMOVED******REMOVED*** 3 && types[0].flags & 98304 /* Nullable */ && types[1].flags & 98304 /* Nullable */ ? types[2] : void 0;
        if (candidate && !(candidate.flags & 98304 /* Nullable */)) {
          target2 ***REMOVED*** getNormalizedType(
            candidate,
            /*writing*/
            true
          );
          if (source2 ***REMOVED******REMOVED******REMOVED*** target2) return -1 /* True */;
        }
      }
      if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation && !(target2.flags & 131072 /* Never */) && isSimpleTypeRelatedTo(target2, source2, relation) || isSimpleTypeRelatedTo(source2, target2, relation, reportErrors2 ? reportError : void 0)) return -1 /* True */;
      if (source2.flags & 469499904 /* StructuredOrInstantiable */ || target2.flags & 469499904 /* StructuredOrInstantiable */) {
        const isPerformingExcessPropertyChecks ***REMOVED*** !(intersectionState & 2 /* Target */) && (isObjectLiteralType(source2) && getObjectFlags(source2) & 8192 /* FreshLiteral */);
        if (isPerformingExcessPropertyChecks) {
          if (hasExcessProperties(source2, target2, reportErrors2)) {
            if (reportErrors2) {
              reportRelationError(headMessage2, source2, originalTarget.aliasSymbol ? originalTarget : target2);
            }
            return 0 /* False */;
          }
        }
        const isPerformingCommonPropertyChecks ***REMOVED*** (relation !***REMOVED******REMOVED*** comparableRelation || isUnitType(source2)) && !(intersectionState & 2 /* Target */) && source2.flags & (402784252 /* Primitive */ | 524288 /* Object */ | 2097152 /* Intersection */) && source2 !***REMOVED******REMOVED*** globalObjectType && target2.flags & (524288 /* Object */ | 2097152 /* Intersection */) && isWeakType(target2) && (getPropertiesOfType(source2).length > 0 || typeHasCallOrConstructSignatures(source2));
        const isComparingJsxAttributes ***REMOVED*** !!(getObjectFlags(source2) & 2048 /* JsxAttributes */);
        if (isPerformingCommonPropertyChecks && !hasCommonProperties(source2, target2, isComparingJsxAttributes)) {
          if (reportErrors2) {
            const sourceString ***REMOVED*** typeToString(originalSource.aliasSymbol ? originalSource : source2);
            const targetString ***REMOVED*** typeToString(originalTarget.aliasSymbol ? originalTarget : target2);
            const calls ***REMOVED*** getSignaturesOfType(source2, 0 /* Call */);
            const constructs ***REMOVED*** getSignaturesOfType(source2, 1 /* Construct */);
            if (calls.length > 0 && isRelatedTo(
              getReturnTypeOfSignature(calls[0]),
              target2,
              1 /* Source */,
              /*reportErrors*/
              false
            ) || constructs.length > 0 && isRelatedTo(
              getReturnTypeOfSignature(constructs[0]),
              target2,
              1 /* Source */,
              /*reportErrors*/
              false
            )) {
              reportError(Diagnostics.Value_of_type_0_has_no_properties_in_common_with_type_1_Did_you_mean_to_call_it, sourceString, targetString);
            } else {
              reportError(Diagnostics.Type_0_has_no_properties_in_common_with_type_1, sourceString, targetString);
            }
          }
          return 0 /* False */;
        }
        traceUnionsOrIntersectionsTooLarge(source2, target2);
        const skipCaching ***REMOVED*** source2.flags & 1048576 /* Union */ && source2.types.length < 4 && !(target2.flags & 1048576 /* Union */) || target2.flags & 1048576 /* Union */ && target2.types.length < 4 && !(source2.flags & 469499904 /* StructuredOrInstantiable */);
        const result2 ***REMOVED*** skipCaching ? unionOrIntersectionRelatedTo(source2, target2, reportErrors2, intersectionState) : recursiveTypeRelatedTo(source2, target2, reportErrors2, intersectionState, recursionFlags);
        if (result2) {
          return result2;
        }
      }
      if (reportErrors2) {
        reportErrorResults(originalSource, originalTarget, source2, target2, headMessage2);
      }
      return 0 /* False */;
    }
    function reportErrorResults(originalSource, originalTarget, source2, target2, headMessage2) {
      var _a2, _b;
      const sourceHasBase ***REMOVED*** !!getSingleBaseForNonAugmentingSubtype(originalSource);
      const targetHasBase ***REMOVED*** !!getSingleBaseForNonAugmentingSubtype(originalTarget);
      source2 ***REMOVED*** originalSource.aliasSymbol || sourceHasBase ? originalSource : source2;
      target2 ***REMOVED*** originalTarget.aliasSymbol || targetHasBase ? originalTarget : target2;
      let maybeSuppress ***REMOVED*** overrideNextErrorInfo > 0;
      if (maybeSuppress) {
        overrideNextErrorInfo--;
      }
      if (source2.flags & 524288 /* Object */ && target2.flags & 524288 /* Object */) {
        const currentError ***REMOVED*** errorInfo;
        tryElaborateArrayLikeErrors(
          source2,
          target2,
          /*reportErrors*/
          true
        );
        if (errorInfo !***REMOVED******REMOVED*** currentError) {
          maybeSuppress ***REMOVED*** !!errorInfo;
        }
      }
      if (source2.flags & 524288 /* Object */ && target2.flags & 402784252 /* Primitive */) {
        tryElaborateErrorsForPrimitivesAndObjects(source2, target2);
      } else if (source2.symbol && source2.flags & 524288 /* Object */ && globalObjectType ***REMOVED******REMOVED******REMOVED*** source2) {
        reportError(Diagnostics.The_Object_type_is_assignable_to_very_few_other_types_Did_you_mean_to_use_the_any_type_instead);
      } else if (getObjectFlags(source2) & 2048 /* JsxAttributes */ && target2.flags & 2097152 /* Intersection */) {
        const targetTypes ***REMOVED*** target2.types;
        const intrinsicAttributes ***REMOVED*** getJsxType(JsxNames.IntrinsicAttributes, errorNode);
        const intrinsicClassAttributes ***REMOVED*** getJsxType(JsxNames.IntrinsicClassAttributes, errorNode);
        if (!isErrorType(intrinsicAttributes) && !isErrorType(intrinsicClassAttributes) && (contains(targetTypes, intrinsicAttributes) || contains(targetTypes, intrinsicClassAttributes))) {
          return;
        }
      } else {
        errorInfo ***REMOVED*** elaborateNeverIntersection(errorInfo, originalTarget);
      }
      if (!headMessage2 && maybeSuppress) {
        const savedErrorState ***REMOVED*** captureErrorCalculationState();
        reportRelationError(headMessage2, source2, target2);
        let canonical;
        if (errorInfo && errorInfo !***REMOVED******REMOVED*** savedErrorState.errorInfo) {
          canonical ***REMOVED*** { code: errorInfo.code, messageText: errorInfo.messageText };
        }
        resetErrorInfo(savedErrorState);
        if (canonical && errorInfo) {
          errorInfo.canonicalHead ***REMOVED*** canonical;
        }
        lastSkippedInfo ***REMOVED*** [source2, target2];
        return;
      }
      reportRelationError(headMessage2, source2, target2);
      if (source2.flags & 262144 /* TypeParameter */ && ((_b ***REMOVED*** (_a2 ***REMOVED*** source2.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a2.declarations) ***REMOVED******REMOVED*** null ? void 0 : _b[0]) && !getConstraintOfType(source2)) {
        const syntheticParam ***REMOVED*** cloneTypeParameter(source2);
        syntheticParam.constraint ***REMOVED*** instantiateType(target2, makeUnaryTypeMapper(source2, syntheticParam));
        if (hasNonCircularBaseConstraint(syntheticParam)) {
          const targetConstraintString ***REMOVED*** typeToString(target2, source2.symbol.declarations[0]);
          associateRelatedInfo(createDiagnosticForNode(source2.symbol.declarations[0], Diagnostics.This_type_parameter_might_need_an_extends_0_constraint, targetConstraintString));
        }
      }
    }
    function traceUnionsOrIntersectionsTooLarge(source2, target2) {
      if (!tracing) {
        return;
      }
      if (source2.flags & 3145728 /* UnionOrIntersection */ && target2.flags & 3145728 /* UnionOrIntersection */) {
        const sourceUnionOrIntersection ***REMOVED*** source2;
        const targetUnionOrIntersection ***REMOVED*** target2;
        if (sourceUnionOrIntersection.objectFlags & targetUnionOrIntersection.objectFlags & 32768 /* PrimitiveUnion */) {
          return;
        }
        const sourceSize ***REMOVED*** sourceUnionOrIntersection.types.length;
        const targetSize ***REMOVED*** targetUnionOrIntersection.types.length;
        if (sourceSize * targetSize > 1e6) {
          tracing.instant(tracing.Phase.CheckTypes, "traceUnionsOrIntersectionsTooLarge_DepthLimit", {
            sourceId: source2.id,
            sourceSize,
            targetId: target2.id,
            targetSize,
            pos: errorNode ***REMOVED******REMOVED*** null ? void 0 : errorNode.pos,
            end: errorNode ***REMOVED******REMOVED*** null ? void 0 : errorNode.end
          });
        }
      }
    }
    function getTypeOfPropertyInTypes(types, name) {
      const appendPropType ***REMOVED*** (propTypes, type) ***REMOVED***> {
        var _a2;
        type ***REMOVED*** getApparentType(type);
        const prop ***REMOVED*** type.flags & 3145728 /* UnionOrIntersection */ ? getPropertyOfUnionOrIntersectionType(type, name) : getPropertyOfObjectType(type, name);
        const propType ***REMOVED*** prop && getTypeOfSymbol(prop) || ((_a2 ***REMOVED*** getApplicableIndexInfoForName(type, name)) ***REMOVED******REMOVED*** null ? void 0 : _a2.type) || undefinedType;
        return append(propTypes, propType);
      };
      return getUnionType(reduceLeft(
        types,
        appendPropType,
        /*initial*/
        void 0
      ) || emptyArray);
    }
    function hasExcessProperties(source2, target2, reportErrors2) {
      var _a2;
      if (!isExcessPropertyCheckTarget(target2) || !noImplicitAny && getObjectFlags(target2) & 4096 /* JSLiteral */) {
        return false;
      }
      const isComparingJsxAttributes ***REMOVED*** !!(getObjectFlags(source2) & 2048 /* JsxAttributes */);
      if ((relation ***REMOVED******REMOVED******REMOVED*** assignableRelation || relation ***REMOVED******REMOVED******REMOVED*** comparableRelation) && (isTypeSubsetOf(globalObjectType, target2) || !isComparingJsxAttributes && isEmptyObjectType(target2))) {
        return false;
      }
      let reducedTarget ***REMOVED*** target2;
      let checkTypes;
      if (target2.flags & 1048576 /* Union */) {
        reducedTarget ***REMOVED*** findMatchingDiscriminantType(source2, target2, isRelatedTo) || filterPrimitivesIfContainsNonPrimitive(target2);
        checkTypes ***REMOVED*** reducedTarget.flags & 1048576 /* Union */ ? reducedTarget.types : [reducedTarget];
      }
      for (const prop of getPropertiesOfType(source2)) {
        if (shouldCheckAsExcessProperty(prop, source2.symbol) && !isIgnoredJsxProperty(source2, prop)) {
          if (!isKnownProperty(reducedTarget, prop.escapedName, isComparingJsxAttributes)) {
            if (reportErrors2) {
              const errorTarget ***REMOVED*** filterType(reducedTarget, isExcessPropertyCheckTarget);
              if (!errorNode) return Debug.fail();
              if (isJsxAttributes(errorNode) || isJsxOpeningLikeElement(errorNode) || isJsxOpeningLikeElement(errorNode.parent)) {
                if (prop.valueDeclaration && isJsxAttribute(prop.valueDeclaration) && getSourceFileOfNode(errorNode) ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(prop.valueDeclaration.name)) {
                  errorNode ***REMOVED*** prop.valueDeclaration.name;
                }
                const propName ***REMOVED*** symbolToString(prop);
                const suggestionSymbol ***REMOVED*** getSuggestedSymbolForNonexistentJSXAttribute(propName, errorTarget);
                const suggestion ***REMOVED*** suggestionSymbol ? symbolToString(suggestionSymbol) : void 0;
                if (suggestion) {
                  reportError(Diagnostics.Property_0_does_not_exist_on_type_1_Did_you_mean_2, propName, typeToString(errorTarget), suggestion);
                } else {
                  reportError(Diagnostics.Property_0_does_not_exist_on_type_1, propName, typeToString(errorTarget));
                }
              } else {
                const objectLiteralDeclaration ***REMOVED*** ((_a2 ***REMOVED*** source2.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a2.declarations) && firstOrUndefined(source2.symbol.declarations);
                let suggestion;
                if (prop.valueDeclaration && findAncestor(prop.valueDeclaration, (d) ***REMOVED***> d ***REMOVED******REMOVED******REMOVED*** objectLiteralDeclaration) && getSourceFileOfNode(objectLiteralDeclaration) ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(errorNode)) {
                  const propDeclaration ***REMOVED*** prop.valueDeclaration;
                  Debug.assertNode(propDeclaration, isObjectLiteralElementLike);
                  const name ***REMOVED*** propDeclaration.name;
                  errorNode ***REMOVED*** name;
                  if (isIdentifier(name)) {
                    suggestion ***REMOVED*** getSuggestionForNonexistentProperty(name, errorTarget);
                  }
                }
                if (suggestion !***REMOVED******REMOVED*** void 0) {
                  reportParentSkippedError(Diagnostics.Object_literal_may_only_specify_known_properties_but_0_does_not_exist_in_type_1_Did_you_mean_to_write_2, symbolToString(prop), typeToString(errorTarget), suggestion);
                } else {
                  reportParentSkippedError(Diagnostics.Object_literal_may_only_specify_known_properties_and_0_does_not_exist_in_type_1, symbolToString(prop), typeToString(errorTarget));
                }
              }
            }
            return true;
          }
          if (checkTypes && !isRelatedTo(getTypeOfSymbol(prop), getTypeOfPropertyInTypes(checkTypes, prop.escapedName), 3 /* Both */, reportErrors2)) {
            if (reportErrors2) {
              reportIncompatibleError(Diagnostics.Types_of_property_0_are_incompatible, symbolToString(prop));
            }
            return true;
          }
        }
      }
      return false;
    }
    function shouldCheckAsExcessProperty(prop, container) {
      return prop.valueDeclaration && container.valueDeclaration && prop.valueDeclaration.parent ***REMOVED******REMOVED******REMOVED*** container.valueDeclaration;
    }
    function unionOrIntersectionRelatedTo(source2, target2, reportErrors2, intersectionState) {
      if (source2.flags & 1048576 /* Union */) {
        if (target2.flags & 1048576 /* Union */) {
          const sourceOrigin ***REMOVED*** source2.origin;
          if (sourceOrigin && sourceOrigin.flags & 2097152 /* Intersection */ && target2.aliasSymbol && contains(sourceOrigin.types, target2)) {
            return -1 /* True */;
          }
          const targetOrigin ***REMOVED*** target2.origin;
          if (targetOrigin && targetOrigin.flags & 1048576 /* Union */ && source2.aliasSymbol && contains(targetOrigin.types, source2)) {
            return -1 /* True */;
          }
        }
        return relation ***REMOVED******REMOVED******REMOVED*** comparableRelation ? someTypeRelatedToType(source2, target2, reportErrors2 && !(source2.flags & 402784252 /* Primitive */), intersectionState) : eachTypeRelatedToType(source2, target2, reportErrors2 && !(source2.flags & 402784252 /* Primitive */), intersectionState);
      }
      if (target2.flags & 1048576 /* Union */) {
        return typeRelatedToSomeType(getRegularTypeOfObjectLiteral(source2), target2, reportErrors2 && !(source2.flags & 402784252 /* Primitive */) && !(target2.flags & 402784252 /* Primitive */), intersectionState);
      }
      if (target2.flags & 2097152 /* Intersection */) {
        return typeRelatedToEachType(source2, target2, reportErrors2, 2 /* Target */);
      }
      if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation && target2.flags & 402784252 /* Primitive */) {
        const constraints ***REMOVED*** sameMap(source2.types, (t) ***REMOVED***> t.flags & 465829888 /* Instantiable */ ? getBaseConstraintOfType(t) || unknownType : t);
        if (constraints !***REMOVED******REMOVED*** source2.types) {
          source2 ***REMOVED*** getIntersectionType(constraints);
          if (source2.flags & 131072 /* Never */) {
            return 0 /* False */;
          }
          if (!(source2.flags & 2097152 /* Intersection */)) {
            return isRelatedTo(
              source2,
              target2,
              1 /* Source */,
              /*reportErrors*/
              false
            ) || isRelatedTo(
              target2,
              source2,
              1 /* Source */,
              /*reportErrors*/
              false
            );
          }
        }
      }
      return someTypeRelatedToType(
        source2,
        target2,
        /*reportErrors*/
        false,
        1 /* Source */
      );
    }
    function eachTypeRelatedToSomeType(source2, target2) {
      let result2 ***REMOVED*** -1 /* True */;
      const sourceTypes ***REMOVED*** source2.types;
      for (const sourceType of sourceTypes) {
        const related ***REMOVED*** typeRelatedToSomeType(
          sourceType,
          target2,
          /*reportErrors*/
          false,
          0 /* None */
        );
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function typeRelatedToSomeType(source2, target2, reportErrors2, intersectionState) {
      const targetTypes ***REMOVED*** target2.types;
      if (target2.flags & 1048576 /* Union */) {
        if (containsType(targetTypes, source2)) {
          return -1 /* True */;
        }
        if (relation !***REMOVED******REMOVED*** comparableRelation && getObjectFlags(target2) & 32768 /* PrimitiveUnion */ && !(source2.flags & 1024 /* EnumLiteral */) && (source2.flags & (128 /* StringLiteral */ | 512 /* BooleanLiteral */ | 2048 /* BigIntLiteral */) || (relation ***REMOVED******REMOVED******REMOVED*** subtypeRelation || relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation) && source2.flags & 256 /* NumberLiteral */)) {
          const alternateForm ***REMOVED*** source2 ***REMOVED******REMOVED******REMOVED*** source2.regularType ? source2.freshType : source2.regularType;
          const primitive ***REMOVED*** source2.flags & 128 /* StringLiteral */ ? stringType : source2.flags & 256 /* NumberLiteral */ ? numberType : source2.flags & 2048 /* BigIntLiteral */ ? bigintType : void 0;
          return primitive && containsType(targetTypes, primitive) || alternateForm && containsType(targetTypes, alternateForm) ? -1 /* True */ : 0 /* False */;
        }
        const match ***REMOVED*** getMatchingUnionConstituentForType(target2, source2);
        if (match) {
          const related ***REMOVED*** isRelatedTo(
            source2,
            match,
            2 /* Target */,
            /*reportErrors*/
            false,
            /*headMessage*/
            void 0,
            intersectionState
          );
          if (related) {
            return related;
          }
        }
      }
      for (const type of targetTypes) {
        const related ***REMOVED*** isRelatedTo(
          source2,
          type,
          2 /* Target */,
          /*reportErrors*/
          false,
          /*headMessage*/
          void 0,
          intersectionState
        );
        if (related) {
          return related;
        }
      }
      if (reportErrors2) {
        const bestMatchingType ***REMOVED*** getBestMatchingType(source2, target2, isRelatedTo);
        if (bestMatchingType) {
          isRelatedTo(
            source2,
            bestMatchingType,
            2 /* Target */,
            /*reportErrors*/
            true,
            /*headMessage*/
            void 0,
            intersectionState
          );
        }
      }
      return 0 /* False */;
    }
    function typeRelatedToEachType(source2, target2, reportErrors2, intersectionState) {
      let result2 ***REMOVED*** -1 /* True */;
      const targetTypes ***REMOVED*** target2.types;
      for (const targetType of targetTypes) {
        const related ***REMOVED*** isRelatedTo(
          source2,
          targetType,
          2 /* Target */,
          reportErrors2,
          /*headMessage*/
          void 0,
          intersectionState
        );
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function someTypeRelatedToType(source2, target2, reportErrors2, intersectionState) {
      const sourceTypes ***REMOVED*** source2.types;
      if (source2.flags & 1048576 /* Union */ && containsType(sourceTypes, target2)) {
        return -1 /* True */;
      }
      const len ***REMOVED*** sourceTypes.length;
      for (let i ***REMOVED*** 0; i < len; i++) {
        const related ***REMOVED*** isRelatedTo(
          sourceTypes[i],
          target2,
          1 /* Source */,
          reportErrors2 && i ***REMOVED******REMOVED******REMOVED*** len - 1,
          /*headMessage*/
          void 0,
          intersectionState
        );
        if (related) {
          return related;
        }
      }
      return 0 /* False */;
    }
    function getUndefinedStrippedTargetIfNeeded(source2, target2) {
      if (source2.flags & 1048576 /* Union */ && target2.flags & 1048576 /* Union */ && !(source2.types[0].flags & 32768 /* Undefined */) && target2.types[0].flags & 32768 /* Undefined */) {
        return extractTypesOfKind(target2, ~32768 /* Undefined */);
      }
      return target2;
    }
    function eachTypeRelatedToType(source2, target2, reportErrors2, intersectionState) {
      let result2 ***REMOVED*** -1 /* True */;
      const sourceTypes ***REMOVED*** source2.types;
      const undefinedStrippedTarget ***REMOVED*** getUndefinedStrippedTargetIfNeeded(source2, target2);
      for (let i ***REMOVED*** 0; i < sourceTypes.length; i++) {
        const sourceType ***REMOVED*** sourceTypes[i];
        if (undefinedStrippedTarget.flags & 1048576 /* Union */ && sourceTypes.length >***REMOVED*** undefinedStrippedTarget.types.length && sourceTypes.length % undefinedStrippedTarget.types.length ***REMOVED******REMOVED******REMOVED*** 0) {
          const related2 ***REMOVED*** isRelatedTo(
            sourceType,
            undefinedStrippedTarget.types[i % undefinedStrippedTarget.types.length],
            3 /* Both */,
            /*reportErrors*/
            false,
            /*headMessage*/
            void 0,
            intersectionState
          );
          if (related2) {
            result2 &***REMOVED*** related2;
            continue;
          }
        }
        const related ***REMOVED*** isRelatedTo(
          sourceType,
          target2,
          1 /* Source */,
          reportErrors2,
          /*headMessage*/
          void 0,
          intersectionState
        );
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function typeArgumentsRelatedTo(sources ***REMOVED*** emptyArray, targets ***REMOVED*** emptyArray, variances ***REMOVED*** emptyArray, reportErrors2, intersectionState) {
      if (sources.length !***REMOVED******REMOVED*** targets.length && relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        return 0 /* False */;
      }
      const length2 ***REMOVED*** sources.length <***REMOVED*** targets.length ? sources.length : targets.length;
      let result2 ***REMOVED*** -1 /* True */;
      for (let i ***REMOVED*** 0; i < length2; i++) {
        const varianceFlags ***REMOVED*** i < variances.length ? variances[i] : 1 /* Covariant */;
        const variance ***REMOVED*** varianceFlags & 7 /* VarianceMask */;
        if (variance !***REMOVED******REMOVED*** 4 /* Independent */) {
          const s ***REMOVED*** sources[i];
          const t ***REMOVED*** targets[i];
          let related ***REMOVED*** -1 /* True */;
          if (varianceFlags & 8 /* Unmeasurable */) {
            related ***REMOVED*** relation ***REMOVED******REMOVED******REMOVED*** identityRelation ? isRelatedTo(
              s,
              t,
              3 /* Both */,
              /*reportErrors*/
              false
            ) : compareTypesIdentical(s, t);
          } else if (variance ***REMOVED******REMOVED******REMOVED*** 1 /* Covariant */) {
            related ***REMOVED*** isRelatedTo(
              s,
              t,
              3 /* Both */,
              reportErrors2,
              /*headMessage*/
              void 0,
              intersectionState
            );
          } else if (variance ***REMOVED******REMOVED******REMOVED*** 2 /* Contravariant */) {
            related ***REMOVED*** isRelatedTo(
              t,
              s,
              3 /* Both */,
              reportErrors2,
              /*headMessage*/
              void 0,
              intersectionState
            );
          } else if (variance ***REMOVED******REMOVED******REMOVED*** 3 /* Bivariant */) {
            related ***REMOVED*** isRelatedTo(
              t,
              s,
              3 /* Both */,
              /*reportErrors*/
              false
            );
            if (!related) {
              related ***REMOVED*** isRelatedTo(
                s,
                t,
                3 /* Both */,
                reportErrors2,
                /*headMessage*/
                void 0,
                intersectionState
              );
            }
          } else {
            related ***REMOVED*** isRelatedTo(
              s,
              t,
              3 /* Both */,
              reportErrors2,
              /*headMessage*/
              void 0,
              intersectionState
            );
            if (related) {
              related &***REMOVED*** isRelatedTo(
                t,
                s,
                3 /* Both */,
                reportErrors2,
                /*headMessage*/
                void 0,
                intersectionState
              );
            }
          }
          if (!related) {
            return 0 /* False */;
          }
          result2 &***REMOVED*** related;
        }
      }
      return result2;
    }
    function recursiveTypeRelatedTo(source2, target2, reportErrors2, intersectionState, recursionFlags) {
      var _a2, _b, _c;
      if (overflow) {
        return 0 /* False */;
      }
      const id ***REMOVED*** getRelationKey(
        source2,
        target2,
        intersectionState,
        relation,
        /*ignoreConstraints*/
        false
      );
      const entry ***REMOVED*** relation.get(id);
      if (entry !***REMOVED******REMOVED*** void 0) {
        if (reportErrors2 && entry & 2 /* Failed */ && !(entry & 96 /* Overflow */)) {
        } else {
          if (outofbandVarianceMarkerHandler) {
            const saved ***REMOVED*** entry & 24 /* ReportsMask */;
            if (saved & 8 /* ReportsUnmeasurable */) {
              instantiateType(source2, reportUnmeasurableMapper);
            }
            if (saved & 16 /* ReportsUnreliable */) {
              instantiateType(source2, reportUnreliableMapper);
            }
          }
          if (reportErrors2 && entry & 96 /* Overflow */) {
            const message ***REMOVED*** entry & 32 /* ComplexityOverflow */ ? Diagnostics.Excessive_complexity_comparing_types_0_and_1 : Diagnostics.Excessive_stack_depth_comparing_types_0_and_1;
            reportError(message, typeToString(source2), typeToString(target2));
            overrideNextErrorInfo++;
          }
          return entry & 1 /* Succeeded */ ? -1 /* True */ : 0 /* False */;
        }
      }
      if (relationCount <***REMOVED*** 0) {
        overflow ***REMOVED*** true;
        return 0 /* False */;
      }
      if (!maybeKeys) {
        maybeKeys ***REMOVED*** [];
        maybeKeysSet ***REMOVED*** /* @__PURE__ */ new Set();
        sourceStack ***REMOVED*** [];
        targetStack ***REMOVED*** [];
      } else {
        if (maybeKeysSet.has(id)) {
          return 3 /* Maybe */;
        }
        const broadestEquivalentId ***REMOVED*** id.startsWith("*") ? getRelationKey(
          source2,
          target2,
          intersectionState,
          relation,
          /*ignoreConstraints*/
          true
        ) : void 0;
        if (broadestEquivalentId && maybeKeysSet.has(broadestEquivalentId)) {
          return 3 /* Maybe */;
        }
        if (sourceDepth ***REMOVED******REMOVED******REMOVED*** 100 || targetDepth ***REMOVED******REMOVED******REMOVED*** 100) {
          overflow ***REMOVED*** true;
          return 0 /* False */;
        }
      }
      const maybeStart ***REMOVED*** maybeCount;
      maybeKeys[maybeCount] ***REMOVED*** id;
      maybeKeysSet.add(id);
      maybeCount++;
      const saveExpandingFlags ***REMOVED*** expandingFlags;
      if (recursionFlags & 1 /* Source */) {
        sourceStack[sourceDepth] ***REMOVED*** source2;
        sourceDepth++;
        if (!(expandingFlags & 1 /* Source */) && isDeeplyNestedType(source2, sourceStack, sourceDepth)) expandingFlags |***REMOVED*** 1 /* Source */;
      }
      if (recursionFlags & 2 /* Target */) {
        targetStack[targetDepth] ***REMOVED*** target2;
        targetDepth++;
        if (!(expandingFlags & 2 /* Target */) && isDeeplyNestedType(target2, targetStack, targetDepth)) expandingFlags |***REMOVED*** 2 /* Target */;
      }
      let originalHandler;
      let propagatingVarianceFlags ***REMOVED*** 0;
      if (outofbandVarianceMarkerHandler) {
        originalHandler ***REMOVED*** outofbandVarianceMarkerHandler;
        outofbandVarianceMarkerHandler ***REMOVED*** (onlyUnreliable) ***REMOVED***> {
          propagatingVarianceFlags |***REMOVED*** onlyUnreliable ? 16 /* ReportsUnreliable */ : 8 /* ReportsUnmeasurable */;
          return originalHandler(onlyUnreliable);
        };
      }
      let result2;
      if (expandingFlags ***REMOVED******REMOVED******REMOVED*** 3 /* Both */) {
        (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.instant(tracing.Phase.CheckTypes, "recursiveTypeRelatedTo_DepthLimit", {
          sourceId: source2.id,
          sourceIdStack: sourceStack.map((t) ***REMOVED***> t.id),
          targetId: target2.id,
          targetIdStack: targetStack.map((t) ***REMOVED***> t.id),
          depth: sourceDepth,
          targetDepth
        });
        result2 ***REMOVED*** 3 /* Maybe */;
      } else {
        (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.push(tracing.Phase.CheckTypes, "structuredTypeRelatedTo", { sourceId: source2.id, targetId: target2.id });
        result2 ***REMOVED*** structuredTypeRelatedTo(source2, target2, reportErrors2, intersectionState);
        (_c ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _c.pop();
      }
      if (outofbandVarianceMarkerHandler) {
        outofbandVarianceMarkerHandler ***REMOVED*** originalHandler;
      }
      if (recursionFlags & 1 /* Source */) {
        sourceDepth--;
      }
      if (recursionFlags & 2 /* Target */) {
        targetDepth--;
      }
      expandingFlags ***REMOVED*** saveExpandingFlags;
      if (result2) {
        if (result2 ***REMOVED******REMOVED******REMOVED*** -1 /* True */ || sourceDepth ***REMOVED******REMOVED******REMOVED*** 0 && targetDepth ***REMOVED******REMOVED******REMOVED*** 0) {
          if (result2 ***REMOVED******REMOVED******REMOVED*** -1 /* True */ || result2 ***REMOVED******REMOVED******REMOVED*** 3 /* Maybe */) {
            resetMaybeStack(
              /*markAllAsSucceeded*/
              true
            );
          } else {
            resetMaybeStack(
              /*markAllAsSucceeded*/
              false
            );
          }
        }
      } else {
        relation.set(id, 2 /* Failed */ | propagatingVarianceFlags);
        relationCount--;
        resetMaybeStack(
          /*markAllAsSucceeded*/
          false
        );
      }
      return result2;
      function resetMaybeStack(markAllAsSucceeded) {
        for (let i ***REMOVED*** maybeStart; i < maybeCount; i++) {
          maybeKeysSet.delete(maybeKeys[i]);
          if (markAllAsSucceeded) {
            relation.set(maybeKeys[i], 1 /* Succeeded */ | propagatingVarianceFlags);
            relationCount--;
          }
        }
        maybeCount ***REMOVED*** maybeStart;
      }
    }
    function structuredTypeRelatedTo(source2, target2, reportErrors2, intersectionState) {
      const saveErrorInfo ***REMOVED*** captureErrorCalculationState();
      let result2 ***REMOVED*** structuredTypeRelatedToWorker(source2, target2, reportErrors2, intersectionState, saveErrorInfo);
      if (relation !***REMOVED******REMOVED*** identityRelation) {
        if (!result2 && (source2.flags & 2097152 /* Intersection */ || source2.flags & 262144 /* TypeParameter */ && target2.flags & 1048576 /* Union */)) {
          const constraint ***REMOVED*** getEffectiveConstraintOfIntersection(source2.flags & 2097152 /* Intersection */ ? source2.types : [source2], !!(target2.flags & 1048576 /* Union */));
          if (constraint && everyType(constraint, (c) ***REMOVED***> c !***REMOVED******REMOVED*** source2)) {
            result2 ***REMOVED*** isRelatedTo(
              constraint,
              target2,
              1 /* Source */,
              /*reportErrors*/
              false,
              /*headMessage*/
              void 0,
              intersectionState
            );
          }
        }
        if (result2 && !(intersectionState & 2 /* Target */) && target2.flags & 2097152 /* Intersection */ && !isGenericObjectType(target2) && source2.flags & (524288 /* Object */ | 2097152 /* Intersection */)) {
          result2 &***REMOVED*** propertiesRelatedTo(
            source2,
            target2,
            reportErrors2,
            /*excludedProperties*/
            void 0,
            /*optionalsOnly*/
            false,
            0 /* None */
          );
          if (result2 && isObjectLiteralType(source2) && getObjectFlags(source2) & 8192 /* FreshLiteral */) {
            result2 &***REMOVED*** indexSignaturesRelatedTo(
              source2,
              target2,
              /*sourceIsPrimitive*/
              false,
              reportErrors2,
              0 /* None */
            );
          }
        } else if (result2 && isNonGenericObjectType(target2) && !isArrayOrTupleType(target2) && source2.flags & 2097152 /* Intersection */ && getApparentType(source2).flags & 3670016 /* StructuredType */ && !some(source2.types, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** target2 || !!(getObjectFlags(t) & 262144 /* NonInferrableType */))) {
          result2 &***REMOVED*** propertiesRelatedTo(
            source2,
            target2,
            reportErrors2,
            /*excludedProperties*/
            void 0,
            /*optionalsOnly*/
            true,
            intersectionState
          );
        }
      }
      if (result2) {
        resetErrorInfo(saveErrorInfo);
      }
      return result2;
    }
    function getApparentMappedTypeKeys(nameType, targetType) {
      const modifiersType ***REMOVED*** getApparentType(getModifiersTypeFromMappedType(targetType));
      const mappedKeys ***REMOVED*** [];
      forEachMappedTypePropertyKeyTypeAndIndexSignatureKeyType(
        modifiersType,
        8576 /* StringOrNumberLiteralOrUnique */,
        /*stringsOnly*/
        false,
        (t) ***REMOVED***> void mappedKeys.push(instantiateType(nameType, appendTypeMapping(targetType.mapper, getTypeParameterFromMappedType(targetType), t)))
      );
      return getUnionType(mappedKeys);
    }
    function structuredTypeRelatedToWorker(source2, target2, reportErrors2, intersectionState, saveErrorInfo) {
      let result2;
      let originalErrorInfo;
      let varianceCheckFailed ***REMOVED*** false;
      let sourceFlags ***REMOVED*** source2.flags;
      const targetFlags ***REMOVED*** target2.flags;
      if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        if (sourceFlags & 3145728 /* UnionOrIntersection */) {
          let result3 ***REMOVED*** eachTypeRelatedToSomeType(source2, target2);
          if (result3) {
            result3 &***REMOVED*** eachTypeRelatedToSomeType(target2, source2);
          }
          return result3;
        }
        if (sourceFlags & 4194304 /* Index */) {
          return isRelatedTo(
            source2.type,
            target2.type,
            3 /* Both */,
            /*reportErrors*/
            false
          );
        }
        if (sourceFlags & 8388608 /* IndexedAccess */) {
          if (result2 ***REMOVED*** isRelatedTo(
            source2.objectType,
            target2.objectType,
            3 /* Both */,
            /*reportErrors*/
            false
          )) {
            if (result2 &***REMOVED*** isRelatedTo(
              source2.indexType,
              target2.indexType,
              3 /* Both */,
              /*reportErrors*/
              false
            )) {
              return result2;
            }
          }
        }
        if (sourceFlags & 16777216 /* Conditional */) {
          if (source2.root.isDistributive ***REMOVED******REMOVED******REMOVED*** target2.root.isDistributive) {
            if (result2 ***REMOVED*** isRelatedTo(
              source2.checkType,
              target2.checkType,
              3 /* Both */,
              /*reportErrors*/
              false
            )) {
              if (result2 &***REMOVED*** isRelatedTo(
                source2.extendsType,
                target2.extendsType,
                3 /* Both */,
                /*reportErrors*/
                false
              )) {
                if (result2 &***REMOVED*** isRelatedTo(
                  getTrueTypeFromConditionalType(source2),
                  getTrueTypeFromConditionalType(target2),
                  3 /* Both */,
                  /*reportErrors*/
                  false
                )) {
                  if (result2 &***REMOVED*** isRelatedTo(
                    getFalseTypeFromConditionalType(source2),
                    getFalseTypeFromConditionalType(target2),
                    3 /* Both */,
                    /*reportErrors*/
                    false
                  )) {
                    return result2;
                  }
                }
              }
            }
          }
        }
        if (sourceFlags & 33554432 /* Substitution */) {
          if (result2 ***REMOVED*** isRelatedTo(
            source2.baseType,
            target2.baseType,
            3 /* Both */,
            /*reportErrors*/
            false
          )) {
            if (result2 &***REMOVED*** isRelatedTo(
              source2.constraint,
              target2.constraint,
              3 /* Both */,
              /*reportErrors*/
              false
            )) {
              return result2;
            }
          }
        }
        if (sourceFlags & 134217728 /* TemplateLiteral */) {
          if (arrayIsEqualTo(source2.texts, target2.texts)) {
            const sourceTypes ***REMOVED*** source2.types;
            const targetTypes ***REMOVED*** target2.types;
            result2 ***REMOVED*** -1 /* True */;
            for (let i ***REMOVED*** 0; i < sourceTypes.length; i++) {
              if (!(result2 &***REMOVED*** isRelatedTo(
                sourceTypes[i],
                targetTypes[i],
                3 /* Both */,
                /*reportErrors*/
                false
              ))) {
                break;
              }
            }
            return result2;
          }
        }
        if (sourceFlags & 268435456 /* StringMapping */) {
          if (source2.symbol ***REMOVED******REMOVED******REMOVED*** target2.symbol) {
            return isRelatedTo(
              source2.type,
              target2.type,
              3 /* Both */,
              /*reportErrors*/
              false
            );
          }
        }
        if (!(sourceFlags & 524288 /* Object */)) {
          return 0 /* False */;
        }
      } else if (sourceFlags & 3145728 /* UnionOrIntersection */ || targetFlags & 3145728 /* UnionOrIntersection */) {
        if (result2 ***REMOVED*** unionOrIntersectionRelatedTo(source2, target2, reportErrors2, intersectionState)) {
          return result2;
        }
        if (!(sourceFlags & 465829888 /* Instantiable */ || sourceFlags & 524288 /* Object */ && targetFlags & 1048576 /* Union */ || sourceFlags & 2097152 /* Intersection */ && targetFlags & (524288 /* Object */ | 1048576 /* Union */ | 465829888 /* Instantiable */))) {
          return 0 /* False */;
        }
      }
      if (sourceFlags & (524288 /* Object */ | 16777216 /* Conditional */) && source2.aliasSymbol && source2.aliasTypeArguments && source2.aliasSymbol ***REMOVED******REMOVED******REMOVED*** target2.aliasSymbol && !(isMarkerType(source2) || isMarkerType(target2))) {
        const variances ***REMOVED*** getAliasVariances(source2.aliasSymbol);
        if (variances ***REMOVED******REMOVED******REMOVED*** emptyArray) {
          return 1 /* Unknown */;
        }
        const params ***REMOVED*** getSymbolLinks(source2.aliasSymbol).typeParameters;
        const minParams ***REMOVED*** getMinTypeArgumentCount(params);
        const sourceTypes ***REMOVED*** fillMissingTypeArguments(source2.aliasTypeArguments, params, minParams, isInJSFile(source2.aliasSymbol.valueDeclaration));
        const targetTypes ***REMOVED*** fillMissingTypeArguments(target2.aliasTypeArguments, params, minParams, isInJSFile(source2.aliasSymbol.valueDeclaration));
        const varianceResult ***REMOVED*** relateVariances(sourceTypes, targetTypes, variances, intersectionState);
        if (varianceResult !***REMOVED******REMOVED*** void 0) {
          return varianceResult;
        }
      }
      if (isSingleElementGenericTupleType(source2) && !source2.target.readonly && (result2 ***REMOVED*** isRelatedTo(getTypeArguments(source2)[0], target2, 1 /* Source */)) || isSingleElementGenericTupleType(target2) && (target2.target.readonly || isMutableArrayOrTuple(getBaseConstraintOfType(source2) || source2)) && (result2 ***REMOVED*** isRelatedTo(source2, getTypeArguments(target2)[0], 2 /* Target */))) {
        return result2;
      }
      if (targetFlags & 262144 /* TypeParameter */) {
        if (getObjectFlags(source2) & 32 /* Mapped */ && !source2.declaration.nameType && isRelatedTo(getIndexType(target2), getConstraintTypeFromMappedType(source2), 3 /* Both */)) {
          if (!(getMappedTypeModifiers(source2) & 4 /* IncludeOptional */)) {
            const templateType ***REMOVED*** getTemplateTypeFromMappedType(source2);
            const indexedAccessType ***REMOVED*** getIndexedAccessType(target2, getTypeParameterFromMappedType(source2));
            if (result2 ***REMOVED*** isRelatedTo(templateType, indexedAccessType, 3 /* Both */, reportErrors2)) {
              return result2;
            }
          }
        }
        if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation && sourceFlags & 262144 /* TypeParameter */) {
          let constraint ***REMOVED*** getConstraintOfTypeParameter(source2);
          if (constraint) {
            while (constraint && someType(constraint, (c) ***REMOVED***> !!(c.flags & 262144 /* TypeParameter */))) {
              if (result2 ***REMOVED*** isRelatedTo(
                constraint,
                target2,
                1 /* Source */,
                /*reportErrors*/
                false
              )) {
                return result2;
              }
              constraint ***REMOVED*** getConstraintOfTypeParameter(constraint);
            }
          }
          return 0 /* False */;
        }
      } else if (targetFlags & 4194304 /* Index */) {
        const targetType ***REMOVED*** target2.type;
        if (sourceFlags & 4194304 /* Index */) {
          if (result2 ***REMOVED*** isRelatedTo(
            targetType,
            source2.type,
            3 /* Both */,
            /*reportErrors*/
            false
          )) {
            return result2;
          }
        }
        if (isTupleType(targetType)) {
          if (result2 ***REMOVED*** isRelatedTo(source2, getKnownKeysOfTupleType(targetType), 2 /* Target */, reportErrors2)) {
            return result2;
          }
        } else {
          const constraint ***REMOVED*** getSimplifiedTypeOrConstraint(targetType);
          if (constraint) {
            if (isRelatedTo(source2, getIndexType(constraint, target2.indexFlags | 4 /* NoReducibleCheck */), 2 /* Target */, reportErrors2) ***REMOVED******REMOVED******REMOVED*** -1 /* True */) {
              return -1 /* True */;
            }
          } else if (isGenericMappedType(targetType)) {
            const nameType ***REMOVED*** getNameTypeFromMappedType(targetType);
            const constraintType ***REMOVED*** getConstraintTypeFromMappedType(targetType);
            let targetKeys;
            if (nameType && isMappedTypeWithKeyofConstraintDeclaration(targetType)) {
              const mappedKeys ***REMOVED*** getApparentMappedTypeKeys(nameType, targetType);
              targetKeys ***REMOVED*** getUnionType([mappedKeys, nameType]);
            } else {
              targetKeys ***REMOVED*** nameType || constraintType;
            }
            if (isRelatedTo(source2, targetKeys, 2 /* Target */, reportErrors2) ***REMOVED******REMOVED******REMOVED*** -1 /* True */) {
              return -1 /* True */;
            }
          }
        }
      } else if (targetFlags & 8388608 /* IndexedAccess */) {
        if (sourceFlags & 8388608 /* IndexedAccess */) {
          if (result2 ***REMOVED*** isRelatedTo(source2.objectType, target2.objectType, 3 /* Both */, reportErrors2)) {
            result2 &***REMOVED*** isRelatedTo(source2.indexType, target2.indexType, 3 /* Both */, reportErrors2);
          }
          if (result2) {
            return result2;
          }
          if (reportErrors2) {
            originalErrorInfo ***REMOVED*** errorInfo;
          }
        }
        if (relation ***REMOVED******REMOVED******REMOVED*** assignableRelation || relation ***REMOVED******REMOVED******REMOVED*** comparableRelation) {
          const objectType ***REMOVED*** target2.objectType;
          const indexType ***REMOVED*** target2.indexType;
          const baseObjectType ***REMOVED*** getBaseConstraintOfType(objectType) || objectType;
          const baseIndexType ***REMOVED*** getBaseConstraintOfType(indexType) || indexType;
          if (!isGenericObjectType(baseObjectType) && !isGenericIndexType(baseIndexType)) {
            const accessFlags ***REMOVED*** 4 /* Writing */ | (baseObjectType !***REMOVED******REMOVED*** objectType ? 2 /* NoIndexSignatures */ : 0);
            const constraint ***REMOVED*** getIndexedAccessTypeOrUndefined(baseObjectType, baseIndexType, accessFlags);
            if (constraint) {
              if (reportErrors2 && originalErrorInfo) {
                resetErrorInfo(saveErrorInfo);
              }
              if (result2 ***REMOVED*** isRelatedTo(
                source2,
                constraint,
                2 /* Target */,
                reportErrors2,
                /*headMessage*/
                void 0,
                intersectionState
              )) {
                return result2;
              }
              if (reportErrors2 && originalErrorInfo && errorInfo) {
                errorInfo ***REMOVED*** countMessageChainBreadth([originalErrorInfo]) <***REMOVED*** countMessageChainBreadth([errorInfo]) ? originalErrorInfo : errorInfo;
              }
            }
          }
        }
        if (reportErrors2) {
          originalErrorInfo ***REMOVED*** void 0;
        }
      } else if (isGenericMappedType(target2) && relation !***REMOVED******REMOVED*** identityRelation) {
        const keysRemapped ***REMOVED*** !!target2.declaration.nameType;
        const templateType ***REMOVED*** getTemplateTypeFromMappedType(target2);
        const modifiers ***REMOVED*** getMappedTypeModifiers(target2);
        if (!(modifiers & 8 /* ExcludeOptional */)) {
          if (!keysRemapped && templateType.flags & 8388608 /* IndexedAccess */ && templateType.objectType ***REMOVED******REMOVED******REMOVED*** source2 && templateType.indexType ***REMOVED******REMOVED******REMOVED*** getTypeParameterFromMappedType(target2)) {
            return -1 /* True */;
          }
          if (!isGenericMappedType(source2)) {
            const targetKeys ***REMOVED*** keysRemapped ? getNameTypeFromMappedType(target2) : getConstraintTypeFromMappedType(target2);
            const sourceKeys ***REMOVED*** getIndexType(source2, 2 /* NoIndexSignatures */);
            const includeOptional ***REMOVED*** modifiers & 4 /* IncludeOptional */;
            const filteredByApplicability ***REMOVED*** includeOptional ? intersectTypes(targetKeys, sourceKeys) : void 0;
            if (includeOptional ? !(filteredByApplicability.flags & 131072 /* Never */) : isRelatedTo(targetKeys, sourceKeys, 3 /* Both */)) {
              const templateType2 ***REMOVED*** getTemplateTypeFromMappedType(target2);
              const typeParameter ***REMOVED*** getTypeParameterFromMappedType(target2);
              const nonNullComponent ***REMOVED*** extractTypesOfKind(templateType2, ~98304 /* Nullable */);
              if (!keysRemapped && nonNullComponent.flags & 8388608 /* IndexedAccess */ && nonNullComponent.indexType ***REMOVED******REMOVED******REMOVED*** typeParameter) {
                if (result2 ***REMOVED*** isRelatedTo(source2, nonNullComponent.objectType, 2 /* Target */, reportErrors2)) {
                  return result2;
                }
              } else {
                const indexingType ***REMOVED*** keysRemapped ? filteredByApplicability || targetKeys : filteredByApplicability ? getIntersectionType([filteredByApplicability, typeParameter]) : typeParameter;
                const indexedAccessType ***REMOVED*** getIndexedAccessType(source2, indexingType);
                if (result2 ***REMOVED*** isRelatedTo(indexedAccessType, templateType2, 3 /* Both */, reportErrors2)) {
                  return result2;
                }
              }
            }
            originalErrorInfo ***REMOVED*** errorInfo;
            resetErrorInfo(saveErrorInfo);
          }
        }
      } else if (targetFlags & 16777216 /* Conditional */) {
        if (isDeeplyNestedType(target2, targetStack, targetDepth, 10)) {
          return 3 /* Maybe */;
        }
        const c ***REMOVED*** target2;
        if (!c.root.inferTypeParameters && !isDistributionDependent(c.root) && !(source2.flags & 16777216 /* Conditional */ && source2.root ***REMOVED******REMOVED******REMOVED*** c.root)) {
          const skipTrue ***REMOVED*** !isTypeAssignableTo(getPermissiveInstantiation(c.checkType), getPermissiveInstantiation(c.extendsType));
          const skipFalse ***REMOVED*** !skipTrue && isTypeAssignableTo(getRestrictiveInstantiation(c.checkType), getRestrictiveInstantiation(c.extendsType));
          if (result2 ***REMOVED*** skipTrue ? -1 /* True */ : isRelatedTo(
            source2,
            getTrueTypeFromConditionalType(c),
            2 /* Target */,
            /*reportErrors*/
            false,
            /*headMessage*/
            void 0,
            intersectionState
          )) {
            result2 &***REMOVED*** skipFalse ? -1 /* True */ : isRelatedTo(
              source2,
              getFalseTypeFromConditionalType(c),
              2 /* Target */,
              /*reportErrors*/
              false,
              /*headMessage*/
              void 0,
              intersectionState
            );
            if (result2) {
              return result2;
            }
          }
        }
      } else if (targetFlags & 134217728 /* TemplateLiteral */) {
        if (sourceFlags & 134217728 /* TemplateLiteral */) {
          if (relation ***REMOVED******REMOVED******REMOVED*** comparableRelation) {
            return templateLiteralTypesDefinitelyUnrelated(source2, target2) ? 0 /* False */ : -1 /* True */;
          }
          instantiateType(source2, reportUnreliableMapper);
        }
        if (isTypeMatchedByTemplateLiteralType(source2, target2)) {
          return -1 /* True */;
        }
      } else if (target2.flags & 268435456 /* StringMapping */) {
        if (!(source2.flags & 268435456 /* StringMapping */)) {
          if (isMemberOfStringMapping(source2, target2)) {
            return -1 /* True */;
          }
        }
      }
      if (sourceFlags & 8650752 /* TypeVariable */) {
        if (!(sourceFlags & 8388608 /* IndexedAccess */ && targetFlags & 8388608 /* IndexedAccess */)) {
          const constraint ***REMOVED*** getConstraintOfType(source2) || unknownType;
          if (result2 ***REMOVED*** isRelatedTo(
            constraint,
            target2,
            1 /* Source */,
            /*reportErrors*/
            false,
            /*headMessage*/
            void 0,
            intersectionState
          )) {
            return result2;
          } else if (result2 ***REMOVED*** isRelatedTo(
            getTypeWithThisArgument(constraint, source2),
            target2,
            1 /* Source */,
            reportErrors2 && constraint !***REMOVED******REMOVED*** unknownType && !(targetFlags & sourceFlags & 262144 /* TypeParameter */),
            /*headMessage*/
            void 0,
            intersectionState
          )) {
            return result2;
          }
          if (isMappedTypeGenericIndexedAccess(source2)) {
            const indexConstraint ***REMOVED*** getConstraintOfType(source2.indexType);
            if (indexConstraint) {
              if (result2 ***REMOVED*** isRelatedTo(getIndexedAccessType(source2.objectType, indexConstraint), target2, 1 /* Source */, reportErrors2)) {
                return result2;
              }
            }
          }
        }
      } else if (sourceFlags & 4194304 /* Index */) {
        const isDeferredMappedIndex ***REMOVED*** shouldDeferIndexType(source2.type, source2.indexFlags) && getObjectFlags(source2.type) & 32 /* Mapped */;
        if (result2 ***REMOVED*** isRelatedTo(stringNumberSymbolType, target2, 1 /* Source */, reportErrors2 && !isDeferredMappedIndex)) {
          return result2;
        }
        if (isDeferredMappedIndex) {
          const mappedType ***REMOVED*** source2.type;
          const nameType ***REMOVED*** getNameTypeFromMappedType(mappedType);
          const sourceMappedKeys ***REMOVED*** nameType && isMappedTypeWithKeyofConstraintDeclaration(mappedType) ? getApparentMappedTypeKeys(nameType, mappedType) : nameType || getConstraintTypeFromMappedType(mappedType);
          if (result2 ***REMOVED*** isRelatedTo(sourceMappedKeys, target2, 1 /* Source */, reportErrors2)) {
            return result2;
          }
        }
      } else if (sourceFlags & 134217728 /* TemplateLiteral */ && !(targetFlags & 524288 /* Object */)) {
        if (!(targetFlags & 134217728 /* TemplateLiteral */)) {
          const constraint ***REMOVED*** getBaseConstraintOfType(source2);
          if (constraint && constraint !***REMOVED******REMOVED*** source2 && (result2 ***REMOVED*** isRelatedTo(constraint, target2, 1 /* Source */, reportErrors2))) {
            return result2;
          }
        }
      } else if (sourceFlags & 268435456 /* StringMapping */) {
        if (targetFlags & 268435456 /* StringMapping */) {
          if (source2.symbol !***REMOVED******REMOVED*** target2.symbol) {
            return 0 /* False */;
          }
          if (result2 ***REMOVED*** isRelatedTo(source2.type, target2.type, 3 /* Both */, reportErrors2)) {
            return result2;
          }
        } else {
          const constraint ***REMOVED*** getBaseConstraintOfType(source2);
          if (constraint && (result2 ***REMOVED*** isRelatedTo(constraint, target2, 1 /* Source */, reportErrors2))) {
            return result2;
          }
        }
      } else if (sourceFlags & 16777216 /* Conditional */) {
        if (isDeeplyNestedType(source2, sourceStack, sourceDepth, 10)) {
          return 3 /* Maybe */;
        }
        if (targetFlags & 16777216 /* Conditional */) {
          const sourceParams ***REMOVED*** source2.root.inferTypeParameters;
          let sourceExtends ***REMOVED*** source2.extendsType;
          let mapper;
          if (sourceParams) {
            const ctx ***REMOVED*** createInferenceContext(
              sourceParams,
              /*signature*/
              void 0,
              0 /* None */,
              isRelatedToWorker
            );
            inferTypes(ctx.inferences, target2.extendsType, sourceExtends, 512 /* NoConstraints */ | 1024 /* AlwaysStrict */);
            sourceExtends ***REMOVED*** instantiateType(sourceExtends, ctx.mapper);
            mapper ***REMOVED*** ctx.mapper;
          }
          if (isTypeIdenticalTo(sourceExtends, target2.extendsType) && (isRelatedTo(source2.checkType, target2.checkType, 3 /* Both */) || isRelatedTo(target2.checkType, source2.checkType, 3 /* Both */))) {
            if (result2 ***REMOVED*** isRelatedTo(instantiateType(getTrueTypeFromConditionalType(source2), mapper), getTrueTypeFromConditionalType(target2), 3 /* Both */, reportErrors2)) {
              result2 &***REMOVED*** isRelatedTo(getFalseTypeFromConditionalType(source2), getFalseTypeFromConditionalType(target2), 3 /* Both */, reportErrors2);
            }
            if (result2) {
              return result2;
            }
          }
        }
        const defaultConstraint ***REMOVED*** getDefaultConstraintOfConditionalType(source2);
        if (defaultConstraint) {
          if (result2 ***REMOVED*** isRelatedTo(defaultConstraint, target2, 1 /* Source */, reportErrors2)) {
            return result2;
          }
        }
        const distributiveConstraint ***REMOVED*** !(targetFlags & 16777216 /* Conditional */) && hasNonCircularBaseConstraint(source2) ? getConstraintOfDistributiveConditionalType(source2) : void 0;
        if (distributiveConstraint) {
          resetErrorInfo(saveErrorInfo);
          if (result2 ***REMOVED*** isRelatedTo(distributiveConstraint, target2, 1 /* Source */, reportErrors2)) {
            return result2;
          }
        }
      } else {
        if (relation !***REMOVED******REMOVED*** subtypeRelation && relation !***REMOVED******REMOVED*** strictSubtypeRelation && isPartialMappedType(target2) && isEmptyObjectType(source2)) {
          return -1 /* True */;
        }
        if (isGenericMappedType(target2)) {
          if (isGenericMappedType(source2)) {
            if (result2 ***REMOVED*** mappedTypeRelatedTo(source2, target2, reportErrors2)) {
              return result2;
            }
          }
          return 0 /* False */;
        }
        const sourceIsPrimitive ***REMOVED*** !!(sourceFlags & 402784252 /* Primitive */);
        if (relation !***REMOVED******REMOVED*** identityRelation) {
          source2 ***REMOVED*** getApparentType(source2);
          sourceFlags ***REMOVED*** source2.flags;
        } else if (isGenericMappedType(source2)) {
          return 0 /* False */;
        }
        if (getObjectFlags(source2) & 4 /* Reference */ && getObjectFlags(target2) & 4 /* Reference */ && source2.target ***REMOVED******REMOVED******REMOVED*** target2.target && !isTupleType(source2) && !(isMarkerType(source2) || isMarkerType(target2))) {
          if (isEmptyArrayLiteralType(source2)) {
            return -1 /* True */;
          }
          const variances ***REMOVED*** getVariances(source2.target);
          if (variances ***REMOVED******REMOVED******REMOVED*** emptyArray) {
            return 1 /* Unknown */;
          }
          const varianceResult ***REMOVED*** relateVariances(getTypeArguments(source2), getTypeArguments(target2), variances, intersectionState);
          if (varianceResult !***REMOVED******REMOVED*** void 0) {
            return varianceResult;
          }
        } else if (isReadonlyArrayType(target2) ? everyType(source2, isArrayOrTupleType) : isArrayType(target2) && everyType(source2, (t) ***REMOVED***> isTupleType(t) && !t.target.readonly)) {
          if (relation !***REMOVED******REMOVED*** identityRelation) {
            return isRelatedTo(getIndexTypeOfType(source2, numberType) || anyType, getIndexTypeOfType(target2, numberType) || anyType, 3 /* Both */, reportErrors2);
          } else {
            return 0 /* False */;
          }
        } else if (isGenericTupleType(source2) && isTupleType(target2) && !isGenericTupleType(target2)) {
          const constraint ***REMOVED*** getBaseConstraintOrType(source2);
          if (constraint !***REMOVED******REMOVED*** source2) {
            return isRelatedTo(constraint, target2, 1 /* Source */, reportErrors2);
          }
        } else if ((relation ***REMOVED******REMOVED******REMOVED*** subtypeRelation || relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation) && isEmptyObjectType(target2) && getObjectFlags(target2) & 8192 /* FreshLiteral */ && !isEmptyObjectType(source2)) {
          return 0 /* False */;
        }
        if (sourceFlags & (524288 /* Object */ | 2097152 /* Intersection */) && targetFlags & 524288 /* Object */) {
          const reportStructuralErrors ***REMOVED*** reportErrors2 && errorInfo ***REMOVED******REMOVED******REMOVED*** saveErrorInfo.errorInfo && !sourceIsPrimitive;
          result2 ***REMOVED*** propertiesRelatedTo(
            source2,
            target2,
            reportStructuralErrors,
            /*excludedProperties*/
            void 0,
            /*optionalsOnly*/
            false,
            intersectionState
          );
          if (result2) {
            result2 &***REMOVED*** signaturesRelatedTo(source2, target2, 0 /* Call */, reportStructuralErrors, intersectionState);
            if (result2) {
              result2 &***REMOVED*** signaturesRelatedTo(source2, target2, 1 /* Construct */, reportStructuralErrors, intersectionState);
              if (result2) {
                result2 &***REMOVED*** indexSignaturesRelatedTo(source2, target2, sourceIsPrimitive, reportStructuralErrors, intersectionState);
              }
            }
          }
          if (varianceCheckFailed && result2) {
            errorInfo ***REMOVED*** originalErrorInfo || errorInfo || saveErrorInfo.errorInfo;
          } else if (result2) {
            return result2;
          }
        }
        if (sourceFlags & (524288 /* Object */ | 2097152 /* Intersection */) && targetFlags & 1048576 /* Union */) {
          const objectOnlyTarget ***REMOVED*** extractTypesOfKind(target2, 524288 /* Object */ | 2097152 /* Intersection */ | 33554432 /* Substitution */);
          if (objectOnlyTarget.flags & 1048576 /* Union */) {
            const result3 ***REMOVED*** typeRelatedToDiscriminatedType(source2, objectOnlyTarget);
            if (result3) {
              return result3;
            }
          }
        }
      }
      return 0 /* False */;
      function countMessageChainBreadth(info) {
        if (!info) return 0;
        return reduceLeft(info, (value, chain) ***REMOVED***> value + 1 + countMessageChainBreadth(chain.next), 0);
      }
      function relateVariances(sourceTypeArguments, targetTypeArguments, variances, intersectionState2) {
        if (result2 ***REMOVED*** typeArgumentsRelatedTo(sourceTypeArguments, targetTypeArguments, variances, reportErrors2, intersectionState2)) {
          return result2;
        }
        if (some(variances, (v) ***REMOVED***> !!(v & 24 /* AllowsStructuralFallback */))) {
          originalErrorInfo ***REMOVED*** void 0;
          resetErrorInfo(saveErrorInfo);
          return void 0;
        }
        const allowStructuralFallback ***REMOVED*** targetTypeArguments && hasCovariantVoidArgument(targetTypeArguments, variances);
        varianceCheckFailed ***REMOVED*** !allowStructuralFallback;
        if (variances !***REMOVED******REMOVED*** emptyArray && !allowStructuralFallback) {
          if (varianceCheckFailed && !(reportErrors2 && some(variances, (v) ***REMOVED***> (v & 7 /* VarianceMask */) ***REMOVED******REMOVED******REMOVED*** 0 /* Invariant */))) {
            return 0 /* False */;
          }
          originalErrorInfo ***REMOVED*** errorInfo;
          resetErrorInfo(saveErrorInfo);
        }
      }
    }
    function mappedTypeRelatedTo(source2, target2, reportErrors2) {
      const modifiersRelated ***REMOVED*** relation ***REMOVED******REMOVED******REMOVED*** comparableRelation || (relation ***REMOVED******REMOVED******REMOVED*** identityRelation ? getMappedTypeModifiers(source2) ***REMOVED******REMOVED******REMOVED*** getMappedTypeModifiers(target2) : getCombinedMappedTypeOptionality(source2) <***REMOVED*** getCombinedMappedTypeOptionality(target2));
      if (modifiersRelated) {
        let result2;
        const targetConstraint ***REMOVED*** getConstraintTypeFromMappedType(target2);
        const sourceConstraint ***REMOVED*** instantiateType(getConstraintTypeFromMappedType(source2), getCombinedMappedTypeOptionality(source2) < 0 ? reportUnmeasurableMapper : reportUnreliableMapper);
        if (result2 ***REMOVED*** isRelatedTo(targetConstraint, sourceConstraint, 3 /* Both */, reportErrors2)) {
          const mapper ***REMOVED*** createTypeMapper([getTypeParameterFromMappedType(source2)], [getTypeParameterFromMappedType(target2)]);
          if (instantiateType(getNameTypeFromMappedType(source2), mapper) ***REMOVED******REMOVED******REMOVED*** instantiateType(getNameTypeFromMappedType(target2), mapper)) {
            return result2 & isRelatedTo(instantiateType(getTemplateTypeFromMappedType(source2), mapper), getTemplateTypeFromMappedType(target2), 3 /* Both */, reportErrors2);
          }
        }
      }
      return 0 /* False */;
    }
    function typeRelatedToDiscriminatedType(source2, target2) {
      var _a2;
      const sourceProperties ***REMOVED*** getPropertiesOfType(source2);
      const sourcePropertiesFiltered ***REMOVED*** findDiscriminantProperties(sourceProperties, target2);
      if (!sourcePropertiesFiltered) return 0 /* False */;
      let numCombinations ***REMOVED*** 1;
      for (const sourceProperty of sourcePropertiesFiltered) {
        numCombinations ****REMOVED*** countTypes(getNonMissingTypeOfSymbol(sourceProperty));
        if (numCombinations > 25) {
          (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.instant(tracing.Phase.CheckTypes, "typeRelatedToDiscriminatedType_DepthLimit", { sourceId: source2.id, targetId: target2.id, numCombinations });
          return 0 /* False */;
        }
      }
      const sourceDiscriminantTypes ***REMOVED*** new Array(sourcePropertiesFiltered.length);
      const excludedProperties ***REMOVED*** /* @__PURE__ */ new Set();
      for (let i ***REMOVED*** 0; i < sourcePropertiesFiltered.length; i++) {
        const sourceProperty ***REMOVED*** sourcePropertiesFiltered[i];
        const sourcePropertyType ***REMOVED*** getNonMissingTypeOfSymbol(sourceProperty);
        sourceDiscriminantTypes[i] ***REMOVED*** sourcePropertyType.flags & 1048576 /* Union */ ? sourcePropertyType.types : [sourcePropertyType];
        excludedProperties.add(sourceProperty.escapedName);
      }
      const discriminantCombinations ***REMOVED*** cartesianProduct(sourceDiscriminantTypes);
      const matchingTypes ***REMOVED*** [];
      for (const combination of discriminantCombinations) {
        let hasMatch ***REMOVED*** false;
        outer:
          for (const type of target2.types) {
            for (let i ***REMOVED*** 0; i < sourcePropertiesFiltered.length; i++) {
              const sourceProperty ***REMOVED*** sourcePropertiesFiltered[i];
              const targetProperty ***REMOVED*** getPropertyOfType(type, sourceProperty.escapedName);
              if (!targetProperty) continue outer;
              if (sourceProperty ***REMOVED******REMOVED******REMOVED*** targetProperty) continue;
              const related ***REMOVED*** propertyRelatedTo(
                source2,
                target2,
                sourceProperty,
                targetProperty,
                (_) ***REMOVED***> combination[i],
                /*reportErrors*/
                false,
                0 /* None */,
                /*skipOptional*/
                strictNullChecks || relation ***REMOVED******REMOVED******REMOVED*** comparableRelation
              );
              if (!related) {
                continue outer;
              }
            }
            pushIfUnique(matchingTypes, type, equateValues);
            hasMatch ***REMOVED*** true;
          }
        if (!hasMatch) {
          return 0 /* False */;
        }
      }
      let result2 ***REMOVED*** -1 /* True */;
      for (const type of matchingTypes) {
        result2 &***REMOVED*** propertiesRelatedTo(
          source2,
          type,
          /*reportErrors*/
          false,
          excludedProperties,
          /*optionalsOnly*/
          false,
          0 /* None */
        );
        if (result2) {
          result2 &***REMOVED*** signaturesRelatedTo(
            source2,
            type,
            0 /* Call */,
            /*reportErrors*/
            false,
            0 /* None */
          );
          if (result2) {
            result2 &***REMOVED*** signaturesRelatedTo(
              source2,
              type,
              1 /* Construct */,
              /*reportErrors*/
              false,
              0 /* None */
            );
            if (result2 && !(isTupleType(source2) && isTupleType(type))) {
              result2 &***REMOVED*** indexSignaturesRelatedTo(
                source2,
                type,
                /*sourceIsPrimitive*/
                false,
                /*reportErrors*/
                false,
                0 /* None */
              );
            }
          }
        }
        if (!result2) {
          return result2;
        }
      }
      return result2;
    }
    function excludeProperties(properties, excludedProperties) {
      if (!excludedProperties || properties.length ***REMOVED******REMOVED******REMOVED*** 0) return properties;
      let result2;
      for (let i ***REMOVED*** 0; i < properties.length; i++) {
        if (!excludedProperties.has(properties[i].escapedName)) {
          if (result2) {
            result2.push(properties[i]);
          }
        } else if (!result2) {
          result2 ***REMOVED*** properties.slice(0, i);
        }
      }
      return result2 || properties;
    }
    function isPropertySymbolTypeRelated(sourceProp, targetProp, getTypeOfSourceProperty, reportErrors2, intersectionState) {
      const targetIsOptional ***REMOVED*** strictNullChecks && !!(getCheckFlags(targetProp) & 48 /* Partial */);
      const effectiveTarget ***REMOVED*** addOptionality(
        getNonMissingTypeOfSymbol(targetProp),
        /*isProperty*/
        false,
        targetIsOptional
      );
      if (effectiveTarget.flags & (relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation ? 1 /* Any */ : 3 /* AnyOrUnknown */)) {
        return -1 /* True */;
      }
      const effectiveSource ***REMOVED*** getTypeOfSourceProperty(sourceProp);
      return isRelatedTo(
        effectiveSource,
        effectiveTarget,
        3 /* Both */,
        reportErrors2,
        /*headMessage*/
        void 0,
        intersectionState
      );
    }
    function propertyRelatedTo(source2, target2, sourceProp, targetProp, getTypeOfSourceProperty, reportErrors2, intersectionState, skipOptional) {
      const sourcePropFlags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(sourceProp);
      const targetPropFlags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(targetProp);
      if (sourcePropFlags & 2 /* Private */ || targetPropFlags & 2 /* Private */) {
        if (sourceProp.valueDeclaration !***REMOVED******REMOVED*** targetProp.valueDeclaration) {
          if (reportErrors2) {
            if (sourcePropFlags & 2 /* Private */ && targetPropFlags & 2 /* Private */) {
              reportError(Diagnostics.Types_have_separate_declarations_of_a_private_property_0, symbolToString(targetProp));
            } else {
              reportError(Diagnostics.Property_0_is_private_in_type_1_but_not_in_type_2, symbolToString(targetProp), typeToString(sourcePropFlags & 2 /* Private */ ? source2 : target2), typeToString(sourcePropFlags & 2 /* Private */ ? target2 : source2));
            }
          }
          return 0 /* False */;
        }
      } else if (targetPropFlags & 4 /* Protected */) {
        if (!isValidOverrideOf(sourceProp, targetProp)) {
          if (reportErrors2) {
            reportError(Diagnostics.Property_0_is_protected_but_type_1_is_not_a_class_derived_from_2, symbolToString(targetProp), typeToString(getDeclaringClass(sourceProp) || source2), typeToString(getDeclaringClass(targetProp) || target2));
          }
          return 0 /* False */;
        }
      } else if (sourcePropFlags & 4 /* Protected */) {
        if (reportErrors2) {
          reportError(Diagnostics.Property_0_is_protected_in_type_1_but_public_in_type_2, symbolToString(targetProp), typeToString(source2), typeToString(target2));
        }
        return 0 /* False */;
      }
      if (relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation && isReadonlySymbol(sourceProp) && !isReadonlySymbol(targetProp)) {
        return 0 /* False */;
      }
      const related ***REMOVED*** isPropertySymbolTypeRelated(sourceProp, targetProp, getTypeOfSourceProperty, reportErrors2, intersectionState);
      if (!related) {
        if (reportErrors2) {
          reportIncompatibleError(Diagnostics.Types_of_property_0_are_incompatible, symbolToString(targetProp));
        }
        return 0 /* False */;
      }
      if (!skipOptional && sourceProp.flags & 16777216 /* Optional */ && targetProp.flags & 106500 /* ClassMember */ && !(targetProp.flags & 16777216 /* Optional */)) {
        if (reportErrors2) {
          reportError(Diagnostics.Property_0_is_optional_in_type_1_but_required_in_type_2, symbolToString(targetProp), typeToString(source2), typeToString(target2));
        }
        return 0 /* False */;
      }
      return related;
    }
    function reportUnmatchedProperty(source2, target2, unmatchedProperty, requireOptionalProperties) {
      let shouldSkipElaboration ***REMOVED*** false;
      if (unmatchedProperty.valueDeclaration && isNamedDeclaration(unmatchedProperty.valueDeclaration) && isPrivateIdentifier(unmatchedProperty.valueDeclaration.name) && source2.symbol && source2.symbol.flags & 32 /* Class */) {
        const privateIdentifierDescription ***REMOVED*** unmatchedProperty.valueDeclaration.name.escapedText;
        const symbolTableKey ***REMOVED*** getSymbolNameForPrivateIdentifier(source2.symbol, privateIdentifierDescription);
        if (symbolTableKey && getPropertyOfType(source2, symbolTableKey)) {
          const sourceName ***REMOVED*** factory.getDeclarationName(source2.symbol.valueDeclaration);
          const targetName ***REMOVED*** factory.getDeclarationName(target2.symbol.valueDeclaration);
          reportError(
            Diagnostics.Property_0_in_type_1_refers_to_a_different_member_that_cannot_be_accessed_from_within_type_2,
            diagnosticName(privateIdentifierDescription),
            diagnosticName(sourceName.escapedText ***REMOVED******REMOVED******REMOVED*** "" ? anon : sourceName),
            diagnosticName(targetName.escapedText ***REMOVED******REMOVED******REMOVED*** "" ? anon : targetName)
          );
          return;
        }
      }
      const props ***REMOVED*** arrayFrom(getUnmatchedProperties(
        source2,
        target2,
        requireOptionalProperties,
        /*matchDiscriminantProperties*/
        false
      ));
      if (!headMessage || headMessage.code !***REMOVED******REMOVED*** Diagnostics.Class_0_incorrectly_implements_interface_1.code && headMessage.code !***REMOVED******REMOVED*** Diagnostics.Class_0_incorrectly_implements_class_1_Did_you_mean_to_extend_1_and_inherit_its_members_as_a_subclass.code) {
        shouldSkipElaboration ***REMOVED*** true;
      }
      if (props.length ***REMOVED******REMOVED******REMOVED*** 1) {
        const propName ***REMOVED*** symbolToString(
          unmatchedProperty,
          /*enclosingDeclaration*/
          void 0,
          0 /* None */,
          4 /* AllowAnyNodeKind */ | 16 /* WriteComputedProps */
        );
        reportError(Diagnostics.Property_0_is_missing_in_type_1_but_required_in_type_2, propName, ...getTypeNamesForErrorDisplay(source2, target2));
        if (length(unmatchedProperty.declarations)) {
          associateRelatedInfo(createDiagnosticForNode(unmatchedProperty.declarations[0], Diagnostics._0_is_declared_here, propName));
        }
        if (shouldSkipElaboration && errorInfo) {
          overrideNextErrorInfo++;
        }
      } else if (tryElaborateArrayLikeErrors(
        source2,
        target2,
        /*reportErrors*/
        false
      )) {
        if (props.length > 5) {
          reportError(Diagnostics.Type_0_is_missing_the_following_properties_from_type_1_Colon_2_and_3_more, typeToString(source2), typeToString(target2), map(props.slice(0, 4), (p) ***REMOVED***> symbolToString(p)).join(", "), props.length - 4);
        } else {
          reportError(Diagnostics.Type_0_is_missing_the_following_properties_from_type_1_Colon_2, typeToString(source2), typeToString(target2), map(props, (p) ***REMOVED***> symbolToString(p)).join(", "));
        }
        if (shouldSkipElaboration && errorInfo) {
          overrideNextErrorInfo++;
        }
      }
    }
    function propertiesRelatedTo(source2, target2, reportErrors2, excludedProperties, optionalsOnly, intersectionState) {
      if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        return propertiesIdenticalTo(source2, target2, excludedProperties);
      }
      let result2 ***REMOVED*** -1 /* True */;
      if (isTupleType(target2)) {
        if (isArrayOrTupleType(source2)) {
          if (!target2.target.readonly && (isReadonlyArrayType(source2) || isTupleType(source2) && source2.target.readonly)) {
            return 0 /* False */;
          }
          const sourceArity ***REMOVED*** getTypeReferenceArity(source2);
          const targetArity ***REMOVED*** getTypeReferenceArity(target2);
          const sourceRestFlag ***REMOVED*** isTupleType(source2) ? source2.target.combinedFlags & 4 /* Rest */ : 4 /* Rest */;
          const targetHasRestElement ***REMOVED*** !!(target2.target.combinedFlags & 12 /* Variable */);
          const sourceMinLength ***REMOVED*** isTupleType(source2) ? source2.target.minLength : 0;
          const targetMinLength ***REMOVED*** target2.target.minLength;
          if (!sourceRestFlag && sourceArity < targetMinLength) {
            if (reportErrors2) {
              reportError(Diagnostics.Source_has_0_element_s_but_target_requires_1, sourceArity, targetMinLength);
            }
            return 0 /* False */;
          }
          if (!targetHasRestElement && targetArity < sourceMinLength) {
            if (reportErrors2) {
              reportError(Diagnostics.Source_has_0_element_s_but_target_allows_only_1, sourceMinLength, targetArity);
            }
            return 0 /* False */;
          }
          if (!targetHasRestElement && (sourceRestFlag || targetArity < sourceArity)) {
            if (reportErrors2) {
              if (sourceMinLength < targetMinLength) {
                reportError(Diagnostics.Target_requires_0_element_s_but_source_may_have_fewer, targetMinLength);
              } else {
                reportError(Diagnostics.Target_allows_only_0_element_s_but_source_may_have_more, targetArity);
              }
            }
            return 0 /* False */;
          }
          const sourceTypeArguments ***REMOVED*** getTypeArguments(source2);
          const targetTypeArguments ***REMOVED*** getTypeArguments(target2);
          const targetStartCount ***REMOVED*** getStartElementCount(target2.target, 11 /* NonRest */);
          const targetEndCount ***REMOVED*** getEndElementCount(target2.target, 11 /* NonRest */);
          let canExcludeDiscriminants ***REMOVED*** !!excludedProperties;
          for (let sourcePosition ***REMOVED*** 0; sourcePosition < sourceArity; sourcePosition++) {
            const sourceFlags ***REMOVED*** isTupleType(source2) ? source2.target.elementFlags[sourcePosition] : 4 /* Rest */;
            const sourcePositionFromEnd ***REMOVED*** sourceArity - 1 - sourcePosition;
            const targetPosition ***REMOVED*** targetHasRestElement && sourcePosition >***REMOVED*** targetStartCount ? targetArity - 1 - Math.min(sourcePositionFromEnd, targetEndCount) : sourcePosition;
            const targetFlags ***REMOVED*** target2.target.elementFlags[targetPosition];
            if (targetFlags & 8 /* Variadic */ && !(sourceFlags & 8 /* Variadic */)) {
              if (reportErrors2) {
                reportError(Diagnostics.Source_provides_no_match_for_variadic_element_at_position_0_in_target, targetPosition);
              }
              return 0 /* False */;
            }
            if (sourceFlags & 8 /* Variadic */ && !(targetFlags & 12 /* Variable */)) {
              if (reportErrors2) {
                reportError(Diagnostics.Variadic_element_at_position_0_in_source_does_not_match_element_at_position_1_in_target, sourcePosition, targetPosition);
              }
              return 0 /* False */;
            }
            if (targetFlags & 1 /* Required */ && !(sourceFlags & 1 /* Required */)) {
              if (reportErrors2) {
                reportError(Diagnostics.Source_provides_no_match_for_required_element_at_position_0_in_target, targetPosition);
              }
              return 0 /* False */;
            }
            if (canExcludeDiscriminants) {
              if (sourceFlags & 12 /* Variable */ || targetFlags & 12 /* Variable */) {
                canExcludeDiscriminants ***REMOVED*** false;
              }
              if (canExcludeDiscriminants && (excludedProperties ***REMOVED******REMOVED*** null ? void 0 : excludedProperties.has("" + sourcePosition))) {
                continue;
              }
            }
            const sourceType ***REMOVED*** removeMissingType(sourceTypeArguments[sourcePosition], !!(sourceFlags & targetFlags & 2 /* Optional */));
            const targetType ***REMOVED*** targetTypeArguments[targetPosition];
            const targetCheckType ***REMOVED*** sourceFlags & 8 /* Variadic */ && targetFlags & 4 /* Rest */ ? createArrayType(targetType) : removeMissingType(targetType, !!(targetFlags & 2 /* Optional */));
            const related ***REMOVED*** isRelatedTo(
              sourceType,
              targetCheckType,
              3 /* Both */,
              reportErrors2,
              /*headMessage*/
              void 0,
              intersectionState
            );
            if (!related) {
              if (reportErrors2 && (targetArity > 1 || sourceArity > 1)) {
                if (targetHasRestElement && sourcePosition >***REMOVED*** targetStartCount && sourcePositionFromEnd >***REMOVED*** targetEndCount && targetStartCount !***REMOVED******REMOVED*** sourceArity - targetEndCount - 1) {
                  reportIncompatibleError(Diagnostics.Type_at_positions_0_through_1_in_source_is_not_compatible_with_type_at_position_2_in_target, targetStartCount, sourceArity - targetEndCount - 1, targetPosition);
                } else {
                  reportIncompatibleError(Diagnostics.Type_at_position_0_in_source_is_not_compatible_with_type_at_position_1_in_target, sourcePosition, targetPosition);
                }
              }
              return 0 /* False */;
            }
            result2 &***REMOVED*** related;
          }
          return result2;
        }
        if (target2.target.combinedFlags & 12 /* Variable */) {
          return 0 /* False */;
        }
      }
      const requireOptionalProperties ***REMOVED*** (relation ***REMOVED******REMOVED******REMOVED*** subtypeRelation || relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation) && !isObjectLiteralType(source2) && !isEmptyArrayLiteralType(source2) && !isTupleType(source2);
      const unmatchedProperty ***REMOVED*** getUnmatchedProperty(
        source2,
        target2,
        requireOptionalProperties,
        /*matchDiscriminantProperties*/
        false
      );
      if (unmatchedProperty) {
        if (reportErrors2 && shouldReportUnmatchedPropertyError(source2, target2)) {
          reportUnmatchedProperty(source2, target2, unmatchedProperty, requireOptionalProperties);
        }
        return 0 /* False */;
      }
      if (isObjectLiteralType(target2)) {
        for (const sourceProp of excludeProperties(getPropertiesOfType(source2), excludedProperties)) {
          if (!getPropertyOfObjectType(target2, sourceProp.escapedName)) {
            const sourceType ***REMOVED*** getTypeOfSymbol(sourceProp);
            if (!(sourceType.flags & 32768 /* Undefined */)) {
              if (reportErrors2) {
                reportError(Diagnostics.Property_0_does_not_exist_on_type_1, symbolToString(sourceProp), typeToString(target2));
              }
              return 0 /* False */;
            }
          }
        }
      }
      const properties ***REMOVED*** getPropertiesOfType(target2);
      const numericNamesOnly ***REMOVED*** isTupleType(source2) && isTupleType(target2);
      for (const targetProp of excludeProperties(properties, excludedProperties)) {
        const name ***REMOVED*** targetProp.escapedName;
        if (!(targetProp.flags & 4194304 /* Prototype */) && (!numericNamesOnly || isNumericLiteralName(name) || name ***REMOVED******REMOVED******REMOVED*** "length") && (!optionalsOnly || targetProp.flags & 16777216 /* Optional */)) {
          const sourceProp ***REMOVED*** getPropertyOfType(source2, name);
          if (sourceProp && sourceProp !***REMOVED******REMOVED*** targetProp) {
            const related ***REMOVED*** propertyRelatedTo(source2, target2, sourceProp, targetProp, getNonMissingTypeOfSymbol, reportErrors2, intersectionState, relation ***REMOVED******REMOVED******REMOVED*** comparableRelation);
            if (!related) {
              return 0 /* False */;
            }
            result2 &***REMOVED*** related;
          }
        }
      }
      return result2;
    }
    function propertiesIdenticalTo(source2, target2, excludedProperties) {
      if (!(source2.flags & 524288 /* Object */ && target2.flags & 524288 /* Object */)) {
        return 0 /* False */;
      }
      const sourceProperties ***REMOVED*** excludeProperties(getPropertiesOfObjectType(source2), excludedProperties);
      const targetProperties ***REMOVED*** excludeProperties(getPropertiesOfObjectType(target2), excludedProperties);
      if (sourceProperties.length !***REMOVED******REMOVED*** targetProperties.length) {
        return 0 /* False */;
      }
      let result2 ***REMOVED*** -1 /* True */;
      for (const sourceProp of sourceProperties) {
        const targetProp ***REMOVED*** getPropertyOfObjectType(target2, sourceProp.escapedName);
        if (!targetProp) {
          return 0 /* False */;
        }
        const related ***REMOVED*** compareProperties(sourceProp, targetProp, isRelatedTo);
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function signaturesRelatedTo(source2, target2, kind, reportErrors2, intersectionState) {
      var _a2, _b;
      if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        return signaturesIdenticalTo(source2, target2, kind);
      }
      if (target2 ***REMOVED******REMOVED******REMOVED*** anyFunctionType || source2 ***REMOVED******REMOVED******REMOVED*** anyFunctionType) {
        return -1 /* True */;
      }
      const sourceIsJSConstructor ***REMOVED*** source2.symbol && isJSConstructor(source2.symbol.valueDeclaration);
      const targetIsJSConstructor ***REMOVED*** target2.symbol && isJSConstructor(target2.symbol.valueDeclaration);
      const sourceSignatures ***REMOVED*** getSignaturesOfType(
        source2,
        sourceIsJSConstructor && kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ ? 0 /* Call */ : kind
      );
      const targetSignatures ***REMOVED*** getSignaturesOfType(
        target2,
        targetIsJSConstructor && kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ ? 0 /* Call */ : kind
      );
      if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ && sourceSignatures.length && targetSignatures.length) {
        const sourceIsAbstract ***REMOVED*** !!(sourceSignatures[0].flags & 4 /* Abstract */);
        const targetIsAbstract ***REMOVED*** !!(targetSignatures[0].flags & 4 /* Abstract */);
        if (sourceIsAbstract && !targetIsAbstract) {
          if (reportErrors2) {
            reportError(Diagnostics.Cannot_assign_an_abstract_constructor_type_to_a_non_abstract_constructor_type);
          }
          return 0 /* False */;
        }
        if (!constructorVisibilitiesAreCompatible(sourceSignatures[0], targetSignatures[0], reportErrors2)) {
          return 0 /* False */;
        }
      }
      let result2 ***REMOVED*** -1 /* True */;
      const incompatibleReporter ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ ? reportIncompatibleConstructSignatureReturn : reportIncompatibleCallSignatureReturn;
      const sourceObjectFlags ***REMOVED*** getObjectFlags(source2);
      const targetObjectFlags ***REMOVED*** getObjectFlags(target2);
      if (sourceObjectFlags & 64 /* Instantiated */ && targetObjectFlags & 64 /* Instantiated */ && source2.symbol ***REMOVED******REMOVED******REMOVED*** target2.symbol || sourceObjectFlags & 4 /* Reference */ && targetObjectFlags & 4 /* Reference */ && source2.target ***REMOVED******REMOVED******REMOVED*** target2.target) {
        Debug.assertEqual(sourceSignatures.length, targetSignatures.length);
        for (let i ***REMOVED*** 0; i < targetSignatures.length; i++) {
          const related ***REMOVED*** signatureRelatedTo(
            sourceSignatures[i],
            targetSignatures[i],
            /*erase*/
            true,
            reportErrors2,
            intersectionState,
            incompatibleReporter(sourceSignatures[i], targetSignatures[i])
          );
          if (!related) {
            return 0 /* False */;
          }
          result2 &***REMOVED*** related;
        }
      } else if (sourceSignatures.length ***REMOVED******REMOVED******REMOVED*** 1 && targetSignatures.length ***REMOVED******REMOVED******REMOVED*** 1) {
        const eraseGenerics ***REMOVED*** relation ***REMOVED******REMOVED******REMOVED*** comparableRelation;
        const sourceSignature ***REMOVED*** first(sourceSignatures);
        const targetSignature ***REMOVED*** first(targetSignatures);
        result2 ***REMOVED*** signatureRelatedTo(sourceSignature, targetSignature, eraseGenerics, reportErrors2, intersectionState, incompatibleReporter(sourceSignature, targetSignature));
        if (!result2 && reportErrors2 && kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ && sourceObjectFlags & targetObjectFlags && (((_a2 ***REMOVED*** targetSignature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a2.kind) ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || ((_b ***REMOVED*** sourceSignature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _b.kind) ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */)) {
          const constructSignatureToString ***REMOVED*** (signature) ***REMOVED***> signatureToString(
            signature,
            /*enclosingDeclaration*/
            void 0,
            262144 /* WriteArrowStyleSignature */,
            kind
          );
          reportError(Diagnostics.Type_0_is_not_assignable_to_type_1, constructSignatureToString(sourceSignature), constructSignatureToString(targetSignature));
          reportError(Diagnostics.Types_of_construct_signatures_are_incompatible);
          return result2;
        }
      } else {
        outer:
          for (const t of targetSignatures) {
            const saveErrorInfo ***REMOVED*** captureErrorCalculationState();
            let shouldElaborateErrors ***REMOVED*** reportErrors2;
            for (const s of sourceSignatures) {
              const related ***REMOVED*** signatureRelatedTo(
                s,
                t,
                /*erase*/
                true,
                shouldElaborateErrors,
                intersectionState,
                incompatibleReporter(s, t)
              );
              if (related) {
                result2 &***REMOVED*** related;
                resetErrorInfo(saveErrorInfo);
                continue outer;
              }
              shouldElaborateErrors ***REMOVED*** false;
            }
            if (shouldElaborateErrors) {
              reportError(Diagnostics.Type_0_provides_no_match_for_the_signature_1, typeToString(source2), signatureToString(
                t,
                /*enclosingDeclaration*/
                void 0,
                /*flags*/
                void 0,
                kind
              ));
            }
            return 0 /* False */;
          }
      }
      return result2;
    }
    function shouldReportUnmatchedPropertyError(source2, target2) {
      const typeCallSignatures ***REMOVED*** getSignaturesOfStructuredType(source2, 0 /* Call */);
      const typeConstructSignatures ***REMOVED*** getSignaturesOfStructuredType(source2, 1 /* Construct */);
      const typeProperties ***REMOVED*** getPropertiesOfObjectType(source2);
      if ((typeCallSignatures.length || typeConstructSignatures.length) && !typeProperties.length) {
        if (getSignaturesOfType(target2, 0 /* Call */).length && typeCallSignatures.length || getSignaturesOfType(target2, 1 /* Construct */).length && typeConstructSignatures.length) {
          return true;
        }
        return false;
      }
      return true;
    }
    function reportIncompatibleCallSignatureReturn(siga, sigb) {
      if (siga.parameters.length ***REMOVED******REMOVED******REMOVED*** 0 && sigb.parameters.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return (source2, target2) ***REMOVED***> reportIncompatibleError(Diagnostics.Call_signatures_with_no_arguments_have_incompatible_return_types_0_and_1, typeToString(source2), typeToString(target2));
      }
      return (source2, target2) ***REMOVED***> reportIncompatibleError(Diagnostics.Call_signature_return_types_0_and_1_are_incompatible, typeToString(source2), typeToString(target2));
    }
    function reportIncompatibleConstructSignatureReturn(siga, sigb) {
      if (siga.parameters.length ***REMOVED******REMOVED******REMOVED*** 0 && sigb.parameters.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return (source2, target2) ***REMOVED***> reportIncompatibleError(Diagnostics.Construct_signatures_with_no_arguments_have_incompatible_return_types_0_and_1, typeToString(source2), typeToString(target2));
      }
      return (source2, target2) ***REMOVED***> reportIncompatibleError(Diagnostics.Construct_signature_return_types_0_and_1_are_incompatible, typeToString(source2), typeToString(target2));
    }
    function signatureRelatedTo(source2, target2, erase, reportErrors2, intersectionState, incompatibleReporter) {
      const checkMode ***REMOVED*** relation ***REMOVED******REMOVED******REMOVED*** subtypeRelation ? 16 /* StrictTopSignature */ : relation ***REMOVED******REMOVED******REMOVED*** strictSubtypeRelation ? 16 /* StrictTopSignature */ | 8 /* StrictArity */ : 0 /* None */;
      return compareSignaturesRelated(erase ? getErasedSignature(source2) : source2, erase ? getErasedSignature(target2) : target2, checkMode, reportErrors2, reportError, incompatibleReporter, isRelatedToWorker2, reportUnreliableMapper);
      function isRelatedToWorker2(source3, target3, reportErrors3) {
        return isRelatedTo(
          source3,
          target3,
          3 /* Both */,
          reportErrors3,
          /*headMessage*/
          void 0,
          intersectionState
        );
      }
    }
    function signaturesIdenticalTo(source2, target2, kind) {
      const sourceSignatures ***REMOVED*** getSignaturesOfType(source2, kind);
      const targetSignatures ***REMOVED*** getSignaturesOfType(target2, kind);
      if (sourceSignatures.length !***REMOVED******REMOVED*** targetSignatures.length) {
        return 0 /* False */;
      }
      let result2 ***REMOVED*** -1 /* True */;
      for (let i ***REMOVED*** 0; i < sourceSignatures.length; i++) {
        const related ***REMOVED*** compareSignaturesIdentical(
          sourceSignatures[i],
          targetSignatures[i],
          /*partialMatch*/
          false,
          /*ignoreThisTypes*/
          false,
          /*ignoreReturnTypes*/
          false,
          isRelatedTo
        );
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function membersRelatedToIndexInfo(source2, targetInfo, reportErrors2, intersectionState) {
      let result2 ***REMOVED*** -1 /* True */;
      const keyType ***REMOVED*** targetInfo.keyType;
      const props ***REMOVED*** source2.flags & 2097152 /* Intersection */ ? getPropertiesOfUnionOrIntersectionType(source2) : getPropertiesOfObjectType(source2);
      for (const prop of props) {
        if (isIgnoredJsxProperty(source2, prop)) {
          continue;
        }
        if (isApplicableIndexType(getLiteralTypeFromProperty(prop, 8576 /* StringOrNumberLiteralOrUnique */), keyType)) {
          const propType ***REMOVED*** getNonMissingTypeOfSymbol(prop);
          const type ***REMOVED*** exactOptionalPropertyTypes || propType.flags & 32768 /* Undefined */ || keyType ***REMOVED******REMOVED******REMOVED*** numberType || !(prop.flags & 16777216 /* Optional */) ? propType : getTypeWithFacts(propType, 524288 /* NEUndefined */);
          const related ***REMOVED*** isRelatedTo(
            type,
            targetInfo.type,
            3 /* Both */,
            reportErrors2,
            /*headMessage*/
            void 0,
            intersectionState
          );
          if (!related) {
            if (reportErrors2) {
              reportError(Diagnostics.Property_0_is_incompatible_with_index_signature, symbolToString(prop));
            }
            return 0 /* False */;
          }
          result2 &***REMOVED*** related;
        }
      }
      for (const info of getIndexInfosOfType(source2)) {
        if (isApplicableIndexType(info.keyType, keyType)) {
          const related ***REMOVED*** indexInfoRelatedTo(info, targetInfo, reportErrors2, intersectionState);
          if (!related) {
            return 0 /* False */;
          }
          result2 &***REMOVED*** related;
        }
      }
      return result2;
    }
    function indexInfoRelatedTo(sourceInfo, targetInfo, reportErrors2, intersectionState) {
      const related ***REMOVED*** isRelatedTo(
        sourceInfo.type,
        targetInfo.type,
        3 /* Both */,
        reportErrors2,
        /*headMessage*/
        void 0,
        intersectionState
      );
      if (!related && reportErrors2) {
        if (sourceInfo.keyType ***REMOVED******REMOVED******REMOVED*** targetInfo.keyType) {
          reportError(Diagnostics._0_index_signatures_are_incompatible, typeToString(sourceInfo.keyType));
        } else {
          reportError(Diagnostics._0_and_1_index_signatures_are_incompatible, typeToString(sourceInfo.keyType), typeToString(targetInfo.keyType));
        }
      }
      return related;
    }
    function indexSignaturesRelatedTo(source2, target2, sourceIsPrimitive, reportErrors2, intersectionState) {
      if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation) {
        return indexSignaturesIdenticalTo(source2, target2);
      }
      const indexInfos ***REMOVED*** getIndexInfosOfType(target2);
      const targetHasStringIndex ***REMOVED*** some(indexInfos, (info) ***REMOVED***> info.keyType ***REMOVED******REMOVED******REMOVED*** stringType);
      let result2 ***REMOVED*** -1 /* True */;
      for (const targetInfo of indexInfos) {
        const related ***REMOVED*** relation !***REMOVED******REMOVED*** strictSubtypeRelation && !sourceIsPrimitive && targetHasStringIndex && targetInfo.type.flags & 1 /* Any */ ? -1 /* True */ : isGenericMappedType(source2) && targetHasStringIndex ? isRelatedTo(getTemplateTypeFromMappedType(source2), targetInfo.type, 3 /* Both */, reportErrors2) : typeRelatedToIndexInfo(source2, targetInfo, reportErrors2, intersectionState);
        if (!related) {
          return 0 /* False */;
        }
        result2 &***REMOVED*** related;
      }
      return result2;
    }
    function typeRelatedToIndexInfo(source2, targetInfo, reportErrors2, intersectionState) {
      const sourceInfo ***REMOVED*** getApplicableIndexInfo(source2, targetInfo.keyType);
      if (sourceInfo) {
        return indexInfoRelatedTo(sourceInfo, targetInfo, reportErrors2, intersectionState);
      }
      if (!(intersectionState & 1 /* Source */) && (relation !***REMOVED******REMOVED*** strictSubtypeRelation || getObjectFlags(source2) & 8192 /* FreshLiteral */) && isObjectTypeWithInferableIndex(source2)) {
        return membersRelatedToIndexInfo(source2, targetInfo, reportErrors2, intersectionState);
      }
      if (reportErrors2) {
        reportError(Diagnostics.Index_signature_for_type_0_is_missing_in_type_1, typeToString(targetInfo.keyType), typeToString(source2));
      }
      return 0 /* False */;
    }
    function indexSignaturesIdenticalTo(source2, target2) {
      const sourceInfos ***REMOVED*** getIndexInfosOfType(source2);
      const targetInfos ***REMOVED*** getIndexInfosOfType(target2);
      if (sourceInfos.length !***REMOVED******REMOVED*** targetInfos.length) {
        return 0 /* False */;
      }
      for (const targetInfo of targetInfos) {
        const sourceInfo ***REMOVED*** getIndexInfoOfType(source2, targetInfo.keyType);
        if (!(sourceInfo && isRelatedTo(sourceInfo.type, targetInfo.type, 3 /* Both */) && sourceInfo.isReadonly ***REMOVED******REMOVED******REMOVED*** targetInfo.isReadonly)) {
          return 0 /* False */;
        }
      }
      return -1 /* True */;
    }
    function constructorVisibilitiesAreCompatible(sourceSignature, targetSignature, reportErrors2) {
      if (!sourceSignature.declaration || !targetSignature.declaration) {
        return true;
      }
      const sourceAccessibility ***REMOVED*** getSelectedEffectiveModifierFlags(sourceSignature.declaration, 6 /* NonPublicAccessibilityModifier */);
      const targetAccessibility ***REMOVED*** getSelectedEffectiveModifierFlags(targetSignature.declaration, 6 /* NonPublicAccessibilityModifier */);
      if (targetAccessibility ***REMOVED******REMOVED******REMOVED*** 2 /* Private */) {
        return true;
      }
      if (targetAccessibility ***REMOVED******REMOVED******REMOVED*** 4 /* Protected */ && sourceAccessibility !***REMOVED******REMOVED*** 2 /* Private */) {
        return true;
      }
      if (targetAccessibility !***REMOVED******REMOVED*** 4 /* Protected */ && !sourceAccessibility) {
        return true;
      }
      if (reportErrors2) {
        reportError(Diagnostics.Cannot_assign_a_0_constructor_type_to_a_1_constructor_type, visibilityToString(sourceAccessibility), visibilityToString(targetAccessibility));
      }
      return false;
    }
  }
  function typeCouldHaveTopLevelSingletonTypes(type) {
    if (type.flags & 16 /* Boolean */) {
      return false;
    }
    if (type.flags & 3145728 /* UnionOrIntersection */) {
      return !!forEach(type.types, typeCouldHaveTopLevelSingletonTypes);
    }
    if (type.flags & 465829888 /* Instantiable */) {
      const constraint ***REMOVED*** getConstraintOfType(type);
      if (constraint && constraint !***REMOVED******REMOVED*** type) {
        return typeCouldHaveTopLevelSingletonTypes(constraint);
      }
    }
    return isUnitType(type) || !!(type.flags & 134217728 /* TemplateLiteral */) || !!(type.flags & 268435456 /* StringMapping */);
  }
  function getExactOptionalUnassignableProperties(source, target) {
    if (isTupleType(source) && isTupleType(target)) return emptyArray;
    return getPropertiesOfType(target).filter((targetProp) ***REMOVED***> isExactOptionalPropertyMismatch(getTypeOfPropertyOfType(source, targetProp.escapedName), getTypeOfSymbol(targetProp)));
  }
  function isExactOptionalPropertyMismatch(source, target) {
    return !!source && !!target && maybeTypeOfKind(source, 32768 /* Undefined */) && !!containsMissingType(target);
  }
  function getExactOptionalProperties(type) {
    return getPropertiesOfType(type).filter((targetProp) ***REMOVED***> containsMissingType(getTypeOfSymbol(targetProp)));
  }
  function getBestMatchingType(source, target, isRelatedTo ***REMOVED*** compareTypesAssignable) {
    return findMatchingDiscriminantType(source, target, isRelatedTo) || findMatchingTypeReferenceOrTypeAliasReference(source, target) || findBestTypeForObjectLiteral(source, target) || findBestTypeForInvokable(source, target) || findMostOverlappyType(source, target);
  }
  function discriminateTypeByDiscriminableItems(target, discriminators, related) {
    const types ***REMOVED*** target.types;
    const include ***REMOVED*** types.map((t) ***REMOVED***> t.flags & 402784252 /* Primitive */ ? 0 /* False */ : -1 /* True */);
    for (const [getDiscriminatingType, propertyName] of discriminators) {
      let matched ***REMOVED*** false;
      for (let i ***REMOVED*** 0; i < types.length; i++) {
        if (include[i]) {
          const targetType ***REMOVED*** getTypeOfPropertyOrIndexSignatureOfType(types[i], propertyName);
          if (targetType) {
            if (someType(getDiscriminatingType(), (t) ***REMOVED***> !!related(t, targetType))) {
              matched ***REMOVED*** true;
            } else {
              include[i] ***REMOVED*** 3 /* Maybe */;
            }
          }
        }
      }
      for (let i ***REMOVED*** 0; i < types.length; i++) {
        if (include[i] ***REMOVED******REMOVED******REMOVED*** 3 /* Maybe */) {
          include[i] ***REMOVED*** matched ? 0 /* False */ : -1 /* True */;
        }
      }
    }
    const filtered ***REMOVED*** contains(include, 0 /* False */) ? getUnionType(types.filter((_, i) ***REMOVED***> include[i]), 0 /* None */) : target;
    return filtered.flags & 131072 /* Never */ ? target : filtered;
  }
  function isWeakType(type) {
    if (type.flags & 524288 /* Object */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      return resolved.callSignatures.length ***REMOVED******REMOVED******REMOVED*** 0 && resolved.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 0 && resolved.indexInfos.length ***REMOVED******REMOVED******REMOVED*** 0 && resolved.properties.length > 0 && every(resolved.properties, (p) ***REMOVED***> !!(p.flags & 16777216 /* Optional */));
    }
    if (type.flags & 33554432 /* Substitution */) {
      return isWeakType(type.baseType);
    }
    if (type.flags & 2097152 /* Intersection */) {
      return every(type.types, isWeakType);
    }
    return false;
  }
  function hasCommonProperties(source, target, isComparingJsxAttributes) {
    for (const prop of getPropertiesOfType(source)) {
      if (isKnownProperty(target, prop.escapedName, isComparingJsxAttributes)) {
        return true;
      }
    }
    return false;
  }
  function getVariances(type) {
    return type ***REMOVED******REMOVED******REMOVED*** globalArrayType || type ***REMOVED******REMOVED******REMOVED*** globalReadonlyArrayType || type.objectFlags & 8 /* Tuple */ ? arrayVariances : getVariancesWorker(type.symbol, type.typeParameters);
  }
  function getAliasVariances(symbol) {
    return getVariancesWorker(symbol, getSymbolLinks(symbol).typeParameters);
  }
  function getVariancesWorker(symbol, typeParameters ***REMOVED*** emptyArray) {
    var _a, _b;
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.variances) {
      (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.CheckTypes, "getVariancesWorker", { arity: typeParameters.length, id: getTypeId(getDeclaredTypeOfSymbol(symbol)) });
      const oldVarianceComputation ***REMOVED*** inVarianceComputation;
      const saveResolutionStart ***REMOVED*** resolutionStart;
      if (!inVarianceComputation) {
        inVarianceComputation ***REMOVED*** true;
        resolutionStart ***REMOVED*** resolutionTargets.length;
      }
      links.variances ***REMOVED*** emptyArray;
      const variances ***REMOVED*** [];
      for (const tp of typeParameters) {
        const modifiers ***REMOVED*** getTypeParameterModifiers(tp);
        let variance ***REMOVED*** modifiers & 16384 /* Out */ ? modifiers & 8192 /* In */ ? 0 /* Invariant */ : 1 /* Covariant */ : modifiers & 8192 /* In */ ? 2 /* Contravariant */ : void 0;
        if (variance ***REMOVED******REMOVED******REMOVED*** void 0) {
          let unmeasurable ***REMOVED*** false;
          let unreliable ***REMOVED*** false;
          const oldHandler ***REMOVED*** outofbandVarianceMarkerHandler;
          outofbandVarianceMarkerHandler ***REMOVED*** (onlyUnreliable) ***REMOVED***> onlyUnreliable ? unreliable ***REMOVED*** true : unmeasurable ***REMOVED*** true;
          const typeWithSuper ***REMOVED*** createMarkerType(symbol, tp, markerSuperType);
          const typeWithSub ***REMOVED*** createMarkerType(symbol, tp, markerSubType);
          variance ***REMOVED*** (isTypeAssignableTo(typeWithSub, typeWithSuper) ? 1 /* Covariant */ : 0) | (isTypeAssignableTo(typeWithSuper, typeWithSub) ? 2 /* Contravariant */ : 0);
          if (variance ***REMOVED******REMOVED******REMOVED*** 3 /* Bivariant */ && isTypeAssignableTo(createMarkerType(symbol, tp, markerOtherType), typeWithSuper)) {
            variance ***REMOVED*** 4 /* Independent */;
          }
          outofbandVarianceMarkerHandler ***REMOVED*** oldHandler;
          if (unmeasurable || unreliable) {
            if (unmeasurable) {
              variance |***REMOVED*** 8 /* Unmeasurable */;
            }
            if (unreliable) {
              variance |***REMOVED*** 16 /* Unreliable */;
            }
          }
        }
        variances.push(variance);
      }
      if (!oldVarianceComputation) {
        inVarianceComputation ***REMOVED*** false;
        resolutionStart ***REMOVED*** saveResolutionStart;
      }
      links.variances ***REMOVED*** variances;
      (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop({ variances: variances.map(Debug.formatVariance) });
    }
    return links.variances;
  }
  function createMarkerType(symbol, source, target) {
    const mapper ***REMOVED*** makeUnaryTypeMapper(source, target);
    const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
    if (isErrorType(type)) {
      return type;
    }
    const result ***REMOVED*** symbol.flags & 524288 /* TypeAlias */ ? getTypeAliasInstantiation(symbol, instantiateTypes(getSymbolLinks(symbol).typeParameters, mapper)) : createTypeReference(type, instantiateTypes(type.typeParameters, mapper));
    markerTypes.add(getTypeId(result));
    return result;
  }
  function isMarkerType(type) {
    return markerTypes.has(getTypeId(type));
  }
  function getTypeParameterModifiers(tp) {
    var _a;
    return reduceLeft((_a ***REMOVED*** tp.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.declarations, (modifiers, d) ***REMOVED***> modifiers | getEffectiveModifierFlags(d), 0 /* None */) & (8192 /* In */ | 16384 /* Out */ | 4096 /* Const */);
  }
  function hasCovariantVoidArgument(typeArguments, variances) {
    for (let i ***REMOVED*** 0; i < variances.length; i++) {
      if ((variances[i] & 7 /* VarianceMask */) ***REMOVED******REMOVED******REMOVED*** 1 /* Covariant */ && typeArguments[i].flags & 16384 /* Void */) {
        return true;
      }
    }
    return false;
  }
  function isUnconstrainedTypeParameter(type) {
    return type.flags & 262144 /* TypeParameter */ && !getConstraintOfTypeParameter(type);
  }
  function isNonDeferredTypeReference(type) {
    return !!(getObjectFlags(type) & 4 /* Reference */) && !type.node;
  }
  function isTypeReferenceWithGenericArguments(type) {
    return isNonDeferredTypeReference(type) && some(getTypeArguments(type), (t) ***REMOVED***> !!(t.flags & 262144 /* TypeParameter */) || isTypeReferenceWithGenericArguments(t));
  }
  function getGenericTypeReferenceRelationKey(source, target, postFix, ignoreConstraints) {
    const typeParameters ***REMOVED*** [];
    let constraintMarker ***REMOVED*** "";
    const sourceId ***REMOVED*** getTypeReferenceId(source, 0);
    const targetId ***REMOVED*** getTypeReferenceId(target, 0);
    return `${constraintMarker}${sourceId},${targetId}${postFix}`;
    function getTypeReferenceId(type, depth ***REMOVED*** 0) {
      let result ***REMOVED*** "" + type.target.id;
      for (const t of getTypeArguments(type)) {
        if (t.flags & 262144 /* TypeParameter */) {
          if (ignoreConstraints || isUnconstrainedTypeParameter(t)) {
            let index ***REMOVED*** typeParameters.indexOf(t);
            if (index < 0) {
              index ***REMOVED*** typeParameters.length;
              typeParameters.push(t);
            }
            result +***REMOVED*** "***REMOVED***" + index;
            continue;
          }
          constraintMarker ***REMOVED*** "*";
        } else if (depth < 4 && isTypeReferenceWithGenericArguments(t)) {
          result +***REMOVED*** "<" + getTypeReferenceId(t, depth + 1) + ">";
          continue;
        }
        result +***REMOVED*** "-" + t.id;
      }
      return result;
    }
  }
  function getRelationKey(source, target, intersectionState, relation, ignoreConstraints) {
    if (relation ***REMOVED******REMOVED******REMOVED*** identityRelation && source.id > target.id) {
      const temp ***REMOVED*** source;
      source ***REMOVED*** target;
      target ***REMOVED*** temp;
    }
    const postFix ***REMOVED*** intersectionState ? ":" + intersectionState : "";
    return isTypeReferenceWithGenericArguments(source) && isTypeReferenceWithGenericArguments(target) ? getGenericTypeReferenceRelationKey(source, target, postFix, ignoreConstraints) : `${source.id},${target.id}${postFix}`;
  }
  function forEachProperty(prop, callback) {
    if (getCheckFlags(prop) & 6 /* Synthetic */) {
      for (const t of prop.links.containingType.types) {
        const p ***REMOVED*** getPropertyOfType(t, prop.escapedName);
        const result ***REMOVED*** p && forEachProperty(p, callback);
        if (result) {
          return result;
        }
      }
      return void 0;
    }
    return callback(prop);
  }
  function getDeclaringClass(prop) {
    return prop.parent && prop.parent.flags & 32 /* Class */ ? getDeclaredTypeOfSymbol(getParentOfSymbol(prop)) : void 0;
  }
  function getTypeOfPropertyInBaseClass(property) {
    const classType ***REMOVED*** getDeclaringClass(property);
    const baseClassType ***REMOVED*** classType && getBaseTypes(classType)[0];
    return baseClassType && getTypeOfPropertyOfType(baseClassType, property.escapedName);
  }
  function isPropertyInClassDerivedFrom(prop, baseClass) {
    return forEachProperty(prop, (sp) ***REMOVED***> {
      const sourceClass ***REMOVED*** getDeclaringClass(sp);
      return sourceClass ? hasBaseType(sourceClass, baseClass) : false;
    });
  }
  function isValidOverrideOf(sourceProp, targetProp) {
    return !forEachProperty(targetProp, (tp) ***REMOVED***> getDeclarationModifierFlagsFromSymbol(tp) & 4 /* Protected */ ? !isPropertyInClassDerivedFrom(sourceProp, getDeclaringClass(tp)) : false);
  }
  function isClassDerivedFromDeclaringClasses(checkClass, prop, writing) {
    return forEachProperty(prop, (p) ***REMOVED***> getDeclarationModifierFlagsFromSymbol(p, writing) & 4 /* Protected */ ? !hasBaseType(checkClass, getDeclaringClass(p)) : false) ? void 0 : checkClass;
  }
  function isDeeplyNestedType(type, stack, depth, maxDepth ***REMOVED*** 3) {
    if (depth >***REMOVED*** maxDepth) {
      if ((getObjectFlags(type) & 96 /* InstantiatedMapped */) ***REMOVED******REMOVED******REMOVED*** 96 /* InstantiatedMapped */) {
        type ***REMOVED*** getMappedTargetWithSymbol(type);
      }
      if (type.flags & 2097152 /* Intersection */) {
        return some(type.types, (t) ***REMOVED***> isDeeplyNestedType(t, stack, depth, maxDepth));
      }
      const identity2 ***REMOVED*** getRecursionIdentity(type);
      let count ***REMOVED*** 0;
      let lastTypeId ***REMOVED*** 0;
      for (let i ***REMOVED*** 0; i < depth; i++) {
        const t ***REMOVED*** stack[i];
        if (hasMatchingRecursionIdentity(t, identity2)) {
          if (t.id >***REMOVED*** lastTypeId) {
            count++;
            if (count >***REMOVED*** maxDepth) {
              return true;
            }
          }
          lastTypeId ***REMOVED*** t.id;
        }
      }
    }
    return false;
  }
  function getMappedTargetWithSymbol(type) {
    let target;
    while ((getObjectFlags(type) & 96 /* InstantiatedMapped */) ***REMOVED******REMOVED******REMOVED*** 96 /* InstantiatedMapped */ && (target ***REMOVED*** getModifiersTypeFromMappedType(type)) && (target.symbol || target.flags & 2097152 /* Intersection */ && some(target.types, (t) ***REMOVED***> !!t.symbol))) {
      type ***REMOVED*** target;
    }
    return type;
  }
  function hasMatchingRecursionIdentity(type, identity2) {
    if ((getObjectFlags(type) & 96 /* InstantiatedMapped */) ***REMOVED******REMOVED******REMOVED*** 96 /* InstantiatedMapped */) {
      type ***REMOVED*** getMappedTargetWithSymbol(type);
    }
    if (type.flags & 2097152 /* Intersection */) {
      return some(type.types, (t) ***REMOVED***> hasMatchingRecursionIdentity(t, identity2));
    }
    return getRecursionIdentity(type) ***REMOVED******REMOVED******REMOVED*** identity2;
  }
  function getRecursionIdentity(type) {
    if (type.flags & 524288 /* Object */ && !isObjectOrArrayLiteralType(type)) {
      if (getObjectFlags(type) & 4 /* Reference */ && type.node) {
        return type.node;
      }
      if (type.symbol && !(getObjectFlags(type) & 16 /* Anonymous */ && type.symbol.flags & 32 /* Class */)) {
        return type.symbol;
      }
      if (isTupleType(type)) {
        return type.target;
      }
    }
    if (type.flags & 262144 /* TypeParameter */) {
      return type.symbol;
    }
    if (type.flags & 8388608 /* IndexedAccess */) {
      do {
        type ***REMOVED*** type.objectType;
      } while (type.flags & 8388608 /* IndexedAccess */);
      return type;
    }
    if (type.flags & 16777216 /* Conditional */) {
      return type.root;
    }
    return type;
  }
  function isPropertyIdenticalTo(sourceProp, targetProp) {
    return compareProperties(sourceProp, targetProp, compareTypesIdentical) !***REMOVED******REMOVED*** 0 /* False */;
  }
  function compareProperties(sourceProp, targetProp, compareTypes) {
    if (sourceProp ***REMOVED******REMOVED******REMOVED*** targetProp) {
      return -1 /* True */;
    }
    const sourcePropAccessibility ***REMOVED*** getDeclarationModifierFlagsFromSymbol(sourceProp) & 6 /* NonPublicAccessibilityModifier */;
    const targetPropAccessibility ***REMOVED*** getDeclarationModifierFlagsFromSymbol(targetProp) & 6 /* NonPublicAccessibilityModifier */;
    if (sourcePropAccessibility !***REMOVED******REMOVED*** targetPropAccessibility) {
      return 0 /* False */;
    }
    if (sourcePropAccessibility) {
      if (getTargetSymbol(sourceProp) !***REMOVED******REMOVED*** getTargetSymbol(targetProp)) {
        return 0 /* False */;
      }
    } else {
      if ((sourceProp.flags & 16777216 /* Optional */) !***REMOVED******REMOVED*** (targetProp.flags & 16777216 /* Optional */)) {
        return 0 /* False */;
      }
    }
    if (isReadonlySymbol(sourceProp) !***REMOVED******REMOVED*** isReadonlySymbol(targetProp)) {
      return 0 /* False */;
    }
    return compareTypes(getTypeOfSymbol(sourceProp), getTypeOfSymbol(targetProp));
  }
  function isMatchingSignature(source, target, partialMatch) {
    const sourceParameterCount ***REMOVED*** getParameterCount(source);
    const targetParameterCount ***REMOVED*** getParameterCount(target);
    const sourceMinArgumentCount ***REMOVED*** getMinArgumentCount(source);
    const targetMinArgumentCount ***REMOVED*** getMinArgumentCount(target);
    const sourceHasRestParameter ***REMOVED*** hasEffectiveRestParameter(source);
    const targetHasRestParameter ***REMOVED*** hasEffectiveRestParameter(target);
    if (sourceParameterCount ***REMOVED******REMOVED******REMOVED*** targetParameterCount && sourceMinArgumentCount ***REMOVED******REMOVED******REMOVED*** targetMinArgumentCount && sourceHasRestParameter ***REMOVED******REMOVED******REMOVED*** targetHasRestParameter) {
      return true;
    }
    if (partialMatch && sourceMinArgumentCount <***REMOVED*** targetMinArgumentCount) {
      return true;
    }
    return false;
  }
  function compareSignaturesIdentical(source, target, partialMatch, ignoreThisTypes, ignoreReturnTypes, compareTypes) {
    if (source ***REMOVED******REMOVED******REMOVED*** target) {
      return -1 /* True */;
    }
    if (!isMatchingSignature(source, target, partialMatch)) {
      return 0 /* False */;
    }
    if (length(source.typeParameters) !***REMOVED******REMOVED*** length(target.typeParameters)) {
      return 0 /* False */;
    }
    if (target.typeParameters) {
      const mapper ***REMOVED*** createTypeMapper(source.typeParameters, target.typeParameters);
      for (let i ***REMOVED*** 0; i < target.typeParameters.length; i++) {
        const s ***REMOVED*** source.typeParameters[i];
        const t ***REMOVED*** target.typeParameters[i];
        if (!(s ***REMOVED******REMOVED******REMOVED*** t || compareTypes(instantiateType(getConstraintFromTypeParameter(s), mapper) || unknownType, getConstraintFromTypeParameter(t) || unknownType) && compareTypes(instantiateType(getDefaultFromTypeParameter(s), mapper) || unknownType, getDefaultFromTypeParameter(t) || unknownType))) {
          return 0 /* False */;
        }
      }
      source ***REMOVED*** instantiateSignature(
        source,
        mapper,
        /*eraseTypeParameters*/
        true
      );
    }
    let result ***REMOVED*** -1 /* True */;
    if (!ignoreThisTypes) {
      const sourceThisType ***REMOVED*** getThisTypeOfSignature(source);
      if (sourceThisType) {
        const targetThisType ***REMOVED*** getThisTypeOfSignature(target);
        if (targetThisType) {
          const related ***REMOVED*** compareTypes(sourceThisType, targetThisType);
          if (!related) {
            return 0 /* False */;
          }
          result &***REMOVED*** related;
        }
      }
    }
    const targetLen ***REMOVED*** getParameterCount(target);
    for (let i ***REMOVED*** 0; i < targetLen; i++) {
      const s ***REMOVED*** getTypeAtPosition(source, i);
      const t ***REMOVED*** getTypeAtPosition(target, i);
      const related ***REMOVED*** compareTypes(t, s);
      if (!related) {
        return 0 /* False */;
      }
      result &***REMOVED*** related;
    }
    if (!ignoreReturnTypes) {
      const sourceTypePredicate ***REMOVED*** getTypePredicateOfSignature(source);
      const targetTypePredicate ***REMOVED*** getTypePredicateOfSignature(target);
      result &***REMOVED*** sourceTypePredicate || targetTypePredicate ? compareTypePredicatesIdentical(sourceTypePredicate, targetTypePredicate, compareTypes) : compareTypes(getReturnTypeOfSignature(source), getReturnTypeOfSignature(target));
    }
    return result;
  }
  function compareTypePredicatesIdentical(source, target, compareTypes) {
    return !(source && target && typePredicateKindsMatch(source, target)) ? 0 /* False */ : source.type ***REMOVED******REMOVED******REMOVED*** target.type ? -1 /* True */ : source.type && target.type ? compareTypes(source.type, target.type) : 0 /* False */;
  }
  function literalTypesWithSameBaseType(types) {
    let commonBaseType;
    for (const t of types) {
      if (!(t.flags & 131072 /* Never */)) {
        const baseType ***REMOVED*** getBaseTypeOfLiteralType(t);
        commonBaseType ?? (commonBaseType ***REMOVED*** baseType);
        if (baseType ***REMOVED******REMOVED******REMOVED*** t || baseType !***REMOVED******REMOVED*** commonBaseType) {
          return false;
        }
      }
    }
    return true;
  }
  function getCombinedTypeFlags(types) {
    return reduceLeft(types, (flags, t) ***REMOVED***> flags | (t.flags & 1048576 /* Union */ ? getCombinedTypeFlags(t.types) : t.flags), 0);
  }
  function getCommonSupertype(types) {
    if (types.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return types[0];
    }
    const primaryTypes ***REMOVED*** strictNullChecks ? sameMap(types, (t) ***REMOVED***> filterType(t, (u) ***REMOVED***> !(u.flags & 98304 /* Nullable */))) : types;
    const superTypeOrUnion ***REMOVED*** literalTypesWithSameBaseType(primaryTypes) ? getUnionType(primaryTypes) : getSingleCommonSupertype(primaryTypes);
    return primaryTypes ***REMOVED******REMOVED******REMOVED*** types ? superTypeOrUnion : getNullableType(superTypeOrUnion, getCombinedTypeFlags(types) & 98304 /* Nullable */);
  }
  function getSingleCommonSupertype(types) {
    const candidate ***REMOVED*** reduceLeft(types, (s, t) ***REMOVED***> isTypeStrictSubtypeOf(s, t) ? t : s);
    return every(types, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** candidate || isTypeStrictSubtypeOf(t, candidate)) ? candidate : reduceLeft(types, (s, t) ***REMOVED***> isTypeSubtypeOf(s, t) ? t : s);
  }
  function getCommonSubtype(types) {
    return reduceLeft(types, (s, t) ***REMOVED***> isTypeSubtypeOf(t, s) ? t : s);
  }
  function isArrayType(type) {
    return !!(getObjectFlags(type) & 4 /* Reference */) && (type.target ***REMOVED******REMOVED******REMOVED*** globalArrayType || type.target ***REMOVED******REMOVED******REMOVED*** globalReadonlyArrayType);
  }
  function isReadonlyArrayType(type) {
    return !!(getObjectFlags(type) & 4 /* Reference */) && type.target ***REMOVED******REMOVED******REMOVED*** globalReadonlyArrayType;
  }
  function isArrayOrTupleType(type) {
    return isArrayType(type) || isTupleType(type);
  }
  function isMutableArrayOrTuple(type) {
    return isArrayType(type) && !isReadonlyArrayType(type) || isTupleType(type) && !type.target.readonly;
  }
  function getElementTypeOfArrayType(type) {
    return isArrayType(type) ? getTypeArguments(type)[0] : void 0;
  }
  function isArrayLikeType(type) {
    return isArrayType(type) || !(type.flags & 98304 /* Nullable */) && isTypeAssignableTo(type, anyReadonlyArrayType);
  }
  function isMutableArrayLikeType(type) {
    return isMutableArrayOrTuple(type) || !(type.flags & (1 /* Any */ | 98304 /* Nullable */)) && isTypeAssignableTo(type, anyArrayType);
  }
  function getSingleBaseForNonAugmentingSubtype(type) {
    if (!(getObjectFlags(type) & 4 /* Reference */) || !(getObjectFlags(type.target) & 3 /* ClassOrInterface */)) {
      return void 0;
    }
    if (getObjectFlags(type) & 33554432 /* IdenticalBaseTypeCalculated */) {
      return getObjectFlags(type) & 67108864 /* IdenticalBaseTypeExists */ ? type.cachedEquivalentBaseType : void 0;
    }
    type.objectFlags |***REMOVED*** 33554432 /* IdenticalBaseTypeCalculated */;
    const target ***REMOVED*** type.target;
    if (getObjectFlags(target) & 1 /* Class */) {
      const baseTypeNode ***REMOVED*** getBaseTypeNodeOfClass(target);
      if (baseTypeNode && baseTypeNode.expression.kind !***REMOVED******REMOVED*** 80 /* Identifier */ && baseTypeNode.expression.kind !***REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
        return void 0;
      }
    }
    const bases ***REMOVED*** getBaseTypes(target);
    if (bases.length !***REMOVED******REMOVED*** 1) {
      return void 0;
    }
    if (getMembersOfSymbol(type.symbol).size) {
      return void 0;
    }
    let instantiatedBase ***REMOVED*** !length(target.typeParameters) ? bases[0] : instantiateType(bases[0], createTypeMapper(target.typeParameters, getTypeArguments(type).slice(0, target.typeParameters.length)));
    if (length(getTypeArguments(type)) > length(target.typeParameters)) {
      instantiatedBase ***REMOVED*** getTypeWithThisArgument(instantiatedBase, last(getTypeArguments(type)));
    }
    type.objectFlags |***REMOVED*** 67108864 /* IdenticalBaseTypeExists */;
    return type.cachedEquivalentBaseType ***REMOVED*** instantiatedBase;
  }
  function isEmptyLiteralType(type) {
    return strictNullChecks ? type ***REMOVED******REMOVED******REMOVED*** implicitNeverType : type ***REMOVED******REMOVED******REMOVED*** undefinedWideningType;
  }
  function isEmptyArrayLiteralType(type) {
    const elementType ***REMOVED*** getElementTypeOfArrayType(type);
    return !!elementType && isEmptyLiteralType(elementType);
  }
  function isTupleLikeType(type) {
    let lengthType;
    return isTupleType(type) || !!getPropertyOfType(type, "0") || isArrayLikeType(type) && !!(lengthType ***REMOVED*** getTypeOfPropertyOfType(type, "length")) && everyType(lengthType, (t) ***REMOVED***> !!(t.flags & 256 /* NumberLiteral */));
  }
  function isArrayOrTupleLikeType(type) {
    return isArrayLikeType(type) || isTupleLikeType(type);
  }
  function getTupleElementType(type, index) {
    const propType ***REMOVED*** getTypeOfPropertyOfType(type, "" + index);
    if (propType) {
      return propType;
    }
    if (everyType(type, isTupleType)) {
      return getTupleElementTypeOutOfStartCount(type, index, compilerOptions.noUncheckedIndexedAccess ? undefinedType : void 0);
    }
    return void 0;
  }
  function isNeitherUnitTypeNorNever(type) {
    return !(type.flags & (109472 /* Unit */ | 131072 /* Never */));
  }
  function isUnitType(type) {
    return !!(type.flags & 109472 /* Unit */);
  }
  function isUnitLikeType(type) {
    const t ***REMOVED*** getBaseConstraintOrType(type);
    return t.flags & 2097152 /* Intersection */ ? some(t.types, isUnitType) : isUnitType(t);
  }
  function extractUnitType(type) {
    return type.flags & 2097152 /* Intersection */ ? find(type.types, isUnitType) || type : type;
  }
  function isLiteralType(type) {
    return type.flags & 16 /* Boolean */ ? true : type.flags & 1048576 /* Union */ ? type.flags & 1024 /* EnumLiteral */ ? true : every(type.types, isUnitType) : isUnitType(type);
  }
  function getBaseTypeOfLiteralType(type) {
    return type.flags & 1056 /* EnumLike */ ? getBaseTypeOfEnumLikeType(type) : type.flags & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) ? stringType : type.flags & 256 /* NumberLiteral */ ? numberType : type.flags & 2048 /* BigIntLiteral */ ? bigintType : type.flags & 512 /* BooleanLiteral */ ? booleanType : type.flags & 1048576 /* Union */ ? getBaseTypeOfLiteralTypeUnion(type) : type;
  }
  function getBaseTypeOfLiteralTypeUnion(type) {
    const key ***REMOVED*** `B${getTypeId(type)}`;
    return getCachedType(key) ?? setCachedType(key, mapType(type, getBaseTypeOfLiteralType));
  }
  function getBaseTypeOfLiteralTypeForComparison(type) {
    return type.flags & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) ? stringType : type.flags & (256 /* NumberLiteral */ | 32 /* Enum */) ? numberType : type.flags & 2048 /* BigIntLiteral */ ? bigintType : type.flags & 512 /* BooleanLiteral */ ? booleanType : type.flags & 1048576 /* Union */ ? mapType(type, getBaseTypeOfLiteralTypeForComparison) : type;
  }
  function getWidenedLiteralType(type) {
    return type.flags & 1056 /* EnumLike */ && isFreshLiteralType(type) ? getBaseTypeOfEnumLikeType(type) : type.flags & 128 /* StringLiteral */ && isFreshLiteralType(type) ? stringType : type.flags & 256 /* NumberLiteral */ && isFreshLiteralType(type) ? numberType : type.flags & 2048 /* BigIntLiteral */ && isFreshLiteralType(type) ? bigintType : type.flags & 512 /* BooleanLiteral */ && isFreshLiteralType(type) ? booleanType : type.flags & 1048576 /* Union */ ? mapType(type, getWidenedLiteralType) : type;
  }
  function getWidenedUniqueESSymbolType(type) {
    return type.flags & 8192 /* UniqueESSymbol */ ? esSymbolType : type.flags & 1048576 /* Union */ ? mapType(type, getWidenedUniqueESSymbolType) : type;
  }
  function getWidenedLiteralLikeTypeForContextualType(type, contextualType) {
    if (!isLiteralOfContextualType(type, contextualType)) {
      type ***REMOVED*** getWidenedUniqueESSymbolType(getWidenedLiteralType(type));
    }
    return getRegularTypeOfLiteralType(type);
  }
  function getWidenedLiteralLikeTypeForContextualReturnTypeIfNeeded(type, contextualSignatureReturnType, isAsync) {
    if (type && isUnitType(type)) {
      const contextualType ***REMOVED*** !contextualSignatureReturnType ? void 0 : isAsync ? getPromisedTypeOfPromise(contextualSignatureReturnType) : contextualSignatureReturnType;
      type ***REMOVED*** getWidenedLiteralLikeTypeForContextualType(type, contextualType);
    }
    return type;
  }
  function getWidenedLiteralLikeTypeForContextualIterationTypeIfNeeded(type, contextualSignatureReturnType, kind, isAsyncGenerator) {
    if (type && isUnitType(type)) {
      const contextualType ***REMOVED*** !contextualSignatureReturnType ? void 0 : getIterationTypeOfGeneratorFunctionReturnType(kind, contextualSignatureReturnType, isAsyncGenerator);
      type ***REMOVED*** getWidenedLiteralLikeTypeForContextualType(type, contextualType);
    }
    return type;
  }
  function isTupleType(type) {
    return !!(getObjectFlags(type) & 4 /* Reference */ && type.target.objectFlags & 8 /* Tuple */);
  }
  function isGenericTupleType(type) {
    return isTupleType(type) && !!(type.target.combinedFlags & 8 /* Variadic */);
  }
  function isSingleElementGenericTupleType(type) {
    return isGenericTupleType(type) && type.target.elementFlags.length ***REMOVED******REMOVED******REMOVED*** 1;
  }
  function getRestTypeOfTupleType(type) {
    return getElementTypeOfSliceOfTupleType(type, type.target.fixedLength);
  }
  function getTupleElementTypeOutOfStartCount(type, index, undefinedOrMissingType2) {
    return mapType(type, (t) ***REMOVED***> {
      const tupleType ***REMOVED*** t;
      const restType ***REMOVED*** getRestTypeOfTupleType(tupleType);
      if (!restType) {
        return undefinedType;
      }
      if (undefinedOrMissingType2 && index >***REMOVED*** getTotalFixedElementCount(tupleType.target)) {
        return getUnionType([restType, undefinedOrMissingType2]);
      }
      return restType;
    });
  }
  function getRestArrayTypeOfTupleType(type) {
    const restType ***REMOVED*** getRestTypeOfTupleType(type);
    return restType && createArrayType(restType);
  }
  function getElementTypeOfSliceOfTupleType(type, index, endSkipCount ***REMOVED*** 0, writing ***REMOVED*** false, noReductions ***REMOVED*** false) {
    const length2 ***REMOVED*** getTypeReferenceArity(type) - endSkipCount;
    if (index < length2) {
      const typeArguments ***REMOVED*** getTypeArguments(type);
      const elementTypes ***REMOVED*** [];
      for (let i ***REMOVED*** index; i < length2; i++) {
        const t ***REMOVED*** typeArguments[i];
        elementTypes.push(type.target.elementFlags[i] & 8 /* Variadic */ ? getIndexedAccessType(t, numberType) : t);
      }
      return writing ? getIntersectionType(elementTypes) : getUnionType(elementTypes, noReductions ? 0 /* None */ : 1 /* Literal */);
    }
    return void 0;
  }
  function isTupleTypeStructureMatching(t1, t2) {
    return getTypeReferenceArity(t1) ***REMOVED******REMOVED******REMOVED*** getTypeReferenceArity(t2) && every(t1.target.elementFlags, (f, i) ***REMOVED***> (f & 12 /* Variable */) ***REMOVED******REMOVED******REMOVED*** (t2.target.elementFlags[i] & 12 /* Variable */));
  }
  function isZeroBigInt({ value }) {
    return value.base10Value ***REMOVED******REMOVED******REMOVED*** "0";
  }
  function removeDefinitelyFalsyTypes(type) {
    return filterType(type, (t) ***REMOVED***> hasTypeFacts(t, 4194304 /* Truthy */));
  }
  function extractDefinitelyFalsyTypes(type) {
    return mapType(type, getDefinitelyFalsyPartOfType);
  }
  function getDefinitelyFalsyPartOfType(type) {
    return type.flags & 4 /* String */ ? emptyStringType : type.flags & 8 /* Number */ ? zeroType : type.flags & 64 /* BigInt */ ? zeroBigIntType : type ***REMOVED******REMOVED******REMOVED*** regularFalseType || type ***REMOVED******REMOVED******REMOVED*** falseType || type.flags & (16384 /* Void */ | 32768 /* Undefined */ | 65536 /* Null */ | 3 /* AnyOrUnknown */) || type.flags & 128 /* StringLiteral */ && type.value ***REMOVED******REMOVED******REMOVED*** "" || type.flags & 256 /* NumberLiteral */ && type.value ***REMOVED******REMOVED******REMOVED*** 0 || type.flags & 2048 /* BigIntLiteral */ && isZeroBigInt(type) ? type : neverType;
  }
  function getNullableType(type, flags) {
    const missing ***REMOVED*** flags & ~type.flags & (32768 /* Undefined */ | 65536 /* Null */);
    return missing ***REMOVED******REMOVED******REMOVED*** 0 ? type : missing ***REMOVED******REMOVED******REMOVED*** 32768 /* Undefined */ ? getUnionType([type, undefinedType]) : missing ***REMOVED******REMOVED******REMOVED*** 65536 /* Null */ ? getUnionType([type, nullType]) : getUnionType([type, undefinedType, nullType]);
  }
  function getOptionalType(type, isProperty ***REMOVED*** false) {
    Debug.assert(strictNullChecks);
    const missingOrUndefined ***REMOVED*** isProperty ? undefinedOrMissingType : undefinedType;
    return type ***REMOVED******REMOVED******REMOVED*** missingOrUndefined || type.flags & 1048576 /* Union */ && type.types[0] ***REMOVED******REMOVED******REMOVED*** missingOrUndefined ? type : getUnionType([type, missingOrUndefined]);
  }
  function getGlobalNonNullableTypeInstantiation(type) {
    if (!deferredGlobalNonNullableTypeAlias) {
      deferredGlobalNonNullableTypeAlias ***REMOVED*** getGlobalSymbol(
        "NonNullable",
        524288 /* TypeAlias */,
        /*diagnostic*/
        void 0
      ) || unknownSymbol;
    }
    return deferredGlobalNonNullableTypeAlias !***REMOVED******REMOVED*** unknownSymbol ? getTypeAliasInstantiation(deferredGlobalNonNullableTypeAlias, [type]) : getIntersectionType([type, emptyObjectType]);
  }
  function getNonNullableType(type) {
    return strictNullChecks ? getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */) : type;
  }
  function addOptionalTypeMarker(type) {
    return strictNullChecks ? getUnionType([type, optionalType]) : type;
  }
  function removeOptionalTypeMarker(type) {
    return strictNullChecks ? removeType(type, optionalType) : type;
  }
  function propagateOptionalTypeMarker(type, node, wasOptional) {
    return wasOptional ? isOutermostOptionalChain(node) ? getOptionalType(type) : addOptionalTypeMarker(type) : type;
  }
  function getOptionalExpressionType(exprType, expression) {
    return isExpressionOfOptionalChainRoot(expression) ? getNonNullableType(exprType) : isOptionalChain(expression) ? removeOptionalTypeMarker(exprType) : exprType;
  }
  function removeMissingType(type, isOptional) {
    return exactOptionalPropertyTypes && isOptional ? removeType(type, missingType) : type;
  }
  function containsMissingType(type) {
    return type ***REMOVED******REMOVED******REMOVED*** missingType || !!(type.flags & 1048576 /* Union */) && type.types[0] ***REMOVED******REMOVED******REMOVED*** missingType;
  }
  function removeMissingOrUndefinedType(type) {
    return exactOptionalPropertyTypes ? removeType(type, missingType) : getTypeWithFacts(type, 524288 /* NEUndefined */);
  }
  function isCoercibleUnderDoubleEquals(source, target) {
    return (source.flags & (8 /* Number */ | 4 /* String */ | 512 /* BooleanLiteral */)) !***REMOVED******REMOVED*** 0 && (target.flags & (8 /* Number */ | 4 /* String */ | 16 /* Boolean */)) !***REMOVED******REMOVED*** 0;
  }
  function isObjectTypeWithInferableIndex(type) {
    const objectFlags ***REMOVED*** getObjectFlags(type);
    return type.flags & 2097152 /* Intersection */ ? every(type.types, isObjectTypeWithInferableIndex) : !!(type.symbol && (type.symbol.flags & (4096 /* ObjectLiteral */ | 2048 /* TypeLiteral */ | 384 /* Enum */ | 512 /* ValueModule */)) !***REMOVED******REMOVED*** 0 && !(type.symbol.flags & 32 /* Class */) && !typeHasCallOrConstructSignatures(type)) || !!(objectFlags & 4194304 /* ObjectRestType */) || !!(objectFlags & 1024 /* ReverseMapped */ && isObjectTypeWithInferableIndex(type.source));
  }
  function createSymbolWithType(source, type) {
    const symbol ***REMOVED*** createSymbol(source.flags, source.escapedName, getCheckFlags(source) & 8 /* Readonly */);
    symbol.declarations ***REMOVED*** source.declarations;
    symbol.parent ***REMOVED*** source.parent;
    symbol.links.type ***REMOVED*** type;
    symbol.links.target ***REMOVED*** source;
    if (source.valueDeclaration) {
      symbol.valueDeclaration ***REMOVED*** source.valueDeclaration;
    }
    const nameType ***REMOVED*** getSymbolLinks(source).nameType;
    if (nameType) {
      symbol.links.nameType ***REMOVED*** nameType;
    }
    return symbol;
  }
  function transformTypeOfMembers(type, f) {
    const members ***REMOVED*** createSymbolTable();
    for (const property of getPropertiesOfObjectType(type)) {
      const original ***REMOVED*** getTypeOfSymbol(property);
      const updated ***REMOVED*** f(original);
      members.set(property.escapedName, updated ***REMOVED******REMOVED******REMOVED*** original ? property : createSymbolWithType(property, updated));
    }
    return members;
  }
  function getRegularTypeOfObjectLiteral(type) {
    if (!(isObjectLiteralType(type) && getObjectFlags(type) & 8192 /* FreshLiteral */)) {
      return type;
    }
    const regularType ***REMOVED*** type.regularType;
    if (regularType) {
      return regularType;
    }
    const resolved ***REMOVED*** type;
    const members ***REMOVED*** transformTypeOfMembers(type, getRegularTypeOfObjectLiteral);
    const regularNew ***REMOVED*** createAnonymousType(resolved.symbol, members, resolved.callSignatures, resolved.constructSignatures, resolved.indexInfos);
    regularNew.flags ***REMOVED*** resolved.flags;
    regularNew.objectFlags |***REMOVED*** resolved.objectFlags & ~8192 /* FreshLiteral */;
    type.regularType ***REMOVED*** regularNew;
    return regularNew;
  }
  function createWideningContext(parent, propertyName, siblings) {
    return { parent, propertyName, siblings, resolvedProperties: void 0 };
  }
  function getSiblingsOfContext(context) {
    if (!context.siblings) {
      const siblings ***REMOVED*** [];
      for (const type of getSiblingsOfContext(context.parent)) {
        if (isObjectLiteralType(type)) {
          const prop ***REMOVED*** getPropertyOfObjectType(type, context.propertyName);
          if (prop) {
            forEachType(getTypeOfSymbol(prop), (t) ***REMOVED***> {
              siblings.push(t);
            });
          }
        }
      }
      context.siblings ***REMOVED*** siblings;
    }
    return context.siblings;
  }
  function getPropertiesOfContext(context) {
    if (!context.resolvedProperties) {
      const names ***REMOVED*** /* @__PURE__ */ new Map();
      for (const t of getSiblingsOfContext(context)) {
        if (isObjectLiteralType(t) && !(getObjectFlags(t) & 2097152 /* ContainsSpread */)) {
          for (const prop of getPropertiesOfType(t)) {
            names.set(prop.escapedName, prop);
          }
        }
      }
      context.resolvedProperties ***REMOVED*** arrayFrom(names.values());
    }
    return context.resolvedProperties;
  }
  function getWidenedProperty(prop, context) {
    if (!(prop.flags & 4 /* Property */)) {
      return prop;
    }
    const original ***REMOVED*** getTypeOfSymbol(prop);
    const propContext ***REMOVED*** context && createWideningContext(
      context,
      prop.escapedName,
      /*siblings*/
      void 0
    );
    const widened ***REMOVED*** getWidenedTypeWithContext(original, propContext);
    return widened ***REMOVED******REMOVED******REMOVED*** original ? prop : createSymbolWithType(prop, widened);
  }
  function getUndefinedProperty(prop) {
    const cached ***REMOVED*** undefinedProperties.get(prop.escapedName);
    if (cached) {
      return cached;
    }
    const result ***REMOVED*** createSymbolWithType(prop, undefinedOrMissingType);
    result.flags |***REMOVED*** 16777216 /* Optional */;
    undefinedProperties.set(prop.escapedName, result);
    return result;
  }
  function getWidenedTypeOfObjectLiteral(type, context) {
    const members ***REMOVED*** createSymbolTable();
    for (const prop of getPropertiesOfObjectType(type)) {
      members.set(prop.escapedName, getWidenedProperty(prop, context));
    }
    if (context) {
      for (const prop of getPropertiesOfContext(context)) {
        if (!members.has(prop.escapedName)) {
          members.set(prop.escapedName, getUndefinedProperty(prop));
        }
      }
    }
    const result ***REMOVED*** createAnonymousType(type.symbol, members, emptyArray, emptyArray, sameMap(getIndexInfosOfType(type), (info) ***REMOVED***> createIndexInfo(info.keyType, getWidenedType(info.type), info.isReadonly, info.declaration, info.components)));
    result.objectFlags |***REMOVED*** getObjectFlags(type) & (4096 /* JSLiteral */ | 262144 /* NonInferrableType */);
    return result;
  }
  function getWidenedType(type) {
    return getWidenedTypeWithContext(
      type,
      /*context*/
      void 0
    );
  }
  function getWidenedTypeWithContext(type, context) {
    if (getObjectFlags(type) & 196608 /* RequiresWidening */) {
      if (context ***REMOVED******REMOVED******REMOVED*** void 0 && type.widened) {
        return type.widened;
      }
      let result;
      if (type.flags & (1 /* Any */ | 98304 /* Nullable */)) {
        result ***REMOVED*** anyType;
      } else if (isObjectLiteralType(type)) {
        result ***REMOVED*** getWidenedTypeOfObjectLiteral(type, context);
      } else if (type.flags & 1048576 /* Union */) {
        const unionContext ***REMOVED*** context || createWideningContext(
          /*parent*/
          void 0,
          /*propertyName*/
          void 0,
          type.types
        );
        const widenedTypes ***REMOVED*** sameMap(type.types, (t) ***REMOVED***> t.flags & 98304 /* Nullable */ ? t : getWidenedTypeWithContext(t, unionContext));
        result ***REMOVED*** getUnionType(widenedTypes, some(widenedTypes, isEmptyObjectType) ? 2 /* Subtype */ : 1 /* Literal */);
      } else if (type.flags & 2097152 /* Intersection */) {
        result ***REMOVED*** getIntersectionType(sameMap(type.types, getWidenedType));
      } else if (isArrayOrTupleType(type)) {
        result ***REMOVED*** createTypeReference(type.target, sameMap(getTypeArguments(type), getWidenedType));
      }
      if (result && context ***REMOVED******REMOVED******REMOVED*** void 0) {
        type.widened ***REMOVED*** result;
      }
      return result || type;
    }
    return type;
  }
  function reportWideningErrorsInType(type) {
    var _a;
    let errorReported ***REMOVED*** false;
    if (getObjectFlags(type) & 65536 /* ContainsWideningType */) {
      if (type.flags & 1048576 /* Union */) {
        if (some(type.types, isEmptyObjectType)) {
          errorReported ***REMOVED*** true;
        } else {
          for (const t of type.types) {
            errorReported || (errorReported ***REMOVED*** reportWideningErrorsInType(t));
          }
        }
      } else if (isArrayOrTupleType(type)) {
        for (const t of getTypeArguments(type)) {
          errorReported || (errorReported ***REMOVED*** reportWideningErrorsInType(t));
        }
      } else if (isObjectLiteralType(type)) {
        for (const p of getPropertiesOfObjectType(type)) {
          const t ***REMOVED*** getTypeOfSymbol(p);
          if (getObjectFlags(t) & 65536 /* ContainsWideningType */) {
            errorReported ***REMOVED*** reportWideningErrorsInType(t);
            if (!errorReported) {
              const valueDeclaration ***REMOVED*** (_a ***REMOVED*** p.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find((d) ***REMOVED***> {
                var _a2;
                return ((_a2 ***REMOVED*** d.symbol.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _a2.parent) ***REMOVED******REMOVED******REMOVED*** type.symbol.valueDeclaration;
              });
              if (valueDeclaration) {
                error(valueDeclaration, Diagnostics.Object_literal_s_property_0_implicitly_has_an_1_type, symbolToString(p), typeToString(getWidenedType(t)));
                errorReported ***REMOVED*** true;
              }
            }
          }
        }
      }
    }
    return errorReported;
  }
  function reportImplicitAny(declaration, type, wideningKind) {
    const typeAsString ***REMOVED*** typeToString(getWidenedType(type));
    if (isInJSFile(declaration) && !isCheckJsEnabledForFile(getSourceFileOfNode(declaration), compilerOptions)) {
      return;
    }
    let diagnostic;
    switch (declaration.kind) {
      case 227 /* BinaryExpression */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
        diagnostic ***REMOVED*** noImplicitAny ? Diagnostics.Member_0_implicitly_has_an_1_type : Diagnostics.Member_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage;
        break;
      case 170 /* Parameter */:
        const param ***REMOVED*** declaration;
        if (isIdentifier(param.name)) {
          const originalKeywordKind ***REMOVED*** identifierToKeywordKind(param.name);
          if ((isCallSignatureDeclaration(param.parent) || isMethodSignature(param.parent) || isFunctionTypeNode(param.parent)) && param.parent.parameters.includes(param) && (resolveName(
            param,
            param.name.escapedText,
            788968 /* Type */,
            /*nameNotFoundMessage*/
            void 0,
            /*isUse*/
            true
          ) || originalKeywordKind && isTypeNodeKind(originalKeywordKind))) {
            const newName ***REMOVED*** "arg" + param.parent.parameters.indexOf(param);
            const typeName ***REMOVED*** declarationNameToString(param.name) + (param.dotDotDotToken ? "[]" : "");
            errorOrSuggestion(noImplicitAny, declaration, Diagnostics.Parameter_has_a_name_but_no_type_Did_you_mean_0_Colon_1, newName, typeName);
            return;
          }
        }
        diagnostic ***REMOVED*** declaration.dotDotDotToken ? noImplicitAny ? Diagnostics.Rest_parameter_0_implicitly_has_an_any_type : Diagnostics.Rest_parameter_0_implicitly_has_an_any_type_but_a_better_type_may_be_inferred_from_usage : noImplicitAny ? Diagnostics.Parameter_0_implicitly_has_an_1_type : Diagnostics.Parameter_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage;
        break;
      case 209 /* BindingElement */:
        diagnostic ***REMOVED*** Diagnostics.Binding_element_0_implicitly_has_an_1_type;
        if (!noImplicitAny) {
          return;
        }
        break;
      case 318 /* JSDocFunctionType */:
        error(declaration, Diagnostics.Function_type_which_lacks_return_type_annotation_implicitly_has_an_0_return_type, typeAsString);
        return;
      case 324 /* JSDocSignature */:
        if (noImplicitAny && isJSDocOverloadTag(declaration.parent)) {
          error(declaration.parent.tagName, Diagnostics.This_overload_implicitly_returns_the_type_0_because_it_lacks_a_return_type_annotation, typeAsString);
        }
        return;
      case 263 /* FunctionDeclaration */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        if (noImplicitAny && !declaration.name) {
          if (wideningKind ***REMOVED******REMOVED******REMOVED*** 3 /* GeneratorYield */) {
            error(declaration, Diagnostics.Generator_implicitly_has_yield_type_0_Consider_supplying_a_return_type_annotation, typeAsString);
          } else {
            error(declaration, Diagnostics.Function_expression_which_lacks_return_type_annotation_implicitly_has_an_0_return_type, typeAsString);
          }
          return;
        }
        diagnostic ***REMOVED*** !noImplicitAny ? Diagnostics._0_implicitly_has_an_1_return_type_but_a_better_type_may_be_inferred_from_usage : wideningKind ***REMOVED******REMOVED******REMOVED*** 3 /* GeneratorYield */ ? Diagnostics._0_which_lacks_return_type_annotation_implicitly_has_an_1_yield_type : Diagnostics._0_which_lacks_return_type_annotation_implicitly_has_an_1_return_type;
        break;
      case 201 /* MappedType */:
        if (noImplicitAny) {
          error(declaration, Diagnostics.Mapped_object_type_implicitly_has_an_any_template_type);
        }
        return;
      default:
        diagnostic ***REMOVED*** noImplicitAny ? Diagnostics.Variable_0_implicitly_has_an_1_type : Diagnostics.Variable_0_implicitly_has_an_1_type_but_a_better_type_may_be_inferred_from_usage;
    }
    errorOrSuggestion(noImplicitAny, declaration, diagnostic, declarationNameToString(getNameOfDeclaration(declaration)), typeAsString);
  }
  function shouldReportErrorsFromWideningWithContextualSignature(declaration, wideningKind) {
    const signature ***REMOVED*** getContextualSignatureForFunctionLikeDeclaration(declaration);
    if (!signature) {
      return true;
    }
    let returnType ***REMOVED*** getReturnTypeOfSignature(signature);
    const flags ***REMOVED*** getFunctionFlags(declaration);
    switch (wideningKind) {
      case 1 /* FunctionReturn */:
        if (flags & 1 /* Generator */) {
          returnType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, returnType, !!(flags & 2 /* Async */)) ?? returnType;
        } else if (flags & 2 /* Async */) {
          returnType ***REMOVED*** getAwaitedTypeNoAlias(returnType) ?? returnType;
        }
        return isGenericType(returnType);
      case 3 /* GeneratorYield */:
        const yieldType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(0 /* Yield */, returnType, !!(flags & 2 /* Async */));
        return !!yieldType && isGenericType(yieldType);
      case 2 /* GeneratorNext */:
        const nextType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(2 /* Next */, returnType, !!(flags & 2 /* Async */));
        return !!nextType && isGenericType(nextType);
    }
    return false;
  }
  function reportErrorsFromWidening(declaration, type, wideningKind) {
    addLazyDiagnostic(() ***REMOVED***> {
      if (noImplicitAny && getObjectFlags(type) & 65536 /* ContainsWideningType */) {
        if (!wideningKind || isFunctionLikeDeclaration(declaration) && shouldReportErrorsFromWideningWithContextualSignature(declaration, wideningKind)) {
          if (!reportWideningErrorsInType(type)) {
            reportImplicitAny(declaration, type, wideningKind);
          }
        }
      }
    });
  }
  function applyToParameterTypes(source, target, callback) {
    const sourceCount ***REMOVED*** getParameterCount(source);
    const targetCount ***REMOVED*** getParameterCount(target);
    const sourceRestType ***REMOVED*** getEffectiveRestType(source);
    const targetRestType ***REMOVED*** getEffectiveRestType(target);
    const targetNonRestCount ***REMOVED*** targetRestType ? targetCount - 1 : targetCount;
    const paramCount ***REMOVED*** sourceRestType ? targetNonRestCount : Math.min(sourceCount, targetNonRestCount);
    const sourceThisType ***REMOVED*** getThisTypeOfSignature(source);
    if (sourceThisType) {
      const targetThisType ***REMOVED*** getThisTypeOfSignature(target);
      if (targetThisType) {
        callback(sourceThisType, targetThisType);
      }
    }
    for (let i ***REMOVED*** 0; i < paramCount; i++) {
      callback(getTypeAtPosition(source, i), getTypeAtPosition(target, i));
    }
    if (targetRestType) {
      callback(getRestTypeAtPosition(
        source,
        paramCount,
        /*readonly*/
        isConstTypeVariable(targetRestType) && !someType(targetRestType, isMutableArrayLikeType)
      ), targetRestType);
    }
  }
  function applyToReturnTypes(source, target, callback) {
    const targetTypePredicate ***REMOVED*** getTypePredicateOfSignature(target);
    if (targetTypePredicate) {
      const sourceTypePredicate ***REMOVED*** getTypePredicateOfSignature(source);
      if (sourceTypePredicate && typePredicateKindsMatch(sourceTypePredicate, targetTypePredicate) && sourceTypePredicate.type && targetTypePredicate.type) {
        callback(sourceTypePredicate.type, targetTypePredicate.type);
        return;
      }
    }
    const targetReturnType ***REMOVED*** getReturnTypeOfSignature(target);
    if (couldContainTypeVariables(targetReturnType)) {
      callback(getReturnTypeOfSignature(source), targetReturnType);
    }
  }
  function createInferenceContext(typeParameters, signature, flags, compareTypes) {
    return createInferenceContextWorker(typeParameters.map(createInferenceInfo), signature, flags, compareTypes || compareTypesAssignable);
  }
  function cloneInferenceContext(context, extraFlags ***REMOVED*** 0) {
    return context && createInferenceContextWorker(map(context.inferences, cloneInferenceInfo), context.signature, context.flags | extraFlags, context.compareTypes);
  }
  function createInferenceContextWorker(inferences, signature, flags, compareTypes) {
    const context ***REMOVED*** {
      inferences,
      signature,
      flags,
      compareTypes,
      mapper: reportUnmeasurableMapper,
      // initialize to a noop mapper so the context object is available, but the underlying object shape is right upon construction
      nonFixingMapper: reportUnmeasurableMapper
    };
    context.mapper ***REMOVED*** makeFixingMapperForContext(context);
    context.nonFixingMapper ***REMOVED*** makeNonFixingMapperForContext(context);
    return context;
  }
  function makeFixingMapperForContext(context) {
    return makeDeferredTypeMapper(
      map(context.inferences, (i) ***REMOVED***> i.typeParameter),
      map(context.inferences, (inference, i) ***REMOVED***> () ***REMOVED***> {
        if (!inference.isFixed) {
          inferFromIntraExpressionSites(context);
          clearCachedInferences(context.inferences);
          inference.isFixed ***REMOVED*** true;
        }
        return getInferredType(context, i);
      })
    );
  }
  function makeNonFixingMapperForContext(context) {
    return makeDeferredTypeMapper(
      map(context.inferences, (i) ***REMOVED***> i.typeParameter),
      map(context.inferences, (_, i) ***REMOVED***> () ***REMOVED***> {
        return getInferredType(context, i);
      })
    );
  }
  function clearCachedInferences(inferences) {
    for (const inference of inferences) {
      if (!inference.isFixed) {
        inference.inferredType ***REMOVED*** void 0;
      }
    }
  }
  function addIntraExpressionInferenceSite(context, node, type) {
    (context.intraExpressionInferenceSites ?? (context.intraExpressionInferenceSites ***REMOVED*** [])).push({ node, type });
  }
  function inferFromIntraExpressionSites(context) {
    if (context.intraExpressionInferenceSites) {
      for (const { node, type } of context.intraExpressionInferenceSites) {
        const contextualType ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ ? getContextualTypeForObjectLiteralMethod(node, 2 /* NoConstraints */) : getContextualType(node, 2 /* NoConstraints */);
        if (contextualType) {
          inferTypes(context.inferences, type, contextualType);
        }
      }
      context.intraExpressionInferenceSites ***REMOVED*** void 0;
    }
  }
  function createInferenceInfo(typeParameter) {
    return {
      typeParameter,
      candidates: void 0,
      contraCandidates: void 0,
      inferredType: void 0,
      priority: void 0,
      topLevel: true,
      isFixed: false,
      impliedArity: void 0
    };
  }
  function cloneInferenceInfo(inference) {
    return {
      typeParameter: inference.typeParameter,
      candidates: inference.candidates && inference.candidates.slice(),
      contraCandidates: inference.contraCandidates && inference.contraCandidates.slice(),
      inferredType: inference.inferredType,
      priority: inference.priority,
      topLevel: inference.topLevel,
      isFixed: inference.isFixed,
      impliedArity: inference.impliedArity
    };
  }
  function cloneInferredPartOfContext(context) {
    const inferences ***REMOVED*** filter(context.inferences, hasInferenceCandidates);
    return inferences.length ? createInferenceContextWorker(map(inferences, cloneInferenceInfo), context.signature, context.flags, context.compareTypes) : void 0;
  }
  function getMapperFromContext(context) {
    return context && context.mapper;
  }
  function couldContainTypeVariables(type) {
    const objectFlags ***REMOVED*** getObjectFlags(type);
    if (objectFlags & 524288 /* CouldContainTypeVariablesComputed */) {
      return !!(objectFlags & 1048576 /* CouldContainTypeVariables */);
    }
    const result ***REMOVED*** !!(type.flags & 465829888 /* Instantiable */ || type.flags & 524288 /* Object */ && !isNonGenericTopLevelType(type) && (objectFlags & 4 /* Reference */ && (type.node || some(getTypeArguments(type), couldContainTypeVariables)) || objectFlags & 16 /* Anonymous */ && type.symbol && type.symbol.flags & (16 /* Function */ | 8192 /* Method */ | 32 /* Class */ | 2048 /* TypeLiteral */ | 4096 /* ObjectLiteral */) && type.symbol.declarations || objectFlags & (32 /* Mapped */ | 1024 /* ReverseMapped */ | 4194304 /* ObjectRestType */ | 8388608 /* InstantiationExpressionType */)) || type.flags & 3145728 /* UnionOrIntersection */ && !(type.flags & 1024 /* EnumLiteral */) && !isNonGenericTopLevelType(type) && some(type.types, couldContainTypeVariables));
    if (type.flags & 3899393 /* ObjectFlagsType */) {
      type.objectFlags |***REMOVED*** 524288 /* CouldContainTypeVariablesComputed */ | (result ? 1048576 /* CouldContainTypeVariables */ : 0);
    }
    return result;
  }
  function isNonGenericTopLevelType(type) {
    if (type.aliasSymbol && !type.aliasTypeArguments) {
      const declaration ***REMOVED*** getDeclarationOfKind(type.aliasSymbol, 266 /* TypeAliasDeclaration */);
      return !!(declaration && findAncestor(declaration.parent, (n) ***REMOVED***> n.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? true : n.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ ? false : "quit"));
    }
    return false;
  }
  function isTypeParameterAtTopLevel(type, tp, depth ***REMOVED*** 0) {
    return !!(type ***REMOVED******REMOVED******REMOVED*** tp || type.flags & 3145728 /* UnionOrIntersection */ && some(type.types, (t) ***REMOVED***> isTypeParameterAtTopLevel(t, tp, depth)) || depth < 3 && type.flags & 16777216 /* Conditional */ && (isTypeParameterAtTopLevel(getTrueTypeFromConditionalType(type), tp, depth + 1) || isTypeParameterAtTopLevel(getFalseTypeFromConditionalType(type), tp, depth + 1)));
  }
  function isTypeParameterAtTopLevelInReturnType(signature, typeParameter) {
    const typePredicate ***REMOVED*** getTypePredicateOfSignature(signature);
    return typePredicate ? !!typePredicate.type && isTypeParameterAtTopLevel(typePredicate.type, typeParameter) : isTypeParameterAtTopLevel(getReturnTypeOfSignature(signature), typeParameter);
  }
  function createEmptyObjectTypeFromStringLiteral(type) {
    const members ***REMOVED*** createSymbolTable();
    forEachType(type, (t) ***REMOVED***> {
      if (!(t.flags & 128 /* StringLiteral */)) {
        return;
      }
      const name ***REMOVED*** escapeLeadingUnderscores(t.value);
      const literalProp ***REMOVED*** createSymbol(4 /* Property */, name);
      literalProp.links.type ***REMOVED*** anyType;
      if (t.symbol) {
        literalProp.declarations ***REMOVED*** t.symbol.declarations;
        literalProp.valueDeclaration ***REMOVED*** t.symbol.valueDeclaration;
      }
      members.set(name, literalProp);
    });
    const indexInfos ***REMOVED*** type.flags & 4 /* String */ ? [createIndexInfo(
      stringType,
      emptyObjectType,
      /*isReadonly*/
      false
    )] : emptyArray;
    return createAnonymousType(
      /*symbol*/
      void 0,
      members,
      emptyArray,
      emptyArray,
      indexInfos
    );
  }
  function inferTypeForHomomorphicMappedType(source, target, constraint) {
    const cacheKey ***REMOVED*** source.id + "," + target.id + "," + constraint.id;
    if (reverseHomomorphicMappedCache.has(cacheKey)) {
      return reverseHomomorphicMappedCache.get(cacheKey);
    }
    const type ***REMOVED*** createReverseMappedType(source, target, constraint);
    reverseHomomorphicMappedCache.set(cacheKey, type);
    return type;
  }
  function isPartiallyInferableType(type) {
    return !(getObjectFlags(type) & 262144 /* NonInferrableType */) || isObjectLiteralType(type) && some(getPropertiesOfType(type), (prop) ***REMOVED***> isPartiallyInferableType(getTypeOfSymbol(prop))) || isTupleType(type) && some(getElementTypes(type), isPartiallyInferableType);
  }
  function createReverseMappedType(source, target, constraint) {
    if (!(getIndexInfoOfType(source, stringType) || getPropertiesOfType(source).length !***REMOVED******REMOVED*** 0 && isPartiallyInferableType(source))) {
      return void 0;
    }
    if (isArrayType(source)) {
      const elementType ***REMOVED*** inferReverseMappedType(getTypeArguments(source)[0], target, constraint);
      if (!elementType) {
        return void 0;
      }
      return createArrayType(elementType, isReadonlyArrayType(source));
    }
    if (isTupleType(source)) {
      const elementTypes ***REMOVED*** map(getElementTypes(source), (t) ***REMOVED***> inferReverseMappedType(t, target, constraint));
      if (!every(elementTypes, (t) ***REMOVED***> !!t)) {
        return void 0;
      }
      const elementFlags ***REMOVED*** getMappedTypeModifiers(target) & 4 /* IncludeOptional */ ? sameMap(source.target.elementFlags, (f) ***REMOVED***> f & 2 /* Optional */ ? 1 /* Required */ : f) : source.target.elementFlags;
      return createTupleType(elementTypes, elementFlags, source.target.readonly, source.target.labeledElementDeclarations);
    }
    const reversed ***REMOVED*** createObjectType(
      1024 /* ReverseMapped */ | 16 /* Anonymous */,
      /*symbol*/
      void 0
    );
    reversed.source ***REMOVED*** source;
    reversed.mappedType ***REMOVED*** target;
    reversed.constraintType ***REMOVED*** constraint;
    return reversed;
  }
  function getTypeOfReverseMappedSymbol(symbol) {
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.type) {
      links.type ***REMOVED*** inferReverseMappedType(symbol.links.propertyType, symbol.links.mappedType, symbol.links.constraintType) || unknownType;
    }
    return links.type;
  }
  function inferReverseMappedTypeWorker(sourceType, target, constraint) {
    const typeParameter ***REMOVED*** getIndexedAccessType(constraint.type, getTypeParameterFromMappedType(target));
    const templateType ***REMOVED*** getTemplateTypeFromMappedType(target);
    const inference ***REMOVED*** createInferenceInfo(typeParameter);
    inferTypes([inference], sourceType, templateType);
    return getTypeFromInference(inference) || unknownType;
  }
  function inferReverseMappedType(source, target, constraint) {
    const cacheKey ***REMOVED*** source.id + "," + target.id + "," + constraint.id;
    if (reverseMappedCache.has(cacheKey)) {
      return reverseMappedCache.get(cacheKey) || unknownType;
    }
    reverseMappedSourceStack.push(source);
    reverseMappedTargetStack.push(target);
    const saveExpandingFlags ***REMOVED*** reverseExpandingFlags;
    if (isDeeplyNestedType(source, reverseMappedSourceStack, reverseMappedSourceStack.length, 2)) reverseExpandingFlags |***REMOVED*** 1 /* Source */;
    if (isDeeplyNestedType(target, reverseMappedTargetStack, reverseMappedTargetStack.length, 2)) reverseExpandingFlags |***REMOVED*** 2 /* Target */;
    let type;
    if (reverseExpandingFlags !***REMOVED******REMOVED*** 3 /* Both */) {
      type ***REMOVED*** inferReverseMappedTypeWorker(source, target, constraint);
    }
    reverseMappedSourceStack.pop();
    reverseMappedTargetStack.pop();
    reverseExpandingFlags ***REMOVED*** saveExpandingFlags;
    reverseMappedCache.set(cacheKey, type);
    return type;
  }
  function* getUnmatchedProperties(source, target, requireOptionalProperties, matchDiscriminantProperties) {
    const properties ***REMOVED*** getPropertiesOfType(target);
    for (const targetProp of properties) {
      if (isStaticPrivateIdentifierProperty(targetProp)) {
        continue;
      }
      if (requireOptionalProperties || !(targetProp.flags & 16777216 /* Optional */ || getCheckFlags(targetProp) & 48 /* Partial */)) {
        const sourceProp ***REMOVED*** getPropertyOfType(source, targetProp.escapedName);
        if (!sourceProp) {
          yield targetProp;
        } else if (matchDiscriminantProperties) {
          const targetType ***REMOVED*** getTypeOfSymbol(targetProp);
          if (targetType.flags & 109472 /* Unit */) {
            const sourceType ***REMOVED*** getTypeOfSymbol(sourceProp);
            if (!(sourceType.flags & 1 /* Any */ || getRegularTypeOfLiteralType(sourceType) ***REMOVED******REMOVED******REMOVED*** getRegularTypeOfLiteralType(targetType))) {
              yield targetProp;
            }
          }
        }
      }
    }
  }
  function getUnmatchedProperty(source, target, requireOptionalProperties, matchDiscriminantProperties) {
    return firstOrUndefinedIterator(getUnmatchedProperties(source, target, requireOptionalProperties, matchDiscriminantProperties));
  }
  function tupleTypesDefinitelyUnrelated(source, target) {
    return !(target.target.combinedFlags & 8 /* Variadic */) && target.target.minLength > source.target.minLength || !(target.target.combinedFlags & 12 /* Variable */) && (!!(source.target.combinedFlags & 12 /* Variable */) || target.target.fixedLength < source.target.fixedLength);
  }
  function typesDefinitelyUnrelated(source, target) {
    return isTupleType(source) && isTupleType(target) ? tupleTypesDefinitelyUnrelated(source, target) : !!getUnmatchedProperty(
      source,
      target,
      /*requireOptionalProperties*/
      false,
      /*matchDiscriminantProperties*/
      true
    ) && !!getUnmatchedProperty(
      target,
      source,
      /*requireOptionalProperties*/
      false,
      /*matchDiscriminantProperties*/
      false
    );
  }
  function getTypeFromInference(inference) {
    return inference.candidates ? getUnionType(inference.candidates, 2 /* Subtype */) : inference.contraCandidates ? getIntersectionType(inference.contraCandidates) : void 0;
  }
  function hasSkipDirectInferenceFlag(node) {
    return !!getNodeLinks(node).skipDirectInference;
  }
  function isFromInferenceBlockedSource(type) {
    return !!(type.symbol && some(type.symbol.declarations, hasSkipDirectInferenceFlag));
  }
  function templateLiteralTypesDefinitelyUnrelated(source, target) {
    const sourceStart ***REMOVED*** source.texts[0];
    const targetStart ***REMOVED*** target.texts[0];
    const sourceEnd ***REMOVED*** source.texts[source.texts.length - 1];
    const targetEnd ***REMOVED*** target.texts[target.texts.length - 1];
    const startLen ***REMOVED*** Math.min(sourceStart.length, targetStart.length);
    const endLen ***REMOVED*** Math.min(sourceEnd.length, targetEnd.length);
    return sourceStart.slice(0, startLen) !***REMOVED******REMOVED*** targetStart.slice(0, startLen) || sourceEnd.slice(sourceEnd.length - endLen) !***REMOVED******REMOVED*** targetEnd.slice(targetEnd.length - endLen);
  }
  function isValidNumberString(s, roundTripOnly) {
    if (s ***REMOVED******REMOVED******REMOVED*** "") return false;
    const n ***REMOVED*** +s;
    return isFinite(n) && (!roundTripOnly || "" + n ***REMOVED******REMOVED******REMOVED*** s);
  }
  function parseBigIntLiteralType(text) {
    return getBigIntLiteralType(parseValidBigInt(text));
  }
  function isMemberOfStringMapping(source, target) {
    if (target.flags & 1 /* Any */) {
      return true;
    }
    if (target.flags & (4 /* String */ | 134217728 /* TemplateLiteral */)) {
      return isTypeAssignableTo(source, target);
    }
    if (target.flags & 268435456 /* StringMapping */) {
      const mappingStack ***REMOVED*** [];
      while (target.flags & 268435456 /* StringMapping */) {
        mappingStack.unshift(target.symbol);
        target ***REMOVED*** target.type;
      }
      const mappedSource ***REMOVED*** reduceLeft(mappingStack, (memo, value) ***REMOVED***> getStringMappingType(value, memo), source);
      return mappedSource ***REMOVED******REMOVED******REMOVED*** source && isMemberOfStringMapping(source, target);
    }
    return false;
  }
  function isValidTypeForTemplateLiteralPlaceholder(source, target) {
    if (target.flags & 2097152 /* Intersection */) {
      return every(target.types, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** emptyTypeLiteralType || isValidTypeForTemplateLiteralPlaceholder(source, t));
    }
    if (target.flags & 4 /* String */ || isTypeAssignableTo(source, target)) {
      return true;
    }
    if (source.flags & 128 /* StringLiteral */) {
      const value ***REMOVED*** source.value;
      return !!(target.flags & 8 /* Number */ && isValidNumberString(
        value,
        /*roundTripOnly*/
        false
      ) || target.flags & 64 /* BigInt */ && isValidBigIntString(
        value,
        /*roundTripOnly*/
        false
      ) || target.flags & (512 /* BooleanLiteral */ | 98304 /* Nullable */) && value ***REMOVED******REMOVED******REMOVED*** target.intrinsicName || target.flags & 268435456 /* StringMapping */ && isMemberOfStringMapping(source, target) || target.flags & 134217728 /* TemplateLiteral */ && isTypeMatchedByTemplateLiteralType(source, target));
    }
    if (source.flags & 134217728 /* TemplateLiteral */) {
      const texts ***REMOVED*** source.texts;
      return texts.length ***REMOVED******REMOVED******REMOVED*** 2 && texts[0] ***REMOVED******REMOVED******REMOVED*** "" && texts[1] ***REMOVED******REMOVED******REMOVED*** "" && isTypeAssignableTo(source.types[0], target);
    }
    return false;
  }
  function inferTypesFromTemplateLiteralType(source, target) {
    return source.flags & 128 /* StringLiteral */ ? inferFromLiteralPartsToTemplateLiteral([source.value], emptyArray, target) : source.flags & 134217728 /* TemplateLiteral */ ? arrayIsEqualTo(source.texts, target.texts) ? map(source.types, (s, i) ***REMOVED***> {
      return isTypeAssignableTo(getBaseConstraintOrType(s), getBaseConstraintOrType(target.types[i])) ? s : getStringLikeTypeForType(s);
    }) : inferFromLiteralPartsToTemplateLiteral(source.texts, source.types, target) : void 0;
  }
  function isTypeMatchedByTemplateLiteralType(source, target) {
    const inferences ***REMOVED*** inferTypesFromTemplateLiteralType(source, target);
    return !!inferences && every(inferences, (r, i) ***REMOVED***> isValidTypeForTemplateLiteralPlaceholder(r, target.types[i]));
  }
  function getStringLikeTypeForType(type) {
    return type.flags & (1 /* Any */ | 402653316 /* StringLike */) ? type : getTemplateLiteralType(["", ""], [type]);
  }
  function inferFromLiteralPartsToTemplateLiteral(sourceTexts, sourceTypes, target) {
    const lastSourceIndex ***REMOVED*** sourceTexts.length - 1;
    const sourceStartText ***REMOVED*** sourceTexts[0];
    const sourceEndText ***REMOVED*** sourceTexts[lastSourceIndex];
    const targetTexts ***REMOVED*** target.texts;
    const lastTargetIndex ***REMOVED*** targetTexts.length - 1;
    const targetStartText ***REMOVED*** targetTexts[0];
    const targetEndText ***REMOVED*** targetTexts[lastTargetIndex];
    if (lastSourceIndex ***REMOVED******REMOVED******REMOVED*** 0 && sourceStartText.length < targetStartText.length + targetEndText.length || !sourceStartText.startsWith(targetStartText) || !sourceEndText.endsWith(targetEndText)) return void 0;
    const remainingEndText ***REMOVED*** sourceEndText.slice(0, sourceEndText.length - targetEndText.length);
    const matches ***REMOVED*** [];
    let seg ***REMOVED*** 0;
    let pos ***REMOVED*** targetStartText.length;
    for (let i ***REMOVED*** 1; i < lastTargetIndex; i++) {
      const delim ***REMOVED*** targetTexts[i];
      if (delim.length > 0) {
        let s ***REMOVED*** seg;
        let p ***REMOVED*** pos;
        while (true) {
          p ***REMOVED*** getSourceText(s).indexOf(delim, p);
          if (p >***REMOVED*** 0) break;
          s++;
          if (s ***REMOVED******REMOVED******REMOVED*** sourceTexts.length) return void 0;
          p ***REMOVED*** 0;
        }
        addMatch(s, p);
        pos +***REMOVED*** delim.length;
      } else if (pos < getSourceText(seg).length) {
        addMatch(seg, pos + 1);
      } else if (seg < lastSourceIndex) {
        addMatch(seg + 1, 0);
      } else {
        return void 0;
      }
    }
    addMatch(lastSourceIndex, getSourceText(lastSourceIndex).length);
    return matches;
    function getSourceText(index) {
      return index < lastSourceIndex ? sourceTexts[index] : remainingEndText;
    }
    function addMatch(s, p) {
      const matchType ***REMOVED*** s ***REMOVED******REMOVED******REMOVED*** seg ? getStringLiteralType(getSourceText(s).slice(pos, p)) : getTemplateLiteralType(
        [sourceTexts[seg].slice(pos), ...sourceTexts.slice(seg + 1, s), getSourceText(s).slice(0, p)],
        sourceTypes.slice(seg, s)
      );
      matches.push(matchType);
      seg ***REMOVED*** s;
      pos ***REMOVED*** p;
    }
  }
  function inferTypes(inferences, originalSource, originalTarget, priority ***REMOVED*** 0 /* None */, contravariant ***REMOVED*** false) {
    let bivariant ***REMOVED*** false;
    let propagationType;
    let inferencePriority ***REMOVED*** 2048 /* MaxValue */;
    let visited;
    let sourceStack;
    let targetStack;
    let expandingFlags ***REMOVED*** 0 /* None */;
    inferFromTypes(originalSource, originalTarget);
    function inferFromTypes(source, target) {
      if (!couldContainTypeVariables(target) || isNoInferType(target)) {
        return;
      }
      if (source ***REMOVED******REMOVED******REMOVED*** wildcardType || source ***REMOVED******REMOVED******REMOVED*** blockedStringType) {
        const savePropagationType ***REMOVED*** propagationType;
        propagationType ***REMOVED*** source;
        inferFromTypes(target, target);
        propagationType ***REMOVED*** savePropagationType;
        return;
      }
      if (source.aliasSymbol && source.aliasSymbol ***REMOVED******REMOVED******REMOVED*** target.aliasSymbol) {
        if (source.aliasTypeArguments) {
          const params ***REMOVED*** getSymbolLinks(source.aliasSymbol).typeParameters;
          const minParams ***REMOVED*** getMinTypeArgumentCount(params);
          const sourceTypes ***REMOVED*** fillMissingTypeArguments(source.aliasTypeArguments, params, minParams, isInJSFile(source.aliasSymbol.valueDeclaration));
          const targetTypes ***REMOVED*** fillMissingTypeArguments(target.aliasTypeArguments, params, minParams, isInJSFile(source.aliasSymbol.valueDeclaration));
          inferFromTypeArguments(sourceTypes, targetTypes, getAliasVariances(source.aliasSymbol));
        }
        return;
      }
      if (source ***REMOVED******REMOVED******REMOVED*** target && source.flags & 3145728 /* UnionOrIntersection */) {
        for (const t of source.types) {
          inferFromTypes(t, t);
        }
        return;
      }
      if (target.flags & 1048576 /* Union */) {
        const [tempSources, tempTargets] ***REMOVED*** inferFromMatchingTypes(source.flags & 1048576 /* Union */ ? source.types : [source], target.types, isTypeOrBaseIdenticalTo);
        const [sources, targets] ***REMOVED*** inferFromMatchingTypes(tempSources, tempTargets, isTypeCloselyMatchedBy);
        if (targets.length ***REMOVED******REMOVED******REMOVED*** 0) {
          return;
        }
        target ***REMOVED*** getUnionType(targets);
        if (sources.length ***REMOVED******REMOVED******REMOVED*** 0) {
          inferWithPriority(source, target, 1 /* NakedTypeVariable */);
          return;
        }
        source ***REMOVED*** getUnionType(sources);
      } else if (target.flags & 2097152 /* Intersection */ && !every(target.types, isNonGenericObjectType)) {
        if (!(source.flags & 1048576 /* Union */)) {
          const [sources, targets] ***REMOVED*** inferFromMatchingTypes(source.flags & 2097152 /* Intersection */ ? source.types : [source], target.types, isTypeIdenticalTo);
          if (sources.length ***REMOVED******REMOVED******REMOVED*** 0 || targets.length ***REMOVED******REMOVED******REMOVED*** 0) {
            return;
          }
          source ***REMOVED*** getIntersectionType(sources);
          target ***REMOVED*** getIntersectionType(targets);
        }
      }
      if (target.flags & (8388608 /* IndexedAccess */ | 33554432 /* Substitution */)) {
        if (isNoInferType(target)) {
          return;
        }
        target ***REMOVED*** getActualTypeVariable(target);
      }
      if (target.flags & 8650752 /* TypeVariable */) {
        if (isFromInferenceBlockedSource(source)) {
          return;
        }
        const inference ***REMOVED*** getInferenceInfoForType(target);
        if (inference) {
          if (getObjectFlags(source) & 262144 /* NonInferrableType */ || source ***REMOVED******REMOVED******REMOVED*** nonInferrableAnyType) {
            return;
          }
          if (!inference.isFixed) {
            const candidate ***REMOVED*** propagationType || source;
            if (candidate ***REMOVED******REMOVED******REMOVED*** blockedStringType) {
              return;
            }
            if (inference.priority ***REMOVED******REMOVED******REMOVED*** void 0 || priority < inference.priority) {
              inference.candidates ***REMOVED*** void 0;
              inference.contraCandidates ***REMOVED*** void 0;
              inference.topLevel ***REMOVED*** true;
              inference.priority ***REMOVED*** priority;
            }
            if (priority ***REMOVED******REMOVED******REMOVED*** inference.priority) {
              if (contravariant && !bivariant) {
                if (!contains(inference.contraCandidates, candidate)) {
                  inference.contraCandidates ***REMOVED*** append(inference.contraCandidates, candidate);
                  clearCachedInferences(inferences);
                }
              } else if (!contains(inference.candidates, candidate)) {
                inference.candidates ***REMOVED*** append(inference.candidates, candidate);
                clearCachedInferences(inferences);
              }
            }
            if (!(priority & 128 /* ReturnType */) && target.flags & 262144 /* TypeParameter */ && inference.topLevel && !isTypeParameterAtTopLevel(originalTarget, target)) {
              inference.topLevel ***REMOVED*** false;
              clearCachedInferences(inferences);
            }
          }
          inferencePriority ***REMOVED*** Math.min(inferencePriority, priority);
          return;
        }
        const simplified ***REMOVED*** getSimplifiedType(
          target,
          /*writing*/
          false
        );
        if (simplified !***REMOVED******REMOVED*** target) {
          inferFromTypes(source, simplified);
        } else if (target.flags & 8388608 /* IndexedAccess */) {
          const indexType ***REMOVED*** getSimplifiedType(
            target.indexType,
            /*writing*/
            false
          );
          if (indexType.flags & 465829888 /* Instantiable */) {
            const simplified2 ***REMOVED*** distributeIndexOverObjectType(
              getSimplifiedType(
                target.objectType,
                /*writing*/
                false
              ),
              indexType,
              /*writing*/
              false
            );
            if (simplified2 && simplified2 !***REMOVED******REMOVED*** target) {
              inferFromTypes(source, simplified2);
            }
          }
        }
      }
      if (getObjectFlags(source) & 4 /* Reference */ && getObjectFlags(target) & 4 /* Reference */ && (source.target ***REMOVED******REMOVED******REMOVED*** target.target || isArrayType(source) && isArrayType(target)) && !(source.node && target.node)) {
        inferFromTypeArguments(getTypeArguments(source), getTypeArguments(target), getVariances(source.target));
      } else if (source.flags & 4194304 /* Index */ && target.flags & 4194304 /* Index */) {
        inferFromContravariantTypes(source.type, target.type);
      } else if ((isLiteralType(source) || source.flags & 4 /* String */) && target.flags & 4194304 /* Index */) {
        const empty ***REMOVED*** createEmptyObjectTypeFromStringLiteral(source);
        inferFromContravariantTypesWithPriority(empty, target.type, 256 /* LiteralKeyof */);
      } else if (source.flags & 8388608 /* IndexedAccess */ && target.flags & 8388608 /* IndexedAccess */) {
        inferFromTypes(source.objectType, target.objectType);
        inferFromTypes(source.indexType, target.indexType);
      } else if (source.flags & 268435456 /* StringMapping */ && target.flags & 268435456 /* StringMapping */) {
        if (source.symbol ***REMOVED******REMOVED******REMOVED*** target.symbol) {
          inferFromTypes(source.type, target.type);
        }
      } else if (source.flags & 33554432 /* Substitution */) {
        inferFromTypes(source.baseType, target);
        inferWithPriority(getSubstitutionIntersection(source), target, 4 /* SubstituteSource */);
      } else if (target.flags & 16777216 /* Conditional */) {
        invokeOnce(source, target, inferToConditionalType);
      } else if (target.flags & 3145728 /* UnionOrIntersection */) {
        inferToMultipleTypes(source, target.types, target.flags);
      } else if (source.flags & 1048576 /* Union */) {
        const sourceTypes ***REMOVED*** source.types;
        for (const sourceType of sourceTypes) {
          inferFromTypes(sourceType, target);
        }
      } else if (target.flags & 134217728 /* TemplateLiteral */) {
        inferToTemplateLiteralType(source, target);
      } else {
        source ***REMOVED*** getReducedType(source);
        if (isGenericMappedType(source) && isGenericMappedType(target)) {
          invokeOnce(source, target, inferFromGenericMappedTypes);
        }
        if (!(priority & 512 /* NoConstraints */ && source.flags & (2097152 /* Intersection */ | 465829888 /* Instantiable */))) {
          const apparentSource ***REMOVED*** getApparentType(source);
          if (apparentSource !***REMOVED******REMOVED*** source && !(apparentSource.flags & (524288 /* Object */ | 2097152 /* Intersection */))) {
            return inferFromTypes(apparentSource, target);
          }
          source ***REMOVED*** apparentSource;
        }
        if (source.flags & (524288 /* Object */ | 2097152 /* Intersection */)) {
          invokeOnce(source, target, inferFromObjectTypes);
        }
      }
    }
    function inferWithPriority(source, target, newPriority) {
      const savePriority ***REMOVED*** priority;
      priority |***REMOVED*** newPriority;
      inferFromTypes(source, target);
      priority ***REMOVED*** savePriority;
    }
    function inferFromContravariantTypesWithPriority(source, target, newPriority) {
      const savePriority ***REMOVED*** priority;
      priority |***REMOVED*** newPriority;
      inferFromContravariantTypes(source, target);
      priority ***REMOVED*** savePriority;
    }
    function inferToMultipleTypesWithPriority(source, targets, targetFlags, newPriority) {
      const savePriority ***REMOVED*** priority;
      priority |***REMOVED*** newPriority;
      inferToMultipleTypes(source, targets, targetFlags);
      priority ***REMOVED*** savePriority;
    }
    function invokeOnce(source, target, action) {
      const key ***REMOVED*** source.id + "," + target.id;
      const status ***REMOVED*** visited && visited.get(key);
      if (status !***REMOVED******REMOVED*** void 0) {
        inferencePriority ***REMOVED*** Math.min(inferencePriority, status);
        return;
      }
      (visited || (visited ***REMOVED*** /* @__PURE__ */ new Map())).set(key, -1 /* Circularity */);
      const saveInferencePriority ***REMOVED*** inferencePriority;
      inferencePriority ***REMOVED*** 2048 /* MaxValue */;
      const saveExpandingFlags ***REMOVED*** expandingFlags;
      (sourceStack ?? (sourceStack ***REMOVED*** [])).push(source);
      (targetStack ?? (targetStack ***REMOVED*** [])).push(target);
      if (isDeeplyNestedType(source, sourceStack, sourceStack.length, 2)) expandingFlags |***REMOVED*** 1 /* Source */;
      if (isDeeplyNestedType(target, targetStack, targetStack.length, 2)) expandingFlags |***REMOVED*** 2 /* Target */;
      if (expandingFlags !***REMOVED******REMOVED*** 3 /* Both */) {
        action(source, target);
      } else {
        inferencePriority ***REMOVED*** -1 /* Circularity */;
      }
      targetStack.pop();
      sourceStack.pop();
      expandingFlags ***REMOVED*** saveExpandingFlags;
      visited.set(key, inferencePriority);
      inferencePriority ***REMOVED*** Math.min(inferencePriority, saveInferencePriority);
    }
    function inferFromMatchingTypes(sources, targets, matches) {
      let matchedSources;
      let matchedTargets;
      for (const t of targets) {
        for (const s of sources) {
          if (matches(s, t)) {
            inferFromTypes(s, t);
            matchedSources ***REMOVED*** appendIfUnique(matchedSources, s);
            matchedTargets ***REMOVED*** appendIfUnique(matchedTargets, t);
          }
        }
      }
      return [
        matchedSources ? filter(sources, (t) ***REMOVED***> !contains(matchedSources, t)) : sources,
        matchedTargets ? filter(targets, (t) ***REMOVED***> !contains(matchedTargets, t)) : targets
      ];
    }
    function inferFromTypeArguments(sourceTypes, targetTypes, variances) {
      const count ***REMOVED*** sourceTypes.length < targetTypes.length ? sourceTypes.length : targetTypes.length;
      for (let i ***REMOVED*** 0; i < count; i++) {
        if (i < variances.length && (variances[i] & 7 /* VarianceMask */) ***REMOVED******REMOVED******REMOVED*** 2 /* Contravariant */) {
          inferFromContravariantTypes(sourceTypes[i], targetTypes[i]);
        } else {
          inferFromTypes(sourceTypes[i], targetTypes[i]);
        }
      }
    }
    function inferFromContravariantTypes(source, target) {
      contravariant ***REMOVED*** !contravariant;
      inferFromTypes(source, target);
      contravariant ***REMOVED*** !contravariant;
    }
    function inferFromContravariantTypesIfStrictFunctionTypes(source, target) {
      if (strictFunctionTypes || priority & 1024 /* AlwaysStrict */) {
        inferFromContravariantTypes(source, target);
      } else {
        inferFromTypes(source, target);
      }
    }
    function getInferenceInfoForType(type) {
      if (type.flags & 8650752 /* TypeVariable */) {
        for (const inference of inferences) {
          if (type ***REMOVED******REMOVED******REMOVED*** inference.typeParameter) {
            return inference;
          }
        }
      }
      return void 0;
    }
    function getSingleTypeVariableFromIntersectionTypes(types) {
      let typeVariable;
      for (const type of types) {
        const t ***REMOVED*** type.flags & 2097152 /* Intersection */ && find(type.types, (t2) ***REMOVED***> !!getInferenceInfoForType(t2));
        if (!t || typeVariable && t !***REMOVED******REMOVED*** typeVariable) {
          return void 0;
        }
        typeVariable ***REMOVED*** t;
      }
      return typeVariable;
    }
    function inferToMultipleTypes(source, targets, targetFlags) {
      let typeVariableCount ***REMOVED*** 0;
      if (targetFlags & 1048576 /* Union */) {
        let nakedTypeVariable;
        const sources ***REMOVED*** source.flags & 1048576 /* Union */ ? source.types : [source];
        const matched ***REMOVED*** new Array(sources.length);
        let inferenceCircularity ***REMOVED*** false;
        for (const t of targets) {
          if (getInferenceInfoForType(t)) {
            nakedTypeVariable ***REMOVED*** t;
            typeVariableCount++;
          } else {
            for (let i ***REMOVED*** 0; i < sources.length; i++) {
              const saveInferencePriority ***REMOVED*** inferencePriority;
              inferencePriority ***REMOVED*** 2048 /* MaxValue */;
              inferFromTypes(sources[i], t);
              if (inferencePriority ***REMOVED******REMOVED******REMOVED*** priority) matched[i] ***REMOVED*** true;
              inferenceCircularity ***REMOVED*** inferenceCircularity || inferencePriority ***REMOVED******REMOVED******REMOVED*** -1 /* Circularity */;
              inferencePriority ***REMOVED*** Math.min(inferencePriority, saveInferencePriority);
            }
          }
        }
        if (typeVariableCount ***REMOVED******REMOVED******REMOVED*** 0) {
          const intersectionTypeVariable ***REMOVED*** getSingleTypeVariableFromIntersectionTypes(targets);
          if (intersectionTypeVariable) {
            inferWithPriority(source, intersectionTypeVariable, 1 /* NakedTypeVariable */);
          }
          return;
        }
        if (typeVariableCount ***REMOVED******REMOVED******REMOVED*** 1 && !inferenceCircularity) {
          const unmatched ***REMOVED*** flatMap(sources, (s, i) ***REMOVED***> matched[i] ? void 0 : s);
          if (unmatched.length) {
            inferFromTypes(getUnionType(unmatched), nakedTypeVariable);
            return;
          }
        }
      } else {
        for (const t of targets) {
          if (getInferenceInfoForType(t)) {
            typeVariableCount++;
          } else {
            inferFromTypes(source, t);
          }
        }
      }
      if (targetFlags & 2097152 /* Intersection */ ? typeVariableCount ***REMOVED******REMOVED******REMOVED*** 1 : typeVariableCount > 0) {
        for (const t of targets) {
          if (getInferenceInfoForType(t)) {
            inferWithPriority(source, t, 1 /* NakedTypeVariable */);
          }
        }
      }
    }
    function inferToMappedType(source, target, constraintType) {
      if (constraintType.flags & 1048576 /* Union */ || constraintType.flags & 2097152 /* Intersection */) {
        let result ***REMOVED*** false;
        for (const type of constraintType.types) {
          result ***REMOVED*** inferToMappedType(source, target, type) || result;
        }
        return result;
      }
      if (constraintType.flags & 4194304 /* Index */) {
        const inference ***REMOVED*** getInferenceInfoForType(constraintType.type);
        if (inference && !inference.isFixed && !isFromInferenceBlockedSource(source)) {
          const inferredType ***REMOVED*** inferTypeForHomomorphicMappedType(source, target, constraintType);
          if (inferredType) {
            inferWithPriority(
              inferredType,
              inference.typeParameter,
              getObjectFlags(source) & 262144 /* NonInferrableType */ ? 16 /* PartialHomomorphicMappedType */ : 8 /* HomomorphicMappedType */
            );
          }
        }
        return true;
      }
      if (constraintType.flags & 262144 /* TypeParameter */) {
        inferWithPriority(getIndexType(
          source,
          /*indexFlags*/
          !!source.pattern ? 2 /* NoIndexSignatures */ : 0 /* None */
        ), constraintType, 32 /* MappedTypeConstraint */);
        const extendedConstraint ***REMOVED*** getConstraintOfType(constraintType);
        if (extendedConstraint && inferToMappedType(source, target, extendedConstraint)) {
          return true;
        }
        const propTypes ***REMOVED*** map(getPropertiesOfType(source), getTypeOfSymbol);
        const indexTypes ***REMOVED*** map(getIndexInfosOfType(source), (info) ***REMOVED***> info !***REMOVED******REMOVED*** enumNumberIndexInfo ? info.type : neverType);
        inferFromTypes(getUnionType(concatenate(propTypes, indexTypes)), getTemplateTypeFromMappedType(target));
        return true;
      }
      return false;
    }
    function inferToConditionalType(source, target) {
      if (source.flags & 16777216 /* Conditional */) {
        inferFromTypes(source.checkType, target.checkType);
        inferFromTypes(source.extendsType, target.extendsType);
        inferFromTypes(getTrueTypeFromConditionalType(source), getTrueTypeFromConditionalType(target));
        inferFromTypes(getFalseTypeFromConditionalType(source), getFalseTypeFromConditionalType(target));
      } else {
        const targetTypes ***REMOVED*** [getTrueTypeFromConditionalType(target), getFalseTypeFromConditionalType(target)];
        inferToMultipleTypesWithPriority(source, targetTypes, target.flags, contravariant ? 64 /* ContravariantConditional */ : 0);
      }
    }
    function inferToTemplateLiteralType(source, target) {
      const matches ***REMOVED*** inferTypesFromTemplateLiteralType(source, target);
      const types ***REMOVED*** target.types;
      if (matches || every(target.texts, (s) ***REMOVED***> s.length ***REMOVED******REMOVED******REMOVED*** 0)) {
        for (let i ***REMOVED*** 0; i < types.length; i++) {
          const source2 ***REMOVED*** matches ? matches[i] : neverType;
          const target2 ***REMOVED*** types[i];
          if (source2.flags & 128 /* StringLiteral */ && target2.flags & 8650752 /* TypeVariable */) {
            const inferenceContext ***REMOVED*** getInferenceInfoForType(target2);
            const constraint ***REMOVED*** inferenceContext ? getBaseConstraintOfType(inferenceContext.typeParameter) : void 0;
            if (constraint && !isTypeAny(constraint)) {
              const constraintTypes ***REMOVED*** constraint.flags & 1048576 /* Union */ ? constraint.types : [constraint];
              let allTypeFlags ***REMOVED*** reduceLeft(constraintTypes, (flags, t) ***REMOVED***> flags | t.flags, 0);
              if (!(allTypeFlags & 4 /* String */)) {
                const str ***REMOVED*** source2.value;
                if (allTypeFlags & 296 /* NumberLike */ && !isValidNumberString(
                  str,
                  /*roundTripOnly*/
                  true
                )) {
                  allTypeFlags &***REMOVED*** ~296 /* NumberLike */;
                }
                if (allTypeFlags & 2112 /* BigIntLike */ && !isValidBigIntString(
                  str,
                  /*roundTripOnly*/
                  true
                )) {
                  allTypeFlags &***REMOVED*** ~2112 /* BigIntLike */;
                }
                const matchingType ***REMOVED*** reduceLeft(constraintTypes, (left, right) ***REMOVED***> !(right.flags & allTypeFlags) ? left : left.flags & 4 /* String */ ? left : right.flags & 4 /* String */ ? source2 : left.flags & 134217728 /* TemplateLiteral */ ? left : right.flags & 134217728 /* TemplateLiteral */ && isTypeMatchedByTemplateLiteralType(source2, right) ? source2 : left.flags & 268435456 /* StringMapping */ ? left : right.flags & 268435456 /* StringMapping */ && str ***REMOVED******REMOVED******REMOVED*** applyStringMapping(right.symbol, str) ? source2 : left.flags & 128 /* StringLiteral */ ? left : right.flags & 128 /* StringLiteral */ && right.value ***REMOVED******REMOVED******REMOVED*** str ? right : left.flags & 8 /* Number */ ? left : right.flags & 8 /* Number */ ? getNumberLiteralType(+str) : left.flags & 32 /* Enum */ ? left : right.flags & 32 /* Enum */ ? getNumberLiteralType(+str) : left.flags & 256 /* NumberLiteral */ ? left : right.flags & 256 /* NumberLiteral */ && right.value ***REMOVED******REMOVED******REMOVED*** +str ? right : left.flags & 64 /* BigInt */ ? left : right.flags & 64 /* BigInt */ ? parseBigIntLiteralType(str) : left.flags & 2048 /* BigIntLiteral */ ? left : right.flags & 2048 /* BigIntLiteral */ && pseudoBigIntToString(right.value) ***REMOVED******REMOVED******REMOVED*** str ? right : left.flags & 16 /* Boolean */ ? left : right.flags & 16 /* Boolean */ ? str ***REMOVED******REMOVED******REMOVED*** "true" ? trueType : str ***REMOVED******REMOVED******REMOVED*** "false" ? falseType : booleanType : left.flags & 512 /* BooleanLiteral */ ? left : right.flags & 512 /* BooleanLiteral */ && right.intrinsicName ***REMOVED******REMOVED******REMOVED*** str ? right : left.flags & 32768 /* Undefined */ ? left : right.flags & 32768 /* Undefined */ && right.intrinsicName ***REMOVED******REMOVED******REMOVED*** str ? right : left.flags & 65536 /* Null */ ? left : right.flags & 65536 /* Null */ && right.intrinsicName ***REMOVED******REMOVED******REMOVED*** str ? right : left, neverType);
                if (!(matchingType.flags & 131072 /* Never */)) {
                  inferFromTypes(matchingType, target2);
                  continue;
                }
              }
            }
          }
          inferFromTypes(source2, target2);
        }
      }
    }
    function inferFromGenericMappedTypes(source, target) {
      inferFromTypes(getConstraintTypeFromMappedType(source), getConstraintTypeFromMappedType(target));
      inferFromTypes(getTemplateTypeFromMappedType(source), getTemplateTypeFromMappedType(target));
      const sourceNameType ***REMOVED*** getNameTypeFromMappedType(source);
      const targetNameType ***REMOVED*** getNameTypeFromMappedType(target);
      if (sourceNameType && targetNameType) inferFromTypes(sourceNameType, targetNameType);
    }
    function inferFromObjectTypes(source, target) {
      var _a, _b;
      if (getObjectFlags(source) & 4 /* Reference */ && getObjectFlags(target) & 4 /* Reference */ && (source.target ***REMOVED******REMOVED******REMOVED*** target.target || isArrayType(source) && isArrayType(target))) {
        inferFromTypeArguments(getTypeArguments(source), getTypeArguments(target), getVariances(source.target));
        return;
      }
      if (isGenericMappedType(source) && isGenericMappedType(target)) {
        inferFromGenericMappedTypes(source, target);
      }
      if (getObjectFlags(target) & 32 /* Mapped */ && !target.declaration.nameType) {
        const constraintType ***REMOVED*** getConstraintTypeFromMappedType(target);
        if (inferToMappedType(source, target, constraintType)) {
          return;
        }
      }
      if (!typesDefinitelyUnrelated(source, target)) {
        if (isArrayOrTupleType(source)) {
          if (isTupleType(target)) {
            const sourceArity ***REMOVED*** getTypeReferenceArity(source);
            const targetArity ***REMOVED*** getTypeReferenceArity(target);
            const elementTypes ***REMOVED*** getTypeArguments(target);
            const elementFlags ***REMOVED*** target.target.elementFlags;
            if (isTupleType(source) && isTupleTypeStructureMatching(source, target)) {
              for (let i ***REMOVED*** 0; i < targetArity; i++) {
                inferFromTypes(getTypeArguments(source)[i], elementTypes[i]);
              }
              return;
            }
            const startLength ***REMOVED*** isTupleType(source) ? Math.min(source.target.fixedLength, target.target.fixedLength) : 0;
            const endLength ***REMOVED*** Math.min(isTupleType(source) ? getEndElementCount(source.target, 3 /* Fixed */) : 0, target.target.combinedFlags & 12 /* Variable */ ? getEndElementCount(target.target, 3 /* Fixed */) : 0);
            for (let i ***REMOVED*** 0; i < startLength; i++) {
              inferFromTypes(getTypeArguments(source)[i], elementTypes[i]);
            }
            if (!isTupleType(source) || sourceArity - startLength - endLength ***REMOVED******REMOVED******REMOVED*** 1 && source.target.elementFlags[startLength] & 4 /* Rest */) {
              const restType ***REMOVED*** getTypeArguments(source)[startLength];
              for (let i ***REMOVED*** startLength; i < targetArity - endLength; i++) {
                inferFromTypes(elementFlags[i] & 8 /* Variadic */ ? createArrayType(restType) : restType, elementTypes[i]);
              }
            } else {
              const middleLength ***REMOVED*** targetArity - startLength - endLength;
              if (middleLength ***REMOVED******REMOVED******REMOVED*** 2) {
                if (elementFlags[startLength] & elementFlags[startLength + 1] & 8 /* Variadic */) {
                  const targetInfo ***REMOVED*** getInferenceInfoForType(elementTypes[startLength]);
                  if (targetInfo && targetInfo.impliedArity !***REMOVED******REMOVED*** void 0) {
                    inferFromTypes(sliceTupleType(source, startLength, endLength + sourceArity - targetInfo.impliedArity), elementTypes[startLength]);
                    inferFromTypes(sliceTupleType(source, startLength + targetInfo.impliedArity, endLength), elementTypes[startLength + 1]);
                  }
                } else if (elementFlags[startLength] & 8 /* Variadic */ && elementFlags[startLength + 1] & 4 /* Rest */) {
                  const param ***REMOVED*** (_a ***REMOVED*** getInferenceInfoForType(elementTypes[startLength])) ***REMOVED******REMOVED*** null ? void 0 : _a.typeParameter;
                  const constraint ***REMOVED*** param && getBaseConstraintOfType(param);
                  if (constraint && isTupleType(constraint) && !(constraint.target.combinedFlags & 12 /* Variable */)) {
                    const impliedArity ***REMOVED*** constraint.target.fixedLength;
                    inferFromTypes(sliceTupleType(source, startLength, sourceArity - (startLength + impliedArity)), elementTypes[startLength]);
                    inferFromTypes(getElementTypeOfSliceOfTupleType(source, startLength + impliedArity, endLength), elementTypes[startLength + 1]);
                  }
                } else if (elementFlags[startLength] & 4 /* Rest */ && elementFlags[startLength + 1] & 8 /* Variadic */) {
                  const param ***REMOVED*** (_b ***REMOVED*** getInferenceInfoForType(elementTypes[startLength + 1])) ***REMOVED******REMOVED*** null ? void 0 : _b.typeParameter;
                  const constraint ***REMOVED*** param && getBaseConstraintOfType(param);
                  if (constraint && isTupleType(constraint) && !(constraint.target.combinedFlags & 12 /* Variable */)) {
                    const impliedArity ***REMOVED*** constraint.target.fixedLength;
                    const endIndex ***REMOVED*** sourceArity - getEndElementCount(target.target, 3 /* Fixed */);
                    const startIndex ***REMOVED*** endIndex - impliedArity;
                    const trailingSlice ***REMOVED*** createTupleType(
                      getTypeArguments(source).slice(startIndex, endIndex),
                      source.target.elementFlags.slice(startIndex, endIndex),
                      /*readonly*/
                      false,
                      source.target.labeledElementDeclarations && source.target.labeledElementDeclarations.slice(startIndex, endIndex)
                    );
                    inferFromTypes(getElementTypeOfSliceOfTupleType(source, startLength, endLength + impliedArity), elementTypes[startLength]);
                    inferFromTypes(trailingSlice, elementTypes[startLength + 1]);
                  }
                }
              } else if (middleLength ***REMOVED******REMOVED******REMOVED*** 1 && elementFlags[startLength] & 8 /* Variadic */) {
                const endsInOptional ***REMOVED*** target.target.elementFlags[targetArity - 1] & 2 /* Optional */;
                const sourceSlice ***REMOVED*** sliceTupleType(source, startLength, endLength);
                inferWithPriority(sourceSlice, elementTypes[startLength], endsInOptional ? 2 /* SpeculativeTuple */ : 0);
              } else if (middleLength ***REMOVED******REMOVED******REMOVED*** 1 && elementFlags[startLength] & 4 /* Rest */) {
                const restType ***REMOVED*** getElementTypeOfSliceOfTupleType(source, startLength, endLength);
                if (restType) {
                  inferFromTypes(restType, elementTypes[startLength]);
                }
              }
            }
            for (let i ***REMOVED*** 0; i < endLength; i++) {
              inferFromTypes(getTypeArguments(source)[sourceArity - i - 1], elementTypes[targetArity - i - 1]);
            }
            return;
          }
          if (isArrayType(target)) {
            inferFromIndexTypes(source, target);
            return;
          }
        }
        inferFromProperties(source, target);
        inferFromSignatures(source, target, 0 /* Call */);
        inferFromSignatures(source, target, 1 /* Construct */);
        inferFromIndexTypes(source, target);
      }
    }
    function inferFromProperties(source, target) {
      const properties ***REMOVED*** getPropertiesOfObjectType(target);
      for (const targetProp of properties) {
        const sourceProp ***REMOVED*** getPropertyOfType(source, targetProp.escapedName);
        if (sourceProp && !some(sourceProp.declarations, hasSkipDirectInferenceFlag)) {
          inferFromTypes(
            removeMissingType(getTypeOfSymbol(sourceProp), !!(sourceProp.flags & 16777216 /* Optional */)),
            removeMissingType(getTypeOfSymbol(targetProp), !!(targetProp.flags & 16777216 /* Optional */))
          );
        }
      }
    }
    function inferFromSignatures(source, target, kind) {
      const sourceSignatures ***REMOVED*** getSignaturesOfType(source, kind);
      const sourceLen ***REMOVED*** sourceSignatures.length;
      if (sourceLen > 0) {
        const targetSignatures ***REMOVED*** getSignaturesOfType(target, kind);
        const targetLen ***REMOVED*** targetSignatures.length;
        for (let i ***REMOVED*** 0; i < targetLen; i++) {
          const sourceIndex ***REMOVED*** Math.max(sourceLen - targetLen + i, 0);
          inferFromSignature(getBaseSignature(sourceSignatures[sourceIndex]), getErasedSignature(targetSignatures[i]));
        }
      }
    }
    function inferFromSignature(source, target) {
      if (!(source.flags & 64 /* IsNonInferrable */)) {
        const saveBivariant ***REMOVED*** bivariant;
        const kind ***REMOVED*** target.declaration ? target.declaration.kind : 0 /* Unknown */;
        bivariant ***REMOVED*** bivariant || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */;
        applyToParameterTypes(source, target, inferFromContravariantTypesIfStrictFunctionTypes);
        bivariant ***REMOVED*** saveBivariant;
      }
      applyToReturnTypes(source, target, inferFromTypes);
    }
    function inferFromIndexTypes(source, target) {
      const priority2 ***REMOVED*** getObjectFlags(source) & getObjectFlags(target) & 32 /* Mapped */ ? 8 /* HomomorphicMappedType */ : 0;
      const indexInfos ***REMOVED*** getIndexInfosOfType(target);
      if (isObjectTypeWithInferableIndex(source)) {
        for (const targetInfo of indexInfos) {
          const propTypes ***REMOVED*** [];
          for (const prop of getPropertiesOfType(source)) {
            if (isApplicableIndexType(getLiteralTypeFromProperty(prop, 8576 /* StringOrNumberLiteralOrUnique */), targetInfo.keyType)) {
              const propType ***REMOVED*** getTypeOfSymbol(prop);
              propTypes.push(prop.flags & 16777216 /* Optional */ ? removeMissingOrUndefinedType(propType) : propType);
            }
          }
          for (const info of getIndexInfosOfType(source)) {
            if (isApplicableIndexType(info.keyType, targetInfo.keyType)) {
              propTypes.push(info.type);
            }
          }
          if (propTypes.length) {
            inferWithPriority(getUnionType(propTypes), targetInfo.type, priority2);
          }
        }
      }
      for (const targetInfo of indexInfos) {
        const sourceInfo ***REMOVED*** getApplicableIndexInfo(source, targetInfo.keyType);
        if (sourceInfo) {
          inferWithPriority(sourceInfo.type, targetInfo.type, priority2);
        }
      }
    }
  }
  function isTypeOrBaseIdenticalTo(s, t) {
    return t ***REMOVED******REMOVED******REMOVED*** missingType ? s ***REMOVED******REMOVED******REMOVED*** t : isTypeIdenticalTo(s, t) || !!(t.flags & 4 /* String */ && s.flags & 128 /* StringLiteral */ || t.flags & 8 /* Number */ && s.flags & 256 /* NumberLiteral */);
  }
  function isTypeCloselyMatchedBy(s, t) {
    return !!(s.flags & 524288 /* Object */ && t.flags & 524288 /* Object */ && s.symbol && s.symbol ***REMOVED******REMOVED******REMOVED*** t.symbol || s.aliasSymbol && s.aliasTypeArguments && s.aliasSymbol ***REMOVED******REMOVED******REMOVED*** t.aliasSymbol);
  }
  function hasPrimitiveConstraint(type) {
    const constraint ***REMOVED*** getConstraintOfTypeParameter(type);
    return !!constraint && maybeTypeOfKind(constraint.flags & 16777216 /* Conditional */ ? getDefaultConstraintOfConditionalType(constraint) : constraint, 402784252 /* Primitive */ | 4194304 /* Index */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */);
  }
  function isObjectLiteralType(type) {
    return !!(getObjectFlags(type) & 128 /* ObjectLiteral */);
  }
  function isObjectOrArrayLiteralType(type) {
    return !!(getObjectFlags(type) & (128 /* ObjectLiteral */ | 16384 /* ArrayLiteral */));
  }
  function unionObjectAndArrayLiteralCandidates(candidates) {
    if (candidates.length > 1) {
      const objectLiterals ***REMOVED*** filter(candidates, isObjectOrArrayLiteralType);
      if (objectLiterals.length) {
        const literalsType ***REMOVED*** getUnionType(objectLiterals, 2 /* Subtype */);
        return concatenate(filter(candidates, (t) ***REMOVED***> !isObjectOrArrayLiteralType(t)), [literalsType]);
      }
    }
    return candidates;
  }
  function getContravariantInference(inference) {
    return inference.priority & 416 /* PriorityImpliesCombination */ ? getIntersectionType(inference.contraCandidates) : getCommonSubtype(inference.contraCandidates);
  }
  function getCovariantInference(inference, signature) {
    const candidates ***REMOVED*** unionObjectAndArrayLiteralCandidates(inference.candidates);
    const primitiveConstraint ***REMOVED*** hasPrimitiveConstraint(inference.typeParameter) || isConstTypeVariable(inference.typeParameter);
    const widenLiteralTypes ***REMOVED*** !primitiveConstraint && inference.topLevel && (inference.isFixed || !isTypeParameterAtTopLevelInReturnType(signature, inference.typeParameter));
    const baseCandidates ***REMOVED*** primitiveConstraint ? sameMap(candidates, getRegularTypeOfLiteralType) : widenLiteralTypes ? sameMap(candidates, getWidenedLiteralType) : candidates;
    const unwidenedType ***REMOVED*** inference.priority & 416 /* PriorityImpliesCombination */ ? getUnionType(baseCandidates, 2 /* Subtype */) : getCommonSupertype(baseCandidates);
    return getWidenedType(unwidenedType);
  }
  function getInferredType(context, index) {
    const inference ***REMOVED*** context.inferences[index];
    if (!inference.inferredType) {
      let inferredType;
      let fallbackType;
      if (context.signature) {
        const inferredCovariantType ***REMOVED*** inference.candidates ? getCovariantInference(inference, context.signature) : void 0;
        const inferredContravariantType ***REMOVED*** inference.contraCandidates ? getContravariantInference(inference) : void 0;
        if (inferredCovariantType || inferredContravariantType) {
          const preferCovariantType ***REMOVED*** inferredCovariantType && (!inferredContravariantType || !(inferredCovariantType.flags & (131072 /* Never */ | 1 /* Any */)) && some(inference.contraCandidates, (t) ***REMOVED***> isTypeAssignableTo(inferredCovariantType, t)) && every(context.inferences, (other) ***REMOVED***> other !***REMOVED******REMOVED*** inference && getConstraintOfTypeParameter(other.typeParameter) !***REMOVED******REMOVED*** inference.typeParameter || every(other.candidates, (t) ***REMOVED***> isTypeAssignableTo(t, inferredCovariantType))));
          inferredType ***REMOVED*** preferCovariantType ? inferredCovariantType : inferredContravariantType;
          fallbackType ***REMOVED*** preferCovariantType ? inferredContravariantType : inferredCovariantType;
        } else if (context.flags & 1 /* NoDefault */) {
          inferredType ***REMOVED*** silentNeverType;
        } else {
          const defaultType ***REMOVED*** getDefaultFromTypeParameter(inference.typeParameter);
          if (defaultType) {
            inferredType ***REMOVED*** instantiateType(defaultType, mergeTypeMappers(createBackreferenceMapper(context, index), context.nonFixingMapper));
          }
        }
      } else {
        inferredType ***REMOVED*** getTypeFromInference(inference);
      }
      inference.inferredType ***REMOVED*** inferredType || getDefaultTypeArgumentType(!!(context.flags & 2 /* AnyDefault */));
      const constraint ***REMOVED*** getConstraintOfTypeParameter(inference.typeParameter);
      if (constraint) {
        const instantiatedConstraint ***REMOVED*** instantiateType(constraint, context.nonFixingMapper);
        if (!inferredType || !context.compareTypes(inferredType, getTypeWithThisArgument(instantiatedConstraint, inferredType))) {
          inference.inferredType ***REMOVED*** fallbackType && context.compareTypes(fallbackType, getTypeWithThisArgument(instantiatedConstraint, fallbackType)) ? fallbackType : instantiatedConstraint;
        }
      }
      clearActiveMapperCaches();
    }
    return inference.inferredType;
  }
  function getDefaultTypeArgumentType(isInJavaScriptFile) {
    return isInJavaScriptFile ? anyType : unknownType;
  }
  function getInferredTypes(context) {
    const result ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < context.inferences.length; i++) {
      result.push(getInferredType(context, i));
    }
    return result;
  }
  function getCannotFindNameDiagnosticForName(node) {
    switch (node.escapedText) {
      case "document":
      case "console":
        return Diagnostics.Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_include_dom;
      case "$":
        return compilerOptions.types ? Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slashjquery_and_then_add_jquery_to_the_types_field_in_your_tsconfig : Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_jQuery_Try_npm_i_save_dev_types_Slashjquery;
      case "describe":
      case "suite":
      case "it":
      case "test":
        return compilerOptions.types ? Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_types_Slashjest_or_npm_i_save_dev_types_Slashmocha_and_then_add_jest_or_mocha_to_the_types_field_in_your_tsconfig : Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_a_test_runner_Try_npm_i_save_dev_types_Slashjest_or_npm_i_save_dev_types_Slashmocha;
      case "process":
      case "require":
      case "Buffer":
      case "module":
        return compilerOptions.types ? Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashnode_and_then_add_node_to_the_types_field_in_your_tsconfig : Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_node_Try_npm_i_save_dev_types_Slashnode;
      case "Bun":
        return compilerOptions.types ? Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun_and_then_add_bun_to_the_types_field_in_your_tsconfig : Diagnostics.Cannot_find_name_0_Do_you_need_to_install_type_definitions_for_Bun_Try_npm_i_save_dev_types_Slashbun;
      case "Map":
      case "Set":
      case "Promise":
      case "Symbol":
      case "WeakMap":
      case "WeakSet":
      case "Iterator":
      case "AsyncIterator":
      case "SharedArrayBuffer":
      case "Atomics":
      case "AsyncIterable":
      case "AsyncIterableIterator":
      case "AsyncGenerator":
      case "AsyncGeneratorFunction":
      case "BigInt":
      case "Reflect":
      case "BigInt64Array":
      case "BigUint64Array":
        return Diagnostics.Cannot_find_name_0_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_1_or_later;
      case "await":
        if (isCallExpression(node.parent)) {
          return Diagnostics.Cannot_find_name_0_Did_you_mean_to_write_this_in_an_async_function;
        }
      // falls through
      default:
        if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
          return Diagnostics.No_value_exists_in_scope_for_the_shorthand_property_0_Either_declare_one_or_provide_an_initializer;
        } else {
          return Diagnostics.Cannot_find_name_0;
        }
    }
  }
  function getResolvedSymbol(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedSymbol) {
      links.resolvedSymbol ***REMOVED*** !nodeIsMissing(node) && resolveName(
        node,
        node,
        111551 /* Value */ | 1048576 /* ExportValue */,
        getCannotFindNameDiagnosticForName(node),
        !isWriteOnlyAccess(node),
        /*excludeGlobals*/
        false
      ) || unknownSymbol;
    }
    return links.resolvedSymbol;
  }
  function isInAmbientOrTypeNode(node) {
    return !!(node.flags & 33554432 /* Ambient */ || findAncestor(node, (n) ***REMOVED***> isInterfaceDeclaration(n) || isTypeAliasDeclaration(n) || isTypeLiteralNode(n)));
  }
  function getFlowCacheKey(node, declaredType, initialType, flowContainer) {
    switch (node.kind) {
      case 80 /* Identifier */:
        if (!isThisInTypeQuery(node)) {
          const symbol ***REMOVED*** getResolvedSymbol(node);
          return symbol !***REMOVED******REMOVED*** unknownSymbol ? `${flowContainer ? getNodeId(flowContainer) : "-1"}|${getTypeId(declaredType)}|${getTypeId(initialType)}|${getSymbolId(symbol)}` : void 0;
        }
      // falls through
      case 110 /* ThisKeyword */:
        return `0|${flowContainer ? getNodeId(flowContainer) : "-1"}|${getTypeId(declaredType)}|${getTypeId(initialType)}`;
      case 236 /* NonNullExpression */:
      case 218 /* ParenthesizedExpression */:
        return getFlowCacheKey(node.expression, declaredType, initialType, flowContainer);
      case 167 /* QualifiedName */:
        const left ***REMOVED*** getFlowCacheKey(node.left, declaredType, initialType, flowContainer);
        return left && `${left}.${node.right.escapedText}`;
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        const propName ***REMOVED*** getAccessedPropertyName(node);
        if (propName !***REMOVED******REMOVED*** void 0) {
          const key ***REMOVED*** getFlowCacheKey(node.expression, declaredType, initialType, flowContainer);
          return key && `${key}.${propName}`;
        }
        if (isElementAccessExpression(node) && isIdentifier(node.argumentExpression)) {
          const symbol ***REMOVED*** getResolvedSymbol(node.argumentExpression);
          if (isConstantVariable(symbol) || isParameterOrMutableLocalVariable(symbol) && !isSymbolAssigned(symbol)) {
            const key ***REMOVED*** getFlowCacheKey(node.expression, declaredType, initialType, flowContainer);
            return key && `${key}.@${getSymbolId(symbol)}`;
          }
        }
        break;
      case 207 /* ObjectBindingPattern */:
      case 208 /* ArrayBindingPattern */:
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 175 /* MethodDeclaration */:
        return `${getNodeId(node)}#${getTypeId(declaredType)}`;
    }
    return void 0;
  }
  function isMatchingReference(source, target) {
    switch (target.kind) {
      case 218 /* ParenthesizedExpression */:
      case 236 /* NonNullExpression */:
        return isMatchingReference(source, target.expression);
      case 227 /* BinaryExpression */:
        return isAssignmentExpression(target) && isMatchingReference(source, target.left) || isBinaryExpression(target) && target.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ && isMatchingReference(source, target.right);
    }
    switch (source.kind) {
      case 237 /* MetaProperty */:
        return target.kind ***REMOVED******REMOVED******REMOVED*** 237 /* MetaProperty */ && source.keywordToken ***REMOVED******REMOVED******REMOVED*** target.keywordToken && source.name.escapedText ***REMOVED******REMOVED******REMOVED*** target.name.escapedText;
      case 80 /* Identifier */:
      case 81 /* PrivateIdentifier */:
        return isThisInTypeQuery(source) ? target.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ : target.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && getResolvedSymbol(source) ***REMOVED******REMOVED******REMOVED*** getResolvedSymbol(target) || (isVariableDeclaration(target) || isBindingElement(target)) && getExportSymbolOfValueSymbolIfExported(getResolvedSymbol(source)) ***REMOVED******REMOVED******REMOVED*** getSymbolOfDeclaration(target);
      case 110 /* ThisKeyword */:
        return target.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
      case 108 /* SuperKeyword */:
        return target.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */;
      case 236 /* NonNullExpression */:
      case 218 /* ParenthesizedExpression */:
      case 239 /* SatisfiesExpression */:
        return isMatchingReference(source.expression, target);
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        const sourcePropertyName ***REMOVED*** getAccessedPropertyName(source);
        if (sourcePropertyName !***REMOVED******REMOVED*** void 0) {
          const targetPropertyName ***REMOVED*** isAccessExpression(target) ? getAccessedPropertyName(target) : void 0;
          if (targetPropertyName !***REMOVED******REMOVED*** void 0) {
            return targetPropertyName ***REMOVED******REMOVED******REMOVED*** sourcePropertyName && isMatchingReference(source.expression, target.expression);
          }
        }
        if (isElementAccessExpression(source) && isElementAccessExpression(target) && isIdentifier(source.argumentExpression) && isIdentifier(target.argumentExpression)) {
          const symbol ***REMOVED*** getResolvedSymbol(source.argumentExpression);
          if (symbol ***REMOVED******REMOVED******REMOVED*** getResolvedSymbol(target.argumentExpression) && (isConstantVariable(symbol) || isParameterOrMutableLocalVariable(symbol) && !isSymbolAssigned(symbol))) {
            return isMatchingReference(source.expression, target.expression);
          }
        }
        break;
      case 167 /* QualifiedName */:
        return isAccessExpression(target) && source.right.escapedText ***REMOVED******REMOVED******REMOVED*** getAccessedPropertyName(target) && isMatchingReference(source.left, target.expression);
      case 227 /* BinaryExpression */:
        return isBinaryExpression(source) && source.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ && isMatchingReference(source.right, target);
    }
    return false;
  }
  function getAccessedPropertyName(access) {
    if (isPropertyAccessExpression(access)) {
      return access.name.escapedText;
    }
    if (isElementAccessExpression(access)) {
      return tryGetElementAccessExpressionName(access);
    }
    if (isBindingElement(access)) {
      const name ***REMOVED*** getDestructuringPropertyName(access);
      return name ? escapeLeadingUnderscores(name) : void 0;
    }
    if (isParameter(access)) {
      return "" + access.parent.parameters.indexOf(access);
    }
    return void 0;
  }
  function tryGetNameFromType(type) {
    return type.flags & 8192 /* UniqueESSymbol */ ? type.escapedName : type.flags & 384 /* StringOrNumberLiteral */ ? escapeLeadingUnderscores("" + type.value) : void 0;
  }
  function tryGetElementAccessExpressionName(node) {
    return isStringOrNumericLiteralLike(node.argumentExpression) ? escapeLeadingUnderscores(node.argumentExpression.text) : isEntityNameExpression(node.argumentExpression) ? tryGetNameFromEntityNameExpression(node.argumentExpression) : void 0;
  }
  function tryGetNameFromEntityNameExpression(node) {
    const symbol ***REMOVED*** resolveEntityName(
      node,
      111551 /* Value */,
      /*ignoreErrors*/
      true
    );
    if (!symbol || !(isConstantVariable(symbol) || symbol.flags & 8 /* EnumMember */)) return void 0;
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (declaration ***REMOVED******REMOVED******REMOVED*** void 0) return void 0;
    const type ***REMOVED*** tryGetTypeFromEffectiveTypeNode(declaration);
    if (type) {
      const name ***REMOVED*** tryGetNameFromType(type);
      if (name !***REMOVED******REMOVED*** void 0) {
        return name;
      }
    }
    if (hasOnlyExpressionInitializer(declaration) && isBlockScopedNameDeclaredBeforeUse(declaration, node)) {
      const initializer ***REMOVED*** getEffectiveInitializer(declaration);
      if (initializer) {
        const initializerType ***REMOVED*** isBindingPattern(declaration.parent) ? getTypeForBindingElement(declaration) : getTypeOfExpression(initializer);
        return initializerType && tryGetNameFromType(initializerType);
      }
      if (isEnumMember(declaration)) {
        return getTextOfPropertyName(declaration.name);
      }
    }
    return void 0;
  }
  function containsMatchingReference(source, target) {
    while (isAccessExpression(source)) {
      source ***REMOVED*** source.expression;
      if (isMatchingReference(source, target)) {
        return true;
      }
    }
    return false;
  }
  function optionalChainContainsReference(source, target) {
    while (isOptionalChain(source)) {
      source ***REMOVED*** source.expression;
      if (isMatchingReference(source, target)) {
        return true;
      }
    }
    return false;
  }
  function isDiscriminantProperty(type, name) {
    if (type && type.flags & 1048576 /* Union */) {
      const prop ***REMOVED*** getUnionOrIntersectionProperty(type, name);
      if (prop && getCheckFlags(prop) & 2 /* SyntheticProperty */) {
        if (prop.links.isDiscriminantProperty ***REMOVED******REMOVED******REMOVED*** void 0) {
          prop.links.isDiscriminantProperty ***REMOVED*** (prop.links.checkFlags & 192 /* Discriminant */) ***REMOVED******REMOVED******REMOVED*** 192 /* Discriminant */ && !isGenericType(getTypeOfSymbol(prop));
        }
        return !!prop.links.isDiscriminantProperty;
      }
    }
    return false;
  }
  function findDiscriminantProperties(sourceProperties, target) {
    let result;
    for (const sourceProperty of sourceProperties) {
      if (isDiscriminantProperty(target, sourceProperty.escapedName)) {
        if (result) {
          result.push(sourceProperty);
          continue;
        }
        result ***REMOVED*** [sourceProperty];
      }
    }
    return result;
  }
  function mapTypesByKeyProperty(types, name) {
    const map2 ***REMOVED*** /* @__PURE__ */ new Map();
    let count ***REMOVED*** 0;
    for (const type of types) {
      if (type.flags & (524288 /* Object */ | 2097152 /* Intersection */ | 58982400 /* InstantiableNonPrimitive */)) {
        const discriminant ***REMOVED*** getTypeOfPropertyOfType(type, name);
        if (discriminant) {
          if (!isLiteralType(discriminant)) {
            return void 0;
          }
          let duplicate ***REMOVED*** false;
          forEachType(discriminant, (t) ***REMOVED***> {
            const id ***REMOVED*** getTypeId(getRegularTypeOfLiteralType(t));
            const existing ***REMOVED*** map2.get(id);
            if (!existing) {
              map2.set(id, type);
            } else if (existing !***REMOVED******REMOVED*** unknownType) {
              map2.set(id, unknownType);
              duplicate ***REMOVED*** true;
            }
          });
          if (!duplicate) count++;
        }
      }
    }
    return count >***REMOVED*** 10 && count * 2 >***REMOVED*** types.length ? map2 : void 0;
  }
  function getKeyPropertyName(unionType) {
    const types ***REMOVED*** unionType.types;
    if (types.length < 10 || getObjectFlags(unionType) & 32768 /* PrimitiveUnion */ || countWhere(types, (t) ***REMOVED***> !!(t.flags & (524288 /* Object */ | 58982400 /* InstantiableNonPrimitive */))) < 10) {
      return void 0;
    }
    if (unionType.keyPropertyName ***REMOVED******REMOVED******REMOVED*** void 0) {
      const keyPropertyName ***REMOVED*** forEach(types, (t) ***REMOVED***> t.flags & (524288 /* Object */ | 58982400 /* InstantiableNonPrimitive */) ? forEach(getPropertiesOfType(t), (p) ***REMOVED***> isUnitType(getTypeOfSymbol(p)) ? p.escapedName : void 0) : void 0);
      const mapByKeyProperty ***REMOVED*** keyPropertyName && mapTypesByKeyProperty(types, keyPropertyName);
      unionType.keyPropertyName ***REMOVED*** mapByKeyProperty ? keyPropertyName : "";
      unionType.constituentMap ***REMOVED*** mapByKeyProperty;
    }
    return unionType.keyPropertyName.length ? unionType.keyPropertyName : void 0;
  }
  function getConstituentTypeForKeyType(unionType, keyType) {
    var _a;
    const result ***REMOVED*** (_a ***REMOVED*** unionType.constituentMap) ***REMOVED******REMOVED*** null ? void 0 : _a.get(getTypeId(getRegularTypeOfLiteralType(keyType)));
    return result !***REMOVED******REMOVED*** unknownType ? result : void 0;
  }
  function getMatchingUnionConstituentForType(unionType, type) {
    const keyPropertyName ***REMOVED*** getKeyPropertyName(unionType);
    const propType ***REMOVED*** keyPropertyName && getTypeOfPropertyOfType(type, keyPropertyName);
    return propType && getConstituentTypeForKeyType(unionType, propType);
  }
  function getMatchingUnionConstituentForObjectLiteral(unionType, node) {
    const keyPropertyName ***REMOVED*** getKeyPropertyName(unionType);
    const propNode ***REMOVED*** keyPropertyName && find(node.properties, (p) ***REMOVED***> p.symbol && p.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ && p.symbol.escapedName ***REMOVED******REMOVED******REMOVED*** keyPropertyName && isPossiblyDiscriminantValue(p.initializer));
    const propType ***REMOVED*** propNode && getContextFreeTypeOfExpression(propNode.initializer);
    return propType && getConstituentTypeForKeyType(unionType, propType);
  }
  function isOrContainsMatchingReference(source, target) {
    return isMatchingReference(source, target) || containsMatchingReference(source, target);
  }
  function hasMatchingArgument(expression, reference) {
    if (expression.arguments) {
      for (const argument of expression.arguments) {
        if (isOrContainsMatchingReference(reference, argument) || optionalChainContainsReference(argument, reference)) {
          return true;
        }
      }
    }
    if (expression.expression.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && isOrContainsMatchingReference(reference, expression.expression.expression)) {
      return true;
    }
    return false;
  }
  function getFlowNodeId(flow) {
    if (flow.id <***REMOVED*** 0) {
      flow.id ***REMOVED*** nextFlowId;
      nextFlowId++;
    }
    return flow.id;
  }
  function typeMaybeAssignableTo(source, target) {
    if (!(source.flags & 1048576 /* Union */)) {
      return isTypeAssignableTo(source, target);
    }
    for (const t of source.types) {
      if (isTypeAssignableTo(t, target)) {
        return true;
      }
    }
    return false;
  }
  function getAssignmentReducedType(declaredType, assignedType) {
    if (declaredType ***REMOVED******REMOVED******REMOVED*** assignedType) {
      return declaredType;
    }
    if (assignedType.flags & 131072 /* Never */) {
      return assignedType;
    }
    const key ***REMOVED*** `A${getTypeId(declaredType)},${getTypeId(assignedType)}`;
    return getCachedType(key) ?? setCachedType(key, getAssignmentReducedTypeWorker(declaredType, assignedType));
  }
  function getAssignmentReducedTypeWorker(declaredType, assignedType) {
    const filteredType ***REMOVED*** filterType(declaredType, (t) ***REMOVED***> typeMaybeAssignableTo(assignedType, t));
    const reducedType ***REMOVED*** assignedType.flags & 512 /* BooleanLiteral */ && isFreshLiteralType(assignedType) ? mapType(filteredType, getFreshTypeOfLiteralType) : filteredType;
    return isTypeAssignableTo(assignedType, reducedType) ? reducedType : declaredType;
  }
  function isFunctionObjectType(type) {
    if (getObjectFlags(type) & 256 /* EvolvingArray */) {
      return false;
    }
    const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
    return !!(resolved.callSignatures.length || resolved.constructSignatures.length || resolved.members.get("bind") && isTypeSubtypeOf(type, globalFunctionType));
  }
  function getTypeFacts(type, mask) {
    return getTypeFactsWorker(type, mask) & mask;
  }
  function hasTypeFacts(type, mask) {
    return getTypeFacts(type, mask) !***REMOVED******REMOVED*** 0;
  }
  function getTypeFactsWorker(type, callerOnlyNeeds) {
    if (type.flags & (2097152 /* Intersection */ | 465829888 /* Instantiable */)) {
      type ***REMOVED*** getBaseConstraintOfType(type) || unknownType;
    }
    const flags ***REMOVED*** type.flags;
    if (flags & (4 /* String */ | 268435456 /* StringMapping */)) {
      return strictNullChecks ? 16317953 /* StringStrictFacts */ : 16776705 /* StringFacts */;
    }
    if (flags & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */)) {
      const isEmpty ***REMOVED*** flags & 128 /* StringLiteral */ && type.value ***REMOVED******REMOVED******REMOVED*** "";
      return strictNullChecks ? isEmpty ? 12123649 /* EmptyStringStrictFacts */ : 7929345 /* NonEmptyStringStrictFacts */ : isEmpty ? 12582401 /* EmptyStringFacts */ : 16776705 /* NonEmptyStringFacts */;
    }
    if (flags & (8 /* Number */ | 32 /* Enum */)) {
      return strictNullChecks ? 16317698 /* NumberStrictFacts */ : 16776450 /* NumberFacts */;
    }
    if (flags & 256 /* NumberLiteral */) {
      const isZero ***REMOVED*** type.value ***REMOVED******REMOVED******REMOVED*** 0;
      return strictNullChecks ? isZero ? 12123394 /* ZeroNumberStrictFacts */ : 7929090 /* NonZeroNumberStrictFacts */ : isZero ? 12582146 /* ZeroNumberFacts */ : 16776450 /* NonZeroNumberFacts */;
    }
    if (flags & 64 /* BigInt */) {
      return strictNullChecks ? 16317188 /* BigIntStrictFacts */ : 16775940 /* BigIntFacts */;
    }
    if (flags & 2048 /* BigIntLiteral */) {
      const isZero ***REMOVED*** isZeroBigInt(type);
      return strictNullChecks ? isZero ? 12122884 /* ZeroBigIntStrictFacts */ : 7928580 /* NonZeroBigIntStrictFacts */ : isZero ? 12581636 /* ZeroBigIntFacts */ : 16775940 /* NonZeroBigIntFacts */;
    }
    if (flags & 16 /* Boolean */) {
      return strictNullChecks ? 16316168 /* BooleanStrictFacts */ : 16774920 /* BooleanFacts */;
    }
    if (flags & 528 /* BooleanLike */) {
      return strictNullChecks ? type ***REMOVED******REMOVED******REMOVED*** falseType || type ***REMOVED******REMOVED******REMOVED*** regularFalseType ? 12121864 /* FalseStrictFacts */ : 7927560 /* TrueStrictFacts */ : type ***REMOVED******REMOVED******REMOVED*** falseType || type ***REMOVED******REMOVED******REMOVED*** regularFalseType ? 12580616 /* FalseFacts */ : 16774920 /* TrueFacts */;
    }
    if (flags & 524288 /* Object */) {
      const possibleFacts ***REMOVED*** strictNullChecks ? 83427327 /* EmptyObjectStrictFacts */ | 7880640 /* FunctionStrictFacts */ | 7888800 /* ObjectStrictFacts */ : 83886079 /* EmptyObjectFacts */ | 16728e3 /* FunctionFacts */ | 16736160 /* ObjectFacts */;
      if ((callerOnlyNeeds & possibleFacts) ***REMOVED******REMOVED******REMOVED*** 0) {
        return 0;
      }
      return getObjectFlags(type) & 16 /* Anonymous */ && isEmptyObjectType(type) ? strictNullChecks ? 83427327 /* EmptyObjectStrictFacts */ : 83886079 /* EmptyObjectFacts */ : isFunctionObjectType(type) ? strictNullChecks ? 7880640 /* FunctionStrictFacts */ : 16728e3 /* FunctionFacts */ : strictNullChecks ? 7888800 /* ObjectStrictFacts */ : 16736160 /* ObjectFacts */;
    }
    if (flags & 16384 /* Void */) {
      return 9830144 /* VoidFacts */;
    }
    if (flags & 32768 /* Undefined */) {
      return 26607360 /* UndefinedFacts */;
    }
    if (flags & 65536 /* Null */) {
      return 42917664 /* NullFacts */;
    }
    if (flags & 12288 /* ESSymbolLike */) {
      return strictNullChecks ? 7925520 /* SymbolStrictFacts */ : 16772880 /* SymbolFacts */;
    }
    if (flags & 67108864 /* NonPrimitive */) {
      return strictNullChecks ? 7888800 /* ObjectStrictFacts */ : 16736160 /* ObjectFacts */;
    }
    if (flags & 131072 /* Never */) {
      return 0 /* None */;
    }
    if (flags & 1048576 /* Union */) {
      return reduceLeft(type.types, (facts, t) ***REMOVED***> facts | getTypeFactsWorker(t, callerOnlyNeeds), 0 /* None */);
    }
    if (flags & 2097152 /* Intersection */) {
      return getIntersectionTypeFacts(type, callerOnlyNeeds);
    }
    return 83886079 /* UnknownFacts */;
  }
  function getIntersectionTypeFacts(type, callerOnlyNeeds) {
    const ignoreObjects ***REMOVED*** maybeTypeOfKind(type, 402784252 /* Primitive */);
    let oredFacts ***REMOVED*** 0 /* None */;
    let andedFacts ***REMOVED*** 134217727 /* All */;
    for (const t of type.types) {
      if (!(ignoreObjects && t.flags & 524288 /* Object */)) {
        const f ***REMOVED*** getTypeFactsWorker(t, callerOnlyNeeds);
        oredFacts |***REMOVED*** f;
        andedFacts &***REMOVED*** f;
      }
    }
    return oredFacts & 8256 /* OrFactsMask */ | andedFacts & 134209471 /* AndFactsMask */;
  }
  function getTypeWithFacts(type, include) {
    return filterType(type, (t) ***REMOVED***> hasTypeFacts(t, include));
  }
  function getAdjustedTypeWithFacts(type, facts) {
    const reduced ***REMOVED*** recombineUnknownType(getTypeWithFacts(strictNullChecks && type.flags & 2 /* Unknown */ ? unknownUnionType : type, facts));
    if (strictNullChecks) {
      switch (facts) {
        case 524288 /* NEUndefined */:
          return removeNullableByIntersection(reduced, 65536 /* EQUndefined */, 131072 /* EQNull */, 33554432 /* IsNull */, nullType);
        case 1048576 /* NENull */:
          return removeNullableByIntersection(reduced, 131072 /* EQNull */, 65536 /* EQUndefined */, 16777216 /* IsUndefined */, undefinedType);
        case 2097152 /* NEUndefinedOrNull */:
        case 4194304 /* Truthy */:
          return mapType(reduced, (t) ***REMOVED***> hasTypeFacts(t, 262144 /* EQUndefinedOrNull */) ? getGlobalNonNullableTypeInstantiation(t) : t);
      }
    }
    return reduced;
  }
  function removeNullableByIntersection(type, targetFacts, otherFacts, otherIncludesFacts, otherType) {
    const facts ***REMOVED*** getTypeFacts(type, 65536 /* EQUndefined */ | 131072 /* EQNull */ | 16777216 /* IsUndefined */ | 33554432 /* IsNull */);
    if (!(facts & targetFacts)) {
      return type;
    }
    const emptyAndOtherUnion ***REMOVED*** getUnionType([emptyObjectType, otherType]);
    return mapType(type, (t) ***REMOVED***> hasTypeFacts(t, targetFacts) ? getIntersectionType([t, !(facts & otherIncludesFacts) && hasTypeFacts(t, otherFacts) ? emptyAndOtherUnion : emptyObjectType]) : t);
  }
  function recombineUnknownType(type) {
    return type ***REMOVED******REMOVED******REMOVED*** unknownUnionType ? unknownType : type;
  }
  function getTypeWithDefault(type, defaultExpression) {
    return defaultExpression ? getUnionType([getNonUndefinedType(type), getTypeOfExpression(defaultExpression)]) : type;
  }
  function getTypeOfDestructuredProperty(type, name) {
    var _a;
    const nameType ***REMOVED*** getLiteralTypeFromPropertyName(name);
    if (!isTypeUsableAsPropertyName(nameType)) return errorType;
    const text ***REMOVED*** getPropertyNameFromType(nameType);
    return getTypeOfPropertyOfType(type, text) || includeUndefinedInIndexSignature((_a ***REMOVED*** getApplicableIndexInfoForName(type, text)) ***REMOVED******REMOVED*** null ? void 0 : _a.type) || errorType;
  }
  function getTypeOfDestructuredArrayElement(type, index) {
    return everyType(type, isTupleLikeType) && getTupleElementType(type, index) || includeUndefinedInIndexSignature(checkIteratedTypeOrElementType(
      65 /* Destructuring */,
      type,
      undefinedType,
      /*errorNode*/
      void 0
    )) || errorType;
  }
  function includeUndefinedInIndexSignature(type) {
    if (!type) return type;
    return compilerOptions.noUncheckedIndexedAccess ? getUnionType([type, missingType]) : type;
  }
  function getTypeOfDestructuredSpreadExpression(type) {
    return createArrayType(checkIteratedTypeOrElementType(
      65 /* Destructuring */,
      type,
      undefinedType,
      /*errorNode*/
      void 0
    ) || errorType);
  }
  function getAssignedTypeOfBinaryExpression(node) {
    const isDestructuringDefaultAssignment ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ && isDestructuringAssignmentTarget(node.parent) || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ && isDestructuringAssignmentTarget(node.parent.parent);
    return isDestructuringDefaultAssignment ? getTypeWithDefault(getAssignedType(node), node.right) : getTypeOfExpression(node.right);
  }
  function isDestructuringAssignmentTarget(parent) {
    return parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && parent.parent.left ***REMOVED******REMOVED******REMOVED*** parent || parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */ && parent.parent.initializer ***REMOVED******REMOVED******REMOVED*** parent;
  }
  function getAssignedTypeOfArrayLiteralElement(node, element) {
    return getTypeOfDestructuredArrayElement(getAssignedType(node), node.elements.indexOf(element));
  }
  function getAssignedTypeOfSpreadExpression(node) {
    return getTypeOfDestructuredSpreadExpression(getAssignedType(node.parent));
  }
  function getAssignedTypeOfPropertyAssignment(node) {
    return getTypeOfDestructuredProperty(getAssignedType(node.parent), node.name);
  }
  function getAssignedTypeOfShorthandPropertyAssignment(node) {
    return getTypeWithDefault(getAssignedTypeOfPropertyAssignment(node), node.objectAssignmentInitializer);
  }
  function getAssignedType(node) {
    const { parent } ***REMOVED*** node;
    switch (parent.kind) {
      case 250 /* ForInStatement */:
        return stringType;
      case 251 /* ForOfStatement */:
        return checkRightHandSideOfForOf(parent) || errorType;
      case 227 /* BinaryExpression */:
        return getAssignedTypeOfBinaryExpression(parent);
      case 221 /* DeleteExpression */:
        return undefinedType;
      case 210 /* ArrayLiteralExpression */:
        return getAssignedTypeOfArrayLiteralElement(parent, node);
      case 231 /* SpreadElement */:
        return getAssignedTypeOfSpreadExpression(parent);
      case 304 /* PropertyAssignment */:
        return getAssignedTypeOfPropertyAssignment(parent);
      case 305 /* ShorthandPropertyAssignment */:
        return getAssignedTypeOfShorthandPropertyAssignment(parent);
    }
    return errorType;
  }
  function getInitialTypeOfBindingElement(node) {
    const pattern ***REMOVED*** node.parent;
    const parentType ***REMOVED*** getInitialType(pattern.parent);
    const type ***REMOVED*** pattern.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ ? getTypeOfDestructuredProperty(parentType, node.propertyName || node.name) : !node.dotDotDotToken ? getTypeOfDestructuredArrayElement(parentType, pattern.elements.indexOf(node)) : getTypeOfDestructuredSpreadExpression(parentType);
    return getTypeWithDefault(type, node.initializer);
  }
  function getTypeOfInitializer(node) {
    const links ***REMOVED*** getNodeLinks(node);
    return links.resolvedType || getTypeOfExpression(node);
  }
  function getInitialTypeOfVariableDeclaration(node) {
    if (node.initializer) {
      return getTypeOfInitializer(node.initializer);
    }
    if (node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */) {
      return stringType;
    }
    if (node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */) {
      return checkRightHandSideOfForOf(node.parent.parent) || errorType;
    }
    return errorType;
  }
  function getInitialType(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ ? getInitialTypeOfVariableDeclaration(node) : getInitialTypeOfBindingElement(node);
  }
  function isEmptyArrayAssignment(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && node.initializer && isEmptyArrayLiteral2(node.initializer) || node.kind !***REMOVED******REMOVED*** 209 /* BindingElement */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && isEmptyArrayLiteral2(node.parent.right);
  }
  function getReferenceCandidate(node) {
    switch (node.kind) {
      case 218 /* ParenthesizedExpression */:
        return getReferenceCandidate(node.expression);
      case 227 /* BinaryExpression */:
        switch (node.operatorToken.kind) {
          case 64 /* EqualsToken */:
          case 76 /* BarBarEqualsToken */:
          case 77 /* AmpersandAmpersandEqualsToken */:
          case 78 /* QuestionQuestionEqualsToken */:
            return getReferenceCandidate(node.left);
          case 28 /* CommaToken */:
            return getReferenceCandidate(node.right);
        }
    }
    return node;
  }
  function getReferenceRoot(node) {
    const { parent } ***REMOVED*** node;
    return parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && parent.left ***REMOVED******REMOVED******REMOVED*** node || parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */ && parent.right ***REMOVED******REMOVED******REMOVED*** node ? getReferenceRoot(parent) : node;
  }
  function getTypeOfSwitchClause(clause) {
    if (clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */) {
      return getRegularTypeOfLiteralType(getTypeOfExpression(clause.expression));
    }
    return neverType;
  }
  function getSwitchClauseTypes(switchStatement) {
    const links ***REMOVED*** getNodeLinks(switchStatement);
    if (!links.switchTypes) {
      links.switchTypes ***REMOVED*** [];
      for (const clause of switchStatement.caseBlock.clauses) {
        links.switchTypes.push(getTypeOfSwitchClause(clause));
      }
    }
    return links.switchTypes;
  }
  function getSwitchClauseTypeOfWitnesses(switchStatement) {
    if (some(switchStatement.caseBlock.clauses, (clause) ***REMOVED***> clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */ && !isStringLiteralLike(clause.expression))) {
      return void 0;
    }
    const witnesses ***REMOVED*** [];
    for (const clause of switchStatement.caseBlock.clauses) {
      const text ***REMOVED*** clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */ ? clause.expression.text : void 0;
      witnesses.push(text && !contains(witnesses, text) ? text : void 0);
    }
    return witnesses;
  }
  function eachTypeContainedIn(source, types) {
    return source.flags & 1048576 /* Union */ ? !forEach(source.types, (t) ***REMOVED***> !contains(types, t)) : contains(types, source);
  }
  function isTypeSubsetOf(source, target) {
    return !!(source ***REMOVED******REMOVED******REMOVED*** target || source.flags & 131072 /* Never */ || target.flags & 1048576 /* Union */ && isTypeSubsetOfUnion(source, target));
  }
  function isTypeSubsetOfUnion(source, target) {
    if (source.flags & 1048576 /* Union */) {
      for (const t of source.types) {
        if (!containsType(target.types, t)) {
          return false;
        }
      }
      return true;
    }
    if (source.flags & 1056 /* EnumLike */ && getBaseTypeOfEnumLikeType(source) ***REMOVED******REMOVED******REMOVED*** target) {
      return true;
    }
    return containsType(target.types, source);
  }
  function forEachType(type, f) {
    return type.flags & 1048576 /* Union */ ? forEach(type.types, f) : f(type);
  }
  function someType(type, f) {
    return type.flags & 1048576 /* Union */ ? some(type.types, f) : f(type);
  }
  function everyType(type, f) {
    return type.flags & 1048576 /* Union */ ? every(type.types, f) : f(type);
  }
  function everyContainedType(type, f) {
    return type.flags & 3145728 /* UnionOrIntersection */ ? every(type.types, f) : f(type);
  }
  function filterType(type, f) {
    if (type.flags & 1048576 /* Union */) {
      const types ***REMOVED*** type.types;
      const filtered ***REMOVED*** filter(types, f);
      if (filtered ***REMOVED******REMOVED******REMOVED*** types) {
        return type;
      }
      const origin ***REMOVED*** type.origin;
      let newOrigin;
      if (origin && origin.flags & 1048576 /* Union */) {
        const originTypes ***REMOVED*** origin.types;
        const originFiltered ***REMOVED*** filter(originTypes, (t) ***REMOVED***> !!(t.flags & 1048576 /* Union */) || f(t));
        if (originTypes.length - originFiltered.length ***REMOVED******REMOVED******REMOVED*** types.length - filtered.length) {
          if (originFiltered.length ***REMOVED******REMOVED******REMOVED*** 1) {
            return originFiltered[0];
          }
          newOrigin ***REMOVED*** createOriginUnionOrIntersectionType(1048576 /* Union */, originFiltered);
        }
      }
      return getUnionTypeFromSortedList(
        filtered,
        type.objectFlags & (32768 /* PrimitiveUnion */ | 16777216 /* ContainsIntersections */),
        /*aliasSymbol*/
        void 0,
        /*aliasTypeArguments*/
        void 0,
        newOrigin
      );
    }
    return type.flags & 131072 /* Never */ || f(type) ? type : neverType;
  }
  function removeType(type, targetType) {
    return filterType(type, (t) ***REMOVED***> t !***REMOVED******REMOVED*** targetType);
  }
  function countTypes(type) {
    return type.flags & 1048576 /* Union */ ? type.types.length : 1;
  }
  function mapType(type, mapper, noReductions) {
    if (type.flags & 131072 /* Never */) {
      return type;
    }
    if (!(type.flags & 1048576 /* Union */)) {
      return mapper(type);
    }
    const origin ***REMOVED*** type.origin;
    const types ***REMOVED*** origin && origin.flags & 1048576 /* Union */ ? origin.types : type.types;
    let mappedTypes;
    let changed ***REMOVED*** false;
    for (const t of types) {
      const mapped ***REMOVED*** t.flags & 1048576 /* Union */ ? mapType(t, mapper, noReductions) : mapper(t);
      changed || (changed ***REMOVED*** t !***REMOVED******REMOVED*** mapped);
      if (mapped) {
        if (!mappedTypes) {
          mappedTypes ***REMOVED*** [mapped];
        } else {
          mappedTypes.push(mapped);
        }
      }
    }
    return changed ? mappedTypes && getUnionType(mappedTypes, noReductions ? 0 /* None */ : 1 /* Literal */) : type;
  }
  function mapTypeWithAlias(type, mapper, aliasSymbol, aliasTypeArguments) {
    return type.flags & 1048576 /* Union */ && aliasSymbol ? getUnionType(map(type.types, mapper), 1 /* Literal */, aliasSymbol, aliasTypeArguments) : mapType(type, mapper);
  }
  function extractTypesOfKind(type, kind) {
    return filterType(type, (t) ***REMOVED***> (t.flags & kind) !***REMOVED******REMOVED*** 0);
  }
  function replacePrimitivesWithLiterals(typeWithPrimitives, typeWithLiterals) {
    if (maybeTypeOfKind(typeWithPrimitives, 4 /* String */ | 134217728 /* TemplateLiteral */ | 8 /* Number */ | 64 /* BigInt */) && maybeTypeOfKind(typeWithLiterals, 128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */ | 256 /* NumberLiteral */ | 2048 /* BigIntLiteral */)) {
      return mapType(typeWithPrimitives, (t) ***REMOVED***> t.flags & 4 /* String */ ? extractTypesOfKind(typeWithLiterals, 4 /* String */ | 128 /* StringLiteral */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) : isPatternLiteralType(t) && !maybeTypeOfKind(typeWithLiterals, 4 /* String */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) ? extractTypesOfKind(typeWithLiterals, 128 /* StringLiteral */) : t.flags & 8 /* Number */ ? extractTypesOfKind(typeWithLiterals, 8 /* Number */ | 256 /* NumberLiteral */) : t.flags & 64 /* BigInt */ ? extractTypesOfKind(typeWithLiterals, 64 /* BigInt */ | 2048 /* BigIntLiteral */) : t);
    }
    return typeWithPrimitives;
  }
  function isIncomplete(flowType) {
    return flowType.flags ***REMOVED******REMOVED******REMOVED*** 0;
  }
  function getTypeFromFlowType(flowType) {
    return flowType.flags ***REMOVED******REMOVED******REMOVED*** 0 ? flowType.type : flowType;
  }
  function createFlowType(type, incomplete) {
    return incomplete ? { flags: 0, type: type.flags & 131072 /* Never */ ? silentNeverType : type } : type;
  }
  function createEvolvingArrayType(elementType) {
    const result ***REMOVED*** createObjectType(256 /* EvolvingArray */);
    result.elementType ***REMOVED*** elementType;
    return result;
  }
  function getEvolvingArrayType(elementType) {
    return evolvingArrayTypes[elementType.id] || (evolvingArrayTypes[elementType.id] ***REMOVED*** createEvolvingArrayType(elementType));
  }
  function addEvolvingArrayElementType(evolvingArrayType, node) {
    const elementType ***REMOVED*** getRegularTypeOfObjectLiteral(getBaseTypeOfLiteralType(getContextFreeTypeOfExpression(node)));
    return isTypeSubsetOf(elementType, evolvingArrayType.elementType) ? evolvingArrayType : getEvolvingArrayType(getUnionType([evolvingArrayType.elementType, elementType]));
  }
  function createFinalArrayType(elementType) {
    return elementType.flags & 131072 /* Never */ ? autoArrayType : createArrayType(
      elementType.flags & 1048576 /* Union */ ? getUnionType(elementType.types, 2 /* Subtype */) : elementType
    );
  }
  function getFinalArrayType(evolvingArrayType) {
    return evolvingArrayType.finalArrayType || (evolvingArrayType.finalArrayType ***REMOVED*** createFinalArrayType(evolvingArrayType.elementType));
  }
  function finalizeEvolvingArrayType(type) {
    return getObjectFlags(type) & 256 /* EvolvingArray */ ? getFinalArrayType(type) : type;
  }
  function getElementTypeOfEvolvingArrayType(type) {
    return getObjectFlags(type) & 256 /* EvolvingArray */ ? type.elementType : neverType;
  }
  function isEvolvingArrayTypeList(types) {
    let hasEvolvingArrayType ***REMOVED*** false;
    for (const t of types) {
      if (!(t.flags & 131072 /* Never */)) {
        if (!(getObjectFlags(t) & 256 /* EvolvingArray */)) {
          return false;
        }
        hasEvolvingArrayType ***REMOVED*** true;
      }
    }
    return hasEvolvingArrayType;
  }
  function isEvolvingArrayOperationTarget(node) {
    const root ***REMOVED*** getReferenceRoot(node);
    const parent ***REMOVED*** root.parent;
    const isLengthPushOrUnshift ***REMOVED*** isPropertyAccessExpression(parent) && (parent.name.escapedText ***REMOVED******REMOVED******REMOVED*** "length" || parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && isIdentifier(parent.name) && isPushOrUnshiftIdentifier(parent.name));
    const isElementAssignment ***REMOVED*** parent.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && parent.expression ***REMOVED******REMOVED******REMOVED*** root && parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && parent.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && parent.parent.left ***REMOVED******REMOVED******REMOVED*** parent && !isAssignmentTarget(parent.parent) && isTypeAssignableToKind(getTypeOfExpression(parent.argumentExpression), 296 /* NumberLike */);
    return isLengthPushOrUnshift || isElementAssignment;
  }
  function isDeclarationWithExplicitTypeAnnotation(node) {
    return (isVariableDeclaration(node) || isPropertyDeclaration(node) || isPropertySignature(node) || isParameter(node)) && !!(getEffectiveTypeAnnotationNode(node) || isInJSFile(node) && hasInitializer(node) && node.initializer && isFunctionExpressionOrArrowFunction(node.initializer) && getEffectiveReturnTypeNode(node.initializer));
  }
  function getExplicitTypeOfSymbol(symbol, diagnostic) {
    symbol ***REMOVED*** resolveSymbol(symbol);
    if (symbol.flags & (16 /* Function */ | 8192 /* Method */ | 32 /* Class */ | 512 /* ValueModule */)) {
      return getTypeOfSymbol(symbol);
    }
    if (symbol.flags & (3 /* Variable */ | 4 /* Property */)) {
      if (getCheckFlags(symbol) & 262144 /* Mapped */) {
        const origin ***REMOVED*** symbol.links.syntheticOrigin;
        if (origin && getExplicitTypeOfSymbol(origin)) {
          return getTypeOfSymbol(symbol);
        }
      }
      const declaration ***REMOVED*** symbol.valueDeclaration;
      if (declaration) {
        if (isDeclarationWithExplicitTypeAnnotation(declaration)) {
          return getTypeOfSymbol(symbol);
        }
        if (isVariableDeclaration(declaration) && declaration.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */) {
          const statement ***REMOVED*** declaration.parent.parent;
          const expressionType ***REMOVED*** getTypeOfDottedName(
            statement.expression,
            /*diagnostic*/
            void 0
          );
          if (expressionType) {
            const use ***REMOVED*** statement.awaitModifier ? 15 /* ForAwaitOf */ : 13 /* ForOf */;
            return checkIteratedTypeOrElementType(
              use,
              expressionType,
              undefinedType,
              /*errorNode*/
              void 0
            );
          }
        }
        if (diagnostic) {
          addRelatedInfo(diagnostic, createDiagnosticForNode(declaration, Diagnostics._0_needs_an_explicit_type_annotation, symbolToString(symbol)));
        }
      }
    }
  }
  function getTypeOfDottedName(node, diagnostic) {
    if (!(node.flags & 67108864 /* InWithStatement */)) {
      switch (node.kind) {
        case 80 /* Identifier */:
          const symbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(getResolvedSymbol(node));
          return getExplicitTypeOfSymbol(symbol, diagnostic);
        case 110 /* ThisKeyword */:
          return getExplicitThisType(node);
        case 108 /* SuperKeyword */:
          return checkSuperExpression(node);
        case 212 /* PropertyAccessExpression */: {
          const type ***REMOVED*** getTypeOfDottedName(node.expression, diagnostic);
          if (type) {
            const name ***REMOVED*** node.name;
            let prop;
            if (isPrivateIdentifier(name)) {
              if (!type.symbol) {
                return void 0;
              }
              prop ***REMOVED*** getPropertyOfType(type, getSymbolNameForPrivateIdentifier(type.symbol, name.escapedText));
            } else {
              prop ***REMOVED*** getPropertyOfType(type, name.escapedText);
            }
            return prop && getExplicitTypeOfSymbol(prop, diagnostic);
          }
          return void 0;
        }
        case 218 /* ParenthesizedExpression */:
          return getTypeOfDottedName(node.expression, diagnostic);
      }
    }
  }
  function getEffectsSignature(node) {
    const links ***REMOVED*** getNodeLinks(node);
    let signature ***REMOVED*** links.effectsSignature;
    if (signature ***REMOVED******REMOVED******REMOVED*** void 0) {
      let funcType;
      if (isBinaryExpression(node)) {
        const rightType ***REMOVED*** checkNonNullExpression(node.right);
        funcType ***REMOVED*** getSymbolHasInstanceMethodOfObjectType(rightType);
      } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */) {
        funcType ***REMOVED*** getTypeOfDottedName(
          node.expression,
          /*diagnostic*/
          void 0
        );
      } else if (node.expression.kind !***REMOVED******REMOVED*** 108 /* SuperKeyword */) {
        if (isOptionalChain(node)) {
          funcType ***REMOVED*** checkNonNullType(
            getOptionalExpressionType(checkExpression(node.expression), node.expression),
            node.expression
          );
        } else {
          funcType ***REMOVED*** checkNonNullExpression(node.expression);
        }
      }
      const signatures ***REMOVED*** getSignaturesOfType(funcType && getApparentType(funcType) || unknownType, 0 /* Call */);
      const candidate ***REMOVED*** signatures.length ***REMOVED******REMOVED******REMOVED*** 1 && !signatures[0].typeParameters ? signatures[0] : some(signatures, hasTypePredicateOrNeverReturnType) ? getResolvedSignature(node) : void 0;
      signature ***REMOVED*** links.effectsSignature ***REMOVED*** candidate && hasTypePredicateOrNeverReturnType(candidate) ? candidate : unknownSignature;
    }
    return signature ***REMOVED******REMOVED******REMOVED*** unknownSignature ? void 0 : signature;
  }
  function hasTypePredicateOrNeverReturnType(signature) {
    return !!(getTypePredicateOfSignature(signature) || signature.declaration && (getReturnTypeFromAnnotation(signature.declaration) || unknownType).flags & 131072 /* Never */);
  }
  function getTypePredicateArgument(predicate, callExpression) {
    if (predicate.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */ || predicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */) {
      return callExpression.arguments[predicate.parameterIndex];
    }
    const invokedExpression ***REMOVED*** skipParentheses(callExpression.expression);
    return isAccessExpression(invokedExpression) ? skipParentheses(invokedExpression.expression) : void 0;
  }
  function reportFlowControlError(node) {
    const block ***REMOVED*** findAncestor(node, isFunctionOrModuleBlock);
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    const span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, block.statements.pos);
    diagnostics.add(createFileDiagnostic(sourceFile, span.start, span.length, Diagnostics.The_containing_function_or_module_body_is_too_large_for_control_flow_analysis));
  }
  function isReachableFlowNode(flow) {
    const result ***REMOVED*** isReachableFlowNodeWorker(
      flow,
      /*noCacheCheck*/
      false
    );
    lastFlowNode ***REMOVED*** flow;
    lastFlowNodeReachable ***REMOVED*** result;
    return result;
  }
  function isFalseExpression(expr) {
    const node ***REMOVED*** skipParentheses(
      expr,
      /*excludeJSDocTypeAssertions*/
      true
    );
    return node.kind ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */ || node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ && (isFalseExpression(node.left) || isFalseExpression(node.right)) || node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ && isFalseExpression(node.left) && isFalseExpression(node.right));
  }
  function isReachableFlowNodeWorker(flow, noCacheCheck) {
    while (true) {
      if (flow ***REMOVED******REMOVED******REMOVED*** lastFlowNode) {
        return lastFlowNodeReachable;
      }
      const flags ***REMOVED*** flow.flags;
      if (flags & 4096 /* Shared */) {
        if (!noCacheCheck) {
          const id ***REMOVED*** getFlowNodeId(flow);
          const reachable ***REMOVED*** flowNodeReachable[id];
          return reachable !***REMOVED******REMOVED*** void 0 ? reachable : flowNodeReachable[id] ***REMOVED*** isReachableFlowNodeWorker(
            flow,
            /*noCacheCheck*/
            true
          );
        }
        noCacheCheck ***REMOVED*** false;
      }
      if (flags & (16 /* Assignment */ | 96 /* Condition */ | 256 /* ArrayMutation */)) {
        flow ***REMOVED*** flow.antecedent;
      } else if (flags & 512 /* Call */) {
        const signature ***REMOVED*** getEffectsSignature(flow.node);
        if (signature) {
          const predicate ***REMOVED*** getTypePredicateOfSignature(signature);
          if (predicate && predicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */ && !predicate.type) {
            const predicateArgument ***REMOVED*** flow.node.arguments[predicate.parameterIndex];
            if (predicateArgument && isFalseExpression(predicateArgument)) {
              return false;
            }
          }
          if (getReturnTypeOfSignature(signature).flags & 131072 /* Never */) {
            return false;
          }
        }
        flow ***REMOVED*** flow.antecedent;
      } else if (flags & 4 /* BranchLabel */) {
        return some(flow.antecedent, (f) ***REMOVED***> isReachableFlowNodeWorker(
          f,
          /*noCacheCheck*/
          false
        ));
      } else if (flags & 8 /* LoopLabel */) {
        const antecedents ***REMOVED*** flow.antecedent;
        if (antecedents ***REMOVED******REMOVED******REMOVED*** void 0 || antecedents.length ***REMOVED******REMOVED******REMOVED*** 0) {
          return false;
        }
        flow ***REMOVED*** antecedents[0];
      } else if (flags & 128 /* SwitchClause */) {
        const data ***REMOVED*** flow.node;
        if (data.clauseStart ***REMOVED******REMOVED******REMOVED*** data.clauseEnd && isExhaustiveSwitchStatement(data.switchStatement)) {
          return false;
        }
        flow ***REMOVED*** flow.antecedent;
      } else if (flags & 1024 /* ReduceLabel */) {
        lastFlowNode ***REMOVED*** void 0;
        const target ***REMOVED*** flow.node.target;
        const saveAntecedents ***REMOVED*** target.antecedent;
        target.antecedent ***REMOVED*** flow.node.antecedents;
        const result ***REMOVED*** isReachableFlowNodeWorker(
          flow.antecedent,
          /*noCacheCheck*/
          false
        );
        target.antecedent ***REMOVED*** saveAntecedents;
        return result;
      } else {
        return !(flags & 1 /* Unreachable */);
      }
    }
  }
  function isPostSuperFlowNode(flow, noCacheCheck) {
    while (true) {
      const flags ***REMOVED*** flow.flags;
      if (flags & 4096 /* Shared */) {
        if (!noCacheCheck) {
          const id ***REMOVED*** getFlowNodeId(flow);
          const postSuper ***REMOVED*** flowNodePostSuper[id];
          return postSuper !***REMOVED******REMOVED*** void 0 ? postSuper : flowNodePostSuper[id] ***REMOVED*** isPostSuperFlowNode(
            flow,
            /*noCacheCheck*/
            true
          );
        }
        noCacheCheck ***REMOVED*** false;
      }
      if (flags & (16 /* Assignment */ | 96 /* Condition */ | 256 /* ArrayMutation */ | 128 /* SwitchClause */)) {
        flow ***REMOVED*** flow.antecedent;
      } else if (flags & 512 /* Call */) {
        if (flow.node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          return true;
        }
        flow ***REMOVED*** flow.antecedent;
      } else if (flags & 4 /* BranchLabel */) {
        return every(flow.antecedent, (f) ***REMOVED***> isPostSuperFlowNode(
          f,
          /*noCacheCheck*/
          false
        ));
      } else if (flags & 8 /* LoopLabel */) {
        flow ***REMOVED*** flow.antecedent[0];
      } else if (flags & 1024 /* ReduceLabel */) {
        const target ***REMOVED*** flow.node.target;
        const saveAntecedents ***REMOVED*** target.antecedent;
        target.antecedent ***REMOVED*** flow.node.antecedents;
        const result ***REMOVED*** isPostSuperFlowNode(
          flow.antecedent,
          /*noCacheCheck*/
          false
        );
        target.antecedent ***REMOVED*** saveAntecedents;
        return result;
      } else {
        return !!(flags & 1 /* Unreachable */);
      }
    }
  }
  function isConstantReference(node) {
    switch (node.kind) {
      case 110 /* ThisKeyword */:
        return true;
      case 80 /* Identifier */:
        if (!isThisInTypeQuery(node)) {
          const symbol ***REMOVED*** getResolvedSymbol(node);
          return isConstantVariable(symbol) || isParameterOrMutableLocalVariable(symbol) && !isSymbolAssigned(symbol) || !!symbol.valueDeclaration && isFunctionExpression(symbol.valueDeclaration);
        }
        break;
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        return isConstantReference(node.expression) && isReadonlySymbol(getNodeLinks(node).resolvedSymbol || unknownSymbol);
      case 207 /* ObjectBindingPattern */:
      case 208 /* ArrayBindingPattern */:
        const rootDeclaration ***REMOVED*** getRootDeclaration(node.parent);
        return isParameter(rootDeclaration) || isCatchClauseVariableDeclaration(rootDeclaration) ? !isSomeSymbolAssigned(rootDeclaration) : isVariableDeclaration(rootDeclaration) && isVarConstLike2(rootDeclaration);
    }
    return false;
  }
  function getFlowTypeOfReference(reference, declaredType, initialType ***REMOVED*** declaredType, flowContainer, flowNode ***REMOVED*** ((_a) ***REMOVED***> (_a ***REMOVED*** tryCast(reference, canHaveFlowNode)) ***REMOVED******REMOVED*** null ? void 0 : _a.flowNode)()) {
    let key;
    let isKeySet ***REMOVED*** false;
    let flowDepth ***REMOVED*** 0;
    if (flowAnalysisDisabled) {
      return errorType;
    }
    if (!flowNode) {
      return declaredType;
    }
    flowInvocationCount++;
    const sharedFlowStart ***REMOVED*** sharedFlowCount;
    const evolvedType ***REMOVED*** getTypeFromFlowType(getTypeAtFlowNode(flowNode));
    sharedFlowCount ***REMOVED*** sharedFlowStart;
    const resultType ***REMOVED*** getObjectFlags(evolvedType) & 256 /* EvolvingArray */ && isEvolvingArrayOperationTarget(reference) ? autoArrayType : finalizeEvolvingArrayType(evolvedType);
    if (resultType ***REMOVED******REMOVED******REMOVED*** unreachableNeverType || reference.parent && reference.parent.kind ***REMOVED******REMOVED******REMOVED*** 236 /* NonNullExpression */ && !(resultType.flags & 131072 /* Never */) && getTypeWithFacts(resultType, 2097152 /* NEUndefinedOrNull */).flags & 131072 /* Never */) {
      return declaredType;
    }
    return resultType;
    function getOrSetCacheKey() {
      if (isKeySet) {
        return key;
      }
      isKeySet ***REMOVED*** true;
      return key ***REMOVED*** getFlowCacheKey(reference, declaredType, initialType, flowContainer);
    }
    function getTypeAtFlowNode(flow) {
      var _a2;
      if (flowDepth ***REMOVED******REMOVED******REMOVED*** 2e3) {
        (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.instant(tracing.Phase.CheckTypes, "getTypeAtFlowNode_DepthLimit", { flowId: flow.id });
        flowAnalysisDisabled ***REMOVED*** true;
        reportFlowControlError(reference);
        return errorType;
      }
      flowDepth++;
      let sharedFlow;
      while (true) {
        const flags ***REMOVED*** flow.flags;
        if (flags & 4096 /* Shared */) {
          for (let i ***REMOVED*** sharedFlowStart; i < sharedFlowCount; i++) {
            if (sharedFlowNodes[i] ***REMOVED******REMOVED******REMOVED*** flow) {
              flowDepth--;
              return sharedFlowTypes[i];
            }
          }
          sharedFlow ***REMOVED*** flow;
        }
        let type;
        if (flags & 16 /* Assignment */) {
          type ***REMOVED*** getTypeAtFlowAssignment(flow);
          if (!type) {
            flow ***REMOVED*** flow.antecedent;
            continue;
          }
        } else if (flags & 512 /* Call */) {
          type ***REMOVED*** getTypeAtFlowCall(flow);
          if (!type) {
            flow ***REMOVED*** flow.antecedent;
            continue;
          }
        } else if (flags & 96 /* Condition */) {
          type ***REMOVED*** getTypeAtFlowCondition(flow);
        } else if (flags & 128 /* SwitchClause */) {
          type ***REMOVED*** getTypeAtSwitchClause(flow);
        } else if (flags & 12 /* Label */) {
          if (flow.antecedent.length ***REMOVED******REMOVED******REMOVED*** 1) {
            flow ***REMOVED*** flow.antecedent[0];
            continue;
          }
          type ***REMOVED*** flags & 4 /* BranchLabel */ ? getTypeAtFlowBranchLabel(flow) : getTypeAtFlowLoopLabel(flow);
        } else if (flags & 256 /* ArrayMutation */) {
          type ***REMOVED*** getTypeAtFlowArrayMutation(flow);
          if (!type) {
            flow ***REMOVED*** flow.antecedent;
            continue;
          }
        } else if (flags & 1024 /* ReduceLabel */) {
          const target ***REMOVED*** flow.node.target;
          const saveAntecedents ***REMOVED*** target.antecedent;
          target.antecedent ***REMOVED*** flow.node.antecedents;
          type ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
          target.antecedent ***REMOVED*** saveAntecedents;
        } else if (flags & 2 /* Start */) {
          const container ***REMOVED*** flow.node;
          if (container && container !***REMOVED******REMOVED*** flowContainer && reference.kind !***REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && reference.kind !***REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && !(reference.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && container.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */)) {
            flow ***REMOVED*** container.flowNode;
            continue;
          }
          type ***REMOVED*** initialType;
        } else {
          type ***REMOVED*** convertAutoToAny(declaredType);
        }
        if (sharedFlow) {
          sharedFlowNodes[sharedFlowCount] ***REMOVED*** sharedFlow;
          sharedFlowTypes[sharedFlowCount] ***REMOVED*** type;
          sharedFlowCount++;
        }
        flowDepth--;
        return type;
      }
    }
    function getInitialOrAssignedType(flow) {
      const node ***REMOVED*** flow.node;
      return getNarrowableTypeForReference(
        node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ ? getInitialType(node) : getAssignedType(node),
        reference
      );
    }
    function getTypeAtFlowAssignment(flow) {
      const node ***REMOVED*** flow.node;
      if (isMatchingReference(reference, node)) {
        if (!isReachableFlowNode(flow)) {
          return unreachableNeverType;
        }
        if (getAssignmentTargetKind(node) ***REMOVED******REMOVED******REMOVED*** 2 /* Compound */) {
          const flowType ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
          return createFlowType(getBaseTypeOfLiteralType(getTypeFromFlowType(flowType)), isIncomplete(flowType));
        }
        if (declaredType ***REMOVED******REMOVED******REMOVED*** autoType || declaredType ***REMOVED******REMOVED******REMOVED*** autoArrayType) {
          if (isEmptyArrayAssignment(node)) {
            return getEvolvingArrayType(neverType);
          }
          const assignedType ***REMOVED*** getWidenedLiteralType(getInitialOrAssignedType(flow));
          return isTypeAssignableTo(assignedType, declaredType) ? assignedType : anyArrayType;
        }
        const t ***REMOVED*** isInCompoundLikeAssignment(node) ? getBaseTypeOfLiteralType(declaredType) : declaredType;
        if (t.flags & 1048576 /* Union */) {
          return getAssignmentReducedType(t, getInitialOrAssignedType(flow));
        }
        return t;
      }
      if (containsMatchingReference(reference, node)) {
        if (!isReachableFlowNode(flow)) {
          return unreachableNeverType;
        }
        if (isVariableDeclaration(node) && (isInJSFile(node) || isVarConstLike2(node))) {
          const init ***REMOVED*** getDeclaredExpandoInitializer(node);
          if (init && (init.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || init.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */)) {
            return getTypeAtFlowNode(flow.antecedent);
          }
        }
        return declaredType;
      }
      if (isVariableDeclaration(node) && node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ && (isMatchingReference(reference, node.parent.parent.expression) || optionalChainContainsReference(node.parent.parent.expression, reference))) {
        return getNonNullableTypeIfNeeded(finalizeEvolvingArrayType(getTypeFromFlowType(getTypeAtFlowNode(flow.antecedent))));
      }
      return void 0;
    }
    function narrowTypeByAssertion(type, expr) {
      const node ***REMOVED*** skipParentheses(
        expr,
        /*excludeJSDocTypeAssertions*/
        true
      );
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */) {
        return unreachableNeverType;
      }
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
        if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */) {
          return narrowTypeByAssertion(narrowTypeByAssertion(type, node.left), node.right);
        }
        if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */) {
          return getUnionType([narrowTypeByAssertion(type, node.left), narrowTypeByAssertion(type, node.right)]);
        }
      }
      return narrowType(
        type,
        node,
        /*assumeTrue*/
        true
      );
    }
    function getTypeAtFlowCall(flow) {
      const signature ***REMOVED*** getEffectsSignature(flow.node);
      if (signature) {
        const predicate ***REMOVED*** getTypePredicateOfSignature(signature);
        if (predicate && (predicate.kind ***REMOVED******REMOVED******REMOVED*** 2 /* AssertsThis */ || predicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */)) {
          const flowType ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
          const type ***REMOVED*** finalizeEvolvingArrayType(getTypeFromFlowType(flowType));
          const narrowedType ***REMOVED*** predicate.type ? narrowTypeByTypePredicate(
            type,
            predicate,
            flow.node,
            /*assumeTrue*/
            true
          ) : predicate.kind ***REMOVED******REMOVED******REMOVED*** 3 /* AssertsIdentifier */ && predicate.parameterIndex >***REMOVED*** 0 && predicate.parameterIndex < flow.node.arguments.length ? narrowTypeByAssertion(type, flow.node.arguments[predicate.parameterIndex]) : type;
          return narrowedType ***REMOVED******REMOVED******REMOVED*** type ? flowType : createFlowType(narrowedType, isIncomplete(flowType));
        }
        if (getReturnTypeOfSignature(signature).flags & 131072 /* Never */) {
          return unreachableNeverType;
        }
      }
      return void 0;
    }
    function getTypeAtFlowArrayMutation(flow) {
      if (declaredType ***REMOVED******REMOVED******REMOVED*** autoType || declaredType ***REMOVED******REMOVED******REMOVED*** autoArrayType) {
        const node ***REMOVED*** flow.node;
        const expr ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ ? node.expression.expression : node.left.expression;
        if (isMatchingReference(reference, getReferenceCandidate(expr))) {
          const flowType ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
          const type ***REMOVED*** getTypeFromFlowType(flowType);
          if (getObjectFlags(type) & 256 /* EvolvingArray */) {
            let evolvedType2 ***REMOVED*** type;
            if (node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */) {
              for (const arg of node.arguments) {
                evolvedType2 ***REMOVED*** addEvolvingArrayElementType(evolvedType2, arg);
              }
            } else {
              const indexType ***REMOVED*** getContextFreeTypeOfExpression(node.left.argumentExpression);
              if (isTypeAssignableToKind(indexType, 296 /* NumberLike */)) {
                evolvedType2 ***REMOVED*** addEvolvingArrayElementType(evolvedType2, node.right);
              }
            }
            return evolvedType2 ***REMOVED******REMOVED******REMOVED*** type ? flowType : createFlowType(evolvedType2, isIncomplete(flowType));
          }
          return flowType;
        }
      }
      return void 0;
    }
    function getTypeAtFlowCondition(flow) {
      const flowType ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
      const type ***REMOVED*** getTypeFromFlowType(flowType);
      if (type.flags & 131072 /* Never */) {
        return flowType;
      }
      const assumeTrue ***REMOVED*** (flow.flags & 32 /* TrueCondition */) !***REMOVED******REMOVED*** 0;
      const nonEvolvingType ***REMOVED*** finalizeEvolvingArrayType(type);
      const narrowedType ***REMOVED*** narrowType(nonEvolvingType, flow.node, assumeTrue);
      if (narrowedType ***REMOVED******REMOVED******REMOVED*** nonEvolvingType) {
        return flowType;
      }
      return createFlowType(narrowedType, isIncomplete(flowType));
    }
    function getTypeAtSwitchClause(flow) {
      const expr ***REMOVED*** skipParentheses(flow.node.switchStatement.expression);
      const flowType ***REMOVED*** getTypeAtFlowNode(flow.antecedent);
      let type ***REMOVED*** getTypeFromFlowType(flowType);
      if (isMatchingReference(reference, expr)) {
        type ***REMOVED*** narrowTypeBySwitchOnDiscriminant(type, flow.node);
      } else if (expr.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */ && isMatchingReference(reference, expr.expression)) {
        type ***REMOVED*** narrowTypeBySwitchOnTypeOf(type, flow.node);
      } else if (expr.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */) {
        type ***REMOVED*** narrowTypeBySwitchOnTrue(type, flow.node);
      } else {
        if (strictNullChecks) {
          if (optionalChainContainsReference(expr, reference)) {
            type ***REMOVED*** narrowTypeBySwitchOptionalChainContainment(type, flow.node, (t) ***REMOVED***> !(t.flags & (32768 /* Undefined */ | 131072 /* Never */)));
          } else if (expr.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */ && optionalChainContainsReference(expr.expression, reference)) {
            type ***REMOVED*** narrowTypeBySwitchOptionalChainContainment(type, flow.node, (t) ***REMOVED***> !(t.flags & 131072 /* Never */ || t.flags & 128 /* StringLiteral */ && t.value ***REMOVED******REMOVED******REMOVED*** "undefined"));
          }
        }
        const access ***REMOVED*** getDiscriminantPropertyAccess(expr, type);
        if (access) {
          type ***REMOVED*** narrowTypeBySwitchOnDiscriminantProperty(type, access, flow.node);
        }
      }
      return createFlowType(type, isIncomplete(flowType));
    }
    function getTypeAtFlowBranchLabel(flow) {
      const antecedentTypes ***REMOVED*** [];
      let subtypeReduction ***REMOVED*** false;
      let seenIncomplete ***REMOVED*** false;
      let bypassFlow;
      for (const antecedent of flow.antecedent) {
        if (!bypassFlow && antecedent.flags & 128 /* SwitchClause */ && antecedent.node.clauseStart ***REMOVED******REMOVED******REMOVED*** antecedent.node.clauseEnd) {
          bypassFlow ***REMOVED*** antecedent;
          continue;
        }
        const flowType ***REMOVED*** getTypeAtFlowNode(antecedent);
        const type ***REMOVED*** getTypeFromFlowType(flowType);
        if (type ***REMOVED******REMOVED******REMOVED*** declaredType && declaredType ***REMOVED******REMOVED******REMOVED*** initialType) {
          return type;
        }
        pushIfUnique(antecedentTypes, type);
        if (!isTypeSubsetOf(type, initialType)) {
          subtypeReduction ***REMOVED*** true;
        }
        if (isIncomplete(flowType)) {
          seenIncomplete ***REMOVED*** true;
        }
      }
      if (bypassFlow) {
        const flowType ***REMOVED*** getTypeAtFlowNode(bypassFlow);
        const type ***REMOVED*** getTypeFromFlowType(flowType);
        if (!(type.flags & 131072 /* Never */) && !contains(antecedentTypes, type) && !isExhaustiveSwitchStatement(bypassFlow.node.switchStatement)) {
          if (type ***REMOVED******REMOVED******REMOVED*** declaredType && declaredType ***REMOVED******REMOVED******REMOVED*** initialType) {
            return type;
          }
          antecedentTypes.push(type);
          if (!isTypeSubsetOf(type, initialType)) {
            subtypeReduction ***REMOVED*** true;
          }
          if (isIncomplete(flowType)) {
            seenIncomplete ***REMOVED*** true;
          }
        }
      }
      return createFlowType(getUnionOrEvolvingArrayType(antecedentTypes, subtypeReduction ? 2 /* Subtype */ : 1 /* Literal */), seenIncomplete);
    }
    function getTypeAtFlowLoopLabel(flow) {
      const id ***REMOVED*** getFlowNodeId(flow);
      const cache ***REMOVED*** flowLoopCaches[id] || (flowLoopCaches[id] ***REMOVED*** /* @__PURE__ */ new Map());
      const key2 ***REMOVED*** getOrSetCacheKey();
      if (!key2) {
        return declaredType;
      }
      const cached ***REMOVED*** cache.get(key2);
      if (cached) {
        return cached;
      }
      for (let i ***REMOVED*** flowLoopStart; i < flowLoopCount; i++) {
        if (flowLoopNodes[i] ***REMOVED******REMOVED******REMOVED*** flow && flowLoopKeys[i] ***REMOVED******REMOVED******REMOVED*** key2 && flowLoopTypes[i].length) {
          return createFlowType(
            getUnionOrEvolvingArrayType(flowLoopTypes[i], 1 /* Literal */),
            /*incomplete*/
            true
          );
        }
      }
      const antecedentTypes ***REMOVED*** [];
      let subtypeReduction ***REMOVED*** false;
      let firstAntecedentType;
      for (const antecedent of flow.antecedent) {
        let flowType;
        if (!firstAntecedentType) {
          flowType ***REMOVED*** firstAntecedentType ***REMOVED*** getTypeAtFlowNode(antecedent);
        } else {
          flowLoopNodes[flowLoopCount] ***REMOVED*** flow;
          flowLoopKeys[flowLoopCount] ***REMOVED*** key2;
          flowLoopTypes[flowLoopCount] ***REMOVED*** antecedentTypes;
          flowLoopCount++;
          const saveFlowTypeCache ***REMOVED*** flowTypeCache;
          flowTypeCache ***REMOVED*** void 0;
          flowType ***REMOVED*** getTypeAtFlowNode(antecedent);
          flowTypeCache ***REMOVED*** saveFlowTypeCache;
          flowLoopCount--;
          const cached2 ***REMOVED*** cache.get(key2);
          if (cached2) {
            return cached2;
          }
        }
        const type ***REMOVED*** getTypeFromFlowType(flowType);
        pushIfUnique(antecedentTypes, type);
        if (!isTypeSubsetOf(type, initialType)) {
          subtypeReduction ***REMOVED*** true;
        }
        if (type ***REMOVED******REMOVED******REMOVED*** declaredType) {
          break;
        }
      }
      const result ***REMOVED*** getUnionOrEvolvingArrayType(antecedentTypes, subtypeReduction ? 2 /* Subtype */ : 1 /* Literal */);
      if (isIncomplete(firstAntecedentType)) {
        return createFlowType(
          result,
          /*incomplete*/
          true
        );
      }
      cache.set(key2, result);
      return result;
    }
    function getUnionOrEvolvingArrayType(types, subtypeReduction) {
      if (isEvolvingArrayTypeList(types)) {
        return getEvolvingArrayType(getUnionType(map(types, getElementTypeOfEvolvingArrayType)));
      }
      const result ***REMOVED*** recombineUnknownType(getUnionType(sameMap(types, finalizeEvolvingArrayType), subtypeReduction));
      if (result !***REMOVED******REMOVED*** declaredType && result.flags & declaredType.flags & 1048576 /* Union */ && arrayIsEqualTo(result.types, declaredType.types)) {
        return declaredType;
      }
      return result;
    }
    function getCandidateDiscriminantPropertyAccess(expr) {
      if (isBindingPattern(reference) || isFunctionExpressionOrArrowFunction(reference) || isObjectLiteralMethod(reference)) {
        if (isIdentifier(expr)) {
          const symbol ***REMOVED*** getResolvedSymbol(expr);
          const declaration ***REMOVED*** getExportSymbolOfValueSymbolIfExported(symbol).valueDeclaration;
          if (declaration && (isBindingElement(declaration) || isParameter(declaration)) && reference ***REMOVED******REMOVED******REMOVED*** declaration.parent && !declaration.initializer && !declaration.dotDotDotToken) {
            return declaration;
          }
        }
      } else if (isAccessExpression(expr)) {
        if (isMatchingReference(reference, expr.expression)) {
          return expr;
        }
      } else if (isIdentifier(expr)) {
        const symbol ***REMOVED*** getResolvedSymbol(expr);
        if (isConstantVariable(symbol)) {
          const declaration ***REMOVED*** symbol.valueDeclaration;
          if (isVariableDeclaration(declaration) && !declaration.type && declaration.initializer && isAccessExpression(declaration.initializer) && isMatchingReference(reference, declaration.initializer.expression)) {
            return declaration.initializer;
          }
          if (isBindingElement(declaration) && !declaration.initializer) {
            const parent ***REMOVED*** declaration.parent.parent;
            if (isVariableDeclaration(parent) && !parent.type && parent.initializer && (isIdentifier(parent.initializer) || isAccessExpression(parent.initializer)) && isMatchingReference(reference, parent.initializer)) {
              return declaration;
            }
          }
        }
      }
      return void 0;
    }
    function getDiscriminantPropertyAccess(expr, computedType) {
      if (declaredType.flags & 1048576 /* Union */ || computedType.flags & 1048576 /* Union */) {
        const access ***REMOVED*** getCandidateDiscriminantPropertyAccess(expr);
        if (access) {
          const name ***REMOVED*** getAccessedPropertyName(access);
          if (name) {
            const type ***REMOVED*** declaredType.flags & 1048576 /* Union */ && isTypeSubsetOf(computedType, declaredType) ? declaredType : computedType;
            if (isDiscriminantProperty(type, name)) {
              return access;
            }
          }
        }
      }
      return void 0;
    }
    function narrowTypeByDiscriminant(type, access, narrowType2) {
      const propName ***REMOVED*** getAccessedPropertyName(access);
      if (propName ***REMOVED******REMOVED******REMOVED*** void 0) {
        return type;
      }
      const optionalChain ***REMOVED*** isOptionalChain(access);
      const removeNullable ***REMOVED*** strictNullChecks && (optionalChain || isNonNullAccess(access)) && maybeTypeOfKind(type, 98304 /* Nullable */);
      let propType ***REMOVED*** getTypeOfPropertyOfType(removeNullable ? getTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */) : type, propName);
      if (!propType) {
        return type;
      }
      propType ***REMOVED*** removeNullable && optionalChain ? getOptionalType(propType) : propType;
      const narrowedPropType ***REMOVED*** narrowType2(propType);
      return filterType(type, (t) ***REMOVED***> {
        const discriminantType ***REMOVED*** getTypeOfPropertyOrIndexSignatureOfType(t, propName) || unknownType;
        return !(discriminantType.flags & 131072 /* Never */) && !(narrowedPropType.flags & 131072 /* Never */) && areTypesComparable(narrowedPropType, discriminantType);
      });
    }
    function narrowTypeByDiscriminantProperty(type, access, operator, value, assumeTrue) {
      if ((operator ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */) && type.flags & 1048576 /* Union */) {
        const keyPropertyName ***REMOVED*** getKeyPropertyName(type);
        if (keyPropertyName && keyPropertyName ***REMOVED******REMOVED******REMOVED*** getAccessedPropertyName(access)) {
          const candidate ***REMOVED*** getConstituentTypeForKeyType(type, getTypeOfExpression(value));
          if (candidate) {
            return operator ***REMOVED******REMOVED******REMOVED*** (assumeTrue ? 37 /* EqualsEqualsEqualsToken */ : 38 /* ExclamationEqualsEqualsToken */) ? candidate : isUnitType(getTypeOfPropertyOfType(candidate, keyPropertyName) || unknownType) ? removeType(type, candidate) : type;
          }
        }
      }
      return narrowTypeByDiscriminant(type, access, (t) ***REMOVED***> narrowTypeByEquality(t, operator, value, assumeTrue));
    }
    function narrowTypeBySwitchOnDiscriminantProperty(type, access, data) {
      if (data.clauseStart < data.clauseEnd && type.flags & 1048576 /* Union */ && getKeyPropertyName(type) ***REMOVED******REMOVED******REMOVED*** getAccessedPropertyName(access)) {
        const clauseTypes ***REMOVED*** getSwitchClauseTypes(data.switchStatement).slice(data.clauseStart, data.clauseEnd);
        const candidate ***REMOVED*** getUnionType(map(clauseTypes, (t) ***REMOVED***> getConstituentTypeForKeyType(type, t) || unknownType));
        if (candidate !***REMOVED******REMOVED*** unknownType) {
          return candidate;
        }
      }
      return narrowTypeByDiscriminant(type, access, (t) ***REMOVED***> narrowTypeBySwitchOnDiscriminant(t, data));
    }
    function narrowTypeByTruthiness(type, expr, assumeTrue) {
      if (isMatchingReference(reference, expr)) {
        return getAdjustedTypeWithFacts(type, assumeTrue ? 4194304 /* Truthy */ : 8388608 /* Falsy */);
      }
      if (strictNullChecks && assumeTrue && optionalChainContainsReference(expr, reference)) {
        type ***REMOVED*** getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */);
      }
      const access ***REMOVED*** getDiscriminantPropertyAccess(expr, type);
      if (access) {
        return narrowTypeByDiscriminant(type, access, (t) ***REMOVED***> getTypeWithFacts(t, assumeTrue ? 4194304 /* Truthy */ : 8388608 /* Falsy */));
      }
      return type;
    }
    function isTypePresencePossible(type, propName, assumeTrue) {
      const prop ***REMOVED*** getPropertyOfType(type, propName);
      return prop ? !!(prop.flags & 16777216 /* Optional */ || getCheckFlags(prop) & 48 /* Partial */) || assumeTrue : !!getApplicableIndexInfoForName(type, propName) || !assumeTrue;
    }
    function narrowTypeByInKeyword(type, nameType, assumeTrue) {
      const name ***REMOVED*** getPropertyNameFromType(nameType);
      const isKnownProperty2 ***REMOVED*** someType(type, (t) ***REMOVED***> isTypePresencePossible(
        t,
        name,
        /*assumeTrue*/
        true
      ));
      if (isKnownProperty2) {
        return filterType(type, (t) ***REMOVED***> isTypePresencePossible(t, name, assumeTrue));
      }
      if (assumeTrue) {
        const recordSymbol ***REMOVED*** getGlobalRecordSymbol();
        if (recordSymbol) {
          return getIntersectionType([type, getTypeAliasInstantiation(recordSymbol, [nameType, unknownType])]);
        }
      }
      return type;
    }
    function narrowTypeByBooleanComparison(type, expr, bool, operator, assumeTrue) {
      assumeTrue ***REMOVED*** assumeTrue !***REMOVED******REMOVED*** (bool.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */) !***REMOVED******REMOVED*** (operator !***REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */ && operator !***REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */);
      return narrowType(type, expr, assumeTrue);
    }
    function narrowTypeByBinaryExpression(type, expr, assumeTrue) {
      switch (expr.operatorToken.kind) {
        case 64 /* EqualsToken */:
        case 76 /* BarBarEqualsToken */:
        case 77 /* AmpersandAmpersandEqualsToken */:
        case 78 /* QuestionQuestionEqualsToken */:
          return narrowTypeByTruthiness(narrowType(type, expr.right, assumeTrue), expr.left, assumeTrue);
        case 35 /* EqualsEqualsToken */:
        case 36 /* ExclamationEqualsToken */:
        case 37 /* EqualsEqualsEqualsToken */:
        case 38 /* ExclamationEqualsEqualsToken */:
          const operator ***REMOVED*** expr.operatorToken.kind;
          const left ***REMOVED*** getReferenceCandidate(expr.left);
          const right ***REMOVED*** getReferenceCandidate(expr.right);
          if (left.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */ && isStringLiteralLike(right)) {
            return narrowTypeByTypeof(type, left, operator, right, assumeTrue);
          }
          if (right.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */ && isStringLiteralLike(left)) {
            return narrowTypeByTypeof(type, right, operator, left, assumeTrue);
          }
          if (isMatchingReference(reference, left)) {
            return narrowTypeByEquality(type, operator, right, assumeTrue);
          }
          if (isMatchingReference(reference, right)) {
            return narrowTypeByEquality(type, operator, left, assumeTrue);
          }
          if (strictNullChecks) {
            if (optionalChainContainsReference(left, reference)) {
              type ***REMOVED*** narrowTypeByOptionalChainContainment(type, operator, right, assumeTrue);
            } else if (optionalChainContainsReference(right, reference)) {
              type ***REMOVED*** narrowTypeByOptionalChainContainment(type, operator, left, assumeTrue);
            }
          }
          const leftAccess ***REMOVED*** getDiscriminantPropertyAccess(left, type);
          if (leftAccess) {
            return narrowTypeByDiscriminantProperty(type, leftAccess, operator, right, assumeTrue);
          }
          const rightAccess ***REMOVED*** getDiscriminantPropertyAccess(right, type);
          if (rightAccess) {
            return narrowTypeByDiscriminantProperty(type, rightAccess, operator, left, assumeTrue);
          }
          if (isMatchingConstructorReference(left)) {
            return narrowTypeByConstructor(type, operator, right, assumeTrue);
          }
          if (isMatchingConstructorReference(right)) {
            return narrowTypeByConstructor(type, operator, left, assumeTrue);
          }
          if (isBooleanLiteral(right) && !isAccessExpression(left)) {
            return narrowTypeByBooleanComparison(type, left, right, operator, assumeTrue);
          }
          if (isBooleanLiteral(left) && !isAccessExpression(right)) {
            return narrowTypeByBooleanComparison(type, right, left, operator, assumeTrue);
          }
          break;
        case 104 /* InstanceOfKeyword */:
          return narrowTypeByInstanceof(type, expr, assumeTrue);
        case 103 /* InKeyword */:
          if (isPrivateIdentifier(expr.left)) {
            return narrowTypeByPrivateIdentifierInInExpression(type, expr, assumeTrue);
          }
          const target ***REMOVED*** getReferenceCandidate(expr.right);
          if (containsMissingType(type) && isAccessExpression(reference) && isMatchingReference(reference.expression, target)) {
            const leftType ***REMOVED*** getTypeOfExpression(expr.left);
            if (isTypeUsableAsPropertyName(leftType) && getAccessedPropertyName(reference) ***REMOVED******REMOVED******REMOVED*** getPropertyNameFromType(leftType)) {
              return getTypeWithFacts(type, assumeTrue ? 524288 /* NEUndefined */ : 65536 /* EQUndefined */);
            }
          }
          if (isMatchingReference(reference, target)) {
            const leftType ***REMOVED*** getTypeOfExpression(expr.left);
            if (isTypeUsableAsPropertyName(leftType)) {
              return narrowTypeByInKeyword(type, leftType, assumeTrue);
            }
          }
          break;
        case 28 /* CommaToken */:
          return narrowType(type, expr.right, assumeTrue);
        // Ordinarily we won't see && and || expressions in control flow analysis because the Binder breaks those
        // expressions down to individual conditional control flows. However, we may encounter them when analyzing
        // aliased conditional expressions.
        case 56 /* AmpersandAmpersandToken */:
          return assumeTrue ? narrowType(
            narrowType(
              type,
              expr.left,
              /*assumeTrue*/
              true
            ),
            expr.right,
            /*assumeTrue*/
            true
          ) : getUnionType([narrowType(
            type,
            expr.left,
            /*assumeTrue*/
            false
          ), narrowType(
            type,
            expr.right,
            /*assumeTrue*/
            false
          )]);
        case 57 /* BarBarToken */:
          return assumeTrue ? getUnionType([narrowType(
            type,
            expr.left,
            /*assumeTrue*/
            true
          ), narrowType(
            type,
            expr.right,
            /*assumeTrue*/
            true
          )]) : narrowType(
            narrowType(
              type,
              expr.left,
              /*assumeTrue*/
              false
            ),
            expr.right,
            /*assumeTrue*/
            false
          );
      }
      return type;
    }
    function narrowTypeByPrivateIdentifierInInExpression(type, expr, assumeTrue) {
      const target ***REMOVED*** getReferenceCandidate(expr.right);
      if (!isMatchingReference(reference, target)) {
        return type;
      }
      Debug.assertNode(expr.left, isPrivateIdentifier);
      const symbol ***REMOVED*** getSymbolForPrivateIdentifierExpression(expr.left);
      if (symbol ***REMOVED******REMOVED******REMOVED*** void 0) {
        return type;
      }
      const classSymbol ***REMOVED*** symbol.parent;
      const targetType ***REMOVED*** hasStaticModifier(Debug.checkDefined(symbol.valueDeclaration, "should always have a declaration")) ? getTypeOfSymbol(classSymbol) : getDeclaredTypeOfSymbol(classSymbol);
      return getNarrowedType(
        type,
        targetType,
        assumeTrue,
        /*checkDerived*/
        true
      );
    }
    function narrowTypeByOptionalChainContainment(type, operator, value, assumeTrue) {
      const equalsOperator ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */;
      const nullableFlags ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ ? 98304 /* Nullable */ : 32768 /* Undefined */;
      const valueType ***REMOVED*** getTypeOfExpression(value);
      const removeNullable ***REMOVED*** equalsOperator !***REMOVED******REMOVED*** assumeTrue && everyType(valueType, (t) ***REMOVED***> !!(t.flags & nullableFlags)) || equalsOperator ***REMOVED******REMOVED******REMOVED*** assumeTrue && everyType(valueType, (t) ***REMOVED***> !(t.flags & (3 /* AnyOrUnknown */ | nullableFlags)));
      return removeNullable ? getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */) : type;
    }
    function narrowTypeByEquality(type, operator, value, assumeTrue) {
      if (type.flags & 1 /* Any */) {
        return type;
      }
      if (operator ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */) {
        assumeTrue ***REMOVED*** !assumeTrue;
      }
      const valueType ***REMOVED*** getTypeOfExpression(value);
      const doubleEquals ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */;
      if (valueType.flags & 98304 /* Nullable */) {
        if (!strictNullChecks) {
          return type;
        }
        const facts ***REMOVED*** doubleEquals ? assumeTrue ? 262144 /* EQUndefinedOrNull */ : 2097152 /* NEUndefinedOrNull */ : valueType.flags & 65536 /* Null */ ? assumeTrue ? 131072 /* EQNull */ : 1048576 /* NENull */ : assumeTrue ? 65536 /* EQUndefined */ : 524288 /* NEUndefined */;
        return getAdjustedTypeWithFacts(type, facts);
      }
      if (assumeTrue) {
        if (!doubleEquals && (type.flags & 2 /* Unknown */ || someType(type, isEmptyAnonymousObjectType))) {
          if (valueType.flags & (402784252 /* Primitive */ | 67108864 /* NonPrimitive */) || isEmptyAnonymousObjectType(valueType)) {
            return valueType;
          }
          if (valueType.flags & 524288 /* Object */) {
            return nonPrimitiveType;
          }
        }
        const filteredType ***REMOVED*** filterType(type, (t) ***REMOVED***> areTypesComparable(t, valueType) || doubleEquals && isCoercibleUnderDoubleEquals(t, valueType));
        return replacePrimitivesWithLiterals(filteredType, valueType);
      }
      if (isUnitType(valueType)) {
        return filterType(type, (t) ***REMOVED***> !(isUnitLikeType(t) && areTypesComparable(t, valueType)));
      }
      return type;
    }
    function narrowTypeByTypeof(type, typeOfExpr, operator, literal, assumeTrue) {
      if (operator ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */) {
        assumeTrue ***REMOVED*** !assumeTrue;
      }
      const target ***REMOVED*** getReferenceCandidate(typeOfExpr.expression);
      if (!isMatchingReference(reference, target)) {
        if (strictNullChecks && optionalChainContainsReference(target, reference) && assumeTrue ***REMOVED******REMOVED******REMOVED*** (literal.text !***REMOVED******REMOVED*** "undefined")) {
          type ***REMOVED*** getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */);
        }
        const propertyAccess ***REMOVED*** getDiscriminantPropertyAccess(target, type);
        if (propertyAccess) {
          return narrowTypeByDiscriminant(type, propertyAccess, (t) ***REMOVED***> narrowTypeByLiteralExpression(t, literal, assumeTrue));
        }
        return type;
      }
      return narrowTypeByLiteralExpression(type, literal, assumeTrue);
    }
    function narrowTypeByLiteralExpression(type, literal, assumeTrue) {
      return assumeTrue ? narrowTypeByTypeName(type, literal.text) : getAdjustedTypeWithFacts(type, typeofNEFacts.get(literal.text) || 32768 /* TypeofNEHostObject */);
    }
    function narrowTypeBySwitchOptionalChainContainment(type, { switchStatement, clauseStart, clauseEnd }, clauseCheck) {
      const everyClauseChecks ***REMOVED*** clauseStart !***REMOVED******REMOVED*** clauseEnd && every(getSwitchClauseTypes(switchStatement).slice(clauseStart, clauseEnd), clauseCheck);
      return everyClauseChecks ? getTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */) : type;
    }
    function narrowTypeBySwitchOnDiscriminant(type, { switchStatement, clauseStart, clauseEnd }) {
      const switchTypes ***REMOVED*** getSwitchClauseTypes(switchStatement);
      if (!switchTypes.length) {
        return type;
      }
      const clauseTypes ***REMOVED*** switchTypes.slice(clauseStart, clauseEnd);
      const hasDefaultClause ***REMOVED*** clauseStart ***REMOVED******REMOVED******REMOVED*** clauseEnd || contains(clauseTypes, neverType);
      if (type.flags & 2 /* Unknown */ && !hasDefaultClause) {
        let groundClauseTypes;
        for (let i ***REMOVED*** 0; i < clauseTypes.length; i +***REMOVED*** 1) {
          const t ***REMOVED*** clauseTypes[i];
          if (t.flags & (402784252 /* Primitive */ | 67108864 /* NonPrimitive */)) {
            if (groundClauseTypes !***REMOVED******REMOVED*** void 0) {
              groundClauseTypes.push(t);
            }
          } else if (t.flags & 524288 /* Object */) {
            if (groundClauseTypes ***REMOVED******REMOVED******REMOVED*** void 0) {
              groundClauseTypes ***REMOVED*** clauseTypes.slice(0, i);
            }
            groundClauseTypes.push(nonPrimitiveType);
          } else {
            return type;
          }
        }
        return getUnionType(groundClauseTypes ***REMOVED******REMOVED******REMOVED*** void 0 ? clauseTypes : groundClauseTypes);
      }
      const discriminantType ***REMOVED*** getUnionType(clauseTypes);
      const caseType ***REMOVED*** discriminantType.flags & 131072 /* Never */ ? neverType : replacePrimitivesWithLiterals(filterType(type, (t) ***REMOVED***> areTypesComparable(discriminantType, t)), discriminantType);
      if (!hasDefaultClause) {
        return caseType;
      }
      const defaultType ***REMOVED*** filterType(type, (t) ***REMOVED***> !(isUnitLikeType(t) && contains(switchTypes, t.flags & 32768 /* Undefined */ ? undefinedType : getRegularTypeOfLiteralType(extractUnitType(t)))));
      return caseType.flags & 131072 /* Never */ ? defaultType : getUnionType([caseType, defaultType]);
    }
    function narrowTypeByTypeName(type, typeName) {
      switch (typeName) {
        case "string":
          return narrowTypeByTypeFacts(type, stringType, 1 /* TypeofEQString */);
        case "number":
          return narrowTypeByTypeFacts(type, numberType, 2 /* TypeofEQNumber */);
        case "bigint":
          return narrowTypeByTypeFacts(type, bigintType, 4 /* TypeofEQBigInt */);
        case "boolean":
          return narrowTypeByTypeFacts(type, booleanType, 8 /* TypeofEQBoolean */);
        case "symbol":
          return narrowTypeByTypeFacts(type, esSymbolType, 16 /* TypeofEQSymbol */);
        case "object":
          return type.flags & 1 /* Any */ ? type : getUnionType([narrowTypeByTypeFacts(type, nonPrimitiveType, 32 /* TypeofEQObject */), narrowTypeByTypeFacts(type, nullType, 131072 /* EQNull */)]);
        case "function":
          return type.flags & 1 /* Any */ ? type : narrowTypeByTypeFacts(type, globalFunctionType, 64 /* TypeofEQFunction */);
        case "undefined":
          return narrowTypeByTypeFacts(type, undefinedType, 65536 /* EQUndefined */);
      }
      return narrowTypeByTypeFacts(type, nonPrimitiveType, 128 /* TypeofEQHostObject */);
    }
    function narrowTypeByTypeFacts(type, impliedType, facts) {
      return mapType(type, (t) ***REMOVED***> (
        // We first check if a constituent is a subtype of the implied type. If so, we either keep or eliminate
        // the constituent based on its type facts. We use the strict subtype relation because it treats `object`
        // as a subtype of `{}`, and we need the type facts check because function types are subtypes of `object`,
        // but are classified as "function" according to `typeof`.
        isTypeRelatedTo(t, impliedType, strictSubtypeRelation) ? hasTypeFacts(t, facts) ? t : neverType : (
          // We next check if the consituent is a supertype of the implied type. If so, we substitute the implied
          // type. This handles top types like `unknown` and `{}`, and supertypes like `{ toString(): string }`.
          isTypeSubtypeOf(impliedType, t) ? impliedType : (
            // Neither the constituent nor the implied type is a subtype of the other, however their domains may still
            // overlap. For example, an unconstrained type parameter and type `string`. If the type facts indicate
            // possible overlap, we form an intersection. Otherwise, we eliminate the constituent.
            hasTypeFacts(t, facts) ? getIntersectionType([t, impliedType]) : neverType
          )
        )
      ));
    }
    function narrowTypeBySwitchOnTypeOf(type, { switchStatement, clauseStart, clauseEnd }) {
      const witnesses ***REMOVED*** getSwitchClauseTypeOfWitnesses(switchStatement);
      if (!witnesses) {
        return type;
      }
      const defaultIndex ***REMOVED*** findIndex(switchStatement.caseBlock.clauses, (clause) ***REMOVED***> clause.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */);
      const hasDefaultClause ***REMOVED*** clauseStart ***REMOVED******REMOVED******REMOVED*** clauseEnd || defaultIndex >***REMOVED*** clauseStart && defaultIndex < clauseEnd;
      if (hasDefaultClause) {
        const notEqualFacts ***REMOVED*** getNotEqualFactsFromTypeofSwitch(clauseStart, clauseEnd, witnesses);
        return filterType(type, (t) ***REMOVED***> getTypeFacts(t, notEqualFacts) ***REMOVED******REMOVED******REMOVED*** notEqualFacts);
      }
      const clauseWitnesses ***REMOVED*** witnesses.slice(clauseStart, clauseEnd);
      return getUnionType(map(clauseWitnesses, (text) ***REMOVED***> text ? narrowTypeByTypeName(type, text) : neverType));
    }
    function narrowTypeBySwitchOnTrue(type, { switchStatement, clauseStart, clauseEnd }) {
      const defaultIndex ***REMOVED*** findIndex(switchStatement.caseBlock.clauses, (clause) ***REMOVED***> clause.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */);
      const hasDefaultClause ***REMOVED*** clauseStart ***REMOVED******REMOVED******REMOVED*** clauseEnd || defaultIndex >***REMOVED*** clauseStart && defaultIndex < clauseEnd;
      for (let i ***REMOVED*** 0; i < clauseStart; i++) {
        const clause ***REMOVED*** switchStatement.caseBlock.clauses[i];
        if (clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */) {
          type ***REMOVED*** narrowType(
            type,
            clause.expression,
            /*assumeTrue*/
            false
          );
        }
      }
      if (hasDefaultClause) {
        for (let i ***REMOVED*** clauseEnd; i < switchStatement.caseBlock.clauses.length; i++) {
          const clause ***REMOVED*** switchStatement.caseBlock.clauses[i];
          if (clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */) {
            type ***REMOVED*** narrowType(
              type,
              clause.expression,
              /*assumeTrue*/
              false
            );
          }
        }
        return type;
      }
      const clauses ***REMOVED*** switchStatement.caseBlock.clauses.slice(clauseStart, clauseEnd);
      return getUnionType(map(clauses, (clause) ***REMOVED***> clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */ ? narrowType(
        type,
        clause.expression,
        /*assumeTrue*/
        true
      ) : neverType));
    }
    function isMatchingConstructorReference(expr) {
      return (isPropertyAccessExpression(expr) && idText(expr.name) ***REMOVED******REMOVED******REMOVED*** "constructor" || isElementAccessExpression(expr) && isStringLiteralLike(expr.argumentExpression) && expr.argumentExpression.text ***REMOVED******REMOVED******REMOVED*** "constructor") && isMatchingReference(reference, expr.expression);
    }
    function narrowTypeByConstructor(type, operator, identifier, assumeTrue) {
      if (assumeTrue ? operator !***REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ && operator !***REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */ : operator !***REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ && operator !***REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */) {
        return type;
      }
      const identifierType ***REMOVED*** getTypeOfExpression(identifier);
      if (!isFunctionType(identifierType) && !isConstructorType(identifierType)) {
        return type;
      }
      const prototypeProperty ***REMOVED*** getPropertyOfType(identifierType, "prototype");
      if (!prototypeProperty) {
        return type;
      }
      const prototypeType ***REMOVED*** getTypeOfSymbol(prototypeProperty);
      const candidate ***REMOVED*** !isTypeAny(prototypeType) ? prototypeType : void 0;
      if (!candidate || candidate ***REMOVED******REMOVED******REMOVED*** globalObjectType || candidate ***REMOVED******REMOVED******REMOVED*** globalFunctionType) {
        return type;
      }
      if (isTypeAny(type)) {
        return candidate;
      }
      return filterType(type, (t) ***REMOVED***> isConstructedBy(t, candidate));
      function isConstructedBy(source, target) {
        if (source.flags & 524288 /* Object */ && getObjectFlags(source) & 1 /* Class */ || target.flags & 524288 /* Object */ && getObjectFlags(target) & 1 /* Class */) {
          return source.symbol ***REMOVED******REMOVED******REMOVED*** target.symbol;
        }
        return isTypeSubtypeOf(source, target);
      }
    }
    function narrowTypeByInstanceof(type, expr, assumeTrue) {
      const left ***REMOVED*** getReferenceCandidate(expr.left);
      if (!isMatchingReference(reference, left)) {
        if (assumeTrue && strictNullChecks && optionalChainContainsReference(left, reference)) {
          return getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */);
        }
        return type;
      }
      const right ***REMOVED*** expr.right;
      const rightType ***REMOVED*** getTypeOfExpression(right);
      if (!isTypeDerivedFrom(rightType, globalObjectType)) {
        return type;
      }
      const signature ***REMOVED*** getEffectsSignature(expr);
      const predicate ***REMOVED*** signature && getTypePredicateOfSignature(signature);
      if (predicate && predicate.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */ && predicate.parameterIndex ***REMOVED******REMOVED******REMOVED*** 0) {
        return getNarrowedType(
          type,
          predicate.type,
          assumeTrue,
          /*checkDerived*/
          true
        );
      }
      if (!isTypeDerivedFrom(rightType, globalFunctionType)) {
        return type;
      }
      const instanceType ***REMOVED*** mapType(rightType, getInstanceType);
      if (isTypeAny(type) && (instanceType ***REMOVED******REMOVED******REMOVED*** globalObjectType || instanceType ***REMOVED******REMOVED******REMOVED*** globalFunctionType) || !assumeTrue && !(instanceType.flags & 524288 /* Object */ && !isEmptyAnonymousObjectType(instanceType))) {
        return type;
      }
      return getNarrowedType(
        type,
        instanceType,
        assumeTrue,
        /*checkDerived*/
        true
      );
    }
    function getInstanceType(constructorType) {
      const prototypePropertyType ***REMOVED*** getTypeOfPropertyOfType(constructorType, "prototype");
      if (prototypePropertyType && !isTypeAny(prototypePropertyType)) {
        return prototypePropertyType;
      }
      const constructSignatures ***REMOVED*** getSignaturesOfType(constructorType, 1 /* Construct */);
      if (constructSignatures.length) {
        return getUnionType(map(constructSignatures, (signature) ***REMOVED***> getReturnTypeOfSignature(getErasedSignature(signature))));
      }
      return emptyObjectType;
    }
    function getNarrowedType(type, candidate, assumeTrue, checkDerived) {
      const key2 ***REMOVED*** type.flags & 1048576 /* Union */ ? `N${getTypeId(type)},${getTypeId(candidate)},${(assumeTrue ? 1 : 0) | (checkDerived ? 2 : 0)}` : void 0;
      return getCachedType(key2) ?? setCachedType(key2, getNarrowedTypeWorker(type, candidate, assumeTrue, checkDerived));
    }
    function getNarrowedTypeWorker(type, candidate, assumeTrue, checkDerived) {
      if (!assumeTrue) {
        if (type ***REMOVED******REMOVED******REMOVED*** candidate) {
          return neverType;
        }
        if (checkDerived) {
          return filterType(type, (t) ***REMOVED***> !isTypeDerivedFrom(t, candidate));
        }
        type ***REMOVED*** type.flags & 2 /* Unknown */ ? unknownUnionType : type;
        const trueType2 ***REMOVED*** getNarrowedType(
          type,
          candidate,
          /*assumeTrue*/
          true,
          /*checkDerived*/
          false
        );
        return recombineUnknownType(filterType(type, (t) ***REMOVED***> !isTypeSubsetOf(t, trueType2)));
      }
      if (type.flags & 3 /* AnyOrUnknown */) {
        return candidate;
      }
      if (type ***REMOVED******REMOVED******REMOVED*** candidate) {
        return candidate;
      }
      const isRelated ***REMOVED*** checkDerived ? isTypeDerivedFrom : isTypeSubtypeOf;
      const keyPropertyName ***REMOVED*** type.flags & 1048576 /* Union */ ? getKeyPropertyName(type) : void 0;
      const narrowedType ***REMOVED*** mapType(candidate, (c) ***REMOVED***> {
        const discriminant ***REMOVED*** keyPropertyName && getTypeOfPropertyOfType(c, keyPropertyName);
        const matching ***REMOVED*** discriminant && getConstituentTypeForKeyType(type, discriminant);
        const directlyRelated ***REMOVED*** mapType(
          matching || type,
          checkDerived ? (t) ***REMOVED***> isTypeDerivedFrom(t, c) ? t : isTypeDerivedFrom(c, t) ? c : neverType : (t) ***REMOVED***> isTypeStrictSubtypeOf(t, c) ? t : isTypeStrictSubtypeOf(c, t) ? c : isTypeSubtypeOf(t, c) ? t : isTypeSubtypeOf(c, t) ? c : neverType
        );
        return directlyRelated.flags & 131072 /* Never */ ? mapType(type, (t) ***REMOVED***> maybeTypeOfKind(t, 465829888 /* Instantiable */) && isRelated(c, getBaseConstraintOfType(t) || unknownType) ? getIntersectionType([t, c]) : neverType) : directlyRelated;
      });
      return !(narrowedType.flags & 131072 /* Never */) ? narrowedType : isTypeSubtypeOf(candidate, type) ? candidate : isTypeAssignableTo(type, candidate) ? type : isTypeAssignableTo(candidate, type) ? candidate : getIntersectionType([type, candidate]);
    }
    function narrowTypeByCallExpression(type, callExpression, assumeTrue) {
      if (hasMatchingArgument(callExpression, reference)) {
        const signature ***REMOVED*** assumeTrue || !isCallChain(callExpression) ? getEffectsSignature(callExpression) : void 0;
        const predicate ***REMOVED*** signature && getTypePredicateOfSignature(signature);
        if (predicate && (predicate.kind ***REMOVED******REMOVED******REMOVED*** 0 /* This */ || predicate.kind ***REMOVED******REMOVED******REMOVED*** 1 /* Identifier */)) {
          return narrowTypeByTypePredicate(type, predicate, callExpression, assumeTrue);
        }
      }
      if (containsMissingType(type) && isAccessExpression(reference) && isPropertyAccessExpression(callExpression.expression)) {
        const callAccess ***REMOVED*** callExpression.expression;
        if (isMatchingReference(reference.expression, getReferenceCandidate(callAccess.expression)) && isIdentifier(callAccess.name) && callAccess.name.escapedText ***REMOVED******REMOVED******REMOVED*** "hasOwnProperty" && callExpression.arguments.length ***REMOVED******REMOVED******REMOVED*** 1) {
          const argument ***REMOVED*** callExpression.arguments[0];
          if (isStringLiteralLike(argument) && getAccessedPropertyName(reference) ***REMOVED******REMOVED******REMOVED*** escapeLeadingUnderscores(argument.text)) {
            return getTypeWithFacts(type, assumeTrue ? 524288 /* NEUndefined */ : 65536 /* EQUndefined */);
          }
        }
      }
      return type;
    }
    function narrowTypeByTypePredicate(type, predicate, callExpression, assumeTrue) {
      if (predicate.type && !(isTypeAny(type) && (predicate.type ***REMOVED******REMOVED******REMOVED*** globalObjectType || predicate.type ***REMOVED******REMOVED******REMOVED*** globalFunctionType))) {
        const predicateArgument ***REMOVED*** getTypePredicateArgument(predicate, callExpression);
        if (predicateArgument) {
          if (isMatchingReference(reference, predicateArgument)) {
            return getNarrowedType(
              type,
              predicate.type,
              assumeTrue,
              /*checkDerived*/
              false
            );
          }
          if (strictNullChecks && optionalChainContainsReference(predicateArgument, reference) && (assumeTrue && !hasTypeFacts(predicate.type, 65536 /* EQUndefined */) || !assumeTrue && everyType(predicate.type, isNullableType))) {
            type ***REMOVED*** getAdjustedTypeWithFacts(type, 2097152 /* NEUndefinedOrNull */);
          }
          const access ***REMOVED*** getDiscriminantPropertyAccess(predicateArgument, type);
          if (access) {
            return narrowTypeByDiscriminant(type, access, (t) ***REMOVED***> getNarrowedType(
              t,
              predicate.type,
              assumeTrue,
              /*checkDerived*/
              false
            ));
          }
        }
      }
      return type;
    }
    function narrowType(type, expr, assumeTrue) {
      if (isExpressionOfOptionalChainRoot(expr) || isBinaryExpression(expr.parent) && (expr.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */ || expr.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 78 /* QuestionQuestionEqualsToken */) && expr.parent.left ***REMOVED******REMOVED******REMOVED*** expr) {
        return narrowTypeByOptionality(type, expr, assumeTrue);
      }
      switch (expr.kind) {
        case 80 /* Identifier */:
          if (!isMatchingReference(reference, expr) && inlineLevel < 5) {
            const symbol ***REMOVED*** getResolvedSymbol(expr);
            if (isConstantVariable(symbol)) {
              const declaration ***REMOVED*** symbol.valueDeclaration;
              if (declaration && isVariableDeclaration(declaration) && !declaration.type && declaration.initializer && isConstantReference(reference)) {
                inlineLevel++;
                const result ***REMOVED*** narrowType(type, declaration.initializer, assumeTrue);
                inlineLevel--;
                return result;
              }
            }
          }
        // falls through
        case 110 /* ThisKeyword */:
        case 108 /* SuperKeyword */:
        case 212 /* PropertyAccessExpression */:
        case 213 /* ElementAccessExpression */:
          return narrowTypeByTruthiness(type, expr, assumeTrue);
        case 214 /* CallExpression */:
          return narrowTypeByCallExpression(type, expr, assumeTrue);
        case 218 /* ParenthesizedExpression */:
        case 236 /* NonNullExpression */:
        case 239 /* SatisfiesExpression */:
          return narrowType(type, expr.expression, assumeTrue);
        case 227 /* BinaryExpression */:
          return narrowTypeByBinaryExpression(type, expr, assumeTrue);
        case 225 /* PrefixUnaryExpression */:
          if (expr.operator ***REMOVED******REMOVED******REMOVED*** 54 /* ExclamationToken */) {
            return narrowType(type, expr.operand, !assumeTrue);
          }
          break;
      }
      return type;
    }
    function narrowTypeByOptionality(type, expr, assumePresent) {
      if (isMatchingReference(reference, expr)) {
        return getAdjustedTypeWithFacts(type, assumePresent ? 2097152 /* NEUndefinedOrNull */ : 262144 /* EQUndefinedOrNull */);
      }
      const access ***REMOVED*** getDiscriminantPropertyAccess(expr, type);
      if (access) {
        return narrowTypeByDiscriminant(type, access, (t) ***REMOVED***> getTypeWithFacts(t, assumePresent ? 2097152 /* NEUndefinedOrNull */ : 262144 /* EQUndefinedOrNull */));
      }
      return type;
    }
  }
  function getTypeOfSymbolAtLocation(symbol, location) {
    symbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(symbol);
    if (location.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || location.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
      if (isRightSideOfQualifiedNameOrPropertyAccess(location)) {
        location ***REMOVED*** location.parent;
      }
      if (isExpressionNode(location) && (!isAssignmentTarget(location) || isWriteAccess(location))) {
        const type ***REMOVED*** removeOptionalTypeMarker(
          isWriteAccess(location) && location.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ ? checkPropertyAccessExpression(
            location,
            /*checkMode*/
            void 0,
            /*writeOnly*/
            true
          ) : getTypeOfExpression(location)
        );
        if (getExportSymbolOfValueSymbolIfExported(getNodeLinks(location).resolvedSymbol) ***REMOVED******REMOVED******REMOVED*** symbol) {
          return type;
        }
      }
    }
    if (isDeclarationName(location) && isSetAccessor(location.parent) && getAnnotatedAccessorTypeNode(location.parent)) {
      return getWriteTypeOfAccessors(location.parent.symbol);
    }
    return isRightSideOfAccessExpression(location) && isWriteAccess(location.parent) ? getWriteTypeOfSymbol(symbol) : getNonMissingTypeOfSymbol(symbol);
  }
  function getControlFlowContainer(node) {
    return findAncestor(node.parent, (node2) ***REMOVED***> isFunctionLike(node2) && !getImmediatelyInvokedFunctionExpression(node2) || node2.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node2.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || node2.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */);
  }
  function isSymbolAssignedDefinitely(symbol) {
    if (symbol.lastAssignmentPos !***REMOVED******REMOVED*** void 0) {
      return symbol.lastAssignmentPos < 0;
    }
    return isSymbolAssigned(symbol) && symbol.lastAssignmentPos !***REMOVED******REMOVED*** void 0 && symbol.lastAssignmentPos < 0;
  }
  function isSymbolAssigned(symbol) {
    return !isPastLastAssignment(
      symbol,
      /*location*/
      void 0
    );
  }
  function isPastLastAssignment(symbol, location) {
    const parent ***REMOVED*** findAncestor(symbol.valueDeclaration, isFunctionOrSourceFile);
    if (!parent) {
      return false;
    }
    const links ***REMOVED*** getNodeLinks(parent);
    if (!(links.flags & 131072 /* AssignmentsMarked */)) {
      links.flags |***REMOVED*** 131072 /* AssignmentsMarked */;
      if (!hasParentWithAssignmentsMarked(parent)) {
        markNodeAssignments(parent);
      }
    }
    return !symbol.lastAssignmentPos || location && Math.abs(symbol.lastAssignmentPos) < location.pos;
  }
  function isSomeSymbolAssigned(rootDeclaration) {
    Debug.assert(isVariableDeclaration(rootDeclaration) || isParameter(rootDeclaration));
    return isSomeSymbolAssignedWorker(rootDeclaration.name);
  }
  function isSomeSymbolAssignedWorker(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return isSymbolAssigned(getSymbolOfDeclaration(node.parent));
    }
    return some(node.elements, (e) ***REMOVED***> e.kind !***REMOVED******REMOVED*** 233 /* OmittedExpression */ && isSomeSymbolAssignedWorker(e.name));
  }
  function hasParentWithAssignmentsMarked(node) {
    return !!findAncestor(node.parent, (node2) ***REMOVED***> isFunctionOrSourceFile(node2) && !!(getNodeLinks(node2).flags & 131072 /* AssignmentsMarked */));
  }
  function isFunctionOrSourceFile(node) {
    return isFunctionLikeDeclaration(node) || isSourceFile(node);
  }
  function markNodeAssignments(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        const assigmentTarget ***REMOVED*** getAssignmentTargetKind(node);
        if (assigmentTarget !***REMOVED******REMOVED*** 0 /* None */) {
          const symbol ***REMOVED*** getResolvedSymbol(node);
          const hasDefiniteAssignment ***REMOVED*** assigmentTarget ***REMOVED******REMOVED******REMOVED*** 1 /* Definite */ || symbol.lastAssignmentPos !***REMOVED******REMOVED*** void 0 && symbol.lastAssignmentPos < 0;
          if (isParameterOrMutableLocalVariable(symbol)) {
            if (symbol.lastAssignmentPos ***REMOVED******REMOVED******REMOVED*** void 0 || Math.abs(symbol.lastAssignmentPos) !***REMOVED******REMOVED*** Number.MAX_VALUE) {
              const referencingFunction ***REMOVED*** findAncestor(node, isFunctionOrSourceFile);
              const declaringFunction ***REMOVED*** findAncestor(symbol.valueDeclaration, isFunctionOrSourceFile);
              symbol.lastAssignmentPos ***REMOVED*** referencingFunction ***REMOVED******REMOVED******REMOVED*** declaringFunction ? extendAssignmentPosition(node, symbol.valueDeclaration) : Number.MAX_VALUE;
            }
            if (hasDefiniteAssignment && symbol.lastAssignmentPos > 0) {
              symbol.lastAssignmentPos ****REMOVED*** -1;
            }
          }
        }
        return;
      case 282 /* ExportSpecifier */:
        const exportDeclaration ***REMOVED*** node.parent.parent;
        const name ***REMOVED*** node.propertyName || node.name;
        if (!node.isTypeOnly && !exportDeclaration.isTypeOnly && !exportDeclaration.moduleSpecifier && name.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
          const symbol ***REMOVED*** resolveEntityName(
            name,
            111551 /* Value */,
            /*ignoreErrors*/
            true,
            /*dontResolveAlias*/
            true
          );
          if (symbol && isParameterOrMutableLocalVariable(symbol)) {
            const sign ***REMOVED*** symbol.lastAssignmentPos !***REMOVED******REMOVED*** void 0 && symbol.lastAssignmentPos < 0 ? -1 : 1;
            symbol.lastAssignmentPos ***REMOVED*** sign * Number.MAX_VALUE;
          }
        }
        return;
      case 265 /* InterfaceDeclaration */:
      case 266 /* TypeAliasDeclaration */:
      case 267 /* EnumDeclaration */:
        return;
    }
    if (isTypeNode(node)) {
      return;
    }
    forEachChild(node, markNodeAssignments);
  }
  function extendAssignmentPosition(node, declaration) {
    let pos ***REMOVED*** node.pos;
    while (node && node.pos > declaration.pos) {
      switch (node.kind) {
        case 244 /* VariableStatement */:
        case 245 /* ExpressionStatement */:
        case 246 /* IfStatement */:
        case 247 /* DoStatement */:
        case 248 /* WhileStatement */:
        case 249 /* ForStatement */:
        case 250 /* ForInStatement */:
        case 251 /* ForOfStatement */:
        case 255 /* WithStatement */:
        case 256 /* SwitchStatement */:
        case 259 /* TryStatement */:
        case 264 /* ClassDeclaration */:
          pos ***REMOVED*** node.end;
      }
      node ***REMOVED*** node.parent;
    }
    return pos;
  }
  function isConstantVariable(symbol) {
    return symbol.flags & 3 /* Variable */ && (getDeclarationNodeFlagsFromSymbol(symbol) & 6 /* Constant */) !***REMOVED******REMOVED*** 0;
  }
  function isParameterOrMutableLocalVariable(symbol) {
    const declaration ***REMOVED*** symbol.valueDeclaration && getRootDeclaration(symbol.valueDeclaration);
    return !!declaration && (isParameter(declaration) || isVariableDeclaration(declaration) && (isCatchClause(declaration.parent) || isMutableLocalVariableDeclaration(declaration)));
  }
  function isMutableLocalVariableDeclaration(declaration) {
    return !!(declaration.parent.flags & 1 /* Let */) && !(getCombinedModifierFlags(declaration) & 32 /* Export */ || declaration.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ && isGlobalSourceFile(declaration.parent.parent.parent));
  }
  function parameterInitializerContainsUndefined(declaration) {
    const links ***REMOVED*** getNodeLinks(declaration);
    if (links.parameterInitializerContainsUndefined ***REMOVED******REMOVED******REMOVED*** void 0) {
      if (!pushTypeResolution(declaration, 8 /* ParameterInitializerContainsUndefined */)) {
        reportCircularityError(declaration.symbol);
        return true;
      }
      const containsUndefined ***REMOVED*** !!hasTypeFacts(checkDeclarationInitializer(declaration, 0 /* Normal */), 16777216 /* IsUndefined */);
      if (!popTypeResolution()) {
        reportCircularityError(declaration.symbol);
        return true;
      }
      links.parameterInitializerContainsUndefined ?? (links.parameterInitializerContainsUndefined ***REMOVED*** containsUndefined);
    }
    return links.parameterInitializerContainsUndefined;
  }
  function removeOptionalityFromDeclaredType(declaredType, declaration) {
    const removeUndefined ***REMOVED*** strictNullChecks && declaration.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && declaration.initializer && hasTypeFacts(declaredType, 16777216 /* IsUndefined */) && !parameterInitializerContainsUndefined(declaration);
    return removeUndefined ? getTypeWithFacts(declaredType, 524288 /* NEUndefined */) : declaredType;
  }
  function isConstraintPosition(type, node) {
    const parent ***REMOVED*** node.parent;
    return parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ || parent.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && parent.expression ***REMOVED******REMOVED******REMOVED*** node || parent.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */ && parent.expression ***REMOVED******REMOVED******REMOVED*** node || parent.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && parent.expression ***REMOVED******REMOVED******REMOVED*** node && !(someType(type, isGenericTypeWithoutNullableConstraint) && isGenericIndexType(getTypeOfExpression(parent.argumentExpression)));
  }
  function isGenericTypeWithUnionConstraint(type) {
    return type.flags & 2097152 /* Intersection */ ? some(type.types, isGenericTypeWithUnionConstraint) : !!(type.flags & 465829888 /* Instantiable */ && getBaseConstraintOrType(type).flags & (98304 /* Nullable */ | 1048576 /* Union */));
  }
  function isGenericTypeWithoutNullableConstraint(type) {
    return type.flags & 2097152 /* Intersection */ ? some(type.types, isGenericTypeWithoutNullableConstraint) : !!(type.flags & 465829888 /* Instantiable */ && !maybeTypeOfKind(getBaseConstraintOrType(type), 98304 /* Nullable */));
  }
  function hasContextualTypeWithNoGenericTypes(node, checkMode) {
    const contextualType ***REMOVED*** (isIdentifier(node) || isPropertyAccessExpression(node) || isElementAccessExpression(node)) && !((isJsxOpeningElement(node.parent) || isJsxSelfClosingElement(node.parent)) && node.parent.tagName ***REMOVED******REMOVED******REMOVED*** node) && (checkMode && checkMode & 32 /* RestBindingElement */ ? getContextualType(node, 8 /* SkipBindingPatterns */) : getContextualType(
      node,
      /*contextFlags*/
      void 0
    ));
    return contextualType && !isGenericType(contextualType);
  }
  function getNarrowableTypeForReference(type, reference, checkMode) {
    if (isNoInferType(type)) {
      type ***REMOVED*** type.baseType;
    }
    const substituteConstraints ***REMOVED*** !(checkMode && checkMode & 2 /* Inferential */) && someType(type, isGenericTypeWithUnionConstraint) && (isConstraintPosition(type, reference) || hasContextualTypeWithNoGenericTypes(reference, checkMode));
    return substituteConstraints ? mapType(type, getBaseConstraintOrType) : type;
  }
  function isExportOrExportExpression(location) {
    return !!findAncestor(location, (n) ***REMOVED***> {
      const parent ***REMOVED*** n.parent;
      if (parent ***REMOVED******REMOVED******REMOVED*** void 0) {
        return "quit";
      }
      if (isExportAssignment(parent)) {
        return parent.expression ***REMOVED******REMOVED******REMOVED*** n && isEntityNameExpression(n);
      }
      if (isExportSpecifier(parent)) {
        return parent.name ***REMOVED******REMOVED******REMOVED*** n || parent.propertyName ***REMOVED******REMOVED******REMOVED*** n;
      }
      return false;
    });
  }
  function markLinkedReferences(location, hint, propSymbol, parentType) {
    if (!canCollectSymbolAliasAccessabilityData) {
      return;
    }
    if (location.flags & 33554432 /* Ambient */ && !isPropertySignature(location) && !isPropertyDeclaration(location)) {
      return;
    }
    switch (hint) {
      case 1 /* Identifier */:
        return markIdentifierAliasReferenced(location);
      case 2 /* Property */:
        return markPropertyAliasReferenced(location, propSymbol, parentType);
      case 3 /* ExportAssignment */:
        return markExportAssignmentAliasReferenced(location);
      case 4 /* Jsx */:
        return markJsxAliasReferenced(location);
      case 5 /* AsyncFunction */:
        return markAsyncFunctionAliasReferenced(location);
      case 6 /* ExportImportEquals */:
        return markImportEqualsAliasReferenced(location);
      case 7 /* ExportSpecifier */:
        return markExportSpecifierAliasReferenced(location);
      case 8 /* Decorator */:
        return markDecoratorAliasReferenced(location);
      case 0 /* Unspecified */: {
        if (isIdentifier(location) && (isExpressionNode(location) || isShorthandPropertyAssignment(location.parent) || isImportEqualsDeclaration(location.parent) && location.parent.moduleReference ***REMOVED******REMOVED******REMOVED*** location) && shouldMarkIdentifierAliasReferenced(location)) {
          if (isPropertyAccessOrQualifiedName(location.parent)) {
            const left ***REMOVED*** isPropertyAccessExpression(location.parent) ? location.parent.expression : location.parent.left;
            if (left !***REMOVED******REMOVED*** location) return;
          }
          markIdentifierAliasReferenced(location);
          return;
        }
        if (isPropertyAccessOrQualifiedName(location)) {
          let topProp ***REMOVED*** location;
          while (isPropertyAccessOrQualifiedName(topProp)) {
            if (isPartOfTypeNode(topProp)) return;
            topProp ***REMOVED*** topProp.parent;
          }
          return markPropertyAliasReferenced(location);
        }
        if (isExportAssignment(location)) {
          return markExportAssignmentAliasReferenced(location);
        }
        if (isJsxOpeningLikeElement(location) || isJsxOpeningFragment(location)) {
          return markJsxAliasReferenced(location);
        }
        if (isImportEqualsDeclaration(location)) {
          if (isInternalModuleImportEqualsDeclaration(location) || checkExternalImportOrExportDeclaration(location)) {
            return markImportEqualsAliasReferenced(location);
          }
          return;
        }
        if (isExportSpecifier(location)) {
          return markExportSpecifierAliasReferenced(location);
        }
        if (isFunctionLikeDeclaration(location) || isMethodSignature(location)) {
          markAsyncFunctionAliasReferenced(location);
        }
        if (!compilerOptions.emitDecoratorMetadata) {
          return;
        }
        if (!canHaveDecorators(location) || !hasDecorators(location) || !location.modifiers || !nodeCanBeDecorated(legacyDecorators, location, location.parent, location.parent.parent)) {
          return;
        }
        return markDecoratorAliasReferenced(location);
      }
      default:
        Debug.assertNever(hint, `Unhandled reference hint: ${hint}`);
    }
  }
  function markIdentifierAliasReferenced(location) {
    const symbol ***REMOVED*** getResolvedSymbol(location);
    if (symbol && symbol !***REMOVED******REMOVED*** argumentsSymbol && symbol !***REMOVED******REMOVED*** unknownSymbol && !isThisInTypeQuery(location)) {
      markAliasReferenced(symbol, location);
    }
  }
  function markPropertyAliasReferenced(location, propSymbol, parentType) {
    const left ***REMOVED*** isPropertyAccessExpression(location) ? location.expression : location.left;
    if (isThisIdentifier(left) || !isIdentifier(left)) {
      return;
    }
    const parentSymbol ***REMOVED*** getResolvedSymbol(left);
    if (!parentSymbol || parentSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return;
    }
    if (getIsolatedModules(compilerOptions) || shouldPreserveConstEnums(compilerOptions) && isExportOrExportExpression(location)) {
      markAliasReferenced(parentSymbol, location);
      return;
    }
    const leftType ***REMOVED*** parentType || checkExpressionCached(left);
    if (isTypeAny(leftType) || leftType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      markAliasReferenced(parentSymbol, location);
      return;
    }
    let prop ***REMOVED*** propSymbol;
    if (!prop && !parentType) {
      const right ***REMOVED*** isPropertyAccessExpression(location) ? location.name : location.right;
      const lexicallyScopedSymbol ***REMOVED*** isPrivateIdentifier(right) && lookupSymbolForPrivateIdentifierDeclaration(right.escapedText, right);
      const assignmentKind ***REMOVED*** getAssignmentTargetKind(location);
      const apparentType ***REMOVED*** getApparentType(assignmentKind !***REMOVED******REMOVED*** 0 /* None */ || isMethodAccessForCall(location) ? getWidenedType(leftType) : leftType);
      prop ***REMOVED*** isPrivateIdentifier(right) ? lexicallyScopedSymbol && getPrivateIdentifierPropertyOfType(apparentType, lexicallyScopedSymbol) || void 0 : getPropertyOfType(apparentType, right.escapedText);
    }
    if (!(prop && (isConstEnumOrConstEnumOnlyModule(prop) || prop.flags & 8 /* EnumMember */ && location.parent.kind ***REMOVED******REMOVED******REMOVED*** 307 /* EnumMember */))) {
      markAliasReferenced(parentSymbol, location);
    }
    return;
  }
  function markExportAssignmentAliasReferenced(location) {
    if (isIdentifier(location.expression)) {
      const id ***REMOVED*** location.expression;
      const sym ***REMOVED*** getExportSymbolOfValueSymbolIfExported(resolveEntityName(
        id,
        -1 /* All */,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        true,
        location
      ));
      if (sym) {
        markAliasReferenced(sym, id);
      }
    }
  }
  function markJsxAliasReferenced(node) {
    if (!getJsxNamespaceContainerForImplicitImport(node)) {
      const jsxFactoryRefErr ***REMOVED*** diagnostics && compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 2 /* React */ ? Diagnostics.This_JSX_tag_requires_0_to_be_in_scope_but_it_could_not_be_found : void 0;
      const jsxFactoryNamespace ***REMOVED*** getJsxNamespace(node);
      const jsxFactoryLocation ***REMOVED*** isJsxOpeningLikeElement(node) ? node.tagName : node;
      const shouldFactoryRefErr ***REMOVED*** compilerOptions.jsx !***REMOVED******REMOVED*** 1 /* Preserve */ && compilerOptions.jsx !***REMOVED******REMOVED*** 3 /* ReactNative */;
      let jsxFactorySym;
      if (!(isJsxOpeningFragment(node) && jsxFactoryNamespace ***REMOVED******REMOVED******REMOVED*** "null")) {
        jsxFactorySym ***REMOVED*** resolveName(
          jsxFactoryLocation,
          jsxFactoryNamespace,
          shouldFactoryRefErr ? 111551 /* Value */ : 111551 /* Value */ & ~384 /* Enum */,
          jsxFactoryRefErr,
          /*isUse*/
          true
        );
      }
      if (jsxFactorySym) {
        jsxFactorySym.isReferenced ***REMOVED*** -1 /* All */;
        if (canCollectSymbolAliasAccessabilityData && jsxFactorySym.flags & 2097152 /* Alias */ && !getTypeOnlyAliasDeclaration(jsxFactorySym)) {
          markAliasSymbolAsReferenced(jsxFactorySym);
        }
      }
      if (isJsxOpeningFragment(node)) {
        const file ***REMOVED*** getSourceFileOfNode(node);
        const entity ***REMOVED*** getJsxFactoryEntity(file);
        if (entity) {
          const localJsxNamespace ***REMOVED*** getFirstIdentifier(entity).escapedText;
          resolveName(
            jsxFactoryLocation,
            localJsxNamespace,
            shouldFactoryRefErr ? 111551 /* Value */ : 111551 /* Value */ & ~384 /* Enum */,
            jsxFactoryRefErr,
            /*isUse*/
            true
          );
        }
      }
    }
    return;
  }
  function markAsyncFunctionAliasReferenced(location) {
    if (languageVersion < 2 /* ES2015 */) {
      if (getFunctionFlags(location) & 2 /* Async */) {
        const returnTypeNode ***REMOVED*** getEffectiveReturnTypeNode(location);
        markTypeNodeAsReferenced(returnTypeNode);
      }
    }
  }
  function markImportEqualsAliasReferenced(location) {
    if (hasSyntacticModifier(location, 32 /* Export */)) {
      markExportAsReferenced(location);
    }
  }
  function markExportSpecifierAliasReferenced(location) {
    if (!location.parent.parent.moduleSpecifier && !location.isTypeOnly && !location.parent.parent.isTypeOnly) {
      const exportedName ***REMOVED*** location.propertyName || location.name;
      if (exportedName.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
        return;
      }
      const symbol ***REMOVED*** resolveName(
        exportedName,
        exportedName.escapedText,
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        true
      );
      if (symbol && (symbol ***REMOVED******REMOVED******REMOVED*** undefinedSymbol || symbol ***REMOVED******REMOVED******REMOVED*** globalThisSymbol || symbol.declarations && isGlobalSourceFile(getDeclarationContainer(symbol.declarations[0])))) {
      } else {
        const target ***REMOVED*** symbol && (symbol.flags & 2097152 /* Alias */ ? resolveAlias(symbol) : symbol);
        if (!target || getSymbolFlags(target) & 111551 /* Value */) {
          markExportAsReferenced(location);
          markIdentifierAliasReferenced(exportedName);
        }
      }
      return;
    }
  }
  function markDecoratorAliasReferenced(node) {
    if (compilerOptions.emitDecoratorMetadata) {
      const firstDecorator ***REMOVED*** find(node.modifiers, isDecorator);
      if (!firstDecorator) {
        return;
      }
      checkExternalEmitHelpers(firstDecorator, 16 /* Metadata */);
      switch (node.kind) {
        case 264 /* ClassDeclaration */:
          const constructor ***REMOVED*** getFirstConstructorWithBody(node);
          if (constructor) {
            for (const parameter of constructor.parameters) {
              markDecoratorMedataDataTypeNodeAsReferenced(getParameterTypeNodeForDecoratorCheck(parameter));
            }
          }
          break;
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          const otherKind ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? 179 /* SetAccessor */ : 178 /* GetAccessor */;
          const otherAccessor ***REMOVED*** getDeclarationOfKind(getSymbolOfDeclaration(node), otherKind);
          markDecoratorMedataDataTypeNodeAsReferenced(getAnnotatedAccessorTypeNode(node) || otherAccessor && getAnnotatedAccessorTypeNode(otherAccessor));
          break;
        case 175 /* MethodDeclaration */:
          for (const parameter of node.parameters) {
            markDecoratorMedataDataTypeNodeAsReferenced(getParameterTypeNodeForDecoratorCheck(parameter));
          }
          markDecoratorMedataDataTypeNodeAsReferenced(getEffectiveReturnTypeNode(node));
          break;
        case 173 /* PropertyDeclaration */:
          markDecoratorMedataDataTypeNodeAsReferenced(getEffectiveTypeAnnotationNode(node));
          break;
        case 170 /* Parameter */:
          markDecoratorMedataDataTypeNodeAsReferenced(getParameterTypeNodeForDecoratorCheck(node));
          const containingSignature ***REMOVED*** node.parent;
          for (const parameter of containingSignature.parameters) {
            markDecoratorMedataDataTypeNodeAsReferenced(getParameterTypeNodeForDecoratorCheck(parameter));
          }
          markDecoratorMedataDataTypeNodeAsReferenced(getEffectiveReturnTypeNode(containingSignature));
          break;
      }
    }
  }
  function markAliasReferenced(symbol, location) {
    if (!canCollectSymbolAliasAccessabilityData) {
      return;
    }
    if (isNonLocalAlias(
      symbol,
      /*excludes*/
      111551 /* Value */
    ) && !isInTypeQuery(location)) {
      const target ***REMOVED*** resolveAlias(symbol);
      if (getSymbolFlags(
        symbol,
        /*excludeTypeOnlyMeanings*/
        true
      ) & (111551 /* Value */ | 1048576 /* ExportValue */)) {
        if (getIsolatedModules(compilerOptions) || shouldPreserveConstEnums(compilerOptions) && isExportOrExportExpression(location) || !isConstEnumOrConstEnumOnlyModule(getExportSymbolOfValueSymbolIfExported(target))) {
          markAliasSymbolAsReferenced(symbol);
        }
      }
    }
  }
  function markAliasSymbolAsReferenced(symbol) {
    Debug.assert(canCollectSymbolAliasAccessabilityData);
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.referenced) {
      links.referenced ***REMOVED*** true;
      const node ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
      if (!node) return Debug.fail();
      if (isInternalModuleImportEqualsDeclaration(node)) {
        if (getSymbolFlags(resolveSymbol(symbol)) & 111551 /* Value */) {
          const left ***REMOVED*** getFirstIdentifier(node.moduleReference);
          markIdentifierAliasReferenced(left);
        }
      }
    }
  }
  function markExportAsReferenced(node) {
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    const target ***REMOVED*** resolveAlias(symbol);
    if (target) {
      const markAlias ***REMOVED*** target ***REMOVED******REMOVED******REMOVED*** unknownSymbol || getSymbolFlags(
        symbol,
        /*excludeTypeOnlyMeanings*/
        true
      ) & 111551 /* Value */ && !isConstEnumOrConstEnumOnlyModule(target);
      if (markAlias) {
        markAliasSymbolAsReferenced(symbol);
      }
    }
  }
  function markEntityNameOrEntityExpressionAsReference(typeName, forDecoratorMetadata) {
    if (!typeName) return;
    const rootName ***REMOVED*** getFirstIdentifier(typeName);
    const meaning ***REMOVED*** (typeName.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? 788968 /* Type */ : 1920 /* Namespace */) | 2097152 /* Alias */;
    const rootSymbol ***REMOVED*** resolveName(
      rootName,
      rootName.escapedText,
      meaning,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true
    );
    if (rootSymbol && rootSymbol.flags & 2097152 /* Alias */) {
      if (canCollectSymbolAliasAccessabilityData && symbolIsValue(rootSymbol) && !isConstEnumOrConstEnumOnlyModule(resolveAlias(rootSymbol)) && !getTypeOnlyAliasDeclaration(rootSymbol)) {
        markAliasSymbolAsReferenced(rootSymbol);
      } else if (forDecoratorMetadata && getIsolatedModules(compilerOptions) && getEmitModuleKind(compilerOptions) >***REMOVED*** 5 /* ES2015 */ && !symbolIsValue(rootSymbol) && !some(rootSymbol.declarations, isTypeOnlyImportOrExportDeclaration)) {
        const diag2 ***REMOVED*** error(typeName, Diagnostics.A_type_referenced_in_a_decorated_signature_must_be_imported_with_import_type_or_a_namespace_import_when_isolatedModules_and_emitDecoratorMetadata_are_enabled);
        const aliasDeclaration ***REMOVED*** find(rootSymbol.declarations || emptyArray, isAliasSymbolDeclaration);
        if (aliasDeclaration) {
          addRelatedInfo(diag2, createDiagnosticForNode(aliasDeclaration, Diagnostics._0_was_imported_here, idText(rootName)));
        }
      }
    }
  }
  function markTypeNodeAsReferenced(node) {
    markEntityNameOrEntityExpressionAsReference(
      node && getEntityNameFromTypeNode(node),
      /*forDecoratorMetadata*/
      false
    );
  }
  function markDecoratorMedataDataTypeNodeAsReferenced(node) {
    const entityName ***REMOVED*** getEntityNameForDecoratorMetadata(node);
    if (entityName && isEntityName(entityName)) {
      markEntityNameOrEntityExpressionAsReference(
        entityName,
        /*forDecoratorMetadata*/
        true
      );
    }
  }
  function getNarrowedTypeOfSymbol(symbol, location) {
    var _a;
    const type ***REMOVED*** getTypeOfSymbol(symbol);
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (declaration) {
      if (isBindingElement(declaration) && !declaration.initializer && !declaration.dotDotDotToken && declaration.parent.elements.length >***REMOVED*** 2) {
        const parent ***REMOVED*** declaration.parent.parent;
        const rootDeclaration ***REMOVED*** getRootDeclaration(parent);
        if (rootDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && getCombinedNodeFlagsCached(rootDeclaration) & 6 /* Constant */ || rootDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
          const links ***REMOVED*** getNodeLinks(parent);
          if (!(links.flags & 4194304 /* InCheckIdentifier */)) {
            links.flags |***REMOVED*** 4194304 /* InCheckIdentifier */;
            const parentType ***REMOVED*** getTypeForBindingElementParent(parent, 0 /* Normal */);
            const parentTypeConstraint ***REMOVED*** parentType && mapType(parentType, getBaseConstraintOrType);
            links.flags &***REMOVED*** ~4194304 /* InCheckIdentifier */;
            if (parentTypeConstraint && parentTypeConstraint.flags & 1048576 /* Union */ && !(rootDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && isSomeSymbolAssigned(rootDeclaration))) {
              const pattern ***REMOVED*** declaration.parent;
              const narrowedType ***REMOVED*** getFlowTypeOfReference(
                pattern,
                parentTypeConstraint,
                parentTypeConstraint,
                /*flowContainer*/
                void 0,
                location.flowNode
              );
              if (narrowedType.flags & 131072 /* Never */) {
                return neverType;
              }
              return getBindingElementTypeFromParentType(
                declaration,
                narrowedType,
                /*noTupleBoundsCheck*/
                true
              );
            }
          }
        }
      }
      if (isParameter(declaration) && !declaration.type && !declaration.initializer && !declaration.dotDotDotToken) {
        const func ***REMOVED*** declaration.parent;
        if (func.parameters.length >***REMOVED*** 2 && isContextSensitiveFunctionOrObjectLiteralMethod(func)) {
          const contextualSignature ***REMOVED*** getContextualSignature(func);
          if (contextualSignature && contextualSignature.parameters.length ***REMOVED******REMOVED******REMOVED*** 1 && signatureHasRestParameter(contextualSignature)) {
            const restType ***REMOVED*** getReducedApparentType(instantiateType(getTypeOfSymbol(contextualSignature.parameters[0]), (_a ***REMOVED*** getInferenceContext(func)) ***REMOVED******REMOVED*** null ? void 0 : _a.nonFixingMapper));
            if (restType.flags & 1048576 /* Union */ && everyType(restType, isTupleType) && !some(func.parameters, isSomeSymbolAssigned)) {
              const narrowedType ***REMOVED*** getFlowTypeOfReference(
                func,
                restType,
                restType,
                /*flowContainer*/
                void 0,
                location.flowNode
              );
              const index ***REMOVED*** func.parameters.indexOf(declaration) - (getThisParameter(func) ? 1 : 0);
              return getIndexedAccessType(narrowedType, getNumberLiteralType(index));
            }
          }
        }
      }
    }
    return type;
  }
  function checkIdentifierCalculateNodeCheckFlags(node, symbol) {
    if (isThisInTypeQuery(node)) return;
    if (symbol ***REMOVED******REMOVED******REMOVED*** argumentsSymbol) {
      if (isInPropertyInitializerOrClassStaticBlock(
        node,
        /*ignoreArrowFunctions*/
        true
      )) {
        error(node, Diagnostics.arguments_cannot_be_referenced_in_property_initializers_or_class_static_initialization_blocks);
        return;
      }
      let container ***REMOVED*** getContainingFunction(node);
      if (container) {
        if (languageVersion < 2 /* ES2015 */) {
          if (container.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
            error(node, Diagnostics.The_arguments_object_cannot_be_referenced_in_an_arrow_function_in_ES5_Consider_using_a_standard_function_expression);
          } else if (hasSyntacticModifier(container, 1024 /* Async */)) {
            error(node, Diagnostics.The_arguments_object_cannot_be_referenced_in_an_async_function_or_method_in_ES5_Consider_using_a_standard_function_or_method);
          }
        }
        getNodeLinks(container).flags |***REMOVED*** 512 /* CaptureArguments */;
        while (container && isArrowFunction(container)) {
          container ***REMOVED*** getContainingFunction(container);
          if (container) {
            getNodeLinks(container).flags |***REMOVED*** 512 /* CaptureArguments */;
          }
        }
      }
      return;
    }
    const localOrExportSymbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(symbol);
    const targetSymbol ***REMOVED*** resolveAliasWithDeprecationCheck(localOrExportSymbol, node);
    if (isDeprecatedSymbol(targetSymbol) && isUncalledFunctionReference(node, targetSymbol) && targetSymbol.declarations) {
      addDeprecatedSuggestion(node, targetSymbol.declarations, node.escapedText);
    }
    const declaration ***REMOVED*** localOrExportSymbol.valueDeclaration;
    if (declaration && localOrExportSymbol.flags & 32 /* Class */) {
      if (isClassLike(declaration) && declaration.name !***REMOVED******REMOVED*** node) {
        let container ***REMOVED*** getThisContainer(
          node,
          /*includeArrowFunctions*/
          false,
          /*includeClassComputedPropertyName*/
          false
        );
        while (container.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ && container.parent !***REMOVED******REMOVED*** declaration) {
          container ***REMOVED*** getThisContainer(
            container,
            /*includeArrowFunctions*/
            false,
            /*includeClassComputedPropertyName*/
            false
          );
        }
        if (container.kind !***REMOVED******REMOVED*** 308 /* SourceFile */) {
          getNodeLinks(declaration).flags |***REMOVED*** 262144 /* ContainsConstructorReference */;
          getNodeLinks(container).flags |***REMOVED*** 262144 /* ContainsConstructorReference */;
          getNodeLinks(node).flags |***REMOVED*** 536870912 /* ConstructorReference */;
        }
      }
    }
    checkNestedBlockScopedBinding(node, symbol);
  }
  function checkIdentifier(node, checkMode) {
    if (isThisInTypeQuery(node)) {
      return checkThisExpression(node);
    }
    const symbol ***REMOVED*** getResolvedSymbol(node);
    if (symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return errorType;
    }
    checkIdentifierCalculateNodeCheckFlags(node, symbol);
    if (symbol ***REMOVED******REMOVED******REMOVED*** argumentsSymbol) {
      if (isInPropertyInitializerOrClassStaticBlock(node)) {
        return errorType;
      }
      return getTypeOfSymbol(symbol);
    }
    if (shouldMarkIdentifierAliasReferenced(node)) {
      markLinkedReferences(node, 1 /* Identifier */);
    }
    const localOrExportSymbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(symbol);
    let declaration ***REMOVED*** localOrExportSymbol.valueDeclaration;
    const immediateDeclaration ***REMOVED*** declaration;
    if (declaration && declaration.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && contains(contextualBindingPatterns, declaration.parent) && findAncestor(node, (parent) ***REMOVED***> parent ***REMOVED******REMOVED******REMOVED*** declaration.parent)) {
      return nonInferrableAnyType;
    }
    let type ***REMOVED*** getNarrowedTypeOfSymbol(localOrExportSymbol, node);
    const assignmentKind ***REMOVED*** getAssignmentTargetKind(node);
    if (assignmentKind) {
      if (!(localOrExportSymbol.flags & 3 /* Variable */) && !(isInJSFile(node) && localOrExportSymbol.flags & 512 /* ValueModule */)) {
        const assignmentError ***REMOVED*** localOrExportSymbol.flags & 384 /* Enum */ ? Diagnostics.Cannot_assign_to_0_because_it_is_an_enum : localOrExportSymbol.flags & 32 /* Class */ ? Diagnostics.Cannot_assign_to_0_because_it_is_a_class : localOrExportSymbol.flags & 1536 /* Module */ ? Diagnostics.Cannot_assign_to_0_because_it_is_a_namespace : localOrExportSymbol.flags & 16 /* Function */ ? Diagnostics.Cannot_assign_to_0_because_it_is_a_function : localOrExportSymbol.flags & 2097152 /* Alias */ ? Diagnostics.Cannot_assign_to_0_because_it_is_an_import : Diagnostics.Cannot_assign_to_0_because_it_is_not_a_variable;
        error(node, assignmentError, symbolToString(symbol));
        return errorType;
      }
      if (isReadonlySymbol(localOrExportSymbol)) {
        if (localOrExportSymbol.flags & 3 /* Variable */) {
          error(node, Diagnostics.Cannot_assign_to_0_because_it_is_a_constant, symbolToString(symbol));
        } else {
          error(node, Diagnostics.Cannot_assign_to_0_because_it_is_a_read_only_property, symbolToString(symbol));
        }
        return errorType;
      }
    }
    const isAlias ***REMOVED*** localOrExportSymbol.flags & 2097152 /* Alias */;
    if (localOrExportSymbol.flags & 3 /* Variable */) {
      if (assignmentKind ***REMOVED******REMOVED******REMOVED*** 1 /* Definite */) {
        return isInCompoundLikeAssignment(node) ? getBaseTypeOfLiteralType(type) : type;
      }
    } else if (isAlias) {
      declaration ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
    } else {
      return type;
    }
    if (!declaration) {
      return type;
    }
    type ***REMOVED*** getNarrowableTypeForReference(type, node, checkMode);
    const isParameter2 ***REMOVED*** getRootDeclaration(declaration).kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */;
    const declarationContainer ***REMOVED*** getControlFlowContainer(declaration);
    let flowContainer ***REMOVED*** getControlFlowContainer(node);
    const isOuterVariable ***REMOVED*** flowContainer !***REMOVED******REMOVED*** declarationContainer;
    const isSpreadDestructuringAssignmentTarget ***REMOVED*** node.parent && node.parent.parent && isSpreadAssignment(node.parent) && isDestructuringAssignmentTarget(node.parent.parent);
    const isModuleExports ***REMOVED*** symbol.flags & 134217728 /* ModuleExports */;
    const typeIsAutomatic ***REMOVED*** type ***REMOVED******REMOVED******REMOVED*** autoType || type ***REMOVED******REMOVED******REMOVED*** autoArrayType;
    const isAutomaticTypeInNonNull ***REMOVED*** typeIsAutomatic && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 236 /* NonNullExpression */;
    while (flowContainer !***REMOVED******REMOVED*** declarationContainer && (flowContainer.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || flowContainer.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */ || isObjectLiteralOrClassExpressionMethodOrAccessor(flowContainer)) && (isConstantVariable(localOrExportSymbol) && type !***REMOVED******REMOVED*** autoArrayType || isParameterOrMutableLocalVariable(localOrExportSymbol) && isPastLastAssignment(localOrExportSymbol, node))) {
      flowContainer ***REMOVED*** getControlFlowContainer(flowContainer);
    }
    const isNeverInitialized ***REMOVED*** immediateDeclaration && isVariableDeclaration(immediateDeclaration) && !immediateDeclaration.initializer && !immediateDeclaration.exclamationToken && isMutableLocalVariableDeclaration(immediateDeclaration) && !isSymbolAssignedDefinitely(symbol);
    const assumeInitialized ***REMOVED*** isParameter2 || isAlias || isOuterVariable && !isNeverInitialized || isSpreadDestructuringAssignmentTarget || isModuleExports || isSameScopedBindingElement(node, declaration) || type !***REMOVED******REMOVED*** autoType && type !***REMOVED******REMOVED*** autoArrayType && (!strictNullChecks || (type.flags & (3 /* AnyOrUnknown */ | 16384 /* Void */)) !***REMOVED******REMOVED*** 0 || isInTypeQuery(node) || isInAmbientOrTypeNode(node) || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */) || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 236 /* NonNullExpression */ || declaration.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && declaration.exclamationToken || declaration.flags & 33554432 /* Ambient */;
    const initialType ***REMOVED*** isAutomaticTypeInNonNull ? undefinedType : assumeInitialized ? isParameter2 ? removeOptionalityFromDeclaredType(type, declaration) : type : typeIsAutomatic ? undefinedType : getOptionalType(type);
    const flowType ***REMOVED*** isAutomaticTypeInNonNull ? getNonNullableType(getFlowTypeOfReference(node, type, initialType, flowContainer)) : getFlowTypeOfReference(node, type, initialType, flowContainer);
    if (!isEvolvingArrayOperationTarget(node) && (type ***REMOVED******REMOVED******REMOVED*** autoType || type ***REMOVED******REMOVED******REMOVED*** autoArrayType)) {
      if (flowType ***REMOVED******REMOVED******REMOVED*** autoType || flowType ***REMOVED******REMOVED******REMOVED*** autoArrayType) {
        if (noImplicitAny) {
          error(getNameOfDeclaration(declaration), Diagnostics.Variable_0_implicitly_has_type_1_in_some_locations_where_its_type_cannot_be_determined, symbolToString(symbol), typeToString(flowType));
          error(node, Diagnostics.Variable_0_implicitly_has_an_1_type, symbolToString(symbol), typeToString(flowType));
        }
        return convertAutoToAny(flowType);
      }
    } else if (!assumeInitialized && !containsUndefinedType(type) && containsUndefinedType(flowType)) {
      error(node, Diagnostics.Variable_0_is_used_before_being_assigned, symbolToString(symbol));
      return type;
    }
    return assignmentKind ? getBaseTypeOfLiteralType(flowType) : flowType;
  }
  function isSameScopedBindingElement(node, declaration) {
    if (isBindingElement(declaration)) {
      const bindingElement ***REMOVED*** findAncestor(node, isBindingElement);
      return bindingElement && getRootDeclaration(bindingElement) ***REMOVED******REMOVED******REMOVED*** getRootDeclaration(declaration);
    }
  }
  function shouldMarkIdentifierAliasReferenced(node) {
    var _a;
    const parent ***REMOVED*** node.parent;
    if (parent) {
      if (isPropertyAccessExpression(parent) && parent.expression ***REMOVED******REMOVED******REMOVED*** node) {
        return false;
      }
      if (isExportSpecifier(parent) && parent.isTypeOnly) {
        return false;
      }
      const greatGrandparent ***REMOVED*** (_a ***REMOVED*** parent.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.parent;
      if (greatGrandparent && isExportDeclaration(greatGrandparent) && greatGrandparent.isTypeOnly) {
        return false;
      }
    }
    return true;
  }
  function isInsideFunctionOrInstancePropertyInitializer(node, threshold) {
    return !!findAncestor(node, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** threshold ? "quit" : isFunctionLike(n) || n.parent && isPropertyDeclaration(n.parent) && !hasStaticModifier(n.parent) && n.parent.initializer ***REMOVED******REMOVED******REMOVED*** n);
  }
  function getPartOfForStatementContainingNode(node, container) {
    return findAncestor(node, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** container ? "quit" : n ***REMOVED******REMOVED******REMOVED*** container.initializer || n ***REMOVED******REMOVED******REMOVED*** container.condition || n ***REMOVED******REMOVED******REMOVED*** container.incrementor || n ***REMOVED******REMOVED******REMOVED*** container.statement);
  }
  function getEnclosingIterationStatement(node) {
    return findAncestor(node, (n) ***REMOVED***> !n || nodeStartsNewLexicalEnvironment(n) ? "quit" : isIterationStatement(
      n,
      /*lookInLabeledStatements*/
      false
    ));
  }
  function checkNestedBlockScopedBinding(node, symbol) {
    if (languageVersion >***REMOVED*** 2 /* ES2015 */ || (symbol.flags & (2 /* BlockScopedVariable */ | 32 /* Class */)) ***REMOVED******REMOVED******REMOVED*** 0 || !symbol.valueDeclaration || isSourceFile(symbol.valueDeclaration) || symbol.valueDeclaration.parent.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */) {
      return;
    }
    const container ***REMOVED*** getEnclosingBlockScopeContainer(symbol.valueDeclaration);
    const isCaptured ***REMOVED*** isInsideFunctionOrInstancePropertyInitializer(node, container);
    const enclosingIterationStatement ***REMOVED*** getEnclosingIterationStatement(container);
    if (enclosingIterationStatement) {
      if (isCaptured) {
        let capturesBlockScopeBindingInLoopBody ***REMOVED*** true;
        if (isForStatement(container)) {
          const varDeclList ***REMOVED*** getAncestor(symbol.valueDeclaration, 262 /* VariableDeclarationList */);
          if (varDeclList && varDeclList.parent ***REMOVED******REMOVED******REMOVED*** container) {
            const part ***REMOVED*** getPartOfForStatementContainingNode(node.parent, container);
            if (part) {
              const links ***REMOVED*** getNodeLinks(part);
              links.flags |***REMOVED*** 8192 /* ContainsCapturedBlockScopeBinding */;
              const capturedBindings ***REMOVED*** links.capturedBlockScopeBindings || (links.capturedBlockScopeBindings ***REMOVED*** []);
              pushIfUnique(capturedBindings, symbol);
              if (part ***REMOVED******REMOVED******REMOVED*** container.initializer) {
                capturesBlockScopeBindingInLoopBody ***REMOVED*** false;
              }
            }
          }
        }
        if (capturesBlockScopeBindingInLoopBody) {
          getNodeLinks(enclosingIterationStatement).flags |***REMOVED*** 4096 /* LoopWithCapturedBlockScopedBinding */;
        }
      }
      if (isForStatement(container)) {
        const varDeclList ***REMOVED*** getAncestor(symbol.valueDeclaration, 262 /* VariableDeclarationList */);
        if (varDeclList && varDeclList.parent ***REMOVED******REMOVED******REMOVED*** container && isAssignedInBodyOfForStatement(node, container)) {
          getNodeLinks(symbol.valueDeclaration).flags |***REMOVED*** 65536 /* NeedsLoopOutParameter */;
        }
      }
      getNodeLinks(symbol.valueDeclaration).flags |***REMOVED*** 32768 /* BlockScopedBindingInLoop */;
    }
    if (isCaptured) {
      getNodeLinks(symbol.valueDeclaration).flags |***REMOVED*** 16384 /* CapturedBlockScopedBinding */;
    }
  }
  function isBindingCapturedByNode(node, decl) {
    const links ***REMOVED*** getNodeLinks(node);
    return !!links && contains(links.capturedBlockScopeBindings, getSymbolOfDeclaration(decl));
  }
  function isAssignedInBodyOfForStatement(node, container) {
    let current ***REMOVED*** node;
    while (current.parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
      current ***REMOVED*** current.parent;
    }
    let isAssigned ***REMOVED*** false;
    if (isAssignmentTarget(current)) {
      isAssigned ***REMOVED*** true;
    } else if (current.parent.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ || current.parent.kind ***REMOVED******REMOVED******REMOVED*** 226 /* PostfixUnaryExpression */) {
      const expr ***REMOVED*** current.parent;
      isAssigned ***REMOVED*** expr.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || expr.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */;
    }
    if (!isAssigned) {
      return false;
    }
    return !!findAncestor(current, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** container ? "quit" : n ***REMOVED******REMOVED******REMOVED*** container.statement);
  }
  function captureLexicalThis(node, container) {
    getNodeLinks(node).flags |***REMOVED*** 2 /* LexicalThis */;
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      const classNode ***REMOVED*** container.parent;
      getNodeLinks(classNode).flags |***REMOVED*** 4 /* CaptureThis */;
    } else {
      getNodeLinks(container).flags |***REMOVED*** 4 /* CaptureThis */;
    }
  }
  function findFirstSuperCall(node) {
    return isSuperCall(node) ? node : isFunctionLike(node) ? void 0 : forEachChild(node, findFirstSuperCall);
  }
  function classDeclarationExtendsNull(classDecl) {
    const classSymbol ***REMOVED*** getSymbolOfDeclaration(classDecl);
    const classInstanceType ***REMOVED*** getDeclaredTypeOfSymbol(classSymbol);
    const baseConstructorType ***REMOVED*** getBaseConstructorTypeOfClass(classInstanceType);
    return baseConstructorType ***REMOVED******REMOVED******REMOVED*** nullWideningType;
  }
  function checkThisBeforeSuper(node, container, diagnosticMessage) {
    const containingClassDecl ***REMOVED*** container.parent;
    const baseTypeNode ***REMOVED*** getClassExtendsHeritageElement(containingClassDecl);
    if (baseTypeNode && !classDeclarationExtendsNull(containingClassDecl)) {
      if (canHaveFlowNode(node) && node.flowNode && !isPostSuperFlowNode(
        node.flowNode,
        /*noCacheCheck*/
        false
      )) {
        error(node, diagnosticMessage);
      }
    }
  }
  function checkThisInStaticClassFieldInitializerInDecoratedClass(thisExpression, container) {
    if (isPropertyDeclaration(container) && hasStaticModifier(container) && legacyDecorators && container.initializer && textRangeContainsPositionInclusive(container.initializer, thisExpression.pos) && hasDecorators(container.parent)) {
      error(thisExpression, Diagnostics.Cannot_use_this_in_a_static_property_initializer_of_a_decorated_class);
    }
  }
  function checkThisExpression(node) {
    const isNodeInTypeQuery ***REMOVED*** isInTypeQuery(node);
    let container ***REMOVED*** getThisContainer(
      node,
      /*includeArrowFunctions*/
      true,
      /*includeClassComputedPropertyName*/
      true
    );
    let capturedByArrowFunction ***REMOVED*** false;
    let thisInComputedPropertyName ***REMOVED*** false;
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      checkThisBeforeSuper(node, container, Diagnostics.super_must_be_called_before_accessing_this_in_the_constructor_of_a_derived_class);
    }
    while (true) {
      if (container.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        container ***REMOVED*** getThisContainer(
          container,
          /*includeArrowFunctions*/
          false,
          !thisInComputedPropertyName
        );
        capturedByArrowFunction ***REMOVED*** true;
      }
      if (container.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        container ***REMOVED*** getThisContainer(
          container,
          !capturedByArrowFunction,
          /*includeClassComputedPropertyName*/
          false
        );
        thisInComputedPropertyName ***REMOVED*** true;
        continue;
      }
      break;
    }
    checkThisInStaticClassFieldInitializerInDecoratedClass(node, container);
    if (thisInComputedPropertyName) {
      error(node, Diagnostics.this_cannot_be_referenced_in_a_computed_property_name);
    } else {
      switch (container.kind) {
        case 268 /* ModuleDeclaration */:
          error(node, Diagnostics.this_cannot_be_referenced_in_a_module_or_namespace_body);
          break;
        case 267 /* EnumDeclaration */:
          error(node, Diagnostics.this_cannot_be_referenced_in_current_location);
          break;
      }
    }
    if (!isNodeInTypeQuery && capturedByArrowFunction && languageVersion < 2 /* ES2015 */) {
      captureLexicalThis(node, container);
    }
    const type ***REMOVED*** tryGetThisTypeAt(
      node,
      /*includeGlobalThis*/
      true,
      container
    );
    if (noImplicitThis) {
      const globalThisType2 ***REMOVED*** getTypeOfSymbol(globalThisSymbol);
      if (type ***REMOVED******REMOVED******REMOVED*** globalThisType2 && capturedByArrowFunction) {
        error(node, Diagnostics.The_containing_arrow_function_captures_the_global_value_of_this);
      } else if (!type) {
        const diag2 ***REMOVED*** error(node, Diagnostics.this_implicitly_has_type_any_because_it_does_not_have_a_type_annotation);
        if (!isSourceFile(container)) {
          const outsideThis ***REMOVED*** tryGetThisTypeAt(container);
          if (outsideThis && outsideThis !***REMOVED******REMOVED*** globalThisType2) {
            addRelatedInfo(diag2, createDiagnosticForNode(container, Diagnostics.An_outer_value_of_this_is_shadowed_by_this_container));
          }
        }
      }
    }
    return type || anyType;
  }
  function tryGetThisTypeAt(node, includeGlobalThis ***REMOVED*** true, container ***REMOVED*** getThisContainer(
    node,
    /*includeArrowFunctions*/
    false,
    /*includeClassComputedPropertyName*/
    false
  )) {
    const isInJS ***REMOVED*** isInJSFile(node);
    if (isFunctionLike(container) && (!isInParameterInitializerBeforeContainingFunction(node) || getThisParameter(container))) {
      let thisType ***REMOVED*** getThisTypeOfDeclaration(container) || isInJS && getTypeForThisExpressionFromJSDoc(container);
      if (!thisType) {
        const className ***REMOVED*** getClassNameFromPrototypeMethod(container);
        if (isInJS && className) {
          const classSymbol ***REMOVED*** checkExpression(className).symbol;
          if (classSymbol && classSymbol.members && classSymbol.flags & 16 /* Function */) {
            thisType ***REMOVED*** getDeclaredTypeOfSymbol(classSymbol).thisType;
          }
        } else if (isJSConstructor(container)) {
          thisType ***REMOVED*** getDeclaredTypeOfSymbol(getMergedSymbol(container.symbol)).thisType;
        }
        thisType || (thisType ***REMOVED*** getContextualThisParameterType(container));
      }
      if (thisType) {
        return getFlowTypeOfReference(node, thisType);
      }
    }
    if (isClassLike(container.parent)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(container.parent);
      const type ***REMOVED*** isStatic(container) ? getTypeOfSymbol(symbol) : getDeclaredTypeOfSymbol(symbol).thisType;
      return getFlowTypeOfReference(node, type);
    }
    if (isSourceFile(container)) {
      if (container.commonJsModuleIndicator) {
        const fileSymbol ***REMOVED*** getSymbolOfDeclaration(container);
        return fileSymbol && getTypeOfSymbol(fileSymbol);
      } else if (container.externalModuleIndicator) {
        return undefinedType;
      } else if (includeGlobalThis) {
        return getTypeOfSymbol(globalThisSymbol);
      }
    }
  }
  function getExplicitThisType(node) {
    const container ***REMOVED*** getThisContainer(
      node,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    if (isFunctionLike(container)) {
      const signature ***REMOVED*** getSignatureFromDeclaration(container);
      if (signature.thisParameter) {
        return getExplicitTypeOfSymbol(signature.thisParameter);
      }
    }
    if (isClassLike(container.parent)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(container.parent);
      return isStatic(container) ? getTypeOfSymbol(symbol) : getDeclaredTypeOfSymbol(symbol).thisType;
    }
  }
  function getClassNameFromPrototypeMethod(container) {
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ && isBinaryExpression(container.parent) && getAssignmentDeclarationKind(container.parent) ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */) {
      return container.parent.left.expression.expression;
    } else if (container.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && container.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ && isBinaryExpression(container.parent.parent) && getAssignmentDeclarationKind(container.parent.parent) ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */) {
      return container.parent.parent.left.expression;
    } else if (container.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ && container.parent.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ && container.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ && isBinaryExpression(container.parent.parent.parent) && getAssignmentDeclarationKind(container.parent.parent.parent) ***REMOVED******REMOVED******REMOVED*** 6 /* Prototype */) {
      return container.parent.parent.parent.left.expression;
    } else if (container.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ && isPropertyAssignment(container.parent) && isIdentifier(container.parent.name) && (container.parent.name.escapedText ***REMOVED******REMOVED******REMOVED*** "value" || container.parent.name.escapedText ***REMOVED******REMOVED******REMOVED*** "get" || container.parent.name.escapedText ***REMOVED******REMOVED******REMOVED*** "set") && isObjectLiteralExpression(container.parent.parent) && isCallExpression(container.parent.parent.parent) && container.parent.parent.parent.arguments[2] ***REMOVED******REMOVED******REMOVED*** container.parent.parent && getAssignmentDeclarationKind(container.parent.parent.parent) ***REMOVED******REMOVED******REMOVED*** 9 /* ObjectDefinePrototypeProperty */) {
      return container.parent.parent.parent.arguments[0].expression;
    } else if (isMethodDeclaration(container) && isIdentifier(container.name) && (container.name.escapedText ***REMOVED******REMOVED******REMOVED*** "value" || container.name.escapedText ***REMOVED******REMOVED******REMOVED*** "get" || container.name.escapedText ***REMOVED******REMOVED******REMOVED*** "set") && isObjectLiteralExpression(container.parent) && isCallExpression(container.parent.parent) && container.parent.parent.arguments[2] ***REMOVED******REMOVED******REMOVED*** container.parent && getAssignmentDeclarationKind(container.parent.parent) ***REMOVED******REMOVED******REMOVED*** 9 /* ObjectDefinePrototypeProperty */) {
      return container.parent.parent.arguments[0].expression;
    }
  }
  function getTypeForThisExpressionFromJSDoc(node) {
    const thisTag ***REMOVED*** getJSDocThisTag(node);
    if (thisTag && thisTag.typeExpression) {
      return getTypeFromTypeNode(thisTag.typeExpression);
    }
    const signature ***REMOVED*** getSignatureOfTypeTag(node);
    if (signature) {
      return getThisTypeOfSignature(signature);
    }
  }
  function isInConstructorArgumentInitializer(node, constructorDecl) {
    return !!findAncestor(node, (n) ***REMOVED***> isFunctionLikeDeclaration(n) ? "quit" : n.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && n.parent ***REMOVED******REMOVED******REMOVED*** constructorDecl);
  }
  function checkSuperExpression(node) {
    const isCallExpression2 ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node;
    const immediateContainer ***REMOVED*** getSuperContainer(
      node,
      /*stopOnFunctions*/
      true
    );
    let container ***REMOVED*** immediateContainer;
    let needToCaptureLexicalThis ***REMOVED*** false;
    let inAsyncFunction ***REMOVED*** false;
    if (!isCallExpression2) {
      while (container && container.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        if (hasSyntacticModifier(container, 1024 /* Async */)) inAsyncFunction ***REMOVED*** true;
        container ***REMOVED*** getSuperContainer(
          container,
          /*stopOnFunctions*/
          true
        );
        needToCaptureLexicalThis ***REMOVED*** languageVersion < 2 /* ES2015 */;
      }
      if (container && hasSyntacticModifier(container, 1024 /* Async */)) inAsyncFunction ***REMOVED*** true;
    }
    let nodeCheckFlag ***REMOVED*** 0;
    if (!container || !isLegalUsageOfSuperExpression(container)) {
      const current ***REMOVED*** findAncestor(node, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** container ? "quit" : n.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */);
      if (current && current.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        error(node, Diagnostics.super_cannot_be_referenced_in_a_computed_property_name);
      } else if (isCallExpression2) {
        error(node, Diagnostics.Super_calls_are_not_permitted_outside_constructors_or_in_nested_functions_inside_constructors);
      } else if (!container || !container.parent || !(isClassLike(container.parent) || container.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */)) {
        error(node, Diagnostics.super_can_only_be_referenced_in_members_of_derived_classes_or_object_literal_expressions);
      } else {
        error(node, Diagnostics.super_property_access_is_permitted_only_in_a_constructor_member_function_or_member_accessor_of_a_derived_class);
      }
      return errorType;
    }
    if (!isCallExpression2 && immediateContainer.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      checkThisBeforeSuper(node, container, Diagnostics.super_must_be_called_before_accessing_a_property_of_super_in_the_constructor_of_a_derived_class);
    }
    if (isStatic(container) || isCallExpression2) {
      nodeCheckFlag ***REMOVED*** 32 /* SuperStatic */;
      if (!isCallExpression2 && languageVersion >***REMOVED*** 2 /* ES2015 */ && languageVersion <***REMOVED*** 8 /* ES2021 */ && (isPropertyDeclaration(container) || isClassStaticBlockDeclaration(container))) {
        forEachEnclosingBlockScopeContainer(node.parent, (current) ***REMOVED***> {
          if (!isSourceFile(current) || isExternalOrCommonJsModule(current)) {
            getNodeLinks(current).flags |***REMOVED*** 2097152 /* ContainsSuperPropertyInStaticInitializer */;
          }
        });
      }
    } else {
      nodeCheckFlag ***REMOVED*** 16 /* SuperInstance */;
    }
    getNodeLinks(node).flags |***REMOVED*** nodeCheckFlag;
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && inAsyncFunction) {
      if (isSuperProperty(node.parent) && isAssignmentTarget(node.parent)) {
        getNodeLinks(container).flags |***REMOVED*** 256 /* MethodWithSuperPropertyAssignmentInAsync */;
      } else {
        getNodeLinks(container).flags |***REMOVED*** 128 /* MethodWithSuperPropertyAccessInAsync */;
      }
    }
    if (needToCaptureLexicalThis) {
      captureLexicalThis(node.parent, container);
    }
    if (container.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
      if (languageVersion < 2 /* ES2015 */) {
        error(node, Diagnostics.super_is_only_allowed_in_members_of_object_literal_expressions_when_option_target_is_ES2015_or_higher);
        return errorType;
      } else {
        return anyType;
      }
    }
    const classLikeDeclaration ***REMOVED*** container.parent;
    if (!getClassExtendsHeritageElement(classLikeDeclaration)) {
      error(node, Diagnostics.super_can_only_be_referenced_in_a_derived_class);
      return errorType;
    }
    if (classDeclarationExtendsNull(classLikeDeclaration)) {
      return isCallExpression2 ? errorType : nullWideningType;
    }
    const classType ***REMOVED*** getDeclaredTypeOfSymbol(getSymbolOfDeclaration(classLikeDeclaration));
    const baseClassType ***REMOVED*** classType && getBaseTypes(classType)[0];
    if (!baseClassType) {
      return errorType;
    }
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ && isInConstructorArgumentInitializer(node, container)) {
      error(node, Diagnostics.super_cannot_be_referenced_in_constructor_arguments);
      return errorType;
    }
    return nodeCheckFlag ***REMOVED******REMOVED******REMOVED*** 32 /* SuperStatic */ ? getBaseConstructorTypeOfClass(classType) : getTypeWithThisArgument(baseClassType, classType.thisType);
    function isLegalUsageOfSuperExpression(container2) {
      if (isCallExpression2) {
        return container2.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */;
      } else {
        if (isClassLike(container2.parent) || container2.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
          if (isStatic(container2)) {
            return container2.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 176 /* ClassStaticBlockDeclaration */;
          } else {
            return container2.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || container2.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */;
          }
        }
      }
      return false;
    }
  }
  function getContainingObjectLiteral(func) {
    return (func.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || func.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || func.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) && func.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ ? func.parent : func.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ && func.parent.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? func.parent.parent : void 0;
  }
  function getThisTypeArgument(type) {
    return getObjectFlags(type) & 4 /* Reference */ && type.target ***REMOVED******REMOVED******REMOVED*** globalThisType ? getTypeArguments(type)[0] : void 0;
  }
  function getThisTypeFromContextualType(type) {
    return mapType(type, (t) ***REMOVED***> {
      return t.flags & 2097152 /* Intersection */ ? forEach(t.types, getThisTypeArgument) : getThisTypeArgument(t);
    });
  }
  function getThisTypeOfObjectLiteralFromContextualType(containingLiteral, contextualType) {
    let literal ***REMOVED*** containingLiteral;
    let type ***REMOVED*** contextualType;
    while (type) {
      const thisType ***REMOVED*** getThisTypeFromContextualType(type);
      if (thisType) {
        return thisType;
      }
      if (literal.parent.kind !***REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
        break;
      }
      literal ***REMOVED*** literal.parent.parent;
      type ***REMOVED*** getApparentTypeOfContextualType(
        literal,
        /*contextFlags*/
        void 0
      );
    }
  }
  function getContextualThisParameterType(func) {
    if (func.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
      return void 0;
    }
    if (isContextSensitiveFunctionOrObjectLiteralMethod(func)) {
      const contextualSignature ***REMOVED*** getContextualSignature(func);
      if (contextualSignature) {
        const thisParameter ***REMOVED*** contextualSignature.thisParameter;
        if (thisParameter) {
          return getTypeOfSymbol(thisParameter);
        }
      }
    }
    const inJs ***REMOVED*** isInJSFile(func);
    if (noImplicitThis || inJs) {
      const containingLiteral ***REMOVED*** getContainingObjectLiteral(func);
      if (containingLiteral) {
        const contextualType ***REMOVED*** getApparentTypeOfContextualType(
          containingLiteral,
          /*contextFlags*/
          void 0
        );
        const thisType ***REMOVED*** getThisTypeOfObjectLiteralFromContextualType(containingLiteral, contextualType);
        if (thisType) {
          return instantiateType(thisType, getMapperFromContext(getInferenceContext(containingLiteral)));
        }
        return getWidenedType(contextualType ? getNonNullableType(contextualType) : checkExpressionCached(containingLiteral));
      }
      const parent ***REMOVED*** walkUpParenthesizedExpressions(func.parent);
      if (isAssignmentExpression(parent)) {
        const target ***REMOVED*** parent.left;
        if (isAccessExpression(target)) {
          const { expression } ***REMOVED*** target;
          if (inJs && isIdentifier(expression)) {
            const sourceFile ***REMOVED*** getSourceFileOfNode(parent);
            if (sourceFile.commonJsModuleIndicator && getResolvedSymbol(expression) ***REMOVED******REMOVED******REMOVED*** sourceFile.symbol) {
              return void 0;
            }
          }
          return getWidenedType(checkExpressionCached(expression));
        }
      }
    }
    return void 0;
  }
  function getContextuallyTypedParameterType(parameter) {
    const func ***REMOVED*** parameter.parent;
    if (!isContextSensitiveFunctionOrObjectLiteralMethod(func)) {
      return void 0;
    }
    const iife ***REMOVED*** getImmediatelyInvokedFunctionExpression(func);
    if (iife && iife.arguments) {
      const args ***REMOVED*** getEffectiveCallArguments(iife);
      const indexOfParameter ***REMOVED*** func.parameters.indexOf(parameter);
      if (parameter.dotDotDotToken) {
        return getSpreadArgumentType(
          args,
          indexOfParameter,
          args.length,
          anyType,
          /*context*/
          void 0,
          0 /* Normal */
        );
      }
      const links ***REMOVED*** getNodeLinks(iife);
      const cached ***REMOVED*** links.resolvedSignature;
      links.resolvedSignature ***REMOVED*** anySignature;
      const type ***REMOVED*** indexOfParameter < args.length ? getWidenedLiteralType(checkExpression(args[indexOfParameter])) : parameter.initializer ? void 0 : undefinedWideningType;
      links.resolvedSignature ***REMOVED*** cached;
      return type;
    }
    const contextualSignature ***REMOVED*** getContextualSignature(func);
    if (contextualSignature) {
      const index ***REMOVED*** func.parameters.indexOf(parameter) - (getThisParameter(func) ? 1 : 0);
      return parameter.dotDotDotToken && lastOrUndefined(func.parameters) ***REMOVED******REMOVED******REMOVED*** parameter ? getRestTypeAtPosition(contextualSignature, index) : tryGetTypeAtPosition(contextualSignature, index);
    }
  }
  function getContextualTypeForVariableLikeDeclaration(declaration, contextFlags) {
    const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(declaration) || (isInJSFile(declaration) ? tryGetJSDocSatisfiesTypeNode(declaration) : void 0);
    if (typeNode) {
      return getTypeFromTypeNode(typeNode);
    }
    switch (declaration.kind) {
      case 170 /* Parameter */:
        return getContextuallyTypedParameterType(declaration);
      case 209 /* BindingElement */:
        return getContextualTypeForBindingElement(declaration, contextFlags);
      case 173 /* PropertyDeclaration */:
        if (isStatic(declaration)) {
          return getContextualTypeForStaticPropertyDeclaration(declaration, contextFlags);
        }
    }
  }
  function getContextualTypeForBindingElement(declaration, contextFlags) {
    const parent ***REMOVED*** declaration.parent.parent;
    const name ***REMOVED*** declaration.propertyName || declaration.name;
    const parentType ***REMOVED*** getContextualTypeForVariableLikeDeclaration(parent, contextFlags) || parent.kind !***REMOVED******REMOVED*** 209 /* BindingElement */ && parent.initializer && checkDeclarationInitializer(parent, declaration.dotDotDotToken ? 32 /* RestBindingElement */ : 0 /* Normal */);
    if (!parentType || isBindingPattern(name) || isComputedNonLiteralName(name)) return void 0;
    if (parent.name.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */) {
      const index ***REMOVED*** indexOfNode(declaration.parent.elements, declaration);
      if (index < 0) return void 0;
      return getContextualTypeForElementExpression(parentType, index);
    }
    const nameType ***REMOVED*** getLiteralTypeFromPropertyName(name);
    if (isTypeUsableAsPropertyName(nameType)) {
      const text ***REMOVED*** getPropertyNameFromType(nameType);
      return getTypeOfPropertyOfType(parentType, text);
    }
  }
  function getContextualTypeForStaticPropertyDeclaration(declaration, contextFlags) {
    const parentType ***REMOVED*** isExpression(declaration.parent) && getContextualType(declaration.parent, contextFlags);
    if (!parentType) return void 0;
    return getTypeOfPropertyOfContextualType(parentType, getSymbolOfDeclaration(declaration).escapedName);
  }
  function getContextualTypeForInitializerExpression(node, contextFlags) {
    const declaration ***REMOVED*** node.parent;
    if (hasInitializer(declaration) && node ***REMOVED******REMOVED******REMOVED*** declaration.initializer) {
      const result ***REMOVED*** getContextualTypeForVariableLikeDeclaration(declaration, contextFlags);
      if (result) {
        return result;
      }
      if (!(contextFlags & 8 /* SkipBindingPatterns */) && isBindingPattern(declaration.name) && declaration.name.elements.length > 0) {
        return getTypeFromBindingPattern(
          declaration.name,
          /*includePatternInType*/
          true,
          /*reportErrors*/
          false
        );
      }
    }
    return void 0;
  }
  function getContextualTypeForReturnExpression(node, contextFlags) {
    const func ***REMOVED*** getContainingFunction(node);
    if (func) {
      let contextualReturnType ***REMOVED*** getContextualReturnType(func, contextFlags);
      if (contextualReturnType) {
        const functionFlags ***REMOVED*** getFunctionFlags(func);
        if (functionFlags & 1 /* Generator */) {
          const isAsyncGenerator ***REMOVED*** (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0;
          if (contextualReturnType.flags & 1048576 /* Union */) {
            contextualReturnType ***REMOVED*** filterType(contextualReturnType, (type) ***REMOVED***> !!getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, type, isAsyncGenerator));
          }
          const iterationReturnType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, contextualReturnType, (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0);
          if (!iterationReturnType) {
            return void 0;
          }
          contextualReturnType ***REMOVED*** iterationReturnType;
        }
        if (functionFlags & 2 /* Async */) {
          const contextualAwaitedType ***REMOVED*** mapType(contextualReturnType, getAwaitedTypeNoAlias);
          return contextualAwaitedType && getUnionType([contextualAwaitedType, createPromiseLikeType(contextualAwaitedType)]);
        }
        return contextualReturnType;
      }
    }
    return void 0;
  }
  function getContextualTypeForAwaitOperand(node, contextFlags) {
    const contextualType ***REMOVED*** getContextualType(node, contextFlags);
    if (contextualType) {
      const contextualAwaitedType ***REMOVED*** getAwaitedTypeNoAlias(contextualType);
      return contextualAwaitedType && getUnionType([contextualAwaitedType, createPromiseLikeType(contextualAwaitedType)]);
    }
    return void 0;
  }
  function getContextualTypeForYieldOperand(node, contextFlags) {
    const func ***REMOVED*** getContainingFunction(node);
    if (func) {
      const functionFlags ***REMOVED*** getFunctionFlags(func);
      let contextualReturnType ***REMOVED*** getContextualReturnType(func, contextFlags);
      if (contextualReturnType) {
        const isAsyncGenerator ***REMOVED*** (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0;
        if (!node.asteriskToken && contextualReturnType.flags & 1048576 /* Union */) {
          contextualReturnType ***REMOVED*** filterType(contextualReturnType, (type) ***REMOVED***> !!getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, type, isAsyncGenerator));
        }
        if (node.asteriskToken) {
          const iterationTypes ***REMOVED*** getIterationTypesOfGeneratorFunctionReturnType(contextualReturnType, isAsyncGenerator);
          const yieldType ***REMOVED*** (iterationTypes ***REMOVED******REMOVED*** null ? void 0 : iterationTypes.yieldType) ?? silentNeverType;
          const returnType ***REMOVED*** getContextualType(node, contextFlags) ?? silentNeverType;
          const nextType ***REMOVED*** (iterationTypes ***REMOVED******REMOVED*** null ? void 0 : iterationTypes.nextType) ?? unknownType;
          const generatorType ***REMOVED*** createGeneratorType(
            yieldType,
            returnType,
            nextType,
            /*isAsyncGenerator*/
            false
          );
          if (isAsyncGenerator) {
            const asyncGeneratorType ***REMOVED*** createGeneratorType(
              yieldType,
              returnType,
              nextType,
              /*isAsyncGenerator*/
              true
            );
            return getUnionType([generatorType, asyncGeneratorType]);
          }
          return generatorType;
        }
        return getIterationTypeOfGeneratorFunctionReturnType(0 /* Yield */, contextualReturnType, isAsyncGenerator);
      }
    }
    return void 0;
  }
  function isInParameterInitializerBeforeContainingFunction(node) {
    let inBindingInitializer ***REMOVED*** false;
    while (node.parent && !isFunctionLike(node.parent)) {
      if (isParameter(node.parent) && (inBindingInitializer || node.parent.initializer ***REMOVED******REMOVED******REMOVED*** node)) {
        return true;
      }
      if (isBindingElement(node.parent) && node.parent.initializer ***REMOVED******REMOVED******REMOVED*** node) {
        inBindingInitializer ***REMOVED*** true;
      }
      node ***REMOVED*** node.parent;
    }
    return false;
  }
  function getContextualIterationType(kind, functionDecl) {
    const isAsync ***REMOVED*** !!(getFunctionFlags(functionDecl) & 2 /* Async */);
    const contextualReturnType ***REMOVED*** getContextualReturnType(
      functionDecl,
      /*contextFlags*/
      void 0
    );
    if (contextualReturnType) {
      return getIterationTypeOfGeneratorFunctionReturnType(kind, contextualReturnType, isAsync) || void 0;
    }
    return void 0;
  }
  function getContextualReturnType(functionDecl, contextFlags) {
    const returnType ***REMOVED*** getReturnTypeFromAnnotation(functionDecl);
    if (returnType) {
      return returnType;
    }
    const signature ***REMOVED*** getContextualSignatureForFunctionLikeDeclaration(functionDecl);
    if (signature && !isResolvingReturnTypeOfSignature(signature)) {
      const returnType2 ***REMOVED*** getReturnTypeOfSignature(signature);
      const functionFlags ***REMOVED*** getFunctionFlags(functionDecl);
      if (functionFlags & 1 /* Generator */) {
        return filterType(returnType2, (t) ***REMOVED***> {
          return !!(t.flags & (3 /* AnyOrUnknown */ | 16384 /* Void */ | 58982400 /* InstantiableNonPrimitive */)) || checkGeneratorInstantiationAssignabilityToReturnType(
            t,
            functionFlags,
            /*errorNode*/
            void 0
          );
        });
      }
      if (functionFlags & 2 /* Async */) {
        return filterType(returnType2, (t) ***REMOVED***> {
          return !!(t.flags & (3 /* AnyOrUnknown */ | 16384 /* Void */ | 58982400 /* InstantiableNonPrimitive */)) || !!getAwaitedTypeOfPromise(t);
        });
      }
      return returnType2;
    }
    const iife ***REMOVED*** getImmediatelyInvokedFunctionExpression(functionDecl);
    if (iife) {
      return getContextualType(iife, contextFlags);
    }
    return void 0;
  }
  function getContextualTypeForArgument(callTarget, arg) {
    const args ***REMOVED*** getEffectiveCallArguments(callTarget);
    const argIndex ***REMOVED*** args.indexOf(arg);
    return argIndex ***REMOVED******REMOVED******REMOVED*** -1 ? void 0 : getContextualTypeForArgumentAtIndex(callTarget, argIndex);
  }
  function getContextualTypeForArgumentAtIndex(callTarget, argIndex) {
    if (isImportCall(callTarget)) {
      return argIndex ***REMOVED******REMOVED******REMOVED*** 0 ? stringType : argIndex ***REMOVED******REMOVED******REMOVED*** 1 ? getGlobalImportCallOptionsType(
        /*reportErrors*/
        false
      ) : anyType;
    }
    const signature ***REMOVED*** getNodeLinks(callTarget).resolvedSignature ***REMOVED******REMOVED******REMOVED*** resolvingSignature ? resolvingSignature : getResolvedSignature(callTarget);
    if (isJsxOpeningLikeElement(callTarget) && argIndex ***REMOVED******REMOVED******REMOVED*** 0) {
      return getEffectiveFirstArgumentForJsxSignature(signature, callTarget);
    }
    const restIndex ***REMOVED*** signature.parameters.length - 1;
    return signatureHasRestParameter(signature) && argIndex >***REMOVED*** restIndex ? getIndexedAccessType(getTypeOfSymbol(signature.parameters[restIndex]), getNumberLiteralType(argIndex - restIndex), 256 /* Contextual */) : getTypeAtPosition(signature, argIndex);
  }
  function getContextualTypeForDecorator(decorator) {
    const signature ***REMOVED*** getDecoratorCallSignature(decorator);
    return signature ? getOrCreateTypeFromSignature(signature) : void 0;
  }
  function getContextualTypeForSubstitutionExpression(template, substitutionExpression) {
    if (template.parent.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */) {
      return getContextualTypeForArgument(template.parent, substitutionExpression);
    }
    return void 0;
  }
  function getContextualTypeForBinaryOperand(node, contextFlags) {
    const binaryExpression ***REMOVED*** node.parent;
    const { left, operatorToken, right } ***REMOVED*** binaryExpression;
    switch (operatorToken.kind) {
      case 64 /* EqualsToken */:
      case 77 /* AmpersandAmpersandEqualsToken */:
      case 76 /* BarBarEqualsToken */:
      case 78 /* QuestionQuestionEqualsToken */:
        return node ***REMOVED******REMOVED******REMOVED*** right ? getContextualTypeForAssignmentDeclaration(binaryExpression) : void 0;
      case 57 /* BarBarToken */:
      case 61 /* QuestionQuestionToken */:
        const type ***REMOVED*** getContextualType(binaryExpression, contextFlags);
        return node ***REMOVED******REMOVED******REMOVED*** right && (type && type.pattern || !type && !isDefaultedExpandoInitializer(binaryExpression)) ? getTypeOfExpression(left) : type;
      case 56 /* AmpersandAmpersandToken */:
      case 28 /* CommaToken */:
        return node ***REMOVED******REMOVED******REMOVED*** right ? getContextualType(binaryExpression, contextFlags) : void 0;
      default:
        return void 0;
    }
  }
  function getSymbolForExpression(e) {
    if (canHaveSymbol(e) && e.symbol) {
      return e.symbol;
    }
    if (isIdentifier(e)) {
      return getResolvedSymbol(e);
    }
    if (isPropertyAccessExpression(e)) {
      const lhsType ***REMOVED*** getTypeOfExpression(e.expression);
      return isPrivateIdentifier(e.name) ? tryGetPrivateIdentifierPropertyOfType(lhsType, e.name) : getPropertyOfType(lhsType, e.name.escapedText);
    }
    if (isElementAccessExpression(e)) {
      const propType ***REMOVED*** checkExpressionCached(e.argumentExpression);
      if (!isTypeUsableAsPropertyName(propType)) {
        return void 0;
      }
      const lhsType ***REMOVED*** getTypeOfExpression(e.expression);
      return getPropertyOfType(lhsType, getPropertyNameFromType(propType));
    }
    return void 0;
    function tryGetPrivateIdentifierPropertyOfType(type, id) {
      const lexicallyScopedSymbol ***REMOVED*** lookupSymbolForPrivateIdentifierDeclaration(id.escapedText, id);
      return lexicallyScopedSymbol && getPrivateIdentifierPropertyOfType(type, lexicallyScopedSymbol);
    }
  }
  function getContextualTypeForAssignmentDeclaration(binaryExpression) {
    var _a, _b;
    const kind ***REMOVED*** getAssignmentDeclarationKind(binaryExpression);
    switch (kind) {
      case 0 /* None */:
      case 4 /* ThisProperty */:
        const lhsSymbol ***REMOVED*** getSymbolForExpression(binaryExpression.left);
        const decl ***REMOVED*** lhsSymbol && lhsSymbol.valueDeclaration;
        if (decl && (isPropertyDeclaration(decl) || isPropertySignature(decl))) {
          const overallAnnotation ***REMOVED*** getEffectiveTypeAnnotationNode(decl);
          return overallAnnotation && instantiateType(getTypeFromTypeNode(overallAnnotation), getSymbolLinks(lhsSymbol).mapper) || (isPropertyDeclaration(decl) ? decl.initializer && getTypeOfExpression(binaryExpression.left) : void 0);
        }
        if (kind ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
          return getTypeOfExpression(binaryExpression.left);
        }
        return getContextualTypeForThisPropertyAssignment(binaryExpression);
      case 5 /* Property */:
        if (isPossiblyAliasedThisProperty(binaryExpression, kind)) {
          return getContextualTypeForThisPropertyAssignment(binaryExpression);
        } else if (!canHaveSymbol(binaryExpression.left) || !binaryExpression.left.symbol) {
          return getTypeOfExpression(binaryExpression.left);
        } else {
          const decl2 ***REMOVED*** binaryExpression.left.symbol.valueDeclaration;
          if (!decl2) {
            return void 0;
          }
          const lhs ***REMOVED*** cast(binaryExpression.left, isAccessExpression);
          const overallAnnotation ***REMOVED*** getEffectiveTypeAnnotationNode(decl2);
          if (overallAnnotation) {
            return getTypeFromTypeNode(overallAnnotation);
          } else if (isIdentifier(lhs.expression)) {
            const id ***REMOVED*** lhs.expression;
            const parentSymbol ***REMOVED*** resolveName(
              id,
              id.escapedText,
              111551 /* Value */,
              /*nameNotFoundMessage*/
              void 0,
              /*isUse*/
              true
            );
            if (parentSymbol) {
              const annotated2 ***REMOVED*** parentSymbol.valueDeclaration && getEffectiveTypeAnnotationNode(parentSymbol.valueDeclaration);
              if (annotated2) {
                const nameStr ***REMOVED*** getElementOrPropertyAccessName(lhs);
                if (nameStr !***REMOVED******REMOVED*** void 0) {
                  return getTypeOfPropertyOfContextualType(getTypeFromTypeNode(annotated2), nameStr);
                }
              }
              return void 0;
            }
          }
          return isInJSFile(decl2) || decl2 ***REMOVED******REMOVED******REMOVED*** binaryExpression.left ? void 0 : getTypeOfExpression(binaryExpression.left);
        }
      case 1 /* ExportsProperty */:
      case 6 /* Prototype */:
      case 3 /* PrototypeProperty */:
      case 2 /* ModuleExports */:
        let valueDeclaration;
        if (kind !***REMOVED******REMOVED*** 2 /* ModuleExports */) {
          valueDeclaration ***REMOVED*** canHaveSymbol(binaryExpression.left) ? (_a ***REMOVED*** binaryExpression.left.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.valueDeclaration : void 0;
        }
        valueDeclaration || (valueDeclaration ***REMOVED*** (_b ***REMOVED*** binaryExpression.symbol) ***REMOVED******REMOVED*** null ? void 0 : _b.valueDeclaration);
        const annotated ***REMOVED*** valueDeclaration && getEffectiveTypeAnnotationNode(valueDeclaration);
        return annotated ? getTypeFromTypeNode(annotated) : void 0;
      case 7 /* ObjectDefinePropertyValue */:
      case 8 /* ObjectDefinePropertyExports */:
      case 9 /* ObjectDefinePrototypeProperty */:
        return Debug.fail("Does not apply");
      default:
        return Debug.assertNever(kind);
    }
  }
  function isPossiblyAliasedThisProperty(declaration, kind ***REMOVED*** getAssignmentDeclarationKind(declaration)) {
    if (kind ***REMOVED******REMOVED******REMOVED*** 4 /* ThisProperty */) {
      return true;
    }
    if (!isInJSFile(declaration) || kind !***REMOVED******REMOVED*** 5 /* Property */ || !isIdentifier(declaration.left.expression)) {
      return false;
    }
    const name ***REMOVED*** declaration.left.expression.escapedText;
    const symbol ***REMOVED*** resolveName(
      declaration.left,
      name,
      111551 /* Value */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true,
      /*excludeGlobals*/
      true
    );
    return isThisInitializedDeclaration(symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.valueDeclaration);
  }
  function getContextualTypeForThisPropertyAssignment(binaryExpression) {
    if (!binaryExpression.symbol) return getTypeOfExpression(binaryExpression.left);
    if (binaryExpression.symbol.valueDeclaration) {
      const annotated ***REMOVED*** getEffectiveTypeAnnotationNode(binaryExpression.symbol.valueDeclaration);
      if (annotated) {
        const type ***REMOVED*** getTypeFromTypeNode(annotated);
        if (type) {
          return type;
        }
      }
    }
    const thisAccess ***REMOVED*** cast(binaryExpression.left, isAccessExpression);
    if (!isObjectLiteralMethod(getThisContainer(
      thisAccess.expression,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    ))) {
      return void 0;
    }
    const thisType ***REMOVED*** checkThisExpression(thisAccess.expression);
    const nameStr ***REMOVED*** getElementOrPropertyAccessName(thisAccess);
    return nameStr !***REMOVED******REMOVED*** void 0 && getTypeOfPropertyOfContextualType(thisType, nameStr) || void 0;
  }
  function isCircularMappedProperty(symbol) {
    return !!(getCheckFlags(symbol) & 262144 /* Mapped */ && !symbol.links.type && findResolutionCycleStartIndex(symbol, 0 /* Type */) >***REMOVED*** 0);
  }
  function isExcludedMappedPropertyName(constraint, propertyNameType) {
    if (constraint.flags & 16777216 /* Conditional */) {
      const type ***REMOVED*** constraint;
      return !!(getReducedType(getTrueTypeFromConditionalType(type)).flags & 131072 /* Never */) && getActualTypeVariable(getFalseTypeFromConditionalType(type)) ***REMOVED******REMOVED******REMOVED*** getActualTypeVariable(type.checkType) && isTypeAssignableTo(propertyNameType, type.extendsType);
    }
    if (constraint.flags & 2097152 /* Intersection */) {
      return some(constraint.types, (t) ***REMOVED***> isExcludedMappedPropertyName(t, propertyNameType));
    }
    return false;
  }
  function getTypeOfPropertyOfContextualType(type, name, nameType) {
    return mapType(
      type,
      (t) ***REMOVED***> {
        if (t.flags & 2097152 /* Intersection */) {
          let types;
          let indexInfoCandidates;
          let ignoreIndexInfos ***REMOVED*** false;
          for (const constituentType of t.types) {
            if (!(constituentType.flags & 524288 /* Object */)) {
              continue;
            }
            if (isGenericMappedType(constituentType) && getMappedTypeNameTypeKind(constituentType) !***REMOVED******REMOVED*** 2 /* Remapping */) {
              const substitutedType ***REMOVED*** getIndexedMappedTypeSubstitutedTypeOfContextualType(constituentType, name, nameType);
              types ***REMOVED*** appendContextualPropertyTypeConstituent(types, substitutedType);
              continue;
            }
            const propertyType ***REMOVED*** getTypeOfConcretePropertyOfContextualType(constituentType, name);
            if (!propertyType) {
              if (!ignoreIndexInfos) {
                indexInfoCandidates ***REMOVED*** append(indexInfoCandidates, constituentType);
              }
              continue;
            }
            ignoreIndexInfos ***REMOVED*** true;
            indexInfoCandidates ***REMOVED*** void 0;
            types ***REMOVED*** appendContextualPropertyTypeConstituent(types, propertyType);
          }
          if (indexInfoCandidates) {
            for (const candidate of indexInfoCandidates) {
              const indexInfoType ***REMOVED*** getTypeFromIndexInfosOfContextualType(candidate, name, nameType);
              types ***REMOVED*** appendContextualPropertyTypeConstituent(types, indexInfoType);
            }
          }
          if (!types) {
            return;
          }
          if (types.length ***REMOVED******REMOVED******REMOVED*** 1) {
            return types[0];
          }
          return getIntersectionType(types);
        }
        if (!(t.flags & 524288 /* Object */)) {
          return;
        }
        return isGenericMappedType(t) && getMappedTypeNameTypeKind(t) !***REMOVED******REMOVED*** 2 /* Remapping */ ? getIndexedMappedTypeSubstitutedTypeOfContextualType(t, name, nameType) : getTypeOfConcretePropertyOfContextualType(t, name) ?? getTypeFromIndexInfosOfContextualType(t, name, nameType);
      },
      /*noReductions*/
      true
    );
  }
  function appendContextualPropertyTypeConstituent(types, type) {
    return type ? append(types, type.flags & 1 /* Any */ ? unknownType : type) : types;
  }
  function getIndexedMappedTypeSubstitutedTypeOfContextualType(type, name, nameType) {
    const propertyNameType ***REMOVED*** nameType || getStringLiteralType(unescapeLeadingUnderscores(name));
    const constraint ***REMOVED*** getConstraintTypeFromMappedType(type);
    if (type.nameType && isExcludedMappedPropertyName(type.nameType, propertyNameType) || isExcludedMappedPropertyName(constraint, propertyNameType)) {
      return;
    }
    const constraintOfConstraint ***REMOVED*** getBaseConstraintOfType(constraint) || constraint;
    if (!isTypeAssignableTo(propertyNameType, constraintOfConstraint)) {
      return;
    }
    return substituteIndexedMappedType(type, propertyNameType);
  }
  function getTypeOfConcretePropertyOfContextualType(type, name) {
    const prop ***REMOVED*** getPropertyOfType(type, name);
    if (!prop || isCircularMappedProperty(prop)) {
      return;
    }
    return removeMissingType(getTypeOfSymbol(prop), !!(prop.flags & 16777216 /* Optional */));
  }
  function getTypeFromIndexInfosOfContextualType(type, name, nameType) {
    var _a;
    if (isTupleType(type) && isNumericLiteralName(name) && +name >***REMOVED*** 0) {
      const restType ***REMOVED*** getElementTypeOfSliceOfTupleType(
        type,
        type.target.fixedLength,
        /*endSkipCount*/
        0,
        /*writing*/
        false,
        /*noReductions*/
        true
      );
      if (restType) {
        return restType;
      }
    }
    return (_a ***REMOVED*** findApplicableIndexInfo(getIndexInfosOfStructuredType(type), nameType || getStringLiteralType(unescapeLeadingUnderscores(name)))) ***REMOVED******REMOVED*** null ? void 0 : _a.type;
  }
  function getContextualTypeForObjectLiteralMethod(node, contextFlags) {
    Debug.assert(isObjectLiteralMethod(node));
    if (node.flags & 67108864 /* InWithStatement */) {
      return void 0;
    }
    return getContextualTypeForObjectLiteralElement(node, contextFlags);
  }
  function getContextualTypeForObjectLiteralElement(element, contextFlags) {
    const objectLiteral ***REMOVED*** element.parent;
    const propertyAssignmentType ***REMOVED*** isPropertyAssignment(element) && getContextualTypeForVariableLikeDeclaration(element, contextFlags);
    if (propertyAssignmentType) {
      return propertyAssignmentType;
    }
    const type ***REMOVED*** getApparentTypeOfContextualType(objectLiteral, contextFlags);
    if (type) {
      if (hasBindableName(element)) {
        const symbol ***REMOVED*** getSymbolOfDeclaration(element);
        return getTypeOfPropertyOfContextualType(type, symbol.escapedName, getSymbolLinks(symbol).nameType);
      }
      if (hasDynamicName(element)) {
        const name ***REMOVED*** getNameOfDeclaration(element);
        if (name && isComputedPropertyName(name)) {
          const exprType ***REMOVED*** checkExpression(name.expression);
          const propType ***REMOVED*** isTypeUsableAsPropertyName(exprType) && getTypeOfPropertyOfContextualType(type, getPropertyNameFromType(exprType));
          if (propType) {
            return propType;
          }
        }
      }
      if (element.name) {
        const nameType ***REMOVED*** getLiteralTypeFromPropertyName(element.name);
        return mapType(
          type,
          (t) ***REMOVED***> {
            var _a;
            return (_a ***REMOVED*** findApplicableIndexInfo(getIndexInfosOfStructuredType(t), nameType)) ***REMOVED******REMOVED*** null ? void 0 : _a.type;
          },
          /*noReductions*/
          true
        );
      }
    }
    return void 0;
  }
  function getSpreadIndices(elements) {
    let first2, last2;
    for (let i ***REMOVED*** 0; i < elements.length; i++) {
      if (isSpreadElement(elements[i])) {
        first2 ?? (first2 ***REMOVED*** i);
        last2 ***REMOVED*** i;
      }
    }
    return { first: first2, last: last2 };
  }
  function getContextualTypeForElementExpression(type, index, length2, firstSpreadIndex, lastSpreadIndex) {
    return type && mapType(
      type,
      (t) ***REMOVED***> {
        if (isTupleType(t)) {
          if ((firstSpreadIndex ***REMOVED******REMOVED******REMOVED*** void 0 || index < firstSpreadIndex) && index < t.target.fixedLength) {
            return removeMissingType(getTypeArguments(t)[index], !!(t.target.elementFlags[index] && 2 /* Optional */));
          }
          const offset ***REMOVED*** length2 !***REMOVED******REMOVED*** void 0 && (lastSpreadIndex ***REMOVED******REMOVED******REMOVED*** void 0 || index > lastSpreadIndex) ? length2 - index : 0;
          const fixedEndLength ***REMOVED*** offset > 0 && t.target.combinedFlags & 12 /* Variable */ ? getEndElementCount(t.target, 3 /* Fixed */) : 0;
          if (offset > 0 && offset <***REMOVED*** fixedEndLength) {
            return getTypeArguments(t)[getTypeReferenceArity(t) - offset];
          }
          return getElementTypeOfSliceOfTupleType(
            t,
            firstSpreadIndex ***REMOVED******REMOVED******REMOVED*** void 0 ? t.target.fixedLength : Math.min(t.target.fixedLength, firstSpreadIndex),
            length2 ***REMOVED******REMOVED******REMOVED*** void 0 || lastSpreadIndex ***REMOVED******REMOVED******REMOVED*** void 0 ? fixedEndLength : Math.min(fixedEndLength, length2 - lastSpreadIndex),
            /*writing*/
            false,
            /*noReductions*/
            true
          );
        }
        return (!firstSpreadIndex || index < firstSpreadIndex) && getTypeOfPropertyOfContextualType(t, "" + index) || getIteratedTypeOrElementType(
          1 /* Element */,
          t,
          undefinedType,
          /*errorNode*/
          void 0,
          /*checkAssignability*/
          false
        );
      },
      /*noReductions*/
      true
    );
  }
  function getContextualTypeForConditionalOperand(node, contextFlags) {
    const conditional ***REMOVED*** node.parent;
    return node ***REMOVED******REMOVED******REMOVED*** conditional.whenTrue || node ***REMOVED******REMOVED******REMOVED*** conditional.whenFalse ? getContextualType(conditional, contextFlags) : void 0;
  }
  function getContextualTypeForChildJsxExpression(node, child, contextFlags) {
    const attributesType ***REMOVED*** getApparentTypeOfContextualType(node.openingElement.attributes, contextFlags);
    const jsxChildrenPropertyName ***REMOVED*** getJsxElementChildrenPropertyName(getJsxNamespaceAt(node));
    if (!(attributesType && !isTypeAny(attributesType) && jsxChildrenPropertyName && jsxChildrenPropertyName !***REMOVED******REMOVED*** "")) {
      return void 0;
    }
    const realChildren ***REMOVED*** getSemanticJsxChildren(node.children);
    const childIndex ***REMOVED*** realChildren.indexOf(child);
    const childFieldType ***REMOVED*** getTypeOfPropertyOfContextualType(attributesType, jsxChildrenPropertyName);
    return childFieldType && (realChildren.length ***REMOVED******REMOVED******REMOVED*** 1 ? childFieldType : mapType(
      childFieldType,
      (t) ***REMOVED***> {
        if (isArrayLikeType(t)) {
          return getIndexedAccessType(t, getNumberLiteralType(childIndex));
        } else {
          return t;
        }
      },
      /*noReductions*/
      true
    ));
  }
  function getContextualTypeForJsxExpression(node, contextFlags) {
    const exprParent ***REMOVED*** node.parent;
    return isJsxAttributeLike(exprParent) ? getContextualType(node, contextFlags) : isJsxElement(exprParent) ? getContextualTypeForChildJsxExpression(exprParent, node, contextFlags) : void 0;
  }
  function getContextualTypeForJsxAttribute(attribute, contextFlags) {
    if (isJsxAttribute(attribute)) {
      const attributesType ***REMOVED*** getApparentTypeOfContextualType(attribute.parent, contextFlags);
      if (!attributesType || isTypeAny(attributesType)) {
        return void 0;
      }
      return getTypeOfPropertyOfContextualType(attributesType, getEscapedTextOfJsxAttributeName(attribute.name));
    } else {
      return getContextualType(attribute.parent, contextFlags);
    }
  }
  function isPossiblyDiscriminantValue(node) {
    switch (node.kind) {
      case 11 /* StringLiteral */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 229 /* TemplateExpression */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 106 /* NullKeyword */:
      case 80 /* Identifier */:
      case 157 /* UndefinedKeyword */:
        return true;
      case 212 /* PropertyAccessExpression */:
      case 218 /* ParenthesizedExpression */:
        return isPossiblyDiscriminantValue(node.expression);
      case 295 /* JsxExpression */:
        return !node.expression || isPossiblyDiscriminantValue(node.expression);
    }
    return false;
  }
  function discriminateContextualTypeByObjectMembers(node, contextualType) {
    const key ***REMOVED*** `D${getNodeId(node)},${getTypeId(contextualType)}`;
    return getCachedType(key) ?? setCachedType(
      key,
      getMatchingUnionConstituentForObjectLiteral(contextualType, node) ?? discriminateTypeByDiscriminableItems(
        contextualType,
        concatenate(
          map(
            filter(node.properties, (p) ***REMOVED***> {
              if (!p.symbol) {
                return false;
              }
              if (p.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
                return isPossiblyDiscriminantValue(p.initializer) && isDiscriminantProperty(contextualType, p.symbol.escapedName);
              }
              if (p.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
                return isDiscriminantProperty(contextualType, p.symbol.escapedName);
              }
              return false;
            }),
            (prop) ***REMOVED***> [() ***REMOVED***> getContextFreeTypeOfExpression(prop.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? prop.initializer : prop.name), prop.symbol.escapedName]
          ),
          map(
            filter(getPropertiesOfType(contextualType), (s) ***REMOVED***> {
              var _a;
              return !!(s.flags & 16777216 /* Optional */) && !!((_a ***REMOVED*** node ***REMOVED******REMOVED*** null ? void 0 : node.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.members) && !node.symbol.members.has(s.escapedName) && isDiscriminantProperty(contextualType, s.escapedName);
            }),
            (s) ***REMOVED***> [() ***REMOVED***> undefinedType, s.escapedName]
          )
        ),
        isTypeAssignableTo
      )
    );
  }
  function discriminateContextualTypeByJSXAttributes(node, contextualType) {
    const key ***REMOVED*** `D${getNodeId(node)},${getTypeId(contextualType)}`;
    const cached ***REMOVED*** getCachedType(key);
    if (cached) return cached;
    const jsxChildrenPropertyName ***REMOVED*** getJsxElementChildrenPropertyName(getJsxNamespaceAt(node));
    return setCachedType(
      key,
      discriminateTypeByDiscriminableItems(
        contextualType,
        concatenate(
          map(
            filter(node.properties, (p) ***REMOVED***> !!p.symbol && p.kind ***REMOVED******REMOVED******REMOVED*** 292 /* JsxAttribute */ && isDiscriminantProperty(contextualType, p.symbol.escapedName) && (!p.initializer || isPossiblyDiscriminantValue(p.initializer))),
            (prop) ***REMOVED***> [!prop.initializer ? () ***REMOVED***> trueType : () ***REMOVED***> getContextFreeTypeOfExpression(prop.initializer), prop.symbol.escapedName]
          ),
          map(
            filter(getPropertiesOfType(contextualType), (s) ***REMOVED***> {
              var _a;
              if (!(s.flags & 16777216 /* Optional */) || !((_a ***REMOVED*** node ***REMOVED******REMOVED*** null ? void 0 : node.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.members)) {
                return false;
              }
              const element ***REMOVED*** node.parent.parent;
              if (s.escapedName ***REMOVED******REMOVED******REMOVED*** jsxChildrenPropertyName && isJsxElement(element) && getSemanticJsxChildren(element.children).length) {
                return false;
              }
              return !node.symbol.members.has(s.escapedName) && isDiscriminantProperty(contextualType, s.escapedName);
            }),
            (s) ***REMOVED***> [() ***REMOVED***> undefinedType, s.escapedName]
          )
        ),
        isTypeAssignableTo
      )
    );
  }
  function getApparentTypeOfContextualType(node, contextFlags) {
    const contextualType ***REMOVED*** isObjectLiteralMethod(node) ? getContextualTypeForObjectLiteralMethod(node, contextFlags) : getContextualType(node, contextFlags);
    const instantiatedType ***REMOVED*** instantiateContextualType(contextualType, node, contextFlags);
    if (instantiatedType && !(contextFlags && contextFlags & 2 /* NoConstraints */ && instantiatedType.flags & 8650752 /* TypeVariable */)) {
      const apparentType ***REMOVED*** mapType(
        instantiatedType,
        // When obtaining apparent type of *contextual* type we don't want to get apparent type of mapped types.
        // That would evaluate mapped types with array or tuple type constraints too eagerly
        // and thus it would prevent `getTypeOfPropertyOfContextualType` from obtaining per-position contextual type for elements of array literal expressions.
        // Apparent type of other mapped types is already the mapped type itself so we can just avoid calling `getApparentType` here for all mapped types.
        (t) ***REMOVED***> getObjectFlags(t) & 32 /* Mapped */ ? t : getApparentType(t),
        /*noReductions*/
        true
      );
      return apparentType.flags & 1048576 /* Union */ && isObjectLiteralExpression(node) ? discriminateContextualTypeByObjectMembers(node, apparentType) : apparentType.flags & 1048576 /* Union */ && isJsxAttributes(node) ? discriminateContextualTypeByJSXAttributes(node, apparentType) : apparentType;
    }
  }
  function instantiateContextualType(contextualType, node, contextFlags) {
    if (contextualType && maybeTypeOfKind(contextualType, 465829888 /* Instantiable */)) {
      const inferenceContext ***REMOVED*** getInferenceContext(node);
      if (inferenceContext && contextFlags & 1 /* Signature */ && some(inferenceContext.inferences, hasInferenceCandidatesOrDefault)) {
        return instantiateInstantiableTypes(contextualType, inferenceContext.nonFixingMapper);
      }
      if (inferenceContext ***REMOVED******REMOVED*** null ? void 0 : inferenceContext.returnMapper) {
        const type ***REMOVED*** instantiateInstantiableTypes(contextualType, inferenceContext.returnMapper);
        return type.flags & 1048576 /* Union */ && containsType(type.types, regularFalseType) && containsType(type.types, regularTrueType) ? filterType(type, (t) ***REMOVED***> t !***REMOVED******REMOVED*** regularFalseType && t !***REMOVED******REMOVED*** regularTrueType) : type;
      }
    }
    return contextualType;
  }
  function instantiateInstantiableTypes(type, mapper) {
    if (type.flags & 465829888 /* Instantiable */) {
      return instantiateType(type, mapper);
    }
    if (type.flags & 1048576 /* Union */) {
      return getUnionType(map(type.types, (t) ***REMOVED***> instantiateInstantiableTypes(t, mapper)), 0 /* None */);
    }
    if (type.flags & 2097152 /* Intersection */) {
      return getIntersectionType(map(type.types, (t) ***REMOVED***> instantiateInstantiableTypes(t, mapper)));
    }
    return type;
  }
  function getContextualType(node, contextFlags) {
    var _a;
    if (node.flags & 67108864 /* InWithStatement */) {
      return void 0;
    }
    const index ***REMOVED*** findContextualNode(
      node,
      /*includeCaches*/
      !contextFlags
    );
    if (index >***REMOVED*** 0) {
      return contextualTypes[index];
    }
    const { parent } ***REMOVED*** node;
    switch (parent.kind) {
      case 261 /* VariableDeclaration */:
      case 170 /* Parameter */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 209 /* BindingElement */:
        return getContextualTypeForInitializerExpression(node, contextFlags);
      case 220 /* ArrowFunction */:
      case 254 /* ReturnStatement */:
        return getContextualTypeForReturnExpression(node, contextFlags);
      case 230 /* YieldExpression */:
        return getContextualTypeForYieldOperand(parent, contextFlags);
      case 224 /* AwaitExpression */:
        return getContextualTypeForAwaitOperand(parent, contextFlags);
      case 214 /* CallExpression */:
      case 215 /* NewExpression */:
        return getContextualTypeForArgument(parent, node);
      case 171 /* Decorator */:
        return getContextualTypeForDecorator(parent);
      case 217 /* TypeAssertionExpression */:
      case 235 /* AsExpression */:
        return isConstTypeReference(parent.type) ? getContextualType(parent, contextFlags) : getTypeFromTypeNode(parent.type);
      case 227 /* BinaryExpression */:
        return getContextualTypeForBinaryOperand(node, contextFlags);
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
        return getContextualTypeForObjectLiteralElement(parent, contextFlags);
      case 306 /* SpreadAssignment */:
        return getContextualType(parent.parent, contextFlags);
      case 210 /* ArrayLiteralExpression */: {
        const arrayLiteral ***REMOVED*** parent;
        const type ***REMOVED*** getApparentTypeOfContextualType(arrayLiteral, contextFlags);
        const elementIndex ***REMOVED*** indexOfNode(arrayLiteral.elements, node);
        const spreadIndices ***REMOVED*** (_a ***REMOVED*** getNodeLinks(arrayLiteral)).spreadIndices ?? (_a.spreadIndices ***REMOVED*** getSpreadIndices(arrayLiteral.elements));
        return getContextualTypeForElementExpression(type, elementIndex, arrayLiteral.elements.length, spreadIndices.first, spreadIndices.last);
      }
      case 228 /* ConditionalExpression */:
        return getContextualTypeForConditionalOperand(node, contextFlags);
      case 240 /* TemplateSpan */:
        Debug.assert(parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 229 /* TemplateExpression */);
        return getContextualTypeForSubstitutionExpression(parent.parent, node);
      case 218 /* ParenthesizedExpression */: {
        if (isInJSFile(parent)) {
          if (isJSDocSatisfiesExpression(parent)) {
            return getTypeFromTypeNode(getJSDocSatisfiesExpressionType(parent));
          }
          const typeTag ***REMOVED*** getJSDocTypeTag(parent);
          if (typeTag && !isConstTypeReference(typeTag.typeExpression.type)) {
            return getTypeFromTypeNode(typeTag.typeExpression.type);
          }
        }
        return getContextualType(parent, contextFlags);
      }
      case 236 /* NonNullExpression */:
        return getContextualType(parent, contextFlags);
      case 239 /* SatisfiesExpression */:
        return getTypeFromTypeNode(parent.type);
      case 278 /* ExportAssignment */:
        return tryGetTypeFromEffectiveTypeNode(parent);
      case 295 /* JsxExpression */:
        return getContextualTypeForJsxExpression(parent, contextFlags);
      case 292 /* JsxAttribute */:
      case 294 /* JsxSpreadAttribute */:
        return getContextualTypeForJsxAttribute(parent, contextFlags);
      case 287 /* JsxOpeningElement */:
      case 286 /* JsxSelfClosingElement */:
        return getContextualJsxElementAttributesType(parent, contextFlags);
      case 302 /* ImportAttribute */:
        return getContextualImportAttributeType(parent);
    }
    return void 0;
  }
  function pushCachedContextualType(node) {
    pushContextualType(
      node,
      getContextualType(
        node,
        /*contextFlags*/
        void 0
      ),
      /*isCache*/
      true
    );
  }
  function pushContextualType(node, type, isCache) {
    contextualTypeNodes[contextualTypeCount] ***REMOVED*** node;
    contextualTypes[contextualTypeCount] ***REMOVED*** type;
    contextualIsCache[contextualTypeCount] ***REMOVED*** isCache;
    contextualTypeCount++;
  }
  function popContextualType() {
    contextualTypeCount--;
    contextualTypeNodes[contextualTypeCount] ***REMOVED*** void 0;
    contextualTypes[contextualTypeCount] ***REMOVED*** void 0;
    contextualIsCache[contextualTypeCount] ***REMOVED*** void 0;
  }
  function findContextualNode(node, includeCaches) {
    for (let i ***REMOVED*** contextualTypeCount - 1; i >***REMOVED*** 0; i--) {
      if (node ***REMOVED******REMOVED******REMOVED*** contextualTypeNodes[i] && (includeCaches || !contextualIsCache[i])) {
        return i;
      }
    }
    return -1;
  }
  function pushInferenceContext(node, inferenceContext) {
    inferenceContextNodes[inferenceContextCount] ***REMOVED*** node;
    inferenceContexts[inferenceContextCount] ***REMOVED*** inferenceContext;
    inferenceContextCount++;
  }
  function popInferenceContext() {
    inferenceContextCount--;
    inferenceContextNodes[inferenceContextCount] ***REMOVED*** void 0;
    inferenceContexts[inferenceContextCount] ***REMOVED*** void 0;
  }
  function getInferenceContext(node) {
    for (let i ***REMOVED*** inferenceContextCount - 1; i >***REMOVED*** 0; i--) {
      if (isNodeDescendantOf(node, inferenceContextNodes[i])) {
        return inferenceContexts[i];
      }
    }
  }
  function pushActiveMapper(mapper) {
    activeTypeMappers[activeTypeMappersCount] ***REMOVED*** mapper;
    activeTypeMappersCaches[activeTypeMappersCount] ?? (activeTypeMappersCaches[activeTypeMappersCount] ***REMOVED*** /* @__PURE__ */ new Map());
    activeTypeMappersCount++;
  }
  function popActiveMapper() {
    activeTypeMappersCount--;
    activeTypeMappers[activeTypeMappersCount] ***REMOVED*** void 0;
    activeTypeMappersCaches[activeTypeMappersCount].clear();
  }
  function findActiveMapper(mapper) {
    for (let i ***REMOVED*** activeTypeMappersCount - 1; i >***REMOVED*** 0; i--) {
      if (mapper ***REMOVED******REMOVED******REMOVED*** activeTypeMappers[i]) {
        return i;
      }
    }
    return -1;
  }
  function clearActiveMapperCaches() {
    for (let i ***REMOVED*** activeTypeMappersCount - 1; i >***REMOVED*** 0; i--) {
      activeTypeMappersCaches[i].clear();
    }
  }
  function getContextualImportAttributeType(node) {
    return getTypeOfPropertyOfContextualType(getGlobalImportAttributesType(
      /*reportErrors*/
      false
    ), getNameFromImportAttribute(node));
  }
  function getContextualJsxElementAttributesType(node, contextFlags) {
    if (isJsxOpeningElement(node) && contextFlags !***REMOVED******REMOVED*** 4 /* Completions */) {
      const index ***REMOVED*** findContextualNode(
        node.parent,
        /*includeCaches*/
        !contextFlags
      );
      if (index >***REMOVED*** 0) {
        return contextualTypes[index];
      }
    }
    return getContextualTypeForArgumentAtIndex(node, 0);
  }
  function getEffectiveFirstArgumentForJsxSignature(signature, node) {
    return isJsxOpeningFragment(node) || getJsxReferenceKind(node) !***REMOVED******REMOVED*** 0 /* Component */ ? getJsxPropsTypeFromCallSignature(signature, node) : getJsxPropsTypeFromClassType(signature, node);
  }
  function getJsxPropsTypeFromCallSignature(sig, context) {
    let propsType ***REMOVED*** getTypeOfFirstParameterOfSignatureWithFallback(sig, unknownType);
    propsType ***REMOVED*** getJsxManagedAttributesFromLocatedAttributes(context, getJsxNamespaceAt(context), propsType);
    const intrinsicAttribs ***REMOVED*** getJsxType(JsxNames.IntrinsicAttributes, context);
    if (!isErrorType(intrinsicAttribs)) {
      propsType ***REMOVED*** intersectTypes(intrinsicAttribs, propsType);
    }
    return propsType;
  }
  function getJsxPropsTypeForSignatureFromMember(sig, forcedLookupLocation) {
    if (sig.compositeSignatures) {
      const results ***REMOVED*** [];
      for (const signature of sig.compositeSignatures) {
        const instance ***REMOVED*** getReturnTypeOfSignature(signature);
        if (isTypeAny(instance)) {
          return instance;
        }
        const propType ***REMOVED*** getTypeOfPropertyOfType(instance, forcedLookupLocation);
        if (!propType) {
          return;
        }
        results.push(propType);
      }
      return getIntersectionType(results);
    }
    const instanceType ***REMOVED*** getReturnTypeOfSignature(sig);
    return isTypeAny(instanceType) ? instanceType : getTypeOfPropertyOfType(instanceType, forcedLookupLocation);
  }
  function getStaticTypeOfReferencedJsxConstructor(context) {
    if (isJsxOpeningFragment(context)) return getJSXFragmentType(context);
    if (isJsxIntrinsicTagName(context.tagName)) {
      const result ***REMOVED*** getIntrinsicAttributesTypeFromJsxOpeningLikeElement(context);
      const fakeSignature ***REMOVED*** createSignatureForJSXIntrinsic(context, result);
      return getOrCreateTypeFromSignature(fakeSignature);
    }
    const tagType ***REMOVED*** checkExpressionCached(context.tagName);
    if (tagType.flags & 128 /* StringLiteral */) {
      const result ***REMOVED*** getIntrinsicAttributesTypeFromStringLiteralType(tagType, context);
      if (!result) {
        return errorType;
      }
      const fakeSignature ***REMOVED*** createSignatureForJSXIntrinsic(context, result);
      return getOrCreateTypeFromSignature(fakeSignature);
    }
    return tagType;
  }
  function getJsxManagedAttributesFromLocatedAttributes(context, ns, attributesType) {
    const managedSym ***REMOVED*** getJsxLibraryManagedAttributes(ns);
    if (managedSym) {
      const ctorType ***REMOVED*** getStaticTypeOfReferencedJsxConstructor(context);
      const result ***REMOVED*** instantiateAliasOrInterfaceWithDefaults(managedSym, isInJSFile(context), ctorType, attributesType);
      if (result) {
        return result;
      }
    }
    return attributesType;
  }
  function getJsxPropsTypeFromClassType(sig, context) {
    const ns ***REMOVED*** getJsxNamespaceAt(context);
    const forcedLookupLocation ***REMOVED*** getJsxElementPropertiesName(ns);
    let attributesType ***REMOVED*** forcedLookupLocation ***REMOVED******REMOVED******REMOVED*** void 0 ? getTypeOfFirstParameterOfSignatureWithFallback(sig, unknownType) : forcedLookupLocation ***REMOVED******REMOVED******REMOVED*** "" ? getReturnTypeOfSignature(sig) : getJsxPropsTypeForSignatureFromMember(sig, forcedLookupLocation);
    if (!attributesType) {
      if (!!forcedLookupLocation && !!length(context.attributes.properties)) {
        error(context, Diagnostics.JSX_element_class_does_not_support_attributes_because_it_does_not_have_a_0_property, unescapeLeadingUnderscores(forcedLookupLocation));
      }
      return unknownType;
    }
    attributesType ***REMOVED*** getJsxManagedAttributesFromLocatedAttributes(context, ns, attributesType);
    if (isTypeAny(attributesType)) {
      return attributesType;
    } else {
      let apparentAttributesType ***REMOVED*** attributesType;
      const intrinsicClassAttribs ***REMOVED*** getJsxType(JsxNames.IntrinsicClassAttributes, context);
      if (!isErrorType(intrinsicClassAttribs)) {
        const typeParams ***REMOVED*** getLocalTypeParametersOfClassOrInterfaceOrTypeAlias(intrinsicClassAttribs.symbol);
        const hostClassType ***REMOVED*** getReturnTypeOfSignature(sig);
        let libraryManagedAttributeType;
        if (typeParams) {
          const inferredArgs ***REMOVED*** fillMissingTypeArguments([hostClassType], typeParams, getMinTypeArgumentCount(typeParams), isInJSFile(context));
          libraryManagedAttributeType ***REMOVED*** instantiateType(intrinsicClassAttribs, createTypeMapper(typeParams, inferredArgs));
        } else libraryManagedAttributeType ***REMOVED*** intrinsicClassAttribs;
        apparentAttributesType ***REMOVED*** intersectTypes(libraryManagedAttributeType, apparentAttributesType);
      }
      const intrinsicAttribs ***REMOVED*** getJsxType(JsxNames.IntrinsicAttributes, context);
      if (!isErrorType(intrinsicAttribs)) {
        apparentAttributesType ***REMOVED*** intersectTypes(intrinsicAttribs, apparentAttributesType);
      }
      return apparentAttributesType;
    }
  }
  function getIntersectedSignatures(signatures) {
    return getStrictOptionValue(compilerOptions, "noImplicitAny") ? reduceLeft(
      signatures,
      (left, right) ***REMOVED***> left ***REMOVED******REMOVED******REMOVED*** right || !left ? left : compareTypeParametersIdentical(left.typeParameters, right.typeParameters) ? combineSignaturesOfIntersectionMembers(left, right) : void 0
    ) : void 0;
  }
  function combineIntersectionThisParam(left, right, mapper) {
    if (!left || !right) {
      return left || right;
    }
    const thisType ***REMOVED*** getUnionType([getTypeOfSymbol(left), instantiateType(getTypeOfSymbol(right), mapper)]);
    return createSymbolWithType(left, thisType);
  }
  function combineIntersectionParameters(left, right, mapper) {
    const leftCount ***REMOVED*** getParameterCount(left);
    const rightCount ***REMOVED*** getParameterCount(right);
    const longest ***REMOVED*** leftCount >***REMOVED*** rightCount ? left : right;
    const shorter ***REMOVED*** longest ***REMOVED******REMOVED******REMOVED*** left ? right : left;
    const longestCount ***REMOVED*** longest ***REMOVED******REMOVED******REMOVED*** left ? leftCount : rightCount;
    const eitherHasEffectiveRest ***REMOVED*** hasEffectiveRestParameter(left) || hasEffectiveRestParameter(right);
    const needsExtraRestElement ***REMOVED*** eitherHasEffectiveRest && !hasEffectiveRestParameter(longest);
    const params ***REMOVED*** new Array(longestCount + (needsExtraRestElement ? 1 : 0));
    for (let i ***REMOVED*** 0; i < longestCount; i++) {
      let longestParamType ***REMOVED*** tryGetTypeAtPosition(longest, i);
      if (longest ***REMOVED******REMOVED******REMOVED*** right) {
        longestParamType ***REMOVED*** instantiateType(longestParamType, mapper);
      }
      let shorterParamType ***REMOVED*** tryGetTypeAtPosition(shorter, i) || unknownType;
      if (shorter ***REMOVED******REMOVED******REMOVED*** right) {
        shorterParamType ***REMOVED*** instantiateType(shorterParamType, mapper);
      }
      const unionParamType ***REMOVED*** getUnionType([longestParamType, shorterParamType]);
      const isRestParam ***REMOVED*** eitherHasEffectiveRest && !needsExtraRestElement && i ***REMOVED******REMOVED******REMOVED*** longestCount - 1;
      const isOptional ***REMOVED*** i >***REMOVED*** getMinArgumentCount(longest) && i >***REMOVED*** getMinArgumentCount(shorter);
      const leftName ***REMOVED*** i >***REMOVED*** leftCount ? void 0 : getParameterNameAtPosition(left, i);
      const rightName ***REMOVED*** i >***REMOVED*** rightCount ? void 0 : getParameterNameAtPosition(right, i);
      const paramName ***REMOVED*** leftName ***REMOVED******REMOVED******REMOVED*** rightName ? leftName : !leftName ? rightName : !rightName ? leftName : void 0;
      const paramSymbol ***REMOVED*** createSymbol(
        1 /* FunctionScopedVariable */ | (isOptional && !isRestParam ? 16777216 /* Optional */ : 0),
        paramName || `arg${i}`,
        isRestParam ? 32768 /* RestParameter */ : isOptional ? 16384 /* OptionalParameter */ : 0
      );
      paramSymbol.links.type ***REMOVED*** isRestParam ? createArrayType(unionParamType) : unionParamType;
      params[i] ***REMOVED*** paramSymbol;
    }
    if (needsExtraRestElement) {
      const restParamSymbol ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, "args", 32768 /* RestParameter */);
      restParamSymbol.links.type ***REMOVED*** createArrayType(getTypeAtPosition(shorter, longestCount));
      if (shorter ***REMOVED******REMOVED******REMOVED*** right) {
        restParamSymbol.links.type ***REMOVED*** instantiateType(restParamSymbol.links.type, mapper);
      }
      params[longestCount] ***REMOVED*** restParamSymbol;
    }
    return params;
  }
  function combineSignaturesOfIntersectionMembers(left, right) {
    const typeParams ***REMOVED*** left.typeParameters || right.typeParameters;
    let paramMapper;
    if (left.typeParameters && right.typeParameters) {
      paramMapper ***REMOVED*** createTypeMapper(right.typeParameters, left.typeParameters);
    }
    let flags ***REMOVED*** (left.flags | right.flags) & (167 /* PropagatingFlags */ & ~1 /* HasRestParameter */);
    const declaration ***REMOVED*** left.declaration;
    const params ***REMOVED*** combineIntersectionParameters(left, right, paramMapper);
    const lastParam ***REMOVED*** lastOrUndefined(params);
    if (lastParam && getCheckFlags(lastParam) & 32768 /* RestParameter */) {
      flags |***REMOVED*** 1 /* HasRestParameter */;
    }
    const thisParam ***REMOVED*** combineIntersectionThisParam(left.thisParameter, right.thisParameter, paramMapper);
    const minArgCount ***REMOVED*** Math.max(left.minArgumentCount, right.minArgumentCount);
    const result ***REMOVED*** createSignature(
      declaration,
      typeParams,
      thisParam,
      params,
      /*resolvedReturnType*/
      void 0,
      /*resolvedTypePredicate*/
      void 0,
      minArgCount,
      flags
    );
    result.compositeKind ***REMOVED*** 2097152 /* Intersection */;
    result.compositeSignatures ***REMOVED*** concatenate(left.compositeKind ***REMOVED******REMOVED******REMOVED*** 2097152 /* Intersection */ && left.compositeSignatures || [left], [right]);
    if (paramMapper) {
      result.mapper ***REMOVED*** left.compositeKind ***REMOVED******REMOVED******REMOVED*** 2097152 /* Intersection */ && left.mapper && left.compositeSignatures ? combineTypeMappers(left.mapper, paramMapper) : paramMapper;
    }
    return result;
  }
  function getContextualCallSignature(type, node) {
    const signatures ***REMOVED*** getSignaturesOfType(type, 0 /* Call */);
    const applicableByArity ***REMOVED*** filter(signatures, (s) ***REMOVED***> !isAritySmaller(s, node));
    return applicableByArity.length ***REMOVED******REMOVED******REMOVED*** 1 ? applicableByArity[0] : getIntersectedSignatures(applicableByArity);
  }
  function isAritySmaller(signature, target) {
    let targetParameterCount ***REMOVED*** 0;
    for (; targetParameterCount < target.parameters.length; targetParameterCount++) {
      const param ***REMOVED*** target.parameters[targetParameterCount];
      if (param.initializer || param.questionToken || param.dotDotDotToken || isJSDocOptionalParameter(param)) {
        break;
      }
    }
    if (target.parameters.length && parameterIsThisKeyword(target.parameters[0])) {
      targetParameterCount--;
    }
    return !hasEffectiveRestParameter(signature) && getParameterCount(signature) < targetParameterCount;
  }
  function getContextualSignatureForFunctionLikeDeclaration(node) {
    return isFunctionExpressionOrArrowFunction(node) || isObjectLiteralMethod(node) ? getContextualSignature(node) : void 0;
  }
  function getContextualSignature(node) {
    Debug.assert(node.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ || isObjectLiteralMethod(node));
    const typeTagSignature ***REMOVED*** getSignatureOfTypeTag(node);
    if (typeTagSignature) {
      return typeTagSignature;
    }
    const type ***REMOVED*** getApparentTypeOfContextualType(node, 1 /* Signature */);
    if (!type) {
      return void 0;
    }
    if (!(type.flags & 1048576 /* Union */)) {
      return getContextualCallSignature(type, node);
    }
    let signatureList;
    const types ***REMOVED*** type.types;
    for (const current of types) {
      const signature ***REMOVED*** getContextualCallSignature(current, node);
      if (signature) {
        if (!signatureList) {
          signatureList ***REMOVED*** [signature];
        } else if (!compareSignaturesIdentical(
          signatureList[0],
          signature,
          /*partialMatch*/
          false,
          /*ignoreThisTypes*/
          true,
          /*ignoreReturnTypes*/
          true,
          compareTypesIdentical
        )) {
          return void 0;
        } else {
          signatureList.push(signature);
        }
      }
    }
    if (signatureList) {
      return signatureList.length ***REMOVED******REMOVED******REMOVED*** 1 ? signatureList[0] : createUnionSignature(signatureList[0], signatureList);
    }
  }
  function checkGrammarRegularExpressionLiteral(node) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile) && !node.isUnterminated) {
      let lastError;
      scanner ?? (scanner ***REMOVED*** createScanner(
        99 /* ESNext */,
        /*skipTrivia*/
        true
      ));
      scanner.setScriptTarget(sourceFile.languageVersion);
      scanner.setLanguageVariant(sourceFile.languageVariant);
      scanner.setOnError((message, length2, arg0) ***REMOVED***> {
        const start ***REMOVED*** scanner.getTokenEnd();
        if (message.category ***REMOVED******REMOVED******REMOVED*** 3 /* Message */ && lastError && start ***REMOVED******REMOVED******REMOVED*** lastError.start && length2 ***REMOVED******REMOVED******REMOVED*** lastError.length) {
          const error2 ***REMOVED*** createDetachedDiagnostic(sourceFile.fileName, sourceFile.text, start, length2, message, arg0);
          addRelatedInfo(lastError, error2);
        } else if (!lastError || start !***REMOVED******REMOVED*** lastError.start) {
          lastError ***REMOVED*** createFileDiagnostic(sourceFile, start, length2, message, arg0);
          diagnostics.add(lastError);
        }
      });
      scanner.setText(sourceFile.text, node.pos, node.end - node.pos);
      try {
        scanner.scan();
        Debug.assert(scanner.reScanSlashToken(
          /*reportErrors*/
          true
        ) ***REMOVED******REMOVED******REMOVED*** 14 /* RegularExpressionLiteral */, "Expected scanner to rescan RegularExpressionLiteral");
        return !!lastError;
      } finally {
        scanner.setText("");
        scanner.setOnError(
          /*onError*/
          void 0
        );
      }
    }
    return false;
  }
  function checkRegularExpressionLiteral(node) {
    const nodeLinks2 ***REMOVED*** getNodeLinks(node);
    if (!(nodeLinks2.flags & 1 /* TypeChecked */)) {
      nodeLinks2.flags |***REMOVED*** 1 /* TypeChecked */;
      addLazyDiagnostic(() ***REMOVED***> checkGrammarRegularExpressionLiteral(node));
    }
    return globalRegExpType;
  }
  function checkSpreadExpression(node, checkMode) {
    if (languageVersion < LanguageFeatureMinimumTarget.SpreadElements) {
      checkExternalEmitHelpers(node, compilerOptions.downlevelIteration ? 1536 /* SpreadIncludes */ : 1024 /* SpreadArray */);
    }
    const arrayOrIterableType ***REMOVED*** checkExpression(node.expression, checkMode);
    return checkIteratedTypeOrElementType(33 /* Spread */, arrayOrIterableType, undefinedType, node.expression);
  }
  function checkSyntheticExpression(node) {
    return node.isSpread ? getIndexedAccessType(node.type, numberType) : node.type;
  }
  function hasDefaultValue(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && !!node.initializer || node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ && hasDefaultValue(node.initializer) || node.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ && !!node.objectAssignmentInitializer || node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */;
  }
  function isSpreadIntoCallOrNew(node) {
    const parent ***REMOVED*** walkUpParenthesizedExpressions(node.parent);
    return isSpreadElement(parent) && isCallOrNewExpression(parent.parent);
  }
  function checkArrayLiteral(node, checkMode, forceTuple) {
    const elements ***REMOVED*** node.elements;
    const elementCount ***REMOVED*** elements.length;
    const elementTypes ***REMOVED*** [];
    const elementFlags ***REMOVED*** [];
    pushCachedContextualType(node);
    const inDestructuringPattern ***REMOVED*** isAssignmentTarget(node);
    const inConstContext ***REMOVED*** isConstContext(node);
    const contextualType ***REMOVED*** getApparentTypeOfContextualType(
      node,
      /*contextFlags*/
      void 0
    );
    const inTupleContext ***REMOVED*** isSpreadIntoCallOrNew(node) || !!contextualType && someType(contextualType, (t) ***REMOVED***> isTupleLikeType(t) || isGenericMappedType(t) && !t.nameType && !!getHomomorphicTypeVariable(t.target || t));
    let hasOmittedExpression ***REMOVED*** false;
    for (let i ***REMOVED*** 0; i < elementCount; i++) {
      const e ***REMOVED*** elements[i];
      if (e.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */) {
        if (languageVersion < LanguageFeatureMinimumTarget.SpreadElements) {
          checkExternalEmitHelpers(e, compilerOptions.downlevelIteration ? 1536 /* SpreadIncludes */ : 1024 /* SpreadArray */);
        }
        const spreadType ***REMOVED*** checkExpression(e.expression, checkMode, forceTuple);
        if (isArrayLikeType(spreadType)) {
          elementTypes.push(spreadType);
          elementFlags.push(8 /* Variadic */);
        } else if (inDestructuringPattern) {
          const restElementType ***REMOVED*** getIndexTypeOfType(spreadType, numberType) || getIteratedTypeOrElementType(
            65 /* Destructuring */,
            spreadType,
            undefinedType,
            /*errorNode*/
            void 0,
            /*checkAssignability*/
            false
          ) || unknownType;
          elementTypes.push(restElementType);
          elementFlags.push(4 /* Rest */);
        } else {
          elementTypes.push(checkIteratedTypeOrElementType(33 /* Spread */, spreadType, undefinedType, e.expression));
          elementFlags.push(4 /* Rest */);
        }
      } else if (exactOptionalPropertyTypes && e.kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */) {
        hasOmittedExpression ***REMOVED*** true;
        elementTypes.push(undefinedOrMissingType);
        elementFlags.push(2 /* Optional */);
      } else {
        const type ***REMOVED*** checkExpressionForMutableLocation(e, checkMode, forceTuple);
        elementTypes.push(addOptionality(
          type,
          /*isProperty*/
          true,
          hasOmittedExpression
        ));
        elementFlags.push(hasOmittedExpression ? 2 /* Optional */ : 1 /* Required */);
        if (inTupleContext && checkMode && checkMode & 2 /* Inferential */ && !(checkMode & 4 /* SkipContextSensitive */) && isContextSensitive(e)) {
          const inferenceContext ***REMOVED*** getInferenceContext(node);
          Debug.assert(inferenceContext);
          addIntraExpressionInferenceSite(inferenceContext, e, type);
        }
      }
    }
    popContextualType();
    if (inDestructuringPattern) {
      return createTupleType(elementTypes, elementFlags);
    }
    if (forceTuple || inConstContext || inTupleContext) {
      return createArrayLiteralType(createTupleType(
        elementTypes,
        elementFlags,
        /*readonly*/
        inConstContext && !(contextualType && someType(contextualType, isMutableArrayLikeType))
      ));
    }
    return createArrayLiteralType(createArrayType(
      elementTypes.length ? getUnionType(sameMap(elementTypes, (t, i) ***REMOVED***> elementFlags[i] & 8 /* Variadic */ ? getIndexedAccessTypeOrUndefined(t, numberType) || anyType : t), 2 /* Subtype */) : strictNullChecks ? implicitNeverType : undefinedWideningType,
      inConstContext
    ));
  }
  function createArrayLiteralType(type) {
    if (!(getObjectFlags(type) & 4 /* Reference */)) {
      return type;
    }
    let literalType ***REMOVED*** type.literalType;
    if (!literalType) {
      literalType ***REMOVED*** type.literalType ***REMOVED*** cloneTypeReference(type);
      literalType.objectFlags |***REMOVED*** 16384 /* ArrayLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
    }
    return literalType;
  }
  function isNumericName(name) {
    switch (name.kind) {
      case 168 /* ComputedPropertyName */:
        return isNumericComputedName(name);
      case 80 /* Identifier */:
        return isNumericLiteralName(name.escapedText);
      case 9 /* NumericLiteral */:
      case 11 /* StringLiteral */:
        return isNumericLiteralName(name.text);
      default:
        return false;
    }
  }
  function isNumericComputedName(name) {
    return isTypeAssignableToKind(checkComputedPropertyName(name), 296 /* NumberLike */);
  }
  function checkComputedPropertyName(node) {
    const links ***REMOVED*** getNodeLinks(node.expression);
    if (!links.resolvedType) {
      if ((isTypeLiteralNode(node.parent.parent) || isClassLike(node.parent.parent) || isInterfaceDeclaration(node.parent.parent)) && isBinaryExpression(node.expression) && node.expression.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ && node.parent.kind !***REMOVED******REMOVED*** 178 /* GetAccessor */ && node.parent.kind !***REMOVED******REMOVED*** 179 /* SetAccessor */) {
        return links.resolvedType ***REMOVED*** errorType;
      }
      links.resolvedType ***REMOVED*** checkExpression(node.expression);
      if (isPropertyDeclaration(node.parent) && !hasStaticModifier(node.parent) && isClassExpression(node.parent.parent)) {
        const container ***REMOVED*** getEnclosingBlockScopeContainer(node.parent.parent);
        const enclosingIterationStatement ***REMOVED*** getEnclosingIterationStatement(container);
        if (enclosingIterationStatement) {
          getNodeLinks(enclosingIterationStatement).flags |***REMOVED*** 4096 /* LoopWithCapturedBlockScopedBinding */;
          getNodeLinks(node).flags |***REMOVED*** 32768 /* BlockScopedBindingInLoop */;
          getNodeLinks(node.parent.parent).flags |***REMOVED*** 32768 /* BlockScopedBindingInLoop */;
        }
      }
      if (links.resolvedType.flags & 98304 /* Nullable */ || !isTypeAssignableToKind(links.resolvedType, 402653316 /* StringLike */ | 296 /* NumberLike */ | 12288 /* ESSymbolLike */) && !isTypeAssignableTo(links.resolvedType, stringNumberSymbolType)) {
        error(node, Diagnostics.A_computed_property_name_must_be_of_type_string_number_symbol_or_any);
      }
    }
    return links.resolvedType;
  }
  function isSymbolWithNumericName(symbol) {
    var _a;
    const firstDecl ***REMOVED*** (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0];
    return isNumericLiteralName(symbol.escapedName) || firstDecl && isNamedDeclaration(firstDecl) && isNumericName(firstDecl.name);
  }
  function isSymbolWithSymbolName(symbol) {
    var _a;
    const firstDecl ***REMOVED*** (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0];
    return isKnownSymbol(symbol) || firstDecl && isNamedDeclaration(firstDecl) && isComputedPropertyName(firstDecl.name) && isTypeAssignableToKind(checkComputedPropertyName(firstDecl.name), 4096 /* ESSymbol */);
  }
  function isSymbolWithComputedName(symbol) {
    var _a;
    const firstDecl ***REMOVED*** (_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0];
    return firstDecl && isNamedDeclaration(firstDecl) && isComputedPropertyName(firstDecl.name);
  }
  function getObjectLiteralIndexInfo(isReadonly, offset, properties, keyType) {
    var _a;
    const propTypes ***REMOVED*** [];
    let components;
    for (let i ***REMOVED*** offset; i < properties.length; i++) {
      const prop ***REMOVED*** properties[i];
      if (keyType ***REMOVED******REMOVED******REMOVED*** stringType && !isSymbolWithSymbolName(prop) || keyType ***REMOVED******REMOVED******REMOVED*** numberType && isSymbolWithNumericName(prop) || keyType ***REMOVED******REMOVED******REMOVED*** esSymbolType && isSymbolWithSymbolName(prop)) {
        propTypes.push(getTypeOfSymbol(properties[i]));
        if (isSymbolWithComputedName(properties[i])) {
          components ***REMOVED*** append(components, (_a ***REMOVED*** properties[i].declarations) ***REMOVED******REMOVED*** null ? void 0 : _a[0]);
        }
      }
    }
    const unionType ***REMOVED*** propTypes.length ? getUnionType(propTypes, 2 /* Subtype */) : undefinedType;
    return createIndexInfo(
      keyType,
      unionType,
      isReadonly,
      /*declaration*/
      void 0,
      components
    );
  }
  function getImmediateAliasedSymbol(symbol) {
    Debug.assert((symbol.flags & 2097152 /* Alias */) !***REMOVED******REMOVED*** 0, "Should only get Alias here.");
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.immediateTarget) {
      const node ***REMOVED*** getDeclarationOfAliasSymbol(symbol);
      if (!node) return Debug.fail();
      links.immediateTarget ***REMOVED*** getTargetOfAliasDeclaration(
        node,
        /*dontRecursivelyResolve*/
        true
      );
    }
    return links.immediateTarget;
  }
  function checkObjectLiteral(node, checkMode ***REMOVED*** 0 /* Normal */) {
    const inDestructuringPattern ***REMOVED*** isAssignmentTarget(node);
    checkGrammarObjectLiteralExpression(node, inDestructuringPattern);
    const allPropertiesTable ***REMOVED*** strictNullChecks ? createSymbolTable() : void 0;
    let propertiesTable ***REMOVED*** createSymbolTable();
    let propertiesArray ***REMOVED*** [];
    let spread ***REMOVED*** emptyObjectType;
    pushCachedContextualType(node);
    const contextualType ***REMOVED*** getApparentTypeOfContextualType(
      node,
      /*contextFlags*/
      void 0
    );
    const contextualTypeHasPattern ***REMOVED*** contextualType && contextualType.pattern && (contextualType.pattern.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ || contextualType.pattern.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */);
    const inConstContext ***REMOVED*** isConstContext(node);
    const checkFlags ***REMOVED*** inConstContext ? 8 /* Readonly */ : 0;
    const isInJavascript ***REMOVED*** isInJSFile(node) && !isInJsonFile(node);
    const enumTag ***REMOVED*** isInJavascript ? getJSDocEnumTag(node) : void 0;
    const isJSObjectLiteral ***REMOVED*** !contextualType && isInJavascript && !enumTag;
    let objectFlags ***REMOVED*** 8192 /* FreshLiteral */;
    let patternWithComputedProperties ***REMOVED*** false;
    let hasComputedStringProperty ***REMOVED*** false;
    let hasComputedNumberProperty ***REMOVED*** false;
    let hasComputedSymbolProperty ***REMOVED*** false;
    for (const elem of node.properties) {
      if (elem.name && isComputedPropertyName(elem.name)) {
        checkComputedPropertyName(elem.name);
      }
    }
    let offset ***REMOVED*** 0;
    for (const memberDecl of node.properties) {
      let member ***REMOVED*** getSymbolOfDeclaration(memberDecl);
      const computedNameType ***REMOVED*** memberDecl.name && memberDecl.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ ? checkComputedPropertyName(memberDecl.name) : void 0;
      if (memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || isObjectLiteralMethod(memberDecl)) {
        let type ***REMOVED*** memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? checkPropertyAssignment(memberDecl, checkMode) : (
          // avoid resolving the left side of the ShorthandPropertyAssignment outside of the destructuring
          // for error recovery purposes. For example, if a user wrote `{ a ***REMOVED*** 100 }` instead of `{ a: 100 }`.
          // we don't want to say "could not find 'a'".
          memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ ? checkExpressionForMutableLocation(!inDestructuringPattern && memberDecl.objectAssignmentInitializer ? memberDecl.objectAssignmentInitializer : memberDecl.name, checkMode) : checkObjectLiteralMethod(memberDecl, checkMode)
        );
        if (isInJavascript) {
          const jsDocType ***REMOVED*** getTypeForDeclarationFromJSDocComment(memberDecl);
          if (jsDocType) {
            checkTypeAssignableTo(type, jsDocType, memberDecl);
            type ***REMOVED*** jsDocType;
          } else if (enumTag && enumTag.typeExpression) {
            checkTypeAssignableTo(type, getTypeFromTypeNode(enumTag.typeExpression), memberDecl);
          }
        }
        objectFlags |***REMOVED*** getObjectFlags(type) & 458752 /* PropagatingFlags */;
        const nameType ***REMOVED*** computedNameType && isTypeUsableAsPropertyName(computedNameType) ? computedNameType : void 0;
        const prop ***REMOVED*** nameType ? createSymbol(4 /* Property */ | member.flags, getPropertyNameFromType(nameType), checkFlags | 4096 /* Late */) : createSymbol(4 /* Property */ | member.flags, member.escapedName, checkFlags);
        if (nameType) {
          prop.links.nameType ***REMOVED*** nameType;
        }
        if (inDestructuringPattern && hasDefaultValue(memberDecl)) {
          prop.flags |***REMOVED*** 16777216 /* Optional */;
        } else if (contextualTypeHasPattern && !(getObjectFlags(contextualType) & 512 /* ObjectLiteralPatternWithComputedProperties */)) {
          const impliedProp ***REMOVED*** getPropertyOfType(contextualType, member.escapedName);
          if (impliedProp) {
            prop.flags |***REMOVED*** impliedProp.flags & 16777216 /* Optional */;
          } else if (!getIndexInfoOfType(contextualType, stringType)) {
            error(memberDecl.name, Diagnostics.Object_literal_may_only_specify_known_properties_and_0_does_not_exist_in_type_1, symbolToString(member), typeToString(contextualType));
          }
        }
        prop.declarations ***REMOVED*** member.declarations;
        prop.parent ***REMOVED*** member.parent;
        if (member.valueDeclaration) {
          prop.valueDeclaration ***REMOVED*** member.valueDeclaration;
        }
        prop.links.type ***REMOVED*** type;
        prop.links.target ***REMOVED*** member;
        member ***REMOVED*** prop;
        allPropertiesTable ***REMOVED******REMOVED*** null ? void 0 : allPropertiesTable.set(prop.escapedName, prop);
        if (contextualType && checkMode & 2 /* Inferential */ && !(checkMode & 4 /* SkipContextSensitive */) && (memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */) && isContextSensitive(memberDecl)) {
          const inferenceContext ***REMOVED*** getInferenceContext(node);
          Debug.assert(inferenceContext);
          const inferenceNode ***REMOVED*** memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? memberDecl.initializer : memberDecl;
          addIntraExpressionInferenceSite(inferenceContext, inferenceNode, type);
        }
      } else if (memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
        if (languageVersion < LanguageFeatureMinimumTarget.ObjectAssign) {
          checkExternalEmitHelpers(memberDecl, 2 /* Assign */);
        }
        if (propertiesArray.length > 0) {
          spread ***REMOVED*** getSpreadType(spread, createObjectLiteralType(), node.symbol, objectFlags, inConstContext);
          propertiesArray ***REMOVED*** [];
          propertiesTable ***REMOVED*** createSymbolTable();
          hasComputedStringProperty ***REMOVED*** false;
          hasComputedNumberProperty ***REMOVED*** false;
          hasComputedSymbolProperty ***REMOVED*** false;
        }
        const type ***REMOVED*** getReducedType(checkExpression(memberDecl.expression, checkMode & 2 /* Inferential */));
        if (isValidSpreadType(type)) {
          const mergedType ***REMOVED*** tryMergeUnionOfObjectTypeAndEmptyObject(type, inConstContext);
          if (allPropertiesTable) {
            checkSpreadPropOverrides(mergedType, allPropertiesTable, memberDecl);
          }
          offset ***REMOVED*** propertiesArray.length;
          if (isErrorType(spread)) {
            continue;
          }
          spread ***REMOVED*** getSpreadType(spread, mergedType, node.symbol, objectFlags, inConstContext);
        } else {
          error(memberDecl, Diagnostics.Spread_types_may_only_be_created_from_object_types);
          spread ***REMOVED*** errorType;
        }
        continue;
      } else {
        Debug.assert(memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || memberDecl.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */);
        checkNodeDeferred(memberDecl);
      }
      if (computedNameType && !(computedNameType.flags & 8576 /* StringOrNumberLiteralOrUnique */)) {
        if (isTypeAssignableTo(computedNameType, stringNumberSymbolType)) {
          if (isTypeAssignableTo(computedNameType, numberType)) {
            hasComputedNumberProperty ***REMOVED*** true;
          } else if (isTypeAssignableTo(computedNameType, esSymbolType)) {
            hasComputedSymbolProperty ***REMOVED*** true;
          } else {
            hasComputedStringProperty ***REMOVED*** true;
          }
          if (inDestructuringPattern) {
            patternWithComputedProperties ***REMOVED*** true;
          }
        }
      } else {
        propertiesTable.set(member.escapedName, member);
      }
      propertiesArray.push(member);
    }
    popContextualType();
    if (isErrorType(spread)) {
      return errorType;
    }
    if (spread !***REMOVED******REMOVED*** emptyObjectType) {
      if (propertiesArray.length > 0) {
        spread ***REMOVED*** getSpreadType(spread, createObjectLiteralType(), node.symbol, objectFlags, inConstContext);
        propertiesArray ***REMOVED*** [];
        propertiesTable ***REMOVED*** createSymbolTable();
        hasComputedStringProperty ***REMOVED*** false;
        hasComputedNumberProperty ***REMOVED*** false;
      }
      return mapType(spread, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** emptyObjectType ? createObjectLiteralType() : t);
    }
    return createObjectLiteralType();
    function createObjectLiteralType() {
      const indexInfos ***REMOVED*** [];
      const isReadonly ***REMOVED*** isConstContext(node);
      if (hasComputedStringProperty) indexInfos.push(getObjectLiteralIndexInfo(isReadonly, offset, propertiesArray, stringType));
      if (hasComputedNumberProperty) indexInfos.push(getObjectLiteralIndexInfo(isReadonly, offset, propertiesArray, numberType));
      if (hasComputedSymbolProperty) indexInfos.push(getObjectLiteralIndexInfo(isReadonly, offset, propertiesArray, esSymbolType));
      const result ***REMOVED*** createAnonymousType(node.symbol, propertiesTable, emptyArray, emptyArray, indexInfos);
      result.objectFlags |***REMOVED*** objectFlags | 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
      if (isJSObjectLiteral) {
        result.objectFlags |***REMOVED*** 4096 /* JSLiteral */;
      }
      if (patternWithComputedProperties) {
        result.objectFlags |***REMOVED*** 512 /* ObjectLiteralPatternWithComputedProperties */;
      }
      if (inDestructuringPattern) {
        result.pattern ***REMOVED*** node;
      }
      return result;
    }
  }
  function isValidSpreadType(type) {
    const t ***REMOVED*** removeDefinitelyFalsyTypes(mapType(type, getBaseConstraintOrType));
    return !!(t.flags & (1 /* Any */ | 67108864 /* NonPrimitive */ | 524288 /* Object */ | 58982400 /* InstantiableNonPrimitive */) || t.flags & 3145728 /* UnionOrIntersection */ && every(t.types, isValidSpreadType));
  }
  function checkJsxSelfClosingElementDeferred(node) {
    checkJsxOpeningLikeElementOrOpeningFragment(node);
  }
  function checkJsxSelfClosingElement(node, _checkMode) {
    checkNodeDeferred(node);
    return getJsxElementTypeAt(node) || anyType;
  }
  function checkJsxElementDeferred(node) {
    checkJsxOpeningLikeElementOrOpeningFragment(node.openingElement);
    if (isJsxIntrinsicTagName(node.closingElement.tagName)) {
      getIntrinsicTagSymbol(node.closingElement);
    } else {
      checkExpression(node.closingElement.tagName);
    }
    checkJsxChildren(node);
  }
  function checkJsxElement(node, _checkMode) {
    checkNodeDeferred(node);
    return getJsxElementTypeAt(node) || anyType;
  }
  function checkJsxFragment(node) {
    checkJsxOpeningLikeElementOrOpeningFragment(node.openingFragment);
    const nodeSourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (getJSXTransformEnabled(compilerOptions) && (compilerOptions.jsxFactory || nodeSourceFile.pragmas.has("jsx")) && !compilerOptions.jsxFragmentFactory && !nodeSourceFile.pragmas.has("jsxfrag")) {
      error(
        node,
        compilerOptions.jsxFactory ? Diagnostics.The_jsxFragmentFactory_compiler_option_must_be_provided_to_use_JSX_fragments_with_the_jsxFactory_compiler_option : Diagnostics.An_jsxFrag_pragma_is_required_when_using_an_jsx_pragma_with_JSX_fragments
      );
    }
    checkJsxChildren(node);
    const jsxElementType ***REMOVED*** getJsxElementTypeAt(node);
    return isErrorType(jsxElementType) ? anyType : jsxElementType;
  }
  function isHyphenatedJsxName(name) {
    return name.includes("-");
  }
  function isJsxIntrinsicTagName(tagName) {
    return isIdentifier(tagName) && isIntrinsicJsxName(tagName.escapedText) || isJsxNamespacedName(tagName);
  }
  function checkJsxAttribute(node, checkMode) {
    return node.initializer ? checkExpressionForMutableLocation(node.initializer, checkMode) : trueType;
  }
  function createJsxAttributesTypeFromAttributesProperty(openingLikeElement, checkMode ***REMOVED*** 0 /* Normal */) {
    const allAttributesTable ***REMOVED*** strictNullChecks ? createSymbolTable() : void 0;
    let attributesTable ***REMOVED*** createSymbolTable();
    let spread ***REMOVED*** emptyJsxObjectType;
    let hasSpreadAnyType ***REMOVED*** false;
    let typeToIntersect;
    let explicitlySpecifyChildrenAttribute ***REMOVED*** false;
    let objectFlags ***REMOVED*** 2048 /* JsxAttributes */;
    const jsxChildrenPropertyName ***REMOVED*** getJsxElementChildrenPropertyName(getJsxNamespaceAt(openingLikeElement));
    const isJsxOpenFragment ***REMOVED*** isJsxOpeningFragment(openingLikeElement);
    let attributesSymbol;
    let attributeParent ***REMOVED*** openingLikeElement;
    if (!isJsxOpenFragment) {
      const attributes ***REMOVED*** openingLikeElement.attributes;
      attributesSymbol ***REMOVED*** attributes.symbol;
      attributeParent ***REMOVED*** attributes;
      const contextualType ***REMOVED*** getContextualType(attributes, 0 /* None */);
      for (const attributeDecl of attributes.properties) {
        const member ***REMOVED*** attributeDecl.symbol;
        if (isJsxAttribute(attributeDecl)) {
          const exprType ***REMOVED*** checkJsxAttribute(attributeDecl, checkMode);
          objectFlags |***REMOVED*** getObjectFlags(exprType) & 458752 /* PropagatingFlags */;
          const attributeSymbol ***REMOVED*** createSymbol(4 /* Property */ | member.flags, member.escapedName);
          attributeSymbol.declarations ***REMOVED*** member.declarations;
          attributeSymbol.parent ***REMOVED*** member.parent;
          if (member.valueDeclaration) {
            attributeSymbol.valueDeclaration ***REMOVED*** member.valueDeclaration;
          }
          attributeSymbol.links.type ***REMOVED*** exprType;
          attributeSymbol.links.target ***REMOVED*** member;
          attributesTable.set(attributeSymbol.escapedName, attributeSymbol);
          allAttributesTable ***REMOVED******REMOVED*** null ? void 0 : allAttributesTable.set(attributeSymbol.escapedName, attributeSymbol);
          if (getEscapedTextOfJsxAttributeName(attributeDecl.name) ***REMOVED******REMOVED******REMOVED*** jsxChildrenPropertyName) {
            explicitlySpecifyChildrenAttribute ***REMOVED*** true;
          }
          if (contextualType) {
            const prop ***REMOVED*** getPropertyOfType(contextualType, member.escapedName);
            if (prop && prop.declarations && isDeprecatedSymbol(prop) && isIdentifier(attributeDecl.name)) {
              addDeprecatedSuggestion(attributeDecl.name, prop.declarations, attributeDecl.name.escapedText);
            }
          }
          if (contextualType && checkMode & 2 /* Inferential */ && !(checkMode & 4 /* SkipContextSensitive */) && isContextSensitive(attributeDecl)) {
            const inferenceContext ***REMOVED*** getInferenceContext(attributes);
            Debug.assert(inferenceContext);
            const inferenceNode ***REMOVED*** attributeDecl.initializer.expression;
            addIntraExpressionInferenceSite(inferenceContext, inferenceNode, exprType);
          }
        } else {
          Debug.assert(attributeDecl.kind ***REMOVED******REMOVED******REMOVED*** 294 /* JsxSpreadAttribute */);
          if (attributesTable.size > 0) {
            spread ***REMOVED*** getSpreadType(
              spread,
              createJsxAttributesTypeHelper(),
              attributes.symbol,
              objectFlags,
              /*readonly*/
              false
            );
            attributesTable ***REMOVED*** createSymbolTable();
          }
          const exprType ***REMOVED*** getReducedType(checkExpression(attributeDecl.expression, checkMode & 2 /* Inferential */));
          if (isTypeAny(exprType)) {
            hasSpreadAnyType ***REMOVED*** true;
          }
          if (isValidSpreadType(exprType)) {
            spread ***REMOVED*** getSpreadType(
              spread,
              exprType,
              attributes.symbol,
              objectFlags,
              /*readonly*/
              false
            );
            if (allAttributesTable) {
              checkSpreadPropOverrides(exprType, allAttributesTable, attributeDecl);
            }
          } else {
            error(attributeDecl.expression, Diagnostics.Spread_types_may_only_be_created_from_object_types);
            typeToIntersect ***REMOVED*** typeToIntersect ? getIntersectionType([typeToIntersect, exprType]) : exprType;
          }
        }
      }
      if (!hasSpreadAnyType) {
        if (attributesTable.size > 0) {
          spread ***REMOVED*** getSpreadType(
            spread,
            createJsxAttributesTypeHelper(),
            attributes.symbol,
            objectFlags,
            /*readonly*/
            false
          );
        }
      }
    }
    const parent ***REMOVED*** openingLikeElement.parent;
    if ((isJsxElement(parent) && parent.openingElement ***REMOVED******REMOVED******REMOVED*** openingLikeElement || isJsxFragment(parent) && parent.openingFragment ***REMOVED******REMOVED******REMOVED*** openingLikeElement) && getSemanticJsxChildren(parent.children).length > 0) {
      const childrenTypes ***REMOVED*** checkJsxChildren(parent, checkMode);
      if (!hasSpreadAnyType && jsxChildrenPropertyName && jsxChildrenPropertyName !***REMOVED******REMOVED*** "") {
        if (explicitlySpecifyChildrenAttribute) {
          error(attributeParent, Diagnostics._0_are_specified_twice_The_attribute_named_0_will_be_overwritten, unescapeLeadingUnderscores(jsxChildrenPropertyName));
        }
        const contextualType ***REMOVED*** isJsxOpeningElement(openingLikeElement) ? getApparentTypeOfContextualType(
          openingLikeElement.attributes,
          /*contextFlags*/
          void 0
        ) : void 0;
        const childrenContextualType ***REMOVED*** contextualType && getTypeOfPropertyOfContextualType(contextualType, jsxChildrenPropertyName);
        const childrenPropSymbol ***REMOVED*** createSymbol(4 /* Property */, jsxChildrenPropertyName);
        childrenPropSymbol.links.type ***REMOVED*** childrenTypes.length ***REMOVED******REMOVED******REMOVED*** 1 ? childrenTypes[0] : childrenContextualType && someType(childrenContextualType, isTupleLikeType) ? createTupleType(childrenTypes) : createArrayType(getUnionType(childrenTypes));
        childrenPropSymbol.valueDeclaration ***REMOVED*** factory.createPropertySignature(
          /*modifiers*/
          void 0,
          unescapeLeadingUnderscores(jsxChildrenPropertyName),
          /*questionToken*/
          void 0,
          /*type*/
          void 0
        );
        setParent(childrenPropSymbol.valueDeclaration, attributeParent);
        childrenPropSymbol.valueDeclaration.symbol ***REMOVED*** childrenPropSymbol;
        const childPropMap ***REMOVED*** createSymbolTable();
        childPropMap.set(jsxChildrenPropertyName, childrenPropSymbol);
        spread ***REMOVED*** getSpreadType(
          spread,
          createAnonymousType(attributesSymbol, childPropMap, emptyArray, emptyArray, emptyArray),
          attributesSymbol,
          objectFlags,
          /*readonly*/
          false
        );
      }
    }
    if (hasSpreadAnyType) {
      return anyType;
    }
    if (typeToIntersect && spread !***REMOVED******REMOVED*** emptyJsxObjectType) {
      return getIntersectionType([typeToIntersect, spread]);
    }
    return typeToIntersect || (spread ***REMOVED******REMOVED******REMOVED*** emptyJsxObjectType ? createJsxAttributesTypeHelper() : spread);
    function createJsxAttributesTypeHelper() {
      objectFlags |***REMOVED*** 8192 /* FreshLiteral */;
      return createJsxAttributesType(objectFlags, attributesSymbol, attributesTable);
    }
  }
  function createJsxAttributesType(objectFlags, attributesSymbol, attributesTable) {
    const result ***REMOVED*** createAnonymousType(attributesSymbol, attributesTable, emptyArray, emptyArray, emptyArray);
    result.objectFlags |***REMOVED*** objectFlags | 8192 /* FreshLiteral */ | 128 /* ObjectLiteral */ | 131072 /* ContainsObjectOrArrayLiteral */;
    return result;
  }
  function checkJsxChildren(node, checkMode) {
    const childrenTypes ***REMOVED*** [];
    for (const child of node.children) {
      if (child.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */) {
        if (!child.containsOnlyTriviaWhiteSpaces) {
          childrenTypes.push(stringType);
        }
      } else if (child.kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */ && !child.expression) {
        continue;
      } else {
        childrenTypes.push(checkExpressionForMutableLocation(child, checkMode));
      }
    }
    return childrenTypes;
  }
  function checkSpreadPropOverrides(type, props, spread) {
    for (const right of getPropertiesOfType(type)) {
      if (!(right.flags & 16777216 /* Optional */)) {
        const left ***REMOVED*** props.get(right.escapedName);
        if (left) {
          const diagnostic ***REMOVED*** error(left.valueDeclaration, Diagnostics._0_is_specified_more_than_once_so_this_usage_will_be_overwritten, unescapeLeadingUnderscores(left.escapedName));
          addRelatedInfo(diagnostic, createDiagnosticForNode(spread, Diagnostics.This_spread_always_overwrites_this_property));
        }
      }
    }
  }
  function checkJsxAttributes(node, checkMode) {
    return createJsxAttributesTypeFromAttributesProperty(node.parent, checkMode);
  }
  function getJsxType(name, location) {
    const namespace ***REMOVED*** getJsxNamespaceAt(location);
    const exports2 ***REMOVED*** namespace && getExportsOfSymbol(namespace);
    const typeSymbol ***REMOVED*** exports2 && getSymbol(exports2, name, 788968 /* Type */);
    return typeSymbol ? getDeclaredTypeOfSymbol(typeSymbol) : errorType;
  }
  function getIntrinsicTagSymbol(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedSymbol) {
      const intrinsicElementsType ***REMOVED*** getJsxType(JsxNames.IntrinsicElements, node);
      if (!isErrorType(intrinsicElementsType)) {
        if (!isIdentifier(node.tagName) && !isJsxNamespacedName(node.tagName)) return Debug.fail();
        const propName ***REMOVED*** isJsxNamespacedName(node.tagName) ? getEscapedTextOfJsxNamespacedName(node.tagName) : node.tagName.escapedText;
        const intrinsicProp ***REMOVED*** getPropertyOfType(intrinsicElementsType, propName);
        if (intrinsicProp) {
          links.jsxFlags |***REMOVED*** 1 /* IntrinsicNamedElement */;
          return links.resolvedSymbol ***REMOVED*** intrinsicProp;
        }
        const indexSymbol ***REMOVED*** getApplicableIndexSymbol(intrinsicElementsType, getStringLiteralType(unescapeLeadingUnderscores(propName)));
        if (indexSymbol) {
          links.jsxFlags |***REMOVED*** 2 /* IntrinsicIndexedElement */;
          return links.resolvedSymbol ***REMOVED*** indexSymbol;
        }
        if (getTypeOfPropertyOrIndexSignatureOfType(intrinsicElementsType, propName)) {
          links.jsxFlags |***REMOVED*** 2 /* IntrinsicIndexedElement */;
          return links.resolvedSymbol ***REMOVED*** intrinsicElementsType.symbol;
        }
        error(node, Diagnostics.Property_0_does_not_exist_on_type_1, intrinsicTagNameToString(node.tagName), "JSX." + JsxNames.IntrinsicElements);
        return links.resolvedSymbol ***REMOVED*** unknownSymbol;
      } else {
        if (noImplicitAny) {
          error(node, Diagnostics.JSX_element_implicitly_has_type_any_because_no_interface_JSX_0_exists, unescapeLeadingUnderscores(JsxNames.IntrinsicElements));
        }
        return links.resolvedSymbol ***REMOVED*** unknownSymbol;
      }
    }
    return links.resolvedSymbol;
  }
  function getJsxNamespaceContainerForImplicitImport(location) {
    const file ***REMOVED*** location && getSourceFileOfNode(location);
    const links ***REMOVED*** file && getNodeLinks(file);
    if (links && links.jsxImplicitImportContainer ***REMOVED******REMOVED******REMOVED*** false) {
      return void 0;
    }
    if (links && links.jsxImplicitImportContainer) {
      return links.jsxImplicitImportContainer;
    }
    const runtimeImportSpecifier ***REMOVED*** getJSXRuntimeImport(getJSXImplicitImportBase(compilerOptions, file), compilerOptions);
    if (!runtimeImportSpecifier) {
      return void 0;
    }
    const isClassic ***REMOVED*** getEmitModuleResolutionKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 1 /* Classic */;
    const errorMessage ***REMOVED*** isClassic ? Diagnostics.Cannot_find_module_0_Did_you_mean_to_set_the_moduleResolution_option_to_nodenext_or_to_add_aliases_to_the_paths_option : Diagnostics.This_JSX_tag_requires_the_module_path_0_to_exist_but_none_could_be_found_Make_sure_you_have_types_for_the_appropriate_package_installed;
    const specifier ***REMOVED*** getJSXRuntimeImportSpecifier(file, runtimeImportSpecifier);
    const mod ***REMOVED*** resolveExternalModule(specifier || location, runtimeImportSpecifier, errorMessage, location);
    const result ***REMOVED*** mod && mod !***REMOVED******REMOVED*** unknownSymbol ? getMergedSymbol(resolveSymbol(mod)) : void 0;
    if (links) {
      links.jsxImplicitImportContainer ***REMOVED*** result || false;
    }
    return result;
  }
  function getJsxNamespaceAt(location) {
    const links ***REMOVED*** location && getNodeLinks(location);
    if (links && links.jsxNamespace) {
      return links.jsxNamespace;
    }
    if (!links || links.jsxNamespace !***REMOVED******REMOVED*** false) {
      let resolvedNamespace ***REMOVED*** getJsxNamespaceContainerForImplicitImport(location);
      if (!resolvedNamespace || resolvedNamespace ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
        const namespaceName ***REMOVED*** getJsxNamespace(location);
        resolvedNamespace ***REMOVED*** resolveName(
          location,
          namespaceName,
          1920 /* Namespace */,
          /*nameNotFoundMessage*/
          void 0,
          /*isUse*/
          false
        );
      }
      if (resolvedNamespace) {
        const candidate ***REMOVED*** resolveSymbol(getSymbol(getExportsOfSymbol(resolveSymbol(resolvedNamespace)), JsxNames.JSX, 1920 /* Namespace */));
        if (candidate && candidate !***REMOVED******REMOVED*** unknownSymbol) {
          if (links) {
            links.jsxNamespace ***REMOVED*** candidate;
          }
          return candidate;
        }
      }
      if (links) {
        links.jsxNamespace ***REMOVED*** false;
      }
    }
    const s ***REMOVED*** resolveSymbol(getGlobalSymbol(
      JsxNames.JSX,
      1920 /* Namespace */,
      /*diagnostic*/
      void 0
    ));
    if (s ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return void 0;
    }
    return s;
  }
  function getNameFromJsxElementAttributesContainer(nameOfAttribPropContainer, jsxNamespace) {
    const jsxElementAttribPropInterfaceSym ***REMOVED*** jsxNamespace && getSymbol(jsxNamespace.exports, nameOfAttribPropContainer, 788968 /* Type */);
    const jsxElementAttribPropInterfaceType ***REMOVED*** jsxElementAttribPropInterfaceSym && getDeclaredTypeOfSymbol(jsxElementAttribPropInterfaceSym);
    const propertiesOfJsxElementAttribPropInterface ***REMOVED*** jsxElementAttribPropInterfaceType && getPropertiesOfType(jsxElementAttribPropInterfaceType);
    if (propertiesOfJsxElementAttribPropInterface) {
      if (propertiesOfJsxElementAttribPropInterface.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return "";
      } else if (propertiesOfJsxElementAttribPropInterface.length ***REMOVED******REMOVED******REMOVED*** 1) {
        return propertiesOfJsxElementAttribPropInterface[0].escapedName;
      } else if (propertiesOfJsxElementAttribPropInterface.length > 1 && jsxElementAttribPropInterfaceSym.declarations) {
        error(jsxElementAttribPropInterfaceSym.declarations[0], Diagnostics.The_global_type_JSX_0_may_not_have_more_than_one_property, unescapeLeadingUnderscores(nameOfAttribPropContainer));
      }
    }
    return void 0;
  }
  function getJsxLibraryManagedAttributes(jsxNamespace) {
    return jsxNamespace && getSymbol(jsxNamespace.exports, JsxNames.LibraryManagedAttributes, 788968 /* Type */);
  }
  function getJsxElementTypeSymbol(jsxNamespace) {
    return jsxNamespace && getSymbol(jsxNamespace.exports, JsxNames.ElementType, 788968 /* Type */);
  }
  function getJsxElementPropertiesName(jsxNamespace) {
    return getNameFromJsxElementAttributesContainer(JsxNames.ElementAttributesPropertyNameContainer, jsxNamespace);
  }
  function getJsxElementChildrenPropertyName(jsxNamespace) {
    if (compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
      return "children";
    }
    return getNameFromJsxElementAttributesContainer(JsxNames.ElementChildrenAttributeNameContainer, jsxNamespace);
  }
  function getUninstantiatedJsxSignaturesOfType(elementType, caller) {
    if (elementType.flags & 4 /* String */) {
      return [anySignature];
    } else if (elementType.flags & 128 /* StringLiteral */) {
      const intrinsicType ***REMOVED*** getIntrinsicAttributesTypeFromStringLiteralType(elementType, caller);
      if (!intrinsicType) {
        error(caller, Diagnostics.Property_0_does_not_exist_on_type_1, elementType.value, "JSX." + JsxNames.IntrinsicElements);
        return emptyArray;
      } else {
        const fakeSignature ***REMOVED*** createSignatureForJSXIntrinsic(caller, intrinsicType);
        return [fakeSignature];
      }
    }
    const apparentElemType ***REMOVED*** getApparentType(elementType);
    let signatures ***REMOVED*** getSignaturesOfType(apparentElemType, 1 /* Construct */);
    if (signatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      signatures ***REMOVED*** getSignaturesOfType(apparentElemType, 0 /* Call */);
    }
    if (signatures.length ***REMOVED******REMOVED******REMOVED*** 0 && apparentElemType.flags & 1048576 /* Union */) {
      signatures ***REMOVED*** getUnionSignatures(map(apparentElemType.types, (t) ***REMOVED***> getUninstantiatedJsxSignaturesOfType(t, caller)));
    }
    return signatures;
  }
  function getIntrinsicAttributesTypeFromStringLiteralType(type, location) {
    const intrinsicElementsType ***REMOVED*** getJsxType(JsxNames.IntrinsicElements, location);
    if (!isErrorType(intrinsicElementsType)) {
      const stringLiteralTypeName ***REMOVED*** type.value;
      const intrinsicProp ***REMOVED*** getPropertyOfType(intrinsicElementsType, escapeLeadingUnderscores(stringLiteralTypeName));
      if (intrinsicProp) {
        return getTypeOfSymbol(intrinsicProp);
      }
      const indexSignatureType ***REMOVED*** getIndexTypeOfType(intrinsicElementsType, stringType);
      if (indexSignatureType) {
        return indexSignatureType;
      }
      return void 0;
    }
    return anyType;
  }
  function checkJsxReturnAssignableToAppropriateBound(refKind, elemInstanceType, openingLikeElement) {
    if (refKind ***REMOVED******REMOVED******REMOVED*** 1 /* Function */) {
      const sfcReturnConstraint ***REMOVED*** getJsxStatelessElementTypeAt(openingLikeElement);
      if (sfcReturnConstraint) {
        checkTypeRelatedTo(elemInstanceType, sfcReturnConstraint, assignableRelation, openingLikeElement.tagName, Diagnostics.Its_return_type_0_is_not_a_valid_JSX_element, generateInitialErrorChain);
      }
    } else if (refKind ***REMOVED******REMOVED******REMOVED*** 0 /* Component */) {
      const classConstraint ***REMOVED*** getJsxElementClassTypeAt(openingLikeElement);
      if (classConstraint) {
        checkTypeRelatedTo(elemInstanceType, classConstraint, assignableRelation, openingLikeElement.tagName, Diagnostics.Its_instance_type_0_is_not_a_valid_JSX_element, generateInitialErrorChain);
      }
    } else {
      const sfcReturnConstraint ***REMOVED*** getJsxStatelessElementTypeAt(openingLikeElement);
      const classConstraint ***REMOVED*** getJsxElementClassTypeAt(openingLikeElement);
      if (!sfcReturnConstraint || !classConstraint) {
        return;
      }
      const combined ***REMOVED*** getUnionType([sfcReturnConstraint, classConstraint]);
      checkTypeRelatedTo(elemInstanceType, combined, assignableRelation, openingLikeElement.tagName, Diagnostics.Its_element_type_0_is_not_a_valid_JSX_element, generateInitialErrorChain);
    }
    function generateInitialErrorChain() {
      const componentName ***REMOVED*** getTextOfNode(openingLikeElement.tagName);
      return chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics._0_cannot_be_used_as_a_JSX_component,
        componentName
      );
    }
  }
  function getIntrinsicAttributesTypeFromJsxOpeningLikeElement(node) {
    var _a;
    Debug.assert(isJsxIntrinsicTagName(node.tagName));
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedJsxElementAttributesType) {
      const symbol ***REMOVED*** getIntrinsicTagSymbol(node);
      if (links.jsxFlags & 1 /* IntrinsicNamedElement */) {
        return links.resolvedJsxElementAttributesType ***REMOVED*** getTypeOfSymbol(symbol) || errorType;
      } else if (links.jsxFlags & 2 /* IntrinsicIndexedElement */) {
        const propName ***REMOVED*** isJsxNamespacedName(node.tagName) ? getEscapedTextOfJsxNamespacedName(node.tagName) : node.tagName.escapedText;
        return links.resolvedJsxElementAttributesType ***REMOVED*** ((_a ***REMOVED*** getApplicableIndexInfoForName(getJsxType(JsxNames.IntrinsicElements, node), propName)) ***REMOVED******REMOVED*** null ? void 0 : _a.type) || errorType;
      } else {
        return links.resolvedJsxElementAttributesType ***REMOVED*** errorType;
      }
    }
    return links.resolvedJsxElementAttributesType;
  }
  function getJsxElementClassTypeAt(location) {
    const type ***REMOVED*** getJsxType(JsxNames.ElementClass, location);
    if (isErrorType(type)) return void 0;
    return type;
  }
  function getJsxElementTypeAt(location) {
    return getJsxType(JsxNames.Element, location);
  }
  function getJsxStatelessElementTypeAt(location) {
    const jsxElementType ***REMOVED*** getJsxElementTypeAt(location);
    if (jsxElementType) {
      return getUnionType([jsxElementType, nullType]);
    }
  }
  function getJsxElementTypeTypeAt(location) {
    const ns ***REMOVED*** getJsxNamespaceAt(location);
    if (!ns) return void 0;
    const sym ***REMOVED*** getJsxElementTypeSymbol(ns);
    if (!sym) return void 0;
    const type ***REMOVED*** instantiateAliasOrInterfaceWithDefaults(sym, isInJSFile(location));
    if (!type || isErrorType(type)) return void 0;
    return type;
  }
  function instantiateAliasOrInterfaceWithDefaults(managedSym, inJs, ...typeArguments) {
    const declaredManagedType ***REMOVED*** getDeclaredTypeOfSymbol(managedSym);
    if (managedSym.flags & 524288 /* TypeAlias */) {
      const params ***REMOVED*** getSymbolLinks(managedSym).typeParameters;
      if (length(params) >***REMOVED*** typeArguments.length) {
        const args ***REMOVED*** fillMissingTypeArguments(typeArguments, params, typeArguments.length, inJs);
        return length(args) ***REMOVED******REMOVED******REMOVED*** 0 ? declaredManagedType : getTypeAliasInstantiation(managedSym, args);
      }
    }
    if (length(declaredManagedType.typeParameters) >***REMOVED*** typeArguments.length) {
      const args ***REMOVED*** fillMissingTypeArguments(typeArguments, declaredManagedType.typeParameters, typeArguments.length, inJs);
      return createTypeReference(declaredManagedType, args);
    }
    return void 0;
  }
  function getJsxIntrinsicTagNamesAt(location) {
    const intrinsics ***REMOVED*** getJsxType(JsxNames.IntrinsicElements, location);
    return intrinsics ? getPropertiesOfType(intrinsics) : emptyArray;
  }
  function checkJsxPreconditions(errorNode) {
    if ((compilerOptions.jsx || 0 /* None */) ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      error(errorNode, Diagnostics.Cannot_use_JSX_unless_the_jsx_flag_is_provided);
    }
    if (getJsxElementTypeAt(errorNode) ***REMOVED******REMOVED******REMOVED*** void 0) {
      if (noImplicitAny) {
        error(errorNode, Diagnostics.JSX_element_implicitly_has_type_any_because_the_global_type_JSX_Element_does_not_exist);
      }
    }
  }
  function checkJsxOpeningLikeElementOrOpeningFragment(node) {
    const isNodeOpeningLikeElement ***REMOVED*** isJsxOpeningLikeElement(node);
    if (isNodeOpeningLikeElement) {
      checkGrammarJsxElement(node);
    }
    checkJsxPreconditions(node);
    markJsxAliasReferenced(node);
    const sig ***REMOVED*** getResolvedSignature(node);
    checkDeprecatedSignature(sig, node);
    if (isNodeOpeningLikeElement) {
      const jsxOpeningLikeNode ***REMOVED*** node;
      const elementTypeConstraint ***REMOVED*** getJsxElementTypeTypeAt(jsxOpeningLikeNode);
      if (elementTypeConstraint !***REMOVED******REMOVED*** void 0) {
        const tagName ***REMOVED*** jsxOpeningLikeNode.tagName;
        const tagType ***REMOVED*** isJsxIntrinsicTagName(tagName) ? getStringLiteralType(intrinsicTagNameToString(tagName)) : checkExpression(tagName);
        checkTypeRelatedTo(tagType, elementTypeConstraint, assignableRelation, tagName, Diagnostics.Its_type_0_is_not_a_valid_JSX_element_type, () ***REMOVED***> {
          const componentName ***REMOVED*** getTextOfNode(tagName);
          return chainDiagnosticMessages(
            /*details*/
            void 0,
            Diagnostics._0_cannot_be_used_as_a_JSX_component,
            componentName
          );
        });
      } else {
        checkJsxReturnAssignableToAppropriateBound(getJsxReferenceKind(jsxOpeningLikeNode), getReturnTypeOfSignature(sig), jsxOpeningLikeNode);
      }
    }
  }
  function isKnownProperty(targetType, name, isComparingJsxAttributes) {
    if (targetType.flags & 524288 /* Object */) {
      if (getPropertyOfObjectType(targetType, name) || getApplicableIndexInfoForName(targetType, name) || isLateBoundName(name) && getIndexInfoOfType(targetType, stringType) || isComparingJsxAttributes && isHyphenatedJsxName(name)) {
        return true;
      }
    }
    if (targetType.flags & 33554432 /* Substitution */) {
      return isKnownProperty(targetType.baseType, name, isComparingJsxAttributes);
    }
    if (targetType.flags & 3145728 /* UnionOrIntersection */ && isExcessPropertyCheckTarget(targetType)) {
      for (const t of targetType.types) {
        if (isKnownProperty(t, name, isComparingJsxAttributes)) {
          return true;
        }
      }
    }
    return false;
  }
  function isExcessPropertyCheckTarget(type) {
    return !!(type.flags & 524288 /* Object */ && !(getObjectFlags(type) & 512 /* ObjectLiteralPatternWithComputedProperties */) || type.flags & 67108864 /* NonPrimitive */ || type.flags & 33554432 /* Substitution */ && isExcessPropertyCheckTarget(type.baseType) || type.flags & 1048576 /* Union */ && some(type.types, isExcessPropertyCheckTarget) || type.flags & 2097152 /* Intersection */ && every(type.types, isExcessPropertyCheckTarget));
  }
  function checkJsxExpression(node, checkMode) {
    checkGrammarJsxExpression(node);
    if (node.expression) {
      const type ***REMOVED*** checkExpression(node.expression, checkMode);
      if (node.dotDotDotToken && type !***REMOVED******REMOVED*** anyType && !isArrayType(type)) {
        error(node, Diagnostics.JSX_spread_child_must_be_an_array_type);
      }
      return type;
    } else {
      return errorType;
    }
  }
  function getDeclarationNodeFlagsFromSymbol(s) {
    return s.valueDeclaration ? getCombinedNodeFlagsCached(s.valueDeclaration) : 0;
  }
  function isPrototypeProperty(symbol) {
    if (symbol.flags & 8192 /* Method */ || getCheckFlags(symbol) & 4 /* SyntheticMethod */) {
      return true;
    }
    if (isInJSFile(symbol.valueDeclaration)) {
      const parent ***REMOVED*** symbol.valueDeclaration.parent;
      return parent && isBinaryExpression(parent) && getAssignmentDeclarationKind(parent) ***REMOVED******REMOVED******REMOVED*** 3 /* PrototypeProperty */;
    }
  }
  function checkPropertyAccessibility(node, isSuper, writing, type, prop, reportError ***REMOVED*** true) {
    const errorNode ***REMOVED*** !reportError ? void 0 : node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */ ? node.right : node.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */ ? node : node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && node.propertyName ? node.propertyName : node.name;
    return checkPropertyAccessibilityAtLocation(node, isSuper, writing, type, prop, errorNode);
  }
  function checkPropertyAccessibilityAtLocation(location, isSuper, writing, containingType, prop, errorNode) {
    var _a;
    const flags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(prop, writing);
    if (isSuper) {
      if (languageVersion < 2 /* ES2015 */) {
        if (symbolHasNonMethodDeclaration(prop)) {
          if (errorNode) {
            error(errorNode, Diagnostics.Only_public_and_protected_methods_of_the_base_class_are_accessible_via_the_super_keyword);
          }
          return false;
        }
      }
      if (flags & 64 /* Abstract */) {
        if (errorNode) {
          error(errorNode, Diagnostics.Abstract_method_0_in_class_1_cannot_be_accessed_via_super_expression, symbolToString(prop), typeToString(getDeclaringClass(prop)));
        }
        return false;
      }
      if (!(flags & 256 /* Static */) && ((_a ***REMOVED*** prop.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.some(isClassInstanceProperty))) {
        if (errorNode) {
          error(errorNode, Diagnostics.Class_field_0_defined_by_the_parent_class_is_not_accessible_in_the_child_class_via_super, symbolToString(prop));
        }
        return false;
      }
    }
    if (flags & 64 /* Abstract */ && symbolHasNonMethodDeclaration(prop) && (isThisProperty(location) || isThisInitializedObjectBindingExpression(location) || isObjectBindingPattern(location.parent) && isThisInitializedDeclaration(location.parent.parent))) {
      const declaringClassDeclaration ***REMOVED*** getClassLikeDeclarationOfSymbol(getParentOfSymbol(prop));
      if (declaringClassDeclaration && isNodeUsedDuringClassInitialization(location)) {
        if (errorNode) {
          error(errorNode, Diagnostics.Abstract_property_0_in_class_1_cannot_be_accessed_in_the_constructor, symbolToString(prop), getTextOfIdentifierOrLiteral(declaringClassDeclaration.name));
        }
        return false;
      }
    }
    if (!(flags & 6 /* NonPublicAccessibilityModifier */)) {
      return true;
    }
    if (flags & 2 /* Private */) {
      const declaringClassDeclaration ***REMOVED*** getClassLikeDeclarationOfSymbol(getParentOfSymbol(prop));
      if (!isNodeWithinClass(location, declaringClassDeclaration)) {
        if (errorNode) {
          error(errorNode, Diagnostics.Property_0_is_private_and_only_accessible_within_class_1, symbolToString(prop), typeToString(getDeclaringClass(prop)));
        }
        return false;
      }
      return true;
    }
    if (isSuper) {
      return true;
    }
    let enclosingClass ***REMOVED*** forEachEnclosingClass(location, (enclosingDeclaration) ***REMOVED***> {
      const enclosingClass2 ***REMOVED*** getDeclaredTypeOfSymbol(getSymbolOfDeclaration(enclosingDeclaration));
      return isClassDerivedFromDeclaringClasses(enclosingClass2, prop, writing);
    });
    if (!enclosingClass) {
      enclosingClass ***REMOVED*** getEnclosingClassFromThisParameter(location);
      enclosingClass ***REMOVED*** enclosingClass && isClassDerivedFromDeclaringClasses(enclosingClass, prop, writing);
      if (flags & 256 /* Static */ || !enclosingClass) {
        if (errorNode) {
          error(errorNode, Diagnostics.Property_0_is_protected_and_only_accessible_within_class_1_and_its_subclasses, symbolToString(prop), typeToString(getDeclaringClass(prop) || containingType));
        }
        return false;
      }
    }
    if (flags & 256 /* Static */) {
      return true;
    }
    if (containingType.flags & 262144 /* TypeParameter */) {
      containingType ***REMOVED*** containingType.isThisType ? getConstraintOfTypeParameter(containingType) : getBaseConstraintOfType(containingType);
    }
    if (!containingType || !hasBaseType(containingType, enclosingClass)) {
      if (errorNode) {
        error(errorNode, Diagnostics.Property_0_is_protected_and_only_accessible_through_an_instance_of_class_1_This_is_an_instance_of_class_2, symbolToString(prop), typeToString(enclosingClass), typeToString(containingType));
      }
      return false;
    }
    return true;
  }
  function getEnclosingClassFromThisParameter(node) {
    const thisParameter ***REMOVED*** getThisParameterFromNodeContext(node);
    let thisType ***REMOVED*** (thisParameter ***REMOVED******REMOVED*** null ? void 0 : thisParameter.type) && getTypeFromTypeNode(thisParameter.type);
    if (thisType) {
      if (thisType.flags & 262144 /* TypeParameter */) {
        thisType ***REMOVED*** getConstraintOfTypeParameter(thisType);
      }
    } else {
      const thisContainer ***REMOVED*** getThisContainer(
        node,
        /*includeArrowFunctions*/
        false,
        /*includeClassComputedPropertyName*/
        false
      );
      if (isFunctionLike(thisContainer)) {
        thisType ***REMOVED*** getContextualThisParameterType(thisContainer);
      }
    }
    if (thisType && getObjectFlags(thisType) & (3 /* ClassOrInterface */ | 4 /* Reference */)) {
      return getTargetType(thisType);
    }
    return void 0;
  }
  function getThisParameterFromNodeContext(node) {
    const thisContainer ***REMOVED*** getThisContainer(
      node,
      /*includeArrowFunctions*/
      false,
      /*includeClassComputedPropertyName*/
      false
    );
    return thisContainer && isFunctionLike(thisContainer) ? getThisParameter(thisContainer) : void 0;
  }
  function symbolHasNonMethodDeclaration(symbol) {
    return !!forEachProperty(symbol, (prop) ***REMOVED***> !(prop.flags & 8192 /* Method */));
  }
  function checkNonNullExpression(node) {
    return checkNonNullType(checkExpression(node), node);
  }
  function isNullableType(type) {
    return hasTypeFacts(type, 50331648 /* IsUndefinedOrNull */);
  }
  function getNonNullableTypeIfNeeded(type) {
    return isNullableType(type) ? getNonNullableType(type) : type;
  }
  function reportObjectPossiblyNullOrUndefinedError(node, facts) {
    const nodeText ***REMOVED*** isEntityNameExpression(node) ? entityNameToString(node) : void 0;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */) {
      error(node, Diagnostics.The_value_0_cannot_be_used_here, "null");
      return;
    }
    if (nodeText !***REMOVED******REMOVED*** void 0 && nodeText.length < 100) {
      if (isIdentifier(node) && nodeText ***REMOVED******REMOVED******REMOVED*** "undefined") {
        error(node, Diagnostics.The_value_0_cannot_be_used_here, "undefined");
        return;
      }
      error(
        node,
        facts & 16777216 /* IsUndefined */ ? facts & 33554432 /* IsNull */ ? Diagnostics._0_is_possibly_null_or_undefined : Diagnostics._0_is_possibly_undefined : Diagnostics._0_is_possibly_null,
        nodeText
      );
    } else {
      error(
        node,
        facts & 16777216 /* IsUndefined */ ? facts & 33554432 /* IsNull */ ? Diagnostics.Object_is_possibly_null_or_undefined : Diagnostics.Object_is_possibly_undefined : Diagnostics.Object_is_possibly_null
      );
    }
  }
  function reportCannotInvokePossiblyNullOrUndefinedError(node, facts) {
    error(
      node,
      facts & 16777216 /* IsUndefined */ ? facts & 33554432 /* IsNull */ ? Diagnostics.Cannot_invoke_an_object_which_is_possibly_null_or_undefined : Diagnostics.Cannot_invoke_an_object_which_is_possibly_undefined : Diagnostics.Cannot_invoke_an_object_which_is_possibly_null
    );
  }
  function checkNonNullTypeWithReporter(type, node, reportError) {
    if (strictNullChecks && type.flags & 2 /* Unknown */) {
      if (isEntityNameExpression(node)) {
        const nodeText ***REMOVED*** entityNameToString(node);
        if (nodeText.length < 100) {
          error(node, Diagnostics._0_is_of_type_unknown, nodeText);
          return errorType;
        }
      }
      error(node, Diagnostics.Object_is_of_type_unknown);
      return errorType;
    }
    const facts ***REMOVED*** getTypeFacts(type, 50331648 /* IsUndefinedOrNull */);
    if (facts & 50331648 /* IsUndefinedOrNull */) {
      reportError(node, facts);
      const t ***REMOVED*** getNonNullableType(type);
      return t.flags & (98304 /* Nullable */ | 131072 /* Never */) ? errorType : t;
    }
    return type;
  }
  function checkNonNullType(type, node) {
    return checkNonNullTypeWithReporter(type, node, reportObjectPossiblyNullOrUndefinedError);
  }
  function checkNonNullNonVoidType(type, node) {
    const nonNullType ***REMOVED*** checkNonNullType(type, node);
    if (nonNullType.flags & 16384 /* Void */) {
      if (isEntityNameExpression(node)) {
        const nodeText ***REMOVED*** entityNameToString(node);
        if (isIdentifier(node) && nodeText ***REMOVED******REMOVED******REMOVED*** "undefined") {
          error(node, Diagnostics.The_value_0_cannot_be_used_here, nodeText);
          return nonNullType;
        }
        if (nodeText.length < 100) {
          error(node, Diagnostics._0_is_possibly_undefined, nodeText);
          return nonNullType;
        }
      }
      error(node, Diagnostics.Object_is_possibly_undefined);
    }
    return nonNullType;
  }
  function checkPropertyAccessExpression(node, checkMode, writeOnly) {
    return node.flags & 64 /* OptionalChain */ ? checkPropertyAccessChain(node, checkMode) : checkPropertyAccessExpressionOrQualifiedName(node, node.expression, checkNonNullExpression(node.expression), node.name, checkMode, writeOnly);
  }
  function checkPropertyAccessChain(node, checkMode) {
    const leftType ***REMOVED*** checkExpression(node.expression);
    const nonOptionalType ***REMOVED*** getOptionalExpressionType(leftType, node.expression);
    return propagateOptionalTypeMarker(checkPropertyAccessExpressionOrQualifiedName(node, node.expression, checkNonNullType(nonOptionalType, node.expression), node.name, checkMode), node, nonOptionalType !***REMOVED******REMOVED*** leftType);
  }
  function checkQualifiedName(node, checkMode) {
    const leftType ***REMOVED*** isPartOfTypeQuery(node) && isThisIdentifier(node.left) ? checkNonNullType(checkThisExpression(node.left), node.left) : checkNonNullExpression(node.left);
    return checkPropertyAccessExpressionOrQualifiedName(node, node.left, leftType, node.right, checkMode);
  }
  function isMethodAccessForCall(node) {
    while (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */) {
      node ***REMOVED*** node.parent;
    }
    return isCallOrNewExpression(node.parent) && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node;
  }
  function lookupSymbolForPrivateIdentifierDeclaration(propName, location) {
    for (let containingClass ***REMOVED*** getContainingClassExcludingClassDecorators(location); !!containingClass; containingClass ***REMOVED*** getContainingClass(containingClass)) {
      const { symbol } ***REMOVED*** containingClass;
      const name ***REMOVED*** getSymbolNameForPrivateIdentifier(symbol, propName);
      const prop ***REMOVED*** symbol.members && symbol.members.get(name) || symbol.exports && symbol.exports.get(name);
      if (prop) {
        return prop;
      }
    }
  }
  function checkGrammarPrivateIdentifierExpression(privId) {
    if (!getContainingClass(privId)) {
      return grammarErrorOnNode(privId, Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
    }
    if (!isForInStatement(privId.parent)) {
      if (!isExpressionNode(privId)) {
        return grammarErrorOnNode(privId, Diagnostics.Private_identifiers_are_only_allowed_in_class_bodies_and_may_only_be_used_as_part_of_a_class_member_declaration_property_access_or_on_the_left_hand_side_of_an_in_expression);
      }
      const isInOperation ***REMOVED*** isBinaryExpression(privId.parent) && privId.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */;
      if (!getSymbolForPrivateIdentifierExpression(privId) && !isInOperation) {
        return grammarErrorOnNode(privId, Diagnostics.Cannot_find_name_0, idText(privId));
      }
    }
    return false;
  }
  function checkPrivateIdentifierExpression(privId) {
    checkGrammarPrivateIdentifierExpression(privId);
    const symbol ***REMOVED*** getSymbolForPrivateIdentifierExpression(privId);
    if (symbol) {
      markPropertyAsReferenced(
        symbol,
        /*nodeForCheckWriteOnly*/
        void 0,
        /*isSelfTypeAccess*/
        false
      );
    }
    return anyType;
  }
  function getSymbolForPrivateIdentifierExpression(privId) {
    if (!isExpressionNode(privId)) {
      return void 0;
    }
    const links ***REMOVED*** getNodeLinks(privId);
    if (links.resolvedSymbol ***REMOVED******REMOVED******REMOVED*** void 0) {
      links.resolvedSymbol ***REMOVED*** lookupSymbolForPrivateIdentifierDeclaration(privId.escapedText, privId);
    }
    return links.resolvedSymbol;
  }
  function getPrivateIdentifierPropertyOfType(leftType, lexicallyScopedIdentifier) {
    return getPropertyOfType(leftType, lexicallyScopedIdentifier.escapedName);
  }
  function checkPrivateIdentifierPropertyAccess(leftType, right, lexicallyScopedIdentifier) {
    let propertyOnType;
    const properties ***REMOVED*** getPropertiesOfType(leftType);
    if (properties) {
      forEach(properties, (symbol) ***REMOVED***> {
        const decl ***REMOVED*** symbol.valueDeclaration;
        if (decl && isNamedDeclaration(decl) && isPrivateIdentifier(decl.name) && decl.name.escapedText ***REMOVED******REMOVED******REMOVED*** right.escapedText) {
          propertyOnType ***REMOVED*** symbol;
          return true;
        }
      });
    }
    const diagName ***REMOVED*** diagnosticName(right);
    if (propertyOnType) {
      const typeValueDecl ***REMOVED*** Debug.checkDefined(propertyOnType.valueDeclaration);
      const typeClass ***REMOVED*** Debug.checkDefined(getContainingClass(typeValueDecl));
      if (lexicallyScopedIdentifier ***REMOVED******REMOVED*** null ? void 0 : lexicallyScopedIdentifier.valueDeclaration) {
        const lexicalValueDecl ***REMOVED*** lexicallyScopedIdentifier.valueDeclaration;
        const lexicalClass ***REMOVED*** getContainingClass(lexicalValueDecl);
        Debug.assert(!!lexicalClass);
        if (findAncestor(lexicalClass, (n) ***REMOVED***> typeClass ***REMOVED******REMOVED******REMOVED*** n)) {
          const diagnostic ***REMOVED*** error(
            right,
            Diagnostics.The_property_0_cannot_be_accessed_on_type_1_within_this_class_because_it_is_shadowed_by_another_private_identifier_with_the_same_spelling,
            diagName,
            typeToString(leftType)
          );
          addRelatedInfo(
            diagnostic,
            createDiagnosticForNode(
              lexicalValueDecl,
              Diagnostics.The_shadowing_declaration_of_0_is_defined_here,
              diagName
            ),
            createDiagnosticForNode(
              typeValueDecl,
              Diagnostics.The_declaration_of_0_that_you_probably_intended_to_use_is_defined_here,
              diagName
            )
          );
          return true;
        }
      }
      error(
        right,
        Diagnostics.Property_0_is_not_accessible_outside_class_1_because_it_has_a_private_identifier,
        diagName,
        diagnosticName(typeClass.name || anon)
      );
      return true;
    }
    return false;
  }
  function isThisPropertyAccessInConstructor(node, prop) {
    return (isConstructorDeclaredProperty(prop) || isThisProperty(node) && isAutoTypedProperty(prop)) && getThisContainer(
      node,
      /*includeArrowFunctions*/
      true,
      /*includeClassComputedPropertyName*/
      false
    ) ***REMOVED******REMOVED******REMOVED*** getDeclaringConstructor(prop);
  }
  function checkPropertyAccessExpressionOrQualifiedName(node, left, leftType, right, checkMode, writeOnly) {
    const parentSymbol ***REMOVED*** getNodeLinks(left).resolvedSymbol;
    const assignmentKind ***REMOVED*** getAssignmentTargetKind(node);
    const apparentType ***REMOVED*** getApparentType(assignmentKind !***REMOVED******REMOVED*** 0 /* None */ || isMethodAccessForCall(node) ? getWidenedType(leftType) : leftType);
    const isAnyLike ***REMOVED*** isTypeAny(apparentType) || apparentType ***REMOVED******REMOVED******REMOVED*** silentNeverType;
    let prop;
    if (isPrivateIdentifier(right)) {
      if (languageVersion < LanguageFeatureMinimumTarget.PrivateNamesAndClassStaticBlocks || languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators || !useDefineForClassFields) {
        if (assignmentKind !***REMOVED******REMOVED*** 0 /* None */) {
          checkExternalEmitHelpers(node, 1048576 /* ClassPrivateFieldSet */);
        }
        if (assignmentKind !***REMOVED******REMOVED*** 1 /* Definite */) {
          checkExternalEmitHelpers(node, 524288 /* ClassPrivateFieldGet */);
        }
      }
      const lexicallyScopedSymbol ***REMOVED*** lookupSymbolForPrivateIdentifierDeclaration(right.escapedText, right);
      if (assignmentKind && lexicallyScopedSymbol && lexicallyScopedSymbol.valueDeclaration && isMethodDeclaration(lexicallyScopedSymbol.valueDeclaration)) {
        grammarErrorOnNode(right, Diagnostics.Cannot_assign_to_private_method_0_Private_methods_are_not_writable, idText(right));
      }
      if (isAnyLike) {
        if (lexicallyScopedSymbol) {
          return isErrorType(apparentType) ? errorType : apparentType;
        }
        if (getContainingClassExcludingClassDecorators(right) ***REMOVED******REMOVED******REMOVED*** void 0) {
          grammarErrorOnNode(right, Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
          return anyType;
        }
      }
      prop ***REMOVED*** lexicallyScopedSymbol && getPrivateIdentifierPropertyOfType(leftType, lexicallyScopedSymbol);
      if (prop ***REMOVED******REMOVED******REMOVED*** void 0) {
        if (checkPrivateIdentifierPropertyAccess(leftType, right, lexicallyScopedSymbol)) {
          return errorType;
        }
        const containingClass ***REMOVED*** getContainingClassExcludingClassDecorators(right);
        if (containingClass && isPlainJsFile(getSourceFileOfNode(containingClass), compilerOptions.checkJs)) {
          grammarErrorOnNode(right, Diagnostics.Private_field_0_must_be_declared_in_an_enclosing_class, idText(right));
        }
      } else {
        const isSetonlyAccessor ***REMOVED*** prop.flags & 65536 /* SetAccessor */ && !(prop.flags & 32768 /* GetAccessor */);
        if (isSetonlyAccessor && assignmentKind !***REMOVED******REMOVED*** 1 /* Definite */) {
          error(node, Diagnostics.Private_accessor_was_defined_without_a_getter);
        }
      }
    } else {
      if (isAnyLike) {
        if (isIdentifier(left) && parentSymbol) {
          markLinkedReferences(
            node,
            2 /* Property */,
            /*propSymbol*/
            void 0,
            leftType
          );
        }
        return isErrorType(apparentType) ? errorType : apparentType;
      }
      prop ***REMOVED*** getPropertyOfType(
        apparentType,
        right.escapedText,
        /*skipObjectFunctionPropertyAugment*/
        isConstEnumObjectType(apparentType),
        /*includeTypeOnlyMembers*/
        node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */
      );
    }
    markLinkedReferences(node, 2 /* Property */, prop, leftType);
    let propType;
    if (!prop) {
      const indexInfo ***REMOVED*** !isPrivateIdentifier(right) && (assignmentKind ***REMOVED******REMOVED******REMOVED*** 0 /* None */ || !isGenericObjectType(leftType) || isThisTypeParameter(leftType)) ? getApplicableIndexInfoForName(apparentType, right.escapedText) : void 0;
      if (!(indexInfo && indexInfo.type)) {
        const isUncheckedJS ***REMOVED*** isUncheckedJSSuggestion(
          node,
          leftType.symbol,
          /*excludeClasses*/
          true
        );
        if (!isUncheckedJS && isJSLiteralType(leftType)) {
          return anyType;
        }
        if (leftType.symbol ***REMOVED******REMOVED******REMOVED*** globalThisSymbol) {
          if (globalThisSymbol.exports.has(right.escapedText) && globalThisSymbol.exports.get(right.escapedText).flags & 418 /* BlockScoped */) {
            error(right, Diagnostics.Property_0_does_not_exist_on_type_1, unescapeLeadingUnderscores(right.escapedText), typeToString(leftType));
          } else if (noImplicitAny) {
            error(right, Diagnostics.Element_implicitly_has_an_any_type_because_type_0_has_no_index_signature, typeToString(leftType));
          }
          return anyType;
        }
        if (right.escapedText && !checkAndReportErrorForExtendingInterface(node)) {
          reportNonexistentProperty(right, isThisTypeParameter(leftType) ? apparentType : leftType, isUncheckedJS);
        }
        return errorType;
      }
      if (indexInfo.isReadonly && (isAssignmentTarget(node) || isDeleteTarget(node))) {
        error(node, Diagnostics.Index_signature_in_type_0_only_permits_reading, typeToString(apparentType));
      }
      propType ***REMOVED*** indexInfo.type;
      if (compilerOptions.noUncheckedIndexedAccess && getAssignmentTargetKind(node) !***REMOVED******REMOVED*** 1 /* Definite */) {
        propType ***REMOVED*** getUnionType([propType, missingType]);
      }
      if (compilerOptions.noPropertyAccessFromIndexSignature && isPropertyAccessExpression(node)) {
        error(right, Diagnostics.Property_0_comes_from_an_index_signature_so_it_must_be_accessed_with_0, unescapeLeadingUnderscores(right.escapedText));
      }
      if (indexInfo.declaration && isDeprecatedDeclaration(indexInfo.declaration)) {
        addDeprecatedSuggestion(right, [indexInfo.declaration], right.escapedText);
      }
    } else {
      const targetPropSymbol ***REMOVED*** resolveAliasWithDeprecationCheck(prop, right);
      if (isDeprecatedSymbol(targetPropSymbol) && isUncalledFunctionReference(node, targetPropSymbol) && targetPropSymbol.declarations) {
        addDeprecatedSuggestion(right, targetPropSymbol.declarations, right.escapedText);
      }
      checkPropertyNotUsedBeforeDeclaration(prop, node, right);
      markPropertyAsReferenced(prop, node, isSelfTypeAccess(left, parentSymbol));
      getNodeLinks(node).resolvedSymbol ***REMOVED*** prop;
      checkPropertyAccessibility(node, left.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */, isWriteAccess(node), apparentType, prop);
      if (isAssignmentToReadonlyEntity(node, prop, assignmentKind)) {
        error(right, Diagnostics.Cannot_assign_to_0_because_it_is_a_read_only_property, idText(right));
        return errorType;
      }
      propType ***REMOVED*** isThisPropertyAccessInConstructor(node, prop) ? autoType : writeOnly || isWriteOnlyAccess(node) ? getWriteTypeOfSymbol(prop) : getTypeOfSymbol(prop);
    }
    return getFlowTypeOfAccessExpression(node, prop, propType, right, checkMode);
  }
  function isUncheckedJSSuggestion(node, suggestion, excludeClasses) {
    var _a;
    const file ***REMOVED*** getSourceFileOfNode(node);
    if (file) {
      if (compilerOptions.checkJs ***REMOVED******REMOVED******REMOVED*** void 0 && file.checkJsDirective ***REMOVED******REMOVED******REMOVED*** void 0 && (file.scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || file.scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */)) {
        const declarationFile ***REMOVED*** forEach(suggestion ***REMOVED******REMOVED*** null ? void 0 : suggestion.declarations, getSourceFileOfNode);
        const suggestionHasNoExtendsOrDecorators ***REMOVED*** !(suggestion ***REMOVED******REMOVED*** null ? void 0 : suggestion.valueDeclaration) || !isClassLike(suggestion.valueDeclaration) || ((_a ***REMOVED*** suggestion.valueDeclaration.heritageClauses) ***REMOVED******REMOVED*** null ? void 0 : _a.length) || classOrConstructorParameterIsDecorated(
          /*useLegacyDecorators*/
          false,
          suggestion.valueDeclaration
        );
        return !(file !***REMOVED******REMOVED*** declarationFile && !!declarationFile && isGlobalSourceFile(declarationFile)) && !(excludeClasses && suggestion && suggestion.flags & 32 /* Class */ && suggestionHasNoExtendsOrDecorators) && !(!!node && excludeClasses && isPropertyAccessExpression(node) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && suggestionHasNoExtendsOrDecorators);
      }
    }
    return false;
  }
  function getFlowTypeOfAccessExpression(node, prop, propType, errorNode, checkMode) {
    const assignmentKind ***REMOVED*** getAssignmentTargetKind(node);
    if (assignmentKind ***REMOVED******REMOVED******REMOVED*** 1 /* Definite */) {
      return removeMissingType(propType, !!(prop && prop.flags & 16777216 /* Optional */));
    }
    if (prop && !(prop.flags & (3 /* Variable */ | 4 /* Property */ | 98304 /* Accessor */)) && !(prop.flags & 8192 /* Method */ && propType.flags & 1048576 /* Union */) && !isDuplicatedCommonJSExport(prop.declarations)) {
      return propType;
    }
    if (propType ***REMOVED******REMOVED******REMOVED*** autoType) {
      return getFlowTypeOfProperty(node, prop);
    }
    propType ***REMOVED*** getNarrowableTypeForReference(propType, node, checkMode);
    let assumeUninitialized ***REMOVED*** false;
    if (strictNullChecks && strictPropertyInitialization && isAccessExpression(node) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      const declaration ***REMOVED*** prop && prop.valueDeclaration;
      if (declaration && isPropertyWithoutInitializer(declaration)) {
        if (!isStatic(declaration)) {
          const flowContainer ***REMOVED*** getControlFlowContainer(node);
          if (flowContainer.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ && flowContainer.parent ***REMOVED******REMOVED******REMOVED*** declaration.parent && !(declaration.flags & 33554432 /* Ambient */)) {
            assumeUninitialized ***REMOVED*** true;
          }
        }
      }
    } else if (strictNullChecks && prop && prop.valueDeclaration && isPropertyAccessExpression(prop.valueDeclaration) && getAssignmentDeclarationPropertyAccessKind(prop.valueDeclaration) && getControlFlowContainer(node) ***REMOVED******REMOVED******REMOVED*** getControlFlowContainer(prop.valueDeclaration)) {
      assumeUninitialized ***REMOVED*** true;
    }
    const flowType ***REMOVED*** getFlowTypeOfReference(node, propType, assumeUninitialized ? getOptionalType(propType) : propType);
    if (assumeUninitialized && !containsUndefinedType(propType) && containsUndefinedType(flowType)) {
      error(errorNode, Diagnostics.Property_0_is_used_before_being_assigned, symbolToString(prop));
      return propType;
    }
    return assignmentKind ? getBaseTypeOfLiteralType(flowType) : flowType;
  }
  function checkPropertyNotUsedBeforeDeclaration(prop, node, right) {
    const { valueDeclaration } ***REMOVED*** prop;
    if (!valueDeclaration || getSourceFileOfNode(node).isDeclarationFile) {
      return;
    }
    let diagnosticMessage;
    const declarationName ***REMOVED*** idText(right);
    if (isInPropertyInitializerOrClassStaticBlock(node) && !isOptionalPropertyDeclaration(valueDeclaration) && !(isAccessExpression(node) && isAccessExpression(node.expression)) && !isBlockScopedNameDeclaredBeforeUse(valueDeclaration, right) && !(isMethodDeclaration(valueDeclaration) && getCombinedModifierFlagsCached(valueDeclaration) & 256 /* Static */) && (useDefineForClassFields || !isPropertyDeclaredInAncestorClass(prop))) {
      diagnosticMessage ***REMOVED*** error(right, Diagnostics.Property_0_is_used_before_its_initialization, declarationName);
    } else if (valueDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ && node.parent.kind !***REMOVED******REMOVED*** 184 /* TypeReference */ && !(valueDeclaration.flags & 33554432 /* Ambient */) && !isBlockScopedNameDeclaredBeforeUse(valueDeclaration, right)) {
      diagnosticMessage ***REMOVED*** error(right, Diagnostics.Class_0_used_before_its_declaration, declarationName);
    }
    if (diagnosticMessage) {
      addRelatedInfo(diagnosticMessage, createDiagnosticForNode(valueDeclaration, Diagnostics._0_is_declared_here, declarationName));
    }
  }
  function isInPropertyInitializerOrClassStaticBlock(node, ignoreArrowFunctions) {
    return !!findAncestor(node, (node2) ***REMOVED***> {
      switch (node2.kind) {
        case 173 /* PropertyDeclaration */:
        case 176 /* ClassStaticBlockDeclaration */:
          return true;
        case 187 /* TypeQuery */:
        case 288 /* JsxClosingElement */:
          return "quit";
        case 220 /* ArrowFunction */:
          return ignoreArrowFunctions ? false : "quit";
        case 242 /* Block */:
          return isFunctionLikeDeclaration(node2.parent) && node2.parent.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */ ? "quit" : false;
        default:
          return false;
      }
    });
  }
  function isPropertyDeclaredInAncestorClass(prop) {
    if (!(prop.parent.flags & 32 /* Class */)) {
      return false;
    }
    let classType ***REMOVED*** getTypeOfSymbol(prop.parent);
    while (true) {
      classType ***REMOVED*** classType.symbol && getSuperClass(classType);
      if (!classType) {
        return false;
      }
      const superProperty ***REMOVED*** getPropertyOfType(classType, prop.escapedName);
      if (superProperty && superProperty.valueDeclaration) {
        return true;
      }
    }
  }
  function getSuperClass(classType) {
    const x ***REMOVED*** getBaseTypes(classType);
    if (x.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return void 0;
    }
    return getIntersectionType(x);
  }
  function reportNonexistentProperty(propNode, containingType, isUncheckedJS) {
    const links ***REMOVED*** getNodeLinks(propNode);
    const cache ***REMOVED*** links.nonExistentPropCheckCache || (links.nonExistentPropCheckCache ***REMOVED*** /* @__PURE__ */ new Set());
    const key ***REMOVED*** `${getTypeId(containingType)}|${isUncheckedJS}`;
    if (cache.has(key)) {
      return;
    }
    cache.add(key);
    let errorInfo;
    let relatedInfo;
    if (!isPrivateIdentifier(propNode) && containingType.flags & 1048576 /* Union */ && !(containingType.flags & 402784252 /* Primitive */)) {
      for (const subtype of containingType.types) {
        if (!getPropertyOfType(subtype, propNode.escapedText) && !getApplicableIndexInfoForName(subtype, propNode.escapedText)) {
          errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, Diagnostics.Property_0_does_not_exist_on_type_1, declarationNameToString(propNode), typeToString(subtype));
          break;
        }
      }
    }
    if (typeHasStaticProperty(propNode.escapedText, containingType)) {
      const propName ***REMOVED*** declarationNameToString(propNode);
      const typeName ***REMOVED*** typeToString(containingType);
      errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, Diagnostics.Property_0_does_not_exist_on_type_1_Did_you_mean_to_access_the_static_member_2_instead, propName, typeName, typeName + "." + propName);
    } else {
      const promisedType ***REMOVED*** getPromisedTypeOfPromise(containingType);
      if (promisedType && getPropertyOfType(promisedType, propNode.escapedText)) {
        errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, Diagnostics.Property_0_does_not_exist_on_type_1, declarationNameToString(propNode), typeToString(containingType));
        relatedInfo ***REMOVED*** createDiagnosticForNode(propNode, Diagnostics.Did_you_forget_to_use_await);
      } else {
        const missingProperty ***REMOVED*** declarationNameToString(propNode);
        const container ***REMOVED*** typeToString(containingType);
        const libSuggestion ***REMOVED*** getSuggestedLibForNonExistentProperty(missingProperty, containingType);
        if (libSuggestion !***REMOVED******REMOVED*** void 0) {
          errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, Diagnostics.Property_0_does_not_exist_on_type_1_Do_you_need_to_change_your_target_library_Try_changing_the_lib_compiler_option_to_2_or_later, missingProperty, container, libSuggestion);
        } else {
          const suggestion ***REMOVED*** getSuggestedSymbolForNonexistentProperty(propNode, containingType);
          if (suggestion !***REMOVED******REMOVED*** void 0) {
            const suggestedName ***REMOVED*** symbolName(suggestion);
            const message ***REMOVED*** isUncheckedJS ? Diagnostics.Property_0_may_not_exist_on_type_1_Did_you_mean_2 : Diagnostics.Property_0_does_not_exist_on_type_1_Did_you_mean_2;
            errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, message, missingProperty, container, suggestedName);
            relatedInfo ***REMOVED*** suggestion.valueDeclaration && createDiagnosticForNode(suggestion.valueDeclaration, Diagnostics._0_is_declared_here, suggestedName);
          } else {
            const diagnostic ***REMOVED*** containerSeemsToBeEmptyDomElement(containingType) ? Diagnostics.Property_0_does_not_exist_on_type_1_Try_changing_the_lib_compiler_option_to_include_dom : Diagnostics.Property_0_does_not_exist_on_type_1;
            errorInfo ***REMOVED*** chainDiagnosticMessages(elaborateNeverIntersection(errorInfo, containingType), diagnostic, missingProperty, container);
          }
        }
      }
    }
    const resultDiagnostic ***REMOVED*** createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(propNode), propNode, errorInfo);
    if (relatedInfo) {
      addRelatedInfo(resultDiagnostic, relatedInfo);
    }
    addErrorOrSuggestion(!isUncheckedJS || errorInfo.code !***REMOVED******REMOVED*** Diagnostics.Property_0_may_not_exist_on_type_1_Did_you_mean_2.code, resultDiagnostic);
  }
  function containerSeemsToBeEmptyDomElement(containingType) {
    return compilerOptions.lib && !compilerOptions.lib.includes("lib.dom.d.ts") && everyContainedType(containingType, (type) ***REMOVED***> type.symbol && /^(?:EventTarget|Node|(?:HTML[a-zA-Z]*)?Element)$/.test(unescapeLeadingUnderscores(type.symbol.escapedName))) && isEmptyObjectType(containingType);
  }
  function typeHasStaticProperty(propName, containingType) {
    const prop ***REMOVED*** containingType.symbol && getPropertyOfType(getTypeOfSymbol(containingType.symbol), propName);
    return prop !***REMOVED******REMOVED*** void 0 && !!prop.valueDeclaration && isStatic(prop.valueDeclaration);
  }
  function getSuggestedLibForNonExistentName(name) {
    const missingName ***REMOVED*** diagnosticName(name);
    const allFeatures ***REMOVED*** getScriptTargetFeatures();
    const typeFeatures ***REMOVED*** allFeatures.get(missingName);
    return typeFeatures && firstIterator(typeFeatures.keys());
  }
  function getSuggestedLibForNonExistentProperty(missingProperty, containingType) {
    const container ***REMOVED*** getApparentType(containingType).symbol;
    if (!container) {
      return void 0;
    }
    const containingTypeName ***REMOVED*** symbolName(container);
    const allFeatures ***REMOVED*** getScriptTargetFeatures();
    const typeFeatures ***REMOVED*** allFeatures.get(containingTypeName);
    if (typeFeatures) {
      for (const [libTarget, featuresOfType] of typeFeatures) {
        if (contains(featuresOfType, missingProperty)) {
          return libTarget;
        }
      }
    }
  }
  function getSuggestedSymbolForNonexistentClassMember(name, baseType) {
    return getSpellingSuggestionForName(name, getPropertiesOfType(baseType), 106500 /* ClassMember */);
  }
  function getSuggestedSymbolForNonexistentProperty(name, containingType) {
    let props ***REMOVED*** getPropertiesOfType(containingType);
    if (typeof name !***REMOVED******REMOVED*** "string") {
      const parent ***REMOVED*** name.parent;
      if (isPropertyAccessExpression(parent)) {
        props ***REMOVED*** filter(props, (prop) ***REMOVED***> isValidPropertyAccessForCompletions(parent, containingType, prop));
      }
      name ***REMOVED*** idText(name);
    }
    return getSpellingSuggestionForName(name, props, 111551 /* Value */);
  }
  function getSuggestedSymbolForNonexistentJSXAttribute(name, containingType) {
    const strName ***REMOVED*** isString(name) ? name : idText(name);
    const properties ***REMOVED*** getPropertiesOfType(containingType);
    const jsxSpecific ***REMOVED*** strName ***REMOVED******REMOVED******REMOVED*** "for" ? find(properties, (x) ***REMOVED***> symbolName(x) ***REMOVED******REMOVED******REMOVED*** "htmlFor") : strName ***REMOVED******REMOVED******REMOVED*** "class" ? find(properties, (x) ***REMOVED***> symbolName(x) ***REMOVED******REMOVED******REMOVED*** "className") : void 0;
    return jsxSpecific ?? getSpellingSuggestionForName(strName, properties, 111551 /* Value */);
  }
  function getSuggestionForNonexistentProperty(name, containingType) {
    const suggestion ***REMOVED*** getSuggestedSymbolForNonexistentProperty(name, containingType);
    return suggestion && symbolName(suggestion);
  }
  function getSuggestionForSymbolNameLookup(symbols, name, meaning) {
    const symbol ***REMOVED*** getSymbol(symbols, name, meaning);
    if (symbol) return symbol;
    let candidates;
    if (symbols ***REMOVED******REMOVED******REMOVED*** globals) {
      const primitives ***REMOVED*** mapDefined(
        ["string", "number", "boolean", "object", "bigint", "symbol"],
        (s) ***REMOVED***> symbols.has(s.charAt(0).toUpperCase() + s.slice(1)) ? createSymbol(524288 /* TypeAlias */, s) : void 0
      );
      candidates ***REMOVED*** primitives.concat(arrayFrom(symbols.values()));
    } else {
      candidates ***REMOVED*** arrayFrom(symbols.values());
    }
    return getSpellingSuggestionForName(unescapeLeadingUnderscores(name), candidates, meaning);
  }
  function getSuggestedSymbolForNonexistentSymbol(location, outerName, meaning) {
    Debug.assert(outerName !***REMOVED******REMOVED*** void 0, "outername should always be defined");
    const result ***REMOVED*** resolveNameForSymbolSuggestion(
      location,
      outerName,
      meaning,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      false,
      /*excludeGlobals*/
      false
    );
    return result;
  }
  function getSuggestedSymbolForNonexistentModule(name, targetModule) {
    return targetModule.exports && getSpellingSuggestionForName(idText(name), getExportsOfModuleAsArray(targetModule), 2623475 /* ModuleMember */);
  }
  function getSuggestionForNonexistentIndexSignature(objectType, expr, keyedType) {
    function hasProp(name) {
      const prop ***REMOVED*** getPropertyOfObjectType(objectType, name);
      if (prop) {
        const s ***REMOVED*** getSingleCallSignature(getTypeOfSymbol(prop));
        return !!s && getMinArgumentCount(s) >***REMOVED*** 1 && isTypeAssignableTo(keyedType, getTypeAtPosition(s, 0));
      }
      return false;
    }
    const suggestedMethod ***REMOVED*** isAssignmentTarget(expr) ? "set" : "get";
    if (!hasProp(suggestedMethod)) {
      return void 0;
    }
    let suggestion ***REMOVED*** tryGetPropertyAccessOrIdentifierToString(expr.expression);
    if (suggestion ***REMOVED******REMOVED******REMOVED*** void 0) {
      suggestion ***REMOVED*** suggestedMethod;
    } else {
      suggestion +***REMOVED*** "." + suggestedMethod;
    }
    return suggestion;
  }
  function getSuggestedTypeForNonexistentStringLiteralType(source, target) {
    const candidates ***REMOVED*** target.types.filter((type) ***REMOVED***> !!(type.flags & 128 /* StringLiteral */));
    return getSpellingSuggestion(source.value, candidates, (type) ***REMOVED***> type.value);
  }
  function getSpellingSuggestionForName(name, symbols, meaning) {
    return getSpellingSuggestion(name, symbols, getCandidateName);
    function getCandidateName(candidate) {
      const candidateName ***REMOVED*** symbolName(candidate);
      if (startsWith(candidateName, '"')) {
        return void 0;
      }
      if (candidate.flags & meaning) {
        return candidateName;
      }
      if (candidate.flags & 2097152 /* Alias */) {
        const alias ***REMOVED*** tryResolveAlias(candidate);
        if (alias && alias.flags & meaning) {
          return candidateName;
        }
      }
      return void 0;
    }
  }
  function markPropertyAsReferenced(prop, nodeForCheckWriteOnly, isSelfTypeAccess2) {
    const valueDeclaration ***REMOVED*** prop && prop.flags & 106500 /* ClassMember */ && prop.valueDeclaration;
    if (!valueDeclaration) {
      return;
    }
    const hasPrivateModifier ***REMOVED*** hasEffectiveModifier(valueDeclaration, 2 /* Private */);
    const hasPrivateIdentifier ***REMOVED*** prop.valueDeclaration && isNamedDeclaration(prop.valueDeclaration) && isPrivateIdentifier(prop.valueDeclaration.name);
    if (!hasPrivateModifier && !hasPrivateIdentifier) {
      return;
    }
    if (nodeForCheckWriteOnly && isWriteOnlyAccess(nodeForCheckWriteOnly) && !(prop.flags & 65536 /* SetAccessor */)) {
      return;
    }
    if (isSelfTypeAccess2) {
      const containingMethod ***REMOVED*** findAncestor(nodeForCheckWriteOnly, isFunctionLikeDeclaration);
      if (containingMethod && containingMethod.symbol ***REMOVED******REMOVED******REMOVED*** prop) {
        return;
      }
    }
    (getCheckFlags(prop) & 1 /* Instantiated */ ? getSymbolLinks(prop).target : prop).isReferenced ***REMOVED*** -1 /* All */;
  }
  function isSelfTypeAccess(name, parent) {
    return name.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ || !!parent && isEntityNameExpression(name) && parent ***REMOVED******REMOVED******REMOVED*** getResolvedSymbol(getFirstIdentifier(name));
  }
  function isValidPropertyAccess(node, propertyName) {
    switch (node.kind) {
      case 212 /* PropertyAccessExpression */:
        return isValidPropertyAccessWithType(node, node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */, propertyName, getWidenedType(checkExpression(node.expression)));
      case 167 /* QualifiedName */:
        return isValidPropertyAccessWithType(
          node,
          /*isSuper*/
          false,
          propertyName,
          getWidenedType(checkExpression(node.left))
        );
      case 206 /* ImportType */:
        return isValidPropertyAccessWithType(
          node,
          /*isSuper*/
          false,
          propertyName,
          getTypeFromTypeNode(node)
        );
    }
  }
  function isValidPropertyAccessForCompletions(node, type, property) {
    return isPropertyAccessible(
      node,
      node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */,
      /*isWrite*/
      false,
      type,
      property
    );
  }
  function isValidPropertyAccessWithType(node, isSuper, propertyName, type) {
    if (isTypeAny(type)) {
      return true;
    }
    const prop ***REMOVED*** getPropertyOfType(type, propertyName);
    return !!prop && isPropertyAccessible(
      node,
      isSuper,
      /*isWrite*/
      false,
      type,
      prop
    );
  }
  function isPropertyAccessible(node, isSuper, isWrite, containingType, property) {
    if (isTypeAny(containingType)) {
      return true;
    }
    if (property.valueDeclaration && isPrivateIdentifierClassElementDeclaration(property.valueDeclaration)) {
      const declClass ***REMOVED*** getContainingClass(property.valueDeclaration);
      return !isOptionalChain(node) && !!findAncestor(node, (parent) ***REMOVED***> parent ***REMOVED******REMOVED******REMOVED*** declClass);
    }
    return checkPropertyAccessibilityAtLocation(node, isSuper, isWrite, containingType, property);
  }
  function getForInVariableSymbol(node) {
    const initializer ***REMOVED*** node.initializer;
    if (initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
      const variable ***REMOVED*** initializer.declarations[0];
      if (variable && !isBindingPattern(variable.name)) {
        return getSymbolOfDeclaration(variable);
      }
    } else if (initializer.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return getResolvedSymbol(initializer);
    }
    return void 0;
  }
  function hasNumericPropertyNames(type) {
    return getIndexInfosOfType(type).length ***REMOVED******REMOVED******REMOVED*** 1 && !!getIndexInfoOfType(type, numberType);
  }
  function isForInVariableForNumericPropertyNames(expr) {
    const e ***REMOVED*** skipParentheses(expr);
    if (e.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const symbol ***REMOVED*** getResolvedSymbol(e);
      if (symbol.flags & 3 /* Variable */) {
        let child ***REMOVED*** expr;
        let node ***REMOVED*** expr.parent;
        while (node) {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ && child ***REMOVED******REMOVED******REMOVED*** node.statement && getForInVariableSymbol(node) ***REMOVED******REMOVED******REMOVED*** symbol && hasNumericPropertyNames(getTypeOfExpression(node.expression))) {
            return true;
          }
          child ***REMOVED*** node;
          node ***REMOVED*** node.parent;
        }
      }
    }
    return false;
  }
  function checkIndexedAccess(node, checkMode) {
    return node.flags & 64 /* OptionalChain */ ? checkElementAccessChain(node, checkMode) : checkElementAccessExpression(node, checkNonNullExpression(node.expression), checkMode);
  }
  function checkElementAccessChain(node, checkMode) {
    const exprType ***REMOVED*** checkExpression(node.expression);
    const nonOptionalType ***REMOVED*** getOptionalExpressionType(exprType, node.expression);
    return propagateOptionalTypeMarker(checkElementAccessExpression(node, checkNonNullType(nonOptionalType, node.expression), checkMode), node, nonOptionalType !***REMOVED******REMOVED*** exprType);
  }
  function checkElementAccessExpression(node, exprType, checkMode) {
    const objectType ***REMOVED*** getAssignmentTargetKind(node) !***REMOVED******REMOVED*** 0 /* None */ || isMethodAccessForCall(node) ? getWidenedType(exprType) : exprType;
    const indexExpression ***REMOVED*** node.argumentExpression;
    const indexType ***REMOVED*** checkExpression(indexExpression);
    if (isErrorType(objectType) || objectType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return objectType;
    }
    if (isConstEnumObjectType(objectType) && !isStringLiteralLike(indexExpression)) {
      error(indexExpression, Diagnostics.A_const_enum_member_can_only_be_accessed_using_a_string_literal);
      return errorType;
    }
    const effectiveIndexType ***REMOVED*** isForInVariableForNumericPropertyNames(indexExpression) ? numberType : indexType;
    const assignmentTargetKind ***REMOVED*** getAssignmentTargetKind(node);
    let accessFlags;
    if (assignmentTargetKind ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      accessFlags ***REMOVED*** 32 /* ExpressionPosition */;
    } else {
      accessFlags ***REMOVED*** 4 /* Writing */ | (isGenericObjectType(objectType) && !isThisTypeParameter(objectType) ? 2 /* NoIndexSignatures */ : 0);
      if (assignmentTargetKind ***REMOVED******REMOVED******REMOVED*** 2 /* Compound */) {
        accessFlags |***REMOVED*** 32 /* ExpressionPosition */;
      }
    }
    const indexedAccessType ***REMOVED*** getIndexedAccessTypeOrUndefined(objectType, effectiveIndexType, accessFlags, node) || errorType;
    return checkIndexedAccessIndexType(getFlowTypeOfAccessExpression(node, getNodeLinks(node).resolvedSymbol, indexedAccessType, indexExpression, checkMode), node);
  }
  function callLikeExpressionMayHaveTypeArguments(node) {
    return isCallOrNewExpression(node) || isTaggedTemplateExpression(node) || isJsxOpeningLikeElement(node);
  }
  function resolveUntypedCall(node) {
    if (callLikeExpressionMayHaveTypeArguments(node)) {
      forEach(node.typeArguments, checkSourceElement);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */) {
      checkExpression(node.template);
    } else if (isJsxOpeningLikeElement(node)) {
      checkExpression(node.attributes);
    } else if (isBinaryExpression(node)) {
      checkExpression(node.left);
    } else if (isCallOrNewExpression(node)) {
      forEach(node.arguments, (argument) ***REMOVED***> {
        checkExpression(argument);
      });
    }
    return anySignature;
  }
  function resolveErrorCall(node) {
    resolveUntypedCall(node);
    return unknownSignature;
  }
  function reorderCandidates(signatures, result, callChainFlags) {
    let lastParent;
    let lastSymbol;
    let cutoffIndex ***REMOVED*** 0;
    let index;
    let specializedIndex ***REMOVED*** -1;
    let spliceIndex;
    Debug.assert(!result.length);
    for (const signature of signatures) {
      const symbol ***REMOVED*** signature.declaration && getSymbolOfDeclaration(signature.declaration);
      const parent ***REMOVED*** signature.declaration && signature.declaration.parent;
      if (!lastSymbol || symbol ***REMOVED******REMOVED******REMOVED*** lastSymbol) {
        if (lastParent && parent ***REMOVED******REMOVED******REMOVED*** lastParent) {
          index ***REMOVED*** index + 1;
        } else {
          lastParent ***REMOVED*** parent;
          index ***REMOVED*** cutoffIndex;
        }
      } else {
        index ***REMOVED*** cutoffIndex ***REMOVED*** result.length;
        lastParent ***REMOVED*** parent;
      }
      lastSymbol ***REMOVED*** symbol;
      if (signatureHasLiteralTypes(signature)) {
        specializedIndex++;
        spliceIndex ***REMOVED*** specializedIndex;
        cutoffIndex++;
      } else {
        spliceIndex ***REMOVED*** index;
      }
      result.splice(spliceIndex, 0, callChainFlags ? getOptionalCallSignature(signature, callChainFlags) : signature);
    }
  }
  function isSpreadArgument(arg) {
    return !!arg && (arg.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */ || arg.kind ***REMOVED******REMOVED******REMOVED*** 238 /* SyntheticExpression */ && arg.isSpread);
  }
  function getSpreadArgumentIndex(args) {
    return findIndex(args, isSpreadArgument);
  }
  function acceptsVoid(t) {
    return !!(t.flags & 16384 /* Void */);
  }
  function acceptsVoidUndefinedUnknownOrAny(t) {
    return !!(t.flags & (16384 /* Void */ | 32768 /* Undefined */ | 2 /* Unknown */ | 1 /* Any */));
  }
  function hasCorrectArity(node, args, signature, signatureHelpTrailingComma ***REMOVED*** false) {
    if (isJsxOpeningFragment(node)) return true;
    let argCount;
    let callIsIncomplete ***REMOVED*** false;
    let effectiveParameterCount ***REMOVED*** getParameterCount(signature);
    let effectiveMinimumArguments ***REMOVED*** getMinArgumentCount(signature);
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */) {
      argCount ***REMOVED*** args.length;
      if (node.template.kind ***REMOVED******REMOVED******REMOVED*** 229 /* TemplateExpression */) {
        const lastSpan ***REMOVED*** last(node.template.templateSpans);
        callIsIncomplete ***REMOVED*** nodeIsMissing(lastSpan.literal) || !!lastSpan.literal.isUnterminated;
      } else {
        const templateLiteral ***REMOVED*** node.template;
        Debug.assert(templateLiteral.kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */);
        callIsIncomplete ***REMOVED*** !!templateLiteral.isUnterminated;
      }
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 171 /* Decorator */) {
      argCount ***REMOVED*** getDecoratorArgumentCount(node, signature);
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
      argCount ***REMOVED*** 1;
    } else if (isJsxOpeningLikeElement(node)) {
      callIsIncomplete ***REMOVED*** node.attributes.end ***REMOVED******REMOVED******REMOVED*** node.end;
      if (callIsIncomplete) {
        return true;
      }
      argCount ***REMOVED*** effectiveMinimumArguments ***REMOVED******REMOVED******REMOVED*** 0 ? args.length : 1;
      effectiveParameterCount ***REMOVED*** args.length ***REMOVED******REMOVED******REMOVED*** 0 ? effectiveParameterCount : 1;
      effectiveMinimumArguments ***REMOVED*** Math.min(effectiveMinimumArguments, 1);
    } else if (!node.arguments) {
      Debug.assert(node.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */);
      return getMinArgumentCount(signature) ***REMOVED******REMOVED******REMOVED*** 0;
    } else {
      argCount ***REMOVED*** signatureHelpTrailingComma ? args.length + 1 : args.length;
      callIsIncomplete ***REMOVED*** node.arguments.end ***REMOVED******REMOVED******REMOVED*** node.end;
      const spreadArgIndex ***REMOVED*** getSpreadArgumentIndex(args);
      if (spreadArgIndex >***REMOVED*** 0) {
        return spreadArgIndex >***REMOVED*** getMinArgumentCount(signature) && (hasEffectiveRestParameter(signature) || spreadArgIndex < getParameterCount(signature));
      }
    }
    if (!hasEffectiveRestParameter(signature) && argCount > effectiveParameterCount) {
      return false;
    }
    if (callIsIncomplete || argCount >***REMOVED*** effectiveMinimumArguments) {
      return true;
    }
    for (let i ***REMOVED*** argCount; i < effectiveMinimumArguments; i++) {
      const type ***REMOVED*** getTypeAtPosition(signature, i);
      if (filterType(type, isInJSFile(node) && !strictNullChecks ? acceptsVoidUndefinedUnknownOrAny : acceptsVoid).flags & 131072 /* Never */) {
        return false;
      }
    }
    return true;
  }
  function hasCorrectTypeArgumentArity(signature, typeArguments) {
    const numTypeParameters ***REMOVED*** length(signature.typeParameters);
    const minTypeArgumentCount ***REMOVED*** getMinTypeArgumentCount(signature.typeParameters);
    return !some(typeArguments) || typeArguments.length >***REMOVED*** minTypeArgumentCount && typeArguments.length <***REMOVED*** numTypeParameters;
  }
  function isInstantiatedGenericParameter(signature, pos) {
    let type;
    return !!(signature.target && (type ***REMOVED*** tryGetTypeAtPosition(signature.target, pos)) && isGenericType(type));
  }
  function getSingleCallSignature(type) {
    return getSingleSignature(
      type,
      0 /* Call */,
      /*allowMembers*/
      false
    );
  }
  function getSingleCallOrConstructSignature(type) {
    return getSingleSignature(
      type,
      0 /* Call */,
      /*allowMembers*/
      false
    ) || getSingleSignature(
      type,
      1 /* Construct */,
      /*allowMembers*/
      false
    );
  }
  function getSingleSignature(type, kind, allowMembers) {
    if (type.flags & 524288 /* Object */) {
      const resolved ***REMOVED*** resolveStructuredTypeMembers(type);
      if (allowMembers || resolved.properties.length ***REMOVED******REMOVED******REMOVED*** 0 && resolved.indexInfos.length ***REMOVED******REMOVED******REMOVED*** 0) {
        if (kind ***REMOVED******REMOVED******REMOVED*** 0 /* Call */ && resolved.callSignatures.length ***REMOVED******REMOVED******REMOVED*** 1 && resolved.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
          return resolved.callSignatures[0];
        }
        if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* Construct */ && resolved.constructSignatures.length ***REMOVED******REMOVED******REMOVED*** 1 && resolved.callSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
          return resolved.constructSignatures[0];
        }
      }
    }
    return void 0;
  }
  function instantiateSignatureInContextOf(signature, contextualSignature, inferenceContext, compareTypes) {
    const context ***REMOVED*** createInferenceContext(getTypeParametersForMapper(signature), signature, 0 /* None */, compareTypes);
    const restType ***REMOVED*** getEffectiveRestType(contextualSignature);
    const mapper ***REMOVED*** inferenceContext && (restType && restType.flags & 262144 /* TypeParameter */ ? inferenceContext.nonFixingMapper : inferenceContext.mapper);
    const sourceSignature ***REMOVED*** mapper ? instantiateSignature(contextualSignature, mapper) : contextualSignature;
    applyToParameterTypes(sourceSignature, signature, (source, target) ***REMOVED***> {
      inferTypes(context.inferences, source, target);
    });
    if (!inferenceContext) {
      applyToReturnTypes(contextualSignature, signature, (source, target) ***REMOVED***> {
        inferTypes(context.inferences, source, target, 128 /* ReturnType */);
      });
    }
    return getSignatureInstantiation(signature, getInferredTypes(context), isInJSFile(contextualSignature.declaration));
  }
  function inferJsxTypeArguments(node, signature, checkMode, context) {
    const paramType ***REMOVED*** getEffectiveFirstArgumentForJsxSignature(signature, node);
    const checkAttrType ***REMOVED*** checkExpressionWithContextualType(node.attributes, paramType, context, checkMode);
    inferTypes(context.inferences, checkAttrType, paramType);
    return getInferredTypes(context);
  }
  function getThisArgumentType(thisArgumentNode) {
    if (!thisArgumentNode) {
      return voidType;
    }
    const thisArgumentType ***REMOVED*** checkExpression(thisArgumentNode);
    return isRightSideOfInstanceofExpression(thisArgumentNode) ? thisArgumentType : isOptionalChainRoot(thisArgumentNode.parent) ? getNonNullableType(thisArgumentType) : isOptionalChain(thisArgumentNode.parent) ? removeOptionalTypeMarker(thisArgumentType) : thisArgumentType;
  }
  function inferTypeArguments(node, signature, args, checkMode, context) {
    if (isJsxOpeningLikeElement(node)) {
      return inferJsxTypeArguments(node, signature, checkMode, context);
    }
    if (node.kind !***REMOVED******REMOVED*** 171 /* Decorator */ && node.kind !***REMOVED******REMOVED*** 227 /* BinaryExpression */) {
      const skipBindingPatterns ***REMOVED*** every(signature.typeParameters, (p) ***REMOVED***> !!getDefaultFromTypeParameter(p));
      const contextualType ***REMOVED*** getContextualType(node, skipBindingPatterns ? 8 /* SkipBindingPatterns */ : 0 /* None */);
      if (contextualType) {
        const inferenceTargetType ***REMOVED*** getReturnTypeOfSignature(signature);
        if (couldContainTypeVariables(inferenceTargetType)) {
          const outerContext ***REMOVED*** getInferenceContext(node);
          const isFromBindingPattern ***REMOVED*** !skipBindingPatterns && getContextualType(node, 8 /* SkipBindingPatterns */) !***REMOVED******REMOVED*** contextualType;
          if (!isFromBindingPattern) {
            const outerMapper ***REMOVED*** getMapperFromContext(cloneInferenceContext(outerContext, 1 /* NoDefault */));
            const instantiatedType ***REMOVED*** instantiateType(contextualType, outerMapper);
            const contextualSignature ***REMOVED*** getSingleCallSignature(instantiatedType);
            const inferenceSourceType ***REMOVED*** contextualSignature && contextualSignature.typeParameters ? getOrCreateTypeFromSignature(getSignatureInstantiationWithoutFillingInTypeArguments(contextualSignature, contextualSignature.typeParameters)) : instantiatedType;
            inferTypes(context.inferences, inferenceSourceType, inferenceTargetType, 128 /* ReturnType */);
          }
          const returnContext ***REMOVED*** createInferenceContext(signature.typeParameters, signature, context.flags);
          const returnSourceType ***REMOVED*** instantiateType(contextualType, outerContext && createOuterReturnMapper(outerContext));
          inferTypes(returnContext.inferences, returnSourceType, inferenceTargetType);
          context.returnMapper ***REMOVED*** some(returnContext.inferences, hasInferenceCandidates) ? getMapperFromContext(cloneInferredPartOfContext(returnContext)) : void 0;
        }
      }
    }
    const restType ***REMOVED*** getNonArrayRestType(signature);
    const argCount ***REMOVED*** restType ? Math.min(getParameterCount(signature) - 1, args.length) : args.length;
    if (restType && restType.flags & 262144 /* TypeParameter */) {
      const info ***REMOVED*** find(context.inferences, (info2) ***REMOVED***> info2.typeParameter ***REMOVED******REMOVED******REMOVED*** restType);
      if (info) {
        info.impliedArity ***REMOVED*** findIndex(args, isSpreadArgument, argCount) < 0 ? args.length - argCount : void 0;
      }
    }
    const thisType ***REMOVED*** getThisTypeOfSignature(signature);
    if (thisType && couldContainTypeVariables(thisType)) {
      const thisArgumentNode ***REMOVED*** getThisArgumentOfCall(node);
      inferTypes(context.inferences, getThisArgumentType(thisArgumentNode), thisType);
    }
    for (let i ***REMOVED*** 0; i < argCount; i++) {
      const arg ***REMOVED*** args[i];
      if (arg.kind !***REMOVED******REMOVED*** 233 /* OmittedExpression */) {
        const paramType ***REMOVED*** getTypeAtPosition(signature, i);
        if (couldContainTypeVariables(paramType)) {
          const argType ***REMOVED*** checkExpressionWithContextualType(arg, paramType, context, checkMode);
          inferTypes(context.inferences, argType, paramType);
        }
      }
    }
    if (restType && couldContainTypeVariables(restType)) {
      const spreadType ***REMOVED*** getSpreadArgumentType(args, argCount, args.length, restType, context, checkMode);
      inferTypes(context.inferences, spreadType, restType);
    }
    return getInferredTypes(context);
  }
  function getMutableArrayOrTupleType(type) {
    return type.flags & 1048576 /* Union */ ? mapType(type, getMutableArrayOrTupleType) : type.flags & 1 /* Any */ || isMutableArrayOrTuple(getBaseConstraintOfType(type) || type) ? type : isTupleType(type) ? createTupleType(
      getElementTypes(type),
      type.target.elementFlags,
      /*readonly*/
      false,
      type.target.labeledElementDeclarations
    ) : createTupleType([type], [8 /* Variadic */]);
  }
  function getSpreadArgumentType(args, index, argCount, restType, context, checkMode) {
    const inConstContext ***REMOVED*** isConstTypeVariable(restType);
    if (index >***REMOVED*** argCount - 1) {
      const arg ***REMOVED*** args[argCount - 1];
      if (isSpreadArgument(arg)) {
        const spreadType ***REMOVED*** arg.kind ***REMOVED******REMOVED******REMOVED*** 238 /* SyntheticExpression */ ? arg.type : checkExpressionWithContextualType(arg.expression, restType, context, checkMode);
        if (isArrayLikeType(spreadType)) {
          return getMutableArrayOrTupleType(spreadType);
        }
        return createArrayType(checkIteratedTypeOrElementType(33 /* Spread */, spreadType, undefinedType, arg.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */ ? arg.expression : arg), inConstContext);
      }
    }
    const types ***REMOVED*** [];
    const flags ***REMOVED*** [];
    const names ***REMOVED*** [];
    for (let i ***REMOVED*** index; i < argCount; i++) {
      const arg ***REMOVED*** args[i];
      if (isSpreadArgument(arg)) {
        const spreadType ***REMOVED*** arg.kind ***REMOVED******REMOVED******REMOVED*** 238 /* SyntheticExpression */ ? arg.type : checkExpression(arg.expression);
        if (isArrayLikeType(spreadType)) {
          types.push(spreadType);
          flags.push(8 /* Variadic */);
        } else {
          types.push(checkIteratedTypeOrElementType(33 /* Spread */, spreadType, undefinedType, arg.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */ ? arg.expression : arg));
          flags.push(4 /* Rest */);
        }
      } else {
        const contextualType ***REMOVED*** isTupleType(restType) ? getContextualTypeForElementExpression(restType, i - index, argCount - index) || unknownType : getIndexedAccessType(restType, getNumberLiteralType(i - index), 256 /* Contextual */);
        const argType ***REMOVED*** checkExpressionWithContextualType(arg, contextualType, context, checkMode);
        const hasPrimitiveContextualType ***REMOVED*** inConstContext || maybeTypeOfKind(contextualType, 402784252 /* Primitive */ | 4194304 /* Index */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */);
        types.push(hasPrimitiveContextualType ? getRegularTypeOfLiteralType(argType) : getWidenedLiteralType(argType));
        flags.push(1 /* Required */);
      }
      if (arg.kind ***REMOVED******REMOVED******REMOVED*** 238 /* SyntheticExpression */ && arg.tupleNameSource) {
        names.push(arg.tupleNameSource);
      } else {
        names.push(void 0);
      }
    }
    return createTupleType(types, flags, inConstContext && !someType(restType, isMutableArrayLikeType), names);
  }
  function checkTypeArguments(signature, typeArgumentNodes, reportErrors2, headMessage) {
    const isJavascript ***REMOVED*** isInJSFile(signature.declaration);
    const typeParameters ***REMOVED*** signature.typeParameters;
    const typeArgumentTypes ***REMOVED*** fillMissingTypeArguments(map(typeArgumentNodes, getTypeFromTypeNode), typeParameters, getMinTypeArgumentCount(typeParameters), isJavascript);
    let mapper;
    for (let i ***REMOVED*** 0; i < typeArgumentNodes.length; i++) {
      Debug.assert(typeParameters[i] !***REMOVED******REMOVED*** void 0, "Should not call checkTypeArguments with too many type arguments");
      const constraint ***REMOVED*** getConstraintOfTypeParameter(typeParameters[i]);
      if (constraint) {
        const errorInfo ***REMOVED*** reportErrors2 && headMessage ? () ***REMOVED***> chainDiagnosticMessages(
          /*details*/
          void 0,
          Diagnostics.Type_0_does_not_satisfy_the_constraint_1
        ) : void 0;
        const typeArgumentHeadMessage ***REMOVED*** headMessage || Diagnostics.Type_0_does_not_satisfy_the_constraint_1;
        if (!mapper) {
          mapper ***REMOVED*** createTypeMapper(typeParameters, typeArgumentTypes);
        }
        const typeArgument ***REMOVED*** typeArgumentTypes[i];
        if (!checkTypeAssignableTo(
          typeArgument,
          getTypeWithThisArgument(instantiateType(constraint, mapper), typeArgument),
          reportErrors2 ? typeArgumentNodes[i] : void 0,
          typeArgumentHeadMessage,
          errorInfo
        )) {
          return void 0;
        }
      }
    }
    return typeArgumentTypes;
  }
  function getJsxReferenceKind(node) {
    if (isJsxIntrinsicTagName(node.tagName)) {
      return 2 /* Mixed */;
    }
    const tagType ***REMOVED*** getApparentType(checkExpression(node.tagName));
    if (length(getSignaturesOfType(tagType, 1 /* Construct */))) {
      return 0 /* Component */;
    }
    if (length(getSignaturesOfType(tagType, 0 /* Call */))) {
      return 1 /* Function */;
    }
    return 2 /* Mixed */;
  }
  function checkApplicableSignatureForJsxCallLikeElement(node, signature, relation, checkMode, reportErrors2, containingMessageChain, errorOutputContainer) {
    const paramType ***REMOVED*** getEffectiveFirstArgumentForJsxSignature(signature, node);
    const attributesType ***REMOVED*** isJsxOpeningFragment(node) ? createJsxAttributesTypeFromAttributesProperty(node) : checkExpressionWithContextualType(
      node.attributes,
      paramType,
      /*inferenceContext*/
      void 0,
      checkMode
    );
    const checkAttributesType ***REMOVED*** checkMode & 4 /* SkipContextSensitive */ ? getRegularTypeOfObjectLiteral(attributesType) : attributesType;
    return checkTagNameDoesNotExpectTooManyArguments() && checkTypeRelatedToAndOptionallyElaborate(
      checkAttributesType,
      paramType,
      relation,
      reportErrors2 ? isJsxOpeningFragment(node) ? node : node.tagName : void 0,
      isJsxOpeningFragment(node) ? void 0 : node.attributes,
      /*headMessage*/
      void 0,
      containingMessageChain,
      errorOutputContainer
    );
    function checkTagNameDoesNotExpectTooManyArguments() {
      var _a;
      if (getJsxNamespaceContainerForImplicitImport(node)) {
        return true;
      }
      const tagType ***REMOVED*** (isJsxOpeningElement(node) || isJsxSelfClosingElement(node)) && !(isJsxIntrinsicTagName(node.tagName) || isJsxNamespacedName(node.tagName)) ? checkExpression(node.tagName) : void 0;
      if (!tagType) {
        return true;
      }
      const tagCallSignatures ***REMOVED*** getSignaturesOfType(tagType, 0 /* Call */);
      if (!length(tagCallSignatures)) {
        return true;
      }
      const factory2 ***REMOVED*** getJsxFactoryEntity(node);
      if (!factory2) {
        return true;
      }
      const factorySymbol ***REMOVED*** resolveEntityName(
        factory2,
        111551 /* Value */,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        false,
        node
      );
      if (!factorySymbol) {
        return true;
      }
      const factoryType ***REMOVED*** getTypeOfSymbol(factorySymbol);
      const callSignatures ***REMOVED*** getSignaturesOfType(factoryType, 0 /* Call */);
      if (!length(callSignatures)) {
        return true;
      }
      let hasFirstParamSignatures ***REMOVED*** false;
      let maxParamCount ***REMOVED*** 0;
      for (const sig of callSignatures) {
        const firstparam ***REMOVED*** getTypeAtPosition(sig, 0);
        const signaturesOfParam ***REMOVED*** getSignaturesOfType(firstparam, 0 /* Call */);
        if (!length(signaturesOfParam)) continue;
        for (const paramSig of signaturesOfParam) {
          hasFirstParamSignatures ***REMOVED*** true;
          if (hasEffectiveRestParameter(paramSig)) {
            return true;
          }
          const paramCount ***REMOVED*** getParameterCount(paramSig);
          if (paramCount > maxParamCount) {
            maxParamCount ***REMOVED*** paramCount;
          }
        }
      }
      if (!hasFirstParamSignatures) {
        return true;
      }
      let absoluteMinArgCount ***REMOVED*** Infinity;
      for (const tagSig of tagCallSignatures) {
        const tagRequiredArgCount ***REMOVED*** getMinArgumentCount(tagSig);
        if (tagRequiredArgCount < absoluteMinArgCount) {
          absoluteMinArgCount ***REMOVED*** tagRequiredArgCount;
        }
      }
      if (absoluteMinArgCount <***REMOVED*** maxParamCount) {
        return true;
      }
      if (reportErrors2) {
        const tagName ***REMOVED*** node.tagName;
        const diag2 ***REMOVED*** createDiagnosticForNode(tagName, Diagnostics.Tag_0_expects_at_least_1_arguments_but_the_JSX_factory_2_provides_at_most_3, entityNameToString(tagName), absoluteMinArgCount, entityNameToString(factory2), maxParamCount);
        const tagNameDeclaration ***REMOVED*** (_a ***REMOVED*** getSymbolAtLocation(tagName)) ***REMOVED******REMOVED*** null ? void 0 : _a.valueDeclaration;
        if (tagNameDeclaration) {
          addRelatedInfo(diag2, createDiagnosticForNode(tagNameDeclaration, Diagnostics._0_is_declared_here, entityNameToString(tagName)));
        }
        if (errorOutputContainer && errorOutputContainer.skipLogging) {
          (errorOutputContainer.errors || (errorOutputContainer.errors ***REMOVED*** [])).push(diag2);
        }
        if (!errorOutputContainer.skipLogging) {
          diagnostics.add(diag2);
        }
      }
      return false;
    }
  }
  function getEffectiveCheckNode(argument) {
    const flags ***REMOVED*** isInJSFile(argument) ? 1 /* Parentheses */ | 32 /* Satisfies */ | -2147483648 /* ExcludeJSDocTypeAssertion */ : 1 /* Parentheses */ | 32 /* Satisfies */;
    return skipOuterExpressions(argument, flags);
  }
  function getSignatureApplicabilityError(node, args, signature, relation, checkMode, reportErrors2, containingMessageChain) {
    const errorOutputContainer ***REMOVED*** { errors: void 0, skipLogging: true };
    if (isJsxCallLike(node)) {
      if (!checkApplicableSignatureForJsxCallLikeElement(node, signature, relation, checkMode, reportErrors2, containingMessageChain, errorOutputContainer)) {
        Debug.assert(!reportErrors2 || !!errorOutputContainer.errors, "jsx should have errors when reporting errors");
        return errorOutputContainer.errors || emptyArray;
      }
      return void 0;
    }
    const thisType ***REMOVED*** getThisTypeOfSignature(signature);
    if (thisType && thisType !***REMOVED******REMOVED*** voidType && !(isNewExpression(node) || isCallExpression(node) && isSuperProperty(node.expression))) {
      const thisArgumentNode ***REMOVED*** getThisArgumentOfCall(node);
      const thisArgumentType ***REMOVED*** getThisArgumentType(thisArgumentNode);
      const errorNode ***REMOVED*** reportErrors2 ? thisArgumentNode || node : void 0;
      const headMessage2 ***REMOVED*** Diagnostics.The_this_context_of_type_0_is_not_assignable_to_method_s_this_of_type_1;
      if (!checkTypeRelatedTo(thisArgumentType, thisType, relation, errorNode, headMessage2, containingMessageChain, errorOutputContainer)) {
        Debug.assert(!reportErrors2 || !!errorOutputContainer.errors, "this parameter should have errors when reporting errors");
        return errorOutputContainer.errors || emptyArray;
      }
    }
    const headMessage ***REMOVED*** Diagnostics.Argument_of_type_0_is_not_assignable_to_parameter_of_type_1;
    const restType ***REMOVED*** getNonArrayRestType(signature);
    const argCount ***REMOVED*** restType ? Math.min(getParameterCount(signature) - 1, args.length) : args.length;
    for (let i ***REMOVED*** 0; i < argCount; i++) {
      const arg ***REMOVED*** args[i];
      if (arg.kind !***REMOVED******REMOVED*** 233 /* OmittedExpression */) {
        const paramType ***REMOVED*** getTypeAtPosition(signature, i);
        const argType ***REMOVED*** checkExpressionWithContextualType(
          arg,
          paramType,
          /*inferenceContext*/
          void 0,
          checkMode
        );
        const checkArgType ***REMOVED*** checkMode & 4 /* SkipContextSensitive */ ? getRegularTypeOfObjectLiteral(argType) : argType;
        const effectiveCheckArgumentNode ***REMOVED*** getEffectiveCheckNode(arg);
        if (!checkTypeRelatedToAndOptionallyElaborate(checkArgType, paramType, relation, reportErrors2 ? effectiveCheckArgumentNode : void 0, effectiveCheckArgumentNode, headMessage, containingMessageChain, errorOutputContainer)) {
          Debug.assert(!reportErrors2 || !!errorOutputContainer.errors, "parameter should have errors when reporting errors");
          maybeAddMissingAwaitInfo(arg, checkArgType, paramType);
          return errorOutputContainer.errors || emptyArray;
        }
      }
    }
    if (restType) {
      const spreadType ***REMOVED*** getSpreadArgumentType(
        args,
        argCount,
        args.length,
        restType,
        /*context*/
        void 0,
        checkMode
      );
      const restArgCount ***REMOVED*** args.length - argCount;
      const errorNode ***REMOVED*** !reportErrors2 ? void 0 : restArgCount ***REMOVED******REMOVED******REMOVED*** 0 ? node : restArgCount ***REMOVED******REMOVED******REMOVED*** 1 ? getEffectiveCheckNode(args[argCount]) : setTextRangePosEnd(createSyntheticExpression(node, spreadType), args[argCount].pos, args[args.length - 1].end);
      if (!checkTypeRelatedTo(
        spreadType,
        restType,
        relation,
        errorNode,
        headMessage,
        /*containingMessageChain*/
        void 0,
        errorOutputContainer
      )) {
        Debug.assert(!reportErrors2 || !!errorOutputContainer.errors, "rest parameter should have errors when reporting errors");
        maybeAddMissingAwaitInfo(errorNode, spreadType, restType);
        return errorOutputContainer.errors || emptyArray;
      }
    }
    return void 0;
    function maybeAddMissingAwaitInfo(errorNode, source, target) {
      if (errorNode && reportErrors2 && errorOutputContainer.errors && errorOutputContainer.errors.length) {
        if (getAwaitedTypeOfPromise(target)) {
          return;
        }
        const awaitedTypeOfSource ***REMOVED*** getAwaitedTypeOfPromise(source);
        if (awaitedTypeOfSource && isTypeRelatedTo(awaitedTypeOfSource, target, relation)) {
          addRelatedInfo(errorOutputContainer.errors[0], createDiagnosticForNode(errorNode, Diagnostics.Did_you_forget_to_use_await));
        }
      }
    }
  }
  function getThisArgumentOfCall(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
      return node.right;
    }
    const expression ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ ? node.expression : node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */ ? node.tag : node.kind ***REMOVED******REMOVED******REMOVED*** 171 /* Decorator */ && !legacyDecorators ? node.expression : void 0;
    if (expression) {
      const callee ***REMOVED*** skipOuterExpressions(expression);
      if (isAccessExpression(callee)) {
        return callee.expression;
      }
    }
  }
  function createSyntheticExpression(parent, type, isSpread, tupleNameSource) {
    const result ***REMOVED*** parseNodeFactory.createSyntheticExpression(type, isSpread, tupleNameSource);
    setTextRange(result, parent);
    setParent(result, parent);
    return result;
  }
  function getEffectiveCallArguments(node) {
    if (isJsxOpeningFragment(node)) {
      return [createSyntheticExpression(node, emptyFreshJsxObjectType)];
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */) {
      const template ***REMOVED*** node.template;
      const args2 ***REMOVED*** [createSyntheticExpression(template, getGlobalTemplateStringsArrayType())];
      if (template.kind ***REMOVED******REMOVED******REMOVED*** 229 /* TemplateExpression */) {
        forEach(template.templateSpans, (span) ***REMOVED***> {
          args2.push(span.expression);
        });
      }
      return args2;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 171 /* Decorator */) {
      return getEffectiveDecoratorArguments(node);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
      return [node.left];
    }
    if (isJsxOpeningLikeElement(node)) {
      return node.attributes.properties.length > 0 || isJsxOpeningElement(node) && node.parent.children.length > 0 ? [node.attributes] : emptyArray;
    }
    const args ***REMOVED*** node.arguments || emptyArray;
    const spreadIndex ***REMOVED*** getSpreadArgumentIndex(args);
    if (spreadIndex >***REMOVED*** 0) {
      const effectiveArgs ***REMOVED*** args.slice(0, spreadIndex);
      for (let i ***REMOVED*** spreadIndex; i < args.length; i++) {
        const arg ***REMOVED*** args[i];
        const spreadType ***REMOVED*** arg.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */ && (flowLoopCount ? checkExpression(arg.expression) : checkExpressionCached(arg.expression));
        if (spreadType && isTupleType(spreadType)) {
          forEach(getElementTypes(spreadType), (t, i2) ***REMOVED***> {
            var _a;
            const flags ***REMOVED*** spreadType.target.elementFlags[i2];
            const syntheticArg ***REMOVED*** createSyntheticExpression(arg, flags & 4 /* Rest */ ? createArrayType(t) : t, !!(flags & 12 /* Variable */), (_a ***REMOVED*** spreadType.target.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _a[i2]);
            effectiveArgs.push(syntheticArg);
          });
        } else {
          effectiveArgs.push(arg);
        }
      }
      return effectiveArgs;
    }
    return args;
  }
  function getEffectiveDecoratorArguments(node) {
    const expr ***REMOVED*** node.expression;
    const signature ***REMOVED*** getDecoratorCallSignature(node);
    if (signature) {
      const args ***REMOVED*** [];
      for (const param of signature.parameters) {
        const type ***REMOVED*** getTypeOfSymbol(param);
        args.push(createSyntheticExpression(expr, type));
      }
      return args;
    }
    return Debug.fail();
  }
  function getDecoratorArgumentCount(node, signature) {
    return compilerOptions.experimentalDecorators ? getLegacyDecoratorArgumentCount(node, signature) : (
      // Allow the runtime to oversupply arguments to an ES decorator as long as there's at least one parameter.
      Math.min(Math.max(getParameterCount(signature), 1), 2)
    );
  }
  function getLegacyDecoratorArgumentCount(node, signature) {
    switch (node.parent.kind) {
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        return 1;
      case 173 /* PropertyDeclaration */:
        return hasAccessorModifier(node.parent) ? 3 : 2;
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return signature.parameters.length <***REMOVED*** 2 ? 2 : 3;
      case 170 /* Parameter */:
        return 3;
      default:
        return Debug.fail();
    }
  }
  function getDiagnosticSpanForCallNode(node) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    const { start, length: length2 } ***REMOVED*** getErrorSpanForNode(sourceFile, isPropertyAccessExpression(node.expression) ? node.expression.name : node.expression);
    return { start, length: length2, sourceFile };
  }
  function getDiagnosticForCallNode(node, message, ...args) {
    if (isCallExpression(node)) {
      const { sourceFile, start, length: length2 } ***REMOVED*** getDiagnosticSpanForCallNode(node);
      if ("message" in message) {
        return createFileDiagnostic(sourceFile, start, length2, message, ...args);
      }
      return createDiagnosticForFileFromMessageChain(sourceFile, message);
    } else {
      if ("message" in message) {
        return createDiagnosticForNode(node, message, ...args);
      }
      return createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(node), node, message);
    }
  }
  function getErrorNodeForCallNode(callLike) {
    if (isCallOrNewExpression(callLike)) {
      return isPropertyAccessExpression(callLike.expression) ? callLike.expression.name : callLike.expression;
    }
    if (isTaggedTemplateExpression(callLike)) {
      return isPropertyAccessExpression(callLike.tag) ? callLike.tag.name : callLike.tag;
    }
    if (isJsxOpeningLikeElement(callLike)) {
      return callLike.tagName;
    }
    return callLike;
  }
  function isPromiseResolveArityError(node) {
    if (!isCallExpression(node) || !isIdentifier(node.expression)) return false;
    const symbol ***REMOVED*** resolveName(
      node.expression,
      node.expression.escapedText,
      111551 /* Value */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      false
    );
    const decl ***REMOVED*** symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.valueDeclaration;
    if (!decl || !isParameter(decl) || !isFunctionExpressionOrArrowFunction(decl.parent) || !isNewExpression(decl.parent.parent) || !isIdentifier(decl.parent.parent.expression)) {
      return false;
    }
    const globalPromiseSymbol ***REMOVED*** getGlobalPromiseConstructorSymbol(
      /*reportErrors*/
      false
    );
    if (!globalPromiseSymbol) return false;
    const constructorSymbol ***REMOVED*** getSymbolAtLocation(
      decl.parent.parent.expression,
      /*ignoreErrors*/
      true
    );
    return constructorSymbol ***REMOVED******REMOVED******REMOVED*** globalPromiseSymbol;
  }
  function getArgumentArityError(node, signatures, args, headMessage) {
    var _a;
    const spreadIndex ***REMOVED*** getSpreadArgumentIndex(args);
    if (spreadIndex > -1) {
      return createDiagnosticForNode(args[spreadIndex], Diagnostics.A_spread_argument_must_either_have_a_tuple_type_or_be_passed_to_a_rest_parameter);
    }
    let min2 ***REMOVED*** Number.POSITIVE_INFINITY;
    let max ***REMOVED*** Number.NEGATIVE_INFINITY;
    let maxBelow ***REMOVED*** Number.NEGATIVE_INFINITY;
    let minAbove ***REMOVED*** Number.POSITIVE_INFINITY;
    let closestSignature;
    for (const sig of signatures) {
      const minParameter ***REMOVED*** getMinArgumentCount(sig);
      const maxParameter ***REMOVED*** getParameterCount(sig);
      if (minParameter < min2) {
        min2 ***REMOVED*** minParameter;
        closestSignature ***REMOVED*** sig;
      }
      max ***REMOVED*** Math.max(max, maxParameter);
      if (minParameter < args.length && minParameter > maxBelow) maxBelow ***REMOVED*** minParameter;
      if (args.length < maxParameter && maxParameter < minAbove) minAbove ***REMOVED*** maxParameter;
    }
    const hasRestParameter2 ***REMOVED*** some(signatures, hasEffectiveRestParameter);
    const parameterRange ***REMOVED*** hasRestParameter2 ? min2 : min2 < max ? min2 + "-" + max : min2;
    const isVoidPromiseError ***REMOVED*** !hasRestParameter2 && parameterRange ***REMOVED******REMOVED******REMOVED*** 1 && args.length ***REMOVED******REMOVED******REMOVED*** 0 && isPromiseResolveArityError(node);
    if (isVoidPromiseError && isInJSFile(node)) {
      return getDiagnosticForCallNode(node, Diagnostics.Expected_1_argument_but_got_0_new_Promise_needs_a_JSDoc_hint_to_produce_a_resolve_that_can_be_called_without_arguments);
    }
    const error2 ***REMOVED*** isDecorator(node) ? hasRestParameter2 ? Diagnostics.The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_at_least_0 : Diagnostics.The_runtime_will_invoke_the_decorator_with_1_arguments_but_the_decorator_expects_0 : hasRestParameter2 ? Diagnostics.Expected_at_least_0_arguments_but_got_1 : isVoidPromiseError ? Diagnostics.Expected_0_arguments_but_got_1_Did_you_forget_to_include_void_in_your_type_argument_to_Promise : Diagnostics.Expected_0_arguments_but_got_1;
    if (min2 < args.length && args.length < max) {
      if (headMessage) {
        let chain ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          Diagnostics.No_overload_expects_0_arguments_but_overloads_do_exist_that_expect_either_1_or_2_arguments,
          args.length,
          maxBelow,
          minAbove
        );
        chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
        return getDiagnosticForCallNode(node, chain);
      }
      return getDiagnosticForCallNode(node, Diagnostics.No_overload_expects_0_arguments_but_overloads_do_exist_that_expect_either_1_or_2_arguments, args.length, maxBelow, minAbove);
    } else if (args.length < min2) {
      let diagnostic;
      if (headMessage) {
        let chain ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          error2,
          parameterRange,
          args.length
        );
        chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
        diagnostic ***REMOVED*** getDiagnosticForCallNode(node, chain);
      } else {
        diagnostic ***REMOVED*** getDiagnosticForCallNode(node, error2, parameterRange, args.length);
      }
      const parameter ***REMOVED*** (_a ***REMOVED*** closestSignature ***REMOVED******REMOVED*** null ? void 0 : closestSignature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a.parameters[closestSignature.thisParameter ? args.length + 1 : args.length];
      if (parameter) {
        const messageAndArgs ***REMOVED*** isBindingPattern(parameter.name) ? [Diagnostics.An_argument_matching_this_binding_pattern_was_not_provided] : isRestParameter(parameter) ? [Diagnostics.Arguments_for_the_rest_parameter_0_were_not_provided, idText(getFirstIdentifier(parameter.name))] : [Diagnostics.An_argument_for_0_was_not_provided, !parameter.name ? args.length : idText(getFirstIdentifier(parameter.name))];
        const parameterError ***REMOVED*** createDiagnosticForNode(parameter, ...messageAndArgs);
        return addRelatedInfo(diagnostic, parameterError);
      }
      return diagnostic;
    } else {
      const errorSpan ***REMOVED*** factory.createNodeArray(args.slice(max));
      const pos ***REMOVED*** first(errorSpan).pos;
      let end ***REMOVED*** last(errorSpan).end;
      if (end ***REMOVED******REMOVED******REMOVED*** pos) {
        end++;
      }
      setTextRangePosEnd(errorSpan, pos, end);
      if (headMessage) {
        let chain ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          error2,
          parameterRange,
          args.length
        );
        chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
        return createDiagnosticForNodeArrayFromMessageChain(getSourceFileOfNode(node), errorSpan, chain);
      }
      return createDiagnosticForNodeArray(getSourceFileOfNode(node), errorSpan, error2, parameterRange, args.length);
    }
  }
  function getTypeArgumentArityError(node, signatures, typeArguments, headMessage) {
    const argCount ***REMOVED*** typeArguments.length;
    if (signatures.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const sig ***REMOVED*** signatures[0];
      const min2 ***REMOVED*** getMinTypeArgumentCount(sig.typeParameters);
      const max ***REMOVED*** length(sig.typeParameters);
      if (headMessage) {
        let chain ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          Diagnostics.Expected_0_type_arguments_but_got_1,
          min2 < max ? min2 + "-" + max : min2,
          argCount
        );
        chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
        return createDiagnosticForNodeArrayFromMessageChain(getSourceFileOfNode(node), typeArguments, chain);
      }
      return createDiagnosticForNodeArray(getSourceFileOfNode(node), typeArguments, Diagnostics.Expected_0_type_arguments_but_got_1, min2 < max ? min2 + "-" + max : min2, argCount);
    }
    let belowArgCount ***REMOVED*** -Infinity;
    let aboveArgCount ***REMOVED*** Infinity;
    for (const sig of signatures) {
      const min2 ***REMOVED*** getMinTypeArgumentCount(sig.typeParameters);
      const max ***REMOVED*** length(sig.typeParameters);
      if (min2 > argCount) {
        aboveArgCount ***REMOVED*** Math.min(aboveArgCount, min2);
      } else if (max < argCount) {
        belowArgCount ***REMOVED*** Math.max(belowArgCount, max);
      }
    }
    if (belowArgCount !***REMOVED******REMOVED*** -Infinity && aboveArgCount !***REMOVED******REMOVED*** Infinity) {
      if (headMessage) {
        let chain ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          Diagnostics.No_overload_expects_0_type_arguments_but_overloads_do_exist_that_expect_either_1_or_2_type_arguments,
          argCount,
          belowArgCount,
          aboveArgCount
        );
        chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
        return createDiagnosticForNodeArrayFromMessageChain(getSourceFileOfNode(node), typeArguments, chain);
      }
      return createDiagnosticForNodeArray(getSourceFileOfNode(node), typeArguments, Diagnostics.No_overload_expects_0_type_arguments_but_overloads_do_exist_that_expect_either_1_or_2_type_arguments, argCount, belowArgCount, aboveArgCount);
    }
    if (headMessage) {
      let chain ***REMOVED*** chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.Expected_0_type_arguments_but_got_1,
        belowArgCount ***REMOVED******REMOVED******REMOVED*** -Infinity ? aboveArgCount : belowArgCount,
        argCount
      );
      chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
      return createDiagnosticForNodeArrayFromMessageChain(getSourceFileOfNode(node), typeArguments, chain);
    }
    return createDiagnosticForNodeArray(getSourceFileOfNode(node), typeArguments, Diagnostics.Expected_0_type_arguments_but_got_1, belowArgCount ***REMOVED******REMOVED******REMOVED*** -Infinity ? aboveArgCount : belowArgCount, argCount);
  }
  function resolveCall(node, signatures, candidatesOutArray, checkMode, callChainFlags, headMessage) {
    const isTaggedTemplate ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */;
    const isDecorator2 ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 171 /* Decorator */;
    const isJsxOpeningOrSelfClosingElement ***REMOVED*** isJsxOpeningLikeElement(node);
    const isJsxOpenFragment ***REMOVED*** isJsxOpeningFragment(node);
    const isInstanceof ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */;
    const reportErrors2 ***REMOVED*** !isInferencePartiallyBlocked && !candidatesOutArray;
    let candidatesForArgumentError;
    let candidateForArgumentArityError;
    let candidateForTypeArgumentError;
    let result;
    let argCheckMode ***REMOVED*** 0 /* Normal */;
    let candidates ***REMOVED*** [];
    let typeArguments;
    if (!isDecorator2 && !isInstanceof && !isSuperCall(node) && !isJsxOpenFragment) {
      typeArguments ***REMOVED*** node.typeArguments;
      if (isTaggedTemplate || isJsxOpeningOrSelfClosingElement || node.expression.kind !***REMOVED******REMOVED*** 108 /* SuperKeyword */) {
        forEach(typeArguments, checkSourceElement);
      }
    }
    candidates ***REMOVED*** candidatesOutArray || [];
    reorderCandidates(signatures, candidates, callChainFlags);
    if (!isJsxOpenFragment) {
      if (!candidates.length) {
        if (reportErrors2) {
          diagnostics.add(getDiagnosticForCallNode(node, Diagnostics.Call_target_does_not_contain_any_signatures));
        }
        return resolveErrorCall(node);
      }
    }
    const args ***REMOVED*** getEffectiveCallArguments(node);
    const isSingleNonGenericCandidate ***REMOVED*** candidates.length ***REMOVED******REMOVED******REMOVED*** 1 && !candidates[0].typeParameters;
    if (!isDecorator2 && !isSingleNonGenericCandidate && some(args, isContextSensitive)) {
      argCheckMode ***REMOVED*** 4 /* SkipContextSensitive */;
    }
    const signatureHelpTrailingComma ***REMOVED*** !!(checkMode & 16 /* IsForSignatureHelp */) && node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && node.arguments.hasTrailingComma;
    if (candidates.length > 1) {
      result ***REMOVED*** chooseOverload(candidates, subtypeRelation, isSingleNonGenericCandidate, signatureHelpTrailingComma);
    }
    if (!result) {
      result ***REMOVED*** chooseOverload(candidates, assignableRelation, isSingleNonGenericCandidate, signatureHelpTrailingComma);
    }
    const links ***REMOVED*** getNodeLinks(node);
    if (links.resolvedSignature !***REMOVED******REMOVED*** resolvingSignature && !candidatesOutArray) {
      Debug.assert(links.resolvedSignature);
      return links.resolvedSignature;
    }
    if (result) {
      return result;
    }
    result ***REMOVED*** getCandidateForOverloadFailure(node, candidates, args, !!candidatesOutArray, checkMode);
    links.resolvedSignature ***REMOVED*** result;
    if (reportErrors2) {
      if (!headMessage && isInstanceof) {
        headMessage ***REMOVED*** Diagnostics.The_left_hand_side_of_an_instanceof_expression_must_be_assignable_to_the_first_argument_of_the_right_hand_side_s_Symbol_hasInstance_method;
      }
      if (candidatesForArgumentError) {
        if (candidatesForArgumentError.length ***REMOVED******REMOVED******REMOVED*** 1 || candidatesForArgumentError.length > 3) {
          const last2 ***REMOVED*** candidatesForArgumentError[candidatesForArgumentError.length - 1];
          let chain;
          if (candidatesForArgumentError.length > 3) {
            chain ***REMOVED*** chainDiagnosticMessages(chain, Diagnostics.The_last_overload_gave_the_following_error);
            chain ***REMOVED*** chainDiagnosticMessages(chain, Diagnostics.No_overload_matches_this_call);
          }
          if (headMessage) {
            chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
          }
          const diags ***REMOVED*** getSignatureApplicabilityError(
            node,
            args,
            last2,
            assignableRelation,
            0 /* Normal */,
            /*reportErrors*/
            true,
            () ***REMOVED***> chain
          );
          if (diags) {
            for (const d of diags) {
              if (last2.declaration && candidatesForArgumentError.length > 3) {
                addRelatedInfo(d, createDiagnosticForNode(last2.declaration, Diagnostics.The_last_overload_is_declared_here));
              }
              addImplementationSuccessElaboration(last2, d);
              diagnostics.add(d);
            }
          } else {
            Debug.fail("No error for last overload signature");
          }
        } else {
          const allDiagnostics ***REMOVED*** [];
          let max ***REMOVED*** 0;
          let min2 ***REMOVED*** Number.MAX_VALUE;
          let minIndex ***REMOVED*** 0;
          let i ***REMOVED*** 0;
          for (const c of candidatesForArgumentError) {
            const chain2 ***REMOVED*** () ***REMOVED***> chainDiagnosticMessages(
              /*details*/
              void 0,
              Diagnostics.Overload_0_of_1_2_gave_the_following_error,
              i + 1,
              candidates.length,
              signatureToString(c)
            );
            const diags2 ***REMOVED*** getSignatureApplicabilityError(
              node,
              args,
              c,
              assignableRelation,
              0 /* Normal */,
              /*reportErrors*/
              true,
              chain2
            );
            if (diags2) {
              if (diags2.length <***REMOVED*** min2) {
                min2 ***REMOVED*** diags2.length;
                minIndex ***REMOVED*** i;
              }
              max ***REMOVED*** Math.max(max, diags2.length);
              allDiagnostics.push(diags2);
            } else {
              Debug.fail("No error for 3 or fewer overload signatures");
            }
            i++;
          }
          const diags ***REMOVED*** max > 1 ? allDiagnostics[minIndex] : flatten(allDiagnostics);
          Debug.assert(diags.length > 0, "No errors reported for 3 or fewer overload signatures");
          let chain ***REMOVED*** chainDiagnosticMessages(
            map(diags, createDiagnosticMessageChainFromDiagnostic),
            Diagnostics.No_overload_matches_this_call
          );
          if (headMessage) {
            chain ***REMOVED*** chainDiagnosticMessages(chain, headMessage);
          }
          const related ***REMOVED*** [...flatMap(diags, (d) ***REMOVED***> d.relatedInformation)];
          let diag2;
          if (every(diags, (d) ***REMOVED***> d.start ***REMOVED******REMOVED******REMOVED*** diags[0].start && d.length ***REMOVED******REMOVED******REMOVED*** diags[0].length && d.file ***REMOVED******REMOVED******REMOVED*** diags[0].file)) {
            const { file, start, length: length2 } ***REMOVED*** diags[0];
            diag2 ***REMOVED*** { file, start, length: length2, code: chain.code, category: chain.category, messageText: chain, relatedInformation: related };
          } else {
            diag2 ***REMOVED*** createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(node), getErrorNodeForCallNode(node), chain, related);
          }
          addImplementationSuccessElaboration(candidatesForArgumentError[0], diag2);
          diagnostics.add(diag2);
        }
      } else if (candidateForArgumentArityError) {
        diagnostics.add(getArgumentArityError(node, [candidateForArgumentArityError], args, headMessage));
      } else if (candidateForTypeArgumentError) {
        checkTypeArguments(
          candidateForTypeArgumentError,
          node.typeArguments,
          /*reportErrors*/
          true,
          headMessage
        );
      } else if (!isJsxOpenFragment) {
        const signaturesWithCorrectTypeArgumentArity ***REMOVED*** filter(signatures, (s) ***REMOVED***> hasCorrectTypeArgumentArity(s, typeArguments));
        if (signaturesWithCorrectTypeArgumentArity.length ***REMOVED******REMOVED******REMOVED*** 0) {
          diagnostics.add(getTypeArgumentArityError(node, signatures, typeArguments, headMessage));
        } else {
          diagnostics.add(getArgumentArityError(node, signaturesWithCorrectTypeArgumentArity, args, headMessage));
        }
      }
    }
    return result;
    function addImplementationSuccessElaboration(failed2, diagnostic) {
      var _a, _b;
      const oldCandidatesForArgumentError ***REMOVED*** candidatesForArgumentError;
      const oldCandidateForArgumentArityError ***REMOVED*** candidateForArgumentArityError;
      const oldCandidateForTypeArgumentError ***REMOVED*** candidateForTypeArgumentError;
      const failedSignatureDeclarations ***REMOVED*** ((_b ***REMOVED*** (_a ***REMOVED*** failed2.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a.symbol) ***REMOVED******REMOVED*** null ? void 0 : _b.declarations) || emptyArray;
      const isOverload ***REMOVED*** failedSignatureDeclarations.length > 1;
      const implDecl ***REMOVED*** isOverload ? find(failedSignatureDeclarations, (d) ***REMOVED***> isFunctionLikeDeclaration(d) && nodeIsPresent(d.body)) : void 0;
      if (implDecl) {
        const candidate ***REMOVED*** getSignatureFromDeclaration(implDecl);
        const isSingleNonGenericCandidate2 ***REMOVED*** !candidate.typeParameters;
        if (chooseOverload([candidate], assignableRelation, isSingleNonGenericCandidate2)) {
          addRelatedInfo(diagnostic, createDiagnosticForNode(implDecl, Diagnostics.The_call_would_have_succeeded_against_this_implementation_but_implementation_signatures_of_overloads_are_not_externally_visible));
        }
      }
      candidatesForArgumentError ***REMOVED*** oldCandidatesForArgumentError;
      candidateForArgumentArityError ***REMOVED*** oldCandidateForArgumentArityError;
      candidateForTypeArgumentError ***REMOVED*** oldCandidateForTypeArgumentError;
    }
    function chooseOverload(candidates2, relation, isSingleNonGenericCandidate2, signatureHelpTrailingComma2 ***REMOVED*** false) {
      candidatesForArgumentError ***REMOVED*** void 0;
      candidateForArgumentArityError ***REMOVED*** void 0;
      candidateForTypeArgumentError ***REMOVED*** void 0;
      if (isSingleNonGenericCandidate2) {
        const candidate ***REMOVED*** candidates2[0];
        if (some(typeArguments) || !hasCorrectArity(node, args, candidate, signatureHelpTrailingComma2)) {
          return void 0;
        }
        if (getSignatureApplicabilityError(
          node,
          args,
          candidate,
          relation,
          0 /* Normal */,
          /*reportErrors*/
          false,
          /*containingMessageChain*/
          void 0
        )) {
          candidatesForArgumentError ***REMOVED*** [candidate];
          return void 0;
        }
        return candidate;
      }
      for (let candidateIndex ***REMOVED*** 0; candidateIndex < candidates2.length; candidateIndex++) {
        const candidate ***REMOVED*** candidates2[candidateIndex];
        if (!hasCorrectTypeArgumentArity(candidate, typeArguments) || !hasCorrectArity(node, args, candidate, signatureHelpTrailingComma2)) {
          continue;
        }
        let checkCandidate;
        let inferenceContext;
        if (candidate.typeParameters) {
          let typeArgumentTypes;
          if (some(typeArguments)) {
            typeArgumentTypes ***REMOVED*** checkTypeArguments(
              candidate,
              typeArguments,
              /*reportErrors*/
              false
            );
            if (!typeArgumentTypes) {
              candidateForTypeArgumentError ***REMOVED*** candidate;
              continue;
            }
          } else {
            inferenceContext ***REMOVED*** createInferenceContext(
              candidate.typeParameters,
              candidate,
              /*flags*/
              isInJSFile(node) ? 2 /* AnyDefault */ : 0 /* None */
            );
            typeArgumentTypes ***REMOVED*** inferTypeArguments(node, candidate, args, argCheckMode | 8 /* SkipGenericFunctions */, inferenceContext);
            argCheckMode |***REMOVED*** inferenceContext.flags & 4 /* SkippedGenericFunction */ ? 8 /* SkipGenericFunctions */ : 0 /* Normal */;
          }
          checkCandidate ***REMOVED*** getSignatureInstantiation(candidate, typeArgumentTypes, isInJSFile(candidate.declaration), inferenceContext && inferenceContext.inferredTypeParameters);
          if (getNonArrayRestType(candidate) && !hasCorrectArity(node, args, checkCandidate, signatureHelpTrailingComma2)) {
            candidateForArgumentArityError ***REMOVED*** checkCandidate;
            continue;
          }
        } else {
          checkCandidate ***REMOVED*** candidate;
        }
        if (getSignatureApplicabilityError(
          node,
          args,
          checkCandidate,
          relation,
          argCheckMode,
          /*reportErrors*/
          false,
          /*containingMessageChain*/
          void 0
        )) {
          (candidatesForArgumentError || (candidatesForArgumentError ***REMOVED*** [])).push(checkCandidate);
          continue;
        }
        if (argCheckMode) {
          argCheckMode ***REMOVED*** 0 /* Normal */;
          if (inferenceContext) {
            const typeArgumentTypes ***REMOVED*** inferTypeArguments(node, candidate, args, argCheckMode, inferenceContext);
            checkCandidate ***REMOVED*** getSignatureInstantiation(candidate, typeArgumentTypes, isInJSFile(candidate.declaration), inferenceContext.inferredTypeParameters);
            if (getNonArrayRestType(candidate) && !hasCorrectArity(node, args, checkCandidate, signatureHelpTrailingComma2)) {
              candidateForArgumentArityError ***REMOVED*** checkCandidate;
              continue;
            }
          }
          if (getSignatureApplicabilityError(
            node,
            args,
            checkCandidate,
            relation,
            argCheckMode,
            /*reportErrors*/
            false,
            /*containingMessageChain*/
            void 0
          )) {
            (candidatesForArgumentError || (candidatesForArgumentError ***REMOVED*** [])).push(checkCandidate);
            continue;
          }
        }
        candidates2[candidateIndex] ***REMOVED*** checkCandidate;
        return checkCandidate;
      }
      return void 0;
    }
  }
  function getCandidateForOverloadFailure(node, candidates, args, hasCandidatesOutArray, checkMode) {
    Debug.assert(candidates.length > 0);
    checkNodeDeferred(node);
    return hasCandidatesOutArray || candidates.length ***REMOVED******REMOVED******REMOVED*** 1 || candidates.some((c) ***REMOVED***> !!c.typeParameters) ? pickLongestCandidateSignature(node, candidates, args, checkMode) : createUnionOfSignaturesForOverloadFailure(candidates);
  }
  function createUnionOfSignaturesForOverloadFailure(candidates) {
    const thisParameters ***REMOVED*** mapDefined(candidates, (c) ***REMOVED***> c.thisParameter);
    let thisParameter;
    if (thisParameters.length) {
      thisParameter ***REMOVED*** createCombinedSymbolFromTypes(thisParameters, thisParameters.map(getTypeOfParameter));
    }
    const { min: minArgumentCount, max: maxNonRestParam } ***REMOVED*** minAndMax(candidates, getNumNonRestParameters);
    const parameters ***REMOVED*** [];
    for (let i ***REMOVED*** 0; i < maxNonRestParam; i++) {
      const symbols ***REMOVED*** mapDefined(candidates, (s) ***REMOVED***> signatureHasRestParameter(s) ? i < s.parameters.length - 1 ? s.parameters[i] : last(s.parameters) : i < s.parameters.length ? s.parameters[i] : void 0);
      Debug.assert(symbols.length !***REMOVED******REMOVED*** 0);
      parameters.push(createCombinedSymbolFromTypes(symbols, mapDefined(candidates, (candidate) ***REMOVED***> tryGetTypeAtPosition(candidate, i))));
    }
    const restParameterSymbols ***REMOVED*** mapDefined(candidates, (c) ***REMOVED***> signatureHasRestParameter(c) ? last(c.parameters) : void 0);
    let flags ***REMOVED*** 128 /* IsSignatureCandidateForOverloadFailure */;
    if (restParameterSymbols.length !***REMOVED******REMOVED*** 0) {
      const type ***REMOVED*** createArrayType(getUnionType(mapDefined(candidates, tryGetRestTypeOfSignature), 2 /* Subtype */));
      parameters.push(createCombinedSymbolForOverloadFailure(restParameterSymbols, type));
      flags |***REMOVED*** 1 /* HasRestParameter */;
    }
    if (candidates.some(signatureHasLiteralTypes)) {
      flags |***REMOVED*** 2 /* HasLiteralTypes */;
    }
    return createSignature(
      candidates[0].declaration,
      /*typeParameters*/
      void 0,
      // Before calling this we tested for `!candidates.some(c ***REMOVED***> !!c.typeParameters)`.
      thisParameter,
      parameters,
      /*resolvedReturnType*/
      getIntersectionType(candidates.map(getReturnTypeOfSignature)),
      /*resolvedTypePredicate*/
      void 0,
      minArgumentCount,
      flags
    );
  }
  function getNumNonRestParameters(signature) {
    const numParams ***REMOVED*** signature.parameters.length;
    return signatureHasRestParameter(signature) ? numParams - 1 : numParams;
  }
  function createCombinedSymbolFromTypes(sources, types) {
    return createCombinedSymbolForOverloadFailure(sources, getUnionType(types, 2 /* Subtype */));
  }
  function createCombinedSymbolForOverloadFailure(sources, type) {
    return createSymbolWithType(first(sources), type);
  }
  function pickLongestCandidateSignature(node, candidates, args, checkMode) {
    const bestIndex ***REMOVED*** getLongestCandidateIndex(candidates, apparentArgumentCount ***REMOVED******REMOVED******REMOVED*** void 0 ? args.length : apparentArgumentCount);
    const candidate ***REMOVED*** candidates[bestIndex];
    const { typeParameters } ***REMOVED*** candidate;
    if (!typeParameters) {
      return candidate;
    }
    const typeArgumentNodes ***REMOVED*** callLikeExpressionMayHaveTypeArguments(node) ? node.typeArguments : void 0;
    const instantiated ***REMOVED*** typeArgumentNodes ? createSignatureInstantiation(candidate, getTypeArgumentsFromNodes(typeArgumentNodes, typeParameters, isInJSFile(node))) : inferSignatureInstantiationForOverloadFailure(node, typeParameters, candidate, args, checkMode);
    candidates[bestIndex] ***REMOVED*** instantiated;
    return instantiated;
  }
  function getTypeArgumentsFromNodes(typeArgumentNodes, typeParameters, isJs) {
    const typeArguments ***REMOVED*** typeArgumentNodes.map(getTypeOfNode);
    while (typeArguments.length > typeParameters.length) {
      typeArguments.pop();
    }
    while (typeArguments.length < typeParameters.length) {
      typeArguments.push(getDefaultFromTypeParameter(typeParameters[typeArguments.length]) || getConstraintOfTypeParameter(typeParameters[typeArguments.length]) || getDefaultTypeArgumentType(isJs));
    }
    return typeArguments;
  }
  function inferSignatureInstantiationForOverloadFailure(node, typeParameters, candidate, args, checkMode) {
    const inferenceContext ***REMOVED*** createInferenceContext(
      typeParameters,
      candidate,
      /*flags*/
      isInJSFile(node) ? 2 /* AnyDefault */ : 0 /* None */
    );
    const typeArgumentTypes ***REMOVED*** inferTypeArguments(node, candidate, args, checkMode | 4 /* SkipContextSensitive */ | 8 /* SkipGenericFunctions */, inferenceContext);
    return createSignatureInstantiation(candidate, typeArgumentTypes);
  }
  function getLongestCandidateIndex(candidates, argsCount) {
    let maxParamsIndex ***REMOVED*** -1;
    let maxParams ***REMOVED*** -1;
    for (let i ***REMOVED*** 0; i < candidates.length; i++) {
      const candidate ***REMOVED*** candidates[i];
      const paramCount ***REMOVED*** getParameterCount(candidate);
      if (hasEffectiveRestParameter(candidate) || paramCount >***REMOVED*** argsCount) {
        return i;
      }
      if (paramCount > maxParams) {
        maxParams ***REMOVED*** paramCount;
        maxParamsIndex ***REMOVED*** i;
      }
    }
    return maxParamsIndex;
  }
  function resolveCallExpression(node, candidatesOutArray, checkMode) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      const superType ***REMOVED*** checkSuperExpression(node.expression);
      if (isTypeAny(superType)) {
        for (const arg of node.arguments) {
          checkExpression(arg);
        }
        return anySignature;
      }
      if (!isErrorType(superType)) {
        const baseTypeNode ***REMOVED*** getEffectiveBaseTypeNode(getContainingClass(node));
        if (baseTypeNode) {
          const baseConstructors ***REMOVED*** getInstantiatedConstructorsForTypeArguments(superType, baseTypeNode.typeArguments, baseTypeNode);
          return resolveCall(node, baseConstructors, candidatesOutArray, checkMode, 0 /* None */);
        }
      }
      return resolveUntypedCall(node);
    }
    let callChainFlags;
    let funcType ***REMOVED*** checkExpression(node.expression);
    if (isCallChain(node)) {
      const nonOptionalType ***REMOVED*** getOptionalExpressionType(funcType, node.expression);
      callChainFlags ***REMOVED*** nonOptionalType ***REMOVED******REMOVED******REMOVED*** funcType ? 0 /* None */ : isOutermostOptionalChain(node) ? 16 /* IsOuterCallChain */ : 8 /* IsInnerCallChain */;
      funcType ***REMOVED*** nonOptionalType;
    } else {
      callChainFlags ***REMOVED*** 0 /* None */;
    }
    funcType ***REMOVED*** checkNonNullTypeWithReporter(
      funcType,
      node.expression,
      reportCannotInvokePossiblyNullOrUndefinedError
    );
    if (funcType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverSignature;
    }
    const apparentType ***REMOVED*** getApparentType(funcType);
    if (isErrorType(apparentType)) {
      return resolveErrorCall(node);
    }
    const callSignatures ***REMOVED*** getSignaturesOfType(apparentType, 0 /* Call */);
    const numConstructSignatures ***REMOVED*** getSignaturesOfType(apparentType, 1 /* Construct */).length;
    if (isUntypedFunctionCall(funcType, apparentType, callSignatures.length, numConstructSignatures)) {
      if (!isErrorType(funcType) && node.typeArguments) {
        error(node, Diagnostics.Untyped_function_calls_may_not_accept_type_arguments);
      }
      return resolveUntypedCall(node);
    }
    if (!callSignatures.length) {
      if (numConstructSignatures) {
        error(node, Diagnostics.Value_of_type_0_is_not_callable_Did_you_mean_to_include_new, typeToString(funcType));
      } else {
        let relatedInformation;
        if (node.arguments.length ***REMOVED******REMOVED******REMOVED*** 1) {
          const text ***REMOVED*** getSourceFileOfNode(node).text;
          if (isLineBreak(text.charCodeAt(skipTrivia(
            text,
            node.expression.end,
            /*stopAfterLineBreak*/
            true
          ) - 1))) {
            relatedInformation ***REMOVED*** createDiagnosticForNode(node.expression, Diagnostics.Are_you_missing_a_semicolon);
          }
        }
        invocationError(node.expression, apparentType, 0 /* Call */, relatedInformation);
      }
      return resolveErrorCall(node);
    }
    if (checkMode & 8 /* SkipGenericFunctions */ && !node.typeArguments && callSignatures.some(isGenericFunctionReturningFunction)) {
      skippedGenericFunction(node, checkMode);
      return resolvingSignature;
    }
    if (callSignatures.some((sig) ***REMOVED***> isInJSFile(sig.declaration) && !!getJSDocClassTag(sig.declaration))) {
      error(node, Diagnostics.Value_of_type_0_is_not_callable_Did_you_mean_to_include_new, typeToString(funcType));
      return resolveErrorCall(node);
    }
    return resolveCall(node, callSignatures, candidatesOutArray, checkMode, callChainFlags);
  }
  function isGenericFunctionReturningFunction(signature) {
    return !!(signature.typeParameters && isFunctionType(getReturnTypeOfSignature(signature)));
  }
  function isUntypedFunctionCall(funcType, apparentFuncType, numCallSignatures, numConstructSignatures) {
    return isTypeAny(funcType) || isTypeAny(apparentFuncType) && !!(funcType.flags & 262144 /* TypeParameter */) || !numCallSignatures && !numConstructSignatures && !(apparentFuncType.flags & 1048576 /* Union */) && !(getReducedType(apparentFuncType).flags & 131072 /* Never */) && isTypeAssignableTo(funcType, globalFunctionType);
  }
  function resolveNewExpression(node, candidatesOutArray, checkMode) {
    let expressionType ***REMOVED*** checkNonNullExpression(node.expression);
    if (expressionType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverSignature;
    }
    expressionType ***REMOVED*** getApparentType(expressionType);
    if (isErrorType(expressionType)) {
      return resolveErrorCall(node);
    }
    if (isTypeAny(expressionType)) {
      if (node.typeArguments) {
        error(node, Diagnostics.Untyped_function_calls_may_not_accept_type_arguments);
      }
      return resolveUntypedCall(node);
    }
    const constructSignatures ***REMOVED*** getSignaturesOfType(expressionType, 1 /* Construct */);
    if (constructSignatures.length) {
      if (!isConstructorAccessible(node, constructSignatures[0])) {
        return resolveErrorCall(node);
      }
      if (someSignature(constructSignatures, (signature) ***REMOVED***> !!(signature.flags & 4 /* Abstract */))) {
        error(node, Diagnostics.Cannot_create_an_instance_of_an_abstract_class);
        return resolveErrorCall(node);
      }
      const valueDecl ***REMOVED*** expressionType.symbol && getClassLikeDeclarationOfSymbol(expressionType.symbol);
      if (valueDecl && hasSyntacticModifier(valueDecl, 64 /* Abstract */)) {
        error(node, Diagnostics.Cannot_create_an_instance_of_an_abstract_class);
        return resolveErrorCall(node);
      }
      return resolveCall(node, constructSignatures, candidatesOutArray, checkMode, 0 /* None */);
    }
    const callSignatures ***REMOVED*** getSignaturesOfType(expressionType, 0 /* Call */);
    if (callSignatures.length) {
      const signature ***REMOVED*** resolveCall(node, callSignatures, candidatesOutArray, checkMode, 0 /* None */);
      if (!noImplicitAny) {
        if (signature.declaration && !isJSConstructor(signature.declaration) && getReturnTypeOfSignature(signature) !***REMOVED******REMOVED*** voidType) {
          error(node, Diagnostics.Only_a_void_function_can_be_called_with_the_new_keyword);
        }
        if (getThisTypeOfSignature(signature) ***REMOVED******REMOVED******REMOVED*** voidType) {
          error(node, Diagnostics.A_function_that_is_called_with_the_new_keyword_cannot_have_a_this_type_that_is_void);
        }
      }
      return signature;
    }
    invocationError(node.expression, expressionType, 1 /* Construct */);
    return resolveErrorCall(node);
  }
  function someSignature(signatures, f) {
    if (isArray(signatures)) {
      return some(signatures, (signature) ***REMOVED***> someSignature(signature, f));
    }
    return signatures.compositeKind ***REMOVED******REMOVED******REMOVED*** 1048576 /* Union */ ? some(signatures.compositeSignatures, f) : f(signatures);
  }
  function typeHasProtectedAccessibleBase(target, type) {
    const baseTypes ***REMOVED*** getBaseTypes(type);
    if (!length(baseTypes)) {
      return false;
    }
    const firstBase ***REMOVED*** baseTypes[0];
    if (firstBase.flags & 2097152 /* Intersection */) {
      const types ***REMOVED*** firstBase.types;
      const mixinFlags ***REMOVED*** findMixins(types);
      let i ***REMOVED*** 0;
      for (const intersectionMember of firstBase.types) {
        if (!mixinFlags[i]) {
          if (getObjectFlags(intersectionMember) & (1 /* Class */ | 2 /* Interface */)) {
            if (intersectionMember.symbol ***REMOVED******REMOVED******REMOVED*** target) {
              return true;
            }
            if (typeHasProtectedAccessibleBase(target, intersectionMember)) {
              return true;
            }
          }
        }
        i++;
      }
      return false;
    }
    if (firstBase.symbol ***REMOVED******REMOVED******REMOVED*** target) {
      return true;
    }
    return typeHasProtectedAccessibleBase(target, firstBase);
  }
  function isConstructorAccessible(node, signature) {
    if (!signature || !signature.declaration) {
      return true;
    }
    const declaration ***REMOVED*** signature.declaration;
    const modifiers ***REMOVED*** getSelectedEffectiveModifierFlags(declaration, 6 /* NonPublicAccessibilityModifier */);
    if (!modifiers || declaration.kind !***REMOVED******REMOVED*** 177 /* Constructor */) {
      return true;
    }
    const declaringClassDeclaration ***REMOVED*** getClassLikeDeclarationOfSymbol(declaration.parent.symbol);
    const declaringClass ***REMOVED*** getDeclaredTypeOfSymbol(declaration.parent.symbol);
    if (!isNodeWithinClass(node, declaringClassDeclaration)) {
      const containingClass ***REMOVED*** getContainingClass(node);
      if (containingClass && modifiers & 4 /* Protected */) {
        const containingType ***REMOVED*** getTypeOfNode(containingClass);
        if (typeHasProtectedAccessibleBase(declaration.parent.symbol, containingType)) {
          return true;
        }
      }
      if (modifiers & 2 /* Private */) {
        error(node, Diagnostics.Constructor_of_class_0_is_private_and_only_accessible_within_the_class_declaration, typeToString(declaringClass));
      }
      if (modifiers & 4 /* Protected */) {
        error(node, Diagnostics.Constructor_of_class_0_is_protected_and_only_accessible_within_the_class_declaration, typeToString(declaringClass));
      }
      return false;
    }
    return true;
  }
  function invocationErrorDetails(errorTarget, apparentType, kind) {
    let errorInfo;
    const isCall ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** 0 /* Call */;
    const awaitedType ***REMOVED*** getAwaitedType(apparentType);
    const maybeMissingAwait ***REMOVED*** awaitedType && getSignaturesOfType(awaitedType, kind).length > 0;
    if (apparentType.flags & 1048576 /* Union */) {
      const types ***REMOVED*** apparentType.types;
      let hasSignatures2 ***REMOVED*** false;
      for (const constituent of types) {
        const signatures ***REMOVED*** getSignaturesOfType(constituent, kind);
        if (signatures.length !***REMOVED******REMOVED*** 0) {
          hasSignatures2 ***REMOVED*** true;
          if (errorInfo) {
            break;
          }
        } else {
          if (!errorInfo) {
            errorInfo ***REMOVED*** chainDiagnosticMessages(
              errorInfo,
              isCall ? Diagnostics.Type_0_has_no_call_signatures : Diagnostics.Type_0_has_no_construct_signatures,
              typeToString(constituent)
            );
            errorInfo ***REMOVED*** chainDiagnosticMessages(
              errorInfo,
              isCall ? Diagnostics.Not_all_constituents_of_type_0_are_callable : Diagnostics.Not_all_constituents_of_type_0_are_constructable,
              typeToString(apparentType)
            );
          }
          if (hasSignatures2) {
            break;
          }
        }
      }
      if (!hasSignatures2) {
        errorInfo ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          isCall ? Diagnostics.No_constituent_of_type_0_is_callable : Diagnostics.No_constituent_of_type_0_is_constructable,
          typeToString(apparentType)
        );
      }
      if (!errorInfo) {
        errorInfo ***REMOVED*** chainDiagnosticMessages(
          errorInfo,
          isCall ? Diagnostics.Each_member_of_the_union_type_0_has_signatures_but_none_of_those_signatures_are_compatible_with_each_other : Diagnostics.Each_member_of_the_union_type_0_has_construct_signatures_but_none_of_those_signatures_are_compatible_with_each_other,
          typeToString(apparentType)
        );
      }
    } else {
      errorInfo ***REMOVED*** chainDiagnosticMessages(
        errorInfo,
        isCall ? Diagnostics.Type_0_has_no_call_signatures : Diagnostics.Type_0_has_no_construct_signatures,
        typeToString(apparentType)
      );
    }
    let headMessage ***REMOVED*** isCall ? Diagnostics.This_expression_is_not_callable : Diagnostics.This_expression_is_not_constructable;
    if (isCallExpression(errorTarget.parent) && errorTarget.parent.arguments.length ***REMOVED******REMOVED******REMOVED*** 0) {
      const { resolvedSymbol } ***REMOVED*** getNodeLinks(errorTarget);
      if (resolvedSymbol && resolvedSymbol.flags & 32768 /* GetAccessor */) {
        headMessage ***REMOVED*** Diagnostics.This_expression_is_not_callable_because_it_is_a_get_accessor_Did_you_mean_to_use_it_without;
      }
    }
    return {
      messageChain: chainDiagnosticMessages(errorInfo, headMessage),
      relatedMessage: maybeMissingAwait ? Diagnostics.Did_you_forget_to_use_await : void 0
    };
  }
  function invocationError(errorTarget, apparentType, kind, relatedInformation) {
    const { messageChain, relatedMessage: relatedInfo } ***REMOVED*** invocationErrorDetails(errorTarget, apparentType, kind);
    const diagnostic ***REMOVED*** createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(errorTarget), errorTarget, messageChain);
    if (relatedInfo) {
      addRelatedInfo(diagnostic, createDiagnosticForNode(errorTarget, relatedInfo));
    }
    if (isCallExpression(errorTarget.parent)) {
      const { start, length: length2 } ***REMOVED*** getDiagnosticSpanForCallNode(errorTarget.parent);
      diagnostic.start ***REMOVED*** start;
      diagnostic.length ***REMOVED*** length2;
    }
    diagnostics.add(diagnostic);
    invocationErrorRecovery(apparentType, kind, relatedInformation ? addRelatedInfo(diagnostic, relatedInformation) : diagnostic);
  }
  function invocationErrorRecovery(apparentType, kind, diagnostic) {
    if (!apparentType.symbol) {
      return;
    }
    const importNode ***REMOVED*** getSymbolLinks(apparentType.symbol).originatingImport;
    if (importNode && !isImportCall(importNode)) {
      const sigs ***REMOVED*** getSignaturesOfType(getTypeOfSymbol(getSymbolLinks(apparentType.symbol).target), kind);
      if (!sigs || !sigs.length) return;
      addRelatedInfo(diagnostic, createDiagnosticForNode(importNode, Diagnostics.Type_originates_at_this_import_A_namespace_style_import_cannot_be_called_or_constructed_and_will_cause_a_failure_at_runtime_Consider_using_a_default_import_or_import_require_here_instead));
    }
  }
  function resolveTaggedTemplateExpression(node, candidatesOutArray, checkMode) {
    const tagType ***REMOVED*** checkExpression(node.tag);
    const apparentType ***REMOVED*** getApparentType(tagType);
    if (isErrorType(apparentType)) {
      return resolveErrorCall(node);
    }
    const callSignatures ***REMOVED*** getSignaturesOfType(apparentType, 0 /* Call */);
    const numConstructSignatures ***REMOVED*** getSignaturesOfType(apparentType, 1 /* Construct */).length;
    if (isUntypedFunctionCall(tagType, apparentType, callSignatures.length, numConstructSignatures)) {
      return resolveUntypedCall(node);
    }
    if (!callSignatures.length) {
      if (isArrayLiteralExpression(node.parent)) {
        const diagnostic ***REMOVED*** createDiagnosticForNode(node.tag, Diagnostics.It_is_likely_that_you_are_missing_a_comma_to_separate_these_two_template_expressions_They_form_a_tagged_template_expression_which_cannot_be_invoked);
        diagnostics.add(diagnostic);
        return resolveErrorCall(node);
      }
      invocationError(node.tag, apparentType, 0 /* Call */);
      return resolveErrorCall(node);
    }
    return resolveCall(node, callSignatures, candidatesOutArray, checkMode, 0 /* None */);
  }
  function getDiagnosticHeadMessageForDecoratorResolution(node) {
    switch (node.parent.kind) {
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        return Diagnostics.Unable_to_resolve_signature_of_class_decorator_when_called_as_an_expression;
      case 170 /* Parameter */:
        return Diagnostics.Unable_to_resolve_signature_of_parameter_decorator_when_called_as_an_expression;
      case 173 /* PropertyDeclaration */:
        return Diagnostics.Unable_to_resolve_signature_of_property_decorator_when_called_as_an_expression;
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return Diagnostics.Unable_to_resolve_signature_of_method_decorator_when_called_as_an_expression;
      default:
        return Debug.fail();
    }
  }
  function resolveDecorator(node, candidatesOutArray, checkMode) {
    const funcType ***REMOVED*** checkExpression(node.expression);
    const apparentType ***REMOVED*** getApparentType(funcType);
    if (isErrorType(apparentType)) {
      return resolveErrorCall(node);
    }
    const callSignatures ***REMOVED*** getSignaturesOfType(apparentType, 0 /* Call */);
    const numConstructSignatures ***REMOVED*** getSignaturesOfType(apparentType, 1 /* Construct */).length;
    if (isUntypedFunctionCall(funcType, apparentType, callSignatures.length, numConstructSignatures)) {
      return resolveUntypedCall(node);
    }
    if (isPotentiallyUncalledDecorator(node, callSignatures) && !isParenthesizedExpression(node.expression)) {
      const nodeStr ***REMOVED*** getTextOfNode(
        node.expression,
        /*includeTrivia*/
        false
      );
      error(node, Diagnostics._0_accepts_too_few_arguments_to_be_used_as_a_decorator_here_Did_you_mean_to_call_it_first_and_write_0, nodeStr);
      return resolveErrorCall(node);
    }
    const headMessage ***REMOVED*** getDiagnosticHeadMessageForDecoratorResolution(node);
    if (!callSignatures.length) {
      const errorDetails ***REMOVED*** invocationErrorDetails(node.expression, apparentType, 0 /* Call */);
      const messageChain ***REMOVED*** chainDiagnosticMessages(errorDetails.messageChain, headMessage);
      const diag2 ***REMOVED*** createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(node.expression), node.expression, messageChain);
      if (errorDetails.relatedMessage) {
        addRelatedInfo(diag2, createDiagnosticForNode(node.expression, errorDetails.relatedMessage));
      }
      diagnostics.add(diag2);
      invocationErrorRecovery(apparentType, 0 /* Call */, diag2);
      return resolveErrorCall(node);
    }
    return resolveCall(node, callSignatures, candidatesOutArray, checkMode, 0 /* None */, headMessage);
  }
  function createSignatureForJSXIntrinsic(node, result) {
    const namespace ***REMOVED*** getJsxNamespaceAt(node);
    const exports2 ***REMOVED*** namespace && getExportsOfSymbol(namespace);
    const typeSymbol ***REMOVED*** exports2 && getSymbol(exports2, JsxNames.Element, 788968 /* Type */);
    const returnNode ***REMOVED*** typeSymbol && nodeBuilder.symbolToEntityName(typeSymbol, 788968 /* Type */, node);
    const declaration ***REMOVED*** factory.createFunctionTypeNode(
      /*typeParameters*/
      void 0,
      [factory.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        "props",
        /*questionToken*/
        void 0,
        nodeBuilder.typeToTypeNode(result, node)
      )],
      returnNode ? factory.createTypeReferenceNode(
        returnNode,
        /*typeArguments*/
        void 0
      ) : factory.createKeywordTypeNode(133 /* AnyKeyword */)
    );
    const parameterSymbol ***REMOVED*** createSymbol(1 /* FunctionScopedVariable */, "props");
    parameterSymbol.links.type ***REMOVED*** result;
    return createSignature(
      declaration,
      /*typeParameters*/
      void 0,
      /*thisParameter*/
      void 0,
      [parameterSymbol],
      typeSymbol ? getDeclaredTypeOfSymbol(typeSymbol) : errorType,
      /*resolvedTypePredicate*/
      void 0,
      1,
      0 /* None */
    );
  }
  function getJSXFragmentType(node) {
    const sourceFileLinks ***REMOVED*** getNodeLinks(getSourceFileOfNode(node));
    if (sourceFileLinks.jsxFragmentType !***REMOVED******REMOVED*** void 0) return sourceFileLinks.jsxFragmentType;
    const jsxFragmentFactoryName ***REMOVED*** getJsxNamespace(node);
    const shouldResolveFactoryReference ***REMOVED*** (compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 2 /* React */ || compilerOptions.jsxFragmentFactory !***REMOVED******REMOVED*** void 0) && jsxFragmentFactoryName !***REMOVED******REMOVED*** "null";
    if (!shouldResolveFactoryReference) return sourceFileLinks.jsxFragmentType ***REMOVED*** anyType;
    const shouldModuleRefErr ***REMOVED*** compilerOptions.jsx !***REMOVED******REMOVED*** 1 /* Preserve */ && compilerOptions.jsx !***REMOVED******REMOVED*** 3 /* ReactNative */;
    const jsxFactoryRefErr ***REMOVED*** diagnostics ? Diagnostics.Using_JSX_fragments_requires_fragment_factory_0_to_be_in_scope_but_it_could_not_be_found : void 0;
    const jsxFactorySymbol ***REMOVED*** getJsxNamespaceContainerForImplicitImport(node) ?? resolveName(
      node,
      jsxFragmentFactoryName,
      shouldModuleRefErr ? 111551 /* Value */ : 111551 /* Value */ & ~384 /* Enum */,
      /*nameNotFoundMessage*/
      jsxFactoryRefErr,
      /*isUse*/
      true
    );
    if (jsxFactorySymbol ***REMOVED******REMOVED******REMOVED*** void 0) return sourceFileLinks.jsxFragmentType ***REMOVED*** errorType;
    if (jsxFactorySymbol.escapedName ***REMOVED******REMOVED******REMOVED*** ReactNames.Fragment) return sourceFileLinks.jsxFragmentType ***REMOVED*** getTypeOfSymbol(jsxFactorySymbol);
    const resolvedAlias ***REMOVED*** (jsxFactorySymbol.flags & 2097152 /* Alias */) ***REMOVED******REMOVED******REMOVED*** 0 ? jsxFactorySymbol : resolveAlias(jsxFactorySymbol);
    const reactExports ***REMOVED*** jsxFactorySymbol && getExportsOfSymbol(resolvedAlias);
    const typeSymbol ***REMOVED*** reactExports && getSymbol(reactExports, ReactNames.Fragment, 2 /* BlockScopedVariable */);
    const type ***REMOVED*** typeSymbol && getTypeOfSymbol(typeSymbol);
    return sourceFileLinks.jsxFragmentType ***REMOVED*** type ***REMOVED******REMOVED******REMOVED*** void 0 ? errorType : type;
  }
  function resolveJsxOpeningLikeElement(node, candidatesOutArray, checkMode) {
    const isJsxOpenFragment ***REMOVED*** isJsxOpeningFragment(node);
    let exprTypes;
    if (!isJsxOpenFragment) {
      if (isJsxIntrinsicTagName(node.tagName)) {
        const result ***REMOVED*** getIntrinsicAttributesTypeFromJsxOpeningLikeElement(node);
        const fakeSignature ***REMOVED*** createSignatureForJSXIntrinsic(node, result);
        checkTypeAssignableToAndOptionallyElaborate(checkExpressionWithContextualType(
          node.attributes,
          getEffectiveFirstArgumentForJsxSignature(fakeSignature, node),
          /*inferenceContext*/
          void 0,
          0 /* Normal */
        ), result, node.tagName, node.attributes);
        if (length(node.typeArguments)) {
          forEach(node.typeArguments, checkSourceElement);
          diagnostics.add(createDiagnosticForNodeArray(getSourceFileOfNode(node), node.typeArguments, Diagnostics.Expected_0_type_arguments_but_got_1, 0, length(node.typeArguments)));
        }
        return fakeSignature;
      }
      exprTypes ***REMOVED*** checkExpression(node.tagName);
    } else {
      exprTypes ***REMOVED*** getJSXFragmentType(node);
    }
    const apparentType ***REMOVED*** getApparentType(exprTypes);
    if (isErrorType(apparentType)) {
      return resolveErrorCall(node);
    }
    const signatures ***REMOVED*** getUninstantiatedJsxSignaturesOfType(exprTypes, node);
    if (isUntypedFunctionCall(
      exprTypes,
      apparentType,
      signatures.length,
      /*constructSignatures*/
      0
    )) {
      return resolveUntypedCall(node);
    }
    if (signatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      if (isJsxOpenFragment) {
        error(node, Diagnostics.JSX_element_type_0_does_not_have_any_construct_or_call_signatures, getTextOfNode(node));
      } else {
        error(node.tagName, Diagnostics.JSX_element_type_0_does_not_have_any_construct_or_call_signatures, getTextOfNode(node.tagName));
      }
      return resolveErrorCall(node);
    }
    return resolveCall(node, signatures, candidatesOutArray, checkMode, 0 /* None */);
  }
  function resolveInstanceofExpression(node, candidatesOutArray, checkMode) {
    const rightType ***REMOVED*** checkExpression(node.right);
    if (!isTypeAny(rightType)) {
      const hasInstanceMethodType ***REMOVED*** getSymbolHasInstanceMethodOfObjectType(rightType);
      if (hasInstanceMethodType) {
        const apparentType ***REMOVED*** getApparentType(hasInstanceMethodType);
        if (isErrorType(apparentType)) {
          return resolveErrorCall(node);
        }
        const callSignatures ***REMOVED*** getSignaturesOfType(apparentType, 0 /* Call */);
        const constructSignatures ***REMOVED*** getSignaturesOfType(apparentType, 1 /* Construct */);
        if (isUntypedFunctionCall(hasInstanceMethodType, apparentType, callSignatures.length, constructSignatures.length)) {
          return resolveUntypedCall(node);
        }
        if (callSignatures.length) {
          return resolveCall(node, callSignatures, candidatesOutArray, checkMode, 0 /* None */);
        }
      } else if (!(typeHasCallOrConstructSignatures(rightType) || isTypeSubtypeOf(rightType, globalFunctionType))) {
        error(node.right, Diagnostics.The_right_hand_side_of_an_instanceof_expression_must_be_either_of_type_any_a_class_function_or_other_type_assignable_to_the_Function_interface_type_or_an_object_type_with_a_Symbol_hasInstance_method);
        return resolveErrorCall(node);
      }
    }
    return anySignature;
  }
  function isPotentiallyUncalledDecorator(decorator, signatures) {
    return signatures.length && every(signatures, (signature) ***REMOVED***> signature.minArgumentCount ***REMOVED******REMOVED******REMOVED*** 0 && !signatureHasRestParameter(signature) && signature.parameters.length < getDecoratorArgumentCount(decorator, signature));
  }
  function resolveSignature(node, candidatesOutArray, checkMode) {
    switch (node.kind) {
      case 214 /* CallExpression */:
        return resolveCallExpression(node, candidatesOutArray, checkMode);
      case 215 /* NewExpression */:
        return resolveNewExpression(node, candidatesOutArray, checkMode);
      case 216 /* TaggedTemplateExpression */:
        return resolveTaggedTemplateExpression(node, candidatesOutArray, checkMode);
      case 171 /* Decorator */:
        return resolveDecorator(node, candidatesOutArray, checkMode);
      case 290 /* JsxOpeningFragment */:
      case 287 /* JsxOpeningElement */:
      case 286 /* JsxSelfClosingElement */:
        return resolveJsxOpeningLikeElement(node, candidatesOutArray, checkMode);
      case 227 /* BinaryExpression */:
        return resolveInstanceofExpression(node, candidatesOutArray, checkMode);
    }
    Debug.assertNever(node, "Branch in 'resolveSignature' should be unreachable.");
  }
  function getResolvedSignature(node, candidatesOutArray, checkMode) {
    const links ***REMOVED*** getNodeLinks(node);
    const cached ***REMOVED*** links.resolvedSignature;
    if (cached && cached !***REMOVED******REMOVED*** resolvingSignature && !candidatesOutArray) {
      return cached;
    }
    const saveResolutionStart ***REMOVED*** resolutionStart;
    if (!cached) {
      resolutionStart ***REMOVED*** resolutionTargets.length;
    }
    links.resolvedSignature ***REMOVED*** resolvingSignature;
    const result ***REMOVED*** resolveSignature(node, candidatesOutArray, checkMode || 0 /* Normal */);
    resolutionStart ***REMOVED*** saveResolutionStart;
    if (result !***REMOVED******REMOVED*** resolvingSignature) {
      links.resolvedSignature ***REMOVED*** flowLoopStart ***REMOVED******REMOVED******REMOVED*** flowLoopCount ? result : cached;
    }
    return result;
  }
  function isJSConstructor(node) {
    var _a;
    if (!node || !isInJSFile(node)) {
      return false;
    }
    const func ***REMOVED*** isFunctionDeclaration(node) || isFunctionExpression(node) ? node : (isVariableDeclaration(node) || isPropertyAssignment(node)) && node.initializer && isFunctionExpression(node.initializer) ? node.initializer : void 0;
    if (func) {
      if (getJSDocClassTag(node)) return true;
      if (isPropertyAssignment(walkUpParenthesizedExpressions(func.parent))) return false;
      const symbol ***REMOVED*** getSymbolOfDeclaration(func);
      return !!((_a ***REMOVED*** symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.members) ***REMOVED******REMOVED*** null ? void 0 : _a.size);
    }
    return false;
  }
  function mergeJSSymbols(target, source) {
    var _a, _b;
    if (source) {
      const links ***REMOVED*** getSymbolLinks(source);
      if (!links.inferredClassSymbol || !links.inferredClassSymbol.has(getSymbolId(target))) {
        const inferred ***REMOVED*** isTransientSymbol(target) ? target : cloneSymbol(target);
        inferred.exports ***REMOVED*** inferred.exports || createSymbolTable();
        inferred.members ***REMOVED*** inferred.members || createSymbolTable();
        inferred.flags |***REMOVED*** source.flags & 32 /* Class */;
        if ((_a ***REMOVED*** source.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.size) {
          mergeSymbolTable(inferred.exports, source.exports);
        }
        if ((_b ***REMOVED*** source.members) ***REMOVED******REMOVED*** null ? void 0 : _b.size) {
          mergeSymbolTable(inferred.members, source.members);
        }
        (links.inferredClassSymbol || (links.inferredClassSymbol ***REMOVED*** /* @__PURE__ */ new Map())).set(getSymbolId(inferred), inferred);
        return inferred;
      }
      return links.inferredClassSymbol.get(getSymbolId(target));
    }
  }
  function getAssignedClassSymbol(decl) {
    var _a;
    const assignmentSymbol ***REMOVED*** decl && getSymbolOfExpando(
      decl,
      /*allowDeclaration*/
      true
    );
    const prototype ***REMOVED*** (_a ***REMOVED*** assignmentSymbol ***REMOVED******REMOVED*** null ? void 0 : assignmentSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.get("prototype");
    const init ***REMOVED*** (prototype ***REMOVED******REMOVED*** null ? void 0 : prototype.valueDeclaration) && getAssignedJSPrototype(prototype.valueDeclaration);
    return init ? getSymbolOfDeclaration(init) : void 0;
  }
  function getSymbolOfExpando(node, allowDeclaration) {
    if (!node.parent) {
      return void 0;
    }
    let name;
    let decl;
    if (isVariableDeclaration(node.parent) && node.parent.initializer ***REMOVED******REMOVED******REMOVED*** node) {
      if (!isInJSFile(node) && !(isVarConstLike2(node.parent) && isFunctionLikeDeclaration(node))) {
        return void 0;
      }
      name ***REMOVED*** node.parent.name;
      decl ***REMOVED*** node.parent;
    } else if (isBinaryExpression(node.parent)) {
      const parentNode ***REMOVED*** node.parent;
      const parentNodeOperator ***REMOVED*** node.parent.operatorToken.kind;
      if (parentNodeOperator ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && (allowDeclaration || parentNode.right ***REMOVED******REMOVED******REMOVED*** node)) {
        name ***REMOVED*** parentNode.left;
        decl ***REMOVED*** name;
      } else if (parentNodeOperator ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || parentNodeOperator ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
        if (isVariableDeclaration(parentNode.parent) && parentNode.parent.initializer ***REMOVED******REMOVED******REMOVED*** parentNode) {
          name ***REMOVED*** parentNode.parent.name;
          decl ***REMOVED*** parentNode.parent;
        } else if (isBinaryExpression(parentNode.parent) && parentNode.parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && (allowDeclaration || parentNode.parent.right ***REMOVED******REMOVED******REMOVED*** parentNode)) {
          name ***REMOVED*** parentNode.parent.left;
          decl ***REMOVED*** name;
        }
        if (!name || !isBindableStaticNameExpression(name) || !isSameEntityName(name, parentNode.left)) {
          return void 0;
        }
      }
    } else if (allowDeclaration && isFunctionDeclaration(node)) {
      name ***REMOVED*** node.name;
      decl ***REMOVED*** node;
    }
    if (!decl || !name || !allowDeclaration && !getExpandoInitializer(node, isPrototypeAccess(name))) {
      return void 0;
    }
    return getSymbolOfNode(decl);
  }
  function getAssignedJSPrototype(node) {
    if (!node.parent) {
      return false;
    }
    let parent ***REMOVED*** node.parent;
    while (parent && parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
      parent ***REMOVED*** parent.parent;
    }
    if (parent && isBinaryExpression(parent) && isPrototypeAccess(parent.left) && parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      const right ***REMOVED*** getInitializerOfBinaryExpression(parent);
      return isObjectLiteralExpression(right) && right;
    }
  }
  function checkCallExpression(node, checkMode) {
    var _a, _b, _c;
    checkGrammarTypeArguments(node, node.typeArguments);
    const signature ***REMOVED*** getResolvedSignature(
      node,
      /*candidatesOutArray*/
      void 0,
      checkMode
    );
    if (signature ***REMOVED******REMOVED******REMOVED*** resolvingSignature) {
      return silentNeverType;
    }
    checkDeprecatedSignature(signature, node);
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return voidType;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 215 /* NewExpression */) {
      const declaration ***REMOVED*** signature.declaration;
      if (declaration && declaration.kind !***REMOVED******REMOVED*** 177 /* Constructor */ && declaration.kind !***REMOVED******REMOVED*** 181 /* ConstructSignature */ && declaration.kind !***REMOVED******REMOVED*** 186 /* ConstructorType */ && !(isJSDocSignature(declaration) && ((_b ***REMOVED*** (_a ***REMOVED*** getJSDocRoot(declaration)) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.kind) ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) && !isJSDocConstructSignature(declaration) && !isJSConstructor(declaration)) {
        if (noImplicitAny) {
          error(node, Diagnostics.new_expression_whose_target_lacks_a_construct_signature_implicitly_has_an_any_type);
        }
        return anyType;
      }
    }
    if (isInJSFile(node) && isCommonJsRequire(node)) {
      return resolveExternalModuleTypeByLiteral(node.arguments[0]);
    }
    const returnType ***REMOVED*** getReturnTypeOfSignature(signature);
    if (returnType.flags & 12288 /* ESSymbolLike */ && isSymbolOrSymbolForCall(node)) {
      return getESSymbolLikeTypeForNode(walkUpParenthesizedExpressions(node.parent));
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && !node.questionDotToken && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 245 /* ExpressionStatement */ && returnType.flags & 16384 /* Void */ && getTypePredicateOfSignature(signature)) {
      if (!isDottedName(node.expression)) {
        error(node.expression, Diagnostics.Assertions_require_the_call_target_to_be_an_identifier_or_qualified_name);
      } else if (!getEffectsSignature(node)) {
        const diagnostic ***REMOVED*** error(node.expression, Diagnostics.Assertions_require_every_name_in_the_call_target_to_be_declared_with_an_explicit_type_annotation);
        getTypeOfDottedName(node.expression, diagnostic);
      }
    }
    if (isInJSFile(node)) {
      const jsSymbol ***REMOVED*** getSymbolOfExpando(
        node,
        /*allowDeclaration*/
        false
      );
      if ((_c ***REMOVED*** jsSymbol ***REMOVED******REMOVED*** null ? void 0 : jsSymbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _c.size) {
        const jsAssignmentType ***REMOVED*** createAnonymousType(jsSymbol, jsSymbol.exports, emptyArray, emptyArray, emptyArray);
        jsAssignmentType.objectFlags |***REMOVED*** 4096 /* JSLiteral */;
        return getIntersectionType([returnType, jsAssignmentType]);
      }
    }
    return returnType;
  }
  function checkDeprecatedSignature(signature, node) {
    if (signature.flags & 128 /* IsSignatureCandidateForOverloadFailure */) return;
    if (signature.declaration && signature.declaration.flags & 536870912 /* Deprecated */) {
      const suggestionNode ***REMOVED*** getDeprecatedSuggestionNode(node);
      const name ***REMOVED*** tryGetPropertyAccessOrIdentifierToString(getInvokedExpression(node));
      addDeprecatedSuggestionWithSignature(suggestionNode, signature.declaration, name, signatureToString(signature));
    }
  }
  function getDeprecatedSuggestionNode(node) {
    node ***REMOVED*** skipParentheses(node);
    switch (node.kind) {
      case 214 /* CallExpression */:
      case 171 /* Decorator */:
      case 215 /* NewExpression */:
        return getDeprecatedSuggestionNode(node.expression);
      case 216 /* TaggedTemplateExpression */:
        return getDeprecatedSuggestionNode(node.tag);
      case 287 /* JsxOpeningElement */:
      case 286 /* JsxSelfClosingElement */:
        return getDeprecatedSuggestionNode(node.tagName);
      case 213 /* ElementAccessExpression */:
        return node.argumentExpression;
      case 212 /* PropertyAccessExpression */:
        return node.name;
      case 184 /* TypeReference */:
        const typeReference ***REMOVED*** node;
        return isQualifiedName(typeReference.typeName) ? typeReference.typeName.right : typeReference;
      default:
        return node;
    }
  }
  function isSymbolOrSymbolForCall(node) {
    if (!isCallExpression(node)) return false;
    let left ***REMOVED*** node.expression;
    if (isPropertyAccessExpression(left) && left.name.escapedText ***REMOVED******REMOVED******REMOVED*** "for") {
      left ***REMOVED*** left.expression;
    }
    if (!isIdentifier(left) || left.escapedText !***REMOVED******REMOVED*** "Symbol") {
      return false;
    }
    const globalESSymbol ***REMOVED*** getGlobalESSymbolConstructorSymbol(
      /*reportErrors*/
      false
    );
    if (!globalESSymbol) {
      return false;
    }
    return globalESSymbol ***REMOVED******REMOVED******REMOVED*** resolveName(
      left,
      "Symbol",
      111551 /* Value */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      false
    );
  }
  function checkImportCallExpression(node) {
    checkGrammarImportCallExpression(node);
    if (node.arguments.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return createPromiseReturnType(node, anyType);
    }
    const specifier ***REMOVED*** node.arguments[0];
    const specifierType ***REMOVED*** checkExpressionCached(specifier);
    const optionsType ***REMOVED*** node.arguments.length > 1 ? checkExpressionCached(node.arguments[1]) : void 0;
    for (let i ***REMOVED*** 2; i < node.arguments.length; ++i) {
      checkExpressionCached(node.arguments[i]);
    }
    if (specifierType.flags & 32768 /* Undefined */ || specifierType.flags & 65536 /* Null */ || !isTypeAssignableTo(specifierType, stringType)) {
      error(specifier, Diagnostics.Dynamic_import_s_specifier_must_be_of_type_string_but_here_has_type_0, typeToString(specifierType));
    }
    if (optionsType) {
      const importCallOptionsType ***REMOVED*** getGlobalImportCallOptionsType(
        /*reportErrors*/
        true
      );
      if (importCallOptionsType !***REMOVED******REMOVED*** emptyObjectType) {
        checkTypeAssignableTo(optionsType, getNullableType(importCallOptionsType, 32768 /* Undefined */), node.arguments[1]);
      }
    }
    const moduleSymbol ***REMOVED*** resolveExternalModuleName(node, specifier);
    if (moduleSymbol) {
      const esModuleSymbol ***REMOVED*** resolveESModuleSymbol(
        moduleSymbol,
        specifier,
        /*dontResolveAlias*/
        true,
        /*suppressInteropError*/
        false
      );
      if (esModuleSymbol) {
        return createPromiseReturnType(
          node,
          getTypeWithSyntheticDefaultOnly(getTypeOfSymbol(esModuleSymbol), esModuleSymbol, moduleSymbol, specifier) || getTypeWithSyntheticDefaultImportType(getTypeOfSymbol(esModuleSymbol), esModuleSymbol, moduleSymbol, specifier)
        );
      }
    }
    return createPromiseReturnType(node, anyType);
  }
  function createDefaultPropertyWrapperForModule(symbol, originalSymbol, anonymousSymbol) {
    const memberTable ***REMOVED*** createSymbolTable();
    const newSymbol ***REMOVED*** createSymbol(2097152 /* Alias */, "default" /* Default */);
    newSymbol.parent ***REMOVED*** originalSymbol;
    newSymbol.links.nameType ***REMOVED*** getStringLiteralType("default");
    newSymbol.links.aliasTarget ***REMOVED*** resolveSymbol(symbol);
    memberTable.set("default" /* Default */, newSymbol);
    return createAnonymousType(anonymousSymbol, memberTable, emptyArray, emptyArray, emptyArray);
  }
  function getTypeWithSyntheticDefaultOnly(type, symbol, originalSymbol, moduleSpecifier) {
    const hasDefaultOnly ***REMOVED*** isOnlyImportableAsDefault(moduleSpecifier);
    if (hasDefaultOnly && type && !isErrorType(type)) {
      const synthType ***REMOVED*** type;
      if (!synthType.defaultOnlyType) {
        const type2 ***REMOVED*** createDefaultPropertyWrapperForModule(symbol, originalSymbol);
        synthType.defaultOnlyType ***REMOVED*** type2;
      }
      return synthType.defaultOnlyType;
    }
    return void 0;
  }
  function getTypeWithSyntheticDefaultImportType(type, symbol, originalSymbol, moduleSpecifier) {
    var _a;
    if (allowSyntheticDefaultImports && type && !isErrorType(type)) {
      const synthType ***REMOVED*** type;
      if (!synthType.syntheticType) {
        const file ***REMOVED*** (_a ***REMOVED*** originalSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(isSourceFile);
        const hasSyntheticDefault ***REMOVED*** canHaveSyntheticDefault(
          file,
          originalSymbol,
          /*dontResolveAlias*/
          false,
          moduleSpecifier
        );
        if (hasSyntheticDefault) {
          const anonymousSymbol ***REMOVED*** createSymbol(2048 /* TypeLiteral */, "__type" /* Type */);
          const defaultContainingObject ***REMOVED*** createDefaultPropertyWrapperForModule(symbol, originalSymbol, anonymousSymbol);
          anonymousSymbol.links.type ***REMOVED*** defaultContainingObject;
          synthType.syntheticType ***REMOVED*** isValidSpreadType(type) ? getSpreadType(
            type,
            defaultContainingObject,
            anonymousSymbol,
            /*objectFlags*/
            0,
            /*readonly*/
            false
          ) : defaultContainingObject;
        } else {
          synthType.syntheticType ***REMOVED*** type;
        }
      }
      return synthType.syntheticType;
    }
    return type;
  }
  function isCommonJsRequire(node) {
    if (!isRequireCall(
      node,
      /*requireStringLiteralLikeArgument*/
      true
    )) {
      return false;
    }
    if (!isIdentifier(node.expression)) return Debug.fail();
    const resolvedRequire ***REMOVED*** resolveName(
      node.expression,
      node.expression.escapedText,
      111551 /* Value */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true
    );
    if (resolvedRequire ***REMOVED******REMOVED******REMOVED*** requireSymbol) {
      return true;
    }
    if (resolvedRequire.flags & 2097152 /* Alias */) {
      return false;
    }
    const targetDeclarationKind ***REMOVED*** resolvedRequire.flags & 16 /* Function */ ? 263 /* FunctionDeclaration */ : resolvedRequire.flags & 3 /* Variable */ ? 261 /* VariableDeclaration */ : 0 /* Unknown */;
    if (targetDeclarationKind !***REMOVED******REMOVED*** 0 /* Unknown */) {
      const decl ***REMOVED*** getDeclarationOfKind(resolvedRequire, targetDeclarationKind);
      return !!decl && !!(decl.flags & 33554432 /* Ambient */);
    }
    return false;
  }
  function checkTaggedTemplateExpression(node) {
    if (!checkGrammarTaggedTemplateChain(node)) checkGrammarTypeArguments(node, node.typeArguments);
    if (languageVersion < LanguageFeatureMinimumTarget.TaggedTemplates) {
      checkExternalEmitHelpers(node, 262144 /* MakeTemplateObject */);
    }
    const signature ***REMOVED*** getResolvedSignature(node);
    checkDeprecatedSignature(signature, node);
    return getReturnTypeOfSignature(signature);
  }
  function checkAssertion(node, checkMode) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */) {
      const file ***REMOVED*** getSourceFileOfNode(node);
      if (file && fileExtensionIsOneOf(file.fileName, [".cts" /* Cts */, ".mts" /* Mts */])) {
        grammarErrorOnNode(node, Diagnostics.This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Use_an_as_expression_instead);
      }
      if (compilerOptions.erasableSyntaxOnly) {
        const start ***REMOVED*** skipTrivia(file.text, node.pos);
        const end ***REMOVED*** node.expression.pos;
        diagnostics.add(createFileDiagnostic(file, start, end - start, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled));
      }
    }
    return checkAssertionWorker(node, checkMode);
  }
  function isValidConstAssertionArgument(node) {
    switch (node.kind) {
      case 11 /* StringLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 210 /* ArrayLiteralExpression */:
      case 211 /* ObjectLiteralExpression */:
      case 229 /* TemplateExpression */:
        return true;
      case 218 /* ParenthesizedExpression */:
        return isValidConstAssertionArgument(node.expression);
      case 225 /* PrefixUnaryExpression */:
        const op ***REMOVED*** node.operator;
        const arg ***REMOVED*** node.operand;
        return op ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ && (arg.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || arg.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */) || op ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ && arg.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */;
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        const expr ***REMOVED*** skipParentheses(node.expression);
        const symbol ***REMOVED*** isEntityNameExpression(expr) ? resolveEntityName(
          expr,
          111551 /* Value */,
          /*ignoreErrors*/
          true
        ) : void 0;
        return !!(symbol && symbol.flags & 384 /* Enum */);
    }
    return false;
  }
  function checkAssertionWorker(node, checkMode) {
    const { type, expression } ***REMOVED*** getAssertionTypeAndExpression(node);
    const exprType ***REMOVED*** checkExpression(expression, checkMode);
    if (isConstTypeReference(type)) {
      if (!isValidConstAssertionArgument(expression)) {
        error(expression, Diagnostics.A_const_assertions_can_only_be_applied_to_references_to_enum_members_or_string_number_boolean_array_or_object_literals);
      }
      return getRegularTypeOfLiteralType(exprType);
    }
    const links ***REMOVED*** getNodeLinks(node);
    links.assertionExpressionType ***REMOVED*** exprType;
    checkSourceElement(type);
    checkNodeDeferred(node);
    return getTypeFromTypeNode(type);
  }
  function getAssertionTypeAndExpression(node) {
    let type;
    let expression;
    switch (node.kind) {
      case 235 /* AsExpression */:
      case 217 /* TypeAssertionExpression */:
        type ***REMOVED*** node.type;
        expression ***REMOVED*** node.expression;
        break;
      case 218 /* ParenthesizedExpression */:
        type ***REMOVED*** getJSDocTypeAssertionType(node);
        expression ***REMOVED*** node.expression;
        break;
    }
    return { type, expression };
  }
  function checkAssertionDeferred(node) {
    const { type } ***REMOVED*** getAssertionTypeAndExpression(node);
    const errNode ***REMOVED*** isParenthesizedExpression(node) ? type : node;
    const links ***REMOVED*** getNodeLinks(node);
    Debug.assertIsDefined(links.assertionExpressionType);
    const exprType ***REMOVED*** getRegularTypeOfObjectLiteral(getBaseTypeOfLiteralType(links.assertionExpressionType));
    const targetType ***REMOVED*** getTypeFromTypeNode(type);
    if (!isErrorType(targetType)) {
      addLazyDiagnostic(() ***REMOVED***> {
        const widenedType ***REMOVED*** getWidenedType(exprType);
        if (!isTypeComparableTo(targetType, widenedType)) {
          checkTypeComparableTo(exprType, targetType, errNode, Diagnostics.Conversion_of_type_0_to_type_1_may_be_a_mistake_because_neither_type_sufficiently_overlaps_with_the_other_If_this_was_intentional_convert_the_expression_to_unknown_first);
        }
      });
    }
  }
  function checkNonNullChain(node) {
    const leftType ***REMOVED*** checkExpression(node.expression);
    const nonOptionalType ***REMOVED*** getOptionalExpressionType(leftType, node.expression);
    return propagateOptionalTypeMarker(getNonNullableType(nonOptionalType), node, nonOptionalType !***REMOVED******REMOVED*** leftType);
  }
  function checkNonNullAssertion(node) {
    return node.flags & 64 /* OptionalChain */ ? checkNonNullChain(node) : getNonNullableType(checkExpression(node.expression));
  }
  function checkExpressionWithTypeArguments(node) {
    checkGrammarExpressionWithTypeArguments(node);
    forEach(node.typeArguments, checkSourceElement);
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */) {
      const parent ***REMOVED*** walkUpParenthesizedExpressions(node.parent);
      if (parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && parent.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 104 /* InstanceOfKeyword */ && isNodeDescendantOf(node, parent.right)) {
        error(node, Diagnostics.The_right_hand_side_of_an_instanceof_expression_must_not_be_an_instantiation_expression);
      }
    }
    const exprType ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */ ? checkExpression(node.expression) : isThisIdentifier(node.exprName) ? checkThisExpression(node.exprName) : checkExpression(node.exprName);
    return getInstantiationExpressionType(exprType, node);
  }
  function getInstantiationExpressionType(exprType, node) {
    const typeArguments ***REMOVED*** node.typeArguments;
    if (exprType ***REMOVED******REMOVED******REMOVED*** silentNeverType || isErrorType(exprType) || !some(typeArguments)) {
      return exprType;
    }
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.instantiationExpressionTypes) {
      links.instantiationExpressionTypes ***REMOVED*** /* @__PURE__ */ new Map();
    }
    if (links.instantiationExpressionTypes.has(exprType.id)) {
      return links.instantiationExpressionTypes.get(exprType.id);
    }
    let hasSomeApplicableSignature ***REMOVED*** false;
    let nonApplicableType;
    const result ***REMOVED*** getInstantiatedType(exprType);
    links.instantiationExpressionTypes.set(exprType.id, result);
    const errorType2 ***REMOVED*** hasSomeApplicableSignature ? nonApplicableType : exprType;
    if (errorType2) {
      diagnostics.add(createDiagnosticForNodeArray(getSourceFileOfNode(node), typeArguments, Diagnostics.Type_0_has_no_signatures_for_which_the_type_argument_list_is_applicable, typeToString(errorType2)));
    }
    return result;
    function getInstantiatedType(type) {
      let hasSignatures2 ***REMOVED*** false;
      let hasApplicableSignature ***REMOVED*** false;
      const result2 ***REMOVED*** getInstantiatedTypePart(type);
      hasSomeApplicableSignature || (hasSomeApplicableSignature ***REMOVED*** hasApplicableSignature);
      if (hasSignatures2 && !hasApplicableSignature) {
        nonApplicableType ?? (nonApplicableType ***REMOVED*** type);
      }
      return result2;
      function getInstantiatedTypePart(type2) {
        if (type2.flags & 524288 /* Object */) {
          const resolved ***REMOVED*** resolveStructuredTypeMembers(type2);
          const callSignatures ***REMOVED*** getInstantiatedSignatures(resolved.callSignatures);
          const constructSignatures ***REMOVED*** getInstantiatedSignatures(resolved.constructSignatures);
          hasSignatures2 || (hasSignatures2 ***REMOVED*** resolved.callSignatures.length !***REMOVED******REMOVED*** 0 || resolved.constructSignatures.length !***REMOVED******REMOVED*** 0);
          hasApplicableSignature || (hasApplicableSignature ***REMOVED*** callSignatures.length !***REMOVED******REMOVED*** 0 || constructSignatures.length !***REMOVED******REMOVED*** 0);
          if (callSignatures !***REMOVED******REMOVED*** resolved.callSignatures || constructSignatures !***REMOVED******REMOVED*** resolved.constructSignatures) {
            const result3 ***REMOVED*** createAnonymousType(createSymbol(0 /* None */, "__instantiationExpression" /* InstantiationExpression */), resolved.members, callSignatures, constructSignatures, resolved.indexInfos);
            result3.objectFlags |***REMOVED*** 8388608 /* InstantiationExpressionType */;
            result3.node ***REMOVED*** node;
            return result3;
          }
        } else if (type2.flags & 58982400 /* InstantiableNonPrimitive */) {
          const constraint ***REMOVED*** getBaseConstraintOfType(type2);
          if (constraint) {
            const instantiated ***REMOVED*** getInstantiatedTypePart(constraint);
            if (instantiated !***REMOVED******REMOVED*** constraint) {
              return instantiated;
            }
          }
        } else if (type2.flags & 1048576 /* Union */) {
          return mapType(type2, getInstantiatedType);
        } else if (type2.flags & 2097152 /* Intersection */) {
          return getIntersectionType(sameMap(type2.types, getInstantiatedTypePart));
        }
        return type2;
      }
    }
    function getInstantiatedSignatures(signatures) {
      const applicableSignatures ***REMOVED*** filter(signatures, (sig) ***REMOVED***> !!sig.typeParameters && hasCorrectTypeArgumentArity(sig, typeArguments));
      return sameMap(applicableSignatures, (sig) ***REMOVED***> {
        const typeArgumentTypes ***REMOVED*** checkTypeArguments(
          sig,
          typeArguments,
          /*reportErrors*/
          true
        );
        return typeArgumentTypes ? getSignatureInstantiation(sig, typeArgumentTypes, isInJSFile(sig.declaration)) : sig;
      });
    }
  }
  function checkSatisfiesExpression(node) {
    checkSourceElement(node.type);
    return checkSatisfiesExpressionWorker(node.expression, node.type);
  }
  function checkSatisfiesExpressionWorker(expression, target, checkMode) {
    const exprType ***REMOVED*** checkExpression(expression, checkMode);
    const targetType ***REMOVED*** getTypeFromTypeNode(target);
    if (isErrorType(targetType)) {
      return targetType;
    }
    const errorNode ***REMOVED*** findAncestor(target.parent, (n) ***REMOVED***> n.kind ***REMOVED******REMOVED******REMOVED*** 239 /* SatisfiesExpression */ || n.kind ***REMOVED******REMOVED******REMOVED*** 351 /* JSDocSatisfiesTag */);
    checkTypeAssignableToAndOptionallyElaborate(exprType, targetType, errorNode, expression, Diagnostics.Type_0_does_not_satisfy_the_expected_type_1);
    return exprType;
  }
  function checkMetaProperty(node) {
    checkGrammarMetaProperty(node);
    if (node.keywordToken ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */) {
      return checkNewTargetMetaProperty(node);
    }
    if (node.keywordToken ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */) {
      if (node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "defer") {
        Debug.assert(!isCallExpression(node.parent) || node.parent.expression !***REMOVED******REMOVED*** node, "Trying to get the type of `import.defer` in `import.defer(...)`");
        return errorType;
      }
      return checkImportMetaProperty(node);
    }
    return Debug.assertNever(node.keywordToken);
  }
  function checkMetaPropertyKeyword(node) {
    switch (node.keywordToken) {
      case 102 /* ImportKeyword */:
        return getGlobalImportMetaExpressionType();
      case 105 /* NewKeyword */:
        const type ***REMOVED*** checkNewTargetMetaProperty(node);
        return isErrorType(type) ? errorType : createNewTargetExpressionType(type);
      default:
        Debug.assertNever(node.keywordToken);
    }
  }
  function checkNewTargetMetaProperty(node) {
    const container ***REMOVED*** getNewTargetContainer(node);
    if (!container) {
      error(node, Diagnostics.Meta_property_0_is_only_allowed_in_the_body_of_a_function_declaration_function_expression_or_constructor, "new.target");
      return errorType;
    } else if (container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(container.parent);
      return getTypeOfSymbol(symbol);
    } else {
      const symbol ***REMOVED*** getSymbolOfDeclaration(container);
      return getTypeOfSymbol(symbol);
    }
  }
  function checkImportMetaProperty(node) {
    if (100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) {
      if (getSourceFileOfNode(node).impliedNodeFormat !***REMOVED******REMOVED*** 99 /* ESNext */) {
        error(node, Diagnostics.The_import_meta_meta_property_is_not_allowed_in_files_which_will_build_into_CommonJS_output);
      }
    } else if (moduleKind < 6 /* ES2020 */ && moduleKind !***REMOVED******REMOVED*** 4 /* System */) {
      error(node, Diagnostics.The_import_meta_meta_property_is_only_allowed_when_the_module_option_is_es2020_es2022_esnext_system_node16_node18_node20_or_nodenext);
    }
    const file ***REMOVED*** getSourceFileOfNode(node);
    Debug.assert(!!(file.flags & 8388608 /* PossiblyContainsImportMeta */), "Containing file is missing import meta node flag.");
    return node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "meta" ? getGlobalImportMetaType() : errorType;
  }
  function getTypeOfParameter(symbol) {
    const declaration ***REMOVED*** symbol.valueDeclaration;
    return addOptionality(
      getTypeOfSymbol(symbol),
      /*isProperty*/
      false,
      /*isOptional*/
      !!declaration && (hasInitializer(declaration) || isOptionalDeclaration(declaration))
    );
  }
  function getTupleElementLabelFromBindingElement(node, index, elementFlags) {
    switch (node.name.kind) {
      case 80 /* Identifier */: {
        const name ***REMOVED*** node.name.escapedText;
        if (node.dotDotDotToken) {
          return elementFlags & 12 /* Variable */ ? name : `${name}_${index}`;
        } else {
          return elementFlags & 3 /* Fixed */ ? name : `${name}_n`;
        }
      }
      case 208 /* ArrayBindingPattern */: {
        if (node.dotDotDotToken) {
          const elements ***REMOVED*** node.name.elements;
          const lastElement ***REMOVED*** tryCast(lastOrUndefined(elements), isBindingElement);
          const elementCount ***REMOVED*** elements.length - ((lastElement ***REMOVED******REMOVED*** null ? void 0 : lastElement.dotDotDotToken) ? 1 : 0);
          if (index < elementCount) {
            const element ***REMOVED*** elements[index];
            if (isBindingElement(element)) {
              return getTupleElementLabelFromBindingElement(element, index, elementFlags);
            }
          } else if (lastElement ***REMOVED******REMOVED*** null ? void 0 : lastElement.dotDotDotToken) {
            return getTupleElementLabelFromBindingElement(lastElement, index - elementCount, elementFlags);
          }
        }
        break;
      }
    }
    return `arg_${index}`;
  }
  function getTupleElementLabel(d, index ***REMOVED*** 0, elementFlags ***REMOVED*** 3 /* Fixed */, restSymbol) {
    if (!d) {
      const restParameter ***REMOVED*** tryCast(restSymbol ***REMOVED******REMOVED*** null ? void 0 : restSymbol.valueDeclaration, isParameter);
      return restParameter ? getTupleElementLabelFromBindingElement(restParameter, index, elementFlags) : `${(restSymbol ***REMOVED******REMOVED*** null ? void 0 : restSymbol.escapedName) ?? "arg"}_${index}`;
    }
    Debug.assert(isIdentifier(d.name));
    return d.name.escapedText;
  }
  function getParameterNameAtPosition(signature, pos, overrideRestType) {
    var _a;
    const paramCount ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    if (pos < paramCount) {
      return signature.parameters[pos].escapedName;
    }
    const restParameter ***REMOVED*** signature.parameters[paramCount] || unknownSymbol;
    const restType ***REMOVED*** overrideRestType || getTypeOfSymbol(restParameter);
    if (isTupleType(restType)) {
      const tupleType ***REMOVED*** restType.target;
      const index ***REMOVED*** pos - paramCount;
      const associatedName ***REMOVED*** (_a ***REMOVED*** tupleType.labeledElementDeclarations) ***REMOVED******REMOVED*** null ? void 0 : _a[index];
      const elementFlags ***REMOVED*** tupleType.elementFlags[index];
      return getTupleElementLabel(associatedName, index, elementFlags, restParameter);
    }
    return restParameter.escapedName;
  }
  function getParameterIdentifierInfoAtPosition(signature, pos) {
    var _a;
    if (((_a ***REMOVED*** signature.declaration) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */) {
      return void 0;
    }
    const paramCount ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    if (pos < paramCount) {
      const param ***REMOVED*** signature.parameters[pos];
      const paramIdent ***REMOVED*** getParameterDeclarationIdentifier(param);
      return paramIdent ? {
        parameter: paramIdent,
        parameterName: param.escapedName,
        isRestParameter: false
      } : void 0;
    }
    const restParameter ***REMOVED*** signature.parameters[paramCount] || unknownSymbol;
    const restIdent ***REMOVED*** getParameterDeclarationIdentifier(restParameter);
    if (!restIdent) {
      return void 0;
    }
    const restType ***REMOVED*** getTypeOfSymbol(restParameter);
    if (isTupleType(restType)) {
      const associatedNames ***REMOVED*** restType.target.labeledElementDeclarations;
      const index ***REMOVED*** pos - paramCount;
      const associatedName ***REMOVED*** associatedNames ***REMOVED******REMOVED*** null ? void 0 : associatedNames[index];
      const isRestTupleElement ***REMOVED*** !!(associatedName ***REMOVED******REMOVED*** null ? void 0 : associatedName.dotDotDotToken);
      if (associatedName) {
        Debug.assert(isIdentifier(associatedName.name));
        return { parameter: associatedName.name, parameterName: associatedName.name.escapedText, isRestParameter: isRestTupleElement };
      }
      return void 0;
    }
    if (pos ***REMOVED******REMOVED******REMOVED*** paramCount) {
      return { parameter: restIdent, parameterName: restParameter.escapedName, isRestParameter: true };
    }
    return void 0;
  }
  function getParameterDeclarationIdentifier(symbol) {
    return symbol.valueDeclaration && isParameter(symbol.valueDeclaration) && isIdentifier(symbol.valueDeclaration.name) && symbol.valueDeclaration.name;
  }
  function isValidDeclarationForTupleLabel(d) {
    return d.kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */ || isParameter(d) && d.name && isIdentifier(d.name);
  }
  function getNameableDeclarationAtPosition(signature, pos) {
    const paramCount ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    if (pos < paramCount) {
      const decl ***REMOVED*** signature.parameters[pos].valueDeclaration;
      return decl && isValidDeclarationForTupleLabel(decl) ? decl : void 0;
    }
    const restParameter ***REMOVED*** signature.parameters[paramCount] || unknownSymbol;
    const restType ***REMOVED*** getTypeOfSymbol(restParameter);
    if (isTupleType(restType)) {
      const associatedNames ***REMOVED*** restType.target.labeledElementDeclarations;
      const index ***REMOVED*** pos - paramCount;
      return associatedNames && associatedNames[index];
    }
    return restParameter.valueDeclaration && isValidDeclarationForTupleLabel(restParameter.valueDeclaration) ? restParameter.valueDeclaration : void 0;
  }
  function getTypeAtPosition(signature, pos) {
    return tryGetTypeAtPosition(signature, pos) || anyType;
  }
  function tryGetTypeAtPosition(signature, pos) {
    const paramCount ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    if (pos < paramCount) {
      return getTypeOfParameter(signature.parameters[pos]);
    }
    if (signatureHasRestParameter(signature)) {
      const restType ***REMOVED*** getTypeOfSymbol(signature.parameters[paramCount]);
      const index ***REMOVED*** pos - paramCount;
      if (!isTupleType(restType) || restType.target.combinedFlags & 12 /* Variable */ || index < restType.target.fixedLength) {
        return getIndexedAccessType(restType, getNumberLiteralType(index));
      }
    }
    return void 0;
  }
  function getRestTypeAtPosition(source, pos, readonly) {
    const parameterCount ***REMOVED*** getParameterCount(source);
    const minArgumentCount ***REMOVED*** getMinArgumentCount(source);
    const restType ***REMOVED*** getEffectiveRestType(source);
    if (restType && pos >***REMOVED*** parameterCount - 1) {
      return pos ***REMOVED******REMOVED******REMOVED*** parameterCount - 1 ? restType : createArrayType(getIndexedAccessType(restType, numberType));
    }
    const types ***REMOVED*** [];
    const flags ***REMOVED*** [];
    const names ***REMOVED*** [];
    for (let i ***REMOVED*** pos; i < parameterCount; i++) {
      if (!restType || i < parameterCount - 1) {
        types.push(getTypeAtPosition(source, i));
        flags.push(i < minArgumentCount ? 1 /* Required */ : 2 /* Optional */);
      } else {
        types.push(restType);
        flags.push(8 /* Variadic */);
      }
      names.push(getNameableDeclarationAtPosition(source, i));
    }
    return createTupleType(types, flags, readonly, names);
  }
  function getRestOrAnyTypeAtPosition(source, pos) {
    const restType ***REMOVED*** getRestTypeAtPosition(source, pos);
    const elementType ***REMOVED*** restType && getElementTypeOfArrayType(restType);
    return elementType && isTypeAny(elementType) ? anyType : restType;
  }
  function getParameterCount(signature) {
    const length2 ***REMOVED*** signature.parameters.length;
    if (signatureHasRestParameter(signature)) {
      const restType ***REMOVED*** getTypeOfSymbol(signature.parameters[length2 - 1]);
      if (isTupleType(restType)) {
        return length2 + restType.target.fixedLength - (restType.target.combinedFlags & 12 /* Variable */ ? 0 : 1);
      }
    }
    return length2;
  }
  function getMinArgumentCount(signature, flags) {
    const strongArityForUntypedJS ***REMOVED*** flags & 1 /* StrongArityForUntypedJS */;
    const voidIsNonOptional ***REMOVED*** flags & 2 /* VoidIsNonOptional */;
    if (voidIsNonOptional || signature.resolvedMinArgumentCount ***REMOVED******REMOVED******REMOVED*** void 0) {
      let minArgumentCount;
      if (signatureHasRestParameter(signature)) {
        const restType ***REMOVED*** getTypeOfSymbol(signature.parameters[signature.parameters.length - 1]);
        if (isTupleType(restType)) {
          const firstOptionalIndex ***REMOVED*** findIndex(restType.target.elementFlags, (f) ***REMOVED***> !(f & 1 /* Required */));
          const requiredCount ***REMOVED*** firstOptionalIndex < 0 ? restType.target.fixedLength : firstOptionalIndex;
          if (requiredCount > 0) {
            minArgumentCount ***REMOVED*** signature.parameters.length - 1 + requiredCount;
          }
        }
      }
      if (minArgumentCount ***REMOVED******REMOVED******REMOVED*** void 0) {
        if (!strongArityForUntypedJS && signature.flags & 32 /* IsUntypedSignatureInJSFile */) {
          return 0;
        }
        minArgumentCount ***REMOVED*** signature.minArgumentCount;
      }
      if (voidIsNonOptional) {
        return minArgumentCount;
      }
      for (let i ***REMOVED*** minArgumentCount - 1; i >***REMOVED*** 0; i--) {
        const type ***REMOVED*** getTypeAtPosition(signature, i);
        if (filterType(type, acceptsVoid).flags & 131072 /* Never */) {
          break;
        }
        minArgumentCount ***REMOVED*** i;
      }
      signature.resolvedMinArgumentCount ***REMOVED*** minArgumentCount;
    }
    return signature.resolvedMinArgumentCount;
  }
  function hasEffectiveRestParameter(signature) {
    if (signatureHasRestParameter(signature)) {
      const restType ***REMOVED*** getTypeOfSymbol(signature.parameters[signature.parameters.length - 1]);
      return !isTupleType(restType) || !!(restType.target.combinedFlags & 12 /* Variable */);
    }
    return false;
  }
  function getEffectiveRestType(signature) {
    if (signatureHasRestParameter(signature)) {
      const restType ***REMOVED*** getTypeOfSymbol(signature.parameters[signature.parameters.length - 1]);
      if (!isTupleType(restType)) {
        return isTypeAny(restType) ? anyArrayType : restType;
      }
      if (restType.target.combinedFlags & 12 /* Variable */) {
        return sliceTupleType(restType, restType.target.fixedLength);
      }
    }
    return void 0;
  }
  function getNonArrayRestType(signature) {
    const restType ***REMOVED*** getEffectiveRestType(signature);
    return restType && !isArrayType(restType) && !isTypeAny(restType) ? restType : void 0;
  }
  function getTypeOfFirstParameterOfSignature(signature) {
    return getTypeOfFirstParameterOfSignatureWithFallback(signature, neverType);
  }
  function getTypeOfFirstParameterOfSignatureWithFallback(signature, fallbackType) {
    return signature.parameters.length > 0 ? getTypeAtPosition(signature, 0) : fallbackType;
  }
  function inferFromAnnotatedParametersAndReturn(signature, context, inferenceContext) {
    const len ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    for (let i ***REMOVED*** 0; i < len; i++) {
      const declaration ***REMOVED*** signature.parameters[i].valueDeclaration;
      const typeNode2 ***REMOVED*** getEffectiveTypeAnnotationNode(declaration);
      if (typeNode2) {
        const source ***REMOVED*** addOptionality(
          getTypeFromTypeNode(typeNode2),
          /*isProperty*/
          false,
          isOptionalDeclaration(declaration)
        );
        const target ***REMOVED*** getTypeAtPosition(context, i);
        inferTypes(inferenceContext.inferences, source, target);
      }
    }
    const typeNode ***REMOVED*** signature.declaration && getEffectiveReturnTypeNode(signature.declaration);
    if (typeNode) {
      const source ***REMOVED*** getTypeFromTypeNode(typeNode);
      const target ***REMOVED*** getReturnTypeOfSignature(context);
      inferTypes(inferenceContext.inferences, source, target);
    }
  }
  function assignContextualParameterTypes(signature, context) {
    if (context.typeParameters) {
      if (!signature.typeParameters) {
        signature.typeParameters ***REMOVED*** context.typeParameters;
      } else {
        return;
      }
    }
    if (context.thisParameter) {
      const parameter ***REMOVED*** signature.thisParameter;
      if (!parameter || parameter.valueDeclaration && !parameter.valueDeclaration.type) {
        if (!parameter) {
          signature.thisParameter ***REMOVED*** createSymbolWithType(
            context.thisParameter,
            /*type*/
            void 0
          );
        }
        assignParameterType(signature.thisParameter, getTypeOfSymbol(context.thisParameter));
      }
    }
    const len ***REMOVED*** signature.parameters.length - (signatureHasRestParameter(signature) ? 1 : 0);
    for (let i ***REMOVED*** 0; i < len; i++) {
      const parameter ***REMOVED*** signature.parameters[i];
      const declaration ***REMOVED*** parameter.valueDeclaration;
      if (!getEffectiveTypeAnnotationNode(declaration)) {
        let type ***REMOVED*** tryGetTypeAtPosition(context, i);
        if (type && declaration.initializer) {
          let initializerType ***REMOVED*** checkDeclarationInitializer(declaration, 0 /* Normal */);
          if (!isTypeAssignableTo(initializerType, type) && isTypeAssignableTo(type, initializerType ***REMOVED*** widenTypeInferredFromInitializer(declaration, initializerType))) {
            type ***REMOVED*** initializerType;
          }
        }
        assignParameterType(parameter, type);
      }
    }
    if (signatureHasRestParameter(signature)) {
      const parameter ***REMOVED*** last(signature.parameters);
      if (parameter.valueDeclaration ? !getEffectiveTypeAnnotationNode(parameter.valueDeclaration) : !!(getCheckFlags(parameter) & 65536 /* DeferredType */)) {
        const contextualParameterType ***REMOVED*** getRestTypeAtPosition(context, len);
        assignParameterType(parameter, contextualParameterType);
      }
    }
  }
  function assignNonContextualParameterTypes(signature) {
    if (signature.thisParameter) {
      assignParameterType(signature.thisParameter);
    }
    for (const parameter of signature.parameters) {
      assignParameterType(parameter);
    }
  }
  function assignParameterType(parameter, contextualType) {
    const links ***REMOVED*** getSymbolLinks(parameter);
    if (!links.type) {
      const declaration ***REMOVED*** parameter.valueDeclaration;
      links.type ***REMOVED*** addOptionality(
        contextualType || (declaration ? getWidenedTypeForVariableLikeDeclaration(
          declaration,
          /*reportErrors*/
          true
        ) : getTypeOfSymbol(parameter)),
        /*isProperty*/
        false,
        /*isOptional*/
        !!declaration && !declaration.initializer && isOptionalDeclaration(declaration)
      );
      if (declaration && declaration.name.kind !***REMOVED******REMOVED*** 80 /* Identifier */) {
        if (links.type ***REMOVED******REMOVED******REMOVED*** unknownType) {
          links.type ***REMOVED*** getTypeFromBindingPattern(declaration.name);
        }
        assignBindingElementTypes(declaration.name, links.type);
      }
    } else if (contextualType) {
      Debug.assertEqual(links.type, contextualType, "Parameter symbol already has a cached type which differs from newly assigned type");
    }
  }
  function assignBindingElementTypes(pattern, parentType) {
    for (const element of pattern.elements) {
      if (!isOmittedExpression(element)) {
        const type ***REMOVED*** getBindingElementTypeFromParentType(
          element,
          parentType,
          /*noTupleBoundsCheck*/
          false
        );
        if (element.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
          getSymbolLinks(getSymbolOfDeclaration(element)).type ***REMOVED*** type;
        } else {
          assignBindingElementTypes(element.name, type);
        }
      }
    }
  }
  function createClassDecoratorContextType(classType) {
    return tryCreateTypeReference(getGlobalClassDecoratorContextType(
      /*reportErrors*/
      true
    ), [classType]);
  }
  function createClassMethodDecoratorContextType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassMethodDecoratorContextType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassGetterDecoratorContextType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassGetterDecoratorContextType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassSetterDecoratorContextType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassSetterDecoratorContextType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassAccessorDecoratorContextType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassAccessorDecoratorContextType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassFieldDecoratorContextType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassFieldDecoratorContextType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function getClassMemberDecoratorContextOverrideType(nameType, isPrivate, isStatic2) {
    const key ***REMOVED*** `${isPrivate ? "p" : "P"}${isStatic2 ? "s" : "S"}${nameType.id}`;
    let overrideType ***REMOVED*** decoratorContextOverrideTypeCache.get(key);
    if (!overrideType) {
      const members ***REMOVED*** createSymbolTable();
      members.set("name", createProperty("name", nameType));
      members.set("private", createProperty("private", isPrivate ? trueType : falseType));
      members.set("static", createProperty("static", isStatic2 ? trueType : falseType));
      overrideType ***REMOVED*** createAnonymousType(
        /*symbol*/
        void 0,
        members,
        emptyArray,
        emptyArray,
        emptyArray
      );
      decoratorContextOverrideTypeCache.set(key, overrideType);
    }
    return overrideType;
  }
  function createClassMemberDecoratorContextTypeForNode(node, thisType, valueType) {
    const isStatic2 ***REMOVED*** hasStaticModifier(node);
    const isPrivate ***REMOVED*** isPrivateIdentifier(node.name);
    const nameType ***REMOVED*** isPrivate ? getStringLiteralType(idText(node.name)) : getLiteralTypeFromPropertyName(node.name);
    const contextType ***REMOVED*** isMethodDeclaration(node) ? createClassMethodDecoratorContextType(thisType, valueType) : isGetAccessorDeclaration(node) ? createClassGetterDecoratorContextType(thisType, valueType) : isSetAccessorDeclaration(node) ? createClassSetterDecoratorContextType(thisType, valueType) : isAutoAccessorPropertyDeclaration(node) ? createClassAccessorDecoratorContextType(thisType, valueType) : isPropertyDeclaration(node) ? createClassFieldDecoratorContextType(thisType, valueType) : Debug.failBadSyntaxKind(node);
    const overrideType ***REMOVED*** getClassMemberDecoratorContextOverrideType(nameType, isPrivate, isStatic2);
    return getIntersectionType([contextType, overrideType]);
  }
  function createClassAccessorDecoratorTargetType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassAccessorDecoratorTargetType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassAccessorDecoratorResultType(thisType, valueType) {
    return tryCreateTypeReference(getGlobalClassAccessorDecoratorResultType(
      /*reportErrors*/
      true
    ), [thisType, valueType]);
  }
  function createClassFieldDecoratorInitializerMutatorType(thisType, valueType) {
    const thisParam ***REMOVED*** createParameter("this", thisType);
    const valueParam ***REMOVED*** createParameter("value", valueType);
    return createFunctionType(
      /*typeParameters*/
      void 0,
      thisParam,
      [valueParam],
      valueType,
      /*typePredicate*/
      void 0,
      1
    );
  }
  function createESDecoratorCallSignature(targetType, contextType, nonOptionalReturnType) {
    const targetParam ***REMOVED*** createParameter("target", targetType);
    const contextParam ***REMOVED*** createParameter("context", contextType);
    const returnType ***REMOVED*** getUnionType([nonOptionalReturnType, voidType]);
    return createCallSignature(
      /*typeParameters*/
      void 0,
      /*thisParameter*/
      void 0,
      [targetParam, contextParam],
      returnType
    );
  }
  function getESDecoratorCallSignature(decorator) {
    const { parent } ***REMOVED*** decorator;
    const links ***REMOVED*** getNodeLinks(parent);
    if (!links.decoratorSignature) {
      links.decoratorSignature ***REMOVED*** anySignature;
      switch (parent.kind) {
        case 264 /* ClassDeclaration */:
        case 232 /* ClassExpression */: {
          const node ***REMOVED*** parent;
          const targetType ***REMOVED*** getTypeOfSymbol(getSymbolOfDeclaration(node));
          const contextType ***REMOVED*** createClassDecoratorContextType(targetType);
          links.decoratorSignature ***REMOVED*** createESDecoratorCallSignature(targetType, contextType, targetType);
          break;
        }
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */: {
          const node ***REMOVED*** parent;
          if (!isClassLike(node.parent)) break;
          const valueType ***REMOVED*** isMethodDeclaration(node) ? getOrCreateTypeFromSignature(getSignatureFromDeclaration(node)) : getTypeOfNode(node);
          const thisType ***REMOVED*** hasStaticModifier(node) ? getTypeOfSymbol(getSymbolOfDeclaration(node.parent)) : getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(node.parent));
          const targetType ***REMOVED*** isGetAccessorDeclaration(node) ? createGetterFunctionType(valueType) : isSetAccessorDeclaration(node) ? createSetterFunctionType(valueType) : valueType;
          const contextType ***REMOVED*** createClassMemberDecoratorContextTypeForNode(node, thisType, valueType);
          const returnType ***REMOVED*** isGetAccessorDeclaration(node) ? createGetterFunctionType(valueType) : isSetAccessorDeclaration(node) ? createSetterFunctionType(valueType) : valueType;
          links.decoratorSignature ***REMOVED*** createESDecoratorCallSignature(targetType, contextType, returnType);
          break;
        }
        case 173 /* PropertyDeclaration */: {
          const node ***REMOVED*** parent;
          if (!isClassLike(node.parent)) break;
          const valueType ***REMOVED*** getTypeOfNode(node);
          const thisType ***REMOVED*** hasStaticModifier(node) ? getTypeOfSymbol(getSymbolOfDeclaration(node.parent)) : getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(node.parent));
          const targetType ***REMOVED*** hasAccessorModifier(node) ? createClassAccessorDecoratorTargetType(thisType, valueType) : undefinedType;
          const contextType ***REMOVED*** createClassMemberDecoratorContextTypeForNode(node, thisType, valueType);
          const returnType ***REMOVED*** hasAccessorModifier(node) ? createClassAccessorDecoratorResultType(thisType, valueType) : createClassFieldDecoratorInitializerMutatorType(thisType, valueType);
          links.decoratorSignature ***REMOVED*** createESDecoratorCallSignature(targetType, contextType, returnType);
          break;
        }
      }
    }
    return links.decoratorSignature ***REMOVED******REMOVED******REMOVED*** anySignature ? void 0 : links.decoratorSignature;
  }
  function getLegacyDecoratorCallSignature(decorator) {
    const { parent } ***REMOVED*** decorator;
    const links ***REMOVED*** getNodeLinks(parent);
    if (!links.decoratorSignature) {
      links.decoratorSignature ***REMOVED*** anySignature;
      switch (parent.kind) {
        case 264 /* ClassDeclaration */:
        case 232 /* ClassExpression */: {
          const node ***REMOVED*** parent;
          const targetType ***REMOVED*** getTypeOfSymbol(getSymbolOfDeclaration(node));
          const targetParam ***REMOVED*** createParameter("target", targetType);
          links.decoratorSignature ***REMOVED*** createCallSignature(
            /*typeParameters*/
            void 0,
            /*thisParameter*/
            void 0,
            [targetParam],
            getUnionType([targetType, voidType])
          );
          break;
        }
        case 170 /* Parameter */: {
          const node ***REMOVED*** parent;
          if (!isConstructorDeclaration(node.parent) && !(isMethodDeclaration(node.parent) || isSetAccessorDeclaration(node.parent) && isClassLike(node.parent.parent))) {
            break;
          }
          if (getThisParameter(node.parent) ***REMOVED******REMOVED******REMOVED*** node) {
            break;
          }
          const index ***REMOVED*** getThisParameter(node.parent) ? node.parent.parameters.indexOf(node) - 1 : node.parent.parameters.indexOf(node);
          Debug.assert(index >***REMOVED*** 0);
          const targetType ***REMOVED*** isConstructorDeclaration(node.parent) ? getTypeOfSymbol(getSymbolOfDeclaration(node.parent.parent)) : getParentTypeOfClassElement(node.parent);
          const keyType ***REMOVED*** isConstructorDeclaration(node.parent) ? undefinedType : getClassElementPropertyKeyType(node.parent);
          const indexType ***REMOVED*** getNumberLiteralType(index);
          const targetParam ***REMOVED*** createParameter("target", targetType);
          const keyParam ***REMOVED*** createParameter("propertyKey", keyType);
          const indexParam ***REMOVED*** createParameter("parameterIndex", indexType);
          links.decoratorSignature ***REMOVED*** createCallSignature(
            /*typeParameters*/
            void 0,
            /*thisParameter*/
            void 0,
            [targetParam, keyParam, indexParam],
            voidType
          );
          break;
        }
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
        case 173 /* PropertyDeclaration */: {
          const node ***REMOVED*** parent;
          if (!isClassLike(node.parent)) break;
          const targetType ***REMOVED*** getParentTypeOfClassElement(node);
          const targetParam ***REMOVED*** createParameter("target", targetType);
          const keyType ***REMOVED*** getClassElementPropertyKeyType(node);
          const keyParam ***REMOVED*** createParameter("propertyKey", keyType);
          const returnType ***REMOVED*** isPropertyDeclaration(node) ? voidType : createTypedPropertyDescriptorType(getTypeOfNode(node));
          const hasPropDesc ***REMOVED*** !isPropertyDeclaration(parent) || hasAccessorModifier(parent);
          if (hasPropDesc) {
            const descriptorType ***REMOVED*** createTypedPropertyDescriptorType(getTypeOfNode(node));
            const descriptorParam ***REMOVED*** createParameter("descriptor", descriptorType);
            links.decoratorSignature ***REMOVED*** createCallSignature(
              /*typeParameters*/
              void 0,
              /*thisParameter*/
              void 0,
              [targetParam, keyParam, descriptorParam],
              getUnionType([returnType, voidType])
            );
          } else {
            links.decoratorSignature ***REMOVED*** createCallSignature(
              /*typeParameters*/
              void 0,
              /*thisParameter*/
              void 0,
              [targetParam, keyParam],
              getUnionType([returnType, voidType])
            );
          }
          break;
        }
      }
    }
    return links.decoratorSignature ***REMOVED******REMOVED******REMOVED*** anySignature ? void 0 : links.decoratorSignature;
  }
  function getDecoratorCallSignature(decorator) {
    return legacyDecorators ? getLegacyDecoratorCallSignature(decorator) : getESDecoratorCallSignature(decorator);
  }
  function createPromiseType(promisedType) {
    const globalPromiseType ***REMOVED*** getGlobalPromiseType(
      /*reportErrors*/
      true
    );
    if (globalPromiseType !***REMOVED******REMOVED*** emptyGenericType) {
      promisedType ***REMOVED*** getAwaitedTypeNoAlias(unwrapAwaitedType(promisedType)) || unknownType;
      return createTypeReference(globalPromiseType, [promisedType]);
    }
    return unknownType;
  }
  function createPromiseLikeType(promisedType) {
    const globalPromiseLikeType ***REMOVED*** getGlobalPromiseLikeType(
      /*reportErrors*/
      true
    );
    if (globalPromiseLikeType !***REMOVED******REMOVED*** emptyGenericType) {
      promisedType ***REMOVED*** getAwaitedTypeNoAlias(unwrapAwaitedType(promisedType)) || unknownType;
      return createTypeReference(globalPromiseLikeType, [promisedType]);
    }
    return unknownType;
  }
  function createPromiseReturnType(func, promisedType) {
    const promiseType ***REMOVED*** createPromiseType(promisedType);
    if (promiseType ***REMOVED******REMOVED******REMOVED*** unknownType) {
      error(
        func,
        isImportCall(func) ? Diagnostics.A_dynamic_import_call_returns_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_include_ES2015_in_your_lib_option : Diagnostics.An_async_function_or_method_must_return_a_Promise_Make_sure_you_have_a_declaration_for_Promise_or_include_ES2015_in_your_lib_option
      );
      return errorType;
    } else if (!getGlobalPromiseConstructorSymbol(
      /*reportErrors*/
      true
    )) {
      error(
        func,
        isImportCall(func) ? Diagnostics.A_dynamic_import_call_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_the_Promise_constructor_or_include_ES2015_in_your_lib_option : Diagnostics.An_async_function_or_method_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_the_Promise_constructor_or_include_ES2015_in_your_lib_option
      );
    }
    return promiseType;
  }
  function createNewTargetExpressionType(targetType) {
    const symbol ***REMOVED*** createSymbol(0 /* None */, "NewTargetExpression");
    const targetPropertySymbol ***REMOVED*** createSymbol(4 /* Property */, "target", 8 /* Readonly */);
    targetPropertySymbol.parent ***REMOVED*** symbol;
    targetPropertySymbol.links.type ***REMOVED*** targetType;
    const members ***REMOVED*** createSymbolTable([targetPropertySymbol]);
    symbol.members ***REMOVED*** members;
    return createAnonymousType(symbol, members, emptyArray, emptyArray, emptyArray);
  }
  function getReturnTypeFromBody(func, checkMode) {
    if (!func.body) {
      return errorType;
    }
    const functionFlags ***REMOVED*** getFunctionFlags(func);
    const isAsync ***REMOVED*** (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0;
    const isGenerator ***REMOVED*** (functionFlags & 1 /* Generator */) !***REMOVED******REMOVED*** 0;
    let returnType;
    let yieldType;
    let nextType;
    let fallbackReturnType ***REMOVED*** voidType;
    if (func.body.kind !***REMOVED******REMOVED*** 242 /* Block */) {
      returnType ***REMOVED*** checkExpressionCached(func.body, checkMode && checkMode & ~8 /* SkipGenericFunctions */);
      if (isAsync) {
        returnType ***REMOVED*** unwrapAwaitedType(checkAwaitedType(
          returnType,
          /*withAlias*/
          false,
          /*errorNode*/
          func,
          Diagnostics.The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
        ));
      }
    } else if (isGenerator) {
      const returnTypes ***REMOVED*** checkAndAggregateReturnExpressionTypes(func, checkMode);
      if (!returnTypes) {
        fallbackReturnType ***REMOVED*** neverType;
      } else if (returnTypes.length > 0) {
        returnType ***REMOVED*** getUnionType(returnTypes, 2 /* Subtype */);
      }
      const { yieldTypes, nextTypes } ***REMOVED*** checkAndAggregateYieldOperandTypes(func, checkMode);
      yieldType ***REMOVED*** some(yieldTypes) ? getUnionType(yieldTypes, 2 /* Subtype */) : void 0;
      nextType ***REMOVED*** some(nextTypes) ? getIntersectionType(nextTypes) : void 0;
    } else {
      const types ***REMOVED*** checkAndAggregateReturnExpressionTypes(func, checkMode);
      if (!types) {
        return functionFlags & 2 /* Async */ ? createPromiseReturnType(func, neverType) : neverType;
      }
      if (types.length ***REMOVED******REMOVED******REMOVED*** 0) {
        const contextualReturnType ***REMOVED*** getContextualReturnType(
          func,
          /*contextFlags*/
          void 0
        );
        const returnType2 ***REMOVED*** contextualReturnType && (unwrapReturnType(contextualReturnType, functionFlags) || voidType).flags & 32768 /* Undefined */ ? undefinedType : voidType;
        return functionFlags & 2 /* Async */ ? createPromiseReturnType(func, returnType2) : (
          // Async function
          returnType2
        );
      }
      returnType ***REMOVED*** getUnionType(types, 2 /* Subtype */);
    }
    if (returnType || yieldType || nextType) {
      if (yieldType) reportErrorsFromWidening(func, yieldType, 3 /* GeneratorYield */);
      if (returnType) reportErrorsFromWidening(func, returnType, 1 /* FunctionReturn */);
      if (nextType) reportErrorsFromWidening(func, nextType, 2 /* GeneratorNext */);
      if (returnType && isUnitType(returnType) || yieldType && isUnitType(yieldType) || nextType && isUnitType(nextType)) {
        const contextualSignature ***REMOVED*** getContextualSignatureForFunctionLikeDeclaration(func);
        const contextualType ***REMOVED*** !contextualSignature ? void 0 : contextualSignature ***REMOVED******REMOVED******REMOVED*** getSignatureFromDeclaration(func) ? isGenerator ? void 0 : returnType : instantiateContextualType(
          getReturnTypeOfSignature(contextualSignature),
          func,
          /*contextFlags*/
          void 0
        );
        if (isGenerator) {
          yieldType ***REMOVED*** getWidenedLiteralLikeTypeForContextualIterationTypeIfNeeded(yieldType, contextualType, 0 /* Yield */, isAsync);
          returnType ***REMOVED*** getWidenedLiteralLikeTypeForContextualIterationTypeIfNeeded(returnType, contextualType, 1 /* Return */, isAsync);
          nextType ***REMOVED*** getWidenedLiteralLikeTypeForContextualIterationTypeIfNeeded(nextType, contextualType, 2 /* Next */, isAsync);
        } else {
          returnType ***REMOVED*** getWidenedLiteralLikeTypeForContextualReturnTypeIfNeeded(returnType, contextualType, isAsync);
        }
      }
      if (yieldType) yieldType ***REMOVED*** getWidenedType(yieldType);
      if (returnType) returnType ***REMOVED*** getWidenedType(returnType);
      if (nextType) nextType ***REMOVED*** getWidenedType(nextType);
    }
    if (isGenerator) {
      return createGeneratorType(
        yieldType || neverType,
        returnType || fallbackReturnType,
        nextType || getContextualIterationType(2 /* Next */, func) || unknownType,
        isAsync
      );
    } else {
      return isAsync ? createPromiseType(returnType || fallbackReturnType) : returnType || fallbackReturnType;
    }
  }
  function createGeneratorType(yieldType, returnType, nextType, isAsyncGenerator) {
    const resolver ***REMOVED*** isAsyncGenerator ? asyncIterationTypesResolver : syncIterationTypesResolver;
    const globalGeneratorType ***REMOVED*** resolver.getGlobalGeneratorType(
      /*reportErrors*/
      false
    );
    yieldType ***REMOVED*** resolver.resolveIterationType(
      yieldType,
      /*errorNode*/
      void 0
    ) || unknownType;
    returnType ***REMOVED*** resolver.resolveIterationType(
      returnType,
      /*errorNode*/
      void 0
    ) || unknownType;
    if (globalGeneratorType ***REMOVED******REMOVED******REMOVED*** emptyGenericType) {
      const globalIterableIteratorType ***REMOVED*** resolver.getGlobalIterableIteratorType(
        /*reportErrors*/
        false
      );
      if (globalIterableIteratorType !***REMOVED******REMOVED*** emptyGenericType) {
        return createTypeFromGenericGlobalType(globalIterableIteratorType, [yieldType, returnType, nextType]);
      }
      resolver.getGlobalIterableIteratorType(
        /*reportErrors*/
        true
      );
      return emptyObjectType;
    }
    return createTypeFromGenericGlobalType(globalGeneratorType, [yieldType, returnType, nextType]);
  }
  function checkAndAggregateYieldOperandTypes(func, checkMode) {
    const yieldTypes ***REMOVED*** [];
    const nextTypes ***REMOVED*** [];
    const isAsync ***REMOVED*** (getFunctionFlags(func) & 2 /* Async */) !***REMOVED******REMOVED*** 0;
    forEachYieldExpression(func.body, (yieldExpression) ***REMOVED***> {
      const yieldExpressionType ***REMOVED*** yieldExpression.expression ? checkExpression(yieldExpression.expression, checkMode) : undefinedWideningType;
      pushIfUnique(yieldTypes, getYieldedTypeOfYieldExpression(yieldExpression, yieldExpressionType, anyType, isAsync));
      let nextType;
      if (yieldExpression.asteriskToken) {
        const iterationTypes ***REMOVED*** getIterationTypesOfIterable(
          yieldExpressionType,
          isAsync ? 19 /* AsyncYieldStar */ : 17 /* YieldStar */,
          yieldExpression.expression
        );
        nextType ***REMOVED*** iterationTypes && iterationTypes.nextType;
      } else {
        nextType ***REMOVED*** getContextualType(
          yieldExpression,
          /*contextFlags*/
          void 0
        );
      }
      if (nextType) pushIfUnique(nextTypes, nextType);
    });
    return { yieldTypes, nextTypes };
  }
  function getYieldedTypeOfYieldExpression(node, expressionType, sentType, isAsync) {
    if (expressionType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverType;
    }
    const errorNode ***REMOVED*** node.expression || node;
    const yieldedType ***REMOVED*** node.asteriskToken ? checkIteratedTypeOrElementType(isAsync ? 19 /* AsyncYieldStar */ : 17 /* YieldStar */, expressionType, sentType, errorNode) : expressionType;
    return !isAsync ? yieldedType : getAwaitedType(
      yieldedType,
      errorNode,
      node.asteriskToken ? Diagnostics.Type_of_iterated_elements_of_a_yield_Asterisk_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member : Diagnostics.Type_of_yield_operand_in_an_async_generator_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
    );
  }
  function getNotEqualFactsFromTypeofSwitch(start, end, witnesses) {
    let facts ***REMOVED*** 0 /* None */;
    for (let i ***REMOVED*** 0; i < witnesses.length; i++) {
      const witness ***REMOVED*** i < start || i >***REMOVED*** end ? witnesses[i] : void 0;
      facts |***REMOVED*** witness !***REMOVED******REMOVED*** void 0 ? typeofNEFacts.get(witness) || 32768 /* TypeofNEHostObject */ : 0;
    }
    return facts;
  }
  function isExhaustiveSwitchStatement(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (links.isExhaustive ***REMOVED******REMOVED******REMOVED*** void 0) {
      links.isExhaustive ***REMOVED*** 0;
      const exhaustive ***REMOVED*** computeExhaustiveSwitchStatement(node);
      if (links.isExhaustive ***REMOVED******REMOVED******REMOVED*** 0) {
        links.isExhaustive ***REMOVED*** exhaustive;
      }
    } else if (links.isExhaustive ***REMOVED******REMOVED******REMOVED*** 0) {
      links.isExhaustive ***REMOVED*** false;
    }
    return links.isExhaustive;
  }
  function computeExhaustiveSwitchStatement(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 222 /* TypeOfExpression */) {
      const witnesses ***REMOVED*** getSwitchClauseTypeOfWitnesses(node);
      if (!witnesses) {
        return false;
      }
      const operandConstraint ***REMOVED*** getBaseConstraintOrType(checkExpressionCached(node.expression.expression));
      const notEqualFacts ***REMOVED*** getNotEqualFactsFromTypeofSwitch(0, 0, witnesses);
      if (operandConstraint.flags & 3 /* AnyOrUnknown */) {
        return (556800 /* AllTypeofNE */ & notEqualFacts) ***REMOVED******REMOVED******REMOVED*** 556800 /* AllTypeofNE */;
      }
      return !someType(operandConstraint, (t) ***REMOVED***> getTypeFacts(t, notEqualFacts) ***REMOVED******REMOVED******REMOVED*** notEqualFacts);
    }
    const type ***REMOVED*** getBaseConstraintOrType(checkExpressionCached(node.expression));
    if (!isLiteralType(type)) {
      return false;
    }
    const switchTypes ***REMOVED*** getSwitchClauseTypes(node);
    if (!switchTypes.length || some(switchTypes, isNeitherUnitTypeNorNever)) {
      return false;
    }
    return eachTypeContainedIn(mapType(type, getRegularTypeOfLiteralType), switchTypes);
  }
  function functionHasImplicitReturn(func) {
    return func.endFlowNode && isReachableFlowNode(func.endFlowNode);
  }
  function checkAndAggregateReturnExpressionTypes(func, checkMode) {
    const functionFlags ***REMOVED*** getFunctionFlags(func);
    const aggregatedTypes ***REMOVED*** [];
    let hasReturnWithNoExpression ***REMOVED*** functionHasImplicitReturn(func);
    let hasReturnOfTypeNever ***REMOVED*** false;
    forEachReturnStatement(func.body, (returnStatement) ***REMOVED***> {
      let expr ***REMOVED*** returnStatement.expression;
      if (expr) {
        expr ***REMOVED*** skipParentheses(
          expr,
          /*excludeJSDocTypeAssertions*/
          true
        );
        if (functionFlags & 2 /* Async */ && expr.kind ***REMOVED******REMOVED******REMOVED*** 224 /* AwaitExpression */) {
          expr ***REMOVED*** skipParentheses(
            expr.expression,
            /*excludeJSDocTypeAssertions*/
            true
          );
        }
        if (expr.kind ***REMOVED******REMOVED******REMOVED*** 214 /* CallExpression */ && expr.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && checkExpressionCached(expr.expression).symbol ***REMOVED******REMOVED******REMOVED*** getMergedSymbol(func.symbol) && (!isFunctionExpressionOrArrowFunction(func.symbol.valueDeclaration) || isConstantReference(expr.expression))) {
          hasReturnOfTypeNever ***REMOVED*** true;
          return;
        }
        let type ***REMOVED*** checkExpressionCached(expr, checkMode && checkMode & ~8 /* SkipGenericFunctions */);
        if (functionFlags & 2 /* Async */) {
          type ***REMOVED*** unwrapAwaitedType(checkAwaitedType(
            type,
            /*withAlias*/
            false,
            func,
            Diagnostics.The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
          ));
        }
        if (type.flags & 131072 /* Never */) {
          hasReturnOfTypeNever ***REMOVED*** true;
        }
        pushIfUnique(aggregatedTypes, type);
      } else {
        hasReturnWithNoExpression ***REMOVED*** true;
      }
    });
    if (aggregatedTypes.length ***REMOVED******REMOVED******REMOVED*** 0 && !hasReturnWithNoExpression && (hasReturnOfTypeNever || mayReturnNever(func))) {
      return void 0;
    }
    if (strictNullChecks && aggregatedTypes.length && hasReturnWithNoExpression && !(isJSConstructor(func) && aggregatedTypes.some((t) ***REMOVED***> t.symbol ***REMOVED******REMOVED******REMOVED*** func.symbol))) {
      pushIfUnique(aggregatedTypes, undefinedType);
    }
    return aggregatedTypes;
  }
  function mayReturnNever(func) {
    switch (func.kind) {
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        return true;
      case 175 /* MethodDeclaration */:
        return func.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */;
      default:
        return false;
    }
  }
  function getTypePredicateFromBody(func) {
    switch (func.kind) {
      case 177 /* Constructor */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return void 0;
    }
    const functionFlags ***REMOVED*** getFunctionFlags(func);
    if (functionFlags !***REMOVED******REMOVED*** 0 /* Normal */) return void 0;
    let singleReturn;
    if (func.body && func.body.kind !***REMOVED******REMOVED*** 242 /* Block */) {
      singleReturn ***REMOVED*** func.body;
    } else {
      const bailedEarly ***REMOVED*** forEachReturnStatement(func.body, (returnStatement) ***REMOVED***> {
        if (singleReturn || !returnStatement.expression) return true;
        singleReturn ***REMOVED*** returnStatement.expression;
      });
      if (bailedEarly || !singleReturn || functionHasImplicitReturn(func)) return void 0;
    }
    return checkIfExpressionRefinesAnyParameter(func, singleReturn);
  }
  function checkIfExpressionRefinesAnyParameter(func, expr) {
    expr ***REMOVED*** skipParentheses(
      expr,
      /*excludeJSDocTypeAssertions*/
      true
    );
    const returnType ***REMOVED*** checkExpressionCached(expr);
    if (!(returnType.flags & 16 /* Boolean */)) return void 0;
    return forEach(func.parameters, (param, i) ***REMOVED***> {
      const initType ***REMOVED*** getTypeOfSymbol(param.symbol);
      if (!initType || initType.flags & 16 /* Boolean */ || !isIdentifier(param.name) || isSymbolAssigned(param.symbol) || isRestParameter(param)) {
        return;
      }
      const trueType2 ***REMOVED*** checkIfExpressionRefinesParameter(func, expr, param, initType);
      if (trueType2) {
        return createTypePredicate(1 /* Identifier */, unescapeLeadingUnderscores(param.name.escapedText), i, trueType2);
      }
    });
  }
  function checkIfExpressionRefinesParameter(func, expr, param, initType) {
    const antecedent ***REMOVED*** canHaveFlowNode(expr) && expr.flowNode || expr.parent.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */ && expr.parent.flowNode || createFlowNode(
      2 /* Start */,
      /*node*/
      void 0,
      /*antecedent*/
      void 0
    );
    const trueCondition ***REMOVED*** createFlowNode(32 /* TrueCondition */, expr, antecedent);
    const trueType2 ***REMOVED*** getFlowTypeOfReference(param.name, initType, initType, func, trueCondition);
    if (trueType2 ***REMOVED******REMOVED******REMOVED*** initType) return void 0;
    const falseCondition ***REMOVED*** createFlowNode(64 /* FalseCondition */, expr, antecedent);
    const falseSubtype ***REMOVED*** getReducedType(getFlowTypeOfReference(param.name, initType, trueType2, func, falseCondition));
    return falseSubtype.flags & 131072 /* Never */ ? trueType2 : void 0;
  }
  function checkAllCodePathsInNonVoidFunctionReturnOrThrow(func, returnType) {
    addLazyDiagnostic(checkAllCodePathsInNonVoidFunctionReturnOrThrowDiagnostics);
    return;
    function checkAllCodePathsInNonVoidFunctionReturnOrThrowDiagnostics() {
      const functionFlags ***REMOVED*** getFunctionFlags(func);
      const type ***REMOVED*** returnType && unwrapReturnType(returnType, functionFlags);
      if (type && (maybeTypeOfKind(type, 16384 /* Void */) || type.flags & (1 /* Any */ | 32768 /* Undefined */))) {
        return;
      }
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || nodeIsMissing(func.body) || func.body.kind !***REMOVED******REMOVED*** 242 /* Block */ || !functionHasImplicitReturn(func)) {
        return;
      }
      const hasExplicitReturn ***REMOVED*** func.flags & 1024 /* HasExplicitReturn */;
      const errorNode ***REMOVED*** getEffectiveReturnTypeNode(func) || func;
      if (type && type.flags & 131072 /* Never */) {
        error(errorNode, Diagnostics.A_function_returning_never_cannot_have_a_reachable_end_point);
      } else if (type && !hasExplicitReturn) {
        error(errorNode, Diagnostics.A_function_whose_declared_type_is_neither_undefined_void_nor_any_must_return_a_value);
      } else if (type && strictNullChecks && !isTypeAssignableTo(undefinedType, type)) {
        error(errorNode, Diagnostics.Function_lacks_ending_return_statement_and_return_type_does_not_include_undefined);
      } else if (compilerOptions.noImplicitReturns) {
        if (!type) {
          if (!hasExplicitReturn) {
            return;
          }
          const inferredReturnType ***REMOVED*** getReturnTypeOfSignature(getSignatureFromDeclaration(func));
          if (isUnwrappedReturnTypeUndefinedVoidOrAny(func, inferredReturnType)) {
            return;
          }
        }
        error(errorNode, Diagnostics.Not_all_code_paths_return_a_value);
      }
    }
  }
  function checkFunctionExpressionOrObjectLiteralMethod(node, checkMode) {
    Debug.assert(node.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ || isObjectLiteralMethod(node));
    checkNodeDeferred(node);
    if (isFunctionExpression(node)) {
      checkCollisionsForDeclarationName(node, node.name);
    }
    if (checkMode && checkMode & 4 /* SkipContextSensitive */ && isContextSensitive(node)) {
      if (!getEffectiveReturnTypeNode(node) && !hasContextSensitiveParameters(node)) {
        const contextualSignature ***REMOVED*** getContextualSignature(node);
        if (contextualSignature && couldContainTypeVariables(getReturnTypeOfSignature(contextualSignature))) {
          const links ***REMOVED*** getNodeLinks(node);
          if (links.contextFreeType) {
            return links.contextFreeType;
          }
          const returnType ***REMOVED*** getReturnTypeFromBody(node, checkMode);
          const returnOnlySignature ***REMOVED*** createSignature(
            /*declaration*/
            void 0,
            /*typeParameters*/
            void 0,
            /*thisParameter*/
            void 0,
            emptyArray,
            returnType,
            /*resolvedTypePredicate*/
            void 0,
            0,
            64 /* IsNonInferrable */
          );
          const returnOnlyType ***REMOVED*** createAnonymousType(node.symbol, emptySymbols, [returnOnlySignature], emptyArray, emptyArray);
          returnOnlyType.objectFlags |***REMOVED*** 262144 /* NonInferrableType */;
          return links.contextFreeType ***REMOVED*** returnOnlyType;
        }
      }
      return anyFunctionType;
    }
    const hasGrammarError ***REMOVED*** checkGrammarFunctionLikeDeclaration(node);
    if (!hasGrammarError && node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */) {
      checkGrammarForGenerator(node);
    }
    contextuallyCheckFunctionExpressionOrObjectLiteralMethod(node, checkMode);
    return getTypeOfSymbol(getSymbolOfDeclaration(node));
  }
  function contextuallyCheckFunctionExpressionOrObjectLiteralMethod(node, checkMode) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!(links.flags & 64 /* ContextChecked */)) {
      const contextualSignature ***REMOVED*** getContextualSignature(node);
      if (!(links.flags & 64 /* ContextChecked */)) {
        links.flags |***REMOVED*** 64 /* ContextChecked */;
        const signature ***REMOVED*** firstOrUndefined(getSignaturesOfType(getTypeOfSymbol(getSymbolOfDeclaration(node)), 0 /* Call */));
        if (!signature) {
          return;
        }
        if (isContextSensitive(node)) {
          if (contextualSignature) {
            const inferenceContext ***REMOVED*** getInferenceContext(node);
            let instantiatedContextualSignature;
            if (checkMode && checkMode & 2 /* Inferential */) {
              inferFromAnnotatedParametersAndReturn(signature, contextualSignature, inferenceContext);
              const restType ***REMOVED*** getEffectiveRestType(contextualSignature);
              if (restType && restType.flags & 262144 /* TypeParameter */) {
                instantiatedContextualSignature ***REMOVED*** instantiateSignature(contextualSignature, inferenceContext.nonFixingMapper);
              }
            }
            instantiatedContextualSignature || (instantiatedContextualSignature ***REMOVED*** inferenceContext ? instantiateSignature(contextualSignature, inferenceContext.mapper) : contextualSignature);
            assignContextualParameterTypes(signature, instantiatedContextualSignature);
          } else {
            assignNonContextualParameterTypes(signature);
          }
        } else if (contextualSignature && !node.typeParameters && contextualSignature.parameters.length > node.parameters.length) {
          const inferenceContext ***REMOVED*** getInferenceContext(node);
          if (checkMode && checkMode & 2 /* Inferential */) {
            inferFromAnnotatedParametersAndReturn(signature, contextualSignature, inferenceContext);
          }
        }
        if (contextualSignature && !getReturnTypeFromAnnotation(node) && !signature.resolvedReturnType) {
          const returnType ***REMOVED*** getReturnTypeFromBody(node, checkMode);
          if (!signature.resolvedReturnType) {
            signature.resolvedReturnType ***REMOVED*** returnType;
          }
        }
        checkSignatureDeclaration(node);
      }
    }
  }
  function checkFunctionExpressionOrObjectLiteralMethodDeferred(node) {
    Debug.assert(node.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ || isObjectLiteralMethod(node));
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    const returnType ***REMOVED*** getReturnTypeFromAnnotation(node);
    checkAllCodePathsInNonVoidFunctionReturnOrThrow(node, returnType);
    if (node.body) {
      if (!getEffectiveReturnTypeNode(node)) {
        getReturnTypeOfSignature(getSignatureFromDeclaration(node));
      }
      if (node.body.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */) {
        checkSourceElement(node.body);
      } else {
        const exprType ***REMOVED*** checkExpression(node.body);
        const returnOrPromisedType ***REMOVED*** returnType && unwrapReturnType(returnType, functionFlags);
        if (returnOrPromisedType) {
          checkReturnExpression(node, returnOrPromisedType, node.body, node.body, exprType);
        }
      }
    }
  }
  function checkArithmeticOperandType(operand, type, diagnostic, isAwaitValid ***REMOVED*** false) {
    if (!isTypeAssignableTo(type, numberOrBigIntType)) {
      const awaitedType ***REMOVED*** isAwaitValid && getAwaitedTypeOfPromise(type);
      errorAndMaybeSuggestAwait(
        operand,
        !!awaitedType && isTypeAssignableTo(awaitedType, numberOrBigIntType),
        diagnostic
      );
      return false;
    }
    return true;
  }
  function isReadonlyAssignmentDeclaration(d) {
    if (!isCallExpression(d)) {
      return false;
    }
    if (!isBindableObjectDefinePropertyCall(d)) {
      return false;
    }
    const objectLitType ***REMOVED*** checkExpressionCached(d.arguments[2]);
    const valueType ***REMOVED*** getTypeOfPropertyOfType(objectLitType, "value");
    if (valueType) {
      const writableProp ***REMOVED*** getPropertyOfType(objectLitType, "writable");
      const writableType ***REMOVED*** writableProp && getTypeOfSymbol(writableProp);
      if (!writableType || writableType ***REMOVED******REMOVED******REMOVED*** falseType || writableType ***REMOVED******REMOVED******REMOVED*** regularFalseType) {
        return true;
      }
      if (writableProp && writableProp.valueDeclaration && isPropertyAssignment(writableProp.valueDeclaration)) {
        const initializer ***REMOVED*** writableProp.valueDeclaration.initializer;
        const rawOriginalType ***REMOVED*** checkExpression(initializer);
        if (rawOriginalType ***REMOVED******REMOVED******REMOVED*** falseType || rawOriginalType ***REMOVED******REMOVED******REMOVED*** regularFalseType) {
          return true;
        }
      }
      return false;
    }
    const setProp ***REMOVED*** getPropertyOfType(objectLitType, "set");
    return !setProp;
  }
  function isReadonlySymbol(symbol) {
    return !!(getCheckFlags(symbol) & 8 /* Readonly */ || symbol.flags & 4 /* Property */ && getDeclarationModifierFlagsFromSymbol(symbol) & 8 /* Readonly */ || symbol.flags & 3 /* Variable */ && getDeclarationNodeFlagsFromSymbol(symbol) & 6 /* Constant */ || symbol.flags & 98304 /* Accessor */ && !(symbol.flags & 65536 /* SetAccessor */) || symbol.flags & 8 /* EnumMember */ || some(symbol.declarations, isReadonlyAssignmentDeclaration));
  }
  function isAssignmentToReadonlyEntity(expr, symbol, assignmentKind) {
    var _a, _b;
    if (assignmentKind ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      return false;
    }
    if (isReadonlySymbol(symbol)) {
      if (symbol.flags & 4 /* Property */ && isAccessExpression(expr) && expr.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
        const ctor ***REMOVED*** getControlFlowContainer(expr);
        if (!(ctor && (ctor.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || isJSConstructor(ctor)))) {
          return true;
        }
        if (symbol.valueDeclaration) {
          const isAssignmentDeclaration2 ***REMOVED*** isBinaryExpression(symbol.valueDeclaration);
          const isLocalPropertyDeclaration ***REMOVED*** ctor.parent ***REMOVED******REMOVED******REMOVED*** symbol.valueDeclaration.parent;
          const isLocalParameterProperty ***REMOVED*** ctor ***REMOVED******REMOVED******REMOVED*** symbol.valueDeclaration.parent;
          const isLocalThisPropertyAssignment ***REMOVED*** isAssignmentDeclaration2 && ((_a ***REMOVED*** symbol.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.valueDeclaration) ***REMOVED******REMOVED******REMOVED*** ctor.parent;
          const isLocalThisPropertyAssignmentConstructorFunction ***REMOVED*** isAssignmentDeclaration2 && ((_b ***REMOVED*** symbol.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.valueDeclaration) ***REMOVED******REMOVED******REMOVED*** ctor;
          const isWriteableSymbol ***REMOVED*** isLocalPropertyDeclaration || isLocalParameterProperty || isLocalThisPropertyAssignment || isLocalThisPropertyAssignmentConstructorFunction;
          return !isWriteableSymbol;
        }
      }
      return true;
    }
    if (isAccessExpression(expr)) {
      const node ***REMOVED*** skipParentheses(expr.expression);
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
        const symbol2 ***REMOVED*** getNodeLinks(node).resolvedSymbol;
        if (symbol2.flags & 2097152 /* Alias */) {
          const declaration ***REMOVED*** getDeclarationOfAliasSymbol(symbol2);
          return !!declaration && declaration.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */;
        }
      }
    }
    return false;
  }
  function checkReferenceExpression(expr, invalidReferenceMessage, invalidOptionalChainMessage) {
    const node ***REMOVED*** skipOuterExpressions(expr, 38 /* Assertions */ | 1 /* Parentheses */);
    if (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */ && !isAccessExpression(node)) {
      error(expr, invalidReferenceMessage);
      return false;
    }
    if (node.flags & 64 /* OptionalChain */) {
      error(expr, invalidOptionalChainMessage);
      return false;
    }
    return true;
  }
  function checkDeleteExpression(node) {
    checkExpression(node.expression);
    const expr ***REMOVED*** skipParentheses(node.expression);
    if (!isAccessExpression(expr)) {
      error(expr, Diagnostics.The_operand_of_a_delete_operator_must_be_a_property_reference);
      return booleanType;
    }
    if (isPropertyAccessExpression(expr) && isPrivateIdentifier(expr.name)) {
      error(expr, Diagnostics.The_operand_of_a_delete_operator_cannot_be_a_private_identifier);
    }
    const links ***REMOVED*** getNodeLinks(expr);
    const symbol ***REMOVED*** getExportSymbolOfValueSymbolIfExported(links.resolvedSymbol);
    if (symbol) {
      if (isReadonlySymbol(symbol)) {
        error(expr, Diagnostics.The_operand_of_a_delete_operator_cannot_be_a_read_only_property);
      } else {
        checkDeleteExpressionMustBeOptional(expr, symbol);
      }
    }
    return booleanType;
  }
  function checkDeleteExpressionMustBeOptional(expr, symbol) {
    const type ***REMOVED*** getTypeOfSymbol(symbol);
    if (strictNullChecks && !(type.flags & (3 /* AnyOrUnknown */ | 131072 /* Never */)) && !(exactOptionalPropertyTypes ? symbol.flags & 16777216 /* Optional */ : hasTypeFacts(type, 16777216 /* IsUndefined */))) {
      error(expr, Diagnostics.The_operand_of_a_delete_operator_must_be_optional);
    }
  }
  function checkTypeOfExpression(node) {
    checkExpression(node.expression);
    return typeofType;
  }
  function checkVoidExpression(node) {
    checkNodeDeferred(node);
    return undefinedWideningType;
  }
  function checkAwaitGrammar(node) {
    let hasError ***REMOVED*** false;
    const container ***REMOVED*** getContainingFunctionOrClassStaticBlock(node);
    if (container && isClassStaticBlockDeclaration(container)) {
      const message ***REMOVED*** isAwaitExpression(node) ? Diagnostics.await_expression_cannot_be_used_inside_a_class_static_block : Diagnostics.await_using_statements_cannot_be_used_inside_a_class_static_block;
      error(node, message);
      hasError ***REMOVED*** true;
    } else if (!(node.flags & 65536 /* AwaitContext */)) {
      if (isInTopLevelContext(node)) {
        const sourceFile ***REMOVED*** getSourceFileOfNode(node);
        if (!hasParseDiagnostics(sourceFile)) {
          let span;
          if (!isEffectiveExternalModule(sourceFile, compilerOptions)) {
            span ?? (span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos));
            const message ***REMOVED*** isAwaitExpression(node) ? Diagnostics.await_expressions_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module : Diagnostics.await_using_statements_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module;
            const diagnostic ***REMOVED*** createFileDiagnostic(sourceFile, span.start, span.length, message);
            diagnostics.add(diagnostic);
            hasError ***REMOVED*** true;
          }
          switch (moduleKind) {
            case 100 /* Node16 */:
            case 101 /* Node18 */:
            case 102 /* Node20 */:
            case 199 /* NodeNext */:
              if (sourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
                span ?? (span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos));
                diagnostics.add(
                  createFileDiagnostic(sourceFile, span.start, span.length, Diagnostics.The_current_file_is_a_CommonJS_module_and_cannot_use_await_at_the_top_level)
                );
                hasError ***REMOVED*** true;
                break;
              }
            // fallthrough
            case 7 /* ES2022 */:
            case 99 /* ESNext */:
            case 200 /* Preserve */:
            case 4 /* System */:
              if (languageVersion >***REMOVED*** 4 /* ES2017 */) {
                break;
              }
            // fallthrough
            default:
              span ?? (span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos));
              const message ***REMOVED*** isAwaitExpression(node) ? Diagnostics.Top_level_await_expressions_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher : Diagnostics.Top_level_await_using_statements_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher;
              diagnostics.add(createFileDiagnostic(sourceFile, span.start, span.length, message));
              hasError ***REMOVED*** true;
              break;
          }
        }
      } else {
        const sourceFile ***REMOVED*** getSourceFileOfNode(node);
        if (!hasParseDiagnostics(sourceFile)) {
          const span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos);
          const message ***REMOVED*** isAwaitExpression(node) ? Diagnostics.await_expressions_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules : Diagnostics.await_using_statements_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules;
          const diagnostic ***REMOVED*** createFileDiagnostic(sourceFile, span.start, span.length, message);
          if (container && container.kind !***REMOVED******REMOVED*** 177 /* Constructor */ && (getFunctionFlags(container) & 2 /* Async */) ***REMOVED******REMOVED******REMOVED*** 0) {
            const relatedInfo ***REMOVED*** createDiagnosticForNode(container, Diagnostics.Did_you_mean_to_mark_this_function_as_async);
            addRelatedInfo(diagnostic, relatedInfo);
          }
          diagnostics.add(diagnostic);
          hasError ***REMOVED*** true;
        }
      }
    }
    if (isAwaitExpression(node) && isInParameterInitializerBeforeContainingFunction(node)) {
      error(node, Diagnostics.await_expressions_cannot_be_used_in_a_parameter_initializer);
      hasError ***REMOVED*** true;
    }
    return hasError;
  }
  function checkAwaitExpression(node) {
    addLazyDiagnostic(() ***REMOVED***> checkAwaitGrammar(node));
    const operandType ***REMOVED*** checkExpression(node.expression);
    const awaitedType ***REMOVED*** checkAwaitedType(
      operandType,
      /*withAlias*/
      true,
      node,
      Diagnostics.Type_of_await_operand_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
    );
    if (awaitedType ***REMOVED******REMOVED******REMOVED*** operandType && !isErrorType(awaitedType) && !(operandType.flags & 3 /* AnyOrUnknown */)) {
      addErrorOrSuggestion(
        /*isError*/
        false,
        createDiagnosticForNode(node, Diagnostics.await_has_no_effect_on_the_type_of_this_expression)
      );
    }
    return awaitedType;
  }
  function checkPrefixUnaryExpression(node) {
    const operandType ***REMOVED*** checkExpression(node.operand);
    if (operandType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverType;
    }
    switch (node.operand.kind) {
      case 9 /* NumericLiteral */:
        switch (node.operator) {
          case 41 /* MinusToken */:
            return getFreshTypeOfLiteralType(getNumberLiteralType(-node.operand.text));
          case 40 /* PlusToken */:
            return getFreshTypeOfLiteralType(getNumberLiteralType(+node.operand.text));
        }
        break;
      case 10 /* BigIntLiteral */:
        if (node.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */) {
          return getFreshTypeOfLiteralType(getBigIntLiteralType({
            negative: true,
            base10Value: parsePseudoBigInt(node.operand.text)
          }));
        }
    }
    switch (node.operator) {
      case 40 /* PlusToken */:
      case 41 /* MinusToken */:
      case 55 /* TildeToken */:
        checkNonNullType(operandType, node.operand);
        if (maybeTypeOfKindConsideringBaseConstraint(operandType, 12288 /* ESSymbolLike */)) {
          error(node.operand, Diagnostics.The_0_operator_cannot_be_applied_to_type_symbol, tokenToString(node.operator));
        }
        if (node.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
          if (maybeTypeOfKindConsideringBaseConstraint(operandType, 2112 /* BigIntLike */)) {
            error(node.operand, Diagnostics.Operator_0_cannot_be_applied_to_type_1, tokenToString(node.operator), typeToString(getBaseTypeOfLiteralType(operandType)));
          }
          return numberType;
        }
        return getUnaryResultType(operandType);
      case 54 /* ExclamationToken */:
        checkTruthinessOfType(operandType, node.operand);
        const facts ***REMOVED*** getTypeFacts(operandType, 4194304 /* Truthy */ | 8388608 /* Falsy */);
        return facts ***REMOVED******REMOVED******REMOVED*** 4194304 /* Truthy */ ? falseType : facts ***REMOVED******REMOVED******REMOVED*** 8388608 /* Falsy */ ? trueType : booleanType;
      case 46 /* PlusPlusToken */:
      case 47 /* MinusMinusToken */:
        const ok ***REMOVED*** checkArithmeticOperandType(node.operand, checkNonNullType(operandType, node.operand), Diagnostics.An_arithmetic_operand_must_be_of_type_any_number_bigint_or_an_enum_type);
        if (ok) {
          checkReferenceExpression(
            node.operand,
            Diagnostics.The_operand_of_an_increment_or_decrement_operator_must_be_a_variable_or_a_property_access,
            Diagnostics.The_operand_of_an_increment_or_decrement_operator_may_not_be_an_optional_property_access
          );
        }
        return getUnaryResultType(operandType);
    }
    return errorType;
  }
  function checkPostfixUnaryExpression(node) {
    const operandType ***REMOVED*** checkExpression(node.operand);
    if (operandType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverType;
    }
    const ok ***REMOVED*** checkArithmeticOperandType(
      node.operand,
      checkNonNullType(operandType, node.operand),
      Diagnostics.An_arithmetic_operand_must_be_of_type_any_number_bigint_or_an_enum_type
    );
    if (ok) {
      checkReferenceExpression(
        node.operand,
        Diagnostics.The_operand_of_an_increment_or_decrement_operator_must_be_a_variable_or_a_property_access,
        Diagnostics.The_operand_of_an_increment_or_decrement_operator_may_not_be_an_optional_property_access
      );
    }
    return getUnaryResultType(operandType);
  }
  function getUnaryResultType(operandType) {
    if (maybeTypeOfKind(operandType, 2112 /* BigIntLike */)) {
      return isTypeAssignableToKind(operandType, 3 /* AnyOrUnknown */) || maybeTypeOfKind(operandType, 296 /* NumberLike */) ? numberOrBigIntType : bigintType;
    }
    return numberType;
  }
  function maybeTypeOfKindConsideringBaseConstraint(type, kind) {
    if (maybeTypeOfKind(type, kind)) {
      return true;
    }
    const baseConstraint ***REMOVED*** getBaseConstraintOrType(type);
    return !!baseConstraint && maybeTypeOfKind(baseConstraint, kind);
  }
  function maybeTypeOfKind(type, kind) {
    if (type.flags & kind) {
      return true;
    }
    if (type.flags & 3145728 /* UnionOrIntersection */) {
      const types ***REMOVED*** type.types;
      for (const t of types) {
        if (maybeTypeOfKind(t, kind)) {
          return true;
        }
      }
    }
    return false;
  }
  function isTypeAssignableToKind(source, kind, strict) {
    if (source.flags & kind) {
      return true;
    }
    if (strict && source.flags & (3 /* AnyOrUnknown */ | 16384 /* Void */ | 32768 /* Undefined */ | 65536 /* Null */)) {
      return false;
    }
    return !!(kind & 296 /* NumberLike */) && isTypeAssignableTo(source, numberType) || !!(kind & 2112 /* BigIntLike */) && isTypeAssignableTo(source, bigintType) || !!(kind & 402653316 /* StringLike */) && isTypeAssignableTo(source, stringType) || !!(kind & 528 /* BooleanLike */) && isTypeAssignableTo(source, booleanType) || !!(kind & 16384 /* Void */) && isTypeAssignableTo(source, voidType) || !!(kind & 131072 /* Never */) && isTypeAssignableTo(source, neverType) || !!(kind & 65536 /* Null */) && isTypeAssignableTo(source, nullType) || !!(kind & 32768 /* Undefined */) && isTypeAssignableTo(source, undefinedType) || !!(kind & 4096 /* ESSymbol */) && isTypeAssignableTo(source, esSymbolType) || !!(kind & 67108864 /* NonPrimitive */) && isTypeAssignableTo(source, nonPrimitiveType);
  }
  function allTypesAssignableToKind(source, kind, strict) {
    return source.flags & 1048576 /* Union */ ? every(source.types, (subType) ***REMOVED***> allTypesAssignableToKind(subType, kind, strict)) : isTypeAssignableToKind(source, kind, strict);
  }
  function isConstEnumObjectType(type) {
    return !!(getObjectFlags(type) & 16 /* Anonymous */) && !!type.symbol && isConstEnumSymbol(type.symbol);
  }
  function isConstEnumSymbol(symbol) {
    return (symbol.flags & 128 /* ConstEnum */) !***REMOVED******REMOVED*** 0;
  }
  function getSymbolHasInstanceMethodOfObjectType(type) {
    const hasInstancePropertyName ***REMOVED*** getPropertyNameForKnownSymbolName("hasInstance");
    if (allTypesAssignableToKind(type, 67108864 /* NonPrimitive */)) {
      const hasInstanceProperty ***REMOVED*** getPropertyOfType(type, hasInstancePropertyName);
      if (hasInstanceProperty) {
        const hasInstancePropertyType ***REMOVED*** getTypeOfSymbol(hasInstanceProperty);
        if (hasInstancePropertyType && getSignaturesOfType(hasInstancePropertyType, 0 /* Call */).length !***REMOVED******REMOVED*** 0) {
          return hasInstancePropertyType;
        }
      }
    }
  }
  function checkInstanceOfExpression(left, right, leftType, rightType, checkMode) {
    if (leftType ***REMOVED******REMOVED******REMOVED*** silentNeverType || rightType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverType;
    }
    if (!isTypeAny(leftType) && allTypesAssignableToKind(leftType, 402784252 /* Primitive */)) {
      error(left, Diagnostics.The_left_hand_side_of_an_instanceof_expression_must_be_of_type_any_an_object_type_or_a_type_parameter);
    }
    Debug.assert(isInstanceOfExpression(left.parent));
    const signature ***REMOVED*** getResolvedSignature(
      left.parent,
      /*candidatesOutArray*/
      void 0,
      checkMode
    );
    if (signature ***REMOVED******REMOVED******REMOVED*** resolvingSignature) {
      return silentNeverType;
    }
    const returnType ***REMOVED*** getReturnTypeOfSignature(signature);
    checkTypeAssignableTo(returnType, booleanType, right, Diagnostics.An_object_s_Symbol_hasInstance_method_must_return_a_boolean_value_for_it_to_be_used_on_the_right_hand_side_of_an_instanceof_expression);
    return booleanType;
  }
  function hasEmptyObjectIntersection(type) {
    return someType(type, (t) ***REMOVED***> t ***REMOVED******REMOVED******REMOVED*** unknownEmptyObjectType || !!(t.flags & 2097152 /* Intersection */) && isEmptyAnonymousObjectType(getBaseConstraintOrType(t)));
  }
  function checkInExpression(left, right, leftType, rightType) {
    if (leftType ***REMOVED******REMOVED******REMOVED*** silentNeverType || rightType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverType;
    }
    if (isPrivateIdentifier(left)) {
      if (languageVersion < LanguageFeatureMinimumTarget.PrivateNamesAndClassStaticBlocks || languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators || !useDefineForClassFields) {
        checkExternalEmitHelpers(left, 2097152 /* ClassPrivateFieldIn */);
      }
      if (!getNodeLinks(left).resolvedSymbol && getContainingClass(left)) {
        const isUncheckedJS ***REMOVED*** isUncheckedJSSuggestion(
          left,
          rightType.symbol,
          /*excludeClasses*/
          true
        );
        reportNonexistentProperty(left, rightType, isUncheckedJS);
      }
    } else {
      checkTypeAssignableTo(checkNonNullType(leftType, left), stringNumberSymbolType, left);
    }
    if (checkTypeAssignableTo(checkNonNullType(rightType, right), nonPrimitiveType, right)) {
      if (hasEmptyObjectIntersection(rightType)) {
        error(right, Diagnostics.Type_0_may_represent_a_primitive_value_which_is_not_permitted_as_the_right_operand_of_the_in_operator, typeToString(rightType));
      }
    }
    return booleanType;
  }
  function checkObjectLiteralAssignment(node, sourceType, rightIsThis) {
    const properties ***REMOVED*** node.properties;
    if (strictNullChecks && properties.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return checkNonNullType(sourceType, node);
    }
    for (let i ***REMOVED*** 0; i < properties.length; i++) {
      checkObjectLiteralDestructuringPropertyAssignment(node, sourceType, i, properties, rightIsThis);
    }
    return sourceType;
  }
  function checkObjectLiteralDestructuringPropertyAssignment(node, objectLiteralType, propertyIndex, allProperties, rightIsThis ***REMOVED*** false) {
    const properties ***REMOVED*** node.properties;
    const property ***REMOVED*** properties[propertyIndex];
    if (property.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ || property.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
      const name ***REMOVED*** property.name;
      const exprType ***REMOVED*** getLiteralTypeFromPropertyName(name);
      if (isTypeUsableAsPropertyName(exprType)) {
        const text ***REMOVED*** getPropertyNameFromType(exprType);
        const prop ***REMOVED*** getPropertyOfType(objectLiteralType, text);
        if (prop) {
          markPropertyAsReferenced(prop, property, rightIsThis);
          checkPropertyAccessibility(
            property,
            /*isSuper*/
            false,
            /*writing*/
            true,
            objectLiteralType,
            prop
          );
        }
      }
      const elementType ***REMOVED*** getIndexedAccessType(objectLiteralType, exprType, 32 /* ExpressionPosition */ | (hasDefaultValue(property) ? 16 /* AllowMissing */ : 0), name);
      const type ***REMOVED*** getFlowTypeOfDestructuring(property, elementType);
      return checkDestructuringAssignment(property.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ ? property : property.initializer, type);
    } else if (property.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
      if (propertyIndex < properties.length - 1) {
        error(property, Diagnostics.A_rest_element_must_be_last_in_a_destructuring_pattern);
      } else {
        if (languageVersion < LanguageFeatureMinimumTarget.ObjectSpreadRest) {
          checkExternalEmitHelpers(property, 4 /* Rest */);
        }
        const nonRestNames ***REMOVED*** [];
        if (allProperties) {
          for (const otherProperty of allProperties) {
            if (!isSpreadAssignment(otherProperty)) {
              nonRestNames.push(otherProperty.name);
            }
          }
        }
        const type ***REMOVED*** getRestType(objectLiteralType, nonRestNames, objectLiteralType.symbol);
        checkGrammarForDisallowedTrailingComma(allProperties, Diagnostics.A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma);
        return checkDestructuringAssignment(property.expression, type);
      }
    } else {
      error(property, Diagnostics.Property_assignment_expected);
    }
  }
  function checkArrayLiteralAssignment(node, sourceType, checkMode) {
    const elements ***REMOVED*** node.elements;
    if (languageVersion < LanguageFeatureMinimumTarget.DestructuringAssignment && compilerOptions.downlevelIteration) {
      checkExternalEmitHelpers(node, 512 /* Read */);
    }
    const possiblyOutOfBoundsType ***REMOVED*** checkIteratedTypeOrElementType(65 /* Destructuring */ | 128 /* PossiblyOutOfBounds */, sourceType, undefinedType, node) || errorType;
    let inBoundsType ***REMOVED*** compilerOptions.noUncheckedIndexedAccess ? void 0 : possiblyOutOfBoundsType;
    for (let i ***REMOVED*** 0; i < elements.length; i++) {
      let type ***REMOVED*** possiblyOutOfBoundsType;
      if (node.elements[i].kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */) {
        type ***REMOVED*** inBoundsType ***REMOVED*** inBoundsType ?? (checkIteratedTypeOrElementType(65 /* Destructuring */, sourceType, undefinedType, node) || errorType);
      }
      checkArrayLiteralDestructuringElementAssignment(node, sourceType, i, type, checkMode);
    }
    return sourceType;
  }
  function checkArrayLiteralDestructuringElementAssignment(node, sourceType, elementIndex, elementType, checkMode) {
    const elements ***REMOVED*** node.elements;
    const element ***REMOVED*** elements[elementIndex];
    if (element.kind !***REMOVED******REMOVED*** 233 /* OmittedExpression */) {
      if (element.kind !***REMOVED******REMOVED*** 231 /* SpreadElement */) {
        const indexType ***REMOVED*** getNumberLiteralType(elementIndex);
        if (isArrayLikeType(sourceType)) {
          const accessFlags ***REMOVED*** 32 /* ExpressionPosition */ | (hasDefaultValue(element) ? 16 /* AllowMissing */ : 0);
          const elementType2 ***REMOVED*** getIndexedAccessTypeOrUndefined(sourceType, indexType, accessFlags, createSyntheticExpression(element, indexType)) || errorType;
          const assignedType ***REMOVED*** hasDefaultValue(element) ? getTypeWithFacts(elementType2, 524288 /* NEUndefined */) : elementType2;
          const type ***REMOVED*** getFlowTypeOfDestructuring(element, assignedType);
          return checkDestructuringAssignment(element, type, checkMode);
        }
        return checkDestructuringAssignment(element, elementType, checkMode);
      }
      if (elementIndex < elements.length - 1) {
        error(element, Diagnostics.A_rest_element_must_be_last_in_a_destructuring_pattern);
      } else {
        const restExpression ***REMOVED*** element.expression;
        if (restExpression.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && restExpression.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
          error(restExpression.operatorToken, Diagnostics.A_rest_element_cannot_have_an_initializer);
        } else {
          checkGrammarForDisallowedTrailingComma(node.elements, Diagnostics.A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma);
          const type ***REMOVED*** everyType(sourceType, isTupleType) ? mapType(sourceType, (t) ***REMOVED***> sliceTupleType(t, elementIndex)) : createArrayType(elementType);
          return checkDestructuringAssignment(restExpression, type, checkMode);
        }
      }
    }
    return void 0;
  }
  function checkDestructuringAssignment(exprOrAssignment, sourceType, checkMode, rightIsThis) {
    let target;
    if (exprOrAssignment.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
      const prop ***REMOVED*** exprOrAssignment;
      if (prop.objectAssignmentInitializer) {
        if (strictNullChecks && !hasTypeFacts(checkExpression(prop.objectAssignmentInitializer), 16777216 /* IsUndefined */)) {
          sourceType ***REMOVED*** getTypeWithFacts(sourceType, 524288 /* NEUndefined */);
        }
        checkBinaryLikeExpression(prop.name, prop.equalsToken, prop.objectAssignmentInitializer, checkMode);
      }
      target ***REMOVED*** exprOrAssignment.name;
    } else {
      target ***REMOVED*** exprOrAssignment;
    }
    if (target.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && target.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */) {
      checkBinaryExpression(target, checkMode);
      target ***REMOVED*** target.left;
      if (strictNullChecks) {
        sourceType ***REMOVED*** getTypeWithFacts(sourceType, 524288 /* NEUndefined */);
      }
    }
    if (target.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
      return checkObjectLiteralAssignment(target, sourceType, rightIsThis);
    }
    if (target.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */) {
      return checkArrayLiteralAssignment(target, sourceType, checkMode);
    }
    return checkReferenceAssignment(target, sourceType, checkMode);
  }
  function checkReferenceAssignment(target, sourceType, checkMode) {
    const targetType ***REMOVED*** checkExpression(target, checkMode);
    const error2 ***REMOVED*** target.parent.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */ ? Diagnostics.The_target_of_an_object_rest_assignment_must_be_a_variable_or_a_property_access : Diagnostics.The_left_hand_side_of_an_assignment_expression_must_be_a_variable_or_a_property_access;
    const optionalError ***REMOVED*** target.parent.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */ ? Diagnostics.The_target_of_an_object_rest_assignment_may_not_be_an_optional_property_access : Diagnostics.The_left_hand_side_of_an_assignment_expression_may_not_be_an_optional_property_access;
    if (checkReferenceExpression(target, error2, optionalError)) {
      checkTypeAssignableToAndOptionallyElaborate(sourceType, targetType, target, target);
    }
    if (isPrivateIdentifierPropertyAccessExpression(target)) {
      checkExternalEmitHelpers(target.parent, 1048576 /* ClassPrivateFieldSet */);
    }
    return sourceType;
  }
  function isSideEffectFree(node) {
    node ***REMOVED*** skipParentheses(node);
    switch (node.kind) {
      case 80 /* Identifier */:
      case 11 /* StringLiteral */:
      case 14 /* RegularExpressionLiteral */:
      case 216 /* TaggedTemplateExpression */:
      case 229 /* TemplateExpression */:
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 9 /* NumericLiteral */:
      case 10 /* BigIntLiteral */:
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
      case 106 /* NullKeyword */:
      case 157 /* UndefinedKeyword */:
      case 219 /* FunctionExpression */:
      case 232 /* ClassExpression */:
      case 220 /* ArrowFunction */:
      case 210 /* ArrayLiteralExpression */:
      case 211 /* ObjectLiteralExpression */:
      case 222 /* TypeOfExpression */:
      case 236 /* NonNullExpression */:
      case 286 /* JsxSelfClosingElement */:
      case 285 /* JsxElement */:
        return true;
      case 228 /* ConditionalExpression */:
        return isSideEffectFree(node.whenTrue) && isSideEffectFree(node.whenFalse);
      case 227 /* BinaryExpression */:
        if (isAssignmentOperator(node.operatorToken.kind)) {
          return false;
        }
        return isSideEffectFree(node.left) && isSideEffectFree(node.right);
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        switch (node.operator) {
          case 54 /* ExclamationToken */:
          case 40 /* PlusToken */:
          case 41 /* MinusToken */:
          case 55 /* TildeToken */:
            return true;
        }
        return false;
      // Some forms listed here for clarity
      case 223 /* VoidExpression */:
      // Explicit opt-out
      case 217 /* TypeAssertionExpression */:
      // Not SEF, but can produce useful type warnings
      case 235 /* AsExpression */:
      // Not SEF, but can produce useful type warnings
      default:
        return false;
    }
  }
  function isTypeEqualityComparableTo(source, target) {
    return (target.flags & 98304 /* Nullable */) !***REMOVED******REMOVED*** 0 || isTypeComparableTo(source, target);
  }
  function createCheckBinaryExpression() {
    const trampoline ***REMOVED*** createBinaryExpressionTrampoline(onEnter, onLeft, onOperator, onRight, onExit, foldState);
    return (node, checkMode) ***REMOVED***> {
      const result ***REMOVED*** trampoline(node, checkMode);
      Debug.assertIsDefined(result);
      return result;
    };
    function onEnter(node, state, checkMode) {
      if (state) {
        state.stackIndex++;
        state.skip ***REMOVED*** false;
        setLeftType(
          state,
          /*type*/
          void 0
        );
        setLastResult(
          state,
          /*type*/
          void 0
        );
      } else {
        state ***REMOVED*** {
          checkMode,
          skip: false,
          stackIndex: 0,
          typeStack: [void 0, void 0]
        };
      }
      if (isInJSFile(node) && getAssignedExpandoInitializer(node)) {
        state.skip ***REMOVED*** true;
        setLastResult(state, checkExpression(node.right, checkMode));
        return state;
      }
      checkNullishCoalesceOperands(node);
      const operator ***REMOVED*** node.operatorToken.kind;
      if (operator ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && (node.left.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || node.left.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */)) {
        state.skip ***REMOVED*** true;
        setLastResult(state, checkDestructuringAssignment(node.left, checkExpression(node.right, checkMode), checkMode, node.right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */));
        return state;
      }
      return state;
    }
    function onLeft(left, state, _node) {
      if (!state.skip) {
        return maybeCheckExpression(state, left);
      }
    }
    function onOperator(operatorToken, state, node) {
      if (!state.skip) {
        const leftType ***REMOVED*** getLastResult(state);
        Debug.assertIsDefined(leftType);
        setLeftType(state, leftType);
        setLastResult(
          state,
          /*type*/
          void 0
        );
        const operator ***REMOVED*** operatorToken.kind;
        if (isLogicalOrCoalescingBinaryOperator(operator)) {
          let parent ***REMOVED*** node.parent;
          while (parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ || isLogicalOrCoalescingBinaryExpression(parent)) {
            parent ***REMOVED*** parent.parent;
          }
          if (operator ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ || isIfStatement(parent)) {
            checkTestingKnownTruthyCallableOrAwaitableOrEnumMemberType(node.left, leftType, isIfStatement(parent) ? parent.thenStatement : void 0);
          }
          if (isBinaryLogicalOperator(operator)) {
            checkTruthinessOfType(leftType, node.left);
          }
        }
      }
    }
    function onRight(right, state, _node) {
      if (!state.skip) {
        return maybeCheckExpression(state, right);
      }
    }
    function onExit(node, state) {
      let result;
      if (state.skip) {
        result ***REMOVED*** getLastResult(state);
      } else {
        const leftType ***REMOVED*** getLeftType(state);
        Debug.assertIsDefined(leftType);
        const rightType ***REMOVED*** getLastResult(state);
        Debug.assertIsDefined(rightType);
        result ***REMOVED*** checkBinaryLikeExpressionWorker(node.left, node.operatorToken, node.right, leftType, rightType, state.checkMode, node);
      }
      state.skip ***REMOVED*** false;
      setLeftType(
        state,
        /*type*/
        void 0
      );
      setLastResult(
        state,
        /*type*/
        void 0
      );
      state.stackIndex--;
      return result;
    }
    function foldState(state, result, _side) {
      setLastResult(state, result);
      return state;
    }
    function maybeCheckExpression(state, node) {
      if (isBinaryExpression(node)) {
        return node;
      }
      setLastResult(state, checkExpression(node, state.checkMode));
    }
    function getLeftType(state) {
      return state.typeStack[state.stackIndex];
    }
    function setLeftType(state, type) {
      state.typeStack[state.stackIndex] ***REMOVED*** type;
    }
    function getLastResult(state) {
      return state.typeStack[state.stackIndex + 1];
    }
    function setLastResult(state, type) {
      state.typeStack[state.stackIndex + 1] ***REMOVED*** type;
    }
  }
  function checkNullishCoalesceOperands(node) {
    if (node.operatorToken.kind !***REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
      return;
    }
    if (isBinaryExpression(node.parent)) {
      const { left, operatorToken } ***REMOVED*** node.parent;
      if (isBinaryExpression(left) && operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */) {
        grammarErrorOnNode(left, Diagnostics._0_and_1_operations_cannot_be_mixed_without_parentheses, tokenToString(61 /* QuestionQuestionToken */), tokenToString(operatorToken.kind));
      }
    } else if (isBinaryExpression(node.left)) {
      const { operatorToken } ***REMOVED*** node.left;
      if (operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */) {
        grammarErrorOnNode(node.left, Diagnostics._0_and_1_operations_cannot_be_mixed_without_parentheses, tokenToString(operatorToken.kind), tokenToString(61 /* QuestionQuestionToken */));
      }
    } else if (isBinaryExpression(node.right)) {
      const { operatorToken } ***REMOVED*** node.right;
      if (operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */) {
        grammarErrorOnNode(node.right, Diagnostics._0_and_1_operations_cannot_be_mixed_without_parentheses, tokenToString(61 /* QuestionQuestionToken */), tokenToString(operatorToken.kind));
      }
    }
    checkNullishCoalesceOperandLeft(node);
    checkNullishCoalesceOperandRight(node);
  }
  function checkNullishCoalesceOperandLeft(node) {
    const leftTarget ***REMOVED*** skipOuterExpressions(node.left, 63 /* All */);
    const nullishSemantics ***REMOVED*** getSyntacticNullishnessSemantics(leftTarget);
    if (nullishSemantics !***REMOVED******REMOVED*** 3 /* Sometimes */) {
      if (nullishSemantics ***REMOVED******REMOVED******REMOVED*** 1 /* Always */) {
        error(leftTarget, Diagnostics.This_expression_is_always_nullish);
      } else {
        error(leftTarget, Diagnostics.Right_operand_of_is_unreachable_because_the_left_operand_is_never_nullish);
      }
    }
  }
  function checkNullishCoalesceOperandRight(node) {
    const rightTarget ***REMOVED*** skipOuterExpressions(node.right, 63 /* All */);
    const nullishSemantics ***REMOVED*** getSyntacticNullishnessSemantics(rightTarget);
    if (isNotWithinNullishCoalesceExpression(node)) {
      return;
    }
    if (nullishSemantics ***REMOVED******REMOVED******REMOVED*** 1 /* Always */) {
      error(rightTarget, Diagnostics.This_expression_is_always_nullish);
    } else if (nullishSemantics ***REMOVED******REMOVED******REMOVED*** 2 /* Never */) {
      error(rightTarget, Diagnostics.This_expression_is_never_nullish);
    }
  }
  function isNotWithinNullishCoalesceExpression(node) {
    return !isBinaryExpression(node.parent) || node.parent.operatorToken.kind !***REMOVED******REMOVED*** 61 /* QuestionQuestionToken */;
  }
  function getSyntacticNullishnessSemantics(node) {
    node ***REMOVED*** skipOuterExpressions(node);
    switch (node.kind) {
      case 224 /* AwaitExpression */:
      case 214 /* CallExpression */:
      case 216 /* TaggedTemplateExpression */:
      case 213 /* ElementAccessExpression */:
      case 237 /* MetaProperty */:
      case 215 /* NewExpression */:
      case 212 /* PropertyAccessExpression */:
      case 230 /* YieldExpression */:
      case 110 /* ThisKeyword */:
        return 3 /* Sometimes */;
      case 227 /* BinaryExpression */:
        switch (node.operatorToken.kind) {
          case 64 /* EqualsToken */:
          case 61 /* QuestionQuestionToken */:
          case 78 /* QuestionQuestionEqualsToken */:
          case 57 /* BarBarToken */:
          case 76 /* BarBarEqualsToken */:
          case 56 /* AmpersandAmpersandToken */:
          case 77 /* AmpersandAmpersandEqualsToken */:
            return 3 /* Sometimes */;
          case 28 /* CommaToken */:
            return getSyntacticNullishnessSemantics(node.right);
        }
        return 2 /* Never */;
      case 228 /* ConditionalExpression */:
        return getSyntacticNullishnessSemantics(node.whenTrue) | getSyntacticNullishnessSemantics(node.whenFalse);
      case 106 /* NullKeyword */:
        return 1 /* Always */;
      case 80 /* Identifier */:
        if (getResolvedSymbol(node) ***REMOVED******REMOVED******REMOVED*** undefinedSymbol) {
          return 1 /* Always */;
        }
        return 3 /* Sometimes */;
    }
    return 2 /* Never */;
  }
  function checkBinaryLikeExpression(left, operatorToken, right, checkMode, errorNode) {
    const operator ***REMOVED*** operatorToken.kind;
    if (operator ***REMOVED******REMOVED******REMOVED*** 64 /* EqualsToken */ && (left.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || left.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */)) {
      return checkDestructuringAssignment(left, checkExpression(right, checkMode), checkMode, right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */);
    }
    let leftType;
    if (isBinaryLogicalOperator(operator)) {
      leftType ***REMOVED*** checkTruthinessExpression(left, checkMode);
    } else {
      leftType ***REMOVED*** checkExpression(left, checkMode);
    }
    const rightType ***REMOVED*** checkExpression(right, checkMode);
    return checkBinaryLikeExpressionWorker(left, operatorToken, right, leftType, rightType, checkMode, errorNode);
  }
  function checkBinaryLikeExpressionWorker(left, operatorToken, right, leftType, rightType, checkMode, errorNode) {
    const operator ***REMOVED*** operatorToken.kind;
    switch (operator) {
      case 42 /* AsteriskToken */:
      case 43 /* AsteriskAsteriskToken */:
      case 67 /* AsteriskEqualsToken */:
      case 68 /* AsteriskAsteriskEqualsToken */:
      case 44 /* SlashToken */:
      case 69 /* SlashEqualsToken */:
      case 45 /* PercentToken */:
      case 70 /* PercentEqualsToken */:
      case 41 /* MinusToken */:
      case 66 /* MinusEqualsToken */:
      case 48 /* LessThanLessThanToken */:
      case 71 /* LessThanLessThanEqualsToken */:
      case 49 /* GreaterThanGreaterThanToken */:
      case 72 /* GreaterThanGreaterThanEqualsToken */:
      case 50 /* GreaterThanGreaterThanGreaterThanToken */:
      case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
      case 52 /* BarToken */:
      case 75 /* BarEqualsToken */:
      case 53 /* CaretToken */:
      case 79 /* CaretEqualsToken */:
      case 51 /* AmpersandToken */:
      case 74 /* AmpersandEqualsToken */:
        if (leftType ***REMOVED******REMOVED******REMOVED*** silentNeverType || rightType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
          return silentNeverType;
        }
        leftType ***REMOVED*** checkNonNullType(leftType, left);
        rightType ***REMOVED*** checkNonNullType(rightType, right);
        let suggestedOperator;
        if (leftType.flags & 528 /* BooleanLike */ && rightType.flags & 528 /* BooleanLike */ && (suggestedOperator ***REMOVED*** getSuggestedBooleanOperator(operatorToken.kind)) !***REMOVED******REMOVED*** void 0) {
          error(errorNode || operatorToken, Diagnostics.The_0_operator_is_not_allowed_for_boolean_types_Consider_using_1_instead, tokenToString(operatorToken.kind), tokenToString(suggestedOperator));
          return numberType;
        } else {
          const leftOk ***REMOVED*** checkArithmeticOperandType(
            left,
            leftType,
            Diagnostics.The_left_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type,
            /*isAwaitValid*/
            true
          );
          const rightOk ***REMOVED*** checkArithmeticOperandType(
            right,
            rightType,
            Diagnostics.The_right_hand_side_of_an_arithmetic_operation_must_be_of_type_any_number_bigint_or_an_enum_type,
            /*isAwaitValid*/
            true
          );
          let resultType2;
          if (isTypeAssignableToKind(leftType, 3 /* AnyOrUnknown */) && isTypeAssignableToKind(rightType, 3 /* AnyOrUnknown */) || // Or, if neither could be bigint, implicit coercion results in a number result
          !(maybeTypeOfKind(leftType, 2112 /* BigIntLike */) || maybeTypeOfKind(rightType, 2112 /* BigIntLike */))) {
            resultType2 ***REMOVED*** numberType;
          } else if (bothAreBigIntLike(leftType, rightType)) {
            switch (operator) {
              case 50 /* GreaterThanGreaterThanGreaterThanToken */:
              case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
                reportOperatorError();
                break;
              case 43 /* AsteriskAsteriskToken */:
              case 68 /* AsteriskAsteriskEqualsToken */:
                if (languageVersion < 3 /* ES2016 */) {
                  error(errorNode, Diagnostics.Exponentiation_cannot_be_performed_on_bigint_values_unless_the_target_option_is_set_to_es2016_or_later);
                }
            }
            resultType2 ***REMOVED*** bigintType;
          } else {
            reportOperatorError(bothAreBigIntLike);
            resultType2 ***REMOVED*** errorType;
          }
          if (leftOk && rightOk) {
            checkAssignmentOperator(resultType2);
            switch (operator) {
              case 48 /* LessThanLessThanToken */:
              case 71 /* LessThanLessThanEqualsToken */:
              case 49 /* GreaterThanGreaterThanToken */:
              case 72 /* GreaterThanGreaterThanEqualsToken */:
              case 50 /* GreaterThanGreaterThanGreaterThanToken */:
              case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
                const rhsEval ***REMOVED*** evaluate(right);
                if (typeof rhsEval.value ***REMOVED******REMOVED******REMOVED*** "number" && Math.abs(rhsEval.value) >***REMOVED*** 32) {
                  errorOrSuggestion(
                    isEnumMember(walkUpParenthesizedExpressions(right.parent.parent)),
                    // elevate from suggestion to error within an enum member
                    errorNode || operatorToken,
                    Diagnostics.This_operation_can_be_simplified_This_shift_is_identical_to_0_1_2,
                    getTextOfNode(left),
                    tokenToString(operator),
                    rhsEval.value % 32
                  );
                }
                break;
              default:
                break;
            }
          }
          return resultType2;
        }
      case 40 /* PlusToken */:
      case 65 /* PlusEqualsToken */:
        if (leftType ***REMOVED******REMOVED******REMOVED*** silentNeverType || rightType ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
          return silentNeverType;
        }
        if (!isTypeAssignableToKind(leftType, 402653316 /* StringLike */) && !isTypeAssignableToKind(rightType, 402653316 /* StringLike */)) {
          leftType ***REMOVED*** checkNonNullType(leftType, left);
          rightType ***REMOVED*** checkNonNullType(rightType, right);
        }
        let resultType;
        if (isTypeAssignableToKind(
          leftType,
          296 /* NumberLike */,
          /*strict*/
          true
        ) && isTypeAssignableToKind(
          rightType,
          296 /* NumberLike */,
          /*strict*/
          true
        )) {
          resultType ***REMOVED*** numberType;
        } else if (isTypeAssignableToKind(
          leftType,
          2112 /* BigIntLike */,
          /*strict*/
          true
        ) && isTypeAssignableToKind(
          rightType,
          2112 /* BigIntLike */,
          /*strict*/
          true
        )) {
          resultType ***REMOVED*** bigintType;
        } else if (isTypeAssignableToKind(
          leftType,
          402653316 /* StringLike */,
          /*strict*/
          true
        ) || isTypeAssignableToKind(
          rightType,
          402653316 /* StringLike */,
          /*strict*/
          true
        )) {
          resultType ***REMOVED*** stringType;
        } else if (isTypeAny(leftType) || isTypeAny(rightType)) {
          resultType ***REMOVED*** isErrorType(leftType) || isErrorType(rightType) ? errorType : anyType;
        }
        if (resultType && !checkForDisallowedESSymbolOperand(operator)) {
          return resultType;
        }
        if (!resultType) {
          const closeEnoughKind ***REMOVED*** 296 /* NumberLike */ | 2112 /* BigIntLike */ | 402653316 /* StringLike */ | 3 /* AnyOrUnknown */;
          reportOperatorError(
            (left2, right2) ***REMOVED***> isTypeAssignableToKind(left2, closeEnoughKind) && isTypeAssignableToKind(right2, closeEnoughKind)
          );
          return anyType;
        }
        if (operator ***REMOVED******REMOVED******REMOVED*** 65 /* PlusEqualsToken */) {
          checkAssignmentOperator(resultType);
        }
        return resultType;
      case 30 /* LessThanToken */:
      case 32 /* GreaterThanToken */:
      case 33 /* LessThanEqualsToken */:
      case 34 /* GreaterThanEqualsToken */:
        if (checkForDisallowedESSymbolOperand(operator)) {
          leftType ***REMOVED*** getBaseTypeOfLiteralTypeForComparison(checkNonNullType(leftType, left));
          rightType ***REMOVED*** getBaseTypeOfLiteralTypeForComparison(checkNonNullType(rightType, right));
          reportOperatorErrorUnless((left2, right2) ***REMOVED***> {
            if (isTypeAny(left2) || isTypeAny(right2)) {
              return true;
            }
            const leftAssignableToNumber ***REMOVED*** isTypeAssignableTo(left2, numberOrBigIntType);
            const rightAssignableToNumber ***REMOVED*** isTypeAssignableTo(right2, numberOrBigIntType);
            return leftAssignableToNumber && rightAssignableToNumber || !leftAssignableToNumber && !rightAssignableToNumber && areTypesComparable(left2, right2);
          });
        }
        return booleanType;
      case 35 /* EqualsEqualsToken */:
      case 36 /* ExclamationEqualsToken */:
      case 37 /* EqualsEqualsEqualsToken */:
      case 38 /* ExclamationEqualsEqualsToken */:
        if (!(checkMode && checkMode & 64 /* TypeOnly */)) {
          if ((isLiteralExpressionOfObject(left) || isLiteralExpressionOfObject(right)) && // only report for ***REMOVED******REMOVED******REMOVED*** and !***REMOVED******REMOVED*** in JS, not ***REMOVED******REMOVED*** or !***REMOVED***
          (!isInJSFile(left) || (operator ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */))) {
            const eqType ***REMOVED*** operator ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ || operator ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */;
            error(errorNode, Diagnostics.This_condition_will_always_return_0_since_JavaScript_compares_objects_by_reference_not_value, eqType ? "false" : "true");
          }
          checkNaNEquality(errorNode, operator, left, right);
          reportOperatorErrorUnless((left2, right2) ***REMOVED***> isTypeEqualityComparableTo(left2, right2) || isTypeEqualityComparableTo(right2, left2));
        }
        return booleanType;
      case 104 /* InstanceOfKeyword */:
        return checkInstanceOfExpression(left, right, leftType, rightType, checkMode);
      case 103 /* InKeyword */:
        return checkInExpression(left, right, leftType, rightType);
      case 56 /* AmpersandAmpersandToken */:
      case 77 /* AmpersandAmpersandEqualsToken */: {
        const resultType2 ***REMOVED*** hasTypeFacts(leftType, 4194304 /* Truthy */) ? getUnionType([extractDefinitelyFalsyTypes(strictNullChecks ? leftType : getBaseTypeOfLiteralType(rightType)), rightType]) : leftType;
        if (operator ***REMOVED******REMOVED******REMOVED*** 77 /* AmpersandAmpersandEqualsToken */) {
          checkAssignmentOperator(rightType);
        }
        return resultType2;
      }
      case 57 /* BarBarToken */:
      case 76 /* BarBarEqualsToken */: {
        const resultType2 ***REMOVED*** hasTypeFacts(leftType, 8388608 /* Falsy */) ? getUnionType([getNonNullableType(removeDefinitelyFalsyTypes(leftType)), rightType], 2 /* Subtype */) : leftType;
        if (operator ***REMOVED******REMOVED******REMOVED*** 76 /* BarBarEqualsToken */) {
          checkAssignmentOperator(rightType);
        }
        return resultType2;
      }
      case 61 /* QuestionQuestionToken */:
      case 78 /* QuestionQuestionEqualsToken */: {
        const resultType2 ***REMOVED*** hasTypeFacts(leftType, 262144 /* EQUndefinedOrNull */) ? getUnionType([getNonNullableType(leftType), rightType], 2 /* Subtype */) : leftType;
        if (operator ***REMOVED******REMOVED******REMOVED*** 78 /* QuestionQuestionEqualsToken */) {
          checkAssignmentOperator(rightType);
        }
        return resultType2;
      }
      case 64 /* EqualsToken */:
        const declKind ***REMOVED*** isBinaryExpression(left.parent) ? getAssignmentDeclarationKind(left.parent) : 0 /* None */;
        checkAssignmentDeclaration(declKind, rightType);
        if (isAssignmentDeclaration2(declKind)) {
          if (!(rightType.flags & 524288 /* Object */) || declKind !***REMOVED******REMOVED*** 2 /* ModuleExports */ && declKind !***REMOVED******REMOVED*** 6 /* Prototype */ && !isEmptyObjectType(rightType) && !isFunctionObjectType(rightType) && !(getObjectFlags(rightType) & 1 /* Class */)) {
            checkAssignmentOperator(rightType);
          }
          return leftType;
        } else {
          checkAssignmentOperator(rightType);
          return rightType;
        }
      case 28 /* CommaToken */:
        if (!compilerOptions.allowUnreachableCode && isSideEffectFree(left) && !isIndirectCall(left.parent)) {
          const sf ***REMOVED*** getSourceFileOfNode(left);
          const sourceText ***REMOVED*** sf.text;
          const start ***REMOVED*** skipTrivia(sourceText, left.pos);
          const isInDiag2657 ***REMOVED*** sf.parseDiagnostics.some((diag2) ***REMOVED***> {
            if (diag2.code !***REMOVED******REMOVED*** Diagnostics.JSX_expressions_must_have_one_parent_element.code) return false;
            return textSpanContainsPosition(diag2, start);
          });
          if (!isInDiag2657) error(left, Diagnostics.Left_side_of_comma_operator_is_unused_and_has_no_side_effects);
        }
        return rightType;
      default:
        return Debug.fail();
    }
    function bothAreBigIntLike(left2, right2) {
      return isTypeAssignableToKind(left2, 2112 /* BigIntLike */) && isTypeAssignableToKind(right2, 2112 /* BigIntLike */);
    }
    function checkAssignmentDeclaration(kind, rightType2) {
      if (kind ***REMOVED******REMOVED******REMOVED*** 2 /* ModuleExports */) {
        for (const prop of getPropertiesOfObjectType(rightType2)) {
          const propType ***REMOVED*** getTypeOfSymbol(prop);
          if (propType.symbol && propType.symbol.flags & 32 /* Class */) {
            const name ***REMOVED*** prop.escapedName;
            const symbol ***REMOVED*** resolveName(
              prop.valueDeclaration,
              name,
              788968 /* Type */,
              /*nameNotFoundMessage*/
              void 0,
              /*isUse*/
              false
            );
            if ((symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.declarations) && symbol.declarations.some(isJSDocTypedefTag)) {
              addDuplicateDeclarationErrorsForSymbols(symbol, Diagnostics.Duplicate_identifier_0, unescapeLeadingUnderscores(name), prop);
              addDuplicateDeclarationErrorsForSymbols(prop, Diagnostics.Duplicate_identifier_0, unescapeLeadingUnderscores(name), symbol);
            }
          }
        }
      }
    }
    function isIndirectCall(node) {
      return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ && isNumericLiteral(node.left) && node.left.text ***REMOVED******REMOVED******REMOVED*** "0" && (isCallExpression(node.parent.parent) && node.parent.parent.expression ***REMOVED******REMOVED******REMOVED*** node.parent || node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */) && // special-case for "eval" because it's the only non-access case where an indirect call actually affects behavior.
      (isAccessExpression(node.right) || isIdentifier(node.right) && node.right.escapedText ***REMOVED******REMOVED******REMOVED*** "eval");
    }
    function checkForDisallowedESSymbolOperand(operator2) {
      const offendingSymbolOperand ***REMOVED*** maybeTypeOfKindConsideringBaseConstraint(leftType, 12288 /* ESSymbolLike */) ? left : maybeTypeOfKindConsideringBaseConstraint(rightType, 12288 /* ESSymbolLike */) ? right : void 0;
      if (offendingSymbolOperand) {
        error(offendingSymbolOperand, Diagnostics.The_0_operator_cannot_be_applied_to_type_symbol, tokenToString(operator2));
        return false;
      }
      return true;
    }
    function getSuggestedBooleanOperator(operator2) {
      switch (operator2) {
        case 52 /* BarToken */:
        case 75 /* BarEqualsToken */:
          return 57 /* BarBarToken */;
        case 53 /* CaretToken */:
        case 79 /* CaretEqualsToken */:
          return 38 /* ExclamationEqualsEqualsToken */;
        case 51 /* AmpersandToken */:
        case 74 /* AmpersandEqualsToken */:
          return 56 /* AmpersandAmpersandToken */;
        default:
          return void 0;
      }
    }
    function checkAssignmentOperator(valueType) {
      if (isAssignmentOperator(operator)) {
        addLazyDiagnostic(checkAssignmentOperatorWorker);
      }
      function checkAssignmentOperatorWorker() {
        let assigneeType ***REMOVED*** leftType;
        if (isCompoundAssignment(operatorToken.kind) && left.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
          assigneeType ***REMOVED*** checkPropertyAccessExpression(
            left,
            /*checkMode*/
            void 0,
            /*writeOnly*/
            true
          );
        }
        if (checkReferenceExpression(left, Diagnostics.The_left_hand_side_of_an_assignment_expression_must_be_a_variable_or_a_property_access, Diagnostics.The_left_hand_side_of_an_assignment_expression_may_not_be_an_optional_property_access)) {
          let headMessage;
          if (exactOptionalPropertyTypes && isPropertyAccessExpression(left) && maybeTypeOfKind(valueType, 32768 /* Undefined */)) {
            const target ***REMOVED*** getTypeOfPropertyOfType(getTypeOfExpression(left.expression), left.name.escapedText);
            if (isExactOptionalPropertyMismatch(valueType, target)) {
              headMessage ***REMOVED*** Diagnostics.Type_0_is_not_assignable_to_type_1_with_exactOptionalPropertyTypes_Colon_true_Consider_adding_undefined_to_the_type_of_the_target;
            }
          }
          checkTypeAssignableToAndOptionallyElaborate(valueType, assigneeType, left, right, headMessage);
        }
      }
    }
    function isAssignmentDeclaration2(kind) {
      var _a;
      switch (kind) {
        case 2 /* ModuleExports */:
          return true;
        case 1 /* ExportsProperty */:
        case 5 /* Property */:
        case 6 /* Prototype */:
        case 3 /* PrototypeProperty */:
        case 4 /* ThisProperty */:
          const symbol ***REMOVED*** getSymbolOfNode(left);
          const init ***REMOVED*** getAssignedExpandoInitializer(right);
          return !!init && isObjectLiteralExpression(init) && !!((_a ***REMOVED*** symbol ***REMOVED******REMOVED*** null ? void 0 : symbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.size);
        default:
          return false;
      }
    }
    function reportOperatorErrorUnless(typesAreCompatible) {
      if (!typesAreCompatible(leftType, rightType)) {
        reportOperatorError(typesAreCompatible);
        return true;
      }
      return false;
    }
    function reportOperatorError(isRelated) {
      let wouldWorkWithAwait ***REMOVED*** false;
      const errNode ***REMOVED*** errorNode || operatorToken;
      if (isRelated) {
        const awaitedLeftType ***REMOVED*** getAwaitedTypeNoAlias(leftType);
        const awaitedRightType ***REMOVED*** getAwaitedTypeNoAlias(rightType);
        wouldWorkWithAwait ***REMOVED*** !(awaitedLeftType ***REMOVED******REMOVED******REMOVED*** leftType && awaitedRightType ***REMOVED******REMOVED******REMOVED*** rightType) && !!(awaitedLeftType && awaitedRightType) && isRelated(awaitedLeftType, awaitedRightType);
      }
      let effectiveLeft ***REMOVED*** leftType;
      let effectiveRight ***REMOVED*** rightType;
      if (!wouldWorkWithAwait && isRelated) {
        [effectiveLeft, effectiveRight] ***REMOVED*** getBaseTypesIfUnrelated(leftType, rightType, isRelated);
      }
      const [leftStr, rightStr] ***REMOVED*** getTypeNamesForErrorDisplay(effectiveLeft, effectiveRight);
      if (!tryGiveBetterPrimaryError(errNode, wouldWorkWithAwait, leftStr, rightStr)) {
        errorAndMaybeSuggestAwait(
          errNode,
          wouldWorkWithAwait,
          Diagnostics.Operator_0_cannot_be_applied_to_types_1_and_2,
          tokenToString(operatorToken.kind),
          leftStr,
          rightStr
        );
      }
    }
    function tryGiveBetterPrimaryError(errNode, maybeMissingAwait, leftStr, rightStr) {
      switch (operatorToken.kind) {
        case 37 /* EqualsEqualsEqualsToken */:
        case 35 /* EqualsEqualsToken */:
        case 38 /* ExclamationEqualsEqualsToken */:
        case 36 /* ExclamationEqualsToken */:
          return errorAndMaybeSuggestAwait(
            errNode,
            maybeMissingAwait,
            Diagnostics.This_comparison_appears_to_be_unintentional_because_the_types_0_and_1_have_no_overlap,
            leftStr,
            rightStr
          );
        default:
          return void 0;
      }
    }
    function checkNaNEquality(errorNode2, operator2, left2, right2) {
      const isLeftNaN ***REMOVED*** isGlobalNaN(skipParentheses(left2));
      const isRightNaN ***REMOVED*** isGlobalNaN(skipParentheses(right2));
      if (isLeftNaN || isRightNaN) {
        const err ***REMOVED*** error(errorNode2, Diagnostics.This_condition_will_always_return_0, tokenToString(operator2 ***REMOVED******REMOVED******REMOVED*** 37 /* EqualsEqualsEqualsToken */ || operator2 ***REMOVED******REMOVED******REMOVED*** 35 /* EqualsEqualsToken */ ? 97 /* FalseKeyword */ : 112 /* TrueKeyword */));
        if (isLeftNaN && isRightNaN) return;
        const operatorString ***REMOVED*** operator2 ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */ || operator2 ***REMOVED******REMOVED******REMOVED*** 36 /* ExclamationEqualsToken */ ? tokenToString(54 /* ExclamationToken */) : "";
        const location ***REMOVED*** isLeftNaN ? right2 : left2;
        const expression ***REMOVED*** skipParentheses(location);
        addRelatedInfo(err, createDiagnosticForNode(location, Diagnostics.Did_you_mean_0, `${operatorString}Number.isNaN(${isEntityNameExpression(expression) ? entityNameToString(expression) : "..."})`));
      }
    }
    function isGlobalNaN(expr) {
      if (isIdentifier(expr) && expr.escapedText ***REMOVED******REMOVED******REMOVED*** "NaN") {
        const globalNaNSymbol ***REMOVED*** getGlobalNaNSymbol();
        return !!globalNaNSymbol && globalNaNSymbol ***REMOVED******REMOVED******REMOVED*** getResolvedSymbol(expr);
      }
      return false;
    }
  }
  function getBaseTypesIfUnrelated(leftType, rightType, isRelated) {
    let effectiveLeft ***REMOVED*** leftType;
    let effectiveRight ***REMOVED*** rightType;
    const leftBase ***REMOVED*** getBaseTypeOfLiteralType(leftType);
    const rightBase ***REMOVED*** getBaseTypeOfLiteralType(rightType);
    if (!isRelated(leftBase, rightBase)) {
      effectiveLeft ***REMOVED*** leftBase;
      effectiveRight ***REMOVED*** rightBase;
    }
    return [effectiveLeft, effectiveRight];
  }
  function checkYieldExpression(node) {
    addLazyDiagnostic(checkYieldExpressionGrammar);
    const func ***REMOVED*** getContainingFunction(node);
    if (!func) return anyType;
    const functionFlags ***REMOVED*** getFunctionFlags(func);
    if (!(functionFlags & 1 /* Generator */)) {
      return anyType;
    }
    const isAsync ***REMOVED*** (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0;
    if (node.asteriskToken) {
      if (isAsync && languageVersion < LanguageFeatureMinimumTarget.AsyncGenerators) {
        checkExternalEmitHelpers(node, 26624 /* AsyncDelegatorIncludes */);
      }
      if (!isAsync && languageVersion < LanguageFeatureMinimumTarget.Generators && compilerOptions.downlevelIteration) {
        checkExternalEmitHelpers(node, 256 /* Values */);
      }
    }
    let returnType ***REMOVED*** getReturnTypeFromAnnotation(func);
    if (returnType && returnType.flags & 1048576 /* Union */) {
      returnType ***REMOVED*** filterType(returnType, (t) ***REMOVED***> checkGeneratorInstantiationAssignabilityToReturnType(
        t,
        functionFlags,
        /*errorNode*/
        void 0
      ));
    }
    const iterationTypes ***REMOVED*** returnType && getIterationTypesOfGeneratorFunctionReturnType(returnType, isAsync);
    const signatureYieldType ***REMOVED*** iterationTypes && iterationTypes.yieldType || anyType;
    const signatureNextType ***REMOVED*** iterationTypes && iterationTypes.nextType || anyType;
    const yieldExpressionType ***REMOVED*** node.expression ? checkExpression(node.expression) : undefinedWideningType;
    const yieldedType ***REMOVED*** getYieldedTypeOfYieldExpression(node, yieldExpressionType, signatureNextType, isAsync);
    if (returnType && yieldedType) {
      checkTypeAssignableToAndOptionallyElaborate(yieldedType, signatureYieldType, node.expression || node, node.expression);
    }
    if (node.asteriskToken) {
      const use ***REMOVED*** isAsync ? 19 /* AsyncYieldStar */ : 17 /* YieldStar */;
      return getIterationTypeOfIterable(use, 1 /* Return */, yieldExpressionType, node.expression) || anyType;
    } else if (returnType) {
      return getIterationTypeOfGeneratorFunctionReturnType(2 /* Next */, returnType, isAsync) || anyType;
    }
    let type ***REMOVED*** getContextualIterationType(2 /* Next */, func);
    if (!type) {
      type ***REMOVED*** anyType;
      addLazyDiagnostic(() ***REMOVED***> {
        if (noImplicitAny && !expressionResultIsUnused(node)) {
          const contextualType ***REMOVED*** getContextualType(
            node,
            /*contextFlags*/
            void 0
          );
          if (!contextualType || isTypeAny(contextualType)) {
            error(node, Diagnostics.yield_expression_implicitly_results_in_an_any_type_because_its_containing_generator_lacks_a_return_type_annotation);
          }
        }
      });
    }
    return type;
    function checkYieldExpressionGrammar() {
      if (!(node.flags & 16384 /* YieldContext */)) {
        grammarErrorOnFirstToken(node, Diagnostics.A_yield_expression_is_only_allowed_in_a_generator_body);
      }
      if (isInParameterInitializerBeforeContainingFunction(node)) {
        error(node, Diagnostics.yield_expressions_cannot_be_used_in_a_parameter_initializer);
      }
    }
  }
  function checkConditionalExpression(node, checkMode) {
    const type ***REMOVED*** checkTruthinessExpression(node.condition, checkMode);
    checkTestingKnownTruthyCallableOrAwaitableOrEnumMemberType(node.condition, type, node.whenTrue);
    const type1 ***REMOVED*** checkExpression(node.whenTrue, checkMode);
    const type2 ***REMOVED*** checkExpression(node.whenFalse, checkMode);
    return getUnionType([type1, type2], 2 /* Subtype */);
  }
  function isTemplateLiteralContext(node) {
    const parent ***REMOVED*** node.parent;
    return isParenthesizedExpression(parent) && isTemplateLiteralContext(parent) || isElementAccessExpression(parent) && parent.argumentExpression ***REMOVED******REMOVED******REMOVED*** node;
  }
  function checkTemplateExpression(node) {
    const texts ***REMOVED*** [node.head.text];
    const types ***REMOVED*** [];
    for (const span of node.templateSpans) {
      const type ***REMOVED*** checkExpression(span.expression);
      if (maybeTypeOfKindConsideringBaseConstraint(type, 12288 /* ESSymbolLike */)) {
        error(span.expression, Diagnostics.Implicit_conversion_of_a_symbol_to_a_string_will_fail_at_runtime_Consider_wrapping_this_expression_in_String);
      }
      texts.push(span.literal.text);
      types.push(isTypeAssignableTo(type, templateConstraintType) ? type : stringType);
    }
    const evaluated ***REMOVED*** node.parent.kind !***REMOVED******REMOVED*** 216 /* TaggedTemplateExpression */ && evaluate(node).value;
    if (evaluated) {
      return getFreshTypeOfLiteralType(getStringLiteralType(evaluated));
    }
    if (isConstContext(node) || isTemplateLiteralContext(node) || someType(getContextualType(
      node,
      /*contextFlags*/
      void 0
    ) || unknownType, isTemplateLiteralContextualType)) {
      return getTemplateLiteralType(texts, types);
    }
    return stringType;
  }
  function isTemplateLiteralContextualType(type) {
    return !!(type.flags & (128 /* StringLiteral */ | 134217728 /* TemplateLiteral */) || type.flags & 58982400 /* InstantiableNonPrimitive */ && maybeTypeOfKind(getBaseConstraintOfType(type) || unknownType, 402653316 /* StringLike */));
  }
  function getContextNode(node) {
    if (isJsxAttributes(node) && !isJsxSelfClosingElement(node.parent)) {
      return node.parent.parent;
    }
    return node;
  }
  function checkExpressionWithContextualType(node, contextualType, inferenceContext, checkMode) {
    const contextNode ***REMOVED*** getContextNode(node);
    pushContextualType(
      contextNode,
      contextualType,
      /*isCache*/
      false
    );
    pushInferenceContext(contextNode, inferenceContext);
    const type ***REMOVED*** checkExpression(node, checkMode | 1 /* Contextual */ | (inferenceContext ? 2 /* Inferential */ : 0));
    if (inferenceContext && inferenceContext.intraExpressionInferenceSites) {
      inferenceContext.intraExpressionInferenceSites ***REMOVED*** void 0;
    }
    const result ***REMOVED*** maybeTypeOfKind(type, 2944 /* Literal */) && isLiteralOfContextualType(type, instantiateContextualType(
      contextualType,
      node,
      /*contextFlags*/
      void 0
    )) ? getRegularTypeOfLiteralType(type) : type;
    popInferenceContext();
    popContextualType();
    return result;
  }
  function checkExpressionCached(node, checkMode) {
    if (checkMode) {
      return checkExpression(node, checkMode);
    }
    const links ***REMOVED*** getNodeLinks(node);
    if (!links.resolvedType) {
      const saveFlowLoopStart ***REMOVED*** flowLoopStart;
      const saveFlowTypeCache ***REMOVED*** flowTypeCache;
      flowLoopStart ***REMOVED*** flowLoopCount;
      flowTypeCache ***REMOVED*** void 0;
      links.resolvedType ***REMOVED*** checkExpression(node, checkMode);
      flowTypeCache ***REMOVED*** saveFlowTypeCache;
      flowLoopStart ***REMOVED*** saveFlowLoopStart;
    }
    return links.resolvedType;
  }
  function isTypeAssertion(node) {
    node ***REMOVED*** skipParentheses(
      node,
      /*excludeJSDocTypeAssertions*/
      true
    );
    return node.kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 235 /* AsExpression */ || isJSDocTypeAssertion(node);
  }
  function checkDeclarationInitializer(declaration, checkMode, contextualType) {
    const initializer ***REMOVED*** getEffectiveInitializer(declaration);
    if (isInJSFile(declaration)) {
      const typeNode ***REMOVED*** tryGetJSDocSatisfiesTypeNode(declaration);
      if (typeNode) {
        return checkSatisfiesExpressionWorker(initializer, typeNode, checkMode);
      }
    }
    const type ***REMOVED*** getQuickTypeOfExpression(initializer) || (contextualType ? checkExpressionWithContextualType(
      initializer,
      contextualType,
      /*inferenceContext*/
      void 0,
      checkMode || 0 /* Normal */
    ) : checkExpressionCached(initializer, checkMode));
    if (isParameter(isBindingElement(declaration) ? walkUpBindingElementsAndPatterns(declaration) : declaration)) {
      if (declaration.name.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ && isObjectLiteralType(type)) {
        return padObjectLiteralType(type, declaration.name);
      }
      if (declaration.name.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */ && isTupleType(type)) {
        return padTupleType(type, declaration.name);
      }
    }
    return type;
  }
  function padObjectLiteralType(type, pattern) {
    let missingElements;
    for (const e of pattern.elements) {
      if (e.initializer) {
        const name ***REMOVED*** getPropertyNameFromBindingElement(e);
        if (name && !getPropertyOfType(type, name)) {
          missingElements ***REMOVED*** append(missingElements, e);
        }
      }
    }
    if (!missingElements) {
      return type;
    }
    const members ***REMOVED*** createSymbolTable();
    for (const prop of getPropertiesOfObjectType(type)) {
      members.set(prop.escapedName, prop);
    }
    for (const e of missingElements) {
      const symbol ***REMOVED*** createSymbol(4 /* Property */ | 16777216 /* Optional */, getPropertyNameFromBindingElement(e));
      symbol.links.type ***REMOVED*** getTypeFromBindingElement(
        e,
        /*includePatternInType*/
        false,
        /*reportErrors*/
        false
      );
      members.set(symbol.escapedName, symbol);
    }
    const result ***REMOVED*** createAnonymousType(type.symbol, members, emptyArray, emptyArray, getIndexInfosOfType(type));
    result.objectFlags ***REMOVED*** type.objectFlags;
    return result;
  }
  function getPropertyNameFromBindingElement(e) {
    const exprType ***REMOVED*** getLiteralTypeFromPropertyName(e.propertyName || e.name);
    return isTypeUsableAsPropertyName(exprType) ? getPropertyNameFromType(exprType) : void 0;
  }
  function padTupleType(type, pattern) {
    if (type.target.combinedFlags & 12 /* Variable */ || getTypeReferenceArity(type) >***REMOVED*** pattern.elements.length) {
      return type;
    }
    const patternElements ***REMOVED*** pattern.elements;
    const elementTypes ***REMOVED*** getElementTypes(type).slice();
    const elementFlags ***REMOVED*** type.target.elementFlags.slice();
    for (let i ***REMOVED*** getTypeReferenceArity(type); i < patternElements.length; i++) {
      const e ***REMOVED*** patternElements[i];
      if (i < patternElements.length - 1 || !(e.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && e.dotDotDotToken)) {
        elementTypes.push(!isOmittedExpression(e) && hasDefaultValue(e) ? getTypeFromBindingElement(
          e,
          /*includePatternInType*/
          false,
          /*reportErrors*/
          false
        ) : anyType);
        elementFlags.push(2 /* Optional */);
        if (!isOmittedExpression(e) && !hasDefaultValue(e)) {
          reportImplicitAny(e, anyType);
        }
      }
    }
    return createTupleType(elementTypes, elementFlags, type.target.readonly);
  }
  function widenTypeInferredFromInitializer(declaration, type) {
    const widened ***REMOVED*** getWidenedLiteralTypeForInitializer(declaration, type);
    if (isInJSFile(declaration)) {
      if (isEmptyLiteralType(widened)) {
        reportImplicitAny(declaration, anyType);
        return anyType;
      } else if (isEmptyArrayLiteralType(widened)) {
        reportImplicitAny(declaration, anyArrayType);
        return anyArrayType;
      }
    }
    return widened;
  }
  function getWidenedLiteralTypeForInitializer(declaration, type) {
    return getCombinedNodeFlagsCached(declaration) & 6 /* Constant */ || isDeclarationReadonly(declaration) ? type : getWidenedLiteralType(type);
  }
  function isLiteralOfContextualType(candidateType, contextualType) {
    if (contextualType) {
      if (contextualType.flags & 3145728 /* UnionOrIntersection */) {
        const types ***REMOVED*** contextualType.types;
        return some(types, (t) ***REMOVED***> isLiteralOfContextualType(candidateType, t));
      }
      if (contextualType.flags & 58982400 /* InstantiableNonPrimitive */) {
        const constraint ***REMOVED*** getBaseConstraintOfType(contextualType) || unknownType;
        return maybeTypeOfKind(constraint, 4 /* String */) && maybeTypeOfKind(candidateType, 128 /* StringLiteral */) || maybeTypeOfKind(constraint, 8 /* Number */) && maybeTypeOfKind(candidateType, 256 /* NumberLiteral */) || maybeTypeOfKind(constraint, 64 /* BigInt */) && maybeTypeOfKind(candidateType, 2048 /* BigIntLiteral */) || maybeTypeOfKind(constraint, 4096 /* ESSymbol */) && maybeTypeOfKind(candidateType, 8192 /* UniqueESSymbol */) || isLiteralOfContextualType(candidateType, constraint);
      }
      return !!(contextualType.flags & (128 /* StringLiteral */ | 4194304 /* Index */ | 134217728 /* TemplateLiteral */ | 268435456 /* StringMapping */) && maybeTypeOfKind(candidateType, 128 /* StringLiteral */) || contextualType.flags & 256 /* NumberLiteral */ && maybeTypeOfKind(candidateType, 256 /* NumberLiteral */) || contextualType.flags & 2048 /* BigIntLiteral */ && maybeTypeOfKind(candidateType, 2048 /* BigIntLiteral */) || contextualType.flags & 512 /* BooleanLiteral */ && maybeTypeOfKind(candidateType, 512 /* BooleanLiteral */) || contextualType.flags & 8192 /* UniqueESSymbol */ && maybeTypeOfKind(candidateType, 8192 /* UniqueESSymbol */));
    }
    return false;
  }
  function isConstContext(node) {
    const parent ***REMOVED*** node.parent;
    return isAssertionExpression(parent) && isConstTypeReference(parent.type) || isJSDocTypeAssertion(parent) && isConstTypeReference(getJSDocTypeAssertionType(parent)) || isValidConstAssertionArgument(node) && isConstTypeVariable(getContextualType(node, 0 /* None */)) || (isParenthesizedExpression(parent) || isArrayLiteralExpression(parent) || isSpreadElement(parent)) && isConstContext(parent) || (isPropertyAssignment(parent) || isShorthandPropertyAssignment(parent) || isTemplateSpan(parent)) && isConstContext(parent.parent);
  }
  function checkExpressionForMutableLocation(node, checkMode, forceTuple) {
    const type ***REMOVED*** checkExpression(node, checkMode, forceTuple);
    return isConstContext(node) || isCommonJsExportedExpression(node) ? getRegularTypeOfLiteralType(type) : isTypeAssertion(node) ? type : getWidenedLiteralLikeTypeForContextualType(type, instantiateContextualType(
      getContextualType(
        node,
        /*contextFlags*/
        void 0
      ),
      node,
      /*contextFlags*/
      void 0
    ));
  }
  function checkPropertyAssignment(node, checkMode) {
    if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
      checkComputedPropertyName(node.name);
    }
    return checkExpressionForMutableLocation(node.initializer, checkMode);
  }
  function checkObjectLiteralMethod(node, checkMode) {
    checkGrammarMethod(node);
    if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
      checkComputedPropertyName(node.name);
    }
    const uninstantiatedType ***REMOVED*** checkFunctionExpressionOrObjectLiteralMethod(node, checkMode);
    return instantiateTypeWithSingleGenericCallSignature(node, uninstantiatedType, checkMode);
  }
  function instantiateTypeWithSingleGenericCallSignature(node, type, checkMode) {
    if (checkMode && checkMode & (2 /* Inferential */ | 8 /* SkipGenericFunctions */)) {
      const callSignature ***REMOVED*** getSingleSignature(
        type,
        0 /* Call */,
        /*allowMembers*/
        true
      );
      const constructSignature ***REMOVED*** getSingleSignature(
        type,
        1 /* Construct */,
        /*allowMembers*/
        true
      );
      const signature ***REMOVED*** callSignature || constructSignature;
      if (signature && signature.typeParameters) {
        const contextualType ***REMOVED*** getApparentTypeOfContextualType(node, 2 /* NoConstraints */);
        if (contextualType) {
          const contextualSignature ***REMOVED*** getSingleSignature(
            getNonNullableType(contextualType),
            callSignature ? 0 /* Call */ : 1 /* Construct */,
            /*allowMembers*/
            false
          );
          if (contextualSignature && !contextualSignature.typeParameters) {
            if (checkMode & 8 /* SkipGenericFunctions */) {
              skippedGenericFunction(node, checkMode);
              return anyFunctionType;
            }
            const context ***REMOVED*** getInferenceContext(node);
            const returnType ***REMOVED*** context.signature && getReturnTypeOfSignature(context.signature);
            const returnSignature ***REMOVED*** returnType && getSingleCallOrConstructSignature(returnType);
            if (returnSignature && !returnSignature.typeParameters && !every(context.inferences, hasInferenceCandidates)) {
              const uniqueTypeParameters ***REMOVED*** getUniqueTypeParameters(context, signature.typeParameters);
              const instantiatedSignature ***REMOVED*** getSignatureInstantiationWithoutFillingInTypeArguments(signature, uniqueTypeParameters);
              const inferences ***REMOVED*** map(context.inferences, (info) ***REMOVED***> createInferenceInfo(info.typeParameter));
              applyToParameterTypes(instantiatedSignature, contextualSignature, (source, target) ***REMOVED***> {
                inferTypes(
                  inferences,
                  source,
                  target,
                  /*priority*/
                  0,
                  /*contravariant*/
                  true
                );
              });
              if (some(inferences, hasInferenceCandidates)) {
                applyToReturnTypes(instantiatedSignature, contextualSignature, (source, target) ***REMOVED***> {
                  inferTypes(inferences, source, target);
                });
                if (!hasOverlappingInferences(context.inferences, inferences)) {
                  mergeInferences(context.inferences, inferences);
                  context.inferredTypeParameters ***REMOVED*** concatenate(context.inferredTypeParameters, uniqueTypeParameters);
                  return getOrCreateTypeFromSignature(instantiatedSignature);
                }
              }
            }
            return getOrCreateTypeFromSignature(instantiateSignatureInContextOf(signature, contextualSignature, context));
          }
        }
      }
    }
    return type;
  }
  function skippedGenericFunction(node, checkMode) {
    if (checkMode & 2 /* Inferential */) {
      const context ***REMOVED*** getInferenceContext(node);
      context.flags |***REMOVED*** 4 /* SkippedGenericFunction */;
    }
  }
  function hasInferenceCandidates(info) {
    return !!(info.candidates || info.contraCandidates);
  }
  function hasInferenceCandidatesOrDefault(info) {
    return !!(info.candidates || info.contraCandidates || hasTypeParameterDefault(info.typeParameter));
  }
  function hasOverlappingInferences(a, b) {
    for (let i ***REMOVED*** 0; i < a.length; i++) {
      if (hasInferenceCandidates(a[i]) && hasInferenceCandidates(b[i])) {
        return true;
      }
    }
    return false;
  }
  function mergeInferences(target, source) {
    for (let i ***REMOVED*** 0; i < target.length; i++) {
      if (!hasInferenceCandidates(target[i]) && hasInferenceCandidates(source[i])) {
        target[i] ***REMOVED*** source[i];
      }
    }
  }
  function getUniqueTypeParameters(context, typeParameters) {
    const result ***REMOVED*** [];
    let oldTypeParameters;
    let newTypeParameters;
    for (const tp of typeParameters) {
      const name ***REMOVED*** tp.symbol.escapedName;
      if (hasTypeParameterByName(context.inferredTypeParameters, name) || hasTypeParameterByName(result, name)) {
        const newName ***REMOVED*** getUniqueTypeParameterName(concatenate(context.inferredTypeParameters, result), name);
        const symbol ***REMOVED*** createSymbol(262144 /* TypeParameter */, newName);
        const newTypeParameter ***REMOVED*** createTypeParameter(symbol);
        newTypeParameter.target ***REMOVED*** tp;
        oldTypeParameters ***REMOVED*** append(oldTypeParameters, tp);
        newTypeParameters ***REMOVED*** append(newTypeParameters, newTypeParameter);
        result.push(newTypeParameter);
      } else {
        result.push(tp);
      }
    }
    if (newTypeParameters) {
      const mapper ***REMOVED*** createTypeMapper(oldTypeParameters, newTypeParameters);
      for (const tp of newTypeParameters) {
        tp.mapper ***REMOVED*** mapper;
      }
    }
    return result;
  }
  function hasTypeParameterByName(typeParameters, name) {
    return some(typeParameters, (tp) ***REMOVED***> tp.symbol.escapedName ***REMOVED******REMOVED******REMOVED*** name);
  }
  function getUniqueTypeParameterName(typeParameters, baseName) {
    let len ***REMOVED*** baseName.length;
    while (len > 1 && baseName.charCodeAt(len - 1) >***REMOVED*** 48 /* _0 */ && baseName.charCodeAt(len - 1) <***REMOVED*** 57 /* _9 */) len--;
    const s ***REMOVED*** baseName.slice(0, len);
    for (let index ***REMOVED*** 1; true; index++) {
      const augmentedName ***REMOVED*** s + index;
      if (!hasTypeParameterByName(typeParameters, augmentedName)) {
        return augmentedName;
      }
    }
  }
  function getReturnTypeOfSingleNonGenericCallSignature(funcType) {
    const signature ***REMOVED*** getSingleCallSignature(funcType);
    if (signature && !signature.typeParameters) {
      return getReturnTypeOfSignature(signature);
    }
  }
  function getReturnTypeOfSingleNonGenericSignatureOfCallChain(expr) {
    const funcType ***REMOVED*** checkExpression(expr.expression);
    const nonOptionalType ***REMOVED*** getOptionalExpressionType(funcType, expr.expression);
    const returnType ***REMOVED*** getReturnTypeOfSingleNonGenericCallSignature(funcType);
    return returnType && propagateOptionalTypeMarker(returnType, expr, nonOptionalType !***REMOVED******REMOVED*** funcType);
  }
  function getTypeOfExpression(node) {
    const quickType ***REMOVED*** getQuickTypeOfExpression(node);
    if (quickType) {
      return quickType;
    }
    if (node.flags & 268435456 /* TypeCached */ && flowTypeCache) {
      const cachedType ***REMOVED*** flowTypeCache[getNodeId(node)];
      if (cachedType) {
        return cachedType;
      }
    }
    const startInvocationCount ***REMOVED*** flowInvocationCount;
    const type ***REMOVED*** checkExpression(node, 64 /* TypeOnly */);
    if (flowInvocationCount !***REMOVED******REMOVED*** startInvocationCount) {
      const cache ***REMOVED*** flowTypeCache || (flowTypeCache ***REMOVED*** []);
      cache[getNodeId(node)] ***REMOVED*** type;
      setNodeFlags(node, node.flags | 268435456 /* TypeCached */);
    }
    return type;
  }
  function getQuickTypeOfExpression(node) {
    let expr ***REMOVED*** skipParentheses(
      node,
      /*excludeJSDocTypeAssertions*/
      true
    );
    if (isJSDocTypeAssertion(expr)) {
      const type ***REMOVED*** getJSDocTypeAssertionType(expr);
      if (!isConstTypeReference(type)) {
        return getTypeFromTypeNode(type);
      }
    }
    expr ***REMOVED*** skipParentheses(node);
    if (isAwaitExpression(expr)) {
      const type ***REMOVED*** getQuickTypeOfExpression(expr.expression);
      return type ? getAwaitedType(type) : void 0;
    }
    if (isCallExpression(expr) && expr.expression.kind !***REMOVED******REMOVED*** 108 /* SuperKeyword */ && !isRequireCall(
      expr,
      /*requireStringLiteralLikeArgument*/
      true
    ) && !isSymbolOrSymbolForCall(expr) && !isImportCall(expr)) {
      return isCallChain(expr) ? getReturnTypeOfSingleNonGenericSignatureOfCallChain(expr) : getReturnTypeOfSingleNonGenericCallSignature(checkNonNullExpression(expr.expression));
    } else if (isAssertionExpression(expr) && !isConstTypeReference(expr.type)) {
      return getTypeFromTypeNode(expr.type);
    } else if (isLiteralExpression(node) || isBooleanLiteral(node)) {
      return checkExpression(node);
    }
    return void 0;
  }
  function getContextFreeTypeOfExpression(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (links.contextFreeType) {
      return links.contextFreeType;
    }
    pushContextualType(
      node,
      anyType,
      /*isCache*/
      false
    );
    const type ***REMOVED*** links.contextFreeType ***REMOVED*** checkExpression(node, 4 /* SkipContextSensitive */);
    popContextualType();
    return type;
  }
  function checkExpression(node, checkMode, forceTuple) {
    var _a, _b;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Check, "checkExpression", { kind: node.kind, pos: node.pos, end: node.end, path: node.tracingPath });
    const saveCurrentNode ***REMOVED*** currentNode;
    currentNode ***REMOVED*** node;
    instantiationCount ***REMOVED*** 0;
    const uninstantiatedType ***REMOVED*** checkExpressionWorker(node, checkMode, forceTuple);
    const type ***REMOVED*** instantiateTypeWithSingleGenericCallSignature(node, uninstantiatedType, checkMode);
    if (isConstEnumObjectType(type)) {
      checkConstEnumAccess(node, type);
    }
    currentNode ***REMOVED*** saveCurrentNode;
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
    return type;
  }
  function checkConstEnumAccess(node, type) {
    var _a;
    const ok ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node || ((node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) && isInRightSideOfImportOrExportAssignment(node) || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 187 /* TypeQuery */ && node.parent.exprName ***REMOVED******REMOVED******REMOVED*** node) || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */;
    if (!ok) {
      error(node, Diagnostics.const_enums_can_only_be_used_in_property_or_index_access_expressions_or_the_right_hand_side_of_an_import_declaration_or_export_assignment_or_type_query);
    }
    if (compilerOptions.isolatedModules || compilerOptions.verbatimModuleSyntax && ok && !resolveName(
      node,
      getFirstIdentifier(node),
      2097152 /* Alias */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      false,
      /*excludeGlobals*/
      true
    )) {
      Debug.assert(!!(type.symbol.flags & 128 /* ConstEnum */));
      const constEnumDeclaration ***REMOVED*** type.symbol.valueDeclaration;
      const redirect ***REMOVED*** (_a ***REMOVED*** host.getRedirectFromOutput(getSourceFileOfNode(constEnumDeclaration).resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _a.resolvedRef;
      if (constEnumDeclaration.flags & 33554432 /* Ambient */ && !isValidTypeOnlyAliasUseSite(node) && (!redirect || !shouldPreserveConstEnums(redirect.commandLine.options))) {
        error(node, Diagnostics.Cannot_access_ambient_const_enums_when_0_is_enabled, isolatedModulesLikeFlagName);
      }
    }
  }
  function checkParenthesizedExpression(node, checkMode) {
    if (hasJSDocNodes(node)) {
      if (isJSDocSatisfiesExpression(node)) {
        return checkSatisfiesExpressionWorker(node.expression, getJSDocSatisfiesExpressionType(node), checkMode);
      }
      if (isJSDocTypeAssertion(node)) {
        return checkAssertionWorker(node, checkMode);
      }
    }
    return checkExpression(node.expression, checkMode);
  }
  function checkExpressionWorker(node, checkMode, forceTuple) {
    const kind ***REMOVED*** node.kind;
    if (cancellationToken) {
      switch (kind) {
        case 232 /* ClassExpression */:
        case 219 /* FunctionExpression */:
        case 220 /* ArrowFunction */:
          cancellationToken.throwIfCancellationRequested();
      }
    }
    switch (kind) {
      case 80 /* Identifier */:
        return checkIdentifier(node, checkMode);
      case 81 /* PrivateIdentifier */:
        return checkPrivateIdentifierExpression(node);
      case 110 /* ThisKeyword */:
        return checkThisExpression(node);
      case 108 /* SuperKeyword */:
        return checkSuperExpression(node);
      case 106 /* NullKeyword */:
        return nullWideningType;
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 11 /* StringLiteral */:
        return hasSkipDirectInferenceFlag(node) ? blockedStringType : getFreshTypeOfLiteralType(getStringLiteralType(node.text));
      case 9 /* NumericLiteral */:
        checkGrammarNumericLiteral(node);
        return getFreshTypeOfLiteralType(getNumberLiteralType(+node.text));
      case 10 /* BigIntLiteral */:
        checkGrammarBigIntLiteral(node);
        return getFreshTypeOfLiteralType(getBigIntLiteralType({
          negative: false,
          base10Value: parsePseudoBigInt(node.text)
        }));
      case 112 /* TrueKeyword */:
        return trueType;
      case 97 /* FalseKeyword */:
        return falseType;
      case 229 /* TemplateExpression */:
        return checkTemplateExpression(node);
      case 14 /* RegularExpressionLiteral */:
        return checkRegularExpressionLiteral(node);
      case 210 /* ArrayLiteralExpression */:
        return checkArrayLiteral(node, checkMode, forceTuple);
      case 211 /* ObjectLiteralExpression */:
        return checkObjectLiteral(node, checkMode);
      case 212 /* PropertyAccessExpression */:
        return checkPropertyAccessExpression(node, checkMode);
      case 167 /* QualifiedName */:
        return checkQualifiedName(node, checkMode);
      case 213 /* ElementAccessExpression */:
        return checkIndexedAccess(node, checkMode);
      case 214 /* CallExpression */:
        if (isImportCall(node)) {
          return checkImportCallExpression(node);
        }
      // falls through
      case 215 /* NewExpression */:
        return checkCallExpression(node, checkMode);
      case 216 /* TaggedTemplateExpression */:
        return checkTaggedTemplateExpression(node);
      case 218 /* ParenthesizedExpression */:
        return checkParenthesizedExpression(node, checkMode);
      case 232 /* ClassExpression */:
        return checkClassExpression(node);
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        return checkFunctionExpressionOrObjectLiteralMethod(node, checkMode);
      case 222 /* TypeOfExpression */:
        return checkTypeOfExpression(node);
      case 217 /* TypeAssertionExpression */:
      case 235 /* AsExpression */:
        return checkAssertion(node, checkMode);
      case 236 /* NonNullExpression */:
        return checkNonNullAssertion(node);
      case 234 /* ExpressionWithTypeArguments */:
        return checkExpressionWithTypeArguments(node);
      case 239 /* SatisfiesExpression */:
        return checkSatisfiesExpression(node);
      case 237 /* MetaProperty */:
        return checkMetaProperty(node);
      case 221 /* DeleteExpression */:
        return checkDeleteExpression(node);
      case 223 /* VoidExpression */:
        return checkVoidExpression(node);
      case 224 /* AwaitExpression */:
        return checkAwaitExpression(node);
      case 225 /* PrefixUnaryExpression */:
        return checkPrefixUnaryExpression(node);
      case 226 /* PostfixUnaryExpression */:
        return checkPostfixUnaryExpression(node);
      case 227 /* BinaryExpression */:
        return checkBinaryExpression(node, checkMode);
      case 228 /* ConditionalExpression */:
        return checkConditionalExpression(node, checkMode);
      case 231 /* SpreadElement */:
        return checkSpreadExpression(node, checkMode);
      case 233 /* OmittedExpression */:
        return undefinedWideningType;
      case 230 /* YieldExpression */:
        return checkYieldExpression(node);
      case 238 /* SyntheticExpression */:
        return checkSyntheticExpression(node);
      case 295 /* JsxExpression */:
        return checkJsxExpression(node, checkMode);
      case 285 /* JsxElement */:
        return checkJsxElement(node, checkMode);
      case 286 /* JsxSelfClosingElement */:
        return checkJsxSelfClosingElement(node, checkMode);
      case 289 /* JsxFragment */:
        return checkJsxFragment(node);
      case 293 /* JsxAttributes */:
        return checkJsxAttributes(node, checkMode);
      case 287 /* JsxOpeningElement */:
        Debug.fail("Shouldn't ever directly check a JsxOpeningElement");
    }
    return errorType;
  }
  function checkTypeParameter(node) {
    checkGrammarModifiers(node);
    if (node.expression) {
      grammarErrorOnFirstToken(node.expression, Diagnostics.Type_expected);
    }
    checkSourceElement(node.constraint);
    checkSourceElement(node.default);
    const typeParameter ***REMOVED*** getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(node));
    getBaseConstraintOfType(typeParameter);
    if (!hasNonCircularTypeParameterDefault(typeParameter)) {
      error(node.default, Diagnostics.Type_parameter_0_has_a_circular_default, typeToString(typeParameter));
    }
    const constraintType ***REMOVED*** getConstraintOfTypeParameter(typeParameter);
    const defaultType ***REMOVED*** getDefaultFromTypeParameter(typeParameter);
    if (constraintType && defaultType) {
      checkTypeAssignableTo(defaultType, getTypeWithThisArgument(instantiateType(constraintType, makeUnaryTypeMapper(typeParameter, defaultType)), defaultType), node.default, Diagnostics.Type_0_does_not_satisfy_the_constraint_1);
    }
    checkNodeDeferred(node);
    addLazyDiagnostic(() ***REMOVED***> checkTypeNameIsReserved(node.name, Diagnostics.Type_parameter_name_cannot_be_0));
  }
  function checkTypeParameterDeferred(node) {
    var _a, _b;
    if (isInterfaceDeclaration(node.parent) || isClassLike(node.parent) || isTypeAliasDeclaration(node.parent)) {
      const typeParameter ***REMOVED*** getDeclaredTypeOfTypeParameter(getSymbolOfDeclaration(node));
      const modifiers ***REMOVED*** getTypeParameterModifiers(typeParameter) & (8192 /* In */ | 16384 /* Out */);
      if (modifiers) {
        const symbol ***REMOVED*** getSymbolOfDeclaration(node.parent);
        if (isTypeAliasDeclaration(node.parent) && !(getObjectFlags(getDeclaredTypeOfSymbol(symbol)) & (16 /* Anonymous */ | 32 /* Mapped */))) {
          error(node, Diagnostics.Variance_annotations_are_only_supported_in_type_aliases_for_object_function_constructor_and_mapped_types);
        } else if (modifiers ***REMOVED******REMOVED******REMOVED*** 8192 /* In */ || modifiers ***REMOVED******REMOVED******REMOVED*** 16384 /* Out */) {
          (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.CheckTypes, "checkTypeParameterDeferred", { parent: getTypeId(getDeclaredTypeOfSymbol(symbol)), id: getTypeId(typeParameter) });
          const source ***REMOVED*** createMarkerType(symbol, typeParameter, modifiers ***REMOVED******REMOVED******REMOVED*** 16384 /* Out */ ? markerSubTypeForCheck : markerSuperTypeForCheck);
          const target ***REMOVED*** createMarkerType(symbol, typeParameter, modifiers ***REMOVED******REMOVED******REMOVED*** 16384 /* Out */ ? markerSuperTypeForCheck : markerSubTypeForCheck);
          const saveVarianceTypeParameter ***REMOVED*** typeParameter;
          varianceTypeParameter ***REMOVED*** typeParameter;
          checkTypeAssignableTo(source, target, node, Diagnostics.Type_0_is_not_assignable_to_type_1_as_implied_by_variance_annotation);
          varianceTypeParameter ***REMOVED*** saveVarianceTypeParameter;
          (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
        }
      }
    }
  }
  function checkParameter(node) {
    checkGrammarModifiers(node);
    checkVariableLikeDeclaration(node);
    const func ***REMOVED*** getContainingFunction(node);
    if (hasSyntacticModifier(node, 31 /* ParameterPropertyModifier */)) {
      if (compilerOptions.erasableSyntaxOnly) {
        error(node, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled);
      }
      if (!(func.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ && nodeIsPresent(func.body))) {
        error(node, Diagnostics.A_parameter_property_is_only_allowed_in_a_constructor_implementation);
      }
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ && isIdentifier(node.name) && node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "constructor") {
        error(node.name, Diagnostics.constructor_cannot_be_used_as_a_parameter_property_name);
      }
    }
    if (!node.initializer && isOptionalDeclaration(node) && isBindingPattern(node.name) && func.body) {
      error(node, Diagnostics.A_binding_pattern_parameter_cannot_be_optional_in_an_implementation_signature);
    }
    if (node.name && isIdentifier(node.name) && (node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "this" || node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "new")) {
      if (func.parameters.indexOf(node) !***REMOVED******REMOVED*** 0) {
        error(node, Diagnostics.A_0_parameter_must_be_the_first_parameter, node.name.escapedText);
      }
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || func.kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */ || func.kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */) {
        error(node, Diagnostics.A_constructor_cannot_have_a_this_parameter);
      }
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
        error(node, Diagnostics.An_arrow_function_cannot_have_a_this_parameter);
      }
      if (func.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || func.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
        error(node, Diagnostics.get_and_set_accessors_cannot_declare_this_parameters);
      }
    }
    if (node.dotDotDotToken && !isBindingPattern(node.name) && !isTypeAssignableTo(getReducedType(getTypeOfSymbol(node.symbol)), anyReadonlyArrayType)) {
      error(node, Diagnostics.A_rest_parameter_must_be_of_an_array_type);
    }
  }
  function checkTypePredicate(node) {
    const parent ***REMOVED*** getTypePredicateParent(node);
    if (!parent) {
      error(node, Diagnostics.A_type_predicate_is_only_allowed_in_return_type_position_for_functions_and_methods);
      return;
    }
    const signature ***REMOVED*** getSignatureFromDeclaration(parent);
    const typePredicate ***REMOVED*** getTypePredicateOfSignature(signature);
    if (!typePredicate) {
      return;
    }
    checkSourceElement(node.type);
    const { parameterName } ***REMOVED*** node;
    if (typePredicate.kind !***REMOVED******REMOVED*** 0 /* This */ && typePredicate.kind !***REMOVED******REMOVED*** 2 /* AssertsThis */) {
      if (typePredicate.parameterIndex >***REMOVED*** 0) {
        if (signatureHasRestParameter(signature) && typePredicate.parameterIndex ***REMOVED******REMOVED******REMOVED*** signature.parameters.length - 1) {
          error(parameterName, Diagnostics.A_type_predicate_cannot_reference_a_rest_parameter);
        } else {
          if (typePredicate.type) {
            const leadingError ***REMOVED*** () ***REMOVED***> chainDiagnosticMessages(
              /*details*/
              void 0,
              Diagnostics.A_type_predicate_s_type_must_be_assignable_to_its_parameter_s_type
            );
            checkTypeAssignableTo(
              typePredicate.type,
              getTypeOfSymbol(signature.parameters[typePredicate.parameterIndex]),
              node.type,
              /*headMessage*/
              void 0,
              leadingError
            );
          }
        }
      } else if (parameterName) {
        let hasReportedError ***REMOVED*** false;
        for (const { name } of parent.parameters) {
          if (isBindingPattern(name) && checkIfTypePredicateVariableIsDeclaredInBindingPattern(name, parameterName, typePredicate.parameterName)) {
            hasReportedError ***REMOVED*** true;
            break;
          }
        }
        if (!hasReportedError) {
          error(node.parameterName, Diagnostics.Cannot_find_parameter_0, typePredicate.parameterName);
        }
      }
    }
  }
  function getTypePredicateParent(node) {
    switch (node.parent.kind) {
      case 220 /* ArrowFunction */:
      case 180 /* CallSignature */:
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 185 /* FunctionType */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        const parent ***REMOVED*** node.parent;
        if (node ***REMOVED******REMOVED******REMOVED*** parent.type) {
          return parent;
        }
    }
  }
  function checkIfTypePredicateVariableIsDeclaredInBindingPattern(pattern, predicateVariableNode, predicateVariableName) {
    for (const element of pattern.elements) {
      if (isOmittedExpression(element)) {
        continue;
      }
      const name ***REMOVED*** element.name;
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && name.escapedText ***REMOVED******REMOVED******REMOVED*** predicateVariableName) {
        error(predicateVariableNode, Diagnostics.A_type_predicate_cannot_reference_element_0_in_a_binding_pattern, predicateVariableName);
        return true;
      } else if (name.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */ || name.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */) {
        if (checkIfTypePredicateVariableIsDeclaredInBindingPattern(
          name,
          predicateVariableNode,
          predicateVariableName
        )) {
          return true;
        }
      }
    }
  }
  function checkSignatureDeclaration(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */) {
      checkGrammarIndexSignature(node);
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 185 /* FunctionType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 180 /* CallSignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 181 /* ConstructSignature */) {
      checkGrammarFunctionLikeDeclaration(node);
    }
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    if (!(functionFlags & 4 /* Invalid */)) {
      if ((functionFlags & 3 /* AsyncGenerator */) ***REMOVED******REMOVED******REMOVED*** 3 /* AsyncGenerator */ && languageVersion < LanguageFeatureMinimumTarget.AsyncGenerators) {
        checkExternalEmitHelpers(node, 6144 /* AsyncGeneratorIncludes */);
      }
      if ((functionFlags & 3 /* AsyncGenerator */) ***REMOVED******REMOVED******REMOVED*** 2 /* Async */ && languageVersion < LanguageFeatureMinimumTarget.AsyncFunctions) {
        checkExternalEmitHelpers(node, 64 /* Awaiter */);
      }
      if ((functionFlags & 3 /* AsyncGenerator */) !***REMOVED******REMOVED*** 0 /* Normal */ && languageVersion < LanguageFeatureMinimumTarget.Generators) {
        checkExternalEmitHelpers(node, 128 /* Generator */);
      }
    }
    checkTypeParameters(getEffectiveTypeParameterDeclarations(node));
    checkUnmatchedJSDocParameters(node);
    forEach(node.parameters, checkParameter);
    if (node.type) {
      checkSourceElement(node.type);
    }
    addLazyDiagnostic(checkSignatureDeclarationDiagnostics);
    function checkSignatureDeclarationDiagnostics() {
      checkCollisionWithArgumentsInGeneratedCode(node);
      let returnTypeNode ***REMOVED*** getEffectiveReturnTypeNode(node);
      let returnTypeErrorLocation ***REMOVED*** returnTypeNode;
      if (isInJSFile(node)) {
        const typeTag ***REMOVED*** getJSDocTypeTag(node);
        if (typeTag && typeTag.typeExpression && isTypeReferenceNode(typeTag.typeExpression.type)) {
          const signature ***REMOVED*** getSingleCallSignature(getTypeFromTypeNode(typeTag.typeExpression));
          if (signature && signature.declaration) {
            returnTypeNode ***REMOVED*** getEffectiveReturnTypeNode(signature.declaration);
            returnTypeErrorLocation ***REMOVED*** typeTag.typeExpression.type;
          }
        }
      }
      if (noImplicitAny && !returnTypeNode) {
        switch (node.kind) {
          case 181 /* ConstructSignature */:
            error(node, Diagnostics.Construct_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type);
            break;
          case 180 /* CallSignature */:
            error(node, Diagnostics.Call_signature_which_lacks_return_type_annotation_implicitly_has_an_any_return_type);
            break;
        }
      }
      if (returnTypeNode && returnTypeErrorLocation) {
        const functionFlags2 ***REMOVED*** getFunctionFlags(node);
        if ((functionFlags2 & (4 /* Invalid */ | 1 /* Generator */)) ***REMOVED******REMOVED******REMOVED*** 1 /* Generator */) {
          const returnType ***REMOVED*** getTypeFromTypeNode(returnTypeNode);
          if (returnType ***REMOVED******REMOVED******REMOVED*** voidType) {
            error(returnTypeErrorLocation, Diagnostics.A_generator_cannot_have_a_void_type_annotation);
          } else {
            checkGeneratorInstantiationAssignabilityToReturnType(returnType, functionFlags2, returnTypeErrorLocation);
          }
        } else if ((functionFlags2 & 3 /* AsyncGenerator */) ***REMOVED******REMOVED******REMOVED*** 2 /* Async */) {
          checkAsyncFunctionReturnType(node, returnTypeNode, returnTypeErrorLocation);
        }
      }
      if (node.kind !***REMOVED******REMOVED*** 182 /* IndexSignature */ && node.kind !***REMOVED******REMOVED*** 318 /* JSDocFunctionType */) {
        registerForUnusedIdentifiersCheck(node);
      }
    }
  }
  function checkGeneratorInstantiationAssignabilityToReturnType(returnType, functionFlags, errorNode) {
    const generatorYieldType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(0 /* Yield */, returnType, (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0) || anyType;
    const generatorReturnType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, returnType, (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0) || generatorYieldType;
    const generatorNextType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(2 /* Next */, returnType, (functionFlags & 2 /* Async */) !***REMOVED******REMOVED*** 0) || unknownType;
    const generatorInstantiation ***REMOVED*** createGeneratorType(generatorYieldType, generatorReturnType, generatorNextType, !!(functionFlags & 2 /* Async */));
    return checkTypeAssignableTo(generatorInstantiation, returnType, errorNode);
  }
  function checkClassForDuplicateDeclarations(node) {
    const instanceNames ***REMOVED*** /* @__PURE__ */ new Map();
    const staticNames ***REMOVED*** /* @__PURE__ */ new Map();
    const privateIdentifiers ***REMOVED*** /* @__PURE__ */ new Map();
    for (const member of node.members) {
      if (member.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
        for (const param of member.parameters) {
          if (isParameterPropertyDeclaration(param, member) && !isBindingPattern(param.name)) {
            addName(instanceNames, param.name, param.name.escapedText, 3 /* GetOrSetAccessor */);
          }
        }
      } else {
        const isStaticMember ***REMOVED*** isStatic(member);
        const name ***REMOVED*** member.name;
        if (!name) {
          continue;
        }
        const isPrivate ***REMOVED*** isPrivateIdentifier(name);
        const privateStaticFlags ***REMOVED*** isPrivate && isStaticMember ? 16 /* PrivateStatic */ : 0;
        const names ***REMOVED*** isPrivate ? privateIdentifiers : isStaticMember ? staticNames : instanceNames;
        const memberName ***REMOVED*** name && getEffectivePropertyNameForPropertyNameNode(name);
        if (memberName) {
          switch (member.kind) {
            case 178 /* GetAccessor */:
              addName(names, name, memberName, 1 /* GetAccessor */ | privateStaticFlags);
              break;
            case 179 /* SetAccessor */:
              addName(names, name, memberName, 2 /* SetAccessor */ | privateStaticFlags);
              break;
            case 173 /* PropertyDeclaration */:
              addName(names, name, memberName, 3 /* GetOrSetAccessor */ | privateStaticFlags);
              break;
            case 175 /* MethodDeclaration */:
              addName(names, name, memberName, 8 /* Method */ | privateStaticFlags);
              break;
          }
        }
      }
    }
    function addName(names, location, name, meaning) {
      const prev ***REMOVED*** names.get(name);
      if (prev) {
        if ((prev & 16 /* PrivateStatic */) !***REMOVED******REMOVED*** (meaning & 16 /* PrivateStatic */)) {
          error(location, Diagnostics.Duplicate_identifier_0_Static_and_instance_elements_cannot_share_the_same_private_name, getTextOfNode(location));
        } else {
          const prevIsMethod ***REMOVED*** !!(prev & 8 /* Method */);
          const isMethod ***REMOVED*** !!(meaning & 8 /* Method */);
          if (prevIsMethod || isMethod) {
            if (prevIsMethod !***REMOVED******REMOVED*** isMethod) {
              error(location, Diagnostics.Duplicate_identifier_0, getTextOfNode(location));
            }
          } else if (prev & meaning & ~16 /* PrivateStatic */) {
            error(location, Diagnostics.Duplicate_identifier_0, getTextOfNode(location));
          } else {
            names.set(name, prev | meaning);
          }
        }
      } else {
        names.set(name, meaning);
      }
    }
  }
  function checkClassForStaticPropertyNameConflicts(node) {
    for (const member of node.members) {
      const memberNameNode ***REMOVED*** member.name;
      const isStaticMember ***REMOVED*** isStatic(member);
      if (isStaticMember && memberNameNode) {
        const memberName ***REMOVED*** getEffectivePropertyNameForPropertyNameNode(memberNameNode);
        switch (memberName) {
          case "name":
          case "length":
          case "caller":
          case "arguments":
            if (useDefineForClassFields) {
              break;
            }
          // fall through
          case "prototype":
            const message ***REMOVED*** Diagnostics.Static_property_0_conflicts_with_built_in_property_Function_0_of_constructor_function_1;
            const className ***REMOVED*** getNameOfSymbolAsWritten(getSymbolOfDeclaration(node));
            error(memberNameNode, message, memberName, className);
            break;
        }
      }
    }
  }
  function checkObjectTypeForDuplicateDeclarations(node) {
    const names ***REMOVED*** /* @__PURE__ */ new Map();
    for (const member of node.members) {
      if (member.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */) {
        let memberName;
        const name ***REMOVED*** member.name;
        switch (name.kind) {
          case 11 /* StringLiteral */:
          case 9 /* NumericLiteral */:
            memberName ***REMOVED*** name.text;
            break;
          case 80 /* Identifier */:
            memberName ***REMOVED*** idText(name);
            break;
          default:
            continue;
        }
        if (names.get(memberName)) {
          error(getNameOfDeclaration(member.symbol.valueDeclaration), Diagnostics.Duplicate_identifier_0, memberName);
          error(member.name, Diagnostics.Duplicate_identifier_0, memberName);
        } else {
          names.set(memberName, true);
        }
      }
    }
  }
  function checkTypeForDuplicateIndexSignatures(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
      const nodeSymbol ***REMOVED*** getSymbolOfDeclaration(node);
      if (nodeSymbol.declarations && nodeSymbol.declarations.length > 0 && nodeSymbol.declarations[0] !***REMOVED******REMOVED*** node) {
        return;
      }
    }
    const indexSymbol ***REMOVED*** getIndexSymbol(getSymbolOfDeclaration(node));
    if (indexSymbol ***REMOVED******REMOVED*** null ? void 0 : indexSymbol.declarations) {
      const indexSignatureMap ***REMOVED*** /* @__PURE__ */ new Map();
      for (const declaration of indexSymbol.declarations) {
        if (isIndexSignatureDeclaration(declaration)) {
          if (declaration.parameters.length ***REMOVED******REMOVED******REMOVED*** 1 && declaration.parameters[0].type) {
            forEachType(getTypeFromTypeNode(declaration.parameters[0].type), (type) ***REMOVED***> {
              const entry ***REMOVED*** indexSignatureMap.get(getTypeId(type));
              if (entry) {
                entry.declarations.push(declaration);
              } else {
                indexSignatureMap.set(getTypeId(type), { type, declarations: [declaration] });
              }
            });
          }
        }
      }
      indexSignatureMap.forEach((entry) ***REMOVED***> {
        if (entry.declarations.length > 1) {
          for (const declaration of entry.declarations) {
            error(declaration, Diagnostics.Duplicate_index_signature_for_type_0, typeToString(entry.type));
          }
        }
      });
    }
  }
  function checkPropertyDeclaration(node) {
    if (!checkGrammarModifiers(node) && !checkGrammarProperty(node)) checkGrammarComputedPropertyName(node.name);
    checkVariableLikeDeclaration(node);
    setNodeLinksForPrivateIdentifierScope(node);
    if (hasSyntacticModifier(node, 64 /* Abstract */) && node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && node.initializer) {
      error(node, Diagnostics.Property_0_cannot_have_an_initializer_because_it_is_marked_abstract, declarationNameToString(node.name));
    }
  }
  function checkPropertySignature(node) {
    if (isPrivateIdentifier(node.name)) {
      error(node, Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
    }
    return checkPropertyDeclaration(node);
  }
  function checkMethodDeclaration(node) {
    if (!checkGrammarMethod(node)) checkGrammarComputedPropertyName(node.name);
    if (isMethodDeclaration(node) && node.asteriskToken && isIdentifier(node.name) && idText(node.name) ***REMOVED******REMOVED******REMOVED*** "constructor") {
      error(node.name, Diagnostics.Class_constructor_may_not_be_a_generator);
    }
    checkFunctionOrMethodDeclaration(node);
    if (hasSyntacticModifier(node, 64 /* Abstract */) && node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && node.body) {
      error(node, Diagnostics.Method_0_cannot_have_an_implementation_because_it_is_marked_abstract, declarationNameToString(node.name));
    }
    if (isPrivateIdentifier(node.name) && !getContainingClass(node)) {
      error(node, Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
    }
    setNodeLinksForPrivateIdentifierScope(node);
  }
  function setNodeLinksForPrivateIdentifierScope(node) {
    if (isPrivateIdentifier(node.name)) {
      if (languageVersion < LanguageFeatureMinimumTarget.PrivateNamesAndClassStaticBlocks || languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators || !useDefineForClassFields) {
        for (let lexicalScope ***REMOVED*** getEnclosingBlockScopeContainer(node); !!lexicalScope; lexicalScope ***REMOVED*** getEnclosingBlockScopeContainer(lexicalScope)) {
          getNodeLinks(lexicalScope).flags |***REMOVED*** 1048576 /* ContainsClassWithPrivateIdentifiers */;
        }
        if (isClassExpression(node.parent)) {
          const enclosingIterationStatement ***REMOVED*** getEnclosingIterationStatement(node.parent);
          if (enclosingIterationStatement) {
            getNodeLinks(node.name).flags |***REMOVED*** 32768 /* BlockScopedBindingInLoop */;
            getNodeLinks(enclosingIterationStatement).flags |***REMOVED*** 4096 /* LoopWithCapturedBlockScopedBinding */;
          }
        }
      }
    }
  }
  function checkClassStaticBlockDeclaration(node) {
    checkGrammarModifiers(node);
    forEachChild(node, checkSourceElement);
  }
  function checkConstructorDeclaration(node) {
    checkSignatureDeclaration(node);
    if (!checkGrammarConstructorTypeParameters(node)) checkGrammarConstructorTypeAnnotation(node);
    checkSourceElement(node.body);
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    const firstDeclaration ***REMOVED*** getDeclarationOfKind(symbol, node.kind);
    if (node ***REMOVED******REMOVED******REMOVED*** firstDeclaration) {
      checkFunctionOrConstructorSymbol(symbol);
    }
    if (nodeIsMissing(node.body)) {
      return;
    }
    addLazyDiagnostic(checkConstructorDeclarationDiagnostics);
    return;
    function isInstancePropertyWithInitializerOrPrivateIdentifierProperty(n) {
      if (isPrivateIdentifierClassElementDeclaration(n)) {
        return true;
      }
      return n.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && !isStatic(n) && !!n.initializer;
    }
    function checkConstructorDeclarationDiagnostics() {
      const containingClassDecl ***REMOVED*** node.parent;
      if (getClassExtendsHeritageElement(containingClassDecl)) {
        captureLexicalThis(node.parent, containingClassDecl);
        const classExtendsNull ***REMOVED*** classDeclarationExtendsNull(containingClassDecl);
        const superCall ***REMOVED*** findFirstSuperCall(node.body);
        if (superCall) {
          if (classExtendsNull) {
            error(superCall, Diagnostics.A_constructor_cannot_contain_a_super_call_when_its_class_extends_null);
          }
          const superCallShouldBeRootLevel ***REMOVED*** !emitStandardClassFields && (some(node.parent.members, isInstancePropertyWithInitializerOrPrivateIdentifierProperty) || some(node.parameters, (p) ***REMOVED***> hasSyntacticModifier(p, 31 /* ParameterPropertyModifier */)));
          if (superCallShouldBeRootLevel) {
            if (!superCallIsRootLevelInConstructor(superCall, node.body)) {
              error(superCall, Diagnostics.A_super_call_must_be_a_root_level_statement_within_a_constructor_of_a_derived_class_that_contains_initialized_properties_parameter_properties_or_private_identifiers);
            } else {
              let superCallStatement;
              for (const statement of node.body.statements) {
                if (isExpressionStatement(statement) && isSuperCall(skipOuterExpressions(statement.expression))) {
                  superCallStatement ***REMOVED*** statement;
                  break;
                }
                if (nodeImmediatelyReferencesSuperOrThis(statement)) {
                  break;
                }
              }
              if (superCallStatement ***REMOVED******REMOVED******REMOVED*** void 0) {
                error(node, Diagnostics.A_super_call_must_be_the_first_statement_in_the_constructor_to_refer_to_super_or_this_when_a_derived_class_contains_initialized_properties_parameter_properties_or_private_identifiers);
              }
            }
          }
        } else if (!classExtendsNull) {
          error(node, Diagnostics.Constructors_for_derived_classes_must_contain_a_super_call);
        }
      }
    }
  }
  function superCallIsRootLevelInConstructor(superCall, body) {
    const superCallParent ***REMOVED*** walkUpParenthesizedExpressions(superCall.parent);
    return isExpressionStatement(superCallParent) && superCallParent.parent ***REMOVED******REMOVED******REMOVED*** body;
  }
  function nodeImmediatelyReferencesSuperOrThis(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ || node.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      return true;
    }
    if (isThisContainerOrFunctionBlock(node)) {
      return false;
    }
    return !!forEachChild(node, nodeImmediatelyReferencesSuperOrThis);
  }
  function checkAccessorDeclaration(node) {
    if (isIdentifier(node.name) && idText(node.name) ***REMOVED******REMOVED******REMOVED*** "constructor" && isClassLike(node.parent)) {
      error(node.name, Diagnostics.Class_constructor_may_not_be_an_accessor);
    }
    addLazyDiagnostic(checkAccessorDeclarationDiagnostics);
    checkSourceElement(node.body);
    setNodeLinksForPrivateIdentifierScope(node);
    function checkAccessorDeclarationDiagnostics() {
      if (!checkGrammarFunctionLikeDeclaration(node) && !checkGrammarAccessor(node)) checkGrammarComputedPropertyName(node.name);
      checkDecorators(node);
      checkSignatureDeclaration(node);
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
        if (!(node.flags & 33554432 /* Ambient */) && nodeIsPresent(node.body) && node.flags & 512 /* HasImplicitReturn */) {
          if (!(node.flags & 1024 /* HasExplicitReturn */)) {
            error(node.name, Diagnostics.A_get_accessor_must_return_a_value);
          }
        }
      }
      if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        checkComputedPropertyName(node.name);
      }
      if (hasBindableName(node)) {
        const symbol ***REMOVED*** getSymbolOfDeclaration(node);
        const getter ***REMOVED*** getDeclarationOfKind(symbol, 178 /* GetAccessor */);
        const setter ***REMOVED*** getDeclarationOfKind(symbol, 179 /* SetAccessor */);
        if (getter && setter && !(getNodeCheckFlags(getter) & 1 /* TypeChecked */)) {
          getNodeLinks(getter).flags |***REMOVED*** 1 /* TypeChecked */;
          const getterFlags ***REMOVED*** getEffectiveModifierFlags(getter);
          const setterFlags ***REMOVED*** getEffectiveModifierFlags(setter);
          if ((getterFlags & 64 /* Abstract */) !***REMOVED******REMOVED*** (setterFlags & 64 /* Abstract */)) {
            error(getter.name, Diagnostics.Accessors_must_both_be_abstract_or_non_abstract);
            error(setter.name, Diagnostics.Accessors_must_both_be_abstract_or_non_abstract);
          }
          if (getterFlags & 4 /* Protected */ && !(setterFlags & (4 /* Protected */ | 2 /* Private */)) || getterFlags & 2 /* Private */ && !(setterFlags & 2 /* Private */)) {
            error(getter.name, Diagnostics.A_get_accessor_must_be_at_least_as_accessible_as_the_setter);
            error(setter.name, Diagnostics.A_get_accessor_must_be_at_least_as_accessible_as_the_setter);
          }
        }
      }
      const returnType ***REMOVED*** getTypeOfAccessors(getSymbolOfDeclaration(node));
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
        checkAllCodePathsInNonVoidFunctionReturnOrThrow(node, returnType);
      }
    }
  }
  function checkMissingDeclaration(node) {
    checkDecorators(node);
  }
  function getEffectiveTypeArgumentAtIndex(node, typeParameters, index) {
    if (node.typeArguments && index < node.typeArguments.length) {
      return getTypeFromTypeNode(node.typeArguments[index]);
    }
    return getEffectiveTypeArguments(node, typeParameters)[index];
  }
  function getEffectiveTypeArguments(node, typeParameters) {
    return fillMissingTypeArguments(map(node.typeArguments, getTypeFromTypeNode), typeParameters, getMinTypeArgumentCount(typeParameters), isInJSFile(node));
  }
  function checkTypeArgumentConstraints(node, typeParameters) {
    let typeArguments;
    let mapper;
    let result ***REMOVED*** true;
    for (let i ***REMOVED*** 0; i < typeParameters.length; i++) {
      const constraint ***REMOVED*** getConstraintOfTypeParameter(typeParameters[i]);
      if (constraint) {
        if (!typeArguments) {
          typeArguments ***REMOVED*** getEffectiveTypeArguments(node, typeParameters);
          mapper ***REMOVED*** createTypeMapper(typeParameters, typeArguments);
        }
        result ***REMOVED*** result && checkTypeAssignableTo(
          typeArguments[i],
          instantiateType(constraint, mapper),
          node.typeArguments[i],
          Diagnostics.Type_0_does_not_satisfy_the_constraint_1
        );
      }
    }
    return result;
  }
  function getTypeParametersForTypeAndSymbol(type, symbol) {
    if (!isErrorType(type)) {
      return symbol.flags & 524288 /* TypeAlias */ && getSymbolLinks(symbol).typeParameters || (getObjectFlags(type) & 4 /* Reference */ ? type.target.localTypeParameters : void 0);
    }
    return void 0;
  }
  function getTypeParametersForTypeReferenceOrImport(node) {
    const type ***REMOVED*** getTypeFromTypeNode(node);
    if (!isErrorType(type)) {
      const symbol ***REMOVED*** getNodeLinks(node).resolvedSymbol;
      if (symbol) {
        return getTypeParametersForTypeAndSymbol(type, symbol);
      }
    }
    return void 0;
  }
  function checkTypeReferenceNode(node) {
    checkGrammarTypeArguments(node, node.typeArguments);
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ && !isInJSFile(node) && !isInJSDoc(node) && node.typeArguments && node.typeName.end !***REMOVED******REMOVED*** node.typeArguments.pos) {
      const sourceFile ***REMOVED*** getSourceFileOfNode(node);
      if (scanTokenAtPosition(sourceFile, node.typeName.end) ***REMOVED******REMOVED******REMOVED*** 25 /* DotToken */) {
        grammarErrorAtPos(node, skipTrivia(sourceFile.text, node.typeName.end), 1, Diagnostics.JSDoc_types_can_only_be_used_inside_documentation_comments);
      }
    }
    forEach(node.typeArguments, checkSourceElement);
    checkTypeReferenceOrImport(node);
  }
  function checkTypeReferenceOrImport(node) {
    const type ***REMOVED*** getTypeFromTypeNode(node);
    if (!isErrorType(type)) {
      if (node.typeArguments) {
        addLazyDiagnostic(() ***REMOVED***> {
          const typeParameters ***REMOVED*** getTypeParametersForTypeReferenceOrImport(node);
          if (typeParameters) {
            checkTypeArgumentConstraints(node, typeParameters);
          }
        });
      }
      const symbol ***REMOVED*** getNodeLinks(node).resolvedSymbol;
      if (symbol) {
        if (some(symbol.declarations, (d) ***REMOVED***> isTypeDeclaration(d) && !!(d.flags & 536870912 /* Deprecated */))) {
          addDeprecatedSuggestion(
            getDeprecatedSuggestionNode(node),
            symbol.declarations,
            symbol.escapedName
          );
        }
      }
    }
  }
  function getTypeArgumentConstraint(node) {
    const typeReferenceNode ***REMOVED*** tryCast(node.parent, isTypeReferenceType);
    if (!typeReferenceNode) return void 0;
    const typeParameters ***REMOVED*** getTypeParametersForTypeReferenceOrImport(typeReferenceNode);
    if (!typeParameters) return void 0;
    const constraint ***REMOVED*** getConstraintOfTypeParameter(typeParameters[typeReferenceNode.typeArguments.indexOf(node)]);
    return constraint && instantiateType(constraint, createTypeMapper(typeParameters, getEffectiveTypeArguments(typeReferenceNode, typeParameters)));
  }
  function checkTypeQuery(node) {
    getTypeFromTypeQueryNode(node);
  }
  function checkTypeLiteral(node) {
    forEach(node.members, checkSourceElement);
    addLazyDiagnostic(checkTypeLiteralDiagnostics);
    function checkTypeLiteralDiagnostics() {
      const type ***REMOVED*** getTypeFromTypeLiteralOrFunctionOrConstructorTypeNode(node);
      checkIndexConstraints(type, type.symbol);
      checkTypeForDuplicateIndexSignatures(node);
      checkObjectTypeForDuplicateDeclarations(node);
    }
  }
  function checkArrayType(node) {
    checkSourceElement(node.elementType);
  }
  function checkTupleType(node) {
    let seenOptionalElement ***REMOVED*** false;
    let seenRestElement ***REMOVED*** false;
    for (const e of node.elements) {
      let flags ***REMOVED*** getTupleElementFlags(e);
      if (flags & 8 /* Variadic */) {
        const type ***REMOVED*** getTypeFromTypeNode(e.type);
        if (!isArrayLikeType(type)) {
          error(e, Diagnostics.A_rest_element_type_must_be_an_array_type);
          break;
        }
        if (isArrayType(type) || isTupleType(type) && type.target.combinedFlags & 4 /* Rest */) {
          flags |***REMOVED*** 4 /* Rest */;
        }
      }
      if (flags & 4 /* Rest */) {
        if (seenRestElement) {
          grammarErrorOnNode(e, Diagnostics.A_rest_element_cannot_follow_another_rest_element);
          break;
        }
        seenRestElement ***REMOVED*** true;
      } else if (flags & 2 /* Optional */) {
        if (seenRestElement) {
          grammarErrorOnNode(e, Diagnostics.An_optional_element_cannot_follow_a_rest_element);
          break;
        }
        seenOptionalElement ***REMOVED*** true;
      } else if (flags & 1 /* Required */ && seenOptionalElement) {
        grammarErrorOnNode(e, Diagnostics.A_required_element_cannot_follow_an_optional_element);
        break;
      }
    }
    forEach(node.elements, checkSourceElement);
    getTypeFromTypeNode(node);
  }
  function checkUnionOrIntersectionType(node) {
    forEach(node.types, checkSourceElement);
    getTypeFromTypeNode(node);
  }
  function checkIndexedAccessIndexType(type, accessNode) {
    if (!(type.flags & 8388608 /* IndexedAccess */)) {
      return type;
    }
    const objectType ***REMOVED*** type.objectType;
    const indexType ***REMOVED*** type.indexType;
    const objectIndexType ***REMOVED*** isGenericMappedType(objectType) && getMappedTypeNameTypeKind(objectType) ***REMOVED******REMOVED******REMOVED*** 2 /* Remapping */ ? getIndexTypeForMappedType(objectType, 0 /* None */) : getIndexType(objectType, 0 /* None */);
    const hasNumberIndexInfo ***REMOVED*** !!getIndexInfoOfType(objectType, numberType);
    if (everyType(indexType, (t) ***REMOVED***> isTypeAssignableTo(t, objectIndexType) || hasNumberIndexInfo && isApplicableIndexType(t, numberType))) {
      if (accessNode.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ && isAssignmentTarget(accessNode) && getObjectFlags(objectType) & 32 /* Mapped */ && getMappedTypeModifiers(objectType) & 1 /* IncludeReadonly */) {
        error(accessNode, Diagnostics.Index_signature_in_type_0_only_permits_reading, typeToString(objectType));
      }
      return type;
    }
    if (isGenericObjectType(objectType)) {
      const propertyName ***REMOVED*** getPropertyNameFromIndex(indexType, accessNode);
      if (propertyName) {
        const propertySymbol ***REMOVED*** forEachType(getApparentType(objectType), (t) ***REMOVED***> getPropertyOfType(t, propertyName));
        if (propertySymbol && getDeclarationModifierFlagsFromSymbol(propertySymbol) & 6 /* NonPublicAccessibilityModifier */) {
          error(accessNode, Diagnostics.Private_or_protected_member_0_cannot_be_accessed_on_a_type_parameter, unescapeLeadingUnderscores(propertyName));
          return errorType;
        }
      }
    }
    error(accessNode, Diagnostics.Type_0_cannot_be_used_to_index_type_1, typeToString(indexType), typeToString(objectType));
    return errorType;
  }
  function checkIndexedAccessType(node) {
    checkSourceElement(node.objectType);
    checkSourceElement(node.indexType);
    checkIndexedAccessIndexType(getTypeFromIndexedAccessTypeNode(node), node);
  }
  function checkMappedType(node) {
    checkGrammarMappedType(node);
    checkSourceElement(node.typeParameter);
    checkSourceElement(node.nameType);
    checkSourceElement(node.type);
    if (!node.type) {
      reportImplicitAny(node, anyType);
    }
    const type ***REMOVED*** getTypeFromMappedTypeNode(node);
    const nameType ***REMOVED*** getNameTypeFromMappedType(type);
    if (nameType) {
      checkTypeAssignableTo(nameType, stringNumberSymbolType, node.nameType);
    } else {
      const constraintType ***REMOVED*** getConstraintTypeFromMappedType(type);
      checkTypeAssignableTo(constraintType, stringNumberSymbolType, getEffectiveConstraintOfTypeParameter(node.typeParameter));
    }
  }
  function checkGrammarMappedType(node) {
    var _a;
    if ((_a ***REMOVED*** node.members) ***REMOVED******REMOVED*** null ? void 0 : _a.length) {
      return grammarErrorOnNode(node.members[0], Diagnostics.A_mapped_type_may_not_declare_properties_or_methods);
    }
  }
  function checkThisType(node) {
    getTypeFromThisTypeNode(node);
  }
  function checkTypeOperator(node) {
    checkGrammarTypeOperatorNode(node);
    checkSourceElement(node.type);
  }
  function checkConditionalType(node) {
    forEachChild(node, checkSourceElement);
  }
  function checkInferType(node) {
    if (!findAncestor(node, (n) ***REMOVED***> n.parent && n.parent.kind ***REMOVED******REMOVED******REMOVED*** 195 /* ConditionalType */ && n.parent.extendsType ***REMOVED******REMOVED******REMOVED*** n)) {
      grammarErrorOnNode(node, Diagnostics.infer_declarations_are_only_permitted_in_the_extends_clause_of_a_conditional_type);
    }
    checkSourceElement(node.typeParameter);
    const symbol ***REMOVED*** getSymbolOfDeclaration(node.typeParameter);
    if (symbol.declarations && symbol.declarations.length > 1) {
      const links ***REMOVED*** getSymbolLinks(symbol);
      if (!links.typeParametersChecked) {
        links.typeParametersChecked ***REMOVED*** true;
        const typeParameter ***REMOVED*** getDeclaredTypeOfTypeParameter(symbol);
        const declarations ***REMOVED*** getDeclarationsOfKind(symbol, 169 /* TypeParameter */);
        if (!areTypeParametersIdentical(declarations, [typeParameter], (decl) ***REMOVED***> [decl])) {
          const name ***REMOVED*** symbolToString(symbol);
          for (const declaration of declarations) {
            error(declaration.name, Diagnostics.All_declarations_of_0_must_have_identical_constraints, name);
          }
        }
      }
    }
    registerForUnusedIdentifiersCheck(node);
  }
  function checkTemplateLiteralType(node) {
    for (const span of node.templateSpans) {
      checkSourceElement(span.type);
      const type ***REMOVED*** getTypeFromTypeNode(span.type);
      checkTypeAssignableTo(type, templateConstraintType, span.type);
    }
    getTypeFromTypeNode(node);
  }
  function checkImportType(node) {
    checkSourceElement(node.argument);
    if (node.attributes) {
      getResolutionModeOverride(node.attributes, grammarErrorOnNode);
    }
    checkTypeReferenceOrImport(node);
  }
  function checkNamedTupleMember(node) {
    if (node.dotDotDotToken && node.questionToken) {
      grammarErrorOnNode(node, Diagnostics.A_tuple_member_cannot_be_both_optional_and_rest);
    }
    if (node.type.kind ***REMOVED******REMOVED******REMOVED*** 191 /* OptionalType */) {
      grammarErrorOnNode(node.type, Diagnostics.A_labeled_tuple_element_is_declared_as_optional_with_a_question_mark_after_the_name_and_before_the_colon_rather_than_after_the_type);
    }
    if (node.type.kind ***REMOVED******REMOVED******REMOVED*** 192 /* RestType */) {
      grammarErrorOnNode(node.type, Diagnostics.A_labeled_tuple_element_is_declared_as_rest_with_a_before_the_name_rather_than_before_the_type);
    }
    checkSourceElement(node.type);
    getTypeFromTypeNode(node);
  }
  function isPrivateWithinAmbient(node) {
    return (hasEffectiveModifier(node, 2 /* Private */) || isPrivateIdentifierClassElementDeclaration(node)) && !!(node.flags & 33554432 /* Ambient */);
  }
  function getEffectiveDeclarationFlags(n, flagsToCheck) {
    let flags ***REMOVED*** getCombinedModifierFlagsCached(n);
    if (n.parent.kind !***REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ && n.parent.kind !***REMOVED******REMOVED*** 264 /* ClassDeclaration */ && n.parent.kind !***REMOVED******REMOVED*** 232 /* ClassExpression */ && n.flags & 33554432 /* Ambient */) {
      const container ***REMOVED*** getEnclosingContainer(n);
      if (container && container.flags & 128 /* ExportContext */ && !(flags & 128 /* Ambient */) && !(isModuleBlock(n.parent) && isModuleDeclaration(n.parent.parent) && isGlobalScopeAugmentation(n.parent.parent))) {
        flags |***REMOVED*** 32 /* Export */;
      }
      flags |***REMOVED*** 128 /* Ambient */;
    }
    return flags & flagsToCheck;
  }
  function checkFunctionOrConstructorSymbol(symbol) {
    addLazyDiagnostic(() ***REMOVED***> checkFunctionOrConstructorSymbolWorker(symbol));
  }
  function checkFunctionOrConstructorSymbolWorker(symbol) {
    function getCanonicalOverload(overloads, implementation) {
      const implementationSharesContainerWithFirstOverload ***REMOVED*** implementation !***REMOVED******REMOVED*** void 0 && implementation.parent ***REMOVED******REMOVED******REMOVED*** overloads[0].parent;
      return implementationSharesContainerWithFirstOverload ? implementation : overloads[0];
    }
    function checkFlagAgreementBetweenOverloads(overloads, implementation, flagsToCheck2, someOverloadFlags, allOverloadFlags) {
      const someButNotAllOverloadFlags ***REMOVED*** someOverloadFlags ^ allOverloadFlags;
      if (someButNotAllOverloadFlags !***REMOVED******REMOVED*** 0) {
        const canonicalFlags ***REMOVED*** getEffectiveDeclarationFlags(getCanonicalOverload(overloads, implementation), flagsToCheck2);
        group(overloads, (o) ***REMOVED***> getSourceFileOfNode(o).fileName).forEach((overloadsInFile) ***REMOVED***> {
          const canonicalFlagsForFile ***REMOVED*** getEffectiveDeclarationFlags(getCanonicalOverload(overloadsInFile, implementation), flagsToCheck2);
          for (const o of overloadsInFile) {
            const deviation ***REMOVED*** getEffectiveDeclarationFlags(o, flagsToCheck2) ^ canonicalFlags;
            const deviationInFile ***REMOVED*** getEffectiveDeclarationFlags(o, flagsToCheck2) ^ canonicalFlagsForFile;
            if (deviationInFile & 32 /* Export */) {
              error(getNameOfDeclaration(o), Diagnostics.Overload_signatures_must_all_be_exported_or_non_exported);
            } else if (deviationInFile & 128 /* Ambient */) {
              error(getNameOfDeclaration(o), Diagnostics.Overload_signatures_must_all_be_ambient_or_non_ambient);
            } else if (deviation & (2 /* Private */ | 4 /* Protected */)) {
              error(getNameOfDeclaration(o) || o, Diagnostics.Overload_signatures_must_all_be_public_private_or_protected);
            } else if (deviation & 64 /* Abstract */) {
              error(getNameOfDeclaration(o), Diagnostics.Overload_signatures_must_all_be_abstract_or_non_abstract);
            }
          }
        });
      }
    }
    function checkQuestionTokenAgreementBetweenOverloads(overloads, implementation, someHaveQuestionToken2, allHaveQuestionToken2) {
      if (someHaveQuestionToken2 !***REMOVED******REMOVED*** allHaveQuestionToken2) {
        const canonicalHasQuestionToken ***REMOVED*** hasQuestionToken(getCanonicalOverload(overloads, implementation));
        forEach(overloads, (o) ***REMOVED***> {
          const deviation ***REMOVED*** hasQuestionToken(o) !***REMOVED******REMOVED*** canonicalHasQuestionToken;
          if (deviation) {
            error(getNameOfDeclaration(o), Diagnostics.Overload_signatures_must_all_be_optional_or_required);
          }
        });
      }
    }
    const flagsToCheck ***REMOVED*** 32 /* Export */ | 128 /* Ambient */ | 2 /* Private */ | 4 /* Protected */ | 64 /* Abstract */;
    let someNodeFlags ***REMOVED*** 0 /* None */;
    let allNodeFlags ***REMOVED*** flagsToCheck;
    let someHaveQuestionToken ***REMOVED*** false;
    let allHaveQuestionToken ***REMOVED*** true;
    let hasOverloads ***REMOVED*** false;
    let bodyDeclaration;
    let lastSeenNonAmbientDeclaration;
    let previousDeclaration;
    const declarations ***REMOVED*** symbol.declarations;
    const isConstructor ***REMOVED*** (symbol.flags & 16384 /* Constructor */) !***REMOVED******REMOVED*** 0;
    function reportImplementationExpectedError(node) {
      if (node.name && nodeIsMissing(node.name)) {
        return;
      }
      let seen ***REMOVED*** false;
      const subsequentNode ***REMOVED*** forEachChild(node.parent, (c) ***REMOVED***> {
        if (seen) {
          return c;
        } else {
          seen ***REMOVED*** c ***REMOVED******REMOVED******REMOVED*** node;
        }
      });
      if (subsequentNode && subsequentNode.pos ***REMOVED******REMOVED******REMOVED*** node.end) {
        if (subsequentNode.kind ***REMOVED******REMOVED******REMOVED*** node.kind) {
          const errorNode2 ***REMOVED*** subsequentNode.name || subsequentNode;
          const subsequentName ***REMOVED*** subsequentNode.name;
          if (node.name && subsequentName && // both are private identifiers
          (isPrivateIdentifier(node.name) && isPrivateIdentifier(subsequentName) && node.name.escapedText ***REMOVED******REMOVED******REMOVED*** subsequentName.escapedText || // Both are computed property names
          isComputedPropertyName(node.name) && isComputedPropertyName(subsequentName) && isTypeIdenticalTo(checkComputedPropertyName(node.name), checkComputedPropertyName(subsequentName)) || // Both are literal property names that are the same.
          isPropertyNameLiteral(node.name) && isPropertyNameLiteral(subsequentName) && getEscapedTextOfIdentifierOrLiteral(node.name) ***REMOVED******REMOVED******REMOVED*** getEscapedTextOfIdentifierOrLiteral(subsequentName))) {
            const reportError ***REMOVED*** (node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */) && isStatic(node) !***REMOVED******REMOVED*** isStatic(subsequentNode);
            if (reportError) {
              const diagnostic ***REMOVED*** isStatic(node) ? Diagnostics.Function_overload_must_be_static : Diagnostics.Function_overload_must_not_be_static;
              error(errorNode2, diagnostic);
            }
            return;
          }
          if (nodeIsPresent(subsequentNode.body)) {
            error(errorNode2, Diagnostics.Function_implementation_name_must_be_0, declarationNameToString(node.name));
            return;
          }
        }
      }
      const errorNode ***REMOVED*** node.name || node;
      if (isConstructor) {
        error(errorNode, Diagnostics.Constructor_implementation_is_missing);
      } else {
        if (hasSyntacticModifier(node, 64 /* Abstract */)) {
          error(errorNode, Diagnostics.All_declarations_of_an_abstract_method_must_be_consecutive);
        } else {
          error(errorNode, Diagnostics.Function_implementation_is_missing_or_not_immediately_following_the_declaration);
        }
      }
    }
    let duplicateFunctionDeclaration ***REMOVED*** false;
    let multipleConstructorImplementation ***REMOVED*** false;
    let hasNonAmbientClass ***REMOVED*** false;
    const functionDeclarations ***REMOVED*** [];
    if (declarations) {
      for (const current of declarations) {
        const node ***REMOVED*** current;
        const inAmbientContext ***REMOVED*** node.flags & 33554432 /* Ambient */;
        const inAmbientContextOrInterface ***REMOVED*** node.parent && (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */) || inAmbientContext;
        if (inAmbientContextOrInterface) {
          previousDeclaration ***REMOVED*** void 0;
        }
        if ((node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 232 /* ClassExpression */) && !inAmbientContext) {
          hasNonAmbientClass ***REMOVED*** true;
        }
        if (node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
          functionDeclarations.push(node);
          const currentNodeFlags ***REMOVED*** getEffectiveDeclarationFlags(node, flagsToCheck);
          someNodeFlags |***REMOVED*** currentNodeFlags;
          allNodeFlags &***REMOVED*** currentNodeFlags;
          someHaveQuestionToken ***REMOVED*** someHaveQuestionToken || hasQuestionToken(node);
          allHaveQuestionToken ***REMOVED*** allHaveQuestionToken && hasQuestionToken(node);
          const bodyIsPresent ***REMOVED*** nodeIsPresent(node.body);
          if (bodyIsPresent && bodyDeclaration) {
            if (isConstructor) {
              multipleConstructorImplementation ***REMOVED*** true;
            } else {
              duplicateFunctionDeclaration ***REMOVED*** true;
            }
          } else if ((previousDeclaration ***REMOVED******REMOVED*** null ? void 0 : previousDeclaration.parent) ***REMOVED******REMOVED******REMOVED*** node.parent && previousDeclaration.end !***REMOVED******REMOVED*** node.pos) {
            reportImplementationExpectedError(previousDeclaration);
          }
          if (bodyIsPresent) {
            if (!bodyDeclaration) {
              bodyDeclaration ***REMOVED*** node;
            }
          } else {
            hasOverloads ***REMOVED*** true;
          }
          previousDeclaration ***REMOVED*** node;
          if (!inAmbientContextOrInterface) {
            lastSeenNonAmbientDeclaration ***REMOVED*** node;
          }
        }
        if (isInJSFile(current) && isFunctionLike(current) && current.jsDoc) {
          hasOverloads ***REMOVED*** length(getJSDocOverloadTags(current)) > 0;
        }
      }
    }
    if (multipleConstructorImplementation) {
      forEach(functionDeclarations, (declaration) ***REMOVED***> {
        error(declaration, Diagnostics.Multiple_constructor_implementations_are_not_allowed);
      });
    }
    if (duplicateFunctionDeclaration) {
      forEach(functionDeclarations, (declaration) ***REMOVED***> {
        error(getNameOfDeclaration(declaration) || declaration, Diagnostics.Duplicate_function_implementation);
      });
    }
    if (hasNonAmbientClass && !isConstructor && symbol.flags & 16 /* Function */ && declarations) {
      const relatedDiagnostics ***REMOVED*** filter(declarations, (d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */).map((d) ***REMOVED***> createDiagnosticForNode(d, Diagnostics.Consider_adding_a_declare_modifier_to_this_class));
      forEach(declarations, (declaration) ***REMOVED***> {
        const diagnostic ***REMOVED*** declaration.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ ? Diagnostics.Class_declaration_cannot_implement_overload_list_for_0 : declaration.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ ? Diagnostics.Function_with_bodies_can_only_merge_with_classes_that_are_ambient : void 0;
        if (diagnostic) {
          addRelatedInfo(
            error(getNameOfDeclaration(declaration) || declaration, diagnostic, symbolName(symbol)),
            ...relatedDiagnostics
          );
        }
      });
    }
    if (lastSeenNonAmbientDeclaration && !lastSeenNonAmbientDeclaration.body && !hasSyntacticModifier(lastSeenNonAmbientDeclaration, 64 /* Abstract */) && !lastSeenNonAmbientDeclaration.questionToken) {
      reportImplementationExpectedError(lastSeenNonAmbientDeclaration);
    }
    if (hasOverloads) {
      if (declarations) {
        checkFlagAgreementBetweenOverloads(declarations, bodyDeclaration, flagsToCheck, someNodeFlags, allNodeFlags);
        checkQuestionTokenAgreementBetweenOverloads(declarations, bodyDeclaration, someHaveQuestionToken, allHaveQuestionToken);
      }
      if (bodyDeclaration) {
        const signatures ***REMOVED*** getSignaturesOfSymbol(symbol);
        const bodySignature ***REMOVED*** getSignatureFromDeclaration(bodyDeclaration);
        for (const signature of signatures) {
          if (!isImplementationCompatibleWithOverload(bodySignature, signature)) {
            const errorNode ***REMOVED*** signature.declaration && isJSDocSignature(signature.declaration) ? signature.declaration.parent.tagName : signature.declaration;
            addRelatedInfo(
              error(errorNode, Diagnostics.This_overload_signature_is_not_compatible_with_its_implementation_signature),
              createDiagnosticForNode(bodyDeclaration, Diagnostics.The_implementation_signature_is_declared_here)
            );
            break;
          }
        }
      }
    }
  }
  function checkExportsOnMergedDeclarations(node) {
    addLazyDiagnostic(() ***REMOVED***> checkExportsOnMergedDeclarationsWorker(node));
  }
  function checkExportsOnMergedDeclarationsWorker(node) {
    let symbol ***REMOVED*** node.localSymbol;
    if (!symbol) {
      symbol ***REMOVED*** getSymbolOfDeclaration(node);
      if (!symbol.exportSymbol) {
        return;
      }
    }
    if (getDeclarationOfKind(symbol, node.kind) !***REMOVED******REMOVED*** node) {
      return;
    }
    let exportedDeclarationSpaces ***REMOVED*** 0 /* None */;
    let nonExportedDeclarationSpaces ***REMOVED*** 0 /* None */;
    let defaultExportedDeclarationSpaces ***REMOVED*** 0 /* None */;
    for (const d of symbol.declarations) {
      const declarationSpaces ***REMOVED*** getDeclarationSpaces(d);
      const effectiveDeclarationFlags ***REMOVED*** getEffectiveDeclarationFlags(d, 32 /* Export */ | 2048 /* Default */);
      if (effectiveDeclarationFlags & 32 /* Export */) {
        if (effectiveDeclarationFlags & 2048 /* Default */) {
          defaultExportedDeclarationSpaces |***REMOVED*** declarationSpaces;
        } else {
          exportedDeclarationSpaces |***REMOVED*** declarationSpaces;
        }
      } else {
        nonExportedDeclarationSpaces |***REMOVED*** declarationSpaces;
      }
    }
    const nonDefaultExportedDeclarationSpaces ***REMOVED*** exportedDeclarationSpaces | nonExportedDeclarationSpaces;
    const commonDeclarationSpacesForExportsAndLocals ***REMOVED*** exportedDeclarationSpaces & nonExportedDeclarationSpaces;
    const commonDeclarationSpacesForDefaultAndNonDefault ***REMOVED*** defaultExportedDeclarationSpaces & nonDefaultExportedDeclarationSpaces;
    if (commonDeclarationSpacesForExportsAndLocals || commonDeclarationSpacesForDefaultAndNonDefault) {
      for (const d of symbol.declarations) {
        const declarationSpaces ***REMOVED*** getDeclarationSpaces(d);
        const name ***REMOVED*** getNameOfDeclaration(d);
        if (declarationSpaces & commonDeclarationSpacesForDefaultAndNonDefault) {
          error(name, Diagnostics.Merged_declaration_0_cannot_include_a_default_export_declaration_Consider_adding_a_separate_export_default_0_declaration_instead, declarationNameToString(name));
        } else if (declarationSpaces & commonDeclarationSpacesForExportsAndLocals) {
          error(name, Diagnostics.Individual_declarations_in_merged_declaration_0_must_be_all_exported_or_all_local, declarationNameToString(name));
        }
      }
    }
    function getDeclarationSpaces(decl) {
      let d ***REMOVED*** decl;
      switch (d.kind) {
        case 265 /* InterfaceDeclaration */:
        case 266 /* TypeAliasDeclaration */:
        // A jsdoc typedef and callback are, by definition, type aliases.
        // falls through
        case 347 /* JSDocTypedefTag */:
        case 339 /* JSDocCallbackTag */:
        case 341 /* JSDocEnumTag */:
          return 2 /* ExportType */;
        case 268 /* ModuleDeclaration */:
          return isAmbientModule(d) || getModuleInstanceState(d) !***REMOVED******REMOVED*** 0 /* NonInstantiated */ ? 4 /* ExportNamespace */ | 1 /* ExportValue */ : 4 /* ExportNamespace */;
        case 264 /* ClassDeclaration */:
        case 267 /* EnumDeclaration */:
        case 307 /* EnumMember */:
          return 2 /* ExportType */ | 1 /* ExportValue */;
        case 308 /* SourceFile */:
          return 2 /* ExportType */ | 1 /* ExportValue */ | 4 /* ExportNamespace */;
        case 278 /* ExportAssignment */:
        case 227 /* BinaryExpression */:
          const node2 ***REMOVED*** d;
          const expression ***REMOVED*** isExportAssignment(node2) ? node2.expression : node2.right;
          if (!isEntityNameExpression(expression)) {
            return 1 /* ExportValue */;
          }
          d ***REMOVED*** expression;
        // The below options all declare an Alias, which is allowed to merge with other values within the importing module.
        // falls through
        case 272 /* ImportEqualsDeclaration */:
        case 275 /* NamespaceImport */:
        case 274 /* ImportClause */:
          let result ***REMOVED*** 0 /* None */;
          const target ***REMOVED*** resolveAlias(getSymbolOfDeclaration(d));
          forEach(target.declarations, (d2) ***REMOVED***> {
            result |***REMOVED*** getDeclarationSpaces(d2);
          });
          return result;
        case 261 /* VariableDeclaration */:
        case 209 /* BindingElement */:
        case 263 /* FunctionDeclaration */:
        case 277 /* ImportSpecifier */:
        // https://github.com/Microsoft/TypeScript/pull/7591
        case 80 /* Identifier */:
          return 1 /* ExportValue */;
        case 174 /* MethodSignature */:
        case 172 /* PropertySignature */:
          return 2 /* ExportType */;
        default:
          return Debug.failBadSyntaxKind(d);
      }
    }
  }
  function getAwaitedTypeOfPromise(type, errorNode, diagnosticMessage, ...args) {
    const promisedType ***REMOVED*** getPromisedTypeOfPromise(type, errorNode);
    return promisedType && getAwaitedType(promisedType, errorNode, diagnosticMessage, ...args);
  }
  function getPromisedTypeOfPromise(type, errorNode, thisTypeForErrorOut) {
    if (isTypeAny(type)) {
      return void 0;
    }
    const typeAsPromise ***REMOVED*** type;
    if (typeAsPromise.promisedTypeOfPromise) {
      return typeAsPromise.promisedTypeOfPromise;
    }
    if (isReferenceToType(type, getGlobalPromiseType(
      /*reportErrors*/
      false
    ))) {
      return typeAsPromise.promisedTypeOfPromise ***REMOVED*** getTypeArguments(type)[0];
    }
    if (allTypesAssignableToKind(getBaseConstraintOrType(type), 402784252 /* Primitive */ | 131072 /* Never */)) {
      return void 0;
    }
    const thenFunction ***REMOVED*** getTypeOfPropertyOfType(type, "then");
    if (isTypeAny(thenFunction)) {
      return void 0;
    }
    const thenSignatures ***REMOVED*** thenFunction ? getSignaturesOfType(thenFunction, 0 /* Call */) : emptyArray;
    if (thenSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      if (errorNode) {
        error(errorNode, Diagnostics.A_promise_must_have_a_then_method);
      }
      return void 0;
    }
    let thisTypeForError;
    let candidates;
    for (const thenSignature of thenSignatures) {
      const thisType ***REMOVED*** getThisTypeOfSignature(thenSignature);
      if (thisType && thisType !***REMOVED******REMOVED*** voidType && !isTypeRelatedTo(type, thisType, subtypeRelation)) {
        thisTypeForError ***REMOVED*** thisType;
      } else {
        candidates ***REMOVED*** append(candidates, thenSignature);
      }
    }
    if (!candidates) {
      Debug.assertIsDefined(thisTypeForError);
      if (thisTypeForErrorOut) {
        thisTypeForErrorOut.value ***REMOVED*** thisTypeForError;
      }
      if (errorNode) {
        error(errorNode, Diagnostics.The_this_context_of_type_0_is_not_assignable_to_method_s_this_of_type_1, typeToString(type), typeToString(thisTypeForError));
      }
      return void 0;
    }
    const onfulfilledParameterType ***REMOVED*** getTypeWithFacts(getUnionType(map(candidates, getTypeOfFirstParameterOfSignature)), 2097152 /* NEUndefinedOrNull */);
    if (isTypeAny(onfulfilledParameterType)) {
      return void 0;
    }
    const onfulfilledParameterSignatures ***REMOVED*** getSignaturesOfType(onfulfilledParameterType, 0 /* Call */);
    if (onfulfilledParameterSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      if (errorNode) {
        error(errorNode, Diagnostics.The_first_parameter_of_the_then_method_of_a_promise_must_be_a_callback);
      }
      return void 0;
    }
    return typeAsPromise.promisedTypeOfPromise ***REMOVED*** getUnionType(map(onfulfilledParameterSignatures, getTypeOfFirstParameterOfSignature), 2 /* Subtype */);
  }
  function checkAwaitedType(type, withAlias, errorNode, diagnosticMessage, ...args) {
    const awaitedType ***REMOVED*** withAlias ? getAwaitedType(type, errorNode, diagnosticMessage, ...args) : getAwaitedTypeNoAlias(type, errorNode, diagnosticMessage, ...args);
    return awaitedType || errorType;
  }
  function isThenableType(type) {
    if (allTypesAssignableToKind(getBaseConstraintOrType(type), 402784252 /* Primitive */ | 131072 /* Never */)) {
      return false;
    }
    const thenFunction ***REMOVED*** getTypeOfPropertyOfType(type, "then");
    return !!thenFunction && getSignaturesOfType(getTypeWithFacts(thenFunction, 2097152 /* NEUndefinedOrNull */), 0 /* Call */).length > 0;
  }
  function isAwaitedTypeInstantiation(type) {
    var _a;
    if (type.flags & 16777216 /* Conditional */) {
      const awaitedSymbol ***REMOVED*** getGlobalAwaitedSymbol(
        /*reportErrors*/
        false
      );
      return !!awaitedSymbol && type.aliasSymbol ***REMOVED******REMOVED******REMOVED*** awaitedSymbol && ((_a ***REMOVED*** type.aliasTypeArguments) ***REMOVED******REMOVED*** null ? void 0 : _a.length) ***REMOVED******REMOVED******REMOVED*** 1;
    }
    return false;
  }
  function unwrapAwaitedType(type) {
    return type.flags & 1048576 /* Union */ ? mapType(type, unwrapAwaitedType) : isAwaitedTypeInstantiation(type) ? type.aliasTypeArguments[0] : type;
  }
  function isAwaitedTypeNeeded(type) {
    if (isTypeAny(type) || isAwaitedTypeInstantiation(type)) {
      return false;
    }
    if (isGenericObjectType(type)) {
      const baseConstraint ***REMOVED*** getBaseConstraintOfType(type);
      if (baseConstraint ? baseConstraint.flags & 3 /* AnyOrUnknown */ || isEmptyObjectType(baseConstraint) || someType(baseConstraint, isThenableType) : maybeTypeOfKind(type, 8650752 /* TypeVariable */)) {
        return true;
      }
    }
    return false;
  }
  function tryCreateAwaitedType(type) {
    const awaitedSymbol ***REMOVED*** getGlobalAwaitedSymbol(
      /*reportErrors*/
      true
    );
    if (awaitedSymbol) {
      return getTypeAliasInstantiation(awaitedSymbol, [unwrapAwaitedType(type)]);
    }
    return void 0;
  }
  function createAwaitedTypeIfNeeded(type) {
    if (isAwaitedTypeNeeded(type)) {
      return tryCreateAwaitedType(type) ?? type;
    }
    Debug.assert(isAwaitedTypeInstantiation(type) || getPromisedTypeOfPromise(type) ***REMOVED******REMOVED******REMOVED*** void 0, "type provided should not be a non-generic 'promise'-like.");
    return type;
  }
  function getAwaitedType(type, errorNode, diagnosticMessage, ...args) {
    const awaitedType ***REMOVED*** getAwaitedTypeNoAlias(type, errorNode, diagnosticMessage, ...args);
    return awaitedType && createAwaitedTypeIfNeeded(awaitedType);
  }
  function getAwaitedTypeNoAlias(type, errorNode, diagnosticMessage, ...args) {
    if (isTypeAny(type)) {
      return type;
    }
    if (isAwaitedTypeInstantiation(type)) {
      return type;
    }
    const typeAsAwaitable ***REMOVED*** type;
    if (typeAsAwaitable.awaitedTypeOfType) {
      return typeAsAwaitable.awaitedTypeOfType;
    }
    if (type.flags & 1048576 /* Union */) {
      if (awaitedTypeStack.lastIndexOf(type.id) >***REMOVED*** 0) {
        if (errorNode) {
          error(errorNode, Diagnostics.Type_is_referenced_directly_or_indirectly_in_the_fulfillment_callback_of_its_own_then_method);
        }
        return void 0;
      }
      const mapper ***REMOVED*** errorNode ? (constituentType) ***REMOVED***> getAwaitedTypeNoAlias(constituentType, errorNode, diagnosticMessage, ...args) : getAwaitedTypeNoAlias;
      awaitedTypeStack.push(type.id);
      const mapped ***REMOVED*** mapType(type, mapper);
      awaitedTypeStack.pop();
      return typeAsAwaitable.awaitedTypeOfType ***REMOVED*** mapped;
    }
    if (isAwaitedTypeNeeded(type)) {
      return typeAsAwaitable.awaitedTypeOfType ***REMOVED*** type;
    }
    const thisTypeForErrorOut ***REMOVED*** { value: void 0 };
    const promisedType ***REMOVED*** getPromisedTypeOfPromise(
      type,
      /*errorNode*/
      void 0,
      thisTypeForErrorOut
    );
    if (promisedType) {
      if (type.id ***REMOVED******REMOVED******REMOVED*** promisedType.id || awaitedTypeStack.lastIndexOf(promisedType.id) >***REMOVED*** 0) {
        if (errorNode) {
          error(errorNode, Diagnostics.Type_is_referenced_directly_or_indirectly_in_the_fulfillment_callback_of_its_own_then_method);
        }
        return void 0;
      }
      awaitedTypeStack.push(type.id);
      const awaitedType ***REMOVED*** getAwaitedTypeNoAlias(promisedType, errorNode, diagnosticMessage, ...args);
      awaitedTypeStack.pop();
      if (!awaitedType) {
        return void 0;
      }
      return typeAsAwaitable.awaitedTypeOfType ***REMOVED*** awaitedType;
    }
    if (isThenableType(type)) {
      if (errorNode) {
        Debug.assertIsDefined(diagnosticMessage);
        let chain;
        if (thisTypeForErrorOut.value) {
          chain ***REMOVED*** chainDiagnosticMessages(chain, Diagnostics.The_this_context_of_type_0_is_not_assignable_to_method_s_this_of_type_1, typeToString(type), typeToString(thisTypeForErrorOut.value));
        }
        chain ***REMOVED*** chainDiagnosticMessages(chain, diagnosticMessage, ...args);
        diagnostics.add(createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(errorNode), errorNode, chain));
      }
      return void 0;
    }
    return typeAsAwaitable.awaitedTypeOfType ***REMOVED*** type;
  }
  function checkAsyncFunctionReturnType(node, returnTypeNode, returnTypeErrorLocation) {
    const returnType ***REMOVED*** getTypeFromTypeNode(returnTypeNode);
    if (languageVersion >***REMOVED*** 2 /* ES2015 */) {
      if (isErrorType(returnType)) {
        return;
      }
      const globalPromiseType ***REMOVED*** getGlobalPromiseType(
        /*reportErrors*/
        true
      );
      if (globalPromiseType !***REMOVED******REMOVED*** emptyGenericType && !isReferenceToType(returnType, globalPromiseType)) {
        reportErrorForInvalidReturnType(Diagnostics.The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type_Did_you_mean_to_write_Promise_0, returnTypeNode, returnTypeErrorLocation, typeToString(getAwaitedTypeNoAlias(returnType) || voidType));
        return;
      }
    } else {
      markLinkedReferences(node, 5 /* AsyncFunction */);
      if (isErrorType(returnType)) {
        return;
      }
      const promiseConstructorName ***REMOVED*** getEntityNameFromTypeNode(returnTypeNode);
      if (promiseConstructorName ***REMOVED******REMOVED******REMOVED*** void 0) {
        reportErrorForInvalidReturnType(Diagnostics.Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compatible_constructor_value, returnTypeNode, returnTypeErrorLocation, typeToString(returnType));
        return;
      }
      const promiseConstructorSymbol ***REMOVED*** resolveEntityName(
        promiseConstructorName,
        111551 /* Value */,
        /*ignoreErrors*/
        true
      );
      const promiseConstructorType ***REMOVED*** promiseConstructorSymbol ? getTypeOfSymbol(promiseConstructorSymbol) : errorType;
      if (isErrorType(promiseConstructorType)) {
        if (promiseConstructorName.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && promiseConstructorName.escapedText ***REMOVED******REMOVED******REMOVED*** "Promise" && getTargetType(returnType) ***REMOVED******REMOVED******REMOVED*** getGlobalPromiseType(
          /*reportErrors*/
          false
        )) {
          error(returnTypeErrorLocation, Diagnostics.An_async_function_or_method_in_ES5_requires_the_Promise_constructor_Make_sure_you_have_a_declaration_for_the_Promise_constructor_or_include_ES2015_in_your_lib_option);
        } else {
          reportErrorForInvalidReturnType(Diagnostics.Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compatible_constructor_value, returnTypeNode, returnTypeErrorLocation, entityNameToString(promiseConstructorName));
        }
        return;
      }
      const globalPromiseConstructorLikeType ***REMOVED*** getGlobalPromiseConstructorLikeType(
        /*reportErrors*/
        true
      );
      if (globalPromiseConstructorLikeType ***REMOVED******REMOVED******REMOVED*** emptyObjectType) {
        reportErrorForInvalidReturnType(Diagnostics.Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compatible_constructor_value, returnTypeNode, returnTypeErrorLocation, entityNameToString(promiseConstructorName));
        return;
      }
      const headMessage ***REMOVED*** Diagnostics.Type_0_is_not_a_valid_async_function_return_type_in_ES5_because_it_does_not_refer_to_a_Promise_compatible_constructor_value;
      const errorInfo ***REMOVED*** () ***REMOVED***> returnTypeNode ***REMOVED******REMOVED******REMOVED*** returnTypeErrorLocation ? void 0 : chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type
      );
      if (!checkTypeAssignableTo(promiseConstructorType, globalPromiseConstructorLikeType, returnTypeErrorLocation, headMessage, errorInfo)) {
        return;
      }
      const rootName ***REMOVED*** promiseConstructorName && getFirstIdentifier(promiseConstructorName);
      const collidingSymbol ***REMOVED*** getSymbol(node.locals, rootName.escapedText, 111551 /* Value */);
      if (collidingSymbol) {
        error(collidingSymbol.valueDeclaration, Diagnostics.Duplicate_identifier_0_Compiler_uses_declaration_1_to_support_async_functions, idText(rootName), entityNameToString(promiseConstructorName));
        return;
      }
    }
    checkAwaitedType(
      returnType,
      /*withAlias*/
      false,
      node,
      Diagnostics.The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
    );
    function reportErrorForInvalidReturnType(message, returnTypeNode2, returnTypeErrorLocation2, typeName) {
      if (returnTypeNode2 ***REMOVED******REMOVED******REMOVED*** returnTypeErrorLocation2) {
        error(returnTypeErrorLocation2, message, typeName);
      } else {
        const diag2 ***REMOVED*** error(returnTypeErrorLocation2, Diagnostics.The_return_type_of_an_async_function_or_method_must_be_the_global_Promise_T_type);
        addRelatedInfo(diag2, createDiagnosticForNode(returnTypeNode2, message, typeName));
      }
    }
  }
  function checkGrammarDecorator(decorator) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(decorator);
    if (!hasParseDiagnostics(sourceFile)) {
      let node ***REMOVED*** decorator.expression;
      if (isParenthesizedExpression(node)) {
        return false;
      }
      let canHaveCallExpression ***REMOVED*** true;
      let errorNode;
      while (true) {
        if (isExpressionWithTypeArguments(node) || isNonNullExpression(node)) {
          node ***REMOVED*** node.expression;
          continue;
        }
        if (isCallExpression(node)) {
          if (!canHaveCallExpression) {
            errorNode ***REMOVED*** node;
          }
          if (node.questionDotToken) {
            errorNode ***REMOVED*** node.questionDotToken;
          }
          node ***REMOVED*** node.expression;
          canHaveCallExpression ***REMOVED*** false;
          continue;
        }
        if (isPropertyAccessExpression(node)) {
          if (node.questionDotToken) {
            errorNode ***REMOVED*** node.questionDotToken;
          }
          node ***REMOVED*** node.expression;
          canHaveCallExpression ***REMOVED*** false;
          continue;
        }
        if (!isIdentifier(node)) {
          errorNode ***REMOVED*** node;
        }
        break;
      }
      if (errorNode) {
        addRelatedInfo(
          error(decorator.expression, Diagnostics.Expression_must_be_enclosed_in_parentheses_to_be_used_as_a_decorator),
          createDiagnosticForNode(errorNode, Diagnostics.Invalid_syntax_in_decorator)
        );
        return true;
      }
    }
    return false;
  }
  function checkDecorator(node) {
    checkGrammarDecorator(node);
    const signature ***REMOVED*** getResolvedSignature(node);
    checkDeprecatedSignature(signature, node);
    const returnType ***REMOVED*** getReturnTypeOfSignature(signature);
    if (returnType.flags & 1 /* Any */) {
      return;
    }
    const decoratorSignature ***REMOVED*** getDecoratorCallSignature(node);
    if (!(decoratorSignature ***REMOVED******REMOVED*** null ? void 0 : decoratorSignature.resolvedReturnType)) return;
    let headMessage;
    const expectedReturnType ***REMOVED*** decoratorSignature.resolvedReturnType;
    switch (node.parent.kind) {
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        headMessage ***REMOVED*** Diagnostics.Decorator_function_return_type_0_is_not_assignable_to_type_1;
        break;
      case 173 /* PropertyDeclaration */:
        if (!legacyDecorators) {
          headMessage ***REMOVED*** Diagnostics.Decorator_function_return_type_0_is_not_assignable_to_type_1;
          break;
        }
      // falls through
      case 170 /* Parameter */:
        headMessage ***REMOVED*** Diagnostics.Decorator_function_return_type_is_0_but_is_expected_to_be_void_or_any;
        break;
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        headMessage ***REMOVED*** Diagnostics.Decorator_function_return_type_0_is_not_assignable_to_type_1;
        break;
      default:
        return Debug.failBadSyntaxKind(node.parent);
    }
    checkTypeAssignableTo(returnType, expectedReturnType, node.expression, headMessage);
  }
  function createCallSignature(typeParameters, thisParameter, parameters, returnType, typePredicate, minArgumentCount ***REMOVED*** parameters.length, flags ***REMOVED*** 0 /* None */) {
    const decl ***REMOVED*** factory.createFunctionTypeNode(
      /*typeParameters*/
      void 0,
      emptyArray,
      factory.createKeywordTypeNode(133 /* AnyKeyword */)
    );
    return createSignature(decl, typeParameters, thisParameter, parameters, returnType, typePredicate, minArgumentCount, flags);
  }
  function createFunctionType(typeParameters, thisParameter, parameters, returnType, typePredicate, minArgumentCount, flags) {
    const signature ***REMOVED*** createCallSignature(typeParameters, thisParameter, parameters, returnType, typePredicate, minArgumentCount, flags);
    return getOrCreateTypeFromSignature(signature);
  }
  function createGetterFunctionType(type) {
    return createFunctionType(
      /*typeParameters*/
      void 0,
      /*thisParameter*/
      void 0,
      emptyArray,
      type
    );
  }
  function createSetterFunctionType(type) {
    const valueParam ***REMOVED*** createParameter("value", type);
    return createFunctionType(
      /*typeParameters*/
      void 0,
      /*thisParameter*/
      void 0,
      [valueParam],
      voidType
    );
  }
  function getEntityNameForDecoratorMetadata(node) {
    if (node) {
      switch (node.kind) {
        case 194 /* IntersectionType */:
        case 193 /* UnionType */:
          return getEntityNameForDecoratorMetadataFromTypeList(node.types);
        case 195 /* ConditionalType */:
          return getEntityNameForDecoratorMetadataFromTypeList([node.trueType, node.falseType]);
        case 197 /* ParenthesizedType */:
        case 203 /* NamedTupleMember */:
          return getEntityNameForDecoratorMetadata(node.type);
        case 184 /* TypeReference */:
          return node.typeName;
      }
    }
  }
  function getEntityNameForDecoratorMetadataFromTypeList(types) {
    let commonEntityName;
    for (let typeNode of types) {
      while (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 197 /* ParenthesizedType */ || typeNode.kind ***REMOVED******REMOVED******REMOVED*** 203 /* NamedTupleMember */) {
        typeNode ***REMOVED*** typeNode.type;
      }
      if (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 146 /* NeverKeyword */) {
        continue;
      }
      if (!strictNullChecks && (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 202 /* LiteralType */ && typeNode.literal.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */ || typeNode.kind ***REMOVED******REMOVED******REMOVED*** 157 /* UndefinedKeyword */)) {
        continue;
      }
      const individualEntityName ***REMOVED*** getEntityNameForDecoratorMetadata(typeNode);
      if (!individualEntityName) {
        return void 0;
      }
      if (commonEntityName) {
        if (!isIdentifier(commonEntityName) || !isIdentifier(individualEntityName) || commonEntityName.escapedText !***REMOVED******REMOVED*** individualEntityName.escapedText) {
          return void 0;
        }
      } else {
        commonEntityName ***REMOVED*** individualEntityName;
      }
    }
    return commonEntityName;
  }
  function getParameterTypeNodeForDecoratorCheck(node) {
    const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    return isRestParameter(node) ? getRestParameterElementType(typeNode) : typeNode;
  }
  function checkDecorators(node) {
    if (!canHaveDecorators(node) || !hasDecorators(node) || !node.modifiers || !nodeCanBeDecorated(legacyDecorators, node, node.parent, node.parent.parent)) {
      return;
    }
    const firstDecorator ***REMOVED*** find(node.modifiers, isDecorator);
    if (!firstDecorator) {
      return;
    }
    if (legacyDecorators) {
      checkExternalEmitHelpers(firstDecorator, 8 /* Decorate */);
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
        checkExternalEmitHelpers(firstDecorator, 32 /* Param */);
      }
    } else if (languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators) {
      checkExternalEmitHelpers(firstDecorator, 8 /* ESDecorateAndRunInitializers */);
      if (isClassDeclaration(node)) {
        if (!node.name) {
          checkExternalEmitHelpers(firstDecorator, 4194304 /* SetFunctionName */);
        } else {
          const member ***REMOVED*** getFirstTransformableStaticClassElement(node);
          if (member) {
            checkExternalEmitHelpers(firstDecorator, 4194304 /* SetFunctionName */);
          }
        }
      } else if (!isClassExpression(node)) {
        if (isPrivateIdentifier(node.name) && (isMethodDeclaration(node) || isAccessor(node) || isAutoAccessorPropertyDeclaration(node))) {
          checkExternalEmitHelpers(firstDecorator, 4194304 /* SetFunctionName */);
        }
        if (isComputedPropertyName(node.name)) {
          checkExternalEmitHelpers(firstDecorator, 8388608 /* PropKey */);
        }
      }
    }
    markLinkedReferences(node, 8 /* Decorator */);
    for (const modifier of node.modifiers) {
      if (isDecorator(modifier)) {
        checkDecorator(modifier);
      }
    }
  }
  function checkFunctionDeclaration(node) {
    addLazyDiagnostic(checkFunctionDeclarationDiagnostics);
    function checkFunctionDeclarationDiagnostics() {
      checkFunctionOrMethodDeclaration(node);
      checkGrammarForGenerator(node);
      checkCollisionsForDeclarationName(node, node.name);
    }
  }
  function checkJSDocTypeAliasTag(node) {
    if (!node.typeExpression) {
      error(node.name, Diagnostics.JSDoc_typedef_tag_should_either_have_a_type_annotation_or_be_followed_by_property_or_member_tags);
    }
    if (node.name) {
      checkTypeNameIsReserved(node.name, Diagnostics.Type_alias_name_cannot_be_0);
    }
    checkSourceElement(node.typeExpression);
    checkTypeParameters(getEffectiveTypeParameterDeclarations(node));
  }
  function checkJSDocTemplateTag(node) {
    checkSourceElement(node.constraint);
    for (const tp of node.typeParameters) {
      checkSourceElement(tp);
    }
  }
  function checkJSDocTypeTag(node) {
    checkSourceElement(node.typeExpression);
  }
  function checkJSDocSatisfiesTag(node) {
    checkSourceElement(node.typeExpression);
    const host2 ***REMOVED*** getEffectiveJSDocHost(node);
    if (host2) {
      const tags ***REMOVED*** getAllJSDocTags(host2, isJSDocSatisfiesTag);
      if (length(tags) > 1) {
        for (let i ***REMOVED*** 1; i < length(tags); i++) {
          const tagName ***REMOVED*** tags[i].tagName;
          error(tagName, Diagnostics._0_tag_already_specified, idText(tagName));
        }
      }
    }
  }
  function checkJSDocLinkLikeTag(node) {
    if (node.name) {
      resolveJSDocMemberName(
        node.name,
        /*ignoreErrors*/
        true
      );
    }
  }
  function checkJSDocParameterTag(node) {
    checkSourceElement(node.typeExpression);
  }
  function checkJSDocPropertyTag(node) {
    checkSourceElement(node.typeExpression);
  }
  function checkJSDocFunctionType(node) {
    addLazyDiagnostic(checkJSDocFunctionTypeImplicitAny);
    checkSignatureDeclaration(node);
    function checkJSDocFunctionTypeImplicitAny() {
      if (!node.type && !isJSDocConstructSignature(node)) {
        reportImplicitAny(node, anyType);
      }
    }
  }
  function checkJSDocThisTag(node) {
    const host2 ***REMOVED*** getEffectiveJSDocHost(node);
    if (host2 && isArrowFunction(host2)) {
      error(node.tagName, Diagnostics.An_arrow_function_cannot_have_a_this_parameter);
    }
  }
  function checkJSDocImportTag(node) {
    checkImportAttributes(node);
  }
  function checkJSDocImplementsTag(node) {
    const classLike ***REMOVED*** getEffectiveJSDocHost(node);
    if (!classLike || !isClassDeclaration(classLike) && !isClassExpression(classLike)) {
      error(classLike, Diagnostics.JSDoc_0_is_not_attached_to_a_class, idText(node.tagName));
    }
  }
  function checkJSDocAugmentsTag(node) {
    const classLike ***REMOVED*** getEffectiveJSDocHost(node);
    if (!classLike || !isClassDeclaration(classLike) && !isClassExpression(classLike)) {
      error(classLike, Diagnostics.JSDoc_0_is_not_attached_to_a_class, idText(node.tagName));
      return;
    }
    const augmentsTags ***REMOVED*** getJSDocTags(classLike).filter(isJSDocAugmentsTag);
    Debug.assert(augmentsTags.length > 0);
    if (augmentsTags.length > 1) {
      error(augmentsTags[1], Diagnostics.Class_declarations_cannot_have_more_than_one_augments_or_extends_tag);
    }
    const name ***REMOVED*** getIdentifierFromEntityNameExpression(node.class.expression);
    const extend2 ***REMOVED*** getClassExtendsHeritageElement(classLike);
    if (extend2) {
      const className ***REMOVED*** getIdentifierFromEntityNameExpression(extend2.expression);
      if (className && name.escapedText !***REMOVED******REMOVED*** className.escapedText) {
        error(name, Diagnostics.JSDoc_0_1_does_not_match_the_extends_2_clause, idText(node.tagName), idText(name), idText(className));
      }
    }
  }
  function checkJSDocAccessibilityModifiers(node) {
    const host2 ***REMOVED*** getJSDocHost(node);
    if (host2 && isPrivateIdentifierClassElementDeclaration(host2)) {
      error(node, Diagnostics.An_accessibility_modifier_cannot_be_used_with_a_private_identifier);
    }
  }
  function getIdentifierFromEntityNameExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return node;
      case 212 /* PropertyAccessExpression */:
        return node.name;
      default:
        return void 0;
    }
  }
  function checkFunctionOrMethodDeclaration(node) {
    var _a;
    checkDecorators(node);
    checkSignatureDeclaration(node);
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    if (node.name && node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
      checkComputedPropertyName(node.name);
    }
    if (hasBindableName(node)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      const localSymbol ***REMOVED*** node.localSymbol || symbol;
      const firstDeclaration ***REMOVED*** (_a ***REMOVED*** localSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find(
        // Get first non javascript function declaration
        (declaration) ***REMOVED***> declaration.kind ***REMOVED******REMOVED******REMOVED*** node.kind && !(declaration.flags & 524288 /* JavaScriptFile */)
      );
      if (node ***REMOVED******REMOVED******REMOVED*** firstDeclaration) {
        checkFunctionOrConstructorSymbol(localSymbol);
      }
      if (symbol.parent) {
        checkFunctionOrConstructorSymbol(symbol);
      }
    }
    const body ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ ? void 0 : node.body;
    checkSourceElement(body);
    checkAllCodePathsInNonVoidFunctionReturnOrThrow(node, getReturnTypeFromAnnotation(node));
    addLazyDiagnostic(checkFunctionOrMethodDeclarationDiagnostics);
    if (isInJSFile(node)) {
      const typeTag ***REMOVED*** getJSDocTypeTag(node);
      if (typeTag && typeTag.typeExpression && !getContextualCallSignature(getTypeFromTypeNode(typeTag.typeExpression), node)) {
        error(typeTag.typeExpression.type, Diagnostics.The_type_of_a_function_declaration_must_match_the_function_s_signature);
      }
    }
    function checkFunctionOrMethodDeclarationDiagnostics() {
      if (!getEffectiveReturnTypeNode(node)) {
        if (nodeIsMissing(body) && !isPrivateWithinAmbient(node)) {
          reportImplicitAny(node, anyType);
        }
        if (functionFlags & 1 /* Generator */ && nodeIsPresent(body)) {
          getReturnTypeOfSignature(getSignatureFromDeclaration(node));
        }
      }
    }
  }
  function registerForUnusedIdentifiersCheck(node) {
    addLazyDiagnostic(registerForUnusedIdentifiersCheckDiagnostics);
    function registerForUnusedIdentifiersCheckDiagnostics() {
      const sourceFile ***REMOVED*** getSourceFileOfNode(node);
      let potentiallyUnusedIdentifiers ***REMOVED*** allPotentiallyUnusedIdentifiers.get(sourceFile.path);
      if (!potentiallyUnusedIdentifiers) {
        potentiallyUnusedIdentifiers ***REMOVED*** [];
        allPotentiallyUnusedIdentifiers.set(sourceFile.path, potentiallyUnusedIdentifiers);
      }
      potentiallyUnusedIdentifiers.push(node);
    }
  }
  function checkUnusedIdentifiers(potentiallyUnusedIdentifiers, addDiagnostic) {
    for (const node of potentiallyUnusedIdentifiers) {
      switch (node.kind) {
        case 264 /* ClassDeclaration */:
        case 232 /* ClassExpression */:
          checkUnusedClassMembers(node, addDiagnostic);
          checkUnusedTypeParameters(node, addDiagnostic);
          break;
        case 308 /* SourceFile */:
        case 268 /* ModuleDeclaration */:
        case 242 /* Block */:
        case 270 /* CaseBlock */:
        case 249 /* ForStatement */:
        case 250 /* ForInStatement */:
        case 251 /* ForOfStatement */:
          checkUnusedLocalsAndParameters(node, addDiagnostic);
          break;
        case 177 /* Constructor */:
        case 219 /* FunctionExpression */:
        case 263 /* FunctionDeclaration */:
        case 220 /* ArrowFunction */:
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          if (node.body) {
            checkUnusedLocalsAndParameters(node, addDiagnostic);
          }
          checkUnusedTypeParameters(node, addDiagnostic);
          break;
        case 174 /* MethodSignature */:
        case 180 /* CallSignature */:
        case 181 /* ConstructSignature */:
        case 185 /* FunctionType */:
        case 186 /* ConstructorType */:
        case 266 /* TypeAliasDeclaration */:
        case 265 /* InterfaceDeclaration */:
          checkUnusedTypeParameters(node, addDiagnostic);
          break;
        case 196 /* InferType */:
          checkUnusedInferTypeParameter(node, addDiagnostic);
          break;
        default:
          Debug.assertNever(node, "Node should not have been registered for unused identifiers check");
      }
    }
  }
  function errorUnusedLocal(declaration, name, addDiagnostic) {
    const node ***REMOVED*** getNameOfDeclaration(declaration) || declaration;
    const message ***REMOVED*** isTypeDeclaration(declaration) ? Diagnostics._0_is_declared_but_never_used : Diagnostics._0_is_declared_but_its_value_is_never_read;
    addDiagnostic(declaration, 0 /* Local */, createDiagnosticForNode(node, message, name));
  }
  function isIdentifierThatStartsWithUnderscore(node) {
    return isIdentifier(node) && idText(node).charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 95 /* _ */;
  }
  function checkUnusedClassMembers(node, addDiagnostic) {
    for (const member of node.members) {
      switch (member.kind) {
        case 175 /* MethodDeclaration */:
        case 173 /* PropertyDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          if (member.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ && member.symbol.flags & 32768 /* GetAccessor */) {
            break;
          }
          const symbol ***REMOVED*** getSymbolOfDeclaration(member);
          if (!symbol.isReferenced && (hasEffectiveModifier(member, 2 /* Private */) || isNamedDeclaration(member) && isPrivateIdentifier(member.name)) && !(member.flags & 33554432 /* Ambient */)) {
            addDiagnostic(member, 0 /* Local */, createDiagnosticForNode(member.name, Diagnostics._0_is_declared_but_its_value_is_never_read, symbolToString(symbol)));
          }
          break;
        case 177 /* Constructor */:
          for (const parameter of member.parameters) {
            if (!parameter.symbol.isReferenced && hasSyntacticModifier(parameter, 2 /* Private */)) {
              addDiagnostic(parameter, 0 /* Local */, createDiagnosticForNode(parameter.name, Diagnostics.Property_0_is_declared_but_its_value_is_never_read, symbolName(parameter.symbol)));
            }
          }
          break;
        case 182 /* IndexSignature */:
        case 241 /* SemicolonClassElement */:
        case 176 /* ClassStaticBlockDeclaration */:
          break;
        default:
          Debug.fail("Unexpected class member");
      }
    }
  }
  function checkUnusedInferTypeParameter(node, addDiagnostic) {
    const { typeParameter } ***REMOVED*** node;
    if (isTypeParameterUnused(typeParameter)) {
      addDiagnostic(node, 1 /* Parameter */, createDiagnosticForNode(node, Diagnostics._0_is_declared_but_its_value_is_never_read, idText(typeParameter.name)));
    }
  }
  function checkUnusedTypeParameters(node, addDiagnostic) {
    const declarations ***REMOVED*** getSymbolOfDeclaration(node).declarations;
    if (!declarations || last(declarations) !***REMOVED******REMOVED*** node) return;
    const typeParameters ***REMOVED*** getEffectiveTypeParameterDeclarations(node);
    const seenParentsWithEveryUnused ***REMOVED*** /* @__PURE__ */ new Set();
    for (const typeParameter of typeParameters) {
      if (!isTypeParameterUnused(typeParameter)) continue;
      const name ***REMOVED*** idText(typeParameter.name);
      const { parent } ***REMOVED*** typeParameter;
      if (parent.kind !***REMOVED******REMOVED*** 196 /* InferType */ && parent.typeParameters.every(isTypeParameterUnused)) {
        if (tryAddToSet(seenParentsWithEveryUnused, parent)) {
          const sourceFile ***REMOVED*** getSourceFileOfNode(parent);
          const range ***REMOVED*** isJSDocTemplateTag(parent) ? rangeOfNode(parent) : rangeOfTypeParameters(sourceFile, parent.typeParameters);
          const only ***REMOVED*** parent.typeParameters.length ***REMOVED******REMOVED******REMOVED*** 1;
          const messageAndArg ***REMOVED*** only ? [Diagnostics._0_is_declared_but_its_value_is_never_read, name] : [Diagnostics.All_type_parameters_are_unused];
          addDiagnostic(typeParameter, 1 /* Parameter */, createFileDiagnostic(sourceFile, range.pos, range.end - range.pos, ...messageAndArg));
        }
      } else {
        addDiagnostic(typeParameter, 1 /* Parameter */, createDiagnosticForNode(typeParameter, Diagnostics._0_is_declared_but_its_value_is_never_read, name));
      }
    }
  }
  function isTypeParameterUnused(typeParameter) {
    return !(getMergedSymbol(typeParameter.symbol).isReferenced & 262144 /* TypeParameter */) && !isIdentifierThatStartsWithUnderscore(typeParameter.name);
  }
  function addToGroup(map2, key, value, getKey) {
    const keyString ***REMOVED*** String(getKey(key));
    const group2 ***REMOVED*** map2.get(keyString);
    if (group2) {
      group2[1].push(value);
    } else {
      map2.set(keyString, [key, [value]]);
    }
  }
  function tryGetRootParameterDeclaration(node) {
    return tryCast(getRootDeclaration(node), isParameter);
  }
  function isValidUnusedLocalDeclaration(declaration) {
    if (isBindingElement(declaration)) {
      if (isObjectBindingPattern(declaration.parent)) {
        return !!(declaration.propertyName && isIdentifierThatStartsWithUnderscore(declaration.name));
      }
      return isIdentifierThatStartsWithUnderscore(declaration.name);
    }
    return isAmbientModule(declaration) || (isVariableDeclaration(declaration) && isForInOrOfStatement(declaration.parent.parent) || isImportedDeclaration(declaration)) && isIdentifierThatStartsWithUnderscore(declaration.name);
  }
  function checkUnusedLocalsAndParameters(nodeWithLocals, addDiagnostic) {
    const unusedImports ***REMOVED*** /* @__PURE__ */ new Map();
    const unusedDestructures ***REMOVED*** /* @__PURE__ */ new Map();
    const unusedVariables ***REMOVED*** /* @__PURE__ */ new Map();
    nodeWithLocals.locals.forEach((local) ***REMOVED***> {
      if (local.flags & 262144 /* TypeParameter */ ? !(local.flags & 3 /* Variable */ && !(local.isReferenced & 3 /* Variable */)) : local.isReferenced || local.exportSymbol) {
        return;
      }
      if (local.declarations) {
        for (const declaration of local.declarations) {
          if (isValidUnusedLocalDeclaration(declaration)) {
            continue;
          }
          if (isImportedDeclaration(declaration)) {
            addToGroup(unusedImports, importClauseFromImported(declaration), declaration, getNodeId);
          } else if (isBindingElement(declaration) && isObjectBindingPattern(declaration.parent)) {
            const lastElement ***REMOVED*** last(declaration.parent.elements);
            if (declaration ***REMOVED******REMOVED******REMOVED*** lastElement || !last(declaration.parent.elements).dotDotDotToken) {
              addToGroup(unusedDestructures, declaration.parent, declaration, getNodeId);
            }
          } else if (isVariableDeclaration(declaration)) {
            const blockScopeKind ***REMOVED*** getCombinedNodeFlagsCached(declaration) & 7 /* BlockScoped */;
            const name ***REMOVED*** getNameOfDeclaration(declaration);
            if (blockScopeKind !***REMOVED******REMOVED*** 4 /* Using */ && blockScopeKind !***REMOVED******REMOVED*** 6 /* AwaitUsing */ || !name || !isIdentifierThatStartsWithUnderscore(name)) {
              addToGroup(unusedVariables, declaration.parent, declaration, getNodeId);
            }
          } else {
            const parameter ***REMOVED*** local.valueDeclaration && tryGetRootParameterDeclaration(local.valueDeclaration);
            const name ***REMOVED*** local.valueDeclaration && getNameOfDeclaration(local.valueDeclaration);
            if (parameter && name) {
              if (!isParameterPropertyDeclaration(parameter, parameter.parent) && !parameterIsThisKeyword(parameter) && !isIdentifierThatStartsWithUnderscore(name)) {
                if (isBindingElement(declaration) && isArrayBindingPattern(declaration.parent)) {
                  addToGroup(unusedDestructures, declaration.parent, declaration, getNodeId);
                } else {
                  addDiagnostic(parameter, 1 /* Parameter */, createDiagnosticForNode(name, Diagnostics._0_is_declared_but_its_value_is_never_read, symbolName(local)));
                }
              }
            } else {
              errorUnusedLocal(declaration, symbolName(local), addDiagnostic);
            }
          }
        }
      }
    });
    unusedImports.forEach(([importClause, unuseds]) ***REMOVED***> {
      const importDecl ***REMOVED*** importClause.parent;
      const nDeclarations ***REMOVED*** (importClause.name ? 1 : 0) + (importClause.namedBindings ? importClause.namedBindings.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */ ? 1 : importClause.namedBindings.elements.length : 0);
      if (nDeclarations ***REMOVED******REMOVED******REMOVED*** unuseds.length) {
        addDiagnostic(
          importDecl,
          0 /* Local */,
          unuseds.length ***REMOVED******REMOVED******REMOVED*** 1 ? createDiagnosticForNode(importDecl, Diagnostics._0_is_declared_but_its_value_is_never_read, idText(first(unuseds).name)) : createDiagnosticForNode(importDecl, Diagnostics.All_imports_in_import_declaration_are_unused)
        );
      } else {
        for (const unused of unuseds) errorUnusedLocal(unused, idText(unused.name), addDiagnostic);
      }
    });
    unusedDestructures.forEach(([bindingPattern, bindingElements]) ***REMOVED***> {
      const kind ***REMOVED*** tryGetRootParameterDeclaration(bindingPattern.parent) ? 1 /* Parameter */ : 0 /* Local */;
      if (bindingPattern.elements.length ***REMOVED******REMOVED******REMOVED*** bindingElements.length) {
        if (bindingElements.length ***REMOVED******REMOVED******REMOVED*** 1 && bindingPattern.parent.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && bindingPattern.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
          addToGroup(unusedVariables, bindingPattern.parent.parent, bindingPattern.parent, getNodeId);
        } else {
          addDiagnostic(
            bindingPattern,
            kind,
            bindingElements.length ***REMOVED******REMOVED******REMOVED*** 1 ? createDiagnosticForNode(bindingPattern, Diagnostics._0_is_declared_but_its_value_is_never_read, bindingNameText(first(bindingElements).name)) : createDiagnosticForNode(bindingPattern, Diagnostics.All_destructured_elements_are_unused)
          );
        }
      } else {
        for (const e of bindingElements) {
          addDiagnostic(e, kind, createDiagnosticForNode(e, Diagnostics._0_is_declared_but_its_value_is_never_read, bindingNameText(e.name)));
        }
      }
    });
    unusedVariables.forEach(([declarationList, declarations]) ***REMOVED***> {
      if (declarationList.declarations.length ***REMOVED******REMOVED******REMOVED*** declarations.length) {
        addDiagnostic(
          declarationList,
          0 /* Local */,
          declarations.length ***REMOVED******REMOVED******REMOVED*** 1 ? createDiagnosticForNode(first(declarations).name, Diagnostics._0_is_declared_but_its_value_is_never_read, bindingNameText(first(declarations).name)) : createDiagnosticForNode(declarationList.parent.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ ? declarationList.parent : declarationList, Diagnostics.All_variables_are_unused)
        );
      } else {
        for (const decl of declarations) {
          addDiagnostic(decl, 0 /* Local */, createDiagnosticForNode(decl, Diagnostics._0_is_declared_but_its_value_is_never_read, bindingNameText(decl.name)));
        }
      }
    });
  }
  function checkPotentialUncheckedRenamedBindingElementsInTypes() {
    var _a;
    for (const node of potentialUnusedRenamedBindingElementsInTypes) {
      if (!((_a ***REMOVED*** getSymbolOfDeclaration(node)) ***REMOVED******REMOVED*** null ? void 0 : _a.isReferenced)) {
        const wrappingDeclaration ***REMOVED*** walkUpBindingElementsAndPatterns(node);
        Debug.assert(isPartOfParameterDeclaration(wrappingDeclaration), "Only parameter declaration should be checked here");
        const diagnostic ***REMOVED*** createDiagnosticForNode(node.name, Diagnostics._0_is_an_unused_renaming_of_1_Did_you_intend_to_use_it_as_a_type_annotation, declarationNameToString(node.name), declarationNameToString(node.propertyName));
        if (!wrappingDeclaration.type) {
          addRelatedInfo(
            diagnostic,
            createFileDiagnostic(getSourceFileOfNode(wrappingDeclaration), wrappingDeclaration.end, 0, Diagnostics.We_can_only_write_a_type_for_0_by_adding_a_type_for_the_entire_parameter_here, declarationNameToString(node.propertyName))
          );
        }
        diagnostics.add(diagnostic);
      }
    }
  }
  function bindingNameText(name) {
    switch (name.kind) {
      case 80 /* Identifier */:
        return idText(name);
      case 208 /* ArrayBindingPattern */:
      case 207 /* ObjectBindingPattern */:
        return bindingNameText(cast(first(name.elements), isBindingElement).name);
      default:
        return Debug.assertNever(name);
    }
  }
  function isImportedDeclaration(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ || node.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ || node.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */;
  }
  function importClauseFromImported(decl) {
    return decl.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ ? decl : decl.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */ ? decl.parent : decl.parent.parent;
  }
  function checkBlock(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */) {
      checkGrammarStatementInAmbientContext(node);
    }
    if (isFunctionOrModuleBlock(node)) {
      const saveFlowAnalysisDisabled ***REMOVED*** flowAnalysisDisabled;
      forEach(node.statements, checkSourceElement);
      flowAnalysisDisabled ***REMOVED*** saveFlowAnalysisDisabled;
    } else {
      forEach(node.statements, checkSourceElement);
    }
    if (node.locals) {
      registerForUnusedIdentifiersCheck(node);
    }
  }
  function checkCollisionWithArgumentsInGeneratedCode(node) {
    if (languageVersion >***REMOVED*** 2 /* ES2015 */ || !hasRestParameter(node) || node.flags & 33554432 /* Ambient */ || nodeIsMissing(node.body)) {
      return;
    }
    forEach(node.parameters, (p) ***REMOVED***> {
      if (p.name && !isBindingPattern(p.name) && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** argumentsSymbol.escapedName) {
        errorSkippedOn("noEmit", p, Diagnostics.Duplicate_identifier_arguments_Compiler_uses_arguments_to_initialize_rest_parameters);
      }
    });
  }
  function needCollisionCheckForIdentifier(node, identifier, name) {
    if ((identifier ***REMOVED******REMOVED*** null ? void 0 : identifier.escapedText) !***REMOVED******REMOVED*** name) {
      return false;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
      return false;
    }
    if (node.flags & 33554432 /* Ambient */) {
      return false;
    }
    if (isImportClause(node) || isImportEqualsDeclaration(node) || isImportSpecifier(node)) {
      if (isTypeOnlyImportOrExportDeclaration(node)) {
        return false;
      }
    }
    const root ***REMOVED*** getRootDeclaration(node);
    if (isParameter(root) && nodeIsMissing(root.parent.body)) {
      return false;
    }
    return true;
  }
  function checkIfThisIsCapturedInEnclosingScope(node) {
    findAncestor(node, (current) ***REMOVED***> {
      if (getNodeCheckFlags(current) & 4 /* CaptureThis */) {
        const isDeclaration2 ***REMOVED*** node.kind !***REMOVED******REMOVED*** 80 /* Identifier */;
        if (isDeclaration2) {
          error(getNameOfDeclaration(node), Diagnostics.Duplicate_identifier_this_Compiler_uses_variable_declaration_this_to_capture_this_reference);
        } else {
          error(node, Diagnostics.Expression_resolves_to_variable_declaration_this_that_compiler_uses_to_capture_this_reference);
        }
        return true;
      }
      return false;
    });
  }
  function checkIfNewTargetIsCapturedInEnclosingScope(node) {
    findAncestor(node, (current) ***REMOVED***> {
      if (getNodeCheckFlags(current) & 8 /* CaptureNewTarget */) {
        const isDeclaration2 ***REMOVED*** node.kind !***REMOVED******REMOVED*** 80 /* Identifier */;
        if (isDeclaration2) {
          error(getNameOfDeclaration(node), Diagnostics.Duplicate_identifier_newTarget_Compiler_uses_variable_declaration_newTarget_to_capture_new_target_meta_property_reference);
        } else {
          error(node, Diagnostics.Expression_resolves_to_variable_declaration_newTarget_that_compiler_uses_to_capture_new_target_meta_property_reference);
        }
        return true;
      }
      return false;
    });
  }
  function checkCollisionWithRequireExportsInGeneratedCode(node, name) {
    if (host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) >***REMOVED*** 5 /* ES2015 */) {
      return;
    }
    if (!name || !needCollisionCheckForIdentifier(node, name, "require") && !needCollisionCheckForIdentifier(node, name, "exports")) {
      return;
    }
    if (isModuleDeclaration(node) && getModuleInstanceState(node) !***REMOVED******REMOVED*** 1 /* Instantiated */) {
      return;
    }
    const parent ***REMOVED*** getDeclarationContainer(node);
    if (parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && isExternalOrCommonJsModule(parent)) {
      errorSkippedOn("noEmit", name, Diagnostics.Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module, declarationNameToString(name), declarationNameToString(name));
    }
  }
  function checkCollisionWithGlobalPromiseInGeneratedCode(node, name) {
    if (!name || languageVersion >***REMOVED*** 4 /* ES2017 */ || !needCollisionCheckForIdentifier(node, name, "Promise")) {
      return;
    }
    if (isModuleDeclaration(node) && getModuleInstanceState(node) !***REMOVED******REMOVED*** 1 /* Instantiated */) {
      return;
    }
    const parent ***REMOVED*** getDeclarationContainer(node);
    if (parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && isExternalOrCommonJsModule(parent) && parent.flags & 4096 /* HasAsyncFunctions */) {
      errorSkippedOn("noEmit", name, Diagnostics.Duplicate_identifier_0_Compiler_reserves_name_1_in_top_level_scope_of_a_module_containing_async_functions, declarationNameToString(name), declarationNameToString(name));
    }
  }
  function recordPotentialCollisionWithWeakMapSetInGeneratedCode(node, name) {
    if (languageVersion <***REMOVED*** 8 /* ES2021 */ && (needCollisionCheckForIdentifier(node, name, "WeakMap") || needCollisionCheckForIdentifier(node, name, "WeakSet"))) {
      potentialWeakMapSetCollisions.push(node);
    }
  }
  function checkWeakMapSetCollision(node) {
    const enclosingBlockScope ***REMOVED*** getEnclosingBlockScopeContainer(node);
    if (getNodeCheckFlags(enclosingBlockScope) & 1048576 /* ContainsClassWithPrivateIdentifiers */) {
      Debug.assert(isNamedDeclaration(node) && isIdentifier(node.name) && typeof node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "string", "The target of a WeakMap/WeakSet collision check should be an identifier");
      errorSkippedOn("noEmit", node, Diagnostics.Compiler_reserves_name_0_when_emitting_private_identifier_downlevel, node.name.escapedText);
    }
  }
  function recordPotentialCollisionWithReflectInGeneratedCode(node, name) {
    if (name && languageVersion >***REMOVED*** 2 /* ES2015 */ && languageVersion <***REMOVED*** 8 /* ES2021 */ && needCollisionCheckForIdentifier(node, name, "Reflect")) {
      potentialReflectCollisions.push(node);
    }
  }
  function checkReflectCollision(node) {
    let hasCollision ***REMOVED*** false;
    if (isClassExpression(node)) {
      for (const member of node.members) {
        if (getNodeCheckFlags(member) & 2097152 /* ContainsSuperPropertyInStaticInitializer */) {
          hasCollision ***REMOVED*** true;
          break;
        }
      }
    } else if (isFunctionExpression(node)) {
      if (getNodeCheckFlags(node) & 2097152 /* ContainsSuperPropertyInStaticInitializer */) {
        hasCollision ***REMOVED*** true;
      }
    } else {
      const container ***REMOVED*** getEnclosingBlockScopeContainer(node);
      if (container && getNodeCheckFlags(container) & 2097152 /* ContainsSuperPropertyInStaticInitializer */) {
        hasCollision ***REMOVED*** true;
      }
    }
    if (hasCollision) {
      Debug.assert(isNamedDeclaration(node) && isIdentifier(node.name), "The target of a Reflect collision check should be an identifier");
      errorSkippedOn("noEmit", node, Diagnostics.Duplicate_identifier_0_Compiler_reserves_name_1_when_emitting_super_references_in_static_initializers, declarationNameToString(node.name), "Reflect");
    }
  }
  function checkCollisionsForDeclarationName(node, name) {
    if (!name) return;
    checkCollisionWithRequireExportsInGeneratedCode(node, name);
    checkCollisionWithGlobalPromiseInGeneratedCode(node, name);
    recordPotentialCollisionWithWeakMapSetInGeneratedCode(node, name);
    recordPotentialCollisionWithReflectInGeneratedCode(node, name);
    if (isClassLike(node)) {
      checkTypeNameIsReserved(name, Diagnostics.Class_name_cannot_be_0);
      if (!(node.flags & 33554432 /* Ambient */)) {
        checkClassNameCollisionWithObject(name);
      }
    } else if (isEnumDeclaration(node)) {
      checkTypeNameIsReserved(name, Diagnostics.Enum_name_cannot_be_0);
    }
  }
  function checkVarDeclaredNamesNotShadowed(node) {
    if ((getCombinedNodeFlagsCached(node) & 7 /* BlockScoped */) !***REMOVED******REMOVED*** 0 || isPartOfParameterDeclaration(node)) {
      return;
    }
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    if (symbol.flags & 1 /* FunctionScopedVariable */) {
      if (!isIdentifier(node.name)) return Debug.fail();
      const localDeclarationSymbol ***REMOVED*** resolveName(
        node,
        node.name.escapedText,
        3 /* Variable */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        false
      );
      if (localDeclarationSymbol && localDeclarationSymbol !***REMOVED******REMOVED*** symbol && localDeclarationSymbol.flags & 2 /* BlockScopedVariable */) {
        if (getDeclarationNodeFlagsFromSymbol(localDeclarationSymbol) & 7 /* BlockScoped */) {
          const varDeclList ***REMOVED*** getAncestor(localDeclarationSymbol.valueDeclaration, 262 /* VariableDeclarationList */);
          const container ***REMOVED*** varDeclList.parent.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */ && varDeclList.parent.parent ? varDeclList.parent.parent : void 0;
          const namesShareScope ***REMOVED*** container && (container.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */ && isFunctionLike(container.parent) || container.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || container.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || container.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */);
          if (!namesShareScope) {
            const name ***REMOVED*** symbolToString(localDeclarationSymbol);
            error(node, Diagnostics.Cannot_initialize_outer_scoped_variable_0_in_the_same_scope_as_block_scoped_declaration_1, name, name);
          }
        }
      }
    }
  }
  function convertAutoToAny(type) {
    return type ***REMOVED******REMOVED******REMOVED*** autoType ? anyType : type ***REMOVED******REMOVED******REMOVED*** autoArrayType ? anyArrayType : type;
  }
  function checkVariableLikeDeclaration(node) {
    var _a;
    checkDecorators(node);
    if (!isBindingElement(node)) {
      checkSourceElement(node.type);
    }
    if (!node.name) {
      return;
    }
    if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
      checkComputedPropertyName(node.name);
      if (hasOnlyExpressionInitializer(node) && node.initializer) {
        checkExpressionCached(node.initializer);
      }
    }
    if (isBindingElement(node)) {
      if (node.propertyName && isIdentifier(node.name) && isPartOfParameterDeclaration(node) && nodeIsMissing(getContainingFunction(node).body)) {
        potentialUnusedRenamedBindingElementsInTypes.push(node);
        return;
      }
      if (isObjectBindingPattern(node.parent) && node.dotDotDotToken && languageVersion < LanguageFeatureMinimumTarget.ObjectSpreadRest) {
        checkExternalEmitHelpers(node, 4 /* Rest */);
      }
      if (node.propertyName && node.propertyName.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        checkComputedPropertyName(node.propertyName);
      }
      const parent ***REMOVED*** node.parent.parent;
      const parentCheckMode ***REMOVED*** node.dotDotDotToken ? 32 /* RestBindingElement */ : 0 /* Normal */;
      const parentType ***REMOVED*** getTypeForBindingElementParent(parent, parentCheckMode);
      const name ***REMOVED*** node.propertyName || node.name;
      if (parentType && !isBindingPattern(name)) {
        const exprType ***REMOVED*** getLiteralTypeFromPropertyName(name);
        if (isTypeUsableAsPropertyName(exprType)) {
          const nameText ***REMOVED*** getPropertyNameFromType(exprType);
          const property ***REMOVED*** getPropertyOfType(parentType, nameText);
          if (property) {
            markPropertyAsReferenced(
              property,
              /*nodeForCheckWriteOnly*/
              void 0,
              /*isSelfTypeAccess*/
              false
            );
            checkPropertyAccessibility(
              node,
              !!parent.initializer && parent.initializer.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */,
              /*writing*/
              false,
              parentType,
              property
            );
          }
        }
      }
    }
    if (isBindingPattern(node.name)) {
      if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */ && languageVersion < LanguageFeatureMinimumTarget.BindingPatterns && compilerOptions.downlevelIteration) {
        checkExternalEmitHelpers(node, 512 /* Read */);
      }
      forEach(node.name.elements, checkSourceElement);
    }
    if (node.initializer && isPartOfParameterDeclaration(node) && nodeIsMissing(getContainingFunction(node).body)) {
      error(node, Diagnostics.A_parameter_initializer_is_only_allowed_in_a_function_or_constructor_implementation);
      return;
    }
    if (isBindingPattern(node.name)) {
      if (isInAmbientOrTypeNode(node)) {
        return;
      }
      const needCheckInitializer ***REMOVED*** hasOnlyExpressionInitializer(node) && node.initializer && node.parent.parent.kind !***REMOVED******REMOVED*** 250 /* ForInStatement */;
      const needCheckWidenedType ***REMOVED*** !some(node.name.elements, not(isOmittedExpression));
      if (needCheckInitializer || needCheckWidenedType) {
        const widenedType ***REMOVED*** getWidenedTypeForVariableLikeDeclaration(node);
        if (needCheckInitializer) {
          const initializerType ***REMOVED*** checkExpressionCached(node.initializer);
          if (strictNullChecks && needCheckWidenedType) {
            checkNonNullNonVoidType(initializerType, node);
          } else {
            checkTypeAssignableToAndOptionallyElaborate(initializerType, getWidenedTypeForVariableLikeDeclaration(node), node, node.initializer);
          }
        }
        if (needCheckWidenedType) {
          if (isArrayBindingPattern(node.name)) {
            checkIteratedTypeOrElementType(65 /* Destructuring */, widenedType, undefinedType, node);
          } else if (strictNullChecks) {
            checkNonNullNonVoidType(widenedType, node);
          }
        }
      }
      return;
    }
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    if (symbol.flags & 2097152 /* Alias */ && (isVariableDeclarationInitializedToBareOrAccessedRequire(node) || isBindingElementOfBareOrAccessedRequire(node))) {
      checkAliasSymbol(node);
      return;
    }
    if (node.name.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */) {
      error(node.name, Diagnostics.A_bigint_literal_cannot_be_used_as_a_property_name);
    }
    const type ***REMOVED*** convertAutoToAny(getTypeOfSymbol(symbol));
    if (node ***REMOVED******REMOVED******REMOVED*** symbol.valueDeclaration) {
      const initializer ***REMOVED*** hasOnlyExpressionInitializer(node) && getEffectiveInitializer(node);
      if (initializer) {
        const isJSObjectLiteralInitializer ***REMOVED*** isInJSFile(node) && isObjectLiteralExpression(initializer) && (initializer.properties.length ***REMOVED******REMOVED******REMOVED*** 0 || isPrototypeAccess(node.name)) && !!((_a ***REMOVED*** symbol.exports) ***REMOVED******REMOVED*** null ? void 0 : _a.size);
        if (!isJSObjectLiteralInitializer && node.parent.parent.kind !***REMOVED******REMOVED*** 250 /* ForInStatement */) {
          const initializerType ***REMOVED*** checkExpressionCached(initializer);
          checkTypeAssignableToAndOptionallyElaborate(
            initializerType,
            type,
            node,
            initializer,
            /*headMessage*/
            void 0
          );
          const blockScopeKind ***REMOVED*** getCombinedNodeFlagsCached(node) & 7 /* BlockScoped */;
          if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
            const globalAsyncDisposableType ***REMOVED*** getGlobalAsyncDisposableType(
              /*reportErrors*/
              true
            );
            const globalDisposableType ***REMOVED*** getGlobalDisposableType(
              /*reportErrors*/
              true
            );
            if (globalAsyncDisposableType !***REMOVED******REMOVED*** emptyObjectType && globalDisposableType !***REMOVED******REMOVED*** emptyObjectType) {
              const optionalDisposableType ***REMOVED*** getUnionType([globalAsyncDisposableType, globalDisposableType, nullType, undefinedType]);
              checkTypeAssignableTo(widenTypeForVariableLikeDeclaration(initializerType, node), optionalDisposableType, initializer, Diagnostics.The_initializer_of_an_await_using_declaration_must_be_either_an_object_with_a_Symbol_asyncDispose_or_Symbol_dispose_method_or_be_null_or_undefined);
            }
          } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */) {
            const globalDisposableType ***REMOVED*** getGlobalDisposableType(
              /*reportErrors*/
              true
            );
            if (globalDisposableType !***REMOVED******REMOVED*** emptyObjectType) {
              const optionalDisposableType ***REMOVED*** getUnionType([globalDisposableType, nullType, undefinedType]);
              checkTypeAssignableTo(widenTypeForVariableLikeDeclaration(initializerType, node), optionalDisposableType, initializer, Diagnostics.The_initializer_of_a_using_declaration_must_be_either_an_object_with_a_Symbol_dispose_method_or_be_null_or_undefined);
            }
          }
        }
      }
      if (symbol.declarations && symbol.declarations.length > 1) {
        if (some(symbol.declarations, (d) ***REMOVED***> d !***REMOVED******REMOVED*** node && isVariableLike(d) && !areDeclarationFlagsIdentical(d, node))) {
          error(node.name, Diagnostics.All_declarations_of_0_must_have_identical_modifiers, declarationNameToString(node.name));
        }
      }
    } else {
      const declarationType ***REMOVED*** convertAutoToAny(getWidenedTypeForVariableLikeDeclaration(node));
      if (!isErrorType(type) && !isErrorType(declarationType) && !isTypeIdenticalTo(type, declarationType) && !(symbol.flags & 67108864 /* Assignment */)) {
        errorNextVariableOrPropertyDeclarationMustHaveSameType(symbol.valueDeclaration, type, node, declarationType);
      }
      if (hasOnlyExpressionInitializer(node) && node.initializer) {
        checkTypeAssignableToAndOptionallyElaborate(
          checkExpressionCached(node.initializer),
          declarationType,
          node,
          node.initializer,
          /*headMessage*/
          void 0
        );
      }
      if (symbol.valueDeclaration && !areDeclarationFlagsIdentical(node, symbol.valueDeclaration)) {
        error(node.name, Diagnostics.All_declarations_of_0_must_have_identical_modifiers, declarationNameToString(node.name));
      }
    }
    if (node.kind !***REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && node.kind !***REMOVED******REMOVED*** 172 /* PropertySignature */) {
      checkExportsOnMergedDeclarations(node);
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */) {
        checkVarDeclaredNamesNotShadowed(node);
      }
      checkCollisionsForDeclarationName(node, node.name);
    }
  }
  function errorNextVariableOrPropertyDeclarationMustHaveSameType(firstDeclaration, firstType, nextDeclaration, nextType) {
    const nextDeclarationName ***REMOVED*** getNameOfDeclaration(nextDeclaration);
    const message ***REMOVED*** nextDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || nextDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ ? Diagnostics.Subsequent_property_declarations_must_have_the_same_type_Property_0_must_be_of_type_1_but_here_has_type_2 : Diagnostics.Subsequent_variable_declarations_must_have_the_same_type_Variable_0_must_be_of_type_1_but_here_has_type_2;
    const declName ***REMOVED*** declarationNameToString(nextDeclarationName);
    const err ***REMOVED*** error(
      nextDeclarationName,
      message,
      declName,
      typeToString(firstType),
      typeToString(nextType)
    );
    if (firstDeclaration) {
      addRelatedInfo(err, createDiagnosticForNode(firstDeclaration, Diagnostics._0_was_also_declared_here, declName));
    }
  }
  function areDeclarationFlagsIdentical(left, right) {
    if (left.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && right.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || left.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ && right.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
      return true;
    }
    if (hasQuestionToken(left) !***REMOVED******REMOVED*** hasQuestionToken(right)) {
      return false;
    }
    const interestingFlags ***REMOVED*** 2 /* Private */ | 4 /* Protected */ | 1024 /* Async */ | 64 /* Abstract */ | 8 /* Readonly */ | 256 /* Static */;
    return getSelectedEffectiveModifierFlags(left, interestingFlags) ***REMOVED******REMOVED******REMOVED*** getSelectedEffectiveModifierFlags(right, interestingFlags);
  }
  function checkVariableDeclaration(node) {
    var _a, _b;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Check, "checkVariableDeclaration", { kind: node.kind, pos: node.pos, end: node.end, path: node.tracingPath });
    checkGrammarVariableDeclaration(node);
    checkVariableLikeDeclaration(node);
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  }
  function checkBindingElement(node) {
    checkGrammarBindingElement(node);
    return checkVariableLikeDeclaration(node);
  }
  function checkVariableDeclarationList(node) {
    const blockScopeKind ***REMOVED*** getCombinedNodeFlags(node) & 7 /* BlockScoped */;
    if ((blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ || blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) && languageVersion < LanguageFeatureMinimumTarget.UsingAndAwaitUsing) {
      checkExternalEmitHelpers(node, 16777216 /* AddDisposableResourceAndDisposeResources */);
    }
    forEach(node.declarations, checkSourceElement);
  }
  function checkVariableStatement(node) {
    if (!checkGrammarModifiers(node) && !checkGrammarVariableDeclarationList(node.declarationList)) checkGrammarForDisallowedBlockScopedVariableStatement(node);
    checkVariableDeclarationList(node.declarationList);
  }
  function checkExpressionStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    checkExpression(node.expression);
  }
  function checkIfStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    const type ***REMOVED*** checkTruthinessExpression(node.expression);
    checkTestingKnownTruthyCallableOrAwaitableOrEnumMemberType(node.expression, type, node.thenStatement);
    checkSourceElement(node.thenStatement);
    if (node.thenStatement.kind ***REMOVED******REMOVED******REMOVED*** 243 /* EmptyStatement */) {
      error(node.thenStatement, Diagnostics.The_body_of_an_if_statement_cannot_be_the_empty_statement);
    }
    checkSourceElement(node.elseStatement);
  }
  function checkTestingKnownTruthyCallableOrAwaitableOrEnumMemberType(condExpr, condType, body) {
    if (!strictNullChecks) return;
    bothHelper(condExpr, body);
    function bothHelper(condExpr2, body2) {
      condExpr2 ***REMOVED*** skipParentheses(condExpr2);
      helper(condExpr2, body2);
      while (isBinaryExpression(condExpr2) && (condExpr2.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ || condExpr2.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */)) {
        condExpr2 ***REMOVED*** skipParentheses(condExpr2.left);
        helper(condExpr2, body2);
      }
    }
    function helper(condExpr2, body2) {
      const location ***REMOVED*** isLogicalOrCoalescingBinaryExpression(condExpr2) ? skipParentheses(condExpr2.right) : condExpr2;
      if (isModuleExportsAccessExpression(location)) {
        return;
      }
      if (isLogicalOrCoalescingBinaryExpression(location)) {
        bothHelper(location, body2);
        return;
      }
      const type ***REMOVED*** location ***REMOVED******REMOVED******REMOVED*** condExpr2 ? condType : checkExpression(location);
      if (type.flags & 1024 /* EnumLiteral */ && isPropertyAccessExpression(location) && (getNodeLinks(location.expression).resolvedSymbol ?? unknownSymbol).flags & 384 /* Enum */) {
        error(location, Diagnostics.This_condition_will_always_return_0, !!type.value ? "true" : "false");
        return;
      }
      const isPropertyExpressionCast ***REMOVED*** isPropertyAccessExpression(location) && isTypeAssertion(location.expression);
      if (!hasTypeFacts(type, 4194304 /* Truthy */) || isPropertyExpressionCast) return;
      const callSignatures ***REMOVED*** getSignaturesOfType(type, 0 /* Call */);
      const isPromise ***REMOVED*** !!getAwaitedTypeOfPromise(type);
      if (callSignatures.length ***REMOVED******REMOVED******REMOVED*** 0 && !isPromise) {
        return;
      }
      const testedNode ***REMOVED*** isIdentifier(location) ? location : isPropertyAccessExpression(location) ? location.name : void 0;
      const testedSymbol ***REMOVED*** testedNode && getSymbolAtLocation(testedNode);
      if (!testedSymbol && !isPromise) {
        return;
      }
      const isUsed ***REMOVED*** testedSymbol && isBinaryExpression(condExpr2.parent) && isSymbolUsedInBinaryExpressionChain(condExpr2.parent, testedSymbol) || testedSymbol && body2 && isSymbolUsedInConditionBody(condExpr2, body2, testedNode, testedSymbol);
      if (!isUsed) {
        if (isPromise) {
          errorAndMaybeSuggestAwait(
            location,
            /*maybeMissingAwait*/
            true,
            Diagnostics.This_condition_will_always_return_true_since_this_0_is_always_defined,
            getTypeNameForErrorDisplay(type)
          );
        } else {
          error(location, Diagnostics.This_condition_will_always_return_true_since_this_function_is_always_defined_Did_you_mean_to_call_it_instead);
        }
      }
    }
  }
  function isSymbolUsedInConditionBody(expr, body, testedNode, testedSymbol) {
    return !!forEachChild(body, function check(childNode) {
      if (isIdentifier(childNode)) {
        const childSymbol ***REMOVED*** getSymbolAtLocation(childNode);
        if (childSymbol && childSymbol ***REMOVED******REMOVED******REMOVED*** testedSymbol) {
          if (isIdentifier(expr) || isIdentifier(testedNode) && isBinaryExpression(testedNode.parent)) {
            return true;
          }
          let testedExpression ***REMOVED*** testedNode.parent;
          let childExpression ***REMOVED*** childNode.parent;
          while (testedExpression && childExpression) {
            if (isIdentifier(testedExpression) && isIdentifier(childExpression) || testedExpression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && childExpression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
              return getSymbolAtLocation(testedExpression) ***REMOVED******REMOVED******REMOVED*** getSymbolAtLocation(childExpression);
            } else if (isPropertyAccessExpression(testedExpression) && isPropertyAccessExpression(childExpression)) {
              if (getSymbolAtLocation(testedExpression.name) !***REMOVED******REMOVED*** getSymbolAtLocation(childExpression.name)) {
                return false;
              }
              childExpression ***REMOVED*** childExpression.expression;
              testedExpression ***REMOVED*** testedExpression.expression;
            } else if (isCallExpression(testedExpression) && isCallExpression(childExpression)) {
              childExpression ***REMOVED*** childExpression.expression;
              testedExpression ***REMOVED*** testedExpression.expression;
            } else {
              return false;
            }
          }
        }
      }
      return forEachChild(childNode, check);
    });
  }
  function isSymbolUsedInBinaryExpressionChain(node, testedSymbol) {
    while (isBinaryExpression(node) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */) {
      const isUsed ***REMOVED*** forEachChild(node.right, function visit(child) {
        if (isIdentifier(child)) {
          const symbol ***REMOVED*** getSymbolAtLocation(child);
          if (symbol && symbol ***REMOVED******REMOVED******REMOVED*** testedSymbol) {
            return true;
          }
        }
        return forEachChild(child, visit);
      });
      if (isUsed) {
        return true;
      }
      node ***REMOVED*** node.parent;
    }
    return false;
  }
  function checkDoStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    checkSourceElement(node.statement);
    checkTruthinessExpression(node.expression);
  }
  function checkWhileStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    checkTruthinessExpression(node.expression);
    checkSourceElement(node.statement);
  }
  function checkTruthinessOfType(type, node) {
    if (type.flags & 16384 /* Void */) {
      error(node, Diagnostics.An_expression_of_type_void_cannot_be_tested_for_truthiness);
    } else {
      const semantics ***REMOVED*** getSyntacticTruthySemantics(node);
      if (semantics !***REMOVED******REMOVED*** 3 /* Sometimes */) {
        error(
          node,
          semantics ***REMOVED******REMOVED******REMOVED*** 1 /* Always */ ? Diagnostics.This_kind_of_expression_is_always_truthy : Diagnostics.This_kind_of_expression_is_always_falsy
        );
      }
    }
    return type;
  }
  function getSyntacticTruthySemantics(node) {
    node ***REMOVED*** skipOuterExpressions(node);
    switch (node.kind) {
      case 9 /* NumericLiteral */:
        if (node.text ***REMOVED******REMOVED******REMOVED*** "0" || node.text ***REMOVED******REMOVED******REMOVED*** "1") {
          return 3 /* Sometimes */;
        }
        return 1 /* Always */;
      case 210 /* ArrayLiteralExpression */:
      case 220 /* ArrowFunction */:
      case 10 /* BigIntLiteral */:
      case 232 /* ClassExpression */:
      case 219 /* FunctionExpression */:
      case 285 /* JsxElement */:
      case 286 /* JsxSelfClosingElement */:
      case 211 /* ObjectLiteralExpression */:
      case 14 /* RegularExpressionLiteral */:
        return 1 /* Always */;
      case 223 /* VoidExpression */:
      case 106 /* NullKeyword */:
        return 2 /* Never */;
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 11 /* StringLiteral */:
        return !!node.text ? 1 /* Always */ : 2 /* Never */;
      case 228 /* ConditionalExpression */:
        return getSyntacticTruthySemantics(node.whenTrue) | getSyntacticTruthySemantics(node.whenFalse);
      case 80 /* Identifier */:
        if (getResolvedSymbol(node) ***REMOVED******REMOVED******REMOVED*** undefinedSymbol) {
          return 2 /* Never */;
        }
        return 3 /* Sometimes */;
    }
    return 3 /* Sometimes */;
  }
  function checkTruthinessExpression(node, checkMode) {
    return checkTruthinessOfType(checkExpression(node, checkMode), node);
  }
  function checkForStatement(node) {
    if (!checkGrammarStatementInAmbientContext(node)) {
      if (node.initializer && node.initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
        checkGrammarVariableDeclarationList(node.initializer);
      }
    }
    if (node.initializer) {
      if (node.initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
        checkVariableDeclarationList(node.initializer);
      } else {
        checkExpression(node.initializer);
      }
    }
    if (node.condition) checkTruthinessExpression(node.condition);
    if (node.incrementor) checkExpression(node.incrementor);
    checkSourceElement(node.statement);
    if (node.locals) {
      registerForUnusedIdentifiersCheck(node);
    }
  }
  function checkForOfStatement(node) {
    checkGrammarForInOrForOfStatement(node);
    const container ***REMOVED*** getContainingFunctionOrClassStaticBlock(node);
    if (node.awaitModifier) {
      if (container && isClassStaticBlockDeclaration(container)) {
        grammarErrorOnNode(node.awaitModifier, Diagnostics.for_await_loops_cannot_be_used_inside_a_class_static_block);
      } else {
        const functionFlags ***REMOVED*** getFunctionFlags(container);
        if ((functionFlags & (4 /* Invalid */ | 2 /* Async */)) ***REMOVED******REMOVED******REMOVED*** 2 /* Async */ && languageVersion < LanguageFeatureMinimumTarget.ForAwaitOf) {
          checkExternalEmitHelpers(node, 16384 /* ForAwaitOfIncludes */);
        }
      }
    } else if (compilerOptions.downlevelIteration && languageVersion < LanguageFeatureMinimumTarget.ForOf) {
      checkExternalEmitHelpers(node, 256 /* ForOfIncludes */);
    }
    if (node.initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
      checkVariableDeclarationList(node.initializer);
    } else {
      const varExpr ***REMOVED*** node.initializer;
      const iteratedType ***REMOVED*** checkRightHandSideOfForOf(node);
      if (varExpr.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ || varExpr.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
        checkDestructuringAssignment(varExpr, iteratedType || errorType);
      } else {
        const leftType ***REMOVED*** checkExpression(varExpr);
        checkReferenceExpression(
          varExpr,
          Diagnostics.The_left_hand_side_of_a_for_of_statement_must_be_a_variable_or_a_property_access,
          Diagnostics.The_left_hand_side_of_a_for_of_statement_may_not_be_an_optional_property_access
        );
        if (iteratedType) {
          checkTypeAssignableToAndOptionallyElaborate(iteratedType, leftType, varExpr, node.expression);
        }
      }
    }
    checkSourceElement(node.statement);
    if (node.locals) {
      registerForUnusedIdentifiersCheck(node);
    }
  }
  function checkForInStatement(node) {
    checkGrammarForInOrForOfStatement(node);
    const rightType ***REMOVED*** getNonNullableTypeIfNeeded(checkExpression(node.expression));
    if (node.initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
      const variable ***REMOVED*** node.initializer.declarations[0];
      if (variable && isBindingPattern(variable.name)) {
        error(variable.name, Diagnostics.The_left_hand_side_of_a_for_in_statement_cannot_be_a_destructuring_pattern);
      }
      checkVariableDeclarationList(node.initializer);
    } else {
      const varExpr ***REMOVED*** node.initializer;
      const leftType ***REMOVED*** checkExpression(varExpr);
      if (varExpr.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */ || varExpr.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
        error(varExpr, Diagnostics.The_left_hand_side_of_a_for_in_statement_cannot_be_a_destructuring_pattern);
      } else if (!isTypeAssignableTo(getIndexTypeOrString(rightType), leftType)) {
        error(varExpr, Diagnostics.The_left_hand_side_of_a_for_in_statement_must_be_of_type_string_or_any);
      } else {
        checkReferenceExpression(
          varExpr,
          Diagnostics.The_left_hand_side_of_a_for_in_statement_must_be_a_variable_or_a_property_access,
          Diagnostics.The_left_hand_side_of_a_for_in_statement_may_not_be_an_optional_property_access
        );
      }
    }
    if (rightType ***REMOVED******REMOVED******REMOVED*** neverType || !isTypeAssignableToKind(rightType, 67108864 /* NonPrimitive */ | 58982400 /* InstantiableNonPrimitive */)) {
      error(node.expression, Diagnostics.The_right_hand_side_of_a_for_in_statement_must_be_of_type_any_an_object_type_or_a_type_parameter_but_here_has_type_0, typeToString(rightType));
    }
    checkSourceElement(node.statement);
    if (node.locals) {
      registerForUnusedIdentifiersCheck(node);
    }
  }
  function checkRightHandSideOfForOf(statement) {
    const use ***REMOVED*** statement.awaitModifier ? 15 /* ForAwaitOf */ : 13 /* ForOf */;
    return checkIteratedTypeOrElementType(use, checkNonNullExpression(statement.expression), undefinedType, statement.expression);
  }
  function checkIteratedTypeOrElementType(use, inputType, sentType, errorNode) {
    if (isTypeAny(inputType)) {
      return inputType;
    }
    return getIteratedTypeOrElementType(
      use,
      inputType,
      sentType,
      errorNode,
      /*checkAssignability*/
      true
    ) || anyType;
  }
  function getIteratedTypeOrElementType(use, inputType, sentType, errorNode, checkAssignability) {
    const allowAsyncIterables ***REMOVED*** (use & 2 /* AllowsAsyncIterablesFlag */) !***REMOVED******REMOVED*** 0;
    if (inputType ***REMOVED******REMOVED******REMOVED*** neverType) {
      if (errorNode) {
        reportTypeNotIterableError(errorNode, inputType, allowAsyncIterables);
      }
      return void 0;
    }
    const uplevelIteration ***REMOVED*** languageVersion >***REMOVED*** 2 /* ES2015 */;
    const downlevelIteration ***REMOVED*** !uplevelIteration && compilerOptions.downlevelIteration;
    const possibleOutOfBounds ***REMOVED*** compilerOptions.noUncheckedIndexedAccess && !!(use & 128 /* PossiblyOutOfBounds */);
    if (uplevelIteration || downlevelIteration || allowAsyncIterables) {
      const iterationTypes ***REMOVED*** getIterationTypesOfIterable(inputType, use, uplevelIteration ? errorNode : void 0);
      if (checkAssignability) {
        if (iterationTypes) {
          const diagnostic ***REMOVED*** use & 8 /* ForOfFlag */ ? Diagnostics.Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_for_of_will_always_send_0 : use & 32 /* SpreadFlag */ ? Diagnostics.Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_spread_will_always_send_0 : use & 64 /* DestructuringFlag */ ? Diagnostics.Cannot_iterate_value_because_the_next_method_of_its_iterator_expects_type_1_but_array_destructuring_will_always_send_0 : use & 16 /* YieldStarFlag */ ? Diagnostics.Cannot_delegate_iteration_to_value_because_the_next_method_of_its_iterator_expects_type_1_but_the_containing_generator_will_always_send_0 : void 0;
          if (diagnostic) {
            checkTypeAssignableTo(sentType, iterationTypes.nextType, errorNode, diagnostic);
          }
        }
      }
      if (iterationTypes || uplevelIteration) {
        return possibleOutOfBounds ? includeUndefinedInIndexSignature(iterationTypes && iterationTypes.yieldType) : iterationTypes && iterationTypes.yieldType;
      }
    }
    let arrayType ***REMOVED*** inputType;
    let hasStringConstituent ***REMOVED*** false;
    if (use & 4 /* AllowsStringInputFlag */) {
      if (arrayType.flags & 1048576 /* Union */) {
        const arrayTypes ***REMOVED*** inputType.types;
        const filteredTypes ***REMOVED*** filter(arrayTypes, (t) ***REMOVED***> !(t.flags & 402653316 /* StringLike */));
        if (filteredTypes !***REMOVED******REMOVED*** arrayTypes) {
          arrayType ***REMOVED*** getUnionType(filteredTypes, 2 /* Subtype */);
        }
      } else if (arrayType.flags & 402653316 /* StringLike */) {
        arrayType ***REMOVED*** neverType;
      }
      hasStringConstituent ***REMOVED*** arrayType !***REMOVED******REMOVED*** inputType;
      if (hasStringConstituent) {
        if (arrayType.flags & 131072 /* Never */) {
          return possibleOutOfBounds ? includeUndefinedInIndexSignature(stringType) : stringType;
        }
      }
    }
    if (!isArrayLikeType(arrayType)) {
      if (errorNode) {
        const allowsStrings ***REMOVED*** !!(use & 4 /* AllowsStringInputFlag */) && !hasStringConstituent;
        const [defaultDiagnostic, maybeMissingAwait] ***REMOVED*** getIterationDiagnosticDetails(allowsStrings, downlevelIteration);
        errorAndMaybeSuggestAwait(
          errorNode,
          maybeMissingAwait && !!getAwaitedTypeOfPromise(arrayType),
          defaultDiagnostic,
          typeToString(arrayType)
        );
      }
      return hasStringConstituent ? possibleOutOfBounds ? includeUndefinedInIndexSignature(stringType) : stringType : void 0;
    }
    const arrayElementType ***REMOVED*** getIndexTypeOfType(arrayType, numberType);
    if (hasStringConstituent && arrayElementType) {
      if (arrayElementType.flags & 402653316 /* StringLike */ && !compilerOptions.noUncheckedIndexedAccess) {
        return stringType;
      }
      return getUnionType(possibleOutOfBounds ? [arrayElementType, stringType, undefinedType] : [arrayElementType, stringType], 2 /* Subtype */);
    }
    return use & 128 /* PossiblyOutOfBounds */ ? includeUndefinedInIndexSignature(arrayElementType) : arrayElementType;
    function getIterationDiagnosticDetails(allowsStrings, downlevelIteration2) {
      var _a;
      if (downlevelIteration2) {
        return allowsStrings ? [Diagnostics.Type_0_is_not_an_array_type_or_a_string_type_or_does_not_have_a_Symbol_iterator_method_that_returns_an_iterator, true] : [Diagnostics.Type_0_is_not_an_array_type_or_does_not_have_a_Symbol_iterator_method_that_returns_an_iterator, true];
      }
      const yieldType ***REMOVED*** getIterationTypeOfIterable(
        use,
        0 /* Yield */,
        inputType,
        /*errorNode*/
        void 0
      );
      if (yieldType) {
        return [Diagnostics.Type_0_can_only_be_iterated_through_when_using_the_downlevelIteration_flag_or_with_a_target_of_es2015_or_higher, false];
      }
      if (isES2015OrLaterIterable((_a ***REMOVED*** inputType.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.escapedName)) {
        return [Diagnostics.Type_0_can_only_be_iterated_through_when_using_the_downlevelIteration_flag_or_with_a_target_of_es2015_or_higher, true];
      }
      return allowsStrings ? [Diagnostics.Type_0_is_not_an_array_type_or_a_string_type, true] : [Diagnostics.Type_0_is_not_an_array_type, true];
    }
  }
  function isES2015OrLaterIterable(n) {
    switch (n) {
      case "Float32Array":
      case "Float64Array":
      case "Int16Array":
      case "Int32Array":
      case "Int8Array":
      case "NodeList":
      case "Uint16Array":
      case "Uint32Array":
      case "Uint8Array":
      case "Uint8ClampedArray":
        return true;
    }
    return false;
  }
  function getIterationTypeOfIterable(use, typeKind, inputType, errorNode) {
    if (isTypeAny(inputType)) {
      return void 0;
    }
    const iterationTypes ***REMOVED*** getIterationTypesOfIterable(inputType, use, errorNode);
    return iterationTypes && iterationTypes[getIterationTypesKeyFromIterationTypeKind(typeKind)];
  }
  function createIterationTypes(yieldType ***REMOVED*** neverType, returnType ***REMOVED*** neverType, nextType ***REMOVED*** unknownType) {
    if (yieldType.flags & 67359327 /* Intrinsic */ && returnType.flags & (1 /* Any */ | 131072 /* Never */ | 2 /* Unknown */ | 16384 /* Void */ | 32768 /* Undefined */) && nextType.flags & (1 /* Any */ | 131072 /* Never */ | 2 /* Unknown */ | 16384 /* Void */ | 32768 /* Undefined */)) {
      const id ***REMOVED*** getTypeListId([yieldType, returnType, nextType]);
      let iterationTypes ***REMOVED*** iterationTypesCache.get(id);
      if (!iterationTypes) {
        iterationTypes ***REMOVED*** { yieldType, returnType, nextType };
        iterationTypesCache.set(id, iterationTypes);
      }
      return iterationTypes;
    }
    return { yieldType, returnType, nextType };
  }
  function combineIterationTypes(array) {
    let yieldTypes;
    let returnTypes;
    let nextTypes;
    for (const iterationTypes of array) {
      if (iterationTypes ***REMOVED******REMOVED******REMOVED*** void 0 || iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes) {
        continue;
      }
      if (iterationTypes ***REMOVED******REMOVED******REMOVED*** anyIterationTypes) {
        return anyIterationTypes;
      }
      yieldTypes ***REMOVED*** append(yieldTypes, iterationTypes.yieldType);
      returnTypes ***REMOVED*** append(returnTypes, iterationTypes.returnType);
      nextTypes ***REMOVED*** append(nextTypes, iterationTypes.nextType);
    }
    if (yieldTypes || returnTypes || nextTypes) {
      return createIterationTypes(
        yieldTypes && getUnionType(yieldTypes),
        returnTypes && getUnionType(returnTypes),
        nextTypes && getIntersectionType(nextTypes)
      );
    }
    return noIterationTypes;
  }
  function getCachedIterationTypes(type, cacheKey) {
    return type[cacheKey];
  }
  function setCachedIterationTypes(type, cacheKey, cachedTypes2) {
    return type[cacheKey] ***REMOVED*** cachedTypes2;
  }
  function getIterationTypesOfIterable(type, use, errorNode) {
    var _a, _b;
    if (type ***REMOVED******REMOVED******REMOVED*** silentNeverType) {
      return silentNeverIterationTypes;
    }
    if (isTypeAny(type)) {
      return anyIterationTypes;
    }
    if (!(type.flags & 1048576 /* Union */)) {
      const errorOutputContainer ***REMOVED*** errorNode ? { errors: void 0, skipLogging: true } : void 0;
      const iterationTypes2 ***REMOVED*** getIterationTypesOfIterableWorker(type, use, errorNode, errorOutputContainer);
      if (iterationTypes2 ***REMOVED******REMOVED******REMOVED*** noIterationTypes) {
        if (errorNode) {
          const rootDiag ***REMOVED*** reportTypeNotIterableError(errorNode, type, !!(use & 2 /* AllowsAsyncIterablesFlag */));
          if (errorOutputContainer ***REMOVED******REMOVED*** null ? void 0 : errorOutputContainer.errors) {
            addRelatedInfo(rootDiag, ...errorOutputContainer.errors);
          }
        }
        return void 0;
      } else if ((_a ***REMOVED*** errorOutputContainer ***REMOVED******REMOVED*** null ? void 0 : errorOutputContainer.errors) ***REMOVED******REMOVED*** null ? void 0 : _a.length) {
        for (const diag2 of errorOutputContainer.errors) {
          diagnostics.add(diag2);
        }
      }
      return iterationTypes2;
    }
    const cacheKey ***REMOVED*** use & 2 /* AllowsAsyncIterablesFlag */ ? "iterationTypesOfAsyncIterable" : "iterationTypesOfIterable";
    const cachedTypes2 ***REMOVED*** getCachedIterationTypes(type, cacheKey);
    if (cachedTypes2) return cachedTypes2 ***REMOVED******REMOVED******REMOVED*** noIterationTypes ? void 0 : cachedTypes2;
    let allIterationTypes;
    for (const constituent of type.types) {
      const errorOutputContainer ***REMOVED*** errorNode ? { errors: void 0 } : void 0;
      const iterationTypes2 ***REMOVED*** getIterationTypesOfIterableWorker(constituent, use, errorNode, errorOutputContainer);
      if (iterationTypes2 ***REMOVED******REMOVED******REMOVED*** noIterationTypes) {
        if (errorNode) {
          const rootDiag ***REMOVED*** reportTypeNotIterableError(errorNode, type, !!(use & 2 /* AllowsAsyncIterablesFlag */));
          if (errorOutputContainer ***REMOVED******REMOVED*** null ? void 0 : errorOutputContainer.errors) {
            addRelatedInfo(rootDiag, ...errorOutputContainer.errors);
          }
        }
        setCachedIterationTypes(type, cacheKey, noIterationTypes);
        return void 0;
      } else if ((_b ***REMOVED*** errorOutputContainer ***REMOVED******REMOVED*** null ? void 0 : errorOutputContainer.errors) ***REMOVED******REMOVED*** null ? void 0 : _b.length) {
        for (const diag2 of errorOutputContainer.errors) {
          diagnostics.add(diag2);
        }
      }
      allIterationTypes ***REMOVED*** append(allIterationTypes, iterationTypes2);
    }
    const iterationTypes ***REMOVED*** allIterationTypes ? combineIterationTypes(allIterationTypes) : noIterationTypes;
    setCachedIterationTypes(type, cacheKey, iterationTypes);
    return iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes ? void 0 : iterationTypes;
  }
  function getAsyncFromSyncIterationTypes(iterationTypes, errorNode) {
    if (iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes) return noIterationTypes;
    if (iterationTypes ***REMOVED******REMOVED******REMOVED*** anyIterationTypes) return anyIterationTypes;
    const { yieldType, returnType, nextType } ***REMOVED*** iterationTypes;
    if (errorNode) {
      getGlobalAwaitedSymbol(
        /*reportErrors*/
        true
      );
    }
    return createIterationTypes(
      getAwaitedType(yieldType, errorNode) || anyType,
      getAwaitedType(returnType, errorNode) || anyType,
      nextType
    );
  }
  function getIterationTypesOfIterableWorker(type, use, errorNode, errorOutputContainer) {
    if (isTypeAny(type)) {
      return anyIterationTypes;
    }
    let noCache ***REMOVED*** false;
    if (use & 2 /* AllowsAsyncIterablesFlag */) {
      const iterationTypes ***REMOVED*** getIterationTypesOfIterableCached(type, asyncIterationTypesResolver) || getIterationTypesOfIterableFast(type, asyncIterationTypesResolver);
      if (iterationTypes) {
        if (iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes && errorNode) {
          noCache ***REMOVED*** true;
        } else {
          return use & 8 /* ForOfFlag */ ? getAsyncFromSyncIterationTypes(iterationTypes, errorNode) : iterationTypes;
        }
      }
    }
    if (use & 1 /* AllowsSyncIterablesFlag */) {
      let iterationTypes ***REMOVED*** getIterationTypesOfIterableCached(type, syncIterationTypesResolver) || getIterationTypesOfIterableFast(type, syncIterationTypesResolver);
      if (iterationTypes) {
        if (iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes && errorNode) {
          noCache ***REMOVED*** true;
        } else {
          if (use & 2 /* AllowsAsyncIterablesFlag */) {
            if (iterationTypes !***REMOVED******REMOVED*** noIterationTypes) {
              iterationTypes ***REMOVED*** getAsyncFromSyncIterationTypes(iterationTypes, errorNode);
              return noCache ? iterationTypes : setCachedIterationTypes(type, "iterationTypesOfAsyncIterable", iterationTypes);
            }
          } else {
            return iterationTypes;
          }
        }
      }
    }
    if (use & 2 /* AllowsAsyncIterablesFlag */) {
      const iterationTypes ***REMOVED*** getIterationTypesOfIterableSlow(type, asyncIterationTypesResolver, errorNode, errorOutputContainer, noCache);
      if (iterationTypes !***REMOVED******REMOVED*** noIterationTypes) {
        return iterationTypes;
      }
    }
    if (use & 1 /* AllowsSyncIterablesFlag */) {
      let iterationTypes ***REMOVED*** getIterationTypesOfIterableSlow(type, syncIterationTypesResolver, errorNode, errorOutputContainer, noCache);
      if (iterationTypes !***REMOVED******REMOVED*** noIterationTypes) {
        if (use & 2 /* AllowsAsyncIterablesFlag */) {
          iterationTypes ***REMOVED*** getAsyncFromSyncIterationTypes(iterationTypes, errorNode);
          return noCache ? iterationTypes : setCachedIterationTypes(type, "iterationTypesOfAsyncIterable", iterationTypes);
        } else {
          return iterationTypes;
        }
      }
    }
    return noIterationTypes;
  }
  function getIterationTypesOfIterableCached(type, resolver) {
    return getCachedIterationTypes(type, resolver.iterableCacheKey);
  }
  function getIterationTypesOfIterableFast(type, resolver) {
    if (isReferenceToType(type, resolver.getGlobalIterableType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalIteratorObjectType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalIterableIteratorType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalGeneratorType(
      /*reportErrors*/
      false
    ))) {
      const [yieldType, returnType, nextType] ***REMOVED*** getTypeArguments(type);
      return setCachedIterationTypes(type, resolver.iterableCacheKey, createIterationTypes(resolver.resolveIterationType(
        yieldType,
        /*errorNode*/
        void 0
      ) || yieldType, resolver.resolveIterationType(
        returnType,
        /*errorNode*/
        void 0
      ) || returnType, nextType));
    }
    if (isReferenceToSomeType(type, resolver.getGlobalBuiltinIteratorTypes())) {
      const [yieldType] ***REMOVED*** getTypeArguments(type);
      const returnType ***REMOVED*** getBuiltinIteratorReturnType();
      const nextType ***REMOVED*** unknownType;
      return setCachedIterationTypes(type, resolver.iterableCacheKey, createIterationTypes(resolver.resolveIterationType(
        yieldType,
        /*errorNode*/
        void 0
      ) || yieldType, resolver.resolveIterationType(
        returnType,
        /*errorNode*/
        void 0
      ) || returnType, nextType));
    }
  }
  function getPropertyNameForKnownSymbolName(symbolName2) {
    const ctorType ***REMOVED*** getGlobalESSymbolConstructorSymbol(
      /*reportErrors*/
      false
    );
    const uniqueType ***REMOVED*** ctorType && getTypeOfPropertyOfType(getTypeOfSymbol(ctorType), escapeLeadingUnderscores(symbolName2));
    return uniqueType && isTypeUsableAsPropertyName(uniqueType) ? getPropertyNameFromType(uniqueType) : `__@${symbolName2}`;
  }
  function getIterationTypesOfIterableSlow(type, resolver, errorNode, errorOutputContainer, noCache) {
    const method ***REMOVED*** getPropertyOfType(type, getPropertyNameForKnownSymbolName(resolver.iteratorSymbolName));
    const methodType ***REMOVED*** method && !(method.flags & 16777216 /* Optional */) ? getTypeOfSymbol(method) : void 0;
    if (isTypeAny(methodType)) {
      return noCache ? anyIterationTypes : setCachedIterationTypes(type, resolver.iterableCacheKey, anyIterationTypes);
    }
    const allSignatures ***REMOVED*** methodType ? getSignaturesOfType(methodType, 0 /* Call */) : void 0;
    const validSignatures ***REMOVED*** filter(allSignatures, (sig) ***REMOVED***> getMinArgumentCount(sig) ***REMOVED******REMOVED******REMOVED*** 0);
    if (!some(validSignatures)) {
      if (errorNode && some(allSignatures)) {
        checkTypeAssignableTo(
          type,
          resolver.getGlobalIterableType(
            /*reportErrors*/
            true
          ),
          errorNode,
          /*headMessage*/
          void 0,
          /*containingMessageChain*/
          void 0,
          errorOutputContainer
        );
      }
      return noCache ? noIterationTypes : setCachedIterationTypes(type, resolver.iterableCacheKey, noIterationTypes);
    }
    const iteratorType ***REMOVED*** getIntersectionType(map(validSignatures, getReturnTypeOfSignature));
    const iterationTypes ***REMOVED*** getIterationTypesOfIteratorWorker(iteratorType, resolver, errorNode, errorOutputContainer, noCache) ?? noIterationTypes;
    return noCache ? iterationTypes : setCachedIterationTypes(type, resolver.iterableCacheKey, iterationTypes);
  }
  function reportTypeNotIterableError(errorNode, type, allowAsyncIterables) {
    const message ***REMOVED*** allowAsyncIterables ? Diagnostics.Type_0_must_have_a_Symbol_asyncIterator_method_that_returns_an_async_iterator : Diagnostics.Type_0_must_have_a_Symbol_iterator_method_that_returns_an_iterator;
    const suggestAwait ***REMOVED*** (
      // for (const x of Promise<...>) or [...Promise<...>]
      !!getAwaitedTypeOfPromise(type) || !allowAsyncIterables && isForOfStatement(errorNode.parent) && errorNode.parent.expression ***REMOVED******REMOVED******REMOVED*** errorNode && getGlobalAsyncIterableType(
        /*reportErrors*/
        false
      ) !***REMOVED******REMOVED*** emptyGenericType && isTypeAssignableTo(type, createTypeFromGenericGlobalType(getGlobalAsyncIterableType(
        /*reportErrors*/
        false
      ), [anyType, anyType, anyType]))
    );
    return errorAndMaybeSuggestAwait(errorNode, suggestAwait, message, typeToString(type));
  }
  function getIterationTypesOfIterator(type, resolver, errorNode, errorOutputContainer) {
    return getIterationTypesOfIteratorWorker(
      type,
      resolver,
      errorNode,
      errorOutputContainer,
      /*noCache*/
      false
    );
  }
  function getIterationTypesOfIteratorWorker(type, resolver, errorNode, errorOutputContainer, noCache) {
    if (isTypeAny(type)) {
      return anyIterationTypes;
    }
    let iterationTypes ***REMOVED*** getIterationTypesOfIteratorCached(type, resolver) || getIterationTypesOfIteratorFast(type, resolver);
    if (iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes && errorNode) {
      iterationTypes ***REMOVED*** void 0;
      noCache ***REMOVED*** true;
    }
    iterationTypes ?? (iterationTypes ***REMOVED*** getIterationTypesOfIteratorSlow(type, resolver, errorNode, errorOutputContainer, noCache));
    return iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes ? void 0 : iterationTypes;
  }
  function getIterationTypesOfIteratorCached(type, resolver) {
    return getCachedIterationTypes(type, resolver.iteratorCacheKey);
  }
  function getIterationTypesOfIteratorFast(type, resolver) {
    if (isReferenceToType(type, resolver.getGlobalIterableIteratorType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalIteratorType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalIteratorObjectType(
      /*reportErrors*/
      false
    )) || isReferenceToType(type, resolver.getGlobalGeneratorType(
      /*reportErrors*/
      false
    ))) {
      const [yieldType, returnType, nextType] ***REMOVED*** getTypeArguments(type);
      return setCachedIterationTypes(type, resolver.iteratorCacheKey, createIterationTypes(yieldType, returnType, nextType));
    }
    if (isReferenceToSomeType(type, resolver.getGlobalBuiltinIteratorTypes())) {
      const [yieldType] ***REMOVED*** getTypeArguments(type);
      const returnType ***REMOVED*** getBuiltinIteratorReturnType();
      const nextType ***REMOVED*** unknownType;
      return setCachedIterationTypes(type, resolver.iteratorCacheKey, createIterationTypes(yieldType, returnType, nextType));
    }
  }
  function isIteratorResult(type, kind) {
    const doneType ***REMOVED*** getTypeOfPropertyOfType(type, "done") || falseType;
    return isTypeAssignableTo(kind ***REMOVED******REMOVED******REMOVED*** 0 /* Yield */ ? falseType : trueType, doneType);
  }
  function isYieldIteratorResult(type) {
    return isIteratorResult(type, 0 /* Yield */);
  }
  function isReturnIteratorResult(type) {
    return isIteratorResult(type, 1 /* Return */);
  }
  function getIterationTypesOfIteratorResult(type) {
    if (isTypeAny(type)) {
      return anyIterationTypes;
    }
    const cachedTypes2 ***REMOVED*** getCachedIterationTypes(type, "iterationTypesOfIteratorResult");
    if (cachedTypes2) {
      return cachedTypes2;
    }
    if (isReferenceToType(type, getGlobalIteratorYieldResultType(
      /*reportErrors*/
      false
    ))) {
      const yieldType2 ***REMOVED*** getTypeArguments(type)[0];
      return setCachedIterationTypes(type, "iterationTypesOfIteratorResult", createIterationTypes(
        yieldType2,
        /*returnType*/
        void 0,
        /*nextType*/
        void 0
      ));
    }
    if (isReferenceToType(type, getGlobalIteratorReturnResultType(
      /*reportErrors*/
      false
    ))) {
      const returnType2 ***REMOVED*** getTypeArguments(type)[0];
      return setCachedIterationTypes(type, "iterationTypesOfIteratorResult", createIterationTypes(
        /*yieldType*/
        void 0,
        returnType2,
        /*nextType*/
        void 0
      ));
    }
    const yieldIteratorResult ***REMOVED*** filterType(type, isYieldIteratorResult);
    const yieldType ***REMOVED*** yieldIteratorResult !***REMOVED******REMOVED*** neverType ? getTypeOfPropertyOfType(yieldIteratorResult, "value") : void 0;
    const returnIteratorResult ***REMOVED*** filterType(type, isReturnIteratorResult);
    const returnType ***REMOVED*** returnIteratorResult !***REMOVED******REMOVED*** neverType ? getTypeOfPropertyOfType(returnIteratorResult, "value") : void 0;
    if (!yieldType && !returnType) {
      return setCachedIterationTypes(type, "iterationTypesOfIteratorResult", noIterationTypes);
    }
    return setCachedIterationTypes(type, "iterationTypesOfIteratorResult", createIterationTypes(
      yieldType,
      returnType || voidType,
      /*nextType*/
      void 0
    ));
  }
  function getIterationTypesOfMethod(type, resolver, methodName, errorNode, errorOutputContainer) {
    var _a, _b, _c, _d;
    const method ***REMOVED*** getPropertyOfType(type, methodName);
    if (!method && methodName !***REMOVED******REMOVED*** "next") {
      return void 0;
    }
    const methodType ***REMOVED*** method && !(methodName ***REMOVED******REMOVED******REMOVED*** "next" && method.flags & 16777216 /* Optional */) ? methodName ***REMOVED******REMOVED******REMOVED*** "next" ? getTypeOfSymbol(method) : getTypeWithFacts(getTypeOfSymbol(method), 2097152 /* NEUndefinedOrNull */) : void 0;
    if (isTypeAny(methodType)) {
      return anyIterationTypes;
    }
    const methodSignatures ***REMOVED*** methodType ? getSignaturesOfType(methodType, 0 /* Call */) : emptyArray;
    if (methodSignatures.length ***REMOVED******REMOVED******REMOVED*** 0) {
      if (errorNode) {
        const diagnostic ***REMOVED*** methodName ***REMOVED******REMOVED******REMOVED*** "next" ? resolver.mustHaveANextMethodDiagnostic : resolver.mustBeAMethodDiagnostic;
        if (errorOutputContainer) {
          errorOutputContainer.errors ?? (errorOutputContainer.errors ***REMOVED*** []);
          errorOutputContainer.errors.push(createDiagnosticForNode(errorNode, diagnostic, methodName));
        } else {
          error(errorNode, diagnostic, methodName);
        }
      }
      return methodName ***REMOVED******REMOVED******REMOVED*** "next" ? noIterationTypes : void 0;
    }
    if ((methodType ***REMOVED******REMOVED*** null ? void 0 : methodType.symbol) && methodSignatures.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const globalGeneratorType ***REMOVED*** resolver.getGlobalGeneratorType(
        /*reportErrors*/
        false
      );
      const globalIteratorType ***REMOVED*** resolver.getGlobalIteratorType(
        /*reportErrors*/
        false
      );
      const isGeneratorMethod ***REMOVED*** ((_b ***REMOVED*** (_a ***REMOVED*** globalGeneratorType.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.members) ***REMOVED******REMOVED*** null ? void 0 : _b.get(methodName)) ***REMOVED******REMOVED******REMOVED*** methodType.symbol;
      const isIteratorMethod ***REMOVED*** !isGeneratorMethod && ((_d ***REMOVED*** (_c ***REMOVED*** globalIteratorType.symbol) ***REMOVED******REMOVED*** null ? void 0 : _c.members) ***REMOVED******REMOVED*** null ? void 0 : _d.get(methodName)) ***REMOVED******REMOVED******REMOVED*** methodType.symbol;
      if (isGeneratorMethod || isIteratorMethod) {
        const globalType ***REMOVED*** isGeneratorMethod ? globalGeneratorType : globalIteratorType;
        const { mapper } ***REMOVED*** methodType;
        return createIterationTypes(
          getMappedType(globalType.typeParameters[0], mapper),
          getMappedType(globalType.typeParameters[1], mapper),
          methodName ***REMOVED******REMOVED******REMOVED*** "next" ? getMappedType(globalType.typeParameters[2], mapper) : void 0
        );
      }
    }
    let methodParameterTypes;
    let methodReturnTypes;
    for (const signature of methodSignatures) {
      if (methodName !***REMOVED******REMOVED*** "throw" && some(signature.parameters)) {
        methodParameterTypes ***REMOVED*** append(methodParameterTypes, getTypeAtPosition(signature, 0));
      }
      methodReturnTypes ***REMOVED*** append(methodReturnTypes, getReturnTypeOfSignature(signature));
    }
    let returnTypes;
    let nextType;
    if (methodName !***REMOVED******REMOVED*** "throw") {
      const methodParameterType ***REMOVED*** methodParameterTypes ? getUnionType(methodParameterTypes) : unknownType;
      if (methodName ***REMOVED******REMOVED******REMOVED*** "next") {
        nextType ***REMOVED*** methodParameterType;
      } else if (methodName ***REMOVED******REMOVED******REMOVED*** "return") {
        const resolvedMethodParameterType ***REMOVED*** resolver.resolveIterationType(methodParameterType, errorNode) || anyType;
        returnTypes ***REMOVED*** append(returnTypes, resolvedMethodParameterType);
      }
    }
    let yieldType;
    const methodReturnType ***REMOVED*** methodReturnTypes ? getIntersectionType(methodReturnTypes) : neverType;
    const resolvedMethodReturnType ***REMOVED*** resolver.resolveIterationType(methodReturnType, errorNode) || anyType;
    const iterationTypes ***REMOVED*** getIterationTypesOfIteratorResult(resolvedMethodReturnType);
    if (iterationTypes ***REMOVED******REMOVED******REMOVED*** noIterationTypes) {
      if (errorNode) {
        if (errorOutputContainer) {
          errorOutputContainer.errors ?? (errorOutputContainer.errors ***REMOVED*** []);
          errorOutputContainer.errors.push(createDiagnosticForNode(errorNode, resolver.mustHaveAValueDiagnostic, methodName));
        } else {
          error(errorNode, resolver.mustHaveAValueDiagnostic, methodName);
        }
      }
      yieldType ***REMOVED*** anyType;
      returnTypes ***REMOVED*** append(returnTypes, anyType);
    } else {
      yieldType ***REMOVED*** iterationTypes.yieldType;
      returnTypes ***REMOVED*** append(returnTypes, iterationTypes.returnType);
    }
    return createIterationTypes(yieldType, getUnionType(returnTypes), nextType);
  }
  function getIterationTypesOfIteratorSlow(type, resolver, errorNode, errorOutputContainer, noCache) {
    const iterationTypes ***REMOVED*** combineIterationTypes([
      getIterationTypesOfMethod(type, resolver, "next", errorNode, errorOutputContainer),
      getIterationTypesOfMethod(type, resolver, "return", errorNode, errorOutputContainer),
      getIterationTypesOfMethod(type, resolver, "throw", errorNode, errorOutputContainer)
    ]);
    return noCache ? iterationTypes : setCachedIterationTypes(type, resolver.iteratorCacheKey, iterationTypes);
  }
  function getIterationTypeOfGeneratorFunctionReturnType(kind, returnType, isAsyncGenerator) {
    if (isTypeAny(returnType)) {
      return void 0;
    }
    const iterationTypes ***REMOVED*** getIterationTypesOfGeneratorFunctionReturnType(returnType, isAsyncGenerator);
    return iterationTypes && iterationTypes[getIterationTypesKeyFromIterationTypeKind(kind)];
  }
  function getIterationTypesOfGeneratorFunctionReturnType(type, isAsyncGenerator) {
    if (isTypeAny(type)) {
      return anyIterationTypes;
    }
    const use ***REMOVED*** isAsyncGenerator ? 2 /* AsyncGeneratorReturnType */ : 1 /* GeneratorReturnType */;
    const resolver ***REMOVED*** isAsyncGenerator ? asyncIterationTypesResolver : syncIterationTypesResolver;
    return getIterationTypesOfIterable(
      type,
      use,
      /*errorNode*/
      void 0
    ) || getIterationTypesOfIterator(
      type,
      resolver,
      /*errorNode*/
      void 0,
      /*errorOutputContainer*/
      void 0
    );
  }
  function checkBreakOrContinueStatement(node) {
    if (!checkGrammarStatementInAmbientContext(node)) checkGrammarBreakOrContinueStatement(node);
  }
  function unwrapReturnType(returnType, functionFlags) {
    const isGenerator ***REMOVED*** !!(functionFlags & 1 /* Generator */);
    const isAsync ***REMOVED*** !!(functionFlags & 2 /* Async */);
    if (isGenerator) {
      const returnIterationType ***REMOVED*** getIterationTypeOfGeneratorFunctionReturnType(1 /* Return */, returnType, isAsync);
      if (!returnIterationType) {
        return errorType;
      }
      return isAsync ? getAwaitedTypeNoAlias(unwrapAwaitedType(returnIterationType)) : returnIterationType;
    }
    return isAsync ? getAwaitedTypeNoAlias(returnType) || errorType : returnType;
  }
  function isUnwrappedReturnTypeUndefinedVoidOrAny(func, returnType) {
    const type ***REMOVED*** unwrapReturnType(returnType, getFunctionFlags(func));
    return !!(type && (maybeTypeOfKind(type, 16384 /* Void */) || type.flags & (1 /* Any */ | 32768 /* Undefined */)));
  }
  function checkReturnStatement(node) {
    if (checkGrammarStatementInAmbientContext(node)) {
      return;
    }
    const container ***REMOVED*** getContainingFunctionOrClassStaticBlock(node);
    if (container && isClassStaticBlockDeclaration(container)) {
      grammarErrorOnFirstToken(node, Diagnostics.A_return_statement_cannot_be_used_inside_a_class_static_block);
      return;
    }
    if (!container) {
      grammarErrorOnFirstToken(node, Diagnostics.A_return_statement_can_only_be_used_within_a_function_body);
      return;
    }
    const signature ***REMOVED*** getSignatureFromDeclaration(container);
    const returnType ***REMOVED*** getReturnTypeOfSignature(signature);
    if (strictNullChecks || node.expression || returnType.flags & 131072 /* Never */) {
      const exprType ***REMOVED*** node.expression ? checkExpressionCached(node.expression) : undefinedType;
      if (container.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
        if (node.expression) {
          error(node, Diagnostics.Setters_cannot_return_a_value);
        }
      } else if (container.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
        const exprType2 ***REMOVED*** node.expression ? checkExpressionCached(node.expression) : undefinedType;
        if (node.expression && !checkTypeAssignableToAndOptionallyElaborate(exprType2, returnType, node, node.expression)) {
          error(node, Diagnostics.Return_type_of_constructor_signature_must_be_assignable_to_the_instance_type_of_the_class);
        }
      } else if (getReturnTypeFromAnnotation(container)) {
        const unwrappedReturnType ***REMOVED*** unwrapReturnType(returnType, getFunctionFlags(container)) ?? returnType;
        checkReturnExpression(container, unwrappedReturnType, node, node.expression, exprType);
      }
    } else if (container.kind !***REMOVED******REMOVED*** 177 /* Constructor */ && compilerOptions.noImplicitReturns && !isUnwrappedReturnTypeUndefinedVoidOrAny(container, returnType)) {
      error(node, Diagnostics.Not_all_code_paths_return_a_value);
    }
  }
  function checkReturnExpression(container, unwrappedReturnType, node, expr, exprType, inConditionalExpression ***REMOVED*** false) {
    const excludeJSDocTypeAssertions ***REMOVED*** isInJSFile(node);
    const functionFlags ***REMOVED*** getFunctionFlags(container);
    if (expr) {
      const unwrappedExpr ***REMOVED*** skipParentheses(expr, excludeJSDocTypeAssertions);
      if (isConditionalExpression(unwrappedExpr)) {
        checkReturnExpression(
          container,
          unwrappedReturnType,
          node,
          unwrappedExpr.whenTrue,
          checkExpression(unwrappedExpr.whenTrue),
          /*inConditionalExpression*/
          true
        );
        checkReturnExpression(
          container,
          unwrappedReturnType,
          node,
          unwrappedExpr.whenFalse,
          checkExpression(unwrappedExpr.whenFalse),
          /*inConditionalExpression*/
          true
        );
        return;
      }
    }
    const inReturnStatement ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */;
    const unwrappedExprType ***REMOVED*** functionFlags & 2 /* Async */ ? checkAwaitedType(
      exprType,
      /*withAlias*/
      false,
      node,
      Diagnostics.The_return_type_of_an_async_function_must_either_be_a_valid_promise_or_must_not_contain_a_callable_then_member
    ) : exprType;
    const effectiveExpr ***REMOVED*** expr && getEffectiveCheckNode(expr);
    const errorNode ***REMOVED*** inReturnStatement && !inConditionalExpression ? node : effectiveExpr;
    checkTypeAssignableToAndOptionallyElaborate(unwrappedExprType, unwrappedReturnType, errorNode, effectiveExpr);
  }
  function checkWithStatement(node) {
    if (!checkGrammarStatementInAmbientContext(node)) {
      if (node.flags & 65536 /* AwaitContext */) {
        grammarErrorOnFirstToken(node, Diagnostics.with_statements_are_not_allowed_in_an_async_function_block);
      }
    }
    checkExpression(node.expression);
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile)) {
      const start ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos).start;
      const end ***REMOVED*** node.statement.pos;
      grammarErrorAtPos(sourceFile, start, end - start, Diagnostics.The_with_statement_is_not_supported_All_symbols_in_a_with_block_will_have_type_any);
    }
  }
  function checkSwitchStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    let firstDefaultClause;
    let hasDuplicateDefaultClause ***REMOVED*** false;
    const expressionType ***REMOVED*** checkExpression(node.expression);
    forEach(node.caseBlock.clauses, (clause) ***REMOVED***> {
      if (clause.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */ && !hasDuplicateDefaultClause) {
        if (firstDefaultClause ***REMOVED******REMOVED******REMOVED*** void 0) {
          firstDefaultClause ***REMOVED*** clause;
        } else {
          grammarErrorOnNode(clause, Diagnostics.A_default_clause_cannot_appear_more_than_once_in_a_switch_statement);
          hasDuplicateDefaultClause ***REMOVED*** true;
        }
      }
      if (clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */) {
        addLazyDiagnostic(createLazyCaseClauseDiagnostics(clause));
      }
      forEach(clause.statements, checkSourceElement);
      if (compilerOptions.noFallthroughCasesInSwitch && clause.fallthroughFlowNode && isReachableFlowNode(clause.fallthroughFlowNode)) {
        error(clause, Diagnostics.Fallthrough_case_in_switch);
      }
      function createLazyCaseClauseDiagnostics(clause2) {
        return () ***REMOVED***> {
          const caseType ***REMOVED*** checkExpression(clause2.expression);
          if (!isTypeEqualityComparableTo(expressionType, caseType)) {
            checkTypeComparableTo(
              caseType,
              expressionType,
              clause2.expression,
              /*headMessage*/
              void 0
            );
          }
        };
      }
    });
    if (node.caseBlock.locals) {
      registerForUnusedIdentifiersCheck(node.caseBlock);
    }
  }
  function checkLabeledStatement(node) {
    if (!checkGrammarStatementInAmbientContext(node)) {
      findAncestor(node.parent, (current) ***REMOVED***> {
        if (isFunctionLike(current)) {
          return "quit";
        }
        if (current.kind ***REMOVED******REMOVED******REMOVED*** 257 /* LabeledStatement */ && current.label.escapedText ***REMOVED******REMOVED******REMOVED*** node.label.escapedText) {
          grammarErrorOnNode(node.label, Diagnostics.Duplicate_label_0, getTextOfNode(node.label));
          return true;
        }
        return false;
      });
    }
    checkSourceElement(node.statement);
  }
  function checkThrowStatement(node) {
    if (!checkGrammarStatementInAmbientContext(node)) {
      if (isIdentifier(node.expression) && !node.expression.escapedText) {
        grammarErrorAfterFirstToken(node, Diagnostics.Line_break_not_permitted_here);
      }
    }
    if (node.expression) {
      checkExpression(node.expression);
    }
  }
  function checkTryStatement(node) {
    checkGrammarStatementInAmbientContext(node);
    checkBlock(node.tryBlock);
    const catchClause ***REMOVED*** node.catchClause;
    if (catchClause) {
      if (catchClause.variableDeclaration) {
        const declaration ***REMOVED*** catchClause.variableDeclaration;
        checkVariableLikeDeclaration(declaration);
        const typeNode ***REMOVED*** getEffectiveTypeAnnotationNode(declaration);
        if (typeNode) {
          const type ***REMOVED*** getTypeFromTypeNode(typeNode);
          if (type && !(type.flags & 3 /* AnyOrUnknown */)) {
            grammarErrorOnFirstToken(typeNode, Diagnostics.Catch_clause_variable_type_annotation_must_be_any_or_unknown_if_specified);
          }
        } else if (declaration.initializer) {
          grammarErrorOnFirstToken(declaration.initializer, Diagnostics.Catch_clause_variable_cannot_have_an_initializer);
        } else {
          const blockLocals ***REMOVED*** catchClause.block.locals;
          if (blockLocals) {
            forEachKey(catchClause.locals, (caughtName) ***REMOVED***> {
              const blockLocal ***REMOVED*** blockLocals.get(caughtName);
              if ((blockLocal ***REMOVED******REMOVED*** null ? void 0 : blockLocal.valueDeclaration) && (blockLocal.flags & 2 /* BlockScopedVariable */) !***REMOVED******REMOVED*** 0) {
                grammarErrorOnNode(blockLocal.valueDeclaration, Diagnostics.Cannot_redeclare_identifier_0_in_catch_clause, unescapeLeadingUnderscores(caughtName));
              }
            });
          }
        }
      }
      checkBlock(catchClause.block);
    }
    if (node.finallyBlock) {
      checkBlock(node.finallyBlock);
    }
  }
  function checkIndexConstraints(type, symbol, isStaticIndex) {
    const indexInfos ***REMOVED*** getIndexInfosOfType(type);
    if (indexInfos.length ***REMOVED******REMOVED******REMOVED*** 0) {
      return;
    }
    for (const prop of getPropertiesOfObjectType(type)) {
      if (!(isStaticIndex && prop.flags & 4194304 /* Prototype */)) {
        checkIndexConstraintForProperty(type, prop, getLiteralTypeFromProperty(
          prop,
          8576 /* StringOrNumberLiteralOrUnique */,
          /*includeNonPublic*/
          true
        ), getNonMissingTypeOfSymbol(prop));
      }
    }
    const typeDeclaration ***REMOVED*** symbol.valueDeclaration;
    if (typeDeclaration && isClassLike(typeDeclaration)) {
      for (const member of typeDeclaration.members) {
        if ((!isStaticIndex && !isStatic(member) || isStaticIndex && isStatic(member)) && !hasBindableName(member)) {
          const symbol2 ***REMOVED*** getSymbolOfDeclaration(member);
          checkIndexConstraintForProperty(type, symbol2, getTypeOfExpression(member.name.expression), getNonMissingTypeOfSymbol(symbol2));
        }
      }
    }
    if (indexInfos.length > 1) {
      for (const info of indexInfos) {
        checkIndexConstraintForIndexSignature(type, info);
      }
    }
  }
  function checkIndexConstraintForProperty(type, prop, propNameType, propType) {
    const declaration ***REMOVED*** prop.valueDeclaration;
    const name ***REMOVED*** getNameOfDeclaration(declaration);
    if (name && isPrivateIdentifier(name)) {
      return;
    }
    const indexInfos ***REMOVED*** getApplicableIndexInfos(type, propNameType);
    const interfaceDeclaration ***REMOVED*** getObjectFlags(type) & 2 /* Interface */ ? getDeclarationOfKind(type.symbol, 265 /* InterfaceDeclaration */) : void 0;
    const propDeclaration ***REMOVED*** declaration && declaration.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ || name && name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ ? declaration : void 0;
    const localPropDeclaration ***REMOVED*** getParentOfSymbol(prop) ***REMOVED******REMOVED******REMOVED*** type.symbol ? declaration : void 0;
    for (const info of indexInfos) {
      const localIndexDeclaration ***REMOVED*** info.declaration && getParentOfSymbol(getSymbolOfDeclaration(info.declaration)) ***REMOVED******REMOVED******REMOVED*** type.symbol ? info.declaration : void 0;
      const errorNode ***REMOVED*** localPropDeclaration || localIndexDeclaration || (interfaceDeclaration && !some(getBaseTypes(type), (base) ***REMOVED***> !!getPropertyOfObjectType(base, prop.escapedName) && !!getIndexTypeOfType(base, info.keyType)) ? interfaceDeclaration : void 0);
      if (errorNode && !isTypeAssignableTo(propType, info.type)) {
        const diagnostic ***REMOVED*** createError(errorNode, Diagnostics.Property_0_of_type_1_is_not_assignable_to_2_index_type_3, symbolToString(prop), typeToString(propType), typeToString(info.keyType), typeToString(info.type));
        if (propDeclaration && errorNode !***REMOVED******REMOVED*** propDeclaration) {
          addRelatedInfo(diagnostic, createDiagnosticForNode(propDeclaration, Diagnostics._0_is_declared_here, symbolToString(prop)));
        }
        diagnostics.add(diagnostic);
      }
    }
  }
  function checkIndexConstraintForIndexSignature(type, checkInfo) {
    const declaration ***REMOVED*** checkInfo.declaration;
    const indexInfos ***REMOVED*** getApplicableIndexInfos(type, checkInfo.keyType);
    const interfaceDeclaration ***REMOVED*** getObjectFlags(type) & 2 /* Interface */ ? getDeclarationOfKind(type.symbol, 265 /* InterfaceDeclaration */) : void 0;
    const localCheckDeclaration ***REMOVED*** declaration && getParentOfSymbol(getSymbolOfDeclaration(declaration)) ***REMOVED******REMOVED******REMOVED*** type.symbol ? declaration : void 0;
    for (const info of indexInfos) {
      if (info ***REMOVED******REMOVED******REMOVED*** checkInfo) continue;
      const localIndexDeclaration ***REMOVED*** info.declaration && getParentOfSymbol(getSymbolOfDeclaration(info.declaration)) ***REMOVED******REMOVED******REMOVED*** type.symbol ? info.declaration : void 0;
      const errorNode ***REMOVED*** localCheckDeclaration || localIndexDeclaration || (interfaceDeclaration && !some(getBaseTypes(type), (base) ***REMOVED***> !!getIndexInfoOfType(base, checkInfo.keyType) && !!getIndexTypeOfType(base, info.keyType)) ? interfaceDeclaration : void 0);
      if (errorNode && !isTypeAssignableTo(checkInfo.type, info.type)) {
        error(errorNode, Diagnostics._0_index_type_1_is_not_assignable_to_2_index_type_3, typeToString(checkInfo.keyType), typeToString(checkInfo.type), typeToString(info.keyType), typeToString(info.type));
      }
    }
  }
  function checkTypeNameIsReserved(name, message) {
    switch (name.escapedText) {
      case "any":
      case "unknown":
      case "never":
      case "number":
      case "bigint":
      case "boolean":
      case "string":
      case "symbol":
      case "void":
      case "object":
      case "undefined":
        error(name, message, name.escapedText);
    }
  }
  function checkClassNameCollisionWithObject(name) {
    if (languageVersion >***REMOVED*** 1 /* ES5 */ && name.escapedText ***REMOVED******REMOVED******REMOVED*** "Object" && host.getEmitModuleFormatOfFile(getSourceFileOfNode(name)) < 5 /* ES2015 */) {
      error(name, Diagnostics.Class_name_cannot_be_Object_when_targeting_ES5_and_above_with_module_0, ModuleKind[moduleKind]);
    }
  }
  function checkUnmatchedJSDocParameters(node) {
    const jsdocParameters ***REMOVED*** filter(getJSDocTags(node), isJSDocParameterTag);
    if (!length(jsdocParameters)) return;
    const isJs ***REMOVED*** isInJSFile(node);
    const parameters ***REMOVED*** /* @__PURE__ */ new Set();
    const excludedParameters ***REMOVED*** /* @__PURE__ */ new Set();
    forEach(node.parameters, ({ name }, index) ***REMOVED***> {
      if (isIdentifier(name)) {
        parameters.add(name.escapedText);
      }
      if (isBindingPattern(name)) {
        excludedParameters.add(index);
      }
    });
    const containsArguments ***REMOVED*** containsArgumentsReference(node);
    if (containsArguments) {
      const lastJSDocParamIndex ***REMOVED*** jsdocParameters.length - 1;
      const lastJSDocParam ***REMOVED*** jsdocParameters[lastJSDocParamIndex];
      if (isJs && lastJSDocParam && isIdentifier(lastJSDocParam.name) && lastJSDocParam.typeExpression && lastJSDocParam.typeExpression.type && !parameters.has(lastJSDocParam.name.escapedText) && !excludedParameters.has(lastJSDocParamIndex) && !isArrayType(getTypeFromTypeNode(lastJSDocParam.typeExpression.type))) {
        error(lastJSDocParam.name, Diagnostics.JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name_It_would_match_arguments_if_it_had_an_array_type, idText(lastJSDocParam.name));
      }
    } else {
      forEach(jsdocParameters, ({ name, isNameFirst }, index) ***REMOVED***> {
        if (excludedParameters.has(index) || isIdentifier(name) && parameters.has(name.escapedText)) {
          return;
        }
        if (isQualifiedName(name)) {
          if (isJs) {
            error(name, Diagnostics.Qualified_name_0_is_not_allowed_without_a_leading_param_object_1, entityNameToString(name), entityNameToString(name.left));
          }
        } else {
          if (!isNameFirst) {
            errorOrSuggestion(isJs, name, Diagnostics.JSDoc_param_tag_has_name_0_but_there_is_no_parameter_with_that_name, idText(name));
          }
        }
      });
    }
  }
  function checkTypeParameters(typeParameterDeclarations) {
    let seenDefault ***REMOVED*** false;
    if (typeParameterDeclarations) {
      for (let i ***REMOVED*** 0; i < typeParameterDeclarations.length; i++) {
        const node ***REMOVED*** typeParameterDeclarations[i];
        checkTypeParameter(node);
        addLazyDiagnostic(createCheckTypeParameterDiagnostic(node, i));
      }
    }
    function createCheckTypeParameterDiagnostic(node, i) {
      return () ***REMOVED***> {
        if (node.default) {
          seenDefault ***REMOVED*** true;
          checkTypeParametersNotReferenced(node.default, typeParameterDeclarations, i);
        } else if (seenDefault) {
          error(node, Diagnostics.Required_type_parameters_may_not_follow_optional_type_parameters);
        }
        for (let j ***REMOVED*** 0; j < i; j++) {
          if (typeParameterDeclarations[j].symbol ***REMOVED******REMOVED******REMOVED*** node.symbol) {
            error(node.name, Diagnostics.Duplicate_identifier_0, declarationNameToString(node.name));
          }
        }
      };
    }
  }
  function checkTypeParametersNotReferenced(root, typeParameters, index) {
    visit(root);
    function visit(node) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */) {
        const type ***REMOVED*** getTypeFromTypeReference(node);
        if (type.flags & 262144 /* TypeParameter */) {
          for (let i ***REMOVED*** index; i < typeParameters.length; i++) {
            if (type.symbol ***REMOVED******REMOVED******REMOVED*** getSymbolOfDeclaration(typeParameters[i])) {
              error(node, Diagnostics.Type_parameter_defaults_can_only_reference_previously_declared_type_parameters);
            }
          }
        }
      }
      forEachChild(node, visit);
    }
  }
  function checkTypeParameterListsIdentical(symbol) {
    if (symbol.declarations && symbol.declarations.length ***REMOVED******REMOVED******REMOVED*** 1) {
      return;
    }
    const links ***REMOVED*** getSymbolLinks(symbol);
    if (!links.typeParametersChecked) {
      links.typeParametersChecked ***REMOVED*** true;
      const declarations ***REMOVED*** getClassOrInterfaceDeclarationsOfSymbol(symbol);
      if (!declarations || declarations.length <***REMOVED*** 1) {
        return;
      }
      const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
      if (!areTypeParametersIdentical(declarations, type.localTypeParameters, getEffectiveTypeParameterDeclarations)) {
        const name ***REMOVED*** symbolToString(symbol);
        for (const declaration of declarations) {
          error(declaration.name, Diagnostics.All_declarations_of_0_must_have_identical_type_parameters, name);
        }
      }
    }
  }
  function areTypeParametersIdentical(declarations, targetParameters, getTypeParameterDeclarations) {
    const maxTypeArgumentCount ***REMOVED*** length(targetParameters);
    const minTypeArgumentCount ***REMOVED*** getMinTypeArgumentCount(targetParameters);
    for (const declaration of declarations) {
      const sourceParameters ***REMOVED*** getTypeParameterDeclarations(declaration);
      const numTypeParameters ***REMOVED*** sourceParameters.length;
      if (numTypeParameters < minTypeArgumentCount || numTypeParameters > maxTypeArgumentCount) {
        return false;
      }
      for (let i ***REMOVED*** 0; i < numTypeParameters; i++) {
        const source ***REMOVED*** sourceParameters[i];
        const target ***REMOVED*** targetParameters[i];
        if (source.name.escapedText !***REMOVED******REMOVED*** target.symbol.escapedName) {
          return false;
        }
        const constraint ***REMOVED*** getEffectiveConstraintOfTypeParameter(source);
        const sourceConstraint ***REMOVED*** constraint && getTypeFromTypeNode(constraint);
        const targetConstraint ***REMOVED*** getConstraintOfTypeParameter(target);
        if (sourceConstraint && targetConstraint && !isTypeIdenticalTo(sourceConstraint, targetConstraint)) {
          return false;
        }
        const sourceDefault ***REMOVED*** source.default && getTypeFromTypeNode(source.default);
        const targetDefault ***REMOVED*** getDefaultFromTypeParameter(target);
        if (sourceDefault && targetDefault && !isTypeIdenticalTo(sourceDefault, targetDefault)) {
          return false;
        }
      }
    }
    return true;
  }
  function getFirstTransformableStaticClassElement(node) {
    const willTransformStaticElementsOfDecoratedClass ***REMOVED*** !legacyDecorators && languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators && classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    );
    const willTransformPrivateElementsOrClassStaticBlocks ***REMOVED*** languageVersion < LanguageFeatureMinimumTarget.PrivateNamesAndClassStaticBlocks || languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators;
    const willTransformInitializers ***REMOVED*** !emitStandardClassFields;
    if (willTransformStaticElementsOfDecoratedClass || willTransformPrivateElementsOrClassStaticBlocks) {
      for (const member of node.members) {
        if (willTransformStaticElementsOfDecoratedClass && classElementOrClassElementParameterIsDecorated(
          /*useLegacyDecorators*/
          false,
          member,
          node
        )) {
          return firstOrUndefined(getDecorators(node)) ?? node;
        } else if (willTransformPrivateElementsOrClassStaticBlocks) {
          if (isClassStaticBlockDeclaration(member)) {
            return member;
          } else if (isStatic(member)) {
            if (isPrivateIdentifierClassElementDeclaration(member) || willTransformInitializers && isInitializedProperty(member)) {
              return member;
            }
          }
        }
      }
    }
  }
  function checkClassExpressionExternalHelpers(node) {
    if (node.name) return;
    const parent ***REMOVED*** walkUpOuterExpressions(node);
    if (!isNamedEvaluationSource(parent)) return;
    const willTransformESDecorators ***REMOVED*** !legacyDecorators && languageVersion < LanguageFeatureMinimumTarget.ClassAndClassElementDecorators;
    let location;
    if (willTransformESDecorators && classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    )) {
      location ***REMOVED*** firstOrUndefined(getDecorators(node)) ?? node;
    } else {
      location ***REMOVED*** getFirstTransformableStaticClassElement(node);
    }
    if (location) {
      checkExternalEmitHelpers(location, 4194304 /* SetFunctionName */);
      if ((isPropertyAssignment(parent) || isPropertyDeclaration(parent) || isBindingElement(parent)) && isComputedPropertyName(parent.name)) {
        checkExternalEmitHelpers(location, 8388608 /* PropKey */);
      }
    }
  }
  function checkClassExpression(node) {
    checkClassLikeDeclaration(node);
    checkNodeDeferred(node);
    checkClassExpressionExternalHelpers(node);
    return getTypeOfSymbol(getSymbolOfDeclaration(node));
  }
  function checkClassExpressionDeferred(node) {
    forEach(node.members, checkSourceElement);
    registerForUnusedIdentifiersCheck(node);
  }
  function checkClassDeclaration(node) {
    const firstDecorator ***REMOVED*** find(node.modifiers, isDecorator);
    if (legacyDecorators && firstDecorator && some(node.members, (p) ***REMOVED***> hasStaticModifier(p) && isPrivateIdentifierClassElementDeclaration(p))) {
      grammarErrorOnNode(firstDecorator, Diagnostics.Class_decorators_can_t_be_used_with_static_private_identifier_Consider_removing_the_experimental_decorator);
    }
    if (!node.name && !hasSyntacticModifier(node, 2048 /* Default */)) {
      grammarErrorOnFirstToken(node, Diagnostics.A_class_declaration_without_the_default_modifier_must_have_a_name);
    }
    checkClassLikeDeclaration(node);
    forEach(node.members, checkSourceElement);
    registerForUnusedIdentifiersCheck(node);
  }
  function checkClassLikeDeclaration(node) {
    checkGrammarClassLikeDeclaration(node);
    checkDecorators(node);
    checkCollisionsForDeclarationName(node, node.name);
    checkTypeParameters(getEffectiveTypeParameterDeclarations(node));
    checkExportsOnMergedDeclarations(node);
    const symbol ***REMOVED*** getSymbolOfDeclaration(node);
    const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
    const typeWithThis ***REMOVED*** getTypeWithThisArgument(type);
    const staticType ***REMOVED*** getTypeOfSymbol(symbol);
    checkTypeParameterListsIdentical(symbol);
    checkFunctionOrConstructorSymbol(symbol);
    checkClassForDuplicateDeclarations(node);
    const nodeInAmbientContext ***REMOVED*** !!(node.flags & 33554432 /* Ambient */);
    if (!nodeInAmbientContext) {
      checkClassForStaticPropertyNameConflicts(node);
    }
    const baseTypeNode ***REMOVED*** getEffectiveBaseTypeNode(node);
    if (baseTypeNode) {
      forEach(baseTypeNode.typeArguments, checkSourceElement);
      if (languageVersion < LanguageFeatureMinimumTarget.Classes) {
        checkExternalEmitHelpers(baseTypeNode.parent, 1 /* Extends */);
      }
      const extendsNode ***REMOVED*** getClassExtendsHeritageElement(node);
      if (extendsNode && extendsNode !***REMOVED******REMOVED*** baseTypeNode) {
        checkExpression(extendsNode.expression);
      }
      const baseTypes ***REMOVED*** getBaseTypes(type);
      if (baseTypes.length) {
        addLazyDiagnostic(() ***REMOVED***> {
          const baseType ***REMOVED*** baseTypes[0];
          const baseConstructorType ***REMOVED*** getBaseConstructorTypeOfClass(type);
          const staticBaseType ***REMOVED*** getApparentType(baseConstructorType);
          checkBaseTypeAccessibility(staticBaseType, baseTypeNode);
          checkSourceElement(baseTypeNode.expression);
          if (some(baseTypeNode.typeArguments)) {
            forEach(baseTypeNode.typeArguments, checkSourceElement);
            for (const constructor of getConstructorsForTypeArguments(staticBaseType, baseTypeNode.typeArguments, baseTypeNode)) {
              if (!checkTypeArgumentConstraints(baseTypeNode, constructor.typeParameters)) {
                break;
              }
            }
          }
          const baseWithThis ***REMOVED*** getTypeWithThisArgument(baseType, type.thisType);
          if (!checkTypeAssignableTo(
            typeWithThis,
            baseWithThis,
            /*errorNode*/
            void 0
          )) {
            issueMemberSpecificError(node, typeWithThis, baseWithThis, Diagnostics.Class_0_incorrectly_extends_base_class_1);
          } else {
            checkTypeAssignableTo(staticType, getTypeWithoutSignatures(staticBaseType), node.name || node, Diagnostics.Class_static_side_0_incorrectly_extends_base_class_static_side_1);
          }
          if (baseConstructorType.flags & 8650752 /* TypeVariable */) {
            if (!isMixinConstructorType(staticType)) {
              error(node.name || node, Diagnostics.A_mixin_class_must_have_a_constructor_with_a_single_rest_parameter_of_type_any);
            } else {
              const constructSignatures ***REMOVED*** getSignaturesOfType(baseConstructorType, 1 /* Construct */);
              if (constructSignatures.some((signature) ***REMOVED***> signature.flags & 4 /* Abstract */) && !hasSyntacticModifier(node, 64 /* Abstract */)) {
                error(node.name || node, Diagnostics.A_mixin_class_that_extends_from_a_type_variable_containing_an_abstract_construct_signature_must_also_be_declared_abstract);
              }
            }
          }
          if (!(staticBaseType.symbol && staticBaseType.symbol.flags & 32 /* Class */) && !(baseConstructorType.flags & 8650752 /* TypeVariable */)) {
            const constructors ***REMOVED*** getInstantiatedConstructorsForTypeArguments(staticBaseType, baseTypeNode.typeArguments, baseTypeNode);
            if (forEach(constructors, (sig) ***REMOVED***> !isJSConstructor(sig.declaration) && !isTypeIdenticalTo(getReturnTypeOfSignature(sig), baseType))) {
              error(baseTypeNode.expression, Diagnostics.Base_constructors_must_all_have_the_same_return_type);
            }
          }
          checkKindsOfPropertyMemberOverrides(type, baseType);
        });
      }
    }
    checkMembersForOverrideModifier(node, type, typeWithThis, staticType);
    const implementedTypeNodes ***REMOVED*** getEffectiveImplementsTypeNodes(node);
    if (implementedTypeNodes) {
      for (const typeRefNode of implementedTypeNodes) {
        if (!isEntityNameExpression(typeRefNode.expression) || isOptionalChain(typeRefNode.expression)) {
          error(typeRefNode.expression, Diagnostics.A_class_can_only_implement_an_identifier_Slashqualified_name_with_optional_type_arguments);
        }
        checkTypeReferenceNode(typeRefNode);
        addLazyDiagnostic(createImplementsDiagnostics(typeRefNode));
      }
    }
    addLazyDiagnostic(() ***REMOVED***> {
      checkIndexConstraints(type, symbol);
      checkIndexConstraints(
        staticType,
        symbol,
        /*isStaticIndex*/
        true
      );
      checkTypeForDuplicateIndexSignatures(node);
      checkPropertyInitialization(node);
    });
    function createImplementsDiagnostics(typeRefNode) {
      return () ***REMOVED***> {
        const t ***REMOVED*** getReducedType(getTypeFromTypeNode(typeRefNode));
        if (!isErrorType(t)) {
          if (isValidBaseType(t)) {
            const genericDiag ***REMOVED*** t.symbol && t.symbol.flags & 32 /* Class */ ? Diagnostics.Class_0_incorrectly_implements_class_1_Did_you_mean_to_extend_1_and_inherit_its_members_as_a_subclass : Diagnostics.Class_0_incorrectly_implements_interface_1;
            const baseWithThis ***REMOVED*** getTypeWithThisArgument(t, type.thisType);
            if (!checkTypeAssignableTo(
              typeWithThis,
              baseWithThis,
              /*errorNode*/
              void 0
            )) {
              issueMemberSpecificError(node, typeWithThis, baseWithThis, genericDiag);
            }
          } else {
            error(typeRefNode, Diagnostics.A_class_can_only_implement_an_object_type_or_intersection_of_object_types_with_statically_known_members);
          }
        }
      };
    }
  }
  function checkMembersForOverrideModifier(node, type, typeWithThis, staticType) {
    const baseTypeNode ***REMOVED*** getEffectiveBaseTypeNode(node);
    const baseTypes ***REMOVED*** baseTypeNode && getBaseTypes(type);
    const baseWithThis ***REMOVED*** (baseTypes ***REMOVED******REMOVED*** null ? void 0 : baseTypes.length) ? getTypeWithThisArgument(first(baseTypes), type.thisType) : void 0;
    const baseStaticType ***REMOVED*** getBaseConstructorTypeOfClass(type);
    for (const member of node.members) {
      if (hasAmbientModifier(member)) {
        continue;
      }
      if (isConstructorDeclaration(member)) {
        forEach(member.parameters, (param) ***REMOVED***> {
          if (isParameterPropertyDeclaration(param, member)) {
            checkExistingMemberForOverrideModifier(
              node,
              staticType,
              baseStaticType,
              baseWithThis,
              type,
              typeWithThis,
              param,
              /*memberIsParameterProperty*/
              true
            );
          }
        });
      }
      checkExistingMemberForOverrideModifier(
        node,
        staticType,
        baseStaticType,
        baseWithThis,
        type,
        typeWithThis,
        member,
        /*memberIsParameterProperty*/
        false
      );
    }
  }
  function checkExistingMemberForOverrideModifier(node, staticType, baseStaticType, baseWithThis, type, typeWithThis, member, memberIsParameterProperty, reportErrors2 ***REMOVED*** true) {
    const declaredProp ***REMOVED*** member.name && getSymbolAtLocation(member.name) || getSymbolAtLocation(member);
    if (!declaredProp) {
      return 0 /* Ok */;
    }
    return checkMemberForOverrideModifier(
      node,
      staticType,
      baseStaticType,
      baseWithThis,
      type,
      typeWithThis,
      hasOverrideModifier(member),
      hasAbstractModifier(member),
      isStatic(member),
      memberIsParameterProperty,
      declaredProp,
      reportErrors2 ? member : void 0
    );
  }
  function checkMemberForOverrideModifier(node, staticType, baseStaticType, baseWithThis, type, typeWithThis, memberHasOverrideModifier, memberHasAbstractModifier, memberIsStatic, memberIsParameterProperty, member, errorNode) {
    const isJs ***REMOVED*** isInJSFile(node);
    const nodeInAmbientContext ***REMOVED*** !!(node.flags & 33554432 /* Ambient */);
    if (memberHasOverrideModifier && (member ***REMOVED******REMOVED*** null ? void 0 : member.valueDeclaration) && isClassElement(member.valueDeclaration) && member.valueDeclaration.name && isNonBindableDynamicName(member.valueDeclaration.name)) {
      error(
        errorNode,
        isJs ? Diagnostics.This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_name_is_dynamic : Diagnostics.This_member_cannot_have_an_override_modifier_because_its_name_is_dynamic
      );
      return 2 /* HasInvalidOverride */;
    }
    if (baseWithThis && (memberHasOverrideModifier || compilerOptions.noImplicitOverride)) {
      const thisType ***REMOVED*** memberIsStatic ? staticType : typeWithThis;
      const baseType ***REMOVED*** memberIsStatic ? baseStaticType : baseWithThis;
      const prop ***REMOVED*** getPropertyOfType(thisType, member.escapedName);
      const baseProp ***REMOVED*** getPropertyOfType(baseType, member.escapedName);
      const baseClassName ***REMOVED*** typeToString(baseWithThis);
      if (prop && !baseProp && memberHasOverrideModifier) {
        if (errorNode) {
          const suggestion ***REMOVED*** getSuggestedSymbolForNonexistentClassMember(symbolName(member), baseType);
          suggestion ? error(
            errorNode,
            isJs ? Diagnostics.This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base_class_0_Did_you_mean_1 : Diagnostics.This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0_Did_you_mean_1,
            baseClassName,
            symbolToString(suggestion)
          ) : error(
            errorNode,
            isJs ? Diagnostics.This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_it_is_not_declared_in_the_base_class_0 : Diagnostics.This_member_cannot_have_an_override_modifier_because_it_is_not_declared_in_the_base_class_0,
            baseClassName
          );
        }
        return 2 /* HasInvalidOverride */;
      } else if (prop && (baseProp ***REMOVED******REMOVED*** null ? void 0 : baseProp.declarations) && compilerOptions.noImplicitOverride && !nodeInAmbientContext) {
        const baseHasAbstract ***REMOVED*** some(baseProp.declarations, hasAbstractModifier);
        if (memberHasOverrideModifier) {
          return 0 /* Ok */;
        }
        if (!baseHasAbstract) {
          if (errorNode) {
            const diag2 ***REMOVED*** memberIsParameterProperty ? isJs ? Diagnostics.This_parameter_property_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_in_the_base_class_0 : Diagnostics.This_parameter_property_must_have_an_override_modifier_because_it_overrides_a_member_in_base_class_0 : isJs ? Diagnostics.This_member_must_have_a_JSDoc_comment_with_an_override_tag_because_it_overrides_a_member_in_the_base_class_0 : Diagnostics.This_member_must_have_an_override_modifier_because_it_overrides_a_member_in_the_base_class_0;
            error(errorNode, diag2, baseClassName);
          }
          return 1 /* NeedsOverride */;
        } else if (memberHasAbstractModifier && baseHasAbstract) {
          if (errorNode) {
            error(errorNode, Diagnostics.This_member_must_have_an_override_modifier_because_it_overrides_an_abstract_method_that_is_declared_in_the_base_class_0, baseClassName);
          }
          return 1 /* NeedsOverride */;
        }
      }
    } else if (memberHasOverrideModifier) {
      if (errorNode) {
        const className ***REMOVED*** typeToString(type);
        error(
          errorNode,
          isJs ? Diagnostics.This_member_cannot_have_a_JSDoc_comment_with_an_override_tag_because_its_containing_class_0_does_not_extend_another_class : Diagnostics.This_member_cannot_have_an_override_modifier_because_its_containing_class_0_does_not_extend_another_class,
          className
        );
      }
      return 2 /* HasInvalidOverride */;
    }
    return 0 /* Ok */;
  }
  function issueMemberSpecificError(node, typeWithThis, baseWithThis, broadDiag) {
    let issuedMemberError ***REMOVED*** false;
    for (const member of node.members) {
      if (isStatic(member)) {
        continue;
      }
      const declaredProp ***REMOVED*** member.name && getSymbolAtLocation(member.name) || getSymbolAtLocation(member);
      if (declaredProp) {
        const prop ***REMOVED*** getPropertyOfType(typeWithThis, declaredProp.escapedName);
        const baseProp ***REMOVED*** getPropertyOfType(baseWithThis, declaredProp.escapedName);
        if (prop && baseProp) {
          const rootChain ***REMOVED*** () ***REMOVED***> chainDiagnosticMessages(
            /*details*/
            void 0,
            Diagnostics.Property_0_in_type_1_is_not_assignable_to_the_same_property_in_base_type_2,
            symbolToString(declaredProp),
            typeToString(typeWithThis),
            typeToString(baseWithThis)
          );
          if (!checkTypeAssignableTo(
            getTypeOfSymbol(prop),
            getTypeOfSymbol(baseProp),
            member.name || member,
            /*headMessage*/
            void 0,
            rootChain
          )) {
            issuedMemberError ***REMOVED*** true;
          }
        }
      }
    }
    if (!issuedMemberError) {
      checkTypeAssignableTo(typeWithThis, baseWithThis, node.name || node, broadDiag);
    }
  }
  function checkBaseTypeAccessibility(type, node) {
    const signatures ***REMOVED*** getSignaturesOfType(type, 1 /* Construct */);
    if (signatures.length) {
      const declaration ***REMOVED*** signatures[0].declaration;
      if (declaration && hasEffectiveModifier(declaration, 2 /* Private */)) {
        const typeClassDeclaration ***REMOVED*** getClassLikeDeclarationOfSymbol(type.symbol);
        if (!isNodeWithinClass(node, typeClassDeclaration)) {
          error(node, Diagnostics.Cannot_extend_a_class_0_Class_constructor_is_marked_as_private, getFullyQualifiedName(type.symbol));
        }
      }
    }
  }
  function getMemberOverrideModifierStatus(node, member, memberSymbol) {
    if (!member.name) {
      return 0 /* Ok */;
    }
    const classSymbol ***REMOVED*** getSymbolOfDeclaration(node);
    const type ***REMOVED*** getDeclaredTypeOfSymbol(classSymbol);
    const typeWithThis ***REMOVED*** getTypeWithThisArgument(type);
    const staticType ***REMOVED*** getTypeOfSymbol(classSymbol);
    const baseTypeNode ***REMOVED*** getEffectiveBaseTypeNode(node);
    const baseTypes ***REMOVED*** baseTypeNode && getBaseTypes(type);
    const baseWithThis ***REMOVED*** (baseTypes ***REMOVED******REMOVED*** null ? void 0 : baseTypes.length) ? getTypeWithThisArgument(first(baseTypes), type.thisType) : void 0;
    const baseStaticType ***REMOVED*** getBaseConstructorTypeOfClass(type);
    const memberHasOverrideModifier ***REMOVED*** member.parent ? hasOverrideModifier(member) : hasSyntacticModifier(member, 16 /* Override */);
    return checkMemberForOverrideModifier(
      node,
      staticType,
      baseStaticType,
      baseWithThis,
      type,
      typeWithThis,
      memberHasOverrideModifier,
      hasAbstractModifier(member),
      isStatic(member),
      /*memberIsParameterProperty*/
      false,
      memberSymbol
    );
  }
  function getTargetSymbol(s) {
    return getCheckFlags(s) & 1 /* Instantiated */ ? s.links.target : s;
  }
  function getClassOrInterfaceDeclarationsOfSymbol(symbol) {
    return filter(symbol.declarations, (d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || d.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */);
  }
  function checkKindsOfPropertyMemberOverrides(type, baseType) {
    var _a, _b, _c, _d, _e;
    const baseProperties ***REMOVED*** getPropertiesOfType(baseType);
    const notImplementedInfo ***REMOVED*** /* @__PURE__ */ new Map();
    basePropertyCheck: for (const baseProperty of baseProperties) {
      const base ***REMOVED*** getTargetSymbol(baseProperty);
      if (base.flags & 4194304 /* Prototype */) {
        continue;
      }
      const baseSymbol ***REMOVED*** getPropertyOfObjectType(type, base.escapedName);
      if (!baseSymbol) {
        continue;
      }
      const derived ***REMOVED*** getTargetSymbol(baseSymbol);
      const baseDeclarationFlags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(base);
      Debug.assert(!!derived, "derived should point to something, even if it is the base class' declaration.");
      if (derived ***REMOVED******REMOVED******REMOVED*** base) {
        const derivedClassDecl ***REMOVED*** getClassLikeDeclarationOfSymbol(type.symbol);
        if (baseDeclarationFlags & 64 /* Abstract */ && (!derivedClassDecl || !hasSyntacticModifier(derivedClassDecl, 64 /* Abstract */))) {
          for (const otherBaseType of getBaseTypes(type)) {
            if (otherBaseType ***REMOVED******REMOVED******REMOVED*** baseType) continue;
            const baseSymbol2 ***REMOVED*** getPropertyOfObjectType(otherBaseType, base.escapedName);
            const derivedElsewhere ***REMOVED*** baseSymbol2 && getTargetSymbol(baseSymbol2);
            if (derivedElsewhere && derivedElsewhere !***REMOVED******REMOVED*** base) {
              continue basePropertyCheck;
            }
          }
          const baseTypeName ***REMOVED*** typeToString(baseType);
          const typeName ***REMOVED*** typeToString(type);
          const basePropertyName ***REMOVED*** symbolToString(baseProperty);
          const missedProperties ***REMOVED*** append((_a ***REMOVED*** notImplementedInfo.get(derivedClassDecl)) ***REMOVED******REMOVED*** null ? void 0 : _a.missedProperties, basePropertyName);
          notImplementedInfo.set(derivedClassDecl, { baseTypeName, typeName, missedProperties });
        }
      } else {
        const derivedDeclarationFlags ***REMOVED*** getDeclarationModifierFlagsFromSymbol(derived);
        if (baseDeclarationFlags & 2 /* Private */ || derivedDeclarationFlags & 2 /* Private */) {
          continue;
        }
        let errorMessage;
        const basePropertyFlags ***REMOVED*** base.flags & 98308 /* PropertyOrAccessor */;
        const derivedPropertyFlags ***REMOVED*** derived.flags & 98308 /* PropertyOrAccessor */;
        if (basePropertyFlags && derivedPropertyFlags) {
          if ((getCheckFlags(base) & 6 /* Synthetic */ ? (_b ***REMOVED*** base.declarations) ***REMOVED******REMOVED*** null ? void 0 : _b.some((d) ***REMOVED***> isPropertyAbstractOrInterface(d, baseDeclarationFlags)) : (_c ***REMOVED*** base.declarations) ***REMOVED******REMOVED*** null ? void 0 : _c.every((d) ***REMOVED***> isPropertyAbstractOrInterface(d, baseDeclarationFlags))) || getCheckFlags(base) & 262144 /* Mapped */ || derived.valueDeclaration && isBinaryExpression(derived.valueDeclaration)) {
            continue;
          }
          const overriddenInstanceProperty ***REMOVED*** basePropertyFlags !***REMOVED******REMOVED*** 4 /* Property */ && derivedPropertyFlags ***REMOVED******REMOVED******REMOVED*** 4 /* Property */;
          const overriddenInstanceAccessor ***REMOVED*** basePropertyFlags ***REMOVED******REMOVED******REMOVED*** 4 /* Property */ && derivedPropertyFlags !***REMOVED******REMOVED*** 4 /* Property */;
          if (overriddenInstanceProperty || overriddenInstanceAccessor) {
            const errorMessage2 ***REMOVED*** overriddenInstanceProperty ? Diagnostics._0_is_defined_as_an_accessor_in_class_1_but_is_overridden_here_in_2_as_an_instance_property : Diagnostics._0_is_defined_as_a_property_in_class_1_but_is_overridden_here_in_2_as_an_accessor;
            error(getNameOfDeclaration(derived.valueDeclaration) || derived.valueDeclaration, errorMessage2, symbolToString(base), typeToString(baseType), typeToString(type));
          } else if (useDefineForClassFields) {
            const uninitialized ***REMOVED*** (_d ***REMOVED*** derived.declarations) ***REMOVED******REMOVED*** null ? void 0 : _d.find((d) ***REMOVED***> d.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && !d.initializer);
            if (uninitialized && !(derived.flags & 33554432 /* Transient */) && !(baseDeclarationFlags & 64 /* Abstract */) && !(derivedDeclarationFlags & 64 /* Abstract */) && !((_e ***REMOVED*** derived.declarations) ***REMOVED******REMOVED*** null ? void 0 : _e.some((d) ***REMOVED***> !!(d.flags & 33554432 /* Ambient */)))) {
              const constructor ***REMOVED*** findConstructorDeclaration(getClassLikeDeclarationOfSymbol(type.symbol));
              const propName ***REMOVED*** uninitialized.name;
              if (uninitialized.exclamationToken || !constructor || !isIdentifier(propName) || !strictNullChecks || !isPropertyInitializedInConstructor(propName, type, constructor)) {
                const errorMessage2 ***REMOVED*** Diagnostics.Property_0_will_overwrite_the_base_property_in_1_If_this_is_intentional_add_an_initializer_Otherwise_add_a_declare_modifier_or_remove_the_redundant_declaration;
                error(getNameOfDeclaration(derived.valueDeclaration) || derived.valueDeclaration, errorMessage2, symbolToString(base), typeToString(baseType));
              }
            }
          }
          continue;
        } else if (isPrototypeProperty(base)) {
          if (isPrototypeProperty(derived) || derived.flags & 4 /* Property */) {
            continue;
          } else {
            Debug.assert(!!(derived.flags & 98304 /* Accessor */));
            errorMessage ***REMOVED*** Diagnostics.Class_0_defines_instance_member_function_1_but_extended_class_2_defines_it_as_instance_member_accessor;
          }
        } else if (base.flags & 98304 /* Accessor */) {
          errorMessage ***REMOVED*** Diagnostics.Class_0_defines_instance_member_accessor_1_but_extended_class_2_defines_it_as_instance_member_function;
        } else {
          errorMessage ***REMOVED*** Diagnostics.Class_0_defines_instance_member_property_1_but_extended_class_2_defines_it_as_instance_member_function;
        }
        error(getNameOfDeclaration(derived.valueDeclaration) || derived.valueDeclaration, errorMessage, typeToString(baseType), symbolToString(base), typeToString(type));
      }
    }
    for (const [errorNode, memberInfo] of notImplementedInfo) {
      if (length(memberInfo.missedProperties) ***REMOVED******REMOVED******REMOVED*** 1) {
        if (isClassExpression(errorNode)) {
          error(errorNode, Diagnostics.Non_abstract_class_expression_does_not_implement_inherited_abstract_member_0_from_class_1, first(memberInfo.missedProperties), memberInfo.baseTypeName);
        } else {
          error(errorNode, Diagnostics.Non_abstract_class_0_does_not_implement_inherited_abstract_member_1_from_class_2, memberInfo.typeName, first(memberInfo.missedProperties), memberInfo.baseTypeName);
        }
      } else if (length(memberInfo.missedProperties) > 5) {
        const missedProperties ***REMOVED*** map(memberInfo.missedProperties.slice(0, 4), (prop) ***REMOVED***> `'${prop}'`).join(", ");
        const remainingMissedProperties ***REMOVED*** length(memberInfo.missedProperties) - 4;
        if (isClassExpression(errorNode)) {
          error(errorNode, Diagnostics.Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1_and_2_more, memberInfo.baseTypeName, missedProperties, remainingMissedProperties);
        } else {
          error(errorNode, Diagnostics.Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2_and_3_more, memberInfo.typeName, memberInfo.baseTypeName, missedProperties, remainingMissedProperties);
        }
      } else {
        const missedProperties ***REMOVED*** map(memberInfo.missedProperties, (prop) ***REMOVED***> `'${prop}'`).join(", ");
        if (isClassExpression(errorNode)) {
          error(errorNode, Diagnostics.Non_abstract_class_expression_is_missing_implementations_for_the_following_members_of_0_Colon_1, memberInfo.baseTypeName, missedProperties);
        } else {
          error(errorNode, Diagnostics.Non_abstract_class_0_is_missing_implementations_for_the_following_members_of_1_Colon_2, memberInfo.typeName, memberInfo.baseTypeName, missedProperties);
        }
      }
    }
  }
  function isPropertyAbstractOrInterface(declaration, baseDeclarationFlags) {
    return baseDeclarationFlags & 64 /* Abstract */ && (!isPropertyDeclaration(declaration) || !declaration.initializer) || isInterfaceDeclaration(declaration.parent);
  }
  function getNonInheritedProperties(type, baseTypes, properties) {
    if (!length(baseTypes)) {
      return properties;
    }
    const seen ***REMOVED*** /* @__PURE__ */ new Map();
    forEach(properties, (p) ***REMOVED***> {
      seen.set(p.escapedName, p);
    });
    for (const base of baseTypes) {
      const properties2 ***REMOVED*** getPropertiesOfType(getTypeWithThisArgument(base, type.thisType));
      for (const prop of properties2) {
        const existing ***REMOVED*** seen.get(prop.escapedName);
        if (existing && prop.parent ***REMOVED******REMOVED******REMOVED*** existing.parent) {
          seen.delete(prop.escapedName);
        }
      }
    }
    return arrayFrom(seen.values());
  }
  function checkInheritedPropertiesAreIdentical(type, typeNode) {
    const baseTypes ***REMOVED*** getBaseTypes(type);
    if (baseTypes.length < 2) {
      return true;
    }
    const seen ***REMOVED*** /* @__PURE__ */ new Map();
    forEach(resolveDeclaredMembers(type).declaredProperties, (p) ***REMOVED***> {
      seen.set(p.escapedName, { prop: p, containingType: type });
    });
    let ok ***REMOVED*** true;
    for (const base of baseTypes) {
      const properties ***REMOVED*** getPropertiesOfType(getTypeWithThisArgument(base, type.thisType));
      for (const prop of properties) {
        const existing ***REMOVED*** seen.get(prop.escapedName);
        if (!existing) {
          seen.set(prop.escapedName, { prop, containingType: base });
        } else {
          const isInheritedProperty ***REMOVED*** existing.containingType !***REMOVED******REMOVED*** type;
          if (isInheritedProperty && !isPropertyIdenticalTo(existing.prop, prop)) {
            ok ***REMOVED*** false;
            const typeName1 ***REMOVED*** typeToString(existing.containingType);
            const typeName2 ***REMOVED*** typeToString(base);
            let errorInfo ***REMOVED*** chainDiagnosticMessages(
              /*details*/
              void 0,
              Diagnostics.Named_property_0_of_types_1_and_2_are_not_identical,
              symbolToString(prop),
              typeName1,
              typeName2
            );
            errorInfo ***REMOVED*** chainDiagnosticMessages(errorInfo, Diagnostics.Interface_0_cannot_simultaneously_extend_types_1_and_2, typeToString(type), typeName1, typeName2);
            diagnostics.add(createDiagnosticForNodeFromMessageChain(getSourceFileOfNode(typeNode), typeNode, errorInfo));
          }
        }
      }
    }
    return ok;
  }
  function checkPropertyInitialization(node) {
    if (!strictNullChecks || !strictPropertyInitialization || node.flags & 33554432 /* Ambient */) {
      return;
    }
    const constructor ***REMOVED*** findConstructorDeclaration(node);
    for (const member of node.members) {
      if (getEffectiveModifierFlags(member) & 128 /* Ambient */) {
        continue;
      }
      if (!isStatic(member) && isPropertyWithoutInitializer(member)) {
        const propName ***REMOVED*** member.name;
        if (isIdentifier(propName) || isPrivateIdentifier(propName) || isComputedPropertyName(propName)) {
          const type ***REMOVED*** getTypeOfSymbol(getSymbolOfDeclaration(member));
          if (!(type.flags & 3 /* AnyOrUnknown */ || containsUndefinedType(type))) {
            if (!constructor || !isPropertyInitializedInConstructor(propName, type, constructor)) {
              error(member.name, Diagnostics.Property_0_has_no_initializer_and_is_not_definitely_assigned_in_the_constructor, declarationNameToString(propName));
            }
          }
        }
      }
    }
  }
  function isPropertyWithoutInitializer(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && !hasAbstractModifier(node) && !node.exclamationToken && !node.initializer;
  }
  function isPropertyInitializedInStaticBlocks(propName, propType, staticBlocks, startPos, endPos) {
    for (const staticBlock of staticBlocks) {
      if (staticBlock.pos >***REMOVED*** startPos && staticBlock.pos <***REMOVED*** endPos) {
        const reference ***REMOVED*** factory.createPropertyAccessExpression(factory.createThis(), propName);
        setParent(reference.expression, reference);
        setParent(reference, staticBlock);
        reference.flowNode ***REMOVED*** staticBlock.returnFlowNode;
        const flowType ***REMOVED*** getFlowTypeOfReference(reference, propType, getOptionalType(propType));
        if (!containsUndefinedType(flowType)) {
          return true;
        }
      }
    }
    return false;
  }
  function isPropertyInitializedInConstructor(propName, propType, constructor) {
    const reference ***REMOVED*** isComputedPropertyName(propName) ? factory.createElementAccessExpression(factory.createThis(), propName.expression) : factory.createPropertyAccessExpression(factory.createThis(), propName);
    setParent(reference.expression, reference);
    setParent(reference, constructor);
    reference.flowNode ***REMOVED*** constructor.returnFlowNode;
    const flowType ***REMOVED*** getFlowTypeOfReference(reference, propType, getOptionalType(propType));
    return !containsUndefinedType(flowType);
  }
  function checkInterfaceDeclaration(node) {
    if (!checkGrammarModifiers(node)) checkGrammarInterfaceDeclaration(node);
    if (!allowBlockDeclarations(node.parent)) {
      grammarErrorOnNode(node, Diagnostics._0_declarations_can_only_be_declared_inside_a_block, "interface");
    }
    checkTypeParameters(node.typeParameters);
    addLazyDiagnostic(() ***REMOVED***> {
      checkTypeNameIsReserved(node.name, Diagnostics.Interface_name_cannot_be_0);
      checkExportsOnMergedDeclarations(node);
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      checkTypeParameterListsIdentical(symbol);
      const firstInterfaceDecl ***REMOVED*** getDeclarationOfKind(symbol, 265 /* InterfaceDeclaration */);
      if (node ***REMOVED******REMOVED******REMOVED*** firstInterfaceDecl) {
        const type ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
        const typeWithThis ***REMOVED*** getTypeWithThisArgument(type);
        if (checkInheritedPropertiesAreIdentical(type, node.name)) {
          for (const baseType of getBaseTypes(type)) {
            checkTypeAssignableTo(typeWithThis, getTypeWithThisArgument(baseType, type.thisType), node.name, Diagnostics.Interface_0_incorrectly_extends_interface_1);
          }
          checkIndexConstraints(type, symbol);
        }
      }
      checkObjectTypeForDuplicateDeclarations(node);
    });
    forEach(getInterfaceBaseTypeNodes(node), (heritageElement) ***REMOVED***> {
      if (!isEntityNameExpression(heritageElement.expression) || isOptionalChain(heritageElement.expression)) {
        error(heritageElement.expression, Diagnostics.An_interface_can_only_extend_an_identifier_Slashqualified_name_with_optional_type_arguments);
      }
      checkTypeReferenceNode(heritageElement);
    });
    forEach(node.members, checkSourceElement);
    addLazyDiagnostic(() ***REMOVED***> {
      checkTypeForDuplicateIndexSignatures(node);
      registerForUnusedIdentifiersCheck(node);
    });
  }
  function checkTypeAliasDeclaration(node) {
    checkGrammarModifiers(node);
    checkTypeNameIsReserved(node.name, Diagnostics.Type_alias_name_cannot_be_0);
    if (!allowBlockDeclarations(node.parent)) {
      grammarErrorOnNode(node, Diagnostics._0_declarations_can_only_be_declared_inside_a_block, "type");
    }
    checkExportsOnMergedDeclarations(node);
    checkTypeParameters(node.typeParameters);
    if (node.type.kind ***REMOVED******REMOVED******REMOVED*** 141 /* IntrinsicKeyword */) {
      const typeParameterCount ***REMOVED*** length(node.typeParameters);
      const valid ***REMOVED*** typeParameterCount ***REMOVED******REMOVED******REMOVED*** 0 ? node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "BuiltinIteratorReturn" : typeParameterCount ***REMOVED******REMOVED******REMOVED*** 1 && intrinsicTypeKinds.has(node.name.escapedText);
      if (!valid) {
        error(node.type, Diagnostics.The_intrinsic_keyword_can_only_be_used_to_declare_compiler_provided_intrinsic_types);
      }
    } else {
      checkSourceElement(node.type);
      registerForUnusedIdentifiersCheck(node);
    }
  }
  function computeEnumMemberValues(node) {
    const nodeLinks2 ***REMOVED*** getNodeLinks(node);
    if (!(nodeLinks2.flags & 1024 /* EnumValuesComputed */)) {
      nodeLinks2.flags |***REMOVED*** 1024 /* EnumValuesComputed */;
      let autoValue ***REMOVED*** 0;
      let previous;
      for (const member of node.members) {
        const result ***REMOVED*** computeEnumMemberValue(member, autoValue, previous);
        getNodeLinks(member).enumMemberValue ***REMOVED*** result;
        autoValue ***REMOVED*** typeof result.value ***REMOVED******REMOVED******REMOVED*** "number" ? result.value + 1 : void 0;
        previous ***REMOVED*** member;
      }
    }
  }
  function computeEnumMemberValue(member, autoValue, previous) {
    if (isComputedNonLiteralName(member.name)) {
      error(member.name, Diagnostics.Computed_property_names_are_not_allowed_in_enums);
    } else if (isBigIntLiteral(member.name)) {
      error(member.name, Diagnostics.An_enum_member_cannot_have_a_numeric_name);
    } else {
      const text ***REMOVED*** getTextOfPropertyName(member.name);
      if (isNumericLiteralName(text) && !isInfinityOrNaNString(text)) {
        error(member.name, Diagnostics.An_enum_member_cannot_have_a_numeric_name);
      }
    }
    if (member.initializer) {
      return computeConstantEnumMemberValue(member);
    }
    if (member.parent.flags & 33554432 /* Ambient */ && !isEnumConst(member.parent)) {
      return evaluatorResult(
        /*value*/
        void 0
      );
    }
    if (autoValue ***REMOVED******REMOVED******REMOVED*** void 0) {
      error(member.name, Diagnostics.Enum_member_must_have_initializer);
      return evaluatorResult(
        /*value*/
        void 0
      );
    }
    if (getIsolatedModules(compilerOptions) && (previous ***REMOVED******REMOVED*** null ? void 0 : previous.initializer)) {
      const prevValue ***REMOVED*** getEnumMemberValue(previous);
      if (!(typeof prevValue.value ***REMOVED******REMOVED******REMOVED*** "number" && !prevValue.resolvedOtherFiles)) {
        error(
          member.name,
          Diagnostics.Enum_member_following_a_non_literal_numeric_member_must_have_an_initializer_when_isolatedModules_is_enabled
        );
      }
    }
    return evaluatorResult(autoValue);
  }
  function computeConstantEnumMemberValue(member) {
    const isConstEnum ***REMOVED*** isEnumConst(member.parent);
    const initializer ***REMOVED*** member.initializer;
    const result ***REMOVED*** evaluate(initializer, member);
    if (result.value !***REMOVED******REMOVED*** void 0) {
      if (isConstEnum && typeof result.value ***REMOVED******REMOVED******REMOVED*** "number" && !isFinite(result.value)) {
        error(
          initializer,
          isNaN(result.value) ? Diagnostics.const_enum_member_initializer_was_evaluated_to_disallowed_value_NaN : Diagnostics.const_enum_member_initializer_was_evaluated_to_a_non_finite_value
        );
      } else if (getIsolatedModules(compilerOptions) && typeof result.value ***REMOVED******REMOVED******REMOVED*** "string" && !result.isSyntacticallyString) {
        error(
          initializer,
          Diagnostics._0_has_a_string_type_but_must_have_syntactically_recognizable_string_syntax_when_isolatedModules_is_enabled,
          `${idText(member.parent.name)}.${getTextOfPropertyName(member.name)}`
        );
      }
    } else if (isConstEnum) {
      error(initializer, Diagnostics.const_enum_member_initializers_must_be_constant_expressions);
    } else if (member.parent.flags & 33554432 /* Ambient */) {
      error(initializer, Diagnostics.In_ambient_enum_declarations_member_initializer_must_be_constant_expression);
    } else {
      checkTypeAssignableTo(checkExpression(initializer), numberType, initializer, Diagnostics.Type_0_is_not_assignable_to_type_1_as_required_for_computed_enum_member_values);
    }
    return result;
  }
  function evaluateEntityNameExpression(expr, location) {
    const symbol ***REMOVED*** resolveEntityName(
      expr,
      111551 /* Value */,
      /*ignoreErrors*/
      true
    );
    if (!symbol) return evaluatorResult(
      /*value*/
      void 0
    );
    if (expr.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const identifier ***REMOVED*** expr;
      if (isInfinityOrNaNString(identifier.escapedText) && symbol ***REMOVED******REMOVED******REMOVED*** getGlobalSymbol(
        identifier.escapedText,
        111551 /* Value */,
        /*diagnostic*/
        void 0
      )) {
        return evaluatorResult(
          +identifier.escapedText,
          /*isSyntacticallyString*/
          false
        );
      }
    }
    if (symbol.flags & 8 /* EnumMember */) {
      return location ? evaluateEnumMember(expr, symbol, location) : getEnumMemberValue(symbol.valueDeclaration);
    }
    if (isConstantVariable(symbol)) {
      const declaration ***REMOVED*** symbol.valueDeclaration;
      if (declaration && isVariableDeclaration(declaration) && !declaration.type && declaration.initializer && (!location || declaration !***REMOVED******REMOVED*** location && isBlockScopedNameDeclaredBeforeUse(declaration, location))) {
        const result ***REMOVED*** evaluate(declaration.initializer, declaration);
        if (location && getSourceFileOfNode(location) !***REMOVED******REMOVED*** getSourceFileOfNode(declaration)) {
          return evaluatorResult(
            result.value,
            /*isSyntacticallyString*/
            false,
            /*resolvedOtherFiles*/
            true,
            /*hasExternalReferences*/
            true
          );
        }
        return evaluatorResult(
          result.value,
          result.isSyntacticallyString,
          result.resolvedOtherFiles,
          /*hasExternalReferences*/
          true
        );
      }
    }
    return evaluatorResult(
      /*value*/
      void 0
    );
  }
  function evaluateElementAccessExpression(expr, location) {
    const root ***REMOVED*** expr.expression;
    if (isEntityNameExpression(root) && isStringLiteralLike(expr.argumentExpression)) {
      const rootSymbol ***REMOVED*** resolveEntityName(
        root,
        111551 /* Value */,
        /*ignoreErrors*/
        true
      );
      if (rootSymbol && rootSymbol.flags & 384 /* Enum */) {
        const name ***REMOVED*** escapeLeadingUnderscores(expr.argumentExpression.text);
        const member ***REMOVED*** rootSymbol.exports.get(name);
        if (member) {
          Debug.assert(getSourceFileOfNode(member.valueDeclaration) ***REMOVED******REMOVED******REMOVED*** getSourceFileOfNode(rootSymbol.valueDeclaration));
          return location ? evaluateEnumMember(expr, member, location) : getEnumMemberValue(member.valueDeclaration);
        }
      }
    }
    return evaluatorResult(
      /*value*/
      void 0
    );
  }
  function evaluateEnumMember(expr, symbol, location) {
    const declaration ***REMOVED*** symbol.valueDeclaration;
    if (!declaration || declaration ***REMOVED******REMOVED******REMOVED*** location) {
      error(expr, Diagnostics.Property_0_is_used_before_being_assigned, symbolToString(symbol));
      return evaluatorResult(
        /*value*/
        void 0
      );
    }
    if (!isBlockScopedNameDeclaredBeforeUse(declaration, location)) {
      error(expr, Diagnostics.A_member_initializer_in_a_enum_declaration_cannot_reference_members_declared_after_it_including_members_defined_in_other_enums);
      return evaluatorResult(
        /*value*/
        0
      );
    }
    const value ***REMOVED*** getEnumMemberValue(declaration);
    if (location.parent !***REMOVED******REMOVED*** declaration.parent) {
      return evaluatorResult(
        value.value,
        value.isSyntacticallyString,
        value.resolvedOtherFiles,
        /*hasExternalReferences*/
        true
      );
    }
    return value;
  }
  function checkEnumDeclaration(node) {
    addLazyDiagnostic(() ***REMOVED***> checkEnumDeclarationWorker(node));
  }
  function checkEnumDeclarationWorker(node) {
    checkGrammarModifiers(node);
    checkCollisionsForDeclarationName(node, node.name);
    checkExportsOnMergedDeclarations(node);
    node.members.forEach(checkSourceElement);
    if (compilerOptions.erasableSyntaxOnly && !(node.flags & 33554432 /* Ambient */)) {
      error(node, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled);
    }
    computeEnumMemberValues(node);
    const enumSymbol ***REMOVED*** getSymbolOfDeclaration(node);
    const firstDeclaration ***REMOVED*** getDeclarationOfKind(enumSymbol, node.kind);
    if (node ***REMOVED******REMOVED******REMOVED*** firstDeclaration) {
      if (enumSymbol.declarations && enumSymbol.declarations.length > 1) {
        const enumIsConst ***REMOVED*** isEnumConst(node);
        forEach(enumSymbol.declarations, (decl) ***REMOVED***> {
          if (isEnumDeclaration(decl) && isEnumConst(decl) !***REMOVED******REMOVED*** enumIsConst) {
            error(getNameOfDeclaration(decl), Diagnostics.Enum_declarations_must_all_be_const_or_non_const);
          }
        });
      }
      let seenEnumMissingInitialInitializer ***REMOVED*** false;
      forEach(enumSymbol.declarations, (declaration) ***REMOVED***> {
        if (declaration.kind !***REMOVED******REMOVED*** 267 /* EnumDeclaration */) {
          return false;
        }
        const enumDeclaration ***REMOVED*** declaration;
        if (!enumDeclaration.members.length) {
          return false;
        }
        const firstEnumMember ***REMOVED*** enumDeclaration.members[0];
        if (!firstEnumMember.initializer) {
          if (seenEnumMissingInitialInitializer) {
            error(firstEnumMember.name, Diagnostics.In_an_enum_with_multiple_declarations_only_one_declaration_can_omit_an_initializer_for_its_first_enum_element);
          } else {
            seenEnumMissingInitialInitializer ***REMOVED*** true;
          }
        }
      });
    }
  }
  function checkEnumMember(node) {
    if (isPrivateIdentifier(node.name)) {
      error(node, Diagnostics.An_enum_member_cannot_be_named_with_a_private_identifier);
    }
    if (node.initializer) {
      checkExpression(node.initializer);
    }
  }
  function getFirstNonAmbientClassOrFunctionDeclaration(symbol) {
    const declarations ***REMOVED*** symbol.declarations;
    if (declarations) {
      for (const declaration of declarations) {
        if ((declaration.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || declaration.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ && nodeIsPresent(declaration.body)) && !(declaration.flags & 33554432 /* Ambient */)) {
          return declaration;
        }
      }
    }
    return void 0;
  }
  function inSameLexicalScope(node1, node2) {
    const container1 ***REMOVED*** getEnclosingBlockScopeContainer(node1);
    const container2 ***REMOVED*** getEnclosingBlockScopeContainer(node2);
    if (isGlobalSourceFile(container1)) {
      return isGlobalSourceFile(container2);
    } else if (isGlobalSourceFile(container2)) {
      return false;
    } else {
      return container1 ***REMOVED******REMOVED******REMOVED*** container2;
    }
  }
  function checkModuleDeclaration(node) {
    if (node.body) {
      checkSourceElement(node.body);
      if (!isGlobalScopeAugmentation(node)) {
        registerForUnusedIdentifiersCheck(node);
      }
    }
    addLazyDiagnostic(checkModuleDeclarationDiagnostics);
    function checkModuleDeclarationDiagnostics() {
      var _a, _b;
      const isGlobalAugmentation ***REMOVED*** isGlobalScopeAugmentation(node);
      const inAmbientContext ***REMOVED*** node.flags & 33554432 /* Ambient */;
      if (isGlobalAugmentation && !inAmbientContext) {
        error(node.name, Diagnostics.Augmentations_for_the_global_scope_should_have_declare_modifier_unless_they_appear_in_already_ambient_context);
      }
      const isAmbientExternalModule ***REMOVED*** isAmbientModule(node);
      const contextErrorMessage ***REMOVED*** isAmbientExternalModule ? Diagnostics.An_ambient_module_declaration_is_only_allowed_at_the_top_level_in_a_file : Diagnostics.A_namespace_declaration_is_only_allowed_at_the_top_level_of_a_namespace_or_module;
      if (checkGrammarModuleElementContext(node, contextErrorMessage)) {
        return;
      }
      if (!checkGrammarModifiers(node)) {
        if (!inAmbientContext && node.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
          grammarErrorOnNode(node.name, Diagnostics.Only_ambient_modules_can_use_quoted_names);
        }
      }
      if (isIdentifier(node.name)) {
        checkCollisionsForDeclarationName(node, node.name);
        if (!(node.flags & (32 /* Namespace */ | 2048 /* GlobalAugmentation */))) {
          const sourceFile ***REMOVED*** getSourceFileOfNode(node);
          const pos ***REMOVED*** getNonModifierTokenPosOfNode(node);
          const span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, pos);
          suggestionDiagnostics.add(
            createFileDiagnostic(sourceFile, span.start, span.length, Diagnostics.A_namespace_declaration_should_not_be_declared_using_the_module_keyword_Please_use_the_namespace_keyword_instead)
          );
        }
      }
      checkExportsOnMergedDeclarations(node);
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      if (symbol.flags & 512 /* ValueModule */ && !inAmbientContext && isInstantiatedModule(node, shouldPreserveConstEnums(compilerOptions))) {
        if (compilerOptions.erasableSyntaxOnly) {
          error(node.name, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled);
        }
        if (getIsolatedModules(compilerOptions) && !getSourceFileOfNode(node).externalModuleIndicator) {
          error(node.name, Diagnostics.Namespaces_are_not_allowed_in_global_script_files_when_0_is_enabled_If_this_file_is_not_intended_to_be_a_global_script_set_moduleDetection_to_force_or_add_an_empty_export_statement, isolatedModulesLikeFlagName);
        }
        if (((_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.length) > 1) {
          const firstNonAmbientClassOrFunc ***REMOVED*** getFirstNonAmbientClassOrFunctionDeclaration(symbol);
          if (firstNonAmbientClassOrFunc) {
            if (getSourceFileOfNode(node) !***REMOVED******REMOVED*** getSourceFileOfNode(firstNonAmbientClassOrFunc)) {
              error(node.name, Diagnostics.A_namespace_declaration_cannot_be_in_a_different_file_from_a_class_or_function_with_which_it_is_merged);
            } else if (node.pos < firstNonAmbientClassOrFunc.pos) {
              error(node.name, Diagnostics.A_namespace_declaration_cannot_be_located_prior_to_a_class_or_function_with_which_it_is_merged);
            }
          }
          const mergedClass ***REMOVED*** getDeclarationOfKind(symbol, 264 /* ClassDeclaration */);
          if (mergedClass && inSameLexicalScope(node, mergedClass)) {
            getNodeLinks(node).flags |***REMOVED*** 2048 /* LexicalModuleMergesWithClass */;
          }
        }
        if (compilerOptions.verbatimModuleSyntax && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && host.getEmitModuleFormatOfFile(node.parent) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
          const exportModifier ***REMOVED*** (_b ***REMOVED*** node.modifiers) ***REMOVED******REMOVED*** null ? void 0 : _b.find((m) ***REMOVED***> m.kind ***REMOVED******REMOVED******REMOVED*** 95 /* ExportKeyword */);
          if (exportModifier) {
            error(exportModifier, Diagnostics.A_top_level_export_modifier_cannot_be_used_on_value_declarations_in_a_CommonJS_module_when_verbatimModuleSyntax_is_enabled);
          }
        }
      }
      if (isAmbientExternalModule) {
        if (isExternalModuleAugmentation(node)) {
          const checkBody ***REMOVED*** isGlobalAugmentation || getSymbolOfDeclaration(node).flags & 33554432 /* Transient */;
          if (checkBody && node.body) {
            for (const statement of node.body.statements) {
              checkModuleAugmentationElement(statement, isGlobalAugmentation);
            }
          }
        } else if (isGlobalSourceFile(node.parent)) {
          if (isGlobalAugmentation) {
            error(node.name, Diagnostics.Augmentations_for_the_global_scope_can_only_be_directly_nested_in_external_modules_or_ambient_module_declarations);
          } else if (isExternalModuleNameRelative(getTextOfIdentifierOrLiteral(node.name))) {
            error(node.name, Diagnostics.Ambient_module_declaration_cannot_specify_relative_module_name);
          }
        } else {
          if (isGlobalAugmentation) {
            error(node.name, Diagnostics.Augmentations_for_the_global_scope_can_only_be_directly_nested_in_external_modules_or_ambient_module_declarations);
          } else {
            error(node.name, Diagnostics.Ambient_modules_cannot_be_nested_in_other_modules_or_namespaces);
          }
        }
      }
    }
  }
  function checkModuleAugmentationElement(node, isGlobalAugmentation) {
    switch (node.kind) {
      case 244 /* VariableStatement */:
        for (const decl of node.declarationList.declarations) {
          checkModuleAugmentationElement(decl, isGlobalAugmentation);
        }
        break;
      case 278 /* ExportAssignment */:
      case 279 /* ExportDeclaration */:
        grammarErrorOnFirstToken(node, Diagnostics.Exports_and_export_assignments_are_not_permitted_in_module_augmentations);
        break;
      case 272 /* ImportEqualsDeclaration */:
        if (isInternalModuleImportEqualsDeclaration(node)) break;
      // falls through
      case 273 /* ImportDeclaration */:
        grammarErrorOnFirstToken(node, Diagnostics.Imports_are_not_permitted_in_module_augmentations_Consider_moving_them_to_the_enclosing_external_module);
        break;
      case 209 /* BindingElement */:
      case 261 /* VariableDeclaration */:
        const name ***REMOVED*** node.name;
        if (isBindingPattern(name)) {
          for (const el of name.elements) {
            checkModuleAugmentationElement(el, isGlobalAugmentation);
          }
          break;
        }
      // falls through
      case 264 /* ClassDeclaration */:
      case 267 /* EnumDeclaration */:
      case 263 /* FunctionDeclaration */:
      case 265 /* InterfaceDeclaration */:
      case 268 /* ModuleDeclaration */:
      case 266 /* TypeAliasDeclaration */:
        if (isGlobalAugmentation) {
          return;
        }
        break;
    }
  }
  function getFirstNonModuleExportsIdentifier(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return node;
      case 167 /* QualifiedName */:
        do {
          node ***REMOVED*** node.left;
        } while (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */);
        return node;
      case 212 /* PropertyAccessExpression */:
        do {
          if (isModuleExportsAccessExpression(node.expression) && !isPrivateIdentifier(node.name)) {
            return node.name;
          }
          node ***REMOVED*** node.expression;
        } while (node.kind !***REMOVED******REMOVED*** 80 /* Identifier */);
        return node;
    }
  }
  function checkExternalImportOrExportDeclaration(node) {
    const moduleName ***REMOVED*** getExternalModuleName(node);
    if (!moduleName || nodeIsMissing(moduleName)) {
      return false;
    }
    if (!isStringLiteral(moduleName)) {
      error(moduleName, Diagnostics.String_literal_expected);
      return false;
    }
    const inAmbientExternalModule ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ && isAmbientModule(node.parent.parent);
    if (node.parent.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ && !inAmbientExternalModule) {
      error(
        moduleName,
        node.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ ? Diagnostics.Export_declarations_are_not_permitted_in_a_namespace : Diagnostics.Import_declarations_in_a_namespace_cannot_reference_a_module
      );
      return false;
    }
    if (inAmbientExternalModule && isExternalModuleNameRelative(moduleName.text)) {
      if (!isTopLevelInExternalModuleAugmentation(node)) {
        error(node, Diagnostics.Import_or_export_declaration_in_an_ambient_module_declaration_cannot_reference_module_through_relative_module_name);
        return false;
      }
    }
    if (!isImportEqualsDeclaration(node) && node.attributes) {
      const diagnostic ***REMOVED*** node.attributes.token ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ ? Diagnostics.Import_attribute_values_must_be_string_literal_expressions : Diagnostics.Import_assertion_values_must_be_string_literal_expressions;
      let hasError ***REMOVED*** false;
      for (const attr of node.attributes.elements) {
        if (!isStringLiteral(attr.value)) {
          hasError ***REMOVED*** true;
          error(attr.value, diagnostic);
        }
      }
      return !hasError;
    }
    return true;
  }
  function checkModuleExportName(name, allowStringLiteral ***REMOVED*** true) {
    if (name ***REMOVED******REMOVED******REMOVED*** void 0 || name.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
      return;
    }
    if (!allowStringLiteral) {
      grammarErrorOnNode(name, Diagnostics.Identifier_expected);
    } else if (moduleKind ***REMOVED******REMOVED******REMOVED*** 5 /* ES2015 */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 6 /* ES2020 */) {
      grammarErrorOnNode(name, Diagnostics.String_literal_import_and_export_names_are_not_supported_when_the_module_flag_is_set_to_es2015_or_es2020);
    }
  }
  function checkAliasSymbol(node) {
    var _a, _b, _c, _d, _e;
    let symbol ***REMOVED*** getSymbolOfDeclaration(node);
    const target ***REMOVED*** resolveAlias(symbol);
    if (target !***REMOVED******REMOVED*** unknownSymbol) {
      symbol ***REMOVED*** getMergedSymbol(symbol.exportSymbol || symbol);
      if (isInJSFile(node) && !(target.flags & 111551 /* Value */) && !isTypeOnlyImportOrExportDeclaration(node)) {
        const errorNode ***REMOVED*** isImportOrExportSpecifier(node) ? node.propertyName || node.name : isNamedDeclaration(node) ? node.name : node;
        Debug.assert(node.kind !***REMOVED******REMOVED*** 281 /* NamespaceExport */);
        if (node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */) {
          const diag2 ***REMOVED*** error(errorNode, Diagnostics.Types_cannot_appear_in_export_declarations_in_JavaScript_files);
          const alreadyExportedSymbol ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** getSourceFileOfNode(node).symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.exports) ***REMOVED******REMOVED*** null ? void 0 : _b.get(moduleExportNameTextEscaped(node.propertyName || node.name));
          if (alreadyExportedSymbol ***REMOVED******REMOVED******REMOVED*** target) {
            const exportingDeclaration ***REMOVED*** (_c ***REMOVED*** alreadyExportedSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _c.find(isJSDocNode);
            if (exportingDeclaration) {
              addRelatedInfo(
                diag2,
                createDiagnosticForNode(
                  exportingDeclaration,
                  Diagnostics._0_is_automatically_exported_here,
                  unescapeLeadingUnderscores(alreadyExportedSymbol.escapedName)
                )
              );
            }
          }
        } else {
          Debug.assert(node.kind !***REMOVED******REMOVED*** 261 /* VariableDeclaration */);
          const importDeclaration ***REMOVED*** findAncestor(node, or(isImportDeclaration, isImportEqualsDeclaration));
          const moduleSpecifier ***REMOVED*** (importDeclaration && ((_d ***REMOVED*** tryGetModuleSpecifierFromDeclaration(importDeclaration)) ***REMOVED******REMOVED*** null ? void 0 : _d.text)) ?? "...";
          const importedIdentifier ***REMOVED*** unescapeLeadingUnderscores(isIdentifier(errorNode) ? errorNode.escapedText : symbol.escapedName);
          error(
            errorNode,
            Diagnostics._0_is_a_type_and_cannot_be_imported_in_JavaScript_files_Use_1_in_a_JSDoc_type_annotation,
            importedIdentifier,
            `import("${moduleSpecifier}").${importedIdentifier}`
          );
        }
        return;
      }
      const targetFlags ***REMOVED*** getSymbolFlags(target);
      const excludedMeanings ***REMOVED*** (symbol.flags & (111551 /* Value */ | 1048576 /* ExportValue */) ? 111551 /* Value */ : 0) | (symbol.flags & 788968 /* Type */ ? 788968 /* Type */ : 0) | (symbol.flags & 1920 /* Namespace */ ? 1920 /* Namespace */ : 0);
      if (targetFlags & excludedMeanings) {
        const message ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 282 /* ExportSpecifier */ ? Diagnostics.Export_declaration_conflicts_with_exported_declaration_of_0 : Diagnostics.Import_declaration_conflicts_with_local_declaration_of_0;
        error(node, message, symbolToString(symbol));
      } else if (node.kind !***REMOVED******REMOVED*** 282 /* ExportSpecifier */) {
        const appearsValueyToTranspiler ***REMOVED*** compilerOptions.isolatedModules && !findAncestor(node, isTypeOnlyImportOrExportDeclaration);
        if (appearsValueyToTranspiler && symbol.flags & (111551 /* Value */ | 1048576 /* ExportValue */)) {
          error(
            node,
            Diagnostics.Import_0_conflicts_with_local_value_so_must_be_declared_with_a_type_only_import_when_isolatedModules_is_enabled,
            symbolToString(symbol),
            isolatedModulesLikeFlagName
          );
        }
      }
      if (getIsolatedModules(compilerOptions) && !isTypeOnlyImportOrExportDeclaration(node) && !(node.flags & 33554432 /* Ambient */)) {
        const typeOnlyAlias ***REMOVED*** getTypeOnlyAliasDeclaration(symbol);
        const isType ***REMOVED*** !(targetFlags & 111551 /* Value */);
        if (isType || typeOnlyAlias) {
          switch (node.kind) {
            case 274 /* ImportClause */:
            case 277 /* ImportSpecifier */:
            case 272 /* ImportEqualsDeclaration */: {
              if (compilerOptions.verbatimModuleSyntax) {
                Debug.assertIsDefined(node.name, "An ImportClause with a symbol should have a name");
                const message ***REMOVED*** compilerOptions.verbatimModuleSyntax && isInternalModuleImportEqualsDeclaration(node) ? Diagnostics.An_import_alias_cannot_resolve_to_a_type_or_type_only_declaration_when_verbatimModuleSyntax_is_enabled : isType ? Diagnostics._0_is_a_type_and_must_be_imported_using_a_type_only_import_when_verbatimModuleSyntax_is_enabled : Diagnostics._0_resolves_to_a_type_only_declaration_and_must_be_imported_using_a_type_only_import_when_verbatimModuleSyntax_is_enabled;
                const name ***REMOVED*** moduleExportNameTextUnescaped(node.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ ? node.propertyName || node.name : node.name);
                addTypeOnlyDeclarationRelatedInfo(
                  error(node, message, name),
                  isType ? void 0 : typeOnlyAlias,
                  name
                );
              }
              if (isType && node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && hasEffectiveModifier(node, 32 /* Export */)) {
                error(node, Diagnostics.Cannot_use_export_import_on_a_type_or_type_only_namespace_when_0_is_enabled, isolatedModulesLikeFlagName);
              }
              break;
            }
            case 282 /* ExportSpecifier */: {
              if (compilerOptions.verbatimModuleSyntax || getSourceFileOfNode(typeOnlyAlias) !***REMOVED******REMOVED*** getSourceFileOfNode(node)) {
                const name ***REMOVED*** moduleExportNameTextUnescaped(node.propertyName || node.name);
                const diagnostic ***REMOVED*** isType ? error(node, Diagnostics.Re_exporting_a_type_when_0_is_enabled_requires_using_export_type, isolatedModulesLikeFlagName) : error(node, Diagnostics._0_resolves_to_a_type_only_declaration_and_must_be_re_exported_using_a_type_only_re_export_when_1_is_enabled, name, isolatedModulesLikeFlagName);
                addTypeOnlyDeclarationRelatedInfo(diagnostic, isType ? void 0 : typeOnlyAlias, name);
                break;
              }
            }
          }
        }
        if (compilerOptions.verbatimModuleSyntax && node.kind !***REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && !isInJSFile(node) && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
          error(node, getVerbatimModuleSyntaxErrorMessage(node));
        } else if (moduleKind ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */ && node.kind !***REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && node.kind !***REMOVED******REMOVED*** 261 /* VariableDeclaration */ && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
          error(node, Diagnostics.ECMAScript_module_syntax_is_not_allowed_in_a_CommonJS_module_when_module_is_set_to_preserve);
        }
        if (compilerOptions.verbatimModuleSyntax && !isTypeOnlyImportOrExportDeclaration(node) && !(node.flags & 33554432 /* Ambient */) && targetFlags & 128 /* ConstEnum */) {
          const constEnumDeclaration ***REMOVED*** target.valueDeclaration;
          const redirect ***REMOVED*** (_e ***REMOVED*** host.getRedirectFromOutput(getSourceFileOfNode(constEnumDeclaration).resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _e.resolvedRef;
          if (constEnumDeclaration.flags & 33554432 /* Ambient */ && (!redirect || !shouldPreserveConstEnums(redirect.commandLine.options))) {
            error(node, Diagnostics.Cannot_access_ambient_const_enums_when_0_is_enabled, isolatedModulesLikeFlagName);
          }
        }
      }
      if (isImportSpecifier(node)) {
        const targetSymbol ***REMOVED*** resolveAliasWithDeprecationCheck(symbol, node);
        if (isDeprecatedSymbol(targetSymbol) && targetSymbol.declarations) {
          addDeprecatedSuggestion(node, targetSymbol.declarations, targetSymbol.escapedName);
        }
      }
    }
  }
  function resolveAliasWithDeprecationCheck(symbol, location) {
    if (!(symbol.flags & 2097152 /* Alias */) || isDeprecatedSymbol(symbol) || !getDeclarationOfAliasSymbol(symbol)) {
      return symbol;
    }
    const targetSymbol ***REMOVED*** resolveAlias(symbol);
    if (targetSymbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol) return targetSymbol;
    while (symbol.flags & 2097152 /* Alias */) {
      const target ***REMOVED*** getImmediateAliasedSymbol(symbol);
      if (target) {
        if (target ***REMOVED******REMOVED******REMOVED*** targetSymbol) break;
        if (target.declarations && length(target.declarations)) {
          if (isDeprecatedSymbol(target)) {
            addDeprecatedSuggestion(location, target.declarations, target.escapedName);
            break;
          } else {
            if (symbol ***REMOVED******REMOVED******REMOVED*** targetSymbol) break;
            symbol ***REMOVED*** target;
          }
        }
      } else {
        break;
      }
    }
    return targetSymbol;
  }
  function checkImportBinding(node) {
    checkCollisionsForDeclarationName(node, node.name);
    checkAliasSymbol(node);
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */) {
      checkModuleExportName(node.propertyName);
      if (moduleExportNameIsDefault(node.propertyName || node.name) && getESModuleInterop(compilerOptions) && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) < 4 /* System */) {
        checkExternalEmitHelpers(node, 131072 /* ImportDefault */);
      }
    }
  }
  function checkImportAttributes(declaration) {
    var _a;
    const node ***REMOVED*** declaration.attributes;
    if (node) {
      const importAttributesType ***REMOVED*** getGlobalImportAttributesType(
        /*reportErrors*/
        true
      );
      if (importAttributesType !***REMOVED******REMOVED*** emptyObjectType) {
        checkTypeAssignableTo(getTypeFromImportAttributes(node), getNullableType(importAttributesType, 32768 /* Undefined */), node);
      }
      const validForTypeAttributes ***REMOVED*** isExclusivelyTypeOnlyImportOrExport(declaration);
      const override ***REMOVED*** getResolutionModeOverride(node, validForTypeAttributes ? grammarErrorOnNode : void 0);
      const isImportAttributes2 ***REMOVED*** declaration.attributes.token ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */;
      if (validForTypeAttributes && override) {
        return;
      }
      if (!moduleSupportsImportAttributes(moduleKind)) {
        return grammarErrorOnNode(
          node,
          isImportAttributes2 ? Diagnostics.Import_attributes_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext_or_preserve : Diagnostics.Import_assertions_are_only_supported_when_the_module_option_is_set_to_esnext_node18_node20_nodenext_or_preserve
        );
      }
      if (102 /* Node20 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ && !isImportAttributes2) {
        return grammarErrorOnFirstToken(node, Diagnostics.Import_assertions_have_been_replaced_by_import_attributes_Use_with_instead_of_assert);
      }
      if (declaration.moduleSpecifier && getEmitSyntaxForModuleSpecifierExpression(declaration.moduleSpecifier) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
        return grammarErrorOnNode(
          node,
          isImportAttributes2 ? Diagnostics.Import_attributes_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls : Diagnostics.Import_assertions_are_not_allowed_on_statements_that_compile_to_CommonJS_require_calls
        );
      }
      const isTypeOnly ***REMOVED*** isJSDocImportTag(declaration) || (isImportDeclaration(declaration) ? (_a ***REMOVED*** declaration.importClause) ***REMOVED******REMOVED*** null ? void 0 : _a.isTypeOnly : declaration.isTypeOnly);
      if (isTypeOnly) {
        return grammarErrorOnNode(node, isImportAttributes2 ? Diagnostics.Import_attributes_cannot_be_used_with_type_only_imports_or_exports : Diagnostics.Import_assertions_cannot_be_used_with_type_only_imports_or_exports);
      }
      if (override) {
        return grammarErrorOnNode(node, Diagnostics.resolution_mode_can_only_be_set_for_type_only_imports);
      }
    }
  }
  function checkImportAttribute(node) {
    return getRegularTypeOfLiteralType(checkExpressionCached(node.value));
  }
  function checkImportDeclaration(node) {
    if (checkGrammarModuleElementContext(node, isInJSFile(node) ? Diagnostics.An_import_declaration_can_only_be_used_at_the_top_level_of_a_module : Diagnostics.An_import_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module)) {
      return;
    }
    if (!checkGrammarModifiers(node) && node.modifiers) {
      grammarErrorOnFirstToken(node, Diagnostics.An_import_declaration_cannot_have_modifiers);
    }
    if (checkExternalImportOrExportDeclaration(node)) {
      let resolvedModule;
      const importClause ***REMOVED*** node.importClause;
      if (importClause && !checkGrammarImportClause(importClause)) {
        if (importClause.name) {
          checkImportBinding(importClause);
        }
        if (importClause.namedBindings) {
          if (importClause.namedBindings.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */) {
            checkImportBinding(importClause.namedBindings);
            if (host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) < 4 /* System */ && getESModuleInterop(compilerOptions)) {
              checkExternalEmitHelpers(node, 65536 /* ImportStar */);
            }
          } else {
            resolvedModule ***REMOVED*** resolveExternalModuleName(node, node.moduleSpecifier);
            if (resolvedModule) {
              forEach(importClause.namedBindings.elements, checkImportBinding);
            }
          }
        }
        if (!importClause.isTypeOnly && 101 /* Node18 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ && isOnlyImportableAsDefault(node.moduleSpecifier, resolvedModule) && !hasTypeJsonImportAttribute(node)) {
          error(node.moduleSpecifier, Diagnostics.Importing_a_JSON_file_into_an_ECMAScript_module_requires_a_type_Colon_json_import_attribute_when_module_is_set_to_0, ModuleKind[moduleKind]);
        }
      } else if (noUncheckedSideEffectImports && !importClause) {
        void resolveExternalModuleName(node, node.moduleSpecifier);
      }
    }
    checkImportAttributes(node);
  }
  function hasTypeJsonImportAttribute(node) {
    return !!node.attributes && node.attributes.elements.some((attr) ***REMOVED***> {
      var _a;
      return getTextOfIdentifierOrLiteral(attr.name) ***REMOVED******REMOVED******REMOVED*** "type" && ((_a ***REMOVED*** tryCast(attr.value, isStringLiteralLike)) ***REMOVED******REMOVED*** null ? void 0 : _a.text) ***REMOVED******REMOVED******REMOVED*** "json";
    });
  }
  function checkImportEqualsDeclaration(node) {
    if (checkGrammarModuleElementContext(node, isInJSFile(node) ? Diagnostics.An_import_declaration_can_only_be_used_at_the_top_level_of_a_module : Diagnostics.An_import_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module)) {
      return;
    }
    checkGrammarModifiers(node);
    if (compilerOptions.erasableSyntaxOnly && !(node.flags & 33554432 /* Ambient */)) {
      error(node, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled);
    }
    if (isInternalModuleImportEqualsDeclaration(node) || checkExternalImportOrExportDeclaration(node)) {
      checkImportBinding(node);
      markLinkedReferences(node, 6 /* ExportImportEquals */);
      if (node.moduleReference.kind !***REMOVED******REMOVED*** 284 /* ExternalModuleReference */) {
        const target ***REMOVED*** resolveAlias(getSymbolOfDeclaration(node));
        if (target !***REMOVED******REMOVED*** unknownSymbol) {
          const targetFlags ***REMOVED*** getSymbolFlags(target);
          if (targetFlags & 111551 /* Value */) {
            const moduleName ***REMOVED*** getFirstIdentifier(node.moduleReference);
            if (!(resolveEntityName(moduleName, 111551 /* Value */ | 1920 /* Namespace */).flags & 1920 /* Namespace */)) {
              error(moduleName, Diagnostics.Module_0_is_hidden_by_a_local_declaration_with_the_same_name, declarationNameToString(moduleName));
            }
          }
          if (targetFlags & 788968 /* Type */) {
            checkTypeNameIsReserved(node.name, Diagnostics.Import_name_cannot_be_0);
          }
        }
        if (node.isTypeOnly) {
          grammarErrorOnNode(node, Diagnostics.An_import_alias_cannot_use_import_type);
        }
      } else {
        if (5 /* ES2015 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 99 /* ESNext */ && !node.isTypeOnly && !(node.flags & 33554432 /* Ambient */)) {
          grammarErrorOnNode(node, Diagnostics.Import_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_import_Asterisk_as_ns_from_mod_import_a_from_mod_import_d_from_mod_or_another_module_format_instead);
        }
      }
    }
  }
  function checkExportDeclaration(node) {
    if (checkGrammarModuleElementContext(node, isInJSFile(node) ? Diagnostics.An_export_declaration_can_only_be_used_at_the_top_level_of_a_module : Diagnostics.An_export_declaration_can_only_be_used_at_the_top_level_of_a_namespace_or_module)) {
      return;
    }
    if (!checkGrammarModifiers(node) && hasSyntacticModifiers(node)) {
      grammarErrorOnFirstToken(node, Diagnostics.An_export_declaration_cannot_have_modifiers);
    }
    checkGrammarExportDeclaration(node);
    if (!node.moduleSpecifier || checkExternalImportOrExportDeclaration(node)) {
      if (node.exportClause && !isNamespaceExport(node.exportClause)) {
        forEach(node.exportClause.elements, checkExportSpecifier);
        const inAmbientExternalModule ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ && isAmbientModule(node.parent.parent);
        const inAmbientNamespaceDeclaration ***REMOVED*** !inAmbientExternalModule && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ && !node.moduleSpecifier && node.flags & 33554432 /* Ambient */;
        if (node.parent.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ && !inAmbientExternalModule && !inAmbientNamespaceDeclaration) {
          error(node, Diagnostics.Export_declarations_are_not_permitted_in_a_namespace);
        }
      } else {
        const moduleSymbol ***REMOVED*** resolveExternalModuleName(node, node.moduleSpecifier);
        if (moduleSymbol && hasExportAssignmentSymbol(moduleSymbol)) {
          error(node.moduleSpecifier, Diagnostics.Module_0_uses_export_and_cannot_be_used_with_export_Asterisk, symbolToString(moduleSymbol));
        } else if (node.exportClause) {
          checkAliasSymbol(node.exportClause);
          checkModuleExportName(node.exportClause.name);
        }
        if (host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) < 4 /* System */) {
          if (node.exportClause) {
            if (getESModuleInterop(compilerOptions)) {
              checkExternalEmitHelpers(node, 65536 /* ImportStar */);
            }
          } else {
            checkExternalEmitHelpers(node, 32768 /* ExportStar */);
          }
        }
      }
    }
    checkImportAttributes(node);
  }
  function checkGrammarExportDeclaration(node) {
    var _a;
    if (node.isTypeOnly && ((_a ***REMOVED*** node.exportClause) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 280 /* NamedExports */) {
      return checkGrammarNamedImportsOrExports(node.exportClause);
    }
    return false;
  }
  function checkGrammarModuleElementContext(node, errorMessage) {
    const isInAppropriateContext ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */;
    if (!isInAppropriateContext) {
      grammarErrorOnFirstToken(node, errorMessage);
    }
    return !isInAppropriateContext;
  }
  function checkExportSpecifier(node) {
    checkAliasSymbol(node);
    const hasModuleSpecifier ***REMOVED*** node.parent.parent.moduleSpecifier !***REMOVED******REMOVED*** void 0;
    checkModuleExportName(node.propertyName, hasModuleSpecifier);
    checkModuleExportName(node.name);
    if (getEmitDeclarations(compilerOptions)) {
      collectLinkedAliases(
        node.propertyName || node.name,
        /*setVisibility*/
        true
      );
    }
    if (!hasModuleSpecifier) {
      const exportedName ***REMOVED*** node.propertyName || node.name;
      if (exportedName.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
        return;
      }
      const symbol ***REMOVED*** resolveName(
        exportedName,
        exportedName.escapedText,
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
        /*nameNotFoundMessage*/
        void 0,
        /*isUse*/
        true
      );
      if (symbol && (symbol ***REMOVED******REMOVED******REMOVED*** undefinedSymbol || symbol ***REMOVED******REMOVED******REMOVED*** globalThisSymbol || symbol.declarations && isGlobalSourceFile(getDeclarationContainer(symbol.declarations[0])))) {
        error(exportedName, Diagnostics.Cannot_export_0_Only_local_declarations_can_be_exported_from_a_module, idText(exportedName));
      } else {
        markLinkedReferences(node, 7 /* ExportSpecifier */);
      }
    } else {
      if (getESModuleInterop(compilerOptions) && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) < 4 /* System */ && moduleExportNameIsDefault(node.propertyName || node.name)) {
        checkExternalEmitHelpers(node, 131072 /* ImportDefault */);
      }
    }
  }
  function checkExportAssignment(node) {
    const illegalContextMessage ***REMOVED*** node.isExportEquals ? Diagnostics.An_export_assignment_must_be_at_the_top_level_of_a_file_or_module_declaration : Diagnostics.A_default_export_must_be_at_the_top_level_of_a_file_or_module_declaration;
    if (checkGrammarModuleElementContext(node, illegalContextMessage)) {
      return;
    }
    if (compilerOptions.erasableSyntaxOnly && node.isExportEquals && !(node.flags & 33554432 /* Ambient */)) {
      error(node, Diagnostics.This_syntax_is_not_allowed_when_erasableSyntaxOnly_is_enabled);
    }
    const container ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? node.parent : node.parent.parent;
    if (container.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && !isAmbientModule(container)) {
      if (node.isExportEquals) {
        error(node, Diagnostics.An_export_assignment_cannot_be_used_in_a_namespace);
      } else {
        error(node, Diagnostics.A_default_export_can_only_be_used_in_an_ECMAScript_style_module);
      }
      return;
    }
    if (!checkGrammarModifiers(node) && hasEffectiveModifiers(node)) {
      grammarErrorOnFirstToken(node, Diagnostics.An_export_assignment_cannot_have_modifiers);
    }
    const typeAnnotationNode ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    if (typeAnnotationNode) {
      checkTypeAssignableTo(checkExpressionCached(node.expression), getTypeFromTypeNode(typeAnnotationNode), node.expression);
    }
    const isIllegalExportDefaultInCJS ***REMOVED*** !node.isExportEquals && !(node.flags & 33554432 /* Ambient */) && compilerOptions.verbatimModuleSyntax && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */;
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const id ***REMOVED*** node.expression;
      const sym ***REMOVED*** getExportSymbolOfValueSymbolIfExported(resolveEntityName(
        id,
        -1 /* All */,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        true,
        node
      ));
      if (sym) {
        markLinkedReferences(node, 3 /* ExportAssignment */);
        const typeOnlyDeclaration ***REMOVED*** getTypeOnlyAliasDeclaration(sym, 111551 /* Value */);
        if (getSymbolFlags(sym) & 111551 /* Value */) {
          checkExpressionCached(id);
          if (!isIllegalExportDefaultInCJS && !(node.flags & 33554432 /* Ambient */) && compilerOptions.verbatimModuleSyntax && typeOnlyDeclaration) {
            error(
              id,
              node.isExportEquals ? Diagnostics.An_export_declaration_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolves_to_a_type_only_declaration : Diagnostics.An_export_default_must_reference_a_real_value_when_verbatimModuleSyntax_is_enabled_but_0_resolves_to_a_type_only_declaration,
              idText(id)
            );
          }
        } else if (!isIllegalExportDefaultInCJS && !(node.flags & 33554432 /* Ambient */) && compilerOptions.verbatimModuleSyntax) {
          error(
            id,
            node.isExportEquals ? Diagnostics.An_export_declaration_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers_to_a_type : Diagnostics.An_export_default_must_reference_a_value_when_verbatimModuleSyntax_is_enabled_but_0_only_refers_to_a_type,
            idText(id)
          );
        }
        if (!isIllegalExportDefaultInCJS && !(node.flags & 33554432 /* Ambient */) && getIsolatedModules(compilerOptions) && !(sym.flags & 111551 /* Value */)) {
          const nonLocalMeanings ***REMOVED*** getSymbolFlags(
            sym,
            /*excludeTypeOnlyMeanings*/
            false,
            /*excludeLocalMeanings*/
            true
          );
          if (sym.flags & 2097152 /* Alias */ && nonLocalMeanings & 788968 /* Type */ && !(nonLocalMeanings & 111551 /* Value */) && (!typeOnlyDeclaration || getSourceFileOfNode(typeOnlyDeclaration) !***REMOVED******REMOVED*** getSourceFileOfNode(node))) {
            error(
              id,
              node.isExportEquals ? Diagnostics._0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_import_type_where_0_is_imported : Diagnostics._0_resolves_to_a_type_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_export_type_0_as_default,
              idText(id),
              isolatedModulesLikeFlagName
            );
          } else if (typeOnlyDeclaration && getSourceFileOfNode(typeOnlyDeclaration) !***REMOVED******REMOVED*** getSourceFileOfNode(node)) {
            addTypeOnlyDeclarationRelatedInfo(
              error(
                id,
                node.isExportEquals ? Diagnostics._0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_import_type_where_0_is_imported : Diagnostics._0_resolves_to_a_type_only_declaration_and_must_be_marked_type_only_in_this_file_before_re_exporting_when_1_is_enabled_Consider_using_export_type_0_as_default,
                idText(id),
                isolatedModulesLikeFlagName
              ),
              typeOnlyDeclaration,
              idText(id)
            );
          }
        }
      } else {
        checkExpressionCached(id);
      }
      if (getEmitDeclarations(compilerOptions)) {
        collectLinkedAliases(
          id,
          /*setVisibility*/
          true
        );
      }
    } else {
      checkExpressionCached(node.expression);
    }
    if (isIllegalExportDefaultInCJS) {
      error(node, getVerbatimModuleSyntaxErrorMessage(node));
    }
    checkExternalModuleExports(container);
    if (node.flags & 33554432 /* Ambient */ && !isEntityNameExpression(node.expression)) {
      grammarErrorOnNode(node.expression, Diagnostics.The_expression_of_an_export_assignment_must_be_an_identifier_or_qualified_name_in_an_ambient_context);
    }
    if (node.isExportEquals) {
      if (moduleKind >***REMOVED*** 5 /* ES2015 */ && moduleKind !***REMOVED******REMOVED*** 200 /* Preserve */ && (node.flags & 33554432 /* Ambient */ && host.getImpliedNodeFormatForEmit(getSourceFileOfNode(node)) ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ || !(node.flags & 33554432 /* Ambient */) && host.getImpliedNodeFormatForEmit(getSourceFileOfNode(node)) !***REMOVED******REMOVED*** 1 /* CommonJS */)) {
        grammarErrorOnNode(node, Diagnostics.Export_assignment_cannot_be_used_when_targeting_ECMAScript_modules_Consider_using_export_default_or_another_module_format_instead);
      } else if (moduleKind ***REMOVED******REMOVED******REMOVED*** 4 /* System */ && !(node.flags & 33554432 /* Ambient */)) {
        grammarErrorOnNode(node, Diagnostics.Export_assignment_is_not_supported_when_module_flag_is_system);
      }
    }
  }
  function hasExportedMembers(moduleSymbol) {
    return forEachEntry(moduleSymbol.exports, (_, id) ***REMOVED***> id !***REMOVED******REMOVED*** "export***REMOVED***");
  }
  function checkExternalModuleExports(node) {
    const moduleSymbol ***REMOVED*** getSymbolOfDeclaration(node);
    const links ***REMOVED*** getSymbolLinks(moduleSymbol);
    if (!links.exportsChecked) {
      const exportEqualsSymbol ***REMOVED*** moduleSymbol.exports.get("export***REMOVED***");
      if (exportEqualsSymbol && hasExportedMembers(moduleSymbol)) {
        const declaration ***REMOVED*** getDeclarationOfAliasSymbol(exportEqualsSymbol) || exportEqualsSymbol.valueDeclaration;
        if (declaration && !isTopLevelInExternalModuleAugmentation(declaration) && !isInJSFile(declaration)) {
          error(declaration, Diagnostics.An_export_assignment_cannot_be_used_in_a_module_with_other_exported_elements);
        }
      }
      const exports2 ***REMOVED*** getExportsOfModule(moduleSymbol);
      if (exports2) {
        exports2.forEach(({ declarations, flags }, id) ***REMOVED***> {
          if (id ***REMOVED******REMOVED******REMOVED*** "__export") {
            return;
          }
          if (flags & (1920 /* Namespace */ | 384 /* Enum */)) {
            return;
          }
          const exportedDeclarationsCount ***REMOVED*** countWhere(declarations, and(isNotOverloadAndNotAccessor, not(isInterfaceDeclaration)));
          if (flags & 524288 /* TypeAlias */ && exportedDeclarationsCount <***REMOVED*** 2) {
            return;
          }
          if (exportedDeclarationsCount > 1) {
            if (!isDuplicatedCommonJSExport(declarations)) {
              for (const declaration of declarations) {
                if (isNotOverload(declaration)) {
                  diagnostics.add(createDiagnosticForNode(declaration, Diagnostics.Cannot_redeclare_exported_variable_0, unescapeLeadingUnderscores(id)));
                }
              }
            }
          }
        });
      }
      links.exportsChecked ***REMOVED*** true;
    }
  }
  function isDuplicatedCommonJSExport(declarations) {
    return declarations && declarations.length > 1 && declarations.every((d) ***REMOVED***> isInJSFile(d) && isAccessExpression(d) && (isExportsIdentifier(d.expression) || isModuleExportsAccessExpression(d.expression)));
  }
  function checkSourceElement(node) {
    if (node) {
      const saveCurrentNode ***REMOVED*** currentNode;
      currentNode ***REMOVED*** node;
      instantiationCount ***REMOVED*** 0;
      checkSourceElementWorker(node);
      currentNode ***REMOVED*** saveCurrentNode;
    }
  }
  function checkSourceElementWorker(node) {
    if (getNodeCheckFlags(node) & 8388608 /* PartiallyTypeChecked */) {
      return;
    }
    if (canHaveJSDoc(node)) {
      forEach(node.jsDoc, ({ comment, tags }) ***REMOVED***> {
        checkJSDocCommentWorker(comment);
        forEach(tags, (tag) ***REMOVED***> {
          checkJSDocCommentWorker(tag.comment);
          if (isInJSFile(node)) {
            checkSourceElement(tag);
          }
        });
      });
    }
    const kind ***REMOVED*** node.kind;
    if (cancellationToken) {
      switch (kind) {
        case 268 /* ModuleDeclaration */:
        case 264 /* ClassDeclaration */:
        case 265 /* InterfaceDeclaration */:
        case 263 /* FunctionDeclaration */:
          cancellationToken.throwIfCancellationRequested();
      }
    }
    if (kind >***REMOVED*** 244 /* FirstStatement */ && kind <***REMOVED*** 260 /* LastStatement */ && canHaveFlowNode(node) && node.flowNode && !isReachableFlowNode(node.flowNode)) {
      errorOrSuggestion(compilerOptions.allowUnreachableCode ***REMOVED******REMOVED******REMOVED*** false, node, Diagnostics.Unreachable_code_detected);
    }
    switch (kind) {
      case 169 /* TypeParameter */:
        return checkTypeParameter(node);
      case 170 /* Parameter */:
        return checkParameter(node);
      case 173 /* PropertyDeclaration */:
        return checkPropertyDeclaration(node);
      case 172 /* PropertySignature */:
        return checkPropertySignature(node);
      case 186 /* ConstructorType */:
      case 185 /* FunctionType */:
      case 180 /* CallSignature */:
      case 181 /* ConstructSignature */:
      case 182 /* IndexSignature */:
        return checkSignatureDeclaration(node);
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        return checkMethodDeclaration(node);
      case 176 /* ClassStaticBlockDeclaration */:
        return checkClassStaticBlockDeclaration(node);
      case 177 /* Constructor */:
        return checkConstructorDeclaration(node);
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return checkAccessorDeclaration(node);
      case 184 /* TypeReference */:
        return checkTypeReferenceNode(node);
      case 183 /* TypePredicate */:
        return checkTypePredicate(node);
      case 187 /* TypeQuery */:
        return checkTypeQuery(node);
      case 188 /* TypeLiteral */:
        return checkTypeLiteral(node);
      case 189 /* ArrayType */:
        return checkArrayType(node);
      case 190 /* TupleType */:
        return checkTupleType(node);
      case 193 /* UnionType */:
      case 194 /* IntersectionType */:
        return checkUnionOrIntersectionType(node);
      case 197 /* ParenthesizedType */:
      case 191 /* OptionalType */:
      case 192 /* RestType */:
        return checkSourceElement(node.type);
      case 198 /* ThisType */:
        return checkThisType(node);
      case 199 /* TypeOperator */:
        return checkTypeOperator(node);
      case 195 /* ConditionalType */:
        return checkConditionalType(node);
      case 196 /* InferType */:
        return checkInferType(node);
      case 204 /* TemplateLiteralType */:
        return checkTemplateLiteralType(node);
      case 206 /* ImportType */:
        return checkImportType(node);
      case 203 /* NamedTupleMember */:
        return checkNamedTupleMember(node);
      case 329 /* JSDocAugmentsTag */:
        return checkJSDocAugmentsTag(node);
      case 330 /* JSDocImplementsTag */:
        return checkJSDocImplementsTag(node);
      case 347 /* JSDocTypedefTag */:
      case 339 /* JSDocCallbackTag */:
      case 341 /* JSDocEnumTag */:
        return checkJSDocTypeAliasTag(node);
      case 346 /* JSDocTemplateTag */:
        return checkJSDocTemplateTag(node);
      case 345 /* JSDocTypeTag */:
        return checkJSDocTypeTag(node);
      case 325 /* JSDocLink */:
      case 326 /* JSDocLinkCode */:
      case 327 /* JSDocLinkPlain */:
        return checkJSDocLinkLikeTag(node);
      case 342 /* JSDocParameterTag */:
        return checkJSDocParameterTag(node);
      case 349 /* JSDocPropertyTag */:
        return checkJSDocPropertyTag(node);
      case 318 /* JSDocFunctionType */:
        checkJSDocFunctionType(node);
      // falls through
      case 316 /* JSDocNonNullableType */:
      case 315 /* JSDocNullableType */:
      case 313 /* JSDocAllType */:
      case 314 /* JSDocUnknownType */:
      case 323 /* JSDocTypeLiteral */:
        checkJSDocTypeIsInJsFile(node);
        forEachChild(node, checkSourceElement);
        return;
      case 319 /* JSDocVariadicType */:
        checkJSDocVariadicType(node);
        return;
      case 310 /* JSDocTypeExpression */:
        return checkSourceElement(node.type);
      case 334 /* JSDocPublicTag */:
      case 336 /* JSDocProtectedTag */:
      case 335 /* JSDocPrivateTag */:
        return checkJSDocAccessibilityModifiers(node);
      case 351 /* JSDocSatisfiesTag */:
        return checkJSDocSatisfiesTag(node);
      case 344 /* JSDocThisTag */:
        return checkJSDocThisTag(node);
      case 352 /* JSDocImportTag */:
        return checkJSDocImportTag(node);
      case 200 /* IndexedAccessType */:
        return checkIndexedAccessType(node);
      case 201 /* MappedType */:
        return checkMappedType(node);
      case 263 /* FunctionDeclaration */:
        return checkFunctionDeclaration(node);
      case 242 /* Block */:
      case 269 /* ModuleBlock */:
        return checkBlock(node);
      case 244 /* VariableStatement */:
        return checkVariableStatement(node);
      case 245 /* ExpressionStatement */:
        return checkExpressionStatement(node);
      case 246 /* IfStatement */:
        return checkIfStatement(node);
      case 247 /* DoStatement */:
        return checkDoStatement(node);
      case 248 /* WhileStatement */:
        return checkWhileStatement(node);
      case 249 /* ForStatement */:
        return checkForStatement(node);
      case 250 /* ForInStatement */:
        return checkForInStatement(node);
      case 251 /* ForOfStatement */:
        return checkForOfStatement(node);
      case 252 /* ContinueStatement */:
      case 253 /* BreakStatement */:
        return checkBreakOrContinueStatement(node);
      case 254 /* ReturnStatement */:
        return checkReturnStatement(node);
      case 255 /* WithStatement */:
        return checkWithStatement(node);
      case 256 /* SwitchStatement */:
        return checkSwitchStatement(node);
      case 257 /* LabeledStatement */:
        return checkLabeledStatement(node);
      case 258 /* ThrowStatement */:
        return checkThrowStatement(node);
      case 259 /* TryStatement */:
        return checkTryStatement(node);
      case 261 /* VariableDeclaration */:
        return checkVariableDeclaration(node);
      case 209 /* BindingElement */:
        return checkBindingElement(node);
      case 264 /* ClassDeclaration */:
        return checkClassDeclaration(node);
      case 265 /* InterfaceDeclaration */:
        return checkInterfaceDeclaration(node);
      case 266 /* TypeAliasDeclaration */:
        return checkTypeAliasDeclaration(node);
      case 267 /* EnumDeclaration */:
        return checkEnumDeclaration(node);
      case 307 /* EnumMember */:
        return checkEnumMember(node);
      case 268 /* ModuleDeclaration */:
        return checkModuleDeclaration(node);
      case 273 /* ImportDeclaration */:
        return checkImportDeclaration(node);
      case 272 /* ImportEqualsDeclaration */:
        return checkImportEqualsDeclaration(node);
      case 279 /* ExportDeclaration */:
        return checkExportDeclaration(node);
      case 278 /* ExportAssignment */:
        return checkExportAssignment(node);
      case 243 /* EmptyStatement */:
      case 260 /* DebuggerStatement */:
        checkGrammarStatementInAmbientContext(node);
        return;
      case 283 /* MissingDeclaration */:
        return checkMissingDeclaration(node);
    }
  }
  function checkJSDocCommentWorker(node) {
    if (isArray(node)) {
      forEach(node, (tag) ***REMOVED***> {
        if (isJSDocLinkLike(tag)) {
          checkSourceElement(tag);
        }
      });
    }
  }
  function checkJSDocTypeIsInJsFile(node) {
    if (!isInJSFile(node)) {
      if (isJSDocNonNullableType(node) || isJSDocNullableType(node)) {
        const token ***REMOVED*** tokenToString(isJSDocNonNullableType(node) ? 54 /* ExclamationToken */ : 58 /* QuestionToken */);
        const diagnostic ***REMOVED*** node.postfix ? Diagnostics._0_at_the_end_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1 : Diagnostics._0_at_the_start_of_a_type_is_not_valid_TypeScript_syntax_Did_you_mean_to_write_1;
        const typeNode ***REMOVED*** node.type;
        const type ***REMOVED*** getTypeFromTypeNode(typeNode);
        grammarErrorOnNode(
          node,
          diagnostic,
          token,
          typeToString(
            isJSDocNullableType(node) && !(type ***REMOVED******REMOVED******REMOVED*** neverType || type ***REMOVED******REMOVED******REMOVED*** voidType) ? getUnionType(append([type, undefinedType], node.postfix ? void 0 : nullType)) : type
          )
        );
      } else {
        grammarErrorOnNode(node, Diagnostics.JSDoc_types_can_only_be_used_inside_documentation_comments);
      }
    }
  }
  function checkJSDocVariadicType(node) {
    checkJSDocTypeIsInJsFile(node);
    checkSourceElement(node.type);
    const { parent } ***REMOVED*** node;
    if (isParameter(parent) && isJSDocFunctionType(parent.parent)) {
      if (last(parent.parent.parameters) !***REMOVED******REMOVED*** parent) {
        error(node, Diagnostics.A_rest_parameter_must_be_last_in_a_parameter_list);
      }
      return;
    }
    if (!isJSDocTypeExpression(parent)) {
      error(node, Diagnostics.JSDoc_may_only_appear_in_the_last_parameter_of_a_signature);
    }
    const paramTag ***REMOVED*** node.parent.parent;
    if (!isJSDocParameterTag(paramTag)) {
      error(node, Diagnostics.JSDoc_may_only_appear_in_the_last_parameter_of_a_signature);
      return;
    }
    const param ***REMOVED*** getParameterSymbolFromJSDoc(paramTag);
    if (!param) {
      return;
    }
    const host2 ***REMOVED*** getHostSignatureFromJSDoc(paramTag);
    if (!host2 || last(host2.parameters).symbol !***REMOVED******REMOVED*** param) {
      error(node, Diagnostics.A_rest_parameter_must_be_last_in_a_parameter_list);
    }
  }
  function getTypeFromJSDocVariadicType(node) {
    const type ***REMOVED*** getTypeFromTypeNode(node.type);
    const { parent } ***REMOVED*** node;
    const paramTag ***REMOVED*** node.parent.parent;
    if (isJSDocTypeExpression(node.parent) && isJSDocParameterTag(paramTag)) {
      const host2 ***REMOVED*** getHostSignatureFromJSDoc(paramTag);
      const isCallbackTag ***REMOVED*** isJSDocCallbackTag(paramTag.parent.parent);
      if (host2 || isCallbackTag) {
        const lastParamDeclaration ***REMOVED*** isCallbackTag ? lastOrUndefined(paramTag.parent.parent.typeExpression.parameters) : lastOrUndefined(host2.parameters);
        const symbol ***REMOVED*** getParameterSymbolFromJSDoc(paramTag);
        if (!lastParamDeclaration || symbol && lastParamDeclaration.symbol ***REMOVED******REMOVED******REMOVED*** symbol && isRestParameter(lastParamDeclaration)) {
          return createArrayType(type);
        }
      }
    }
    if (isParameter(parent) && isJSDocFunctionType(parent.parent)) {
      return createArrayType(type);
    }
    return addOptionality(type);
  }
  function checkNodeDeferred(node) {
    const enclosingFile ***REMOVED*** getSourceFileOfNode(node);
    const links ***REMOVED*** getNodeLinks(enclosingFile);
    if (!(links.flags & 1 /* TypeChecked */)) {
      links.deferredNodes || (links.deferredNodes ***REMOVED*** /* @__PURE__ */ new Set());
      links.deferredNodes.add(node);
    } else {
      Debug.assert(!links.deferredNodes, "A type-checked file should have no deferred nodes.");
    }
  }
  function checkDeferredNodes(context) {
    const links ***REMOVED*** getNodeLinks(context);
    if (links.deferredNodes) {
      links.deferredNodes.forEach(checkDeferredNode);
    }
    links.deferredNodes ***REMOVED*** void 0;
  }
  function checkDeferredNode(node) {
    var _a, _b;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Check, "checkDeferredNode", { kind: node.kind, pos: node.pos, end: node.end, path: node.tracingPath });
    const saveCurrentNode ***REMOVED*** currentNode;
    currentNode ***REMOVED*** node;
    instantiationCount ***REMOVED*** 0;
    switch (node.kind) {
      case 214 /* CallExpression */:
      case 215 /* NewExpression */:
      case 216 /* TaggedTemplateExpression */:
      case 171 /* Decorator */:
      case 287 /* JsxOpeningElement */:
        resolveUntypedCall(node);
        break;
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        checkFunctionExpressionOrObjectLiteralMethodDeferred(node);
        break;
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        checkAccessorDeclaration(node);
        break;
      case 232 /* ClassExpression */:
        checkClassExpressionDeferred(node);
        break;
      case 169 /* TypeParameter */:
        checkTypeParameterDeferred(node);
        break;
      case 286 /* JsxSelfClosingElement */:
        checkJsxSelfClosingElementDeferred(node);
        break;
      case 285 /* JsxElement */:
        checkJsxElementDeferred(node);
        break;
      case 217 /* TypeAssertionExpression */:
      case 235 /* AsExpression */:
      case 218 /* ParenthesizedExpression */:
        checkAssertionDeferred(node);
        break;
      case 223 /* VoidExpression */:
        checkExpression(node.expression);
        break;
      case 227 /* BinaryExpression */:
        if (isInstanceOfExpression(node)) {
          resolveUntypedCall(node);
        }
        break;
    }
    currentNode ***REMOVED*** saveCurrentNode;
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  }
  function checkSourceFile(node, nodesToCheck) {
    var _a, _b;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(
      tracing.Phase.Check,
      nodesToCheck ? "checkSourceFileNodes" : "checkSourceFile",
      { path: node.path },
      /*separateBeginAndEnd*/
      true
    );
    const beforeMark ***REMOVED*** nodesToCheck ? "beforeCheckNodes" : "beforeCheck";
    const afterMark ***REMOVED*** nodesToCheck ? "afterCheckNodes" : "afterCheck";
    mark(beforeMark);
    nodesToCheck ? checkSourceFileNodesWorker(node, nodesToCheck) : checkSourceFileWorker(node);
    mark(afterMark);
    measure("Check", beforeMark, afterMark);
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  }
  function unusedIsError(kind, isAmbient) {
    if (isAmbient) {
      return false;
    }
    switch (kind) {
      case 0 /* Local */:
        return !!compilerOptions.noUnusedLocals;
      case 1 /* Parameter */:
        return !!compilerOptions.noUnusedParameters;
      default:
        return Debug.assertNever(kind);
    }
  }
  function getPotentiallyUnusedIdentifiers(sourceFile) {
    return allPotentiallyUnusedIdentifiers.get(sourceFile.path) || emptyArray;
  }
  function checkSourceFileWorker(node) {
    const links ***REMOVED*** getNodeLinks(node);
    if (!(links.flags & 1 /* TypeChecked */)) {
      if (skipTypeChecking(node, compilerOptions, host)) {
        return;
      }
      checkGrammarSourceFile(node);
      clear(potentialThisCollisions);
      clear(potentialNewTargetCollisions);
      clear(potentialWeakMapSetCollisions);
      clear(potentialReflectCollisions);
      clear(potentialUnusedRenamedBindingElementsInTypes);
      if (links.flags & 8388608 /* PartiallyTypeChecked */) {
        potentialThisCollisions ***REMOVED*** links.potentialThisCollisions;
        potentialNewTargetCollisions ***REMOVED*** links.potentialNewTargetCollisions;
        potentialWeakMapSetCollisions ***REMOVED*** links.potentialWeakMapSetCollisions;
        potentialReflectCollisions ***REMOVED*** links.potentialReflectCollisions;
        potentialUnusedRenamedBindingElementsInTypes ***REMOVED*** links.potentialUnusedRenamedBindingElementsInTypes;
      }
      forEach(node.statements, checkSourceElement);
      checkSourceElement(node.endOfFileToken);
      checkDeferredNodes(node);
      if (isExternalOrCommonJsModule(node)) {
        registerForUnusedIdentifiersCheck(node);
      }
      addLazyDiagnostic(() ***REMOVED***> {
        if (!node.isDeclarationFile && (compilerOptions.noUnusedLocals || compilerOptions.noUnusedParameters)) {
          checkUnusedIdentifiers(getPotentiallyUnusedIdentifiers(node), (containingNode, kind, diag2) ***REMOVED***> {
            if (!containsParseError(containingNode) && unusedIsError(kind, !!(containingNode.flags & 33554432 /* Ambient */))) {
              diagnostics.add(diag2);
            }
          });
        }
        if (!node.isDeclarationFile) {
          checkPotentialUncheckedRenamedBindingElementsInTypes();
        }
      });
      if (isExternalOrCommonJsModule(node)) {
        checkExternalModuleExports(node);
      }
      if (potentialThisCollisions.length) {
        forEach(potentialThisCollisions, checkIfThisIsCapturedInEnclosingScope);
        clear(potentialThisCollisions);
      }
      if (potentialNewTargetCollisions.length) {
        forEach(potentialNewTargetCollisions, checkIfNewTargetIsCapturedInEnclosingScope);
        clear(potentialNewTargetCollisions);
      }
      if (potentialWeakMapSetCollisions.length) {
        forEach(potentialWeakMapSetCollisions, checkWeakMapSetCollision);
        clear(potentialWeakMapSetCollisions);
      }
      if (potentialReflectCollisions.length) {
        forEach(potentialReflectCollisions, checkReflectCollision);
        clear(potentialReflectCollisions);
      }
      links.flags |***REMOVED*** 1 /* TypeChecked */;
    }
  }
  function checkSourceFileNodesWorker(file, nodes) {
    const links ***REMOVED*** getNodeLinks(file);
    if (!(links.flags & 1 /* TypeChecked */)) {
      if (skipTypeChecking(file, compilerOptions, host)) {
        return;
      }
      checkGrammarSourceFile(file);
      clear(potentialThisCollisions);
      clear(potentialNewTargetCollisions);
      clear(potentialWeakMapSetCollisions);
      clear(potentialReflectCollisions);
      clear(potentialUnusedRenamedBindingElementsInTypes);
      forEach(nodes, checkSourceElement);
      checkDeferredNodes(file);
      (links.potentialThisCollisions || (links.potentialThisCollisions ***REMOVED*** [])).push(...potentialThisCollisions);
      (links.potentialNewTargetCollisions || (links.potentialNewTargetCollisions ***REMOVED*** [])).push(...potentialNewTargetCollisions);
      (links.potentialWeakMapSetCollisions || (links.potentialWeakMapSetCollisions ***REMOVED*** [])).push(...potentialWeakMapSetCollisions);
      (links.potentialReflectCollisions || (links.potentialReflectCollisions ***REMOVED*** [])).push(...potentialReflectCollisions);
      (links.potentialUnusedRenamedBindingElementsInTypes || (links.potentialUnusedRenamedBindingElementsInTypes ***REMOVED*** [])).push(
        ...potentialUnusedRenamedBindingElementsInTypes
      );
      links.flags |***REMOVED*** 8388608 /* PartiallyTypeChecked */;
      for (const node of nodes) {
        const nodeLinks2 ***REMOVED*** getNodeLinks(node);
        nodeLinks2.flags |***REMOVED*** 8388608 /* PartiallyTypeChecked */;
      }
    }
  }
  function getDiagnostics(sourceFile, ct, nodesToCheck) {
    try {
      cancellationToken ***REMOVED*** ct;
      return getDiagnosticsWorker(sourceFile, nodesToCheck);
    } finally {
      cancellationToken ***REMOVED*** void 0;
    }
  }
  function ensurePendingDiagnosticWorkComplete() {
    for (const cb of deferredDiagnosticsCallbacks) {
      cb();
    }
    deferredDiagnosticsCallbacks ***REMOVED*** [];
  }
  function checkSourceFileWithEagerDiagnostics(sourceFile, nodesToCheck) {
    ensurePendingDiagnosticWorkComplete();
    const oldAddLazyDiagnostics ***REMOVED*** addLazyDiagnostic;
    addLazyDiagnostic ***REMOVED*** (cb) ***REMOVED***> cb();
    checkSourceFile(sourceFile, nodesToCheck);
    addLazyDiagnostic ***REMOVED*** oldAddLazyDiagnostics;
  }
  function getDiagnosticsWorker(sourceFile, nodesToCheck) {
    if (sourceFile) {
      ensurePendingDiagnosticWorkComplete();
      const previousGlobalDiagnostics ***REMOVED*** diagnostics.getGlobalDiagnostics();
      const previousGlobalDiagnosticsSize ***REMOVED*** previousGlobalDiagnostics.length;
      checkSourceFileWithEagerDiagnostics(sourceFile, nodesToCheck);
      const semanticDiagnostics ***REMOVED*** diagnostics.getDiagnostics(sourceFile.fileName);
      if (nodesToCheck) {
        return semanticDiagnostics;
      }
      const currentGlobalDiagnostics ***REMOVED*** diagnostics.getGlobalDiagnostics();
      if (currentGlobalDiagnostics !***REMOVED******REMOVED*** previousGlobalDiagnostics) {
        const deferredGlobalDiagnostics ***REMOVED*** relativeComplement(previousGlobalDiagnostics, currentGlobalDiagnostics, compareDiagnostics);
        return concatenate(deferredGlobalDiagnostics, semanticDiagnostics);
      } else if (previousGlobalDiagnosticsSize ***REMOVED******REMOVED******REMOVED*** 0 && currentGlobalDiagnostics.length > 0) {
        return concatenate(currentGlobalDiagnostics, semanticDiagnostics);
      }
      return semanticDiagnostics;
    }
    forEach(host.getSourceFiles(), (file) ***REMOVED***> checkSourceFileWithEagerDiagnostics(file));
    return diagnostics.getDiagnostics();
  }
  function getGlobalDiagnostics() {
    ensurePendingDiagnosticWorkComplete();
    return diagnostics.getGlobalDiagnostics();
  }
  function getSymbolsInScope(location, meaning) {
    if (location.flags & 67108864 /* InWithStatement */) {
      return [];
    }
    const symbols ***REMOVED*** createSymbolTable();
    let isStaticSymbol ***REMOVED*** false;
    populateSymbols();
    symbols.delete("this" /* This */);
    return symbolsToArray(symbols);
    function populateSymbols() {
      while (location) {
        if (canHaveLocals(location) && location.locals && !isGlobalSourceFile(location)) {
          copySymbols(location.locals, meaning);
        }
        switch (location.kind) {
          case 308 /* SourceFile */:
            if (!isExternalModule(location)) break;
          // falls through
          case 268 /* ModuleDeclaration */:
            copyLocallyVisibleExportSymbols(getSymbolOfDeclaration(location).exports, meaning & 2623475 /* ModuleMember */);
            break;
          case 267 /* EnumDeclaration */:
            copySymbols(getSymbolOfDeclaration(location).exports, meaning & 8 /* EnumMember */);
            break;
          case 232 /* ClassExpression */:
            const className ***REMOVED*** location.name;
            if (className) {
              copySymbol(location.symbol, meaning);
            }
          // this fall-through is necessary because we would like to handle
          // type parameter inside class expression similar to how we handle it in classDeclaration and interface Declaration.
          // falls through
          case 264 /* ClassDeclaration */:
          case 265 /* InterfaceDeclaration */:
            if (!isStaticSymbol) {
              copySymbols(getMembersOfSymbol(getSymbolOfDeclaration(location)), meaning & 788968 /* Type */);
            }
            break;
          case 219 /* FunctionExpression */:
            const funcName ***REMOVED*** location.name;
            if (funcName) {
              copySymbol(location.symbol, meaning);
            }
            break;
        }
        if (introducesArgumentsExoticObject(location)) {
          copySymbol(argumentsSymbol, meaning);
        }
        isStaticSymbol ***REMOVED*** isStatic(location);
        location ***REMOVED*** location.parent;
      }
      copySymbols(globals, meaning);
    }
    function copySymbol(symbol, meaning2) {
      if (getCombinedLocalAndExportSymbolFlags(symbol) & meaning2) {
        const id ***REMOVED*** symbol.escapedName;
        if (!symbols.has(id)) {
          symbols.set(id, symbol);
        }
      }
    }
    function copySymbols(source, meaning2) {
      if (meaning2) {
        source.forEach((symbol) ***REMOVED***> {
          copySymbol(symbol, meaning2);
        });
      }
    }
    function copyLocallyVisibleExportSymbols(source, meaning2) {
      if (meaning2) {
        source.forEach((symbol) ***REMOVED***> {
          if (!getDeclarationOfKind(symbol, 282 /* ExportSpecifier */) && !getDeclarationOfKind(symbol, 281 /* NamespaceExport */) && symbol.escapedName !***REMOVED******REMOVED*** "default" /* Default */) {
            copySymbol(symbol, meaning2);
          }
        });
      }
    }
  }
  function isTypeDeclarationName(name) {
    return name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && isTypeDeclaration(name.parent) && getNameOfDeclaration(name.parent) ***REMOVED******REMOVED******REMOVED*** name;
  }
  function isTypeReferenceIdentifier(node) {
    while (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
      node ***REMOVED*** node.parent;
    }
    return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */;
  }
  function isInNameOfExpressionWithTypeArguments(node) {
    while (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
      node ***REMOVED*** node.parent;
    }
    return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */;
  }
  function forEachEnclosingClass(node, callback) {
    let result;
    let containingClass ***REMOVED*** getContainingClass(node);
    while (containingClass) {
      if (result ***REMOVED*** callback(containingClass)) break;
      containingClass ***REMOVED*** getContainingClass(containingClass);
    }
    return result;
  }
  function isNodeUsedDuringClassInitialization(node) {
    return !!findAncestor(node, (element) ***REMOVED***> {
      if (isConstructorDeclaration(element) && nodeIsPresent(element.body) || isPropertyDeclaration(element)) {
        return true;
      } else if (isClassLike(element) || isFunctionLikeDeclaration(element)) {
        return "quit";
      }
      return false;
    });
  }
  function isNodeWithinClass(node, classDeclaration) {
    return !!forEachEnclosingClass(node, (n) ***REMOVED***> n ***REMOVED******REMOVED******REMOVED*** classDeclaration);
  }
  function getLeftSideOfImportEqualsOrExportAssignment(nodeOnRightSide) {
    while (nodeOnRightSide.parent.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
      nodeOnRightSide ***REMOVED*** nodeOnRightSide.parent;
    }
    if (nodeOnRightSide.parent.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */) {
      return nodeOnRightSide.parent.moduleReference ***REMOVED******REMOVED******REMOVED*** nodeOnRightSide ? nodeOnRightSide.parent : void 0;
    }
    if (nodeOnRightSide.parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */) {
      return nodeOnRightSide.parent.expression ***REMOVED******REMOVED******REMOVED*** nodeOnRightSide ? nodeOnRightSide.parent : void 0;
    }
    return void 0;
  }
  function isInRightSideOfImportOrExportAssignment(node) {
    return getLeftSideOfImportEqualsOrExportAssignment(node) !***REMOVED******REMOVED*** void 0;
  }
  function getSpecialPropertyAssignmentSymbolFromEntityName(entityName) {
    const specialPropertyAssignmentKind ***REMOVED*** getAssignmentDeclarationKind(entityName.parent.parent);
    switch (specialPropertyAssignmentKind) {
      case 1 /* ExportsProperty */:
      case 3 /* PrototypeProperty */:
        return getSymbolOfNode(entityName.parent);
      case 5 /* Property */:
        if (isPropertyAccessExpression(entityName.parent) && getLeftmostAccessExpression(entityName.parent) ***REMOVED******REMOVED******REMOVED*** entityName) {
          return void 0;
        }
      // falls through
      case 4 /* ThisProperty */:
      case 2 /* ModuleExports */:
        return getSymbolOfDeclaration(entityName.parent.parent);
    }
  }
  function isImportTypeQualifierPart(node) {
    let parent ***REMOVED*** node.parent;
    while (isQualifiedName(parent)) {
      node ***REMOVED*** parent;
      parent ***REMOVED*** parent.parent;
    }
    if (parent && parent.kind ***REMOVED******REMOVED******REMOVED*** 206 /* ImportType */ && parent.qualifier ***REMOVED******REMOVED******REMOVED*** node) {
      return parent;
    }
    return void 0;
  }
  function isThisPropertyAndThisTyped(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
      const container ***REMOVED*** getThisContainer(
        node,
        /*includeArrowFunctions*/
        false,
        /*includeClassComputedPropertyName*/
        false
      );
      if (isFunctionLike(container)) {
        const containingLiteral ***REMOVED*** getContainingObjectLiteral(container);
        if (containingLiteral) {
          const contextualType ***REMOVED*** getApparentTypeOfContextualType(
            containingLiteral,
            /*contextFlags*/
            void 0
          );
          const type ***REMOVED*** getThisTypeOfObjectLiteralFromContextualType(containingLiteral, contextualType);
          return type && !isTypeAny(type);
        }
      }
    }
  }
  function getSymbolOfNameOrPropertyAccessExpression(name) {
    if (isDeclarationName(name)) {
      return getSymbolOfNode(name.parent);
    }
    if (isInJSFile(name) && name.parent.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ && name.parent ***REMOVED******REMOVED******REMOVED*** name.parent.parent.left) {
      if (!isPrivateIdentifier(name) && !isJSDocMemberName(name) && !isThisPropertyAndThisTyped(name.parent)) {
        const specialPropertyAssignmentSymbol ***REMOVED*** getSpecialPropertyAssignmentSymbolFromEntityName(name);
        if (specialPropertyAssignmentSymbol) {
          return specialPropertyAssignmentSymbol;
        }
      }
    }
    if (name.parent.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ && isEntityNameExpression(name)) {
      const success ***REMOVED*** resolveEntityName(
        name,
        /*all meanings*/
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
        /*ignoreErrors*/
        true
      );
      if (success && success !***REMOVED******REMOVED*** unknownSymbol) {
        return success;
      }
    } else if (isEntityName(name) && isInRightSideOfImportOrExportAssignment(name)) {
      const importEqualsDeclaration ***REMOVED*** getAncestor(name, 272 /* ImportEqualsDeclaration */);
      Debug.assert(importEqualsDeclaration !***REMOVED******REMOVED*** void 0);
      return getSymbolOfPartOfRightHandSideOfImportEquals(
        name,
        /*dontResolveAlias*/
        true
      );
    }
    if (isEntityName(name)) {
      const possibleImportNode ***REMOVED*** isImportTypeQualifierPart(name);
      if (possibleImportNode) {
        getTypeFromTypeNode(possibleImportNode);
        const sym ***REMOVED*** getNodeLinks(name).resolvedSymbol;
        return sym ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : sym;
      }
    }
    while (isRightSideOfQualifiedNameOrPropertyAccessOrJSDocMemberName(name)) {
      name ***REMOVED*** name.parent;
    }
    if (isInNameOfExpressionWithTypeArguments(name)) {
      let meaning ***REMOVED*** 0 /* None */;
      if (name.parent.kind ***REMOVED******REMOVED******REMOVED*** 234 /* ExpressionWithTypeArguments */) {
        meaning ***REMOVED*** isPartOfTypeNode(name) ? 788968 /* Type */ : 111551 /* Value */;
        if (isExpressionWithTypeArgumentsInClassExtendsClause(name.parent)) {
          meaning |***REMOVED*** 111551 /* Value */;
        }
      } else {
        meaning ***REMOVED*** 1920 /* Namespace */;
      }
      meaning |***REMOVED*** 2097152 /* Alias */;
      const entityNameSymbol ***REMOVED*** isEntityNameExpression(name) ? resolveEntityName(
        name,
        meaning,
        /*ignoreErrors*/
        true
      ) : void 0;
      if (entityNameSymbol) {
        return entityNameSymbol;
      }
    }
    if (name.parent.kind ***REMOVED******REMOVED******REMOVED*** 342 /* JSDocParameterTag */) {
      return getParameterSymbolFromJSDoc(name.parent);
    }
    if (name.parent.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ && name.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 346 /* JSDocTemplateTag */) {
      Debug.assert(!isInJSFile(name));
      const typeParameter ***REMOVED*** getTypeParameterFromJsDoc(name.parent);
      return typeParameter && typeParameter.symbol;
    }
    if (isExpressionNode(name)) {
      if (nodeIsMissing(name)) {
        return void 0;
      }
      const isJSDoc2 ***REMOVED*** findAncestor(name, or(isJSDocLinkLike, isJSDocNameReference, isJSDocMemberName));
      const meaning ***REMOVED*** isJSDoc2 ? 788968 /* Type */ | 1920 /* Namespace */ | 111551 /* Value */ : 111551 /* Value */;
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
        if (isJSXTagName(name) && isJsxIntrinsicTagName(name)) {
          const symbol ***REMOVED*** getIntrinsicTagSymbol(name.parent);
          return symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : symbol;
        }
        const result ***REMOVED*** resolveEntityName(
          name,
          meaning,
          /*ignoreErrors*/
          true,
          /*dontResolveAlias*/
          true,
          getHostSignatureFromJSDoc(name)
        );
        if (!result && isJSDoc2) {
          const container ***REMOVED*** findAncestor(name, or(isClassLike, isInterfaceDeclaration));
          if (container) {
            return resolveJSDocMemberName(
              name,
              /*ignoreErrors*/
              true,
              getSymbolOfDeclaration(container)
            );
          }
        }
        if (result && isJSDoc2) {
          const container ***REMOVED*** getJSDocHost(name);
          if (container && isEnumMember(container) && container ***REMOVED******REMOVED******REMOVED*** result.valueDeclaration) {
            return resolveEntityName(
              name,
              meaning,
              /*ignoreErrors*/
              true,
              /*dontResolveAlias*/
              true,
              getSourceFileOfNode(container)
            ) || result;
          }
        }
        return result;
      } else if (isPrivateIdentifier(name)) {
        return getSymbolForPrivateIdentifierExpression(name);
      } else if (name.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || name.kind ***REMOVED******REMOVED******REMOVED*** 167 /* QualifiedName */) {
        const links ***REMOVED*** getNodeLinks(name);
        if (links.resolvedSymbol) {
          return links.resolvedSymbol;
        }
        if (name.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */) {
          checkPropertyAccessExpression(name, 0 /* Normal */);
          if (!links.resolvedSymbol) {
            links.resolvedSymbol ***REMOVED*** getApplicableIndexSymbol(checkExpressionCached(name.expression), getLiteralTypeFromPropertyName(name.name));
          }
        } else {
          checkQualifiedName(name, 0 /* Normal */);
        }
        if (!links.resolvedSymbol && isJSDoc2 && isQualifiedName(name)) {
          return resolveJSDocMemberName(name);
        }
        return links.resolvedSymbol;
      } else if (isJSDocMemberName(name)) {
        return resolveJSDocMemberName(name);
      }
    } else if (isEntityName(name) && isTypeReferenceIdentifier(name)) {
      const meaning ***REMOVED*** name.parent.kind ***REMOVED******REMOVED******REMOVED*** 184 /* TypeReference */ ? 788968 /* Type */ : 1920 /* Namespace */;
      const symbol ***REMOVED*** resolveEntityName(
        name,
        meaning,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        true
      );
      return symbol && symbol !***REMOVED******REMOVED*** unknownSymbol ? symbol : getUnresolvedSymbolForEntityName(name);
    }
    if (name.parent.kind ***REMOVED******REMOVED******REMOVED*** 183 /* TypePredicate */) {
      return resolveEntityName(
        name,
        /*meaning*/
        1 /* FunctionScopedVariable */,
        /*ignoreErrors*/
        true
      );
    }
    return void 0;
  }
  function getApplicableIndexSymbol(type, keyType) {
    const infos ***REMOVED*** getApplicableIndexInfos(type, keyType);
    if (infos.length && type.members) {
      const symbol ***REMOVED*** getIndexSymbolFromSymbolTable(resolveStructuredTypeMembers(type).members);
      if (infos ***REMOVED******REMOVED******REMOVED*** getIndexInfosOfType(type)) {
        return symbol;
      } else if (symbol) {
        const symbolLinks2 ***REMOVED*** getSymbolLinks(symbol);
        const declarationList ***REMOVED*** mapDefined(infos, (i) ***REMOVED***> i.declaration);
        const nodeListId ***REMOVED*** map(declarationList, getNodeId).join(",");
        if (!symbolLinks2.filteredIndexSymbolCache) {
          symbolLinks2.filteredIndexSymbolCache ***REMOVED*** /* @__PURE__ */ new Map();
        }
        if (symbolLinks2.filteredIndexSymbolCache.has(nodeListId)) {
          return symbolLinks2.filteredIndexSymbolCache.get(nodeListId);
        } else {
          const copy ***REMOVED*** createSymbol(131072 /* Signature */, "__index" /* Index */);
          copy.declarations ***REMOVED*** mapDefined(infos, (i) ***REMOVED***> i.declaration);
          copy.parent ***REMOVED*** type.aliasSymbol ? type.aliasSymbol : type.symbol ? type.symbol : getSymbolAtLocation(copy.declarations[0].parent);
          symbolLinks2.filteredIndexSymbolCache.set(nodeListId, copy);
          return copy;
        }
      }
    }
  }
  function resolveJSDocMemberName(name, ignoreErrors, container) {
    if (isEntityName(name)) {
      const meaning ***REMOVED*** 788968 /* Type */ | 1920 /* Namespace */ | 111551 /* Value */;
      let symbol ***REMOVED*** resolveEntityName(
        name,
        meaning,
        ignoreErrors,
        /*dontResolveAlias*/
        true,
        getHostSignatureFromJSDoc(name)
      );
      if (!symbol && isIdentifier(name) && container) {
        symbol ***REMOVED*** getMergedSymbol(getSymbol(getExportsOfSymbol(container), name.escapedText, meaning));
      }
      if (symbol) {
        return symbol;
      }
    }
    const left ***REMOVED*** isIdentifier(name) ? container : resolveJSDocMemberName(name.left, ignoreErrors, container);
    const right ***REMOVED*** isIdentifier(name) ? name.escapedText : name.right.escapedText;
    if (left) {
      const proto ***REMOVED*** left.flags & 111551 /* Value */ && getPropertyOfType(getTypeOfSymbol(left), "prototype");
      const t ***REMOVED*** proto ? getTypeOfSymbol(proto) : getDeclaredTypeOfSymbol(left);
      return getPropertyOfType(t, right);
    }
  }
  function getSymbolAtLocation(node, ignoreErrors) {
    if (isSourceFile(node)) {
      return isExternalModule(node) ? getMergedSymbol(node.symbol) : void 0;
    }
    const { parent } ***REMOVED*** node;
    const grandParent ***REMOVED*** parent.parent;
    if (node.flags & 67108864 /* InWithStatement */) {
      return void 0;
    }
    if (isDeclarationNameOrImportPropertyName(node)) {
      const parentSymbol ***REMOVED*** getSymbolOfDeclaration(parent);
      return isImportOrExportSpecifier(node.parent) && node.parent.propertyName ***REMOVED******REMOVED******REMOVED*** node ? getImmediateAliasedSymbol(parentSymbol) : parentSymbol;
    } else if (isLiteralComputedPropertyDeclarationName(node)) {
      return getSymbolOfDeclaration(parent.parent);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      if (isInRightSideOfImportOrExportAssignment(node)) {
        return getSymbolOfNameOrPropertyAccessExpression(node);
      } else if (parent.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */ && grandParent.kind ***REMOVED******REMOVED******REMOVED*** 207 /* ObjectBindingPattern */ && node ***REMOVED******REMOVED******REMOVED*** parent.propertyName) {
        const typeOfPattern ***REMOVED*** getTypeOfNode(grandParent);
        const propertyDeclaration ***REMOVED*** getPropertyOfType(typeOfPattern, node.escapedText);
        if (propertyDeclaration) {
          return propertyDeclaration;
        }
      } else if (isMetaProperty(parent) && parent.name ***REMOVED******REMOVED******REMOVED*** node) {
        if (parent.keywordToken ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */ && idText(node) ***REMOVED******REMOVED******REMOVED*** "target") {
          return checkNewTargetMetaProperty(parent).symbol;
        }
        if (parent.keywordToken ***REMOVED******REMOVED******REMOVED*** 102 /* ImportKeyword */ && idText(node) ***REMOVED******REMOVED******REMOVED*** "meta") {
          return getGlobalImportMetaExpressionType().members.get("meta");
        }
        return void 0;
      }
    }
    switch (node.kind) {
      case 80 /* Identifier */:
      case 81 /* PrivateIdentifier */:
      case 212 /* PropertyAccessExpression */:
      case 167 /* QualifiedName */:
        if (!isThisInTypeQuery(node)) {
          return getSymbolOfNameOrPropertyAccessExpression(node);
        }
      // falls through
      case 110 /* ThisKeyword */:
        const container ***REMOVED*** getThisContainer(
          node,
          /*includeArrowFunctions*/
          false,
          /*includeClassComputedPropertyName*/
          false
        );
        if (isFunctionLike(container)) {
          const sig ***REMOVED*** getSignatureFromDeclaration(container);
          if (sig.thisParameter) {
            return sig.thisParameter;
          }
        }
        if (isInExpressionContext(node)) {
          return checkExpression(node).symbol;
        }
      // falls through
      case 198 /* ThisType */:
        return getTypeFromThisTypeNode(node).symbol;
      case 108 /* SuperKeyword */:
        return checkExpression(node).symbol;
      case 137 /* ConstructorKeyword */:
        const constructorDeclaration ***REMOVED*** node.parent;
        if (constructorDeclaration && constructorDeclaration.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
          return constructorDeclaration.parent.symbol;
        }
        return void 0;
      case 11 /* StringLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
        if (isExternalModuleImportEqualsDeclaration(node.parent.parent) && getExternalModuleImportEqualsDeclarationExpression(node.parent.parent) ***REMOVED******REMOVED******REMOVED*** node || (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */) && node.parent.moduleSpecifier ***REMOVED******REMOVED******REMOVED*** node || isInJSFile(node) && isJSDocImportTag(node.parent) && node.parent.moduleSpecifier ***REMOVED******REMOVED******REMOVED*** node || (isInJSFile(node) && isRequireCall(
          node.parent,
          /*requireStringLiteralLikeArgument*/
          false
        ) || isImportCall(node.parent)) || isLiteralTypeNode(node.parent) && isLiteralImportTypeNode(node.parent.parent) && node.parent.parent.argument ***REMOVED******REMOVED******REMOVED*** node.parent) {
          return resolveExternalModuleName(node, node, ignoreErrors);
        }
        if (isCallExpression(parent) && isBindableObjectDefinePropertyCall(parent) && parent.arguments[1] ***REMOVED******REMOVED******REMOVED*** node) {
          return getSymbolOfDeclaration(parent);
        }
      // falls through
      case 9 /* NumericLiteral */:
        const objectType ***REMOVED*** isElementAccessExpression(parent) ? parent.argumentExpression ***REMOVED******REMOVED******REMOVED*** node ? getTypeOfExpression(parent.expression) : void 0 : isLiteralTypeNode(parent) && isIndexedAccessTypeNode(grandParent) ? getTypeFromTypeNode(grandParent.objectType) : void 0;
        return objectType && getPropertyOfType(objectType, escapeLeadingUnderscores(node.text));
      case 90 /* DefaultKeyword */:
      case 100 /* FunctionKeyword */:
      case 39 /* EqualsGreaterThanToken */:
      case 86 /* ClassKeyword */:
        return getSymbolOfNode(node.parent);
      case 206 /* ImportType */:
        return isLiteralImportTypeNode(node) ? getSymbolAtLocation(node.argument.literal, ignoreErrors) : void 0;
      case 95 /* ExportKeyword */:
        return isExportAssignment(node.parent) ? Debug.checkDefined(node.parent.symbol) : void 0;
      case 102 /* ImportKeyword */:
        if (isMetaProperty(node.parent) && node.parent.name.escapedText ***REMOVED******REMOVED******REMOVED*** "defer") {
          return void 0;
        }
      // falls through
      case 105 /* NewKeyword */:
        return isMetaProperty(node.parent) ? checkMetaPropertyKeyword(node.parent).symbol : void 0;
      case 104 /* InstanceOfKeyword */:
        if (isBinaryExpression(node.parent)) {
          const type ***REMOVED*** getTypeOfExpression(node.parent.right);
          const hasInstanceMethodType ***REMOVED*** getSymbolHasInstanceMethodOfObjectType(type);
          return (hasInstanceMethodType ***REMOVED******REMOVED*** null ? void 0 : hasInstanceMethodType.symbol) ?? type.symbol;
        }
        return void 0;
      case 237 /* MetaProperty */:
        return checkExpression(node).symbol;
      case 296 /* JsxNamespacedName */:
        if (isJSXTagName(node) && isJsxIntrinsicTagName(node)) {
          const symbol ***REMOVED*** getIntrinsicTagSymbol(node.parent);
          return symbol ***REMOVED******REMOVED******REMOVED*** unknownSymbol ? void 0 : symbol;
        }
      // falls through
      default:
        return void 0;
    }
  }
  function getIndexInfosAtLocation(node) {
    if (isIdentifier(node) && isPropertyAccessExpression(node.parent) && node.parent.name ***REMOVED******REMOVED******REMOVED*** node) {
      const keyType ***REMOVED*** getLiteralTypeFromPropertyName(node);
      const objectType ***REMOVED*** getTypeOfExpression(node.parent.expression);
      const objectTypes ***REMOVED*** objectType.flags & 1048576 /* Union */ ? objectType.types : [objectType];
      return flatMap(objectTypes, (t) ***REMOVED***> filter(getIndexInfosOfType(t), (info) ***REMOVED***> isApplicableIndexType(keyType, info.keyType)));
    }
    return void 0;
  }
  function getShorthandAssignmentValueSymbol(location) {
    if (location && location.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */) {
      return resolveEntityName(
        location.name,
        111551 /* Value */ | 2097152 /* Alias */,
        /*ignoreErrors*/
        true
      );
    }
    return void 0;
  }
  function getExportSpecifierLocalTargetSymbol(node) {
    if (isExportSpecifier(node)) {
      const name ***REMOVED*** node.propertyName || node.name;
      return node.parent.parent.moduleSpecifier ? getExternalModuleMember(node.parent.parent, node) : name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? void 0 : (
        // Skip for invalid syntax like this: export { "x" }
        resolveEntityName(
          name,
          111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
          /*ignoreErrors*/
          true
        )
      );
    } else {
      return resolveEntityName(
        node,
        111551 /* Value */ | 788968 /* Type */ | 1920 /* Namespace */ | 2097152 /* Alias */,
        /*ignoreErrors*/
        true
      );
    }
  }
  function getTypeOfNode(node) {
    if (isSourceFile(node) && !isExternalModule(node)) {
      return errorType;
    }
    if (node.flags & 67108864 /* InWithStatement */) {
      return errorType;
    }
    const classDecl ***REMOVED*** tryGetClassImplementingOrExtendingExpressionWithTypeArguments(node);
    const classType ***REMOVED*** classDecl && getDeclaredTypeOfClassOrInterface(getSymbolOfDeclaration(classDecl.class));
    if (isPartOfTypeNode(node)) {
      const typeFromTypeNode ***REMOVED*** getTypeFromTypeNode(node);
      return classType ? getTypeWithThisArgument(typeFromTypeNode, classType.thisType) : typeFromTypeNode;
    }
    if (isExpressionNode(node)) {
      return getRegularTypeOfExpression(node);
    }
    if (classType && !classDecl.isImplements) {
      const baseType ***REMOVED*** firstOrUndefined(getBaseTypes(classType));
      return baseType ? getTypeWithThisArgument(baseType, classType.thisType) : errorType;
    }
    if (isTypeDeclaration(node)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      return getDeclaredTypeOfSymbol(symbol);
    }
    if (isTypeDeclarationName(node)) {
      const symbol ***REMOVED*** getSymbolAtLocation(node);
      return symbol ? getDeclaredTypeOfSymbol(symbol) : errorType;
    }
    if (isBindingElement(node)) {
      return getTypeForVariableLikeDeclaration(
        node,
        /*includeOptionality*/
        true,
        0 /* Normal */
      ) || errorType;
    }
    if (isDeclaration(node)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      return symbol ? getTypeOfSymbol(symbol) : errorType;
    }
    if (isDeclarationNameOrImportPropertyName(node)) {
      const symbol ***REMOVED*** getSymbolAtLocation(node);
      if (symbol) {
        return getTypeOfSymbol(symbol);
      }
      return errorType;
    }
    if (isBindingPattern(node)) {
      return getTypeForVariableLikeDeclaration(
        node.parent,
        /*includeOptionality*/
        true,
        0 /* Normal */
      ) || errorType;
    }
    if (isInRightSideOfImportOrExportAssignment(node)) {
      const symbol ***REMOVED*** getSymbolAtLocation(node);
      if (symbol) {
        const declaredType ***REMOVED*** getDeclaredTypeOfSymbol(symbol);
        return !isErrorType(declaredType) ? declaredType : getTypeOfSymbol(symbol);
      }
    }
    if (isMetaProperty(node.parent) && node.parent.keywordToken ***REMOVED******REMOVED******REMOVED*** node.kind) {
      return checkMetaPropertyKeyword(node.parent);
    }
    if (isImportAttributes(node)) {
      return getGlobalImportAttributesType(
        /*reportErrors*/
        false
      );
    }
    return errorType;
  }
  function getTypeOfAssignmentPattern(expr) {
    Debug.assert(expr.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */ || expr.kind ***REMOVED******REMOVED******REMOVED*** 210 /* ArrayLiteralExpression */);
    if (expr.parent.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */) {
      const iteratedType ***REMOVED*** checkRightHandSideOfForOf(expr.parent);
      return checkDestructuringAssignment(expr, iteratedType || errorType);
    }
    if (expr.parent.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */) {
      const iteratedType ***REMOVED*** getTypeOfExpression(expr.parent.right);
      return checkDestructuringAssignment(expr, iteratedType || errorType);
    }
    if (expr.parent.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
      const node2 ***REMOVED*** cast(expr.parent.parent, isObjectLiteralExpression);
      const typeOfParentObjectLiteral ***REMOVED*** getTypeOfAssignmentPattern(node2) || errorType;
      const propertyIndex ***REMOVED*** indexOfNode(node2.properties, expr.parent);
      return checkObjectLiteralDestructuringPropertyAssignment(node2, typeOfParentObjectLiteral, propertyIndex);
    }
    const node ***REMOVED*** cast(expr.parent, isArrayLiteralExpression);
    const typeOfArrayLiteral ***REMOVED*** getTypeOfAssignmentPattern(node) || errorType;
    const elementType ***REMOVED*** checkIteratedTypeOrElementType(65 /* Destructuring */, typeOfArrayLiteral, undefinedType, expr.parent) || errorType;
    return checkArrayLiteralDestructuringElementAssignment(node, typeOfArrayLiteral, node.elements.indexOf(expr), elementType);
  }
  function getPropertySymbolOfDestructuringAssignment(location) {
    const typeOfObjectLiteral ***REMOVED*** getTypeOfAssignmentPattern(cast(location.parent.parent, isAssignmentPattern));
    return typeOfObjectLiteral && getPropertyOfType(typeOfObjectLiteral, location.escapedText);
  }
  function getRegularTypeOfExpression(expr) {
    if (isRightSideOfQualifiedNameOrPropertyAccess(expr)) {
      expr ***REMOVED*** expr.parent;
    }
    return getRegularTypeOfLiteralType(getTypeOfExpression(expr));
  }
  function getParentTypeOfClassElement(node) {
    const classSymbol ***REMOVED*** getSymbolOfNode(node.parent);
    return isStatic(node) ? getTypeOfSymbol(classSymbol) : getDeclaredTypeOfSymbol(classSymbol);
  }
  function getClassElementPropertyKeyType(element) {
    const name ***REMOVED*** element.name;
    switch (name.kind) {
      case 80 /* Identifier */:
        return getStringLiteralType(idText(name));
      case 9 /* NumericLiteral */:
      case 11 /* StringLiteral */:
        return getStringLiteralType(name.text);
      case 168 /* ComputedPropertyName */:
        const nameType ***REMOVED*** checkComputedPropertyName(name);
        return isTypeAssignableToKind(nameType, 12288 /* ESSymbolLike */) ? nameType : stringType;
      default:
        return Debug.fail("Unsupported property name.");
    }
  }
  function getAugmentedPropertiesOfType(type) {
    type ***REMOVED*** getApparentType(type);
    const propsByName ***REMOVED*** createSymbolTable(getPropertiesOfType(type));
    const functionType ***REMOVED*** getSignaturesOfType(type, 0 /* Call */).length ? globalCallableFunctionType : getSignaturesOfType(type, 1 /* Construct */).length ? globalNewableFunctionType : void 0;
    if (functionType) {
      forEach(getPropertiesOfType(functionType), (p) ***REMOVED***> {
        if (!propsByName.has(p.escapedName)) {
          propsByName.set(p.escapedName, p);
        }
      });
    }
    return getNamedMembers(propsByName);
  }
  function typeHasCallOrConstructSignatures(type) {
    return getSignaturesOfType(type, 0 /* Call */).length !***REMOVED******REMOVED*** 0 || getSignaturesOfType(type, 1 /* Construct */).length !***REMOVED******REMOVED*** 0;
  }
  function getRootSymbols(symbol) {
    const roots ***REMOVED*** getImmediateRootSymbols(symbol);
    return roots ? flatMap(roots, getRootSymbols) : [symbol];
  }
  function getImmediateRootSymbols(symbol) {
    if (getCheckFlags(symbol) & 6 /* Synthetic */) {
      return mapDefined(getSymbolLinks(symbol).containingType.types, (type) ***REMOVED***> getPropertyOfType(type, symbol.escapedName));
    } else if (symbol.flags & 33554432 /* Transient */) {
      const { links: { leftSpread, rightSpread, syntheticOrigin } } ***REMOVED*** symbol;
      return leftSpread ? [leftSpread, rightSpread] : syntheticOrigin ? [syntheticOrigin] : singleElementArray(tryGetTarget(symbol));
    }
    return void 0;
  }
  function tryGetTarget(symbol) {
    let target;
    let next ***REMOVED*** symbol;
    while (next ***REMOVED*** getSymbolLinks(next).target) {
      target ***REMOVED*** next;
    }
    return target;
  }
  function isArgumentsLocalBinding(nodeIn) {
    if (isGeneratedIdentifier(nodeIn)) return false;
    const node ***REMOVED*** getParseTreeNode(nodeIn, isIdentifier);
    if (!node) return false;
    const parent ***REMOVED*** node.parent;
    if (!parent) return false;
    const isPropertyName2 ***REMOVED*** (isPropertyAccessExpression(parent) || isPropertyAssignment(parent)) && parent.name ***REMOVED******REMOVED******REMOVED*** node;
    return !isPropertyName2 && getReferencedValueSymbol(node) ***REMOVED******REMOVED******REMOVED*** argumentsSymbol;
  }
  function isNameOfModuleOrEnumDeclaration(node) {
    return isModuleOrEnumDeclaration(node.parent) && node ***REMOVED******REMOVED******REMOVED*** node.parent.name;
  }
  function getReferencedExportContainer(nodeIn, prefixLocals) {
    var _a;
    const node ***REMOVED*** getParseTreeNode(nodeIn, isIdentifier);
    if (node) {
      let symbol ***REMOVED*** getReferencedValueSymbol(
        node,
        /*startInDeclarationContainer*/
        isNameOfModuleOrEnumDeclaration(node)
      );
      if (symbol) {
        if (symbol.flags & 1048576 /* ExportValue */) {
          const exportSymbol ***REMOVED*** getMergedSymbol(symbol.exportSymbol);
          if (!prefixLocals && exportSymbol.flags & 944 /* ExportHasLocal */ && !(exportSymbol.flags & 3 /* Variable */)) {
            return void 0;
          }
          symbol ***REMOVED*** exportSymbol;
        }
        const parentSymbol ***REMOVED*** getParentOfSymbol(symbol);
        if (parentSymbol) {
          if (parentSymbol.flags & 512 /* ValueModule */ && ((_a ***REMOVED*** parentSymbol.valueDeclaration) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
            const symbolFile ***REMOVED*** parentSymbol.valueDeclaration;
            const referenceFile ***REMOVED*** getSourceFileOfNode(node);
            const symbolIsUmdExport ***REMOVED*** symbolFile !***REMOVED******REMOVED*** referenceFile;
            return symbolIsUmdExport ? void 0 : symbolFile;
          }
          return findAncestor(node.parent, (n) ***REMOVED***> isModuleOrEnumDeclaration(n) && getSymbolOfDeclaration(n) ***REMOVED******REMOVED******REMOVED*** parentSymbol);
        }
      }
    }
  }
  function getReferencedImportDeclaration(nodeIn) {
    const specifier ***REMOVED*** getIdentifierGeneratedImportReference(nodeIn);
    if (specifier) {
      return specifier;
    }
    const node ***REMOVED*** getParseTreeNode(nodeIn, isIdentifier);
    if (node) {
      const symbol ***REMOVED*** getReferencedValueOrAliasSymbol(node);
      if (isNonLocalAlias(
        symbol,
        /*excludes*/
        111551 /* Value */
      ) && !getTypeOnlyAliasDeclaration(symbol, 111551 /* Value */)) {
        return getDeclarationOfAliasSymbol(symbol);
      }
    }
    return void 0;
  }
  function isSymbolOfDestructuredElementOfCatchBinding(symbol) {
    return symbol.valueDeclaration && isBindingElement(symbol.valueDeclaration) && walkUpBindingElementsAndPatterns(symbol.valueDeclaration).parent.kind ***REMOVED******REMOVED******REMOVED*** 300 /* CatchClause */;
  }
  function isSymbolOfDeclarationWithCollidingName(symbol) {
    if (symbol.flags & 418 /* BlockScoped */ && symbol.valueDeclaration && !isSourceFile(symbol.valueDeclaration)) {
      const links ***REMOVED*** getSymbolLinks(symbol);
      if (links.isDeclarationWithCollidingName ***REMOVED******REMOVED******REMOVED*** void 0) {
        const container ***REMOVED*** getEnclosingBlockScopeContainer(symbol.valueDeclaration);
        if (isStatementWithLocals(container) || isSymbolOfDestructuredElementOfCatchBinding(symbol)) {
          if (resolveName(
            container.parent,
            symbol.escapedName,
            111551 /* Value */,
            /*nameNotFoundMessage*/
            void 0,
            /*isUse*/
            false
          )) {
            links.isDeclarationWithCollidingName ***REMOVED*** true;
          } else if (hasNodeCheckFlag(symbol.valueDeclaration, 16384 /* CapturedBlockScopedBinding */)) {
            const isDeclaredInLoop ***REMOVED*** hasNodeCheckFlag(symbol.valueDeclaration, 32768 /* BlockScopedBindingInLoop */);
            const inLoopInitializer ***REMOVED*** isIterationStatement(
              container,
              /*lookInLabeledStatements*/
              false
            );
            const inLoopBodyBlock ***REMOVED*** container.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */ && isIterationStatement(
              container.parent,
              /*lookInLabeledStatements*/
              false
            );
            links.isDeclarationWithCollidingName ***REMOVED*** !isBlockScopedContainerTopLevel(container) && (!isDeclaredInLoop || !inLoopInitializer && !inLoopBodyBlock);
          } else {
            links.isDeclarationWithCollidingName ***REMOVED*** false;
          }
        }
      }
      return links.isDeclarationWithCollidingName;
    }
    return false;
  }
  function getReferencedDeclarationWithCollidingName(nodeIn) {
    if (!isGeneratedIdentifier(nodeIn)) {
      const node ***REMOVED*** getParseTreeNode(nodeIn, isIdentifier);
      if (node) {
        const symbol ***REMOVED*** getReferencedValueSymbol(node);
        if (symbol && isSymbolOfDeclarationWithCollidingName(symbol)) {
          return symbol.valueDeclaration;
        }
      }
    }
    return void 0;
  }
  function isDeclarationWithCollidingName(nodeIn) {
    const node ***REMOVED*** getParseTreeNode(nodeIn, isDeclaration);
    if (node) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      if (symbol) {
        return isSymbolOfDeclarationWithCollidingName(symbol);
      }
    }
    return false;
  }
  function isValueAliasDeclaration(node) {
    Debug.assert(canCollectSymbolAliasAccessabilityData);
    switch (node.kind) {
      case 272 /* ImportEqualsDeclaration */:
        return isAliasResolvedToValue(getSymbolOfDeclaration(node));
      case 274 /* ImportClause */:
      case 275 /* NamespaceImport */:
      case 277 /* ImportSpecifier */:
      case 282 /* ExportSpecifier */:
        const symbol ***REMOVED*** getSymbolOfDeclaration(node);
        return !!symbol && isAliasResolvedToValue(
          symbol,
          /*excludeTypeOnlyValues*/
          true
        );
      case 279 /* ExportDeclaration */:
        const exportClause ***REMOVED*** node.exportClause;
        return !!exportClause && (isNamespaceExport(exportClause) || some(exportClause.elements, isValueAliasDeclaration));
      case 278 /* ExportAssignment */:
        return node.expression && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? isAliasResolvedToValue(
          getSymbolOfDeclaration(node),
          /*excludeTypeOnlyValues*/
          true
        ) : true;
    }
    return false;
  }
  function isTopLevelValueImportEqualsWithEntityName(nodeIn) {
    const node ***REMOVED*** getParseTreeNode(nodeIn, isImportEqualsDeclaration);
    if (node ***REMOVED******REMOVED******REMOVED*** void 0 || node.parent.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ || !isInternalModuleImportEqualsDeclaration(node)) {
      return false;
    }
    const isValue ***REMOVED*** isAliasResolvedToValue(getSymbolOfDeclaration(node));
    return isValue && node.moduleReference && !nodeIsMissing(node.moduleReference);
  }
  function isAliasResolvedToValue(symbol, excludeTypeOnlyValues) {
    if (!symbol) {
      return false;
    }
    const container ***REMOVED*** getSourceFileOfNode(symbol.valueDeclaration);
    const fileSymbol ***REMOVED*** container && getSymbolOfDeclaration(container);
    void resolveExternalModuleSymbol(fileSymbol);
    const target ***REMOVED*** getExportSymbolOfValueSymbolIfExported(resolveAlias(symbol));
    if (target ***REMOVED******REMOVED******REMOVED*** unknownSymbol) {
      return !excludeTypeOnlyValues || !getTypeOnlyAliasDeclaration(symbol);
    }
    return !!(getSymbolFlags(
      symbol,
      excludeTypeOnlyValues,
      /*excludeLocalMeanings*/
      true
    ) & 111551 /* Value */) && (shouldPreserveConstEnums(compilerOptions) || !isConstEnumOrConstEnumOnlyModule(target));
  }
  function isConstEnumOrConstEnumOnlyModule(s) {
    return isConstEnumSymbol(s) || !!s.constEnumOnlyModule;
  }
  function isReferencedAliasDeclaration(node, checkChildren) {
    Debug.assert(canCollectSymbolAliasAccessabilityData);
    if (isAliasSymbolDeclaration(node)) {
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      const links ***REMOVED*** symbol && getSymbolLinks(symbol);
      if (links ***REMOVED******REMOVED*** null ? void 0 : links.referenced) {
        return true;
      }
      const target ***REMOVED*** getSymbolLinks(symbol).aliasTarget;
      if (target && getEffectiveModifierFlags(node) & 32 /* Export */ && getSymbolFlags(target) & 111551 /* Value */ && (shouldPreserveConstEnums(compilerOptions) || !isConstEnumOrConstEnumOnlyModule(target))) {
        return true;
      }
    }
    if (checkChildren) {
      return !!forEachChild(node, (node2) ***REMOVED***> isReferencedAliasDeclaration(node2, checkChildren));
    }
    return false;
  }
  function isImplementationOfOverload(node) {
    if (nodeIsPresent(node.body)) {
      if (isGetAccessor(node) || isSetAccessor(node)) return false;
      const symbol ***REMOVED*** getSymbolOfDeclaration(node);
      const signaturesOfSymbol ***REMOVED*** getSignaturesOfSymbol(symbol);
      return signaturesOfSymbol.length > 1 || // If there is single signature for the symbol, it is overload if that signature isn't coming from the node
      // e.g.: function foo(a: string): string;
      //       function foo(a: any) { // This is implementation of the overloads
      //           return a;
      //       }
      signaturesOfSymbol.length ***REMOVED******REMOVED******REMOVED*** 1 && signaturesOfSymbol[0].declaration !***REMOVED******REMOVED*** node;
    }
    return false;
  }
  function declaredParameterTypeContainsUndefined(parameter) {
    const typeNode ***REMOVED*** getNonlocalEffectiveTypeAnnotationNode(parameter);
    if (!typeNode) return false;
    const type ***REMOVED*** getTypeFromTypeNode(typeNode);
    return isErrorType(type) || containsUndefinedType(type);
  }
  function requiresAddingImplicitUndefined(parameter, enclosingDeclaration) {
    return (isRequiredInitializedParameter(parameter, enclosingDeclaration) || isOptionalUninitializedParameterProperty(parameter)) && !declaredParameterTypeContainsUndefined(parameter);
  }
  function isRequiredInitializedParameter(parameter, enclosingDeclaration) {
    if (!strictNullChecks || isOptionalParameter(parameter) || isJSDocParameterTag(parameter) || !parameter.initializer) {
      return false;
    }
    if (hasSyntacticModifier(parameter, 31 /* ParameterPropertyModifier */)) {
      return !!enclosingDeclaration && isFunctionLikeDeclaration(enclosingDeclaration);
    }
    return true;
  }
  function isOptionalUninitializedParameterProperty(parameter) {
    return strictNullChecks && isOptionalParameter(parameter) && (isJSDocParameterTag(parameter) || !parameter.initializer) && hasSyntacticModifier(parameter, 31 /* ParameterPropertyModifier */);
  }
  function isExpandoFunctionDeclaration(node) {
    const declaration ***REMOVED*** getParseTreeNode(node, (n) ***REMOVED***> isFunctionDeclaration(n) || isVariableDeclaration(n));
    if (!declaration) {
      return false;
    }
    let symbol;
    if (isVariableDeclaration(declaration)) {
      if (declaration.type || !isInJSFile(declaration) && !isVarConstLike2(declaration)) {
        return false;
      }
      const initializer ***REMOVED*** getDeclaredExpandoInitializer(declaration);
      if (!initializer || !canHaveSymbol(initializer)) {
        return false;
      }
      symbol ***REMOVED*** getSymbolOfDeclaration(initializer);
    } else {
      symbol ***REMOVED*** getSymbolOfDeclaration(declaration);
    }
    if (!symbol || !(symbol.flags & 16 /* Function */ | 3 /* Variable */)) {
      return false;
    }
    return !!forEachEntry(getExportsOfSymbol(symbol), (p) ***REMOVED***> p.flags & 111551 /* Value */ && isExpandoPropertyDeclaration(p.valueDeclaration));
  }
  function getPropertiesOfContainerFunction(node) {
    const declaration ***REMOVED*** getParseTreeNode(node, isFunctionDeclaration);
    if (!declaration) {
      return emptyArray;
    }
    const symbol ***REMOVED*** getSymbolOfDeclaration(declaration);
    return symbol && getPropertiesOfType(getTypeOfSymbol(symbol)) || emptyArray;
  }
  function getNodeCheckFlags(node) {
    var _a;
    const nodeId ***REMOVED*** node.id || 0;
    if (nodeId < 0 || nodeId >***REMOVED*** nodeLinks.length) return 0;
    return ((_a ***REMOVED*** nodeLinks[nodeId]) ***REMOVED******REMOVED*** null ? void 0 : _a.flags) || 0;
  }
  function hasNodeCheckFlag(node, flag) {
    calculateNodeCheckFlagWorker(node, flag);
    return !!(getNodeCheckFlags(node) & flag);
  }
  function calculateNodeCheckFlagWorker(node, flag) {
    if (!compilerOptions.noCheck && canIncludeBindAndCheckDiagnostics(getSourceFileOfNode(node), compilerOptions)) {
      return;
    }
    const links ***REMOVED*** getNodeLinks(node);
    if (links.calculatedFlags & flag) {
      return;
    }
    switch (flag) {
      case 16 /* SuperInstance */:
      case 32 /* SuperStatic */:
        return checkSingleSuperExpression(node);
      case 128 /* MethodWithSuperPropertyAccessInAsync */:
      case 256 /* MethodWithSuperPropertyAssignmentInAsync */:
      case 2097152 /* ContainsSuperPropertyInStaticInitializer */:
        return checkChildSuperExpressions(node);
      case 512 /* CaptureArguments */:
      case 8192 /* ContainsCapturedBlockScopeBinding */:
      case 65536 /* NeedsLoopOutParameter */:
      case 262144 /* ContainsConstructorReference */:
        return checkChildIdentifiers(node);
      case 536870912 /* ConstructorReference */:
        return checkSingleIdentifier(node);
      case 4096 /* LoopWithCapturedBlockScopedBinding */:
      case 32768 /* BlockScopedBindingInLoop */:
      case 16384 /* CapturedBlockScopedBinding */:
        return checkContainingBlockScopeBindingUses(node);
      default:
        return Debug.assertNever(flag, `Unhandled node check flag calculation: ${Debug.formatNodeCheckFlags(flag)}`);
    }
    function forEachNodeRecursively(root, cb) {
      const rootResult ***REMOVED*** cb(root, root.parent);
      if (rootResult ***REMOVED******REMOVED******REMOVED*** "skip") return void 0;
      if (rootResult) return rootResult;
      return forEachChildRecursively(root, cb);
    }
    function checkSuperExpressions(node2) {
      const links2 ***REMOVED*** getNodeLinks(node2);
      if (links2.calculatedFlags & flag) return "skip";
      links2.calculatedFlags |***REMOVED*** 128 /* MethodWithSuperPropertyAccessInAsync */ | 256 /* MethodWithSuperPropertyAssignmentInAsync */ | 2097152 /* ContainsSuperPropertyInStaticInitializer */;
      checkSingleSuperExpression(node2);
      return void 0;
    }
    function checkChildSuperExpressions(node2) {
      forEachNodeRecursively(node2, checkSuperExpressions);
    }
    function checkSingleSuperExpression(node2) {
      const nodeLinks2 ***REMOVED*** getNodeLinks(node2);
      nodeLinks2.calculatedFlags |***REMOVED*** 16 /* SuperInstance */ | 32 /* SuperStatic */;
      if (node2.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
        checkSuperExpression(node2);
      }
    }
    function checkIdentifiers(node2) {
      const links2 ***REMOVED*** getNodeLinks(node2);
      if (links2.calculatedFlags & flag) return "skip";
      links2.calculatedFlags |***REMOVED*** 512 /* CaptureArguments */ | 8192 /* ContainsCapturedBlockScopeBinding */ | 65536 /* NeedsLoopOutParameter */ | 262144 /* ContainsConstructorReference */;
      checkSingleIdentifier(node2);
      return void 0;
    }
    function checkChildIdentifiers(node2) {
      forEachNodeRecursively(node2, checkIdentifiers);
    }
    function isExpressionNodeOrShorthandPropertyAssignmentName(node2) {
      return isExpressionNode(node2) || isShorthandPropertyAssignment(node2.parent) && (node2.parent.objectAssignmentInitializer ?? node2.parent.name) ***REMOVED******REMOVED******REMOVED*** node2;
    }
    function checkSingleIdentifier(node2) {
      const nodeLinks2 ***REMOVED*** getNodeLinks(node2);
      nodeLinks2.calculatedFlags |***REMOVED*** 536870912 /* ConstructorReference */;
      if (isIdentifier(node2)) {
        nodeLinks2.calculatedFlags |***REMOVED*** 32768 /* BlockScopedBindingInLoop */ | 16384 /* CapturedBlockScopedBinding */;
        if (isExpressionNodeOrShorthandPropertyAssignmentName(node2) && !(isPropertyAccessExpression(node2.parent) && node2.parent.name ***REMOVED******REMOVED******REMOVED*** node2)) {
          const s ***REMOVED*** getResolvedSymbol(node2);
          if (s && s !***REMOVED******REMOVED*** unknownSymbol) {
            checkIdentifierCalculateNodeCheckFlags(node2, s);
          }
        }
      }
    }
    function checkBlockScopeBindings(node2) {
      const links2 ***REMOVED*** getNodeLinks(node2);
      if (links2.calculatedFlags & flag) return "skip";
      links2.calculatedFlags |***REMOVED*** 4096 /* LoopWithCapturedBlockScopedBinding */ | 32768 /* BlockScopedBindingInLoop */ | 16384 /* CapturedBlockScopedBinding */;
      checkSingleBlockScopeBinding(node2);
      return void 0;
    }
    function checkContainingBlockScopeBindingUses(node2) {
      const scope ***REMOVED*** getEnclosingBlockScopeContainer(isDeclarationName(node2) ? node2.parent : node2);
      forEachNodeRecursively(scope, checkBlockScopeBindings);
    }
    function checkSingleBlockScopeBinding(node2) {
      checkSingleIdentifier(node2);
      if (isComputedPropertyName(node2)) {
        checkComputedPropertyName(node2);
      }
      if (isPrivateIdentifier(node2) && isClassElement(node2.parent)) {
        setNodeLinksForPrivateIdentifierScope(node2.parent);
      }
    }
  }
  function getEnumMemberValue(node) {
    computeEnumMemberValues(node.parent);
    return getNodeLinks(node).enumMemberValue ?? evaluatorResult(
      /*value*/
      void 0
    );
  }
  function canHaveConstantValue(node) {
    switch (node.kind) {
      case 307 /* EnumMember */:
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        return true;
    }
    return false;
  }
  function getConstantValue2(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 307 /* EnumMember */) {
      return getEnumMemberValue(node).value;
    }
    if (!getNodeLinks(node).resolvedSymbol) {
      void checkExpressionCached(node);
    }
    const symbol ***REMOVED*** getNodeLinks(node).resolvedSymbol || (isEntityNameExpression(node) ? resolveEntityName(
      node,
      111551 /* Value */,
      /*ignoreErrors*/
      true
    ) : void 0);
    if (symbol && symbol.flags & 8 /* EnumMember */) {
      const member ***REMOVED*** symbol.valueDeclaration;
      if (isEnumConst(member.parent)) {
        return getEnumMemberValue(member).value;
      }
    }
    return void 0;
  }
  function isFunctionType(type) {
    return !!(type.flags & 524288 /* Object */) && getSignaturesOfType(type, 0 /* Call */).length > 0;
  }
  function getTypeReferenceSerializationKind(typeNameIn, location) {
    var _a;
    const typeName ***REMOVED*** getParseTreeNode(typeNameIn, isEntityName);
    if (!typeName) return 0 /* Unknown */;
    if (location) {
      location ***REMOVED*** getParseTreeNode(location);
      if (!location) return 0 /* Unknown */;
    }
    let isTypeOnly ***REMOVED*** false;
    if (isQualifiedName(typeName)) {
      const rootValueSymbol ***REMOVED*** resolveEntityName(
        getFirstIdentifier(typeName),
        111551 /* Value */,
        /*ignoreErrors*/
        true,
        /*dontResolveAlias*/
        true,
        location
      );
      isTypeOnly ***REMOVED*** !!((_a ***REMOVED*** rootValueSymbol ***REMOVED******REMOVED*** null ? void 0 : rootValueSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.every(isTypeOnlyImportOrExportDeclaration));
    }
    const valueSymbol ***REMOVED*** resolveEntityName(
      typeName,
      111551 /* Value */,
      /*ignoreErrors*/
      true,
      /*dontResolveAlias*/
      true,
      location
    );
    const resolvedValueSymbol ***REMOVED*** valueSymbol && valueSymbol.flags & 2097152 /* Alias */ ? resolveAlias(valueSymbol) : valueSymbol;
    isTypeOnly || (isTypeOnly ***REMOVED*** !!(valueSymbol && getTypeOnlyAliasDeclaration(valueSymbol, 111551 /* Value */)));
    const typeSymbol ***REMOVED*** resolveEntityName(
      typeName,
      788968 /* Type */,
      /*ignoreErrors*/
      true,
      /*dontResolveAlias*/
      true,
      location
    );
    const resolvedTypeSymbol ***REMOVED*** typeSymbol && typeSymbol.flags & 2097152 /* Alias */ ? resolveAlias(typeSymbol) : typeSymbol;
    if (!valueSymbol) {
      isTypeOnly || (isTypeOnly ***REMOVED*** !!(typeSymbol && getTypeOnlyAliasDeclaration(typeSymbol, 788968 /* Type */)));
    }
    if (resolvedValueSymbol && resolvedValueSymbol ***REMOVED******REMOVED******REMOVED*** resolvedTypeSymbol) {
      const globalPromiseSymbol ***REMOVED*** getGlobalPromiseConstructorSymbol(
        /*reportErrors*/
        false
      );
      if (globalPromiseSymbol && resolvedValueSymbol ***REMOVED******REMOVED******REMOVED*** globalPromiseSymbol) {
        return 9 /* Promise */;
      }
      const constructorType ***REMOVED*** getTypeOfSymbol(resolvedValueSymbol);
      if (constructorType && isConstructorType(constructorType)) {
        return isTypeOnly ? 10 /* TypeWithCallSignature */ : 1 /* TypeWithConstructSignatureAndValue */;
      }
    }
    if (!resolvedTypeSymbol) {
      return isTypeOnly ? 11 /* ObjectType */ : 0 /* Unknown */;
    }
    const type ***REMOVED*** getDeclaredTypeOfSymbol(resolvedTypeSymbol);
    if (isErrorType(type)) {
      return isTypeOnly ? 11 /* ObjectType */ : 0 /* Unknown */;
    } else if (type.flags & 3 /* AnyOrUnknown */) {
      return 11 /* ObjectType */;
    } else if (isTypeAssignableToKind(type, 16384 /* Void */ | 98304 /* Nullable */ | 131072 /* Never */)) {
      return 2 /* VoidNullableOrNeverType */;
    } else if (isTypeAssignableToKind(type, 528 /* BooleanLike */)) {
      return 6 /* BooleanType */;
    } else if (isTypeAssignableToKind(type, 296 /* NumberLike */)) {
      return 3 /* NumberLikeType */;
    } else if (isTypeAssignableToKind(type, 2112 /* BigIntLike */)) {
      return 4 /* BigIntLikeType */;
    } else if (isTypeAssignableToKind(type, 402653316 /* StringLike */)) {
      return 5 /* StringLikeType */;
    } else if (isTupleType(type)) {
      return 7 /* ArrayLikeType */;
    } else if (isTypeAssignableToKind(type, 12288 /* ESSymbolLike */)) {
      return 8 /* ESSymbolType */;
    } else if (isFunctionType(type)) {
      return 10 /* TypeWithCallSignature */;
    } else if (isArrayType(type)) {
      return 7 /* ArrayLikeType */;
    } else {
      return 11 /* ObjectType */;
    }
  }
  function createTypeOfDeclaration(declarationIn, enclosingDeclaration, flags, internalFlags, tracker) {
    const declaration ***REMOVED*** getParseTreeNode(declarationIn, hasInferredType);
    if (!declaration) {
      return factory.createToken(133 /* AnyKeyword */);
    }
    const symbol ***REMOVED*** getSymbolOfDeclaration(declaration);
    return nodeBuilder.serializeTypeForDeclaration(declaration, symbol, enclosingDeclaration, flags | 1024 /* MultilineObjectLiterals */, internalFlags, tracker);
  }
  function getAllAccessorDeclarationsForDeclaration(accessor) {
    accessor ***REMOVED*** getParseTreeNode(accessor, isGetOrSetAccessorDeclaration);
    const otherKind ***REMOVED*** accessor.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ ? 178 /* GetAccessor */ : 179 /* SetAccessor */;
    const otherAccessor ***REMOVED*** getDeclarationOfKind(getSymbolOfDeclaration(accessor), otherKind);
    const firstAccessor ***REMOVED*** otherAccessor && otherAccessor.pos < accessor.pos ? otherAccessor : accessor;
    const secondAccessor ***REMOVED*** otherAccessor && otherAccessor.pos < accessor.pos ? accessor : otherAccessor;
    const setAccessor ***REMOVED*** accessor.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ ? accessor : otherAccessor;
    const getAccessor ***REMOVED*** accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? accessor : otherAccessor;
    return {
      firstAccessor,
      secondAccessor,
      setAccessor,
      getAccessor
    };
  }
  function createReturnTypeOfSignatureDeclaration(signatureDeclarationIn, enclosingDeclaration, flags, internalFlags, tracker) {
    const signatureDeclaration ***REMOVED*** getParseTreeNode(signatureDeclarationIn, isFunctionLike);
    if (!signatureDeclaration) {
      return factory.createToken(133 /* AnyKeyword */);
    }
    return nodeBuilder.serializeReturnTypeForSignature(signatureDeclaration, enclosingDeclaration, flags | 1024 /* MultilineObjectLiterals */, internalFlags, tracker);
  }
  function createTypeOfExpression(exprIn, enclosingDeclaration, flags, internalFlags, tracker) {
    const expr ***REMOVED*** getParseTreeNode(exprIn, isExpression);
    if (!expr) {
      return factory.createToken(133 /* AnyKeyword */);
    }
    return nodeBuilder.serializeTypeForExpression(expr, enclosingDeclaration, flags | 1024 /* MultilineObjectLiterals */, internalFlags, tracker);
  }
  function hasGlobalName(name) {
    return globals.has(escapeLeadingUnderscores(name));
  }
  function getReferencedValueSymbol(reference, startInDeclarationContainer) {
    const resolvedSymbol ***REMOVED*** getNodeLinks(reference).resolvedSymbol;
    if (resolvedSymbol) {
      return resolvedSymbol;
    }
    let location ***REMOVED*** reference;
    if (startInDeclarationContainer) {
      const parent ***REMOVED*** reference.parent;
      if (isDeclaration(parent) && reference ***REMOVED******REMOVED******REMOVED*** parent.name) {
        location ***REMOVED*** getDeclarationContainer(parent);
      }
    }
    return resolveName(
      location,
      reference.escapedText,
      111551 /* Value */ | 1048576 /* ExportValue */ | 2097152 /* Alias */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true
    );
  }
  function getReferencedValueOrAliasSymbol(reference) {
    const resolvedSymbol ***REMOVED*** getNodeLinks(reference).resolvedSymbol;
    if (resolvedSymbol && resolvedSymbol !***REMOVED******REMOVED*** unknownSymbol) {
      return resolvedSymbol;
    }
    return resolveName(
      reference,
      reference.escapedText,
      111551 /* Value */ | 1048576 /* ExportValue */ | 2097152 /* Alias */,
      /*nameNotFoundMessage*/
      void 0,
      /*isUse*/
      true,
      /*excludeGlobals*/
      void 0
    );
  }
  function getReferencedValueDeclaration(referenceIn) {
    if (!isGeneratedIdentifier(referenceIn)) {
      const reference ***REMOVED*** getParseTreeNode(referenceIn, isIdentifier);
      if (reference) {
        const symbol ***REMOVED*** getReferencedValueSymbol(reference);
        if (symbol) {
          return getExportSymbolOfValueSymbolIfExported(symbol).valueDeclaration;
        }
      }
    }
    return void 0;
  }
  function getReferencedValueDeclarations(referenceIn) {
    if (!isGeneratedIdentifier(referenceIn)) {
      const reference ***REMOVED*** getParseTreeNode(referenceIn, isIdentifier);
      if (reference) {
        const symbol ***REMOVED*** getReferencedValueSymbol(reference);
        if (symbol) {
          return filter(getExportSymbolOfValueSymbolIfExported(symbol).declarations, (declaration) ***REMOVED***> {
            switch (declaration.kind) {
              case 261 /* VariableDeclaration */:
              case 170 /* Parameter */:
              case 209 /* BindingElement */:
              case 173 /* PropertyDeclaration */:
              case 304 /* PropertyAssignment */:
              case 305 /* ShorthandPropertyAssignment */:
              case 307 /* EnumMember */:
              case 211 /* ObjectLiteralExpression */:
              case 263 /* FunctionDeclaration */:
              case 219 /* FunctionExpression */:
              case 220 /* ArrowFunction */:
              case 264 /* ClassDeclaration */:
              case 232 /* ClassExpression */:
              case 267 /* EnumDeclaration */:
              case 175 /* MethodDeclaration */:
              case 178 /* GetAccessor */:
              case 179 /* SetAccessor */:
              case 268 /* ModuleDeclaration */:
                return true;
            }
            return false;
          });
        }
      }
    }
    return void 0;
  }
  function isLiteralConstDeclaration(node) {
    if (isDeclarationReadonly(node) || isVariableDeclaration(node) && isVarConstLike2(node)) {
      return isFreshLiteralType(getTypeOfSymbol(getSymbolOfDeclaration(node)));
    }
    return false;
  }
  function literalTypeToNode(type, enclosing, tracker) {
    const enumResult ***REMOVED*** type.flags & 1056 /* EnumLike */ ? nodeBuilder.symbolToExpression(
      type.symbol,
      111551 /* Value */,
      enclosing,
      /*flags*/
      void 0,
      /*internalFlags*/
      void 0,
      tracker
    ) : type ***REMOVED******REMOVED******REMOVED*** trueType ? factory.createTrue() : type ***REMOVED******REMOVED******REMOVED*** falseType && factory.createFalse();
    if (enumResult) return enumResult;
    const literalValue ***REMOVED*** type.value;
    return typeof literalValue ***REMOVED******REMOVED******REMOVED*** "object" ? factory.createBigIntLiteral(literalValue) : typeof literalValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory.createStringLiteral(literalValue) : literalValue < 0 ? factory.createPrefixUnaryExpression(41 /* MinusToken */, factory.createNumericLiteral(-literalValue)) : factory.createNumericLiteral(literalValue);
  }
  function createLiteralConstValue(node, tracker) {
    const type ***REMOVED*** getTypeOfSymbol(getSymbolOfDeclaration(node));
    return literalTypeToNode(type, node, tracker);
  }
  function getJsxFactoryEntity(location) {
    return location ? (getJsxNamespace(location), getSourceFileOfNode(location).localJsxFactory || _jsxFactoryEntity) : _jsxFactoryEntity;
  }
  function getJsxFragmentFactoryEntity(location) {
    if (location) {
      const file ***REMOVED*** getSourceFileOfNode(location);
      if (file) {
        if (file.localJsxFragmentFactory) {
          return file.localJsxFragmentFactory;
        }
        const jsxFragPragmas ***REMOVED*** file.pragmas.get("jsxfrag");
        const jsxFragPragma ***REMOVED*** isArray(jsxFragPragmas) ? jsxFragPragmas[0] : jsxFragPragmas;
        if (jsxFragPragma) {
          file.localJsxFragmentFactory ***REMOVED*** parseIsolatedEntityName(jsxFragPragma.arguments.factory, languageVersion);
          return file.localJsxFragmentFactory;
        }
      }
    }
    if (compilerOptions.jsxFragmentFactory) {
      return parseIsolatedEntityName(compilerOptions.jsxFragmentFactory, languageVersion);
    }
  }
  function getNonlocalEffectiveTypeAnnotationNode(node) {
    const direct ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    if (direct) {
      return direct;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
      const other ***REMOVED*** getAllAccessorDeclarationsForDeclaration(node.parent).getAccessor;
      if (other) {
        return getEffectiveReturnTypeNode(other);
      }
    }
    return void 0;
  }
  function createResolver() {
    return {
      getReferencedExportContainer,
      getReferencedImportDeclaration,
      getReferencedDeclarationWithCollidingName,
      isDeclarationWithCollidingName,
      isValueAliasDeclaration: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn);
        return node && canCollectSymbolAliasAccessabilityData ? isValueAliasDeclaration(node) : true;
      },
      hasGlobalName,
      isReferencedAliasDeclaration: (nodeIn, checkChildren) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn);
        return node && canCollectSymbolAliasAccessabilityData ? isReferencedAliasDeclaration(node, checkChildren) : true;
      },
      hasNodeCheckFlag: (nodeIn, flag) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn);
        if (!node) return false;
        return hasNodeCheckFlag(node, flag);
      },
      isTopLevelValueImportEqualsWithEntityName,
      isDeclarationVisible,
      isImplementationOfOverload,
      requiresAddingImplicitUndefined,
      isExpandoFunctionDeclaration,
      getPropertiesOfContainerFunction,
      createTypeOfDeclaration,
      createReturnTypeOfSignatureDeclaration,
      createTypeOfExpression,
      createLiteralConstValue,
      isSymbolAccessible,
      isEntityNameVisible,
      getConstantValue: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn, canHaveConstantValue);
        return node ? getConstantValue2(node) : void 0;
      },
      getEnumMemberValue: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn, isEnumMember);
        return node ? getEnumMemberValue(node) : void 0;
      },
      collectLinkedAliases,
      markLinkedReferences: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn);
        return node && markLinkedReferences(node, 0 /* Unspecified */);
      },
      getReferencedValueDeclaration,
      getReferencedValueDeclarations,
      getTypeReferenceSerializationKind,
      isOptionalParameter,
      isArgumentsLocalBinding,
      getExternalModuleFileFromDeclaration: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn, hasPossibleExternalModuleReference);
        return node && getExternalModuleFileFromDeclaration(node);
      },
      isLiteralConstDeclaration,
      isLateBound: (nodeIn) ***REMOVED***> {
        const node ***REMOVED*** getParseTreeNode(nodeIn, isDeclaration);
        const symbol ***REMOVED*** node && getSymbolOfDeclaration(node);
        return !!(symbol && getCheckFlags(symbol) & 4096 /* Late */);
      },
      getJsxFactoryEntity,
      getJsxFragmentFactoryEntity,
      isBindingCapturedByNode: (node, decl) ***REMOVED***> {
        const parseNode ***REMOVED*** getParseTreeNode(node);
        const parseDecl ***REMOVED*** getParseTreeNode(decl);
        return !!parseNode && !!parseDecl && (isVariableDeclaration(parseDecl) || isBindingElement(parseDecl)) && isBindingCapturedByNode(parseNode, parseDecl);
      },
      getDeclarationStatementsForSourceFile: (node, flags, internalFlags, tracker) ***REMOVED***> {
        const n ***REMOVED*** getParseTreeNode(node);
        Debug.assert(n && n.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */, "Non-sourcefile node passed into getDeclarationsForSourceFile");
        const sym ***REMOVED*** getSymbolOfDeclaration(node);
        if (!sym) {
          return !node.locals ? [] : nodeBuilder.symbolTableToDeclarationStatements(node.locals, node, flags, internalFlags, tracker);
        }
        resolveExternalModuleSymbol(sym);
        return !sym.exports ? [] : nodeBuilder.symbolTableToDeclarationStatements(sym.exports, node, flags, internalFlags, tracker);
      },
      isImportRequiredByAugmentation,
      isDefinitelyReferenceToGlobalSymbolObject,
      createLateBoundIndexSignatures: (cls, enclosing, flags, internalFlags, tracker) ***REMOVED***> {
        const sym ***REMOVED*** cls.symbol;
        const staticInfos ***REMOVED*** getIndexInfosOfType(getTypeOfSymbol(sym));
        const instanceIndexSymbol ***REMOVED*** getIndexSymbol(sym);
        const instanceInfos ***REMOVED*** instanceIndexSymbol && getIndexInfosOfIndexSymbol(instanceIndexSymbol, arrayFrom(getMembersOfSymbol(sym).values()));
        let result;
        for (const infoList of [staticInfos, instanceInfos]) {
          if (!length(infoList)) continue;
          result || (result ***REMOVED*** []);
          for (const info of infoList) {
            if (info.declaration) continue;
            if (info ***REMOVED******REMOVED******REMOVED*** anyBaseTypeIndexInfo) continue;
            if (info.components) {
              const allComponentComputedNamesSerializable ***REMOVED*** every(info.components, (e) ***REMOVED***> {
                var _a;
                return !!(e.name && isComputedPropertyName(e.name) && isEntityNameExpression(e.name.expression) && enclosing && ((_a ***REMOVED*** isEntityNameVisible(
                  e.name.expression,
                  enclosing,
                  /*shouldComputeAliasToMakeVisible*/
                  false
                )) ***REMOVED******REMOVED*** null ? void 0 : _a.accessibility) ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */);
              });
              if (allComponentComputedNamesSerializable) {
                const newComponents ***REMOVED*** filter(info.components, (e) ***REMOVED***> {
                  return !hasLateBindableName(e);
                });
                result.push(...map(newComponents, (e) ***REMOVED***> {
                  trackComputedName(e.name.expression);
                  const mods ***REMOVED*** infoList ***REMOVED******REMOVED******REMOVED*** staticInfos ? [factory.createModifier(126 /* StaticKeyword */)] : void 0;
                  return factory.createPropertyDeclaration(
                    append(mods, info.isReadonly ? factory.createModifier(148 /* ReadonlyKeyword */) : void 0),
                    e.name,
                    (isPropertySignature(e) || isPropertyDeclaration(e) || isMethodSignature(e) || isMethodDeclaration(e) || isGetAccessor(e) || isSetAccessor(e)) && e.questionToken ? factory.createToken(58 /* QuestionToken */) : void 0,
                    nodeBuilder.typeToTypeNode(getTypeOfSymbol(e.symbol), enclosing, flags, internalFlags, tracker),
                    /*initializer*/
                    void 0
                  );
                }));
                continue;
              }
            }
            const node ***REMOVED*** nodeBuilder.indexInfoToIndexSignatureDeclaration(info, enclosing, flags, internalFlags, tracker);
            if (node && infoList ***REMOVED******REMOVED******REMOVED*** staticInfos) {
              (node.modifiers || (node.modifiers ***REMOVED*** factory.createNodeArray())).unshift(factory.createModifier(126 /* StaticKeyword */));
            }
            if (node) {
              result.push(node);
            }
          }
        }
        return result;
        function trackComputedName(accessExpression) {
          if (!tracker.trackSymbol) return;
          const firstIdentifier ***REMOVED*** getFirstIdentifier(accessExpression);
          const name ***REMOVED*** resolveName(
            firstIdentifier,
            firstIdentifier.escapedText,
            111551 /* Value */ | 1048576 /* ExportValue */,
            /*nameNotFoundMessage*/
            void 0,
            /*isUse*/
            true
          );
          if (name) {
            tracker.trackSymbol(name, enclosing, 111551 /* Value */);
          }
        }
      },
      symbolToDeclarations: (symbol, meaning, flags, maximumLength, verbosityLevel, out) ***REMOVED***> {
        return nodeBuilder.symbolToDeclarations(symbol, meaning, flags, maximumLength, verbosityLevel, out);
      }
    };
    function isImportRequiredByAugmentation(node) {
      const file ***REMOVED*** getSourceFileOfNode(node);
      if (!file.symbol) return false;
      const importTarget ***REMOVED*** getExternalModuleFileFromDeclaration(node);
      if (!importTarget) return false;
      if (importTarget ***REMOVED******REMOVED******REMOVED*** file) return false;
      const exports2 ***REMOVED*** getExportsOfModule(file.symbol);
      for (const s of arrayFrom(exports2.values())) {
        if (s.mergeId) {
          const merged ***REMOVED*** getMergedSymbol(s);
          if (merged.declarations) {
            for (const d of merged.declarations) {
              const declFile ***REMOVED*** getSourceFileOfNode(d);
              if (declFile ***REMOVED******REMOVED******REMOVED*** importTarget) {
                return true;
              }
            }
          }
        }
      }
      return false;
    }
  }
  function getExternalModuleFileFromDeclaration(declaration) {
    const specifier ***REMOVED*** declaration.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ ? tryCast(declaration.name, isStringLiteral) : getExternalModuleName(declaration);
    const moduleSymbol ***REMOVED*** resolveExternalModuleNameWorker(
      specifier,
      specifier,
      /*moduleNotFoundError*/
      void 0
    );
    if (!moduleSymbol) {
      return void 0;
    }
    return getDeclarationOfKind(moduleSymbol, 308 /* SourceFile */);
  }
  function initializeTypeChecker() {
    for (const file of host.getSourceFiles()) {
      bindSourceFile(file, compilerOptions);
    }
    amalgamatedDuplicates ***REMOVED*** /* @__PURE__ */ new Map();
    let augmentations;
    for (const file of host.getSourceFiles()) {
      if (file.redirectInfo) {
        continue;
      }
      if (!isExternalOrCommonJsModule(file)) {
        const fileGlobalThisSymbol ***REMOVED*** file.locals.get("globalThis");
        if (fileGlobalThisSymbol ***REMOVED******REMOVED*** null ? void 0 : fileGlobalThisSymbol.declarations) {
          for (const declaration of fileGlobalThisSymbol.declarations) {
            diagnostics.add(createDiagnosticForNode(declaration, Diagnostics.Declaration_name_conflicts_with_built_in_global_identifier_0, "globalThis"));
          }
        }
        mergeSymbolTable(globals, file.locals);
      }
      if (file.jsGlobalAugmentations) {
        mergeSymbolTable(globals, file.jsGlobalAugmentations);
      }
      if (file.patternAmbientModules && file.patternAmbientModules.length) {
        patternAmbientModules ***REMOVED*** concatenate(patternAmbientModules, file.patternAmbientModules);
      }
      if (file.moduleAugmentations.length) {
        (augmentations || (augmentations ***REMOVED*** [])).push(file.moduleAugmentations);
      }
      if (file.symbol && file.symbol.globalExports) {
        const source ***REMOVED*** file.symbol.globalExports;
        source.forEach((sourceSymbol, id) ***REMOVED***> {
          if (!globals.has(id)) {
            globals.set(id, sourceSymbol);
          }
        });
      }
    }
    if (augmentations) {
      for (const list of augmentations) {
        for (const augmentation of list) {
          if (!isGlobalScopeAugmentation(augmentation.parent)) continue;
          mergeModuleAugmentation(augmentation);
        }
      }
    }
    addUndefinedToGlobalsOrErrorOnRedeclaration();
    getSymbolLinks(undefinedSymbol).type ***REMOVED*** undefinedWideningType;
    getSymbolLinks(argumentsSymbol).type ***REMOVED*** getGlobalType(
      "IArguments",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    getSymbolLinks(unknownSymbol).type ***REMOVED*** errorType;
    getSymbolLinks(globalThisSymbol).type ***REMOVED*** createObjectType(16 /* Anonymous */, globalThisSymbol);
    globalArrayType ***REMOVED*** getGlobalType(
      "Array",
      /*arity*/
      1,
      /*reportErrors*/
      true
    );
    globalObjectType ***REMOVED*** getGlobalType(
      "Object",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    globalFunctionType ***REMOVED*** getGlobalType(
      "Function",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    globalCallableFunctionType ***REMOVED*** strictBindCallApply && getGlobalType(
      "CallableFunction",
      /*arity*/
      0,
      /*reportErrors*/
      true
    ) || globalFunctionType;
    globalNewableFunctionType ***REMOVED*** strictBindCallApply && getGlobalType(
      "NewableFunction",
      /*arity*/
      0,
      /*reportErrors*/
      true
    ) || globalFunctionType;
    globalStringType ***REMOVED*** getGlobalType(
      "String",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    globalNumberType ***REMOVED*** getGlobalType(
      "Number",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    globalBooleanType ***REMOVED*** getGlobalType(
      "Boolean",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    globalRegExpType ***REMOVED*** getGlobalType(
      "RegExp",
      /*arity*/
      0,
      /*reportErrors*/
      true
    );
    anyArrayType ***REMOVED*** createArrayType(anyType);
    autoArrayType ***REMOVED*** createArrayType(autoType);
    if (autoArrayType ***REMOVED******REMOVED******REMOVED*** emptyObjectType) {
      autoArrayType ***REMOVED*** createAnonymousType(
        /*symbol*/
        void 0,
        emptySymbols,
        emptyArray,
        emptyArray,
        emptyArray
      );
    }
    globalReadonlyArrayType ***REMOVED*** getGlobalTypeOrUndefined(
      "ReadonlyArray",
      /*arity*/
      1
    ) || globalArrayType;
    anyReadonlyArrayType ***REMOVED*** globalReadonlyArrayType ? createTypeFromGenericGlobalType(globalReadonlyArrayType, [anyType]) : anyArrayType;
    globalThisType ***REMOVED*** getGlobalTypeOrUndefined(
      "ThisType",
      /*arity*/
      1
    );
    if (augmentations) {
      for (const list of augmentations) {
        for (const augmentation of list) {
          if (isGlobalScopeAugmentation(augmentation.parent)) continue;
          mergeModuleAugmentation(augmentation);
        }
      }
    }
    amalgamatedDuplicates.forEach(({ firstFile, secondFile, conflictingSymbols }) ***REMOVED***> {
      if (conflictingSymbols.size < 8) {
        conflictingSymbols.forEach(({ isBlockScoped, firstFileLocations, secondFileLocations }, symbolName2) ***REMOVED***> {
          const message ***REMOVED*** isBlockScoped ? Diagnostics.Cannot_redeclare_block_scoped_variable_0 : Diagnostics.Duplicate_identifier_0;
          for (const node of firstFileLocations) {
            addDuplicateDeclarationError(node, message, symbolName2, secondFileLocations);
          }
          for (const node of secondFileLocations) {
            addDuplicateDeclarationError(node, message, symbolName2, firstFileLocations);
          }
        });
      } else {
        const list ***REMOVED*** arrayFrom(conflictingSymbols.keys()).join(", ");
        diagnostics.add(addRelatedInfo(
          createDiagnosticForNode(firstFile, Diagnostics.Definitions_of_the_following_identifiers_conflict_with_those_in_another_file_Colon_0, list),
          createDiagnosticForNode(secondFile, Diagnostics.Conflicts_are_in_this_file)
        ));
        diagnostics.add(addRelatedInfo(
          createDiagnosticForNode(secondFile, Diagnostics.Definitions_of_the_following_identifiers_conflict_with_those_in_another_file_Colon_0, list),
          createDiagnosticForNode(firstFile, Diagnostics.Conflicts_are_in_this_file)
        ));
      }
    });
    amalgamatedDuplicates ***REMOVED*** void 0;
  }
  function checkExternalEmitHelpers(location, helpers) {
    if (compilerOptions.importHelpers) {
      const sourceFile ***REMOVED*** getSourceFileOfNode(location);
      if (isEffectiveExternalModule(sourceFile, compilerOptions) && !(location.flags & 33554432 /* Ambient */)) {
        const helpersModule ***REMOVED*** resolveHelpersModule(sourceFile, location);
        if (helpersModule !***REMOVED******REMOVED*** unknownSymbol) {
          const links ***REMOVED*** getSymbolLinks(helpersModule);
          links.requestedExternalEmitHelpers ?? (links.requestedExternalEmitHelpers ***REMOVED*** 0);
          if ((links.requestedExternalEmitHelpers & helpers) !***REMOVED******REMOVED*** helpers) {
            const uncheckedHelpers ***REMOVED*** helpers & ~links.requestedExternalEmitHelpers;
            for (let helper ***REMOVED*** 1 /* FirstEmitHelper */; helper <***REMOVED*** 16777216 /* LastEmitHelper */; helper <<***REMOVED*** 1) {
              if (uncheckedHelpers & helper) {
                for (const name of getHelperNames(helper)) {
                  const symbol ***REMOVED*** resolveSymbol(getSymbol(getExportsOfModule(helpersModule), escapeLeadingUnderscores(name), 111551 /* Value */));
                  if (!symbol) {
                    error(location, Diagnostics.This_syntax_requires_an_imported_helper_named_1_which_does_not_exist_in_0_Consider_upgrading_your_version_of_0, externalHelpersModuleNameText, name);
                  } else if (helper & 524288 /* ClassPrivateFieldGet */) {
                    if (!some(getSignaturesOfSymbol(symbol), (signature) ***REMOVED***> getParameterCount(signature) > 3)) {
                      error(location, Diagnostics.This_syntax_requires_an_imported_helper_named_1_with_2_parameters_which_is_not_compatible_with_the_one_in_0_Consider_upgrading_your_version_of_0, externalHelpersModuleNameText, name, 4);
                    }
                  } else if (helper & 1048576 /* ClassPrivateFieldSet */) {
                    if (!some(getSignaturesOfSymbol(symbol), (signature) ***REMOVED***> getParameterCount(signature) > 4)) {
                      error(location, Diagnostics.This_syntax_requires_an_imported_helper_named_1_with_2_parameters_which_is_not_compatible_with_the_one_in_0_Consider_upgrading_your_version_of_0, externalHelpersModuleNameText, name, 5);
                    }
                  } else if (helper & 1024 /* SpreadArray */) {
                    if (!some(getSignaturesOfSymbol(symbol), (signature) ***REMOVED***> getParameterCount(signature) > 2)) {
                      error(location, Diagnostics.This_syntax_requires_an_imported_helper_named_1_with_2_parameters_which_is_not_compatible_with_the_one_in_0_Consider_upgrading_your_version_of_0, externalHelpersModuleNameText, name, 3);
                    }
                  }
                }
              }
            }
          }
          links.requestedExternalEmitHelpers |***REMOVED*** helpers;
        }
      }
    }
  }
  function getHelperNames(helper) {
    switch (helper) {
      case 1 /* Extends */:
        return ["__extends"];
      case 2 /* Assign */:
        return ["__assign"];
      case 4 /* Rest */:
        return ["__rest"];
      case 8 /* Decorate */:
        return legacyDecorators ? ["__decorate"] : ["__esDecorate", "__runInitializers"];
      case 16 /* Metadata */:
        return ["__metadata"];
      case 32 /* Param */:
        return ["__param"];
      case 64 /* Awaiter */:
        return ["__awaiter"];
      case 128 /* Generator */:
        return ["__generator"];
      case 256 /* Values */:
        return ["__values"];
      case 512 /* Read */:
        return ["__read"];
      case 1024 /* SpreadArray */:
        return ["__spreadArray"];
      case 2048 /* Await */:
        return ["__await"];
      case 4096 /* AsyncGenerator */:
        return ["__asyncGenerator"];
      case 8192 /* AsyncDelegator */:
        return ["__asyncDelegator"];
      case 16384 /* AsyncValues */:
        return ["__asyncValues"];
      case 32768 /* ExportStar */:
        return ["__exportStar"];
      case 65536 /* ImportStar */:
        return ["__importStar"];
      case 131072 /* ImportDefault */:
        return ["__importDefault"];
      case 262144 /* MakeTemplateObject */:
        return ["__makeTemplateObject"];
      case 524288 /* ClassPrivateFieldGet */:
        return ["__classPrivateFieldGet"];
      case 1048576 /* ClassPrivateFieldSet */:
        return ["__classPrivateFieldSet"];
      case 2097152 /* ClassPrivateFieldIn */:
        return ["__classPrivateFieldIn"];
      case 4194304 /* SetFunctionName */:
        return ["__setFunctionName"];
      case 8388608 /* PropKey */:
        return ["__propKey"];
      case 16777216 /* AddDisposableResourceAndDisposeResources */:
        return ["__addDisposableResource", "__disposeResources"];
      case 33554432 /* RewriteRelativeImportExtension */:
        return ["__rewriteRelativeImportExtension"];
      default:
        return Debug.fail("Unrecognized helper");
    }
  }
  function resolveHelpersModule(file, errorNode) {
    const links ***REMOVED*** getNodeLinks(file);
    if (!links.externalHelpersModule) {
      links.externalHelpersModule ***REMOVED*** resolveExternalModule(getImportHelpersImportSpecifier(file), externalHelpersModuleNameText, Diagnostics.This_syntax_requires_an_imported_helper_but_module_0_cannot_be_found, errorNode) || unknownSymbol;
    }
    return links.externalHelpersModule;
  }
  function checkGrammarModifiers(node) {
    var _a;
    const quickResult ***REMOVED*** reportObviousDecoratorErrors(node) || reportObviousModifierErrors(node);
    if (quickResult !***REMOVED******REMOVED*** void 0) {
      return quickResult;
    }
    if (isParameter(node) && parameterIsThisKeyword(node)) {
      return grammarErrorOnFirstToken(node, Diagnostics.Neither_decorators_nor_modifiers_may_be_applied_to_this_parameters);
    }
    const blockScopeKind ***REMOVED*** isVariableStatement(node) ? node.declarationList.flags & 7 /* BlockScoped */ : 0 /* None */;
    let lastStatic, lastDeclare, lastAsync, lastOverride, firstDecorator;
    let flags ***REMOVED*** 0 /* None */;
    let sawExportBeforeDecorators ***REMOVED*** false;
    let hasLeadingDecorators ***REMOVED*** false;
    for (const modifier of node.modifiers) {
      if (isDecorator(modifier)) {
        if (!nodeCanBeDecorated(legacyDecorators, node, node.parent, node.parent.parent)) {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && !nodeIsPresent(node.body)) {
            return grammarErrorOnFirstToken(node, Diagnostics.A_decorator_can_only_decorate_a_method_implementation_not_an_overload);
          } else {
            return grammarErrorOnFirstToken(node, Diagnostics.Decorators_are_not_valid_here);
          }
        } else if (legacyDecorators && (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */)) {
          const accessors ***REMOVED*** getAllAccessorDeclarationsForDeclaration(node);
          if (hasDecorators(accessors.firstAccessor) && node ***REMOVED******REMOVED******REMOVED*** accessors.secondAccessor) {
            return grammarErrorOnFirstToken(node, Diagnostics.Decorators_cannot_be_applied_to_multiple_get_Slashset_accessors_of_the_same_name);
          }
        }
        if (flags & ~(2080 /* ExportDefault */ | 32768 /* Decorator */)) {
          return grammarErrorOnNode(modifier, Diagnostics.Decorators_are_not_valid_here);
        }
        if (hasLeadingDecorators && flags & 98303 /* Modifier */) {
          Debug.assertIsDefined(firstDecorator);
          const sourceFile ***REMOVED*** getSourceFileOfNode(modifier);
          if (!hasParseDiagnostics(sourceFile)) {
            addRelatedInfo(
              error(modifier, Diagnostics.Decorators_may_not_appear_after_export_or_export_default_if_they_also_appear_before_export),
              createDiagnosticForNode(firstDecorator, Diagnostics.Decorator_used_before_export_here)
            );
            return true;
          }
          return false;
        }
        flags |***REMOVED*** 32768 /* Decorator */;
        if (!(flags & 98303 /* Modifier */)) {
          hasLeadingDecorators ***REMOVED*** true;
        } else if (flags & 32 /* Export */) {
          sawExportBeforeDecorators ***REMOVED*** true;
        }
        firstDecorator ?? (firstDecorator ***REMOVED*** modifier);
      } else {
        if (modifier.kind !***REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 174 /* MethodSignature */) {
            return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_type_member, tokenToString(modifier.kind));
          }
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 182 /* IndexSignature */ && (modifier.kind !***REMOVED******REMOVED*** 126 /* StaticKeyword */ || !isClassLike(node.parent))) {
            return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_an_index_signature, tokenToString(modifier.kind));
          }
        }
        if (modifier.kind !***REMOVED******REMOVED*** 103 /* InKeyword */ && modifier.kind !***REMOVED******REMOVED*** 147 /* OutKeyword */ && modifier.kind !***REMOVED******REMOVED*** 87 /* ConstKeyword */) {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */) {
            return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_type_parameter, tokenToString(modifier.kind));
          }
        }
        switch (modifier.kind) {
          case 87 /* ConstKeyword */: {
            if (node.kind !***REMOVED******REMOVED*** 267 /* EnumDeclaration */ && node.kind !***REMOVED******REMOVED*** 169 /* TypeParameter */) {
              return grammarErrorOnNode(node, Diagnostics.A_class_member_cannot_have_the_0_keyword, tokenToString(87 /* ConstKeyword */));
            }
            const parent ***REMOVED*** isJSDocTemplateTag(node.parent) && getEffectiveJSDocHost(node.parent) || node.parent;
            if (node.kind ***REMOVED******REMOVED******REMOVED*** 169 /* TypeParameter */ && !(isFunctionLikeDeclaration(parent) || isClassLike(parent) || isFunctionTypeNode(parent) || isConstructorTypeNode(parent) || isCallSignatureDeclaration(parent) || isConstructSignatureDeclaration(parent) || isMethodSignature(parent))) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_can_only_appear_on_a_type_parameter_of_a_function_method_or_class, tokenToString(modifier.kind));
            }
            break;
          }
          case 164 /* OverrideKeyword */:
            if (flags & 16 /* Override */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "override");
            } else if (flags & 128 /* Ambient */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "override", "declare");
            } else if (flags & 8 /* Readonly */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "override", "readonly");
            } else if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "override", "accessor");
            } else if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "override", "async");
            }
            flags |***REMOVED*** 16 /* Override */;
            lastOverride ***REMOVED*** modifier;
            break;
          case 125 /* PublicKeyword */:
          case 124 /* ProtectedKeyword */:
          case 123 /* PrivateKeyword */:
            const text ***REMOVED*** visibilityToString(modifierToFlag(modifier.kind));
            if (flags & 7 /* AccessibilityModifier */) {
              return grammarErrorOnNode(modifier, Diagnostics.Accessibility_modifier_already_seen);
            } else if (flags & 16 /* Override */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "override");
            } else if (flags & 256 /* Static */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "static");
            } else if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "accessor");
            } else if (flags & 8 /* Readonly */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "readonly");
            } else if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "async");
            } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_module_or_namespace_element, text);
            } else if (flags & 64 /* Abstract */) {
              if (modifier.kind ***REMOVED******REMOVED******REMOVED*** 123 /* PrivateKeyword */) {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, text, "abstract");
              } else {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, text, "abstract");
              }
            } else if (isPrivateIdentifierClassElementDeclaration(node)) {
              return grammarErrorOnNode(modifier, Diagnostics.An_accessibility_modifier_cannot_be_used_with_a_private_identifier);
            }
            flags |***REMOVED*** modifierToFlag(modifier.kind);
            break;
          case 126 /* StaticKeyword */:
            if (flags & 256 /* Static */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "static");
            } else if (flags & 8 /* Readonly */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "static", "readonly");
            } else if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "static", "async");
            } else if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "static", "accessor");
            } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_module_or_namespace_element, "static");
            } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_parameter, "static");
            } else if (flags & 64 /* Abstract */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "static", "abstract");
            } else if (flags & 16 /* Override */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "static", "override");
            }
            flags |***REMOVED*** 256 /* Static */;
            lastStatic ***REMOVED*** modifier;
            break;
          case 129 /* AccessorKeyword */:
            if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "accessor");
            } else if (flags & 8 /* Readonly */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "accessor", "readonly");
            } else if (flags & 128 /* Ambient */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "accessor", "declare");
            } else if (node.kind !***REMOVED******REMOVED*** 173 /* PropertyDeclaration */) {
              return grammarErrorOnNode(modifier, Diagnostics.accessor_modifier_can_only_appear_on_a_property_declaration);
            }
            flags |***REMOVED*** 512 /* Accessor */;
            break;
          case 148 /* ReadonlyKeyword */:
            if (flags & 8 /* Readonly */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "readonly");
            } else if (node.kind !***REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && node.kind !***REMOVED******REMOVED*** 172 /* PropertySignature */ && node.kind !***REMOVED******REMOVED*** 182 /* IndexSignature */ && node.kind !***REMOVED******REMOVED*** 170 /* Parameter */) {
              return grammarErrorOnNode(modifier, Diagnostics.readonly_modifier_can_only_appear_on_a_property_declaration_or_index_signature);
            } else if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "readonly", "accessor");
            }
            flags |***REMOVED*** 8 /* Readonly */;
            break;
          case 95 /* ExportKeyword */:
            if (compilerOptions.verbatimModuleSyntax && !(node.flags & 33554432 /* Ambient */) && node.kind !***REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ && node.kind !***REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ && // ModuleDeclaration needs to be checked that it is uninstantiated later
            node.kind !***REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
              return grammarErrorOnNode(modifier, Diagnostics.A_top_level_export_modifier_cannot_be_used_on_value_declarations_in_a_CommonJS_module_when_verbatimModuleSyntax_is_enabled);
            }
            if (flags & 32 /* Export */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "export");
            } else if (flags & 128 /* Ambient */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "export", "declare");
            } else if (flags & 64 /* Abstract */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "export", "abstract");
            } else if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "export", "async");
            } else if (isClassLike(node.parent)) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_class_elements_of_this_kind, "export");
            } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_parameter, "export");
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_using_declaration, "export");
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_an_await_using_declaration, "export");
            }
            flags |***REMOVED*** 32 /* Export */;
            break;
          case 90 /* DefaultKeyword */:
            const container ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? node.parent : node.parent.parent;
            if (container.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && !isAmbientModule(container)) {
              return grammarErrorOnNode(modifier, Diagnostics.A_default_export_can_only_be_used_in_an_ECMAScript_style_module);
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_using_declaration, "default");
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_an_await_using_declaration, "default");
            } else if (!(flags & 32 /* Export */)) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "export", "default");
            } else if (sawExportBeforeDecorators) {
              return grammarErrorOnNode(firstDecorator, Diagnostics.Decorators_are_not_valid_here);
            }
            flags |***REMOVED*** 2048 /* Default */;
            break;
          case 138 /* DeclareKeyword */:
            if (flags & 128 /* Ambient */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "declare");
            } else if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_in_an_ambient_context, "async");
            } else if (flags & 16 /* Override */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_in_an_ambient_context, "override");
            } else if (isClassLike(node.parent) && !isPropertyDeclaration(node)) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_class_elements_of_this_kind, "declare");
            } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_parameter, "declare");
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_using_declaration, "declare");
            } else if (blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_an_await_using_declaration, "declare");
            } else if (node.parent.flags & 33554432 /* Ambient */ && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */) {
              return grammarErrorOnNode(modifier, Diagnostics.A_declare_modifier_cannot_be_used_in_an_already_ambient_context);
            } else if (isPrivateIdentifierClassElementDeclaration(node)) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_a_private_identifier, "declare");
            } else if (flags & 512 /* Accessor */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "declare", "accessor");
            }
            flags |***REMOVED*** 128 /* Ambient */;
            lastDeclare ***REMOVED*** modifier;
            break;
          case 128 /* AbstractKeyword */:
            if (flags & 64 /* Abstract */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "abstract");
            }
            if (node.kind !***REMOVED******REMOVED*** 264 /* ClassDeclaration */ && node.kind !***REMOVED******REMOVED*** 186 /* ConstructorType */) {
              if (node.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ && node.kind !***REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && node.kind !***REMOVED******REMOVED*** 178 /* GetAccessor */ && node.kind !***REMOVED******REMOVED*** 179 /* SetAccessor */) {
                return grammarErrorOnNode(modifier, Diagnostics.abstract_modifier_can_only_appear_on_a_class_method_or_property_declaration);
              }
              if (!(node.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ && hasSyntacticModifier(node.parent, 64 /* Abstract */))) {
                const message ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ ? Diagnostics.Abstract_properties_can_only_appear_within_an_abstract_class : Diagnostics.Abstract_methods_can_only_appear_within_an_abstract_class;
                return grammarErrorOnNode(modifier, message);
              }
              if (flags & 256 /* Static */) {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "static", "abstract");
              }
              if (flags & 2 /* Private */) {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "private", "abstract");
              }
              if (flags & 1024 /* Async */ && lastAsync) {
                return grammarErrorOnNode(lastAsync, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "async", "abstract");
              }
              if (flags & 16 /* Override */) {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "abstract", "override");
              }
              if (flags & 512 /* Accessor */) {
                return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "abstract", "accessor");
              }
            }
            if (isNamedDeclaration(node) && node.name.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_a_private_identifier, "abstract");
            }
            flags |***REMOVED*** 64 /* Abstract */;
            break;
          case 134 /* AsyncKeyword */:
            if (flags & 1024 /* Async */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, "async");
            } else if (flags & 128 /* Ambient */ || node.parent.flags & 33554432 /* Ambient */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_in_an_ambient_context, "async");
            } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_appear_on_a_parameter, "async");
            }
            if (flags & 64 /* Abstract */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_cannot_be_used_with_1_modifier, "async", "abstract");
            }
            flags |***REMOVED*** 1024 /* Async */;
            lastAsync ***REMOVED*** modifier;
            break;
          case 103 /* InKeyword */:
          case 147 /* OutKeyword */: {
            const inOutFlag ***REMOVED*** modifier.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ ? 8192 /* In */ : 16384 /* Out */;
            const inOutText ***REMOVED*** modifier.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ ? "in" : "out";
            const parent ***REMOVED*** isJSDocTemplateTag(node.parent) && (getEffectiveJSDocHost(node.parent) || find((_a ***REMOVED*** getJSDocRoot(node.parent)) ***REMOVED******REMOVED*** null ? void 0 : _a.tags, isJSDocTypedefTag)) || node.parent;
            if (node.kind !***REMOVED******REMOVED*** 169 /* TypeParameter */ || parent && !(isInterfaceDeclaration(parent) || isClassLike(parent) || isTypeAliasDeclaration(parent) || isJSDocTypedefTag(parent))) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_can_only_appear_on_a_type_parameter_of_a_class_interface_or_type_alias, inOutText);
            }
            if (flags & inOutFlag) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_already_seen, inOutText);
            }
            if (inOutFlag & 8192 /* In */ && flags & 16384 /* Out */) {
              return grammarErrorOnNode(modifier, Diagnostics._0_modifier_must_precede_1_modifier, "in", "out");
            }
            flags |***REMOVED*** inOutFlag;
            break;
          }
        }
      }
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */) {
      if (flags & 256 /* Static */) {
        return grammarErrorOnNode(lastStatic, Diagnostics._0_modifier_cannot_appear_on_a_constructor_declaration, "static");
      }
      if (flags & 16 /* Override */) {
        return grammarErrorOnNode(lastOverride, Diagnostics._0_modifier_cannot_appear_on_a_constructor_declaration, "override");
      }
      if (flags & 1024 /* Async */) {
        return grammarErrorOnNode(lastAsync, Diagnostics._0_modifier_cannot_appear_on_a_constructor_declaration, "async");
      }
      return false;
    } else if ((node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */) && flags & 128 /* Ambient */) {
      return grammarErrorOnNode(lastDeclare, Diagnostics.A_0_modifier_cannot_be_used_with_an_import_declaration, "declare");
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && flags & 31 /* ParameterPropertyModifier */ && isBindingPattern(node.name)) {
      return grammarErrorOnNode(node, Diagnostics.A_parameter_property_may_not_be_declared_using_a_binding_pattern);
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && flags & 31 /* ParameterPropertyModifier */ && node.dotDotDotToken) {
      return grammarErrorOnNode(node, Diagnostics.A_parameter_property_cannot_be_declared_using_a_rest_parameter);
    }
    if (flags & 1024 /* Async */) {
      return checkGrammarAsyncModifier(node, lastAsync);
    }
    return false;
  }
  function reportObviousModifierErrors(node) {
    if (!node.modifiers) return false;
    const modifier ***REMOVED*** findFirstIllegalModifier(node);
    return modifier && grammarErrorOnFirstToken(modifier, Diagnostics.Modifiers_cannot_appear_here);
  }
  function findFirstModifierExcept(node, allowedModifier) {
    const modifier ***REMOVED*** find(node.modifiers, isModifier);
    return modifier && modifier.kind !***REMOVED******REMOVED*** allowedModifier ? modifier : void 0;
  }
  function findFirstIllegalModifier(node) {
    switch (node.kind) {
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 177 /* Constructor */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 182 /* IndexSignature */:
      case 268 /* ModuleDeclaration */:
      case 273 /* ImportDeclaration */:
      case 272 /* ImportEqualsDeclaration */:
      case 279 /* ExportDeclaration */:
      case 278 /* ExportAssignment */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 170 /* Parameter */:
      case 169 /* TypeParameter */:
        return void 0;
      case 176 /* ClassStaticBlockDeclaration */:
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
      case 271 /* NamespaceExportDeclaration */:
      case 283 /* MissingDeclaration */:
        return find(node.modifiers, isModifier);
      default:
        if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
          return void 0;
        }
        switch (node.kind) {
          case 263 /* FunctionDeclaration */:
            return findFirstModifierExcept(node, 134 /* AsyncKeyword */);
          case 264 /* ClassDeclaration */:
          case 186 /* ConstructorType */:
            return findFirstModifierExcept(node, 128 /* AbstractKeyword */);
          case 232 /* ClassExpression */:
          case 265 /* InterfaceDeclaration */:
          case 266 /* TypeAliasDeclaration */:
            return find(node.modifiers, isModifier);
          case 244 /* VariableStatement */:
            return node.declarationList.flags & 4 /* Using */ ? findFirstModifierExcept(node, 135 /* AwaitKeyword */) : find(node.modifiers, isModifier);
          case 267 /* EnumDeclaration */:
            return findFirstModifierExcept(node, 87 /* ConstKeyword */);
          default:
            Debug.assertNever(node);
        }
    }
  }
  function reportObviousDecoratorErrors(node) {
    const decorator ***REMOVED*** findFirstIllegalDecorator(node);
    return decorator && grammarErrorOnFirstToken(decorator, Diagnostics.Decorators_are_not_valid_here);
  }
  function findFirstIllegalDecorator(node) {
    return canHaveIllegalDecorators(node) ? find(node.modifiers, isDecorator) : void 0;
  }
  function checkGrammarAsyncModifier(node, asyncModifier) {
    switch (node.kind) {
      case 175 /* MethodDeclaration */:
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
        return false;
    }
    return grammarErrorOnNode(asyncModifier, Diagnostics._0_modifier_cannot_be_used_here, "async");
  }
  function checkGrammarForDisallowedTrailingComma(list, diag2 ***REMOVED*** Diagnostics.Trailing_comma_not_allowed) {
    if (list && list.hasTrailingComma) {
      return grammarErrorAtPos(list[0], list.end - ",".length, ",".length, diag2);
    }
    return false;
  }
  function checkGrammarTypeParameterList(typeParameters, file) {
    if (typeParameters && typeParameters.length ***REMOVED******REMOVED******REMOVED*** 0) {
      const start ***REMOVED*** typeParameters.pos - "<".length;
      const end ***REMOVED*** skipTrivia(file.text, typeParameters.end) + ">".length;
      return grammarErrorAtPos(file, start, end - start, Diagnostics.Type_parameter_list_cannot_be_empty);
    }
    return false;
  }
  function checkGrammarParameterList(parameters) {
    let seenOptionalParameter ***REMOVED*** false;
    const parameterCount ***REMOVED*** parameters.length;
    for (let i ***REMOVED*** 0; i < parameterCount; i++) {
      const parameter ***REMOVED*** parameters[i];
      if (parameter.dotDotDotToken) {
        if (i !***REMOVED******REMOVED*** parameterCount - 1) {
          return grammarErrorOnNode(parameter.dotDotDotToken, Diagnostics.A_rest_parameter_must_be_last_in_a_parameter_list);
        }
        if (!(parameter.flags & 33554432 /* Ambient */)) {
          checkGrammarForDisallowedTrailingComma(parameters, Diagnostics.A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma);
        }
        if (parameter.questionToken) {
          return grammarErrorOnNode(parameter.questionToken, Diagnostics.A_rest_parameter_cannot_be_optional);
        }
        if (parameter.initializer) {
          return grammarErrorOnNode(parameter.name, Diagnostics.A_rest_parameter_cannot_have_an_initializer);
        }
      } else if (hasEffectiveQuestionToken(parameter)) {
        seenOptionalParameter ***REMOVED*** true;
        if (parameter.questionToken && parameter.initializer) {
          return grammarErrorOnNode(parameter.name, Diagnostics.Parameter_cannot_have_question_mark_and_initializer);
        }
      } else if (seenOptionalParameter && !parameter.initializer) {
        return grammarErrorOnNode(parameter.name, Diagnostics.A_required_parameter_cannot_follow_an_optional_parameter);
      }
    }
  }
  function getNonSimpleParameters(parameters) {
    return filter(parameters, (parameter) ***REMOVED***> !!parameter.initializer || isBindingPattern(parameter.name) || isRestParameter(parameter));
  }
  function checkGrammarForUseStrictSimpleParameterList(node) {
    if (languageVersion >***REMOVED*** 3 /* ES2016 */) {
      const useStrictDirective ***REMOVED*** node.body && isBlock(node.body) && findUseStrictPrologue(node.body.statements);
      if (useStrictDirective) {
        const nonSimpleParameters ***REMOVED*** getNonSimpleParameters(node.parameters);
        if (length(nonSimpleParameters)) {
          forEach(nonSimpleParameters, (parameter) ***REMOVED***> {
            addRelatedInfo(
              error(parameter, Diagnostics.This_parameter_is_not_allowed_with_use_strict_directive),
              createDiagnosticForNode(useStrictDirective, Diagnostics.use_strict_directive_used_here)
            );
          });
          const diagnostics2 ***REMOVED*** nonSimpleParameters.map((parameter, index) ***REMOVED***> index ***REMOVED******REMOVED******REMOVED*** 0 ? createDiagnosticForNode(parameter, Diagnostics.Non_simple_parameter_declared_here) : createDiagnosticForNode(parameter, Diagnostics.and_here));
          addRelatedInfo(error(useStrictDirective, Diagnostics.use_strict_directive_cannot_be_used_with_non_simple_parameter_list), ...diagnostics2);
          return true;
        }
      }
    }
    return false;
  }
  function checkGrammarFunctionLikeDeclaration(node) {
    const file ***REMOVED*** getSourceFileOfNode(node);
    return checkGrammarModifiers(node) || checkGrammarTypeParameterList(node.typeParameters, file) || checkGrammarParameterList(node.parameters) || checkGrammarArrowFunction(node, file) || isFunctionLikeDeclaration(node) && checkGrammarForUseStrictSimpleParameterList(node);
  }
  function checkGrammarClassLikeDeclaration(node) {
    const file ***REMOVED*** getSourceFileOfNode(node);
    return checkGrammarClassDeclarationHeritageClauses(node) || checkGrammarTypeParameterList(node.typeParameters, file);
  }
  function checkGrammarArrowFunction(node, file) {
    if (!isArrowFunction(node)) {
      return false;
    }
    if (node.typeParameters && !(length(node.typeParameters) > 1 || node.typeParameters.hasTrailingComma || node.typeParameters[0].constraint)) {
      if (file && fileExtensionIsOneOf(file.fileName, [".mts" /* Mts */, ".cts" /* Cts */])) {
        grammarErrorOnNode(node.typeParameters[0], Diagnostics.This_syntax_is_reserved_in_files_with_the_mts_or_cts_extension_Add_a_trailing_comma_or_explicit_constraint);
      }
    }
    const { equalsGreaterThanToken } ***REMOVED*** node;
    const startLine ***REMOVED*** getLineAndCharacterOfPosition(file, equalsGreaterThanToken.pos).line;
    const endLine ***REMOVED*** getLineAndCharacterOfPosition(file, equalsGreaterThanToken.end).line;
    return startLine !***REMOVED******REMOVED*** endLine && grammarErrorOnNode(equalsGreaterThanToken, Diagnostics.Line_terminator_not_permitted_before_arrow);
  }
  function checkGrammarIndexSignatureParameters(node) {
    const parameter ***REMOVED*** node.parameters[0];
    if (node.parameters.length !***REMOVED******REMOVED*** 1) {
      if (parameter) {
        return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_must_have_exactly_one_parameter);
      } else {
        return grammarErrorOnNode(node, Diagnostics.An_index_signature_must_have_exactly_one_parameter);
      }
    }
    checkGrammarForDisallowedTrailingComma(node.parameters, Diagnostics.An_index_signature_cannot_have_a_trailing_comma);
    if (parameter.dotDotDotToken) {
      return grammarErrorOnNode(parameter.dotDotDotToken, Diagnostics.An_index_signature_cannot_have_a_rest_parameter);
    }
    if (hasEffectiveModifiers(parameter)) {
      return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_parameter_cannot_have_an_accessibility_modifier);
    }
    if (parameter.questionToken) {
      return grammarErrorOnNode(parameter.questionToken, Diagnostics.An_index_signature_parameter_cannot_have_a_question_mark);
    }
    if (parameter.initializer) {
      return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_parameter_cannot_have_an_initializer);
    }
    if (!parameter.type) {
      return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_parameter_must_have_a_type_annotation);
    }
    const type ***REMOVED*** getTypeFromTypeNode(parameter.type);
    if (someType(type, (t) ***REMOVED***> !!(t.flags & 8576 /* StringOrNumberLiteralOrUnique */)) || isGenericType(type)) {
      return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_parameter_type_cannot_be_a_literal_type_or_generic_type_Consider_using_a_mapped_object_type_instead);
    }
    if (!everyType(type, isValidIndexKeyType)) {
      return grammarErrorOnNode(parameter.name, Diagnostics.An_index_signature_parameter_type_must_be_string_number_symbol_or_a_template_literal_type);
    }
    if (!node.type) {
      return grammarErrorOnNode(node, Diagnostics.An_index_signature_must_have_a_type_annotation);
    }
    return false;
  }
  function checkGrammarIndexSignature(node) {
    return checkGrammarModifiers(node) || checkGrammarIndexSignatureParameters(node);
  }
  function checkGrammarForAtLeastOneTypeArgument(node, typeArguments) {
    if (typeArguments && typeArguments.length ***REMOVED******REMOVED******REMOVED*** 0) {
      const sourceFile ***REMOVED*** getSourceFileOfNode(node);
      const start ***REMOVED*** typeArguments.pos - "<".length;
      const end ***REMOVED*** skipTrivia(sourceFile.text, typeArguments.end) + ">".length;
      return grammarErrorAtPos(sourceFile, start, end - start, Diagnostics.Type_argument_list_cannot_be_empty);
    }
    return false;
  }
  function checkGrammarTypeArguments(node, typeArguments) {
    return checkGrammarForDisallowedTrailingComma(typeArguments) || checkGrammarForAtLeastOneTypeArgument(node, typeArguments);
  }
  function checkGrammarTaggedTemplateChain(node) {
    if (node.questionDotToken || node.flags & 64 /* OptionalChain */) {
      return grammarErrorOnNode(node.template, Diagnostics.Tagged_template_expressions_are_not_permitted_in_an_optional_chain);
    }
    return false;
  }
  function checkGrammarHeritageClause(node) {
    const types ***REMOVED*** node.types;
    if (checkGrammarForDisallowedTrailingComma(types)) {
      return true;
    }
    if (types && types.length ***REMOVED******REMOVED******REMOVED*** 0) {
      const listType ***REMOVED*** tokenToString(node.token);
      return grammarErrorAtPos(node, types.pos, 0, Diagnostics._0_list_cannot_be_empty, listType);
    }
    return some(types, checkGrammarExpressionWithTypeArguments);
  }
  function checkGrammarExpressionWithTypeArguments(node) {
    if (isExpressionWithTypeArguments(node) && isImportKeyword(node.expression) && node.typeArguments) {
      return grammarErrorOnNode(node, Diagnostics.This_use_of_import_is_invalid_import_calls_can_be_written_but_they_must_have_parentheses_and_cannot_have_type_arguments);
    }
    return checkGrammarTypeArguments(node, node.typeArguments);
  }
  function checkGrammarClassDeclarationHeritageClauses(node) {
    let seenExtendsClause ***REMOVED*** false;
    let seenImplementsClause ***REMOVED*** false;
    if (!checkGrammarModifiers(node) && node.heritageClauses) {
      for (const heritageClause of node.heritageClauses) {
        if (heritageClause.token ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
          if (seenExtendsClause) {
            return grammarErrorOnFirstToken(heritageClause, Diagnostics.extends_clause_already_seen);
          }
          if (seenImplementsClause) {
            return grammarErrorOnFirstToken(heritageClause, Diagnostics.extends_clause_must_precede_implements_clause);
          }
          if (heritageClause.types.length > 1) {
            return grammarErrorOnFirstToken(heritageClause.types[1], Diagnostics.Classes_can_only_extend_a_single_class);
          }
          seenExtendsClause ***REMOVED*** true;
        } else {
          Debug.assert(heritageClause.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */);
          if (seenImplementsClause) {
            return grammarErrorOnFirstToken(heritageClause, Diagnostics.implements_clause_already_seen);
          }
          seenImplementsClause ***REMOVED*** true;
        }
        checkGrammarHeritageClause(heritageClause);
      }
    }
  }
  function checkGrammarInterfaceDeclaration(node) {
    let seenExtendsClause ***REMOVED*** false;
    if (node.heritageClauses) {
      for (const heritageClause of node.heritageClauses) {
        if (heritageClause.token ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
          if (seenExtendsClause) {
            return grammarErrorOnFirstToken(heritageClause, Diagnostics.extends_clause_already_seen);
          }
          seenExtendsClause ***REMOVED*** true;
        } else {
          Debug.assert(heritageClause.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */);
          return grammarErrorOnFirstToken(heritageClause, Diagnostics.Interface_declaration_cannot_have_implements_clause);
        }
        checkGrammarHeritageClause(heritageClause);
      }
    }
    return false;
  }
  function checkGrammarComputedPropertyName(node) {
    if (node.kind !***REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
      return false;
    }
    const computedPropertyName ***REMOVED*** node;
    if (computedPropertyName.expression.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ && computedPropertyName.expression.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      return grammarErrorOnNode(computedPropertyName.expression, Diagnostics.A_comma_expression_is_not_allowed_in_a_computed_property_name);
    }
    return false;
  }
  function checkGrammarForGenerator(node) {
    if (node.asteriskToken) {
      Debug.assert(
        node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */
      );
      if (node.flags & 33554432 /* Ambient */) {
        return grammarErrorOnNode(node.asteriskToken, Diagnostics.Generators_are_not_allowed_in_an_ambient_context);
      }
      if (!node.body) {
        return grammarErrorOnNode(node.asteriskToken, Diagnostics.An_overload_signature_cannot_be_declared_as_a_generator);
      }
    }
  }
  function checkGrammarForInvalidQuestionMark(questionToken, message) {
    return !!questionToken && grammarErrorOnNode(questionToken, message);
  }
  function checkGrammarForInvalidExclamationToken(exclamationToken, message) {
    return !!exclamationToken && grammarErrorOnNode(exclamationToken, message);
  }
  function checkGrammarObjectLiteralExpression(node, inDestructuring) {
    const seen ***REMOVED*** /* @__PURE__ */ new Map();
    for (const prop of node.properties) {
      if (prop.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
        if (inDestructuring) {
          const expression ***REMOVED*** skipParentheses(prop.expression);
          if (isArrayLiteralExpression(expression) || isObjectLiteralExpression(expression)) {
            return grammarErrorOnNode(prop.expression, Diagnostics.A_rest_element_cannot_contain_a_binding_pattern);
          }
        }
        continue;
      }
      const name ***REMOVED*** prop.name;
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        checkGrammarComputedPropertyName(name);
      }
      if (prop.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ && !inDestructuring && prop.objectAssignmentInitializer) {
        grammarErrorOnNode(prop.equalsToken, Diagnostics.Did_you_mean_to_use_a_Colon_An_can_only_follow_a_property_name_when_the_containing_object_literal_is_part_of_a_destructuring_pattern);
      }
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
        grammarErrorOnNode(name, Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies);
      }
      if (canHaveModifiers(prop) && prop.modifiers) {
        for (const mod of prop.modifiers) {
          if (isModifier(mod) && (mod.kind !***REMOVED******REMOVED*** 134 /* AsyncKeyword */ || prop.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */)) {
            grammarErrorOnNode(mod, Diagnostics._0_modifier_cannot_be_used_here, getTextOfNode(mod));
          }
        }
      } else if (canHaveIllegalModifiers(prop) && prop.modifiers) {
        for (const mod of prop.modifiers) {
          if (isModifier(mod)) {
            grammarErrorOnNode(mod, Diagnostics._0_modifier_cannot_be_used_here, getTextOfNode(mod));
          }
        }
      }
      let currentKind;
      switch (prop.kind) {
        case 305 /* ShorthandPropertyAssignment */:
        case 304 /* PropertyAssignment */:
          checkGrammarForInvalidExclamationToken(prop.exclamationToken, Diagnostics.A_definite_assignment_assertion_is_not_permitted_in_this_context);
          checkGrammarForInvalidQuestionMark(prop.questionToken, Diagnostics.An_object_member_cannot_be_declared_optional);
          if (name.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */) {
            checkGrammarNumericLiteral(name);
          }
          if (name.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */) {
            addErrorOrSuggestion(
              /*isError*/
              true,
              createDiagnosticForNode(name, Diagnostics.A_bigint_literal_cannot_be_used_as_a_property_name)
            );
          }
          currentKind ***REMOVED*** 4 /* PropertyAssignment */;
          break;
        case 175 /* MethodDeclaration */:
          currentKind ***REMOVED*** 8 /* Method */;
          break;
        case 178 /* GetAccessor */:
          currentKind ***REMOVED*** 1 /* GetAccessor */;
          break;
        case 179 /* SetAccessor */:
          currentKind ***REMOVED*** 2 /* SetAccessor */;
          break;
        default:
          Debug.assertNever(prop, "Unexpected syntax kind:" + prop.kind);
      }
      if (!inDestructuring) {
        const effectiveName ***REMOVED*** getEffectivePropertyNameForPropertyNameNode(name);
        if (effectiveName ***REMOVED******REMOVED******REMOVED*** void 0) {
          continue;
        }
        const existingKind ***REMOVED*** seen.get(effectiveName);
        if (!existingKind) {
          seen.set(effectiveName, currentKind);
        } else {
          if (currentKind & 8 /* Method */ && existingKind & 8 /* Method */) {
            grammarErrorOnNode(name, Diagnostics.Duplicate_identifier_0, getTextOfNode(name));
          } else if (currentKind & 4 /* PropertyAssignment */ && existingKind & 4 /* PropertyAssignment */) {
            grammarErrorOnNode(name, Diagnostics.An_object_literal_cannot_have_multiple_properties_with_the_same_name, getTextOfNode(name));
          } else if (currentKind & 3 /* GetOrSetAccessor */ && existingKind & 3 /* GetOrSetAccessor */) {
            if (existingKind !***REMOVED******REMOVED*** 3 /* GetOrSetAccessor */ && currentKind !***REMOVED******REMOVED*** existingKind) {
              seen.set(effectiveName, currentKind | existingKind);
            } else {
              return grammarErrorOnNode(name, Diagnostics.An_object_literal_cannot_have_multiple_get_Slashset_accessors_with_the_same_name);
            }
          } else {
            return grammarErrorOnNode(name, Diagnostics.An_object_literal_cannot_have_property_and_accessor_with_the_same_name);
          }
        }
      }
    }
  }
  function checkGrammarJsxElement(node) {
    checkGrammarJsxName(node.tagName);
    checkGrammarTypeArguments(node, node.typeArguments);
    const seen ***REMOVED*** /* @__PURE__ */ new Map();
    for (const attr of node.attributes.properties) {
      if (attr.kind ***REMOVED******REMOVED******REMOVED*** 294 /* JsxSpreadAttribute */) {
        continue;
      }
      const { name, initializer } ***REMOVED*** attr;
      const escapedText ***REMOVED*** getEscapedTextOfJsxAttributeName(name);
      if (!seen.get(escapedText)) {
        seen.set(escapedText, true);
      } else {
        return grammarErrorOnNode(name, Diagnostics.JSX_elements_cannot_have_multiple_attributes_with_the_same_name);
      }
      if (initializer && initializer.kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */ && !initializer.expression) {
        return grammarErrorOnNode(initializer, Diagnostics.JSX_attributes_must_only_be_assigned_a_non_empty_expression);
      }
    }
  }
  function checkGrammarJsxName(node) {
    if (isPropertyAccessExpression(node) && isJsxNamespacedName(node.expression)) {
      return grammarErrorOnNode(node.expression, Diagnostics.JSX_property_access_expressions_cannot_include_JSX_namespace_names);
    }
    if (isJsxNamespacedName(node) && getJSXTransformEnabled(compilerOptions) && !isIntrinsicJsxName(node.namespace.escapedText)) {
      return grammarErrorOnNode(node, Diagnostics.React_components_cannot_include_JSX_namespace_names);
    }
  }
  function checkGrammarJsxExpression(node) {
    if (node.expression && isCommaSequence(node.expression)) {
      return grammarErrorOnNode(node.expression, Diagnostics.JSX_expressions_may_not_use_the_comma_operator_Did_you_mean_to_write_an_array);
    }
  }
  function checkGrammarForInOrForOfStatement(forInOrOfStatement) {
    if (checkGrammarStatementInAmbientContext(forInOrOfStatement)) {
      return true;
    }
    if (forInOrOfStatement.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */ && forInOrOfStatement.awaitModifier) {
      if (!(forInOrOfStatement.flags & 65536 /* AwaitContext */)) {
        const sourceFile ***REMOVED*** getSourceFileOfNode(forInOrOfStatement);
        if (isInTopLevelContext(forInOrOfStatement)) {
          if (!hasParseDiagnostics(sourceFile)) {
            if (!isEffectiveExternalModule(sourceFile, compilerOptions)) {
              diagnostics.add(createDiagnosticForNode(forInOrOfStatement.awaitModifier, Diagnostics.for_await_loops_are_only_allowed_at_the_top_level_of_a_file_when_that_file_is_a_module_but_this_file_has_no_imports_or_exports_Consider_adding_an_empty_export_to_make_this_file_a_module));
            }
            switch (moduleKind) {
              case 100 /* Node16 */:
              case 101 /* Node18 */:
              case 102 /* Node20 */:
              case 199 /* NodeNext */:
                if (sourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
                  diagnostics.add(
                    createDiagnosticForNode(forInOrOfStatement.awaitModifier, Diagnostics.The_current_file_is_a_CommonJS_module_and_cannot_use_await_at_the_top_level)
                  );
                  break;
                }
              // fallthrough
              case 7 /* ES2022 */:
              case 99 /* ESNext */:
              case 200 /* Preserve */:
              case 4 /* System */:
                if (languageVersion >***REMOVED*** 4 /* ES2017 */) {
                  break;
                }
              // fallthrough
              default:
                diagnostics.add(
                  createDiagnosticForNode(forInOrOfStatement.awaitModifier, Diagnostics.Top_level_for_await_loops_are_only_allowed_when_the_module_option_is_set_to_es2022_esnext_system_node16_node18_node20_nodenext_or_preserve_and_the_target_option_is_set_to_es2017_or_higher)
                );
                break;
            }
          }
        } else {
          if (!hasParseDiagnostics(sourceFile)) {
            const diagnostic ***REMOVED*** createDiagnosticForNode(forInOrOfStatement.awaitModifier, Diagnostics.for_await_loops_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules);
            const func ***REMOVED*** getContainingFunction(forInOrOfStatement);
            if (func && func.kind !***REMOVED******REMOVED*** 177 /* Constructor */) {
              Debug.assert((getFunctionFlags(func) & 2 /* Async */) ***REMOVED******REMOVED******REMOVED*** 0, "Enclosing function should never be an async function.");
              const relatedInfo ***REMOVED*** createDiagnosticForNode(func, Diagnostics.Did_you_mean_to_mark_this_function_as_async);
              addRelatedInfo(diagnostic, relatedInfo);
            }
            diagnostics.add(diagnostic);
            return true;
          }
        }
      }
    }
    if (isForOfStatement(forInOrOfStatement) && !(forInOrOfStatement.flags & 65536 /* AwaitContext */) && isIdentifier(forInOrOfStatement.initializer) && forInOrOfStatement.initializer.escapedText ***REMOVED******REMOVED******REMOVED*** "async") {
      grammarErrorOnNode(forInOrOfStatement.initializer, Diagnostics.The_left_hand_side_of_a_for_of_statement_may_not_be_async);
      return false;
    }
    if (forInOrOfStatement.initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
      const variableList ***REMOVED*** forInOrOfStatement.initializer;
      if (!checkGrammarVariableDeclarationList(variableList)) {
        const declarations ***REMOVED*** variableList.declarations;
        if (!declarations.length) {
          return false;
        }
        if (declarations.length > 1) {
          const diagnostic ***REMOVED*** forInOrOfStatement.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ ? Diagnostics.Only_a_single_variable_declaration_is_allowed_in_a_for_in_statement : Diagnostics.Only_a_single_variable_declaration_is_allowed_in_a_for_of_statement;
          return grammarErrorOnFirstToken(variableList.declarations[1], diagnostic);
        }
        const firstDeclaration ***REMOVED*** declarations[0];
        if (firstDeclaration.initializer) {
          const diagnostic ***REMOVED*** forInOrOfStatement.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ ? Diagnostics.The_variable_declaration_of_a_for_in_statement_cannot_have_an_initializer : Diagnostics.The_variable_declaration_of_a_for_of_statement_cannot_have_an_initializer;
          return grammarErrorOnNode(firstDeclaration.name, diagnostic);
        }
        if (firstDeclaration.type) {
          const diagnostic ***REMOVED*** forInOrOfStatement.kind ***REMOVED******REMOVED******REMOVED*** 250 /* ForInStatement */ ? Diagnostics.The_left_hand_side_of_a_for_in_statement_cannot_use_a_type_annotation : Diagnostics.The_left_hand_side_of_a_for_of_statement_cannot_use_a_type_annotation;
          return grammarErrorOnNode(firstDeclaration, diagnostic);
        }
      }
    }
    return false;
  }
  function checkGrammarAccessor(accessor) {
    if (!(accessor.flags & 33554432 /* Ambient */) && accessor.parent.kind !***REMOVED******REMOVED*** 188 /* TypeLiteral */ && accessor.parent.kind !***REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
      if (languageVersion < 2 /* ES2015 */ && isPrivateIdentifier(accessor.name)) {
        return grammarErrorOnNode(accessor.name, Diagnostics.Private_identifiers_are_only_available_when_targeting_ECMAScript_2015_and_higher);
      }
      if (accessor.body ***REMOVED******REMOVED******REMOVED*** void 0 && !hasSyntacticModifier(accessor, 64 /* Abstract */)) {
        return grammarErrorAtPos(accessor, accessor.end - 1, ";".length, Diagnostics._0_expected, "{");
      }
    }
    if (accessor.body) {
      if (hasSyntacticModifier(accessor, 64 /* Abstract */)) {
        return grammarErrorOnNode(accessor, Diagnostics.An_abstract_accessor_cannot_have_an_implementation);
      }
      if (accessor.parent.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */ || accessor.parent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
        return grammarErrorOnNode(accessor.body, Diagnostics.An_implementation_cannot_be_declared_in_ambient_contexts);
      }
    }
    if (accessor.typeParameters) {
      return grammarErrorOnNode(accessor.name, Diagnostics.An_accessor_cannot_have_type_parameters);
    }
    if (!doesAccessorHaveCorrectParameterCount(accessor)) {
      return grammarErrorOnNode(
        accessor.name,
        accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? Diagnostics.A_get_accessor_cannot_have_parameters : Diagnostics.A_set_accessor_must_have_exactly_one_parameter
      );
    }
    if (accessor.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
      if (accessor.type) {
        return grammarErrorOnNode(accessor.name, Diagnostics.A_set_accessor_cannot_have_a_return_type_annotation);
      }
      const parameter ***REMOVED*** Debug.checkDefined(getSetAccessorValueParameter(accessor), "Return value does not match parameter count assertion.");
      if (parameter.dotDotDotToken) {
        return grammarErrorOnNode(parameter.dotDotDotToken, Diagnostics.A_set_accessor_cannot_have_rest_parameter);
      }
      if (parameter.questionToken) {
        return grammarErrorOnNode(parameter.questionToken, Diagnostics.A_set_accessor_cannot_have_an_optional_parameter);
      }
      if (parameter.initializer) {
        return grammarErrorOnNode(accessor.name, Diagnostics.A_set_accessor_parameter_cannot_have_an_initializer);
      }
    }
    return false;
  }
  function doesAccessorHaveCorrectParameterCount(accessor) {
    return getAccessorThisParameter(accessor) || accessor.parameters.length ***REMOVED******REMOVED******REMOVED*** (accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? 0 : 1);
  }
  function getAccessorThisParameter(accessor) {
    if (accessor.parameters.length ***REMOVED******REMOVED******REMOVED*** (accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? 1 : 2)) {
      return getThisParameter(accessor);
    }
  }
  function checkGrammarTypeOperatorNode(node) {
    if (node.operator ***REMOVED******REMOVED******REMOVED*** 158 /* UniqueKeyword */) {
      if (node.type.kind !***REMOVED******REMOVED*** 155 /* SymbolKeyword */) {
        return grammarErrorOnNode(node.type, Diagnostics._0_expected, tokenToString(155 /* SymbolKeyword */));
      }
      let parent ***REMOVED*** walkUpParenthesizedTypes(node.parent);
      if (isInJSFile(parent) && isJSDocTypeExpression(parent)) {
        const host2 ***REMOVED*** getJSDocHost(parent);
        if (host2) {
          parent ***REMOVED*** getSingleVariableOfVariableStatement(host2) || host2;
        }
      }
      switch (parent.kind) {
        case 261 /* VariableDeclaration */:
          const decl ***REMOVED*** parent;
          if (decl.name.kind !***REMOVED******REMOVED*** 80 /* Identifier */) {
            return grammarErrorOnNode(node, Diagnostics.unique_symbol_types_may_not_be_used_on_a_variable_declaration_with_a_binding_name);
          }
          if (!isVariableDeclarationInVariableStatement(decl)) {
            return grammarErrorOnNode(node, Diagnostics.unique_symbol_types_are_only_allowed_on_variables_in_a_variable_statement);
          }
          if (!(decl.parent.flags & 2 /* Const */)) {
            return grammarErrorOnNode(parent.name, Diagnostics.A_variable_whose_type_is_a_unique_symbol_type_must_be_const);
          }
          break;
        case 173 /* PropertyDeclaration */:
          if (!isStatic(parent) || !hasEffectiveReadonlyModifier(parent)) {
            return grammarErrorOnNode(parent.name, Diagnostics.A_property_of_a_class_whose_type_is_a_unique_symbol_type_must_be_both_static_and_readonly);
          }
          break;
        case 172 /* PropertySignature */:
          if (!hasSyntacticModifier(parent, 8 /* Readonly */)) {
            return grammarErrorOnNode(parent.name, Diagnostics.A_property_of_an_interface_or_type_literal_whose_type_is_a_unique_symbol_type_must_be_readonly);
          }
          break;
        default:
          return grammarErrorOnNode(node, Diagnostics.unique_symbol_types_are_not_allowed_here);
      }
    } else if (node.operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
      if (node.type.kind !***REMOVED******REMOVED*** 189 /* ArrayType */ && node.type.kind !***REMOVED******REMOVED*** 190 /* TupleType */) {
        return grammarErrorOnFirstToken(node, Diagnostics.readonly_type_modifier_is_only_permitted_on_array_and_tuple_literal_types, tokenToString(155 /* SymbolKeyword */));
      }
    }
  }
  function checkGrammarForInvalidDynamicName(node, message) {
    if (isNonBindableDynamicName(node) && !isEntityNameExpression(isElementAccessExpression(node) ? skipParentheses(node.argumentExpression) : node.expression)) {
      return grammarErrorOnNode(node, message);
    }
  }
  function checkGrammarMethod(node) {
    if (checkGrammarFunctionLikeDeclaration(node)) {
      return true;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */) {
      if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
        if (node.modifiers && !(node.modifiers.length ***REMOVED******REMOVED******REMOVED*** 1 && first(node.modifiers).kind ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */)) {
          return grammarErrorOnFirstToken(node, Diagnostics.Modifiers_cannot_appear_here);
        } else if (checkGrammarForInvalidQuestionMark(node.questionToken, Diagnostics.An_object_member_cannot_be_declared_optional)) {
          return true;
        } else if (checkGrammarForInvalidExclamationToken(node.exclamationToken, Diagnostics.A_definite_assignment_assertion_is_not_permitted_in_this_context)) {
          return true;
        } else if (node.body ***REMOVED******REMOVED******REMOVED*** void 0) {
          return grammarErrorAtPos(node, node.end - 1, ";".length, Diagnostics._0_expected, "{");
        }
      }
      if (checkGrammarForGenerator(node)) {
        return true;
      }
    }
    if (isClassLike(node.parent)) {
      if (languageVersion < 2 /* ES2015 */ && isPrivateIdentifier(node.name)) {
        return grammarErrorOnNode(node.name, Diagnostics.Private_identifiers_are_only_available_when_targeting_ECMAScript_2015_and_higher);
      }
      if (node.flags & 33554432 /* Ambient */) {
        return checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_an_ambient_context_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type);
      } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && !node.body) {
        return checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_a_method_overload_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type);
      }
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
      return checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_an_interface_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type);
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */) {
      return checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_a_type_literal_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type);
    }
  }
  function checkGrammarBreakOrContinueStatement(node) {
    let current ***REMOVED*** node;
    while (current) {
      if (isFunctionLikeOrClassStaticBlockDeclaration(current)) {
        return grammarErrorOnNode(node, Diagnostics.Jump_target_cannot_cross_function_boundary);
      }
      switch (current.kind) {
        case 257 /* LabeledStatement */:
          if (node.label && current.label.escapedText ***REMOVED******REMOVED******REMOVED*** node.label.escapedText) {
            const isMisplacedContinueLabel ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 252 /* ContinueStatement */ && !isIterationStatement(
              current.statement,
              /*lookInLabeledStatements*/
              true
            );
            if (isMisplacedContinueLabel) {
              return grammarErrorOnNode(node, Diagnostics.A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement);
            }
            return false;
          }
          break;
        case 256 /* SwitchStatement */:
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ && !node.label) {
            return false;
          }
          break;
        default:
          if (isIterationStatement(
            current,
            /*lookInLabeledStatements*/
            false
          ) && !node.label) {
            return false;
          }
          break;
      }
      current ***REMOVED*** current.parent;
    }
    if (node.label) {
      const message ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? Diagnostics.A_break_statement_can_only_jump_to_a_label_of_an_enclosing_statement : Diagnostics.A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement;
      return grammarErrorOnNode(node, message);
    } else {
      const message ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? Diagnostics.A_break_statement_can_only_be_used_within_an_enclosing_iteration_or_switch_statement : Diagnostics.A_continue_statement_can_only_be_used_within_an_enclosing_iteration_statement;
      return grammarErrorOnNode(node, message);
    }
  }
  function checkGrammarBindingElement(node) {
    if (node.dotDotDotToken) {
      const elements ***REMOVED*** node.parent.elements;
      if (node !***REMOVED******REMOVED*** last(elements)) {
        return grammarErrorOnNode(node, Diagnostics.A_rest_element_must_be_last_in_a_destructuring_pattern);
      }
      checkGrammarForDisallowedTrailingComma(elements, Diagnostics.A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma);
      if (node.propertyName) {
        return grammarErrorOnNode(node.name, Diagnostics.A_rest_element_cannot_have_a_property_name);
      }
    }
    if (node.dotDotDotToken && node.initializer) {
      return grammarErrorAtPos(node, node.initializer.pos - 1, 1, Diagnostics.A_rest_element_cannot_have_an_initializer);
    }
  }
  function isStringOrNumberLiteralExpression(expr) {
    return isStringOrNumericLiteralLike(expr) || expr.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ && expr.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ && expr.operand.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */;
  }
  function isBigIntLiteralExpression(expr) {
    return expr.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ || expr.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ && expr.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ && expr.operand.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */;
  }
  function isSimpleLiteralEnumReference(expr) {
    if ((isPropertyAccessExpression(expr) || isElementAccessExpression(expr) && isStringOrNumberLiteralExpression(expr.argumentExpression)) && isEntityNameExpression(expr.expression)) {
      return !!(checkExpressionCached(expr).flags & 1056 /* EnumLike */);
    }
  }
  function checkAmbientInitializer(node) {
    const initializer ***REMOVED*** node.initializer;
    if (initializer) {
      const isInvalidInitializer ***REMOVED*** !(isStringOrNumberLiteralExpression(initializer) || isSimpleLiteralEnumReference(initializer) || initializer.kind ***REMOVED******REMOVED******REMOVED*** 112 /* TrueKeyword */ || initializer.kind ***REMOVED******REMOVED******REMOVED*** 97 /* FalseKeyword */ || isBigIntLiteralExpression(initializer));
      const isConstOrReadonly ***REMOVED*** isDeclarationReadonly(node) || isVariableDeclaration(node) && isVarConstLike2(node);
      if (isConstOrReadonly && !node.type) {
        if (isInvalidInitializer) {
          return grammarErrorOnNode(initializer, Diagnostics.A_const_initializer_in_an_ambient_context_must_be_a_string_or_numeric_literal_or_literal_enum_reference);
        }
      } else {
        return grammarErrorOnNode(initializer, Diagnostics.Initializers_are_not_allowed_in_ambient_contexts);
      }
    }
  }
  function checkGrammarVariableDeclaration(node) {
    const nodeFlags ***REMOVED*** getCombinedNodeFlagsCached(node);
    const blockScopeKind ***REMOVED*** nodeFlags & 7 /* BlockScoped */;
    if (isBindingPattern(node.name)) {
      switch (blockScopeKind) {
        case 6 /* AwaitUsing */:
          return grammarErrorOnNode(node, Diagnostics._0_declarations_may_not_have_binding_patterns, "await using");
        case 4 /* Using */:
          return grammarErrorOnNode(node, Diagnostics._0_declarations_may_not_have_binding_patterns, "using");
      }
    }
    if (node.parent.parent.kind !***REMOVED******REMOVED*** 250 /* ForInStatement */ && node.parent.parent.kind !***REMOVED******REMOVED*** 251 /* ForOfStatement */) {
      if (nodeFlags & 33554432 /* Ambient */) {
        checkAmbientInitializer(node);
      } else if (!node.initializer) {
        if (isBindingPattern(node.name) && !isBindingPattern(node.parent)) {
          return grammarErrorOnNode(node, Diagnostics.A_destructuring_declaration_must_have_an_initializer);
        }
        switch (blockScopeKind) {
          case 6 /* AwaitUsing */:
            return grammarErrorOnNode(node, Diagnostics._0_declarations_must_be_initialized, "await using");
          case 4 /* Using */:
            return grammarErrorOnNode(node, Diagnostics._0_declarations_must_be_initialized, "using");
          case 2 /* Const */:
            return grammarErrorOnNode(node, Diagnostics._0_declarations_must_be_initialized, "const");
        }
      }
    }
    if (node.exclamationToken && (node.parent.parent.kind !***REMOVED******REMOVED*** 244 /* VariableStatement */ || !node.type || node.initializer || nodeFlags & 33554432 /* Ambient */)) {
      const message ***REMOVED*** node.initializer ? Diagnostics.Declarations_with_initializers_cannot_also_have_definite_assignment_assertions : !node.type ? Diagnostics.Declarations_with_definite_assignment_assertions_must_also_have_type_annotations : Diagnostics.A_definite_assignment_assertion_is_not_permitted_in_this_context;
      return grammarErrorOnNode(node.exclamationToken, message);
    }
    if (host.getEmitModuleFormatOfFile(getSourceFileOfNode(node)) < 4 /* System */ && !(node.parent.parent.flags & 33554432 /* Ambient */) && hasSyntacticModifier(node.parent.parent, 32 /* Export */)) {
      checkESModuleMarker(node.name);
    }
    return !!blockScopeKind && checkGrammarNameInLetOrConstDeclarations(node.name);
  }
  function checkESModuleMarker(name) {
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      if (idText(name) ***REMOVED******REMOVED******REMOVED*** "__esModule") {
        return grammarErrorOnNodeSkippedOn("noEmit", name, Diagnostics.Identifier_expected_esModule_is_reserved_as_an_exported_marker_when_transforming_ECMAScript_modules);
      }
    } else {
      const elements ***REMOVED*** name.elements;
      for (const element of elements) {
        if (!isOmittedExpression(element)) {
          return checkESModuleMarker(element.name);
        }
      }
    }
    return false;
  }
  function checkGrammarNameInLetOrConstDeclarations(name) {
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      if (name.escapedText ***REMOVED******REMOVED******REMOVED*** "let") {
        return grammarErrorOnNode(name, Diagnostics.let_is_not_allowed_to_be_used_as_a_name_in_let_or_const_declarations);
      }
    } else {
      const elements ***REMOVED*** name.elements;
      for (const element of elements) {
        if (!isOmittedExpression(element)) {
          checkGrammarNameInLetOrConstDeclarations(element.name);
        }
      }
    }
    return false;
  }
  function checkGrammarVariableDeclarationList(declarationList) {
    const declarations ***REMOVED*** declarationList.declarations;
    if (checkGrammarForDisallowedTrailingComma(declarationList.declarations)) {
      return true;
    }
    if (!declarationList.declarations.length) {
      return grammarErrorAtPos(declarationList, declarations.pos, declarations.end - declarations.pos, Diagnostics.Variable_declaration_list_cannot_be_empty);
    }
    const blockScopeFlags ***REMOVED*** declarationList.flags & 7 /* BlockScoped */;
    if (blockScopeFlags ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ || blockScopeFlags ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
      if (isForInStatement(declarationList.parent)) {
        return grammarErrorOnNode(
          declarationList,
          blockScopeFlags ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ ? Diagnostics.The_left_hand_side_of_a_for_in_statement_cannot_be_a_using_declaration : Diagnostics.The_left_hand_side_of_a_for_in_statement_cannot_be_an_await_using_declaration
        );
      }
      if (declarationList.flags & 33554432 /* Ambient */) {
        return grammarErrorOnNode(
          declarationList,
          blockScopeFlags ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ ? Diagnostics.using_declarations_are_not_allowed_in_ambient_contexts : Diagnostics.await_using_declarations_are_not_allowed_in_ambient_contexts
        );
      }
      if (blockScopeFlags ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */) {
        return checkAwaitGrammar(declarationList);
      }
    }
    return false;
  }
  function allowBlockDeclarations(parent) {
    switch (parent.kind) {
      case 246 /* IfStatement */:
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
      case 255 /* WithStatement */:
      case 249 /* ForStatement */:
      case 250 /* ForInStatement */:
      case 251 /* ForOfStatement */:
        return false;
      case 257 /* LabeledStatement */:
        return allowBlockDeclarations(parent.parent);
    }
    return true;
  }
  function checkGrammarForDisallowedBlockScopedVariableStatement(node) {
    if (!allowBlockDeclarations(node.parent)) {
      const blockScopeKind ***REMOVED*** getCombinedNodeFlagsCached(node.declarationList) & 7 /* BlockScoped */;
      if (blockScopeKind) {
        const keyword ***REMOVED*** blockScopeKind ***REMOVED******REMOVED******REMOVED*** 1 /* Let */ ? "let" : blockScopeKind ***REMOVED******REMOVED******REMOVED*** 2 /* Const */ ? "const" : blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ ? "using" : blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */ ? "await using" : Debug.fail("Unknown BlockScope flag");
        error(node, Diagnostics._0_declarations_can_only_be_declared_inside_a_block, keyword);
      }
    }
  }
  function checkGrammarMetaProperty(node) {
    const escapedText ***REMOVED*** node.name.escapedText;
    switch (node.keywordToken) {
      case 105 /* NewKeyword */:
        if (escapedText !***REMOVED******REMOVED*** "target") {
          return grammarErrorOnNode(node.name, Diagnostics._0_is_not_a_valid_meta_property_for_keyword_1_Did_you_mean_2, unescapeLeadingUnderscores(node.name.escapedText), tokenToString(node.keywordToken), "target");
        }
        break;
      case 102 /* ImportKeyword */:
        if (escapedText !***REMOVED******REMOVED*** "meta") {
          const isCallee ***REMOVED*** isCallExpression(node.parent) && node.parent.expression ***REMOVED******REMOVED******REMOVED*** node;
          if (escapedText ***REMOVED******REMOVED******REMOVED*** "defer") {
            if (!isCallee) {
              return grammarErrorAtPos(node, node.end, 0, Diagnostics._0_expected, "(");
            }
          } else {
            if (isCallee) {
              return grammarErrorOnNode(node.name, Diagnostics._0_is_not_a_valid_meta_property_for_keyword_import_Did_you_mean_meta_or_defer, unescapeLeadingUnderscores(node.name.escapedText));
            }
            return grammarErrorOnNode(node.name, Diagnostics._0_is_not_a_valid_meta_property_for_keyword_1_Did_you_mean_2, unescapeLeadingUnderscores(node.name.escapedText), tokenToString(node.keywordToken), "meta");
          }
        }
        break;
    }
  }
  function hasParseDiagnostics(sourceFile) {
    return sourceFile.parseDiagnostics.length > 0;
  }
  function grammarErrorOnFirstToken(node, message, ...args) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile)) {
      const span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos);
      diagnostics.add(createFileDiagnostic(sourceFile, span.start, span.length, message, ...args));
      return true;
    }
    return false;
  }
  function grammarErrorAtPos(nodeForSourceFile, start, length2, message, ...args) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(nodeForSourceFile);
    if (!hasParseDiagnostics(sourceFile)) {
      diagnostics.add(createFileDiagnostic(sourceFile, start, length2, message, ...args));
      return true;
    }
    return false;
  }
  function grammarErrorOnNodeSkippedOn(key, node, message, ...args) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile)) {
      errorSkippedOn(key, node, message, ...args);
      return true;
    }
    return false;
  }
  function grammarErrorOnNode(node, message, ...args) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile)) {
      error(node, message, ...args);
      return true;
    }
    return false;
  }
  function checkGrammarConstructorTypeParameters(node) {
    const jsdocTypeParameters ***REMOVED*** isInJSFile(node) ? getJSDocTypeParameterDeclarations(node) : void 0;
    const range ***REMOVED*** node.typeParameters || jsdocTypeParameters && firstOrUndefined(jsdocTypeParameters);
    if (range) {
      const pos ***REMOVED*** range.pos ***REMOVED******REMOVED******REMOVED*** range.end ? range.pos : skipTrivia(getSourceFileOfNode(node).text, range.pos);
      return grammarErrorAtPos(node, pos, range.end - pos, Diagnostics.Type_parameters_cannot_appear_on_a_constructor_declaration);
    }
  }
  function checkGrammarConstructorTypeAnnotation(node) {
    const type ***REMOVED*** node.type || getEffectiveReturnTypeNode(node);
    if (type) {
      return grammarErrorOnNode(type, Diagnostics.Type_annotation_cannot_appear_on_a_constructor_declaration);
    }
  }
  function checkGrammarProperty(node) {
    if (isComputedPropertyName(node.name) && isBinaryExpression(node.name.expression) && node.name.expression.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */) {
      return grammarErrorOnNode(node.parent.members[0], Diagnostics.A_mapped_type_may_not_declare_properties_or_methods);
    }
    if (isClassLike(node.parent)) {
      if (isStringLiteral(node.name) && node.name.text ***REMOVED******REMOVED******REMOVED*** "constructor") {
        return grammarErrorOnNode(node.name, Diagnostics.Classes_may_not_have_a_field_named_constructor);
      }
      if (checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_a_class_property_declaration_must_have_a_simple_literal_type_or_a_unique_symbol_type)) {
        return true;
      }
      if (languageVersion < 2 /* ES2015 */ && isPrivateIdentifier(node.name)) {
        return grammarErrorOnNode(node.name, Diagnostics.Private_identifiers_are_only_available_when_targeting_ECMAScript_2015_and_higher);
      }
      if (languageVersion < 2 /* ES2015 */ && isAutoAccessorPropertyDeclaration(node) && !(node.flags & 33554432 /* Ambient */)) {
        return grammarErrorOnNode(node.name, Diagnostics.Properties_with_the_accessor_modifier_are_only_available_when_targeting_ECMAScript_2015_and_higher);
      }
      if (isAutoAccessorPropertyDeclaration(node) && checkGrammarForInvalidQuestionMark(node.questionToken, Diagnostics.An_accessor_property_cannot_be_declared_optional)) {
        return true;
      }
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
      if (checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_an_interface_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type)) {
        return true;
      }
      Debug.assertNode(node, isPropertySignature);
      if (node.initializer) {
        return grammarErrorOnNode(node.initializer, Diagnostics.An_interface_property_cannot_have_an_initializer);
      }
    } else if (isTypeLiteralNode(node.parent)) {
      if (checkGrammarForInvalidDynamicName(node.name, Diagnostics.A_computed_property_name_in_a_type_literal_must_refer_to_an_expression_whose_type_is_a_literal_type_or_a_unique_symbol_type)) {
        return true;
      }
      Debug.assertNode(node, isPropertySignature);
      if (node.initializer) {
        return grammarErrorOnNode(node.initializer, Diagnostics.A_type_literal_property_cannot_have_an_initializer);
      }
    }
    if (node.flags & 33554432 /* Ambient */) {
      checkAmbientInitializer(node);
    }
    if (isPropertyDeclaration(node) && node.exclamationToken && (!isClassLike(node.parent) || !node.type || node.initializer || node.flags & 33554432 /* Ambient */ || isStatic(node) || hasAbstractModifier(node))) {
      const message ***REMOVED*** node.initializer ? Diagnostics.Declarations_with_initializers_cannot_also_have_definite_assignment_assertions : !node.type ? Diagnostics.Declarations_with_definite_assignment_assertions_must_also_have_type_annotations : Diagnostics.A_definite_assignment_assertion_is_not_permitted_in_this_context;
      return grammarErrorOnNode(node.exclamationToken, message);
    }
  }
  function checkGrammarTopLevelElementForRequiredDeclareModifier(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 278 /* ExportAssignment */ || node.kind ***REMOVED******REMOVED******REMOVED*** 271 /* NamespaceExportDeclaration */ || hasSyntacticModifier(node, 128 /* Ambient */ | 32 /* Export */ | 2048 /* Default */)) {
      return false;
    }
    return grammarErrorOnFirstToken(node, Diagnostics.Top_level_declarations_in_d_ts_files_must_start_with_either_a_declare_or_export_modifier);
  }
  function checkGrammarTopLevelElementsForRequiredDeclareModifier(file) {
    for (const decl of file.statements) {
      if (isDeclaration(decl) || decl.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */) {
        if (checkGrammarTopLevelElementForRequiredDeclareModifier(decl)) {
          return true;
        }
      }
    }
    return false;
  }
  function checkGrammarSourceFile(node) {
    return !!(node.flags & 33554432 /* Ambient */) && checkGrammarTopLevelElementsForRequiredDeclareModifier(node);
  }
  function checkGrammarStatementInAmbientContext(node) {
    if (node.flags & 33554432 /* Ambient */) {
      const links ***REMOVED*** getNodeLinks(node);
      if (!links.hasReportedStatementInAmbientContext && (isFunctionLike(node.parent) || isAccessor(node.parent))) {
        return getNodeLinks(node).hasReportedStatementInAmbientContext ***REMOVED*** grammarErrorOnFirstToken(node, Diagnostics.An_implementation_cannot_be_declared_in_ambient_contexts);
      }
      if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */ || node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
        const links2 ***REMOVED*** getNodeLinks(node.parent);
        if (!links2.hasReportedStatementInAmbientContext) {
          return links2.hasReportedStatementInAmbientContext ***REMOVED*** grammarErrorOnFirstToken(node, Diagnostics.Statements_are_not_allowed_in_ambient_contexts);
        }
      } else {
      }
    }
    return false;
  }
  function checkGrammarNumericLiteral(node) {
    const isFractional ***REMOVED*** getTextOfNode(node).includes(".");
    const isScientific ***REMOVED*** node.numericLiteralFlags & 16 /* Scientific */;
    if (isFractional || isScientific) {
      return;
    }
    const value ***REMOVED*** +node.text;
    if (value <***REMOVED*** 2 ** 53 - 1) {
      return;
    }
    addErrorOrSuggestion(
      /*isError*/
      false,
      createDiagnosticForNode(node, Diagnostics.Numeric_literals_with_absolute_values_equal_to_2_53_or_greater_are_too_large_to_be_represented_accurately_as_integers)
    );
  }
  function checkGrammarBigIntLiteral(node) {
    const literalType ***REMOVED*** isLiteralTypeNode(node.parent) || isPrefixUnaryExpression(node.parent) && isLiteralTypeNode(node.parent.parent);
    if (!literalType) {
      if (!(node.flags & 33554432 /* Ambient */) && languageVersion < 7 /* ES2020 */) {
        if (grammarErrorOnNode(node, Diagnostics.BigInt_literals_are_not_available_when_targeting_lower_than_ES2020)) {
          return true;
        }
      }
    }
    return false;
  }
  function grammarErrorAfterFirstToken(node, message, ...args) {
    const sourceFile ***REMOVED*** getSourceFileOfNode(node);
    if (!hasParseDiagnostics(sourceFile)) {
      const span ***REMOVED*** getSpanOfTokenAtPosition(sourceFile, node.pos);
      diagnostics.add(createFileDiagnostic(
        sourceFile,
        textSpanEnd(span),
        /*length*/
        0,
        message,
        ...args
      ));
      return true;
    }
    return false;
  }
  function getAmbientModules() {
    if (!ambientModulesCache) {
      ambientModulesCache ***REMOVED*** [];
      globals.forEach((global2, sym) ***REMOVED***> {
        if (ambientModuleSymbolRegex.test(sym)) {
          ambientModulesCache.push(global2);
        }
      });
    }
    return ambientModulesCache;
  }
  function checkGrammarImportClause(node) {
    var _a, _b;
    if (node.phaseModifier ***REMOVED******REMOVED******REMOVED*** 156 /* TypeKeyword */) {
      if (node.name && node.namedBindings) {
        return grammarErrorOnNode(node, Diagnostics.A_type_only_import_can_specify_a_default_import_or_named_bindings_but_not_both);
      }
      if (((_a ***REMOVED*** node.namedBindings) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** 276 /* NamedImports */) {
        return checkGrammarNamedImportsOrExports(node.namedBindings);
      }
    } else if (node.phaseModifier ***REMOVED******REMOVED******REMOVED*** 166 /* DeferKeyword */) {
      if (node.name) {
        return grammarErrorOnNode(node, Diagnostics.Default_imports_are_not_allowed_in_a_deferred_import);
      }
      if (((_b ***REMOVED*** node.namedBindings) ***REMOVED******REMOVED*** null ? void 0 : _b.kind) ***REMOVED******REMOVED******REMOVED*** 276 /* NamedImports */) {
        return grammarErrorOnNode(node, Diagnostics.Named_imports_are_not_allowed_in_a_deferred_import);
      }
      if (moduleKind !***REMOVED******REMOVED*** 99 /* ESNext */ && moduleKind !***REMOVED******REMOVED*** 200 /* Preserve */) {
        return grammarErrorOnNode(node, Diagnostics.Deferred_imports_are_only_supported_when_the_module_flag_is_set_to_esnext_or_preserve);
      }
    }
    return false;
  }
  function checkGrammarNamedImportsOrExports(namedBindings) {
    return !!forEach(namedBindings.elements, (specifier) ***REMOVED***> {
      if (specifier.isTypeOnly) {
        return grammarErrorOnFirstToken(
          specifier,
          specifier.kind ***REMOVED******REMOVED******REMOVED*** 277 /* ImportSpecifier */ ? Diagnostics.The_type_modifier_cannot_be_used_on_a_named_import_when_import_type_is_used_on_its_import_statement : Diagnostics.The_type_modifier_cannot_be_used_on_a_named_export_when_export_type_is_used_on_its_export_statement
        );
      }
    });
  }
  function checkGrammarImportCallExpression(node) {
    if (compilerOptions.verbatimModuleSyntax && moduleKind ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */) {
      return grammarErrorOnNode(node, getVerbatimModuleSyntaxErrorMessage(node));
    }
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 237 /* MetaProperty */) {
      if (moduleKind !***REMOVED******REMOVED*** 99 /* ESNext */ && moduleKind !***REMOVED******REMOVED*** 200 /* Preserve */) {
        return grammarErrorOnNode(node, Diagnostics.Deferred_imports_are_only_supported_when_the_module_flag_is_set_to_esnext_or_preserve);
      }
    } else if (moduleKind ***REMOVED******REMOVED******REMOVED*** 5 /* ES2015 */) {
      return grammarErrorOnNode(node, Diagnostics.Dynamic_imports_are_only_supported_when_the_module_flag_is_set_to_es2020_es2022_esnext_commonjs_amd_system_umd_node16_node18_node20_or_nodenext);
    }
    if (node.typeArguments) {
      return grammarErrorOnNode(node, Diagnostics.This_use_of_import_is_invalid_import_calls_can_be_written_but_they_must_have_parentheses_and_cannot_have_type_arguments);
    }
    const nodeArguments ***REMOVED*** node.arguments;
    if (!(100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) && moduleKind !***REMOVED******REMOVED*** 99 /* ESNext */ && moduleKind !***REMOVED******REMOVED*** 200 /* Preserve */) {
      checkGrammarForDisallowedTrailingComma(nodeArguments);
      if (nodeArguments.length > 1) {
        const importAttributesArgument ***REMOVED*** nodeArguments[1];
        return grammarErrorOnNode(importAttributesArgument, Diagnostics.Dynamic_imports_only_support_a_second_argument_when_the_module_option_is_set_to_esnext_node16_node18_node20_nodenext_or_preserve);
      }
    }
    if (nodeArguments.length ***REMOVED******REMOVED******REMOVED*** 0 || nodeArguments.length > 2) {
      return grammarErrorOnNode(node, Diagnostics.Dynamic_imports_can_only_accept_a_module_specifier_and_an_optional_set_of_attributes_as_arguments);
    }
    const spreadElement ***REMOVED*** find(nodeArguments, isSpreadElement);
    if (spreadElement) {
      return grammarErrorOnNode(spreadElement, Diagnostics.Argument_of_dynamic_import_cannot_be_spread_element);
    }
    return false;
  }
  function findMatchingTypeReferenceOrTypeAliasReference(source, unionTarget) {
    const sourceObjectFlags ***REMOVED*** getObjectFlags(source);
    if (sourceObjectFlags & (4 /* Reference */ | 16 /* Anonymous */) && unionTarget.flags & 1048576 /* Union */) {
      return find(unionTarget.types, (target) ***REMOVED***> {
        if (target.flags & 524288 /* Object */) {
          const overlapObjFlags ***REMOVED*** sourceObjectFlags & getObjectFlags(target);
          if (overlapObjFlags & 4 /* Reference */) {
            return source.target ***REMOVED******REMOVED******REMOVED*** target.target;
          }
          if (overlapObjFlags & 16 /* Anonymous */) {
            return !!source.aliasSymbol && source.aliasSymbol ***REMOVED******REMOVED******REMOVED*** target.aliasSymbol;
          }
        }
        return false;
      });
    }
  }
  function findBestTypeForObjectLiteral(source, unionTarget) {
    if (getObjectFlags(source) & 128 /* ObjectLiteral */ && someType(unionTarget, isArrayLikeType)) {
      return find(unionTarget.types, (t) ***REMOVED***> !isArrayLikeType(t));
    }
  }
  function findBestTypeForInvokable(source, unionTarget) {
    let signatureKind ***REMOVED*** 0 /* Call */;
    const hasSignatures2 ***REMOVED*** getSignaturesOfType(source, signatureKind).length > 0 || (signatureKind ***REMOVED*** 1 /* Construct */, getSignaturesOfType(source, signatureKind).length > 0);
    if (hasSignatures2) {
      return find(unionTarget.types, (t) ***REMOVED***> getSignaturesOfType(t, signatureKind).length > 0);
    }
  }
  function findMostOverlappyType(source, unionTarget) {
    let bestMatch;
    if (!(source.flags & (402784252 /* Primitive */ | 406847488 /* InstantiablePrimitive */))) {
      let matchingCount ***REMOVED*** 0;
      for (const target of unionTarget.types) {
        if (!(target.flags & (402784252 /* Primitive */ | 406847488 /* InstantiablePrimitive */))) {
          const overlap ***REMOVED*** getIntersectionType([getIndexType(source), getIndexType(target)]);
          if (overlap.flags & 4194304 /* Index */) {
            return target;
          } else if (isUnitType(overlap) || overlap.flags & 1048576 /* Union */) {
            const len ***REMOVED*** overlap.flags & 1048576 /* Union */ ? countWhere(overlap.types, isUnitType) : 1;
            if (len >***REMOVED*** matchingCount) {
              bestMatch ***REMOVED*** target;
              matchingCount ***REMOVED*** len;
            }
          }
        }
      }
    }
    return bestMatch;
  }
  function filterPrimitivesIfContainsNonPrimitive(type) {
    if (maybeTypeOfKind(type, 67108864 /* NonPrimitive */)) {
      const result ***REMOVED*** filterType(type, (t) ***REMOVED***> !(t.flags & 402784252 /* Primitive */));
      if (!(result.flags & 131072 /* Never */)) {
        return result;
      }
    }
    return type;
  }
  function findMatchingDiscriminantType(source, target, isRelatedTo) {
    if (target.flags & 1048576 /* Union */ && source.flags & (2097152 /* Intersection */ | 524288 /* Object */)) {
      const match ***REMOVED*** getMatchingUnionConstituentForType(target, source);
      if (match) {
        return match;
      }
      const sourceProperties ***REMOVED*** getPropertiesOfType(source);
      if (sourceProperties) {
        const sourcePropertiesFiltered ***REMOVED*** findDiscriminantProperties(sourceProperties, target);
        if (sourcePropertiesFiltered) {
          const discriminated ***REMOVED*** discriminateTypeByDiscriminableItems(target, map(sourcePropertiesFiltered, (p) ***REMOVED***> [() ***REMOVED***> getTypeOfSymbol(p), p.escapedName]), isRelatedTo);
          if (discriminated !***REMOVED******REMOVED*** target) {
            return discriminated;
          }
        }
      }
    }
    return void 0;
  }
  function getEffectivePropertyNameForPropertyNameNode(node) {
    const name ***REMOVED*** getPropertyNameForPropertyNameNode(node);
    return name ? name : isComputedPropertyName(node) ? tryGetNameFromType(getTypeOfExpression(node.expression)) : void 0;
  }
  function getCombinedModifierFlagsCached(node) {
    if (lastGetCombinedModifierFlagsNode ***REMOVED******REMOVED******REMOVED*** node) {
      return lastGetCombinedModifierFlagsResult;
    }
    lastGetCombinedModifierFlagsNode ***REMOVED*** node;
    lastGetCombinedModifierFlagsResult ***REMOVED*** getCombinedModifierFlags(node);
    return lastGetCombinedModifierFlagsResult;
  }
  function getCombinedNodeFlagsCached(node) {
    if (lastGetCombinedNodeFlagsNode ***REMOVED******REMOVED******REMOVED*** node) {
      return lastGetCombinedNodeFlagsResult;
    }
    lastGetCombinedNodeFlagsNode ***REMOVED*** node;
    lastGetCombinedNodeFlagsResult ***REMOVED*** getCombinedNodeFlags(node);
    return lastGetCombinedNodeFlagsResult;
  }
  function isVarConstLike2(node) {
    const blockScopeKind ***REMOVED*** getCombinedNodeFlagsCached(node) & 7 /* BlockScoped */;
    return blockScopeKind ***REMOVED******REMOVED******REMOVED*** 2 /* Const */ || blockScopeKind ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ || blockScopeKind ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */;
  }
  function getJSXRuntimeImportSpecifier(file, specifierText) {
    const jsxImportIndex ***REMOVED*** compilerOptions.importHelpers ? 1 : 0;
    const specifier ***REMOVED*** file ***REMOVED******REMOVED*** null ? void 0 : file.imports[jsxImportIndex];
    if (specifier) {
      Debug.assert(nodeIsSynthesized(specifier) && specifier.text ***REMOVED******REMOVED******REMOVED*** specifierText, `Expected sourceFile.imports[${jsxImportIndex}] to be the synthesized JSX runtime import`);
    }
    return specifier;
  }
  function getImportHelpersImportSpecifier(file) {
    Debug.assert(compilerOptions.importHelpers, "Expected importHelpers to be enabled");
    const specifier ***REMOVED*** file.imports[0];
    Debug.assert(specifier && nodeIsSynthesized(specifier) && specifier.text ***REMOVED******REMOVED******REMOVED*** "tslib", `Expected sourceFile.imports[0] to be the synthesized tslib import`);
    return specifier;
  }
}
function isNotAccessor(declaration) {
  return !isAccessor(declaration);
}
function isNotOverload(declaration) {
  return declaration.kind !***REMOVED******REMOVED*** 263 /* FunctionDeclaration */ && declaration.kind !***REMOVED******REMOVED*** 175 /* MethodDeclaration */ || !!declaration.body;
}
function isDeclarationNameOrImportPropertyName(name) {
  switch (name.parent.kind) {
    case 277 /* ImportSpecifier */:
    case 282 /* ExportSpecifier */:
      return isIdentifier(name) || name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */;
    default:
      return isDeclarationName(name);
  }
}
var JsxNames;
((JsxNames2) ***REMOVED***> {
  JsxNames2.JSX ***REMOVED*** "JSX";
  JsxNames2.IntrinsicElements ***REMOVED*** "IntrinsicElements";
  JsxNames2.ElementClass ***REMOVED*** "ElementClass";
  JsxNames2.ElementAttributesPropertyNameContainer ***REMOVED*** "ElementAttributesProperty";
  JsxNames2.ElementChildrenAttributeNameContainer ***REMOVED*** "ElementChildrenAttribute";
  JsxNames2.Element ***REMOVED*** "Element";
  JsxNames2.ElementType ***REMOVED*** "ElementType";
  JsxNames2.IntrinsicAttributes ***REMOVED*** "IntrinsicAttributes";
  JsxNames2.IntrinsicClassAttributes ***REMOVED*** "IntrinsicClassAttributes";
  JsxNames2.LibraryManagedAttributes ***REMOVED*** "LibraryManagedAttributes";
})(JsxNames || (JsxNames ***REMOVED*** {}));
var ReactNames;
((ReactNames2) ***REMOVED***> {
  ReactNames2.Fragment ***REMOVED*** "Fragment";
})(ReactNames || (ReactNames ***REMOVED*** {}));
function getIterationTypesKeyFromIterationTypeKind(typeKind) {
  switch (typeKind) {
    case 0 /* Yield */:
      return "yieldType";
    case 1 /* Return */:
      return "returnType";
    case 2 /* Next */:
      return "nextType";
  }
}
function signatureHasRestParameter(s) {
  return !!(s.flags & 1 /* HasRestParameter */);
}
function signatureHasLiteralTypes(s) {
  return !!(s.flags & 2 /* HasLiteralTypes */);
}
function createBasicNodeBuilderModuleSpecifierResolutionHost(host) {
  return {
    getCommonSourceDirectory: !!host.getCommonSourceDirectory ? () ***REMOVED***> host.getCommonSourceDirectory() : () ***REMOVED***> "",
    getCurrentDirectory: () ***REMOVED***> host.getCurrentDirectory(),
    getSymlinkCache: maybeBind(host, host.getSymlinkCache),
    getPackageJsonInfoCache: () ***REMOVED***> {
      var _a;
      return (_a ***REMOVED*** host.getPackageJsonInfoCache) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host);
    },
    useCaseSensitiveFileNames: () ***REMOVED***> host.useCaseSensitiveFileNames(),
    redirectTargetsMap: host.redirectTargetsMap,
    getRedirectFromSourceFile: (fileName) ***REMOVED***> host.getRedirectFromSourceFile(fileName),
    isSourceOfProjectReferenceRedirect: (fileName) ***REMOVED***> host.isSourceOfProjectReferenceRedirect(fileName),
    fileExists: (fileName) ***REMOVED***> host.fileExists(fileName),
    getFileIncludeReasons: () ***REMOVED***> host.getFileIncludeReasons(),
    readFile: host.readFile ? (fileName) ***REMOVED***> host.readFile(fileName) : void 0,
    getDefaultResolutionModeForFile: (file) ***REMOVED***> host.getDefaultResolutionModeForFile(file),
    getModeForResolutionAtIndex: (file, index) ***REMOVED***> host.getModeForResolutionAtIndex(file, index),
    getGlobalTypingsCacheLocation: maybeBind(host, host.getGlobalTypingsCacheLocation)
  };
}
var SymbolTrackerImpl ***REMOVED*** class _SymbolTrackerImpl {
  constructor(context, tracker, moduleResolverHost) {
    this.moduleResolverHost ***REMOVED*** void 0;
    this.inner ***REMOVED*** void 0;
    this.disableTrackSymbol ***REMOVED*** false;
    var _a;
    while (tracker instanceof _SymbolTrackerImpl) {
      tracker ***REMOVED*** tracker.inner;
    }
    this.inner ***REMOVED*** tracker;
    this.moduleResolverHost ***REMOVED*** moduleResolverHost;
    this.context ***REMOVED*** context;
    this.canTrackSymbol ***REMOVED*** !!((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.trackSymbol);
  }
  trackSymbol(symbol, enclosingDeclaration, meaning) {
    var _a, _b;
    if (((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.trackSymbol) && !this.disableTrackSymbol) {
      if (this.inner.trackSymbol(symbol, enclosingDeclaration, meaning)) {
        this.onDiagnosticReported();
        return true;
      }
      if (!(symbol.flags & 262144 /* TypeParameter */)) ((_b ***REMOVED*** this.context).trackedSymbols ?? (_b.trackedSymbols ***REMOVED*** [])).push([symbol, enclosingDeclaration, meaning]);
    }
    return false;
  }
  reportInaccessibleThisError() {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportInaccessibleThisError) {
      this.onDiagnosticReported();
      this.inner.reportInaccessibleThisError();
    }
  }
  reportPrivateInBaseOfClassExpression(propertyName) {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportPrivateInBaseOfClassExpression) {
      this.onDiagnosticReported();
      this.inner.reportPrivateInBaseOfClassExpression(propertyName);
    }
  }
  reportInaccessibleUniqueSymbolError() {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportInaccessibleUniqueSymbolError) {
      this.onDiagnosticReported();
      this.inner.reportInaccessibleUniqueSymbolError();
    }
  }
  reportCyclicStructureError() {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportCyclicStructureError) {
      this.onDiagnosticReported();
      this.inner.reportCyclicStructureError();
    }
  }
  reportLikelyUnsafeImportRequiredError(specifier) {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportLikelyUnsafeImportRequiredError) {
      this.onDiagnosticReported();
      this.inner.reportLikelyUnsafeImportRequiredError(specifier);
    }
  }
  reportTruncationError() {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportTruncationError) {
      this.onDiagnosticReported();
      this.inner.reportTruncationError();
    }
  }
  reportNonlocalAugmentation(containingFile, parentSymbol, augmentingSymbol) {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportNonlocalAugmentation) {
      this.onDiagnosticReported();
      this.inner.reportNonlocalAugmentation(containingFile, parentSymbol, augmentingSymbol);
    }
  }
  reportNonSerializableProperty(propertyName) {
    var _a;
    if ((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportNonSerializableProperty) {
      this.onDiagnosticReported();
      this.inner.reportNonSerializableProperty(propertyName);
    }
  }
  onDiagnosticReported() {
    this.context.reportedDiagnostic ***REMOVED*** true;
  }
  reportInferenceFallback(node) {
    var _a;
    if (((_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.reportInferenceFallback) && !this.context.suppressReportInferenceFallback) {
      this.onDiagnosticReported();
      this.inner.reportInferenceFallback(node);
    }
  }
  pushErrorFallbackNode(node) {
    var _a, _b;
    return (_b ***REMOVED*** (_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.pushErrorFallbackNode) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a, node);
  }
  popErrorFallbackNode() {
    var _a, _b;
    return (_b ***REMOVED*** (_a ***REMOVED*** this.inner) ***REMOVED******REMOVED*** null ? void 0 : _a.popErrorFallbackNode) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a);
  }
};

// src/compiler/visitorPublic.ts
function visitNode(node, visitor, test, lift) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
    return node;
  }
  const visited ***REMOVED*** visitor(node);
  let visitedNode;
  if (visited ***REMOVED******REMOVED******REMOVED*** void 0) {
    return void 0;
  } else if (isArray(visited)) {
    visitedNode ***REMOVED*** (lift || extractSingleNode)(visited);
  } else {
    visitedNode ***REMOVED*** visited;
  }
  Debug.assertNode(visitedNode, test);
  return visitedNode;
}
function visitNodes2(nodes, visitor, test, start, count) {
  if (nodes ***REMOVED******REMOVED******REMOVED*** void 0) {
    return nodes;
  }
  const length2 ***REMOVED*** nodes.length;
  if (start ***REMOVED******REMOVED******REMOVED*** void 0 || start < 0) {
    start ***REMOVED*** 0;
  }
  if (count ***REMOVED******REMOVED******REMOVED*** void 0 || count > length2 - start) {
    count ***REMOVED*** length2 - start;
  }
  let hasTrailingComma;
  let pos ***REMOVED*** -1;
  let end ***REMOVED*** -1;
  if (start > 0 || count < length2) {
    hasTrailingComma ***REMOVED*** nodes.hasTrailingComma && start + count ***REMOVED******REMOVED******REMOVED*** length2;
  } else {
    pos ***REMOVED*** nodes.pos;
    end ***REMOVED*** nodes.end;
    hasTrailingComma ***REMOVED*** nodes.hasTrailingComma;
  }
  const updated ***REMOVED*** visitArrayWorker(nodes, visitor, test, start, count);
  if (updated !***REMOVED******REMOVED*** nodes) {
    const updatedArray ***REMOVED*** factory.createNodeArray(updated, hasTrailingComma);
    setTextRangePosEnd(updatedArray, pos, end);
    return updatedArray;
  }
  return nodes;
}
function visitArray(nodes, visitor, test, start, count) {
  if (nodes ***REMOVED******REMOVED******REMOVED*** void 0) {
    return nodes;
  }
  const length2 ***REMOVED*** nodes.length;
  if (start ***REMOVED******REMOVED******REMOVED*** void 0 || start < 0) {
    start ***REMOVED*** 0;
  }
  if (count ***REMOVED******REMOVED******REMOVED*** void 0 || count > length2 - start) {
    count ***REMOVED*** length2 - start;
  }
  return visitArrayWorker(nodes, visitor, test, start, count);
}
function visitArrayWorker(nodes, visitor, test, start, count) {
  let updated;
  const length2 ***REMOVED*** nodes.length;
  if (start > 0 || count < length2) {
    updated ***REMOVED*** [];
  }
  for (let i ***REMOVED*** 0; i < count; i++) {
    const node ***REMOVED*** nodes[i + start];
    const visited ***REMOVED*** node !***REMOVED******REMOVED*** void 0 ? visitor ? visitor(node) : node : void 0;
    if (updated !***REMOVED******REMOVED*** void 0 || visited ***REMOVED******REMOVED******REMOVED*** void 0 || visited !***REMOVED******REMOVED*** node) {
      if (updated ***REMOVED******REMOVED******REMOVED*** void 0) {
        updated ***REMOVED*** nodes.slice(0, i);
        Debug.assertEachNode(updated, test);
      }
      if (visited) {
        if (isArray(visited)) {
          for (const visitedNode of visited) {
            Debug.assertNode(visitedNode, test);
            updated.push(visitedNode);
          }
        } else {
          Debug.assertNode(visited, test);
          updated.push(visited);
        }
      }
    }
  }
  if (updated) {
    return updated;
  }
  Debug.assertEachNode(nodes, test);
  return nodes;
}
function visitLexicalEnvironment(statements, visitor, context, start, ensureUseStrict, nodesVisitor ***REMOVED*** visitNodes2) {
  context.startLexicalEnvironment();
  statements ***REMOVED*** nodesVisitor(statements, visitor, isStatement, start);
  if (ensureUseStrict) statements ***REMOVED*** context.factory.ensureUseStrict(statements);
  return factory.mergeLexicalEnvironment(statements, context.endLexicalEnvironment());
}
function visitParameterList(nodes, visitor, context, nodesVisitor ***REMOVED*** visitNodes2) {
  let updated;
  context.startLexicalEnvironment();
  if (nodes) {
    context.setLexicalEnvironmentFlags(1 /* InParameters */, true);
    updated ***REMOVED*** nodesVisitor(nodes, visitor, isParameter);
    if (context.getLexicalEnvironmentFlags() & 2 /* VariablesHoistedInParameters */ && getEmitScriptTarget(context.getCompilerOptions()) >***REMOVED*** 2 /* ES2015 */) {
      updated ***REMOVED*** addDefaultValueAssignmentsIfNeeded(updated, context);
    }
    context.setLexicalEnvironmentFlags(1 /* InParameters */, false);
  }
  context.suspendLexicalEnvironment();
  return updated;
}
function addDefaultValueAssignmentsIfNeeded(parameters, context) {
  let result;
  for (let i ***REMOVED*** 0; i < parameters.length; i++) {
    const parameter ***REMOVED*** parameters[i];
    const updated ***REMOVED*** addDefaultValueAssignmentIfNeeded(parameter, context);
    if (result || updated !***REMOVED******REMOVED*** parameter) {
      if (!result) result ***REMOVED*** parameters.slice(0, i);
      result[i] ***REMOVED*** updated;
    }
  }
  if (result) {
    return setTextRange(context.factory.createNodeArray(result, parameters.hasTrailingComma), parameters);
  }
  return parameters;
}
function addDefaultValueAssignmentIfNeeded(parameter, context) {
  return parameter.dotDotDotToken ? parameter : isBindingPattern(parameter.name) ? addDefaultValueAssignmentForBindingPattern(parameter, context) : parameter.initializer ? addDefaultValueAssignmentForInitializer(parameter, parameter.name, parameter.initializer, context) : parameter;
}
function addDefaultValueAssignmentForBindingPattern(parameter, context) {
  const { factory: factory2 } ***REMOVED*** context;
  context.addInitializationStatement(
    factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      factory2.createVariableDeclarationList([
        factory2.createVariableDeclaration(
          parameter.name,
          /*exclamationToken*/
          void 0,
          parameter.type,
          parameter.initializer ? factory2.createConditionalExpression(
            factory2.createStrictEquality(
              factory2.getGeneratedNameForNode(parameter),
              factory2.createVoidZero()
            ),
            /*questionToken*/
            void 0,
            parameter.initializer,
            /*colonToken*/
            void 0,
            factory2.getGeneratedNameForNode(parameter)
          ) : factory2.getGeneratedNameForNode(parameter)
        )
      ])
    )
  );
  return factory2.updateParameterDeclaration(
    parameter,
    parameter.modifiers,
    parameter.dotDotDotToken,
    factory2.getGeneratedNameForNode(parameter),
    parameter.questionToken,
    parameter.type,
    /*initializer*/
    void 0
  );
}
function addDefaultValueAssignmentForInitializer(parameter, name, initializer, context) {
  const factory2 ***REMOVED*** context.factory;
  context.addInitializationStatement(
    factory2.createIfStatement(
      factory2.createTypeCheck(factory2.cloneNode(name), "undefined"),
      setEmitFlags(
        setTextRange(
          factory2.createBlock([
            factory2.createExpressionStatement(
              setEmitFlags(
                setTextRange(
                  factory2.createAssignment(
                    setEmitFlags(factory2.cloneNode(name), 96 /* NoSourceMap */),
                    setEmitFlags(initializer, 96 /* NoSourceMap */ | getEmitFlags(initializer) | 3072 /* NoComments */)
                  ),
                  parameter
                ),
                3072 /* NoComments */
              )
            )
          ]),
          parameter
        ),
        1 /* SingleLine */ | 64 /* NoTrailingSourceMap */ | 768 /* NoTokenSourceMaps */ | 3072 /* NoComments */
      )
    )
  );
  return factory2.updateParameterDeclaration(
    parameter,
    parameter.modifiers,
    parameter.dotDotDotToken,
    parameter.name,
    parameter.questionToken,
    parameter.type,
    /*initializer*/
    void 0
  );
}
function visitFunctionBody(node, visitor, context, nodeVisitor ***REMOVED*** visitNode) {
  context.resumeLexicalEnvironment();
  const updated ***REMOVED*** nodeVisitor(node, visitor, isConciseBody);
  const declarations ***REMOVED*** context.endLexicalEnvironment();
  if (some(declarations)) {
    if (!updated) {
      return context.factory.createBlock(declarations);
    }
    const block ***REMOVED*** context.factory.converters.convertToFunctionBlock(updated);
    const statements ***REMOVED*** factory.mergeLexicalEnvironment(block.statements, declarations);
    return context.factory.updateBlock(block, statements);
  }
  return updated;
}
function visitIterationBody(body, visitor, context, nodeVisitor ***REMOVED*** visitNode) {
  context.startBlockScope();
  const updated ***REMOVED*** nodeVisitor(body, visitor, isStatement, context.factory.liftToBlock);
  Debug.assert(updated);
  const declarations ***REMOVED*** context.endBlockScope();
  if (some(declarations)) {
    if (isBlock(updated)) {
      declarations.push(...updated.statements);
      return context.factory.updateBlock(updated, declarations);
    }
    declarations.push(updated);
    return context.factory.createBlock(declarations);
  }
  return updated;
}
function visitCommaListElements(elements, visitor, discardVisitor ***REMOVED*** visitor) {
  if (discardVisitor ***REMOVED******REMOVED******REMOVED*** visitor || elements.length <***REMOVED*** 1) {
    return visitNodes2(elements, visitor, isExpression);
  }
  let i ***REMOVED*** 0;
  const length2 ***REMOVED*** elements.length;
  return visitNodes2(elements, (node) ***REMOVED***> {
    const discarded ***REMOVED*** i < length2 - 1;
    i++;
    return discarded ? discardVisitor(node) : visitor(node);
  }, isExpression);
}
function visitEachChild(node, visitor, context ***REMOVED*** nullTransformationContext, nodesVisitor ***REMOVED*** visitNodes2, tokenVisitor, nodeVisitor ***REMOVED*** visitNode) {
  if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
    return void 0;
  }
  const fn ***REMOVED*** visitEachChildTable[node.kind];
  return fn ***REMOVED******REMOVED******REMOVED*** void 0 ? node : fn(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor);
}
var visitEachChildTable ***REMOVED*** {
  [167 /* QualifiedName */]: function visitEachChildOfQualifiedName(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateQualifiedName(
      node,
      Debug.checkDefined(nodeVisitor(node.left, visitor, isEntityName)),
      Debug.checkDefined(nodeVisitor(node.right, visitor, isIdentifier))
    );
  },
  [168 /* ComputedPropertyName */]: function visitEachChildOfComputedPropertyName(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateComputedPropertyName(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  // Signature elements
  [169 /* TypeParameter */]: function visitEachChildOfTypeParameterDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeParameterDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      nodeVisitor(node.constraint, visitor, isTypeNode),
      nodeVisitor(node.default, visitor, isTypeNode)
    );
  },
  [170 /* Parameter */]: function visitEachChildOfParameterDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateParameterDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      tokenVisitor ? nodeVisitor(node.dotDotDotToken, tokenVisitor, isDotDotDotToken) : node.dotDotDotToken,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isBindingName)),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken) : node.questionToken,
      nodeVisitor(node.type, visitor, isTypeNode),
      nodeVisitor(node.initializer, visitor, isExpression)
    );
  },
  [171 /* Decorator */]: function visitEachChildOfDecorator(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateDecorator(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  // Type elements
  [172 /* PropertySignature */]: function visitEachChildOfPropertySignature(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updatePropertySignature(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken) : node.questionToken,
      nodeVisitor(node.type, visitor, isTypeNode)
    );
  },
  [173 /* PropertyDeclaration */]: function visitEachChildOfPropertyDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updatePropertyDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      // QuestionToken and ExclamationToken are mutually exclusive in PropertyDeclaration
      tokenVisitor ? nodeVisitor(node.questionToken ?? node.exclamationToken, tokenVisitor, isQuestionOrExclamationToken) : node.questionToken ?? node.exclamationToken,
      nodeVisitor(node.type, visitor, isTypeNode),
      nodeVisitor(node.initializer, visitor, isExpression)
    );
  },
  [174 /* MethodSignature */]: function visitEachChildOfMethodSignature(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateMethodSignature(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken) : node.questionToken,
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.parameters, visitor, isParameter),
      nodeVisitor(node.type, visitor, isTypeNode)
    );
  },
  [175 /* MethodDeclaration */]: function visitEachChildOfMethodDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateMethodDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      tokenVisitor ? nodeVisitor(node.asteriskToken, tokenVisitor, isAsteriskToken) : node.asteriskToken,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken) : node.questionToken,
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      nodeVisitor(node.type, visitor, isTypeNode),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [177 /* Constructor */]: function visitEachChildOfConstructorDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateConstructorDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [178 /* GetAccessor */]: function visitEachChildOfGetAccessorDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateGetAccessorDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      nodeVisitor(node.type, visitor, isTypeNode),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [179 /* SetAccessor */]: function visitEachChildOfSetAccessorDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateSetAccessorDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [176 /* ClassStaticBlockDeclaration */]: function visitEachChildOfClassStaticBlockDeclaration(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    context.startLexicalEnvironment();
    context.suspendLexicalEnvironment();
    return context.factory.updateClassStaticBlockDeclaration(
      node,
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [180 /* CallSignature */]: function visitEachChildOfCallSignatureDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateCallSignature(
      node,
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.parameters, visitor, isParameter),
      nodeVisitor(node.type, visitor, isTypeNode)
    );
  },
  [181 /* ConstructSignature */]: function visitEachChildOfConstructSignatureDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateConstructSignature(
      node,
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.parameters, visitor, isParameter),
      nodeVisitor(node.type, visitor, isTypeNode)
    );
  },
  [182 /* IndexSignature */]: function visitEachChildOfIndexSignatureDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateIndexSignature(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      nodesVisitor(node.parameters, visitor, isParameter),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  // Types
  [183 /* TypePredicate */]: function visitEachChildOfTypePredicateNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypePredicateNode(
      node,
      nodeVisitor(node.assertsModifier, visitor, isAssertsKeyword),
      Debug.checkDefined(nodeVisitor(node.parameterName, visitor, isIdentifierOrThisTypeNode)),
      nodeVisitor(node.type, visitor, isTypeNode)
    );
  },
  [184 /* TypeReference */]: function visitEachChildOfTypeReferenceNode(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeReferenceNode(
      node,
      Debug.checkDefined(nodeVisitor(node.typeName, visitor, isEntityName)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode)
    );
  },
  [185 /* FunctionType */]: function visitEachChildOfFunctionTypeNode(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateFunctionTypeNode(
      node,
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.parameters, visitor, isParameter),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [186 /* ConstructorType */]: function visitEachChildOfConstructorTypeNode(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateConstructorTypeNode(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.parameters, visitor, isParameter),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [187 /* TypeQuery */]: function visitEachChildOfTypeQueryNode(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeQueryNode(
      node,
      Debug.checkDefined(nodeVisitor(node.exprName, visitor, isEntityName)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode)
    );
  },
  [188 /* TypeLiteral */]: function visitEachChildOfTypeLiteralNode(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeLiteralNode(
      node,
      nodesVisitor(node.members, visitor, isTypeElement)
    );
  },
  [189 /* ArrayType */]: function visitEachChildOfArrayTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateArrayTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.elementType, visitor, isTypeNode))
    );
  },
  [190 /* TupleType */]: function visitEachChildOfTupleTypeNode(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateTupleTypeNode(
      node,
      nodesVisitor(node.elements, visitor, isTypeNode)
    );
  },
  [191 /* OptionalType */]: function visitEachChildOfOptionalTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateOptionalTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [192 /* RestType */]: function visitEachChildOfRestTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateRestTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [193 /* UnionType */]: function visitEachChildOfUnionTypeNode(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateUnionTypeNode(
      node,
      nodesVisitor(node.types, visitor, isTypeNode)
    );
  },
  [194 /* IntersectionType */]: function visitEachChildOfIntersectionTypeNode(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateIntersectionTypeNode(
      node,
      nodesVisitor(node.types, visitor, isTypeNode)
    );
  },
  [195 /* ConditionalType */]: function visitEachChildOfConditionalTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateConditionalTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.checkType, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.extendsType, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.trueType, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.falseType, visitor, isTypeNode))
    );
  },
  [196 /* InferType */]: function visitEachChildOfInferTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateInferTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.typeParameter, visitor, isTypeParameterDeclaration))
    );
  },
  [206 /* ImportType */]: function visitEachChildOfImportTypeNode(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.argument, visitor, isTypeNode)),
      nodeVisitor(node.attributes, visitor, isImportAttributes),
      nodeVisitor(node.qualifier, visitor, isEntityName),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      node.isTypeOf
    );
  },
  [303 /* ImportTypeAssertionContainer */]: function visitEachChildOfImportTypeAssertionContainer(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportTypeAssertionContainer(
      node,
      Debug.checkDefined(nodeVisitor(node.assertClause, visitor, isAssertClause)),
      node.multiLine
    );
  },
  [203 /* NamedTupleMember */]: function visitEachChildOfNamedTupleMember(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateNamedTupleMember(
      node,
      tokenVisitor ? nodeVisitor(node.dotDotDotToken, tokenVisitor, isDotDotDotToken) : node.dotDotDotToken,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken) : node.questionToken,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [197 /* ParenthesizedType */]: function visitEachChildOfParenthesizedType(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateParenthesizedType(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [199 /* TypeOperator */]: function visitEachChildOfTypeOperatorNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeOperatorNode(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [200 /* IndexedAccessType */]: function visitEachChildOfIndexedAccessType(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateIndexedAccessTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.objectType, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.indexType, visitor, isTypeNode))
    );
  },
  [201 /* MappedType */]: function visitEachChildOfMappedType(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateMappedTypeNode(
      node,
      tokenVisitor ? nodeVisitor(node.readonlyToken, tokenVisitor, isReadonlyKeywordOrPlusOrMinusToken) : node.readonlyToken,
      Debug.checkDefined(nodeVisitor(node.typeParameter, visitor, isTypeParameterDeclaration)),
      nodeVisitor(node.nameType, visitor, isTypeNode),
      tokenVisitor ? nodeVisitor(node.questionToken, tokenVisitor, isQuestionOrPlusOrMinusToken) : node.questionToken,
      nodeVisitor(node.type, visitor, isTypeNode),
      nodesVisitor(node.members, visitor, isTypeElement)
    );
  },
  [202 /* LiteralType */]: function visitEachChildOfLiteralTypeNode(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateLiteralTypeNode(
      node,
      Debug.checkDefined(nodeVisitor(node.literal, visitor, isLiteralTypeLiteral))
    );
  },
  [204 /* TemplateLiteralType */]: function visitEachChildOfTemplateLiteralType(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTemplateLiteralType(
      node,
      Debug.checkDefined(nodeVisitor(node.head, visitor, isTemplateHead)),
      nodesVisitor(node.templateSpans, visitor, isTemplateLiteralTypeSpan)
    );
  },
  [205 /* TemplateLiteralTypeSpan */]: function visitEachChildOfTemplateLiteralTypeSpan(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTemplateLiteralTypeSpan(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.literal, visitor, isTemplateMiddleOrTemplateTail))
    );
  },
  // Binding patterns
  [207 /* ObjectBindingPattern */]: function visitEachChildOfObjectBindingPattern(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateObjectBindingPattern(
      node,
      nodesVisitor(node.elements, visitor, isBindingElement)
    );
  },
  [208 /* ArrayBindingPattern */]: function visitEachChildOfArrayBindingPattern(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateArrayBindingPattern(
      node,
      nodesVisitor(node.elements, visitor, isArrayBindingElement)
    );
  },
  [209 /* BindingElement */]: function visitEachChildOfBindingElement(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateBindingElement(
      node,
      tokenVisitor ? nodeVisitor(node.dotDotDotToken, tokenVisitor, isDotDotDotToken) : node.dotDotDotToken,
      nodeVisitor(node.propertyName, visitor, isPropertyName),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isBindingName)),
      nodeVisitor(node.initializer, visitor, isExpression)
    );
  },
  // Expression
  [210 /* ArrayLiteralExpression */]: function visitEachChildOfArrayLiteralExpression(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateArrayLiteralExpression(
      node,
      nodesVisitor(node.elements, visitor, isExpression)
    );
  },
  [211 /* ObjectLiteralExpression */]: function visitEachChildOfObjectLiteralExpression(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateObjectLiteralExpression(
      node,
      nodesVisitor(node.properties, visitor, isObjectLiteralElementLike)
    );
  },
  [212 /* PropertyAccessExpression */]: function visitEachChildOfPropertyAccessExpression(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return isPropertyAccessChain(node) ? context.factory.updatePropertyAccessChain(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      tokenVisitor ? nodeVisitor(node.questionDotToken, tokenVisitor, isQuestionDotToken) : node.questionDotToken,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isMemberName))
    ) : context.factory.updatePropertyAccessExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isMemberName))
    );
  },
  [213 /* ElementAccessExpression */]: function visitEachChildOfElementAccessExpression(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return isElementAccessChain(node) ? context.factory.updateElementAccessChain(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      tokenVisitor ? nodeVisitor(node.questionDotToken, tokenVisitor, isQuestionDotToken) : node.questionDotToken,
      Debug.checkDefined(nodeVisitor(node.argumentExpression, visitor, isExpression))
    ) : context.factory.updateElementAccessExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.argumentExpression, visitor, isExpression))
    );
  },
  [214 /* CallExpression */]: function visitEachChildOfCallExpression(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return isCallChain(node) ? context.factory.updateCallChain(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      tokenVisitor ? nodeVisitor(node.questionDotToken, tokenVisitor, isQuestionDotToken) : node.questionDotToken,
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      nodesVisitor(node.arguments, visitor, isExpression)
    ) : context.factory.updateCallExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      nodesVisitor(node.arguments, visitor, isExpression)
    );
  },
  [215 /* NewExpression */]: function visitEachChildOfNewExpression(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateNewExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      nodesVisitor(node.arguments, visitor, isExpression)
    );
  },
  [216 /* TaggedTemplateExpression */]: function visitEachChildOfTaggedTemplateExpression(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTaggedTemplateExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.tag, visitor, isExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      Debug.checkDefined(nodeVisitor(node.template, visitor, isTemplateLiteral))
    );
  },
  [217 /* TypeAssertionExpression */]: function visitEachChildOfTypeAssertionExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeAssertion(
      node,
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode)),
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [218 /* ParenthesizedExpression */]: function visitEachChildOfParenthesizedExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateParenthesizedExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [219 /* FunctionExpression */]: function visitEachChildOfFunctionExpression(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateFunctionExpression(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      tokenVisitor ? nodeVisitor(node.asteriskToken, tokenVisitor, isAsteriskToken) : node.asteriskToken,
      nodeVisitor(node.name, visitor, isIdentifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      nodeVisitor(node.type, visitor, isTypeNode),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [220 /* ArrowFunction */]: function visitEachChildOfArrowFunction(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateArrowFunction(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      nodeVisitor(node.type, visitor, isTypeNode),
      tokenVisitor ? Debug.checkDefined(nodeVisitor(node.equalsGreaterThanToken, tokenVisitor, isEqualsGreaterThanToken)) : node.equalsGreaterThanToken,
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [221 /* DeleteExpression */]: function visitEachChildOfDeleteExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateDeleteExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [222 /* TypeOfExpression */]: function visitEachChildOfTypeOfExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeOfExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [223 /* VoidExpression */]: function visitEachChildOfVoidExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateVoidExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [224 /* AwaitExpression */]: function visitEachChildOfAwaitExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateAwaitExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [225 /* PrefixUnaryExpression */]: function visitEachChildOfPrefixUnaryExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updatePrefixUnaryExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.operand, visitor, isExpression))
    );
  },
  [226 /* PostfixUnaryExpression */]: function visitEachChildOfPostfixUnaryExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updatePostfixUnaryExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.operand, visitor, isExpression))
    );
  },
  [227 /* BinaryExpression */]: function visitEachChildOfBinaryExpression(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateBinaryExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.left, visitor, isExpression)),
      tokenVisitor ? Debug.checkDefined(nodeVisitor(node.operatorToken, tokenVisitor, isBinaryOperatorToken)) : node.operatorToken,
      Debug.checkDefined(nodeVisitor(node.right, visitor, isExpression))
    );
  },
  [228 /* ConditionalExpression */]: function visitEachChildOfConditionalExpression(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateConditionalExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.condition, visitor, isExpression)),
      tokenVisitor ? Debug.checkDefined(nodeVisitor(node.questionToken, tokenVisitor, isQuestionToken)) : node.questionToken,
      Debug.checkDefined(nodeVisitor(node.whenTrue, visitor, isExpression)),
      tokenVisitor ? Debug.checkDefined(nodeVisitor(node.colonToken, tokenVisitor, isColonToken)) : node.colonToken,
      Debug.checkDefined(nodeVisitor(node.whenFalse, visitor, isExpression))
    );
  },
  [229 /* TemplateExpression */]: function visitEachChildOfTemplateExpression(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTemplateExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.head, visitor, isTemplateHead)),
      nodesVisitor(node.templateSpans, visitor, isTemplateSpan)
    );
  },
  [230 /* YieldExpression */]: function visitEachChildOfYieldExpression(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateYieldExpression(
      node,
      tokenVisitor ? nodeVisitor(node.asteriskToken, tokenVisitor, isAsteriskToken) : node.asteriskToken,
      nodeVisitor(node.expression, visitor, isExpression)
    );
  },
  [231 /* SpreadElement */]: function visitEachChildOfSpreadElement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateSpreadElement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [232 /* ClassExpression */]: function visitEachChildOfClassExpression(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateClassExpression(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      nodeVisitor(node.name, visitor, isIdentifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.heritageClauses, visitor, isHeritageClause),
      nodesVisitor(node.members, visitor, isClassElement)
    );
  },
  [234 /* ExpressionWithTypeArguments */]: function visitEachChildOfExpressionWithTypeArguments(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExpressionWithTypeArguments(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode)
    );
  },
  [235 /* AsExpression */]: function visitEachChildOfAsExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateAsExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [239 /* SatisfiesExpression */]: function visitEachChildOfSatisfiesExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateSatisfiesExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [236 /* NonNullExpression */]: function visitEachChildOfNonNullExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return isOptionalChain(node) ? context.factory.updateNonNullChain(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    ) : context.factory.updateNonNullExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [237 /* MetaProperty */]: function visitEachChildOfMetaProperty(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateMetaProperty(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  // Misc
  [240 /* TemplateSpan */]: function visitEachChildOfTemplateSpan(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTemplateSpan(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.literal, visitor, isTemplateMiddleOrTemplateTail))
    );
  },
  // Element
  [242 /* Block */]: function visitEachChildOfBlock(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateBlock(
      node,
      nodesVisitor(node.statements, visitor, isStatement)
    );
  },
  [244 /* VariableStatement */]: function visitEachChildOfVariableStatement(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateVariableStatement(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.declarationList, visitor, isVariableDeclarationList))
    );
  },
  [245 /* ExpressionStatement */]: function visitEachChildOfExpressionStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExpressionStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [246 /* IfStatement */]: function visitEachChildOfIfStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateIfStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.thenStatement, visitor, isStatement, context.factory.liftToBlock)),
      nodeVisitor(node.elseStatement, visitor, isStatement, context.factory.liftToBlock)
    );
  },
  [247 /* DoStatement */]: function visitEachChildOfDoStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateDoStatement(
      node,
      visitIterationBody(node.statement, visitor, context, nodeVisitor),
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [248 /* WhileStatement */]: function visitEachChildOfWhileStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateWhileStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      visitIterationBody(node.statement, visitor, context, nodeVisitor)
    );
  },
  [249 /* ForStatement */]: function visitEachChildOfForStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateForStatement(
      node,
      nodeVisitor(node.initializer, visitor, isForInitializer),
      nodeVisitor(node.condition, visitor, isExpression),
      nodeVisitor(node.incrementor, visitor, isExpression),
      visitIterationBody(node.statement, visitor, context, nodeVisitor)
    );
  },
  [250 /* ForInStatement */]: function visitEachChildOfForInStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateForInStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      visitIterationBody(node.statement, visitor, context, nodeVisitor)
    );
  },
  [251 /* ForOfStatement */]: function visitEachChildOfForOfStatement(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateForOfStatement(
      node,
      tokenVisitor ? nodeVisitor(node.awaitModifier, tokenVisitor, isAwaitKeyword) : node.awaitModifier,
      Debug.checkDefined(nodeVisitor(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      visitIterationBody(node.statement, visitor, context, nodeVisitor)
    );
  },
  [252 /* ContinueStatement */]: function visitEachChildOfContinueStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateContinueStatement(
      node,
      nodeVisitor(node.label, visitor, isIdentifier)
    );
  },
  [253 /* BreakStatement */]: function visitEachChildOfBreakStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateBreakStatement(
      node,
      nodeVisitor(node.label, visitor, isIdentifier)
    );
  },
  [254 /* ReturnStatement */]: function visitEachChildOfReturnStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateReturnStatement(
      node,
      nodeVisitor(node.expression, visitor, isExpression)
    );
  },
  [255 /* WithStatement */]: function visitEachChildOfWithStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateWithStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.statement, visitor, isStatement, context.factory.liftToBlock))
    );
  },
  [256 /* SwitchStatement */]: function visitEachChildOfSwitchStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateSwitchStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      Debug.checkDefined(nodeVisitor(node.caseBlock, visitor, isCaseBlock))
    );
  },
  [257 /* LabeledStatement */]: function visitEachChildOfLabeledStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateLabeledStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.label, visitor, isIdentifier)),
      Debug.checkDefined(nodeVisitor(node.statement, visitor, isStatement, context.factory.liftToBlock))
    );
  },
  [258 /* ThrowStatement */]: function visitEachChildOfThrowStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateThrowStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [259 /* TryStatement */]: function visitEachChildOfTryStatement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTryStatement(
      node,
      Debug.checkDefined(nodeVisitor(node.tryBlock, visitor, isBlock)),
      nodeVisitor(node.catchClause, visitor, isCatchClause),
      nodeVisitor(node.finallyBlock, visitor, isBlock)
    );
  },
  [261 /* VariableDeclaration */]: function visitEachChildOfVariableDeclaration(node, visitor, context, _nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateVariableDeclaration(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isBindingName)),
      tokenVisitor ? nodeVisitor(node.exclamationToken, tokenVisitor, isExclamationToken) : node.exclamationToken,
      nodeVisitor(node.type, visitor, isTypeNode),
      nodeVisitor(node.initializer, visitor, isExpression)
    );
  },
  [262 /* VariableDeclarationList */]: function visitEachChildOfVariableDeclarationList(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateVariableDeclarationList(
      node,
      nodesVisitor(node.declarations, visitor, isVariableDeclaration)
    );
  },
  [263 /* FunctionDeclaration */]: function visitEachChildOfFunctionDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, tokenVisitor) {
    return context.factory.updateFunctionDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifier),
      tokenVisitor ? nodeVisitor(node.asteriskToken, tokenVisitor, isAsteriskToken) : node.asteriskToken,
      nodeVisitor(node.name, visitor, isIdentifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      visitParameterList(node.parameters, visitor, context, nodesVisitor),
      nodeVisitor(node.type, visitor, isTypeNode),
      visitFunctionBody(node.body, visitor, context, nodeVisitor)
    );
  },
  [264 /* ClassDeclaration */]: function visitEachChildOfClassDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateClassDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      nodeVisitor(node.name, visitor, isIdentifier),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.heritageClauses, visitor, isHeritageClause),
      nodesVisitor(node.members, visitor, isClassElement)
    );
  },
  [265 /* InterfaceDeclaration */]: function visitEachChildOfInterfaceDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateInterfaceDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      nodesVisitor(node.heritageClauses, visitor, isHeritageClause),
      nodesVisitor(node.members, visitor, isTypeElement)
    );
  },
  [266 /* TypeAliasDeclaration */]: function visitEachChildOfTypeAliasDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateTypeAliasDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      nodesVisitor(node.typeParameters, visitor, isTypeParameterDeclaration),
      Debug.checkDefined(nodeVisitor(node.type, visitor, isTypeNode))
    );
  },
  [267 /* EnumDeclaration */]: function visitEachChildOfEnumDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateEnumDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      nodesVisitor(node.members, visitor, isEnumMember)
    );
  },
  [268 /* ModuleDeclaration */]: function visitEachChildOfModuleDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateModuleDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isModuleName)),
      nodeVisitor(node.body, visitor, isModuleBody)
    );
  },
  [269 /* ModuleBlock */]: function visitEachChildOfModuleBlock(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateModuleBlock(
      node,
      nodesVisitor(node.statements, visitor, isStatement)
    );
  },
  [270 /* CaseBlock */]: function visitEachChildOfCaseBlock(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateCaseBlock(
      node,
      nodesVisitor(node.clauses, visitor, isCaseOrDefaultClause)
    );
  },
  [271 /* NamespaceExportDeclaration */]: function visitEachChildOfNamespaceExportDeclaration(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateNamespaceExportDeclaration(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  [272 /* ImportEqualsDeclaration */]: function visitEachChildOfImportEqualsDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportEqualsDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      node.isTypeOnly,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      Debug.checkDefined(nodeVisitor(node.moduleReference, visitor, isModuleReference))
    );
  },
  [273 /* ImportDeclaration */]: function visitEachChildOfImportDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      nodeVisitor(node.importClause, visitor, isImportClause),
      Debug.checkDefined(nodeVisitor(node.moduleSpecifier, visitor, isExpression)),
      nodeVisitor(node.attributes, visitor, isImportAttributes)
    );
  },
  [301 /* ImportAttributes */]: function visitEachChildOfImportAttributes(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportAttributes(
      node,
      nodesVisitor(node.elements, visitor, isImportAttribute),
      node.multiLine
    );
  },
  [302 /* ImportAttribute */]: function visitEachChildOfImportAttribute(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportAttribute(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isImportAttributeName)),
      Debug.checkDefined(nodeVisitor(node.value, visitor, isExpression))
    );
  },
  [274 /* ImportClause */]: function visitEachChildOfImportClause(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportClause(
      node,
      node.phaseModifier,
      nodeVisitor(node.name, visitor, isIdentifier),
      nodeVisitor(node.namedBindings, visitor, isNamedImportBindings)
    );
  },
  [275 /* NamespaceImport */]: function visitEachChildOfNamespaceImport(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateNamespaceImport(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  [281 /* NamespaceExport */]: function visitEachChildOfNamespaceExport(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateNamespaceExport(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  [276 /* NamedImports */]: function visitEachChildOfNamedImports(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateNamedImports(
      node,
      nodesVisitor(node.elements, visitor, isImportSpecifier)
    );
  },
  [277 /* ImportSpecifier */]: function visitEachChildOfImportSpecifier(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateImportSpecifier(
      node,
      node.isTypeOnly,
      nodeVisitor(node.propertyName, visitor, isModuleExportName),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  [278 /* ExportAssignment */]: function visitEachChildOfExportAssignment(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExportAssignment(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [279 /* ExportDeclaration */]: function visitEachChildOfExportDeclaration(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExportDeclaration(
      node,
      nodesVisitor(node.modifiers, visitor, isModifierLike),
      node.isTypeOnly,
      nodeVisitor(node.exportClause, visitor, isNamedExportBindings),
      nodeVisitor(node.moduleSpecifier, visitor, isExpression),
      nodeVisitor(node.attributes, visitor, isImportAttributes)
    );
  },
  [280 /* NamedExports */]: function visitEachChildOfNamedExports(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateNamedExports(
      node,
      nodesVisitor(node.elements, visitor, isExportSpecifier)
    );
  },
  [282 /* ExportSpecifier */]: function visitEachChildOfExportSpecifier(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExportSpecifier(
      node,
      node.isTypeOnly,
      nodeVisitor(node.propertyName, visitor, isModuleExportName),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isModuleExportName))
    );
  },
  // Module references
  [284 /* ExternalModuleReference */]: function visitEachChildOfExternalModuleReference(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateExternalModuleReference(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  // JSX
  [285 /* JsxElement */]: function visitEachChildOfJsxElement(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxElement(
      node,
      Debug.checkDefined(nodeVisitor(node.openingElement, visitor, isJsxOpeningElement)),
      nodesVisitor(node.children, visitor, isJsxChild),
      Debug.checkDefined(nodeVisitor(node.closingElement, visitor, isJsxClosingElement))
    );
  },
  [286 /* JsxSelfClosingElement */]: function visitEachChildOfJsxSelfClosingElement(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxSelfClosingElement(
      node,
      Debug.checkDefined(nodeVisitor(node.tagName, visitor, isJsxTagNameExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      Debug.checkDefined(nodeVisitor(node.attributes, visitor, isJsxAttributes))
    );
  },
  [287 /* JsxOpeningElement */]: function visitEachChildOfJsxOpeningElement(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxOpeningElement(
      node,
      Debug.checkDefined(nodeVisitor(node.tagName, visitor, isJsxTagNameExpression)),
      nodesVisitor(node.typeArguments, visitor, isTypeNode),
      Debug.checkDefined(nodeVisitor(node.attributes, visitor, isJsxAttributes))
    );
  },
  [288 /* JsxClosingElement */]: function visitEachChildOfJsxClosingElement(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxClosingElement(
      node,
      Debug.checkDefined(nodeVisitor(node.tagName, visitor, isJsxTagNameExpression))
    );
  },
  [296 /* JsxNamespacedName */]: function forEachChildInJsxNamespacedName2(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxNamespacedName(
      node,
      Debug.checkDefined(nodeVisitor(node.namespace, visitor, isIdentifier)),
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier))
    );
  },
  [289 /* JsxFragment */]: function visitEachChildOfJsxFragment(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxFragment(
      node,
      Debug.checkDefined(nodeVisitor(node.openingFragment, visitor, isJsxOpeningFragment)),
      nodesVisitor(node.children, visitor, isJsxChild),
      Debug.checkDefined(nodeVisitor(node.closingFragment, visitor, isJsxClosingFragment))
    );
  },
  [292 /* JsxAttribute */]: function visitEachChildOfJsxAttribute(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxAttribute(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isJsxAttributeName)),
      nodeVisitor(node.initializer, visitor, isStringLiteralOrJsxExpression)
    );
  },
  [293 /* JsxAttributes */]: function visitEachChildOfJsxAttributes(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxAttributes(
      node,
      nodesVisitor(node.properties, visitor, isJsxAttributeLike)
    );
  },
  [294 /* JsxSpreadAttribute */]: function visitEachChildOfJsxSpreadAttribute(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxSpreadAttribute(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [295 /* JsxExpression */]: function visitEachChildOfJsxExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateJsxExpression(
      node,
      nodeVisitor(node.expression, visitor, isExpression)
    );
  },
  // Clauses
  [297 /* CaseClause */]: function visitEachChildOfCaseClause(node, visitor, context, nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateCaseClause(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression)),
      nodesVisitor(node.statements, visitor, isStatement)
    );
  },
  [298 /* DefaultClause */]: function visitEachChildOfDefaultClause(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateDefaultClause(
      node,
      nodesVisitor(node.statements, visitor, isStatement)
    );
  },
  [299 /* HeritageClause */]: function visitEachChildOfHeritageClause(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateHeritageClause(
      node,
      nodesVisitor(node.types, visitor, isExpressionWithTypeArguments)
    );
  },
  [300 /* CatchClause */]: function visitEachChildOfCatchClause(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateCatchClause(
      node,
      nodeVisitor(node.variableDeclaration, visitor, isVariableDeclaration),
      Debug.checkDefined(nodeVisitor(node.block, visitor, isBlock))
    );
  },
  // Property assignments
  [304 /* PropertyAssignment */]: function visitEachChildOfPropertyAssignment(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updatePropertyAssignment(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      Debug.checkDefined(nodeVisitor(node.initializer, visitor, isExpression))
    );
  },
  [305 /* ShorthandPropertyAssignment */]: function visitEachChildOfShorthandPropertyAssignment(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateShorthandPropertyAssignment(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isIdentifier)),
      nodeVisitor(node.objectAssignmentInitializer, visitor, isExpression)
    );
  },
  [306 /* SpreadAssignment */]: function visitEachChildOfSpreadAssignment(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateSpreadAssignment(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  // Enum
  [307 /* EnumMember */]: function visitEachChildOfEnumMember(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updateEnumMember(
      node,
      Debug.checkDefined(nodeVisitor(node.name, visitor, isPropertyName)),
      nodeVisitor(node.initializer, visitor, isExpression)
    );
  },
  // Top-level nodes
  [308 /* SourceFile */]: function visitEachChildOfSourceFile(node, visitor, context, _nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateSourceFile(
      node,
      visitLexicalEnvironment(node.statements, visitor, context)
    );
  },
  // Transformation nodes
  [356 /* PartiallyEmittedExpression */]: function visitEachChildOfPartiallyEmittedExpression(node, visitor, context, _nodesVisitor, nodeVisitor, _tokenVisitor) {
    return context.factory.updatePartiallyEmittedExpression(
      node,
      Debug.checkDefined(nodeVisitor(node.expression, visitor, isExpression))
    );
  },
  [357 /* CommaListExpression */]: function visitEachChildOfCommaListExpression(node, visitor, context, nodesVisitor, _nodeVisitor, _tokenVisitor) {
    return context.factory.updateCommaListExpression(
      node,
      nodesVisitor(node.elements, visitor, isExpression)
    );
  }
};
function extractSingleNode(nodes) {
  Debug.assert(nodes.length <***REMOVED*** 1, "Too many nodes written to output.");
  return singleOrUndefined(nodes);
}

// src/compiler/sourcemap.ts
function createSourceMapGenerator(host, file, sourceRoot, sourcesDirectoryPath, generatorOptions) {
  var { enter, exit } ***REMOVED*** generatorOptions.extendedDiagnostics ? createTimer("Source Map", "beforeSourcemap", "afterSourcemap") : nullTimer;
  var rawSources ***REMOVED*** [];
  var sources ***REMOVED*** [];
  var sourceToSourceIndexMap ***REMOVED*** /* @__PURE__ */ new Map();
  var sourcesContent;
  var names ***REMOVED*** [];
  var nameToNameIndexMap;
  var mappingCharCodes ***REMOVED*** [];
  var mappings ***REMOVED*** "";
  var lastGeneratedLine ***REMOVED*** 0;
  var lastGeneratedCharacter ***REMOVED*** 0;
  var lastSourceIndex ***REMOVED*** 0;
  var lastSourceLine ***REMOVED*** 0;
  var lastSourceCharacter ***REMOVED*** 0;
  var lastNameIndex ***REMOVED*** 0;
  var hasLast ***REMOVED*** false;
  var pendingGeneratedLine ***REMOVED*** 0;
  var pendingGeneratedCharacter ***REMOVED*** 0;
  var pendingSourceIndex ***REMOVED*** 0;
  var pendingSourceLine ***REMOVED*** 0;
  var pendingSourceCharacter ***REMOVED*** 0;
  var pendingNameIndex ***REMOVED*** 0;
  var hasPending ***REMOVED*** false;
  var hasPendingSource ***REMOVED*** false;
  var hasPendingName ***REMOVED*** false;
  return {
    getSources: () ***REMOVED***> rawSources,
    addSource,
    setSourceContent,
    addName,
    addMapping,
    appendSourceMap,
    toJSON,
    toString: () ***REMOVED***> JSON.stringify(toJSON())
  };
  function addSource(fileName) {
    enter();
    const source ***REMOVED*** getRelativePathToDirectoryOrUrl(
      sourcesDirectoryPath,
      fileName,
      host.getCurrentDirectory(),
      host.getCanonicalFileName,
      /*isAbsolutePathAnUrl*/
      true
    );
    let sourceIndex ***REMOVED*** sourceToSourceIndexMap.get(source);
    if (sourceIndex ***REMOVED******REMOVED******REMOVED*** void 0) {
      sourceIndex ***REMOVED*** sources.length;
      sources.push(source);
      rawSources.push(fileName);
      sourceToSourceIndexMap.set(source, sourceIndex);
    }
    exit();
    return sourceIndex;
  }
  function setSourceContent(sourceIndex, content) {
    enter();
    if (content !***REMOVED******REMOVED*** null) {
      if (!sourcesContent) sourcesContent ***REMOVED*** [];
      while (sourcesContent.length < sourceIndex) {
        sourcesContent.push(null);
      }
      sourcesContent[sourceIndex] ***REMOVED*** content;
    }
    exit();
  }
  function addName(name) {
    enter();
    if (!nameToNameIndexMap) nameToNameIndexMap ***REMOVED*** /* @__PURE__ */ new Map();
    let nameIndex ***REMOVED*** nameToNameIndexMap.get(name);
    if (nameIndex ***REMOVED******REMOVED******REMOVED*** void 0) {
      nameIndex ***REMOVED*** names.length;
      names.push(name);
      nameToNameIndexMap.set(name, nameIndex);
    }
    exit();
    return nameIndex;
  }
  function isNewGeneratedPosition(generatedLine, generatedCharacter) {
    return !hasPending || pendingGeneratedLine !***REMOVED******REMOVED*** generatedLine || pendingGeneratedCharacter !***REMOVED******REMOVED*** generatedCharacter;
  }
  function isBacktrackingSourcePosition(sourceIndex, sourceLine, sourceCharacter) {
    return sourceIndex !***REMOVED******REMOVED*** void 0 && sourceLine !***REMOVED******REMOVED*** void 0 && sourceCharacter !***REMOVED******REMOVED*** void 0 && pendingSourceIndex ***REMOVED******REMOVED******REMOVED*** sourceIndex && (pendingSourceLine > sourceLine || pendingSourceLine ***REMOVED******REMOVED******REMOVED*** sourceLine && pendingSourceCharacter > sourceCharacter);
  }
  function addMapping(generatedLine, generatedCharacter, sourceIndex, sourceLine, sourceCharacter, nameIndex) {
    Debug.assert(generatedLine >***REMOVED*** pendingGeneratedLine, "generatedLine cannot backtrack");
    Debug.assert(generatedCharacter >***REMOVED*** 0, "generatedCharacter cannot be negative");
    Debug.assert(sourceIndex ***REMOVED******REMOVED******REMOVED*** void 0 || sourceIndex >***REMOVED*** 0, "sourceIndex cannot be negative");
    Debug.assert(sourceLine ***REMOVED******REMOVED******REMOVED*** void 0 || sourceLine >***REMOVED*** 0, "sourceLine cannot be negative");
    Debug.assert(sourceCharacter ***REMOVED******REMOVED******REMOVED*** void 0 || sourceCharacter >***REMOVED*** 0, "sourceCharacter cannot be negative");
    enter();
    if (isNewGeneratedPosition(generatedLine, generatedCharacter) || isBacktrackingSourcePosition(sourceIndex, sourceLine, sourceCharacter)) {
      commitPendingMapping();
      pendingGeneratedLine ***REMOVED*** generatedLine;
      pendingGeneratedCharacter ***REMOVED*** generatedCharacter;
      hasPendingSource ***REMOVED*** false;
      hasPendingName ***REMOVED*** false;
      hasPending ***REMOVED*** true;
    }
    if (sourceIndex !***REMOVED******REMOVED*** void 0 && sourceLine !***REMOVED******REMOVED*** void 0 && sourceCharacter !***REMOVED******REMOVED*** void 0) {
      pendingSourceIndex ***REMOVED*** sourceIndex;
      pendingSourceLine ***REMOVED*** sourceLine;
      pendingSourceCharacter ***REMOVED*** sourceCharacter;
      hasPendingSource ***REMOVED*** true;
      if (nameIndex !***REMOVED******REMOVED*** void 0) {
        pendingNameIndex ***REMOVED*** nameIndex;
        hasPendingName ***REMOVED*** true;
      }
    }
    exit();
  }
  function appendSourceMap(generatedLine, generatedCharacter, map2, sourceMapPath, start, end) {
    Debug.assert(generatedLine >***REMOVED*** pendingGeneratedLine, "generatedLine cannot backtrack");
    Debug.assert(generatedCharacter >***REMOVED*** 0, "generatedCharacter cannot be negative");
    enter();
    const sourceIndexToNewSourceIndexMap ***REMOVED*** [];
    let nameIndexToNewNameIndexMap;
    const mappingIterator ***REMOVED*** decodeMappings(map2.mappings);
    for (const raw of mappingIterator) {
      if (end && (raw.generatedLine > end.line || raw.generatedLine ***REMOVED******REMOVED******REMOVED*** end.line && raw.generatedCharacter > end.character)) {
        break;
      }
      if (start && (raw.generatedLine < start.line || start.line ***REMOVED******REMOVED******REMOVED*** raw.generatedLine && raw.generatedCharacter < start.character)) {
        continue;
      }
      let newSourceIndex;
      let newSourceLine;
      let newSourceCharacter;
      let newNameIndex;
      if (raw.sourceIndex !***REMOVED******REMOVED*** void 0) {
        newSourceIndex ***REMOVED*** sourceIndexToNewSourceIndexMap[raw.sourceIndex];
        if (newSourceIndex ***REMOVED******REMOVED******REMOVED*** void 0) {
          const rawPath ***REMOVED*** map2.sources[raw.sourceIndex];
          const relativePath ***REMOVED*** map2.sourceRoot ? combinePaths(map2.sourceRoot, rawPath) : rawPath;
          const combinedPath ***REMOVED*** combinePaths(getDirectoryPath(sourceMapPath), relativePath);
          sourceIndexToNewSourceIndexMap[raw.sourceIndex] ***REMOVED*** newSourceIndex ***REMOVED*** addSource(combinedPath);
          if (map2.sourcesContent && typeof map2.sourcesContent[raw.sourceIndex] ***REMOVED******REMOVED******REMOVED*** "string") {
            setSourceContent(newSourceIndex, map2.sourcesContent[raw.sourceIndex]);
          }
        }
        newSourceLine ***REMOVED*** raw.sourceLine;
        newSourceCharacter ***REMOVED*** raw.sourceCharacter;
        if (map2.names && raw.nameIndex !***REMOVED******REMOVED*** void 0) {
          if (!nameIndexToNewNameIndexMap) nameIndexToNewNameIndexMap ***REMOVED*** [];
          newNameIndex ***REMOVED*** nameIndexToNewNameIndexMap[raw.nameIndex];
          if (newNameIndex ***REMOVED******REMOVED******REMOVED*** void 0) {
            nameIndexToNewNameIndexMap[raw.nameIndex] ***REMOVED*** newNameIndex ***REMOVED*** addName(map2.names[raw.nameIndex]);
          }
        }
      }
      const rawGeneratedLine ***REMOVED*** raw.generatedLine - (start ? start.line : 0);
      const newGeneratedLine ***REMOVED*** rawGeneratedLine + generatedLine;
      const rawGeneratedCharacter ***REMOVED*** start && start.line ***REMOVED******REMOVED******REMOVED*** raw.generatedLine ? raw.generatedCharacter - start.character : raw.generatedCharacter;
      const newGeneratedCharacter ***REMOVED*** rawGeneratedLine ***REMOVED******REMOVED******REMOVED*** 0 ? rawGeneratedCharacter + generatedCharacter : rawGeneratedCharacter;
      addMapping(newGeneratedLine, newGeneratedCharacter, newSourceIndex, newSourceLine, newSourceCharacter, newNameIndex);
    }
    exit();
  }
  function shouldCommitMapping() {
    return !hasLast || lastGeneratedLine !***REMOVED******REMOVED*** pendingGeneratedLine || lastGeneratedCharacter !***REMOVED******REMOVED*** pendingGeneratedCharacter || lastSourceIndex !***REMOVED******REMOVED*** pendingSourceIndex || lastSourceLine !***REMOVED******REMOVED*** pendingSourceLine || lastSourceCharacter !***REMOVED******REMOVED*** pendingSourceCharacter || lastNameIndex !***REMOVED******REMOVED*** pendingNameIndex;
  }
  function appendMappingCharCode(charCode) {
    mappingCharCodes.push(charCode);
    if (mappingCharCodes.length >***REMOVED*** 1024) {
      flushMappingBuffer();
    }
  }
  function commitPendingMapping() {
    if (!hasPending || !shouldCommitMapping()) {
      return;
    }
    enter();
    if (lastGeneratedLine < pendingGeneratedLine) {
      do {
        appendMappingCharCode(59 /* semicolon */);
        lastGeneratedLine++;
      } while (lastGeneratedLine < pendingGeneratedLine);
      lastGeneratedCharacter ***REMOVED*** 0;
    } else {
      Debug.assertEqual(lastGeneratedLine, pendingGeneratedLine, "generatedLine cannot backtrack");
      if (hasLast) {
        appendMappingCharCode(44 /* comma */);
      }
    }
    appendBase64VLQ(pendingGeneratedCharacter - lastGeneratedCharacter);
    lastGeneratedCharacter ***REMOVED*** pendingGeneratedCharacter;
    if (hasPendingSource) {
      appendBase64VLQ(pendingSourceIndex - lastSourceIndex);
      lastSourceIndex ***REMOVED*** pendingSourceIndex;
      appendBase64VLQ(pendingSourceLine - lastSourceLine);
      lastSourceLine ***REMOVED*** pendingSourceLine;
      appendBase64VLQ(pendingSourceCharacter - lastSourceCharacter);
      lastSourceCharacter ***REMOVED*** pendingSourceCharacter;
      if (hasPendingName) {
        appendBase64VLQ(pendingNameIndex - lastNameIndex);
        lastNameIndex ***REMOVED*** pendingNameIndex;
      }
    }
    hasLast ***REMOVED*** true;
    exit();
  }
  function flushMappingBuffer() {
    if (mappingCharCodes.length > 0) {
      mappings +***REMOVED*** String.fromCharCode.apply(void 0, mappingCharCodes);
      mappingCharCodes.length ***REMOVED*** 0;
    }
  }
  function toJSON() {
    commitPendingMapping();
    flushMappingBuffer();
    return {
      version: 3,
      file,
      sourceRoot,
      sources,
      names,
      mappings,
      sourcesContent
    };
  }
  function appendBase64VLQ(inValue) {
    if (inValue < 0) {
      inValue ***REMOVED*** (-inValue << 1) + 1;
    } else {
      inValue ***REMOVED*** inValue << 1;
    }
    do {
      let currentDigit ***REMOVED*** inValue & 31;
      inValue ***REMOVED*** inValue >> 5;
      if (inValue > 0) {
        currentDigit ***REMOVED*** currentDigit | 32;
      }
      appendMappingCharCode(base64FormatEncode(currentDigit));
    } while (inValue > 0);
  }
}
var sourceMapCommentRegExpDontCareLineStart ***REMOVED*** /\/\/[@#] source[M]appingURL***REMOVED***(.+)\r?\n?$/;
var sourceMapCommentRegExp ***REMOVED*** /^\/\/[@#] source[M]appingURL***REMOVED***(.+)\r?\n?$/;
var whitespaceOrMapCommentRegExp ***REMOVED*** /^\s*(\/\/[@#] .*)?$/;
function decodeMappings(mappings) {
  let done ***REMOVED*** false;
  let pos ***REMOVED*** 0;
  let generatedLine ***REMOVED*** 0;
  let generatedCharacter ***REMOVED*** 0;
  let sourceIndex ***REMOVED*** 0;
  let sourceLine ***REMOVED*** 0;
  let sourceCharacter ***REMOVED*** 0;
  let nameIndex ***REMOVED*** 0;
  let error;
  return {
    get pos() {
      return pos;
    },
    get error() {
      return error;
    },
    get state() {
      return captureMapping(
        /*hasSource*/
        true,
        /*hasName*/
        true
      );
    },
    next() {
      while (!done && pos < mappings.length) {
        const ch ***REMOVED*** mappings.charCodeAt(pos);
        if (ch ***REMOVED******REMOVED******REMOVED*** 59 /* semicolon */) {
          generatedLine++;
          generatedCharacter ***REMOVED*** 0;
          pos++;
          continue;
        }
        if (ch ***REMOVED******REMOVED******REMOVED*** 44 /* comma */) {
          pos++;
          continue;
        }
        let hasSource ***REMOVED*** false;
        let hasName ***REMOVED*** false;
        generatedCharacter +***REMOVED*** base64VLQFormatDecode();
        if (hasReportedError()) return stopIterating();
        if (generatedCharacter < 0) return setErrorAndStopIterating("Invalid generatedCharacter found");
        if (!isSourceMappingSegmentEnd()) {
          hasSource ***REMOVED*** true;
          sourceIndex +***REMOVED*** base64VLQFormatDecode();
          if (hasReportedError()) return stopIterating();
          if (sourceIndex < 0) return setErrorAndStopIterating("Invalid sourceIndex found");
          if (isSourceMappingSegmentEnd()) return setErrorAndStopIterating("Unsupported Format: No entries after sourceIndex");
          sourceLine +***REMOVED*** base64VLQFormatDecode();
          if (hasReportedError()) return stopIterating();
          if (sourceLine < 0) return setErrorAndStopIterating("Invalid sourceLine found");
          if (isSourceMappingSegmentEnd()) return setErrorAndStopIterating("Unsupported Format: No entries after sourceLine");
          sourceCharacter +***REMOVED*** base64VLQFormatDecode();
          if (hasReportedError()) return stopIterating();
          if (sourceCharacter < 0) return setErrorAndStopIterating("Invalid sourceCharacter found");
          if (!isSourceMappingSegmentEnd()) {
            hasName ***REMOVED*** true;
            nameIndex +***REMOVED*** base64VLQFormatDecode();
            if (hasReportedError()) return stopIterating();
            if (nameIndex < 0) return setErrorAndStopIterating("Invalid nameIndex found");
            if (!isSourceMappingSegmentEnd()) return setErrorAndStopIterating("Unsupported Error Format: Entries after nameIndex");
          }
        }
        return { value: captureMapping(hasSource, hasName), done };
      }
      return stopIterating();
    },
    [Symbol.iterator]() {
      return this;
    }
  };
  function captureMapping(hasSource, hasName) {
    return {
      generatedLine,
      generatedCharacter,
      sourceIndex: hasSource ? sourceIndex : void 0,
      sourceLine: hasSource ? sourceLine : void 0,
      sourceCharacter: hasSource ? sourceCharacter : void 0,
      nameIndex: hasName ? nameIndex : void 0
    };
  }
  function stopIterating() {
    done ***REMOVED*** true;
    return { value: void 0, done: true };
  }
  function setError(message) {
    if (error ***REMOVED******REMOVED******REMOVED*** void 0) {
      error ***REMOVED*** message;
    }
  }
  function setErrorAndStopIterating(message) {
    setError(message);
    return stopIterating();
  }
  function hasReportedError() {
    return error !***REMOVED******REMOVED*** void 0;
  }
  function isSourceMappingSegmentEnd() {
    return pos ***REMOVED******REMOVED******REMOVED*** mappings.length || mappings.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 44 /* comma */ || mappings.charCodeAt(pos) ***REMOVED******REMOVED******REMOVED*** 59 /* semicolon */;
  }
  function base64VLQFormatDecode() {
    let moreDigits ***REMOVED*** true;
    let shiftCount ***REMOVED*** 0;
    let value ***REMOVED*** 0;
    for (; moreDigits; pos++) {
      if (pos >***REMOVED*** mappings.length) return setError("Error in decoding base64VLQFormatDecode, past the mapping string"), -1;
      const currentByte ***REMOVED*** base64FormatDecode(mappings.charCodeAt(pos));
      if (currentByte ***REMOVED******REMOVED******REMOVED*** -1) return setError("Invalid character in VLQ"), -1;
      moreDigits ***REMOVED*** (currentByte & 32) !***REMOVED******REMOVED*** 0;
      value ***REMOVED*** value | (currentByte & 31) << shiftCount;
      shiftCount +***REMOVED*** 5;
    }
    if ((value & 1) ***REMOVED******REMOVED******REMOVED*** 0) {
      value ***REMOVED*** value >> 1;
    } else {
      value ***REMOVED*** value >> 1;
      value ***REMOVED*** -value;
    }
    return value;
  }
}
function base64FormatEncode(value) {
  return value >***REMOVED*** 0 && value < 26 ? 65 /* A */ + value : value >***REMOVED*** 26 && value < 52 ? 97 /* a */ + value - 26 : value >***REMOVED*** 52 && value < 62 ? 48 /* _0 */ + value - 52 : value ***REMOVED******REMOVED******REMOVED*** 62 ? 43 /* plus */ : value ***REMOVED******REMOVED******REMOVED*** 63 ? 47 /* slash */ : Debug.fail(`${value}: not a base64 value`);
}
function base64FormatDecode(ch) {
  return ch >***REMOVED*** 65 /* A */ && ch <***REMOVED*** 90 /* Z */ ? ch - 65 /* A */ : ch >***REMOVED*** 97 /* a */ && ch <***REMOVED*** 122 /* z */ ? ch - 97 /* a */ + 26 : ch >***REMOVED*** 48 /* _0 */ && ch <***REMOVED*** 57 /* _9 */ ? ch - 48 /* _0 */ + 52 : ch ***REMOVED******REMOVED******REMOVED*** 43 /* plus */ ? 62 : ch ***REMOVED******REMOVED******REMOVED*** 47 /* slash */ ? 63 : -1;
}

// src/compiler/transformers/utilities.ts
function getOriginalNodeId(node) {
  node ***REMOVED*** getOriginalNode(node);
  return node ? getNodeId(node) : 0;
}
function containsDefaultReference(node) {
  if (!node) return false;
  if (!isNamedImports(node) && !isNamedExports(node)) return false;
  return some(node.elements, isNamedDefaultReference);
}
function isNamedDefaultReference(e) {
  return moduleExportNameIsDefault(e.propertyName || e.name);
}
function chainBundle(context, transformSourceFile) {
  return transformSourceFileOrBundle;
  function transformSourceFileOrBundle(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? transformSourceFile(node) : transformBundle(node);
  }
  function transformBundle(node) {
    return context.factory.createBundle(map(node.sourceFiles, transformSourceFile));
  }
}
function getExportNeedsImportStarHelper(node) {
  return !!getNamespaceDeclarationNode(node);
}
function getImportNeedsImportStarHelper(node) {
  if (!!getNamespaceDeclarationNode(node)) {
    return true;
  }
  const bindings ***REMOVED*** node.importClause && node.importClause.namedBindings;
  if (!bindings) {
    return false;
  }
  if (!isNamedImports(bindings)) return false;
  let defaultRefCount ***REMOVED*** 0;
  for (const binding of bindings.elements) {
    if (isNamedDefaultReference(binding)) {
      defaultRefCount++;
    }
  }
  return defaultRefCount > 0 && defaultRefCount !***REMOVED******REMOVED*** bindings.elements.length || !!(bindings.elements.length - defaultRefCount) && isDefaultImport(node);
}
function getImportNeedsImportDefaultHelper(node) {
  return !getImportNeedsImportStarHelper(node) && (isDefaultImport(node) || !!node.importClause && isNamedImports(node.importClause.namedBindings) && containsDefaultReference(node.importClause.namedBindings));
}
function collectExternalModuleInfo(context, sourceFile) {
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const externalImports ***REMOVED*** [];
  const exportSpecifiers ***REMOVED*** new IdentifierNameMultiMap();
  const exportedBindings ***REMOVED*** [];
  const uniqueExports ***REMOVED*** /* @__PURE__ */ new Map();
  const exportedFunctions ***REMOVED*** /* @__PURE__ */ new Set();
  let exportedNames;
  let hasExportDefault ***REMOVED*** false;
  let exportEquals;
  let hasExportStarsToExportValues ***REMOVED*** false;
  let hasImportStar ***REMOVED*** false;
  let hasImportDefault ***REMOVED*** false;
  for (const node of sourceFile.statements) {
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
        externalImports.push(node);
        if (!hasImportStar && getImportNeedsImportStarHelper(node)) {
          hasImportStar ***REMOVED*** true;
        }
        if (!hasImportDefault && getImportNeedsImportDefaultHelper(node)) {
          hasImportDefault ***REMOVED*** true;
        }
        break;
      case 272 /* ImportEqualsDeclaration */:
        if (node.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */) {
          externalImports.push(node);
        }
        break;
      case 279 /* ExportDeclaration */:
        if (node.moduleSpecifier) {
          if (!node.exportClause) {
            externalImports.push(node);
            hasExportStarsToExportValues ***REMOVED*** true;
          } else {
            externalImports.push(node);
            if (isNamedExports(node.exportClause)) {
              addExportedNamesForExportDeclaration(node);
              hasImportDefault || (hasImportDefault ***REMOVED*** containsDefaultReference(node.exportClause));
            } else {
              const name ***REMOVED*** node.exportClause.name;
              const nameText ***REMOVED*** moduleExportNameTextUnescaped(name);
              if (!uniqueExports.get(nameText)) {
                multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(node), name);
                uniqueExports.set(nameText, true);
                exportedNames ***REMOVED*** append(exportedNames, name);
              }
              hasImportStar ***REMOVED*** true;
            }
          }
        } else {
          addExportedNamesForExportDeclaration(node);
        }
        break;
      case 278 /* ExportAssignment */:
        if (node.isExportEquals && !exportEquals) {
          exportEquals ***REMOVED*** node;
        }
        break;
      case 244 /* VariableStatement */:
        if (hasSyntacticModifier(node, 32 /* Export */)) {
          for (const decl of node.declarationList.declarations) {
            exportedNames ***REMOVED*** collectExportedVariableInfo(decl, uniqueExports, exportedNames, exportedBindings);
          }
        }
        break;
      case 263 /* FunctionDeclaration */:
        if (hasSyntacticModifier(node, 32 /* Export */)) {
          addExportedFunctionDeclaration(
            node,
            /*name*/
            void 0,
            hasSyntacticModifier(node, 2048 /* Default */)
          );
        }
        break;
      case 264 /* ClassDeclaration */:
        if (hasSyntacticModifier(node, 32 /* Export */)) {
          if (hasSyntacticModifier(node, 2048 /* Default */)) {
            if (!hasExportDefault) {
              multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(node), context.factory.getDeclarationName(node));
              hasExportDefault ***REMOVED*** true;
            }
          } else {
            const name ***REMOVED*** node.name;
            if (name && !uniqueExports.get(idText(name))) {
              multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(node), name);
              uniqueExports.set(idText(name), true);
              exportedNames ***REMOVED*** append(exportedNames, name);
            }
          }
        }
        break;
    }
  }
  const externalHelpersImportDeclaration ***REMOVED*** createExternalHelpersImportDeclarationIfNeeded(context.factory, context.getEmitHelperFactory(), sourceFile, compilerOptions, hasExportStarsToExportValues, hasImportStar, hasImportDefault);
  if (externalHelpersImportDeclaration) {
    externalImports.unshift(externalHelpersImportDeclaration);
  }
  return { externalImports, exportSpecifiers, exportEquals, hasExportStarsToExportValues, exportedBindings, exportedNames, exportedFunctions, externalHelpersImportDeclaration };
  function addExportedNamesForExportDeclaration(node) {
    for (const specifier of cast(node.exportClause, isNamedExports).elements) {
      const specifierNameText ***REMOVED*** moduleExportNameTextUnescaped(specifier.name);
      if (!uniqueExports.get(specifierNameText)) {
        const name ***REMOVED*** specifier.propertyName || specifier.name;
        if (name.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
          if (!node.moduleSpecifier) {
            exportSpecifiers.add(name, specifier);
          }
          const decl ***REMOVED*** resolver.getReferencedImportDeclaration(name) || resolver.getReferencedValueDeclaration(name);
          if (decl) {
            if (decl.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */) {
              addExportedFunctionDeclaration(decl, specifier.name, moduleExportNameIsDefault(specifier.name));
              continue;
            }
            multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(decl), specifier.name);
          }
        }
        uniqueExports.set(specifierNameText, true);
        exportedNames ***REMOVED*** append(exportedNames, specifier.name);
      }
    }
  }
  function addExportedFunctionDeclaration(node, name, isDefault) {
    exportedFunctions.add(getOriginalNode(node, isFunctionDeclaration));
    if (isDefault) {
      if (!hasExportDefault) {
        multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(node), name ?? context.factory.getDeclarationName(node));
        hasExportDefault ***REMOVED*** true;
      }
    } else {
      name ?? (name ***REMOVED*** node.name);
      const nameText ***REMOVED*** moduleExportNameTextUnescaped(name);
      if (!uniqueExports.get(nameText)) {
        multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(node), name);
        uniqueExports.set(nameText, true);
      }
    }
  }
}
function collectExportedVariableInfo(decl, uniqueExports, exportedNames, exportedBindings) {
  if (isBindingPattern(decl.name)) {
    for (const element of decl.name.elements) {
      if (!isOmittedExpression(element)) {
        exportedNames ***REMOVED*** collectExportedVariableInfo(element, uniqueExports, exportedNames, exportedBindings);
      }
    }
  } else if (!isGeneratedIdentifier(decl.name)) {
    const text ***REMOVED*** idText(decl.name);
    if (!uniqueExports.get(text)) {
      uniqueExports.set(text, true);
      exportedNames ***REMOVED*** append(exportedNames, decl.name);
      if (isLocalName(decl.name)) {
        multiMapSparseArrayAdd(exportedBindings, getOriginalNodeId(decl), decl.name);
      }
    }
  }
  return exportedNames;
}
function multiMapSparseArrayAdd(map2, key, value) {
  let values ***REMOVED*** map2[key];
  if (values) {
    values.push(value);
  } else {
    map2[key] ***REMOVED*** values ***REMOVED*** [value];
  }
  return values;
}
var IdentifierNameMap ***REMOVED*** class _IdentifierNameMap {
  constructor() {
    this._map ***REMOVED*** /* @__PURE__ */ new Map();
  }
  get size() {
    return this._map.size;
  }
  has(key) {
    return this._map.has(_IdentifierNameMap.toKey(key));
  }
  get(key) {
    return this._map.get(_IdentifierNameMap.toKey(key));
  }
  set(key, value) {
    this._map.set(_IdentifierNameMap.toKey(key), value);
    return this;
  }
  delete(key) {
    var _a;
    return ((_a ***REMOVED*** this._map) ***REMOVED******REMOVED*** null ? void 0 : _a.delete(_IdentifierNameMap.toKey(key))) ?? false;
  }
  clear() {
    this._map.clear();
  }
  values() {
    return this._map.values();
  }
  static toKey(name) {
    if (isGeneratedPrivateIdentifier(name) || isGeneratedIdentifier(name)) {
      const autoGenerate ***REMOVED*** name.emitNode.autoGenerate;
      if ((autoGenerate.flags & 7 /* KindMask */) ***REMOVED******REMOVED******REMOVED*** 4 /* Node */) {
        const node ***REMOVED*** getNodeForGeneratedName(name);
        const baseName ***REMOVED*** isMemberName(node) && node !***REMOVED******REMOVED*** name ? _IdentifierNameMap.toKey(node) : `(generated@${getNodeId(node)})`;
        return formatGeneratedName(
          /*privateName*/
          false,
          autoGenerate.prefix,
          baseName,
          autoGenerate.suffix,
          _IdentifierNameMap.toKey
        );
      } else {
        const baseName ***REMOVED*** `(auto@${autoGenerate.id})`;
        return formatGeneratedName(
          /*privateName*/
          false,
          autoGenerate.prefix,
          baseName,
          autoGenerate.suffix,
          _IdentifierNameMap.toKey
        );
      }
    }
    if (isPrivateIdentifier(name)) {
      return idText(name).slice(1);
    }
    return idText(name);
  }
};
var IdentifierNameMultiMap ***REMOVED*** class extends IdentifierNameMap {
  add(key, value) {
    let values ***REMOVED*** this.get(key);
    if (values) {
      values.push(value);
    } else {
      this.set(key, values ***REMOVED*** [value]);
    }
    return values;
  }
  remove(key, value) {
    const values ***REMOVED*** this.get(key);
    if (values) {
      unorderedRemoveItem(values, value);
      if (!values.length) {
        this.delete(key);
      }
    }
  }
};
function isSimpleCopiableExpression(expression) {
  return isStringLiteralLike(expression) || expression.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ || isKeyword(expression.kind) || isIdentifier(expression);
}
function isSimpleInlineableExpression(expression) {
  return !isIdentifier(expression) && isSimpleCopiableExpression(expression);
}
function isCompoundAssignment(kind) {
  return kind >***REMOVED*** 65 /* FirstCompoundAssignment */ && kind <***REMOVED*** 79 /* LastCompoundAssignment */;
}
function getNonAssignmentOperatorForCompoundAssignment(kind) {
  switch (kind) {
    case 65 /* PlusEqualsToken */:
      return 40 /* PlusToken */;
    case 66 /* MinusEqualsToken */:
      return 41 /* MinusToken */;
    case 67 /* AsteriskEqualsToken */:
      return 42 /* AsteriskToken */;
    case 68 /* AsteriskAsteriskEqualsToken */:
      return 43 /* AsteriskAsteriskToken */;
    case 69 /* SlashEqualsToken */:
      return 44 /* SlashToken */;
    case 70 /* PercentEqualsToken */:
      return 45 /* PercentToken */;
    case 71 /* LessThanLessThanEqualsToken */:
      return 48 /* LessThanLessThanToken */;
    case 72 /* GreaterThanGreaterThanEqualsToken */:
      return 49 /* GreaterThanGreaterThanToken */;
    case 73 /* GreaterThanGreaterThanGreaterThanEqualsToken */:
      return 50 /* GreaterThanGreaterThanGreaterThanToken */;
    case 74 /* AmpersandEqualsToken */:
      return 51 /* AmpersandToken */;
    case 75 /* BarEqualsToken */:
      return 52 /* BarToken */;
    case 79 /* CaretEqualsToken */:
      return 53 /* CaretToken */;
    case 76 /* BarBarEqualsToken */:
      return 57 /* BarBarToken */;
    case 77 /* AmpersandAmpersandEqualsToken */:
      return 56 /* AmpersandAmpersandToken */;
    case 78 /* QuestionQuestionEqualsToken */:
      return 61 /* QuestionQuestionToken */;
  }
}
function getSuperCallFromStatement(statement) {
  if (!isExpressionStatement(statement)) {
    return void 0;
  }
  const expression ***REMOVED*** skipParentheses(statement.expression);
  return isSuperCall(expression) ? expression : void 0;
}
function findSuperStatementIndexPathWorker(statements, start, indices) {
  for (let i ***REMOVED*** start; i < statements.length; i +***REMOVED*** 1) {
    const statement ***REMOVED*** statements[i];
    if (getSuperCallFromStatement(statement)) {
      indices.unshift(i);
      return true;
    } else if (isTryStatement(statement) && findSuperStatementIndexPathWorker(statement.tryBlock.statements, 0, indices)) {
      indices.unshift(i);
      return true;
    }
  }
  return false;
}
function findSuperStatementIndexPath(statements, start) {
  const indices ***REMOVED*** [];
  findSuperStatementIndexPathWorker(statements, start, indices);
  return indices;
}
function getProperties(node, requireInitializer, isStatic2) {
  return filter(node.members, (m) ***REMOVED***> isInitializedOrStaticProperty(m, requireInitializer, isStatic2));
}
function isStaticPropertyDeclarationOrClassStaticBlockDeclaration(element) {
  return isStaticPropertyDeclaration(element) || isClassStaticBlockDeclaration(element);
}
function getStaticPropertiesAndClassStaticBlock(node) {
  return filter(node.members, isStaticPropertyDeclarationOrClassStaticBlockDeclaration);
}
function isInitializedOrStaticProperty(member, requireInitializer, isStatic2) {
  return isPropertyDeclaration(member) && (!!member.initializer || !requireInitializer) && hasStaticModifier(member) ***REMOVED******REMOVED******REMOVED*** isStatic2;
}
function isStaticPropertyDeclaration(member) {
  return isPropertyDeclaration(member) && hasStaticModifier(member);
}
function isInitializedProperty(member) {
  return member.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ && member.initializer !***REMOVED******REMOVED*** void 0;
}
function isNonStaticMethodOrAccessorWithPrivateName(member) {
  return !isStatic(member) && (isMethodOrAccessor(member) || isAutoAccessorPropertyDeclaration(member)) && isPrivateIdentifier(member.name);
}
function getDecoratorsOfParameters(node) {
  let decorators;
  if (node) {
    const parameters ***REMOVED*** node.parameters;
    const firstParameterIsThis ***REMOVED*** parameters.length > 0 && parameterIsThisKeyword(parameters[0]);
    const firstParameterOffset ***REMOVED*** firstParameterIsThis ? 1 : 0;
    const numParameters ***REMOVED*** firstParameterIsThis ? parameters.length - 1 : parameters.length;
    for (let i ***REMOVED*** 0; i < numParameters; i++) {
      const parameter ***REMOVED*** parameters[i + firstParameterOffset];
      if (decorators || hasDecorators(parameter)) {
        if (!decorators) {
          decorators ***REMOVED*** new Array(numParameters);
        }
        decorators[i] ***REMOVED*** getDecorators(parameter);
      }
    }
  }
  return decorators;
}
function getAllDecoratorsOfClass(node, useLegacyDecorators) {
  const decorators ***REMOVED*** getDecorators(node);
  const parameters ***REMOVED*** useLegacyDecorators ? getDecoratorsOfParameters(getFirstConstructorWithBody(node)) : void 0;
  if (!some(decorators) && !some(parameters)) {
    return void 0;
  }
  return {
    decorators,
    parameters
  };
}
function getAllDecoratorsOfClassElement(member, parent, useLegacyDecorators) {
  switch (member.kind) {
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
      if (!useLegacyDecorators) {
        return getAllDecoratorsOfMethod(
          member,
          /*useLegacyDecorators*/
          false
        );
      }
      return getAllDecoratorsOfAccessors(
        member,
        parent,
        /*useLegacyDecorators*/
        true
      );
    case 175 /* MethodDeclaration */:
      return getAllDecoratorsOfMethod(member, useLegacyDecorators);
    case 173 /* PropertyDeclaration */:
      return getAllDecoratorsOfProperty(member);
    default:
      return void 0;
  }
}
function getAllDecoratorsOfAccessors(accessor, parent, useLegacyDecorators) {
  if (!accessor.body) {
    return void 0;
  }
  const { firstAccessor, secondAccessor, getAccessor, setAccessor } ***REMOVED*** getAllAccessorDeclarations(parent.members, accessor);
  const firstAccessorWithDecorators ***REMOVED*** hasDecorators(firstAccessor) ? firstAccessor : secondAccessor && hasDecorators(secondAccessor) ? secondAccessor : void 0;
  if (!firstAccessorWithDecorators || accessor !***REMOVED******REMOVED*** firstAccessorWithDecorators) {
    return void 0;
  }
  const decorators ***REMOVED*** getDecorators(firstAccessorWithDecorators);
  const parameters ***REMOVED*** useLegacyDecorators ? getDecoratorsOfParameters(setAccessor) : void 0;
  if (!some(decorators) && !some(parameters)) {
    return void 0;
  }
  return {
    decorators,
    parameters,
    getDecorators: getAccessor && getDecorators(getAccessor),
    setDecorators: setAccessor && getDecorators(setAccessor)
  };
}
function getAllDecoratorsOfMethod(method, useLegacyDecorators) {
  if (!method.body) {
    return void 0;
  }
  const decorators ***REMOVED*** getDecorators(method);
  const parameters ***REMOVED*** useLegacyDecorators ? getDecoratorsOfParameters(method) : void 0;
  if (!some(decorators) && !some(parameters)) {
    return void 0;
  }
  return { decorators, parameters };
}
function getAllDecoratorsOfProperty(property) {
  const decorators ***REMOVED*** getDecorators(property);
  if (!some(decorators)) {
    return void 0;
  }
  return { decorators };
}
function walkUpLexicalEnvironments(env, cb) {
  while (env) {
    const result ***REMOVED*** cb(env);
    if (result !***REMOVED******REMOVED*** void 0) return result;
    env ***REMOVED*** env.previous;
  }
}
function newPrivateEnvironment(data) {
  return { data };
}
function getPrivateIdentifier(privateEnv, name) {
  var _a, _b;
  return isGeneratedPrivateIdentifier(name) ? (_a ***REMOVED*** privateEnv ***REMOVED******REMOVED*** null ? void 0 : privateEnv.generatedIdentifiers) ***REMOVED******REMOVED*** null ? void 0 : _a.get(getNodeForGeneratedName(name)) : (_b ***REMOVED*** privateEnv ***REMOVED******REMOVED*** null ? void 0 : privateEnv.identifiers) ***REMOVED******REMOVED*** null ? void 0 : _b.get(name.escapedText);
}
function setPrivateIdentifier(privateEnv, name, entry) {
  if (isGeneratedPrivateIdentifier(name)) {
    privateEnv.generatedIdentifiers ?? (privateEnv.generatedIdentifiers ***REMOVED*** /* @__PURE__ */ new Map());
    privateEnv.generatedIdentifiers.set(getNodeForGeneratedName(name), entry);
  } else {
    privateEnv.identifiers ?? (privateEnv.identifiers ***REMOVED*** /* @__PURE__ */ new Map());
    privateEnv.identifiers.set(name.escapedText, entry);
  }
}
function accessPrivateIdentifier(env, name) {
  return walkUpLexicalEnvironments(env, (env2) ***REMOVED***> getPrivateIdentifier(env2.privateEnv, name));
}
function isSimpleParameter(node) {
  return !node.initializer && isIdentifier(node.name);
}
function isSimpleParameterList(nodes) {
  return every(nodes, isSimpleParameter);
}
function rewriteModuleSpecifier(node, compilerOptions) {
  if (!node || !isStringLiteral(node) || !shouldRewriteModuleSpecifier(node.text, compilerOptions)) {
    return node;
  }
  const updatedText ***REMOVED*** changeExtension(node.text, getOutputExtension(node.text, compilerOptions));
  return updatedText !***REMOVED******REMOVED*** node.text ? setOriginalNode(setTextRange(factory.createStringLiteral(updatedText, node.singleQuote), node), node) : node;
}

// src/compiler/transformers/destructuring.ts
function flattenDestructuringAssignment(node, visitor, context, level, needsValue, createAssignmentCallback) {
  let location ***REMOVED*** node;
  let value;
  if (isDestructuringAssignment(node)) {
    value ***REMOVED*** node.right;
    while (isEmptyArrayLiteral(node.left) || isEmptyObjectLiteral(node.left)) {
      if (isDestructuringAssignment(value)) {
        location ***REMOVED*** node ***REMOVED*** value;
        value ***REMOVED*** node.right;
      } else {
        return Debug.checkDefined(visitNode(value, visitor, isExpression));
      }
    }
  }
  let expressions;
  const flattenContext ***REMOVED*** {
    context,
    level,
    downlevelIteration: !!context.getCompilerOptions().downlevelIteration,
    hoistTempVariables: true,
    emitExpression,
    emitBindingOrAssignment,
    createArrayBindingOrAssignmentPattern: (elements) ***REMOVED***> makeArrayAssignmentPattern(context.factory, elements),
    createObjectBindingOrAssignmentPattern: (elements) ***REMOVED***> makeObjectAssignmentPattern(context.factory, elements),
    createArrayBindingOrAssignmentElement: makeAssignmentElement,
    visitor
  };
  if (value) {
    value ***REMOVED*** visitNode(value, visitor, isExpression);
    Debug.assert(value);
    if (isIdentifier(value) && bindingOrAssignmentElementAssignsToName(node, value.escapedText) || bindingOrAssignmentElementContainsNonLiteralComputedName(node)) {
      value ***REMOVED*** ensureIdentifier(
        flattenContext,
        value,
        /*reuseIdentifierExpressions*/
        false,
        location
      );
    } else if (needsValue) {
      value ***REMOVED*** ensureIdentifier(
        flattenContext,
        value,
        /*reuseIdentifierExpressions*/
        true,
        location
      );
    } else if (nodeIsSynthesized(node)) {
      location ***REMOVED*** value;
    }
  }
  flattenBindingOrAssignmentElement(
    flattenContext,
    node,
    value,
    location,
    /*skipInitializer*/
    isDestructuringAssignment(node)
  );
  if (value && needsValue) {
    if (!some(expressions)) {
      return value;
    }
    expressions.push(value);
  }
  return context.factory.inlineExpressions(expressions) || context.factory.createOmittedExpression();
  function emitExpression(expression) {
    expressions ***REMOVED*** append(expressions, expression);
  }
  function emitBindingOrAssignment(target, value2, location2, original) {
    Debug.assertNode(target, createAssignmentCallback ? isIdentifier : isExpression);
    const expression ***REMOVED*** createAssignmentCallback ? createAssignmentCallback(target, value2, location2) : setTextRange(
      context.factory.createAssignment(Debug.checkDefined(visitNode(target, visitor, isExpression)), value2),
      location2
    );
    expression.original ***REMOVED*** original;
    emitExpression(expression);
  }
}
function bindingOrAssignmentElementAssignsToName(element, escapedName) {
  const target ***REMOVED*** getTargetOfBindingOrAssignmentElement(element);
  if (isBindingOrAssignmentPattern(target)) {
    return bindingOrAssignmentPatternAssignsToName(target, escapedName);
  } else if (isIdentifier(target)) {
    return target.escapedText ***REMOVED******REMOVED******REMOVED*** escapedName;
  }
  return false;
}
function bindingOrAssignmentPatternAssignsToName(pattern, escapedName) {
  const elements ***REMOVED*** getElementsOfBindingOrAssignmentPattern(pattern);
  for (const element of elements) {
    if (bindingOrAssignmentElementAssignsToName(element, escapedName)) {
      return true;
    }
  }
  return false;
}
function bindingOrAssignmentElementContainsNonLiteralComputedName(element) {
  const propertyName ***REMOVED*** tryGetPropertyNameOfBindingOrAssignmentElement(element);
  if (propertyName && isComputedPropertyName(propertyName) && !isLiteralExpression(propertyName.expression)) {
    return true;
  }
  const target ***REMOVED*** getTargetOfBindingOrAssignmentElement(element);
  return !!target && isBindingOrAssignmentPattern(target) && bindingOrAssignmentPatternContainsNonLiteralComputedName(target);
}
function bindingOrAssignmentPatternContainsNonLiteralComputedName(pattern) {
  return !!forEach(getElementsOfBindingOrAssignmentPattern(pattern), bindingOrAssignmentElementContainsNonLiteralComputedName);
}
function flattenDestructuringBinding(node, visitor, context, level, rval, hoistTempVariables ***REMOVED*** false, skipInitializer) {
  let pendingExpressions;
  const pendingDeclarations ***REMOVED*** [];
  const declarations ***REMOVED*** [];
  const flattenContext ***REMOVED*** {
    context,
    level,
    downlevelIteration: !!context.getCompilerOptions().downlevelIteration,
    hoistTempVariables,
    emitExpression,
    emitBindingOrAssignment,
    createArrayBindingOrAssignmentPattern: (elements) ***REMOVED***> makeArrayBindingPattern(context.factory, elements),
    createObjectBindingOrAssignmentPattern: (elements) ***REMOVED***> makeObjectBindingPattern(context.factory, elements),
    createArrayBindingOrAssignmentElement: (name) ***REMOVED***> makeBindingElement(context.factory, name),
    visitor
  };
  if (isVariableDeclaration(node)) {
    let initializer ***REMOVED*** getInitializerOfBindingOrAssignmentElement(node);
    if (initializer && (isIdentifier(initializer) && bindingOrAssignmentElementAssignsToName(node, initializer.escapedText) || bindingOrAssignmentElementContainsNonLiteralComputedName(node))) {
      initializer ***REMOVED*** ensureIdentifier(
        flattenContext,
        Debug.checkDefined(visitNode(initializer, flattenContext.visitor, isExpression)),
        /*reuseIdentifierExpressions*/
        false,
        initializer
      );
      node ***REMOVED*** context.factory.updateVariableDeclaration(
        node,
        node.name,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        initializer
      );
    }
  }
  flattenBindingOrAssignmentElement(flattenContext, node, rval, node, skipInitializer);
  if (pendingExpressions) {
    const temp ***REMOVED*** context.factory.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    if (hoistTempVariables) {
      const value ***REMOVED*** context.factory.inlineExpressions(pendingExpressions);
      pendingExpressions ***REMOVED*** void 0;
      emitBindingOrAssignment(
        temp,
        value,
        /*location*/
        void 0,
        /*original*/
        void 0
      );
    } else {
      context.hoistVariableDeclaration(temp);
      const pendingDeclaration ***REMOVED*** last(pendingDeclarations);
      pendingDeclaration.pendingExpressions ***REMOVED*** append(
        pendingDeclaration.pendingExpressions,
        context.factory.createAssignment(temp, pendingDeclaration.value)
      );
      addRange(pendingDeclaration.pendingExpressions, pendingExpressions);
      pendingDeclaration.value ***REMOVED*** temp;
    }
  }
  for (const { pendingExpressions: pendingExpressions2, name, value, location, original } of pendingDeclarations) {
    const variable ***REMOVED*** context.factory.createVariableDeclaration(
      name,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      pendingExpressions2 ? context.factory.inlineExpressions(append(pendingExpressions2, value)) : value
    );
    variable.original ***REMOVED*** original;
    setTextRange(variable, location);
    declarations.push(variable);
  }
  return declarations;
  function emitExpression(value) {
    pendingExpressions ***REMOVED*** append(pendingExpressions, value);
  }
  function emitBindingOrAssignment(target, value, location, original) {
    Debug.assertNode(target, isBindingName);
    if (pendingExpressions) {
      value ***REMOVED*** context.factory.inlineExpressions(append(pendingExpressions, value));
      pendingExpressions ***REMOVED*** void 0;
    }
    pendingDeclarations.push({ pendingExpressions, name: target, value, location, original });
  }
}
function flattenBindingOrAssignmentElement(flattenContext, element, value, location, skipInitializer) {
  const bindingTarget ***REMOVED*** getTargetOfBindingOrAssignmentElement(element);
  if (!skipInitializer) {
    const initializer ***REMOVED*** visitNode(getInitializerOfBindingOrAssignmentElement(element), flattenContext.visitor, isExpression);
    if (initializer) {
      if (value) {
        value ***REMOVED*** createDefaultValueCheck(flattenContext, value, initializer, location);
        if (!isSimpleInlineableExpression(initializer) && isBindingOrAssignmentPattern(bindingTarget)) {
          value ***REMOVED*** ensureIdentifier(
            flattenContext,
            value,
            /*reuseIdentifierExpressions*/
            true,
            location
          );
        }
      } else {
        value ***REMOVED*** initializer;
      }
    } else if (!value) {
      value ***REMOVED*** flattenContext.context.factory.createVoidZero();
    }
  }
  if (isObjectBindingOrAssignmentPattern(bindingTarget)) {
    flattenObjectBindingOrAssignmentPattern(flattenContext, element, bindingTarget, value, location);
  } else if (isArrayBindingOrAssignmentPattern(bindingTarget)) {
    flattenArrayBindingOrAssignmentPattern(flattenContext, element, bindingTarget, value, location);
  } else {
    flattenContext.emitBindingOrAssignment(
      bindingTarget,
      value,
      location,
      /*original*/
      element
    );
  }
}
function flattenObjectBindingOrAssignmentPattern(flattenContext, parent, pattern, value, location) {
  const elements ***REMOVED*** getElementsOfBindingOrAssignmentPattern(pattern);
  const numElements ***REMOVED*** elements.length;
  if (numElements !***REMOVED******REMOVED*** 1) {
    const reuseIdentifierExpressions ***REMOVED*** !isDeclarationBindingElement(parent) || numElements !***REMOVED******REMOVED*** 0;
    value ***REMOVED*** ensureIdentifier(flattenContext, value, reuseIdentifierExpressions, location);
  }
  let bindingElements;
  let computedTempVariables;
  for (let i ***REMOVED*** 0; i < numElements; i++) {
    const element ***REMOVED*** elements[i];
    if (!getRestIndicatorOfBindingOrAssignmentElement(element)) {
      const propertyName ***REMOVED*** getPropertyNameOfBindingOrAssignmentElement(element);
      if (flattenContext.level >***REMOVED*** 1 /* ObjectRest */ && !(element.transformFlags & (32768 /* ContainsRestOrSpread */ | 65536 /* ContainsObjectRestOrSpread */)) && !(getTargetOfBindingOrAssignmentElement(element).transformFlags & (32768 /* ContainsRestOrSpread */ | 65536 /* ContainsObjectRestOrSpread */)) && !isComputedPropertyName(propertyName)) {
        bindingElements ***REMOVED*** append(bindingElements, visitNode(element, flattenContext.visitor, isBindingOrAssignmentElement));
      } else {
        if (bindingElements) {
          flattenContext.emitBindingOrAssignment(flattenContext.createObjectBindingOrAssignmentPattern(bindingElements), value, location, pattern);
          bindingElements ***REMOVED*** void 0;
        }
        const rhsValue ***REMOVED*** createDestructuringPropertyAccess(flattenContext, value, propertyName);
        if (isComputedPropertyName(propertyName)) {
          computedTempVariables ***REMOVED*** append(computedTempVariables, rhsValue.argumentExpression);
        }
        flattenBindingOrAssignmentElement(
          flattenContext,
          element,
          rhsValue,
          /*location*/
          element
        );
      }
    } else if (i ***REMOVED******REMOVED******REMOVED*** numElements - 1) {
      if (bindingElements) {
        flattenContext.emitBindingOrAssignment(flattenContext.createObjectBindingOrAssignmentPattern(bindingElements), value, location, pattern);
        bindingElements ***REMOVED*** void 0;
      }
      const rhsValue ***REMOVED*** flattenContext.context.getEmitHelperFactory().createRestHelper(value, elements, computedTempVariables, pattern);
      flattenBindingOrAssignmentElement(flattenContext, element, rhsValue, element);
    }
  }
  if (bindingElements) {
    flattenContext.emitBindingOrAssignment(flattenContext.createObjectBindingOrAssignmentPattern(bindingElements), value, location, pattern);
  }
}
function flattenArrayBindingOrAssignmentPattern(flattenContext, parent, pattern, value, location) {
  const elements ***REMOVED*** getElementsOfBindingOrAssignmentPattern(pattern);
  const numElements ***REMOVED*** elements.length;
  if (flattenContext.level < 1 /* ObjectRest */ && flattenContext.downlevelIteration) {
    value ***REMOVED*** ensureIdentifier(
      flattenContext,
      setTextRange(
        flattenContext.context.getEmitHelperFactory().createReadHelper(
          value,
          numElements > 0 && getRestIndicatorOfBindingOrAssignmentElement(elements[numElements - 1]) ? void 0 : numElements
        ),
        location
      ),
      /*reuseIdentifierExpressions*/
      false,
      location
    );
  } else if (numElements !***REMOVED******REMOVED*** 1 && (flattenContext.level < 1 /* ObjectRest */ || numElements ***REMOVED******REMOVED******REMOVED*** 0) || every(elements, isOmittedExpression)) {
    const reuseIdentifierExpressions ***REMOVED*** !isDeclarationBindingElement(parent) || numElements !***REMOVED******REMOVED*** 0;
    value ***REMOVED*** ensureIdentifier(flattenContext, value, reuseIdentifierExpressions, location);
  }
  let bindingElements;
  let restContainingElements;
  for (let i ***REMOVED*** 0; i < numElements; i++) {
    const element ***REMOVED*** elements[i];
    if (flattenContext.level >***REMOVED*** 1 /* ObjectRest */) {
      if (element.transformFlags & 65536 /* ContainsObjectRestOrSpread */ || flattenContext.hasTransformedPriorElement && !isSimpleBindingOrAssignmentElement(element)) {
        flattenContext.hasTransformedPriorElement ***REMOVED*** true;
        const temp ***REMOVED*** flattenContext.context.factory.createTempVariable(
          /*recordTempVariable*/
          void 0
        );
        if (flattenContext.hoistTempVariables) {
          flattenContext.context.hoistVariableDeclaration(temp);
        }
        restContainingElements ***REMOVED*** append(restContainingElements, [temp, element]);
        bindingElements ***REMOVED*** append(bindingElements, flattenContext.createArrayBindingOrAssignmentElement(temp));
      } else {
        bindingElements ***REMOVED*** append(bindingElements, element);
      }
    } else if (isOmittedExpression(element)) {
      continue;
    } else if (!getRestIndicatorOfBindingOrAssignmentElement(element)) {
      const rhsValue ***REMOVED*** flattenContext.context.factory.createElementAccessExpression(value, i);
      flattenBindingOrAssignmentElement(
        flattenContext,
        element,
        rhsValue,
        /*location*/
        element
      );
    } else if (i ***REMOVED******REMOVED******REMOVED*** numElements - 1) {
      const rhsValue ***REMOVED*** flattenContext.context.factory.createArraySliceCall(value, i);
      flattenBindingOrAssignmentElement(
        flattenContext,
        element,
        rhsValue,
        /*location*/
        element
      );
    }
  }
  if (bindingElements) {
    flattenContext.emitBindingOrAssignment(flattenContext.createArrayBindingOrAssignmentPattern(bindingElements), value, location, pattern);
  }
  if (restContainingElements) {
    for (const [id, element] of restContainingElements) {
      flattenBindingOrAssignmentElement(flattenContext, element, id, element);
    }
  }
}
function isSimpleBindingOrAssignmentElement(element) {
  const target ***REMOVED*** getTargetOfBindingOrAssignmentElement(element);
  if (!target || isOmittedExpression(target)) return true;
  const propertyName ***REMOVED*** tryGetPropertyNameOfBindingOrAssignmentElement(element);
  if (propertyName && !isPropertyNameLiteral(propertyName)) return false;
  const initializer ***REMOVED*** getInitializerOfBindingOrAssignmentElement(element);
  if (initializer && !isSimpleInlineableExpression(initializer)) return false;
  if (isBindingOrAssignmentPattern(target)) return every(getElementsOfBindingOrAssignmentPattern(target), isSimpleBindingOrAssignmentElement);
  return isIdentifier(target);
}
function createDefaultValueCheck(flattenContext, value, defaultValue, location) {
  value ***REMOVED*** ensureIdentifier(
    flattenContext,
    value,
    /*reuseIdentifierExpressions*/
    true,
    location
  );
  return flattenContext.context.factory.createConditionalExpression(
    flattenContext.context.factory.createTypeCheck(value, "undefined"),
    /*questionToken*/
    void 0,
    defaultValue,
    /*colonToken*/
    void 0,
    value
  );
}
function createDestructuringPropertyAccess(flattenContext, value, propertyName) {
  const { factory: factory2 } ***REMOVED*** flattenContext.context;
  if (isComputedPropertyName(propertyName)) {
    const argumentExpression ***REMOVED*** ensureIdentifier(
      flattenContext,
      Debug.checkDefined(visitNode(propertyName.expression, flattenContext.visitor, isExpression)),
      /*reuseIdentifierExpressions*/
      false,
      /*location*/
      propertyName
    );
    return flattenContext.context.factory.createElementAccessExpression(value, argumentExpression);
  } else if (isStringOrNumericLiteralLike(propertyName) || isBigIntLiteral(propertyName)) {
    const argumentExpression ***REMOVED*** factory2.cloneNode(propertyName);
    return flattenContext.context.factory.createElementAccessExpression(value, argumentExpression);
  } else {
    const name ***REMOVED*** flattenContext.context.factory.createIdentifier(idText(propertyName));
    return flattenContext.context.factory.createPropertyAccessExpression(value, name);
  }
}
function ensureIdentifier(flattenContext, value, reuseIdentifierExpressions, location) {
  if (isIdentifier(value) && reuseIdentifierExpressions) {
    return value;
  } else {
    const temp ***REMOVED*** flattenContext.context.factory.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    if (flattenContext.hoistTempVariables) {
      flattenContext.context.hoistVariableDeclaration(temp);
      flattenContext.emitExpression(setTextRange(flattenContext.context.factory.createAssignment(temp, value), location));
    } else {
      flattenContext.emitBindingOrAssignment(
        temp,
        value,
        location,
        /*original*/
        void 0
      );
    }
    return temp;
  }
}
function makeArrayBindingPattern(factory2, elements) {
  Debug.assertEachNode(elements, isArrayBindingElement);
  return factory2.createArrayBindingPattern(elements);
}
function makeArrayAssignmentPattern(factory2, elements) {
  Debug.assertEachNode(elements, isArrayBindingOrAssignmentElement);
  return factory2.createArrayLiteralExpression(map(elements, factory2.converters.convertToArrayAssignmentElement));
}
function makeObjectBindingPattern(factory2, elements) {
  Debug.assertEachNode(elements, isBindingElement);
  return factory2.createObjectBindingPattern(elements);
}
function makeObjectAssignmentPattern(factory2, elements) {
  Debug.assertEachNode(elements, isObjectBindingOrAssignmentElement);
  return factory2.createObjectLiteralExpression(map(elements, factory2.converters.convertToObjectAssignmentElement));
}
function makeBindingElement(factory2, name) {
  return factory2.createBindingElement(
    /*dotDotDotToken*/
    void 0,
    /*propertyName*/
    void 0,
    name
  );
}
function makeAssignmentElement(name) {
  return name;
}

// src/compiler/transformers/classThis.ts
function createClassThisAssignmentBlock(factory2, classThis, thisExpression ***REMOVED*** factory2.createThis()) {
  const expression ***REMOVED*** factory2.createAssignment(classThis, thisExpression);
  const statement ***REMOVED*** factory2.createExpressionStatement(expression);
  const body ***REMOVED*** factory2.createBlock(
    [statement],
    /*multiLine*/
    false
  );
  const block ***REMOVED*** factory2.createClassStaticBlockDeclaration(body);
  getOrCreateEmitNode(block).classThis ***REMOVED*** classThis;
  return block;
}
function isClassThisAssignmentBlock(node) {
  var _a;
  if (!isClassStaticBlockDeclaration(node) || node.body.statements.length !***REMOVED******REMOVED*** 1) {
    return false;
  }
  const statement ***REMOVED*** node.body.statements[0];
  return isExpressionStatement(statement) && isAssignmentExpression(
    statement.expression,
    /*excludeCompoundAssignment*/
    true
  ) && isIdentifier(statement.expression.left) && ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis) ***REMOVED******REMOVED******REMOVED*** statement.expression.left && statement.expression.right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
}
function classHasClassThisAssignment(node) {
  var _a;
  return !!((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis) && some(node.members, isClassThisAssignmentBlock);
}
function injectClassThisAssignmentIfMissing(factory2, node, classThis, thisExpression) {
  if (classHasClassThisAssignment(node)) {
    return node;
  }
  const staticBlock ***REMOVED*** createClassThisAssignmentBlock(factory2, classThis, thisExpression);
  if (node.name) {
    setSourceMapRange(staticBlock.body.statements[0], node.name);
  }
  const members ***REMOVED*** factory2.createNodeArray([staticBlock, ...node.members]);
  setTextRange(members, node.members);
  const updatedNode ***REMOVED*** isClassDeclaration(node) ? factory2.updateClassDeclaration(
    node,
    node.modifiers,
    node.name,
    node.typeParameters,
    node.heritageClauses,
    members
  ) : factory2.updateClassExpression(
    node,
    node.modifiers,
    node.name,
    node.typeParameters,
    node.heritageClauses,
    members
  );
  getOrCreateEmitNode(updatedNode).classThis ***REMOVED*** classThis;
  return updatedNode;
}

// src/compiler/transformers/namedEvaluation.ts
function getAssignedNameOfIdentifier(factory2, name, expression) {
  const original ***REMOVED*** getOriginalNode(skipOuterExpressions(expression));
  if ((isClassDeclaration(original) || isFunctionDeclaration(original)) && !original.name && hasSyntacticModifier(original, 2048 /* Default */)) {
    return factory2.createStringLiteral("default");
  }
  return factory2.createStringLiteralFromNode(name);
}
function getAssignedNameOfPropertyName(context, name, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  if (assignedNameText !***REMOVED******REMOVED*** void 0) {
    const assignedName2 ***REMOVED*** factory2.createStringLiteral(assignedNameText);
    return { assignedName: assignedName2, name };
  }
  if (isPropertyNameLiteral(name) || isPrivateIdentifier(name)) {
    const assignedName2 ***REMOVED*** factory2.createStringLiteralFromNode(name);
    return { assignedName: assignedName2, name };
  }
  if (isPropertyNameLiteral(name.expression) && !isIdentifier(name.expression)) {
    const assignedName2 ***REMOVED*** factory2.createStringLiteralFromNode(name.expression);
    return { assignedName: assignedName2, name };
  }
  const assignedName ***REMOVED*** factory2.getGeneratedNameForNode(name);
  context.hoistVariableDeclaration(assignedName);
  const key ***REMOVED*** context.getEmitHelperFactory().createPropKeyHelper(name.expression);
  const assignment ***REMOVED*** factory2.createAssignment(assignedName, key);
  const updatedName ***REMOVED*** factory2.updateComputedPropertyName(name, assignment);
  return { assignedName, name: updatedName };
}
function createClassNamedEvaluationHelperBlock(context, assignedName, thisExpression ***REMOVED*** context.factory.createThis()) {
  const { factory: factory2 } ***REMOVED*** context;
  const expression ***REMOVED*** context.getEmitHelperFactory().createSetFunctionNameHelper(thisExpression, assignedName);
  const statement ***REMOVED*** factory2.createExpressionStatement(expression);
  const body ***REMOVED*** factory2.createBlock(
    [statement],
    /*multiLine*/
    false
  );
  const block ***REMOVED*** factory2.createClassStaticBlockDeclaration(body);
  getOrCreateEmitNode(block).assignedName ***REMOVED*** assignedName;
  return block;
}
function isClassNamedEvaluationHelperBlock(node) {
  var _a;
  if (!isClassStaticBlockDeclaration(node) || node.body.statements.length !***REMOVED******REMOVED*** 1) {
    return false;
  }
  const statement ***REMOVED*** node.body.statements[0];
  return isExpressionStatement(statement) && isCallToHelper(statement.expression, "___setFunctionName") && statement.expression.arguments.length >***REMOVED*** 2 && statement.expression.arguments[1] ***REMOVED******REMOVED******REMOVED*** ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.assignedName);
}
function classHasExplicitlyAssignedName(node) {
  var _a;
  return !!((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.assignedName) && some(node.members, isClassNamedEvaluationHelperBlock);
}
function classHasDeclaredOrExplicitlyAssignedName(node) {
  return !!node.name || classHasExplicitlyAssignedName(node);
}
function injectClassNamedEvaluationHelperBlockIfMissing(context, node, assignedName, thisExpression) {
  if (classHasExplicitlyAssignedName(node)) {
    return node;
  }
  const { factory: factory2 } ***REMOVED*** context;
  const namedEvaluationBlock ***REMOVED*** createClassNamedEvaluationHelperBlock(context, assignedName, thisExpression);
  if (node.name) {
    setSourceMapRange(namedEvaluationBlock.body.statements[0], node.name);
  }
  const insertionIndex ***REMOVED*** findIndex(node.members, isClassThisAssignmentBlock) + 1;
  const leading ***REMOVED*** node.members.slice(0, insertionIndex);
  const trailing ***REMOVED*** node.members.slice(insertionIndex);
  const members ***REMOVED*** factory2.createNodeArray([...leading, namedEvaluationBlock, ...trailing]);
  setTextRange(members, node.members);
  node ***REMOVED*** isClassDeclaration(node) ? factory2.updateClassDeclaration(
    node,
    node.modifiers,
    node.name,
    node.typeParameters,
    node.heritageClauses,
    members
  ) : factory2.updateClassExpression(
    node,
    node.modifiers,
    node.name,
    node.typeParameters,
    node.heritageClauses,
    members
  );
  getOrCreateEmitNode(node).assignedName ***REMOVED*** assignedName;
  return node;
}
function finishTransformNamedEvaluation(context, expression, assignedName, ignoreEmptyStringLiteral) {
  if (ignoreEmptyStringLiteral && isStringLiteral(assignedName) && isEmptyStringLiteral(assignedName)) {
    return expression;
  }
  const { factory: factory2 } ***REMOVED*** context;
  const innerExpression ***REMOVED*** skipOuterExpressions(expression);
  const updatedExpression ***REMOVED*** isClassExpression(innerExpression) ? cast(injectClassNamedEvaluationHelperBlockIfMissing(context, innerExpression, assignedName), isClassExpression) : context.getEmitHelperFactory().createSetFunctionNameHelper(innerExpression, assignedName);
  return factory2.restoreOuterExpressions(expression, updatedExpression);
}
function transformNamedEvaluationOfPropertyAssignment(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const { assignedName, name } ***REMOVED*** getAssignedNameOfPropertyName(context, node.name, assignedNameText);
  const initializer ***REMOVED*** finishTransformNamedEvaluation(context, node.initializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updatePropertyAssignment(
    node,
    name,
    initializer
  );
}
function transformNamedEvaluationOfShorthandAssignmentProperty(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : getAssignedNameOfIdentifier(factory2, node.name, node.objectAssignmentInitializer);
  const objectAssignmentInitializer ***REMOVED*** finishTransformNamedEvaluation(context, node.objectAssignmentInitializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateShorthandPropertyAssignment(
    node,
    node.name,
    objectAssignmentInitializer
  );
}
function transformNamedEvaluationOfVariableDeclaration(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : getAssignedNameOfIdentifier(factory2, node.name, node.initializer);
  const initializer ***REMOVED*** finishTransformNamedEvaluation(context, node.initializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateVariableDeclaration(
    node,
    node.name,
    node.exclamationToken,
    node.type,
    initializer
  );
}
function transformNamedEvaluationOfParameterDeclaration(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : getAssignedNameOfIdentifier(factory2, node.name, node.initializer);
  const initializer ***REMOVED*** finishTransformNamedEvaluation(context, node.initializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateParameterDeclaration(
    node,
    node.modifiers,
    node.dotDotDotToken,
    node.name,
    node.questionToken,
    node.type,
    initializer
  );
}
function transformNamedEvaluationOfBindingElement(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : getAssignedNameOfIdentifier(factory2, node.name, node.initializer);
  const initializer ***REMOVED*** finishTransformNamedEvaluation(context, node.initializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateBindingElement(
    node,
    node.dotDotDotToken,
    node.propertyName,
    node.name,
    initializer
  );
}
function transformNamedEvaluationOfPropertyDeclaration(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const { assignedName, name } ***REMOVED*** getAssignedNameOfPropertyName(context, node.name, assignedNameText);
  const initializer ***REMOVED*** finishTransformNamedEvaluation(context, node.initializer, assignedName, ignoreEmptyStringLiteral);
  return factory2.updatePropertyDeclaration(
    node,
    node.modifiers,
    name,
    node.questionToken ?? node.exclamationToken,
    node.type,
    initializer
  );
}
function transformNamedEvaluationOfAssignmentExpression(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : getAssignedNameOfIdentifier(factory2, node.left, node.right);
  const right ***REMOVED*** finishTransformNamedEvaluation(context, node.right, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateBinaryExpression(
    node,
    node.left,
    node.operatorToken,
    right
  );
}
function transformNamedEvaluationOfExportAssignment(context, node, ignoreEmptyStringLiteral, assignedNameText) {
  const { factory: factory2 } ***REMOVED*** context;
  const assignedName ***REMOVED*** assignedNameText !***REMOVED******REMOVED*** void 0 ? factory2.createStringLiteral(assignedNameText) : factory2.createStringLiteral(node.isExportEquals ? "" : "default");
  const expression ***REMOVED*** finishTransformNamedEvaluation(context, node.expression, assignedName, ignoreEmptyStringLiteral);
  return factory2.updateExportAssignment(
    node,
    node.modifiers,
    expression
  );
}
function transformNamedEvaluation(context, node, ignoreEmptyStringLiteral, assignedName) {
  switch (node.kind) {
    case 304 /* PropertyAssignment */:
      return transformNamedEvaluationOfPropertyAssignment(context, node, ignoreEmptyStringLiteral, assignedName);
    case 305 /* ShorthandPropertyAssignment */:
      return transformNamedEvaluationOfShorthandAssignmentProperty(context, node, ignoreEmptyStringLiteral, assignedName);
    case 261 /* VariableDeclaration */:
      return transformNamedEvaluationOfVariableDeclaration(context, node, ignoreEmptyStringLiteral, assignedName);
    case 170 /* Parameter */:
      return transformNamedEvaluationOfParameterDeclaration(context, node, ignoreEmptyStringLiteral, assignedName);
    case 209 /* BindingElement */:
      return transformNamedEvaluationOfBindingElement(context, node, ignoreEmptyStringLiteral, assignedName);
    case 173 /* PropertyDeclaration */:
      return transformNamedEvaluationOfPropertyDeclaration(context, node, ignoreEmptyStringLiteral, assignedName);
    case 227 /* BinaryExpression */:
      return transformNamedEvaluationOfAssignmentExpression(context, node, ignoreEmptyStringLiteral, assignedName);
    case 278 /* ExportAssignment */:
      return transformNamedEvaluationOfExportAssignment(context, node, ignoreEmptyStringLiteral, assignedName);
  }
}

// src/compiler/transformers/taggedTemplate.ts
function processTaggedTemplateExpression(context, node, visitor, currentSourceFile, recordTaggedTemplateString, level) {
  const tag ***REMOVED*** visitNode(node.tag, visitor, isExpression);
  Debug.assert(tag);
  const templateArguments ***REMOVED*** [void 0];
  const cookedStrings ***REMOVED*** [];
  const rawStrings ***REMOVED*** [];
  const template ***REMOVED*** node.template;
  if (level ***REMOVED******REMOVED******REMOVED*** 0 /* LiftRestriction */ && !hasInvalidEscape(template)) {
    return visitEachChild(node, visitor, context);
  }
  const { factory: factory2 } ***REMOVED*** context;
  if (isNoSubstitutionTemplateLiteral(template)) {
    cookedStrings.push(createTemplateCooked(factory2, template));
    rawStrings.push(getRawLiteral(factory2, template, currentSourceFile));
  } else {
    cookedStrings.push(createTemplateCooked(factory2, template.head));
    rawStrings.push(getRawLiteral(factory2, template.head, currentSourceFile));
    for (const templateSpan of template.templateSpans) {
      cookedStrings.push(createTemplateCooked(factory2, templateSpan.literal));
      rawStrings.push(getRawLiteral(factory2, templateSpan.literal, currentSourceFile));
      templateArguments.push(Debug.checkDefined(visitNode(templateSpan.expression, visitor, isExpression)));
    }
  }
  const helperCall ***REMOVED*** context.getEmitHelperFactory().createTemplateObjectHelper(
    factory2.createArrayLiteralExpression(cookedStrings),
    factory2.createArrayLiteralExpression(rawStrings)
  );
  if (isExternalModule(currentSourceFile)) {
    const tempVar ***REMOVED*** factory2.createUniqueName("templateObject");
    recordTaggedTemplateString(tempVar);
    templateArguments[0] ***REMOVED*** factory2.createLogicalOr(
      tempVar,
      factory2.createAssignment(
        tempVar,
        helperCall
      )
    );
  } else {
    templateArguments[0] ***REMOVED*** helperCall;
  }
  return factory2.createCallExpression(
    tag,
    /*typeArguments*/
    void 0,
    templateArguments
  );
}
function createTemplateCooked(factory2, template) {
  return template.templateFlags & 26656 /* IsInvalid */ ? factory2.createVoidZero() : factory2.createStringLiteral(template.text);
}
function getRawLiteral(factory2, node, currentSourceFile) {
  let text ***REMOVED*** node.rawText;
  if (text ***REMOVED******REMOVED******REMOVED*** void 0) {
    Debug.assertIsDefined(currentSourceFile, "Template literal node is missing 'rawText' and does not have a source file. Possibly bad transform.");
    text ***REMOVED*** getSourceTextOfNodeFromSourceFile(currentSourceFile, node);
    const isLast ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 15 /* NoSubstitutionTemplateLiteral */ || node.kind ***REMOVED******REMOVED******REMOVED*** 18 /* TemplateTail */;
    text ***REMOVED*** text.substring(1, text.length - (isLast ? 1 : 2));
  }
  text ***REMOVED*** text.replace(/\r\n?/g, "\n");
  return setTextRange(factory2.createStringLiteral(text), node);
}

// src/compiler/transformers/ts.ts
var USE_NEW_TYPE_METADATA_FORMAT ***REMOVED*** false;
function transformTypeScript(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    startLexicalEnvironment,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const moduleKind ***REMOVED*** getEmitModuleKind(compilerOptions);
  const legacyDecorators ***REMOVED*** !!compilerOptions.experimentalDecorators;
  const typeSerializer ***REMOVED*** compilerOptions.emitDecoratorMetadata ? createRuntimeTypeSerializer(context) : void 0;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  context.enableSubstitution(212 /* PropertyAccessExpression */);
  context.enableSubstitution(213 /* ElementAccessExpression */);
  let currentSourceFile;
  let currentNamespace;
  let currentNamespaceContainerName;
  let currentLexicalScope;
  let currentScopeFirstDeclarationsOfName;
  let enabledSubstitutions ***REMOVED*** 0 /* None */;
  let applicableSubstitutions;
  return transformSourceFileOrBundle;
  function transformSourceFileOrBundle(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */) {
      return transformBundle(node);
    }
    return transformSourceFile(node);
  }
  function transformBundle(node) {
    return factory2.createBundle(
      node.sourceFiles.map(transformSourceFile)
    );
  }
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    const visited ***REMOVED*** saveStateAndInvoke(node, visitSourceFile);
    addEmitHelpers(visited, context.readEmitHelpers());
    currentSourceFile ***REMOVED*** void 0;
    return visited;
  }
  function saveStateAndInvoke(node, f) {
    const savedCurrentScope ***REMOVED*** currentLexicalScope;
    const savedCurrentScopeFirstDeclarationsOfName ***REMOVED*** currentScopeFirstDeclarationsOfName;
    onBeforeVisitNode(node);
    const visited ***REMOVED*** f(node);
    if (currentLexicalScope !***REMOVED******REMOVED*** savedCurrentScope) {
      currentScopeFirstDeclarationsOfName ***REMOVED*** savedCurrentScopeFirstDeclarationsOfName;
    }
    currentLexicalScope ***REMOVED*** savedCurrentScope;
    return visited;
  }
  function onBeforeVisitNode(node) {
    switch (node.kind) {
      case 308 /* SourceFile */:
      case 270 /* CaseBlock */:
      case 269 /* ModuleBlock */:
      case 242 /* Block */:
        currentLexicalScope ***REMOVED*** node;
        currentScopeFirstDeclarationsOfName ***REMOVED*** void 0;
        break;
      case 264 /* ClassDeclaration */:
      case 263 /* FunctionDeclaration */:
        if (hasSyntacticModifier(node, 128 /* Ambient */)) {
          break;
        }
        if (node.name) {
          recordEmittedDeclarationInScope(node);
        } else {
          Debug.assert(node.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || hasSyntacticModifier(node, 2048 /* Default */));
        }
        break;
    }
  }
  function visitor(node) {
    return saveStateAndInvoke(node, visitorWorker);
  }
  function visitorWorker(node) {
    if (node.transformFlags & 1 /* ContainsTypeScript */) {
      return visitTypeScript(node);
    }
    return node;
  }
  function sourceElementVisitor(node) {
    return saveStateAndInvoke(node, sourceElementVisitorWorker);
  }
  function sourceElementVisitorWorker(node) {
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
      case 272 /* ImportEqualsDeclaration */:
      case 278 /* ExportAssignment */:
      case 279 /* ExportDeclaration */:
        return visitElidableStatement(node);
      default:
        return visitorWorker(node);
    }
  }
  function isElisionBlocked(node) {
    const parsed ***REMOVED*** getParseTreeNode(node);
    if (parsed ***REMOVED******REMOVED******REMOVED*** node || isExportAssignment(node)) {
      return false;
    }
    if (!parsed || parsed.kind !***REMOVED******REMOVED*** node.kind) {
      return true;
    }
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
        Debug.assertNode(parsed, isImportDeclaration);
        if (node.importClause !***REMOVED******REMOVED*** parsed.importClause) {
          return true;
        }
        if (node.attributes !***REMOVED******REMOVED*** parsed.attributes) {
          return true;
        }
        break;
      case 272 /* ImportEqualsDeclaration */:
        Debug.assertNode(parsed, isImportEqualsDeclaration);
        if (node.name !***REMOVED******REMOVED*** parsed.name) {
          return true;
        }
        if (node.isTypeOnly !***REMOVED******REMOVED*** parsed.isTypeOnly) {
          return true;
        }
        if (node.moduleReference !***REMOVED******REMOVED*** parsed.moduleReference && (isEntityName(node.moduleReference) || isEntityName(parsed.moduleReference))) {
          return true;
        }
        break;
      case 279 /* ExportDeclaration */:
        Debug.assertNode(parsed, isExportDeclaration);
        if (node.exportClause !***REMOVED******REMOVED*** parsed.exportClause) {
          return true;
        }
        if (node.attributes !***REMOVED******REMOVED*** parsed.attributes) {
          return true;
        }
        break;
    }
    return false;
  }
  function visitElidableStatement(node) {
    if (isElisionBlocked(node)) {
      if (node.transformFlags & 1 /* ContainsTypeScript */) {
        return visitEachChild(node, visitor, context);
      }
      return node;
    }
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
        return visitImportDeclaration(node);
      case 272 /* ImportEqualsDeclaration */:
        return visitImportEqualsDeclaration(node);
      case 278 /* ExportAssignment */:
        return visitExportAssignment(node);
      case 279 /* ExportDeclaration */:
        return visitExportDeclaration(node);
      default:
        Debug.fail("Unhandled ellided statement");
    }
  }
  function namespaceElementVisitor(node) {
    return saveStateAndInvoke(node, namespaceElementVisitorWorker);
  }
  function namespaceElementVisitorWorker(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 273 /* ImportDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 274 /* ImportClause */ || node.kind ***REMOVED******REMOVED******REMOVED*** 272 /* ImportEqualsDeclaration */ && node.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */) {
      return void 0;
    } else if (node.transformFlags & 1 /* ContainsTypeScript */ || hasSyntacticModifier(node, 32 /* Export */)) {
      return visitTypeScript(node);
    }
    return node;
  }
  function getClassElementVisitor(parent) {
    return (node) ***REMOVED***> saveStateAndInvoke(node, (n) ***REMOVED***> classElementVisitorWorker(n, parent));
  }
  function classElementVisitorWorker(node, parent) {
    switch (node.kind) {
      case 177 /* Constructor */:
        return visitConstructor(node);
      case 173 /* PropertyDeclaration */:
        return visitPropertyDeclaration(node, parent);
      case 178 /* GetAccessor */:
        return visitGetAccessor(node, parent);
      case 179 /* SetAccessor */:
        return visitSetAccessor(node, parent);
      case 175 /* MethodDeclaration */:
        return visitMethodDeclaration(node, parent);
      case 176 /* ClassStaticBlockDeclaration */:
        return visitEachChild(node, visitor, context);
      case 241 /* SemicolonClassElement */:
        return node;
      case 182 /* IndexSignature */:
        return;
      default:
        return Debug.failBadSyntaxKind(node);
    }
  }
  function getObjectLiteralElementVisitor(parent) {
    return (node) ***REMOVED***> saveStateAndInvoke(node, (n) ***REMOVED***> objectLiteralElementVisitorWorker(n, parent));
  }
  function objectLiteralElementVisitorWorker(node, parent) {
    switch (node.kind) {
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
      case 306 /* SpreadAssignment */:
        return visitor(node);
      case 178 /* GetAccessor */:
        return visitGetAccessor(node, parent);
      case 179 /* SetAccessor */:
        return visitSetAccessor(node, parent);
      case 175 /* MethodDeclaration */:
        return visitMethodDeclaration(node, parent);
      default:
        return Debug.failBadSyntaxKind(node);
    }
  }
  function decoratorElidingVisitor(node) {
    return isDecorator(node) ? void 0 : visitor(node);
  }
  function modifierElidingVisitor(node) {
    return isModifier(node) ? void 0 : visitor(node);
  }
  function modifierVisitor(node) {
    if (isDecorator(node)) return void 0;
    if (modifierToFlag(node.kind) & 28895 /* TypeScriptModifier */) {
      return void 0;
    } else if (currentNamespace && node.kind ***REMOVED******REMOVED******REMOVED*** 95 /* ExportKeyword */) {
      return void 0;
    }
    return node;
  }
  function visitTypeScript(node) {
    if (isStatement(node) && hasSyntacticModifier(node, 128 /* Ambient */)) {
      return factory2.createNotEmittedStatement(node);
    }
    switch (node.kind) {
      case 95 /* ExportKeyword */:
      case 90 /* DefaultKeyword */:
        return currentNamespace ? void 0 : node;
      case 125 /* PublicKeyword */:
      case 123 /* PrivateKeyword */:
      case 124 /* ProtectedKeyword */:
      case 128 /* AbstractKeyword */:
      case 164 /* OverrideKeyword */:
      case 87 /* ConstKeyword */:
      case 138 /* DeclareKeyword */:
      case 148 /* ReadonlyKeyword */:
      case 103 /* InKeyword */:
      case 147 /* OutKeyword */:
      // TypeScript accessibility and readonly modifiers are elided
      // falls through
      case 189 /* ArrayType */:
      case 190 /* TupleType */:
      case 191 /* OptionalType */:
      case 192 /* RestType */:
      case 188 /* TypeLiteral */:
      case 183 /* TypePredicate */:
      case 169 /* TypeParameter */:
      case 133 /* AnyKeyword */:
      case 159 /* UnknownKeyword */:
      case 136 /* BooleanKeyword */:
      case 154 /* StringKeyword */:
      case 150 /* NumberKeyword */:
      case 146 /* NeverKeyword */:
      case 116 /* VoidKeyword */:
      case 155 /* SymbolKeyword */:
      case 186 /* ConstructorType */:
      case 185 /* FunctionType */:
      case 187 /* TypeQuery */:
      case 184 /* TypeReference */:
      case 193 /* UnionType */:
      case 194 /* IntersectionType */:
      case 195 /* ConditionalType */:
      case 197 /* ParenthesizedType */:
      case 198 /* ThisType */:
      case 199 /* TypeOperator */:
      case 200 /* IndexedAccessType */:
      case 201 /* MappedType */:
      case 202 /* LiteralType */:
      // TypeScript type nodes are elided.
      // falls through
      case 182 /* IndexSignature */:
        return void 0;
      case 266 /* TypeAliasDeclaration */:
        return factory2.createNotEmittedStatement(node);
      case 271 /* NamespaceExportDeclaration */:
        return void 0;
      case 265 /* InterfaceDeclaration */:
        return factory2.createNotEmittedStatement(node);
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 232 /* ClassExpression */:
        return visitClassExpression(node);
      case 299 /* HeritageClause */:
        return visitHeritageClause(node);
      case 234 /* ExpressionWithTypeArguments */:
        return visitExpressionWithTypeArguments(node);
      case 211 /* ObjectLiteralExpression */:
        return visitObjectLiteralExpression(node);
      case 177 /* Constructor */:
      case 173 /* PropertyDeclaration */:
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 176 /* ClassStaticBlockDeclaration */:
        return Debug.fail("Class and object literal elements must be visited with their respective visitors");
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 219 /* FunctionExpression */:
        return visitFunctionExpression(node);
      case 220 /* ArrowFunction */:
        return visitArrowFunction(node);
      case 170 /* Parameter */:
        return visitParameter(node);
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(node);
      case 217 /* TypeAssertionExpression */:
      case 235 /* AsExpression */:
        return visitAssertionExpression(node);
      case 239 /* SatisfiesExpression */:
        return visitSatisfiesExpression(node);
      case 214 /* CallExpression */:
        return visitCallExpression(node);
      case 215 /* NewExpression */:
        return visitNewExpression(node);
      case 216 /* TaggedTemplateExpression */:
        return visitTaggedTemplateExpression(node);
      case 236 /* NonNullExpression */:
        return visitNonNullExpression(node);
      case 267 /* EnumDeclaration */:
        return visitEnumDeclaration(node);
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 261 /* VariableDeclaration */:
        return visitVariableDeclaration(node);
      case 268 /* ModuleDeclaration */:
        return visitModuleDeclaration(node);
      case 272 /* ImportEqualsDeclaration */:
        return visitImportEqualsDeclaration(node);
      case 286 /* JsxSelfClosingElement */:
        return visitJsxSelfClosingElement(node);
      case 287 /* JsxOpeningElement */:
        return visitJsxJsxOpeningElement(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitSourceFile(node) {
    const alwaysStrict ***REMOVED*** getStrictOptionValue(compilerOptions, "alwaysStrict") && !(isExternalModule(node) && moduleKind >***REMOVED*** 5 /* ES2015 */) && !isJsonSourceFile(node);
    return factory2.updateSourceFile(
      node,
      visitLexicalEnvironment(
        node.statements,
        sourceElementVisitor,
        context,
        /*start*/
        0,
        alwaysStrict
      )
    );
  }
  function visitObjectLiteralExpression(node) {
    return factory2.updateObjectLiteralExpression(
      node,
      visitNodes2(node.properties, getObjectLiteralElementVisitor(node), isObjectLiteralElementLike)
    );
  }
  function getClassFacts(node) {
    let facts ***REMOVED*** 0 /* None */;
    if (some(getProperties(
      node,
      /*requireInitializer*/
      true,
      /*isStatic*/
      true
    ))) facts |***REMOVED*** 1 /* HasStaticInitializedProperties */;
    const extendsClauseElement ***REMOVED*** getEffectiveBaseTypeNode(node);
    if (extendsClauseElement && skipOuterExpressions(extendsClauseElement.expression).kind !***REMOVED******REMOVED*** 106 /* NullKeyword */) facts |***REMOVED*** 64 /* IsDerivedClass */;
    if (classOrConstructorParameterIsDecorated(legacyDecorators, node)) facts |***REMOVED*** 2 /* HasClassOrConstructorParameterDecorators */;
    if (childIsDecorated(legacyDecorators, node)) facts |***REMOVED*** 4 /* HasMemberDecorators */;
    if (isExportOfNamespace(node)) facts |***REMOVED*** 8 /* IsExportOfNamespace */;
    else if (isDefaultExternalModuleExport(node)) facts |***REMOVED*** 32 /* IsDefaultExternalExport */;
    else if (isNamedExternalModuleExport(node)) facts |***REMOVED*** 16 /* IsNamedExternalExport */;
    return facts;
  }
  function hasTypeScriptClassSyntax(node) {
    return !!(node.transformFlags & 8192 /* ContainsTypeScriptClassSyntax */);
  }
  function isClassLikeDeclarationWithTypeScriptSyntax(node) {
    return hasDecorators(node) || some(node.typeParameters) || some(node.heritageClauses, hasTypeScriptClassSyntax) || some(node.members, hasTypeScriptClassSyntax);
  }
  function visitClassDeclaration(node) {
    const facts ***REMOVED*** getClassFacts(node);
    const promoteToIIFE ***REMOVED*** languageVersion <***REMOVED*** 1 /* ES5 */ && !!(facts & 7 /* MayNeedImmediatelyInvokedFunctionExpression */);
    if (!isClassLikeDeclarationWithTypeScriptSyntax(node) && !classOrConstructorParameterIsDecorated(legacyDecorators, node) && !isExportOfNamespace(node)) {
      return factory2.updateClassDeclaration(
        node,
        visitNodes2(node.modifiers, modifierVisitor, isModifier),
        node.name,
        /*typeParameters*/
        void 0,
        visitNodes2(node.heritageClauses, visitor, isHeritageClause),
        visitNodes2(node.members, getClassElementVisitor(node), isClassElement)
      );
    }
    if (promoteToIIFE) {
      context.startLexicalEnvironment();
    }
    const moveModifiers ***REMOVED*** promoteToIIFE || facts & 8 /* IsExportOfNamespace */;
    let modifiers ***REMOVED*** moveModifiers ? visitNodes2(node.modifiers, modifierElidingVisitor, isModifierLike) : visitNodes2(node.modifiers, visitor, isModifierLike);
    if (facts & 2 /* HasClassOrConstructorParameterDecorators */) {
      modifiers ***REMOVED*** injectClassTypeMetadata(modifiers, node);
    }
    const needsName ***REMOVED*** moveModifiers && !node.name || facts & 4 /* HasMemberDecorators */ || facts & 1 /* HasStaticInitializedProperties */;
    const name ***REMOVED*** needsName ? node.name ?? factory2.getGeneratedNameForNode(node) : node.name;
    const classDeclaration ***REMOVED*** factory2.updateClassDeclaration(
      node,
      modifiers,
      name,
      /*typeParameters*/
      void 0,
      visitNodes2(node.heritageClauses, visitor, isHeritageClause),
      transformClassMembers(node)
    );
    let emitFlags ***REMOVED*** getEmitFlags(node);
    if (facts & 1 /* HasStaticInitializedProperties */) {
      emitFlags |***REMOVED*** 64 /* NoTrailingSourceMap */;
    }
    setEmitFlags(classDeclaration, emitFlags);
    let statement;
    if (promoteToIIFE) {
      const statements ***REMOVED*** [classDeclaration];
      const closingBraceLocation ***REMOVED*** createTokenRange(skipTrivia(currentSourceFile.text, node.members.end), 20 /* CloseBraceToken */);
      const localName ***REMOVED*** factory2.getInternalName(node);
      const outer ***REMOVED*** factory2.createPartiallyEmittedExpression(localName);
      setTextRangeEnd(outer, closingBraceLocation.end);
      setEmitFlags(outer, 3072 /* NoComments */);
      const returnStatement ***REMOVED*** factory2.createReturnStatement(outer);
      setTextRangePos(returnStatement, closingBraceLocation.pos);
      setEmitFlags(returnStatement, 3072 /* NoComments */ | 768 /* NoTokenSourceMaps */);
      statements.push(returnStatement);
      insertStatementsAfterStandardPrologue(statements, context.endLexicalEnvironment());
      const iife ***REMOVED*** factory2.createImmediatelyInvokedArrowFunction(statements);
      setInternalEmitFlags(iife, 1 /* TypeScriptClassWrapper */);
      const varDecl ***REMOVED*** factory2.createVariableDeclaration(
        factory2.getLocalName(
          node,
          /*allowComments*/
          false,
          /*allowSourceMaps*/
          false
        ),
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        iife
      );
      setOriginalNode(varDecl, node);
      const varStatement ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList([varDecl], 1 /* Let */)
      );
      setOriginalNode(varStatement, node);
      setCommentRange(varStatement, node);
      setSourceMapRange(varStatement, moveRangePastDecorators(node));
      startOnNewLine(varStatement);
      statement ***REMOVED*** varStatement;
    } else {
      statement ***REMOVED*** classDeclaration;
    }
    if (moveModifiers) {
      if (facts & 8 /* IsExportOfNamespace */) {
        return [
          statement,
          createExportMemberAssignmentStatement(node)
        ];
      }
      if (facts & 32 /* IsDefaultExternalExport */) {
        return [
          statement,
          factory2.createExportDefault(factory2.getLocalName(
            node,
            /*allowComments*/
            false,
            /*allowSourceMaps*/
            true
          ))
        ];
      }
      if (facts & 16 /* IsNamedExternalExport */) {
        return [
          statement,
          factory2.createExternalModuleExport(factory2.getDeclarationName(
            node,
            /*allowComments*/
            false,
            /*allowSourceMaps*/
            true
          ))
        ];
      }
    }
    return statement;
  }
  function visitClassExpression(node) {
    let modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierElidingVisitor, isModifierLike);
    if (classOrConstructorParameterIsDecorated(legacyDecorators, node)) {
      modifiers ***REMOVED*** injectClassTypeMetadata(modifiers, node);
    }
    return factory2.updateClassExpression(
      node,
      modifiers,
      node.name,
      /*typeParameters*/
      void 0,
      visitNodes2(node.heritageClauses, visitor, isHeritageClause),
      transformClassMembers(node)
    );
  }
  function transformClassMembers(node) {
    const members ***REMOVED*** visitNodes2(node.members, getClassElementVisitor(node), isClassElement);
    let newMembers;
    const constructor ***REMOVED*** getFirstConstructorWithBody(node);
    const parametersWithPropertyAssignments ***REMOVED*** constructor && filter(constructor.parameters, (p) ***REMOVED***> isParameterPropertyDeclaration(p, constructor));
    if (parametersWithPropertyAssignments) {
      for (const parameter of parametersWithPropertyAssignments) {
        const parameterProperty ***REMOVED*** factory2.createPropertyDeclaration(
          /*modifiers*/
          void 0,
          parameter.name,
          /*questionOrExclamationToken*/
          void 0,
          /*type*/
          void 0,
          /*initializer*/
          void 0
        );
        setOriginalNode(parameterProperty, parameter);
        newMembers ***REMOVED*** append(newMembers, parameterProperty);
      }
    }
    if (newMembers) {
      newMembers ***REMOVED*** addRange(newMembers, members);
      return setTextRange(
        factory2.createNodeArray(newMembers),
        /*location*/
        node.members
      );
    }
    return members;
  }
  function injectClassTypeMetadata(modifiers, node) {
    const metadata ***REMOVED*** getTypeMetadata(node, node);
    if (some(metadata)) {
      const modifiersArray ***REMOVED*** [];
      addRange(modifiersArray, takeWhile(modifiers, isExportOrDefaultModifier));
      addRange(modifiersArray, filter(modifiers, isDecorator));
      addRange(modifiersArray, metadata);
      addRange(modifiersArray, filter(skipWhile(modifiers, isExportOrDefaultModifier), isModifier));
      modifiers ***REMOVED*** setTextRange(factory2.createNodeArray(modifiersArray), modifiers);
    }
    return modifiers;
  }
  function injectClassElementTypeMetadata(modifiers, node, container) {
    if (isClassLike(container) && classElementOrClassElementParameterIsDecorated(legacyDecorators, node, container)) {
      const metadata ***REMOVED*** getTypeMetadata(node, container);
      if (some(metadata)) {
        const modifiersArray ***REMOVED*** [];
        addRange(modifiersArray, filter(modifiers, isDecorator));
        addRange(modifiersArray, metadata);
        addRange(modifiersArray, filter(modifiers, isModifier));
        modifiers ***REMOVED*** setTextRange(factory2.createNodeArray(modifiersArray), modifiers);
      }
    }
    return modifiers;
  }
  function getTypeMetadata(node, container) {
    if (!legacyDecorators) return void 0;
    return USE_NEW_TYPE_METADATA_FORMAT ? getNewTypeMetadata(node, container) : getOldTypeMetadata(node, container);
  }
  function getOldTypeMetadata(node, container) {
    if (typeSerializer) {
      let decorators;
      if (shouldAddTypeMetadata(node)) {
        const typeMetadata ***REMOVED*** emitHelpers().createMetadataHelper("design:type", typeSerializer.serializeTypeOfNode({ currentLexicalScope, currentNameScope: container }, node, container));
        decorators ***REMOVED*** append(decorators, factory2.createDecorator(typeMetadata));
      }
      if (shouldAddParamTypesMetadata(node)) {
        const paramTypesMetadata ***REMOVED*** emitHelpers().createMetadataHelper("design:paramtypes", typeSerializer.serializeParameterTypesOfNode({ currentLexicalScope, currentNameScope: container }, node, container));
        decorators ***REMOVED*** append(decorators, factory2.createDecorator(paramTypesMetadata));
      }
      if (shouldAddReturnTypeMetadata(node)) {
        const returnTypeMetadata ***REMOVED*** emitHelpers().createMetadataHelper("design:returntype", typeSerializer.serializeReturnTypeOfNode({ currentLexicalScope, currentNameScope: container }, node));
        decorators ***REMOVED*** append(decorators, factory2.createDecorator(returnTypeMetadata));
      }
      return decorators;
    }
  }
  function getNewTypeMetadata(node, container) {
    if (typeSerializer) {
      let properties;
      if (shouldAddTypeMetadata(node)) {
        const typeProperty ***REMOVED*** factory2.createPropertyAssignment("type", factory2.createArrowFunction(
          /*modifiers*/
          void 0,
          /*typeParameters*/
          void 0,
          [],
          /*type*/
          void 0,
          factory2.createToken(39 /* EqualsGreaterThanToken */),
          typeSerializer.serializeTypeOfNode({ currentLexicalScope, currentNameScope: container }, node, container)
        ));
        properties ***REMOVED*** append(properties, typeProperty);
      }
      if (shouldAddParamTypesMetadata(node)) {
        const paramTypeProperty ***REMOVED*** factory2.createPropertyAssignment("paramTypes", factory2.createArrowFunction(
          /*modifiers*/
          void 0,
          /*typeParameters*/
          void 0,
          [],
          /*type*/
          void 0,
          factory2.createToken(39 /* EqualsGreaterThanToken */),
          typeSerializer.serializeParameterTypesOfNode({ currentLexicalScope, currentNameScope: container }, node, container)
        ));
        properties ***REMOVED*** append(properties, paramTypeProperty);
      }
      if (shouldAddReturnTypeMetadata(node)) {
        const returnTypeProperty ***REMOVED*** factory2.createPropertyAssignment("returnType", factory2.createArrowFunction(
          /*modifiers*/
          void 0,
          /*typeParameters*/
          void 0,
          [],
          /*type*/
          void 0,
          factory2.createToken(39 /* EqualsGreaterThanToken */),
          typeSerializer.serializeReturnTypeOfNode({ currentLexicalScope, currentNameScope: container }, node)
        ));
        properties ***REMOVED*** append(properties, returnTypeProperty);
      }
      if (properties) {
        const typeInfoMetadata ***REMOVED*** emitHelpers().createMetadataHelper("design:typeinfo", factory2.createObjectLiteralExpression(
          properties,
          /*multiLine*/
          true
        ));
        return [factory2.createDecorator(typeInfoMetadata)];
      }
    }
  }
  function shouldAddTypeMetadata(node) {
    const kind ***REMOVED*** node.kind;
    return kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */;
  }
  function shouldAddReturnTypeMetadata(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */;
  }
  function shouldAddParamTypesMetadata(node) {
    switch (node.kind) {
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        return getFirstConstructorWithBody(node) !***REMOVED******REMOVED*** void 0;
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return true;
    }
    return false;
  }
  function getExpressionForPropertyName(member, generateNameForComputedPropertyName) {
    const name ***REMOVED*** member.name;
    if (isPrivateIdentifier(name)) {
      return factory2.createIdentifier("");
    } else if (isComputedPropertyName(name)) {
      return generateNameForComputedPropertyName && !isSimpleInlineableExpression(name.expression) ? factory2.getGeneratedNameForNode(name) : name.expression;
    } else if (isIdentifier(name)) {
      return factory2.createStringLiteral(idText(name));
    } else {
      return factory2.cloneNode(name);
    }
  }
  function visitPropertyNameOfClassElement(member) {
    const name ***REMOVED*** member.name;
    if (legacyDecorators && isComputedPropertyName(name) && hasDecorators(member)) {
      const expression ***REMOVED*** visitNode(name.expression, visitor, isExpression);
      Debug.assert(expression);
      const innerExpression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
      if (!isSimpleInlineableExpression(innerExpression)) {
        const generatedName ***REMOVED*** factory2.getGeneratedNameForNode(name);
        hoistVariableDeclaration(generatedName);
        return factory2.updateComputedPropertyName(name, factory2.createAssignment(generatedName, expression));
      }
    }
    return Debug.checkDefined(visitNode(name, visitor, isPropertyName));
  }
  function visitHeritageClause(node) {
    if (node.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */) {
      return void 0;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitExpressionWithTypeArguments(node) {
    return factory2.updateExpressionWithTypeArguments(
      node,
      Debug.checkDefined(visitNode(node.expression, visitor, isLeftHandSideExpression)),
      /*typeArguments*/
      void 0
    );
  }
  function shouldEmitFunctionLikeDeclaration(node) {
    return !nodeIsMissing(node.body);
  }
  function visitPropertyDeclaration(node, parent) {
    const isAmbient ***REMOVED*** node.flags & 33554432 /* Ambient */ || hasSyntacticModifier(node, 64 /* Abstract */);
    if (isAmbient && !(legacyDecorators && hasDecorators(node))) {
      return void 0;
    }
    let modifiers ***REMOVED*** isClassLike(parent) ? !isAmbient ? visitNodes2(node.modifiers, visitor, isModifierLike) : visitNodes2(node.modifiers, modifierElidingVisitor, isModifierLike) : visitNodes2(node.modifiers, decoratorElidingVisitor, isModifierLike);
    modifiers ***REMOVED*** injectClassElementTypeMetadata(modifiers, node, parent);
    if (isAmbient) {
      return factory2.updatePropertyDeclaration(
        node,
        concatenate(modifiers, factory2.createModifiersFromModifierFlags(128 /* Ambient */)),
        Debug.checkDefined(visitNode(node.name, visitor, isPropertyName)),
        /*questionOrExclamationToken*/
        void 0,
        /*type*/
        void 0,
        /*initializer*/
        void 0
      );
    }
    return factory2.updatePropertyDeclaration(
      node,
      modifiers,
      visitPropertyNameOfClassElement(node),
      /*questionOrExclamationToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
  }
  function visitConstructor(node) {
    if (!shouldEmitFunctionLikeDeclaration(node)) {
      return void 0;
    }
    return factory2.updateConstructorDeclaration(
      node,
      /*modifiers*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      transformConstructorBody(node.body, node)
    );
  }
  function transformConstructorBodyWorker(statementsOut, statementsIn, statementOffset, superPath, superPathDepth, initializerStatements) {
    const superStatementIndex ***REMOVED*** superPath[superPathDepth];
    const superStatement ***REMOVED*** statementsIn[superStatementIndex];
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, statementOffset, superStatementIndex - statementOffset));
    if (isTryStatement(superStatement)) {
      const tryBlockStatements ***REMOVED*** [];
      transformConstructorBodyWorker(
        tryBlockStatements,
        superStatement.tryBlock.statements,
        /*statementOffset*/
        0,
        superPath,
        superPathDepth + 1,
        initializerStatements
      );
      const tryBlockStatementsArray ***REMOVED*** factory2.createNodeArray(tryBlockStatements);
      setTextRange(tryBlockStatementsArray, superStatement.tryBlock.statements);
      statementsOut.push(factory2.updateTryStatement(
        superStatement,
        factory2.updateBlock(superStatement.tryBlock, tryBlockStatements),
        visitNode(superStatement.catchClause, visitor, isCatchClause),
        visitNode(superStatement.finallyBlock, visitor, isBlock)
      ));
    } else {
      addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, superStatementIndex, 1));
      addRange(statementsOut, initializerStatements);
    }
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, superStatementIndex + 1));
  }
  function transformConstructorBody(body, constructor) {
    const parametersWithPropertyAssignments ***REMOVED*** constructor && filter(constructor.parameters, (p) ***REMOVED***> isParameterPropertyDeclaration(p, constructor));
    if (!some(parametersWithPropertyAssignments)) {
      return visitFunctionBody(body, visitor, context);
    }
    let statements ***REMOVED*** [];
    resumeLexicalEnvironment();
    const prologueStatementCount ***REMOVED*** factory2.copyPrologue(
      body.statements,
      statements,
      /*ensureUseStrict*/
      false,
      visitor
    );
    const superPath ***REMOVED*** findSuperStatementIndexPath(body.statements, prologueStatementCount);
    const parameterPropertyAssignments ***REMOVED*** mapDefined(parametersWithPropertyAssignments, transformParameterWithPropertyAssignment);
    if (superPath.length) {
      transformConstructorBodyWorker(
        statements,
        body.statements,
        prologueStatementCount,
        superPath,
        /*superPathDepth*/
        0,
        parameterPropertyAssignments
      );
    } else {
      addRange(statements, parameterPropertyAssignments);
      addRange(statements, visitNodes2(body.statements, visitor, isStatement, prologueStatementCount));
    }
    statements ***REMOVED*** factory2.mergeLexicalEnvironment(statements, endLexicalEnvironment());
    const block ***REMOVED*** factory2.createBlock(
      setTextRange(factory2.createNodeArray(statements), body.statements),
      /*multiLine*/
      true
    );
    setTextRange(
      block,
      /*location*/
      body
    );
    setOriginalNode(block, body);
    return block;
  }
  function transformParameterWithPropertyAssignment(node) {
    const name ***REMOVED*** node.name;
    if (!isIdentifier(name)) {
      return void 0;
    }
    const propertyName ***REMOVED*** setParent(setTextRange(factory2.cloneNode(name), name), name.parent);
    setEmitFlags(propertyName, 3072 /* NoComments */ | 96 /* NoSourceMap */);
    const localName ***REMOVED*** setParent(setTextRange(factory2.cloneNode(name), name), name.parent);
    setEmitFlags(localName, 3072 /* NoComments */);
    return startOnNewLine(
      removeAllComments(
        setTextRange(
          setOriginalNode(
            factory2.createExpressionStatement(
              factory2.createAssignment(
                setTextRange(
                  factory2.createPropertyAccessExpression(
                    factory2.createThis(),
                    propertyName
                  ),
                  node.name
                ),
                localName
              )
            ),
            node
          ),
          moveRangePos(node, -1)
        )
      )
    );
  }
  function visitMethodDeclaration(node, parent) {
    if (!(node.transformFlags & 1 /* ContainsTypeScript */)) {
      return node;
    }
    if (!shouldEmitFunctionLikeDeclaration(node)) {
      return void 0;
    }
    let modifiers ***REMOVED*** isClassLike(parent) ? visitNodes2(node.modifiers, visitor, isModifierLike) : visitNodes2(node.modifiers, decoratorElidingVisitor, isModifierLike);
    modifiers ***REMOVED*** injectClassElementTypeMetadata(modifiers, node, parent);
    return factory2.updateMethodDeclaration(
      node,
      modifiers,
      node.asteriskToken,
      visitPropertyNameOfClassElement(node),
      /*questionToken*/
      void 0,
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      visitFunctionBody(node.body, visitor, context)
    );
  }
  function shouldEmitAccessorDeclaration(node) {
    return !(nodeIsMissing(node.body) && hasSyntacticModifier(node, 64 /* Abstract */));
  }
  function visitGetAccessor(node, parent) {
    if (!(node.transformFlags & 1 /* ContainsTypeScript */)) {
      return node;
    }
    if (!shouldEmitAccessorDeclaration(node)) {
      return void 0;
    }
    let modifiers ***REMOVED*** isClassLike(parent) ? visitNodes2(node.modifiers, visitor, isModifierLike) : visitNodes2(node.modifiers, decoratorElidingVisitor, isModifierLike);
    modifiers ***REMOVED*** injectClassElementTypeMetadata(modifiers, node, parent);
    return factory2.updateGetAccessorDeclaration(
      node,
      modifiers,
      visitPropertyNameOfClassElement(node),
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      visitFunctionBody(node.body, visitor, context) || factory2.createBlock([])
    );
  }
  function visitSetAccessor(node, parent) {
    if (!(node.transformFlags & 1 /* ContainsTypeScript */)) {
      return node;
    }
    if (!shouldEmitAccessorDeclaration(node)) {
      return void 0;
    }
    let modifiers ***REMOVED*** isClassLike(parent) ? visitNodes2(node.modifiers, visitor, isModifierLike) : visitNodes2(node.modifiers, decoratorElidingVisitor, isModifierLike);
    modifiers ***REMOVED*** injectClassElementTypeMetadata(modifiers, node, parent);
    return factory2.updateSetAccessorDeclaration(
      node,
      modifiers,
      visitPropertyNameOfClassElement(node),
      visitParameterList(node.parameters, visitor, context),
      visitFunctionBody(node.body, visitor, context) || factory2.createBlock([])
    );
  }
  function visitFunctionDeclaration(node) {
    if (!shouldEmitFunctionLikeDeclaration(node)) {
      return factory2.createNotEmittedStatement(node);
    }
    const updated ***REMOVED*** factory2.updateFunctionDeclaration(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      visitFunctionBody(node.body, visitor, context) || factory2.createBlock([])
    );
    if (isExportOfNamespace(node)) {
      const statements ***REMOVED*** [updated];
      addExportMemberAssignment(statements, node);
      return statements;
    }
    return updated;
  }
  function visitFunctionExpression(node) {
    if (!shouldEmitFunctionLikeDeclaration(node)) {
      return factory2.createOmittedExpression();
    }
    const updated ***REMOVED*** factory2.updateFunctionExpression(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      visitFunctionBody(node.body, visitor, context) || factory2.createBlock([])
    );
    return updated;
  }
  function visitArrowFunction(node) {
    const updated ***REMOVED*** factory2.updateArrowFunction(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      node.equalsGreaterThanToken,
      visitFunctionBody(node.body, visitor, context)
    );
    return updated;
  }
  function visitParameter(node) {
    if (parameterIsThisKeyword(node)) {
      return void 0;
    }
    const updated ***REMOVED*** factory2.updateParameterDeclaration(
      node,
      visitNodes2(node.modifiers, (node2) ***REMOVED***> isDecorator(node2) ? visitor(node2) : void 0, isModifierLike),
      node.dotDotDotToken,
      Debug.checkDefined(visitNode(node.name, visitor, isBindingName)),
      /*questionToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
    if (updated !***REMOVED******REMOVED*** node) {
      setCommentRange(updated, node);
      setTextRange(updated, moveRangePastModifiers(node));
      setSourceMapRange(updated, moveRangePastModifiers(node));
      setEmitFlags(updated.name, 64 /* NoTrailingSourceMap */);
    }
    return updated;
  }
  function visitVariableStatement(node) {
    if (isExportOfNamespace(node)) {
      const variables ***REMOVED*** getInitializedVariables(node.declarationList);
      if (variables.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return void 0;
      }
      return setTextRange(
        factory2.createExpressionStatement(
          factory2.inlineExpressions(
            map(variables, transformInitializedVariable)
          )
        ),
        node
      );
    } else {
      return visitEachChild(node, visitor, context);
    }
  }
  function transformInitializedVariable(node) {
    const name ***REMOVED*** node.name;
    if (isBindingPattern(name)) {
      return flattenDestructuringAssignment(
        node,
        visitor,
        context,
        0 /* All */,
        /*needsValue*/
        false,
        createNamespaceExportExpression
      );
    } else {
      return setTextRange(
        factory2.createAssignment(
          getNamespaceMemberNameWithSourceMapsAndWithoutComments(name),
          Debug.checkDefined(visitNode(node.initializer, visitor, isExpression))
        ),
        /*location*/
        node
      );
    }
  }
  function visitVariableDeclaration(node) {
    const updated ***REMOVED*** factory2.updateVariableDeclaration(
      node,
      Debug.checkDefined(visitNode(node.name, visitor, isBindingName)),
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
    if (node.type) {
      setTypeNode(updated.name, node.type);
    }
    return updated;
  }
  function visitParenthesizedExpression(node) {
    const innerExpression ***REMOVED*** skipOuterExpressions(node.expression, ~(38 /* Assertions */ | 16 /* ExpressionsWithTypeArguments */));
    if (isAssertionExpression(innerExpression) || isSatisfiesExpression(innerExpression)) {
      const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
      Debug.assert(expression);
      return factory2.createPartiallyEmittedExpression(expression, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssertionExpression(node) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assert(expression);
    return factory2.createPartiallyEmittedExpression(expression, node);
  }
  function visitNonNullExpression(node) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isLeftHandSideExpression);
    Debug.assert(expression);
    return factory2.createPartiallyEmittedExpression(expression, node);
  }
  function visitSatisfiesExpression(node) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assert(expression);
    return factory2.createPartiallyEmittedExpression(expression, node);
  }
  function visitCallExpression(node) {
    return factory2.updateCallExpression(
      node,
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      /*typeArguments*/
      void 0,
      visitNodes2(node.arguments, visitor, isExpression)
    );
  }
  function visitNewExpression(node) {
    return factory2.updateNewExpression(
      node,
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      /*typeArguments*/
      void 0,
      visitNodes2(node.arguments, visitor, isExpression)
    );
  }
  function visitTaggedTemplateExpression(node) {
    return factory2.updateTaggedTemplateExpression(
      node,
      Debug.checkDefined(visitNode(node.tag, visitor, isExpression)),
      /*typeArguments*/
      void 0,
      Debug.checkDefined(visitNode(node.template, visitor, isTemplateLiteral))
    );
  }
  function visitJsxSelfClosingElement(node) {
    return factory2.updateJsxSelfClosingElement(
      node,
      Debug.checkDefined(visitNode(node.tagName, visitor, isJsxTagNameExpression)),
      /*typeArguments*/
      void 0,
      Debug.checkDefined(visitNode(node.attributes, visitor, isJsxAttributes))
    );
  }
  function visitJsxJsxOpeningElement(node) {
    return factory2.updateJsxOpeningElement(
      node,
      Debug.checkDefined(visitNode(node.tagName, visitor, isJsxTagNameExpression)),
      /*typeArguments*/
      void 0,
      Debug.checkDefined(visitNode(node.attributes, visitor, isJsxAttributes))
    );
  }
  function shouldEmitEnumDeclaration(node) {
    return !isEnumConst(node) || shouldPreserveConstEnums(compilerOptions);
  }
  function visitEnumDeclaration(node) {
    if (!shouldEmitEnumDeclaration(node)) {
      return factory2.createNotEmittedStatement(node);
    }
    const statements ***REMOVED*** [];
    let emitFlags ***REMOVED*** 4 /* AdviseOnEmitNode */;
    const varAdded ***REMOVED*** addVarForEnumOrModuleDeclaration(statements, node);
    if (varAdded) {
      if (moduleKind !***REMOVED******REMOVED*** 4 /* System */ || currentLexicalScope !***REMOVED******REMOVED*** currentSourceFile) {
        emitFlags |***REMOVED*** 1024 /* NoLeadingComments */;
      }
    }
    const parameterName ***REMOVED*** getNamespaceParameterName(node);
    const containerName ***REMOVED*** getNamespaceContainerName(node);
    const exportName ***REMOVED*** isExportOfNamespace(node) ? factory2.getExternalModuleOrNamespaceExportName(
      currentNamespaceContainerName,
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    ) : factory2.getDeclarationName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    );
    let moduleArg ***REMOVED*** factory2.createLogicalOr(
      exportName,
      factory2.createAssignment(
        exportName,
        factory2.createObjectLiteralExpression()
      )
    );
    if (isExportOfNamespace(node)) {
      const localName ***REMOVED*** factory2.getLocalName(
        node,
        /*allowComments*/
        false,
        /*allowSourceMaps*/
        true
      );
      moduleArg ***REMOVED*** factory2.createAssignment(localName, moduleArg);
    }
    const enumStatement ***REMOVED*** factory2.createExpressionStatement(
      factory2.createCallExpression(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          /*asteriskToken*/
          void 0,
          /*name*/
          void 0,
          /*typeParameters*/
          void 0,
          [factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            parameterName
          )],
          /*type*/
          void 0,
          transformEnumBody(node, containerName)
        ),
        /*typeArguments*/
        void 0,
        [moduleArg]
      )
    );
    setOriginalNode(enumStatement, node);
    if (varAdded) {
      setSyntheticLeadingComments(enumStatement, void 0);
      setSyntheticTrailingComments(enumStatement, void 0);
    }
    setTextRange(enumStatement, node);
    addEmitFlags(enumStatement, emitFlags);
    statements.push(enumStatement);
    return statements;
  }
  function transformEnumBody(node, localName) {
    const savedCurrentNamespaceLocalName ***REMOVED*** currentNamespaceContainerName;
    currentNamespaceContainerName ***REMOVED*** localName;
    const statements ***REMOVED*** [];
    startLexicalEnvironment();
    const members ***REMOVED*** map(node.members, transformEnumMember);
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    addRange(statements, members);
    currentNamespaceContainerName ***REMOVED*** savedCurrentNamespaceLocalName;
    return factory2.createBlock(
      setTextRange(
        factory2.createNodeArray(statements),
        /*location*/
        node.members
      ),
      /*multiLine*/
      true
    );
  }
  function transformEnumMember(member) {
    const name ***REMOVED*** getExpressionForPropertyName(
      member,
      /*generateNameForComputedPropertyName*/
      false
    );
    const evaluated ***REMOVED*** resolver.getEnumMemberValue(member);
    const valueExpression ***REMOVED*** transformEnumMemberDeclarationValue(member, evaluated ***REMOVED******REMOVED*** null ? void 0 : evaluated.value);
    const innerAssignment ***REMOVED*** factory2.createAssignment(
      factory2.createElementAccessExpression(
        currentNamespaceContainerName,
        name
      ),
      valueExpression
    );
    const outerAssignment ***REMOVED*** typeof (evaluated ***REMOVED******REMOVED*** null ? void 0 : evaluated.value) ***REMOVED******REMOVED******REMOVED*** "string" || (evaluated ***REMOVED******REMOVED*** null ? void 0 : evaluated.isSyntacticallyString) ? innerAssignment : factory2.createAssignment(
      factory2.createElementAccessExpression(
        currentNamespaceContainerName,
        innerAssignment
      ),
      name
    );
    return setTextRange(
      factory2.createExpressionStatement(
        setTextRange(
          outerAssignment,
          member
        )
      ),
      member
    );
  }
  function transformEnumMemberDeclarationValue(member, constantValue) {
    if (constantValue !***REMOVED******REMOVED*** void 0) {
      return typeof constantValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory2.createStringLiteral(constantValue) : constantValue < 0 ? factory2.createPrefixUnaryExpression(41 /* MinusToken */, factory2.createNumericLiteral(-constantValue)) : factory2.createNumericLiteral(constantValue);
    } else {
      enableSubstitutionForNonQualifiedEnumMembers();
      if (member.initializer) {
        return Debug.checkDefined(visitNode(member.initializer, visitor, isExpression));
      } else {
        return factory2.createVoidZero();
      }
    }
  }
  function shouldEmitModuleDeclaration(nodeIn) {
    const node ***REMOVED*** getParseTreeNode(nodeIn, isModuleDeclaration);
    if (!node) {
      return true;
    }
    return isInstantiatedModule(node, shouldPreserveConstEnums(compilerOptions));
  }
  function recordEmittedDeclarationInScope(node) {
    if (!currentScopeFirstDeclarationsOfName) {
      currentScopeFirstDeclarationsOfName ***REMOVED*** /* @__PURE__ */ new Map();
    }
    const name ***REMOVED*** declaredNameInScope(node);
    if (!currentScopeFirstDeclarationsOfName.has(name)) {
      currentScopeFirstDeclarationsOfName.set(name, node);
    }
  }
  function isFirstEmittedDeclarationInScope(node) {
    if (currentScopeFirstDeclarationsOfName) {
      const name ***REMOVED*** declaredNameInScope(node);
      return currentScopeFirstDeclarationsOfName.get(name) ***REMOVED******REMOVED******REMOVED*** node;
    }
    return true;
  }
  function declaredNameInScope(node) {
    Debug.assertNode(node.name, isIdentifier);
    return node.name.escapedText;
  }
  function addVarForEnumOrModuleDeclaration(statements, node) {
    const varDecl ***REMOVED*** factory2.createVariableDeclaration(factory2.getLocalName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    ));
    const varFlags ***REMOVED*** currentLexicalScope.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? 0 /* None */ : 1 /* Let */;
    const statement ***REMOVED*** factory2.createVariableStatement(
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      factory2.createVariableDeclarationList([varDecl], varFlags)
    );
    setOriginalNode(varDecl, node);
    setSyntheticLeadingComments(varDecl, void 0);
    setSyntheticTrailingComments(varDecl, void 0);
    setOriginalNode(statement, node);
    recordEmittedDeclarationInScope(node);
    if (isFirstEmittedDeclarationInScope(node)) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */) {
        setSourceMapRange(statement.declarationList, node);
      } else {
        setSourceMapRange(statement, node);
      }
      setCommentRange(statement, node);
      addEmitFlags(statement, 2048 /* NoTrailingComments */);
      statements.push(statement);
      return true;
    }
    return false;
  }
  function visitModuleDeclaration(node) {
    if (!shouldEmitModuleDeclaration(node)) {
      return factory2.createNotEmittedStatement(node);
    }
    Debug.assertNode(node.name, isIdentifier, "A TypeScript namespace should have an Identifier name.");
    enableSubstitutionForNamespaceExports();
    const statements ***REMOVED*** [];
    let emitFlags ***REMOVED*** 4 /* AdviseOnEmitNode */;
    const varAdded ***REMOVED*** addVarForEnumOrModuleDeclaration(statements, node);
    if (varAdded) {
      if (moduleKind !***REMOVED******REMOVED*** 4 /* System */ || currentLexicalScope !***REMOVED******REMOVED*** currentSourceFile) {
        emitFlags |***REMOVED*** 1024 /* NoLeadingComments */;
      }
    }
    const parameterName ***REMOVED*** getNamespaceParameterName(node);
    const containerName ***REMOVED*** getNamespaceContainerName(node);
    const exportName ***REMOVED*** isExportOfNamespace(node) ? factory2.getExternalModuleOrNamespaceExportName(
      currentNamespaceContainerName,
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    ) : factory2.getDeclarationName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    );
    let moduleArg ***REMOVED*** factory2.createLogicalOr(
      exportName,
      factory2.createAssignment(
        exportName,
        factory2.createObjectLiteralExpression()
      )
    );
    if (isExportOfNamespace(node)) {
      const localName ***REMOVED*** factory2.getLocalName(
        node,
        /*allowComments*/
        false,
        /*allowSourceMaps*/
        true
      );
      moduleArg ***REMOVED*** factory2.createAssignment(localName, moduleArg);
    }
    const moduleStatement ***REMOVED*** factory2.createExpressionStatement(
      factory2.createCallExpression(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          /*asteriskToken*/
          void 0,
          /*name*/
          void 0,
          /*typeParameters*/
          void 0,
          [factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            parameterName
          )],
          /*type*/
          void 0,
          transformModuleBody(node, containerName)
        ),
        /*typeArguments*/
        void 0,
        [moduleArg]
      )
    );
    setOriginalNode(moduleStatement, node);
    if (varAdded) {
      setSyntheticLeadingComments(moduleStatement, void 0);
      setSyntheticTrailingComments(moduleStatement, void 0);
    }
    setTextRange(moduleStatement, node);
    addEmitFlags(moduleStatement, emitFlags);
    statements.push(moduleStatement);
    return statements;
  }
  function transformModuleBody(node, namespaceLocalName) {
    const savedCurrentNamespaceContainerName ***REMOVED*** currentNamespaceContainerName;
    const savedCurrentNamespace ***REMOVED*** currentNamespace;
    const savedCurrentScopeFirstDeclarationsOfName ***REMOVED*** currentScopeFirstDeclarationsOfName;
    currentNamespaceContainerName ***REMOVED*** namespaceLocalName;
    currentNamespace ***REMOVED*** node;
    currentScopeFirstDeclarationsOfName ***REMOVED*** void 0;
    const statements ***REMOVED*** [];
    startLexicalEnvironment();
    let statementsLocation;
    let blockLocation;
    if (node.body) {
      if (node.body.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */) {
        saveStateAndInvoke(node.body, (body) ***REMOVED***> addRange(statements, visitNodes2(body.statements, namespaceElementVisitor, isStatement)));
        statementsLocation ***REMOVED*** node.body.statements;
        blockLocation ***REMOVED*** node.body;
      } else {
        const result ***REMOVED*** visitModuleDeclaration(node.body);
        if (result) {
          if (isArray(result)) {
            addRange(statements, result);
          } else {
            statements.push(result);
          }
        }
        const moduleBlock ***REMOVED*** getInnerMostModuleDeclarationFromDottedModule(node).body;
        statementsLocation ***REMOVED*** moveRangePos(moduleBlock.statements, -1);
      }
    }
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    currentNamespaceContainerName ***REMOVED*** savedCurrentNamespaceContainerName;
    currentNamespace ***REMOVED*** savedCurrentNamespace;
    currentScopeFirstDeclarationsOfName ***REMOVED*** savedCurrentScopeFirstDeclarationsOfName;
    const block ***REMOVED*** factory2.createBlock(
      setTextRange(
        factory2.createNodeArray(statements),
        /*location*/
        statementsLocation
      ),
      /*multiLine*/
      true
    );
    setTextRange(block, blockLocation);
    if (!node.body || node.body.kind !***REMOVED******REMOVED*** 269 /* ModuleBlock */) {
      setEmitFlags(block, getEmitFlags(block) | 3072 /* NoComments */);
    }
    return block;
  }
  function getInnerMostModuleDeclarationFromDottedModule(moduleDeclaration) {
    if (moduleDeclaration.body.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */) {
      const recursiveInnerModule ***REMOVED*** getInnerMostModuleDeclarationFromDottedModule(moduleDeclaration.body);
      return recursiveInnerModule || moduleDeclaration.body;
    }
  }
  function visitImportDeclaration(node) {
    if (!node.importClause) {
      return node;
    }
    if (node.importClause.isTypeOnly) {
      return void 0;
    }
    const importClause ***REMOVED*** visitNode(node.importClause, visitImportClause, isImportClause);
    return importClause ? factory2.updateImportDeclaration(
      node,
      /*modifiers*/
      void 0,
      importClause,
      node.moduleSpecifier,
      node.attributes
    ) : void 0;
  }
  function visitImportClause(node) {
    Debug.assert(node.phaseModifier !***REMOVED******REMOVED*** 156 /* TypeKeyword */);
    const name ***REMOVED*** shouldEmitAliasDeclaration(node) ? node.name : void 0;
    const namedBindings ***REMOVED*** visitNode(node.namedBindings, visitNamedImportBindings, isNamedImportBindings);
    return name || namedBindings ? factory2.updateImportClause(node, node.phaseModifier, name, namedBindings) : void 0;
  }
  function visitNamedImportBindings(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */) {
      return shouldEmitAliasDeclaration(node) ? node : void 0;
    } else {
      const allowEmpty ***REMOVED*** compilerOptions.verbatimModuleSyntax;
      const elements ***REMOVED*** visitNodes2(node.elements, visitImportSpecifier, isImportSpecifier);
      return allowEmpty || some(elements) ? factory2.updateNamedImports(node, elements) : void 0;
    }
  }
  function visitImportSpecifier(node) {
    return !node.isTypeOnly && shouldEmitAliasDeclaration(node) ? node : void 0;
  }
  function visitExportAssignment(node) {
    return compilerOptions.verbatimModuleSyntax || resolver.isValueAliasDeclaration(node) ? visitEachChild(node, visitor, context) : void 0;
  }
  function visitExportDeclaration(node) {
    if (node.isTypeOnly) {
      return void 0;
    }
    if (!node.exportClause || isNamespaceExport(node.exportClause)) {
      return factory2.updateExportDeclaration(
        node,
        node.modifiers,
        node.isTypeOnly,
        node.exportClause,
        node.moduleSpecifier,
        node.attributes
      );
    }
    const allowEmpty ***REMOVED*** !!compilerOptions.verbatimModuleSyntax;
    const exportClause ***REMOVED*** visitNode(
      node.exportClause,
      (bindings) ***REMOVED***> visitNamedExportBindings(bindings, allowEmpty),
      isNamedExportBindings
    );
    return exportClause ? factory2.updateExportDeclaration(
      node,
      /*modifiers*/
      void 0,
      node.isTypeOnly,
      exportClause,
      node.moduleSpecifier,
      node.attributes
    ) : void 0;
  }
  function visitNamedExports(node, allowEmpty) {
    const elements ***REMOVED*** visitNodes2(node.elements, visitExportSpecifier, isExportSpecifier);
    return allowEmpty || some(elements) ? factory2.updateNamedExports(node, elements) : void 0;
  }
  function visitNamespaceExports(node) {
    return factory2.updateNamespaceExport(node, Debug.checkDefined(visitNode(node.name, visitor, isIdentifier)));
  }
  function visitNamedExportBindings(node, allowEmpty) {
    return isNamespaceExport(node) ? visitNamespaceExports(node) : visitNamedExports(node, allowEmpty);
  }
  function visitExportSpecifier(node) {
    return !node.isTypeOnly && (compilerOptions.verbatimModuleSyntax || resolver.isValueAliasDeclaration(node)) ? node : void 0;
  }
  function shouldEmitImportEqualsDeclaration(node) {
    return shouldEmitAliasDeclaration(node) || !isExternalModule(currentSourceFile) && resolver.isTopLevelValueImportEqualsWithEntityName(node);
  }
  function visitImportEqualsDeclaration(node) {
    if (node.isTypeOnly) {
      return void 0;
    }
    if (isExternalModuleImportEqualsDeclaration(node)) {
      if (!shouldEmitAliasDeclaration(node)) {
        return void 0;
      }
      return visitEachChild(node, visitor, context);
    }
    if (!shouldEmitImportEqualsDeclaration(node)) {
      return void 0;
    }
    const moduleReference ***REMOVED*** createExpressionFromEntityName(factory2, node.moduleReference);
    setEmitFlags(moduleReference, 3072 /* NoComments */ | 4096 /* NoNestedComments */);
    if (isNamedExternalModuleExport(node) || !isExportOfNamespace(node)) {
      return setOriginalNode(
        setTextRange(
          factory2.createVariableStatement(
            visitNodes2(node.modifiers, modifierVisitor, isModifier),
            factory2.createVariableDeclarationList([
              setOriginalNode(
                factory2.createVariableDeclaration(
                  node.name,
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  moduleReference
                ),
                node
              )
            ])
          ),
          node
        ),
        node
      );
    } else {
      return setOriginalNode(
        createNamespaceExport(
          node.name,
          moduleReference,
          node
        ),
        node
      );
    }
  }
  function isExportOfNamespace(node) {
    return currentNamespace !***REMOVED******REMOVED*** void 0 && hasSyntacticModifier(node, 32 /* Export */);
  }
  function isExternalModuleExport(node) {
    return currentNamespace ***REMOVED******REMOVED******REMOVED*** void 0 && hasSyntacticModifier(node, 32 /* Export */);
  }
  function isNamedExternalModuleExport(node) {
    return isExternalModuleExport(node) && !hasSyntacticModifier(node, 2048 /* Default */);
  }
  function isDefaultExternalModuleExport(node) {
    return isExternalModuleExport(node) && hasSyntacticModifier(node, 2048 /* Default */);
  }
  function createExportMemberAssignmentStatement(node) {
    const expression ***REMOVED*** factory2.createAssignment(
      factory2.getExternalModuleOrNamespaceExportName(
        currentNamespaceContainerName,
        node,
        /*allowComments*/
        false,
        /*allowSourceMaps*/
        true
      ),
      factory2.getLocalName(node)
    );
    setSourceMapRange(expression, createRange(node.name ? node.name.pos : node.pos, node.end));
    const statement ***REMOVED*** factory2.createExpressionStatement(expression);
    setSourceMapRange(statement, createRange(-1, node.end));
    return statement;
  }
  function addExportMemberAssignment(statements, node) {
    statements.push(createExportMemberAssignmentStatement(node));
  }
  function createNamespaceExport(exportName, exportValue, location) {
    return setTextRange(
      factory2.createExpressionStatement(
        factory2.createAssignment(
          factory2.getNamespaceMemberName(
            currentNamespaceContainerName,
            exportName,
            /*allowComments*/
            false,
            /*allowSourceMaps*/
            true
          ),
          exportValue
        )
      ),
      location
    );
  }
  function createNamespaceExportExpression(exportName, exportValue, location) {
    return setTextRange(factory2.createAssignment(getNamespaceMemberNameWithSourceMapsAndWithoutComments(exportName), exportValue), location);
  }
  function getNamespaceMemberNameWithSourceMapsAndWithoutComments(name) {
    return factory2.getNamespaceMemberName(
      currentNamespaceContainerName,
      name,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    );
  }
  function getNamespaceParameterName(node) {
    const name ***REMOVED*** factory2.getGeneratedNameForNode(node);
    setSourceMapRange(name, node.name);
    return name;
  }
  function getNamespaceContainerName(node) {
    return factory2.getGeneratedNameForNode(node);
  }
  function enableSubstitutionForNonQualifiedEnumMembers() {
    if ((enabledSubstitutions & 8 /* NonQualifiedEnumMembers */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 8 /* NonQualifiedEnumMembers */;
      context.enableSubstitution(80 /* Identifier */);
    }
  }
  function enableSubstitutionForNamespaceExports() {
    if ((enabledSubstitutions & 2 /* NamespaceExports */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 2 /* NamespaceExports */;
      context.enableSubstitution(80 /* Identifier */);
      context.enableSubstitution(305 /* ShorthandPropertyAssignment */);
      context.enableEmitNotification(268 /* ModuleDeclaration */);
    }
  }
  function isTransformedModuleDeclaration(node) {
    return getOriginalNode(node).kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */;
  }
  function isTransformedEnumDeclaration(node) {
    return getOriginalNode(node).kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */;
  }
  function onEmitNode(hint, node, emitCallback) {
    const savedApplicableSubstitutions ***REMOVED*** applicableSubstitutions;
    const savedCurrentSourceFile ***REMOVED*** currentSourceFile;
    if (isSourceFile(node)) {
      currentSourceFile ***REMOVED*** node;
    }
    if (enabledSubstitutions & 2 /* NamespaceExports */ && isTransformedModuleDeclaration(node)) {
      applicableSubstitutions |***REMOVED*** 2 /* NamespaceExports */;
    }
    if (enabledSubstitutions & 8 /* NonQualifiedEnumMembers */ && isTransformedEnumDeclaration(node)) {
      applicableSubstitutions |***REMOVED*** 8 /* NonQualifiedEnumMembers */;
    }
    previousOnEmitNode(hint, node, emitCallback);
    applicableSubstitutions ***REMOVED*** savedApplicableSubstitutions;
    currentSourceFile ***REMOVED*** savedCurrentSourceFile;
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    } else if (isShorthandPropertyAssignment(node)) {
      return substituteShorthandPropertyAssignment(node);
    }
    return node;
  }
  function substituteShorthandPropertyAssignment(node) {
    if (enabledSubstitutions & 2 /* NamespaceExports */) {
      const name ***REMOVED*** node.name;
      const exportedName ***REMOVED*** trySubstituteNamespaceExportedName(name);
      if (exportedName) {
        if (node.objectAssignmentInitializer) {
          const initializer ***REMOVED*** factory2.createAssignment(exportedName, node.objectAssignmentInitializer);
          return setTextRange(factory2.createPropertyAssignment(name, initializer), node);
        }
        return setTextRange(factory2.createPropertyAssignment(name, exportedName), node);
      }
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
      case 212 /* PropertyAccessExpression */:
        return substitutePropertyAccessExpression(node);
      case 213 /* ElementAccessExpression */:
        return substituteElementAccessExpression(node);
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    return trySubstituteNamespaceExportedName(node) || node;
  }
  function trySubstituteNamespaceExportedName(node) {
    if (enabledSubstitutions & applicableSubstitutions && !isGeneratedIdentifier(node) && !isLocalName(node)) {
      const container ***REMOVED*** resolver.getReferencedExportContainer(
        node,
        /*prefixLocals*/
        false
      );
      if (container && container.kind !***REMOVED******REMOVED*** 308 /* SourceFile */) {
        const substitute ***REMOVED*** applicableSubstitutions & 2 /* NamespaceExports */ && container.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */ || applicableSubstitutions & 8 /* NonQualifiedEnumMembers */ && container.kind ***REMOVED******REMOVED******REMOVED*** 267 /* EnumDeclaration */;
        if (substitute) {
          return setTextRange(
            factory2.createPropertyAccessExpression(factory2.getGeneratedNameForNode(container), node),
            /*location*/
            node
          );
        }
      }
    }
    return void 0;
  }
  function substitutePropertyAccessExpression(node) {
    return substituteConstantValue(node);
  }
  function substituteElementAccessExpression(node) {
    return substituteConstantValue(node);
  }
  function safeMultiLineComment(value) {
    return value.replace(/\*\//g, "*_/");
  }
  function substituteConstantValue(node) {
    const constantValue ***REMOVED*** tryGetConstEnumValue(node);
    if (constantValue !***REMOVED******REMOVED*** void 0) {
      setConstantValue(node, constantValue);
      const substitute ***REMOVED*** typeof constantValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory2.createStringLiteral(constantValue) : constantValue < 0 ? factory2.createPrefixUnaryExpression(41 /* MinusToken */, factory2.createNumericLiteral(-constantValue)) : factory2.createNumericLiteral(constantValue);
      if (!compilerOptions.removeComments) {
        const originalNode ***REMOVED*** getOriginalNode(node, isAccessExpression);
        addSyntheticTrailingComment(substitute, 3 /* MultiLineCommentTrivia */, ` ${safeMultiLineComment(getTextOfNode(originalNode))} `);
      }
      return substitute;
    }
    return node;
  }
  function tryGetConstEnumValue(node) {
    if (getIsolatedModules(compilerOptions)) {
      return void 0;
    }
    return isPropertyAccessExpression(node) || isElementAccessExpression(node) ? resolver.getConstantValue(node) : void 0;
  }
  function shouldEmitAliasDeclaration(node) {
    return compilerOptions.verbatimModuleSyntax || isInJSFile(node) || resolver.isReferencedAliasDeclaration(node);
  }
}

// src/compiler/transformers/classFields.ts
function transformClassFields(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    hoistVariableDeclaration,
    endLexicalEnvironment,
    startLexicalEnvironment,
    resumeLexicalEnvironment,
    addBlockScopedVariable
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const useDefineForClassFields ***REMOVED*** getUseDefineForClassFields(compilerOptions);
  const legacyDecorators ***REMOVED*** !!compilerOptions.experimentalDecorators;
  const shouldTransformInitializersUsingSet ***REMOVED*** !useDefineForClassFields;
  const shouldTransformInitializersUsingDefine ***REMOVED*** useDefineForClassFields && languageVersion < 9 /* ES2022 */;
  const shouldTransformInitializers ***REMOVED*** shouldTransformInitializersUsingSet || shouldTransformInitializersUsingDefine;
  const shouldTransformPrivateElementsOrClassStaticBlocks ***REMOVED*** languageVersion < 9 /* ES2022 */;
  const shouldTransformAutoAccessors ***REMOVED*** languageVersion < 99 /* ESNext */ ? -1 /* True */ : !useDefineForClassFields ? 3 /* Maybe */ : 0 /* False */;
  const shouldTransformThisInStaticInitializers ***REMOVED*** languageVersion < 9 /* ES2022 */;
  const shouldTransformSuperInStaticInitializers ***REMOVED*** shouldTransformThisInStaticInitializers && languageVersion >***REMOVED*** 2 /* ES2015 */;
  const shouldTransformAnything ***REMOVED*** shouldTransformInitializers || shouldTransformPrivateElementsOrClassStaticBlocks || shouldTransformAutoAccessors ***REMOVED******REMOVED******REMOVED*** -1 /* True */;
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  let shouldTransformPrivateStaticElementsInFile ***REMOVED*** false;
  let enabledSubstitutions ***REMOVED*** 0 /* None */;
  let classAliases;
  let pendingExpressions;
  let pendingStatements;
  let lexicalEnvironment;
  const lexicalEnvironmentMap ***REMOVED*** /* @__PURE__ */ new Map();
  const noSubstitution ***REMOVED*** /* @__PURE__ */ new Set();
  let currentClassContainer;
  let currentClassElement;
  let shouldSubstituteThisWithClassThis ***REMOVED*** false;
  let previousShouldSubstituteThisWithClassThis ***REMOVED*** false;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    lexicalEnvironment ***REMOVED*** void 0;
    shouldTransformPrivateStaticElementsInFile ***REMOVED*** !!(getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */);
    if (!shouldTransformAnything && !shouldTransformPrivateStaticElementsInFile) {
      return node;
    }
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    return visited;
  }
  function modifierVisitor(node) {
    switch (node.kind) {
      case 129 /* AccessorKeyword */:
        return shouldTransformAutoAccessorsInCurrentClass() ? void 0 : node;
      default:
        return tryCast(node, isModifier);
    }
  }
  function visitor(node) {
    if (!(node.transformFlags & 16777216 /* ContainsClassFields */) && !(node.transformFlags & 134234112 /* ContainsLexicalThisOrSuper */)) {
      return node;
    }
    switch (node.kind) {
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 232 /* ClassExpression */:
        return visitClassExpression(node);
      case 176 /* ClassStaticBlockDeclaration */:
      case 173 /* PropertyDeclaration */:
        return Debug.fail("Use `classElementVisitor` instead.");
      case 304 /* PropertyAssignment */:
        return visitPropertyAssignment(node);
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 261 /* VariableDeclaration */:
        return visitVariableDeclaration(node);
      case 170 /* Parameter */:
        return visitParameterDeclaration(node);
      case 209 /* BindingElement */:
        return visitBindingElement(node);
      case 278 /* ExportAssignment */:
        return visitExportAssignment(node);
      case 81 /* PrivateIdentifier */:
        return visitPrivateIdentifier(node);
      case 212 /* PropertyAccessExpression */:
        return visitPropertyAccessExpression(node);
      case 213 /* ElementAccessExpression */:
        return visitElementAccessExpression(node);
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPreOrPostfixUnaryExpression(
          node,
          /*discarded*/
          false
        );
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(
          node,
          /*discarded*/
          false
        );
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(
          node,
          /*discarded*/
          false
        );
      case 214 /* CallExpression */:
        return visitCallExpression(node);
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 216 /* TaggedTemplateExpression */:
        return visitTaggedTemplateExpression(node);
      case 249 /* ForStatement */:
        return visitForStatement(node);
      case 110 /* ThisKeyword */:
        return visitThisExpression(node);
      case 263 /* FunctionDeclaration */:
      case 219 /* FunctionExpression */:
        return setCurrentClassElementAnd(
          /*classElement*/
          void 0,
          fallbackVisitor,
          node
        );
      case 177 /* Constructor */:
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */: {
        return setCurrentClassElementAnd(
          node,
          fallbackVisitor,
          node
        );
      }
      default:
        return fallbackVisitor(node);
    }
  }
  function fallbackVisitor(node) {
    return visitEachChild(node, visitor, context);
  }
  function discardedValueVisitor(node) {
    switch (node.kind) {
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPreOrPostfixUnaryExpression(
          node,
          /*discarded*/
          true
        );
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(
          node,
          /*discarded*/
          true
        );
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(
          node,
          /*discarded*/
          true
        );
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(
          node,
          /*discarded*/
          true
        );
      default:
        return visitor(node);
    }
  }
  function heritageClauseVisitor(node) {
    switch (node.kind) {
      case 299 /* HeritageClause */:
        return visitEachChild(node, heritageClauseVisitor, context);
      case 234 /* ExpressionWithTypeArguments */:
        return visitExpressionWithTypeArgumentsInHeritageClause(node);
      default:
        return visitor(node);
    }
  }
  function assignmentTargetVisitor(node) {
    switch (node.kind) {
      case 211 /* ObjectLiteralExpression */:
      case 210 /* ArrayLiteralExpression */:
        return visitAssignmentPattern(node);
      default:
        return visitor(node);
    }
  }
  function classElementVisitor(node) {
    switch (node.kind) {
      case 177 /* Constructor */:
        return setCurrentClassElementAnd(
          node,
          visitConstructorDeclaration,
          node
        );
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 175 /* MethodDeclaration */:
        return setCurrentClassElementAnd(
          node,
          visitMethodOrAccessorDeclaration,
          node
        );
      case 173 /* PropertyDeclaration */:
        return setCurrentClassElementAnd(
          node,
          visitPropertyDeclaration,
          node
        );
      case 176 /* ClassStaticBlockDeclaration */:
        return setCurrentClassElementAnd(
          node,
          visitClassStaticBlockDeclaration,
          node
        );
      case 168 /* ComputedPropertyName */:
        return visitComputedPropertyName(node);
      case 241 /* SemicolonClassElement */:
        return node;
      default:
        return isModifierLike(node) ? modifierVisitor(node) : visitor(node);
    }
  }
  function propertyNameVisitor(node) {
    switch (node.kind) {
      case 168 /* ComputedPropertyName */:
        return visitComputedPropertyName(node);
      default:
        return visitor(node);
    }
  }
  function accessorFieldResultVisitor(node) {
    switch (node.kind) {
      case 173 /* PropertyDeclaration */:
        return transformFieldInitializer(node);
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return classElementVisitor(node);
      default:
        Debug.assertMissingNode(node, "Expected node to either be a PropertyDeclaration, GetAccessorDeclaration, or SetAccessorDeclaration");
        break;
    }
  }
  function visitPrivateIdentifier(node) {
    if (!shouldTransformPrivateElementsOrClassStaticBlocks) {
      return node;
    }
    if (isStatement(node.parent)) {
      return node;
    }
    return setOriginalNode(factory2.createIdentifier(""), node);
  }
  function transformPrivateIdentifierInInExpression(node) {
    const info ***REMOVED*** accessPrivateIdentifier2(node.left);
    if (info) {
      const receiver ***REMOVED*** visitNode(node.right, visitor, isExpression);
      return setOriginalNode(
        emitHelpers().createClassPrivateFieldInHelper(info.brandCheckIdentifier, receiver),
        node
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitPropertyAssignment(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitVariableStatement(node) {
    const savedPendingStatements ***REMOVED*** pendingStatements;
    pendingStatements ***REMOVED*** [];
    const visitedNode ***REMOVED*** visitEachChild(node, visitor, context);
    const statement ***REMOVED*** some(pendingStatements) ? [visitedNode, ...pendingStatements] : visitedNode;
    pendingStatements ***REMOVED*** savedPendingStatements;
    return statement;
  }
  function visitVariableDeclaration(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitParameterDeclaration(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitBindingElement(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitExportAssignment(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(
        context,
        node,
        /*ignoreEmptyStringLiteral*/
        true,
        node.isExportEquals ? "" : "default"
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function injectPendingExpressions(expression) {
    if (some(pendingExpressions)) {
      if (isParenthesizedExpression(expression)) {
        pendingExpressions.push(expression.expression);
        expression ***REMOVED*** factory2.updateParenthesizedExpression(expression, factory2.inlineExpressions(pendingExpressions));
      } else {
        pendingExpressions.push(expression);
        expression ***REMOVED*** factory2.inlineExpressions(pendingExpressions);
      }
      pendingExpressions ***REMOVED*** void 0;
    }
    return expression;
  }
  function visitComputedPropertyName(node) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    return factory2.updateComputedPropertyName(node, injectPendingExpressions(expression));
  }
  function visitConstructorDeclaration(node) {
    if (currentClassContainer) {
      return transformConstructor(node, currentClassContainer);
    }
    return fallbackVisitor(node);
  }
  function shouldTransformClassElementToWeakMap(node) {
    if (shouldTransformPrivateElementsOrClassStaticBlocks) return true;
    if (hasStaticModifier(node) && getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */) return true;
    return false;
  }
  function visitMethodOrAccessorDeclaration(node) {
    Debug.assert(!hasDecorators(node));
    if (!isPrivateIdentifierClassElementDeclaration(node) || !shouldTransformClassElementToWeakMap(node)) {
      return visitEachChild(node, classElementVisitor, context);
    }
    const info ***REMOVED*** accessPrivateIdentifier2(node.name);
    Debug.assert(info, "Undeclared private name for property declaration.");
    if (!info.isValid) {
      return node;
    }
    const functionName ***REMOVED*** getHoistedFunctionName(node);
    if (functionName) {
      getPendingExpressions().push(
        factory2.createAssignment(
          functionName,
          factory2.createFunctionExpression(
            filter(node.modifiers, (m) ***REMOVED***> isModifier(m) && !isStaticModifier(m) && !isAccessorModifier(m)),
            node.asteriskToken,
            functionName,
            /*typeParameters*/
            void 0,
            visitParameterList(node.parameters, visitor, context),
            /*type*/
            void 0,
            visitFunctionBody(node.body, visitor, context)
          )
        )
      );
    }
    return void 0;
  }
  function setCurrentClassElementAnd(classElement, visitor2, arg) {
    if (classElement !***REMOVED******REMOVED*** currentClassElement) {
      const savedCurrentClassElement ***REMOVED*** currentClassElement;
      currentClassElement ***REMOVED*** classElement;
      const result ***REMOVED*** visitor2(arg);
      currentClassElement ***REMOVED*** savedCurrentClassElement;
      return result;
    }
    return visitor2(arg);
  }
  function getHoistedFunctionName(node) {
    Debug.assert(isPrivateIdentifier(node.name));
    const info ***REMOVED*** accessPrivateIdentifier2(node.name);
    Debug.assert(info, "Undeclared private name for property declaration.");
    if (info.kind ***REMOVED******REMOVED******REMOVED*** "m" /* Method */) {
      return info.methodName;
    }
    if (info.kind ***REMOVED******REMOVED******REMOVED*** "a" /* Accessor */) {
      if (isGetAccessor(node)) {
        return info.getterName;
      }
      if (isSetAccessor(node)) {
        return info.setterName;
      }
    }
  }
  function tryGetClassThis() {
    const lex ***REMOVED*** getClassLexicalEnvironment();
    return lex.classThis ?? lex.classConstructor ?? (currentClassContainer ***REMOVED******REMOVED*** null ? void 0 : currentClassContainer.name);
  }
  function transformAutoAccessor(node) {
    const commentRange ***REMOVED*** getCommentRange(node);
    const sourceMapRange ***REMOVED*** getSourceMapRange(node);
    const name ***REMOVED*** node.name;
    let getterName ***REMOVED*** name;
    let setterName ***REMOVED*** name;
    if (isComputedPropertyName(name) && !isSimpleInlineableExpression(name.expression)) {
      const cacheAssignment ***REMOVED*** findComputedPropertyNameCacheAssignment(name);
      if (cacheAssignment) {
        getterName ***REMOVED*** factory2.updateComputedPropertyName(name, visitNode(name.expression, visitor, isExpression));
        setterName ***REMOVED*** factory2.updateComputedPropertyName(name, cacheAssignment.left);
      } else {
        const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
        setSourceMapRange(temp, name.expression);
        const expression ***REMOVED*** visitNode(name.expression, visitor, isExpression);
        const assignment ***REMOVED*** factory2.createAssignment(temp, expression);
        setSourceMapRange(assignment, name.expression);
        getterName ***REMOVED*** factory2.updateComputedPropertyName(name, assignment);
        setterName ***REMOVED*** factory2.updateComputedPropertyName(name, temp);
      }
    }
    const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
    const backingField ***REMOVED*** createAccessorPropertyBackingField(factory2, node, modifiers, node.initializer);
    setOriginalNode(backingField, node);
    setEmitFlags(backingField, 3072 /* NoComments */);
    setSourceMapRange(backingField, sourceMapRange);
    const receiver ***REMOVED*** isStatic(node) ? tryGetClassThis() ?? factory2.createThis() : factory2.createThis();
    const getter ***REMOVED*** createAccessorPropertyGetRedirector(factory2, node, modifiers, getterName, receiver);
    setOriginalNode(getter, node);
    setCommentRange(getter, commentRange);
    setSourceMapRange(getter, sourceMapRange);
    const setterModifiers ***REMOVED*** factory2.createModifiersFromModifierFlags(modifiersToFlags(modifiers));
    const setter ***REMOVED*** createAccessorPropertySetRedirector(factory2, node, setterModifiers, setterName, receiver);
    setOriginalNode(setter, node);
    setEmitFlags(setter, 3072 /* NoComments */);
    setSourceMapRange(setter, sourceMapRange);
    return visitArray([backingField, getter, setter], accessorFieldResultVisitor, isClassElement);
  }
  function transformPrivateFieldInitializer(node) {
    if (shouldTransformClassElementToWeakMap(node)) {
      const info ***REMOVED*** accessPrivateIdentifier2(node.name);
      Debug.assert(info, "Undeclared private name for property declaration.");
      if (!info.isValid) {
        return node;
      }
      if (info.isStatic && !shouldTransformPrivateElementsOrClassStaticBlocks) {
        const statement ***REMOVED*** transformPropertyOrClassStaticBlock(node, factory2.createThis());
        if (statement) {
          return factory2.createClassStaticBlockDeclaration(factory2.createBlock(
            [statement],
            /*multiLine*/
            true
          ));
        }
      }
      return void 0;
    }
    if (shouldTransformInitializersUsingSet && !isStatic(node) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) && lexicalEnvironment.data.facts & 16 /* WillHoistInitializersToConstructor */) {
      return factory2.updatePropertyDeclaration(
        node,
        visitNodes2(node.modifiers, visitor, isModifierLike),
        node.name,
        /*questionOrExclamationToken*/
        void 0,
        /*type*/
        void 0,
        /*initializer*/
        void 0
      );
    }
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return factory2.updatePropertyDeclaration(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      visitNode(node.name, propertyNameVisitor, isPropertyName),
      /*questionOrExclamationToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
  }
  function transformPublicFieldInitializer(node) {
    if (shouldTransformInitializers && !isAutoAccessorPropertyDeclaration(node)) {
      const expr ***REMOVED*** getPropertyNameExpressionIfNeeded(
        node.name,
        /*shouldHoist*/
        !!node.initializer || useDefineForClassFields
      );
      if (expr) {
        getPendingExpressions().push(...flattenCommaList(expr));
      }
      if (isStatic(node) && !shouldTransformPrivateElementsOrClassStaticBlocks) {
        const initializerStatement ***REMOVED*** transformPropertyOrClassStaticBlock(node, factory2.createThis());
        if (initializerStatement) {
          const staticBlock ***REMOVED*** factory2.createClassStaticBlockDeclaration(
            factory2.createBlock([initializerStatement])
          );
          setOriginalNode(staticBlock, node);
          setCommentRange(staticBlock, node);
          setCommentRange(initializerStatement, { pos: -1, end: -1 });
          setSyntheticLeadingComments(initializerStatement, void 0);
          setSyntheticTrailingComments(initializerStatement, void 0);
          return staticBlock;
        }
      }
      return void 0;
    }
    return factory2.updatePropertyDeclaration(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      visitNode(node.name, propertyNameVisitor, isPropertyName),
      /*questionOrExclamationToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
  }
  function transformFieldInitializer(node) {
    Debug.assert(!hasDecorators(node), "Decorators should already have been transformed and elided.");
    return isPrivateIdentifierClassElementDeclaration(node) ? transformPrivateFieldInitializer(node) : transformPublicFieldInitializer(node);
  }
  function shouldTransformAutoAccessorsInCurrentClass() {
    return shouldTransformAutoAccessors ***REMOVED******REMOVED******REMOVED*** -1 /* True */ || shouldTransformAutoAccessors ***REMOVED******REMOVED******REMOVED*** 3 /* Maybe */ && !!(lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) && !!(lexicalEnvironment.data.facts & 16 /* WillHoistInitializersToConstructor */);
  }
  function visitPropertyDeclaration(node) {
    if (isAutoAccessorPropertyDeclaration(node) && (shouldTransformAutoAccessorsInCurrentClass() || hasStaticModifier(node) && getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */)) {
      return transformAutoAccessor(node);
    }
    return transformFieldInitializer(node);
  }
  function shouldForceDynamicThis() {
    return !!currentClassElement && hasStaticModifier(currentClassElement) && isAccessor(currentClassElement) && isAutoAccessorPropertyDeclaration(getOriginalNode(currentClassElement));
  }
  function ensureDynamicThisIfNeeded(node) {
    if (shouldForceDynamicThis()) {
      const innerExpression ***REMOVED*** skipOuterExpressions(node);
      if (innerExpression.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
        noSubstitution.add(innerExpression);
      }
    }
  }
  function createPrivateIdentifierAccess(info, receiver) {
    receiver ***REMOVED*** visitNode(receiver, visitor, isExpression);
    ensureDynamicThisIfNeeded(receiver);
    return createPrivateIdentifierAccessHelper(info, receiver);
  }
  function createPrivateIdentifierAccessHelper(info, receiver) {
    setCommentRange(receiver, moveRangePos(receiver, -1));
    switch (info.kind) {
      case "a" /* Accessor */:
        return emitHelpers().createClassPrivateFieldGetHelper(
          receiver,
          info.brandCheckIdentifier,
          info.kind,
          info.getterName
        );
      case "m" /* Method */:
        return emitHelpers().createClassPrivateFieldGetHelper(
          receiver,
          info.brandCheckIdentifier,
          info.kind,
          info.methodName
        );
      case "f" /* Field */:
        return emitHelpers().createClassPrivateFieldGetHelper(
          receiver,
          info.brandCheckIdentifier,
          info.kind,
          info.isStatic ? info.variableName : void 0
        );
      case "untransformed":
        return Debug.fail("Access helpers should not be created for untransformed private elements");
      default:
        Debug.assertNever(info, "Unknown private element type");
    }
  }
  function visitPropertyAccessExpression(node) {
    if (isPrivateIdentifier(node.name)) {
      const privateIdentifierInfo ***REMOVED*** accessPrivateIdentifier2(node.name);
      if (privateIdentifierInfo) {
        return setTextRange(
          setOriginalNode(
            createPrivateIdentifierAccess(privateIdentifierInfo, node.expression),
            node
          ),
          node
        );
      }
    }
    if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node) && isIdentifier(node.name) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
      const { classConstructor, superClassReference, facts } ***REMOVED*** lexicalEnvironment.data;
      if (facts & 1 /* ClassWasDecorated */) {
        return visitInvalidSuperProperty(node);
      }
      if (classConstructor && superClassReference) {
        const superProperty ***REMOVED*** factory2.createReflectGetCall(
          superClassReference,
          factory2.createStringLiteralFromNode(node.name),
          classConstructor
        );
        setOriginalNode(superProperty, node.expression);
        setTextRange(superProperty, node.expression);
        return superProperty;
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitElementAccessExpression(node) {
    if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
      const { classConstructor, superClassReference, facts } ***REMOVED*** lexicalEnvironment.data;
      if (facts & 1 /* ClassWasDecorated */) {
        return visitInvalidSuperProperty(node);
      }
      if (classConstructor && superClassReference) {
        const superProperty ***REMOVED*** factory2.createReflectGetCall(
          superClassReference,
          visitNode(node.argumentExpression, visitor, isExpression),
          classConstructor
        );
        setOriginalNode(superProperty, node.expression);
        setTextRange(superProperty, node.expression);
        return superProperty;
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitPreOrPostfixUnaryExpression(node, discarded) {
    if (node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
      const operand ***REMOVED*** skipParentheses(node.operand);
      if (isPrivateIdentifierPropertyAccessExpression(operand)) {
        let info;
        if (info ***REMOVED*** accessPrivateIdentifier2(operand.name)) {
          const receiver ***REMOVED*** visitNode(operand.expression, visitor, isExpression);
          ensureDynamicThisIfNeeded(receiver);
          const { readExpression, initializeExpression } ***REMOVED*** createCopiableReceiverExpr(receiver);
          let expression ***REMOVED*** createPrivateIdentifierAccess(info, readExpression);
          const temp ***REMOVED*** isPrefixUnaryExpression(node) || discarded ? void 0 : factory2.createTempVariable(hoistVariableDeclaration);
          expression ***REMOVED*** expandPreOrPostfixIncrementOrDecrementExpression(factory2, node, expression, hoistVariableDeclaration, temp);
          expression ***REMOVED*** createPrivateIdentifierAssignment(
            info,
            initializeExpression || readExpression,
            expression,
            64 /* EqualsToken */
          );
          setOriginalNode(expression, node);
          setTextRange(expression, node);
          if (temp) {
            expression ***REMOVED*** factory2.createComma(expression, temp);
            setTextRange(expression, node);
          }
          return expression;
        }
      } else if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(operand) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
        const { classConstructor, superClassReference, facts } ***REMOVED*** lexicalEnvironment.data;
        if (facts & 1 /* ClassWasDecorated */) {
          const expression ***REMOVED*** visitInvalidSuperProperty(operand);
          return isPrefixUnaryExpression(node) ? factory2.updatePrefixUnaryExpression(node, expression) : factory2.updatePostfixUnaryExpression(node, expression);
        }
        if (classConstructor && superClassReference) {
          let setterName;
          let getterName;
          if (isPropertyAccessExpression(operand)) {
            if (isIdentifier(operand.name)) {
              getterName ***REMOVED*** setterName ***REMOVED*** factory2.createStringLiteralFromNode(operand.name);
            }
          } else {
            if (isSimpleInlineableExpression(operand.argumentExpression)) {
              getterName ***REMOVED*** setterName ***REMOVED*** operand.argumentExpression;
            } else {
              getterName ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
              setterName ***REMOVED*** factory2.createAssignment(getterName, visitNode(operand.argumentExpression, visitor, isExpression));
            }
          }
          if (setterName && getterName) {
            let expression ***REMOVED*** factory2.createReflectGetCall(superClassReference, getterName, classConstructor);
            setTextRange(expression, operand);
            const temp ***REMOVED*** discarded ? void 0 : factory2.createTempVariable(hoistVariableDeclaration);
            expression ***REMOVED*** expandPreOrPostfixIncrementOrDecrementExpression(factory2, node, expression, hoistVariableDeclaration, temp);
            expression ***REMOVED*** factory2.createReflectSetCall(superClassReference, setterName, expression, classConstructor);
            setOriginalNode(expression, node);
            setTextRange(expression, node);
            if (temp) {
              expression ***REMOVED*** factory2.createComma(expression, temp);
              setTextRange(expression, node);
            }
            return expression;
          }
        }
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForStatement(node) {
    return factory2.updateForStatement(
      node,
      visitNode(node.initializer, discardedValueVisitor, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, discardedValueVisitor, isExpression),
      visitIterationBody(node.statement, visitor, context)
    );
  }
  function visitExpressionStatement(node) {
    return factory2.updateExpressionStatement(
      node,
      visitNode(node.expression, discardedValueVisitor, isExpression)
    );
  }
  function createCopiableReceiverExpr(receiver) {
    const clone ***REMOVED*** nodeIsSynthesized(receiver) ? receiver : factory2.cloneNode(receiver);
    if (receiver.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && noSubstitution.has(receiver)) {
      noSubstitution.add(clone);
    }
    if (isSimpleInlineableExpression(receiver)) {
      return { readExpression: clone, initializeExpression: void 0 };
    }
    const readExpression ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    const initializeExpression ***REMOVED*** factory2.createAssignment(readExpression, clone);
    return { readExpression, initializeExpression };
  }
  function visitCallExpression(node) {
    var _a;
    if (isPrivateIdentifierPropertyAccessExpression(node.expression) && accessPrivateIdentifier2(node.expression.name)) {
      const { thisArg, target } ***REMOVED*** factory2.createCallBinding(node.expression, hoistVariableDeclaration, languageVersion);
      if (isCallChain(node)) {
        return factory2.updateCallChain(
          node,
          factory2.createPropertyAccessChain(visitNode(target, visitor, isExpression), node.questionDotToken, "call"),
          /*questionDotToken*/
          void 0,
          /*typeArguments*/
          void 0,
          [visitNode(thisArg, visitor, isExpression), ...visitNodes2(node.arguments, visitor, isExpression)]
        );
      }
      return factory2.updateCallExpression(
        node,
        factory2.createPropertyAccessExpression(visitNode(target, visitor, isExpression), "call"),
        /*typeArguments*/
        void 0,
        [visitNode(thisArg, visitor, isExpression), ...visitNodes2(node.arguments, visitor, isExpression)]
      );
    }
    if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node.expression) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && ((_a ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) ***REMOVED******REMOVED*** null ? void 0 : _a.classConstructor)) {
      const invocation ***REMOVED*** factory2.createFunctionCallCall(
        visitNode(node.expression, visitor, isExpression),
        lexicalEnvironment.data.classConstructor,
        visitNodes2(node.arguments, visitor, isExpression)
      );
      setOriginalNode(invocation, node);
      setTextRange(invocation, node);
      return invocation;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitTaggedTemplateExpression(node) {
    var _a;
    if (isPrivateIdentifierPropertyAccessExpression(node.tag) && accessPrivateIdentifier2(node.tag.name)) {
      const { thisArg, target } ***REMOVED*** factory2.createCallBinding(node.tag, hoistVariableDeclaration, languageVersion);
      return factory2.updateTaggedTemplateExpression(
        node,
        factory2.createCallExpression(
          factory2.createPropertyAccessExpression(visitNode(target, visitor, isExpression), "bind"),
          /*typeArguments*/
          void 0,
          [visitNode(thisArg, visitor, isExpression)]
        ),
        /*typeArguments*/
        void 0,
        visitNode(node.template, visitor, isTemplateLiteral)
      );
    }
    if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node.tag) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && ((_a ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) ***REMOVED******REMOVED*** null ? void 0 : _a.classConstructor)) {
      const invocation ***REMOVED*** factory2.createFunctionBindCall(
        visitNode(node.tag, visitor, isExpression),
        lexicalEnvironment.data.classConstructor,
        []
      );
      setOriginalNode(invocation, node);
      setTextRange(invocation, node);
      return factory2.updateTaggedTemplateExpression(
        node,
        invocation,
        /*typeArguments*/
        void 0,
        visitNode(node.template, visitor, isTemplateLiteral)
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function transformClassStaticBlockDeclaration(node) {
    if (lexicalEnvironment) {
      lexicalEnvironmentMap.set(getOriginalNode(node), lexicalEnvironment);
    }
    if (shouldTransformPrivateElementsOrClassStaticBlocks) {
      if (isClassThisAssignmentBlock(node)) {
        const result ***REMOVED*** visitNode(node.body.statements[0].expression, visitor, isExpression);
        if (isAssignmentExpression(
          result,
          /*excludeCompoundAssignment*/
          true
        ) && result.left ***REMOVED******REMOVED******REMOVED*** result.right) {
          return void 0;
        }
        return result;
      }
      if (isClassNamedEvaluationHelperBlock(node)) {
        return visitNode(node.body.statements[0].expression, visitor, isExpression);
      }
      startLexicalEnvironment();
      let statements ***REMOVED*** setCurrentClassElementAnd(
        node,
        (statements2) ***REMOVED***> visitNodes2(statements2, visitor, isStatement),
        node.body.statements
      );
      statements ***REMOVED*** factory2.mergeLexicalEnvironment(statements, endLexicalEnvironment());
      const iife ***REMOVED*** factory2.createImmediatelyInvokedArrowFunction(statements);
      setOriginalNode(skipParentheses(iife.expression), node);
      addEmitFlags(skipParentheses(iife.expression), 4 /* AdviseOnEmitNode */);
      setOriginalNode(iife, node);
      setTextRange(iife, node);
      return iife;
    }
  }
  function isAnonymousClassNeedingAssignedName(node) {
    if (isClassExpression(node) && !node.name) {
      const staticPropertiesOrClassStaticBlocks ***REMOVED*** getStaticPropertiesAndClassStaticBlock(node);
      if (some(staticPropertiesOrClassStaticBlocks, isClassNamedEvaluationHelperBlock)) {
        return false;
      }
      const hasTransformableStatics ***REMOVED*** (shouldTransformPrivateElementsOrClassStaticBlocks || !!(getInternalEmitFlags(node) && 32 /* TransformPrivateStaticElements */)) && some(staticPropertiesOrClassStaticBlocks, (node2) ***REMOVED***> isClassStaticBlockDeclaration(node2) || isPrivateIdentifierClassElementDeclaration(node2) || shouldTransformInitializers && isInitializedProperty(node2));
      return hasTransformableStatics;
    }
    return false;
  }
  function visitBinaryExpression(node, discarded) {
    if (isDestructuringAssignment(node)) {
      const savedPendingExpressions ***REMOVED*** pendingExpressions;
      pendingExpressions ***REMOVED*** void 0;
      node ***REMOVED*** factory2.updateBinaryExpression(
        node,
        visitNode(node.left, assignmentTargetVisitor, isExpression),
        node.operatorToken,
        visitNode(node.right, visitor, isExpression)
      );
      const expr ***REMOVED*** some(pendingExpressions) ? factory2.inlineExpressions(compact([...pendingExpressions, node])) : node;
      pendingExpressions ***REMOVED*** savedPendingExpressions;
      return expr;
    }
    if (isAssignmentExpression(node)) {
      if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
        node ***REMOVED*** transformNamedEvaluation(context, node);
        Debug.assertNode(node, isAssignmentExpression);
      }
      const left ***REMOVED*** skipOuterExpressions(node.left, 8 /* PartiallyEmittedExpressions */ | 1 /* Parentheses */);
      if (isPrivateIdentifierPropertyAccessExpression(left)) {
        const info ***REMOVED*** accessPrivateIdentifier2(left.name);
        if (info) {
          return setTextRange(
            setOriginalNode(
              createPrivateIdentifierAssignment(info, left.expression, node.right, node.operatorToken.kind),
              node
            ),
            node
          );
        }
      } else if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node.left) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
        const { classConstructor, superClassReference, facts } ***REMOVED*** lexicalEnvironment.data;
        if (facts & 1 /* ClassWasDecorated */) {
          return factory2.updateBinaryExpression(
            node,
            visitInvalidSuperProperty(node.left),
            node.operatorToken,
            visitNode(node.right, visitor, isExpression)
          );
        }
        if (classConstructor && superClassReference) {
          let setterName ***REMOVED*** isElementAccessExpression(node.left) ? visitNode(node.left.argumentExpression, visitor, isExpression) : isIdentifier(node.left.name) ? factory2.createStringLiteralFromNode(node.left.name) : void 0;
          if (setterName) {
            let expression ***REMOVED*** visitNode(node.right, visitor, isExpression);
            if (isCompoundAssignment(node.operatorToken.kind)) {
              let getterName ***REMOVED*** setterName;
              if (!isSimpleInlineableExpression(setterName)) {
                getterName ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
                setterName ***REMOVED*** factory2.createAssignment(getterName, setterName);
              }
              const superPropertyGet ***REMOVED*** factory2.createReflectGetCall(
                superClassReference,
                getterName,
                classConstructor
              );
              setOriginalNode(superPropertyGet, node.left);
              setTextRange(superPropertyGet, node.left);
              expression ***REMOVED*** factory2.createBinaryExpression(
                superPropertyGet,
                getNonAssignmentOperatorForCompoundAssignment(node.operatorToken.kind),
                expression
              );
              setTextRange(expression, node);
            }
            const temp ***REMOVED*** discarded ? void 0 : factory2.createTempVariable(hoistVariableDeclaration);
            if (temp) {
              expression ***REMOVED*** factory2.createAssignment(temp, expression);
              setTextRange(temp, node);
            }
            expression ***REMOVED*** factory2.createReflectSetCall(
              superClassReference,
              setterName,
              expression,
              classConstructor
            );
            setOriginalNode(expression, node);
            setTextRange(expression, node);
            if (temp) {
              expression ***REMOVED*** factory2.createComma(expression, temp);
              setTextRange(expression, node);
            }
            return expression;
          }
        }
      }
    }
    if (isPrivateIdentifierInExpression(node)) {
      return transformPrivateIdentifierInInExpression(node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCommaListExpression(node, discarded) {
    const elements ***REMOVED*** discarded ? visitCommaListElements(node.elements, discardedValueVisitor) : visitCommaListElements(node.elements, visitor, discardedValueVisitor);
    return factory2.updateCommaListExpression(node, elements);
  }
  function visitParenthesizedExpression(node, discarded) {
    const visitorFunc ***REMOVED*** discarded ? discardedValueVisitor : visitor;
    const expression ***REMOVED*** visitNode(node.expression, visitorFunc, isExpression);
    return factory2.updateParenthesizedExpression(node, expression);
  }
  function createPrivateIdentifierAssignment(info, receiver, right, operator) {
    receiver ***REMOVED*** visitNode(receiver, visitor, isExpression);
    right ***REMOVED*** visitNode(right, visitor, isExpression);
    ensureDynamicThisIfNeeded(receiver);
    if (isCompoundAssignment(operator)) {
      const { readExpression, initializeExpression } ***REMOVED*** createCopiableReceiverExpr(receiver);
      receiver ***REMOVED*** initializeExpression || readExpression;
      right ***REMOVED*** factory2.createBinaryExpression(
        createPrivateIdentifierAccessHelper(info, readExpression),
        getNonAssignmentOperatorForCompoundAssignment(operator),
        right
      );
    }
    setCommentRange(receiver, moveRangePos(receiver, -1));
    switch (info.kind) {
      case "a" /* Accessor */:
        return emitHelpers().createClassPrivateFieldSetHelper(
          receiver,
          info.brandCheckIdentifier,
          right,
          info.kind,
          info.setterName
        );
      case "m" /* Method */:
        return emitHelpers().createClassPrivateFieldSetHelper(
          receiver,
          info.brandCheckIdentifier,
          right,
          info.kind,
          /*f*/
          void 0
        );
      case "f" /* Field */:
        return emitHelpers().createClassPrivateFieldSetHelper(
          receiver,
          info.brandCheckIdentifier,
          right,
          info.kind,
          info.isStatic ? info.variableName : void 0
        );
      case "untransformed":
        return Debug.fail("Access helpers should not be created for untransformed private elements");
      default:
        Debug.assertNever(info, "Unknown private element type");
    }
  }
  function getPrivateInstanceMethodsAndAccessors(node) {
    return filter(node.members, isNonStaticMethodOrAccessorWithPrivateName);
  }
  function getClassFacts(node) {
    var _a;
    let facts ***REMOVED*** 0 /* None */;
    const original ***REMOVED*** getOriginalNode(node);
    if (isClassLike(original) && classOrConstructorParameterIsDecorated(legacyDecorators, original)) {
      facts |***REMOVED*** 1 /* ClassWasDecorated */;
    }
    if (shouldTransformPrivateElementsOrClassStaticBlocks && (classHasClassThisAssignment(node) || classHasExplicitlyAssignedName(node))) {
      facts |***REMOVED*** 2 /* NeedsClassConstructorReference */;
    }
    let containsPublicInstanceFields ***REMOVED*** false;
    let containsInitializedPublicInstanceFields ***REMOVED*** false;
    let containsInstancePrivateElements ***REMOVED*** false;
    let containsInstanceAutoAccessors ***REMOVED*** false;
    for (const member of node.members) {
      if (isStatic(member)) {
        if (member.name && (isPrivateIdentifier(member.name) || isAutoAccessorPropertyDeclaration(member)) && shouldTransformPrivateElementsOrClassStaticBlocks) {
          facts |***REMOVED*** 2 /* NeedsClassConstructorReference */;
        } else if (isAutoAccessorPropertyDeclaration(member) && shouldTransformAutoAccessors ***REMOVED******REMOVED******REMOVED*** -1 /* True */ && !node.name && !((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis)) {
          facts |***REMOVED*** 2 /* NeedsClassConstructorReference */;
        }
        if (isPropertyDeclaration(member) || isClassStaticBlockDeclaration(member)) {
          if (shouldTransformThisInStaticInitializers && member.transformFlags & 16384 /* ContainsLexicalThis */) {
            facts |***REMOVED*** 8 /* NeedsSubstitutionForThisInClassStaticField */;
            if (!(facts & 1 /* ClassWasDecorated */)) {
              facts |***REMOVED*** 2 /* NeedsClassConstructorReference */;
            }
          }
          if (shouldTransformSuperInStaticInitializers && member.transformFlags & 134217728 /* ContainsLexicalSuper */) {
            if (!(facts & 1 /* ClassWasDecorated */)) {
              facts |***REMOVED*** 2 /* NeedsClassConstructorReference */ | 4 /* NeedsClassSuperReference */;
            }
          }
        }
      } else if (!hasAbstractModifier(getOriginalNode(member))) {
        if (isAutoAccessorPropertyDeclaration(member)) {
          containsInstanceAutoAccessors ***REMOVED*** true;
          containsInstancePrivateElements || (containsInstancePrivateElements ***REMOVED*** isPrivateIdentifierClassElementDeclaration(member));
        } else if (isPrivateIdentifierClassElementDeclaration(member)) {
          containsInstancePrivateElements ***REMOVED*** true;
          if (resolver.hasNodeCheckFlag(member, 262144 /* ContainsConstructorReference */)) {
            facts |***REMOVED*** 2 /* NeedsClassConstructorReference */;
          }
        } else if (isPropertyDeclaration(member)) {
          containsPublicInstanceFields ***REMOVED*** true;
          containsInitializedPublicInstanceFields || (containsInitializedPublicInstanceFields ***REMOVED*** !!member.initializer);
        }
      }
    }
    const willHoistInitializersToConstructor ***REMOVED*** shouldTransformInitializersUsingDefine && containsPublicInstanceFields || shouldTransformInitializersUsingSet && containsInitializedPublicInstanceFields || shouldTransformPrivateElementsOrClassStaticBlocks && containsInstancePrivateElements || shouldTransformPrivateElementsOrClassStaticBlocks && containsInstanceAutoAccessors && shouldTransformAutoAccessors ***REMOVED******REMOVED******REMOVED*** -1 /* True */;
    if (willHoistInitializersToConstructor) {
      facts |***REMOVED*** 16 /* WillHoistInitializersToConstructor */;
    }
    return facts;
  }
  function visitExpressionWithTypeArgumentsInHeritageClause(node) {
    var _a;
    const facts ***REMOVED*** ((_a ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) ***REMOVED******REMOVED*** null ? void 0 : _a.facts) || 0 /* None */;
    if (facts & 4 /* NeedsClassSuperReference */) {
      const temp ***REMOVED*** factory2.createTempVariable(
        hoistVariableDeclaration,
        /*reservedInNestedScopes*/
        true
      );
      getClassLexicalEnvironment().superClassReference ***REMOVED*** temp;
      return factory2.updateExpressionWithTypeArguments(
        node,
        factory2.createAssignment(
          temp,
          visitNode(node.expression, visitor, isExpression)
        ),
        /*typeArguments*/
        void 0
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitInNewClassLexicalEnvironment(node, visitor2) {
    var _a;
    const savedCurrentClassContainer ***REMOVED*** currentClassContainer;
    const savedPendingExpressions ***REMOVED*** pendingExpressions;
    const savedLexicalEnvironment ***REMOVED*** lexicalEnvironment;
    currentClassContainer ***REMOVED*** node;
    pendingExpressions ***REMOVED*** void 0;
    startClassLexicalEnvironment();
    const shouldAlwaysTransformPrivateStaticElements ***REMOVED*** getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */;
    if (shouldTransformPrivateElementsOrClassStaticBlocks || shouldAlwaysTransformPrivateStaticElements) {
      const name ***REMOVED*** getNameOfDeclaration(node);
      if (name && isIdentifier(name)) {
        getPrivateIdentifierEnvironment().data.className ***REMOVED*** name;
      } else if ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.assignedName) {
        if (isStringLiteral(node.emitNode.assignedName)) {
          if (node.emitNode.assignedName.textSourceNode && isIdentifier(node.emitNode.assignedName.textSourceNode)) {
            getPrivateIdentifierEnvironment().data.className ***REMOVED*** node.emitNode.assignedName.textSourceNode;
          } else if (isIdentifierText(node.emitNode.assignedName.text, languageVersion)) {
            const prefixName ***REMOVED*** factory2.createIdentifier(node.emitNode.assignedName.text);
            getPrivateIdentifierEnvironment().data.className ***REMOVED*** prefixName;
          }
        }
      }
    }
    if (shouldTransformPrivateElementsOrClassStaticBlocks) {
      const privateInstanceMethodsAndAccessors ***REMOVED*** getPrivateInstanceMethodsAndAccessors(node);
      if (some(privateInstanceMethodsAndAccessors)) {
        getPrivateIdentifierEnvironment().data.weakSetName ***REMOVED*** createHoistedVariableForClass(
          "instances",
          privateInstanceMethodsAndAccessors[0].name
        );
      }
    }
    const facts ***REMOVED*** getClassFacts(node);
    if (facts) {
      getClassLexicalEnvironment().facts ***REMOVED*** facts;
    }
    if (facts & 8 /* NeedsSubstitutionForThisInClassStaticField */) {
      enableSubstitutionForClassStaticThisOrSuperReference();
    }
    const result ***REMOVED*** visitor2(node, facts);
    endClassLexicalEnvironment();
    Debug.assert(lexicalEnvironment ***REMOVED******REMOVED******REMOVED*** savedLexicalEnvironment);
    currentClassContainer ***REMOVED*** savedCurrentClassContainer;
    pendingExpressions ***REMOVED*** savedPendingExpressions;
    return result;
  }
  function visitClassDeclaration(node) {
    return visitInNewClassLexicalEnvironment(node, visitClassDeclarationInNewClassLexicalEnvironment);
  }
  function visitClassDeclarationInNewClassLexicalEnvironment(node, facts) {
    var _a, _b;
    let pendingClassReferenceAssignment;
    if (facts & 2 /* NeedsClassConstructorReference */) {
      if (shouldTransformPrivateElementsOrClassStaticBlocks && ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis)) {
        getClassLexicalEnvironment().classConstructor ***REMOVED*** node.emitNode.classThis;
        pendingClassReferenceAssignment ***REMOVED*** factory2.createAssignment(node.emitNode.classThis, factory2.getInternalName(node));
      } else {
        const temp ***REMOVED*** factory2.createTempVariable(
          hoistVariableDeclaration,
          /*reservedInNestedScopes*/
          true
        );
        getClassLexicalEnvironment().classConstructor ***REMOVED*** factory2.cloneNode(temp);
        pendingClassReferenceAssignment ***REMOVED*** factory2.createAssignment(temp, factory2.getInternalName(node));
      }
    }
    if ((_b ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _b.classThis) {
      getClassLexicalEnvironment().classThis ***REMOVED*** node.emitNode.classThis;
    }
    const isClassWithConstructorReference ***REMOVED*** resolver.hasNodeCheckFlag(node, 262144 /* ContainsConstructorReference */);
    const isExport ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
    const isDefault ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */);
    let modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
    const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, heritageClauseVisitor, isHeritageClause);
    const { members, prologue } ***REMOVED*** transformClassMembers(node);
    const statements ***REMOVED*** [];
    if (pendingClassReferenceAssignment) {
      getPendingExpressions().unshift(pendingClassReferenceAssignment);
    }
    if (some(pendingExpressions)) {
      statements.push(factory2.createExpressionStatement(factory2.inlineExpressions(pendingExpressions)));
    }
    if (shouldTransformInitializersUsingSet || shouldTransformPrivateElementsOrClassStaticBlocks || getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */) {
      const staticProperties ***REMOVED*** getStaticPropertiesAndClassStaticBlock(node);
      if (some(staticProperties)) {
        addPropertyOrClassStaticBlockStatements(statements, staticProperties, factory2.getInternalName(node));
      }
    }
    if (statements.length > 0 && isExport && isDefault) {
      modifiers ***REMOVED*** visitNodes2(modifiers, (node2) ***REMOVED***> isExportOrDefaultModifier(node2) ? void 0 : node2, isModifier);
      statements.push(factory2.createExportAssignment(
        /*modifiers*/
        void 0,
        /*isExportEquals*/
        false,
        factory2.getLocalName(
          node,
          /*allowComments*/
          false,
          /*allowSourceMaps*/
          true
        )
      ));
    }
    const alias ***REMOVED*** getClassLexicalEnvironment().classConstructor;
    if (isClassWithConstructorReference && alias) {
      enableSubstitutionForClassAliases();
      classAliases[getOriginalNodeId(node)] ***REMOVED*** alias;
    }
    const classDecl ***REMOVED*** factory2.updateClassDeclaration(
      node,
      modifiers,
      node.name,
      /*typeParameters*/
      void 0,
      heritageClauses,
      members
    );
    statements.unshift(classDecl);
    if (prologue) {
      statements.unshift(factory2.createExpressionStatement(prologue));
    }
    return statements;
  }
  function visitClassExpression(node) {
    return visitInNewClassLexicalEnvironment(node, visitClassExpressionInNewClassLexicalEnvironment);
  }
  function visitClassExpressionInNewClassLexicalEnvironment(node, facts) {
    var _a, _b, _c;
    const isDecoratedClassDeclaration ***REMOVED*** !!(facts & 1 /* ClassWasDecorated */);
    const staticPropertiesOrClassStaticBlocks ***REMOVED*** getStaticPropertiesAndClassStaticBlock(node);
    const isClassWithConstructorReference ***REMOVED*** resolver.hasNodeCheckFlag(node, 262144 /* ContainsConstructorReference */);
    const requiresBlockScopedVar ***REMOVED*** resolver.hasNodeCheckFlag(node, 32768 /* BlockScopedBindingInLoop */);
    let temp;
    function createClassTempVar() {
      var _a2;
      if (shouldTransformPrivateElementsOrClassStaticBlocks && ((_a2 ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a2.classThis)) {
        return getClassLexicalEnvironment().classConstructor ***REMOVED*** node.emitNode.classThis;
      }
      const temp2 ***REMOVED*** factory2.createTempVariable(
        requiresBlockScopedVar ? addBlockScopedVariable : hoistVariableDeclaration,
        /*reservedInNestedScopes*/
        true
      );
      getClassLexicalEnvironment().classConstructor ***REMOVED*** factory2.cloneNode(temp2);
      return temp2;
    }
    if ((_a ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis) {
      getClassLexicalEnvironment().classThis ***REMOVED*** node.emitNode.classThis;
    }
    if (facts & 2 /* NeedsClassConstructorReference */) {
      temp ?? (temp ***REMOVED*** createClassTempVar());
    }
    const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
    const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, heritageClauseVisitor, isHeritageClause);
    const { members, prologue } ***REMOVED*** transformClassMembers(node);
    const classExpression ***REMOVED*** factory2.updateClassExpression(
      node,
      modifiers,
      node.name,
      /*typeParameters*/
      void 0,
      heritageClauses,
      members
    );
    const expressions ***REMOVED*** [];
    if (prologue) {
      expressions.push(prologue);
    }
    const hasTransformableStatics ***REMOVED*** (shouldTransformPrivateElementsOrClassStaticBlocks || getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */) && some(staticPropertiesOrClassStaticBlocks, (node2) ***REMOVED***> isClassStaticBlockDeclaration(node2) || isPrivateIdentifierClassElementDeclaration(node2) || shouldTransformInitializers && isInitializedProperty(node2));
    if (hasTransformableStatics || some(pendingExpressions)) {
      if (isDecoratedClassDeclaration) {
        Debug.assertIsDefined(pendingStatements, "Decorated classes transformed by TypeScript are expected to be within a variable declaration.");
        if (some(pendingExpressions)) {
          addRange(pendingStatements, map(pendingExpressions, factory2.createExpressionStatement));
        }
        if (some(staticPropertiesOrClassStaticBlocks)) {
          addPropertyOrClassStaticBlockStatements(pendingStatements, staticPropertiesOrClassStaticBlocks, ((_b ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _b.classThis) ?? factory2.getInternalName(node));
        }
        if (temp) {
          expressions.push(factory2.createAssignment(temp, classExpression));
        } else if (shouldTransformPrivateElementsOrClassStaticBlocks && ((_c ***REMOVED*** node.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _c.classThis)) {
          expressions.push(factory2.createAssignment(node.emitNode.classThis, classExpression));
        } else {
          expressions.push(classExpression);
        }
      } else {
        temp ?? (temp ***REMOVED*** createClassTempVar());
        if (isClassWithConstructorReference) {
          enableSubstitutionForClassAliases();
          const alias ***REMOVED*** factory2.cloneNode(temp);
          alias.emitNode.autoGenerate.flags &***REMOVED*** ~8 /* ReservedInNestedScopes */;
          classAliases[getOriginalNodeId(node)] ***REMOVED*** alias;
        }
        expressions.push(factory2.createAssignment(temp, classExpression));
        addRange(expressions, pendingExpressions);
        addRange(expressions, generateInitializedPropertyExpressionsOrClassStaticBlock(staticPropertiesOrClassStaticBlocks, temp));
        expressions.push(factory2.cloneNode(temp));
      }
    } else {
      expressions.push(classExpression);
    }
    if (expressions.length > 1) {
      addEmitFlags(classExpression, 131072 /* Indented */);
      expressions.forEach(startOnNewLine);
    }
    return factory2.inlineExpressions(expressions);
  }
  function visitClassStaticBlockDeclaration(node) {
    if (!shouldTransformPrivateElementsOrClassStaticBlocks) {
      return visitEachChild(node, visitor, context);
    }
    return void 0;
  }
  function visitThisExpression(node) {
    if (shouldTransformThisInStaticInitializers && currentClassElement && isClassStaticBlockDeclaration(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
      const { classThis, classConstructor } ***REMOVED*** lexicalEnvironment.data;
      return classThis ?? classConstructor ?? node;
    }
    return node;
  }
  function transformClassMembers(node) {
    const shouldTransformPrivateStaticElementsInClass ***REMOVED*** !!(getInternalEmitFlags(node) & 32 /* TransformPrivateStaticElements */);
    if (shouldTransformPrivateElementsOrClassStaticBlocks || shouldTransformPrivateStaticElementsInFile) {
      for (const member of node.members) {
        if (isPrivateIdentifierClassElementDeclaration(member)) {
          if (shouldTransformClassElementToWeakMap(member)) {
            addPrivateIdentifierToEnvironment(member, member.name, addPrivateIdentifierClassElementToEnvironment);
          } else {
            const privateEnv ***REMOVED*** getPrivateIdentifierEnvironment();
            setPrivateIdentifier(privateEnv, member.name, { kind: "untransformed" });
          }
        }
      }
      if (shouldTransformPrivateElementsOrClassStaticBlocks) {
        if (some(getPrivateInstanceMethodsAndAccessors(node))) {
          createBrandCheckWeakSetForPrivateMethods();
        }
      }
      if (shouldTransformAutoAccessorsInCurrentClass()) {
        for (const member of node.members) {
          if (isAutoAccessorPropertyDeclaration(member)) {
            const storageName ***REMOVED*** factory2.getGeneratedPrivateNameForNode(
              member.name,
              /*prefix*/
              void 0,
              "_accessor_storage"
            );
            if (shouldTransformPrivateElementsOrClassStaticBlocks || shouldTransformPrivateStaticElementsInClass && hasStaticModifier(member)) {
              addPrivateIdentifierToEnvironment(member, storageName, addPrivateIdentifierPropertyDeclarationToEnvironment);
            } else {
              const privateEnv ***REMOVED*** getPrivateIdentifierEnvironment();
              setPrivateIdentifier(privateEnv, storageName, { kind: "untransformed" });
            }
          }
        }
      }
    }
    let members ***REMOVED*** visitNodes2(node.members, classElementVisitor, isClassElement);
    let syntheticConstructor;
    if (!some(members, isConstructorDeclaration)) {
      syntheticConstructor ***REMOVED*** transformConstructor(
        /*constructor*/
        void 0,
        node
      );
    }
    let prologue;
    let syntheticStaticBlock;
    if (!shouldTransformPrivateElementsOrClassStaticBlocks && some(pendingExpressions)) {
      let statement ***REMOVED*** factory2.createExpressionStatement(factory2.inlineExpressions(pendingExpressions));
      if (statement.transformFlags & 134234112 /* ContainsLexicalThisOrSuper */) {
        const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
        const arrow ***REMOVED*** factory2.createArrowFunction(
          /*modifiers*/
          void 0,
          /*typeParameters*/
          void 0,
          /*parameters*/
          [],
          /*type*/
          void 0,
          /*equalsGreaterThanToken*/
          void 0,
          factory2.createBlock([statement])
        );
        prologue ***REMOVED*** factory2.createAssignment(temp, arrow);
        statement ***REMOVED*** factory2.createExpressionStatement(factory2.createCallExpression(
          temp,
          /*typeArguments*/
          void 0,
          []
        ));
      }
      const block ***REMOVED*** factory2.createBlock([statement]);
      syntheticStaticBlock ***REMOVED*** factory2.createClassStaticBlockDeclaration(block);
      pendingExpressions ***REMOVED*** void 0;
    }
    if (syntheticConstructor || syntheticStaticBlock) {
      let membersArray;
      const classThisAssignmentBlock ***REMOVED*** find(members, isClassThisAssignmentBlock);
      const classNamedEvaluationHelperBlock ***REMOVED*** find(members, isClassNamedEvaluationHelperBlock);
      membersArray ***REMOVED*** append(membersArray, classThisAssignmentBlock);
      membersArray ***REMOVED*** append(membersArray, classNamedEvaluationHelperBlock);
      membersArray ***REMOVED*** append(membersArray, syntheticConstructor);
      membersArray ***REMOVED*** append(membersArray, syntheticStaticBlock);
      const remainingMembers ***REMOVED*** classThisAssignmentBlock || classNamedEvaluationHelperBlock ? filter(members, (member) ***REMOVED***> member !***REMOVED******REMOVED*** classThisAssignmentBlock && member !***REMOVED******REMOVED*** classNamedEvaluationHelperBlock) : members;
      membersArray ***REMOVED*** addRange(membersArray, remainingMembers);
      members ***REMOVED*** setTextRange(
        factory2.createNodeArray(membersArray),
        /*location*/
        node.members
      );
    }
    return { members, prologue };
  }
  function createBrandCheckWeakSetForPrivateMethods() {
    const { weakSetName } ***REMOVED*** getPrivateIdentifierEnvironment().data;
    Debug.assert(weakSetName, "weakSetName should be set in private identifier environment");
    getPendingExpressions().push(
      factory2.createAssignment(
        weakSetName,
        factory2.createNewExpression(
          factory2.createIdentifier("WeakSet"),
          /*typeArguments*/
          void 0,
          []
        )
      )
    );
  }
  function transformConstructor(constructor, container) {
    constructor ***REMOVED*** visitNode(constructor, visitor, isConstructorDeclaration);
    if (!(lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) || !(lexicalEnvironment.data.facts & 16 /* WillHoistInitializersToConstructor */)) {
      return constructor;
    }
    const extendsClauseElement ***REMOVED*** getEffectiveBaseTypeNode(container);
    const isDerivedClass ***REMOVED*** !!(extendsClauseElement && skipOuterExpressions(extendsClauseElement.expression).kind !***REMOVED******REMOVED*** 106 /* NullKeyword */);
    const parameters ***REMOVED*** visitParameterList(constructor ? constructor.parameters : void 0, visitor, context);
    const body ***REMOVED*** transformConstructorBody(container, constructor, isDerivedClass);
    if (!body) {
      return constructor;
    }
    if (constructor) {
      Debug.assert(parameters);
      return factory2.updateConstructorDeclaration(
        constructor,
        /*modifiers*/
        void 0,
        parameters,
        body
      );
    }
    return startOnNewLine(
      setOriginalNode(
        setTextRange(
          factory2.createConstructorDeclaration(
            /*modifiers*/
            void 0,
            parameters ?? [],
            body
          ),
          constructor || container
        ),
        constructor
      )
    );
  }
  function transformConstructorBodyWorker(statementsOut, statementsIn, statementOffset, superPath, superPathDepth, initializerStatements, constructor) {
    const superStatementIndex ***REMOVED*** superPath[superPathDepth];
    const superStatement ***REMOVED*** statementsIn[superStatementIndex];
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, statementOffset, superStatementIndex - statementOffset));
    statementOffset ***REMOVED*** superStatementIndex + 1;
    if (isTryStatement(superStatement)) {
      const tryBlockStatements ***REMOVED*** [];
      transformConstructorBodyWorker(
        tryBlockStatements,
        superStatement.tryBlock.statements,
        /*statementOffset*/
        0,
        superPath,
        superPathDepth + 1,
        initializerStatements,
        constructor
      );
      const tryBlockStatementsArray ***REMOVED*** factory2.createNodeArray(tryBlockStatements);
      setTextRange(tryBlockStatementsArray, superStatement.tryBlock.statements);
      statementsOut.push(factory2.updateTryStatement(
        superStatement,
        factory2.updateBlock(superStatement.tryBlock, tryBlockStatements),
        visitNode(superStatement.catchClause, visitor, isCatchClause),
        visitNode(superStatement.finallyBlock, visitor, isBlock)
      ));
    } else {
      addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, superStatementIndex, 1));
      while (statementOffset < statementsIn.length) {
        const statement ***REMOVED*** statementsIn[statementOffset];
        if (isParameterPropertyDeclaration(getOriginalNode(statement), constructor)) {
          statementOffset++;
        } else {
          break;
        }
      }
      addRange(statementsOut, initializerStatements);
    }
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, statementOffset));
  }
  function transformConstructorBody(node, constructor, isDerivedClass) {
    var _a;
    const instanceProperties ***REMOVED*** getProperties(
      node,
      /*requireInitializer*/
      false,
      /*isStatic*/
      false
    );
    let properties ***REMOVED*** instanceProperties;
    if (!useDefineForClassFields) {
      properties ***REMOVED*** filter(properties, (property) ***REMOVED***> !!property.initializer || isPrivateIdentifier(property.name) || hasAccessorModifier(property));
    }
    const privateMethodsAndAccessors ***REMOVED*** getPrivateInstanceMethodsAndAccessors(node);
    const needsConstructorBody ***REMOVED*** some(properties) || some(privateMethodsAndAccessors);
    if (!constructor && !needsConstructorBody) {
      return visitFunctionBody(
        /*node*/
        void 0,
        visitor,
        context
      );
    }
    resumeLexicalEnvironment();
    const needsSyntheticConstructor ***REMOVED*** !constructor && isDerivedClass;
    let statementOffset ***REMOVED*** 0;
    let statements ***REMOVED*** [];
    const initializerStatements ***REMOVED*** [];
    const receiver ***REMOVED*** factory2.createThis();
    addInstanceMethodStatements(initializerStatements, privateMethodsAndAccessors, receiver);
    if (constructor) {
      const parameterProperties ***REMOVED*** filter(instanceProperties, (prop) ***REMOVED***> isParameterPropertyDeclaration(getOriginalNode(prop), constructor));
      const nonParameterProperties ***REMOVED*** filter(properties, (prop) ***REMOVED***> !isParameterPropertyDeclaration(getOriginalNode(prop), constructor));
      addPropertyOrClassStaticBlockStatements(initializerStatements, parameterProperties, receiver);
      addPropertyOrClassStaticBlockStatements(initializerStatements, nonParameterProperties, receiver);
    } else {
      addPropertyOrClassStaticBlockStatements(initializerStatements, properties, receiver);
    }
    if (constructor ***REMOVED******REMOVED*** null ? void 0 : constructor.body) {
      statementOffset ***REMOVED*** factory2.copyPrologue(
        constructor.body.statements,
        statements,
        /*ensureUseStrict*/
        false,
        visitor
      );
      const superStatementIndices ***REMOVED*** findSuperStatementIndexPath(constructor.body.statements, statementOffset);
      if (superStatementIndices.length) {
        transformConstructorBodyWorker(
          statements,
          constructor.body.statements,
          statementOffset,
          superStatementIndices,
          /*superPathDepth*/
          0,
          initializerStatements,
          constructor
        );
      } else {
        while (statementOffset < constructor.body.statements.length) {
          const statement ***REMOVED*** constructor.body.statements[statementOffset];
          if (isParameterPropertyDeclaration(getOriginalNode(statement), constructor)) {
            statementOffset++;
          } else {
            break;
          }
        }
        addRange(statements, initializerStatements);
        addRange(statements, visitNodes2(constructor.body.statements, visitor, isStatement, statementOffset));
      }
    } else {
      if (needsSyntheticConstructor) {
        statements.push(
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              factory2.createSuper(),
              /*typeArguments*/
              void 0,
              [factory2.createSpreadElement(factory2.createIdentifier("arguments"))]
            )
          )
        );
      }
      addRange(statements, initializerStatements);
    }
    statements ***REMOVED*** factory2.mergeLexicalEnvironment(statements, endLexicalEnvironment());
    if (statements.length ***REMOVED******REMOVED******REMOVED*** 0 && !constructor) {
      return void 0;
    }
    const multiLine ***REMOVED*** (constructor ***REMOVED******REMOVED*** null ? void 0 : constructor.body) && constructor.body.statements.length >***REMOVED*** statements.length ? constructor.body.multiLine ?? statements.length > 0 : statements.length > 0;
    return setTextRange(
      factory2.createBlock(
        setTextRange(
          factory2.createNodeArray(statements),
          /*location*/
          ((_a ***REMOVED*** constructor ***REMOVED******REMOVED*** null ? void 0 : constructor.body) ***REMOVED******REMOVED*** null ? void 0 : _a.statements) ?? node.members
        ),
        multiLine
      ),
      /*location*/
      constructor ***REMOVED******REMOVED*** null ? void 0 : constructor.body
    );
  }
  function addPropertyOrClassStaticBlockStatements(statements, properties, receiver) {
    for (const property of properties) {
      if (isStatic(property) && !shouldTransformPrivateElementsOrClassStaticBlocks) {
        continue;
      }
      const statement ***REMOVED*** transformPropertyOrClassStaticBlock(property, receiver);
      if (!statement) {
        continue;
      }
      statements.push(statement);
    }
  }
  function transformPropertyOrClassStaticBlock(property, receiver) {
    const expression ***REMOVED*** isClassStaticBlockDeclaration(property) ? setCurrentClassElementAnd(property, transformClassStaticBlockDeclaration, property) : transformProperty(property, receiver);
    if (!expression) {
      return void 0;
    }
    const statement ***REMOVED*** factory2.createExpressionStatement(expression);
    setOriginalNode(statement, property);
    addEmitFlags(statement, getEmitFlags(property) & 3072 /* NoComments */);
    setCommentRange(statement, property);
    const propertyOriginalNode ***REMOVED*** getOriginalNode(property);
    if (isParameter(propertyOriginalNode)) {
      setSourceMapRange(statement, propertyOriginalNode);
      removeAllComments(statement);
    } else {
      setSourceMapRange(statement, moveRangePastModifiers(property));
    }
    setSyntheticLeadingComments(expression, void 0);
    setSyntheticTrailingComments(expression, void 0);
    if (hasAccessorModifier(propertyOriginalNode)) {
      addEmitFlags(statement, 3072 /* NoComments */);
    }
    return statement;
  }
  function generateInitializedPropertyExpressionsOrClassStaticBlock(propertiesOrClassStaticBlocks, receiver) {
    const expressions ***REMOVED*** [];
    for (const property of propertiesOrClassStaticBlocks) {
      const expression ***REMOVED*** isClassStaticBlockDeclaration(property) ? setCurrentClassElementAnd(property, transformClassStaticBlockDeclaration, property) : setCurrentClassElementAnd(
        property,
        () ***REMOVED***> transformProperty(property, receiver),
        /*arg*/
        void 0
      );
      if (!expression) {
        continue;
      }
      startOnNewLine(expression);
      setOriginalNode(expression, property);
      addEmitFlags(expression, getEmitFlags(property) & 3072 /* NoComments */);
      setSourceMapRange(expression, moveRangePastModifiers(property));
      setCommentRange(expression, property);
      expressions.push(expression);
    }
    return expressions;
  }
  function transformProperty(property, receiver) {
    var _a;
    const savedCurrentClassElement ***REMOVED*** currentClassElement;
    const transformed ***REMOVED*** transformPropertyWorker(property, receiver);
    if (transformed && hasStaticModifier(property) && ((_a ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) ***REMOVED******REMOVED*** null ? void 0 : _a.facts)) {
      setOriginalNode(transformed, property);
      addEmitFlags(transformed, 4 /* AdviseOnEmitNode */);
      setSourceMapRange(transformed, getSourceMapRange(property.name));
      lexicalEnvironmentMap.set(getOriginalNode(property), lexicalEnvironment);
    }
    currentClassElement ***REMOVED*** savedCurrentClassElement;
    return transformed;
  }
  function transformPropertyWorker(property, receiver) {
    const emitAssignment ***REMOVED*** !useDefineForClassFields;
    if (isNamedEvaluation(property, isAnonymousClassNeedingAssignedName)) {
      property ***REMOVED*** transformNamedEvaluation(context, property);
    }
    const propertyName ***REMOVED*** hasAccessorModifier(property) ? factory2.getGeneratedPrivateNameForNode(property.name) : isComputedPropertyName(property.name) && !isSimpleInlineableExpression(property.name.expression) ? factory2.updateComputedPropertyName(property.name, factory2.getGeneratedNameForNode(property.name)) : property.name;
    if (hasStaticModifier(property)) {
      currentClassElement ***REMOVED*** property;
    }
    if (isPrivateIdentifier(propertyName) && shouldTransformClassElementToWeakMap(property)) {
      const privateIdentifierInfo ***REMOVED*** accessPrivateIdentifier2(propertyName);
      if (privateIdentifierInfo) {
        if (privateIdentifierInfo.kind ***REMOVED******REMOVED******REMOVED*** "f" /* Field */) {
          if (!privateIdentifierInfo.isStatic) {
            return createPrivateInstanceFieldInitializer(
              factory2,
              receiver,
              visitNode(property.initializer, visitor, isExpression),
              privateIdentifierInfo.brandCheckIdentifier
            );
          } else {
            return createPrivateStaticFieldInitializer(
              factory2,
              privateIdentifierInfo.variableName,
              visitNode(property.initializer, visitor, isExpression)
            );
          }
        } else {
          return void 0;
        }
      } else {
        Debug.fail("Undeclared private name for property declaration.");
      }
    }
    if ((isPrivateIdentifier(propertyName) || hasStaticModifier(property)) && !property.initializer) {
      return void 0;
    }
    const propertyOriginalNode ***REMOVED*** getOriginalNode(property);
    if (hasSyntacticModifier(propertyOriginalNode, 64 /* Abstract */)) {
      return void 0;
    }
    let initializer ***REMOVED*** visitNode(property.initializer, visitor, isExpression);
    if (isParameterPropertyDeclaration(propertyOriginalNode, propertyOriginalNode.parent) && isIdentifier(propertyName)) {
      const localName ***REMOVED*** factory2.cloneNode(propertyName);
      if (initializer) {
        if (isParenthesizedExpression(initializer) && isCommaExpression(initializer.expression) && isCallToHelper(initializer.expression.left, "___runInitializers") && isVoidExpression(initializer.expression.right) && isNumericLiteral(initializer.expression.right.expression)) {
          initializer ***REMOVED*** initializer.expression.left;
        }
        initializer ***REMOVED*** factory2.inlineExpressions([initializer, localName]);
      } else {
        initializer ***REMOVED*** localName;
      }
      setEmitFlags(propertyName, 3072 /* NoComments */ | 96 /* NoSourceMap */);
      setSourceMapRange(localName, propertyOriginalNode.name);
      setEmitFlags(localName, 3072 /* NoComments */);
    } else {
      initializer ?? (initializer ***REMOVED*** factory2.createVoidZero());
    }
    if (emitAssignment || isPrivateIdentifier(propertyName)) {
      const memberAccess ***REMOVED*** createMemberAccessForPropertyName(
        factory2,
        receiver,
        propertyName,
        /*location*/
        propertyName
      );
      addEmitFlags(memberAccess, 1024 /* NoLeadingComments */);
      const expression ***REMOVED*** factory2.createAssignment(memberAccess, initializer);
      return expression;
    } else {
      const name ***REMOVED*** isComputedPropertyName(propertyName) ? propertyName.expression : isIdentifier(propertyName) ? factory2.createStringLiteral(unescapeLeadingUnderscores(propertyName.escapedText)) : propertyName;
      const descriptor ***REMOVED*** factory2.createPropertyDescriptor({ value: initializer, configurable: true, writable: true, enumerable: true });
      return factory2.createObjectDefinePropertyCall(receiver, name, descriptor);
    }
  }
  function enableSubstitutionForClassAliases() {
    if ((enabledSubstitutions & 1 /* ClassAliases */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 1 /* ClassAliases */;
      context.enableSubstitution(80 /* Identifier */);
      classAliases ***REMOVED*** [];
    }
  }
  function enableSubstitutionForClassStaticThisOrSuperReference() {
    if ((enabledSubstitutions & 2 /* ClassStaticThisOrSuperReference */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 2 /* ClassStaticThisOrSuperReference */;
      context.enableSubstitution(110 /* ThisKeyword */);
      context.enableEmitNotification(263 /* FunctionDeclaration */);
      context.enableEmitNotification(219 /* FunctionExpression */);
      context.enableEmitNotification(177 /* Constructor */);
      context.enableEmitNotification(178 /* GetAccessor */);
      context.enableEmitNotification(179 /* SetAccessor */);
      context.enableEmitNotification(175 /* MethodDeclaration */);
      context.enableEmitNotification(173 /* PropertyDeclaration */);
      context.enableEmitNotification(168 /* ComputedPropertyName */);
    }
  }
  function addInstanceMethodStatements(statements, methods, receiver) {
    if (!shouldTransformPrivateElementsOrClassStaticBlocks || !some(methods)) {
      return;
    }
    const { weakSetName } ***REMOVED*** getPrivateIdentifierEnvironment().data;
    Debug.assert(weakSetName, "weakSetName should be set in private identifier environment");
    statements.push(
      factory2.createExpressionStatement(
        createPrivateInstanceMethodInitializer(factory2, receiver, weakSetName)
      )
    );
  }
  function visitInvalidSuperProperty(node) {
    return isPropertyAccessExpression(node) ? factory2.updatePropertyAccessExpression(
      node,
      factory2.createVoidZero(),
      node.name
    ) : factory2.updateElementAccessExpression(
      node,
      factory2.createVoidZero(),
      visitNode(node.argumentExpression, visitor, isExpression)
    );
  }
  function getPropertyNameExpressionIfNeeded(name, shouldHoist) {
    if (isComputedPropertyName(name)) {
      const cacheAssignment ***REMOVED*** findComputedPropertyNameCacheAssignment(name);
      const expression ***REMOVED*** visitNode(name.expression, visitor, isExpression);
      const innerExpression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
      const inlinable ***REMOVED*** isSimpleInlineableExpression(innerExpression);
      const alreadyTransformed ***REMOVED*** !!cacheAssignment || isAssignmentExpression(innerExpression) && isGeneratedIdentifier(innerExpression.left);
      if (!alreadyTransformed && !inlinable && shouldHoist) {
        const generatedName ***REMOVED*** factory2.getGeneratedNameForNode(name);
        if (resolver.hasNodeCheckFlag(name, 32768 /* BlockScopedBindingInLoop */)) {
          addBlockScopedVariable(generatedName);
        } else {
          hoistVariableDeclaration(generatedName);
        }
        return factory2.createAssignment(generatedName, expression);
      }
      return inlinable || isIdentifier(innerExpression) ? void 0 : expression;
    }
  }
  function startClassLexicalEnvironment() {
    lexicalEnvironment ***REMOVED*** { previous: lexicalEnvironment, data: void 0 };
  }
  function endClassLexicalEnvironment() {
    lexicalEnvironment ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.previous;
  }
  function getClassLexicalEnvironment() {
    Debug.assert(lexicalEnvironment);
    return lexicalEnvironment.data ?? (lexicalEnvironment.data ***REMOVED*** {
      facts: 0 /* None */,
      classConstructor: void 0,
      classThis: void 0,
      superClassReference: void 0
      // privateIdentifierEnvironment: undefined,
    });
  }
  function getPrivateIdentifierEnvironment() {
    Debug.assert(lexicalEnvironment);
    return lexicalEnvironment.privateEnv ?? (lexicalEnvironment.privateEnv ***REMOVED*** newPrivateEnvironment({
      className: void 0,
      weakSetName: void 0
    }));
  }
  function getPendingExpressions() {
    return pendingExpressions ?? (pendingExpressions ***REMOVED*** []);
  }
  function addPrivateIdentifierClassElementToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo) {
    if (isAutoAccessorPropertyDeclaration(node)) {
      addPrivateIdentifierAutoAccessorPropertyDeclarationToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
    } else if (isPropertyDeclaration(node)) {
      addPrivateIdentifierPropertyDeclarationToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
    } else if (isMethodDeclaration(node)) {
      addPrivateIdentifierMethodDeclarationToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
    } else if (isGetAccessorDeclaration(node)) {
      addPrivateIdentifierGetAccessorDeclarationToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
    } else if (isSetAccessorDeclaration(node)) {
      addPrivateIdentifierSetAccessorDeclarationToEnvironment(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
    }
  }
  function addPrivateIdentifierPropertyDeclarationToEnvironment(_node, name, lex, privateEnv, isStatic2, isValid, _previousInfo) {
    if (isStatic2) {
      const brandCheckIdentifier ***REMOVED*** Debug.checkDefined(lex.classThis ?? lex.classConstructor, "classConstructor should be set in private identifier environment");
      const variableName ***REMOVED*** createHoistedVariableForPrivateName(name);
      setPrivateIdentifier(privateEnv, name, {
        kind: "f" /* Field */,
        isStatic: true,
        brandCheckIdentifier,
        variableName,
        isValid
      });
    } else {
      const weakMapName ***REMOVED*** createHoistedVariableForPrivateName(name);
      setPrivateIdentifier(privateEnv, name, {
        kind: "f" /* Field */,
        isStatic: false,
        brandCheckIdentifier: weakMapName,
        isValid
      });
      getPendingExpressions().push(factory2.createAssignment(
        weakMapName,
        factory2.createNewExpression(
          factory2.createIdentifier("WeakMap"),
          /*typeArguments*/
          void 0,
          []
        )
      ));
    }
  }
  function addPrivateIdentifierMethodDeclarationToEnvironment(_node, name, lex, privateEnv, isStatic2, isValid, _previousInfo) {
    const methodName ***REMOVED*** createHoistedVariableForPrivateName(name);
    const brandCheckIdentifier ***REMOVED*** isStatic2 ? Debug.checkDefined(lex.classThis ?? lex.classConstructor, "classConstructor should be set in private identifier environment") : Debug.checkDefined(privateEnv.data.weakSetName, "weakSetName should be set in private identifier environment");
    setPrivateIdentifier(privateEnv, name, {
      kind: "m" /* Method */,
      methodName,
      brandCheckIdentifier,
      isStatic: isStatic2,
      isValid
    });
  }
  function addPrivateIdentifierGetAccessorDeclarationToEnvironment(_node, name, lex, privateEnv, isStatic2, isValid, previousInfo) {
    const getterName ***REMOVED*** createHoistedVariableForPrivateName(name, "_get");
    const brandCheckIdentifier ***REMOVED*** isStatic2 ? Debug.checkDefined(lex.classThis ?? lex.classConstructor, "classConstructor should be set in private identifier environment") : Debug.checkDefined(privateEnv.data.weakSetName, "weakSetName should be set in private identifier environment");
    if ((previousInfo ***REMOVED******REMOVED*** null ? void 0 : previousInfo.kind) ***REMOVED******REMOVED******REMOVED*** "a" /* Accessor */ && previousInfo.isStatic ***REMOVED******REMOVED******REMOVED*** isStatic2 && !previousInfo.getterName) {
      previousInfo.getterName ***REMOVED*** getterName;
    } else {
      setPrivateIdentifier(privateEnv, name, {
        kind: "a" /* Accessor */,
        getterName,
        setterName: void 0,
        brandCheckIdentifier,
        isStatic: isStatic2,
        isValid
      });
    }
  }
  function addPrivateIdentifierSetAccessorDeclarationToEnvironment(_node, name, lex, privateEnv, isStatic2, isValid, previousInfo) {
    const setterName ***REMOVED*** createHoistedVariableForPrivateName(name, "_set");
    const brandCheckIdentifier ***REMOVED*** isStatic2 ? Debug.checkDefined(lex.classThis ?? lex.classConstructor, "classConstructor should be set in private identifier environment") : Debug.checkDefined(privateEnv.data.weakSetName, "weakSetName should be set in private identifier environment");
    if ((previousInfo ***REMOVED******REMOVED*** null ? void 0 : previousInfo.kind) ***REMOVED******REMOVED******REMOVED*** "a" /* Accessor */ && previousInfo.isStatic ***REMOVED******REMOVED******REMOVED*** isStatic2 && !previousInfo.setterName) {
      previousInfo.setterName ***REMOVED*** setterName;
    } else {
      setPrivateIdentifier(privateEnv, name, {
        kind: "a" /* Accessor */,
        getterName: void 0,
        setterName,
        brandCheckIdentifier,
        isStatic: isStatic2,
        isValid
      });
    }
  }
  function addPrivateIdentifierAutoAccessorPropertyDeclarationToEnvironment(_node, name, lex, privateEnv, isStatic2, isValid, _previousInfo) {
    const getterName ***REMOVED*** createHoistedVariableForPrivateName(name, "_get");
    const setterName ***REMOVED*** createHoistedVariableForPrivateName(name, "_set");
    const brandCheckIdentifier ***REMOVED*** isStatic2 ? Debug.checkDefined(lex.classThis ?? lex.classConstructor, "classConstructor should be set in private identifier environment") : Debug.checkDefined(privateEnv.data.weakSetName, "weakSetName should be set in private identifier environment");
    setPrivateIdentifier(privateEnv, name, {
      kind: "a" /* Accessor */,
      getterName,
      setterName,
      brandCheckIdentifier,
      isStatic: isStatic2,
      isValid
    });
  }
  function addPrivateIdentifierToEnvironment(node, name, addDeclaration) {
    const lex ***REMOVED*** getClassLexicalEnvironment();
    const privateEnv ***REMOVED*** getPrivateIdentifierEnvironment();
    const previousInfo ***REMOVED*** getPrivateIdentifier(privateEnv, name);
    const isStatic2 ***REMOVED*** hasStaticModifier(node);
    const isValid ***REMOVED*** !isReservedPrivateName(name) && previousInfo ***REMOVED******REMOVED******REMOVED*** void 0;
    addDeclaration(node, name, lex, privateEnv, isStatic2, isValid, previousInfo);
  }
  function createHoistedVariableForClass(name, node, suffix) {
    const { className } ***REMOVED*** getPrivateIdentifierEnvironment().data;
    const prefix ***REMOVED*** className ? { prefix: "_", node: className, suffix: "_" } : "_";
    const identifier ***REMOVED*** typeof name ***REMOVED******REMOVED******REMOVED*** "object" ? factory2.getGeneratedNameForNode(name, 16 /* Optimistic */ | 8 /* ReservedInNestedScopes */, prefix, suffix) : typeof name ***REMOVED******REMOVED******REMOVED*** "string" ? factory2.createUniqueName(name, 16 /* Optimistic */, prefix, suffix) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0,
      /*reservedInNestedScopes*/
      true,
      prefix,
      suffix
    );
    if (resolver.hasNodeCheckFlag(node, 32768 /* BlockScopedBindingInLoop */)) {
      addBlockScopedVariable(identifier);
    } else {
      hoistVariableDeclaration(identifier);
    }
    return identifier;
  }
  function createHoistedVariableForPrivateName(name, suffix) {
    const text ***REMOVED*** tryGetTextOfPropertyName(name);
    return createHoistedVariableForClass((text ***REMOVED******REMOVED*** null ? void 0 : text.substring(1)) ?? name, name, suffix);
  }
  function accessPrivateIdentifier2(name) {
    const info ***REMOVED*** accessPrivateIdentifier(lexicalEnvironment, name);
    return (info ***REMOVED******REMOVED*** null ? void 0 : info.kind) ***REMOVED******REMOVED******REMOVED*** "untransformed" ? void 0 : info;
  }
  function wrapPrivateIdentifierForDestructuringTarget(node) {
    const parameter ***REMOVED*** factory2.getGeneratedNameForNode(node);
    const info ***REMOVED*** accessPrivateIdentifier2(node.name);
    if (!info) {
      return visitEachChild(node, visitor, context);
    }
    let receiver ***REMOVED*** node.expression;
    if (isThisProperty(node) || isSuperProperty(node) || !isSimpleCopiableExpression(node.expression)) {
      receiver ***REMOVED*** factory2.createTempVariable(
        hoistVariableDeclaration,
        /*reservedInNestedScopes*/
        true
      );
      getPendingExpressions().push(factory2.createBinaryExpression(receiver, 64 /* EqualsToken */, visitNode(node.expression, visitor, isExpression)));
    }
    return factory2.createAssignmentTargetWrapper(
      parameter,
      createPrivateIdentifierAssignment(
        info,
        receiver,
        parameter,
        64 /* EqualsToken */
      )
    );
  }
  function visitDestructuringAssignmentTarget(node) {
    if (isObjectLiteralExpression(node) || isArrayLiteralExpression(node)) {
      return visitAssignmentPattern(node);
    }
    if (isPrivateIdentifierPropertyAccessExpression(node)) {
      return wrapPrivateIdentifierForDestructuringTarget(node);
    } else if (shouldTransformSuperInStaticInitializers && currentClassElement && isSuperProperty(node) && isStaticPropertyDeclarationOrClassStaticBlock(currentClassElement) && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data)) {
      const { classConstructor, superClassReference, facts } ***REMOVED*** lexicalEnvironment.data;
      if (facts & 1 /* ClassWasDecorated */) {
        return visitInvalidSuperProperty(node);
      } else if (classConstructor && superClassReference) {
        const name ***REMOVED*** isElementAccessExpression(node) ? visitNode(node.argumentExpression, visitor, isExpression) : isIdentifier(node.name) ? factory2.createStringLiteralFromNode(node.name) : void 0;
        if (name) {
          const temp ***REMOVED*** factory2.createTempVariable(
            /*recordTempVariable*/
            void 0
          );
          return factory2.createAssignmentTargetWrapper(
            temp,
            factory2.createReflectSetCall(
              superClassReference,
              name,
              temp,
              classConstructor
            )
          );
        }
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentElement(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    if (isAssignmentExpression(
      node,
      /*excludeCompoundAssignment*/
      true
    )) {
      const left ***REMOVED*** visitDestructuringAssignmentTarget(node.left);
      const right ***REMOVED*** visitNode(node.right, visitor, isExpression);
      return factory2.updateBinaryExpression(node, left, node.operatorToken, right);
    }
    return visitDestructuringAssignmentTarget(node);
  }
  function visitAssignmentRestElement(node) {
    if (isLeftHandSideExpression(node.expression)) {
      const expression ***REMOVED*** visitDestructuringAssignmentTarget(node.expression);
      return factory2.updateSpreadElement(node, expression);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitArrayAssignmentElement(node) {
    if (isArrayBindingOrAssignmentElement(node)) {
      if (isSpreadElement(node)) return visitAssignmentRestElement(node);
      if (!isOmittedExpression(node)) return visitAssignmentElement(node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentProperty(node) {
    const name ***REMOVED*** visitNode(node.name, visitor, isPropertyName);
    if (isAssignmentExpression(
      node.initializer,
      /*excludeCompoundAssignment*/
      true
    )) {
      const assignmentElement ***REMOVED*** visitAssignmentElement(node.initializer);
      return factory2.updatePropertyAssignment(node, name, assignmentElement);
    }
    if (isLeftHandSideExpression(node.initializer)) {
      const assignmentElement ***REMOVED*** visitDestructuringAssignmentTarget(node.initializer);
      return factory2.updatePropertyAssignment(node, name, assignmentElement);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitShorthandAssignmentProperty(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentRestProperty(node) {
    if (isLeftHandSideExpression(node.expression)) {
      const expression ***REMOVED*** visitDestructuringAssignmentTarget(node.expression);
      return factory2.updateSpreadAssignment(node, expression);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitObjectAssignmentElement(node) {
    Debug.assertNode(node, isObjectBindingOrAssignmentElement);
    if (isSpreadAssignment(node)) return visitAssignmentRestProperty(node);
    if (isShorthandPropertyAssignment(node)) return visitShorthandAssignmentProperty(node);
    if (isPropertyAssignment(node)) return visitAssignmentProperty(node);
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentPattern(node) {
    if (isArrayLiteralExpression(node)) {
      return factory2.updateArrayLiteralExpression(
        node,
        visitNodes2(node.elements, visitArrayAssignmentElement, isExpression)
      );
    } else {
      return factory2.updateObjectLiteralExpression(
        node,
        visitNodes2(node.properties, visitObjectAssignmentElement, isObjectLiteralElementLike)
      );
    }
  }
  function onEmitNode(hint, node, emitCallback) {
    const original ***REMOVED*** getOriginalNode(node);
    const lex ***REMOVED*** lexicalEnvironmentMap.get(original);
    if (lex) {
      const savedLexicalEnvironment ***REMOVED*** lexicalEnvironment;
      const savedPreviousShouldSubstituteThisWithClassThis ***REMOVED*** previousShouldSubstituteThisWithClassThis;
      lexicalEnvironment ***REMOVED*** lex;
      previousShouldSubstituteThisWithClassThis ***REMOVED*** shouldSubstituteThisWithClassThis;
      shouldSubstituteThisWithClassThis ***REMOVED*** !isClassStaticBlockDeclaration(original) || !(getInternalEmitFlags(original) & 32 /* TransformPrivateStaticElements */);
      previousOnEmitNode(hint, node, emitCallback);
      shouldSubstituteThisWithClassThis ***REMOVED*** previousShouldSubstituteThisWithClassThis;
      previousShouldSubstituteThisWithClassThis ***REMOVED*** savedPreviousShouldSubstituteThisWithClassThis;
      lexicalEnvironment ***REMOVED*** savedLexicalEnvironment;
      return;
    }
    switch (node.kind) {
      case 219 /* FunctionExpression */:
        if (isArrowFunction(original) || getEmitFlags(node) & 524288 /* AsyncFunctionBody */) {
          break;
        }
      // falls through
      case 263 /* FunctionDeclaration */:
      case 177 /* Constructor */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 175 /* MethodDeclaration */:
      case 173 /* PropertyDeclaration */: {
        const savedLexicalEnvironment ***REMOVED*** lexicalEnvironment;
        const savedPreviousShouldSubstituteThisWithClassThis ***REMOVED*** previousShouldSubstituteThisWithClassThis;
        lexicalEnvironment ***REMOVED*** void 0;
        previousShouldSubstituteThisWithClassThis ***REMOVED*** shouldSubstituteThisWithClassThis;
        shouldSubstituteThisWithClassThis ***REMOVED*** false;
        previousOnEmitNode(hint, node, emitCallback);
        shouldSubstituteThisWithClassThis ***REMOVED*** previousShouldSubstituteThisWithClassThis;
        previousShouldSubstituteThisWithClassThis ***REMOVED*** savedPreviousShouldSubstituteThisWithClassThis;
        lexicalEnvironment ***REMOVED*** savedLexicalEnvironment;
        return;
      }
      case 168 /* ComputedPropertyName */: {
        const savedLexicalEnvironment ***REMOVED*** lexicalEnvironment;
        const savedShouldSubstituteThisWithClassThis ***REMOVED*** shouldSubstituteThisWithClassThis;
        lexicalEnvironment ***REMOVED*** lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.previous;
        shouldSubstituteThisWithClassThis ***REMOVED*** previousShouldSubstituteThisWithClassThis;
        previousOnEmitNode(hint, node, emitCallback);
        shouldSubstituteThisWithClassThis ***REMOVED*** savedShouldSubstituteThisWithClassThis;
        lexicalEnvironment ***REMOVED*** savedLexicalEnvironment;
        return;
      }
    }
    previousOnEmitNode(hint, node, emitCallback);
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
      case 110 /* ThisKeyword */:
        return substituteThisExpression(node);
    }
    return node;
  }
  function substituteThisExpression(node) {
    if (enabledSubstitutions & 2 /* ClassStaticThisOrSuperReference */ && (lexicalEnvironment ***REMOVED******REMOVED*** null ? void 0 : lexicalEnvironment.data) && !noSubstitution.has(node)) {
      const { facts, classConstructor, classThis } ***REMOVED*** lexicalEnvironment.data;
      const substituteThis ***REMOVED*** shouldSubstituteThisWithClassThis ? classThis ?? classConstructor : classConstructor;
      if (substituteThis) {
        return setTextRange(
          setOriginalNode(
            factory2.cloneNode(substituteThis),
            node
          ),
          node
        );
      }
      if (facts & 1 /* ClassWasDecorated */ && legacyDecorators) {
        return factory2.createParenthesizedExpression(factory2.createVoidZero());
      }
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    return trySubstituteClassAlias(node) || node;
  }
  function trySubstituteClassAlias(node) {
    if (enabledSubstitutions & 1 /* ClassAliases */) {
      if (resolver.hasNodeCheckFlag(node, 536870912 /* ConstructorReference */)) {
        const declaration ***REMOVED*** resolver.getReferencedValueDeclaration(node);
        if (declaration) {
          const classAlias ***REMOVED*** classAliases[declaration.id];
          if (classAlias) {
            const clone ***REMOVED*** factory2.cloneNode(classAlias);
            setSourceMapRange(clone, node);
            setCommentRange(clone, node);
            return clone;
          }
        }
      }
    }
    return void 0;
  }
}
function createPrivateStaticFieldInitializer(factory2, variableName, initializer) {
  return factory2.createAssignment(
    variableName,
    factory2.createObjectLiteralExpression([
      factory2.createPropertyAssignment("value", initializer || factory2.createVoidZero())
    ])
  );
}
function createPrivateInstanceFieldInitializer(factory2, receiver, initializer, weakMapName) {
  return factory2.createCallExpression(
    factory2.createPropertyAccessExpression(weakMapName, "set"),
    /*typeArguments*/
    void 0,
    [receiver, initializer || factory2.createVoidZero()]
  );
}
function createPrivateInstanceMethodInitializer(factory2, receiver, weakSetName) {
  return factory2.createCallExpression(
    factory2.createPropertyAccessExpression(weakSetName, "add"),
    /*typeArguments*/
    void 0,
    [receiver]
  );
}
function isReservedPrivateName(node) {
  return !isGeneratedPrivateIdentifier(node) && node.escapedText ***REMOVED******REMOVED******REMOVED*** "#constructor";
}
function isPrivateIdentifierInExpression(node) {
  return isPrivateIdentifier(node.left) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */;
}
function isStaticPropertyDeclaration2(node) {
  return isPropertyDeclaration(node) && hasStaticModifier(node);
}
function isStaticPropertyDeclarationOrClassStaticBlock(node) {
  return isClassStaticBlockDeclaration(node) || isStaticPropertyDeclaration2(node);
}

// src/compiler/transformers/typeSerializer.ts
function createRuntimeTypeSerializer(context) {
  const {
    factory: factory2,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const strictNullChecks ***REMOVED*** getStrictOptionValue(compilerOptions, "strictNullChecks");
  let currentLexicalScope;
  let currentNameScope;
  return {
    serializeTypeNode: (serializerContext, node) ***REMOVED***> setSerializerContextAnd(serializerContext, serializeTypeNode, node),
    serializeTypeOfNode: (serializerContext, node, container) ***REMOVED***> setSerializerContextAnd(serializerContext, serializeTypeOfNode, node, container),
    serializeParameterTypesOfNode: (serializerContext, node, container) ***REMOVED***> setSerializerContextAnd(serializerContext, serializeParameterTypesOfNode, node, container),
    serializeReturnTypeOfNode: (serializerContext, node) ***REMOVED***> setSerializerContextAnd(serializerContext, serializeReturnTypeOfNode, node)
  };
  function setSerializerContextAnd(serializerContext, cb, node, arg) {
    const savedCurrentLexicalScope ***REMOVED*** currentLexicalScope;
    const savedCurrentNameScope ***REMOVED*** currentNameScope;
    currentLexicalScope ***REMOVED*** serializerContext.currentLexicalScope;
    currentNameScope ***REMOVED*** serializerContext.currentNameScope;
    const result ***REMOVED*** arg ***REMOVED******REMOVED******REMOVED*** void 0 ? cb(node) : cb(node, arg);
    currentLexicalScope ***REMOVED*** savedCurrentLexicalScope;
    currentNameScope ***REMOVED*** savedCurrentNameScope;
    return result;
  }
  function getAccessorTypeNode(node, container) {
    const accessors ***REMOVED*** getAllAccessorDeclarations(container.members, node);
    return accessors.setAccessor && getSetAccessorTypeAnnotationNode(accessors.setAccessor) || accessors.getAccessor && getEffectiveReturnTypeNode(accessors.getAccessor);
  }
  function serializeTypeOfNode(node, container) {
    switch (node.kind) {
      case 173 /* PropertyDeclaration */:
      case 170 /* Parameter */:
        return serializeTypeNode(node.type);
      case 179 /* SetAccessor */:
      case 178 /* GetAccessor */:
        return serializeTypeNode(getAccessorTypeNode(node, container));
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
      case 175 /* MethodDeclaration */:
        return factory2.createIdentifier("Function");
      default:
        return factory2.createVoidZero();
    }
  }
  function serializeParameterTypesOfNode(node, container) {
    const valueDeclaration ***REMOVED*** isClassLike(node) ? getFirstConstructorWithBody(node) : isFunctionLike(node) && nodeIsPresent(node.body) ? node : void 0;
    const expressions ***REMOVED*** [];
    if (valueDeclaration) {
      const parameters ***REMOVED*** getParametersOfDecoratedDeclaration(valueDeclaration, container);
      const numParameters ***REMOVED*** parameters.length;
      for (let i ***REMOVED*** 0; i < numParameters; i++) {
        const parameter ***REMOVED*** parameters[i];
        if (i ***REMOVED******REMOVED******REMOVED*** 0 && isIdentifier(parameter.name) && parameter.name.escapedText ***REMOVED******REMOVED******REMOVED*** "this") {
          continue;
        }
        if (parameter.dotDotDotToken) {
          expressions.push(serializeTypeNode(getRestParameterElementType(parameter.type)));
        } else {
          expressions.push(serializeTypeOfNode(parameter, container));
        }
      }
    }
    return factory2.createArrayLiteralExpression(expressions);
  }
  function getParametersOfDecoratedDeclaration(node, container) {
    if (container && node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
      const { setAccessor } ***REMOVED*** getAllAccessorDeclarations(container.members, node);
      if (setAccessor) {
        return setAccessor.parameters;
      }
    }
    return node.parameters;
  }
  function serializeReturnTypeOfNode(node) {
    if (isFunctionLike(node) && node.type) {
      return serializeTypeNode(node.type);
    } else if (isAsyncFunction(node)) {
      return factory2.createIdentifier("Promise");
    }
    return factory2.createVoidZero();
  }
  function serializeTypeNode(node) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
      return factory2.createIdentifier("Object");
    }
    node ***REMOVED*** skipTypeParentheses(node);
    switch (node.kind) {
      case 116 /* VoidKeyword */:
      case 157 /* UndefinedKeyword */:
      case 146 /* NeverKeyword */:
        return factory2.createVoidZero();
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */:
        return factory2.createIdentifier("Function");
      case 189 /* ArrayType */:
      case 190 /* TupleType */:
        return factory2.createIdentifier("Array");
      case 183 /* TypePredicate */:
        return node.assertsModifier ? factory2.createVoidZero() : factory2.createIdentifier("Boolean");
      case 136 /* BooleanKeyword */:
        return factory2.createIdentifier("Boolean");
      case 204 /* TemplateLiteralType */:
      case 154 /* StringKeyword */:
        return factory2.createIdentifier("String");
      case 151 /* ObjectKeyword */:
        return factory2.createIdentifier("Object");
      case 202 /* LiteralType */:
        return serializeLiteralOfLiteralTypeNode(node.literal);
      case 150 /* NumberKeyword */:
        return factory2.createIdentifier("Number");
      case 163 /* BigIntKeyword */:
        return getGlobalConstructor("BigInt", 7 /* ES2020 */);
      case 155 /* SymbolKeyword */:
        return getGlobalConstructor("Symbol", 2 /* ES2015 */);
      case 184 /* TypeReference */:
        return serializeTypeReferenceNode(node);
      case 194 /* IntersectionType */:
        return serializeUnionOrIntersectionConstituents(
          node.types,
          /*isIntersection*/
          true
        );
      case 193 /* UnionType */:
        return serializeUnionOrIntersectionConstituents(
          node.types,
          /*isIntersection*/
          false
        );
      case 195 /* ConditionalType */:
        return serializeUnionOrIntersectionConstituents(
          [node.trueType, node.falseType],
          /*isIntersection*/
          false
        );
      case 199 /* TypeOperator */:
        if (node.operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
          return serializeTypeNode(node.type);
        }
        break;
      case 187 /* TypeQuery */:
      case 200 /* IndexedAccessType */:
      case 201 /* MappedType */:
      case 188 /* TypeLiteral */:
      case 133 /* AnyKeyword */:
      case 159 /* UnknownKeyword */:
      case 198 /* ThisType */:
      case 206 /* ImportType */:
        break;
      // handle JSDoc types from an invalid parse
      case 313 /* JSDocAllType */:
      case 314 /* JSDocUnknownType */:
      case 318 /* JSDocFunctionType */:
      case 319 /* JSDocVariadicType */:
      case 320 /* JSDocNamepathType */:
        break;
      case 315 /* JSDocNullableType */:
      case 316 /* JSDocNonNullableType */:
      case 317 /* JSDocOptionalType */:
        return serializeTypeNode(node.type);
      default:
        return Debug.failBadSyntaxKind(node);
    }
    return factory2.createIdentifier("Object");
  }
  function serializeLiteralOfLiteralTypeNode(node) {
    switch (node.kind) {
      case 11 /* StringLiteral */:
      case 15 /* NoSubstitutionTemplateLiteral */:
        return factory2.createIdentifier("String");
      case 225 /* PrefixUnaryExpression */: {
        const operand ***REMOVED*** node.operand;
        switch (operand.kind) {
          case 9 /* NumericLiteral */:
          case 10 /* BigIntLiteral */:
            return serializeLiteralOfLiteralTypeNode(operand);
          default:
            return Debug.failBadSyntaxKind(operand);
        }
      }
      case 9 /* NumericLiteral */:
        return factory2.createIdentifier("Number");
      case 10 /* BigIntLiteral */:
        return getGlobalConstructor("BigInt", 7 /* ES2020 */);
      case 112 /* TrueKeyword */:
      case 97 /* FalseKeyword */:
        return factory2.createIdentifier("Boolean");
      case 106 /* NullKeyword */:
        return factory2.createVoidZero();
      default:
        return Debug.failBadSyntaxKind(node);
    }
  }
  function serializeUnionOrIntersectionConstituents(types, isIntersection) {
    let serializedType;
    for (let typeNode of types) {
      typeNode ***REMOVED*** skipTypeParentheses(typeNode);
      if (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 146 /* NeverKeyword */) {
        if (isIntersection) return factory2.createVoidZero();
        continue;
      }
      if (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 159 /* UnknownKeyword */) {
        if (!isIntersection) return factory2.createIdentifier("Object");
        continue;
      }
      if (typeNode.kind ***REMOVED******REMOVED******REMOVED*** 133 /* AnyKeyword */) {
        return factory2.createIdentifier("Object");
      }
      if (!strictNullChecks && (isLiteralTypeNode(typeNode) && typeNode.literal.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */ || typeNode.kind ***REMOVED******REMOVED******REMOVED*** 157 /* UndefinedKeyword */)) {
        continue;
      }
      const serializedConstituent ***REMOVED*** serializeTypeNode(typeNode);
      if (isIdentifier(serializedConstituent) && serializedConstituent.escapedText ***REMOVED******REMOVED******REMOVED*** "Object") {
        return serializedConstituent;
      }
      if (serializedType) {
        if (!equateSerializedTypeNodes(serializedType, serializedConstituent)) {
          return factory2.createIdentifier("Object");
        }
      } else {
        serializedType ***REMOVED*** serializedConstituent;
      }
    }
    return serializedType ?? factory2.createVoidZero();
  }
  function equateSerializedTypeNodes(left, right) {
    return (
      // temp vars used in fallback
      isGeneratedIdentifier(left) ? isGeneratedIdentifier(right) : (
        // entity names
        isIdentifier(left) ? isIdentifier(right) && left.escapedText ***REMOVED******REMOVED******REMOVED*** right.escapedText : isPropertyAccessExpression(left) ? isPropertyAccessExpression(right) && equateSerializedTypeNodes(left.expression, right.expression) && equateSerializedTypeNodes(left.name, right.name) : (
          // `void 0`
          isVoidExpression(left) ? isVoidExpression(right) && isNumericLiteral(left.expression) && left.expression.text ***REMOVED******REMOVED******REMOVED*** "0" && isNumericLiteral(right.expression) && right.expression.text ***REMOVED******REMOVED******REMOVED*** "0" : (
            // `"undefined"` or `"function"` in `typeof` checks
            isStringLiteral(left) ? isStringLiteral(right) && left.text ***REMOVED******REMOVED******REMOVED*** right.text : (
              // used in `typeof` checks for fallback
              isTypeOfExpression(left) ? isTypeOfExpression(right) && equateSerializedTypeNodes(left.expression, right.expression) : (
                // parens in `typeof` checks with temps
                isParenthesizedExpression(left) ? isParenthesizedExpression(right) && equateSerializedTypeNodes(left.expression, right.expression) : (
                  // conditionals used in fallback
                  isConditionalExpression(left) ? isConditionalExpression(right) && equateSerializedTypeNodes(left.condition, right.condition) && equateSerializedTypeNodes(left.whenTrue, right.whenTrue) && equateSerializedTypeNodes(left.whenFalse, right.whenFalse) : (
                    // logical binary and assignments used in fallback
                    isBinaryExpression(left) ? isBinaryExpression(right) && left.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** right.operatorToken.kind && equateSerializedTypeNodes(left.left, right.left) && equateSerializedTypeNodes(left.right, right.right) : false
                  )
                )
              )
            )
          )
        )
      )
    );
  }
  function serializeTypeReferenceNode(node) {
    const kind ***REMOVED*** resolver.getTypeReferenceSerializationKind(node.typeName, currentNameScope ?? currentLexicalScope);
    switch (kind) {
      case 0 /* Unknown */:
        if (findAncestor(node, (n) ***REMOVED***> n.parent && isConditionalTypeNode(n.parent) && (n.parent.trueType ***REMOVED******REMOVED******REMOVED*** n || n.parent.falseType ***REMOVED******REMOVED******REMOVED*** n))) {
          return factory2.createIdentifier("Object");
        }
        const serialized ***REMOVED*** serializeEntityNameAsExpressionFallback(node.typeName);
        const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
        return factory2.createConditionalExpression(
          factory2.createTypeCheck(factory2.createAssignment(temp, serialized), "function"),
          /*questionToken*/
          void 0,
          temp,
          /*colonToken*/
          void 0,
          factory2.createIdentifier("Object")
        );
      case 1 /* TypeWithConstructSignatureAndValue */:
        return serializeEntityNameAsExpression(node.typeName);
      case 2 /* VoidNullableOrNeverType */:
        return factory2.createVoidZero();
      case 4 /* BigIntLikeType */:
        return getGlobalConstructor("BigInt", 7 /* ES2020 */);
      case 6 /* BooleanType */:
        return factory2.createIdentifier("Boolean");
      case 3 /* NumberLikeType */:
        return factory2.createIdentifier("Number");
      case 5 /* StringLikeType */:
        return factory2.createIdentifier("String");
      case 7 /* ArrayLikeType */:
        return factory2.createIdentifier("Array");
      case 8 /* ESSymbolType */:
        return getGlobalConstructor("Symbol", 2 /* ES2015 */);
      case 10 /* TypeWithCallSignature */:
        return factory2.createIdentifier("Function");
      case 9 /* Promise */:
        return factory2.createIdentifier("Promise");
      case 11 /* ObjectType */:
        return factory2.createIdentifier("Object");
      default:
        return Debug.assertNever(kind);
    }
  }
  function createCheckedValue(left, right) {
    return factory2.createLogicalAnd(
      factory2.createStrictInequality(factory2.createTypeOfExpression(left), factory2.createStringLiteral("undefined")),
      right
    );
  }
  function serializeEntityNameAsExpressionFallback(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      const copied ***REMOVED*** serializeEntityNameAsExpression(node);
      return createCheckedValue(copied, copied);
    }
    if (node.left.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return createCheckedValue(serializeEntityNameAsExpression(node.left), serializeEntityNameAsExpression(node));
    }
    const left ***REMOVED*** serializeEntityNameAsExpressionFallback(node.left);
    const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    return factory2.createLogicalAnd(
      factory2.createLogicalAnd(
        left.left,
        factory2.createStrictInequality(factory2.createAssignment(temp, left.right), factory2.createVoidZero())
      ),
      factory2.createPropertyAccessExpression(temp, node.right)
    );
  }
  function serializeEntityNameAsExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        const name ***REMOVED*** setParent(setTextRange(parseNodeFactory.cloneNode(node), node), node.parent);
        name.original ***REMOVED*** void 0;
        setParent(name, getParseTreeNode(currentLexicalScope));
        return name;
      case 167 /* QualifiedName */:
        return serializeQualifiedNameAsExpression(node);
    }
  }
  function serializeQualifiedNameAsExpression(node) {
    return factory2.createPropertyAccessExpression(serializeEntityNameAsExpression(node.left), node.right);
  }
  function getGlobalConstructorWithFallback(name) {
    return factory2.createConditionalExpression(
      factory2.createTypeCheck(factory2.createIdentifier(name), "function"),
      /*questionToken*/
      void 0,
      factory2.createIdentifier(name),
      /*colonToken*/
      void 0,
      factory2.createIdentifier("Object")
    );
  }
  function getGlobalConstructor(name, minLanguageVersion) {
    return languageVersion < minLanguageVersion ? getGlobalConstructorWithFallback(name) : factory2.createIdentifier(name);
  }
}

// src/compiler/transformers/legacyDecorators.ts
function transformLegacyDecorators(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  let classAliases;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    return visited;
  }
  function modifierVisitor(node) {
    return isDecorator(node) ? void 0 : node;
  }
  function visitor(node) {
    if (!(node.transformFlags & 33554432 /* ContainsDecorators */)) {
      return node;
    }
    switch (node.kind) {
      case 171 /* Decorator */:
        return void 0;
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 232 /* ClassExpression */:
        return visitClassExpression(node);
      case 177 /* Constructor */:
        return visitConstructorDeclaration(node);
      case 175 /* MethodDeclaration */:
        return visitMethodDeclaration(node);
      case 179 /* SetAccessor */:
        return visitSetAccessorDeclaration(node);
      case 178 /* GetAccessor */:
        return visitGetAccessorDeclaration(node);
      case 173 /* PropertyDeclaration */:
        return visitPropertyDeclaration(node);
      case 170 /* Parameter */:
        return visitParameterDeclaration(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitClassDeclaration(node) {
    if (!(classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      true,
      node
    ) || childIsDecorated(
      /*useLegacyDecorators*/
      true,
      node
    ))) {
      return visitEachChild(node, visitor, context);
    }
    const statements ***REMOVED*** classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      true,
      node
    ) ? transformClassDeclarationWithClassDecorators(node, node.name) : transformClassDeclarationWithoutClassDecorators(node, node.name);
    return singleOrMany(statements);
  }
  function decoratorContainsPrivateIdentifierInExpression(decorator) {
    return !!(decorator.transformFlags & 536870912 /* ContainsPrivateIdentifierInExpression */);
  }
  function parameterDecoratorsContainPrivateIdentifierInExpression(parameterDecorators) {
    return some(parameterDecorators, decoratorContainsPrivateIdentifierInExpression);
  }
  function hasClassElementWithDecoratorContainingPrivateIdentifierInExpression(node) {
    for (const member of node.members) {
      if (!canHaveDecorators(member)) continue;
      const allDecorators ***REMOVED*** getAllDecoratorsOfClassElement(
        member,
        node,
        /*useLegacyDecorators*/
        true
      );
      if (some(allDecorators ***REMOVED******REMOVED*** null ? void 0 : allDecorators.decorators, decoratorContainsPrivateIdentifierInExpression)) return true;
      if (some(allDecorators ***REMOVED******REMOVED*** null ? void 0 : allDecorators.parameters, parameterDecoratorsContainPrivateIdentifierInExpression)) return true;
    }
    return false;
  }
  function transformDecoratorsOfClassElements(node, members) {
    let decorationStatements ***REMOVED*** [];
    addClassElementDecorationStatements(
      decorationStatements,
      node,
      /*isStatic*/
      false
    );
    addClassElementDecorationStatements(
      decorationStatements,
      node,
      /*isStatic*/
      true
    );
    if (hasClassElementWithDecoratorContainingPrivateIdentifierInExpression(node)) {
      members ***REMOVED*** setTextRange(
        factory2.createNodeArray([
          ...members,
          factory2.createClassStaticBlockDeclaration(
            factory2.createBlock(
              decorationStatements,
              /*multiLine*/
              true
            )
          )
        ]),
        members
      );
      decorationStatements ***REMOVED*** void 0;
    }
    return { decorationStatements, members };
  }
  function transformClassDeclarationWithoutClassDecorators(node, name) {
    const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
    const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, visitor, isHeritageClause);
    let members ***REMOVED*** visitNodes2(node.members, visitor, isClassElement);
    let decorationStatements ***REMOVED*** [];
    ({ members, decorationStatements } ***REMOVED*** transformDecoratorsOfClassElements(node, members));
    const updated ***REMOVED*** factory2.updateClassDeclaration(
      node,
      modifiers,
      name,
      /*typeParameters*/
      void 0,
      heritageClauses,
      members
    );
    return addRange([updated], decorationStatements);
  }
  function transformClassDeclarationWithClassDecorators(node, name) {
    const isExport ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
    const isDefault ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */);
    const modifiers ***REMOVED*** visitNodes2(node.modifiers, (node2) ***REMOVED***> isExportOrDefaultModifier(node2) || isDecorator(node2) ? void 0 : node2, isModifierLike);
    const location ***REMOVED*** moveRangePastModifiers(node);
    const classAlias ***REMOVED*** getClassAliasIfNeeded(node);
    const declName ***REMOVED*** languageVersion < 2 /* ES2015 */ ? factory2.getInternalName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    ) : factory2.getLocalName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    );
    const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, visitor, isHeritageClause);
    let members ***REMOVED*** visitNodes2(node.members, visitor, isClassElement);
    let decorationStatements ***REMOVED*** [];
    ({ members, decorationStatements } ***REMOVED*** transformDecoratorsOfClassElements(node, members));
    const assignClassAliasInStaticBlock ***REMOVED*** languageVersion >***REMOVED*** 9 /* ES2022 */ && !!classAlias && some(members, (member) ***REMOVED***> isPropertyDeclaration(member) && hasSyntacticModifier(member, 256 /* Static */) || isClassStaticBlockDeclaration(member));
    if (assignClassAliasInStaticBlock) {
      members ***REMOVED*** setTextRange(
        factory2.createNodeArray([
          factory2.createClassStaticBlockDeclaration(
            factory2.createBlock([
              factory2.createExpressionStatement(
                factory2.createAssignment(classAlias, factory2.createThis())
              )
            ])
          ),
          ...members
        ]),
        members
      );
    }
    const classExpression ***REMOVED*** factory2.createClassExpression(
      modifiers,
      name && isGeneratedIdentifier(name) ? void 0 : name,
      /*typeParameters*/
      void 0,
      heritageClauses,
      members
    );
    setOriginalNode(classExpression, node);
    setTextRange(classExpression, location);
    const varInitializer ***REMOVED*** classAlias && !assignClassAliasInStaticBlock ? factory2.createAssignment(classAlias, classExpression) : classExpression;
    const varDecl ***REMOVED*** factory2.createVariableDeclaration(
      declName,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      varInitializer
    );
    setOriginalNode(varDecl, node);
    const varDeclList ***REMOVED*** factory2.createVariableDeclarationList([varDecl], 1 /* Let */);
    const varStatement ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      varDeclList
    );
    setOriginalNode(varStatement, node);
    setTextRange(varStatement, location);
    setCommentRange(varStatement, node);
    const statements ***REMOVED*** [varStatement];
    addRange(statements, decorationStatements);
    addConstructorDecorationStatement(statements, node);
    if (isExport) {
      if (isDefault) {
        const exportStatement ***REMOVED*** factory2.createExportDefault(declName);
        statements.push(exportStatement);
      } else {
        const exportStatement ***REMOVED*** factory2.createExternalModuleExport(factory2.getDeclarationName(node));
        statements.push(exportStatement);
      }
    }
    return statements;
  }
  function visitClassExpression(node) {
    return factory2.updateClassExpression(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      node.name,
      /*typeParameters*/
      void 0,
      visitNodes2(node.heritageClauses, visitor, isHeritageClause),
      visitNodes2(node.members, visitor, isClassElement)
    );
  }
  function visitConstructorDeclaration(node) {
    return factory2.updateConstructorDeclaration(
      node,
      visitNodes2(node.modifiers, modifierVisitor, isModifier),
      visitNodes2(node.parameters, visitor, isParameter),
      visitNode(node.body, visitor, isBlock)
    );
  }
  function finishClassElement(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      setCommentRange(updated, original);
      setSourceMapRange(updated, moveRangePastModifiers(original));
    }
    return updated;
  }
  function visitMethodDeclaration(node) {
    return finishClassElement(
      factory2.updateMethodDeclaration(
        node,
        visitNodes2(node.modifiers, modifierVisitor, isModifier),
        node.asteriskToken,
        Debug.checkDefined(visitNode(node.name, visitor, isPropertyName)),
        /*questionToken*/
        void 0,
        /*typeParameters*/
        void 0,
        visitNodes2(node.parameters, visitor, isParameter),
        /*type*/
        void 0,
        visitNode(node.body, visitor, isBlock)
      ),
      node
    );
  }
  function visitGetAccessorDeclaration(node) {
    return finishClassElement(
      factory2.updateGetAccessorDeclaration(
        node,
        visitNodes2(node.modifiers, modifierVisitor, isModifier),
        Debug.checkDefined(visitNode(node.name, visitor, isPropertyName)),
        visitNodes2(node.parameters, visitor, isParameter),
        /*type*/
        void 0,
        visitNode(node.body, visitor, isBlock)
      ),
      node
    );
  }
  function visitSetAccessorDeclaration(node) {
    return finishClassElement(
      factory2.updateSetAccessorDeclaration(
        node,
        visitNodes2(node.modifiers, modifierVisitor, isModifier),
        Debug.checkDefined(visitNode(node.name, visitor, isPropertyName)),
        visitNodes2(node.parameters, visitor, isParameter),
        visitNode(node.body, visitor, isBlock)
      ),
      node
    );
  }
  function visitPropertyDeclaration(node) {
    if (node.flags & 33554432 /* Ambient */ || hasSyntacticModifier(node, 128 /* Ambient */)) {
      return void 0;
    }
    return finishClassElement(
      factory2.updatePropertyDeclaration(
        node,
        visitNodes2(node.modifiers, modifierVisitor, isModifier),
        Debug.checkDefined(visitNode(node.name, visitor, isPropertyName)),
        /*questionOrExclamationToken*/
        void 0,
        /*type*/
        void 0,
        visitNode(node.initializer, visitor, isExpression)
      ),
      node
    );
  }
  function visitParameterDeclaration(node) {
    const updated ***REMOVED*** factory2.updateParameterDeclaration(
      node,
      elideNodes(factory2, node.modifiers),
      node.dotDotDotToken,
      Debug.checkDefined(visitNode(node.name, visitor, isBindingName)),
      /*questionToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
    if (updated !***REMOVED******REMOVED*** node) {
      setCommentRange(updated, node);
      setTextRange(updated, moveRangePastModifiers(node));
      setSourceMapRange(updated, moveRangePastModifiers(node));
      setEmitFlags(updated.name, 64 /* NoTrailingSourceMap */);
    }
    return updated;
  }
  function isSyntheticMetadataDecorator(node) {
    return isCallToHelper(node.expression, "___metadata");
  }
  function transformAllDecoratorsOfDeclaration(allDecorators) {
    if (!allDecorators) {
      return void 0;
    }
    const { false: decorators, true: metadata } ***REMOVED*** groupBy(allDecorators.decorators, isSyntheticMetadataDecorator);
    const decoratorExpressions ***REMOVED*** [];
    addRange(decoratorExpressions, map(decorators, transformDecorator));
    addRange(decoratorExpressions, flatMap(allDecorators.parameters, transformDecoratorsOfParameter));
    addRange(decoratorExpressions, map(metadata, transformDecorator));
    return decoratorExpressions;
  }
  function addClassElementDecorationStatements(statements, node, isStatic2) {
    addRange(statements, map(generateClassElementDecorationExpressions(node, isStatic2), (expr) ***REMOVED***> factory2.createExpressionStatement(expr)));
  }
  function isDecoratedClassElement(member, isStaticElement, parent) {
    return nodeOrChildIsDecorated(
      /*useLegacyDecorators*/
      true,
      member,
      parent
    ) && isStaticElement ***REMOVED******REMOVED******REMOVED*** isStatic(member);
  }
  function getDecoratedClassElements(node, isStatic2) {
    return filter(node.members, (m) ***REMOVED***> isDecoratedClassElement(m, isStatic2, node));
  }
  function generateClassElementDecorationExpressions(node, isStatic2) {
    const members ***REMOVED*** getDecoratedClassElements(node, isStatic2);
    let expressions;
    for (const member of members) {
      expressions ***REMOVED*** append(expressions, generateClassElementDecorationExpression(node, member));
    }
    return expressions;
  }
  function generateClassElementDecorationExpression(node, member) {
    const allDecorators ***REMOVED*** getAllDecoratorsOfClassElement(
      member,
      node,
      /*useLegacyDecorators*/
      true
    );
    const decoratorExpressions ***REMOVED*** transformAllDecoratorsOfDeclaration(allDecorators);
    if (!decoratorExpressions) {
      return void 0;
    }
    const prefix ***REMOVED*** getClassMemberPrefix(node, member);
    const memberName ***REMOVED*** getExpressionForPropertyName(
      member,
      /*generateNameForComputedPropertyName*/
      !hasSyntacticModifier(member, 128 /* Ambient */)
    );
    const descriptor ***REMOVED*** isPropertyDeclaration(member) && !hasAccessorModifier(member) ? factory2.createVoidZero() : factory2.createNull();
    const helper ***REMOVED*** emitHelpers().createDecorateHelper(
      decoratorExpressions,
      prefix,
      memberName,
      descriptor
    );
    setEmitFlags(helper, 3072 /* NoComments */);
    setSourceMapRange(helper, moveRangePastModifiers(member));
    return helper;
  }
  function addConstructorDecorationStatement(statements, node) {
    const expression ***REMOVED*** generateConstructorDecorationExpression(node);
    if (expression) {
      statements.push(setOriginalNode(factory2.createExpressionStatement(expression), node));
    }
  }
  function generateConstructorDecorationExpression(node) {
    const allDecorators ***REMOVED*** getAllDecoratorsOfClass(
      node,
      /*useLegacyDecorators*/
      true
    );
    const decoratorExpressions ***REMOVED*** transformAllDecoratorsOfDeclaration(allDecorators);
    if (!decoratorExpressions) {
      return void 0;
    }
    const classAlias ***REMOVED*** classAliases && classAliases[getOriginalNodeId(node)];
    const localName ***REMOVED*** languageVersion < 2 /* ES2015 */ ? factory2.getInternalName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    ) : factory2.getDeclarationName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      true
    );
    const decorate ***REMOVED*** emitHelpers().createDecorateHelper(decoratorExpressions, localName);
    const expression ***REMOVED*** factory2.createAssignment(localName, classAlias ? factory2.createAssignment(classAlias, decorate) : decorate);
    setEmitFlags(expression, 3072 /* NoComments */);
    setSourceMapRange(expression, moveRangePastModifiers(node));
    return expression;
  }
  function transformDecorator(decorator) {
    return Debug.checkDefined(visitNode(decorator.expression, visitor, isExpression));
  }
  function transformDecoratorsOfParameter(decorators, parameterOffset) {
    let expressions;
    if (decorators) {
      expressions ***REMOVED*** [];
      for (const decorator of decorators) {
        const helper ***REMOVED*** emitHelpers().createParamHelper(
          transformDecorator(decorator),
          parameterOffset
        );
        setTextRange(helper, decorator.expression);
        setEmitFlags(helper, 3072 /* NoComments */);
        expressions.push(helper);
      }
    }
    return expressions;
  }
  function getExpressionForPropertyName(member, generateNameForComputedPropertyName) {
    const name ***REMOVED*** member.name;
    if (isPrivateIdentifier(name)) {
      return factory2.createIdentifier("");
    } else if (isComputedPropertyName(name)) {
      return generateNameForComputedPropertyName && !isSimpleInlineableExpression(name.expression) ? factory2.getGeneratedNameForNode(name) : name.expression;
    } else if (isIdentifier(name)) {
      return factory2.createStringLiteral(idText(name));
    } else {
      return factory2.cloneNode(name);
    }
  }
  function enableSubstitutionForClassAliases() {
    if (!classAliases) {
      context.enableSubstitution(80 /* Identifier */);
      classAliases ***REMOVED*** [];
    }
  }
  function getClassAliasIfNeeded(node) {
    if (resolver.hasNodeCheckFlag(node, 262144 /* ContainsConstructorReference */)) {
      enableSubstitutionForClassAliases();
      const classAlias ***REMOVED*** factory2.createUniqueName(node.name && !isGeneratedIdentifier(node.name) ? idText(node.name) : "default");
      classAliases[getOriginalNodeId(node)] ***REMOVED*** classAlias;
      hoistVariableDeclaration(classAlias);
      return classAlias;
    }
  }
  function getClassPrototype(node) {
    return factory2.createPropertyAccessExpression(factory2.getDeclarationName(node), "prototype");
  }
  function getClassMemberPrefix(node, member) {
    return isStatic(member) ? factory2.getDeclarationName(node) : getClassPrototype(node);
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    return trySubstituteClassAlias(node) ?? node;
  }
  function trySubstituteClassAlias(node) {
    if (classAliases) {
      if (resolver.hasNodeCheckFlag(node, 536870912 /* ConstructorReference */)) {
        const declaration ***REMOVED*** resolver.getReferencedValueDeclaration(node);
        if (declaration) {
          const classAlias ***REMOVED*** classAliases[declaration.id];
          if (classAlias) {
            const clone ***REMOVED*** factory2.cloneNode(classAlias);
            setSourceMapRange(clone, node);
            setCommentRange(clone, node);
            return clone;
          }
        }
      }
    }
    return void 0;
  }
}

// src/compiler/transformers/esDecorators.ts
function transformESDecorators(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    startLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const languageVersion ***REMOVED*** getEmitScriptTarget(context.getCompilerOptions());
  let top;
  let classInfo;
  let classThis;
  let classSuper;
  let pendingExpressions;
  let shouldTransformPrivateStaticElementsInFile;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    top ***REMOVED*** void 0;
    shouldTransformPrivateStaticElementsInFile ***REMOVED*** false;
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    if (shouldTransformPrivateStaticElementsInFile) {
      addInternalEmitFlags(visited, 32 /* TransformPrivateStaticElements */);
      shouldTransformPrivateStaticElementsInFile ***REMOVED*** false;
    }
    return visited;
  }
  function updateState() {
    classInfo ***REMOVED*** void 0;
    classThis ***REMOVED*** void 0;
    classSuper ***REMOVED*** void 0;
    switch (top ***REMOVED******REMOVED*** null ? void 0 : top.kind) {
      case "class":
        classInfo ***REMOVED*** top.classInfo;
        break;
      case "class-element":
        classInfo ***REMOVED*** top.next.classInfo;
        classThis ***REMOVED*** top.classThis;
        classSuper ***REMOVED*** top.classSuper;
        break;
      case "name":
        const grandparent ***REMOVED*** top.next.next.next;
        if ((grandparent ***REMOVED******REMOVED*** null ? void 0 : grandparent.kind) ***REMOVED******REMOVED******REMOVED*** "class-element") {
          classInfo ***REMOVED*** grandparent.next.classInfo;
          classThis ***REMOVED*** grandparent.classThis;
          classSuper ***REMOVED*** grandparent.classSuper;
        }
        break;
    }
  }
  function enterClass(classInfo2) {
    top ***REMOVED*** { kind: "class", next: top, classInfo: classInfo2, savedPendingExpressions: pendingExpressions };
    pendingExpressions ***REMOVED*** void 0;
    updateState();
  }
  function exitClass() {
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "class", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'class' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    pendingExpressions ***REMOVED*** top.savedPendingExpressions;
    top ***REMOVED*** top.next;
    updateState();
  }
  function enterClassElement(node) {
    var _a, _b;
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "class", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'class' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    top ***REMOVED*** { kind: "class-element", next: top };
    if (isClassStaticBlockDeclaration(node) || isPropertyDeclaration(node) && hasStaticModifier(node)) {
      top.classThis ***REMOVED*** (_a ***REMOVED*** top.next.classInfo) ***REMOVED******REMOVED*** null ? void 0 : _a.classThis;
      top.classSuper ***REMOVED*** (_b ***REMOVED*** top.next.classInfo) ***REMOVED******REMOVED*** null ? void 0 : _b.classSuper;
    }
    updateState();
  }
  function exitClassElement() {
    var _a;
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "class-element", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'class-element' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    Debug.assert(((_a ***REMOVED*** top.next) ***REMOVED******REMOVED*** null ? void 0 : _a.kind) ***REMOVED******REMOVED******REMOVED*** "class", "Incorrect value for top.next.kind.", () ***REMOVED***> {
      var _a2;
      return `Expected top.next.kind to be 'class' but got '${(_a2 ***REMOVED*** top.next) ***REMOVED******REMOVED*** null ? void 0 : _a2.kind}' instead.`;
    });
    top ***REMOVED*** top.next;
    updateState();
  }
  function enterName() {
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "class-element", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'class-element' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    top ***REMOVED*** { kind: "name", next: top };
    updateState();
  }
  function exitName() {
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "name", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'name' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    top ***REMOVED*** top.next;
    updateState();
  }
  function enterOther() {
    if ((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "other") {
      Debug.assert(!pendingExpressions);
      top.depth++;
    } else {
      top ***REMOVED*** { kind: "other", next: top, depth: 0, savedPendingExpressions: pendingExpressions };
      pendingExpressions ***REMOVED*** void 0;
      updateState();
    }
  }
  function exitOther() {
    Debug.assert((top ***REMOVED******REMOVED*** null ? void 0 : top.kind) ***REMOVED******REMOVED******REMOVED*** "other", "Incorrect value for top.kind.", () ***REMOVED***> `Expected top.kind to be 'other' but got '${top ***REMOVED******REMOVED*** null ? void 0 : top.kind}' instead.`);
    if (top.depth > 0) {
      Debug.assert(!pendingExpressions);
      top.depth--;
    } else {
      pendingExpressions ***REMOVED*** top.savedPendingExpressions;
      top ***REMOVED*** top.next;
      updateState();
    }
  }
  function shouldVisitNode(node) {
    return !!(node.transformFlags & 33554432 /* ContainsDecorators */) || !!classThis && !!(node.transformFlags & 16384 /* ContainsLexicalThis */) || !!classThis && !!classSuper && !!(node.transformFlags & 134217728 /* ContainsLexicalSuper */);
  }
  function visitor(node) {
    if (!shouldVisitNode(node)) {
      return node;
    }
    switch (node.kind) {
      case 171 /* Decorator */:
        return Debug.fail("Use `modifierVisitor` instead.");
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 232 /* ClassExpression */:
        return visitClassExpression(node);
      case 177 /* Constructor */:
      case 173 /* PropertyDeclaration */:
      case 176 /* ClassStaticBlockDeclaration */:
        return Debug.fail("Not supported outside of a class. Use 'classElementVisitor' instead.");
      case 170 /* Parameter */:
        return visitParameterDeclaration(node);
      // Support NamedEvaluation to ensure the correct class name for class expressions.
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(
          node,
          /*discarded*/
          false
        );
      case 304 /* PropertyAssignment */:
        return visitPropertyAssignment(node);
      case 261 /* VariableDeclaration */:
        return visitVariableDeclaration(node);
      case 209 /* BindingElement */:
        return visitBindingElement(node);
      case 278 /* ExportAssignment */:
        return visitExportAssignment(node);
      case 110 /* ThisKeyword */:
        return visitThisExpression(node);
      case 249 /* ForStatement */:
        return visitForStatement(node);
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(
          node,
          /*discarded*/
          false
        );
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(
          node,
          /*discarded*/
          false
        );
      case 356 /* PartiallyEmittedExpression */:
        return visitPartiallyEmittedExpression(
          node,
          /*discarded*/
          false
        );
      case 214 /* CallExpression */:
        return visitCallExpression(node);
      case 216 /* TaggedTemplateExpression */:
        return visitTaggedTemplateExpression(node);
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPreOrPostfixUnaryExpression(
          node,
          /*discarded*/
          false
        );
      case 212 /* PropertyAccessExpression */:
        return visitPropertyAccessExpression(node);
      case 213 /* ElementAccessExpression */:
        return visitElementAccessExpression(node);
      case 168 /* ComputedPropertyName */:
        return visitComputedPropertyName(node);
      case 175 /* MethodDeclaration */:
      // object literal methods and accessors
      case 179 /* SetAccessor */:
      case 178 /* GetAccessor */:
      case 219 /* FunctionExpression */:
      case 263 /* FunctionDeclaration */: {
        enterOther();
        const result ***REMOVED*** visitEachChild(node, fallbackVisitor, context);
        exitOther();
        return result;
      }
      default:
        return visitEachChild(node, fallbackVisitor, context);
    }
  }
  function fallbackVisitor(node) {
    switch (node.kind) {
      case 171 /* Decorator */:
        return void 0;
      default:
        return visitor(node);
    }
  }
  function modifierVisitor(node) {
    switch (node.kind) {
      case 171 /* Decorator */:
        return void 0;
      default:
        return node;
    }
  }
  function classElementVisitor(node) {
    switch (node.kind) {
      case 177 /* Constructor */:
        return visitConstructorDeclaration(node);
      case 175 /* MethodDeclaration */:
        return visitMethodDeclaration(node);
      case 178 /* GetAccessor */:
        return visitGetAccessorDeclaration(node);
      case 179 /* SetAccessor */:
        return visitSetAccessorDeclaration(node);
      case 173 /* PropertyDeclaration */:
        return visitPropertyDeclaration(node);
      case 176 /* ClassStaticBlockDeclaration */:
        return visitClassStaticBlockDeclaration(node);
      default:
        return visitor(node);
    }
  }
  function discardedValueVisitor(node) {
    switch (node.kind) {
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPreOrPostfixUnaryExpression(
          node,
          /*discarded*/
          true
        );
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(
          node,
          /*discarded*/
          true
        );
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(
          node,
          /*discarded*/
          true
        );
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(
          node,
          /*discarded*/
          true
        );
      default:
        return visitor(node);
    }
  }
  function getHelperVariableName(node) {
    let declarationName ***REMOVED*** node.name && isIdentifier(node.name) && !isGeneratedIdentifier(node.name) ? idText(node.name) : node.name && isPrivateIdentifier(node.name) && !isGeneratedIdentifier(node.name) ? idText(node.name).slice(1) : node.name && isStringLiteral(node.name) && isIdentifierText(node.name.text, 99 /* ESNext */) ? node.name.text : isClassLike(node) ? "class" : "member";
    if (isGetAccessor(node)) declarationName ***REMOVED*** `get_${declarationName}`;
    if (isSetAccessor(node)) declarationName ***REMOVED*** `set_${declarationName}`;
    if (node.name && isPrivateIdentifier(node.name)) declarationName ***REMOVED*** `private_${declarationName}`;
    if (isStatic(node)) declarationName ***REMOVED*** `static_${declarationName}`;
    return "_" + declarationName;
  }
  function createHelperVariable(node, suffix) {
    return factory2.createUniqueName(`${getHelperVariableName(node)}_${suffix}`, 16 /* Optimistic */ | 8 /* ReservedInNestedScopes */);
  }
  function createLet(name, initializer) {
    return factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      factory2.createVariableDeclarationList([
        factory2.createVariableDeclaration(
          name,
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          initializer
        )
      ], 1 /* Let */)
    );
  }
  function createClassInfo(node) {
    const metadataReference ***REMOVED*** factory2.createUniqueName("_metadata", 16 /* Optimistic */ | 32 /* FileLevel */);
    let instanceMethodExtraInitializersName;
    let staticMethodExtraInitializersName;
    let hasStaticInitializers ***REMOVED*** false;
    let hasNonAmbientInstanceFields ***REMOVED*** false;
    let hasStaticPrivateClassElements ***REMOVED*** false;
    let classThis2;
    let pendingStaticInitializers;
    let pendingInstanceInitializers;
    if (nodeIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    )) {
      const needsUniqueClassThis ***REMOVED*** some(node.members, (member) ***REMOVED***> (isPrivateIdentifierClassElementDeclaration(member) || isAutoAccessorPropertyDeclaration(member)) && hasStaticModifier(member));
      classThis2 ***REMOVED*** factory2.createUniqueName(
        "_classThis",
        needsUniqueClassThis ? 16 /* Optimistic */ | 8 /* ReservedInNestedScopes */ : 16 /* Optimistic */ | 32 /* FileLevel */
      );
    }
    for (const member of node.members) {
      if (isMethodOrAccessor(member) && nodeOrChildIsDecorated(
        /*useLegacyDecorators*/
        false,
        member,
        node
      )) {
        if (hasStaticModifier(member)) {
          if (!staticMethodExtraInitializersName) {
            staticMethodExtraInitializersName ***REMOVED*** factory2.createUniqueName("_staticExtraInitializers", 16 /* Optimistic */ | 32 /* FileLevel */);
            const initializer ***REMOVED*** emitHelpers().createRunInitializersHelper(classThis2 ?? factory2.createThis(), staticMethodExtraInitializersName);
            setSourceMapRange(initializer, node.name ?? moveRangePastDecorators(node));
            pendingStaticInitializers ?? (pendingStaticInitializers ***REMOVED*** []);
            pendingStaticInitializers.push(initializer);
          }
        } else {
          if (!instanceMethodExtraInitializersName) {
            instanceMethodExtraInitializersName ***REMOVED*** factory2.createUniqueName("_instanceExtraInitializers", 16 /* Optimistic */ | 32 /* FileLevel */);
            const initializer ***REMOVED*** emitHelpers().createRunInitializersHelper(factory2.createThis(), instanceMethodExtraInitializersName);
            setSourceMapRange(initializer, node.name ?? moveRangePastDecorators(node));
            pendingInstanceInitializers ?? (pendingInstanceInitializers ***REMOVED*** []);
            pendingInstanceInitializers.push(initializer);
          }
          instanceMethodExtraInitializersName ?? (instanceMethodExtraInitializersName ***REMOVED*** factory2.createUniqueName("_instanceExtraInitializers", 16 /* Optimistic */ | 32 /* FileLevel */));
        }
      }
      if (isClassStaticBlockDeclaration(member)) {
        if (!isClassNamedEvaluationHelperBlock(member)) {
          hasStaticInitializers ***REMOVED*** true;
        }
      } else if (isPropertyDeclaration(member)) {
        if (hasStaticModifier(member)) {
          hasStaticInitializers || (hasStaticInitializers ***REMOVED*** !!member.initializer || hasDecorators(member));
        } else {
          hasNonAmbientInstanceFields || (hasNonAmbientInstanceFields ***REMOVED*** !isAmbientPropertyDeclaration(member));
        }
      }
      if ((isPrivateIdentifierClassElementDeclaration(member) || isAutoAccessorPropertyDeclaration(member)) && hasStaticModifier(member)) {
        hasStaticPrivateClassElements ***REMOVED*** true;
      }
      if (staticMethodExtraInitializersName && instanceMethodExtraInitializersName && hasStaticInitializers && hasNonAmbientInstanceFields && hasStaticPrivateClassElements) {
        break;
      }
    }
    return {
      class: node,
      classThis: classThis2,
      metadataReference,
      instanceMethodExtraInitializersName,
      staticMethodExtraInitializersName,
      hasStaticInitializers,
      hasNonAmbientInstanceFields,
      hasStaticPrivateClassElements,
      pendingStaticInitializers,
      pendingInstanceInitializers
    };
  }
  function transformClassLike(node) {
    startLexicalEnvironment();
    if (!classHasDeclaredOrExplicitlyAssignedName(node) && classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    )) {
      node ***REMOVED*** injectClassNamedEvaluationHelperBlockIfMissing(context, node, factory2.createStringLiteral(""));
    }
    const classReference ***REMOVED*** factory2.getLocalName(
      node,
      /*allowComments*/
      false,
      /*allowSourceMaps*/
      false,
      /*ignoreAssignedName*/
      true
    );
    const classInfo2 ***REMOVED*** createClassInfo(node);
    const classDefinitionStatements ***REMOVED*** [];
    let leadingBlockStatements;
    let trailingBlockStatements;
    let syntheticConstructor;
    let heritageClauses;
    let shouldTransformPrivateStaticElementsInClass ***REMOVED*** false;
    const classDecorators ***REMOVED*** transformAllDecoratorsOfDeclaration(getAllDecoratorsOfClass(
      node,
      /*useLegacyDecorators*/
      false
    ));
    if (classDecorators) {
      classInfo2.classDecoratorsName ***REMOVED*** factory2.createUniqueName("_classDecorators", 16 /* Optimistic */ | 32 /* FileLevel */);
      classInfo2.classDescriptorName ***REMOVED*** factory2.createUniqueName("_classDescriptor", 16 /* Optimistic */ | 32 /* FileLevel */);
      classInfo2.classExtraInitializersName ***REMOVED*** factory2.createUniqueName("_classExtraInitializers", 16 /* Optimistic */ | 32 /* FileLevel */);
      Debug.assertIsDefined(classInfo2.classThis);
      classDefinitionStatements.push(
        createLet(classInfo2.classDecoratorsName, factory2.createArrayLiteralExpression(classDecorators)),
        createLet(classInfo2.classDescriptorName),
        createLet(classInfo2.classExtraInitializersName, factory2.createArrayLiteralExpression()),
        createLet(classInfo2.classThis)
      );
      if (classInfo2.hasStaticPrivateClassElements) {
        shouldTransformPrivateStaticElementsInClass ***REMOVED*** true;
        shouldTransformPrivateStaticElementsInFile ***REMOVED*** true;
      }
    }
    const extendsClause ***REMOVED*** getHeritageClause(node.heritageClauses, 96 /* ExtendsKeyword */);
    const extendsElement ***REMOVED*** extendsClause && firstOrUndefined(extendsClause.types);
    const extendsExpression ***REMOVED*** extendsElement && visitNode(extendsElement.expression, visitor, isExpression);
    if (extendsExpression) {
      classInfo2.classSuper ***REMOVED*** factory2.createUniqueName("_classSuper", 16 /* Optimistic */ | 32 /* FileLevel */);
      const unwrapped ***REMOVED*** skipOuterExpressions(extendsExpression);
      const safeExtendsExpression ***REMOVED*** isClassExpression(unwrapped) && !unwrapped.name || isFunctionExpression(unwrapped) && !unwrapped.name || isArrowFunction(unwrapped) ? factory2.createComma(factory2.createNumericLiteral(0), extendsExpression) : extendsExpression;
      classDefinitionStatements.push(createLet(classInfo2.classSuper, safeExtendsExpression));
      const updatedExtendsElement ***REMOVED*** factory2.updateExpressionWithTypeArguments(
        extendsElement,
        classInfo2.classSuper,
        /*typeArguments*/
        void 0
      );
      const updatedExtendsClause ***REMOVED*** factory2.updateHeritageClause(extendsClause, [updatedExtendsElement]);
      heritageClauses ***REMOVED*** factory2.createNodeArray([updatedExtendsClause]);
    }
    const renamedClassThis ***REMOVED*** classInfo2.classThis ?? factory2.createThis();
    enterClass(classInfo2);
    leadingBlockStatements ***REMOVED*** append(leadingBlockStatements, createMetadata(classInfo2.metadataReference, classInfo2.classSuper));
    let members ***REMOVED*** node.members;
    members ***REMOVED*** visitNodes2(members, (node2) ***REMOVED***> isConstructorDeclaration(node2) ? node2 : classElementVisitor(node2), isClassElement);
    members ***REMOVED*** visitNodes2(members, (node2) ***REMOVED***> isConstructorDeclaration(node2) ? classElementVisitor(node2) : node2, isClassElement);
    if (pendingExpressions) {
      let outerThis;
      for (let expression of pendingExpressions) {
        expression ***REMOVED*** visitNode(expression, function thisVisitor(node2) {
          if (!(node2.transformFlags & 16384 /* ContainsLexicalThis */)) {
            return node2;
          }
          switch (node2.kind) {
            case 110 /* ThisKeyword */:
              if (!outerThis) {
                outerThis ***REMOVED*** factory2.createUniqueName("_outerThis", 16 /* Optimistic */);
                classDefinitionStatements.unshift(createLet(outerThis, factory2.createThis()));
              }
              return outerThis;
            default:
              return visitEachChild(node2, thisVisitor, context);
          }
        }, isExpression);
        const statement ***REMOVED*** factory2.createExpressionStatement(expression);
        leadingBlockStatements ***REMOVED*** append(leadingBlockStatements, statement);
      }
      pendingExpressions ***REMOVED*** void 0;
    }
    exitClass();
    if (some(classInfo2.pendingInstanceInitializers) && !getFirstConstructorWithBody(node)) {
      const initializerStatements ***REMOVED*** prepareConstructor(node, classInfo2);
      if (initializerStatements) {
        const extendsClauseElement ***REMOVED*** getEffectiveBaseTypeNode(node);
        const isDerivedClass ***REMOVED*** !!(extendsClauseElement && skipOuterExpressions(extendsClauseElement.expression).kind !***REMOVED******REMOVED*** 106 /* NullKeyword */);
        const constructorStatements ***REMOVED*** [];
        if (isDerivedClass) {
          const spreadArguments ***REMOVED*** factory2.createSpreadElement(factory2.createIdentifier("arguments"));
          const superCall ***REMOVED*** factory2.createCallExpression(
            factory2.createSuper(),
            /*typeArguments*/
            void 0,
            [spreadArguments]
          );
          constructorStatements.push(factory2.createExpressionStatement(superCall));
        }
        addRange(constructorStatements, initializerStatements);
        const constructorBody ***REMOVED*** factory2.createBlock(
          constructorStatements,
          /*multiLine*/
          true
        );
        syntheticConstructor ***REMOVED*** factory2.createConstructorDeclaration(
          /*modifiers*/
          void 0,
          [],
          constructorBody
        );
      }
    }
    if (classInfo2.staticMethodExtraInitializersName) {
      classDefinitionStatements.push(
        createLet(classInfo2.staticMethodExtraInitializersName, factory2.createArrayLiteralExpression())
      );
    }
    if (classInfo2.instanceMethodExtraInitializersName) {
      classDefinitionStatements.push(
        createLet(classInfo2.instanceMethodExtraInitializersName, factory2.createArrayLiteralExpression())
      );
    }
    if (classInfo2.memberInfos) {
      forEachEntry(classInfo2.memberInfos, (memberInfo, member) ***REMOVED***> {
        if (isStatic(member)) {
          classDefinitionStatements.push(createLet(memberInfo.memberDecoratorsName));
          if (memberInfo.memberInitializersName) {
            classDefinitionStatements.push(createLet(memberInfo.memberInitializersName, factory2.createArrayLiteralExpression()));
          }
          if (memberInfo.memberExtraInitializersName) {
            classDefinitionStatements.push(createLet(memberInfo.memberExtraInitializersName, factory2.createArrayLiteralExpression()));
          }
          if (memberInfo.memberDescriptorName) {
            classDefinitionStatements.push(createLet(memberInfo.memberDescriptorName));
          }
        }
      });
    }
    if (classInfo2.memberInfos) {
      forEachEntry(classInfo2.memberInfos, (memberInfo, member) ***REMOVED***> {
        if (!isStatic(member)) {
          classDefinitionStatements.push(createLet(memberInfo.memberDecoratorsName));
          if (memberInfo.memberInitializersName) {
            classDefinitionStatements.push(createLet(memberInfo.memberInitializersName, factory2.createArrayLiteralExpression()));
          }
          if (memberInfo.memberExtraInitializersName) {
            classDefinitionStatements.push(createLet(memberInfo.memberExtraInitializersName, factory2.createArrayLiteralExpression()));
          }
          if (memberInfo.memberDescriptorName) {
            classDefinitionStatements.push(createLet(memberInfo.memberDescriptorName));
          }
        }
      });
    }
    leadingBlockStatements ***REMOVED*** addRange(leadingBlockStatements, classInfo2.staticNonFieldDecorationStatements);
    leadingBlockStatements ***REMOVED*** addRange(leadingBlockStatements, classInfo2.nonStaticNonFieldDecorationStatements);
    leadingBlockStatements ***REMOVED*** addRange(leadingBlockStatements, classInfo2.staticFieldDecorationStatements);
    leadingBlockStatements ***REMOVED*** addRange(leadingBlockStatements, classInfo2.nonStaticFieldDecorationStatements);
    if (classInfo2.classDescriptorName && classInfo2.classDecoratorsName && classInfo2.classExtraInitializersName && classInfo2.classThis) {
      leadingBlockStatements ?? (leadingBlockStatements ***REMOVED*** []);
      const valueProperty ***REMOVED*** factory2.createPropertyAssignment("value", renamedClassThis);
      const classDescriptor ***REMOVED*** factory2.createObjectLiteralExpression([valueProperty]);
      const classDescriptorAssignment ***REMOVED*** factory2.createAssignment(classInfo2.classDescriptorName, classDescriptor);
      const classNameReference ***REMOVED*** factory2.createPropertyAccessExpression(renamedClassThis, "name");
      const esDecorateHelper2 ***REMOVED*** emitHelpers().createESDecorateHelper(
        factory2.createNull(),
        classDescriptorAssignment,
        classInfo2.classDecoratorsName,
        { kind: "class", name: classNameReference, metadata: classInfo2.metadataReference },
        factory2.createNull(),
        classInfo2.classExtraInitializersName
      );
      const esDecorateStatement ***REMOVED*** factory2.createExpressionStatement(esDecorateHelper2);
      setSourceMapRange(esDecorateStatement, moveRangePastDecorators(node));
      leadingBlockStatements.push(esDecorateStatement);
      const classDescriptorValueReference ***REMOVED*** factory2.createPropertyAccessExpression(classInfo2.classDescriptorName, "value");
      const classThisAssignment ***REMOVED*** factory2.createAssignment(classInfo2.classThis, classDescriptorValueReference);
      const classReferenceAssignment ***REMOVED*** factory2.createAssignment(classReference, classThisAssignment);
      leadingBlockStatements.push(factory2.createExpressionStatement(classReferenceAssignment));
    }
    leadingBlockStatements.push(createSymbolMetadata(renamedClassThis, classInfo2.metadataReference));
    if (some(classInfo2.pendingStaticInitializers)) {
      for (const initializer of classInfo2.pendingStaticInitializers) {
        const initializerStatement ***REMOVED*** factory2.createExpressionStatement(initializer);
        setSourceMapRange(initializerStatement, getSourceMapRange(initializer));
        trailingBlockStatements ***REMOVED*** append(trailingBlockStatements, initializerStatement);
      }
      classInfo2.pendingStaticInitializers ***REMOVED*** void 0;
    }
    if (classInfo2.classExtraInitializersName) {
      const runClassInitializersHelper ***REMOVED*** emitHelpers().createRunInitializersHelper(renamedClassThis, classInfo2.classExtraInitializersName);
      const runClassInitializersStatement ***REMOVED*** factory2.createExpressionStatement(runClassInitializersHelper);
      setSourceMapRange(runClassInitializersStatement, node.name ?? moveRangePastDecorators(node));
      trailingBlockStatements ***REMOVED*** append(trailingBlockStatements, runClassInitializersStatement);
    }
    if (leadingBlockStatements && trailingBlockStatements && !classInfo2.hasStaticInitializers) {
      addRange(leadingBlockStatements, trailingBlockStatements);
      trailingBlockStatements ***REMOVED*** void 0;
    }
    const leadingStaticBlock ***REMOVED*** leadingBlockStatements && factory2.createClassStaticBlockDeclaration(factory2.createBlock(
      leadingBlockStatements,
      /*multiLine*/
      true
    ));
    if (leadingStaticBlock && shouldTransformPrivateStaticElementsInClass) {
      setInternalEmitFlags(leadingStaticBlock, 32 /* TransformPrivateStaticElements */);
    }
    const trailingStaticBlock ***REMOVED*** trailingBlockStatements && factory2.createClassStaticBlockDeclaration(factory2.createBlock(
      trailingBlockStatements,
      /*multiLine*/
      true
    ));
    if (leadingStaticBlock || syntheticConstructor || trailingStaticBlock) {
      const newMembers ***REMOVED*** [];
      const existingNamedEvaluationHelperBlockIndex ***REMOVED*** members.findIndex(isClassNamedEvaluationHelperBlock);
      if (leadingStaticBlock) {
        addRange(newMembers, members, 0, existingNamedEvaluationHelperBlockIndex + 1);
        newMembers.push(leadingStaticBlock);
        addRange(newMembers, members, existingNamedEvaluationHelperBlockIndex + 1);
      } else {
        addRange(newMembers, members);
      }
      if (syntheticConstructor) {
        newMembers.push(syntheticConstructor);
      }
      if (trailingStaticBlock) {
        newMembers.push(trailingStaticBlock);
      }
      members ***REMOVED*** setTextRange(factory2.createNodeArray(newMembers), members);
    }
    const lexicalEnvironment ***REMOVED*** endLexicalEnvironment();
    let classExpression;
    if (classDecorators) {
      classExpression ***REMOVED*** factory2.createClassExpression(
        /*modifiers*/
        void 0,
        /*name*/
        void 0,
        /*typeParameters*/
        void 0,
        heritageClauses,
        members
      );
      if (classInfo2.classThis) {
        classExpression ***REMOVED*** injectClassThisAssignmentIfMissing(factory2, classExpression, classInfo2.classThis);
      }
      const classReferenceDeclaration ***REMOVED*** factory2.createVariableDeclaration(
        classReference,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        classExpression
      );
      const classReferenceVarDeclList ***REMOVED*** factory2.createVariableDeclarationList([classReferenceDeclaration]);
      const returnExpr ***REMOVED*** classInfo2.classThis ? factory2.createAssignment(classReference, classInfo2.classThis) : classReference;
      classDefinitionStatements.push(
        factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          classReferenceVarDeclList
        ),
        factory2.createReturnStatement(returnExpr)
      );
    } else {
      classExpression ***REMOVED*** factory2.createClassExpression(
        /*modifiers*/
        void 0,
        node.name,
        /*typeParameters*/
        void 0,
        heritageClauses,
        members
      );
      classDefinitionStatements.push(factory2.createReturnStatement(classExpression));
    }
    if (shouldTransformPrivateStaticElementsInClass) {
      addInternalEmitFlags(classExpression, 32 /* TransformPrivateStaticElements */);
      for (const member of classExpression.members) {
        if ((isPrivateIdentifierClassElementDeclaration(member) || isAutoAccessorPropertyDeclaration(member)) && hasStaticModifier(member)) {
          addInternalEmitFlags(member, 32 /* TransformPrivateStaticElements */);
        }
      }
    }
    setOriginalNode(classExpression, node);
    return factory2.createImmediatelyInvokedArrowFunction(factory2.mergeLexicalEnvironment(classDefinitionStatements, lexicalEnvironment));
  }
  function isDecoratedClassLike(node) {
    return classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    ) || childIsDecorated(
      /*useLegacyDecorators*/
      false,
      node
    );
  }
  function visitClassDeclaration(node) {
    if (isDecoratedClassLike(node)) {
      const statements ***REMOVED*** [];
      const originalClass ***REMOVED*** getOriginalNode(node, isClassLike) ?? node;
      const className ***REMOVED*** originalClass.name ? factory2.createStringLiteralFromNode(originalClass.name) : factory2.createStringLiteral("default");
      const isExport ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
      const isDefault ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */);
      if (!node.name) {
        node ***REMOVED*** injectClassNamedEvaluationHelperBlockIfMissing(context, node, className);
      }
      if (isExport && isDefault) {
        const iife ***REMOVED*** transformClassLike(node);
        if (node.name) {
          const varDecl ***REMOVED*** factory2.createVariableDeclaration(
            factory2.getLocalName(node),
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            iife
          );
          setOriginalNode(varDecl, node);
          const varDecls ***REMOVED*** factory2.createVariableDeclarationList([varDecl], 1 /* Let */);
          const varStatement ***REMOVED*** factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            varDecls
          );
          statements.push(varStatement);
          const exportStatement ***REMOVED*** factory2.createExportDefault(factory2.getDeclarationName(node));
          setOriginalNode(exportStatement, node);
          setCommentRange(exportStatement, getCommentRange(node));
          setSourceMapRange(exportStatement, moveRangePastDecorators(node));
          statements.push(exportStatement);
        } else {
          const exportStatement ***REMOVED*** factory2.createExportDefault(iife);
          setOriginalNode(exportStatement, node);
          setCommentRange(exportStatement, getCommentRange(node));
          setSourceMapRange(exportStatement, moveRangePastDecorators(node));
          statements.push(exportStatement);
        }
      } else {
        Debug.assertIsDefined(node.name, "A class declaration that is not a default export must have a name.");
        const iife ***REMOVED*** transformClassLike(node);
        const modifierVisitorNoExport ***REMOVED*** isExport ? (node2) ***REMOVED***> isExportModifier(node2) ? void 0 : modifierVisitor(node2) : modifierVisitor;
        const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitorNoExport, isModifier);
        const declName ***REMOVED*** factory2.getLocalName(
          node,
          /*allowComments*/
          false,
          /*allowSourceMaps*/
          true
        );
        const varDecl ***REMOVED*** factory2.createVariableDeclaration(
          declName,
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          iife
        );
        setOriginalNode(varDecl, node);
        const varDecls ***REMOVED*** factory2.createVariableDeclarationList([varDecl], 1 /* Let */);
        const varStatement ***REMOVED*** factory2.createVariableStatement(modifiers, varDecls);
        setOriginalNode(varStatement, node);
        setCommentRange(varStatement, getCommentRange(node));
        statements.push(varStatement);
        if (isExport) {
          const exportStatement ***REMOVED*** factory2.createExternalModuleExport(declName);
          setOriginalNode(exportStatement, node);
          statements.push(exportStatement);
        }
      }
      return singleOrMany(statements);
    } else {
      const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
      const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, visitor, isHeritageClause);
      enterClass(
        /*classInfo*/
        void 0
      );
      const members ***REMOVED*** visitNodes2(node.members, classElementVisitor, isClassElement);
      exitClass();
      return factory2.updateClassDeclaration(
        node,
        modifiers,
        node.name,
        /*typeParameters*/
        void 0,
        heritageClauses,
        members
      );
    }
  }
  function visitClassExpression(node) {
    if (isDecoratedClassLike(node)) {
      const iife ***REMOVED*** transformClassLike(node);
      setOriginalNode(iife, node);
      return iife;
    } else {
      const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
      const heritageClauses ***REMOVED*** visitNodes2(node.heritageClauses, visitor, isHeritageClause);
      enterClass(
        /*classInfo*/
        void 0
      );
      const members ***REMOVED*** visitNodes2(node.members, classElementVisitor, isClassElement);
      exitClass();
      return factory2.updateClassExpression(
        node,
        modifiers,
        node.name,
        /*typeParameters*/
        void 0,
        heritageClauses,
        members
      );
    }
  }
  function prepareConstructor(_parent, classInfo2) {
    if (some(classInfo2.pendingInstanceInitializers)) {
      const statements ***REMOVED*** [];
      statements.push(
        factory2.createExpressionStatement(
          factory2.inlineExpressions(classInfo2.pendingInstanceInitializers)
        )
      );
      classInfo2.pendingInstanceInitializers ***REMOVED*** void 0;
      return statements;
    }
  }
  function transformConstructorBodyWorker(statementsOut, statementsIn, statementOffset, superPath, superPathDepth, initializerStatements) {
    const superStatementIndex ***REMOVED*** superPath[superPathDepth];
    const superStatement ***REMOVED*** statementsIn[superStatementIndex];
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, statementOffset, superStatementIndex - statementOffset));
    if (isTryStatement(superStatement)) {
      const tryBlockStatements ***REMOVED*** [];
      transformConstructorBodyWorker(
        tryBlockStatements,
        superStatement.tryBlock.statements,
        /*statementOffset*/
        0,
        superPath,
        superPathDepth + 1,
        initializerStatements
      );
      const tryBlockStatementsArray ***REMOVED*** factory2.createNodeArray(tryBlockStatements);
      setTextRange(tryBlockStatementsArray, superStatement.tryBlock.statements);
      statementsOut.push(factory2.updateTryStatement(
        superStatement,
        factory2.updateBlock(superStatement.tryBlock, tryBlockStatements),
        visitNode(superStatement.catchClause, visitor, isCatchClause),
        visitNode(superStatement.finallyBlock, visitor, isBlock)
      ));
    } else {
      addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, superStatementIndex, 1));
      addRange(statementsOut, initializerStatements);
    }
    addRange(statementsOut, visitNodes2(statementsIn, visitor, isStatement, superStatementIndex + 1));
  }
  function visitConstructorDeclaration(node) {
    enterClassElement(node);
    const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
    const parameters ***REMOVED*** visitNodes2(node.parameters, visitor, isParameter);
    let body;
    if (node.body && classInfo) {
      const initializerStatements ***REMOVED*** prepareConstructor(classInfo.class, classInfo);
      if (initializerStatements) {
        const statements ***REMOVED*** [];
        const nonPrologueStart ***REMOVED*** factory2.copyPrologue(
          node.body.statements,
          statements,
          /*ensureUseStrict*/
          false,
          visitor
        );
        const superStatementIndices ***REMOVED*** findSuperStatementIndexPath(node.body.statements, nonPrologueStart);
        if (superStatementIndices.length > 0) {
          transformConstructorBodyWorker(statements, node.body.statements, nonPrologueStart, superStatementIndices, 0, initializerStatements);
        } else {
          addRange(statements, initializerStatements);
          addRange(statements, visitNodes2(node.body.statements, visitor, isStatement));
        }
        body ***REMOVED*** factory2.createBlock(
          statements,
          /*multiLine*/
          true
        );
        setOriginalNode(body, node.body);
        setTextRange(body, node.body);
      }
    }
    body ?? (body ***REMOVED*** visitNode(node.body, visitor, isBlock));
    exitClassElement();
    return factory2.updateConstructorDeclaration(node, modifiers, parameters, body);
  }
  function finishClassElement(updated, original) {
    if (updated !***REMOVED******REMOVED*** original) {
      setCommentRange(updated, original);
      setSourceMapRange(updated, moveRangePastDecorators(original));
    }
    return updated;
  }
  function partialTransformClassElement(member, classInfo2, createDescriptor) {
    let referencedName;
    let name;
    let initializersName;
    let extraInitializersName;
    let thisArg;
    let descriptorName;
    if (!classInfo2) {
      const modifiers2 ***REMOVED*** visitNodes2(member.modifiers, modifierVisitor, isModifier);
      enterName();
      name ***REMOVED*** visitPropertyName(member.name);
      exitName();
      return { modifiers: modifiers2, referencedName, name, initializersName, descriptorName, thisArg };
    }
    const memberDecorators ***REMOVED*** transformAllDecoratorsOfDeclaration(getAllDecoratorsOfClassElement(
      member,
      classInfo2.class,
      /*useLegacyDecorators*/
      false
    ));
    const modifiers ***REMOVED*** visitNodes2(member.modifiers, modifierVisitor, isModifier);
    if (memberDecorators) {
      const memberDecoratorsName ***REMOVED*** createHelperVariable(member, "decorators");
      const memberDecoratorsArray ***REMOVED*** factory2.createArrayLiteralExpression(memberDecorators);
      const memberDecoratorsAssignment ***REMOVED*** factory2.createAssignment(memberDecoratorsName, memberDecoratorsArray);
      const memberInfo ***REMOVED*** { memberDecoratorsName };
      classInfo2.memberInfos ?? (classInfo2.memberInfos ***REMOVED*** /* @__PURE__ */ new Map());
      classInfo2.memberInfos.set(member, memberInfo);
      pendingExpressions ?? (pendingExpressions ***REMOVED*** []);
      pendingExpressions.push(memberDecoratorsAssignment);
      const statements ***REMOVED*** isMethodOrAccessor(member) || isAutoAccessorPropertyDeclaration(member) ? isStatic(member) ? classInfo2.staticNonFieldDecorationStatements ?? (classInfo2.staticNonFieldDecorationStatements ***REMOVED*** []) : classInfo2.nonStaticNonFieldDecorationStatements ?? (classInfo2.nonStaticNonFieldDecorationStatements ***REMOVED*** []) : isPropertyDeclaration(member) && !isAutoAccessorPropertyDeclaration(member) ? isStatic(member) ? classInfo2.staticFieldDecorationStatements ?? (classInfo2.staticFieldDecorationStatements ***REMOVED*** []) : classInfo2.nonStaticFieldDecorationStatements ?? (classInfo2.nonStaticFieldDecorationStatements ***REMOVED*** []) : Debug.fail();
      const kind ***REMOVED*** isGetAccessorDeclaration(member) ? "getter" : isSetAccessorDeclaration(member) ? "setter" : isMethodDeclaration(member) ? "method" : isAutoAccessorPropertyDeclaration(member) ? "accessor" : isPropertyDeclaration(member) ? "field" : Debug.fail();
      let propertyName;
      if (isIdentifier(member.name) || isPrivateIdentifier(member.name)) {
        propertyName ***REMOVED*** { computed: false, name: member.name };
      } else if (isPropertyNameLiteral(member.name)) {
        propertyName ***REMOVED*** { computed: true, name: factory2.createStringLiteralFromNode(member.name) };
      } else {
        const expression ***REMOVED*** member.name.expression;
        if (isPropertyNameLiteral(expression) && !isIdentifier(expression)) {
          propertyName ***REMOVED*** { computed: true, name: factory2.createStringLiteralFromNode(expression) };
        } else {
          enterName();
          ({ referencedName, name } ***REMOVED*** visitReferencedPropertyName(member.name));
          propertyName ***REMOVED*** { computed: true, name: referencedName };
          exitName();
        }
      }
      const context2 ***REMOVED*** {
        kind,
        name: propertyName,
        static: isStatic(member),
        private: isPrivateIdentifier(member.name),
        access: {
          // 15.7.3 CreateDecoratorAccessObject (kind, name)
          // 2. If _kind_ is ~field~, ~method~, ~accessor~, or ~getter~, then ...
          get: isPropertyDeclaration(member) || isGetAccessorDeclaration(member) || isMethodDeclaration(member),
          // 3. If _kind_ is ~field~, ~accessor~, or ~setter~, then ...
          set: isPropertyDeclaration(member) || isSetAccessorDeclaration(member)
        },
        metadata: classInfo2.metadataReference
      };
      if (isMethodOrAccessor(member)) {
        const methodExtraInitializersName ***REMOVED*** isStatic(member) ? classInfo2.staticMethodExtraInitializersName : classInfo2.instanceMethodExtraInitializersName;
        Debug.assertIsDefined(methodExtraInitializersName);
        let descriptor;
        if (isPrivateIdentifierClassElementDeclaration(member) && createDescriptor) {
          descriptor ***REMOVED*** createDescriptor(member, visitNodes2(modifiers, (node) ***REMOVED***> tryCast(node, isAsyncModifier), isModifier));
          memberInfo.memberDescriptorName ***REMOVED*** descriptorName ***REMOVED*** createHelperVariable(member, "descriptor");
          descriptor ***REMOVED*** factory2.createAssignment(descriptorName, descriptor);
        }
        const esDecorateExpression ***REMOVED*** emitHelpers().createESDecorateHelper(factory2.createThis(), descriptor ?? factory2.createNull(), memberDecoratorsName, context2, factory2.createNull(), methodExtraInitializersName);
        const esDecorateStatement ***REMOVED*** factory2.createExpressionStatement(esDecorateExpression);
        setSourceMapRange(esDecorateStatement, moveRangePastDecorators(member));
        statements.push(esDecorateStatement);
      } else if (isPropertyDeclaration(member)) {
        initializersName ***REMOVED*** memberInfo.memberInitializersName ?? (memberInfo.memberInitializersName ***REMOVED*** createHelperVariable(member, "initializers"));
        extraInitializersName ***REMOVED*** memberInfo.memberExtraInitializersName ?? (memberInfo.memberExtraInitializersName ***REMOVED*** createHelperVariable(member, "extraInitializers"));
        if (isStatic(member)) {
          thisArg ***REMOVED*** classInfo2.classThis;
        }
        let descriptor;
        if (isPrivateIdentifierClassElementDeclaration(member) && hasAccessorModifier(member) && createDescriptor) {
          descriptor ***REMOVED*** createDescriptor(
            member,
            /*modifiers*/
            void 0
          );
          memberInfo.memberDescriptorName ***REMOVED*** descriptorName ***REMOVED*** createHelperVariable(member, "descriptor");
          descriptor ***REMOVED*** factory2.createAssignment(descriptorName, descriptor);
        }
        const esDecorateExpression ***REMOVED*** emitHelpers().createESDecorateHelper(
          isAutoAccessorPropertyDeclaration(member) ? factory2.createThis() : factory2.createNull(),
          descriptor ?? factory2.createNull(),
          memberDecoratorsName,
          context2,
          initializersName,
          extraInitializersName
        );
        const esDecorateStatement ***REMOVED*** factory2.createExpressionStatement(esDecorateExpression);
        setSourceMapRange(esDecorateStatement, moveRangePastDecorators(member));
        statements.push(esDecorateStatement);
      }
    }
    if (name ***REMOVED******REMOVED******REMOVED*** void 0) {
      enterName();
      name ***REMOVED*** visitPropertyName(member.name);
      exitName();
    }
    if (!some(modifiers) && (isMethodDeclaration(member) || isPropertyDeclaration(member))) {
      setEmitFlags(name, 1024 /* NoLeadingComments */);
    }
    return { modifiers, referencedName, name, initializersName, extraInitializersName, descriptorName, thisArg };
  }
  function visitMethodDeclaration(node) {
    enterClassElement(node);
    const { modifiers, name, descriptorName } ***REMOVED*** partialTransformClassElement(node, classInfo, createMethodDescriptorObject);
    if (descriptorName) {
      exitClassElement();
      return finishClassElement(createMethodDescriptorForwarder(modifiers, name, descriptorName), node);
    } else {
      const parameters ***REMOVED*** visitNodes2(node.parameters, visitor, isParameter);
      const body ***REMOVED*** visitNode(node.body, visitor, isBlock);
      exitClassElement();
      return finishClassElement(factory2.updateMethodDeclaration(
        node,
        modifiers,
        node.asteriskToken,
        name,
        /*questionToken*/
        void 0,
        /*typeParameters*/
        void 0,
        parameters,
        /*type*/
        void 0,
        body
      ), node);
    }
  }
  function visitGetAccessorDeclaration(node) {
    enterClassElement(node);
    const { modifiers, name, descriptorName } ***REMOVED*** partialTransformClassElement(node, classInfo, createGetAccessorDescriptorObject);
    if (descriptorName) {
      exitClassElement();
      return finishClassElement(createGetAccessorDescriptorForwarder(modifiers, name, descriptorName), node);
    } else {
      const parameters ***REMOVED*** visitNodes2(node.parameters, visitor, isParameter);
      const body ***REMOVED*** visitNode(node.body, visitor, isBlock);
      exitClassElement();
      return finishClassElement(factory2.updateGetAccessorDeclaration(
        node,
        modifiers,
        name,
        parameters,
        /*type*/
        void 0,
        body
      ), node);
    }
  }
  function visitSetAccessorDeclaration(node) {
    enterClassElement(node);
    const { modifiers, name, descriptorName } ***REMOVED*** partialTransformClassElement(node, classInfo, createSetAccessorDescriptorObject);
    if (descriptorName) {
      exitClassElement();
      return finishClassElement(createSetAccessorDescriptorForwarder(modifiers, name, descriptorName), node);
    } else {
      const parameters ***REMOVED*** visitNodes2(node.parameters, visitor, isParameter);
      const body ***REMOVED*** visitNode(node.body, visitor, isBlock);
      exitClassElement();
      return finishClassElement(factory2.updateSetAccessorDeclaration(node, modifiers, name, parameters, body), node);
    }
  }
  function visitClassStaticBlockDeclaration(node) {
    enterClassElement(node);
    let result;
    if (isClassNamedEvaluationHelperBlock(node)) {
      result ***REMOVED*** visitEachChild(node, visitor, context);
    } else if (isClassThisAssignmentBlock(node)) {
      const savedClassThis ***REMOVED*** classThis;
      classThis ***REMOVED*** void 0;
      result ***REMOVED*** visitEachChild(node, visitor, context);
      classThis ***REMOVED*** savedClassThis;
    } else {
      node ***REMOVED*** visitEachChild(node, visitor, context);
      result ***REMOVED*** node;
      if (classInfo) {
        classInfo.hasStaticInitializers ***REMOVED*** true;
        if (some(classInfo.pendingStaticInitializers)) {
          const statements ***REMOVED*** [];
          for (const initializer of classInfo.pendingStaticInitializers) {
            const initializerStatement ***REMOVED*** factory2.createExpressionStatement(initializer);
            setSourceMapRange(initializerStatement, getSourceMapRange(initializer));
            statements.push(initializerStatement);
          }
          const body ***REMOVED*** factory2.createBlock(
            statements,
            /*multiLine*/
            true
          );
          const staticBlock ***REMOVED*** factory2.createClassStaticBlockDeclaration(body);
          result ***REMOVED*** [staticBlock, result];
          classInfo.pendingStaticInitializers ***REMOVED*** void 0;
        }
      }
    }
    exitClassElement();
    return result;
  }
  function visitPropertyDeclaration(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.initializer));
    }
    enterClassElement(node);
    Debug.assert(!isAmbientPropertyDeclaration(node), "Not yet implemented.");
    const { modifiers, name, initializersName, extraInitializersName, descriptorName, thisArg } ***REMOVED*** partialTransformClassElement(node, classInfo, hasAccessorModifier(node) ? createAccessorPropertyDescriptorObject : void 0);
    startLexicalEnvironment();
    let initializer ***REMOVED*** visitNode(node.initializer, visitor, isExpression);
    if (initializersName) {
      initializer ***REMOVED*** emitHelpers().createRunInitializersHelper(
        thisArg ?? factory2.createThis(),
        initializersName,
        initializer ?? factory2.createVoidZero()
      );
    }
    if (isStatic(node) && classInfo && initializer) {
      classInfo.hasStaticInitializers ***REMOVED*** true;
    }
    const declarations ***REMOVED*** endLexicalEnvironment();
    if (some(declarations)) {
      initializer ***REMOVED*** factory2.createImmediatelyInvokedArrowFunction([
        ...declarations,
        factory2.createReturnStatement(initializer)
      ]);
    }
    if (classInfo) {
      if (isStatic(node)) {
        initializer ***REMOVED*** injectPendingInitializers(
          classInfo,
          /*isStatic*/
          true,
          initializer
        );
        if (extraInitializersName) {
          classInfo.pendingStaticInitializers ?? (classInfo.pendingStaticInitializers ***REMOVED*** []);
          classInfo.pendingStaticInitializers.push(
            emitHelpers().createRunInitializersHelper(
              classInfo.classThis ?? factory2.createThis(),
              extraInitializersName
            )
          );
        }
      } else {
        initializer ***REMOVED*** injectPendingInitializers(
          classInfo,
          /*isStatic*/
          false,
          initializer
        );
        if (extraInitializersName) {
          classInfo.pendingInstanceInitializers ?? (classInfo.pendingInstanceInitializers ***REMOVED*** []);
          classInfo.pendingInstanceInitializers.push(
            emitHelpers().createRunInitializersHelper(
              factory2.createThis(),
              extraInitializersName
            )
          );
        }
      }
    }
    exitClassElement();
    if (hasAccessorModifier(node) && descriptorName) {
      const commentRange ***REMOVED*** getCommentRange(node);
      const sourceMapRange ***REMOVED*** getSourceMapRange(node);
      const name2 ***REMOVED*** node.name;
      let getterName ***REMOVED*** name2;
      let setterName ***REMOVED*** name2;
      if (isComputedPropertyName(name2) && !isSimpleInlineableExpression(name2.expression)) {
        const cacheAssignment ***REMOVED*** findComputedPropertyNameCacheAssignment(name2);
        if (cacheAssignment) {
          getterName ***REMOVED*** factory2.updateComputedPropertyName(name2, visitNode(name2.expression, visitor, isExpression));
          setterName ***REMOVED*** factory2.updateComputedPropertyName(name2, cacheAssignment.left);
        } else {
          const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
          setSourceMapRange(temp, name2.expression);
          const expression ***REMOVED*** visitNode(name2.expression, visitor, isExpression);
          const assignment ***REMOVED*** factory2.createAssignment(temp, expression);
          setSourceMapRange(assignment, name2.expression);
          getterName ***REMOVED*** factory2.updateComputedPropertyName(name2, assignment);
          setterName ***REMOVED*** factory2.updateComputedPropertyName(name2, temp);
        }
      }
      const modifiersWithoutAccessor ***REMOVED*** visitNodes2(modifiers, (node2) ***REMOVED***> node2.kind !***REMOVED******REMOVED*** 129 /* AccessorKeyword */ ? node2 : void 0, isModifier);
      const backingField ***REMOVED*** createAccessorPropertyBackingField(factory2, node, modifiersWithoutAccessor, initializer);
      setOriginalNode(backingField, node);
      setEmitFlags(backingField, 3072 /* NoComments */);
      setSourceMapRange(backingField, sourceMapRange);
      setSourceMapRange(backingField.name, node.name);
      const getter ***REMOVED*** createGetAccessorDescriptorForwarder(modifiersWithoutAccessor, getterName, descriptorName);
      setOriginalNode(getter, node);
      setCommentRange(getter, commentRange);
      setSourceMapRange(getter, sourceMapRange);
      const setter ***REMOVED*** createSetAccessorDescriptorForwarder(modifiersWithoutAccessor, setterName, descriptorName);
      setOriginalNode(setter, node);
      setEmitFlags(setter, 3072 /* NoComments */);
      setSourceMapRange(setter, sourceMapRange);
      return [backingField, getter, setter];
    }
    return finishClassElement(factory2.updatePropertyDeclaration(
      node,
      modifiers,
      name,
      /*questionOrExclamationToken*/
      void 0,
      /*type*/
      void 0,
      initializer
    ), node);
  }
  function visitThisExpression(node) {
    return classThis ?? node;
  }
  function visitCallExpression(node) {
    if (isSuperProperty(node.expression) && classThis) {
      const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
      const argumentsList ***REMOVED*** visitNodes2(node.arguments, visitor, isExpression);
      const invocation ***REMOVED*** factory2.createFunctionCallCall(expression, classThis, argumentsList);
      setOriginalNode(invocation, node);
      setTextRange(invocation, node);
      return invocation;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitTaggedTemplateExpression(node) {
    if (isSuperProperty(node.tag) && classThis) {
      const tag ***REMOVED*** visitNode(node.tag, visitor, isExpression);
      const boundTag ***REMOVED*** factory2.createFunctionBindCall(tag, classThis, []);
      setOriginalNode(boundTag, node);
      setTextRange(boundTag, node);
      const template ***REMOVED*** visitNode(node.template, visitor, isTemplateLiteral);
      return factory2.updateTaggedTemplateExpression(
        node,
        boundTag,
        /*typeArguments*/
        void 0,
        template
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitPropertyAccessExpression(node) {
    if (isSuperProperty(node) && isIdentifier(node.name) && classThis && classSuper) {
      const propertyName ***REMOVED*** factory2.createStringLiteralFromNode(node.name);
      const superProperty ***REMOVED*** factory2.createReflectGetCall(classSuper, propertyName, classThis);
      setOriginalNode(superProperty, node.expression);
      setTextRange(superProperty, node.expression);
      return superProperty;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitElementAccessExpression(node) {
    if (isSuperProperty(node) && classThis && classSuper) {
      const propertyName ***REMOVED*** visitNode(node.argumentExpression, visitor, isExpression);
      const superProperty ***REMOVED*** factory2.createReflectGetCall(classSuper, propertyName, classThis);
      setOriginalNode(superProperty, node.expression);
      setTextRange(superProperty, node.expression);
      return superProperty;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitParameterDeclaration(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.initializer));
    }
    const updated ***REMOVED*** factory2.updateParameterDeclaration(
      node,
      /*modifiers*/
      void 0,
      node.dotDotDotToken,
      visitNode(node.name, visitor, isBindingName),
      /*questionToken*/
      void 0,
      /*type*/
      void 0,
      visitNode(node.initializer, visitor, isExpression)
    );
    if (updated !***REMOVED******REMOVED*** node) {
      setCommentRange(updated, node);
      setTextRange(updated, moveRangePastModifiers(node));
      setSourceMapRange(updated, moveRangePastModifiers(node));
      setEmitFlags(updated.name, 64 /* NoTrailingSourceMap */);
    }
    return updated;
  }
  function isAnonymousClassNeedingAssignedName(node) {
    return isClassExpression(node) && !node.name && isDecoratedClassLike(node);
  }
  function canIgnoreEmptyStringLiteralInAssignedName(node) {
    const innerExpression ***REMOVED*** skipOuterExpressions(node);
    return isClassExpression(innerExpression) && !innerExpression.name && !classOrConstructorParameterIsDecorated(
      /*useLegacyDecorators*/
      false,
      innerExpression
    );
  }
  function visitForStatement(node) {
    return factory2.updateForStatement(
      node,
      visitNode(node.initializer, discardedValueVisitor, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, discardedValueVisitor, isExpression),
      visitIterationBody(node.statement, visitor, context)
    );
  }
  function visitExpressionStatement(node) {
    return visitEachChild(node, discardedValueVisitor, context);
  }
  function visitBinaryExpression(node, discarded) {
    if (isDestructuringAssignment(node)) {
      const left ***REMOVED*** visitAssignmentPattern(node.left);
      const right ***REMOVED*** visitNode(node.right, visitor, isExpression);
      return factory2.updateBinaryExpression(node, left, node.operatorToken, right);
    }
    if (isAssignmentExpression(node)) {
      if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
        node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.right));
        return visitEachChild(node, visitor, context);
      }
      if (isSuperProperty(node.left) && classThis && classSuper) {
        let setterName ***REMOVED*** isElementAccessExpression(node.left) ? visitNode(node.left.argumentExpression, visitor, isExpression) : isIdentifier(node.left.name) ? factory2.createStringLiteralFromNode(node.left.name) : void 0;
        if (setterName) {
          let expression ***REMOVED*** visitNode(node.right, visitor, isExpression);
          if (isCompoundAssignment(node.operatorToken.kind)) {
            let getterName ***REMOVED*** setterName;
            if (!isSimpleInlineableExpression(setterName)) {
              getterName ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
              setterName ***REMOVED*** factory2.createAssignment(getterName, setterName);
            }
            const superPropertyGet ***REMOVED*** factory2.createReflectGetCall(
              classSuper,
              getterName,
              classThis
            );
            setOriginalNode(superPropertyGet, node.left);
            setTextRange(superPropertyGet, node.left);
            expression ***REMOVED*** factory2.createBinaryExpression(
              superPropertyGet,
              getNonAssignmentOperatorForCompoundAssignment(node.operatorToken.kind),
              expression
            );
            setTextRange(expression, node);
          }
          const temp ***REMOVED*** discarded ? void 0 : factory2.createTempVariable(hoistVariableDeclaration);
          if (temp) {
            expression ***REMOVED*** factory2.createAssignment(temp, expression);
            setTextRange(temp, node);
          }
          expression ***REMOVED*** factory2.createReflectSetCall(
            classSuper,
            setterName,
            expression,
            classThis
          );
          setOriginalNode(expression, node);
          setTextRange(expression, node);
          if (temp) {
            expression ***REMOVED*** factory2.createComma(expression, temp);
            setTextRange(expression, node);
          }
          return expression;
        }
      }
    }
    if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      const left ***REMOVED*** visitNode(node.left, discardedValueVisitor, isExpression);
      const right ***REMOVED*** visitNode(node.right, discarded ? discardedValueVisitor : visitor, isExpression);
      return factory2.updateBinaryExpression(node, left, node.operatorToken, right);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitPreOrPostfixUnaryExpression(node, discarded) {
    if (node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) {
      const operand ***REMOVED*** skipParentheses(node.operand);
      if (isSuperProperty(operand) && classThis && classSuper) {
        let setterName ***REMOVED*** isElementAccessExpression(operand) ? visitNode(operand.argumentExpression, visitor, isExpression) : isIdentifier(operand.name) ? factory2.createStringLiteralFromNode(operand.name) : void 0;
        if (setterName) {
          let getterName ***REMOVED*** setterName;
          if (!isSimpleInlineableExpression(setterName)) {
            getterName ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
            setterName ***REMOVED*** factory2.createAssignment(getterName, setterName);
          }
          let expression ***REMOVED*** factory2.createReflectGetCall(classSuper, getterName, classThis);
          setOriginalNode(expression, node);
          setTextRange(expression, node);
          const temp ***REMOVED*** discarded ? void 0 : factory2.createTempVariable(hoistVariableDeclaration);
          expression ***REMOVED*** expandPreOrPostfixIncrementOrDecrementExpression(factory2, node, expression, hoistVariableDeclaration, temp);
          expression ***REMOVED*** factory2.createReflectSetCall(classSuper, setterName, expression, classThis);
          setOriginalNode(expression, node);
          setTextRange(expression, node);
          if (temp) {
            expression ***REMOVED*** factory2.createComma(expression, temp);
            setTextRange(expression, node);
          }
          return expression;
        }
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCommaListExpression(node, discarded) {
    const elements ***REMOVED*** discarded ? visitCommaListElements(node.elements, discardedValueVisitor) : visitCommaListElements(node.elements, visitor, discardedValueVisitor);
    return factory2.updateCommaListExpression(node, elements);
  }
  function visitReferencedPropertyName(node) {
    if (isPropertyNameLiteral(node) || isPrivateIdentifier(node)) {
      const referencedName2 ***REMOVED*** factory2.createStringLiteralFromNode(node);
      const name2 ***REMOVED*** visitNode(node, visitor, isPropertyName);
      return { referencedName: referencedName2, name: name2 };
    }
    if (isPropertyNameLiteral(node.expression) && !isIdentifier(node.expression)) {
      const referencedName2 ***REMOVED*** factory2.createStringLiteralFromNode(node.expression);
      const name2 ***REMOVED*** visitNode(node, visitor, isPropertyName);
      return { referencedName: referencedName2, name: name2 };
    }
    const referencedName ***REMOVED*** factory2.getGeneratedNameForNode(node);
    hoistVariableDeclaration(referencedName);
    const key ***REMOVED*** emitHelpers().createPropKeyHelper(visitNode(node.expression, visitor, isExpression));
    const assignment ***REMOVED*** factory2.createAssignment(referencedName, key);
    const name ***REMOVED*** factory2.updateComputedPropertyName(node, injectPendingExpressions(assignment));
    return { referencedName, name };
  }
  function visitPropertyName(node) {
    if (isComputedPropertyName(node)) {
      return visitComputedPropertyName(node);
    }
    return visitNode(node, visitor, isPropertyName);
  }
  function visitComputedPropertyName(node) {
    let expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    if (!isSimpleInlineableExpression(expression)) {
      expression ***REMOVED*** injectPendingExpressions(expression);
    }
    return factory2.updateComputedPropertyName(node, expression);
  }
  function visitPropertyAssignment(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.initializer));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitVariableDeclaration(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.initializer));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitBindingElement(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.initializer));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitDestructuringAssignmentTarget(node) {
    if (isObjectLiteralExpression(node) || isArrayLiteralExpression(node)) {
      return visitAssignmentPattern(node);
    }
    if (isSuperProperty(node) && classThis && classSuper) {
      const propertyName ***REMOVED*** isElementAccessExpression(node) ? visitNode(node.argumentExpression, visitor, isExpression) : isIdentifier(node.name) ? factory2.createStringLiteralFromNode(node.name) : void 0;
      if (propertyName) {
        const paramName ***REMOVED*** factory2.createTempVariable(
          /*recordTempVariable*/
          void 0
        );
        const expression ***REMOVED*** factory2.createAssignmentTargetWrapper(
          paramName,
          factory2.createReflectSetCall(
            classSuper,
            propertyName,
            paramName,
            classThis
          )
        );
        setOriginalNode(expression, node);
        setTextRange(expression, node);
        return expression;
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentElement(node) {
    if (isAssignmentExpression(
      node,
      /*excludeCompoundAssignment*/
      true
    )) {
      if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
        node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.right));
      }
      const assignmentTarget ***REMOVED*** visitDestructuringAssignmentTarget(node.left);
      const initializer ***REMOVED*** visitNode(node.right, visitor, isExpression);
      return factory2.updateBinaryExpression(node, assignmentTarget, node.operatorToken, initializer);
    } else {
      return visitDestructuringAssignmentTarget(node);
    }
  }
  function visitAssignmentRestElement(node) {
    if (isLeftHandSideExpression(node.expression)) {
      const expression ***REMOVED*** visitDestructuringAssignmentTarget(node.expression);
      return factory2.updateSpreadElement(node, expression);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitArrayAssignmentElement(node) {
    Debug.assertNode(node, isArrayBindingOrAssignmentElement);
    if (isSpreadElement(node)) return visitAssignmentRestElement(node);
    if (!isOmittedExpression(node)) return visitAssignmentElement(node);
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentProperty(node) {
    const name ***REMOVED*** visitNode(node.name, visitor, isPropertyName);
    if (isAssignmentExpression(
      node.initializer,
      /*excludeCompoundAssignment*/
      true
    )) {
      const assignmentElement ***REMOVED*** visitAssignmentElement(node.initializer);
      return factory2.updatePropertyAssignment(node, name, assignmentElement);
    }
    if (isLeftHandSideExpression(node.initializer)) {
      const assignmentElement ***REMOVED*** visitDestructuringAssignmentTarget(node.initializer);
      return factory2.updatePropertyAssignment(node, name, assignmentElement);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitShorthandAssignmentProperty(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.objectAssignmentInitializer));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentRestProperty(node) {
    if (isLeftHandSideExpression(node.expression)) {
      const expression ***REMOVED*** visitDestructuringAssignmentTarget(node.expression);
      return factory2.updateSpreadAssignment(node, expression);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitObjectAssignmentElement(node) {
    Debug.assertNode(node, isObjectBindingOrAssignmentElement);
    if (isSpreadAssignment(node)) return visitAssignmentRestProperty(node);
    if (isShorthandPropertyAssignment(node)) return visitShorthandAssignmentProperty(node);
    if (isPropertyAssignment(node)) return visitAssignmentProperty(node);
    return visitEachChild(node, visitor, context);
  }
  function visitAssignmentPattern(node) {
    if (isArrayLiteralExpression(node)) {
      const elements ***REMOVED*** visitNodes2(node.elements, visitArrayAssignmentElement, isExpression);
      return factory2.updateArrayLiteralExpression(node, elements);
    } else {
      const properties ***REMOVED*** visitNodes2(node.properties, visitObjectAssignmentElement, isObjectLiteralElementLike);
      return factory2.updateObjectLiteralExpression(node, properties);
    }
  }
  function visitExportAssignment(node) {
    if (isNamedEvaluation(node, isAnonymousClassNeedingAssignedName)) {
      node ***REMOVED*** transformNamedEvaluation(context, node, canIgnoreEmptyStringLiteralInAssignedName(node.expression));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitParenthesizedExpression(node, discarded) {
    const visitorFunc ***REMOVED*** discarded ? discardedValueVisitor : visitor;
    const expression ***REMOVED*** visitNode(node.expression, visitorFunc, isExpression);
    return factory2.updateParenthesizedExpression(node, expression);
  }
  function visitPartiallyEmittedExpression(node, discarded) {
    const visitorFunc ***REMOVED*** discarded ? discardedValueVisitor : visitor;
    const expression ***REMOVED*** visitNode(node.expression, visitorFunc, isExpression);
    return factory2.updatePartiallyEmittedExpression(node, expression);
  }
  function injectPendingExpressionsCommon(pendingExpressions2, expression) {
    if (some(pendingExpressions2)) {
      if (expression) {
        if (isParenthesizedExpression(expression)) {
          pendingExpressions2.push(expression.expression);
          expression ***REMOVED*** factory2.updateParenthesizedExpression(expression, factory2.inlineExpressions(pendingExpressions2));
        } else {
          pendingExpressions2.push(expression);
          expression ***REMOVED*** factory2.inlineExpressions(pendingExpressions2);
        }
      } else {
        expression ***REMOVED*** factory2.inlineExpressions(pendingExpressions2);
      }
    }
    return expression;
  }
  function injectPendingExpressions(expression) {
    const result ***REMOVED*** injectPendingExpressionsCommon(pendingExpressions, expression);
    Debug.assertIsDefined(result);
    if (result !***REMOVED******REMOVED*** expression) {
      pendingExpressions ***REMOVED*** void 0;
    }
    return result;
  }
  function injectPendingInitializers(classInfo2, isStatic2, expression) {
    const result ***REMOVED*** injectPendingExpressionsCommon(isStatic2 ? classInfo2.pendingStaticInitializers : classInfo2.pendingInstanceInitializers, expression);
    if (result !***REMOVED******REMOVED*** expression) {
      if (isStatic2) {
        classInfo2.pendingStaticInitializers ***REMOVED*** void 0;
      } else {
        classInfo2.pendingInstanceInitializers ***REMOVED*** void 0;
      }
    }
    return result;
  }
  function transformAllDecoratorsOfDeclaration(allDecorators) {
    if (!allDecorators) {
      return void 0;
    }
    const decoratorExpressions ***REMOVED*** [];
    addRange(decoratorExpressions, map(allDecorators.decorators, transformDecorator));
    return decoratorExpressions;
  }
  function transformDecorator(decorator) {
    const expression ***REMOVED*** visitNode(decorator.expression, visitor, isExpression);
    setEmitFlags(expression, 3072 /* NoComments */);
    const innerExpression ***REMOVED*** skipOuterExpressions(expression);
    if (isAccessExpression(innerExpression)) {
      const { target, thisArg } ***REMOVED*** factory2.createCallBinding(
        expression,
        hoistVariableDeclaration,
        languageVersion,
        /*cacheIdentifiers*/
        true
      );
      return factory2.restoreOuterExpressions(expression, factory2.createFunctionBindCall(target, thisArg, []));
    }
    return expression;
  }
  function createDescriptorMethod(original, name, modifiers, asteriskToken, kind, parameters, body) {
    const func ***REMOVED*** factory2.createFunctionExpression(
      modifiers,
      asteriskToken,
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      parameters,
      /*type*/
      void 0,
      body ?? factory2.createBlock([])
    );
    setOriginalNode(func, original);
    setSourceMapRange(func, moveRangePastDecorators(original));
    setEmitFlags(func, 3072 /* NoComments */);
    const prefix ***REMOVED*** kind ***REMOVED******REMOVED******REMOVED*** "get" || kind ***REMOVED******REMOVED******REMOVED*** "set" ? kind : void 0;
    const functionName ***REMOVED*** factory2.createStringLiteralFromNode(
      name,
      /*isSingleQuote*/
      void 0
    );
    const namedFunction ***REMOVED*** emitHelpers().createSetFunctionNameHelper(func, functionName, prefix);
    const method ***REMOVED*** factory2.createPropertyAssignment(factory2.createIdentifier(kind), namedFunction);
    setOriginalNode(method, original);
    setSourceMapRange(method, moveRangePastDecorators(original));
    setEmitFlags(method, 3072 /* NoComments */);
    return method;
  }
  function createMethodDescriptorObject(node, modifiers) {
    return factory2.createObjectLiteralExpression([
      createDescriptorMethod(
        node,
        node.name,
        modifiers,
        node.asteriskToken,
        "value",
        visitNodes2(node.parameters, visitor, isParameter),
        visitNode(node.body, visitor, isBlock)
      )
    ]);
  }
  function createGetAccessorDescriptorObject(node, modifiers) {
    return factory2.createObjectLiteralExpression([
      createDescriptorMethod(
        node,
        node.name,
        modifiers,
        /*asteriskToken*/
        void 0,
        "get",
        [],
        visitNode(node.body, visitor, isBlock)
      )
    ]);
  }
  function createSetAccessorDescriptorObject(node, modifiers) {
    return factory2.createObjectLiteralExpression([
      createDescriptorMethod(
        node,
        node.name,
        modifiers,
        /*asteriskToken*/
        void 0,
        "set",
        visitNodes2(node.parameters, visitor, isParameter),
        visitNode(node.body, visitor, isBlock)
      )
    ]);
  }
  function createAccessorPropertyDescriptorObject(node, modifiers) {
    return factory2.createObjectLiteralExpression([
      createDescriptorMethod(
        node,
        node.name,
        modifiers,
        /*asteriskToken*/
        void 0,
        "get",
        [],
        factory2.createBlock([
          factory2.createReturnStatement(
            factory2.createPropertyAccessExpression(
              factory2.createThis(),
              factory2.getGeneratedPrivateNameForNode(node.name)
            )
          )
        ])
      ),
      createDescriptorMethod(
        node,
        node.name,
        modifiers,
        /*asteriskToken*/
        void 0,
        "set",
        [factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          "value"
        )],
        factory2.createBlock([
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(
                factory2.createThis(),
                factory2.getGeneratedPrivateNameForNode(node.name)
              ),
              factory2.createIdentifier("value")
            )
          )
        ])
      )
    ]);
  }
  function createMethodDescriptorForwarder(modifiers, name, descriptorName) {
    modifiers ***REMOVED*** visitNodes2(modifiers, (node) ***REMOVED***> isStaticModifier(node) ? node : void 0, isModifier);
    return factory2.createGetAccessorDeclaration(
      modifiers,
      name,
      [],
      /*type*/
      void 0,
      factory2.createBlock([
        factory2.createReturnStatement(
          factory2.createPropertyAccessExpression(
            descriptorName,
            factory2.createIdentifier("value")
          )
        )
      ])
    );
  }
  function createGetAccessorDescriptorForwarder(modifiers, name, descriptorName) {
    modifiers ***REMOVED*** visitNodes2(modifiers, (node) ***REMOVED***> isStaticModifier(node) ? node : void 0, isModifier);
    return factory2.createGetAccessorDeclaration(
      modifiers,
      name,
      [],
      /*type*/
      void 0,
      factory2.createBlock([
        factory2.createReturnStatement(
          factory2.createFunctionCallCall(
            factory2.createPropertyAccessExpression(
              descriptorName,
              factory2.createIdentifier("get")
            ),
            factory2.createThis(),
            []
          )
        )
      ])
    );
  }
  function createSetAccessorDescriptorForwarder(modifiers, name, descriptorName) {
    modifiers ***REMOVED*** visitNodes2(modifiers, (node) ***REMOVED***> isStaticModifier(node) ? node : void 0, isModifier);
    return factory2.createSetAccessorDeclaration(
      modifiers,
      name,
      [factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        "value"
      )],
      factory2.createBlock([
        factory2.createReturnStatement(
          factory2.createFunctionCallCall(
            factory2.createPropertyAccessExpression(
              descriptorName,
              factory2.createIdentifier("set")
            ),
            factory2.createThis(),
            [factory2.createIdentifier("value")]
          )
        )
      ])
    );
  }
  function createMetadata(name, classSuper2) {
    const varDecl ***REMOVED*** factory2.createVariableDeclaration(
      name,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      factory2.createConditionalExpression(
        factory2.createLogicalAnd(
          factory2.createTypeCheck(factory2.createIdentifier("Symbol"), "function"),
          factory2.createPropertyAccessExpression(factory2.createIdentifier("Symbol"), "metadata")
        ),
        factory2.createToken(58 /* QuestionToken */),
        factory2.createCallExpression(
          factory2.createPropertyAccessExpression(factory2.createIdentifier("Object"), "create"),
          /*typeArguments*/
          void 0,
          [classSuper2 ? createSymbolMetadataReference(classSuper2) : factory2.createNull()]
        ),
        factory2.createToken(59 /* ColonToken */),
        factory2.createVoidZero()
      )
    );
    return factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      factory2.createVariableDeclarationList([varDecl], 2 /* Const */)
    );
  }
  function createSymbolMetadata(target, value) {
    const defineProperty ***REMOVED*** factory2.createObjectDefinePropertyCall(
      target,
      factory2.createPropertyAccessExpression(factory2.createIdentifier("Symbol"), "metadata"),
      factory2.createPropertyDescriptor(
        { configurable: true, writable: true, enumerable: true, value },
        /*singleLine*/
        true
      )
    );
    return setEmitFlags(
      factory2.createIfStatement(value, factory2.createExpressionStatement(defineProperty)),
      1 /* SingleLine */
    );
  }
  function createSymbolMetadataReference(classSuper2) {
    return factory2.createBinaryExpression(
      factory2.createElementAccessExpression(
        classSuper2,
        factory2.createPropertyAccessExpression(factory2.createIdentifier("Symbol"), "metadata")
      ),
      61 /* QuestionQuestionToken */,
      factory2.createNull()
    );
  }
}

// src/compiler/transformers/es2017.ts
function transformES2017(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  let enabledSubstitutions ***REMOVED*** 0 /* None */;
  let enclosingSuperContainerFlags ***REMOVED*** 0;
  let enclosingFunctionParameterNames;
  let capturedSuperProperties;
  let hasSuperElementAccess;
  let lexicalArgumentsBinding;
  const substitutedSuperAccessors ***REMOVED*** [];
  let contextFlags ***REMOVED*** 0 /* None */;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    setContextFlag(1 /* NonTopLevel */, false);
    setContextFlag(2 /* HasLexicalThis */, !isEffectiveStrictModeSourceFile(node, compilerOptions));
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    return visited;
  }
  function setContextFlag(flag, val) {
    contextFlags ***REMOVED*** val ? contextFlags | flag : contextFlags & ~flag;
  }
  function inContext(flags) {
    return (contextFlags & flags) !***REMOVED******REMOVED*** 0;
  }
  function inTopLevelContext() {
    return !inContext(1 /* NonTopLevel */);
  }
  function inHasLexicalThisContext() {
    return inContext(2 /* HasLexicalThis */);
  }
  function doWithContext(flags, cb, value) {
    const contextFlagsToSet ***REMOVED*** flags & ~contextFlags;
    if (contextFlagsToSet) {
      setContextFlag(
        contextFlagsToSet,
        /*val*/
        true
      );
      const result ***REMOVED*** cb(value);
      setContextFlag(
        contextFlagsToSet,
        /*val*/
        false
      );
      return result;
    }
    return cb(value);
  }
  function visitDefault(node) {
    return visitEachChild(node, visitor, context);
  }
  function argumentsVisitor(node) {
    switch (node.kind) {
      case 219 /* FunctionExpression */:
      case 263 /* FunctionDeclaration */:
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 177 /* Constructor */:
        return node;
      case 170 /* Parameter */:
      case 209 /* BindingElement */:
      case 261 /* VariableDeclaration */:
        break;
      case 80 /* Identifier */:
        if (lexicalArgumentsBinding && resolver.isArgumentsLocalBinding(node)) {
          return lexicalArgumentsBinding;
        }
        break;
    }
    return visitEachChild(node, argumentsVisitor, context);
  }
  function visitor(node) {
    if ((node.transformFlags & 256 /* ContainsES2017 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return lexicalArgumentsBinding ? argumentsVisitor(node) : node;
    }
    switch (node.kind) {
      case 134 /* AsyncKeyword */:
        return void 0;
      case 224 /* AwaitExpression */:
        return visitAwaitExpression(node);
      case 175 /* MethodDeclaration */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitMethodDeclaration, node);
      case 263 /* FunctionDeclaration */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitFunctionDeclaration, node);
      case 219 /* FunctionExpression */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitFunctionExpression, node);
      case 220 /* ArrowFunction */:
        return doWithContext(1 /* NonTopLevel */, visitArrowFunction, node);
      case 212 /* PropertyAccessExpression */:
        if (capturedSuperProperties && isPropertyAccessExpression(node) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          capturedSuperProperties.add(node.name.escapedText);
        }
        return visitEachChild(node, visitor, context);
      case 213 /* ElementAccessExpression */:
        if (capturedSuperProperties && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          hasSuperElementAccess ***REMOVED*** true;
        }
        return visitEachChild(node, visitor, context);
      case 178 /* GetAccessor */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitGetAccessorDeclaration, node);
      case 179 /* SetAccessor */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitSetAccessorDeclaration, node);
      case 177 /* Constructor */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitConstructorDeclaration, node);
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        return doWithContext(1 /* NonTopLevel */ | 2 /* HasLexicalThis */, visitDefault, node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function asyncBodyVisitor(node) {
    if (isNodeWithPossibleHoistedDeclaration(node)) {
      switch (node.kind) {
        case 244 /* VariableStatement */:
          return visitVariableStatementInAsyncBody(node);
        case 249 /* ForStatement */:
          return visitForStatementInAsyncBody(node);
        case 250 /* ForInStatement */:
          return visitForInStatementInAsyncBody(node);
        case 251 /* ForOfStatement */:
          return visitForOfStatementInAsyncBody(node);
        case 300 /* CatchClause */:
          return visitCatchClauseInAsyncBody(node);
        case 242 /* Block */:
        case 256 /* SwitchStatement */:
        case 270 /* CaseBlock */:
        case 297 /* CaseClause */:
        case 298 /* DefaultClause */:
        case 259 /* TryStatement */:
        case 247 /* DoStatement */:
        case 248 /* WhileStatement */:
        case 246 /* IfStatement */:
        case 255 /* WithStatement */:
        case 257 /* LabeledStatement */:
          return visitEachChild(node, asyncBodyVisitor, context);
        default:
          return Debug.assertNever(node, "Unhandled node.");
      }
    }
    return visitor(node);
  }
  function visitCatchClauseInAsyncBody(node) {
    const catchClauseNames ***REMOVED*** /* @__PURE__ */ new Set();
    recordDeclarationName(node.variableDeclaration, catchClauseNames);
    let catchClauseUnshadowedNames;
    catchClauseNames.forEach((_, escapedName) ***REMOVED***> {
      if (enclosingFunctionParameterNames.has(escapedName)) {
        if (!catchClauseUnshadowedNames) {
          catchClauseUnshadowedNames ***REMOVED*** new Set(enclosingFunctionParameterNames);
        }
        catchClauseUnshadowedNames.delete(escapedName);
      }
    });
    if (catchClauseUnshadowedNames) {
      const savedEnclosingFunctionParameterNames ***REMOVED*** enclosingFunctionParameterNames;
      enclosingFunctionParameterNames ***REMOVED*** catchClauseUnshadowedNames;
      const result ***REMOVED*** visitEachChild(node, asyncBodyVisitor, context);
      enclosingFunctionParameterNames ***REMOVED*** savedEnclosingFunctionParameterNames;
      return result;
    } else {
      return visitEachChild(node, asyncBodyVisitor, context);
    }
  }
  function visitVariableStatementInAsyncBody(node) {
    if (isVariableDeclarationListWithCollidingName(node.declarationList)) {
      const expression ***REMOVED*** visitVariableDeclarationListWithCollidingNames(
        node.declarationList,
        /*hasReceiver*/
        false
      );
      return expression ? factory2.createExpressionStatement(expression) : void 0;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForInStatementInAsyncBody(node) {
    return factory2.updateForInStatement(
      node,
      isVariableDeclarationListWithCollidingName(node.initializer) ? visitVariableDeclarationListWithCollidingNames(
        node.initializer,
        /*hasReceiver*/
        true
      ) : Debug.checkDefined(visitNode(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      visitIterationBody(node.statement, asyncBodyVisitor, context)
    );
  }
  function visitForOfStatementInAsyncBody(node) {
    return factory2.updateForOfStatement(
      node,
      visitNode(node.awaitModifier, visitor, isAwaitKeyword),
      isVariableDeclarationListWithCollidingName(node.initializer) ? visitVariableDeclarationListWithCollidingNames(
        node.initializer,
        /*hasReceiver*/
        true
      ) : Debug.checkDefined(visitNode(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      visitIterationBody(node.statement, asyncBodyVisitor, context)
    );
  }
  function visitForStatementInAsyncBody(node) {
    const initializer ***REMOVED*** node.initializer;
    return factory2.updateForStatement(
      node,
      isVariableDeclarationListWithCollidingName(initializer) ? visitVariableDeclarationListWithCollidingNames(
        initializer,
        /*hasReceiver*/
        false
      ) : visitNode(node.initializer, visitor, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, visitor, isExpression),
      visitIterationBody(node.statement, asyncBodyVisitor, context)
    );
  }
  function visitAwaitExpression(node) {
    if (inTopLevelContext()) {
      return visitEachChild(node, visitor, context);
    }
    return setOriginalNode(
      setTextRange(
        factory2.createYieldExpression(
          /*asteriskToken*/
          void 0,
          visitNode(node.expression, visitor, isExpression)
        ),
        node
      ),
      node
    );
  }
  function visitConstructorDeclaration(node) {
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const updated ***REMOVED*** factory2.updateConstructorDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifier),
      visitParameterList(node.parameters, visitor, context),
      transformMethodBody(node)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitMethodDeclaration(node) {
    let parameters;
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const updated ***REMOVED*** factory2.updateMethodDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifierLike),
      node.asteriskToken,
      node.name,
      /*questionToken*/
      void 0,
      /*typeParameters*/
      void 0,
      parameters ***REMOVED*** functionFlags & 2 /* Async */ ? transformAsyncFunctionParameterList(node) : visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      functionFlags & 2 /* Async */ ? transformAsyncFunctionBody(node, parameters) : transformMethodBody(node)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitGetAccessorDeclaration(node) {
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const updated ***REMOVED*** factory2.updateGetAccessorDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifierLike),
      node.name,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      transformMethodBody(node)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitSetAccessorDeclaration(node) {
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const updated ***REMOVED*** factory2.updateSetAccessorDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifierLike),
      node.name,
      visitParameterList(node.parameters, visitor, context),
      transformMethodBody(node)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitFunctionDeclaration(node) {
    let parameters;
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    const updated ***REMOVED*** factory2.updateFunctionDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifierLike),
      node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      parameters ***REMOVED*** functionFlags & 2 /* Async */ ? transformAsyncFunctionParameterList(node) : visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      functionFlags & 2 /* Async */ ? transformAsyncFunctionBody(node, parameters) : visitFunctionBody(node.body, visitor, context)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitFunctionExpression(node) {
    let parameters;
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    lexicalArgumentsBinding ***REMOVED*** void 0;
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    const updated ***REMOVED*** factory2.updateFunctionExpression(
      node,
      visitNodes2(node.modifiers, visitor, isModifier),
      node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      parameters ***REMOVED*** functionFlags & 2 /* Async */ ? transformAsyncFunctionParameterList(node) : visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      functionFlags & 2 /* Async */ ? transformAsyncFunctionBody(node, parameters) : visitFunctionBody(node.body, visitor, context)
    );
    lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    return updated;
  }
  function visitArrowFunction(node) {
    let parameters;
    const functionFlags ***REMOVED*** getFunctionFlags(node);
    return factory2.updateArrowFunction(
      node,
      visitNodes2(node.modifiers, visitor, isModifier),
      /*typeParameters*/
      void 0,
      parameters ***REMOVED*** functionFlags & 2 /* Async */ ? transformAsyncFunctionParameterList(node) : visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      node.equalsGreaterThanToken,
      functionFlags & 2 /* Async */ ? transformAsyncFunctionBody(node, parameters) : visitFunctionBody(node.body, visitor, context)
    );
  }
  function recordDeclarationName({ name }, names) {
    if (isIdentifier(name)) {
      names.add(name.escapedText);
    } else {
      for (const element of name.elements) {
        if (!isOmittedExpression(element)) {
          recordDeclarationName(element, names);
        }
      }
    }
  }
  function isVariableDeclarationListWithCollidingName(node) {
    return !!node && isVariableDeclarationList(node) && !(node.flags & 7 /* BlockScoped */) && node.declarations.some(collidesWithParameterName);
  }
  function visitVariableDeclarationListWithCollidingNames(node, hasReceiver) {
    hoistVariableDeclarationList(node);
    const variables ***REMOVED*** getInitializedVariables(node);
    if (variables.length ***REMOVED******REMOVED******REMOVED*** 0) {
      if (hasReceiver) {
        return visitNode(factory2.converters.convertToAssignmentElementTarget(node.declarations[0].name), visitor, isExpression);
      }
      return void 0;
    }
    return factory2.inlineExpressions(map(variables, transformInitializedVariable));
  }
  function hoistVariableDeclarationList(node) {
    forEach(node.declarations, hoistVariable);
  }
  function hoistVariable({ name }) {
    if (isIdentifier(name)) {
      hoistVariableDeclaration(name);
    } else {
      for (const element of name.elements) {
        if (!isOmittedExpression(element)) {
          hoistVariable(element);
        }
      }
    }
  }
  function transformInitializedVariable(node) {
    const converted ***REMOVED*** setSourceMapRange(
      factory2.createAssignment(
        factory2.converters.convertToAssignmentElementTarget(node.name),
        node.initializer
      ),
      node
    );
    return Debug.checkDefined(visitNode(converted, visitor, isExpression));
  }
  function collidesWithParameterName({ name }) {
    if (isIdentifier(name)) {
      return enclosingFunctionParameterNames.has(name.escapedText);
    } else {
      for (const element of name.elements) {
        if (!isOmittedExpression(element) && collidesWithParameterName(element)) {
          return true;
        }
      }
    }
    return false;
  }
  function transformMethodBody(node) {
    Debug.assertIsDefined(node.body);
    const savedCapturedSuperProperties ***REMOVED*** capturedSuperProperties;
    const savedHasSuperElementAccess ***REMOVED*** hasSuperElementAccess;
    capturedSuperProperties ***REMOVED*** /* @__PURE__ */ new Set();
    hasSuperElementAccess ***REMOVED*** false;
    let updated ***REMOVED*** visitFunctionBody(node.body, visitor, context);
    const originalMethod ***REMOVED*** getOriginalNode(node, isFunctionLikeDeclaration);
    const emitSuperHelpers ***REMOVED*** languageVersion >***REMOVED*** 2 /* ES2015 */ && (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */) || resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */)) && (getFunctionFlags(originalMethod) & 3 /* AsyncGenerator */) !***REMOVED******REMOVED*** 3 /* AsyncGenerator */;
    if (emitSuperHelpers) {
      enableSubstitutionForAsyncMethodsWithSuper();
      if (capturedSuperProperties.size) {
        const variableStatement ***REMOVED*** createSuperAccessVariableStatement(factory2, resolver, node, capturedSuperProperties);
        substitutedSuperAccessors[getNodeId(variableStatement)] ***REMOVED*** true;
        const statements ***REMOVED*** updated.statements.slice();
        insertStatementsAfterStandardPrologue(statements, [variableStatement]);
        updated ***REMOVED*** factory2.updateBlock(updated, statements);
      }
      if (hasSuperElementAccess) {
        if (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */)) {
          addEmitHelper(updated, advancedAsyncSuperHelper);
        } else if (resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */)) {
          addEmitHelper(updated, asyncSuperHelper);
        }
      }
    }
    capturedSuperProperties ***REMOVED*** savedCapturedSuperProperties;
    hasSuperElementAccess ***REMOVED*** savedHasSuperElementAccess;
    return updated;
  }
  function createCaptureArgumentsStatement() {
    Debug.assert(lexicalArgumentsBinding);
    const variable ***REMOVED*** factory2.createVariableDeclaration(
      lexicalArgumentsBinding,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      factory2.createIdentifier("arguments")
    );
    const statement ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      [variable]
    );
    startOnNewLine(statement);
    addEmitFlags(statement, 2097152 /* CustomPrologue */);
    return statement;
  }
  function transformAsyncFunctionParameterList(node) {
    if (isSimpleParameterList(node.parameters)) {
      return visitParameterList(node.parameters, visitor, context);
    }
    const newParameters ***REMOVED*** [];
    for (const parameter of node.parameters) {
      if (parameter.initializer || parameter.dotDotDotToken) {
        if (node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */) {
          const restParameter ***REMOVED*** factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            factory2.createToken(26 /* DotDotDotToken */),
            factory2.createUniqueName("args", 8 /* ReservedInNestedScopes */)
          );
          newParameters.push(restParameter);
        }
        break;
      }
      const newParameter ***REMOVED*** factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        factory2.getGeneratedNameForNode(parameter.name, 8 /* ReservedInNestedScopes */)
      );
      newParameters.push(newParameter);
    }
    const newParametersArray ***REMOVED*** factory2.createNodeArray(newParameters);
    setTextRange(newParametersArray, node.parameters);
    return newParametersArray;
  }
  function transformAsyncFunctionBody(node, outerParameters) {
    const innerParameters ***REMOVED*** !isSimpleParameterList(node.parameters) ? visitParameterList(node.parameters, visitor, context) : void 0;
    resumeLexicalEnvironment();
    const original ***REMOVED*** getOriginalNode(node, isFunctionLike);
    const nodeType ***REMOVED*** original.type;
    const promiseConstructor ***REMOVED*** languageVersion < 2 /* ES2015 */ ? getPromiseConstructor(nodeType) : void 0;
    const isArrowFunction2 ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */;
    const savedLexicalArgumentsBinding ***REMOVED*** lexicalArgumentsBinding;
    const hasLexicalArguments ***REMOVED*** resolver.hasNodeCheckFlag(node, 512 /* CaptureArguments */);
    const captureLexicalArguments ***REMOVED*** hasLexicalArguments && !lexicalArgumentsBinding;
    if (captureLexicalArguments) {
      lexicalArgumentsBinding ***REMOVED*** factory2.createUniqueName("arguments");
    }
    let argumentsExpression;
    if (innerParameters) {
      if (isArrowFunction2) {
        const parameterBindings ***REMOVED*** [];
        Debug.assert(outerParameters.length <***REMOVED*** node.parameters.length);
        for (let i ***REMOVED*** 0; i < node.parameters.length; i++) {
          Debug.assert(i < outerParameters.length);
          const originalParameter ***REMOVED*** node.parameters[i];
          const outerParameter ***REMOVED*** outerParameters[i];
          Debug.assertNode(outerParameter.name, isIdentifier);
          if (originalParameter.initializer || originalParameter.dotDotDotToken) {
            Debug.assert(i ***REMOVED******REMOVED******REMOVED*** outerParameters.length - 1);
            parameterBindings.push(factory2.createSpreadElement(outerParameter.name));
            break;
          }
          parameterBindings.push(outerParameter.name);
        }
        argumentsExpression ***REMOVED*** factory2.createArrayLiteralExpression(parameterBindings);
      } else {
        argumentsExpression ***REMOVED*** factory2.createIdentifier("arguments");
      }
    }
    const savedEnclosingFunctionParameterNames ***REMOVED*** enclosingFunctionParameterNames;
    enclosingFunctionParameterNames ***REMOVED*** /* @__PURE__ */ new Set();
    for (const parameter of node.parameters) {
      recordDeclarationName(parameter, enclosingFunctionParameterNames);
    }
    const savedCapturedSuperProperties ***REMOVED*** capturedSuperProperties;
    const savedHasSuperElementAccess ***REMOVED*** hasSuperElementAccess;
    if (!isArrowFunction2) {
      capturedSuperProperties ***REMOVED*** /* @__PURE__ */ new Set();
      hasSuperElementAccess ***REMOVED*** false;
    }
    const hasLexicalThis ***REMOVED*** inHasLexicalThisContext();
    let asyncBody ***REMOVED*** transformAsyncFunctionBodyWorker(node.body);
    asyncBody ***REMOVED*** factory2.updateBlock(asyncBody, factory2.mergeLexicalEnvironment(asyncBody.statements, endLexicalEnvironment()));
    let result;
    if (!isArrowFunction2) {
      const statements ***REMOVED*** [];
      statements.push(
        factory2.createReturnStatement(
          emitHelpers().createAwaiterHelper(
            hasLexicalThis,
            argumentsExpression,
            promiseConstructor,
            innerParameters,
            asyncBody
          )
        )
      );
      const emitSuperHelpers ***REMOVED*** languageVersion >***REMOVED*** 2 /* ES2015 */ && (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */) || resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */));
      if (emitSuperHelpers) {
        enableSubstitutionForAsyncMethodsWithSuper();
        if (capturedSuperProperties.size) {
          const variableStatement ***REMOVED*** createSuperAccessVariableStatement(factory2, resolver, node, capturedSuperProperties);
          substitutedSuperAccessors[getNodeId(variableStatement)] ***REMOVED*** true;
          insertStatementsAfterStandardPrologue(statements, [variableStatement]);
        }
      }
      if (captureLexicalArguments) {
        insertStatementsAfterStandardPrologue(statements, [createCaptureArgumentsStatement()]);
      }
      const block ***REMOVED*** factory2.createBlock(
        statements,
        /*multiLine*/
        true
      );
      setTextRange(block, node.body);
      if (emitSuperHelpers && hasSuperElementAccess) {
        if (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */)) {
          addEmitHelper(block, advancedAsyncSuperHelper);
        } else if (resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */)) {
          addEmitHelper(block, asyncSuperHelper);
        }
      }
      result ***REMOVED*** block;
    } else {
      result ***REMOVED*** emitHelpers().createAwaiterHelper(
        hasLexicalThis,
        argumentsExpression,
        promiseConstructor,
        innerParameters,
        asyncBody
      );
      if (captureLexicalArguments) {
        const block ***REMOVED*** factory2.converters.convertToFunctionBlock(result);
        result ***REMOVED*** factory2.updateBlock(block, factory2.mergeLexicalEnvironment(block.statements, [createCaptureArgumentsStatement()]));
      }
    }
    enclosingFunctionParameterNames ***REMOVED*** savedEnclosingFunctionParameterNames;
    if (!isArrowFunction2) {
      capturedSuperProperties ***REMOVED*** savedCapturedSuperProperties;
      hasSuperElementAccess ***REMOVED*** savedHasSuperElementAccess;
      lexicalArgumentsBinding ***REMOVED*** savedLexicalArgumentsBinding;
    }
    return result;
  }
  function transformAsyncFunctionBodyWorker(body, start) {
    if (isBlock(body)) {
      return factory2.updateBlock(body, visitNodes2(body.statements, asyncBodyVisitor, isStatement, start));
    } else {
      return factory2.converters.convertToFunctionBlock(Debug.checkDefined(visitNode(body, asyncBodyVisitor, isConciseBody)));
    }
  }
  function getPromiseConstructor(type) {
    const typeName ***REMOVED*** type && getEntityNameFromTypeNode(type);
    if (typeName && isEntityName(typeName)) {
      const serializationKind ***REMOVED*** resolver.getTypeReferenceSerializationKind(typeName);
      if (serializationKind ***REMOVED******REMOVED******REMOVED*** 1 /* TypeWithConstructSignatureAndValue */ || serializationKind ***REMOVED******REMOVED******REMOVED*** 0 /* Unknown */) {
        return typeName;
      }
    }
    return void 0;
  }
  function enableSubstitutionForAsyncMethodsWithSuper() {
    if ((enabledSubstitutions & 1 /* AsyncMethodsWithSuper */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 1 /* AsyncMethodsWithSuper */;
      context.enableSubstitution(214 /* CallExpression */);
      context.enableSubstitution(212 /* PropertyAccessExpression */);
      context.enableSubstitution(213 /* ElementAccessExpression */);
      context.enableEmitNotification(264 /* ClassDeclaration */);
      context.enableEmitNotification(175 /* MethodDeclaration */);
      context.enableEmitNotification(178 /* GetAccessor */);
      context.enableEmitNotification(179 /* SetAccessor */);
      context.enableEmitNotification(177 /* Constructor */);
      context.enableEmitNotification(244 /* VariableStatement */);
    }
  }
  function onEmitNode(hint, node, emitCallback) {
    if (enabledSubstitutions & 1 /* AsyncMethodsWithSuper */ && isSuperContainer(node)) {
      const superContainerFlags ***REMOVED*** (resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */) ? 128 /* MethodWithSuperPropertyAccessInAsync */ : 0) | (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */) ? 256 /* MethodWithSuperPropertyAssignmentInAsync */ : 0);
      if (superContainerFlags !***REMOVED******REMOVED*** enclosingSuperContainerFlags) {
        const savedEnclosingSuperContainerFlags ***REMOVED*** enclosingSuperContainerFlags;
        enclosingSuperContainerFlags ***REMOVED*** superContainerFlags;
        previousOnEmitNode(hint, node, emitCallback);
        enclosingSuperContainerFlags ***REMOVED*** savedEnclosingSuperContainerFlags;
        return;
      }
    } else if (enabledSubstitutions && substitutedSuperAccessors[getNodeId(node)]) {
      const savedEnclosingSuperContainerFlags ***REMOVED*** enclosingSuperContainerFlags;
      enclosingSuperContainerFlags ***REMOVED*** 0;
      previousOnEmitNode(hint, node, emitCallback);
      enclosingSuperContainerFlags ***REMOVED*** savedEnclosingSuperContainerFlags;
      return;
    }
    previousOnEmitNode(hint, node, emitCallback);
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */ && enclosingSuperContainerFlags) {
      return substituteExpression(node);
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 212 /* PropertyAccessExpression */:
        return substitutePropertyAccessExpression(node);
      case 213 /* ElementAccessExpression */:
        return substituteElementAccessExpression(node);
      case 214 /* CallExpression */:
        return substituteCallExpression(node);
    }
    return node;
  }
  function substitutePropertyAccessExpression(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return setTextRange(
        factory2.createPropertyAccessExpression(
          factory2.createUniqueName("_super", 16 /* Optimistic */ | 32 /* FileLevel */),
          node.name
        ),
        node
      );
    }
    return node;
  }
  function substituteElementAccessExpression(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return createSuperElementAccessInAsyncMethod(
        node.argumentExpression,
        node
      );
    }
    return node;
  }
  function substituteCallExpression(node) {
    const expression ***REMOVED*** node.expression;
    if (isSuperProperty(expression)) {
      const argumentExpression ***REMOVED*** isPropertyAccessExpression(expression) ? substitutePropertyAccessExpression(expression) : substituteElementAccessExpression(expression);
      return factory2.createCallExpression(
        factory2.createPropertyAccessExpression(argumentExpression, "call"),
        /*typeArguments*/
        void 0,
        [
          factory2.createThis(),
          ...node.arguments
        ]
      );
    }
    return node;
  }
  function isSuperContainer(node) {
    const kind ***REMOVED*** node.kind;
    return kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
  }
  function createSuperElementAccessInAsyncMethod(argumentExpression, location) {
    if (enclosingSuperContainerFlags & 256 /* MethodWithSuperPropertyAssignmentInAsync */) {
      return setTextRange(
        factory2.createPropertyAccessExpression(
          factory2.createCallExpression(
            factory2.createUniqueName("_superIndex", 16 /* Optimistic */ | 32 /* FileLevel */),
            /*typeArguments*/
            void 0,
            [argumentExpression]
          ),
          "value"
        ),
        location
      );
    } else {
      return setTextRange(
        factory2.createCallExpression(
          factory2.createUniqueName("_superIndex", 16 /* Optimistic */ | 32 /* FileLevel */),
          /*typeArguments*/
          void 0,
          [argumentExpression]
        ),
        location
      );
    }
  }
}
function createSuperAccessVariableStatement(factory2, resolver, node, names) {
  const hasBinding ***REMOVED*** resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */);
  const accessors ***REMOVED*** [];
  names.forEach((_, key) ***REMOVED***> {
    const name ***REMOVED*** unescapeLeadingUnderscores(key);
    const getterAndSetter ***REMOVED*** [];
    getterAndSetter.push(factory2.createPropertyAssignment(
      "get",
      factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        /* parameters */
        [],
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        setEmitFlags(
          factory2.createPropertyAccessExpression(
            setEmitFlags(
              factory2.createSuper(),
              8 /* NoSubstitution */
            ),
            name
          ),
          8 /* NoSubstitution */
        )
      )
    ));
    if (hasBinding) {
      getterAndSetter.push(
        factory2.createPropertyAssignment(
          "set",
          factory2.createArrowFunction(
            /*modifiers*/
            void 0,
            /*typeParameters*/
            void 0,
            /* parameters */
            [
              factory2.createParameterDeclaration(
                /*modifiers*/
                void 0,
                /*dotDotDotToken*/
                void 0,
                "v",
                /*questionToken*/
                void 0,
                /*type*/
                void 0,
                /*initializer*/
                void 0
              )
            ],
            /*type*/
            void 0,
            /*equalsGreaterThanToken*/
            void 0,
            factory2.createAssignment(
              setEmitFlags(
                factory2.createPropertyAccessExpression(
                  setEmitFlags(
                    factory2.createSuper(),
                    8 /* NoSubstitution */
                  ),
                  name
                ),
                8 /* NoSubstitution */
              ),
              factory2.createIdentifier("v")
            )
          )
        )
      );
    }
    accessors.push(
      factory2.createPropertyAssignment(
        name,
        factory2.createObjectLiteralExpression(getterAndSetter)
      )
    );
  });
  return factory2.createVariableStatement(
    /*modifiers*/
    void 0,
    factory2.createVariableDeclarationList(
      [
        factory2.createVariableDeclaration(
          factory2.createUniqueName("_super", 16 /* Optimistic */ | 32 /* FileLevel */),
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          factory2.createCallExpression(
            factory2.createPropertyAccessExpression(
              factory2.createIdentifier("Object"),
              "create"
            ),
            /*typeArguments*/
            void 0,
            [
              factory2.createNull(),
              factory2.createObjectLiteralExpression(
                accessors,
                /*multiLine*/
                true
              )
            ]
          )
        )
      ],
      2 /* Const */
    )
  );
}

// src/compiler/transformers/es2018.ts
function transformES2018(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  let exportedVariableStatement ***REMOVED*** false;
  let enabledSubstitutions ***REMOVED*** 0 /* None */;
  let enclosingFunctionFlags;
  let parametersWithPrecedingObjectRestOrSpread;
  let enclosingSuperContainerFlags ***REMOVED*** 0;
  let hierarchyFacts ***REMOVED*** 0;
  let currentSourceFile;
  let taggedTemplateStringDeclarations;
  let capturedSuperProperties;
  let hasSuperElementAccess;
  const substitutedSuperAccessors ***REMOVED*** [];
  return chainBundle(context, transformSourceFile);
  function affectsSubtree(excludeFacts, includeFacts) {
    return hierarchyFacts !***REMOVED******REMOVED*** (hierarchyFacts & ~excludeFacts | includeFacts);
  }
  function enterSubtree(excludeFacts, includeFacts) {
    const ancestorFacts ***REMOVED*** hierarchyFacts;
    hierarchyFacts ***REMOVED*** (hierarchyFacts & ~excludeFacts | includeFacts) & 3 /* AncestorFactsMask */;
    return ancestorFacts;
  }
  function exitSubtree(ancestorFacts) {
    hierarchyFacts ***REMOVED*** ancestorFacts;
  }
  function recordTaggedTemplateString(temp) {
    taggedTemplateStringDeclarations ***REMOVED*** append(
      taggedTemplateStringDeclarations,
      factory2.createVariableDeclaration(temp)
    );
  }
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    const visited ***REMOVED*** visitSourceFile(node);
    addEmitHelpers(visited, context.readEmitHelpers());
    currentSourceFile ***REMOVED*** void 0;
    taggedTemplateStringDeclarations ***REMOVED*** void 0;
    return visited;
  }
  function visitor(node) {
    return visitorWorker(
      node,
      /*expressionResultIsUnused*/
      false
    );
  }
  function visitorWithUnusedExpressionResult(node) {
    return visitorWorker(
      node,
      /*expressionResultIsUnused*/
      true
    );
  }
  function visitorNoAsyncModifier(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 134 /* AsyncKeyword */) {
      return void 0;
    }
    return node;
  }
  function doWithHierarchyFacts(cb, value, excludeFacts, includeFacts) {
    if (affectsSubtree(excludeFacts, includeFacts)) {
      const ancestorFacts ***REMOVED*** enterSubtree(excludeFacts, includeFacts);
      const result ***REMOVED*** cb(value);
      exitSubtree(ancestorFacts);
      return result;
    }
    return cb(value);
  }
  function visitDefault(node) {
    return visitEachChild(node, visitor, context);
  }
  function visitorWorker(node, expressionResultIsUnused2) {
    if ((node.transformFlags & 128 /* ContainsES2018 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    switch (node.kind) {
      case 224 /* AwaitExpression */:
        return visitAwaitExpression(node);
      case 230 /* YieldExpression */:
        return visitYieldExpression(node);
      case 254 /* ReturnStatement */:
        return visitReturnStatement(node);
      case 257 /* LabeledStatement */:
        return visitLabeledStatement(node);
      case 211 /* ObjectLiteralExpression */:
        return visitObjectLiteralExpression(node);
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(node, expressionResultIsUnused2);
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(node, expressionResultIsUnused2);
      case 300 /* CatchClause */:
        return visitCatchClause(node);
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 261 /* VariableDeclaration */:
        return visitVariableDeclaration(node);
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
      case 250 /* ForInStatement */:
        return doWithHierarchyFacts(
          visitDefault,
          node,
          0 /* IterationStatementExcludes */,
          2 /* IterationStatementIncludes */
        );
      case 251 /* ForOfStatement */:
        return visitForOfStatement(
          node,
          /*outermostLabeledStatement*/
          void 0
        );
      case 249 /* ForStatement */:
        return doWithHierarchyFacts(
          visitForStatement,
          node,
          0 /* IterationStatementExcludes */,
          2 /* IterationStatementIncludes */
        );
      case 223 /* VoidExpression */:
        return visitVoidExpression(node);
      case 177 /* Constructor */:
        return doWithHierarchyFacts(
          visitConstructorDeclaration,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 175 /* MethodDeclaration */:
        return doWithHierarchyFacts(
          visitMethodDeclaration,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 178 /* GetAccessor */:
        return doWithHierarchyFacts(
          visitGetAccessorDeclaration,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 179 /* SetAccessor */:
        return doWithHierarchyFacts(
          visitSetAccessorDeclaration,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 263 /* FunctionDeclaration */:
        return doWithHierarchyFacts(
          visitFunctionDeclaration,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 219 /* FunctionExpression */:
        return doWithHierarchyFacts(
          visitFunctionExpression,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      case 220 /* ArrowFunction */:
        return doWithHierarchyFacts(
          visitArrowFunction,
          node,
          2 /* ArrowFunctionExcludes */,
          0 /* ArrowFunctionIncludes */
        );
      case 170 /* Parameter */:
        return visitParameter(node);
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(node, expressionResultIsUnused2);
      case 216 /* TaggedTemplateExpression */:
        return visitTaggedTemplateExpression(node);
      case 212 /* PropertyAccessExpression */:
        if (capturedSuperProperties && isPropertyAccessExpression(node) && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          capturedSuperProperties.add(node.name.escapedText);
        }
        return visitEachChild(node, visitor, context);
      case 213 /* ElementAccessExpression */:
        if (capturedSuperProperties && node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
          hasSuperElementAccess ***REMOVED*** true;
        }
        return visitEachChild(node, visitor, context);
      case 264 /* ClassDeclaration */:
      case 232 /* ClassExpression */:
        return doWithHierarchyFacts(
          visitDefault,
          node,
          2 /* ClassOrFunctionExcludes */,
          1 /* ClassOrFunctionIncludes */
        );
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitAwaitExpression(node) {
    if (enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */) {
      return setOriginalNode(
        setTextRange(
          factory2.createYieldExpression(
            /*asteriskToken*/
            void 0,
            emitHelpers().createAwaitHelper(visitNode(node.expression, visitor, isExpression))
          ),
          /*location*/
          node
        ),
        node
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitYieldExpression(node) {
    if (enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */) {
      if (node.asteriskToken) {
        const expression ***REMOVED*** visitNode(Debug.checkDefined(node.expression), visitor, isExpression);
        return setOriginalNode(
          setTextRange(
            factory2.createYieldExpression(
              /*asteriskToken*/
              void 0,
              emitHelpers().createAwaitHelper(
                factory2.updateYieldExpression(
                  node,
                  node.asteriskToken,
                  setTextRange(
                    emitHelpers().createAsyncDelegatorHelper(
                      setTextRange(
                        emitHelpers().createAsyncValuesHelper(expression),
                        expression
                      )
                    ),
                    expression
                  )
                )
              )
            ),
            node
          ),
          node
        );
      }
      return setOriginalNode(
        setTextRange(
          factory2.createYieldExpression(
            /*asteriskToken*/
            void 0,
            createDownlevelAwait(
              node.expression ? visitNode(node.expression, visitor, isExpression) : factory2.createVoidZero()
            )
          ),
          node
        ),
        node
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitReturnStatement(node) {
    if (enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */) {
      return factory2.updateReturnStatement(
        node,
        createDownlevelAwait(
          node.expression ? visitNode(node.expression, visitor, isExpression) : factory2.createVoidZero()
        )
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitLabeledStatement(node) {
    if (enclosingFunctionFlags & 2 /* Async */) {
      const statement ***REMOVED*** unwrapInnermostStatementOfLabel(node);
      if (statement.kind ***REMOVED******REMOVED******REMOVED*** 251 /* ForOfStatement */ && statement.awaitModifier) {
        return visitForOfStatement(statement, node);
      }
      return factory2.restoreEnclosingLabel(visitNode(statement, visitor, isStatement, factory2.liftToBlock), node);
    }
    return visitEachChild(node, visitor, context);
  }
  function chunkObjectLiteralElements(elements) {
    let chunkObject;
    const objects ***REMOVED*** [];
    for (const e of elements) {
      if (e.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
        if (chunkObject) {
          objects.push(factory2.createObjectLiteralExpression(chunkObject));
          chunkObject ***REMOVED*** void 0;
        }
        const target ***REMOVED*** e.expression;
        objects.push(visitNode(target, visitor, isExpression));
      } else {
        chunkObject ***REMOVED*** append(
          chunkObject,
          e.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */ ? factory2.createPropertyAssignment(e.name, visitNode(e.initializer, visitor, isExpression)) : visitNode(e, visitor, isObjectLiteralElementLike)
        );
      }
    }
    if (chunkObject) {
      objects.push(factory2.createObjectLiteralExpression(chunkObject));
    }
    return objects;
  }
  function visitObjectLiteralExpression(node) {
    if (node.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
      const objects ***REMOVED*** chunkObjectLiteralElements(node.properties);
      if (objects.length && objects[0].kind !***REMOVED******REMOVED*** 211 /* ObjectLiteralExpression */) {
        objects.unshift(factory2.createObjectLiteralExpression());
      }
      let expression ***REMOVED*** objects[0];
      if (objects.length > 1) {
        for (let i ***REMOVED*** 1; i < objects.length; i++) {
          expression ***REMOVED*** emitHelpers().createAssignHelper([expression, objects[i]]);
        }
        return expression;
      } else {
        return emitHelpers().createAssignHelper(objects);
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitExpressionStatement(node) {
    return visitEachChild(node, visitorWithUnusedExpressionResult, context);
  }
  function visitParenthesizedExpression(node, expressionResultIsUnused2) {
    return visitEachChild(node, expressionResultIsUnused2 ? visitorWithUnusedExpressionResult : visitor, context);
  }
  function visitSourceFile(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(
      2 /* SourceFileExcludes */,
      isEffectiveStrictModeSourceFile(node, compilerOptions) ? 0 /* StrictModeSourceFileIncludes */ : 1 /* SourceFileIncludes */
    );
    exportedVariableStatement ***REMOVED*** false;
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    const statement ***REMOVED*** concatenate(
      visited.statements,
      taggedTemplateStringDeclarations && [
        factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          factory2.createVariableDeclarationList(taggedTemplateStringDeclarations)
        )
      ]
    );
    const result ***REMOVED*** factory2.updateSourceFile(visited, setTextRange(factory2.createNodeArray(statement), node.statements));
    exitSubtree(ancestorFacts);
    return result;
  }
  function visitTaggedTemplateExpression(node) {
    return processTaggedTemplateExpression(
      context,
      node,
      visitor,
      currentSourceFile,
      recordTaggedTemplateString,
      0 /* LiftRestriction */
    );
  }
  function visitBinaryExpression(node, expressionResultIsUnused2) {
    if (isDestructuringAssignment(node) && containsObjectRestOrSpread(node.left)) {
      return flattenDestructuringAssignment(
        node,
        visitor,
        context,
        1 /* ObjectRest */,
        !expressionResultIsUnused2
      );
    }
    if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      return factory2.updateBinaryExpression(
        node,
        visitNode(node.left, visitorWithUnusedExpressionResult, isExpression),
        node.operatorToken,
        visitNode(node.right, expressionResultIsUnused2 ? visitorWithUnusedExpressionResult : visitor, isExpression)
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCommaListExpression(node, expressionResultIsUnused2) {
    if (expressionResultIsUnused2) {
      return visitEachChild(node, visitorWithUnusedExpressionResult, context);
    }
    let result;
    for (let i ***REMOVED*** 0; i < node.elements.length; i++) {
      const element ***REMOVED*** node.elements[i];
      const visited ***REMOVED*** visitNode(element, i < node.elements.length - 1 ? visitorWithUnusedExpressionResult : visitor, isExpression);
      if (result || visited !***REMOVED******REMOVED*** element) {
        result || (result ***REMOVED*** node.elements.slice(0, i));
        result.push(visited);
      }
    }
    const elements ***REMOVED*** result ? setTextRange(factory2.createNodeArray(result), node.elements) : node.elements;
    return factory2.updateCommaListExpression(node, elements);
  }
  function visitCatchClause(node) {
    if (node.variableDeclaration && isBindingPattern(node.variableDeclaration.name) && node.variableDeclaration.name.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
      const name ***REMOVED*** factory2.getGeneratedNameForNode(node.variableDeclaration.name);
      const updatedDecl ***REMOVED*** factory2.updateVariableDeclaration(
        node.variableDeclaration,
        node.variableDeclaration.name,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        name
      );
      const visitedBindings ***REMOVED*** flattenDestructuringBinding(updatedDecl, visitor, context, 1 /* ObjectRest */);
      let block ***REMOVED*** visitNode(node.block, visitor, isBlock);
      if (some(visitedBindings)) {
        block ***REMOVED*** factory2.updateBlock(block, [
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            visitedBindings
          ),
          ...block.statements
        ]);
      }
      return factory2.updateCatchClause(
        node,
        factory2.updateVariableDeclaration(
          node.variableDeclaration,
          name,
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          /*initializer*/
          void 0
        ),
        block
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitVariableStatement(node) {
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      const savedExportedVariableStatement ***REMOVED*** exportedVariableStatement;
      exportedVariableStatement ***REMOVED*** true;
      const visited ***REMOVED*** visitEachChild(node, visitor, context);
      exportedVariableStatement ***REMOVED*** savedExportedVariableStatement;
      return visited;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitVariableDeclaration(node) {
    if (exportedVariableStatement) {
      const savedExportedVariableStatement ***REMOVED*** exportedVariableStatement;
      exportedVariableStatement ***REMOVED*** false;
      const visited ***REMOVED*** visitVariableDeclarationWorker(
        node,
        /*exportedVariableStatement*/
        true
      );
      exportedVariableStatement ***REMOVED*** savedExportedVariableStatement;
      return visited;
    }
    return visitVariableDeclarationWorker(
      node,
      /*exportedVariableStatement*/
      false
    );
  }
  function visitVariableDeclarationWorker(node, exportedVariableStatement2) {
    if (isBindingPattern(node.name) && node.name.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
      return flattenDestructuringBinding(
        node,
        visitor,
        context,
        1 /* ObjectRest */,
        /*rval*/
        void 0,
        exportedVariableStatement2
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForStatement(node) {
    return factory2.updateForStatement(
      node,
      visitNode(node.initializer, visitorWithUnusedExpressionResult, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, visitorWithUnusedExpressionResult, isExpression),
      visitIterationBody(node.statement, visitor, context)
    );
  }
  function visitVoidExpression(node) {
    return visitEachChild(node, visitorWithUnusedExpressionResult, context);
  }
  function visitForOfStatement(node, outermostLabeledStatement) {
    const ancestorFacts ***REMOVED*** enterSubtree(0 /* IterationStatementExcludes */, 2 /* IterationStatementIncludes */);
    if (node.initializer.transformFlags & 65536 /* ContainsObjectRestOrSpread */ || isAssignmentPattern(node.initializer) && containsObjectRestOrSpread(node.initializer)) {
      node ***REMOVED*** transformForOfStatementWithObjectRest(node);
    }
    const result ***REMOVED*** node.awaitModifier ? transformForAwaitOfStatement(node, outermostLabeledStatement, ancestorFacts) : factory2.restoreEnclosingLabel(visitEachChild(node, visitor, context), outermostLabeledStatement);
    exitSubtree(ancestorFacts);
    return result;
  }
  function transformForOfStatementWithObjectRest(node) {
    const initializerWithoutParens ***REMOVED*** skipParentheses(node.initializer);
    if (isVariableDeclarationList(initializerWithoutParens) || isAssignmentPattern(initializerWithoutParens)) {
      let bodyLocation;
      let statementsLocation;
      const temp ***REMOVED*** factory2.createTempVariable(
        /*recordTempVariable*/
        void 0
      );
      const statements ***REMOVED*** [createForOfBindingStatement(factory2, initializerWithoutParens, temp)];
      if (isBlock(node.statement)) {
        addRange(statements, node.statement.statements);
        bodyLocation ***REMOVED*** node.statement;
        statementsLocation ***REMOVED*** node.statement.statements;
      } else if (node.statement) {
        append(statements, node.statement);
        bodyLocation ***REMOVED*** node.statement;
        statementsLocation ***REMOVED*** node.statement;
      }
      return factory2.updateForOfStatement(
        node,
        node.awaitModifier,
        setTextRange(
          factory2.createVariableDeclarationList(
            [
              setTextRange(factory2.createVariableDeclaration(temp), node.initializer)
            ],
            1 /* Let */
          ),
          node.initializer
        ),
        node.expression,
        setTextRange(
          factory2.createBlock(
            setTextRange(factory2.createNodeArray(statements), statementsLocation),
            /*multiLine*/
            true
          ),
          bodyLocation
        )
      );
    }
    return node;
  }
  function convertForOfStatementHead(node, boundValue, nonUserCode) {
    const value ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    const iteratorValueExpression ***REMOVED*** factory2.createAssignment(value, boundValue);
    const iteratorValueStatement ***REMOVED*** factory2.createExpressionStatement(iteratorValueExpression);
    setSourceMapRange(iteratorValueStatement, node.expression);
    const exitNonUserCodeExpression ***REMOVED*** factory2.createAssignment(nonUserCode, factory2.createFalse());
    const exitNonUserCodeStatement ***REMOVED*** factory2.createExpressionStatement(exitNonUserCodeExpression);
    setSourceMapRange(exitNonUserCodeStatement, node.expression);
    const statements ***REMOVED*** [iteratorValueStatement, exitNonUserCodeStatement];
    const binding ***REMOVED*** createForOfBindingStatement(factory2, node.initializer, value);
    statements.push(visitNode(binding, visitor, isStatement));
    let bodyLocation;
    let statementsLocation;
    const statement ***REMOVED*** visitIterationBody(node.statement, visitor, context);
    if (isBlock(statement)) {
      addRange(statements, statement.statements);
      bodyLocation ***REMOVED*** statement;
      statementsLocation ***REMOVED*** statement.statements;
    } else {
      statements.push(statement);
    }
    return setTextRange(
      factory2.createBlock(
        setTextRange(factory2.createNodeArray(statements), statementsLocation),
        /*multiLine*/
        true
      ),
      bodyLocation
    );
  }
  function createDownlevelAwait(expression) {
    return enclosingFunctionFlags & 1 /* Generator */ ? factory2.createYieldExpression(
      /*asteriskToken*/
      void 0,
      emitHelpers().createAwaitHelper(expression)
    ) : factory2.createAwaitExpression(expression);
  }
  function transformForAwaitOfStatement(node, outermostLabeledStatement, ancestorFacts) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    const iterator ***REMOVED*** isIdentifier(expression) ? factory2.getGeneratedNameForNode(expression) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const result ***REMOVED*** isIdentifier(expression) ? factory2.getGeneratedNameForNode(iterator) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const nonUserCode ***REMOVED*** factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const done ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    const errorRecord ***REMOVED*** factory2.createUniqueName("e");
    const catchVariable ***REMOVED*** factory2.getGeneratedNameForNode(errorRecord);
    const returnMethod ***REMOVED*** factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const callValues ***REMOVED*** setTextRange(emitHelpers().createAsyncValuesHelper(expression), node.expression);
    const callNext ***REMOVED*** factory2.createCallExpression(
      factory2.createPropertyAccessExpression(iterator, "next"),
      /*typeArguments*/
      void 0,
      []
    );
    const getDone ***REMOVED*** factory2.createPropertyAccessExpression(result, "done");
    const getValue ***REMOVED*** factory2.createPropertyAccessExpression(result, "value");
    const callReturn ***REMOVED*** factory2.createFunctionCallCall(returnMethod, iterator, []);
    hoistVariableDeclaration(errorRecord);
    hoistVariableDeclaration(returnMethod);
    const initializer ***REMOVED*** ancestorFacts & 2 /* IterationContainer */ ? factory2.inlineExpressions([factory2.createAssignment(errorRecord, factory2.createVoidZero()), callValues]) : callValues;
    const forStatement ***REMOVED*** setEmitFlags(
      setTextRange(
        factory2.createForStatement(
          /*initializer*/
          setEmitFlags(
            setTextRange(
              factory2.createVariableDeclarationList([
                factory2.createVariableDeclaration(
                  nonUserCode,
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  factory2.createTrue()
                ),
                setTextRange(factory2.createVariableDeclaration(
                  iterator,
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  initializer
                ), node.expression),
                factory2.createVariableDeclaration(result)
              ]),
              node.expression
            ),
            4194304 /* NoHoisting */
          ),
          /*condition*/
          factory2.inlineExpressions([
            factory2.createAssignment(result, createDownlevelAwait(callNext)),
            factory2.createAssignment(done, getDone),
            factory2.createLogicalNot(done)
          ]),
          /*incrementor*/
          factory2.createAssignment(nonUserCode, factory2.createTrue()),
          /*statement*/
          convertForOfStatementHead(node, getValue, nonUserCode)
        ),
        /*location*/
        node
      ),
      512 /* NoTokenTrailingSourceMaps */
    );
    setOriginalNode(forStatement, node);
    return factory2.createTryStatement(
      factory2.createBlock([
        factory2.restoreEnclosingLabel(
          forStatement,
          outermostLabeledStatement
        )
      ]),
      factory2.createCatchClause(
        factory2.createVariableDeclaration(catchVariable),
        setEmitFlags(
          factory2.createBlock([
            factory2.createExpressionStatement(
              factory2.createAssignment(
                errorRecord,
                factory2.createObjectLiteralExpression([
                  factory2.createPropertyAssignment("error", catchVariable)
                ])
              )
            )
          ]),
          1 /* SingleLine */
        )
      ),
      factory2.createBlock([
        factory2.createTryStatement(
          /*tryBlock*/
          factory2.createBlock([
            setEmitFlags(
              factory2.createIfStatement(
                factory2.createLogicalAnd(
                  factory2.createLogicalAnd(
                    factory2.createLogicalNot(nonUserCode),
                    factory2.createLogicalNot(done)
                  ),
                  factory2.createAssignment(
                    returnMethod,
                    factory2.createPropertyAccessExpression(iterator, "return")
                  )
                ),
                factory2.createExpressionStatement(createDownlevelAwait(callReturn))
              ),
              1 /* SingleLine */
            )
          ]),
          /*catchClause*/
          void 0,
          /*finallyBlock*/
          setEmitFlags(
            factory2.createBlock([
              setEmitFlags(
                factory2.createIfStatement(
                  errorRecord,
                  factory2.createThrowStatement(
                    factory2.createPropertyAccessExpression(errorRecord, "error")
                  )
                ),
                1 /* SingleLine */
              )
            ]),
            1 /* SingleLine */
          )
        )
      ])
    );
  }
  function parameterVisitor(node) {
    Debug.assertNode(node, isParameter);
    return visitParameter(node);
  }
  function visitParameter(node) {
    if (parametersWithPrecedingObjectRestOrSpread ***REMOVED******REMOVED*** null ? void 0 : parametersWithPrecedingObjectRestOrSpread.has(node)) {
      return factory2.updateParameterDeclaration(
        node,
        /*modifiers*/
        void 0,
        node.dotDotDotToken,
        isBindingPattern(node.name) ? factory2.getGeneratedNameForNode(node) : node.name,
        /*questionToken*/
        void 0,
        /*type*/
        void 0,
        /*initializer*/
        void 0
      );
    }
    if (node.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
      return factory2.updateParameterDeclaration(
        node,
        /*modifiers*/
        void 0,
        node.dotDotDotToken,
        factory2.getGeneratedNameForNode(node),
        /*questionToken*/
        void 0,
        /*type*/
        void 0,
        visitNode(node.initializer, visitor, isExpression)
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function collectParametersWithPrecedingObjectRestOrSpread(node) {
    let parameters;
    for (const parameter of node.parameters) {
      if (parameters) {
        parameters.add(parameter);
      } else if (parameter.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
        parameters ***REMOVED*** /* @__PURE__ */ new Set();
      }
    }
    return parameters;
  }
  function visitConstructorDeclaration(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateConstructorDeclaration(
      node,
      node.modifiers,
      visitParameterList(node.parameters, parameterVisitor, context),
      transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitGetAccessorDeclaration(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateGetAccessorDeclaration(
      node,
      node.modifiers,
      visitNode(node.name, visitor, isPropertyName),
      visitParameterList(node.parameters, parameterVisitor, context),
      /*type*/
      void 0,
      transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitSetAccessorDeclaration(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateSetAccessorDeclaration(
      node,
      node.modifiers,
      visitNode(node.name, visitor, isPropertyName),
      visitParameterList(node.parameters, parameterVisitor, context),
      transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitMethodDeclaration(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateMethodDeclaration(
      node,
      enclosingFunctionFlags & 1 /* Generator */ ? visitNodes2(node.modifiers, visitorNoAsyncModifier, isModifierLike) : node.modifiers,
      enclosingFunctionFlags & 2 /* Async */ ? void 0 : node.asteriskToken,
      visitNode(node.name, visitor, isPropertyName),
      visitNode(
        /*node*/
        void 0,
        visitor,
        isQuestionToken
      ),
      /*typeParameters*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionParameterList(node) : visitParameterList(node.parameters, parameterVisitor, context),
      /*type*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionBody(node) : transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitFunctionDeclaration(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateFunctionDeclaration(
      node,
      enclosingFunctionFlags & 1 /* Generator */ ? visitNodes2(node.modifiers, visitorNoAsyncModifier, isModifier) : node.modifiers,
      enclosingFunctionFlags & 2 /* Async */ ? void 0 : node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionParameterList(node) : visitParameterList(node.parameters, parameterVisitor, context),
      /*type*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionBody(node) : transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitArrowFunction(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateArrowFunction(
      node,
      node.modifiers,
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, parameterVisitor, context),
      /*type*/
      void 0,
      node.equalsGreaterThanToken,
      transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function visitFunctionExpression(node) {
    const savedEnclosingFunctionFlags ***REMOVED*** enclosingFunctionFlags;
    const savedParametersWithPrecedingObjectRestOrSpread ***REMOVED*** parametersWithPrecedingObjectRestOrSpread;
    enclosingFunctionFlags ***REMOVED*** getFunctionFlags(node);
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** collectParametersWithPrecedingObjectRestOrSpread(node);
    const updated ***REMOVED*** factory2.updateFunctionExpression(
      node,
      enclosingFunctionFlags & 1 /* Generator */ ? visitNodes2(node.modifiers, visitorNoAsyncModifier, isModifier) : node.modifiers,
      enclosingFunctionFlags & 2 /* Async */ ? void 0 : node.asteriskToken,
      node.name,
      /*typeParameters*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionParameterList(node) : visitParameterList(node.parameters, parameterVisitor, context),
      /*type*/
      void 0,
      enclosingFunctionFlags & 2 /* Async */ && enclosingFunctionFlags & 1 /* Generator */ ? transformAsyncGeneratorFunctionBody(node) : transformFunctionBody(node)
    );
    enclosingFunctionFlags ***REMOVED*** savedEnclosingFunctionFlags;
    parametersWithPrecedingObjectRestOrSpread ***REMOVED*** savedParametersWithPrecedingObjectRestOrSpread;
    return updated;
  }
  function transformAsyncGeneratorFunctionParameterList(node) {
    if (isSimpleParameterList(node.parameters)) {
      return visitParameterList(node.parameters, visitor, context);
    }
    const newParameters ***REMOVED*** [];
    for (const parameter of node.parameters) {
      if (parameter.initializer || parameter.dotDotDotToken) {
        break;
      }
      const newParameter ***REMOVED*** factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        factory2.getGeneratedNameForNode(parameter.name, 8 /* ReservedInNestedScopes */)
      );
      newParameters.push(newParameter);
    }
    const newParametersArray ***REMOVED*** factory2.createNodeArray(newParameters);
    setTextRange(newParametersArray, node.parameters);
    return newParametersArray;
  }
  function transformAsyncGeneratorFunctionBody(node) {
    const innerParameters ***REMOVED*** !isSimpleParameterList(node.parameters) ? visitParameterList(node.parameters, visitor, context) : void 0;
    resumeLexicalEnvironment();
    const savedCapturedSuperProperties ***REMOVED*** capturedSuperProperties;
    const savedHasSuperElementAccess ***REMOVED*** hasSuperElementAccess;
    capturedSuperProperties ***REMOVED*** /* @__PURE__ */ new Set();
    hasSuperElementAccess ***REMOVED*** false;
    const outerStatements ***REMOVED*** [];
    let asyncBody ***REMOVED*** factory2.updateBlock(node.body, visitNodes2(node.body.statements, visitor, isStatement));
    asyncBody ***REMOVED*** factory2.updateBlock(asyncBody, factory2.mergeLexicalEnvironment(asyncBody.statements, appendObjectRestAssignmentsIfNeeded(endLexicalEnvironment(), node)));
    const returnStatement ***REMOVED*** factory2.createReturnStatement(
      emitHelpers().createAsyncGeneratorHelper(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          factory2.createToken(42 /* AsteriskToken */),
          node.name && factory2.getGeneratedNameForNode(node.name),
          /*typeParameters*/
          void 0,
          innerParameters ?? [],
          /*type*/
          void 0,
          asyncBody
        ),
        !!(hierarchyFacts & 1 /* HasLexicalThis */)
      )
    );
    const emitSuperHelpers ***REMOVED*** languageVersion >***REMOVED*** 2 /* ES2015 */ && (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */) || resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */));
    if (emitSuperHelpers) {
      enableSubstitutionForAsyncMethodsWithSuper();
      const variableStatement ***REMOVED*** createSuperAccessVariableStatement(factory2, resolver, node, capturedSuperProperties);
      substitutedSuperAccessors[getNodeId(variableStatement)] ***REMOVED*** true;
      insertStatementsAfterStandardPrologue(outerStatements, [variableStatement]);
    }
    outerStatements.push(returnStatement);
    const block ***REMOVED*** factory2.updateBlock(node.body, outerStatements);
    if (emitSuperHelpers && hasSuperElementAccess) {
      if (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */)) {
        addEmitHelper(block, advancedAsyncSuperHelper);
      } else if (resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */)) {
        addEmitHelper(block, asyncSuperHelper);
      }
    }
    capturedSuperProperties ***REMOVED*** savedCapturedSuperProperties;
    hasSuperElementAccess ***REMOVED*** savedHasSuperElementAccess;
    return block;
  }
  function transformFunctionBody(node) {
    resumeLexicalEnvironment();
    let statementOffset ***REMOVED*** 0;
    const statements ***REMOVED*** [];
    const body ***REMOVED*** visitNode(node.body, visitor, isConciseBody) ?? factory2.createBlock([]);
    if (isBlock(body)) {
      statementOffset ***REMOVED*** factory2.copyPrologue(
        body.statements,
        statements,
        /*ensureUseStrict*/
        false,
        visitor
      );
    }
    addRange(statements, appendObjectRestAssignmentsIfNeeded(
      /*statements*/
      void 0,
      node
    ));
    const leadingStatements ***REMOVED*** endLexicalEnvironment();
    if (statementOffset > 0 || some(statements) || some(leadingStatements)) {
      const block ***REMOVED*** factory2.converters.convertToFunctionBlock(
        body,
        /*multiLine*/
        true
      );
      insertStatementsAfterStandardPrologue(statements, leadingStatements);
      addRange(statements, block.statements.slice(statementOffset));
      return factory2.updateBlock(block, setTextRange(factory2.createNodeArray(statements), block.statements));
    }
    return body;
  }
  function appendObjectRestAssignmentsIfNeeded(statements, node) {
    let containsPrecedingObjectRestOrSpread ***REMOVED*** false;
    for (const parameter of node.parameters) {
      if (containsPrecedingObjectRestOrSpread) {
        if (isBindingPattern(parameter.name)) {
          if (parameter.name.elements.length > 0) {
            const declarations ***REMOVED*** flattenDestructuringBinding(
              parameter,
              visitor,
              context,
              0 /* All */,
              factory2.getGeneratedNameForNode(parameter)
            );
            if (some(declarations)) {
              const declarationList ***REMOVED*** factory2.createVariableDeclarationList(declarations);
              const statement ***REMOVED*** factory2.createVariableStatement(
                /*modifiers*/
                void 0,
                declarationList
              );
              setEmitFlags(statement, 2097152 /* CustomPrologue */);
              statements ***REMOVED*** append(statements, statement);
            }
          } else if (parameter.initializer) {
            const name ***REMOVED*** factory2.getGeneratedNameForNode(parameter);
            const initializer ***REMOVED*** visitNode(parameter.initializer, visitor, isExpression);
            const assignment ***REMOVED*** factory2.createAssignment(name, initializer);
            const statement ***REMOVED*** factory2.createExpressionStatement(assignment);
            setEmitFlags(statement, 2097152 /* CustomPrologue */);
            statements ***REMOVED*** append(statements, statement);
          }
        } else if (parameter.initializer) {
          const name ***REMOVED*** factory2.cloneNode(parameter.name);
          setTextRange(name, parameter.name);
          setEmitFlags(name, 96 /* NoSourceMap */);
          const initializer ***REMOVED*** visitNode(parameter.initializer, visitor, isExpression);
          addEmitFlags(initializer, 96 /* NoSourceMap */ | 3072 /* NoComments */);
          const assignment ***REMOVED*** factory2.createAssignment(name, initializer);
          setTextRange(assignment, parameter);
          setEmitFlags(assignment, 3072 /* NoComments */);
          const block ***REMOVED*** factory2.createBlock([factory2.createExpressionStatement(assignment)]);
          setTextRange(block, parameter);
          setEmitFlags(block, 1 /* SingleLine */ | 64 /* NoTrailingSourceMap */ | 768 /* NoTokenSourceMaps */ | 3072 /* NoComments */);
          const typeCheck ***REMOVED*** factory2.createTypeCheck(factory2.cloneNode(parameter.name), "undefined");
          const statement ***REMOVED*** factory2.createIfStatement(typeCheck, block);
          startOnNewLine(statement);
          setTextRange(statement, parameter);
          setEmitFlags(statement, 768 /* NoTokenSourceMaps */ | 64 /* NoTrailingSourceMap */ | 2097152 /* CustomPrologue */ | 3072 /* NoComments */);
          statements ***REMOVED*** append(statements, statement);
        }
      } else if (parameter.transformFlags & 65536 /* ContainsObjectRestOrSpread */) {
        containsPrecedingObjectRestOrSpread ***REMOVED*** true;
        const declarations ***REMOVED*** flattenDestructuringBinding(
          parameter,
          visitor,
          context,
          1 /* ObjectRest */,
          factory2.getGeneratedNameForNode(parameter),
          /*hoistTempVariables*/
          false,
          /*skipInitializer*/
          true
        );
        if (some(declarations)) {
          const declarationList ***REMOVED*** factory2.createVariableDeclarationList(declarations);
          const statement ***REMOVED*** factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            declarationList
          );
          setEmitFlags(statement, 2097152 /* CustomPrologue */);
          statements ***REMOVED*** append(statements, statement);
        }
      }
    }
    return statements;
  }
  function enableSubstitutionForAsyncMethodsWithSuper() {
    if ((enabledSubstitutions & 1 /* AsyncMethodsWithSuper */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 1 /* AsyncMethodsWithSuper */;
      context.enableSubstitution(214 /* CallExpression */);
      context.enableSubstitution(212 /* PropertyAccessExpression */);
      context.enableSubstitution(213 /* ElementAccessExpression */);
      context.enableEmitNotification(264 /* ClassDeclaration */);
      context.enableEmitNotification(175 /* MethodDeclaration */);
      context.enableEmitNotification(178 /* GetAccessor */);
      context.enableEmitNotification(179 /* SetAccessor */);
      context.enableEmitNotification(177 /* Constructor */);
      context.enableEmitNotification(244 /* VariableStatement */);
    }
  }
  function onEmitNode(hint, node, emitCallback) {
    if (enabledSubstitutions & 1 /* AsyncMethodsWithSuper */ && isSuperContainer(node)) {
      const superContainerFlags ***REMOVED*** (resolver.hasNodeCheckFlag(node, 128 /* MethodWithSuperPropertyAccessInAsync */) ? 128 /* MethodWithSuperPropertyAccessInAsync */ : 0) | (resolver.hasNodeCheckFlag(node, 256 /* MethodWithSuperPropertyAssignmentInAsync */) ? 256 /* MethodWithSuperPropertyAssignmentInAsync */ : 0);
      if (superContainerFlags !***REMOVED******REMOVED*** enclosingSuperContainerFlags) {
        const savedEnclosingSuperContainerFlags ***REMOVED*** enclosingSuperContainerFlags;
        enclosingSuperContainerFlags ***REMOVED*** superContainerFlags;
        previousOnEmitNode(hint, node, emitCallback);
        enclosingSuperContainerFlags ***REMOVED*** savedEnclosingSuperContainerFlags;
        return;
      }
    } else if (enabledSubstitutions && substitutedSuperAccessors[getNodeId(node)]) {
      const savedEnclosingSuperContainerFlags ***REMOVED*** enclosingSuperContainerFlags;
      enclosingSuperContainerFlags ***REMOVED*** 0;
      previousOnEmitNode(hint, node, emitCallback);
      enclosingSuperContainerFlags ***REMOVED*** savedEnclosingSuperContainerFlags;
      return;
    }
    previousOnEmitNode(hint, node, emitCallback);
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */ && enclosingSuperContainerFlags) {
      return substituteExpression(node);
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 212 /* PropertyAccessExpression */:
        return substitutePropertyAccessExpression(node);
      case 213 /* ElementAccessExpression */:
        return substituteElementAccessExpression(node);
      case 214 /* CallExpression */:
        return substituteCallExpression(node);
    }
    return node;
  }
  function substitutePropertyAccessExpression(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return setTextRange(
        factory2.createPropertyAccessExpression(
          factory2.createUniqueName("_super", 16 /* Optimistic */ | 32 /* FileLevel */),
          node.name
        ),
        node
      );
    }
    return node;
  }
  function substituteElementAccessExpression(node) {
    if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return createSuperElementAccessInAsyncMethod(
        node.argumentExpression,
        node
      );
    }
    return node;
  }
  function substituteCallExpression(node) {
    const expression ***REMOVED*** node.expression;
    if (isSuperProperty(expression)) {
      const argumentExpression ***REMOVED*** isPropertyAccessExpression(expression) ? substitutePropertyAccessExpression(expression) : substituteElementAccessExpression(expression);
      return factory2.createCallExpression(
        factory2.createPropertyAccessExpression(argumentExpression, "call"),
        /*typeArguments*/
        void 0,
        [
          factory2.createThis(),
          ...node.arguments
        ]
      );
    }
    return node;
  }
  function isSuperContainer(node) {
    const kind ***REMOVED*** node.kind;
    return kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 177 /* Constructor */ || kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ || kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ || kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */;
  }
  function createSuperElementAccessInAsyncMethod(argumentExpression, location) {
    if (enclosingSuperContainerFlags & 256 /* MethodWithSuperPropertyAssignmentInAsync */) {
      return setTextRange(
        factory2.createPropertyAccessExpression(
          factory2.createCallExpression(
            factory2.createIdentifier("_superIndex"),
            /*typeArguments*/
            void 0,
            [argumentExpression]
          ),
          "value"
        ),
        location
      );
    } else {
      return setTextRange(
        factory2.createCallExpression(
          factory2.createIdentifier("_superIndex"),
          /*typeArguments*/
          void 0,
          [argumentExpression]
        ),
        location
      );
    }
  }
}

// src/compiler/transformers/es2019.ts
function transformES2019(context) {
  const factory2 ***REMOVED*** context.factory;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    if ((node.transformFlags & 64 /* ContainsES2019 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    switch (node.kind) {
      case 300 /* CatchClause */:
        return visitCatchClause(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitCatchClause(node) {
    if (!node.variableDeclaration) {
      return factory2.updateCatchClause(
        node,
        factory2.createVariableDeclaration(factory2.createTempVariable(
          /*recordTempVariable*/
          void 0
        )),
        visitNode(node.block, visitor, isBlock)
      );
    }
    return visitEachChild(node, visitor, context);
  }
}

// src/compiler/transformers/es2020.ts
function transformES2020(context) {
  const {
    factory: factory2,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    if ((node.transformFlags & 32 /* ContainsES2020 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    switch (node.kind) {
      case 214 /* CallExpression */: {
        const updated ***REMOVED*** visitNonOptionalCallExpression(
          node,
          /*captureThisArg*/
          false
        );
        Debug.assertNotNode(updated, isSyntheticReference);
        return updated;
      }
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        if (isOptionalChain(node)) {
          const updated ***REMOVED*** visitOptionalExpression(
            node,
            /*captureThisArg*/
            false,
            /*isDelete*/
            false
          );
          Debug.assertNotNode(updated, isSyntheticReference);
          return updated;
        }
        return visitEachChild(node, visitor, context);
      case 227 /* BinaryExpression */:
        if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 61 /* QuestionQuestionToken */) {
          return transformNullishCoalescingExpression(node);
        }
        return visitEachChild(node, visitor, context);
      case 221 /* DeleteExpression */:
        return visitDeleteExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function flattenChain(chain) {
    Debug.assertNotNode(chain, isNonNullChain);
    const links ***REMOVED*** [chain];
    while (!chain.questionDotToken && !isTaggedTemplateExpression(chain)) {
      chain ***REMOVED*** cast(skipPartiallyEmittedExpressions(chain.expression), isOptionalChain);
      Debug.assertNotNode(chain, isNonNullChain);
      links.unshift(chain);
    }
    return { expression: chain.expression, chain: links };
  }
  function visitNonOptionalParenthesizedExpression(node, captureThisArg, isDelete) {
    const expression ***REMOVED*** visitNonOptionalExpression(node.expression, captureThisArg, isDelete);
    if (isSyntheticReference(expression)) {
      return factory2.createSyntheticReferenceExpression(factory2.updateParenthesizedExpression(node, expression.expression), expression.thisArg);
    }
    return factory2.updateParenthesizedExpression(node, expression);
  }
  function visitNonOptionalPropertyOrElementAccessExpression(node, captureThisArg, isDelete) {
    if (isOptionalChain(node)) {
      return visitOptionalExpression(node, captureThisArg, isDelete);
    }
    let expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assertNotNode(expression, isSyntheticReference);
    let thisArg;
    if (captureThisArg) {
      if (!isSimpleCopiableExpression(expression)) {
        thisArg ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
        expression ***REMOVED*** factory2.createAssignment(thisArg, expression);
      } else {
        thisArg ***REMOVED*** expression;
      }
    }
    expression ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ ? factory2.updatePropertyAccessExpression(node, expression, visitNode(node.name, visitor, isIdentifier)) : factory2.updateElementAccessExpression(node, expression, visitNode(node.argumentExpression, visitor, isExpression));
    return thisArg ? factory2.createSyntheticReferenceExpression(expression, thisArg) : expression;
  }
  function visitNonOptionalCallExpression(node, captureThisArg) {
    if (isOptionalChain(node)) {
      return visitOptionalExpression(
        node,
        captureThisArg,
        /*isDelete*/
        false
      );
    }
    if (isParenthesizedExpression(node.expression) && isOptionalChain(skipParentheses(node.expression))) {
      const expression ***REMOVED*** visitNonOptionalParenthesizedExpression(
        node.expression,
        /*captureThisArg*/
        true,
        /*isDelete*/
        false
      );
      const args ***REMOVED*** visitNodes2(node.arguments, visitor, isExpression);
      if (isSyntheticReference(expression)) {
        return setTextRange(factory2.createFunctionCallCall(expression.expression, expression.thisArg, args), node);
      }
      return factory2.updateCallExpression(
        node,
        expression,
        /*typeArguments*/
        void 0,
        args
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitNonOptionalExpression(node, captureThisArg, isDelete) {
    switch (node.kind) {
      case 218 /* ParenthesizedExpression */:
        return visitNonOptionalParenthesizedExpression(node, captureThisArg, isDelete);
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
        return visitNonOptionalPropertyOrElementAccessExpression(node, captureThisArg, isDelete);
      case 214 /* CallExpression */:
        return visitNonOptionalCallExpression(node, captureThisArg);
      default:
        return visitNode(node, visitor, isExpression);
    }
  }
  function visitOptionalExpression(node, captureThisArg, isDelete) {
    const { expression, chain } ***REMOVED*** flattenChain(node);
    const left ***REMOVED*** visitNonOptionalExpression(
      skipPartiallyEmittedExpressions(expression),
      isCallChain(chain[0]),
      /*isDelete*/
      false
    );
    let leftThisArg ***REMOVED*** isSyntheticReference(left) ? left.thisArg : void 0;
    let capturedLeft ***REMOVED*** isSyntheticReference(left) ? left.expression : left;
    let leftExpression ***REMOVED*** factory2.restoreOuterExpressions(expression, capturedLeft, 8 /* PartiallyEmittedExpressions */);
    if (!isSimpleCopiableExpression(capturedLeft)) {
      capturedLeft ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      leftExpression ***REMOVED*** factory2.createAssignment(capturedLeft, leftExpression);
    }
    let rightExpression ***REMOVED*** capturedLeft;
    let thisArg;
    for (let i ***REMOVED*** 0; i < chain.length; i++) {
      const segment ***REMOVED*** chain[i];
      switch (segment.kind) {
        case 212 /* PropertyAccessExpression */:
        case 213 /* ElementAccessExpression */:
          if (i ***REMOVED******REMOVED******REMOVED*** chain.length - 1 && captureThisArg) {
            if (!isSimpleCopiableExpression(rightExpression)) {
              thisArg ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
              rightExpression ***REMOVED*** factory2.createAssignment(thisArg, rightExpression);
            } else {
              thisArg ***REMOVED*** rightExpression;
            }
          }
          rightExpression ***REMOVED*** segment.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ ? factory2.createPropertyAccessExpression(rightExpression, visitNode(segment.name, visitor, isIdentifier)) : factory2.createElementAccessExpression(rightExpression, visitNode(segment.argumentExpression, visitor, isExpression));
          break;
        case 214 /* CallExpression */:
          if (i ***REMOVED******REMOVED******REMOVED*** 0 && leftThisArg) {
            if (!isGeneratedIdentifier(leftThisArg)) {
              leftThisArg ***REMOVED*** factory2.cloneNode(leftThisArg);
              addEmitFlags(leftThisArg, 3072 /* NoComments */);
            }
            rightExpression ***REMOVED*** factory2.createFunctionCallCall(
              rightExpression,
              leftThisArg.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ ? factory2.createThis() : leftThisArg,
              visitNodes2(segment.arguments, visitor, isExpression)
            );
          } else {
            rightExpression ***REMOVED*** factory2.createCallExpression(
              rightExpression,
              /*typeArguments*/
              void 0,
              visitNodes2(segment.arguments, visitor, isExpression)
            );
          }
          break;
      }
      setOriginalNode(rightExpression, segment);
    }
    const target ***REMOVED*** isDelete ? factory2.createConditionalExpression(
      createNotNullCondition(
        leftExpression,
        capturedLeft,
        /*invert*/
        true
      ),
      /*questionToken*/
      void 0,
      factory2.createTrue(),
      /*colonToken*/
      void 0,
      factory2.createDeleteExpression(rightExpression)
    ) : factory2.createConditionalExpression(
      createNotNullCondition(
        leftExpression,
        capturedLeft,
        /*invert*/
        true
      ),
      /*questionToken*/
      void 0,
      factory2.createVoidZero(),
      /*colonToken*/
      void 0,
      rightExpression
    );
    setTextRange(target, node);
    return thisArg ? factory2.createSyntheticReferenceExpression(target, thisArg) : target;
  }
  function createNotNullCondition(left, right, invert) {
    return factory2.createBinaryExpression(
      factory2.createBinaryExpression(
        left,
        factory2.createToken(invert ? 37 /* EqualsEqualsEqualsToken */ : 38 /* ExclamationEqualsEqualsToken */),
        factory2.createNull()
      ),
      factory2.createToken(invert ? 57 /* BarBarToken */ : 56 /* AmpersandAmpersandToken */),
      factory2.createBinaryExpression(
        right,
        factory2.createToken(invert ? 37 /* EqualsEqualsEqualsToken */ : 38 /* ExclamationEqualsEqualsToken */),
        factory2.createVoidZero()
      )
    );
  }
  function transformNullishCoalescingExpression(node) {
    let left ***REMOVED*** visitNode(node.left, visitor, isExpression);
    let right ***REMOVED*** left;
    if (!isSimpleCopiableExpression(left)) {
      right ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      left ***REMOVED*** factory2.createAssignment(right, left);
    }
    return setTextRange(
      factory2.createConditionalExpression(
        createNotNullCondition(left, right),
        /*questionToken*/
        void 0,
        right,
        /*colonToken*/
        void 0,
        visitNode(node.right, visitor, isExpression)
      ),
      node
    );
  }
  function visitDeleteExpression(node) {
    return isOptionalChain(skipParentheses(node.expression)) ? setOriginalNode(visitNonOptionalExpression(
      node.expression,
      /*captureThisArg*/
      false,
      /*isDelete*/
      true
    ), node) : factory2.updateDeleteExpression(node, visitNode(node.expression, visitor, isExpression));
  }
}

// src/compiler/transformers/es2021.ts
function transformES2021(context) {
  const {
    hoistVariableDeclaration,
    factory: factory2
  } ***REMOVED*** context;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    if ((node.transformFlags & 16 /* ContainsES2021 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    if (isLogicalOrCoalescingAssignmentExpression(node)) {
      return transformLogicalAssignment(node);
    }
    return visitEachChild(node, visitor, context);
  }
  function transformLogicalAssignment(binaryExpression) {
    const operator ***REMOVED*** binaryExpression.operatorToken;
    const nonAssignmentOperator ***REMOVED*** getNonAssignmentOperatorForCompoundAssignment(operator.kind);
    let left ***REMOVED*** skipParentheses(visitNode(binaryExpression.left, visitor, isLeftHandSideExpression));
    let assignmentTarget ***REMOVED*** left;
    const right ***REMOVED*** skipParentheses(visitNode(binaryExpression.right, visitor, isExpression));
    if (isAccessExpression(left)) {
      const propertyAccessTargetSimpleCopiable ***REMOVED*** isSimpleCopiableExpression(left.expression);
      const propertyAccessTarget ***REMOVED*** propertyAccessTargetSimpleCopiable ? left.expression : factory2.createTempVariable(hoistVariableDeclaration);
      const propertyAccessTargetAssignment ***REMOVED*** propertyAccessTargetSimpleCopiable ? left.expression : factory2.createAssignment(
        propertyAccessTarget,
        left.expression
      );
      if (isPropertyAccessExpression(left)) {
        assignmentTarget ***REMOVED*** factory2.createPropertyAccessExpression(
          propertyAccessTarget,
          left.name
        );
        left ***REMOVED*** factory2.createPropertyAccessExpression(
          propertyAccessTargetAssignment,
          left.name
        );
      } else {
        const elementAccessArgumentSimpleCopiable ***REMOVED*** isSimpleCopiableExpression(left.argumentExpression);
        const elementAccessArgument ***REMOVED*** elementAccessArgumentSimpleCopiable ? left.argumentExpression : factory2.createTempVariable(hoistVariableDeclaration);
        assignmentTarget ***REMOVED*** factory2.createElementAccessExpression(
          propertyAccessTarget,
          elementAccessArgument
        );
        left ***REMOVED*** factory2.createElementAccessExpression(
          propertyAccessTargetAssignment,
          elementAccessArgumentSimpleCopiable ? left.argumentExpression : factory2.createAssignment(
            elementAccessArgument,
            left.argumentExpression
          )
        );
      }
    }
    return factory2.createBinaryExpression(
      left,
      nonAssignmentOperator,
      factory2.createParenthesizedExpression(
        factory2.createAssignment(
          assignmentTarget,
          right
        )
      )
    );
  }
}

// src/compiler/transformers/esnext.ts
function transformESNext(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    hoistVariableDeclaration,
    startLexicalEnvironment,
    endLexicalEnvironment
  } ***REMOVED*** context;
  let exportBindings;
  let exportVars;
  let defaultExportBinding;
  let exportEqualsBinding;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    const visited ***REMOVED*** visitNode(node, visitor, isSourceFile);
    addEmitHelpers(visited, context.readEmitHelpers());
    exportVars ***REMOVED*** void 0;
    exportBindings ***REMOVED*** void 0;
    defaultExportBinding ***REMOVED*** void 0;
    return visited;
  }
  function visitor(node) {
    if ((node.transformFlags & 4 /* ContainsESNext */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    switch (node.kind) {
      case 308 /* SourceFile */:
        return visitSourceFile(node);
      case 242 /* Block */:
        return visitBlock(node);
      case 249 /* ForStatement */:
        return visitForStatement(node);
      case 251 /* ForOfStatement */:
        return visitForOfStatement(node);
      case 256 /* SwitchStatement */:
        return visitSwitchStatement(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitSourceFile(node) {
    const usingKind ***REMOVED*** getUsingKindOfStatements(node.statements);
    if (usingKind) {
      startLexicalEnvironment();
      exportBindings ***REMOVED*** new IdentifierNameMap();
      exportVars ***REMOVED*** [];
      const prologueCount ***REMOVED*** countPrologueStatements(node.statements);
      const topLevelStatements ***REMOVED*** [];
      addRange(topLevelStatements, visitArray(node.statements, visitor, isStatement, 0, prologueCount));
      let pos ***REMOVED*** prologueCount;
      while (pos < node.statements.length) {
        const statement ***REMOVED*** node.statements[pos];
        if (getUsingKind(statement) !***REMOVED******REMOVED*** 0 /* None */) {
          if (pos > prologueCount) {
            addRange(topLevelStatements, visitNodes2(node.statements, visitor, isStatement, prologueCount, pos - prologueCount));
          }
          break;
        }
        pos++;
      }
      Debug.assert(pos < node.statements.length, "Should have encountered at least one 'using' statement.");
      const envBinding ***REMOVED*** createEnvBinding();
      const bodyStatements ***REMOVED*** transformUsingDeclarations(node.statements, pos, node.statements.length, envBinding, topLevelStatements);
      if (exportBindings.size) {
        append(
          topLevelStatements,
          factory2.createExportDeclaration(
            /*modifiers*/
            void 0,
            /*isTypeOnly*/
            false,
            factory2.createNamedExports(arrayFrom(exportBindings.values()))
          )
        );
      }
      addRange(topLevelStatements, endLexicalEnvironment());
      if (exportVars.length) {
        topLevelStatements.push(factory2.createVariableStatement(
          factory2.createModifiersFromModifierFlags(32 /* Export */),
          factory2.createVariableDeclarationList(
            exportVars,
            1 /* Let */
          )
        ));
      }
      addRange(topLevelStatements, createDownlevelUsingStatements(bodyStatements, envBinding, usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */));
      if (exportEqualsBinding) {
        topLevelStatements.push(factory2.createExportAssignment(
          /*modifiers*/
          void 0,
          /*isExportEquals*/
          true,
          exportEqualsBinding
        ));
      }
      return factory2.updateSourceFile(node, topLevelStatements);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitBlock(node) {
    const usingKind ***REMOVED*** getUsingKindOfStatements(node.statements);
    if (usingKind) {
      const prologueCount ***REMOVED*** countPrologueStatements(node.statements);
      const envBinding ***REMOVED*** createEnvBinding();
      return factory2.updateBlock(
        node,
        [
          ...visitArray(node.statements, visitor, isStatement, 0, prologueCount),
          ...createDownlevelUsingStatements(
            transformUsingDeclarations(
              node.statements,
              prologueCount,
              node.statements.length,
              envBinding,
              /*topLevelStatements*/
              void 0
            ),
            envBinding,
            usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */
          )
        ]
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForStatement(node) {
    if (node.initializer && isUsingVariableDeclarationList(node.initializer)) {
      return visitNode(
        factory2.createBlock([
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            node.initializer
          ),
          factory2.updateForStatement(
            node,
            /*initializer*/
            void 0,
            node.condition,
            node.incrementor,
            node.statement
          )
        ]),
        visitor,
        isStatement
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForOfStatement(node) {
    if (isUsingVariableDeclarationList(node.initializer)) {
      const forInitializer ***REMOVED*** node.initializer;
      const forDecl ***REMOVED*** firstOrUndefined(forInitializer.declarations) || factory2.createVariableDeclaration(factory2.createTempVariable(
        /*recordTempVariable*/
        void 0
      ));
      const isAwaitUsing ***REMOVED*** getUsingKindOfVariableDeclarationList(forInitializer) ***REMOVED******REMOVED******REMOVED*** 2 /* Async */;
      const temp ***REMOVED*** factory2.getGeneratedNameForNode(forDecl.name);
      const usingVar ***REMOVED*** factory2.updateVariableDeclaration(
        forDecl,
        forDecl.name,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        temp
      );
      const usingVarList ***REMOVED*** factory2.createVariableDeclarationList([usingVar], isAwaitUsing ? 6 /* AwaitUsing */ : 4 /* Using */);
      const usingVarStatement ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        usingVarList
      );
      return visitNode(
        factory2.updateForOfStatement(
          node,
          node.awaitModifier,
          factory2.createVariableDeclarationList([
            factory2.createVariableDeclaration(temp)
          ], 2 /* Const */),
          node.expression,
          isBlock(node.statement) ? factory2.updateBlock(node.statement, [
            usingVarStatement,
            ...node.statement.statements
          ]) : factory2.createBlock(
            [
              usingVarStatement,
              node.statement
            ],
            /*multiLine*/
            true
          )
        ),
        visitor,
        isStatement
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCaseOrDefaultClause(node, envBinding) {
    if (getUsingKindOfStatements(node.statements) !***REMOVED******REMOVED*** 0 /* None */) {
      if (isCaseClause(node)) {
        return factory2.updateCaseClause(
          node,
          visitNode(node.expression, visitor, isExpression),
          transformUsingDeclarations(
            node.statements,
            /*start*/
            0,
            node.statements.length,
            envBinding,
            /*topLevelStatements*/
            void 0
          )
        );
      } else {
        return factory2.updateDefaultClause(
          node,
          transformUsingDeclarations(
            node.statements,
            /*start*/
            0,
            node.statements.length,
            envBinding,
            /*topLevelStatements*/
            void 0
          )
        );
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitSwitchStatement(node) {
    const usingKind ***REMOVED*** getUsingKindOfCaseOrDefaultClauses(node.caseBlock.clauses);
    if (usingKind) {
      const envBinding ***REMOVED*** createEnvBinding();
      return createDownlevelUsingStatements(
        [
          factory2.updateSwitchStatement(
            node,
            visitNode(node.expression, visitor, isExpression),
            factory2.updateCaseBlock(
              node.caseBlock,
              node.caseBlock.clauses.map((clause) ***REMOVED***> visitCaseOrDefaultClause(clause, envBinding))
            )
          )
        ],
        envBinding,
        usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function transformUsingDeclarations(statementsIn, start, end, envBinding, topLevelStatements) {
    const statements ***REMOVED*** [];
    for (let i ***REMOVED*** start; i < end; i++) {
      const statement ***REMOVED*** statementsIn[i];
      const usingKind ***REMOVED*** getUsingKind(statement);
      if (usingKind) {
        Debug.assertNode(statement, isVariableStatement);
        const declarations ***REMOVED*** [];
        for (let declaration of statement.declarationList.declarations) {
          if (!isIdentifier(declaration.name)) {
            declarations.length ***REMOVED*** 0;
            break;
          }
          if (isNamedEvaluation(declaration)) {
            declaration ***REMOVED*** transformNamedEvaluation(context, declaration);
          }
          const initializer ***REMOVED*** visitNode(declaration.initializer, visitor, isExpression) ?? factory2.createVoidZero();
          declarations.push(factory2.updateVariableDeclaration(
            declaration,
            declaration.name,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            emitHelpers().createAddDisposableResourceHelper(
              envBinding,
              initializer,
              usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */
            )
          ));
        }
        if (declarations.length) {
          const varList ***REMOVED*** factory2.createVariableDeclarationList(declarations, 2 /* Const */);
          setOriginalNode(varList, statement.declarationList);
          setTextRange(varList, statement.declarationList);
          hoistOrAppendNode(factory2.updateVariableStatement(
            statement,
            /*modifiers*/
            void 0,
            varList
          ));
          continue;
        }
      }
      const result ***REMOVED*** visitor(statement);
      if (isArray(result)) {
        result.forEach(hoistOrAppendNode);
      } else if (result) {
        hoistOrAppendNode(result);
      }
    }
    return statements;
    function hoistOrAppendNode(node) {
      Debug.assertNode(node, isStatement);
      append(statements, hoist(node));
    }
    function hoist(node) {
      if (!topLevelStatements) return node;
      switch (node.kind) {
        case 273 /* ImportDeclaration */:
        case 272 /* ImportEqualsDeclaration */:
        case 279 /* ExportDeclaration */:
        case 263 /* FunctionDeclaration */:
          return hoistImportOrExportOrHoistedDeclaration(node, topLevelStatements);
        case 278 /* ExportAssignment */:
          return hoistExportAssignment(node);
        case 264 /* ClassDeclaration */:
          return hoistClassDeclaration(node);
        case 244 /* VariableStatement */:
          return hoistVariableStatement(node);
      }
      return node;
    }
  }
  function hoistImportOrExportOrHoistedDeclaration(node, topLevelStatements) {
    topLevelStatements.push(node);
    return void 0;
  }
  function hoistExportAssignment(node) {
    return node.isExportEquals ? hoistExportEquals(node) : hoistExportDefault(node);
  }
  function hoistExportDefault(node) {
    if (defaultExportBinding) {
      return node;
    }
    defaultExportBinding ***REMOVED*** factory2.createUniqueName("_default", 8 /* ReservedInNestedScopes */ | 32 /* FileLevel */ | 16 /* Optimistic */);
    hoistBindingIdentifier(
      defaultExportBinding,
      /*isExport*/
      true,
      "default",
      node
    );
    let expression ***REMOVED*** node.expression;
    let innerExpression ***REMOVED*** skipOuterExpressions(expression);
    if (isNamedEvaluation(innerExpression)) {
      innerExpression ***REMOVED*** transformNamedEvaluation(
        context,
        innerExpression,
        /*ignoreEmptyStringLiteral*/
        false,
        "default"
      );
      expression ***REMOVED*** factory2.restoreOuterExpressions(expression, innerExpression);
    }
    const assignment ***REMOVED*** factory2.createAssignment(defaultExportBinding, expression);
    return factory2.createExpressionStatement(assignment);
  }
  function hoistExportEquals(node) {
    if (exportEqualsBinding) {
      return node;
    }
    exportEqualsBinding ***REMOVED*** factory2.createUniqueName("_default", 8 /* ReservedInNestedScopes */ | 32 /* FileLevel */ | 16 /* Optimistic */);
    hoistVariableDeclaration(exportEqualsBinding);
    const assignment ***REMOVED*** factory2.createAssignment(exportEqualsBinding, node.expression);
    return factory2.createExpressionStatement(assignment);
  }
  function hoistClassDeclaration(node) {
    if (!node.name && defaultExportBinding) {
      return node;
    }
    const isExported ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
    const isDefault ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */);
    let expression ***REMOVED*** factory2.converters.convertToClassExpression(node);
    if (node.name) {
      hoistBindingIdentifier(
        factory2.getLocalName(node),
        isExported && !isDefault,
        /*exportAlias*/
        void 0,
        node
      );
      expression ***REMOVED*** factory2.createAssignment(factory2.getDeclarationName(node), expression);
      if (isNamedEvaluation(expression)) {
        expression ***REMOVED*** transformNamedEvaluation(
          context,
          expression,
          /*ignoreEmptyStringLiteral*/
          false
        );
      }
      setOriginalNode(expression, node);
      setSourceMapRange(expression, node);
      setCommentRange(expression, node);
    }
    if (isDefault && !defaultExportBinding) {
      defaultExportBinding ***REMOVED*** factory2.createUniqueName("_default", 8 /* ReservedInNestedScopes */ | 32 /* FileLevel */ | 16 /* Optimistic */);
      hoistBindingIdentifier(
        defaultExportBinding,
        /*isExport*/
        true,
        "default",
        node
      );
      expression ***REMOVED*** factory2.createAssignment(defaultExportBinding, expression);
      if (isNamedEvaluation(expression)) {
        expression ***REMOVED*** transformNamedEvaluation(
          context,
          expression,
          /*ignoreEmptyStringLiteral*/
          false,
          "default"
        );
      }
      setOriginalNode(expression, node);
    }
    return factory2.createExpressionStatement(expression);
  }
  function hoistVariableStatement(node) {
    let expressions;
    const isExported ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
    for (const variable of node.declarationList.declarations) {
      hoistBindingElement(variable, isExported, variable);
      if (variable.initializer) {
        expressions ***REMOVED*** append(expressions, hoistInitializedVariable(variable));
      }
    }
    if (expressions) {
      const statement ***REMOVED*** factory2.createExpressionStatement(factory2.inlineExpressions(expressions));
      setOriginalNode(statement, node);
      setCommentRange(statement, node);
      setSourceMapRange(statement, node);
      return statement;
    }
    return void 0;
  }
  function hoistInitializedVariable(node) {
    Debug.assertIsDefined(node.initializer);
    let target;
    if (isIdentifier(node.name)) {
      target ***REMOVED*** factory2.cloneNode(node.name);
      setEmitFlags(target, getEmitFlags(target) & ~(32768 /* LocalName */ | 16384 /* ExportName */ | 65536 /* InternalName */));
    } else {
      target ***REMOVED*** factory2.converters.convertToAssignmentPattern(node.name);
    }
    const assignment ***REMOVED*** factory2.createAssignment(target, node.initializer);
    setOriginalNode(assignment, node);
    setCommentRange(assignment, node);
    setSourceMapRange(assignment, node);
    return assignment;
  }
  function hoistBindingElement(node, isExportedDeclaration, original) {
    if (isBindingPattern(node.name)) {
      for (const element of node.name.elements) {
        if (!isOmittedExpression(element)) {
          hoistBindingElement(element, isExportedDeclaration, original);
        }
      }
    } else {
      hoistBindingIdentifier(
        node.name,
        isExportedDeclaration,
        /*exportAlias*/
        void 0,
        original
      );
    }
  }
  function hoistBindingIdentifier(node, isExport, exportAlias, original) {
    const name ***REMOVED*** isGeneratedIdentifier(node) ? node : factory2.cloneNode(node);
    if (isExport) {
      if (exportAlias ***REMOVED******REMOVED******REMOVED*** void 0 && !isLocalName(name)) {
        const varDecl ***REMOVED*** factory2.createVariableDeclaration(name);
        if (original) {
          setOriginalNode(varDecl, original);
        }
        exportVars.push(varDecl);
        return;
      }
      const localName ***REMOVED*** exportAlias !***REMOVED******REMOVED*** void 0 ? name : void 0;
      const exportName ***REMOVED*** exportAlias !***REMOVED******REMOVED*** void 0 ? exportAlias : name;
      const specifier ***REMOVED*** factory2.createExportSpecifier(
        /*isTypeOnly*/
        false,
        localName,
        exportName
      );
      if (original) {
        setOriginalNode(specifier, original);
      }
      exportBindings.set(name, specifier);
    }
    hoistVariableDeclaration(name);
  }
  function createEnvBinding() {
    return factory2.createUniqueName("env");
  }
  function createDownlevelUsingStatements(bodyStatements, envBinding, async) {
    const statements ***REMOVED*** [];
    const envObject ***REMOVED*** factory2.createObjectLiteralExpression([
      factory2.createPropertyAssignment("stack", factory2.createArrayLiteralExpression()),
      factory2.createPropertyAssignment("error", factory2.createVoidZero()),
      factory2.createPropertyAssignment("hasError", factory2.createFalse())
    ]);
    const envVar ***REMOVED*** factory2.createVariableDeclaration(
      envBinding,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      envObject
    );
    const envVarList ***REMOVED*** factory2.createVariableDeclarationList([envVar], 2 /* Const */);
    const envVarStatement ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      envVarList
    );
    statements.push(envVarStatement);
    const tryBlock ***REMOVED*** factory2.createBlock(
      bodyStatements,
      /*multiLine*/
      true
    );
    const bodyCatchBinding ***REMOVED*** factory2.createUniqueName("e");
    const catchClause ***REMOVED*** factory2.createCatchClause(
      bodyCatchBinding,
      factory2.createBlock(
        [
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(envBinding, "error"),
              bodyCatchBinding
            )
          ),
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(envBinding, "hasError"),
              factory2.createTrue()
            )
          )
        ],
        /*multiLine*/
        true
      )
    );
    let finallyBlock;
    if (async) {
      const result ***REMOVED*** factory2.createUniqueName("result");
      finallyBlock ***REMOVED*** factory2.createBlock(
        [
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList([
              factory2.createVariableDeclaration(
                result,
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                emitHelpers().createDisposeResourcesHelper(envBinding)
              )
            ], 2 /* Const */)
          ),
          factory2.createIfStatement(result, factory2.createExpressionStatement(factory2.createAwaitExpression(result)))
        ],
        /*multiLine*/
        true
      );
    } else {
      finallyBlock ***REMOVED*** factory2.createBlock(
        [
          factory2.createExpressionStatement(
            emitHelpers().createDisposeResourcesHelper(envBinding)
          )
        ],
        /*multiLine*/
        true
      );
    }
    const tryStatement ***REMOVED*** factory2.createTryStatement(tryBlock, catchClause, finallyBlock);
    statements.push(tryStatement);
    return statements;
  }
}
function countPrologueStatements(statements) {
  for (let i ***REMOVED*** 0; i < statements.length; i++) {
    if (!isPrologueDirective(statements[i]) && !isCustomPrologue(statements[i])) {
      return i;
    }
  }
  return 0;
}
function isUsingVariableDeclarationList(node) {
  return isVariableDeclarationList(node) && getUsingKindOfVariableDeclarationList(node) !***REMOVED******REMOVED*** 0 /* None */;
}
function getUsingKindOfVariableDeclarationList(node) {
  return (node.flags & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 6 /* AwaitUsing */ ? 2 /* Async */ : (node.flags & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 4 /* Using */ ? 1 /* Sync */ : 0 /* None */;
}
function getUsingKindOfVariableStatement(node) {
  return getUsingKindOfVariableDeclarationList(node.declarationList);
}
function getUsingKind(statement) {
  return isVariableStatement(statement) ? getUsingKindOfVariableStatement(statement) : 0 /* None */;
}
function getUsingKindOfStatements(statements) {
  let result ***REMOVED*** 0 /* None */;
  for (const statement of statements) {
    const usingKind ***REMOVED*** getUsingKind(statement);
    if (usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */) return 2 /* Async */;
    if (usingKind > result) result ***REMOVED*** usingKind;
  }
  return result;
}
function getUsingKindOfCaseOrDefaultClauses(clauses) {
  let result ***REMOVED*** 0 /* None */;
  for (const clause of clauses) {
    const usingKind ***REMOVED*** getUsingKindOfStatements(clause.statements);
    if (usingKind ***REMOVED******REMOVED******REMOVED*** 2 /* Async */) return 2 /* Async */;
    if (usingKind > result) result ***REMOVED*** usingKind;
  }
  return result;
}

// src/compiler/transformers/jsx.ts
function transformJsx(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers
  } ***REMOVED*** context;
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  let currentSourceFile;
  let currentFileState;
  return chainBundle(context, transformSourceFile);
  function getCurrentFileNameExpression() {
    if (currentFileState.filenameDeclaration) {
      return currentFileState.filenameDeclaration.name;
    }
    const declaration ***REMOVED*** factory2.createVariableDeclaration(
      factory2.createUniqueName("_jsxFileName", 16 /* Optimistic */ | 32 /* FileLevel */),
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      factory2.createStringLiteral(currentSourceFile.fileName)
    );
    currentFileState.filenameDeclaration ***REMOVED*** declaration;
    return currentFileState.filenameDeclaration.name;
  }
  function getJsxFactoryCalleePrimitive(isStaticChildren) {
    return compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */ ? "jsxDEV" : isStaticChildren ? "jsxs" : "jsx";
  }
  function getJsxFactoryCallee(isStaticChildren) {
    const type ***REMOVED*** getJsxFactoryCalleePrimitive(isStaticChildren);
    return getImplicitImportForName(type);
  }
  function getImplicitJsxFragmentReference() {
    return getImplicitImportForName("Fragment");
  }
  function getImplicitImportForName(name) {
    var _a, _b;
    const importSource ***REMOVED*** name ***REMOVED******REMOVED******REMOVED*** "createElement" ? currentFileState.importSpecifier : getJSXRuntimeImport(currentFileState.importSpecifier, compilerOptions);
    const existing ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** currentFileState.utilizedImplicitRuntimeImports) ***REMOVED******REMOVED*** null ? void 0 : _a.get(importSource)) ***REMOVED******REMOVED*** null ? void 0 : _b.get(name);
    if (existing) {
      return existing.name;
    }
    if (!currentFileState.utilizedImplicitRuntimeImports) {
      currentFileState.utilizedImplicitRuntimeImports ***REMOVED*** /* @__PURE__ */ new Map();
    }
    let specifierSourceImports ***REMOVED*** currentFileState.utilizedImplicitRuntimeImports.get(importSource);
    if (!specifierSourceImports) {
      specifierSourceImports ***REMOVED*** /* @__PURE__ */ new Map();
      currentFileState.utilizedImplicitRuntimeImports.set(importSource, specifierSourceImports);
    }
    const generatedName ***REMOVED*** factory2.createUniqueName(`_${name}`, 16 /* Optimistic */ | 32 /* FileLevel */ | 64 /* AllowNameSubstitution */);
    const specifier ***REMOVED*** factory2.createImportSpecifier(
      /*isTypeOnly*/
      false,
      factory2.createIdentifier(name),
      generatedName
    );
    setIdentifierGeneratedImportReference(generatedName, specifier);
    specifierSourceImports.set(name, specifier);
    return generatedName;
  }
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    currentFileState ***REMOVED*** {};
    currentFileState.importSpecifier ***REMOVED*** getJSXImplicitImportBase(compilerOptions, node);
    let visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    let statements ***REMOVED*** visited.statements;
    if (currentFileState.filenameDeclaration) {
      statements ***REMOVED*** insertStatementAfterCustomPrologue(statements.slice(), factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList([currentFileState.filenameDeclaration], 2 /* Const */)
      ));
    }
    if (currentFileState.utilizedImplicitRuntimeImports) {
      for (const [importSource, importSpecifiersMap] of arrayFrom(currentFileState.utilizedImplicitRuntimeImports.entries())) {
        if (isExternalModule(node)) {
          const importStatement ***REMOVED*** factory2.createImportDeclaration(
            /*modifiers*/
            void 0,
            factory2.createImportClause(
              /*phaseModifier*/
              void 0,
              /*name*/
              void 0,
              factory2.createNamedImports(arrayFrom(importSpecifiersMap.values()))
            ),
            factory2.createStringLiteral(importSource),
            /*attributes*/
            void 0
          );
          setParentRecursive(
            importStatement,
            /*incremental*/
            false
          );
          statements ***REMOVED*** insertStatementAfterCustomPrologue(statements.slice(), importStatement);
        } else if (isExternalOrCommonJsModule(node)) {
          const requireStatement ***REMOVED*** factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList([
              factory2.createVariableDeclaration(
                factory2.createObjectBindingPattern(arrayFrom(importSpecifiersMap.values(), (s) ***REMOVED***> factory2.createBindingElement(
                  /*dotDotDotToken*/
                  void 0,
                  s.propertyName,
                  s.name
                ))),
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                factory2.createCallExpression(
                  factory2.createIdentifier("require"),
                  /*typeArguments*/
                  void 0,
                  [factory2.createStringLiteral(importSource)]
                )
              )
            ], 2 /* Const */)
          );
          setParentRecursive(
            requireStatement,
            /*incremental*/
            false
          );
          statements ***REMOVED*** insertStatementAfterCustomPrologue(statements.slice(), requireStatement);
        } else {
        }
      }
    }
    if (statements !***REMOVED******REMOVED*** visited.statements) {
      visited ***REMOVED*** factory2.updateSourceFile(visited, statements);
    }
    currentFileState ***REMOVED*** void 0;
    return visited;
  }
  function visitor(node) {
    if (node.transformFlags & 2 /* ContainsJsx */) {
      return visitorWorker(node);
    } else {
      return node;
    }
  }
  function visitorWorker(node) {
    switch (node.kind) {
      case 285 /* JsxElement */:
        return visitJsxElement(
          node,
          /*isChild*/
          false
        );
      case 286 /* JsxSelfClosingElement */:
        return visitJsxSelfClosingElement(
          node,
          /*isChild*/
          false
        );
      case 289 /* JsxFragment */:
        return visitJsxFragment(
          node,
          /*isChild*/
          false
        );
      case 295 /* JsxExpression */:
        return visitJsxExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function transformJsxChildToExpression(node) {
    switch (node.kind) {
      case 12 /* JsxText */:
        return visitJsxText(node);
      case 295 /* JsxExpression */:
        return visitJsxExpression(node);
      case 285 /* JsxElement */:
        return visitJsxElement(
          node,
          /*isChild*/
          true
        );
      case 286 /* JsxSelfClosingElement */:
        return visitJsxSelfClosingElement(
          node,
          /*isChild*/
          true
        );
      case 289 /* JsxFragment */:
        return visitJsxFragment(
          node,
          /*isChild*/
          true
        );
      default:
        return Debug.failBadSyntaxKind(node);
    }
  }
  function hasProto(obj) {
    return obj.properties.some(
      (p) ***REMOVED***> isPropertyAssignment(p) && (isIdentifier(p.name) && idText(p.name) ***REMOVED******REMOVED******REMOVED*** "__proto__" || isStringLiteral(p.name) && p.name.text ***REMOVED******REMOVED******REMOVED*** "__proto__")
    );
  }
  function hasKeyAfterPropsSpread(node) {
    let spread ***REMOVED*** false;
    for (const elem of node.attributes.properties) {
      if (isJsxSpreadAttribute(elem) && (!isObjectLiteralExpression(elem.expression) || elem.expression.properties.some(isSpreadAssignment))) {
        spread ***REMOVED*** true;
      } else if (spread && isJsxAttribute(elem) && isIdentifier(elem.name) && elem.name.escapedText ***REMOVED******REMOVED******REMOVED*** "key") {
        return true;
      }
    }
    return false;
  }
  function shouldUseCreateElement(node) {
    return currentFileState.importSpecifier ***REMOVED******REMOVED******REMOVED*** void 0 || hasKeyAfterPropsSpread(node);
  }
  function visitJsxElement(node, isChild) {
    const tagTransform ***REMOVED*** shouldUseCreateElement(node.openingElement) ? visitJsxOpeningLikeElementCreateElement : visitJsxOpeningLikeElementJSX;
    return tagTransform(
      node.openingElement,
      node.children,
      isChild,
      /*location*/
      node
    );
  }
  function visitJsxSelfClosingElement(node, isChild) {
    const tagTransform ***REMOVED*** shouldUseCreateElement(node) ? visitJsxOpeningLikeElementCreateElement : visitJsxOpeningLikeElementJSX;
    return tagTransform(
      node,
      /*children*/
      void 0,
      isChild,
      /*location*/
      node
    );
  }
  function visitJsxFragment(node, isChild) {
    const tagTransform ***REMOVED*** currentFileState.importSpecifier ***REMOVED******REMOVED******REMOVED*** void 0 ? visitJsxOpeningFragmentCreateElement : visitJsxOpeningFragmentJSX;
    return tagTransform(
      node.openingFragment,
      node.children,
      isChild,
      /*location*/
      node
    );
  }
  function convertJsxChildrenToChildrenPropObject(children) {
    const prop ***REMOVED*** convertJsxChildrenToChildrenPropAssignment(children);
    return prop && factory2.createObjectLiteralExpression([prop]);
  }
  function convertJsxChildrenToChildrenPropAssignment(children) {
    const nonWhitespaceChildren ***REMOVED*** getSemanticJsxChildren(children);
    if (length(nonWhitespaceChildren) ***REMOVED******REMOVED******REMOVED*** 1 && !nonWhitespaceChildren[0].dotDotDotToken) {
      const result2 ***REMOVED*** transformJsxChildToExpression(nonWhitespaceChildren[0]);
      return result2 && factory2.createPropertyAssignment("children", result2);
    }
    const result ***REMOVED*** mapDefined(children, transformJsxChildToExpression);
    return length(result) ? factory2.createPropertyAssignment("children", factory2.createArrayLiteralExpression(result)) : void 0;
  }
  function visitJsxOpeningLikeElementJSX(node, children, isChild, location) {
    const tagName ***REMOVED*** getTagName(node);
    const childrenProp ***REMOVED*** children && children.length ? convertJsxChildrenToChildrenPropAssignment(children) : void 0;
    const keyAttr ***REMOVED*** find(node.attributes.properties, (p) ***REMOVED***> !!p.name && isIdentifier(p.name) && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** "key");
    const attrs ***REMOVED*** keyAttr ? filter(node.attributes.properties, (p) ***REMOVED***> p !***REMOVED******REMOVED*** keyAttr) : node.attributes.properties;
    const objectProperties ***REMOVED*** length(attrs) ? transformJsxAttributesToObjectProps(attrs, childrenProp) : factory2.createObjectLiteralExpression(childrenProp ? [childrenProp] : emptyArray);
    return visitJsxOpeningLikeElementOrFragmentJSX(
      tagName,
      objectProperties,
      keyAttr,
      children || emptyArray,
      isChild,
      location
    );
  }
  function visitJsxOpeningLikeElementOrFragmentJSX(tagName, objectProperties, keyAttr, children, isChild, location) {
    var _a;
    const nonWhitespaceChildren ***REMOVED*** getSemanticJsxChildren(children);
    const isStaticChildren ***REMOVED*** length(nonWhitespaceChildren) > 1 || !!((_a ***REMOVED*** nonWhitespaceChildren[0]) ***REMOVED******REMOVED*** null ? void 0 : _a.dotDotDotToken);
    const args ***REMOVED*** [tagName, objectProperties];
    if (keyAttr) {
      args.push(transformJsxAttributeInitializer(keyAttr.initializer));
    }
    if (compilerOptions.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
      const originalFile ***REMOVED*** getOriginalNode(currentSourceFile);
      if (originalFile && isSourceFile(originalFile)) {
        if (keyAttr ***REMOVED******REMOVED******REMOVED*** void 0) {
          args.push(factory2.createVoidZero());
        }
        args.push(isStaticChildren ? factory2.createTrue() : factory2.createFalse());
        const lineCol ***REMOVED*** getLineAndCharacterOfPosition(originalFile, location.pos);
        args.push(factory2.createObjectLiteralExpression([
          factory2.createPropertyAssignment("fileName", getCurrentFileNameExpression()),
          factory2.createPropertyAssignment("lineNumber", factory2.createNumericLiteral(lineCol.line + 1)),
          factory2.createPropertyAssignment("columnNumber", factory2.createNumericLiteral(lineCol.character + 1))
        ]));
        args.push(factory2.createThis());
      }
    }
    const element ***REMOVED*** setTextRange(
      factory2.createCallExpression(
        getJsxFactoryCallee(isStaticChildren),
        /*typeArguments*/
        void 0,
        args
      ),
      location
    );
    if (isChild) {
      startOnNewLine(element);
    }
    return element;
  }
  function visitJsxOpeningLikeElementCreateElement(node, children, isChild, location) {
    const tagName ***REMOVED*** getTagName(node);
    const attrs ***REMOVED*** node.attributes.properties;
    const objectProperties ***REMOVED*** length(attrs) ? transformJsxAttributesToObjectProps(attrs) : factory2.createNull();
    const callee ***REMOVED*** currentFileState.importSpecifier ***REMOVED******REMOVED******REMOVED*** void 0 ? createJsxFactoryExpression(
      factory2,
      context.getEmitResolver().getJsxFactoryEntity(currentSourceFile),
      compilerOptions.reactNamespace,
      // TODO: GH#18217
      node
    ) : getImplicitImportForName("createElement");
    const element ***REMOVED*** createExpressionForJsxElement(
      factory2,
      callee,
      tagName,
      objectProperties,
      mapDefined(children, transformJsxChildToExpression),
      location
    );
    if (isChild) {
      startOnNewLine(element);
    }
    return element;
  }
  function visitJsxOpeningFragmentJSX(_node, children, isChild, location) {
    let childrenProps;
    if (children && children.length) {
      const result ***REMOVED*** convertJsxChildrenToChildrenPropObject(children);
      if (result) {
        childrenProps ***REMOVED*** result;
      }
    }
    return visitJsxOpeningLikeElementOrFragmentJSX(
      getImplicitJsxFragmentReference(),
      childrenProps || factory2.createObjectLiteralExpression([]),
      /*keyAttr*/
      void 0,
      children,
      isChild,
      location
    );
  }
  function visitJsxOpeningFragmentCreateElement(node, children, isChild, location) {
    const element ***REMOVED*** createExpressionForJsxFragment(
      factory2,
      context.getEmitResolver().getJsxFactoryEntity(currentSourceFile),
      context.getEmitResolver().getJsxFragmentFactoryEntity(currentSourceFile),
      compilerOptions.reactNamespace,
      // TODO: GH#18217
      mapDefined(children, transformJsxChildToExpression),
      node,
      location
    );
    if (isChild) {
      startOnNewLine(element);
    }
    return element;
  }
  function transformJsxSpreadAttributeToProps(node) {
    if (isObjectLiteralExpression(node.expression) && !hasProto(node.expression)) {
      return sameMap(node.expression.properties, (p) ***REMOVED***> Debug.checkDefined(visitNode(p, visitor, isObjectLiteralElementLike)));
    }
    return factory2.createSpreadAssignment(Debug.checkDefined(visitNode(node.expression, visitor, isExpression)));
  }
  function transformJsxAttributesToObjectProps(attrs, children) {
    const target ***REMOVED*** getEmitScriptTarget(compilerOptions);
    return target && target >***REMOVED*** 5 /* ES2018 */ ? factory2.createObjectLiteralExpression(transformJsxAttributesToProps(attrs, children)) : transformJsxAttributesToExpression(attrs, children);
  }
  function transformJsxAttributesToProps(attrs, children) {
    const props ***REMOVED*** flatten(spanMap(attrs, isJsxSpreadAttribute, (attrs2, isSpread) ***REMOVED***> flatten(map(attrs2, (attr) ***REMOVED***> isSpread ? transformJsxSpreadAttributeToProps(attr) : transformJsxAttributeToObjectLiteralElement(attr)))));
    if (children) {
      props.push(children);
    }
    return props;
  }
  function transformJsxAttributesToExpression(attrs, children) {
    const expressions ***REMOVED*** [];
    let properties ***REMOVED*** [];
    for (const attr of attrs) {
      if (isJsxSpreadAttribute(attr)) {
        if (isObjectLiteralExpression(attr.expression) && !hasProto(attr.expression)) {
          for (const prop of attr.expression.properties) {
            if (isSpreadAssignment(prop)) {
              finishObjectLiteralIfNeeded();
              expressions.push(Debug.checkDefined(visitNode(prop.expression, visitor, isExpression)));
              continue;
            }
            properties.push(Debug.checkDefined(visitNode(prop, visitor)));
          }
          continue;
        }
        finishObjectLiteralIfNeeded();
        expressions.push(Debug.checkDefined(visitNode(attr.expression, visitor, isExpression)));
        continue;
      }
      properties.push(transformJsxAttributeToObjectLiteralElement(attr));
    }
    if (children) {
      properties.push(children);
    }
    finishObjectLiteralIfNeeded();
    if (expressions.length && !isObjectLiteralExpression(expressions[0])) {
      expressions.unshift(factory2.createObjectLiteralExpression());
    }
    return singleOrUndefined(expressions) || emitHelpers().createAssignHelper(expressions);
    function finishObjectLiteralIfNeeded() {
      if (properties.length) {
        expressions.push(factory2.createObjectLiteralExpression(properties));
        properties ***REMOVED*** [];
      }
    }
  }
  function transformJsxAttributeToObjectLiteralElement(node) {
    const name ***REMOVED*** getAttributeName(node);
    const expression ***REMOVED*** transformJsxAttributeInitializer(node.initializer);
    return factory2.createPropertyAssignment(name, expression);
  }
  function transformJsxAttributeInitializer(node) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) {
      return factory2.createTrue();
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      const singleQuote ***REMOVED*** node.singleQuote !***REMOVED******REMOVED*** void 0 ? node.singleQuote : !isStringDoubleQuoted(node, currentSourceFile);
      const literal ***REMOVED*** factory2.createStringLiteral(tryDecodeEntities(node.text) || node.text, singleQuote);
      return setTextRange(literal, node);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */) {
      if (node.expression ***REMOVED******REMOVED******REMOVED*** void 0) {
        return factory2.createTrue();
      }
      return Debug.checkDefined(visitNode(node.expression, visitor, isExpression));
    }
    if (isJsxElement(node)) {
      return visitJsxElement(
        node,
        /*isChild*/
        false
      );
    }
    if (isJsxSelfClosingElement(node)) {
      return visitJsxSelfClosingElement(
        node,
        /*isChild*/
        false
      );
    }
    if (isJsxFragment(node)) {
      return visitJsxFragment(
        node,
        /*isChild*/
        false
      );
    }
    return Debug.failBadSyntaxKind(node);
  }
  function visitJsxText(node) {
    const fixed ***REMOVED*** fixupWhitespaceAndDecodeEntities(node.text);
    return fixed ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : factory2.createStringLiteral(fixed);
  }
  function fixupWhitespaceAndDecodeEntities(text) {
    let acc;
    let firstNonWhitespace ***REMOVED*** 0;
    let lastNonWhitespace ***REMOVED*** -1;
    for (let i ***REMOVED*** 0; i < text.length; i++) {
      const c ***REMOVED*** text.charCodeAt(i);
      if (isLineBreak(c)) {
        if (firstNonWhitespace !***REMOVED******REMOVED*** -1 && lastNonWhitespace !***REMOVED******REMOVED*** -1) {
          acc ***REMOVED*** addLineOfJsxText(acc, text.substr(firstNonWhitespace, lastNonWhitespace - firstNonWhitespace + 1));
        }
        firstNonWhitespace ***REMOVED*** -1;
      } else if (!isWhiteSpaceSingleLine(c)) {
        lastNonWhitespace ***REMOVED*** i;
        if (firstNonWhitespace ***REMOVED******REMOVED******REMOVED*** -1) {
          firstNonWhitespace ***REMOVED*** i;
        }
      }
    }
    return firstNonWhitespace !***REMOVED******REMOVED*** -1 ? addLineOfJsxText(acc, text.substr(firstNonWhitespace)) : acc;
  }
  function addLineOfJsxText(acc, trimmedLine) {
    const decoded ***REMOVED*** decodeEntities(trimmedLine);
    return acc ***REMOVED******REMOVED******REMOVED*** void 0 ? decoded : acc + " " + decoded;
  }
  function decodeEntities(text) {
    return text.replace(/&((#((\d+)|x([\da-fA-F]+)))|(\w+));/g, (match, _all, _number, _digits, decimal, hex, word) ***REMOVED***> {
      if (decimal) {
        return utf16EncodeAsString(parseInt(decimal, 10));
      } else if (hex) {
        return utf16EncodeAsString(parseInt(hex, 16));
      } else {
        const ch ***REMOVED*** entities.get(word);
        return ch ? utf16EncodeAsString(ch) : match;
      }
    });
  }
  function tryDecodeEntities(text) {
    const decoded ***REMOVED*** decodeEntities(text);
    return decoded ***REMOVED******REMOVED******REMOVED*** text ? void 0 : decoded;
  }
  function getTagName(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 285 /* JsxElement */) {
      return getTagName(node.openingElement);
    } else {
      const tagName ***REMOVED*** node.tagName;
      if (isIdentifier(tagName) && isIntrinsicJsxName(tagName.escapedText)) {
        return factory2.createStringLiteral(idText(tagName));
      } else if (isJsxNamespacedName(tagName)) {
        return factory2.createStringLiteral(idText(tagName.namespace) + ":" + idText(tagName.name));
      } else {
        return createExpressionFromEntityName(factory2, tagName);
      }
    }
  }
  function getAttributeName(node) {
    const name ***REMOVED*** node.name;
    if (isIdentifier(name)) {
      const text ***REMOVED*** idText(name);
      return /^[A-Z_]\w*$/i.test(text) ? name : factory2.createStringLiteral(text);
    }
    return factory2.createStringLiteral(idText(name.namespace) + ":" + idText(name.name));
  }
  function visitJsxExpression(node) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    return node.dotDotDotToken ? factory2.createSpreadElement(expression) : expression;
  }
}
var entities ***REMOVED*** new Map(Object.entries({
  quot: 34,
  amp: 38,
  apos: 39,
  lt: 60,
  gt: 62,
  nbsp: 160,
  iexcl: 161,
  cent: 162,
  pound: 163,
  curren: 164,
  yen: 165,
  brvbar: 166,
  sect: 167,
  uml: 168,
  copy: 169,
  ordf: 170,
  laquo: 171,
  not: 172,
  shy: 173,
  reg: 174,
  macr: 175,
  deg: 176,
  plusmn: 177,
  sup2: 178,
  sup3: 179,
  acute: 180,
  micro: 181,
  para: 182,
  middot: 183,
  cedil: 184,
  sup1: 185,
  ordm: 186,
  raquo: 187,
  frac14: 188,
  frac12: 189,
  frac34: 190,
  iquest: 191,
  Agrave: 192,
  Aacute: 193,
  Acirc: 194,
  Atilde: 195,
  Auml: 196,
  Aring: 197,
  AElig: 198,
  Ccedil: 199,
  Egrave: 200,
  Eacute: 201,
  Ecirc: 202,
  Euml: 203,
  Igrave: 204,
  Iacute: 205,
  Icirc: 206,
  Iuml: 207,
  ETH: 208,
  Ntilde: 209,
  Ograve: 210,
  Oacute: 211,
  Ocirc: 212,
  Otilde: 213,
  Ouml: 214,
  times: 215,
  Oslash: 216,
  Ugrave: 217,
  Uacute: 218,
  Ucirc: 219,
  Uuml: 220,
  Yacute: 221,
  THORN: 222,
  szlig: 223,
  agrave: 224,
  aacute: 225,
  acirc: 226,
  atilde: 227,
  auml: 228,
  aring: 229,
  aelig: 230,
  ccedil: 231,
  egrave: 232,
  eacute: 233,
  ecirc: 234,
  euml: 235,
  igrave: 236,
  iacute: 237,
  icirc: 238,
  iuml: 239,
  eth: 240,
  ntilde: 241,
  ograve: 242,
  oacute: 243,
  ocirc: 244,
  otilde: 245,
  ouml: 246,
  divide: 247,
  oslash: 248,
  ugrave: 249,
  uacute: 250,
  ucirc: 251,
  uuml: 252,
  yacute: 253,
  thorn: 254,
  yuml: 255,
  OElig: 338,
  oelig: 339,
  Scaron: 352,
  scaron: 353,
  Yuml: 376,
  fnof: 402,
  circ: 710,
  tilde: 732,
  Alpha: 913,
  Beta: 914,
  Gamma: 915,
  Delta: 916,
  Epsilon: 917,
  Zeta: 918,
  Eta: 919,
  Theta: 920,
  Iota: 921,
  Kappa: 922,
  Lambda: 923,
  Mu: 924,
  Nu: 925,
  Xi: 926,
  Omicron: 927,
  Pi: 928,
  Rho: 929,
  Sigma: 931,
  Tau: 932,
  Upsilon: 933,
  Phi: 934,
  Chi: 935,
  Psi: 936,
  Omega: 937,
  alpha: 945,
  beta: 946,
  gamma: 947,
  delta: 948,
  epsilon: 949,
  zeta: 950,
  eta: 951,
  theta: 952,
  iota: 953,
  kappa: 954,
  lambda: 955,
  mu: 956,
  nu: 957,
  xi: 958,
  omicron: 959,
  pi: 960,
  rho: 961,
  sigmaf: 962,
  sigma: 963,
  tau: 964,
  upsilon: 965,
  phi: 966,
  chi: 967,
  psi: 968,
  omega: 969,
  thetasym: 977,
  upsih: 978,
  piv: 982,
  ensp: 8194,
  emsp: 8195,
  thinsp: 8201,
  zwnj: 8204,
  zwj: 8205,
  lrm: 8206,
  rlm: 8207,
  ndash: 8211,
  mdash: 8212,
  lsquo: 8216,
  rsquo: 8217,
  sbquo: 8218,
  ldquo: 8220,
  rdquo: 8221,
  bdquo: 8222,
  dagger: 8224,
  Dagger: 8225,
  bull: 8226,
  hellip: 8230,
  permil: 8240,
  prime: 8242,
  Prime: 8243,
  lsaquo: 8249,
  rsaquo: 8250,
  oline: 8254,
  frasl: 8260,
  euro: 8364,
  image: 8465,
  weierp: 8472,
  real: 8476,
  trade: 8482,
  alefsym: 8501,
  larr: 8592,
  uarr: 8593,
  rarr: 8594,
  darr: 8595,
  harr: 8596,
  crarr: 8629,
  lArr: 8656,
  uArr: 8657,
  rArr: 8658,
  dArr: 8659,
  hArr: 8660,
  forall: 8704,
  part: 8706,
  exist: 8707,
  empty: 8709,
  nabla: 8711,
  isin: 8712,
  notin: 8713,
  ni: 8715,
  prod: 8719,
  sum: 8721,
  minus: 8722,
  lowast: 8727,
  radic: 8730,
  prop: 8733,
  infin: 8734,
  ang: 8736,
  and: 8743,
  or: 8744,
  cap: 8745,
  cup: 8746,
  int: 8747,
  there4: 8756,
  sim: 8764,
  cong: 8773,
  asymp: 8776,
  ne: 8800,
  equiv: 8801,
  le: 8804,
  ge: 8805,
  sub: 8834,
  sup: 8835,
  nsub: 8836,
  sube: 8838,
  supe: 8839,
  oplus: 8853,
  otimes: 8855,
  perp: 8869,
  sdot: 8901,
  lceil: 8968,
  rceil: 8969,
  lfloor: 8970,
  rfloor: 8971,
  lang: 9001,
  rang: 9002,
  loz: 9674,
  spades: 9824,
  clubs: 9827,
  hearts: 9829,
  diams: 9830
}));

// src/compiler/transformers/es2016.ts
function transformES2016(context) {
  const {
    factory: factory2,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    if ((node.transformFlags & 512 /* ContainsES2016 */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    switch (node.kind) {
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitBinaryExpression(node) {
    switch (node.operatorToken.kind) {
      case 68 /* AsteriskAsteriskEqualsToken */:
        return visitExponentiationAssignmentExpression(node);
      case 43 /* AsteriskAsteriskToken */:
        return visitExponentiationExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitExponentiationAssignmentExpression(node) {
    let target;
    let value;
    const left ***REMOVED*** visitNode(node.left, visitor, isExpression);
    const right ***REMOVED*** visitNode(node.right, visitor, isExpression);
    if (isElementAccessExpression(left)) {
      const expressionTemp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      const argumentExpressionTemp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      target ***REMOVED*** setTextRange(
        factory2.createElementAccessExpression(
          setTextRange(factory2.createAssignment(expressionTemp, left.expression), left.expression),
          setTextRange(factory2.createAssignment(argumentExpressionTemp, left.argumentExpression), left.argumentExpression)
        ),
        left
      );
      value ***REMOVED*** setTextRange(
        factory2.createElementAccessExpression(
          expressionTemp,
          argumentExpressionTemp
        ),
        left
      );
    } else if (isPropertyAccessExpression(left)) {
      const expressionTemp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      target ***REMOVED*** setTextRange(
        factory2.createPropertyAccessExpression(
          setTextRange(factory2.createAssignment(expressionTemp, left.expression), left.expression),
          left.name
        ),
        left
      );
      value ***REMOVED*** setTextRange(
        factory2.createPropertyAccessExpression(
          expressionTemp,
          left.name
        ),
        left
      );
    } else {
      target ***REMOVED*** left;
      value ***REMOVED*** left;
    }
    return setTextRange(
      factory2.createAssignment(
        target,
        setTextRange(factory2.createGlobalMethodCall("Math", "pow", [value, right]), node)
      ),
      node
    );
  }
  function visitExponentiationExpression(node) {
    const left ***REMOVED*** visitNode(node.left, visitor, isExpression);
    const right ***REMOVED*** visitNode(node.right, visitor, isExpression);
    return setTextRange(factory2.createGlobalMethodCall("Math", "pow", [left, right]), node);
  }
}

// src/compiler/transformers/es2015.ts
function createSpreadSegment(kind, expression) {
  return { kind, expression };
}
function transformES2015(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    startLexicalEnvironment,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const resolver ***REMOVED*** context.getEmitResolver();
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  let currentSourceFile;
  let currentText;
  let hierarchyFacts;
  let taggedTemplateStringDeclarations;
  function recordTaggedTemplateString(temp) {
    taggedTemplateStringDeclarations ***REMOVED*** append(
      taggedTemplateStringDeclarations,
      factory2.createVariableDeclaration(temp)
    );
  }
  let convertedLoopState;
  let enabledSubstitutions ***REMOVED*** 0 /* None */;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    currentText ***REMOVED*** node.text;
    const visited ***REMOVED*** visitSourceFile(node);
    addEmitHelpers(visited, context.readEmitHelpers());
    currentSourceFile ***REMOVED*** void 0;
    currentText ***REMOVED*** void 0;
    taggedTemplateStringDeclarations ***REMOVED*** void 0;
    hierarchyFacts ***REMOVED*** 0 /* None */;
    return visited;
  }
  function enterSubtree(excludeFacts, includeFacts) {
    const ancestorFacts ***REMOVED*** hierarchyFacts;
    hierarchyFacts ***REMOVED*** (hierarchyFacts & ~excludeFacts | includeFacts) & 32767 /* AncestorFactsMask */;
    return ancestorFacts;
  }
  function exitSubtree(ancestorFacts, excludeFacts, includeFacts) {
    hierarchyFacts ***REMOVED*** (hierarchyFacts & ~excludeFacts | includeFacts) & -32768 /* SubtreeFactsMask */ | ancestorFacts;
  }
  function isReturnVoidStatementInConstructorWithCapturedSuper(node) {
    return (hierarchyFacts & 8192 /* ConstructorWithSuperCall */) !***REMOVED******REMOVED*** 0 && node.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */ && !node.expression;
  }
  function isOrMayContainReturnCompletion(node) {
    return node.transformFlags & 4194304 /* ContainsHoistedDeclarationOrCompletion */ && (isReturnStatement(node) || isIfStatement(node) || isWithStatement(node) || isSwitchStatement(node) || isCaseBlock(node) || isCaseClause(node) || isDefaultClause(node) || isTryStatement(node) || isCatchClause(node) || isLabeledStatement(node) || isIterationStatement(
      node,
      /*lookInLabeledStatements*/
      false
    ) || isBlock(node));
  }
  function shouldVisitNode(node) {
    return (node.transformFlags & 1024 /* ContainsES2015 */) !***REMOVED******REMOVED*** 0 || convertedLoopState !***REMOVED******REMOVED*** void 0 || hierarchyFacts & 8192 /* ConstructorWithSuperCall */ && isOrMayContainReturnCompletion(node) || isIterationStatement(
      node,
      /*lookInLabeledStatements*/
      false
    ) && shouldConvertIterationStatement(node) || (getInternalEmitFlags(node) & 1 /* TypeScriptClassWrapper */) !***REMOVED******REMOVED*** 0;
  }
  function visitor(node) {
    return shouldVisitNode(node) ? visitorWorker(
      node,
      /*expressionResultIsUnused*/
      false
    ) : node;
  }
  function visitorWithUnusedExpressionResult(node) {
    return shouldVisitNode(node) ? visitorWorker(
      node,
      /*expressionResultIsUnused*/
      true
    ) : node;
  }
  function classWrapperStatementVisitor(node) {
    if (shouldVisitNode(node)) {
      const original ***REMOVED*** getOriginalNode(node);
      if (isPropertyDeclaration(original) && hasStaticModifier(original)) {
        const ancestorFacts ***REMOVED*** enterSubtree(
          32670 /* StaticInitializerExcludes */,
          16449 /* StaticInitializerIncludes */
        );
        const result ***REMOVED*** visitorWorker(
          node,
          /*expressionResultIsUnused*/
          false
        );
        exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
        return result;
      }
      return visitorWorker(
        node,
        /*expressionResultIsUnused*/
        false
      );
    }
    return node;
  }
  function callExpressionVisitor(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return visitSuperKeyword(
        node,
        /*isExpressionOfCall*/
        true
      );
    }
    return visitor(node);
  }
  function visitorWorker(node, expressionResultIsUnused2) {
    switch (node.kind) {
      case 126 /* StaticKeyword */:
        return void 0;
      // elide static keyword
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 232 /* ClassExpression */:
        return visitClassExpression(node);
      case 170 /* Parameter */:
        return visitParameter(node);
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 220 /* ArrowFunction */:
        return visitArrowFunction(node);
      case 219 /* FunctionExpression */:
        return visitFunctionExpression(node);
      case 261 /* VariableDeclaration */:
        return visitVariableDeclaration(node);
      case 80 /* Identifier */:
        return visitIdentifier(node);
      case 262 /* VariableDeclarationList */:
        return visitVariableDeclarationList(node);
      case 256 /* SwitchStatement */:
        return visitSwitchStatement(node);
      case 270 /* CaseBlock */:
        return visitCaseBlock(node);
      case 242 /* Block */:
        return visitBlock(
          node,
          /*isFunctionBody*/
          false
        );
      case 253 /* BreakStatement */:
      case 252 /* ContinueStatement */:
        return visitBreakOrContinueStatement(node);
      case 257 /* LabeledStatement */:
        return visitLabeledStatement(node);
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
        return visitDoOrWhileStatement(
          node,
          /*outermostLabeledStatement*/
          void 0
        );
      case 249 /* ForStatement */:
        return visitForStatement(
          node,
          /*outermostLabeledStatement*/
          void 0
        );
      case 250 /* ForInStatement */:
        return visitForInStatement(
          node,
          /*outermostLabeledStatement*/
          void 0
        );
      case 251 /* ForOfStatement */:
        return visitForOfStatement(
          node,
          /*outermostLabeledStatement*/
          void 0
        );
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 211 /* ObjectLiteralExpression */:
        return visitObjectLiteralExpression(node);
      case 300 /* CatchClause */:
        return visitCatchClause(node);
      case 305 /* ShorthandPropertyAssignment */:
        return visitShorthandPropertyAssignment(node);
      case 168 /* ComputedPropertyName */:
        return visitComputedPropertyName(node);
      case 210 /* ArrayLiteralExpression */:
        return visitArrayLiteralExpression(node);
      case 214 /* CallExpression */:
        return visitCallExpression(node);
      case 215 /* NewExpression */:
        return visitNewExpression(node);
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(node, expressionResultIsUnused2);
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(node, expressionResultIsUnused2);
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(node, expressionResultIsUnused2);
      case 15 /* NoSubstitutionTemplateLiteral */:
      case 16 /* TemplateHead */:
      case 17 /* TemplateMiddle */:
      case 18 /* TemplateTail */:
        return visitTemplateLiteral(node);
      case 11 /* StringLiteral */:
        return visitStringLiteral(node);
      case 9 /* NumericLiteral */:
        return visitNumericLiteral(node);
      case 216 /* TaggedTemplateExpression */:
        return visitTaggedTemplateExpression(node);
      case 229 /* TemplateExpression */:
        return visitTemplateExpression(node);
      case 230 /* YieldExpression */:
        return visitYieldExpression(node);
      case 231 /* SpreadElement */:
        return visitSpreadElement(node);
      case 108 /* SuperKeyword */:
        return visitSuperKeyword(
          node,
          /*isExpressionOfCall*/
          false
        );
      case 110 /* ThisKeyword */:
        return visitThisKeyword(node);
      case 237 /* MetaProperty */:
        return visitMetaProperty(node);
      case 175 /* MethodDeclaration */:
        return visitMethodDeclaration(node);
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return visitAccessorDeclaration(node);
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 254 /* ReturnStatement */:
        return visitReturnStatement(node);
      case 223 /* VoidExpression */:
        return visitVoidExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitSourceFile(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(8064 /* SourceFileExcludes */, 64 /* SourceFileIncludes */);
    const prologue ***REMOVED*** [];
    const statements ***REMOVED*** [];
    startLexicalEnvironment();
    const statementOffset ***REMOVED*** factory2.copyPrologue(
      node.statements,
      prologue,
      /*ensureUseStrict*/
      false,
      visitor
    );
    addRange(statements, visitNodes2(node.statements, visitor, isStatement, statementOffset));
    if (taggedTemplateStringDeclarations) {
      statements.push(
        factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          factory2.createVariableDeclarationList(taggedTemplateStringDeclarations)
        )
      );
    }
    factory2.mergeLexicalEnvironment(prologue, endLexicalEnvironment());
    insertCaptureThisForNodeIfNeeded(prologue, node);
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return factory2.updateSourceFile(
      node,
      setTextRange(factory2.createNodeArray(concatenate(prologue, statements)), node.statements)
    );
  }
  function visitSwitchStatement(node) {
    if (convertedLoopState !***REMOVED******REMOVED*** void 0) {
      const savedAllowedNonLabeledJumps ***REMOVED*** convertedLoopState.allowedNonLabeledJumps;
      convertedLoopState.allowedNonLabeledJumps |***REMOVED*** 2 /* Break */;
      const result ***REMOVED*** visitEachChild(node, visitor, context);
      convertedLoopState.allowedNonLabeledJumps ***REMOVED*** savedAllowedNonLabeledJumps;
      return result;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCaseBlock(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(7104 /* BlockScopeExcludes */, 0 /* BlockScopeIncludes */);
    const updated ***REMOVED*** visitEachChild(node, visitor, context);
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function returnCapturedThis(node) {
    return setOriginalNode(factory2.createReturnStatement(createCapturedThis()), node);
  }
  function createCapturedThis() {
    return factory2.createUniqueName("_this", 16 /* Optimistic */ | 32 /* FileLevel */);
  }
  function visitReturnStatement(node) {
    if (convertedLoopState) {
      convertedLoopState.nonLocalJumps |***REMOVED*** 8 /* Return */;
      if (isReturnVoidStatementInConstructorWithCapturedSuper(node)) {
        node ***REMOVED*** returnCapturedThis(node);
      }
      return factory2.createReturnStatement(
        factory2.createObjectLiteralExpression(
          [
            factory2.createPropertyAssignment(
              factory2.createIdentifier("value"),
              node.expression ? Debug.checkDefined(visitNode(node.expression, visitor, isExpression)) : factory2.createVoidZero()
            )
          ]
        )
      );
    } else if (isReturnVoidStatementInConstructorWithCapturedSuper(node)) {
      return returnCapturedThis(node);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitThisKeyword(node) {
    hierarchyFacts |***REMOVED*** 65536 /* LexicalThis */;
    if (hierarchyFacts & 2 /* ArrowFunction */ && !(hierarchyFacts & 16384 /* StaticInitializer */)) {
      hierarchyFacts |***REMOVED*** 131072 /* CapturedLexicalThis */;
    }
    if (convertedLoopState) {
      if (hierarchyFacts & 2 /* ArrowFunction */) {
        convertedLoopState.containsLexicalThis ***REMOVED*** true;
        return node;
      }
      return convertedLoopState.thisName || (convertedLoopState.thisName ***REMOVED*** factory2.createUniqueName("this"));
    }
    return node;
  }
  function visitVoidExpression(node) {
    return visitEachChild(node, visitorWithUnusedExpressionResult, context);
  }
  function visitIdentifier(node) {
    if (convertedLoopState) {
      if (resolver.isArgumentsLocalBinding(node)) {
        return convertedLoopState.argumentsName || (convertedLoopState.argumentsName ***REMOVED*** factory2.createUniqueName("arguments"));
      }
    }
    if (node.flags & 256 /* IdentifierHasExtendedUnicodeEscape */) {
      return setOriginalNode(
        setTextRange(
          factory2.createIdentifier(unescapeLeadingUnderscores(node.escapedText)),
          node
        ),
        node
      );
    }
    return node;
  }
  function visitBreakOrContinueStatement(node) {
    if (convertedLoopState) {
      const jump ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */ ? 2 /* Break */ : 4 /* Continue */;
      const canUseBreakOrContinue ***REMOVED*** node.label && convertedLoopState.labels && convertedLoopState.labels.get(idText(node.label)) || !node.label && convertedLoopState.allowedNonLabeledJumps & jump;
      if (!canUseBreakOrContinue) {
        let labelMarker;
        const label ***REMOVED*** node.label;
        if (!label) {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */) {
            convertedLoopState.nonLocalJumps |***REMOVED*** 2 /* Break */;
            labelMarker ***REMOVED*** "break";
          } else {
            convertedLoopState.nonLocalJumps |***REMOVED*** 4 /* Continue */;
            labelMarker ***REMOVED*** "continue";
          }
        } else {
          if (node.kind ***REMOVED******REMOVED******REMOVED*** 253 /* BreakStatement */) {
            labelMarker ***REMOVED*** `break-${label.escapedText}`;
            setLabeledJump(
              convertedLoopState,
              /*isBreak*/
              true,
              idText(label),
              labelMarker
            );
          } else {
            labelMarker ***REMOVED*** `continue-${label.escapedText}`;
            setLabeledJump(
              convertedLoopState,
              /*isBreak*/
              false,
              idText(label),
              labelMarker
            );
          }
        }
        let returnExpression ***REMOVED*** factory2.createStringLiteral(labelMarker);
        if (convertedLoopState.loopOutParameters.length) {
          const outParams ***REMOVED*** convertedLoopState.loopOutParameters;
          let expr;
          for (let i ***REMOVED*** 0; i < outParams.length; i++) {
            const copyExpr ***REMOVED*** copyOutParameter(outParams[i], 1 /* ToOutParameter */);
            if (i ***REMOVED******REMOVED******REMOVED*** 0) {
              expr ***REMOVED*** copyExpr;
            } else {
              expr ***REMOVED*** factory2.createBinaryExpression(expr, 28 /* CommaToken */, copyExpr);
            }
          }
          returnExpression ***REMOVED*** factory2.createBinaryExpression(expr, 28 /* CommaToken */, returnExpression);
        }
        return factory2.createReturnStatement(returnExpression);
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitClassDeclaration(node) {
    const variable ***REMOVED*** factory2.createVariableDeclaration(
      factory2.getLocalName(
        node,
        /*allowComments*/
        true
      ),
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      transformClassLikeDeclarationToExpression(node)
    );
    setOriginalNode(variable, node);
    const statements ***REMOVED*** [];
    const statement ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      factory2.createVariableDeclarationList([variable])
    );
    setOriginalNode(statement, node);
    setTextRange(statement, node);
    startOnNewLine(statement);
    statements.push(statement);
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      const exportStatement ***REMOVED*** hasSyntacticModifier(node, 2048 /* Default */) ? factory2.createExportDefault(factory2.getLocalName(node)) : factory2.createExternalModuleExport(factory2.getLocalName(node));
      setOriginalNode(exportStatement, statement);
      statements.push(exportStatement);
    }
    return singleOrMany(statements);
  }
  function visitClassExpression(node) {
    return transformClassLikeDeclarationToExpression(node);
  }
  function transformClassLikeDeclarationToExpression(node) {
    if (node.name) {
      enableSubstitutionsForBlockScopedBindings();
    }
    const extendsClauseElement ***REMOVED*** getClassExtendsHeritageElement(node);
    const classFunction ***REMOVED*** factory2.createFunctionExpression(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      extendsClauseElement ? [factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        createSyntheticSuper()
      )] : [],
      /*type*/
      void 0,
      transformClassBody(node, extendsClauseElement)
    );
    setEmitFlags(classFunction, getEmitFlags(node) & 131072 /* Indented */ | 1048576 /* ReuseTempVariableScope */);
    const inner ***REMOVED*** factory2.createPartiallyEmittedExpression(classFunction);
    setTextRangeEnd(inner, node.end);
    setEmitFlags(inner, 3072 /* NoComments */);
    const outer ***REMOVED*** factory2.createPartiallyEmittedExpression(inner);
    setTextRangeEnd(outer, skipTrivia(currentText, node.pos));
    setEmitFlags(outer, 3072 /* NoComments */);
    const result ***REMOVED*** factory2.createParenthesizedExpression(
      factory2.createCallExpression(
        outer,
        /*typeArguments*/
        void 0,
        extendsClauseElement ? [Debug.checkDefined(visitNode(extendsClauseElement.expression, visitor, isExpression))] : []
      )
    );
    addSyntheticLeadingComment(result, 3 /* MultiLineCommentTrivia */, "* @class ");
    return result;
  }
  function transformClassBody(node, extendsClauseElement) {
    const statements ***REMOVED*** [];
    const name ***REMOVED*** factory2.getInternalName(node);
    const constructorLikeName ***REMOVED*** isIdentifierANonContextualKeyword(name) ? factory2.getGeneratedNameForNode(name) : name;
    startLexicalEnvironment();
    addExtendsHelperIfNeeded(statements, node, extendsClauseElement);
    addConstructor(statements, node, constructorLikeName, extendsClauseElement);
    addClassMembers(statements, node);
    const closingBraceLocation ***REMOVED*** createTokenRange(skipTrivia(currentText, node.members.end), 20 /* CloseBraceToken */);
    const outer ***REMOVED*** factory2.createPartiallyEmittedExpression(constructorLikeName);
    setTextRangeEnd(outer, closingBraceLocation.end);
    setEmitFlags(outer, 3072 /* NoComments */);
    const statement ***REMOVED*** factory2.createReturnStatement(outer);
    setTextRangePos(statement, closingBraceLocation.pos);
    setEmitFlags(statement, 3072 /* NoComments */ | 768 /* NoTokenSourceMaps */);
    statements.push(statement);
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    const block ***REMOVED*** factory2.createBlock(
      setTextRange(
        factory2.createNodeArray(statements),
        /*location*/
        node.members
      ),
      /*multiLine*/
      true
    );
    setEmitFlags(block, 3072 /* NoComments */);
    return block;
  }
  function addExtendsHelperIfNeeded(statements, node, extendsClauseElement) {
    if (extendsClauseElement) {
      statements.push(
        setTextRange(
          factory2.createExpressionStatement(
            emitHelpers().createExtendsHelper(factory2.getInternalName(node))
          ),
          /*location*/
          extendsClauseElement
        )
      );
    }
  }
  function addConstructor(statements, node, name, extendsClauseElement) {
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const ancestorFacts ***REMOVED*** enterSubtree(32662 /* ConstructorExcludes */, 73 /* ConstructorIncludes */);
    const constructor ***REMOVED*** getFirstConstructorWithBody(node);
    const hasSynthesizedSuper ***REMOVED*** hasSynthesizedDefaultSuperCall(constructor, extendsClauseElement !***REMOVED******REMOVED*** void 0);
    const constructorFunction ***REMOVED*** factory2.createFunctionDeclaration(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      name,
      /*typeParameters*/
      void 0,
      transformConstructorParameters(constructor, hasSynthesizedSuper),
      /*type*/
      void 0,
      transformConstructorBody(constructor, node, extendsClauseElement, hasSynthesizedSuper)
    );
    setTextRange(constructorFunction, constructor || node);
    if (extendsClauseElement) {
      setEmitFlags(constructorFunction, 16 /* CapturesThis */);
    }
    statements.push(constructorFunction);
    exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
  }
  function transformConstructorParameters(constructor, hasSynthesizedSuper) {
    return visitParameterList(constructor && !hasSynthesizedSuper ? constructor.parameters : void 0, visitor, context) || [];
  }
  function createDefaultConstructorBody(node, isDerivedClass) {
    const statements ***REMOVED*** [];
    resumeLexicalEnvironment();
    factory2.mergeLexicalEnvironment(statements, endLexicalEnvironment());
    if (isDerivedClass) {
      statements.push(factory2.createReturnStatement(createDefaultSuperCallOrThis()));
    }
    const statementsArray ***REMOVED*** factory2.createNodeArray(statements);
    setTextRange(statementsArray, node.members);
    const block ***REMOVED*** factory2.createBlock(
      statementsArray,
      /*multiLine*/
      true
    );
    setTextRange(block, node);
    setEmitFlags(block, 3072 /* NoComments */);
    return block;
  }
  function isUninitializedVariableStatement(node) {
    return isVariableStatement(node) && every(node.declarationList.declarations, (decl) ***REMOVED***> isIdentifier(decl.name) && !decl.initializer);
  }
  function containsSuperCall(node) {
    if (isSuperCall(node)) {
      return true;
    }
    if (!(node.transformFlags & 134217728 /* ContainsLexicalSuper */)) {
      return false;
    }
    switch (node.kind) {
      // stop at function boundaries
      case 220 /* ArrowFunction */:
      case 219 /* FunctionExpression */:
      case 263 /* FunctionDeclaration */:
      case 177 /* Constructor */:
      case 176 /* ClassStaticBlockDeclaration */:
        return false;
      // only step into computed property names for class and object literal elements
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 175 /* MethodDeclaration */:
      case 173 /* PropertyDeclaration */: {
        const named ***REMOVED*** node;
        if (isComputedPropertyName(named.name)) {
          return !!forEachChild(named.name, containsSuperCall);
        }
        return false;
      }
    }
    return !!forEachChild(node, containsSuperCall);
  }
  function transformConstructorBody(constructor, node, extendsClauseElement, hasSynthesizedSuper) {
    const isDerivedClass ***REMOVED*** !!extendsClauseElement && skipOuterExpressions(extendsClauseElement.expression).kind !***REMOVED******REMOVED*** 106 /* NullKeyword */;
    if (!constructor) return createDefaultConstructorBody(node, isDerivedClass);
    const prologue ***REMOVED*** [];
    const statements ***REMOVED*** [];
    resumeLexicalEnvironment();
    const standardPrologueEnd ***REMOVED*** factory2.copyStandardPrologue(
      constructor.body.statements,
      prologue,
      /*statementOffset*/
      0
    );
    if (hasSynthesizedSuper || containsSuperCall(constructor.body)) {
      hierarchyFacts |***REMOVED*** 8192 /* ConstructorWithSuperCall */;
    }
    addRange(statements, visitNodes2(constructor.body.statements, visitor, isStatement, standardPrologueEnd));
    const mayReplaceThis ***REMOVED*** isDerivedClass || hierarchyFacts & 8192 /* ConstructorWithSuperCall */;
    addDefaultValueAssignmentsIfNeeded2(prologue, constructor);
    addRestParameterIfNeeded(prologue, constructor, hasSynthesizedSuper);
    insertCaptureNewTargetIfNeeded(prologue, constructor);
    if (mayReplaceThis) {
      insertCaptureThisForNode(prologue, constructor, createActualThis());
    } else {
      insertCaptureThisForNodeIfNeeded(prologue, constructor);
    }
    factory2.mergeLexicalEnvironment(prologue, endLexicalEnvironment());
    if (mayReplaceThis && !isSufficientlyCoveredByReturnStatements(constructor.body)) {
      statements.push(factory2.createReturnStatement(createCapturedThis()));
    }
    const body ***REMOVED*** factory2.createBlock(
      setTextRange(
        factory2.createNodeArray(
          [
            ...prologue,
            ...statements
          ]
        ),
        /*location*/
        constructor.body.statements
      ),
      /*multiLine*/
      true
    );
    setTextRange(body, constructor.body);
    return simplifyConstructor(body, constructor.body, hasSynthesizedSuper);
  }
  function isCapturedThis(node) {
    return isGeneratedIdentifier(node) && idText(node) ***REMOVED******REMOVED******REMOVED*** "_this";
  }
  function isSyntheticSuper(node) {
    return isGeneratedIdentifier(node) && idText(node) ***REMOVED******REMOVED******REMOVED*** "_super";
  }
  function isThisCapturingVariableStatement(node) {
    return isVariableStatement(node) && node.declarationList.declarations.length ***REMOVED******REMOVED******REMOVED*** 1 && isThisCapturingVariableDeclaration(node.declarationList.declarations[0]);
  }
  function isThisCapturingVariableDeclaration(node) {
    return isVariableDeclaration(node) && isCapturedThis(node.name) && !!node.initializer;
  }
  function isThisCapturingAssignment(node) {
    return isAssignmentExpression(
      node,
      /*excludeCompoundAssignment*/
      true
    ) && isCapturedThis(node.left);
  }
  function isTransformedSuperCall(node) {
    return isCallExpression(node) && isPropertyAccessExpression(node.expression) && isSyntheticSuper(node.expression.expression) && isIdentifier(node.expression.name) && (idText(node.expression.name) ***REMOVED******REMOVED******REMOVED*** "call" || idText(node.expression.name) ***REMOVED******REMOVED******REMOVED*** "apply") && node.arguments.length >***REMOVED*** 1 && node.arguments[0].kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */;
  }
  function isTransformedSuperCallWithFallback(node) {
    return isBinaryExpression(node) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ && node.right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && isTransformedSuperCall(node.left);
  }
  function isImplicitSuperCall(node) {
    return isBinaryExpression(node) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */ && isBinaryExpression(node.left) && node.left.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 38 /* ExclamationEqualsEqualsToken */ && isSyntheticSuper(node.left.left) && node.left.right.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */ && isTransformedSuperCall(node.right) && idText(node.right.expression.name) ***REMOVED******REMOVED******REMOVED*** "apply";
  }
  function isImplicitSuperCallWithFallback(node) {
    return isBinaryExpression(node) && node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 57 /* BarBarToken */ && node.right.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */ && isImplicitSuperCall(node.left);
  }
  function isThisCapturingTransformedSuperCallWithFallback(node) {
    return isThisCapturingAssignment(node) && isTransformedSuperCallWithFallback(node.right);
  }
  function isThisCapturingImplicitSuperCallWithFallback(node) {
    return isThisCapturingAssignment(node) && isImplicitSuperCallWithFallback(node.right);
  }
  function isTransformedSuperCallLike(node) {
    return isTransformedSuperCall(node) || isTransformedSuperCallWithFallback(node) || isThisCapturingTransformedSuperCallWithFallback(node) || isImplicitSuperCall(node) || isImplicitSuperCallWithFallback(node) || isThisCapturingImplicitSuperCallWithFallback(node);
  }
  function simplifyConstructorInlineSuperInThisCaptureVariable(body) {
    for (let i ***REMOVED*** 0; i < body.statements.length - 1; i++) {
      const statement ***REMOVED*** body.statements[i];
      if (!isThisCapturingVariableStatement(statement)) {
        continue;
      }
      const varDecl ***REMOVED*** statement.declarationList.declarations[0];
      if (varDecl.initializer.kind !***REMOVED******REMOVED*** 110 /* ThisKeyword */) {
        continue;
      }
      const thisCaptureStatementIndex ***REMOVED*** i;
      let superCallIndex ***REMOVED*** i + 1;
      while (superCallIndex < body.statements.length) {
        const statement2 ***REMOVED*** body.statements[superCallIndex];
        if (isExpressionStatement(statement2)) {
          if (isTransformedSuperCallLike(skipOuterExpressions(statement2.expression))) {
            break;
          }
        }
        if (isUninitializedVariableStatement(statement2)) {
          superCallIndex++;
          continue;
        }
        return body;
      }
      const following ***REMOVED*** body.statements[superCallIndex];
      let expression ***REMOVED*** following.expression;
      if (isThisCapturingAssignment(expression)) {
        expression ***REMOVED*** expression.right;
      }
      const newVarDecl ***REMOVED*** factory2.updateVariableDeclaration(
        varDecl,
        varDecl.name,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        expression
      );
      const newDeclList ***REMOVED*** factory2.updateVariableDeclarationList(statement.declarationList, [newVarDecl]);
      const newVarStatement ***REMOVED*** factory2.createVariableStatement(statement.modifiers, newDeclList);
      setOriginalNode(newVarStatement, following);
      setTextRange(newVarStatement, following);
      const newStatements ***REMOVED*** factory2.createNodeArray([
        ...body.statements.slice(0, thisCaptureStatementIndex),
        // copy statements preceding to `var _this`
        ...body.statements.slice(thisCaptureStatementIndex + 1, superCallIndex),
        // copy intervening temp variables
        newVarStatement,
        ...body.statements.slice(superCallIndex + 1)
        // copy statements following `super.call(this, ...)`
      ]);
      setTextRange(newStatements, body.statements);
      return factory2.updateBlock(body, newStatements);
    }
    return body;
  }
  function simplifyConstructorInlineSuperReturn(body, original) {
    for (const statement of original.statements) {
      if (statement.transformFlags & 134217728 /* ContainsLexicalSuper */ && !getSuperCallFromStatement(statement)) {
        return body;
      }
    }
    const canElideThisCapturingVariable ***REMOVED*** !(original.transformFlags & 16384 /* ContainsLexicalThis */) && !(hierarchyFacts & 65536 /* LexicalThis */) && !(hierarchyFacts & 131072 /* CapturedLexicalThis */);
    for (let i ***REMOVED*** body.statements.length - 1; i > 0; i--) {
      const statement ***REMOVED*** body.statements[i];
      if (isReturnStatement(statement) && statement.expression && isCapturedThis(statement.expression)) {
        const preceding ***REMOVED*** body.statements[i - 1];
        let expression;
        if (isExpressionStatement(preceding) && isThisCapturingTransformedSuperCallWithFallback(skipOuterExpressions(preceding.expression))) {
          expression ***REMOVED*** preceding.expression;
        } else if (canElideThisCapturingVariable && isThisCapturingVariableStatement(preceding)) {
          const varDecl ***REMOVED*** preceding.declarationList.declarations[0];
          if (isTransformedSuperCallLike(skipOuterExpressions(varDecl.initializer))) {
            expression ***REMOVED*** factory2.createAssignment(
              createCapturedThis(),
              varDecl.initializer
            );
          }
        }
        if (!expression) {
          break;
        }
        const newReturnStatement ***REMOVED*** factory2.createReturnStatement(expression);
        setOriginalNode(newReturnStatement, preceding);
        setTextRange(newReturnStatement, preceding);
        const newStatements ***REMOVED*** factory2.createNodeArray([
          ...body.statements.slice(0, i - 1),
          // copy all statements preceding `_super.call(this, ...)`
          newReturnStatement,
          ...body.statements.slice(i + 1)
          // copy all statements following `return _this;`
        ]);
        setTextRange(newStatements, body.statements);
        return factory2.updateBlock(body, newStatements);
      }
    }
    return body;
  }
  function elideUnusedThisCaptureWorker(node) {
    if (isThisCapturingVariableStatement(node)) {
      const varDecl ***REMOVED*** node.declarationList.declarations[0];
      if (varDecl.initializer.kind ***REMOVED******REMOVED******REMOVED*** 110 /* ThisKeyword */) {
        return void 0;
      }
    } else if (isThisCapturingAssignment(node)) {
      return factory2.createPartiallyEmittedExpression(node.right, node);
    }
    switch (node.kind) {
      // stop at function boundaries
      case 220 /* ArrowFunction */:
      case 219 /* FunctionExpression */:
      case 263 /* FunctionDeclaration */:
      case 177 /* Constructor */:
      case 176 /* ClassStaticBlockDeclaration */:
        return node;
      // only step into computed property names for class and object literal elements
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 175 /* MethodDeclaration */:
      case 173 /* PropertyDeclaration */: {
        const named ***REMOVED*** node;
        if (isComputedPropertyName(named.name)) {
          return factory2.replacePropertyName(named, visitEachChild(
            named.name,
            elideUnusedThisCaptureWorker,
            /*context*/
            void 0
          ));
        }
        return node;
      }
    }
    return visitEachChild(
      node,
      elideUnusedThisCaptureWorker,
      /*context*/
      void 0
    );
  }
  function simplifyConstructorElideUnusedThisCapture(body, original) {
    if (original.transformFlags & 16384 /* ContainsLexicalThis */ || hierarchyFacts & 65536 /* LexicalThis */ || hierarchyFacts & 131072 /* CapturedLexicalThis */) {
      return body;
    }
    for (const statement of original.statements) {
      if (statement.transformFlags & 134217728 /* ContainsLexicalSuper */ && !getSuperCallFromStatement(statement)) {
        return body;
      }
    }
    return factory2.updateBlock(body, visitNodes2(body.statements, elideUnusedThisCaptureWorker, isStatement));
  }
  function injectSuperPresenceCheckWorker(node) {
    if (isTransformedSuperCall(node) && node.arguments.length ***REMOVED******REMOVED******REMOVED*** 2 && isIdentifier(node.arguments[1]) && idText(node.arguments[1]) ***REMOVED******REMOVED******REMOVED*** "arguments") {
      return factory2.createLogicalAnd(
        factory2.createStrictInequality(
          createSyntheticSuper(),
          factory2.createNull()
        ),
        node
      );
    }
    switch (node.kind) {
      // stop at function boundaries
      case 220 /* ArrowFunction */:
      case 219 /* FunctionExpression */:
      case 263 /* FunctionDeclaration */:
      case 177 /* Constructor */:
      case 176 /* ClassStaticBlockDeclaration */:
        return node;
      // only step into computed property names for class and object literal elements
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
      case 175 /* MethodDeclaration */:
      case 173 /* PropertyDeclaration */: {
        const named ***REMOVED*** node;
        if (isComputedPropertyName(named.name)) {
          return factory2.replacePropertyName(named, visitEachChild(
            named.name,
            injectSuperPresenceCheckWorker,
            /*context*/
            void 0
          ));
        }
        return node;
      }
    }
    return visitEachChild(
      node,
      injectSuperPresenceCheckWorker,
      /*context*/
      void 0
    );
  }
  function complicateConstructorInjectSuperPresenceCheck(body) {
    return factory2.updateBlock(body, visitNodes2(body.statements, injectSuperPresenceCheckWorker, isStatement));
  }
  function simplifyConstructor(body, original, hasSynthesizedSuper) {
    const inputBody ***REMOVED*** body;
    body ***REMOVED*** simplifyConstructorInlineSuperInThisCaptureVariable(body);
    body ***REMOVED*** simplifyConstructorInlineSuperReturn(body, original);
    if (body !***REMOVED******REMOVED*** inputBody) {
      body ***REMOVED*** simplifyConstructorElideUnusedThisCapture(body, original);
    }
    if (hasSynthesizedSuper) {
      body ***REMOVED*** complicateConstructorInjectSuperPresenceCheck(body);
    }
    return body;
  }
  function isSufficientlyCoveredByReturnStatements(statement) {
    if (statement.kind ***REMOVED******REMOVED******REMOVED*** 254 /* ReturnStatement */) {
      return true;
    } else if (statement.kind ***REMOVED******REMOVED******REMOVED*** 246 /* IfStatement */) {
      const ifStatement ***REMOVED*** statement;
      if (ifStatement.elseStatement) {
        return isSufficientlyCoveredByReturnStatements(ifStatement.thenStatement) && isSufficientlyCoveredByReturnStatements(ifStatement.elseStatement);
      }
    } else if (statement.kind ***REMOVED******REMOVED******REMOVED*** 242 /* Block */) {
      const lastStatement ***REMOVED*** lastOrUndefined(statement.statements);
      if (lastStatement && isSufficientlyCoveredByReturnStatements(lastStatement)) {
        return true;
      }
    }
    return false;
  }
  function createActualThis() {
    return setEmitFlags(factory2.createThis(), 8 /* NoSubstitution */);
  }
  function createDefaultSuperCallOrThis() {
    return factory2.createLogicalOr(
      factory2.createLogicalAnd(
        factory2.createStrictInequality(
          createSyntheticSuper(),
          factory2.createNull()
        ),
        factory2.createFunctionApplyCall(
          createSyntheticSuper(),
          createActualThis(),
          factory2.createIdentifier("arguments")
        )
      ),
      createActualThis()
    );
  }
  function visitParameter(node) {
    if (node.dotDotDotToken) {
      return void 0;
    } else if (isBindingPattern(node.name)) {
      return setOriginalNode(
        setTextRange(
          factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            factory2.getGeneratedNameForNode(node),
            /*questionToken*/
            void 0,
            /*type*/
            void 0,
            /*initializer*/
            void 0
          ),
          /*location*/
          node
        ),
        /*original*/
        node
      );
    } else if (node.initializer) {
      return setOriginalNode(
        setTextRange(
          factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            node.name,
            /*questionToken*/
            void 0,
            /*type*/
            void 0,
            /*initializer*/
            void 0
          ),
          /*location*/
          node
        ),
        /*original*/
        node
      );
    } else {
      return node;
    }
  }
  function hasDefaultValueOrBindingPattern(node) {
    return node.initializer !***REMOVED******REMOVED*** void 0 || isBindingPattern(node.name);
  }
  function addDefaultValueAssignmentsIfNeeded2(statements, node) {
    if (!some(node.parameters, hasDefaultValueOrBindingPattern)) {
      return false;
    }
    let added ***REMOVED*** false;
    for (const parameter of node.parameters) {
      const { name, initializer, dotDotDotToken } ***REMOVED*** parameter;
      if (dotDotDotToken) {
        continue;
      }
      if (isBindingPattern(name)) {
        added ***REMOVED*** insertDefaultValueAssignmentForBindingPattern(statements, parameter, name, initializer) || added;
      } else if (initializer) {
        insertDefaultValueAssignmentForInitializer(statements, parameter, name, initializer);
        added ***REMOVED*** true;
      }
    }
    return added;
  }
  function insertDefaultValueAssignmentForBindingPattern(statements, parameter, name, initializer) {
    if (name.elements.length > 0) {
      insertStatementAfterCustomPrologue(
        statements,
        setEmitFlags(
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList(
              flattenDestructuringBinding(
                parameter,
                visitor,
                context,
                0 /* All */,
                factory2.getGeneratedNameForNode(parameter)
              )
            )
          ),
          2097152 /* CustomPrologue */
        )
      );
      return true;
    } else if (initializer) {
      insertStatementAfterCustomPrologue(
        statements,
        setEmitFlags(
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.getGeneratedNameForNode(parameter),
              Debug.checkDefined(visitNode(initializer, visitor, isExpression))
            )
          ),
          2097152 /* CustomPrologue */
        )
      );
      return true;
    }
    return false;
  }
  function insertDefaultValueAssignmentForInitializer(statements, parameter, name, initializer) {
    initializer ***REMOVED*** Debug.checkDefined(visitNode(initializer, visitor, isExpression));
    const statement ***REMOVED*** factory2.createIfStatement(
      factory2.createTypeCheck(factory2.cloneNode(name), "undefined"),
      setEmitFlags(
        setTextRange(
          factory2.createBlock([
            factory2.createExpressionStatement(
              setEmitFlags(
                setTextRange(
                  factory2.createAssignment(
                    // TODO(rbuckton): Does this need to be parented?
                    setEmitFlags(setParent(setTextRange(factory2.cloneNode(name), name), name.parent), 96 /* NoSourceMap */),
                    setEmitFlags(initializer, 96 /* NoSourceMap */ | getEmitFlags(initializer) | 3072 /* NoComments */)
                  ),
                  parameter
                ),
                3072 /* NoComments */
              )
            )
          ]),
          parameter
        ),
        1 /* SingleLine */ | 64 /* NoTrailingSourceMap */ | 768 /* NoTokenSourceMaps */ | 3072 /* NoComments */
      )
    );
    startOnNewLine(statement);
    setTextRange(statement, parameter);
    setEmitFlags(statement, 768 /* NoTokenSourceMaps */ | 64 /* NoTrailingSourceMap */ | 2097152 /* CustomPrologue */ | 3072 /* NoComments */);
    insertStatementAfterCustomPrologue(statements, statement);
  }
  function shouldAddRestParameter(node, inConstructorWithSynthesizedSuper) {
    return !!(node && node.dotDotDotToken && !inConstructorWithSynthesizedSuper);
  }
  function addRestParameterIfNeeded(statements, node, inConstructorWithSynthesizedSuper) {
    const prologueStatements ***REMOVED*** [];
    const parameter ***REMOVED*** lastOrUndefined(node.parameters);
    if (!shouldAddRestParameter(parameter, inConstructorWithSynthesizedSuper)) {
      return false;
    }
    const declarationName ***REMOVED*** parameter.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? setParent(setTextRange(factory2.cloneNode(parameter.name), parameter.name), parameter.name.parent) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    setEmitFlags(declarationName, 96 /* NoSourceMap */);
    const expressionName ***REMOVED*** parameter.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? factory2.cloneNode(parameter.name) : declarationName;
    const restIndex ***REMOVED*** node.parameters.length - 1;
    const temp ***REMOVED*** factory2.createLoopVariable();
    prologueStatements.push(
      setEmitFlags(
        setTextRange(
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList([
              factory2.createVariableDeclaration(
                declarationName,
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                factory2.createArrayLiteralExpression([])
              )
            ])
          ),
          /*location*/
          parameter
        ),
        2097152 /* CustomPrologue */
      )
    );
    const forStatement ***REMOVED*** factory2.createForStatement(
      setTextRange(
        factory2.createVariableDeclarationList([
          factory2.createVariableDeclaration(
            temp,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            factory2.createNumericLiteral(restIndex)
          )
        ]),
        parameter
      ),
      setTextRange(
        factory2.createLessThan(
          temp,
          factory2.createPropertyAccessExpression(factory2.createIdentifier("arguments"), "length")
        ),
        parameter
      ),
      setTextRange(factory2.createPostfixIncrement(temp), parameter),
      factory2.createBlock([
        startOnNewLine(
          setTextRange(
            factory2.createExpressionStatement(
              factory2.createAssignment(
                factory2.createElementAccessExpression(
                  expressionName,
                  restIndex ***REMOVED******REMOVED******REMOVED*** 0 ? temp : factory2.createSubtract(temp, factory2.createNumericLiteral(restIndex))
                ),
                factory2.createElementAccessExpression(factory2.createIdentifier("arguments"), temp)
              )
            ),
            /*location*/
            parameter
          )
        )
      ])
    );
    setEmitFlags(forStatement, 2097152 /* CustomPrologue */);
    startOnNewLine(forStatement);
    prologueStatements.push(forStatement);
    if (parameter.name.kind !***REMOVED******REMOVED*** 80 /* Identifier */) {
      prologueStatements.push(
        setEmitFlags(
          setTextRange(
            factory2.createVariableStatement(
              /*modifiers*/
              void 0,
              factory2.createVariableDeclarationList(
                flattenDestructuringBinding(parameter, visitor, context, 0 /* All */, expressionName)
              )
            ),
            parameter
          ),
          2097152 /* CustomPrologue */
        )
      );
    }
    insertStatementsAfterCustomPrologue(statements, prologueStatements);
    return true;
  }
  function insertCaptureThisForNodeIfNeeded(statements, node) {
    if (hierarchyFacts & 131072 /* CapturedLexicalThis */ && node.kind !***REMOVED******REMOVED*** 220 /* ArrowFunction */) {
      insertCaptureThisForNode(statements, node, factory2.createThis());
      return true;
    }
    return false;
  }
  function insertCaptureThisForNode(statements, node, initializer) {
    enableSubstitutionsForCapturedThis();
    const captureThisStatement ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      factory2.createVariableDeclarationList([
        factory2.createVariableDeclaration(
          createCapturedThis(),
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          initializer
        )
      ])
    );
    setEmitFlags(captureThisStatement, 3072 /* NoComments */ | 2097152 /* CustomPrologue */);
    setSourceMapRange(captureThisStatement, node);
    insertStatementAfterCustomPrologue(statements, captureThisStatement);
  }
  function insertCaptureNewTargetIfNeeded(statements, node) {
    if (hierarchyFacts & 32768 /* NewTarget */) {
      let newTarget;
      switch (node.kind) {
        case 220 /* ArrowFunction */:
          return statements;
        case 175 /* MethodDeclaration */:
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          newTarget ***REMOVED*** factory2.createVoidZero();
          break;
        case 177 /* Constructor */:
          newTarget ***REMOVED*** factory2.createPropertyAccessExpression(
            setEmitFlags(factory2.createThis(), 8 /* NoSubstitution */),
            "constructor"
          );
          break;
        case 263 /* FunctionDeclaration */:
        case 219 /* FunctionExpression */:
          newTarget ***REMOVED*** factory2.createConditionalExpression(
            factory2.createLogicalAnd(
              setEmitFlags(factory2.createThis(), 8 /* NoSubstitution */),
              factory2.createBinaryExpression(
                setEmitFlags(factory2.createThis(), 8 /* NoSubstitution */),
                104 /* InstanceOfKeyword */,
                factory2.getLocalName(node)
              )
            ),
            /*questionToken*/
            void 0,
            factory2.createPropertyAccessExpression(
              setEmitFlags(factory2.createThis(), 8 /* NoSubstitution */),
              "constructor"
            ),
            /*colonToken*/
            void 0,
            factory2.createVoidZero()
          );
          break;
        default:
          return Debug.failBadSyntaxKind(node);
      }
      const captureNewTargetStatement ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList([
          factory2.createVariableDeclaration(
            factory2.createUniqueName("_newTarget", 16 /* Optimistic */ | 32 /* FileLevel */),
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            newTarget
          )
        ])
      );
      setEmitFlags(captureNewTargetStatement, 3072 /* NoComments */ | 2097152 /* CustomPrologue */);
      insertStatementAfterCustomPrologue(statements, captureNewTargetStatement);
    }
    return statements;
  }
  function addClassMembers(statements, node) {
    for (const member of node.members) {
      switch (member.kind) {
        case 241 /* SemicolonClassElement */:
          statements.push(transformSemicolonClassElementToStatement(member));
          break;
        case 175 /* MethodDeclaration */:
          statements.push(transformClassMethodDeclarationToStatement(getClassMemberPrefix(node, member), member, node));
          break;
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          const accessors ***REMOVED*** getAllAccessorDeclarations(node.members, member);
          if (member ***REMOVED******REMOVED******REMOVED*** accessors.firstAccessor) {
            statements.push(transformAccessorsToStatement(getClassMemberPrefix(node, member), accessors, node));
          }
          break;
        case 177 /* Constructor */:
        case 176 /* ClassStaticBlockDeclaration */:
          break;
        default:
          Debug.failBadSyntaxKind(member, currentSourceFile && currentSourceFile.fileName);
          break;
      }
    }
  }
  function transformSemicolonClassElementToStatement(member) {
    return setTextRange(factory2.createEmptyStatement(), member);
  }
  function transformClassMethodDeclarationToStatement(receiver, member, container) {
    const commentRange ***REMOVED*** getCommentRange(member);
    const sourceMapRange ***REMOVED*** getSourceMapRange(member);
    const memberFunction ***REMOVED*** transformFunctionLikeToExpression(
      member,
      /*location*/
      member,
      /*name*/
      void 0,
      container
    );
    const propertyName ***REMOVED*** visitNode(member.name, visitor, isPropertyName);
    Debug.assert(propertyName);
    let e;
    if (!isPrivateIdentifier(propertyName) && getUseDefineForClassFields(context.getCompilerOptions())) {
      const name ***REMOVED*** isComputedPropertyName(propertyName) ? propertyName.expression : isIdentifier(propertyName) ? factory2.createStringLiteral(unescapeLeadingUnderscores(propertyName.escapedText)) : propertyName;
      e ***REMOVED*** factory2.createObjectDefinePropertyCall(receiver, name, factory2.createPropertyDescriptor({ value: memberFunction, enumerable: false, writable: true, configurable: true }));
    } else {
      const memberName ***REMOVED*** createMemberAccessForPropertyName(
        factory2,
        receiver,
        propertyName,
        /*location*/
        member.name
      );
      e ***REMOVED*** factory2.createAssignment(memberName, memberFunction);
    }
    setEmitFlags(memberFunction, 3072 /* NoComments */);
    setSourceMapRange(memberFunction, sourceMapRange);
    const statement ***REMOVED*** setTextRange(
      factory2.createExpressionStatement(e),
      /*location*/
      member
    );
    setOriginalNode(statement, member);
    setCommentRange(statement, commentRange);
    setEmitFlags(statement, 96 /* NoSourceMap */);
    return statement;
  }
  function transformAccessorsToStatement(receiver, accessors, container) {
    const statement ***REMOVED*** factory2.createExpressionStatement(transformAccessorsToExpression(
      receiver,
      accessors,
      container,
      /*startsOnNewLine*/
      false
    ));
    setEmitFlags(statement, 3072 /* NoComments */);
    setSourceMapRange(statement, getSourceMapRange(accessors.firstAccessor));
    return statement;
  }
  function transformAccessorsToExpression(receiver, { firstAccessor, getAccessor, setAccessor }, container, startsOnNewLine) {
    const target ***REMOVED*** setParent(setTextRange(factory2.cloneNode(receiver), receiver), receiver.parent);
    setEmitFlags(target, 3072 /* NoComments */ | 64 /* NoTrailingSourceMap */);
    setSourceMapRange(target, firstAccessor.name);
    const visitedAccessorName ***REMOVED*** visitNode(firstAccessor.name, visitor, isPropertyName);
    Debug.assert(visitedAccessorName);
    if (isPrivateIdentifier(visitedAccessorName)) {
      return Debug.failBadSyntaxKind(visitedAccessorName, "Encountered unhandled private identifier while transforming ES2015.");
    }
    const propertyName ***REMOVED*** createExpressionForPropertyName(factory2, visitedAccessorName);
    setEmitFlags(propertyName, 3072 /* NoComments */ | 32 /* NoLeadingSourceMap */);
    setSourceMapRange(propertyName, firstAccessor.name);
    const properties ***REMOVED*** [];
    if (getAccessor) {
      const getterFunction ***REMOVED*** transformFunctionLikeToExpression(
        getAccessor,
        /*location*/
        void 0,
        /*name*/
        void 0,
        container
      );
      setSourceMapRange(getterFunction, getSourceMapRange(getAccessor));
      setEmitFlags(getterFunction, 1024 /* NoLeadingComments */);
      const getter ***REMOVED*** factory2.createPropertyAssignment("get", getterFunction);
      setCommentRange(getter, getCommentRange(getAccessor));
      properties.push(getter);
    }
    if (setAccessor) {
      const setterFunction ***REMOVED*** transformFunctionLikeToExpression(
        setAccessor,
        /*location*/
        void 0,
        /*name*/
        void 0,
        container
      );
      setSourceMapRange(setterFunction, getSourceMapRange(setAccessor));
      setEmitFlags(setterFunction, 1024 /* NoLeadingComments */);
      const setter ***REMOVED*** factory2.createPropertyAssignment("set", setterFunction);
      setCommentRange(setter, getCommentRange(setAccessor));
      properties.push(setter);
    }
    properties.push(
      factory2.createPropertyAssignment("enumerable", getAccessor || setAccessor ? factory2.createFalse() : factory2.createTrue()),
      factory2.createPropertyAssignment("configurable", factory2.createTrue())
    );
    const call ***REMOVED*** factory2.createCallExpression(
      factory2.createPropertyAccessExpression(factory2.createIdentifier("Object"), "defineProperty"),
      /*typeArguments*/
      void 0,
      [
        target,
        propertyName,
        factory2.createObjectLiteralExpression(
          properties,
          /*multiLine*/
          true
        )
      ]
    );
    if (startsOnNewLine) {
      startOnNewLine(call);
    }
    return call;
  }
  function visitArrowFunction(node) {
    if (node.transformFlags & 16384 /* ContainsLexicalThis */ && !(hierarchyFacts & 16384 /* StaticInitializer */)) {
      hierarchyFacts |***REMOVED*** 131072 /* CapturedLexicalThis */;
    }
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const ancestorFacts ***REMOVED*** enterSubtree(15232 /* ArrowFunctionExcludes */, 66 /* ArrowFunctionIncludes */);
    const func ***REMOVED*** factory2.createFunctionExpression(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      visitParameterList(node.parameters, visitor, context),
      /*type*/
      void 0,
      transformFunctionBody(node)
    );
    setTextRange(func, node);
    setOriginalNode(func, node);
    setEmitFlags(func, 16 /* CapturesThis */);
    exitSubtree(ancestorFacts, 0 /* ArrowFunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    return func;
  }
  function visitFunctionExpression(node) {
    const ancestorFacts ***REMOVED*** getEmitFlags(node) & 524288 /* AsyncFunctionBody */ ? enterSubtree(32662 /* AsyncFunctionBodyExcludes */, 69 /* AsyncFunctionBodyIncludes */) : enterSubtree(32670 /* FunctionExcludes */, 65 /* FunctionIncludes */);
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const parameters ***REMOVED*** visitParameterList(node.parameters, visitor, context);
    const body ***REMOVED*** transformFunctionBody(node);
    const name ***REMOVED*** hierarchyFacts & 32768 /* NewTarget */ ? factory2.getLocalName(node) : node.name;
    exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    return factory2.updateFunctionExpression(
      node,
      /*modifiers*/
      void 0,
      node.asteriskToken,
      name,
      /*typeParameters*/
      void 0,
      parameters,
      /*type*/
      void 0,
      body
    );
  }
  function visitFunctionDeclaration(node) {
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const ancestorFacts ***REMOVED*** enterSubtree(32670 /* FunctionExcludes */, 65 /* FunctionIncludes */);
    const parameters ***REMOVED*** visitParameterList(node.parameters, visitor, context);
    const body ***REMOVED*** transformFunctionBody(node);
    const name ***REMOVED*** hierarchyFacts & 32768 /* NewTarget */ ? factory2.getLocalName(node) : node.name;
    exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    return factory2.updateFunctionDeclaration(
      node,
      visitNodes2(node.modifiers, visitor, isModifier),
      node.asteriskToken,
      name,
      /*typeParameters*/
      void 0,
      parameters,
      /*type*/
      void 0,
      body
    );
  }
  function transformFunctionLikeToExpression(node, location, name, container) {
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const ancestorFacts ***REMOVED*** container && isClassLike(container) && !isStatic(node) ? enterSubtree(32670 /* FunctionExcludes */, 65 /* FunctionIncludes */ | 8 /* NonStaticClassElement */) : enterSubtree(32670 /* FunctionExcludes */, 65 /* FunctionIncludes */);
    const parameters ***REMOVED*** visitParameterList(node.parameters, visitor, context);
    const body ***REMOVED*** transformFunctionBody(node);
    if (hierarchyFacts & 32768 /* NewTarget */ && !name && (node.kind ***REMOVED******REMOVED******REMOVED*** 263 /* FunctionDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 219 /* FunctionExpression */)) {
      name ***REMOVED*** factory2.getGeneratedNameForNode(node);
    }
    exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    return setOriginalNode(
      setTextRange(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          node.asteriskToken,
          name,
          /*typeParameters*/
          void 0,
          parameters,
          /*type*/
          void 0,
          body
        ),
        location
      ),
      /*original*/
      node
    );
  }
  function transformFunctionBody(node) {
    let multiLine ***REMOVED*** false;
    let singleLine ***REMOVED*** false;
    let statementsLocation;
    let closeBraceLocation;
    const prologue ***REMOVED*** [];
    const statements ***REMOVED*** [];
    const body ***REMOVED*** node.body;
    let statementOffset;
    resumeLexicalEnvironment();
    if (isBlock(body)) {
      statementOffset ***REMOVED*** factory2.copyStandardPrologue(
        body.statements,
        prologue,
        0,
        /*ensureUseStrict*/
        false
      );
      statementOffset ***REMOVED*** factory2.copyCustomPrologue(body.statements, statements, statementOffset, visitor, isHoistedFunction);
      statementOffset ***REMOVED*** factory2.copyCustomPrologue(body.statements, statements, statementOffset, visitor, isHoistedVariableStatement);
    }
    multiLine ***REMOVED*** addDefaultValueAssignmentsIfNeeded2(statements, node) || multiLine;
    multiLine ***REMOVED*** addRestParameterIfNeeded(
      statements,
      node,
      /*inConstructorWithSynthesizedSuper*/
      false
    ) || multiLine;
    if (isBlock(body)) {
      statementOffset ***REMOVED*** factory2.copyCustomPrologue(body.statements, statements, statementOffset, visitor);
      statementsLocation ***REMOVED*** body.statements;
      addRange(statements, visitNodes2(body.statements, visitor, isStatement, statementOffset));
      if (!multiLine && body.multiLine) {
        multiLine ***REMOVED*** true;
      }
    } else {
      Debug.assert(node.kind ***REMOVED******REMOVED******REMOVED*** 220 /* ArrowFunction */);
      statementsLocation ***REMOVED*** moveRangeEnd(body, -1);
      const equalsGreaterThanToken ***REMOVED*** node.equalsGreaterThanToken;
      if (!nodeIsSynthesized(equalsGreaterThanToken) && !nodeIsSynthesized(body)) {
        if (rangeEndIsOnSameLineAsRangeStart(equalsGreaterThanToken, body, currentSourceFile)) {
          singleLine ***REMOVED*** true;
        } else {
          multiLine ***REMOVED*** true;
        }
      }
      const expression ***REMOVED*** visitNode(body, visitor, isExpression);
      const returnStatement ***REMOVED*** factory2.createReturnStatement(expression);
      setTextRange(returnStatement, body);
      moveSyntheticComments(returnStatement, body);
      setEmitFlags(returnStatement, 768 /* NoTokenSourceMaps */ | 64 /* NoTrailingSourceMap */ | 2048 /* NoTrailingComments */);
      statements.push(returnStatement);
      closeBraceLocation ***REMOVED*** body;
    }
    factory2.mergeLexicalEnvironment(prologue, endLexicalEnvironment());
    insertCaptureNewTargetIfNeeded(prologue, node);
    insertCaptureThisForNodeIfNeeded(prologue, node);
    if (some(prologue)) {
      multiLine ***REMOVED*** true;
    }
    statements.unshift(...prologue);
    if (isBlock(body) && arrayIsEqualTo(statements, body.statements)) {
      return body;
    }
    const block ***REMOVED*** factory2.createBlock(setTextRange(factory2.createNodeArray(statements), statementsLocation), multiLine);
    setTextRange(block, node.body);
    if (!multiLine && singleLine) {
      setEmitFlags(block, 1 /* SingleLine */);
    }
    if (closeBraceLocation) {
      setTokenSourceMapRange(block, 20 /* CloseBraceToken */, closeBraceLocation);
    }
    setOriginalNode(block, node.body);
    return block;
  }
  function visitBlock(node, isFunctionBody) {
    if (isFunctionBody) {
      return visitEachChild(node, visitor, context);
    }
    const ancestorFacts ***REMOVED*** hierarchyFacts & 256 /* IterationStatement */ ? enterSubtree(7104 /* IterationStatementBlockExcludes */, 512 /* IterationStatementBlockIncludes */) : enterSubtree(6976 /* BlockExcludes */, 128 /* BlockIncludes */);
    const updated ***REMOVED*** visitEachChild(node, visitor, context);
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function visitExpressionStatement(node) {
    return visitEachChild(node, visitorWithUnusedExpressionResult, context);
  }
  function visitParenthesizedExpression(node, expressionResultIsUnused2) {
    return visitEachChild(node, expressionResultIsUnused2 ? visitorWithUnusedExpressionResult : visitor, context);
  }
  function visitBinaryExpression(node, expressionResultIsUnused2) {
    if (isDestructuringAssignment(node)) {
      return flattenDestructuringAssignment(
        node,
        visitor,
        context,
        0 /* All */,
        !expressionResultIsUnused2
      );
    }
    if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      return factory2.updateBinaryExpression(
        node,
        Debug.checkDefined(visitNode(node.left, visitorWithUnusedExpressionResult, isExpression)),
        node.operatorToken,
        Debug.checkDefined(visitNode(node.right, expressionResultIsUnused2 ? visitorWithUnusedExpressionResult : visitor, isExpression))
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCommaListExpression(node, expressionResultIsUnused2) {
    if (expressionResultIsUnused2) {
      return visitEachChild(node, visitorWithUnusedExpressionResult, context);
    }
    let result;
    for (let i ***REMOVED*** 0; i < node.elements.length; i++) {
      const element ***REMOVED*** node.elements[i];
      const visited ***REMOVED*** visitNode(element, i < node.elements.length - 1 ? visitorWithUnusedExpressionResult : visitor, isExpression);
      if (result || visited !***REMOVED******REMOVED*** element) {
        result || (result ***REMOVED*** node.elements.slice(0, i));
        Debug.assert(visited);
        result.push(visited);
      }
    }
    const elements ***REMOVED*** result ? setTextRange(factory2.createNodeArray(result), node.elements) : node.elements;
    return factory2.updateCommaListExpression(node, elements);
  }
  function isVariableStatementOfTypeScriptClassWrapper(node) {
    return node.declarationList.declarations.length ***REMOVED******REMOVED******REMOVED*** 1 && !!node.declarationList.declarations[0].initializer && !!(getInternalEmitFlags(node.declarationList.declarations[0].initializer) & 1 /* TypeScriptClassWrapper */);
  }
  function visitVariableStatement(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(0 /* None */, hasSyntacticModifier(node, 32 /* Export */) ? 32 /* ExportedVariableStatement */ : 0 /* None */);
    let updated;
    if (convertedLoopState && (node.declarationList.flags & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 0 && !isVariableStatementOfTypeScriptClassWrapper(node)) {
      let assignments;
      for (const decl of node.declarationList.declarations) {
        hoistVariableDeclarationDeclaredInConvertedLoop(convertedLoopState, decl);
        if (decl.initializer) {
          let assignment;
          if (isBindingPattern(decl.name)) {
            assignment ***REMOVED*** flattenDestructuringAssignment(
              decl,
              visitor,
              context,
              0 /* All */
            );
          } else {
            assignment ***REMOVED*** factory2.createBinaryExpression(decl.name, 64 /* EqualsToken */, Debug.checkDefined(visitNode(decl.initializer, visitor, isExpression)));
            setTextRange(assignment, decl);
          }
          assignments ***REMOVED*** append(assignments, assignment);
        }
      }
      if (assignments) {
        updated ***REMOVED*** setTextRange(factory2.createExpressionStatement(factory2.inlineExpressions(assignments)), node);
      } else {
        updated ***REMOVED*** void 0;
      }
    } else {
      updated ***REMOVED*** visitEachChild(node, visitor, context);
    }
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function visitVariableDeclarationList(node) {
    if (node.flags & 7 /* BlockScoped */ || node.transformFlags & 524288 /* ContainsBindingPattern */) {
      if (node.flags & 7 /* BlockScoped */) {
        enableSubstitutionsForBlockScopedBindings();
      }
      const declarations ***REMOVED*** visitNodes2(
        node.declarations,
        node.flags & 1 /* Let */ ? visitVariableDeclarationInLetDeclarationList : visitVariableDeclaration,
        isVariableDeclaration
      );
      const declarationList ***REMOVED*** factory2.createVariableDeclarationList(declarations);
      setOriginalNode(declarationList, node);
      setTextRange(declarationList, node);
      setCommentRange(declarationList, node);
      if (node.transformFlags & 524288 /* ContainsBindingPattern */ && (isBindingPattern(node.declarations[0].name) || isBindingPattern(last(node.declarations).name))) {
        setSourceMapRange(declarationList, getRangeUnion(declarations));
      }
      return declarationList;
    }
    return visitEachChild(node, visitor, context);
  }
  function getRangeUnion(declarations) {
    let pos ***REMOVED*** -1, end ***REMOVED*** -1;
    for (const node of declarations) {
      pos ***REMOVED*** pos ***REMOVED******REMOVED******REMOVED*** -1 ? node.pos : node.pos ***REMOVED******REMOVED******REMOVED*** -1 ? pos : Math.min(pos, node.pos);
      end ***REMOVED*** Math.max(end, node.end);
    }
    return createRange(pos, end);
  }
  function shouldEmitExplicitInitializerForLetDeclaration(node) {
    const isCapturedInFunction ***REMOVED*** resolver.hasNodeCheckFlag(node, 16384 /* CapturedBlockScopedBinding */);
    const isDeclaredInLoop ***REMOVED*** resolver.hasNodeCheckFlag(node, 32768 /* BlockScopedBindingInLoop */);
    const emittedAsTopLevel ***REMOVED*** (hierarchyFacts & 64 /* TopLevel */) !***REMOVED******REMOVED*** 0 || isCapturedInFunction && isDeclaredInLoop && (hierarchyFacts & 512 /* IterationStatementBlock */) !***REMOVED******REMOVED*** 0;
    const emitExplicitInitializer ***REMOVED*** !emittedAsTopLevel && (hierarchyFacts & 4096 /* ForInOrForOfStatement */) ***REMOVED******REMOVED******REMOVED*** 0 && (!resolver.isDeclarationWithCollidingName(node) || isDeclaredInLoop && !isCapturedInFunction && (hierarchyFacts & (2048 /* ForStatement */ | 4096 /* ForInOrForOfStatement */)) ***REMOVED******REMOVED******REMOVED*** 0);
    return emitExplicitInitializer;
  }
  function visitVariableDeclarationInLetDeclarationList(node) {
    const name ***REMOVED*** node.name;
    if (isBindingPattern(name)) {
      return visitVariableDeclaration(node);
    }
    if (!node.initializer && shouldEmitExplicitInitializerForLetDeclaration(node)) {
      return factory2.updateVariableDeclaration(
        node,
        node.name,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        factory2.createVoidZero()
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitVariableDeclaration(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(32 /* ExportedVariableStatement */, 0 /* None */);
    let updated;
    if (isBindingPattern(node.name)) {
      updated ***REMOVED*** flattenDestructuringBinding(
        node,
        visitor,
        context,
        0 /* All */,
        /*rval*/
        void 0,
        (ancestorFacts & 32 /* ExportedVariableStatement */) !***REMOVED******REMOVED*** 0
      );
    } else {
      updated ***REMOVED*** visitEachChild(node, visitor, context);
    }
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function recordLabel(node) {
    convertedLoopState.labels.set(idText(node.label), true);
  }
  function resetLabel(node) {
    convertedLoopState.labels.set(idText(node.label), false);
  }
  function visitLabeledStatement(node) {
    if (convertedLoopState && !convertedLoopState.labels) {
      convertedLoopState.labels ***REMOVED*** /* @__PURE__ */ new Map();
    }
    const statement ***REMOVED*** unwrapInnermostStatementOfLabel(node, convertedLoopState && recordLabel);
    return isIterationStatement(
      statement,
      /*lookInLabeledStatements*/
      false
    ) ? visitIterationStatement(
      statement,
      /*outermostLabeledStatement*/
      node
    ) : factory2.restoreEnclosingLabel(visitNode(statement, visitor, isStatement, factory2.liftToBlock) ?? setTextRange(factory2.createEmptyStatement(), statement), node, convertedLoopState && resetLabel);
  }
  function visitIterationStatement(node, outermostLabeledStatement) {
    switch (node.kind) {
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
        return visitDoOrWhileStatement(node, outermostLabeledStatement);
      case 249 /* ForStatement */:
        return visitForStatement(node, outermostLabeledStatement);
      case 250 /* ForInStatement */:
        return visitForInStatement(node, outermostLabeledStatement);
      case 251 /* ForOfStatement */:
        return visitForOfStatement(node, outermostLabeledStatement);
    }
  }
  function visitIterationStatementWithFacts(excludeFacts, includeFacts, node, outermostLabeledStatement, convert) {
    const ancestorFacts ***REMOVED*** enterSubtree(excludeFacts, includeFacts);
    const updated ***REMOVED*** convertIterationStatementBodyIfNecessary(node, outermostLabeledStatement, ancestorFacts, convert);
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function visitDoOrWhileStatement(node, outermostLabeledStatement) {
    return visitIterationStatementWithFacts(
      0 /* DoOrWhileStatementExcludes */,
      1280 /* DoOrWhileStatementIncludes */,
      node,
      outermostLabeledStatement
    );
  }
  function visitForStatement(node, outermostLabeledStatement) {
    return visitIterationStatementWithFacts(
      5056 /* ForStatementExcludes */,
      3328 /* ForStatementIncludes */,
      node,
      outermostLabeledStatement
    );
  }
  function visitEachChildOfForStatement2(node) {
    return factory2.updateForStatement(
      node,
      visitNode(node.initializer, visitorWithUnusedExpressionResult, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, visitorWithUnusedExpressionResult, isExpression),
      Debug.checkDefined(visitNode(node.statement, visitor, isStatement, factory2.liftToBlock))
    );
  }
  function visitForInStatement(node, outermostLabeledStatement) {
    return visitIterationStatementWithFacts(
      3008 /* ForInOrForOfStatementExcludes */,
      5376 /* ForInOrForOfStatementIncludes */,
      node,
      outermostLabeledStatement
    );
  }
  function visitForOfStatement(node, outermostLabeledStatement) {
    return visitIterationStatementWithFacts(
      3008 /* ForInOrForOfStatementExcludes */,
      5376 /* ForInOrForOfStatementIncludes */,
      node,
      outermostLabeledStatement,
      compilerOptions.downlevelIteration ? convertForOfStatementForIterable : convertForOfStatementForArray
    );
  }
  function convertForOfStatementHead(node, boundValue, convertedLoopBodyStatements) {
    const statements ***REMOVED*** [];
    const initializer ***REMOVED*** node.initializer;
    if (isVariableDeclarationList(initializer)) {
      if (node.initializer.flags & 7 /* BlockScoped */) {
        enableSubstitutionsForBlockScopedBindings();
      }
      const firstOriginalDeclaration ***REMOVED*** firstOrUndefined(initializer.declarations);
      if (firstOriginalDeclaration && isBindingPattern(firstOriginalDeclaration.name)) {
        const declarations ***REMOVED*** flattenDestructuringBinding(
          firstOriginalDeclaration,
          visitor,
          context,
          0 /* All */,
          boundValue
        );
        const declarationList ***REMOVED*** setTextRange(factory2.createVariableDeclarationList(declarations), node.initializer);
        setOriginalNode(declarationList, node.initializer);
        setSourceMapRange(declarationList, createRange(declarations[0].pos, last(declarations).end));
        statements.push(
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            declarationList
          )
        );
      } else {
        statements.push(
          setTextRange(
            factory2.createVariableStatement(
              /*modifiers*/
              void 0,
              setOriginalNode(
                setTextRange(
                  factory2.createVariableDeclarationList([
                    factory2.createVariableDeclaration(
                      firstOriginalDeclaration ? firstOriginalDeclaration.name : factory2.createTempVariable(
                        /*recordTempVariable*/
                        void 0
                      ),
                      /*exclamationToken*/
                      void 0,
                      /*type*/
                      void 0,
                      boundValue
                    )
                  ]),
                  moveRangePos(initializer, -1)
                ),
                initializer
              )
            ),
            moveRangeEnd(initializer, -1)
          )
        );
      }
    } else {
      const assignment ***REMOVED*** factory2.createAssignment(initializer, boundValue);
      if (isDestructuringAssignment(assignment)) {
        statements.push(factory2.createExpressionStatement(visitBinaryExpression(
          assignment,
          /*expressionResultIsUnused*/
          true
        )));
      } else {
        setTextRangeEnd(assignment, initializer.end);
        statements.push(setTextRange(factory2.createExpressionStatement(Debug.checkDefined(visitNode(assignment, visitor, isExpression))), moveRangeEnd(initializer, -1)));
      }
    }
    if (convertedLoopBodyStatements) {
      return createSyntheticBlockForConvertedStatements(addRange(statements, convertedLoopBodyStatements));
    } else {
      const statement ***REMOVED*** visitNode(node.statement, visitor, isStatement, factory2.liftToBlock);
      Debug.assert(statement);
      if (isBlock(statement)) {
        return factory2.updateBlock(statement, setTextRange(factory2.createNodeArray(concatenate(statements, statement.statements)), statement.statements));
      } else {
        statements.push(statement);
        return createSyntheticBlockForConvertedStatements(statements);
      }
    }
  }
  function createSyntheticBlockForConvertedStatements(statements) {
    return setEmitFlags(
      factory2.createBlock(
        factory2.createNodeArray(statements),
        /*multiLine*/
        true
      ),
      96 /* NoSourceMap */ | 768 /* NoTokenSourceMaps */
    );
  }
  function convertForOfStatementForArray(node, outermostLabeledStatement, convertedLoopBodyStatements) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assert(expression);
    const counter ***REMOVED*** factory2.createLoopVariable();
    const rhsReference ***REMOVED*** isIdentifier(expression) ? factory2.getGeneratedNameForNode(expression) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    setEmitFlags(expression, 96 /* NoSourceMap */ | getEmitFlags(expression));
    const forStatement ***REMOVED*** setTextRange(
      factory2.createForStatement(
        /*initializer*/
        setEmitFlags(
          setTextRange(
            factory2.createVariableDeclarationList([
              setTextRange(factory2.createVariableDeclaration(
                counter,
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                factory2.createNumericLiteral(0)
              ), moveRangePos(node.expression, -1)),
              setTextRange(factory2.createVariableDeclaration(
                rhsReference,
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                expression
              ), node.expression)
            ]),
            node.expression
          ),
          4194304 /* NoHoisting */
        ),
        /*condition*/
        setTextRange(
          factory2.createLessThan(
            counter,
            factory2.createPropertyAccessExpression(rhsReference, "length")
          ),
          node.expression
        ),
        /*incrementor*/
        setTextRange(factory2.createPostfixIncrement(counter), node.expression),
        /*statement*/
        convertForOfStatementHead(
          node,
          factory2.createElementAccessExpression(rhsReference, counter),
          convertedLoopBodyStatements
        )
      ),
      /*location*/
      node
    );
    setEmitFlags(forStatement, 512 /* NoTokenTrailingSourceMaps */);
    setTextRange(forStatement, node);
    return factory2.restoreEnclosingLabel(forStatement, outermostLabeledStatement, convertedLoopState && resetLabel);
  }
  function convertForOfStatementForIterable(node, outermostLabeledStatement, convertedLoopBodyStatements, ancestorFacts) {
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assert(expression);
    const iterator ***REMOVED*** isIdentifier(expression) ? factory2.getGeneratedNameForNode(expression) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const result ***REMOVED*** isIdentifier(expression) ? factory2.getGeneratedNameForNode(iterator) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const errorRecord ***REMOVED*** factory2.createUniqueName("e");
    const catchVariable ***REMOVED*** factory2.getGeneratedNameForNode(errorRecord);
    const returnMethod ***REMOVED*** factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    const values ***REMOVED*** setTextRange(emitHelpers().createValuesHelper(expression), node.expression);
    const next ***REMOVED*** factory2.createCallExpression(
      factory2.createPropertyAccessExpression(iterator, "next"),
      /*typeArguments*/
      void 0,
      []
    );
    hoistVariableDeclaration(errorRecord);
    hoistVariableDeclaration(returnMethod);
    const initializer ***REMOVED*** ancestorFacts & 1024 /* IterationContainer */ ? factory2.inlineExpressions([factory2.createAssignment(errorRecord, factory2.createVoidZero()), values]) : values;
    const forStatement ***REMOVED*** setEmitFlags(
      setTextRange(
        factory2.createForStatement(
          /*initializer*/
          setEmitFlags(
            setTextRange(
              factory2.createVariableDeclarationList([
                setTextRange(factory2.createVariableDeclaration(
                  iterator,
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  initializer
                ), node.expression),
                factory2.createVariableDeclaration(
                  result,
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  next
                )
              ]),
              node.expression
            ),
            4194304 /* NoHoisting */
          ),
          /*condition*/
          factory2.createLogicalNot(factory2.createPropertyAccessExpression(result, "done")),
          /*incrementor*/
          factory2.createAssignment(result, next),
          /*statement*/
          convertForOfStatementHead(
            node,
            factory2.createPropertyAccessExpression(result, "value"),
            convertedLoopBodyStatements
          )
        ),
        /*location*/
        node
      ),
      512 /* NoTokenTrailingSourceMaps */
    );
    return factory2.createTryStatement(
      factory2.createBlock([
        factory2.restoreEnclosingLabel(
          forStatement,
          outermostLabeledStatement,
          convertedLoopState && resetLabel
        )
      ]),
      factory2.createCatchClause(
        factory2.createVariableDeclaration(catchVariable),
        setEmitFlags(
          factory2.createBlock([
            factory2.createExpressionStatement(
              factory2.createAssignment(
                errorRecord,
                factory2.createObjectLiteralExpression([
                  factory2.createPropertyAssignment("error", catchVariable)
                ])
              )
            )
          ]),
          1 /* SingleLine */
        )
      ),
      factory2.createBlock([
        factory2.createTryStatement(
          /*tryBlock*/
          factory2.createBlock([
            setEmitFlags(
              factory2.createIfStatement(
                factory2.createLogicalAnd(
                  factory2.createLogicalAnd(
                    result,
                    factory2.createLogicalNot(
                      factory2.createPropertyAccessExpression(result, "done")
                    )
                  ),
                  factory2.createAssignment(
                    returnMethod,
                    factory2.createPropertyAccessExpression(iterator, "return")
                  )
                ),
                factory2.createExpressionStatement(
                  factory2.createFunctionCallCall(returnMethod, iterator, [])
                )
              ),
              1 /* SingleLine */
            )
          ]),
          /*catchClause*/
          void 0,
          /*finallyBlock*/
          setEmitFlags(
            factory2.createBlock([
              setEmitFlags(
                factory2.createIfStatement(
                  errorRecord,
                  factory2.createThrowStatement(
                    factory2.createPropertyAccessExpression(errorRecord, "error")
                  )
                ),
                1 /* SingleLine */
              )
            ]),
            1 /* SingleLine */
          )
        )
      ])
    );
  }
  function visitObjectLiteralExpression(node) {
    const properties ***REMOVED*** node.properties;
    let numInitialProperties ***REMOVED*** -1, hasComputed ***REMOVED*** false;
    for (let i ***REMOVED*** 0; i < properties.length; i++) {
      const property ***REMOVED*** properties[i];
      if (property.transformFlags & 1048576 /* ContainsYield */ && hierarchyFacts & 4 /* AsyncFunctionBody */ || (hasComputed ***REMOVED*** Debug.checkDefined(property.name).kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */)) {
        numInitialProperties ***REMOVED*** i;
        break;
      }
    }
    if (numInitialProperties < 0) {
      return visitEachChild(node, visitor, context);
    }
    const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    const expressions ***REMOVED*** [];
    const assignment ***REMOVED*** factory2.createAssignment(
      temp,
      setEmitFlags(
        factory2.createObjectLiteralExpression(
          visitNodes2(properties, visitor, isObjectLiteralElementLike, 0, numInitialProperties),
          node.multiLine
        ),
        hasComputed ? 131072 /* Indented */ : 0
      )
    );
    if (node.multiLine) {
      startOnNewLine(assignment);
    }
    expressions.push(assignment);
    addObjectLiteralMembers(expressions, node, temp, numInitialProperties);
    expressions.push(node.multiLine ? startOnNewLine(setParent(setTextRange(factory2.cloneNode(temp), temp), temp.parent)) : temp);
    return factory2.inlineExpressions(expressions);
  }
  function shouldConvertPartOfIterationStatement(node) {
    return resolver.hasNodeCheckFlag(node, 8192 /* ContainsCapturedBlockScopeBinding */);
  }
  function shouldConvertInitializerOfForStatement(node) {
    return isForStatement(node) && !!node.initializer && shouldConvertPartOfIterationStatement(node.initializer);
  }
  function shouldConvertConditionOfForStatement(node) {
    return isForStatement(node) && !!node.condition && shouldConvertPartOfIterationStatement(node.condition);
  }
  function shouldConvertIncrementorOfForStatement(node) {
    return isForStatement(node) && !!node.incrementor && shouldConvertPartOfIterationStatement(node.incrementor);
  }
  function shouldConvertIterationStatement(node) {
    return shouldConvertBodyOfIterationStatement(node) || shouldConvertInitializerOfForStatement(node);
  }
  function shouldConvertBodyOfIterationStatement(node) {
    return resolver.hasNodeCheckFlag(node, 4096 /* LoopWithCapturedBlockScopedBinding */);
  }
  function hoistVariableDeclarationDeclaredInConvertedLoop(state, node) {
    if (!state.hoistedLocalVariables) {
      state.hoistedLocalVariables ***REMOVED*** [];
    }
    visit(node.name);
    function visit(node2) {
      if (node2.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
        state.hoistedLocalVariables.push(node2);
      } else {
        for (const element of node2.elements) {
          if (!isOmittedExpression(element)) {
            visit(element.name);
          }
        }
      }
    }
  }
  function convertIterationStatementBodyIfNecessary(node, outermostLabeledStatement, ancestorFacts, convert) {
    if (!shouldConvertIterationStatement(node)) {
      let saveAllowedNonLabeledJumps;
      if (convertedLoopState) {
        saveAllowedNonLabeledJumps ***REMOVED*** convertedLoopState.allowedNonLabeledJumps;
        convertedLoopState.allowedNonLabeledJumps ***REMOVED*** 2 /* Break */ | 4 /* Continue */;
      }
      const result ***REMOVED*** convert ? convert(
        node,
        outermostLabeledStatement,
        /*convertedLoopBodyStatements*/
        void 0,
        ancestorFacts
      ) : factory2.restoreEnclosingLabel(
        isForStatement(node) ? visitEachChildOfForStatement2(node) : visitEachChild(node, visitor, context),
        outermostLabeledStatement,
        convertedLoopState && resetLabel
      );
      if (convertedLoopState) {
        convertedLoopState.allowedNonLabeledJumps ***REMOVED*** saveAllowedNonLabeledJumps;
      }
      return result;
    }
    const currentState ***REMOVED*** createConvertedLoopState(node);
    const statements ***REMOVED*** [];
    const outerConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** currentState;
    const initializerFunction ***REMOVED*** shouldConvertInitializerOfForStatement(node) ? createFunctionForInitializerOfForStatement(node, currentState) : void 0;
    const bodyFunction ***REMOVED*** shouldConvertBodyOfIterationStatement(node) ? createFunctionForBodyOfIterationStatement(node, currentState, outerConvertedLoopState) : void 0;
    convertedLoopState ***REMOVED*** outerConvertedLoopState;
    if (initializerFunction) statements.push(initializerFunction.functionDeclaration);
    if (bodyFunction) statements.push(bodyFunction.functionDeclaration);
    addExtraDeclarationsForConvertedLoop(statements, currentState, outerConvertedLoopState);
    if (initializerFunction) {
      statements.push(generateCallToConvertedLoopInitializer(initializerFunction.functionName, initializerFunction.containsYield));
    }
    let loop;
    if (bodyFunction) {
      if (convert) {
        loop ***REMOVED*** convert(node, outermostLabeledStatement, bodyFunction.part, ancestorFacts);
      } else {
        const clone ***REMOVED*** convertIterationStatementCore(node, initializerFunction, factory2.createBlock(
          bodyFunction.part,
          /*multiLine*/
          true
        ));
        loop ***REMOVED*** factory2.restoreEnclosingLabel(clone, outermostLabeledStatement, convertedLoopState && resetLabel);
      }
    } else {
      const clone ***REMOVED*** convertIterationStatementCore(node, initializerFunction, Debug.checkDefined(visitNode(node.statement, visitor, isStatement, factory2.liftToBlock)));
      loop ***REMOVED*** factory2.restoreEnclosingLabel(clone, outermostLabeledStatement, convertedLoopState && resetLabel);
    }
    statements.push(loop);
    return statements;
  }
  function convertIterationStatementCore(node, initializerFunction, convertedLoopBody) {
    switch (node.kind) {
      case 249 /* ForStatement */:
        return convertForStatement(node, initializerFunction, convertedLoopBody);
      case 250 /* ForInStatement */:
        return convertForInStatement(node, convertedLoopBody);
      case 251 /* ForOfStatement */:
        return convertForOfStatement(node, convertedLoopBody);
      case 247 /* DoStatement */:
        return convertDoStatement(node, convertedLoopBody);
      case 248 /* WhileStatement */:
        return convertWhileStatement(node, convertedLoopBody);
      default:
        return Debug.failBadSyntaxKind(node, "IterationStatement expected");
    }
  }
  function convertForStatement(node, initializerFunction, convertedLoopBody) {
    const shouldConvertCondition ***REMOVED*** node.condition && shouldConvertPartOfIterationStatement(node.condition);
    const shouldConvertIncrementor ***REMOVED*** shouldConvertCondition || node.incrementor && shouldConvertPartOfIterationStatement(node.incrementor);
    return factory2.updateForStatement(
      node,
      visitNode(initializerFunction ? initializerFunction.part : node.initializer, visitorWithUnusedExpressionResult, isForInitializer),
      visitNode(shouldConvertCondition ? void 0 : node.condition, visitor, isExpression),
      visitNode(shouldConvertIncrementor ? void 0 : node.incrementor, visitorWithUnusedExpressionResult, isExpression),
      convertedLoopBody
    );
  }
  function convertForOfStatement(node, convertedLoopBody) {
    return factory2.updateForOfStatement(
      node,
      /*awaitModifier*/
      void 0,
      Debug.checkDefined(visitNode(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      convertedLoopBody
    );
  }
  function convertForInStatement(node, convertedLoopBody) {
    return factory2.updateForInStatement(
      node,
      Debug.checkDefined(visitNode(node.initializer, visitor, isForInitializer)),
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      convertedLoopBody
    );
  }
  function convertDoStatement(node, convertedLoopBody) {
    return factory2.updateDoStatement(
      node,
      convertedLoopBody,
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression))
    );
  }
  function convertWhileStatement(node, convertedLoopBody) {
    return factory2.updateWhileStatement(
      node,
      Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
      convertedLoopBody
    );
  }
  function createConvertedLoopState(node) {
    let loopInitializer;
    switch (node.kind) {
      case 249 /* ForStatement */:
      case 250 /* ForInStatement */:
      case 251 /* ForOfStatement */:
        const initializer ***REMOVED*** node.initializer;
        if (initializer && initializer.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
          loopInitializer ***REMOVED*** initializer;
        }
        break;
    }
    const loopParameters ***REMOVED*** [];
    const loopOutParameters ***REMOVED*** [];
    if (loopInitializer && getCombinedNodeFlags(loopInitializer) & 7 /* BlockScoped */) {
      const hasCapturedBindingsInForHead ***REMOVED*** shouldConvertInitializerOfForStatement(node) || shouldConvertConditionOfForStatement(node) || shouldConvertIncrementorOfForStatement(node);
      for (const decl of loopInitializer.declarations) {
        processLoopVariableDeclaration(node, decl, loopParameters, loopOutParameters, hasCapturedBindingsInForHead);
      }
    }
    const currentState ***REMOVED*** { loopParameters, loopOutParameters };
    if (convertedLoopState) {
      if (convertedLoopState.argumentsName) {
        currentState.argumentsName ***REMOVED*** convertedLoopState.argumentsName;
      }
      if (convertedLoopState.thisName) {
        currentState.thisName ***REMOVED*** convertedLoopState.thisName;
      }
      if (convertedLoopState.hoistedLocalVariables) {
        currentState.hoistedLocalVariables ***REMOVED*** convertedLoopState.hoistedLocalVariables;
      }
    }
    return currentState;
  }
  function addExtraDeclarationsForConvertedLoop(statements, state, outerState) {
    let extraVariableDeclarations;
    if (state.argumentsName) {
      if (outerState) {
        outerState.argumentsName ***REMOVED*** state.argumentsName;
      } else {
        (extraVariableDeclarations || (extraVariableDeclarations ***REMOVED*** [])).push(
          factory2.createVariableDeclaration(
            state.argumentsName,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            factory2.createIdentifier("arguments")
          )
        );
      }
    }
    if (state.thisName) {
      if (outerState) {
        outerState.thisName ***REMOVED*** state.thisName;
      } else {
        (extraVariableDeclarations || (extraVariableDeclarations ***REMOVED*** [])).push(
          factory2.createVariableDeclaration(
            state.thisName,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            factory2.createIdentifier("this")
          )
        );
      }
    }
    if (state.hoistedLocalVariables) {
      if (outerState) {
        outerState.hoistedLocalVariables ***REMOVED*** state.hoistedLocalVariables;
      } else {
        if (!extraVariableDeclarations) {
          extraVariableDeclarations ***REMOVED*** [];
        }
        for (const identifier of state.hoistedLocalVariables) {
          extraVariableDeclarations.push(factory2.createVariableDeclaration(identifier));
        }
      }
    }
    if (state.loopOutParameters.length) {
      if (!extraVariableDeclarations) {
        extraVariableDeclarations ***REMOVED*** [];
      }
      for (const outParam of state.loopOutParameters) {
        extraVariableDeclarations.push(factory2.createVariableDeclaration(outParam.outParamName));
      }
    }
    if (state.conditionVariable) {
      if (!extraVariableDeclarations) {
        extraVariableDeclarations ***REMOVED*** [];
      }
      extraVariableDeclarations.push(factory2.createVariableDeclaration(
        state.conditionVariable,
        /*exclamationToken*/
        void 0,
        /*type*/
        void 0,
        factory2.createFalse()
      ));
    }
    if (extraVariableDeclarations) {
      statements.push(factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList(extraVariableDeclarations)
      ));
    }
  }
  function createOutVariable(p) {
    return factory2.createVariableDeclaration(
      p.originalName,
      /*exclamationToken*/
      void 0,
      /*type*/
      void 0,
      p.outParamName
    );
  }
  function createFunctionForInitializerOfForStatement(node, currentState) {
    const functionName ***REMOVED*** factory2.createUniqueName("_loop_init");
    const containsYield ***REMOVED*** (node.initializer.transformFlags & 1048576 /* ContainsYield */) !***REMOVED******REMOVED*** 0;
    let emitFlags ***REMOVED*** 0 /* None */;
    if (currentState.containsLexicalThis) emitFlags |***REMOVED*** 16 /* CapturesThis */;
    if (containsYield && hierarchyFacts & 4 /* AsyncFunctionBody */) emitFlags |***REMOVED*** 524288 /* AsyncFunctionBody */;
    const statements ***REMOVED*** [];
    statements.push(factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      node.initializer
    ));
    copyOutParameters(currentState.loopOutParameters, 2 /* Initializer */, 1 /* ToOutParameter */, statements);
    const functionDeclaration ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      setEmitFlags(
        factory2.createVariableDeclarationList([
          factory2.createVariableDeclaration(
            functionName,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            setEmitFlags(
              factory2.createFunctionExpression(
                /*modifiers*/
                void 0,
                containsYield ? factory2.createToken(42 /* AsteriskToken */) : void 0,
                /*name*/
                void 0,
                /*typeParameters*/
                void 0,
                /*parameters*/
                void 0,
                /*type*/
                void 0,
                Debug.checkDefined(visitNode(
                  factory2.createBlock(
                    statements,
                    /*multiLine*/
                    true
                  ),
                  visitor,
                  isBlock
                ))
              ),
              emitFlags
            )
          )
        ]),
        4194304 /* NoHoisting */
      )
    );
    const part ***REMOVED*** factory2.createVariableDeclarationList(map(currentState.loopOutParameters, createOutVariable));
    return { functionName, containsYield, functionDeclaration, part };
  }
  function createFunctionForBodyOfIterationStatement(node, currentState, outerState) {
    const functionName ***REMOVED*** factory2.createUniqueName("_loop");
    startLexicalEnvironment();
    const statement ***REMOVED*** visitNode(node.statement, visitor, isStatement, factory2.liftToBlock);
    const lexicalEnvironment ***REMOVED*** endLexicalEnvironment();
    const statements ***REMOVED*** [];
    if (shouldConvertConditionOfForStatement(node) || shouldConvertIncrementorOfForStatement(node)) {
      currentState.conditionVariable ***REMOVED*** factory2.createUniqueName("inc");
      if (node.incrementor) {
        statements.push(factory2.createIfStatement(
          currentState.conditionVariable,
          factory2.createExpressionStatement(Debug.checkDefined(visitNode(node.incrementor, visitor, isExpression))),
          factory2.createExpressionStatement(factory2.createAssignment(currentState.conditionVariable, factory2.createTrue()))
        ));
      } else {
        statements.push(factory2.createIfStatement(
          factory2.createLogicalNot(currentState.conditionVariable),
          factory2.createExpressionStatement(factory2.createAssignment(currentState.conditionVariable, factory2.createTrue()))
        ));
      }
      if (shouldConvertConditionOfForStatement(node)) {
        statements.push(factory2.createIfStatement(
          factory2.createPrefixUnaryExpression(54 /* ExclamationToken */, Debug.checkDefined(visitNode(node.condition, visitor, isExpression))),
          Debug.checkDefined(visitNode(factory2.createBreakStatement(), visitor, isStatement))
        ));
      }
    }
    Debug.assert(statement);
    if (isBlock(statement)) {
      addRange(statements, statement.statements);
    } else {
      statements.push(statement);
    }
    copyOutParameters(currentState.loopOutParameters, 1 /* Body */, 1 /* ToOutParameter */, statements);
    insertStatementsAfterStandardPrologue(statements, lexicalEnvironment);
    const loopBody ***REMOVED*** factory2.createBlock(
      statements,
      /*multiLine*/
      true
    );
    if (isBlock(statement)) setOriginalNode(loopBody, statement);
    const containsYield ***REMOVED*** (node.statement.transformFlags & 1048576 /* ContainsYield */) !***REMOVED******REMOVED*** 0;
    let emitFlags ***REMOVED*** 1048576 /* ReuseTempVariableScope */;
    if (currentState.containsLexicalThis) emitFlags |***REMOVED*** 16 /* CapturesThis */;
    if (containsYield && (hierarchyFacts & 4 /* AsyncFunctionBody */) !***REMOVED******REMOVED*** 0) emitFlags |***REMOVED*** 524288 /* AsyncFunctionBody */;
    const functionDeclaration ***REMOVED*** factory2.createVariableStatement(
      /*modifiers*/
      void 0,
      setEmitFlags(
        factory2.createVariableDeclarationList(
          [
            factory2.createVariableDeclaration(
              functionName,
              /*exclamationToken*/
              void 0,
              /*type*/
              void 0,
              setEmitFlags(
                factory2.createFunctionExpression(
                  /*modifiers*/
                  void 0,
                  containsYield ? factory2.createToken(42 /* AsteriskToken */) : void 0,
                  /*name*/
                  void 0,
                  /*typeParameters*/
                  void 0,
                  currentState.loopParameters,
                  /*type*/
                  void 0,
                  loopBody
                ),
                emitFlags
              )
            )
          ]
        ),
        4194304 /* NoHoisting */
      )
    );
    const part ***REMOVED*** generateCallToConvertedLoop(functionName, currentState, outerState, containsYield);
    return { functionName, containsYield, functionDeclaration, part };
  }
  function copyOutParameter(outParam, copyDirection) {
    const source ***REMOVED*** copyDirection ***REMOVED******REMOVED******REMOVED*** 0 /* ToOriginal */ ? outParam.outParamName : outParam.originalName;
    const target ***REMOVED*** copyDirection ***REMOVED******REMOVED******REMOVED*** 0 /* ToOriginal */ ? outParam.originalName : outParam.outParamName;
    return factory2.createBinaryExpression(target, 64 /* EqualsToken */, source);
  }
  function copyOutParameters(outParams, partFlags, copyDirection, statements) {
    for (const outParam of outParams) {
      if (outParam.flags & partFlags) {
        statements.push(factory2.createExpressionStatement(copyOutParameter(outParam, copyDirection)));
      }
    }
  }
  function generateCallToConvertedLoopInitializer(initFunctionExpressionName, containsYield) {
    const call ***REMOVED*** factory2.createCallExpression(
      initFunctionExpressionName,
      /*typeArguments*/
      void 0,
      []
    );
    const callResult ***REMOVED*** containsYield ? factory2.createYieldExpression(
      factory2.createToken(42 /* AsteriskToken */),
      setEmitFlags(call, 8388608 /* Iterator */)
    ) : call;
    return factory2.createExpressionStatement(callResult);
  }
  function generateCallToConvertedLoop(loopFunctionExpressionName, state, outerState, containsYield) {
    const statements ***REMOVED*** [];
    const isSimpleLoop ***REMOVED*** !(state.nonLocalJumps & ~4 /* Continue */) && !state.labeledNonLocalBreaks && !state.labeledNonLocalContinues;
    const call ***REMOVED*** factory2.createCallExpression(
      loopFunctionExpressionName,
      /*typeArguments*/
      void 0,
      map(state.loopParameters, (p) ***REMOVED***> p.name)
    );
    const callResult ***REMOVED*** containsYield ? factory2.createYieldExpression(
      factory2.createToken(42 /* AsteriskToken */),
      setEmitFlags(call, 8388608 /* Iterator */)
    ) : call;
    if (isSimpleLoop) {
      statements.push(factory2.createExpressionStatement(callResult));
      copyOutParameters(state.loopOutParameters, 1 /* Body */, 0 /* ToOriginal */, statements);
    } else {
      const loopResultName ***REMOVED*** factory2.createUniqueName("state");
      const stateVariable ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList(
          [factory2.createVariableDeclaration(
            loopResultName,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            callResult
          )]
        )
      );
      statements.push(stateVariable);
      copyOutParameters(state.loopOutParameters, 1 /* Body */, 0 /* ToOriginal */, statements);
      if (state.nonLocalJumps & 8 /* Return */) {
        let returnStatement;
        if (outerState) {
          outerState.nonLocalJumps |***REMOVED*** 8 /* Return */;
          returnStatement ***REMOVED*** factory2.createReturnStatement(loopResultName);
        } else {
          returnStatement ***REMOVED*** factory2.createReturnStatement(factory2.createPropertyAccessExpression(loopResultName, "value"));
        }
        statements.push(
          factory2.createIfStatement(
            factory2.createTypeCheck(loopResultName, "object"),
            returnStatement
          )
        );
      }
      if (state.nonLocalJumps & 2 /* Break */) {
        statements.push(
          factory2.createIfStatement(
            factory2.createStrictEquality(
              loopResultName,
              factory2.createStringLiteral("break")
            ),
            factory2.createBreakStatement()
          )
        );
      }
      if (state.labeledNonLocalBreaks || state.labeledNonLocalContinues) {
        const caseClauses ***REMOVED*** [];
        processLabeledJumps(
          state.labeledNonLocalBreaks,
          /*isBreak*/
          true,
          loopResultName,
          outerState,
          caseClauses
        );
        processLabeledJumps(
          state.labeledNonLocalContinues,
          /*isBreak*/
          false,
          loopResultName,
          outerState,
          caseClauses
        );
        statements.push(
          factory2.createSwitchStatement(
            loopResultName,
            factory2.createCaseBlock(caseClauses)
          )
        );
      }
    }
    return statements;
  }
  function setLabeledJump(state, isBreak, labelText, labelMarker) {
    if (isBreak) {
      if (!state.labeledNonLocalBreaks) {
        state.labeledNonLocalBreaks ***REMOVED*** /* @__PURE__ */ new Map();
      }
      state.labeledNonLocalBreaks.set(labelText, labelMarker);
    } else {
      if (!state.labeledNonLocalContinues) {
        state.labeledNonLocalContinues ***REMOVED*** /* @__PURE__ */ new Map();
      }
      state.labeledNonLocalContinues.set(labelText, labelMarker);
    }
  }
  function processLabeledJumps(table, isBreak, loopResultName, outerLoop, caseClauses) {
    if (!table) {
      return;
    }
    table.forEach((labelMarker, labelText) ***REMOVED***> {
      const statements ***REMOVED*** [];
      if (!outerLoop || outerLoop.labels && outerLoop.labels.get(labelText)) {
        const label ***REMOVED*** factory2.createIdentifier(labelText);
        statements.push(isBreak ? factory2.createBreakStatement(label) : factory2.createContinueStatement(label));
      } else {
        setLabeledJump(outerLoop, isBreak, labelText, labelMarker);
        statements.push(factory2.createReturnStatement(loopResultName));
      }
      caseClauses.push(factory2.createCaseClause(factory2.createStringLiteral(labelMarker), statements));
    });
  }
  function processLoopVariableDeclaration(container, decl, loopParameters, loopOutParameters, hasCapturedBindingsInForHead) {
    const name ***REMOVED*** decl.name;
    if (isBindingPattern(name)) {
      for (const element of name.elements) {
        if (!isOmittedExpression(element)) {
          processLoopVariableDeclaration(container, element, loopParameters, loopOutParameters, hasCapturedBindingsInForHead);
        }
      }
    } else {
      loopParameters.push(factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        name
      ));
      const needsOutParam ***REMOVED*** resolver.hasNodeCheckFlag(decl, 65536 /* NeedsLoopOutParameter */);
      if (needsOutParam || hasCapturedBindingsInForHead) {
        const outParamName ***REMOVED*** factory2.createUniqueName("out_" + idText(name));
        let flags ***REMOVED*** 0 /* None */;
        if (needsOutParam) {
          flags |***REMOVED*** 1 /* Body */;
        }
        if (isForStatement(container)) {
          if (container.initializer && resolver.isBindingCapturedByNode(container.initializer, decl)) {
            flags |***REMOVED*** 2 /* Initializer */;
          }
          if (container.condition && resolver.isBindingCapturedByNode(container.condition, decl) || container.incrementor && resolver.isBindingCapturedByNode(container.incrementor, decl)) {
            flags |***REMOVED*** 1 /* Body */;
          }
        }
        loopOutParameters.push({ flags, originalName: name, outParamName });
      }
    }
  }
  function addObjectLiteralMembers(expressions, node, receiver, start) {
    const properties ***REMOVED*** node.properties;
    const numProperties ***REMOVED*** properties.length;
    for (let i ***REMOVED*** start; i < numProperties; i++) {
      const property ***REMOVED*** properties[i];
      switch (property.kind) {
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          const accessors ***REMOVED*** getAllAccessorDeclarations(node.properties, property);
          if (property ***REMOVED******REMOVED******REMOVED*** accessors.firstAccessor) {
            expressions.push(transformAccessorsToExpression(receiver, accessors, node, !!node.multiLine));
          }
          break;
        case 175 /* MethodDeclaration */:
          expressions.push(transformObjectLiteralMethodDeclarationToExpression(property, receiver, node, node.multiLine));
          break;
        case 304 /* PropertyAssignment */:
          expressions.push(transformPropertyAssignmentToExpression(property, receiver, node.multiLine));
          break;
        case 305 /* ShorthandPropertyAssignment */:
          expressions.push(transformShorthandPropertyAssignmentToExpression(property, receiver, node.multiLine));
          break;
        default:
          Debug.failBadSyntaxKind(node);
          break;
      }
    }
  }
  function transformPropertyAssignmentToExpression(property, receiver, startsOnNewLine) {
    const expression ***REMOVED*** factory2.createAssignment(
      createMemberAccessForPropertyName(
        factory2,
        receiver,
        Debug.checkDefined(visitNode(property.name, visitor, isPropertyName))
      ),
      Debug.checkDefined(visitNode(property.initializer, visitor, isExpression))
    );
    setTextRange(expression, property);
    if (startsOnNewLine) {
      startOnNewLine(expression);
    }
    return expression;
  }
  function transformShorthandPropertyAssignmentToExpression(property, receiver, startsOnNewLine) {
    const expression ***REMOVED*** factory2.createAssignment(
      createMemberAccessForPropertyName(
        factory2,
        receiver,
        Debug.checkDefined(visitNode(property.name, visitor, isPropertyName))
      ),
      factory2.cloneNode(property.name)
    );
    setTextRange(expression, property);
    if (startsOnNewLine) {
      startOnNewLine(expression);
    }
    return expression;
  }
  function transformObjectLiteralMethodDeclarationToExpression(method, receiver, container, startsOnNewLine) {
    const expression ***REMOVED*** factory2.createAssignment(
      createMemberAccessForPropertyName(
        factory2,
        receiver,
        Debug.checkDefined(visitNode(method.name, visitor, isPropertyName))
      ),
      transformFunctionLikeToExpression(
        method,
        /*location*/
        method,
        /*name*/
        void 0,
        container
      )
    );
    setTextRange(expression, method);
    if (startsOnNewLine) {
      startOnNewLine(expression);
    }
    return expression;
  }
  function visitCatchClause(node) {
    const ancestorFacts ***REMOVED*** enterSubtree(7104 /* BlockScopeExcludes */, 0 /* BlockScopeIncludes */);
    let updated;
    Debug.assert(!!node.variableDeclaration, "Catch clause variable should always be present when downleveling ES2015.");
    if (isBindingPattern(node.variableDeclaration.name)) {
      const temp ***REMOVED*** factory2.createTempVariable(
        /*recordTempVariable*/
        void 0
      );
      const newVariableDeclaration ***REMOVED*** factory2.createVariableDeclaration(temp);
      setTextRange(newVariableDeclaration, node.variableDeclaration);
      const vars ***REMOVED*** flattenDestructuringBinding(
        node.variableDeclaration,
        visitor,
        context,
        0 /* All */,
        temp
      );
      const list ***REMOVED*** factory2.createVariableDeclarationList(vars);
      setTextRange(list, node.variableDeclaration);
      const destructure ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        list
      );
      updated ***REMOVED*** factory2.updateCatchClause(node, newVariableDeclaration, addStatementToStartOfBlock(node.block, destructure));
    } else {
      updated ***REMOVED*** visitEachChild(node, visitor, context);
    }
    exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
    return updated;
  }
  function addStatementToStartOfBlock(block, statement) {
    const transformedStatements ***REMOVED*** visitNodes2(block.statements, visitor, isStatement);
    return factory2.updateBlock(block, [statement, ...transformedStatements]);
  }
  function visitMethodDeclaration(node) {
    Debug.assert(!isComputedPropertyName(node.name));
    const functionExpression ***REMOVED*** transformFunctionLikeToExpression(
      node,
      /*location*/
      moveRangePos(node, -1),
      /*name*/
      void 0,
      /*container*/
      void 0
    );
    setEmitFlags(functionExpression, 1024 /* NoLeadingComments */ | getEmitFlags(functionExpression));
    return setTextRange(
      factory2.createPropertyAssignment(
        node.name,
        functionExpression
      ),
      /*location*/
      node
    );
  }
  function visitAccessorDeclaration(node) {
    Debug.assert(!isComputedPropertyName(node.name));
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const ancestorFacts ***REMOVED*** enterSubtree(32670 /* FunctionExcludes */, 65 /* FunctionIncludes */);
    let updated;
    const parameters ***REMOVED*** visitParameterList(node.parameters, visitor, context);
    const body ***REMOVED*** transformFunctionBody(node);
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
      updated ***REMOVED*** factory2.updateGetAccessorDeclaration(node, node.modifiers, node.name, parameters, node.type, body);
    } else {
      updated ***REMOVED*** factory2.updateSetAccessorDeclaration(node, node.modifiers, node.name, parameters, body);
    }
    exitSubtree(ancestorFacts, 229376 /* FunctionSubtreeExcludes */, 0 /* None */);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    return updated;
  }
  function visitShorthandPropertyAssignment(node) {
    return setTextRange(
      factory2.createPropertyAssignment(
        node.name,
        visitIdentifier(factory2.cloneNode(node.name))
      ),
      /*location*/
      node
    );
  }
  function visitComputedPropertyName(node) {
    return visitEachChild(node, visitor, context);
  }
  function visitYieldExpression(node) {
    return visitEachChild(node, visitor, context);
  }
  function visitArrayLiteralExpression(node) {
    if (some(node.elements, isSpreadElement)) {
      return transformAndSpreadElements(
        node.elements,
        /*isArgumentList*/
        false,
        !!node.multiLine,
        /*hasTrailingComma*/
        !!node.elements.hasTrailingComma
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCallExpression(node) {
    if (getInternalEmitFlags(node) & 1 /* TypeScriptClassWrapper */) {
      return visitTypeScriptClassWrapper(node);
    }
    const expression ***REMOVED*** skipOuterExpressions(node.expression);
    if (expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ || isSuperProperty(expression) || some(node.arguments, isSpreadElement)) {
      return visitCallExpressionWithPotentialCapturedThisAssignment(
        node,
        /*assignToCapturedThis*/
        true
      );
    }
    return factory2.updateCallExpression(
      node,
      Debug.checkDefined(visitNode(node.expression, callExpressionVisitor, isExpression)),
      /*typeArguments*/
      void 0,
      visitNodes2(node.arguments, visitor, isExpression)
    );
  }
  function visitTypeScriptClassWrapper(node) {
    const body ***REMOVED*** cast(cast(skipOuterExpressions(node.expression), isArrowFunction).body, isBlock);
    const isVariableStatementWithInitializer ***REMOVED*** (stmt) ***REMOVED***> isVariableStatement(stmt) && !!first(stmt.declarationList.declarations).initializer;
    const savedConvertedLoopState ***REMOVED*** convertedLoopState;
    convertedLoopState ***REMOVED*** void 0;
    const bodyStatements ***REMOVED*** visitNodes2(body.statements, classWrapperStatementVisitor, isStatement);
    convertedLoopState ***REMOVED*** savedConvertedLoopState;
    const classStatements ***REMOVED*** filter(bodyStatements, isVariableStatementWithInitializer);
    const remainingStatements ***REMOVED*** filter(bodyStatements, (stmt) ***REMOVED***> !isVariableStatementWithInitializer(stmt));
    const varStatement ***REMOVED*** cast(first(classStatements), isVariableStatement);
    const variable ***REMOVED*** varStatement.declarationList.declarations[0];
    const initializer ***REMOVED*** skipOuterExpressions(variable.initializer);
    let aliasAssignment ***REMOVED*** tryCast(initializer, isAssignmentExpression);
    if (!aliasAssignment && isBinaryExpression(initializer) && initializer.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
      aliasAssignment ***REMOVED*** tryCast(initializer.left, isAssignmentExpression);
    }
    const call ***REMOVED*** cast(aliasAssignment ? skipOuterExpressions(aliasAssignment.right) : initializer, isCallExpression);
    const func ***REMOVED*** cast(skipOuterExpressions(call.expression), isFunctionExpression);
    const funcStatements ***REMOVED*** func.body.statements;
    let classBodyStart ***REMOVED*** 0;
    let classBodyEnd ***REMOVED*** -1;
    const statements ***REMOVED*** [];
    if (aliasAssignment) {
      const extendsCall ***REMOVED*** tryCast(funcStatements[classBodyStart], isExpressionStatement);
      if (extendsCall) {
        statements.push(extendsCall);
        classBodyStart++;
      }
      statements.push(funcStatements[classBodyStart]);
      classBodyStart++;
      statements.push(
        factory2.createExpressionStatement(
          factory2.createAssignment(
            aliasAssignment.left,
            cast(variable.name, isIdentifier)
          )
        )
      );
    }
    while (!isReturnStatement(elementAt(funcStatements, classBodyEnd))) {
      classBodyEnd--;
    }
    addRange(statements, funcStatements, classBodyStart, classBodyEnd);
    if (classBodyEnd < -1) {
      addRange(statements, funcStatements, classBodyEnd + 1);
    }
    const returnStatement ***REMOVED*** tryCast(elementAt(funcStatements, classBodyEnd), isReturnStatement);
    for (const statement of remainingStatements) {
      if (isReturnStatement(statement) && (returnStatement ***REMOVED******REMOVED*** null ? void 0 : returnStatement.expression) && !isIdentifier(returnStatement.expression)) {
        statements.push(returnStatement);
      } else {
        statements.push(statement);
      }
    }
    addRange(
      statements,
      classStatements,
      /*start*/
      1
    );
    return factory2.restoreOuterExpressions(
      node.expression,
      factory2.restoreOuterExpressions(
        variable.initializer,
        factory2.restoreOuterExpressions(
          aliasAssignment && aliasAssignment.right,
          factory2.updateCallExpression(
            call,
            factory2.restoreOuterExpressions(
              call.expression,
              factory2.updateFunctionExpression(
                func,
                /*modifiers*/
                void 0,
                /*asteriskToken*/
                void 0,
                /*name*/
                void 0,
                /*typeParameters*/
                void 0,
                func.parameters,
                /*type*/
                void 0,
                factory2.updateBlock(
                  func.body,
                  statements
                )
              )
            ),
            /*typeArguments*/
            void 0,
            call.arguments
          )
        )
      )
    );
  }
  function visitCallExpressionWithPotentialCapturedThisAssignment(node, assignToCapturedThis) {
    if (node.transformFlags & 32768 /* ContainsRestOrSpread */ || node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ || isSuperProperty(skipOuterExpressions(node.expression))) {
      const { target, thisArg } ***REMOVED*** factory2.createCallBinding(node.expression, hoistVariableDeclaration);
      if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
        setEmitFlags(thisArg, 8 /* NoSubstitution */);
      }
      let resultingCall;
      if (node.transformFlags & 32768 /* ContainsRestOrSpread */) {
        resultingCall ***REMOVED*** factory2.createFunctionApplyCall(
          Debug.checkDefined(visitNode(target, callExpressionVisitor, isExpression)),
          node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ ? thisArg : Debug.checkDefined(visitNode(thisArg, visitor, isExpression)),
          transformAndSpreadElements(
            node.arguments,
            /*isArgumentList*/
            true,
            /*multiLine*/
            false,
            /*hasTrailingComma*/
            false
          )
        );
      } else {
        resultingCall ***REMOVED*** setTextRange(
          factory2.createFunctionCallCall(
            Debug.checkDefined(visitNode(target, callExpressionVisitor, isExpression)),
            node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */ ? thisArg : Debug.checkDefined(visitNode(thisArg, visitor, isExpression)),
            visitNodes2(node.arguments, visitor, isExpression)
          ),
          node
        );
      }
      if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 108 /* SuperKeyword */) {
        const initializer ***REMOVED*** factory2.createLogicalOr(
          resultingCall,
          createActualThis()
        );
        resultingCall ***REMOVED*** assignToCapturedThis ? factory2.createAssignment(createCapturedThis(), initializer) : initializer;
      }
      return setOriginalNode(resultingCall, node);
    }
    if (isSuperCall(node)) {
      hierarchyFacts |***REMOVED*** 131072 /* CapturedLexicalThis */;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitNewExpression(node) {
    if (some(node.arguments, isSpreadElement)) {
      const { target, thisArg } ***REMOVED*** factory2.createCallBinding(factory2.createPropertyAccessExpression(node.expression, "bind"), hoistVariableDeclaration);
      return factory2.createNewExpression(
        factory2.createFunctionApplyCall(
          Debug.checkDefined(visitNode(target, visitor, isExpression)),
          thisArg,
          transformAndSpreadElements(
            factory2.createNodeArray([factory2.createVoidZero(), ...node.arguments]),
            /*isArgumentList*/
            true,
            /*multiLine*/
            false,
            /*hasTrailingComma*/
            false
          )
        ),
        /*typeArguments*/
        void 0,
        []
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function transformAndSpreadElements(elements, isArgumentList, multiLine, hasTrailingComma) {
    const numElements ***REMOVED*** elements.length;
    const segments ***REMOVED*** flatten(
      // As we visit each element, we return one of two functions to use as the "key":
      // - `visitSpanOfSpreads` for one or more contiguous `...` spread expressions, i.e. `...a, ...b` in `[1, 2, ...a, ...b]`
      // - `visitSpanOfNonSpreads` for one or more contiguous non-spread elements, i.e. `1, 2`, in `[1, 2, ...a, ...b]`
      spanMap(elements, partitionSpread, (partition, visitPartition, _start, end) ***REMOVED***> visitPartition(partition, multiLine, hasTrailingComma && end ***REMOVED******REMOVED******REMOVED*** numElements))
    );
    if (segments.length ***REMOVED******REMOVED******REMOVED*** 1) {
      const firstSegment ***REMOVED*** segments[0];
      if (isArgumentList && !compilerOptions.downlevelIteration || isPackedArrayLiteral(firstSegment.expression) || isCallToHelper(firstSegment.expression, "___spreadArray")) {
        return firstSegment.expression;
      }
    }
    const helpers ***REMOVED*** emitHelpers();
    const startsWithSpread ***REMOVED*** segments[0].kind !***REMOVED******REMOVED*** 0 /* None */;
    let expression ***REMOVED*** startsWithSpread ? factory2.createArrayLiteralExpression() : segments[0].expression;
    for (let i ***REMOVED*** startsWithSpread ? 0 : 1; i < segments.length; i++) {
      const segment ***REMOVED*** segments[i];
      expression ***REMOVED*** helpers.createSpreadArrayHelper(
        expression,
        segment.expression,
        segment.kind ***REMOVED******REMOVED******REMOVED*** 1 /* UnpackedSpread */ && !isArgumentList
      );
    }
    return expression;
  }
  function partitionSpread(node) {
    return isSpreadElement(node) ? visitSpanOfSpreads : visitSpanOfNonSpreads;
  }
  function visitSpanOfSpreads(chunk) {
    return map(chunk, visitExpressionOfSpread);
  }
  function visitExpressionOfSpread(node) {
    Debug.assertNode(node, isSpreadElement);
    let expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    Debug.assert(expression);
    const isCallToReadHelper ***REMOVED*** isCallToHelper(expression, "___read");
    let kind ***REMOVED*** isCallToReadHelper || isPackedArrayLiteral(expression) ? 2 /* PackedSpread */ : 1 /* UnpackedSpread */;
    if (compilerOptions.downlevelIteration && kind ***REMOVED******REMOVED******REMOVED*** 1 /* UnpackedSpread */ && !isArrayLiteralExpression(expression) && !isCallToReadHelper) {
      expression ***REMOVED*** emitHelpers().createReadHelper(
        expression,
        /*count*/
        void 0
      );
      kind ***REMOVED*** 2 /* PackedSpread */;
    }
    return createSpreadSegment(kind, expression);
  }
  function visitSpanOfNonSpreads(chunk, multiLine, hasTrailingComma) {
    const expression ***REMOVED*** factory2.createArrayLiteralExpression(
      visitNodes2(factory2.createNodeArray(chunk, hasTrailingComma), visitor, isExpression),
      multiLine
    );
    return createSpreadSegment(0 /* None */, expression);
  }
  function visitSpreadElement(node) {
    return visitNode(node.expression, visitor, isExpression);
  }
  function visitTemplateLiteral(node) {
    return setTextRange(factory2.createStringLiteral(node.text), node);
  }
  function visitStringLiteral(node) {
    if (node.hasExtendedUnicodeEscape) {
      return setTextRange(factory2.createStringLiteral(node.text), node);
    }
    return node;
  }
  function visitNumericLiteral(node) {
    if (node.numericLiteralFlags & 384 /* BinaryOrOctalSpecifier */) {
      return setTextRange(factory2.createNumericLiteral(node.text), node);
    }
    return node;
  }
  function visitTaggedTemplateExpression(node) {
    return processTaggedTemplateExpression(
      context,
      node,
      visitor,
      currentSourceFile,
      recordTaggedTemplateString,
      1 /* All */
    );
  }
  function visitTemplateExpression(node) {
    let expression ***REMOVED*** factory2.createStringLiteral(node.head.text);
    for (const span of node.templateSpans) {
      const args ***REMOVED*** [Debug.checkDefined(visitNode(span.expression, visitor, isExpression))];
      if (span.literal.text.length > 0) {
        args.push(factory2.createStringLiteral(span.literal.text));
      }
      expression ***REMOVED*** factory2.createCallExpression(
        factory2.createPropertyAccessExpression(expression, "concat"),
        /*typeArguments*/
        void 0,
        args
      );
    }
    return setTextRange(expression, node);
  }
  function createSyntheticSuper() {
    return factory2.createUniqueName("_super", 16 /* Optimistic */ | 32 /* FileLevel */);
  }
  function visitSuperKeyword(node, isExpressionOfCall) {
    const expression ***REMOVED*** hierarchyFacts & 8 /* NonStaticClassElement */ && !isExpressionOfCall ? factory2.createPropertyAccessExpression(setOriginalNode(createSyntheticSuper(), node), "prototype") : createSyntheticSuper();
    setOriginalNode(expression, node);
    setCommentRange(expression, node);
    setSourceMapRange(expression, node);
    return expression;
  }
  function visitMetaProperty(node) {
    if (node.keywordToken ***REMOVED******REMOVED******REMOVED*** 105 /* NewKeyword */ && node.name.escapedText ***REMOVED******REMOVED******REMOVED*** "target") {
      hierarchyFacts |***REMOVED*** 32768 /* NewTarget */;
      return factory2.createUniqueName("_newTarget", 16 /* Optimistic */ | 32 /* FileLevel */);
    }
    return node;
  }
  function onEmitNode(hint, node, emitCallback) {
    if (enabledSubstitutions & 1 /* CapturedThis */ && isFunctionLike(node)) {
      const ancestorFacts ***REMOVED*** enterSubtree(
        32670 /* FunctionExcludes */,
        getEmitFlags(node) & 16 /* CapturesThis */ ? 65 /* FunctionIncludes */ | 16 /* CapturesThis */ : 65 /* FunctionIncludes */
      );
      previousOnEmitNode(hint, node, emitCallback);
      exitSubtree(ancestorFacts, 0 /* None */, 0 /* None */);
      return;
    }
    previousOnEmitNode(hint, node, emitCallback);
  }
  function enableSubstitutionsForBlockScopedBindings() {
    if ((enabledSubstitutions & 2 /* BlockScopedBindings */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 2 /* BlockScopedBindings */;
      context.enableSubstitution(80 /* Identifier */);
    }
  }
  function enableSubstitutionsForCapturedThis() {
    if ((enabledSubstitutions & 1 /* CapturedThis */) ***REMOVED******REMOVED******REMOVED*** 0) {
      enabledSubstitutions |***REMOVED*** 1 /* CapturedThis */;
      context.enableSubstitution(110 /* ThisKeyword */);
      context.enableEmitNotification(177 /* Constructor */);
      context.enableEmitNotification(175 /* MethodDeclaration */);
      context.enableEmitNotification(178 /* GetAccessor */);
      context.enableEmitNotification(179 /* SetAccessor */);
      context.enableEmitNotification(220 /* ArrowFunction */);
      context.enableEmitNotification(219 /* FunctionExpression */);
      context.enableEmitNotification(263 /* FunctionDeclaration */);
    }
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    }
    if (isIdentifier(node)) {
      return substituteIdentifier(node);
    }
    return node;
  }
  function substituteIdentifier(node) {
    if (enabledSubstitutions & 2 /* BlockScopedBindings */ && !isInternalName(node)) {
      const original ***REMOVED*** getParseTreeNode(node, isIdentifier);
      if (original && isNameOfDeclarationWithCollidingName(original)) {
        return setTextRange(factory2.getGeneratedNameForNode(original), node);
      }
    }
    return node;
  }
  function isNameOfDeclarationWithCollidingName(node) {
    switch (node.parent.kind) {
      case 209 /* BindingElement */:
      case 264 /* ClassDeclaration */:
      case 267 /* EnumDeclaration */:
      case 261 /* VariableDeclaration */:
        return node.parent.name ***REMOVED******REMOVED******REMOVED*** node && resolver.isDeclarationWithCollidingName(node.parent);
    }
    return false;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
      case 110 /* ThisKeyword */:
        return substituteThisKeyword(node);
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    if (enabledSubstitutions & 2 /* BlockScopedBindings */ && !isInternalName(node)) {
      const declaration ***REMOVED*** resolver.getReferencedDeclarationWithCollidingName(node);
      if (declaration && !(isClassLike(declaration) && isPartOfClassBody(declaration, node))) {
        return setTextRange(factory2.getGeneratedNameForNode(getNameOfDeclaration(declaration)), node);
      }
    }
    return node;
  }
  function isPartOfClassBody(declaration, node) {
    let currentNode ***REMOVED*** getParseTreeNode(node);
    if (!currentNode || currentNode ***REMOVED******REMOVED******REMOVED*** declaration || currentNode.end <***REMOVED*** declaration.pos || currentNode.pos >***REMOVED*** declaration.end) {
      return false;
    }
    const blockScope ***REMOVED*** getEnclosingBlockScopeContainer(declaration);
    while (currentNode) {
      if (currentNode ***REMOVED******REMOVED******REMOVED*** blockScope || currentNode ***REMOVED******REMOVED******REMOVED*** declaration) {
        return false;
      }
      if (isClassElement(currentNode) && currentNode.parent ***REMOVED******REMOVED******REMOVED*** declaration) {
        return true;
      }
      currentNode ***REMOVED*** currentNode.parent;
    }
    return false;
  }
  function substituteThisKeyword(node) {
    if (enabledSubstitutions & 1 /* CapturedThis */ && hierarchyFacts & 16 /* CapturesThis */) {
      return setTextRange(createCapturedThis(), node);
    }
    return node;
  }
  function getClassMemberPrefix(node, member) {
    return isStatic(member) ? factory2.getInternalName(node) : factory2.createPropertyAccessExpression(factory2.getInternalName(node), "prototype");
  }
  function hasSynthesizedDefaultSuperCall(constructor, hasExtendsClause) {
    if (!constructor || !hasExtendsClause) {
      return false;
    }
    if (some(constructor.parameters)) {
      return false;
    }
    const statement ***REMOVED*** firstOrUndefined(constructor.body.statements);
    if (!statement || !nodeIsSynthesized(statement) || statement.kind !***REMOVED******REMOVED*** 245 /* ExpressionStatement */) {
      return false;
    }
    const statementExpression ***REMOVED*** statement.expression;
    if (!nodeIsSynthesized(statementExpression) || statementExpression.kind !***REMOVED******REMOVED*** 214 /* CallExpression */) {
      return false;
    }
    const callTarget ***REMOVED*** statementExpression.expression;
    if (!nodeIsSynthesized(callTarget) || callTarget.kind !***REMOVED******REMOVED*** 108 /* SuperKeyword */) {
      return false;
    }
    const callArgument ***REMOVED*** singleOrUndefined(statementExpression.arguments);
    if (!callArgument || !nodeIsSynthesized(callArgument) || callArgument.kind !***REMOVED******REMOVED*** 231 /* SpreadElement */) {
      return false;
    }
    const expression ***REMOVED*** callArgument.expression;
    return isIdentifier(expression) && expression.escapedText ***REMOVED******REMOVED******REMOVED*** "arguments";
  }
}

// src/compiler/transformers/generators.ts
function getInstructionName(instruction) {
  switch (instruction) {
    case 2 /* Return */:
      return "return";
    case 3 /* Break */:
      return "break";
    case 4 /* Yield */:
      return "yield";
    case 5 /* YieldStar */:
      return "yield*";
    case 7 /* Endfinally */:
      return "endfinally";
    default:
      return void 0;
  }
}
function transformGenerators(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    hoistFunctionDeclaration,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const resolver ***REMOVED*** context.getEmitResolver();
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  let renamedCatchVariables;
  let renamedCatchVariableDeclarations;
  let inGeneratorFunctionBody;
  let inStatementContainingYield;
  let blocks;
  let blockOffsets;
  let blockActions;
  let blockStack;
  let labelOffsets;
  let labelExpressions;
  let nextLabelId ***REMOVED*** 1;
  let operations;
  let operationArguments;
  let operationLocations;
  let state;
  let blockIndex ***REMOVED*** 0;
  let labelNumber ***REMOVED*** 0;
  let labelNumbers;
  let lastOperationWasAbrupt;
  let lastOperationWasCompletion;
  let clauses;
  let statements;
  let exceptionBlockStack;
  let currentExceptionBlock;
  let withBlockStack;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile || (node.transformFlags & 2048 /* ContainsGenerator */) ***REMOVED******REMOVED******REMOVED*** 0) {
      return node;
    }
    const visited ***REMOVED*** visitEachChild(node, visitor, context);
    addEmitHelpers(visited, context.readEmitHelpers());
    return visited;
  }
  function visitor(node) {
    const transformFlags ***REMOVED*** node.transformFlags;
    if (inStatementContainingYield) {
      return visitJavaScriptInStatementContainingYield(node);
    } else if (inGeneratorFunctionBody) {
      return visitJavaScriptInGeneratorFunctionBody(node);
    } else if (isFunctionLikeDeclaration(node) && node.asteriskToken) {
      return visitGenerator(node);
    } else if (transformFlags & 2048 /* ContainsGenerator */) {
      return visitEachChild(node, visitor, context);
    } else {
      return node;
    }
  }
  function visitJavaScriptInStatementContainingYield(node) {
    switch (node.kind) {
      case 247 /* DoStatement */:
        return visitDoStatement(node);
      case 248 /* WhileStatement */:
        return visitWhileStatement(node);
      case 256 /* SwitchStatement */:
        return visitSwitchStatement(node);
      case 257 /* LabeledStatement */:
        return visitLabeledStatement(node);
      default:
        return visitJavaScriptInGeneratorFunctionBody(node);
    }
  }
  function visitJavaScriptInGeneratorFunctionBody(node) {
    switch (node.kind) {
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 219 /* FunctionExpression */:
        return visitFunctionExpression(node);
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return visitAccessorDeclaration(node);
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 249 /* ForStatement */:
        return visitForStatement(node);
      case 250 /* ForInStatement */:
        return visitForInStatement(node);
      case 253 /* BreakStatement */:
        return visitBreakStatement(node);
      case 252 /* ContinueStatement */:
        return visitContinueStatement(node);
      case 254 /* ReturnStatement */:
        return visitReturnStatement(node);
      default:
        if (node.transformFlags & 1048576 /* ContainsYield */) {
          return visitJavaScriptContainingYield(node);
        } else if (node.transformFlags & (2048 /* ContainsGenerator */ | 4194304 /* ContainsHoistedDeclarationOrCompletion */)) {
          return visitEachChild(node, visitor, context);
        } else {
          return node;
        }
    }
  }
  function visitJavaScriptContainingYield(node) {
    switch (node.kind) {
      case 227 /* BinaryExpression */:
        return visitBinaryExpression(node);
      case 357 /* CommaListExpression */:
        return visitCommaListExpression(node);
      case 228 /* ConditionalExpression */:
        return visitConditionalExpression(node);
      case 230 /* YieldExpression */:
        return visitYieldExpression(node);
      case 210 /* ArrayLiteralExpression */:
        return visitArrayLiteralExpression(node);
      case 211 /* ObjectLiteralExpression */:
        return visitObjectLiteralExpression(node);
      case 213 /* ElementAccessExpression */:
        return visitElementAccessExpression(node);
      case 214 /* CallExpression */:
        return visitCallExpression(node);
      case 215 /* NewExpression */:
        return visitNewExpression(node);
      default:
        return visitEachChild(node, visitor, context);
    }
  }
  function visitGenerator(node) {
    switch (node.kind) {
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 219 /* FunctionExpression */:
        return visitFunctionExpression(node);
      default:
        return Debug.failBadSyntaxKind(node);
    }
  }
  function visitFunctionDeclaration(node) {
    if (node.asteriskToken) {
      node ***REMOVED*** setOriginalNode(
        setTextRange(
          factory2.createFunctionDeclaration(
            node.modifiers,
            /*asteriskToken*/
            void 0,
            node.name,
            /*typeParameters*/
            void 0,
            visitParameterList(node.parameters, visitor, context),
            /*type*/
            void 0,
            transformGeneratorFunctionBody(node.body)
          ),
          /*location*/
          node
        ),
        node
      );
    } else {
      const savedInGeneratorFunctionBody ***REMOVED*** inGeneratorFunctionBody;
      const savedInStatementContainingYield ***REMOVED*** inStatementContainingYield;
      inGeneratorFunctionBody ***REMOVED*** false;
      inStatementContainingYield ***REMOVED*** false;
      node ***REMOVED*** visitEachChild(node, visitor, context);
      inGeneratorFunctionBody ***REMOVED*** savedInGeneratorFunctionBody;
      inStatementContainingYield ***REMOVED*** savedInStatementContainingYield;
    }
    if (inGeneratorFunctionBody) {
      hoistFunctionDeclaration(node);
      return void 0;
    } else {
      return node;
    }
  }
  function visitFunctionExpression(node) {
    if (node.asteriskToken) {
      node ***REMOVED*** setOriginalNode(
        setTextRange(
          factory2.createFunctionExpression(
            /*modifiers*/
            void 0,
            /*asteriskToken*/
            void 0,
            node.name,
            /*typeParameters*/
            void 0,
            visitParameterList(node.parameters, visitor, context),
            /*type*/
            void 0,
            transformGeneratorFunctionBody(node.body)
          ),
          /*location*/
          node
        ),
        node
      );
    } else {
      const savedInGeneratorFunctionBody ***REMOVED*** inGeneratorFunctionBody;
      const savedInStatementContainingYield ***REMOVED*** inStatementContainingYield;
      inGeneratorFunctionBody ***REMOVED*** false;
      inStatementContainingYield ***REMOVED*** false;
      node ***REMOVED*** visitEachChild(node, visitor, context);
      inGeneratorFunctionBody ***REMOVED*** savedInGeneratorFunctionBody;
      inStatementContainingYield ***REMOVED*** savedInStatementContainingYield;
    }
    return node;
  }
  function visitAccessorDeclaration(node) {
    const savedInGeneratorFunctionBody ***REMOVED*** inGeneratorFunctionBody;
    const savedInStatementContainingYield ***REMOVED*** inStatementContainingYield;
    inGeneratorFunctionBody ***REMOVED*** false;
    inStatementContainingYield ***REMOVED*** false;
    node ***REMOVED*** visitEachChild(node, visitor, context);
    inGeneratorFunctionBody ***REMOVED*** savedInGeneratorFunctionBody;
    inStatementContainingYield ***REMOVED*** savedInStatementContainingYield;
    return node;
  }
  function transformGeneratorFunctionBody(body) {
    const statements2 ***REMOVED*** [];
    const savedInGeneratorFunctionBody ***REMOVED*** inGeneratorFunctionBody;
    const savedInStatementContainingYield ***REMOVED*** inStatementContainingYield;
    const savedBlocks ***REMOVED*** blocks;
    const savedBlockOffsets ***REMOVED*** blockOffsets;
    const savedBlockActions ***REMOVED*** blockActions;
    const savedBlockStack ***REMOVED*** blockStack;
    const savedLabelOffsets ***REMOVED*** labelOffsets;
    const savedLabelExpressions ***REMOVED*** labelExpressions;
    const savedNextLabelId ***REMOVED*** nextLabelId;
    const savedOperations ***REMOVED*** operations;
    const savedOperationArguments ***REMOVED*** operationArguments;
    const savedOperationLocations ***REMOVED*** operationLocations;
    const savedState ***REMOVED*** state;
    inGeneratorFunctionBody ***REMOVED*** true;
    inStatementContainingYield ***REMOVED*** false;
    blocks ***REMOVED*** void 0;
    blockOffsets ***REMOVED*** void 0;
    blockActions ***REMOVED*** void 0;
    blockStack ***REMOVED*** void 0;
    labelOffsets ***REMOVED*** void 0;
    labelExpressions ***REMOVED*** void 0;
    nextLabelId ***REMOVED*** 1;
    operations ***REMOVED*** void 0;
    operationArguments ***REMOVED*** void 0;
    operationLocations ***REMOVED*** void 0;
    state ***REMOVED*** factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    resumeLexicalEnvironment();
    const statementOffset ***REMOVED*** factory2.copyPrologue(
      body.statements,
      statements2,
      /*ensureUseStrict*/
      false,
      visitor
    );
    transformAndEmitStatements(body.statements, statementOffset);
    const buildResult ***REMOVED*** build2();
    insertStatementsAfterStandardPrologue(statements2, endLexicalEnvironment());
    statements2.push(factory2.createReturnStatement(buildResult));
    inGeneratorFunctionBody ***REMOVED*** savedInGeneratorFunctionBody;
    inStatementContainingYield ***REMOVED*** savedInStatementContainingYield;
    blocks ***REMOVED*** savedBlocks;
    blockOffsets ***REMOVED*** savedBlockOffsets;
    blockActions ***REMOVED*** savedBlockActions;
    blockStack ***REMOVED*** savedBlockStack;
    labelOffsets ***REMOVED*** savedLabelOffsets;
    labelExpressions ***REMOVED*** savedLabelExpressions;
    nextLabelId ***REMOVED*** savedNextLabelId;
    operations ***REMOVED*** savedOperations;
    operationArguments ***REMOVED*** savedOperationArguments;
    operationLocations ***REMOVED*** savedOperationLocations;
    state ***REMOVED*** savedState;
    return setTextRange(factory2.createBlock(statements2, body.multiLine), body);
  }
  function visitVariableStatement(node) {
    if (node.transformFlags & 1048576 /* ContainsYield */) {
      transformAndEmitVariableDeclarationList(node.declarationList);
      return void 0;
    } else {
      if (getEmitFlags(node) & 2097152 /* CustomPrologue */) {
        return node;
      }
      for (const variable of node.declarationList.declarations) {
        hoistVariableDeclaration(variable.name);
      }
      const variables ***REMOVED*** getInitializedVariables(node.declarationList);
      if (variables.length ***REMOVED******REMOVED******REMOVED*** 0) {
        return void 0;
      }
      return setSourceMapRange(
        factory2.createExpressionStatement(
          factory2.inlineExpressions(
            map(variables, transformInitializedVariable)
          )
        ),
        node
      );
    }
  }
  function visitBinaryExpression(node) {
    const assoc ***REMOVED*** getExpressionAssociativity(node);
    switch (assoc) {
      case 0 /* Left */:
        return visitLeftAssociativeBinaryExpression(node);
      case 1 /* Right */:
        return visitRightAssociativeBinaryExpression(node);
      default:
        return Debug.assertNever(assoc);
    }
  }
  function visitRightAssociativeBinaryExpression(node) {
    const { left, right } ***REMOVED*** node;
    if (containsYield(right)) {
      let target;
      switch (left.kind) {
        case 212 /* PropertyAccessExpression */:
          target ***REMOVED*** factory2.updatePropertyAccessExpression(
            left,
            cacheExpression(Debug.checkDefined(visitNode(left.expression, visitor, isLeftHandSideExpression))),
            left.name
          );
          break;
        case 213 /* ElementAccessExpression */:
          target ***REMOVED*** factory2.updateElementAccessExpression(left, cacheExpression(Debug.checkDefined(visitNode(left.expression, visitor, isLeftHandSideExpression))), cacheExpression(Debug.checkDefined(visitNode(left.argumentExpression, visitor, isExpression))));
          break;
        default:
          target ***REMOVED*** Debug.checkDefined(visitNode(left, visitor, isExpression));
          break;
      }
      const operator ***REMOVED*** node.operatorToken.kind;
      if (isCompoundAssignment(operator)) {
        return setTextRange(
          factory2.createAssignment(
            target,
            setTextRange(
              factory2.createBinaryExpression(
                cacheExpression(target),
                getNonAssignmentOperatorForCompoundAssignment(operator),
                Debug.checkDefined(visitNode(right, visitor, isExpression))
              ),
              node
            )
          ),
          node
        );
      } else {
        return factory2.updateBinaryExpression(node, target, node.operatorToken, Debug.checkDefined(visitNode(right, visitor, isExpression)));
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function visitLeftAssociativeBinaryExpression(node) {
    if (containsYield(node.right)) {
      if (isLogicalOperator(node.operatorToken.kind)) {
        return visitLogicalBinaryExpression(node);
      } else if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
        return visitCommaExpression(node);
      }
      return factory2.updateBinaryExpression(node, cacheExpression(Debug.checkDefined(visitNode(node.left, visitor, isExpression))), node.operatorToken, Debug.checkDefined(visitNode(node.right, visitor, isExpression)));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCommaExpression(node) {
    let pendingExpressions ***REMOVED*** [];
    visit(node.left);
    visit(node.right);
    return factory2.inlineExpressions(pendingExpressions);
    function visit(node2) {
      if (isBinaryExpression(node2) && node2.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
        visit(node2.left);
        visit(node2.right);
      } else {
        if (containsYield(node2) && pendingExpressions.length > 0) {
          emitWorker(1 /* Statement */, [factory2.createExpressionStatement(factory2.inlineExpressions(pendingExpressions))]);
          pendingExpressions ***REMOVED*** [];
        }
        pendingExpressions.push(Debug.checkDefined(visitNode(node2, visitor, isExpression)));
      }
    }
  }
  function visitCommaListExpression(node) {
    let pendingExpressions ***REMOVED*** [];
    for (const elem of node.elements) {
      if (isBinaryExpression(elem) && elem.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 28 /* CommaToken */) {
        pendingExpressions.push(visitCommaExpression(elem));
      } else {
        if (containsYield(elem) && pendingExpressions.length > 0) {
          emitWorker(1 /* Statement */, [factory2.createExpressionStatement(factory2.inlineExpressions(pendingExpressions))]);
          pendingExpressions ***REMOVED*** [];
        }
        pendingExpressions.push(Debug.checkDefined(visitNode(elem, visitor, isExpression)));
      }
    }
    return factory2.inlineExpressions(pendingExpressions);
  }
  function visitLogicalBinaryExpression(node) {
    const resultLabel ***REMOVED*** defineLabel();
    const resultLocal ***REMOVED*** declareLocal();
    emitAssignment(
      resultLocal,
      Debug.checkDefined(visitNode(node.left, visitor, isExpression)),
      /*location*/
      node.left
    );
    if (node.operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 56 /* AmpersandAmpersandToken */) {
      emitBreakWhenFalse(
        resultLabel,
        resultLocal,
        /*location*/
        node.left
      );
    } else {
      emitBreakWhenTrue(
        resultLabel,
        resultLocal,
        /*location*/
        node.left
      );
    }
    emitAssignment(
      resultLocal,
      Debug.checkDefined(visitNode(node.right, visitor, isExpression)),
      /*location*/
      node.right
    );
    markLabel(resultLabel);
    return resultLocal;
  }
  function visitConditionalExpression(node) {
    if (containsYield(node.whenTrue) || containsYield(node.whenFalse)) {
      const whenFalseLabel ***REMOVED*** defineLabel();
      const resultLabel ***REMOVED*** defineLabel();
      const resultLocal ***REMOVED*** declareLocal();
      emitBreakWhenFalse(
        whenFalseLabel,
        Debug.checkDefined(visitNode(node.condition, visitor, isExpression)),
        /*location*/
        node.condition
      );
      emitAssignment(
        resultLocal,
        Debug.checkDefined(visitNode(node.whenTrue, visitor, isExpression)),
        /*location*/
        node.whenTrue
      );
      emitBreak(resultLabel);
      markLabel(whenFalseLabel);
      emitAssignment(
        resultLocal,
        Debug.checkDefined(visitNode(node.whenFalse, visitor, isExpression)),
        /*location*/
        node.whenFalse
      );
      markLabel(resultLabel);
      return resultLocal;
    }
    return visitEachChild(node, visitor, context);
  }
  function visitYieldExpression(node) {
    const resumeLabel ***REMOVED*** defineLabel();
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    if (node.asteriskToken) {
      const iterator ***REMOVED*** (getEmitFlags(node.expression) & 8388608 /* Iterator */) ***REMOVED******REMOVED******REMOVED*** 0 ? setTextRange(emitHelpers().createValuesHelper(expression), node) : expression;
      emitYieldStar(
        iterator,
        /*location*/
        node
      );
    } else {
      emitYield(
        expression,
        /*location*/
        node
      );
    }
    markLabel(resumeLabel);
    return createGeneratorResume(
      /*location*/
      node
    );
  }
  function visitArrayLiteralExpression(node) {
    return visitElements(
      node.elements,
      /*leadingElement*/
      void 0,
      /*location*/
      void 0,
      node.multiLine
    );
  }
  function visitElements(elements, leadingElement, location, multiLine) {
    const numInitialElements ***REMOVED*** countInitialNodesWithoutYield(elements);
    let temp;
    if (numInitialElements > 0) {
      temp ***REMOVED*** declareLocal();
      const initialElements ***REMOVED*** visitNodes2(elements, visitor, isExpression, 0, numInitialElements);
      emitAssignment(
        temp,
        factory2.createArrayLiteralExpression(
          leadingElement ? [leadingElement, ...initialElements] : initialElements
        )
      );
      leadingElement ***REMOVED*** void 0;
    }
    const expressions ***REMOVED*** reduceLeft(elements, reduceElement, [], numInitialElements);
    return temp ? factory2.createArrayConcatCall(temp, [factory2.createArrayLiteralExpression(expressions, multiLine)]) : setTextRange(
      factory2.createArrayLiteralExpression(leadingElement ? [leadingElement, ...expressions] : expressions, multiLine),
      location
    );
    function reduceElement(expressions2, element) {
      if (containsYield(element) && expressions2.length > 0) {
        const hasAssignedTemp ***REMOVED*** temp !***REMOVED******REMOVED*** void 0;
        if (!temp) {
          temp ***REMOVED*** declareLocal();
        }
        emitAssignment(
          temp,
          hasAssignedTemp ? factory2.createArrayConcatCall(
            temp,
            [factory2.createArrayLiteralExpression(expressions2, multiLine)]
          ) : factory2.createArrayLiteralExpression(
            leadingElement ? [leadingElement, ...expressions2] : expressions2,
            multiLine
          )
        );
        leadingElement ***REMOVED*** void 0;
        expressions2 ***REMOVED*** [];
      }
      expressions2.push(Debug.checkDefined(visitNode(element, visitor, isExpression)));
      return expressions2;
    }
  }
  function visitObjectLiteralExpression(node) {
    const properties ***REMOVED*** node.properties;
    const multiLine ***REMOVED*** node.multiLine;
    const numInitialProperties ***REMOVED*** countInitialNodesWithoutYield(properties);
    const temp ***REMOVED*** declareLocal();
    emitAssignment(
      temp,
      factory2.createObjectLiteralExpression(
        visitNodes2(properties, visitor, isObjectLiteralElementLike, 0, numInitialProperties),
        multiLine
      )
    );
    const expressions ***REMOVED*** reduceLeft(properties, reduceProperty, [], numInitialProperties);
    expressions.push(multiLine ? startOnNewLine(setParent(setTextRange(factory2.cloneNode(temp), temp), temp.parent)) : temp);
    return factory2.inlineExpressions(expressions);
    function reduceProperty(expressions2, property) {
      if (containsYield(property) && expressions2.length > 0) {
        emitStatement(factory2.createExpressionStatement(factory2.inlineExpressions(expressions2)));
        expressions2 ***REMOVED*** [];
      }
      const expression ***REMOVED*** createExpressionForObjectLiteralElementLike(factory2, node, property, temp);
      const visited ***REMOVED*** visitNode(expression, visitor, isExpression);
      if (visited) {
        if (multiLine) {
          startOnNewLine(visited);
        }
        expressions2.push(visited);
      }
      return expressions2;
    }
  }
  function visitElementAccessExpression(node) {
    if (containsYield(node.argumentExpression)) {
      return factory2.updateElementAccessExpression(node, cacheExpression(Debug.checkDefined(visitNode(node.expression, visitor, isLeftHandSideExpression))), Debug.checkDefined(visitNode(node.argumentExpression, visitor, isExpression)));
    }
    return visitEachChild(node, visitor, context);
  }
  function visitCallExpression(node) {
    if (!isImportCall(node) && forEach(node.arguments, containsYield)) {
      const { target, thisArg } ***REMOVED*** factory2.createCallBinding(
        node.expression,
        hoistVariableDeclaration,
        languageVersion,
        /*cacheIdentifiers*/
        true
      );
      return setOriginalNode(
        setTextRange(
          factory2.createFunctionApplyCall(
            cacheExpression(Debug.checkDefined(visitNode(target, visitor, isLeftHandSideExpression))),
            thisArg,
            visitElements(node.arguments)
          ),
          node
        ),
        node
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function visitNewExpression(node) {
    if (forEach(node.arguments, containsYield)) {
      const { target, thisArg } ***REMOVED*** factory2.createCallBinding(factory2.createPropertyAccessExpression(node.expression, "bind"), hoistVariableDeclaration);
      return setOriginalNode(
        setTextRange(
          factory2.createNewExpression(
            factory2.createFunctionApplyCall(
              cacheExpression(Debug.checkDefined(visitNode(target, visitor, isExpression))),
              thisArg,
              visitElements(
                node.arguments,
                /*leadingElement*/
                factory2.createVoidZero()
              )
            ),
            /*typeArguments*/
            void 0,
            []
          ),
          node
        ),
        node
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function transformAndEmitStatements(statements2, start ***REMOVED*** 0) {
    const numStatements ***REMOVED*** statements2.length;
    for (let i ***REMOVED*** start; i < numStatements; i++) {
      transformAndEmitStatement(statements2[i]);
    }
  }
  function transformAndEmitEmbeddedStatement(node) {
    if (isBlock(node)) {
      transformAndEmitStatements(node.statements);
    } else {
      transformAndEmitStatement(node);
    }
  }
  function transformAndEmitStatement(node) {
    const savedInStatementContainingYield ***REMOVED*** inStatementContainingYield;
    if (!inStatementContainingYield) {
      inStatementContainingYield ***REMOVED*** containsYield(node);
    }
    transformAndEmitStatementWorker(node);
    inStatementContainingYield ***REMOVED*** savedInStatementContainingYield;
  }
  function transformAndEmitStatementWorker(node) {
    switch (node.kind) {
      case 242 /* Block */:
        return transformAndEmitBlock(node);
      case 245 /* ExpressionStatement */:
        return transformAndEmitExpressionStatement(node);
      case 246 /* IfStatement */:
        return transformAndEmitIfStatement(node);
      case 247 /* DoStatement */:
        return transformAndEmitDoStatement(node);
      case 248 /* WhileStatement */:
        return transformAndEmitWhileStatement(node);
      case 249 /* ForStatement */:
        return transformAndEmitForStatement(node);
      case 250 /* ForInStatement */:
        return transformAndEmitForInStatement(node);
      case 252 /* ContinueStatement */:
        return transformAndEmitContinueStatement(node);
      case 253 /* BreakStatement */:
        return transformAndEmitBreakStatement(node);
      case 254 /* ReturnStatement */:
        return transformAndEmitReturnStatement(node);
      case 255 /* WithStatement */:
        return transformAndEmitWithStatement(node);
      case 256 /* SwitchStatement */:
        return transformAndEmitSwitchStatement(node);
      case 257 /* LabeledStatement */:
        return transformAndEmitLabeledStatement(node);
      case 258 /* ThrowStatement */:
        return transformAndEmitThrowStatement(node);
      case 259 /* TryStatement */:
        return transformAndEmitTryStatement(node);
      default:
        return emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function transformAndEmitBlock(node) {
    if (containsYield(node)) {
      transformAndEmitStatements(node.statements);
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function transformAndEmitExpressionStatement(node) {
    emitStatement(visitNode(node, visitor, isStatement));
  }
  function transformAndEmitVariableDeclarationList(node) {
    for (const variable of node.declarations) {
      const name ***REMOVED*** factory2.cloneNode(variable.name);
      setCommentRange(name, variable.name);
      hoistVariableDeclaration(name);
    }
    const variables ***REMOVED*** getInitializedVariables(node);
    const numVariables ***REMOVED*** variables.length;
    let variablesWritten ***REMOVED*** 0;
    let pendingExpressions ***REMOVED*** [];
    while (variablesWritten < numVariables) {
      for (let i ***REMOVED*** variablesWritten; i < numVariables; i++) {
        const variable ***REMOVED*** variables[i];
        if (containsYield(variable.initializer) && pendingExpressions.length > 0) {
          break;
        }
        pendingExpressions.push(transformInitializedVariable(variable));
      }
      if (pendingExpressions.length) {
        emitStatement(factory2.createExpressionStatement(factory2.inlineExpressions(pendingExpressions)));
        variablesWritten +***REMOVED*** pendingExpressions.length;
        pendingExpressions ***REMOVED*** [];
      }
    }
    return void 0;
  }
  function transformInitializedVariable(node) {
    return setSourceMapRange(
      factory2.createAssignment(
        setSourceMapRange(factory2.cloneNode(node.name), node.name),
        Debug.checkDefined(visitNode(node.initializer, visitor, isExpression))
      ),
      node
    );
  }
  function transformAndEmitIfStatement(node) {
    if (containsYield(node)) {
      if (containsYield(node.thenStatement) || containsYield(node.elseStatement)) {
        const endLabel ***REMOVED*** defineLabel();
        const elseLabel ***REMOVED*** node.elseStatement ? defineLabel() : void 0;
        emitBreakWhenFalse(
          node.elseStatement ? elseLabel : endLabel,
          Debug.checkDefined(visitNode(node.expression, visitor, isExpression)),
          /*location*/
          node.expression
        );
        transformAndEmitEmbeddedStatement(node.thenStatement);
        if (node.elseStatement) {
          emitBreak(endLabel);
          markLabel(elseLabel);
          transformAndEmitEmbeddedStatement(node.elseStatement);
        }
        markLabel(endLabel);
      } else {
        emitStatement(visitNode(node, visitor, isStatement));
      }
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function transformAndEmitDoStatement(node) {
    if (containsYield(node)) {
      const conditionLabel ***REMOVED*** defineLabel();
      const loopLabel ***REMOVED*** defineLabel();
      beginLoopBlock(
        /*continueLabel*/
        conditionLabel
      );
      markLabel(loopLabel);
      transformAndEmitEmbeddedStatement(node.statement);
      markLabel(conditionLabel);
      emitBreakWhenTrue(loopLabel, Debug.checkDefined(visitNode(node.expression, visitor, isExpression)));
      endLoopBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitDoStatement(node) {
    if (inStatementContainingYield) {
      beginScriptLoopBlock();
      node ***REMOVED*** visitEachChild(node, visitor, context);
      endLoopBlock();
      return node;
    } else {
      return visitEachChild(node, visitor, context);
    }
  }
  function transformAndEmitWhileStatement(node) {
    if (containsYield(node)) {
      const loopLabel ***REMOVED*** defineLabel();
      const endLabel ***REMOVED*** beginLoopBlock(loopLabel);
      markLabel(loopLabel);
      emitBreakWhenFalse(endLabel, Debug.checkDefined(visitNode(node.expression, visitor, isExpression)));
      transformAndEmitEmbeddedStatement(node.statement);
      emitBreak(loopLabel);
      endLoopBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitWhileStatement(node) {
    if (inStatementContainingYield) {
      beginScriptLoopBlock();
      node ***REMOVED*** visitEachChild(node, visitor, context);
      endLoopBlock();
      return node;
    } else {
      return visitEachChild(node, visitor, context);
    }
  }
  function transformAndEmitForStatement(node) {
    if (containsYield(node)) {
      const conditionLabel ***REMOVED*** defineLabel();
      const incrementLabel ***REMOVED*** defineLabel();
      const endLabel ***REMOVED*** beginLoopBlock(incrementLabel);
      if (node.initializer) {
        const initializer ***REMOVED*** node.initializer;
        if (isVariableDeclarationList(initializer)) {
          transformAndEmitVariableDeclarationList(initializer);
        } else {
          emitStatement(
            setTextRange(
              factory2.createExpressionStatement(
                Debug.checkDefined(visitNode(initializer, visitor, isExpression))
              ),
              initializer
            )
          );
        }
      }
      markLabel(conditionLabel);
      if (node.condition) {
        emitBreakWhenFalse(endLabel, Debug.checkDefined(visitNode(node.condition, visitor, isExpression)));
      }
      transformAndEmitEmbeddedStatement(node.statement);
      markLabel(incrementLabel);
      if (node.incrementor) {
        emitStatement(
          setTextRange(
            factory2.createExpressionStatement(
              Debug.checkDefined(visitNode(node.incrementor, visitor, isExpression))
            ),
            node.incrementor
          )
        );
      }
      emitBreak(conditionLabel);
      endLoopBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitForStatement(node) {
    if (inStatementContainingYield) {
      beginScriptLoopBlock();
    }
    const initializer ***REMOVED*** node.initializer;
    if (initializer && isVariableDeclarationList(initializer)) {
      for (const variable of initializer.declarations) {
        hoistVariableDeclaration(variable.name);
      }
      const variables ***REMOVED*** getInitializedVariables(initializer);
      node ***REMOVED*** factory2.updateForStatement(
        node,
        variables.length > 0 ? factory2.inlineExpressions(map(variables, transformInitializedVariable)) : void 0,
        visitNode(node.condition, visitor, isExpression),
        visitNode(node.incrementor, visitor, isExpression),
        visitIterationBody(node.statement, visitor, context)
      );
    } else {
      node ***REMOVED*** visitEachChild(node, visitor, context);
    }
    if (inStatementContainingYield) {
      endLoopBlock();
    }
    return node;
  }
  function transformAndEmitForInStatement(node) {
    if (containsYield(node)) {
      const obj ***REMOVED*** declareLocal();
      const keysArray ***REMOVED*** declareLocal();
      const key ***REMOVED*** declareLocal();
      const keysIndex ***REMOVED*** factory2.createLoopVariable();
      const initializer ***REMOVED*** node.initializer;
      hoistVariableDeclaration(keysIndex);
      emitAssignment(obj, Debug.checkDefined(visitNode(node.expression, visitor, isExpression)));
      emitAssignment(keysArray, factory2.createArrayLiteralExpression());
      emitStatement(
        factory2.createForInStatement(
          key,
          obj,
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              factory2.createPropertyAccessExpression(keysArray, "push"),
              /*typeArguments*/
              void 0,
              [key]
            )
          )
        )
      );
      emitAssignment(keysIndex, factory2.createNumericLiteral(0));
      const conditionLabel ***REMOVED*** defineLabel();
      const incrementLabel ***REMOVED*** defineLabel();
      const endLoopLabel ***REMOVED*** beginLoopBlock(incrementLabel);
      markLabel(conditionLabel);
      emitBreakWhenFalse(endLoopLabel, factory2.createLessThan(keysIndex, factory2.createPropertyAccessExpression(keysArray, "length")));
      emitAssignment(key, factory2.createElementAccessExpression(keysArray, keysIndex));
      emitBreakWhenFalse(incrementLabel, factory2.createBinaryExpression(key, 103 /* InKeyword */, obj));
      let variable;
      if (isVariableDeclarationList(initializer)) {
        for (const variable2 of initializer.declarations) {
          hoistVariableDeclaration(variable2.name);
        }
        variable ***REMOVED*** factory2.cloneNode(initializer.declarations[0].name);
      } else {
        variable ***REMOVED*** Debug.checkDefined(visitNode(initializer, visitor, isExpression));
        Debug.assert(isLeftHandSideExpression(variable));
      }
      emitAssignment(variable, key);
      transformAndEmitEmbeddedStatement(node.statement);
      markLabel(incrementLabel);
      emitStatement(factory2.createExpressionStatement(factory2.createPostfixIncrement(keysIndex)));
      emitBreak(conditionLabel);
      endLoopBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitForInStatement(node) {
    if (inStatementContainingYield) {
      beginScriptLoopBlock();
    }
    const initializer ***REMOVED*** node.initializer;
    if (isVariableDeclarationList(initializer)) {
      for (const variable of initializer.declarations) {
        hoistVariableDeclaration(variable.name);
      }
      node ***REMOVED*** factory2.updateForInStatement(node, initializer.declarations[0].name, Debug.checkDefined(visitNode(node.expression, visitor, isExpression)), Debug.checkDefined(visitNode(node.statement, visitor, isStatement, factory2.liftToBlock)));
    } else {
      node ***REMOVED*** visitEachChild(node, visitor, context);
    }
    if (inStatementContainingYield) {
      endLoopBlock();
    }
    return node;
  }
  function transformAndEmitContinueStatement(node) {
    const label ***REMOVED*** findContinueTarget(node.label ? idText(node.label) : void 0);
    if (label > 0) {
      emitBreak(
        label,
        /*location*/
        node
      );
    } else {
      emitStatement(node);
    }
  }
  function visitContinueStatement(node) {
    if (inStatementContainingYield) {
      const label ***REMOVED*** findContinueTarget(node.label && idText(node.label));
      if (label > 0) {
        return createInlineBreak(
          label,
          /*location*/
          node
        );
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function transformAndEmitBreakStatement(node) {
    const label ***REMOVED*** findBreakTarget(node.label ? idText(node.label) : void 0);
    if (label > 0) {
      emitBreak(
        label,
        /*location*/
        node
      );
    } else {
      emitStatement(node);
    }
  }
  function visitBreakStatement(node) {
    if (inStatementContainingYield) {
      const label ***REMOVED*** findBreakTarget(node.label && idText(node.label));
      if (label > 0) {
        return createInlineBreak(
          label,
          /*location*/
          node
        );
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function transformAndEmitReturnStatement(node) {
    emitReturn(
      visitNode(node.expression, visitor, isExpression),
      /*location*/
      node
    );
  }
  function visitReturnStatement(node) {
    return createInlineReturn(
      visitNode(node.expression, visitor, isExpression),
      /*location*/
      node
    );
  }
  function transformAndEmitWithStatement(node) {
    if (containsYield(node)) {
      beginWithBlock(cacheExpression(Debug.checkDefined(visitNode(node.expression, visitor, isExpression))));
      transformAndEmitEmbeddedStatement(node.statement);
      endWithBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function transformAndEmitSwitchStatement(node) {
    if (containsYield(node.caseBlock)) {
      const caseBlock ***REMOVED*** node.caseBlock;
      const numClauses ***REMOVED*** caseBlock.clauses.length;
      const endLabel ***REMOVED*** beginSwitchBlock();
      const expression ***REMOVED*** cacheExpression(Debug.checkDefined(visitNode(node.expression, visitor, isExpression)));
      const clauseLabels ***REMOVED*** [];
      let defaultClauseIndex ***REMOVED*** -1;
      for (let i ***REMOVED*** 0; i < numClauses; i++) {
        const clause ***REMOVED*** caseBlock.clauses[i];
        clauseLabels.push(defineLabel());
        if (clause.kind ***REMOVED******REMOVED******REMOVED*** 298 /* DefaultClause */ && defaultClauseIndex ***REMOVED******REMOVED******REMOVED*** -1) {
          defaultClauseIndex ***REMOVED*** i;
        }
      }
      let clausesWritten ***REMOVED*** 0;
      let pendingClauses ***REMOVED*** [];
      while (clausesWritten < numClauses) {
        let defaultClausesSkipped ***REMOVED*** 0;
        for (let i ***REMOVED*** clausesWritten; i < numClauses; i++) {
          const clause ***REMOVED*** caseBlock.clauses[i];
          if (clause.kind ***REMOVED******REMOVED******REMOVED*** 297 /* CaseClause */) {
            if (containsYield(clause.expression) && pendingClauses.length > 0) {
              break;
            }
            pendingClauses.push(
              factory2.createCaseClause(
                Debug.checkDefined(visitNode(clause.expression, visitor, isExpression)),
                [
                  createInlineBreak(
                    clauseLabels[i],
                    /*location*/
                    clause.expression
                  )
                ]
              )
            );
          } else {
            defaultClausesSkipped++;
          }
        }
        if (pendingClauses.length) {
          emitStatement(factory2.createSwitchStatement(expression, factory2.createCaseBlock(pendingClauses)));
          clausesWritten +***REMOVED*** pendingClauses.length;
          pendingClauses ***REMOVED*** [];
        }
        if (defaultClausesSkipped > 0) {
          clausesWritten +***REMOVED*** defaultClausesSkipped;
          defaultClausesSkipped ***REMOVED*** 0;
        }
      }
      if (defaultClauseIndex >***REMOVED*** 0) {
        emitBreak(clauseLabels[defaultClauseIndex]);
      } else {
        emitBreak(endLabel);
      }
      for (let i ***REMOVED*** 0; i < numClauses; i++) {
        markLabel(clauseLabels[i]);
        transformAndEmitStatements(caseBlock.clauses[i].statements);
      }
      endSwitchBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitSwitchStatement(node) {
    if (inStatementContainingYield) {
      beginScriptSwitchBlock();
    }
    node ***REMOVED*** visitEachChild(node, visitor, context);
    if (inStatementContainingYield) {
      endSwitchBlock();
    }
    return node;
  }
  function transformAndEmitLabeledStatement(node) {
    if (containsYield(node)) {
      beginLabeledBlock(idText(node.label));
      transformAndEmitEmbeddedStatement(node.statement);
      endLabeledBlock();
    } else {
      emitStatement(visitNode(node, visitor, isStatement));
    }
  }
  function visitLabeledStatement(node) {
    if (inStatementContainingYield) {
      beginScriptLabeledBlock(idText(node.label));
    }
    node ***REMOVED*** visitEachChild(node, visitor, context);
    if (inStatementContainingYield) {
      endLabeledBlock();
    }
    return node;
  }
  function transformAndEmitThrowStatement(node) {
    emitThrow(
      Debug.checkDefined(visitNode(node.expression ?? factory2.createVoidZero(), visitor, isExpression)),
      /*location*/
      node
    );
  }
  function transformAndEmitTryStatement(node) {
    if (containsYield(node)) {
      beginExceptionBlock();
      transformAndEmitEmbeddedStatement(node.tryBlock);
      if (node.catchClause) {
        beginCatchBlock(node.catchClause.variableDeclaration);
        transformAndEmitEmbeddedStatement(node.catchClause.block);
      }
      if (node.finallyBlock) {
        beginFinallyBlock();
        transformAndEmitEmbeddedStatement(node.finallyBlock);
      }
      endExceptionBlock();
    } else {
      emitStatement(visitEachChild(node, visitor, context));
    }
  }
  function containsYield(node) {
    return !!node && (node.transformFlags & 1048576 /* ContainsYield */) !***REMOVED******REMOVED*** 0;
  }
  function countInitialNodesWithoutYield(nodes) {
    const numNodes ***REMOVED*** nodes.length;
    for (let i ***REMOVED*** 0; i < numNodes; i++) {
      if (containsYield(nodes[i])) {
        return i;
      }
    }
    return -1;
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    }
    return node;
  }
  function substituteExpression(node) {
    if (isIdentifier(node)) {
      return substituteExpressionIdentifier(node);
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    if (!isGeneratedIdentifier(node) && renamedCatchVariables && renamedCatchVariables.has(idText(node))) {
      const original ***REMOVED*** getOriginalNode(node);
      if (isIdentifier(original) && original.parent) {
        const declaration ***REMOVED*** resolver.getReferencedValueDeclaration(original);
        if (declaration) {
          const name ***REMOVED*** renamedCatchVariableDeclarations[getOriginalNodeId(declaration)];
          if (name) {
            const clone ***REMOVED*** setParent(setTextRange(factory2.cloneNode(name), name), name.parent);
            setSourceMapRange(clone, node);
            setCommentRange(clone, node);
            return clone;
          }
        }
      }
    }
    return node;
  }
  function cacheExpression(node) {
    if (isGeneratedIdentifier(node) || getEmitFlags(node) & 8192 /* HelperName */) {
      return node;
    }
    const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
    emitAssignment(
      temp,
      node,
      /*location*/
      node
    );
    return temp;
  }
  function declareLocal(name) {
    const temp ***REMOVED*** name ? factory2.createUniqueName(name) : factory2.createTempVariable(
      /*recordTempVariable*/
      void 0
    );
    hoistVariableDeclaration(temp);
    return temp;
  }
  function defineLabel() {
    if (!labelOffsets) {
      labelOffsets ***REMOVED*** [];
    }
    const label ***REMOVED*** nextLabelId;
    nextLabelId++;
    labelOffsets[label] ***REMOVED*** -1;
    return label;
  }
  function markLabel(label) {
    Debug.assert(labelOffsets !***REMOVED******REMOVED*** void 0, "No labels were defined.");
    labelOffsets[label] ***REMOVED*** operations ? operations.length : 0;
  }
  function beginBlock(block) {
    if (!blocks) {
      blocks ***REMOVED*** [];
      blockActions ***REMOVED*** [];
      blockOffsets ***REMOVED*** [];
      blockStack ***REMOVED*** [];
    }
    const index ***REMOVED*** blockActions.length;
    blockActions[index] ***REMOVED*** 0 /* Open */;
    blockOffsets[index] ***REMOVED*** operations ? operations.length : 0;
    blocks[index] ***REMOVED*** block;
    blockStack.push(block);
    return index;
  }
  function endBlock() {
    const block ***REMOVED*** peekBlock();
    if (block ***REMOVED******REMOVED******REMOVED*** void 0) return Debug.fail("beginBlock was never called.");
    const index ***REMOVED*** blockActions.length;
    blockActions[index] ***REMOVED*** 1 /* Close */;
    blockOffsets[index] ***REMOVED*** operations ? operations.length : 0;
    blocks[index] ***REMOVED*** block;
    blockStack.pop();
    return block;
  }
  function peekBlock() {
    return lastOrUndefined(blockStack);
  }
  function peekBlockKind() {
    const block ***REMOVED*** peekBlock();
    return block && block.kind;
  }
  function beginWithBlock(expression) {
    const startLabel ***REMOVED*** defineLabel();
    const endLabel ***REMOVED*** defineLabel();
    markLabel(startLabel);
    beginBlock({
      kind: 1 /* With */,
      expression,
      startLabel,
      endLabel
    });
  }
  function endWithBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 1 /* With */);
    const block ***REMOVED*** endBlock();
    markLabel(block.endLabel);
  }
  function beginExceptionBlock() {
    const startLabel ***REMOVED*** defineLabel();
    const endLabel ***REMOVED*** defineLabel();
    markLabel(startLabel);
    beginBlock({
      kind: 0 /* Exception */,
      state: 0 /* Try */,
      startLabel,
      endLabel
    });
    emitNop();
    return endLabel;
  }
  function beginCatchBlock(variable) {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 0 /* Exception */);
    let name;
    if (isGeneratedIdentifier(variable.name)) {
      name ***REMOVED*** variable.name;
      hoistVariableDeclaration(variable.name);
    } else {
      const text ***REMOVED*** idText(variable.name);
      name ***REMOVED*** declareLocal(text);
      if (!renamedCatchVariables) {
        renamedCatchVariables ***REMOVED*** /* @__PURE__ */ new Map();
        renamedCatchVariableDeclarations ***REMOVED*** [];
        context.enableSubstitution(80 /* Identifier */);
      }
      renamedCatchVariables.set(text, true);
      renamedCatchVariableDeclarations[getOriginalNodeId(variable)] ***REMOVED*** name;
    }
    const exception ***REMOVED*** peekBlock();
    Debug.assert(exception.state < 1 /* Catch */);
    const endLabel ***REMOVED*** exception.endLabel;
    emitBreak(endLabel);
    const catchLabel ***REMOVED*** defineLabel();
    markLabel(catchLabel);
    exception.state ***REMOVED*** 1 /* Catch */;
    exception.catchVariable ***REMOVED*** name;
    exception.catchLabel ***REMOVED*** catchLabel;
    emitAssignment(name, factory2.createCallExpression(
      factory2.createPropertyAccessExpression(state, "sent"),
      /*typeArguments*/
      void 0,
      []
    ));
    emitNop();
  }
  function beginFinallyBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 0 /* Exception */);
    const exception ***REMOVED*** peekBlock();
    Debug.assert(exception.state < 2 /* Finally */);
    const endLabel ***REMOVED*** exception.endLabel;
    emitBreak(endLabel);
    const finallyLabel ***REMOVED*** defineLabel();
    markLabel(finallyLabel);
    exception.state ***REMOVED*** 2 /* Finally */;
    exception.finallyLabel ***REMOVED*** finallyLabel;
  }
  function endExceptionBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 0 /* Exception */);
    const exception ***REMOVED*** endBlock();
    const state2 ***REMOVED*** exception.state;
    if (state2 < 2 /* Finally */) {
      emitBreak(exception.endLabel);
    } else {
      emitEndfinally();
    }
    markLabel(exception.endLabel);
    emitNop();
    exception.state ***REMOVED*** 3 /* Done */;
  }
  function beginScriptLoopBlock() {
    beginBlock({
      kind: 3 /* Loop */,
      isScript: true,
      breakLabel: -1,
      continueLabel: -1
    });
  }
  function beginLoopBlock(continueLabel) {
    const breakLabel ***REMOVED*** defineLabel();
    beginBlock({
      kind: 3 /* Loop */,
      isScript: false,
      breakLabel,
      continueLabel
    });
    return breakLabel;
  }
  function endLoopBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 3 /* Loop */);
    const block ***REMOVED*** endBlock();
    const breakLabel ***REMOVED*** block.breakLabel;
    if (!block.isScript) {
      markLabel(breakLabel);
    }
  }
  function beginScriptSwitchBlock() {
    beginBlock({
      kind: 2 /* Switch */,
      isScript: true,
      breakLabel: -1
    });
  }
  function beginSwitchBlock() {
    const breakLabel ***REMOVED*** defineLabel();
    beginBlock({
      kind: 2 /* Switch */,
      isScript: false,
      breakLabel
    });
    return breakLabel;
  }
  function endSwitchBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 2 /* Switch */);
    const block ***REMOVED*** endBlock();
    const breakLabel ***REMOVED*** block.breakLabel;
    if (!block.isScript) {
      markLabel(breakLabel);
    }
  }
  function beginScriptLabeledBlock(labelText) {
    beginBlock({
      kind: 4 /* Labeled */,
      isScript: true,
      labelText,
      breakLabel: -1
    });
  }
  function beginLabeledBlock(labelText) {
    const breakLabel ***REMOVED*** defineLabel();
    beginBlock({
      kind: 4 /* Labeled */,
      isScript: false,
      labelText,
      breakLabel
    });
  }
  function endLabeledBlock() {
    Debug.assert(peekBlockKind() ***REMOVED******REMOVED******REMOVED*** 4 /* Labeled */);
    const block ***REMOVED*** endBlock();
    if (!block.isScript) {
      markLabel(block.breakLabel);
    }
  }
  function supportsUnlabeledBreak(block) {
    return block.kind ***REMOVED******REMOVED******REMOVED*** 2 /* Switch */ || block.kind ***REMOVED******REMOVED******REMOVED*** 3 /* Loop */;
  }
  function supportsLabeledBreakOrContinue(block) {
    return block.kind ***REMOVED******REMOVED******REMOVED*** 4 /* Labeled */;
  }
  function supportsUnlabeledContinue(block) {
    return block.kind ***REMOVED******REMOVED******REMOVED*** 3 /* Loop */;
  }
  function hasImmediateContainingLabeledBlock(labelText, start) {
    for (let j ***REMOVED*** start; j >***REMOVED*** 0; j--) {
      const containingBlock ***REMOVED*** blockStack[j];
      if (supportsLabeledBreakOrContinue(containingBlock)) {
        if (containingBlock.labelText ***REMOVED******REMOVED******REMOVED*** labelText) {
          return true;
        }
      } else {
        break;
      }
    }
    return false;
  }
  function findBreakTarget(labelText) {
    if (blockStack) {
      if (labelText) {
        for (let i ***REMOVED*** blockStack.length - 1; i >***REMOVED*** 0; i--) {
          const block ***REMOVED*** blockStack[i];
          if (supportsLabeledBreakOrContinue(block) && block.labelText ***REMOVED******REMOVED******REMOVED*** labelText) {
            return block.breakLabel;
          } else if (supportsUnlabeledBreak(block) && hasImmediateContainingLabeledBlock(labelText, i - 1)) {
            return block.breakLabel;
          }
        }
      } else {
        for (let i ***REMOVED*** blockStack.length - 1; i >***REMOVED*** 0; i--) {
          const block ***REMOVED*** blockStack[i];
          if (supportsUnlabeledBreak(block)) {
            return block.breakLabel;
          }
        }
      }
    }
    return 0;
  }
  function findContinueTarget(labelText) {
    if (blockStack) {
      if (labelText) {
        for (let i ***REMOVED*** blockStack.length - 1; i >***REMOVED*** 0; i--) {
          const block ***REMOVED*** blockStack[i];
          if (supportsUnlabeledContinue(block) && hasImmediateContainingLabeledBlock(labelText, i - 1)) {
            return block.continueLabel;
          }
        }
      } else {
        for (let i ***REMOVED*** blockStack.length - 1; i >***REMOVED*** 0; i--) {
          const block ***REMOVED*** blockStack[i];
          if (supportsUnlabeledContinue(block)) {
            return block.continueLabel;
          }
        }
      }
    }
    return 0;
  }
  function createLabel(label) {
    if (label !***REMOVED******REMOVED*** void 0 && label > 0) {
      if (labelExpressions ***REMOVED******REMOVED******REMOVED*** void 0) {
        labelExpressions ***REMOVED*** [];
      }
      const expression ***REMOVED*** factory2.createNumericLiteral(Number.MAX_SAFE_INTEGER);
      if (labelExpressions[label] ***REMOVED******REMOVED******REMOVED*** void 0) {
        labelExpressions[label] ***REMOVED*** [expression];
      } else {
        labelExpressions[label].push(expression);
      }
      return expression;
    }
    return factory2.createOmittedExpression();
  }
  function createInstruction(instruction) {
    const literal ***REMOVED*** factory2.createNumericLiteral(instruction);
    addSyntheticTrailingComment(literal, 3 /* MultiLineCommentTrivia */, getInstructionName(instruction));
    return literal;
  }
  function createInlineBreak(label, location) {
    Debug.assertLessThan(0, label, "Invalid label");
    return setTextRange(
      factory2.createReturnStatement(
        factory2.createArrayLiteralExpression([
          createInstruction(3 /* Break */),
          createLabel(label)
        ])
      ),
      location
    );
  }
  function createInlineReturn(expression, location) {
    return setTextRange(
      factory2.createReturnStatement(
        factory2.createArrayLiteralExpression(
          expression ? [createInstruction(2 /* Return */), expression] : [createInstruction(2 /* Return */)]
        )
      ),
      location
    );
  }
  function createGeneratorResume(location) {
    return setTextRange(
      factory2.createCallExpression(
        factory2.createPropertyAccessExpression(state, "sent"),
        /*typeArguments*/
        void 0,
        []
      ),
      location
    );
  }
  function emitNop() {
    emitWorker(0 /* Nop */);
  }
  function emitStatement(node) {
    if (node) {
      emitWorker(1 /* Statement */, [node]);
    } else {
      emitNop();
    }
  }
  function emitAssignment(left, right, location) {
    emitWorker(2 /* Assign */, [left, right], location);
  }
  function emitBreak(label, location) {
    emitWorker(3 /* Break */, [label], location);
  }
  function emitBreakWhenTrue(label, condition, location) {
    emitWorker(4 /* BreakWhenTrue */, [label, condition], location);
  }
  function emitBreakWhenFalse(label, condition, location) {
    emitWorker(5 /* BreakWhenFalse */, [label, condition], location);
  }
  function emitYieldStar(expression, location) {
    emitWorker(7 /* YieldStar */, [expression], location);
  }
  function emitYield(expression, location) {
    emitWorker(6 /* Yield */, [expression], location);
  }
  function emitReturn(expression, location) {
    emitWorker(8 /* Return */, [expression], location);
  }
  function emitThrow(expression, location) {
    emitWorker(9 /* Throw */, [expression], location);
  }
  function emitEndfinally() {
    emitWorker(10 /* Endfinally */);
  }
  function emitWorker(code, args, location) {
    if (operations ***REMOVED******REMOVED******REMOVED*** void 0) {
      operations ***REMOVED*** [];
      operationArguments ***REMOVED*** [];
      operationLocations ***REMOVED*** [];
    }
    if (labelOffsets ***REMOVED******REMOVED******REMOVED*** void 0) {
      markLabel(defineLabel());
    }
    const operationIndex ***REMOVED*** operations.length;
    operations[operationIndex] ***REMOVED*** code;
    operationArguments[operationIndex] ***REMOVED*** args;
    operationLocations[operationIndex] ***REMOVED*** location;
  }
  function build2() {
    blockIndex ***REMOVED*** 0;
    labelNumber ***REMOVED*** 0;
    labelNumbers ***REMOVED*** void 0;
    lastOperationWasAbrupt ***REMOVED*** false;
    lastOperationWasCompletion ***REMOVED*** false;
    clauses ***REMOVED*** void 0;
    statements ***REMOVED*** void 0;
    exceptionBlockStack ***REMOVED*** void 0;
    currentExceptionBlock ***REMOVED*** void 0;
    withBlockStack ***REMOVED*** void 0;
    const buildResult ***REMOVED*** buildStatements();
    return emitHelpers().createGeneratorHelper(
      setEmitFlags(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          /*asteriskToken*/
          void 0,
          /*name*/
          void 0,
          /*typeParameters*/
          void 0,
          [factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            state
          )],
          /*type*/
          void 0,
          factory2.createBlock(
            buildResult,
            /*multiLine*/
            buildResult.length > 0
          )
        ),
        1048576 /* ReuseTempVariableScope */
      )
    );
  }
  function buildStatements() {
    if (operations) {
      for (let operationIndex ***REMOVED*** 0; operationIndex < operations.length; operationIndex++) {
        writeOperation(operationIndex);
      }
      flushFinalLabel(operations.length);
    } else {
      flushFinalLabel(0);
    }
    if (clauses) {
      const labelExpression ***REMOVED*** factory2.createPropertyAccessExpression(state, "label");
      const switchStatement ***REMOVED*** factory2.createSwitchStatement(labelExpression, factory2.createCaseBlock(clauses));
      return [startOnNewLine(switchStatement)];
    }
    if (statements) {
      return statements;
    }
    return [];
  }
  function flushLabel() {
    if (!statements) {
      return;
    }
    appendLabel(
      /*markLabelEnd*/
      !lastOperationWasAbrupt
    );
    lastOperationWasAbrupt ***REMOVED*** false;
    lastOperationWasCompletion ***REMOVED*** false;
    labelNumber++;
  }
  function flushFinalLabel(operationIndex) {
    if (isFinalLabelReachable(operationIndex)) {
      tryEnterLabel(operationIndex);
      withBlockStack ***REMOVED*** void 0;
      writeReturn(
        /*expression*/
        void 0,
        /*operationLocation*/
        void 0
      );
    }
    if (statements && clauses) {
      appendLabel(
        /*markLabelEnd*/
        false
      );
    }
    updateLabelExpressions();
  }
  function isFinalLabelReachable(operationIndex) {
    if (!lastOperationWasCompletion) {
      return true;
    }
    if (!labelOffsets || !labelExpressions) {
      return false;
    }
    for (let label ***REMOVED*** 0; label < labelOffsets.length; label++) {
      if (labelOffsets[label] ***REMOVED******REMOVED******REMOVED*** operationIndex && labelExpressions[label]) {
        return true;
      }
    }
    return false;
  }
  function appendLabel(markLabelEnd) {
    if (!clauses) {
      clauses ***REMOVED*** [];
    }
    if (statements) {
      if (withBlockStack) {
        for (let i ***REMOVED*** withBlockStack.length - 1; i >***REMOVED*** 0; i--) {
          const withBlock ***REMOVED*** withBlockStack[i];
          statements ***REMOVED*** [factory2.createWithStatement(withBlock.expression, factory2.createBlock(statements))];
        }
      }
      if (currentExceptionBlock) {
        const { startLabel, catchLabel, finallyLabel, endLabel } ***REMOVED*** currentExceptionBlock;
        statements.unshift(
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              factory2.createPropertyAccessExpression(factory2.createPropertyAccessExpression(state, "trys"), "push"),
              /*typeArguments*/
              void 0,
              [
                factory2.createArrayLiteralExpression([
                  createLabel(startLabel),
                  createLabel(catchLabel),
                  createLabel(finallyLabel),
                  createLabel(endLabel)
                ])
              ]
            )
          )
        );
        currentExceptionBlock ***REMOVED*** void 0;
      }
      if (markLabelEnd) {
        statements.push(
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(state, "label"),
              factory2.createNumericLiteral(labelNumber + 1)
            )
          )
        );
      }
    }
    clauses.push(
      factory2.createCaseClause(
        factory2.createNumericLiteral(labelNumber),
        statements || []
      )
    );
    statements ***REMOVED*** void 0;
  }
  function tryEnterLabel(operationIndex) {
    if (!labelOffsets) {
      return;
    }
    for (let label ***REMOVED*** 0; label < labelOffsets.length; label++) {
      if (labelOffsets[label] ***REMOVED******REMOVED******REMOVED*** operationIndex) {
        flushLabel();
        if (labelNumbers ***REMOVED******REMOVED******REMOVED*** void 0) {
          labelNumbers ***REMOVED*** [];
        }
        if (labelNumbers[labelNumber] ***REMOVED******REMOVED******REMOVED*** void 0) {
          labelNumbers[labelNumber] ***REMOVED*** [label];
        } else {
          labelNumbers[labelNumber].push(label);
        }
      }
    }
  }
  function updateLabelExpressions() {
    if (labelExpressions !***REMOVED******REMOVED*** void 0 && labelNumbers !***REMOVED******REMOVED*** void 0) {
      for (let labelNumber2 ***REMOVED*** 0; labelNumber2 < labelNumbers.length; labelNumber2++) {
        const labels ***REMOVED*** labelNumbers[labelNumber2];
        if (labels !***REMOVED******REMOVED*** void 0) {
          for (const label of labels) {
            const expressions ***REMOVED*** labelExpressions[label];
            if (expressions !***REMOVED******REMOVED*** void 0) {
              for (const expression of expressions) {
                expression.text ***REMOVED*** String(labelNumber2);
              }
            }
          }
        }
      }
    }
  }
  function tryEnterOrLeaveBlock(operationIndex) {
    if (blocks) {
      for (; blockIndex < blockActions.length && blockOffsets[blockIndex] <***REMOVED*** operationIndex; blockIndex++) {
        const block ***REMOVED*** blocks[blockIndex];
        const blockAction ***REMOVED*** blockActions[blockIndex];
        switch (block.kind) {
          case 0 /* Exception */:
            if (blockAction ***REMOVED******REMOVED******REMOVED*** 0 /* Open */) {
              if (!exceptionBlockStack) {
                exceptionBlockStack ***REMOVED*** [];
              }
              if (!statements) {
                statements ***REMOVED*** [];
              }
              exceptionBlockStack.push(currentExceptionBlock);
              currentExceptionBlock ***REMOVED*** block;
            } else if (blockAction ***REMOVED******REMOVED******REMOVED*** 1 /* Close */) {
              currentExceptionBlock ***REMOVED*** exceptionBlockStack.pop();
            }
            break;
          case 1 /* With */:
            if (blockAction ***REMOVED******REMOVED******REMOVED*** 0 /* Open */) {
              if (!withBlockStack) {
                withBlockStack ***REMOVED*** [];
              }
              withBlockStack.push(block);
            } else if (blockAction ***REMOVED******REMOVED******REMOVED*** 1 /* Close */) {
              withBlockStack.pop();
            }
            break;
        }
      }
    }
  }
  function writeOperation(operationIndex) {
    tryEnterLabel(operationIndex);
    tryEnterOrLeaveBlock(operationIndex);
    if (lastOperationWasAbrupt) {
      return;
    }
    lastOperationWasAbrupt ***REMOVED*** false;
    lastOperationWasCompletion ***REMOVED*** false;
    const opcode ***REMOVED*** operations[operationIndex];
    if (opcode ***REMOVED******REMOVED******REMOVED*** 0 /* Nop */) {
      return;
    } else if (opcode ***REMOVED******REMOVED******REMOVED*** 10 /* Endfinally */) {
      return writeEndfinally();
    }
    const args ***REMOVED*** operationArguments[operationIndex];
    if (opcode ***REMOVED******REMOVED******REMOVED*** 1 /* Statement */) {
      return writeStatement(args[0]);
    }
    const location ***REMOVED*** operationLocations[operationIndex];
    switch (opcode) {
      case 2 /* Assign */:
        return writeAssign(args[0], args[1], location);
      case 3 /* Break */:
        return writeBreak(args[0], location);
      case 4 /* BreakWhenTrue */:
        return writeBreakWhenTrue(args[0], args[1], location);
      case 5 /* BreakWhenFalse */:
        return writeBreakWhenFalse(args[0], args[1], location);
      case 6 /* Yield */:
        return writeYield(args[0], location);
      case 7 /* YieldStar */:
        return writeYieldStar(args[0], location);
      case 8 /* Return */:
        return writeReturn(args[0], location);
      case 9 /* Throw */:
        return writeThrow(args[0], location);
    }
  }
  function writeStatement(statement) {
    if (statement) {
      if (!statements) {
        statements ***REMOVED*** [statement];
      } else {
        statements.push(statement);
      }
    }
  }
  function writeAssign(left, right, operationLocation) {
    writeStatement(setTextRange(factory2.createExpressionStatement(factory2.createAssignment(left, right)), operationLocation));
  }
  function writeThrow(expression, operationLocation) {
    lastOperationWasAbrupt ***REMOVED*** true;
    lastOperationWasCompletion ***REMOVED*** true;
    writeStatement(setTextRange(factory2.createThrowStatement(expression), operationLocation));
  }
  function writeReturn(expression, operationLocation) {
    lastOperationWasAbrupt ***REMOVED*** true;
    lastOperationWasCompletion ***REMOVED*** true;
    writeStatement(
      setEmitFlags(
        setTextRange(
          factory2.createReturnStatement(
            factory2.createArrayLiteralExpression(
              expression ? [createInstruction(2 /* Return */), expression] : [createInstruction(2 /* Return */)]
            )
          ),
          operationLocation
        ),
        768 /* NoTokenSourceMaps */
      )
    );
  }
  function writeBreak(label, operationLocation) {
    lastOperationWasAbrupt ***REMOVED*** true;
    writeStatement(
      setEmitFlags(
        setTextRange(
          factory2.createReturnStatement(
            factory2.createArrayLiteralExpression([
              createInstruction(3 /* Break */),
              createLabel(label)
            ])
          ),
          operationLocation
        ),
        768 /* NoTokenSourceMaps */
      )
    );
  }
  function writeBreakWhenTrue(label, condition, operationLocation) {
    writeStatement(
      setEmitFlags(
        factory2.createIfStatement(
          condition,
          setEmitFlags(
            setTextRange(
              factory2.createReturnStatement(
                factory2.createArrayLiteralExpression([
                  createInstruction(3 /* Break */),
                  createLabel(label)
                ])
              ),
              operationLocation
            ),
            768 /* NoTokenSourceMaps */
          )
        ),
        1 /* SingleLine */
      )
    );
  }
  function writeBreakWhenFalse(label, condition, operationLocation) {
    writeStatement(
      setEmitFlags(
        factory2.createIfStatement(
          factory2.createLogicalNot(condition),
          setEmitFlags(
            setTextRange(
              factory2.createReturnStatement(
                factory2.createArrayLiteralExpression([
                  createInstruction(3 /* Break */),
                  createLabel(label)
                ])
              ),
              operationLocation
            ),
            768 /* NoTokenSourceMaps */
          )
        ),
        1 /* SingleLine */
      )
    );
  }
  function writeYield(expression, operationLocation) {
    lastOperationWasAbrupt ***REMOVED*** true;
    writeStatement(
      setEmitFlags(
        setTextRange(
          factory2.createReturnStatement(
            factory2.createArrayLiteralExpression(
              expression ? [createInstruction(4 /* Yield */), expression] : [createInstruction(4 /* Yield */)]
            )
          ),
          operationLocation
        ),
        768 /* NoTokenSourceMaps */
      )
    );
  }
  function writeYieldStar(expression, operationLocation) {
    lastOperationWasAbrupt ***REMOVED*** true;
    writeStatement(
      setEmitFlags(
        setTextRange(
          factory2.createReturnStatement(
            factory2.createArrayLiteralExpression([
              createInstruction(5 /* YieldStar */),
              expression
            ])
          ),
          operationLocation
        ),
        768 /* NoTokenSourceMaps */
      )
    );
  }
  function writeEndfinally() {
    lastOperationWasAbrupt ***REMOVED*** true;
    writeStatement(
      factory2.createReturnStatement(
        factory2.createArrayLiteralExpression([
          createInstruction(7 /* Endfinally */)
        ])
      )
    );
  }
}

// src/compiler/transformers/module/module.ts
function transformModule(context) {
  function getTransformModuleDelegate(moduleKind2) {
    switch (moduleKind2) {
      case 2 /* AMD */:
        return transformAMDModule;
      case 3 /* UMD */:
        return transformUMDModule;
      default:
        return transformCommonJSModule;
    }
  }
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers,
    startLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const resolver ***REMOVED*** context.getEmitResolver();
  const host ***REMOVED*** context.getEmitHost();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const moduleKind ***REMOVED*** getEmitModuleKind(compilerOptions);
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.enableSubstitution(214 /* CallExpression */);
  context.enableSubstitution(216 /* TaggedTemplateExpression */);
  context.enableSubstitution(80 /* Identifier */);
  context.enableSubstitution(227 /* BinaryExpression */);
  context.enableSubstitution(305 /* ShorthandPropertyAssignment */);
  context.enableEmitNotification(308 /* SourceFile */);
  const moduleInfoMap ***REMOVED*** [];
  let currentSourceFile;
  let currentModuleInfo;
  let importsAndRequiresToRewriteOrShim;
  const noSubstitution ***REMOVED*** [];
  let needUMDDynamicImportHelper;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile || !(isEffectiveExternalModule(node, compilerOptions) || node.transformFlags & 8388608 /* ContainsDynamicImport */ || isJsonSourceFile(node) && hasJsonModuleEmitEnabled(compilerOptions) && compilerOptions.outFile)) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    currentModuleInfo ***REMOVED*** collectExternalModuleInfo(context, node);
    moduleInfoMap[getOriginalNodeId(node)] ***REMOVED*** currentModuleInfo;
    if (compilerOptions.rewriteRelativeImportExtensions) {
      forEachDynamicImportOrRequireCall(
        node,
        /*includeTypeSpaceImports*/
        false,
        /*requireStringLiteralLikeArgument*/
        false,
        (node2) ***REMOVED***> {
          if (!isStringLiteralLike(node2.arguments[0]) || shouldRewriteModuleSpecifier(node2.arguments[0].text, compilerOptions)) {
            importsAndRequiresToRewriteOrShim ***REMOVED*** append(importsAndRequiresToRewriteOrShim, node2);
          }
        }
      );
    }
    const transformModule2 ***REMOVED*** getTransformModuleDelegate(moduleKind);
    const updated ***REMOVED*** transformModule2(node);
    currentSourceFile ***REMOVED*** void 0;
    currentModuleInfo ***REMOVED*** void 0;
    needUMDDynamicImportHelper ***REMOVED*** false;
    return updated;
  }
  function shouldEmitUnderscoreUnderscoreESModule() {
    if (hasJSFileExtension(currentSourceFile.fileName) && currentSourceFile.commonJsModuleIndicator && (!currentSourceFile.externalModuleIndicator || currentSourceFile.externalModuleIndicator ***REMOVED******REMOVED******REMOVED*** true)) {
      return false;
    }
    if (!currentModuleInfo.exportEquals && isExternalModule(currentSourceFile)) {
      return true;
    }
    return false;
  }
  function transformCommonJSModule(node) {
    startLexicalEnvironment();
    const statements ***REMOVED*** [];
    const ensureUseStrict ***REMOVED*** getStrictOptionValue(compilerOptions, "alwaysStrict") || isExternalModule(currentSourceFile);
    const statementOffset ***REMOVED*** factory2.copyPrologue(node.statements, statements, ensureUseStrict && !isJsonSourceFile(node), topLevelVisitor);
    if (shouldEmitUnderscoreUnderscoreESModule()) {
      append(statements, createUnderscoreUnderscoreESModule());
    }
    if (some(currentModuleInfo.exportedNames)) {
      const chunkSize ***REMOVED*** 50;
      for (let i ***REMOVED*** 0; i < currentModuleInfo.exportedNames.length; i +***REMOVED*** chunkSize) {
        append(
          statements,
          factory2.createExpressionStatement(
            reduceLeft(
              currentModuleInfo.exportedNames.slice(i, i + chunkSize),
              (prev, nextId) ***REMOVED***> nextId.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createAssignment(factory2.createElementAccessExpression(factory2.createIdentifier("exports"), factory2.createStringLiteral(nextId.text)), prev) : factory2.createAssignment(factory2.createPropertyAccessExpression(factory2.createIdentifier("exports"), factory2.createIdentifier(idText(nextId))), prev),
              factory2.createVoidZero()
            )
          )
        );
      }
    }
    for (const f of currentModuleInfo.exportedFunctions) {
      appendExportsOfHoistedDeclaration(statements, f);
    }
    append(statements, visitNode(currentModuleInfo.externalHelpersImportDeclaration, topLevelVisitor, isStatement));
    addRange(statements, visitNodes2(node.statements, topLevelVisitor, isStatement, statementOffset));
    addExportEqualsIfNeeded(
      statements,
      /*emitAsReturn*/
      false
    );
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    const updated ***REMOVED*** factory2.updateSourceFile(node, setTextRange(factory2.createNodeArray(statements), node.statements));
    addEmitHelpers(updated, context.readEmitHelpers());
    return updated;
  }
  function transformAMDModule(node) {
    const define ***REMOVED*** factory2.createIdentifier("define");
    const moduleName ***REMOVED*** tryGetModuleNameFromFile(factory2, node, host, compilerOptions);
    const jsonSourceFile ***REMOVED*** isJsonSourceFile(node) && node;
    const { aliasedModuleNames, unaliasedModuleNames, importAliasNames } ***REMOVED*** collectAsynchronousDependencies(
      node,
      /*includeNonAmdDependencies*/
      true
    );
    const updated ***REMOVED*** factory2.updateSourceFile(
      node,
      setTextRange(
        factory2.createNodeArray([
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              define,
              /*typeArguments*/
              void 0,
              [
                // Add the module name (if provided).
                ...moduleName ? [moduleName] : [],
                // Add the dependency array argument:
                //
                //     ["require", "exports", module1", "module2", ...]
                factory2.createArrayLiteralExpression(
                  jsonSourceFile ? emptyArray : [
                    factory2.createStringLiteral("require"),
                    factory2.createStringLiteral("exports"),
                    ...aliasedModuleNames,
                    ...unaliasedModuleNames
                  ]
                ),
                // Add the module body function argument:
                //
                //     function (require, exports, module1, module2) ...
                jsonSourceFile ? jsonSourceFile.statements.length ? jsonSourceFile.statements[0].expression : factory2.createObjectLiteralExpression() : factory2.createFunctionExpression(
                  /*modifiers*/
                  void 0,
                  /*asteriskToken*/
                  void 0,
                  /*name*/
                  void 0,
                  /*typeParameters*/
                  void 0,
                  [
                    factory2.createParameterDeclaration(
                      /*modifiers*/
                      void 0,
                      /*dotDotDotToken*/
                      void 0,
                      "require"
                    ),
                    factory2.createParameterDeclaration(
                      /*modifiers*/
                      void 0,
                      /*dotDotDotToken*/
                      void 0,
                      "exports"
                    ),
                    ...importAliasNames
                  ],
                  /*type*/
                  void 0,
                  transformAsynchronousModuleBody(node)
                )
              ]
            )
          )
        ]),
        /*location*/
        node.statements
      )
    );
    addEmitHelpers(updated, context.readEmitHelpers());
    return updated;
  }
  function transformUMDModule(node) {
    const { aliasedModuleNames, unaliasedModuleNames, importAliasNames } ***REMOVED*** collectAsynchronousDependencies(
      node,
      /*includeNonAmdDependencies*/
      false
    );
    const moduleName ***REMOVED*** tryGetModuleNameFromFile(factory2, node, host, compilerOptions);
    const umdHeader ***REMOVED*** factory2.createFunctionExpression(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      [factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        "factory"
      )],
      /*type*/
      void 0,
      setTextRange(
        factory2.createBlock(
          [
            factory2.createIfStatement(
              factory2.createLogicalAnd(
                factory2.createTypeCheck(factory2.createIdentifier("module"), "object"),
                factory2.createTypeCheck(factory2.createPropertyAccessExpression(factory2.createIdentifier("module"), "exports"), "object")
              ),
              factory2.createBlock([
                factory2.createVariableStatement(
                  /*modifiers*/
                  void 0,
                  [
                    factory2.createVariableDeclaration(
                      "v",
                      /*exclamationToken*/
                      void 0,
                      /*type*/
                      void 0,
                      factory2.createCallExpression(
                        factory2.createIdentifier("factory"),
                        /*typeArguments*/
                        void 0,
                        [
                          factory2.createIdentifier("require"),
                          factory2.createIdentifier("exports")
                        ]
                      )
                    )
                  ]
                ),
                setEmitFlags(
                  factory2.createIfStatement(
                    factory2.createStrictInequality(
                      factory2.createIdentifier("v"),
                      factory2.createIdentifier("undefined")
                    ),
                    factory2.createExpressionStatement(
                      factory2.createAssignment(
                        factory2.createPropertyAccessExpression(factory2.createIdentifier("module"), "exports"),
                        factory2.createIdentifier("v")
                      )
                    )
                  ),
                  1 /* SingleLine */
                )
              ]),
              factory2.createIfStatement(
                factory2.createLogicalAnd(
                  factory2.createTypeCheck(factory2.createIdentifier("define"), "function"),
                  factory2.createPropertyAccessExpression(factory2.createIdentifier("define"), "amd")
                ),
                factory2.createBlock([
                  factory2.createExpressionStatement(
                    factory2.createCallExpression(
                      factory2.createIdentifier("define"),
                      /*typeArguments*/
                      void 0,
                      [
                        // Add the module name (if provided).
                        ...moduleName ? [moduleName] : [],
                        factory2.createArrayLiteralExpression([
                          factory2.createStringLiteral("require"),
                          factory2.createStringLiteral("exports"),
                          ...aliasedModuleNames,
                          ...unaliasedModuleNames
                        ]),
                        factory2.createIdentifier("factory")
                      ]
                    )
                  )
                ])
              )
            )
          ],
          /*multiLine*/
          true
        ),
        /*location*/
        void 0
      )
    );
    const updated ***REMOVED*** factory2.updateSourceFile(
      node,
      setTextRange(
        factory2.createNodeArray([
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              umdHeader,
              /*typeArguments*/
              void 0,
              [
                // Add the module body function argument:
                //
                //     function (require, exports) ...
                factory2.createFunctionExpression(
                  /*modifiers*/
                  void 0,
                  /*asteriskToken*/
                  void 0,
                  /*name*/
                  void 0,
                  /*typeParameters*/
                  void 0,
                  [
                    factory2.createParameterDeclaration(
                      /*modifiers*/
                      void 0,
                      /*dotDotDotToken*/
                      void 0,
                      "require"
                    ),
                    factory2.createParameterDeclaration(
                      /*modifiers*/
                      void 0,
                      /*dotDotDotToken*/
                      void 0,
                      "exports"
                    ),
                    ...importAliasNames
                  ],
                  /*type*/
                  void 0,
                  transformAsynchronousModuleBody(node)
                )
              ]
            )
          )
        ]),
        /*location*/
        node.statements
      )
    );
    addEmitHelpers(updated, context.readEmitHelpers());
    return updated;
  }
  function collectAsynchronousDependencies(node, includeNonAmdDependencies) {
    const aliasedModuleNames ***REMOVED*** [];
    const unaliasedModuleNames ***REMOVED*** [];
    const importAliasNames ***REMOVED*** [];
    for (const amdDependency of node.amdDependencies) {
      if (amdDependency.name) {
        aliasedModuleNames.push(factory2.createStringLiteral(amdDependency.path));
        importAliasNames.push(factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          amdDependency.name
        ));
      } else {
        unaliasedModuleNames.push(factory2.createStringLiteral(amdDependency.path));
      }
    }
    for (const importNode of currentModuleInfo.externalImports) {
      const externalModuleName ***REMOVED*** getExternalModuleNameLiteral(factory2, importNode, currentSourceFile, host, resolver, compilerOptions);
      const importAliasName ***REMOVED*** getLocalNameForExternalImport(factory2, importNode, currentSourceFile);
      if (externalModuleName) {
        if (includeNonAmdDependencies && importAliasName) {
          setEmitFlags(importAliasName, 8 /* NoSubstitution */);
          aliasedModuleNames.push(externalModuleName);
          importAliasNames.push(factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            importAliasName
          ));
        } else {
          unaliasedModuleNames.push(externalModuleName);
        }
      }
    }
    return { aliasedModuleNames, unaliasedModuleNames, importAliasNames };
  }
  function getAMDImportExpressionForImport(node) {
    if (isImportEqualsDeclaration(node) || isExportDeclaration(node) || !getExternalModuleNameLiteral(factory2, node, currentSourceFile, host, resolver, compilerOptions)) {
      return void 0;
    }
    const name ***REMOVED*** getLocalNameForExternalImport(factory2, node, currentSourceFile);
    const expr ***REMOVED*** getHelperExpressionForImport(node, name);
    if (expr ***REMOVED******REMOVED******REMOVED*** name) {
      return void 0;
    }
    return factory2.createExpressionStatement(factory2.createAssignment(name, expr));
  }
  function transformAsynchronousModuleBody(node) {
    startLexicalEnvironment();
    const statements ***REMOVED*** [];
    const statementOffset ***REMOVED*** factory2.copyPrologue(
      node.statements,
      statements,
      /*ensureUseStrict*/
      true,
      topLevelVisitor
    );
    if (shouldEmitUnderscoreUnderscoreESModule()) {
      append(statements, createUnderscoreUnderscoreESModule());
    }
    if (some(currentModuleInfo.exportedNames)) {
      append(
        statements,
        factory2.createExpressionStatement(reduceLeft(currentModuleInfo.exportedNames, (prev, nextId) ***REMOVED***> nextId.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createAssignment(factory2.createElementAccessExpression(factory2.createIdentifier("exports"), factory2.createStringLiteral(nextId.text)), prev) : factory2.createAssignment(factory2.createPropertyAccessExpression(factory2.createIdentifier("exports"), factory2.createIdentifier(idText(nextId))), prev), factory2.createVoidZero()))
      );
    }
    for (const f of currentModuleInfo.exportedFunctions) {
      appendExportsOfHoistedDeclaration(statements, f);
    }
    append(statements, visitNode(currentModuleInfo.externalHelpersImportDeclaration, topLevelVisitor, isStatement));
    if (moduleKind ***REMOVED******REMOVED******REMOVED*** 2 /* AMD */) {
      addRange(statements, mapDefined(currentModuleInfo.externalImports, getAMDImportExpressionForImport));
    }
    addRange(statements, visitNodes2(node.statements, topLevelVisitor, isStatement, statementOffset));
    addExportEqualsIfNeeded(
      statements,
      /*emitAsReturn*/
      true
    );
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    const body ***REMOVED*** factory2.createBlock(
      statements,
      /*multiLine*/
      true
    );
    if (needUMDDynamicImportHelper) {
      addEmitHelper(body, dynamicImportUMDHelper);
    }
    return body;
  }
  function addExportEqualsIfNeeded(statements, emitAsReturn) {
    if (currentModuleInfo.exportEquals) {
      const expressionResult ***REMOVED*** visitNode(currentModuleInfo.exportEquals.expression, visitor, isExpression);
      if (expressionResult) {
        if (emitAsReturn) {
          const statement ***REMOVED*** factory2.createReturnStatement(expressionResult);
          setTextRange(statement, currentModuleInfo.exportEquals);
          setEmitFlags(statement, 768 /* NoTokenSourceMaps */ | 3072 /* NoComments */);
          statements.push(statement);
        } else {
          const statement ***REMOVED*** factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(
                factory2.createIdentifier("module"),
                "exports"
              ),
              expressionResult
            )
          );
          setTextRange(statement, currentModuleInfo.exportEquals);
          setEmitFlags(statement, 3072 /* NoComments */);
          statements.push(statement);
        }
      }
    }
  }
  function topLevelVisitor(node) {
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
        return visitTopLevelImportDeclaration(node);
      case 272 /* ImportEqualsDeclaration */:
        return visitTopLevelImportEqualsDeclaration(node);
      case 279 /* ExportDeclaration */:
        return visitTopLevelExportDeclaration(node);
      case 278 /* ExportAssignment */:
        return visitTopLevelExportAssignment(node);
      default:
        return topLevelNestedVisitor(node);
    }
  }
  function topLevelNestedVisitor(node) {
    switch (node.kind) {
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 249 /* ForStatement */:
        return visitForStatement(
          node,
          /*isTopLevel*/
          true
        );
      case 250 /* ForInStatement */:
        return visitForInStatement(node);
      case 251 /* ForOfStatement */:
        return visitForOfStatement(node);
      case 247 /* DoStatement */:
        return visitDoStatement(node);
      case 248 /* WhileStatement */:
        return visitWhileStatement(node);
      case 257 /* LabeledStatement */:
        return visitLabeledStatement(node);
      case 255 /* WithStatement */:
        return visitWithStatement(node);
      case 246 /* IfStatement */:
        return visitIfStatement(node);
      case 256 /* SwitchStatement */:
        return visitSwitchStatement(node);
      case 270 /* CaseBlock */:
        return visitCaseBlock(node);
      case 297 /* CaseClause */:
        return visitCaseClause(node);
      case 298 /* DefaultClause */:
        return visitDefaultClause(node);
      case 259 /* TryStatement */:
        return visitTryStatement(node);
      case 300 /* CatchClause */:
        return visitCatchClause(node);
      case 242 /* Block */:
        return visitBlock(node);
      default:
        return visitor(node);
    }
  }
  function visitorWorker(node, valueIsDiscarded) {
    if (!(node.transformFlags & (8388608 /* ContainsDynamicImport */ | 4096 /* ContainsDestructuringAssignment */ | 268435456 /* ContainsUpdateExpressionForIdentifier */)) && !(importsAndRequiresToRewriteOrShim ***REMOVED******REMOVED*** null ? void 0 : importsAndRequiresToRewriteOrShim.length)) {
      return node;
    }
    switch (node.kind) {
      case 249 /* ForStatement */:
        return visitForStatement(
          node,
          /*isTopLevel*/
          false
        );
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(node, valueIsDiscarded);
      case 356 /* PartiallyEmittedExpression */:
        return visitPartiallyEmittedExpression(node, valueIsDiscarded);
      case 214 /* CallExpression */:
        const needsRewrite ***REMOVED*** node ***REMOVED******REMOVED******REMOVED*** firstOrUndefined(importsAndRequiresToRewriteOrShim);
        if (needsRewrite) {
          importsAndRequiresToRewriteOrShim.shift();
        }
        if (isImportCall(node) && host.shouldTransformImportCall(currentSourceFile)) {
          return visitImportCallExpression(node, needsRewrite);
        } else if (needsRewrite) {
          return shimOrRewriteImportOrRequireCall(node);
        }
        break;
      case 227 /* BinaryExpression */:
        if (isDestructuringAssignment(node)) {
          return visitDestructuringAssignment(node, valueIsDiscarded);
        }
        break;
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPreOrPostfixUnaryExpression(node, valueIsDiscarded);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    return visitorWorker(
      node,
      /*valueIsDiscarded*/
      false
    );
  }
  function discardedValueVisitor(node) {
    return visitorWorker(
      node,
      /*valueIsDiscarded*/
      true
    );
  }
  function destructuringNeedsFlattening(node) {
    if (isObjectLiteralExpression(node)) {
      for (const elem of node.properties) {
        switch (elem.kind) {
          case 304 /* PropertyAssignment */:
            if (destructuringNeedsFlattening(elem.initializer)) {
              return true;
            }
            break;
          case 305 /* ShorthandPropertyAssignment */:
            if (destructuringNeedsFlattening(elem.name)) {
              return true;
            }
            break;
          case 306 /* SpreadAssignment */:
            if (destructuringNeedsFlattening(elem.expression)) {
              return true;
            }
            break;
          case 175 /* MethodDeclaration */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
            return false;
          default:
            Debug.assertNever(elem, "Unhandled object member kind");
        }
      }
    } else if (isArrayLiteralExpression(node)) {
      for (const elem of node.elements) {
        if (isSpreadElement(elem)) {
          if (destructuringNeedsFlattening(elem.expression)) {
            return true;
          }
        } else if (destructuringNeedsFlattening(elem)) {
          return true;
        }
      }
    } else if (isIdentifier(node)) {
      return length(getExports(node)) > (isExportName(node) ? 1 : 0);
    }
    return false;
  }
  function visitDestructuringAssignment(node, valueIsDiscarded) {
    if (destructuringNeedsFlattening(node.left)) {
      return flattenDestructuringAssignment(node, visitor, context, 0 /* All */, !valueIsDiscarded, createAllExportExpressions);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitForStatement(node, isTopLevel) {
    if (isTopLevel && node.initializer && isVariableDeclarationList(node.initializer) && !(node.initializer.flags & 7 /* BlockScoped */)) {
      const exportStatements ***REMOVED*** appendExportsOfVariableDeclarationList(
        /*statements*/
        void 0,
        node.initializer,
        /*isForInOrOfInitializer*/
        false
      );
      if (exportStatements) {
        const statements ***REMOVED*** [];
        const varDeclList ***REMOVED*** visitNode(node.initializer, discardedValueVisitor, isVariableDeclarationList);
        const varStatement ***REMOVED*** factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          varDeclList
        );
        statements.push(varStatement);
        addRange(statements, exportStatements);
        const condition ***REMOVED*** visitNode(node.condition, visitor, isExpression);
        const incrementor ***REMOVED*** visitNode(node.incrementor, discardedValueVisitor, isExpression);
        const body ***REMOVED*** visitIterationBody(node.statement, isTopLevel ? topLevelNestedVisitor : visitor, context);
        statements.push(factory2.updateForStatement(
          node,
          /*initializer*/
          void 0,
          condition,
          incrementor,
          body
        ));
        return statements;
      }
    }
    return factory2.updateForStatement(
      node,
      visitNode(node.initializer, discardedValueVisitor, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, discardedValueVisitor, isExpression),
      visitIterationBody(node.statement, isTopLevel ? topLevelNestedVisitor : visitor, context)
    );
  }
  function visitForInStatement(node) {
    if (isVariableDeclarationList(node.initializer) && !(node.initializer.flags & 7 /* BlockScoped */)) {
      const exportStatements ***REMOVED*** appendExportsOfVariableDeclarationList(
        /*statements*/
        void 0,
        node.initializer,
        /*isForInOrOfInitializer*/
        true
      );
      if (some(exportStatements)) {
        const initializer ***REMOVED*** visitNode(node.initializer, discardedValueVisitor, isForInitializer);
        const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
        const body ***REMOVED*** visitIterationBody(node.statement, topLevelNestedVisitor, context);
        const mergedBody ***REMOVED*** isBlock(body) ? factory2.updateBlock(body, [...exportStatements, ...body.statements]) : factory2.createBlock(
          [...exportStatements, body],
          /*multiLine*/
          true
        );
        return factory2.updateForInStatement(node, initializer, expression, mergedBody);
      }
    }
    return factory2.updateForInStatement(
      node,
      visitNode(node.initializer, discardedValueVisitor, isForInitializer),
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
  }
  function visitForOfStatement(node) {
    if (isVariableDeclarationList(node.initializer) && !(node.initializer.flags & 7 /* BlockScoped */)) {
      const exportStatements ***REMOVED*** appendExportsOfVariableDeclarationList(
        /*statements*/
        void 0,
        node.initializer,
        /*isForInOrOfInitializer*/
        true
      );
      const initializer ***REMOVED*** visitNode(node.initializer, discardedValueVisitor, isForInitializer);
      const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
      let body ***REMOVED*** visitIterationBody(node.statement, topLevelNestedVisitor, context);
      if (some(exportStatements)) {
        body ***REMOVED*** isBlock(body) ? factory2.updateBlock(body, [...exportStatements, ...body.statements]) : factory2.createBlock(
          [...exportStatements, body],
          /*multiLine*/
          true
        );
      }
      return factory2.updateForOfStatement(node, node.awaitModifier, initializer, expression, body);
    }
    return factory2.updateForOfStatement(
      node,
      node.awaitModifier,
      visitNode(node.initializer, discardedValueVisitor, isForInitializer),
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
  }
  function visitDoStatement(node) {
    return factory2.updateDoStatement(
      node,
      visitIterationBody(node.statement, topLevelNestedVisitor, context),
      visitNode(node.expression, visitor, isExpression)
    );
  }
  function visitWhileStatement(node) {
    return factory2.updateWhileStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
  }
  function visitLabeledStatement(node) {
    return factory2.updateLabeledStatement(
      node,
      node.label,
      visitNode(node.statement, topLevelNestedVisitor, isStatement, factory2.liftToBlock) ?? setTextRange(factory2.createEmptyStatement(), node.statement)
    );
  }
  function visitWithStatement(node) {
    return factory2.updateWithStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      Debug.checkDefined(visitNode(node.statement, topLevelNestedVisitor, isStatement, factory2.liftToBlock))
    );
  }
  function visitIfStatement(node) {
    return factory2.updateIfStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitNode(node.thenStatement, topLevelNestedVisitor, isStatement, factory2.liftToBlock) ?? factory2.createBlock([]),
      visitNode(node.elseStatement, topLevelNestedVisitor, isStatement, factory2.liftToBlock)
    );
  }
  function visitSwitchStatement(node) {
    return factory2.updateSwitchStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      Debug.checkDefined(visitNode(node.caseBlock, topLevelNestedVisitor, isCaseBlock))
    );
  }
  function visitCaseBlock(node) {
    return factory2.updateCaseBlock(
      node,
      visitNodes2(node.clauses, topLevelNestedVisitor, isCaseOrDefaultClause)
    );
  }
  function visitCaseClause(node) {
    return factory2.updateCaseClause(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitNodes2(node.statements, topLevelNestedVisitor, isStatement)
    );
  }
  function visitDefaultClause(node) {
    return visitEachChild(node, topLevelNestedVisitor, context);
  }
  function visitTryStatement(node) {
    return visitEachChild(node, topLevelNestedVisitor, context);
  }
  function visitCatchClause(node) {
    return factory2.updateCatchClause(
      node,
      node.variableDeclaration,
      Debug.checkDefined(visitNode(node.block, topLevelNestedVisitor, isBlock))
    );
  }
  function visitBlock(node) {
    node ***REMOVED*** visitEachChild(node, topLevelNestedVisitor, context);
    return node;
  }
  function visitExpressionStatement(node) {
    return factory2.updateExpressionStatement(
      node,
      visitNode(node.expression, discardedValueVisitor, isExpression)
    );
  }
  function visitParenthesizedExpression(node, valueIsDiscarded) {
    return factory2.updateParenthesizedExpression(node, visitNode(node.expression, valueIsDiscarded ? discardedValueVisitor : visitor, isExpression));
  }
  function visitPartiallyEmittedExpression(node, valueIsDiscarded) {
    return factory2.updatePartiallyEmittedExpression(node, visitNode(node.expression, valueIsDiscarded ? discardedValueVisitor : visitor, isExpression));
  }
  function visitPreOrPostfixUnaryExpression(node, valueIsDiscarded) {
    if ((node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) && isIdentifier(node.operand) && !isGeneratedIdentifier(node.operand) && !isLocalName(node.operand) && !isDeclarationNameOfEnumOrNamespace(node.operand)) {
      const exportedNames ***REMOVED*** getExports(node.operand);
      if (exportedNames) {
        let temp;
        let expression ***REMOVED*** visitNode(node.operand, visitor, isExpression);
        if (isPrefixUnaryExpression(node)) {
          expression ***REMOVED*** factory2.updatePrefixUnaryExpression(node, expression);
        } else {
          expression ***REMOVED*** factory2.updatePostfixUnaryExpression(node, expression);
          if (!valueIsDiscarded) {
            temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
            expression ***REMOVED*** factory2.createAssignment(temp, expression);
            setTextRange(expression, node);
          }
          expression ***REMOVED*** factory2.createComma(expression, factory2.cloneNode(node.operand));
          setTextRange(expression, node);
        }
        for (const exportName of exportedNames) {
          noSubstitution[getNodeId(expression)] ***REMOVED*** true;
          expression ***REMOVED*** createExportExpression(exportName, expression);
          setTextRange(expression, node);
        }
        if (temp) {
          noSubstitution[getNodeId(expression)] ***REMOVED*** true;
          expression ***REMOVED*** factory2.createComma(expression, temp);
          setTextRange(expression, node);
        }
        return expression;
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function shimOrRewriteImportOrRequireCall(node) {
    return factory2.updateCallExpression(
      node,
      node.expression,
      /*typeArguments*/
      void 0,
      visitNodes2(node.arguments, (arg) ***REMOVED***> {
        if (arg ***REMOVED******REMOVED******REMOVED*** node.arguments[0]) {
          return isStringLiteralLike(arg) ? rewriteModuleSpecifier(arg, compilerOptions) : emitHelpers().createRewriteRelativeImportExtensionsHelper(arg);
        }
        return visitor(arg);
      }, isExpression)
    );
  }
  function visitImportCallExpression(node, rewriteOrShim) {
    if (moduleKind ***REMOVED******REMOVED******REMOVED*** 0 /* None */ && languageVersion >***REMOVED*** 7 /* ES2020 */) {
      return visitEachChild(node, visitor, context);
    }
    const externalModuleName ***REMOVED*** getExternalModuleNameLiteral(factory2, node, currentSourceFile, host, resolver, compilerOptions);
    const firstArgument ***REMOVED*** visitNode(firstOrUndefined(node.arguments), visitor, isExpression);
    const argument ***REMOVED*** externalModuleName && (!firstArgument || !isStringLiteral(firstArgument) || firstArgument.text !***REMOVED******REMOVED*** externalModuleName.text) ? externalModuleName : firstArgument && rewriteOrShim ? isStringLiteral(firstArgument) ? rewriteModuleSpecifier(firstArgument, compilerOptions) : emitHelpers().createRewriteRelativeImportExtensionsHelper(firstArgument) : firstArgument;
    const containsLexicalThis ***REMOVED*** !!(node.transformFlags & 16384 /* ContainsLexicalThis */);
    switch (compilerOptions.module) {
      case 2 /* AMD */:
        return createImportCallExpressionAMD(argument, containsLexicalThis);
      case 3 /* UMD */:
        return createImportCallExpressionUMD(argument ?? factory2.createVoidZero(), containsLexicalThis);
      case 1 /* CommonJS */:
      default:
        return createImportCallExpressionCommonJS(argument);
    }
  }
  function createImportCallExpressionUMD(arg, containsLexicalThis) {
    needUMDDynamicImportHelper ***REMOVED*** true;
    if (isSimpleCopiableExpression(arg)) {
      const argClone ***REMOVED*** isGeneratedIdentifier(arg) ? arg : isStringLiteral(arg) ? factory2.createStringLiteralFromNode(arg) : setEmitFlags(setTextRange(factory2.cloneNode(arg), arg), 3072 /* NoComments */);
      return factory2.createConditionalExpression(
        /*condition*/
        factory2.createIdentifier("__syncRequire"),
        /*questionToken*/
        void 0,
        /*whenTrue*/
        createImportCallExpressionCommonJS(arg),
        /*colonToken*/
        void 0,
        /*whenFalse*/
        createImportCallExpressionAMD(argClone, containsLexicalThis)
      );
    } else {
      const temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
      return factory2.createComma(
        factory2.createAssignment(temp, arg),
        factory2.createConditionalExpression(
          /*condition*/
          factory2.createIdentifier("__syncRequire"),
          /*questionToken*/
          void 0,
          /*whenTrue*/
          createImportCallExpressionCommonJS(
            temp,
            /*isInlineable*/
            true
          ),
          /*colonToken*/
          void 0,
          /*whenFalse*/
          createImportCallExpressionAMD(temp, containsLexicalThis)
        )
      );
    }
  }
  function createImportCallExpressionAMD(arg, containsLexicalThis) {
    const resolve ***REMOVED*** factory2.createUniqueName("resolve");
    const reject ***REMOVED*** factory2.createUniqueName("reject");
    const parameters ***REMOVED*** [
      factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        /*name*/
        resolve
      ),
      factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        /*name*/
        reject
      )
    ];
    const body ***REMOVED*** factory2.createBlock([
      factory2.createExpressionStatement(
        factory2.createCallExpression(
          factory2.createIdentifier("require"),
          /*typeArguments*/
          void 0,
          [factory2.createArrayLiteralExpression([arg || factory2.createOmittedExpression()]), resolve, reject]
        )
      )
    ]);
    let func;
    if (languageVersion >***REMOVED*** 2 /* ES2015 */) {
      func ***REMOVED*** factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        parameters,
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        body
      );
    } else {
      func ***REMOVED*** factory2.createFunctionExpression(
        /*modifiers*/
        void 0,
        /*asteriskToken*/
        void 0,
        /*name*/
        void 0,
        /*typeParameters*/
        void 0,
        parameters,
        /*type*/
        void 0,
        body
      );
      if (containsLexicalThis) {
        setEmitFlags(func, 16 /* CapturesThis */);
      }
    }
    const promise ***REMOVED*** factory2.createNewExpression(
      factory2.createIdentifier("Promise"),
      /*typeArguments*/
      void 0,
      [func]
    );
    if (getESModuleInterop(compilerOptions)) {
      return factory2.createCallExpression(
        factory2.createPropertyAccessExpression(promise, factory2.createIdentifier("then")),
        /*typeArguments*/
        void 0,
        [emitHelpers().createImportStarCallbackHelper()]
      );
    }
    return promise;
  }
  function createImportCallExpressionCommonJS(arg, isInlineable) {
    const needSyncEval ***REMOVED*** arg && !isSimpleInlineableExpression(arg) && !isInlineable;
    const promiseResolveCall ***REMOVED*** factory2.createCallExpression(
      factory2.createPropertyAccessExpression(factory2.createIdentifier("Promise"), "resolve"),
      /*typeArguments*/
      void 0,
      /*argumentsArray*/
      needSyncEval ? languageVersion >***REMOVED*** 2 /* ES2015 */ ? [
        factory2.createTemplateExpression(factory2.createTemplateHead(""), [
          factory2.createTemplateSpan(arg, factory2.createTemplateTail(""))
        ])
      ] : [
        factory2.createCallExpression(
          factory2.createPropertyAccessExpression(factory2.createStringLiteral(""), "concat"),
          /*typeArguments*/
          void 0,
          [arg]
        )
      ] : []
    );
    let requireCall ***REMOVED*** factory2.createCallExpression(
      factory2.createIdentifier("require"),
      /*typeArguments*/
      void 0,
      needSyncEval ? [factory2.createIdentifier("s")] : arg ? [arg] : []
    );
    if (getESModuleInterop(compilerOptions)) {
      requireCall ***REMOVED*** emitHelpers().createImportStarHelper(requireCall);
    }
    const parameters ***REMOVED*** needSyncEval ? [
      factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        /*name*/
        "s"
      )
    ] : [];
    let func;
    if (languageVersion >***REMOVED*** 2 /* ES2015 */) {
      func ***REMOVED*** factory2.createArrowFunction(
        /*modifiers*/
        void 0,
        /*typeParameters*/
        void 0,
        /*parameters*/
        parameters,
        /*type*/
        void 0,
        /*equalsGreaterThanToken*/
        void 0,
        requireCall
      );
    } else {
      func ***REMOVED*** factory2.createFunctionExpression(
        /*modifiers*/
        void 0,
        /*asteriskToken*/
        void 0,
        /*name*/
        void 0,
        /*typeParameters*/
        void 0,
        /*parameters*/
        parameters,
        /*type*/
        void 0,
        factory2.createBlock([factory2.createReturnStatement(requireCall)])
      );
    }
    const downleveledImport ***REMOVED*** factory2.createCallExpression(
      factory2.createPropertyAccessExpression(promiseResolveCall, "then"),
      /*typeArguments*/
      void 0,
      [func]
    );
    return downleveledImport;
  }
  function getHelperExpressionForExport(node, innerExpr) {
    if (!getESModuleInterop(compilerOptions) || getInternalEmitFlags(node) & 2 /* NeverApplyImportHelper */) {
      return innerExpr;
    }
    if (getExportNeedsImportStarHelper(node)) {
      return emitHelpers().createImportStarHelper(innerExpr);
    }
    return innerExpr;
  }
  function getHelperExpressionForImport(node, innerExpr) {
    if (!getESModuleInterop(compilerOptions) || getInternalEmitFlags(node) & 2 /* NeverApplyImportHelper */) {
      return innerExpr;
    }
    if (getImportNeedsImportStarHelper(node)) {
      return emitHelpers().createImportStarHelper(innerExpr);
    }
    if (getImportNeedsImportDefaultHelper(node)) {
      return emitHelpers().createImportDefaultHelper(innerExpr);
    }
    return innerExpr;
  }
  function visitTopLevelImportDeclaration(node) {
    let statements;
    const namespaceDeclaration ***REMOVED*** getNamespaceDeclarationNode(node);
    if (moduleKind !***REMOVED******REMOVED*** 2 /* AMD */) {
      if (!node.importClause) {
        return setOriginalNode(setTextRange(factory2.createExpressionStatement(createRequireCall(node)), node), node);
      } else {
        const variables ***REMOVED*** [];
        if (namespaceDeclaration && !isDefaultImport(node)) {
          variables.push(
            factory2.createVariableDeclaration(
              factory2.cloneNode(namespaceDeclaration.name),
              /*exclamationToken*/
              void 0,
              /*type*/
              void 0,
              getHelperExpressionForImport(node, createRequireCall(node))
            )
          );
        } else {
          variables.push(
            factory2.createVariableDeclaration(
              factory2.getGeneratedNameForNode(node),
              /*exclamationToken*/
              void 0,
              /*type*/
              void 0,
              getHelperExpressionForImport(node, createRequireCall(node))
            )
          );
          if (namespaceDeclaration && isDefaultImport(node)) {
            variables.push(
              factory2.createVariableDeclaration(
                factory2.cloneNode(namespaceDeclaration.name),
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                factory2.getGeneratedNameForNode(node)
              )
            );
          }
        }
        statements ***REMOVED*** append(
          statements,
          setOriginalNode(
            setTextRange(
              factory2.createVariableStatement(
                /*modifiers*/
                void 0,
                factory2.createVariableDeclarationList(
                  variables,
                  languageVersion >***REMOVED*** 2 /* ES2015 */ ? 2 /* Const */ : 0 /* None */
                )
              ),
              /*location*/
              node
            ),
            /*original*/
            node
          )
        );
      }
    } else if (namespaceDeclaration && isDefaultImport(node)) {
      statements ***REMOVED*** append(
        statements,
        factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          factory2.createVariableDeclarationList(
            [
              setOriginalNode(
                setTextRange(
                  factory2.createVariableDeclaration(
                    factory2.cloneNode(namespaceDeclaration.name),
                    /*exclamationToken*/
                    void 0,
                    /*type*/
                    void 0,
                    factory2.getGeneratedNameForNode(node)
                  ),
                  /*location*/
                  node
                ),
                /*original*/
                node
              )
            ],
            languageVersion >***REMOVED*** 2 /* ES2015 */ ? 2 /* Const */ : 0 /* None */
          )
        )
      );
    }
    statements ***REMOVED*** appendExportsOfImportDeclaration(statements, node);
    return singleOrMany(statements);
  }
  function createRequireCall(importNode) {
    const moduleName ***REMOVED*** getExternalModuleNameLiteral(factory2, importNode, currentSourceFile, host, resolver, compilerOptions);
    const args ***REMOVED*** [];
    if (moduleName) {
      args.push(rewriteModuleSpecifier(moduleName, compilerOptions));
    }
    return factory2.createCallExpression(
      factory2.createIdentifier("require"),
      /*typeArguments*/
      void 0,
      args
    );
  }
  function visitTopLevelImportEqualsDeclaration(node) {
    Debug.assert(isExternalModuleImportEqualsDeclaration(node), "import***REMOVED*** for internal module references should be handled in an earlier transformer.");
    let statements;
    if (moduleKind !***REMOVED******REMOVED*** 2 /* AMD */) {
      if (hasSyntacticModifier(node, 32 /* Export */)) {
        statements ***REMOVED*** append(
          statements,
          setOriginalNode(
            setTextRange(
              factory2.createExpressionStatement(
                createExportExpression(
                  node.name,
                  createRequireCall(node)
                )
              ),
              node
            ),
            node
          )
        );
      } else {
        statements ***REMOVED*** append(
          statements,
          setOriginalNode(
            setTextRange(
              factory2.createVariableStatement(
                /*modifiers*/
                void 0,
                factory2.createVariableDeclarationList(
                  [
                    factory2.createVariableDeclaration(
                      factory2.cloneNode(node.name),
                      /*exclamationToken*/
                      void 0,
                      /*type*/
                      void 0,
                      createRequireCall(node)
                    )
                  ],
                  /*flags*/
                  languageVersion >***REMOVED*** 2 /* ES2015 */ ? 2 /* Const */ : 0 /* None */
                )
              ),
              node
            ),
            node
          )
        );
      }
    } else {
      if (hasSyntacticModifier(node, 32 /* Export */)) {
        statements ***REMOVED*** append(
          statements,
          setOriginalNode(
            setTextRange(
              factory2.createExpressionStatement(
                createExportExpression(factory2.getExportName(node), factory2.getLocalName(node))
              ),
              node
            ),
            node
          )
        );
      }
    }
    statements ***REMOVED*** appendExportsOfImportEqualsDeclaration(statements, node);
    return singleOrMany(statements);
  }
  function visitTopLevelExportDeclaration(node) {
    if (!node.moduleSpecifier) {
      return void 0;
    }
    const generatedName ***REMOVED*** factory2.getGeneratedNameForNode(node);
    if (node.exportClause && isNamedExports(node.exportClause)) {
      const statements ***REMOVED*** [];
      if (moduleKind !***REMOVED******REMOVED*** 2 /* AMD */) {
        statements.push(
          setOriginalNode(
            setTextRange(
              factory2.createVariableStatement(
                /*modifiers*/
                void 0,
                factory2.createVariableDeclarationList([
                  factory2.createVariableDeclaration(
                    generatedName,
                    /*exclamationToken*/
                    void 0,
                    /*type*/
                    void 0,
                    createRequireCall(node)
                  )
                ])
              ),
              /*location*/
              node
            ),
            /* original */
            node
          )
        );
      }
      for (const specifier of node.exportClause.elements) {
        const specifierName ***REMOVED*** specifier.propertyName || specifier.name;
        const exportNeedsImportDefault ***REMOVED*** !!getESModuleInterop(compilerOptions) && !(getInternalEmitFlags(node) & 2 /* NeverApplyImportHelper */) && moduleExportNameIsDefault(specifierName);
        const target ***REMOVED*** exportNeedsImportDefault ? emitHelpers().createImportDefaultHelper(generatedName) : generatedName;
        const exportedValue ***REMOVED*** specifierName.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createElementAccessExpression(target, specifierName) : factory2.createPropertyAccessExpression(target, specifierName);
        statements.push(
          setOriginalNode(
            setTextRange(
              factory2.createExpressionStatement(
                createExportExpression(
                  specifier.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.cloneNode(specifier.name) : factory2.getExportName(specifier),
                  exportedValue,
                  /*location*/
                  void 0,
                  /*liveBinding*/
                  true
                )
              ),
              specifier
            ),
            specifier
          )
        );
      }
      return singleOrMany(statements);
    } else if (node.exportClause) {
      const statements ***REMOVED*** [];
      statements.push(
        setOriginalNode(
          setTextRange(
            factory2.createExpressionStatement(
              createExportExpression(
                factory2.cloneNode(node.exportClause.name),
                getHelperExpressionForExport(
                  node,
                  moduleKind !***REMOVED******REMOVED*** 2 /* AMD */ ? createRequireCall(node) : isExportNamespaceAsDefaultDeclaration(node) ? generatedName : node.exportClause.name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? generatedName : factory2.createIdentifier(idText(node.exportClause.name))
                )
              )
            ),
            node
          ),
          node
        )
      );
      return singleOrMany(statements);
    } else {
      return setOriginalNode(
        setTextRange(
          factory2.createExpressionStatement(
            emitHelpers().createExportStarHelper(moduleKind !***REMOVED******REMOVED*** 2 /* AMD */ ? createRequireCall(node) : generatedName)
          ),
          node
        ),
        node
      );
    }
  }
  function visitTopLevelExportAssignment(node) {
    if (node.isExportEquals) {
      return void 0;
    }
    return createExportStatement(
      factory2.createIdentifier("default"),
      visitNode(node.expression, visitor, isExpression),
      /*location*/
      node,
      /*allowComments*/
      true
    );
  }
  function visitFunctionDeclaration(node) {
    let statements;
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      statements ***REMOVED*** append(
        statements,
        setOriginalNode(
          setTextRange(
            factory2.createFunctionDeclaration(
              visitNodes2(node.modifiers, modifierVisitor, isModifier),
              node.asteriskToken,
              factory2.getDeclarationName(
                node,
                /*allowComments*/
                true,
                /*allowSourceMaps*/
                true
              ),
              /*typeParameters*/
              void 0,
              visitNodes2(node.parameters, visitor, isParameter),
              /*type*/
              void 0,
              visitEachChild(node.body, visitor, context)
            ),
            /*location*/
            node
          ),
          /*original*/
          node
        )
      );
    } else {
      statements ***REMOVED*** append(statements, visitEachChild(node, visitor, context));
    }
    return singleOrMany(statements);
  }
  function visitClassDeclaration(node) {
    let statements;
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      statements ***REMOVED*** append(
        statements,
        setOriginalNode(
          setTextRange(
            factory2.createClassDeclaration(
              visitNodes2(node.modifiers, modifierVisitor, isModifierLike),
              factory2.getDeclarationName(
                node,
                /*allowComments*/
                true,
                /*allowSourceMaps*/
                true
              ),
              /*typeParameters*/
              void 0,
              visitNodes2(node.heritageClauses, visitor, isHeritageClause),
              visitNodes2(node.members, visitor, isClassElement)
            ),
            node
          ),
          node
        )
      );
    } else {
      statements ***REMOVED*** append(statements, visitEachChild(node, visitor, context));
    }
    statements ***REMOVED*** appendExportsOfHoistedDeclaration(statements, node);
    return singleOrMany(statements);
  }
  function visitVariableStatement(node) {
    let statements;
    let variables;
    let expressions;
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      let modifiers;
      let removeCommentsOnExpressions ***REMOVED*** false;
      for (const variable of node.declarationList.declarations) {
        if (isIdentifier(variable.name) && isLocalName(variable.name)) {
          if (!modifiers) {
            modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifier);
          }
          if (variable.initializer) {
            const updatedVariable ***REMOVED*** factory2.updateVariableDeclaration(
              variable,
              variable.name,
              /*exclamationToken*/
              void 0,
              /*type*/
              void 0,
              createExportExpression(
                variable.name,
                visitNode(variable.initializer, visitor, isExpression)
              )
            );
            variables ***REMOVED*** append(variables, updatedVariable);
          } else {
            variables ***REMOVED*** append(variables, variable);
          }
        } else if (variable.initializer) {
          if (!isBindingPattern(variable.name) && (isArrowFunction(variable.initializer) || isFunctionExpression(variable.initializer) || isClassExpression(variable.initializer))) {
            const expression ***REMOVED*** factory2.createAssignment(
              setTextRange(
                factory2.createPropertyAccessExpression(
                  factory2.createIdentifier("exports"),
                  variable.name
                ),
                /*location*/
                variable.name
              ),
              factory2.createIdentifier(getTextOfIdentifierOrLiteral(variable.name))
            );
            const updatedVariable ***REMOVED*** factory2.createVariableDeclaration(
              variable.name,
              variable.exclamationToken,
              variable.type,
              visitNode(variable.initializer, visitor, isExpression)
            );
            variables ***REMOVED*** append(variables, updatedVariable);
            expressions ***REMOVED*** append(expressions, expression);
            removeCommentsOnExpressions ***REMOVED*** true;
          } else {
            expressions ***REMOVED*** append(expressions, transformInitializedVariable(variable));
          }
        }
      }
      if (variables) {
        statements ***REMOVED*** append(statements, factory2.updateVariableStatement(node, modifiers, factory2.updateVariableDeclarationList(node.declarationList, variables)));
      }
      if (expressions) {
        const statement ***REMOVED*** setOriginalNode(setTextRange(factory2.createExpressionStatement(factory2.inlineExpressions(expressions)), node), node);
        if (removeCommentsOnExpressions) {
          removeAllComments(statement);
        }
        statements ***REMOVED*** append(statements, statement);
      }
    } else {
      statements ***REMOVED*** append(statements, visitEachChild(node, visitor, context));
    }
    statements ***REMOVED*** appendExportsOfVariableStatement(statements, node);
    return singleOrMany(statements);
  }
  function createAllExportExpressions(name, value, location) {
    const exportedNames ***REMOVED*** getExports(name);
    if (exportedNames) {
      let expression ***REMOVED*** isExportName(name) ? value : factory2.createAssignment(name, value);
      for (const exportName of exportedNames) {
        setEmitFlags(expression, 8 /* NoSubstitution */);
        expression ***REMOVED*** createExportExpression(
          exportName,
          expression,
          /*location*/
          location
        );
      }
      return expression;
    }
    return factory2.createAssignment(name, value);
  }
  function transformInitializedVariable(node) {
    if (isBindingPattern(node.name)) {
      return flattenDestructuringAssignment(
        visitNode(node, visitor, isInitializedVariable),
        visitor,
        context,
        0 /* All */,
        /*needsValue*/
        false,
        createAllExportExpressions
      );
    } else {
      return factory2.createAssignment(
        setTextRange(
          factory2.createPropertyAccessExpression(
            factory2.createIdentifier("exports"),
            node.name
          ),
          /*location*/
          node.name
        ),
        node.initializer ? visitNode(node.initializer, visitor, isExpression) : factory2.createVoidZero()
      );
    }
  }
  function appendExportsOfImportDeclaration(statements, decl) {
    if (currentModuleInfo.exportEquals) {
      return statements;
    }
    const importClause ***REMOVED*** decl.importClause;
    if (!importClause) {
      return statements;
    }
    const seen ***REMOVED*** new IdentifierNameMap();
    if (importClause.name) {
      statements ***REMOVED*** appendExportsOfDeclaration(statements, seen, importClause);
    }
    const namedBindings ***REMOVED*** importClause.namedBindings;
    if (namedBindings) {
      switch (namedBindings.kind) {
        case 275 /* NamespaceImport */:
          statements ***REMOVED*** appendExportsOfDeclaration(statements, seen, namedBindings);
          break;
        case 276 /* NamedImports */:
          for (const importBinding of namedBindings.elements) {
            statements ***REMOVED*** appendExportsOfDeclaration(
              statements,
              seen,
              importBinding,
              /*liveBinding*/
              true
            );
          }
          break;
      }
    }
    return statements;
  }
  function appendExportsOfImportEqualsDeclaration(statements, decl) {
    if (currentModuleInfo.exportEquals) {
      return statements;
    }
    return appendExportsOfDeclaration(statements, new IdentifierNameMap(), decl);
  }
  function appendExportsOfVariableStatement(statements, node) {
    return appendExportsOfVariableDeclarationList(
      statements,
      node.declarationList,
      /*isForInOrOfInitializer*/
      false
    );
  }
  function appendExportsOfVariableDeclarationList(statements, node, isForInOrOfInitializer) {
    if (currentModuleInfo.exportEquals) {
      return statements;
    }
    for (const decl of node.declarations) {
      statements ***REMOVED*** appendExportsOfBindingElement(statements, decl, isForInOrOfInitializer);
    }
    return statements;
  }
  function appendExportsOfBindingElement(statements, decl, isForInOrOfInitializer) {
    if (currentModuleInfo.exportEquals) {
      return statements;
    }
    if (isBindingPattern(decl.name)) {
      for (const element of decl.name.elements) {
        if (!isOmittedExpression(element)) {
          statements ***REMOVED*** appendExportsOfBindingElement(statements, element, isForInOrOfInitializer);
        }
      }
    } else if (!isGeneratedIdentifier(decl.name) && (!isVariableDeclaration(decl) || decl.initializer || isForInOrOfInitializer)) {
      statements ***REMOVED*** appendExportsOfDeclaration(statements, new IdentifierNameMap(), decl);
    }
    return statements;
  }
  function appendExportsOfHoistedDeclaration(statements, decl) {
    if (currentModuleInfo.exportEquals) {
      return statements;
    }
    const seen ***REMOVED*** new IdentifierNameMap();
    if (hasSyntacticModifier(decl, 32 /* Export */)) {
      const exportName ***REMOVED*** hasSyntacticModifier(decl, 2048 /* Default */) ? factory2.createIdentifier("default") : factory2.getDeclarationName(decl);
      statements ***REMOVED*** appendExportStatement(
        statements,
        seen,
        exportName,
        factory2.getLocalName(decl),
        /*location*/
        decl
      );
    }
    if (decl.name) {
      statements ***REMOVED*** appendExportsOfDeclaration(statements, seen, decl);
    }
    return statements;
  }
  function appendExportsOfDeclaration(statements, seen, decl, liveBinding) {
    const name ***REMOVED*** factory2.getDeclarationName(decl);
    const exportSpecifiers ***REMOVED*** currentModuleInfo.exportSpecifiers.get(name);
    if (exportSpecifiers) {
      for (const exportSpecifier of exportSpecifiers) {
        statements ***REMOVED*** appendExportStatement(
          statements,
          seen,
          exportSpecifier.name,
          name,
          /*location*/
          exportSpecifier.name,
          /*allowComments*/
          void 0,
          liveBinding
        );
      }
    }
    return statements;
  }
  function appendExportStatement(statements, seen, exportName, expression, location, allowComments, liveBinding) {
    if (exportName.kind !***REMOVED******REMOVED*** 11 /* StringLiteral */) {
      if (seen.has(exportName)) {
        return statements;
      }
      seen.set(exportName, true);
    }
    statements ***REMOVED*** append(statements, createExportStatement(exportName, expression, location, allowComments, liveBinding));
    return statements;
  }
  function createUnderscoreUnderscoreESModule() {
    const statement ***REMOVED*** factory2.createExpressionStatement(
      factory2.createCallExpression(
        factory2.createPropertyAccessExpression(factory2.createIdentifier("Object"), "defineProperty"),
        /*typeArguments*/
        void 0,
        [
          factory2.createIdentifier("exports"),
          factory2.createStringLiteral("__esModule"),
          factory2.createObjectLiteralExpression([
            factory2.createPropertyAssignment("value", factory2.createTrue())
          ])
        ]
      )
    );
    setEmitFlags(statement, 2097152 /* CustomPrologue */);
    return statement;
  }
  function createExportStatement(name, value, location, allowComments, liveBinding) {
    const statement ***REMOVED*** setTextRange(factory2.createExpressionStatement(createExportExpression(
      name,
      value,
      /*location*/
      void 0,
      liveBinding
    )), location);
    startOnNewLine(statement);
    if (!allowComments) {
      setEmitFlags(statement, 3072 /* NoComments */);
    }
    return statement;
  }
  function createExportExpression(name, value, location, liveBinding) {
    return setTextRange(
      liveBinding ? factory2.createCallExpression(
        factory2.createPropertyAccessExpression(
          factory2.createIdentifier("Object"),
          "defineProperty"
        ),
        /*typeArguments*/
        void 0,
        [
          factory2.createIdentifier("exports"),
          factory2.createStringLiteralFromNode(name),
          factory2.createObjectLiteralExpression([
            factory2.createPropertyAssignment("enumerable", factory2.createTrue()),
            factory2.createPropertyAssignment(
              "get",
              factory2.createFunctionExpression(
                /*modifiers*/
                void 0,
                /*asteriskToken*/
                void 0,
                /*name*/
                void 0,
                /*typeParameters*/
                void 0,
                /*parameters*/
                [],
                /*type*/
                void 0,
                factory2.createBlock([factory2.createReturnStatement(value)])
              )
            )
          ])
        ]
      ) : factory2.createAssignment(
        name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createElementAccessExpression(
          factory2.createIdentifier("exports"),
          factory2.cloneNode(name)
        ) : factory2.createPropertyAccessExpression(
          factory2.createIdentifier("exports"),
          factory2.cloneNode(name)
        ),
        value
      ),
      location
    );
  }
  function modifierVisitor(node) {
    switch (node.kind) {
      case 95 /* ExportKeyword */:
      case 90 /* DefaultKeyword */:
        return void 0;
    }
    return node;
  }
  function onEmitNode(hint, node, emitCallback) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
      currentSourceFile ***REMOVED*** node;
      currentModuleInfo ***REMOVED*** moduleInfoMap[getOriginalNodeId(currentSourceFile)];
      previousOnEmitNode(hint, node, emitCallback);
      currentSourceFile ***REMOVED*** void 0;
      currentModuleInfo ***REMOVED*** void 0;
    } else {
      previousOnEmitNode(hint, node, emitCallback);
    }
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (node.id && noSubstitution[node.id]) {
      return node;
    }
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    } else if (isShorthandPropertyAssignment(node)) {
      return substituteShorthandPropertyAssignment(node);
    }
    return node;
  }
  function substituteShorthandPropertyAssignment(node) {
    const name ***REMOVED*** node.name;
    const exportedOrImportedName ***REMOVED*** substituteExpressionIdentifier(name);
    if (exportedOrImportedName !***REMOVED******REMOVED*** name) {
      if (node.objectAssignmentInitializer) {
        const initializer ***REMOVED*** factory2.createAssignment(exportedOrImportedName, node.objectAssignmentInitializer);
        return setTextRange(factory2.createPropertyAssignment(name, initializer), node);
      }
      return setTextRange(factory2.createPropertyAssignment(name, exportedOrImportedName), node);
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
      case 214 /* CallExpression */:
        return substituteCallExpression(node);
      case 216 /* TaggedTemplateExpression */:
        return substituteTaggedTemplateExpression(node);
      case 227 /* BinaryExpression */:
        return substituteBinaryExpression(node);
    }
    return node;
  }
  function substituteCallExpression(node) {
    if (isIdentifier(node.expression)) {
      const expression ***REMOVED*** substituteExpressionIdentifier(node.expression);
      noSubstitution[getNodeId(expression)] ***REMOVED*** true;
      if (!isIdentifier(expression) && !(getEmitFlags(node.expression) & 8192 /* HelperName */)) {
        return addInternalEmitFlags(
          factory2.updateCallExpression(
            node,
            expression,
            /*typeArguments*/
            void 0,
            node.arguments
          ),
          16 /* IndirectCall */
        );
      }
    }
    return node;
  }
  function substituteTaggedTemplateExpression(node) {
    if (isIdentifier(node.tag)) {
      const tag ***REMOVED*** substituteExpressionIdentifier(node.tag);
      noSubstitution[getNodeId(tag)] ***REMOVED*** true;
      if (!isIdentifier(tag) && !(getEmitFlags(node.tag) & 8192 /* HelperName */)) {
        return addInternalEmitFlags(
          factory2.updateTaggedTemplateExpression(
            node,
            tag,
            /*typeArguments*/
            void 0,
            node.template
          ),
          16 /* IndirectCall */
        );
      }
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    var _a, _b;
    if (getEmitFlags(node) & 8192 /* HelperName */) {
      const externalHelpersModuleName ***REMOVED*** getExternalHelpersModuleName(currentSourceFile);
      if (externalHelpersModuleName) {
        return factory2.createPropertyAccessExpression(externalHelpersModuleName, node);
      }
      return node;
    } else if (!(isGeneratedIdentifier(node) && !(node.emitNode.autoGenerate.flags & 64 /* AllowNameSubstitution */)) && !isLocalName(node)) {
      const exportContainer ***REMOVED*** resolver.getReferencedExportContainer(node, isExportName(node));
      if (exportContainer && exportContainer.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
        return setTextRange(
          factory2.createPropertyAccessExpression(
            factory2.createIdentifier("exports"),
            factory2.cloneNode(node)
          ),
          /*location*/
          node
        );
      }
      const importDeclaration ***REMOVED*** resolver.getReferencedImportDeclaration(node);
      if (importDeclaration) {
        if (isImportClause(importDeclaration)) {
          return setTextRange(
            factory2.createPropertyAccessExpression(
              factory2.getGeneratedNameForNode(importDeclaration.parent),
              factory2.createIdentifier("default")
            ),
            /*location*/
            node
          );
        } else if (isImportSpecifier(importDeclaration)) {
          const name ***REMOVED*** importDeclaration.propertyName || importDeclaration.name;
          const target ***REMOVED*** factory2.getGeneratedNameForNode(((_b ***REMOVED*** (_a ***REMOVED*** importDeclaration.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.parent) || importDeclaration);
          return setTextRange(
            name.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createElementAccessExpression(target, factory2.cloneNode(name)) : factory2.createPropertyAccessExpression(target, factory2.cloneNode(name)),
            /*location*/
            node
          );
        }
      }
    }
    return node;
  }
  function substituteBinaryExpression(node) {
    if (isAssignmentOperator(node.operatorToken.kind) && isIdentifier(node.left) && (!isGeneratedIdentifier(node.left) || isFileLevelReservedGeneratedIdentifier(node.left)) && !isLocalName(node.left)) {
      const exportedNames ***REMOVED*** getExports(node.left);
      if (exportedNames) {
        let expression ***REMOVED*** node;
        for (const exportName of exportedNames) {
          noSubstitution[getNodeId(expression)] ***REMOVED*** true;
          expression ***REMOVED*** createExportExpression(
            exportName,
            expression,
            /*location*/
            node
          );
        }
        return expression;
      }
    }
    return node;
  }
  function getExports(name) {
    if (!isGeneratedIdentifier(name)) {
      const importDeclaration ***REMOVED*** resolver.getReferencedImportDeclaration(name);
      if (importDeclaration) {
        return currentModuleInfo ***REMOVED******REMOVED*** null ? void 0 : currentModuleInfo.exportedBindings[getOriginalNodeId(importDeclaration)];
      }
      const bindingsSet ***REMOVED*** /* @__PURE__ */ new Set();
      const declarations ***REMOVED*** resolver.getReferencedValueDeclarations(name);
      if (declarations) {
        for (const declaration of declarations) {
          const bindings ***REMOVED*** currentModuleInfo ***REMOVED******REMOVED*** null ? void 0 : currentModuleInfo.exportedBindings[getOriginalNodeId(declaration)];
          if (bindings) {
            for (const binding of bindings) {
              bindingsSet.add(binding);
            }
          }
        }
        if (bindingsSet.size) {
          return arrayFrom(bindingsSet);
        }
      }
    } else if (isFileLevelReservedGeneratedIdentifier(name)) {
      const exportSpecifiers ***REMOVED*** currentModuleInfo ***REMOVED******REMOVED*** null ? void 0 : currentModuleInfo.exportSpecifiers.get(name);
      if (exportSpecifiers) {
        const exportedNames ***REMOVED*** [];
        for (const exportSpecifier of exportSpecifiers) {
          exportedNames.push(exportSpecifier.name);
        }
        return exportedNames;
      }
    }
  }
}
var dynamicImportUMDHelper ***REMOVED*** {
  name: "typescript:dynamicimport-sync-require",
  scoped: true,
  text: `
            var __syncRequire ***REMOVED*** typeof module ***REMOVED******REMOVED******REMOVED*** "object" && typeof module.exports ***REMOVED******REMOVED******REMOVED*** "object";`
};

// src/compiler/transformers/module/system.ts
function transformSystemModule(context) {
  const {
    factory: factory2,
    startLexicalEnvironment,
    endLexicalEnvironment,
    hoistVariableDeclaration
  } ***REMOVED*** context;
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const resolver ***REMOVED*** context.getEmitResolver();
  const host ***REMOVED*** context.getEmitHost();
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.enableSubstitution(80 /* Identifier */);
  context.enableSubstitution(305 /* ShorthandPropertyAssignment */);
  context.enableSubstitution(227 /* BinaryExpression */);
  context.enableSubstitution(237 /* MetaProperty */);
  context.enableEmitNotification(308 /* SourceFile */);
  const moduleInfoMap ***REMOVED*** [];
  const exportFunctionsMap ***REMOVED*** [];
  const noSubstitutionMap ***REMOVED*** [];
  const contextObjectMap ***REMOVED*** [];
  let currentSourceFile;
  let moduleInfo;
  let exportFunction;
  let contextObject;
  let hoistedStatements;
  let enclosingBlockScopedContainer;
  let noSubstitution;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile || !(isEffectiveExternalModule(node, compilerOptions) || node.transformFlags & 8388608 /* ContainsDynamicImport */)) {
      return node;
    }
    const id ***REMOVED*** getOriginalNodeId(node);
    currentSourceFile ***REMOVED*** node;
    enclosingBlockScopedContainer ***REMOVED*** node;
    moduleInfo ***REMOVED*** moduleInfoMap[id] ***REMOVED*** collectExternalModuleInfo(context, node);
    exportFunction ***REMOVED*** factory2.createUniqueName("exports");
    exportFunctionsMap[id] ***REMOVED*** exportFunction;
    contextObject ***REMOVED*** contextObjectMap[id] ***REMOVED*** factory2.createUniqueName("context");
    const dependencyGroups ***REMOVED*** collectDependencyGroups(moduleInfo.externalImports);
    const moduleBodyBlock ***REMOVED*** createSystemModuleBody(node, dependencyGroups);
    const moduleBodyFunction ***REMOVED*** factory2.createFunctionExpression(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      /*name*/
      void 0,
      /*typeParameters*/
      void 0,
      [
        factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          exportFunction
        ),
        factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          contextObject
        )
      ],
      /*type*/
      void 0,
      moduleBodyBlock
    );
    const moduleName ***REMOVED*** tryGetModuleNameFromFile(factory2, node, host, compilerOptions);
    const dependencies ***REMOVED*** factory2.createArrayLiteralExpression(map(dependencyGroups, (dependencyGroup) ***REMOVED***> dependencyGroup.name));
    const updated ***REMOVED*** setEmitFlags(
      factory2.updateSourceFile(
        node,
        setTextRange(
          factory2.createNodeArray([
            factory2.createExpressionStatement(
              factory2.createCallExpression(
                factory2.createPropertyAccessExpression(factory2.createIdentifier("System"), "register"),
                /*typeArguments*/
                void 0,
                moduleName ? [moduleName, dependencies, moduleBodyFunction] : [dependencies, moduleBodyFunction]
              )
            )
          ]),
          node.statements
        )
      ),
      2048 /* NoTrailingComments */
    );
    if (!compilerOptions.outFile) {
      moveEmitHelpers(updated, moduleBodyBlock, (helper) ***REMOVED***> !helper.scoped);
    }
    if (noSubstitution) {
      noSubstitutionMap[id] ***REMOVED*** noSubstitution;
      noSubstitution ***REMOVED*** void 0;
    }
    currentSourceFile ***REMOVED*** void 0;
    moduleInfo ***REMOVED*** void 0;
    exportFunction ***REMOVED*** void 0;
    contextObject ***REMOVED*** void 0;
    hoistedStatements ***REMOVED*** void 0;
    enclosingBlockScopedContainer ***REMOVED*** void 0;
    return updated;
  }
  function collectDependencyGroups(externalImports) {
    const groupIndices ***REMOVED*** /* @__PURE__ */ new Map();
    const dependencyGroups ***REMOVED*** [];
    for (const externalImport of externalImports) {
      const externalModuleName ***REMOVED*** getExternalModuleNameLiteral(factory2, externalImport, currentSourceFile, host, resolver, compilerOptions);
      if (externalModuleName) {
        const text ***REMOVED*** externalModuleName.text;
        const groupIndex ***REMOVED*** groupIndices.get(text);
        if (groupIndex !***REMOVED******REMOVED*** void 0) {
          dependencyGroups[groupIndex].externalImports.push(externalImport);
        } else {
          groupIndices.set(text, dependencyGroups.length);
          dependencyGroups.push({
            name: externalModuleName,
            externalImports: [externalImport]
          });
        }
      }
    }
    return dependencyGroups;
  }
  function createSystemModuleBody(node, dependencyGroups) {
    const statements ***REMOVED*** [];
    startLexicalEnvironment();
    const ensureUseStrict ***REMOVED*** getStrictOptionValue(compilerOptions, "alwaysStrict") || isExternalModule(currentSourceFile);
    const statementOffset ***REMOVED*** factory2.copyPrologue(node.statements, statements, ensureUseStrict, topLevelVisitor);
    statements.push(
      factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList([
          factory2.createVariableDeclaration(
            "__moduleName",
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            factory2.createLogicalAnd(
              contextObject,
              factory2.createPropertyAccessExpression(contextObject, "id")
            )
          )
        ])
      )
    );
    visitNode(moduleInfo.externalHelpersImportDeclaration, topLevelVisitor, isStatement);
    const executeStatements ***REMOVED*** visitNodes2(node.statements, topLevelVisitor, isStatement, statementOffset);
    addRange(statements, hoistedStatements);
    insertStatementsAfterStandardPrologue(statements, endLexicalEnvironment());
    const exportStarFunction ***REMOVED*** addExportStarIfNeeded(statements);
    const modifiers ***REMOVED*** node.transformFlags & 2097152 /* ContainsAwait */ ? factory2.createModifiersFromModifierFlags(1024 /* Async */) : void 0;
    const moduleObject ***REMOVED*** factory2.createObjectLiteralExpression(
      [
        factory2.createPropertyAssignment("setters", createSettersArray(exportStarFunction, dependencyGroups)),
        factory2.createPropertyAssignment(
          "execute",
          factory2.createFunctionExpression(
            modifiers,
            /*asteriskToken*/
            void 0,
            /*name*/
            void 0,
            /*typeParameters*/
            void 0,
            /*parameters*/
            [],
            /*type*/
            void 0,
            factory2.createBlock(
              executeStatements,
              /*multiLine*/
              true
            )
          )
        )
      ],
      /*multiLine*/
      true
    );
    statements.push(factory2.createReturnStatement(moduleObject));
    return factory2.createBlock(
      statements,
      /*multiLine*/
      true
    );
  }
  function addExportStarIfNeeded(statements) {
    if (!moduleInfo.hasExportStarsToExportValues) {
      return;
    }
    if (!some(moduleInfo.exportedNames) && moduleInfo.exportedFunctions.size ***REMOVED******REMOVED******REMOVED*** 0 && moduleInfo.exportSpecifiers.size ***REMOVED******REMOVED******REMOVED*** 0) {
      let hasExportDeclarationWithExportClause ***REMOVED*** false;
      for (const externalImport of moduleInfo.externalImports) {
        if (externalImport.kind ***REMOVED******REMOVED******REMOVED*** 279 /* ExportDeclaration */ && externalImport.exportClause) {
          hasExportDeclarationWithExportClause ***REMOVED*** true;
          break;
        }
      }
      if (!hasExportDeclarationWithExportClause) {
        const exportStarFunction2 ***REMOVED*** createExportStarFunction(
          /*localNames*/
          void 0
        );
        statements.push(exportStarFunction2);
        return exportStarFunction2.name;
      }
    }
    const exportedNames ***REMOVED*** [];
    if (moduleInfo.exportedNames) {
      for (const exportedLocalName of moduleInfo.exportedNames) {
        if (moduleExportNameIsDefault(exportedLocalName)) {
          continue;
        }
        exportedNames.push(
          factory2.createPropertyAssignment(
            factory2.createStringLiteralFromNode(exportedLocalName),
            factory2.createTrue()
          )
        );
      }
    }
    for (const f of moduleInfo.exportedFunctions) {
      if (hasSyntacticModifier(f, 2048 /* Default */)) {
        continue;
      }
      Debug.assert(!!f.name);
      exportedNames.push(
        factory2.createPropertyAssignment(
          factory2.createStringLiteralFromNode(f.name),
          factory2.createTrue()
        )
      );
    }
    const exportedNamesStorageRef ***REMOVED*** factory2.createUniqueName("exportedNames");
    statements.push(
      factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList([
          factory2.createVariableDeclaration(
            exportedNamesStorageRef,
            /*exclamationToken*/
            void 0,
            /*type*/
            void 0,
            factory2.createObjectLiteralExpression(
              exportedNames,
              /*multiLine*/
              true
            )
          )
        ])
      )
    );
    const exportStarFunction ***REMOVED*** createExportStarFunction(exportedNamesStorageRef);
    statements.push(exportStarFunction);
    return exportStarFunction.name;
  }
  function createExportStarFunction(localNames) {
    const exportStarFunction ***REMOVED*** factory2.createUniqueName("exportStar");
    const m ***REMOVED*** factory2.createIdentifier("m");
    const n ***REMOVED*** factory2.createIdentifier("n");
    const exports2 ***REMOVED*** factory2.createIdentifier("exports");
    let condition ***REMOVED*** factory2.createStrictInequality(n, factory2.createStringLiteral("default"));
    if (localNames) {
      condition ***REMOVED*** factory2.createLogicalAnd(
        condition,
        factory2.createLogicalNot(
          factory2.createCallExpression(
            factory2.createPropertyAccessExpression(localNames, "hasOwnProperty"),
            /*typeArguments*/
            void 0,
            [n]
          )
        )
      );
    }
    return factory2.createFunctionDeclaration(
      /*modifiers*/
      void 0,
      /*asteriskToken*/
      void 0,
      exportStarFunction,
      /*typeParameters*/
      void 0,
      [factory2.createParameterDeclaration(
        /*modifiers*/
        void 0,
        /*dotDotDotToken*/
        void 0,
        m
      )],
      /*type*/
      void 0,
      factory2.createBlock(
        [
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList([
              factory2.createVariableDeclaration(
                exports2,
                /*exclamationToken*/
                void 0,
                /*type*/
                void 0,
                factory2.createObjectLiteralExpression([])
              )
            ])
          ),
          factory2.createForInStatement(
            factory2.createVariableDeclarationList([
              factory2.createVariableDeclaration(n)
            ]),
            m,
            factory2.createBlock([
              setEmitFlags(
                factory2.createIfStatement(
                  condition,
                  factory2.createExpressionStatement(
                    factory2.createAssignment(
                      factory2.createElementAccessExpression(exports2, n),
                      factory2.createElementAccessExpression(m, n)
                    )
                  )
                ),
                1 /* SingleLine */
              )
            ])
          ),
          factory2.createExpressionStatement(
            factory2.createCallExpression(
              exportFunction,
              /*typeArguments*/
              void 0,
              [exports2]
            )
          )
        ],
        /*multiLine*/
        true
      )
    );
  }
  function createSettersArray(exportStarFunction, dependencyGroups) {
    const setters ***REMOVED*** [];
    for (const group2 of dependencyGroups) {
      const localName ***REMOVED*** forEach(group2.externalImports, (i) ***REMOVED***> getLocalNameForExternalImport(factory2, i, currentSourceFile));
      const parameterName ***REMOVED*** localName ? factory2.getGeneratedNameForNode(localName) : factory2.createUniqueName("");
      const statements ***REMOVED*** [];
      for (const entry of group2.externalImports) {
        const importVariableName ***REMOVED*** getLocalNameForExternalImport(factory2, entry, currentSourceFile);
        switch (entry.kind) {
          case 273 /* ImportDeclaration */:
            if (!entry.importClause) {
              break;
            }
          // falls through
          case 272 /* ImportEqualsDeclaration */:
            Debug.assert(importVariableName !***REMOVED******REMOVED*** void 0);
            statements.push(
              factory2.createExpressionStatement(
                factory2.createAssignment(importVariableName, parameterName)
              )
            );
            if (hasSyntacticModifier(entry, 32 /* Export */)) {
              statements.push(
                factory2.createExpressionStatement(
                  factory2.createCallExpression(
                    exportFunction,
                    /*typeArguments*/
                    void 0,
                    [
                      factory2.createStringLiteral(idText(importVariableName)),
                      parameterName
                    ]
                  )
                )
              );
            }
            break;
          case 279 /* ExportDeclaration */:
            Debug.assert(importVariableName !***REMOVED******REMOVED*** void 0);
            if (entry.exportClause) {
              if (isNamedExports(entry.exportClause)) {
                const properties ***REMOVED*** [];
                for (const e of entry.exportClause.elements) {
                  properties.push(
                    factory2.createPropertyAssignment(
                      factory2.createStringLiteral(moduleExportNameTextUnescaped(e.name)),
                      factory2.createElementAccessExpression(
                        parameterName,
                        factory2.createStringLiteral(moduleExportNameTextUnescaped(e.propertyName || e.name))
                      )
                    )
                  );
                }
                statements.push(
                  factory2.createExpressionStatement(
                    factory2.createCallExpression(
                      exportFunction,
                      /*typeArguments*/
                      void 0,
                      [factory2.createObjectLiteralExpression(
                        properties,
                        /*multiLine*/
                        true
                      )]
                    )
                  )
                );
              } else {
                statements.push(
                  factory2.createExpressionStatement(
                    factory2.createCallExpression(
                      exportFunction,
                      /*typeArguments*/
                      void 0,
                      [
                        factory2.createStringLiteral(moduleExportNameTextUnescaped(entry.exportClause.name)),
                        parameterName
                      ]
                    )
                  )
                );
              }
            } else {
              statements.push(
                factory2.createExpressionStatement(
                  factory2.createCallExpression(
                    exportStarFunction,
                    /*typeArguments*/
                    void 0,
                    [parameterName]
                  )
                )
              );
            }
            break;
        }
      }
      setters.push(
        factory2.createFunctionExpression(
          /*modifiers*/
          void 0,
          /*asteriskToken*/
          void 0,
          /*name*/
          void 0,
          /*typeParameters*/
          void 0,
          [factory2.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            parameterName
          )],
          /*type*/
          void 0,
          factory2.createBlock(
            statements,
            /*multiLine*/
            true
          )
        )
      );
    }
    return factory2.createArrayLiteralExpression(
      setters,
      /*multiLine*/
      true
    );
  }
  function topLevelVisitor(node) {
    switch (node.kind) {
      case 273 /* ImportDeclaration */:
        return visitImportDeclaration(node);
      case 272 /* ImportEqualsDeclaration */:
        return visitImportEqualsDeclaration(node);
      case 279 /* ExportDeclaration */:
        return visitExportDeclaration(node);
      case 278 /* ExportAssignment */:
        return visitExportAssignment(node);
      default:
        return topLevelNestedVisitor(node);
    }
  }
  function visitImportDeclaration(node) {
    let statements;
    if (node.importClause) {
      hoistVariableDeclaration(getLocalNameForExternalImport(factory2, node, currentSourceFile));
    }
    return singleOrMany(appendExportsOfImportDeclaration(statements, node));
  }
  function visitExportDeclaration(node) {
    Debug.assertIsDefined(node);
    return void 0;
  }
  function visitImportEqualsDeclaration(node) {
    Debug.assert(isExternalModuleImportEqualsDeclaration(node), "import***REMOVED*** for internal module references should be handled in an earlier transformer.");
    let statements;
    hoistVariableDeclaration(getLocalNameForExternalImport(factory2, node, currentSourceFile));
    return singleOrMany(appendExportsOfImportEqualsDeclaration(statements, node));
  }
  function visitExportAssignment(node) {
    if (node.isExportEquals) {
      return void 0;
    }
    const expression ***REMOVED*** visitNode(node.expression, visitor, isExpression);
    return createExportStatement(
      factory2.createIdentifier("default"),
      expression,
      /*allowComments*/
      true
    );
  }
  function visitFunctionDeclaration(node) {
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      hoistedStatements ***REMOVED*** append(
        hoistedStatements,
        factory2.updateFunctionDeclaration(
          node,
          visitNodes2(node.modifiers, modifierVisitor, isModifierLike),
          node.asteriskToken,
          factory2.getDeclarationName(
            node,
            /*allowComments*/
            true,
            /*allowSourceMaps*/
            true
          ),
          /*typeParameters*/
          void 0,
          visitNodes2(node.parameters, visitor, isParameter),
          /*type*/
          void 0,
          visitNode(node.body, visitor, isBlock)
        )
      );
    } else {
      hoistedStatements ***REMOVED*** append(hoistedStatements, visitEachChild(node, visitor, context));
    }
    hoistedStatements ***REMOVED*** appendExportsOfHoistedDeclaration(hoistedStatements, node);
    return void 0;
  }
  function visitClassDeclaration(node) {
    let statements;
    const name ***REMOVED*** factory2.getLocalName(node);
    hoistVariableDeclaration(name);
    statements ***REMOVED*** append(
      statements,
      setTextRange(
        factory2.createExpressionStatement(
          factory2.createAssignment(
            name,
            setTextRange(
              factory2.createClassExpression(
                visitNodes2(node.modifiers, modifierVisitor, isModifierLike),
                node.name,
                /*typeParameters*/
                void 0,
                visitNodes2(node.heritageClauses, visitor, isHeritageClause),
                visitNodes2(node.members, visitor, isClassElement)
              ),
              node
            )
          )
        ),
        node
      )
    );
    statements ***REMOVED*** appendExportsOfHoistedDeclaration(statements, node);
    return singleOrMany(statements);
  }
  function visitVariableStatement(node) {
    if (!shouldHoistVariableDeclarationList(node.declarationList)) {
      return visitNode(node, visitor, isStatement);
    }
    let statements;
    if (isVarUsing(node.declarationList) || isVarAwaitUsing(node.declarationList)) {
      const modifiers ***REMOVED*** visitNodes2(node.modifiers, modifierVisitor, isModifierLike);
      const declarations ***REMOVED*** [];
      for (const variable of node.declarationList.declarations) {
        declarations.push(factory2.updateVariableDeclaration(
          variable,
          factory2.getGeneratedNameForNode(variable.name),
          /*exclamationToken*/
          void 0,
          /*type*/
          void 0,
          transformInitializedVariable(
            variable,
            /*isExportedDeclaration*/
            false
          )
        ));
      }
      const declarationList ***REMOVED*** factory2.updateVariableDeclarationList(
        node.declarationList,
        declarations
      );
      statements ***REMOVED*** append(statements, factory2.updateVariableStatement(node, modifiers, declarationList));
    } else {
      let expressions;
      const isExportedDeclaration ***REMOVED*** hasSyntacticModifier(node, 32 /* Export */);
      for (const variable of node.declarationList.declarations) {
        if (variable.initializer) {
          expressions ***REMOVED*** append(expressions, transformInitializedVariable(variable, isExportedDeclaration));
        } else {
          hoistBindingElement(variable);
        }
      }
      if (expressions) {
        statements ***REMOVED*** append(statements, setTextRange(factory2.createExpressionStatement(factory2.inlineExpressions(expressions)), node));
      }
    }
    statements ***REMOVED*** appendExportsOfVariableStatement(
      statements,
      node,
      /*exportSelf*/
      false
    );
    return singleOrMany(statements);
  }
  function hoistBindingElement(node) {
    if (isBindingPattern(node.name)) {
      for (const element of node.name.elements) {
        if (!isOmittedExpression(element)) {
          hoistBindingElement(element);
        }
      }
    } else {
      hoistVariableDeclaration(factory2.cloneNode(node.name));
    }
  }
  function shouldHoistVariableDeclarationList(node) {
    return (getEmitFlags(node) & 4194304 /* NoHoisting */) ***REMOVED******REMOVED******REMOVED*** 0 && (enclosingBlockScopedContainer.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ || (getOriginalNode(node).flags & 7 /* BlockScoped */) ***REMOVED******REMOVED******REMOVED*** 0);
  }
  function transformInitializedVariable(node, isExportedDeclaration) {
    const createAssignment ***REMOVED*** isExportedDeclaration ? createExportedVariableAssignment : createNonExportedVariableAssignment;
    return isBindingPattern(node.name) ? flattenDestructuringAssignment(
      node,
      visitor,
      context,
      0 /* All */,
      /*needsValue*/
      false,
      createAssignment
    ) : node.initializer ? createAssignment(node.name, visitNode(node.initializer, visitor, isExpression)) : node.name;
  }
  function createExportedVariableAssignment(name, value, location) {
    return createVariableAssignment(
      name,
      value,
      location,
      /*isExportedDeclaration*/
      true
    );
  }
  function createNonExportedVariableAssignment(name, value, location) {
    return createVariableAssignment(
      name,
      value,
      location,
      /*isExportedDeclaration*/
      false
    );
  }
  function createVariableAssignment(name, value, location, isExportedDeclaration) {
    hoistVariableDeclaration(factory2.cloneNode(name));
    return isExportedDeclaration ? createExportExpression(name, preventSubstitution(setTextRange(factory2.createAssignment(name, value), location))) : preventSubstitution(setTextRange(factory2.createAssignment(name, value), location));
  }
  function appendExportsOfImportDeclaration(statements, decl) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    const importClause ***REMOVED*** decl.importClause;
    if (!importClause) {
      return statements;
    }
    if (importClause.name) {
      statements ***REMOVED*** appendExportsOfDeclaration(statements, importClause);
    }
    const namedBindings ***REMOVED*** importClause.namedBindings;
    if (namedBindings) {
      switch (namedBindings.kind) {
        case 275 /* NamespaceImport */:
          statements ***REMOVED*** appendExportsOfDeclaration(statements, namedBindings);
          break;
        case 276 /* NamedImports */:
          for (const importBinding of namedBindings.elements) {
            statements ***REMOVED*** appendExportsOfDeclaration(statements, importBinding);
          }
          break;
      }
    }
    return statements;
  }
  function appendExportsOfImportEqualsDeclaration(statements, decl) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    return appendExportsOfDeclaration(statements, decl);
  }
  function appendExportsOfVariableStatement(statements, node, exportSelf) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    for (const decl of node.declarationList.declarations) {
      if (decl.initializer || exportSelf) {
        statements ***REMOVED*** appendExportsOfBindingElement(statements, decl, exportSelf);
      }
    }
    return statements;
  }
  function appendExportsOfBindingElement(statements, decl, exportSelf) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    if (isBindingPattern(decl.name)) {
      for (const element of decl.name.elements) {
        if (!isOmittedExpression(element)) {
          statements ***REMOVED*** appendExportsOfBindingElement(statements, element, exportSelf);
        }
      }
    } else if (!isGeneratedIdentifier(decl.name)) {
      let excludeName;
      if (exportSelf) {
        statements ***REMOVED*** appendExportStatement(statements, decl.name, factory2.getLocalName(decl));
        excludeName ***REMOVED*** idText(decl.name);
      }
      statements ***REMOVED*** appendExportsOfDeclaration(statements, decl, excludeName);
    }
    return statements;
  }
  function appendExportsOfHoistedDeclaration(statements, decl) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    let excludeName;
    if (hasSyntacticModifier(decl, 32 /* Export */)) {
      const exportName ***REMOVED*** hasSyntacticModifier(decl, 2048 /* Default */) ? factory2.createStringLiteral("default") : decl.name;
      statements ***REMOVED*** appendExportStatement(statements, exportName, factory2.getLocalName(decl));
      excludeName ***REMOVED*** getTextOfIdentifierOrLiteral(exportName);
    }
    if (decl.name) {
      statements ***REMOVED*** appendExportsOfDeclaration(statements, decl, excludeName);
    }
    return statements;
  }
  function appendExportsOfDeclaration(statements, decl, excludeName) {
    if (moduleInfo.exportEquals) {
      return statements;
    }
    const name ***REMOVED*** factory2.getDeclarationName(decl);
    const exportSpecifiers ***REMOVED*** moduleInfo.exportSpecifiers.get(name);
    if (exportSpecifiers) {
      for (const exportSpecifier of exportSpecifiers) {
        if (moduleExportNameTextUnescaped(exportSpecifier.name) !***REMOVED******REMOVED*** excludeName) {
          statements ***REMOVED*** appendExportStatement(statements, exportSpecifier.name, name);
        }
      }
    }
    return statements;
  }
  function appendExportStatement(statements, exportName, expression, allowComments) {
    statements ***REMOVED*** append(statements, createExportStatement(exportName, expression, allowComments));
    return statements;
  }
  function createExportStatement(name, value, allowComments) {
    const statement ***REMOVED*** factory2.createExpressionStatement(createExportExpression(name, value));
    startOnNewLine(statement);
    if (!allowComments) {
      setEmitFlags(statement, 3072 /* NoComments */);
    }
    return statement;
  }
  function createExportExpression(name, value) {
    const exportName ***REMOVED*** isIdentifier(name) ? factory2.createStringLiteralFromNode(name) : name;
    setEmitFlags(value, getEmitFlags(value) | 3072 /* NoComments */);
    return setCommentRange(factory2.createCallExpression(
      exportFunction,
      /*typeArguments*/
      void 0,
      [exportName, value]
    ), value);
  }
  function topLevelNestedVisitor(node) {
    switch (node.kind) {
      case 244 /* VariableStatement */:
        return visitVariableStatement(node);
      case 263 /* FunctionDeclaration */:
        return visitFunctionDeclaration(node);
      case 264 /* ClassDeclaration */:
        return visitClassDeclaration(node);
      case 249 /* ForStatement */:
        return visitForStatement(
          node,
          /*isTopLevel*/
          true
        );
      case 250 /* ForInStatement */:
        return visitForInStatement(node);
      case 251 /* ForOfStatement */:
        return visitForOfStatement(node);
      case 247 /* DoStatement */:
        return visitDoStatement(node);
      case 248 /* WhileStatement */:
        return visitWhileStatement(node);
      case 257 /* LabeledStatement */:
        return visitLabeledStatement(node);
      case 255 /* WithStatement */:
        return visitWithStatement(node);
      case 246 /* IfStatement */:
        return visitIfStatement(node);
      case 256 /* SwitchStatement */:
        return visitSwitchStatement(node);
      case 270 /* CaseBlock */:
        return visitCaseBlock(node);
      case 297 /* CaseClause */:
        return visitCaseClause(node);
      case 298 /* DefaultClause */:
        return visitDefaultClause(node);
      case 259 /* TryStatement */:
        return visitTryStatement(node);
      case 300 /* CatchClause */:
        return visitCatchClause(node);
      case 242 /* Block */:
        return visitBlock(node);
      default:
        return visitor(node);
    }
  }
  function visitForStatement(node, isTopLevel) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** factory2.updateForStatement(
      node,
      visitNode(node.initializer, isTopLevel ? visitForInitializer : discardedValueVisitor, isForInitializer),
      visitNode(node.condition, visitor, isExpression),
      visitNode(node.incrementor, discardedValueVisitor, isExpression),
      visitIterationBody(node.statement, isTopLevel ? topLevelNestedVisitor : visitor, context)
    );
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function visitForInStatement(node) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** factory2.updateForInStatement(
      node,
      visitForInitializer(node.initializer),
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function visitForOfStatement(node) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** factory2.updateForOfStatement(
      node,
      node.awaitModifier,
      visitForInitializer(node.initializer),
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function shouldHoistForInitializer(node) {
    return isVariableDeclarationList(node) && shouldHoistVariableDeclarationList(node);
  }
  function visitForInitializer(node) {
    if (shouldHoistForInitializer(node)) {
      let expressions;
      for (const variable of node.declarations) {
        expressions ***REMOVED*** append(expressions, transformInitializedVariable(
          variable,
          /*isExportedDeclaration*/
          false
        ));
        if (!variable.initializer) {
          hoistBindingElement(variable);
        }
      }
      return expressions ? factory2.inlineExpressions(expressions) : factory2.createOmittedExpression();
    } else {
      return visitNode(node, discardedValueVisitor, isForInitializer);
    }
  }
  function visitDoStatement(node) {
    return factory2.updateDoStatement(
      node,
      visitIterationBody(node.statement, topLevelNestedVisitor, context),
      visitNode(node.expression, visitor, isExpression)
    );
  }
  function visitWhileStatement(node) {
    return factory2.updateWhileStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitIterationBody(node.statement, topLevelNestedVisitor, context)
    );
  }
  function visitLabeledStatement(node) {
    return factory2.updateLabeledStatement(
      node,
      node.label,
      visitNode(node.statement, topLevelNestedVisitor, isStatement, factory2.liftToBlock) ?? factory2.createExpressionStatement(factory2.createIdentifier(""))
    );
  }
  function visitWithStatement(node) {
    return factory2.updateWithStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      Debug.checkDefined(visitNode(node.statement, topLevelNestedVisitor, isStatement, factory2.liftToBlock))
    );
  }
  function visitIfStatement(node) {
    return factory2.updateIfStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitNode(node.thenStatement, topLevelNestedVisitor, isStatement, factory2.liftToBlock) ?? factory2.createBlock([]),
      visitNode(node.elseStatement, topLevelNestedVisitor, isStatement, factory2.liftToBlock)
    );
  }
  function visitSwitchStatement(node) {
    return factory2.updateSwitchStatement(
      node,
      visitNode(node.expression, visitor, isExpression),
      Debug.checkDefined(visitNode(node.caseBlock, topLevelNestedVisitor, isCaseBlock))
    );
  }
  function visitCaseBlock(node) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** factory2.updateCaseBlock(
      node,
      visitNodes2(node.clauses, topLevelNestedVisitor, isCaseOrDefaultClause)
    );
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function visitCaseClause(node) {
    return factory2.updateCaseClause(
      node,
      visitNode(node.expression, visitor, isExpression),
      visitNodes2(node.statements, topLevelNestedVisitor, isStatement)
    );
  }
  function visitDefaultClause(node) {
    return visitEachChild(node, topLevelNestedVisitor, context);
  }
  function visitTryStatement(node) {
    return visitEachChild(node, topLevelNestedVisitor, context);
  }
  function visitCatchClause(node) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** factory2.updateCatchClause(
      node,
      node.variableDeclaration,
      Debug.checkDefined(visitNode(node.block, topLevelNestedVisitor, isBlock))
    );
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function visitBlock(node) {
    const savedEnclosingBlockScopedContainer ***REMOVED*** enclosingBlockScopedContainer;
    enclosingBlockScopedContainer ***REMOVED*** node;
    node ***REMOVED*** visitEachChild(node, topLevelNestedVisitor, context);
    enclosingBlockScopedContainer ***REMOVED*** savedEnclosingBlockScopedContainer;
    return node;
  }
  function visitorWorker(node, valueIsDiscarded) {
    if (!(node.transformFlags & (4096 /* ContainsDestructuringAssignment */ | 8388608 /* ContainsDynamicImport */ | 268435456 /* ContainsUpdateExpressionForIdentifier */))) {
      return node;
    }
    switch (node.kind) {
      case 249 /* ForStatement */:
        return visitForStatement(
          node,
          /*isTopLevel*/
          false
        );
      case 245 /* ExpressionStatement */:
        return visitExpressionStatement(node);
      case 218 /* ParenthesizedExpression */:
        return visitParenthesizedExpression(node, valueIsDiscarded);
      case 356 /* PartiallyEmittedExpression */:
        return visitPartiallyEmittedExpression(node, valueIsDiscarded);
      case 227 /* BinaryExpression */:
        if (isDestructuringAssignment(node)) {
          return visitDestructuringAssignment(node, valueIsDiscarded);
        }
        break;
      case 214 /* CallExpression */:
        if (isImportCall(node)) {
          return visitImportCallExpression(node);
        }
        break;
      case 225 /* PrefixUnaryExpression */:
      case 226 /* PostfixUnaryExpression */:
        return visitPrefixOrPostfixUnaryExpression(node, valueIsDiscarded);
    }
    return visitEachChild(node, visitor, context);
  }
  function visitor(node) {
    return visitorWorker(
      node,
      /*valueIsDiscarded*/
      false
    );
  }
  function discardedValueVisitor(node) {
    return visitorWorker(
      node,
      /*valueIsDiscarded*/
      true
    );
  }
  function visitExpressionStatement(node) {
    return factory2.updateExpressionStatement(node, visitNode(node.expression, discardedValueVisitor, isExpression));
  }
  function visitParenthesizedExpression(node, valueIsDiscarded) {
    return factory2.updateParenthesizedExpression(node, visitNode(node.expression, valueIsDiscarded ? discardedValueVisitor : visitor, isExpression));
  }
  function visitPartiallyEmittedExpression(node, valueIsDiscarded) {
    return factory2.updatePartiallyEmittedExpression(node, visitNode(node.expression, valueIsDiscarded ? discardedValueVisitor : visitor, isExpression));
  }
  function visitImportCallExpression(node) {
    const externalModuleName ***REMOVED*** getExternalModuleNameLiteral(factory2, node, currentSourceFile, host, resolver, compilerOptions);
    const firstArgument ***REMOVED*** visitNode(firstOrUndefined(node.arguments), visitor, isExpression);
    const argument ***REMOVED*** externalModuleName && (!firstArgument || !isStringLiteral(firstArgument) || firstArgument.text !***REMOVED******REMOVED*** externalModuleName.text) ? externalModuleName : firstArgument;
    return factory2.createCallExpression(
      factory2.createPropertyAccessExpression(
        contextObject,
        factory2.createIdentifier("import")
      ),
      /*typeArguments*/
      void 0,
      argument ? [argument] : []
    );
  }
  function visitDestructuringAssignment(node, valueIsDiscarded) {
    if (hasExportedReferenceInDestructuringTarget(node.left)) {
      return flattenDestructuringAssignment(
        node,
        visitor,
        context,
        0 /* All */,
        !valueIsDiscarded
      );
    }
    return visitEachChild(node, visitor, context);
  }
  function hasExportedReferenceInDestructuringTarget(node) {
    if (isAssignmentExpression(
      node,
      /*excludeCompoundAssignment*/
      true
    )) {
      return hasExportedReferenceInDestructuringTarget(node.left);
    } else if (isSpreadElement(node)) {
      return hasExportedReferenceInDestructuringTarget(node.expression);
    } else if (isObjectLiteralExpression(node)) {
      return some(node.properties, hasExportedReferenceInDestructuringTarget);
    } else if (isArrayLiteralExpression(node)) {
      return some(node.elements, hasExportedReferenceInDestructuringTarget);
    } else if (isShorthandPropertyAssignment(node)) {
      return hasExportedReferenceInDestructuringTarget(node.name);
    } else if (isPropertyAssignment(node)) {
      return hasExportedReferenceInDestructuringTarget(node.initializer);
    } else if (isIdentifier(node)) {
      const container ***REMOVED*** resolver.getReferencedExportContainer(node);
      return container !***REMOVED******REMOVED*** void 0 && container.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
    } else {
      return false;
    }
  }
  function visitPrefixOrPostfixUnaryExpression(node, valueIsDiscarded) {
    if ((node.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */ || node.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */) && isIdentifier(node.operand) && !isGeneratedIdentifier(node.operand) && !isLocalName(node.operand) && !isDeclarationNameOfEnumOrNamespace(node.operand)) {
      const exportedNames ***REMOVED*** getExports(node.operand);
      if (exportedNames) {
        let temp;
        let expression ***REMOVED*** visitNode(node.operand, visitor, isExpression);
        if (isPrefixUnaryExpression(node)) {
          expression ***REMOVED*** factory2.updatePrefixUnaryExpression(node, expression);
        } else {
          expression ***REMOVED*** factory2.updatePostfixUnaryExpression(node, expression);
          if (!valueIsDiscarded) {
            temp ***REMOVED*** factory2.createTempVariable(hoistVariableDeclaration);
            expression ***REMOVED*** factory2.createAssignment(temp, expression);
            setTextRange(expression, node);
          }
          expression ***REMOVED*** factory2.createComma(expression, factory2.cloneNode(node.operand));
          setTextRange(expression, node);
        }
        for (const exportName of exportedNames) {
          expression ***REMOVED*** createExportExpression(exportName, preventSubstitution(expression));
        }
        if (temp) {
          expression ***REMOVED*** factory2.createComma(expression, temp);
          setTextRange(expression, node);
        }
        return expression;
      }
    }
    return visitEachChild(node, visitor, context);
  }
  function modifierVisitor(node) {
    switch (node.kind) {
      case 95 /* ExportKeyword */:
      case 90 /* DefaultKeyword */:
        return void 0;
    }
    return node;
  }
  function onEmitNode(hint, node, emitCallback) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
      const id ***REMOVED*** getOriginalNodeId(node);
      currentSourceFile ***REMOVED*** node;
      moduleInfo ***REMOVED*** moduleInfoMap[id];
      exportFunction ***REMOVED*** exportFunctionsMap[id];
      noSubstitution ***REMOVED*** noSubstitutionMap[id];
      contextObject ***REMOVED*** contextObjectMap[id];
      if (noSubstitution) {
        delete noSubstitutionMap[id];
      }
      previousOnEmitNode(hint, node, emitCallback);
      currentSourceFile ***REMOVED*** void 0;
      moduleInfo ***REMOVED*** void 0;
      exportFunction ***REMOVED*** void 0;
      contextObject ***REMOVED*** void 0;
      noSubstitution ***REMOVED*** void 0;
    } else {
      previousOnEmitNode(hint, node, emitCallback);
    }
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (isSubstitutionPrevented(node)) {
      return node;
    }
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      return substituteExpression(node);
    } else if (hint ***REMOVED******REMOVED******REMOVED*** 4 /* Unspecified */) {
      return substituteUnspecified(node);
    }
    return node;
  }
  function substituteUnspecified(node) {
    switch (node.kind) {
      case 305 /* ShorthandPropertyAssignment */:
        return substituteShorthandPropertyAssignment(node);
    }
    return node;
  }
  function substituteShorthandPropertyAssignment(node) {
    var _a, _b;
    const name ***REMOVED*** node.name;
    if (!isGeneratedIdentifier(name) && !isLocalName(name)) {
      const importDeclaration ***REMOVED*** resolver.getReferencedImportDeclaration(name);
      if (importDeclaration) {
        if (isImportClause(importDeclaration)) {
          return setTextRange(
            factory2.createPropertyAssignment(
              factory2.cloneNode(name),
              factory2.createPropertyAccessExpression(
                factory2.getGeneratedNameForNode(importDeclaration.parent),
                factory2.createIdentifier("default")
              )
            ),
            /*location*/
            node
          );
        } else if (isImportSpecifier(importDeclaration)) {
          const importedName ***REMOVED*** importDeclaration.propertyName || importDeclaration.name;
          const target ***REMOVED*** factory2.getGeneratedNameForNode(((_b ***REMOVED*** (_a ***REMOVED*** importDeclaration.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.parent) || importDeclaration);
          return setTextRange(
            factory2.createPropertyAssignment(
              factory2.cloneNode(name),
              importedName.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createElementAccessExpression(target, factory2.cloneNode(importedName)) : factory2.createPropertyAccessExpression(target, factory2.cloneNode(importedName))
            ),
            /*location*/
            node
          );
        }
      }
    }
    return node;
  }
  function substituteExpression(node) {
    switch (node.kind) {
      case 80 /* Identifier */:
        return substituteExpressionIdentifier(node);
      case 227 /* BinaryExpression */:
        return substituteBinaryExpression(node);
      case 237 /* MetaProperty */:
        return substituteMetaProperty(node);
    }
    return node;
  }
  function substituteExpressionIdentifier(node) {
    var _a, _b;
    if (getEmitFlags(node) & 8192 /* HelperName */) {
      const externalHelpersModuleName ***REMOVED*** getExternalHelpersModuleName(currentSourceFile);
      if (externalHelpersModuleName) {
        return factory2.createPropertyAccessExpression(externalHelpersModuleName, node);
      }
      return node;
    }
    if (!isGeneratedIdentifier(node) && !isLocalName(node)) {
      const importDeclaration ***REMOVED*** resolver.getReferencedImportDeclaration(node);
      if (importDeclaration) {
        if (isImportClause(importDeclaration)) {
          return setTextRange(
            factory2.createPropertyAccessExpression(
              factory2.getGeneratedNameForNode(importDeclaration.parent),
              factory2.createIdentifier("default")
            ),
            /*location*/
            node
          );
        } else if (isImportSpecifier(importDeclaration)) {
          const importedName ***REMOVED*** importDeclaration.propertyName || importDeclaration.name;
          const target ***REMOVED*** factory2.getGeneratedNameForNode(((_b ***REMOVED*** (_a ***REMOVED*** importDeclaration.parent) ***REMOVED******REMOVED*** null ? void 0 : _a.parent) ***REMOVED******REMOVED*** null ? void 0 : _b.parent) || importDeclaration);
          return setTextRange(
            importedName.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ ? factory2.createElementAccessExpression(target, factory2.cloneNode(importedName)) : factory2.createPropertyAccessExpression(target, factory2.cloneNode(importedName)),
            /*location*/
            node
          );
        }
      }
    }
    return node;
  }
  function substituteBinaryExpression(node) {
    if (isAssignmentOperator(node.operatorToken.kind) && isIdentifier(node.left) && (!isGeneratedIdentifier(node.left) || isFileLevelReservedGeneratedIdentifier(node.left)) && !isLocalName(node.left)) {
      const exportedNames ***REMOVED*** getExports(node.left);
      if (exportedNames) {
        let expression ***REMOVED*** node;
        for (const exportName of exportedNames) {
          expression ***REMOVED*** createExportExpression(exportName, preventSubstitution(expression));
        }
        return expression;
      }
    }
    return node;
  }
  function substituteMetaProperty(node) {
    if (isImportMeta(node)) {
      return factory2.createPropertyAccessExpression(contextObject, factory2.createIdentifier("meta"));
    }
    return node;
  }
  function getExports(name) {
    let exportedNames;
    const valueDeclaration ***REMOVED*** getReferencedDeclaration(name);
    if (valueDeclaration) {
      const exportContainer ***REMOVED*** resolver.getReferencedExportContainer(
        name,
        /*prefixLocals*/
        false
      );
      if (exportContainer && exportContainer.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */) {
        exportedNames ***REMOVED*** append(exportedNames, factory2.getDeclarationName(valueDeclaration));
      }
      exportedNames ***REMOVED*** addRange(exportedNames, moduleInfo ***REMOVED******REMOVED*** null ? void 0 : moduleInfo.exportedBindings[getOriginalNodeId(valueDeclaration)]);
    } else if (isGeneratedIdentifier(name) && isFileLevelReservedGeneratedIdentifier(name)) {
      const exportSpecifiers ***REMOVED*** moduleInfo ***REMOVED******REMOVED*** null ? void 0 : moduleInfo.exportSpecifiers.get(name);
      if (exportSpecifiers) {
        const exportedNames2 ***REMOVED*** [];
        for (const exportSpecifier of exportSpecifiers) {
          exportedNames2.push(exportSpecifier.name);
        }
        return exportedNames2;
      }
    }
    return exportedNames;
  }
  function getReferencedDeclaration(name) {
    if (!isGeneratedIdentifier(name)) {
      const importDeclaration ***REMOVED*** resolver.getReferencedImportDeclaration(name);
      if (importDeclaration) return importDeclaration;
      const valueDeclaration ***REMOVED*** resolver.getReferencedValueDeclaration(name);
      if (valueDeclaration && (moduleInfo ***REMOVED******REMOVED*** null ? void 0 : moduleInfo.exportedBindings[getOriginalNodeId(valueDeclaration)])) return valueDeclaration;
      const declarations ***REMOVED*** resolver.getReferencedValueDeclarations(name);
      if (declarations) {
        for (const declaration of declarations) {
          if (declaration !***REMOVED******REMOVED*** valueDeclaration && (moduleInfo ***REMOVED******REMOVED*** null ? void 0 : moduleInfo.exportedBindings[getOriginalNodeId(declaration)])) return declaration;
        }
      }
      return valueDeclaration;
    }
  }
  function preventSubstitution(node) {
    if (noSubstitution ***REMOVED******REMOVED******REMOVED*** void 0) noSubstitution ***REMOVED*** [];
    noSubstitution[getNodeId(node)] ***REMOVED*** true;
    return node;
  }
  function isSubstitutionPrevented(node) {
    return noSubstitution && node.id && noSubstitution[node.id];
  }
}

// src/compiler/transformers/module/esnextAnd2015.ts
function transformECMAScriptModule(context) {
  const {
    factory: factory2,
    getEmitHelperFactory: emitHelpers
  } ***REMOVED*** context;
  const host ***REMOVED*** context.getEmitHost();
  const resolver ***REMOVED*** context.getEmitResolver();
  const compilerOptions ***REMOVED*** context.getCompilerOptions();
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  context.enableEmitNotification(308 /* SourceFile */);
  context.enableSubstitution(80 /* Identifier */);
  const noSubstitution ***REMOVED*** /* @__PURE__ */ new Set();
  let importsAndRequiresToRewriteOrShim;
  let helperNameSubstitutions;
  let currentSourceFile;
  let importRequireStatements;
  return chainBundle(context, transformSourceFile);
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    if (isExternalModule(node) || getIsolatedModules(compilerOptions)) {
      currentSourceFile ***REMOVED*** node;
      importRequireStatements ***REMOVED*** void 0;
      if (compilerOptions.rewriteRelativeImportExtensions && (currentSourceFile.flags & 4194304 /* PossiblyContainsDynamicImport */ || isInJSFile(node))) {
        forEachDynamicImportOrRequireCall(
          node,
          /*includeTypeSpaceImports*/
          false,
          /*requireStringLiteralLikeArgument*/
          false,
          (node2) ***REMOVED***> {
            if (!isStringLiteralLike(node2.arguments[0]) || shouldRewriteModuleSpecifier(node2.arguments[0].text, compilerOptions)) {
              importsAndRequiresToRewriteOrShim ***REMOVED*** append(importsAndRequiresToRewriteOrShim, node2);
            }
          }
        );
      }
      let result ***REMOVED*** updateExternalModule(node);
      addEmitHelpers(result, context.readEmitHelpers());
      currentSourceFile ***REMOVED*** void 0;
      if (importRequireStatements) {
        result ***REMOVED*** factory2.updateSourceFile(
          result,
          setTextRange(factory2.createNodeArray(insertStatementsAfterCustomPrologue(result.statements.slice(), importRequireStatements)), result.statements)
        );
      }
      if (!isExternalModule(node) || getEmitModuleKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */ || some(result.statements, isExternalModuleIndicator)) {
        return result;
      }
      return factory2.updateSourceFile(
        result,
        setTextRange(factory2.createNodeArray([...result.statements, createEmptyExports(factory2)]), result.statements)
      );
    }
    return node;
  }
  function updateExternalModule(node) {
    const externalHelpersImportDeclaration ***REMOVED*** createExternalHelpersImportDeclarationIfNeeded(factory2, emitHelpers(), node, compilerOptions);
    if (externalHelpersImportDeclaration) {
      const statements ***REMOVED*** [];
      const statementOffset ***REMOVED*** factory2.copyPrologue(node.statements, statements);
      addRange(statements, visitArray([externalHelpersImportDeclaration], visitor, isStatement));
      addRange(statements, visitNodes2(node.statements, visitor, isStatement, statementOffset));
      return factory2.updateSourceFile(
        node,
        setTextRange(factory2.createNodeArray(statements), node.statements)
      );
    } else {
      return visitEachChild(node, visitor, context);
    }
  }
  function visitor(node) {
    switch (node.kind) {
      case 272 /* ImportEqualsDeclaration */:
        return getEmitModuleKind(compilerOptions) >***REMOVED*** 100 /* Node16 */ ? visitImportEqualsDeclaration(node) : void 0;
      case 278 /* ExportAssignment */:
        return visitExportAssignment(node);
      case 279 /* ExportDeclaration */:
        const exportDecl ***REMOVED*** node;
        return visitExportDeclaration(exportDecl);
      case 273 /* ImportDeclaration */:
        return visitImportDeclaration(node);
      case 214 /* CallExpression */:
        if (node ***REMOVED******REMOVED******REMOVED*** (importsAndRequiresToRewriteOrShim ***REMOVED******REMOVED*** null ? void 0 : importsAndRequiresToRewriteOrShim[0])) {
          return visitImportOrRequireCall(importsAndRequiresToRewriteOrShim.shift());
        }
      // fallthrough
      default:
        if ((importsAndRequiresToRewriteOrShim ***REMOVED******REMOVED*** null ? void 0 : importsAndRequiresToRewriteOrShim.length) && rangeContainsRange(node, importsAndRequiresToRewriteOrShim[0])) {
          return visitEachChild(node, visitor, context);
        }
    }
    return node;
  }
  function visitImportDeclaration(node) {
    if (!compilerOptions.rewriteRelativeImportExtensions) {
      return node;
    }
    const updatedModuleSpecifier ***REMOVED*** rewriteModuleSpecifier(node.moduleSpecifier, compilerOptions);
    if (updatedModuleSpecifier ***REMOVED******REMOVED******REMOVED*** node.moduleSpecifier) {
      return node;
    }
    return factory2.updateImportDeclaration(
      node,
      node.modifiers,
      node.importClause,
      updatedModuleSpecifier,
      node.attributes
    );
  }
  function visitImportOrRequireCall(node) {
    return factory2.updateCallExpression(
      node,
      node.expression,
      node.typeArguments,
      [
        isStringLiteralLike(node.arguments[0]) ? rewriteModuleSpecifier(node.arguments[0], compilerOptions) : emitHelpers().createRewriteRelativeImportExtensionsHelper(node.arguments[0]),
        ...node.arguments.slice(1)
      ]
    );
  }
  function createRequireCall(importNode) {
    const moduleName ***REMOVED*** getExternalModuleNameLiteral(factory2, importNode, Debug.checkDefined(currentSourceFile), host, resolver, compilerOptions);
    const args ***REMOVED*** [];
    if (moduleName) {
      args.push(rewriteModuleSpecifier(moduleName, compilerOptions));
    }
    if (getEmitModuleKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */) {
      return factory2.createCallExpression(
        factory2.createIdentifier("require"),
        /*typeArguments*/
        void 0,
        args
      );
    }
    if (!importRequireStatements) {
      const createRequireName ***REMOVED*** factory2.createUniqueName("_createRequire", 16 /* Optimistic */ | 32 /* FileLevel */);
      const importStatement ***REMOVED*** factory2.createImportDeclaration(
        /*modifiers*/
        void 0,
        factory2.createImportClause(
          /*phaseModifier*/
          void 0,
          /*name*/
          void 0,
          factory2.createNamedImports([
            factory2.createImportSpecifier(
              /*isTypeOnly*/
              false,
              factory2.createIdentifier("createRequire"),
              createRequireName
            )
          ])
        ),
        factory2.createStringLiteral("module"),
        /*attributes*/
        void 0
      );
      const requireHelperName ***REMOVED*** factory2.createUniqueName("__require", 16 /* Optimistic */ | 32 /* FileLevel */);
      const requireStatement ***REMOVED*** factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList(
          [
            factory2.createVariableDeclaration(
              requireHelperName,
              /*exclamationToken*/
              void 0,
              /*type*/
              void 0,
              factory2.createCallExpression(
                factory2.cloneNode(createRequireName),
                /*typeArguments*/
                void 0,
                [
                  factory2.createPropertyAccessExpression(factory2.createMetaProperty(102 /* ImportKeyword */, factory2.createIdentifier("meta")), factory2.createIdentifier("url"))
                ]
              )
            )
          ],
          /*flags*/
          languageVersion >***REMOVED*** 2 /* ES2015 */ ? 2 /* Const */ : 0 /* None */
        )
      );
      importRequireStatements ***REMOVED*** [importStatement, requireStatement];
    }
    const name ***REMOVED*** importRequireStatements[1].declarationList.declarations[0].name;
    Debug.assertNode(name, isIdentifier);
    return factory2.createCallExpression(
      factory2.cloneNode(name),
      /*typeArguments*/
      void 0,
      args
    );
  }
  function visitImportEqualsDeclaration(node) {
    Debug.assert(isExternalModuleImportEqualsDeclaration(node), "import***REMOVED*** for internal module references should be handled in an earlier transformer.");
    let statements;
    statements ***REMOVED*** append(
      statements,
      setOriginalNode(
        setTextRange(
          factory2.createVariableStatement(
            /*modifiers*/
            void 0,
            factory2.createVariableDeclarationList(
              [
                factory2.createVariableDeclaration(
                  factory2.cloneNode(node.name),
                  /*exclamationToken*/
                  void 0,
                  /*type*/
                  void 0,
                  createRequireCall(node)
                )
              ],
              /*flags*/
              languageVersion >***REMOVED*** 2 /* ES2015 */ ? 2 /* Const */ : 0 /* None */
            )
          ),
          node
        ),
        node
      )
    );
    statements ***REMOVED*** appendExportsOfImportEqualsDeclaration(statements, node);
    return singleOrMany(statements);
  }
  function appendExportsOfImportEqualsDeclaration(statements, node) {
    if (hasSyntacticModifier(node, 32 /* Export */)) {
      statements ***REMOVED*** append(
        statements,
        factory2.createExportDeclaration(
          /*modifiers*/
          void 0,
          node.isTypeOnly,
          factory2.createNamedExports([factory2.createExportSpecifier(
            /*isTypeOnly*/
            false,
            /*propertyName*/
            void 0,
            idText(node.name)
          )])
        )
      );
    }
    return statements;
  }
  function visitExportAssignment(node) {
    if (node.isExportEquals) {
      if (getEmitModuleKind(compilerOptions) ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */) {
        const statement ***REMOVED*** setOriginalNode(
          factory2.createExpressionStatement(
            factory2.createAssignment(
              factory2.createPropertyAccessExpression(
                factory2.createIdentifier("module"),
                "exports"
              ),
              node.expression
            )
          ),
          node
        );
        return statement;
      }
      return void 0;
    }
    return node;
  }
  function visitExportDeclaration(node) {
    const updatedModuleSpecifier ***REMOVED*** rewriteModuleSpecifier(node.moduleSpecifier, compilerOptions);
    if (compilerOptions.module !***REMOVED******REMOVED*** void 0 && compilerOptions.module > 5 /* ES2015 */ || !node.exportClause || !isNamespaceExport(node.exportClause) || !node.moduleSpecifier) {
      return !node.moduleSpecifier || updatedModuleSpecifier ***REMOVED******REMOVED******REMOVED*** node.moduleSpecifier ? node : factory2.updateExportDeclaration(
        node,
        node.modifiers,
        node.isTypeOnly,
        node.exportClause,
        updatedModuleSpecifier,
        node.attributes
      );
    }
    const oldIdentifier ***REMOVED*** node.exportClause.name;
    const synthName ***REMOVED*** factory2.getGeneratedNameForNode(oldIdentifier);
    const importDecl ***REMOVED*** factory2.createImportDeclaration(
      /*modifiers*/
      void 0,
      factory2.createImportClause(
        /*phaseModifier*/
        void 0,
        /*name*/
        void 0,
        factory2.createNamespaceImport(
          synthName
        )
      ),
      updatedModuleSpecifier,
      node.attributes
    );
    setOriginalNode(importDecl, node.exportClause);
    const exportDecl ***REMOVED*** isExportNamespaceAsDefaultDeclaration(node) ? factory2.createExportDefault(synthName) : factory2.createExportDeclaration(
      /*modifiers*/
      void 0,
      /*isTypeOnly*/
      false,
      factory2.createNamedExports([factory2.createExportSpecifier(
        /*isTypeOnly*/
        false,
        synthName,
        oldIdentifier
      )])
    );
    setOriginalNode(exportDecl, node);
    return [importDecl, exportDecl];
  }
  function onEmitNode(hint, node, emitCallback) {
    if (isSourceFile(node)) {
      if ((isExternalModule(node) || getIsolatedModules(compilerOptions)) && compilerOptions.importHelpers) {
        helperNameSubstitutions ***REMOVED*** /* @__PURE__ */ new Map();
      }
      currentSourceFile ***REMOVED*** node;
      previousOnEmitNode(hint, node, emitCallback);
      currentSourceFile ***REMOVED*** void 0;
      helperNameSubstitutions ***REMOVED*** void 0;
    } else {
      previousOnEmitNode(hint, node, emitCallback);
    }
  }
  function onSubstituteNode(hint, node) {
    node ***REMOVED*** previousOnSubstituteNode(hint, node);
    if (node.id && noSubstitution.has(node.id)) {
      return node;
    }
    if (isIdentifier(node) && getEmitFlags(node) & 8192 /* HelperName */) {
      return substituteHelperName(node);
    }
    return node;
  }
  function substituteHelperName(node) {
    const externalHelpersModuleName ***REMOVED*** currentSourceFile && getExternalHelpersModuleName(currentSourceFile);
    if (externalHelpersModuleName) {
      noSubstitution.add(getNodeId(node));
      return factory2.createPropertyAccessExpression(externalHelpersModuleName, node);
    }
    if (helperNameSubstitutions) {
      const name ***REMOVED*** idText(node);
      let substitution ***REMOVED*** helperNameSubstitutions.get(name);
      if (!substitution) {
        helperNameSubstitutions.set(name, substitution ***REMOVED*** factory2.createUniqueName(name, 16 /* Optimistic */ | 32 /* FileLevel */));
      }
      return substitution;
    }
    return node;
  }
}

// src/compiler/transformers/module/impliedNodeFormatDependent.ts
function transformImpliedNodeFormatDependentModule(context) {
  const previousOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const previousOnEmitNode ***REMOVED*** context.onEmitNode;
  const esmTransform ***REMOVED*** transformECMAScriptModule(context);
  const esmOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const esmOnEmitNode ***REMOVED*** context.onEmitNode;
  context.onSubstituteNode ***REMOVED*** previousOnSubstituteNode;
  context.onEmitNode ***REMOVED*** previousOnEmitNode;
  const cjsTransform ***REMOVED*** transformModule(context);
  const cjsOnSubstituteNode ***REMOVED*** context.onSubstituteNode;
  const cjsOnEmitNode ***REMOVED*** context.onEmitNode;
  const getEmitModuleFormatOfFile ***REMOVED*** (file) ***REMOVED***> context.getEmitHost().getEmitModuleFormatOfFile(file);
  context.onSubstituteNode ***REMOVED*** onSubstituteNode;
  context.onEmitNode ***REMOVED*** onEmitNode;
  context.enableSubstitution(308 /* SourceFile */);
  context.enableEmitNotification(308 /* SourceFile */);
  let currentSourceFile;
  return transformSourceFileOrBundle;
  function onSubstituteNode(hint, node) {
    if (isSourceFile(node)) {
      currentSourceFile ***REMOVED*** node;
      return previousOnSubstituteNode(hint, node);
    } else {
      if (!currentSourceFile) {
        return previousOnSubstituteNode(hint, node);
      }
      if (getEmitModuleFormatOfFile(currentSourceFile) >***REMOVED*** 5 /* ES2015 */) {
        return esmOnSubstituteNode(hint, node);
      }
      return cjsOnSubstituteNode(hint, node);
    }
  }
  function onEmitNode(hint, node, emitCallback) {
    if (isSourceFile(node)) {
      currentSourceFile ***REMOVED*** node;
    }
    if (!currentSourceFile) {
      return previousOnEmitNode(hint, node, emitCallback);
    }
    if (getEmitModuleFormatOfFile(currentSourceFile) >***REMOVED*** 5 /* ES2015 */) {
      return esmOnEmitNode(hint, node, emitCallback);
    }
    return cjsOnEmitNode(hint, node, emitCallback);
  }
  function getModuleTransformForFile(file) {
    return getEmitModuleFormatOfFile(file) >***REMOVED*** 5 /* ES2015 */ ? esmTransform : cjsTransform;
  }
  function transformSourceFile(node) {
    if (node.isDeclarationFile) {
      return node;
    }
    currentSourceFile ***REMOVED*** node;
    const result ***REMOVED*** getModuleTransformForFile(node)(node);
    currentSourceFile ***REMOVED*** void 0;
    Debug.assert(isSourceFile(result));
    return result;
  }
  function transformSourceFileOrBundle(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? transformSourceFile(node) : transformBundle(node);
  }
  function transformBundle(node) {
    return context.factory.createBundle(map(node.sourceFiles, transformSourceFile));
  }
}

// src/compiler/transformers/declarations/diagnostics.ts
function canProduceDiagnostics(node) {
  return isVariableDeclaration(node) || isPropertyDeclaration(node) || isPropertySignature(node) || isBindingElement(node) || isSetAccessor(node) || isGetAccessor(node) || isConstructSignatureDeclaration(node) || isCallSignatureDeclaration(node) || isMethodDeclaration(node) || isMethodSignature(node) || isFunctionDeclaration(node) || isParameter(node) || isTypeParameterDeclaration(node) || isExpressionWithTypeArguments(node) || isImportEqualsDeclaration(node) || isTypeAliasDeclaration(node) || isConstructorDeclaration(node) || isIndexSignatureDeclaration(node) || isPropertyAccessExpression(node) || isElementAccessExpression(node) || isBinaryExpression(node) || isJSDocTypeAlias(node);
}
function createGetSymbolAccessibilityDiagnosticForNodeName(node) {
  if (isSetAccessor(node) || isGetAccessor(node)) {
    return getAccessorNameVisibilityError;
  } else if (isMethodSignature(node) || isMethodDeclaration(node)) {
    return getMethodNameVisibilityError;
  } else {
    return createGetSymbolAccessibilityDiagnosticForNode(node);
  }
  function getAccessorNameVisibilityError(symbolAccessibilityResult) {
    const diagnosticMessage ***REMOVED*** getAccessorNameVisibilityDiagnosticMessage(symbolAccessibilityResult);
    return diagnosticMessage !***REMOVED******REMOVED*** void 0 ? {
      diagnosticMessage,
      errorNode: node,
      typeName: node.name
    } : void 0;
  }
  function getAccessorNameVisibilityDiagnosticMessage(symbolAccessibilityResult) {
    if (isStatic(node)) {
      return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_private_name_1;
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
      return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_property_0_of_exported_class_has_or_is_using_private_name_1;
    } else {
      return symbolAccessibilityResult.errorModuleName ? Diagnostics.Property_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Property_0_of_exported_interface_has_or_is_using_private_name_1;
    }
  }
  function getMethodNameVisibilityError(symbolAccessibilityResult) {
    const diagnosticMessage ***REMOVED*** getMethodNameVisibilityDiagnosticMessage(symbolAccessibilityResult);
    return diagnosticMessage !***REMOVED******REMOVED*** void 0 ? {
      diagnosticMessage,
      errorNode: node,
      typeName: node.name
    } : void 0;
  }
  function getMethodNameVisibilityDiagnosticMessage(symbolAccessibilityResult) {
    if (isStatic(node)) {
      return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_static_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_static_method_0_of_exported_class_has_or_is_using_private_name_1;
    } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
      return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_method_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_method_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_method_0_of_exported_class_has_or_is_using_private_name_1;
    } else {
      return symbolAccessibilityResult.errorModuleName ? Diagnostics.Method_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Method_0_of_exported_interface_has_or_is_using_private_name_1;
    }
  }
}
function createGetSymbolAccessibilityDiagnosticForNode(node) {
  if (isVariableDeclaration(node) || isPropertyDeclaration(node) || isPropertySignature(node) || isPropertyAccessExpression(node) || isElementAccessExpression(node) || isBinaryExpression(node) || isBindingElement(node) || isConstructorDeclaration(node)) {
    return getVariableDeclarationTypeVisibilityError;
  } else if (isSetAccessor(node) || isGetAccessor(node)) {
    return getAccessorDeclarationTypeVisibilityError;
  } else if (isConstructSignatureDeclaration(node) || isCallSignatureDeclaration(node) || isMethodDeclaration(node) || isMethodSignature(node) || isFunctionDeclaration(node) || isIndexSignatureDeclaration(node)) {
    return getReturnTypeVisibilityError;
  } else if (isParameter(node)) {
    if (isParameterPropertyDeclaration(node, node.parent) && hasSyntacticModifier(node.parent, 2 /* Private */)) {
      return getVariableDeclarationTypeVisibilityError;
    }
    return getParameterDeclarationTypeVisibilityError;
  } else if (isTypeParameterDeclaration(node)) {
    return getTypeParameterConstraintVisibilityError;
  } else if (isExpressionWithTypeArguments(node)) {
    return getHeritageClauseVisibilityError;
  } else if (isImportEqualsDeclaration(node)) {
    return getImportEntityNameVisibilityError;
  } else if (isTypeAliasDeclaration(node) || isJSDocTypeAlias(node)) {
    return getTypeAliasDeclarationVisibilityError;
  } else {
    return Debug.assertNever(node, `Attempted to set a declaration diagnostic context for unhandled node kind: ${Debug.formatSyntaxKind(node.kind)}`);
  }
  function getVariableDeclarationTypeVisibilityDiagnosticMessage(symbolAccessibilityResult) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 261 /* VariableDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 209 /* BindingElement */) {
      return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Exported_variable_0_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Exported_variable_0_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Exported_variable_0_has_or_is_using_private_name_1;
    } else if (node.kind ***REMOVED******REMOVED******REMOVED*** 173 /* PropertyDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 212 /* PropertyAccessExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 213 /* ElementAccessExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 227 /* BinaryExpression */ || node.kind ***REMOVED******REMOVED******REMOVED*** 172 /* PropertySignature */ || node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */ && hasSyntacticModifier(node.parent, 2 /* Private */)) {
      if (isStatic(node)) {
        return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_static_property_0_of_exported_class_has_or_is_using_private_name_1;
      } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */ || node.kind ***REMOVED******REMOVED******REMOVED*** 170 /* Parameter */) {
        return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Public_property_0_of_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Public_property_0_of_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Public_property_0_of_exported_class_has_or_is_using_private_name_1;
      } else {
        return symbolAccessibilityResult.errorModuleName ? Diagnostics.Property_0_of_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Property_0_of_exported_interface_has_or_is_using_private_name_1;
      }
    }
  }
  function getVariableDeclarationTypeVisibilityError(symbolAccessibilityResult) {
    const diagnosticMessage ***REMOVED*** getVariableDeclarationTypeVisibilityDiagnosticMessage(symbolAccessibilityResult);
    return diagnosticMessage !***REMOVED******REMOVED*** void 0 ? {
      diagnosticMessage,
      errorNode: node,
      typeName: node.name
    } : void 0;
  }
  function getAccessorDeclarationTypeVisibilityError(symbolAccessibilityResult) {
    let diagnosticMessage;
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
      if (isStatic(node)) {
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_type_of_public_static_setter_0_from_exported_class_has_or_is_using_private_name_1;
      } else {
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_type_of_public_setter_0_from_exported_class_has_or_is_using_private_name_1;
      }
    } else {
      if (isStatic(node)) {
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Return_type_of_public_static_getter_0_from_exported_class_has_or_is_using_private_name_1;
      } else {
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Return_type_of_public_getter_0_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Return_type_of_public_getter_0_from_exported_class_has_or_is_using_private_name_1;
      }
    }
    return {
      diagnosticMessage,
      errorNode: node.name,
      typeName: node.name
    };
  }
  function getReturnTypeVisibilityError(symbolAccessibilityResult) {
    let diagnosticMessage;
    switch (node.kind) {
      case 181 /* ConstructSignature */:
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_0;
        break;
      case 180 /* CallSignature */:
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Return_type_of_call_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_call_signature_from_exported_interface_has_or_is_using_private_name_0;
        break;
      case 182 /* IndexSignature */:
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Return_type_of_index_signature_from_exported_interface_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_index_signature_from_exported_interface_has_or_is_using_private_name_0;
        break;
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        if (isStatic(node)) {
          diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named : Diagnostics.Return_type_of_public_static_method_from_exported_class_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_public_static_method_from_exported_class_has_or_is_using_private_name_0;
        } else if (node.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
          diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named : Diagnostics.Return_type_of_public_method_from_exported_class_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_public_method_from_exported_class_has_or_is_using_private_name_0;
        } else {
          diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? Diagnostics.Return_type_of_method_from_exported_interface_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_method_from_exported_interface_has_or_is_using_private_name_0;
        }
        break;
      case 263 /* FunctionDeclaration */:
        diagnosticMessage ***REMOVED*** symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Return_type_of_exported_function_has_or_is_using_name_0_from_external_module_1_but_cannot_be_named : Diagnostics.Return_type_of_exported_function_has_or_is_using_name_0_from_private_module_1 : Diagnostics.Return_type_of_exported_function_has_or_is_using_private_name_0;
        break;
      default:
        return Debug.fail("This is unknown kind for signature: " + node.kind);
    }
    return {
      diagnosticMessage,
      errorNode: node.name || node
    };
  }
  function getParameterDeclarationTypeVisibilityError(symbolAccessibilityResult) {
    const diagnosticMessage ***REMOVED*** getParameterDeclarationTypeVisibilityDiagnosticMessage(symbolAccessibilityResult);
    return diagnosticMessage !***REMOVED******REMOVED*** void 0 ? {
      diagnosticMessage,
      errorNode: node,
      typeName: node.name
    } : void 0;
  }
  function getParameterDeclarationTypeVisibilityDiagnosticMessage(symbolAccessibilityResult) {
    switch (node.parent.kind) {
      case 177 /* Constructor */:
        return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Parameter_0_of_constructor_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_constructor_from_exported_class_has_or_is_using_private_name_1;
      case 181 /* ConstructSignature */:
      case 186 /* ConstructorType */:
        return symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1;
      case 180 /* CallSignature */:
        return symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1;
      case 182 /* IndexSignature */:
        return symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_index_signature_from_exported_interface_has_or_is_using_private_name_1;
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        if (isStatic(node.parent)) {
          return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1;
        } else if (node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
          return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Parameter_0_of_public_method_from_exported_class_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1;
        } else {
          return symbolAccessibilityResult.errorModuleName ? Diagnostics.Parameter_0_of_method_from_exported_interface_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1;
        }
      case 263 /* FunctionDeclaration */:
      case 185 /* FunctionType */:
        return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Parameter_0_of_exported_function_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Parameter_0_of_exported_function_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_exported_function_has_or_is_using_private_name_1;
      case 179 /* SetAccessor */:
      case 178 /* GetAccessor */:
        return symbolAccessibilityResult.errorModuleName ? symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 2 /* CannotBeNamed */ ? Diagnostics.Parameter_0_of_accessor_has_or_is_using_name_1_from_external_module_2_but_cannot_be_named : Diagnostics.Parameter_0_of_accessor_has_or_is_using_name_1_from_private_module_2 : Diagnostics.Parameter_0_of_accessor_has_or_is_using_private_name_1;
      default:
        return Debug.fail(`Unknown parent for parameter: ${Debug.formatSyntaxKind(node.parent.kind)}`);
    }
  }
  function getTypeParameterConstraintVisibilityError() {
    let diagnosticMessage;
    switch (node.parent.kind) {
      case 264 /* ClassDeclaration */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_exported_class_has_or_is_using_private_name_1;
        break;
      case 265 /* InterfaceDeclaration */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_exported_interface_has_or_is_using_private_name_1;
        break;
      case 201 /* MappedType */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_exported_mapped_object_type_is_using_private_name_1;
        break;
      case 186 /* ConstructorType */:
      case 181 /* ConstructSignature */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_constructor_signature_from_exported_interface_has_or_is_using_private_name_1;
        break;
      case 180 /* CallSignature */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_call_signature_from_exported_interface_has_or_is_using_private_name_1;
        break;
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
        if (isStatic(node.parent)) {
          diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_public_static_method_from_exported_class_has_or_is_using_private_name_1;
        } else if (node.parent.parent.kind ***REMOVED******REMOVED******REMOVED*** 264 /* ClassDeclaration */) {
          diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_public_method_from_exported_class_has_or_is_using_private_name_1;
        } else {
          diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_method_from_exported_interface_has_or_is_using_private_name_1;
        }
        break;
      case 185 /* FunctionType */:
      case 263 /* FunctionDeclaration */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_exported_function_has_or_is_using_private_name_1;
        break;
      case 196 /* InferType */:
        diagnosticMessage ***REMOVED*** Diagnostics.Extends_clause_for_inferred_type_0_has_or_is_using_private_name_1;
        break;
      case 266 /* TypeAliasDeclaration */:
        diagnosticMessage ***REMOVED*** Diagnostics.Type_parameter_0_of_exported_type_alias_has_or_is_using_private_name_1;
        break;
      default:
        return Debug.fail("This is unknown parent for type parameter: " + node.parent.kind);
    }
    return {
      diagnosticMessage,
      errorNode: node,
      typeName: node.name
    };
  }
  function getHeritageClauseVisibilityError() {
    let diagnosticMessage;
    if (isClassDeclaration(node.parent.parent)) {
      diagnosticMessage ***REMOVED*** isHeritageClause(node.parent) && node.parent.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */ ? Diagnostics.Implements_clause_of_exported_class_0_has_or_is_using_private_name_1 : node.parent.parent.name ? Diagnostics.extends_clause_of_exported_class_0_has_or_is_using_private_name_1 : Diagnostics.extends_clause_of_exported_class_has_or_is_using_private_name_0;
    } else {
      diagnosticMessage ***REMOVED*** Diagnostics.extends_clause_of_exported_interface_0_has_or_is_using_private_name_1;
    }
    return {
      diagnosticMessage,
      errorNode: node,
      typeName: getNameOfDeclaration(node.parent.parent)
    };
  }
  function getImportEntityNameVisibilityError() {
    return {
      diagnosticMessage: Diagnostics.Import_declaration_0_is_using_private_name_1,
      errorNode: node,
      typeName: node.name
    };
  }
  function getTypeAliasDeclarationVisibilityError(symbolAccessibilityResult) {
    return {
      diagnosticMessage: symbolAccessibilityResult.errorModuleName ? Diagnostics.Exported_type_alias_0_has_or_is_using_private_name_1_from_module_2 : Diagnostics.Exported_type_alias_0_has_or_is_using_private_name_1,
      errorNode: isJSDocTypeAlias(node) ? Debug.checkDefined(node.typeExpression) : node.type,
      typeName: isJSDocTypeAlias(node) ? getNameOfDeclaration(node) : node.name
    };
  }
}
function createGetIsolatedDeclarationErrors(resolver) {
  const relatedSuggestionByDeclarationKind ***REMOVED*** {
    [220 /* ArrowFunction */]: Diagnostics.Add_a_return_type_to_the_function_expression,
    [219 /* FunctionExpression */]: Diagnostics.Add_a_return_type_to_the_function_expression,
    [175 /* MethodDeclaration */]: Diagnostics.Add_a_return_type_to_the_method,
    [178 /* GetAccessor */]: Diagnostics.Add_a_return_type_to_the_get_accessor_declaration,
    [179 /* SetAccessor */]: Diagnostics.Add_a_type_to_parameter_of_the_set_accessor_declaration,
    [263 /* FunctionDeclaration */]: Diagnostics.Add_a_return_type_to_the_function_declaration,
    [181 /* ConstructSignature */]: Diagnostics.Add_a_return_type_to_the_function_declaration,
    [170 /* Parameter */]: Diagnostics.Add_a_type_annotation_to_the_parameter_0,
    [261 /* VariableDeclaration */]: Diagnostics.Add_a_type_annotation_to_the_variable_0,
    [173 /* PropertyDeclaration */]: Diagnostics.Add_a_type_annotation_to_the_property_0,
    [172 /* PropertySignature */]: Diagnostics.Add_a_type_annotation_to_the_property_0,
    [278 /* ExportAssignment */]: Diagnostics.Move_the_expression_in_default_export_to_a_variable_and_add_a_type_annotation_to_it
  };
  const errorByDeclarationKind ***REMOVED*** {
    [219 /* FunctionExpression */]: Diagnostics.Function_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations,
    [263 /* FunctionDeclaration */]: Diagnostics.Function_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations,
    [220 /* ArrowFunction */]: Diagnostics.Function_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations,
    [175 /* MethodDeclaration */]: Diagnostics.Method_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations,
    [181 /* ConstructSignature */]: Diagnostics.Method_must_have_an_explicit_return_type_annotation_with_isolatedDeclarations,
    [178 /* GetAccessor */]: Diagnostics.At_least_one_accessor_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [179 /* SetAccessor */]: Diagnostics.At_least_one_accessor_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [170 /* Parameter */]: Diagnostics.Parameter_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [261 /* VariableDeclaration */]: Diagnostics.Variable_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [173 /* PropertyDeclaration */]: Diagnostics.Property_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [172 /* PropertySignature */]: Diagnostics.Property_must_have_an_explicit_type_annotation_with_isolatedDeclarations,
    [168 /* ComputedPropertyName */]: Diagnostics.Computed_property_names_on_class_or_object_literals_cannot_be_inferred_with_isolatedDeclarations,
    [306 /* SpreadAssignment */]: Diagnostics.Objects_that_contain_spread_assignments_can_t_be_inferred_with_isolatedDeclarations,
    [305 /* ShorthandPropertyAssignment */]: Diagnostics.Objects_that_contain_shorthand_properties_can_t_be_inferred_with_isolatedDeclarations,
    [210 /* ArrayLiteralExpression */]: Diagnostics.Only_const_arrays_can_be_inferred_with_isolatedDeclarations,
    [278 /* ExportAssignment */]: Diagnostics.Default_exports_can_t_be_inferred_with_isolatedDeclarations,
    [231 /* SpreadElement */]: Diagnostics.Arrays_with_spread_elements_can_t_inferred_with_isolatedDeclarations
  };
  return getDiagnostic;
  function getDiagnostic(node) {
    const heritageClause ***REMOVED*** findAncestor(node, isHeritageClause);
    if (heritageClause) {
      return createDiagnosticForNode(node, Diagnostics.Extends_clause_can_t_contain_an_expression_with_isolatedDeclarations);
    }
    if ((isPartOfTypeNode(node) || isTypeQueryNode(node.parent)) && (isEntityName(node) || isEntityNameExpression(node))) {
      return createEntityInTypeNodeError(node);
    }
    Debug.type(node);
    switch (node.kind) {
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return createAccessorTypeError(node);
      case 168 /* ComputedPropertyName */:
      case 305 /* ShorthandPropertyAssignment */:
      case 306 /* SpreadAssignment */:
        return createObjectLiteralError(node);
      case 210 /* ArrayLiteralExpression */:
      case 231 /* SpreadElement */:
        return createArrayLiteralError(node);
      case 175 /* MethodDeclaration */:
      case 181 /* ConstructSignature */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 263 /* FunctionDeclaration */:
        return createReturnTypeError(node);
      case 209 /* BindingElement */:
        return createBindingElementError(node);
      case 173 /* PropertyDeclaration */:
      case 261 /* VariableDeclaration */:
        return createVariableOrPropertyError(node);
      case 170 /* Parameter */:
        return createParameterError(node);
      case 304 /* PropertyAssignment */:
        return createExpressionError(node.initializer);
      case 232 /* ClassExpression */:
        return createClassExpressionError(node);
      default:
        assertType(node);
        return createExpressionError(node);
    }
  }
  function findNearestDeclaration(node) {
    const result ***REMOVED*** findAncestor(node, (n) ***REMOVED***> isExportAssignment(n) || isStatement(n) || isVariableDeclaration(n) || isPropertyDeclaration(n) || isParameter(n));
    if (!result) return void 0;
    if (isExportAssignment(result)) return result;
    if (isReturnStatement(result)) {
      return findAncestor(result, (n) ***REMOVED***> isFunctionLikeDeclaration(n) && !isConstructorDeclaration(n));
    }
    return isStatement(result) ? void 0 : result;
  }
  function createAccessorTypeError(node) {
    const { getAccessor, setAccessor } ***REMOVED*** getAllAccessorDeclarations(node.symbol.declarations, node);
    const targetNode ***REMOVED*** (isSetAccessor(node) ? node.parameters[0] : node) ?? node;
    const diag2 ***REMOVED*** createDiagnosticForNode(targetNode, errorByDeclarationKind[node.kind]);
    if (setAccessor) {
      addRelatedInfo(diag2, createDiagnosticForNode(setAccessor, relatedSuggestionByDeclarationKind[setAccessor.kind]));
    }
    if (getAccessor) {
      addRelatedInfo(diag2, createDiagnosticForNode(getAccessor, relatedSuggestionByDeclarationKind[getAccessor.kind]));
    }
    return diag2;
  }
  function addParentDeclarationRelatedInfo(node, diag2) {
    const parentDeclaration ***REMOVED*** findNearestDeclaration(node);
    if (parentDeclaration) {
      const targetStr ***REMOVED*** isExportAssignment(parentDeclaration) || !parentDeclaration.name ? "" : getTextOfNode(
        parentDeclaration.name,
        /*includeTrivia*/
        false
      );
      addRelatedInfo(diag2, createDiagnosticForNode(parentDeclaration, relatedSuggestionByDeclarationKind[parentDeclaration.kind], targetStr));
    }
    return diag2;
  }
  function createObjectLiteralError(node) {
    const diag2 ***REMOVED*** createDiagnosticForNode(node, errorByDeclarationKind[node.kind]);
    addParentDeclarationRelatedInfo(node, diag2);
    return diag2;
  }
  function createArrayLiteralError(node) {
    const diag2 ***REMOVED*** createDiagnosticForNode(node, errorByDeclarationKind[node.kind]);
    addParentDeclarationRelatedInfo(node, diag2);
    return diag2;
  }
  function createReturnTypeError(node) {
    const diag2 ***REMOVED*** createDiagnosticForNode(node, errorByDeclarationKind[node.kind]);
    addParentDeclarationRelatedInfo(node, diag2);
    addRelatedInfo(diag2, createDiagnosticForNode(node, relatedSuggestionByDeclarationKind[node.kind]));
    return diag2;
  }
  function createBindingElementError(node) {
    return createDiagnosticForNode(node, Diagnostics.Binding_elements_can_t_be_exported_directly_with_isolatedDeclarations);
  }
  function createVariableOrPropertyError(node) {
    const diag2 ***REMOVED*** createDiagnosticForNode(node, errorByDeclarationKind[node.kind]);
    const targetStr ***REMOVED*** getTextOfNode(
      node.name,
      /*includeTrivia*/
      false
    );
    addRelatedInfo(diag2, createDiagnosticForNode(node, relatedSuggestionByDeclarationKind[node.kind], targetStr));
    return diag2;
  }
  function createParameterError(node) {
    if (isSetAccessor(node.parent)) {
      return createAccessorTypeError(node.parent);
    }
    const addUndefined ***REMOVED*** resolver.requiresAddingImplicitUndefined(node, node.parent);
    if (!addUndefined && node.initializer) {
      return createExpressionError(node.initializer);
    }
    const message ***REMOVED*** addUndefined ? Diagnostics.Declaration_emit_for_this_parameter_requires_implicitly_adding_undefined_to_its_type_This_is_not_supported_with_isolatedDeclarations : errorByDeclarationKind[node.kind];
    const diag2 ***REMOVED*** createDiagnosticForNode(node, message);
    const targetStr ***REMOVED*** getTextOfNode(
      node.name,
      /*includeTrivia*/
      false
    );
    addRelatedInfo(diag2, createDiagnosticForNode(node, relatedSuggestionByDeclarationKind[node.kind], targetStr));
    return diag2;
  }
  function createClassExpressionError(node) {
    return createExpressionError(node, Diagnostics.Inference_from_class_expressions_is_not_supported_with_isolatedDeclarations);
  }
  function createEntityInTypeNodeError(node) {
    const diag2 ***REMOVED*** createDiagnosticForNode(node, Diagnostics.Type_containing_private_name_0_can_t_be_used_with_isolatedDeclarations, getTextOfNode(
      node,
      /*includeTrivia*/
      false
    ));
    addParentDeclarationRelatedInfo(node, diag2);
    return diag2;
  }
  function createExpressionError(node, diagnosticMessage) {
    const parentDeclaration ***REMOVED*** findNearestDeclaration(node);
    let diag2;
    if (parentDeclaration) {
      const targetStr ***REMOVED*** isExportAssignment(parentDeclaration) || !parentDeclaration.name ? "" : getTextOfNode(
        parentDeclaration.name,
        /*includeTrivia*/
        false
      );
      const parent ***REMOVED*** findAncestor(node.parent, (n) ***REMOVED***> isExportAssignment(n) || (isStatement(n) ? "quit" : !isParenthesizedExpression(n) && !isTypeAssertionExpression(n) && !isAsExpression(n)));
      if (parentDeclaration ***REMOVED******REMOVED******REMOVED*** parent) {
        diag2 ***REMOVED*** createDiagnosticForNode(node, diagnosticMessage ?? errorByDeclarationKind[parentDeclaration.kind]);
        addRelatedInfo(diag2, createDiagnosticForNode(parentDeclaration, relatedSuggestionByDeclarationKind[parentDeclaration.kind], targetStr));
      } else {
        diag2 ***REMOVED*** createDiagnosticForNode(node, diagnosticMessage ?? Diagnostics.Expression_type_can_t_be_inferred_with_isolatedDeclarations);
        addRelatedInfo(diag2, createDiagnosticForNode(parentDeclaration, relatedSuggestionByDeclarationKind[parentDeclaration.kind], targetStr));
        addRelatedInfo(diag2, createDiagnosticForNode(node, Diagnostics.Add_satisfies_and_a_type_assertion_to_this_expression_satisfies_T_as_T_to_make_the_type_explicit));
      }
    } else {
      diag2 ***REMOVED*** createDiagnosticForNode(node, diagnosticMessage ?? Diagnostics.Expression_type_can_t_be_inferred_with_isolatedDeclarations);
    }
    return diag2;
  }
}

// src/compiler/transformers/declarations.ts
function getDeclarationDiagnostics(host, resolver, file) {
  const compilerOptions ***REMOVED*** host.getCompilerOptions();
  const files ***REMOVED*** filter(getSourceFilesToEmit(host, file), isSourceFileNotJson);
  return contains(files, file) ? transformNodes(
    resolver,
    host,
    factory,
    compilerOptions,
    [file],
    [transformDeclarations],
    /*allowDtsFiles*/
    false
  ).diagnostics : void 0;
}
var declarationEmitNodeBuilderFlags ***REMOVED*** 1024 /* MultilineObjectLiterals */ | 2048 /* WriteClassExpressionAsTypeLiteral */ | 4096 /* UseTypeOfFunction */ | 8 /* UseStructuralFallback */ | 524288 /* AllowEmptyTuple */ | 4 /* GenerateNamesForShadowedTypeParams */ | 1 /* NoTruncation */;
var declarationEmitInternalNodeBuilderFlags ***REMOVED*** 8 /* AllowUnresolvedNames */;
function transformDeclarations(context) {
  const throwDiagnostic ***REMOVED*** () ***REMOVED***> Debug.fail("Diagnostic emitted without context");
  let getSymbolAccessibilityDiagnostic ***REMOVED*** throwDiagnostic;
  let needsDeclare ***REMOVED*** true;
  let isBundledEmit ***REMOVED*** false;
  let resultHasExternalModuleIndicator ***REMOVED*** false;
  let needsScopeFixMarker ***REMOVED*** false;
  let resultHasScopeMarker ***REMOVED*** false;
  let enclosingDeclaration;
  let lateMarkedStatements;
  let lateStatementReplacementMap;
  let suppressNewDiagnosticContexts;
  const { factory: factory2 } ***REMOVED*** context;
  const host ***REMOVED*** context.getEmitHost();
  let restoreFallbackNode ***REMOVED*** () ***REMOVED***> void 0;
  const symbolTracker ***REMOVED*** {
    trackSymbol,
    reportInaccessibleThisError,
    reportInaccessibleUniqueSymbolError,
    reportCyclicStructureError,
    reportPrivateInBaseOfClassExpression,
    reportLikelyUnsafeImportRequiredError,
    reportTruncationError,
    moduleResolverHost: host,
    reportNonlocalAugmentation,
    reportNonSerializableProperty,
    reportInferenceFallback,
    pushErrorFallbackNode(node) {
      const currentFallback ***REMOVED*** errorFallbackNode;
      const currentRestore ***REMOVED*** restoreFallbackNode;
      restoreFallbackNode ***REMOVED*** () ***REMOVED***> {
        restoreFallbackNode ***REMOVED*** currentRestore;
        errorFallbackNode ***REMOVED*** currentFallback;
      };
      errorFallbackNode ***REMOVED*** node;
    },
    popErrorFallbackNode() {
      restoreFallbackNode();
    }
  };
  let errorNameNode;
  let errorFallbackNode;
  let currentSourceFile;
  let rawReferencedFiles;
  let rawTypeReferenceDirectives;
  let rawLibReferenceDirectives;
  const resolver ***REMOVED*** context.getEmitResolver();
  const options ***REMOVED*** context.getCompilerOptions();
  const getIsolatedDeclarationError ***REMOVED*** createGetIsolatedDeclarationErrors(resolver);
  const { stripInternal, isolatedDeclarations } ***REMOVED*** options;
  return transformRoot;
  function reportExpandoFunctionErrors(node) {
    resolver.getPropertiesOfContainerFunction(node).forEach((p) ***REMOVED***> {
      if (isExpandoPropertyDeclaration(p.valueDeclaration)) {
        const errorTarget ***REMOVED*** isBinaryExpression(p.valueDeclaration) ? p.valueDeclaration.left : p.valueDeclaration;
        context.addDiagnostic(createDiagnosticForNode(
          errorTarget,
          Diagnostics.Assigning_properties_to_functions_without_declaring_them_is_not_supported_with_isolatedDeclarations_Add_an_explicit_declaration_for_the_properties_assigned_to_this_function
        ));
      }
    });
  }
  function reportInferenceFallback(node) {
    if (!isolatedDeclarations || isSourceFileJS(currentSourceFile)) return;
    if (getSourceFileOfNode(node) !***REMOVED******REMOVED*** currentSourceFile) return;
    if (isVariableDeclaration(node) && resolver.isExpandoFunctionDeclaration(node)) {
      reportExpandoFunctionErrors(node);
    } else {
      context.addDiagnostic(getIsolatedDeclarationError(node));
    }
  }
  function handleSymbolAccessibilityError(symbolAccessibilityResult) {
    if (symbolAccessibilityResult.accessibility ***REMOVED******REMOVED******REMOVED*** 0 /* Accessible */) {
      if (symbolAccessibilityResult.aliasesToMakeVisible) {
        if (!lateMarkedStatements) {
          lateMarkedStatements ***REMOVED*** symbolAccessibilityResult.aliasesToMakeVisible;
        } else {
          for (const ref of symbolAccessibilityResult.aliasesToMakeVisible) {
            pushIfUnique(lateMarkedStatements, ref);
          }
        }
      }
    } else if (symbolAccessibilityResult.accessibility !***REMOVED******REMOVED*** 3 /* NotResolved */) {
      const errorInfo ***REMOVED*** getSymbolAccessibilityDiagnostic(symbolAccessibilityResult);
      if (errorInfo) {
        if (errorInfo.typeName) {
          context.addDiagnostic(createDiagnosticForNode(symbolAccessibilityResult.errorNode || errorInfo.errorNode, errorInfo.diagnosticMessage, getTextOfNode(errorInfo.typeName), symbolAccessibilityResult.errorSymbolName, symbolAccessibilityResult.errorModuleName));
        } else {
          context.addDiagnostic(createDiagnosticForNode(symbolAccessibilityResult.errorNode || errorInfo.errorNode, errorInfo.diagnosticMessage, symbolAccessibilityResult.errorSymbolName, symbolAccessibilityResult.errorModuleName));
        }
        return true;
      }
    }
    return false;
  }
  function trackSymbol(symbol, enclosingDeclaration2, meaning) {
    if (symbol.flags & 262144 /* TypeParameter */) return false;
    const issuedDiagnostic ***REMOVED*** handleSymbolAccessibilityError(resolver.isSymbolAccessible(
      symbol,
      enclosingDeclaration2,
      meaning,
      /*shouldComputeAliasToMarkVisible*/
      true
    ));
    return issuedDiagnostic;
  }
  function reportPrivateInBaseOfClassExpression(propertyName) {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(
        addRelatedInfo(
          createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.Property_0_of_exported_anonymous_class_type_may_not_be_private_or_protected, propertyName),
          ...isVariableDeclaration((errorNameNode || errorFallbackNode).parent) ? [createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.Add_a_type_annotation_to_the_variable_0, errorDeclarationNameWithFallback())] : []
        )
      );
    }
  }
  function errorDeclarationNameWithFallback() {
    return errorNameNode ? declarationNameToString(errorNameNode) : errorFallbackNode && getNameOfDeclaration(errorFallbackNode) ? declarationNameToString(getNameOfDeclaration(errorFallbackNode)) : errorFallbackNode && isExportAssignment(errorFallbackNode) ? errorFallbackNode.isExportEquals ? "export***REMOVED***" : "default" : "(Missing)";
  }
  function reportInaccessibleUniqueSymbolError() {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_inferred_type_of_0_references_an_inaccessible_1_type_A_type_annotation_is_necessary, errorDeclarationNameWithFallback(), "unique symbol"));
    }
  }
  function reportCyclicStructureError() {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_inferred_type_of_0_references_a_type_with_a_cyclic_structure_which_cannot_be_trivially_serialized_A_type_annotation_is_necessary, errorDeclarationNameWithFallback()));
    }
  }
  function reportInaccessibleThisError() {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_inferred_type_of_0_references_an_inaccessible_1_type_A_type_annotation_is_necessary, errorDeclarationNameWithFallback(), "this"));
    }
  }
  function reportLikelyUnsafeImportRequiredError(specifier) {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_inferred_type_of_0_cannot_be_named_without_a_reference_to_1_This_is_likely_not_portable_A_type_annotation_is_necessary, errorDeclarationNameWithFallback(), specifier));
    }
  }
  function reportTruncationError() {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_inferred_type_of_this_node_exceeds_the_maximum_length_the_compiler_will_serialize_An_explicit_type_annotation_is_needed));
    }
  }
  function reportNonlocalAugmentation(containingFile, parentSymbol, symbol) {
    var _a;
    const primaryDeclaration ***REMOVED*** (_a ***REMOVED*** parentSymbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.find((d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** containingFile);
    const augmentingDeclarations ***REMOVED*** filter(symbol.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) !***REMOVED******REMOVED*** containingFile);
    if (primaryDeclaration && augmentingDeclarations) {
      for (const augmentations of augmentingDeclarations) {
        context.addDiagnostic(addRelatedInfo(
          createDiagnosticForNode(augmentations, Diagnostics.Declaration_augments_declaration_in_another_file_This_cannot_be_serialized),
          createDiagnosticForNode(primaryDeclaration, Diagnostics.This_is_the_declaration_being_augmented_Consider_moving_the_augmenting_declaration_into_the_same_file)
        ));
      }
    }
  }
  function reportNonSerializableProperty(propertyName) {
    if (errorNameNode || errorFallbackNode) {
      context.addDiagnostic(createDiagnosticForNode(errorNameNode || errorFallbackNode, Diagnostics.The_type_of_this_node_cannot_be_serialized_because_its_property_0_cannot_be_serialized, propertyName));
    }
  }
  function transformDeclarationsForJS(sourceFile) {
    const oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
    getSymbolAccessibilityDiagnostic ***REMOVED*** (s) ***REMOVED***> s.errorNode && canProduceDiagnostics(s.errorNode) ? createGetSymbolAccessibilityDiagnosticForNode(s.errorNode)(s) : {
      diagnosticMessage: s.errorModuleName ? Diagnostics.Declaration_emit_for_this_file_requires_using_private_name_0_from_module_1_An_explicit_type_annotation_may_unblock_declaration_emit : Diagnostics.Declaration_emit_for_this_file_requires_using_private_name_0_An_explicit_type_annotation_may_unblock_declaration_emit,
      errorNode: s.errorNode || sourceFile
    };
    const result ***REMOVED*** resolver.getDeclarationStatementsForSourceFile(sourceFile, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags, symbolTracker);
    getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
    return result;
  }
  function transformRoot(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ && node.isDeclarationFile) {
      return node;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */) {
      isBundledEmit ***REMOVED*** true;
      rawReferencedFiles ***REMOVED*** [];
      rawTypeReferenceDirectives ***REMOVED*** [];
      rawLibReferenceDirectives ***REMOVED*** [];
      let hasNoDefaultLib ***REMOVED*** false;
      const bundle ***REMOVED*** factory2.createBundle(
        map(node.sourceFiles, (sourceFile) ***REMOVED***> {
          if (sourceFile.isDeclarationFile) return void 0;
          hasNoDefaultLib ***REMOVED*** hasNoDefaultLib || sourceFile.hasNoDefaultLib;
          currentSourceFile ***REMOVED*** sourceFile;
          enclosingDeclaration ***REMOVED*** sourceFile;
          lateMarkedStatements ***REMOVED*** void 0;
          suppressNewDiagnosticContexts ***REMOVED*** false;
          lateStatementReplacementMap ***REMOVED*** /* @__PURE__ */ new Map();
          getSymbolAccessibilityDiagnostic ***REMOVED*** throwDiagnostic;
          needsScopeFixMarker ***REMOVED*** false;
          resultHasScopeMarker ***REMOVED*** false;
          collectFileReferences(sourceFile);
          if (isExternalOrCommonJsModule(sourceFile) || isJsonSourceFile(sourceFile)) {
            resultHasExternalModuleIndicator ***REMOVED*** false;
            needsDeclare ***REMOVED*** false;
            const statements ***REMOVED*** isSourceFileJS(sourceFile) ? factory2.createNodeArray(transformDeclarationsForJS(sourceFile)) : visitNodes2(sourceFile.statements, visitDeclarationStatements, isStatement);
            const newFile ***REMOVED*** factory2.updateSourceFile(
              sourceFile,
              [factory2.createModuleDeclaration(
                [factory2.createModifier(138 /* DeclareKeyword */)],
                factory2.createStringLiteral(getResolvedExternalModuleName(context.getEmitHost(), sourceFile)),
                factory2.createModuleBlock(setTextRange(factory2.createNodeArray(transformAndReplaceLatePaintedStatements(statements)), sourceFile.statements))
              )],
              /*isDeclarationFile*/
              true,
              /*referencedFiles*/
              [],
              /*typeReferences*/
              [],
              /*hasNoDefaultLib*/
              false,
              /*libReferences*/
              []
            );
            return newFile;
          }
          needsDeclare ***REMOVED*** true;
          const updated ***REMOVED*** isSourceFileJS(sourceFile) ? factory2.createNodeArray(transformDeclarationsForJS(sourceFile)) : visitNodes2(sourceFile.statements, visitDeclarationStatements, isStatement);
          return factory2.updateSourceFile(
            sourceFile,
            transformAndReplaceLatePaintedStatements(updated),
            /*isDeclarationFile*/
            true,
            /*referencedFiles*/
            [],
            /*typeReferences*/
            [],
            /*hasNoDefaultLib*/
            false,
            /*libReferences*/
            []
          );
        })
      );
      const outputFilePath2 ***REMOVED*** getDirectoryPath(normalizeSlashes(getOutputPathsFor(
        node,
        host,
        /*forceDtsPaths*/
        true
      ).declarationFilePath));
      bundle.syntheticFileReferences ***REMOVED*** getReferencedFiles(outputFilePath2);
      bundle.syntheticTypeReferences ***REMOVED*** getTypeReferences();
      bundle.syntheticLibReferences ***REMOVED*** getLibReferences();
      bundle.hasNoDefaultLib ***REMOVED*** hasNoDefaultLib;
      return bundle;
    }
    needsDeclare ***REMOVED*** true;
    needsScopeFixMarker ***REMOVED*** false;
    resultHasScopeMarker ***REMOVED*** false;
    enclosingDeclaration ***REMOVED*** node;
    currentSourceFile ***REMOVED*** node;
    getSymbolAccessibilityDiagnostic ***REMOVED*** throwDiagnostic;
    isBundledEmit ***REMOVED*** false;
    resultHasExternalModuleIndicator ***REMOVED*** false;
    suppressNewDiagnosticContexts ***REMOVED*** false;
    lateMarkedStatements ***REMOVED*** void 0;
    lateStatementReplacementMap ***REMOVED*** /* @__PURE__ */ new Map();
    rawReferencedFiles ***REMOVED*** [];
    rawTypeReferenceDirectives ***REMOVED*** [];
    rawLibReferenceDirectives ***REMOVED*** [];
    collectFileReferences(currentSourceFile);
    let combinedStatements;
    if (isSourceFileJS(currentSourceFile)) {
      combinedStatements ***REMOVED*** factory2.createNodeArray(transformDeclarationsForJS(node));
    } else {
      const statements ***REMOVED*** visitNodes2(node.statements, visitDeclarationStatements, isStatement);
      combinedStatements ***REMOVED*** setTextRange(factory2.createNodeArray(transformAndReplaceLatePaintedStatements(statements)), node.statements);
      if (isExternalModule(node) && (!resultHasExternalModuleIndicator || needsScopeFixMarker && !resultHasScopeMarker)) {
        combinedStatements ***REMOVED*** setTextRange(factory2.createNodeArray([...combinedStatements, createEmptyExports(factory2)]), combinedStatements);
      }
    }
    const outputFilePath ***REMOVED*** getDirectoryPath(normalizeSlashes(getOutputPathsFor(
      node,
      host,
      /*forceDtsPaths*/
      true
    ).declarationFilePath));
    return factory2.updateSourceFile(
      node,
      combinedStatements,
      /*isDeclarationFile*/
      true,
      getReferencedFiles(outputFilePath),
      getTypeReferences(),
      node.hasNoDefaultLib,
      getLibReferences()
    );
    function collectFileReferences(sourceFile) {
      rawReferencedFiles ***REMOVED*** concatenate(rawReferencedFiles, map(sourceFile.referencedFiles, (f) ***REMOVED***> [sourceFile, f]));
      rawTypeReferenceDirectives ***REMOVED*** concatenate(rawTypeReferenceDirectives, sourceFile.typeReferenceDirectives);
      rawLibReferenceDirectives ***REMOVED*** concatenate(rawLibReferenceDirectives, sourceFile.libReferenceDirectives);
    }
    function copyFileReferenceAsSynthetic(ref) {
      const newRef ***REMOVED*** { ...ref };
      newRef.pos ***REMOVED*** -1;
      newRef.end ***REMOVED*** -1;
      return newRef;
    }
    function getTypeReferences() {
      return mapDefined(rawTypeReferenceDirectives, (ref) ***REMOVED***> {
        if (!ref.preserve) return void 0;
        return copyFileReferenceAsSynthetic(ref);
      });
    }
    function getLibReferences() {
      return mapDefined(rawLibReferenceDirectives, (ref) ***REMOVED***> {
        if (!ref.preserve) return void 0;
        return copyFileReferenceAsSynthetic(ref);
      });
    }
    function getReferencedFiles(outputFilePath2) {
      return mapDefined(rawReferencedFiles, ([sourceFile, ref]) ***REMOVED***> {
        if (!ref.preserve) return void 0;
        const file ***REMOVED*** host.getSourceFileFromReference(sourceFile, ref);
        if (!file) {
          return void 0;
        }
        let declFileName;
        if (file.isDeclarationFile) {
          declFileName ***REMOVED*** file.fileName;
        } else {
          if (isBundledEmit && contains(node.sourceFiles, file)) return;
          const paths ***REMOVED*** getOutputPathsFor(
            file,
            host,
            /*forceDtsPaths*/
            true
          );
          declFileName ***REMOVED*** paths.declarationFilePath || paths.jsFilePath || file.fileName;
        }
        if (!declFileName) return void 0;
        const fileName ***REMOVED*** getRelativePathToDirectoryOrUrl(
          outputFilePath2,
          declFileName,
          host.getCurrentDirectory(),
          host.getCanonicalFileName,
          /*isAbsolutePathAnUrl*/
          false
        );
        const newRef ***REMOVED*** copyFileReferenceAsSynthetic(ref);
        newRef.fileName ***REMOVED*** fileName;
        return newRef;
      });
    }
  }
  function filterBindingPatternInitializers(name) {
    if (name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      return name;
    } else {
      if (name.kind ***REMOVED******REMOVED******REMOVED*** 208 /* ArrayBindingPattern */) {
        return factory2.updateArrayBindingPattern(name, visitNodes2(name.elements, visitBindingElement, isArrayBindingElement));
      } else {
        return factory2.updateObjectBindingPattern(name, visitNodes2(name.elements, visitBindingElement, isBindingElement));
      }
    }
    function visitBindingElement(elem) {
      if (elem.kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */) {
        return elem;
      }
      if (elem.propertyName && isComputedPropertyName(elem.propertyName) && isEntityNameExpression(elem.propertyName.expression)) {
        checkEntityNameVisibility(elem.propertyName.expression, enclosingDeclaration);
      }
      return factory2.updateBindingElement(
        elem,
        elem.dotDotDotToken,
        elem.propertyName,
        filterBindingPatternInitializers(elem.name),
        /*initializer*/
        void 0
      );
    }
  }
  function ensureParameter(p, modifierMask) {
    let oldDiag;
    if (!suppressNewDiagnosticContexts) {
      oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
      getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(p);
    }
    const newParam ***REMOVED*** factory2.updateParameterDeclaration(
      p,
      maskModifiers(factory2, p, modifierMask),
      p.dotDotDotToken,
      filterBindingPatternInitializers(p.name),
      resolver.isOptionalParameter(p) ? p.questionToken || factory2.createToken(58 /* QuestionToken */) : void 0,
      ensureType(
        p,
        /*ignorePrivate*/
        true
      ),
      // Ignore private param props, since this type is going straight back into a param
      ensureNoInitializer(p)
    );
    if (!suppressNewDiagnosticContexts) {
      getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
    }
    return newParam;
  }
  function shouldPrintWithInitializer(node) {
    return canHaveLiteralInitializer(node) && !!node.initializer && resolver.isLiteralConstDeclaration(getParseTreeNode(node));
  }
  function ensureNoInitializer(node) {
    if (shouldPrintWithInitializer(node)) {
      const unwrappedInitializer ***REMOVED*** unwrapParenthesizedExpression(node.initializer);
      if (!isPrimitiveLiteralValue(unwrappedInitializer)) {
        reportInferenceFallback(node);
      }
      return resolver.createLiteralConstValue(getParseTreeNode(node, canHaveLiteralInitializer), symbolTracker);
    }
    return void 0;
  }
  function ensureType(node, ignorePrivate) {
    if (!ignorePrivate && hasEffectiveModifier(node, 2 /* Private */)) {
      return;
    }
    if (shouldPrintWithInitializer(node)) {
      return;
    }
    if (!isExportAssignment(node) && !isBindingElement(node) && node.type && (!isParameter(node) || !resolver.requiresAddingImplicitUndefined(node, enclosingDeclaration))) {
      return visitNode(node.type, visitDeclarationSubtree, isTypeNode);
    }
    const oldErrorNameNode ***REMOVED*** errorNameNode;
    errorNameNode ***REMOVED*** node.name;
    let oldDiag;
    if (!suppressNewDiagnosticContexts) {
      oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
      if (canProduceDiagnostics(node)) {
        getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(node);
      }
    }
    let typeNode;
    if (hasInferredType(node)) {
      typeNode ***REMOVED*** resolver.createTypeOfDeclaration(node, enclosingDeclaration, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags, symbolTracker);
    } else if (isFunctionLike(node)) {
      typeNode ***REMOVED*** resolver.createReturnTypeOfSignatureDeclaration(node, enclosingDeclaration, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags, symbolTracker);
    } else {
      Debug.assertNever(node);
    }
    errorNameNode ***REMOVED*** oldErrorNameNode;
    if (!suppressNewDiagnosticContexts) {
      getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
    }
    return typeNode ?? factory2.createKeywordTypeNode(133 /* AnyKeyword */);
  }
  function isDeclarationAndNotVisible(node) {
    node ***REMOVED*** getParseTreeNode(node);
    switch (node.kind) {
      case 263 /* FunctionDeclaration */:
      case 268 /* ModuleDeclaration */:
      case 265 /* InterfaceDeclaration */:
      case 264 /* ClassDeclaration */:
      case 266 /* TypeAliasDeclaration */:
      case 267 /* EnumDeclaration */:
        return !resolver.isDeclarationVisible(node);
      // The following should be doing their own visibility checks based on filtering their members
      case 261 /* VariableDeclaration */:
        return !getBindingNameVisible(node);
      case 272 /* ImportEqualsDeclaration */:
      case 273 /* ImportDeclaration */:
      case 279 /* ExportDeclaration */:
      case 278 /* ExportAssignment */:
        return false;
      case 176 /* ClassStaticBlockDeclaration */:
        return true;
    }
    return false;
  }
  function shouldEmitFunctionProperties(input) {
    var _a;
    if (input.body) {
      return true;
    }
    const overloadSignatures ***REMOVED*** (_a ***REMOVED*** input.symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.filter((decl) ***REMOVED***> isFunctionDeclaration(decl) && !decl.body);
    return !overloadSignatures || overloadSignatures.indexOf(input) ***REMOVED******REMOVED******REMOVED*** overloadSignatures.length - 1;
  }
  function getBindingNameVisible(elem) {
    if (isOmittedExpression(elem)) {
      return false;
    }
    if (isBindingPattern(elem.name)) {
      return some(elem.name.elements, getBindingNameVisible);
    } else {
      return resolver.isDeclarationVisible(elem);
    }
  }
  function updateParamsList(node, params, modifierMask) {
    if (hasEffectiveModifier(node, 2 /* Private */)) {
      return factory2.createNodeArray();
    }
    const newParams ***REMOVED*** map(params, (p) ***REMOVED***> ensureParameter(p, modifierMask));
    if (!newParams) {
      return factory2.createNodeArray();
    }
    return factory2.createNodeArray(newParams, params.hasTrailingComma);
  }
  function updateAccessorParamsList(input, isPrivate) {
    let newParams;
    if (!isPrivate) {
      const thisParameter ***REMOVED*** getThisParameter(input);
      if (thisParameter) {
        newParams ***REMOVED*** [ensureParameter(thisParameter)];
      }
    }
    if (isSetAccessorDeclaration(input)) {
      let newValueParameter;
      if (!isPrivate) {
        const valueParameter ***REMOVED*** getSetAccessorValueParameter(input);
        if (valueParameter) {
          newValueParameter ***REMOVED*** ensureParameter(valueParameter);
        }
      }
      if (!newValueParameter) {
        newValueParameter ***REMOVED*** factory2.createParameterDeclaration(
          /*modifiers*/
          void 0,
          /*dotDotDotToken*/
          void 0,
          "value"
        );
      }
      newParams ***REMOVED*** append(newParams, newValueParameter);
    }
    return factory2.createNodeArray(newParams || emptyArray);
  }
  function ensureTypeParams(node, params) {
    return hasEffectiveModifier(node, 2 /* Private */) ? void 0 : visitNodes2(params, visitDeclarationSubtree, isTypeParameterDeclaration);
  }
  function isEnclosingDeclaration(node) {
    return isSourceFile(node) || isTypeAliasDeclaration(node) || isModuleDeclaration(node) || isClassDeclaration(node) || isInterfaceDeclaration(node) || isFunctionLike(node) || isIndexSignatureDeclaration(node) || isMappedTypeNode(node);
  }
  function checkEntityNameVisibility(entityName, enclosingDeclaration2) {
    const visibilityResult ***REMOVED*** resolver.isEntityNameVisible(entityName, enclosingDeclaration2);
    handleSymbolAccessibilityError(visibilityResult);
  }
  function preserveJsDoc(updated, original) {
    if (hasJSDocNodes(updated) && hasJSDocNodes(original)) {
      updated.jsDoc ***REMOVED*** original.jsDoc;
    }
    return setCommentRange(updated, getCommentRange(original));
  }
  function rewriteModuleSpecifier2(parent, input) {
    if (!input) return void 0;
    resultHasExternalModuleIndicator ***REMOVED*** resultHasExternalModuleIndicator || parent.kind !***REMOVED******REMOVED*** 268 /* ModuleDeclaration */ && parent.kind !***REMOVED******REMOVED*** 206 /* ImportType */;
    if (isStringLiteralLike(input)) {
      if (isBundledEmit) {
        const newName ***REMOVED*** getExternalModuleNameFromDeclaration(context.getEmitHost(), resolver, parent);
        if (newName) {
          return factory2.createStringLiteral(newName);
        }
      }
    }
    return input;
  }
  function transformImportEqualsDeclaration(decl) {
    if (!resolver.isDeclarationVisible(decl)) return;
    if (decl.moduleReference.kind ***REMOVED******REMOVED******REMOVED*** 284 /* ExternalModuleReference */) {
      const specifier ***REMOVED*** getExternalModuleImportEqualsDeclarationExpression(decl);
      return factory2.updateImportEqualsDeclaration(
        decl,
        decl.modifiers,
        decl.isTypeOnly,
        decl.name,
        factory2.updateExternalModuleReference(decl.moduleReference, rewriteModuleSpecifier2(decl, specifier))
      );
    } else {
      const oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
      getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(decl);
      checkEntityNameVisibility(decl.moduleReference, enclosingDeclaration);
      getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
      return decl;
    }
  }
  function transformImportDeclaration(decl) {
    if (!decl.importClause) {
      return factory2.updateImportDeclaration(
        decl,
        decl.modifiers,
        decl.importClause,
        rewriteModuleSpecifier2(decl, decl.moduleSpecifier),
        tryGetResolutionModeOverride(decl.attributes)
      );
    }
    const phaseModifier ***REMOVED*** decl.importClause.phaseModifier ***REMOVED******REMOVED******REMOVED*** 166 /* DeferKeyword */ ? void 0 : decl.importClause.phaseModifier;
    const visibleDefaultBinding ***REMOVED*** decl.importClause && decl.importClause.name && resolver.isDeclarationVisible(decl.importClause) ? decl.importClause.name : void 0;
    if (!decl.importClause.namedBindings) {
      return visibleDefaultBinding && factory2.updateImportDeclaration(
        decl,
        decl.modifiers,
        factory2.updateImportClause(
          decl.importClause,
          phaseModifier,
          visibleDefaultBinding,
          /*namedBindings*/
          void 0
        ),
        rewriteModuleSpecifier2(decl, decl.moduleSpecifier),
        tryGetResolutionModeOverride(decl.attributes)
      );
    }
    if (decl.importClause.namedBindings.kind ***REMOVED******REMOVED******REMOVED*** 275 /* NamespaceImport */) {
      const namedBindings ***REMOVED*** resolver.isDeclarationVisible(decl.importClause.namedBindings) ? decl.importClause.namedBindings : (
        /*namedBindings*/
        void 0
      );
      return visibleDefaultBinding || namedBindings ? factory2.updateImportDeclaration(
        decl,
        decl.modifiers,
        factory2.updateImportClause(
          decl.importClause,
          phaseModifier,
          visibleDefaultBinding,
          namedBindings
        ),
        rewriteModuleSpecifier2(decl, decl.moduleSpecifier),
        tryGetResolutionModeOverride(decl.attributes)
      ) : void 0;
    }
    const bindingList ***REMOVED*** mapDefined(decl.importClause.namedBindings.elements, (b) ***REMOVED***> resolver.isDeclarationVisible(b) ? b : void 0);
    if (bindingList && bindingList.length || visibleDefaultBinding) {
      return factory2.updateImportDeclaration(
        decl,
        decl.modifiers,
        factory2.updateImportClause(
          decl.importClause,
          phaseModifier,
          visibleDefaultBinding,
          bindingList && bindingList.length ? factory2.updateNamedImports(decl.importClause.namedBindings, bindingList) : void 0
        ),
        rewriteModuleSpecifier2(decl, decl.moduleSpecifier),
        tryGetResolutionModeOverride(decl.attributes)
      );
    }
    if (resolver.isImportRequiredByAugmentation(decl)) {
      if (isolatedDeclarations) {
        context.addDiagnostic(createDiagnosticForNode(decl, Diagnostics.Declaration_emit_for_this_file_requires_preserving_this_import_for_augmentations_This_is_not_supported_with_isolatedDeclarations));
      }
      return factory2.updateImportDeclaration(
        decl,
        decl.modifiers,
        /*importClause*/
        void 0,
        rewriteModuleSpecifier2(decl, decl.moduleSpecifier),
        tryGetResolutionModeOverride(decl.attributes)
      );
    }
  }
  function tryGetResolutionModeOverride(node) {
    const mode ***REMOVED*** getResolutionModeOverride(node);
    return node && mode !***REMOVED******REMOVED*** void 0 ? node : void 0;
  }
  function transformAndReplaceLatePaintedStatements(statements) {
    while (length(lateMarkedStatements)) {
      const i ***REMOVED*** lateMarkedStatements.shift();
      if (!isLateVisibilityPaintedStatement(i)) {
        return Debug.fail(`Late replaced statement was found which is not handled by the declaration transformer!: ${Debug.formatSyntaxKind(i.kind)}`);
      }
      const priorNeedsDeclare ***REMOVED*** needsDeclare;
      needsDeclare ***REMOVED*** i.parent && isSourceFile(i.parent) && !(isExternalModule(i.parent) && isBundledEmit);
      const result ***REMOVED*** transformTopLevelDeclaration(i);
      needsDeclare ***REMOVED*** priorNeedsDeclare;
      lateStatementReplacementMap.set(getOriginalNodeId(i), result);
    }
    return visitNodes2(statements, visitLateVisibilityMarkedStatements, isStatement);
    function visitLateVisibilityMarkedStatements(statement) {
      if (isLateVisibilityPaintedStatement(statement)) {
        const key ***REMOVED*** getOriginalNodeId(statement);
        if (lateStatementReplacementMap.has(key)) {
          const result ***REMOVED*** lateStatementReplacementMap.get(key);
          lateStatementReplacementMap.delete(key);
          if (result) {
            if (isArray(result) ? some(result, needsScopeMarker) : needsScopeMarker(result)) {
              needsScopeFixMarker ***REMOVED*** true;
            }
            if (isSourceFile(statement.parent) && (isArray(result) ? some(result, isExternalModuleIndicator) : isExternalModuleIndicator(result))) {
              resultHasExternalModuleIndicator ***REMOVED*** true;
            }
          }
          return result;
        }
      }
      return statement;
    }
  }
  function visitDeclarationSubtree(input) {
    if (shouldStripInternal(input)) return;
    if (isDeclaration(input)) {
      if (isDeclarationAndNotVisible(input)) return;
      if (hasDynamicName(input)) {
        if (isolatedDeclarations) {
          if (!resolver.isDefinitelyReferenceToGlobalSymbolObject(input.name.expression)) {
            if (isClassDeclaration(input.parent) || isObjectLiteralExpression(input.parent)) {
              context.addDiagnostic(createDiagnosticForNode(input, Diagnostics.Computed_property_names_on_class_or_object_literals_cannot_be_inferred_with_isolatedDeclarations));
              return;
            } else if (
              // Type declarations just need to double-check that the input computed name is an entity name expression
              (isInterfaceDeclaration(input.parent) || isTypeLiteralNode(input.parent)) && !isEntityNameExpression(input.name.expression)
            ) {
              context.addDiagnostic(createDiagnosticForNode(input, Diagnostics.Computed_properties_must_be_number_or_string_literals_variables_or_dotted_expressions_with_isolatedDeclarations));
              return;
            }
          }
        } else if (!resolver.isLateBound(getParseTreeNode(input)) || !isEntityNameExpression(input.name.expression)) {
          return;
        }
      }
    }
    if (isFunctionLike(input) && resolver.isImplementationOfOverload(input)) return;
    if (isSemicolonClassElement(input)) return;
    let previousEnclosingDeclaration;
    if (isEnclosingDeclaration(input)) {
      previousEnclosingDeclaration ***REMOVED*** enclosingDeclaration;
      enclosingDeclaration ***REMOVED*** input;
    }
    const oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
    const canProduceDiagnostic ***REMOVED*** canProduceDiagnostics(input);
    const oldWithinObjectLiteralType ***REMOVED*** suppressNewDiagnosticContexts;
    let shouldEnterSuppressNewDiagnosticsContextContext ***REMOVED*** (input.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */ || input.kind ***REMOVED******REMOVED******REMOVED*** 201 /* MappedType */) && input.parent.kind !***REMOVED******REMOVED*** 266 /* TypeAliasDeclaration */;
    if (isMethodDeclaration(input) || isMethodSignature(input)) {
      if (hasEffectiveModifier(input, 2 /* Private */)) {
        if (input.symbol && input.symbol.declarations && input.symbol.declarations[0] !***REMOVED******REMOVED*** input) return;
        return cleanup(factory2.createPropertyDeclaration(
          ensureModifiers(input),
          input.name,
          /*questionOrExclamationToken*/
          void 0,
          /*type*/
          void 0,
          /*initializer*/
          void 0
        ));
      }
    }
    if (canProduceDiagnostic && !suppressNewDiagnosticContexts) {
      getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(input);
    }
    if (isTypeQueryNode(input)) {
      checkEntityNameVisibility(input.exprName, enclosingDeclaration);
    }
    if (shouldEnterSuppressNewDiagnosticsContextContext) {
      suppressNewDiagnosticContexts ***REMOVED*** true;
    }
    if (isProcessedComponent(input)) {
      switch (input.kind) {
        case 234 /* ExpressionWithTypeArguments */: {
          if (isEntityName(input.expression) || isEntityNameExpression(input.expression)) {
            checkEntityNameVisibility(input.expression, enclosingDeclaration);
          }
          const node ***REMOVED*** visitEachChild(input, visitDeclarationSubtree, context);
          return cleanup(factory2.updateExpressionWithTypeArguments(node, node.expression, node.typeArguments));
        }
        case 184 /* TypeReference */: {
          checkEntityNameVisibility(input.typeName, enclosingDeclaration);
          const node ***REMOVED*** visitEachChild(input, visitDeclarationSubtree, context);
          return cleanup(factory2.updateTypeReferenceNode(node, node.typeName, node.typeArguments));
        }
        case 181 /* ConstructSignature */:
          return cleanup(factory2.updateConstructSignature(
            input,
            ensureTypeParams(input, input.typeParameters),
            updateParamsList(input, input.parameters),
            ensureType(input)
          ));
        case 177 /* Constructor */: {
          const ctor ***REMOVED*** factory2.createConstructorDeclaration(
            /*modifiers*/
            ensureModifiers(input),
            updateParamsList(input, input.parameters, 0 /* None */),
            /*body*/
            void 0
          );
          return cleanup(ctor);
        }
        case 175 /* MethodDeclaration */: {
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          const sig ***REMOVED*** factory2.createMethodDeclaration(
            ensureModifiers(input),
            /*asteriskToken*/
            void 0,
            input.name,
            input.questionToken,
            ensureTypeParams(input, input.typeParameters),
            updateParamsList(input, input.parameters),
            ensureType(input),
            /*body*/
            void 0
          );
          return cleanup(sig);
        }
        case 178 /* GetAccessor */: {
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          return cleanup(factory2.updateGetAccessorDeclaration(
            input,
            ensureModifiers(input),
            input.name,
            updateAccessorParamsList(input, hasEffectiveModifier(input, 2 /* Private */)),
            ensureType(input),
            /*body*/
            void 0
          ));
        }
        case 179 /* SetAccessor */: {
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          return cleanup(factory2.updateSetAccessorDeclaration(
            input,
            ensureModifiers(input),
            input.name,
            updateAccessorParamsList(input, hasEffectiveModifier(input, 2 /* Private */)),
            /*body*/
            void 0
          ));
        }
        case 173 /* PropertyDeclaration */:
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          return cleanup(factory2.updatePropertyDeclaration(
            input,
            ensureModifiers(input),
            input.name,
            input.questionToken,
            ensureType(input),
            ensureNoInitializer(input)
          ));
        case 172 /* PropertySignature */:
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          return cleanup(factory2.updatePropertySignature(
            input,
            ensureModifiers(input),
            input.name,
            input.questionToken,
            ensureType(input)
          ));
        case 174 /* MethodSignature */: {
          if (isPrivateIdentifier(input.name)) {
            return cleanup(
              /*returnValue*/
              void 0
            );
          }
          return cleanup(factory2.updateMethodSignature(
            input,
            ensureModifiers(input),
            input.name,
            input.questionToken,
            ensureTypeParams(input, input.typeParameters),
            updateParamsList(input, input.parameters),
            ensureType(input)
          ));
        }
        case 180 /* CallSignature */: {
          return cleanup(
            factory2.updateCallSignature(
              input,
              ensureTypeParams(input, input.typeParameters),
              updateParamsList(input, input.parameters),
              ensureType(input)
            )
          );
        }
        case 182 /* IndexSignature */: {
          return cleanup(factory2.updateIndexSignature(
            input,
            ensureModifiers(input),
            updateParamsList(input, input.parameters),
            visitNode(input.type, visitDeclarationSubtree, isTypeNode) || factory2.createKeywordTypeNode(133 /* AnyKeyword */)
          ));
        }
        case 261 /* VariableDeclaration */: {
          if (isBindingPattern(input.name)) {
            return recreateBindingPattern(input.name);
          }
          shouldEnterSuppressNewDiagnosticsContextContext ***REMOVED*** true;
          suppressNewDiagnosticContexts ***REMOVED*** true;
          return cleanup(factory2.updateVariableDeclaration(
            input,
            input.name,
            /*exclamationToken*/
            void 0,
            ensureType(input),
            ensureNoInitializer(input)
          ));
        }
        case 169 /* TypeParameter */: {
          if (isPrivateMethodTypeParameter(input) && (input.default || input.constraint)) {
            return cleanup(factory2.updateTypeParameterDeclaration(
              input,
              input.modifiers,
              input.name,
              /*constraint*/
              void 0,
              /*defaultType*/
              void 0
            ));
          }
          return cleanup(visitEachChild(input, visitDeclarationSubtree, context));
        }
        case 195 /* ConditionalType */: {
          const checkType ***REMOVED*** visitNode(input.checkType, visitDeclarationSubtree, isTypeNode);
          const extendsType ***REMOVED*** visitNode(input.extendsType, visitDeclarationSubtree, isTypeNode);
          const oldEnclosingDecl ***REMOVED*** enclosingDeclaration;
          enclosingDeclaration ***REMOVED*** input.trueType;
          const trueType ***REMOVED*** visitNode(input.trueType, visitDeclarationSubtree, isTypeNode);
          enclosingDeclaration ***REMOVED*** oldEnclosingDecl;
          const falseType ***REMOVED*** visitNode(input.falseType, visitDeclarationSubtree, isTypeNode);
          Debug.assert(checkType);
          Debug.assert(extendsType);
          Debug.assert(trueType);
          Debug.assert(falseType);
          return cleanup(factory2.updateConditionalTypeNode(input, checkType, extendsType, trueType, falseType));
        }
        case 185 /* FunctionType */: {
          return cleanup(factory2.updateFunctionTypeNode(
            input,
            visitNodes2(input.typeParameters, visitDeclarationSubtree, isTypeParameterDeclaration),
            updateParamsList(input, input.parameters),
            Debug.checkDefined(visitNode(input.type, visitDeclarationSubtree, isTypeNode))
          ));
        }
        case 186 /* ConstructorType */: {
          return cleanup(factory2.updateConstructorTypeNode(
            input,
            ensureModifiers(input),
            visitNodes2(input.typeParameters, visitDeclarationSubtree, isTypeParameterDeclaration),
            updateParamsList(input, input.parameters),
            Debug.checkDefined(visitNode(input.type, visitDeclarationSubtree, isTypeNode))
          ));
        }
        case 206 /* ImportType */: {
          if (!isLiteralImportTypeNode(input)) return cleanup(input);
          return cleanup(factory2.updateImportTypeNode(
            input,
            factory2.updateLiteralTypeNode(input.argument, rewriteModuleSpecifier2(input, input.argument.literal)),
            input.attributes,
            input.qualifier,
            visitNodes2(input.typeArguments, visitDeclarationSubtree, isTypeNode),
            input.isTypeOf
          ));
        }
        default:
          Debug.assertNever(input, `Attempted to process unhandled node kind: ${Debug.formatSyntaxKind(input.kind)}`);
      }
    }
    if (isTupleTypeNode(input) && getLineAndCharacterOfPosition(currentSourceFile, input.pos).line ***REMOVED******REMOVED******REMOVED*** getLineAndCharacterOfPosition(currentSourceFile, input.end).line) {
      setEmitFlags(input, 1 /* SingleLine */);
    }
    return cleanup(visitEachChild(input, visitDeclarationSubtree, context));
    function cleanup(returnValue) {
      if (returnValue && canProduceDiagnostic && hasDynamicName(input)) {
        checkName(input);
      }
      if (isEnclosingDeclaration(input)) {
        enclosingDeclaration ***REMOVED*** previousEnclosingDeclaration;
      }
      if (canProduceDiagnostic && !suppressNewDiagnosticContexts) {
        getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
      }
      if (shouldEnterSuppressNewDiagnosticsContextContext) {
        suppressNewDiagnosticContexts ***REMOVED*** oldWithinObjectLiteralType;
      }
      if (returnValue ***REMOVED******REMOVED******REMOVED*** input) {
        return returnValue;
      }
      return returnValue && setOriginalNode(preserveJsDoc(returnValue, input), input);
    }
  }
  function isPrivateMethodTypeParameter(node) {
    return node.parent.kind ***REMOVED******REMOVED******REMOVED*** 175 /* MethodDeclaration */ && hasEffectiveModifier(node.parent, 2 /* Private */);
  }
  function visitDeclarationStatements(input) {
    if (!isPreservedDeclarationStatement(input)) {
      return;
    }
    if (shouldStripInternal(input)) return;
    switch (input.kind) {
      case 279 /* ExportDeclaration */: {
        if (isSourceFile(input.parent)) {
          resultHasExternalModuleIndicator ***REMOVED*** true;
        }
        resultHasScopeMarker ***REMOVED*** true;
        return factory2.updateExportDeclaration(
          input,
          input.modifiers,
          input.isTypeOnly,
          input.exportClause,
          rewriteModuleSpecifier2(input, input.moduleSpecifier),
          tryGetResolutionModeOverride(input.attributes)
        );
      }
      case 278 /* ExportAssignment */: {
        if (isSourceFile(input.parent)) {
          resultHasExternalModuleIndicator ***REMOVED*** true;
        }
        resultHasScopeMarker ***REMOVED*** true;
        if (input.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
          return input;
        } else {
          const newId ***REMOVED*** factory2.createUniqueName("_default", 16 /* Optimistic */);
          getSymbolAccessibilityDiagnostic ***REMOVED*** () ***REMOVED***> ({
            diagnosticMessage: Diagnostics.Default_export_of_the_module_has_or_is_using_private_name_0,
            errorNode: input
          });
          errorFallbackNode ***REMOVED*** input;
          const type ***REMOVED*** ensureType(input);
          const varDecl ***REMOVED*** factory2.createVariableDeclaration(
            newId,
            /*exclamationToken*/
            void 0,
            type,
            /*initializer*/
            void 0
          );
          errorFallbackNode ***REMOVED*** void 0;
          const statement ***REMOVED*** factory2.createVariableStatement(needsDeclare ? [factory2.createModifier(138 /* DeclareKeyword */)] : [], factory2.createVariableDeclarationList([varDecl], 2 /* Const */));
          preserveJsDoc(statement, input);
          removeAllComments(input);
          return [statement, factory2.updateExportAssignment(input, input.modifiers, newId)];
        }
      }
    }
    const result ***REMOVED*** transformTopLevelDeclaration(input);
    lateStatementReplacementMap.set(getOriginalNodeId(input), result);
    return input;
  }
  function stripExportModifiers(statement) {
    if (isImportEqualsDeclaration(statement) || hasEffectiveModifier(statement, 2048 /* Default */) || !canHaveModifiers(statement)) {
      return statement;
    }
    const modifiers ***REMOVED*** factory2.createModifiersFromModifierFlags(getEffectiveModifierFlags(statement) & (131071 /* All */ ^ 32 /* Export */));
    return factory2.replaceModifiers(statement, modifiers);
  }
  function updateModuleDeclarationAndKeyword(node, modifiers, name, body) {
    const updated ***REMOVED*** factory2.updateModuleDeclaration(node, modifiers, name, body);
    if (isAmbientModule(updated) || updated.flags & 32 /* Namespace */) {
      return updated;
    }
    const fixed ***REMOVED*** factory2.createModuleDeclaration(
      updated.modifiers,
      updated.name,
      updated.body,
      updated.flags | 32 /* Namespace */
    );
    setOriginalNode(fixed, updated);
    setTextRange(fixed, updated);
    return fixed;
  }
  function transformTopLevelDeclaration(input) {
    if (lateMarkedStatements) {
      while (orderedRemoveItem(lateMarkedStatements, input)) ;
    }
    if (shouldStripInternal(input)) return;
    switch (input.kind) {
      case 272 /* ImportEqualsDeclaration */: {
        return transformImportEqualsDeclaration(input);
      }
      case 273 /* ImportDeclaration */: {
        return transformImportDeclaration(input);
      }
    }
    if (isDeclaration(input) && isDeclarationAndNotVisible(input)) return;
    if (isJSDocImportTag(input)) return;
    if (isFunctionLike(input) && resolver.isImplementationOfOverload(input)) return;
    let previousEnclosingDeclaration;
    if (isEnclosingDeclaration(input)) {
      previousEnclosingDeclaration ***REMOVED*** enclosingDeclaration;
      enclosingDeclaration ***REMOVED*** input;
    }
    const canProdiceDiagnostic ***REMOVED*** canProduceDiagnostics(input);
    const oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
    if (canProdiceDiagnostic) {
      getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(input);
    }
    const previousNeedsDeclare ***REMOVED*** needsDeclare;
    switch (input.kind) {
      case 266 /* TypeAliasDeclaration */: {
        needsDeclare ***REMOVED*** false;
        const clean2 ***REMOVED*** cleanup(factory2.updateTypeAliasDeclaration(
          input,
          ensureModifiers(input),
          input.name,
          visitNodes2(input.typeParameters, visitDeclarationSubtree, isTypeParameterDeclaration),
          Debug.checkDefined(visitNode(input.type, visitDeclarationSubtree, isTypeNode))
        ));
        needsDeclare ***REMOVED*** previousNeedsDeclare;
        return clean2;
      }
      case 265 /* InterfaceDeclaration */: {
        return cleanup(factory2.updateInterfaceDeclaration(
          input,
          ensureModifiers(input),
          input.name,
          ensureTypeParams(input, input.typeParameters),
          transformHeritageClauses(input.heritageClauses),
          visitNodes2(input.members, visitDeclarationSubtree, isTypeElement)
        ));
      }
      case 263 /* FunctionDeclaration */: {
        const clean2 ***REMOVED*** cleanup(factory2.updateFunctionDeclaration(
          input,
          ensureModifiers(input),
          /*asteriskToken*/
          void 0,
          input.name,
          ensureTypeParams(input, input.typeParameters),
          updateParamsList(input, input.parameters),
          ensureType(input),
          /*body*/
          void 0
        ));
        if (clean2 && resolver.isExpandoFunctionDeclaration(input) && shouldEmitFunctionProperties(input)) {
          const props ***REMOVED*** resolver.getPropertiesOfContainerFunction(input);
          if (isolatedDeclarations) {
            reportExpandoFunctionErrors(input);
          }
          const fakespace ***REMOVED*** parseNodeFactory.createModuleDeclaration(
            /*modifiers*/
            void 0,
            clean2.name || factory2.createIdentifier("_default"),
            factory2.createModuleBlock([]),
            32 /* Namespace */
          );
          setParent(fakespace, enclosingDeclaration);
          fakespace.locals ***REMOVED*** createSymbolTable(props);
          fakespace.symbol ***REMOVED*** props[0].parent;
          const exportMappings ***REMOVED*** [];
          let declarations ***REMOVED*** mapDefined(props, (p) ***REMOVED***> {
            if (!isExpandoPropertyDeclaration(p.valueDeclaration)) {
              return void 0;
            }
            const nameStr ***REMOVED*** unescapeLeadingUnderscores(p.escapedName);
            if (!isIdentifierText(nameStr, 99 /* ESNext */)) {
              return void 0;
            }
            getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(p.valueDeclaration);
            const type ***REMOVED*** resolver.createTypeOfDeclaration(p.valueDeclaration, fakespace, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags | 2 /* NoSyntacticPrinter */, symbolTracker);
            getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
            const isNonContextualKeywordName ***REMOVED*** isStringANonContextualKeyword(nameStr);
            const name ***REMOVED*** isNonContextualKeywordName ? factory2.getGeneratedNameForNode(p.valueDeclaration) : factory2.createIdentifier(nameStr);
            if (isNonContextualKeywordName) {
              exportMappings.push([name, nameStr]);
            }
            const varDecl ***REMOVED*** factory2.createVariableDeclaration(
              name,
              /*exclamationToken*/
              void 0,
              type,
              /*initializer*/
              void 0
            );
            return factory2.createVariableStatement(isNonContextualKeywordName ? void 0 : [factory2.createToken(95 /* ExportKeyword */)], factory2.createVariableDeclarationList([varDecl]));
          });
          if (!exportMappings.length) {
            declarations ***REMOVED*** mapDefined(declarations, (declaration) ***REMOVED***> factory2.replaceModifiers(declaration, 0 /* None */));
          } else {
            declarations.push(factory2.createExportDeclaration(
              /*modifiers*/
              void 0,
              /*isTypeOnly*/
              false,
              factory2.createNamedExports(map(exportMappings, ([gen, exp]) ***REMOVED***> {
                return factory2.createExportSpecifier(
                  /*isTypeOnly*/
                  false,
                  gen,
                  exp
                );
              }))
            ));
          }
          const namespaceDecl ***REMOVED*** factory2.createModuleDeclaration(ensureModifiers(input), input.name, factory2.createModuleBlock(declarations), 32 /* Namespace */);
          if (!hasEffectiveModifier(clean2, 2048 /* Default */)) {
            return [clean2, namespaceDecl];
          }
          const modifiers ***REMOVED*** factory2.createModifiersFromModifierFlags(getEffectiveModifierFlags(clean2) & ~2080 /* ExportDefault */ | 128 /* Ambient */);
          const cleanDeclaration ***REMOVED*** factory2.updateFunctionDeclaration(
            clean2,
            modifiers,
            /*asteriskToken*/
            void 0,
            clean2.name,
            clean2.typeParameters,
            clean2.parameters,
            clean2.type,
            /*body*/
            void 0
          );
          const namespaceDeclaration ***REMOVED*** factory2.updateModuleDeclaration(
            namespaceDecl,
            modifiers,
            namespaceDecl.name,
            namespaceDecl.body
          );
          const exportDefaultDeclaration ***REMOVED*** factory2.createExportAssignment(
            /*modifiers*/
            void 0,
            /*isExportEquals*/
            false,
            namespaceDecl.name
          );
          if (isSourceFile(input.parent)) {
            resultHasExternalModuleIndicator ***REMOVED*** true;
          }
          resultHasScopeMarker ***REMOVED*** true;
          return [cleanDeclaration, namespaceDeclaration, exportDefaultDeclaration];
        } else {
          return clean2;
        }
      }
      case 268 /* ModuleDeclaration */: {
        needsDeclare ***REMOVED*** false;
        const inner ***REMOVED*** input.body;
        if (inner && inner.kind ***REMOVED******REMOVED******REMOVED*** 269 /* ModuleBlock */) {
          const oldNeedsScopeFix ***REMOVED*** needsScopeFixMarker;
          const oldHasScopeFix ***REMOVED*** resultHasScopeMarker;
          resultHasScopeMarker ***REMOVED*** false;
          needsScopeFixMarker ***REMOVED*** false;
          const statements ***REMOVED*** visitNodes2(inner.statements, visitDeclarationStatements, isStatement);
          let lateStatements ***REMOVED*** transformAndReplaceLatePaintedStatements(statements);
          if (input.flags & 33554432 /* Ambient */) {
            needsScopeFixMarker ***REMOVED*** false;
          }
          if (!isGlobalScopeAugmentation(input) && !hasScopeMarker2(lateStatements) && !resultHasScopeMarker) {
            if (needsScopeFixMarker) {
              lateStatements ***REMOVED*** factory2.createNodeArray([...lateStatements, createEmptyExports(factory2)]);
            } else {
              lateStatements ***REMOVED*** visitNodes2(lateStatements, stripExportModifiers, isStatement);
            }
          }
          const body ***REMOVED*** factory2.updateModuleBlock(inner, lateStatements);
          needsDeclare ***REMOVED*** previousNeedsDeclare;
          needsScopeFixMarker ***REMOVED*** oldNeedsScopeFix;
          resultHasScopeMarker ***REMOVED*** oldHasScopeFix;
          const mods ***REMOVED*** ensureModifiers(input);
          return cleanup(updateModuleDeclarationAndKeyword(
            input,
            mods,
            isExternalModuleAugmentation(input) ? rewriteModuleSpecifier2(input, input.name) : input.name,
            body
          ));
        } else {
          needsDeclare ***REMOVED*** previousNeedsDeclare;
          const mods ***REMOVED*** ensureModifiers(input);
          needsDeclare ***REMOVED*** false;
          visitNode(inner, visitDeclarationStatements);
          const id ***REMOVED*** getOriginalNodeId(inner);
          const body ***REMOVED*** lateStatementReplacementMap.get(id);
          lateStatementReplacementMap.delete(id);
          return cleanup(updateModuleDeclarationAndKeyword(
            input,
            mods,
            input.name,
            body
          ));
        }
      }
      case 264 /* ClassDeclaration */: {
        errorNameNode ***REMOVED*** input.name;
        errorFallbackNode ***REMOVED*** input;
        const modifiers ***REMOVED*** factory2.createNodeArray(ensureModifiers(input));
        const typeParameters ***REMOVED*** ensureTypeParams(input, input.typeParameters);
        const ctor ***REMOVED*** getFirstConstructorWithBody(input);
        let parameterProperties;
        if (ctor) {
          const oldDiag2 ***REMOVED*** getSymbolAccessibilityDiagnostic;
          parameterProperties ***REMOVED*** compact(flatMap(ctor.parameters, (param) ***REMOVED***> {
            if (!hasSyntacticModifier(param, 31 /* ParameterPropertyModifier */) || shouldStripInternal(param)) return;
            getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(param);
            if (param.name.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
              return preserveJsDoc(
                factory2.createPropertyDeclaration(
                  ensureModifiers(param),
                  param.name,
                  param.questionToken,
                  ensureType(param),
                  ensureNoInitializer(param)
                ),
                param
              );
            } else {
              return walkBindingPattern(param.name);
            }
            function walkBindingPattern(pattern) {
              let elems;
              for (const elem of pattern.elements) {
                if (isOmittedExpression(elem)) continue;
                if (isBindingPattern(elem.name)) {
                  elems ***REMOVED*** concatenate(elems, walkBindingPattern(elem.name));
                }
                elems ***REMOVED*** elems || [];
                elems.push(factory2.createPropertyDeclaration(
                  ensureModifiers(param),
                  elem.name,
                  /*questionOrExclamationToken*/
                  void 0,
                  ensureType(elem),
                  /*initializer*/
                  void 0
                ));
              }
              return elems;
            }
          }));
          getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag2;
        }
        const hasPrivateIdentifier ***REMOVED*** some(input.members, (member) ***REMOVED***> !!member.name && isPrivateIdentifier(member.name));
        const privateIdentifier ***REMOVED*** hasPrivateIdentifier ? [
          factory2.createPropertyDeclaration(
            /*modifiers*/
            void 0,
            factory2.createPrivateIdentifier("#private"),
            /*questionOrExclamationToken*/
            void 0,
            /*type*/
            void 0,
            /*initializer*/
            void 0
          )
        ] : void 0;
        const lateIndexes ***REMOVED*** resolver.createLateBoundIndexSignatures(input, enclosingDeclaration, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags, symbolTracker);
        const memberNodes ***REMOVED*** concatenate(concatenate(concatenate(privateIdentifier, lateIndexes), parameterProperties), visitNodes2(input.members, visitDeclarationSubtree, isClassElement));
        const members ***REMOVED*** factory2.createNodeArray(memberNodes);
        const extendsClause ***REMOVED*** getEffectiveBaseTypeNode(input);
        if (extendsClause && !isEntityNameExpression(extendsClause.expression) && extendsClause.expression.kind !***REMOVED******REMOVED*** 106 /* NullKeyword */) {
          const oldId ***REMOVED*** input.name ? unescapeLeadingUnderscores(input.name.escapedText) : "default";
          const newId ***REMOVED*** factory2.createUniqueName(`${oldId}_base`, 16 /* Optimistic */);
          getSymbolAccessibilityDiagnostic ***REMOVED*** () ***REMOVED***> ({
            diagnosticMessage: Diagnostics.extends_clause_of_exported_class_0_has_or_is_using_private_name_1,
            errorNode: extendsClause,
            typeName: input.name
          });
          const varDecl ***REMOVED*** factory2.createVariableDeclaration(
            newId,
            /*exclamationToken*/
            void 0,
            resolver.createTypeOfExpression(extendsClause.expression, input, declarationEmitNodeBuilderFlags, declarationEmitInternalNodeBuilderFlags, symbolTracker),
            /*initializer*/
            void 0
          );
          const statement ***REMOVED*** factory2.createVariableStatement(needsDeclare ? [factory2.createModifier(138 /* DeclareKeyword */)] : [], factory2.createVariableDeclarationList([varDecl], 2 /* Const */));
          const heritageClauses ***REMOVED*** factory2.createNodeArray(map(input.heritageClauses, (clause) ***REMOVED***> {
            if (clause.token ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */) {
              const oldDiag2 ***REMOVED*** getSymbolAccessibilityDiagnostic;
              getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNode(clause.types[0]);
              const newClause ***REMOVED*** factory2.updateHeritageClause(clause, map(clause.types, (t) ***REMOVED***> factory2.updateExpressionWithTypeArguments(t, newId, visitNodes2(t.typeArguments, visitDeclarationSubtree, isTypeNode))));
              getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag2;
              return newClause;
            }
            return factory2.updateHeritageClause(clause, visitNodes2(factory2.createNodeArray(filter(clause.types, (t) ***REMOVED***> isEntityNameExpression(t.expression) || t.expression.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */)), visitDeclarationSubtree, isExpressionWithTypeArguments));
          }));
          return [
            statement,
            cleanup(factory2.updateClassDeclaration(
              input,
              modifiers,
              input.name,
              typeParameters,
              heritageClauses,
              members
            ))
          ];
        } else {
          const heritageClauses ***REMOVED*** transformHeritageClauses(input.heritageClauses);
          return cleanup(factory2.updateClassDeclaration(
            input,
            modifiers,
            input.name,
            typeParameters,
            heritageClauses,
            members
          ));
        }
      }
      case 244 /* VariableStatement */: {
        return cleanup(transformVariableStatement(input));
      }
      case 267 /* EnumDeclaration */: {
        return cleanup(factory2.updateEnumDeclaration(
          input,
          factory2.createNodeArray(ensureModifiers(input)),
          input.name,
          factory2.createNodeArray(mapDefined(input.members, (m) ***REMOVED***> {
            if (shouldStripInternal(m)) return;
            const enumValue ***REMOVED*** resolver.getEnumMemberValue(m);
            const constValue ***REMOVED*** enumValue ***REMOVED******REMOVED*** null ? void 0 : enumValue.value;
            if (isolatedDeclarations && m.initializer && (enumValue ***REMOVED******REMOVED*** null ? void 0 : enumValue.hasExternalReferences) && // This will be its own compiler error instead, so don't report.
            !isComputedPropertyName(m.name)) {
              context.addDiagnostic(createDiagnosticForNode(m, Diagnostics.Enum_member_initializers_must_be_computable_without_references_to_external_symbols_with_isolatedDeclarations));
            }
            const newInitializer ***REMOVED*** constValue ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : typeof constValue ***REMOVED******REMOVED******REMOVED*** "string" ? factory2.createStringLiteral(constValue) : constValue < 0 ? factory2.createPrefixUnaryExpression(41 /* MinusToken */, factory2.createNumericLiteral(-constValue)) : factory2.createNumericLiteral(constValue);
            return preserveJsDoc(factory2.updateEnumMember(m, m.name, newInitializer), m);
          }))
        ));
      }
    }
    return Debug.assertNever(input, `Unhandled top-level node in declaration emit: ${Debug.formatSyntaxKind(input.kind)}`);
    function cleanup(node) {
      if (isEnclosingDeclaration(input)) {
        enclosingDeclaration ***REMOVED*** previousEnclosingDeclaration;
      }
      if (canProdiceDiagnostic) {
        getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
      }
      if (input.kind ***REMOVED******REMOVED******REMOVED*** 268 /* ModuleDeclaration */) {
        needsDeclare ***REMOVED*** previousNeedsDeclare;
      }
      if (node ***REMOVED******REMOVED******REMOVED*** input) {
        return node;
      }
      errorFallbackNode ***REMOVED*** void 0;
      errorNameNode ***REMOVED*** void 0;
      return node && setOriginalNode(preserveJsDoc(node, input), input);
    }
  }
  function transformVariableStatement(input) {
    if (!forEach(input.declarationList.declarations, getBindingNameVisible)) return;
    const nodes ***REMOVED*** visitNodes2(input.declarationList.declarations, visitDeclarationSubtree, isVariableDeclaration);
    if (!length(nodes)) return;
    const modifiers ***REMOVED*** factory2.createNodeArray(ensureModifiers(input));
    let declList;
    if (isVarUsing(input.declarationList) || isVarAwaitUsing(input.declarationList)) {
      declList ***REMOVED*** factory2.createVariableDeclarationList(nodes, 2 /* Const */);
      setOriginalNode(declList, input.declarationList);
      setTextRange(declList, input.declarationList);
      setCommentRange(declList, input.declarationList);
    } else {
      declList ***REMOVED*** factory2.updateVariableDeclarationList(input.declarationList, nodes);
    }
    return factory2.updateVariableStatement(input, modifiers, declList);
  }
  function recreateBindingPattern(d) {
    return flatten(mapDefined(d.elements, (e) ***REMOVED***> recreateBindingElement(e)));
  }
  function recreateBindingElement(e) {
    if (e.kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */) {
      return;
    }
    if (e.name) {
      if (!getBindingNameVisible(e)) return;
      if (isBindingPattern(e.name)) {
        return recreateBindingPattern(e.name);
      } else {
        return factory2.createVariableDeclaration(
          e.name,
          /*exclamationToken*/
          void 0,
          ensureType(e),
          /*initializer*/
          void 0
        );
      }
    }
  }
  function checkName(node) {
    let oldDiag;
    if (!suppressNewDiagnosticContexts) {
      oldDiag ***REMOVED*** getSymbolAccessibilityDiagnostic;
      getSymbolAccessibilityDiagnostic ***REMOVED*** createGetSymbolAccessibilityDiagnosticForNodeName(node);
    }
    errorNameNode ***REMOVED*** node.name;
    Debug.assert(hasDynamicName(node));
    const decl ***REMOVED*** node;
    const entityName ***REMOVED*** decl.name.expression;
    checkEntityNameVisibility(entityName, enclosingDeclaration);
    if (!suppressNewDiagnosticContexts) {
      getSymbolAccessibilityDiagnostic ***REMOVED*** oldDiag;
    }
    errorNameNode ***REMOVED*** void 0;
  }
  function shouldStripInternal(node) {
    return !!stripInternal && !!node && isInternalDeclaration(node, currentSourceFile);
  }
  function isScopeMarker2(node) {
    return isExportAssignment(node) || isExportDeclaration(node);
  }
  function hasScopeMarker2(statements) {
    return some(statements, isScopeMarker2);
  }
  function ensureModifiers(node) {
    const currentFlags ***REMOVED*** getEffectiveModifierFlags(node);
    const newFlags ***REMOVED*** ensureModifierFlags(node);
    if (currentFlags ***REMOVED******REMOVED******REMOVED*** newFlags) {
      return visitArray(node.modifiers, (n) ***REMOVED***> tryCast(n, isModifier), isModifier);
    }
    return factory2.createModifiersFromModifierFlags(newFlags);
  }
  function ensureModifierFlags(node) {
    let mask ***REMOVED*** 131071 /* All */ ^ (1 /* Public */ | 1024 /* Async */ | 16 /* Override */);
    let additions ***REMOVED*** needsDeclare && !isAlwaysType(node) ? 128 /* Ambient */ : 0 /* None */;
    const parentIsFile ***REMOVED*** node.parent.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */;
    if (!parentIsFile || isBundledEmit && parentIsFile && isExternalModule(node.parent)) {
      mask ^***REMOVED*** 128 /* Ambient */;
      additions ***REMOVED*** 0 /* None */;
    }
    return maskModifierFlags(node, mask, additions);
  }
  function transformHeritageClauses(nodes) {
    return factory2.createNodeArray(filter(
      map(nodes, (clause) ***REMOVED***> factory2.updateHeritageClause(
        clause,
        visitNodes2(
          factory2.createNodeArray(filter(clause.types, (t) ***REMOVED***> {
            return isEntityNameExpression(t.expression) || clause.token ***REMOVED******REMOVED******REMOVED*** 96 /* ExtendsKeyword */ && t.expression.kind ***REMOVED******REMOVED******REMOVED*** 106 /* NullKeyword */;
          })),
          visitDeclarationSubtree,
          isExpressionWithTypeArguments
        )
      )),
      (clause) ***REMOVED***> clause.types && !!clause.types.length
    ));
  }
}
function isAlwaysType(node) {
  if (node.kind ***REMOVED******REMOVED******REMOVED*** 265 /* InterfaceDeclaration */) {
    return true;
  }
  return false;
}
function maskModifiers(factory2, node, modifierMask, modifierAdditions) {
  return factory2.createModifiersFromModifierFlags(maskModifierFlags(node, modifierMask, modifierAdditions));
}
function maskModifierFlags(node, modifierMask ***REMOVED*** 131071 /* All */ ^ 1 /* Public */, modifierAdditions ***REMOVED*** 0 /* None */) {
  let flags ***REMOVED*** getEffectiveModifierFlags(node) & modifierMask | modifierAdditions;
  if (flags & 2048 /* Default */ && !(flags & 32 /* Export */)) {
    flags ^***REMOVED*** 32 /* Export */;
  }
  if (flags & 2048 /* Default */ && flags & 128 /* Ambient */) {
    flags ^***REMOVED*** 128 /* Ambient */;
  }
  return flags;
}
function canHaveLiteralInitializer(node) {
  switch (node.kind) {
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
      return !hasEffectiveModifier(node, 2 /* Private */);
    case 170 /* Parameter */:
    case 261 /* VariableDeclaration */:
      return true;
  }
  return false;
}
function isPreservedDeclarationStatement(node) {
  switch (node.kind) {
    case 263 /* FunctionDeclaration */:
    case 268 /* ModuleDeclaration */:
    case 272 /* ImportEqualsDeclaration */:
    case 265 /* InterfaceDeclaration */:
    case 264 /* ClassDeclaration */:
    case 266 /* TypeAliasDeclaration */:
    case 267 /* EnumDeclaration */:
    case 244 /* VariableStatement */:
    case 273 /* ImportDeclaration */:
    case 279 /* ExportDeclaration */:
    case 278 /* ExportAssignment */:
      return true;
  }
  return false;
}
function isProcessedComponent(node) {
  switch (node.kind) {
    case 181 /* ConstructSignature */:
    case 177 /* Constructor */:
    case 175 /* MethodDeclaration */:
    case 178 /* GetAccessor */:
    case 179 /* SetAccessor */:
    case 173 /* PropertyDeclaration */:
    case 172 /* PropertySignature */:
    case 174 /* MethodSignature */:
    case 180 /* CallSignature */:
    case 182 /* IndexSignature */:
    case 261 /* VariableDeclaration */:
    case 169 /* TypeParameter */:
    case 234 /* ExpressionWithTypeArguments */:
    case 184 /* TypeReference */:
    case 195 /* ConditionalType */:
    case 185 /* FunctionType */:
    case 186 /* ConstructorType */:
    case 206 /* ImportType */:
      return true;
  }
  return false;
}

// src/compiler/transformer.ts
function getModuleTransformer(moduleKind) {
  switch (moduleKind) {
    case 200 /* Preserve */:
      return transformECMAScriptModule;
    case 99 /* ESNext */:
    case 7 /* ES2022 */:
    case 6 /* ES2020 */:
    case 5 /* ES2015 */:
    case 100 /* Node16 */:
    case 101 /* Node18 */:
    case 102 /* Node20 */:
    case 199 /* NodeNext */:
    case 1 /* CommonJS */:
      return transformImpliedNodeFormatDependentModule;
    case 4 /* System */:
      return transformSystemModule;
    default:
      return transformModule;
  }
}
var noTransformers ***REMOVED*** { scriptTransformers: emptyArray, declarationTransformers: emptyArray };
function getTransformers(compilerOptions, customTransformers, emitOnly) {
  return {
    scriptTransformers: getScriptTransformers(compilerOptions, customTransformers, emitOnly),
    declarationTransformers: getDeclarationTransformers(customTransformers)
  };
}
function getScriptTransformers(compilerOptions, customTransformers, emitOnly) {
  if (emitOnly) return emptyArray;
  const languageVersion ***REMOVED*** getEmitScriptTarget(compilerOptions);
  const moduleKind ***REMOVED*** getEmitModuleKind(compilerOptions);
  const useDefineForClassFields ***REMOVED*** getUseDefineForClassFields(compilerOptions);
  const transformers ***REMOVED*** [];
  addRange(transformers, customTransformers && map(customTransformers.before, wrapScriptTransformerFactory));
  transformers.push(transformTypeScript);
  if (compilerOptions.experimentalDecorators) {
    transformers.push(transformLegacyDecorators);
  }
  if (getJSXTransformEnabled(compilerOptions)) {
    transformers.push(transformJsx);
  }
  if (languageVersion < 99 /* ESNext */) {
    transformers.push(transformESNext);
  }
  if (!compilerOptions.experimentalDecorators && (languageVersion < 99 /* ESNext */ || !useDefineForClassFields)) {
    transformers.push(transformESDecorators);
  }
  transformers.push(transformClassFields);
  if (languageVersion < 8 /* ES2021 */) {
    transformers.push(transformES2021);
  }
  if (languageVersion < 7 /* ES2020 */) {
    transformers.push(transformES2020);
  }
  if (languageVersion < 6 /* ES2019 */) {
    transformers.push(transformES2019);
  }
  if (languageVersion < 5 /* ES2018 */) {
    transformers.push(transformES2018);
  }
  if (languageVersion < 4 /* ES2017 */) {
    transformers.push(transformES2017);
  }
  if (languageVersion < 3 /* ES2016 */) {
    transformers.push(transformES2016);
  }
  if (languageVersion < 2 /* ES2015 */) {
    transformers.push(transformES2015);
    transformers.push(transformGenerators);
  }
  transformers.push(getModuleTransformer(moduleKind));
  addRange(transformers, customTransformers && map(customTransformers.after, wrapScriptTransformerFactory));
  return transformers;
}
function getDeclarationTransformers(customTransformers) {
  const transformers ***REMOVED*** [];
  transformers.push(transformDeclarations);
  addRange(transformers, customTransformers && map(customTransformers.afterDeclarations, wrapDeclarationTransformerFactory));
  return transformers;
}
function wrapCustomTransformer(transformer) {
  return (node) ***REMOVED***> isBundle(node) ? transformer.transformBundle(node) : transformer.transformSourceFile(node);
}
function wrapCustomTransformerFactory(transformer, handleDefault) {
  return (context) ***REMOVED***> {
    const customTransformer ***REMOVED*** transformer(context);
    return typeof customTransformer ***REMOVED******REMOVED******REMOVED*** "function" ? handleDefault(context, customTransformer) : wrapCustomTransformer(customTransformer);
  };
}
function wrapScriptTransformerFactory(transformer) {
  return wrapCustomTransformerFactory(transformer, chainBundle);
}
function wrapDeclarationTransformerFactory(transformer) {
  return wrapCustomTransformerFactory(transformer, (_, node) ***REMOVED***> node);
}
function noEmitSubstitution(_hint, node) {
  return node;
}
function noEmitNotification(hint, node, callback) {
  callback(hint, node);
}
function transformNodes(resolver, host, factory2, options, nodes, transformers, allowDtsFiles) {
  var _a, _b;
  const enabledSyntaxKindFeatures ***REMOVED*** new Array(359 /* Count */);
  let lexicalEnvironmentVariableDeclarations;
  let lexicalEnvironmentFunctionDeclarations;
  let lexicalEnvironmentStatements;
  let lexicalEnvironmentFlags ***REMOVED*** 0 /* None */;
  let lexicalEnvironmentVariableDeclarationsStack ***REMOVED*** [];
  let lexicalEnvironmentFunctionDeclarationsStack ***REMOVED*** [];
  let lexicalEnvironmentStatementsStack ***REMOVED*** [];
  let lexicalEnvironmentFlagsStack ***REMOVED*** [];
  let lexicalEnvironmentStackOffset ***REMOVED*** 0;
  let lexicalEnvironmentSuspended ***REMOVED*** false;
  let blockScopedVariableDeclarationsStack ***REMOVED*** [];
  let blockScopeStackOffset ***REMOVED*** 0;
  let blockScopedVariableDeclarations;
  let emitHelpers;
  let onSubstituteNode ***REMOVED*** noEmitSubstitution;
  let onEmitNode ***REMOVED*** noEmitNotification;
  let state ***REMOVED*** 0 /* Uninitialized */;
  const diagnostics ***REMOVED*** [];
  const context ***REMOVED*** {
    factory: factory2,
    getCompilerOptions: () ***REMOVED***> options,
    getEmitResolver: () ***REMOVED***> resolver,
    // TODO: GH#18217
    getEmitHost: () ***REMOVED***> host,
    // TODO: GH#18217
    getEmitHelperFactory: memoize(() ***REMOVED***> createEmitHelperFactory(context)),
    startLexicalEnvironment,
    suspendLexicalEnvironment,
    resumeLexicalEnvironment,
    endLexicalEnvironment,
    setLexicalEnvironmentFlags,
    getLexicalEnvironmentFlags,
    hoistVariableDeclaration,
    hoistFunctionDeclaration,
    addInitializationStatement,
    startBlockScope,
    endBlockScope,
    addBlockScopedVariable,
    requestEmitHelper,
    readEmitHelpers,
    enableSubstitution,
    enableEmitNotification,
    isSubstitutionEnabled,
    isEmitNotificationEnabled,
    get onSubstituteNode() {
      return onSubstituteNode;
    },
    set onSubstituteNode(value) {
      Debug.assert(state < 1 /* Initialized */, "Cannot modify transformation hooks after initialization has completed.");
      Debug.assert(value !***REMOVED******REMOVED*** void 0, "Value must not be 'undefined'");
      onSubstituteNode ***REMOVED*** value;
    },
    get onEmitNode() {
      return onEmitNode;
    },
    set onEmitNode(value) {
      Debug.assert(state < 1 /* Initialized */, "Cannot modify transformation hooks after initialization has completed.");
      Debug.assert(value !***REMOVED******REMOVED*** void 0, "Value must not be 'undefined'");
      onEmitNode ***REMOVED*** value;
    },
    addDiagnostic(diag2) {
      diagnostics.push(diag2);
    }
  };
  for (const node of nodes) {
    disposeEmitNodes(getSourceFileOfNode(getParseTreeNode(node)));
  }
  mark("beforeTransform");
  const transformersWithContext ***REMOVED*** transformers.map((t) ***REMOVED***> t(context));
  const transformation ***REMOVED*** (node) ***REMOVED***> {
    for (const transform of transformersWithContext) {
      node ***REMOVED*** transform(node);
    }
    return node;
  };
  state ***REMOVED*** 1 /* Initialized */;
  const transformed ***REMOVED*** [];
  for (const node of nodes) {
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Emit, "transformNodes", node.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? { path: node.path } : { kind: node.kind, pos: node.pos, end: node.end });
    transformed.push((allowDtsFiles ? transformation : transformRoot)(node));
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
  }
  state ***REMOVED*** 2 /* Completed */;
  mark("afterTransform");
  measure("transformTime", "beforeTransform", "afterTransform");
  return {
    transformed,
    substituteNode,
    emitNodeWithNotification,
    isEmitNotificationEnabled,
    dispose,
    diagnostics
  };
  function transformRoot(node) {
    return node && (!isSourceFile(node) || !node.isDeclarationFile) ? transformation(node) : node;
  }
  function enableSubstitution(kind) {
    Debug.assert(state < 2 /* Completed */, "Cannot modify the transformation context after transformation has completed.");
    enabledSyntaxKindFeatures[kind] |***REMOVED*** 1 /* Substitution */;
  }
  function isSubstitutionEnabled(node) {
    return (enabledSyntaxKindFeatures[node.kind] & 1 /* Substitution */) !***REMOVED******REMOVED*** 0 && (getEmitFlags(node) & 8 /* NoSubstitution */) ***REMOVED******REMOVED******REMOVED*** 0;
  }
  function substituteNode(hint, node) {
    Debug.assert(state < 3 /* Disposed */, "Cannot substitute a node after the result is disposed.");
    return node && isSubstitutionEnabled(node) && onSubstituteNode(hint, node) || node;
  }
  function enableEmitNotification(kind) {
    Debug.assert(state < 2 /* Completed */, "Cannot modify the transformation context after transformation has completed.");
    enabledSyntaxKindFeatures[kind] |***REMOVED*** 2 /* EmitNotifications */;
  }
  function isEmitNotificationEnabled(node) {
    return (enabledSyntaxKindFeatures[node.kind] & 2 /* EmitNotifications */) !***REMOVED******REMOVED*** 0 || (getEmitFlags(node) & 4 /* AdviseOnEmitNode */) !***REMOVED******REMOVED*** 0;
  }
  function emitNodeWithNotification(hint, node, emitCallback) {
    Debug.assert(state < 3 /* Disposed */, "Cannot invoke TransformationResult callbacks after the result is disposed.");
    if (node) {
      if (isEmitNotificationEnabled(node)) {
        onEmitNode(hint, node, emitCallback);
      } else {
        emitCallback(hint, node);
      }
    }
  }
  function hoistVariableDeclaration(name) {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    const decl ***REMOVED*** setEmitFlags(factory2.createVariableDeclaration(name), 128 /* NoNestedSourceMaps */);
    if (!lexicalEnvironmentVariableDeclarations) {
      lexicalEnvironmentVariableDeclarations ***REMOVED*** [decl];
    } else {
      lexicalEnvironmentVariableDeclarations.push(decl);
    }
    if (lexicalEnvironmentFlags & 1 /* InParameters */) {
      lexicalEnvironmentFlags |***REMOVED*** 2 /* VariablesHoistedInParameters */;
    }
  }
  function hoistFunctionDeclaration(func) {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    setEmitFlags(func, 2097152 /* CustomPrologue */);
    if (!lexicalEnvironmentFunctionDeclarations) {
      lexicalEnvironmentFunctionDeclarations ***REMOVED*** [func];
    } else {
      lexicalEnvironmentFunctionDeclarations.push(func);
    }
  }
  function addInitializationStatement(node) {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    setEmitFlags(node, 2097152 /* CustomPrologue */);
    if (!lexicalEnvironmentStatements) {
      lexicalEnvironmentStatements ***REMOVED*** [node];
    } else {
      lexicalEnvironmentStatements.push(node);
    }
  }
  function startLexicalEnvironment() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    Debug.assert(!lexicalEnvironmentSuspended, "Lexical environment is suspended.");
    lexicalEnvironmentVariableDeclarationsStack[lexicalEnvironmentStackOffset] ***REMOVED*** lexicalEnvironmentVariableDeclarations;
    lexicalEnvironmentFunctionDeclarationsStack[lexicalEnvironmentStackOffset] ***REMOVED*** lexicalEnvironmentFunctionDeclarations;
    lexicalEnvironmentStatementsStack[lexicalEnvironmentStackOffset] ***REMOVED*** lexicalEnvironmentStatements;
    lexicalEnvironmentFlagsStack[lexicalEnvironmentStackOffset] ***REMOVED*** lexicalEnvironmentFlags;
    lexicalEnvironmentStackOffset++;
    lexicalEnvironmentVariableDeclarations ***REMOVED*** void 0;
    lexicalEnvironmentFunctionDeclarations ***REMOVED*** void 0;
    lexicalEnvironmentStatements ***REMOVED*** void 0;
    lexicalEnvironmentFlags ***REMOVED*** 0 /* None */;
  }
  function suspendLexicalEnvironment() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    Debug.assert(!lexicalEnvironmentSuspended, "Lexical environment is already suspended.");
    lexicalEnvironmentSuspended ***REMOVED*** true;
  }
  function resumeLexicalEnvironment() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    Debug.assert(lexicalEnvironmentSuspended, "Lexical environment is not suspended.");
    lexicalEnvironmentSuspended ***REMOVED*** false;
  }
  function endLexicalEnvironment() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the lexical environment during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the lexical environment after transformation has completed.");
    Debug.assert(!lexicalEnvironmentSuspended, "Lexical environment is suspended.");
    let statements;
    if (lexicalEnvironmentVariableDeclarations || lexicalEnvironmentFunctionDeclarations || lexicalEnvironmentStatements) {
      if (lexicalEnvironmentFunctionDeclarations) {
        statements ***REMOVED*** [...lexicalEnvironmentFunctionDeclarations];
      }
      if (lexicalEnvironmentVariableDeclarations) {
        const statement ***REMOVED*** factory2.createVariableStatement(
          /*modifiers*/
          void 0,
          factory2.createVariableDeclarationList(lexicalEnvironmentVariableDeclarations)
        );
        setEmitFlags(statement, 2097152 /* CustomPrologue */);
        if (!statements) {
          statements ***REMOVED*** [statement];
        } else {
          statements.push(statement);
        }
      }
      if (lexicalEnvironmentStatements) {
        if (!statements) {
          statements ***REMOVED*** [...lexicalEnvironmentStatements];
        } else {
          statements ***REMOVED*** [...statements, ...lexicalEnvironmentStatements];
        }
      }
    }
    lexicalEnvironmentStackOffset--;
    lexicalEnvironmentVariableDeclarations ***REMOVED*** lexicalEnvironmentVariableDeclarationsStack[lexicalEnvironmentStackOffset];
    lexicalEnvironmentFunctionDeclarations ***REMOVED*** lexicalEnvironmentFunctionDeclarationsStack[lexicalEnvironmentStackOffset];
    lexicalEnvironmentStatements ***REMOVED*** lexicalEnvironmentStatementsStack[lexicalEnvironmentStackOffset];
    lexicalEnvironmentFlags ***REMOVED*** lexicalEnvironmentFlagsStack[lexicalEnvironmentStackOffset];
    if (lexicalEnvironmentStackOffset ***REMOVED******REMOVED******REMOVED*** 0) {
      lexicalEnvironmentVariableDeclarationsStack ***REMOVED*** [];
      lexicalEnvironmentFunctionDeclarationsStack ***REMOVED*** [];
      lexicalEnvironmentStatementsStack ***REMOVED*** [];
      lexicalEnvironmentFlagsStack ***REMOVED*** [];
    }
    return statements;
  }
  function setLexicalEnvironmentFlags(flags, value) {
    lexicalEnvironmentFlags ***REMOVED*** value ? lexicalEnvironmentFlags | flags : lexicalEnvironmentFlags & ~flags;
  }
  function getLexicalEnvironmentFlags() {
    return lexicalEnvironmentFlags;
  }
  function startBlockScope() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot start a block scope during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot start a block scope after transformation has completed.");
    blockScopedVariableDeclarationsStack[blockScopeStackOffset] ***REMOVED*** blockScopedVariableDeclarations;
    blockScopeStackOffset++;
    blockScopedVariableDeclarations ***REMOVED*** void 0;
  }
  function endBlockScope() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot end a block scope during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot end a block scope after transformation has completed.");
    const statements ***REMOVED*** some(blockScopedVariableDeclarations) ? [
      factory2.createVariableStatement(
        /*modifiers*/
        void 0,
        factory2.createVariableDeclarationList(
          blockScopedVariableDeclarations.map((identifier) ***REMOVED***> factory2.createVariableDeclaration(identifier)),
          1 /* Let */
        )
      )
    ] : void 0;
    blockScopeStackOffset--;
    blockScopedVariableDeclarations ***REMOVED*** blockScopedVariableDeclarationsStack[blockScopeStackOffset];
    if (blockScopeStackOffset ***REMOVED******REMOVED******REMOVED*** 0) {
      blockScopedVariableDeclarationsStack ***REMOVED*** [];
    }
    return statements;
  }
  function addBlockScopedVariable(name) {
    Debug.assert(blockScopeStackOffset > 0, "Cannot add a block scoped variable outside of an iteration body.");
    (blockScopedVariableDeclarations || (blockScopedVariableDeclarations ***REMOVED*** [])).push(name);
  }
  function requestEmitHelper(helper) {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the transformation context during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the transformation context after transformation has completed.");
    Debug.assert(!helper.scoped, "Cannot request a scoped emit helper.");
    if (helper.dependencies) {
      for (const h of helper.dependencies) {
        requestEmitHelper(h);
      }
    }
    emitHelpers ***REMOVED*** append(emitHelpers, helper);
  }
  function readEmitHelpers() {
    Debug.assert(state > 0 /* Uninitialized */, "Cannot modify the transformation context during initialization.");
    Debug.assert(state < 2 /* Completed */, "Cannot modify the transformation context after transformation has completed.");
    const helpers ***REMOVED*** emitHelpers;
    emitHelpers ***REMOVED*** void 0;
    return helpers;
  }
  function dispose() {
    if (state < 3 /* Disposed */) {
      for (const node of nodes) {
        disposeEmitNodes(getSourceFileOfNode(getParseTreeNode(node)));
      }
      lexicalEnvironmentVariableDeclarations ***REMOVED*** void 0;
      lexicalEnvironmentVariableDeclarationsStack ***REMOVED*** void 0;
      lexicalEnvironmentFunctionDeclarations ***REMOVED*** void 0;
      lexicalEnvironmentFunctionDeclarationsStack ***REMOVED*** void 0;
      onSubstituteNode ***REMOVED*** void 0;
      onEmitNode ***REMOVED*** void 0;
      emitHelpers ***REMOVED*** void 0;
      state ***REMOVED*** 3 /* Disposed */;
    }
  }
}
var nullTransformationContext ***REMOVED*** {
  factory,
  // eslint-disable-line object-shorthand
  getCompilerOptions: () ***REMOVED***> ({}),
  getEmitResolver: notImplemented,
  getEmitHost: notImplemented,
  getEmitHelperFactory: notImplemented,
  startLexicalEnvironment: noop,
  resumeLexicalEnvironment: noop,
  suspendLexicalEnvironment: noop,
  endLexicalEnvironment: returnUndefined,
  setLexicalEnvironmentFlags: noop,
  getLexicalEnvironmentFlags: () ***REMOVED***> 0,
  hoistVariableDeclaration: noop,
  hoistFunctionDeclaration: noop,
  addInitializationStatement: noop,
  startBlockScope: noop,
  endBlockScope: returnUndefined,
  addBlockScopedVariable: noop,
  requestEmitHelper: noop,
  readEmitHelpers: notImplemented,
  enableSubstitution: noop,
  enableEmitNotification: noop,
  isSubstitutionEnabled: notImplemented,
  isEmitNotificationEnabled: notImplemented,
  onSubstituteNode: noEmitSubstitution,
  onEmitNode: noEmitNotification,
  addDiagnostic: noop
};

// src/compiler/emitter.ts
var brackets ***REMOVED*** createBracketsMap();
function isBuildInfoFile(file) {
  return fileExtensionIs(file, ".tsbuildinfo" /* TsBuildInfo */);
}
function forEachEmittedFile(host, action, sourceFilesOrTargetSourceFile, forceDtsEmit ***REMOVED*** false, onlyBuildInfo, includeBuildInfo) {
  const sourceFiles ***REMOVED*** isArray(sourceFilesOrTargetSourceFile) ? sourceFilesOrTargetSourceFile : getSourceFilesToEmit(host, sourceFilesOrTargetSourceFile, forceDtsEmit);
  const options ***REMOVED*** host.getCompilerOptions();
  if (!onlyBuildInfo) {
    if (options.outFile) {
      if (sourceFiles.length) {
        const bundle ***REMOVED*** factory.createBundle(sourceFiles);
        const result ***REMOVED*** action(getOutputPathsFor(bundle, host, forceDtsEmit), bundle);
        if (result) {
          return result;
        }
      }
    } else {
      for (const sourceFile of sourceFiles) {
        const result ***REMOVED*** action(getOutputPathsFor(sourceFile, host, forceDtsEmit), sourceFile);
        if (result) {
          return result;
        }
      }
    }
  }
  if (includeBuildInfo) {
    const buildInfoPath ***REMOVED*** getTsBuildInfoEmitOutputFilePath(options);
    if (buildInfoPath) return action(
      { buildInfoPath },
      /*sourceFileOrBundle*/
      void 0
    );
  }
}
function getTsBuildInfoEmitOutputFilePath(options) {
  const configFile ***REMOVED*** options.configFilePath;
  if (!canEmitTsBuildInfo(options)) return void 0;
  if (options.tsBuildInfoFile) return options.tsBuildInfoFile;
  const outPath ***REMOVED*** options.outFile;
  let buildInfoExtensionLess;
  if (outPath) {
    buildInfoExtensionLess ***REMOVED*** removeFileExtension(outPath);
  } else {
    if (!configFile) return void 0;
    const configFileExtensionLess ***REMOVED*** removeFileExtension(configFile);
    buildInfoExtensionLess ***REMOVED*** options.outDir ? options.rootDir ? resolvePath(options.outDir, getRelativePathFromDirectory(
      options.rootDir,
      configFileExtensionLess,
      /*ignoreCase*/
      true
    )) : combinePaths(options.outDir, getBaseFileName(configFileExtensionLess)) : configFileExtensionLess;
  }
  return buildInfoExtensionLess + ".tsbuildinfo" /* TsBuildInfo */;
}
function canEmitTsBuildInfo(options) {
  return isIncrementalCompilation(options) || !!options.tscBuild;
}
function getOutputPathsForBundle(options, forceDtsPaths) {
  const outPath ***REMOVED*** options.outFile;
  const jsFilePath ***REMOVED*** options.emitDeclarationOnly ? void 0 : outPath;
  const sourceMapFilePath ***REMOVED*** jsFilePath && getSourceMapFilePath(jsFilePath, options);
  const declarationFilePath ***REMOVED*** forceDtsPaths || getEmitDeclarations(options) ? removeFileExtension(outPath) + ".d.ts" /* Dts */ : void 0;
  const declarationMapPath ***REMOVED*** declarationFilePath && getAreDeclarationMapsEnabled(options) ? declarationFilePath + ".map" : void 0;
  return { jsFilePath, sourceMapFilePath, declarationFilePath, declarationMapPath };
}
function getOutputPathsFor(sourceFile, host, forceDtsPaths) {
  const options ***REMOVED*** host.getCompilerOptions();
  if (sourceFile.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */) {
    return getOutputPathsForBundle(options, forceDtsPaths);
  } else {
    const ownOutputFilePath ***REMOVED*** getOwnEmitOutputFilePath(sourceFile.fileName, host, getOutputExtension(sourceFile.fileName, options));
    const isJsonFile ***REMOVED*** isJsonSourceFile(sourceFile);
    const isJsonEmittedToSameLocation ***REMOVED*** isJsonFile && comparePaths(sourceFile.fileName, ownOutputFilePath, host.getCurrentDirectory(), !host.useCaseSensitiveFileNames()) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */;
    const jsFilePath ***REMOVED*** options.emitDeclarationOnly || isJsonEmittedToSameLocation ? void 0 : ownOutputFilePath;
    const sourceMapFilePath ***REMOVED*** !jsFilePath || isJsonSourceFile(sourceFile) ? void 0 : getSourceMapFilePath(jsFilePath, options);
    const declarationFilePath ***REMOVED*** forceDtsPaths || getEmitDeclarations(options) && !isJsonFile ? getDeclarationEmitOutputFilePath(sourceFile.fileName, host) : void 0;
    const declarationMapPath ***REMOVED*** declarationFilePath && getAreDeclarationMapsEnabled(options) ? declarationFilePath + ".map" : void 0;
    return { jsFilePath, sourceMapFilePath, declarationFilePath, declarationMapPath };
  }
}
function getSourceMapFilePath(jsFilePath, options) {
  return options.sourceMap && !options.inlineSourceMap ? jsFilePath + ".map" : void 0;
}
function getOutputExtension(fileName, options) {
  return fileExtensionIs(fileName, ".json" /* Json */) ? ".json" /* Json */ : options.jsx ***REMOVED******REMOVED******REMOVED*** 1 /* Preserve */ && fileExtensionIsOneOf(fileName, [".jsx" /* Jsx */, ".tsx" /* Tsx */]) ? ".jsx" /* Jsx */ : fileExtensionIsOneOf(fileName, [".mts" /* Mts */, ".mjs" /* Mjs */]) ? ".mjs" /* Mjs */ : fileExtensionIsOneOf(fileName, [".cts" /* Cts */, ".cjs" /* Cjs */]) ? ".cjs" /* Cjs */ : ".js" /* Js */;
}
function getOutputPathWithoutChangingExt(inputFileName, ignoreCase, outputDir, getCommonSourceDirectory2) {
  return outputDir ? resolvePath(
    outputDir,
    getRelativePathFromDirectory(getCommonSourceDirectory2(), inputFileName, ignoreCase)
  ) : inputFileName;
}
function getOutputDeclarationFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2 ***REMOVED*** () ***REMOVED***> getCommonSourceDirectoryOfConfig(configFile, ignoreCase)) {
  return getOutputDeclarationFileNameWorker(inputFileName, configFile.options, ignoreCase, getCommonSourceDirectory2);
}
function getOutputDeclarationFileNameWorker(inputFileName, options, ignoreCase, getCommonSourceDirectory2) {
  return changeExtension(
    getOutputPathWithoutChangingExt(inputFileName, ignoreCase, options.declarationDir || options.outDir, getCommonSourceDirectory2),
    getDeclarationEmitExtensionForPath(inputFileName)
  );
}
function getOutputJSFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2 ***REMOVED*** () ***REMOVED***> getCommonSourceDirectoryOfConfig(configFile, ignoreCase)) {
  if (configFile.options.emitDeclarationOnly) return void 0;
  const isJsonFile ***REMOVED*** fileExtensionIs(inputFileName, ".json" /* Json */);
  const outputFileName ***REMOVED*** getOutputJSFileNameWorker(inputFileName, configFile.options, ignoreCase, getCommonSourceDirectory2);
  return !isJsonFile || comparePaths(inputFileName, outputFileName, Debug.checkDefined(configFile.options.configFilePath), ignoreCase) !***REMOVED******REMOVED*** 0 /* EqualTo */ ? outputFileName : void 0;
}
function getOutputJSFileNameWorker(inputFileName, options, ignoreCase, getCommonSourceDirectory2) {
  return changeExtension(
    getOutputPathWithoutChangingExt(inputFileName, ignoreCase, options.outDir, getCommonSourceDirectory2),
    getOutputExtension(inputFileName, options)
  );
}
function createAddOutput() {
  let outputs;
  return { addOutput, getOutputs };
  function addOutput(path) {
    if (path) {
      (outputs || (outputs ***REMOVED*** [])).push(path);
    }
  }
  function getOutputs() {
    return outputs || emptyArray;
  }
}
function getSingleOutputFileNames(configFile, addOutput) {
  const { jsFilePath, sourceMapFilePath, declarationFilePath, declarationMapPath } ***REMOVED*** getOutputPathsForBundle(
    configFile.options,
    /*forceDtsPaths*/
    false
  );
  addOutput(jsFilePath);
  addOutput(sourceMapFilePath);
  addOutput(declarationFilePath);
  addOutput(declarationMapPath);
}
function getOwnOutputFileNames(configFile, inputFileName, ignoreCase, addOutput, getCommonSourceDirectory2) {
  if (isDeclarationFileName(inputFileName)) return;
  const js ***REMOVED*** getOutputJSFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2);
  addOutput(js);
  if (fileExtensionIs(inputFileName, ".json" /* Json */)) return;
  if (js && configFile.options.sourceMap) {
    addOutput(`${js}.map`);
  }
  if (getEmitDeclarations(configFile.options)) {
    const dts ***REMOVED*** getOutputDeclarationFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2);
    addOutput(dts);
    if (configFile.options.declarationMap) {
      addOutput(`${dts}.map`);
    }
  }
}
function getCommonSourceDirectory(options, emittedFiles, currentDirectory, getCanonicalFileName, checkSourceFilesBelongToPath) {
  let commonSourceDirectory;
  if (options.rootDir) {
    commonSourceDirectory ***REMOVED*** getNormalizedAbsolutePath(options.rootDir, currentDirectory);
    checkSourceFilesBelongToPath ***REMOVED******REMOVED*** null ? void 0 : checkSourceFilesBelongToPath(options.rootDir);
  } else if (options.composite && options.configFilePath) {
    commonSourceDirectory ***REMOVED*** getDirectoryPath(normalizeSlashes(options.configFilePath));
    checkSourceFilesBelongToPath ***REMOVED******REMOVED*** null ? void 0 : checkSourceFilesBelongToPath(commonSourceDirectory);
  } else {
    commonSourceDirectory ***REMOVED*** computeCommonSourceDirectoryOfFilenames(emittedFiles(), currentDirectory, getCanonicalFileName);
  }
  if (commonSourceDirectory && commonSourceDirectory[commonSourceDirectory.length - 1] !***REMOVED******REMOVED*** directorySeparator) {
    commonSourceDirectory +***REMOVED*** directorySeparator;
  }
  return commonSourceDirectory;
}
function getCommonSourceDirectoryOfConfig({ options, fileNames }, ignoreCase) {
  return getCommonSourceDirectory(
    options,
    () ***REMOVED***> filter(fileNames, (file) ***REMOVED***> !(options.noEmitForJsFiles && fileExtensionIsOneOf(file, supportedJSExtensionsFlat)) && !isDeclarationFileName(file)),
    getDirectoryPath(normalizeSlashes(Debug.checkDefined(options.configFilePath))),
    createGetCanonicalFileName(!ignoreCase)
  );
}
function getAllProjectOutputs(configFile, ignoreCase) {
  const { addOutput, getOutputs } ***REMOVED*** createAddOutput();
  if (configFile.options.outFile) {
    getSingleOutputFileNames(configFile, addOutput);
  } else {
    const getCommonSourceDirectory2 ***REMOVED*** memoize(() ***REMOVED***> getCommonSourceDirectoryOfConfig(configFile, ignoreCase));
    for (const inputFileName of configFile.fileNames) {
      getOwnOutputFileNames(configFile, inputFileName, ignoreCase, addOutput, getCommonSourceDirectory2);
    }
  }
  addOutput(getTsBuildInfoEmitOutputFilePath(configFile.options));
  return getOutputs();
}
function getFirstProjectOutput(configFile, ignoreCase) {
  if (configFile.options.outFile) {
    const { jsFilePath, declarationFilePath } ***REMOVED*** getOutputPathsForBundle(
      configFile.options,
      /*forceDtsPaths*/
      false
    );
    return Debug.checkDefined(jsFilePath || declarationFilePath, `project ${configFile.options.configFilePath} expected to have at least one output`);
  }
  const getCommonSourceDirectory2 ***REMOVED*** memoize(() ***REMOVED***> getCommonSourceDirectoryOfConfig(configFile, ignoreCase));
  for (const inputFileName of configFile.fileNames) {
    if (isDeclarationFileName(inputFileName)) continue;
    const jsFilePath ***REMOVED*** getOutputJSFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2);
    if (jsFilePath) return jsFilePath;
    if (fileExtensionIs(inputFileName, ".json" /* Json */)) continue;
    if (getEmitDeclarations(configFile.options)) {
      return getOutputDeclarationFileName(inputFileName, configFile, ignoreCase, getCommonSourceDirectory2);
    }
  }
  const buildInfoPath ***REMOVED*** getTsBuildInfoEmitOutputFilePath(configFile.options);
  if (buildInfoPath) return buildInfoPath;
  return Debug.fail(`project ${configFile.options.configFilePath} expected to have at least one output`);
}
function emitResolverSkipsTypeChecking(emitOnly, forceDtsEmit) {
  return !!forceDtsEmit && !!emitOnly;
}
function emitFiles(resolver, host, targetSourceFile, { scriptTransformers, declarationTransformers }, emitOnly, onlyBuildInfo, forceDtsEmit, skipBuildInfo) {
  var compilerOptions ***REMOVED*** host.getCompilerOptions();
  var sourceMapDataList ***REMOVED*** compilerOptions.sourceMap || compilerOptions.inlineSourceMap || getAreDeclarationMapsEnabled(compilerOptions) ? [] : void 0;
  var emittedFilesList ***REMOVED*** compilerOptions.listEmittedFiles ? [] : void 0;
  var emitterDiagnostics ***REMOVED*** createDiagnosticCollection();
  var newLine ***REMOVED*** getNewLineCharacter(compilerOptions);
  var writer ***REMOVED*** createTextWriter(newLine);
  var { enter, exit } ***REMOVED*** createTimer("printTime", "beforePrint", "afterPrint");
  var emitSkipped ***REMOVED*** false;
  enter();
  forEachEmittedFile(
    host,
    emitSourceFileOrBundle,
    getSourceFilesToEmit(host, targetSourceFile, forceDtsEmit),
    forceDtsEmit,
    onlyBuildInfo,
    !targetSourceFile && !skipBuildInfo
  );
  exit();
  return {
    emitSkipped,
    diagnostics: emitterDiagnostics.getDiagnostics(),
    emittedFiles: emittedFilesList,
    sourceMaps: sourceMapDataList
  };
  function emitSourceFileOrBundle({ jsFilePath, sourceMapFilePath, declarationFilePath, declarationMapPath, buildInfoPath }, sourceFileOrBundle) {
    var _a, _b, _c, _d, _e, _f;
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(tracing.Phase.Emit, "emitJsFileOrBundle", { jsFilePath });
    emitJsFileOrBundle(sourceFileOrBundle, jsFilePath, sourceMapFilePath);
    (_b ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b.pop();
    (_c ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _c.push(tracing.Phase.Emit, "emitDeclarationFileOrBundle", { declarationFilePath });
    emitDeclarationFileOrBundle(sourceFileOrBundle, declarationFilePath, declarationMapPath);
    (_d ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _d.pop();
    (_e ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _e.push(tracing.Phase.Emit, "emitBuildInfo", { buildInfoPath });
    emitBuildInfo(buildInfoPath);
    (_f ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _f.pop();
  }
  function emitBuildInfo(buildInfoPath) {
    if (!buildInfoPath || targetSourceFile) return;
    if (host.isEmitBlocked(buildInfoPath)) {
      emitSkipped ***REMOVED*** true;
      return;
    }
    const buildInfo ***REMOVED*** host.getBuildInfo() || { version };
    writeFile(
      host,
      emitterDiagnostics,
      buildInfoPath,
      getBuildInfoText(buildInfo),
      /*writeByteOrderMark*/
      false,
      /*sourceFiles*/
      void 0,
      { buildInfo }
    );
    emittedFilesList ***REMOVED******REMOVED*** null ? void 0 : emittedFilesList.push(buildInfoPath);
  }
  function emitJsFileOrBundle(sourceFileOrBundle, jsFilePath, sourceMapFilePath) {
    if (!sourceFileOrBundle || emitOnly || !jsFilePath) {
      return;
    }
    if (host.isEmitBlocked(jsFilePath) || compilerOptions.noEmit) {
      emitSkipped ***REMOVED*** true;
      return;
    }
    (isSourceFile(sourceFileOrBundle) ? [sourceFileOrBundle] : filter(sourceFileOrBundle.sourceFiles, isSourceFileNotJson)).forEach(
      (sourceFile) ***REMOVED***> {
        if (compilerOptions.noCheck || !canIncludeBindAndCheckDiagnostics(sourceFile, compilerOptions)) markLinkedReferences(sourceFile);
      }
    );
    const transform ***REMOVED*** transformNodes(
      resolver,
      host,
      factory,
      compilerOptions,
      [sourceFileOrBundle],
      scriptTransformers,
      /*allowDtsFiles*/
      false
    );
    const printerOptions ***REMOVED*** {
      removeComments: compilerOptions.removeComments,
      newLine: compilerOptions.newLine,
      noEmitHelpers: compilerOptions.noEmitHelpers,
      module: getEmitModuleKind(compilerOptions),
      moduleResolution: getEmitModuleResolutionKind(compilerOptions),
      target: getEmitScriptTarget(compilerOptions),
      sourceMap: compilerOptions.sourceMap,
      inlineSourceMap: compilerOptions.inlineSourceMap,
      inlineSources: compilerOptions.inlineSources,
      extendedDiagnostics: compilerOptions.extendedDiagnostics
    };
    const printer ***REMOVED*** createPrinter(printerOptions, {
      // resolver hooks
      hasGlobalName: resolver.hasGlobalName,
      // transform hooks
      onEmitNode: transform.emitNodeWithNotification,
      isEmitNotificationEnabled: transform.isEmitNotificationEnabled,
      substituteNode: transform.substituteNode
    });
    Debug.assert(transform.transformed.length ***REMOVED******REMOVED******REMOVED*** 1, "Should only see one output from the transform");
    printSourceFileOrBundle(jsFilePath, sourceMapFilePath, transform, printer, compilerOptions);
    transform.dispose();
    if (emittedFilesList) {
      emittedFilesList.push(jsFilePath);
      if (sourceMapFilePath) {
        emittedFilesList.push(sourceMapFilePath);
      }
    }
  }
  function emitDeclarationFileOrBundle(sourceFileOrBundle, declarationFilePath, declarationMapPath) {
    if (!sourceFileOrBundle || emitOnly ***REMOVED******REMOVED******REMOVED*** 0 /* Js */) return;
    if (!declarationFilePath) {
      if (emitOnly || compilerOptions.emitDeclarationOnly) emitSkipped ***REMOVED*** true;
      return;
    }
    const sourceFiles ***REMOVED*** isSourceFile(sourceFileOrBundle) ? [sourceFileOrBundle] : sourceFileOrBundle.sourceFiles;
    const filesForEmit ***REMOVED*** forceDtsEmit ? sourceFiles : filter(sourceFiles, isSourceFileNotJson);
    const inputListOrBundle ***REMOVED*** compilerOptions.outFile ? [factory.createBundle(filesForEmit)] : filesForEmit;
    filesForEmit.forEach((sourceFile) ***REMOVED***> {
      if (emitOnly && !getEmitDeclarations(compilerOptions) || compilerOptions.noCheck || emitResolverSkipsTypeChecking(emitOnly, forceDtsEmit) || !canIncludeBindAndCheckDiagnostics(sourceFile, compilerOptions)) {
        collectLinkedAliases(sourceFile);
      }
    });
    const declarationTransform ***REMOVED*** transformNodes(
      resolver,
      host,
      factory,
      compilerOptions,
      inputListOrBundle,
      declarationTransformers,
      /*allowDtsFiles*/
      false
    );
    if (length(declarationTransform.diagnostics)) {
      for (const diagnostic of declarationTransform.diagnostics) {
        emitterDiagnostics.add(diagnostic);
      }
    }
    const declBlocked ***REMOVED*** !!declarationTransform.diagnostics && !!declarationTransform.diagnostics.length || !!host.isEmitBlocked(declarationFilePath) || !!compilerOptions.noEmit;
    emitSkipped ***REMOVED*** emitSkipped || declBlocked;
    if (!declBlocked || forceDtsEmit) {
      Debug.assert(declarationTransform.transformed.length ***REMOVED******REMOVED******REMOVED*** 1, "Should only see one output from the decl transform");
      const printerOptions ***REMOVED*** {
        removeComments: compilerOptions.removeComments,
        newLine: compilerOptions.newLine,
        noEmitHelpers: true,
        module: compilerOptions.module,
        moduleResolution: compilerOptions.moduleResolution,
        target: compilerOptions.target,
        sourceMap: emitOnly !***REMOVED******REMOVED*** 2 /* BuilderSignature */ && compilerOptions.declarationMap,
        inlineSourceMap: compilerOptions.inlineSourceMap,
        extendedDiagnostics: compilerOptions.extendedDiagnostics,
        onlyPrintJsDocStyle: true,
        omitBraceSourceMapPositions: true
      };
      const declarationPrinter ***REMOVED*** createPrinter(printerOptions, {
        // resolver hooks
        hasGlobalName: resolver.hasGlobalName,
        // transform hooks
        onEmitNode: declarationTransform.emitNodeWithNotification,
        isEmitNotificationEnabled: declarationTransform.isEmitNotificationEnabled,
        substituteNode: declarationTransform.substituteNode
      });
      const dtsWritten ***REMOVED*** printSourceFileOrBundle(
        declarationFilePath,
        declarationMapPath,
        declarationTransform,
        declarationPrinter,
        {
          sourceMap: printerOptions.sourceMap,
          sourceRoot: compilerOptions.sourceRoot,
          mapRoot: compilerOptions.mapRoot,
          extendedDiagnostics: compilerOptions.extendedDiagnostics
          // Explicitly do not passthru either `inline` option
        }
      );
      if (emittedFilesList) {
        if (dtsWritten) emittedFilesList.push(declarationFilePath);
        if (declarationMapPath) {
          emittedFilesList.push(declarationMapPath);
        }
      }
    }
    declarationTransform.dispose();
  }
  function collectLinkedAliases(node) {
    if (isExportAssignment(node)) {
      if (node.expression.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
        resolver.collectLinkedAliases(
          node.expression,
          /*setVisibility*/
          true
        );
      }
      return;
    } else if (isExportSpecifier(node)) {
      resolver.collectLinkedAliases(
        node.propertyName || node.name,
        /*setVisibility*/
        true
      );
      return;
    }
    forEachChild(node, collectLinkedAliases);
  }
  function markLinkedReferences(file) {
    if (isSourceFileJS(file)) return;
    forEachChildRecursively(file, (n) ***REMOVED***> {
      if (isImportEqualsDeclaration(n) && !(getSyntacticModifierFlags(n) & 32 /* Export */)) return "skip";
      if (isImportDeclaration(n)) return "skip";
      resolver.markLinkedReferences(n);
    });
  }
  function printSourceFileOrBundle(jsFilePath, sourceMapFilePath, transform, printer, mapOptions) {
    const sourceFileOrBundle ***REMOVED*** transform.transformed[0];
    const bundle ***REMOVED*** sourceFileOrBundle.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */ ? sourceFileOrBundle : void 0;
    const sourceFile ***REMOVED*** sourceFileOrBundle.kind ***REMOVED******REMOVED******REMOVED*** 308 /* SourceFile */ ? sourceFileOrBundle : void 0;
    const sourceFiles ***REMOVED*** bundle ? bundle.sourceFiles : [sourceFile];
    let sourceMapGenerator;
    if (shouldEmitSourceMaps(mapOptions, sourceFileOrBundle)) {
      sourceMapGenerator ***REMOVED*** createSourceMapGenerator(
        host,
        getBaseFileName(normalizeSlashes(jsFilePath)),
        getSourceRoot(mapOptions),
        getSourceMapDirectory(mapOptions, jsFilePath, sourceFile),
        mapOptions
      );
    }
    if (bundle) {
      printer.writeBundle(bundle, writer, sourceMapGenerator);
    } else {
      printer.writeFile(sourceFile, writer, sourceMapGenerator);
    }
    let sourceMapUrlPos;
    if (sourceMapGenerator) {
      if (sourceMapDataList) {
        sourceMapDataList.push({
          inputSourceFileNames: sourceMapGenerator.getSources(),
          sourceMap: sourceMapGenerator.toJSON()
        });
      }
      const sourceMappingURL ***REMOVED*** getSourceMappingURL(
        mapOptions,
        sourceMapGenerator,
        jsFilePath,
        sourceMapFilePath,
        sourceFile
      );
      if (sourceMappingURL) {
        if (!writer.isAtStartOfLine()) writer.rawWrite(newLine);
        sourceMapUrlPos ***REMOVED*** writer.getTextPos();
        writer.writeComment(`//# ${"sourceMappingURL"}***REMOVED***${sourceMappingURL}`);
      }
      if (sourceMapFilePath) {
        const sourceMap ***REMOVED*** sourceMapGenerator.toString();
        writeFile(
          host,
          emitterDiagnostics,
          sourceMapFilePath,
          sourceMap,
          /*writeByteOrderMark*/
          false,
          sourceFiles
        );
      }
    } else {
      writer.writeLine();
    }
    const text ***REMOVED*** writer.getText();
    const data ***REMOVED*** { sourceMapUrlPos, diagnostics: transform.diagnostics };
    writeFile(host, emitterDiagnostics, jsFilePath, text, !!compilerOptions.emitBOM, sourceFiles, data);
    writer.clear();
    return !data.skippedDtsWrite;
  }
  function shouldEmitSourceMaps(mapOptions, sourceFileOrBundle) {
    return (mapOptions.sourceMap || mapOptions.inlineSourceMap) && (sourceFileOrBundle.kind !***REMOVED******REMOVED*** 308 /* SourceFile */ || !fileExtensionIs(sourceFileOrBundle.fileName, ".json" /* Json */));
  }
  function getSourceRoot(mapOptions) {
    const sourceRoot ***REMOVED*** normalizeSlashes(mapOptions.sourceRoot || "");
    return sourceRoot ? ensureTrailingDirectorySeparator(sourceRoot) : sourceRoot;
  }
  function getSourceMapDirectory(mapOptions, filePath, sourceFile) {
    if (mapOptions.sourceRoot) return host.getCommonSourceDirectory();
    if (mapOptions.mapRoot) {
      let sourceMapDir ***REMOVED*** normalizeSlashes(mapOptions.mapRoot);
      if (sourceFile) {
        sourceMapDir ***REMOVED*** getDirectoryPath(getSourceFilePathInNewDir(sourceFile.fileName, host, sourceMapDir));
      }
      if (getRootLength(sourceMapDir) ***REMOVED******REMOVED******REMOVED*** 0) {
        sourceMapDir ***REMOVED*** combinePaths(host.getCommonSourceDirectory(), sourceMapDir);
      }
      return sourceMapDir;
    }
    return getDirectoryPath(normalizePath(filePath));
  }
  function getSourceMappingURL(mapOptions, sourceMapGenerator, filePath, sourceMapFilePath, sourceFile) {
    if (mapOptions.inlineSourceMap) {
      const sourceMapText ***REMOVED*** sourceMapGenerator.toString();
      const base64SourceMapText ***REMOVED*** base64encode(sys, sourceMapText);
      return `data:application/json;base64,${base64SourceMapText}`;
    }
    const sourceMapFile ***REMOVED*** getBaseFileName(normalizeSlashes(Debug.checkDefined(sourceMapFilePath)));
    if (mapOptions.mapRoot) {
      let sourceMapDir ***REMOVED*** normalizeSlashes(mapOptions.mapRoot);
      if (sourceFile) {
        sourceMapDir ***REMOVED*** getDirectoryPath(getSourceFilePathInNewDir(sourceFile.fileName, host, sourceMapDir));
      }
      if (getRootLength(sourceMapDir) ***REMOVED******REMOVED******REMOVED*** 0) {
        sourceMapDir ***REMOVED*** combinePaths(host.getCommonSourceDirectory(), sourceMapDir);
        return encodeURI(
          getRelativePathToDirectoryOrUrl(
            getDirectoryPath(normalizePath(filePath)),
            // get the relative sourceMapDir path based on jsFilePath
            combinePaths(sourceMapDir, sourceMapFile),
            // this is where user expects to see sourceMap
            host.getCurrentDirectory(),
            host.getCanonicalFileName,
            /*isAbsolutePathAnUrl*/
            true
          )
        );
      } else {
        return encodeURI(combinePaths(sourceMapDir, sourceMapFile));
      }
    }
    return encodeURI(sourceMapFile);
  }
}
function getBuildInfoText(buildInfo) {
  return JSON.stringify(buildInfo);
}
function getBuildInfo(buildInfoFile, buildInfoText) {
  return readJsonOrUndefined(buildInfoFile, buildInfoText);
}
var notImplementedResolver ***REMOVED*** {
  hasGlobalName: notImplemented,
  getReferencedExportContainer: notImplemented,
  getReferencedImportDeclaration: notImplemented,
  getReferencedDeclarationWithCollidingName: notImplemented,
  isDeclarationWithCollidingName: notImplemented,
  isValueAliasDeclaration: notImplemented,
  isReferencedAliasDeclaration: notImplemented,
  isTopLevelValueImportEqualsWithEntityName: notImplemented,
  hasNodeCheckFlag: notImplemented,
  isDeclarationVisible: notImplemented,
  isLateBound: (_node) ***REMOVED***> false,
  collectLinkedAliases: notImplemented,
  markLinkedReferences: notImplemented,
  isImplementationOfOverload: notImplemented,
  requiresAddingImplicitUndefined: notImplemented,
  isExpandoFunctionDeclaration: notImplemented,
  getPropertiesOfContainerFunction: notImplemented,
  createTypeOfDeclaration: notImplemented,
  createReturnTypeOfSignatureDeclaration: notImplemented,
  createTypeOfExpression: notImplemented,
  createLiteralConstValue: notImplemented,
  isSymbolAccessible: notImplemented,
  isEntityNameVisible: notImplemented,
  // Returns the constant value this property access resolves to: notImplemented, or 'undefined' for a non-constant
  getConstantValue: notImplemented,
  getEnumMemberValue: notImplemented,
  getReferencedValueDeclaration: notImplemented,
  getReferencedValueDeclarations: notImplemented,
  getTypeReferenceSerializationKind: notImplemented,
  isOptionalParameter: notImplemented,
  isArgumentsLocalBinding: notImplemented,
  getExternalModuleFileFromDeclaration: notImplemented,
  isLiteralConstDeclaration: notImplemented,
  getJsxFactoryEntity: notImplemented,
  getJsxFragmentFactoryEntity: notImplemented,
  isBindingCapturedByNode: notImplemented,
  getDeclarationStatementsForSourceFile: notImplemented,
  isImportRequiredByAugmentation: notImplemented,
  isDefinitelyReferenceToGlobalSymbolObject: notImplemented,
  createLateBoundIndexSignatures: notImplemented,
  symbolToDeclarations: notImplemented
};
var createPrinterWithDefaults ***REMOVED*** /* @__PURE__ */ memoize(() ***REMOVED***> createPrinter({}));
var createPrinterWithRemoveComments ***REMOVED*** /* @__PURE__ */ memoize(() ***REMOVED***> createPrinter({ removeComments: true }));
var createPrinterWithRemoveCommentsNeverAsciiEscape ***REMOVED*** /* @__PURE__ */ memoize(() ***REMOVED***> createPrinter({ removeComments: true, neverAsciiEscape: true }));
var createPrinterWithRemoveCommentsOmitTrailingSemicolon ***REMOVED*** /* @__PURE__ */ memoize(() ***REMOVED***> createPrinter({ removeComments: true, omitTrailingSemicolon: true }));
function createPrinter(printerOptions ***REMOVED*** {}, handlers ***REMOVED*** {}) {
  var {
    hasGlobalName,
    onEmitNode ***REMOVED*** noEmitNotification,
    isEmitNotificationEnabled,
    substituteNode ***REMOVED*** noEmitSubstitution,
    onBeforeEmitNode,
    onAfterEmitNode,
    onBeforeEmitNodeArray,
    onAfterEmitNodeArray,
    onBeforeEmitToken,
    onAfterEmitToken
  } ***REMOVED*** handlers;
  var extendedDiagnostics ***REMOVED*** !!printerOptions.extendedDiagnostics;
  var omitBraceSourcePositions ***REMOVED*** !!printerOptions.omitBraceSourceMapPositions;
  var newLine ***REMOVED*** getNewLineCharacter(printerOptions);
  var moduleKind ***REMOVED*** getEmitModuleKind(printerOptions);
  var bundledHelpers ***REMOVED*** /* @__PURE__ */ new Map();
  var currentSourceFile;
  var nodeIdToGeneratedName;
  var nodeIdToGeneratedPrivateName;
  var autoGeneratedIdToGeneratedName;
  var generatedNames;
  var formattedNameTempFlagsStack;
  var formattedNameTempFlags;
  var privateNameTempFlagsStack;
  var privateNameTempFlags;
  var tempFlagsStack;
  var tempFlags;
  var reservedNamesStack;
  var reservedNames;
  var reservedPrivateNamesStack;
  var reservedPrivateNames;
  var preserveSourceNewlines ***REMOVED*** printerOptions.preserveSourceNewlines;
  var nextListElementPos;
  var writer;
  var ownWriter;
  var write ***REMOVED*** writeBase;
  var isOwnFileEmit;
  var sourceMapsDisabled ***REMOVED*** true;
  var sourceMapGenerator;
  var sourceMapSource;
  var sourceMapSourceIndex ***REMOVED*** -1;
  var mostRecentlyAddedSourceMapSource;
  var mostRecentlyAddedSourceMapSourceIndex ***REMOVED*** -1;
  var containerPos ***REMOVED*** -1;
  var containerEnd ***REMOVED*** -1;
  var declarationListContainerEnd ***REMOVED*** -1;
  var currentLineMap;
  var detachedCommentsInfo;
  var hasWrittenComment ***REMOVED*** false;
  var commentsDisabled ***REMOVED*** !!printerOptions.removeComments;
  var lastSubstitution;
  var currentParenthesizerRule;
  var { enter: enterComment, exit: exitComment } ***REMOVED*** createTimerIf(extendedDiagnostics, "commentTime", "beforeComment", "afterComment");
  var parenthesizer ***REMOVED*** factory.parenthesizer;
  var typeArgumentParenthesizerRuleSelector ***REMOVED*** {
    select: (index) ***REMOVED***> index ***REMOVED******REMOVED******REMOVED*** 0 ? parenthesizer.parenthesizeLeadingTypeArgument : void 0
  };
  var emitBinaryExpression ***REMOVED*** createEmitBinaryExpression();
  reset();
  return {
    // public API
    printNode,
    printList,
    printFile,
    printBundle,
    // internal API
    writeNode,
    writeList,
    writeFile: writeFile2,
    writeBundle
  };
  function printNode(hint, node, sourceFile) {
    switch (hint) {
      case 0 /* SourceFile */:
        Debug.assert(isSourceFile(node), "Expected a SourceFile node.");
        break;
      case 2 /* IdentifierName */:
        Debug.assert(isIdentifier(node), "Expected an Identifier node.");
        break;
      case 1 /* Expression */:
        Debug.assert(isExpression(node), "Expected an Expression node.");
        break;
    }
    switch (node.kind) {
      case 308 /* SourceFile */:
        return printFile(node);
      case 309 /* Bundle */:
        return printBundle(node);
    }
    writeNode(hint, node, sourceFile, beginPrint());
    return endPrint();
  }
  function printList(format, nodes, sourceFile) {
    writeList(format, nodes, sourceFile, beginPrint());
    return endPrint();
  }
  function printBundle(bundle) {
    writeBundle(
      bundle,
      beginPrint(),
      /*sourceMapGenerator*/
      void 0
    );
    return endPrint();
  }
  function printFile(sourceFile) {
    writeFile2(
      sourceFile,
      beginPrint(),
      /*sourceMapGenerator*/
      void 0
    );
    return endPrint();
  }
  function writeNode(hint, node, sourceFile, output) {
    const previousWriter ***REMOVED*** writer;
    setWriter(
      output,
      /*_sourceMapGenerator*/
      void 0
    );
    print(hint, node, sourceFile);
    reset();
    writer ***REMOVED*** previousWriter;
  }
  function writeList(format, nodes, sourceFile, output) {
    const previousWriter ***REMOVED*** writer;
    setWriter(
      output,
      /*_sourceMapGenerator*/
      void 0
    );
    if (sourceFile) {
      setSourceFile(sourceFile);
    }
    emitList(
      /*parentNode*/
      void 0,
      nodes,
      format
    );
    reset();
    writer ***REMOVED*** previousWriter;
  }
  function writeBundle(bundle, output, sourceMapGenerator2) {
    isOwnFileEmit ***REMOVED*** false;
    const previousWriter ***REMOVED*** writer;
    setWriter(output, sourceMapGenerator2);
    emitShebangIfNeeded(bundle);
    emitPrologueDirectivesIfNeeded(bundle);
    emitHelpers(bundle);
    emitSyntheticTripleSlashReferencesIfNeeded(bundle);
    for (const sourceFile of bundle.sourceFiles) {
      print(0 /* SourceFile */, sourceFile, sourceFile);
    }
    reset();
    writer ***REMOVED*** previousWriter;
  }
  function writeFile2(sourceFile, output, sourceMapGenerator2) {
    isOwnFileEmit ***REMOVED*** true;
    const previousWriter ***REMOVED*** writer;
    setWriter(output, sourceMapGenerator2);
    emitShebangIfNeeded(sourceFile);
    emitPrologueDirectivesIfNeeded(sourceFile);
    print(0 /* SourceFile */, sourceFile, sourceFile);
    reset();
    writer ***REMOVED*** previousWriter;
  }
  function beginPrint() {
    return ownWriter || (ownWriter ***REMOVED*** createTextWriter(newLine));
  }
  function endPrint() {
    const text ***REMOVED*** ownWriter.getText();
    ownWriter.clear();
    return text;
  }
  function print(hint, node, sourceFile) {
    if (sourceFile) {
      setSourceFile(sourceFile);
    }
    pipelineEmit(
      hint,
      node,
      /*parenthesizerRule*/
      void 0
    );
  }
  function setSourceFile(sourceFile) {
    currentSourceFile ***REMOVED*** sourceFile;
    currentLineMap ***REMOVED*** void 0;
    detachedCommentsInfo ***REMOVED*** void 0;
    if (sourceFile) {
      setSourceMapSource(sourceFile);
    }
  }
  function setWriter(_writer, _sourceMapGenerator) {
    if (_writer && printerOptions.omitTrailingSemicolon) {
      _writer ***REMOVED*** getTrailingSemicolonDeferringWriter(_writer);
    }
    writer ***REMOVED*** _writer;
    sourceMapGenerator ***REMOVED*** _sourceMapGenerator;
    sourceMapsDisabled ***REMOVED*** !writer || !sourceMapGenerator;
  }
  function reset() {
    nodeIdToGeneratedName ***REMOVED*** [];
    nodeIdToGeneratedPrivateName ***REMOVED*** [];
    autoGeneratedIdToGeneratedName ***REMOVED*** [];
    generatedNames ***REMOVED*** /* @__PURE__ */ new Set();
    formattedNameTempFlagsStack ***REMOVED*** [];
    formattedNameTempFlags ***REMOVED*** /* @__PURE__ */ new Map();
    privateNameTempFlagsStack ***REMOVED*** [];
    privateNameTempFlags ***REMOVED*** 0 /* Auto */;
    tempFlagsStack ***REMOVED*** [];
    tempFlags ***REMOVED*** 0 /* Auto */;
    reservedNamesStack ***REMOVED*** [];
    reservedNames ***REMOVED*** void 0;
    reservedPrivateNamesStack ***REMOVED*** [];
    reservedPrivateNames ***REMOVED*** void 0;
    currentSourceFile ***REMOVED*** void 0;
    currentLineMap ***REMOVED*** void 0;
    detachedCommentsInfo ***REMOVED*** void 0;
    setWriter(
      /*output*/
      void 0,
      /*_sourceMapGenerator*/
      void 0
    );
  }
  function getCurrentLineMap() {
    return currentLineMap || (currentLineMap ***REMOVED*** getLineStarts(Debug.checkDefined(currentSourceFile)));
  }
  function emit(node, parenthesizerRule) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) return;
    pipelineEmit(4 /* Unspecified */, node, parenthesizerRule);
  }
  function emitIdentifierName(node) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) return;
    pipelineEmit(
      2 /* IdentifierName */,
      node,
      /*parenthesizerRule*/
      void 0
    );
  }
  function emitExpression(node, parenthesizerRule) {
    if (node ***REMOVED******REMOVED******REMOVED*** void 0) return;
    pipelineEmit(1 /* Expression */, node, parenthesizerRule);
  }
  function emitJsxAttributeValue(node) {
    pipelineEmit(isStringLiteral(node) ? 6 /* JsxAttributeValue */ : 4 /* Unspecified */, node);
  }
  function beforeEmitNode(node) {
    if (preserveSourceNewlines && getInternalEmitFlags(node) & 4 /* IgnoreSourceNewlines */) {
      preserveSourceNewlines ***REMOVED*** false;
    }
  }
  function afterEmitNode(savedPreserveSourceNewlines) {
    preserveSourceNewlines ***REMOVED*** savedPreserveSourceNewlines;
  }
  function pipelineEmit(emitHint, node, parenthesizerRule) {
    currentParenthesizerRule ***REMOVED*** parenthesizerRule;
    const pipelinePhase ***REMOVED*** getPipelinePhase(0 /* Notification */, emitHint, node);
    pipelinePhase(emitHint, node);
    currentParenthesizerRule ***REMOVED*** void 0;
  }
  function shouldEmitComments(node) {
    return !commentsDisabled && !isSourceFile(node);
  }
  function shouldEmitSourceMaps(node) {
    return !sourceMapsDisabled && !isSourceFile(node) && !isInJsonFile(node);
  }
  function getPipelinePhase(phase, emitHint, node) {
    switch (phase) {
      case 0 /* Notification */:
        if (onEmitNode !***REMOVED******REMOVED*** noEmitNotification && (!isEmitNotificationEnabled || isEmitNotificationEnabled(node))) {
          return pipelineEmitWithNotification;
        }
      // falls through
      case 1 /* Substitution */:
        if (substituteNode !***REMOVED******REMOVED*** noEmitSubstitution && (lastSubstitution ***REMOVED*** substituteNode(emitHint, node) || node) !***REMOVED******REMOVED*** node) {
          if (currentParenthesizerRule) {
            lastSubstitution ***REMOVED*** currentParenthesizerRule(lastSubstitution);
          }
          return pipelineEmitWithSubstitution;
        }
      // falls through
      case 2 /* Comments */:
        if (shouldEmitComments(node)) {
          return pipelineEmitWithComments;
        }
      // falls through
      case 3 /* SourceMaps */:
        if (shouldEmitSourceMaps(node)) {
          return pipelineEmitWithSourceMaps;
        }
      // falls through
      case 4 /* Emit */:
        return pipelineEmitWithHint;
      default:
        return Debug.assertNever(phase);
    }
  }
  function getNextPipelinePhase(currentPhase, emitHint, node) {
    return getPipelinePhase(currentPhase + 1, emitHint, node);
  }
  function pipelineEmitWithNotification(hint, node) {
    const pipelinePhase ***REMOVED*** getNextPipelinePhase(0 /* Notification */, hint, node);
    onEmitNode(hint, node, pipelinePhase);
  }
  function pipelineEmitWithHint(hint, node) {
    onBeforeEmitNode ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNode(node);
    if (preserveSourceNewlines) {
      const savedPreserveSourceNewlines ***REMOVED*** preserveSourceNewlines;
      beforeEmitNode(node);
      pipelineEmitWithHintWorker(hint, node);
      afterEmitNode(savedPreserveSourceNewlines);
    } else {
      pipelineEmitWithHintWorker(hint, node);
    }
    onAfterEmitNode ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNode(node);
    currentParenthesizerRule ***REMOVED*** void 0;
  }
  function pipelineEmitWithHintWorker(hint, node, allowSnippets ***REMOVED*** true) {
    if (allowSnippets) {
      const snippet ***REMOVED*** getSnippetElement(node);
      if (snippet) {
        return emitSnippetNode(hint, node, snippet);
      }
    }
    if (hint ***REMOVED******REMOVED******REMOVED*** 0 /* SourceFile */) return emitSourceFile(cast(node, isSourceFile));
    if (hint ***REMOVED******REMOVED******REMOVED*** 2 /* IdentifierName */) return emitIdentifier(cast(node, isIdentifier));
    if (hint ***REMOVED******REMOVED******REMOVED*** 6 /* JsxAttributeValue */) return emitLiteral(
      cast(node, isStringLiteral),
      /*jsxAttributeEscape*/
      true
    );
    if (hint ***REMOVED******REMOVED******REMOVED*** 3 /* MappedTypeParameter */) return emitMappedTypeParameter(cast(node, isTypeParameterDeclaration));
    if (hint ***REMOVED******REMOVED******REMOVED*** 7 /* ImportTypeNodeAttributes */) return emitImportTypeNodeAttributes(cast(node, isImportAttributes));
    if (hint ***REMOVED******REMOVED******REMOVED*** 5 /* EmbeddedStatement */) {
      Debug.assertNode(node, isEmptyStatement);
      return emitEmptyStatement(
        /*isEmbeddedStatement*/
        true
      );
    }
    if (hint ***REMOVED******REMOVED******REMOVED*** 4 /* Unspecified */) {
      switch (node.kind) {
        // Pseudo-literals
        case 16 /* TemplateHead */:
        case 17 /* TemplateMiddle */:
        case 18 /* TemplateTail */:
          return emitLiteral(
            node,
            /*jsxAttributeEscape*/
            false
          );
        // Identifiers
        case 80 /* Identifier */:
          return emitIdentifier(node);
        // PrivateIdentifiers
        case 81 /* PrivateIdentifier */:
          return emitPrivateIdentifier(node);
        // Parse tree nodes
        // Names
        case 167 /* QualifiedName */:
          return emitQualifiedName(node);
        case 168 /* ComputedPropertyName */:
          return emitComputedPropertyName(node);
        // Signature elements
        case 169 /* TypeParameter */:
          return emitTypeParameter(node);
        case 170 /* Parameter */:
          return emitParameter(node);
        case 171 /* Decorator */:
          return emitDecorator(node);
        // Type members
        case 172 /* PropertySignature */:
          return emitPropertySignature(node);
        case 173 /* PropertyDeclaration */:
          return emitPropertyDeclaration(node);
        case 174 /* MethodSignature */:
          return emitMethodSignature(node);
        case 175 /* MethodDeclaration */:
          return emitMethodDeclaration(node);
        case 176 /* ClassStaticBlockDeclaration */:
          return emitClassStaticBlockDeclaration(node);
        case 177 /* Constructor */:
          return emitConstructor(node);
        case 178 /* GetAccessor */:
        case 179 /* SetAccessor */:
          return emitAccessorDeclaration(node);
        case 180 /* CallSignature */:
          return emitCallSignature(node);
        case 181 /* ConstructSignature */:
          return emitConstructSignature(node);
        case 182 /* IndexSignature */:
          return emitIndexSignature(node);
        // Types
        case 183 /* TypePredicate */:
          return emitTypePredicate(node);
        case 184 /* TypeReference */:
          return emitTypeReference(node);
        case 185 /* FunctionType */:
          return emitFunctionType(node);
        case 186 /* ConstructorType */:
          return emitConstructorType(node);
        case 187 /* TypeQuery */:
          return emitTypeQuery(node);
        case 188 /* TypeLiteral */:
          return emitTypeLiteral(node);
        case 189 /* ArrayType */:
          return emitArrayType(node);
        case 190 /* TupleType */:
          return emitTupleType(node);
        case 191 /* OptionalType */:
          return emitOptionalType(node);
        // SyntaxKind.RestType is handled below
        case 193 /* UnionType */:
          return emitUnionType(node);
        case 194 /* IntersectionType */:
          return emitIntersectionType(node);
        case 195 /* ConditionalType */:
          return emitConditionalType(node);
        case 196 /* InferType */:
          return emitInferType(node);
        case 197 /* ParenthesizedType */:
          return emitParenthesizedType(node);
        case 234 /* ExpressionWithTypeArguments */:
          return emitExpressionWithTypeArguments(node);
        case 198 /* ThisType */:
          return emitThisType();
        case 199 /* TypeOperator */:
          return emitTypeOperator(node);
        case 200 /* IndexedAccessType */:
          return emitIndexedAccessType(node);
        case 201 /* MappedType */:
          return emitMappedType(node);
        case 202 /* LiteralType */:
          return emitLiteralType(node);
        case 203 /* NamedTupleMember */:
          return emitNamedTupleMember(node);
        case 204 /* TemplateLiteralType */:
          return emitTemplateType(node);
        case 205 /* TemplateLiteralTypeSpan */:
          return emitTemplateTypeSpan(node);
        case 206 /* ImportType */:
          return emitImportTypeNode(node);
        // Binding patterns
        case 207 /* ObjectBindingPattern */:
          return emitObjectBindingPattern(node);
        case 208 /* ArrayBindingPattern */:
          return emitArrayBindingPattern(node);
        case 209 /* BindingElement */:
          return emitBindingElement(node);
        // Misc
        case 240 /* TemplateSpan */:
          return emitTemplateSpan(node);
        case 241 /* SemicolonClassElement */:
          return emitSemicolonClassElement();
        // Statements
        case 242 /* Block */:
          return emitBlock(node);
        case 244 /* VariableStatement */:
          return emitVariableStatement(node);
        case 243 /* EmptyStatement */:
          return emitEmptyStatement(
            /*isEmbeddedStatement*/
            false
          );
        case 245 /* ExpressionStatement */:
          return emitExpressionStatement(node);
        case 246 /* IfStatement */:
          return emitIfStatement(node);
        case 247 /* DoStatement */:
          return emitDoStatement(node);
        case 248 /* WhileStatement */:
          return emitWhileStatement(node);
        case 249 /* ForStatement */:
          return emitForStatement(node);
        case 250 /* ForInStatement */:
          return emitForInStatement(node);
        case 251 /* ForOfStatement */:
          return emitForOfStatement(node);
        case 252 /* ContinueStatement */:
          return emitContinueStatement(node);
        case 253 /* BreakStatement */:
          return emitBreakStatement(node);
        case 254 /* ReturnStatement */:
          return emitReturnStatement(node);
        case 255 /* WithStatement */:
          return emitWithStatement(node);
        case 256 /* SwitchStatement */:
          return emitSwitchStatement(node);
        case 257 /* LabeledStatement */:
          return emitLabeledStatement(node);
        case 258 /* ThrowStatement */:
          return emitThrowStatement(node);
        case 259 /* TryStatement */:
          return emitTryStatement(node);
        case 260 /* DebuggerStatement */:
          return emitDebuggerStatement(node);
        // Declarations
        case 261 /* VariableDeclaration */:
          return emitVariableDeclaration(node);
        case 262 /* VariableDeclarationList */:
          return emitVariableDeclarationList(node);
        case 263 /* FunctionDeclaration */:
          return emitFunctionDeclaration(node);
        case 264 /* ClassDeclaration */:
          return emitClassDeclaration(node);
        case 265 /* InterfaceDeclaration */:
          return emitInterfaceDeclaration(node);
        case 266 /* TypeAliasDeclaration */:
          return emitTypeAliasDeclaration(node);
        case 267 /* EnumDeclaration */:
          return emitEnumDeclaration(node);
        case 268 /* ModuleDeclaration */:
          return emitModuleDeclaration(node);
        case 269 /* ModuleBlock */:
          return emitModuleBlock(node);
        case 270 /* CaseBlock */:
          return emitCaseBlock(node);
        case 271 /* NamespaceExportDeclaration */:
          return emitNamespaceExportDeclaration(node);
        case 272 /* ImportEqualsDeclaration */:
          return emitImportEqualsDeclaration(node);
        case 273 /* ImportDeclaration */:
          return emitImportDeclaration(node);
        case 274 /* ImportClause */:
          return emitImportClause(node);
        case 275 /* NamespaceImport */:
          return emitNamespaceImport(node);
        case 281 /* NamespaceExport */:
          return emitNamespaceExport(node);
        case 276 /* NamedImports */:
          return emitNamedImports(node);
        case 277 /* ImportSpecifier */:
          return emitImportSpecifier(node);
        case 278 /* ExportAssignment */:
          return emitExportAssignment(node);
        case 279 /* ExportDeclaration */:
          return emitExportDeclaration(node);
        case 280 /* NamedExports */:
          return emitNamedExports(node);
        case 282 /* ExportSpecifier */:
          return emitExportSpecifier(node);
        case 301 /* ImportAttributes */:
          return emitImportAttributes(node);
        case 302 /* ImportAttribute */:
          return emitImportAttribute(node);
        case 283 /* MissingDeclaration */:
          return;
        // Module references
        case 284 /* ExternalModuleReference */:
          return emitExternalModuleReference(node);
        // JSX (non-expression)
        case 12 /* JsxText */:
          return emitJsxText(node);
        case 287 /* JsxOpeningElement */:
        case 290 /* JsxOpeningFragment */:
          return emitJsxOpeningElementOrFragment(node);
        case 288 /* JsxClosingElement */:
        case 291 /* JsxClosingFragment */:
          return emitJsxClosingElementOrFragment(node);
        case 292 /* JsxAttribute */:
          return emitJsxAttribute(node);
        case 293 /* JsxAttributes */:
          return emitJsxAttributes(node);
        case 294 /* JsxSpreadAttribute */:
          return emitJsxSpreadAttribute(node);
        case 295 /* JsxExpression */:
          return emitJsxExpression(node);
        case 296 /* JsxNamespacedName */:
          return emitJsxNamespacedName(node);
        // Clauses
        case 297 /* CaseClause */:
          return emitCaseClause(node);
        case 298 /* DefaultClause */:
          return emitDefaultClause(node);
        case 299 /* HeritageClause */:
          return emitHeritageClause(node);
        case 300 /* CatchClause */:
          return emitCatchClause(node);
        // Property assignments
        case 304 /* PropertyAssignment */:
          return emitPropertyAssignment(node);
        case 305 /* ShorthandPropertyAssignment */:
          return emitShorthandPropertyAssignment(node);
        case 306 /* SpreadAssignment */:
          return emitSpreadAssignment(node);
        // Enum
        case 307 /* EnumMember */:
          return emitEnumMember(node);
        // Top-level nodes
        case 308 /* SourceFile */:
          return emitSourceFile(node);
        case 309 /* Bundle */:
          return Debug.fail("Bundles should be printed using printBundle");
        // JSDoc nodes (only used in codefixes currently)
        case 310 /* JSDocTypeExpression */:
          return emitJSDocTypeExpression(node);
        case 311 /* JSDocNameReference */:
          return emitJSDocNameReference(node);
        case 313 /* JSDocAllType */:
          return writePunctuation("*");
        case 314 /* JSDocUnknownType */:
          return writePunctuation("?");
        case 315 /* JSDocNullableType */:
          return emitJSDocNullableType(node);
        case 316 /* JSDocNonNullableType */:
          return emitJSDocNonNullableType(node);
        case 317 /* JSDocOptionalType */:
          return emitJSDocOptionalType(node);
        case 318 /* JSDocFunctionType */:
          return emitJSDocFunctionType(node);
        case 192 /* RestType */:
        case 319 /* JSDocVariadicType */:
          return emitRestOrJSDocVariadicType(node);
        case 320 /* JSDocNamepathType */:
          return;
        case 321 /* JSDoc */:
          return emitJSDoc(node);
        case 323 /* JSDocTypeLiteral */:
          return emitJSDocTypeLiteral(node);
        case 324 /* JSDocSignature */:
          return emitJSDocSignature(node);
        case 328 /* JSDocTag */:
        case 333 /* JSDocClassTag */:
        case 338 /* JSDocOverrideTag */:
          return emitJSDocSimpleTag(node);
        case 329 /* JSDocAugmentsTag */:
        case 330 /* JSDocImplementsTag */:
          return emitJSDocHeritageTag(node);
        case 331 /* JSDocAuthorTag */:
        case 332 /* JSDocDeprecatedTag */:
          return;
        // SyntaxKind.JSDocClassTag (see JSDocTag, above)
        case 334 /* JSDocPublicTag */:
        case 335 /* JSDocPrivateTag */:
        case 336 /* JSDocProtectedTag */:
        case 337 /* JSDocReadonlyTag */:
          return;
        case 339 /* JSDocCallbackTag */:
          return emitJSDocCallbackTag(node);
        case 340 /* JSDocOverloadTag */:
          return emitJSDocOverloadTag(node);
        // SyntaxKind.JSDocEnumTag (see below)
        case 342 /* JSDocParameterTag */:
        case 349 /* JSDocPropertyTag */:
          return emitJSDocPropertyLikeTag(node);
        case 341 /* JSDocEnumTag */:
        case 343 /* JSDocReturnTag */:
        case 344 /* JSDocThisTag */:
        case 345 /* JSDocTypeTag */:
        case 350 /* JSDocThrowsTag */:
        case 351 /* JSDocSatisfiesTag */:
          return emitJSDocSimpleTypedTag(node);
        case 346 /* JSDocTemplateTag */:
          return emitJSDocTemplateTag(node);
        case 347 /* JSDocTypedefTag */:
          return emitJSDocTypedefTag(node);
        case 348 /* JSDocSeeTag */:
          return emitJSDocSeeTag(node);
        case 352 /* JSDocImportTag */:
          return emitJSDocImportTag(node);
        // SyntaxKind.JSDocPropertyTag (see JSDocParameterTag, above)
        // Transformation nodes
        case 354 /* NotEmittedStatement */:
        case 355 /* NotEmittedTypeElement */:
          return;
      }
      if (isExpression(node)) {
        hint ***REMOVED*** 1 /* Expression */;
        if (substituteNode !***REMOVED******REMOVED*** noEmitSubstitution) {
          const substitute ***REMOVED*** substituteNode(hint, node) || node;
          if (substitute !***REMOVED******REMOVED*** node) {
            node ***REMOVED*** substitute;
            if (currentParenthesizerRule) {
              node ***REMOVED*** currentParenthesizerRule(node);
            }
          }
        }
      }
    }
    if (hint ***REMOVED******REMOVED******REMOVED*** 1 /* Expression */) {
      switch (node.kind) {
        // Literals
        case 9 /* NumericLiteral */:
        case 10 /* BigIntLiteral */:
          return emitNumericOrBigIntLiteral(node);
        case 11 /* StringLiteral */:
        case 14 /* RegularExpressionLiteral */:
        case 15 /* NoSubstitutionTemplateLiteral */:
          return emitLiteral(
            node,
            /*jsxAttributeEscape*/
            false
          );
        // Identifiers
        case 80 /* Identifier */:
          return emitIdentifier(node);
        case 81 /* PrivateIdentifier */:
          return emitPrivateIdentifier(node);
        // Expressions
        case 210 /* ArrayLiteralExpression */:
          return emitArrayLiteralExpression(node);
        case 211 /* ObjectLiteralExpression */:
          return emitObjectLiteralExpression(node);
        case 212 /* PropertyAccessExpression */:
          return emitPropertyAccessExpression(node);
        case 213 /* ElementAccessExpression */:
          return emitElementAccessExpression(node);
        case 214 /* CallExpression */:
          return emitCallExpression(node);
        case 215 /* NewExpression */:
          return emitNewExpression(node);
        case 216 /* TaggedTemplateExpression */:
          return emitTaggedTemplateExpression(node);
        case 217 /* TypeAssertionExpression */:
          return emitTypeAssertionExpression(node);
        case 218 /* ParenthesizedExpression */:
          return emitParenthesizedExpression(node);
        case 219 /* FunctionExpression */:
          return emitFunctionExpression(node);
        case 220 /* ArrowFunction */:
          return emitArrowFunction(node);
        case 221 /* DeleteExpression */:
          return emitDeleteExpression(node);
        case 222 /* TypeOfExpression */:
          return emitTypeOfExpression(node);
        case 223 /* VoidExpression */:
          return emitVoidExpression(node);
        case 224 /* AwaitExpression */:
          return emitAwaitExpression(node);
        case 225 /* PrefixUnaryExpression */:
          return emitPrefixUnaryExpression(node);
        case 226 /* PostfixUnaryExpression */:
          return emitPostfixUnaryExpression(node);
        case 227 /* BinaryExpression */:
          return emitBinaryExpression(node);
        case 228 /* ConditionalExpression */:
          return emitConditionalExpression(node);
        case 229 /* TemplateExpression */:
          return emitTemplateExpression(node);
        case 230 /* YieldExpression */:
          return emitYieldExpression(node);
        case 231 /* SpreadElement */:
          return emitSpreadElement(node);
        case 232 /* ClassExpression */:
          return emitClassExpression(node);
        case 233 /* OmittedExpression */:
          return;
        case 235 /* AsExpression */:
          return emitAsExpression(node);
        case 236 /* NonNullExpression */:
          return emitNonNullExpression(node);
        case 234 /* ExpressionWithTypeArguments */:
          return emitExpressionWithTypeArguments(node);
        case 239 /* SatisfiesExpression */:
          return emitSatisfiesExpression(node);
        case 237 /* MetaProperty */:
          return emitMetaProperty(node);
        case 238 /* SyntheticExpression */:
          return Debug.fail("SyntheticExpression should never be printed.");
        case 283 /* MissingDeclaration */:
          return;
        // JSX
        case 285 /* JsxElement */:
          return emitJsxElement(node);
        case 286 /* JsxSelfClosingElement */:
          return emitJsxSelfClosingElement(node);
        case 289 /* JsxFragment */:
          return emitJsxFragment(node);
        // Synthesized list
        case 353 /* SyntaxList */:
          return Debug.fail("SyntaxList should not be printed");
        // Transformation nodes
        case 354 /* NotEmittedStatement */:
          return;
        case 356 /* PartiallyEmittedExpression */:
          return emitPartiallyEmittedExpression(node);
        case 357 /* CommaListExpression */:
          return emitCommaList(node);
        case 358 /* SyntheticReferenceExpression */:
          return Debug.fail("SyntheticReferenceExpression should not be printed");
      }
    }
    if (isKeyword(node.kind)) return writeTokenNode(node, writeKeyword);
    if (isTokenKind(node.kind)) return writeTokenNode(node, writePunctuation);
    Debug.fail(`Unhandled SyntaxKind: ${Debug.formatSyntaxKind(node.kind)}.`);
  }
  function emitMappedTypeParameter(node) {
    emit(node.name);
    writeSpace();
    writeKeyword("in");
    writeSpace();
    emit(node.constraint);
  }
  function pipelineEmitWithSubstitution(hint, node) {
    const pipelinePhase ***REMOVED*** getNextPipelinePhase(1 /* Substitution */, hint, node);
    Debug.assertIsDefined(lastSubstitution);
    node ***REMOVED*** lastSubstitution;
    lastSubstitution ***REMOVED*** void 0;
    pipelinePhase(hint, node);
  }
  function emitHelpers(node) {
    let helpersEmitted ***REMOVED*** false;
    const bundle ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 309 /* Bundle */ ? node : void 0;
    if (bundle && moduleKind ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      return;
    }
    const numNodes ***REMOVED*** bundle ? bundle.sourceFiles.length : 1;
    for (let i ***REMOVED*** 0; i < numNodes; i++) {
      const currentNode ***REMOVED*** bundle ? bundle.sourceFiles[i] : node;
      const sourceFile ***REMOVED*** isSourceFile(currentNode) ? currentNode : currentSourceFile;
      const shouldSkip ***REMOVED*** printerOptions.noEmitHelpers || !!sourceFile && hasRecordedExternalHelpers(sourceFile);
      const shouldBundle ***REMOVED*** isSourceFile(currentNode) && !isOwnFileEmit;
      const helpers ***REMOVED*** getSortedEmitHelpers(currentNode);
      if (helpers) {
        for (const helper of helpers) {
          if (!helper.scoped) {
            if (shouldSkip) continue;
            if (shouldBundle) {
              if (bundledHelpers.get(helper.name)) {
                continue;
              }
              bundledHelpers.set(helper.name, true);
            }
          } else if (bundle) {
            continue;
          }
          if (typeof helper.text ***REMOVED******REMOVED******REMOVED*** "string") {
            writeLines(helper.text);
          } else {
            writeLines(helper.text(makeFileLevelOptimisticUniqueName));
          }
          helpersEmitted ***REMOVED*** true;
        }
      }
    }
    return helpersEmitted;
  }
  function getSortedEmitHelpers(node) {
    const helpers ***REMOVED*** getEmitHelpers(node);
    return helpers && toSorted(helpers, compareEmitHelpers);
  }
  function emitNumericOrBigIntLiteral(node) {
    emitLiteral(
      node,
      /*jsxAttributeEscape*/
      false
    );
  }
  function emitLiteral(node, jsxAttributeEscape) {
    const text ***REMOVED*** getLiteralTextOfNode(
      node,
      /*sourceFile*/
      void 0,
      printerOptions.neverAsciiEscape,
      jsxAttributeEscape
    );
    if ((printerOptions.sourceMap || printerOptions.inlineSourceMap) && (node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ || isTemplateLiteralKind(node.kind))) {
      writeLiteral(text);
    } else {
      writeStringLiteral(text);
    }
  }
  function emitSnippetNode(hint, node, snippet) {
    switch (snippet.kind) {
      case 1 /* Placeholder */:
        emitPlaceholder(hint, node, snippet);
        break;
      case 0 /* TabStop */:
        emitTabStop(hint, node, snippet);
        break;
    }
  }
  function emitPlaceholder(hint, node, snippet) {
    nonEscapingWrite(`\${${snippet.order}:`);
    pipelineEmitWithHintWorker(
      hint,
      node,
      /*allowSnippets*/
      false
    );
    nonEscapingWrite(`}`);
  }
  function emitTabStop(hint, node, snippet) {
    Debug.assert(node.kind ***REMOVED******REMOVED******REMOVED*** 243 /* EmptyStatement */, `A tab stop cannot be attached to a node of kind ${Debug.formatSyntaxKind(node.kind)}.`);
    Debug.assert(hint !***REMOVED******REMOVED*** 5 /* EmbeddedStatement */, `A tab stop cannot be attached to an embedded statement.`);
    nonEscapingWrite(`$${snippet.order}`);
  }
  function emitIdentifier(node) {
    const writeText ***REMOVED*** node.symbol ? writeSymbol : write;
    writeText(getTextOfNode2(
      node,
      /*includeTrivia*/
      false
    ), node.symbol);
    emitList(node, getIdentifierTypeArguments(node), 53776 /* TypeParameters */);
  }
  function emitPrivateIdentifier(node) {
    write(getTextOfNode2(
      node,
      /*includeTrivia*/
      false
    ));
  }
  function emitQualifiedName(node) {
    emitEntityName(node.left);
    writePunctuation(".");
    emit(node.right);
  }
  function emitEntityName(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      emitExpression(node);
    } else {
      emit(node);
    }
  }
  function emitComputedPropertyName(node) {
    writePunctuation("[");
    emitExpression(node.expression, parenthesizer.parenthesizeExpressionOfComputedPropertyName);
    writePunctuation("]");
  }
  function emitTypeParameter(node) {
    emitModifierList(node, node.modifiers);
    emit(node.name);
    if (node.constraint) {
      writeSpace();
      writeKeyword("extends");
      writeSpace();
      emit(node.constraint);
    }
    if (node.default) {
      writeSpace();
      writeOperator("***REMOVED***");
      writeSpace();
      emit(node.default);
    }
  }
  function emitParameter(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      true
    );
    emit(node.dotDotDotToken);
    emitNodeWithWriter(node.name, writeParameter);
    emit(node.questionToken);
    if (node.parent && node.parent.kind ***REMOVED******REMOVED******REMOVED*** 318 /* JSDocFunctionType */ && !node.name) {
      emit(node.type);
    } else {
      emitTypeAnnotation(node.type);
    }
    emitInitializer(node.initializer, node.type ? node.type.end : node.questionToken ? node.questionToken.end : node.name ? node.name.end : node.modifiers ? node.modifiers.end : node.pos, node, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitDecorator(decorator) {
    writePunctuation("@");
    emitExpression(decorator.expression, parenthesizer.parenthesizeLeftSideOfAccess);
  }
  function emitPropertySignature(node) {
    emitModifierList(node, node.modifiers);
    emitNodeWithWriter(node.name, writeProperty);
    emit(node.questionToken);
    emitTypeAnnotation(node.type);
    writeTrailingSemicolon();
  }
  function emitPropertyDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      true
    );
    emit(node.name);
    emit(node.questionToken);
    emit(node.exclamationToken);
    emitTypeAnnotation(node.type);
    emitInitializer(node.initializer, node.type ? node.type.end : node.questionToken ? node.questionToken.end : node.name.end, node);
    writeTrailingSemicolon();
  }
  function emitMethodSignature(node) {
    emitModifierList(node, node.modifiers);
    emit(node.name);
    emit(node.questionToken);
    emitSignatureAndBody(node, emitSignatureHead, emitEmptyFunctionBody);
  }
  function emitMethodDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      true
    );
    emit(node.asteriskToken);
    emit(node.name);
    emit(node.questionToken);
    emitSignatureAndBody(node, emitSignatureHead, emitFunctionBody);
  }
  function emitClassStaticBlockDeclaration(node) {
    writeKeyword("static");
    pushNameGenerationScope(node);
    emitBlockFunctionBody(node.body);
    popNameGenerationScope(node);
  }
  function emitConstructor(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    writeKeyword("constructor");
    emitSignatureAndBody(node, emitSignatureHead, emitFunctionBody);
  }
  function emitAccessorDeclaration(node) {
    const pos ***REMOVED*** emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      true
    );
    const token ***REMOVED*** node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? 139 /* GetKeyword */ : 153 /* SetKeyword */;
    emitTokenWithComment(token, pos, writeKeyword, node);
    writeSpace();
    emit(node.name);
    emitSignatureAndBody(node, emitSignatureHead, emitFunctionBody);
  }
  function emitCallSignature(node) {
    emitSignatureAndBody(node, emitSignatureHead, emitEmptyFunctionBody);
  }
  function emitConstructSignature(node) {
    writeKeyword("new");
    writeSpace();
    emitSignatureAndBody(node, emitSignatureHead, emitEmptyFunctionBody);
  }
  function emitIndexSignature(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    emitParametersForIndexSignature(node, node.parameters);
    emitTypeAnnotation(node.type);
    writeTrailingSemicolon();
  }
  function emitTemplateTypeSpan(node) {
    emit(node.type);
    emit(node.literal);
  }
  function emitSemicolonClassElement() {
    writeTrailingSemicolon();
  }
  function emitTypePredicate(node) {
    if (node.assertsModifier) {
      emit(node.assertsModifier);
      writeSpace();
    }
    emit(node.parameterName);
    if (node.type) {
      writeSpace();
      writeKeyword("is");
      writeSpace();
      emit(node.type);
    }
  }
  function emitTypeReference(node) {
    emit(node.typeName);
    emitTypeArguments(node, node.typeArguments);
  }
  function emitFunctionType(node) {
    emitSignatureAndBody(node, emitFunctionTypeHead, emitFunctionTypeBody);
  }
  function emitFunctionTypeHead(node) {
    emitTypeParameters(node, node.typeParameters);
    emitParametersForArrow(node, node.parameters);
    writeSpace();
    writePunctuation("***REMOVED***>");
  }
  function emitFunctionTypeBody(node) {
    writeSpace();
    emit(node.type);
  }
  function emitJSDocFunctionType(node) {
    writeKeyword("function");
    emitParameters(node, node.parameters);
    writePunctuation(":");
    emit(node.type);
  }
  function emitJSDocNullableType(node) {
    writePunctuation("?");
    emit(node.type);
  }
  function emitJSDocNonNullableType(node) {
    writePunctuation("!");
    emit(node.type);
  }
  function emitJSDocOptionalType(node) {
    emit(node.type);
    writePunctuation("***REMOVED***");
  }
  function emitConstructorType(node) {
    emitModifierList(node, node.modifiers);
    writeKeyword("new");
    writeSpace();
    emitSignatureAndBody(node, emitFunctionTypeHead, emitFunctionTypeBody);
  }
  function emitTypeQuery(node) {
    writeKeyword("typeof");
    writeSpace();
    emit(node.exprName);
    emitTypeArguments(node, node.typeArguments);
  }
  function emitTypeLiteral(node) {
    pushNameGenerationScope(node);
    forEach(node.members, generateMemberNames);
    writePunctuation("{");
    const flags ***REMOVED*** getEmitFlags(node) & 1 /* SingleLine */ ? 768 /* SingleLineTypeLiteralMembers */ : 32897 /* MultiLineTypeLiteralMembers */;
    emitList(node, node.members, flags | 524288 /* NoSpaceIfEmpty */);
    writePunctuation("}");
    popNameGenerationScope(node);
  }
  function emitArrayType(node) {
    emit(node.elementType, parenthesizer.parenthesizeNonArrayTypeOfPostfixType);
    writePunctuation("[");
    writePunctuation("]");
  }
  function emitRestOrJSDocVariadicType(node) {
    writePunctuation("...");
    emit(node.type);
  }
  function emitTupleType(node) {
    emitTokenWithComment(23 /* OpenBracketToken */, node.pos, writePunctuation, node);
    const flags ***REMOVED*** getEmitFlags(node) & 1 /* SingleLine */ ? 528 /* SingleLineTupleTypeElements */ : 657 /* MultiLineTupleTypeElements */;
    emitList(node, node.elements, flags | 524288 /* NoSpaceIfEmpty */, parenthesizer.parenthesizeElementTypeOfTupleType);
    emitTokenWithComment(24 /* CloseBracketToken */, node.elements.end, writePunctuation, node);
  }
  function emitNamedTupleMember(node) {
    emit(node.dotDotDotToken);
    emit(node.name);
    emit(node.questionToken);
    emitTokenWithComment(59 /* ColonToken */, node.name.end, writePunctuation, node);
    writeSpace();
    emit(node.type);
  }
  function emitOptionalType(node) {
    emit(node.type, parenthesizer.parenthesizeTypeOfOptionalType);
    writePunctuation("?");
  }
  function emitUnionType(node) {
    emitList(node, node.types, 516 /* UnionTypeConstituents */, parenthesizer.parenthesizeConstituentTypeOfUnionType);
  }
  function emitIntersectionType(node) {
    emitList(node, node.types, 520 /* IntersectionTypeConstituents */, parenthesizer.parenthesizeConstituentTypeOfIntersectionType);
  }
  function emitConditionalType(node) {
    emit(node.checkType, parenthesizer.parenthesizeCheckTypeOfConditionalType);
    writeSpace();
    writeKeyword("extends");
    writeSpace();
    emit(node.extendsType, parenthesizer.parenthesizeExtendsTypeOfConditionalType);
    writeSpace();
    writePunctuation("?");
    writeSpace();
    emit(node.trueType);
    writeSpace();
    writePunctuation(":");
    writeSpace();
    emit(node.falseType);
  }
  function emitInferType(node) {
    writeKeyword("infer");
    writeSpace();
    emit(node.typeParameter);
  }
  function emitParenthesizedType(node) {
    writePunctuation("(");
    emit(node.type);
    writePunctuation(")");
  }
  function emitThisType() {
    writeKeyword("this");
  }
  function emitTypeOperator(node) {
    writeTokenText(node.operator, writeKeyword);
    writeSpace();
    const parenthesizerRule ***REMOVED*** node.operator ***REMOVED******REMOVED******REMOVED*** 148 /* ReadonlyKeyword */ ? parenthesizer.parenthesizeOperandOfReadonlyTypeOperator : parenthesizer.parenthesizeOperandOfTypeOperator;
    emit(node.type, parenthesizerRule);
  }
  function emitIndexedAccessType(node) {
    emit(node.objectType, parenthesizer.parenthesizeNonArrayTypeOfPostfixType);
    writePunctuation("[");
    emit(node.indexType);
    writePunctuation("]");
  }
  function emitMappedType(node) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    writePunctuation("{");
    if (emitFlags & 1 /* SingleLine */) {
      writeSpace();
    } else {
      writeLine();
      increaseIndent();
    }
    if (node.readonlyToken) {
      emit(node.readonlyToken);
      if (node.readonlyToken.kind !***REMOVED******REMOVED*** 148 /* ReadonlyKeyword */) {
        writeKeyword("readonly");
      }
      writeSpace();
    }
    writePunctuation("[");
    pipelineEmit(3 /* MappedTypeParameter */, node.typeParameter);
    if (node.nameType) {
      writeSpace();
      writeKeyword("as");
      writeSpace();
      emit(node.nameType);
    }
    writePunctuation("]");
    if (node.questionToken) {
      emit(node.questionToken);
      if (node.questionToken.kind !***REMOVED******REMOVED*** 58 /* QuestionToken */) {
        writePunctuation("?");
      }
    }
    writePunctuation(":");
    writeSpace();
    emit(node.type);
    writeTrailingSemicolon();
    if (emitFlags & 1 /* SingleLine */) {
      writeSpace();
    } else {
      writeLine();
      decreaseIndent();
    }
    emitList(node, node.members, 2 /* PreserveLines */);
    writePunctuation("}");
  }
  function emitLiteralType(node) {
    emitExpression(node.literal);
  }
  function emitTemplateType(node) {
    emit(node.head);
    emitList(node, node.templateSpans, 262144 /* TemplateExpressionSpans */);
  }
  function emitImportTypeNode(node) {
    if (node.isTypeOf) {
      writeKeyword("typeof");
      writeSpace();
    }
    writeKeyword("import");
    writePunctuation("(");
    emit(node.argument);
    if (node.attributes) {
      writePunctuation(",");
      writeSpace();
      pipelineEmit(7 /* ImportTypeNodeAttributes */, node.attributes);
    }
    writePunctuation(")");
    if (node.qualifier) {
      writePunctuation(".");
      emit(node.qualifier);
    }
    emitTypeArguments(node, node.typeArguments);
  }
  function emitObjectBindingPattern(node) {
    writePunctuation("{");
    emitList(node, node.elements, 525136 /* ObjectBindingPatternElements */);
    writePunctuation("}");
  }
  function emitArrayBindingPattern(node) {
    writePunctuation("[");
    emitList(node, node.elements, 524880 /* ArrayBindingPatternElements */);
    writePunctuation("]");
  }
  function emitBindingElement(node) {
    emit(node.dotDotDotToken);
    if (node.propertyName) {
      emit(node.propertyName);
      writePunctuation(":");
      writeSpace();
    }
    emit(node.name);
    emitInitializer(node.initializer, node.name.end, node, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitArrayLiteralExpression(node) {
    const elements ***REMOVED*** node.elements;
    const preferNewLine ***REMOVED*** node.multiLine ? 65536 /* PreferNewLine */ : 0 /* None */;
    emitExpressionList(node, elements, 8914 /* ArrayLiteralExpressionElements */ | preferNewLine, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitObjectLiteralExpression(node) {
    pushNameGenerationScope(node);
    forEach(node.properties, generateMemberNames);
    const indentedFlag ***REMOVED*** getEmitFlags(node) & 131072 /* Indented */;
    if (indentedFlag) {
      increaseIndent();
    }
    const preferNewLine ***REMOVED*** node.multiLine ? 65536 /* PreferNewLine */ : 0 /* None */;
    const allowTrailingComma ***REMOVED*** currentSourceFile && currentSourceFile.languageVersion >***REMOVED*** 1 /* ES5 */ && !isJsonSourceFile(currentSourceFile) ? 64 /* AllowTrailingComma */ : 0 /* None */;
    emitList(node, node.properties, 526226 /* ObjectLiteralExpressionProperties */ | allowTrailingComma | preferNewLine);
    if (indentedFlag) {
      decreaseIndent();
    }
    popNameGenerationScope(node);
  }
  function emitPropertyAccessExpression(node) {
    emitExpression(node.expression, parenthesizer.parenthesizeLeftSideOfAccess);
    const token ***REMOVED*** node.questionDotToken || setTextRangePosEnd(factory.createToken(25 /* DotToken */), node.expression.end, node.name.pos);
    const linesBeforeDot ***REMOVED*** getLinesBetweenNodes(node, node.expression, token);
    const linesAfterDot ***REMOVED*** getLinesBetweenNodes(node, token, node.name);
    writeLinesAndIndent(
      linesBeforeDot,
      /*writeSpaceIfNotIndenting*/
      false
    );
    const shouldEmitDotDot ***REMOVED*** token.kind !***REMOVED******REMOVED*** 29 /* QuestionDotToken */ && mayNeedDotDotForPropertyAccess(node.expression) && !writer.hasTrailingComment() && !writer.hasTrailingWhitespace();
    if (shouldEmitDotDot) {
      writePunctuation(".");
    }
    if (node.questionDotToken) {
      emit(token);
    } else {
      emitTokenWithComment(token.kind, node.expression.end, writePunctuation, node);
    }
    writeLinesAndIndent(
      linesAfterDot,
      /*writeSpaceIfNotIndenting*/
      false
    );
    emit(node.name);
    decreaseIndentIf(linesBeforeDot, linesAfterDot);
  }
  function mayNeedDotDotForPropertyAccess(expression) {
    expression ***REMOVED*** skipPartiallyEmittedExpressions(expression);
    if (isNumericLiteral(expression)) {
      const text ***REMOVED*** getLiteralTextOfNode(
        expression,
        /*sourceFile*/
        void 0,
        /*neverAsciiEscape*/
        true,
        /*jsxAttributeEscape*/
        false
      );
      return !(expression.numericLiteralFlags & 448 /* WithSpecifier */) && !text.includes(tokenToString(25 /* DotToken */)) && !text.includes(String.fromCharCode(69 /* E */)) && !text.includes(String.fromCharCode(101 /* e */));
    } else if (isAccessExpression(expression)) {
      const constantValue ***REMOVED*** getConstantValue(expression);
      return typeof constantValue ***REMOVED******REMOVED******REMOVED*** "number" && isFinite(constantValue) && constantValue >***REMOVED*** 0 && Math.floor(constantValue) ***REMOVED******REMOVED******REMOVED*** constantValue;
    }
  }
  function emitElementAccessExpression(node) {
    emitExpression(node.expression, parenthesizer.parenthesizeLeftSideOfAccess);
    emit(node.questionDotToken);
    emitTokenWithComment(23 /* OpenBracketToken */, node.expression.end, writePunctuation, node);
    emitExpression(node.argumentExpression);
    emitTokenWithComment(24 /* CloseBracketToken */, node.argumentExpression.end, writePunctuation, node);
  }
  function emitCallExpression(node) {
    const indirectCall ***REMOVED*** getInternalEmitFlags(node) & 16 /* IndirectCall */;
    if (indirectCall) {
      writePunctuation("(");
      writeLiteral("0");
      writePunctuation(",");
      writeSpace();
    }
    emitExpression(node.expression, parenthesizer.parenthesizeLeftSideOfAccess);
    if (indirectCall) {
      writePunctuation(")");
    }
    emit(node.questionDotToken);
    emitTypeArguments(node, node.typeArguments);
    emitExpressionList(node, node.arguments, 2576 /* CallExpressionArguments */, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitNewExpression(node) {
    emitTokenWithComment(105 /* NewKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeExpressionOfNew);
    emitTypeArguments(node, node.typeArguments);
    emitExpressionList(node, node.arguments, 18960 /* NewExpressionArguments */, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitTaggedTemplateExpression(node) {
    const indirectCall ***REMOVED*** getInternalEmitFlags(node) & 16 /* IndirectCall */;
    if (indirectCall) {
      writePunctuation("(");
      writeLiteral("0");
      writePunctuation(",");
      writeSpace();
    }
    emitExpression(node.tag, parenthesizer.parenthesizeLeftSideOfAccess);
    if (indirectCall) {
      writePunctuation(")");
    }
    emitTypeArguments(node, node.typeArguments);
    writeSpace();
    emitExpression(node.template);
  }
  function emitTypeAssertionExpression(node) {
    writePunctuation("<");
    emit(node.type);
    writePunctuation(">");
    emitExpression(node.expression, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function emitParenthesizedExpression(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(21 /* OpenParenToken */, node.pos, writePunctuation, node);
    const indented ***REMOVED*** writeLineSeparatorsAndIndentBefore(node.expression, node);
    emitExpression(
      node.expression,
      /*parenthesizerRule*/
      void 0
    );
    writeLineSeparatorsAfter(node.expression, node);
    decreaseIndentIf(indented);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression ? node.expression.end : openParenPos, writePunctuation, node);
  }
  function emitFunctionExpression(node) {
    generateNameIfNeeded(node.name);
    emitFunctionDeclarationOrExpression(node);
  }
  function emitArrowFunction(node) {
    emitModifierList(node, node.modifiers);
    emitSignatureAndBody(node, emitArrowFunctionHead, emitArrowFunctionBody);
  }
  function emitArrowFunctionHead(node) {
    emitTypeParameters(node, node.typeParameters);
    emitParametersForArrow(node, node.parameters);
    emitTypeAnnotation(node.type);
    writeSpace();
    emit(node.equalsGreaterThanToken);
  }
  function emitArrowFunctionBody(node) {
    if (isBlock(node.body)) {
      emitBlockFunctionBody(node.body);
    } else {
      writeSpace();
      emitExpression(node.body, parenthesizer.parenthesizeConciseBodyOfArrowFunction);
    }
  }
  function emitDeleteExpression(node) {
    emitTokenWithComment(91 /* DeleteKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function emitTypeOfExpression(node) {
    emitTokenWithComment(114 /* TypeOfKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function emitVoidExpression(node) {
    emitTokenWithComment(116 /* VoidKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function emitAwaitExpression(node) {
    emitTokenWithComment(135 /* AwaitKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function emitPrefixUnaryExpression(node) {
    writeTokenText(node.operator, writeOperator);
    if (shouldEmitWhitespaceBeforeOperand(node)) {
      writeSpace();
    }
    emitExpression(node.operand, parenthesizer.parenthesizeOperandOfPrefixUnary);
  }
  function shouldEmitWhitespaceBeforeOperand(node) {
    const operand ***REMOVED*** node.operand;
    return operand.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ && (node.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ && (operand.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ || operand.operator ***REMOVED******REMOVED******REMOVED*** 46 /* PlusPlusToken */) || node.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ && (operand.operator ***REMOVED******REMOVED******REMOVED*** 41 /* MinusToken */ || operand.operator ***REMOVED******REMOVED******REMOVED*** 47 /* MinusMinusToken */));
  }
  function emitPostfixUnaryExpression(node) {
    emitExpression(node.operand, parenthesizer.parenthesizeOperandOfPostfixUnary);
    writeTokenText(node.operator, writeOperator);
  }
  function createEmitBinaryExpression() {
    return createBinaryExpressionTrampoline(
      onEnter,
      onLeft,
      onOperator,
      onRight,
      onExit,
      /*foldState*/
      void 0
    );
    function onEnter(node, state) {
      if (state) {
        state.stackIndex++;
        state.preserveSourceNewlinesStack[state.stackIndex] ***REMOVED*** preserveSourceNewlines;
        state.containerPosStack[state.stackIndex] ***REMOVED*** containerPos;
        state.containerEndStack[state.stackIndex] ***REMOVED*** containerEnd;
        state.declarationListContainerEndStack[state.stackIndex] ***REMOVED*** declarationListContainerEnd;
        const emitComments2 ***REMOVED*** state.shouldEmitCommentsStack[state.stackIndex] ***REMOVED*** shouldEmitComments(node);
        const emitSourceMaps ***REMOVED*** state.shouldEmitSourceMapsStack[state.stackIndex] ***REMOVED*** shouldEmitSourceMaps(node);
        onBeforeEmitNode ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNode(node);
        if (emitComments2) emitCommentsBeforeNode(node);
        if (emitSourceMaps) emitSourceMapsBeforeNode(node);
        beforeEmitNode(node);
      } else {
        state ***REMOVED*** {
          stackIndex: 0,
          preserveSourceNewlinesStack: [void 0],
          containerPosStack: [-1],
          containerEndStack: [-1],
          declarationListContainerEndStack: [-1],
          shouldEmitCommentsStack: [false],
          shouldEmitSourceMapsStack: [false]
        };
      }
      return state;
    }
    function onLeft(next, _workArea, parent) {
      return maybeEmitExpression(next, parent, "left");
    }
    function onOperator(operatorToken, _state, node) {
      const isCommaOperator ***REMOVED*** operatorToken.kind !***REMOVED******REMOVED*** 28 /* CommaToken */;
      const linesBeforeOperator ***REMOVED*** getLinesBetweenNodes(node, node.left, operatorToken);
      const linesAfterOperator ***REMOVED*** getLinesBetweenNodes(node, operatorToken, node.right);
      writeLinesAndIndent(linesBeforeOperator, isCommaOperator);
      emitLeadingCommentsOfPosition(operatorToken.pos);
      writeTokenNode(operatorToken, operatorToken.kind ***REMOVED******REMOVED******REMOVED*** 103 /* InKeyword */ ? writeKeyword : writeOperator);
      emitTrailingCommentsOfPosition(
        operatorToken.end,
        /*prefixSpace*/
        true
      );
      writeLinesAndIndent(
        linesAfterOperator,
        /*writeSpaceIfNotIndenting*/
        true
      );
    }
    function onRight(next, _workArea, parent) {
      return maybeEmitExpression(next, parent, "right");
    }
    function onExit(node, state) {
      const linesBeforeOperator ***REMOVED*** getLinesBetweenNodes(node, node.left, node.operatorToken);
      const linesAfterOperator ***REMOVED*** getLinesBetweenNodes(node, node.operatorToken, node.right);
      decreaseIndentIf(linesBeforeOperator, linesAfterOperator);
      if (state.stackIndex > 0) {
        const savedPreserveSourceNewlines ***REMOVED*** state.preserveSourceNewlinesStack[state.stackIndex];
        const savedContainerPos ***REMOVED*** state.containerPosStack[state.stackIndex];
        const savedContainerEnd ***REMOVED*** state.containerEndStack[state.stackIndex];
        const savedDeclarationListContainerEnd ***REMOVED*** state.declarationListContainerEndStack[state.stackIndex];
        const shouldEmitComments2 ***REMOVED*** state.shouldEmitCommentsStack[state.stackIndex];
        const shouldEmitSourceMaps2 ***REMOVED*** state.shouldEmitSourceMapsStack[state.stackIndex];
        afterEmitNode(savedPreserveSourceNewlines);
        if (shouldEmitSourceMaps2) emitSourceMapsAfterNode(node);
        if (shouldEmitComments2) emitCommentsAfterNode(node, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd);
        onAfterEmitNode ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNode(node);
        state.stackIndex--;
      }
    }
    function maybeEmitExpression(next, parent, side) {
      const parenthesizerRule ***REMOVED*** side ***REMOVED******REMOVED******REMOVED*** "left" ? parenthesizer.getParenthesizeLeftSideOfBinaryForOperator(parent.operatorToken.kind) : parenthesizer.getParenthesizeRightSideOfBinaryForOperator(parent.operatorToken.kind);
      let pipelinePhase ***REMOVED*** getPipelinePhase(0 /* Notification */, 1 /* Expression */, next);
      if (pipelinePhase ***REMOVED******REMOVED******REMOVED*** pipelineEmitWithSubstitution) {
        Debug.assertIsDefined(lastSubstitution);
        next ***REMOVED*** parenthesizerRule(cast(lastSubstitution, isExpression));
        pipelinePhase ***REMOVED*** getNextPipelinePhase(1 /* Substitution */, 1 /* Expression */, next);
        lastSubstitution ***REMOVED*** void 0;
      }
      if (pipelinePhase ***REMOVED******REMOVED******REMOVED*** pipelineEmitWithComments || pipelinePhase ***REMOVED******REMOVED******REMOVED*** pipelineEmitWithSourceMaps || pipelinePhase ***REMOVED******REMOVED******REMOVED*** pipelineEmitWithHint) {
        if (isBinaryExpression(next)) {
          return next;
        }
      }
      currentParenthesizerRule ***REMOVED*** parenthesizerRule;
      pipelinePhase(1 /* Expression */, next);
    }
  }
  function emitConditionalExpression(node) {
    const linesBeforeQuestion ***REMOVED*** getLinesBetweenNodes(node, node.condition, node.questionToken);
    const linesAfterQuestion ***REMOVED*** getLinesBetweenNodes(node, node.questionToken, node.whenTrue);
    const linesBeforeColon ***REMOVED*** getLinesBetweenNodes(node, node.whenTrue, node.colonToken);
    const linesAfterColon ***REMOVED*** getLinesBetweenNodes(node, node.colonToken, node.whenFalse);
    emitExpression(node.condition, parenthesizer.parenthesizeConditionOfConditionalExpression);
    writeLinesAndIndent(
      linesBeforeQuestion,
      /*writeSpaceIfNotIndenting*/
      true
    );
    emit(node.questionToken);
    writeLinesAndIndent(
      linesAfterQuestion,
      /*writeSpaceIfNotIndenting*/
      true
    );
    emitExpression(node.whenTrue, parenthesizer.parenthesizeBranchOfConditionalExpression);
    decreaseIndentIf(linesBeforeQuestion, linesAfterQuestion);
    writeLinesAndIndent(
      linesBeforeColon,
      /*writeSpaceIfNotIndenting*/
      true
    );
    emit(node.colonToken);
    writeLinesAndIndent(
      linesAfterColon,
      /*writeSpaceIfNotIndenting*/
      true
    );
    emitExpression(node.whenFalse, parenthesizer.parenthesizeBranchOfConditionalExpression);
    decreaseIndentIf(linesBeforeColon, linesAfterColon);
  }
  function emitTemplateExpression(node) {
    emit(node.head);
    emitList(node, node.templateSpans, 262144 /* TemplateExpressionSpans */);
  }
  function emitYieldExpression(node) {
    emitTokenWithComment(127 /* YieldKeyword */, node.pos, writeKeyword, node);
    emit(node.asteriskToken);
    emitExpressionWithLeadingSpace(node.expression && parenthesizeExpressionForNoAsi(node.expression), parenthesizeExpressionForNoAsiAndDisallowedComma);
  }
  function emitSpreadElement(node) {
    emitTokenWithComment(26 /* DotDotDotToken */, node.pos, writePunctuation, node);
    emitExpression(node.expression, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitClassExpression(node) {
    generateNameIfNeeded(node.name);
    emitClassDeclarationOrExpression(node);
  }
  function emitExpressionWithTypeArguments(node) {
    emitExpression(node.expression, parenthesizer.parenthesizeLeftSideOfAccess);
    emitTypeArguments(node, node.typeArguments);
  }
  function emitAsExpression(node) {
    emitExpression(
      node.expression,
      /*parenthesizerRule*/
      void 0
    );
    if (node.type) {
      writeSpace();
      writeKeyword("as");
      writeSpace();
      emit(node.type);
    }
  }
  function emitNonNullExpression(node) {
    emitExpression(node.expression, parenthesizer.parenthesizeLeftSideOfAccess);
    writeOperator("!");
  }
  function emitSatisfiesExpression(node) {
    emitExpression(
      node.expression,
      /*parenthesizerRule*/
      void 0
    );
    if (node.type) {
      writeSpace();
      writeKeyword("satisfies");
      writeSpace();
      emit(node.type);
    }
  }
  function emitMetaProperty(node) {
    writeToken(node.keywordToken, node.pos, writePunctuation);
    writePunctuation(".");
    emit(node.name);
  }
  function emitTemplateSpan(node) {
    emitExpression(node.expression);
    emit(node.literal);
  }
  function emitBlock(node) {
    emitBlockStatements(
      node,
      /*forceSingleLine*/
      !node.multiLine && isEmptyBlock(node)
    );
  }
  function emitBlockStatements(node, forceSingleLine) {
    emitTokenWithComment(
      19 /* OpenBraceToken */,
      node.pos,
      writePunctuation,
      /*contextNode*/
      node
    );
    const format ***REMOVED*** forceSingleLine || getEmitFlags(node) & 1 /* SingleLine */ ? 768 /* SingleLineBlockStatements */ : 129 /* MultiLineBlockStatements */;
    emitList(node, node.statements, format);
    emitTokenWithComment(
      20 /* CloseBraceToken */,
      node.statements.end,
      writePunctuation,
      /*contextNode*/
      node,
      /*indentLeading*/
      !!(format & 1 /* MultiLine */)
    );
  }
  function emitVariableStatement(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    emit(node.declarationList);
    writeTrailingSemicolon();
  }
  function emitEmptyStatement(isEmbeddedStatement) {
    if (isEmbeddedStatement) {
      writePunctuation(";");
    } else {
      writeTrailingSemicolon();
    }
  }
  function emitExpressionStatement(node) {
    emitExpression(node.expression, parenthesizer.parenthesizeExpressionOfExpressionStatement);
    if (!currentSourceFile || !isJsonSourceFile(currentSourceFile) || nodeIsSynthesized(node.expression)) {
      writeTrailingSemicolon();
    }
  }
  function emitIfStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(101 /* IfKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
    emitEmbeddedStatement(node, node.thenStatement);
    if (node.elseStatement) {
      writeLineOrSpace(node, node.thenStatement, node.elseStatement);
      emitTokenWithComment(93 /* ElseKeyword */, node.thenStatement.end, writeKeyword, node);
      if (node.elseStatement.kind ***REMOVED******REMOVED******REMOVED*** 246 /* IfStatement */) {
        writeSpace();
        emit(node.elseStatement);
      } else {
        emitEmbeddedStatement(node, node.elseStatement);
      }
    }
  }
  function emitWhileClause(node, startPos) {
    const openParenPos ***REMOVED*** emitTokenWithComment(117 /* WhileKeyword */, startPos, writeKeyword, node);
    writeSpace();
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
  }
  function emitDoStatement(node) {
    emitTokenWithComment(92 /* DoKeyword */, node.pos, writeKeyword, node);
    emitEmbeddedStatement(node, node.statement);
    if (isBlock(node.statement) && !preserveSourceNewlines) {
      writeSpace();
    } else {
      writeLineOrSpace(node, node.statement, node.expression);
    }
    emitWhileClause(node, node.statement.end);
    writeTrailingSemicolon();
  }
  function emitWhileStatement(node) {
    emitWhileClause(node, node.pos);
    emitEmbeddedStatement(node, node.statement);
  }
  function emitForStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(99 /* ForKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    let pos ***REMOVED*** emitTokenWithComment(
      21 /* OpenParenToken */,
      openParenPos,
      writePunctuation,
      /*contextNode*/
      node
    );
    emitForBinding(node.initializer);
    pos ***REMOVED*** emitTokenWithComment(27 /* SemicolonToken */, node.initializer ? node.initializer.end : pos, writePunctuation, node);
    emitExpressionWithLeadingSpace(node.condition);
    pos ***REMOVED*** emitTokenWithComment(27 /* SemicolonToken */, node.condition ? node.condition.end : pos, writePunctuation, node);
    emitExpressionWithLeadingSpace(node.incrementor);
    emitTokenWithComment(22 /* CloseParenToken */, node.incrementor ? node.incrementor.end : pos, writePunctuation, node);
    emitEmbeddedStatement(node, node.statement);
  }
  function emitForInStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(99 /* ForKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitForBinding(node.initializer);
    writeSpace();
    emitTokenWithComment(103 /* InKeyword */, node.initializer.end, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
    emitEmbeddedStatement(node, node.statement);
  }
  function emitForOfStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(99 /* ForKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitWithTrailingSpace(node.awaitModifier);
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitForBinding(node.initializer);
    writeSpace();
    emitTokenWithComment(165 /* OfKeyword */, node.initializer.end, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
    emitEmbeddedStatement(node, node.statement);
  }
  function emitForBinding(node) {
    if (node !***REMOVED******REMOVED*** void 0) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
        emit(node);
      } else {
        emitExpression(node);
      }
    }
  }
  function emitContinueStatement(node) {
    emitTokenWithComment(88 /* ContinueKeyword */, node.pos, writeKeyword, node);
    emitWithLeadingSpace(node.label);
    writeTrailingSemicolon();
  }
  function emitBreakStatement(node) {
    emitTokenWithComment(83 /* BreakKeyword */, node.pos, writeKeyword, node);
    emitWithLeadingSpace(node.label);
    writeTrailingSemicolon();
  }
  function emitTokenWithComment(token, pos, writer2, contextNode, indentLeading) {
    const node ***REMOVED*** getParseTreeNode(contextNode);
    const isSimilarNode ***REMOVED*** node && node.kind ***REMOVED******REMOVED******REMOVED*** contextNode.kind;
    const startPos ***REMOVED*** pos;
    if (isSimilarNode && currentSourceFile) {
      pos ***REMOVED*** skipTrivia(currentSourceFile.text, pos);
    }
    if (isSimilarNode && contextNode.pos !***REMOVED******REMOVED*** startPos) {
      const needsIndent ***REMOVED*** indentLeading && currentSourceFile && !positionsAreOnSameLine(startPos, pos, currentSourceFile);
      if (needsIndent) {
        increaseIndent();
      }
      emitLeadingCommentsOfPosition(startPos);
      if (needsIndent) {
        decreaseIndent();
      }
    }
    if (!omitBraceSourcePositions && (token ***REMOVED******REMOVED******REMOVED*** 19 /* OpenBraceToken */ || token ***REMOVED******REMOVED******REMOVED*** 20 /* CloseBraceToken */)) {
      pos ***REMOVED*** writeToken(token, pos, writer2, contextNode);
    } else {
      pos ***REMOVED*** writeTokenText(token, writer2, pos);
    }
    if (isSimilarNode && contextNode.end !***REMOVED******REMOVED*** pos) {
      const isJsxExprContext ***REMOVED*** contextNode.kind ***REMOVED******REMOVED******REMOVED*** 295 /* JsxExpression */;
      emitTrailingCommentsOfPosition(
        pos,
        /*prefixSpace*/
        !isJsxExprContext,
        /*forceNoNewline*/
        isJsxExprContext
      );
    }
    return pos;
  }
  function commentWillEmitNewLine(node) {
    return node.kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */ || !!node.hasTrailingNewLine;
  }
  function willEmitLeadingNewLine(node) {
    if (!currentSourceFile) return false;
    const leadingCommentRanges ***REMOVED*** getLeadingCommentRanges(currentSourceFile.text, node.pos);
    if (leadingCommentRanges) {
      const parseNode ***REMOVED*** getParseTreeNode(node);
      if (parseNode && isParenthesizedExpression(parseNode.parent)) {
        return true;
      }
    }
    if (some(leadingCommentRanges, commentWillEmitNewLine)) return true;
    if (some(getSyntheticLeadingComments(node), commentWillEmitNewLine)) return true;
    if (isPartiallyEmittedExpression(node)) {
      if (node.pos !***REMOVED******REMOVED*** node.expression.pos) {
        if (some(getTrailingCommentRanges(currentSourceFile.text, node.expression.pos), commentWillEmitNewLine)) return true;
      }
      return willEmitLeadingNewLine(node.expression);
    }
    return false;
  }
  function parenthesizeExpressionForNoAsi(node) {
    if (!commentsDisabled) {
      switch (node.kind) {
        case 356 /* PartiallyEmittedExpression */:
          if (willEmitLeadingNewLine(node)) {
            const parseNode ***REMOVED*** getParseTreeNode(node);
            if (parseNode && isParenthesizedExpression(parseNode)) {
              const parens ***REMOVED*** factory.createParenthesizedExpression(node.expression);
              setOriginalNode(parens, node);
              setTextRange(parens, parseNode);
              return parens;
            }
            return factory.createParenthesizedExpression(node);
          }
          return factory.updatePartiallyEmittedExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression)
          );
        case 212 /* PropertyAccessExpression */:
          return factory.updatePropertyAccessExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression),
            node.name
          );
        case 213 /* ElementAccessExpression */:
          return factory.updateElementAccessExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression),
            node.argumentExpression
          );
        case 214 /* CallExpression */:
          return factory.updateCallExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression),
            node.typeArguments,
            node.arguments
          );
        case 216 /* TaggedTemplateExpression */:
          return factory.updateTaggedTemplateExpression(
            node,
            parenthesizeExpressionForNoAsi(node.tag),
            node.typeArguments,
            node.template
          );
        case 226 /* PostfixUnaryExpression */:
          return factory.updatePostfixUnaryExpression(
            node,
            parenthesizeExpressionForNoAsi(node.operand)
          );
        case 227 /* BinaryExpression */:
          return factory.updateBinaryExpression(
            node,
            parenthesizeExpressionForNoAsi(node.left),
            node.operatorToken,
            node.right
          );
        case 228 /* ConditionalExpression */:
          return factory.updateConditionalExpression(
            node,
            parenthesizeExpressionForNoAsi(node.condition),
            node.questionToken,
            node.whenTrue,
            node.colonToken,
            node.whenFalse
          );
        case 235 /* AsExpression */:
          return factory.updateAsExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression),
            node.type
          );
        case 239 /* SatisfiesExpression */:
          return factory.updateSatisfiesExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression),
            node.type
          );
        case 236 /* NonNullExpression */:
          return factory.updateNonNullExpression(
            node,
            parenthesizeExpressionForNoAsi(node.expression)
          );
      }
    }
    return node;
  }
  function parenthesizeExpressionForNoAsiAndDisallowedComma(node) {
    return parenthesizeExpressionForNoAsi(parenthesizer.parenthesizeExpressionForDisallowedComma(node));
  }
  function emitReturnStatement(node) {
    emitTokenWithComment(
      107 /* ReturnKeyword */,
      node.pos,
      writeKeyword,
      /*contextNode*/
      node
    );
    emitExpressionWithLeadingSpace(node.expression && parenthesizeExpressionForNoAsi(node.expression), parenthesizeExpressionForNoAsi);
    writeTrailingSemicolon();
  }
  function emitWithStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(118 /* WithKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
    emitEmbeddedStatement(node, node.statement);
  }
  function emitSwitchStatement(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(109 /* SwitchKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
    emitExpression(node.expression);
    emitTokenWithComment(22 /* CloseParenToken */, node.expression.end, writePunctuation, node);
    writeSpace();
    emit(node.caseBlock);
  }
  function emitLabeledStatement(node) {
    emit(node.label);
    emitTokenWithComment(59 /* ColonToken */, node.label.end, writePunctuation, node);
    writeSpace();
    emit(node.statement);
  }
  function emitThrowStatement(node) {
    emitTokenWithComment(111 /* ThrowKeyword */, node.pos, writeKeyword, node);
    emitExpressionWithLeadingSpace(parenthesizeExpressionForNoAsi(node.expression), parenthesizeExpressionForNoAsi);
    writeTrailingSemicolon();
  }
  function emitTryStatement(node) {
    emitTokenWithComment(113 /* TryKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emit(node.tryBlock);
    if (node.catchClause) {
      writeLineOrSpace(node, node.tryBlock, node.catchClause);
      emit(node.catchClause);
    }
    if (node.finallyBlock) {
      writeLineOrSpace(node, node.catchClause || node.tryBlock, node.finallyBlock);
      emitTokenWithComment(98 /* FinallyKeyword */, (node.catchClause || node.tryBlock).end, writeKeyword, node);
      writeSpace();
      emit(node.finallyBlock);
    }
  }
  function emitDebuggerStatement(node) {
    writeToken(89 /* DebuggerKeyword */, node.pos, writeKeyword);
    writeTrailingSemicolon();
  }
  function emitVariableDeclaration(node) {
    var _a, _b, _c;
    emit(node.name);
    emit(node.exclamationToken);
    emitTypeAnnotation(node.type);
    emitInitializer(node.initializer, ((_a ***REMOVED*** node.type) ***REMOVED******REMOVED*** null ? void 0 : _a.end) ?? ((_c ***REMOVED*** (_b ***REMOVED*** node.name.emitNode) ***REMOVED******REMOVED*** null ? void 0 : _b.typeNode) ***REMOVED******REMOVED*** null ? void 0 : _c.end) ?? node.name.end, node, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitVariableDeclarationList(node) {
    if (isVarAwaitUsing(node)) {
      writeKeyword("await");
      writeSpace();
      writeKeyword("using");
    } else {
      const head ***REMOVED*** isLet(node) ? "let" : isVarConst(node) ? "const" : isVarUsing(node) ? "using" : "var";
      writeKeyword(head);
    }
    writeSpace();
    emitList(node, node.declarations, 528 /* VariableDeclarationList */);
  }
  function emitFunctionDeclaration(node) {
    emitFunctionDeclarationOrExpression(node);
  }
  function emitFunctionDeclarationOrExpression(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    writeKeyword("function");
    emit(node.asteriskToken);
    writeSpace();
    emitIdentifierName(node.name);
    emitSignatureAndBody(node, emitSignatureHead, emitFunctionBody);
  }
  function emitSignatureAndBody(node, emitSignatureHead2, emitBody) {
    const indentedFlag ***REMOVED*** getEmitFlags(node) & 131072 /* Indented */;
    if (indentedFlag) {
      increaseIndent();
    }
    pushNameGenerationScope(node);
    forEach(node.parameters, generateNames);
    emitSignatureHead2(node);
    emitBody(node);
    popNameGenerationScope(node);
    if (indentedFlag) {
      decreaseIndent();
    }
  }
  function emitFunctionBody(node) {
    const body ***REMOVED*** node.body;
    if (body) {
      emitBlockFunctionBody(body);
    } else {
      writeTrailingSemicolon();
    }
  }
  function emitEmptyFunctionBody(_node) {
    writeTrailingSemicolon();
  }
  function emitSignatureHead(node) {
    emitTypeParameters(node, node.typeParameters);
    emitParameters(node, node.parameters);
    emitTypeAnnotation(node.type);
  }
  function shouldEmitBlockFunctionBodyOnSingleLine(body) {
    if (getEmitFlags(body) & 1 /* SingleLine */) {
      return true;
    }
    if (body.multiLine) {
      return false;
    }
    if (!nodeIsSynthesized(body) && currentSourceFile && !rangeIsOnSingleLine(body, currentSourceFile)) {
      return false;
    }
    if (getLeadingLineTerminatorCount(body, firstOrUndefined(body.statements), 2 /* PreserveLines */) || getClosingLineTerminatorCount(body, lastOrUndefined(body.statements), 2 /* PreserveLines */, body.statements)) {
      return false;
    }
    let previousStatement;
    for (const statement of body.statements) {
      if (getSeparatingLineTerminatorCount(previousStatement, statement, 2 /* PreserveLines */) > 0) {
        return false;
      }
      previousStatement ***REMOVED*** statement;
    }
    return true;
  }
  function emitBlockFunctionBody(body) {
    generateNames(body);
    onBeforeEmitNode ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNode(body);
    writeSpace();
    writePunctuation("{");
    increaseIndent();
    const emitBlockFunctionBody2 ***REMOVED*** shouldEmitBlockFunctionBodyOnSingleLine(body) ? emitBlockFunctionBodyOnSingleLine : emitBlockFunctionBodyWorker;
    emitBodyWithDetachedComments(body, body.statements, emitBlockFunctionBody2);
    decreaseIndent();
    writeToken(20 /* CloseBraceToken */, body.statements.end, writePunctuation, body);
    onAfterEmitNode ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNode(body);
  }
  function emitBlockFunctionBodyOnSingleLine(body) {
    emitBlockFunctionBodyWorker(
      body,
      /*emitBlockFunctionBodyOnSingleLine*/
      true
    );
  }
  function emitBlockFunctionBodyWorker(body, emitBlockFunctionBodyOnSingleLine2) {
    const statementOffset ***REMOVED*** emitPrologueDirectives(body.statements);
    const pos ***REMOVED*** writer.getTextPos();
    emitHelpers(body);
    if (statementOffset ***REMOVED******REMOVED******REMOVED*** 0 && pos ***REMOVED******REMOVED******REMOVED*** writer.getTextPos() && emitBlockFunctionBodyOnSingleLine2) {
      decreaseIndent();
      emitList(body, body.statements, 768 /* SingleLineFunctionBodyStatements */);
      increaseIndent();
    } else {
      emitList(
        body,
        body.statements,
        1 /* MultiLineFunctionBodyStatements */,
        /*parenthesizerRule*/
        void 0,
        statementOffset
      );
    }
  }
  function emitClassDeclaration(node) {
    emitClassDeclarationOrExpression(node);
  }
  function emitClassDeclarationOrExpression(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      true
    );
    emitTokenWithComment(86 /* ClassKeyword */, moveRangePastModifiers(node).pos, writeKeyword, node);
    if (node.name) {
      writeSpace();
      emitIdentifierName(node.name);
    }
    const indentedFlag ***REMOVED*** getEmitFlags(node) & 131072 /* Indented */;
    if (indentedFlag) {
      increaseIndent();
    }
    emitTypeParameters(node, node.typeParameters);
    emitList(node, node.heritageClauses, 0 /* ClassHeritageClauses */);
    writeSpace();
    writePunctuation("{");
    pushNameGenerationScope(node);
    forEach(node.members, generateMemberNames);
    emitList(node, node.members, 129 /* ClassMembers */);
    popNameGenerationScope(node);
    writePunctuation("}");
    if (indentedFlag) {
      decreaseIndent();
    }
  }
  function emitInterfaceDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    writeKeyword("interface");
    writeSpace();
    emit(node.name);
    emitTypeParameters(node, node.typeParameters);
    emitList(node, node.heritageClauses, 512 /* HeritageClauses */);
    writeSpace();
    writePunctuation("{");
    pushNameGenerationScope(node);
    forEach(node.members, generateMemberNames);
    emitList(node, node.members, 129 /* InterfaceMembers */);
    popNameGenerationScope(node);
    writePunctuation("}");
  }
  function emitTypeAliasDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    writeKeyword("type");
    writeSpace();
    emit(node.name);
    emitTypeParameters(node, node.typeParameters);
    writeSpace();
    writePunctuation("***REMOVED***");
    writeSpace();
    emit(node.type);
    writeTrailingSemicolon();
  }
  function emitEnumDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    writeKeyword("enum");
    writeSpace();
    emit(node.name);
    writeSpace();
    writePunctuation("{");
    emitList(node, node.members, 145 /* EnumMembers */);
    writePunctuation("}");
  }
  function emitModuleDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    if (~node.flags & 2048 /* GlobalAugmentation */) {
      writeKeyword(node.flags & 32 /* Namespace */ ? "namespace" : "module");
      writeSpace();
    }
    emit(node.name);
    let body ***REMOVED*** node.body;
    if (!body) return writeTrailingSemicolon();
    while (body && isModuleDeclaration(body)) {
      writePunctuation(".");
      emit(body.name);
      body ***REMOVED*** body.body;
    }
    writeSpace();
    emit(body);
  }
  function emitModuleBlock(node) {
    pushNameGenerationScope(node);
    forEach(node.statements, generateNames);
    emitBlockStatements(
      node,
      /*forceSingleLine*/
      isEmptyBlock(node)
    );
    popNameGenerationScope(node);
  }
  function emitCaseBlock(node) {
    emitTokenWithComment(19 /* OpenBraceToken */, node.pos, writePunctuation, node);
    emitList(node, node.clauses, 129 /* CaseBlockClauses */);
    emitTokenWithComment(
      20 /* CloseBraceToken */,
      node.clauses.end,
      writePunctuation,
      node,
      /*indentLeading*/
      true
    );
  }
  function emitImportEqualsDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    emitTokenWithComment(102 /* ImportKeyword */, node.modifiers ? node.modifiers.end : node.pos, writeKeyword, node);
    writeSpace();
    if (node.isTypeOnly) {
      emitTokenWithComment(156 /* TypeKeyword */, node.pos, writeKeyword, node);
      writeSpace();
    }
    emit(node.name);
    writeSpace();
    emitTokenWithComment(64 /* EqualsToken */, node.name.end, writePunctuation, node);
    writeSpace();
    emitModuleReference(node.moduleReference);
    writeTrailingSemicolon();
  }
  function emitModuleReference(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      emitExpression(node);
    } else {
      emit(node);
    }
  }
  function emitImportDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    emitTokenWithComment(102 /* ImportKeyword */, node.modifiers ? node.modifiers.end : node.pos, writeKeyword, node);
    writeSpace();
    if (node.importClause) {
      emit(node.importClause);
      writeSpace();
      emitTokenWithComment(161 /* FromKeyword */, node.importClause.end, writeKeyword, node);
      writeSpace();
    }
    emitExpression(node.moduleSpecifier);
    if (node.attributes) {
      emitWithLeadingSpace(node.attributes);
    }
    writeTrailingSemicolon();
  }
  function emitImportClause(node) {
    if (node.phaseModifier !***REMOVED******REMOVED*** void 0) {
      emitTokenWithComment(node.phaseModifier, node.pos, writeKeyword, node);
      writeSpace();
    }
    emit(node.name);
    if (node.name && node.namedBindings) {
      emitTokenWithComment(28 /* CommaToken */, node.name.end, writePunctuation, node);
      writeSpace();
    }
    emit(node.namedBindings);
  }
  function emitNamespaceImport(node) {
    const asPos ***REMOVED*** emitTokenWithComment(42 /* AsteriskToken */, node.pos, writePunctuation, node);
    writeSpace();
    emitTokenWithComment(130 /* AsKeyword */, asPos, writeKeyword, node);
    writeSpace();
    emit(node.name);
  }
  function emitNamedImports(node) {
    emitNamedImportsOrExports(node);
  }
  function emitImportSpecifier(node) {
    emitImportOrExportSpecifier(node);
  }
  function emitExportAssignment(node) {
    const nextPos ***REMOVED*** emitTokenWithComment(95 /* ExportKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    if (node.isExportEquals) {
      emitTokenWithComment(64 /* EqualsToken */, nextPos, writeOperator, node);
    } else {
      emitTokenWithComment(90 /* DefaultKeyword */, nextPos, writeKeyword, node);
    }
    writeSpace();
    emitExpression(
      node.expression,
      node.isExportEquals ? parenthesizer.getParenthesizeRightSideOfBinaryForOperator(64 /* EqualsToken */) : parenthesizer.parenthesizeExpressionOfExportDefault
    );
    writeTrailingSemicolon();
  }
  function emitExportDeclaration(node) {
    emitDecoratorsAndModifiers(
      node,
      node.modifiers,
      /*allowDecorators*/
      false
    );
    let nextPos ***REMOVED*** emitTokenWithComment(95 /* ExportKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    if (node.isTypeOnly) {
      nextPos ***REMOVED*** emitTokenWithComment(156 /* TypeKeyword */, nextPos, writeKeyword, node);
      writeSpace();
    }
    if (node.exportClause) {
      emit(node.exportClause);
    } else {
      nextPos ***REMOVED*** emitTokenWithComment(42 /* AsteriskToken */, nextPos, writePunctuation, node);
    }
    if (node.moduleSpecifier) {
      writeSpace();
      const fromPos ***REMOVED*** node.exportClause ? node.exportClause.end : nextPos;
      emitTokenWithComment(161 /* FromKeyword */, fromPos, writeKeyword, node);
      writeSpace();
      emitExpression(node.moduleSpecifier);
    }
    if (node.attributes) {
      emitWithLeadingSpace(node.attributes);
    }
    writeTrailingSemicolon();
  }
  function emitImportTypeNodeAttributes(node) {
    writePunctuation("{");
    writeSpace();
    writeKeyword(node.token ***REMOVED******REMOVED******REMOVED*** 132 /* AssertKeyword */ ? "assert" : "with");
    writePunctuation(":");
    writeSpace();
    const elements ***REMOVED*** node.elements;
    emitList(node, elements, 526226 /* ImportAttributes */);
    writeSpace();
    writePunctuation("}");
  }
  function emitImportAttributes(node) {
    emitTokenWithComment(node.token, node.pos, writeKeyword, node);
    writeSpace();
    const elements ***REMOVED*** node.elements;
    emitList(node, elements, 526226 /* ImportAttributes */);
  }
  function emitImportAttribute(node) {
    emit(node.name);
    writePunctuation(":");
    writeSpace();
    const value ***REMOVED*** node.value;
    if ((getEmitFlags(value) & 1024 /* NoLeadingComments */) ***REMOVED******REMOVED******REMOVED*** 0) {
      const commentRange ***REMOVED*** getCommentRange(value);
      emitTrailingCommentsOfPosition(commentRange.pos);
    }
    emit(value);
  }
  function emitNamespaceExportDeclaration(node) {
    let nextPos ***REMOVED*** emitTokenWithComment(95 /* ExportKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    nextPos ***REMOVED*** emitTokenWithComment(130 /* AsKeyword */, nextPos, writeKeyword, node);
    writeSpace();
    nextPos ***REMOVED*** emitTokenWithComment(145 /* NamespaceKeyword */, nextPos, writeKeyword, node);
    writeSpace();
    emit(node.name);
    writeTrailingSemicolon();
  }
  function emitNamespaceExport(node) {
    const asPos ***REMOVED*** emitTokenWithComment(42 /* AsteriskToken */, node.pos, writePunctuation, node);
    writeSpace();
    emitTokenWithComment(130 /* AsKeyword */, asPos, writeKeyword, node);
    writeSpace();
    emit(node.name);
  }
  function emitNamedExports(node) {
    emitNamedImportsOrExports(node);
  }
  function emitExportSpecifier(node) {
    emitImportOrExportSpecifier(node);
  }
  function emitNamedImportsOrExports(node) {
    writePunctuation("{");
    emitList(node, node.elements, 525136 /* NamedImportsOrExportsElements */);
    writePunctuation("}");
  }
  function emitImportOrExportSpecifier(node) {
    if (node.isTypeOnly) {
      writeKeyword("type");
      writeSpace();
    }
    if (node.propertyName) {
      emit(node.propertyName);
      writeSpace();
      emitTokenWithComment(130 /* AsKeyword */, node.propertyName.end, writeKeyword, node);
      writeSpace();
    }
    emit(node.name);
  }
  function emitExternalModuleReference(node) {
    writeKeyword("require");
    writePunctuation("(");
    emitExpression(node.expression);
    writePunctuation(")");
  }
  function emitJsxElement(node) {
    emit(node.openingElement);
    emitList(node, node.children, 262144 /* JsxElementOrFragmentChildren */);
    emit(node.closingElement);
  }
  function emitJsxSelfClosingElement(node) {
    writePunctuation("<");
    emitJsxTagName(node.tagName);
    emitTypeArguments(node, node.typeArguments);
    writeSpace();
    emit(node.attributes);
    writePunctuation("/>");
  }
  function emitJsxFragment(node) {
    emit(node.openingFragment);
    emitList(node, node.children, 262144 /* JsxElementOrFragmentChildren */);
    emit(node.closingFragment);
  }
  function emitJsxOpeningElementOrFragment(node) {
    writePunctuation("<");
    if (isJsxOpeningElement(node)) {
      const indented ***REMOVED*** writeLineSeparatorsAndIndentBefore(node.tagName, node);
      emitJsxTagName(node.tagName);
      emitTypeArguments(node, node.typeArguments);
      if (node.attributes.properties && node.attributes.properties.length > 0) {
        writeSpace();
      }
      emit(node.attributes);
      writeLineSeparatorsAfter(node.attributes, node);
      decreaseIndentIf(indented);
    }
    writePunctuation(">");
  }
  function emitJsxText(node) {
    writer.writeLiteral(node.text);
  }
  function emitJsxClosingElementOrFragment(node) {
    writePunctuation("</");
    if (isJsxClosingElement(node)) {
      emitJsxTagName(node.tagName);
    }
    writePunctuation(">");
  }
  function emitJsxAttributes(node) {
    emitList(node, node.properties, 262656 /* JsxElementAttributes */);
  }
  function emitJsxAttribute(node) {
    emit(node.name);
    emitNodeWithPrefix("***REMOVED***", writePunctuation, node.initializer, emitJsxAttributeValue);
  }
  function emitJsxSpreadAttribute(node) {
    writePunctuation("{...");
    emitExpression(node.expression);
    writePunctuation("}");
  }
  function hasTrailingCommentsAtPosition(pos) {
    let result ***REMOVED*** false;
    forEachTrailingCommentRange((currentSourceFile ***REMOVED******REMOVED*** null ? void 0 : currentSourceFile.text) || "", pos + 1, () ***REMOVED***> result ***REMOVED*** true);
    return result;
  }
  function hasLeadingCommentsAtPosition(pos) {
    let result ***REMOVED*** false;
    forEachLeadingCommentRange((currentSourceFile ***REMOVED******REMOVED*** null ? void 0 : currentSourceFile.text) || "", pos + 1, () ***REMOVED***> result ***REMOVED*** true);
    return result;
  }
  function hasCommentsAtPosition(pos) {
    return hasTrailingCommentsAtPosition(pos) || hasLeadingCommentsAtPosition(pos);
  }
  function emitJsxExpression(node) {
    var _a;
    if (node.expression || !commentsDisabled && !nodeIsSynthesized(node) && hasCommentsAtPosition(node.pos)) {
      const isMultiline ***REMOVED*** currentSourceFile && !nodeIsSynthesized(node) && getLineAndCharacterOfPosition(currentSourceFile, node.pos).line !***REMOVED******REMOVED*** getLineAndCharacterOfPosition(currentSourceFile, node.end).line;
      if (isMultiline) {
        writer.increaseIndent();
      }
      const end ***REMOVED*** emitTokenWithComment(19 /* OpenBraceToken */, node.pos, writePunctuation, node);
      emit(node.dotDotDotToken);
      emitExpression(node.expression);
      emitTokenWithComment(20 /* CloseBraceToken */, ((_a ***REMOVED*** node.expression) ***REMOVED******REMOVED*** null ? void 0 : _a.end) || end, writePunctuation, node);
      if (isMultiline) {
        writer.decreaseIndent();
      }
    }
  }
  function emitJsxNamespacedName(node) {
    emitIdentifierName(node.namespace);
    writePunctuation(":");
    emitIdentifierName(node.name);
  }
  function emitJsxTagName(node) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */) {
      emitExpression(node);
    } else {
      emit(node);
    }
  }
  function emitCaseClause(node) {
    emitTokenWithComment(84 /* CaseKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    emitExpression(node.expression, parenthesizer.parenthesizeExpressionForDisallowedComma);
    emitCaseOrDefaultClauseRest(node, node.statements, node.expression.end);
  }
  function emitDefaultClause(node) {
    const pos ***REMOVED*** emitTokenWithComment(90 /* DefaultKeyword */, node.pos, writeKeyword, node);
    emitCaseOrDefaultClauseRest(node, node.statements, pos);
  }
  function emitCaseOrDefaultClauseRest(parentNode, statements, colonPos) {
    const emitAsSingleStatement ***REMOVED*** statements.length ***REMOVED******REMOVED******REMOVED*** 1 && // treat synthesized nodes as located on the same line for emit purposes
    (!currentSourceFile || nodeIsSynthesized(parentNode) || nodeIsSynthesized(statements[0]) || rangeStartPositionsAreOnSameLine(parentNode, statements[0], currentSourceFile));
    let format ***REMOVED*** 163969 /* CaseOrDefaultClauseStatements */;
    if (emitAsSingleStatement) {
      writeToken(59 /* ColonToken */, colonPos, writePunctuation, parentNode);
      writeSpace();
      format &***REMOVED*** ~(1 /* MultiLine */ | 128 /* Indented */);
    } else {
      emitTokenWithComment(59 /* ColonToken */, colonPos, writePunctuation, parentNode);
    }
    emitList(parentNode, statements, format);
  }
  function emitHeritageClause(node) {
    writeSpace();
    writeTokenText(node.token, writeKeyword);
    writeSpace();
    emitList(node, node.types, 528 /* HeritageClauseTypes */);
  }
  function emitCatchClause(node) {
    const openParenPos ***REMOVED*** emitTokenWithComment(85 /* CatchKeyword */, node.pos, writeKeyword, node);
    writeSpace();
    if (node.variableDeclaration) {
      emitTokenWithComment(21 /* OpenParenToken */, openParenPos, writePunctuation, node);
      emit(node.variableDeclaration);
      emitTokenWithComment(22 /* CloseParenToken */, node.variableDeclaration.end, writePunctuation, node);
      writeSpace();
    }
    emit(node.block);
  }
  function emitPropertyAssignment(node) {
    emit(node.name);
    writePunctuation(":");
    writeSpace();
    const initializer ***REMOVED*** node.initializer;
    if ((getEmitFlags(initializer) & 1024 /* NoLeadingComments */) ***REMOVED******REMOVED******REMOVED*** 0) {
      const commentRange ***REMOVED*** getCommentRange(initializer);
      emitTrailingCommentsOfPosition(commentRange.pos);
    }
    emitExpression(initializer, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitShorthandPropertyAssignment(node) {
    emit(node.name);
    if (node.objectAssignmentInitializer) {
      writeSpace();
      writePunctuation("***REMOVED***");
      writeSpace();
      emitExpression(node.objectAssignmentInitializer, parenthesizer.parenthesizeExpressionForDisallowedComma);
    }
  }
  function emitSpreadAssignment(node) {
    if (node.expression) {
      emitTokenWithComment(26 /* DotDotDotToken */, node.pos, writePunctuation, node);
      emitExpression(node.expression, parenthesizer.parenthesizeExpressionForDisallowedComma);
    }
  }
  function emitEnumMember(node) {
    emit(node.name);
    emitInitializer(node.initializer, node.name.end, node, parenthesizer.parenthesizeExpressionForDisallowedComma);
  }
  function emitJSDoc(node) {
    write("/**");
    if (node.comment) {
      const text ***REMOVED*** getTextOfJSDocComment(node.comment);
      if (text) {
        const lines ***REMOVED*** text.split(/\r\n?|\n/);
        for (const line of lines) {
          writeLine();
          writeSpace();
          writePunctuation("*");
          writeSpace();
          write(line);
        }
      }
    }
    if (node.tags) {
      if (node.tags.length ***REMOVED******REMOVED******REMOVED*** 1 && node.tags[0].kind ***REMOVED******REMOVED******REMOVED*** 345 /* JSDocTypeTag */ && !node.comment) {
        writeSpace();
        emit(node.tags[0]);
      } else {
        emitList(node, node.tags, 33 /* JSDocComment */);
      }
    }
    writeSpace();
    write("*/");
  }
  function emitJSDocSimpleTypedTag(tag) {
    emitJSDocTagName(tag.tagName);
    emitJSDocTypeExpression(tag.typeExpression);
    emitJSDocComment(tag.comment);
  }
  function emitJSDocSeeTag(tag) {
    emitJSDocTagName(tag.tagName);
    emit(tag.name);
    emitJSDocComment(tag.comment);
  }
  function emitJSDocImportTag(tag) {
    emitJSDocTagName(tag.tagName);
    writeSpace();
    if (tag.importClause) {
      emit(tag.importClause);
      writeSpace();
      emitTokenWithComment(161 /* FromKeyword */, tag.importClause.end, writeKeyword, tag);
      writeSpace();
    }
    emitExpression(tag.moduleSpecifier);
    if (tag.attributes) {
      emitWithLeadingSpace(tag.attributes);
    }
    emitJSDocComment(tag.comment);
  }
  function emitJSDocNameReference(node) {
    writeSpace();
    writePunctuation("{");
    emit(node.name);
    writePunctuation("}");
  }
  function emitJSDocHeritageTag(tag) {
    emitJSDocTagName(tag.tagName);
    writeSpace();
    writePunctuation("{");
    emit(tag.class);
    writePunctuation("}");
    emitJSDocComment(tag.comment);
  }
  function emitJSDocTemplateTag(tag) {
    emitJSDocTagName(tag.tagName);
    emitJSDocTypeExpression(tag.constraint);
    writeSpace();
    emitList(tag, tag.typeParameters, 528 /* CommaListElements */);
    emitJSDocComment(tag.comment);
  }
  function emitJSDocTypedefTag(tag) {
    emitJSDocTagName(tag.tagName);
    if (tag.typeExpression) {
      if (tag.typeExpression.kind ***REMOVED******REMOVED******REMOVED*** 310 /* JSDocTypeExpression */) {
        emitJSDocTypeExpression(tag.typeExpression);
      } else {
        writeSpace();
        writePunctuation("{");
        write("Object");
        if (tag.typeExpression.isArrayType) {
          writePunctuation("[");
          writePunctuation("]");
        }
        writePunctuation("}");
      }
    }
    if (tag.fullName) {
      writeSpace();
      emit(tag.fullName);
    }
    emitJSDocComment(tag.comment);
    if (tag.typeExpression && tag.typeExpression.kind ***REMOVED******REMOVED******REMOVED*** 323 /* JSDocTypeLiteral */) {
      emitJSDocTypeLiteral(tag.typeExpression);
    }
  }
  function emitJSDocCallbackTag(tag) {
    emitJSDocTagName(tag.tagName);
    if (tag.name) {
      writeSpace();
      emit(tag.name);
    }
    emitJSDocComment(tag.comment);
    emitJSDocSignature(tag.typeExpression);
  }
  function emitJSDocOverloadTag(tag) {
    emitJSDocComment(tag.comment);
    emitJSDocSignature(tag.typeExpression);
  }
  function emitJSDocSimpleTag(tag) {
    emitJSDocTagName(tag.tagName);
    emitJSDocComment(tag.comment);
  }
  function emitJSDocTypeLiteral(lit) {
    emitList(lit, factory.createNodeArray(lit.jsDocPropertyTags), 33 /* JSDocComment */);
  }
  function emitJSDocSignature(sig) {
    if (sig.typeParameters) {
      emitList(sig, factory.createNodeArray(sig.typeParameters), 33 /* JSDocComment */);
    }
    if (sig.parameters) {
      emitList(sig, factory.createNodeArray(sig.parameters), 33 /* JSDocComment */);
    }
    if (sig.type) {
      writeLine();
      writeSpace();
      writePunctuation("*");
      writeSpace();
      emit(sig.type);
    }
  }
  function emitJSDocPropertyLikeTag(param) {
    emitJSDocTagName(param.tagName);
    emitJSDocTypeExpression(param.typeExpression);
    writeSpace();
    if (param.isBracketed) {
      writePunctuation("[");
    }
    emit(param.name);
    if (param.isBracketed) {
      writePunctuation("]");
    }
    emitJSDocComment(param.comment);
  }
  function emitJSDocTagName(tagName) {
    writePunctuation("@");
    emit(tagName);
  }
  function emitJSDocComment(comment) {
    const text ***REMOVED*** getTextOfJSDocComment(comment);
    if (text) {
      writeSpace();
      write(text);
    }
  }
  function emitJSDocTypeExpression(typeExpression) {
    if (typeExpression) {
      writeSpace();
      writePunctuation("{");
      emit(typeExpression.type);
      writePunctuation("}");
    }
  }
  function emitSourceFile(node) {
    writeLine();
    const statements ***REMOVED*** node.statements;
    const shouldEmitDetachedComment ***REMOVED*** statements.length ***REMOVED******REMOVED******REMOVED*** 0 || !isPrologueDirective(statements[0]) || nodeIsSynthesized(statements[0]);
    if (shouldEmitDetachedComment) {
      emitBodyWithDetachedComments(node, statements, emitSourceFileWorker);
      return;
    }
    emitSourceFileWorker(node);
  }
  function emitSyntheticTripleSlashReferencesIfNeeded(node) {
    emitTripleSlashDirectives(!!node.hasNoDefaultLib, node.syntheticFileReferences || [], node.syntheticTypeReferences || [], node.syntheticLibReferences || []);
  }
  function emitTripleSlashDirectivesIfNeeded(node) {
    if (node.isDeclarationFile) emitTripleSlashDirectives(node.hasNoDefaultLib, node.referencedFiles, node.typeReferenceDirectives, node.libReferenceDirectives);
  }
  function emitTripleSlashDirectives(hasNoDefaultLib, files, types, libs2) {
    if (hasNoDefaultLib) {
      writeComment(`/// <reference no-default-lib***REMOVED***"true"/>`);
      writeLine();
    }
    if (currentSourceFile && currentSourceFile.moduleName) {
      writeComment(`/// <amd-module name***REMOVED***"${currentSourceFile.moduleName}" />`);
      writeLine();
    }
    if (currentSourceFile && currentSourceFile.amdDependencies) {
      for (const dep of currentSourceFile.amdDependencies) {
        if (dep.name) {
          writeComment(`/// <amd-dependency name***REMOVED***"${dep.name}" path***REMOVED***"${dep.path}" />`);
        } else {
          writeComment(`/// <amd-dependency path***REMOVED***"${dep.path}" />`);
        }
        writeLine();
      }
    }
    function writeDirectives(kind, directives) {
      for (const directive of directives) {
        const resolutionMode ***REMOVED*** directive.resolutionMode ? `resolution-mode***REMOVED***"${directive.resolutionMode ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ ? "import" : "require"}" ` : "";
        const preserve ***REMOVED*** directive.preserve ? `preserve***REMOVED***"true" ` : "";
        writeComment(`/// <reference ${kind}***REMOVED***"${directive.fileName}" ${resolutionMode}${preserve}/>`);
        writeLine();
      }
    }
    writeDirectives("path", files);
    writeDirectives("types", types);
    writeDirectives("lib", libs2);
  }
  function emitSourceFileWorker(node) {
    const statements ***REMOVED*** node.statements;
    pushNameGenerationScope(node);
    forEach(node.statements, generateNames);
    emitHelpers(node);
    const index ***REMOVED*** findIndex(statements, (statement) ***REMOVED***> !isPrologueDirective(statement));
    emitTripleSlashDirectivesIfNeeded(node);
    emitList(
      node,
      statements,
      1 /* MultiLine */,
      /*parenthesizerRule*/
      void 0,
      index ***REMOVED******REMOVED******REMOVED*** -1 ? statements.length : index
    );
    popNameGenerationScope(node);
  }
  function emitPartiallyEmittedExpression(node) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    if (!(emitFlags & 1024 /* NoLeadingComments */) && node.pos !***REMOVED******REMOVED*** node.expression.pos) {
      emitTrailingCommentsOfPosition(node.expression.pos);
    }
    emitExpression(node.expression);
    if (!(emitFlags & 2048 /* NoTrailingComments */) && node.end !***REMOVED******REMOVED*** node.expression.end) {
      emitLeadingCommentsOfPosition(node.expression.end);
    }
  }
  function emitCommaList(node) {
    emitExpressionList(
      node,
      node.elements,
      528 /* CommaListElements */,
      /*parenthesizerRule*/
      void 0
    );
  }
  function emitPrologueDirectives(statements, sourceFile, seenPrologueDirectives) {
    let needsToSetSourceFile ***REMOVED*** !!sourceFile;
    for (let i ***REMOVED*** 0; i < statements.length; i++) {
      const statement ***REMOVED*** statements[i];
      if (isPrologueDirective(statement)) {
        const shouldEmitPrologueDirective ***REMOVED*** seenPrologueDirectives ? !seenPrologueDirectives.has(statement.expression.text) : true;
        if (shouldEmitPrologueDirective) {
          if (needsToSetSourceFile) {
            needsToSetSourceFile ***REMOVED*** false;
            setSourceFile(sourceFile);
          }
          writeLine();
          emit(statement);
          if (seenPrologueDirectives) {
            seenPrologueDirectives.add(statement.expression.text);
          }
        }
      } else {
        return i;
      }
    }
    return statements.length;
  }
  function emitPrologueDirectivesIfNeeded(sourceFileOrBundle) {
    if (isSourceFile(sourceFileOrBundle)) {
      emitPrologueDirectives(sourceFileOrBundle.statements, sourceFileOrBundle);
    } else {
      const seenPrologueDirectives ***REMOVED*** /* @__PURE__ */ new Set();
      for (const sourceFile of sourceFileOrBundle.sourceFiles) {
        emitPrologueDirectives(sourceFile.statements, sourceFile, seenPrologueDirectives);
      }
      setSourceFile(void 0);
    }
  }
  function emitShebangIfNeeded(sourceFileOrBundle) {
    if (isSourceFile(sourceFileOrBundle)) {
      const shebang ***REMOVED*** getShebang(sourceFileOrBundle.text);
      if (shebang) {
        writeComment(shebang);
        writeLine();
        return true;
      }
    } else {
      for (const sourceFile of sourceFileOrBundle.sourceFiles) {
        if (emitShebangIfNeeded(sourceFile)) {
          return true;
        }
      }
    }
  }
  function emitNodeWithWriter(node, writer2) {
    if (!node) return;
    const savedWrite ***REMOVED*** write;
    write ***REMOVED*** writer2;
    emit(node);
    write ***REMOVED*** savedWrite;
  }
  function emitDecoratorsAndModifiers(node, modifiers, allowDecorators) {
    if (modifiers ***REMOVED******REMOVED*** null ? void 0 : modifiers.length) {
      if (every(modifiers, isModifier)) {
        return emitModifierList(node, modifiers);
      }
      if (every(modifiers, isDecorator)) {
        if (allowDecorators) {
          return emitDecoratorList(node, modifiers);
        }
        return node.pos;
      }
      onBeforeEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNodeArray(modifiers);
      let lastMode;
      let mode;
      let start ***REMOVED*** 0;
      let pos ***REMOVED*** 0;
      let lastModifier;
      while (start < modifiers.length) {
        while (pos < modifiers.length) {
          lastModifier ***REMOVED*** modifiers[pos];
          mode ***REMOVED*** isDecorator(lastModifier) ? "decorators" : "modifiers";
          if (lastMode ***REMOVED******REMOVED******REMOVED*** void 0) {
            lastMode ***REMOVED*** mode;
          } else if (mode !***REMOVED******REMOVED*** lastMode) {
            break;
          }
          pos++;
        }
        const textRange ***REMOVED*** { pos: -1, end: -1 };
        if (start ***REMOVED******REMOVED******REMOVED*** 0) textRange.pos ***REMOVED*** modifiers.pos;
        if (pos ***REMOVED******REMOVED******REMOVED*** modifiers.length - 1) textRange.end ***REMOVED*** modifiers.end;
        if (lastMode ***REMOVED******REMOVED******REMOVED*** "modifiers" || allowDecorators) {
          emitNodeListItems(
            emit,
            node,
            modifiers,
            lastMode ***REMOVED******REMOVED******REMOVED*** "modifiers" ? 2359808 /* Modifiers */ : 2146305 /* Decorators */,
            /*parenthesizerRule*/
            void 0,
            start,
            pos - start,
            /*hasTrailingComma*/
            false,
            textRange
          );
        }
        start ***REMOVED*** pos;
        lastMode ***REMOVED*** mode;
        pos++;
      }
      onAfterEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNodeArray(modifiers);
      if (lastModifier && !positionIsSynthesized(lastModifier.end)) {
        return lastModifier.end;
      }
    }
    return node.pos;
  }
  function emitModifierList(node, modifiers) {
    emitList(node, modifiers, 2359808 /* Modifiers */);
    const lastModifier ***REMOVED*** lastOrUndefined(modifiers);
    return lastModifier && !positionIsSynthesized(lastModifier.end) ? lastModifier.end : node.pos;
  }
  function emitTypeAnnotation(node) {
    if (node) {
      writePunctuation(":");
      writeSpace();
      emit(node);
    }
  }
  function emitInitializer(node, equalCommentStartPos, container, parenthesizerRule) {
    if (node) {
      writeSpace();
      emitTokenWithComment(64 /* EqualsToken */, equalCommentStartPos, writeOperator, container);
      writeSpace();
      emitExpression(node, parenthesizerRule);
    }
  }
  function emitNodeWithPrefix(prefix, prefixWriter, node, emit2) {
    if (node) {
      prefixWriter(prefix);
      emit2(node);
    }
  }
  function emitWithLeadingSpace(node) {
    if (node) {
      writeSpace();
      emit(node);
    }
  }
  function emitExpressionWithLeadingSpace(node, parenthesizerRule) {
    if (node) {
      writeSpace();
      emitExpression(node, parenthesizerRule);
    }
  }
  function emitWithTrailingSpace(node) {
    if (node) {
      emit(node);
      writeSpace();
    }
  }
  function emitEmbeddedStatement(parent, node) {
    if (isBlock(node) || getEmitFlags(parent) & 1 /* SingleLine */ || preserveSourceNewlines && !getLeadingLineTerminatorCount(parent, node, 0 /* None */)) {
      writeSpace();
      emit(node);
    } else {
      writeLine();
      increaseIndent();
      if (isEmptyStatement(node)) {
        pipelineEmit(5 /* EmbeddedStatement */, node);
      } else {
        emit(node);
      }
      decreaseIndent();
    }
  }
  function emitDecoratorList(parentNode, decorators) {
    emitList(parentNode, decorators, 2146305 /* Decorators */);
    const lastDecorator ***REMOVED*** lastOrUndefined(decorators);
    return lastDecorator && !positionIsSynthesized(lastDecorator.end) ? lastDecorator.end : parentNode.pos;
  }
  function emitTypeArguments(parentNode, typeArguments) {
    emitList(parentNode, typeArguments, 53776 /* TypeArguments */, typeArgumentParenthesizerRuleSelector);
  }
  function emitTypeParameters(parentNode, typeParameters) {
    if (isFunctionLike(parentNode) && parentNode.typeArguments) {
      return emitTypeArguments(parentNode, parentNode.typeArguments);
    }
    emitList(parentNode, typeParameters, 53776 /* TypeParameters */ | (isArrowFunction(parentNode) ? 64 /* AllowTrailingComma */ : 0 /* None */));
  }
  function emitParameters(parentNode, parameters) {
    emitList(parentNode, parameters, 2576 /* Parameters */);
  }
  function canEmitSimpleArrowHead(parentNode, parameters) {
    const parameter ***REMOVED*** singleOrUndefined(parameters);
    return parameter && parameter.pos ***REMOVED******REMOVED******REMOVED*** parentNode.pos && isArrowFunction(parentNode) && !parentNode.type && !some(parentNode.modifiers) && !some(parentNode.typeParameters) && !some(parameter.modifiers) && !parameter.dotDotDotToken && !parameter.questionToken && !parameter.type && !parameter.initializer && isIdentifier(parameter.name);
  }
  function emitParametersForArrow(parentNode, parameters) {
    if (canEmitSimpleArrowHead(parentNode, parameters)) {
      emitList(parentNode, parameters, 2576 /* Parameters */ & ~2048 /* Parenthesis */);
    } else {
      emitParameters(parentNode, parameters);
    }
  }
  function emitParametersForIndexSignature(parentNode, parameters) {
    emitList(parentNode, parameters, 8848 /* IndexSignatureParameters */);
  }
  function writeDelimiter(format) {
    switch (format & 60 /* DelimitersMask */) {
      case 0 /* None */:
        break;
      case 16 /* CommaDelimited */:
        writePunctuation(",");
        break;
      case 4 /* BarDelimited */:
        writeSpace();
        writePunctuation("|");
        break;
      case 32 /* AsteriskDelimited */:
        writeSpace();
        writePunctuation("*");
        writeSpace();
        break;
      case 8 /* AmpersandDelimited */:
        writeSpace();
        writePunctuation("&");
        break;
    }
  }
  function emitList(parentNode, children, format, parenthesizerRule, start, count) {
    emitNodeList(
      emit,
      parentNode,
      children,
      format | (parentNode && getEmitFlags(parentNode) & 2 /* MultiLine */ ? 65536 /* PreferNewLine */ : 0),
      parenthesizerRule,
      start,
      count
    );
  }
  function emitExpressionList(parentNode, children, format, parenthesizerRule, start, count) {
    emitNodeList(emitExpression, parentNode, children, format, parenthesizerRule, start, count);
  }
  function emitNodeList(emit2, parentNode, children, format, parenthesizerRule, start ***REMOVED*** 0, count ***REMOVED*** children ? children.length - start : 0) {
    const isUndefined ***REMOVED*** children ***REMOVED******REMOVED******REMOVED*** void 0;
    if (isUndefined && format & 16384 /* OptionalIfUndefined */) {
      return;
    }
    const isEmpty ***REMOVED*** children ***REMOVED******REMOVED******REMOVED*** void 0 || start >***REMOVED*** children.length || count ***REMOVED******REMOVED******REMOVED*** 0;
    if (isEmpty && format & 32768 /* OptionalIfEmpty */) {
      onBeforeEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNodeArray(children);
      onAfterEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNodeArray(children);
      return;
    }
    if (format & 15360 /* BracketsMask */) {
      writePunctuation(getOpeningBracket(format));
      if (isEmpty && children) {
        emitTrailingCommentsOfPosition(
          children.pos,
          /*prefixSpace*/
          true
        );
      }
    }
    onBeforeEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onBeforeEmitNodeArray(children);
    if (isEmpty) {
      if (format & 1 /* MultiLine */ && !(preserveSourceNewlines && (!parentNode || currentSourceFile && rangeIsOnSingleLine(parentNode, currentSourceFile)))) {
        writeLine();
      } else if (format & 256 /* SpaceBetweenBraces */ && !(format & 524288 /* NoSpaceIfEmpty */)) {
        writeSpace();
      }
    } else {
      emitNodeListItems(emit2, parentNode, children, format, parenthesizerRule, start, count, children.hasTrailingComma, children);
    }
    onAfterEmitNodeArray ***REMOVED******REMOVED*** null ? void 0 : onAfterEmitNodeArray(children);
    if (format & 15360 /* BracketsMask */) {
      if (isEmpty && children) {
        emitLeadingCommentsOfPosition(children.end);
      }
      writePunctuation(getClosingBracket(format));
    }
  }
  function emitNodeListItems(emit2, parentNode, children, format, parenthesizerRule, start, count, hasTrailingComma, childrenTextRange) {
    const mayEmitInterveningComments ***REMOVED*** (format & 262144 /* NoInterveningComments */) ***REMOVED******REMOVED******REMOVED*** 0;
    let shouldEmitInterveningComments ***REMOVED*** mayEmitInterveningComments;
    const leadingLineTerminatorCount ***REMOVED*** getLeadingLineTerminatorCount(parentNode, children[start], format);
    if (leadingLineTerminatorCount) {
      writeLine(leadingLineTerminatorCount);
      shouldEmitInterveningComments ***REMOVED*** false;
    } else if (format & 256 /* SpaceBetweenBraces */) {
      writeSpace();
    }
    if (format & 128 /* Indented */) {
      increaseIndent();
    }
    const emitListItem ***REMOVED*** getEmitListItem(emit2, parenthesizerRule);
    let previousSibling;
    let shouldDecreaseIndentAfterEmit ***REMOVED*** false;
    for (let i ***REMOVED*** 0; i < count; i++) {
      const child ***REMOVED*** children[start + i];
      if (format & 32 /* AsteriskDelimited */) {
        writeLine();
        writeDelimiter(format);
      } else if (previousSibling) {
        if (format & 60 /* DelimitersMask */ && previousSibling.end !***REMOVED******REMOVED*** (parentNode ? parentNode.end : -1)) {
          const previousSiblingEmitFlags ***REMOVED*** getEmitFlags(previousSibling);
          if (!(previousSiblingEmitFlags & 2048 /* NoTrailingComments */)) {
            emitLeadingCommentsOfPosition(previousSibling.end);
          }
        }
        writeDelimiter(format);
        const separatingLineTerminatorCount ***REMOVED*** getSeparatingLineTerminatorCount(previousSibling, child, format);
        if (separatingLineTerminatorCount > 0) {
          if ((format & (3 /* LinesMask */ | 128 /* Indented */)) ***REMOVED******REMOVED******REMOVED*** 0 /* SingleLine */) {
            increaseIndent();
            shouldDecreaseIndentAfterEmit ***REMOVED*** true;
          }
          if (shouldEmitInterveningComments && format & 60 /* DelimitersMask */ && !positionIsSynthesized(child.pos)) {
            const commentRange ***REMOVED*** getCommentRange(child);
            emitTrailingCommentsOfPosition(
              commentRange.pos,
              /*prefixSpace*/
              !!(format & 512 /* SpaceBetweenSiblings */),
              /*forceNoNewline*/
              true
            );
          }
          writeLine(separatingLineTerminatorCount);
          shouldEmitInterveningComments ***REMOVED*** false;
        } else if (previousSibling && format & 512 /* SpaceBetweenSiblings */) {
          writeSpace();
        }
      }
      if (shouldEmitInterveningComments) {
        const commentRange ***REMOVED*** getCommentRange(child);
        emitTrailingCommentsOfPosition(commentRange.pos);
      } else {
        shouldEmitInterveningComments ***REMOVED*** mayEmitInterveningComments;
      }
      nextListElementPos ***REMOVED*** child.pos;
      emitListItem(child, emit2, parenthesizerRule, i);
      if (shouldDecreaseIndentAfterEmit) {
        decreaseIndent();
        shouldDecreaseIndentAfterEmit ***REMOVED*** false;
      }
      previousSibling ***REMOVED*** child;
    }
    const emitFlags ***REMOVED*** previousSibling ? getEmitFlags(previousSibling) : 0;
    const skipTrailingComments ***REMOVED*** commentsDisabled || !!(emitFlags & 2048 /* NoTrailingComments */);
    const emitTrailingComma ***REMOVED*** hasTrailingComma && format & 64 /* AllowTrailingComma */ && format & 16 /* CommaDelimited */;
    if (emitTrailingComma) {
      if (previousSibling && !skipTrailingComments) {
        emitTokenWithComment(28 /* CommaToken */, previousSibling.end, writePunctuation, previousSibling);
      } else {
        writePunctuation(",");
      }
    }
    if (previousSibling && (parentNode ? parentNode.end : -1) !***REMOVED******REMOVED*** previousSibling.end && format & 60 /* DelimitersMask */ && !skipTrailingComments) {
      emitLeadingCommentsOfPosition(emitTrailingComma && (childrenTextRange ***REMOVED******REMOVED*** null ? void 0 : childrenTextRange.end) ? childrenTextRange.end : previousSibling.end);
    }
    if (format & 128 /* Indented */) {
      decreaseIndent();
    }
    const closingLineTerminatorCount ***REMOVED*** getClosingLineTerminatorCount(parentNode, children[start + count - 1], format, childrenTextRange);
    if (closingLineTerminatorCount) {
      writeLine(closingLineTerminatorCount);
    } else if (format & (2097152 /* SpaceAfterList */ | 256 /* SpaceBetweenBraces */)) {
      writeSpace();
    }
  }
  function writeLiteral(s) {
    writer.writeLiteral(s);
  }
  function writeStringLiteral(s) {
    writer.writeStringLiteral(s);
  }
  function writeBase(s) {
    writer.write(s);
  }
  function writeSymbol(s, sym) {
    writer.writeSymbol(s, sym);
  }
  function writePunctuation(s) {
    writer.writePunctuation(s);
  }
  function writeTrailingSemicolon() {
    writer.writeTrailingSemicolon(";");
  }
  function writeKeyword(s) {
    writer.writeKeyword(s);
  }
  function writeOperator(s) {
    writer.writeOperator(s);
  }
  function writeParameter(s) {
    writer.writeParameter(s);
  }
  function writeComment(s) {
    writer.writeComment(s);
  }
  function writeSpace() {
    writer.writeSpace(" ");
  }
  function writeProperty(s) {
    writer.writeProperty(s);
  }
  function nonEscapingWrite(s) {
    if (writer.nonEscapingWrite) {
      writer.nonEscapingWrite(s);
    } else {
      writer.write(s);
    }
  }
  function writeLine(count ***REMOVED*** 1) {
    for (let i ***REMOVED*** 0; i < count; i++) {
      writer.writeLine(i > 0);
    }
  }
  function increaseIndent() {
    writer.increaseIndent();
  }
  function decreaseIndent() {
    writer.decreaseIndent();
  }
  function writeToken(token, pos, writer2, contextNode) {
    return !sourceMapsDisabled ? emitTokenWithSourceMap(contextNode, token, writer2, pos, writeTokenText) : writeTokenText(token, writer2, pos);
  }
  function writeTokenNode(node, writer2) {
    if (onBeforeEmitToken) {
      onBeforeEmitToken(node);
    }
    writer2(tokenToString(node.kind));
    if (onAfterEmitToken) {
      onAfterEmitToken(node);
    }
  }
  function writeTokenText(token, writer2, pos) {
    const tokenString ***REMOVED*** tokenToString(token);
    writer2(tokenString);
    return pos < 0 ? pos : pos + tokenString.length;
  }
  function writeLineOrSpace(parentNode, prevChildNode, nextChildNode) {
    if (getEmitFlags(parentNode) & 1 /* SingleLine */) {
      writeSpace();
    } else if (preserveSourceNewlines) {
      const lines ***REMOVED*** getLinesBetweenNodes(parentNode, prevChildNode, nextChildNode);
      if (lines) {
        writeLine(lines);
      } else {
        writeSpace();
      }
    } else {
      writeLine();
    }
  }
  function writeLines(text) {
    const lines ***REMOVED*** text.split(/\r\n?|\n/);
    const indentation ***REMOVED*** guessIndentation(lines);
    for (const lineText of lines) {
      const line ***REMOVED*** indentation ? lineText.slice(indentation) : lineText;
      if (line.length) {
        writeLine();
        write(line);
      }
    }
  }
  function writeLinesAndIndent(lineCount, writeSpaceIfNotIndenting) {
    if (lineCount) {
      increaseIndent();
      writeLine(lineCount);
    } else if (writeSpaceIfNotIndenting) {
      writeSpace();
    }
  }
  function decreaseIndentIf(value1, value2) {
    if (value1) {
      decreaseIndent();
    }
    if (value2) {
      decreaseIndent();
    }
  }
  function getLeadingLineTerminatorCount(parentNode, firstChild, format) {
    if (format & 2 /* PreserveLines */ || preserveSourceNewlines) {
      if (format & 65536 /* PreferNewLine */) {
        return 1;
      }
      if (firstChild ***REMOVED******REMOVED******REMOVED*** void 0) {
        return !parentNode || currentSourceFile && rangeIsOnSingleLine(parentNode, currentSourceFile) ? 0 : 1;
      }
      if (firstChild.pos ***REMOVED******REMOVED******REMOVED*** nextListElementPos) {
        return 0;
      }
      if (firstChild.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */) {
        return 0;
      }
      if (currentSourceFile && parentNode && !positionIsSynthesized(parentNode.pos) && !nodeIsSynthesized(firstChild) && (!firstChild.parent || getOriginalNode(firstChild.parent) ***REMOVED******REMOVED******REMOVED*** getOriginalNode(parentNode))) {
        if (preserveSourceNewlines) {
          return getEffectiveLines(
            (includeComments) ***REMOVED***> getLinesBetweenPositionAndPrecedingNonWhitespaceCharacter(
              firstChild.pos,
              parentNode.pos,
              currentSourceFile,
              includeComments
            )
          );
        }
        return rangeStartPositionsAreOnSameLine(parentNode, firstChild, currentSourceFile) ? 0 : 1;
      }
      if (synthesizedNodeStartsOnNewLine(firstChild, format)) {
        return 1;
      }
    }
    return format & 1 /* MultiLine */ ? 1 : 0;
  }
  function getSeparatingLineTerminatorCount(previousNode, nextNode, format) {
    if (format & 2 /* PreserveLines */ || preserveSourceNewlines) {
      if (previousNode ***REMOVED******REMOVED******REMOVED*** void 0 || nextNode ***REMOVED******REMOVED******REMOVED*** void 0) {
        return 0;
      }
      if (nextNode.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */) {
        return 0;
      } else if (currentSourceFile && !nodeIsSynthesized(previousNode) && !nodeIsSynthesized(nextNode)) {
        if (preserveSourceNewlines && siblingNodePositionsAreComparable(previousNode, nextNode)) {
          return getEffectiveLines(
            (includeComments) ***REMOVED***> getLinesBetweenRangeEndAndRangeStart(
              previousNode,
              nextNode,
              currentSourceFile,
              includeComments
            )
          );
        } else if (!preserveSourceNewlines && originalNodesHaveSameParent(previousNode, nextNode)) {
          return rangeEndIsOnSameLineAsRangeStart(previousNode, nextNode, currentSourceFile) ? 0 : 1;
        }
        return format & 65536 /* PreferNewLine */ ? 1 : 0;
      } else if (synthesizedNodeStartsOnNewLine(previousNode, format) || synthesizedNodeStartsOnNewLine(nextNode, format)) {
        return 1;
      }
    } else if (getStartsOnNewLine(nextNode)) {
      return 1;
    }
    return format & 1 /* MultiLine */ ? 1 : 0;
  }
  function getClosingLineTerminatorCount(parentNode, lastChild, format, childrenTextRange) {
    if (format & 2 /* PreserveLines */ || preserveSourceNewlines) {
      if (format & 65536 /* PreferNewLine */) {
        return 1;
      }
      if (lastChild ***REMOVED******REMOVED******REMOVED*** void 0) {
        return !parentNode || currentSourceFile && rangeIsOnSingleLine(parentNode, currentSourceFile) ? 0 : 1;
      }
      if (currentSourceFile && parentNode && !positionIsSynthesized(parentNode.pos) && !nodeIsSynthesized(lastChild) && (!lastChild.parent || lastChild.parent ***REMOVED******REMOVED******REMOVED*** parentNode)) {
        if (preserveSourceNewlines) {
          const end ***REMOVED*** childrenTextRange && !positionIsSynthesized(childrenTextRange.end) ? childrenTextRange.end : lastChild.end;
          return getEffectiveLines(
            (includeComments) ***REMOVED***> getLinesBetweenPositionAndNextNonWhitespaceCharacter(
              end,
              parentNode.end,
              currentSourceFile,
              includeComments
            )
          );
        }
        return rangeEndPositionsAreOnSameLine(parentNode, lastChild, currentSourceFile) ? 0 : 1;
      }
      if (synthesizedNodeStartsOnNewLine(lastChild, format)) {
        return 1;
      }
    }
    if (format & 1 /* MultiLine */ && !(format & 131072 /* NoTrailingNewLine */)) {
      return 1;
    }
    return 0;
  }
  function getEffectiveLines(getLineDifference) {
    Debug.assert(!!preserveSourceNewlines);
    const lines ***REMOVED*** getLineDifference(
      /*includeComments*/
      true
    );
    if (lines ***REMOVED******REMOVED******REMOVED*** 0) {
      return getLineDifference(
        /*includeComments*/
        false
      );
    }
    return lines;
  }
  function writeLineSeparatorsAndIndentBefore(node, parent) {
    const leadingNewlines ***REMOVED*** preserveSourceNewlines && getLeadingLineTerminatorCount(parent, node, 0 /* None */);
    if (leadingNewlines) {
      writeLinesAndIndent(
        leadingNewlines,
        /*writeSpaceIfNotIndenting*/
        false
      );
    }
    return !!leadingNewlines;
  }
  function writeLineSeparatorsAfter(node, parent) {
    const trailingNewlines ***REMOVED*** preserveSourceNewlines && getClosingLineTerminatorCount(
      parent,
      node,
      0 /* None */,
      /*childrenTextRange*/
      void 0
    );
    if (trailingNewlines) {
      writeLine(trailingNewlines);
    }
  }
  function synthesizedNodeStartsOnNewLine(node, format) {
    if (nodeIsSynthesized(node)) {
      const startsOnNewLine ***REMOVED*** getStartsOnNewLine(node);
      if (startsOnNewLine ***REMOVED******REMOVED******REMOVED*** void 0) {
        return (format & 65536 /* PreferNewLine */) !***REMOVED******REMOVED*** 0;
      }
      return startsOnNewLine;
    }
    return (format & 65536 /* PreferNewLine */) !***REMOVED******REMOVED*** 0;
  }
  function getLinesBetweenNodes(parent, node1, node2) {
    if (getEmitFlags(parent) & 262144 /* NoIndentation */) {
      return 0;
    }
    parent ***REMOVED*** skipSynthesizedParentheses(parent);
    node1 ***REMOVED*** skipSynthesizedParentheses(node1);
    node2 ***REMOVED*** skipSynthesizedParentheses(node2);
    if (getStartsOnNewLine(node2)) {
      return 1;
    }
    if (currentSourceFile && !nodeIsSynthesized(parent) && !nodeIsSynthesized(node1) && !nodeIsSynthesized(node2)) {
      if (preserveSourceNewlines) {
        return getEffectiveLines(
          (includeComments) ***REMOVED***> getLinesBetweenRangeEndAndRangeStart(
            node1,
            node2,
            currentSourceFile,
            includeComments
          )
        );
      }
      return rangeEndIsOnSameLineAsRangeStart(node1, node2, currentSourceFile) ? 0 : 1;
    }
    return 0;
  }
  function isEmptyBlock(block) {
    return block.statements.length ***REMOVED******REMOVED******REMOVED*** 0 && (!currentSourceFile || rangeEndIsOnSameLineAsRangeStart(block, block, currentSourceFile));
  }
  function skipSynthesizedParentheses(node) {
    while (node.kind ***REMOVED******REMOVED******REMOVED*** 218 /* ParenthesizedExpression */ && nodeIsSynthesized(node)) {
      node ***REMOVED*** node.expression;
    }
    return node;
  }
  function getTextOfNode2(node, includeTrivia) {
    if (isGeneratedIdentifier(node) || isGeneratedPrivateIdentifier(node)) {
      return generateName(node);
    }
    if (isStringLiteral(node) && node.textSourceNode) {
      return getTextOfNode2(node.textSourceNode, includeTrivia);
    }
    const sourceFile ***REMOVED*** currentSourceFile;
    const canUseSourceFile ***REMOVED*** !!sourceFile && !!node.parent && !nodeIsSynthesized(node);
    if (isMemberName(node)) {
      if (!canUseSourceFile || getSourceFileOfNode(node) !***REMOVED******REMOVED*** getOriginalNode(sourceFile)) {
        return idText(node);
      }
    } else if (isJsxNamespacedName(node)) {
      if (!canUseSourceFile || getSourceFileOfNode(node) !***REMOVED******REMOVED*** getOriginalNode(sourceFile)) {
        return getTextOfJsxNamespacedName(node);
      }
    } else {
      Debug.assertNode(node, isLiteralExpression);
      if (!canUseSourceFile) {
        return node.text;
      }
    }
    return getSourceTextOfNodeFromSourceFile(sourceFile, node, includeTrivia);
  }
  function getLiteralTextOfNode(node, sourceFile ***REMOVED*** currentSourceFile, neverAsciiEscape, jsxAttributeEscape) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ && node.textSourceNode) {
      const textSourceNode ***REMOVED*** node.textSourceNode;
      if (isIdentifier(textSourceNode) || isPrivateIdentifier(textSourceNode) || isNumericLiteral(textSourceNode) || isJsxNamespacedName(textSourceNode)) {
        const text ***REMOVED*** isNumericLiteral(textSourceNode) ? textSourceNode.text : getTextOfNode2(textSourceNode);
        return jsxAttributeEscape ? `"${escapeJsxAttributeString(text)}"` : neverAsciiEscape || getEmitFlags(node) & 16777216 /* NoAsciiEscaping */ ? `"${escapeString(text)}"` : `"${escapeNonAsciiString(text)}"`;
      } else {
        return getLiteralTextOfNode(textSourceNode, getSourceFileOfNode(textSourceNode), neverAsciiEscape, jsxAttributeEscape);
      }
    }
    const flags ***REMOVED*** (neverAsciiEscape ? 1 /* NeverAsciiEscape */ : 0) | (jsxAttributeEscape ? 2 /* JsxAttributeEscape */ : 0) | (printerOptions.terminateUnterminatedLiterals ? 4 /* TerminateUnterminatedLiterals */ : 0) | (printerOptions.target && printerOptions.target >***REMOVED*** 8 /* ES2021 */ ? 8 /* AllowNumericSeparator */ : 0);
    return getLiteralText(node, sourceFile, flags);
  }
  function pushNameGenerationScope(node) {
    privateNameTempFlagsStack.push(privateNameTempFlags);
    privateNameTempFlags ***REMOVED*** 0 /* Auto */;
    reservedPrivateNamesStack.push(reservedPrivateNames);
    if (node && getEmitFlags(node) & 1048576 /* ReuseTempVariableScope */) {
      return;
    }
    tempFlagsStack.push(tempFlags);
    tempFlags ***REMOVED*** 0 /* Auto */;
    formattedNameTempFlagsStack.push(formattedNameTempFlags);
    formattedNameTempFlags ***REMOVED*** void 0;
    reservedNamesStack.push(reservedNames);
  }
  function popNameGenerationScope(node) {
    privateNameTempFlags ***REMOVED*** privateNameTempFlagsStack.pop();
    reservedPrivateNames ***REMOVED*** reservedPrivateNamesStack.pop();
    if (node && getEmitFlags(node) & 1048576 /* ReuseTempVariableScope */) {
      return;
    }
    tempFlags ***REMOVED*** tempFlagsStack.pop();
    formattedNameTempFlags ***REMOVED*** formattedNameTempFlagsStack.pop();
    reservedNames ***REMOVED*** reservedNamesStack.pop();
  }
  function reserveNameInNestedScopes(name) {
    if (!reservedNames || reservedNames ***REMOVED******REMOVED******REMOVED*** lastOrUndefined(reservedNamesStack)) {
      reservedNames ***REMOVED*** /* @__PURE__ */ new Set();
    }
    reservedNames.add(name);
  }
  function reservePrivateNameInNestedScopes(name) {
    if (!reservedPrivateNames || reservedPrivateNames ***REMOVED******REMOVED******REMOVED*** lastOrUndefined(reservedPrivateNamesStack)) {
      reservedPrivateNames ***REMOVED*** /* @__PURE__ */ new Set();
    }
    reservedPrivateNames.add(name);
  }
  function generateNames(node) {
    if (!node) return;
    switch (node.kind) {
      case 242 /* Block */:
        forEach(node.statements, generateNames);
        break;
      case 257 /* LabeledStatement */:
      case 255 /* WithStatement */:
      case 247 /* DoStatement */:
      case 248 /* WhileStatement */:
        generateNames(node.statement);
        break;
      case 246 /* IfStatement */:
        generateNames(node.thenStatement);
        generateNames(node.elseStatement);
        break;
      case 249 /* ForStatement */:
      case 251 /* ForOfStatement */:
      case 250 /* ForInStatement */:
        generateNames(node.initializer);
        generateNames(node.statement);
        break;
      case 256 /* SwitchStatement */:
        generateNames(node.caseBlock);
        break;
      case 270 /* CaseBlock */:
        forEach(node.clauses, generateNames);
        break;
      case 297 /* CaseClause */:
      case 298 /* DefaultClause */:
        forEach(node.statements, generateNames);
        break;
      case 259 /* TryStatement */:
        generateNames(node.tryBlock);
        generateNames(node.catchClause);
        generateNames(node.finallyBlock);
        break;
      case 300 /* CatchClause */:
        generateNames(node.variableDeclaration);
        generateNames(node.block);
        break;
      case 244 /* VariableStatement */:
        generateNames(node.declarationList);
        break;
      case 262 /* VariableDeclarationList */:
        forEach(node.declarations, generateNames);
        break;
      case 261 /* VariableDeclaration */:
      case 170 /* Parameter */:
      case 209 /* BindingElement */:
      case 264 /* ClassDeclaration */:
        generateNameIfNeeded(node.name);
        break;
      case 263 /* FunctionDeclaration */:
        generateNameIfNeeded(node.name);
        if (getEmitFlags(node) & 1048576 /* ReuseTempVariableScope */) {
          forEach(node.parameters, generateNames);
          generateNames(node.body);
        }
        break;
      case 207 /* ObjectBindingPattern */:
      case 208 /* ArrayBindingPattern */:
        forEach(node.elements, generateNames);
        break;
      case 273 /* ImportDeclaration */:
        generateNames(node.importClause);
        break;
      case 274 /* ImportClause */:
        generateNameIfNeeded(node.name);
        generateNames(node.namedBindings);
        break;
      case 275 /* NamespaceImport */:
        generateNameIfNeeded(node.name);
        break;
      case 281 /* NamespaceExport */:
        generateNameIfNeeded(node.name);
        break;
      case 276 /* NamedImports */:
        forEach(node.elements, generateNames);
        break;
      case 277 /* ImportSpecifier */:
        generateNameIfNeeded(node.propertyName || node.name);
        break;
    }
  }
  function generateMemberNames(node) {
    if (!node) return;
    switch (node.kind) {
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
      case 173 /* PropertyDeclaration */:
      case 172 /* PropertySignature */:
      case 175 /* MethodDeclaration */:
      case 174 /* MethodSignature */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        generateNameIfNeeded(node.name);
        break;
    }
  }
  function generateNameIfNeeded(name) {
    if (name) {
      if (isGeneratedIdentifier(name) || isGeneratedPrivateIdentifier(name)) {
        generateName(name);
      } else if (isBindingPattern(name)) {
        generateNames(name);
      }
    }
  }
  function generateName(name) {
    const autoGenerate ***REMOVED*** name.emitNode.autoGenerate;
    if ((autoGenerate.flags & 7 /* KindMask */) ***REMOVED******REMOVED******REMOVED*** 4 /* Node */) {
      return generateNameCached(getNodeForGeneratedName(name), isPrivateIdentifier(name), autoGenerate.flags, autoGenerate.prefix, autoGenerate.suffix);
    } else {
      const autoGenerateId ***REMOVED*** autoGenerate.id;
      return autoGeneratedIdToGeneratedName[autoGenerateId] || (autoGeneratedIdToGeneratedName[autoGenerateId] ***REMOVED*** makeName(name));
    }
  }
  function generateNameCached(node, privateName, flags, prefix, suffix) {
    const nodeId ***REMOVED*** getNodeId(node);
    const cache ***REMOVED*** privateName ? nodeIdToGeneratedPrivateName : nodeIdToGeneratedName;
    return cache[nodeId] || (cache[nodeId] ***REMOVED*** generateNameForNode(node, privateName, flags ?? 0 /* None */, formatGeneratedNamePart(prefix, generateName), formatGeneratedNamePart(suffix)));
  }
  function isUniqueName(name, privateName) {
    return isFileLevelUniqueNameInCurrentFile(name, privateName) && !isReservedName(name, privateName) && !generatedNames.has(name);
  }
  function isReservedName(name, privateName) {
    let set;
    let stack;
    if (privateName) {
      set ***REMOVED*** reservedPrivateNames;
      stack ***REMOVED*** reservedPrivateNamesStack;
    } else {
      set ***REMOVED*** reservedNames;
      stack ***REMOVED*** reservedNamesStack;
    }
    if (set ***REMOVED******REMOVED*** null ? void 0 : set.has(name)) {
      return true;
    }
    for (let i ***REMOVED*** stack.length - 1; i >***REMOVED*** 0; i--) {
      if (set ***REMOVED******REMOVED******REMOVED*** stack[i]) {
        continue;
      }
      set ***REMOVED*** stack[i];
      if (set ***REMOVED******REMOVED*** null ? void 0 : set.has(name)) {
        return true;
      }
    }
    return false;
  }
  function isFileLevelUniqueNameInCurrentFile(name, _isPrivate) {
    return currentSourceFile ? isFileLevelUniqueName(currentSourceFile, name, hasGlobalName) : true;
  }
  function isUniqueLocalName(name, container) {
    for (let node ***REMOVED*** container; node && isNodeDescendantOf(node, container); node ***REMOVED*** node.nextContainer) {
      if (canHaveLocals(node) && node.locals) {
        const local ***REMOVED*** node.locals.get(escapeLeadingUnderscores(name));
        if (local && local.flags & (111551 /* Value */ | 1048576 /* ExportValue */ | 2097152 /* Alias */)) {
          return false;
        }
      }
    }
    return true;
  }
  function getTempFlags(formattedNameKey) {
    switch (formattedNameKey) {
      case "":
        return tempFlags;
      case "#":
        return privateNameTempFlags;
      default:
        return (formattedNameTempFlags ***REMOVED******REMOVED*** null ? void 0 : formattedNameTempFlags.get(formattedNameKey)) ?? 0 /* Auto */;
    }
  }
  function setTempFlags(formattedNameKey, flags) {
    switch (formattedNameKey) {
      case "":
        tempFlags ***REMOVED*** flags;
        break;
      case "#":
        privateNameTempFlags ***REMOVED*** flags;
        break;
      default:
        formattedNameTempFlags ?? (formattedNameTempFlags ***REMOVED*** /* @__PURE__ */ new Map());
        formattedNameTempFlags.set(formattedNameKey, flags);
        break;
    }
  }
  function makeTempVariableName(flags, reservedInNestedScopes, privateName, prefix, suffix) {
    if (prefix.length > 0 && prefix.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 35 /* hash */) {
      prefix ***REMOVED*** prefix.slice(1);
    }
    const key ***REMOVED*** formatGeneratedName(privateName, prefix, "", suffix);
    let tempFlags2 ***REMOVED*** getTempFlags(key);
    if (flags && !(tempFlags2 & flags)) {
      const name ***REMOVED*** flags ***REMOVED******REMOVED******REMOVED*** 268435456 /* _i */ ? "_i" : "_n";
      const fullName ***REMOVED*** formatGeneratedName(privateName, prefix, name, suffix);
      if (isUniqueName(fullName, privateName)) {
        tempFlags2 |***REMOVED*** flags;
        if (privateName) {
          reservePrivateNameInNestedScopes(fullName);
        } else if (reservedInNestedScopes) {
          reserveNameInNestedScopes(fullName);
        }
        setTempFlags(key, tempFlags2);
        return fullName;
      }
    }
    while (true) {
      const count ***REMOVED*** tempFlags2 & 268435455 /* CountMask */;
      tempFlags2++;
      if (count !***REMOVED******REMOVED*** 8 && count !***REMOVED******REMOVED*** 13) {
        const name ***REMOVED*** count < 26 ? "_" + String.fromCharCode(97 /* a */ + count) : "_" + (count - 26);
        const fullName ***REMOVED*** formatGeneratedName(privateName, prefix, name, suffix);
        if (isUniqueName(fullName, privateName)) {
          if (privateName) {
            reservePrivateNameInNestedScopes(fullName);
          } else if (reservedInNestedScopes) {
            reserveNameInNestedScopes(fullName);
          }
          setTempFlags(key, tempFlags2);
          return fullName;
        }
      }
    }
  }
  function makeUniqueName(baseName, checkFn ***REMOVED*** isUniqueName, optimistic, scoped, privateName, prefix, suffix) {
    if (baseName.length > 0 && baseName.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 35 /* hash */) {
      baseName ***REMOVED*** baseName.slice(1);
    }
    if (prefix.length > 0 && prefix.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 35 /* hash */) {
      prefix ***REMOVED*** prefix.slice(1);
    }
    if (optimistic) {
      const fullName ***REMOVED*** formatGeneratedName(privateName, prefix, baseName, suffix);
      if (checkFn(fullName, privateName)) {
        if (privateName) {
          reservePrivateNameInNestedScopes(fullName);
        } else if (scoped) {
          reserveNameInNestedScopes(fullName);
        } else {
          generatedNames.add(fullName);
        }
        return fullName;
      }
    }
    if (baseName.charCodeAt(baseName.length - 1) !***REMOVED******REMOVED*** 95 /* _ */) {
      baseName +***REMOVED*** "_";
    }
    let i ***REMOVED*** 1;
    while (true) {
      const fullName ***REMOVED*** formatGeneratedName(privateName, prefix, baseName + i, suffix);
      if (checkFn(fullName, privateName)) {
        if (privateName) {
          reservePrivateNameInNestedScopes(fullName);
        } else if (scoped) {
          reserveNameInNestedScopes(fullName);
        } else {
          generatedNames.add(fullName);
        }
        return fullName;
      }
      i++;
    }
  }
  function makeFileLevelOptimisticUniqueName(name) {
    return makeUniqueName(
      name,
      isFileLevelUniqueNameInCurrentFile,
      /*optimistic*/
      true,
      /*scoped*/
      false,
      /*privateName*/
      false,
      /*prefix*/
      "",
      /*suffix*/
      ""
    );
  }
  function generateNameForModuleOrEnum(node) {
    const name ***REMOVED*** getTextOfNode2(node.name);
    return isUniqueLocalName(name, tryCast(node, canHaveLocals)) ? name : makeUniqueName(
      name,
      isUniqueName,
      /*optimistic*/
      false,
      /*scoped*/
      false,
      /*privateName*/
      false,
      /*prefix*/
      "",
      /*suffix*/
      ""
    );
  }
  function generateNameForImportOrExportDeclaration(node) {
    const expr ***REMOVED*** getExternalModuleName(node);
    const baseName ***REMOVED*** isStringLiteral(expr) ? makeIdentifierFromModuleName(expr.text) : "module";
    return makeUniqueName(
      baseName,
      isUniqueName,
      /*optimistic*/
      false,
      /*scoped*/
      false,
      /*privateName*/
      false,
      /*prefix*/
      "",
      /*suffix*/
      ""
    );
  }
  function generateNameForExportDefault() {
    return makeUniqueName(
      "default",
      isUniqueName,
      /*optimistic*/
      false,
      /*scoped*/
      false,
      /*privateName*/
      false,
      /*prefix*/
      "",
      /*suffix*/
      ""
    );
  }
  function generateNameForClassExpression() {
    return makeUniqueName(
      "class",
      isUniqueName,
      /*optimistic*/
      false,
      /*scoped*/
      false,
      /*privateName*/
      false,
      /*prefix*/
      "",
      /*suffix*/
      ""
    );
  }
  function generateNameForMethodOrAccessor(node, privateName, prefix, suffix) {
    if (isIdentifier(node.name)) {
      return generateNameCached(node.name, privateName);
    }
    return makeTempVariableName(
      0 /* Auto */,
      /*reservedInNestedScopes*/
      false,
      privateName,
      prefix,
      suffix
    );
  }
  function generateNameForNode(node, privateName, flags, prefix, suffix) {
    switch (node.kind) {
      case 80 /* Identifier */:
      case 81 /* PrivateIdentifier */:
        return makeUniqueName(
          getTextOfNode2(node),
          isUniqueName,
          !!(flags & 16 /* Optimistic */),
          !!(flags & 8 /* ReservedInNestedScopes */),
          privateName,
          prefix,
          suffix
        );
      case 268 /* ModuleDeclaration */:
      case 267 /* EnumDeclaration */:
        Debug.assert(!prefix && !suffix && !privateName);
        return generateNameForModuleOrEnum(node);
      case 273 /* ImportDeclaration */:
      case 279 /* ExportDeclaration */:
        Debug.assert(!prefix && !suffix && !privateName);
        return generateNameForImportOrExportDeclaration(node);
      case 263 /* FunctionDeclaration */:
      case 264 /* ClassDeclaration */: {
        Debug.assert(!prefix && !suffix && !privateName);
        const name ***REMOVED*** node.name;
        if (name && !isGeneratedIdentifier(name)) {
          return generateNameForNode(
            name,
            /*privateName*/
            false,
            flags,
            prefix,
            suffix
          );
        }
        return generateNameForExportDefault();
      }
      case 278 /* ExportAssignment */:
        Debug.assert(!prefix && !suffix && !privateName);
        return generateNameForExportDefault();
      case 232 /* ClassExpression */:
        Debug.assert(!prefix && !suffix && !privateName);
        return generateNameForClassExpression();
      case 175 /* MethodDeclaration */:
      case 178 /* GetAccessor */:
      case 179 /* SetAccessor */:
        return generateNameForMethodOrAccessor(node, privateName, prefix, suffix);
      case 168 /* ComputedPropertyName */:
        return makeTempVariableName(
          0 /* Auto */,
          /*reservedInNestedScopes*/
          true,
          privateName,
          prefix,
          suffix
        );
      default:
        return makeTempVariableName(
          0 /* Auto */,
          /*reservedInNestedScopes*/
          false,
          privateName,
          prefix,
          suffix
        );
    }
  }
  function makeName(name) {
    const autoGenerate ***REMOVED*** name.emitNode.autoGenerate;
    const prefix ***REMOVED*** formatGeneratedNamePart(autoGenerate.prefix, generateName);
    const suffix ***REMOVED*** formatGeneratedNamePart(autoGenerate.suffix);
    switch (autoGenerate.flags & 7 /* KindMask */) {
      case 1 /* Auto */:
        return makeTempVariableName(0 /* Auto */, !!(autoGenerate.flags & 8 /* ReservedInNestedScopes */), isPrivateIdentifier(name), prefix, suffix);
      case 2 /* Loop */:
        Debug.assertNode(name, isIdentifier);
        return makeTempVariableName(
          268435456 /* _i */,
          !!(autoGenerate.flags & 8 /* ReservedInNestedScopes */),
          /*privateName*/
          false,
          prefix,
          suffix
        );
      case 3 /* Unique */:
        return makeUniqueName(
          idText(name),
          autoGenerate.flags & 32 /* FileLevel */ ? isFileLevelUniqueNameInCurrentFile : isUniqueName,
          !!(autoGenerate.flags & 16 /* Optimistic */),
          !!(autoGenerate.flags & 8 /* ReservedInNestedScopes */),
          isPrivateIdentifier(name),
          prefix,
          suffix
        );
    }
    return Debug.fail(`Unsupported GeneratedIdentifierKind: ${Debug.formatEnum(
      autoGenerate.flags & 7 /* KindMask */,
      GeneratedIdentifierFlags,
      /*isFlags*/
      true
    )}.`);
  }
  function pipelineEmitWithComments(hint, node) {
    const pipelinePhase ***REMOVED*** getNextPipelinePhase(2 /* Comments */, hint, node);
    const savedContainerPos ***REMOVED*** containerPos;
    const savedContainerEnd ***REMOVED*** containerEnd;
    const savedDeclarationListContainerEnd ***REMOVED*** declarationListContainerEnd;
    emitCommentsBeforeNode(node);
    pipelinePhase(hint, node);
    emitCommentsAfterNode(node, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd);
  }
  function emitCommentsBeforeNode(node) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    const commentRange ***REMOVED*** getCommentRange(node);
    emitLeadingCommentsOfNode(node, emitFlags, commentRange.pos, commentRange.end);
    if (emitFlags & 4096 /* NoNestedComments */) {
      commentsDisabled ***REMOVED*** true;
    }
  }
  function emitCommentsAfterNode(node, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    const commentRange ***REMOVED*** getCommentRange(node);
    if (emitFlags & 4096 /* NoNestedComments */) {
      commentsDisabled ***REMOVED*** false;
    }
    emitTrailingCommentsOfNode(node, emitFlags, commentRange.pos, commentRange.end, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd);
    const typeNode ***REMOVED*** getTypeNode(node);
    if (typeNode) {
      emitTrailingCommentsOfNode(node, emitFlags, typeNode.pos, typeNode.end, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd);
    }
  }
  function emitLeadingCommentsOfNode(node, emitFlags, pos, end) {
    enterComment();
    hasWrittenComment ***REMOVED*** false;
    const skipLeadingComments ***REMOVED*** pos < 0 || (emitFlags & 1024 /* NoLeadingComments */) !***REMOVED******REMOVED*** 0 || node.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */;
    const skipTrailingComments ***REMOVED*** end < 0 || (emitFlags & 2048 /* NoTrailingComments */) !***REMOVED******REMOVED*** 0 || node.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */;
    if ((pos > 0 || end > 0) && pos !***REMOVED******REMOVED*** end) {
      if (!skipLeadingComments) {
        emitLeadingComments(
          pos,
          /*isEmittedNode*/
          node.kind !***REMOVED******REMOVED*** 354 /* NotEmittedStatement */
        );
      }
      if (!skipLeadingComments || pos >***REMOVED*** 0 && (emitFlags & 1024 /* NoLeadingComments */) !***REMOVED******REMOVED*** 0) {
        containerPos ***REMOVED*** pos;
      }
      if (!skipTrailingComments || end >***REMOVED*** 0 && (emitFlags & 2048 /* NoTrailingComments */) !***REMOVED******REMOVED*** 0) {
        containerEnd ***REMOVED*** end;
        if (node.kind ***REMOVED******REMOVED******REMOVED*** 262 /* VariableDeclarationList */) {
          declarationListContainerEnd ***REMOVED*** end;
        }
      }
    }
    forEach(getSyntheticLeadingComments(node), emitLeadingSynthesizedComment);
    exitComment();
  }
  function emitTrailingCommentsOfNode(node, emitFlags, pos, end, savedContainerPos, savedContainerEnd, savedDeclarationListContainerEnd) {
    enterComment();
    const skipTrailingComments ***REMOVED*** end < 0 || (emitFlags & 2048 /* NoTrailingComments */) !***REMOVED******REMOVED*** 0 || node.kind ***REMOVED******REMOVED******REMOVED*** 12 /* JsxText */;
    forEach(getSyntheticTrailingComments(node), emitTrailingSynthesizedComment);
    if ((pos > 0 || end > 0) && pos !***REMOVED******REMOVED*** end) {
      containerPos ***REMOVED*** savedContainerPos;
      containerEnd ***REMOVED*** savedContainerEnd;
      declarationListContainerEnd ***REMOVED*** savedDeclarationListContainerEnd;
      if (!skipTrailingComments && node.kind !***REMOVED******REMOVED*** 354 /* NotEmittedStatement */) {
        emitTrailingComments(end);
      }
    }
    exitComment();
  }
  function emitLeadingSynthesizedComment(comment) {
    if (comment.hasLeadingNewline || comment.kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */) {
      writer.writeLine();
    }
    writeSynthesizedComment(comment);
    if (comment.hasTrailingNewLine || comment.kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */) {
      writer.writeLine();
    } else {
      writer.writeSpace(" ");
    }
  }
  function emitTrailingSynthesizedComment(comment) {
    if (!writer.isAtStartOfLine()) {
      writer.writeSpace(" ");
    }
    writeSynthesizedComment(comment);
    if (comment.hasTrailingNewLine) {
      writer.writeLine();
    }
  }
  function writeSynthesizedComment(comment) {
    const text ***REMOVED*** formatSynthesizedComment(comment);
    const lineMap ***REMOVED*** comment.kind ***REMOVED******REMOVED******REMOVED*** 3 /* MultiLineCommentTrivia */ ? computeLineStarts(text) : void 0;
    writeCommentRange(text, lineMap, writer, 0, text.length, newLine);
  }
  function formatSynthesizedComment(comment) {
    return comment.kind ***REMOVED******REMOVED******REMOVED*** 3 /* MultiLineCommentTrivia */ ? `/*${comment.text}*/` : `//${comment.text}`;
  }
  function emitBodyWithDetachedComments(node, detachedRange, emitCallback) {
    enterComment();
    const { pos, end } ***REMOVED*** detachedRange;
    const emitFlags ***REMOVED*** getEmitFlags(node);
    const skipLeadingComments ***REMOVED*** pos < 0 || (emitFlags & 1024 /* NoLeadingComments */) !***REMOVED******REMOVED*** 0;
    const skipTrailingComments ***REMOVED*** commentsDisabled || end < 0 || (emitFlags & 2048 /* NoTrailingComments */) !***REMOVED******REMOVED*** 0;
    if (!skipLeadingComments) {
      emitDetachedCommentsAndUpdateCommentsInfo(detachedRange);
    }
    exitComment();
    if (emitFlags & 4096 /* NoNestedComments */ && !commentsDisabled) {
      commentsDisabled ***REMOVED*** true;
      emitCallback(node);
      commentsDisabled ***REMOVED*** false;
    } else {
      emitCallback(node);
    }
    enterComment();
    if (!skipTrailingComments) {
      emitLeadingComments(
        detachedRange.end,
        /*isEmittedNode*/
        true
      );
      if (hasWrittenComment && !writer.isAtStartOfLine()) {
        writer.writeLine();
      }
    }
    exitComment();
  }
  function originalNodesHaveSameParent(nodeA, nodeB) {
    nodeA ***REMOVED*** getOriginalNode(nodeA);
    return nodeA.parent && nodeA.parent ***REMOVED******REMOVED******REMOVED*** getOriginalNode(nodeB).parent;
  }
  function siblingNodePositionsAreComparable(previousNode, nextNode) {
    if (nextNode.pos < previousNode.end) {
      return false;
    }
    previousNode ***REMOVED*** getOriginalNode(previousNode);
    nextNode ***REMOVED*** getOriginalNode(nextNode);
    const parent ***REMOVED*** previousNode.parent;
    if (!parent || parent !***REMOVED******REMOVED*** nextNode.parent) {
      return false;
    }
    const parentNodeArray ***REMOVED*** getContainingNodeArray(previousNode);
    const prevNodeIndex ***REMOVED*** parentNodeArray ***REMOVED******REMOVED*** null ? void 0 : parentNodeArray.indexOf(previousNode);
    return prevNodeIndex !***REMOVED******REMOVED*** void 0 && prevNodeIndex > -1 && parentNodeArray.indexOf(nextNode) ***REMOVED******REMOVED******REMOVED*** prevNodeIndex + 1;
  }
  function emitLeadingComments(pos, isEmittedNode) {
    hasWrittenComment ***REMOVED*** false;
    if (isEmittedNode) {
      if (pos ***REMOVED******REMOVED******REMOVED*** 0 && (currentSourceFile ***REMOVED******REMOVED*** null ? void 0 : currentSourceFile.isDeclarationFile)) {
        forEachLeadingCommentToEmit(pos, emitNonTripleSlashLeadingComment);
      } else {
        forEachLeadingCommentToEmit(pos, emitLeadingComment);
      }
    } else if (pos ***REMOVED******REMOVED******REMOVED*** 0) {
      forEachLeadingCommentToEmit(pos, emitTripleSlashLeadingComment);
    }
  }
  function emitTripleSlashLeadingComment(commentPos, commentEnd, kind, hasTrailingNewLine, rangePos) {
    if (isTripleSlashComment(commentPos, commentEnd)) {
      emitLeadingComment(commentPos, commentEnd, kind, hasTrailingNewLine, rangePos);
    }
  }
  function emitNonTripleSlashLeadingComment(commentPos, commentEnd, kind, hasTrailingNewLine, rangePos) {
    if (!isTripleSlashComment(commentPos, commentEnd)) {
      emitLeadingComment(commentPos, commentEnd, kind, hasTrailingNewLine, rangePos);
    }
  }
  function shouldWriteComment(text, pos) {
    if (printerOptions.onlyPrintJsDocStyle) {
      return isJSDocLikeText(text, pos) || isPinnedComment(text, pos);
    }
    return true;
  }
  function emitLeadingComment(commentPos, commentEnd, kind, hasTrailingNewLine, rangePos) {
    if (!currentSourceFile || !shouldWriteComment(currentSourceFile.text, commentPos)) return;
    if (!hasWrittenComment) {
      emitNewLineBeforeLeadingCommentOfPosition(getCurrentLineMap(), writer, rangePos, commentPos);
      hasWrittenComment ***REMOVED*** true;
    }
    emitPos(commentPos);
    writeCommentRange(currentSourceFile.text, getCurrentLineMap(), writer, commentPos, commentEnd, newLine);
    emitPos(commentEnd);
    if (hasTrailingNewLine) {
      writer.writeLine();
    } else if (kind ***REMOVED******REMOVED******REMOVED*** 3 /* MultiLineCommentTrivia */) {
      writer.writeSpace(" ");
    }
  }
  function emitLeadingCommentsOfPosition(pos) {
    if (commentsDisabled || pos ***REMOVED******REMOVED******REMOVED*** -1) {
      return;
    }
    emitLeadingComments(
      pos,
      /*isEmittedNode*/
      true
    );
  }
  function emitTrailingComments(pos) {
    forEachTrailingCommentToEmit(pos, emitTrailingComment);
  }
  function emitTrailingComment(commentPos, commentEnd, _kind, hasTrailingNewLine) {
    if (!currentSourceFile || !shouldWriteComment(currentSourceFile.text, commentPos)) return;
    if (!writer.isAtStartOfLine()) {
      writer.writeSpace(" ");
    }
    emitPos(commentPos);
    writeCommentRange(currentSourceFile.text, getCurrentLineMap(), writer, commentPos, commentEnd, newLine);
    emitPos(commentEnd);
    if (hasTrailingNewLine) {
      writer.writeLine();
    }
  }
  function emitTrailingCommentsOfPosition(pos, prefixSpace, forceNoNewline) {
    if (commentsDisabled) {
      return;
    }
    enterComment();
    forEachTrailingCommentToEmit(pos, prefixSpace ? emitTrailingComment : forceNoNewline ? emitTrailingCommentOfPositionNoNewline : emitTrailingCommentOfPosition);
    exitComment();
  }
  function emitTrailingCommentOfPositionNoNewline(commentPos, commentEnd, kind) {
    if (!currentSourceFile) return;
    emitPos(commentPos);
    writeCommentRange(currentSourceFile.text, getCurrentLineMap(), writer, commentPos, commentEnd, newLine);
    emitPos(commentEnd);
    if (kind ***REMOVED******REMOVED******REMOVED*** 2 /* SingleLineCommentTrivia */) {
      writer.writeLine();
    }
  }
  function emitTrailingCommentOfPosition(commentPos, commentEnd, _kind, hasTrailingNewLine) {
    if (!currentSourceFile) return;
    emitPos(commentPos);
    writeCommentRange(currentSourceFile.text, getCurrentLineMap(), writer, commentPos, commentEnd, newLine);
    emitPos(commentEnd);
    if (hasTrailingNewLine) {
      writer.writeLine();
    } else {
      writer.writeSpace(" ");
    }
  }
  function forEachLeadingCommentToEmit(pos, cb) {
    if (currentSourceFile && (containerPos ***REMOVED******REMOVED******REMOVED*** -1 || pos !***REMOVED******REMOVED*** containerPos)) {
      if (hasDetachedComments(pos)) {
        forEachLeadingCommentWithoutDetachedComments(cb);
      } else {
        forEachLeadingCommentRange(
          currentSourceFile.text,
          pos,
          cb,
          /*state*/
          pos
        );
      }
    }
  }
  function forEachTrailingCommentToEmit(end, cb) {
    if (currentSourceFile && (containerEnd ***REMOVED******REMOVED******REMOVED*** -1 || end !***REMOVED******REMOVED*** containerEnd && end !***REMOVED******REMOVED*** declarationListContainerEnd)) {
      forEachTrailingCommentRange(currentSourceFile.text, end, cb);
    }
  }
  function hasDetachedComments(pos) {
    return detachedCommentsInfo !***REMOVED******REMOVED*** void 0 && last(detachedCommentsInfo).nodePos ***REMOVED******REMOVED******REMOVED*** pos;
  }
  function forEachLeadingCommentWithoutDetachedComments(cb) {
    if (!currentSourceFile) return;
    const pos ***REMOVED*** last(detachedCommentsInfo).detachedCommentEndPos;
    if (detachedCommentsInfo.length - 1) {
      detachedCommentsInfo.pop();
    } else {
      detachedCommentsInfo ***REMOVED*** void 0;
    }
    forEachLeadingCommentRange(
      currentSourceFile.text,
      pos,
      cb,
      /*state*/
      pos
    );
  }
  function emitDetachedCommentsAndUpdateCommentsInfo(range) {
    const currentDetachedCommentInfo ***REMOVED*** currentSourceFile && emitDetachedComments(currentSourceFile.text, getCurrentLineMap(), writer, emitComment, range, newLine, commentsDisabled);
    if (currentDetachedCommentInfo) {
      if (detachedCommentsInfo) {
        detachedCommentsInfo.push(currentDetachedCommentInfo);
      } else {
        detachedCommentsInfo ***REMOVED*** [currentDetachedCommentInfo];
      }
    }
  }
  function emitComment(text, lineMap, writer2, commentPos, commentEnd, newLine2) {
    if (!currentSourceFile || !shouldWriteComment(currentSourceFile.text, commentPos)) return;
    emitPos(commentPos);
    writeCommentRange(text, lineMap, writer2, commentPos, commentEnd, newLine2);
    emitPos(commentEnd);
  }
  function isTripleSlashComment(commentPos, commentEnd) {
    return !!currentSourceFile && isRecognizedTripleSlashComment(currentSourceFile.text, commentPos, commentEnd);
  }
  function pipelineEmitWithSourceMaps(hint, node) {
    const pipelinePhase ***REMOVED*** getNextPipelinePhase(3 /* SourceMaps */, hint, node);
    emitSourceMapsBeforeNode(node);
    pipelinePhase(hint, node);
    emitSourceMapsAfterNode(node);
  }
  function emitSourceMapsBeforeNode(node) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    const sourceMapRange ***REMOVED*** getSourceMapRange(node);
    const source ***REMOVED*** sourceMapRange.source || sourceMapSource;
    if (node.kind !***REMOVED******REMOVED*** 354 /* NotEmittedStatement */ && (emitFlags & 32 /* NoLeadingSourceMap */) ***REMOVED******REMOVED******REMOVED*** 0 && sourceMapRange.pos >***REMOVED*** 0) {
      emitSourcePos(sourceMapRange.source || sourceMapSource, skipSourceTrivia(source, sourceMapRange.pos));
    }
    if (emitFlags & 128 /* NoNestedSourceMaps */) {
      sourceMapsDisabled ***REMOVED*** true;
    }
  }
  function emitSourceMapsAfterNode(node) {
    const emitFlags ***REMOVED*** getEmitFlags(node);
    const sourceMapRange ***REMOVED*** getSourceMapRange(node);
    if (emitFlags & 128 /* NoNestedSourceMaps */) {
      sourceMapsDisabled ***REMOVED*** false;
    }
    if (node.kind !***REMOVED******REMOVED*** 354 /* NotEmittedStatement */ && (emitFlags & 64 /* NoTrailingSourceMap */) ***REMOVED******REMOVED******REMOVED*** 0 && sourceMapRange.end >***REMOVED*** 0) {
      emitSourcePos(sourceMapRange.source || sourceMapSource, sourceMapRange.end);
    }
  }
  function skipSourceTrivia(source, pos) {
    return source.skipTrivia ? source.skipTrivia(pos) : skipTrivia(source.text, pos);
  }
  function emitPos(pos) {
    if (sourceMapsDisabled || positionIsSynthesized(pos) || isJsonSourceMapSource(sourceMapSource)) {
      return;
    }
    const { line: sourceLine, character: sourceCharacter } ***REMOVED*** getLineAndCharacterOfPosition(sourceMapSource, pos);
    sourceMapGenerator.addMapping(
      writer.getLine(),
      writer.getColumn(),
      sourceMapSourceIndex,
      sourceLine,
      sourceCharacter,
      /*nameIndex*/
      void 0
    );
  }
  function emitSourcePos(source, pos) {
    if (source !***REMOVED******REMOVED*** sourceMapSource) {
      const savedSourceMapSource ***REMOVED*** sourceMapSource;
      const savedSourceMapSourceIndex ***REMOVED*** sourceMapSourceIndex;
      setSourceMapSource(source);
      emitPos(pos);
      resetSourceMapSource(savedSourceMapSource, savedSourceMapSourceIndex);
    } else {
      emitPos(pos);
    }
  }
  function emitTokenWithSourceMap(node, token, writer2, tokenPos, emitCallback) {
    if (sourceMapsDisabled || node && isInJsonFile(node)) {
      return emitCallback(token, writer2, tokenPos);
    }
    const emitNode ***REMOVED*** node && node.emitNode;
    const emitFlags ***REMOVED*** emitNode && emitNode.flags || 0 /* None */;
    const range ***REMOVED*** emitNode && emitNode.tokenSourceMapRanges && emitNode.tokenSourceMapRanges[token];
    const source ***REMOVED*** range && range.source || sourceMapSource;
    tokenPos ***REMOVED*** skipSourceTrivia(source, range ? range.pos : tokenPos);
    if ((emitFlags & 256 /* NoTokenLeadingSourceMaps */) ***REMOVED******REMOVED******REMOVED*** 0 && tokenPos >***REMOVED*** 0) {
      emitSourcePos(source, tokenPos);
    }
    tokenPos ***REMOVED*** emitCallback(token, writer2, tokenPos);
    if (range) tokenPos ***REMOVED*** range.end;
    if ((emitFlags & 512 /* NoTokenTrailingSourceMaps */) ***REMOVED******REMOVED******REMOVED*** 0 && tokenPos >***REMOVED*** 0) {
      emitSourcePos(source, tokenPos);
    }
    return tokenPos;
  }
  function setSourceMapSource(source) {
    if (sourceMapsDisabled) {
      return;
    }
    sourceMapSource ***REMOVED*** source;
    if (source ***REMOVED******REMOVED******REMOVED*** mostRecentlyAddedSourceMapSource) {
      sourceMapSourceIndex ***REMOVED*** mostRecentlyAddedSourceMapSourceIndex;
      return;
    }
    if (isJsonSourceMapSource(source)) {
      return;
    }
    sourceMapSourceIndex ***REMOVED*** sourceMapGenerator.addSource(source.fileName);
    if (printerOptions.inlineSources) {
      sourceMapGenerator.setSourceContent(sourceMapSourceIndex, source.text);
    }
    mostRecentlyAddedSourceMapSource ***REMOVED*** source;
    mostRecentlyAddedSourceMapSourceIndex ***REMOVED*** sourceMapSourceIndex;
  }
  function resetSourceMapSource(source, sourceIndex) {
    sourceMapSource ***REMOVED*** source;
    sourceMapSourceIndex ***REMOVED*** sourceIndex;
  }
  function isJsonSourceMapSource(sourceFile) {
    return fileExtensionIs(sourceFile.fileName, ".json" /* Json */);
  }
}
function createBracketsMap() {
  const brackets2 ***REMOVED*** [];
  brackets2[1024 /* Braces */] ***REMOVED*** ["{", "}"];
  brackets2[2048 /* Parenthesis */] ***REMOVED*** ["(", ")"];
  brackets2[4096 /* AngleBrackets */] ***REMOVED*** ["<", ">"];
  brackets2[8192 /* SquareBrackets */] ***REMOVED*** ["[", "]"];
  return brackets2;
}
function getOpeningBracket(format) {
  return brackets[format & 15360 /* BracketsMask */][0];
}
function getClosingBracket(format) {
  return brackets[format & 15360 /* BracketsMask */][1];
}
function emitListItemNoParenthesizer(node, emit, _parenthesizerRule, _index) {
  emit(node);
}
function emitListItemWithParenthesizerRuleSelector(node, emit, parenthesizerRuleSelector, index) {
  emit(node, parenthesizerRuleSelector.select(index));
}
function emitListItemWithParenthesizerRule(node, emit, parenthesizerRule, _index) {
  emit(node, parenthesizerRule);
}
function getEmitListItem(emit, parenthesizerRule) {
  return emit.length ***REMOVED******REMOVED******REMOVED*** 1 ? emitListItemNoParenthesizer : typeof parenthesizerRule ***REMOVED******REMOVED******REMOVED*** "object" ? emitListItemWithParenthesizerRuleSelector : emitListItemWithParenthesizerRule;
}

// src/compiler/watchUtilities.ts
function createCachedDirectoryStructureHost(host, currentDirectory, useCaseSensitiveFileNames2) {
  if (!host.getDirectories || !host.readDirectory) {
    return void 0;
  }
  const cachedReadDirectoryResult ***REMOVED*** /* @__PURE__ */ new Map();
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  return {
    useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
    fileExists,
    readFile: (path, encoding) ***REMOVED***> host.readFile(path, encoding),
    directoryExists: host.directoryExists && directoryExists,
    getDirectories,
    readDirectory,
    createDirectory: host.createDirectory && createDirectory,
    writeFile: host.writeFile && writeFile2,
    addOrDeleteFileOrDirectory,
    addOrDeleteFile,
    clearCache,
    realpath: host.realpath && realpath
  };
  function toPath3(fileName) {
    return toPath(fileName, currentDirectory, getCanonicalFileName);
  }
  function getCachedFileSystemEntries(rootDirPath) {
    return cachedReadDirectoryResult.get(ensureTrailingDirectorySeparator(rootDirPath));
  }
  function getCachedFileSystemEntriesForBaseDir(path) {
    const entries ***REMOVED*** getCachedFileSystemEntries(getDirectoryPath(path));
    if (!entries) {
      return entries;
    }
    if (!entries.sortedAndCanonicalizedFiles) {
      entries.sortedAndCanonicalizedFiles ***REMOVED*** entries.files.map(getCanonicalFileName).sort();
      entries.sortedAndCanonicalizedDirectories ***REMOVED*** entries.directories.map(getCanonicalFileName).sort();
    }
    return entries;
  }
  function getBaseNameOfFileName(fileName) {
    return getBaseFileName(normalizePath(fileName));
  }
  function createCachedFileSystemEntries(rootDir, rootDirPath) {
    var _a;
    if (!host.realpath || ensureTrailingDirectorySeparator(toPath3(host.realpath(rootDir))) ***REMOVED******REMOVED******REMOVED*** rootDirPath) {
      const resultFromHost ***REMOVED*** {
        files: map(host.readDirectory(
          rootDir,
          /*extensions*/
          void 0,
          /*exclude*/
          void 0,
          /*include*/
          ["*.*"]
        ), getBaseNameOfFileName) || [],
        directories: host.getDirectories(rootDir) || []
      };
      cachedReadDirectoryResult.set(ensureTrailingDirectorySeparator(rootDirPath), resultFromHost);
      return resultFromHost;
    }
    if ((_a ***REMOVED*** host.directoryExists) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host, rootDir)) {
      cachedReadDirectoryResult.set(rootDirPath, false);
      return false;
    }
    return void 0;
  }
  function tryReadDirectory(rootDir, rootDirPath) {
    rootDirPath ***REMOVED*** ensureTrailingDirectorySeparator(rootDirPath);
    const cachedResult ***REMOVED*** getCachedFileSystemEntries(rootDirPath);
    if (cachedResult) {
      return cachedResult;
    }
    try {
      return createCachedFileSystemEntries(rootDir, rootDirPath);
    } catch {
      Debug.assert(!cachedReadDirectoryResult.has(ensureTrailingDirectorySeparator(rootDirPath)));
      return void 0;
    }
  }
  function hasEntry(entries, name) {
    const index ***REMOVED*** binarySearch(entries, name, identity, compareStringsCaseSensitive);
    return index >***REMOVED*** 0;
  }
  function writeFile2(fileName, data, writeByteOrderMark) {
    const path ***REMOVED*** toPath3(fileName);
    const result ***REMOVED*** getCachedFileSystemEntriesForBaseDir(path);
    if (result) {
      updateFilesOfFileSystemEntry(
        result,
        getBaseNameOfFileName(fileName),
        /*fileExists*/
        true
      );
    }
    return host.writeFile(fileName, data, writeByteOrderMark);
  }
  function fileExists(fileName) {
    const path ***REMOVED*** toPath3(fileName);
    const result ***REMOVED*** getCachedFileSystemEntriesForBaseDir(path);
    return result && hasEntry(result.sortedAndCanonicalizedFiles, getCanonicalFileName(getBaseNameOfFileName(fileName))) || host.fileExists(fileName);
  }
  function directoryExists(dirPath) {
    const path ***REMOVED*** toPath3(dirPath);
    return cachedReadDirectoryResult.has(ensureTrailingDirectorySeparator(path)) || host.directoryExists(dirPath);
  }
  function createDirectory(dirPath) {
    const path ***REMOVED*** toPath3(dirPath);
    const result ***REMOVED*** getCachedFileSystemEntriesForBaseDir(path);
    if (result) {
      const baseName ***REMOVED*** getBaseNameOfFileName(dirPath);
      const canonicalizedBaseName ***REMOVED*** getCanonicalFileName(baseName);
      const canonicalizedDirectories ***REMOVED*** result.sortedAndCanonicalizedDirectories;
      if (insertSorted(canonicalizedDirectories, canonicalizedBaseName, compareStringsCaseSensitive)) {
        result.directories.push(baseName);
      }
    }
    host.createDirectory(dirPath);
  }
  function getDirectories(rootDir) {
    const rootDirPath ***REMOVED*** toPath3(rootDir);
    const result ***REMOVED*** tryReadDirectory(rootDir, rootDirPath);
    if (result) {
      return result.directories.slice();
    }
    return host.getDirectories(rootDir);
  }
  function readDirectory(rootDir, extensions, excludes, includes, depth) {
    const rootDirPath ***REMOVED*** toPath3(rootDir);
    const rootResult ***REMOVED*** tryReadDirectory(rootDir, rootDirPath);
    let rootSymLinkResult;
    if (rootResult !***REMOVED******REMOVED*** void 0) {
      return matchFiles(rootDir, extensions, excludes, includes, useCaseSensitiveFileNames2, currentDirectory, depth, getFileSystemEntries, realpath);
    }
    return host.readDirectory(rootDir, extensions, excludes, includes, depth);
    function getFileSystemEntries(dir) {
      const path ***REMOVED*** toPath3(dir);
      if (path ***REMOVED******REMOVED******REMOVED*** rootDirPath) {
        return rootResult || getFileSystemEntriesFromHost(dir, path);
      }
      const result ***REMOVED*** tryReadDirectory(dir, path);
      return result !***REMOVED******REMOVED*** void 0 ? result || getFileSystemEntriesFromHost(dir, path) : emptyFileSystemEntries;
    }
    function getFileSystemEntriesFromHost(dir, path) {
      if (rootSymLinkResult && path ***REMOVED******REMOVED******REMOVED*** rootDirPath) return rootSymLinkResult;
      const result ***REMOVED*** {
        files: map(host.readDirectory(
          dir,
          /*extensions*/
          void 0,
          /*exclude*/
          void 0,
          /*include*/
          ["*.*"]
        ), getBaseNameOfFileName) || emptyArray,
        directories: host.getDirectories(dir) || emptyArray
      };
      if (path ***REMOVED******REMOVED******REMOVED*** rootDirPath) rootSymLinkResult ***REMOVED*** result;
      return result;
    }
  }
  function realpath(s) {
    return host.realpath ? host.realpath(s) : s;
  }
  function clearFirstAncestorEntry(fileOrDirectoryPath) {
    forEachAncestorDirectory(
      getDirectoryPath(fileOrDirectoryPath),
      (ancestor) ***REMOVED***> cachedReadDirectoryResult.delete(ensureTrailingDirectorySeparator(ancestor)) ? true : void 0
    );
  }
  function addOrDeleteFileOrDirectory(fileOrDirectory, fileOrDirectoryPath) {
    const existingResult ***REMOVED*** getCachedFileSystemEntries(fileOrDirectoryPath);
    if (existingResult !***REMOVED******REMOVED*** void 0) {
      clearCache();
      return void 0;
    }
    const parentResult ***REMOVED*** getCachedFileSystemEntriesForBaseDir(fileOrDirectoryPath);
    if (!parentResult) {
      clearFirstAncestorEntry(fileOrDirectoryPath);
      return void 0;
    }
    if (!host.directoryExists) {
      clearCache();
      return void 0;
    }
    const baseName ***REMOVED*** getBaseNameOfFileName(fileOrDirectory);
    const fsQueryResult ***REMOVED*** {
      fileExists: host.fileExists(fileOrDirectory),
      directoryExists: host.directoryExists(fileOrDirectory)
    };
    if (fsQueryResult.directoryExists || hasEntry(parentResult.sortedAndCanonicalizedDirectories, getCanonicalFileName(baseName))) {
      clearCache();
    } else {
      updateFilesOfFileSystemEntry(parentResult, baseName, fsQueryResult.fileExists);
    }
    return fsQueryResult;
  }
  function addOrDeleteFile(fileName, filePath, eventKind) {
    if (eventKind ***REMOVED******REMOVED******REMOVED*** 1 /* Changed */) {
      return;
    }
    const parentResult ***REMOVED*** getCachedFileSystemEntriesForBaseDir(filePath);
    if (parentResult) {
      updateFilesOfFileSystemEntry(parentResult, getBaseNameOfFileName(fileName), eventKind ***REMOVED******REMOVED******REMOVED*** 0 /* Created */);
    } else {
      clearFirstAncestorEntry(filePath);
    }
  }
  function updateFilesOfFileSystemEntry(parentResult, baseName, fileExists2) {
    const canonicalizedFiles ***REMOVED*** parentResult.sortedAndCanonicalizedFiles;
    const canonicalizedBaseName ***REMOVED*** getCanonicalFileName(baseName);
    if (fileExists2) {
      if (insertSorted(canonicalizedFiles, canonicalizedBaseName, compareStringsCaseSensitive)) {
        parentResult.files.push(baseName);
      }
    } else {
      const sortedIndex ***REMOVED*** binarySearch(canonicalizedFiles, canonicalizedBaseName, identity, compareStringsCaseSensitive);
      if (sortedIndex >***REMOVED*** 0) {
        canonicalizedFiles.splice(sortedIndex, 1);
        const unsortedIndex ***REMOVED*** parentResult.files.findIndex((entry) ***REMOVED***> getCanonicalFileName(entry) ***REMOVED******REMOVED******REMOVED*** canonicalizedBaseName);
        parentResult.files.splice(unsortedIndex, 1);
      }
    }
  }
  function clearCache() {
    cachedReadDirectoryResult.clear();
  }
}
function updateSharedExtendedConfigFileWatcher(projectPath, options, extendedConfigFilesMap, createExtendedConfigFileWatch, toPath3) {
  var _a;
  const extendedConfigs ***REMOVED*** arrayToMap(((_a ***REMOVED*** options ***REMOVED******REMOVED*** null ? void 0 : options.configFile) ***REMOVED******REMOVED*** null ? void 0 : _a.extendedSourceFiles) || emptyArray, toPath3);
  extendedConfigFilesMap.forEach((watcher, extendedConfigFilePath) ***REMOVED***> {
    if (!extendedConfigs.has(extendedConfigFilePath)) {
      watcher.projects.delete(projectPath);
      watcher.close();
    }
  });
  extendedConfigs.forEach((extendedConfigFileName, extendedConfigFilePath) ***REMOVED***> {
    const existing ***REMOVED*** extendedConfigFilesMap.get(extendedConfigFilePath);
    if (existing) {
      existing.projects.add(projectPath);
    } else {
      extendedConfigFilesMap.set(extendedConfigFilePath, {
        projects: /* @__PURE__ */ new Set([projectPath]),
        watcher: createExtendedConfigFileWatch(extendedConfigFileName, extendedConfigFilePath),
        close: () ***REMOVED***> {
          const existing2 ***REMOVED*** extendedConfigFilesMap.get(extendedConfigFilePath);
          if (!existing2 || existing2.projects.size !***REMOVED******REMOVED*** 0) return;
          existing2.watcher.close();
          extendedConfigFilesMap.delete(extendedConfigFilePath);
        }
      });
    }
  });
}
function clearSharedExtendedConfigFileWatcher(projectPath, extendedConfigFilesMap) {
  extendedConfigFilesMap.forEach((watcher) ***REMOVED***> {
    if (watcher.projects.delete(projectPath)) watcher.close();
  });
}
function cleanExtendedConfigCache(extendedConfigCache, extendedConfigFilePath, toPath3) {
  if (!extendedConfigCache.delete(extendedConfigFilePath)) return;
  extendedConfigCache.forEach(({ extendedResult }, key) ***REMOVED***> {
    var _a;
    if ((_a ***REMOVED*** extendedResult.extendedSourceFiles) ***REMOVED******REMOVED*** null ? void 0 : _a.some((extendedFile) ***REMOVED***> toPath3(extendedFile) ***REMOVED******REMOVED******REMOVED*** extendedConfigFilePath)) {
      cleanExtendedConfigCache(extendedConfigCache, key, toPath3);
    }
  });
}
function updateMissingFilePathsWatch(program, missingFileWatches, createMissingFileWatch) {
  mutateMap(
    missingFileWatches,
    program.getMissingFilePaths(),
    {
      // Watch the missing files
      createNewValue: createMissingFileWatch,
      // Files that are no longer missing (e.g. because they are no longer required)
      // should no longer be watched.
      onDeleteValue: closeFileWatcher
    }
  );
}
function updateWatchingWildcardDirectories(existingWatchedForWildcards, wildcardDirectories, watchDirectory) {
  if (wildcardDirectories) {
    mutateMap(
      existingWatchedForWildcards,
      new Map(Object.entries(wildcardDirectories)),
      {
        // Create new watch and recursive info
        createNewValue: createWildcardDirectoryWatcher,
        // Close existing watch thats not needed any more
        onDeleteValue: closeFileWatcherOf,
        // Close existing watch that doesnt match in the flags
        onExistingValue: updateWildcardDirectoryWatcher
      }
    );
  } else {
    clearMap(existingWatchedForWildcards, closeFileWatcherOf);
  }
  function createWildcardDirectoryWatcher(directory, flags) {
    return {
      watcher: watchDirectory(directory, flags),
      flags
    };
  }
  function updateWildcardDirectoryWatcher(existingWatcher, flags, directory) {
    if (existingWatcher.flags ***REMOVED******REMOVED******REMOVED*** flags) {
      return;
    }
    existingWatcher.watcher.close();
    existingWatchedForWildcards.set(directory, createWildcardDirectoryWatcher(directory, flags));
  }
}
function isIgnoredFileFromWildCardWatching({
  watchedDirPath,
  fileOrDirectory,
  fileOrDirectoryPath,
  configFileName,
  options,
  program,
  extraFileExtensions,
  currentDirectory,
  useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
  writeLog,
  toPath: toPath3,
  getScriptKind
}) {
  const newPath ***REMOVED*** removeIgnoredPath(fileOrDirectoryPath);
  if (!newPath) {
    writeLog(`Project: ${configFileName} Detected ignored path: ${fileOrDirectory}`);
    return true;
  }
  fileOrDirectoryPath ***REMOVED*** newPath;
  if (fileOrDirectoryPath ***REMOVED******REMOVED******REMOVED*** watchedDirPath) return false;
  if (hasExtension(fileOrDirectoryPath) && !(isSupportedSourceFileName(fileOrDirectory, options, extraFileExtensions) || isSupportedScriptKind())) {
    writeLog(`Project: ${configFileName} Detected file add/remove of non supported extension: ${fileOrDirectory}`);
    return true;
  }
  if (isExcludedFile(fileOrDirectory, options.configFile.configFileSpecs, getNormalizedAbsolutePath(getDirectoryPath(configFileName), currentDirectory), useCaseSensitiveFileNames2, currentDirectory)) {
    writeLog(`Project: ${configFileName} Detected excluded file: ${fileOrDirectory}`);
    return true;
  }
  if (!program) return false;
  if (options.outFile || options.outDir) return false;
  if (isDeclarationFileName(fileOrDirectoryPath)) {
    if (options.declarationDir) return false;
  } else if (!fileExtensionIsOneOf(fileOrDirectoryPath, supportedJSExtensionsFlat)) {
    return false;
  }
  const filePathWithoutExtension ***REMOVED*** removeFileExtension(fileOrDirectoryPath);
  const realProgram ***REMOVED*** isArray(program) ? void 0 : isBuilderProgram(program) ? program.getProgramOrUndefined() : program;
  const builderProgram ***REMOVED*** !realProgram && !isArray(program) ? program : void 0;
  if (hasSourceFile(filePathWithoutExtension + ".ts" /* Ts */) || hasSourceFile(filePathWithoutExtension + ".tsx" /* Tsx */)) {
    writeLog(`Project: ${configFileName} Detected output file: ${fileOrDirectory}`);
    return true;
  }
  return false;
  function hasSourceFile(file) {
    return realProgram ? !!realProgram.getSourceFileByPath(file) : builderProgram ? builderProgram.state.fileInfos.has(file) : !!find(program, (rootFile) ***REMOVED***> toPath3(rootFile) ***REMOVED******REMOVED******REMOVED*** file);
  }
  function isSupportedScriptKind() {
    if (!getScriptKind) return false;
    const scriptKind ***REMOVED*** getScriptKind(fileOrDirectory);
    switch (scriptKind) {
      case 3 /* TS */:
      case 4 /* TSX */:
      case 7 /* Deferred */:
      case 5 /* External */:
        return true;
      case 1 /* JS */:
      case 2 /* JSX */:
        return getAllowJSCompilerOption(options);
      case 6 /* JSON */:
        return getResolveJsonModule(options);
      case 0 /* Unknown */:
        return false;
    }
  }
}
function isEmittedFileOfProgram(program, file) {
  if (!program) {
    return false;
  }
  return program.isEmittedFile(file);
}
function getWatchFactory(host, watchLogLevel, log, getDetailWatchInfo) {
  setSysLog(watchLogLevel ***REMOVED******REMOVED******REMOVED*** 2 /* Verbose */ ? log : noop);
  const plainInvokeFactory ***REMOVED*** {
    watchFile: (file, callback, pollingInterval, options) ***REMOVED***> host.watchFile(file, callback, pollingInterval, options),
    watchDirectory: (directory, callback, flags, options) ***REMOVED***> host.watchDirectory(directory, callback, (flags & 1 /* Recursive */) !***REMOVED******REMOVED*** 0, options)
  };
  const triggerInvokingFactory ***REMOVED*** watchLogLevel !***REMOVED******REMOVED*** 0 /* None */ ? {
    watchFile: createTriggerLoggingAddWatch("watchFile"),
    watchDirectory: createTriggerLoggingAddWatch("watchDirectory")
  } : void 0;
  const factory2 ***REMOVED*** watchLogLevel ***REMOVED******REMOVED******REMOVED*** 2 /* Verbose */ ? {
    watchFile: createFileWatcherWithLogging,
    watchDirectory: createDirectoryWatcherWithLogging
  } : triggerInvokingFactory || plainInvokeFactory;
  const excludeWatcherFactory ***REMOVED*** watchLogLevel ***REMOVED******REMOVED******REMOVED*** 2 /* Verbose */ ? createExcludeWatcherWithLogging : returnNoopFileWatcher;
  return {
    watchFile: createExcludeHandlingAddWatch("watchFile"),
    watchDirectory: createExcludeHandlingAddWatch("watchDirectory")
  };
  function createExcludeHandlingAddWatch(key) {
    return (file, cb, flags, options, detailInfo1, detailInfo2) ***REMOVED***> {
      var _a;
      return !matchesExclude(file, key ***REMOVED******REMOVED******REMOVED*** "watchFile" ? options ***REMOVED******REMOVED*** null ? void 0 : options.excludeFiles : options ***REMOVED******REMOVED*** null ? void 0 : options.excludeDirectories, useCaseSensitiveFileNames2(), ((_a ***REMOVED*** host.getCurrentDirectory) ***REMOVED******REMOVED*** null ? void 0 : _a.call(host)) || "") ? factory2[key].call(
        /*thisArgs*/
        void 0,
        file,
        cb,
        flags,
        options,
        detailInfo1,
        detailInfo2
      ) : excludeWatcherFactory(file, flags, options, detailInfo1, detailInfo2);
    };
  }
  function useCaseSensitiveFileNames2() {
    return typeof host.useCaseSensitiveFileNames ***REMOVED******REMOVED******REMOVED*** "boolean" ? host.useCaseSensitiveFileNames : host.useCaseSensitiveFileNames();
  }
  function createExcludeWatcherWithLogging(file, flags, options, detailInfo1, detailInfo2) {
    log(`ExcludeWatcher:: Added:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`);
    return {
      close: () ***REMOVED***> log(`ExcludeWatcher:: Close:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`)
    };
  }
  function createFileWatcherWithLogging(file, cb, flags, options, detailInfo1, detailInfo2) {
    log(`FileWatcher:: Added:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`);
    const watcher ***REMOVED*** triggerInvokingFactory.watchFile(file, cb, flags, options, detailInfo1, detailInfo2);
    return {
      close: () ***REMOVED***> {
        log(`FileWatcher:: Close:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`);
        watcher.close();
      }
    };
  }
  function createDirectoryWatcherWithLogging(file, cb, flags, options, detailInfo1, detailInfo2) {
    const watchInfo ***REMOVED*** `DirectoryWatcher:: Added:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`;
    log(watchInfo);
    const start ***REMOVED*** timestamp();
    const watcher ***REMOVED*** triggerInvokingFactory.watchDirectory(file, cb, flags, options, detailInfo1, detailInfo2);
    const elapsed ***REMOVED*** timestamp() - start;
    log(`Elapsed:: ${elapsed}ms ${watchInfo}`);
    return {
      close: () ***REMOVED***> {
        const watchInfo2 ***REMOVED*** `DirectoryWatcher:: Close:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`;
        log(watchInfo2);
        const start2 ***REMOVED*** timestamp();
        watcher.close();
        const elapsed2 ***REMOVED*** timestamp() - start2;
        log(`Elapsed:: ${elapsed2}ms ${watchInfo2}`);
      }
    };
  }
  function createTriggerLoggingAddWatch(key) {
    return (file, cb, flags, options, detailInfo1, detailInfo2) ***REMOVED***> plainInvokeFactory[key].call(
      /*thisArgs*/
      void 0,
      file,
      (...args) ***REMOVED***> {
        const triggerredInfo ***REMOVED*** `${key ***REMOVED******REMOVED******REMOVED*** "watchFile" ? "FileWatcher" : "DirectoryWatcher"}:: Triggered with ${args[0]} ${args[1] !***REMOVED******REMOVED*** void 0 ? args[1] : ""}:: ${getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo)}`;
        log(triggerredInfo);
        const start ***REMOVED*** timestamp();
        cb.call(
          /*thisArg*/
          void 0,
          ...args
        );
        const elapsed ***REMOVED*** timestamp() - start;
        log(`Elapsed:: ${elapsed}ms ${triggerredInfo}`);
      },
      flags,
      options,
      detailInfo1,
      detailInfo2
    );
  }
  function getWatchInfo(file, flags, options, detailInfo1, detailInfo2, getDetailWatchInfo2) {
    return `WatchInfo: ${file} ${flags} ${JSON.stringify(options)} ${getDetailWatchInfo2 ? getDetailWatchInfo2(detailInfo1, detailInfo2) : detailInfo2 ***REMOVED******REMOVED******REMOVED*** void 0 ? detailInfo1 : `${detailInfo1} ${detailInfo2}`}`;
  }
}
function getFallbackOptions(options) {
  const fallbackPolling ***REMOVED*** options ***REMOVED******REMOVED*** null ? void 0 : options.fallbackPolling;
  return {
    watchFile: fallbackPolling !***REMOVED******REMOVED*** void 0 ? fallbackPolling : 1 /* PriorityPollingInterval */
  };
}
function closeFileWatcherOf(objWithWatcher) {
  objWithWatcher.watcher.close();
}

// src/compiler/program.ts
function findConfigFile(searchPath, fileExists, configName ***REMOVED*** "tsconfig.json") {
  return forEachAncestorDirectory(searchPath, (ancestor) ***REMOVED***> {
    const fileName ***REMOVED*** combinePaths(ancestor, configName);
    return fileExists(fileName) ? fileName : void 0;
  });
}
function resolveTripleslashReference(moduleName, containingFile) {
  const basePath ***REMOVED*** getDirectoryPath(containingFile);
  const referencedFileName ***REMOVED*** isRootedDiskPath(moduleName) ? moduleName : combinePaths(basePath, moduleName);
  return normalizePath(referencedFileName);
}
function computeCommonSourceDirectoryOfFilenames(fileNames, currentDirectory, getCanonicalFileName) {
  let commonPathComponents;
  const failed2 ***REMOVED*** forEach(fileNames, (sourceFile) ***REMOVED***> {
    const sourcePathComponents ***REMOVED*** getNormalizedPathComponents(sourceFile, currentDirectory);
    sourcePathComponents.pop();
    if (!commonPathComponents) {
      commonPathComponents ***REMOVED*** sourcePathComponents;
      return;
    }
    const n ***REMOVED*** Math.min(commonPathComponents.length, sourcePathComponents.length);
    for (let i ***REMOVED*** 0; i < n; i++) {
      if (getCanonicalFileName(commonPathComponents[i]) !***REMOVED******REMOVED*** getCanonicalFileName(sourcePathComponents[i])) {
        if (i ***REMOVED******REMOVED******REMOVED*** 0) {
          return true;
        }
        commonPathComponents.length ***REMOVED*** i;
        break;
      }
    }
    if (sourcePathComponents.length < commonPathComponents.length) {
      commonPathComponents.length ***REMOVED*** sourcePathComponents.length;
    }
  });
  if (failed2) {
    return "";
  }
  if (!commonPathComponents) {
    return currentDirectory;
  }
  return getPathFromPathComponents(commonPathComponents);
}
function createCompilerHost(options, setParentNodes) {
  return createCompilerHostWorker(options, setParentNodes);
}
function createGetSourceFile(readFile, setParentNodes) {
  return (fileName, languageVersionOrOptions, onError) ***REMOVED***> {
    let text;
    try {
      mark("beforeIORead");
      text ***REMOVED*** readFile(fileName);
      mark("afterIORead");
      measure("I/O Read", "beforeIORead", "afterIORead");
    } catch (e) {
      if (onError) {
        onError(e.message);
      }
      text ***REMOVED*** "";
    }
    return text !***REMOVED******REMOVED*** void 0 ? createSourceFile(fileName, text, languageVersionOrOptions, setParentNodes) : void 0;
  };
}
function createWriteFileMeasuringIO(actualWriteFile, createDirectory, directoryExists) {
  return (fileName, data, writeByteOrderMark, onError) ***REMOVED***> {
    try {
      mark("beforeIOWrite");
      writeFileEnsuringDirectories(
        fileName,
        data,
        writeByteOrderMark,
        actualWriteFile,
        createDirectory,
        directoryExists
      );
      mark("afterIOWrite");
      measure("I/O Write", "beforeIOWrite", "afterIOWrite");
    } catch (e) {
      if (onError) {
        onError(e.message);
      }
    }
  };
}
function createCompilerHostWorker(options, setParentNodes, system ***REMOVED*** sys) {
  const existingDirectories ***REMOVED*** /* @__PURE__ */ new Map();
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(system.useCaseSensitiveFileNames);
  function directoryExists(directoryPath) {
    if (existingDirectories.has(directoryPath)) {
      return true;
    }
    if ((compilerHost.directoryExists || system.directoryExists)(directoryPath)) {
      existingDirectories.set(directoryPath, true);
      return true;
    }
    return false;
  }
  function getDefaultLibLocation() {
    return getDirectoryPath(normalizePath(system.getExecutingFilePath()));
  }
  const newLine ***REMOVED*** getNewLineCharacter(options);
  const realpath ***REMOVED*** system.realpath && ((path) ***REMOVED***> system.realpath(path));
  const compilerHost ***REMOVED*** {
    getSourceFile: createGetSourceFile((fileName) ***REMOVED***> compilerHost.readFile(fileName), setParentNodes),
    getDefaultLibLocation,
    getDefaultLibFileName: (options2) ***REMOVED***> combinePaths(getDefaultLibLocation(), getDefaultLibFileName(options2)),
    writeFile: createWriteFileMeasuringIO(
      (path, data, writeByteOrderMark) ***REMOVED***> system.writeFile(path, data, writeByteOrderMark),
      (path) ***REMOVED***> (compilerHost.createDirectory || system.createDirectory)(path),
      (path) ***REMOVED***> directoryExists(path)
    ),
    getCurrentDirectory: memoize(() ***REMOVED***> system.getCurrentDirectory()),
    useCaseSensitiveFileNames: () ***REMOVED***> system.useCaseSensitiveFileNames,
    getCanonicalFileName,
    getNewLine: () ***REMOVED***> newLine,
    fileExists: (fileName) ***REMOVED***> system.fileExists(fileName),
    readFile: (fileName) ***REMOVED***> system.readFile(fileName),
    trace: (s) ***REMOVED***> system.write(s + newLine),
    directoryExists: (directoryName) ***REMOVED***> system.directoryExists(directoryName),
    getEnvironmentVariable: (name) ***REMOVED***> system.getEnvironmentVariable ? system.getEnvironmentVariable(name) : "",
    getDirectories: (path) ***REMOVED***> system.getDirectories(path),
    realpath,
    readDirectory: (path, extensions, include, exclude, depth) ***REMOVED***> system.readDirectory(path, extensions, include, exclude, depth),
    createDirectory: (d) ***REMOVED***> system.createDirectory(d),
    createHash: maybeBind(system, system.createHash)
  };
  return compilerHost;
}
function changeCompilerHostLikeToUseCache(host, toPath3, getSourceFile) {
  const originalReadFile ***REMOVED*** host.readFile;
  const originalFileExists ***REMOVED*** host.fileExists;
  const originalDirectoryExists ***REMOVED*** host.directoryExists;
  const originalCreateDirectory ***REMOVED*** host.createDirectory;
  const originalWriteFile ***REMOVED*** host.writeFile;
  const readFileCache ***REMOVED*** /* @__PURE__ */ new Map();
  const fileExistsCache ***REMOVED*** /* @__PURE__ */ new Map();
  const directoryExistsCache ***REMOVED*** /* @__PURE__ */ new Map();
  const sourceFileCache ***REMOVED*** /* @__PURE__ */ new Map();
  const readFileWithCache ***REMOVED*** (fileName) ***REMOVED***> {
    const key ***REMOVED*** toPath3(fileName);
    const value ***REMOVED*** readFileCache.get(key);
    if (value !***REMOVED******REMOVED*** void 0) return value !***REMOVED******REMOVED*** false ? value : void 0;
    return setReadFileCache(key, fileName);
  };
  const setReadFileCache ***REMOVED*** (key, fileName) ***REMOVED***> {
    const newValue ***REMOVED*** originalReadFile.call(host, fileName);
    readFileCache.set(key, newValue !***REMOVED******REMOVED*** void 0 ? newValue : false);
    return newValue;
  };
  host.readFile ***REMOVED*** (fileName) ***REMOVED***> {
    const key ***REMOVED*** toPath3(fileName);
    const value ***REMOVED*** readFileCache.get(key);
    if (value !***REMOVED******REMOVED*** void 0) return value !***REMOVED******REMOVED*** false ? value : void 0;
    if (!fileExtensionIs(fileName, ".json" /* Json */) && !isBuildInfoFile(fileName)) {
      return originalReadFile.call(host, fileName);
    }
    return setReadFileCache(key, fileName);
  };
  const getSourceFileWithCache ***REMOVED*** getSourceFile ? (fileName, languageVersionOrOptions, onError, shouldCreateNewSourceFile) ***REMOVED***> {
    const key ***REMOVED*** toPath3(fileName);
    const impliedNodeFormat ***REMOVED*** typeof languageVersionOrOptions ***REMOVED******REMOVED******REMOVED*** "object" ? languageVersionOrOptions.impliedNodeFormat : void 0;
    const forImpliedNodeFormat ***REMOVED*** sourceFileCache.get(impliedNodeFormat);
    const value ***REMOVED*** forImpliedNodeFormat ***REMOVED******REMOVED*** null ? void 0 : forImpliedNodeFormat.get(key);
    if (value) return value;
    const sourceFile ***REMOVED*** getSourceFile(fileName, languageVersionOrOptions, onError, shouldCreateNewSourceFile);
    if (sourceFile && (isDeclarationFileName(fileName) || fileExtensionIs(fileName, ".json" /* Json */))) {
      sourceFileCache.set(impliedNodeFormat, (forImpliedNodeFormat || /* @__PURE__ */ new Map()).set(key, sourceFile));
    }
    return sourceFile;
  } : void 0;
  host.fileExists ***REMOVED*** (fileName) ***REMOVED***> {
    const key ***REMOVED*** toPath3(fileName);
    const value ***REMOVED*** fileExistsCache.get(key);
    if (value !***REMOVED******REMOVED*** void 0) return value;
    const newValue ***REMOVED*** originalFileExists.call(host, fileName);
    fileExistsCache.set(key, !!newValue);
    return newValue;
  };
  if (originalWriteFile) {
    host.writeFile ***REMOVED*** (fileName, data, ...rest) ***REMOVED***> {
      const key ***REMOVED*** toPath3(fileName);
      fileExistsCache.delete(key);
      const value ***REMOVED*** readFileCache.get(key);
      if (value !***REMOVED******REMOVED*** void 0 && value !***REMOVED******REMOVED*** data) {
        readFileCache.delete(key);
        sourceFileCache.forEach((map2) ***REMOVED***> map2.delete(key));
      } else if (getSourceFileWithCache) {
        sourceFileCache.forEach((map2) ***REMOVED***> {
          const sourceFile ***REMOVED*** map2.get(key);
          if (sourceFile && sourceFile.text !***REMOVED******REMOVED*** data) {
            map2.delete(key);
          }
        });
      }
      originalWriteFile.call(host, fileName, data, ...rest);
    };
  }
  if (originalDirectoryExists) {
    host.directoryExists ***REMOVED*** (directory) ***REMOVED***> {
      const key ***REMOVED*** toPath3(directory);
      const value ***REMOVED*** directoryExistsCache.get(key);
      if (value !***REMOVED******REMOVED*** void 0) return value;
      const newValue ***REMOVED*** originalDirectoryExists.call(host, directory);
      directoryExistsCache.set(key, !!newValue);
      return newValue;
    };
    if (originalCreateDirectory) {
      host.createDirectory ***REMOVED*** (directory) ***REMOVED***> {
        const key ***REMOVED*** toPath3(directory);
        directoryExistsCache.delete(key);
        originalCreateDirectory.call(host, directory);
      };
    }
  }
  return {
    originalReadFile,
    originalFileExists,
    originalDirectoryExists,
    originalCreateDirectory,
    originalWriteFile,
    getSourceFileWithCache,
    readFileWithCache
  };
}
function formatDiagnostic(diagnostic, host) {
  const errorMessage ***REMOVED*** `${diagnosticCategoryName(diagnostic)} TS${diagnostic.code}: ${flattenDiagnosticMessageText(diagnostic.messageText, host.getNewLine())}${host.getNewLine()}`;
  if (diagnostic.file) {
    const { line, character } ***REMOVED*** getLineAndCharacterOfPosition(diagnostic.file, diagnostic.start);
    const fileName ***REMOVED*** diagnostic.file.fileName;
    const relativeFileName ***REMOVED*** convertToRelativePath(fileName, host.getCurrentDirectory(), (fileName2) ***REMOVED***> host.getCanonicalFileName(fileName2));
    return `${relativeFileName}(${line + 1},${character + 1}): ` + errorMessage;
  }
  return errorMessage;
}
var gutterStyleSequence ***REMOVED*** "\x1B[7m";
var gutterSeparator ***REMOVED*** " ";
var resetEscapeSequence ***REMOVED*** "\x1B[0m";
var ellipsis ***REMOVED*** "...";
var halfIndent ***REMOVED*** "  ";
var indent ***REMOVED*** "    ";
function getCategoryFormat(category) {
  switch (category) {
    case 1 /* Error */:
      return "\x1B[91m" /* Red */;
    case 0 /* Warning */:
      return "\x1B[93m" /* Yellow */;
    case 2 /* Suggestion */:
      return Debug.fail("Should never get an Info diagnostic on the command line.");
    case 3 /* Message */:
      return "\x1B[94m" /* Blue */;
  }
}
function formatColorAndReset(text, formatStyle) {
  return formatStyle + text + resetEscapeSequence;
}
function formatCodeSpan(file, start, length2, indent2, squiggleColor, host) {
  const { line: firstLine, character: firstLineChar } ***REMOVED*** getLineAndCharacterOfPosition(file, start);
  const { line: lastLine, character: lastLineChar } ***REMOVED*** getLineAndCharacterOfPosition(file, start + length2);
  const lastLineInFile ***REMOVED*** getLineAndCharacterOfPosition(file, file.text.length).line;
  const hasMoreThanFiveLines ***REMOVED*** lastLine - firstLine >***REMOVED*** 4;
  let gutterWidth ***REMOVED*** (lastLine + 1 + "").length;
  if (hasMoreThanFiveLines) {
    gutterWidth ***REMOVED*** Math.max(ellipsis.length, gutterWidth);
  }
  let context ***REMOVED*** "";
  for (let i ***REMOVED*** firstLine; i <***REMOVED*** lastLine; i++) {
    context +***REMOVED*** host.getNewLine();
    if (hasMoreThanFiveLines && firstLine + 1 < i && i < lastLine - 1) {
      context +***REMOVED*** indent2 + formatColorAndReset(ellipsis.padStart(gutterWidth), gutterStyleSequence) + gutterSeparator + host.getNewLine();
      i ***REMOVED*** lastLine - 1;
    }
    const lineStart ***REMOVED*** getPositionOfLineAndCharacter(file, i, 0);
    const lineEnd ***REMOVED*** i < lastLineInFile ? getPositionOfLineAndCharacter(file, i + 1, 0) : file.text.length;
    let lineContent ***REMOVED*** file.text.slice(lineStart, lineEnd);
    lineContent ***REMOVED*** lineContent.trimEnd();
    lineContent ***REMOVED*** lineContent.replace(/\t/g, " ");
    context +***REMOVED*** indent2 + formatColorAndReset((i + 1 + "").padStart(gutterWidth), gutterStyleSequence) + gutterSeparator;
    context +***REMOVED*** lineContent + host.getNewLine();
    context +***REMOVED*** indent2 + formatColorAndReset("".padStart(gutterWidth), gutterStyleSequence) + gutterSeparator;
    context +***REMOVED*** squiggleColor;
    if (i ***REMOVED******REMOVED******REMOVED*** firstLine) {
      const lastCharForLine ***REMOVED*** i ***REMOVED******REMOVED******REMOVED*** lastLine ? lastLineChar : void 0;
      context +***REMOVED*** lineContent.slice(0, firstLineChar).replace(/\S/g, " ");
      context +***REMOVED*** lineContent.slice(firstLineChar, lastCharForLine).replace(/./g, "~");
    } else if (i ***REMOVED******REMOVED******REMOVED*** lastLine) {
      context +***REMOVED*** lineContent.slice(0, lastLineChar).replace(/./g, "~");
    } else {
      context +***REMOVED*** lineContent.replace(/./g, "~");
    }
    context +***REMOVED*** resetEscapeSequence;
  }
  return context;
}
function formatLocation(file, start, host, color ***REMOVED*** formatColorAndReset) {
  const { line: firstLine, character: firstLineChar } ***REMOVED*** getLineAndCharacterOfPosition(file, start);
  const relativeFileName ***REMOVED*** host ? convertToRelativePath(file.fileName, host.getCurrentDirectory(), (fileName) ***REMOVED***> host.getCanonicalFileName(fileName)) : file.fileName;
  let output ***REMOVED*** "";
  output +***REMOVED*** color(relativeFileName, "\x1B[96m" /* Cyan */);
  output +***REMOVED*** ":";
  output +***REMOVED*** color(`${firstLine + 1}`, "\x1B[93m" /* Yellow */);
  output +***REMOVED*** ":";
  output +***REMOVED*** color(`${firstLineChar + 1}`, "\x1B[93m" /* Yellow */);
  return output;
}
function formatDiagnosticsWithColorAndContext(diagnostics, host) {
  let output ***REMOVED*** "";
  for (const diagnostic of diagnostics) {
    if (diagnostic.file) {
      const { file, start } ***REMOVED*** diagnostic;
      output +***REMOVED*** formatLocation(file, start, host);
      output +***REMOVED*** " - ";
    }
    output +***REMOVED*** formatColorAndReset(diagnosticCategoryName(diagnostic), getCategoryFormat(diagnostic.category));
    output +***REMOVED*** formatColorAndReset(` TS${diagnostic.code}: `, "\x1B[90m" /* Grey */);
    output +***REMOVED*** flattenDiagnosticMessageText(diagnostic.messageText, host.getNewLine());
    if (diagnostic.file && diagnostic.code !***REMOVED******REMOVED*** Diagnostics.File_appears_to_be_binary.code) {
      output +***REMOVED*** host.getNewLine();
      output +***REMOVED*** formatCodeSpan(diagnostic.file, diagnostic.start, diagnostic.length, "", getCategoryFormat(diagnostic.category), host);
    }
    if (diagnostic.relatedInformation) {
      output +***REMOVED*** host.getNewLine();
      for (const { file, start, length: length2, messageText } of diagnostic.relatedInformation) {
        if (file) {
          output +***REMOVED*** host.getNewLine();
          output +***REMOVED*** halfIndent + formatLocation(file, start, host);
          output +***REMOVED*** formatCodeSpan(file, start, length2, indent, "\x1B[96m" /* Cyan */, host);
        }
        output +***REMOVED*** host.getNewLine();
        output +***REMOVED*** indent + flattenDiagnosticMessageText(messageText, host.getNewLine());
      }
    }
    output +***REMOVED*** host.getNewLine();
  }
  return output;
}
function flattenDiagnosticMessageText(diag2, newLine, indent2 ***REMOVED*** 0) {
  if (isString(diag2)) {
    return diag2;
  } else if (diag2 ***REMOVED******REMOVED******REMOVED*** void 0) {
    return "";
  }
  let result ***REMOVED*** "";
  if (indent2) {
    result +***REMOVED*** newLine;
    for (let i ***REMOVED*** 0; i < indent2; i++) {
      result +***REMOVED*** "  ";
    }
  }
  result +***REMOVED*** diag2.messageText;
  indent2++;
  if (diag2.next) {
    for (const kid of diag2.next) {
      result +***REMOVED*** flattenDiagnosticMessageText(kid, newLine, indent2);
    }
  }
  return result;
}
function getModeForFileReference(ref, containingFileMode) {
  return (isString(ref) ? containingFileMode : ref.resolutionMode) || containingFileMode;
}
function isExclusivelyTypeOnlyImportOrExport(decl) {
  var _a;
  if (isExportDeclaration(decl)) {
    return decl.isTypeOnly;
  }
  if ((_a ***REMOVED*** decl.importClause) ***REMOVED******REMOVED*** null ? void 0 : _a.isTypeOnly) {
    return true;
  }
  return false;
}
function getModeForUsageLocation(file, usage, compilerOptions) {
  return getModeForUsageLocationWorker(file, usage, compilerOptions);
}
function getModeForUsageLocationWorker(file, usage, compilerOptions) {
  if (isImportDeclaration(usage.parent) || isExportDeclaration(usage.parent) || isJSDocImportTag(usage.parent)) {
    const isTypeOnly ***REMOVED*** isExclusivelyTypeOnlyImportOrExport(usage.parent);
    if (isTypeOnly) {
      const override ***REMOVED*** getResolutionModeOverride(usage.parent.attributes);
      if (override) {
        return override;
      }
    }
  }
  if (usage.parent.parent && isImportTypeNode(usage.parent.parent)) {
    const override ***REMOVED*** getResolutionModeOverride(usage.parent.parent.attributes);
    if (override) {
      return override;
    }
  }
  if (compilerOptions && importSyntaxAffectsModuleResolution(compilerOptions)) {
    return getEmitSyntaxForUsageLocationWorker(file, usage, compilerOptions);
  }
}
function getEmitSyntaxForUsageLocationWorker(file, usage, compilerOptions) {
  var _a;
  if (!compilerOptions) {
    return void 0;
  }
  const exprParentParent ***REMOVED*** (_a ***REMOVED*** walkUpParenthesizedExpressions(usage.parent)) ***REMOVED******REMOVED*** null ? void 0 : _a.parent;
  if (exprParentParent && isImportEqualsDeclaration(exprParentParent) || isRequireCall(
    usage.parent,
    /*requireStringLiteralLikeArgument*/
    false
  )) {
    return 1 /* CommonJS */;
  }
  if (isImportCall(walkUpParenthesizedExpressions(usage.parent))) {
    return shouldTransformImportCallWorker(file, compilerOptions) ? 1 /* CommonJS */ : 99 /* ESNext */;
  }
  const fileEmitMode ***REMOVED*** getEmitModuleFormatOfFileWorker(file, compilerOptions);
  return fileEmitMode ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ ? 1 /* CommonJS */ : emitModuleKindIsNonNodeESM(fileEmitMode) || fileEmitMode ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */ ? 99 /* ESNext */ : void 0;
}
function getResolutionModeOverride(node, grammarErrorOnNode) {
  if (!node) return void 0;
  if (length(node.elements) !***REMOVED******REMOVED*** 1) {
    grammarErrorOnNode ***REMOVED******REMOVED*** null ? void 0 : grammarErrorOnNode(
      node,
      node.token ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ ? Diagnostics.Type_import_attributes_should_have_exactly_one_key_resolution_mode_with_value_import_or_require : Diagnostics.Type_import_assertions_should_have_exactly_one_key_resolution_mode_with_value_import_or_require
    );
    return void 0;
  }
  const elem ***REMOVED*** node.elements[0];
  if (!isStringLiteralLike(elem.name)) return void 0;
  if (elem.name.text !***REMOVED******REMOVED*** "resolution-mode") {
    grammarErrorOnNode ***REMOVED******REMOVED*** null ? void 0 : grammarErrorOnNode(
      elem.name,
      node.token ***REMOVED******REMOVED******REMOVED*** 118 /* WithKeyword */ ? Diagnostics.resolution_mode_is_the_only_valid_key_for_type_import_attributes : Diagnostics.resolution_mode_is_the_only_valid_key_for_type_import_assertions
    );
    return void 0;
  }
  if (!isStringLiteralLike(elem.value)) return void 0;
  if (elem.value.text !***REMOVED******REMOVED*** "import" && elem.value.text !***REMOVED******REMOVED*** "require") {
    grammarErrorOnNode ***REMOVED******REMOVED*** null ? void 0 : grammarErrorOnNode(elem.value, Diagnostics.resolution_mode_should_be_either_require_or_import);
    return void 0;
  }
  return elem.value.text ***REMOVED******REMOVED******REMOVED*** "import" ? 99 /* ESNext */ : 1 /* CommonJS */;
}
var emptyResolution ***REMOVED*** {
  resolvedModule: void 0,
  resolvedTypeReferenceDirective: void 0
};
function getModuleResolutionName(literal) {
  return literal.text;
}
var moduleResolutionNameAndModeGetter ***REMOVED*** {
  getName: getModuleResolutionName,
  getMode: (entry, file, compilerOptions) ***REMOVED***> getModeForUsageLocation(file, entry, compilerOptions)
};
function createModuleResolutionLoader(containingFile, redirectedReference, options, host, cache) {
  return {
    nameAndMode: moduleResolutionNameAndModeGetter,
    resolve: (moduleName, resolutionMode) ***REMOVED***> resolveModuleName(
      moduleName,
      containingFile,
      options,
      host,
      cache,
      redirectedReference,
      resolutionMode
    )
  };
}
function getTypeReferenceResolutionName(entry) {
  return !isString(entry) ? entry.fileName : entry;
}
var typeReferenceResolutionNameAndModeGetter ***REMOVED*** {
  getName: getTypeReferenceResolutionName,
  getMode: (entry, file, compilerOptions) ***REMOVED***> getModeForFileReference(entry, file && getDefaultResolutionModeForFileWorker(file, compilerOptions))
};
function createTypeReferenceResolutionLoader(containingFile, redirectedReference, options, host, cache) {
  return {
    nameAndMode: typeReferenceResolutionNameAndModeGetter,
    resolve: (typeRef, resoluionMode) ***REMOVED***> resolveTypeReferenceDirective(
      typeRef,
      containingFile,
      options,
      host,
      redirectedReference,
      cache,
      resoluionMode
    )
  };
}
function loadWithModeAwareCache(entries, containingFile, redirectedReference, options, containingSourceFile, host, resolutionCache, createLoader) {
  if (entries.length ***REMOVED******REMOVED******REMOVED*** 0) return emptyArray;
  const resolutions ***REMOVED*** [];
  const cache ***REMOVED*** /* @__PURE__ */ new Map();
  const loader ***REMOVED*** createLoader(containingFile, redirectedReference, options, host, resolutionCache);
  for (const entry of entries) {
    const name ***REMOVED*** loader.nameAndMode.getName(entry);
    const mode ***REMOVED*** loader.nameAndMode.getMode(entry, containingSourceFile, (redirectedReference ***REMOVED******REMOVED*** null ? void 0 : redirectedReference.commandLine.options) || options);
    const key ***REMOVED*** createModeAwareCacheKey(name, mode);
    let result ***REMOVED*** cache.get(key);
    if (!result) {
      cache.set(key, result ***REMOVED*** loader.resolve(name, mode));
    }
    resolutions.push(result);
  }
  return resolutions;
}
var inferredTypesContainingFile ***REMOVED*** "__inferred type names__.ts";
function getInferredLibraryNameResolveFrom(options, currentDirectory, libFileName) {
  const containingDirectory ***REMOVED*** options.configFilePath ? getDirectoryPath(options.configFilePath) : currentDirectory;
  return combinePaths(containingDirectory, `__lib_node_modules_lookup_${libFileName}__.ts`);
}
function getLibraryNameFromLibFileName(libFileName) {
  const components ***REMOVED*** libFileName.split(".");
  let path ***REMOVED*** components[1];
  let i ***REMOVED*** 2;
  while (components[i] && components[i] !***REMOVED******REMOVED*** "d") {
    path +***REMOVED*** (i ***REMOVED******REMOVED******REMOVED*** 2 ? "/" : "-") + components[i];
    i++;
  }
  return "@typescript/lib-" + path;
}
function isReferencedFile(reason) {
  switch (reason ***REMOVED******REMOVED*** null ? void 0 : reason.kind) {
    case 3 /* Import */:
    case 4 /* ReferenceFile */:
    case 5 /* TypeReferenceDirective */:
    case 7 /* LibReferenceDirective */:
      return true;
    default:
      return false;
  }
}
function isReferenceFileLocation(location) {
  return location.pos !***REMOVED******REMOVED*** void 0;
}
function getReferencedFileLocation(program, ref) {
  var _a, _b, _c, _d;
  const file ***REMOVED*** Debug.checkDefined(program.getSourceFileByPath(ref.file));
  const { kind, index } ***REMOVED*** ref;
  let pos, end, packageId;
  switch (kind) {
    case 3 /* Import */:
      const importLiteral ***REMOVED*** getModuleNameStringLiteralAt(file, index);
      packageId ***REMOVED*** (_b ***REMOVED*** (_a ***REMOVED*** program.getResolvedModuleFromModuleSpecifier(importLiteral, file)) ***REMOVED******REMOVED*** null ? void 0 : _a.resolvedModule) ***REMOVED******REMOVED*** null ? void 0 : _b.packageId;
      if (importLiteral.pos ***REMOVED******REMOVED******REMOVED*** -1) return { file, packageId, text: importLiteral.text };
      pos ***REMOVED*** skipTrivia(file.text, importLiteral.pos);
      end ***REMOVED*** importLiteral.end;
      break;
    case 4 /* ReferenceFile */:
      ({ pos, end } ***REMOVED*** file.referencedFiles[index]);
      break;
    case 5 /* TypeReferenceDirective */:
      ({ pos, end } ***REMOVED*** file.typeReferenceDirectives[index]);
      packageId ***REMOVED*** (_d ***REMOVED*** (_c ***REMOVED*** program.getResolvedTypeReferenceDirectiveFromTypeReferenceDirective(file.typeReferenceDirectives[index], file)) ***REMOVED******REMOVED*** null ? void 0 : _c.resolvedTypeReferenceDirective) ***REMOVED******REMOVED*** null ? void 0 : _d.packageId;
      break;
    case 7 /* LibReferenceDirective */:
      ({ pos, end } ***REMOVED*** file.libReferenceDirectives[index]);
      break;
    default:
      return Debug.assertNever(kind);
  }
  return { file, pos, end, packageId };
}
function isProgramUptoDate(program, rootFileNames, newOptions, getSourceVersion, fileExists, hasInvalidatedResolutions, hasInvalidatedLibResolutions, hasChangedAutomaticTypeDirectiveNames, getParsedCommandLine, projectReferences) {
  if (!program || (hasChangedAutomaticTypeDirectiveNames ***REMOVED******REMOVED*** null ? void 0 : hasChangedAutomaticTypeDirectiveNames())) return false;
  if (!arrayIsEqualTo(program.getRootFileNames(), rootFileNames)) return false;
  let seenResolvedRefs;
  if (!arrayIsEqualTo(program.getProjectReferences(), projectReferences, projectReferenceUptoDate)) return false;
  if (program.getSourceFiles().some(sourceFileNotUptoDate)) return false;
  const missingPaths ***REMOVED*** program.getMissingFilePaths();
  if (missingPaths && forEachEntry(missingPaths, fileExists)) return false;
  const currentOptions ***REMOVED*** program.getCompilerOptions();
  if (!compareDataObjects(currentOptions, newOptions)) return false;
  if (program.resolvedLibReferences && forEachEntry(program.resolvedLibReferences, (_value, libFileName) ***REMOVED***> hasInvalidatedLibResolutions(libFileName))) return false;
  if (currentOptions.configFile && newOptions.configFile) return currentOptions.configFile.text ***REMOVED******REMOVED******REMOVED*** newOptions.configFile.text;
  return true;
  function sourceFileNotUptoDate(sourceFile) {
    return !sourceFileVersionUptoDate(sourceFile) || hasInvalidatedResolutions(sourceFile.path);
  }
  function sourceFileVersionUptoDate(sourceFile) {
    return sourceFile.version ***REMOVED******REMOVED******REMOVED*** getSourceVersion(sourceFile.resolvedPath, sourceFile.fileName);
  }
  function projectReferenceUptoDate(oldRef, newRef, index) {
    return projectReferenceIsEqualTo(oldRef, newRef) && resolvedProjectReferenceUptoDate(program.getResolvedProjectReferences()[index], oldRef);
  }
  function resolvedProjectReferenceUptoDate(oldResolvedRef, oldRef) {
    if (oldResolvedRef) {
      if (contains(seenResolvedRefs, oldResolvedRef)) return true;
      const refPath2 ***REMOVED*** resolveProjectReferencePath(oldRef);
      const newParsedCommandLine ***REMOVED*** getParsedCommandLine(refPath2);
      if (!newParsedCommandLine) return false;
      if (oldResolvedRef.commandLine.options.configFile !***REMOVED******REMOVED*** newParsedCommandLine.options.configFile) return false;
      if (!arrayIsEqualTo(oldResolvedRef.commandLine.fileNames, newParsedCommandLine.fileNames)) return false;
      (seenResolvedRefs || (seenResolvedRefs ***REMOVED*** [])).push(oldResolvedRef);
      return !forEach(
        oldResolvedRef.references,
        (childResolvedRef, index) ***REMOVED***> !resolvedProjectReferenceUptoDate(
          childResolvedRef,
          oldResolvedRef.commandLine.projectReferences[index]
        )
      );
    }
    const refPath ***REMOVED*** resolveProjectReferencePath(oldRef);
    return !getParsedCommandLine(refPath);
  }
}
function getConfigFileParsingDiagnostics(configFileParseResult) {
  return configFileParseResult.options.configFile ? [...configFileParseResult.options.configFile.parseDiagnostics, ...configFileParseResult.errors] : configFileParseResult.errors;
}
function getImpliedNodeFormatForFileWorker(fileName, packageJsonInfoCache, host, options) {
  const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(options);
  const shouldLookupFromPackageJson ***REMOVED*** 3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */ || pathContainsNodeModules(fileName);
  return fileExtensionIsOneOf(fileName, [".d.mts" /* Dmts */, ".mts" /* Mts */, ".mjs" /* Mjs */]) ? 99 /* ESNext */ : fileExtensionIsOneOf(fileName, [".d.cts" /* Dcts */, ".cts" /* Cts */, ".cjs" /* Cjs */]) ? 1 /* CommonJS */ : shouldLookupFromPackageJson && fileExtensionIsOneOf(fileName, [".d.ts" /* Dts */, ".ts" /* Ts */, ".tsx" /* Tsx */, ".js" /* Js */, ".jsx" /* Jsx */]) ? lookupFromPackageJson() : void 0;
  function lookupFromPackageJson() {
    const state ***REMOVED*** getTemporaryModuleResolutionState(packageJsonInfoCache, host, options);
    const packageJsonLocations ***REMOVED*** [];
    state.failedLookupLocations ***REMOVED*** packageJsonLocations;
    state.affectingLocations ***REMOVED*** packageJsonLocations;
    const packageJsonScope ***REMOVED*** getPackageScopeForPath(getDirectoryPath(fileName), state);
    const impliedNodeFormat ***REMOVED*** (packageJsonScope ***REMOVED******REMOVED*** null ? void 0 : packageJsonScope.contents.packageJsonContent.type) ***REMOVED******REMOVED******REMOVED*** "module" ? 99 /* ESNext */ : 1 /* CommonJS */;
    return { impliedNodeFormat, packageJsonLocations, packageJsonScope };
  }
}
var plainJSErrors ***REMOVED*** /* @__PURE__ */ new Set([
  // binder errors
  Diagnostics.Cannot_redeclare_block_scoped_variable_0.code,
  Diagnostics.A_module_cannot_have_multiple_default_exports.code,
  Diagnostics.Another_export_default_is_here.code,
  Diagnostics.The_first_export_default_is_here.code,
  Diagnostics.Identifier_expected_0_is_a_reserved_word_at_the_top_level_of_a_module.code,
  Diagnostics.Identifier_expected_0_is_a_reserved_word_in_strict_mode_Modules_are_automatically_in_strict_mode.code,
  Diagnostics.Identifier_expected_0_is_a_reserved_word_that_cannot_be_used_here.code,
  Diagnostics.constructor_is_a_reserved_word.code,
  Diagnostics.delete_cannot_be_called_on_an_identifier_in_strict_mode.code,
  Diagnostics.Code_contained_in_a_class_is_evaluated_in_JavaScript_s_strict_mode_which_does_not_allow_this_use_of_0_For_more_information_see_https_Colon_Slash_Slashdeveloper_mozilla_org_Slashen_US_Slashdocs_SlashWeb_SlashJavaScript_SlashReference_SlashStrict_mode.code,
  Diagnostics.Invalid_use_of_0_Modules_are_automatically_in_strict_mode.code,
  Diagnostics.Invalid_use_of_0_in_strict_mode.code,
  Diagnostics.A_label_is_not_allowed_here.code,
  Diagnostics.with_statements_are_not_allowed_in_strict_mode.code,
  // grammar errors
  Diagnostics.A_break_statement_can_only_be_used_within_an_enclosing_iteration_or_switch_statement.code,
  Diagnostics.A_break_statement_can_only_jump_to_a_label_of_an_enclosing_statement.code,
  Diagnostics.A_class_declaration_without_the_default_modifier_must_have_a_name.code,
  Diagnostics.A_class_member_cannot_have_the_0_keyword.code,
  Diagnostics.A_comma_expression_is_not_allowed_in_a_computed_property_name.code,
  Diagnostics.A_continue_statement_can_only_be_used_within_an_enclosing_iteration_statement.code,
  Diagnostics.A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement.code,
  Diagnostics.A_continue_statement_can_only_jump_to_a_label_of_an_enclosing_iteration_statement.code,
  Diagnostics.A_default_clause_cannot_appear_more_than_once_in_a_switch_statement.code,
  Diagnostics.A_default_export_must_be_at_the_top_level_of_a_file_or_module_declaration.code,
  Diagnostics.A_definite_assignment_assertion_is_not_permitted_in_this_context.code,
  Diagnostics.A_destructuring_declaration_must_have_an_initializer.code,
  Diagnostics.A_get_accessor_cannot_have_parameters.code,
  Diagnostics.A_rest_element_cannot_contain_a_binding_pattern.code,
  Diagnostics.A_rest_element_cannot_have_a_property_name.code,
  Diagnostics.A_rest_element_cannot_have_an_initializer.code,
  Diagnostics.A_rest_element_must_be_last_in_a_destructuring_pattern.code,
  Diagnostics.A_rest_parameter_cannot_have_an_initializer.code,
  Diagnostics.A_rest_parameter_must_be_last_in_a_parameter_list.code,
  Diagnostics.A_rest_parameter_or_binding_pattern_may_not_have_a_trailing_comma.code,
  Diagnostics.A_return_statement_cannot_be_used_inside_a_class_static_block.code,
  Diagnostics.A_set_accessor_cannot_have_rest_parameter.code,
  Diagnostics.A_set_accessor_must_have_exactly_one_parameter.code,
  Diagnostics.An_export_declaration_can_only_be_used_at_the_top_level_of_a_module.code,
  Diagnostics.An_export_declaration_cannot_have_modifiers.code,
  Diagnostics.An_import_declaration_can_only_be_used_at_the_top_level_of_a_module.code,
  Diagnostics.An_import_declaration_cannot_have_modifiers.code,
  Diagnostics.An_object_member_cannot_be_declared_optional.code,
  Diagnostics.Argument_of_dynamic_import_cannot_be_spread_element.code,
  Diagnostics.Cannot_assign_to_private_method_0_Private_methods_are_not_writable.code,
  Diagnostics.Cannot_redeclare_identifier_0_in_catch_clause.code,
  Diagnostics.Catch_clause_variable_cannot_have_an_initializer.code,
  Diagnostics.Class_decorators_can_t_be_used_with_static_private_identifier_Consider_removing_the_experimental_decorator.code,
  Diagnostics.Classes_can_only_extend_a_single_class.code,
  Diagnostics.Classes_may_not_have_a_field_named_constructor.code,
  Diagnostics.Did_you_mean_to_use_a_Colon_An_can_only_follow_a_property_name_when_the_containing_object_literal_is_part_of_a_destructuring_pattern.code,
  Diagnostics.Duplicate_label_0.code,
  Diagnostics.Dynamic_imports_can_only_accept_a_module_specifier_and_an_optional_set_of_attributes_as_arguments.code,
  Diagnostics.for_await_loops_cannot_be_used_inside_a_class_static_block.code,
  Diagnostics.JSX_attributes_must_only_be_assigned_a_non_empty_expression.code,
  Diagnostics.JSX_elements_cannot_have_multiple_attributes_with_the_same_name.code,
  Diagnostics.JSX_expressions_may_not_use_the_comma_operator_Did_you_mean_to_write_an_array.code,
  Diagnostics.JSX_property_access_expressions_cannot_include_JSX_namespace_names.code,
  Diagnostics.Jump_target_cannot_cross_function_boundary.code,
  Diagnostics.Line_terminator_not_permitted_before_arrow.code,
  Diagnostics.Modifiers_cannot_appear_here.code,
  Diagnostics.Only_a_single_variable_declaration_is_allowed_in_a_for_in_statement.code,
  Diagnostics.Only_a_single_variable_declaration_is_allowed_in_a_for_of_statement.code,
  Diagnostics.Private_identifiers_are_not_allowed_outside_class_bodies.code,
  Diagnostics.Private_identifiers_are_only_allowed_in_class_bodies_and_may_only_be_used_as_part_of_a_class_member_declaration_property_access_or_on_the_left_hand_side_of_an_in_expression.code,
  Diagnostics.Property_0_is_not_accessible_outside_class_1_because_it_has_a_private_identifier.code,
  Diagnostics.Tagged_template_expressions_are_not_permitted_in_an_optional_chain.code,
  Diagnostics.The_left_hand_side_of_a_for_of_statement_may_not_be_async.code,
  Diagnostics.The_variable_declaration_of_a_for_in_statement_cannot_have_an_initializer.code,
  Diagnostics.The_variable_declaration_of_a_for_of_statement_cannot_have_an_initializer.code,
  Diagnostics.Trailing_comma_not_allowed.code,
  Diagnostics.Variable_declaration_list_cannot_be_empty.code,
  Diagnostics._0_and_1_operations_cannot_be_mixed_without_parentheses.code,
  Diagnostics._0_expected.code,
  Diagnostics._0_is_not_a_valid_meta_property_for_keyword_1_Did_you_mean_2.code,
  Diagnostics._0_list_cannot_be_empty.code,
  Diagnostics._0_modifier_already_seen.code,
  Diagnostics._0_modifier_cannot_appear_on_a_constructor_declaration.code,
  Diagnostics._0_modifier_cannot_appear_on_a_module_or_namespace_element.code,
  Diagnostics._0_modifier_cannot_appear_on_a_parameter.code,
  Diagnostics._0_modifier_cannot_appear_on_class_elements_of_this_kind.code,
  Diagnostics._0_modifier_cannot_be_used_here.code,
  Diagnostics._0_modifier_must_precede_1_modifier.code,
  Diagnostics._0_declarations_can_only_be_declared_inside_a_block.code,
  Diagnostics._0_declarations_must_be_initialized.code,
  Diagnostics.extends_clause_already_seen.code,
  Diagnostics.let_is_not_allowed_to_be_used_as_a_name_in_let_or_const_declarations.code,
  Diagnostics.Class_constructor_may_not_be_a_generator.code,
  Diagnostics.Class_constructor_may_not_be_an_accessor.code,
  Diagnostics.await_expressions_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules.code,
  Diagnostics.await_using_statements_are_only_allowed_within_async_functions_and_at_the_top_levels_of_modules.code,
  Diagnostics.Private_field_0_must_be_declared_in_an_enclosing_class.code,
  // Type errors
  Diagnostics.This_condition_will_always_return_0_since_JavaScript_compares_objects_by_reference_not_value.code
]);
function shouldProgramCreateNewSourceFiles(program, newOptions) {
  if (!program) return false;
  return optionsHaveChanges(program.getCompilerOptions(), newOptions, sourceFileAffectingCompilerOptions);
}
function createCreateProgramOptions(rootNames, options, host, oldProgram, configFileParsingDiagnostics, typeScriptVersion2) {
  return {
    rootNames,
    options,
    host,
    oldProgram,
    configFileParsingDiagnostics,
    typeScriptVersion: typeScriptVersion2
  };
}
function createProgram(_rootNamesOrOptions, _options, _host, _oldProgram, _configFileParsingDiagnostics) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
  let _createProgramOptions ***REMOVED*** isArray(_rootNamesOrOptions) ? createCreateProgramOptions(_rootNamesOrOptions, _options, _host, _oldProgram, _configFileParsingDiagnostics) : _rootNamesOrOptions;
  const { rootNames, options, configFileParsingDiagnostics, projectReferences, typeScriptVersion: typeScriptVersion2, host: createProgramOptionsHost } ***REMOVED*** _createProgramOptions;
  let { oldProgram } ***REMOVED*** _createProgramOptions;
  _createProgramOptions ***REMOVED*** void 0;
  _rootNamesOrOptions ***REMOVED*** void 0;
  for (const option of commandLineOptionOfCustomType) {
    if (hasProperty(options, option.name)) {
      if (typeof options[option.name] ***REMOVED******REMOVED******REMOVED*** "string") {
        throw new Error(`${option.name} is a string value; tsconfig JSON must be parsed with parseJsonSourceFileConfigFileContent or getParsedCommandLineOfConfigFile before passing to createProgram`);
      }
    }
  }
  const reportInvalidIgnoreDeprecations ***REMOVED*** memoize(() ***REMOVED***> createOptionValueDiagnostic("ignoreDeprecations", Diagnostics.Invalid_value_for_ignoreDeprecations));
  let processingDefaultLibFiles;
  let processingOtherFiles;
  let files;
  let symlinks;
  let typeChecker;
  let classifiableNames;
  let filesWithReferencesProcessed;
  let cachedBindAndCheckDiagnosticsForFile;
  let cachedDeclarationDiagnosticsForFile;
  const programDiagnostics ***REMOVED*** createProgramDiagnostics(getCompilerOptionsObjectLiteralSyntax);
  let automaticTypeDirectiveNames;
  let automaticTypeDirectiveResolutions;
  let resolvedLibReferences;
  let resolvedLibProcessing;
  let resolvedModules;
  let resolvedModulesProcessing;
  let resolvedTypeReferenceDirectiveNames;
  let resolvedTypeReferenceDirectiveNamesProcessing;
  let packageMap;
  const maxNodeModuleJsDepth ***REMOVED*** typeof options.maxNodeModuleJsDepth ***REMOVED******REMOVED******REMOVED*** "number" ? options.maxNodeModuleJsDepth : 0;
  let currentNodeModulesDepth ***REMOVED*** 0;
  const modulesWithElidedImports ***REMOVED*** /* @__PURE__ */ new Map();
  const sourceFilesFoundSearchingNodeModules ***REMOVED*** /* @__PURE__ */ new Map();
  (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.push(
    tracing.Phase.Program,
    "createProgram",
    { configFilePath: options.configFilePath, rootDir: options.rootDir },
    /*separateBeginAndEnd*/
    true
  );
  mark("beforeProgram");
  const host ***REMOVED*** createProgramOptionsHost || createCompilerHost(options);
  const configParsingHost ***REMOVED*** parseConfigHostFromCompilerHostLike(host);
  let skipDefaultLib ***REMOVED*** options.noLib;
  const getDefaultLibraryFileName ***REMOVED*** memoize(() ***REMOVED***> host.getDefaultLibFileName(options));
  const defaultLibraryPath ***REMOVED*** host.getDefaultLibLocation ? host.getDefaultLibLocation() : getDirectoryPath(getDefaultLibraryFileName());
  let skipVerifyCompilerOptions ***REMOVED*** false;
  const currentDirectory ***REMOVED*** host.getCurrentDirectory();
  const supportedExtensions ***REMOVED*** getSupportedExtensions(options);
  const supportedExtensionsWithJsonIfResolveJsonModule ***REMOVED*** getSupportedExtensionsWithJsonIfResolveJsonModule(options, supportedExtensions);
  const hasEmitBlockingDiagnostics ***REMOVED*** /* @__PURE__ */ new Map();
  let _compilerOptionsObjectLiteralSyntax;
  let _compilerOptionsPropertySyntax;
  let moduleResolutionCache;
  let actualResolveModuleNamesWorker;
  const hasInvalidatedResolutions ***REMOVED*** host.hasInvalidatedResolutions || returnFalse;
  if (host.resolveModuleNameLiterals) {
    actualResolveModuleNamesWorker ***REMOVED*** host.resolveModuleNameLiterals.bind(host);
    moduleResolutionCache ***REMOVED*** (_b ***REMOVED*** host.getModuleResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _b.call(host);
  } else if (host.resolveModuleNames) {
    actualResolveModuleNamesWorker ***REMOVED*** (moduleNames, containingFile, redirectedReference, options2, containingSourceFile, reusedNames) ***REMOVED***> host.resolveModuleNames(
      moduleNames.map(getModuleResolutionName),
      containingFile,
      reusedNames ***REMOVED******REMOVED*** null ? void 0 : reusedNames.map(getModuleResolutionName),
      redirectedReference,
      options2,
      containingSourceFile
    ).map(
      (resolved) ***REMOVED***> resolved ? resolved.extension !***REMOVED******REMOVED*** void 0 ? { resolvedModule: resolved } : (
        // An older host may have omitted extension, in which case we should infer it from the file extension of resolvedFileName.
        { resolvedModule: { ...resolved, extension: extensionFromPath(resolved.resolvedFileName) } }
      ) : emptyResolution
    );
    moduleResolutionCache ***REMOVED*** (_c ***REMOVED*** host.getModuleResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _c.call(host);
  } else {
    moduleResolutionCache ***REMOVED*** createModuleResolutionCache(currentDirectory, getCanonicalFileName, options);
    actualResolveModuleNamesWorker ***REMOVED*** (moduleNames, containingFile, redirectedReference, options2, containingSourceFile) ***REMOVED***> loadWithModeAwareCache(
      moduleNames,
      containingFile,
      redirectedReference,
      options2,
      containingSourceFile,
      host,
      moduleResolutionCache,
      createModuleResolutionLoader
    );
  }
  let actualResolveTypeReferenceDirectiveNamesWorker;
  if (host.resolveTypeReferenceDirectiveReferences) {
    actualResolveTypeReferenceDirectiveNamesWorker ***REMOVED*** host.resolveTypeReferenceDirectiveReferences.bind(host);
  } else if (host.resolveTypeReferenceDirectives) {
    actualResolveTypeReferenceDirectiveNamesWorker ***REMOVED*** (typeDirectiveNames, containingFile, redirectedReference, options2, containingSourceFile) ***REMOVED***> host.resolveTypeReferenceDirectives(
      typeDirectiveNames.map(getTypeReferenceResolutionName),
      containingFile,
      redirectedReference,
      options2,
      containingSourceFile ***REMOVED******REMOVED*** null ? void 0 : containingSourceFile.impliedNodeFormat
    ).map((resolvedTypeReferenceDirective) ***REMOVED***> ({ resolvedTypeReferenceDirective }));
  } else {
    const typeReferenceDirectiveResolutionCache ***REMOVED*** createTypeReferenceDirectiveResolutionCache(
      currentDirectory,
      getCanonicalFileName,
      /*options*/
      void 0,
      moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.getPackageJsonInfoCache(),
      moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.optionsToRedirectsKey
    );
    actualResolveTypeReferenceDirectiveNamesWorker ***REMOVED*** (typeDirectiveNames, containingFile, redirectedReference, options2, containingSourceFile) ***REMOVED***> loadWithModeAwareCache(
      typeDirectiveNames,
      containingFile,
      redirectedReference,
      options2,
      containingSourceFile,
      host,
      typeReferenceDirectiveResolutionCache,
      createTypeReferenceResolutionLoader
    );
  }
  const hasInvalidatedLibResolutions ***REMOVED*** host.hasInvalidatedLibResolutions || returnFalse;
  let actualResolveLibrary;
  if (host.resolveLibrary) {
    actualResolveLibrary ***REMOVED*** host.resolveLibrary.bind(host);
  } else {
    const libraryResolutionCache ***REMOVED*** createModuleResolutionCache(currentDirectory, getCanonicalFileName, options, moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.getPackageJsonInfoCache());
    actualResolveLibrary ***REMOVED*** (libraryName, resolveFrom, options2) ***REMOVED***> resolveLibrary(libraryName, resolveFrom, options2, host, libraryResolutionCache);
  }
  const packageIdToSourceFile ***REMOVED*** /* @__PURE__ */ new Map();
  let sourceFileToPackageName ***REMOVED*** /* @__PURE__ */ new Map();
  let redirectTargetsMap ***REMOVED*** createMultiMap();
  let usesUriStyleNodeCoreModules;
  const filesByName ***REMOVED*** /* @__PURE__ */ new Map();
  let missingFileNames ***REMOVED*** /* @__PURE__ */ new Map();
  const filesByNameIgnoreCase ***REMOVED*** host.useCaseSensitiveFileNames() ? /* @__PURE__ */ new Map() : void 0;
  let resolvedProjectReferences;
  let projectReferenceRedirects;
  let mapSourceFileToResolvedRef;
  let mapOutputFileToResolvedRef;
  const useSourceOfProjectReferenceRedirect ***REMOVED*** !!((_d ***REMOVED*** host.useSourceOfProjectReferenceRedirect) ***REMOVED******REMOVED*** null ? void 0 : _d.call(host)) && !options.disableSourceOfProjectReferenceRedirect;
  const { onProgramCreateComplete, fileExists, directoryExists } ***REMOVED*** updateHostForUseSourceOfProjectReferenceRedirect({
    compilerHost: host,
    getSymlinkCache,
    useSourceOfProjectReferenceRedirect,
    toPath: toPath3,
    getResolvedProjectReferences,
    getRedirectFromOutput,
    forEachResolvedProjectReference: forEachResolvedProjectReference2
  });
  const readFile ***REMOVED*** host.readFile.bind(host);
  (_e ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _e.push(tracing.Phase.Program, "shouldProgramCreateNewSourceFiles", { hasOldProgram: !!oldProgram });
  const shouldCreateNewSourceFile ***REMOVED*** shouldProgramCreateNewSourceFiles(oldProgram, options);
  (_f ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _f.pop();
  let structureIsReused;
  (_g ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _g.push(tracing.Phase.Program, "tryReuseStructureFromOldProgram", {});
  structureIsReused ***REMOVED*** tryReuseStructureFromOldProgram();
  (_h ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _h.pop();
  if (structureIsReused !***REMOVED******REMOVED*** 2 /* Completely */) {
    processingDefaultLibFiles ***REMOVED*** [];
    processingOtherFiles ***REMOVED*** [];
    if (projectReferences) {
      if (!resolvedProjectReferences) {
        resolvedProjectReferences ***REMOVED*** projectReferences.map(parseProjectReferenceConfigFile);
      }
      if (rootNames.length) {
        resolvedProjectReferences ***REMOVED******REMOVED*** null ? void 0 : resolvedProjectReferences.forEach((parsedRef, index) ***REMOVED***> {
          if (!parsedRef) return;
          const out ***REMOVED*** parsedRef.commandLine.options.outFile;
          if (useSourceOfProjectReferenceRedirect) {
            if (out || getEmitModuleKind(parsedRef.commandLine.options) ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
              for (const fileName of parsedRef.commandLine.fileNames) {
                processProjectReferenceFile(fileName, { kind: 1 /* SourceFromProjectReference */, index });
              }
            }
          } else {
            if (out) {
              processProjectReferenceFile(changeExtension(out, ".d.ts"), { kind: 2 /* OutputFromProjectReference */, index });
            } else if (getEmitModuleKind(parsedRef.commandLine.options) ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
              const getCommonSourceDirectory3 ***REMOVED*** memoize(() ***REMOVED***> getCommonSourceDirectoryOfConfig(parsedRef.commandLine, !host.useCaseSensitiveFileNames()));
              for (const fileName of parsedRef.commandLine.fileNames) {
                if (!isDeclarationFileName(fileName) && !fileExtensionIs(fileName, ".json" /* Json */)) {
                  processProjectReferenceFile(getOutputDeclarationFileName(fileName, parsedRef.commandLine, !host.useCaseSensitiveFileNames(), getCommonSourceDirectory3), { kind: 2 /* OutputFromProjectReference */, index });
                }
              }
            }
          }
        });
      }
    }
    (_i ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _i.push(tracing.Phase.Program, "processRootFiles", { count: rootNames.length });
    forEach(rootNames, (name, index) ***REMOVED***> processRootFile(
      name,
      /*isDefaultLib*/
      false,
      /*ignoreNoDefaultLib*/
      false,
      { kind: 0 /* RootFile */, index }
    ));
    (_j ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _j.pop();
    automaticTypeDirectiveNames ?? (automaticTypeDirectiveNames ***REMOVED*** rootNames.length ? getAutomaticTypeDirectiveNames(options, host) : emptyArray);
    automaticTypeDirectiveResolutions ***REMOVED*** createModeAwareCache();
    if (automaticTypeDirectiveNames.length) {
      (_k ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _k.push(tracing.Phase.Program, "processTypeReferences", { count: automaticTypeDirectiveNames.length });
      const containingDirectory ***REMOVED*** options.configFilePath ? getDirectoryPath(options.configFilePath) : currentDirectory;
      const containingFilename ***REMOVED*** combinePaths(containingDirectory, inferredTypesContainingFile);
      const resolutions ***REMOVED*** resolveTypeReferenceDirectiveNamesReusingOldState(automaticTypeDirectiveNames, containingFilename);
      for (let i ***REMOVED*** 0; i < automaticTypeDirectiveNames.length; i++) {
        automaticTypeDirectiveResolutions.set(
          automaticTypeDirectiveNames[i],
          /*mode*/
          void 0,
          resolutions[i]
        );
        processTypeReferenceDirective(
          automaticTypeDirectiveNames[i],
          /*mode*/
          void 0,
          resolutions[i],
          {
            kind: 8 /* AutomaticTypeDirectiveFile */,
            typeReference: automaticTypeDirectiveNames[i],
            packageId: (_m ***REMOVED*** (_l ***REMOVED*** resolutions[i]) ***REMOVED******REMOVED*** null ? void 0 : _l.resolvedTypeReferenceDirective) ***REMOVED******REMOVED*** null ? void 0 : _m.packageId
          }
        );
      }
      (_n ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _n.pop();
    }
    if (rootNames.length && !skipDefaultLib) {
      const defaultLibraryFileName ***REMOVED*** getDefaultLibraryFileName();
      if (!options.lib && defaultLibraryFileName) {
        processRootFile(
          defaultLibraryFileName,
          /*isDefaultLib*/
          true,
          /*ignoreNoDefaultLib*/
          false,
          { kind: 6 /* LibFile */ }
        );
      } else {
        forEach(options.lib, (libFileName, index) ***REMOVED***> {
          processRootFile(
            pathForLibFile(libFileName),
            /*isDefaultLib*/
            true,
            /*ignoreNoDefaultLib*/
            false,
            { kind: 6 /* LibFile */, index }
          );
        });
      }
    }
    files ***REMOVED*** toSorted(processingDefaultLibFiles, compareDefaultLibFiles).concat(processingOtherFiles);
    processingDefaultLibFiles ***REMOVED*** void 0;
    processingOtherFiles ***REMOVED*** void 0;
    filesWithReferencesProcessed ***REMOVED*** void 0;
  }
  if (oldProgram && host.onReleaseOldSourceFile) {
    const oldSourceFiles ***REMOVED*** oldProgram.getSourceFiles();
    for (const oldSourceFile of oldSourceFiles) {
      const newFile ***REMOVED*** getSourceFileByPath(oldSourceFile.resolvedPath);
      if (shouldCreateNewSourceFile || !newFile || newFile.impliedNodeFormat !***REMOVED******REMOVED*** oldSourceFile.impliedNodeFormat || // old file wasn't redirect but new file is
      oldSourceFile.resolvedPath ***REMOVED******REMOVED******REMOVED*** oldSourceFile.path && newFile.resolvedPath !***REMOVED******REMOVED*** oldSourceFile.path) {
        host.onReleaseOldSourceFile(oldSourceFile, oldProgram.getCompilerOptions(), !!getSourceFileByPath(oldSourceFile.path), newFile);
      }
    }
    if (!host.getParsedCommandLine) {
      oldProgram.forEachResolvedProjectReference((resolvedProjectReference) ***REMOVED***> {
        if (!getResolvedProjectReferenceByPath(resolvedProjectReference.sourceFile.path)) {
          host.onReleaseOldSourceFile(
            resolvedProjectReference.sourceFile,
            oldProgram.getCompilerOptions(),
            /*hasSourceFileByPath*/
            false,
            /*newSourceFileByResolvedPath*/
            void 0
          );
        }
      });
    }
  }
  if (oldProgram && host.onReleaseParsedCommandLine) {
    forEachProjectReference(
      oldProgram.getProjectReferences(),
      oldProgram.getResolvedProjectReferences(),
      (oldResolvedRef, parent, index) ***REMOVED***> {
        const oldReference ***REMOVED*** (parent ***REMOVED******REMOVED*** null ? void 0 : parent.commandLine.projectReferences[index]) || oldProgram.getProjectReferences()[index];
        const oldRefPath ***REMOVED*** resolveProjectReferencePath(oldReference);
        if (!(projectReferenceRedirects ***REMOVED******REMOVED*** null ? void 0 : projectReferenceRedirects.has(toPath3(oldRefPath)))) {
          host.onReleaseParsedCommandLine(oldRefPath, oldResolvedRef, oldProgram.getCompilerOptions());
        }
      }
    );
  }
  oldProgram ***REMOVED*** void 0;
  resolvedLibProcessing ***REMOVED*** void 0;
  resolvedModulesProcessing ***REMOVED*** void 0;
  resolvedTypeReferenceDirectiveNamesProcessing ***REMOVED*** void 0;
  const program ***REMOVED*** {
    getRootFileNames: () ***REMOVED***> rootNames,
    getSourceFile,
    getSourceFileByPath,
    getSourceFiles: () ***REMOVED***> files,
    getMissingFilePaths: () ***REMOVED***> missingFileNames,
    getModuleResolutionCache: () ***REMOVED***> moduleResolutionCache,
    getFilesByNameMap: () ***REMOVED***> filesByName,
    getCompilerOptions: () ***REMOVED***> options,
    getSyntacticDiagnostics,
    getOptionsDiagnostics,
    getGlobalDiagnostics,
    getSemanticDiagnostics,
    getCachedSemanticDiagnostics,
    getSuggestionDiagnostics,
    getDeclarationDiagnostics: getDeclarationDiagnostics2,
    getBindAndCheckDiagnostics,
    getProgramDiagnostics,
    getTypeChecker,
    getClassifiableNames,
    getCommonSourceDirectory: getCommonSourceDirectory2,
    emit,
    getCurrentDirectory: () ***REMOVED***> currentDirectory,
    getNodeCount: () ***REMOVED***> getTypeChecker().getNodeCount(),
    getIdentifierCount: () ***REMOVED***> getTypeChecker().getIdentifierCount(),
    getSymbolCount: () ***REMOVED***> getTypeChecker().getSymbolCount(),
    getTypeCount: () ***REMOVED***> getTypeChecker().getTypeCount(),
    getInstantiationCount: () ***REMOVED***> getTypeChecker().getInstantiationCount(),
    getRelationCacheSizes: () ***REMOVED***> getTypeChecker().getRelationCacheSizes(),
    getFileProcessingDiagnostics: () ***REMOVED***> programDiagnostics.getFileProcessingDiagnostics(),
    getAutomaticTypeDirectiveNames: () ***REMOVED***> automaticTypeDirectiveNames,
    getAutomaticTypeDirectiveResolutions: () ***REMOVED***> automaticTypeDirectiveResolutions,
    isSourceFileFromExternalLibrary,
    isSourceFileDefaultLibrary,
    getModeForUsageLocation: getModeForUsageLocation2,
    getEmitSyntaxForUsageLocation,
    getModeForResolutionAtIndex,
    getSourceFileFromReference,
    getLibFileFromReference,
    sourceFileToPackageName,
    redirectTargetsMap,
    usesUriStyleNodeCoreModules,
    resolvedModules,
    resolvedTypeReferenceDirectiveNames,
    resolvedLibReferences,
    getProgramDiagnosticsContainer: () ***REMOVED***> programDiagnostics,
    getResolvedModule,
    getResolvedModuleFromModuleSpecifier,
    getResolvedTypeReferenceDirective,
    getResolvedTypeReferenceDirectiveFromTypeReferenceDirective,
    forEachResolvedModule,
    forEachResolvedTypeReferenceDirective,
    getCurrentPackagesMap: () ***REMOVED***> packageMap,
    typesPackageExists,
    packageBundlesTypes,
    isEmittedFile,
    getConfigFileParsingDiagnostics: getConfigFileParsingDiagnostics2,
    getProjectReferences,
    getResolvedProjectReferences,
    getRedirectFromSourceFile,
    getResolvedProjectReferenceByPath,
    forEachResolvedProjectReference: forEachResolvedProjectReference2,
    isSourceOfProjectReferenceRedirect,
    getRedirectFromOutput,
    getCompilerOptionsForFile,
    getDefaultResolutionModeForFile: getDefaultResolutionModeForFile2,
    getEmitModuleFormatOfFile,
    getImpliedNodeFormatForEmit,
    shouldTransformImportCall,
    emitBuildInfo,
    fileExists,
    readFile,
    directoryExists,
    getSymlinkCache,
    realpath: (_o ***REMOVED*** host.realpath) ***REMOVED******REMOVED*** null ? void 0 : _o.bind(host),
    useCaseSensitiveFileNames: () ***REMOVED***> host.useCaseSensitiveFileNames(),
    getCanonicalFileName,
    getFileIncludeReasons: () ***REMOVED***> programDiagnostics.getFileReasons(),
    structureIsReused,
    writeFile: writeFile2,
    getGlobalTypingsCacheLocation: maybeBind(host, host.getGlobalTypingsCacheLocation)
  };
  onProgramCreateComplete();
  if (!skipVerifyCompilerOptions) {
    verifyCompilerOptions();
  }
  mark("afterProgram");
  measure("Program", "beforeProgram", "afterProgram");
  (_p ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _p.pop();
  return program;
  function getResolvedModule(file, moduleName, mode) {
    var _a2;
    return (_a2 ***REMOVED*** resolvedModules ***REMOVED******REMOVED*** null ? void 0 : resolvedModules.get(file.path)) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(moduleName, mode);
  }
  function getResolvedModuleFromModuleSpecifier(moduleSpecifier, sourceFile) {
    sourceFile ?? (sourceFile ***REMOVED*** getSourceFileOfNode(moduleSpecifier));
    Debug.assertIsDefined(sourceFile, "`moduleSpecifier` must have a `SourceFile` ancestor. Use `program.getResolvedModule` instead to provide the containing file and resolution mode.");
    return getResolvedModule(sourceFile, moduleSpecifier.text, getModeForUsageLocation2(sourceFile, moduleSpecifier));
  }
  function getResolvedTypeReferenceDirective(file, typeDirectiveName, mode) {
    var _a2;
    return (_a2 ***REMOVED*** resolvedTypeReferenceDirectiveNames ***REMOVED******REMOVED*** null ? void 0 : resolvedTypeReferenceDirectiveNames.get(file.path)) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(typeDirectiveName, mode);
  }
  function getResolvedTypeReferenceDirectiveFromTypeReferenceDirective(typeRef, sourceFile) {
    return getResolvedTypeReferenceDirective(
      sourceFile,
      typeRef.fileName,
      getModeForTypeReferenceDirectiveInFile(typeRef, sourceFile)
    );
  }
  function forEachResolvedModule(callback, file) {
    forEachResolution(resolvedModules, callback, file);
  }
  function forEachResolvedTypeReferenceDirective(callback, file) {
    forEachResolution(resolvedTypeReferenceDirectiveNames, callback, file);
  }
  function forEachResolution(resolutionCache, callback, file) {
    var _a2;
    if (file) (_a2 ***REMOVED*** resolutionCache ***REMOVED******REMOVED*** null ? void 0 : resolutionCache.get(file.path)) ***REMOVED******REMOVED*** null ? void 0 : _a2.forEach((resolution, name, mode) ***REMOVED***> callback(resolution, name, mode, file.path));
    else resolutionCache ***REMOVED******REMOVED*** null ? void 0 : resolutionCache.forEach((resolutions, filePath) ***REMOVED***> resolutions.forEach((resolution, name, mode) ***REMOVED***> callback(resolution, name, mode, filePath)));
  }
  function getPackagesMap() {
    if (packageMap) return packageMap;
    packageMap ***REMOVED*** /* @__PURE__ */ new Map();
    forEachResolvedModule(({ resolvedModule }) ***REMOVED***> {
      if (resolvedModule ***REMOVED******REMOVED*** null ? void 0 : resolvedModule.packageId) packageMap.set(resolvedModule.packageId.name, resolvedModule.extension ***REMOVED******REMOVED******REMOVED*** ".d.ts" /* Dts */ || !!packageMap.get(resolvedModule.packageId.name));
    });
    return packageMap;
  }
  function typesPackageExists(packageName) {
    return getPackagesMap().has(getTypesPackageName(packageName));
  }
  function packageBundlesTypes(packageName) {
    return !!getPackagesMap().get(packageName);
  }
  function addResolutionDiagnostics(resolution) {
    var _a2;
    if (!((_a2 ***REMOVED*** resolution.resolutionDiagnostics) ***REMOVED******REMOVED*** null ? void 0 : _a2.length)) return;
    programDiagnostics.addFileProcessingDiagnostic({
      kind: 2 /* ResolutionDiagnostics */,
      diagnostics: resolution.resolutionDiagnostics
    });
  }
  function addResolutionDiagnosticsFromResolutionOrCache(containingFile, name, resolution, mode) {
    if (host.resolveModuleNameLiterals || !host.resolveModuleNames) return addResolutionDiagnostics(resolution);
    if (!moduleResolutionCache || isExternalModuleNameRelative(name)) return;
    const containingFileName ***REMOVED*** getNormalizedAbsolutePath(containingFile.originalFileName, currentDirectory);
    const containingDir ***REMOVED*** getDirectoryPath(containingFileName);
    const redirectedReference ***REMOVED*** getRedirectReferenceForResolution(containingFile);
    const fromCache ***REMOVED*** moduleResolutionCache.getFromNonRelativeNameCache(name, mode, containingDir, redirectedReference);
    if (fromCache) addResolutionDiagnostics(fromCache);
  }
  function resolveModuleNamesWorker(moduleNames, containingFile, reusedNames) {
    var _a2, _b2;
    const containingFileName ***REMOVED*** getNormalizedAbsolutePath(containingFile.originalFileName, currentDirectory);
    const redirectedReference ***REMOVED*** getRedirectReferenceForResolution(containingFile);
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(tracing.Phase.Program, "resolveModuleNamesWorker", { containingFileName });
    mark("beforeResolveModule");
    const result ***REMOVED*** actualResolveModuleNamesWorker(
      moduleNames,
      containingFileName,
      redirectedReference,
      options,
      containingFile,
      reusedNames
    );
    mark("afterResolveModule");
    measure("ResolveModule", "beforeResolveModule", "afterResolveModule");
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
    return result;
  }
  function resolveTypeReferenceDirectiveNamesWorker(typeDirectiveNames, containingFile, reusedNames) {
    var _a2, _b2;
    const containingSourceFile ***REMOVED*** !isString(containingFile) ? containingFile : void 0;
    const containingFileName ***REMOVED*** !isString(containingFile) ? getNormalizedAbsolutePath(containingFile.originalFileName, currentDirectory) : containingFile;
    const redirectedReference ***REMOVED*** containingSourceFile && getRedirectReferenceForResolution(containingSourceFile);
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(tracing.Phase.Program, "resolveTypeReferenceDirectiveNamesWorker", { containingFileName });
    mark("beforeResolveTypeReference");
    const result ***REMOVED*** actualResolveTypeReferenceDirectiveNamesWorker(
      typeDirectiveNames,
      containingFileName,
      redirectedReference,
      options,
      containingSourceFile,
      reusedNames
    );
    mark("afterResolveTypeReference");
    measure("ResolveTypeReference", "beforeResolveTypeReference", "afterResolveTypeReference");
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
    return result;
  }
  function getRedirectReferenceForResolution(file) {
    var _a2, _b2;
    const redirect ***REMOVED*** getRedirectFromSourceFile(file.originalFileName);
    if (redirect || !isDeclarationFileName(file.originalFileName)) return redirect ***REMOVED******REMOVED*** null ? void 0 : redirect.resolvedRef;
    const resultFromDts ***REMOVED*** (_a2 ***REMOVED*** getRedirectFromOutput(file.path)) ***REMOVED******REMOVED*** null ? void 0 : _a2.resolvedRef;
    if (resultFromDts) return resultFromDts;
    if (!host.realpath || !options.preserveSymlinks || !file.originalFileName.includes(nodeModulesPathPart)) return void 0;
    const realDeclarationPath ***REMOVED*** toPath3(host.realpath(file.originalFileName));
    return realDeclarationPath ***REMOVED******REMOVED******REMOVED*** file.path ? void 0 : (_b2 ***REMOVED*** getRedirectFromOutput(realDeclarationPath)) ***REMOVED******REMOVED*** null ? void 0 : _b2.resolvedRef;
  }
  function compareDefaultLibFiles(a, b) {
    return compareValues(getDefaultLibFilePriority(a), getDefaultLibFilePriority(b));
  }
  function getDefaultLibFilePriority(a) {
    if (containsPath(
      defaultLibraryPath,
      a.fileName,
      /*ignoreCase*/
      false
    )) {
      const basename ***REMOVED*** getBaseFileName(a.fileName);
      if (basename ***REMOVED******REMOVED******REMOVED*** "lib.d.ts" || basename ***REMOVED******REMOVED******REMOVED*** "lib.es6.d.ts") return 0;
      const name ***REMOVED*** removeSuffix(removePrefix(basename, "lib."), ".d.ts");
      const index ***REMOVED*** libs.indexOf(name);
      if (index !***REMOVED******REMOVED*** -1) return index + 1;
    }
    return libs.length + 2;
  }
  function toPath3(fileName) {
    return toPath(fileName, currentDirectory, getCanonicalFileName);
  }
  function getCommonSourceDirectory2() {
    let commonSourceDirectory ***REMOVED*** programDiagnostics.getCommonSourceDirectory();
    if (commonSourceDirectory !***REMOVED******REMOVED*** void 0) {
      return commonSourceDirectory;
    }
    const emittedFiles ***REMOVED*** filter(files, (file) ***REMOVED***> sourceFileMayBeEmitted(file, program));
    commonSourceDirectory ***REMOVED*** getCommonSourceDirectory(
      options,
      () ***REMOVED***> mapDefined(emittedFiles, (file) ***REMOVED***> file.isDeclarationFile ? void 0 : file.fileName),
      currentDirectory,
      getCanonicalFileName,
      (commonSourceDirectory2) ***REMOVED***> checkSourceFilesBelongToPath(emittedFiles, commonSourceDirectory2)
    );
    programDiagnostics.setCommonSourceDirectory(commonSourceDirectory);
    return commonSourceDirectory;
  }
  function getClassifiableNames() {
    var _a2;
    if (!classifiableNames) {
      getTypeChecker();
      classifiableNames ***REMOVED*** /* @__PURE__ */ new Set();
      for (const sourceFile of files) {
        (_a2 ***REMOVED*** sourceFile.classifiableNames) ***REMOVED******REMOVED*** null ? void 0 : _a2.forEach((value) ***REMOVED***> classifiableNames.add(value));
      }
    }
    return classifiableNames;
  }
  function resolveModuleNamesReusingOldState(moduleNames, containingFile) {
    return resolveNamesReusingOldState({
      entries: moduleNames,
      containingFile,
      containingSourceFile: containingFile,
      redirectedReference: getRedirectReferenceForResolution(containingFile),
      nameAndModeGetter: moduleResolutionNameAndModeGetter,
      resolutionWorker: resolveModuleNamesWorker,
      getResolutionFromOldProgram: (name, mode) ***REMOVED***> oldProgram ***REMOVED******REMOVED*** null ? void 0 : oldProgram.getResolvedModule(containingFile, name, mode),
      getResolved: getResolvedModuleFromResolution,
      canReuseResolutionsInFile: () ***REMOVED***> containingFile ***REMOVED******REMOVED******REMOVED*** (oldProgram ***REMOVED******REMOVED*** null ? void 0 : oldProgram.getSourceFile(containingFile.fileName)) && !hasInvalidatedResolutions(containingFile.path),
      resolveToOwnAmbientModule: true
    });
  }
  function resolveTypeReferenceDirectiveNamesReusingOldState(typeDirectiveNames, containingFile) {
    const containingSourceFile ***REMOVED*** !isString(containingFile) ? containingFile : void 0;
    return resolveNamesReusingOldState({
      entries: typeDirectiveNames,
      containingFile,
      containingSourceFile,
      redirectedReference: containingSourceFile && getRedirectReferenceForResolution(containingSourceFile),
      nameAndModeGetter: typeReferenceResolutionNameAndModeGetter,
      resolutionWorker: resolveTypeReferenceDirectiveNamesWorker,
      getResolutionFromOldProgram: (name, mode) ***REMOVED***> {
        var _a2;
        return containingSourceFile ? oldProgram ***REMOVED******REMOVED*** null ? void 0 : oldProgram.getResolvedTypeReferenceDirective(containingSourceFile, name, mode) : (_a2 ***REMOVED*** oldProgram ***REMOVED******REMOVED*** null ? void 0 : oldProgram.getAutomaticTypeDirectiveResolutions()) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(name, mode);
      },
      getResolved: getResolvedTypeReferenceDirectiveFromResolution,
      canReuseResolutionsInFile: () ***REMOVED***> containingSourceFile ? containingSourceFile ***REMOVED******REMOVED******REMOVED*** (oldProgram ***REMOVED******REMOVED*** null ? void 0 : oldProgram.getSourceFile(containingSourceFile.fileName)) && !hasInvalidatedResolutions(containingSourceFile.path) : !hasInvalidatedResolutions(toPath3(containingFile))
    });
  }
  function resolveNamesReusingOldState({
    entries,
    containingFile,
    containingSourceFile,
    redirectedReference,
    nameAndModeGetter,
    resolutionWorker,
    getResolutionFromOldProgram,
    getResolved,
    canReuseResolutionsInFile,
    resolveToOwnAmbientModule
  }) {
    if (!entries.length) return emptyArray;
    if (structureIsReused ***REMOVED******REMOVED******REMOVED*** 0 /* Not */ && (!resolveToOwnAmbientModule || !containingSourceFile.ambientModuleNames.length)) {
      return resolutionWorker(
        entries,
        containingFile,
        /*reusedNames*/
        void 0
      );
    }
    let unknownEntries;
    let unknownEntryIndices;
    let result;
    let reusedNames;
    const reuseResolutions ***REMOVED*** canReuseResolutionsInFile();
    for (let i ***REMOVED*** 0; i < entries.length; i++) {
      const entry ***REMOVED*** entries[i];
      if (reuseResolutions) {
        const name ***REMOVED*** nameAndModeGetter.getName(entry);
        const mode ***REMOVED*** nameAndModeGetter.getMode(entry, containingSourceFile, (redirectedReference ***REMOVED******REMOVED*** null ? void 0 : redirectedReference.commandLine.options) ?? options);
        const oldResolution ***REMOVED*** getResolutionFromOldProgram(name, mode);
        const oldResolved ***REMOVED*** oldResolution && getResolved(oldResolution);
        if (oldResolved) {
          if (isTraceEnabled(options, host)) {
            trace(
              host,
              resolutionWorker ***REMOVED******REMOVED******REMOVED*** resolveModuleNamesWorker ? oldResolved.packageId ? Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2 : oldResolved.packageId ? Diagnostics.Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2,
              name,
              containingSourceFile ? getNormalizedAbsolutePath(containingSourceFile.originalFileName, currentDirectory) : containingFile,
              oldResolved.resolvedFileName,
              oldResolved.packageId && packageIdToString(oldResolved.packageId)
            );
          }
          (result ?? (result ***REMOVED*** new Array(entries.length)))[i] ***REMOVED*** oldResolution;
          (reusedNames ?? (reusedNames ***REMOVED*** [])).push(entry);
          continue;
        }
      }
      if (resolveToOwnAmbientModule) {
        const name ***REMOVED*** nameAndModeGetter.getName(entry);
        if (contains(containingSourceFile.ambientModuleNames, name)) {
          if (isTraceEnabled(options, host)) {
            trace(
              host,
              Diagnostics.Module_0_was_resolved_as_locally_declared_ambient_module_in_file_1,
              name,
              getNormalizedAbsolutePath(containingSourceFile.originalFileName, currentDirectory)
            );
          }
          (result ?? (result ***REMOVED*** new Array(entries.length)))[i] ***REMOVED*** emptyResolution;
          continue;
        }
      }
      (unknownEntries ?? (unknownEntries ***REMOVED*** [])).push(entry);
      (unknownEntryIndices ?? (unknownEntryIndices ***REMOVED*** [])).push(i);
    }
    if (!unknownEntries) return result;
    const resolutions ***REMOVED*** resolutionWorker(unknownEntries, containingFile, reusedNames);
    if (!result) return resolutions;
    resolutions.forEach((resolution, index) ***REMOVED***> result[unknownEntryIndices[index]] ***REMOVED*** resolution);
    return result;
  }
  function canReuseProjectReferences() {
    return !forEachProjectReference(
      oldProgram.getProjectReferences(),
      oldProgram.getResolvedProjectReferences(),
      (oldResolvedRef, parent, index) ***REMOVED***> {
        const newRef ***REMOVED*** (parent ? parent.commandLine.projectReferences : projectReferences)[index];
        const newResolvedRef ***REMOVED*** parseProjectReferenceConfigFile(newRef);
        if (oldResolvedRef) {
          return !newResolvedRef || newResolvedRef.sourceFile !***REMOVED******REMOVED*** oldResolvedRef.sourceFile || !arrayIsEqualTo(oldResolvedRef.commandLine.fileNames, newResolvedRef.commandLine.fileNames);
        } else {
          return newResolvedRef !***REMOVED******REMOVED*** void 0;
        }
      },
      (oldProjectReferences, parent) ***REMOVED***> {
        const newReferences ***REMOVED*** parent ? getResolvedProjectReferenceByPath(parent.sourceFile.path).commandLine.projectReferences : projectReferences;
        return !arrayIsEqualTo(oldProjectReferences, newReferences, projectReferenceIsEqualTo);
      }
    );
  }
  function tryReuseStructureFromOldProgram() {
    var _a2;
    if (!oldProgram) {
      return 0 /* Not */;
    }
    const oldOptions ***REMOVED*** oldProgram.getCompilerOptions();
    if (changesAffectModuleResolution(oldOptions, options)) {
      return 0 /* Not */;
    }
    const oldRootNames ***REMOVED*** oldProgram.getRootFileNames();
    if (!arrayIsEqualTo(oldRootNames, rootNames)) {
      return 0 /* Not */;
    }
    if (!canReuseProjectReferences()) {
      return 0 /* Not */;
    }
    if (projectReferences) {
      resolvedProjectReferences ***REMOVED*** projectReferences.map(parseProjectReferenceConfigFile);
    }
    const newSourceFiles ***REMOVED*** [];
    const modifiedSourceFiles ***REMOVED*** [];
    structureIsReused ***REMOVED*** 2 /* Completely */;
    if (forEachEntry(oldProgram.getMissingFilePaths(), (missingFileName) ***REMOVED***> host.fileExists(missingFileName))) {
      return 0 /* Not */;
    }
    const oldSourceFiles ***REMOVED*** oldProgram.getSourceFiles();
    let SeenPackageName;
    ((SeenPackageName2) ***REMOVED***> {
      SeenPackageName2[SeenPackageName2["Exists"] ***REMOVED*** 0] ***REMOVED*** "Exists";
      SeenPackageName2[SeenPackageName2["Modified"] ***REMOVED*** 1] ***REMOVED*** "Modified";
    })(SeenPackageName || (SeenPackageName ***REMOVED*** {}));
    const seenPackageNames ***REMOVED*** /* @__PURE__ */ new Map();
    for (const oldSourceFile of oldSourceFiles) {
      const sourceFileOptions ***REMOVED*** getCreateSourceFileOptions(oldSourceFile.fileName, moduleResolutionCache, host, options);
      let newSourceFile ***REMOVED*** host.getSourceFileByPath ? host.getSourceFileByPath(
        oldSourceFile.fileName,
        oldSourceFile.resolvedPath,
        sourceFileOptions,
        /*onError*/
        void 0,
        shouldCreateNewSourceFile
      ) : host.getSourceFile(
        oldSourceFile.fileName,
        sourceFileOptions,
        /*onError*/
        void 0,
        shouldCreateNewSourceFile
      );
      if (!newSourceFile) {
        return 0 /* Not */;
      }
      newSourceFile.packageJsonLocations ***REMOVED*** ((_a2 ***REMOVED*** sourceFileOptions.packageJsonLocations) ***REMOVED******REMOVED*** null ? void 0 : _a2.length) ? sourceFileOptions.packageJsonLocations : void 0;
      newSourceFile.packageJsonScope ***REMOVED*** sourceFileOptions.packageJsonScope;
      Debug.assert(!newSourceFile.redirectInfo, "Host should not return a redirect source file from `getSourceFile`");
      let fileChanged;
      if (oldSourceFile.redirectInfo) {
        if (newSourceFile !***REMOVED******REMOVED*** oldSourceFile.redirectInfo.unredirected) {
          return 0 /* Not */;
        }
        fileChanged ***REMOVED*** false;
        newSourceFile ***REMOVED*** oldSourceFile;
      } else if (oldProgram.redirectTargetsMap.has(oldSourceFile.path)) {
        if (newSourceFile !***REMOVED******REMOVED*** oldSourceFile) {
          return 0 /* Not */;
        }
        fileChanged ***REMOVED*** false;
      } else {
        fileChanged ***REMOVED*** newSourceFile !***REMOVED******REMOVED*** oldSourceFile;
      }
      newSourceFile.path ***REMOVED*** oldSourceFile.path;
      newSourceFile.originalFileName ***REMOVED*** oldSourceFile.originalFileName;
      newSourceFile.resolvedPath ***REMOVED*** oldSourceFile.resolvedPath;
      newSourceFile.fileName ***REMOVED*** oldSourceFile.fileName;
      const packageName ***REMOVED*** oldProgram.sourceFileToPackageName.get(oldSourceFile.path);
      if (packageName !***REMOVED******REMOVED*** void 0) {
        const prevKind ***REMOVED*** seenPackageNames.get(packageName);
        const newKind ***REMOVED*** fileChanged ? 1 /* Modified */ : 0 /* Exists */;
        if (prevKind !***REMOVED******REMOVED*** void 0 && newKind ***REMOVED******REMOVED******REMOVED*** 1 /* Modified */ || prevKind ***REMOVED******REMOVED******REMOVED*** 1 /* Modified */) {
          return 0 /* Not */;
        }
        seenPackageNames.set(packageName, newKind);
      }
      if (fileChanged) {
        if (oldSourceFile.impliedNodeFormat !***REMOVED******REMOVED*** newSourceFile.impliedNodeFormat) {
          structureIsReused ***REMOVED*** 1 /* SafeModules */;
        } else if (!arrayIsEqualTo(oldSourceFile.libReferenceDirectives, newSourceFile.libReferenceDirectives, fileReferenceIsEqualTo)) {
          structureIsReused ***REMOVED*** 1 /* SafeModules */;
        } else if (oldSourceFile.hasNoDefaultLib !***REMOVED******REMOVED*** newSourceFile.hasNoDefaultLib) {
          structureIsReused ***REMOVED*** 1 /* SafeModules */;
        } else if (!arrayIsEqualTo(oldSourceFile.referencedFiles, newSourceFile.referencedFiles, fileReferenceIsEqualTo)) {
          structureIsReused ***REMOVED*** 1 /* SafeModules */;
        } else {
          collectExternalModuleReferences(newSourceFile);
          if (!arrayIsEqualTo(oldSourceFile.imports, newSourceFile.imports, moduleNameIsEqualTo)) {
            structureIsReused ***REMOVED*** 1 /* SafeModules */;
          } else if (!arrayIsEqualTo(oldSourceFile.moduleAugmentations, newSourceFile.moduleAugmentations, moduleNameIsEqualTo)) {
            structureIsReused ***REMOVED*** 1 /* SafeModules */;
          } else if ((oldSourceFile.flags & 12582912 /* PermanentlySetIncrementalFlags */) !***REMOVED******REMOVED*** (newSourceFile.flags & 12582912 /* PermanentlySetIncrementalFlags */)) {
            structureIsReused ***REMOVED*** 1 /* SafeModules */;
          } else if (!arrayIsEqualTo(oldSourceFile.typeReferenceDirectives, newSourceFile.typeReferenceDirectives, fileReferenceIsEqualTo)) {
            structureIsReused ***REMOVED*** 1 /* SafeModules */;
          }
        }
        modifiedSourceFiles.push(newSourceFile);
      } else if (hasInvalidatedResolutions(oldSourceFile.path)) {
        structureIsReused ***REMOVED*** 1 /* SafeModules */;
        modifiedSourceFiles.push(newSourceFile);
      }
      newSourceFiles.push(newSourceFile);
    }
    if (structureIsReused !***REMOVED******REMOVED*** 2 /* Completely */) {
      return structureIsReused;
    }
    for (const newSourceFile of modifiedSourceFiles) {
      const moduleNames ***REMOVED*** getModuleNames(newSourceFile);
      const resolutions ***REMOVED*** resolveModuleNamesReusingOldState(moduleNames, newSourceFile);
      (resolvedModulesProcessing ?? (resolvedModulesProcessing ***REMOVED*** /* @__PURE__ */ new Map())).set(newSourceFile.path, resolutions);
      const optionsForFile ***REMOVED*** getCompilerOptionsForFile(newSourceFile);
      const resolutionsChanged ***REMOVED*** hasChangesInResolutions(
        moduleNames,
        resolutions,
        (name) ***REMOVED***> oldProgram.getResolvedModule(newSourceFile, name.text, getModeForUsageLocationWorker(newSourceFile, name, optionsForFile)),
        moduleResolutionIsEqualTo
      );
      if (resolutionsChanged) structureIsReused ***REMOVED*** 1 /* SafeModules */;
      const typesReferenceDirectives ***REMOVED*** newSourceFile.typeReferenceDirectives;
      const typeReferenceResolutions ***REMOVED*** resolveTypeReferenceDirectiveNamesReusingOldState(typesReferenceDirectives, newSourceFile);
      (resolvedTypeReferenceDirectiveNamesProcessing ?? (resolvedTypeReferenceDirectiveNamesProcessing ***REMOVED*** /* @__PURE__ */ new Map())).set(newSourceFile.path, typeReferenceResolutions);
      const typeReferenceResolutionsChanged ***REMOVED*** hasChangesInResolutions(
        typesReferenceDirectives,
        typeReferenceResolutions,
        (name) ***REMOVED***> oldProgram.getResolvedTypeReferenceDirective(
          newSourceFile,
          getTypeReferenceResolutionName(name),
          getModeForTypeReferenceDirectiveInFile(name, newSourceFile)
        ),
        typeDirectiveIsEqualTo
      );
      if (typeReferenceResolutionsChanged) structureIsReused ***REMOVED*** 1 /* SafeModules */;
    }
    if (structureIsReused !***REMOVED******REMOVED*** 2 /* Completely */) {
      return structureIsReused;
    }
    if (changesAffectingProgramStructure(oldOptions, options)) {
      return 1 /* SafeModules */;
    }
    if (oldProgram.resolvedLibReferences && forEachEntry(oldProgram.resolvedLibReferences, (resolution, libFileName) ***REMOVED***> pathForLibFileWorker(libFileName).actual !***REMOVED******REMOVED*** resolution.actual)) {
      return 1 /* SafeModules */;
    }
    if (host.hasChangedAutomaticTypeDirectiveNames) {
      if (host.hasChangedAutomaticTypeDirectiveNames()) return 1 /* SafeModules */;
    } else {
      automaticTypeDirectiveNames ***REMOVED*** getAutomaticTypeDirectiveNames(options, host);
      if (!arrayIsEqualTo(oldProgram.getAutomaticTypeDirectiveNames(), automaticTypeDirectiveNames)) return 1 /* SafeModules */;
    }
    missingFileNames ***REMOVED*** oldProgram.getMissingFilePaths();
    Debug.assert(newSourceFiles.length ***REMOVED******REMOVED******REMOVED*** oldProgram.getSourceFiles().length);
    for (const newSourceFile of newSourceFiles) {
      filesByName.set(newSourceFile.path, newSourceFile);
    }
    const oldFilesByNameMap ***REMOVED*** oldProgram.getFilesByNameMap();
    oldFilesByNameMap.forEach((oldFile, path) ***REMOVED***> {
      if (!oldFile) {
        filesByName.set(path, oldFile);
        return;
      }
      if (oldFile.path ***REMOVED******REMOVED******REMOVED*** path) {
        if (oldProgram.isSourceFileFromExternalLibrary(oldFile)) {
          sourceFilesFoundSearchingNodeModules.set(oldFile.path, true);
        }
        return;
      }
      filesByName.set(path, filesByName.get(oldFile.path));
    });
    const isConfigIdentical ***REMOVED*** oldOptions.configFile && oldOptions.configFile ***REMOVED******REMOVED******REMOVED*** options.configFile || !oldOptions.configFile && !options.configFile && !optionsHaveChanges(oldOptions, options, optionDeclarations);
    programDiagnostics.reuseStateFromOldProgram(oldProgram.getProgramDiagnosticsContainer(), isConfigIdentical);
    skipVerifyCompilerOptions ***REMOVED*** isConfigIdentical;
    files ***REMOVED*** newSourceFiles;
    automaticTypeDirectiveNames ***REMOVED*** oldProgram.getAutomaticTypeDirectiveNames();
    automaticTypeDirectiveResolutions ***REMOVED*** oldProgram.getAutomaticTypeDirectiveResolutions();
    sourceFileToPackageName ***REMOVED*** oldProgram.sourceFileToPackageName;
    redirectTargetsMap ***REMOVED*** oldProgram.redirectTargetsMap;
    usesUriStyleNodeCoreModules ***REMOVED*** oldProgram.usesUriStyleNodeCoreModules;
    resolvedModules ***REMOVED*** oldProgram.resolvedModules;
    resolvedTypeReferenceDirectiveNames ***REMOVED*** oldProgram.resolvedTypeReferenceDirectiveNames;
    resolvedLibReferences ***REMOVED*** oldProgram.resolvedLibReferences;
    packageMap ***REMOVED*** oldProgram.getCurrentPackagesMap();
    return 2 /* Completely */;
  }
  function getEmitHost(writeFileCallback) {
    return {
      getCanonicalFileName,
      getCommonSourceDirectory: program.getCommonSourceDirectory,
      getCompilerOptions: program.getCompilerOptions,
      getCurrentDirectory: () ***REMOVED***> currentDirectory,
      getSourceFile: program.getSourceFile,
      getSourceFileByPath: program.getSourceFileByPath,
      getSourceFiles: program.getSourceFiles,
      isSourceFileFromExternalLibrary,
      getRedirectFromSourceFile,
      isSourceOfProjectReferenceRedirect,
      getSymlinkCache,
      writeFile: writeFileCallback || writeFile2,
      isEmitBlocked,
      shouldTransformImportCall,
      getEmitModuleFormatOfFile,
      getDefaultResolutionModeForFile: getDefaultResolutionModeForFile2,
      getModeForResolutionAtIndex,
      readFile: (f) ***REMOVED***> host.readFile(f),
      fileExists: (f) ***REMOVED***> {
        const path ***REMOVED*** toPath3(f);
        if (getSourceFileByPath(path)) return true;
        if (missingFileNames.has(path)) return false;
        return host.fileExists(f);
      },
      realpath: maybeBind(host, host.realpath),
      useCaseSensitiveFileNames: () ***REMOVED***> host.useCaseSensitiveFileNames(),
      getBuildInfo: () ***REMOVED***> {
        var _a2;
        return (_a2 ***REMOVED*** program.getBuildInfo) ***REMOVED******REMOVED*** null ? void 0 : _a2.call(program);
      },
      getSourceFileFromReference: (file, ref) ***REMOVED***> program.getSourceFileFromReference(file, ref),
      redirectTargetsMap,
      getFileIncludeReasons: program.getFileIncludeReasons,
      createHash: maybeBind(host, host.createHash),
      getModuleResolutionCache: () ***REMOVED***> program.getModuleResolutionCache(),
      trace: maybeBind(host, host.trace),
      getGlobalTypingsCacheLocation: program.getGlobalTypingsCacheLocation
    };
  }
  function writeFile2(fileName, text, writeByteOrderMark, onError, sourceFiles, data) {
    host.writeFile(fileName, text, writeByteOrderMark, onError, sourceFiles, data);
  }
  function emitBuildInfo(writeFileCallback) {
    var _a2, _b2;
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(
      tracing.Phase.Emit,
      "emitBuildInfo",
      {},
      /*separateBeginAndEnd*/
      true
    );
    mark("beforeEmit");
    const emitResult ***REMOVED*** emitFiles(
      notImplementedResolver,
      getEmitHost(writeFileCallback),
      /*targetSourceFile*/
      void 0,
      /*transformers*/
      noTransformers,
      /*emitOnly*/
      false,
      /*onlyBuildInfo*/
      true
    );
    mark("afterEmit");
    measure("Emit", "beforeEmit", "afterEmit");
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
    return emitResult;
  }
  function getResolvedProjectReferences() {
    return resolvedProjectReferences;
  }
  function getProjectReferences() {
    return projectReferences;
  }
  function isSourceFileFromExternalLibrary(file) {
    return !!sourceFilesFoundSearchingNodeModules.get(file.path);
  }
  function isSourceFileDefaultLibrary(file) {
    if (!file.isDeclarationFile) {
      return false;
    }
    if (file.hasNoDefaultLib) {
      return true;
    }
    if (options.noLib) {
      return false;
    }
    const equalityComparer ***REMOVED*** host.useCaseSensitiveFileNames() ? equateStringsCaseSensitive : equateStringsCaseInsensitive;
    if (!options.lib) {
      return equalityComparer(file.fileName, getDefaultLibraryFileName());
    } else {
      return some(options.lib, (libFileName) ***REMOVED***> {
        const resolvedLib ***REMOVED*** resolvedLibReferences.get(libFileName);
        return !!resolvedLib && equalityComparer(file.fileName, resolvedLib.actual);
      });
    }
  }
  function getTypeChecker() {
    return typeChecker || (typeChecker ***REMOVED*** createTypeChecker(program));
  }
  function emit(sourceFile, writeFileCallback, cancellationToken, emitOnly, transformers, forceDtsEmit, skipBuildInfo) {
    var _a2, _b2;
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(
      tracing.Phase.Emit,
      "emit",
      { path: sourceFile ***REMOVED******REMOVED*** null ? void 0 : sourceFile.path },
      /*separateBeginAndEnd*/
      true
    );
    const result ***REMOVED*** runWithCancellationToken(
      () ***REMOVED***> emitWorker(
        program,
        sourceFile,
        writeFileCallback,
        cancellationToken,
        emitOnly,
        transformers,
        forceDtsEmit,
        skipBuildInfo
      )
    );
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
    return result;
  }
  function isEmitBlocked(emitFileName) {
    return hasEmitBlockingDiagnostics.has(toPath3(emitFileName));
  }
  function emitWorker(program2, sourceFile, writeFileCallback, cancellationToken, emitOnly, customTransformers, forceDtsEmit, skipBuildInfo) {
    if (!forceDtsEmit) {
      const result ***REMOVED*** handleNoEmitOptions(program2, sourceFile, writeFileCallback, cancellationToken);
      if (result) return result;
    }
    const typeChecker2 ***REMOVED*** getTypeChecker();
    const emitResolver ***REMOVED*** typeChecker2.getEmitResolver(
      options.outFile ? void 0 : sourceFile,
      cancellationToken,
      emitResolverSkipsTypeChecking(emitOnly, forceDtsEmit)
    );
    mark("beforeEmit");
    const emitResult ***REMOVED*** typeChecker2.runWithCancellationToken(
      cancellationToken,
      () ***REMOVED***> emitFiles(
        emitResolver,
        getEmitHost(writeFileCallback),
        sourceFile,
        getTransformers(options, customTransformers, emitOnly),
        emitOnly,
        /*onlyBuildInfo*/
        false,
        forceDtsEmit,
        skipBuildInfo
      )
    );
    mark("afterEmit");
    measure("Emit", "beforeEmit", "afterEmit");
    return emitResult;
  }
  function getSourceFile(fileName) {
    return getSourceFileByPath(toPath3(fileName));
  }
  function getSourceFileByPath(path) {
    return filesByName.get(path) || void 0;
  }
  function getDiagnosticsHelper(sourceFile, getDiagnostics, cancellationToken) {
    if (sourceFile) {
      return sortAndDeduplicateDiagnostics(getDiagnostics(sourceFile, cancellationToken));
    }
    return sortAndDeduplicateDiagnostics(flatMap(program.getSourceFiles(), (sourceFile2) ***REMOVED***> {
      if (cancellationToken) {
        cancellationToken.throwIfCancellationRequested();
      }
      return getDiagnostics(sourceFile2, cancellationToken);
    }));
  }
  function getSyntacticDiagnostics(sourceFile, cancellationToken) {
    return getDiagnosticsHelper(sourceFile, getSyntacticDiagnosticsForFile, cancellationToken);
  }
  function getSemanticDiagnostics(sourceFile, cancellationToken, nodesToCheck) {
    return getDiagnosticsHelper(
      sourceFile,
      (sourceFile2, cancellationToken2) ***REMOVED***> getSemanticDiagnosticsForFile(sourceFile2, cancellationToken2, nodesToCheck),
      cancellationToken
    );
  }
  function getCachedSemanticDiagnostics(sourceFile) {
    return cachedBindAndCheckDiagnosticsForFile ***REMOVED******REMOVED*** null ? void 0 : cachedBindAndCheckDiagnosticsForFile.get(sourceFile.path);
  }
  function getBindAndCheckDiagnostics(sourceFile, cancellationToken) {
    return getBindAndCheckDiagnosticsForFile(
      sourceFile,
      cancellationToken,
      /*nodesToCheck*/
      void 0
    );
  }
  function getProgramDiagnostics(sourceFile) {
    var _a2;
    if (skipTypeChecking(sourceFile, options, program)) {
      return emptyArray;
    }
    const programDiagnosticsInFile ***REMOVED*** programDiagnostics.getCombinedDiagnostics(program).getDiagnostics(sourceFile.fileName);
    if (!((_a2 ***REMOVED*** sourceFile.commentDirectives) ***REMOVED******REMOVED*** null ? void 0 : _a2.length)) {
      return programDiagnosticsInFile;
    }
    return getDiagnosticsWithPrecedingDirectives(sourceFile, sourceFile.commentDirectives, programDiagnosticsInFile).diagnostics;
  }
  function getDeclarationDiagnostics2(sourceFile, cancellationToken) {
    return getDiagnosticsHelper(sourceFile, getDeclarationDiagnosticsForFile, cancellationToken);
  }
  function getSyntacticDiagnosticsForFile(sourceFile) {
    if (isSourceFileJS(sourceFile)) {
      if (!sourceFile.additionalSyntacticDiagnostics) {
        sourceFile.additionalSyntacticDiagnostics ***REMOVED*** getJSSyntacticDiagnosticsForFile(sourceFile);
      }
      return concatenate(sourceFile.additionalSyntacticDiagnostics, sourceFile.parseDiagnostics);
    }
    return sourceFile.parseDiagnostics;
  }
  function runWithCancellationToken(func) {
    try {
      return func();
    } catch (e) {
      if (e instanceof OperationCanceledException) {
        typeChecker ***REMOVED*** void 0;
      }
      throw e;
    }
  }
  function getSemanticDiagnosticsForFile(sourceFile, cancellationToken, nodesToCheck) {
    return concatenate(
      filterSemanticDiagnostics(getBindAndCheckDiagnosticsForFile(sourceFile, cancellationToken, nodesToCheck), options),
      getProgramDiagnostics(sourceFile)
    );
  }
  function getBindAndCheckDiagnosticsForFile(sourceFile, cancellationToken, nodesToCheck) {
    if (nodesToCheck) {
      return getBindAndCheckDiagnosticsForFileNoCache(sourceFile, cancellationToken, nodesToCheck);
    }
    let result ***REMOVED*** cachedBindAndCheckDiagnosticsForFile ***REMOVED******REMOVED*** null ? void 0 : cachedBindAndCheckDiagnosticsForFile.get(sourceFile.path);
    if (!result) {
      (cachedBindAndCheckDiagnosticsForFile ?? (cachedBindAndCheckDiagnosticsForFile ***REMOVED*** /* @__PURE__ */ new Map())).set(
        sourceFile.path,
        result ***REMOVED*** getBindAndCheckDiagnosticsForFileNoCache(sourceFile, cancellationToken)
      );
    }
    return result;
  }
  function getBindAndCheckDiagnosticsForFileNoCache(sourceFile, cancellationToken, nodesToCheck) {
    return runWithCancellationToken(() ***REMOVED***> {
      if (skipTypeChecking(sourceFile, options, program)) {
        return emptyArray;
      }
      const typeChecker2 ***REMOVED*** getTypeChecker();
      Debug.assert(!!sourceFile.bindDiagnostics);
      const isJs ***REMOVED*** sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 1 /* JS */ || sourceFile.scriptKind ***REMOVED******REMOVED******REMOVED*** 2 /* JSX */;
      const isPlainJs ***REMOVED*** isPlainJsFile(sourceFile, options.checkJs);
      const isCheckJs ***REMOVED*** isJs && isCheckJsEnabledForFile(sourceFile, options);
      let bindDiagnostics ***REMOVED*** sourceFile.bindDiagnostics;
      let checkDiagnostics ***REMOVED*** typeChecker2.getDiagnostics(sourceFile, cancellationToken, nodesToCheck);
      if (isPlainJs) {
        bindDiagnostics ***REMOVED*** filter(bindDiagnostics, (d) ***REMOVED***> plainJSErrors.has(d.code));
        checkDiagnostics ***REMOVED*** filter(checkDiagnostics, (d) ***REMOVED***> plainJSErrors.has(d.code));
      }
      return getMergedBindAndCheckDiagnostics(
        sourceFile,
        !isPlainJs,
        !!nodesToCheck,
        bindDiagnostics,
        checkDiagnostics,
        isCheckJs ? sourceFile.jsDocDiagnostics : void 0
      );
    });
  }
  function getMergedBindAndCheckDiagnostics(sourceFile, includeBindAndCheckDiagnostics, partialCheck, ...allDiagnostics) {
    var _a2;
    const flatDiagnostics ***REMOVED*** flatten(allDiagnostics);
    if (!includeBindAndCheckDiagnostics || !((_a2 ***REMOVED*** sourceFile.commentDirectives) ***REMOVED******REMOVED*** null ? void 0 : _a2.length)) {
      return flatDiagnostics;
    }
    const { diagnostics, directives } ***REMOVED*** getDiagnosticsWithPrecedingDirectives(sourceFile, sourceFile.commentDirectives, flatDiagnostics);
    if (partialCheck) {
      return diagnostics;
    }
    for (const errorExpectation of directives.getUnusedExpectations()) {
      diagnostics.push(createDiagnosticForRange(sourceFile, errorExpectation.range, Diagnostics.Unused_ts_expect_error_directive));
    }
    return diagnostics;
  }
  function getDiagnosticsWithPrecedingDirectives(sourceFile, commentDirectives, flatDiagnostics) {
    const directives ***REMOVED*** createCommentDirectivesMap(sourceFile, commentDirectives);
    const diagnostics ***REMOVED*** flatDiagnostics.filter((diagnostic) ***REMOVED***> markPrecedingCommentDirectiveLine(diagnostic, directives) ***REMOVED******REMOVED******REMOVED*** -1);
    return { diagnostics, directives };
  }
  function getSuggestionDiagnostics(sourceFile, cancellationToken) {
    return runWithCancellationToken(() ***REMOVED***> {
      return getTypeChecker().getSuggestionDiagnostics(sourceFile, cancellationToken);
    });
  }
  function markPrecedingCommentDirectiveLine(diagnostic, directives) {
    const { file, start } ***REMOVED*** diagnostic;
    if (!file) {
      return -1;
    }
    const lineStarts ***REMOVED*** getLineStarts(file);
    let line ***REMOVED*** computeLineAndCharacterOfPosition(lineStarts, start).line - 1;
    while (line >***REMOVED*** 0) {
      if (directives.markUsed(line)) {
        return line;
      }
      const lineText ***REMOVED*** file.text.slice(lineStarts[line], lineStarts[line + 1]).trim();
      if (lineText !***REMOVED******REMOVED*** "" && !/^\s*\/\/.*$/.test(lineText)) {
        return -1;
      }
      line--;
    }
    return -1;
  }
  function getJSSyntacticDiagnosticsForFile(sourceFile) {
    return runWithCancellationToken(() ***REMOVED***> {
      const diagnostics ***REMOVED*** [];
      walk(sourceFile, sourceFile);
      forEachChildRecursively(sourceFile, walk, walkArray);
      return diagnostics;
      function walk(node, parent) {
        switch (parent.kind) {
          case 170 /* Parameter */:
          case 173 /* PropertyDeclaration */:
          case 175 /* MethodDeclaration */:
            if (parent.questionToken ***REMOVED******REMOVED******REMOVED*** node) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics.The_0_modifier_can_only_be_used_in_TypeScript_files, "?"));
              return "skip";
            }
          // falls through
          case 174 /* MethodSignature */:
          case 177 /* Constructor */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
          case 219 /* FunctionExpression */:
          case 263 /* FunctionDeclaration */:
          case 220 /* ArrowFunction */:
          case 261 /* VariableDeclaration */:
            if (parent.type ***REMOVED******REMOVED******REMOVED*** node) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics.Type_annotations_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
        }
        switch (node.kind) {
          case 274 /* ImportClause */:
            if (node.isTypeOnly) {
              diagnostics.push(createDiagnosticForNode2(parent, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, "import type"));
              return "skip";
            }
            break;
          case 279 /* ExportDeclaration */:
            if (node.isTypeOnly) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, "export type"));
              return "skip";
            }
            break;
          case 277 /* ImportSpecifier */:
          case 282 /* ExportSpecifier */:
            if (node.isTypeOnly) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, isImportSpecifier(node) ? "import...type" : "export...type"));
              return "skip";
            }
            break;
          case 272 /* ImportEqualsDeclaration */:
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics.import_can_only_be_used_in_TypeScript_files));
            return "skip";
          case 278 /* ExportAssignment */:
            if (node.isExportEquals) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics.export_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
            break;
          case 299 /* HeritageClause */:
            const heritageClause ***REMOVED*** node;
            if (heritageClause.token ***REMOVED******REMOVED******REMOVED*** 119 /* ImplementsKeyword */) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics.implements_clauses_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
            break;
          case 265 /* InterfaceDeclaration */:
            const interfaceKeyword ***REMOVED*** tokenToString(120 /* InterfaceKeyword */);
            Debug.assertIsDefined(interfaceKeyword);
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, interfaceKeyword));
            return "skip";
          case 268 /* ModuleDeclaration */:
            const moduleKeyword ***REMOVED*** node.flags & 32 /* Namespace */ ? tokenToString(145 /* NamespaceKeyword */) : tokenToString(144 /* ModuleKeyword */);
            Debug.assertIsDefined(moduleKeyword);
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, moduleKeyword));
            return "skip";
          case 266 /* TypeAliasDeclaration */:
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics.Type_aliases_can_only_be_used_in_TypeScript_files));
            return "skip";
          case 177 /* Constructor */:
          case 175 /* MethodDeclaration */:
          case 263 /* FunctionDeclaration */:
            if (!node.body) {
              diagnostics.push(createDiagnosticForNode2(node, Diagnostics.Signature_declarations_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
            return;
          case 267 /* EnumDeclaration */:
            const enumKeyword ***REMOVED*** Debug.checkDefined(tokenToString(94 /* EnumKeyword */));
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics._0_declarations_can_only_be_used_in_TypeScript_files, enumKeyword));
            return "skip";
          case 236 /* NonNullExpression */:
            diagnostics.push(createDiagnosticForNode2(node, Diagnostics.Non_null_assertions_can_only_be_used_in_TypeScript_files));
            return "skip";
          case 235 /* AsExpression */:
            diagnostics.push(createDiagnosticForNode2(node.type, Diagnostics.Type_assertion_expressions_can_only_be_used_in_TypeScript_files));
            return "skip";
          case 239 /* SatisfiesExpression */:
            diagnostics.push(createDiagnosticForNode2(node.type, Diagnostics.Type_satisfaction_expressions_can_only_be_used_in_TypeScript_files));
            return "skip";
          case 217 /* TypeAssertionExpression */:
            Debug.fail();
        }
      }
      function walkArray(nodes, parent) {
        if (canHaveIllegalDecorators(parent)) {
          const decorator ***REMOVED*** find(parent.modifiers, isDecorator);
          if (decorator) {
            diagnostics.push(createDiagnosticForNode2(decorator, Diagnostics.Decorators_are_not_valid_here));
          }
        } else if (canHaveDecorators(parent) && parent.modifiers) {
          const decoratorIndex ***REMOVED*** findIndex(parent.modifiers, isDecorator);
          if (decoratorIndex >***REMOVED*** 0) {
            if (isParameter(parent) && !options.experimentalDecorators) {
              diagnostics.push(createDiagnosticForNode2(parent.modifiers[decoratorIndex], Diagnostics.Decorators_are_not_valid_here));
            } else if (isClassDeclaration(parent)) {
              const exportIndex ***REMOVED*** findIndex(parent.modifiers, isExportModifier);
              if (exportIndex >***REMOVED*** 0) {
                const defaultIndex ***REMOVED*** findIndex(parent.modifiers, isDefaultModifier);
                if (decoratorIndex > exportIndex && defaultIndex >***REMOVED*** 0 && decoratorIndex < defaultIndex) {
                  diagnostics.push(createDiagnosticForNode2(parent.modifiers[decoratorIndex], Diagnostics.Decorators_are_not_valid_here));
                } else if (exportIndex >***REMOVED*** 0 && decoratorIndex < exportIndex) {
                  const trailingDecoratorIndex ***REMOVED*** findIndex(parent.modifiers, isDecorator, exportIndex);
                  if (trailingDecoratorIndex >***REMOVED*** 0) {
                    diagnostics.push(addRelatedInfo(
                      createDiagnosticForNode2(parent.modifiers[trailingDecoratorIndex], Diagnostics.Decorators_may_not_appear_after_export_or_export_default_if_they_also_appear_before_export),
                      createDiagnosticForNode2(parent.modifiers[decoratorIndex], Diagnostics.Decorator_used_before_export_here)
                    ));
                  }
                }
              }
            }
          }
        }
        switch (parent.kind) {
          case 264 /* ClassDeclaration */:
          case 232 /* ClassExpression */:
          case 175 /* MethodDeclaration */:
          case 177 /* Constructor */:
          case 178 /* GetAccessor */:
          case 179 /* SetAccessor */:
          case 219 /* FunctionExpression */:
          case 263 /* FunctionDeclaration */:
          case 220 /* ArrowFunction */:
            if (nodes ***REMOVED******REMOVED******REMOVED*** parent.typeParameters) {
              diagnostics.push(createDiagnosticForNodeArray2(nodes, Diagnostics.Type_parameter_declarations_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
          // falls through
          case 244 /* VariableStatement */:
            if (nodes ***REMOVED******REMOVED******REMOVED*** parent.modifiers) {
              checkModifiers(parent.modifiers, parent.kind ***REMOVED******REMOVED******REMOVED*** 244 /* VariableStatement */);
              return "skip";
            }
            break;
          case 173 /* PropertyDeclaration */:
            if (nodes ***REMOVED******REMOVED******REMOVED*** parent.modifiers) {
              for (const modifier of nodes) {
                if (isModifier(modifier) && modifier.kind !***REMOVED******REMOVED*** 126 /* StaticKeyword */ && modifier.kind !***REMOVED******REMOVED*** 129 /* AccessorKeyword */) {
                  diagnostics.push(createDiagnosticForNode2(modifier, Diagnostics.The_0_modifier_can_only_be_used_in_TypeScript_files, tokenToString(modifier.kind)));
                }
              }
              return "skip";
            }
            break;
          case 170 /* Parameter */:
            if (nodes ***REMOVED******REMOVED******REMOVED*** parent.modifiers && some(nodes, isModifier)) {
              diagnostics.push(createDiagnosticForNodeArray2(nodes, Diagnostics.Parameter_modifiers_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
            break;
          case 214 /* CallExpression */:
          case 215 /* NewExpression */:
          case 234 /* ExpressionWithTypeArguments */:
          case 286 /* JsxSelfClosingElement */:
          case 287 /* JsxOpeningElement */:
          case 216 /* TaggedTemplateExpression */:
            if (nodes ***REMOVED******REMOVED******REMOVED*** parent.typeArguments) {
              diagnostics.push(createDiagnosticForNodeArray2(nodes, Diagnostics.Type_arguments_can_only_be_used_in_TypeScript_files));
              return "skip";
            }
            break;
        }
      }
      function checkModifiers(modifiers, isConstValid) {
        for (const modifier of modifiers) {
          switch (modifier.kind) {
            case 87 /* ConstKeyword */:
              if (isConstValid) {
                continue;
              }
            // to report error,
            // falls through
            case 125 /* PublicKeyword */:
            case 123 /* PrivateKeyword */:
            case 124 /* ProtectedKeyword */:
            case 148 /* ReadonlyKeyword */:
            case 138 /* DeclareKeyword */:
            case 128 /* AbstractKeyword */:
            case 164 /* OverrideKeyword */:
            case 103 /* InKeyword */:
            case 147 /* OutKeyword */:
              diagnostics.push(createDiagnosticForNode2(modifier, Diagnostics.The_0_modifier_can_only_be_used_in_TypeScript_files, tokenToString(modifier.kind)));
              break;
            // These are all legal modifiers.
            case 126 /* StaticKeyword */:
            case 95 /* ExportKeyword */:
            case 90 /* DefaultKeyword */:
            case 129 /* AccessorKeyword */:
          }
        }
      }
      function createDiagnosticForNodeArray2(nodes, message, ...args) {
        const start ***REMOVED*** nodes.pos;
        return createFileDiagnostic(sourceFile, start, nodes.end - start, message, ...args);
      }
      function createDiagnosticForNode2(node, message, ...args) {
        return createDiagnosticForNodeInSourceFile(sourceFile, node, message, ...args);
      }
    });
  }
  function getDeclarationDiagnosticsWorker(sourceFile, cancellationToken) {
    let result ***REMOVED*** cachedDeclarationDiagnosticsForFile ***REMOVED******REMOVED*** null ? void 0 : cachedDeclarationDiagnosticsForFile.get(sourceFile.path);
    if (!result) {
      (cachedDeclarationDiagnosticsForFile ?? (cachedDeclarationDiagnosticsForFile ***REMOVED*** /* @__PURE__ */ new Map())).set(
        sourceFile.path,
        result ***REMOVED*** getDeclarationDiagnosticsForFileNoCache(sourceFile, cancellationToken)
      );
    }
    return result;
  }
  function getDeclarationDiagnosticsForFileNoCache(sourceFile, cancellationToken) {
    return runWithCancellationToken(() ***REMOVED***> {
      const resolver ***REMOVED*** getTypeChecker().getEmitResolver(sourceFile, cancellationToken);
      return getDeclarationDiagnostics(getEmitHost(noop), resolver, sourceFile) || emptyArray;
    });
  }
  function getDeclarationDiagnosticsForFile(sourceFile, cancellationToken) {
    return sourceFile.isDeclarationFile ? emptyArray : getDeclarationDiagnosticsWorker(sourceFile, cancellationToken);
  }
  function getOptionsDiagnostics() {
    return sortAndDeduplicateDiagnostics(concatenate(
      programDiagnostics.getCombinedDiagnostics(program).getGlobalDiagnostics(),
      getOptionsDiagnosticsOfConfigFile()
    ));
  }
  function getOptionsDiagnosticsOfConfigFile() {
    if (!options.configFile) return emptyArray;
    let diagnostics ***REMOVED*** programDiagnostics.getCombinedDiagnostics(program).getDiagnostics(options.configFile.fileName);
    forEachResolvedProjectReference2((resolvedRef) ***REMOVED***> {
      diagnostics ***REMOVED*** concatenate(diagnostics, programDiagnostics.getCombinedDiagnostics(program).getDiagnostics(resolvedRef.sourceFile.fileName));
    });
    return diagnostics;
  }
  function getGlobalDiagnostics() {
    return rootNames.length ? sortAndDeduplicateDiagnostics(getTypeChecker().getGlobalDiagnostics().slice()) : emptyArray;
  }
  function getConfigFileParsingDiagnostics2() {
    return configFileParsingDiagnostics || emptyArray;
  }
  function processRootFile(fileName, isDefaultLib, ignoreNoDefaultLib, reason) {
    processSourceFile(
      normalizePath(fileName),
      isDefaultLib,
      ignoreNoDefaultLib,
      /*packageId*/
      void 0,
      reason
    );
  }
  function fileReferenceIsEqualTo(a, b) {
    return a.fileName ***REMOVED******REMOVED******REMOVED*** b.fileName;
  }
  function moduleNameIsEqualTo(a, b) {
    return a.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ ? b.kind ***REMOVED******REMOVED******REMOVED*** 80 /* Identifier */ && a.escapedText ***REMOVED******REMOVED******REMOVED*** b.escapedText : b.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ && a.text ***REMOVED******REMOVED******REMOVED*** b.text;
  }
  function createSyntheticImport(text, file) {
    const externalHelpersModuleReference ***REMOVED*** factory.createStringLiteral(text);
    const importDecl ***REMOVED*** factory.createImportDeclaration(
      /*modifiers*/
      void 0,
      /*importClause*/
      void 0,
      externalHelpersModuleReference
    );
    addInternalEmitFlags(importDecl, 2 /* NeverApplyImportHelper */);
    setParent(externalHelpersModuleReference, importDecl);
    setParent(importDecl, file);
    externalHelpersModuleReference.flags &***REMOVED*** ~16 /* Synthesized */;
    importDecl.flags &***REMOVED*** ~16 /* Synthesized */;
    return externalHelpersModuleReference;
  }
  function collectExternalModuleReferences(file) {
    if (file.imports) {
      return;
    }
    const isJavaScriptFile ***REMOVED*** isSourceFileJS(file);
    const isExternalModuleFile ***REMOVED*** isExternalModule(file);
    let imports;
    let moduleAugmentations;
    let ambientModules;
    if (isJavaScriptFile || !file.isDeclarationFile && (getIsolatedModules(options) || isExternalModule(file))) {
      if (options.importHelpers) {
        imports ***REMOVED*** [createSyntheticImport(externalHelpersModuleNameText, file)];
      }
      const jsxImport ***REMOVED*** getJSXRuntimeImport(getJSXImplicitImportBase(options, file), options);
      if (jsxImport) {
        (imports || (imports ***REMOVED*** [])).push(createSyntheticImport(jsxImport, file));
      }
    }
    for (const node of file.statements) {
      collectModuleReferences(
        node,
        /*inAmbientModule*/
        false
      );
    }
    if (file.flags & 4194304 /* PossiblyContainsDynamicImport */ || isJavaScriptFile) {
      forEachDynamicImportOrRequireCall(
        file,
        /*includeTypeSpaceImports*/
        true,
        /*requireStringLiteralLikeArgument*/
        true,
        (node, moduleSpecifier) ***REMOVED***> {
          setParentRecursive(
            node,
            /*incremental*/
            false
          );
          imports ***REMOVED*** append(imports, moduleSpecifier);
        }
      );
    }
    file.imports ***REMOVED*** imports || emptyArray;
    file.moduleAugmentations ***REMOVED*** moduleAugmentations || emptyArray;
    file.ambientModuleNames ***REMOVED*** ambientModules || emptyArray;
    return;
    function collectModuleReferences(node, inAmbientModule) {
      if (isAnyImportOrReExport(node)) {
        const moduleNameExpr ***REMOVED*** getExternalModuleName(node);
        if (moduleNameExpr && isStringLiteral(moduleNameExpr) && moduleNameExpr.text && (!inAmbientModule || !isExternalModuleNameRelative(moduleNameExpr.text))) {
          setParentRecursive(
            node,
            /*incremental*/
            false
          );
          imports ***REMOVED*** append(imports, moduleNameExpr);
          if (!usesUriStyleNodeCoreModules && currentNodeModulesDepth ***REMOVED******REMOVED******REMOVED*** 0 && !file.isDeclarationFile) {
            if (startsWith(moduleNameExpr.text, "node:") && !exclusivelyPrefixedNodeCoreModules.has(moduleNameExpr.text)) {
              usesUriStyleNodeCoreModules ***REMOVED*** true;
            } else if (usesUriStyleNodeCoreModules ***REMOVED******REMOVED******REMOVED*** void 0 && unprefixedNodeCoreModules.has(moduleNameExpr.text)) {
              usesUriStyleNodeCoreModules ***REMOVED*** false;
            }
          }
        }
      } else if (isModuleDeclaration(node)) {
        if (isAmbientModule(node) && (inAmbientModule || hasSyntacticModifier(node, 128 /* Ambient */) || file.isDeclarationFile)) {
          node.name.parent ***REMOVED*** node;
          const nameText ***REMOVED*** getTextOfIdentifierOrLiteral(node.name);
          if (isExternalModuleFile || inAmbientModule && !isExternalModuleNameRelative(nameText)) {
            (moduleAugmentations || (moduleAugmentations ***REMOVED*** [])).push(node.name);
          } else if (!inAmbientModule) {
            if (file.isDeclarationFile) {
              (ambientModules || (ambientModules ***REMOVED*** [])).push(nameText);
            }
            const body ***REMOVED*** node.body;
            if (body) {
              for (const statement of body.statements) {
                collectModuleReferences(
                  statement,
                  /*inAmbientModule*/
                  true
                );
              }
            }
          }
        }
      }
    }
  }
  function getLibFileFromReference(ref) {
    var _a2;
    const libFileName ***REMOVED*** getLibFileNameFromLibReference(ref);
    const actualFileName ***REMOVED*** libFileName && ((_a2 ***REMOVED*** resolvedLibReferences ***REMOVED******REMOVED*** null ? void 0 : resolvedLibReferences.get(libFileName)) ***REMOVED******REMOVED*** null ? void 0 : _a2.actual);
    return actualFileName !***REMOVED******REMOVED*** void 0 ? getSourceFile(actualFileName) : void 0;
  }
  function getSourceFileFromReference(referencingFile, ref) {
    return getSourceFileFromReferenceWorker(resolveTripleslashReference(ref.fileName, referencingFile.fileName), getSourceFile);
  }
  function getSourceFileFromReferenceWorker(fileName, getSourceFile2, fail, reason) {
    if (hasExtension(fileName)) {
      const canonicalFileName ***REMOVED*** host.getCanonicalFileName(fileName);
      if (!options.allowNonTsExtensions && !forEach(flatten(supportedExtensionsWithJsonIfResolveJsonModule), (extension) ***REMOVED***> fileExtensionIs(canonicalFileName, extension))) {
        if (fail) {
          if (hasJSFileExtension(canonicalFileName)) {
            fail(Diagnostics.File_0_is_a_JavaScript_file_Did_you_mean_to_enable_the_allowJs_option, fileName);
          } else {
            fail(Diagnostics.File_0_has_an_unsupported_extension_The_only_supported_extensions_are_1, fileName, "'" + flatten(supportedExtensions).join("', '") + "'");
          }
        }
        return void 0;
      }
      const sourceFile ***REMOVED*** getSourceFile2(fileName);
      if (fail) {
        if (!sourceFile) {
          const redirect ***REMOVED*** getRedirectFromSourceFile(fileName);
          if (redirect ***REMOVED******REMOVED*** null ? void 0 : redirect.outputDts) {
            fail(Diagnostics.Output_file_0_has_not_been_built_from_source_file_1, redirect.outputDts, fileName);
          } else {
            fail(Diagnostics.File_0_not_found, fileName);
          }
        } else if (isReferencedFile(reason) && canonicalFileName ***REMOVED******REMOVED******REMOVED*** host.getCanonicalFileName(getSourceFileByPath(reason.file).fileName)) {
          fail(Diagnostics.A_file_cannot_have_a_reference_to_itself);
        }
      }
      return sourceFile;
    } else {
      const sourceFileNoExtension ***REMOVED*** options.allowNonTsExtensions && getSourceFile2(fileName);
      if (sourceFileNoExtension) return sourceFileNoExtension;
      if (fail && options.allowNonTsExtensions) {
        fail(Diagnostics.File_0_not_found, fileName);
        return void 0;
      }
      const sourceFileWithAddedExtension ***REMOVED*** forEach(supportedExtensions[0], (extension) ***REMOVED***> getSourceFile2(fileName + extension));
      if (fail && !sourceFileWithAddedExtension) fail(Diagnostics.Could_not_resolve_the_path_0_with_the_extensions_Colon_1, fileName, "'" + flatten(supportedExtensions).join("', '") + "'");
      return sourceFileWithAddedExtension;
    }
  }
  function processSourceFile(fileName, isDefaultLib, ignoreNoDefaultLib, packageId, reason) {
    getSourceFileFromReferenceWorker(
      fileName,
      (fileName2) ***REMOVED***> findSourceFile(fileName2, isDefaultLib, ignoreNoDefaultLib, reason, packageId),
      // TODO: GH#18217
      (diagnostic, ...args) ***REMOVED***> addFilePreprocessingFileExplainingDiagnostic(
        /*file*/
        void 0,
        reason,
        diagnostic,
        args
      ),
      reason
    );
  }
  function processProjectReferenceFile(fileName, reason) {
    return processSourceFile(
      fileName,
      /*isDefaultLib*/
      false,
      /*ignoreNoDefaultLib*/
      false,
      /*packageId*/
      void 0,
      reason
    );
  }
  function reportFileNamesDifferOnlyInCasingError(fileName, existingFile, reason) {
    const hasExistingReasonToReportErrorOn ***REMOVED*** !isReferencedFile(reason) && some(programDiagnostics.getFileReasons().get(existingFile.path), isReferencedFile);
    if (hasExistingReasonToReportErrorOn) {
      addFilePreprocessingFileExplainingDiagnostic(existingFile, reason, Diagnostics.Already_included_file_name_0_differs_from_file_name_1_only_in_casing, [existingFile.fileName, fileName]);
    } else {
      addFilePreprocessingFileExplainingDiagnostic(existingFile, reason, Diagnostics.File_name_0_differs_from_already_included_file_name_1_only_in_casing, [fileName, existingFile.fileName]);
    }
  }
  function createRedirectedSourceFile(redirectTarget, unredirected, fileName, path, resolvedPath, originalFileName, sourceFileOptions) {
    var _a2;
    const redirect ***REMOVED*** parseNodeFactory.createRedirectedSourceFile({ redirectTarget, unredirected });
    redirect.fileName ***REMOVED*** fileName;
    redirect.path ***REMOVED*** path;
    redirect.resolvedPath ***REMOVED*** resolvedPath;
    redirect.originalFileName ***REMOVED*** originalFileName;
    redirect.packageJsonLocations ***REMOVED*** ((_a2 ***REMOVED*** sourceFileOptions.packageJsonLocations) ***REMOVED******REMOVED*** null ? void 0 : _a2.length) ? sourceFileOptions.packageJsonLocations : void 0;
    redirect.packageJsonScope ***REMOVED*** sourceFileOptions.packageJsonScope;
    sourceFilesFoundSearchingNodeModules.set(path, currentNodeModulesDepth > 0);
    return redirect;
  }
  function findSourceFile(fileName, isDefaultLib, ignoreNoDefaultLib, reason, packageId) {
    var _a2, _b2;
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(tracing.Phase.Program, "findSourceFile", {
      fileName,
      isDefaultLib: isDefaultLib || void 0,
      fileIncludeKind: FileIncludeKind[reason.kind]
    });
    const result ***REMOVED*** findSourceFileWorker(fileName, isDefaultLib, ignoreNoDefaultLib, reason, packageId);
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
    return result;
  }
  function getCreateSourceFileOptions(fileName, moduleResolutionCache2, host2, options2) {
    const result ***REMOVED*** getImpliedNodeFormatForFileWorker(getNormalizedAbsolutePath(fileName, currentDirectory), moduleResolutionCache2 ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache2.getPackageJsonInfoCache(), host2, options2);
    const languageVersion ***REMOVED*** getEmitScriptTarget(options2);
    const setExternalModuleIndicator2 ***REMOVED*** getSetExternalModuleIndicator(options2);
    return typeof result ***REMOVED******REMOVED******REMOVED*** "object" ? { ...result, languageVersion, setExternalModuleIndicator: setExternalModuleIndicator2, jsDocParsingMode: host2.jsDocParsingMode } : { languageVersion, impliedNodeFormat: result, setExternalModuleIndicator: setExternalModuleIndicator2, jsDocParsingMode: host2.jsDocParsingMode };
  }
  function findSourceFileWorker(fileName, isDefaultLib, ignoreNoDefaultLib, reason, packageId) {
    var _a2, _b2;
    const path ***REMOVED*** toPath3(fileName);
    if (useSourceOfProjectReferenceRedirect) {
      let source ***REMOVED*** getRedirectFromOutput(path);
      if (!source && host.realpath && options.preserveSymlinks && isDeclarationFileName(fileName) && fileName.includes(nodeModulesPathPart)) {
        const realPath2 ***REMOVED*** toPath3(host.realpath(fileName));
        if (realPath2 !***REMOVED******REMOVED*** path) source ***REMOVED*** getRedirectFromOutput(realPath2);
      }
      if (source ***REMOVED******REMOVED*** null ? void 0 : source.source) {
        const file2 ***REMOVED*** findSourceFile(source.source, isDefaultLib, ignoreNoDefaultLib, reason, packageId);
        if (file2) addFileToFilesByName(
          file2,
          path,
          fileName,
          /*redirectedPath*/
          void 0
        );
        return file2;
      }
    }
    const originalFileName ***REMOVED*** fileName;
    if (filesByName.has(path)) {
      const file2 ***REMOVED*** filesByName.get(path);
      const addedReason ***REMOVED*** addFileIncludeReason(
        file2 || void 0,
        reason,
        /*checkExisting*/
        true
      );
      if (file2 && addedReason && !(options.forceConsistentCasingInFileNames ***REMOVED******REMOVED******REMOVED*** false)) {
        const checkedName ***REMOVED*** file2.fileName;
        const isRedirect ***REMOVED*** toPath3(checkedName) !***REMOVED******REMOVED*** toPath3(fileName);
        if (isRedirect) {
          fileName ***REMOVED*** ((_a2 ***REMOVED*** getRedirectFromSourceFile(fileName)) ***REMOVED******REMOVED*** null ? void 0 : _a2.outputDts) || fileName;
        }
        const checkedAbsolutePath ***REMOVED*** getNormalizedAbsolutePathWithoutRoot(checkedName, currentDirectory);
        const inputAbsolutePath ***REMOVED*** getNormalizedAbsolutePathWithoutRoot(fileName, currentDirectory);
        if (checkedAbsolutePath !***REMOVED******REMOVED*** inputAbsolutePath) {
          reportFileNamesDifferOnlyInCasingError(fileName, file2, reason);
        }
      }
      if (file2 && sourceFilesFoundSearchingNodeModules.get(file2.path) && currentNodeModulesDepth ***REMOVED******REMOVED******REMOVED*** 0) {
        sourceFilesFoundSearchingNodeModules.set(file2.path, false);
        if (!options.noResolve) {
          processReferencedFiles(file2, isDefaultLib);
          processTypeReferenceDirectives(file2);
        }
        if (!options.noLib) {
          processLibReferenceDirectives(file2);
        }
        modulesWithElidedImports.set(file2.path, false);
        processImportedModules(file2);
      } else if (file2 && modulesWithElidedImports.get(file2.path)) {
        if (currentNodeModulesDepth < maxNodeModuleJsDepth) {
          modulesWithElidedImports.set(file2.path, false);
          processImportedModules(file2);
        }
      }
      return file2 || void 0;
    }
    let redirectedPath;
    if (!useSourceOfProjectReferenceRedirect) {
      const redirectProject ***REMOVED*** getRedirectFromSourceFile(fileName);
      if (redirectProject ***REMOVED******REMOVED*** null ? void 0 : redirectProject.outputDts) {
        if (redirectProject.resolvedRef.commandLine.options.outFile) {
          return void 0;
        }
        fileName ***REMOVED*** redirectProject.outputDts;
        redirectedPath ***REMOVED*** toPath3(redirectProject.outputDts);
      }
    }
    const sourceFileOptions ***REMOVED*** getCreateSourceFileOptions(fileName, moduleResolutionCache, host, options);
    const file ***REMOVED*** host.getSourceFile(
      fileName,
      sourceFileOptions,
      (hostErrorMessage) ***REMOVED***> addFilePreprocessingFileExplainingDiagnostic(
        /*file*/
        void 0,
        reason,
        Diagnostics.Cannot_read_file_0_Colon_1,
        [fileName, hostErrorMessage]
      ),
      shouldCreateNewSourceFile
    );
    if (packageId) {
      const packageIdKey ***REMOVED*** packageIdToString(packageId);
      const fileFromPackageId ***REMOVED*** packageIdToSourceFile.get(packageIdKey);
      if (fileFromPackageId) {
        const dupFile ***REMOVED*** createRedirectedSourceFile(fileFromPackageId, file, fileName, path, toPath3(fileName), originalFileName, sourceFileOptions);
        redirectTargetsMap.add(fileFromPackageId.path, fileName);
        addFileToFilesByName(dupFile, path, fileName, redirectedPath);
        addFileIncludeReason(
          dupFile,
          reason,
          /*checkExisting*/
          false
        );
        sourceFileToPackageName.set(path, packageIdToPackageName(packageId));
        processingOtherFiles.push(dupFile);
        return dupFile;
      } else if (file) {
        packageIdToSourceFile.set(packageIdKey, file);
        sourceFileToPackageName.set(path, packageIdToPackageName(packageId));
      }
    }
    addFileToFilesByName(file, path, fileName, redirectedPath);
    if (file) {
      sourceFilesFoundSearchingNodeModules.set(path, currentNodeModulesDepth > 0);
      file.fileName ***REMOVED*** fileName;
      file.path ***REMOVED*** path;
      file.resolvedPath ***REMOVED*** toPath3(fileName);
      file.originalFileName ***REMOVED*** originalFileName;
      file.packageJsonLocations ***REMOVED*** ((_b2 ***REMOVED*** sourceFileOptions.packageJsonLocations) ***REMOVED******REMOVED*** null ? void 0 : _b2.length) ? sourceFileOptions.packageJsonLocations : void 0;
      file.packageJsonScope ***REMOVED*** sourceFileOptions.packageJsonScope;
      addFileIncludeReason(
        file,
        reason,
        /*checkExisting*/
        false
      );
      if (host.useCaseSensitiveFileNames()) {
        const pathLowerCase ***REMOVED*** toFileNameLowerCase(path);
        const existingFile ***REMOVED*** filesByNameIgnoreCase.get(pathLowerCase);
        if (existingFile) {
          reportFileNamesDifferOnlyInCasingError(fileName, existingFile, reason);
        } else {
          filesByNameIgnoreCase.set(pathLowerCase, file);
        }
      }
      skipDefaultLib ***REMOVED*** skipDefaultLib || file.hasNoDefaultLib && !ignoreNoDefaultLib;
      if (!options.noResolve) {
        processReferencedFiles(file, isDefaultLib);
        processTypeReferenceDirectives(file);
      }
      if (!options.noLib) {
        processLibReferenceDirectives(file);
      }
      processImportedModules(file);
      if (isDefaultLib) {
        processingDefaultLibFiles.push(file);
      } else {
        processingOtherFiles.push(file);
      }
      (filesWithReferencesProcessed ?? (filesWithReferencesProcessed ***REMOVED*** /* @__PURE__ */ new Set())).add(file.path);
    }
    return file;
  }
  function addFileIncludeReason(file, reason, checkExisting) {
    if (file && (!checkExisting || !isReferencedFile(reason) || !(filesWithReferencesProcessed ***REMOVED******REMOVED*** null ? void 0 : filesWithReferencesProcessed.has(reason.file)))) {
      programDiagnostics.getFileReasons().add(file.path, reason);
      return true;
    }
    return false;
  }
  function addFileToFilesByName(file, path, fileName, redirectedPath) {
    if (redirectedPath) {
      updateFilesByNameMap(fileName, redirectedPath, file);
      updateFilesByNameMap(fileName, path, file || false);
    } else {
      updateFilesByNameMap(fileName, path, file);
    }
  }
  function updateFilesByNameMap(fileName, path, file) {
    filesByName.set(path, file);
    if (file !***REMOVED******REMOVED*** void 0) missingFileNames.delete(path);
    else missingFileNames.set(path, fileName);
  }
  function getRedirectFromSourceFile(fileName) {
    return mapSourceFileToResolvedRef ***REMOVED******REMOVED*** null ? void 0 : mapSourceFileToResolvedRef.get(toPath3(fileName));
  }
  function forEachResolvedProjectReference2(cb) {
    return forEachResolvedProjectReference(resolvedProjectReferences, cb);
  }
  function getRedirectFromOutput(path) {
    return mapOutputFileToResolvedRef ***REMOVED******REMOVED*** null ? void 0 : mapOutputFileToResolvedRef.get(path);
  }
  function isSourceOfProjectReferenceRedirect(fileName) {
    return useSourceOfProjectReferenceRedirect && !!getRedirectFromSourceFile(fileName);
  }
  function getResolvedProjectReferenceByPath(projectReferencePath) {
    if (!projectReferenceRedirects) {
      return void 0;
    }
    return projectReferenceRedirects.get(projectReferencePath) || void 0;
  }
  function processReferencedFiles(file, isDefaultLib) {
    forEach(file.referencedFiles, (ref, index) ***REMOVED***> {
      processSourceFile(
        resolveTripleslashReference(ref.fileName, file.fileName),
        isDefaultLib,
        /*ignoreNoDefaultLib*/
        false,
        /*packageId*/
        void 0,
        { kind: 4 /* ReferenceFile */, file: file.path, index }
      );
    });
  }
  function processTypeReferenceDirectives(file) {
    const typeDirectives ***REMOVED*** file.typeReferenceDirectives;
    if (!typeDirectives.length) return;
    const resolutions ***REMOVED*** (resolvedTypeReferenceDirectiveNamesProcessing ***REMOVED******REMOVED*** null ? void 0 : resolvedTypeReferenceDirectiveNamesProcessing.get(file.path)) || resolveTypeReferenceDirectiveNamesReusingOldState(typeDirectives, file);
    const resolutionsInFile ***REMOVED*** createModeAwareCache();
    (resolvedTypeReferenceDirectiveNames ?? (resolvedTypeReferenceDirectiveNames ***REMOVED*** /* @__PURE__ */ new Map())).set(file.path, resolutionsInFile);
    for (let index ***REMOVED*** 0; index < typeDirectives.length; index++) {
      const ref ***REMOVED*** file.typeReferenceDirectives[index];
      const resolvedTypeReferenceDirective ***REMOVED*** resolutions[index];
      const fileName ***REMOVED*** ref.fileName;
      const mode ***REMOVED*** getModeForTypeReferenceDirectiveInFile(ref, file);
      resolutionsInFile.set(fileName, mode, resolvedTypeReferenceDirective);
      processTypeReferenceDirective(fileName, mode, resolvedTypeReferenceDirective, { kind: 5 /* TypeReferenceDirective */, file: file.path, index });
    }
  }
  function getCompilerOptionsForFile(file) {
    var _a2;
    return ((_a2 ***REMOVED*** getRedirectReferenceForResolution(file)) ***REMOVED******REMOVED*** null ? void 0 : _a2.commandLine.options) || options;
  }
  function processTypeReferenceDirective(typeReferenceDirective, mode, resolution, reason) {
    var _a2, _b2;
    (_a2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a2.push(tracing.Phase.Program, "processTypeReferenceDirective", { directive: typeReferenceDirective, hasResolved: !!resolution.resolvedTypeReferenceDirective, refKind: reason.kind, refPath: isReferencedFile(reason) ? reason.file : void 0 });
    processTypeReferenceDirectiveWorker(typeReferenceDirective, mode, resolution, reason);
    (_b2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _b2.pop();
  }
  function processTypeReferenceDirectiveWorker(typeReferenceDirective, mode, resolution, reason) {
    addResolutionDiagnostics(resolution);
    const { resolvedTypeReferenceDirective } ***REMOVED*** resolution;
    if (resolvedTypeReferenceDirective) {
      if (resolvedTypeReferenceDirective.isExternalLibraryImport) currentNodeModulesDepth++;
      processSourceFile(
        resolvedTypeReferenceDirective.resolvedFileName,
        /*isDefaultLib*/
        false,
        /*ignoreNoDefaultLib*/
        false,
        resolvedTypeReferenceDirective.packageId,
        reason
      );
      if (resolvedTypeReferenceDirective.isExternalLibraryImport) currentNodeModulesDepth--;
    } else {
      addFilePreprocessingFileExplainingDiagnostic(
        /*file*/
        void 0,
        reason,
        Diagnostics.Cannot_find_type_definition_file_for_0,
        [typeReferenceDirective]
      );
    }
  }
  function pathForLibFile(libFileName) {
    const existing ***REMOVED*** resolvedLibReferences ***REMOVED******REMOVED*** null ? void 0 : resolvedLibReferences.get(libFileName);
    if (existing) return existing.actual;
    const result ***REMOVED*** pathForLibFileWorker(libFileName);
    (resolvedLibReferences ?? (resolvedLibReferences ***REMOVED*** /* @__PURE__ */ new Map())).set(libFileName, result);
    return result.actual;
  }
  function pathForLibFileWorker(libFileName) {
    var _a2, _b2, _c2, _d2, _e2;
    const existing ***REMOVED*** resolvedLibProcessing ***REMOVED******REMOVED*** null ? void 0 : resolvedLibProcessing.get(libFileName);
    if (existing) return existing;
    if (options.libReplacement ***REMOVED******REMOVED******REMOVED*** false) {
      const result2 ***REMOVED*** {
        resolution: {
          resolvedModule: void 0
        },
        actual: combinePaths(defaultLibraryPath, libFileName)
      };
      (resolvedLibProcessing ?? (resolvedLibProcessing ***REMOVED*** /* @__PURE__ */ new Map())).set(libFileName, result2);
      return result2;
    }
    if (structureIsReused !***REMOVED******REMOVED*** 0 /* Not */ && oldProgram && !hasInvalidatedLibResolutions(libFileName)) {
      const oldResolution ***REMOVED*** (_a2 ***REMOVED*** oldProgram.resolvedLibReferences) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(libFileName);
      if (oldResolution) {
        if (oldResolution.resolution && isTraceEnabled(options, host)) {
          const libraryName2 ***REMOVED*** getLibraryNameFromLibFileName(libFileName);
          const resolveFrom2 ***REMOVED*** getInferredLibraryNameResolveFrom(options, currentDirectory, libFileName);
          trace(
            host,
            oldResolution.resolution.resolvedModule ? oldResolution.resolution.resolvedModule.packageId ? Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_not_resolved,
            libraryName2,
            getNormalizedAbsolutePath(resolveFrom2, currentDirectory),
            (_b2 ***REMOVED*** oldResolution.resolution.resolvedModule) ***REMOVED******REMOVED*** null ? void 0 : _b2.resolvedFileName,
            ((_c2 ***REMOVED*** oldResolution.resolution.resolvedModule) ***REMOVED******REMOVED*** null ? void 0 : _c2.packageId) && packageIdToString(oldResolution.resolution.resolvedModule.packageId)
          );
        }
        (resolvedLibProcessing ?? (resolvedLibProcessing ***REMOVED*** /* @__PURE__ */ new Map())).set(libFileName, oldResolution);
        return oldResolution;
      }
    }
    const libraryName ***REMOVED*** getLibraryNameFromLibFileName(libFileName);
    const resolveFrom ***REMOVED*** getInferredLibraryNameResolveFrom(options, currentDirectory, libFileName);
    (_d2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _d2.push(tracing.Phase.Program, "resolveLibrary", { resolveFrom });
    mark("beforeResolveLibrary");
    const resolution ***REMOVED*** actualResolveLibrary(libraryName, resolveFrom, options, libFileName);
    mark("afterResolveLibrary");
    measure("ResolveLibrary", "beforeResolveLibrary", "afterResolveLibrary");
    (_e2 ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _e2.pop();
    const result ***REMOVED*** {
      resolution,
      actual: resolution.resolvedModule ? resolution.resolvedModule.resolvedFileName : combinePaths(defaultLibraryPath, libFileName)
    };
    (resolvedLibProcessing ?? (resolvedLibProcessing ***REMOVED*** /* @__PURE__ */ new Map())).set(libFileName, result);
    return result;
  }
  function processLibReferenceDirectives(file) {
    forEach(file.libReferenceDirectives, (libReference, index) ***REMOVED***> {
      const libFileName ***REMOVED*** getLibFileNameFromLibReference(libReference);
      if (libFileName) {
        processRootFile(
          pathForLibFile(libFileName),
          /*isDefaultLib*/
          true,
          /*ignoreNoDefaultLib*/
          true,
          { kind: 7 /* LibReferenceDirective */, file: file.path, index }
        );
      } else {
        programDiagnostics.addFileProcessingDiagnostic({
          kind: 0 /* FilePreprocessingLibReferenceDiagnostic */,
          reason: { kind: 7 /* LibReferenceDirective */, file: file.path, index }
        });
      }
    });
  }
  function getCanonicalFileName(fileName) {
    return host.getCanonicalFileName(fileName);
  }
  function processImportedModules(file) {
    collectExternalModuleReferences(file);
    if (file.imports.length || file.moduleAugmentations.length) {
      const moduleNames ***REMOVED*** getModuleNames(file);
      const resolutions ***REMOVED*** (resolvedModulesProcessing ***REMOVED******REMOVED*** null ? void 0 : resolvedModulesProcessing.get(file.path)) || resolveModuleNamesReusingOldState(moduleNames, file);
      Debug.assert(resolutions.length ***REMOVED******REMOVED******REMOVED*** moduleNames.length);
      const optionsForFile ***REMOVED*** getCompilerOptionsForFile(file);
      const resolutionsInFile ***REMOVED*** createModeAwareCache();
      (resolvedModules ?? (resolvedModules ***REMOVED*** /* @__PURE__ */ new Map())).set(file.path, resolutionsInFile);
      for (let index ***REMOVED*** 0; index < moduleNames.length; index++) {
        const resolution ***REMOVED*** resolutions[index].resolvedModule;
        const moduleName ***REMOVED*** moduleNames[index].text;
        const mode ***REMOVED*** getModeForUsageLocationWorker(file, moduleNames[index], optionsForFile);
        resolutionsInFile.set(moduleName, mode, resolutions[index]);
        addResolutionDiagnosticsFromResolutionOrCache(file, moduleName, resolutions[index], mode);
        if (!resolution) {
          continue;
        }
        const isFromNodeModulesSearch ***REMOVED*** resolution.isExternalLibraryImport;
        const isJsFile ***REMOVED*** !resolutionExtensionIsTSOrJson(resolution.extension) && !getRedirectFromSourceFile(resolution.resolvedFileName);
        const isJsFileFromNodeModules ***REMOVED*** isFromNodeModulesSearch && isJsFile && (!resolution.originalPath || pathContainsNodeModules(resolution.resolvedFileName));
        const resolvedFileName ***REMOVED*** resolution.resolvedFileName;
        if (isFromNodeModulesSearch) {
          currentNodeModulesDepth++;
        }
        const elideImport ***REMOVED*** isJsFileFromNodeModules && currentNodeModulesDepth > maxNodeModuleJsDepth;
        const shouldAddFile ***REMOVED*** resolvedFileName && !getResolutionDiagnostic(optionsForFile, resolution, file) && !optionsForFile.noResolve && index < file.imports.length && !elideImport && !(isJsFile && !getAllowJSCompilerOption(optionsForFile)) && (isInJSFile(file.imports[index]) || !(file.imports[index].flags & 16777216 /* JSDoc */));
        if (elideImport) {
          modulesWithElidedImports.set(file.path, true);
        } else if (shouldAddFile) {
          findSourceFile(
            resolvedFileName,
            /*isDefaultLib*/
            false,
            /*ignoreNoDefaultLib*/
            false,
            { kind: 3 /* Import */, file: file.path, index },
            resolution.packageId
          );
        }
        if (isFromNodeModulesSearch) {
          currentNodeModulesDepth--;
        }
      }
    }
  }
  function checkSourceFilesBelongToPath(sourceFiles, rootDirectory) {
    let allFilesBelongToPath ***REMOVED*** true;
    const absoluteRootDirectoryPath ***REMOVED*** host.getCanonicalFileName(getNormalizedAbsolutePath(rootDirectory, currentDirectory));
    for (const sourceFile of sourceFiles) {
      if (!sourceFile.isDeclarationFile) {
        const absoluteSourceFilePath ***REMOVED*** host.getCanonicalFileName(getNormalizedAbsolutePath(sourceFile.fileName, currentDirectory));
        if (absoluteSourceFilePath.indexOf(absoluteRootDirectoryPath) !***REMOVED******REMOVED*** 0) {
          programDiagnostics.addLazyConfigDiagnostic(
            sourceFile,
            Diagnostics.File_0_is_not_under_rootDir_1_rootDir_is_expected_to_contain_all_source_files,
            sourceFile.fileName,
            rootDirectory
          );
          allFilesBelongToPath ***REMOVED*** false;
        }
      }
    }
    return allFilesBelongToPath;
  }
  function parseProjectReferenceConfigFile(ref) {
    if (!projectReferenceRedirects) {
      projectReferenceRedirects ***REMOVED*** /* @__PURE__ */ new Map();
    }
    const refPath ***REMOVED*** resolveProjectReferencePath(ref);
    const sourceFilePath ***REMOVED*** toPath3(refPath);
    const fromCache ***REMOVED*** projectReferenceRedirects.get(sourceFilePath);
    if (fromCache !***REMOVED******REMOVED*** void 0) {
      return fromCache || void 0;
    }
    let commandLine;
    let sourceFile;
    if (host.getParsedCommandLine) {
      commandLine ***REMOVED*** host.getParsedCommandLine(refPath);
      if (!commandLine) {
        addFileToFilesByName(
          /*file*/
          void 0,
          sourceFilePath,
          refPath,
          /*redirectedPath*/
          void 0
        );
        projectReferenceRedirects.set(sourceFilePath, false);
        return void 0;
      }
      sourceFile ***REMOVED*** Debug.checkDefined(commandLine.options.configFile);
      Debug.assert(!sourceFile.path || sourceFile.path ***REMOVED******REMOVED******REMOVED*** sourceFilePath);
      addFileToFilesByName(
        sourceFile,
        sourceFilePath,
        refPath,
        /*redirectedPath*/
        void 0
      );
    } else {
      const basePath ***REMOVED*** getNormalizedAbsolutePath(getDirectoryPath(refPath), currentDirectory);
      sourceFile ***REMOVED*** host.getSourceFile(refPath, 100 /* JSON */);
      addFileToFilesByName(
        sourceFile,
        sourceFilePath,
        refPath,
        /*redirectedPath*/
        void 0
      );
      if (sourceFile ***REMOVED******REMOVED******REMOVED*** void 0) {
        projectReferenceRedirects.set(sourceFilePath, false);
        return void 0;
      }
      commandLine ***REMOVED*** parseJsonSourceFileConfigFileContent(
        sourceFile,
        configParsingHost,
        basePath,
        /*existingOptions*/
        void 0,
        refPath
      );
    }
    sourceFile.fileName ***REMOVED*** refPath;
    sourceFile.path ***REMOVED*** sourceFilePath;
    sourceFile.resolvedPath ***REMOVED*** sourceFilePath;
    sourceFile.originalFileName ***REMOVED*** refPath;
    const resolvedRef ***REMOVED*** { commandLine, sourceFile };
    projectReferenceRedirects.set(sourceFilePath, resolvedRef);
    if (options.configFile !***REMOVED******REMOVED*** sourceFile) {
      mapSourceFileToResolvedRef ?? (mapSourceFileToResolvedRef ***REMOVED*** /* @__PURE__ */ new Map());
      mapOutputFileToResolvedRef ?? (mapOutputFileToResolvedRef ***REMOVED*** /* @__PURE__ */ new Map());
      let outDts;
      if (commandLine.options.outFile) {
        outDts ***REMOVED*** changeExtension(commandLine.options.outFile, ".d.ts" /* Dts */);
        mapOutputFileToResolvedRef ***REMOVED******REMOVED*** null ? void 0 : mapOutputFileToResolvedRef.set(toPath3(outDts), { resolvedRef });
      }
      const getCommonSourceDirectory3 ***REMOVED*** memoize(() ***REMOVED***> getCommonSourceDirectoryOfConfig(resolvedRef.commandLine, !host.useCaseSensitiveFileNames()));
      commandLine.fileNames.forEach((fileName) ***REMOVED***> {
        const path ***REMOVED*** toPath3(fileName);
        let outputDts;
        if (!isDeclarationFileName(fileName) && !fileExtensionIs(fileName, ".json" /* Json */)) {
          if (!commandLine.options.outFile) {
            outputDts ***REMOVED*** getOutputDeclarationFileName(fileName, resolvedRef.commandLine, !host.useCaseSensitiveFileNames(), getCommonSourceDirectory3);
            mapOutputFileToResolvedRef.set(toPath3(outputDts), { resolvedRef, source: fileName });
          } else {
            outputDts ***REMOVED*** outDts;
          }
        }
        mapSourceFileToResolvedRef.set(path, { resolvedRef, outputDts });
      });
    }
    if (commandLine.projectReferences) {
      resolvedRef.references ***REMOVED*** commandLine.projectReferences.map(parseProjectReferenceConfigFile);
    }
    return resolvedRef;
  }
  function verifyCompilerOptions() {
    if (options.strictPropertyInitialization && !getStrictOptionValue(options, "strictNullChecks")) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1, "strictPropertyInitialization", "strictNullChecks");
    }
    if (options.exactOptionalPropertyTypes && !getStrictOptionValue(options, "strictNullChecks")) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1, "exactOptionalPropertyTypes", "strictNullChecks");
    }
    if (options.isolatedModules || options.verbatimModuleSyntax) {
      if (options.outFile) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "outFile", options.verbatimModuleSyntax ? "verbatimModuleSyntax" : "isolatedModules");
      }
    }
    if (options.isolatedDeclarations) {
      if (getAllowJSCompilerOption(options)) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "allowJs", "isolatedDeclarations");
      }
      if (!getEmitDeclarations(options)) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1_or_option_2, "isolatedDeclarations", "declaration", "composite");
      }
    }
    if (options.inlineSourceMap) {
      if (options.sourceMap) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "sourceMap", "inlineSourceMap");
      }
      if (options.mapRoot) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "mapRoot", "inlineSourceMap");
      }
    }
    if (options.composite) {
      if (options.declaration ***REMOVED******REMOVED******REMOVED*** false) {
        createDiagnosticForOptionName(Diagnostics.Composite_projects_may_not_disable_declaration_emit, "declaration");
      }
      if (options.incremental ***REMOVED******REMOVED******REMOVED*** false) {
        createDiagnosticForOptionName(Diagnostics.Composite_projects_may_not_disable_incremental_compilation, "declaration");
      }
    }
    const outputFile ***REMOVED*** options.outFile;
    if (!options.tsBuildInfoFile && options.incremental && !outputFile && !options.configFilePath) {
      programDiagnostics.addConfigDiagnostic(createCompilerDiagnostic(Diagnostics.Option_incremental_can_only_be_specified_using_tsconfig_emitting_to_single_file_or_when_option_tsBuildInfoFile_is_specified));
    }
    verifyDeprecatedCompilerOptions();
    verifyProjectReferences();
    if (options.composite) {
      const rootPaths ***REMOVED*** new Set(rootNames.map(toPath3));
      for (const file of files) {
        if (sourceFileMayBeEmitted(file, program) && !rootPaths.has(file.path)) {
          programDiagnostics.addLazyConfigDiagnostic(
            file,
            Diagnostics.File_0_is_not_listed_within_the_file_list_of_project_1_Projects_must_list_all_files_or_use_an_include_pattern,
            file.fileName,
            options.configFilePath || ""
          );
        }
      }
    }
    if (options.paths) {
      for (const key in options.paths) {
        if (!hasProperty(options.paths, key)) {
          continue;
        }
        if (!hasZeroOrOneAsteriskCharacter(key)) {
          createDiagnosticForOptionPaths(
            /*onKey*/
            true,
            key,
            Diagnostics.Pattern_0_can_have_at_most_one_Asterisk_character,
            key
          );
        }
        if (isArray(options.paths[key])) {
          const len ***REMOVED*** options.paths[key].length;
          if (len ***REMOVED******REMOVED******REMOVED*** 0) {
            createDiagnosticForOptionPaths(
              /*onKey*/
              false,
              key,
              Diagnostics.Substitutions_for_pattern_0_shouldn_t_be_an_empty_array,
              key
            );
          }
          for (let i ***REMOVED*** 0; i < len; i++) {
            const subst ***REMOVED*** options.paths[key][i];
            const typeOfSubst ***REMOVED*** typeof subst;
            if (typeOfSubst ***REMOVED******REMOVED******REMOVED*** "string") {
              if (!hasZeroOrOneAsteriskCharacter(subst)) {
                createDiagnosticForOptionPathKeyValue(key, i, Diagnostics.Substitution_0_in_pattern_1_can_have_at_most_one_Asterisk_character, subst, key);
              }
              if (!options.baseUrl && !pathIsRelative(subst) && !pathIsAbsolute(subst)) {
                createDiagnosticForOptionPathKeyValue(key, i, Diagnostics.Non_relative_paths_are_not_allowed_when_baseUrl_is_not_set_Did_you_forget_a_leading_Slash);
              }
            } else {
              createDiagnosticForOptionPathKeyValue(key, i, Diagnostics.Substitution_0_for_pattern_1_has_incorrect_type_expected_string_got_2, subst, key, typeOfSubst);
            }
          }
        } else {
          createDiagnosticForOptionPaths(
            /*onKey*/
            false,
            key,
            Diagnostics.Substitutions_for_pattern_0_should_be_an_array,
            key
          );
        }
      }
    }
    if (!options.sourceMap && !options.inlineSourceMap) {
      if (options.inlineSources) {
        createDiagnosticForOptionName(Diagnostics.Option_0_can_only_be_used_when_either_option_inlineSourceMap_or_option_sourceMap_is_provided, "inlineSources");
      }
      if (options.sourceRoot) {
        createDiagnosticForOptionName(Diagnostics.Option_0_can_only_be_used_when_either_option_inlineSourceMap_or_option_sourceMap_is_provided, "sourceRoot");
      }
    }
    if (options.mapRoot && !(options.sourceMap || options.declarationMap)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1_or_option_2, "mapRoot", "sourceMap", "declarationMap");
    }
    if (options.declarationDir) {
      if (!getEmitDeclarations(options)) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1_or_option_2, "declarationDir", "declaration", "composite");
      }
      if (outputFile) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "declarationDir", "outFile");
      }
    }
    if (options.declarationMap && !getEmitDeclarations(options)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1_or_option_2, "declarationMap", "declaration", "composite");
    }
    if (options.lib && options.noLib) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "lib", "noLib");
    }
    const languageVersion ***REMOVED*** getEmitScriptTarget(options);
    const firstNonAmbientExternalModuleSourceFile ***REMOVED*** find(files, (f) ***REMOVED***> isExternalModule(f) && !f.isDeclarationFile);
    if (options.isolatedModules || options.verbatimModuleSyntax) {
      if (options.module ***REMOVED******REMOVED******REMOVED*** 0 /* None */ && languageVersion < 2 /* ES2015 */ && options.isolatedModules) {
        createDiagnosticForOptionName(Diagnostics.Option_isolatedModules_can_only_be_used_when_either_option_module_is_provided_or_option_target_is_ES2015_or_higher, "isolatedModules", "target");
      }
      if (options.preserveConstEnums ***REMOVED******REMOVED******REMOVED*** false) {
        createDiagnosticForOptionName(Diagnostics.Option_preserveConstEnums_cannot_be_disabled_when_0_is_enabled, options.verbatimModuleSyntax ? "verbatimModuleSyntax" : "isolatedModules", "preserveConstEnums");
      }
    } else if (firstNonAmbientExternalModuleSourceFile && languageVersion < 2 /* ES2015 */ && options.module ***REMOVED******REMOVED******REMOVED*** 0 /* None */) {
      const span ***REMOVED*** getErrorSpanForNode(firstNonAmbientExternalModuleSourceFile, typeof firstNonAmbientExternalModuleSourceFile.externalModuleIndicator ***REMOVED******REMOVED******REMOVED*** "boolean" ? firstNonAmbientExternalModuleSourceFile : firstNonAmbientExternalModuleSourceFile.externalModuleIndicator);
      programDiagnostics.addConfigDiagnostic(createFileDiagnostic(firstNonAmbientExternalModuleSourceFile, span.start, span.length, Diagnostics.Cannot_use_imports_exports_or_module_augmentations_when_module_is_none));
    }
    if (outputFile && !options.emitDeclarationOnly) {
      if (options.module && !(options.module ***REMOVED******REMOVED******REMOVED*** 2 /* AMD */ || options.module ***REMOVED******REMOVED******REMOVED*** 4 /* System */)) {
        createDiagnosticForOptionName(Diagnostics.Only_amd_and_system_modules_are_supported_alongside_0, "outFile", "module");
      } else if (options.module ***REMOVED******REMOVED******REMOVED*** void 0 && firstNonAmbientExternalModuleSourceFile) {
        const span ***REMOVED*** getErrorSpanForNode(firstNonAmbientExternalModuleSourceFile, typeof firstNonAmbientExternalModuleSourceFile.externalModuleIndicator ***REMOVED******REMOVED******REMOVED*** "boolean" ? firstNonAmbientExternalModuleSourceFile : firstNonAmbientExternalModuleSourceFile.externalModuleIndicator);
        programDiagnostics.addConfigDiagnostic(createFileDiagnostic(firstNonAmbientExternalModuleSourceFile, span.start, span.length, Diagnostics.Cannot_compile_modules_using_option_0_unless_the_module_flag_is_amd_or_system, "outFile"));
      }
    }
    if (getResolveJsonModule(options)) {
      if (getEmitModuleResolutionKind(options) ***REMOVED******REMOVED******REMOVED*** 1 /* Classic */) {
        createDiagnosticForOptionName(Diagnostics.Option_resolveJsonModule_cannot_be_specified_when_moduleResolution_is_set_to_classic, "resolveJsonModule");
      } else if (!hasJsonModuleEmitEnabled(options)) {
        createDiagnosticForOptionName(Diagnostics.Option_resolveJsonModule_cannot_be_specified_when_module_is_set_to_none_system_or_umd, "resolveJsonModule", "module");
      }
    }
    if (options.outDir || // there is --outDir specified
    options.rootDir || // there is --rootDir specified
    options.sourceRoot || // there is --sourceRoot specified
    options.mapRoot || // there is --mapRoot specified
    getEmitDeclarations(options) && options.declarationDir) {
      const dir ***REMOVED*** getCommonSourceDirectory2();
      if (options.outDir && dir ***REMOVED******REMOVED******REMOVED*** "" && files.some((file) ***REMOVED***> getRootLength(file.fileName) > 1)) {
        createDiagnosticForOptionName(Diagnostics.Cannot_find_the_common_subdirectory_path_for_the_input_files, "outDir");
      }
    }
    if (options.checkJs && !getAllowJSCompilerOption(options)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1, "checkJs", "allowJs");
    }
    if (options.emitDeclarationOnly) {
      if (!getEmitDeclarations(options)) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1_or_option_2, "emitDeclarationOnly", "declaration", "composite");
      }
    }
    if (options.emitDecoratorMetadata && !options.experimentalDecorators) {
      createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1, "emitDecoratorMetadata", "experimentalDecorators");
    }
    if (options.jsxFactory) {
      if (options.reactNamespace) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_with_option_1, "reactNamespace", "jsxFactory");
      }
      if (options.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || options.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_when_option_jsx_is_1, "jsxFactory", inverseJsxOptionMap.get("" + options.jsx));
      }
      if (!parseIsolatedEntityName(options.jsxFactory, languageVersion)) {
        createOptionValueDiagnostic("jsxFactory", Diagnostics.Invalid_value_for_jsxFactory_0_is_not_a_valid_identifier_or_qualified_name, options.jsxFactory);
      }
    } else if (options.reactNamespace && !isIdentifierText(options.reactNamespace, languageVersion)) {
      createOptionValueDiagnostic("reactNamespace", Diagnostics.Invalid_value_for_reactNamespace_0_is_not_a_valid_identifier, options.reactNamespace);
    }
    if (options.jsxFragmentFactory) {
      if (!options.jsxFactory) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_without_specifying_option_1, "jsxFragmentFactory", "jsxFactory");
      }
      if (options.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || options.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_when_option_jsx_is_1, "jsxFragmentFactory", inverseJsxOptionMap.get("" + options.jsx));
      }
      if (!parseIsolatedEntityName(options.jsxFragmentFactory, languageVersion)) {
        createOptionValueDiagnostic("jsxFragmentFactory", Diagnostics.Invalid_value_for_jsxFragmentFactory_0_is_not_a_valid_identifier_or_qualified_name, options.jsxFragmentFactory);
      }
    }
    if (options.reactNamespace) {
      if (options.jsx ***REMOVED******REMOVED******REMOVED*** 4 /* ReactJSX */ || options.jsx ***REMOVED******REMOVED******REMOVED*** 5 /* ReactJSXDev */) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_when_option_jsx_is_1, "reactNamespace", inverseJsxOptionMap.get("" + options.jsx));
      }
    }
    if (options.jsxImportSource) {
      if (options.jsx ***REMOVED******REMOVED******REMOVED*** 2 /* React */) {
        createDiagnosticForOptionName(Diagnostics.Option_0_cannot_be_specified_when_option_jsx_is_1, "jsxImportSource", inverseJsxOptionMap.get("" + options.jsx));
      }
    }
    const moduleKind ***REMOVED*** getEmitModuleKind(options);
    if (options.verbatimModuleSyntax) {
      if (moduleKind ***REMOVED******REMOVED******REMOVED*** 2 /* AMD */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 3 /* UMD */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 4 /* System */) {
        createDiagnosticForOptionName(Diagnostics.Option_verbatimModuleSyntax_cannot_be_used_when_module_is_set_to_UMD_AMD_or_System, "verbatimModuleSyntax");
      }
    }
    if (options.allowImportingTsExtensions && !(options.noEmit || options.emitDeclarationOnly || options.rewriteRelativeImportExtensions)) {
      createOptionValueDiagnostic("allowImportingTsExtensions", Diagnostics.Option_allowImportingTsExtensions_can_only_be_used_when_either_noEmit_or_emitDeclarationOnly_is_set);
    }
    const moduleResolution ***REMOVED*** getEmitModuleResolutionKind(options);
    if (options.resolvePackageJsonExports && !moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_can_only_be_used_when_moduleResolution_is_set_to_node16_nodenext_or_bundler, "resolvePackageJsonExports");
    }
    if (options.resolvePackageJsonImports && !moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_can_only_be_used_when_moduleResolution_is_set_to_node16_nodenext_or_bundler, "resolvePackageJsonImports");
    }
    if (options.customConditions && !moduleResolutionSupportsPackageJsonExportsAndImports(moduleResolution)) {
      createDiagnosticForOptionName(Diagnostics.Option_0_can_only_be_used_when_moduleResolution_is_set_to_node16_nodenext_or_bundler, "customConditions");
    }
    if (moduleResolution ***REMOVED******REMOVED******REMOVED*** 100 /* Bundler */ && !emitModuleKindIsNonNodeESM(moduleKind) && moduleKind !***REMOVED******REMOVED*** 200 /* Preserve */) {
      createOptionValueDiagnostic("moduleResolution", Diagnostics.Option_0_can_only_be_used_when_module_is_set_to_preserve_or_to_es2015_or_later, "bundler");
    }
    if (ModuleKind[moduleKind] && (100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) && !(3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */)) {
      const moduleKindName ***REMOVED*** ModuleKind[moduleKind];
      const moduleResolutionName ***REMOVED*** ModuleResolutionKind[moduleKindName] ? moduleKindName : "Node16";
      createOptionValueDiagnostic("moduleResolution", Diagnostics.Option_moduleResolution_must_be_set_to_0_or_left_unspecified_when_option_module_is_set_to_1, moduleResolutionName, moduleKindName);
    } else if (ModuleResolutionKind[moduleResolution] && (3 /* Node16 */ <***REMOVED*** moduleResolution && moduleResolution <***REMOVED*** 99 /* NodeNext */) && !(100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */)) {
      const moduleResolutionName ***REMOVED*** ModuleResolutionKind[moduleResolution];
      createOptionValueDiagnostic("module", Diagnostics.Option_module_must_be_set_to_0_when_option_moduleResolution_is_set_to_1, moduleResolutionName, moduleResolutionName);
    }
    if (!options.noEmit && !options.suppressOutputPathCheck) {
      const emitHost ***REMOVED*** getEmitHost();
      const emitFilesSeen ***REMOVED*** /* @__PURE__ */ new Set();
      forEachEmittedFile(emitHost, (emitFileNames) ***REMOVED***> {
        if (!options.emitDeclarationOnly) {
          verifyEmitFilePath(emitFileNames.jsFilePath, emitFilesSeen);
        }
        verifyEmitFilePath(emitFileNames.declarationFilePath, emitFilesSeen);
      });
    }
    function verifyEmitFilePath(emitFileName, emitFilesSeen) {
      if (emitFileName) {
        const emitFilePath ***REMOVED*** toPath3(emitFileName);
        if (filesByName.has(emitFilePath)) {
          let chain;
          if (!options.configFilePath) {
            chain ***REMOVED*** chainDiagnosticMessages(
              /*details*/
              void 0,
              Diagnostics.Adding_a_tsconfig_json_file_will_help_organize_projects_that_contain_both_TypeScript_and_JavaScript_files_Learn_more_at_https_Colon_Slash_Slashaka_ms_Slashtsconfig
            );
          }
          chain ***REMOVED*** chainDiagnosticMessages(chain, Diagnostics.Cannot_write_file_0_because_it_would_overwrite_input_file, emitFileName);
          blockEmittingOfFile(emitFileName, createCompilerDiagnosticFromMessageChain(chain));
        }
        const emitFileKey ***REMOVED*** !host.useCaseSensitiveFileNames() ? toFileNameLowerCase(emitFilePath) : emitFilePath;
        if (emitFilesSeen.has(emitFileKey)) {
          blockEmittingOfFile(emitFileName, createCompilerDiagnostic(Diagnostics.Cannot_write_file_0_because_it_would_be_overwritten_by_multiple_input_files, emitFileName));
        } else {
          emitFilesSeen.add(emitFileKey);
        }
      }
    }
  }
  function getIgnoreDeprecationsVersion() {
    const ignoreDeprecations ***REMOVED*** options.ignoreDeprecations;
    if (ignoreDeprecations) {
      if (ignoreDeprecations ***REMOVED******REMOVED******REMOVED*** "5.0") {
        return new Version(ignoreDeprecations);
      }
      reportInvalidIgnoreDeprecations();
    }
    return Version.zero;
  }
  function checkDeprecations(deprecatedIn, removedIn, createDiagnostic, fn) {
    const deprecatedInVersion ***REMOVED*** new Version(deprecatedIn);
    const removedInVersion ***REMOVED*** new Version(removedIn);
    const typescriptVersion ***REMOVED*** new Version(typeScriptVersion2 || versionMajorMinor);
    const ignoreDeprecationsVersion ***REMOVED*** getIgnoreDeprecationsVersion();
    const mustBeRemoved ***REMOVED*** !(removedInVersion.compareTo(typescriptVersion) ***REMOVED******REMOVED******REMOVED*** 1 /* GreaterThan */);
    const canBeSilenced ***REMOVED*** !mustBeRemoved && ignoreDeprecationsVersion.compareTo(deprecatedInVersion) ***REMOVED******REMOVED******REMOVED*** -1 /* LessThan */;
    if (mustBeRemoved || canBeSilenced) {
      fn((name, value, useInstead) ***REMOVED***> {
        if (mustBeRemoved) {
          if (value ***REMOVED******REMOVED******REMOVED*** void 0) {
            createDiagnostic(name, value, useInstead, Diagnostics.Option_0_has_been_removed_Please_remove_it_from_your_configuration, name);
          } else {
            createDiagnostic(name, value, useInstead, Diagnostics.Option_0_1_has_been_removed_Please_remove_it_from_your_configuration, name, value);
          }
        } else {
          if (value ***REMOVED******REMOVED******REMOVED*** void 0) {
            createDiagnostic(name, value, useInstead, Diagnostics.Option_0_is_deprecated_and_will_stop_functioning_in_TypeScript_1_Specify_compilerOption_ignoreDeprecations_Colon_2_to_silence_this_error, name, removedIn, deprecatedIn);
          } else {
            createDiagnostic(name, value, useInstead, Diagnostics.Option_0_1_is_deprecated_and_will_stop_functioning_in_TypeScript_2_Specify_compilerOption_ignoreDeprecations_Colon_3_to_silence_this_error, name, value, removedIn, deprecatedIn);
          }
        }
      });
    }
  }
  function verifyDeprecatedCompilerOptions() {
    function createDiagnostic(name, value, useInstead, message, ...args) {
      if (useInstead) {
        const details ***REMOVED*** chainDiagnosticMessages(
          /*details*/
          void 0,
          Diagnostics.Use_0_instead,
          useInstead
        );
        const chain ***REMOVED*** chainDiagnosticMessages(details, message, ...args);
        createDiagnosticForOption(
          /*onKey*/
          !value,
          name,
          /*option2*/
          void 0,
          chain
        );
      } else {
        createDiagnosticForOption(
          /*onKey*/
          !value,
          name,
          /*option2*/
          void 0,
          message,
          ...args
        );
      }
    }
    checkDeprecations("5.0", "5.5", createDiagnostic, (createDeprecatedDiagnostic) ***REMOVED***> {
      if (options.target ***REMOVED******REMOVED******REMOVED*** 0 /* ES3 */) {
        createDeprecatedDiagnostic("target", "ES3");
      }
      if (options.noImplicitUseStrict) {
        createDeprecatedDiagnostic("noImplicitUseStrict");
      }
      if (options.keyofStringsOnly) {
        createDeprecatedDiagnostic("keyofStringsOnly");
      }
      if (options.suppressExcessPropertyErrors) {
        createDeprecatedDiagnostic("suppressExcessPropertyErrors");
      }
      if (options.suppressImplicitAnyIndexErrors) {
        createDeprecatedDiagnostic("suppressImplicitAnyIndexErrors");
      }
      if (options.noStrictGenericChecks) {
        createDeprecatedDiagnostic("noStrictGenericChecks");
      }
      if (options.charset) {
        createDeprecatedDiagnostic("charset");
      }
      if (options.out) {
        createDeprecatedDiagnostic(
          "out",
          /*value*/
          void 0,
          "outFile"
        );
      }
      if (options.importsNotUsedAsValues) {
        createDeprecatedDiagnostic(
          "importsNotUsedAsValues",
          /*value*/
          void 0,
          "verbatimModuleSyntax"
        );
      }
      if (options.preserveValueImports) {
        createDeprecatedDiagnostic(
          "preserveValueImports",
          /*value*/
          void 0,
          "verbatimModuleSyntax"
        );
      }
    });
  }
  function verifyDeprecatedProjectReference(ref, parentFile, index) {
    function createDiagnostic(_name, _value, _useInstead, message, ...args) {
      createDiagnosticForReference(parentFile, index, message, ...args);
    }
    checkDeprecations("5.0", "5.5", createDiagnostic, (createDeprecatedDiagnostic) ***REMOVED***> {
      if (ref.prepend) {
        createDeprecatedDiagnostic("prepend");
      }
    });
  }
  function addFilePreprocessingFileExplainingDiagnostic(file, fileProcessingReason, diagnostic, args) {
    programDiagnostics.addFileProcessingDiagnostic({
      kind: 1 /* FilePreprocessingFileExplainingDiagnostic */,
      file: file && file.path,
      fileProcessingReason,
      diagnostic,
      args
    });
  }
  function verifyProjectReferences() {
    const buildInfoPath ***REMOVED*** !options.suppressOutputPathCheck ? getTsBuildInfoEmitOutputFilePath(options) : void 0;
    forEachProjectReference(
      projectReferences,
      resolvedProjectReferences,
      (resolvedRef, parent, index) ***REMOVED***> {
        const ref ***REMOVED*** (parent ? parent.commandLine.projectReferences : projectReferences)[index];
        const parentFile ***REMOVED*** parent && parent.sourceFile;
        verifyDeprecatedProjectReference(ref, parentFile, index);
        if (!resolvedRef) {
          createDiagnosticForReference(parentFile, index, Diagnostics.File_0_not_found, ref.path);
          return;
        }
        const options2 ***REMOVED*** resolvedRef.commandLine.options;
        if (!options2.composite || options2.noEmit) {
          const inputs ***REMOVED*** parent ? parent.commandLine.fileNames : rootNames;
          if (inputs.length) {
            if (!options2.composite) createDiagnosticForReference(parentFile, index, Diagnostics.Referenced_project_0_must_have_setting_composite_Colon_true, ref.path);
            if (options2.noEmit) createDiagnosticForReference(parentFile, index, Diagnostics.Referenced_project_0_may_not_disable_emit, ref.path);
          }
        }
        if (!parent && buildInfoPath && buildInfoPath ***REMOVED******REMOVED******REMOVED*** getTsBuildInfoEmitOutputFilePath(options2)) {
          createDiagnosticForReference(parentFile, index, Diagnostics.Cannot_write_file_0_because_it_will_overwrite_tsbuildinfo_file_generated_by_referenced_project_1, buildInfoPath, ref.path);
          hasEmitBlockingDiagnostics.set(toPath3(buildInfoPath), true);
        }
      }
    );
  }
  function createDiagnosticForOptionPathKeyValue(key, valueIndex, message, ...args) {
    let needCompilerDiagnostic ***REMOVED*** true;
    forEachOptionPathsSyntax((pathProp) ***REMOVED***> {
      if (isObjectLiteralExpression(pathProp.initializer)) {
        forEachPropertyAssignment(pathProp.initializer, key, (keyProps) ***REMOVED***> {
          const initializer ***REMOVED*** keyProps.initializer;
          if (isArrayLiteralExpression(initializer) && initializer.elements.length > valueIndex) {
            programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeInSourceFile(options.configFile, initializer.elements[valueIndex], message, ...args));
            needCompilerDiagnostic ***REMOVED*** false;
          }
        });
      }
    });
    if (needCompilerDiagnostic) {
      createCompilerOptionsDiagnostic(message, ...args);
    }
  }
  function createDiagnosticForOptionPaths(onKey, key, message, ...args) {
    let needCompilerDiagnostic ***REMOVED*** true;
    forEachOptionPathsSyntax((pathProp) ***REMOVED***> {
      if (isObjectLiteralExpression(pathProp.initializer) && createOptionDiagnosticInObjectLiteralSyntax(
        pathProp.initializer,
        onKey,
        key,
        /*key2*/
        void 0,
        message,
        ...args
      )) {
        needCompilerDiagnostic ***REMOVED*** false;
      }
    });
    if (needCompilerDiagnostic) {
      createCompilerOptionsDiagnostic(message, ...args);
    }
  }
  function forEachOptionPathsSyntax(callback) {
    return forEachOptionsSyntaxByName(getCompilerOptionsObjectLiteralSyntax(), "paths", callback);
  }
  function createDiagnosticForOptionName(message, option1, option2, option3) {
    createDiagnosticForOption(
      /*onKey*/
      true,
      option1,
      option2,
      message,
      option1,
      option2,
      option3
    );
  }
  function createOptionValueDiagnostic(option1, message, ...args) {
    createDiagnosticForOption(
      /*onKey*/
      false,
      option1,
      /*option2*/
      void 0,
      message,
      ...args
    );
  }
  function createDiagnosticForReference(sourceFile, index, message, ...args) {
    const referencesSyntax ***REMOVED*** forEachTsConfigPropArray(sourceFile || options.configFile, "references", (property) ***REMOVED***> isArrayLiteralExpression(property.initializer) ? property.initializer : void 0);
    if (referencesSyntax && referencesSyntax.elements.length > index) {
      programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeInSourceFile(sourceFile || options.configFile, referencesSyntax.elements[index], message, ...args));
    } else {
      programDiagnostics.addConfigDiagnostic(createCompilerDiagnostic(message, ...args));
    }
  }
  function createDiagnosticForOption(onKey, option1, option2, message, ...args) {
    const compilerOptionsObjectLiteralSyntax ***REMOVED*** getCompilerOptionsObjectLiteralSyntax();
    const needCompilerDiagnostic ***REMOVED*** !compilerOptionsObjectLiteralSyntax || !createOptionDiagnosticInObjectLiteralSyntax(compilerOptionsObjectLiteralSyntax, onKey, option1, option2, message, ...args);
    if (needCompilerDiagnostic) {
      createCompilerOptionsDiagnostic(message, ...args);
    }
  }
  function createCompilerOptionsDiagnostic(message, ...args) {
    const compilerOptionsProperty ***REMOVED*** getCompilerOptionsPropertySyntax();
    if (compilerOptionsProperty) {
      if ("messageText" in message) {
        programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeFromMessageChain(options.configFile, compilerOptionsProperty.name, message));
      } else {
        programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeInSourceFile(options.configFile, compilerOptionsProperty.name, message, ...args));
      }
    } else if ("messageText" in message) {
      programDiagnostics.addConfigDiagnostic(createCompilerDiagnosticFromMessageChain(message));
    } else {
      programDiagnostics.addConfigDiagnostic(createCompilerDiagnostic(message, ...args));
    }
  }
  function getCompilerOptionsObjectLiteralSyntax() {
    if (_compilerOptionsObjectLiteralSyntax ***REMOVED******REMOVED******REMOVED*** void 0) {
      const compilerOptionsProperty ***REMOVED*** getCompilerOptionsPropertySyntax();
      _compilerOptionsObjectLiteralSyntax ***REMOVED*** compilerOptionsProperty ? tryCast(compilerOptionsProperty.initializer, isObjectLiteralExpression) || false : false;
    }
    return _compilerOptionsObjectLiteralSyntax || void 0;
  }
  function getCompilerOptionsPropertySyntax() {
    if (_compilerOptionsPropertySyntax ***REMOVED******REMOVED******REMOVED*** void 0) {
      _compilerOptionsPropertySyntax ***REMOVED*** forEachPropertyAssignment(
        getTsConfigObjectLiteralExpression(options.configFile),
        "compilerOptions",
        identity
      ) || false;
    }
    return _compilerOptionsPropertySyntax || void 0;
  }
  function createOptionDiagnosticInObjectLiteralSyntax(objectLiteral, onKey, key1, key2, message, ...args) {
    let needsCompilerDiagnostic ***REMOVED*** false;
    forEachPropertyAssignment(objectLiteral, key1, (prop) ***REMOVED***> {
      if ("messageText" in message) {
        programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeFromMessageChain(options.configFile, onKey ? prop.name : prop.initializer, message));
      } else {
        programDiagnostics.addConfigDiagnostic(createDiagnosticForNodeInSourceFile(options.configFile, onKey ? prop.name : prop.initializer, message, ...args));
      }
      needsCompilerDiagnostic ***REMOVED*** true;
    }, key2);
    return needsCompilerDiagnostic;
  }
  function blockEmittingOfFile(emitFileName, diag2) {
    hasEmitBlockingDiagnostics.set(toPath3(emitFileName), true);
    programDiagnostics.addConfigDiagnostic(diag2);
  }
  function isEmittedFile(file) {
    if (options.noEmit) {
      return false;
    }
    const filePath ***REMOVED*** toPath3(file);
    if (getSourceFileByPath(filePath)) {
      return false;
    }
    const out ***REMOVED*** options.outFile;
    if (out) {
      return isSameFile(filePath, out) || isSameFile(filePath, removeFileExtension(out) + ".d.ts" /* Dts */);
    }
    if (options.declarationDir && containsPath(options.declarationDir, filePath, currentDirectory, !host.useCaseSensitiveFileNames())) {
      return true;
    }
    if (options.outDir) {
      return containsPath(options.outDir, filePath, currentDirectory, !host.useCaseSensitiveFileNames());
    }
    if (fileExtensionIsOneOf(filePath, supportedJSExtensionsFlat) || isDeclarationFileName(filePath)) {
      const filePathWithoutExtension ***REMOVED*** removeFileExtension(filePath);
      return !!getSourceFileByPath(filePathWithoutExtension + ".ts" /* Ts */) || !!getSourceFileByPath(filePathWithoutExtension + ".tsx" /* Tsx */);
    }
    return false;
  }
  function isSameFile(file1, file2) {
    return comparePaths(file1, file2, currentDirectory, !host.useCaseSensitiveFileNames()) ***REMOVED******REMOVED******REMOVED*** 0 /* EqualTo */;
  }
  function getSymlinkCache() {
    if (host.getSymlinkCache) {
      return host.getSymlinkCache();
    }
    if (!symlinks) {
      symlinks ***REMOVED*** createSymlinkCache(currentDirectory, getCanonicalFileName);
    }
    if (files && !symlinks.hasProcessedResolutions()) {
      symlinks.setSymlinksFromResolutions(forEachResolvedModule, forEachResolvedTypeReferenceDirective, automaticTypeDirectiveResolutions);
    }
    return symlinks;
  }
  function getModeForUsageLocation2(file, usage) {
    return getModeForUsageLocationWorker(file, usage, getCompilerOptionsForFile(file));
  }
  function getEmitSyntaxForUsageLocation(file, usage) {
    return getEmitSyntaxForUsageLocationWorker(file, usage, getCompilerOptionsForFile(file));
  }
  function getModeForResolutionAtIndex(file, index) {
    return getModeForUsageLocation2(file, getModuleNameStringLiteralAt(file, index));
  }
  function getDefaultResolutionModeForFile2(sourceFile) {
    return getDefaultResolutionModeForFileWorker(sourceFile, getCompilerOptionsForFile(sourceFile));
  }
  function getImpliedNodeFormatForEmit(sourceFile) {
    return getImpliedNodeFormatForEmitWorker(sourceFile, getCompilerOptionsForFile(sourceFile));
  }
  function getEmitModuleFormatOfFile(sourceFile) {
    return getEmitModuleFormatOfFileWorker(sourceFile, getCompilerOptionsForFile(sourceFile));
  }
  function shouldTransformImportCall(sourceFile) {
    return shouldTransformImportCallWorker(sourceFile, getCompilerOptionsForFile(sourceFile));
  }
  function getModeForTypeReferenceDirectiveInFile(ref, sourceFile) {
    return ref.resolutionMode || getDefaultResolutionModeForFile2(sourceFile);
  }
}
function shouldTransformImportCallWorker(sourceFile, options) {
  const moduleKind ***REMOVED*** getEmitModuleKind(options);
  if (100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */ || moduleKind ***REMOVED******REMOVED******REMOVED*** 200 /* Preserve */) {
    return false;
  }
  return getEmitModuleFormatOfFileWorker(sourceFile, options) < 5 /* ES2015 */;
}
function getEmitModuleFormatOfFileWorker(sourceFile, options) {
  return getImpliedNodeFormatForEmitWorker(sourceFile, options) ?? getEmitModuleKind(options);
}
function getImpliedNodeFormatForEmitWorker(sourceFile, options) {
  var _a, _b;
  const moduleKind ***REMOVED*** getEmitModuleKind(options);
  if (100 /* Node16 */ <***REMOVED*** moduleKind && moduleKind <***REMOVED*** 199 /* NodeNext */) {
    return sourceFile.impliedNodeFormat;
  }
  if (sourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 1 /* CommonJS */ && (((_a ***REMOVED*** sourceFile.packageJsonScope) ***REMOVED******REMOVED*** null ? void 0 : _a.contents.packageJsonContent.type) ***REMOVED******REMOVED******REMOVED*** "commonjs" || fileExtensionIsOneOf(sourceFile.fileName, [".cjs" /* Cjs */, ".cts" /* Cts */]))) {
    return 1 /* CommonJS */;
  }
  if (sourceFile.impliedNodeFormat ***REMOVED******REMOVED******REMOVED*** 99 /* ESNext */ && (((_b ***REMOVED*** sourceFile.packageJsonScope) ***REMOVED******REMOVED*** null ? void 0 : _b.contents.packageJsonContent.type) ***REMOVED******REMOVED******REMOVED*** "module" || fileExtensionIsOneOf(sourceFile.fileName, [".mjs" /* Mjs */, ".mts" /* Mts */]))) {
    return 99 /* ESNext */;
  }
  return void 0;
}
function getDefaultResolutionModeForFileWorker(sourceFile, options) {
  return importSyntaxAffectsModuleResolution(options) ? getImpliedNodeFormatForEmitWorker(sourceFile, options) : void 0;
}
function updateHostForUseSourceOfProjectReferenceRedirect(host) {
  let setOfDeclarationDirectories;
  const originalFileExists ***REMOVED*** host.compilerHost.fileExists;
  const originalDirectoryExists ***REMOVED*** host.compilerHost.directoryExists;
  const originalGetDirectories ***REMOVED*** host.compilerHost.getDirectories;
  const originalRealpath ***REMOVED*** host.compilerHost.realpath;
  if (!host.useSourceOfProjectReferenceRedirect) return { onProgramCreateComplete: noop, fileExists };
  host.compilerHost.fileExists ***REMOVED*** fileExists;
  let directoryExists;
  if (originalDirectoryExists) {
    directoryExists ***REMOVED*** host.compilerHost.directoryExists ***REMOVED*** (path) ***REMOVED***> {
      if (originalDirectoryExists.call(host.compilerHost, path)) {
        handleDirectoryCouldBeSymlink(path);
        return true;
      }
      if (!host.getResolvedProjectReferences()) return false;
      if (!setOfDeclarationDirectories) {
        setOfDeclarationDirectories ***REMOVED*** /* @__PURE__ */ new Set();
        host.forEachResolvedProjectReference((ref) ***REMOVED***> {
          const out ***REMOVED*** ref.commandLine.options.outFile;
          if (out) {
            setOfDeclarationDirectories.add(getDirectoryPath(host.toPath(out)));
          } else {
            const declarationDir ***REMOVED*** ref.commandLine.options.declarationDir || ref.commandLine.options.outDir;
            if (declarationDir) {
              setOfDeclarationDirectories.add(host.toPath(declarationDir));
            }
          }
        });
      }
      return fileOrDirectoryExistsUsingSource(
        path,
        /*isFile*/
        false
      );
    };
  }
  if (originalGetDirectories) {
    host.compilerHost.getDirectories ***REMOVED*** (path) ***REMOVED***> !host.getResolvedProjectReferences() || originalDirectoryExists && originalDirectoryExists.call(host.compilerHost, path) ? originalGetDirectories.call(host.compilerHost, path) : [];
  }
  if (originalRealpath) {
    host.compilerHost.realpath ***REMOVED*** (s) ***REMOVED***> {
      var _a;
      return ((_a ***REMOVED*** host.getSymlinkCache().getSymlinkedFiles()) ***REMOVED******REMOVED*** null ? void 0 : _a.get(host.toPath(s))) || originalRealpath.call(host.compilerHost, s);
    };
  }
  return { onProgramCreateComplete, fileExists, directoryExists };
  function onProgramCreateComplete() {
    host.compilerHost.fileExists ***REMOVED*** originalFileExists;
    host.compilerHost.directoryExists ***REMOVED*** originalDirectoryExists;
    host.compilerHost.getDirectories ***REMOVED*** originalGetDirectories;
  }
  function fileExists(file) {
    if (originalFileExists.call(host.compilerHost, file)) return true;
    if (!host.getResolvedProjectReferences()) return false;
    if (!isDeclarationFileName(file)) return false;
    return fileOrDirectoryExistsUsingSource(
      file,
      /*isFile*/
      true
    );
  }
  function fileExistsIfProjectReferenceDts(file) {
    const source ***REMOVED*** host.getRedirectFromOutput(host.toPath(file));
    return source !***REMOVED******REMOVED*** void 0 ? isString(source.source) ? originalFileExists.call(host.compilerHost, source.source) : true : void 0;
  }
  function directoryExistsIfProjectReferenceDeclDir(dir) {
    const dirPath ***REMOVED*** host.toPath(dir);
    const dirPathWithTrailingDirectorySeparator ***REMOVED*** `${dirPath}${directorySeparator}`;
    return forEachKey(
      setOfDeclarationDirectories,
      (declDirPath) ***REMOVED***> dirPath ***REMOVED******REMOVED******REMOVED*** declDirPath || // Any parent directory of declaration dir
      startsWith(declDirPath, dirPathWithTrailingDirectorySeparator) || // Any directory inside declaration dir
      startsWith(dirPath, `${declDirPath}/`)
    );
  }
  function handleDirectoryCouldBeSymlink(directory) {
    var _a;
    if (!host.getResolvedProjectReferences() || containsIgnoredPath(directory)) return;
    if (!originalRealpath || !directory.includes(nodeModulesPathPart)) return;
    const symlinkCache ***REMOVED*** host.getSymlinkCache();
    const directoryPath ***REMOVED*** ensureTrailingDirectorySeparator(host.toPath(directory));
    if ((_a ***REMOVED*** symlinkCache.getSymlinkedDirectories()) ***REMOVED******REMOVED*** null ? void 0 : _a.has(directoryPath)) return;
    const real ***REMOVED*** normalizePath(originalRealpath.call(host.compilerHost, directory));
    let realPath2;
    if (real ***REMOVED******REMOVED******REMOVED*** directory || (realPath2 ***REMOVED*** ensureTrailingDirectorySeparator(host.toPath(real))) ***REMOVED******REMOVED******REMOVED*** directoryPath) {
      symlinkCache.setSymlinkedDirectory(directoryPath, false);
      return;
    }
    symlinkCache.setSymlinkedDirectory(directory, {
      real: ensureTrailingDirectorySeparator(real),
      realPath: realPath2
    });
  }
  function fileOrDirectoryExistsUsingSource(fileOrDirectory, isFile) {
    var _a;
    const fileOrDirectoryExistsUsingSource2 ***REMOVED*** isFile ? fileExistsIfProjectReferenceDts : directoryExistsIfProjectReferenceDeclDir;
    const result ***REMOVED*** fileOrDirectoryExistsUsingSource2(fileOrDirectory);
    if (result !***REMOVED******REMOVED*** void 0) return result;
    const symlinkCache ***REMOVED*** host.getSymlinkCache();
    const symlinkedDirectories ***REMOVED*** symlinkCache.getSymlinkedDirectories();
    if (!symlinkedDirectories) return false;
    const fileOrDirectoryPath ***REMOVED*** host.toPath(fileOrDirectory);
    if (!fileOrDirectoryPath.includes(nodeModulesPathPart)) return false;
    if (isFile && ((_a ***REMOVED*** symlinkCache.getSymlinkedFiles()) ***REMOVED******REMOVED*** null ? void 0 : _a.has(fileOrDirectoryPath))) return true;
    return firstDefinedIterator(
      symlinkedDirectories.entries(),
      ([directoryPath, symlinkedDirectory]) ***REMOVED***> {
        if (!symlinkedDirectory || !startsWith(fileOrDirectoryPath, directoryPath)) return void 0;
        const result2 ***REMOVED*** fileOrDirectoryExistsUsingSource2(fileOrDirectoryPath.replace(directoryPath, symlinkedDirectory.realPath));
        if (isFile && result2) {
          const absolutePath ***REMOVED*** getNormalizedAbsolutePath(fileOrDirectory, host.compilerHost.getCurrentDirectory());
          symlinkCache.setSymlinkedFile(
            fileOrDirectoryPath,
            `${symlinkedDirectory.real}${absolutePath.replace(new RegExp(directoryPath, "i"), "")}`
          );
        }
        return result2;
      }
    ) || false;
  }
}
var emitSkippedWithNoDiagnostics ***REMOVED*** { diagnostics: emptyArray, sourceMaps: void 0, emittedFiles: void 0, emitSkipped: true };
function handleNoEmitOptions(program, sourceFile, writeFile2, cancellationToken) {
  const options ***REMOVED*** program.getCompilerOptions();
  if (options.noEmit) {
    return sourceFile ? emitSkippedWithNoDiagnostics : program.emitBuildInfo(writeFile2, cancellationToken);
  }
  if (!options.noEmitOnError) return void 0;
  let diagnostics ***REMOVED*** [
    ...program.getOptionsDiagnostics(cancellationToken),
    ...program.getSyntacticDiagnostics(sourceFile, cancellationToken),
    ...program.getGlobalDiagnostics(cancellationToken),
    ...program.getSemanticDiagnostics(sourceFile, cancellationToken)
  ];
  if (diagnostics.length ***REMOVED******REMOVED******REMOVED*** 0 && getEmitDeclarations(program.getCompilerOptions())) {
    diagnostics ***REMOVED*** program.getDeclarationDiagnostics(
      /*sourceFile*/
      void 0,
      cancellationToken
    );
  }
  if (!diagnostics.length) return void 0;
  let emittedFiles;
  if (!sourceFile) {
    const emitResult ***REMOVED*** program.emitBuildInfo(writeFile2, cancellationToken);
    if (emitResult.diagnostics) diagnostics ***REMOVED*** [...diagnostics, ...emitResult.diagnostics];
    emittedFiles ***REMOVED*** emitResult.emittedFiles;
  }
  return { diagnostics, sourceMaps: void 0, emittedFiles, emitSkipped: true };
}
function filterSemanticDiagnostics(diagnostic, option) {
  return filter(diagnostic, (d) ***REMOVED***> !d.skippedOn || !option[d.skippedOn]);
}
function parseConfigHostFromCompilerHostLike(host, directoryStructureHost ***REMOVED*** host) {
  return {
    fileExists: (f) ***REMOVED***> directoryStructureHost.fileExists(f),
    readDirectory(root, extensions, excludes, includes, depth) {
      Debug.assertIsDefined(directoryStructureHost.readDirectory, "'CompilerHost.readDirectory' must be implemented to correctly process 'projectReferences'");
      return directoryStructureHost.readDirectory(root, extensions, excludes, includes, depth);
    },
    readFile: (f) ***REMOVED***> directoryStructureHost.readFile(f),
    directoryExists: maybeBind(directoryStructureHost, directoryStructureHost.directoryExists),
    getDirectories: maybeBind(directoryStructureHost, directoryStructureHost.getDirectories),
    realpath: maybeBind(directoryStructureHost, directoryStructureHost.realpath),
    useCaseSensitiveFileNames: host.useCaseSensitiveFileNames(),
    getCurrentDirectory: () ***REMOVED***> host.getCurrentDirectory(),
    onUnRecoverableConfigFileDiagnostic: host.onUnRecoverableConfigFileDiagnostic || returnUndefined,
    trace: host.trace ? (s) ***REMOVED***> host.trace(s) : void 0
  };
}
function resolveProjectReferencePath(ref) {
  return resolveConfigFileProjectName(ref.path);
}
function getResolutionDiagnostic(options, { extension }, { isDeclarationFile }) {
  switch (extension) {
    case ".ts" /* Ts */:
    case ".d.ts" /* Dts */:
    case ".mts" /* Mts */:
    case ".d.mts" /* Dmts */:
    case ".cts" /* Cts */:
    case ".d.cts" /* Dcts */:
      return void 0;
    case ".tsx" /* Tsx */:
      return needJsx();
    case ".jsx" /* Jsx */:
      return needJsx() || needAllowJs();
    case ".js" /* Js */:
    case ".mjs" /* Mjs */:
    case ".cjs" /* Cjs */:
      return needAllowJs();
    case ".json" /* Json */:
      return needResolveJsonModule();
    default:
      return needAllowArbitraryExtensions();
  }
  function needJsx() {
    return options.jsx ? void 0 : Diagnostics.Module_0_was_resolved_to_1_but_jsx_is_not_set;
  }
  function needAllowJs() {
    return getAllowJSCompilerOption(options) || !getStrictOptionValue(options, "noImplicitAny") ? void 0 : Diagnostics.Could_not_find_a_declaration_file_for_module_0_1_implicitly_has_an_any_type;
  }
  function needResolveJsonModule() {
    return getResolveJsonModule(options) ? void 0 : Diagnostics.Module_0_was_resolved_to_1_but_resolveJsonModule_is_not_used;
  }
  function needAllowArbitraryExtensions() {
    return isDeclarationFile || options.allowArbitraryExtensions ? void 0 : Diagnostics.Module_0_was_resolved_to_1_but_allowArbitraryExtensions_is_not_set;
  }
}
function getModuleNames({ imports, moduleAugmentations }) {
  const res ***REMOVED*** imports.map((i) ***REMOVED***> i);
  for (const aug of moduleAugmentations) {
    if (aug.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      res.push(aug);
    }
  }
  return res;
}
function getModuleNameStringLiteralAt({ imports, moduleAugmentations }, index) {
  if (index < imports.length) return imports[index];
  let augIndex ***REMOVED*** imports.length;
  for (const aug of moduleAugmentations) {
    if (aug.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */) {
      if (index ***REMOVED******REMOVED******REMOVED*** augIndex) return aug;
      augIndex++;
    }
  }
  Debug.fail("should never ask for module name at index higher than possible module name");
}

// src/compiler/programDiagnostics.ts
function createProgramDiagnostics(getCompilerOptionsObjectLiteralSyntax) {
  let computedDiagnostics;
  let fileReasons ***REMOVED*** createMultiMap();
  let fileProcessingDiagnostics;
  let commonSourceDirectory;
  let configDiagnostics;
  let lazyConfigDiagnostics;
  let fileReasonsToChain;
  let reasonToRelatedInfo;
  return {
    addConfigDiagnostic(diag2) {
      Debug.assert(computedDiagnostics ***REMOVED******REMOVED******REMOVED*** void 0, "Cannot modify program diagnostic state after requesting combined diagnostics");
      (configDiagnostics ?? (configDiagnostics ***REMOVED*** createDiagnosticCollection())).add(diag2);
    },
    addLazyConfigDiagnostic(file, message, ...args) {
      Debug.assert(computedDiagnostics ***REMOVED******REMOVED******REMOVED*** void 0, "Cannot modify program diagnostic state after requesting combined diagnostics");
      (lazyConfigDiagnostics ?? (lazyConfigDiagnostics ***REMOVED*** [])).push({ file, diagnostic: message, args });
    },
    addFileProcessingDiagnostic(diag2) {
      Debug.assert(computedDiagnostics ***REMOVED******REMOVED******REMOVED*** void 0, "Cannot modify program diagnostic state after requesting combined diagnostics");
      (fileProcessingDiagnostics ?? (fileProcessingDiagnostics ***REMOVED*** [])).push(diag2);
    },
    setCommonSourceDirectory(directory) {
      commonSourceDirectory ***REMOVED*** directory;
    },
    reuseStateFromOldProgram(oldProgramDiagnostics, isConfigIdentical) {
      fileReasons ***REMOVED*** oldProgramDiagnostics.getFileReasons();
      fileProcessingDiagnostics ***REMOVED*** oldProgramDiagnostics.getFileProcessingDiagnostics();
      if (isConfigIdentical) {
        commonSourceDirectory ***REMOVED*** oldProgramDiagnostics.getCommonSourceDirectory();
        configDiagnostics ***REMOVED*** oldProgramDiagnostics.getConfigDiagnostics();
        lazyConfigDiagnostics ***REMOVED*** oldProgramDiagnostics.getLazyConfigDiagnostics();
      }
    },
    getFileProcessingDiagnostics() {
      return fileProcessingDiagnostics;
    },
    getFileReasons() {
      return fileReasons;
    },
    getCommonSourceDirectory() {
      return commonSourceDirectory;
    },
    getConfigDiagnostics() {
      return configDiagnostics;
    },
    getLazyConfigDiagnostics() {
      return lazyConfigDiagnostics;
    },
    getCombinedDiagnostics(program) {
      if (computedDiagnostics) {
        return computedDiagnostics;
      }
      computedDiagnostics ***REMOVED*** createDiagnosticCollection();
      configDiagnostics ***REMOVED******REMOVED*** null ? void 0 : configDiagnostics.getDiagnostics().forEach((d) ***REMOVED***> computedDiagnostics.add(d));
      fileProcessingDiagnostics ***REMOVED******REMOVED*** null ? void 0 : fileProcessingDiagnostics.forEach((diagnostic) ***REMOVED***> {
        switch (diagnostic.kind) {
          case 1 /* FilePreprocessingFileExplainingDiagnostic */:
            return computedDiagnostics.add(
              createDiagnosticExplainingFile(
                program,
                diagnostic.file && program.getSourceFileByPath(diagnostic.file),
                diagnostic.fileProcessingReason,
                diagnostic.diagnostic,
                diagnostic.args || emptyArray
              )
            );
          case 0 /* FilePreprocessingLibReferenceDiagnostic */:
            return computedDiagnostics.add(filePreprocessingLibreferenceDiagnostic(program, diagnostic));
          case 2 /* ResolutionDiagnostics */:
            return diagnostic.diagnostics.forEach((d) ***REMOVED***> computedDiagnostics.add(d));
          default:
            Debug.assertNever(diagnostic);
        }
      });
      lazyConfigDiagnostics ***REMOVED******REMOVED*** null ? void 0 : lazyConfigDiagnostics.forEach(
        ({ file, diagnostic, args }) ***REMOVED***> computedDiagnostics.add(
          createDiagnosticExplainingFile(
            program,
            file,
            /*fileProcessingReason*/
            void 0,
            diagnostic,
            args
          )
        )
      );
      fileReasonsToChain ***REMOVED*** void 0;
      reasonToRelatedInfo ***REMOVED*** void 0;
      return computedDiagnostics;
    }
  };
  function filePreprocessingLibreferenceDiagnostic(program, { reason }) {
    const { file, pos, end } ***REMOVED*** getReferencedFileLocation(program, reason);
    const libReference ***REMOVED*** file.libReferenceDirectives[reason.index];
    const libName ***REMOVED*** getLibNameFromLibReference(libReference);
    const unqualifiedLibName ***REMOVED*** removeSuffix(removePrefix(libName, "lib."), ".d.ts");
    const suggestion ***REMOVED*** getSpellingSuggestion(unqualifiedLibName, libs, identity);
    return createFileDiagnostic(
      file,
      Debug.checkDefined(pos),
      Debug.checkDefined(end) - pos,
      suggestion ? Diagnostics.Cannot_find_lib_definition_for_0_Did_you_mean_1 : Diagnostics.Cannot_find_lib_definition_for_0,
      libName,
      suggestion
    );
  }
  function createDiagnosticExplainingFile(program, file, fileProcessingReason, diagnostic, args) {
    let seenReasons;
    let fileIncludeReasons;
    let relatedInfo;
    let fileIncludeReasonDetails;
    let redirectInfo;
    let chain;
    const reasons ***REMOVED*** file && fileReasons.get(file.path);
    let locationReason ***REMOVED*** isReferencedFile(fileProcessingReason) ? fileProcessingReason : void 0;
    let cachedChain ***REMOVED*** file && (fileReasonsToChain ***REMOVED******REMOVED*** null ? void 0 : fileReasonsToChain.get(file.path));
    if (cachedChain) {
      if (cachedChain.fileIncludeReasonDetails) {
        seenReasons ***REMOVED*** new Set(reasons);
        reasons ***REMOVED******REMOVED*** null ? void 0 : reasons.forEach(populateRelatedInfo);
      } else {
        reasons ***REMOVED******REMOVED*** null ? void 0 : reasons.forEach(processReason);
      }
      redirectInfo ***REMOVED*** cachedChain.redirectInfo;
    } else {
      reasons ***REMOVED******REMOVED*** null ? void 0 : reasons.forEach(processReason);
      redirectInfo ***REMOVED*** file && explainIfFileIsRedirectAndImpliedFormat(file, program.getCompilerOptionsForFile(file));
    }
    if (fileProcessingReason) processReason(fileProcessingReason);
    const processedExtraReason ***REMOVED*** (seenReasons ***REMOVED******REMOVED*** null ? void 0 : seenReasons.size) !***REMOVED******REMOVED*** (reasons ***REMOVED******REMOVED*** null ? void 0 : reasons.length);
    if (locationReason && (seenReasons ***REMOVED******REMOVED*** null ? void 0 : seenReasons.size) ***REMOVED******REMOVED******REMOVED*** 1) seenReasons ***REMOVED*** void 0;
    if (seenReasons && cachedChain) {
      if (cachedChain.details && !processedExtraReason) {
        chain ***REMOVED*** chainDiagnosticMessages(cachedChain.details, diagnostic, ...args ?? emptyArray);
      } else if (cachedChain.fileIncludeReasonDetails) {
        if (!processedExtraReason) {
          if (!cachedFileIncludeDetailsHasProcessedExtraReason()) {
            fileIncludeReasonDetails ***REMOVED*** cachedChain.fileIncludeReasonDetails;
          } else {
            fileIncludeReasons ***REMOVED*** cachedChain.fileIncludeReasonDetails.next.slice(0, reasons.length);
          }
        } else {
          if (!cachedFileIncludeDetailsHasProcessedExtraReason()) {
            fileIncludeReasons ***REMOVED*** [...cachedChain.fileIncludeReasonDetails.next, fileIncludeReasons[0]];
          } else {
            fileIncludeReasons ***REMOVED*** append(cachedChain.fileIncludeReasonDetails.next.slice(0, reasons.length), fileIncludeReasons[0]);
          }
        }
      }
    }
    if (!chain) {
      if (!fileIncludeReasonDetails) fileIncludeReasonDetails ***REMOVED*** seenReasons && chainDiagnosticMessages(fileIncludeReasons, Diagnostics.The_file_is_in_the_program_because_Colon);
      chain ***REMOVED*** chainDiagnosticMessages(
        redirectInfo ? fileIncludeReasonDetails ? [fileIncludeReasonDetails, ...redirectInfo] : redirectInfo : fileIncludeReasonDetails,
        diagnostic,
        ...args || emptyArray
      );
    }
    if (file) {
      if (cachedChain) {
        if (!cachedChain.fileIncludeReasonDetails || !processedExtraReason && fileIncludeReasonDetails) {
          cachedChain.fileIncludeReasonDetails ***REMOVED*** fileIncludeReasonDetails;
        }
      } else {
        (fileReasonsToChain ?? (fileReasonsToChain ***REMOVED*** /* @__PURE__ */ new Map())).set(file.path, cachedChain ***REMOVED*** { fileIncludeReasonDetails, redirectInfo });
      }
      if (!cachedChain.details && !processedExtraReason) cachedChain.details ***REMOVED*** chain.next;
    }
    const location ***REMOVED*** locationReason && getReferencedFileLocation(program, locationReason);
    return location && isReferenceFileLocation(location) ? createFileDiagnosticFromMessageChain(location.file, location.pos, location.end - location.pos, chain, relatedInfo) : createCompilerDiagnosticFromMessageChain(chain, relatedInfo);
    function processReason(reason) {
      if (seenReasons ***REMOVED******REMOVED*** null ? void 0 : seenReasons.has(reason)) return;
      (seenReasons ?? (seenReasons ***REMOVED*** /* @__PURE__ */ new Set())).add(reason);
      (fileIncludeReasons ?? (fileIncludeReasons ***REMOVED*** [])).push(fileIncludeReasonToDiagnostics(program, reason));
      populateRelatedInfo(reason);
    }
    function populateRelatedInfo(reason) {
      if (!locationReason && isReferencedFile(reason)) {
        locationReason ***REMOVED*** reason;
      } else if (locationReason !***REMOVED******REMOVED*** reason) {
        relatedInfo ***REMOVED*** append(relatedInfo, getFileIncludeReasonToRelatedInformation(program, reason));
      }
    }
    function cachedFileIncludeDetailsHasProcessedExtraReason() {
      var _a;
      return ((_a ***REMOVED*** cachedChain.fileIncludeReasonDetails.next) ***REMOVED******REMOVED*** null ? void 0 : _a.length) !***REMOVED******REMOVED*** (reasons ***REMOVED******REMOVED*** null ? void 0 : reasons.length);
    }
  }
  function getFileIncludeReasonToRelatedInformation(program, reason) {
    let relatedInfo ***REMOVED*** reasonToRelatedInfo ***REMOVED******REMOVED*** null ? void 0 : reasonToRelatedInfo.get(reason);
    if (relatedInfo ***REMOVED******REMOVED******REMOVED*** void 0) (reasonToRelatedInfo ?? (reasonToRelatedInfo ***REMOVED*** /* @__PURE__ */ new Map())).set(reason, relatedInfo ***REMOVED*** fileIncludeReasonToRelatedInformation(program, reason) ?? false);
    return relatedInfo || void 0;
  }
  function fileIncludeReasonToRelatedInformation(program, reason) {
    if (isReferencedFile(reason)) {
      const referenceLocation ***REMOVED*** getReferencedFileLocation(program, reason);
      let message2;
      switch (reason.kind) {
        case 3 /* Import */:
          message2 ***REMOVED*** Diagnostics.File_is_included_via_import_here;
          break;
        case 4 /* ReferenceFile */:
          message2 ***REMOVED*** Diagnostics.File_is_included_via_reference_here;
          break;
        case 5 /* TypeReferenceDirective */:
          message2 ***REMOVED*** Diagnostics.File_is_included_via_type_library_reference_here;
          break;
        case 7 /* LibReferenceDirective */:
          message2 ***REMOVED*** Diagnostics.File_is_included_via_library_reference_here;
          break;
        default:
          Debug.assertNever(reason);
      }
      return isReferenceFileLocation(referenceLocation) ? createFileDiagnostic(
        referenceLocation.file,
        referenceLocation.pos,
        referenceLocation.end - referenceLocation.pos,
        message2
      ) : void 0;
    }
    const currentDirectory ***REMOVED*** program.getCurrentDirectory();
    const rootNames ***REMOVED*** program.getRootFileNames();
    const options ***REMOVED*** program.getCompilerOptions();
    if (!options.configFile) return void 0;
    let configFileNode;
    let message;
    switch (reason.kind) {
      case 0 /* RootFile */:
        if (!options.configFile.configFileSpecs) return void 0;
        const fileName ***REMOVED*** getNormalizedAbsolutePath(rootNames[reason.index], currentDirectory);
        const matchedByFiles ***REMOVED*** getMatchedFileSpec(program, fileName);
        if (matchedByFiles) {
          configFileNode ***REMOVED*** getTsConfigPropArrayElementValue(options.configFile, "files", matchedByFiles);
          message ***REMOVED*** Diagnostics.File_is_matched_by_files_list_specified_here;
          break;
        }
        const matchedByInclude ***REMOVED*** getMatchedIncludeSpec(program, fileName);
        if (!matchedByInclude || !isString(matchedByInclude)) return void 0;
        configFileNode ***REMOVED*** getTsConfigPropArrayElementValue(options.configFile, "include", matchedByInclude);
        message ***REMOVED*** Diagnostics.File_is_matched_by_include_pattern_specified_here;
        break;
      case 1 /* SourceFromProjectReference */:
      case 2 /* OutputFromProjectReference */:
        const resolvedProjectReferences ***REMOVED*** program.getResolvedProjectReferences();
        const projectReferences ***REMOVED*** program.getProjectReferences();
        const referencedResolvedRef ***REMOVED*** Debug.checkDefined(resolvedProjectReferences ***REMOVED******REMOVED*** null ? void 0 : resolvedProjectReferences[reason.index]);
        const referenceInfo ***REMOVED*** forEachProjectReference(
          projectReferences,
          resolvedProjectReferences,
          (resolvedRef, parent, index2) ***REMOVED***> resolvedRef ***REMOVED******REMOVED******REMOVED*** referencedResolvedRef ? { sourceFile: (parent ***REMOVED******REMOVED*** null ? void 0 : parent.sourceFile) || options.configFile, index: index2 } : void 0
        );
        if (!referenceInfo) return void 0;
        const { sourceFile, index } ***REMOVED*** referenceInfo;
        const referencesSyntax ***REMOVED*** forEachTsConfigPropArray(sourceFile, "references", (property) ***REMOVED***> isArrayLiteralExpression(property.initializer) ? property.initializer : void 0);
        return referencesSyntax && referencesSyntax.elements.length > index ? createDiagnosticForNodeInSourceFile(
          sourceFile,
          referencesSyntax.elements[index],
          reason.kind ***REMOVED******REMOVED******REMOVED*** 2 /* OutputFromProjectReference */ ? Diagnostics.File_is_output_from_referenced_project_specified_here : Diagnostics.File_is_source_from_referenced_project_specified_here
        ) : void 0;
      case 8 /* AutomaticTypeDirectiveFile */:
        if (!options.types) return void 0;
        configFileNode ***REMOVED*** getOptionsSyntaxByArrayElementValue(getCompilerOptionsObjectLiteralSyntax(), "types", reason.typeReference);
        message ***REMOVED*** Diagnostics.File_is_entry_point_of_type_library_specified_here;
        break;
      case 6 /* LibFile */:
        if (reason.index !***REMOVED******REMOVED*** void 0) {
          configFileNode ***REMOVED*** getOptionsSyntaxByArrayElementValue(getCompilerOptionsObjectLiteralSyntax(), "lib", options.lib[reason.index]);
          message ***REMOVED*** Diagnostics.File_is_library_specified_here;
          break;
        }
        const target ***REMOVED*** getNameOfScriptTarget(getEmitScriptTarget(options));
        configFileNode ***REMOVED*** target ? getOptionsSyntaxByValue(getCompilerOptionsObjectLiteralSyntax(), "target", target) : void 0;
        message ***REMOVED*** Diagnostics.File_is_default_library_for_target_specified_here;
        break;
      default:
        Debug.assertNever(reason);
    }
    return configFileNode && createDiagnosticForNodeInSourceFile(
      options.configFile,
      configFileNode,
      message
    );
  }
}

// src/compiler/builderState.ts
var BuilderState;
((BuilderState2) ***REMOVED***> {
  function createManyToManyPathMap() {
    function create2(forward, reverse, deleted) {
      const map2 ***REMOVED*** {
        getKeys: (v) ***REMOVED***> reverse.get(v),
        getValues: (k) ***REMOVED***> forward.get(k),
        keys: () ***REMOVED***> forward.keys(),
        size: () ***REMOVED***> forward.size,
        deleteKey: (k) ***REMOVED***> {
          (deleted || (deleted ***REMOVED*** /* @__PURE__ */ new Set())).add(k);
          const set ***REMOVED*** forward.get(k);
          if (!set) {
            return false;
          }
          set.forEach((v) ***REMOVED***> deleteFromMultimap(reverse, v, k));
          forward.delete(k);
          return true;
        },
        set: (k, vSet) ***REMOVED***> {
          deleted ***REMOVED******REMOVED*** null ? void 0 : deleted.delete(k);
          const existingVSet ***REMOVED*** forward.get(k);
          forward.set(k, vSet);
          existingVSet ***REMOVED******REMOVED*** null ? void 0 : existingVSet.forEach((v) ***REMOVED***> {
            if (!vSet.has(v)) {
              deleteFromMultimap(reverse, v, k);
            }
          });
          vSet.forEach((v) ***REMOVED***> {
            if (!(existingVSet ***REMOVED******REMOVED*** null ? void 0 : existingVSet.has(v))) {
              addToMultimap(reverse, v, k);
            }
          });
          return map2;
        }
      };
      return map2;
    }
    return create2(
      /* @__PURE__ */ new Map(),
      /* @__PURE__ */ new Map(),
      /*deleted*/
      void 0
    );
  }
  BuilderState2.createManyToManyPathMap ***REMOVED*** createManyToManyPathMap;
  function addToMultimap(map2, k, v) {
    let set ***REMOVED*** map2.get(k);
    if (!set) {
      set ***REMOVED*** /* @__PURE__ */ new Set();
      map2.set(k, set);
    }
    set.add(v);
  }
  function deleteFromMultimap(map2, k, v) {
    const set ***REMOVED*** map2.get(k);
    if (set ***REMOVED******REMOVED*** null ? void 0 : set.delete(v)) {
      if (!set.size) {
        map2.delete(k);
      }
      return true;
    }
    return false;
  }
  function getReferencedFilesFromImportedModuleSymbol(symbol) {
    return mapDefined(symbol.declarations, (declaration) ***REMOVED***> {
      var _a;
      return (_a ***REMOVED*** getSourceFileOfNode(declaration)) ***REMOVED******REMOVED*** null ? void 0 : _a.resolvedPath;
    });
  }
  function getReferencedFilesFromImportLiteral(checker, importName) {
    const symbol ***REMOVED*** checker.getSymbolAtLocation(importName);
    return symbol && getReferencedFilesFromImportedModuleSymbol(symbol);
  }
  function getReferencedFileFromFileName(program, fileName, sourceFileDirectory, getCanonicalFileName) {
    var _a;
    return toPath(((_a ***REMOVED*** program.getRedirectFromSourceFile(fileName)) ***REMOVED******REMOVED*** null ? void 0 : _a.outputDts) || fileName, sourceFileDirectory, getCanonicalFileName);
  }
  function getReferencedFiles(program, sourceFile, getCanonicalFileName) {
    let referencedFiles;
    if (sourceFile.imports && sourceFile.imports.length > 0) {
      const checker ***REMOVED*** program.getTypeChecker();
      for (const importName of sourceFile.imports) {
        const declarationSourceFilePaths ***REMOVED*** getReferencedFilesFromImportLiteral(checker, importName);
        declarationSourceFilePaths ***REMOVED******REMOVED*** null ? void 0 : declarationSourceFilePaths.forEach(addReferencedFile);
      }
    }
    const sourceFileDirectory ***REMOVED*** getDirectoryPath(sourceFile.resolvedPath);
    if (sourceFile.referencedFiles && sourceFile.referencedFiles.length > 0) {
      for (const referencedFile of sourceFile.referencedFiles) {
        const referencedPath ***REMOVED*** getReferencedFileFromFileName(program, referencedFile.fileName, sourceFileDirectory, getCanonicalFileName);
        addReferencedFile(referencedPath);
      }
    }
    program.forEachResolvedTypeReferenceDirective(({ resolvedTypeReferenceDirective }) ***REMOVED***> {
      if (!resolvedTypeReferenceDirective) {
        return;
      }
      const fileName ***REMOVED*** resolvedTypeReferenceDirective.resolvedFileName;
      const typeFilePath ***REMOVED*** getReferencedFileFromFileName(program, fileName, sourceFileDirectory, getCanonicalFileName);
      addReferencedFile(typeFilePath);
    }, sourceFile);
    if (sourceFile.moduleAugmentations.length) {
      const checker ***REMOVED*** program.getTypeChecker();
      for (const moduleName of sourceFile.moduleAugmentations) {
        if (!isStringLiteral(moduleName)) continue;
        const symbol ***REMOVED*** checker.getSymbolAtLocation(moduleName);
        if (!symbol) continue;
        addReferenceFromAmbientModule(symbol);
      }
    }
    for (const ambientModule of program.getTypeChecker().getAmbientModules()) {
      if (ambientModule.declarations && ambientModule.declarations.length > 1) {
        addReferenceFromAmbientModule(ambientModule);
      }
    }
    return referencedFiles;
    function addReferenceFromAmbientModule(symbol) {
      if (!symbol.declarations) {
        return;
      }
      for (const declaration of symbol.declarations) {
        const declarationSourceFile ***REMOVED*** getSourceFileOfNode(declaration);
        if (declarationSourceFile && declarationSourceFile !***REMOVED******REMOVED*** sourceFile) {
          addReferencedFile(declarationSourceFile.resolvedPath);
        }
      }
    }
    function addReferencedFile(referencedPath) {
      (referencedFiles || (referencedFiles ***REMOVED*** /* @__PURE__ */ new Set())).add(referencedPath);
    }
  }
  function canReuseOldState(newReferencedMap, oldState) {
    return oldState && !oldState.referencedMap ***REMOVED******REMOVED******REMOVED*** !newReferencedMap;
  }
  BuilderState2.canReuseOldState ***REMOVED*** canReuseOldState;
  function createReferencedMap(options) {
    return options.module !***REMOVED******REMOVED*** 0 /* None */ && !options.outFile ? createManyToManyPathMap() : void 0;
  }
  BuilderState2.createReferencedMap ***REMOVED*** createReferencedMap;
  function create(newProgram, oldState, disableUseFileVersionAsSignature) {
    var _a, _b;
    const fileInfos ***REMOVED*** /* @__PURE__ */ new Map();
    const options ***REMOVED*** newProgram.getCompilerOptions();
    const referencedMap ***REMOVED*** createReferencedMap(options);
    const useOldState ***REMOVED*** canReuseOldState(referencedMap, oldState);
    newProgram.getTypeChecker();
    for (const sourceFile of newProgram.getSourceFiles()) {
      const version2 ***REMOVED*** Debug.checkDefined(sourceFile.version, "Program intended to be used with Builder should have source files with versions set");
      const oldUncommittedSignature ***REMOVED*** useOldState ? (_a ***REMOVED*** oldState.oldSignatures) ***REMOVED******REMOVED*** null ? void 0 : _a.get(sourceFile.resolvedPath) : void 0;
      const signature ***REMOVED*** oldUncommittedSignature ***REMOVED******REMOVED******REMOVED*** void 0 ? useOldState ? (_b ***REMOVED*** oldState.fileInfos.get(sourceFile.resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _b.signature : void 0 : oldUncommittedSignature || void 0;
      if (referencedMap) {
        const newReferences ***REMOVED*** getReferencedFiles(newProgram, sourceFile, newProgram.getCanonicalFileName);
        if (newReferences) {
          referencedMap.set(sourceFile.resolvedPath, newReferences);
        }
      }
      fileInfos.set(sourceFile.resolvedPath, {
        version: version2,
        signature,
        // No need to calculate affectsGlobalScope with --out since its not used at all
        affectsGlobalScope: !options.outFile ? isFileAffectingGlobalScope(sourceFile) || void 0 : void 0,
        impliedFormat: sourceFile.impliedNodeFormat
      });
    }
    return {
      fileInfos,
      referencedMap,
      useFileVersionAsSignature: !disableUseFileVersionAsSignature && !useOldState
    };
  }
  BuilderState2.create ***REMOVED*** create;
  function releaseCache2(state) {
    state.allFilesExcludingDefaultLibraryFile ***REMOVED*** void 0;
    state.allFileNames ***REMOVED*** void 0;
  }
  BuilderState2.releaseCache ***REMOVED*** releaseCache2;
  function getFilesAffectedBy(state, programOfThisState, path, cancellationToken, host) {
    var _a;
    const result ***REMOVED*** getFilesAffectedByWithOldState(
      state,
      programOfThisState,
      path,
      cancellationToken,
      host
    );
    (_a ***REMOVED*** state.oldSignatures) ***REMOVED******REMOVED*** null ? void 0 : _a.clear();
    return result;
  }
  BuilderState2.getFilesAffectedBy ***REMOVED*** getFilesAffectedBy;
  function getFilesAffectedByWithOldState(state, programOfThisState, path, cancellationToken, host) {
    const sourceFile ***REMOVED*** programOfThisState.getSourceFileByPath(path);
    if (!sourceFile) {
      return emptyArray;
    }
    if (!updateShapeSignature(state, programOfThisState, sourceFile, cancellationToken, host)) {
      return [sourceFile];
    }
    return (state.referencedMap ? getFilesAffectedByUpdatedShapeWhenModuleEmit : getFilesAffectedByUpdatedShapeWhenNonModuleEmit)(state, programOfThisState, sourceFile, cancellationToken, host);
  }
  BuilderState2.getFilesAffectedByWithOldState ***REMOVED*** getFilesAffectedByWithOldState;
  function updateSignatureOfFile(state, signature, path) {
    state.fileInfos.get(path).signature ***REMOVED*** signature;
    (state.hasCalledUpdateShapeSignature || (state.hasCalledUpdateShapeSignature ***REMOVED*** /* @__PURE__ */ new Set())).add(path);
  }
  BuilderState2.updateSignatureOfFile ***REMOVED*** updateSignatureOfFile;
  function computeDtsSignature(programOfThisState, sourceFile, cancellationToken, host, onNewSignature) {
    programOfThisState.emit(
      sourceFile,
      (fileName, text, _writeByteOrderMark, _onError, sourceFiles, data) ***REMOVED***> {
        Debug.assert(isDeclarationFileName(fileName), `File extension for signature expected to be dts: Got:: ${fileName}`);
        onNewSignature(
          computeSignatureWithDiagnostics(
            programOfThisState,
            sourceFile,
            text,
            host,
            data
          ),
          sourceFiles
        );
      },
      cancellationToken,
      2 /* BuilderSignature */,
      /*customTransformers*/
      void 0,
      /*forceDtsEmit*/
      true
    );
  }
  BuilderState2.computeDtsSignature ***REMOVED*** computeDtsSignature;
  function updateShapeSignature(state, programOfThisState, sourceFile, cancellationToken, host, useFileVersionAsSignature ***REMOVED*** state.useFileVersionAsSignature) {
    var _a;
    if ((_a ***REMOVED*** state.hasCalledUpdateShapeSignature) ***REMOVED******REMOVED*** null ? void 0 : _a.has(sourceFile.resolvedPath)) return false;
    const info ***REMOVED*** state.fileInfos.get(sourceFile.resolvedPath);
    const prevSignature ***REMOVED*** info.signature;
    let latestSignature;
    if (!sourceFile.isDeclarationFile && !useFileVersionAsSignature) {
      computeDtsSignature(programOfThisState, sourceFile, cancellationToken, host, (signature) ***REMOVED***> {
        latestSignature ***REMOVED*** signature;
        if (host.storeSignatureInfo) (state.signatureInfo ?? (state.signatureInfo ***REMOVED*** /* @__PURE__ */ new Map())).set(sourceFile.resolvedPath, 0 /* ComputedDts */);
      });
    }
    if (latestSignature ***REMOVED******REMOVED******REMOVED*** void 0) {
      latestSignature ***REMOVED*** sourceFile.version;
      if (host.storeSignatureInfo) (state.signatureInfo ?? (state.signatureInfo ***REMOVED*** /* @__PURE__ */ new Map())).set(sourceFile.resolvedPath, 2 /* UsedVersion */);
    }
    (state.oldSignatures || (state.oldSignatures ***REMOVED*** /* @__PURE__ */ new Map())).set(sourceFile.resolvedPath, prevSignature || false);
    (state.hasCalledUpdateShapeSignature || (state.hasCalledUpdateShapeSignature ***REMOVED*** /* @__PURE__ */ new Set())).add(sourceFile.resolvedPath);
    info.signature ***REMOVED*** latestSignature;
    return latestSignature !***REMOVED******REMOVED*** prevSignature;
  }
  BuilderState2.updateShapeSignature ***REMOVED*** updateShapeSignature;
  function getAllDependencies(state, programOfThisState, sourceFile) {
    const compilerOptions ***REMOVED*** programOfThisState.getCompilerOptions();
    if (compilerOptions.outFile) {
      return getAllFileNames(state, programOfThisState);
    }
    if (!state.referencedMap || isFileAffectingGlobalScope(sourceFile)) {
      return getAllFileNames(state, programOfThisState);
    }
    const seenMap ***REMOVED*** /* @__PURE__ */ new Set();
    const queue ***REMOVED*** [sourceFile.resolvedPath];
    while (queue.length) {
      const path ***REMOVED*** queue.pop();
      if (!seenMap.has(path)) {
        seenMap.add(path);
        const references ***REMOVED*** state.referencedMap.getValues(path);
        if (references) {
          for (const key of references.keys()) {
            queue.push(key);
          }
        }
      }
    }
    return arrayFrom(mapDefinedIterator(seenMap.keys(), (path) ***REMOVED***> {
      var _a;
      return ((_a ***REMOVED*** programOfThisState.getSourceFileByPath(path)) ***REMOVED******REMOVED*** null ? void 0 : _a.fileName) ?? path;
    }));
  }
  BuilderState2.getAllDependencies ***REMOVED*** getAllDependencies;
  function getAllFileNames(state, programOfThisState) {
    if (!state.allFileNames) {
      const sourceFiles ***REMOVED*** programOfThisState.getSourceFiles();
      state.allFileNames ***REMOVED*** sourceFiles ***REMOVED******REMOVED******REMOVED*** emptyArray ? emptyArray : sourceFiles.map((file) ***REMOVED***> file.fileName);
    }
    return state.allFileNames;
  }
  function getReferencedByPaths(state, referencedFilePath) {
    const keys ***REMOVED*** state.referencedMap.getKeys(referencedFilePath);
    return keys ? arrayFrom(keys.keys()) : [];
  }
  BuilderState2.getReferencedByPaths ***REMOVED*** getReferencedByPaths;
  function containsOnlyAmbientModules(sourceFile) {
    for (const statement of sourceFile.statements) {
      if (!isModuleWithStringLiteralName(statement)) {
        return false;
      }
    }
    return true;
  }
  function containsGlobalScopeAugmentation(sourceFile) {
    return some(sourceFile.moduleAugmentations, (augmentation) ***REMOVED***> isGlobalScopeAugmentation(augmentation.parent));
  }
  function isFileAffectingGlobalScope(sourceFile) {
    return containsGlobalScopeAugmentation(sourceFile) || !isExternalOrCommonJsModule(sourceFile) && !isJsonSourceFile(sourceFile) && !containsOnlyAmbientModules(sourceFile);
  }
  function getAllFilesExcludingDefaultLibraryFile(state, programOfThisState, firstSourceFile) {
    if (state.allFilesExcludingDefaultLibraryFile) {
      return state.allFilesExcludingDefaultLibraryFile;
    }
    let result;
    if (firstSourceFile) addSourceFile(firstSourceFile);
    for (const sourceFile of programOfThisState.getSourceFiles()) {
      if (sourceFile !***REMOVED******REMOVED*** firstSourceFile) {
        addSourceFile(sourceFile);
      }
    }
    state.allFilesExcludingDefaultLibraryFile ***REMOVED*** result || emptyArray;
    return state.allFilesExcludingDefaultLibraryFile;
    function addSourceFile(sourceFile) {
      if (!programOfThisState.isSourceFileDefaultLibrary(sourceFile)) {
        (result || (result ***REMOVED*** [])).push(sourceFile);
      }
    }
  }
  BuilderState2.getAllFilesExcludingDefaultLibraryFile ***REMOVED*** getAllFilesExcludingDefaultLibraryFile;
  function getFilesAffectedByUpdatedShapeWhenNonModuleEmit(state, programOfThisState, sourceFileWithUpdatedShape) {
    const compilerOptions ***REMOVED*** programOfThisState.getCompilerOptions();
    if (compilerOptions && compilerOptions.outFile) {
      return [sourceFileWithUpdatedShape];
    }
    return getAllFilesExcludingDefaultLibraryFile(state, programOfThisState, sourceFileWithUpdatedShape);
  }
  function getFilesAffectedByUpdatedShapeWhenModuleEmit(state, programOfThisState, sourceFileWithUpdatedShape, cancellationToken, host) {
    if (isFileAffectingGlobalScope(sourceFileWithUpdatedShape)) {
      return getAllFilesExcludingDefaultLibraryFile(state, programOfThisState, sourceFileWithUpdatedShape);
    }
    const compilerOptions ***REMOVED*** programOfThisState.getCompilerOptions();
    if (compilerOptions && (getIsolatedModules(compilerOptions) || compilerOptions.outFile)) {
      return [sourceFileWithUpdatedShape];
    }
    const seenFileNamesMap ***REMOVED*** /* @__PURE__ */ new Map();
    seenFileNamesMap.set(sourceFileWithUpdatedShape.resolvedPath, sourceFileWithUpdatedShape);
    const queue ***REMOVED*** getReferencedByPaths(state, sourceFileWithUpdatedShape.resolvedPath);
    while (queue.length > 0) {
      const currentPath ***REMOVED*** queue.pop();
      if (!seenFileNamesMap.has(currentPath)) {
        const currentSourceFile ***REMOVED*** programOfThisState.getSourceFileByPath(currentPath);
        seenFileNamesMap.set(currentPath, currentSourceFile);
        if (currentSourceFile && updateShapeSignature(state, programOfThisState, currentSourceFile, cancellationToken, host)) {
          queue.push(...getReferencedByPaths(state, currentSourceFile.resolvedPath));
        }
      }
    }
    return arrayFrom(mapDefinedIterator(seenFileNamesMap.values(), (value) ***REMOVED***> value));
  }
})(BuilderState || (BuilderState ***REMOVED*** {}));

// src/compiler/builder.ts
function isBuilderProgramStateWithDefinedProgram(state) {
  return state.program !***REMOVED******REMOVED*** void 0;
}
function toBuilderProgramStateWithDefinedProgram(state) {
  Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
  return state;
}
function getBuilderFileEmit(options) {
  let result ***REMOVED*** 1 /* Js */;
  if (options.sourceMap) result ***REMOVED*** result | 2 /* JsMap */;
  if (options.inlineSourceMap) result ***REMOVED*** result | 4 /* JsInlineMap */;
  if (getEmitDeclarations(options)) result ***REMOVED*** result | 24 /* Dts */;
  if (options.declarationMap) result ***REMOVED*** result | 32 /* DtsMap */;
  if (options.emitDeclarationOnly) result ***REMOVED*** result & 56 /* AllDts */;
  return result;
}
function getPendingEmitKind(optionsOrEmitKind, oldOptionsOrEmitKind) {
  const oldEmitKind ***REMOVED*** oldOptionsOrEmitKind && (isNumber(oldOptionsOrEmitKind) ? oldOptionsOrEmitKind : getBuilderFileEmit(oldOptionsOrEmitKind));
  const emitKind ***REMOVED*** isNumber(optionsOrEmitKind) ? optionsOrEmitKind : getBuilderFileEmit(optionsOrEmitKind);
  if (oldEmitKind ***REMOVED******REMOVED******REMOVED*** emitKind) return 0 /* None */;
  if (!oldEmitKind || !emitKind) return emitKind;
  const diff ***REMOVED*** oldEmitKind ^ emitKind;
  let result ***REMOVED*** 0 /* None */;
  if (diff & 7 /* AllJs */) result ***REMOVED*** emitKind & 7 /* AllJs */;
  if (diff & 8 /* DtsErrors */) result ***REMOVED*** result | emitKind & 8 /* DtsErrors */;
  if (diff & 48 /* AllDtsEmit */) result ***REMOVED*** result | emitKind & 48 /* AllDtsEmit */;
  return result;
}
function hasSameKeys(map1, map2) {
  return map1 ***REMOVED******REMOVED******REMOVED*** map2 || map1 !***REMOVED******REMOVED*** void 0 && map2 !***REMOVED******REMOVED*** void 0 && map1.size ***REMOVED******REMOVED******REMOVED*** map2.size && !forEachKey(map1, (key) ***REMOVED***> !map2.has(key));
}
function createBuilderProgramState(newProgram, oldState) {
  var _a, _b;
  const state ***REMOVED*** BuilderState.create(
    newProgram,
    oldState,
    /*disableUseFileVersionAsSignature*/
    false
  );
  state.program ***REMOVED*** newProgram;
  const compilerOptions ***REMOVED*** newProgram.getCompilerOptions();
  state.compilerOptions ***REMOVED*** compilerOptions;
  const outFilePath ***REMOVED*** compilerOptions.outFile;
  state.semanticDiagnosticsPerFile ***REMOVED*** /* @__PURE__ */ new Map();
  if (outFilePath && compilerOptions.composite && (oldState ***REMOVED******REMOVED*** null ? void 0 : oldState.outSignature) && outFilePath ***REMOVED******REMOVED******REMOVED*** oldState.compilerOptions.outFile) {
    state.outSignature ***REMOVED*** oldState.outSignature && getEmitSignatureFromOldSignature(compilerOptions, oldState.compilerOptions, oldState.outSignature);
  }
  state.changedFilesSet ***REMOVED*** /* @__PURE__ */ new Set();
  state.latestChangedDtsFile ***REMOVED*** compilerOptions.composite ? oldState ***REMOVED******REMOVED*** null ? void 0 : oldState.latestChangedDtsFile : void 0;
  state.checkPending ***REMOVED*** state.compilerOptions.noCheck ? true : void 0;
  const useOldState ***REMOVED*** BuilderState.canReuseOldState(state.referencedMap, oldState);
  const oldCompilerOptions ***REMOVED*** useOldState ? oldState.compilerOptions : void 0;
  let canCopySemanticDiagnostics ***REMOVED*** useOldState && !compilerOptionsAffectSemanticDiagnostics(compilerOptions, oldCompilerOptions);
  const canCopyEmitSignatures ***REMOVED*** compilerOptions.composite && (oldState ***REMOVED******REMOVED*** null ? void 0 : oldState.emitSignatures) && !outFilePath && !compilerOptionsAffectDeclarationPath(compilerOptions, oldState.compilerOptions);
  let canCopyEmitDiagnostics ***REMOVED*** true;
  if (useOldState) {
    (_a ***REMOVED*** oldState.changedFilesSet) ***REMOVED******REMOVED*** null ? void 0 : _a.forEach((value) ***REMOVED***> state.changedFilesSet.add(value));
    if (!outFilePath && ((_b ***REMOVED*** oldState.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _b.size)) {
      state.affectedFilesPendingEmit ***REMOVED*** new Map(oldState.affectedFilesPendingEmit);
      state.seenAffectedFiles ***REMOVED*** /* @__PURE__ */ new Set();
    }
    state.programEmitPending ***REMOVED*** oldState.programEmitPending;
    if (outFilePath && state.changedFilesSet.size) {
      canCopySemanticDiagnostics ***REMOVED*** false;
      canCopyEmitDiagnostics ***REMOVED*** false;
    }
    state.hasErrorsFromOldState ***REMOVED*** oldState.hasErrors;
  } else {
    state.buildInfoEmitPending ***REMOVED*** isIncrementalCompilation(compilerOptions);
  }
  const referencedMap ***REMOVED*** state.referencedMap;
  const oldReferencedMap ***REMOVED*** useOldState ? oldState.referencedMap : void 0;
  const copyDeclarationFileDiagnostics ***REMOVED*** canCopySemanticDiagnostics && !compilerOptions.skipLibCheck ***REMOVED******REMOVED******REMOVED*** !oldCompilerOptions.skipLibCheck;
  const copyLibFileDiagnostics ***REMOVED*** copyDeclarationFileDiagnostics && !compilerOptions.skipDefaultLibCheck ***REMOVED******REMOVED******REMOVED*** !oldCompilerOptions.skipDefaultLibCheck;
  state.fileInfos.forEach((info, sourceFilePath) ***REMOVED***> {
    var _a2;
    let oldInfo;
    let newReferences;
    if (!useOldState || // File wasn't present in old state
    !(oldInfo ***REMOVED*** oldState.fileInfos.get(sourceFilePath)) || // versions dont match
    oldInfo.version !***REMOVED******REMOVED*** info.version || // Implied formats dont match
    oldInfo.impliedFormat !***REMOVED******REMOVED*** info.impliedFormat || // Referenced files changed
    !hasSameKeys(newReferences ***REMOVED*** referencedMap && referencedMap.getValues(sourceFilePath), oldReferencedMap && oldReferencedMap.getValues(sourceFilePath)) || // Referenced file was deleted in the new program
    newReferences && forEachKey(newReferences, (path) ***REMOVED***> !state.fileInfos.has(path) && oldState.fileInfos.has(path))) {
      addFileToChangeSet(sourceFilePath);
    } else {
      const sourceFile ***REMOVED*** newProgram.getSourceFileByPath(sourceFilePath);
      const emitDiagnostics ***REMOVED*** canCopyEmitDiagnostics ? (_a2 ***REMOVED*** oldState.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(sourceFilePath) : void 0;
      if (emitDiagnostics) {
        (state.emitDiagnosticsPerFile ?? (state.emitDiagnosticsPerFile ***REMOVED*** /* @__PURE__ */ new Map())).set(
          sourceFilePath,
          oldState.hasReusableDiagnostic ? convertToDiagnostics(emitDiagnostics, sourceFilePath, newProgram) : repopulateDiagnostics(emitDiagnostics, newProgram)
        );
      }
      if (canCopySemanticDiagnostics) {
        if (sourceFile.isDeclarationFile && !copyDeclarationFileDiagnostics) return;
        if (sourceFile.hasNoDefaultLib && !copyLibFileDiagnostics) return;
        const diagnostics ***REMOVED*** oldState.semanticDiagnosticsPerFile.get(sourceFilePath);
        if (diagnostics) {
          state.semanticDiagnosticsPerFile.set(
            sourceFilePath,
            oldState.hasReusableDiagnostic ? convertToDiagnostics(diagnostics, sourceFilePath, newProgram) : repopulateDiagnostics(diagnostics, newProgram)
          );
          (state.semanticDiagnosticsFromOldState ?? (state.semanticDiagnosticsFromOldState ***REMOVED*** /* @__PURE__ */ new Set())).add(sourceFilePath);
        }
      }
    }
    if (canCopyEmitSignatures) {
      const oldEmitSignature ***REMOVED*** oldState.emitSignatures.get(sourceFilePath);
      if (oldEmitSignature) {
        (state.emitSignatures ?? (state.emitSignatures ***REMOVED*** /* @__PURE__ */ new Map())).set(sourceFilePath, getEmitSignatureFromOldSignature(compilerOptions, oldState.compilerOptions, oldEmitSignature));
      }
    }
  });
  if (useOldState && forEachEntry(oldState.fileInfos, (info, sourceFilePath) ***REMOVED***> {
    if (state.fileInfos.has(sourceFilePath)) return false;
    if (info.affectsGlobalScope) return true;
    state.buildInfoEmitPending ***REMOVED*** true;
    return !!outFilePath;
  })) {
    BuilderState.getAllFilesExcludingDefaultLibraryFile(
      state,
      newProgram,
      /*firstSourceFile*/
      void 0
    ).forEach((file) ***REMOVED***> addFileToChangeSet(file.resolvedPath));
  } else if (oldCompilerOptions) {
    const pendingEmitKind ***REMOVED*** compilerOptionsAffectEmit(compilerOptions, oldCompilerOptions) ? getBuilderFileEmit(compilerOptions) : getPendingEmitKind(compilerOptions, oldCompilerOptions);
    if (pendingEmitKind !***REMOVED******REMOVED*** 0 /* None */) {
      if (!outFilePath) {
        newProgram.getSourceFiles().forEach((f) ***REMOVED***> {
          if (!state.changedFilesSet.has(f.resolvedPath)) {
            addToAffectedFilesPendingEmit(
              state,
              f.resolvedPath,
              pendingEmitKind
            );
          }
        });
        Debug.assert(!state.seenAffectedFiles || !state.seenAffectedFiles.size);
        state.seenAffectedFiles ***REMOVED*** state.seenAffectedFiles || /* @__PURE__ */ new Set();
      } else if (!state.changedFilesSet.size) {
        state.programEmitPending ***REMOVED*** state.programEmitPending ? state.programEmitPending | pendingEmitKind : pendingEmitKind;
      }
      state.buildInfoEmitPending ***REMOVED*** true;
    }
  }
  if (useOldState && state.semanticDiagnosticsPerFile.size !***REMOVED******REMOVED*** state.fileInfos.size && oldState.checkPending !***REMOVED******REMOVED*** state.checkPending) state.buildInfoEmitPending ***REMOVED*** true;
  return state;
  function addFileToChangeSet(path) {
    state.changedFilesSet.add(path);
    if (outFilePath) {
      canCopySemanticDiagnostics ***REMOVED*** false;
      canCopyEmitDiagnostics ***REMOVED*** false;
      state.semanticDiagnosticsFromOldState ***REMOVED*** void 0;
      state.semanticDiagnosticsPerFile.clear();
      state.emitDiagnosticsPerFile ***REMOVED*** void 0;
    }
    state.buildInfoEmitPending ***REMOVED*** true;
    state.programEmitPending ***REMOVED*** void 0;
  }
}
function getEmitSignatureFromOldSignature(options, oldOptions, oldEmitSignature) {
  return !!options.declarationMap ***REMOVED******REMOVED******REMOVED*** !!oldOptions.declarationMap ? (
    // Use same format of signature
    oldEmitSignature
  ) : (
    // Convert to different format
    isString(oldEmitSignature) ? [oldEmitSignature] : oldEmitSignature[0]
  );
}
function repopulateDiagnostics(diagnostics, newProgram) {
  if (!diagnostics.length) return diagnostics;
  return sameMap(diagnostics, (diag2) ***REMOVED***> {
    if (isString(diag2.messageText)) return diag2;
    const repopulatedChain ***REMOVED*** convertOrRepopulateDiagnosticMessageChain(diag2.messageText, diag2.file, newProgram, (chain) ***REMOVED***> {
      var _a;
      return (_a ***REMOVED*** chain.repopulateInfo) ***REMOVED******REMOVED*** null ? void 0 : _a.call(chain);
    });
    return repopulatedChain ***REMOVED******REMOVED******REMOVED*** diag2.messageText ? diag2 : { ...diag2, messageText: repopulatedChain };
  });
}
function convertOrRepopulateDiagnosticMessageChain(chain, sourceFile, newProgram, repopulateInfo) {
  const info ***REMOVED*** repopulateInfo(chain);
  if (info ***REMOVED******REMOVED******REMOVED*** true) {
    return {
      ...createModeMismatchDetails(sourceFile),
      next: convertOrRepopulateDiagnosticMessageChainArray(chain.next, sourceFile, newProgram, repopulateInfo)
    };
  } else if (info) {
    return {
      ...createModuleNotFoundChain(sourceFile, newProgram, info.moduleReference, info.mode, info.packageName || info.moduleReference),
      next: convertOrRepopulateDiagnosticMessageChainArray(chain.next, sourceFile, newProgram, repopulateInfo)
    };
  }
  const next ***REMOVED*** convertOrRepopulateDiagnosticMessageChainArray(chain.next, sourceFile, newProgram, repopulateInfo);
  return next ***REMOVED******REMOVED******REMOVED*** chain.next ? chain : { ...chain, next };
}
function convertOrRepopulateDiagnosticMessageChainArray(array, sourceFile, newProgram, repopulateInfo) {
  return sameMap(array, (chain) ***REMOVED***> convertOrRepopulateDiagnosticMessageChain(chain, sourceFile, newProgram, repopulateInfo));
}
function convertToDiagnostics(diagnostics, diagnosticFilePath, newProgram) {
  if (!diagnostics.length) return emptyArray;
  let buildInfoDirectory;
  return diagnostics.map((diagnostic) ***REMOVED***> {
    const result ***REMOVED*** convertToDiagnosticRelatedInformation(diagnostic, diagnosticFilePath, newProgram, toPathInBuildInfoDirectory);
    result.reportsUnnecessary ***REMOVED*** diagnostic.reportsUnnecessary;
    result.reportsDeprecated ***REMOVED*** diagnostic.reportDeprecated;
    result.source ***REMOVED*** diagnostic.source;
    result.skippedOn ***REMOVED*** diagnostic.skippedOn;
    const { relatedInformation } ***REMOVED*** diagnostic;
    result.relatedInformation ***REMOVED*** relatedInformation ? relatedInformation.length ? relatedInformation.map((r) ***REMOVED***> convertToDiagnosticRelatedInformation(r, diagnosticFilePath, newProgram, toPathInBuildInfoDirectory)) : [] : void 0;
    return result;
  });
  function toPathInBuildInfoDirectory(path) {
    buildInfoDirectory ?? (buildInfoDirectory ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(getTsBuildInfoEmitOutputFilePath(newProgram.getCompilerOptions()), newProgram.getCurrentDirectory())));
    return toPath(path, buildInfoDirectory, newProgram.getCanonicalFileName);
  }
}
function convertToDiagnosticRelatedInformation(diagnostic, diagnosticFilePath, newProgram, toPath3) {
  const { file } ***REMOVED*** diagnostic;
  const sourceFile ***REMOVED*** file !***REMOVED******REMOVED*** false ? newProgram.getSourceFileByPath(file ? toPath3(file) : diagnosticFilePath) : void 0;
  return {
    ...diagnostic,
    file: sourceFile,
    messageText: isString(diagnostic.messageText) ? diagnostic.messageText : convertOrRepopulateDiagnosticMessageChain(diagnostic.messageText, sourceFile, newProgram, (chain) ***REMOVED***> chain.info)
  };
}
function releaseCache(state) {
  BuilderState.releaseCache(state);
  state.program ***REMOVED*** void 0;
}
function assertSourceFileOkWithoutNextAffectedCall(state, sourceFile) {
  Debug.assert(!sourceFile || !state.affectedFiles || state.affectedFiles[state.affectedFilesIndex - 1] !***REMOVED******REMOVED*** sourceFile || !state.semanticDiagnosticsPerFile.has(sourceFile.resolvedPath));
}
function getNextAffectedFile(state, cancellationToken, host) {
  var _a;
  while (true) {
    const { affectedFiles } ***REMOVED*** state;
    if (affectedFiles) {
      const seenAffectedFiles ***REMOVED*** state.seenAffectedFiles;
      let affectedFilesIndex ***REMOVED*** state.affectedFilesIndex;
      while (affectedFilesIndex < affectedFiles.length) {
        const affectedFile ***REMOVED*** affectedFiles[affectedFilesIndex];
        if (!seenAffectedFiles.has(affectedFile.resolvedPath)) {
          state.affectedFilesIndex ***REMOVED*** affectedFilesIndex;
          addToAffectedFilesPendingEmit(
            state,
            affectedFile.resolvedPath,
            getBuilderFileEmit(state.compilerOptions)
          );
          handleDtsMayChangeOfAffectedFile(
            state,
            affectedFile,
            cancellationToken,
            host
          );
          return affectedFile;
        }
        affectedFilesIndex++;
      }
      state.changedFilesSet.delete(state.currentChangedFilePath);
      state.currentChangedFilePath ***REMOVED*** void 0;
      (_a ***REMOVED*** state.oldSignatures) ***REMOVED******REMOVED*** null ? void 0 : _a.clear();
      state.affectedFiles ***REMOVED*** void 0;
    }
    const nextKey ***REMOVED*** state.changedFilesSet.keys().next();
    if (nextKey.done) {
      return void 0;
    }
    const compilerOptions ***REMOVED*** state.program.getCompilerOptions();
    if (compilerOptions.outFile) return state.program;
    state.affectedFiles ***REMOVED*** BuilderState.getFilesAffectedByWithOldState(
      state,
      state.program,
      nextKey.value,
      cancellationToken,
      host
    );
    state.currentChangedFilePath ***REMOVED*** nextKey.value;
    state.affectedFilesIndex ***REMOVED*** 0;
    if (!state.seenAffectedFiles) state.seenAffectedFiles ***REMOVED*** /* @__PURE__ */ new Set();
  }
}
function clearAffectedFilesPendingEmit(state, emitOnlyDtsFiles, isForDtsErrors) {
  var _a, _b;
  if (!((_a ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _a.size) && !state.programEmitPending) return;
  if (!emitOnlyDtsFiles && !isForDtsErrors) {
    state.affectedFilesPendingEmit ***REMOVED*** void 0;
    state.programEmitPending ***REMOVED*** void 0;
  }
  (_b ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _b.forEach((emitKind, path) ***REMOVED***> {
    const pending ***REMOVED*** !isForDtsErrors ? emitKind & 7 /* AllJs */ : emitKind & (7 /* AllJs */ | 48 /* AllDtsEmit */);
    if (!pending) state.affectedFilesPendingEmit.delete(path);
    else state.affectedFilesPendingEmit.set(path, pending);
  });
  if (state.programEmitPending) {
    const pending ***REMOVED*** !isForDtsErrors ? state.programEmitPending & 7 /* AllJs */ : state.programEmitPending & (7 /* AllJs */ | 48 /* AllDtsEmit */);
    if (!pending) state.programEmitPending ***REMOVED*** void 0;
    else state.programEmitPending ***REMOVED*** pending;
  }
}
function getPendingEmitKindWithSeen(optionsOrEmitKind, seenOldOptionsOrEmitKind, emitOnlyDtsFiles, isForDtsErrors) {
  let pendingKind ***REMOVED*** getPendingEmitKind(optionsOrEmitKind, seenOldOptionsOrEmitKind);
  if (emitOnlyDtsFiles) pendingKind ***REMOVED*** pendingKind & 56 /* AllDts */;
  if (isForDtsErrors) pendingKind ***REMOVED*** pendingKind & 8 /* DtsErrors */;
  return pendingKind;
}
function getBuilderFileEmitAllDts(isForDtsErrors) {
  return !isForDtsErrors ? 56 /* AllDts */ : 8 /* DtsErrors */;
}
function getNextAffectedFilePendingEmit(state, emitOnlyDtsFiles, isForDtsErrors) {
  var _a;
  if (!((_a ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _a.size)) return void 0;
  return forEachEntry(state.affectedFilesPendingEmit, (emitKind, path) ***REMOVED***> {
    var _a2;
    const affectedFile ***REMOVED*** state.program.getSourceFileByPath(path);
    if (!affectedFile || !sourceFileMayBeEmitted(affectedFile, state.program)) {
      state.affectedFilesPendingEmit.delete(path);
      return void 0;
    }
    const seenKind ***REMOVED*** (_a2 ***REMOVED*** state.seenEmittedFiles) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(affectedFile.resolvedPath);
    const pendingKind ***REMOVED*** getPendingEmitKindWithSeen(
      emitKind,
      seenKind,
      emitOnlyDtsFiles,
      isForDtsErrors
    );
    if (pendingKind) return { affectedFile, emitKind: pendingKind };
  });
}
function getNextPendingEmitDiagnosticsFile(state, isForDtsErrors) {
  var _a;
  if (!((_a ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a.size)) return void 0;
  return forEachEntry(state.emitDiagnosticsPerFile, (diagnostics, path) ***REMOVED***> {
    var _a2;
    const affectedFile ***REMOVED*** state.program.getSourceFileByPath(path);
    if (!affectedFile || !sourceFileMayBeEmitted(affectedFile, state.program)) {
      state.emitDiagnosticsPerFile.delete(path);
      return void 0;
    }
    const seenKind ***REMOVED*** ((_a2 ***REMOVED*** state.seenEmittedFiles) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(affectedFile.resolvedPath)) || 0 /* None */;
    if (!(seenKind & getBuilderFileEmitAllDts(isForDtsErrors))) return { affectedFile, diagnostics, seenKind };
  });
}
function removeDiagnosticsOfLibraryFiles(state) {
  if (!state.cleanedDiagnosticsOfLibFiles) {
    state.cleanedDiagnosticsOfLibFiles ***REMOVED*** true;
    const options ***REMOVED*** state.program.getCompilerOptions();
    forEach(state.program.getSourceFiles(), (f) ***REMOVED***> state.program.isSourceFileDefaultLibrary(f) && !skipTypeCheckingIgnoringNoCheck(f, options, state.program) && removeSemanticDiagnosticsOf(state, f.resolvedPath));
  }
}
function handleDtsMayChangeOfAffectedFile(state, affectedFile, cancellationToken, host) {
  removeSemanticDiagnosticsOf(state, affectedFile.resolvedPath);
  if (state.allFilesExcludingDefaultLibraryFile ***REMOVED******REMOVED******REMOVED*** state.affectedFiles) {
    removeDiagnosticsOfLibraryFiles(state);
    BuilderState.updateShapeSignature(
      state,
      state.program,
      affectedFile,
      cancellationToken,
      host
    );
    return;
  }
  if (state.compilerOptions.assumeChangesOnlyAffectDirectDependencies) return;
  handleDtsMayChangeOfReferencingExportOfAffectedFile(
    state,
    affectedFile,
    cancellationToken,
    host
  );
}
function handleDtsMayChangeOf(state, path, invalidateJsFiles, cancellationToken, host) {
  removeSemanticDiagnosticsOf(state, path);
  if (!state.changedFilesSet.has(path)) {
    const sourceFile ***REMOVED*** state.program.getSourceFileByPath(path);
    if (sourceFile) {
      BuilderState.updateShapeSignature(
        state,
        state.program,
        sourceFile,
        cancellationToken,
        host,
        /*useFileVersionAsSignature*/
        true
      );
      if (invalidateJsFiles) {
        addToAffectedFilesPendingEmit(
          state,
          path,
          getBuilderFileEmit(state.compilerOptions)
        );
      } else if (getEmitDeclarations(state.compilerOptions)) {
        addToAffectedFilesPendingEmit(
          state,
          path,
          state.compilerOptions.declarationMap ? 56 /* AllDts */ : 24 /* Dts */
        );
      }
    }
  }
}
function removeSemanticDiagnosticsOf(state, path) {
  if (!state.semanticDiagnosticsFromOldState) {
    return true;
  }
  state.semanticDiagnosticsFromOldState.delete(path);
  state.semanticDiagnosticsPerFile.delete(path);
  return !state.semanticDiagnosticsFromOldState.size;
}
function isChangedSignature(state, path) {
  const oldSignature ***REMOVED*** Debug.checkDefined(state.oldSignatures).get(path) || void 0;
  const newSignature ***REMOVED*** Debug.checkDefined(state.fileInfos.get(path)).signature;
  return newSignature !***REMOVED******REMOVED*** oldSignature;
}
function handleDtsMayChangeOfGlobalScope(state, filePath, invalidateJsFiles, cancellationToken, host) {
  var _a;
  if (!((_a ***REMOVED*** state.fileInfos.get(filePath)) ***REMOVED******REMOVED*** null ? void 0 : _a.affectsGlobalScope)) return false;
  BuilderState.getAllFilesExcludingDefaultLibraryFile(
    state,
    state.program,
    /*firstSourceFile*/
    void 0
  ).forEach(
    (file) ***REMOVED***> handleDtsMayChangeOf(
      state,
      file.resolvedPath,
      invalidateJsFiles,
      cancellationToken,
      host
    )
  );
  removeDiagnosticsOfLibraryFiles(state);
  return true;
}
function handleDtsMayChangeOfReferencingExportOfAffectedFile(state, affectedFile, cancellationToken, host) {
  var _a, _b;
  if (!state.referencedMap || !state.changedFilesSet.has(affectedFile.resolvedPath)) return;
  if (!isChangedSignature(state, affectedFile.resolvedPath)) return;
  if (getIsolatedModules(state.compilerOptions)) {
    const seenFileNamesMap ***REMOVED*** /* @__PURE__ */ new Map();
    seenFileNamesMap.set(affectedFile.resolvedPath, true);
    const queue ***REMOVED*** BuilderState.getReferencedByPaths(state, affectedFile.resolvedPath);
    while (queue.length > 0) {
      const currentPath ***REMOVED*** queue.pop();
      if (!seenFileNamesMap.has(currentPath)) {
        seenFileNamesMap.set(currentPath, true);
        if (handleDtsMayChangeOfGlobalScope(
          state,
          currentPath,
          /*invalidateJsFiles*/
          false,
          cancellationToken,
          host
        )) return;
        handleDtsMayChangeOf(
          state,
          currentPath,
          /*invalidateJsFiles*/
          false,
          cancellationToken,
          host
        );
        if (isChangedSignature(state, currentPath)) {
          const currentSourceFile ***REMOVED*** state.program.getSourceFileByPath(currentPath);
          queue.push(...BuilderState.getReferencedByPaths(state, currentSourceFile.resolvedPath));
        }
      }
    }
  }
  const seenFileAndExportsOfFile ***REMOVED*** /* @__PURE__ */ new Set();
  const invalidateJsFiles ***REMOVED*** !!((_a ***REMOVED*** affectedFile.symbol) ***REMOVED******REMOVED*** null ? void 0 : _a.exports) && !!forEachEntry(
    affectedFile.symbol.exports,
    (exported) ***REMOVED***> {
      if ((exported.flags & 128 /* ConstEnum */) !***REMOVED******REMOVED*** 0) return true;
      const aliased ***REMOVED*** skipAlias(exported, state.program.getTypeChecker());
      if (aliased ***REMOVED******REMOVED******REMOVED*** exported) return false;
      return (aliased.flags & 128 /* ConstEnum */) !***REMOVED******REMOVED*** 0 && some(aliased.declarations, (d) ***REMOVED***> getSourceFileOfNode(d) ***REMOVED******REMOVED******REMOVED*** affectedFile);
    }
  );
  (_b ***REMOVED*** state.referencedMap.getKeys(affectedFile.resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _b.forEach((exportedFromPath) ***REMOVED***> {
    if (handleDtsMayChangeOfGlobalScope(state, exportedFromPath, invalidateJsFiles, cancellationToken, host)) return true;
    const references ***REMOVED*** state.referencedMap.getKeys(exportedFromPath);
    return references && forEachKey(references, (filePath) ***REMOVED***> handleDtsMayChangeOfFileAndExportsOfFile(
      state,
      filePath,
      invalidateJsFiles,
      seenFileAndExportsOfFile,
      cancellationToken,
      host
    ));
  });
}
function handleDtsMayChangeOfFileAndExportsOfFile(state, filePath, invalidateJsFiles, seenFileAndExportsOfFile, cancellationToken, host) {
  var _a;
  if (!tryAddToSet(seenFileAndExportsOfFile, filePath)) return void 0;
  if (handleDtsMayChangeOfGlobalScope(state, filePath, invalidateJsFiles, cancellationToken, host)) return true;
  handleDtsMayChangeOf(state, filePath, invalidateJsFiles, cancellationToken, host);
  (_a ***REMOVED*** state.referencedMap.getKeys(filePath)) ***REMOVED******REMOVED*** null ? void 0 : _a.forEach(
    (referencingFilePath) ***REMOVED***> handleDtsMayChangeOfFileAndExportsOfFile(
      state,
      referencingFilePath,
      invalidateJsFiles,
      seenFileAndExportsOfFile,
      cancellationToken,
      host
    )
  );
  return void 0;
}
function getSemanticDiagnosticsOfFile(state, sourceFile, cancellationToken, semanticDiagnosticsPerFile) {
  if (state.compilerOptions.noCheck) return emptyArray;
  return concatenate(
    getBinderAndCheckerDiagnosticsOfFile(state, sourceFile, cancellationToken, semanticDiagnosticsPerFile),
    state.program.getProgramDiagnostics(sourceFile)
  );
}
function getBinderAndCheckerDiagnosticsOfFile(state, sourceFile, cancellationToken, semanticDiagnosticsPerFile) {
  semanticDiagnosticsPerFile ?? (semanticDiagnosticsPerFile ***REMOVED*** state.semanticDiagnosticsPerFile);
  const path ***REMOVED*** sourceFile.resolvedPath;
  const cachedDiagnostics ***REMOVED*** semanticDiagnosticsPerFile.get(path);
  if (cachedDiagnostics) {
    return filterSemanticDiagnostics(cachedDiagnostics, state.compilerOptions);
  }
  const diagnostics ***REMOVED*** state.program.getBindAndCheckDiagnostics(sourceFile, cancellationToken);
  semanticDiagnosticsPerFile.set(path, diagnostics);
  state.buildInfoEmitPending ***REMOVED*** true;
  return filterSemanticDiagnostics(diagnostics, state.compilerOptions);
}
function isIncrementalBundleEmitBuildInfo(info) {
  var _a;
  return !!((_a ***REMOVED*** info.options) ***REMOVED******REMOVED*** null ? void 0 : _a.outFile);
}
function isIncrementalBuildInfo(info) {
  return !!info.fileNames;
}
function isNonIncrementalBuildInfo(info) {
  return !isIncrementalBuildInfo(info) && !!info.root;
}
function ensureHasErrorsForState(state) {
  if (state.hasErrors !***REMOVED******REMOVED*** void 0) return;
  if (isIncrementalCompilation(state.compilerOptions)) {
    state.hasErrors ***REMOVED*** !some(state.program.getSourceFiles(), (f) ***REMOVED***> {
      var _a, _b;
      const bindAndCheckDiagnostics ***REMOVED*** state.semanticDiagnosticsPerFile.get(f.resolvedPath);
      return bindAndCheckDiagnostics ***REMOVED******REMOVED******REMOVED*** void 0 || // Missing semantic diagnostics in cache will be encoded in buildInfo
      !!bindAndCheckDiagnostics.length || // cached semantic diagnostics will be encoded in buildInfo
      !!((_b ***REMOVED*** (_a ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a.get(f.resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _b.length);
    }) && (hasSyntaxOrGlobalErrors(state) || some(state.program.getSourceFiles(), (f) ***REMOVED***> !!state.program.getProgramDiagnostics(f).length));
  } else {
    state.hasErrors ***REMOVED*** some(state.program.getSourceFiles(), (f) ***REMOVED***> {
      var _a, _b;
      const bindAndCheckDiagnostics ***REMOVED*** state.semanticDiagnosticsPerFile.get(f.resolvedPath);
      return !!(bindAndCheckDiagnostics ***REMOVED******REMOVED*** null ? void 0 : bindAndCheckDiagnostics.length) || // If has semantic diagnostics
      !!((_b ***REMOVED*** (_a ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a.get(f.resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _b.length);
    }) || hasSyntaxOrGlobalErrors(state);
  }
}
function hasSyntaxOrGlobalErrors(state) {
  return !!state.program.getConfigFileParsingDiagnostics().length || !!state.program.getSyntacticDiagnostics().length || !!state.program.getOptionsDiagnostics().length || !!state.program.getGlobalDiagnostics().length;
}
function getBuildInfoEmitPending(state) {
  ensureHasErrorsForState(state);
  return state.buildInfoEmitPending ?? (state.buildInfoEmitPending ***REMOVED*** !!state.hasErrorsFromOldState !***REMOVED******REMOVED*** !!state.hasErrors);
}
function getBuildInfo2(state) {
  var _a, _b;
  const currentDirectory ***REMOVED*** state.program.getCurrentDirectory();
  const buildInfoDirectory ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(getTsBuildInfoEmitOutputFilePath(state.compilerOptions), currentDirectory));
  const latestChangedDtsFile ***REMOVED*** state.latestChangedDtsFile ? relativeToBuildInfoEnsuringAbsolutePath(state.latestChangedDtsFile) : void 0;
  const fileNames ***REMOVED*** [];
  const fileNameToFileId ***REMOVED*** /* @__PURE__ */ new Map();
  const rootFileNames ***REMOVED*** new Set(state.program.getRootFileNames().map((f) ***REMOVED***> toPath(f, currentDirectory, state.program.getCanonicalFileName)));
  ensureHasErrorsForState(state);
  if (!isIncrementalCompilation(state.compilerOptions)) {
    const buildInfo2 ***REMOVED*** {
      root: arrayFrom(rootFileNames, (r) ***REMOVED***> relativeToBuildInfo(r)),
      errors: state.hasErrors ? true : void 0,
      checkPending: state.checkPending,
      version
    };
    return buildInfo2;
  }
  const root ***REMOVED*** [];
  if (state.compilerOptions.outFile) {
    const fileInfos2 ***REMOVED*** arrayFrom(state.fileInfos.entries(), ([key, value]) ***REMOVED***> {
      const fileId ***REMOVED*** toFileId(key);
      tryAddRoot(key, fileId);
      return value.impliedFormat ? { version: value.version, impliedFormat: value.impliedFormat, signature: void 0, affectsGlobalScope: void 0 } : value.version;
    });
    const buildInfo2 ***REMOVED*** {
      fileNames,
      fileInfos: fileInfos2,
      root,
      resolvedRoot: toResolvedRoot(),
      options: toIncrementalBuildInfoCompilerOptions(state.compilerOptions),
      semanticDiagnosticsPerFile: !state.changedFilesSet.size ? toIncrementalBuildInfoDiagnostics() : void 0,
      emitDiagnosticsPerFile: toIncrementalBuildInfoEmitDiagnostics(),
      changeFileSet: toChangeFileSet(),
      outSignature: state.outSignature,
      latestChangedDtsFile,
      pendingEmit: !state.programEmitPending ? void 0 : (
        // Pending is undefined or None is encoded as undefined
        state.programEmitPending ***REMOVED******REMOVED******REMOVED*** getBuilderFileEmit(state.compilerOptions) ? false : (
          // Pending emit is same as deteremined by compilerOptions
          state.programEmitPending
        )
      ),
      // Actual value
      errors: state.hasErrors ? true : void 0,
      checkPending: state.checkPending,
      version
    };
    return buildInfo2;
  }
  let fileIdsList;
  let fileNamesToFileIdListId;
  let emitSignatures;
  const fileInfos ***REMOVED*** arrayFrom(state.fileInfos.entries(), ([key, value]) ***REMOVED***> {
    var _a2, _b2;
    const fileId ***REMOVED*** toFileId(key);
    tryAddRoot(key, fileId);
    Debug.assert(fileNames[fileId - 1] ***REMOVED******REMOVED******REMOVED*** relativeToBuildInfo(key));
    const oldSignature ***REMOVED*** (_a2 ***REMOVED*** state.oldSignatures) ***REMOVED******REMOVED*** null ? void 0 : _a2.get(key);
    const actualSignature ***REMOVED*** oldSignature !***REMOVED******REMOVED*** void 0 ? oldSignature || void 0 : value.signature;
    if (state.compilerOptions.composite) {
      const file ***REMOVED*** state.program.getSourceFileByPath(key);
      if (!isJsonSourceFile(file) && sourceFileMayBeEmitted(file, state.program)) {
        const emitSignature ***REMOVED*** (_b2 ***REMOVED*** state.emitSignatures) ***REMOVED******REMOVED*** null ? void 0 : _b2.get(key);
        if (emitSignature !***REMOVED******REMOVED*** actualSignature) {
          emitSignatures ***REMOVED*** append(
            emitSignatures,
            emitSignature ***REMOVED******REMOVED******REMOVED*** void 0 ? fileId : (
              // There is no emit, encode as false
              // fileId, signature: emptyArray if signature only differs in dtsMap option than our own compilerOptions otherwise EmitSignature
              [fileId, !isString(emitSignature) && emitSignature[0] ***REMOVED******REMOVED******REMOVED*** actualSignature ? emptyArray : emitSignature]
            )
          );
        }
      }
    }
    return value.version ***REMOVED******REMOVED******REMOVED*** actualSignature ? value.affectsGlobalScope || value.impliedFormat ? (
      // If file version is same as signature, dont serialize signature
      { version: value.version, signature: void 0, affectsGlobalScope: value.affectsGlobalScope, impliedFormat: value.impliedFormat }
    ) : (
      // If file info only contains version and signature and both are same we can just write string
      value.version
    ) : actualSignature !***REMOVED******REMOVED*** void 0 ? (
      // If signature is not same as version, encode signature in the fileInfo
      oldSignature ***REMOVED******REMOVED******REMOVED*** void 0 ? (
        // If we havent computed signature, use fileInfo as is
        value
      ) : (
        // Serialize fileInfo with new updated signature
        { version: value.version, signature: actualSignature, affectsGlobalScope: value.affectsGlobalScope, impliedFormat: value.impliedFormat }
      )
    ) : (
      // Signature of the FileInfo is undefined, serialize it as false
      { version: value.version, signature: false, affectsGlobalScope: value.affectsGlobalScope, impliedFormat: value.impliedFormat }
    );
  });
  let referencedMap;
  if ((_a ***REMOVED*** state.referencedMap) ***REMOVED******REMOVED*** null ? void 0 : _a.size()) {
    referencedMap ***REMOVED*** arrayFrom(state.referencedMap.keys()).sort(compareStringsCaseSensitive).map((key) ***REMOVED***> [
      toFileId(key),
      toFileIdListId(state.referencedMap.getValues(key))
    ]);
  }
  const semanticDiagnosticsPerFile ***REMOVED*** toIncrementalBuildInfoDiagnostics();
  let affectedFilesPendingEmit;
  if ((_b ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _b.size) {
    const fullEmitForOptions ***REMOVED*** getBuilderFileEmit(state.compilerOptions);
    const seenFiles ***REMOVED*** /* @__PURE__ */ new Set();
    for (const path of arrayFrom(state.affectedFilesPendingEmit.keys()).sort(compareStringsCaseSensitive)) {
      if (tryAddToSet(seenFiles, path)) {
        const file ***REMOVED*** state.program.getSourceFileByPath(path);
        if (!file || !sourceFileMayBeEmitted(file, state.program)) continue;
        const fileId ***REMOVED*** toFileId(path), pendingEmit ***REMOVED*** state.affectedFilesPendingEmit.get(path);
        affectedFilesPendingEmit ***REMOVED*** append(
          affectedFilesPendingEmit,
          pendingEmit ***REMOVED******REMOVED******REMOVED*** fullEmitForOptions ? fileId : (
            // Pending full emit per options
            pendingEmit ***REMOVED******REMOVED******REMOVED*** 24 /* Dts */ ? [fileId] : (
              // Pending on Dts only
              [fileId, pendingEmit]
            )
          )
          // Anything else
        );
      }
    }
  }
  const buildInfo ***REMOVED*** {
    fileNames,
    fileIdsList,
    fileInfos,
    root,
    resolvedRoot: toResolvedRoot(),
    options: toIncrementalBuildInfoCompilerOptions(state.compilerOptions),
    referencedMap,
    semanticDiagnosticsPerFile,
    emitDiagnosticsPerFile: toIncrementalBuildInfoEmitDiagnostics(),
    changeFileSet: toChangeFileSet(),
    affectedFilesPendingEmit,
    emitSignatures,
    latestChangedDtsFile,
    errors: state.hasErrors ? true : void 0,
    checkPending: state.checkPending,
    version
  };
  return buildInfo;
  function relativeToBuildInfoEnsuringAbsolutePath(path) {
    return relativeToBuildInfo(getNormalizedAbsolutePath(path, currentDirectory));
  }
  function relativeToBuildInfo(path) {
    return ensurePathIsNonModuleName(getRelativePathFromDirectory(buildInfoDirectory, path, state.program.getCanonicalFileName));
  }
  function toFileId(path) {
    let fileId ***REMOVED*** fileNameToFileId.get(path);
    if (fileId ***REMOVED******REMOVED******REMOVED*** void 0) {
      fileNames.push(relativeToBuildInfo(path));
      fileNameToFileId.set(path, fileId ***REMOVED*** fileNames.length);
    }
    return fileId;
  }
  function toFileIdListId(set) {
    const fileIds ***REMOVED*** arrayFrom(set.keys(), toFileId).sort(compareValues);
    const key ***REMOVED*** fileIds.join();
    let fileIdListId ***REMOVED*** fileNamesToFileIdListId ***REMOVED******REMOVED*** null ? void 0 : fileNamesToFileIdListId.get(key);
    if (fileIdListId ***REMOVED******REMOVED******REMOVED*** void 0) {
      fileIdsList ***REMOVED*** append(fileIdsList, fileIds);
      (fileNamesToFileIdListId ?? (fileNamesToFileIdListId ***REMOVED*** /* @__PURE__ */ new Map())).set(key, fileIdListId ***REMOVED*** fileIdsList.length);
    }
    return fileIdListId;
  }
  function tryAddRoot(path, fileId) {
    const file ***REMOVED*** state.program.getSourceFile(path);
    if (!state.program.getFileIncludeReasons().get(file.path).some((r) ***REMOVED***> r.kind ***REMOVED******REMOVED******REMOVED*** 0 /* RootFile */)) return;
    if (!root.length) return root.push(fileId);
    const last2 ***REMOVED*** root[root.length - 1];
    const isLastStartEnd ***REMOVED*** isArray(last2);
    if (isLastStartEnd && last2[1] ***REMOVED******REMOVED******REMOVED*** fileId - 1) return last2[1] ***REMOVED*** fileId;
    if (isLastStartEnd || root.length ***REMOVED******REMOVED******REMOVED*** 1 || last2 !***REMOVED******REMOVED*** fileId - 1) return root.push(fileId);
    const lastButOne ***REMOVED*** root[root.length - 2];
    if (!isNumber(lastButOne) || lastButOne !***REMOVED******REMOVED*** last2 - 1) return root.push(fileId);
    root[root.length - 2] ***REMOVED*** [lastButOne, fileId];
    return root.length ***REMOVED*** root.length - 1;
  }
  function toResolvedRoot() {
    let result;
    rootFileNames.forEach((path) ***REMOVED***> {
      const file ***REMOVED*** state.program.getSourceFileByPath(path);
      if (file && path !***REMOVED******REMOVED*** file.resolvedPath) {
        result ***REMOVED*** append(result, [toFileId(file.resolvedPath), toFileId(path)]);
      }
    });
    return result;
  }
  function toIncrementalBuildInfoCompilerOptions(options) {
    let result;
    const { optionsNameMap } ***REMOVED*** getOptionsNameMap();
    for (const name of getOwnKeys(options).sort(compareStringsCaseSensitive)) {
      const optionInfo ***REMOVED*** optionsNameMap.get(name.toLowerCase());
      if (optionInfo ***REMOVED******REMOVED*** null ? void 0 : optionInfo.affectsBuildInfo) {
        (result || (result ***REMOVED*** {}))[name] ***REMOVED*** toReusableCompilerOptionValue(
          optionInfo,
          options[name]
        );
      }
    }
    return result;
  }
  function toReusableCompilerOptionValue(option, value) {
    if (option) {
      Debug.assert(option.type !***REMOVED******REMOVED*** "listOrElement");
      if (option.type ***REMOVED******REMOVED******REMOVED*** "list") {
        const values ***REMOVED*** value;
        if (option.element.isFilePath && values.length) {
          return values.map(relativeToBuildInfoEnsuringAbsolutePath);
        }
      } else if (option.isFilePath) {
        return relativeToBuildInfoEnsuringAbsolutePath(value);
      }
    }
    return value;
  }
  function toIncrementalBuildInfoDiagnostics() {
    let result;
    state.fileInfos.forEach((_value, key) ***REMOVED***> {
      const value ***REMOVED*** state.semanticDiagnosticsPerFile.get(key);
      if (!value) {
        if (!state.changedFilesSet.has(key)) result ***REMOVED*** append(result, toFileId(key));
      } else if (value.length) {
        result ***REMOVED*** append(result, [
          toFileId(key),
          toReusableDiagnostic(value, key)
        ]);
      }
    });
    return result;
  }
  function toIncrementalBuildInfoEmitDiagnostics() {
    var _a2;
    let result;
    if (!((_a2 ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a2.size)) return result;
    for (const key of arrayFrom(state.emitDiagnosticsPerFile.keys()).sort(compareStringsCaseSensitive)) {
      const value ***REMOVED*** state.emitDiagnosticsPerFile.get(key);
      result ***REMOVED*** append(result, [
        toFileId(key),
        toReusableDiagnostic(value, key)
      ]);
    }
    return result;
  }
  function toReusableDiagnostic(diagnostics, diagnosticFilePath) {
    Debug.assert(!!diagnostics.length);
    return diagnostics.map((diagnostic) ***REMOVED***> {
      const result ***REMOVED*** toReusableDiagnosticRelatedInformation(diagnostic, diagnosticFilePath);
      result.reportsUnnecessary ***REMOVED*** diagnostic.reportsUnnecessary;
      result.reportDeprecated ***REMOVED*** diagnostic.reportsDeprecated;
      result.source ***REMOVED*** diagnostic.source;
      result.skippedOn ***REMOVED*** diagnostic.skippedOn;
      const { relatedInformation } ***REMOVED*** diagnostic;
      result.relatedInformation ***REMOVED*** relatedInformation ? relatedInformation.length ? relatedInformation.map((r) ***REMOVED***> toReusableDiagnosticRelatedInformation(r, diagnosticFilePath)) : [] : void 0;
      return result;
    });
  }
  function toReusableDiagnosticRelatedInformation(diagnostic, diagnosticFilePath) {
    const { file } ***REMOVED*** diagnostic;
    return {
      ...diagnostic,
      file: file ? file.resolvedPath ***REMOVED******REMOVED******REMOVED*** diagnosticFilePath ? void 0 : relativeToBuildInfo(file.resolvedPath) : false,
      messageText: isString(diagnostic.messageText) ? diagnostic.messageText : toReusableDiagnosticMessageChain(diagnostic.messageText)
    };
  }
  function toReusableDiagnosticMessageChain(chain) {
    if (chain.repopulateInfo) {
      return {
        info: chain.repopulateInfo(),
        next: toReusableDiagnosticMessageChainArray(chain.next)
      };
    }
    const next ***REMOVED*** toReusableDiagnosticMessageChainArray(chain.next);
    return next ***REMOVED******REMOVED******REMOVED*** chain.next ? chain : { ...chain, next };
  }
  function toReusableDiagnosticMessageChainArray(array) {
    if (!array) return array;
    return forEach(array, (chain, index) ***REMOVED***> {
      const reusable ***REMOVED*** toReusableDiagnosticMessageChain(chain);
      if (chain ***REMOVED******REMOVED******REMOVED*** reusable) return void 0;
      const result ***REMOVED*** index > 0 ? array.slice(0, index - 1) : [];
      result.push(reusable);
      for (let i ***REMOVED*** index + 1; i < array.length; i++) {
        result.push(toReusableDiagnosticMessageChain(array[i]));
      }
      return result;
    }) || array;
  }
  function toChangeFileSet() {
    let changeFileSet;
    if (state.changedFilesSet.size) {
      for (const path of arrayFrom(state.changedFilesSet.keys()).sort(compareStringsCaseSensitive)) {
        changeFileSet ***REMOVED*** append(changeFileSet, toFileId(path));
      }
    }
    return changeFileSet;
  }
}
function getBuilderCreationParameters(newProgramOrRootNames, hostOrOptions, oldProgramOrHost, configFileParsingDiagnosticsOrOldProgram, configFileParsingDiagnostics, projectReferences) {
  let host;
  let newProgram;
  let oldProgram;
  if (newProgramOrRootNames ***REMOVED******REMOVED******REMOVED*** void 0) {
    Debug.assert(hostOrOptions ***REMOVED******REMOVED******REMOVED*** void 0);
    host ***REMOVED*** oldProgramOrHost;
    oldProgram ***REMOVED*** configFileParsingDiagnosticsOrOldProgram;
    Debug.assert(!!oldProgram);
    newProgram ***REMOVED*** oldProgram.getProgram();
  } else if (isArray(newProgramOrRootNames)) {
    oldProgram ***REMOVED*** configFileParsingDiagnosticsOrOldProgram;
    newProgram ***REMOVED*** createProgram({
      rootNames: newProgramOrRootNames,
      options: hostOrOptions,
      host: oldProgramOrHost,
      oldProgram: oldProgram && oldProgram.getProgramOrUndefined(),
      configFileParsingDiagnostics,
      projectReferences
    });
    host ***REMOVED*** oldProgramOrHost;
  } else {
    newProgram ***REMOVED*** newProgramOrRootNames;
    host ***REMOVED*** hostOrOptions;
    oldProgram ***REMOVED*** oldProgramOrHost;
    configFileParsingDiagnostics ***REMOVED*** configFileParsingDiagnosticsOrOldProgram;
  }
  return { host, newProgram, oldProgram, configFileParsingDiagnostics: configFileParsingDiagnostics || emptyArray };
}
function getTextHandlingSourceMapForSignature(text, data) {
  return (data ***REMOVED******REMOVED*** null ? void 0 : data.sourceMapUrlPos) !***REMOVED******REMOVED*** void 0 ? text.substring(0, data.sourceMapUrlPos) : text;
}
function computeSignatureWithDiagnostics(program, sourceFile, text, host, data) {
  var _a;
  text ***REMOVED*** getTextHandlingSourceMapForSignature(text, data);
  let sourceFileDirectory;
  if ((_a ***REMOVED*** data ***REMOVED******REMOVED*** null ? void 0 : data.diagnostics) ***REMOVED******REMOVED*** null ? void 0 : _a.length) {
    text +***REMOVED*** data.diagnostics.map((diagnostic) ***REMOVED***> `${locationInfo(diagnostic)}${DiagnosticCategory[diagnostic.category]}${diagnostic.code}: ${flattenDiagnosticMessageText2(diagnostic.messageText)}`).join("\n");
  }
  return (host.createHash ?? generateDjb2Hash)(text);
  function flattenDiagnosticMessageText2(diagnostic) {
    return isString(diagnostic) ? diagnostic : diagnostic ***REMOVED******REMOVED******REMOVED*** void 0 ? "" : !diagnostic.next ? diagnostic.messageText : diagnostic.messageText + diagnostic.next.map(flattenDiagnosticMessageText2).join("\n");
  }
  function locationInfo(diagnostic) {
    if (diagnostic.file.resolvedPath ***REMOVED******REMOVED******REMOVED*** sourceFile.resolvedPath) return `(${diagnostic.start},${diagnostic.length})`;
    if (sourceFileDirectory ***REMOVED******REMOVED******REMOVED*** void 0) sourceFileDirectory ***REMOVED*** getDirectoryPath(sourceFile.resolvedPath);
    return `${ensurePathIsNonModuleName(getRelativePathFromDirectory(
      sourceFileDirectory,
      diagnostic.file.resolvedPath,
      program.getCanonicalFileName
    ))}(${diagnostic.start},${diagnostic.length})`;
  }
}
function computeSignature(text, host, data) {
  return (host.createHash ?? generateDjb2Hash)(getTextHandlingSourceMapForSignature(text, data));
}
function createBuilderProgram(kind, { newProgram, host, oldProgram, configFileParsingDiagnostics }) {
  let oldState ***REMOVED*** oldProgram && oldProgram.state;
  if (oldState && newProgram ***REMOVED******REMOVED******REMOVED*** oldState.program && configFileParsingDiagnostics ***REMOVED******REMOVED******REMOVED*** newProgram.getConfigFileParsingDiagnostics()) {
    newProgram ***REMOVED*** void 0;
    oldState ***REMOVED*** void 0;
    return oldProgram;
  }
  const state ***REMOVED*** createBuilderProgramState(newProgram, oldState);
  newProgram.getBuildInfo ***REMOVED*** () ***REMOVED***> getBuildInfo2(toBuilderProgramStateWithDefinedProgram(state));
  newProgram ***REMOVED*** void 0;
  oldProgram ***REMOVED*** void 0;
  oldState ***REMOVED*** void 0;
  const builderProgram ***REMOVED*** createRedirectedBuilderProgram(state, configFileParsingDiagnostics);
  builderProgram.state ***REMOVED*** state;
  builderProgram.hasChangedEmitSignature ***REMOVED*** () ***REMOVED***> !!state.hasChangedEmitSignature;
  builderProgram.getAllDependencies ***REMOVED*** (sourceFile) ***REMOVED***> BuilderState.getAllDependencies(
    state,
    Debug.checkDefined(state.program),
    sourceFile
  );
  builderProgram.getSemanticDiagnostics ***REMOVED*** getSemanticDiagnostics;
  builderProgram.getDeclarationDiagnostics ***REMOVED*** getDeclarationDiagnostics2;
  builderProgram.emit ***REMOVED*** emit;
  builderProgram.releaseProgram ***REMOVED*** () ***REMOVED***> releaseCache(state);
  if (kind ***REMOVED******REMOVED******REMOVED*** 0 /* SemanticDiagnosticsBuilderProgram */) {
    builderProgram.getSemanticDiagnosticsOfNextAffectedFile ***REMOVED*** getSemanticDiagnosticsOfNextAffectedFile;
  } else if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* EmitAndSemanticDiagnosticsBuilderProgram */) {
    builderProgram.getSemanticDiagnosticsOfNextAffectedFile ***REMOVED*** getSemanticDiagnosticsOfNextAffectedFile;
    builderProgram.emitNextAffectedFile ***REMOVED*** emitNextAffectedFile;
    builderProgram.emitBuildInfo ***REMOVED*** emitBuildInfo;
  } else {
    notImplemented();
  }
  return builderProgram;
  function emitBuildInfo(writeFile2, cancellationToken) {
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    if (getBuildInfoEmitPending(state)) {
      const result ***REMOVED*** state.program.emitBuildInfo(
        writeFile2 || maybeBind(host, host.writeFile),
        cancellationToken
      );
      state.buildInfoEmitPending ***REMOVED*** false;
      return result;
    }
    return emitSkippedWithNoDiagnostics;
  }
  function emitNextAffectedFileOrDtsErrors(writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers, isForDtsErrors) {
    var _a, _b, _c, _d;
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    let affected ***REMOVED*** getNextAffectedFile(state, cancellationToken, host);
    const programEmitKind ***REMOVED*** getBuilderFileEmit(state.compilerOptions);
    let emitKind ***REMOVED*** !isForDtsErrors ? emitOnlyDtsFiles ? programEmitKind & 56 /* AllDts */ : programEmitKind : 8 /* DtsErrors */;
    if (!affected) {
      if (!state.compilerOptions.outFile) {
        const pendingAffectedFile ***REMOVED*** getNextAffectedFilePendingEmit(
          state,
          emitOnlyDtsFiles,
          isForDtsErrors
        );
        if (pendingAffectedFile) {
          ({ affectedFile: affected, emitKind } ***REMOVED*** pendingAffectedFile);
        } else {
          const pendingForDiagnostics ***REMOVED*** getNextPendingEmitDiagnosticsFile(
            state,
            isForDtsErrors
          );
          if (pendingForDiagnostics) {
            (state.seenEmittedFiles ?? (state.seenEmittedFiles ***REMOVED*** /* @__PURE__ */ new Map())).set(
              pendingForDiagnostics.affectedFile.resolvedPath,
              pendingForDiagnostics.seenKind | getBuilderFileEmitAllDts(isForDtsErrors)
            );
            return {
              result: { emitSkipped: true, diagnostics: pendingForDiagnostics.diagnostics },
              affected: pendingForDiagnostics.affectedFile
            };
          }
        }
      } else {
        if (state.programEmitPending) {
          emitKind ***REMOVED*** getPendingEmitKindWithSeen(
            state.programEmitPending,
            state.seenProgramEmit,
            emitOnlyDtsFiles,
            isForDtsErrors
          );
          if (emitKind) affected ***REMOVED*** state.program;
        }
        if (!affected && ((_a ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a.size)) {
          const seenKind ***REMOVED*** state.seenProgramEmit || 0 /* None */;
          if (!(seenKind & getBuilderFileEmitAllDts(isForDtsErrors))) {
            state.seenProgramEmit ***REMOVED*** getBuilderFileEmitAllDts(isForDtsErrors) | seenKind;
            const diagnostics ***REMOVED*** [];
            state.emitDiagnosticsPerFile.forEach((d) ***REMOVED***> addRange(diagnostics, d));
            return {
              result: { emitSkipped: true, diagnostics },
              affected: state.program
            };
          }
        }
      }
      if (!affected) {
        if (isForDtsErrors || !getBuildInfoEmitPending(state)) return void 0;
        const affected2 ***REMOVED*** state.program;
        const result2 ***REMOVED*** affected2.emitBuildInfo(
          writeFile2 || maybeBind(host, host.writeFile),
          cancellationToken
        );
        state.buildInfoEmitPending ***REMOVED*** false;
        return { result: result2, affected: affected2 };
      }
    }
    let emitOnly;
    if (emitKind & 7 /* AllJs */) emitOnly ***REMOVED*** 0 /* Js */;
    if (emitKind & 56 /* AllDts */) emitOnly ***REMOVED*** emitOnly ***REMOVED******REMOVED******REMOVED*** void 0 ? 1 /* Dts */ : void 0;
    const result ***REMOVED*** !isForDtsErrors ? state.program.emit(
      affected ***REMOVED******REMOVED******REMOVED*** state.program ? void 0 : affected,
      getWriteFileCallback(writeFile2, customTransformers),
      cancellationToken,
      emitOnly,
      customTransformers,
      /*forceDtsEmit*/
      void 0,
      /*skipBuildInfo*/
      true
    ) : {
      emitSkipped: true,
      diagnostics: state.program.getDeclarationDiagnostics(
        affected ***REMOVED******REMOVED******REMOVED*** state.program ? void 0 : affected,
        cancellationToken
      )
    };
    if (affected !***REMOVED******REMOVED*** state.program) {
      const affectedSourceFile ***REMOVED*** affected;
      state.seenAffectedFiles.add(affectedSourceFile.resolvedPath);
      if (state.affectedFilesIndex !***REMOVED******REMOVED*** void 0) state.affectedFilesIndex++;
      state.buildInfoEmitPending ***REMOVED*** true;
      const existing ***REMOVED*** ((_b ***REMOVED*** state.seenEmittedFiles) ***REMOVED******REMOVED*** null ? void 0 : _b.get(affectedSourceFile.resolvedPath)) || 0 /* None */;
      (state.seenEmittedFiles ?? (state.seenEmittedFiles ***REMOVED*** /* @__PURE__ */ new Map())).set(affectedSourceFile.resolvedPath, emitKind | existing);
      const existingPending ***REMOVED*** ((_c ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _c.get(affectedSourceFile.resolvedPath)) || programEmitKind;
      const pendingKind ***REMOVED*** getPendingEmitKind(existingPending, emitKind | existing);
      if (pendingKind) (state.affectedFilesPendingEmit ?? (state.affectedFilesPendingEmit ***REMOVED*** /* @__PURE__ */ new Map())).set(affectedSourceFile.resolvedPath, pendingKind);
      else (_d ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _d.delete(affectedSourceFile.resolvedPath);
      if (result.diagnostics.length) (state.emitDiagnosticsPerFile ?? (state.emitDiagnosticsPerFile ***REMOVED*** /* @__PURE__ */ new Map())).set(affectedSourceFile.resolvedPath, result.diagnostics);
    } else {
      state.changedFilesSet.clear();
      state.programEmitPending ***REMOVED*** state.changedFilesSet.size ? getPendingEmitKind(programEmitKind, emitKind) : state.programEmitPending ? getPendingEmitKind(state.programEmitPending, emitKind) : void 0;
      state.seenProgramEmit ***REMOVED*** emitKind | (state.seenProgramEmit || 0 /* None */);
      setEmitDiagnosticsPerFile(result.diagnostics);
      state.buildInfoEmitPending ***REMOVED*** true;
    }
    return { result, affected };
  }
  function setEmitDiagnosticsPerFile(diagnostics) {
    let emitDiagnosticsPerFile;
    diagnostics.forEach((d) ***REMOVED***> {
      if (!d.file) return;
      let diagnostics2 ***REMOVED*** emitDiagnosticsPerFile ***REMOVED******REMOVED*** null ? void 0 : emitDiagnosticsPerFile.get(d.file.resolvedPath);
      if (!diagnostics2) (emitDiagnosticsPerFile ?? (emitDiagnosticsPerFile ***REMOVED*** /* @__PURE__ */ new Map())).set(d.file.resolvedPath, diagnostics2 ***REMOVED*** []);
      diagnostics2.push(d);
    });
    if (emitDiagnosticsPerFile) state.emitDiagnosticsPerFile ***REMOVED*** emitDiagnosticsPerFile;
  }
  function emitNextAffectedFile(writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers) {
    return emitNextAffectedFileOrDtsErrors(
      writeFile2,
      cancellationToken,
      emitOnlyDtsFiles,
      customTransformers,
      /*isForDtsErrors*/
      false
    );
  }
  function getWriteFileCallback(writeFile2, customTransformers) {
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    if (!getEmitDeclarations(state.compilerOptions)) return writeFile2 || maybeBind(host, host.writeFile);
    return (fileName, text, writeByteOrderMark, onError, sourceFiles, data) ***REMOVED***> {
      var _a, _b, _c;
      if (isDeclarationFileName(fileName)) {
        if (!state.compilerOptions.outFile) {
          Debug.assert((sourceFiles ***REMOVED******REMOVED*** null ? void 0 : sourceFiles.length) ***REMOVED******REMOVED******REMOVED*** 1);
          let emitSignature;
          if (!customTransformers) {
            const file ***REMOVED*** sourceFiles[0];
            const info ***REMOVED*** state.fileInfos.get(file.resolvedPath);
            if (info.signature ***REMOVED******REMOVED******REMOVED*** file.version) {
              const signature ***REMOVED*** computeSignatureWithDiagnostics(
                state.program,
                file,
                text,
                host,
                data
              );
              if (!((_a ***REMOVED*** data ***REMOVED******REMOVED*** null ? void 0 : data.diagnostics) ***REMOVED******REMOVED*** null ? void 0 : _a.length)) emitSignature ***REMOVED*** signature;
              if (signature !***REMOVED******REMOVED*** file.version) {
                if (host.storeSignatureInfo) (state.signatureInfo ?? (state.signatureInfo ***REMOVED*** /* @__PURE__ */ new Map())).set(file.resolvedPath, 1 /* StoredSignatureAtEmit */);
                if (state.affectedFiles) {
                  const existing ***REMOVED*** (_b ***REMOVED*** state.oldSignatures) ***REMOVED******REMOVED*** null ? void 0 : _b.get(file.resolvedPath);
                  if (existing ***REMOVED******REMOVED******REMOVED*** void 0) (state.oldSignatures ?? (state.oldSignatures ***REMOVED*** /* @__PURE__ */ new Map())).set(file.resolvedPath, info.signature || false);
                  info.signature ***REMOVED*** signature;
                } else {
                  info.signature ***REMOVED*** signature;
                }
              }
            }
          }
          if (state.compilerOptions.composite) {
            const filePath ***REMOVED*** sourceFiles[0].resolvedPath;
            emitSignature ***REMOVED*** handleNewSignature((_c ***REMOVED*** state.emitSignatures) ***REMOVED******REMOVED*** null ? void 0 : _c.get(filePath), emitSignature);
            if (!emitSignature) return data.skippedDtsWrite ***REMOVED*** true;
            (state.emitSignatures ?? (state.emitSignatures ***REMOVED*** /* @__PURE__ */ new Map())).set(filePath, emitSignature);
          }
        } else if (state.compilerOptions.composite) {
          const newSignature ***REMOVED*** handleNewSignature(
            state.outSignature,
            /*newSignature*/
            void 0
          );
          if (!newSignature) return data.skippedDtsWrite ***REMOVED*** true;
          state.outSignature ***REMOVED*** newSignature;
        }
      }
      if (writeFile2) writeFile2(fileName, text, writeByteOrderMark, onError, sourceFiles, data);
      else if (host.writeFile) host.writeFile(fileName, text, writeByteOrderMark, onError, sourceFiles, data);
      else state.program.writeFile(fileName, text, writeByteOrderMark, onError, sourceFiles, data);
      function handleNewSignature(oldSignatureFormat, newSignature) {
        const oldSignature ***REMOVED*** !oldSignatureFormat || isString(oldSignatureFormat) ? oldSignatureFormat : oldSignatureFormat[0];
        newSignature ?? (newSignature ***REMOVED*** computeSignature(text, host, data));
        if (newSignature ***REMOVED******REMOVED******REMOVED*** oldSignature) {
          if (oldSignatureFormat ***REMOVED******REMOVED******REMOVED*** oldSignature) return void 0;
          else if (data) data.differsOnlyInMap ***REMOVED*** true;
          else data ***REMOVED*** { differsOnlyInMap: true };
        } else {
          state.hasChangedEmitSignature ***REMOVED*** true;
          state.latestChangedDtsFile ***REMOVED*** fileName;
        }
        return newSignature;
      }
    };
  }
  function emit(targetSourceFile, writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers) {
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* EmitAndSemanticDiagnosticsBuilderProgram */) {
      assertSourceFileOkWithoutNextAffectedCall(state, targetSourceFile);
    }
    const result ***REMOVED*** handleNoEmitOptions(builderProgram, targetSourceFile, writeFile2, cancellationToken);
    if (result) return result;
    if (!targetSourceFile) {
      if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* EmitAndSemanticDiagnosticsBuilderProgram */) {
        let sourceMaps ***REMOVED*** [];
        let emitSkipped ***REMOVED*** false;
        let diagnostics;
        let emittedFiles ***REMOVED*** [];
        let affectedEmitResult;
        while (affectedEmitResult ***REMOVED*** emitNextAffectedFile(
          writeFile2,
          cancellationToken,
          emitOnlyDtsFiles,
          customTransformers
        )) {
          emitSkipped ***REMOVED*** emitSkipped || affectedEmitResult.result.emitSkipped;
          diagnostics ***REMOVED*** addRange(diagnostics, affectedEmitResult.result.diagnostics);
          emittedFiles ***REMOVED*** addRange(emittedFiles, affectedEmitResult.result.emittedFiles);
          sourceMaps ***REMOVED*** addRange(sourceMaps, affectedEmitResult.result.sourceMaps);
        }
        return {
          emitSkipped,
          diagnostics: diagnostics || emptyArray,
          emittedFiles,
          sourceMaps
        };
      } else {
        clearAffectedFilesPendingEmit(
          state,
          emitOnlyDtsFiles,
          /*isForDtsErrors*/
          false
        );
      }
    }
    const emitResult ***REMOVED*** state.program.emit(
      targetSourceFile,
      getWriteFileCallback(writeFile2, customTransformers),
      cancellationToken,
      emitOnlyDtsFiles,
      customTransformers
    );
    handleNonEmitBuilderWithEmitOrDtsErrors(
      targetSourceFile,
      emitOnlyDtsFiles,
      /*isForDtsErrors*/
      false,
      emitResult.diagnostics
    );
    return emitResult;
  }
  function handleNonEmitBuilderWithEmitOrDtsErrors(targetSourceFile, emitOnlyDtsFiles, isForDtsErrors, diagnostics) {
    if (!targetSourceFile && kind !***REMOVED******REMOVED*** 1 /* EmitAndSemanticDiagnosticsBuilderProgram */) {
      clearAffectedFilesPendingEmit(state, emitOnlyDtsFiles, isForDtsErrors);
      setEmitDiagnosticsPerFile(diagnostics);
    }
  }
  function getDeclarationDiagnostics2(sourceFile, cancellationToken) {
    var _a;
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    if (kind ***REMOVED******REMOVED******REMOVED*** 1 /* EmitAndSemanticDiagnosticsBuilderProgram */) {
      assertSourceFileOkWithoutNextAffectedCall(state, sourceFile);
      let affectedEmitResult;
      let diagnostics;
      while (affectedEmitResult ***REMOVED*** emitNextAffectedFileOrDtsErrors(
        /*writeFile*/
        void 0,
        cancellationToken,
        /*emitOnlyDtsFiles*/
        void 0,
        /*customTransformers*/
        void 0,
        /*isForDtsErrors*/
        true
      )) {
        if (!sourceFile) diagnostics ***REMOVED*** addRange(diagnostics, affectedEmitResult.result.diagnostics);
      }
      return (!sourceFile ? diagnostics : (_a ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _a.get(sourceFile.resolvedPath)) || emptyArray;
    } else {
      const result ***REMOVED*** state.program.getDeclarationDiagnostics(sourceFile, cancellationToken);
      handleNonEmitBuilderWithEmitOrDtsErrors(
        sourceFile,
        /*emitOnlyDtsFiles*/
        void 0,
        /*isForDtsErrors*/
        true,
        result
      );
      return result;
    }
  }
  function getSemanticDiagnosticsOfNextAffectedFile(cancellationToken, ignoreSourceFile) {
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    while (true) {
      const affected ***REMOVED*** getNextAffectedFile(state, cancellationToken, host);
      let result;
      if (!affected) {
        if (state.checkPending && !state.compilerOptions.noCheck) {
          state.checkPending ***REMOVED*** void 0;
          state.buildInfoEmitPending ***REMOVED*** true;
        }
        return void 0;
      } else if (affected !***REMOVED******REMOVED*** state.program) {
        const affectedSourceFile ***REMOVED*** affected;
        if (!ignoreSourceFile || !ignoreSourceFile(affectedSourceFile)) {
          result ***REMOVED*** getSemanticDiagnosticsOfFile(state, affectedSourceFile, cancellationToken);
        }
        state.seenAffectedFiles.add(affectedSourceFile.resolvedPath);
        state.affectedFilesIndex++;
        state.buildInfoEmitPending ***REMOVED*** true;
        if (!result) continue;
      } else {
        let diagnostics;
        const semanticDiagnosticsPerFile ***REMOVED*** /* @__PURE__ */ new Map();
        state.program.getSourceFiles().forEach(
          (sourceFile) ***REMOVED***> diagnostics ***REMOVED*** addRange(
            diagnostics,
            getSemanticDiagnosticsOfFile(
              state,
              sourceFile,
              cancellationToken,
              semanticDiagnosticsPerFile
            )
          )
        );
        state.semanticDiagnosticsPerFile ***REMOVED*** semanticDiagnosticsPerFile;
        result ***REMOVED*** diagnostics || emptyArray;
        state.changedFilesSet.clear();
        state.programEmitPending ***REMOVED*** getBuilderFileEmit(state.compilerOptions);
        if (!state.compilerOptions.noCheck) state.checkPending ***REMOVED*** void 0;
        state.buildInfoEmitPending ***REMOVED*** true;
      }
      return { result, affected };
    }
  }
  function getSemanticDiagnostics(sourceFile, cancellationToken) {
    Debug.assert(isBuilderProgramStateWithDefinedProgram(state));
    assertSourceFileOkWithoutNextAffectedCall(state, sourceFile);
    if (sourceFile) {
      return getSemanticDiagnosticsOfFile(state, sourceFile, cancellationToken);
    }
    while (true) {
      const affectedResult ***REMOVED*** getSemanticDiagnosticsOfNextAffectedFile(cancellationToken);
      if (!affectedResult) break;
      if (affectedResult.affected ***REMOVED******REMOVED******REMOVED*** state.program) return affectedResult.result;
    }
    let diagnostics;
    for (const sourceFile2 of state.program.getSourceFiles()) {
      diagnostics ***REMOVED*** addRange(diagnostics, getSemanticDiagnosticsOfFile(state, sourceFile2, cancellationToken));
    }
    if (state.checkPending && !state.compilerOptions.noCheck) {
      state.checkPending ***REMOVED*** void 0;
      state.buildInfoEmitPending ***REMOVED*** true;
    }
    return diagnostics || emptyArray;
  }
}
function addToAffectedFilesPendingEmit(state, affectedFilePendingEmit, kind) {
  var _a, _b;
  const existingKind ***REMOVED*** ((_a ***REMOVED*** state.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _a.get(affectedFilePendingEmit)) || 0 /* None */;
  (state.affectedFilesPendingEmit ?? (state.affectedFilesPendingEmit ***REMOVED*** /* @__PURE__ */ new Map())).set(affectedFilePendingEmit, existingKind | kind);
  (_b ***REMOVED*** state.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _b.delete(affectedFilePendingEmit);
}
function toBuilderStateFileInfoForMultiEmit(fileInfo) {
  return isString(fileInfo) ? { version: fileInfo, signature: fileInfo, affectsGlobalScope: void 0, impliedFormat: void 0 } : isString(fileInfo.signature) ? fileInfo : { version: fileInfo.version, signature: fileInfo.signature ***REMOVED******REMOVED******REMOVED*** false ? void 0 : fileInfo.version, affectsGlobalScope: fileInfo.affectsGlobalScope, impliedFormat: fileInfo.impliedFormat };
}
function toBuilderFileEmit(value, fullEmitForOptions) {
  return isNumber(value) ? fullEmitForOptions : value[1] || 24 /* Dts */;
}
function toProgramEmitPending(value, options) {
  return !value ? getBuilderFileEmit(options || {}) : value;
}
function createBuilderProgramUsingIncrementalBuildInfo(buildInfo, buildInfoPath, host) {
  var _a, _b, _c, _d;
  const buildInfoDirectory ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(buildInfoPath, host.getCurrentDirectory()));
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames());
  let state;
  const filePaths ***REMOVED*** (_a ***REMOVED*** buildInfo.fileNames) ***REMOVED******REMOVED*** null ? void 0 : _a.map(toPathInBuildInfoDirectory);
  let filePathsSetList;
  const latestChangedDtsFile ***REMOVED*** buildInfo.latestChangedDtsFile ? toAbsolutePath(buildInfo.latestChangedDtsFile) : void 0;
  const fileInfos ***REMOVED*** /* @__PURE__ */ new Map();
  const changedFilesSet ***REMOVED*** new Set(map(buildInfo.changeFileSet, toFilePath));
  if (isIncrementalBundleEmitBuildInfo(buildInfo)) {
    buildInfo.fileInfos.forEach((fileInfo, index) ***REMOVED***> {
      const path ***REMOVED*** toFilePath(index + 1);
      fileInfos.set(path, isString(fileInfo) ? { version: fileInfo, signature: void 0, affectsGlobalScope: void 0, impliedFormat: void 0 } : fileInfo);
    });
    state ***REMOVED*** {
      fileInfos,
      compilerOptions: buildInfo.options ? convertToOptionsWithAbsolutePaths(buildInfo.options, toAbsolutePath) : {},
      semanticDiagnosticsPerFile: toPerFileSemanticDiagnostics(buildInfo.semanticDiagnosticsPerFile),
      emitDiagnosticsPerFile: toPerFileEmitDiagnostics(buildInfo.emitDiagnosticsPerFile),
      hasReusableDiagnostic: true,
      changedFilesSet,
      latestChangedDtsFile,
      outSignature: buildInfo.outSignature,
      programEmitPending: buildInfo.pendingEmit ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : toProgramEmitPending(buildInfo.pendingEmit, buildInfo.options),
      hasErrors: buildInfo.errors,
      checkPending: buildInfo.checkPending
    };
  } else {
    filePathsSetList ***REMOVED*** (_b ***REMOVED*** buildInfo.fileIdsList) ***REMOVED******REMOVED*** null ? void 0 : _b.map((fileIds) ***REMOVED***> new Set(fileIds.map(toFilePath)));
    const emitSignatures ***REMOVED*** ((_c ***REMOVED*** buildInfo.options) ***REMOVED******REMOVED*** null ? void 0 : _c.composite) && !buildInfo.options.outFile ? /* @__PURE__ */ new Map() : void 0;
    buildInfo.fileInfos.forEach((fileInfo, index) ***REMOVED***> {
      const path ***REMOVED*** toFilePath(index + 1);
      const stateFileInfo ***REMOVED*** toBuilderStateFileInfoForMultiEmit(fileInfo);
      fileInfos.set(path, stateFileInfo);
      if (emitSignatures && stateFileInfo.signature) emitSignatures.set(path, stateFileInfo.signature);
    });
    (_d ***REMOVED*** buildInfo.emitSignatures) ***REMOVED******REMOVED*** null ? void 0 : _d.forEach((value) ***REMOVED***> {
      if (isNumber(value)) emitSignatures.delete(toFilePath(value));
      else {
        const key ***REMOVED*** toFilePath(value[0]);
        emitSignatures.set(
          key,
          !isString(value[1]) && !value[1].length ? (
            // File signature is emit signature but differs in map
            [emitSignatures.get(key)]
          ) : value[1]
        );
      }
    });
    const fullEmitForOptions ***REMOVED*** buildInfo.affectedFilesPendingEmit ? getBuilderFileEmit(buildInfo.options || {}) : void 0;
    state ***REMOVED*** {
      fileInfos,
      compilerOptions: buildInfo.options ? convertToOptionsWithAbsolutePaths(buildInfo.options, toAbsolutePath) : {},
      referencedMap: toManyToManyPathMap(buildInfo.referencedMap, buildInfo.options ?? {}),
      semanticDiagnosticsPerFile: toPerFileSemanticDiagnostics(buildInfo.semanticDiagnosticsPerFile),
      emitDiagnosticsPerFile: toPerFileEmitDiagnostics(buildInfo.emitDiagnosticsPerFile),
      hasReusableDiagnostic: true,
      changedFilesSet,
      affectedFilesPendingEmit: buildInfo.affectedFilesPendingEmit && arrayToMap(buildInfo.affectedFilesPendingEmit, (value) ***REMOVED***> toFilePath(isNumber(value) ? value : value[0]), (value) ***REMOVED***> toBuilderFileEmit(value, fullEmitForOptions)),
      latestChangedDtsFile,
      emitSignatures: (emitSignatures ***REMOVED******REMOVED*** null ? void 0 : emitSignatures.size) ? emitSignatures : void 0,
      hasErrors: buildInfo.errors,
      checkPending: buildInfo.checkPending
    };
  }
  return {
    state,
    getProgram: notImplemented,
    getProgramOrUndefined: returnUndefined,
    releaseProgram: noop,
    getCompilerOptions: () ***REMOVED***> state.compilerOptions,
    getSourceFile: notImplemented,
    getSourceFiles: notImplemented,
    getOptionsDiagnostics: notImplemented,
    getGlobalDiagnostics: notImplemented,
    getConfigFileParsingDiagnostics: notImplemented,
    getSyntacticDiagnostics: notImplemented,
    getDeclarationDiagnostics: notImplemented,
    getSemanticDiagnostics: notImplemented,
    emit: notImplemented,
    getAllDependencies: notImplemented,
    getCurrentDirectory: notImplemented,
    emitNextAffectedFile: notImplemented,
    getSemanticDiagnosticsOfNextAffectedFile: notImplemented,
    emitBuildInfo: notImplemented,
    close: noop,
    hasChangedEmitSignature: returnFalse
  };
  function toPathInBuildInfoDirectory(path) {
    return toPath(path, buildInfoDirectory, getCanonicalFileName);
  }
  function toAbsolutePath(path) {
    return getNormalizedAbsolutePath(path, buildInfoDirectory);
  }
  function toFilePath(fileId) {
    return filePaths[fileId - 1];
  }
  function toFilePathsSet(fileIdsListId) {
    return filePathsSetList[fileIdsListId - 1];
  }
  function toManyToManyPathMap(referenceMap, options) {
    const map2 ***REMOVED*** BuilderState.createReferencedMap(options);
    if (!map2 || !referenceMap) return map2;
    referenceMap.forEach(([fileId, fileIdListId]) ***REMOVED***> map2.set(toFilePath(fileId), toFilePathsSet(fileIdListId)));
    return map2;
  }
  function toPerFileSemanticDiagnostics(diagnostics) {
    const semanticDiagnostics ***REMOVED*** new Map(
      mapDefinedIterator(
        fileInfos.keys(),
        (key) ***REMOVED***> !changedFilesSet.has(key) ? [key, emptyArray] : void 0
      )
    );
    diagnostics ***REMOVED******REMOVED*** null ? void 0 : diagnostics.forEach((value) ***REMOVED***> {
      if (isNumber(value)) semanticDiagnostics.delete(toFilePath(value));
      else semanticDiagnostics.set(toFilePath(value[0]), value[1]);
    });
    return semanticDiagnostics;
  }
  function toPerFileEmitDiagnostics(diagnostics) {
    return diagnostics && arrayToMap(diagnostics, (value) ***REMOVED***> toFilePath(value[0]), (value) ***REMOVED***> value[1]);
  }
}
function getBuildInfoFileVersionMap(program, buildInfoPath, host) {
  const buildInfoDirectory ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(buildInfoPath, host.getCurrentDirectory()));
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames());
  const fileInfos ***REMOVED*** /* @__PURE__ */ new Map();
  let rootIndex ***REMOVED*** 0;
  const roots ***REMOVED*** /* @__PURE__ */ new Map();
  const resolvedRoots ***REMOVED*** new Map(program.resolvedRoot);
  program.fileInfos.forEach((fileInfo, index) ***REMOVED***> {
    const path ***REMOVED*** toPath(program.fileNames[index], buildInfoDirectory, getCanonicalFileName);
    const version2 ***REMOVED*** isString(fileInfo) ? fileInfo : fileInfo.version;
    fileInfos.set(path, version2);
    if (rootIndex < program.root.length) {
      const current ***REMOVED*** program.root[rootIndex];
      const fileId ***REMOVED*** index + 1;
      if (isArray(current)) {
        if (current[0] <***REMOVED*** fileId && fileId <***REMOVED*** current[1]) {
          addRoot(fileId, path);
          if (current[1] ***REMOVED******REMOVED******REMOVED*** fileId) rootIndex++;
        }
      } else if (current ***REMOVED******REMOVED******REMOVED*** fileId) {
        addRoot(fileId, path);
        rootIndex++;
      }
    }
  });
  return { fileInfos, roots };
  function addRoot(fileId, path) {
    const root ***REMOVED*** resolvedRoots.get(fileId);
    if (root) {
      roots.set(toPath(program.fileNames[root - 1], buildInfoDirectory, getCanonicalFileName), path);
    } else {
      roots.set(path, void 0);
    }
  }
}
function getNonIncrementalBuildInfoRoots(buildInfo, buildInfoPath, host) {
  if (!isNonIncrementalBuildInfo(buildInfo)) return void 0;
  const buildInfoDirectory ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(buildInfoPath, host.getCurrentDirectory()));
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames());
  return buildInfo.root.map((r) ***REMOVED***> toPath(r, buildInfoDirectory, getCanonicalFileName));
}
function createRedirectedBuilderProgram(state, configFileParsingDiagnostics) {
  return {
    state: void 0,
    getProgram,
    getProgramOrUndefined: () ***REMOVED***> state.program,
    releaseProgram: () ***REMOVED***> state.program ***REMOVED*** void 0,
    getCompilerOptions: () ***REMOVED***> state.compilerOptions,
    getSourceFile: (fileName) ***REMOVED***> getProgram().getSourceFile(fileName),
    getSourceFiles: () ***REMOVED***> getProgram().getSourceFiles(),
    getOptionsDiagnostics: (cancellationToken) ***REMOVED***> getProgram().getOptionsDiagnostics(cancellationToken),
    getGlobalDiagnostics: (cancellationToken) ***REMOVED***> getProgram().getGlobalDiagnostics(cancellationToken),
    getConfigFileParsingDiagnostics: () ***REMOVED***> configFileParsingDiagnostics,
    getSyntacticDiagnostics: (sourceFile, cancellationToken) ***REMOVED***> getProgram().getSyntacticDiagnostics(sourceFile, cancellationToken),
    getDeclarationDiagnostics: (sourceFile, cancellationToken) ***REMOVED***> getProgram().getDeclarationDiagnostics(sourceFile, cancellationToken),
    getSemanticDiagnostics: (sourceFile, cancellationToken) ***REMOVED***> getProgram().getSemanticDiagnostics(sourceFile, cancellationToken),
    emit: (sourceFile, writeFile2, cancellationToken, emitOnlyDts, customTransformers) ***REMOVED***> getProgram().emit(sourceFile, writeFile2, cancellationToken, emitOnlyDts, customTransformers),
    emitBuildInfo: (writeFile2, cancellationToken) ***REMOVED***> getProgram().emitBuildInfo(writeFile2, cancellationToken),
    getAllDependencies: notImplemented,
    getCurrentDirectory: () ***REMOVED***> getProgram().getCurrentDirectory(),
    close: noop
  };
  function getProgram() {
    return Debug.checkDefined(state.program);
  }
}

// src/compiler/builderPublic.ts
function createEmitAndSemanticDiagnosticsBuilderProgram(newProgramOrRootNames, hostOrOptions, oldProgramOrHost, configFileParsingDiagnosticsOrOldProgram, configFileParsingDiagnostics, projectReferences) {
  return createBuilderProgram(
    1 /* EmitAndSemanticDiagnosticsBuilderProgram */,
    getBuilderCreationParameters(
      newProgramOrRootNames,
      hostOrOptions,
      oldProgramOrHost,
      configFileParsingDiagnosticsOrOldProgram,
      configFileParsingDiagnostics,
      projectReferences
    )
  );
}

// src/compiler/resolutionCache.ts
function removeIgnoredPath(path) {
  if (endsWith(path, "/node_modules/.staging")) {
    return removeSuffix(path, "/.staging");
  }
  return some(ignoredPaths, (searchPath) ***REMOVED***> path.includes(searchPath)) ? void 0 : path;
}
function perceivedOsRootLengthForWatching(pathComponents2, length2) {
  if (length2 <***REMOVED*** 1) return 1;
  let indexAfterOsRoot ***REMOVED*** 1;
  let isDosStyle ***REMOVED*** pathComponents2[0].search(/[a-z]:/i) ***REMOVED******REMOVED******REMOVED*** 0;
  if (pathComponents2[0] !***REMOVED******REMOVED*** directorySeparator && !isDosStyle && // Non dos style paths
  pathComponents2[1].search(/[a-z]\$$/i) ***REMOVED******REMOVED******REMOVED*** 0) {
    if (length2 ***REMOVED******REMOVED******REMOVED*** 2) return 2;
    indexAfterOsRoot ***REMOVED*** 2;
    isDosStyle ***REMOVED*** true;
  }
  if (isDosStyle && !pathComponents2[indexAfterOsRoot].match(/^users$/i)) {
    return indexAfterOsRoot;
  }
  if (pathComponents2[indexAfterOsRoot].match(/^workspaces$/i)) {
    return indexAfterOsRoot + 1;
  }
  return indexAfterOsRoot + 2;
}
function canWatchDirectoryOrFile(pathComponents2, length2) {
  if (length2 ***REMOVED******REMOVED******REMOVED*** void 0) length2 ***REMOVED*** pathComponents2.length;
  if (length2 <***REMOVED*** 2) return false;
  const perceivedOsRootLength ***REMOVED*** perceivedOsRootLengthForWatching(pathComponents2, length2);
  return length2 > perceivedOsRootLength + 1;
}
function canWatchDirectoryOrFilePath(path) {
  return canWatchDirectoryOrFile(getPathComponents(path));
}
function canWatchAtTypes(atTypes) {
  return canWatchAffectedPackageJsonOrNodeModulesOfAtTypes(getDirectoryPath(atTypes));
}
function isInDirectoryPath(dirComponents, fileOrDirComponents) {
  if (fileOrDirComponents.length < dirComponents.length) return false;
  for (let i ***REMOVED*** 0; i < dirComponents.length; i++) {
    if (fileOrDirComponents[i] !***REMOVED******REMOVED*** dirComponents[i]) return false;
  }
  return true;
}
function canWatchAffectedPackageJsonOrNodeModulesOfAtTypes(fileOrDirPath) {
  return canWatchDirectoryOrFilePath(fileOrDirPath);
}
function canWatchAffectingLocation(filePath) {
  return canWatchAffectedPackageJsonOrNodeModulesOfAtTypes(filePath);
}
function getDirectoryToWatchFailedLookupLocation(failedLookupLocation, failedLookupLocationPath, rootDir, rootPath, rootPathComponents, isRootWatchable, getCurrentDirectory, preferNonRecursiveWatch) {
  const failedLookupPathComponents ***REMOVED*** getPathComponents(failedLookupLocationPath);
  failedLookupLocation ***REMOVED*** isRootedDiskPath(failedLookupLocation) ? normalizePath(failedLookupLocation) : getNormalizedAbsolutePath(failedLookupLocation, getCurrentDirectory());
  const failedLookupComponents ***REMOVED*** getPathComponents(failedLookupLocation);
  const perceivedOsRootLength ***REMOVED*** perceivedOsRootLengthForWatching(failedLookupPathComponents, failedLookupPathComponents.length);
  if (failedLookupPathComponents.length <***REMOVED*** perceivedOsRootLength + 1) return void 0;
  const nodeModulesIndex ***REMOVED*** failedLookupPathComponents.indexOf("node_modules");
  if (nodeModulesIndex !***REMOVED******REMOVED*** -1 && nodeModulesIndex + 1 <***REMOVED*** perceivedOsRootLength + 1) return void 0;
  const lastNodeModulesIndex ***REMOVED*** failedLookupPathComponents.lastIndexOf("node_modules");
  if (isRootWatchable && isInDirectoryPath(rootPathComponents, failedLookupPathComponents)) {
    if (failedLookupPathComponents.length > rootPathComponents.length + 1) {
      return getDirectoryOfFailedLookupWatch(
        failedLookupComponents,
        failedLookupPathComponents,
        Math.max(rootPathComponents.length + 1, perceivedOsRootLength + 1),
        lastNodeModulesIndex
      );
    } else {
      return {
        dir: rootDir,
        dirPath: rootPath,
        nonRecursive: true
      };
    }
  }
  return getDirectoryToWatchFromFailedLookupLocationDirectory(
    failedLookupComponents,
    failedLookupPathComponents,
    failedLookupPathComponents.length - 1,
    perceivedOsRootLength,
    nodeModulesIndex,
    rootPathComponents,
    lastNodeModulesIndex,
    preferNonRecursiveWatch
  );
}
function getDirectoryToWatchFromFailedLookupLocationDirectory(dirComponents, dirPathComponents, dirPathComponentsLength, perceivedOsRootLength, nodeModulesIndex, rootPathComponents, lastNodeModulesIndex, preferNonRecursiveWatch) {
  if (nodeModulesIndex !***REMOVED******REMOVED*** -1) {
    return getDirectoryOfFailedLookupWatch(
      dirComponents,
      dirPathComponents,
      nodeModulesIndex + 1,
      lastNodeModulesIndex
    );
  }
  let nonRecursive ***REMOVED*** true;
  let length2 ***REMOVED*** dirPathComponentsLength;
  if (!preferNonRecursiveWatch) {
    for (let i ***REMOVED*** 0; i < dirPathComponentsLength; i++) {
      if (dirPathComponents[i] !***REMOVED******REMOVED*** rootPathComponents[i]) {
        nonRecursive ***REMOVED*** false;
        length2 ***REMOVED*** Math.max(i + 1, perceivedOsRootLength + 1);
        break;
      }
    }
  }
  return getDirectoryOfFailedLookupWatch(
    dirComponents,
    dirPathComponents,
    length2,
    lastNodeModulesIndex,
    nonRecursive
  );
}
function getDirectoryOfFailedLookupWatch(dirComponents, dirPathComponents, length2, lastNodeModulesIndex, nonRecursive) {
  let packageDirLength;
  if (lastNodeModulesIndex !***REMOVED******REMOVED*** -1 && lastNodeModulesIndex + 1 >***REMOVED*** length2 && lastNodeModulesIndex + 2 < dirPathComponents.length) {
    if (!startsWith(dirPathComponents[lastNodeModulesIndex + 1], "@")) {
      packageDirLength ***REMOVED*** lastNodeModulesIndex + 2;
    } else if (lastNodeModulesIndex + 3 < dirPathComponents.length) {
      packageDirLength ***REMOVED*** lastNodeModulesIndex + 3;
    }
  }
  return {
    dir: getPathFromPathComponents(dirComponents, length2),
    dirPath: getPathFromPathComponents(dirPathComponents, length2),
    nonRecursive,
    packageDir: packageDirLength !***REMOVED******REMOVED*** void 0 ? getPathFromPathComponents(dirComponents, packageDirLength) : void 0,
    packageDirPath: packageDirLength !***REMOVED******REMOVED*** void 0 ? getPathFromPathComponents(dirPathComponents, packageDirLength) : void 0
  };
}
function getDirectoryToWatchFailedLookupLocationFromTypeRoot(typeRoot, typeRootPath, rootPath, rootPathComponents, isRootWatchable, getCurrentDirectory, preferNonRecursiveWatch, filterCustomPath) {
  const typeRootPathComponents ***REMOVED*** getPathComponents(typeRootPath);
  if (isRootWatchable && isInDirectoryPath(rootPathComponents, typeRootPathComponents)) {
    return rootPath;
  }
  typeRoot ***REMOVED*** isRootedDiskPath(typeRoot) ? normalizePath(typeRoot) : getNormalizedAbsolutePath(typeRoot, getCurrentDirectory());
  const toWatch ***REMOVED*** getDirectoryToWatchFromFailedLookupLocationDirectory(
    getPathComponents(typeRoot),
    typeRootPathComponents,
    typeRootPathComponents.length,
    perceivedOsRootLengthForWatching(typeRootPathComponents, typeRootPathComponents.length),
    typeRootPathComponents.indexOf("node_modules"),
    rootPathComponents,
    typeRootPathComponents.lastIndexOf("node_modules"),
    preferNonRecursiveWatch
  );
  return toWatch && filterCustomPath(toWatch.dirPath) ? toWatch.dirPath : void 0;
}
function getRootDirectoryOfResolutionCache(rootDirForResolution, getCurrentDirectory) {
  const normalized ***REMOVED*** getNormalizedAbsolutePath(rootDirForResolution, getCurrentDirectory());
  return !isDiskPathRoot(normalized) ? removeTrailingDirectorySeparator(normalized) : normalized;
}
function getModuleResolutionHost(resolutionHost) {
  var _a;
  return ((_a ***REMOVED*** resolutionHost.getCompilerHost) ***REMOVED******REMOVED*** null ? void 0 : _a.call(resolutionHost)) || resolutionHost;
}
function createModuleResolutionLoaderUsingGlobalCache(containingFile, redirectedReference, options, resolutionHost, moduleResolutionCache) {
  return {
    nameAndMode: moduleResolutionNameAndModeGetter,
    resolve: (moduleName, resoluionMode) ***REMOVED***> resolveModuleNameUsingGlobalCache(
      resolutionHost,
      moduleResolutionCache,
      moduleName,
      containingFile,
      options,
      redirectedReference,
      resoluionMode
    )
  };
}
function resolveModuleNameUsingGlobalCache(resolutionHost, moduleResolutionCache, moduleName, containingFile, compilerOptions, redirectedReference, mode) {
  const host ***REMOVED*** getModuleResolutionHost(resolutionHost);
  const primaryResult ***REMOVED*** resolveModuleName(moduleName, containingFile, compilerOptions, host, moduleResolutionCache, redirectedReference, mode);
  if (!resolutionHost.getGlobalTypingsCacheLocation) {
    return primaryResult;
  }
  const globalCache ***REMOVED*** resolutionHost.getGlobalTypingsCacheLocation();
  if (globalCache !***REMOVED******REMOVED*** void 0 && !isExternalModuleNameRelative(moduleName) && !(primaryResult.resolvedModule && extensionIsTS(primaryResult.resolvedModule.extension))) {
    const { resolvedModule, failedLookupLocations, affectingLocations, resolutionDiagnostics } ***REMOVED*** loadModuleFromGlobalCache(
      Debug.checkDefined(resolutionHost.globalCacheResolutionModuleName)(moduleName),
      resolutionHost.projectName,
      compilerOptions,
      host,
      globalCache,
      moduleResolutionCache
    );
    if (resolvedModule) {
      primaryResult.resolvedModule ***REMOVED*** resolvedModule;
      primaryResult.failedLookupLocations ***REMOVED*** updateResolutionField(primaryResult.failedLookupLocations, failedLookupLocations);
      primaryResult.affectingLocations ***REMOVED*** updateResolutionField(primaryResult.affectingLocations, affectingLocations);
      primaryResult.resolutionDiagnostics ***REMOVED*** updateResolutionField(primaryResult.resolutionDiagnostics, resolutionDiagnostics);
      return primaryResult;
    }
  }
  return primaryResult;
}
function createResolutionCache(resolutionHost, rootDirForResolution, logChangesWhenResolvingModule) {
  let filesWithChangedSetOfUnresolvedImports;
  let filesWithInvalidatedResolutions;
  let filesWithInvalidatedNonRelativeUnresolvedImports;
  const nonRelativeExternalModuleResolutions ***REMOVED*** /* @__PURE__ */ new Set();
  const resolutionsWithFailedLookups ***REMOVED*** /* @__PURE__ */ new Set();
  const resolutionsWithOnlyAffectingLocations ***REMOVED*** /* @__PURE__ */ new Set();
  const resolvedFileToResolution ***REMOVED*** /* @__PURE__ */ new Map();
  const impliedFormatPackageJsons ***REMOVED*** /* @__PURE__ */ new Map();
  let hasChangedAutomaticTypeDirectiveNames ***REMOVED*** false;
  let affectingPathChecksForFile;
  let affectingPathChecks;
  let failedLookupChecks;
  let startsWithPathChecks;
  let isInDirectoryChecks;
  let allModuleAndTypeResolutionsAreInvalidated ***REMOVED*** false;
  const getCurrentDirectory ***REMOVED*** memoize(() ***REMOVED***> resolutionHost.getCurrentDirectory());
  const cachedDirectoryStructureHost ***REMOVED*** resolutionHost.getCachedDirectoryStructureHost();
  const resolvedModuleNames ***REMOVED*** /* @__PURE__ */ new Map();
  const moduleResolutionCache ***REMOVED*** createModuleResolutionCache(
    getCurrentDirectory(),
    resolutionHost.getCanonicalFileName,
    resolutionHost.getCompilationSettings()
  );
  const resolvedTypeReferenceDirectives ***REMOVED*** /* @__PURE__ */ new Map();
  const typeReferenceDirectiveResolutionCache ***REMOVED*** createTypeReferenceDirectiveResolutionCache(
    getCurrentDirectory(),
    resolutionHost.getCanonicalFileName,
    resolutionHost.getCompilationSettings(),
    moduleResolutionCache.getPackageJsonInfoCache(),
    moduleResolutionCache.optionsToRedirectsKey
  );
  const resolvedLibraries ***REMOVED*** /* @__PURE__ */ new Map();
  const libraryResolutionCache ***REMOVED*** createModuleResolutionCache(
    getCurrentDirectory(),
    resolutionHost.getCanonicalFileName,
    getOptionsForLibraryResolution(resolutionHost.getCompilationSettings()),
    moduleResolutionCache.getPackageJsonInfoCache()
  );
  const directoryWatchesOfFailedLookups ***REMOVED*** /* @__PURE__ */ new Map();
  const fileWatchesOfAffectingLocations ***REMOVED*** /* @__PURE__ */ new Map();
  const rootDir ***REMOVED*** getRootDirectoryOfResolutionCache(rootDirForResolution, getCurrentDirectory);
  const rootPath ***REMOVED*** resolutionHost.toPath(rootDir);
  const rootPathComponents ***REMOVED*** getPathComponents(rootPath);
  const isRootWatchable ***REMOVED*** canWatchDirectoryOrFile(rootPathComponents);
  const isSymlinkCache ***REMOVED*** /* @__PURE__ */ new Map();
  const packageDirWatchers ***REMOVED*** /* @__PURE__ */ new Map();
  const dirPathToSymlinkPackageRefCount ***REMOVED*** /* @__PURE__ */ new Map();
  const typeRootsWatches ***REMOVED*** /* @__PURE__ */ new Map();
  return {
    rootDirForResolution,
    resolvedModuleNames,
    resolvedTypeReferenceDirectives,
    resolvedLibraries,
    resolvedFileToResolution,
    resolutionsWithFailedLookups,
    resolutionsWithOnlyAffectingLocations,
    directoryWatchesOfFailedLookups,
    fileWatchesOfAffectingLocations,
    packageDirWatchers,
    dirPathToSymlinkPackageRefCount,
    watchFailedLookupLocationsOfExternalModuleResolutions,
    getModuleResolutionCache: () ***REMOVED***> moduleResolutionCache,
    startRecordingFilesWithChangedResolutions,
    finishRecordingFilesWithChangedResolutions,
    // perDirectoryResolvedModuleNames and perDirectoryResolvedTypeReferenceDirectives could be non empty if there was exception during program update
    // (between startCachingPerDirectoryResolution and finishCachingPerDirectoryResolution)
    startCachingPerDirectoryResolution,
    finishCachingPerDirectoryResolution,
    resolveModuleNameLiterals,
    resolveTypeReferenceDirectiveReferences,
    resolveLibrary: resolveLibrary2,
    resolveSingleModuleNameWithoutWatching,
    removeResolutionsFromProjectReferenceRedirects,
    removeResolutionsOfFile,
    hasChangedAutomaticTypeDirectiveNames: () ***REMOVED***> hasChangedAutomaticTypeDirectiveNames,
    invalidateResolutionOfFile,
    invalidateResolutionsOfFailedLookupLocations,
    setFilesWithInvalidatedNonRelativeUnresolvedImports,
    createHasInvalidatedResolutions,
    isFileWithInvalidatedNonRelativeUnresolvedImports,
    updateTypeRootsWatch,
    closeTypeRootsWatch,
    clear: clear2,
    onChangesAffectModuleResolution
  };
  function clear2() {
    clearMap(directoryWatchesOfFailedLookups, closeFileWatcherOf);
    clearMap(fileWatchesOfAffectingLocations, closeFileWatcherOf);
    isSymlinkCache.clear();
    packageDirWatchers.clear();
    dirPathToSymlinkPackageRefCount.clear();
    nonRelativeExternalModuleResolutions.clear();
    closeTypeRootsWatch();
    resolvedModuleNames.clear();
    resolvedTypeReferenceDirectives.clear();
    resolvedFileToResolution.clear();
    resolutionsWithFailedLookups.clear();
    resolutionsWithOnlyAffectingLocations.clear();
    failedLookupChecks ***REMOVED*** void 0;
    startsWithPathChecks ***REMOVED*** void 0;
    isInDirectoryChecks ***REMOVED*** void 0;
    affectingPathChecks ***REMOVED*** void 0;
    affectingPathChecksForFile ***REMOVED*** void 0;
    allModuleAndTypeResolutionsAreInvalidated ***REMOVED*** false;
    moduleResolutionCache.clear();
    typeReferenceDirectiveResolutionCache.clear();
    moduleResolutionCache.update(resolutionHost.getCompilationSettings());
    typeReferenceDirectiveResolutionCache.update(resolutionHost.getCompilationSettings());
    libraryResolutionCache.clear();
    impliedFormatPackageJsons.clear();
    resolvedLibraries.clear();
    hasChangedAutomaticTypeDirectiveNames ***REMOVED*** false;
  }
  function onChangesAffectModuleResolution() {
    allModuleAndTypeResolutionsAreInvalidated ***REMOVED*** true;
    moduleResolutionCache.clearAllExceptPackageJsonInfoCache();
    typeReferenceDirectiveResolutionCache.clearAllExceptPackageJsonInfoCache();
    moduleResolutionCache.update(resolutionHost.getCompilationSettings());
    typeReferenceDirectiveResolutionCache.update(resolutionHost.getCompilationSettings());
  }
  function startRecordingFilesWithChangedResolutions() {
    filesWithChangedSetOfUnresolvedImports ***REMOVED*** [];
  }
  function finishRecordingFilesWithChangedResolutions() {
    const collected ***REMOVED*** filesWithChangedSetOfUnresolvedImports;
    filesWithChangedSetOfUnresolvedImports ***REMOVED*** void 0;
    return collected;
  }
  function isFileWithInvalidatedNonRelativeUnresolvedImports(path) {
    if (!filesWithInvalidatedNonRelativeUnresolvedImports) {
      return false;
    }
    const value ***REMOVED*** filesWithInvalidatedNonRelativeUnresolvedImports.get(path);
    return !!value && !!value.length;
  }
  function createHasInvalidatedResolutions(customHasInvalidatedResolutions, customHasInvalidatedLibResolutions) {
    invalidateResolutionsOfFailedLookupLocations();
    const collected ***REMOVED*** filesWithInvalidatedResolutions;
    filesWithInvalidatedResolutions ***REMOVED*** void 0;
    return {
      hasInvalidatedResolutions: (path) ***REMOVED***> customHasInvalidatedResolutions(path) || allModuleAndTypeResolutionsAreInvalidated || !!(collected ***REMOVED******REMOVED*** null ? void 0 : collected.has(path)) || isFileWithInvalidatedNonRelativeUnresolvedImports(path),
      hasInvalidatedLibResolutions: (libFileName) ***REMOVED***> {
        var _a;
        return customHasInvalidatedLibResolutions(libFileName) || !!((_a ***REMOVED*** resolvedLibraries ***REMOVED******REMOVED*** null ? void 0 : resolvedLibraries.get(libFileName)) ***REMOVED******REMOVED*** null ? void 0 : _a.isInvalidated);
      }
    };
  }
  function startCachingPerDirectoryResolution() {
    moduleResolutionCache.isReadonly ***REMOVED*** void 0;
    typeReferenceDirectiveResolutionCache.isReadonly ***REMOVED*** void 0;
    libraryResolutionCache.isReadonly ***REMOVED*** void 0;
    moduleResolutionCache.getPackageJsonInfoCache().isReadonly ***REMOVED*** void 0;
    moduleResolutionCache.clearAllExceptPackageJsonInfoCache();
    typeReferenceDirectiveResolutionCache.clearAllExceptPackageJsonInfoCache();
    libraryResolutionCache.clearAllExceptPackageJsonInfoCache();
    watchFailedLookupLocationOfNonRelativeModuleResolutions();
    isSymlinkCache.clear();
  }
  function cleanupLibResolutionWatching(newProgram) {
    resolvedLibraries.forEach((resolution, libFileName) ***REMOVED***> {
      var _a;
      if (!((_a ***REMOVED*** newProgram ***REMOVED******REMOVED*** null ? void 0 : newProgram.resolvedLibReferences) ***REMOVED******REMOVED*** null ? void 0 : _a.has(libFileName))) {
        stopWatchFailedLookupLocationOfResolution(
          resolution,
          resolutionHost.toPath(getInferredLibraryNameResolveFrom(resolutionHost.getCompilationSettings(), getCurrentDirectory(), libFileName)),
          getResolvedModuleFromResolution
        );
        resolvedLibraries.delete(libFileName);
      }
    });
  }
  function finishCachingPerDirectoryResolution(newProgram, oldProgram) {
    filesWithInvalidatedNonRelativeUnresolvedImports ***REMOVED*** void 0;
    allModuleAndTypeResolutionsAreInvalidated ***REMOVED*** false;
    watchFailedLookupLocationOfNonRelativeModuleResolutions();
    if (newProgram !***REMOVED******REMOVED*** oldProgram) {
      cleanupLibResolutionWatching(newProgram);
      newProgram ***REMOVED******REMOVED*** null ? void 0 : newProgram.getSourceFiles().forEach((newFile) ***REMOVED***> {
        var _a;
        const expected ***REMOVED*** ((_a ***REMOVED*** newFile.packageJsonLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.length) ?? 0;
        const existing ***REMOVED*** impliedFormatPackageJsons.get(newFile.resolvedPath) ?? emptyArray;
        for (let i ***REMOVED*** existing.length; i < expected; i++) {
          createFileWatcherOfAffectingLocation(
            newFile.packageJsonLocations[i],
            /*forResolution*/
            false
          );
        }
        if (existing.length > expected) {
          for (let i ***REMOVED*** expected; i < existing.length; i++) {
            fileWatchesOfAffectingLocations.get(existing[i]).files--;
          }
        }
        if (expected) impliedFormatPackageJsons.set(newFile.resolvedPath, newFile.packageJsonLocations);
        else impliedFormatPackageJsons.delete(newFile.resolvedPath);
      });
      impliedFormatPackageJsons.forEach((existing, path) ***REMOVED***> {
        const newFile ***REMOVED*** newProgram ***REMOVED******REMOVED*** null ? void 0 : newProgram.getSourceFileByPath(path);
        if (!newFile || newFile.resolvedPath !***REMOVED******REMOVED*** path) {
          existing.forEach((location) ***REMOVED***> fileWatchesOfAffectingLocations.get(location).files--);
          impliedFormatPackageJsons.delete(path);
        }
      });
    }
    directoryWatchesOfFailedLookups.forEach(closeDirectoryWatchesOfFailedLookup);
    fileWatchesOfAffectingLocations.forEach(closeFileWatcherOfAffectingLocation);
    packageDirWatchers.forEach(closePackageDirWatcher);
    hasChangedAutomaticTypeDirectiveNames ***REMOVED*** false;
    moduleResolutionCache.isReadonly ***REMOVED*** true;
    typeReferenceDirectiveResolutionCache.isReadonly ***REMOVED*** true;
    libraryResolutionCache.isReadonly ***REMOVED*** true;
    moduleResolutionCache.getPackageJsonInfoCache().isReadonly ***REMOVED*** true;
    isSymlinkCache.clear();
  }
  function closePackageDirWatcher(watcher, packageDirPath) {
    if (watcher.dirPathToWatcher.size ***REMOVED******REMOVED******REMOVED*** 0) {
      packageDirWatchers.delete(packageDirPath);
    }
  }
  function closeDirectoryWatchesOfFailedLookup(watcher, path) {
    if (watcher.refCount ***REMOVED******REMOVED******REMOVED*** 0) {
      directoryWatchesOfFailedLookups.delete(path);
      watcher.watcher.close();
    }
  }
  function closeFileWatcherOfAffectingLocation(watcher, path) {
    var _a;
    if (watcher.files ***REMOVED******REMOVED******REMOVED*** 0 && watcher.resolutions ***REMOVED******REMOVED******REMOVED*** 0 && !((_a ***REMOVED*** watcher.symlinks) ***REMOVED******REMOVED*** null ? void 0 : _a.size)) {
      fileWatchesOfAffectingLocations.delete(path);
      watcher.watcher.close();
    }
  }
  function resolveNamesWithLocalCache({
    entries,
    containingFile,
    containingSourceFile,
    redirectedReference,
    options,
    perFileCache,
    reusedNames,
    loader,
    getResolutionWithResolvedFileName,
    deferWatchingNonRelativeResolution,
    shouldRetryResolution,
    logChanges
  }) {
    var _a;
    const path ***REMOVED*** resolutionHost.toPath(containingFile);
    const resolutionsInFile ***REMOVED*** perFileCache.get(path) || perFileCache.set(path, createModeAwareCache()).get(path);
    const resolvedModules ***REMOVED*** [];
    const hasInvalidatedNonRelativeUnresolvedImport ***REMOVED*** logChanges && isFileWithInvalidatedNonRelativeUnresolvedImports(path);
    const program ***REMOVED*** resolutionHost.getCurrentProgram();
    const oldRedirect ***REMOVED*** program && ((_a ***REMOVED*** program.getRedirectFromSourceFile(containingFile)) ***REMOVED******REMOVED*** null ? void 0 : _a.resolvedRef);
    const unmatchedRedirects ***REMOVED*** oldRedirect ? !redirectedReference || redirectedReference.sourceFile.path !***REMOVED******REMOVED*** oldRedirect.sourceFile.path : !!redirectedReference;
    const seenNamesInFile ***REMOVED*** createModeAwareCache();
    for (const entry of entries) {
      const name ***REMOVED*** loader.nameAndMode.getName(entry);
      const mode ***REMOVED*** loader.nameAndMode.getMode(entry, containingSourceFile, (redirectedReference ***REMOVED******REMOVED*** null ? void 0 : redirectedReference.commandLine.options) || options);
      let resolution ***REMOVED*** resolutionsInFile.get(name, mode);
      if (!seenNamesInFile.has(name, mode) && (allModuleAndTypeResolutionsAreInvalidated || unmatchedRedirects || !resolution || resolution.isInvalidated || // If the name is unresolved import that was invalidated, recalculate
      hasInvalidatedNonRelativeUnresolvedImport && !isExternalModuleNameRelative(name) && shouldRetryResolution(resolution))) {
        const existingResolution ***REMOVED*** resolution;
        resolution ***REMOVED*** loader.resolve(name, mode);
        if (resolutionHost.onDiscoveredSymlink && resolutionIsSymlink(resolution)) {
          resolutionHost.onDiscoveredSymlink();
        }
        resolutionsInFile.set(name, mode, resolution);
        if (resolution !***REMOVED******REMOVED*** existingResolution) {
          watchFailedLookupLocationsOfExternalModuleResolutions(name, resolution, path, getResolutionWithResolvedFileName, deferWatchingNonRelativeResolution);
          if (existingResolution) {
            stopWatchFailedLookupLocationOfResolution(existingResolution, path, getResolutionWithResolvedFileName);
          }
        }
        if (logChanges && filesWithChangedSetOfUnresolvedImports && !resolutionIsEqualTo(existingResolution, resolution)) {
          filesWithChangedSetOfUnresolvedImports.push(path);
          logChanges ***REMOVED*** false;
        }
      } else {
        const host ***REMOVED*** getModuleResolutionHost(resolutionHost);
        if (isTraceEnabled(options, host) && !seenNamesInFile.has(name, mode)) {
          const resolved ***REMOVED*** getResolutionWithResolvedFileName(resolution);
          trace(
            host,
            perFileCache ***REMOVED******REMOVED******REMOVED*** resolvedModuleNames ? (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.resolvedFileName) ? resolved.packageId ? Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_not_resolved : (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.resolvedFileName) ? resolved.packageId ? Diagnostics.Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_successfully_resolved_to_2 : Diagnostics.Reusing_resolution_of_type_reference_directive_0_from_1_of_old_program_it_was_not_resolved,
            name,
            containingFile,
            resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.resolvedFileName,
            (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.packageId) && packageIdToString(resolved.packageId)
          );
        }
      }
      Debug.assert(resolution !***REMOVED******REMOVED*** void 0 && !resolution.isInvalidated);
      seenNamesInFile.set(name, mode, true);
      resolvedModules.push(resolution);
    }
    reusedNames ***REMOVED******REMOVED*** null ? void 0 : reusedNames.forEach(
      (entry) ***REMOVED***> seenNamesInFile.set(
        loader.nameAndMode.getName(entry),
        loader.nameAndMode.getMode(entry, containingSourceFile, (redirectedReference ***REMOVED******REMOVED*** null ? void 0 : redirectedReference.commandLine.options) || options),
        true
      )
    );
    if (resolutionsInFile.size() !***REMOVED******REMOVED*** seenNamesInFile.size()) {
      resolutionsInFile.forEach((resolution, name, mode) ***REMOVED***> {
        if (!seenNamesInFile.has(name, mode)) {
          stopWatchFailedLookupLocationOfResolution(resolution, path, getResolutionWithResolvedFileName);
          resolutionsInFile.delete(name, mode);
        }
      });
    }
    return resolvedModules;
    function resolutionIsEqualTo(oldResolution, newResolution) {
      if (oldResolution ***REMOVED******REMOVED******REMOVED*** newResolution) {
        return true;
      }
      if (!oldResolution || !newResolution) {
        return false;
      }
      const oldResult ***REMOVED*** getResolutionWithResolvedFileName(oldResolution);
      const newResult ***REMOVED*** getResolutionWithResolvedFileName(newResolution);
      if (oldResult ***REMOVED******REMOVED******REMOVED*** newResult) {
        return true;
      }
      if (!oldResult || !newResult) {
        return false;
      }
      return oldResult.resolvedFileName ***REMOVED******REMOVED******REMOVED*** newResult.resolvedFileName;
    }
  }
  function resolveTypeReferenceDirectiveReferences(typeDirectiveReferences, containingFile, redirectedReference, options, containingSourceFile, reusedNames) {
    return resolveNamesWithLocalCache({
      entries: typeDirectiveReferences,
      containingFile,
      containingSourceFile,
      redirectedReference,
      options,
      reusedNames,
      perFileCache: resolvedTypeReferenceDirectives,
      loader: createTypeReferenceResolutionLoader(
        containingFile,
        redirectedReference,
        options,
        getModuleResolutionHost(resolutionHost),
        typeReferenceDirectiveResolutionCache
      ),
      getResolutionWithResolvedFileName: getResolvedTypeReferenceDirectiveFromResolution,
      shouldRetryResolution: (resolution) ***REMOVED***> resolution.resolvedTypeReferenceDirective ***REMOVED******REMOVED******REMOVED*** void 0,
      deferWatchingNonRelativeResolution: false
    });
  }
  function resolveModuleNameLiterals(moduleLiterals, containingFile, redirectedReference, options, containingSourceFile, reusedNames) {
    return resolveNamesWithLocalCache({
      entries: moduleLiterals,
      containingFile,
      containingSourceFile,
      redirectedReference,
      options,
      reusedNames,
      perFileCache: resolvedModuleNames,
      loader: createModuleResolutionLoaderUsingGlobalCache(
        containingFile,
        redirectedReference,
        options,
        resolutionHost,
        moduleResolutionCache
      ),
      getResolutionWithResolvedFileName: getResolvedModuleFromResolution,
      shouldRetryResolution: (resolution) ***REMOVED***> !resolution.resolvedModule || !resolutionExtensionIsTSOrJson(resolution.resolvedModule.extension),
      logChanges: logChangesWhenResolvingModule,
      deferWatchingNonRelativeResolution: true
      // Defer non relative resolution watch because we could be using ambient modules
    });
  }
  function resolveLibrary2(libraryName, resolveFrom, options, libFileName) {
    const host ***REMOVED*** getModuleResolutionHost(resolutionHost);
    let resolution ***REMOVED*** resolvedLibraries ***REMOVED******REMOVED*** null ? void 0 : resolvedLibraries.get(libFileName);
    if (!resolution || resolution.isInvalidated) {
      const existingResolution ***REMOVED*** resolution;
      resolution ***REMOVED*** resolveLibrary(libraryName, resolveFrom, options, host, libraryResolutionCache);
      const path ***REMOVED*** resolutionHost.toPath(resolveFrom);
      watchFailedLookupLocationsOfExternalModuleResolutions(
        libraryName,
        resolution,
        path,
        getResolvedModuleFromResolution,
        /*deferWatchingNonRelativeResolution*/
        false
      );
      resolvedLibraries.set(libFileName, resolution);
      if (existingResolution) {
        stopWatchFailedLookupLocationOfResolution(existingResolution, path, getResolvedModuleFromResolution);
      }
    } else {
      if (isTraceEnabled(options, host)) {
        const resolved ***REMOVED*** getResolvedModuleFromResolution(resolution);
        trace(
          host,
          (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.resolvedFileName) ? resolved.packageId ? Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2_with_Package_ID_3 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_successfully_resolved_to_2 : Diagnostics.Reusing_resolution_of_module_0_from_1_of_old_program_it_was_not_resolved,
          libraryName,
          resolveFrom,
          resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.resolvedFileName,
          (resolved ***REMOVED******REMOVED*** null ? void 0 : resolved.packageId) && packageIdToString(resolved.packageId)
        );
      }
    }
    return resolution;
  }
  function resolveSingleModuleNameWithoutWatching(moduleName, containingFile) {
    var _a, _b;
    const path ***REMOVED*** resolutionHost.toPath(containingFile);
    const resolutionsInFile ***REMOVED*** resolvedModuleNames.get(path);
    const resolution ***REMOVED*** resolutionsInFile ***REMOVED******REMOVED*** null ? void 0 : resolutionsInFile.get(
      moduleName,
      /*mode*/
      void 0
    );
    if (resolution && !resolution.isInvalidated) return resolution;
    const data ***REMOVED*** (_a ***REMOVED*** resolutionHost.beforeResolveSingleModuleNameWithoutWatching) ***REMOVED******REMOVED*** null ? void 0 : _a.call(resolutionHost, moduleResolutionCache);
    const host ***REMOVED*** getModuleResolutionHost(resolutionHost);
    const result ***REMOVED*** resolveModuleName(
      moduleName,
      containingFile,
      resolutionHost.getCompilationSettings(),
      host,
      moduleResolutionCache
    );
    (_b ***REMOVED*** resolutionHost.afterResolveSingleModuleNameWithoutWatching) ***REMOVED******REMOVED*** null ? void 0 : _b.call(resolutionHost, moduleResolutionCache, moduleName, containingFile, result, data);
    return result;
  }
  function isNodeModulesAtTypesDirectory(dirPath) {
    return endsWith(dirPath, "/node_modules/@types");
  }
  function watchFailedLookupLocationsOfExternalModuleResolutions(name, resolution, filePath, getResolutionWithResolvedFileName, deferWatchingNonRelativeResolution) {
    (resolution.files ?? (resolution.files ***REMOVED*** /* @__PURE__ */ new Set())).add(filePath);
    if (resolution.files.size !***REMOVED******REMOVED*** 1) return;
    if (!deferWatchingNonRelativeResolution || isExternalModuleNameRelative(name)) {
      watchFailedLookupLocationOfResolution(resolution);
    } else {
      nonRelativeExternalModuleResolutions.add(resolution);
    }
    const resolved ***REMOVED*** getResolutionWithResolvedFileName(resolution);
    if (resolved && resolved.resolvedFileName) {
      const key ***REMOVED*** resolutionHost.toPath(resolved.resolvedFileName);
      let resolutions ***REMOVED*** resolvedFileToResolution.get(key);
      if (!resolutions) resolvedFileToResolution.set(key, resolutions ***REMOVED*** /* @__PURE__ */ new Set());
      resolutions.add(resolution);
    }
  }
  function watchFailedLookupLocation(failedLookupLocation, setAtRoot) {
    const failedLookupLocationPath ***REMOVED*** resolutionHost.toPath(failedLookupLocation);
    const toWatch ***REMOVED*** getDirectoryToWatchFailedLookupLocation(
      failedLookupLocation,
      failedLookupLocationPath,
      rootDir,
      rootPath,
      rootPathComponents,
      isRootWatchable,
      getCurrentDirectory,
      resolutionHost.preferNonRecursiveWatch
    );
    if (toWatch) {
      const { dir, dirPath, nonRecursive, packageDir, packageDirPath } ***REMOVED*** toWatch;
      if (dirPath ***REMOVED******REMOVED******REMOVED*** rootPath) {
        Debug.assert(nonRecursive);
        Debug.assert(!packageDir);
        setAtRoot ***REMOVED*** true;
      } else {
        setDirectoryWatcher(dir, dirPath, packageDir, packageDirPath, nonRecursive);
      }
    }
    return setAtRoot;
  }
  function watchFailedLookupLocationOfResolution(resolution) {
    var _a;
    Debug.assert(!!((_a ***REMOVED*** resolution.files) ***REMOVED******REMOVED*** null ? void 0 : _a.size));
    const { failedLookupLocations, affectingLocations, alternateResult } ***REMOVED*** resolution;
    if (!(failedLookupLocations ***REMOVED******REMOVED*** null ? void 0 : failedLookupLocations.length) && !(affectingLocations ***REMOVED******REMOVED*** null ? void 0 : affectingLocations.length) && !alternateResult) return;
    if ((failedLookupLocations ***REMOVED******REMOVED*** null ? void 0 : failedLookupLocations.length) || alternateResult) resolutionsWithFailedLookups.add(resolution);
    let setAtRoot ***REMOVED*** false;
    if (failedLookupLocations) {
      for (const failedLookupLocation of failedLookupLocations) {
        setAtRoot ***REMOVED*** watchFailedLookupLocation(failedLookupLocation, setAtRoot);
      }
    }
    if (alternateResult) setAtRoot ***REMOVED*** watchFailedLookupLocation(alternateResult, setAtRoot);
    if (setAtRoot) {
      setDirectoryWatcher(
        rootDir,
        rootPath,
        /*packageDir*/
        void 0,
        /*packageDirPath*/
        void 0,
        /*nonRecursive*/
        true
      );
    }
    watchAffectingLocationsOfResolution(resolution, !(failedLookupLocations ***REMOVED******REMOVED*** null ? void 0 : failedLookupLocations.length) && !alternateResult);
  }
  function watchAffectingLocationsOfResolution(resolution, addToResolutionsWithOnlyAffectingLocations) {
    var _a;
    Debug.assert(!!((_a ***REMOVED*** resolution.files) ***REMOVED******REMOVED*** null ? void 0 : _a.size));
    const { affectingLocations } ***REMOVED*** resolution;
    if (!(affectingLocations ***REMOVED******REMOVED*** null ? void 0 : affectingLocations.length)) return;
    if (addToResolutionsWithOnlyAffectingLocations) resolutionsWithOnlyAffectingLocations.add(resolution);
    for (const affectingLocation of affectingLocations) {
      createFileWatcherOfAffectingLocation(
        affectingLocation,
        /*forResolution*/
        true
      );
    }
  }
  function createFileWatcherOfAffectingLocation(affectingLocation, forResolution) {
    const fileWatcher ***REMOVED*** fileWatchesOfAffectingLocations.get(affectingLocation);
    if (fileWatcher) {
      if (forResolution) fileWatcher.resolutions++;
      else fileWatcher.files++;
      return;
    }
    let locationToWatch ***REMOVED*** affectingLocation;
    let isSymlink ***REMOVED*** false;
    let symlinkWatcher;
    if (resolutionHost.realpath) {
      locationToWatch ***REMOVED*** resolutionHost.realpath(affectingLocation);
      if (affectingLocation !***REMOVED******REMOVED*** locationToWatch) {
        isSymlink ***REMOVED*** true;
        symlinkWatcher ***REMOVED*** fileWatchesOfAffectingLocations.get(locationToWatch);
      }
    }
    const resolutions ***REMOVED*** forResolution ? 1 : 0;
    const files ***REMOVED*** forResolution ? 0 : 1;
    if (!isSymlink || !symlinkWatcher) {
      const watcher ***REMOVED*** {
        watcher: canWatchAffectingLocation(resolutionHost.toPath(locationToWatch)) ? resolutionHost.watchAffectingFileLocation(locationToWatch, (fileName, eventKind) ***REMOVED***> {
          cachedDirectoryStructureHost ***REMOVED******REMOVED*** null ? void 0 : cachedDirectoryStructureHost.addOrDeleteFile(fileName, resolutionHost.toPath(locationToWatch), eventKind);
          invalidateAffectingFileWatcher(locationToWatch, moduleResolutionCache.getPackageJsonInfoCache().getInternalMap());
          resolutionHost.scheduleInvalidateResolutionsOfFailedLookupLocations();
        }) : noopFileWatcher,
        resolutions: isSymlink ? 0 : resolutions,
        files: isSymlink ? 0 : files,
        symlinks: void 0
      };
      fileWatchesOfAffectingLocations.set(locationToWatch, watcher);
      if (isSymlink) symlinkWatcher ***REMOVED*** watcher;
    }
    if (isSymlink) {
      Debug.assert(!!symlinkWatcher);
      const watcher ***REMOVED*** {
        watcher: {
          close: () ***REMOVED***> {
            var _a;
            const symlinkWatcher2 ***REMOVED*** fileWatchesOfAffectingLocations.get(locationToWatch);
            if (((_a ***REMOVED*** symlinkWatcher2 ***REMOVED******REMOVED*** null ? void 0 : symlinkWatcher2.symlinks) ***REMOVED******REMOVED*** null ? void 0 : _a.delete(affectingLocation)) && !symlinkWatcher2.symlinks.size && !symlinkWatcher2.resolutions && !symlinkWatcher2.files) {
              fileWatchesOfAffectingLocations.delete(locationToWatch);
              symlinkWatcher2.watcher.close();
            }
          }
        },
        resolutions,
        files,
        symlinks: void 0
      };
      fileWatchesOfAffectingLocations.set(affectingLocation, watcher);
      (symlinkWatcher.symlinks ?? (symlinkWatcher.symlinks ***REMOVED*** /* @__PURE__ */ new Set())).add(affectingLocation);
    }
  }
  function invalidateAffectingFileWatcher(path, packageJsonMap) {
    var _a;
    const watcher ***REMOVED*** fileWatchesOfAffectingLocations.get(path);
    if (watcher ***REMOVED******REMOVED*** null ? void 0 : watcher.resolutions) (affectingPathChecks ?? (affectingPathChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(path);
    if (watcher ***REMOVED******REMOVED*** null ? void 0 : watcher.files) (affectingPathChecksForFile ?? (affectingPathChecksForFile ***REMOVED*** /* @__PURE__ */ new Set())).add(path);
    (_a ***REMOVED*** watcher ***REMOVED******REMOVED*** null ? void 0 : watcher.symlinks) ***REMOVED******REMOVED*** null ? void 0 : _a.forEach((path2) ***REMOVED***> invalidateAffectingFileWatcher(path2, packageJsonMap));
    packageJsonMap ***REMOVED******REMOVED*** null ? void 0 : packageJsonMap.delete(resolutionHost.toPath(path));
  }
  function watchFailedLookupLocationOfNonRelativeModuleResolutions() {
    nonRelativeExternalModuleResolutions.forEach(watchFailedLookupLocationOfResolution);
    nonRelativeExternalModuleResolutions.clear();
  }
  function createDirectoryWatcherForPackageDir(dir, dirPath, packageDir, packageDirPath, nonRecursive) {
    Debug.assert(!nonRecursive);
    let isSymlink ***REMOVED*** isSymlinkCache.get(packageDirPath);
    let packageDirWatcher ***REMOVED*** packageDirWatchers.get(packageDirPath);
    if (isSymlink ***REMOVED******REMOVED******REMOVED*** void 0) {
      const realPath2 ***REMOVED*** resolutionHost.realpath(packageDir);
      isSymlink ***REMOVED*** realPath2 !***REMOVED******REMOVED*** packageDir && resolutionHost.toPath(realPath2) !***REMOVED******REMOVED*** packageDirPath;
      isSymlinkCache.set(packageDirPath, isSymlink);
      if (!packageDirWatcher) {
        packageDirWatchers.set(
          packageDirPath,
          packageDirWatcher ***REMOVED*** {
            dirPathToWatcher: /* @__PURE__ */ new Map(),
            isSymlink
          }
        );
      } else if (packageDirWatcher.isSymlink !***REMOVED******REMOVED*** isSymlink) {
        packageDirWatcher.dirPathToWatcher.forEach((watcher) ***REMOVED***> {
          removeDirectoryWatcher(packageDirWatcher.isSymlink ? packageDirPath : dirPath);
          watcher.watcher ***REMOVED*** createDirPathToWatcher();
        });
        packageDirWatcher.isSymlink ***REMOVED*** isSymlink;
      }
    } else {
      Debug.assertIsDefined(packageDirWatcher);
      Debug.assert(isSymlink ***REMOVED******REMOVED******REMOVED*** packageDirWatcher.isSymlink);
    }
    const forDirPath ***REMOVED*** packageDirWatcher.dirPathToWatcher.get(dirPath);
    if (forDirPath) {
      forDirPath.refCount++;
    } else {
      packageDirWatcher.dirPathToWatcher.set(dirPath, {
        watcher: createDirPathToWatcher(),
        refCount: 1
      });
      if (isSymlink) dirPathToSymlinkPackageRefCount.set(dirPath, (dirPathToSymlinkPackageRefCount.get(dirPath) ?? 0) + 1);
    }
    function createDirPathToWatcher() {
      return isSymlink ? createOrAddRefToDirectoryWatchOfFailedLookups(packageDir, packageDirPath, nonRecursive) : createOrAddRefToDirectoryWatchOfFailedLookups(dir, dirPath, nonRecursive);
    }
  }
  function setDirectoryWatcher(dir, dirPath, packageDir, packageDirPath, nonRecursive) {
    if (!packageDirPath || !resolutionHost.realpath) {
      createOrAddRefToDirectoryWatchOfFailedLookups(dir, dirPath, nonRecursive);
    } else {
      createDirectoryWatcherForPackageDir(dir, dirPath, packageDir, packageDirPath, nonRecursive);
    }
  }
  function createOrAddRefToDirectoryWatchOfFailedLookups(dir, dirPath, nonRecursive) {
    let dirWatcher ***REMOVED*** directoryWatchesOfFailedLookups.get(dirPath);
    if (dirWatcher) {
      Debug.assert(!!nonRecursive ***REMOVED******REMOVED******REMOVED*** !!dirWatcher.nonRecursive);
      dirWatcher.refCount++;
    } else {
      directoryWatchesOfFailedLookups.set(dirPath, dirWatcher ***REMOVED*** { watcher: createDirectoryWatcher(dir, dirPath, nonRecursive), refCount: 1, nonRecursive });
    }
    return dirWatcher;
  }
  function stopWatchFailedLookupLocation(failedLookupLocation, removeAtRoot) {
    const failedLookupLocationPath ***REMOVED*** resolutionHost.toPath(failedLookupLocation);
    const toWatch ***REMOVED*** getDirectoryToWatchFailedLookupLocation(
      failedLookupLocation,
      failedLookupLocationPath,
      rootDir,
      rootPath,
      rootPathComponents,
      isRootWatchable,
      getCurrentDirectory,
      resolutionHost.preferNonRecursiveWatch
    );
    if (toWatch) {
      const { dirPath, packageDirPath } ***REMOVED*** toWatch;
      if (dirPath ***REMOVED******REMOVED******REMOVED*** rootPath) {
        removeAtRoot ***REMOVED*** true;
      } else if (packageDirPath && resolutionHost.realpath) {
        const packageDirWatcher ***REMOVED*** packageDirWatchers.get(packageDirPath);
        const forDirPath ***REMOVED*** packageDirWatcher.dirPathToWatcher.get(dirPath);
        forDirPath.refCount--;
        if (forDirPath.refCount ***REMOVED******REMOVED******REMOVED*** 0) {
          removeDirectoryWatcher(packageDirWatcher.isSymlink ? packageDirPath : dirPath);
          packageDirWatcher.dirPathToWatcher.delete(dirPath);
          if (packageDirWatcher.isSymlink) {
            const refCount ***REMOVED*** dirPathToSymlinkPackageRefCount.get(dirPath) - 1;
            if (refCount ***REMOVED******REMOVED******REMOVED*** 0) {
              dirPathToSymlinkPackageRefCount.delete(dirPath);
            } else {
              dirPathToSymlinkPackageRefCount.set(dirPath, refCount);
            }
          }
        }
      } else {
        removeDirectoryWatcher(dirPath);
      }
    }
    return removeAtRoot;
  }
  function stopWatchFailedLookupLocationOfResolution(resolution, filePath, getResolutionWithResolvedFileName) {
    Debug.checkDefined(resolution.files).delete(filePath);
    if (resolution.files.size) return;
    resolution.files ***REMOVED*** void 0;
    const resolved ***REMOVED*** getResolutionWithResolvedFileName(resolution);
    if (resolved && resolved.resolvedFileName) {
      const key ***REMOVED*** resolutionHost.toPath(resolved.resolvedFileName);
      const resolutions ***REMOVED*** resolvedFileToResolution.get(key);
      if ((resolutions ***REMOVED******REMOVED*** null ? void 0 : resolutions.delete(resolution)) && !resolutions.size) resolvedFileToResolution.delete(key);
    }
    const { failedLookupLocations, affectingLocations, alternateResult } ***REMOVED*** resolution;
    if (resolutionsWithFailedLookups.delete(resolution)) {
      let removeAtRoot ***REMOVED*** false;
      if (failedLookupLocations) {
        for (const failedLookupLocation of failedLookupLocations) {
          removeAtRoot ***REMOVED*** stopWatchFailedLookupLocation(failedLookupLocation, removeAtRoot);
        }
      }
      if (alternateResult) removeAtRoot ***REMOVED*** stopWatchFailedLookupLocation(alternateResult, removeAtRoot);
      if (removeAtRoot) removeDirectoryWatcher(rootPath);
    } else if (affectingLocations ***REMOVED******REMOVED*** null ? void 0 : affectingLocations.length) {
      resolutionsWithOnlyAffectingLocations.delete(resolution);
    }
    if (affectingLocations) {
      for (const affectingLocation of affectingLocations) {
        const watcher ***REMOVED*** fileWatchesOfAffectingLocations.get(affectingLocation);
        watcher.resolutions--;
      }
    }
  }
  function removeDirectoryWatcher(dirPath) {
    const dirWatcher ***REMOVED*** directoryWatchesOfFailedLookups.get(dirPath);
    dirWatcher.refCount--;
  }
  function createDirectoryWatcher(directory, dirPath, nonRecursive) {
    return resolutionHost.watchDirectoryOfFailedLookupLocation(directory, (fileOrDirectory) ***REMOVED***> {
      const fileOrDirectoryPath ***REMOVED*** resolutionHost.toPath(fileOrDirectory);
      if (cachedDirectoryStructureHost) {
        cachedDirectoryStructureHost.addOrDeleteFileOrDirectory(fileOrDirectory, fileOrDirectoryPath);
      }
      scheduleInvalidateResolutionOfFailedLookupLocation(fileOrDirectoryPath, dirPath ***REMOVED******REMOVED******REMOVED*** fileOrDirectoryPath);
    }, nonRecursive ? 0 /* None */ : 1 /* Recursive */);
  }
  function removeResolutionsOfFileFromCache(cache, filePath, getResolutionWithResolvedFileName) {
    const resolutions ***REMOVED*** cache.get(filePath);
    if (resolutions) {
      resolutions.forEach(
        (resolution) ***REMOVED***> stopWatchFailedLookupLocationOfResolution(
          resolution,
          filePath,
          getResolutionWithResolvedFileName
        )
      );
      cache.delete(filePath);
    }
  }
  function removeResolutionsFromProjectReferenceRedirects(filePath) {
    if (!fileExtensionIs(filePath, ".json" /* Json */)) return;
    const program ***REMOVED*** resolutionHost.getCurrentProgram();
    if (!program) return;
    const resolvedProjectReference ***REMOVED*** program.getResolvedProjectReferenceByPath(filePath);
    if (!resolvedProjectReference) return;
    resolvedProjectReference.commandLine.fileNames.forEach((f) ***REMOVED***> removeResolutionsOfFile(resolutionHost.toPath(f)));
  }
  function removeResolutionsOfFile(filePath) {
    removeResolutionsOfFileFromCache(resolvedModuleNames, filePath, getResolvedModuleFromResolution);
    removeResolutionsOfFileFromCache(resolvedTypeReferenceDirectives, filePath, getResolvedTypeReferenceDirectiveFromResolution);
  }
  function invalidateResolutions(resolutions, canInvalidate) {
    if (!resolutions) return false;
    let invalidated ***REMOVED*** false;
    resolutions.forEach((resolution) ***REMOVED***> {
      if (resolution.isInvalidated || !canInvalidate(resolution)) return;
      resolution.isInvalidated ***REMOVED*** invalidated ***REMOVED*** true;
      for (const containingFilePath of Debug.checkDefined(resolution.files)) {
        (filesWithInvalidatedResolutions ?? (filesWithInvalidatedResolutions ***REMOVED*** /* @__PURE__ */ new Set())).add(containingFilePath);
        hasChangedAutomaticTypeDirectiveNames ***REMOVED*** hasChangedAutomaticTypeDirectiveNames || endsWith(containingFilePath, inferredTypesContainingFile);
      }
    });
    return invalidated;
  }
  function invalidateResolutionOfFile(filePath) {
    removeResolutionsOfFile(filePath);
    const prevHasChangedAutomaticTypeDirectiveNames ***REMOVED*** hasChangedAutomaticTypeDirectiveNames;
    if (invalidateResolutions(resolvedFileToResolution.get(filePath), returnTrue) && hasChangedAutomaticTypeDirectiveNames && !prevHasChangedAutomaticTypeDirectiveNames) {
      resolutionHost.onChangedAutomaticTypeDirectiveNames();
    }
  }
  function setFilesWithInvalidatedNonRelativeUnresolvedImports(filesMap) {
    Debug.assert(filesWithInvalidatedNonRelativeUnresolvedImports ***REMOVED******REMOVED******REMOVED*** filesMap || filesWithInvalidatedNonRelativeUnresolvedImports ***REMOVED******REMOVED******REMOVED*** void 0);
    filesWithInvalidatedNonRelativeUnresolvedImports ***REMOVED*** filesMap;
  }
  function scheduleInvalidateResolutionOfFailedLookupLocation(fileOrDirectoryPath, isCreatingWatchedDirectory) {
    if (isCreatingWatchedDirectory) {
      (isInDirectoryChecks || (isInDirectoryChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(fileOrDirectoryPath);
    } else {
      const updatedPath ***REMOVED*** removeIgnoredPath(fileOrDirectoryPath);
      if (!updatedPath) return false;
      fileOrDirectoryPath ***REMOVED*** updatedPath;
      if (resolutionHost.fileIsOpen(fileOrDirectoryPath)) {
        return false;
      }
      const dirOfFileOrDirectory ***REMOVED*** getDirectoryPath(fileOrDirectoryPath);
      if (isNodeModulesAtTypesDirectory(fileOrDirectoryPath) || isNodeModulesDirectory(fileOrDirectoryPath) || isNodeModulesAtTypesDirectory(dirOfFileOrDirectory) || isNodeModulesDirectory(dirOfFileOrDirectory)) {
        (failedLookupChecks || (failedLookupChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(fileOrDirectoryPath);
        (startsWithPathChecks || (startsWithPathChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(fileOrDirectoryPath);
      } else {
        if (isEmittedFileOfProgram(resolutionHost.getCurrentProgram(), fileOrDirectoryPath)) {
          return false;
        }
        if (fileExtensionIs(fileOrDirectoryPath, ".map")) {
          return false;
        }
        (failedLookupChecks || (failedLookupChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(fileOrDirectoryPath);
        (startsWithPathChecks || (startsWithPathChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(fileOrDirectoryPath);
        const packagePath ***REMOVED*** parseNodeModuleFromPath(
          fileOrDirectoryPath,
          /*isFolder*/
          true
        );
        if (packagePath) (startsWithPathChecks || (startsWithPathChecks ***REMOVED*** /* @__PURE__ */ new Set())).add(packagePath);
      }
    }
    resolutionHost.scheduleInvalidateResolutionsOfFailedLookupLocations();
  }
  function invalidatePackageJsonMap() {
    const packageJsonMap ***REMOVED*** moduleResolutionCache.getPackageJsonInfoCache().getInternalMap();
    if (packageJsonMap && (failedLookupChecks || startsWithPathChecks || isInDirectoryChecks)) {
      packageJsonMap.forEach((_value, path) ***REMOVED***> isInvalidatedFailedLookup(path) ? packageJsonMap.delete(path) : void 0);
    }
  }
  function invalidateResolutionsOfFailedLookupLocations() {
    var _a;
    if (allModuleAndTypeResolutionsAreInvalidated) {
      affectingPathChecksForFile ***REMOVED*** void 0;
      invalidatePackageJsonMap();
      if (failedLookupChecks || startsWithPathChecks || isInDirectoryChecks || affectingPathChecks) {
        invalidateResolutions(resolvedLibraries, canInvalidateFailedLookupResolution);
      }
      failedLookupChecks ***REMOVED*** void 0;
      startsWithPathChecks ***REMOVED*** void 0;
      isInDirectoryChecks ***REMOVED*** void 0;
      affectingPathChecks ***REMOVED*** void 0;
      return true;
    }
    let invalidated ***REMOVED*** false;
    if (affectingPathChecksForFile) {
      (_a ***REMOVED*** resolutionHost.getCurrentProgram()) ***REMOVED******REMOVED*** null ? void 0 : _a.getSourceFiles().forEach((f) ***REMOVED***> {
        if (some(f.packageJsonLocations, (location) ***REMOVED***> affectingPathChecksForFile.has(location))) {
          (filesWithInvalidatedResolutions ?? (filesWithInvalidatedResolutions ***REMOVED*** /* @__PURE__ */ new Set())).add(f.path);
          invalidated ***REMOVED*** true;
        }
      });
      affectingPathChecksForFile ***REMOVED*** void 0;
    }
    if (!failedLookupChecks && !startsWithPathChecks && !isInDirectoryChecks && !affectingPathChecks) {
      return invalidated;
    }
    invalidated ***REMOVED*** invalidateResolutions(resolutionsWithFailedLookups, canInvalidateFailedLookupResolution) || invalidated;
    invalidatePackageJsonMap();
    failedLookupChecks ***REMOVED*** void 0;
    startsWithPathChecks ***REMOVED*** void 0;
    isInDirectoryChecks ***REMOVED*** void 0;
    invalidated ***REMOVED*** invalidateResolutions(resolutionsWithOnlyAffectingLocations, canInvalidatedFailedLookupResolutionWithAffectingLocation) || invalidated;
    affectingPathChecks ***REMOVED*** void 0;
    return invalidated;
  }
  function canInvalidateFailedLookupResolution(resolution) {
    var _a;
    if (canInvalidatedFailedLookupResolutionWithAffectingLocation(resolution)) return true;
    if (!failedLookupChecks && !startsWithPathChecks && !isInDirectoryChecks) return false;
    return ((_a ***REMOVED*** resolution.failedLookupLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.some((location) ***REMOVED***> isInvalidatedFailedLookup(resolutionHost.toPath(location)))) || !!resolution.alternateResult && isInvalidatedFailedLookup(resolutionHost.toPath(resolution.alternateResult));
  }
  function isInvalidatedFailedLookup(locationPath) {
    return (failedLookupChecks ***REMOVED******REMOVED*** null ? void 0 : failedLookupChecks.has(locationPath)) || firstDefinedIterator((startsWithPathChecks ***REMOVED******REMOVED*** null ? void 0 : startsWithPathChecks.keys()) || [], (fileOrDirectoryPath) ***REMOVED***> startsWith(locationPath, fileOrDirectoryPath) ? true : void 0) || firstDefinedIterator((isInDirectoryChecks ***REMOVED******REMOVED*** null ? void 0 : isInDirectoryChecks.keys()) || [], (dirPath) ***REMOVED***> locationPath.length > dirPath.length && startsWith(locationPath, dirPath) && (isDiskPathRoot(dirPath) || locationPath[dirPath.length] ***REMOVED******REMOVED******REMOVED*** directorySeparator) ? true : void 0);
  }
  function canInvalidatedFailedLookupResolutionWithAffectingLocation(resolution) {
    var _a;
    return !!affectingPathChecks && ((_a ***REMOVED*** resolution.affectingLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.some((location) ***REMOVED***> affectingPathChecks.has(location)));
  }
  function closeTypeRootsWatch() {
    clearMap(typeRootsWatches, closeFileWatcher);
  }
  function createTypeRootsWatch(typeRoot) {
    return canWatchTypeRootPath(typeRoot) ? resolutionHost.watchTypeRootsDirectory(typeRoot, (fileOrDirectory) ***REMOVED***> {
      const fileOrDirectoryPath ***REMOVED*** resolutionHost.toPath(fileOrDirectory);
      if (cachedDirectoryStructureHost) {
        cachedDirectoryStructureHost.addOrDeleteFileOrDirectory(fileOrDirectory, fileOrDirectoryPath);
      }
      hasChangedAutomaticTypeDirectiveNames ***REMOVED*** true;
      resolutionHost.onChangedAutomaticTypeDirectiveNames();
      const dirPath ***REMOVED*** getDirectoryToWatchFailedLookupLocationFromTypeRoot(
        typeRoot,
        resolutionHost.toPath(typeRoot),
        rootPath,
        rootPathComponents,
        isRootWatchable,
        getCurrentDirectory,
        resolutionHost.preferNonRecursiveWatch,
        (dirPath2) ***REMOVED***> directoryWatchesOfFailedLookups.has(dirPath2) || dirPathToSymlinkPackageRefCount.has(dirPath2)
      );
      if (dirPath) {
        scheduleInvalidateResolutionOfFailedLookupLocation(fileOrDirectoryPath, dirPath ***REMOVED******REMOVED******REMOVED*** fileOrDirectoryPath);
      }
    }, 1 /* Recursive */) : noopFileWatcher;
  }
  function updateTypeRootsWatch() {
    const options ***REMOVED*** resolutionHost.getCompilationSettings();
    if (options.types) {
      closeTypeRootsWatch();
      return;
    }
    const typeRoots ***REMOVED*** getEffectiveTypeRoots(options, { getCurrentDirectory });
    if (typeRoots) {
      mutateMap(
        typeRootsWatches,
        new Set(typeRoots),
        {
          createNewValue: createTypeRootsWatch,
          onDeleteValue: closeFileWatcher
        }
      );
    } else {
      closeTypeRootsWatch();
    }
  }
  function canWatchTypeRootPath(typeRoot) {
    if (resolutionHost.getCompilationSettings().typeRoots) return true;
    return canWatchAtTypes(resolutionHost.toPath(typeRoot));
  }
}
function resolutionIsSymlink(resolution) {
  var _a, _b;
  return !!(((_a ***REMOVED*** resolution.resolvedModule) ***REMOVED******REMOVED*** null ? void 0 : _a.originalPath) || ((_b ***REMOVED*** resolution.resolvedTypeReferenceDirective) ***REMOVED******REMOVED*** null ? void 0 : _b.originalPath));
}

// src/compiler/watch.ts
var sysFormatDiagnosticsHost ***REMOVED*** sys ? {
  getCurrentDirectory: () ***REMOVED***> sys.getCurrentDirectory(),
  getNewLine: () ***REMOVED***> sys.newLine,
  getCanonicalFileName: createGetCanonicalFileName(sys.useCaseSensitiveFileNames)
} : void 0;
function createDiagnosticReporter(system, pretty) {
  const host ***REMOVED*** system ***REMOVED******REMOVED******REMOVED*** sys && sysFormatDiagnosticsHost ? sysFormatDiagnosticsHost : {
    getCurrentDirectory: () ***REMOVED***> system.getCurrentDirectory(),
    getNewLine: () ***REMOVED***> system.newLine,
    getCanonicalFileName: createGetCanonicalFileName(system.useCaseSensitiveFileNames)
  };
  if (!pretty) {
    return (diagnostic) ***REMOVED***> system.write(formatDiagnostic(diagnostic, host));
  }
  const diagnostics ***REMOVED*** new Array(1);
  return (diagnostic) ***REMOVED***> {
    diagnostics[0] ***REMOVED*** diagnostic;
    system.write(formatDiagnosticsWithColorAndContext(diagnostics, host) + host.getNewLine());
    diagnostics[0] ***REMOVED*** void 0;
  };
}
function clearScreenIfNotWatchingForFileChanges(system, diagnostic, options) {
  if (system.clearScreen && !options.preserveWatchOutput && !options.extendedDiagnostics && !options.diagnostics && contains(screenStartingMessageCodes, diagnostic.code)) {
    system.clearScreen();
    return true;
  }
  return false;
}
var screenStartingMessageCodes ***REMOVED*** [
  Diagnostics.Starting_compilation_in_watch_mode.code,
  Diagnostics.File_change_detected_Starting_incremental_compilation.code
];
function getPlainDiagnosticFollowingNewLines(diagnostic, newLine) {
  return contains(screenStartingMessageCodes, diagnostic.code) ? newLine + newLine : newLine;
}
function getLocaleTimeString(system) {
  return !system.now ? (/* @__PURE__ */ new Date()).toLocaleTimeString() : (
    // On some systems / builds of Node, there's a non-breaking space between the time and AM/PM.
    // This branch is solely for testing, so just switch it to a normal space for baseline stability.
    // See:
    //     - https://github.com/nodejs/node/issues/45171
    //     - https://github.com/nodejs/node/issues/45753
    system.now().toLocaleTimeString("en-US", { timeZone: "UTC" }).replace("\u202F", " ")
  );
}
function createWatchStatusReporter(system, pretty) {
  return pretty ? (diagnostic, newLine, options) ***REMOVED***> {
    clearScreenIfNotWatchingForFileChanges(system, diagnostic, options);
    let output ***REMOVED*** `[${formatColorAndReset(getLocaleTimeString(system), "\x1B[90m" /* Grey */)}] `;
    output +***REMOVED*** `${flattenDiagnosticMessageText(diagnostic.messageText, system.newLine)}${newLine + newLine}`;
    system.write(output);
  } : (diagnostic, newLine, options) ***REMOVED***> {
    let output ***REMOVED*** "";
    if (!clearScreenIfNotWatchingForFileChanges(system, diagnostic, options)) {
      output +***REMOVED*** newLine;
    }
    output +***REMOVED*** `${getLocaleTimeString(system)} - `;
    output +***REMOVED*** `${flattenDiagnosticMessageText(diagnostic.messageText, system.newLine)}${getPlainDiagnosticFollowingNewLines(diagnostic, newLine)}`;
    system.write(output);
  };
}
function parseConfigFileWithSystem(configFileName, optionsToExtend, extendedConfigCache, watchOptionsToExtend, system, reportDiagnostic) {
  const host ***REMOVED*** system;
  host.onUnRecoverableConfigFileDiagnostic ***REMOVED*** (diagnostic) ***REMOVED***> reportUnrecoverableDiagnostic(system, reportDiagnostic, diagnostic);
  const result ***REMOVED*** getParsedCommandLineOfConfigFile(configFileName, optionsToExtend, host, extendedConfigCache, watchOptionsToExtend);
  host.onUnRecoverableConfigFileDiagnostic ***REMOVED*** void 0;
  return result;
}
function getErrorCountForSummary(diagnostics) {
  return countWhere(diagnostics, (diagnostic) ***REMOVED***> diagnostic.category ***REMOVED******REMOVED******REMOVED*** 1 /* Error */);
}
function getFilesInErrorForSummary(diagnostics) {
  const filesInError ***REMOVED*** filter(diagnostics, (diagnostic) ***REMOVED***> diagnostic.category ***REMOVED******REMOVED******REMOVED*** 1 /* Error */).map(
    (errorDiagnostic) ***REMOVED***> {
      if (errorDiagnostic.file ***REMOVED******REMOVED******REMOVED*** void 0) return;
      return `${errorDiagnostic.file.fileName}`;
    }
  );
  return filesInError.map((fileName) ***REMOVED***> {
    if (fileName ***REMOVED******REMOVED******REMOVED*** void 0) {
      return void 0;
    }
    const diagnosticForFileName ***REMOVED*** find(diagnostics, (diagnostic) ***REMOVED***> diagnostic.file !***REMOVED******REMOVED*** void 0 && diagnostic.file.fileName ***REMOVED******REMOVED******REMOVED*** fileName);
    if (diagnosticForFileName !***REMOVED******REMOVED*** void 0) {
      const { line } ***REMOVED*** getLineAndCharacterOfPosition(diagnosticForFileName.file, diagnosticForFileName.start);
      return {
        fileName,
        line: line + 1
      };
    }
  });
}
function getWatchErrorSummaryDiagnosticMessage(errorCount) {
  return errorCount ***REMOVED******REMOVED******REMOVED*** 1 ? Diagnostics.Found_1_error_Watching_for_file_changes : Diagnostics.Found_0_errors_Watching_for_file_changes;
}
function prettyPathForFileError(error, cwd) {
  const line ***REMOVED*** formatColorAndReset(":" + error.line, "\x1B[90m" /* Grey */);
  if (pathIsAbsolute(error.fileName) && pathIsAbsolute(cwd)) {
    return getRelativePathFromDirectory(
      cwd,
      error.fileName,
      /*ignoreCase*/
      false
    ) + line;
  }
  return error.fileName + line;
}
function getErrorSummaryText(errorCount, filesInError, newLine, host) {
  if (errorCount ***REMOVED******REMOVED******REMOVED*** 0) return "";
  const nonNilFiles ***REMOVED*** filesInError.filter((fileInError) ***REMOVED***> fileInError !***REMOVED******REMOVED*** void 0);
  const distinctFileNamesWithLines ***REMOVED*** nonNilFiles.map((fileInError) ***REMOVED***> `${fileInError.fileName}:${fileInError.line}`).filter((value, index, self) ***REMOVED***> self.indexOf(value) ***REMOVED******REMOVED******REMOVED*** index);
  const firstFileReference ***REMOVED*** nonNilFiles[0] && prettyPathForFileError(nonNilFiles[0], host.getCurrentDirectory());
  let messageAndArgs;
  if (errorCount ***REMOVED******REMOVED******REMOVED*** 1) {
    messageAndArgs ***REMOVED*** filesInError[0] !***REMOVED******REMOVED*** void 0 ? [Diagnostics.Found_1_error_in_0, firstFileReference] : [Diagnostics.Found_1_error];
  } else {
    messageAndArgs ***REMOVED*** distinctFileNamesWithLines.length ***REMOVED******REMOVED******REMOVED*** 0 ? [Diagnostics.Found_0_errors, errorCount] : distinctFileNamesWithLines.length ***REMOVED******REMOVED******REMOVED*** 1 ? [Diagnostics.Found_0_errors_in_the_same_file_starting_at_Colon_1, errorCount, firstFileReference] : [Diagnostics.Found_0_errors_in_1_files, errorCount, distinctFileNamesWithLines.length];
  }
  const d ***REMOVED*** createCompilerDiagnostic(...messageAndArgs);
  const suffix ***REMOVED*** distinctFileNamesWithLines.length > 1 ? createTabularErrorsDisplay(nonNilFiles, host) : "";
  return `${newLine}${flattenDiagnosticMessageText(d.messageText, newLine)}${newLine}${newLine}${suffix}`;
}
function createTabularErrorsDisplay(filesInError, host) {
  const distinctFiles ***REMOVED*** filesInError.filter((value, index, self) ***REMOVED***> index ***REMOVED******REMOVED******REMOVED*** self.findIndex((file) ***REMOVED***> (file ***REMOVED******REMOVED*** null ? void 0 : file.fileName) ***REMOVED******REMOVED******REMOVED*** (value ***REMOVED******REMOVED*** null ? void 0 : value.fileName)));
  if (distinctFiles.length ***REMOVED******REMOVED******REMOVED*** 0) return "";
  const numberLength ***REMOVED*** (num) ***REMOVED***> Math.log(num) * Math.LOG10E + 1;
  const fileToErrorCount ***REMOVED*** distinctFiles.map((file) ***REMOVED***> [file, countWhere(filesInError, (fileInError) ***REMOVED***> fileInError.fileName ***REMOVED******REMOVED******REMOVED*** file.fileName)]);
  const maxErrors ***REMOVED*** maxBy(fileToErrorCount, 0, (value) ***REMOVED***> value[1]);
  const headerRow ***REMOVED*** Diagnostics.Errors_Files.message;
  const leftColumnHeadingLength ***REMOVED*** headerRow.split(" ")[0].length;
  const leftPaddingGoal ***REMOVED*** Math.max(leftColumnHeadingLength, numberLength(maxErrors));
  const headerPadding ***REMOVED*** Math.max(numberLength(maxErrors) - leftColumnHeadingLength, 0);
  let tabularData ***REMOVED*** "";
  tabularData +***REMOVED*** " ".repeat(headerPadding) + headerRow + "\n";
  fileToErrorCount.forEach((row) ***REMOVED***> {
    const [file, errorCount] ***REMOVED*** row;
    const errorCountDigitsLength ***REMOVED*** Math.log(errorCount) * Math.LOG10E + 1 | 0;
    const leftPadding ***REMOVED*** errorCountDigitsLength < leftPaddingGoal ? " ".repeat(leftPaddingGoal - errorCountDigitsLength) : "";
    const fileRef ***REMOVED*** prettyPathForFileError(file, host.getCurrentDirectory());
    tabularData +***REMOVED*** `${leftPadding}${errorCount}  ${fileRef}
`;
  });
  return tabularData;
}
function isBuilderProgram(program) {
  return !!program.state;
}
function listFiles(program, write) {
  const options ***REMOVED*** program.getCompilerOptions();
  if (options.explainFiles) {
    explainFiles(isBuilderProgram(program) ? program.getProgram() : program, write);
  } else if (options.listFiles || options.listFilesOnly) {
    forEach(program.getSourceFiles(), (file) ***REMOVED***> {
      write(file.fileName);
    });
  }
}
function explainFiles(program, write) {
  var _a, _b;
  const reasons ***REMOVED*** program.getFileIncludeReasons();
  const relativeFileName ***REMOVED*** (fileName) ***REMOVED***> convertToRelativePath(fileName, program.getCurrentDirectory(), program.getCanonicalFileName);
  for (const file of program.getSourceFiles()) {
    write(`${toFileName(file, relativeFileName)}`);
    (_a ***REMOVED*** reasons.get(file.path)) ***REMOVED******REMOVED*** null ? void 0 : _a.forEach((reason) ***REMOVED***> write(`  ${fileIncludeReasonToDiagnostics(program, reason, relativeFileName).messageText}`));
    (_b ***REMOVED*** explainIfFileIsRedirectAndImpliedFormat(file, program.getCompilerOptionsForFile(file), relativeFileName)) ***REMOVED******REMOVED*** null ? void 0 : _b.forEach((d) ***REMOVED***> write(`  ${d.messageText}`));
  }
}
function explainIfFileIsRedirectAndImpliedFormat(file, options, fileNameConvertor) {
  var _a;
  let result;
  if (file.path !***REMOVED******REMOVED*** file.resolvedPath) {
    (result ?? (result ***REMOVED*** [])).push(chainDiagnosticMessages(
      /*details*/
      void 0,
      Diagnostics.File_is_output_of_project_reference_source_0,
      toFileName(file.originalFileName, fileNameConvertor)
    ));
  }
  if (file.redirectInfo) {
    (result ?? (result ***REMOVED*** [])).push(chainDiagnosticMessages(
      /*details*/
      void 0,
      Diagnostics.File_redirects_to_file_0,
      toFileName(file.redirectInfo.redirectTarget, fileNameConvertor)
    ));
  }
  if (isExternalOrCommonJsModule(file)) {
    switch (getImpliedNodeFormatForEmitWorker(file, options)) {
      case 99 /* ESNext */:
        if (file.packageJsonScope) {
          (result ?? (result ***REMOVED*** [])).push(chainDiagnosticMessages(
            /*details*/
            void 0,
            Diagnostics.File_is_ECMAScript_module_because_0_has_field_type_with_value_module,
            toFileName(last(file.packageJsonLocations), fileNameConvertor)
          ));
        }
        break;
      case 1 /* CommonJS */:
        if (file.packageJsonScope) {
          (result ?? (result ***REMOVED*** [])).push(chainDiagnosticMessages(
            /*details*/
            void 0,
            file.packageJsonScope.contents.packageJsonContent.type ? Diagnostics.File_is_CommonJS_module_because_0_has_field_type_whose_value_is_not_module : Diagnostics.File_is_CommonJS_module_because_0_does_not_have_field_type,
            toFileName(last(file.packageJsonLocations), fileNameConvertor)
          ));
        } else if ((_a ***REMOVED*** file.packageJsonLocations) ***REMOVED******REMOVED*** null ? void 0 : _a.length) {
          (result ?? (result ***REMOVED*** [])).push(chainDiagnosticMessages(
            /*details*/
            void 0,
            Diagnostics.File_is_CommonJS_module_because_package_json_was_not_found
          ));
        }
        break;
    }
  }
  return result;
}
function getMatchedFileSpec(program, fileName) {
  var _a;
  const configFile ***REMOVED*** program.getCompilerOptions().configFile;
  if (!((_a ***REMOVED*** configFile ***REMOVED******REMOVED*** null ? void 0 : configFile.configFileSpecs) ***REMOVED******REMOVED*** null ? void 0 : _a.validatedFilesSpec)) return void 0;
  const filePath ***REMOVED*** program.getCanonicalFileName(fileName);
  const basePath ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(configFile.fileName, program.getCurrentDirectory()));
  const index ***REMOVED*** findIndex(configFile.configFileSpecs.validatedFilesSpec, (fileSpec) ***REMOVED***> program.getCanonicalFileName(getNormalizedAbsolutePath(fileSpec, basePath)) ***REMOVED******REMOVED******REMOVED*** filePath);
  return index !***REMOVED******REMOVED*** -1 ? configFile.configFileSpecs.validatedFilesSpecBeforeSubstitution[index] : void 0;
}
function getMatchedIncludeSpec(program, fileName) {
  var _a, _b;
  const configFile ***REMOVED*** program.getCompilerOptions().configFile;
  if (!((_a ***REMOVED*** configFile ***REMOVED******REMOVED*** null ? void 0 : configFile.configFileSpecs) ***REMOVED******REMOVED*** null ? void 0 : _a.validatedIncludeSpecs)) return void 0;
  if (configFile.configFileSpecs.isDefaultIncludeSpec) return true;
  const isJsonFile ***REMOVED*** fileExtensionIs(fileName, ".json" /* Json */);
  const basePath ***REMOVED*** getDirectoryPath(getNormalizedAbsolutePath(configFile.fileName, program.getCurrentDirectory()));
  const useCaseSensitiveFileNames2 ***REMOVED*** program.useCaseSensitiveFileNames();
  const index ***REMOVED*** findIndex((_b ***REMOVED*** configFile ***REMOVED******REMOVED*** null ? void 0 : configFile.configFileSpecs) ***REMOVED******REMOVED*** null ? void 0 : _b.validatedIncludeSpecs, (includeSpec) ***REMOVED***> {
    if (isJsonFile && !endsWith(includeSpec, ".json" /* Json */)) return false;
    const pattern ***REMOVED*** getPatternFromSpec(includeSpec, basePath, "files");
    return !!pattern && getRegexFromPattern(`(?:${pattern})$`, useCaseSensitiveFileNames2).test(fileName);
  });
  return index !***REMOVED******REMOVED*** -1 ? configFile.configFileSpecs.validatedIncludeSpecsBeforeSubstitution[index] : void 0;
}
function fileIncludeReasonToDiagnostics(program, reason, fileNameConvertor) {
  var _a, _b;
  const options ***REMOVED*** program.getCompilerOptions();
  if (isReferencedFile(reason)) {
    const referenceLocation ***REMOVED*** getReferencedFileLocation(program, reason);
    const referenceText ***REMOVED*** isReferenceFileLocation(referenceLocation) ? referenceLocation.file.text.substring(referenceLocation.pos, referenceLocation.end) : `"${referenceLocation.text}"`;
    let message;
    Debug.assert(isReferenceFileLocation(referenceLocation) || reason.kind ***REMOVED******REMOVED******REMOVED*** 3 /* Import */, "Only synthetic references are imports");
    switch (reason.kind) {
      case 3 /* Import */:
        if (isReferenceFileLocation(referenceLocation)) {
          message ***REMOVED*** referenceLocation.packageId ? Diagnostics.Imported_via_0_from_file_1_with_packageId_2 : Diagnostics.Imported_via_0_from_file_1;
        } else if (referenceLocation.text ***REMOVED******REMOVED******REMOVED*** externalHelpersModuleNameText) {
          message ***REMOVED*** referenceLocation.packageId ? Diagnostics.Imported_via_0_from_file_1_with_packageId_2_to_import_importHelpers_as_specified_in_compilerOptions : Diagnostics.Imported_via_0_from_file_1_to_import_importHelpers_as_specified_in_compilerOptions;
        } else {
          message ***REMOVED*** referenceLocation.packageId ? Diagnostics.Imported_via_0_from_file_1_with_packageId_2_to_import_jsx_and_jsxs_factory_functions : Diagnostics.Imported_via_0_from_file_1_to_import_jsx_and_jsxs_factory_functions;
        }
        break;
      case 4 /* ReferenceFile */:
        Debug.assert(!referenceLocation.packageId);
        message ***REMOVED*** Diagnostics.Referenced_via_0_from_file_1;
        break;
      case 5 /* TypeReferenceDirective */:
        message ***REMOVED*** referenceLocation.packageId ? Diagnostics.Type_library_referenced_via_0_from_file_1_with_packageId_2 : Diagnostics.Type_library_referenced_via_0_from_file_1;
        break;
      case 7 /* LibReferenceDirective */:
        Debug.assert(!referenceLocation.packageId);
        message ***REMOVED*** Diagnostics.Library_referenced_via_0_from_file_1;
        break;
      default:
        Debug.assertNever(reason);
    }
    return chainDiagnosticMessages(
      /*details*/
      void 0,
      message,
      referenceText,
      toFileName(referenceLocation.file, fileNameConvertor),
      referenceLocation.packageId && packageIdToString(referenceLocation.packageId)
    );
  }
  switch (reason.kind) {
    case 0 /* RootFile */:
      if (!((_a ***REMOVED*** options.configFile) ***REMOVED******REMOVED*** null ? void 0 : _a.configFileSpecs)) return chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.Root_file_specified_for_compilation
      );
      const fileName ***REMOVED*** getNormalizedAbsolutePath(program.getRootFileNames()[reason.index], program.getCurrentDirectory());
      const matchedByFiles ***REMOVED*** getMatchedFileSpec(program, fileName);
      if (matchedByFiles) return chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.Part_of_files_list_in_tsconfig_json
      );
      const matchedByInclude ***REMOVED*** getMatchedIncludeSpec(program, fileName);
      return isString(matchedByInclude) ? chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.Matched_by_include_pattern_0_in_1,
        matchedByInclude,
        toFileName(options.configFile, fileNameConvertor)
      ) : (
        // Could be additional files specified as roots or matched by default include
        chainDiagnosticMessages(
          /*details*/
          void 0,
          matchedByInclude ? Diagnostics.Matched_by_default_include_pattern_Asterisk_Asterisk_Slash_Asterisk : Diagnostics.Root_file_specified_for_compilation
        )
      );
    case 1 /* SourceFromProjectReference */:
    case 2 /* OutputFromProjectReference */:
      const isOutput ***REMOVED*** reason.kind ***REMOVED******REMOVED******REMOVED*** 2 /* OutputFromProjectReference */;
      const referencedResolvedRef ***REMOVED*** Debug.checkDefined((_b ***REMOVED*** program.getResolvedProjectReferences()) ***REMOVED******REMOVED*** null ? void 0 : _b[reason.index]);
      return chainDiagnosticMessages(
        /*details*/
        void 0,
        options.outFile ? isOutput ? Diagnostics.Output_from_referenced_project_0_included_because_1_specified : Diagnostics.Source_from_referenced_project_0_included_because_1_specified : isOutput ? Diagnostics.Output_from_referenced_project_0_included_because_module_is_specified_as_none : Diagnostics.Source_from_referenced_project_0_included_because_module_is_specified_as_none,
        toFileName(referencedResolvedRef.sourceFile.fileName, fileNameConvertor),
        options.outFile ? "--outFile" : "--out"
      );
    case 8 /* AutomaticTypeDirectiveFile */: {
      const messageAndArgs ***REMOVED*** options.types ? reason.packageId ? [Diagnostics.Entry_point_of_type_library_0_specified_in_compilerOptions_with_packageId_1, reason.typeReference, packageIdToString(reason.packageId)] : [Diagnostics.Entry_point_of_type_library_0_specified_in_compilerOptions, reason.typeReference] : reason.packageId ? [Diagnostics.Entry_point_for_implicit_type_library_0_with_packageId_1, reason.typeReference, packageIdToString(reason.packageId)] : [Diagnostics.Entry_point_for_implicit_type_library_0, reason.typeReference];
      return chainDiagnosticMessages(
        /*details*/
        void 0,
        ...messageAndArgs
      );
    }
    case 6 /* LibFile */: {
      if (reason.index !***REMOVED******REMOVED*** void 0) return chainDiagnosticMessages(
        /*details*/
        void 0,
        Diagnostics.Library_0_specified_in_compilerOptions,
        options.lib[reason.index]
      );
      const target ***REMOVED*** getNameOfScriptTarget(getEmitScriptTarget(options));
      const messageAndArgs ***REMOVED*** target ? [Diagnostics.Default_library_for_target_0, target] : [Diagnostics.Default_library];
      return chainDiagnosticMessages(
        /*details*/
        void 0,
        ...messageAndArgs
      );
    }
    default:
      Debug.assertNever(reason);
  }
}
function toFileName(file, fileNameConvertor) {
  const fileName ***REMOVED*** isString(file) ? file : file.fileName;
  return fileNameConvertor ? fileNameConvertor(fileName) : fileName;
}
function emitFilesAndReportErrors(program, reportDiagnostic, write, reportSummary, writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers) {
  const options ***REMOVED*** program.getCompilerOptions();
  const allDiagnostics ***REMOVED*** program.getConfigFileParsingDiagnostics().slice();
  const configFileParsingDiagnosticsLength ***REMOVED*** allDiagnostics.length;
  addRange(allDiagnostics, program.getSyntacticDiagnostics(
    /*sourceFile*/
    void 0,
    cancellationToken
  ));
  if (allDiagnostics.length ***REMOVED******REMOVED******REMOVED*** configFileParsingDiagnosticsLength) {
    addRange(allDiagnostics, program.getOptionsDiagnostics(cancellationToken));
    if (!options.listFilesOnly) {
      addRange(allDiagnostics, program.getGlobalDiagnostics(cancellationToken));
      if (allDiagnostics.length ***REMOVED******REMOVED******REMOVED*** configFileParsingDiagnosticsLength) {
        addRange(allDiagnostics, program.getSemanticDiagnostics(
          /*sourceFile*/
          void 0,
          cancellationToken
        ));
      }
      if (options.noEmit && getEmitDeclarations(options) && allDiagnostics.length ***REMOVED******REMOVED******REMOVED*** configFileParsingDiagnosticsLength) {
        addRange(allDiagnostics, program.getDeclarationDiagnostics(
          /*sourceFile*/
          void 0,
          cancellationToken
        ));
      }
    }
  }
  const emitResult ***REMOVED*** options.listFilesOnly ? { emitSkipped: true, diagnostics: emptyArray } : program.emit(
    /*targetSourceFile*/
    void 0,
    writeFile2,
    cancellationToken,
    emitOnlyDtsFiles,
    customTransformers
  );
  addRange(allDiagnostics, emitResult.diagnostics);
  const diagnostics ***REMOVED*** sortAndDeduplicateDiagnostics(allDiagnostics);
  diagnostics.forEach(reportDiagnostic);
  if (write) {
    const currentDir ***REMOVED*** program.getCurrentDirectory();
    forEach(emitResult.emittedFiles, (file) ***REMOVED***> {
      const filepath ***REMOVED*** getNormalizedAbsolutePath(file, currentDir);
      write(`TSFILE: ${filepath}`);
    });
    listFiles(program, write);
  }
  if (reportSummary) {
    reportSummary(getErrorCountForSummary(diagnostics), getFilesInErrorForSummary(diagnostics));
  }
  return {
    emitResult,
    diagnostics
  };
}
function emitFilesAndReportErrorsAndGetExitStatus(program, reportDiagnostic, write, reportSummary, writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers) {
  const { emitResult, diagnostics } ***REMOVED*** emitFilesAndReportErrors(
    program,
    reportDiagnostic,
    write,
    reportSummary,
    writeFile2,
    cancellationToken,
    emitOnlyDtsFiles,
    customTransformers
  );
  if (emitResult.emitSkipped && diagnostics.length > 0) {
    return 1 /* DiagnosticsPresent_OutputsSkipped */;
  } else if (diagnostics.length > 0) {
    return 2 /* DiagnosticsPresent_OutputsGenerated */;
  }
  return 0 /* Success */;
}
var noopFileWatcher ***REMOVED*** { close: noop };
var returnNoopFileWatcher ***REMOVED*** () ***REMOVED***> noopFileWatcher;
function createWatchHost(system ***REMOVED*** sys, reportWatchStatus2) {
  const onWatchStatusChange ***REMOVED*** reportWatchStatus2 || createWatchStatusReporter(system);
  return {
    onWatchStatusChange,
    watchFile: maybeBind(system, system.watchFile) || returnNoopFileWatcher,
    watchDirectory: maybeBind(system, system.watchDirectory) || returnNoopFileWatcher,
    setTimeout: maybeBind(system, system.setTimeout) || noop,
    clearTimeout: maybeBind(system, system.clearTimeout) || noop,
    preferNonRecursiveWatch: system.preferNonRecursiveWatch
  };
}
var WatchType ***REMOVED*** {
  ConfigFile: "Config file",
  ExtendedConfigFile: "Extended config file",
  SourceFile: "Source file",
  MissingFile: "Missing file",
  WildcardDirectory: "Wild card directory",
  FailedLookupLocations: "Failed Lookup Locations",
  AffectingFileLocation: "File location affecting resolution",
  TypeRoots: "Type roots",
  ConfigFileOfReferencedProject: "Config file of referened project",
  ExtendedConfigOfReferencedProject: "Extended config file of referenced project",
  WildcardDirectoryOfReferencedProject: "Wild card directory of referenced project",
  PackageJson: "package.json file",
  ClosedScriptInfo: "Closed Script info",
  ConfigFileForInferredRoot: "Config file for the inferred project root",
  NodeModules: "node_modules for closed script infos and package.jsons affecting module specifier cache",
  MissingSourceMapFile: "Missing source map file",
  NoopConfigFileForInferredRoot: "Noop Config file for the inferred project root",
  MissingGeneratedFile: "Missing generated file",
  NodeModulesForModuleSpecifierCache: "node_modules for module specifier cache invalidation",
  TypingInstallerLocationFile: "File location for typing installer",
  TypingInstallerLocationDirectory: "Directory location for typing installer"
};
function createWatchFactory(host, options) {
  const watchLogLevel ***REMOVED*** host.trace ? options.extendedDiagnostics ? 2 /* Verbose */ : options.diagnostics ? 1 /* TriggerOnly */ : 0 /* None */ : 0 /* None */;
  const writeLog ***REMOVED*** watchLogLevel !***REMOVED******REMOVED*** 0 /* None */ ? (s) ***REMOVED***> host.trace(s) : noop;
  const result ***REMOVED*** getWatchFactory(host, watchLogLevel, writeLog);
  result.writeLog ***REMOVED*** writeLog;
  return result;
}
function createCompilerHostFromProgramHost(host, getCompilerOptions, directoryStructureHost ***REMOVED*** host) {
  const useCaseSensitiveFileNames2 ***REMOVED*** host.useCaseSensitiveFileNames();
  const compilerHost ***REMOVED*** {
    getSourceFile: createGetSourceFile(
      (fileName, encoding) ***REMOVED***> !encoding ? compilerHost.readFile(fileName) : host.readFile(fileName, encoding),
      /*setParentNodes*/
      void 0
    ),
    getDefaultLibLocation: maybeBind(host, host.getDefaultLibLocation),
    getDefaultLibFileName: (options) ***REMOVED***> host.getDefaultLibFileName(options),
    writeFile: createWriteFileMeasuringIO(
      (path, data, writeByteOrderMark) ***REMOVED***> host.writeFile(path, data, writeByteOrderMark),
      (path) ***REMOVED***> host.createDirectory(path),
      (path) ***REMOVED***> host.directoryExists(path)
    ),
    getCurrentDirectory: memoize(() ***REMOVED***> host.getCurrentDirectory()),
    useCaseSensitiveFileNames: () ***REMOVED***> useCaseSensitiveFileNames2,
    getCanonicalFileName: createGetCanonicalFileName(useCaseSensitiveFileNames2),
    getNewLine: () ***REMOVED***> getNewLineCharacter(getCompilerOptions()),
    fileExists: (f) ***REMOVED***> host.fileExists(f),
    readFile: (f) ***REMOVED***> host.readFile(f),
    trace: maybeBind(host, host.trace),
    directoryExists: maybeBind(directoryStructureHost, directoryStructureHost.directoryExists),
    getDirectories: maybeBind(directoryStructureHost, directoryStructureHost.getDirectories),
    realpath: maybeBind(host, host.realpath),
    getEnvironmentVariable: maybeBind(host, host.getEnvironmentVariable) || (() ***REMOVED***> ""),
    createHash: maybeBind(host, host.createHash),
    readDirectory: maybeBind(host, host.readDirectory),
    storeSignatureInfo: host.storeSignatureInfo,
    jsDocParsingMode: host.jsDocParsingMode
  };
  return compilerHost;
}
function getSourceFileVersionAsHashFromText(host, text) {
  if (text.match(sourceMapCommentRegExpDontCareLineStart)) {
    let lineEnd ***REMOVED*** text.length;
    let lineStart ***REMOVED*** lineEnd;
    for (let pos ***REMOVED*** lineEnd - 1; pos >***REMOVED*** 0; pos--) {
      const ch ***REMOVED*** text.charCodeAt(pos);
      switch (ch) {
        case 10 /* lineFeed */:
          if (pos && text.charCodeAt(pos - 1) ***REMOVED******REMOVED******REMOVED*** 13 /* carriageReturn */) {
            pos--;
          }
        // falls through
        case 13 /* carriageReturn */:
          break;
        default:
          if (ch < 127 /* maxAsciiCharacter */ || !isLineBreak(ch)) {
            lineStart ***REMOVED*** pos;
            continue;
          }
          break;
      }
      const line ***REMOVED*** text.substring(lineStart, lineEnd);
      if (line.match(sourceMapCommentRegExp)) {
        text ***REMOVED*** text.substring(0, lineStart);
        break;
      } else if (!line.match(whitespaceOrMapCommentRegExp)) {
        break;
      }
      lineEnd ***REMOVED*** lineStart;
    }
  }
  return (host.createHash || generateDjb2Hash)(text);
}
function setGetSourceFileAsHashVersioned(compilerHost) {
  const originalGetSourceFile ***REMOVED*** compilerHost.getSourceFile;
  compilerHost.getSourceFile ***REMOVED*** (...args) ***REMOVED***> {
    const result ***REMOVED*** originalGetSourceFile.call(compilerHost, ...args);
    if (result) {
      result.version ***REMOVED*** getSourceFileVersionAsHashFromText(compilerHost, result.text);
    }
    return result;
  };
}
function createProgramHost(system, createProgram2) {
  const getDefaultLibLocation ***REMOVED*** memoize(() ***REMOVED***> getDirectoryPath(normalizePath(system.getExecutingFilePath())));
  return {
    useCaseSensitiveFileNames: () ***REMOVED***> system.useCaseSensitiveFileNames,
    getNewLine: () ***REMOVED***> system.newLine,
    getCurrentDirectory: memoize(() ***REMOVED***> system.getCurrentDirectory()),
    getDefaultLibLocation,
    getDefaultLibFileName: (options) ***REMOVED***> combinePaths(getDefaultLibLocation(), getDefaultLibFileName(options)),
    fileExists: (path) ***REMOVED***> system.fileExists(path),
    readFile: (path, encoding) ***REMOVED***> system.readFile(path, encoding),
    directoryExists: (path) ***REMOVED***> system.directoryExists(path),
    getDirectories: (path) ***REMOVED***> system.getDirectories(path),
    readDirectory: (path, extensions, exclude, include, depth) ***REMOVED***> system.readDirectory(path, extensions, exclude, include, depth),
    realpath: maybeBind(system, system.realpath),
    getEnvironmentVariable: maybeBind(system, system.getEnvironmentVariable),
    trace: (s) ***REMOVED***> system.write(s + system.newLine),
    createDirectory: (path) ***REMOVED***> system.createDirectory(path),
    writeFile: (path, data, writeByteOrderMark) ***REMOVED***> system.writeFile(path, data, writeByteOrderMark),
    createHash: maybeBind(system, system.createHash),
    createProgram: createProgram2 || createEmitAndSemanticDiagnosticsBuilderProgram,
    storeSignatureInfo: system.storeSignatureInfo,
    now: maybeBind(system, system.now)
  };
}
function createWatchCompilerHost(system ***REMOVED*** sys, createProgram2, reportDiagnostic, reportWatchStatus2) {
  const write ***REMOVED*** (s) ***REMOVED***> system.write(s + system.newLine);
  const result ***REMOVED*** createProgramHost(system, createProgram2);
  copyProperties(result, createWatchHost(system, reportWatchStatus2));
  result.afterProgramCreate ***REMOVED*** (builderProgram) ***REMOVED***> {
    const compilerOptions ***REMOVED*** builderProgram.getCompilerOptions();
    const newLine ***REMOVED*** getNewLineCharacter(compilerOptions);
    emitFilesAndReportErrors(
      builderProgram,
      reportDiagnostic,
      write,
      (errorCount) ***REMOVED***> result.onWatchStatusChange(
        createCompilerDiagnostic(getWatchErrorSummaryDiagnosticMessage(errorCount), errorCount),
        newLine,
        compilerOptions,
        errorCount
      )
    );
  };
  return result;
}
function reportUnrecoverableDiagnostic(system, reportDiagnostic, diagnostic) {
  reportDiagnostic(diagnostic);
  system.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
}
function createWatchCompilerHostOfConfigFile({
  configFileName,
  optionsToExtend,
  watchOptionsToExtend,
  extraFileExtensions,
  system,
  createProgram: createProgram2,
  reportDiagnostic,
  reportWatchStatus: reportWatchStatus2
}) {
  const diagnosticReporter ***REMOVED*** reportDiagnostic || createDiagnosticReporter(system);
  const host ***REMOVED*** createWatchCompilerHost(system, createProgram2, diagnosticReporter, reportWatchStatus2);
  host.onUnRecoverableConfigFileDiagnostic ***REMOVED*** (diagnostic) ***REMOVED***> reportUnrecoverableDiagnostic(system, diagnosticReporter, diagnostic);
  host.configFileName ***REMOVED*** configFileName;
  host.optionsToExtend ***REMOVED*** optionsToExtend;
  host.watchOptionsToExtend ***REMOVED*** watchOptionsToExtend;
  host.extraFileExtensions ***REMOVED*** extraFileExtensions;
  return host;
}
function createWatchCompilerHostOfFilesAndCompilerOptions({
  rootFiles,
  options,
  watchOptions,
  projectReferences,
  system,
  createProgram: createProgram2,
  reportDiagnostic,
  reportWatchStatus: reportWatchStatus2
}) {
  const host ***REMOVED*** createWatchCompilerHost(system, createProgram2, reportDiagnostic || createDiagnosticReporter(system), reportWatchStatus2);
  host.rootFiles ***REMOVED*** rootFiles;
  host.options ***REMOVED*** options;
  host.watchOptions ***REMOVED*** watchOptions;
  host.projectReferences ***REMOVED*** projectReferences;
  return host;
}
function performIncrementalCompilation(input) {
  const system ***REMOVED*** input.system || sys;
  const host ***REMOVED*** input.host || (input.host ***REMOVED*** createIncrementalCompilerHost(input.options, system));
  const builderProgram ***REMOVED*** createIncrementalProgram(input);
  const exitStatus ***REMOVED*** emitFilesAndReportErrorsAndGetExitStatus(
    builderProgram,
    input.reportDiagnostic || createDiagnosticReporter(system),
    (s) ***REMOVED***> host.trace && host.trace(s),
    input.reportErrorSummary || input.options.pretty ? (errorCount, filesInError) ***REMOVED***> system.write(getErrorSummaryText(errorCount, filesInError, system.newLine, host)) : void 0
  );
  if (input.afterProgramEmitAndDiagnostics) input.afterProgramEmitAndDiagnostics(builderProgram);
  return exitStatus;
}

// src/compiler/watchPublic.ts
function readBuilderProgram(compilerOptions, host) {
  const buildInfoPath ***REMOVED*** getTsBuildInfoEmitOutputFilePath(compilerOptions);
  if (!buildInfoPath) return void 0;
  let buildInfo;
  if (host.getBuildInfo) {
    buildInfo ***REMOVED*** host.getBuildInfo(buildInfoPath, compilerOptions.configFilePath);
  } else {
    const content ***REMOVED*** host.readFile(buildInfoPath);
    if (!content) return void 0;
    buildInfo ***REMOVED*** getBuildInfo(buildInfoPath, content);
  }
  if (!buildInfo || buildInfo.version !***REMOVED******REMOVED*** version || !isIncrementalBuildInfo(buildInfo)) return void 0;
  return createBuilderProgramUsingIncrementalBuildInfo(buildInfo, buildInfoPath, host);
}
function createIncrementalCompilerHost(options, system ***REMOVED*** sys) {
  const host ***REMOVED*** createCompilerHostWorker(
    options,
    /*setParentNodes*/
    void 0,
    system
  );
  host.createHash ***REMOVED*** maybeBind(system, system.createHash);
  host.storeSignatureInfo ***REMOVED*** system.storeSignatureInfo;
  setGetSourceFileAsHashVersioned(host);
  changeCompilerHostLikeToUseCache(host, (fileName) ***REMOVED***> toPath(fileName, host.getCurrentDirectory(), host.getCanonicalFileName));
  return host;
}
function createIncrementalProgram({
  rootNames,
  options,
  configFileParsingDiagnostics,
  projectReferences,
  host,
  createProgram: createProgram2
}) {
  host ***REMOVED*** host || createIncrementalCompilerHost(options);
  createProgram2 ***REMOVED*** createProgram2 || createEmitAndSemanticDiagnosticsBuilderProgram;
  const oldProgram ***REMOVED*** readBuilderProgram(options, host);
  return createProgram2(rootNames, options, host, oldProgram, configFileParsingDiagnostics, projectReferences);
}
function createWatchProgram(host) {
  let builderProgram;
  let updateLevel;
  let missingFilesMap;
  let watchedWildcardDirectories;
  let staleWatches ***REMOVED*** /* @__PURE__ */ new Map([[void 0, void 0]]);
  let timerToUpdateProgram;
  let timerToInvalidateFailedLookupResolutions;
  let parsedConfigs;
  let sharedExtendedConfigFileWatchers;
  let extendedConfigCache ***REMOVED*** host.extendedConfigCache;
  let reportFileChangeDetectedOnCreateProgram ***REMOVED*** false;
  const sourceFilesCache ***REMOVED*** /* @__PURE__ */ new Map();
  let missingFilePathsRequestedForRelease;
  let hasChangedCompilerOptions ***REMOVED*** false;
  const useCaseSensitiveFileNames2 ***REMOVED*** host.useCaseSensitiveFileNames();
  const currentDirectory ***REMOVED*** host.getCurrentDirectory();
  const { configFileName, optionsToExtend: optionsToExtendForConfigFile ***REMOVED*** {}, watchOptionsToExtend, extraFileExtensions, createProgram: createProgram2 } ***REMOVED*** host;
  let { rootFiles: rootFileNames, options: compilerOptions, watchOptions, projectReferences } ***REMOVED*** host;
  let wildcardDirectories;
  let configFileParsingDiagnostics;
  let canConfigFileJsonReportNoInputFiles ***REMOVED*** false;
  let hasChangedConfigFileParsingErrors ***REMOVED*** false;
  const cachedDirectoryStructureHost ***REMOVED*** configFileName ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : createCachedDirectoryStructureHost(host, currentDirectory, useCaseSensitiveFileNames2);
  const directoryStructureHost ***REMOVED*** cachedDirectoryStructureHost || host;
  const parseConfigFileHost ***REMOVED*** parseConfigHostFromCompilerHostLike(host, directoryStructureHost);
  let newLine ***REMOVED*** updateNewLine();
  if (configFileName && host.configFileParsingResult) {
    setConfigFileParsingResult(host.configFileParsingResult);
    newLine ***REMOVED*** updateNewLine();
  }
  reportWatchDiagnostic(Diagnostics.Starting_compilation_in_watch_mode);
  if (configFileName && !host.configFileParsingResult) {
    newLine ***REMOVED*** getNewLineCharacter(optionsToExtendForConfigFile);
    Debug.assert(!rootFileNames);
    parseConfigFile2();
    newLine ***REMOVED*** updateNewLine();
  }
  Debug.assert(compilerOptions);
  Debug.assert(rootFileNames);
  const { watchFile: watchFile2, watchDirectory, writeLog } ***REMOVED*** createWatchFactory(host, compilerOptions);
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(useCaseSensitiveFileNames2);
  writeLog(`Current directory: ${currentDirectory} CaseSensitiveFileNames: ${useCaseSensitiveFileNames2}`);
  let configFileWatcher;
  if (configFileName) {
    configFileWatcher ***REMOVED*** watchFile2(configFileName, scheduleProgramReload, 2e3 /* High */, watchOptions, WatchType.ConfigFile);
  }
  const compilerHost ***REMOVED*** createCompilerHostFromProgramHost(host, () ***REMOVED***> compilerOptions, directoryStructureHost);
  setGetSourceFileAsHashVersioned(compilerHost);
  const getNewSourceFile ***REMOVED*** compilerHost.getSourceFile;
  compilerHost.getSourceFile ***REMOVED*** (fileName, ...args) ***REMOVED***> getVersionedSourceFileByPath(fileName, toPath3(fileName), ...args);
  compilerHost.getSourceFileByPath ***REMOVED*** getVersionedSourceFileByPath;
  compilerHost.getNewLine ***REMOVED*** () ***REMOVED***> newLine;
  compilerHost.fileExists ***REMOVED*** fileExists;
  compilerHost.onReleaseOldSourceFile ***REMOVED*** onReleaseOldSourceFile;
  compilerHost.onReleaseParsedCommandLine ***REMOVED*** onReleaseParsedCommandLine;
  compilerHost.toPath ***REMOVED*** toPath3;
  compilerHost.getCompilationSettings ***REMOVED*** () ***REMOVED***> compilerOptions;
  compilerHost.useSourceOfProjectReferenceRedirect ***REMOVED*** maybeBind(host, host.useSourceOfProjectReferenceRedirect);
  compilerHost.preferNonRecursiveWatch ***REMOVED*** host.preferNonRecursiveWatch;
  compilerHost.watchDirectoryOfFailedLookupLocation ***REMOVED*** (dir, cb, flags) ***REMOVED***> watchDirectory(dir, cb, flags, watchOptions, WatchType.FailedLookupLocations);
  compilerHost.watchAffectingFileLocation ***REMOVED*** (file, cb) ***REMOVED***> watchFile2(file, cb, 2e3 /* High */, watchOptions, WatchType.AffectingFileLocation);
  compilerHost.watchTypeRootsDirectory ***REMOVED*** (dir, cb, flags) ***REMOVED***> watchDirectory(dir, cb, flags, watchOptions, WatchType.TypeRoots);
  compilerHost.getCachedDirectoryStructureHost ***REMOVED*** () ***REMOVED***> cachedDirectoryStructureHost;
  compilerHost.scheduleInvalidateResolutionsOfFailedLookupLocations ***REMOVED*** scheduleInvalidateResolutionsOfFailedLookupLocations;
  compilerHost.onInvalidatedResolution ***REMOVED*** scheduleProgramUpdate;
  compilerHost.onChangedAutomaticTypeDirectiveNames ***REMOVED*** scheduleProgramUpdate;
  compilerHost.fileIsOpen ***REMOVED*** returnFalse;
  compilerHost.getCurrentProgram ***REMOVED*** getCurrentProgram;
  compilerHost.writeLog ***REMOVED*** writeLog;
  compilerHost.getParsedCommandLine ***REMOVED*** getParsedCommandLine;
  const resolutionCache ***REMOVED*** createResolutionCache(
    compilerHost,
    configFileName ? getDirectoryPath(getNormalizedAbsolutePath(configFileName, currentDirectory)) : currentDirectory,
    /*logChangesWhenResolvingModule*/
    false
  );
  compilerHost.resolveModuleNameLiterals ***REMOVED*** maybeBind(host, host.resolveModuleNameLiterals);
  compilerHost.resolveModuleNames ***REMOVED*** maybeBind(host, host.resolveModuleNames);
  if (!compilerHost.resolveModuleNameLiterals && !compilerHost.resolveModuleNames) {
    compilerHost.resolveModuleNameLiterals ***REMOVED*** resolutionCache.resolveModuleNameLiterals.bind(resolutionCache);
  }
  compilerHost.resolveTypeReferenceDirectiveReferences ***REMOVED*** maybeBind(host, host.resolveTypeReferenceDirectiveReferences);
  compilerHost.resolveTypeReferenceDirectives ***REMOVED*** maybeBind(host, host.resolveTypeReferenceDirectives);
  if (!compilerHost.resolveTypeReferenceDirectiveReferences && !compilerHost.resolveTypeReferenceDirectives) {
    compilerHost.resolveTypeReferenceDirectiveReferences ***REMOVED*** resolutionCache.resolveTypeReferenceDirectiveReferences.bind(resolutionCache);
  }
  compilerHost.resolveLibrary ***REMOVED*** !host.resolveLibrary ? resolutionCache.resolveLibrary.bind(resolutionCache) : host.resolveLibrary.bind(host);
  compilerHost.getModuleResolutionCache ***REMOVED*** host.resolveModuleNameLiterals || host.resolveModuleNames ? maybeBind(host, host.getModuleResolutionCache) : () ***REMOVED***> resolutionCache.getModuleResolutionCache();
  const userProvidedResolution ***REMOVED*** !!host.resolveModuleNameLiterals || !!host.resolveTypeReferenceDirectiveReferences || !!host.resolveModuleNames || !!host.resolveTypeReferenceDirectives;
  const customHasInvalidatedResolutions ***REMOVED*** userProvidedResolution ? maybeBind(host, host.hasInvalidatedResolutions) || returnTrue : returnFalse;
  const customHasInvalidLibResolutions ***REMOVED*** host.resolveLibrary ? maybeBind(host, host.hasInvalidatedLibResolutions) || returnTrue : returnFalse;
  builderProgram ***REMOVED*** readBuilderProgram(compilerOptions, compilerHost);
  synchronizeProgram();
  return configFileName ? { getCurrentProgram: getCurrentBuilderProgram, getProgram: updateProgram, close, getResolutionCache } : { getCurrentProgram: getCurrentBuilderProgram, getProgram: updateProgram, updateRootFileNames, close, getResolutionCache };
  function close() {
    clearInvalidateResolutionsOfFailedLookupLocations();
    resolutionCache.clear();
    clearMap(sourceFilesCache, (value) ***REMOVED***> {
      if (value && value.fileWatcher) {
        value.fileWatcher.close();
        value.fileWatcher ***REMOVED*** void 0;
      }
    });
    if (configFileWatcher) {
      configFileWatcher.close();
      configFileWatcher ***REMOVED*** void 0;
    }
    extendedConfigCache ***REMOVED******REMOVED*** null ? void 0 : extendedConfigCache.clear();
    extendedConfigCache ***REMOVED*** void 0;
    if (sharedExtendedConfigFileWatchers) {
      clearMap(sharedExtendedConfigFileWatchers, closeFileWatcherOf);
      sharedExtendedConfigFileWatchers ***REMOVED*** void 0;
    }
    if (watchedWildcardDirectories) {
      clearMap(watchedWildcardDirectories, closeFileWatcherOf);
      watchedWildcardDirectories ***REMOVED*** void 0;
    }
    if (missingFilesMap) {
      clearMap(missingFilesMap, closeFileWatcher);
      missingFilesMap ***REMOVED*** void 0;
    }
    if (parsedConfigs) {
      clearMap(parsedConfigs, (config) ***REMOVED***> {
        var _a;
        (_a ***REMOVED*** config.watcher) ***REMOVED******REMOVED*** null ? void 0 : _a.close();
        config.watcher ***REMOVED*** void 0;
        if (config.watchedDirectories) clearMap(config.watchedDirectories, closeFileWatcherOf);
        config.watchedDirectories ***REMOVED*** void 0;
      });
      parsedConfigs ***REMOVED*** void 0;
    }
    builderProgram ***REMOVED*** void 0;
  }
  function getResolutionCache() {
    return resolutionCache;
  }
  function getCurrentBuilderProgram() {
    return builderProgram;
  }
  function getCurrentProgram() {
    return builderProgram && builderProgram.getProgramOrUndefined();
  }
  function synchronizeProgram() {
    writeLog(`Synchronizing program`);
    Debug.assert(compilerOptions);
    Debug.assert(rootFileNames);
    clearInvalidateResolutionsOfFailedLookupLocations();
    const program ***REMOVED*** getCurrentBuilderProgram();
    if (hasChangedCompilerOptions) {
      newLine ***REMOVED*** updateNewLine();
      if (program && changesAffectModuleResolution(program.getCompilerOptions(), compilerOptions)) {
        resolutionCache.onChangesAffectModuleResolution();
      }
    }
    const { hasInvalidatedResolutions, hasInvalidatedLibResolutions } ***REMOVED*** resolutionCache.createHasInvalidatedResolutions(customHasInvalidatedResolutions, customHasInvalidLibResolutions);
    const {
      originalReadFile,
      originalFileExists,
      originalDirectoryExists,
      originalCreateDirectory,
      originalWriteFile,
      readFileWithCache
    } ***REMOVED*** changeCompilerHostLikeToUseCache(compilerHost, toPath3);
    if (isProgramUptoDate(getCurrentProgram(), rootFileNames, compilerOptions, (path) ***REMOVED***> getSourceVersion(path, readFileWithCache), (fileName) ***REMOVED***> compilerHost.fileExists(fileName), hasInvalidatedResolutions, hasInvalidatedLibResolutions, hasChangedAutomaticTypeDirectiveNames, getParsedCommandLine, projectReferences)) {
      if (hasChangedConfigFileParsingErrors) {
        if (reportFileChangeDetectedOnCreateProgram) {
          reportWatchDiagnostic(Diagnostics.File_change_detected_Starting_incremental_compilation);
        }
        builderProgram ***REMOVED*** createProgram2(
          /*rootNames*/
          void 0,
          /*options*/
          void 0,
          compilerHost,
          builderProgram,
          configFileParsingDiagnostics,
          projectReferences
        );
        hasChangedConfigFileParsingErrors ***REMOVED*** false;
      }
    } else {
      if (reportFileChangeDetectedOnCreateProgram) {
        reportWatchDiagnostic(Diagnostics.File_change_detected_Starting_incremental_compilation);
      }
      createNewProgram(hasInvalidatedResolutions, hasInvalidatedLibResolutions);
    }
    reportFileChangeDetectedOnCreateProgram ***REMOVED*** false;
    if (host.afterProgramCreate && program !***REMOVED******REMOVED*** builderProgram) {
      host.afterProgramCreate(builderProgram);
    }
    compilerHost.readFile ***REMOVED*** originalReadFile;
    compilerHost.fileExists ***REMOVED*** originalFileExists;
    compilerHost.directoryExists ***REMOVED*** originalDirectoryExists;
    compilerHost.createDirectory ***REMOVED*** originalCreateDirectory;
    compilerHost.writeFile ***REMOVED*** originalWriteFile;
    staleWatches ***REMOVED******REMOVED*** null ? void 0 : staleWatches.forEach((configFile, configPath) ***REMOVED***> {
      if (!configPath) {
        watchConfigFileWildCardDirectories();
        if (configFileName) updateExtendedConfigFilesWatches(toPath3(configFileName), compilerOptions, watchOptions, WatchType.ExtendedConfigFile);
      } else {
        const config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(configPath);
        if (config) watchReferencedProject(configFile, configPath, config);
      }
    });
    staleWatches ***REMOVED*** void 0;
    return builderProgram;
  }
  function createNewProgram(hasInvalidatedResolutions, hasInvalidatedLibResolutions) {
    writeLog("CreatingProgramWith::");
    writeLog(`  roots: ${JSON.stringify(rootFileNames)}`);
    writeLog(`  options: ${JSON.stringify(compilerOptions)}`);
    if (projectReferences) writeLog(`  projectReferences: ${JSON.stringify(projectReferences)}`);
    const needsUpdateInTypeRootWatch ***REMOVED*** hasChangedCompilerOptions || !getCurrentProgram();
    hasChangedCompilerOptions ***REMOVED*** false;
    hasChangedConfigFileParsingErrors ***REMOVED*** false;
    resolutionCache.startCachingPerDirectoryResolution();
    compilerHost.hasInvalidatedResolutions ***REMOVED*** hasInvalidatedResolutions;
    compilerHost.hasInvalidatedLibResolutions ***REMOVED*** hasInvalidatedLibResolutions;
    compilerHost.hasChangedAutomaticTypeDirectiveNames ***REMOVED*** hasChangedAutomaticTypeDirectiveNames;
    const oldProgram ***REMOVED*** getCurrentProgram();
    builderProgram ***REMOVED*** createProgram2(rootFileNames, compilerOptions, compilerHost, builderProgram, configFileParsingDiagnostics, projectReferences);
    resolutionCache.finishCachingPerDirectoryResolution(builderProgram.getProgram(), oldProgram);
    updateMissingFilePathsWatch(
      builderProgram.getProgram(),
      missingFilesMap || (missingFilesMap ***REMOVED*** /* @__PURE__ */ new Map()),
      watchMissingFilePath
    );
    if (needsUpdateInTypeRootWatch) {
      resolutionCache.updateTypeRootsWatch();
    }
    if (missingFilePathsRequestedForRelease) {
      for (const missingFilePath of missingFilePathsRequestedForRelease) {
        if (!missingFilesMap.has(missingFilePath)) {
          sourceFilesCache.delete(missingFilePath);
        }
      }
      missingFilePathsRequestedForRelease ***REMOVED*** void 0;
    }
  }
  function updateRootFileNames(files) {
    Debug.assert(!configFileName, "Cannot update root file names with config file watch mode");
    rootFileNames ***REMOVED*** files;
    scheduleProgramUpdate();
  }
  function updateNewLine() {
    return getNewLineCharacter(compilerOptions || optionsToExtendForConfigFile);
  }
  function toPath3(fileName) {
    return toPath(fileName, currentDirectory, getCanonicalFileName);
  }
  function isFileMissingOnHost(hostSourceFile) {
    return typeof hostSourceFile ***REMOVED******REMOVED******REMOVED*** "boolean";
  }
  function isFilePresenceUnknownOnHost(hostSourceFile) {
    return typeof hostSourceFile.version ***REMOVED******REMOVED******REMOVED*** "boolean";
  }
  function fileExists(fileName) {
    const path ***REMOVED*** toPath3(fileName);
    if (isFileMissingOnHost(sourceFilesCache.get(path))) {
      return false;
    }
    return directoryStructureHost.fileExists(fileName);
  }
  function getVersionedSourceFileByPath(fileName, path, languageVersionOrOptions, onError, shouldCreateNewSourceFile) {
    const hostSourceFile ***REMOVED*** sourceFilesCache.get(path);
    if (isFileMissingOnHost(hostSourceFile)) {
      return void 0;
    }
    const impliedNodeFormat ***REMOVED*** typeof languageVersionOrOptions ***REMOVED******REMOVED******REMOVED*** "object" ? languageVersionOrOptions.impliedNodeFormat : void 0;
    if (hostSourceFile ***REMOVED******REMOVED******REMOVED*** void 0 || shouldCreateNewSourceFile || isFilePresenceUnknownOnHost(hostSourceFile) || hostSourceFile.sourceFile.impliedNodeFormat !***REMOVED******REMOVED*** impliedNodeFormat) {
      const sourceFile ***REMOVED*** getNewSourceFile(fileName, languageVersionOrOptions, onError);
      if (hostSourceFile) {
        if (sourceFile) {
          hostSourceFile.sourceFile ***REMOVED*** sourceFile;
          hostSourceFile.version ***REMOVED*** sourceFile.version;
          if (!hostSourceFile.fileWatcher) {
            hostSourceFile.fileWatcher ***REMOVED*** watchFilePath(path, fileName, onSourceFileChange, 250 /* Low */, watchOptions, WatchType.SourceFile);
          }
        } else {
          if (hostSourceFile.fileWatcher) {
            hostSourceFile.fileWatcher.close();
          }
          sourceFilesCache.set(path, false);
        }
      } else {
        if (sourceFile) {
          const fileWatcher ***REMOVED*** watchFilePath(path, fileName, onSourceFileChange, 250 /* Low */, watchOptions, WatchType.SourceFile);
          sourceFilesCache.set(path, { sourceFile, version: sourceFile.version, fileWatcher });
        } else {
          sourceFilesCache.set(path, false);
        }
      }
      return sourceFile;
    }
    return hostSourceFile.sourceFile;
  }
  function nextSourceFileVersion(path) {
    const hostSourceFile ***REMOVED*** sourceFilesCache.get(path);
    if (hostSourceFile !***REMOVED******REMOVED*** void 0) {
      if (isFileMissingOnHost(hostSourceFile)) {
        sourceFilesCache.set(path, { version: false });
      } else {
        hostSourceFile.version ***REMOVED*** false;
      }
    }
  }
  function getSourceVersion(path, readFileWithCache) {
    const hostSourceFile ***REMOVED*** sourceFilesCache.get(path);
    if (!hostSourceFile) return void 0;
    if (hostSourceFile.version) return hostSourceFile.version;
    const text ***REMOVED*** readFileWithCache(path);
    return text !***REMOVED******REMOVED*** void 0 ? getSourceFileVersionAsHashFromText(compilerHost, text) : void 0;
  }
  function onReleaseOldSourceFile(oldSourceFile, _oldOptions, hasSourceFileByPath) {
    const hostSourceFileInfo ***REMOVED*** sourceFilesCache.get(oldSourceFile.resolvedPath);
    if (hostSourceFileInfo !***REMOVED******REMOVED*** void 0) {
      if (isFileMissingOnHost(hostSourceFileInfo)) {
        (missingFilePathsRequestedForRelease || (missingFilePathsRequestedForRelease ***REMOVED*** [])).push(oldSourceFile.path);
      } else if (hostSourceFileInfo.sourceFile ***REMOVED******REMOVED******REMOVED*** oldSourceFile) {
        if (hostSourceFileInfo.fileWatcher) {
          hostSourceFileInfo.fileWatcher.close();
        }
        sourceFilesCache.delete(oldSourceFile.resolvedPath);
        if (!hasSourceFileByPath) {
          resolutionCache.removeResolutionsOfFile(oldSourceFile.path);
        }
      }
    }
  }
  function reportWatchDiagnostic(message) {
    if (host.onWatchStatusChange) {
      host.onWatchStatusChange(createCompilerDiagnostic(message), newLine, compilerOptions || optionsToExtendForConfigFile);
    }
  }
  function hasChangedAutomaticTypeDirectiveNames() {
    return resolutionCache.hasChangedAutomaticTypeDirectiveNames();
  }
  function clearInvalidateResolutionsOfFailedLookupLocations() {
    if (!timerToInvalidateFailedLookupResolutions) return false;
    host.clearTimeout(timerToInvalidateFailedLookupResolutions);
    timerToInvalidateFailedLookupResolutions ***REMOVED*** void 0;
    return true;
  }
  function scheduleInvalidateResolutionsOfFailedLookupLocations() {
    if (!host.setTimeout || !host.clearTimeout) {
      return resolutionCache.invalidateResolutionsOfFailedLookupLocations();
    }
    const pending ***REMOVED*** clearInvalidateResolutionsOfFailedLookupLocations();
    writeLog(`Scheduling invalidateFailedLookup${pending ? ", Cancelled earlier one" : ""}`);
    timerToInvalidateFailedLookupResolutions ***REMOVED*** host.setTimeout(invalidateResolutionsOfFailedLookup, 250, "timerToInvalidateFailedLookupResolutions");
  }
  function invalidateResolutionsOfFailedLookup() {
    timerToInvalidateFailedLookupResolutions ***REMOVED*** void 0;
    if (resolutionCache.invalidateResolutionsOfFailedLookupLocations()) {
      scheduleProgramUpdate();
    }
  }
  function scheduleProgramUpdate() {
    if (!host.setTimeout || !host.clearTimeout) {
      return;
    }
    if (timerToUpdateProgram) {
      host.clearTimeout(timerToUpdateProgram);
    }
    writeLog("Scheduling update");
    timerToUpdateProgram ***REMOVED*** host.setTimeout(updateProgramWithWatchStatus, 250, "timerToUpdateProgram");
  }
  function scheduleProgramReload() {
    Debug.assert(!!configFileName);
    updateLevel ***REMOVED*** 2 /* Full */;
    scheduleProgramUpdate();
  }
  function updateProgramWithWatchStatus() {
    timerToUpdateProgram ***REMOVED*** void 0;
    reportFileChangeDetectedOnCreateProgram ***REMOVED*** true;
    updateProgram();
  }
  function updateProgram() {
    switch (updateLevel) {
      case 1 /* RootNamesAndUpdate */:
        reloadFileNamesFromConfigFile();
        break;
      case 2 /* Full */:
        reloadConfigFile();
        break;
      default:
        synchronizeProgram();
        break;
    }
    return getCurrentBuilderProgram();
  }
  function reloadFileNamesFromConfigFile() {
    writeLog("Reloading new file names and options");
    Debug.assert(compilerOptions);
    Debug.assert(configFileName);
    updateLevel ***REMOVED*** 0 /* Update */;
    rootFileNames ***REMOVED*** getFileNamesFromConfigSpecs(compilerOptions.configFile.configFileSpecs, getNormalizedAbsolutePath(getDirectoryPath(configFileName), currentDirectory), compilerOptions, parseConfigFileHost, extraFileExtensions);
    if (updateErrorForNoInputFiles(
      rootFileNames,
      getNormalizedAbsolutePath(configFileName, currentDirectory),
      compilerOptions.configFile.configFileSpecs,
      configFileParsingDiagnostics,
      canConfigFileJsonReportNoInputFiles
    )) {
      hasChangedConfigFileParsingErrors ***REMOVED*** true;
    }
    synchronizeProgram();
  }
  function reloadConfigFile() {
    Debug.assert(configFileName);
    writeLog(`Reloading config file: ${configFileName}`);
    updateLevel ***REMOVED*** 0 /* Update */;
    if (cachedDirectoryStructureHost) {
      cachedDirectoryStructureHost.clearCache();
    }
    parseConfigFile2();
    hasChangedCompilerOptions ***REMOVED*** true;
    (staleWatches ?? (staleWatches ***REMOVED*** /* @__PURE__ */ new Map())).set(void 0, void 0);
    synchronizeProgram();
  }
  function parseConfigFile2() {
    Debug.assert(configFileName);
    setConfigFileParsingResult(
      getParsedCommandLineOfConfigFile(
        configFileName,
        optionsToExtendForConfigFile,
        parseConfigFileHost,
        extendedConfigCache || (extendedConfigCache ***REMOVED*** /* @__PURE__ */ new Map()),
        watchOptionsToExtend,
        extraFileExtensions
      )
    );
  }
  function setConfigFileParsingResult(configFileParseResult) {
    rootFileNames ***REMOVED*** configFileParseResult.fileNames;
    compilerOptions ***REMOVED*** configFileParseResult.options;
    watchOptions ***REMOVED*** configFileParseResult.watchOptions;
    projectReferences ***REMOVED*** configFileParseResult.projectReferences;
    wildcardDirectories ***REMOVED*** configFileParseResult.wildcardDirectories;
    configFileParsingDiagnostics ***REMOVED*** getConfigFileParsingDiagnostics(configFileParseResult).slice();
    canConfigFileJsonReportNoInputFiles ***REMOVED*** canJsonReportNoInputFiles(configFileParseResult.raw);
    hasChangedConfigFileParsingErrors ***REMOVED*** true;
  }
  function getParsedCommandLine(configFileName2) {
    const configPath ***REMOVED*** toPath3(configFileName2);
    let config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(configPath);
    if (config) {
      if (!config.updateLevel) return config.parsedCommandLine;
      if (config.parsedCommandLine && config.updateLevel ***REMOVED******REMOVED******REMOVED*** 1 /* RootNamesAndUpdate */ && !host.getParsedCommandLine) {
        writeLog("Reloading new file names and options");
        Debug.assert(compilerOptions);
        const fileNames ***REMOVED*** getFileNamesFromConfigSpecs(
          config.parsedCommandLine.options.configFile.configFileSpecs,
          getNormalizedAbsolutePath(getDirectoryPath(configFileName2), currentDirectory),
          compilerOptions,
          parseConfigFileHost
        );
        config.parsedCommandLine ***REMOVED*** { ...config.parsedCommandLine, fileNames };
        config.updateLevel ***REMOVED*** void 0;
        return config.parsedCommandLine;
      }
    }
    writeLog(`Loading config file: ${configFileName2}`);
    const parsedCommandLine ***REMOVED*** host.getParsedCommandLine ? host.getParsedCommandLine(configFileName2) : getParsedCommandLineFromConfigFileHost(configFileName2);
    if (config) {
      config.parsedCommandLine ***REMOVED*** parsedCommandLine;
      config.updateLevel ***REMOVED*** void 0;
    } else {
      (parsedConfigs || (parsedConfigs ***REMOVED*** /* @__PURE__ */ new Map())).set(configPath, config ***REMOVED*** { parsedCommandLine });
    }
    (staleWatches ?? (staleWatches ***REMOVED*** /* @__PURE__ */ new Map())).set(configPath, configFileName2);
    return parsedCommandLine;
  }
  function getParsedCommandLineFromConfigFileHost(configFileName2) {
    const onUnRecoverableConfigFileDiagnostic ***REMOVED*** parseConfigFileHost.onUnRecoverableConfigFileDiagnostic;
    parseConfigFileHost.onUnRecoverableConfigFileDiagnostic ***REMOVED*** noop;
    const parsedCommandLine ***REMOVED*** getParsedCommandLineOfConfigFile(
      configFileName2,
      /*optionsToExtend*/
      void 0,
      parseConfigFileHost,
      extendedConfigCache || (extendedConfigCache ***REMOVED*** /* @__PURE__ */ new Map()),
      watchOptionsToExtend
    );
    parseConfigFileHost.onUnRecoverableConfigFileDiagnostic ***REMOVED*** onUnRecoverableConfigFileDiagnostic;
    return parsedCommandLine;
  }
  function onReleaseParsedCommandLine(fileName) {
    var _a;
    const path ***REMOVED*** toPath3(fileName);
    const config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(path);
    if (!config) return;
    parsedConfigs.delete(path);
    if (config.watchedDirectories) clearMap(config.watchedDirectories, closeFileWatcherOf);
    (_a ***REMOVED*** config.watcher) ***REMOVED******REMOVED*** null ? void 0 : _a.close();
    clearSharedExtendedConfigFileWatcher(path, sharedExtendedConfigFileWatchers);
  }
  function watchFilePath(path, file, callback, pollingInterval, options, watchType) {
    return watchFile2(file, (fileName, eventKind) ***REMOVED***> callback(fileName, eventKind, path), pollingInterval, options, watchType);
  }
  function onSourceFileChange(fileName, eventKind, path) {
    updateCachedSystemWithFile(fileName, path, eventKind);
    if (eventKind ***REMOVED******REMOVED******REMOVED*** 2 /* Deleted */ && sourceFilesCache.has(path)) {
      resolutionCache.invalidateResolutionOfFile(path);
    }
    nextSourceFileVersion(path);
    scheduleProgramUpdate();
  }
  function updateCachedSystemWithFile(fileName, path, eventKind) {
    if (cachedDirectoryStructureHost) {
      cachedDirectoryStructureHost.addOrDeleteFile(fileName, path, eventKind);
    }
  }
  function watchMissingFilePath(missingFilePath, missingFileName) {
    return (parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.has(missingFilePath)) ? noopFileWatcher : watchFilePath(
      missingFilePath,
      missingFileName,
      onMissingFileChange,
      500 /* Medium */,
      watchOptions,
      WatchType.MissingFile
    );
  }
  function onMissingFileChange(fileName, eventKind, missingFilePath) {
    updateCachedSystemWithFile(fileName, missingFilePath, eventKind);
    if (eventKind ***REMOVED******REMOVED******REMOVED*** 0 /* Created */ && missingFilesMap.has(missingFilePath)) {
      missingFilesMap.get(missingFilePath).close();
      missingFilesMap.delete(missingFilePath);
      nextSourceFileVersion(missingFilePath);
      scheduleProgramUpdate();
    }
  }
  function watchConfigFileWildCardDirectories() {
    updateWatchingWildcardDirectories(
      watchedWildcardDirectories || (watchedWildcardDirectories ***REMOVED*** /* @__PURE__ */ new Map()),
      wildcardDirectories,
      watchWildcardDirectory
    );
  }
  function watchWildcardDirectory(directory, flags) {
    return watchDirectory(
      directory,
      (fileOrDirectory) ***REMOVED***> {
        Debug.assert(configFileName);
        Debug.assert(compilerOptions);
        const fileOrDirectoryPath ***REMOVED*** toPath3(fileOrDirectory);
        if (cachedDirectoryStructureHost) {
          cachedDirectoryStructureHost.addOrDeleteFileOrDirectory(fileOrDirectory, fileOrDirectoryPath);
        }
        nextSourceFileVersion(fileOrDirectoryPath);
        if (isIgnoredFileFromWildCardWatching({
          watchedDirPath: toPath3(directory),
          fileOrDirectory,
          fileOrDirectoryPath,
          configFileName,
          extraFileExtensions,
          options: compilerOptions,
          program: getCurrentBuilderProgram() || rootFileNames,
          currentDirectory,
          useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
          writeLog,
          toPath: toPath3
        })) return;
        if (updateLevel !***REMOVED******REMOVED*** 2 /* Full */) {
          updateLevel ***REMOVED*** 1 /* RootNamesAndUpdate */;
          scheduleProgramUpdate();
        }
      },
      flags,
      watchOptions,
      WatchType.WildcardDirectory
    );
  }
  function updateExtendedConfigFilesWatches(forProjectPath, options, watchOptions2, watchType) {
    updateSharedExtendedConfigFileWatcher(
      forProjectPath,
      options,
      sharedExtendedConfigFileWatchers || (sharedExtendedConfigFileWatchers ***REMOVED*** /* @__PURE__ */ new Map()),
      (extendedConfigFileName, extendedConfigFilePath) ***REMOVED***> watchFile2(
        extendedConfigFileName,
        (_fileName, eventKind) ***REMOVED***> {
          var _a;
          updateCachedSystemWithFile(extendedConfigFileName, extendedConfigFilePath, eventKind);
          if (extendedConfigCache) cleanExtendedConfigCache(extendedConfigCache, extendedConfigFilePath, toPath3);
          const projects ***REMOVED*** (_a ***REMOVED*** sharedExtendedConfigFileWatchers.get(extendedConfigFilePath)) ***REMOVED******REMOVED*** null ? void 0 : _a.projects;
          if (!(projects ***REMOVED******REMOVED*** null ? void 0 : projects.size)) return;
          projects.forEach((projectPath) ***REMOVED***> {
            if (configFileName && toPath3(configFileName) ***REMOVED******REMOVED******REMOVED*** projectPath) {
              updateLevel ***REMOVED*** 2 /* Full */;
            } else {
              const config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(projectPath);
              if (config) config.updateLevel ***REMOVED*** 2 /* Full */;
              resolutionCache.removeResolutionsFromProjectReferenceRedirects(projectPath);
            }
            scheduleProgramUpdate();
          });
        },
        2e3 /* High */,
        watchOptions2,
        watchType
      ),
      toPath3
    );
  }
  function watchReferencedProject(configFileName2, configPath, commandLine) {
    var _a, _b, _c, _d;
    commandLine.watcher || (commandLine.watcher ***REMOVED*** watchFile2(
      configFileName2,
      (_fileName, eventKind) ***REMOVED***> {
        updateCachedSystemWithFile(configFileName2, configPath, eventKind);
        const config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(configPath);
        if (config) config.updateLevel ***REMOVED*** 2 /* Full */;
        resolutionCache.removeResolutionsFromProjectReferenceRedirects(configPath);
        scheduleProgramUpdate();
      },
      2e3 /* High */,
      ((_a ***REMOVED*** commandLine.parsedCommandLine) ***REMOVED******REMOVED*** null ? void 0 : _a.watchOptions) || watchOptions,
      WatchType.ConfigFileOfReferencedProject
    ));
    updateWatchingWildcardDirectories(
      commandLine.watchedDirectories || (commandLine.watchedDirectories ***REMOVED*** /* @__PURE__ */ new Map()),
      (_b ***REMOVED*** commandLine.parsedCommandLine) ***REMOVED******REMOVED*** null ? void 0 : _b.wildcardDirectories,
      (directory, flags) ***REMOVED***> {
        var _a2;
        return watchDirectory(
          directory,
          (fileOrDirectory) ***REMOVED***> {
            const fileOrDirectoryPath ***REMOVED*** toPath3(fileOrDirectory);
            if (cachedDirectoryStructureHost) {
              cachedDirectoryStructureHost.addOrDeleteFileOrDirectory(fileOrDirectory, fileOrDirectoryPath);
            }
            nextSourceFileVersion(fileOrDirectoryPath);
            const config ***REMOVED*** parsedConfigs ***REMOVED******REMOVED*** null ? void 0 : parsedConfigs.get(configPath);
            if (!(config ***REMOVED******REMOVED*** null ? void 0 : config.parsedCommandLine)) return;
            if (isIgnoredFileFromWildCardWatching({
              watchedDirPath: toPath3(directory),
              fileOrDirectory,
              fileOrDirectoryPath,
              configFileName: configFileName2,
              options: config.parsedCommandLine.options,
              program: config.parsedCommandLine.fileNames,
              currentDirectory,
              useCaseSensitiveFileNames: useCaseSensitiveFileNames2,
              writeLog,
              toPath: toPath3
            })) return;
            if (config.updateLevel !***REMOVED******REMOVED*** 2 /* Full */) {
              config.updateLevel ***REMOVED*** 1 /* RootNamesAndUpdate */;
              scheduleProgramUpdate();
            }
          },
          flags,
          ((_a2 ***REMOVED*** commandLine.parsedCommandLine) ***REMOVED******REMOVED*** null ? void 0 : _a2.watchOptions) || watchOptions,
          WatchType.WildcardDirectoryOfReferencedProject
        );
      }
    );
    updateExtendedConfigFilesWatches(
      configPath,
      (_c ***REMOVED*** commandLine.parsedCommandLine) ***REMOVED******REMOVED*** null ? void 0 : _c.options,
      ((_d ***REMOVED*** commandLine.parsedCommandLine) ***REMOVED******REMOVED*** null ? void 0 : _d.watchOptions) || watchOptions,
      WatchType.ExtendedConfigOfReferencedProject
    );
  }
}

// src/compiler/tsbuild.ts
function resolveConfigFileProjectName(project) {
  if (fileExtensionIs(project, ".json" /* Json */)) {
    return project;
  }
  return combinePaths(project, "tsconfig.json");
}

// src/compiler/tsbuildPublic.ts
var minimumDate ***REMOVED*** /* @__PURE__ */ new Date(-864e13);
function getOrCreateValueFromConfigFileMap(configFileMap, resolved, createT) {
  const existingValue ***REMOVED*** configFileMap.get(resolved);
  let newValue;
  if (!existingValue) {
    newValue ***REMOVED*** createT();
    configFileMap.set(resolved, newValue);
  }
  return existingValue || newValue;
}
function getOrCreateValueMapFromConfigFileMap(configFileMap, resolved) {
  return getOrCreateValueFromConfigFileMap(configFileMap, resolved, () ***REMOVED***> /* @__PURE__ */ new Map());
}
function getCurrentTime(host) {
  return host.now ? host.now() : /* @__PURE__ */ new Date();
}
function isCircularBuildOrder(buildOrder) {
  return !!buildOrder && !!buildOrder.buildOrder;
}
function getBuildOrderFromAnyBuildOrder(anyBuildOrder) {
  return isCircularBuildOrder(anyBuildOrder) ? anyBuildOrder.buildOrder : anyBuildOrder;
}
function createBuilderStatusReporter(system, pretty) {
  return (diagnostic) ***REMOVED***> {
    let output ***REMOVED*** pretty ? `[${formatColorAndReset(getLocaleTimeString(system), "\x1B[90m" /* Grey */)}] ` : `${getLocaleTimeString(system)} - `;
    output +***REMOVED*** `${flattenDiagnosticMessageText(diagnostic.messageText, system.newLine)}${system.newLine + system.newLine}`;
    system.write(output);
  };
}
function createSolutionBuilderHostBase(system, createProgram2, reportDiagnostic, reportSolutionBuilderStatus) {
  const host ***REMOVED*** createProgramHost(system, createProgram2);
  host.getModifiedTime ***REMOVED*** system.getModifiedTime ? (path) ***REMOVED***> system.getModifiedTime(path) : returnUndefined;
  host.setModifiedTime ***REMOVED*** system.setModifiedTime ? (path, date) ***REMOVED***> system.setModifiedTime(path, date) : noop;
  host.deleteFile ***REMOVED*** system.deleteFile ? (path) ***REMOVED***> system.deleteFile(path) : noop;
  host.reportDiagnostic ***REMOVED*** reportDiagnostic || createDiagnosticReporter(system);
  host.reportSolutionBuilderStatus ***REMOVED*** reportSolutionBuilderStatus || createBuilderStatusReporter(system);
  host.now ***REMOVED*** maybeBind(system, system.now);
  return host;
}
function createSolutionBuilderHost(system ***REMOVED*** sys, createProgram2, reportDiagnostic, reportSolutionBuilderStatus, reportErrorSummary2) {
  const host ***REMOVED*** createSolutionBuilderHostBase(system, createProgram2, reportDiagnostic, reportSolutionBuilderStatus);
  host.reportErrorSummary ***REMOVED*** reportErrorSummary2;
  return host;
}
function createSolutionBuilderWithWatchHost(system ***REMOVED*** sys, createProgram2, reportDiagnostic, reportSolutionBuilderStatus, reportWatchStatus2) {
  const host ***REMOVED*** createSolutionBuilderHostBase(system, createProgram2, reportDiagnostic, reportSolutionBuilderStatus);
  const watchHost ***REMOVED*** createWatchHost(system, reportWatchStatus2);
  copyProperties(host, watchHost);
  return host;
}
function getCompilerOptionsOfBuildOptions(buildOptions) {
  const result ***REMOVED*** {};
  commonOptionsWithBuild.forEach((option) ***REMOVED***> {
    if (hasProperty(buildOptions, option.name)) result[option.name] ***REMOVED*** buildOptions[option.name];
  });
  result.tscBuild ***REMOVED*** true;
  return result;
}
function createSolutionBuilder(host, rootNames, defaultOptions) {
  return createSolutionBuilderWorker(
    /*watch*/
    false,
    host,
    rootNames,
    defaultOptions
  );
}
function createSolutionBuilderWithWatch(host, rootNames, defaultOptions, baseWatchOptions) {
  return createSolutionBuilderWorker(
    /*watch*/
    true,
    host,
    rootNames,
    defaultOptions,
    baseWatchOptions
  );
}
function createSolutionBuilderState(watch, hostOrHostWithWatch, rootNames, options, baseWatchOptions) {
  const host ***REMOVED*** hostOrHostWithWatch;
  const hostWithWatch ***REMOVED*** hostOrHostWithWatch;
  const baseCompilerOptions ***REMOVED*** getCompilerOptionsOfBuildOptions(options);
  const compilerHost ***REMOVED*** createCompilerHostFromProgramHost(host, () ***REMOVED***> state.projectCompilerOptions);
  setGetSourceFileAsHashVersioned(compilerHost);
  compilerHost.getParsedCommandLine ***REMOVED*** (fileName) ***REMOVED***> parseConfigFile(state, fileName, toResolvedConfigFilePath(state, fileName));
  compilerHost.resolveModuleNameLiterals ***REMOVED*** maybeBind(host, host.resolveModuleNameLiterals);
  compilerHost.resolveTypeReferenceDirectiveReferences ***REMOVED*** maybeBind(host, host.resolveTypeReferenceDirectiveReferences);
  compilerHost.resolveLibrary ***REMOVED*** maybeBind(host, host.resolveLibrary);
  compilerHost.resolveModuleNames ***REMOVED*** maybeBind(host, host.resolveModuleNames);
  compilerHost.resolveTypeReferenceDirectives ***REMOVED*** maybeBind(host, host.resolveTypeReferenceDirectives);
  compilerHost.getModuleResolutionCache ***REMOVED*** maybeBind(host, host.getModuleResolutionCache);
  let moduleResolutionCache, typeReferenceDirectiveResolutionCache;
  if (!compilerHost.resolveModuleNameLiterals && !compilerHost.resolveModuleNames) {
    moduleResolutionCache ***REMOVED*** createModuleResolutionCache(compilerHost.getCurrentDirectory(), compilerHost.getCanonicalFileName);
    compilerHost.resolveModuleNameLiterals ***REMOVED*** (moduleNames, containingFile, redirectedReference, options2, containingSourceFile) ***REMOVED***> loadWithModeAwareCache(
      moduleNames,
      containingFile,
      redirectedReference,
      options2,
      containingSourceFile,
      host,
      moduleResolutionCache,
      createModuleResolutionLoader
    );
    compilerHost.getModuleResolutionCache ***REMOVED*** () ***REMOVED***> moduleResolutionCache;
  }
  if (!compilerHost.resolveTypeReferenceDirectiveReferences && !compilerHost.resolveTypeReferenceDirectives) {
    typeReferenceDirectiveResolutionCache ***REMOVED*** createTypeReferenceDirectiveResolutionCache(
      compilerHost.getCurrentDirectory(),
      compilerHost.getCanonicalFileName,
      /*options*/
      void 0,
      moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.getPackageJsonInfoCache(),
      moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.optionsToRedirectsKey
    );
    compilerHost.resolveTypeReferenceDirectiveReferences ***REMOVED*** (typeDirectiveNames, containingFile, redirectedReference, options2, containingSourceFile) ***REMOVED***> loadWithModeAwareCache(
      typeDirectiveNames,
      containingFile,
      redirectedReference,
      options2,
      containingSourceFile,
      host,
      typeReferenceDirectiveResolutionCache,
      createTypeReferenceResolutionLoader
    );
  }
  let libraryResolutionCache;
  if (!compilerHost.resolveLibrary) {
    libraryResolutionCache ***REMOVED*** createModuleResolutionCache(
      compilerHost.getCurrentDirectory(),
      compilerHost.getCanonicalFileName,
      /*options*/
      void 0,
      moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.getPackageJsonInfoCache()
    );
    compilerHost.resolveLibrary ***REMOVED*** (libraryName, resolveFrom, options2) ***REMOVED***> resolveLibrary(
      libraryName,
      resolveFrom,
      options2,
      host,
      libraryResolutionCache
    );
  }
  compilerHost.getBuildInfo ***REMOVED*** (fileName, configFilePath) ***REMOVED***> getBuildInfo3(
    state,
    fileName,
    toResolvedConfigFilePath(state, configFilePath),
    /*modifiedTime*/
    void 0
  );
  const { watchFile: watchFile2, watchDirectory, writeLog } ***REMOVED*** createWatchFactory(hostWithWatch, options);
  const state ***REMOVED*** {
    host,
    hostWithWatch,
    parseConfigFileHost: parseConfigHostFromCompilerHostLike(host),
    write: maybeBind(host, host.trace),
    // State of solution
    options,
    baseCompilerOptions,
    rootNames,
    baseWatchOptions,
    resolvedConfigFilePaths: /* @__PURE__ */ new Map(),
    configFileCache: /* @__PURE__ */ new Map(),
    projectStatus: /* @__PURE__ */ new Map(),
    extendedConfigCache: /* @__PURE__ */ new Map(),
    buildInfoCache: /* @__PURE__ */ new Map(),
    outputTimeStamps: /* @__PURE__ */ new Map(),
    builderPrograms: /* @__PURE__ */ new Map(),
    diagnostics: /* @__PURE__ */ new Map(),
    projectPendingBuild: /* @__PURE__ */ new Map(),
    projectErrorsReported: /* @__PURE__ */ new Map(),
    compilerHost,
    moduleResolutionCache,
    typeReferenceDirectiveResolutionCache,
    libraryResolutionCache,
    // Mutable state
    buildOrder: void 0,
    readFileWithCache: (f) ***REMOVED***> host.readFile(f),
    projectCompilerOptions: baseCompilerOptions,
    cache: void 0,
    allProjectBuildPending: true,
    needsSummary: true,
    watchAllProjectsPending: watch,
    // Watch state
    watch,
    allWatchedWildcardDirectories: /* @__PURE__ */ new Map(),
    allWatchedInputFiles: /* @__PURE__ */ new Map(),
    allWatchedConfigFiles: /* @__PURE__ */ new Map(),
    allWatchedExtendedConfigFiles: /* @__PURE__ */ new Map(),
    allWatchedPackageJsonFiles: /* @__PURE__ */ new Map(),
    filesWatched: /* @__PURE__ */ new Map(),
    lastCachedPackageJsonLookups: /* @__PURE__ */ new Map(),
    timerToBuildInvalidatedProject: void 0,
    reportFileChangeDetected: false,
    watchFile: watchFile2,
    watchDirectory,
    writeLog
  };
  return state;
}
function toPath2(state, fileName) {
  return toPath(fileName, state.compilerHost.getCurrentDirectory(), state.compilerHost.getCanonicalFileName);
}
function toResolvedConfigFilePath(state, fileName) {
  const { resolvedConfigFilePaths } ***REMOVED*** state;
  const path ***REMOVED*** resolvedConfigFilePaths.get(fileName);
  if (path !***REMOVED******REMOVED*** void 0) return path;
  const resolvedPath ***REMOVED*** toPath2(state, fileName);
  resolvedConfigFilePaths.set(fileName, resolvedPath);
  return resolvedPath;
}
function isParsedCommandLine(entry) {
  return !!entry.options;
}
function getCachedParsedConfigFile(state, configFilePath) {
  const value ***REMOVED*** state.configFileCache.get(configFilePath);
  return value && isParsedCommandLine(value) ? value : void 0;
}
function parseConfigFile(state, configFileName, configFilePath) {
  const { configFileCache } ***REMOVED*** state;
  const value ***REMOVED*** configFileCache.get(configFilePath);
  if (value) {
    return isParsedCommandLine(value) ? value : void 0;
  }
  mark("SolutionBuilder::beforeConfigFileParsing");
  let diagnostic;
  const { parseConfigFileHost, baseCompilerOptions, baseWatchOptions, extendedConfigCache, host } ***REMOVED*** state;
  let parsed;
  if (host.getParsedCommandLine) {
    parsed ***REMOVED*** host.getParsedCommandLine(configFileName);
    if (!parsed) diagnostic ***REMOVED*** createCompilerDiagnostic(Diagnostics.File_0_not_found, configFileName);
  } else {
    parseConfigFileHost.onUnRecoverableConfigFileDiagnostic ***REMOVED*** (d) ***REMOVED***> diagnostic ***REMOVED*** d;
    parsed ***REMOVED*** getParsedCommandLineOfConfigFile(configFileName, baseCompilerOptions, parseConfigFileHost, extendedConfigCache, baseWatchOptions);
    parseConfigFileHost.onUnRecoverableConfigFileDiagnostic ***REMOVED*** noop;
  }
  configFileCache.set(configFilePath, parsed || diagnostic);
  mark("SolutionBuilder::afterConfigFileParsing");
  measure("SolutionBuilder::Config file parsing", "SolutionBuilder::beforeConfigFileParsing", "SolutionBuilder::afterConfigFileParsing");
  return parsed;
}
function resolveProjectName(state, name) {
  return resolveConfigFileProjectName(resolvePath(state.compilerHost.getCurrentDirectory(), name));
}
function createBuildOrder(state, roots) {
  const temporaryMarks ***REMOVED*** /* @__PURE__ */ new Map();
  const permanentMarks ***REMOVED*** /* @__PURE__ */ new Map();
  const circularityReportStack ***REMOVED*** [];
  let buildOrder;
  let circularDiagnostics;
  for (const root of roots) {
    visit(root);
  }
  return circularDiagnostics ? { buildOrder: buildOrder || emptyArray, circularDiagnostics } : buildOrder || emptyArray;
  function visit(configFileName, inCircularContext) {
    const projPath ***REMOVED*** toResolvedConfigFilePath(state, configFileName);
    if (permanentMarks.has(projPath)) return;
    if (temporaryMarks.has(projPath)) {
      if (!inCircularContext) {
        (circularDiagnostics || (circularDiagnostics ***REMOVED*** [])).push(
          createCompilerDiagnostic(
            Diagnostics.Project_references_may_not_form_a_circular_graph_Cycle_detected_Colon_0,
            circularityReportStack.join("\r\n")
          )
        );
      }
      return;
    }
    temporaryMarks.set(projPath, true);
    circularityReportStack.push(configFileName);
    const parsed ***REMOVED*** parseConfigFile(state, configFileName, projPath);
    if (parsed && parsed.projectReferences) {
      for (const ref of parsed.projectReferences) {
        const resolvedRefPath ***REMOVED*** resolveProjectName(state, ref.path);
        visit(resolvedRefPath, inCircularContext || ref.circular);
      }
    }
    circularityReportStack.pop();
    permanentMarks.set(projPath, true);
    (buildOrder || (buildOrder ***REMOVED*** [])).push(configFileName);
  }
}
function getBuildOrder(state) {
  return state.buildOrder || createStateBuildOrder(state);
}
function createStateBuildOrder(state) {
  const buildOrder ***REMOVED*** createBuildOrder(state, state.rootNames.map((f) ***REMOVED***> resolveProjectName(state, f)));
  state.resolvedConfigFilePaths.clear();
  const currentProjects ***REMOVED*** new Set(
    getBuildOrderFromAnyBuildOrder(buildOrder).map(
      (resolved) ***REMOVED***> toResolvedConfigFilePath(state, resolved)
    )
  );
  const noopOnDelete ***REMOVED*** { onDeleteValue: noop };
  mutateMapSkippingNewValues(state.configFileCache, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.projectStatus, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.builderPrograms, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.diagnostics, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.projectPendingBuild, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.projectErrorsReported, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.buildInfoCache, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.outputTimeStamps, currentProjects, noopOnDelete);
  mutateMapSkippingNewValues(state.lastCachedPackageJsonLookups, currentProjects, noopOnDelete);
  if (state.watch) {
    mutateMapSkippingNewValues(
      state.allWatchedConfigFiles,
      currentProjects,
      { onDeleteValue: closeFileWatcher }
    );
    state.allWatchedExtendedConfigFiles.forEach((watcher) ***REMOVED***> {
      watcher.projects.forEach((project) ***REMOVED***> {
        if (!currentProjects.has(project)) {
          watcher.projects.delete(project);
        }
      });
      watcher.close();
    });
    mutateMapSkippingNewValues(
      state.allWatchedWildcardDirectories,
      currentProjects,
      { onDeleteValue: (existingMap) ***REMOVED***> existingMap.forEach(closeFileWatcherOf) }
    );
    mutateMapSkippingNewValues(
      state.allWatchedInputFiles,
      currentProjects,
      { onDeleteValue: (existingMap) ***REMOVED***> existingMap.forEach(closeFileWatcher) }
    );
    mutateMapSkippingNewValues(
      state.allWatchedPackageJsonFiles,
      currentProjects,
      { onDeleteValue: (existingMap) ***REMOVED***> existingMap.forEach(closeFileWatcher) }
    );
  }
  return state.buildOrder ***REMOVED*** buildOrder;
}
function getBuildOrderFor(state, project, onlyReferences) {
  const resolvedProject ***REMOVED*** project && resolveProjectName(state, project);
  const buildOrderFromState ***REMOVED*** getBuildOrder(state);
  if (isCircularBuildOrder(buildOrderFromState)) return buildOrderFromState;
  if (resolvedProject) {
    const projectPath ***REMOVED*** toResolvedConfigFilePath(state, resolvedProject);
    const projectIndex ***REMOVED*** findIndex(
      buildOrderFromState,
      (configFileName) ***REMOVED***> toResolvedConfigFilePath(state, configFileName) ***REMOVED******REMOVED******REMOVED*** projectPath
    );
    if (projectIndex ***REMOVED******REMOVED******REMOVED*** -1) return void 0;
  }
  const buildOrder ***REMOVED*** resolvedProject ? createBuildOrder(state, [resolvedProject]) : buildOrderFromState;
  Debug.assert(!isCircularBuildOrder(buildOrder));
  Debug.assert(!onlyReferences || resolvedProject !***REMOVED******REMOVED*** void 0);
  Debug.assert(!onlyReferences || buildOrder[buildOrder.length - 1] ***REMOVED******REMOVED******REMOVED*** resolvedProject);
  return onlyReferences ? buildOrder.slice(0, buildOrder.length - 1) : buildOrder;
}
function enableCache(state) {
  if (state.cache) {
    disableCache(state);
  }
  const { compilerHost, host } ***REMOVED*** state;
  const originalReadFileWithCache ***REMOVED*** state.readFileWithCache;
  const originalGetSourceFile ***REMOVED*** compilerHost.getSourceFile;
  const {
    originalReadFile,
    originalFileExists,
    originalDirectoryExists,
    originalCreateDirectory,
    originalWriteFile,
    getSourceFileWithCache,
    readFileWithCache
  } ***REMOVED*** changeCompilerHostLikeToUseCache(
    host,
    (fileName) ***REMOVED***> toPath2(state, fileName),
    (...args) ***REMOVED***> originalGetSourceFile.call(compilerHost, ...args)
  );
  state.readFileWithCache ***REMOVED*** readFileWithCache;
  compilerHost.getSourceFile ***REMOVED*** getSourceFileWithCache;
  state.cache ***REMOVED*** {
    originalReadFile,
    originalFileExists,
    originalDirectoryExists,
    originalCreateDirectory,
    originalWriteFile,
    originalReadFileWithCache,
    originalGetSourceFile
  };
}
function disableCache(state) {
  if (!state.cache) return;
  const { cache, host, compilerHost, extendedConfigCache, moduleResolutionCache, typeReferenceDirectiveResolutionCache, libraryResolutionCache } ***REMOVED*** state;
  host.readFile ***REMOVED*** cache.originalReadFile;
  host.fileExists ***REMOVED*** cache.originalFileExists;
  host.directoryExists ***REMOVED*** cache.originalDirectoryExists;
  host.createDirectory ***REMOVED*** cache.originalCreateDirectory;
  host.writeFile ***REMOVED*** cache.originalWriteFile;
  compilerHost.getSourceFile ***REMOVED*** cache.originalGetSourceFile;
  state.readFileWithCache ***REMOVED*** cache.originalReadFileWithCache;
  extendedConfigCache.clear();
  moduleResolutionCache ***REMOVED******REMOVED*** null ? void 0 : moduleResolutionCache.clear();
  typeReferenceDirectiveResolutionCache ***REMOVED******REMOVED*** null ? void 0 : typeReferenceDirectiveResolutionCache.clear();
  libraryResolutionCache ***REMOVED******REMOVED*** null ? void 0 : libraryResolutionCache.clear();
  state.cache ***REMOVED*** void 0;
}
function clearProjectStatus(state, resolved) {
  state.projectStatus.delete(resolved);
  state.diagnostics.delete(resolved);
}
function addProjToQueue({ projectPendingBuild }, proj, updateLevel) {
  const value ***REMOVED*** projectPendingBuild.get(proj);
  if (value ***REMOVED******REMOVED******REMOVED*** void 0) {
    projectPendingBuild.set(proj, updateLevel);
  } else if (value < updateLevel) {
    projectPendingBuild.set(proj, updateLevel);
  }
}
function setupInitialBuild(state, cancellationToken) {
  if (!state.allProjectBuildPending) return;
  state.allProjectBuildPending ***REMOVED*** false;
  if (state.options.watch) reportWatchStatus(state, Diagnostics.Starting_compilation_in_watch_mode);
  enableCache(state);
  const buildOrder ***REMOVED*** getBuildOrderFromAnyBuildOrder(getBuildOrder(state));
  buildOrder.forEach(
    (configFileName) ***REMOVED***> state.projectPendingBuild.set(
      toResolvedConfigFilePath(state, configFileName),
      0 /* Update */
    )
  );
  if (cancellationToken) {
    cancellationToken.throwIfCancellationRequested();
  }
}
function doneInvalidatedProject(state, projectPath) {
  state.projectPendingBuild.delete(projectPath);
  return state.diagnostics.has(projectPath) ? 1 /* DiagnosticsPresent_OutputsSkipped */ : 0 /* Success */;
}
function createUpdateOutputFileStampsProject(state, project, projectPath, config, buildOrder) {
  let updateOutputFileStampsPending ***REMOVED*** true;
  return {
    kind: 1 /* UpdateOutputFileStamps */,
    project,
    projectPath,
    buildOrder,
    getCompilerOptions: () ***REMOVED***> config.options,
    getCurrentDirectory: () ***REMOVED***> state.compilerHost.getCurrentDirectory(),
    updateOutputFileStatmps: () ***REMOVED***> {
      updateOutputTimestamps(state, config, projectPath);
      updateOutputFileStampsPending ***REMOVED*** false;
    },
    done: () ***REMOVED***> {
      if (updateOutputFileStampsPending) {
        updateOutputTimestamps(state, config, projectPath);
      }
      mark("SolutionBuilder::Timestamps only updates");
      return doneInvalidatedProject(state, projectPath);
    }
  };
}
function createBuildOrUpdateInvalidedProject(state, project, projectPath, projectIndex, config, status, buildOrder) {
  let step ***REMOVED*** 0 /* CreateProgram */;
  let program;
  let buildResult;
  return {
    kind: 0 /* Build */,
    project,
    projectPath,
    buildOrder,
    getCompilerOptions: () ***REMOVED***> config.options,
    getCurrentDirectory: () ***REMOVED***> state.compilerHost.getCurrentDirectory(),
    getBuilderProgram: () ***REMOVED***> withProgramOrUndefined(identity),
    getProgram: () ***REMOVED***> withProgramOrUndefined(
      (program2) ***REMOVED***> program2.getProgramOrUndefined()
    ),
    getSourceFile: (fileName) ***REMOVED***> withProgramOrUndefined(
      (program2) ***REMOVED***> program2.getSourceFile(fileName)
    ),
    getSourceFiles: () ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getSourceFiles()
    ),
    getOptionsDiagnostics: (cancellationToken) ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getOptionsDiagnostics(cancellationToken)
    ),
    getGlobalDiagnostics: (cancellationToken) ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getGlobalDiagnostics(cancellationToken)
    ),
    getConfigFileParsingDiagnostics: () ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getConfigFileParsingDiagnostics()
    ),
    getSyntacticDiagnostics: (sourceFile, cancellationToken) ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getSyntacticDiagnostics(sourceFile, cancellationToken)
    ),
    getAllDependencies: (sourceFile) ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getAllDependencies(sourceFile)
    ),
    getSemanticDiagnostics: (sourceFile, cancellationToken) ***REMOVED***> withProgramOrEmptyArray(
      (program2) ***REMOVED***> program2.getSemanticDiagnostics(sourceFile, cancellationToken)
    ),
    getSemanticDiagnosticsOfNextAffectedFile: (cancellationToken, ignoreSourceFile) ***REMOVED***> withProgramOrUndefined(
      (program2) ***REMOVED***> program2.getSemanticDiagnosticsOfNextAffectedFile && program2.getSemanticDiagnosticsOfNextAffectedFile(cancellationToken, ignoreSourceFile)
    ),
    emit: (targetSourceFile, writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers) ***REMOVED***> {
      if (targetSourceFile || emitOnlyDtsFiles) {
        return withProgramOrUndefined(
          (program2) ***REMOVED***> {
            var _a, _b;
            return program2.emit(targetSourceFile, writeFile2, cancellationToken, emitOnlyDtsFiles, customTransformers || ((_b ***REMOVED*** (_a ***REMOVED*** state.host).getCustomTransformers) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a, project)));
          }
        );
      }
      executeSteps(0 /* CreateProgram */, cancellationToken);
      return emit(writeFile2, cancellationToken, customTransformers);
    },
    done
  };
  function done(cancellationToken, writeFile2, customTransformers) {
    executeSteps(3 /* Done */, cancellationToken, writeFile2, customTransformers);
    mark("SolutionBuilder::Projects built");
    return doneInvalidatedProject(state, projectPath);
  }
  function withProgramOrUndefined(action) {
    executeSteps(0 /* CreateProgram */);
    return program && action(program);
  }
  function withProgramOrEmptyArray(action) {
    return withProgramOrUndefined(action) || emptyArray;
  }
  function createProgram2() {
    var _a, _b, _c;
    Debug.assert(program ***REMOVED******REMOVED******REMOVED*** void 0);
    if (state.options.dry) {
      reportStatus(state, Diagnostics.A_non_dry_build_would_build_project_0, project);
      buildResult ***REMOVED*** 1 /* Success */;
      step ***REMOVED*** 2 /* QueueReferencingProjects */;
      return;
    }
    if (state.options.verbose) reportStatus(state, Diagnostics.Building_project_0, project);
    if (config.fileNames.length ***REMOVED******REMOVED******REMOVED*** 0) {
      reportAndStoreErrors(state, projectPath, getConfigFileParsingDiagnostics(config));
      buildResult ***REMOVED*** 0 /* None */;
      step ***REMOVED*** 2 /* QueueReferencingProjects */;
      return;
    }
    const { host, compilerHost } ***REMOVED*** state;
    state.projectCompilerOptions ***REMOVED*** config.options;
    (_a ***REMOVED*** state.moduleResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _a.update(config.options);
    (_b ***REMOVED*** state.typeReferenceDirectiveResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _b.update(config.options);
    program ***REMOVED*** host.createProgram(
      config.fileNames,
      config.options,
      compilerHost,
      getOldProgram(state, projectPath, config),
      getConfigFileParsingDiagnostics(config),
      config.projectReferences
    );
    if (state.watch) {
      const internalMap ***REMOVED*** (_c ***REMOVED*** state.moduleResolutionCache) ***REMOVED******REMOVED*** null ? void 0 : _c.getPackageJsonInfoCache().getInternalMap();
      state.lastCachedPackageJsonLookups.set(
        projectPath,
        internalMap && new Set(arrayFrom(
          internalMap.values(),
          (data) ***REMOVED***> state.host.realpath && (isPackageJsonInfo(data) || data.directoryExists) ? state.host.realpath(combinePaths(data.packageDirectory, "package.json")) : combinePaths(data.packageDirectory, "package.json")
        ))
      );
      state.builderPrograms.set(projectPath, program);
    }
    step++;
  }
  function emit(writeFileCallback, cancellationToken, customTransformers) {
    var _a, _b, _c;
    Debug.assertIsDefined(program);
    Debug.assert(step ***REMOVED******REMOVED******REMOVED*** 1 /* Emit */);
    const { host, compilerHost } ***REMOVED*** state;
    const emittedOutputs ***REMOVED*** /* @__PURE__ */ new Map();
    const options ***REMOVED*** program.getCompilerOptions();
    const isIncremental ***REMOVED*** isIncrementalCompilation(options);
    let outputTimeStampMap;
    let now;
    const { emitResult, diagnostics } ***REMOVED*** emitFilesAndReportErrors(
      program,
      (d) ***REMOVED***> host.reportDiagnostic(d),
      state.write,
      /*reportSummary*/
      void 0,
      (name, text, writeByteOrderMark, onError, sourceFiles, data) ***REMOVED***> {
        var _a2;
        const path ***REMOVED*** toPath2(state, name);
        emittedOutputs.set(toPath2(state, name), name);
        if (data ***REMOVED******REMOVED*** null ? void 0 : data.buildInfo) {
          now || (now ***REMOVED*** getCurrentTime(state.host));
          const isChangedSignature2 ***REMOVED*** (_a2 ***REMOVED*** program.hasChangedEmitSignature) ***REMOVED******REMOVED*** null ? void 0 : _a2.call(program);
          const existing ***REMOVED*** getBuildInfoCacheEntry(state, name, projectPath);
          if (existing) {
            existing.buildInfo ***REMOVED*** data.buildInfo;
            existing.modifiedTime ***REMOVED*** now;
            if (isChangedSignature2) existing.latestChangedDtsTime ***REMOVED*** now;
          } else {
            state.buildInfoCache.set(projectPath, {
              path: toPath2(state, name),
              buildInfo: data.buildInfo,
              modifiedTime: now,
              latestChangedDtsTime: isChangedSignature2 ? now : void 0
            });
          }
        }
        const modifiedTime ***REMOVED*** (data ***REMOVED******REMOVED*** null ? void 0 : data.differsOnlyInMap) ? getModifiedTime(state.host, name) : void 0;
        (writeFileCallback || compilerHost.writeFile)(
          name,
          text,
          writeByteOrderMark,
          onError,
          sourceFiles,
          data
        );
        if (data ***REMOVED******REMOVED*** null ? void 0 : data.differsOnlyInMap) state.host.setModifiedTime(name, modifiedTime);
        else if (!isIncremental && state.watch) {
          (outputTimeStampMap || (outputTimeStampMap ***REMOVED*** getOutputTimeStampMap(state, projectPath))).set(path, now || (now ***REMOVED*** getCurrentTime(state.host)));
        }
      },
      cancellationToken,
      /*emitOnlyDtsFiles*/
      void 0,
      customTransformers || ((_b ***REMOVED*** (_a ***REMOVED*** state.host).getCustomTransformers) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a, project))
    );
    if ((!options.noEmitOnError || !diagnostics.length) && (emittedOutputs.size || status.type !***REMOVED******REMOVED*** 8 /* OutOfDateBuildInfoWithErrors */)) {
      updateOutputTimestampsWorker(state, config, projectPath, Diagnostics.Updating_unchanged_output_timestamps_of_project_0, emittedOutputs);
    }
    state.projectErrorsReported.set(projectPath, true);
    buildResult ***REMOVED*** ((_c ***REMOVED*** program.hasChangedEmitSignature) ***REMOVED******REMOVED*** null ? void 0 : _c.call(program)) ? 0 /* None */ : 2 /* DeclarationOutputUnchanged */;
    if (!diagnostics.length) {
      state.diagnostics.delete(projectPath);
      state.projectStatus.set(projectPath, {
        type: 1 /* UpToDate */,
        oldestOutputFileName: firstOrUndefinedIterator(emittedOutputs.values()) ?? getFirstProjectOutput(config, !host.useCaseSensitiveFileNames())
      });
    } else {
      state.diagnostics.set(projectPath, diagnostics);
      state.projectStatus.set(projectPath, { type: 0 /* Unbuildable */, reason: `it had errors` });
      buildResult |***REMOVED*** 4 /* AnyErrors */;
    }
    afterProgramDone(state, program);
    step ***REMOVED*** 2 /* QueueReferencingProjects */;
    return emitResult;
  }
  function executeSteps(till, cancellationToken, writeFile2, customTransformers) {
    while (step <***REMOVED*** till && step < 3 /* Done */) {
      const currentStep ***REMOVED*** step;
      switch (step) {
        case 0 /* CreateProgram */:
          createProgram2();
          break;
        case 1 /* Emit */:
          emit(writeFile2, cancellationToken, customTransformers);
          break;
        case 2 /* QueueReferencingProjects */:
          queueReferencingProjects(state, project, projectPath, projectIndex, config, buildOrder, Debug.checkDefined(buildResult));
          step++;
          break;
        // Should never be done
        case 3 /* Done */:
        default:
          assertType(step);
      }
      Debug.assert(step > currentStep);
    }
  }
}
function getNextInvalidatedProjectCreateInfo(state, buildOrder, reportQueue) {
  if (!state.projectPendingBuild.size) return void 0;
  if (isCircularBuildOrder(buildOrder)) return void 0;
  const { options, projectPendingBuild } ***REMOVED*** state;
  for (let projectIndex ***REMOVED*** 0; projectIndex < buildOrder.length; projectIndex++) {
    const project ***REMOVED*** buildOrder[projectIndex];
    const projectPath ***REMOVED*** toResolvedConfigFilePath(state, project);
    const updateLevel ***REMOVED*** state.projectPendingBuild.get(projectPath);
    if (updateLevel ***REMOVED******REMOVED******REMOVED*** void 0) continue;
    if (reportQueue) {
      reportQueue ***REMOVED*** false;
      reportBuildQueue(state, buildOrder);
    }
    const config ***REMOVED*** parseConfigFile(state, project, projectPath);
    if (!config) {
      reportParseConfigFileDiagnostic(state, projectPath);
      projectPendingBuild.delete(projectPath);
      continue;
    }
    if (updateLevel ***REMOVED******REMOVED******REMOVED*** 2 /* Full */) {
      watchConfigFile(state, project, projectPath, config);
      watchExtendedConfigFiles(state, projectPath, config);
      watchWildCardDirectories(state, project, projectPath, config);
      watchInputFiles(state, project, projectPath, config);
      watchPackageJsonFiles(state, project, projectPath, config);
    } else if (updateLevel ***REMOVED******REMOVED******REMOVED*** 1 /* RootNamesAndUpdate */) {
      config.fileNames ***REMOVED*** getFileNamesFromConfigSpecs(config.options.configFile.configFileSpecs, getDirectoryPath(project), config.options, state.parseConfigFileHost);
      updateErrorForNoInputFiles(
        config.fileNames,
        project,
        config.options.configFile.configFileSpecs,
        config.errors,
        canJsonReportNoInputFiles(config.raw)
      );
      watchInputFiles(state, project, projectPath, config);
      watchPackageJsonFiles(state, project, projectPath, config);
    }
    const status ***REMOVED*** getUpToDateStatus(state, config, projectPath);
    if (!options.force) {
      if (status.type ***REMOVED******REMOVED******REMOVED*** 1 /* UpToDate */) {
        verboseReportProjectStatus(state, project, status);
        reportAndStoreErrors(state, projectPath, getConfigFileParsingDiagnostics(config));
        projectPendingBuild.delete(projectPath);
        if (options.dry) {
          reportStatus(state, Diagnostics.Project_0_is_up_to_date, project);
        }
        continue;
      }
      if (status.type ***REMOVED******REMOVED******REMOVED*** 2 /* UpToDateWithUpstreamTypes */ || status.type ***REMOVED******REMOVED******REMOVED*** 15 /* UpToDateWithInputFileText */) {
        reportAndStoreErrors(state, projectPath, getConfigFileParsingDiagnostics(config));
        return {
          kind: 1 /* UpdateOutputFileStamps */,
          status,
          project,
          projectPath,
          projectIndex,
          config
        };
      }
    }
    if (status.type ***REMOVED******REMOVED******REMOVED*** 12 /* UpstreamBlocked */) {
      verboseReportProjectStatus(state, project, status);
      reportAndStoreErrors(state, projectPath, getConfigFileParsingDiagnostics(config));
      projectPendingBuild.delete(projectPath);
      if (options.verbose) {
        reportStatus(
          state,
          status.upstreamProjectBlocked ? Diagnostics.Skipping_build_of_project_0_because_its_dependency_1_was_not_built : Diagnostics.Skipping_build_of_project_0_because_its_dependency_1_has_errors,
          project,
          status.upstreamProjectName
        );
      }
      continue;
    }
    if (status.type ***REMOVED******REMOVED******REMOVED*** 16 /* ContainerOnly */) {
      verboseReportProjectStatus(state, project, status);
      reportAndStoreErrors(state, projectPath, getConfigFileParsingDiagnostics(config));
      projectPendingBuild.delete(projectPath);
      continue;
    }
    return {
      kind: 0 /* Build */,
      status,
      project,
      projectPath,
      projectIndex,
      config
    };
  }
  return void 0;
}
function createInvalidatedProjectWithInfo(state, info, buildOrder) {
  verboseReportProjectStatus(state, info.project, info.status);
  return info.kind !***REMOVED******REMOVED*** 1 /* UpdateOutputFileStamps */ ? createBuildOrUpdateInvalidedProject(
    state,
    info.project,
    info.projectPath,
    info.projectIndex,
    info.config,
    info.status,
    buildOrder
  ) : createUpdateOutputFileStampsProject(
    state,
    info.project,
    info.projectPath,
    info.config,
    buildOrder
  );
}
function getNextInvalidatedProject(state, buildOrder, reportQueue) {
  const info ***REMOVED*** getNextInvalidatedProjectCreateInfo(state, buildOrder, reportQueue);
  if (!info) return info;
  return createInvalidatedProjectWithInfo(state, info, buildOrder);
}
function getOldProgram({ options, builderPrograms, compilerHost }, proj, parsed) {
  if (options.force) return void 0;
  const value ***REMOVED*** builderPrograms.get(proj);
  if (value) return value;
  return readBuilderProgram(parsed.options, compilerHost);
}
function afterProgramDone(state, program) {
  if (program) {
    if (state.host.afterProgramEmitAndDiagnostics) {
      state.host.afterProgramEmitAndDiagnostics(program);
    }
    program.releaseProgram();
  }
  state.projectCompilerOptions ***REMOVED*** state.baseCompilerOptions;
}
function isFileWatcherWithModifiedTime(value) {
  return !!value.watcher;
}
function getModifiedTime2(state, fileName) {
  const path ***REMOVED*** toPath2(state, fileName);
  const existing ***REMOVED*** state.filesWatched.get(path);
  if (state.watch && !!existing) {
    if (!isFileWatcherWithModifiedTime(existing)) return existing;
    if (existing.modifiedTime) return existing.modifiedTime;
  }
  const result ***REMOVED*** getModifiedTime(state.host, fileName);
  if (state.watch) {
    if (existing) existing.modifiedTime ***REMOVED*** result;
    else state.filesWatched.set(path, result);
  }
  return result;
}
function watchFile(state, file, callback, pollingInterval, options, watchType, project) {
  const path ***REMOVED*** toPath2(state, file);
  const existing ***REMOVED*** state.filesWatched.get(path);
  if (existing && isFileWatcherWithModifiedTime(existing)) {
    existing.callbacks.push(callback);
  } else {
    const watcher ***REMOVED*** state.watchFile(
      file,
      (fileName, eventKind, modifiedTime) ***REMOVED***> {
        const existing2 ***REMOVED*** Debug.checkDefined(state.filesWatched.get(path));
        Debug.assert(isFileWatcherWithModifiedTime(existing2));
        existing2.modifiedTime ***REMOVED*** modifiedTime;
        existing2.callbacks.forEach((cb) ***REMOVED***> cb(fileName, eventKind, modifiedTime));
      },
      pollingInterval,
      options,
      watchType,
      project
    );
    state.filesWatched.set(path, { callbacks: [callback], watcher, modifiedTime: existing });
  }
  return {
    close: () ***REMOVED***> {
      const existing2 ***REMOVED*** Debug.checkDefined(state.filesWatched.get(path));
      Debug.assert(isFileWatcherWithModifiedTime(existing2));
      if (existing2.callbacks.length ***REMOVED******REMOVED******REMOVED*** 1) {
        state.filesWatched.delete(path);
        closeFileWatcherOf(existing2);
      } else {
        unorderedRemoveItem(existing2.callbacks, callback);
      }
    }
  };
}
function getOutputTimeStampMap(state, resolvedConfigFilePath) {
  if (!state.watch) return void 0;
  let result ***REMOVED*** state.outputTimeStamps.get(resolvedConfigFilePath);
  if (!result) state.outputTimeStamps.set(resolvedConfigFilePath, result ***REMOVED*** /* @__PURE__ */ new Map());
  return result;
}
function getBuildInfoCacheEntry(state, buildInfoPath, resolvedConfigPath) {
  const path ***REMOVED*** toPath2(state, buildInfoPath);
  const existing ***REMOVED*** state.buildInfoCache.get(resolvedConfigPath);
  return (existing ***REMOVED******REMOVED*** null ? void 0 : existing.path) ***REMOVED******REMOVED******REMOVED*** path ? existing : void 0;
}
function getBuildInfo3(state, buildInfoPath, resolvedConfigPath, modifiedTime) {
  const path ***REMOVED*** toPath2(state, buildInfoPath);
  const existing ***REMOVED*** state.buildInfoCache.get(resolvedConfigPath);
  if (existing !***REMOVED******REMOVED*** void 0 && existing.path ***REMOVED******REMOVED******REMOVED*** path) {
    return existing.buildInfo || void 0;
  }
  const value ***REMOVED*** state.readFileWithCache(buildInfoPath);
  const buildInfo ***REMOVED*** value ? getBuildInfo(buildInfoPath, value) : void 0;
  state.buildInfoCache.set(resolvedConfigPath, { path, buildInfo: buildInfo || false, modifiedTime: modifiedTime || missingFileModifiedTime });
  return buildInfo;
}
function checkConfigFileUpToDateStatus(state, configFile, oldestOutputFileTime, oldestOutputFileName) {
  const tsconfigTime ***REMOVED*** getModifiedTime2(state, configFile);
  if (oldestOutputFileTime < tsconfigTime) {
    return {
      type: 5 /* OutOfDateWithSelf */,
      outOfDateOutputFileName: oldestOutputFileName,
      newerInputFileName: configFile
    };
  }
}
function getUpToDateStatusWorker(state, project, resolvedPath) {
  var _a, _b, _c, _d, _e;
  if (isSolutionConfig(project)) return { type: 16 /* ContainerOnly */ };
  let referenceStatuses;
  const force ***REMOVED*** !!state.options.force;
  if (project.projectReferences) {
    state.projectStatus.set(resolvedPath, { type: 13 /* ComputingUpstream */ });
    for (const ref of project.projectReferences) {
      const resolvedRef ***REMOVED*** resolveProjectReferencePath(ref);
      const resolvedRefPath ***REMOVED*** toResolvedConfigFilePath(state, resolvedRef);
      const resolvedConfig ***REMOVED*** parseConfigFile(state, resolvedRef, resolvedRefPath);
      const refStatus ***REMOVED*** getUpToDateStatus(state, resolvedConfig, resolvedRefPath);
      if (refStatus.type ***REMOVED******REMOVED******REMOVED*** 13 /* ComputingUpstream */ || refStatus.type ***REMOVED******REMOVED******REMOVED*** 16 /* ContainerOnly */) {
        continue;
      }
      if (state.options.stopBuildOnErrors && (refStatus.type ***REMOVED******REMOVED******REMOVED*** 0 /* Unbuildable */ || refStatus.type ***REMOVED******REMOVED******REMOVED*** 12 /* UpstreamBlocked */)) {
        return {
          type: 12 /* UpstreamBlocked */,
          upstreamProjectName: ref.path,
          upstreamProjectBlocked: refStatus.type ***REMOVED******REMOVED******REMOVED*** 12 /* UpstreamBlocked */
        };
      }
      if (!force) (referenceStatuses || (referenceStatuses ***REMOVED*** [])).push({ ref, refStatus, resolvedRefPath, resolvedConfig });
    }
  }
  if (force) return { type: 17 /* ForceBuild */ };
  const { host } ***REMOVED*** state;
  const buildInfoPath ***REMOVED*** getTsBuildInfoEmitOutputFilePath(project.options);
  const isIncremental ***REMOVED*** isIncrementalCompilation(project.options);
  let buildInfoCacheEntry ***REMOVED*** getBuildInfoCacheEntry(state, buildInfoPath, resolvedPath);
  const buildInfoTime ***REMOVED*** (buildInfoCacheEntry ***REMOVED******REMOVED*** null ? void 0 : buildInfoCacheEntry.modifiedTime) || getModifiedTime(host, buildInfoPath);
  if (buildInfoTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) {
    if (!buildInfoCacheEntry) {
      state.buildInfoCache.set(resolvedPath, {
        path: toPath2(state, buildInfoPath),
        buildInfo: false,
        modifiedTime: buildInfoTime
      });
    }
    return {
      type: 3 /* OutputMissing */,
      missingOutputFileName: buildInfoPath
    };
  }
  const buildInfo ***REMOVED*** getBuildInfo3(state, buildInfoPath, resolvedPath, buildInfoTime);
  if (!buildInfo) {
    return {
      type: 4 /* ErrorReadingFile */,
      fileName: buildInfoPath
    };
  }
  const incrementalBuildInfo ***REMOVED*** isIncremental && isIncrementalBuildInfo(buildInfo) ? buildInfo : void 0;
  if ((incrementalBuildInfo || !isIncremental) && buildInfo.version !***REMOVED******REMOVED*** version) {
    return {
      type: 14 /* TsVersionOutputOfDate */,
      version: buildInfo.version
    };
  }
  if (!project.options.noCheck && (buildInfo.errors || // TODO: syntax errors????
  buildInfo.checkPending)) {
    return {
      type: 8 /* OutOfDateBuildInfoWithErrors */,
      buildInfoFile: buildInfoPath
    };
  }
  if (incrementalBuildInfo) {
    if (!project.options.noCheck && (((_a ***REMOVED*** incrementalBuildInfo.changeFileSet) ***REMOVED******REMOVED*** null ? void 0 : _a.length) || ((_b ***REMOVED*** incrementalBuildInfo.semanticDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _b.length) || getEmitDeclarations(project.options) && ((_c ***REMOVED*** incrementalBuildInfo.emitDiagnosticsPerFile) ***REMOVED******REMOVED*** null ? void 0 : _c.length))) {
      return {
        type: 8 /* OutOfDateBuildInfoWithErrors */,
        buildInfoFile: buildInfoPath
      };
    }
    if (!project.options.noEmit && (((_d ***REMOVED*** incrementalBuildInfo.changeFileSet) ***REMOVED******REMOVED*** null ? void 0 : _d.length) || ((_e ***REMOVED*** incrementalBuildInfo.affectedFilesPendingEmit) ***REMOVED******REMOVED*** null ? void 0 : _e.length) || incrementalBuildInfo.pendingEmit !***REMOVED******REMOVED*** void 0)) {
      return {
        type: 7 /* OutOfDateBuildInfoWithPendingEmit */,
        buildInfoFile: buildInfoPath
      };
    }
    if ((!project.options.noEmit || project.options.noEmit && getEmitDeclarations(project.options)) && getPendingEmitKindWithSeen(
      project.options,
      incrementalBuildInfo.options || {},
      /*emitOnlyDtsFiles*/
      void 0,
      !!project.options.noEmit
    )) {
      return {
        type: 9 /* OutOfDateOptions */,
        buildInfoFile: buildInfoPath
      };
    }
  }
  let oldestOutputFileTime ***REMOVED*** buildInfoTime;
  let oldestOutputFileName ***REMOVED*** buildInfoPath;
  let newestInputFileName ***REMOVED*** void 0;
  let newestInputFileTime ***REMOVED*** minimumDate;
  let pseudoInputUpToDate ***REMOVED*** false;
  const seenRoots ***REMOVED*** /* @__PURE__ */ new Set();
  let buildInfoVersionMap;
  for (const inputFile of project.fileNames) {
    const inputTime ***REMOVED*** getModifiedTime2(state, inputFile);
    if (inputTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) {
      return {
        type: 0 /* Unbuildable */,
        reason: `${inputFile} does not exist`
      };
    }
    const inputPath ***REMOVED*** toPath2(state, inputFile);
    if (buildInfoTime < inputTime) {
      let version2;
      let currentVersion;
      if (incrementalBuildInfo) {
        if (!buildInfoVersionMap) buildInfoVersionMap ***REMOVED*** getBuildInfoFileVersionMap(incrementalBuildInfo, buildInfoPath, host);
        const resolvedInputPath ***REMOVED*** buildInfoVersionMap.roots.get(inputPath);
        version2 ***REMOVED*** buildInfoVersionMap.fileInfos.get(resolvedInputPath ?? inputPath);
        const text ***REMOVED*** version2 ? state.readFileWithCache(resolvedInputPath ?? inputFile) : void 0;
        currentVersion ***REMOVED*** text !***REMOVED******REMOVED*** void 0 ? getSourceFileVersionAsHashFromText(host, text) : void 0;
        if (version2 && version2 ***REMOVED******REMOVED******REMOVED*** currentVersion) pseudoInputUpToDate ***REMOVED*** true;
      }
      if (!version2 || version2 !***REMOVED******REMOVED*** currentVersion) {
        return {
          type: 5 /* OutOfDateWithSelf */,
          outOfDateOutputFileName: buildInfoPath,
          newerInputFileName: inputFile
        };
      }
    }
    if (inputTime > newestInputFileTime) {
      newestInputFileName ***REMOVED*** inputFile;
      newestInputFileTime ***REMOVED*** inputTime;
    }
    seenRoots.add(inputPath);
  }
  let existingRoot;
  if (incrementalBuildInfo) {
    if (!buildInfoVersionMap) buildInfoVersionMap ***REMOVED*** getBuildInfoFileVersionMap(incrementalBuildInfo, buildInfoPath, host);
    existingRoot ***REMOVED*** forEachEntry(
      buildInfoVersionMap.roots,
      // File was root file when project was built but its not any more
      (_resolved, existingRoot2) ***REMOVED***> !seenRoots.has(existingRoot2) ? existingRoot2 : void 0
    );
  } else {
    existingRoot ***REMOVED*** forEach(
      getNonIncrementalBuildInfoRoots(buildInfo, buildInfoPath, host),
      (root) ***REMOVED***> !seenRoots.has(root) ? root : void 0
    );
  }
  if (existingRoot) {
    return {
      type: 10 /* OutOfDateRoots */,
      buildInfoFile: buildInfoPath,
      inputFile: existingRoot
    };
  }
  if (!isIncremental) {
    const outputs ***REMOVED*** getAllProjectOutputs(project, !host.useCaseSensitiveFileNames());
    const outputTimeStampMap ***REMOVED*** getOutputTimeStampMap(state, resolvedPath);
    for (const output of outputs) {
      if (output ***REMOVED******REMOVED******REMOVED*** buildInfoPath) continue;
      const path ***REMOVED*** toPath2(state, output);
      let outputTime ***REMOVED*** outputTimeStampMap ***REMOVED******REMOVED*** null ? void 0 : outputTimeStampMap.get(path);
      if (!outputTime) {
        outputTime ***REMOVED*** getModifiedTime(state.host, output);
        outputTimeStampMap ***REMOVED******REMOVED*** null ? void 0 : outputTimeStampMap.set(path, outputTime);
      }
      if (outputTime ***REMOVED******REMOVED******REMOVED*** missingFileModifiedTime) {
        return {
          type: 3 /* OutputMissing */,
          missingOutputFileName: output
        };
      }
      if (outputTime < newestInputFileTime) {
        return {
          type: 5 /* OutOfDateWithSelf */,
          outOfDateOutputFileName: output,
          newerInputFileName: newestInputFileName
        };
      }
      if (outputTime < oldestOutputFileTime) {
        oldestOutputFileTime ***REMOVED*** outputTime;
        oldestOutputFileName ***REMOVED*** output;
      }
    }
  }
  let pseudoUpToDate ***REMOVED*** false;
  if (referenceStatuses) {
    for (const { ref, refStatus, resolvedConfig, resolvedRefPath } of referenceStatuses) {
      if (refStatus.newestInputFileTime && refStatus.newestInputFileTime <***REMOVED*** oldestOutputFileTime) {
        continue;
      }
      if (hasSameBuildInfo(state, buildInfoCacheEntry ?? (buildInfoCacheEntry ***REMOVED*** state.buildInfoCache.get(resolvedPath)), resolvedRefPath)) {
        return {
          type: 6 /* OutOfDateWithUpstream */,
          outOfDateOutputFileName: buildInfoPath,
          newerProjectName: ref.path
        };
      }
      const newestDeclarationFileContentChangedTime ***REMOVED*** getLatestChangedDtsTime(state, resolvedConfig.options, resolvedRefPath);
      if (newestDeclarationFileContentChangedTime && newestDeclarationFileContentChangedTime <***REMOVED*** oldestOutputFileTime) {
        pseudoUpToDate ***REMOVED*** true;
        continue;
      }
      Debug.assert(oldestOutputFileName !***REMOVED******REMOVED*** void 0, "Should have an oldest output filename here");
      return {
        type: 6 /* OutOfDateWithUpstream */,
        outOfDateOutputFileName: oldestOutputFileName,
        newerProjectName: ref.path
      };
    }
  }
  const configStatus ***REMOVED*** checkConfigFileUpToDateStatus(state, project.options.configFilePath, oldestOutputFileTime, oldestOutputFileName);
  if (configStatus) return configStatus;
  const extendedConfigStatus ***REMOVED*** forEach(project.options.configFile.extendedSourceFiles || emptyArray, (configFile) ***REMOVED***> checkConfigFileUpToDateStatus(state, configFile, oldestOutputFileTime, oldestOutputFileName));
  if (extendedConfigStatus) return extendedConfigStatus;
  const packageJsonLookups ***REMOVED*** state.lastCachedPackageJsonLookups.get(resolvedPath);
  const dependentPackageFileStatus ***REMOVED*** packageJsonLookups && forEachKey(
    packageJsonLookups,
    (path) ***REMOVED***> checkConfigFileUpToDateStatus(state, path, oldestOutputFileTime, oldestOutputFileName)
  );
  if (dependentPackageFileStatus) return dependentPackageFileStatus;
  return {
    type: pseudoUpToDate ? 2 /* UpToDateWithUpstreamTypes */ : pseudoInputUpToDate ? 15 /* UpToDateWithInputFileText */ : 1 /* UpToDate */,
    newestInputFileTime,
    newestInputFileName,
    oldestOutputFileName
  };
}
function hasSameBuildInfo(state, buildInfoCacheEntry, resolvedRefPath) {
  const refBuildInfo ***REMOVED*** state.buildInfoCache.get(resolvedRefPath);
  return refBuildInfo.path ***REMOVED******REMOVED******REMOVED*** buildInfoCacheEntry.path;
}
function getUpToDateStatus(state, project, resolvedPath) {
  if (project ***REMOVED******REMOVED******REMOVED*** void 0) {
    return { type: 0 /* Unbuildable */, reason: "config file deleted mid-build" };
  }
  const prior ***REMOVED*** state.projectStatus.get(resolvedPath);
  if (prior !***REMOVED******REMOVED*** void 0) {
    return prior;
  }
  mark("SolutionBuilder::beforeUpToDateCheck");
  const actual ***REMOVED*** getUpToDateStatusWorker(state, project, resolvedPath);
  mark("SolutionBuilder::afterUpToDateCheck");
  measure("SolutionBuilder::Up-to-date check", "SolutionBuilder::beforeUpToDateCheck", "SolutionBuilder::afterUpToDateCheck");
  state.projectStatus.set(resolvedPath, actual);
  return actual;
}
function updateOutputTimestampsWorker(state, proj, projectPath, verboseMessage, skipOutputs) {
  if (proj.options.noEmit) return;
  let now;
  const buildInfoPath ***REMOVED*** getTsBuildInfoEmitOutputFilePath(proj.options);
  const isIncremental ***REMOVED*** isIncrementalCompilation(proj.options);
  if (buildInfoPath && isIncremental) {
    if (!(skipOutputs ***REMOVED******REMOVED*** null ? void 0 : skipOutputs.has(toPath2(state, buildInfoPath)))) {
      if (!!state.options.verbose) reportStatus(state, verboseMessage, proj.options.configFilePath);
      state.host.setModifiedTime(buildInfoPath, now ***REMOVED*** getCurrentTime(state.host));
      getBuildInfoCacheEntry(state, buildInfoPath, projectPath).modifiedTime ***REMOVED*** now;
    }
    state.outputTimeStamps.delete(projectPath);
    return;
  }
  const { host } ***REMOVED*** state;
  const outputs ***REMOVED*** getAllProjectOutputs(proj, !host.useCaseSensitiveFileNames());
  const outputTimeStampMap ***REMOVED*** getOutputTimeStampMap(state, projectPath);
  const modifiedOutputs ***REMOVED*** outputTimeStampMap ? /* @__PURE__ */ new Set() : void 0;
  if (!skipOutputs || outputs.length !***REMOVED******REMOVED*** skipOutputs.size) {
    let reportVerbose ***REMOVED*** !!state.options.verbose;
    for (const file of outputs) {
      const path ***REMOVED*** toPath2(state, file);
      if (skipOutputs ***REMOVED******REMOVED*** null ? void 0 : skipOutputs.has(path)) continue;
      if (reportVerbose) {
        reportVerbose ***REMOVED*** false;
        reportStatus(state, verboseMessage, proj.options.configFilePath);
      }
      host.setModifiedTime(file, now || (now ***REMOVED*** getCurrentTime(state.host)));
      if (file ***REMOVED******REMOVED******REMOVED*** buildInfoPath) getBuildInfoCacheEntry(state, buildInfoPath, projectPath).modifiedTime ***REMOVED*** now;
      else if (outputTimeStampMap) {
        outputTimeStampMap.set(path, now);
        modifiedOutputs.add(path);
      }
    }
  }
  outputTimeStampMap ***REMOVED******REMOVED*** null ? void 0 : outputTimeStampMap.forEach((_value, key) ***REMOVED***> {
    if (!(skipOutputs ***REMOVED******REMOVED*** null ? void 0 : skipOutputs.has(key)) && !modifiedOutputs.has(key)) outputTimeStampMap.delete(key);
  });
}
function getLatestChangedDtsTime(state, options, resolvedConfigPath) {
  if (!options.composite) return void 0;
  const entry ***REMOVED*** Debug.checkDefined(state.buildInfoCache.get(resolvedConfigPath));
  if (entry.latestChangedDtsTime !***REMOVED******REMOVED*** void 0) return entry.latestChangedDtsTime || void 0;
  const latestChangedDtsTime ***REMOVED*** entry.buildInfo && isIncrementalBuildInfo(entry.buildInfo) && entry.buildInfo.latestChangedDtsFile ? state.host.getModifiedTime(getNormalizedAbsolutePath(entry.buildInfo.latestChangedDtsFile, getDirectoryPath(entry.path))) : void 0;
  entry.latestChangedDtsTime ***REMOVED*** latestChangedDtsTime || false;
  return latestChangedDtsTime;
}
function updateOutputTimestamps(state, proj, resolvedPath) {
  if (state.options.dry) {
    return reportStatus(state, Diagnostics.A_non_dry_build_would_update_timestamps_for_output_of_project_0, proj.options.configFilePath);
  }
  updateOutputTimestampsWorker(state, proj, resolvedPath, Diagnostics.Updating_output_timestamps_of_project_0);
  state.projectStatus.set(resolvedPath, {
    type: 1 /* UpToDate */,
    oldestOutputFileName: getFirstProjectOutput(proj, !state.host.useCaseSensitiveFileNames())
  });
}
function queueReferencingProjects(state, project, projectPath, projectIndex, config, buildOrder, buildResult) {
  if (state.options.stopBuildOnErrors && buildResult & 4 /* AnyErrors */) return;
  if (!config.options.composite) return;
  for (let index ***REMOVED*** projectIndex + 1; index < buildOrder.length; index++) {
    const nextProject ***REMOVED*** buildOrder[index];
    const nextProjectPath ***REMOVED*** toResolvedConfigFilePath(state, nextProject);
    if (state.projectPendingBuild.has(nextProjectPath)) continue;
    const nextProjectConfig ***REMOVED*** parseConfigFile(state, nextProject, nextProjectPath);
    if (!nextProjectConfig || !nextProjectConfig.projectReferences) continue;
    for (const ref of nextProjectConfig.projectReferences) {
      const resolvedRefPath ***REMOVED*** resolveProjectName(state, ref.path);
      if (toResolvedConfigFilePath(state, resolvedRefPath) !***REMOVED******REMOVED*** projectPath) continue;
      const status ***REMOVED*** state.projectStatus.get(nextProjectPath);
      if (status) {
        switch (status.type) {
          case 1 /* UpToDate */:
            if (buildResult & 2 /* DeclarationOutputUnchanged */) {
              status.type ***REMOVED*** 2 /* UpToDateWithUpstreamTypes */;
              break;
            }
          // falls through
          case 15 /* UpToDateWithInputFileText */:
          case 2 /* UpToDateWithUpstreamTypes */:
            if (!(buildResult & 2 /* DeclarationOutputUnchanged */)) {
              state.projectStatus.set(nextProjectPath, {
                type: 6 /* OutOfDateWithUpstream */,
                outOfDateOutputFileName: status.oldestOutputFileName,
                newerProjectName: project
              });
            }
            break;
          case 12 /* UpstreamBlocked */:
            if (toResolvedConfigFilePath(state, resolveProjectName(state, status.upstreamProjectName)) ***REMOVED******REMOVED******REMOVED*** projectPath) {
              clearProjectStatus(state, nextProjectPath);
            }
            break;
        }
      }
      addProjToQueue(state, nextProjectPath, 0 /* Update */);
      break;
    }
  }
}
function build(state, project, cancellationToken, writeFile2, getCustomTransformers, onlyReferences) {
  mark("SolutionBuilder::beforeBuild");
  const result ***REMOVED*** buildWorker(state, project, cancellationToken, writeFile2, getCustomTransformers, onlyReferences);
  mark("SolutionBuilder::afterBuild");
  measure("SolutionBuilder::Build", "SolutionBuilder::beforeBuild", "SolutionBuilder::afterBuild");
  return result;
}
function buildWorker(state, project, cancellationToken, writeFile2, getCustomTransformers, onlyReferences) {
  const buildOrder ***REMOVED*** getBuildOrderFor(state, project, onlyReferences);
  if (!buildOrder) return 3 /* InvalidProject_OutputsSkipped */;
  setupInitialBuild(state, cancellationToken);
  let reportQueue ***REMOVED*** true;
  let successfulProjects ***REMOVED*** 0;
  while (true) {
    const invalidatedProject ***REMOVED*** getNextInvalidatedProject(state, buildOrder, reportQueue);
    if (!invalidatedProject) break;
    reportQueue ***REMOVED*** false;
    invalidatedProject.done(cancellationToken, writeFile2, getCustomTransformers ***REMOVED******REMOVED*** null ? void 0 : getCustomTransformers(invalidatedProject.project));
    if (!state.diagnostics.has(invalidatedProject.projectPath)) successfulProjects++;
  }
  disableCache(state);
  reportErrorSummary(state, buildOrder);
  startWatching(state, buildOrder);
  return isCircularBuildOrder(buildOrder) ? 4 /* ProjectReferenceCycle_OutputsSkipped */ : !buildOrder.some((p) ***REMOVED***> state.diagnostics.has(toResolvedConfigFilePath(state, p))) ? 0 /* Success */ : successfulProjects ? 2 /* DiagnosticsPresent_OutputsGenerated */ : 1 /* DiagnosticsPresent_OutputsSkipped */;
}
function clean(state, project, onlyReferences) {
  mark("SolutionBuilder::beforeClean");
  const result ***REMOVED*** cleanWorker(state, project, onlyReferences);
  mark("SolutionBuilder::afterClean");
  measure("SolutionBuilder::Clean", "SolutionBuilder::beforeClean", "SolutionBuilder::afterClean");
  return result;
}
function cleanWorker(state, project, onlyReferences) {
  const buildOrder ***REMOVED*** getBuildOrderFor(state, project, onlyReferences);
  if (!buildOrder) return 3 /* InvalidProject_OutputsSkipped */;
  if (isCircularBuildOrder(buildOrder)) {
    reportErrors(state, buildOrder.circularDiagnostics);
    return 4 /* ProjectReferenceCycle_OutputsSkipped */;
  }
  const { options, host } ***REMOVED*** state;
  const filesToDelete ***REMOVED*** options.dry ? [] : void 0;
  for (const proj of buildOrder) {
    const resolvedPath ***REMOVED*** toResolvedConfigFilePath(state, proj);
    const parsed ***REMOVED*** parseConfigFile(state, proj, resolvedPath);
    if (parsed ***REMOVED******REMOVED******REMOVED*** void 0) {
      reportParseConfigFileDiagnostic(state, resolvedPath);
      continue;
    }
    const outputs ***REMOVED*** getAllProjectOutputs(parsed, !host.useCaseSensitiveFileNames());
    if (!outputs.length) continue;
    const inputFileNames ***REMOVED*** new Set(parsed.fileNames.map((f) ***REMOVED***> toPath2(state, f)));
    for (const output of outputs) {
      if (inputFileNames.has(toPath2(state, output))) continue;
      if (host.fileExists(output)) {
        if (filesToDelete) {
          filesToDelete.push(output);
        } else {
          host.deleteFile(output);
          invalidateProject(state, resolvedPath, 0 /* Update */);
        }
      }
    }
  }
  if (filesToDelete) {
    reportStatus(state, Diagnostics.A_non_dry_build_would_delete_the_following_files_Colon_0, filesToDelete.map((f) ***REMOVED***> `\r
 * ${f}`).join(""));
  }
  return 0 /* Success */;
}
function invalidateProject(state, resolved, updateLevel) {
  if (state.host.getParsedCommandLine && updateLevel ***REMOVED******REMOVED******REMOVED*** 1 /* RootNamesAndUpdate */) {
    updateLevel ***REMOVED*** 2 /* Full */;
  }
  if (updateLevel ***REMOVED******REMOVED******REMOVED*** 2 /* Full */) {
    state.configFileCache.delete(resolved);
    state.buildOrder ***REMOVED*** void 0;
  }
  state.needsSummary ***REMOVED*** true;
  clearProjectStatus(state, resolved);
  addProjToQueue(state, resolved, updateLevel);
  enableCache(state);
}
function invalidateProjectAndScheduleBuilds(state, resolvedPath, updateLevel) {
  state.reportFileChangeDetected ***REMOVED*** true;
  invalidateProject(state, resolvedPath, updateLevel);
  scheduleBuildInvalidatedProject(
    state,
    250,
    /*changeDetected*/
    true
  );
}
function scheduleBuildInvalidatedProject(state, time, changeDetected) {
  const { hostWithWatch } ***REMOVED*** state;
  if (!hostWithWatch.setTimeout || !hostWithWatch.clearTimeout) {
    return;
  }
  if (state.timerToBuildInvalidatedProject) {
    hostWithWatch.clearTimeout(state.timerToBuildInvalidatedProject);
  }
  state.timerToBuildInvalidatedProject ***REMOVED*** hostWithWatch.setTimeout(buildNextInvalidatedProject, time, "timerToBuildInvalidatedProject", state, changeDetected);
}
function buildNextInvalidatedProject(_timeoutType, state, changeDetected) {
  mark("SolutionBuilder::beforeBuild");
  const buildOrder ***REMOVED*** buildNextInvalidatedProjectWorker(state, changeDetected);
  mark("SolutionBuilder::afterBuild");
  measure("SolutionBuilder::Build", "SolutionBuilder::beforeBuild", "SolutionBuilder::afterBuild");
  if (buildOrder) reportErrorSummary(state, buildOrder);
}
function buildNextInvalidatedProjectWorker(state, changeDetected) {
  state.timerToBuildInvalidatedProject ***REMOVED*** void 0;
  if (state.reportFileChangeDetected) {
    state.reportFileChangeDetected ***REMOVED*** false;
    state.projectErrorsReported.clear();
    reportWatchStatus(state, Diagnostics.File_change_detected_Starting_incremental_compilation);
  }
  let projectsBuilt ***REMOVED*** 0;
  const buildOrder ***REMOVED*** getBuildOrder(state);
  const invalidatedProject ***REMOVED*** getNextInvalidatedProject(
    state,
    buildOrder,
    /*reportQueue*/
    false
  );
  if (invalidatedProject) {
    invalidatedProject.done();
    projectsBuilt++;
    while (state.projectPendingBuild.size) {
      if (state.timerToBuildInvalidatedProject) return;
      const info ***REMOVED*** getNextInvalidatedProjectCreateInfo(
        state,
        buildOrder,
        /*reportQueue*/
        false
      );
      if (!info) break;
      if (info.kind !***REMOVED******REMOVED*** 1 /* UpdateOutputFileStamps */ && (changeDetected || projectsBuilt ***REMOVED******REMOVED******REMOVED*** 5)) {
        scheduleBuildInvalidatedProject(
          state,
          100,
          /*changeDetected*/
          false
        );
        return;
      }
      const project ***REMOVED*** createInvalidatedProjectWithInfo(state, info, buildOrder);
      project.done();
      if (info.kind !***REMOVED******REMOVED*** 1 /* UpdateOutputFileStamps */) projectsBuilt++;
    }
  }
  disableCache(state);
  return buildOrder;
}
function watchConfigFile(state, resolved, resolvedPath, parsed) {
  if (!state.watch || state.allWatchedConfigFiles.has(resolvedPath)) return;
  state.allWatchedConfigFiles.set(
    resolvedPath,
    watchFile(
      state,
      resolved,
      () ***REMOVED***> invalidateProjectAndScheduleBuilds(state, resolvedPath, 2 /* Full */),
      2e3 /* High */,
      parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.watchOptions,
      WatchType.ConfigFile,
      resolved
    )
  );
}
function watchExtendedConfigFiles(state, resolvedPath, parsed) {
  updateSharedExtendedConfigFileWatcher(
    resolvedPath,
    parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.options,
    state.allWatchedExtendedConfigFiles,
    (extendedConfigFileName, extendedConfigFilePath) ***REMOVED***> watchFile(
      state,
      extendedConfigFileName,
      () ***REMOVED***> {
        var _a;
        return (_a ***REMOVED*** state.allWatchedExtendedConfigFiles.get(extendedConfigFilePath)) ***REMOVED******REMOVED*** null ? void 0 : _a.projects.forEach((projectConfigFilePath) ***REMOVED***> invalidateProjectAndScheduleBuilds(state, projectConfigFilePath, 2 /* Full */));
      },
      2e3 /* High */,
      parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.watchOptions,
      WatchType.ExtendedConfigFile
    ),
    (fileName) ***REMOVED***> toPath2(state, fileName)
  );
}
function watchWildCardDirectories(state, resolved, resolvedPath, parsed) {
  if (!state.watch) return;
  updateWatchingWildcardDirectories(
    getOrCreateValueMapFromConfigFileMap(state.allWatchedWildcardDirectories, resolvedPath),
    parsed.wildcardDirectories,
    (dir, flags) ***REMOVED***> state.watchDirectory(
      dir,
      (fileOrDirectory) ***REMOVED***> {
        var _a;
        if (isIgnoredFileFromWildCardWatching({
          watchedDirPath: toPath2(state, dir),
          fileOrDirectory,
          fileOrDirectoryPath: toPath2(state, fileOrDirectory),
          configFileName: resolved,
          currentDirectory: state.compilerHost.getCurrentDirectory(),
          options: parsed.options,
          program: state.builderPrograms.get(resolvedPath) || ((_a ***REMOVED*** getCachedParsedConfigFile(state, resolvedPath)) ***REMOVED******REMOVED*** null ? void 0 : _a.fileNames),
          useCaseSensitiveFileNames: state.parseConfigFileHost.useCaseSensitiveFileNames,
          writeLog: (s) ***REMOVED***> state.writeLog(s),
          toPath: (fileName) ***REMOVED***> toPath2(state, fileName)
        })) return;
        invalidateProjectAndScheduleBuilds(state, resolvedPath, 1 /* RootNamesAndUpdate */);
      },
      flags,
      parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.watchOptions,
      WatchType.WildcardDirectory,
      resolved
    )
  );
}
function watchInputFiles(state, resolved, resolvedPath, parsed) {
  if (!state.watch) return;
  mutateMap(
    getOrCreateValueMapFromConfigFileMap(state.allWatchedInputFiles, resolvedPath),
    new Set(parsed.fileNames),
    {
      createNewValue: (input) ***REMOVED***> watchFile(
        state,
        input,
        () ***REMOVED***> invalidateProjectAndScheduleBuilds(state, resolvedPath, 0 /* Update */),
        250 /* Low */,
        parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.watchOptions,
        WatchType.SourceFile,
        resolved
      ),
      onDeleteValue: closeFileWatcher
    }
  );
}
function watchPackageJsonFiles(state, resolved, resolvedPath, parsed) {
  if (!state.watch || !state.lastCachedPackageJsonLookups) return;
  mutateMap(
    getOrCreateValueMapFromConfigFileMap(state.allWatchedPackageJsonFiles, resolvedPath),
    state.lastCachedPackageJsonLookups.get(resolvedPath),
    {
      createNewValue: (input) ***REMOVED***> watchFile(
        state,
        input,
        () ***REMOVED***> invalidateProjectAndScheduleBuilds(state, resolvedPath, 0 /* Update */),
        2e3 /* High */,
        parsed ***REMOVED******REMOVED*** null ? void 0 : parsed.watchOptions,
        WatchType.PackageJson,
        resolved
      ),
      onDeleteValue: closeFileWatcher
    }
  );
}
function startWatching(state, buildOrder) {
  if (!state.watchAllProjectsPending) return;
  mark("SolutionBuilder::beforeWatcherCreation");
  state.watchAllProjectsPending ***REMOVED*** false;
  for (const resolved of getBuildOrderFromAnyBuildOrder(buildOrder)) {
    const resolvedPath ***REMOVED*** toResolvedConfigFilePath(state, resolved);
    const cfg ***REMOVED*** parseConfigFile(state, resolved, resolvedPath);
    watchConfigFile(state, resolved, resolvedPath, cfg);
    watchExtendedConfigFiles(state, resolvedPath, cfg);
    if (cfg) {
      watchWildCardDirectories(state, resolved, resolvedPath, cfg);
      watchInputFiles(state, resolved, resolvedPath, cfg);
      watchPackageJsonFiles(state, resolved, resolvedPath, cfg);
    }
  }
  mark("SolutionBuilder::afterWatcherCreation");
  measure("SolutionBuilder::Watcher creation", "SolutionBuilder::beforeWatcherCreation", "SolutionBuilder::afterWatcherCreation");
}
function stopWatching(state) {
  clearMap(state.allWatchedConfigFiles, closeFileWatcher);
  clearMap(state.allWatchedExtendedConfigFiles, closeFileWatcherOf);
  clearMap(state.allWatchedWildcardDirectories, (watchedWildcardDirectories) ***REMOVED***> clearMap(watchedWildcardDirectories, closeFileWatcherOf));
  clearMap(state.allWatchedInputFiles, (watchedWildcardDirectories) ***REMOVED***> clearMap(watchedWildcardDirectories, closeFileWatcher));
  clearMap(state.allWatchedPackageJsonFiles, (watchedPacageJsonFiles) ***REMOVED***> clearMap(watchedPacageJsonFiles, closeFileWatcher));
}
function createSolutionBuilderWorker(watch, hostOrHostWithWatch, rootNames, options, baseWatchOptions) {
  const state ***REMOVED*** createSolutionBuilderState(watch, hostOrHostWithWatch, rootNames, options, baseWatchOptions);
  return {
    build: (project, cancellationToken, writeFile2, getCustomTransformers) ***REMOVED***> build(state, project, cancellationToken, writeFile2, getCustomTransformers),
    clean: (project) ***REMOVED***> clean(state, project),
    buildReferences: (project, cancellationToken, writeFile2, getCustomTransformers) ***REMOVED***> build(
      state,
      project,
      cancellationToken,
      writeFile2,
      getCustomTransformers,
      /*onlyReferences*/
      true
    ),
    cleanReferences: (project) ***REMOVED***> clean(
      state,
      project,
      /*onlyReferences*/
      true
    ),
    getNextInvalidatedProject: (cancellationToken) ***REMOVED***> {
      setupInitialBuild(state, cancellationToken);
      return getNextInvalidatedProject(
        state,
        getBuildOrder(state),
        /*reportQueue*/
        false
      );
    },
    getBuildOrder: () ***REMOVED***> getBuildOrder(state),
    getUpToDateStatusOfProject: (project) ***REMOVED***> {
      const configFileName ***REMOVED*** resolveProjectName(state, project);
      const configFilePath ***REMOVED*** toResolvedConfigFilePath(state, configFileName);
      return getUpToDateStatus(state, parseConfigFile(state, configFileName, configFilePath), configFilePath);
    },
    invalidateProject: (configFilePath, updateLevel) ***REMOVED***> invalidateProject(state, configFilePath, updateLevel || 0 /* Update */),
    close: () ***REMOVED***> stopWatching(state)
  };
}
function relName(state, path) {
  return convertToRelativePath(path, state.compilerHost.getCurrentDirectory(), state.compilerHost.getCanonicalFileName);
}
function reportStatus(state, message, ...args) {
  state.host.reportSolutionBuilderStatus(createCompilerDiagnostic(message, ...args));
}
function reportWatchStatus(state, message, ...args) {
  var _a, _b;
  (_b ***REMOVED*** (_a ***REMOVED*** state.hostWithWatch).onWatchStatusChange) ***REMOVED******REMOVED*** null ? void 0 : _b.call(_a, createCompilerDiagnostic(message, ...args), state.host.getNewLine(), state.baseCompilerOptions);
}
function reportErrors({ host }, errors) {
  errors.forEach((err) ***REMOVED***> host.reportDiagnostic(err));
}
function reportAndStoreErrors(state, proj, errors) {
  reportErrors(state, errors);
  state.projectErrorsReported.set(proj, true);
  if (errors.length) {
    state.diagnostics.set(proj, errors);
  }
}
function reportParseConfigFileDiagnostic(state, proj) {
  reportAndStoreErrors(state, proj, [state.configFileCache.get(proj)]);
}
function reportErrorSummary(state, buildOrder) {
  if (!state.needsSummary) return;
  state.needsSummary ***REMOVED*** false;
  const canReportSummary ***REMOVED*** state.watch || !!state.host.reportErrorSummary;
  const { diagnostics } ***REMOVED*** state;
  let totalErrors ***REMOVED*** 0;
  let filesInError ***REMOVED*** [];
  if (isCircularBuildOrder(buildOrder)) {
    reportBuildQueue(state, buildOrder.buildOrder);
    reportErrors(state, buildOrder.circularDiagnostics);
    if (canReportSummary) totalErrors +***REMOVED*** getErrorCountForSummary(buildOrder.circularDiagnostics);
    if (canReportSummary) filesInError ***REMOVED*** [...filesInError, ...getFilesInErrorForSummary(buildOrder.circularDiagnostics)];
  } else {
    buildOrder.forEach((project) ***REMOVED***> {
      const projectPath ***REMOVED*** toResolvedConfigFilePath(state, project);
      if (!state.projectErrorsReported.has(projectPath)) {
        reportErrors(state, diagnostics.get(projectPath) || emptyArray);
      }
    });
    if (canReportSummary) diagnostics.forEach((singleProjectErrors) ***REMOVED***> totalErrors +***REMOVED*** getErrorCountForSummary(singleProjectErrors));
    if (canReportSummary) diagnostics.forEach((singleProjectErrors) ***REMOVED***> [...filesInError, ...getFilesInErrorForSummary(singleProjectErrors)]);
  }
  if (state.watch) {
    reportWatchStatus(state, getWatchErrorSummaryDiagnosticMessage(totalErrors), totalErrors);
  } else if (state.host.reportErrorSummary) {
    state.host.reportErrorSummary(totalErrors, filesInError);
  }
}
function reportBuildQueue(state, buildQueue) {
  if (state.options.verbose) {
    reportStatus(state, Diagnostics.Projects_in_this_build_Colon_0, buildQueue.map((s) ***REMOVED***> "\r\n    * " + relName(state, s)).join(""));
  }
}
function reportUpToDateStatus(state, configFileName, status) {
  switch (status.type) {
    case 5 /* OutOfDateWithSelf */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_output_1_is_older_than_input_2,
        relName(state, configFileName),
        relName(state, status.outOfDateOutputFileName),
        relName(state, status.newerInputFileName)
      );
    case 6 /* OutOfDateWithUpstream */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_output_1_is_older_than_input_2,
        relName(state, configFileName),
        relName(state, status.outOfDateOutputFileName),
        relName(state, status.newerProjectName)
      );
    case 3 /* OutputMissing */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_output_file_1_does_not_exist,
        relName(state, configFileName),
        relName(state, status.missingOutputFileName)
      );
    case 4 /* ErrorReadingFile */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_there_was_error_reading_file_1,
        relName(state, configFileName),
        relName(state, status.fileName)
      );
    case 7 /* OutOfDateBuildInfoWithPendingEmit */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_some_of_the_changes_were_not_emitted,
        relName(state, configFileName),
        relName(state, status.buildInfoFile)
      );
    case 8 /* OutOfDateBuildInfoWithErrors */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_program_needs_to_report_errors,
        relName(state, configFileName),
        relName(state, status.buildInfoFile)
      );
    case 9 /* OutOfDateOptions */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_buildinfo_file_1_indicates_there_is_change_in_compilerOptions,
        relName(state, configFileName),
        relName(state, status.buildInfoFile)
      );
    case 10 /* OutOfDateRoots */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_buildinfo_file_1_indicates_that_file_2_was_root_file_of_compilation_but_not_any_more,
        relName(state, configFileName),
        relName(state, status.buildInfoFile),
        relName(state, status.inputFile)
      );
    case 1 /* UpToDate */:
      if (status.newestInputFileTime !***REMOVED******REMOVED*** void 0) {
        return reportStatus(
          state,
          Diagnostics.Project_0_is_up_to_date_because_newest_input_1_is_older_than_output_2,
          relName(state, configFileName),
          relName(state, status.newestInputFileName || ""),
          relName(state, status.oldestOutputFileName || "")
        );
      }
      break;
    case 2 /* UpToDateWithUpstreamTypes */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_up_to_date_with_d_ts_files_from_its_dependencies,
        relName(state, configFileName)
      );
    case 15 /* UpToDateWithInputFileText */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_up_to_date_but_needs_to_update_timestamps_of_output_files_that_are_older_than_input_files,
        relName(state, configFileName)
      );
    case 11 /* UpstreamOutOfDate */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_its_dependency_1_is_out_of_date,
        relName(state, configFileName),
        relName(state, status.upstreamProjectName)
      );
    case 12 /* UpstreamBlocked */:
      return reportStatus(
        state,
        status.upstreamProjectBlocked ? Diagnostics.Project_0_can_t_be_built_because_its_dependency_1_was_not_built : Diagnostics.Project_0_can_t_be_built_because_its_dependency_1_has_errors,
        relName(state, configFileName),
        relName(state, status.upstreamProjectName)
      );
    case 0 /* Unbuildable */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_1,
        relName(state, configFileName),
        status.reason
      );
    case 14 /* TsVersionOutputOfDate */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_out_of_date_because_output_for_it_was_generated_with_version_1_that_differs_with_current_version_2,
        relName(state, configFileName),
        status.version,
        version
      );
    case 17 /* ForceBuild */:
      return reportStatus(
        state,
        Diagnostics.Project_0_is_being_forcibly_rebuilt,
        relName(state, configFileName)
      );
    case 16 /* ContainerOnly */:
    // Don't report status on "solution" projects
    // falls through
    case 13 /* ComputingUpstream */:
      break;
    default:
      assertType(status);
  }
}
function verboseReportProjectStatus(state, configFileName, status) {
  if (state.options.verbose) {
    reportUpToDateStatus(state, configFileName, status);
  }
}

// src/compiler/executeCommandLine.ts
function countLines(program) {
  const counts2 ***REMOVED*** getCountsMap();
  forEach(program.getSourceFiles(), (file) ***REMOVED***> {
    const key ***REMOVED*** getCountKey(program, file);
    const lineCount ***REMOVED*** getLineStarts(file).length;
    counts2.set(key, counts2.get(key) + lineCount);
  });
  return counts2;
}
function getCountsMap() {
  const counts2 ***REMOVED*** /* @__PURE__ */ new Map();
  counts2.set("Library", 0);
  counts2.set("Definitions", 0);
  counts2.set("TypeScript", 0);
  counts2.set("JavaScript", 0);
  counts2.set("JSON", 0);
  counts2.set("Other", 0);
  return counts2;
}
function getCountKey(program, file) {
  if (program.isSourceFileDefaultLibrary(file)) {
    return "Library";
  } else if (file.isDeclarationFile) {
    return "Definitions";
  }
  const path ***REMOVED*** file.path;
  if (fileExtensionIsOneOf(path, supportedTSExtensionsFlat)) {
    return "TypeScript";
  } else if (fileExtensionIsOneOf(path, supportedJSExtensionsFlat)) {
    return "JavaScript";
  } else if (fileExtensionIs(path, ".json" /* Json */)) {
    return "JSON";
  } else {
    return "Other";
  }
}
function updateReportDiagnostic(sys2, existing, options) {
  return shouldBePretty(sys2, options) ? createDiagnosticReporter(
    sys2,
    /*pretty*/
    true
  ) : existing;
}
function defaultIsPretty(sys2) {
  return !!sys2.writeOutputIsTTY && sys2.writeOutputIsTTY() && !sys2.getEnvironmentVariable("NO_COLOR");
}
function shouldBePretty(sys2, options) {
  if (!options || typeof options.pretty ***REMOVED******REMOVED******REMOVED*** "undefined") {
    return defaultIsPretty(sys2);
  }
  return options.pretty;
}
function getOptionsForHelp(commandLine) {
  return !!commandLine.options.all ? toSorted(optionDeclarations.concat(tscBuildOption), (a, b) ***REMOVED***> compareStringsCaseInsensitive(a.name, b.name)) : filter(optionDeclarations.concat(tscBuildOption), (v) ***REMOVED***> !!v.showInSimplifiedHelpView);
}
function printVersion(sys2) {
  sys2.write(getDiagnosticText(Diagnostics.Version_0, version) + sys2.newLine);
}
function createColors(sys2) {
  const showColors ***REMOVED*** defaultIsPretty(sys2);
  if (!showColors) {
    return {
      bold: (str) ***REMOVED***> str,
      blue: (str) ***REMOVED***> str,
      blueBackground: (str) ***REMOVED***> str,
      brightWhite: (str) ***REMOVED***> str
    };
  }
  function bold(str) {
    return `\x1B[1m${str}\x1B[22m`;
  }
  const isWindows ***REMOVED*** sys2.getEnvironmentVariable("OS") && sys2.getEnvironmentVariable("OS").toLowerCase().includes("windows");
  const isWindowsTerminal ***REMOVED*** sys2.getEnvironmentVariable("WT_SESSION");
  const isVSCode ***REMOVED*** sys2.getEnvironmentVariable("TERM_PROGRAM") && sys2.getEnvironmentVariable("TERM_PROGRAM") ***REMOVED******REMOVED******REMOVED*** "vscode";
  function blue(str) {
    if (isWindows && !isWindowsTerminal && !isVSCode) {
      return brightWhite(str);
    }
    return `\x1B[94m${str}\x1B[39m`;
  }
  const supportsRicherColors ***REMOVED*** sys2.getEnvironmentVariable("COLORTERM") ***REMOVED******REMOVED******REMOVED*** "truecolor" || sys2.getEnvironmentVariable("TERM") ***REMOVED******REMOVED******REMOVED*** "xterm-256color";
  function blueBackground(str) {
    if (supportsRicherColors) {
      return `\x1B[48;5;68m${str}\x1B[39;49m`;
    } else {
      return `\x1B[44m${str}\x1B[39;49m`;
    }
  }
  function brightWhite(str) {
    return `\x1B[97m${str}\x1B[39m`;
  }
  return {
    bold,
    blue,
    brightWhite,
    blueBackground
  };
}
function getDisplayNameTextOfOption(option) {
  return `--${option.name}${option.shortName ? `, -${option.shortName}` : ""}`;
}
function generateOptionOutput(sys2, option, rightAlignOfLeft, leftAlignOfRight) {
  var _a;
  const text ***REMOVED*** [];
  const colors ***REMOVED*** createColors(sys2);
  const name ***REMOVED*** getDisplayNameTextOfOption(option);
  const valueCandidates ***REMOVED*** getValueCandidate(option);
  const defaultValueDescription ***REMOVED*** typeof option.defaultValueDescription ***REMOVED******REMOVED******REMOVED*** "object" ? getDiagnosticText(option.defaultValueDescription) : formatDefaultValue(
    option.defaultValueDescription,
    option.type ***REMOVED******REMOVED******REMOVED*** "list" || option.type ***REMOVED******REMOVED******REMOVED*** "listOrElement" ? option.element.type : option.type
  );
  const terminalWidth ***REMOVED*** ((_a ***REMOVED*** sys2.getWidthOfTerminal) ***REMOVED******REMOVED*** null ? void 0 : _a.call(sys2)) ?? 0;
  if (terminalWidth >***REMOVED*** 80) {
    let description ***REMOVED*** "";
    if (option.description) {
      description ***REMOVED*** getDiagnosticText(option.description);
    }
    text.push(...getPrettyOutput(
      name,
      description,
      rightAlignOfLeft,
      leftAlignOfRight,
      terminalWidth,
      /*colorLeft*/
      true
    ), sys2.newLine);
    if (showAdditionalInfoOutput(valueCandidates, option)) {
      if (valueCandidates) {
        text.push(...getPrettyOutput(
          valueCandidates.valueType,
          valueCandidates.possibleValues,
          rightAlignOfLeft,
          leftAlignOfRight,
          terminalWidth,
          /*colorLeft*/
          false
        ), sys2.newLine);
      }
      if (defaultValueDescription) {
        text.push(...getPrettyOutput(
          getDiagnosticText(Diagnostics.default_Colon),
          defaultValueDescription,
          rightAlignOfLeft,
          leftAlignOfRight,
          terminalWidth,
          /*colorLeft*/
          false
        ), sys2.newLine);
      }
    }
    text.push(sys2.newLine);
  } else {
    text.push(colors.blue(name), sys2.newLine);
    if (option.description) {
      const description ***REMOVED*** getDiagnosticText(option.description);
      text.push(description);
    }
    text.push(sys2.newLine);
    if (showAdditionalInfoOutput(valueCandidates, option)) {
      if (valueCandidates) {
        text.push(`${valueCandidates.valueType} ${valueCandidates.possibleValues}`);
      }
      if (defaultValueDescription) {
        if (valueCandidates) text.push(sys2.newLine);
        const diagType ***REMOVED*** getDiagnosticText(Diagnostics.default_Colon);
        text.push(`${diagType} ${defaultValueDescription}`);
      }
      text.push(sys2.newLine);
    }
    text.push(sys2.newLine);
  }
  return text;
  function formatDefaultValue(defaultValue, type) {
    return defaultValue !***REMOVED******REMOVED*** void 0 && typeof type ***REMOVED******REMOVED******REMOVED*** "object" ? arrayFrom(type.entries()).filter(([, value]) ***REMOVED***> value ***REMOVED******REMOVED******REMOVED*** defaultValue).map(([name2]) ***REMOVED***> name2).join("/") : String(defaultValue);
  }
  function showAdditionalInfoOutput(valueCandidates2, option2) {
    const ignoreValues ***REMOVED*** ["string"];
    const ignoredDescriptions ***REMOVED*** [void 0, "false", "n/a"];
    const defaultValueDescription2 ***REMOVED*** option2.defaultValueDescription;
    if (option2.category ***REMOVED******REMOVED******REMOVED*** Diagnostics.Command_line_Options) return false;
    if (contains(ignoreValues, valueCandidates2 ***REMOVED******REMOVED*** null ? void 0 : valueCandidates2.possibleValues) && contains(ignoredDescriptions, defaultValueDescription2)) {
      return false;
    }
    return true;
  }
  function getPrettyOutput(left, right, rightAlignOfLeft2, leftAlignOfRight2, terminalWidth2, colorLeft) {
    const res ***REMOVED*** [];
    let isFirstLine ***REMOVED*** true;
    let remainRight ***REMOVED*** right;
    const rightCharacterNumber ***REMOVED*** terminalWidth2 - leftAlignOfRight2;
    while (remainRight.length > 0) {
      let curLeft ***REMOVED*** "";
      if (isFirstLine) {
        curLeft ***REMOVED*** left.padStart(rightAlignOfLeft2);
        curLeft ***REMOVED*** curLeft.padEnd(leftAlignOfRight2);
        curLeft ***REMOVED*** colorLeft ? colors.blue(curLeft) : curLeft;
      } else {
        curLeft ***REMOVED*** "".padStart(leftAlignOfRight2);
      }
      const curRight ***REMOVED*** remainRight.substr(0, rightCharacterNumber);
      remainRight ***REMOVED*** remainRight.slice(rightCharacterNumber);
      res.push(`${curLeft}${curRight}`);
      isFirstLine ***REMOVED*** false;
    }
    return res;
  }
  function getValueCandidate(option2) {
    if (option2.type ***REMOVED******REMOVED******REMOVED*** "object") {
      return void 0;
    }
    return {
      valueType: getValueType(option2),
      possibleValues: getPossibleValues(option2)
    };
    function getValueType(option3) {
      Debug.assert(option3.type !***REMOVED******REMOVED*** "listOrElement");
      switch (option3.type) {
        case "string":
        case "number":
        case "boolean":
          return getDiagnosticText(Diagnostics.type_Colon);
        case "list":
          return getDiagnosticText(Diagnostics.one_or_more_Colon);
        default:
          return getDiagnosticText(Diagnostics.one_of_Colon);
      }
    }
    function getPossibleValues(option3) {
      let possibleValues;
      switch (option3.type) {
        case "string":
        case "number":
        case "boolean":
          possibleValues ***REMOVED*** option3.type;
          break;
        case "list":
        case "listOrElement":
          possibleValues ***REMOVED*** getPossibleValues(option3.element);
          break;
        case "object":
          possibleValues ***REMOVED*** "";
          break;
        default:
          const inverted ***REMOVED*** {};
          option3.type.forEach((value, name2) ***REMOVED***> {
            var _a2;
            if (!((_a2 ***REMOVED*** option3.deprecatedKeys) ***REMOVED******REMOVED*** null ? void 0 : _a2.has(name2))) {
              (inverted[value] || (inverted[value] ***REMOVED*** [])).push(name2);
            }
          });
          return Object.entries(inverted).map(([, synonyms]) ***REMOVED***> synonyms.join("/")).join(", ");
      }
      return possibleValues;
    }
  }
}
function generateGroupOptionOutput(sys2, optionsList) {
  let maxLength ***REMOVED*** 0;
  for (const option of optionsList) {
    const curLength ***REMOVED*** getDisplayNameTextOfOption(option).length;
    maxLength ***REMOVED*** maxLength > curLength ? maxLength : curLength;
  }
  const rightAlignOfLeftPart ***REMOVED*** maxLength + 2;
  const leftAlignOfRightPart ***REMOVED*** rightAlignOfLeftPart + 2;
  let lines ***REMOVED*** [];
  for (const option of optionsList) {
    const tmp ***REMOVED*** generateOptionOutput(sys2, option, rightAlignOfLeftPart, leftAlignOfRightPart);
    lines ***REMOVED*** [...lines, ...tmp];
  }
  if (lines[lines.length - 2] !***REMOVED******REMOVED*** sys2.newLine) {
    lines.push(sys2.newLine);
  }
  return lines;
}
function generateSectionOptionsOutput(sys2, sectionName, options, subCategory, beforeOptionsDescription, afterOptionsDescription) {
  let res ***REMOVED*** [];
  res.push(createColors(sys2).bold(sectionName) + sys2.newLine + sys2.newLine);
  if (beforeOptionsDescription) {
    res.push(beforeOptionsDescription + sys2.newLine + sys2.newLine);
  }
  if (!subCategory) {
    res ***REMOVED*** [...res, ...generateGroupOptionOutput(sys2, options)];
    if (afterOptionsDescription) {
      res.push(afterOptionsDescription + sys2.newLine + sys2.newLine);
    }
    return res;
  }
  const categoryMap ***REMOVED*** /* @__PURE__ */ new Map();
  for (const option of options) {
    if (!option.category) {
      continue;
    }
    const curCategory ***REMOVED*** getDiagnosticText(option.category);
    const optionsOfCurCategory ***REMOVED*** categoryMap.get(curCategory) ?? [];
    optionsOfCurCategory.push(option);
    categoryMap.set(curCategory, optionsOfCurCategory);
  }
  categoryMap.forEach((value, key) ***REMOVED***> {
    res.push(`### ${key}${sys2.newLine}${sys2.newLine}`);
    res ***REMOVED*** [...res, ...generateGroupOptionOutput(sys2, value)];
  });
  if (afterOptionsDescription) {
    res.push(afterOptionsDescription + sys2.newLine + sys2.newLine);
  }
  return res;
}
function printEasyHelp(sys2, simpleOptions) {
  const colors ***REMOVED*** createColors(sys2);
  let output ***REMOVED*** [...getHeader(sys2, `${getDiagnosticText(Diagnostics.tsc_Colon_The_TypeScript_Compiler)} - ${getDiagnosticText(Diagnostics.Version_0, version)}`)];
  output.push(colors.bold(getDiagnosticText(Diagnostics.COMMON_COMMANDS)) + sys2.newLine + sys2.newLine);
  example("tsc", Diagnostics.Compiles_the_current_project_tsconfig_json_in_the_working_directory);
  example("tsc app.ts util.ts", Diagnostics.Ignoring_tsconfig_json_compiles_the_specified_files_with_default_compiler_options);
  example("tsc -b", Diagnostics.Build_a_composite_project_in_the_working_directory);
  example("tsc --init", Diagnostics.Creates_a_tsconfig_json_with_the_recommended_settings_in_the_working_directory);
  example("tsc -p ./path/to/tsconfig.json", Diagnostics.Compiles_the_TypeScript_project_located_at_the_specified_path);
  example("tsc --help --all", Diagnostics.An_expanded_version_of_this_information_showing_all_possible_compiler_options);
  example(["tsc --noEmit", "tsc --target esnext"], Diagnostics.Compiles_the_current_project_with_additional_settings);
  const cliCommands ***REMOVED*** simpleOptions.filter((opt) ***REMOVED***> opt.isCommandLineOnly || opt.category ***REMOVED******REMOVED******REMOVED*** Diagnostics.Command_line_Options);
  const configOpts ***REMOVED*** simpleOptions.filter((opt) ***REMOVED***> !contains(cliCommands, opt));
  output ***REMOVED*** [
    ...output,
    ...generateSectionOptionsOutput(
      sys2,
      getDiagnosticText(Diagnostics.COMMAND_LINE_FLAGS),
      cliCommands,
      /*subCategory*/
      false,
      /*beforeOptionsDescription*/
      void 0,
      /*afterOptionsDescription*/
      void 0
    ),
    ...generateSectionOptionsOutput(
      sys2,
      getDiagnosticText(Diagnostics.COMMON_COMPILER_OPTIONS),
      configOpts,
      /*subCategory*/
      false,
      /*beforeOptionsDescription*/
      void 0,
      formatMessage(Diagnostics.You_can_learn_about_all_of_the_compiler_options_at_0, "https://aka.ms/tsc")
    )
  ];
  for (const line of output) {
    sys2.write(line);
  }
  function example(ex, desc) {
    const examples ***REMOVED*** typeof ex ***REMOVED******REMOVED******REMOVED*** "string" ? [ex] : ex;
    for (const example2 of examples) {
      output.push("  " + colors.blue(example2) + sys2.newLine);
    }
    output.push("  " + getDiagnosticText(desc) + sys2.newLine + sys2.newLine);
  }
}
function printAllHelp(sys2, compilerOptions, buildOptions, watchOptions) {
  let output ***REMOVED*** [...getHeader(sys2, `${getDiagnosticText(Diagnostics.tsc_Colon_The_TypeScript_Compiler)} - ${getDiagnosticText(Diagnostics.Version_0, version)}`)];
  output ***REMOVED*** [...output, ...generateSectionOptionsOutput(
    sys2,
    getDiagnosticText(Diagnostics.ALL_COMPILER_OPTIONS),
    compilerOptions,
    /*subCategory*/
    true,
    /*beforeOptionsDescription*/
    void 0,
    formatMessage(Diagnostics.You_can_learn_about_all_of_the_compiler_options_at_0, "https://aka.ms/tsc")
  )];
  output ***REMOVED*** [...output, ...generateSectionOptionsOutput(
    sys2,
    getDiagnosticText(Diagnostics.WATCH_OPTIONS),
    watchOptions,
    /*subCategory*/
    false,
    getDiagnosticText(Diagnostics.Including_watch_w_will_start_watching_the_current_project_for_the_file_changes_Once_set_you_can_config_watch_mode_with_Colon)
  )];
  output ***REMOVED*** [...output, ...generateSectionOptionsOutput(
    sys2,
    getDiagnosticText(Diagnostics.BUILD_OPTIONS),
    filter(buildOptions, (option) ***REMOVED***> option !***REMOVED******REMOVED*** tscBuildOption),
    /*subCategory*/
    false,
    formatMessage(Diagnostics.Using_build_b_will_make_tsc_behave_more_like_a_build_orchestrator_than_a_compiler_This_is_used_to_trigger_building_composite_projects_which_you_can_learn_more_about_at_0, "https://aka.ms/tsc-composite-builds")
  )];
  for (const line of output) {
    sys2.write(line);
  }
}
function printBuildHelp(sys2, buildOptions) {
  let output ***REMOVED*** [...getHeader(sys2, `${getDiagnosticText(Diagnostics.tsc_Colon_The_TypeScript_Compiler)} - ${getDiagnosticText(Diagnostics.Version_0, version)}`)];
  output ***REMOVED*** [...output, ...generateSectionOptionsOutput(
    sys2,
    getDiagnosticText(Diagnostics.BUILD_OPTIONS),
    filter(buildOptions, (option) ***REMOVED***> option !***REMOVED******REMOVED*** tscBuildOption),
    /*subCategory*/
    false,
    formatMessage(Diagnostics.Using_build_b_will_make_tsc_behave_more_like_a_build_orchestrator_than_a_compiler_This_is_used_to_trigger_building_composite_projects_which_you_can_learn_more_about_at_0, "https://aka.ms/tsc-composite-builds")
  )];
  for (const line of output) {
    sys2.write(line);
  }
}
function getHeader(sys2, message) {
  var _a;
  const colors ***REMOVED*** createColors(sys2);
  const header ***REMOVED*** [];
  const terminalWidth ***REMOVED*** ((_a ***REMOVED*** sys2.getWidthOfTerminal) ***REMOVED******REMOVED*** null ? void 0 : _a.call(sys2)) ?? 0;
  const tsIconLength ***REMOVED*** 5;
  const tsIconFirstLine ***REMOVED*** colors.blueBackground("".padStart(tsIconLength));
  const tsIconSecondLine ***REMOVED*** colors.blueBackground(colors.brightWhite("TS ".padStart(tsIconLength)));
  if (terminalWidth >***REMOVED*** message.length + tsIconLength) {
    const rightAlign ***REMOVED*** terminalWidth > 120 ? 120 : terminalWidth;
    const leftAlign ***REMOVED*** rightAlign - tsIconLength;
    header.push(message.padEnd(leftAlign) + tsIconFirstLine + sys2.newLine);
    header.push("".padStart(leftAlign) + tsIconSecondLine + sys2.newLine);
  } else {
    header.push(message + sys2.newLine);
    header.push(sys2.newLine);
  }
  return header;
}
function printHelp(sys2, commandLine) {
  if (!commandLine.options.all) {
    printEasyHelp(sys2, getOptionsForHelp(commandLine));
  } else {
    printAllHelp(sys2, getOptionsForHelp(commandLine), optionsForBuild, optionsForWatch);
  }
}
function executeCommandLineWorker(sys2, cb, commandLine) {
  let reportDiagnostic ***REMOVED*** createDiagnosticReporter(sys2);
  let configFileName;
  if (commandLine.options.locale) {
    validateLocaleAndSetLanguage(commandLine.options.locale, sys2, commandLine.errors);
  }
  if (commandLine.errors.length > 0) {
    commandLine.errors.forEach(reportDiagnostic);
    return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
  }
  if (commandLine.options.init) {
    writeConfigFile(sys2, reportDiagnostic, commandLine.options);
    return sys2.exit(0 /* Success */);
  }
  if (commandLine.options.version) {
    printVersion(sys2);
    return sys2.exit(0 /* Success */);
  }
  if (commandLine.options.help || commandLine.options.all) {
    printHelp(sys2, commandLine);
    return sys2.exit(0 /* Success */);
  }
  if (commandLine.options.watch && commandLine.options.listFilesOnly) {
    reportDiagnostic(createCompilerDiagnostic(Diagnostics.Options_0_and_1_cannot_be_combined, "watch", "listFilesOnly"));
    return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
  }
  if (commandLine.options.project) {
    if (commandLine.fileNames.length !***REMOVED******REMOVED*** 0) {
      reportDiagnostic(createCompilerDiagnostic(Diagnostics.Option_project_cannot_be_mixed_with_source_files_on_a_command_line));
      return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
    }
    const fileOrDirectory ***REMOVED*** normalizePath(commandLine.options.project);
    if (!fileOrDirectory || sys2.directoryExists(fileOrDirectory)) {
      configFileName ***REMOVED*** combinePaths(fileOrDirectory, "tsconfig.json");
      if (!sys2.fileExists(configFileName)) {
        reportDiagnostic(createCompilerDiagnostic(Diagnostics.Cannot_find_a_tsconfig_json_file_at_the_specified_directory_Colon_0, commandLine.options.project));
        return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
      }
    } else {
      configFileName ***REMOVED*** fileOrDirectory;
      if (!sys2.fileExists(configFileName)) {
        reportDiagnostic(createCompilerDiagnostic(Diagnostics.The_specified_path_does_not_exist_Colon_0, commandLine.options.project));
        return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
      }
    }
  } else if (commandLine.fileNames.length ***REMOVED******REMOVED******REMOVED*** 0) {
    const searchPath ***REMOVED*** normalizePath(sys2.getCurrentDirectory());
    configFileName ***REMOVED*** findConfigFile(searchPath, (fileName) ***REMOVED***> sys2.fileExists(fileName));
  }
  if (commandLine.fileNames.length ***REMOVED******REMOVED******REMOVED*** 0 && !configFileName) {
    if (commandLine.options.showConfig) {
      reportDiagnostic(createCompilerDiagnostic(Diagnostics.Cannot_find_a_tsconfig_json_file_at_the_current_directory_Colon_0, normalizePath(sys2.getCurrentDirectory())));
    } else {
      printVersion(sys2);
      printHelp(sys2, commandLine);
    }
    return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
  }
  const currentDirectory ***REMOVED*** sys2.getCurrentDirectory();
  const commandLineOptions ***REMOVED*** convertToOptionsWithAbsolutePaths(
    commandLine.options,
    (fileName) ***REMOVED***> getNormalizedAbsolutePath(fileName, currentDirectory)
  );
  if (configFileName) {
    const extendedConfigCache ***REMOVED*** /* @__PURE__ */ new Map();
    const configParseResult ***REMOVED*** parseConfigFileWithSystem(configFileName, commandLineOptions, extendedConfigCache, commandLine.watchOptions, sys2, reportDiagnostic);
    if (commandLineOptions.showConfig) {
      if (configParseResult.errors.length !***REMOVED******REMOVED*** 0) {
        reportDiagnostic ***REMOVED*** updateReportDiagnostic(
          sys2,
          reportDiagnostic,
          configParseResult.options
        );
        configParseResult.errors.forEach(reportDiagnostic);
        return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
      }
      sys2.write(JSON.stringify(convertToTSConfig(configParseResult, configFileName, sys2), null, 4) + sys2.newLine);
      return sys2.exit(0 /* Success */);
    }
    reportDiagnostic ***REMOVED*** updateReportDiagnostic(
      sys2,
      reportDiagnostic,
      configParseResult.options
    );
    if (isWatchSet(configParseResult.options)) {
      if (reportWatchModeWithoutSysSupport(sys2, reportDiagnostic)) return;
      return createWatchOfConfigFile(
        sys2,
        cb,
        reportDiagnostic,
        configParseResult,
        commandLineOptions,
        commandLine.watchOptions,
        extendedConfigCache
      );
    } else if (isIncrementalCompilation(configParseResult.options)) {
      performIncrementalCompilation2(
        sys2,
        cb,
        reportDiagnostic,
        configParseResult
      );
    } else {
      performCompilation(
        sys2,
        cb,
        reportDiagnostic,
        configParseResult
      );
    }
  } else {
    if (commandLineOptions.showConfig) {
      sys2.write(JSON.stringify(convertToTSConfig(commandLine, combinePaths(currentDirectory, "tsconfig.json"), sys2), null, 4) + sys2.newLine);
      return sys2.exit(0 /* Success */);
    }
    reportDiagnostic ***REMOVED*** updateReportDiagnostic(
      sys2,
      reportDiagnostic,
      commandLineOptions
    );
    if (isWatchSet(commandLineOptions)) {
      if (reportWatchModeWithoutSysSupport(sys2, reportDiagnostic)) return;
      return createWatchOfFilesAndCompilerOptions(
        sys2,
        cb,
        reportDiagnostic,
        commandLine.fileNames,
        commandLineOptions,
        commandLine.watchOptions
      );
    } else if (isIncrementalCompilation(commandLineOptions)) {
      performIncrementalCompilation2(
        sys2,
        cb,
        reportDiagnostic,
        { ...commandLine, options: commandLineOptions }
      );
    } else {
      performCompilation(
        sys2,
        cb,
        reportDiagnostic,
        { ...commandLine, options: commandLineOptions }
      );
    }
  }
}
function isBuildCommand(commandLineArgs) {
  if (commandLineArgs.length > 0 && commandLineArgs[0].charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */) {
    const firstOption ***REMOVED*** commandLineArgs[0].slice(commandLineArgs[0].charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** 45 /* minus */ ? 2 : 1).toLowerCase();
    return firstOption ***REMOVED******REMOVED******REMOVED*** tscBuildOption.name || firstOption ***REMOVED******REMOVED******REMOVED*** tscBuildOption.shortName;
  }
  return false;
}
function executeCommandLine(system, cb, commandLineArgs) {
  if (isBuildCommand(commandLineArgs)) {
    const { buildOptions, watchOptions, projects, errors } ***REMOVED*** parseBuildCommand(commandLineArgs);
    if (buildOptions.generateCpuProfile && system.enableCPUProfiler) {
      system.enableCPUProfiler(buildOptions.generateCpuProfile, () ***REMOVED***> performBuild(
        system,
        cb,
        buildOptions,
        watchOptions,
        projects,
        errors
      ));
    } else {
      return performBuild(
        system,
        cb,
        buildOptions,
        watchOptions,
        projects,
        errors
      );
    }
  }
  const commandLine ***REMOVED*** parseCommandLine(commandLineArgs, (path) ***REMOVED***> system.readFile(path));
  if (commandLine.options.generateCpuProfile && system.enableCPUProfiler) {
    system.enableCPUProfiler(commandLine.options.generateCpuProfile, () ***REMOVED***> executeCommandLineWorker(
      system,
      cb,
      commandLine
    ));
  } else {
    return executeCommandLineWorker(system, cb, commandLine);
  }
}
function reportWatchModeWithoutSysSupport(sys2, reportDiagnostic) {
  if (!sys2.watchFile || !sys2.watchDirectory) {
    reportDiagnostic(createCompilerDiagnostic(Diagnostics.The_current_host_does_not_support_the_0_option, "--watch"));
    sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
    return true;
  }
  return false;
}
var defaultJSDocParsingMode ***REMOVED*** 2 /* ParseForTypeErrors */;
function performBuild(sys2, cb, buildOptions, watchOptions, projects, errors) {
  const reportDiagnostic ***REMOVED*** updateReportDiagnostic(
    sys2,
    createDiagnosticReporter(sys2),
    buildOptions
  );
  if (buildOptions.locale) {
    validateLocaleAndSetLanguage(buildOptions.locale, sys2, errors);
  }
  if (errors.length > 0) {
    errors.forEach(reportDiagnostic);
    return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
  }
  if (buildOptions.help) {
    printVersion(sys2);
    printBuildHelp(sys2, buildOpts);
    return sys2.exit(0 /* Success */);
  }
  if (projects.length ***REMOVED******REMOVED******REMOVED*** 0) {
    printVersion(sys2);
    printBuildHelp(sys2, buildOpts);
    return sys2.exit(0 /* Success */);
  }
  if (!sys2.getModifiedTime || !sys2.setModifiedTime || buildOptions.clean && !sys2.deleteFile) {
    reportDiagnostic(createCompilerDiagnostic(Diagnostics.The_current_host_does_not_support_the_0_option, "--build"));
    return sys2.exit(1 /* DiagnosticsPresent_OutputsSkipped */);
  }
  if (buildOptions.watch) {
    if (reportWatchModeWithoutSysSupport(sys2, reportDiagnostic)) return;
    const buildHost2 ***REMOVED*** createSolutionBuilderWithWatchHost(
      sys2,
      /*createProgram*/
      void 0,
      reportDiagnostic,
      createBuilderStatusReporter(sys2, shouldBePretty(sys2, buildOptions)),
      createWatchStatusReporter2(sys2, buildOptions)
    );
    buildHost2.jsDocParsingMode ***REMOVED*** defaultJSDocParsingMode;
    const solutionPerformance2 ***REMOVED*** enableSolutionPerformance(sys2, buildOptions);
    updateSolutionBuilderHost(sys2, cb, buildHost2, solutionPerformance2);
    const onWatchStatusChange ***REMOVED*** buildHost2.onWatchStatusChange;
    let reportBuildStatistics ***REMOVED*** false;
    buildHost2.onWatchStatusChange ***REMOVED*** (d, newLine, options, errorCount) ***REMOVED***> {
      onWatchStatusChange ***REMOVED******REMOVED*** null ? void 0 : onWatchStatusChange(d, newLine, options, errorCount);
      if (reportBuildStatistics && (d.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Found_0_errors_Watching_for_file_changes.code || d.code ***REMOVED******REMOVED******REMOVED*** Diagnostics.Found_1_error_Watching_for_file_changes.code)) {
        reportSolutionBuilderTimes(builder2, solutionPerformance2);
      }
    };
    const builder2 ***REMOVED*** createSolutionBuilderWithWatch(buildHost2, projects, buildOptions, watchOptions);
    builder2.build();
    reportSolutionBuilderTimes(builder2, solutionPerformance2);
    reportBuildStatistics ***REMOVED*** true;
    return builder2;
  }
  const buildHost ***REMOVED*** createSolutionBuilderHost(
    sys2,
    /*createProgram*/
    void 0,
    reportDiagnostic,
    createBuilderStatusReporter(sys2, shouldBePretty(sys2, buildOptions)),
    createReportErrorSummary(sys2, buildOptions)
  );
  buildHost.jsDocParsingMode ***REMOVED*** defaultJSDocParsingMode;
  const solutionPerformance ***REMOVED*** enableSolutionPerformance(sys2, buildOptions);
  updateSolutionBuilderHost(sys2, cb, buildHost, solutionPerformance);
  const builder ***REMOVED*** createSolutionBuilder(buildHost, projects, buildOptions);
  const exitStatus ***REMOVED*** buildOptions.clean ? builder.clean() : builder.build();
  reportSolutionBuilderTimes(builder, solutionPerformance);
  dumpTracingLegend();
  return sys2.exit(exitStatus);
}
function createReportErrorSummary(sys2, options) {
  return shouldBePretty(sys2, options) ? (errorCount, filesInError) ***REMOVED***> sys2.write(getErrorSummaryText(errorCount, filesInError, sys2.newLine, sys2)) : void 0;
}
function performCompilation(sys2, cb, reportDiagnostic, config) {
  const { fileNames, options, projectReferences } ***REMOVED*** config;
  const host ***REMOVED*** createCompilerHostWorker(
    options,
    /*setParentNodes*/
    void 0,
    sys2
  );
  host.jsDocParsingMode ***REMOVED*** defaultJSDocParsingMode;
  const currentDirectory ***REMOVED*** host.getCurrentDirectory();
  const getCanonicalFileName ***REMOVED*** createGetCanonicalFileName(host.useCaseSensitiveFileNames());
  changeCompilerHostLikeToUseCache(host, (fileName) ***REMOVED***> toPath(fileName, currentDirectory, getCanonicalFileName));
  enableStatisticsAndTracing(
    sys2,
    options,
    /*isBuildMode*/
    false
  );
  const programOptions ***REMOVED*** {
    rootNames: fileNames,
    options,
    projectReferences,
    host,
    configFileParsingDiagnostics: getConfigFileParsingDiagnostics(config)
  };
  const program ***REMOVED*** createProgram(programOptions);
  const exitStatus ***REMOVED*** emitFilesAndReportErrorsAndGetExitStatus(
    program,
    reportDiagnostic,
    (s) ***REMOVED***> sys2.write(s + sys2.newLine),
    createReportErrorSummary(sys2, options)
  );
  reportStatistics(
    sys2,
    program,
    /*solutionPerformance*/
    void 0
  );
  cb(program);
  return sys2.exit(exitStatus);
}
function performIncrementalCompilation2(sys2, cb, reportDiagnostic, config) {
  const { options, fileNames, projectReferences } ***REMOVED*** config;
  enableStatisticsAndTracing(
    sys2,
    options,
    /*isBuildMode*/
    false
  );
  const host ***REMOVED*** createIncrementalCompilerHost(options, sys2);
  host.jsDocParsingMode ***REMOVED*** defaultJSDocParsingMode;
  const exitStatus ***REMOVED*** performIncrementalCompilation({
    host,
    system: sys2,
    rootNames: fileNames,
    options,
    configFileParsingDiagnostics: getConfigFileParsingDiagnostics(config),
    projectReferences,
    reportDiagnostic,
    reportErrorSummary: createReportErrorSummary(sys2, options),
    afterProgramEmitAndDiagnostics: (builderProgram) ***REMOVED***> {
      reportStatistics(
        sys2,
        builderProgram.getProgram(),
        /*solutionPerformance*/
        void 0
      );
      cb(builderProgram);
    }
  });
  return sys2.exit(exitStatus);
}
function updateSolutionBuilderHost(sys2, cb, buildHost, solutionPerformance) {
  updateCreateProgram(
    sys2,
    buildHost,
    /*isBuildMode*/
    true
  );
  buildHost.afterProgramEmitAndDiagnostics ***REMOVED*** (program) ***REMOVED***> {
    reportStatistics(sys2, program.getProgram(), solutionPerformance);
    cb(program);
  };
}
function updateCreateProgram(sys2, host, isBuildMode) {
  const compileUsingBuilder ***REMOVED*** host.createProgram;
  host.createProgram ***REMOVED*** (rootNames, options, host2, oldProgram, configFileParsingDiagnostics, projectReferences) ***REMOVED***> {
    Debug.assert(rootNames !***REMOVED******REMOVED*** void 0 || options ***REMOVED******REMOVED******REMOVED*** void 0 && !!oldProgram);
    if (options !***REMOVED******REMOVED*** void 0) {
      enableStatisticsAndTracing(sys2, options, isBuildMode);
    }
    return compileUsingBuilder(rootNames, options, host2, oldProgram, configFileParsingDiagnostics, projectReferences);
  };
}
function updateWatchCompilationHost(sys2, cb, watchCompilerHost) {
  watchCompilerHost.jsDocParsingMode ***REMOVED*** defaultJSDocParsingMode;
  updateCreateProgram(
    sys2,
    watchCompilerHost,
    /*isBuildMode*/
    false
  );
  const emitFilesUsingBuilder ***REMOVED*** watchCompilerHost.afterProgramCreate;
  watchCompilerHost.afterProgramCreate ***REMOVED*** (builderProgram) ***REMOVED***> {
    emitFilesUsingBuilder(builderProgram);
    reportStatistics(
      sys2,
      builderProgram.getProgram(),
      /*solutionPerformance*/
      void 0
    );
    cb(builderProgram);
  };
}
function createWatchStatusReporter2(sys2, options) {
  return createWatchStatusReporter(sys2, shouldBePretty(sys2, options));
}
function createWatchOfConfigFile(system, cb, reportDiagnostic, configParseResult, optionsToExtend, watchOptionsToExtend, extendedConfigCache) {
  const watchCompilerHost ***REMOVED*** createWatchCompilerHostOfConfigFile({
    configFileName: configParseResult.options.configFilePath,
    optionsToExtend,
    watchOptionsToExtend,
    system,
    reportDiagnostic,
    reportWatchStatus: createWatchStatusReporter2(system, configParseResult.options)
  });
  updateWatchCompilationHost(system, cb, watchCompilerHost);
  watchCompilerHost.configFileParsingResult ***REMOVED*** configParseResult;
  watchCompilerHost.extendedConfigCache ***REMOVED*** extendedConfigCache;
  return createWatchProgram(watchCompilerHost);
}
function createWatchOfFilesAndCompilerOptions(system, cb, reportDiagnostic, rootFiles, options, watchOptions) {
  const watchCompilerHost ***REMOVED*** createWatchCompilerHostOfFilesAndCompilerOptions({
    rootFiles,
    options,
    watchOptions,
    system,
    reportDiagnostic,
    reportWatchStatus: createWatchStatusReporter2(system, options)
  });
  updateWatchCompilationHost(system, cb, watchCompilerHost);
  return createWatchProgram(watchCompilerHost);
}
function enableSolutionPerformance(system, options) {
  if (system ***REMOVED******REMOVED******REMOVED*** sys && options.extendedDiagnostics) {
    enable();
    return createSolutionPerfomrance();
  }
}
function createSolutionPerfomrance() {
  let statistics;
  return {
    addAggregateStatistic,
    forEachAggregateStatistics: forEachAggreateStatistics,
    clear: clear2
  };
  function addAggregateStatistic(s) {
    const existing ***REMOVED*** statistics ***REMOVED******REMOVED*** null ? void 0 : statistics.get(s.name);
    if (existing) {
      if (existing.type ***REMOVED******REMOVED******REMOVED*** 2 /* memory */) existing.value ***REMOVED*** Math.max(existing.value, s.value);
      else existing.value +***REMOVED*** s.value;
    } else {
      (statistics ?? (statistics ***REMOVED*** /* @__PURE__ */ new Map())).set(s.name, s);
    }
  }
  function forEachAggreateStatistics(cb) {
    statistics ***REMOVED******REMOVED*** null ? void 0 : statistics.forEach(cb);
  }
  function clear2() {
    statistics ***REMOVED*** void 0;
  }
}
function reportSolutionBuilderTimes(builder, solutionPerformance) {
  if (!solutionPerformance) return;
  if (!isEnabled()) {
    sys.write(Diagnostics.Performance_timings_for_diagnostics_or_extendedDiagnostics_are_not_available_in_this_session_A_native_implementation_of_the_Web_Performance_API_could_not_be_found.message + "\n");
    return;
  }
  const statistics ***REMOVED*** [];
  statistics.push(
    { name: "Projects in scope", value: getBuildOrderFromAnyBuildOrder(builder.getBuildOrder()).length, type: 1 /* count */ }
  );
  reportSolutionBuilderCountStatistic("SolutionBuilder::Projects built");
  reportSolutionBuilderCountStatistic("SolutionBuilder::Timestamps only updates");
  reportSolutionBuilderCountStatistic("SolutionBuilder::Bundles updated");
  solutionPerformance.forEachAggregateStatistics((s) ***REMOVED***> {
    s.name ***REMOVED*** `Aggregate ${s.name}`;
    statistics.push(s);
  });
  forEachMeasure((name, duration) ***REMOVED***> {
    if (isSolutionMarkOrMeasure(name)) statistics.push({ name: `${getNameFromSolutionBuilderMarkOrMeasure(name)} time`, value: duration, type: 0 /* time */ });
  });
  disable();
  enable();
  solutionPerformance.clear();
  reportAllStatistics(sys, statistics);
  function reportSolutionBuilderCountStatistic(name) {
    const value ***REMOVED*** getCount(name);
    if (value) {
      statistics.push({ name: getNameFromSolutionBuilderMarkOrMeasure(name), value, type: 1 /* count */ });
    }
  }
  function getNameFromSolutionBuilderMarkOrMeasure(name) {
    return name.replace("SolutionBuilder::", "");
  }
}
function canReportDiagnostics(system, compilerOptions) {
  return system ***REMOVED******REMOVED******REMOVED*** sys && (compilerOptions.diagnostics || compilerOptions.extendedDiagnostics);
}
function canTrace(system, compilerOptions) {
  return system ***REMOVED******REMOVED******REMOVED*** sys && compilerOptions.generateTrace;
}
function enableStatisticsAndTracing(system, compilerOptions, isBuildMode) {
  if (canReportDiagnostics(system, compilerOptions)) {
    enable(system);
  }
  if (canTrace(system, compilerOptions)) {
    startTracing(isBuildMode ? "build" : "project", compilerOptions.generateTrace, compilerOptions.configFilePath);
  }
}
function isSolutionMarkOrMeasure(name) {
  return startsWith(name, "SolutionBuilder::");
}
function reportStatistics(sys2, program, solutionPerformance) {
  var _a;
  const compilerOptions ***REMOVED*** program.getCompilerOptions();
  if (canTrace(sys2, compilerOptions)) {
    (_a ***REMOVED*** tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.stopTracing();
  }
  let statistics;
  if (canReportDiagnostics(sys2, compilerOptions)) {
    statistics ***REMOVED*** [];
    const memoryUsed ***REMOVED*** sys2.getMemoryUsage ? sys2.getMemoryUsage() : -1;
    reportCountStatistic("Files", program.getSourceFiles().length);
    const lineCounts ***REMOVED*** countLines(program);
    if (compilerOptions.extendedDiagnostics) {
      for (const [key, value] of lineCounts.entries()) {
        reportCountStatistic("Lines of " + key, value);
      }
    } else {
      reportCountStatistic("Lines", reduceLeftIterator(lineCounts.values(), (sum, count) ***REMOVED***> sum + count, 0));
    }
    reportCountStatistic("Identifiers", program.getIdentifierCount());
    reportCountStatistic("Symbols", program.getSymbolCount());
    reportCountStatistic("Types", program.getTypeCount());
    reportCountStatistic("Instantiations", program.getInstantiationCount());
    if (memoryUsed >***REMOVED*** 0) {
      reportStatisticalValue(
        { name: "Memory used", value: memoryUsed, type: 2 /* memory */ },
        /*aggregate*/
        true
      );
    }
    const isPerformanceEnabled ***REMOVED*** isEnabled();
    const programTime ***REMOVED*** isPerformanceEnabled ? getDuration("Program") : 0;
    const bindTime ***REMOVED*** isPerformanceEnabled ? getDuration("Bind") : 0;
    const checkTime ***REMOVED*** isPerformanceEnabled ? getDuration("Check") : 0;
    const emitTime ***REMOVED*** isPerformanceEnabled ? getDuration("Emit") : 0;
    if (compilerOptions.extendedDiagnostics) {
      const caches ***REMOVED*** program.getRelationCacheSizes();
      reportCountStatistic("Assignability cache size", caches.assignable);
      reportCountStatistic("Identity cache size", caches.identity);
      reportCountStatistic("Subtype cache size", caches.subtype);
      reportCountStatistic("Strict subtype cache size", caches.strictSubtype);
      if (isPerformanceEnabled) {
        forEachMeasure((name, duration) ***REMOVED***> {
          if (!isSolutionMarkOrMeasure(name)) reportTimeStatistic(
            `${name} time`,
            duration,
            /*aggregate*/
            true
          );
        });
      }
    } else if (isPerformanceEnabled) {
      reportTimeStatistic(
        "I/O read",
        getDuration("I/O Read"),
        /*aggregate*/
        true
      );
      reportTimeStatistic(
        "I/O write",
        getDuration("I/O Write"),
        /*aggregate*/
        true
      );
      reportTimeStatistic(
        "Parse time",
        programTime,
        /*aggregate*/
        true
      );
      reportTimeStatistic(
        "Bind time",
        bindTime,
        /*aggregate*/
        true
      );
      reportTimeStatistic(
        "Check time",
        checkTime,
        /*aggregate*/
        true
      );
      reportTimeStatistic(
        "Emit time",
        emitTime,
        /*aggregate*/
        true
      );
    }
    if (isPerformanceEnabled) {
      reportTimeStatistic(
        "Total time",
        programTime + bindTime + checkTime + emitTime,
        /*aggregate*/
        false
      );
    }
    reportAllStatistics(sys2, statistics);
    if (!isPerformanceEnabled) {
      sys2.write(Diagnostics.Performance_timings_for_diagnostics_or_extendedDiagnostics_are_not_available_in_this_session_A_native_implementation_of_the_Web_Performance_API_could_not_be_found.message + "\n");
    } else {
      if (solutionPerformance) {
        forEachMeasure((name) ***REMOVED***> {
          if (!isSolutionMarkOrMeasure(name)) clearMeasures(name);
        });
        forEachMark((name) ***REMOVED***> {
          if (!isSolutionMarkOrMeasure(name)) clearMarks(name);
        });
      } else {
        disable();
      }
    }
  }
  function reportStatisticalValue(s, aggregate) {
    statistics.push(s);
    if (aggregate) solutionPerformance ***REMOVED******REMOVED*** null ? void 0 : solutionPerformance.addAggregateStatistic(s);
  }
  function reportCountStatistic(name, count) {
    reportStatisticalValue(
      { name, value: count, type: 1 /* count */ },
      /*aggregate*/
      true
    );
  }
  function reportTimeStatistic(name, time, aggregate) {
    reportStatisticalValue({ name, value: time, type: 0 /* time */ }, aggregate);
  }
}
function reportAllStatistics(sys2, statistics) {
  let nameSize ***REMOVED*** 0;
  let valueSize ***REMOVED*** 0;
  for (const s of statistics) {
    if (s.name.length > nameSize) {
      nameSize ***REMOVED*** s.name.length;
    }
    const value ***REMOVED*** statisticValue(s);
    if (value.length > valueSize) {
      valueSize ***REMOVED*** value.length;
    }
  }
  for (const s of statistics) {
    sys2.write(`${s.name}:`.padEnd(nameSize + 2) + statisticValue(s).toString().padStart(valueSize) + sys2.newLine);
  }
}
function statisticValue(s) {
  switch (s.type) {
    case 1 /* count */:
      return "" + s.value;
    case 0 /* time */:
      return (s.value / 1e3).toFixed(2) + "s";
    case 2 /* memory */:
      return Math.round(s.value / 1e3) + "K";
    default:
      Debug.assertNever(s.type);
  }
}
function writeConfigFile(sys2, reportDiagnostic, options) {
  const currentDirectory ***REMOVED*** sys2.getCurrentDirectory();
  const file ***REMOVED*** normalizePath(combinePaths(currentDirectory, "tsconfig.json"));
  if (sys2.fileExists(file)) {
    reportDiagnostic(createCompilerDiagnostic(Diagnostics.A_tsconfig_json_file_is_already_defined_at_Colon_0, file));
  } else {
    sys2.writeFile(file, generateTSConfig(options, sys2.newLine));
    const output ***REMOVED*** [sys2.newLine, ...getHeader(sys2, "Created a new tsconfig.json")];
    output.push(`You can learn more at https://aka.ms/tsconfig` + sys2.newLine);
    for (const line of output) {
      sys2.write(line);
    }
  }
  return;
}

// src/compiler/expressionToTypeNode.ts
function syntacticResult(type, reportFallback ***REMOVED*** true) {
  return { type, reportFallback };
}
var notImplemented2 ***REMOVED*** syntacticResult(
  /*type*/
  void 0,
  /*reportFallback*/
  false
);
var alreadyReported ***REMOVED*** syntacticResult(
  /*type*/
  void 0,
  /*reportFallback*/
  false
);
var failed ***REMOVED*** syntacticResult(
  /*type*/
  void 0,
  /*reportFallback*/
  true
);
function createSyntacticTypeNodeBuilder(options, resolver) {
  const strictNullChecks ***REMOVED*** getStrictOptionValue(options, "strictNullChecks");
  return {
    serializeTypeOfDeclaration,
    serializeReturnTypeForSignature,
    serializeTypeOfExpression,
    serializeTypeOfAccessor,
    tryReuseExistingTypeNode(context, existing) {
      if (!resolver.canReuseTypeNode(context, existing)) {
        return void 0;
      }
      return tryReuseExistingTypeNode(context, existing);
    }
  };
  function reuseNode(context, node, range ***REMOVED*** node) {
    return node ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : resolver.markNodeReuse(context, node.flags & 16 /* Synthesized */ ? node : factory.cloneNode(node), range ?? node);
  }
  function tryReuseExistingTypeNode(context, existing) {
    const { finalizeBoundary, startRecoveryScope, hadError, markError } ***REMOVED*** resolver.createRecoveryBoundary(context);
    const transformed ***REMOVED*** visitNode(existing, visitExistingNodeTreeSymbols, isTypeNode);
    if (!finalizeBoundary()) {
      return void 0;
    }
    context.approximateLength +***REMOVED*** existing.end - existing.pos;
    return transformed;
    function visitExistingNodeTreeSymbols(node) {
      if (hadError()) return node;
      const recover ***REMOVED*** startRecoveryScope();
      const onExitNewScope ***REMOVED*** isNewScopeNode(node) ? resolver.enterNewScope(context, node) : void 0;
      const result ***REMOVED*** visitExistingNodeTreeSymbolsWorker(node);
      onExitNewScope ***REMOVED******REMOVED*** null ? void 0 : onExitNewScope();
      if (hadError()) {
        if (isTypeNode(node) && !isTypePredicateNode(node)) {
          recover();
          return resolver.serializeExistingTypeNode(context, node);
        }
        return node;
      }
      return result ? resolver.markNodeReuse(context, result, node) : void 0;
    }
    function tryVisitSimpleTypeNode(node) {
      const innerNode ***REMOVED*** skipTypeParentheses(node);
      switch (innerNode.kind) {
        case 184 /* TypeReference */:
          return tryVisitTypeReference(innerNode);
        case 187 /* TypeQuery */:
          return tryVisitTypeQuery(innerNode);
        case 200 /* IndexedAccessType */:
          return tryVisitIndexedAccess(innerNode);
        case 199 /* TypeOperator */:
          const typeOperatorNode ***REMOVED*** innerNode;
          if (typeOperatorNode.operator ***REMOVED******REMOVED******REMOVED*** 143 /* KeyOfKeyword */) {
            return tryVisitKeyOf(typeOperatorNode);
          }
      }
      return visitNode(node, visitExistingNodeTreeSymbols, isTypeNode);
    }
    function tryVisitIndexedAccess(node) {
      const resultObjectType ***REMOVED*** tryVisitSimpleTypeNode(node.objectType);
      if (resultObjectType ***REMOVED******REMOVED******REMOVED*** void 0) {
        return void 0;
      }
      return factory.updateIndexedAccessTypeNode(node, resultObjectType, visitNode(node.indexType, visitExistingNodeTreeSymbols, isTypeNode));
    }
    function tryVisitKeyOf(node) {
      Debug.assertEqual(node.operator, 143 /* KeyOfKeyword */);
      const type ***REMOVED*** tryVisitSimpleTypeNode(node.type);
      if (type ***REMOVED******REMOVED******REMOVED*** void 0) {
        return void 0;
      }
      return factory.updateTypeOperatorNode(node, type);
    }
    function tryVisitTypeQuery(node) {
      const { introducesError, node: exprName } ***REMOVED*** resolver.trackExistingEntityName(context, node.exprName);
      if (!introducesError) {
        return factory.updateTypeQueryNode(
          node,
          exprName,
          visitNodes2(node.typeArguments, visitExistingNodeTreeSymbols, isTypeNode)
        );
      }
      const serializedName ***REMOVED*** resolver.serializeTypeName(
        context,
        node.exprName,
        /*isTypeOf*/
        true
      );
      if (serializedName) {
        return resolver.markNodeReuse(context, serializedName, node.exprName);
      }
    }
    function tryVisitTypeReference(node) {
      if (resolver.canReuseTypeNode(context, node)) {
        const { introducesError, node: newName } ***REMOVED*** resolver.trackExistingEntityName(context, node.typeName);
        const typeArguments ***REMOVED*** visitNodes2(node.typeArguments, visitExistingNodeTreeSymbols, isTypeNode);
        if (!introducesError) {
          const updated ***REMOVED*** factory.updateTypeReferenceNode(
            node,
            newName,
            typeArguments
          );
          return resolver.markNodeReuse(context, updated, node);
        } else {
          const serializedName ***REMOVED*** resolver.serializeTypeName(
            context,
            node.typeName,
            /*isTypeOf*/
            false,
            typeArguments
          );
          if (serializedName) {
            return resolver.markNodeReuse(context, serializedName, node.typeName);
          }
        }
      }
    }
    function visitExistingNodeTreeSymbolsWorker(node) {
      var _a;
      if (isJSDocTypeExpression(node)) {
        return visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode);
      }
      if (isJSDocAllType(node) || node.kind ***REMOVED******REMOVED******REMOVED*** 320 /* JSDocNamepathType */) {
        return factory.createKeywordTypeNode(133 /* AnyKeyword */);
      }
      if (isJSDocUnknownType(node)) {
        return factory.createKeywordTypeNode(159 /* UnknownKeyword */);
      }
      if (isJSDocNullableType(node)) {
        return factory.createUnionTypeNode([visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode), factory.createLiteralTypeNode(factory.createNull())]);
      }
      if (isJSDocOptionalType(node)) {
        return factory.createUnionTypeNode([visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode), factory.createKeywordTypeNode(157 /* UndefinedKeyword */)]);
      }
      if (isJSDocNonNullableType(node)) {
        return visitNode(node.type, visitExistingNodeTreeSymbols);
      }
      if (isJSDocVariadicType(node)) {
        return factory.createArrayTypeNode(visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode));
      }
      if (isJSDocTypeLiteral(node)) {
        return factory.createTypeLiteralNode(map(node.jsDocPropertyTags, (t) ***REMOVED***> {
          const name ***REMOVED*** visitNode(isIdentifier(t.name) ? t.name : t.name.right, visitExistingNodeTreeSymbols, isIdentifier);
          const overrideTypeNode ***REMOVED*** resolver.getJsDocPropertyOverride(context, node, t);
          return factory.createPropertySignature(
            /*modifiers*/
            void 0,
            name,
            t.isBracketed || t.typeExpression && isJSDocOptionalType(t.typeExpression.type) ? factory.createToken(58 /* QuestionToken */) : void 0,
            overrideTypeNode || t.typeExpression && visitNode(t.typeExpression.type, visitExistingNodeTreeSymbols, isTypeNode) || factory.createKeywordTypeNode(133 /* AnyKeyword */)
          );
        }));
      }
      if (isTypeReferenceNode(node) && isIdentifier(node.typeName) && node.typeName.escapedText ***REMOVED******REMOVED******REMOVED*** "") {
        return setOriginalNode(factory.createKeywordTypeNode(133 /* AnyKeyword */), node);
      }
      if ((isExpressionWithTypeArguments(node) || isTypeReferenceNode(node)) && isJSDocIndexSignature(node)) {
        return factory.createTypeLiteralNode([factory.createIndexSignature(
          /*modifiers*/
          void 0,
          [factory.createParameterDeclaration(
            /*modifiers*/
            void 0,
            /*dotDotDotToken*/
            void 0,
            "x",
            /*questionToken*/
            void 0,
            visitNode(node.typeArguments[0], visitExistingNodeTreeSymbols, isTypeNode)
          )],
          visitNode(node.typeArguments[1], visitExistingNodeTreeSymbols, isTypeNode)
        )]);
      }
      if (isJSDocFunctionType(node)) {
        if (isJSDocConstructSignature(node)) {
          let newTypeNode;
          return factory.createConstructorTypeNode(
            /*modifiers*/
            void 0,
            visitNodes2(node.typeParameters, visitExistingNodeTreeSymbols, isTypeParameterDeclaration),
            mapDefined(node.parameters, (p, i) ***REMOVED***> p.name && isIdentifier(p.name) && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** "new" ? (newTypeNode ***REMOVED*** p.type, void 0) : factory.createParameterDeclaration(
              /*modifiers*/
              void 0,
              getEffectiveDotDotDotForParameter(p),
              resolver.markNodeReuse(context, factory.createIdentifier(getNameForJSDocFunctionParameter(p, i)), p),
              factory.cloneNode(p.questionToken),
              visitNode(p.type, visitExistingNodeTreeSymbols, isTypeNode),
              /*initializer*/
              void 0
            )),
            visitNode(newTypeNode || node.type, visitExistingNodeTreeSymbols, isTypeNode) || factory.createKeywordTypeNode(133 /* AnyKeyword */)
          );
        } else {
          return factory.createFunctionTypeNode(
            visitNodes2(node.typeParameters, visitExistingNodeTreeSymbols, isTypeParameterDeclaration),
            map(node.parameters, (p, i) ***REMOVED***> factory.createParameterDeclaration(
              /*modifiers*/
              void 0,
              getEffectiveDotDotDotForParameter(p),
              resolver.markNodeReuse(context, factory.createIdentifier(getNameForJSDocFunctionParameter(p, i)), p),
              factory.cloneNode(p.questionToken),
              visitNode(p.type, visitExistingNodeTreeSymbols, isTypeNode),
              /*initializer*/
              void 0
            )),
            visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode) || factory.createKeywordTypeNode(133 /* AnyKeyword */)
          );
        }
      }
      if (isThisTypeNode(node)) {
        if (resolver.canReuseTypeNode(context, node)) {
          return node;
        }
        markError();
        return node;
      }
      if (isTypeParameterDeclaration(node)) {
        const { node: newName } ***REMOVED*** resolver.trackExistingEntityName(context, node.name);
        return factory.updateTypeParameterDeclaration(
          node,
          visitNodes2(node.modifiers, visitExistingNodeTreeSymbols, isModifier),
          // resolver.markNodeReuse(context, typeParameterToName(getDeclaredTypeOfSymbol(getSymbolOfDeclaration(node)), context), node),
          newName,
          visitNode(node.constraint, visitExistingNodeTreeSymbols, isTypeNode),
          visitNode(node.default, visitExistingNodeTreeSymbols, isTypeNode)
        );
      }
      if (isIndexedAccessTypeNode(node)) {
        const result ***REMOVED*** tryVisitIndexedAccess(node);
        if (!result) {
          markError();
          return node;
        }
        return result;
      }
      if (isTypeReferenceNode(node)) {
        const result ***REMOVED*** tryVisitTypeReference(node);
        if (result) {
          return result;
        }
        markError();
        return node;
      }
      if (isLiteralImportTypeNode(node)) {
        if (((_a ***REMOVED*** node.attributes) ***REMOVED******REMOVED*** null ? void 0 : _a.token) ***REMOVED******REMOVED******REMOVED*** 132 /* AssertKeyword */) {
          markError();
          return node;
        }
        if (!resolver.canReuseTypeNode(context, node)) {
          return resolver.serializeExistingTypeNode(context, node);
        }
        const specifier ***REMOVED*** rewriteModuleSpecifier2(node, node.argument.literal);
        const literal ***REMOVED*** specifier ***REMOVED******REMOVED******REMOVED*** node.argument.literal ? reuseNode(context, node.argument.literal) : specifier;
        return factory.updateImportTypeNode(
          node,
          literal ***REMOVED******REMOVED******REMOVED*** node.argument.literal ? reuseNode(context, node.argument) : factory.createLiteralTypeNode(literal),
          visitNode(node.attributes, visitExistingNodeTreeSymbols, isImportAttributes),
          visitNode(node.qualifier, visitExistingNodeTreeSymbols, isEntityName),
          visitNodes2(node.typeArguments, visitExistingNodeTreeSymbols, isTypeNode),
          node.isTypeOf
        );
      }
      if (isNamedDeclaration(node) && node.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */ && !resolver.hasLateBindableName(node)) {
        if (!hasDynamicName(node)) {
          return visitEachChild2(node, visitExistingNodeTreeSymbols);
        }
        if (resolver.shouldRemoveDeclaration(context, node)) {
          return void 0;
        }
      }
      if (isFunctionLike(node) && !node.type || isPropertyDeclaration(node) && !node.type && !node.initializer || isPropertySignature(node) && !node.type && !node.initializer || isParameter(node) && !node.type && !node.initializer) {
        let visited ***REMOVED*** visitEachChild2(node, visitExistingNodeTreeSymbols);
        if (visited ***REMOVED******REMOVED******REMOVED*** node) {
          visited ***REMOVED*** resolver.markNodeReuse(context, factory.cloneNode(node), node);
        }
        visited.type ***REMOVED*** factory.createKeywordTypeNode(133 /* AnyKeyword */);
        if (isParameter(node)) {
          visited.modifiers ***REMOVED*** void 0;
        }
        return visited;
      }
      if (isTypeQueryNode(node)) {
        const result ***REMOVED*** tryVisitTypeQuery(node);
        if (!result) {
          markError();
          return node;
        }
        return result;
      }
      if (isComputedPropertyName(node) && isEntityNameExpression(node.expression)) {
        const { node: result, introducesError } ***REMOVED*** resolver.trackExistingEntityName(context, node.expression);
        if (!introducesError) {
          return factory.updateComputedPropertyName(node, result);
        } else {
          const computedPropertyNameType ***REMOVED*** resolver.serializeTypeOfExpression(context, node.expression);
          let literal;
          if (isLiteralTypeNode(computedPropertyNameType)) {
            literal ***REMOVED*** computedPropertyNameType.literal;
          } else {
            const evaluated ***REMOVED*** resolver.evaluateEntityNameExpression(node.expression);
            const literalNode ***REMOVED*** typeof evaluated.value ***REMOVED******REMOVED******REMOVED*** "string" ? factory.createStringLiteral(
              evaluated.value,
              /*isSingleQuote*/
              void 0
            ) : typeof evaluated.value ***REMOVED******REMOVED******REMOVED*** "number" ? factory.createNumericLiteral(
              evaluated.value,
              /*numericLiteralFlags*/
              0
            ) : void 0;
            if (!literalNode) {
              if (isImportTypeNode(computedPropertyNameType)) {
                resolver.trackComputedName(context, node.expression);
              }
              return node;
            }
            literal ***REMOVED*** literalNode;
          }
          if (literal.kind ***REMOVED******REMOVED******REMOVED*** 11 /* StringLiteral */ && isIdentifierText(literal.text, getEmitScriptTarget(options))) {
            return factory.createIdentifier(literal.text);
          }
          if (literal.kind ***REMOVED******REMOVED******REMOVED*** 9 /* NumericLiteral */ && !literal.text.startsWith("-")) {
            return literal;
          }
          return factory.updateComputedPropertyName(node, literal);
        }
      }
      if (isTypePredicateNode(node)) {
        let parameterName;
        if (isIdentifier(node.parameterName)) {
          const { node: result, introducesError } ***REMOVED*** resolver.trackExistingEntityName(context, node.parameterName);
          if (introducesError) markError();
          parameterName ***REMOVED*** result;
        } else {
          parameterName ***REMOVED*** factory.cloneNode(node.parameterName);
        }
        return factory.updateTypePredicateNode(node, factory.cloneNode(node.assertsModifier), parameterName, visitNode(node.type, visitExistingNodeTreeSymbols, isTypeNode));
      }
      if (isTupleTypeNode(node) || isTypeLiteralNode(node) || isMappedTypeNode(node)) {
        const visited ***REMOVED*** visitEachChild2(node, visitExistingNodeTreeSymbols);
        const clone ***REMOVED*** resolver.markNodeReuse(context, visited ***REMOVED******REMOVED******REMOVED*** node ? factory.cloneNode(node) : visited, node);
        const flags ***REMOVED*** getEmitFlags(clone);
        setEmitFlags(clone, flags | (context.flags & 1024 /* MultilineObjectLiterals */ && isTypeLiteralNode(node) ? 0 : 1 /* SingleLine */));
        return clone;
      }
      if (isStringLiteral(node) && !!(context.flags & 268435456 /* UseSingleQuotesForStringLiteralType */) && !node.singleQuote) {
        const clone ***REMOVED*** factory.cloneNode(node);
        clone.singleQuote ***REMOVED*** true;
        return clone;
      }
      if (isConditionalTypeNode(node)) {
        const checkType ***REMOVED*** visitNode(node.checkType, visitExistingNodeTreeSymbols, isTypeNode);
        const disposeScope ***REMOVED*** resolver.enterNewScope(context, node);
        const extendType ***REMOVED*** visitNode(node.extendsType, visitExistingNodeTreeSymbols, isTypeNode);
        const trueType ***REMOVED*** visitNode(node.trueType, visitExistingNodeTreeSymbols, isTypeNode);
        disposeScope();
        const falseType ***REMOVED*** visitNode(node.falseType, visitExistingNodeTreeSymbols, isTypeNode);
        return factory.updateConditionalTypeNode(
          node,
          checkType,
          extendType,
          trueType,
          falseType
        );
      }
      if (isTypeOperatorNode(node)) {
        if (node.operator ***REMOVED******REMOVED******REMOVED*** 158 /* UniqueKeyword */ && node.type.kind ***REMOVED******REMOVED******REMOVED*** 155 /* SymbolKeyword */) {
          if (!resolver.canReuseTypeNode(context, node)) {
            markError();
            return node;
          }
        } else if (node.operator ***REMOVED******REMOVED******REMOVED*** 143 /* KeyOfKeyword */) {
          const result ***REMOVED*** tryVisitKeyOf(node);
          if (!result) {
            markError();
            return node;
          }
          return result;
        }
      }
      return visitEachChild2(node, visitExistingNodeTreeSymbols);
      function visitEachChild2(node2, visitor) {
        const nonlocalNode ***REMOVED*** !context.enclosingFile || context.enclosingFile !***REMOVED******REMOVED*** getSourceFileOfNode(node2);
        return visitEachChild(
          node2,
          visitor,
          /*context*/
          void 0,
          nonlocalNode ? visitNodesWithoutCopyingPositions : void 0
        );
      }
      function visitNodesWithoutCopyingPositions(nodes, visitor, test, start, count) {
        let result ***REMOVED*** visitNodes2(nodes, visitor, test, start, count);
        if (result) {
          if (result.pos !***REMOVED******REMOVED*** -1 || result.end !***REMOVED******REMOVED*** -1) {
            if (result ***REMOVED******REMOVED******REMOVED*** nodes) {
              result ***REMOVED*** factory.createNodeArray(nodes.slice(), nodes.hasTrailingComma);
            }
            setTextRangePosEnd(result, -1, -1);
          }
        }
        return result;
      }
      function getEffectiveDotDotDotForParameter(p) {
        return p.dotDotDotToken || (p.type && isJSDocVariadicType(p.type) ? factory.createToken(26 /* DotDotDotToken */) : void 0);
      }
      function getNameForJSDocFunctionParameter(p, index) {
        return p.name && isIdentifier(p.name) && p.name.escapedText ***REMOVED******REMOVED******REMOVED*** "this" ? "this" : getEffectiveDotDotDotForParameter(p) ? `args` : `arg${index}`;
      }
      function rewriteModuleSpecifier2(parent, lit) {
        const newName ***REMOVED*** resolver.getModuleSpecifierOverride(context, parent, lit);
        return newName ? setOriginalNode(factory.createStringLiteral(newName), lit) : lit;
      }
    }
  }
  function serializeExistingTypeNode(typeNode, context, addUndefined) {
    if (!typeNode) return void 0;
    let result;
    if ((!addUndefined || canAddUndefined(typeNode)) && resolver.canReuseTypeNode(context, typeNode)) {
      result ***REMOVED*** tryReuseExistingTypeNode(context, typeNode);
      if (result !***REMOVED******REMOVED*** void 0) {
        result ***REMOVED*** addUndefinedIfNeeded(
          result,
          addUndefined,
          /*owner*/
          void 0,
          context
        );
      }
    }
    return result;
  }
  function serializeTypeAnnotationOfDeclaration(declaredType, context, node, symbol, requiresAddingUndefined, useFallback ***REMOVED*** requiresAddingUndefined !***REMOVED******REMOVED*** void 0) {
    if (!declaredType) return void 0;
    if (!resolver.canReuseTypeNodeAnnotation(context, node, declaredType, symbol, requiresAddingUndefined)) {
      if (!requiresAddingUndefined || !resolver.canReuseTypeNodeAnnotation(
        context,
        node,
        declaredType,
        symbol,
        /*requiresAddingUndefined*/
        false
      )) {
        return void 0;
      }
    }
    let result;
    if (!requiresAddingUndefined || canAddUndefined(declaredType)) {
      result ***REMOVED*** serializeExistingTypeNode(declaredType, context, requiresAddingUndefined);
    }
    if (result !***REMOVED******REMOVED*** void 0 || !useFallback) {
      return result;
    }
    context.tracker.reportInferenceFallback(node);
    return resolver.serializeExistingTypeNode(context, declaredType, requiresAddingUndefined) ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
  }
  function serializeExistingTypeNodeWithFallback(typeNode, context, addUndefined, targetNode) {
    if (!typeNode) return void 0;
    const result ***REMOVED*** serializeExistingTypeNode(typeNode, context, addUndefined);
    if (result !***REMOVED******REMOVED*** void 0) {
      return result;
    }
    context.tracker.reportInferenceFallback(targetNode ?? typeNode);
    return resolver.serializeExistingTypeNode(context, typeNode, addUndefined) ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
  }
  function serializeTypeOfAccessor(accessor, symbol, context) {
    return typeFromAccessor(accessor, symbol, context) ?? inferAccessorType(accessor, resolver.getAllAccessorDeclarations(accessor), context, symbol);
  }
  function serializeTypeOfExpression(expr, context, addUndefined, preserveLiterals) {
    const result ***REMOVED*** typeFromExpression(
      expr,
      context,
      /*isConstContext*/
      false,
      addUndefined,
      preserveLiterals
    );
    return result.type !***REMOVED******REMOVED*** void 0 ? result.type : inferExpressionType(expr, context, result.reportFallback);
  }
  function serializeTypeOfDeclaration(node, symbol, context) {
    switch (node.kind) {
      case 170 /* Parameter */:
      case 342 /* JSDocParameterTag */:
        return typeFromParameter(node, symbol, context);
      case 261 /* VariableDeclaration */:
        return typeFromVariable(node, symbol, context);
      case 172 /* PropertySignature */:
      case 349 /* JSDocPropertyTag */:
      case 173 /* PropertyDeclaration */:
        return typeFromProperty(node, symbol, context);
      case 209 /* BindingElement */:
        return inferTypeOfDeclaration(node, symbol, context);
      case 278 /* ExportAssignment */:
        return serializeTypeOfExpression(
          node.expression,
          context,
          /*addUndefined*/
          void 0,
          /*preserveLiterals*/
          true
        );
      case 212 /* PropertyAccessExpression */:
      case 213 /* ElementAccessExpression */:
      case 227 /* BinaryExpression */:
        return typeFromExpandoProperty(node, symbol, context);
      case 304 /* PropertyAssignment */:
      case 305 /* ShorthandPropertyAssignment */:
        return typeFromPropertyAssignment(node, symbol, context);
      default:
        Debug.assertNever(node, `Node needs to be an inferrable node, found ${Debug.formatSyntaxKind(node.kind)}`);
    }
  }
  function typeFromPropertyAssignment(node, symbol, context) {
    const typeAnnotation ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    let result;
    if (typeAnnotation && resolver.canReuseTypeNodeAnnotation(context, node, typeAnnotation, symbol)) {
      result ***REMOVED*** serializeExistingTypeNode(typeAnnotation, context);
    }
    if (!result && node.kind ***REMOVED******REMOVED******REMOVED*** 304 /* PropertyAssignment */) {
      const initializer ***REMOVED*** node.initializer;
      const assertionNode ***REMOVED*** isJSDocTypeAssertion(initializer) ? getJSDocTypeAssertionType(initializer) : initializer.kind ***REMOVED******REMOVED******REMOVED*** 235 /* AsExpression */ || initializer.kind ***REMOVED******REMOVED******REMOVED*** 217 /* TypeAssertionExpression */ ? initializer.type : void 0;
      if (assertionNode && !isConstTypeReference(assertionNode) && resolver.canReuseTypeNodeAnnotation(context, node, assertionNode, symbol)) {
        result ***REMOVED*** serializeExistingTypeNode(assertionNode, context);
      }
    }
    return result ?? inferTypeOfDeclaration(
      node,
      symbol,
      context,
      /*reportFallback*/
      false
    );
  }
  function serializeReturnTypeForSignature(node, symbol, context) {
    switch (node.kind) {
      case 178 /* GetAccessor */:
        return serializeTypeOfAccessor(node, symbol, context);
      case 175 /* MethodDeclaration */:
      case 263 /* FunctionDeclaration */:
      case 181 /* ConstructSignature */:
      case 174 /* MethodSignature */:
      case 180 /* CallSignature */:
      case 177 /* Constructor */:
      case 179 /* SetAccessor */:
      case 182 /* IndexSignature */:
      case 185 /* FunctionType */:
      case 186 /* ConstructorType */:
      case 219 /* FunctionExpression */:
      case 220 /* ArrowFunction */:
      case 318 /* JSDocFunctionType */:
      case 324 /* JSDocSignature */:
        return createReturnFromSignature(node, symbol, context);
      default:
        Debug.assertNever(node, `Node needs to be an inferrable node, found ${Debug.formatSyntaxKind(node.kind)}`);
    }
  }
  function getTypeAnnotationFromAccessor(accessor) {
    if (accessor) {
      return accessor.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */ ? isInJSFile(accessor) && getJSDocType(accessor) || getEffectiveReturnTypeNode(accessor) : getEffectiveSetAccessorTypeAnnotationNode(accessor);
    }
  }
  function getTypeAnnotationFromAllAccessorDeclarations(node, accessors) {
    let accessorType ***REMOVED*** getTypeAnnotationFromAccessor(node);
    if (!accessorType && node !***REMOVED******REMOVED*** accessors.firstAccessor) {
      accessorType ***REMOVED*** getTypeAnnotationFromAccessor(accessors.firstAccessor);
    }
    if (!accessorType && accessors.secondAccessor && node !***REMOVED******REMOVED*** accessors.secondAccessor) {
      accessorType ***REMOVED*** getTypeAnnotationFromAccessor(accessors.secondAccessor);
    }
    return accessorType;
  }
  function typeFromAccessor(node, symbol, context) {
    const accessorDeclarations ***REMOVED*** resolver.getAllAccessorDeclarations(node);
    const accessorType ***REMOVED*** getTypeAnnotationFromAllAccessorDeclarations(node, accessorDeclarations);
    if (accessorType && !isTypePredicateNode(accessorType)) {
      return withNewScope(context, node, () ***REMOVED***> serializeTypeAnnotationOfDeclaration(accessorType, context, node, symbol) ?? inferTypeOfDeclaration(node, symbol, context));
    }
    if (accessorDeclarations.getAccessor) {
      return withNewScope(context, accessorDeclarations.getAccessor, () ***REMOVED***> createReturnFromSignature(accessorDeclarations.getAccessor, symbol, context));
    }
    return void 0;
  }
  function typeFromVariable(node, symbol, context) {
    var _a;
    const declaredType ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    let resultType ***REMOVED*** failed;
    if (declaredType) {
      resultType ***REMOVED*** syntacticResult(serializeTypeAnnotationOfDeclaration(declaredType, context, node, symbol));
    } else if (node.initializer && (((_a ***REMOVED*** symbol.declarations) ***REMOVED******REMOVED*** null ? void 0 : _a.length) ***REMOVED******REMOVED******REMOVED*** 1 || countWhere(symbol.declarations, isVariableDeclaration) ***REMOVED******REMOVED******REMOVED*** 1)) {
      if (!resolver.isExpandoFunctionDeclaration(node) && !isContextuallyTyped(node)) {
        resultType ***REMOVED*** typeFromExpression(
          node.initializer,
          context,
          /*isConstContext*/
          void 0,
          /*requiresAddingUndefined*/
          void 0,
          isVarConstLike(node)
        );
      }
    }
    return resultType.type !***REMOVED******REMOVED*** void 0 ? resultType.type : inferTypeOfDeclaration(node, symbol, context, resultType.reportFallback);
  }
  function typeFromParameter(node, symbol, context) {
    const parent ***REMOVED*** node.parent;
    if (parent.kind ***REMOVED******REMOVED******REMOVED*** 179 /* SetAccessor */) {
      return serializeTypeOfAccessor(
        parent,
        /*symbol*/
        void 0,
        context
      );
    }
    const declaredType ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    const addUndefined ***REMOVED*** resolver.requiresAddingImplicitUndefined(node, symbol, context.enclosingDeclaration);
    let resultType ***REMOVED*** failed;
    if (declaredType) {
      resultType ***REMOVED*** syntacticResult(serializeTypeAnnotationOfDeclaration(declaredType, context, node, symbol, addUndefined));
    } else if (isParameter(node) && node.initializer && isIdentifier(node.name) && !isContextuallyTyped(node)) {
      resultType ***REMOVED*** typeFromExpression(
        node.initializer,
        context,
        /*isConstContext*/
        void 0,
        addUndefined
      );
    }
    return resultType.type !***REMOVED******REMOVED*** void 0 ? resultType.type : inferTypeOfDeclaration(node, symbol, context, resultType.reportFallback);
  }
  function typeFromExpandoProperty(node, symbol, context) {
    const declaredType ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    let result;
    if (declaredType) {
      result ***REMOVED*** serializeTypeAnnotationOfDeclaration(declaredType, context, node, symbol);
    }
    const oldSuppressReportInferenceFallback ***REMOVED*** context.suppressReportInferenceFallback;
    context.suppressReportInferenceFallback ***REMOVED*** true;
    const resultType ***REMOVED*** result ?? inferTypeOfDeclaration(
      node,
      symbol,
      context,
      /*reportFallback*/
      false
    );
    context.suppressReportInferenceFallback ***REMOVED*** oldSuppressReportInferenceFallback;
    return resultType;
  }
  function typeFromProperty(node, symbol, context) {
    const declaredType ***REMOVED*** getEffectiveTypeAnnotationNode(node);
    const requiresAddingUndefined ***REMOVED*** resolver.requiresAddingImplicitUndefined(node, symbol, context.enclosingDeclaration);
    let resultType ***REMOVED*** failed;
    if (declaredType) {
      resultType ***REMOVED*** syntacticResult(serializeTypeAnnotationOfDeclaration(declaredType, context, node, symbol, requiresAddingUndefined));
    } else {
      const initializer ***REMOVED*** isPropertyDeclaration(node) ? node.initializer : void 0;
      if (initializer && !isContextuallyTyped(node)) {
        const isReadonly ***REMOVED*** isDeclarationReadonly(node);
        resultType ***REMOVED*** typeFromExpression(
          initializer,
          context,
          /*isConstContext*/
          void 0,
          requiresAddingUndefined,
          isReadonly
        );
      }
    }
    return resultType.type !***REMOVED******REMOVED*** void 0 ? resultType.type : inferTypeOfDeclaration(node, symbol, context, resultType.reportFallback);
  }
  function inferTypeOfDeclaration(node, symbol, context, reportFallback ***REMOVED*** true) {
    if (reportFallback) {
      context.tracker.reportInferenceFallback(node);
    }
    if (context.noInferenceFallback ***REMOVED******REMOVED******REMOVED*** true) {
      return factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
    return resolver.serializeTypeOfDeclaration(context, node, symbol);
  }
  function inferExpressionType(node, context, reportFallback ***REMOVED*** true, requiresAddingUndefined) {
    Debug.assert(!requiresAddingUndefined);
    if (reportFallback) {
      context.tracker.reportInferenceFallback(node);
    }
    if (context.noInferenceFallback ***REMOVED******REMOVED******REMOVED*** true) {
      return factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
    return resolver.serializeTypeOfExpression(context, node) ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
  }
  function inferReturnTypeOfSignatureSignature(node, context, symbol, reportFallback) {
    if (reportFallback) {
      context.tracker.reportInferenceFallback(node);
    }
    if (context.noInferenceFallback ***REMOVED******REMOVED******REMOVED*** true) {
      return factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
    return resolver.serializeReturnTypeForSignature(context, node, symbol) ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
  }
  function inferAccessorType(node, allAccessors, context, symbol, reportFallback ***REMOVED*** true) {
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 178 /* GetAccessor */) {
      return createReturnFromSignature(node, symbol, context, reportFallback);
    } else {
      if (reportFallback) {
        context.tracker.reportInferenceFallback(node);
      }
      const result ***REMOVED*** allAccessors.getAccessor && createReturnFromSignature(allAccessors.getAccessor, symbol, context, reportFallback);
      return result ?? resolver.serializeTypeOfDeclaration(context, node, symbol) ?? factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
  }
  function withNewScope(context, node, fn) {
    const cleanup ***REMOVED*** resolver.enterNewScope(context, node);
    const result ***REMOVED*** fn();
    cleanup();
    return result;
  }
  function typeFromTypeAssertion(expression, type, context, requiresAddingUndefined) {
    if (isConstTypeReference(type)) {
      return typeFromExpression(
        expression,
        context,
        /*isConstContext*/
        true,
        requiresAddingUndefined
      );
    }
    return syntacticResult(serializeExistingTypeNodeWithFallback(type, context, requiresAddingUndefined));
  }
  function typeFromExpression(node, context, isConstContext ***REMOVED*** false, requiresAddingUndefined ***REMOVED*** false, preserveLiterals ***REMOVED*** false) {
    switch (node.kind) {
      case 218 /* ParenthesizedExpression */:
        if (isJSDocTypeAssertion(node)) {
          return typeFromTypeAssertion(node.expression, getJSDocTypeAssertionType(node), context, requiresAddingUndefined);
        }
        return typeFromExpression(node.expression, context, isConstContext, requiresAddingUndefined);
      case 80 /* Identifier */:
        if (resolver.isUndefinedIdentifierExpression(node)) {
          return syntacticResult(createUndefinedTypeNode());
        }
        break;
      case 106 /* NullKeyword */:
        if (strictNullChecks) {
          return syntacticResult(addUndefinedIfNeeded(factory.createLiteralTypeNode(factory.createNull()), requiresAddingUndefined, node, context));
        } else {
          return syntacticResult(factory.createKeywordTypeNode(133 /* AnyKeyword */));
        }
      case 220 /* ArrowFunction */:
      case 219 /* FunctionExpression */:
        Debug.type(node);
        return withNewScope(context, node, () ***REMOVED***> typeFromFunctionLikeExpression(node, context));
      case 217 /* TypeAssertionExpression */:
      case 235 /* AsExpression */:
        const asExpression ***REMOVED*** node;
        return typeFromTypeAssertion(asExpression.expression, asExpression.type, context, requiresAddingUndefined);
      case 225 /* PrefixUnaryExpression */:
        const unaryExpression ***REMOVED*** node;
        if (isPrimitiveLiteralValue(unaryExpression)) {
          return typeFromPrimitiveLiteral(
            unaryExpression.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */ ? unaryExpression.operand : unaryExpression,
            unaryExpression.operand.kind ***REMOVED******REMOVED******REMOVED*** 10 /* BigIntLiteral */ ? 163 /* BigIntKeyword */ : 150 /* NumberKeyword */,
            context,
            isConstContext || preserveLiterals,
            requiresAddingUndefined
          );
        }
        break;
      case 210 /* ArrayLiteralExpression */:
        return typeFromArrayLiteral(node, context, isConstContext, requiresAddingUndefined);
      case 211 /* ObjectLiteralExpression */:
        return typeFromObjectLiteral(node, context, isConstContext, requiresAddingUndefined);
      case 232 /* ClassExpression */:
        return syntacticResult(inferExpressionType(
          node,
          context,
          /*reportFallback*/
          true,
          requiresAddingUndefined
        ));
      case 229 /* TemplateExpression */:
        if (!isConstContext && !preserveLiterals) {
          return syntacticResult(factory.createKeywordTypeNode(154 /* StringKeyword */));
        }
        break;
      default:
        let typeKind;
        let primitiveNode ***REMOVED*** node;
        switch (node.kind) {
          case 9 /* NumericLiteral */:
            typeKind ***REMOVED*** 150 /* NumberKeyword */;
            break;
          case 15 /* NoSubstitutionTemplateLiteral */:
            primitiveNode ***REMOVED*** factory.createStringLiteral(node.text);
            typeKind ***REMOVED*** 154 /* StringKeyword */;
            break;
          case 11 /* StringLiteral */:
            typeKind ***REMOVED*** 154 /* StringKeyword */;
            break;
          case 10 /* BigIntLiteral */:
            typeKind ***REMOVED*** 163 /* BigIntKeyword */;
            break;
          case 112 /* TrueKeyword */:
          case 97 /* FalseKeyword */:
            typeKind ***REMOVED*** 136 /* BooleanKeyword */;
            break;
        }
        if (typeKind) {
          return typeFromPrimitiveLiteral(primitiveNode, typeKind, context, isConstContext || preserveLiterals, requiresAddingUndefined);
        }
    }
    return failed;
  }
  function typeFromFunctionLikeExpression(fnNode, context) {
    const returnType ***REMOVED*** createReturnFromSignature(
      fnNode,
      /*symbol*/
      void 0,
      context
    );
    const typeParameters ***REMOVED*** reuseTypeParameters(fnNode.typeParameters, context);
    const parameters ***REMOVED*** fnNode.parameters.map((p) ***REMOVED***> ensureParameter(p, context));
    return syntacticResult(
      factory.createFunctionTypeNode(
        typeParameters,
        parameters,
        returnType
      )
    );
  }
  function canGetTypeFromArrayLiteral(arrayLiteral, context, isConstContext) {
    if (!isConstContext) {
      context.tracker.reportInferenceFallback(arrayLiteral);
      return false;
    }
    for (const element of arrayLiteral.elements) {
      if (element.kind ***REMOVED******REMOVED******REMOVED*** 231 /* SpreadElement */) {
        context.tracker.reportInferenceFallback(element);
        return false;
      }
    }
    return true;
  }
  function typeFromArrayLiteral(arrayLiteral, context, isConstContext, requiresAddingUndefined) {
    if (!canGetTypeFromArrayLiteral(arrayLiteral, context, isConstContext)) {
      if (requiresAddingUndefined || isDeclaration(walkUpParenthesizedExpressions(arrayLiteral).parent)) {
        return alreadyReported;
      }
      return syntacticResult(inferExpressionType(
        arrayLiteral,
        context,
        /*reportFallback*/
        false,
        requiresAddingUndefined
      ));
    }
    const oldNoInferenceFallback ***REMOVED*** context.noInferenceFallback;
    context.noInferenceFallback ***REMOVED*** true;
    const elementTypesInfo ***REMOVED*** [];
    for (const element of arrayLiteral.elements) {
      Debug.assert(element.kind !***REMOVED******REMOVED*** 231 /* SpreadElement */);
      if (element.kind ***REMOVED******REMOVED******REMOVED*** 233 /* OmittedExpression */) {
        elementTypesInfo.push(
          createUndefinedTypeNode()
        );
      } else {
        const expressionType ***REMOVED*** typeFromExpression(element, context, isConstContext);
        const elementType ***REMOVED*** expressionType.type !***REMOVED******REMOVED*** void 0 ? expressionType.type : inferExpressionType(element, context, expressionType.reportFallback);
        elementTypesInfo.push(elementType);
      }
    }
    const tupleType ***REMOVED*** factory.createTupleTypeNode(elementTypesInfo);
    tupleType.emitNode ***REMOVED*** { flags: 1, autoGenerate: void 0, internalFlags: 0 };
    context.noInferenceFallback ***REMOVED*** oldNoInferenceFallback;
    return notImplemented2;
  }
  function canGetTypeFromObjectLiteral(objectLiteral, context) {
    let result ***REMOVED*** true;
    for (const prop of objectLiteral.properties) {
      if (prop.flags & 262144 /* ThisNodeHasError */) {
        result ***REMOVED*** false;
        break;
      }
      if (prop.kind ***REMOVED******REMOVED******REMOVED*** 305 /* ShorthandPropertyAssignment */ || prop.kind ***REMOVED******REMOVED******REMOVED*** 306 /* SpreadAssignment */) {
        context.tracker.reportInferenceFallback(prop);
        result ***REMOVED*** false;
      } else if (prop.name.flags & 262144 /* ThisNodeHasError */) {
        result ***REMOVED*** false;
        break;
      } else if (prop.name.kind ***REMOVED******REMOVED******REMOVED*** 81 /* PrivateIdentifier */) {
        result ***REMOVED*** false;
      } else if (prop.name.kind ***REMOVED******REMOVED******REMOVED*** 168 /* ComputedPropertyName */) {
        const expression ***REMOVED*** prop.name.expression;
        if (!isPrimitiveLiteralValue(
          expression,
          /*includeBigInt*/
          false
        ) && !resolver.isDefinitelyReferenceToGlobalSymbolObject(expression)) {
          context.tracker.reportInferenceFallback(prop.name);
          result ***REMOVED*** false;
        }
      }
    }
    return result;
  }
  function typeFromObjectLiteral(objectLiteral, context, isConstContext, requiresAddingUndefined) {
    if (!canGetTypeFromObjectLiteral(objectLiteral, context)) {
      if (requiresAddingUndefined || isDeclaration(walkUpParenthesizedExpressions(objectLiteral).parent)) {
        return alreadyReported;
      }
      return syntacticResult(inferExpressionType(
        objectLiteral,
        context,
        /*reportFallback*/
        false,
        requiresAddingUndefined
      ));
    }
    const oldNoInferenceFallback ***REMOVED*** context.noInferenceFallback;
    context.noInferenceFallback ***REMOVED*** true;
    const properties ***REMOVED*** [];
    const oldFlags ***REMOVED*** context.flags;
    context.flags |***REMOVED*** 4194304 /* InObjectTypeLiteral */;
    for (const prop of objectLiteral.properties) {
      Debug.assert(!isShorthandPropertyAssignment(prop) && !isSpreadAssignment(prop));
      const name ***REMOVED*** prop.name;
      let newProp;
      switch (prop.kind) {
        case 175 /* MethodDeclaration */:
          newProp ***REMOVED*** withNewScope(context, prop, () ***REMOVED***> typeFromObjectLiteralMethod(prop, name, context, isConstContext));
          break;
        case 304 /* PropertyAssignment */:
          newProp ***REMOVED*** typeFromObjectLiteralPropertyAssignment(prop, name, context, isConstContext);
          break;
        case 179 /* SetAccessor */:
        case 178 /* GetAccessor */:
          newProp ***REMOVED*** typeFromObjectLiteralAccessor(prop, name, context);
          break;
      }
      if (newProp) {
        setCommentRange(newProp, prop);
        properties.push(newProp);
      }
    }
    context.flags ***REMOVED*** oldFlags;
    const typeNode ***REMOVED*** factory.createTypeLiteralNode(properties);
    if (!(context.flags & 1024 /* MultilineObjectLiterals */)) {
      setEmitFlags(typeNode, 1 /* SingleLine */);
    }
    context.noInferenceFallback ***REMOVED*** oldNoInferenceFallback;
    return notImplemented2;
  }
  function typeFromObjectLiteralPropertyAssignment(prop, name, context, isConstContext) {
    const modifiers ***REMOVED*** isConstContext ? [factory.createModifier(148 /* ReadonlyKeyword */)] : [];
    const expressionResult ***REMOVED*** typeFromExpression(prop.initializer, context, isConstContext);
    const typeNode ***REMOVED*** expressionResult.type !***REMOVED******REMOVED*** void 0 ? expressionResult.type : inferTypeOfDeclaration(
      prop,
      /*symbol*/
      void 0,
      context,
      expressionResult.reportFallback
    );
    return factory.createPropertySignature(
      modifiers,
      reuseNode(context, name),
      /*questionToken*/
      void 0,
      typeNode
    );
  }
  function ensureParameter(p, context) {
    return factory.updateParameterDeclaration(
      p,
      /*modifiers*/
      void 0,
      reuseNode(context, p.dotDotDotToken),
      resolver.serializeNameOfParameter(context, p),
      resolver.isOptionalParameter(p) ? factory.createToken(58 /* QuestionToken */) : void 0,
      typeFromParameter(
        p,
        /*symbol*/
        void 0,
        context
      ),
      // Ignore private param props, since this type is going straight back into a param
      /*initializer*/
      void 0
    );
  }
  function reuseTypeParameters(typeParameters, context) {
    return typeParameters ***REMOVED******REMOVED*** null ? void 0 : typeParameters.map((tp) ***REMOVED***> {
      var _a;
      const { node: tpName } ***REMOVED*** resolver.trackExistingEntityName(context, tp.name);
      return factory.updateTypeParameterDeclaration(
        tp,
        (_a ***REMOVED*** tp.modifiers) ***REMOVED******REMOVED*** null ? void 0 : _a.map((m) ***REMOVED***> reuseNode(context, m)),
        tpName,
        serializeExistingTypeNodeWithFallback(tp.constraint, context),
        serializeExistingTypeNodeWithFallback(tp.default, context)
      );
    });
  }
  function typeFromObjectLiteralMethod(method, name, context, isConstContext) {
    const returnType ***REMOVED*** createReturnFromSignature(
      method,
      /*symbol*/
      void 0,
      context
    );
    const typeParameters ***REMOVED*** reuseTypeParameters(method.typeParameters, context);
    const parameters ***REMOVED*** method.parameters.map((p) ***REMOVED***> ensureParameter(p, context));
    if (isConstContext) {
      return factory.createPropertySignature(
        [factory.createModifier(148 /* ReadonlyKeyword */)],
        reuseNode(context, name),
        reuseNode(context, method.questionToken),
        factory.createFunctionTypeNode(
          typeParameters,
          parameters,
          returnType
        )
      );
    } else {
      if (isIdentifier(name) && name.escapedText ***REMOVED******REMOVED******REMOVED*** "new") {
        name ***REMOVED*** factory.createStringLiteral("new");
      }
      return factory.createMethodSignature(
        [],
        reuseNode(context, name),
        reuseNode(context, method.questionToken),
        typeParameters,
        parameters,
        returnType
      );
    }
  }
  function typeFromObjectLiteralAccessor(accessor, name, context) {
    const allAccessors ***REMOVED*** resolver.getAllAccessorDeclarations(accessor);
    const getAccessorType ***REMOVED*** allAccessors.getAccessor && getTypeAnnotationFromAccessor(allAccessors.getAccessor);
    const setAccessorType ***REMOVED*** allAccessors.setAccessor && getTypeAnnotationFromAccessor(allAccessors.setAccessor);
    if (getAccessorType !***REMOVED******REMOVED*** void 0 && setAccessorType !***REMOVED******REMOVED*** void 0) {
      return withNewScope(context, accessor, () ***REMOVED***> {
        const parameters ***REMOVED*** accessor.parameters.map((p) ***REMOVED***> ensureParameter(p, context));
        if (isGetAccessor(accessor)) {
          return factory.updateGetAccessorDeclaration(
            accessor,
            [],
            reuseNode(context, name),
            parameters,
            serializeExistingTypeNodeWithFallback(getAccessorType, context),
            /*body*/
            void 0
          );
        } else {
          return factory.updateSetAccessorDeclaration(
            accessor,
            [],
            reuseNode(context, name),
            parameters,
            /*body*/
            void 0
          );
        }
      });
    } else if (allAccessors.firstAccessor ***REMOVED******REMOVED******REMOVED*** accessor) {
      const foundType ***REMOVED*** getAccessorType ? withNewScope(context, allAccessors.getAccessor, () ***REMOVED***> serializeExistingTypeNodeWithFallback(getAccessorType, context)) : setAccessorType ? withNewScope(context, allAccessors.setAccessor, () ***REMOVED***> serializeExistingTypeNodeWithFallback(setAccessorType, context)) : void 0;
      const propertyType ***REMOVED*** foundType ?? inferAccessorType(
        accessor,
        allAccessors,
        context,
        /*symbol*/
        void 0
      );
      const propertySignature ***REMOVED*** factory.createPropertySignature(
        allAccessors.setAccessor ***REMOVED******REMOVED******REMOVED*** void 0 ? [factory.createModifier(148 /* ReadonlyKeyword */)] : [],
        reuseNode(context, name),
        /*questionToken*/
        void 0,
        propertyType
      );
      return propertySignature;
    }
  }
  function createUndefinedTypeNode() {
    if (strictNullChecks) {
      return factory.createKeywordTypeNode(157 /* UndefinedKeyword */);
    } else {
      return factory.createKeywordTypeNode(133 /* AnyKeyword */);
    }
  }
  function typeFromPrimitiveLiteral(node, baseType, context, preserveLiterals, requiresAddingUndefined) {
    let result;
    if (preserveLiterals) {
      if (node.kind ***REMOVED******REMOVED******REMOVED*** 225 /* PrefixUnaryExpression */ && node.operator ***REMOVED******REMOVED******REMOVED*** 40 /* PlusToken */) {
        result ***REMOVED*** factory.createLiteralTypeNode(reuseNode(context, node.operand));
      }
      result ***REMOVED*** factory.createLiteralTypeNode(reuseNode(context, node));
    } else {
      result ***REMOVED*** factory.createKeywordTypeNode(baseType);
    }
    return syntacticResult(addUndefinedIfNeeded(result, requiresAddingUndefined, node, context));
  }
  function addUndefinedIfNeeded(node, addUndefined, owner, context) {
    const parentDeclaration ***REMOVED*** owner && walkUpParenthesizedExpressions(owner).parent;
    const optionalDeclaration ***REMOVED*** parentDeclaration && isDeclaration(parentDeclaration) && isOptionalDeclaration(parentDeclaration);
    if (!strictNullChecks || !(addUndefined || optionalDeclaration)) return node;
    if (!canAddUndefined(node)) {
      context.tracker.reportInferenceFallback(node);
    }
    if (isUnionTypeNode(node)) {
      return factory.createUnionTypeNode([...node.types, factory.createKeywordTypeNode(157 /* UndefinedKeyword */)]);
    }
    return factory.createUnionTypeNode([node, factory.createKeywordTypeNode(157 /* UndefinedKeyword */)]);
  }
  function canAddUndefined(node) {
    if (!strictNullChecks) return true;
    if (isKeyword(node.kind) || node.kind ***REMOVED******REMOVED******REMOVED*** 202 /* LiteralType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 185 /* FunctionType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 186 /* ConstructorType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 189 /* ArrayType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 190 /* TupleType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 188 /* TypeLiteral */ || node.kind ***REMOVED******REMOVED******REMOVED*** 204 /* TemplateLiteralType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 198 /* ThisType */) {
      return true;
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 197 /* ParenthesizedType */) {
      return canAddUndefined(node.type);
    }
    if (node.kind ***REMOVED******REMOVED******REMOVED*** 193 /* UnionType */ || node.kind ***REMOVED******REMOVED******REMOVED*** 194 /* IntersectionType */) {
      return node.types.every(canAddUndefined);
    }
    return false;
  }
  function createReturnFromSignature(fn, symbol, context, reportFallback ***REMOVED*** true) {
    let returnType ***REMOVED*** failed;
    const returnTypeNode ***REMOVED*** isJSDocConstructSignature(fn) ? getEffectiveTypeAnnotationNode(fn.parameters[0]) : getEffectiveReturnTypeNode(fn);
    if (returnTypeNode) {
      returnType ***REMOVED*** syntacticResult(serializeTypeAnnotationOfDeclaration(returnTypeNode, context, fn, symbol));
    } else if (isValueSignatureDeclaration(fn)) {
      returnType ***REMOVED*** typeFromSingleReturnExpression(fn, context);
    }
    return returnType.type !***REMOVED******REMOVED*** void 0 ? returnType.type : inferReturnTypeOfSignatureSignature(fn, context, symbol, reportFallback && returnType.reportFallback && !returnTypeNode);
  }
  function typeFromSingleReturnExpression(declaration, context) {
    let candidateExpr;
    if (declaration && !nodeIsMissing(declaration.body)) {
      const flags ***REMOVED*** getFunctionFlags(declaration);
      if (flags & 3 /* AsyncGenerator */) return failed;
      const body ***REMOVED*** declaration.body;
      if (body && isBlock(body)) {
        forEachReturnStatement(body, (s) ***REMOVED***> {
          if (s.parent !***REMOVED******REMOVED*** body) {
            candidateExpr ***REMOVED*** void 0;
            return true;
          }
          if (!candidateExpr) {
            candidateExpr ***REMOVED*** s.expression;
          } else {
            candidateExpr ***REMOVED*** void 0;
            return true;
          }
        });
      } else {
        candidateExpr ***REMOVED*** body;
      }
    }
    if (candidateExpr) {
      if (isContextuallyTyped(candidateExpr)) {
        const type ***REMOVED*** isJSDocTypeAssertion(candidateExpr) ? getJSDocTypeAssertionType(candidateExpr) : isAsExpression(candidateExpr) || isTypeAssertionExpression(candidateExpr) ? candidateExpr.type : void 0;
        if (type && !isConstTypeReference(type)) {
          return syntacticResult(serializeExistingTypeNode(type, context));
        }
      } else {
        return typeFromExpression(candidateExpr, context);
      }
    }
    return failed;
  }
  function isContextuallyTyped(node) {
    return findAncestor(node.parent, (n) ***REMOVED***> {
      return isCallExpression(n) || !isFunctionLikeDeclaration(n) && !!getEffectiveTypeAnnotationNode(n) || isJsxElement(n) || isJsxExpression(n);
    });
  }
}

// src/tsc/tsc.ts
Debug.loggingHost ***REMOVED*** {
  log(_level, s) {
    sys.write(`${s || ""}${sys.newLine}`);
  }
};
if (Debug.isDebugging) {
  Debug.enableDebugInfo();
}
if (sys.tryEnableSourceMapsForHost && /^development$/i.test(sys.getEnvironmentVariable("NODE_ENV"))) {
  sys.tryEnableSourceMapsForHost();
}
if (sys.setBlocking) {
  sys.setBlocking();
}
executeCommandLine(sys, noop, sys.args);
//# sourceMappingURL***REMOVED***_tsc.js.map
