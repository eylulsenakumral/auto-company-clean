/*! *****************************************************************************
Copyright (c) Microsoft Corporation. All rights reserved.
Licensed under the Apache License, Version 2.0 (the "License"); you may not use
this file except in compliance with the License. You may obtain a copy of the
License at http://www.apache.org/licenses/LICENSE-2.0

THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
MERCHANTABLITY OR NON-INFRINGEMENT.

See the Apache Version 2.0 License for specific language governing permissions
and limitations under the License.
***************************************************************************** */


"use strict";
var __create ***REMOVED*** Object.create;
var __defProp ***REMOVED*** Object.defineProperty;
var __getOwnPropDesc ***REMOVED*** Object.getOwnPropertyDescriptor;
var __getOwnPropNames ***REMOVED*** Object.getOwnPropertyNames;
var __getProtoOf ***REMOVED*** Object.getPrototypeOf;
var __hasOwnProp ***REMOVED*** Object.prototype.hasOwnProperty;
var __copyProps ***REMOVED*** (to, from, except, desc) ***REMOVED***> {
  if (from && typeof from ***REMOVED******REMOVED******REMOVED*** "object" || typeof from ***REMOVED******REMOVED******REMOVED*** "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !***REMOVED******REMOVED*** except)
        __defProp(to, key, { get: () ***REMOVED***> from[key], enumerable: !(desc ***REMOVED*** __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __reExport ***REMOVED*** (target, mod, secondTarget) ***REMOVED***> (__copyProps(target, mod, "default"), secondTarget && __copyProps(secondTarget, mod, "default"));
var __toESM ***REMOVED*** (mod, isNodeMode, target) ***REMOVED***> (target ***REMOVED*** mod !***REMOVED*** null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/tsserver/server.ts
var import_os2 ***REMOVED*** __toESM(require("os"));

// src/typescript/typescript.ts
var typescript_exports ***REMOVED*** {};
__reExport(typescript_exports, require("./typescript.js"));

// src/tsserver/nodeServer.ts
var import_child_process ***REMOVED*** __toESM(require("child_process"));
var import_fs ***REMOVED*** __toESM(require("fs"));
var import_net ***REMOVED*** __toESM(require("net"));
var import_os ***REMOVED*** __toESM(require("os"));
var import_readline ***REMOVED*** __toESM(require("readline"));

// src/tsserver/common.ts
function getLogLevel(level) {
  if (level) {
    const l ***REMOVED*** level.toLowerCase();
    for (const name in typescript_exports.server.LogLevel) {
      if (isNaN(+name) && l ***REMOVED******REMOVED******REMOVED*** name.toLowerCase()) {
        return typescript_exports.server.LogLevel[name];
      }
    }
  }
  return void 0;
}

// src/tsserver/nodeServer.ts
function parseLoggingEnvironmentString(logEnvStr) {
  if (!logEnvStr) {
    return {};
  }
  const logEnv ***REMOVED*** { logToFile: true };
  const args ***REMOVED*** logEnvStr.split(" ");
  const len ***REMOVED*** args.length - 1;
  for (let i ***REMOVED*** 0; i < len; i +***REMOVED*** 2) {
    const option ***REMOVED*** args[i];
    const { value, extraPartCounter } ***REMOVED*** getEntireValue(i + 1);
    i +***REMOVED*** extraPartCounter;
    if (option && value) {
      switch (option) {
        case "-file":
          logEnv.file ***REMOVED*** value;
          break;
        case "-level":
          const level ***REMOVED*** getLogLevel(value);
          logEnv.detailLevel ***REMOVED*** level !***REMOVED******REMOVED*** void 0 ? level : typescript_exports.server.LogLevel.normal;
          break;
        case "-traceToConsole":
          logEnv.traceToConsole ***REMOVED*** value.toLowerCase() ***REMOVED******REMOVED******REMOVED*** "true";
          break;
        case "-logToFile":
          logEnv.logToFile ***REMOVED*** value.toLowerCase() ***REMOVED******REMOVED******REMOVED*** "true";
          break;
      }
    }
  }
  return logEnv;
  function getEntireValue(initialIndex) {
    let pathStart ***REMOVED*** args[initialIndex];
    let extraPartCounter ***REMOVED*** 0;
    if (pathStart.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.doubleQuote && pathStart.charCodeAt(pathStart.length - 1) !***REMOVED******REMOVED*** typescript_exports.CharacterCodes.doubleQuote) {
      for (let i ***REMOVED*** initialIndex + 1; i < args.length; i++) {
        pathStart +***REMOVED*** " ";
        pathStart +***REMOVED*** args[i];
        extraPartCounter++;
        if (pathStart.charCodeAt(pathStart.length - 1) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.doubleQuote) break;
      }
    }
    return { value: (0, typescript_exports.stripQuotes)(pathStart), extraPartCounter };
  }
}
function parseServerMode() {
  const mode ***REMOVED*** typescript_exports.server.findArgument("--serverMode");
  if (!mode) return void 0;
  switch (mode.toLowerCase()) {
    case "semantic":
      return typescript_exports.LanguageServiceMode.Semantic;
    case "partialsemantic":
      return typescript_exports.LanguageServiceMode.PartialSemantic;
    case "syntactic":
      return typescript_exports.LanguageServiceMode.Syntactic;
    default:
      return mode;
  }
}
function initializeNodeSystem() {
  const sys4 ***REMOVED*** typescript_exports.Debug.checkDefined(typescript_exports.sys);
  class Logger {
    constructor(logFilename, traceToConsole, level) {
      this.logFilename ***REMOVED*** logFilename;
      this.traceToConsole ***REMOVED*** traceToConsole;
      this.level ***REMOVED*** level;
      this.seq ***REMOVED*** 0;
      this.inGroup ***REMOVED*** false;
      this.firstInGroup ***REMOVED*** true;
      this.fd ***REMOVED*** -1;
      if (this.logFilename) {
        try {
          this.fd ***REMOVED*** import_fs.default.openSync(this.logFilename, "w");
        } catch {
        }
      }
    }
    static padStringRight(str, padding) {
      return (str + padding).slice(0, padding.length);
    }
    close() {
      if (this.fd >***REMOVED*** 0) {
        import_fs.default.close(this.fd, typescript_exports.noop);
      }
    }
    getLogFileName() {
      return this.logFilename;
    }
    perftrc(s) {
      this.msg(s, typescript_exports.server.Msg.Perf);
    }
    info(s) {
      this.msg(s, typescript_exports.server.Msg.Info);
    }
    err(s) {
      this.msg(s, typescript_exports.server.Msg.Err);
    }
    startGroup() {
      this.inGroup ***REMOVED*** true;
      this.firstInGroup ***REMOVED*** true;
    }
    endGroup() {
      this.inGroup ***REMOVED*** false;
    }
    loggingEnabled() {
      return !!this.logFilename || this.traceToConsole;
    }
    hasLevel(level) {
      return this.loggingEnabled() && this.level >***REMOVED*** level;
    }
    msg(s, type ***REMOVED*** typescript_exports.server.Msg.Err) {
      if (!this.canWrite()) return;
      s ***REMOVED*** `[${typescript_exports.server.nowString()}] ${s}
`;
      if (!this.inGroup || this.firstInGroup) {
        const prefix ***REMOVED*** Logger.padStringRight(type + " " + this.seq.toString(), "          ");
        s ***REMOVED*** prefix + s;
      }
      this.write(s, type);
      if (!this.inGroup) {
        this.seq++;
      }
    }
    canWrite() {
      return this.fd >***REMOVED*** 0 || this.traceToConsole;
    }
    write(s, _type) {
      if (this.fd >***REMOVED*** 0) {
        const buf ***REMOVED*** Buffer.from(s);
        import_fs.default.writeSync(
          this.fd,
          buf,
          0,
          buf.length,
          /*position*/
          null
        );
      }
      if (this.traceToConsole) {
        console.warn(s);
      }
    }
  }
  const libDirectory ***REMOVED*** (0, typescript_exports.getDirectoryPath)((0, typescript_exports.normalizePath)(sys4.getExecutingFilePath()));
  const useWatchGuard ***REMOVED*** process.platform ***REMOVED******REMOVED******REMOVED*** "win32";
  const originalWatchDirectory ***REMOVED*** sys4.watchDirectory.bind(sys4);
  const logger ***REMOVED*** createLogger();
  typescript_exports.Debug.loggingHost ***REMOVED*** {
    log(level, s) {
      switch (level) {
        case typescript_exports.LogLevel.Error:
        case typescript_exports.LogLevel.Warning:
          return logger.msg(s, typescript_exports.server.Msg.Err);
        case typescript_exports.LogLevel.Info:
        case typescript_exports.LogLevel.Verbose:
          return logger.msg(s, typescript_exports.server.Msg.Info);
      }
    }
  };
  const pending ***REMOVED*** (0, typescript_exports.createQueue)();
  let canWrite ***REMOVED*** true;
  if (useWatchGuard) {
    const currentDrive ***REMOVED*** extractWatchDirectoryCacheKey(
      sys4.resolvePath(sys4.getCurrentDirectory()),
      /*currentDriveKey*/
      void 0
    );
    const statusCache ***REMOVED*** /* @__PURE__ */ new Map();
    sys4.watchDirectory ***REMOVED*** (path, callback, recursive, options) ***REMOVED***> {
      const cacheKey ***REMOVED*** extractWatchDirectoryCacheKey(path, currentDrive);
      let status ***REMOVED*** cacheKey && statusCache.get(cacheKey);
      if (status ***REMOVED******REMOVED******REMOVED*** void 0) {
        if (logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
          logger.info(`${cacheKey} for path ${path} not found in cache...`);
        }
        try {
          const args ***REMOVED*** [(0, typescript_exports.combinePaths)(libDirectory, "watchGuard.js"), path];
          if (logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
            logger.info(`Starting ${process.execPath} with args:${typescript_exports.server.stringifyIndented(args)}`);
          }
          import_child_process.default.execFileSync(process.execPath, args, { stdio: "ignore", env: { ELECTRON_RUN_AS_NODE: "1" } });
          status ***REMOVED*** true;
          if (logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
            logger.info(`WatchGuard for path ${path} returned: OK`);
          }
        } catch (e) {
          status ***REMOVED*** false;
          if (logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
            logger.info(`WatchGuard for path ${path} returned: ${e.message}`);
          }
        }
        if (cacheKey) {
          statusCache.set(cacheKey, status);
        }
      } else if (logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
        logger.info(`watchDirectory for ${path} uses cached drive information.`);
      }
      if (status) {
        return watchDirectorySwallowingException(path, callback, recursive, options);
      } else {
        return typescript_exports.noopFileWatcher;
      }
    };
  } else {
    sys4.watchDirectory ***REMOVED*** watchDirectorySwallowingException;
  }
  sys4.write ***REMOVED*** (s) ***REMOVED***> writeMessage(Buffer.from(s, "utf8"));
  sys4.setTimeout ***REMOVED*** setTimeout;
  sys4.clearTimeout ***REMOVED*** clearTimeout;
  sys4.setImmediate ***REMOVED*** setImmediate;
  sys4.clearImmediate ***REMOVED*** clearImmediate;
  if (typeof global !***REMOVED******REMOVED*** "undefined" && global.gc) {
    sys4.gc ***REMOVED*** () ***REMOVED***> {
      var _a;
      return (_a ***REMOVED*** global.gc) ***REMOVED******REMOVED*** null ? void 0 : _a.call(global);
    };
  }
  const cancellationToken ***REMOVED*** createCancellationToken(sys4.args);
  const localeStr ***REMOVED*** typescript_exports.server.findArgument("--locale");
  if (localeStr) {
    (0, typescript_exports.validateLocaleAndSetLanguage)(localeStr, sys4);
  }
  const modeOrUnknown ***REMOVED*** parseServerMode();
  let serverMode;
  let unknownServerMode;
  if (modeOrUnknown !***REMOVED******REMOVED*** void 0) {
    if (typeof modeOrUnknown ***REMOVED******REMOVED******REMOVED*** "number") serverMode ***REMOVED*** modeOrUnknown;
    else unknownServerMode ***REMOVED*** modeOrUnknown;
  }
  return {
    args: process.argv,
    logger,
    cancellationToken,
    serverMode,
    unknownServerMode,
    startSession: startNodeSession
  };
  function createLogger() {
    const cmdLineLogFileName ***REMOVED*** typescript_exports.server.findArgument("--logFile");
    const cmdLineVerbosity ***REMOVED*** getLogLevel(typescript_exports.server.findArgument("--logVerbosity"));
    const envLogOptions ***REMOVED*** parseLoggingEnvironmentString(process.env.TSS_LOG);
    const unsubstitutedLogFileName ***REMOVED*** cmdLineLogFileName ? (0, typescript_exports.stripQuotes)(cmdLineLogFileName) : envLogOptions.logToFile ? envLogOptions.file || libDirectory + "/.log" + process.pid.toString() : void 0;
    const substitutedLogFileName ***REMOVED*** unsubstitutedLogFileName ? unsubstitutedLogFileName.replace("PID", process.pid.toString()) : void 0;
    const logVerbosity ***REMOVED*** cmdLineVerbosity || envLogOptions.detailLevel;
    return new Logger(substitutedLogFileName, envLogOptions.traceToConsole, logVerbosity);
  }
  function writeMessage(buf) {
    if (!canWrite) {
      pending.enqueue(buf);
    } else {
      canWrite ***REMOVED*** false;
      process.stdout.write(buf, setCanWriteFlagAndWriteMessageIfNecessary);
    }
  }
  function setCanWriteFlagAndWriteMessageIfNecessary() {
    canWrite ***REMOVED*** true;
    if (!pending.isEmpty()) {
      writeMessage(pending.dequeue());
    }
  }
  function extractWatchDirectoryCacheKey(path, currentDriveKey) {
    path ***REMOVED*** (0, typescript_exports.normalizeSlashes)(path);
    if (isUNCPath(path)) {
      const firstSlash ***REMOVED*** path.indexOf(typescript_exports.directorySeparator, 2);
      return firstSlash !***REMOVED******REMOVED*** -1 ? (0, typescript_exports.toFileNameLowerCase)(path.substring(0, firstSlash)) : path;
    }
    const rootLength ***REMOVED*** (0, typescript_exports.getRootLength)(path);
    if (rootLength ***REMOVED******REMOVED******REMOVED*** 0) {
      return currentDriveKey;
    }
    if (path.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.colon && path.charCodeAt(2) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.slash) {
      return (0, typescript_exports.toFileNameLowerCase)(path.charAt(0));
    }
    if (path.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.slash && path.charCodeAt(1) !***REMOVED******REMOVED*** typescript_exports.CharacterCodes.slash) {
      return currentDriveKey;
    }
    return void 0;
  }
  function isUNCPath(s) {
    return s.length > 2 && s.charCodeAt(0) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.slash && s.charCodeAt(1) ***REMOVED******REMOVED******REMOVED*** typescript_exports.CharacterCodes.slash;
  }
  function watchDirectorySwallowingException(path, callback, recursive, options) {
    try {
      return originalWatchDirectory(path, callback, recursive, options);
    } catch (e) {
      logger.info(`Exception when creating directory watcher: ${e.message}`);
      return typescript_exports.noopFileWatcher;
    }
  }
}
function parseEventPort(eventPortStr) {
  const eventPort ***REMOVED*** eventPortStr ***REMOVED******REMOVED******REMOVED*** void 0 ? void 0 : parseInt(eventPortStr);
  return eventPort !***REMOVED******REMOVED*** void 0 && !isNaN(eventPort) ? eventPort : void 0;
}
function startNodeSession(options, logger, cancellationToken) {
  const rl ***REMOVED*** import_readline.default.createInterface({
    input: process.stdin,
    output: process.stdout,
    terminal: false
  });
  const _NodeTypingsInstallerAdapter ***REMOVED*** class _NodeTypingsInstallerAdapter extends typescript_exports.server.TypingsInstallerAdapter {
    constructor(telemetryEnabled2, logger2, host, globalTypingsCacheLocation, typingSafeListLocation2, typesMapLocation2, npmLocation2, validateDefaultNpmLocation2, event) {
      super(
        telemetryEnabled2,
        logger2,
        host,
        globalTypingsCacheLocation,
        event,
        _NodeTypingsInstallerAdapter.maxActiveRequestCount
      );
      this.typingSafeListLocation ***REMOVED*** typingSafeListLocation2;
      this.typesMapLocation ***REMOVED*** typesMapLocation2;
      this.npmLocation ***REMOVED*** npmLocation2;
      this.validateDefaultNpmLocation ***REMOVED*** validateDefaultNpmLocation2;
    }
    createInstallerProcess() {
      if (this.logger.hasLevel(typescript_exports.server.LogLevel.requestTime)) {
        this.logger.info("Binding...");
      }
      const args ***REMOVED*** [typescript_exports.server.Arguments.GlobalCacheLocation, this.globalTypingsCacheLocation];
      if (this.telemetryEnabled) {
        args.push(typescript_exports.server.Arguments.EnableTelemetry);
      }
      if (this.logger.loggingEnabled() && this.logger.getLogFileName()) {
        args.push(typescript_exports.server.Arguments.LogFile, (0, typescript_exports.combinePaths)((0, typescript_exports.getDirectoryPath)((0, typescript_exports.normalizeSlashes)(this.logger.getLogFileName())), `ti-${process.pid}.log`));
      }
      if (this.typingSafeListLocation) {
        args.push(typescript_exports.server.Arguments.TypingSafeListLocation, this.typingSafeListLocation);
      }
      if (this.typesMapLocation) {
        args.push(typescript_exports.server.Arguments.TypesMapLocation, this.typesMapLocation);
      }
      if (this.npmLocation) {
        args.push(typescript_exports.server.Arguments.NpmLocation, this.npmLocation);
      }
      if (this.validateDefaultNpmLocation) {
        args.push(typescript_exports.server.Arguments.ValidateDefaultNpmLocation);
      }
      const execArgv ***REMOVED*** [];
      for (const arg of process.execArgv) {
        const match ***REMOVED*** /^--((?:debug|inspect)(?:-brk)?)(?:***REMOVED***(\d+))?$/.exec(arg);
        if (match) {
          const currentPort ***REMOVED*** match[2] !***REMOVED******REMOVED*** void 0 ? +match[2] : match[1].charAt(0) ***REMOVED******REMOVED******REMOVED*** "d" ? 5858 : 9229;
          execArgv.push(`--${match[1]}***REMOVED***${currentPort + 1}`);
          break;
        }
      }
      const typingsInstaller ***REMOVED*** (0, typescript_exports.combinePaths)((0, typescript_exports.getDirectoryPath)(typescript_exports.sys.getExecutingFilePath()), "typingsInstaller.js");
      this.installer ***REMOVED*** import_child_process.default.fork(typingsInstaller, args, { execArgv });
      this.installer.on("message", (m) ***REMOVED***> this.handleMessage(m));
      this.host.setImmediate(() ***REMOVED***> this.event({ pid: this.installer.pid }, "typingsInstallerPid"));
      process.on("exit", () ***REMOVED***> {
        this.installer.kill();
      });
      return this.installer;
    }
  };
  // This number is essentially arbitrary.  Processing more than one typings request
  // at a time makes sense, but having too many in the pipe results in a hang
  // (see https://github.com/nodejs/node/issues/7657).
  // It would be preferable to base our limit on the amount of space left in the
  // buffer, but we have yet to find a way to retrieve that value.
  _NodeTypingsInstallerAdapter.maxActiveRequestCount ***REMOVED*** 10;
  let NodeTypingsInstallerAdapter ***REMOVED*** _NodeTypingsInstallerAdapter;
  class IOSession extends typescript_exports.server.Session {
    constructor() {
      const event ***REMOVED*** (body, eventName) ***REMOVED***> {
        this.event(body, eventName);
      };
      const host ***REMOVED*** typescript_exports.sys;
      const typingsInstaller ***REMOVED*** disableAutomaticTypingAcquisition ? void 0 : new NodeTypingsInstallerAdapter(telemetryEnabled, logger, host, getGlobalTypingsCacheLocation(), typingSafeListLocation, typesMapLocation, npmLocation, validateDefaultNpmLocation, event);
      super({
        host,
        cancellationToken,
        ...options,
        typingsInstaller,
        byteLength: Buffer.byteLength,
        hrtime: process.hrtime,
        logger,
        canUseEvents: true,
        typesMapLocation
      });
      this.eventPort ***REMOVED*** eventPort;
      if (this.canUseEvents && this.eventPort) {
        const s ***REMOVED*** import_net.default.connect({ port: this.eventPort }, () ***REMOVED***> {
          this.eventSocket ***REMOVED*** s;
          if (this.socketEventQueue) {
            for (const event2 of this.socketEventQueue) {
              this.writeToEventSocket(event2.body, event2.eventName);
            }
            this.socketEventQueue ***REMOVED*** void 0;
          }
        });
      }
      this.constructed ***REMOVED*** true;
    }
    event(body, eventName) {
      typescript_exports.Debug.assert(!!this.constructed, "Should only call `IOSession.prototype.event` on an initialized IOSession");
      if (this.canUseEvents && this.eventPort) {
        if (!this.eventSocket) {
          if (this.logger.hasLevel(typescript_exports.server.LogLevel.verbose)) {
            this.logger.info(`eventPort: event "${eventName}" queued, but socket not yet initialized`);
          }
          (this.socketEventQueue || (this.socketEventQueue ***REMOVED*** [])).push({ body, eventName });
          return;
        } else {
          typescript_exports.Debug.assert(this.socketEventQueue ***REMOVED******REMOVED******REMOVED*** void 0);
          this.writeToEventSocket(body, eventName);
        }
      } else {
        super.event(body, eventName);
      }
    }
    writeToEventSocket(body, eventName) {
      this.eventSocket.write(typescript_exports.server.formatMessage(typescript_exports.server.toEvent(eventName, body), this.logger, this.byteLength, this.host.newLine), "utf8");
    }
    exit() {
      var _a;
      this.logger.info("Exiting...");
      this.projectService.closeLog();
      (_a ***REMOVED*** typescript_exports.tracing) ***REMOVED******REMOVED*** null ? void 0 : _a.stopTracing();
      process.exit(0);
    }
    listen() {
      rl.on("line", (input) ***REMOVED***> {
        const message ***REMOVED*** input.trim();
        this.onMessage(message);
      });
      rl.on("close", () ***REMOVED***> {
        this.exit();
      });
    }
  }
  class IpcIOSession extends IOSession {
    writeMessage(msg) {
      const verboseLogging ***REMOVED*** logger.hasLevel(typescript_exports.server.LogLevel.verbose);
      if (verboseLogging) {
        const json ***REMOVED*** JSON.stringify(msg);
        logger.info(`${msg.type}:${typescript_exports.server.indent(json)}`);
      }
      process.send(msg);
    }
    parseMessage(message) {
      return message;
    }
    toStringMessage(message) {
      return JSON.stringify(message, void 0, 2);
    }
    listen() {
      process.on("message", (e) ***REMOVED***> {
        this.onMessage(e);
      });
      process.on("disconnect", () ***REMOVED***> {
        this.exit();
      });
    }
  }
  const eventPort ***REMOVED*** parseEventPort(typescript_exports.server.findArgument("--eventPort"));
  const typingSafeListLocation ***REMOVED*** typescript_exports.server.findArgument(typescript_exports.server.Arguments.TypingSafeListLocation);
  const typesMapLocation ***REMOVED*** typescript_exports.server.findArgument(typescript_exports.server.Arguments.TypesMapLocation) || (0, typescript_exports.combinePaths)((0, typescript_exports.getDirectoryPath)(typescript_exports.sys.getExecutingFilePath()), "typesMap.json");
  const npmLocation ***REMOVED*** typescript_exports.server.findArgument(typescript_exports.server.Arguments.NpmLocation);
  const validateDefaultNpmLocation ***REMOVED*** typescript_exports.server.hasArgument(typescript_exports.server.Arguments.ValidateDefaultNpmLocation);
  const disableAutomaticTypingAcquisition ***REMOVED*** typescript_exports.server.hasArgument("--disableAutomaticTypingAcquisition");
  const useNodeIpc ***REMOVED*** typescript_exports.server.hasArgument("--useNodeIpc");
  const telemetryEnabled ***REMOVED*** typescript_exports.server.hasArgument(typescript_exports.server.Arguments.EnableTelemetry);
  const commandLineTraceDir ***REMOVED*** typescript_exports.server.findArgument("--traceDirectory");
  const traceDir ***REMOVED*** commandLineTraceDir ? (0, typescript_exports.stripQuotes)(commandLineTraceDir) : process.env.TSS_TRACE;
  if (traceDir) {
    (0, typescript_exports.startTracing)("server", traceDir);
  }
  const ioSession ***REMOVED*** useNodeIpc ? new IpcIOSession() : new IOSession();
  process.on("uncaughtException", (err) ***REMOVED***> {
    ioSession.logError(err, "unknown");
  });
  process.noAsar ***REMOVED*** true;
  ioSession.listen();
  function getGlobalTypingsCacheLocation() {
    switch (process.platform) {
      case "win32": {
        const basePath ***REMOVED*** process.env.LOCALAPPDATA || process.env.APPDATA || import_os.default.homedir && import_os.default.homedir() || process.env.USERPROFILE || process.env.HOMEDRIVE && process.env.HOMEPATH && (0, typescript_exports.normalizeSlashes)(process.env.HOMEDRIVE + process.env.HOMEPATH) || import_os.default.tmpdir();
        return (0, typescript_exports.combinePaths)((0, typescript_exports.combinePaths)((0, typescript_exports.normalizeSlashes)(basePath), "Microsoft/TypeScript"), typescript_exports.versionMajorMinor);
      }
      case "openbsd":
      case "freebsd":
      case "netbsd":
      case "darwin":
      case "linux":
      case "android": {
        const cacheLocation ***REMOVED*** getNonWindowsCacheLocation(process.platform ***REMOVED******REMOVED******REMOVED*** "darwin");
        return (0, typescript_exports.combinePaths)((0, typescript_exports.combinePaths)(cacheLocation, "typescript"), typescript_exports.versionMajorMinor);
      }
      default:
        return typescript_exports.Debug.fail(`unsupported platform '${process.platform}'`);
    }
  }
  function getNonWindowsCacheLocation(platformIsDarwin) {
    if (process.env.XDG_CACHE_HOME) {
      return process.env.XDG_CACHE_HOME;
    }
    const usersDir ***REMOVED*** platformIsDarwin ? "Users" : "home";
    const homePath ***REMOVED*** import_os.default.homedir && import_os.default.homedir() || process.env.HOME || (process.env.LOGNAME || process.env.USER) && `/${usersDir}/${process.env.LOGNAME || process.env.USER}` || import_os.default.tmpdir();
    const cacheFolder ***REMOVED*** platformIsDarwin ? "Library/Caches" : ".cache";
    return (0, typescript_exports.combinePaths)((0, typescript_exports.normalizeSlashes)(homePath), cacheFolder);
  }
}
function pipeExists(name) {
  return import_fs.default.existsSync(name);
}
function createCancellationToken(args) {
  let cancellationPipeName;
  for (let i ***REMOVED*** 0; i < args.length - 1; i++) {
    if (args[i] ***REMOVED******REMOVED******REMOVED*** "--cancellationPipeName") {
      cancellationPipeName ***REMOVED*** args[i + 1];
      break;
    }
  }
  if (!cancellationPipeName) {
    return typescript_exports.server.nullCancellationToken;
  }
  if (cancellationPipeName.charAt(cancellationPipeName.length - 1) ***REMOVED******REMOVED******REMOVED*** "*") {
    const namePrefix ***REMOVED*** cancellationPipeName.slice(0, -1);
    if (namePrefix.length ***REMOVED******REMOVED******REMOVED*** 0 || namePrefix.includes("*")) {
      throw new Error("Invalid name for template cancellation pipe: it should have length greater than 2 characters and contain only one '*'.");
    }
    let perRequestPipeName;
    let currentRequestId;
    return {
      isCancellationRequested: () ***REMOVED***> perRequestPipeName !***REMOVED******REMOVED*** void 0 && pipeExists(perRequestPipeName),
      setRequest(requestId) {
        currentRequestId ***REMOVED*** requestId;
        perRequestPipeName ***REMOVED*** namePrefix + requestId;
      },
      resetRequest(requestId) {
        if (currentRequestId !***REMOVED******REMOVED*** requestId) {
          throw new Error(`Mismatched request id, expected ${currentRequestId}, actual ${requestId}`);
        }
        perRequestPipeName ***REMOVED*** void 0;
      }
    };
  } else {
    return {
      isCancellationRequested: () ***REMOVED***> pipeExists(cancellationPipeName),
      setRequest: (_requestId) ***REMOVED***> void 0,
      resetRequest: (_requestId) ***REMOVED***> void 0
    };
  }
}

// src/tsserver/server.ts
function findArgumentStringArray(argName) {
  const arg ***REMOVED*** typescript_exports.server.findArgument(argName);
  if (arg ***REMOVED******REMOVED******REMOVED*** void 0) {
    return typescript_exports.emptyArray;
  }
  return arg.split(",").filter((name) ***REMOVED***> name !***REMOVED******REMOVED*** "");
}
function start({ args, logger, cancellationToken, serverMode, unknownServerMode, startSession: startServer }, platform) {
  logger.info(`Starting TS Server`);
  logger.info(`Version: ${typescript_exports.version}`);
  logger.info(`Arguments: ${args.join(" ")}`);
  logger.info(`Platform: ${platform} NodeVersion: ${process.version} CaseSensitive: ${typescript_exports.sys.useCaseSensitiveFileNames}`);
  logger.info(`ServerMode: ${serverMode} hasUnknownServerMode: ${unknownServerMode}`);
  typescript_exports.setStackTraceLimit();
  if (typescript_exports.Debug.isDebugging) {
    typescript_exports.Debug.enableDebugInfo();
  }
  if (typescript_exports.sys.tryEnableSourceMapsForHost && /^development$/i.test(typescript_exports.sys.getEnvironmentVariable("NODE_ENV"))) {
    typescript_exports.sys.tryEnableSourceMapsForHost();
  }
  console.log ***REMOVED*** (...args2) ***REMOVED***> logger.msg(args2.length ***REMOVED******REMOVED******REMOVED*** 1 ? args2[0] : args2.join(", "), typescript_exports.server.Msg.Info);
  console.warn ***REMOVED*** (...args2) ***REMOVED***> logger.msg(args2.length ***REMOVED******REMOVED******REMOVED*** 1 ? args2[0] : args2.join(", "), typescript_exports.server.Msg.Err);
  console.error ***REMOVED*** (...args2) ***REMOVED***> logger.msg(args2.length ***REMOVED******REMOVED******REMOVED*** 1 ? args2[0] : args2.join(", "), typescript_exports.server.Msg.Err);
  startServer(
    {
      globalPlugins: findArgumentStringArray("--globalPlugins"),
      pluginProbeLocations: findArgumentStringArray("--pluginProbeLocations"),
      allowLocalPluginLoads: typescript_exports.server.hasArgument("--allowLocalPluginLoads"),
      useSingleInferredProject: typescript_exports.server.hasArgument("--useSingleInferredProject"),
      useInferredProjectPerProjectRoot: typescript_exports.server.hasArgument("--useInferredProjectPerProjectRoot"),
      suppressDiagnosticEvents: typescript_exports.server.hasArgument("--suppressDiagnosticEvents"),
      noGetErrOnBackgroundUpdate: typescript_exports.server.hasArgument("--noGetErrOnBackgroundUpdate"),
      canUseWatchEvents: typescript_exports.server.hasArgument("--canUseWatchEvents"),
      serverMode
    },
    logger,
    cancellationToken
  );
}
typescript_exports.setStackTraceLimit();
start(initializeNodeSystem(), import_os2.default.platform());
//# sourceMappingURL***REMOVED***_tsserver.js.map
