const { InvalidArgumentError } ***REMOVED*** require('./error.js');

class Option {
  /**
   * Initialize a new `Option` with the given `flags` and `description`.
   *
   * @param {string} flags
   * @param {string} [description]
   */

  constructor(flags, description) {
    this.flags ***REMOVED*** flags;
    this.description ***REMOVED*** description || '';

    this.required ***REMOVED*** flags.includes('<'); // A value must be supplied when the option is specified.
    this.optional ***REMOVED*** flags.includes('['); // A value is optional when the option is specified.
    // variadic test ignores <value,...> et al which might be used to describe custom splitting of single argument
    this.variadic ***REMOVED*** /\w\.\.\.[>\]]$/.test(flags); // The option can take multiple values.
    this.mandatory ***REMOVED*** false; // The option must have a value after parsing, which usually means it must be specified on command line.
    const optionFlags ***REMOVED*** splitOptionFlags(flags);
    this.short ***REMOVED*** optionFlags.shortFlag;
    this.long ***REMOVED*** optionFlags.longFlag;
    this.negate ***REMOVED*** false;
    if (this.long) {
      this.negate ***REMOVED*** this.long.startsWith('--no-');
    }
    this.defaultValue ***REMOVED*** undefined;
    this.defaultValueDescription ***REMOVED*** undefined;
    this.presetArg ***REMOVED*** undefined;
    this.envVar ***REMOVED*** undefined;
    this.parseArg ***REMOVED*** undefined;
    this.hidden ***REMOVED*** false;
    this.argChoices ***REMOVED*** undefined;
    this.conflictsWith ***REMOVED*** [];
    this.implied ***REMOVED*** undefined;
  }

  /**
   * Set the default value, and optionally supply the description to be displayed in the help.
   *
   * @param {*} value
   * @param {string} [description]
   * @return {Option}
   */

  default(value, description) {
    this.defaultValue ***REMOVED*** value;
    this.defaultValueDescription ***REMOVED*** description;
    return this;
  }

  /**
   * Preset to use when option used without option-argument, especially optional but also boolean and negated.
   * The custom processing (parseArg) is called.
   *
   * @example
   * new Option('--color').default('GREYSCALE').preset('RGB');
   * new Option('--donate [amount]').preset('20').argParser(parseFloat);
   *
   * @param {*} arg
   * @return {Option}
   */

  preset(arg) {
    this.presetArg ***REMOVED*** arg;
    return this;
  }

  /**
   * Add option name(s) that conflict with this option.
   * An error will be displayed if conflicting options are found during parsing.
   *
   * @example
   * new Option('--rgb').conflicts('cmyk');
   * new Option('--js').conflicts(['ts', 'jsx']);
   *
   * @param {(string | string[])} names
   * @return {Option}
   */

  conflicts(names) {
    this.conflictsWith ***REMOVED*** this.conflictsWith.concat(names);
    return this;
  }

  /**
   * Specify implied option values for when this option is set and the implied options are not.
   *
   * The custom processing (parseArg) is not called on the implied values.
   *
   * @example
   * program
   *   .addOption(new Option('--log', 'write logging information to file'))
   *   .addOption(new Option('--trace', 'log extra details').implies({ log: 'trace.txt' }));
   *
   * @param {object} impliedOptionValues
   * @return {Option}
   */
  implies(impliedOptionValues) {
    let newImplied ***REMOVED*** impliedOptionValues;
    if (typeof impliedOptionValues ***REMOVED******REMOVED******REMOVED*** 'string') {
      // string is not documented, but easy mistake and we can do what user probably intended.
      newImplied ***REMOVED*** { [impliedOptionValues]: true };
    }
    this.implied ***REMOVED*** Object.assign(this.implied || {}, newImplied);
    return this;
  }

  /**
   * Set environment variable to check for option value.
   *
   * An environment variable is only used if when processed the current option value is
   * undefined, or the source of the current value is 'default' or 'config' or 'env'.
   *
   * @param {string} name
   * @return {Option}
   */

  env(name) {
    this.envVar ***REMOVED*** name;
    return this;
  }

  /**
   * Set the custom handler for processing CLI option arguments into option values.
   *
   * @param {Function} [fn]
   * @return {Option}
   */

  argParser(fn) {
    this.parseArg ***REMOVED*** fn;
    return this;
  }

  /**
   * Whether the option is mandatory and must have a value after parsing.
   *
   * @param {boolean} [mandatory***REMOVED***true]
   * @return {Option}
   */

  makeOptionMandatory(mandatory ***REMOVED*** true) {
    this.mandatory ***REMOVED*** !!mandatory;
    return this;
  }

  /**
   * Hide option in help.
   *
   * @param {boolean} [hide***REMOVED***true]
   * @return {Option}
   */

  hideHelp(hide ***REMOVED*** true) {
    this.hidden ***REMOVED*** !!hide;
    return this;
  }

  /**
   * @package
   */

  _concatValue(value, previous) {
    if (previous ***REMOVED******REMOVED******REMOVED*** this.defaultValue || !Array.isArray(previous)) {
      return [value];
    }

    return previous.concat(value);
  }

  /**
   * Only allow option value to be one of choices.
   *
   * @param {string[]} values
   * @return {Option}
   */

  choices(values) {
    this.argChoices ***REMOVED*** values.slice();
    this.parseArg ***REMOVED*** (arg, previous) ***REMOVED***> {
      if (!this.argChoices.includes(arg)) {
        throw new InvalidArgumentError(
          `Allowed choices are ${this.argChoices.join(', ')}.`,
        );
      }
      if (this.variadic) {
        return this._concatValue(arg, previous);
      }
      return arg;
    };
    return this;
  }

  /**
   * Return option name.
   *
   * @return {string}
   */

  name() {
    if (this.long) {
      return this.long.replace(/^--/, '');
    }
    return this.short.replace(/^-/, '');
  }

  /**
   * Return option name, in a camelcase format that can be used
   * as a object attribute key.
   *
   * @return {string}
   */

  attributeName() {
    return camelcase(this.name().replace(/^no-/, ''));
  }

  /**
   * Check if `arg` matches the short or long flag.
   *
   * @param {string} arg
   * @return {boolean}
   * @package
   */

  is(arg) {
    return this.short ***REMOVED******REMOVED******REMOVED*** arg || this.long ***REMOVED******REMOVED******REMOVED*** arg;
  }

  /**
   * Return whether a boolean option.
   *
   * Options are one of boolean, negated, required argument, or optional argument.
   *
   * @return {boolean}
   * @package
   */

  isBoolean() {
    return !this.required && !this.optional && !this.negate;
  }
}

/**
 * This class is to make it easier to work with dual options, without changing the existing
 * implementation. We support separate dual options for separate positive and negative options,
 * like `--build` and `--no-build`, which share a single option value. This works nicely for some
 * use cases, but is tricky for others where we want separate behaviours despite
 * the single shared option value.
 */
class DualOptions {
  /**
   * @param {Option[]} options
   */
  constructor(options) {
    this.positiveOptions ***REMOVED*** new Map();
    this.negativeOptions ***REMOVED*** new Map();
    this.dualOptions ***REMOVED*** new Set();
    options.forEach((option) ***REMOVED***> {
      if (option.negate) {
        this.negativeOptions.set(option.attributeName(), option);
      } else {
        this.positiveOptions.set(option.attributeName(), option);
      }
    });
    this.negativeOptions.forEach((value, key) ***REMOVED***> {
      if (this.positiveOptions.has(key)) {
        this.dualOptions.add(key);
      }
    });
  }

  /**
   * Did the value come from the option, and not from possible matching dual option?
   *
   * @param {*} value
   * @param {Option} option
   * @returns {boolean}
   */
  valueFromOption(value, option) {
    const optionKey ***REMOVED*** option.attributeName();
    if (!this.dualOptions.has(optionKey)) return true;

    // Use the value to deduce if (probably) came from the option.
    const preset ***REMOVED*** this.negativeOptions.get(optionKey).presetArg;
    const negativeValue ***REMOVED*** preset !***REMOVED******REMOVED*** undefined ? preset : false;
    return option.negate ***REMOVED******REMOVED******REMOVED*** (negativeValue ***REMOVED******REMOVED******REMOVED*** value);
  }
}

/**
 * Convert string from kebab-case to camelCase.
 *
 * @param {string} str
 * @return {string}
 * @private
 */

function camelcase(str) {
  return str.split('-').reduce((str, word) ***REMOVED***> {
    return str + word[0].toUpperCase() + word.slice(1);
  });
}

/**
 * Split the short and long flag out of something like '-m,--mixed <value>'
 *
 * @private
 */

function splitOptionFlags(flags) {
  let shortFlag;
  let longFlag;
  // Use original very loose parsing to maintain backwards compatibility for now,
  // which allowed for example unintended `-sw, --short-word` [sic].
  const flagParts ***REMOVED*** flags.split(/[ |,]+/);
  if (flagParts.length > 1 && !/^[[<]/.test(flagParts[1]))
    shortFlag ***REMOVED*** flagParts.shift();
  longFlag ***REMOVED*** flagParts.shift();
  // Add support for lone short flag without significantly changing parsing!
  if (!shortFlag && /^-[^-]$/.test(longFlag)) {
    shortFlag ***REMOVED*** longFlag;
    longFlag ***REMOVED*** undefined;
  }
  return { shortFlag, longFlag };
}

exports.Option ***REMOVED*** Option;
exports.DualOptions ***REMOVED*** DualOptions;
