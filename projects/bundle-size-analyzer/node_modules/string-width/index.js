'use strict';
const stripAnsi ***REMOVED*** require('strip-ansi');
const isFullwidthCodePoint ***REMOVED*** require('is-fullwidth-code-point');
const emojiRegex ***REMOVED*** require('emoji-regex');

const stringWidth ***REMOVED*** string ***REMOVED***> {
	if (typeof string !***REMOVED******REMOVED*** 'string' || string.length ***REMOVED******REMOVED******REMOVED*** 0) {
		return 0;
	}

	string ***REMOVED*** stripAnsi(string);

	if (string.length ***REMOVED******REMOVED******REMOVED*** 0) {
		return 0;
	}

	string ***REMOVED*** string.replace(emojiRegex(), '  ');

	let width ***REMOVED*** 0;

	for (let i ***REMOVED*** 0; i < string.length; i++) {
		const code ***REMOVED*** string.codePointAt(i);

		// Ignore control characters
		if (code <***REMOVED*** 0x1F || (code >***REMOVED*** 0x7F && code <***REMOVED*** 0x9F)) {
			continue;
		}

		// Ignore combining characters
		if (code >***REMOVED*** 0x300 && code <***REMOVED*** 0x36F) {
			continue;
		}

		// Surrogates
		if (code > 0xFFFF) {
			i++;
		}

		width +***REMOVED*** isFullwidthCodePoint(code) ? 2 : 1;
	}

	return width;
};

module.exports ***REMOVED*** stringWidth;
// TODO: remove this in the next major version
module.exports.default ***REMOVED*** stringWidth;
